# D20 full-objective closure audit — what the corpus can and cannot establish

**Status:** final after mathematics, physics and independent full-objective and
synthesis-manifest audits, 2026-07-12.

## Provenance labels

```text
EXACT       dependency-free finite executable theorem/receipt;
SYNTHESIS   inference obtained by combining exact corpus results;
LITERATURE  established external physics or current research status;
PROTOCOL    claim/holdout discipline, not a machine access lock.
```

## Objective under audit

```text
Derive—or uniquely select through independent physical evidence—the simplest
local, generally covariant amplitude/action law for interacting sealed
diamonds that requires no global clock, generates the complete non-Markov
whole-history measure and durable records, recovers quantum correlations and
relativistic causal structure, fixes or explains its couplings and physical
scales, and makes new quantitative predictions for influence propagation,
round cones, 3+1 spacetime and gravity that survive hostile review and
untouched empirical holdouts.
```

## Clause-by-clause verdict

| objective clause | strongest proved result | status |
|---|---|---|
| simplest | D12 audits MDL/simplicity only after a description language and prior are supplied | no representation-invariant complexity or physical prior selects one law |
| local generally covariant action | finite relabeling-invariant interval-action families exist; general-boundary amplitudes are the closest established regional-sewing architecture in the D18 literature audit; observed low-energy baseline is SM plus Einstein-Hilbert EFT | no unique fundamental action selected |
| no global clock | regional sewing and whole-history conditionals need no universal commit race; D17 quotients compatible element labels within one supplied filtration | full construction-filtration gauge and continuum refoliation equivalence open |
| complete non-Markov law | on a finite/cylinder domain, a primitive projective history law or `(E,D)` supplies all positive event conditionals | supplying it is the missing law by stipulation; durable-record semantics, physical derivation and quantum sigma extension remain additional/open |
| durable records | D14 and integrated D17 give exact owner-local commits, protected futures, memory and reset | finite conditional instruments supplied; universal record emergence open |
| quantum correlations | D13-D18 give exact interference, Bell, decoherence and Born-once witnesses | compatibility proved; nature's complete quantum-gravity functional unselected |
| relativistic causal structure | D10 gives the exact Bloch/celestial `S^2` kinematic bridge; D11 is a broader incomplete packet; D16 gives finite causal-order covariance; EFT4 uses observed Lorentzian 3+1 spacetime | 3+1 Lorentzian emergence from records not derived |
| couplings and scales | D15 locates coefficients and unit dictionary; `G` calibrates Planck units once `hbar,c` are supplied | values mostly measured/fitted; no SHARD derivation of `G`, `Lambda`, masses or mixings |
| new cone/dimension/gravity predictions | V9 is historical `PARKED-AT-PROTOCOL` evidence; at the strict exact disposition `m4` passed, `dom` failed, their conjunction failed and dimension was excluded | no selected generator fixes cross-candidate predictions; no untouched holdout exists yet |

The D14-D19 finite claims and D8 strict two-leg numerical disposition are
`EXACT` corpus results; the combined clause verdicts are `SYNTHESIS`; the
SM/Einstein-Hilbert and live UV-candidate status statements are `LITERATURE`;
refusing to reuse V9 as a holdout is `PROTOCOL`.

## Results that are genuinely closed

1. **Construction order is not automatically physical time.**  A whole-history
   law can be sampled in an arbitrary bookkeeping order.  Observable
   filtration/refoliation equivalence still requires proof.
2. **Sealed-record principles do not create support.**  No-silent-center,
   diamonds, holonomy and profinite completion constrain or compose supplied
   histories; they do not select which new record arises.
3. **An action is not the complete rulebook.**  D17's hostile-reviewed exact
   packet holds the causal action fixed while positive boundary/orbit kernels
   produce distinct integrated local projective record laws.
4. **The operational next-click answer is a conditional, not a second law.**
   Given a full classical record law, or a supplied decoherent sector of
   `(E,D)` plus record semantics and its protected future algebra, the
   next-record probability is ordinary disintegration.  Bare `(E,D)` gives
   event probabilities; it does not identify which events are durable records.
5. **Primitive `D` does not derive itself.**  D18 proves finite sufficiency,
   while explicitly separating the local explanatory generator
   `(domain/fields, measure/contour, action, state, instruments)`.
6. **Finite local shadows need not identify the history law.**  D19's exact
   rank-null family agrees on every one/two-record training statistic and
   differs in a higher correlation/future conditional.
7. **Unit conversion does not round a fixed dimensionless cone.**  Relabeling
   axes in metres or seconds leaves anisotropy invariant.  A genuinely
   `G`-dependent dynamical coupling ratio can change the generated ensemble
   and hence its cone; that is new dynamics, not unit recovery.  `G` is a
   measured action coefficient in the current ledger.

## The strongest honest rulebook statements today

At operational finite scope:

```text
typed event algebra E
+ normalized strongly-positive whole-history functional D
+ supplied record semantics and protected future algebra
-> probabilities on the declared decoherent durable-record sector
-> all positive next-record conditionals.
```

At tested low-energy physical scope, the leading extensively tested baseline
and a family of context-specific predictive packets have the template:

```text
3+1 Lorentzian field domain
+ Einstein-Hilbert + Standard Model EFT with measured coefficients
+ prepared/cosmological state
+ gauge measure/contour and renormalization prescription
+ physical record/environment dynamics
-> a predictive effective packet only after those context-specific fields are fixed.
```

The first is finite/cylinder complete only because `D` and the record
interpretation are supplied.  The second is not one empirically selected
complete generator: the SM plus Einstein-Hilbert action is a leading tested
baseline, while state, contour, apparatus/record dynamics and scheme are fixed
per physical context.  It assumes the spacetime/field carrier, contains
measured coefficients and is not a unique ultraviolet completion.  Neither is
the requested derived fundamental SHARD law.

## Why V9 may not be reused as a holdout

V9 is historical program-development evidence and is
`PARKED-AT-PROTOCOL`, not an untouched dataset.  Its reproduced continuous-
leak cone improvement is scientifically useful, but the dimension estimate is
approximately `4.4 +/- 0.2` and instrument-suspect, multiplicity weakens one
cone convention, and “crossed a preregistered tolerance” is not “ideally
round.”  At the strict exact gate, `m4` passed, `dom` failed, the conjunction
failed and dimension was excluded.

The legal geometry gate requires at least two complete physical candidates
with:

```text
state, measure, action, record map and units fixed independently;
no post-hoc cone/dimension tuning;
different preregistered F/dimension/scale-ladder predictions;
an untouched dataset.
```

No current survivor supplies that packet.  `EFT4` inputs 3+1 Lorentzian
geometry; `BDQ` lacks a selected quantum measure/state/records/scale; `ASQ`
lacks a uniquely selected realistic Lorentzian trajectory and record map;
spin foams build four dimensions into the model and lack a controlled selected
continuum packet.  Opening V9 now would rank incomplete, tuneable models with
data already used to develop the program.  The refusal is a documented
PROTOCOL/receipt gate, not a machine-enforced access control.

## The discriminating work that would change the verdict

1. Complete at least two genuinely inequivalent generators to the same
   observable interface.  A causal-order quantum generator is one motivated
   route, not a uniquely forced first candidate.
2. Each must print its domain/quotient, measure, action, matter, state, record
   dynamics, scale dictionary and nuisance parameters.
3. Fit nuisance coefficients on non-holdout laboratory/cosmological data.
4. Let the completed survivors determine and then freeze a cross-candidate
   discriminator.  Possible, not uniquely compelled, classes are a
   higher-order multi-record correlation, energy-dependent influence kernel,
   causal nonlocality scale, scale-dependent cone anisotropy, or a dimension
   estimator validated on known synthetic geometries.
5. Only then collect or designate a genuinely future untouched
   cone/dimension/gravity dataset and run hostile replication.  Existing V9
   results remain closed historical evidence, not that holdout.

## Full-objective verdict

```text
NOT ACHIEVED.
```

This is not a failure to find a clever formula hidden in V1-V10.  The corpus
now contains exact countermodels proving that the stated principles leave
physical law, state, kernel and empirical null directions open.  Claiming the
requested unique law anyway would require inventing an extra principle or
using the target phenomenology to tune it.  The scientifically valid endpoint
is a sharply characterized missing generator plus a future discriminator
program chosen after complete survivors print differing predictions—not a
fabricated proof of the universe's law.
