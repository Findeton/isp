# D18 theorem draft — the rulebook is a history functional, not an action alone

**Status:** final after three hostile rounds at finite/cylinder sufficiency
scope, 2026-07-12.

## 1. The two layers that must not be conflated

The finite **operational probability core** is

```text
O = (E, D),
```

where `E` is the typed history event algebra and `D` is one normalized
strongly-positive decoherence functional on it.  A coarse event question is an
argument supplied to this core, not another dynamics field.  For a decoherent
partition,

```math
P(alpha)=D(alpha,alpha),
qquad
P(next|past)=P(past,next)/P(past).
```

Thus there is no additional next-click lottery.  The next-record law is the
conditional shadow of the one whole-history object.

A separate **interpretation/query layer** identifies which event variables are
physical durable records, which future algebra protects them, and how
dimensionless quantities are reported in metres, seconds and measured
couplings.  Those data matter for SHARD ontology and dimensional reports, but
they are not additional probability inputs once `D` has been supplied.

A physically explanatory **generator** is richer:

```text
G = (C, F, nu, S, rho_boundary, J, coarse, units).
```

It gives `D` through regional class operators,

```math
D(alpha,beta)=Tr(C_alpha rho_boundary C_beta^dagger).
```

Here the local action and instruments can sew without a preferred global
clock, while the boundary state and completed correlations are global.  A
global state is not a global update machine: it is boundary data for the
whole-history law.

## 2. Exact finite sufficiency theorem

The executable uses the four histories of a two-Hadamard interferometer,
typed by intermediate path and final output.  Before a path record, its exact
decoherence functional gives output probabilities `(1,0)`.  A local orthogonal
path commit changes only the off-diagonal history terms and gives `(1/2,1/2)`.
Both functionals are Hermitian, normalized and Gram/strongly positive.  Their
four fine-history diagonal entries are identical, so an ordinary probability
measure on fine paths is insufficient: the interference terms matter.

The general proof is algebraic, not a finite grid inference:

```math
c^dagger D c=||sum_alpha c_alpha b_alpha||^2 >= 0,
```

and every coarse functional is `M D M^dagger`.  The receipt exhausts all
fifteen partitions of the four-history set as regression coverage.

Coarse graining the same `D` by path gives `(1/2,1/2)`.  An explicitly
nondecoherent matrix is rejected rather than read diagonally.  The integrated
D17 causal-record tower is now lifted to one diagonal functional `D_n` on each
actual cylinder algebra.  Exact block restriction gives
`D_n=M D_(n+1) M^dagger` at every depth one through six, and its diagonals give
the same non-Markov local next conditional by ordinary disintegration.

**Finite operational sufficiency.**  On a finite typed event algebra, a
normalized strongly-positive decoherence functional plus declared decoherent
event questions determines every licensed probability and conditional.
No separate sequential click law is needed.

## 3. Layer separation and physical interventions

The receipt separates what is logically necessary from one way of generating
it:

| intervention | exact consequence |
|---|---|
| keep the four diagonals, change off-diagonals by committing a path record | output law changes from `(1,0)` to `(1/2,1/2)` |
| keep the two-Hadamard action, change boundary state `0 -> 1` | deterministic output flips |
| keep the local action, use positive path envelope `(3/5,4/5)` | output becomes `(49/50,1/50)` |
| ask output versus path record question of the same `D` | returned observable law differs |
| move a sign between boundary envelope and action phase | final amplitudes and all predictions are unchanged |
| rescale the units dictionary | dimensionless probabilities stay fixed while reported metres/seconds change |

The sign-moving row is representation gauge only; it is not evidence for two
physically different laws.  The state and record-generator interventions do
change observables and therefore establish physical nonselection at their
finite scope.

Even `(E,D)` is not proved **minimal modulo every operational equivalence**.
For example, imaginary antisymmetric off-diagonal terms can be invisible to
all subset quantum measures and licensed decoherent-partition probabilities
on a frozen event algebra, while differing as matrices.  Phase-sensitive
composition or a declared quotient is needed to distinguish them.  D18 proves
sufficiency, not literal uniqueness/minimality of the representation.

`D` is sufficient operationally, while its explanatory generation from an
action, state, measure and instruments needs additional physical structure.
The action alone cannot replace that generator packet.  Conversely,
specifying a primitive `D`/whole-history law solves the next-click problem by
stipulation rather than deriving it from sealed records.

## 4. Locality and construction order

The proposed answer is local in the sense that the generator factorizes and
sews on owned boundaries.  It is global in the different sense that a
whole-history functional contains correlations across the completed history.
Those statements are compatible.  A computational sampler may expose one
linear extension, but construction-order gauge requires all admissible linear
extensions to give the same observable cylinder law.

The D17 causal witness now realizes that distinction exactly at finite scope:
its action, orders, typed grammar, D14 boundaries, records and projective
kernel are integrated; the kernel remains supplied.  D18 does not infer that
arbitrary continuum conditionals are computable from bounded memory.

## 5. Profinite ceiling

For the durable **classical record subalgebra**, compatible finite cylinder
probabilities support the usual inverse-limit move.  For a quantum
decoherence functional, extension to the full sigma algebra is a separate and
nonautomatic theorem.  D18 therefore claims completeness only on its declared
finite/cylinder event domain.  “Profinite” supplies a home for compatible
shadows; it does not select `D`, and it does not guarantee extension of every
quantum measure.

## 6. What this answers—and what it does not

It answers the structural generative question:

> the interactive click law is not a universal race of records.  It is the
> conditional record shadow of one covariant whole-history decoherence
> functional.  A local action can generate that functional only together with
> a history domain, gauge measure/contour, boundary state and physical record
> dynamics.

It does not identify nature's unique fundamental `G`.  The exact repaired
subresult is

```text
FINITE-DECOHERENCE-FUNCTIONAL-SUFFICIENCY
```

at finite/cylinder supplied scope.  D18 does not yet earn its protocol's
`MINIMAL-COMPLETE-CONDITIONAL-RULEBOOK` verdict, and certainly not
`UNIQUE-FUNDAMENTAL-RULEBOOK-SELECTED`.  Field
content, coefficients, cosmological state, ultraviolet measure/contour,
record emergence, scale and `G` remain to be selected or measured.  No V9
geometry holdout opens from this theorem alone.
