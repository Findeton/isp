# D12 protocol — restore diamonds and adjudicate the generative law

**Status:** frozen before D12 executables, numerical outcomes, new literature
search, or hostile review, 2026-07-11.

## 1. Question

> Does the existing sealed-diamond, closed-holonomy, evidence-clock structure
> uniquely determine the interactive between-diamond extension law; if not,
> what maximal universal form is derivable, and exactly which primitive
> physical data remain?

A law is not selected because it grows, looks round, or reproduces a desired
dimension. Selection requires uniqueness under the frozen principles or an
independent empirical principle fixed before the prediction.

## 2. Objects that must not be conflated

The compatibility audit must distinguish:

```text
sealed diamond D;
finite history atom omega;
whole-history law P_D^hist;
complete closed-holonomy cochain h_D;
eventless repair Pi_0;
local accumulated RN/KL evidence I;
V7 content coordinate chi and any conversion kappa;
commit order versus auxiliary presentation order;
durable outcome versus output-collar birth versus observational truncation;
dense seals versus sparse seals/inter-seal profile;
within-diamond law versus between-diamond extension support;
local Bloch event/effect algebra versus downstream coordinate shadow.
```

## 3. Restored diamond packet

The minimum candidate object is

```text
D = (Omega_D, mu_D, P_D^hist, C_D, h_D,
     lower_screen, upper_screen, eventless_collar,
     local order_unit, frame links, typed exposed interfaces).
```

The complete finite contrast ledger `C_D` must contain every sealed history
distinction capable of affecting future gluing. `P_D^hist` must be recovered
from `h_D` when the V6 hypotheses apply. A live output interface may not carry
an unbounded copy of the past; history remains distributed over immutable
diamonds.

## 4. Competing architecture classes

All must be implemented or killed by theorem before selection.

### A — terminal birth/death token law

D11's globally raced terminal-SEAL packet. Retained only as a negative
control; almost-sure extinction is already proved.

### B — continuing local-instrument SCIR

Local opportunities accumulate evidence, commit an instrument outcome, and
emit typed output collars. Disjoint firings are quotient presentations.

### C — primitive whole-history diamond law

A compatible projective family `P_D^hist` is primitive. Conditional extension
probabilities are disintegrations of that law; no Markov factorization is
assumed.

### D — amplitude/decoherence-functional history law

Local diamond amplitudes compose before commitment; exclusive committed
records receive a positive decoherence-functional/Born readout. Alternative
construction presentations belonging to one physical history are summed at
the amplitude level where licensed.

### E — local-threshold representation of C or D

Independent local thresholds are permitted only if they are proved to
represent the same projective whole-history law. A convenient simulation race
is not a new physical postulate.

## 5. Candidate universal form

For a finite boundary/collar `B`, let `Ext(B)` be the intrinsically licensed
typed diamond extensions. A general positive conditional law has the form

```math
p(e|H,B)
  = {mu_B(e) exp(<h_B(H), chi_B(e)> )
     \over
     sum_{e' in Ext(B)} mu_B(e') exp(<h_B(H),chi_B(e')>)}.
```

This becomes a derivation only if all of the following are intrinsic:

```text
Ext(B)       extension support/grammar;
mu_B         reference/count measure;
chi_B        complete primitive oriented contrast ledger;
h_B(H)       coefficients/history dependence;
gluing maps  between lower and upper screens;
instrument   quantum outcome and output interface maps.
```

V6's fixed-point proposal

```math
nabla psi_B(h_B)=exp(-h_B)
```

must be tested for basis, reference-measure, support, orientation, and
refinement dependence. Strict convexity proves uniqueness only after those
objects are fixed; it cannot silently select them.

## 6. Frozen gates

### U0 — corpus compatibility

Produce a clause-level V6–V10 ledger. Resolve `S(I)=exp(-I)` versus
`S(chi)=exp(-kappa chi)`, dense versus sparse seals, within-diamond versus
extension dynamics, and D8 seal-and-birth versus terminal observation.

### U1 — finite diamond ontology

All screens, collars, links, frames, history laws, outcomes, output interfaces,
and ownership are typed. No silent root, bridge, frame, matrix, or terminal
deletion is allowed.

### U2 — local evidence clock

Eventless gluing proves evidence additivity. Survival is evaluated in actual
RN/KL evidence. Any external-time or equal-token race must be derived as a
representation.

### U3 — whole-history/projective law

Explicit truncation maps push fine/canonical diamond histories to coarse
histories and reproduce cylinder masses. A selected Markov kernel is not a
substitute.

### U4 — construction-order gauge

All auxiliary linearizations of bounded concurrent firings are grouped into
canonical physical fibers. Their pushed probability or decoherence-functional
weight agrees. Overlapping operations retain physical order.

### U5 — seal, birth, and observation

Internal commitment creates a durable record and the declared output collar.
Observational truncation is external coarse-graining and cannot change the
unobserved internal future law. Any physical lineage annihilation requires a
separate typed rule and conservation/boundary justification.

### U6 — interaction locality

Multileg interaction requires a previously recorded connected collar owning
all legs. Direct siblings are a special case. Arbitrary all-pairs graph search
and silent cross-component joins are forbidden.

### U7 — integrated gauge

Generate the same bounded history in independently changed vertex frames.
States, effects, order units, instruments, links, anchors, screens, outcome
probabilities, and canonical history agree after transport.

### U8 — uniqueness/underdetermination

Construct every architecture class surviving U0–U7. If two inequivalent laws
share all frozen intrinsic data and pass every structural gate while differing
on a record probability, survival statistic, or influence prediction, unique
derivation is refuted. Aesthetic economy or favorable geometry cannot choose.

### U9 — empirical selection

Only a predeclared independent observation may distinguish structurally
admissible survivors. The selection dataset and the later prediction dataset
must be disjoint. Existing quantum, relativistic, and no-signalling results
are constraints, not post-hoc fit targets.

### U10 — geometry after law selection

Only a uniquely derived or independently selected packet may be tested for
survival, influence envelope, cone shape, scale dependence, and dimension.
Algebraic coordinate placement is never counted as emergent spacetime.

## 7. Verdicts

```text
UNIQUE-DERIVED-DIAMOND-LAW
  all U0–U8 pass with one equivalence class;

EMPIRICALLY-SELECTED-DIAMOND-LAW
  U0–U8 leave alternatives and a predeclared independent observation selects;

UNIVERSAL-FORM/PRIMITIVE-MEASURE-REMAINS
  the form is derived but inequivalent path measures/grammars/couplings survive;

REFUTED-DIAMOND-ARCHITECTURE
  no candidate passes locality/projectivity/gauge/quantum gates;

INCOMPLETE-INVESTIGATION
  a promised object, alternative class, or decisive gate is not executed.
```

The words “real law of the universe” are permitted only for an empirically
selected law with novel holdout predictions, not for a structural theorem or
successful simulation alone.

