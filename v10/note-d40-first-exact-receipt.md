# D40 first exact receipt — action/functional descent and the serial-to-DAG level audit

**Status:** exact receipt candidate, `PASS 11/11`; first independent hostile
review open.  Paper 29 remains held.  
**Date:** 2026-07-15.  
**Pin:** `note-d40-where-the-action-cocycle-lives.md`, frozen before the
executable at commit `274ee63`.  
**Source:** `code/d40_action_cocycle_level_audit_exact.py`.  
**Stdout:** `data/d40_action_cocycle_level_audit_exact.out`.

## 1. Frozen receipt

```text
source_sha256           = 3930ae7abd704763767ececcf2b0de8e4ad5926143211e981e90d389cb88b9d5
stdout_body_sha256      = d25d1520a870813b09e6a72a597e515644eb8c0de1987686b272914838fa7bbb
internal_science_sha256 = 5f501de58c437b5ad0c19ee02a68abfdcbfdac274bdbe197be915f3c6145d15d
complete_stdout_sha256  = 7f00c92019765086089c2f9a5aa1f22d3a52076c124c77228e1d99466bac464a
```

The frozen run exits zero and is byte-identical to the committed stdout.  All
theorem arithmetic is integer, `Fraction` or exact `Q(sqrt(2))`.

The first execution reached R5 and then stopped because the generic quantum
`h`-ratio helper assumed the outcome alphabet `{0,1}`, whereas the Bell
fixture deliberately uses `{-1,+1}`.  The helper was repaired to infer both
finite alphabets from its terminal keys.  No frozen scientific value was
changed or selected by that repair; the stdout above is the first complete
run and repeats byte-identically.

## 2. Descent theorem

R1 checks 1,024 exact squares from all four-atom positive integer measures
with weights in `{1,2,3,4}`.  Revealing either coordinate first gives the
same refined cylinder ratio.  The finite boundary criterion is exact: a
coarse boundary has a well-defined pushed next-record kernel precisely when
all refined states in each fibre have the same pushed law.  A hostile fibre
with unequal laws is retained.

R2 imports the hash-locked D34c fixture.  The disjoint incomparable
`CNOT(S,R)` and `Z(P)` operators commute; both construction orders give the
same eight branch vectors and rank-four decoherence functional.  The
shared-support `H(S)`/`CNOT(S,R)` reorder differs, and all 8-to-4-to-2
functional restrictions remain exact.

R3 produces two decoherent durable-record measures and checks eight refined
click squares with sixteen positive queried denominators.  The exact
hypotheses are exhaustive exclusive records, decoherence, a common refined
cylinder or declared pushforward atom, positive denominator and sufficient
boundary.

R4 retains the failure control.  The unrecorded-path law is

```text
(0, 1/2, 1/2, 0),
```

with eight nonzero off-diagonal functional entries.  Adding an orthogonal
path record gives `(1/4,1/4,1/4,1/4)`.  An operator fixture therefore does not
by itself select a unique click law; the record instrument is load-bearing.

## 3. What `1/18 != 2/33` means

R5 independently rebuilds the D39 action census and reproduces the Paper 28
star products:

```text
root-idle then B-birth = 1/18;
B-birth then root-idle = 2/33.
```

The final projected star is the same.  On the complete embedded D38b event
race, however, the corresponding serial probabilities are

```text
root-idle then B-birth = 1/32;
B-birth then root-idle = 1/48.
```

The event identities, final authenticated record store and typed causal DAG
are equal after forgetting the incomparable serialization.  The probability
of that typed DAG atom is the required pushforward sum

```text
1/32 + 1/48 = 5/96.
```

Thus Paper 28's theorem survives exactly as stated: chosen projected D38b is
not a member of the registered classical flat action-cocycle variety.  What
does **not** follow is that the D38b probability law is inconsistent.  The
flat square equated two serial weights that the actual typed-DAG law sums.
The receipt's primary noun is

```text
LEVEL-MISMATCH-AT-SERIAL-TO-TYPED-DAG-DESCENT.
```

This finding does not require quantum interference.  It decides the proposed
finite control before the quantum column is invoked.

## 4. The quantum column and K-flat

R6 proves the positive completion-ratio form for uniform continuation counts,
nonuniform classical terminal weights and exact Bell/Born terminal weights.
All seven classical rows normalize in each classical fixture, all sixteen
Bell terminal ratios telescope, and D39's separate depth-three registry
remains 21 normalized rows, 78/78 squares and 179 terminal paths.

The licensed conclusion is

```text
K-flat shape = a general positive h-ratio / Doob completion form.
```

It is not uniquely quantum and does not select its terminal weight.

R7 executes an exact Bell state with

```text
A0=Z, A1=X,
B0=(Z+X)/sqrt(2), B1=(Z-X)/sqrt(2).
```

It gives `CHSH=2*sqrt(2)`, four spacelike operator interchanges, four exact
Gram forms, sixteen no-signalling marginals and sixteen refined click
cocycles.  Erasing Bob's setting from the predictive boundary leaves the
distinct conditionals

```text
1/2 + sqrt(2)/4,
1/2 - sqrt(2)/4,
```

so the coarse boundary is not sufficient.  The fixture proves compatibility
of Bell-class records with functional and refined-cylinder consistency.  It
does not execute D15, require joins for entanglement or turn D23's
in-degree-at-least-two identifiability ceiling into a dynamical theorem.

## 5. The physical-law audit

R8 finds all eight corpus admissions concerning supplied action-to-kernel,
state, measure/contour/renormalization, record-instrument, preferred-algebra,
unit and register-to-system dictionaries.  The same exact operator fixture
supports two distinct record laws.  Therefore the identified D15 low-energy
action content is retained, but an executable record-closed D15 click law is
not established.

R9 assigns twelve downstream claims exactly once among grammar/information,
classical-generator-specific, decoherent-record universal,
quantum-functional-specific and bridge-dependent/open classes.  The D38b
numerical obstruction remains toy-law-specific; the cylinder descent theorem
is record-law universal at its hypotheses.

R10 reproduces the conditional D26 values

```text
g=9/25;
one same-line BORN visibility = 4/5;
three same-line BORN visibility = 64/125.
```

It preregisters six protocol fields for a monitored BORN/TOKEN comparison.
There are no new lab data, no rate claim and no cross-system bound without the
same-line BORN/system dictionary.

## 6. Claim ceiling and review target

```text
FINITE ACTION/DECOHERENCE-FUNCTIONAL TO DECOHERENT-RECORD DESCENT THEOREM;
EXACT SERIAL-TO-TYPED-DAG LEVEL-MISMATCH WITNESS;
EXACT BELL ARCHITECTURE FIXTURE;
UNRESOLVED D15 DICTIONARY.
```

The hostile review must independently re-derive at least:

1. the classical cylinder identity and the sufficiency iff;
2. `1/32 + 1/48 = 5/96` from complete event intensities;
3. equality of the two typed D38b event sets and causal-DAG atoms;
4. `CHSH=2*sqrt(2)` and one erased-setting conditional pair;
5. the distinction between action-filter nonmembership and probability-law
   inconsistency;
6. the D15 dictionary ceiling.

Paper 29 remains held until that review closes or its findings are repaired.

