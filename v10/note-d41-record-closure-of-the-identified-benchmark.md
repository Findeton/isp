# D41 — record-closure of the identified benchmark

**Status:** CAMPAIGN PIN (pre-receipt), 2026-07-15. **Parent:** paper 29
(terminal audit: the identified law is NOT proved record-closed; seven
items supplied) × v1 paper 15 (the minimal genuinely interacting
gauge-matter benchmark: fixed finite Z₂ lattice + fermionic matter, exact
Gauss-sector decomposition, exact link elimination, non-bilinear reduced
Hamiltonian, the exchange-defect coefficient; validator
`validate_minimal_interacting_gauge_matter_benchmark.py` in the repo root).
**The question:** run the FIRST record-closure attempt on the physics the
program identified, at the smallest exact interacting fixture the corpus
owns. Every one of paper 29's supplied items must end in exactly one of
three verdicts, per item, receipt-gated:

```text
GENERATED     the item is produced by a record law (D34-class machinery),
              with the construction exact at fixture scope;
IRREDUCIBLE   the item is declared supplied-at-scope, with the reason
              stated (paper-16 discipline: never smuggled into
              normalization);
OBSTRUCTED    a finite witness that no admitted construction supplies it.
```

## 1. The seven items against the benchmark

1. **Boundary state.** The benchmark's initial data = one Gauss sector ×
   one Z₂ flux block × a matter configuration. Gate: enumerate the admitted
   sectors exactly; attempt generation of the sector CHOICE by a record law
   (expected: IRREDUCIBLE — paper 16's boundary-state clause; the receipt
   prints the sector census and the declaration).
2. **Measure/contour.** Finite-dimensional Hamiltonian dynamics: construct
   the finite decoherence functional on reduced-strip histories explicitly
   (class operators from the reduced Hamiltonian's Trotter steps at exact
   rational couplings where available; else declared precision). Gate:
   strong positivity + the paper-29 ladder hypotheses checked at fixture
   scope.
3. **Renormalization.** EXPECTED COLLAPSE AT SCOPE: the fixture is finite;
   no renormalization data exist to supply. Gate: the absence is PROVED
   (no cutoff parameter appears in any constructed object), recorded as
   the first supplied item that vanishes at benchmark scope — this is why
   the benchmark is the right first fixture.
4. **Record instrument [THE CENTER].** Attempt to GENERATE the click
   instrument as record actors: a D34-class local law whose records read
   the benchmark's gauge-invariant observables (matter occupation; parity
   string). Gates: (a) the instrument's clicks are typed records with D24
   birth content at declared g; (b) D25/D27 admissibility (Busch class;
   distinguishability-isometry on the probed family); (c) NSE at fixture
   scope (every discarded distinction received — the value/content split
   checked on the Z₂ observables); (d) the honest fork: if only a SUPPLIED
   instrument closes, the item is IRREDUCIBLE with the failing clause
   printed.
5. **Record grammar.** The dictionary: benchmark elementary processes
   (hops; parity-string flips) ↔ typed record events (interact-class on
   adjacent record lines; the link variables as shared-edge records).
   Gate: a bijective-at-scope typed map with the D33 gauge respected
   (comparable = shared wire), or the obstruction witness (a benchmark
   process with no admitted record type).
6. **Clock dictionary.** Attempt the record-native clock: a repeatable
   click pattern in the generated instrument whose count serves as
   duration; re-express the exchange-defect coefficient in record-counted
   time. Gate: the coefficient's VALUE is invariant under the re-expression
   (ratios-only per paper 28 Theorem 5 — absolute scale is expected to
   remain bridge-dependent, stated), or the obstruction.
7. **Generated opportunity structure.** Which interactions are OFFERED at
   each step (the benchmark's adjacency = the lattice): the lattice is
   static and supplied — gate: record it as the fixture's analog of the
   supplied opportunity complex, with the D42 (conflict-generating)
   dependency named. EXPECTED: IRREDUCIBLE at scope, discharged only by
   D42-class dynamics.

## 2. Capacity pin

The two-bond reduced strip (the paper-15 prototype) at the smallest
compatible Gauss/flux block; exact rationals wherever the couplings admit
them; declared-precision mpmath (dps ≥ 80 per standing discipline) where
not. No object larger than the strip enters a gate.

## 3. Fronts (pinned)

(RF1) Hegerfeldt is a CONTINUUM hazard — out of scope at the finite
fixture, pre-registered for any lift. (RF2) If the generated instrument
ever joins record lines, D23's in-degree ≥ 2 identifiability limit applies
from birth. (RF3) The universality note's protocol (note-d41b-universality)
applies to every D41 claim: benchmark-relative vs grammar-universal must be
sorted at closure. (RF4) The item-4 fork's IRREDUCIBLE outcome must state
WHICH Busch/NSE clause fails generation — that clause becomes the next
campaign's target. (RF5) Item 6's expected residue (absolute scale) feeds
the D26 experiment note's dictionary caveat — the two artifacts must cite
each other's scope guards.

## 4. Deliverable

`code/d41_benchmark_record_closure_exact.py` (gates B1–B7 per §1's items +
the scorecard); LOG + LEDGER per house style; hostile round on green; the
paper-30 decision AFTER the round (the campaign may need d41b/d41c before
any paper).
