# Relativistic ISP v10 paper 31

# Four decisions at the joints

## Rule-independence of the collar bracket, the six-state renewal chain, the constructed arbitration operator, and dimension from transport

**Author:** Felix Robles Elvira (ORCID 0009-0009-2017-4394).
Relativistic ISP series, v10 paper 31.
**Status:** EXACT-RECEIPT PAPER at the declared finite scope.  Every
quantitative claim is bound to a committed source: one of the four
terminal receipts of this campaign (outputs under `v10/data/`), or —
where an in-line tag says so — the committed campaign record (LOG, pins,
frozen review files).  Independent paper-level review: round 1 + delta,
frozen at `reviews/paper31-round1-hostile-review.md`.
**Date:** 2026-07-19.

## Abstract

Paper 30 closed with a residue ledger headed by two items and a
prediction about neither: the infinite-volume positive-harmonic core of
the decided completion problem (residue 1), and the arbitration-layer
step operator behind a three-horn obstruction (residue 2).  This paper
reports a four-part interrogation of the generated record at its
load-bearing joints: the two ledger heads, plus two standing foundations
questions the ledger did not order — whether the click-law lineage's
coefficient anchor depends on its regulator (a comparison deferred since
the series' first paper), and whether generated record orders carry
dimension (where paper 30's pilot returned a null).

All four are decided at their declared scopes.  **First**, the collar
exchange bracket is rule-independent at the operator level: the
collar-excision and Lie–Trotter admission rules produce lattice defect
coefficients that are genuinely non-proportional (exact rational orbit
spectra, mass-dependent), yet under the corpus's own continuum
identification both collapse onto the *same* tangential operator ray
`K_par = (beta d_x + (1/2) d_x beta)(I - alpha)`, with the EXC
constant exactly 1 at both masses and one per-mass LT constant:
`kappa(1/2) = 13/2304`, `kappa(1) = -1/72`.  The sign
flip is itself a theorem: no shared generator of the corpus's
positive-square class can underlie both rules — rule-relative data is
constants, never structure.  **Second**, the intrinsic state object of
the generated grammar — refinement by probabilistic bisimulation at
uniform lookahead, with no truncation marker — stabilizes at SIX states;
on it the completion core is exact and small: Perron root `lambda = 2`
with rational spectrum, a positive harmonic vector `f = (4,4,3,7,3,3)/3`
unique up to scale (existence and uniqueness), root = renewal state at
every computable lookahead, and an exact mass-transport identity with
`pi = (1,1,2)/4`.  Residue 1 is thereby DECIDED on every computed
window; the all-depth statement is reduced to one named closure theorem
whose two ingredients are exhibited and receipt-gated.  The 17-state
quotient of paper 30 is superseded: it is the truncation-marked
stratification of the six.  **Third**, residue 2's operator-existence
face is discharged constructively: an isometric family `{V_single,
V_pair}` — acceptance composed with the join-typed opening click —
reproduces the committed Born weights (the `K1` kernel) and reconstructs
the committed arbitration menus at both cuts from one cut-independent
matrix per component type plus a past-locally computed index.  The
randomized escape class is excluded by a visibility theorem: an
initiator-local-coin family is a function of an own view that is
identical at both cuts while the required branch sets differ.
**Fourth**, transport generates dimension: six deliveries among six
actors, each admissible at exactly `1/20`, realize the three-crown `S3`
— a minimum-size order of dimension 3 — and four actors
suffice at six events; two actors never leave dimension two (a width
theorem), and three actors do not through ten events.  Actor width, not
event count, is the capability axis.  The residue ledger is updated:
what was the program's deepest open pair is now a closure theorem with
its ingredients in hand and a foliation/measure question about an
operator that exists.

## 1. Where the record stood, and the four questions

### 1.1 The inherited state

Paper 30 replaced the supplied coordination structure of the earlier
program with a generated one — batch, conflict, arbitration opportunity,
transport, and reconciliation demand all produced by a past-local
admission relation — and decided the classical completion problem on the
weight system that structure induces: ratio-preserving completions do
not exist [THEOREM, 36 of 202 diamonds]; gradient completions exist at
every finite depth at the exact cost of within-cut ratio deformation
[EXACT, 21 of 114 interior cut classes].  Its ledger reduced the
remainder to six scoped residues, headed by (1) the infinite-volume
positive-harmonic core — the one analytic remainder of the decided
discrete Tomonaga–Schwinger condition — and (2) the arbitration-layer
step operator, pinned behind a three-horn obstruction: cut-independent
operators reproduce the arbitration-killing zero class, cut-dependent
operators require carrier overlap with the blind wire, and dilations
re-import cut data.

Beneath the ledger, two older questions stood open at the foundations.
The click-law lineage's coefficient anchor — the collar exchange bracket
from which the series' identified coefficients descend — exists only
under the collar-excision regulator; the corpus's own alternative
admission rule (Lie–Trotter splitting) was posed in the series' first
paper ("whether both rules, after the appropriate onset renormalization
and continuum identification, land on the same tangential bracket") and
never executed.  And the dimension question — whether generated record
orders are two-clock configurations or something larger — had only paper
30 §7.2's honest null: a pilot at scales where the dimension gate is
provably vacuous.

### 1.2 The four questions, posed exactly

- **Q1 (rule-independence).**  Compute both admission rules' exchange
  defects on the v1 free-core fixture in exact arithmetic; compare at
  the rules' own onset orders, at the one-region-coefficient commutator
  level, and under the corpus's continuum identification.  Decide:
  is the tangential bracket a regulator artifact?
- **Q2 (residue 1 at its natural scale).**  Build the *intrinsic* state
  object of the generated grammar — no truncation marker, uniform
  lookahead — and pose the positive-harmonic problem on it: Perron
  root, dominant class, existence and uniqueness of the positive
  eigenvector, the root-free certificate, and the invariance
  (mass-transport) face.
- **Q3 (residue 2 against its escape classes).**  Test the two known
  escape shapes from consensus-impossibility theory against the pinned
  requirements: randomized (initiator-local coins) and higher-order
  (component-typed operator families).  Either a fourth door opens
  constructively or the pincer extends.
- **Q4 (dimension with a theorem-backed instrument).**  Run the
  canonical star-discrepancy/two-clock instrument (v8 papers 12–13),
  ported code-faithfully, on the generated families and on constructed
  transport configurations large enough for the dimension gate to be
  live.  Decide: does transport generate dimension?

Sections 2–5 decide Q1–Q4.  Section 6 states what the four decisions
jointly establish; section 7 updates the residue ledger; section 8 is
the methods note binding every number above to its receipt.

### 1.3 Supersessions

This paper supersedes five statements of the earlier record, each in
the body where the mathematics lives:

- Paper 30 §5.7/§9 residue 1, "17 states at the decided depth": the 17
  is the truncation-marked stratification of a six-state intrinsic
  object (§3.2 below); the six-state chain is the correct carrier of
  the residue, and on it the residue is decided on the computed window.
- Paper 30 §7.2, the dimension pilot's null: superseded positively by
  §5 — the null was an artifact of scale (the gate is vacuous below six
  elements) and of width (chains of width ≤ 2 cannot fail it).
- Paper 30 §6.2, the pincer's scope: sharpened to cut/initiator-
  attached *channels*.  The three horns stand there; the constructed
  family of §4 lives in the typed class the horns do not reach, and
  the randomized class is excluded (§4.2).
- Paper 30 §8.3, "until the step operator exists at the discrete
  arbitration layer, there is nothing to take a limit of": the
  existence face is discharged at fixture scale (§4), so the
  continuum gate transfers to the foliation/commutation face and the
  measure side — the conclusion (no continuum claim) stands with its
  premise updated.
- Paper 30 §7.2, "the full probe needs depth beyond enumeration": the
  wrong axis — §5 shows the capability axis is actor width (six
  events suffice at width `>= 4`; depth alone never suffices at
  width `<= 2`).

Provenance labels as in the series: [THEOREM] proved at the declared
scope; [EXACT] machine-verified identity in exact arithmetic;
[MEASURED] computed value with no universality claim; [POSITED]
modeling choice; [LITERATURE] external result used as statement.

## 2. The collar bracket is rule-independent at the operator level

### 2.1 The fixture and the two rules

The fixture is the series' free core [ported verbatim from v1 paper 1 /
v2 paper 1 / the root validator — no reconstruction]: a 1+1D periodic
lattice of `L = 12` sites, two-component spinors, `alpha = sigma_x`,
`beta = sigma_z`, lattice constant `a = 1`, mass `m in {1/2, 1}`.
`H_D = sum_n m beta_n + sum k_{n+1/2}` with hopping
`K = -(i/2) alpha (T_+ - T_-)`.  For a region `R`, the collar split is
`C_R` = the mass terms in `R` plus the bonds meeting `R`, and
`B_R = H_D - C_R`.  The two admission rules are

- **EXC (collar excision):** `U_R = exp(-i Delta B_R)`;
- **LT (Lie–Trotter):** `U_R = exp(-i Delta B_R) exp(-i Delta C_R)`.

`Gamma(U)` is the entrywise Born square `|U|^2`; the comparison map is
`J_R(Delta) = Gamma(U_R) Gamma(exp(-i Delta H_D))^{-1}`; the exchange
defect for two regions is the group commutator
`E_{R,S} = J_R J_S J_R^{-1} J_S^{-1}`, with singletons `R = {n_0}`,
`S = {n_0 + d}`.  All expansions below are truncated power series in
`Delta` to order 12 — series arithmetic at `mpmath dps 50` against
`1e-30` gates, with the coefficients' exact rational values confirmed
by the frozen round's independent exact-rational rebuild; there is no
fitting anywhere in the receipt [EXACT, values record-confirmed].

Anchors reproduced before any new content runs: the EXC singleton
coefficient `A^(1)` with the documented exact entries (diagonal `1/2`
at `n_0`, `1/4` at `n_0 +- 1`, `-1/4` spin-flip off-diagonals,
same-spin zero, columns summing to zero); the `lambda`-family scaling
`c_lambda = lambda(2 - lambda)` (at `lambda = 1/2`, `A^(1)` scales by
exactly `3/4`); the documented `d = 1` exchange entries `+-1/8` in the
spin-flip channel; the LT onset law `E^LT - I = O(Delta^{2 max(4,d)})`
with all sub-onset and all odd orders vanishing identically; and
support vanishing off the collar [EXACT, all at `1e-30` with exact
series behind them].

### 2.2 The raw defects are genuinely rule-relative

At the rules' own onset orders the defect coefficient matrices are
non-proportional in every cell — not marginally, but with order-one
structure.  The entrywise comparison over the maximal-magnitude entry
orbit yields exact rational ratio spectra [EXACT]:

| cell | ratio orbit (LT/EXC over the maximal orbit) | verdict |
|---|---|---|
| `m = 1/2, d = 2` | `{-137/2304, -5/256, +79/2304}` | DIVERGENT |
| `m = 1, d = 2` | `{-7/288, -3/128, +13/576}` | DIVERGENT |
| `m = 1/2, d = 3` | `{-13/48}` (one ratio; strip deviation `~0.77`) | DIVERGENT |
| `m = 1, d = 3` | `{-11/24}` (one ratio; strip deviation `~0.45`) | DIVERGENT |

Strip-channel moments (the p7/p8 basis) are likewise non-proportional
(at `d = 3`: six distinct channel ratios; twelve LT residual entries
outside the basis).  If the comparison stopped at the lattice-entrywise level, the
foundation alarm would stand: the defect structure is rule-relative,
with mass-dependent exact ratios.

### 2.3 The bracket level: the same divergence, one support corollary

The orphaned question lives one level down, at the one-region-
coefficient commutator — the object the series' second paper actually
anchors.  Extract `A^rule_R` = the leading one-region `J` coefficient
(EXC at `Delta^2`, LT at `Delta^4`) and compare
`C^rule_d = [A^rule_R, A^rule_S]` directly.  A structural identity
makes this comparison exact rather than extrapolated: the LT one-region
ladder is even-only, so at the LT onset order the *entire* defect
coefficient is the pure bracket — `E^LT[Delta^8] = [A^LT_R, A^LT_S]`
with no admixture [THEOREM at fixture scale, per the frozen review
record].  The verdicts per `(m, d)` [EXACT]:

| cell | `[A^LT_R, A^LT_S]` vs `[A^exc_R, A^exc_S]` |
|---|---|
| `m = 1/2, d = 1` | DIVERGENT, `kappa = -5/384` |
| `m = 1/2, d = 2` | DIVERGENT, `kappa = +79/2304` |
| `m = 1, d = 1` | DIVERGENT, `kappa = +19/384` |
| `m = 1, d = 2` | DIVERGENT, `kappa = +13/576` |
| `m = 1/2, d = 3` and `m = 1, d = 3` | SUPPORT-MISMATCH: the EXC bracket vanishes identically at `d = 3` while the LT bracket does not |

(the `d = 2` kappas are the deterministic max-orbit representatives —
the full `d = 2` orbits are the §2.2 table's; the `d = 1` orbits are
single-ratio.)

So at every lattice grain the answer is the same: the two rules do not
share defect coefficients, and the LT rule populates channels the EXC
rule cannot reach.

### 2.4 The continuum identification: one ray, one constant per mass

The corpus's own continuum identification (v2 paper 1 §5) converts a
defect coefficient into a tangential transport operator through its
first distance moment: with
`tau(delta, s, s') = sum_{r > 0} r [A_0, A_r]_{(i,s),(i+delta,s')}`
per channel, the identified operator is `D = sum_delta delta tau`.  The
LT channel tables are exact rationals and `delta`-odd in every channel
[EXACT]:

| `m` | `tau(1, flip)` | `tau(2, same)` | `tau(3, flip)` | `tau(4, same)` |
|---|---|---|---|---|
| `1/2` | `+1/9216` | `-23/9216` | `-1/1024` | `+1/512` |
| `1` | `-19/1152` | `-17/2304` | `+1/128` | `+1/512` |

(both spin-flip channels equal; negative-`delta` entries are the exact
negatives; the EXC tables occupy `|delta| <= 2` only.)  Summing, the
lattice differences collapse [EXACT]:

- **EXC:** `D = 1 . (I - sigma_x)`, both masses;
- **LT:** `D = kappa(m) . (I - sigma_x)` with
  `kappa(1/2) = +13/2304` and `kappa(1) = -1/72`.

Both rules land on the *same tangential operator ray*
`K_par[beta] = (beta d_x + (1/2) d_x beta)(I - alpha)`, with one
per-mass constant for the LT rule across both derivative layers (the
second layer per the frozen review record).  This
is the decision of Q1: **the operator content of the collar bracket is
rule-independent at this order; what is rule-relative is a scalar**
[THEOREM at fixture scale].

The sign flip of `kappa(m)` between the two masses carries its own
theorem.  Any account of the two rules by a shared generator of the
corpus's positive-square class (the `c^2 > 0` mechanism of the v2
Theorem-3 form) would force a constant of fixed sign; `kappa` changes
sign between `m = 1/2` and `m = 1`.  Hence: **no single shared
generator of that class underlies both rules — bracket-ray universality
holds without the mechanism that would have explained it**
[THEOREM at fixture scale].

### 2.5 Scope

One fixture (the v1 free core), singleton regions, `L = 12`, two mass
points, exact series at order 12; distances `d <= 3` at defect level
and channels `|delta| <= 4` in the transport tables.  The lattice-level
rule-relativity is real and quantified (§2.2–2.3): coefficient
*normalizations* are regulator data, and any statement in the series
that reads a lattice defect coefficient as physical must scope itself
to a named rule.  What propagates to the continuum identification —
the operator ray — does not depend on the rule at this order.  The
finite-slab smeared theorem (log-smeared test functions on slabs of
growing width), the exact function `kappa(m)` with the location of its
zero crossing, and the interacting-fixture cross-check are named open
items (§7).

## 3. The record chain closes at six states, and residue 1 is decided on the window

### 3.1 The intrinsic state object

The generated grammar of paper 30 §1 (two actors; events `p`/`r`/`n`;
budgets `1/4` propose, `1/4` arbitrate, idle remainder; the past-local
candidate relation as the only opportunity source) induces, at every
history, a candidate menu of `(event, weight)` pairs.  The intrinsic
state of a history is defined with no reference to any enumeration
depth [POSITED, the definition; everything after it is computed]:

- `P_0` = the partition of histories by *menu shape* (event kinds with
  their exact weights, as a multiset);
- `P_{t+1}` = one probabilistic-bisimulation refinement of `P_t`: two
  histories stay equivalent iff for every successor class the total
  transition weight into that class agrees exactly;
- the intrinsic partition is the stable limit `P_t = P_{t+1}`.

A history's `P_t` class depends only on its `t`-step subtree — the
definition is horizon-uniform.  The depth-6 family census is
`[1, 7, 39, 215, 1191, 6471, 34375]` cumulative by depth [EXACT].

The distinction between this object and a depth-quotient is not
pedantry; it is a theorem.  Appending an idle event to any history
changes no candidate menu anywhere in its future — the *pad-shift
identity*, verified family-wide (12,942 checks, 0 violations) [EXACT].
Consequently a quotient computed with a boundary marker at the
enumeration depth cannot be intrinsic: every noop-padded copy of a
shallow history sits at a different remaining horizon and is split from
its own pad, so the depth-marked quotient stratifies each intrinsic
state into horizon shells and its state count grows with depth (17, 23,
29 at depths 4, 5, 6) while representing no new structure.  The
17-state quotient of paper 30 §5.7 is exactly this stratification: its
17 states map onto the six intrinsic classes as 14 pure horizon shells
(three per shared-sector class, one per diverged-sector class) plus
exactly three genuinely mixed blocks — the map is onto, and the old
quotient is simultaneously coarser and finer than the intrinsic object
[EXACT].  The old computation is retained in the receipt as a gated
negative control, not as a state count.

### 3.2 Six states

The intrinsic partition stabilizes at lookahead `t = 2` on every window
computable from the depth-6 cache, at SIX states (`|P_t|` tables
`[4,4,5,5,5]`, `[4,5,6,6]`, `[4,5,6]` on the `len <= 2/3/4` windows;
blockwise agreement at `t = 2, 3, 4`) [EXACT], with an independent
depth-7 confirmation (179,783 histories; still six; cited to the
committed campaign record).  The six, by shortest representatives:

| state | representative | content |
|---|---|---|
| 0 | `[]` | quiescent, shared base (root, all pads, all renewals) |
| 1 | `[pA0]` | one live proposal |
| 2 | `[pA0, pB0]` | two non-conflicting live proposals |
| 3 | `[pA0, pB1]` | THE CONFLICT PAIR |
| 4 | `[pA0, selfA]` | diverged holdings, quiescent |
| 5 | `[pA0, pB0, selfA]` | diverged holdings, one live proposal |

### 3.3 The exact transfer

The `6 x 6` transfer is well-defined: every one of the 215 histories of
length `<= 3` in a class produces the same exact row — the
probabilistic-bisimulation property, gated member-by-member — and the
matrix is [EXACT]

```text
T = [ 3/2  1/2   0    0    0    0  ]      row sums:
    [  0   3/2  1/8  1/8  1/4   0  ]      (2, 2, 2, 5/2, 2, 2)
    [  0    0   3/2   0    0   1/2 ]
    [ 1/2   0    0   3/2   0   1/2 ]
    [  0    0    0    0   3/2  1/2 ]
    [  0    0   1/4   0   1/4  3/2 ]
```

The conflict state's excess row sum `5/2` is the ladder's `5/4` per
actor — the same quarter-quantized surplus that paper 30 censused at
the support level, now appearing as the one non-uniform row of the
intrinsic chain.

### 3.4 The Perron decision

On `T`, everything residue 1 requires at this scope is exact and small
[EXACT throughout]:

- **Spectrum.**  The dominant class `{2, 4, 5}` (Tarjan decomposition;
  `{0, 1, 3}` is transient and carries the renewal loop `3 -> 0`).  Its
  characteristic polynomial vanishes at `lambda = 2`; indeed the
  nonzero shifted eigenvalues `u = lambda - 3/2` satisfy `u^2 = 1/4`
  (the full dominant-class spectrum is `{1, 3/2, 2}`) — the spectrum
  is rational, and no numerical eigensolve enters anywhere.
- **Uniqueness of the dominant class.**  `det(2I - M_transient) = 3/32
  > 0` and the resolvent `(2I - M_transient)^{-1}` is entrywise
  nonnegative, with exact rows `(8/3, 8/3, 2/3)`, `(2/3, 8/3, 2/3)`,
  `(8/3, 8/3, 8/3)` — by the M-matrix theorem the transient spectral
  radius is `< 2` (it is `3/2 + (1/32)^{1/3} ~ 1.815`), so `{2, 4, 5}`
  is the *unique* class at the Perron root.
- **The positive harmonic vector, existence AND uniqueness.**
  `T f = 2 f` exactly at `f = (4, 4, 3, 7, 3, 3)/3 > 0`, and the
  transient extension is *forced*: applying the nonnegative resolvent
  to the cross-flow returns exactly `(4/3, 4/3, 7/3)`.  The positive
  2-harmonic function is unique up to scale on the whole chain.
- **The completed transfer.**  `q'(i -> j) = T_ij f_j / (2 f_i)` is
  per-state normalized exactly — all six rows sum to 1; the conflict
  row is `{0: 1/7, 3: 3/4, 5: 3/28}`.  This is the root-free
  completion, exhibited on the intrinsic chain.
- **Root = renewal.**  The root and the post-arbitration renewal point
  `H3 = [pA0, pB1, PAIR]` are the *same intrinsic state* (class 0) at
  every computable lookahead.  The root-free certificate of paper 30
  §5.6–5.7 is YES on this object: nothing distinguishes genesis from
  any completed-arbitration clean slate.
- **Mass transport.**  The left eigenvector `pi = (1/4, 1/4, 1/2)` on
  the dominant class satisfies `pi T = 2 pi` exactly, and the
  `pi . f`-weighted law is invariant under the completed `q'` with no
  residual — the unimodularity/mass-transport face of the invariance
  question, exact at state-chain scope.

### 3.5 The decision, and the one theorem between window and limit

**Residue 1 is DECIDED on every computed window at this grammar's
scope**: the Perron reduction holds on the generated grammar's own
intrinsic chain — existence and uniqueness of the strictly positive
harmonic solution, the root-free certificate answered YES, and the
invariance face exact.  The remaining distance to the all-depth
statement is a single named successor, the **renewal-pumping closure
theorem**: that the depth-window decision pumps to infinite volume
along the renewal structure.  Its two ingredients are exhibited and
receipt-gated:

1. the pad-shift identity (idle events never change any menu;
   family-wide, `12,942/0`) [EXACT];
2. the renewal subtree isomorphism: the base substitution
   `v0 -> v1` maps the root tree ONTO the subtree above `H3` —
   `215 == 215` nodes with menus event-bijective and weight-equal at
   every node including the depth-3 shell — together with the
   clean-slate census: all 144 renewal points at length `<= 4` carry a
   unique shared non-superseded base and root-identical one-step menus
   under their own base substitution [EXACT].

Between them, the intended induction is exactly this: that every deep
history is, up to idle padding and base renaming, a concatenation of
copies of the finite window already decided.  Assembling the two gated
identities into that induction is a theorem statement, not a
computation, and it is left open here.

At transport scope the picture will change and is declared: deliveries
reopen the absorbing sector (diverged holdings can reconverge), the
class structure of §3.2 is not stable under the transport grammar, and
the Martin-boundary machinery that this section did *not* need is
expected to become load-bearing exactly there (§7, successor 2).

## 4. The arbitration operator exists at fixture scale

### 4.1 The pincer and its two escape classes

Paper 30 §6.2 pinned the arbitration-layer lift behind three horns: a
cut-independent step operator reproduces the arbitration-killing zero
class; a cut-dependent operator requires carrier overlap with the blind
wire; a dilation re-imports the cut data in ancilla form.  The horns
classify *channels attached to cuts*.  Consensus-impossibility theory
[LITERATURE: Fischer–Lynch–Paterson; Ben-Or] offers exactly two escape
shapes from trichotomies of this form, and higher-order quantum
operations [LITERATURE: process matrices, quantum combs] supply the
second's natural formalization:

- **E-A (randomized):** the operator applied depends on a recorded coin
  local to the initiator;
- **E-B (component-typed):** the operation's *type* takes the conflict
  component itself as a classical input wire — a family `{V_C}` indexed
  by the join's own past-visible component.

The fixture is the committed pair exhibit: at the early cut the
initiator's arbitration menu is one event (the self singleton; full
sum 1); at the join it is three (self plus the two pair winners; full
sum `5/4`); the pair branches are inadmissible early [EXACT, the
committed baseline re-run].

### 4.2 The randomized class is excluded by visibility [THEOREM at fixture scale]

The initiator's *own view* — the record data any initiator-local family
can read — is identical at the two cuts, while the required arbitration
branch sets differ.  Hence any family measurable in (own view, private
randomness) emits the same output distribution at both cuts and cannot
reproduce both committed menus; this covers coins, local clocks,
counters, and any data carried on the initiator's wire (the view
equality is stable under inserting extra initiator ticks).  Any repair
must read the join — which relocates the cut-dependence into the firing
predicate and lands the candidate in E-B's typed-input form.  The
consensus analogy is thereby *partial* in a precise sense: the
obstruction here is **visibility, not adversarial scheduling** — the
grammar has no adversary, and randomization, which defeats the
scheduling obstruction elsewhere, does not touch this one.

### 4.3 The constructed family

The E-B candidate is not a type ascription; it is two matrices, built
from committed machinery only (the join-typed opening click and the
acceptance step of the elementary-click refinement, composed as one
typed higher-order operation `V_C = Acceptance o OpeningClick`)
[EXACT throughout, isometries at `1e-40`]:

- `V_single` (`2 x 1`) on singleton components; deterministic branch;
- `V_pair` (`4 x 1`) on the 2-conflict:
  `|rec> -> sum_w sqrt(1/2) |rec, w, v(C, w)>`;
- both factors of each are isometric, and the composed maps are
  isometric (defect `0.0` at the working precision);
- **Born = the committed kernel:** the squared branch amplitudes of
  `V_pair` are exactly `1/2`–`1/2` (the `K1` law on the 2-conflict,
  recomputed from the layer) and `V_single`'s branch is deterministic;
- **menu reconstruction at both cuts, exact in rationals:** (the
  past-local admissible-component index) `x` (the budget share
  `1/4 / #components`) `x` (the `V_C` Born splits) equals the committed
  menus — `1/4` on the arbitration sector at the early cut;
  `1/4 + 1/8 + 1/8` at the join;
- **cut-independence where it belongs:** the *same* matrices serve at
  both cuts.  The family is indexed by the component only; what
  differs between cuts is the classical index set (`{singleton}`
  early; `{singleton, pair}` at the join), and that index is computed
  by the admission relation from the event's own past cone — it is
  past-local event data, not cut data.  The record side is honest: the
  two winner-version records of `V_pair` are orthogonal outputs,
  acceptance preserves probe distances, and the lossy control fires.

**Residue 2's operator-existence face is discharged at fixture scale
by this family.**  The three horns of the pincer stand, scoped to
cut/initiator-attached channels; the constructed family lives in the
typed class the horns do not reach, and it is not a fourth door around
them — it is the explicitly open arm of the carrier-overlap horn, now
typed past-locally and exhibited.

### 4.4 The price, and what remains open

Two faces remain open, and they are *separate*:

- **The foliation face.**  `V_pair` acts on both proposers' records —
  licensed join carriers under the grammar's own carrier discipline —
  so commutation-by-disjointness is forfeited at joins: concurrent
  arbitration operations commute only on disjoint components.  Whether
  the family extends to a commuting/foliation-consistent assignment at
  overlapping visibility is the standing arm of horn 2, untouched.
- **The measure side.**  Each `V_C`'s Born split sums to 1 exactly, so
  the operator carries the branch *split* only; the family's weights
  across components remain the classical `q`'s.  The completion
  question does not vanish into the operator — it stays where §3 put
  it: residue 1.

Scope: the pair-plus-path fixture, grammar = the terminal admission
layer plus the click refinement; realized component sizes at the two
cuts are exactly `{1, 2}` and the constructed family covers precisely
those; no continuum claim; localization pathologies [LITERATURE:
Hegerfeldt] untouched.

## 5. Transport generates dimension

### 5.1 The instrument, ported

The dimension instrument is the corpus's own theorem-backed pair (v8
papers 12–13): the *canonical star discrepancy* `D*` — exact over the
rank embeddings of a finite order, in rational arithmetic — and the
`dim <= 2` gate by transitive orientability of the incomparability
graph, with the two-clock certificate (the `b`/`chi` rank pair) on
passes.  Port fidelity is anchored before any new content: the
three-crown `S3` (six elements) is REJECTED by `dim <= 2`; all 219
labeled 4-element posets and all 4,231 labeled 5-element posets pass
(the gate is provably vacuous below six elements); the full
120-permutation `n = 5` `D*` range is exactly `[1/4, 7/20]` [EXACT].

Honest floor: at `n <= 4` the `D*` landscape spans `[19/64, 25/64]`
(mean `4735/14016 ~ 0.3378`), the generated depth-4 family's
mu-weighted mean is `23595/66368 ~ 0.3555`, and the family mass at or
below the landscape median is `512/1037 ~ 49.4%` [MEASURED] — 
descriptors only; at these scales no manifoldlikeness verdict is
possible and none is made.  The statistical layer has zero qualifying
pairs and is disclosed as not run.

### 5.2 Width is the capability axis

The committed constructed chains — the deep exhibits that carry the
grammar's physics beyond enumeration reach — all pass `dim <= 2`,
with the `(b, chi)` two-clock certificate computed in-receipt and
exact canonical `D*` values (`SIG_KR: 23/64`; `h5: 1/4`;
`SIG_FM: 79/256`; `CH: 83/400`; `h11: 1/4`; `h12: 145/576`) [EXACT].  But this sample cannot decide
the dimension question, for a structural reason: five of the six have
order width `<= 2` (widths `2,2,2,3,2,2`), and **an order of width
`<= 2` has dimension `<= 2` by theorem** [LITERATURE: Dilworth-type;
the receipt gates the widths].  Event count is not the axis on which
dimension can appear; *actor width* is.

### 5.3 The witnesses

Two transport configurations decide the question [EXACT]:

- **W6.**  Six delivery events among six actors, arranged so that each
  is admissible with weight exactly `1/20`, produce the event poset
  with predecessor structure `[[], [], [], [1,2], [0,2], [0,1]]` —
  *exactly the three-crown `S3`*, the standard minimal example of
  dimension 3 (minimum size; not unique at six elements — `W4` below
  is a second).
  The generated grammar, running pure transport at width 6, realizes
  a minimum-size order of dimension 3.  It FAILS `dim <= 2`.
- **W4.**  Four actors (the widest committed chain's own actor count)
  suffice: a six-event configuration with exact admissible weights
  `(1/12, 1/12, 1/8, 1/8, 1/12, 1/12)` and predecessors
  `[[], [], [0], [0], [0,1,2], [0,1,3]]` FAILS `dim <= 2`.

With the sharp negative side — two actors never leave dimension 2 (a
width theorem, before any run), and three actors stay two-dimensional
through ten events — the thresholds are:

| actor width | dimension behaviour |
|---|---|
| 2 | `dim <= 2` always [THEOREM via width] |
| 3 | `dim <= 2` through 10 events [EXACT, cited to the committed campaign record] |
| 4 | first failure at 6 events (`W4`) [EXACT, receipt-gated] |
| 6 | `S3` realized by pure transport (`W6`) [EXACT, receipt-gated] |

**Transport generates dimension** — the decision of Q4, positive.  The
grammar's record orders are not intrinsically two-clock; they are
two-clock exactly as far as their width confines them, and transport
across four or more actors is a dimension-generating mechanism with
sharp finite thresholds.

### 5.4 The residual corner, provably minimal

One channel remains untested, and it is provably the only one:
multi-author arbitrations.  The candidate alternative corner
(version-name recurrence) is mechanically vacuous — exact-repeat
arbitrations and same-base re-proposals are inadmissible under the
grammar — so "multi-author arbitration events" is exactly the residual
scope [EXACT, cited to the committed campaign record].  Whether arbitration alone (no transport) can
also generate dimension is open there and named in §7.

## 6. What the four decisions jointly establish

**The foundation holds — and the sense in which it holds is sharper
than hoped.**  The identified law's coefficient anchor was exposed to
its regulator alternative and survived at the level that matters: the
operator ray is rule-independent; everything rule-relative is a scalar
constant, including its sign.  The practical rule for the whole series
is now explicit: lattice defect *normalizations* are regulator data and
must be cited with their rule; identified *operators* are not.  And the
sign flip closes a tempting explanation — bracket-ray universality is
a fact without a shared-generator mechanism behind it at this order.

**The completion problem's core is decided at its natural scale.**  The
program's deepest analytic residue was posed on a 17-state object that
turns out to be a horizon-stratified image of a six-state one.  On the
right object the entire Perron package is rational, exact, and unique,
the root-free question is answered YES (root = renewal), and the
invariance face closes with an exact mass-transport identity.  What
separates the window decision from the infinite-volume statement is one
theorem whose two ingredients are already receipt-gated.  Residue 1 has
moved from "open analytic problem" to "one named induction away, with
the induction's premises machine-checked."

**The quantum completion now starts from an object, not a hole.**
Paper 30 ended the quantum side at a pincer.  This paper adds the two
missing classifications: the randomized escape is excluded by a
visibility theorem (and the exclusion is *informative*: this grammar's
obstruction is visibility, not scheduling — a genuine disanalogy with
consensus impossibility), and the typed escape is realized
constructively by an isometric, Born-correct, cut-independent family
indexed past-locally.  The remaining quantum questions are now
concrete: the foliation/commutation face of an operator that exists,
and a measure side that §3 already owns.

**Dimension is generated, not assumed.**  The series' standing
embarrassment — a program about records in spacetime with no positive
statement about dimension at the identified-law grain — is replaced by
a mechanism with sharp thresholds: width-2 confinement is a theorem,
width-4 transport breaks two-dimensionality at six events, and width-6
pure transport realizes the standard minimal example of dimension 3
exactly.
The two-clock certificates of the committed chains are re-scoped
honestly as width-limited facts, not evidence about the grammar.

Jointly: every load-bearing joint interrogated held or improved.  None
of the four decisions weakens a committed claim; two of them
(§3, §5) supersede committed *scaffolding* (the 17-state carrier, the
pilot's null) in the direction of more structure, and the remaining
supersessions (§1.3) are scope sharpenings — the pincer's channel
scope, §8.3's premise, §7.2's axis — under which the committed
conclusions stand.  All five are stated in §1.3 and gated in the
receipts.

## 7. The residue ledger, updated

Paper 30's six residues, with their post-campaign state, followed by
the items this paper opens.  Entry conditions are unchanged unless
restated.

**1.  The infinite-volume positive-harmonic core — now: the
renewal-pumping closure theorem.**  Decided on every computed window on
the six-state intrinsic chain (§3): `lambda = 2` rational, positive
harmonic vector unique up to scale, root-free certificate YES,
mass-transport exact.  The residue *is now a single theorem*: pump the
window decision along the renewal structure.  Both ingredients
(pad-shift identity; renewal subtree isomorphism with the 144-point
clean-slate census) are receipt-gated.  Entry condition: prove the
induction; no computation remains at this scope.  Separately declared:
at transport scope the absorbing sector reopens and the class structure
changes — the Martin-boundary machinery is expected to become
load-bearing there, and nothing from §3 transfers without proof.

**2.  The arbitration-layer lift — now: the foliation face of an
existing operator.**  The operator-existence face is discharged (§4):
`{V_single, V_pair}` constructed, isometric, Born-correct,
cut-independent with past-local index.  What remains: (i) the
foliation/commuting-family face at overlapping visibility (concurrent
arbitrations commute only on disjoint components); (ii) the per-type
reception census carried by the lift campaign (which record types can
receive which operations — the obligation is recorded and untouched
here).  Entry condition for (i): a commutation or consistency theorem
for the family at overlapping joins, or a counterexample forcing a
foliation choice.

**3.  Fine-versus-coarse sealing.**  Unchanged (empirical; the exact
`1/6`-versus-`0` order-coherence discriminator stands).

**4.  Mid-chain drift.**  Unchanged.

**5.  Grammar breadth.**  Unchanged in both halves (carrier-level third
grammar; superset-generator condition at transport depths) — and
sharpened by §5.4: the multi-author-arbitration corner is now the
*provably minimal* untested dimension channel.  Entry condition
addition: decide whether arbitration alone generates dimension.

**6.  `h12` dead-component pricing.**  Unchanged.

**New items opened by this paper:**

**7.  The finite-slab smeared theorem and `kappa(m)`.**  Promote §2.4's
fixture-scale ray collapse to a theorem for log-smeared test functions
on finite slabs; compute `kappa(m)` as an exact function and locate its
zero crossing — the mass at which the LT rule's identified transport
operator vanishes at this order (the sign flip's guaranteed crossing),
a distinguished point of the fixture.  (Whether `kappa(m) = 1` —
agreement with the EXC constant — occurs anywhere is a separate
question, not established either way by the record.)
Entry condition: the slab construction over the committed series
machinery; then the interacting-fixture cross-check.

**8.  Transport-scope invariance.**  Re-pose §3 on the transport
grammar (deliveries reopen the absorbing sector).  Entry condition: the
intrinsic partition of the transport family, its transfer, and the
Perron/Martin package there; expect genuinely new structure — this is
where the boundary machinery earns its import.

## 8. Methods note

Every quantitative claim above is bound to a committed
exit-1-by-design receipt, deterministic (rerun byte-identical; the
d43b/d43c pair additionally verified byte-identical across hash seeds
0/7/11/61), adversarially reviewed to terminal status through the
campaign's standard cycle, and stated at its as-run precision: exact
rational arithmetic (`Fraction`) wherever grammar weights, transfers,
spectra, and menus appear; series arithmetic at `mpmath dps 50`
against `1e-30` gates for the order-12 defect ladders of §2, with the
coefficients' exact rational values confirmed by the frozen round's
independent exact-rational rebuild — no fitting anywhere; `mpmath dps
50` against `1e-40` for the d43c isometry layer, where the measured
defects are zero at working precision.  The exact-`Fraction` layers
behind the gates carry no tolerance at all.  The receipt surface of
this paper:

```text
v10/code/d43a_lie_trotter_exact.py    PASS  7/7   rule-independence (S1)
v10/code/d43b_state_chain_exact.py    PASS 18/18  the six-state chain (S2)
v10/code/d43c_pincer_escapes_exact.py PASS 11/11  escapes + the operator (S3)
v10/code/d43d_dstar_generated_exact.py PASS  6/6  D* + dimension (S4)
```

Outputs are committed under `v10/data/` with matching names.  The d43b
and d43c receipts embed the terminal admission layer by executing the
committed placement receipt (path-anchored); the d43d receipt ports the
v8 instrument algorithms code-faithfully and re-anchors their committed
facts in-receipt before any new content.  Negative controls are gated,
not narrated: the depth-marked quotient computation (§3.1) runs inside
the d43b receipt with its stratification map as a pass/fail gate, and
the width diagnostic (§5.2) is a gate of the d43d receipt.  Claims
cited to the committed campaign record rather than a receipt say so
in-line (the depth-7 confirmation in §3.2; the frozen review files for
the independently re-derived certificates).

## 9. Coda

Paper 30 ended by naming the next object: "whichever of residues 1 and
2 yields first."  The honest answer turned out to be *both, and neither
alone*.  Residue 1 yielded once the question was moved off a
truncation-stratified carrier onto the six-state object the grammar
itself defines — and there it yielded completely, up to one induction
whose premises are machine-checked.  Residue 2 yielded its existence
face to a two-matrix construction that was always available to the
grammar's own click layer, once the escape classes were separated by
what they can *see* rather than what they can compute.  The two
foundations questions the ledger did not rank — the regulator
dependence of the oldest anchor, and dimension — resolved in opposite
directions to the cautious expectation: the anchor is safer than
claimed (rule-relative only in a scalar), and the record orders are
richer than measured (dimension appears the moment transport has four
actors to work across).

The program's nearest open surface is three theorems and a census —
pump the window (residue 1), commute the family (residue 2), smear the
slab (`kappa(m)`), count the reception types — with the
arbitration-alone dimension question, transport-scope invariance, and
the ledger's standing empirical and breadth items behind them.  Every
one has its premises, its fixtures, or its instrument already
committed.  That is what this campaign was for.

## References

1. Relativistic ISP v10 Paper 30, *The generated record and its
   completion*.
2. Relativistic ISP v1 Paper 1 (the free-core fixture and the collar
   admission rules; the deferred Lie–Trotter comparison); v1 Paper 8
   (the `lambda`-family within-class result); v2 Paper 1 (the
   one-region coefficient anchors and the continuum identification).
3. Relativistic ISP v8 Paper 12, *The 2D sufficiency theorem: a
   canonical realizer, the rank embedding, and an intrinsic
   quantitative characterization of volume-faithful record orders*;
   v8 Paper 13, *Do records measure positive?  The two-clock frame,
   the natural-supply sweep, and the Gibbs reconnaissance* (Theorem
   2.1: two-clock universality, the `b`/`chi` ranks).
4. D43 campaign pins and terminal notes,
   `note-d43-corpus-audit-and-the-next-step.md`,
   `note-d43a-lie-trotter-rule-independence.md`,
   `note-d43b-martin-boundary-unimodularity.md`,
   `note-d43c-pincer-vs-escape-classes.md`,
   `note-d43d-dstar-on-generated-families.md`,
   `note-d43e-n-program-synthesis.md`; frozen review records under
   `v10/reviews/`.
5. D42 campaign pins (the generated grammar and its receipts), cited
   through paper 30 §9.1.
6. M. J. Fischer, N. A. Lynch, and M. S. Paterson, "Impossibility of
   distributed consensus with one faulty process," *Journal of the
   ACM* 32 (1985), 374–382.
7. M. Ben-Or, "Another advantage of free choice: completely
   asynchronous agreement protocols," *Proc. 2nd ACM PODC* (1983),
   27–30.
8. O. Oreshkov, F. Costa, and Č. Brukner, "Quantum correlations with
   no causal order," *Nature Communications* 3 (2012), 1092;
   G. Chiribella, G. M. D'Ariano, and P. Perinotti, "Theoretical
   framework for quantum networks," *Physical Review A* 80 (2009),
   022339.
9. J. L. Doob, *Classical Potential Theory and Its Probabilistic
   Counterpart*, Springer (1984) — the `h`-transform; the Martin
   boundary for positive harmonic functions.
10. D. Aldous and R. Lyons, "Processes on unimodular random networks,"
    *Electronic Journal of Probability* 12 (2007), 1454–1508 — the
    mass-transport principle.
11. K. G. Larsen and A. Skou, "Bisimulation through probabilistic
    testing," *Information and Computation* 94 (1991), 1–28.
12. B. Dushnik and E. W. Miller, "Partially ordered sets," *American
    Journal of Mathematics* 63 (1941), 600–610 — order dimension; the
    standard example `S3`.
13. G. C. Hegerfeldt, "Remark on causality and particle localization,"
    *Physical Review D* 10 (1974), 3320–3321.
