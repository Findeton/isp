# The D44/D45 campaign synthesis: eight terminals

**v10 — synthesis note (no new receipts; every claim carried by a
terminal unit's receipt and cited as such; the d44b delta's
synthesis flags are BINDING on this note's wording).**  Status:
closes the user-mandated successor campaign ("after those paper 31
reviews, work on all the 1-6 successors" + the D45 pair authorized
at #356).  Units: D44a #368, D44b #374, D44c #355, D44d #353,
D44e #364, D44f #372, D45a #362, D45b #367 — all through the full
cycle (pin -> receipt -> hostile round -> repairs -> delta ->
terminal).

## 1. The four joint achievements

**1. Residue 1 — decided at every verified depth, one lemma from
closed.**  The sigma abstraction is menu-exact and
transition-deterministic with ZERO exceptions over 179,783
histories (exhaustive through depth 7, receipt-carried); its
depth-free BFS closes at 36 states whose exact per-candidate
bisimulation quotient is the committed six-state chain with
transfer T_REF; the full Perron package holds on the quotient
(D44a).  The all-depth conclusion is EXACTLY conditional on the H1
structural lemma (menu factorization from sigma at all depths,
despite the own-view lag) — residue 1's final named gap — and the
theorem's boundary is EXACTLY the delivery-free grammar: at
transport scope closure is OPEN (D44b: the intrinsic chain is
window-consistent at six len <= 2 classes — an echo of, not
identical to, the d42a six — but ESCAPES its windows; escape, not
non-stabilization; no depth-5+ prediction is made).  The reopening
prediction is CONFIRMED in-family (1,044 diverged histories; 124
reconverging (history, delivery) pairs over 84 distinct diverged
prefixes; 4 distinct minimal 3-event chains at exact weight 1/256;
every pair-history ends non-diverged): the d42a absorption is a
deliverylessness artifact, in the precise sense that the diverged
class's delivery row carries weight into a non-diverged class at
the tested cap.

**2. Dimension — fully mechanized, ceiling-free, doctrine-scoped.**
Arbitration alone cannot generate order dimension (the six-clause
component-confinement law; the funnel lemma making the S3 pattern
impossible at EVERY width and depth; 1,124,884 distinct histories,
zero failures — D44c); transport realizes S_n as an induced
subposet at every tested n by ONE uniform courier-firewall
constructor with the all-n schema proof note (D45b) — unbounded
order dimension, the NECESSARY condition for any round-cone
(>= 2+1) reading, as ORDER dimension only under the binding §1
doctrine (order dimension = a 1+1-escape detector and
clock-complexity grade, never a spacetime-dimension estimator; the
physical ladder = Minkowski-dimension certificates + statistical
estimators, successors).  The schedule resolution: the
Charron-Bost crown dies under every schedule, her sends-first
schedule is inexpressible under fused deliveries, the dimension
route on her multiset is open at N >= 4 (248/40,320,
receipt-gated), and idle marks are load-bearing.

**3. The quantum layer — welded to the classical completion.**  The
reception census is complete at fixture scope (11 types derived
from the grammar; 6,567 instances; per-type imprint anchors; the
reception-DYNAMICS arm typed and open — D44e).  The foliation face
resolves to a SINGLE record datum — the initiator tag of the join
arbitration — with initiator-ERASED content order-invariant and
the residual datum itself record data (D44f).  The operator family
cannot carry a measure tilt in its squared-amplitude content (the
sign/phase gauge sector, zero |amp|^2 content, is the surviving
freedom; its {+-1}^3 exhaustiveness characterization is
referee-carried); the measure side reduces arithmetically to
residue 1's within-cut object, and with D44a terminal the family's
completed cross-component weights are FORCED to the decided
completion's SECTOR CONDITIONAL (1/2, 1/4, 1/4 — the
horizon-stable object, gated at two horizons) at verified-depth
scope; the all-depth form is conditional on H1 (D44f).

**4. The foundation — regulator-closed for all masses.**  The slab
smeared theorem holds at fixture scale (slab-width independence of
the ray and constant; D44d), and kappa(m) = (9m^4 - 15m^2 + 4)/144
= (3m^2 - 1)(3m^2 - 4)/144 is DERIVED as an exact all-m polynomial
identity with the factor origins channel-resolved (m^2 = 1/3 a
per-channel kill; m^2 = 4/3 combination-only; D45a).  The
interacting divergence is grain-inherited (g = 0 concurs);
ray-level arrival under interaction is undecided — the smeared
interacting identification with a g = 0 column is the successor.

## 2. The meta-record

Across all eight units and their rounds: hundreds of independently
recomputed quantitative claims, ZERO false numbers.  What the
rounds corrected, repeatedly and productively: over-broad
quantifiers (the D44a all-depth claim -> every-verified-depth; the
D45b dimension-silence -> schedule-specific; the D44f
foliation-invariance -> initiator-erased invariance; the D44b
census subset phrasing), vacuous gates (four check(True)-class
convictions across the campaign, each rebuilt with demonstrated
teeth and each ownership logged), and print-only facts promoted to
anchors.  Every reversal made the record sharper; the ledger's
forward-correction discipline carried each by name (#366, #365,
#370, #373).  Warnings binding on successors: no citation of d44b
as proof the per-candidate distinction bites at transport scope
(extensionally null at cap); purity gates use the allow-list walk
(#362); dimension witness branches must be genuine exit-0 outcomes
(#354).

## 3. Successors (named, not started)

1. **The H1 structural lemma** — residue 1's final gap; discharging
   it makes D44a's six-state decision and D44f's forced weights
   all-depth at d42a scope.
2. **[I1] Martin/R-theory at transport scope** — the escape
   structure (D44b) is exactly the regime the import was for.
3. **The physical dimension ladder** — Minkowski-dimension
   (circle/sphere-order) certificates and the statistical
   estimators under the completed measure (typicality: does the
   measure favor the courier structures it prices at 1/212-class
   weights?).
4. **The smeared interacting identification (+ g = 0 column)** —
   whether ray universality survives interaction.
5. **The reception-DYNAMICS arm** (D44e's typed-open face) and
   horn 2's remaining commutation questions beyond fixture scale.
6. **The d42b2 embedded-head reconciliation** — the standing corpus
   obligation (#363): the click layer's embedded transport text is
   pre-#300 (D2H 1/16 vs terminal 1/24).

The paper-32-class decision (whether D44/D45 becomes the next
paper), the publication route for papers 30/31, and the N5
housekeeping batch remain the user's.
