# D65 — THE DESCENT CONDITIONS: does the generated law satisfy what the action line requires of a record measure? (PIN)

**Status:** PIN, STRICT, 2026-07-26.  Parents: paper 29 ("Where the
action cocycle lives" — Theorem 1 refined cylinder descent, Theorem 2
finite boundary sufficiency, §4.3's five durable-record hypotheses,
the F1/F2 falsifiers), D59 (the two click-law objects meet at ONE
MISSING MAP), the CLOSED generated law (D44a unconditional at
two-actor delivery-free d42a scope: sigma's 36 states, the menu law
menu = G(sigma), (H2)'s update table — #460), the quarter law
(per-state menu masses 2 or 5/2).  User authorization: the
construction order "wide crystal → cocycle → DESCENT-CONDITIONS"
(#453; "yes after the cocycle continue with the descent-conditions
unit").

## 1. The question

Paper 29 states what ANY record process must satisfy for the action
line's conditional-measure reading to descend to it.  The generated
law now exists as a THEOREM at its scope.  **This unit gates the
generated law against paper 29's descent conditions, one by one.**
Each condition that HOLDS is a plank of the missing map laid from the
generated side; each that FAILS localizes exactly what order/record
data the map must carry; each that is UNDEFINED without supplied
structure sharpens what remains missing.  No transfer to the
identified click law is claimed either way — this is the generated
side of the gap, measured against the action side's stated
requirements.

## 2. The conditions, instantiated on the generated law

The generated process at closed scope: states sigma (36), next-event
law = the menu (exact weights; per-actor masses {1, 5/4}, totals
{2, 5/2}).  The PROBABILITY kernel is the normalized menu
(p(e|sigma) = weight/mass — the normalization is printed and every
downstream number is exact in Fractions).

- **DC1 — THE COMMUTING-SQUARE IDENTITY (paper 29 Theorem 1 / F1).**
  Define: events a, b COMMUTE at H iff both orders are admissible
  (a at H, b at Ha; b at H, a at Hb) and sigma(Hab) = sigma(Hba) —
  by (H1)+(H2) sigma equality makes the two continuations carry THE
  SAME entire future law, which is the generated analog of paper
  29's record-cylinder identity [Hab] = [Hba].  THE TEST:

      P(a|H) · P(b|Ha)  =?  P(b|H) · P(a|Hb)

  exactly, over EVERY commuting pair at every history of the
  exhaustive family (depth ≥ 6), P the normalized kernel.  **This
  can genuinely fail**: the per-state masses vary (2 vs 5/2), so the
  normalized products are not obviously equal even when the raw
  weights are.  If it HOLDS everywhere: the generated law descends
  to an order-quotiented record measure — Theorem 1's hypothesis set
  is satisfied from the generated side (a plank of the map).  If it
  FAILS: the failure census (which pair classes, which sigma states,
  the exact ratio defect) is the deliverable — the generated law is
  order-weighted, and the map must carry order data.  BOTH outcomes
  pre-registered as results.  Also censused: pairs where both orders
  are admissible but sigma differs (non-commuting; excluded with
  count printed) and pairs where one order is refused (the
  admissibility asymmetry census).
- **DC2 — BOUNDARY SUFFICIENCY (paper 29 Theorem 2 / F2).**  With
  pi = sigma, fibre constancy of the next-record law is EXACTLY (H1)
  (+ (H2) for the state update).  This is NOT a new result and the
  receipt must label it a RESTATEMENT gate: its content is the
  IDENTIFICATION — the generated line's declared boundary statistic
  exists and is sigma; the 36-row kernel K(.|sigma) is printed.
  Paper 29's Theorem 2 requirement "a condition on the declared
  boundary" is met by a THEOREM, not a supplication — the first
  supplied-not-derived item of D59's list (the boundary state) is,
  at this scope, derived.
- **DC3 — THE FIVE DURABLE-RECORD HYPOTHESES (paper 29 §4.3).**
  Gated one by one on the generated law: (1) exclusive & exhaustive
  alternatives (the menu is a partition of the admissible
  continuations; masses printed); (2) decoherence of the queried
  algebra — the generated law is CLASSICAL (a stochastic process on
  records), so decoherence is trivially satisfied AND trivially
  uninformative: the receipt must say this is where the map's
  remaining segment lives (the generated line has no functional
  level YET — no class operators, no Gram functional — and this
  unit does not create one); (3) common refined cylinder — DC1's
  commutation census; (4) positivity of every displayed conditioning
  cylinder — every menu weight > 0, gated exactly; (5) sufficient
  declared boundary — DC2.
- **DC4 — THE SUPPLIED-VS-DERIVED LEDGER, UPDATED.**  D59 names six
  supplied-not-derived items.  The receipt ends with the ledger
  re-scored at closed scope: which items this unit's gates move from
  supplied to derived (candidates: boundary state via DC2; parts of
  the measure via the closure), which stand (renormalization,
  record instrument, clock dictionary, the functional level), with
  one-line grounds each.  No item moves without a gate above it.

## 3. Receipt requirements

Layers by AST/text-slice from committed sources ONLY (d42b3
candidates_for/admissible, d44a sigma/canon machinery, d62's TABLE if
the update is consulted); anchors 36 states / 176 keys; the
exhaustive family depth ≥ 6 with depth printed; exact Fractions
end-to-end; every census printed both ways; no invented thresholds;
exit 0 for substantive negatives (a DC1 failure IS a deliverable),
exit 1 only on anchor breakage; determinism gated if any set
iteration is load-bearing; no bare-constant predicates.

## 4. Pre-registered lean and falsifiers

**Lean (stated before any computation):** DC1 likely HOLDS for
disjoint-actor propose pairs by symmetry, and the genuine risk sits
at pairs whose intermediate states have DIFFERENT masses (a propose
composed with an opponent arb: masses 2 vs 5/2 on the two paths).
If DC1 fails exactly there, the result is still structural: the
failure is a mass-ratio cocycle and its pattern should be a function
of sigma alone — check and report.  F-DC1: any commuting pair with
unequal products.  F-DC2: none (restatement; mislabeling it as new
is the falsifiable sin).  F-DC3: any hypothesis gate that cannot
fail must be labeled reporting-only.  No verdict sentence exceeds
the exhaustive family and the closed scope.

## 5. Scope

Two-actor delivery-free d42a — the closed scope, and only there.
The unit measures the GENERATED side against the ACTION side's
requirements; it does not construct the functional level, does not
identify the two measures, and does not touch transport.  The
missing map remains missing where the ledger says so; this unit's
product is the first measured segment of it.
