# D67 — THE k = 4 DOUBLE GRID: sprinkling-grade width, or the reason why not (PIN)

**Status:** PIN, STRICT, 2026-07-27.  Parents: D66 (TERMINAL — the
width ceiling is `k·b ≤ k²`, saturated at 9 by DOUBLE-GRID(3,R);
crossed conflict is the mechanism; the flagship is in-band at d = 3
with max |D| = 9), W4c (the dead-wire theorem: width is bought by
proposer count), the committed instruments (d58 atlas, d47a sky,
d64/d66 C7 coboundary, d55c sprinklings).  Book ranking #1 (#473).

## 1. The question

The genuine sprinkling range is `max |D|` 10–17 at d = 2.  A k = 4
double grid bounds at `k² = 16` — inside that range for the first
time.  **Does the crossed-conflict mechanism reach sprinkling-grade
width, and can it stay band-uniform while doing it?**

## 2. The objects

- **DOUBLE-GRID(4, R)** per D66's committed blueprint generalized to
  4-proposer arbitrations on both axes (rows and columns of a 4×4
  actor grid conflicting concurrently); the bootstrap and rotation
  schedule are the worker's to design under C1-grade forcedness,
  conflict share printed, in-round deliveries counted.
- **Sweeps:** R swept ≥ 4 values; the (3,R) family re-run as the
  committed anchor; a (5,R) probe if runtime permits (bound 25).
- **Controls:** the committed sprinklings (both dims), DR(8,10,8),
  DOUBLE-GRID(3,R) — all re-run in-process.

## 3. Gates

- **K0** anchors: committed layers by AST; DOUBLE-GRID(3,2/4)
  reproduces D66's committed row EXACTLY (exit 1 only on anchors).
- **K1** forcedness at C1 grade (full-menu replay on the headline
  record; a refusal/ambiguity is a first-class finding — the
  4-proposer schedule may genuinely not force; two variants tried
  before concluding).
- **K2** the atlas census at d = 2 AND d = 3, both orderings,
  controls beside; the interior control on headline records; W4c's
  bound with the record's own measured `k·b` gated.
- **K3 THE WIDTH VERDICT:** `max |D|` against the sprinkling range
  10–17 and the bound 16.  Either answer is the result.  Any
  `|D| ≥ 10` witness verified event-by-event against the committed
  sky() and poset, with P-paths printed (the D66-round standard).
- **K4** the band question at both depths: is any k = 4 record
  in-band while wide?  (D66's flagship achieved it at d = 3 with 9;
  the question is whether it survives k = 4's sparser arb share —
  the budget bound gives ≤ 1/5 for conflict groups.)
- **K5** the coboundary gate (C7 both routes, the convention
  battery) on any wide record — the gauge question rides along at
  zero marginal cost; no lean either way.
- **K6** no invented thresholds; censuses printed; substantive
  negatives exit 0; determinism gated.

## 4. Pre-registered outcomes

- **K-I:** the 4-proposer schedule refuses to force — the break
  mechanism is the deliverable (4-way conflicts may demand more
  supply than rotation can provide).
- **K-II:** forces, but `max |D|` stays below 10 — the gap between
  the k² bound and realized width is the finding; characterize which
  successor slots go unfilled.
- **K-III:** `max |D| ≥ 10` — SPRINKLING-GRADE WIDTH FROM PURE
  INTERACTION; then K4 decides whether uniformity survives, and K5
  whether the class is (again) trivial.
- **Lean:** none on the width verdict — D66's round showed schedule
  choice dominates outcomes in this family and the pin declines an
  intuition it cannot ground.  On K4 the lean is honest: the budget
  bound thins conflicts as k grows, so band membership should get
  HARDER; if it fails, the width-uniformity frontier at growing k is
  itself the result.

## 5. Scope

Grammar layer; swept family only; scale doctrine (#440); no measure
claim at transport scope; width claims carry the record's own bound;
gauge claims carry the convention battery; the missing map is not
touched.
