# D12 geometry gate — why no new cone score is licensed

**Status:** final pre-review disposition, 2026-07-11.

## 1. Frozen rule

D12 gate U10 permits a cone, dimension, scale-ladder, or influence prediction
only after the generative packet is uniquely derived or selected on independent
data.  D12 proves that current SHARD/ISP principles leave inequivalent
whole-history measures and interaction couplings.  Therefore a favorable
geometry score cannot be used to choose among them without circularity.

## 2. Why the observable is not yet defined

The exact restored process supplies:

```text
typed connected collars;
local quantum interactions;
durable record instruments;
whole-history probabilities;
conditional next records;
construction-order gauge;
continuation.
```

It does not supply:

```text
a unique root/bridge/branch grammar for cosmological growth;
a unique interaction/action packet;
a record-intrinsic map from operator channels to spatial directions;
an evidence-to-proper-time or length scale;
a metric estimator proved stationary under the selected process;
an equality between algebraic Lorentz positivity and intervention reachability.
```

Consequently `F`, dimension, and physical cone roundness are undefined for the
universal D12 form.  This is stronger than “the simulation has not yet been
run”: different admissible primitive processes define different ensembles and
can define different geometry maps.

## 3. Existing numerical evidence, correctly scoped

- D8's chosen continuous leak shadow at `g=0.18` passed the strict `m4` shape
  convention but failed the strict `dom` conjunction.  It was promising, not a
  selected law.
- D9 used Bell data to select `theta=pi/4` within a partial-iSWAP preparation
  family and froze the extra identification `g=sin^2(theta)=1/2`.  The V9
  geometry holdout then failed shape, corrected dimension, and scale trend.
  This refuted that one-coupling bridge, not SCIR or the diamond architecture.
- D11's terminal global race became almost surely extinct.  Its algebraic cone
  containment was constructed and its sparse generated geometry was not a
  physical round-cone test.  D12 restores continuation and reclassifies D11 as
  a negative-control ablation.

No further dial scan is evidentially legitimate.  In particular, mapping the
two D12 iSWAP angles to V9's `g` would repeat the D9 assumption that the holdout
already refuted.

## 4. What a future licensed test requires

Before geometry resumes, a candidate must independently specify or identify:

1. the local field/process content and interaction couplings;
2. the root and all locally owned output/bridge opportunities;
3. the record instrument/decoherence criterion;
4. the relational conversion from record/evidence data to causal or metric
   observables;
5. a calibration test on synthetic spaces with known answer;
6. an untouched geometry holdout and a scale ladder.

Only then can the V9 measurements decide whether the selected process produces
approximately round causal cones and an effective `3+1` dimension.

## 5. D12 consequence

```text
GEOMETRY_AFTER-LAW GATE = CORRECTLY REFUSED
```

The refusal is itself a result: roundness and dimension cannot repair a
missing generative law, and a working record law cannot be promoted to the law
of our universe by choosing the geometry it happens to fit.
