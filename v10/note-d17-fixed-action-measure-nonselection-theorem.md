# D17 theorem draft — the causal action does not determine the history measure

**Status:** final after five hostile rounds at finite supplied-filtration scope,
2026-07-11.

## Exact counterexample

Fix the D16 interval action `S=N_0` and the two unlabeled four-element orders
`chain4` and `diamond4`.  Their exact action phases are

```math
(exp(i pi S(chain4)), exp(i pi S(diamond4)))=(-1,+1).
```

The executable separates the supplied boundary envelope, orbit square-root
factor and fixed action phase.  With a unit orbit factor and equal envelope the amplitudes are
`(-1/sqrt(2),+1/sqrt(2))`; the unrecorded alternatives cancel.  A real D14
isometry then copies the alternative into an orthogonal sealed record and
emits a live collar.  The record-extended branches decohere exactly and have
probabilities `(1/2,1/2)`.  The same erasure effect has probability zero before
the record and `1/2` after it, so the record changes a modeled interference
observable.  A protected future composes and preserves the
record.

Hold the action, alternative set, types and record instrument fixed, but use a
second positive-support normalized boundary envelope `(3/5,4/5)`.  The
recorded law is now `(9/25,16/25)`.  Therefore the fixed action does not determine the boundary state
or recorded probabilities.

## Orbit measure freedom

On the same two orders, uniform unlabeled weighting gives `(1/2,1/2)`, while
normalizing candidate inverse-automorphism weights gives `(2/3,1/3)` because
the chain and diamond automorphism counts are `1` and `2`.  D17 does not claim
either convention is forced.  Exact square-root orbit factors are propagated
through the D14 seal before normalization, rather than compared only on paper.
The difference shows that an orbit/groupoid
measure is additional action-packet data.

## Integrated projective causal histories

The second exact executable replaces the disconnected mark witness with one
owned causal-extension packet on a **supplied filtration**.  Its nodes are
actual orders:

```text
root1 -> chain2 / antichain2
      -> chain3 / V3
      -> chain4 / relabeled diamond4
      -> one declared maximal extension on each later branch.
```

The relabeled diamond has `V3` as its induced first-three-element suborder, so
the previously missing size-four edge is exact.  Executable `pi`/induced-order
restriction and `Ext(C)` checks connect every cylinder edge through depth six,
with a unique declared continuation giving the all-finite-depth induction.
Every node has an owned collar carrying the first branch bit.  Undeclared and
nonowner extensions reject; a two-owner join rejects without and passes with
the exact entitlement.  Joint canonical marked-edge keys quotient compatible
element relabelings while preserving the parent embedding and D16 boundary
metadata.  This is label covariance within the supplied filtration, not yet a
quotient over every construction filtration of the same completed order.

Three owner-local D14 morphisms seal `X`, fixed `Y=0`, and later `Z=X` while
emitting live collars and preserving all prior records.  The equal tower has
only

```text
000 with probability 1/2,
101 with probability 1/2,
```

so

```math
P(z=1|y=0,x=1)=1,
P(z=1|y=0,x=0)=0.
```

The second tower carries weights `(9/25,16/25)`, and the same local morphisms
propagate them exactly.  An inverse-automorphism packet propagates `(2/3,1/3)`
through the same records and causal tower; uniform labeled mass descends to
that groupoid ratio.  All three are projective and compatible with the same
fixed action and declared grammar; none is selected by that action.  A local
CPTP memory reset changes both later branches to `z=0`.  A separately
normalized inconsistent depth-two table fails the parent-cylinder equation.

This proves finite causal-extension/projective/non-Markov compatibility and
nonselection.  The later deterministic continuation rule gives a finite-
cylinder induction, but the rule itself remains supplied rather than selected
by the action.

## Theorem

**Finite fixed-action measure nonselection.**  On the frozen two-order class,
one fixed relabeling-invariant interval action, history domain and D14 record
instrument admit inequivalent positive-support normalized boundary/orbit
packets and projective finite-cylinder record families with different
predictions.  Hence
the causal action alone does not uniquely determine the complete history
measure or next-record law.

Candidate D17 result:

```text
CAUSAL-ACTION-TO-MEASURE-NONSELECTION.
```

The integrated packet closes the former causal-tower/grammar/local-memory
construction gap at finite scope.  It does **not** derive a universe law: the
record commit, extension grammar, branch kernel and deterministic continuation
remain supplied rather than action-derived.  No BDG quantum packet, continuum
geometry, scale, `G` or empirical holdout is present.  Therefore the D17
nonselection theorem may close after focused hostile review, while the full
thread objective remains open.
