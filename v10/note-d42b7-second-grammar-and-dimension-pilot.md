# D42b7 — the second grammar (the d41b Bin-T lift) + the dimension pilot

**Status:** CAMPAIGN PIN (strict), 2026-07-18. Base: d42a TERMINAL
(#289), d42b2 TERMINAL (#298). The d41b protocol binds: every fixture
result is Bin T until a SECOND GRAMMAR runs the same claims. This pin
executes that protocol and the paper-20 dynamic-adjacency pilot.
Receipt: `v10/code/d42b7_second_grammar_exact.py`.

## 1. The second grammar G3 [POSITED]

Identical carrier/event/budget structure to the d42a terminal grammar
(A1-A8 verbatim) with ONE structural change: the payload alphabet is
TERNARY, {0, 1, 2}. Conflict: same base, payloads DIFFER, events
incomparable (unchanged rule, larger alphabet). Everything else —
admission, budgets (propose 1/4 split over enabled (base, payload)
options, arb 1/4 join-view, idle absorber), kernels K1/K2, the
candidate relation — is the SAME TEXT. G3 therefore isolates the
alphabet as the varied structure (a minimal second grammar; a
carrier-level variant is a named future third).

## 2. Pinned predictions (FORM vs VALUE, stated before the receipt)

- **U1 (forms expected to LIFT toward Bin U):** opportunity
  generation (the pair/component iff-sweeps, extension closure,
  enabled-set gauge invariance); the 1+k/4 ladder with
  quarter-quantized component-additive blind density (the obstruction
  law is CARRIER-level, blind to the payload alphabet); ratio
  locality (L1); L1-uniqueness (the A8 proof never mentions payload
  count); per-observer fork-freeness in the delivery-free grammar.
- **V1 (values expected to be TOY-RELATIVE, Bin T by design):** the
  depth-2 conflict census (binary 4 → ternary 12: ordered pairs of
  same-base differing-payload seeds, 2 orders x 6 ordered payload
  pairs); mu(seed pair) (1/64 → 1/144: the propose split becomes
  1/4 / 3 = 1/12); the shape census (triangles REALIZABLE in G3 —
  d42b2's boundary control becomes in-grammar).
- **N1 (new structure, predicted):** G3 has KERNEL-DEGENERATE
  components: on the generated triangle (payloads 0,1,2) K1 == K2 ==
  uniform singletons at 1/3 — the kernels COINCIDE; discrimination
  survives via the still-realizable path (0,1,0). So the K1-vs-K2
  discriminating power is COMPONENT-SHAPE-DEPENDENT: a second-grammar
  fact invisible in the binary fixture, and a new observable for the
  selection question (paper 25's K-choice becomes undecidable on
  triangle-rich records).

## 3. The dimension pilot [PILOT — declared as such]

Paper 20's dynamic-adjacency fork: does GENERATED adjacency change
the effective dimension signature vs static? At fixture depths the
Myrheim-Meyer estimator is underpowered — this is a PILOT, not a
verdict: compute the mu-weighted ordering-fraction spectrum (comparable
pairs / all pairs) over the depth-capped binary and ternary families
and a STATIC-chain baseline; print [MEASURED, PILOT]; gate only
well-definedness (weights positive, fractions in [0,1], spectra
nonempty) and the baseline separations that are exact (the static
chain has ordering fraction exactly 1). Dimension VERDICTS are out of
scope (declared; the full probe needs depth beyond enumeration and
belongs to a dedicated growth study).

## 4. Gates

GG1 G3 well-formed: closure + enabled-(event, weight) invariance on
the declared slice (the d42a delta standard). GG2 the V1 values:
census 12, mu(seed) = 1/144 exact (the TOY-RELATIVITY exhibited).
GG3 the U1 forms: iff-sweeps, ladder + density (zero violations,
both grammars), ratio locality, L1-uniqueness census. GG4 N1: the
generated triangle exists in G3 with K1 == K2 == 1/3 uniform EXACT
(kernel degeneracy), while the generated path discriminates 2/3 vs
1/2 in BOTH grammars (the surviving discriminator). GG5 the BIN
TABLE printed: per-claim binary-value / ternary-value / FORM verdict
(LIFTED toward U vs TOY-RELATIVE) — the d41b protocol's deliverable.
GG6 the dimension pilot well-definedness + static-baseline exactness.

## 5. Scope

One alphabet variant is ONE second grammar: lifts here are "two-of-
two grammars", not universality proofs (d41b's Bin U needs argument
+ breadth; the carrier-level variant is the named next). The
dimension pilot decides nothing about dimension (declared).
