# D74 — THE TRANSPORT HOLONOMY: carrier, group, removability, and the odd-sector U(1) search (PIN)

**Status:** PIN, STRICT, 2026-07-27.  Parents: D72 TERMINAL (#490 —
transport scope is CURVED: 88 non-unit + 40 half-open closed exchange
squares at (A,B) d ≤ 4 with values {1/2, 2/3, 3/2, 2}, 12 more at
three actors, ALL delivery-bearing, from depth 3; the record-graph
instrument is structurally blind (μ is an exact gradient there;
record-closure ⇔ register-disjointness); the values 2/3 and 3/2 lie
OUTSIDE ⟨5/4⟩ — a NEW object, not D65's coboundary family; F4 LIVE:
everything exhibited is R⁺, no U(1) part anywhere), D65 (the
mass-ratio coboundary and its repair theory), D71b (#485 — the odd
sector as the phase's address; H¹ = 0 along commit paths — phase
lives transverse; the v7 i-twist form), D73 TERMINAL (#491 — the
generic-geometry lesson: hand-built symmetry is a cage; carry at
least one asymmetric/defected substrate), D64 (the coboundary-first
discipline: NO structure claim before the removability test), D70
TERMINAL (the measure these loops live under).

## 1. The four questions (one unit, four gates-arms)

- **TH-A THE CARRIER.**  Map where transport holonomy lives: extend
  D72's census in depth, actor pool, and schedule family (incl. one
  defected/asymmetric variant per D73); classify the non-unit squares
  by event-kind pair and register overlap; and construct the QUOTIENT
  THAT SEES IT — the record graph is blind, so exhibit the coarsest
  quotient on which the holonomy descends (candidate: the
  register-overlap classes; the D62 sigma machinery at transport
  scope is NOT expected to work — say what does).
- **TH-B THE GROUP.**  The multiplicative group generated by the
  holonomy values, per scope and cumulatively; its prime content
  (the exhibited values generate ⟨2, 3⟩ ∩ …); whether it stabilizes
  with depth/pool or grows; exact rationals only.
- **TH-C REMOVABILITY (the D64 discipline, FIRST).**  Is the
  transport holonomy a coboundary — removable by a per-vertex
  potential on its carrier graph?  Run both routes (obstruction
  count by propagation; relabelled recount).  A removable twist is
  the null outcome and MUST be tested before any structure claim.
  Also test: is it the coboundary of a per-STATE function under any
  committed abstraction (the D65 pattern one level up)?
- **TH-D THE ODD-SECTOR U(1) SEARCH.**  On the holonomy-carrying
  loops: decompose under the order-dual/traversal-reversal (the
  D72-welded carrier where both reversals coincide); gate whether
  any ODD (orientation-sensitive) residue exists — a quantity that
  CONJUGATES under reversal rather than inverting in R⁺; if found,
  run the v7 i-twist correspondence (does e^{iΦ} on the odd residue
  restore a dual-conjugation identity that the real form breaks?).
  This is the imaginary exponential's last known address; the search
  must include the asymmetric substrate (D73's lesson).

## 2. Pre-registered outcomes

- **TH-I (removable):** the transport twist is a coboundary — the
  scope is secretly flat too; the phase hunt fails at this address;
  combined with #490's closed-scope theorem this would make
  flatness grammar-wide and the enrichment fork becomes the
  program's live question.  A first-class negative.
- **TH-II (real curvature only):** non-coboundary, R⁺-valued, odd
  sector empty — the grammar carries genuine MODULUS curvature
  (gravity-flavored) with NO phase; the quantum layer needs a
  different entry point than transport loops.
- **TH-III (the odd residue exists):** an orientation-sensitive
  quantity on the curved loops — the U(1) seed; the i-twist
  correspondence then decides whether it composes as a phase.
- **Lean: NONE.**  The campaign is 0-for-3 on phase-location
  intuitions and this pin bets nothing.

## 3. Receipt requirements

Committed layers by AST/text-slice (d42b1 the primary; the D72
census machinery reused as the anchor — its 88/40/12 numbers must
reproduce exactly, exit 1 only there); exact Fractions end-to-end;
every census printed both ways; the removability gates before any
structure gate in the output order; controls (tamper-detection on
the carrier graph; the closed-grammar flat control through the same
pipeline; a perturbed-weight negative control); determinism probe;
no bare-constant predicates; runtime < ~25 min printed; substantive
negatives exit 0.

## 4. Scope

Transport scope, the declared families/depths/pools only.  No
measure-existence claim (D70's bound is open); no claim that any
found structure IS the v7 phase (correspondence ≠ identity — the
missing map discipline); no infinite-volume claims; scale doctrine
(#440).  The odd-sector search's negative is as reportable as its
positive.
