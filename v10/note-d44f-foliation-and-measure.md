# D44f (successor 6) — horn 2's two open arms: the foliation face and the measure side

**Status:** CAMPAIGN PIN (strict), 2026-07-19.  Parents: d43c
TERMINAL (#344; §5 C3: the foliation/commuting-family face at
overlapping visibility and the measure question "stay OPEN and
SEPARATE"); the d42b2 click layer; paper 31 §4.4/§7.2.  Receipt:
`v10/code/d44f_foliation_measure_exact.py`.  Execution gated on
paper-31 terminal.

## 1. Arm F — the foliation face

**The question.**  V_pair acts on BOTH proposers' records (licensed
join carriers), so commutation-by-disjointness is forfeited at
joins.  Does the constructed family admit a foliation-consistent
assignment where visibility overlaps — or is a slicing choice
forced?  Concretely, on the pair fixture: from the join cut, BOTH
actors hold admissible arbitration opportunities on the SAME
component (different initiators).  Advance the cut through A's
opportunity first, then B's remaining opportunities; and through
B's first, then A's — computing the full branch ensembles (records,
classical weights, amplitudes through the V-family) EXACTLY both
ways.

**Gates (pre-registered).**
- **FG0 (baseline):** the committed d43c objects re-anchored
  (V_single/V_pair; Born = K1; both-cut menus) before new content.
- **FG1 (the two orders):** the two cut-advance orders' final
  ensembles computed exactly; the comparison is entrywise (record
  identity up to the committed canonical form; weights in
  Fractions; amplitudes at 1e-40).
- **FG2 (the verdict, open):** EQUAL => foliation-consistency of
  the constructed family at fixture scale [EXACT positive — horn
  2's standing arm closes at this grain].  UNEQUAL => the
  obstruction exhibited entry-by-entry, PLUS the attribution gate:
  which committed requirement (admission structure, carrier
  licensing, Born weights, isometry) forces the difference — the
  choice a foliation would have to make, stated exactly.  Either
  outcome is a result; neither is pre-committed.
- **FG3 (disjoint control):** two arbs on DISJOINT components must
  commute exactly (the disjointness lemma made a firing gate — this
  control must PASS, and a deliberately overlapped mutant of it
  must show the gate can fail).
- **FG4 (sequential control):** same-initiator sequential
  opportunities (no overlap ambiguity) — order irrelevance gated.

## 2. Arm M — the measure side

**The question.**  Each V_C's Born split sums to 1, so the operator
carries the branch split only; the CROSS-component weights remain
the classical q's.  Upgrade this from an observation to a gated
reduction: the measure side of horn 2 IS residue 1 — not
metaphorically, arithmetically.

**Gates (pre-registered).**
- **MG1 (the forcing sweep):** parameterized per-component
  amplitude re-weightings of the family (tilt directions spanning
  the component index at both cuts, pinned in-receipt): EVERY
  non-trivial tilt violates at least one committed gate (isometry /
  Born = K1 / menu reconstruction at a cut) — each direction
  convicted individually, exit-1-wired.  The operator layer FORCES
  the classical cross-component weights.
- **MG2 (the reduction map):** the cross-component weight
  assignment problem at the fixture's cuts mapped onto the
  completion problem's within-cut ratio structure (the d42b3
  object): the correspondence exhibited cut-by-cut in Fractions —
  same normalizers, same ratio constraints — and gated
  arithmetically.  Deliverable: "horn 2's measure side reduces to
  residue 1 at fixture scale" as a gated equivalence, replacing the
  informal "stays with residue 1".
- **MG3 (honesty):** the reduction is FIXTURE-SCALE; no continuum
  claim; if MG2's correspondence needs a nontrivial dictionary
  (weights vs ratios normalization), the dictionary is printed and
  gated, not narrated.

## 3. Scope

The pair-plus-path fixture (and the width-3 fixture for FG3 if
runtime permits); grammar = d42a terminal + the d42b2 click layer;
realized component sizes {1, 2}; Hegerfeldt untouched; no continuum
claim.  Exact Fractions; amplitudes mp.dps 50 at 1e-40.

## 4. First-run amendments (2026-07-19, pre-round; the receipt's
## declared deviations)

**A1 (FG1's advance rule).** The two-order comparison branches the
ARB SECTOR only (proposal/idle candidates not advanced) — the pin's
"arb resolves, then remaining opportunities" reading, declared
in-receipt.

**A2 (FG3's fixture).** The disjoint control uses the same-value
two-singleton fixture [pA0, pB0] (genuinely disjoint components at
one cut) instead of width-3 — the pin's "if runtime permits" clause
taken in the cheap direction; the overlapped mutant fails the same
gate (a firing negative control).

**A3 (FG4's realization).** Strictly-sequential same-initiator
opportunities admit NO posable order choice in this grammar (each
needs the previous event's output register) — FG4 anchors the
committed A7 witness pair (mu = 1/256) and declares the vacuity in
the gate label.

**A4 (MG1's fourth gate — required by the pin's own
no-silent-acceptance clause).** The pin's parenthetical named three
committed gates (isometry / Born / menu); the cut-asymmetric sign
tilt T5 slips ALL THREE and is caught only by CUTIND (d43c
PG3-E5's same-matrices-at-both-cuts) — the battery therefore runs
FOUR committed gates, and MG1-c demonstrates the reduced 3-gate
battery failing the sweep on T5 (no silent acceptance, live).

**A5 (MG2's recomputation).** The depth-4 Z recursion is recomputed
in-receipt (the d43c exec slice stops before d42b3's enumeration),
anchored to the committed numbers (Z(genesis) = 1037/64; family
1,191; the root completed weight 133/2074 = paper 30 §5.3's
printed extreme) BEFORE use.

## 5. Round-1 amendments (2026-07-19; round frozen at
## reviews/d44f-round1-hostile-review.md: REVISE, 1B/1M/3m/4n)

**B1 (BLOCKER-1 — the forcing sweep did not span its tilt space;
rescoped and gated).** The referee constructed four sign tilts
slipping ALL FOUR gates (the sign group {+-1}^3 on amplitudes);
every one carries EXACTLY ZERO squared-amplitude content
(referee-verified, now gated). The pinned "every non-trivial tilt
is convicted" lemma was FALSE as stated; MG1-d is RESCOPED to
|amp|^2 content (the forced object is the WEIGHTS) and the new
MG1-e adopts the referee's four tilts as the sign-sector gate
(all-gates-slipped AND zero |amp|^2 each); MG1-d's false "only
surviving freedom is the global scale, which MG2-e cancels" clause
is retired (ISO convicts scales; MG2-e's lambda scales the z
multipliers — a different object). The battery is real-valued;
complex phases belong to the same zero-|amp|^2 gauge sector
(declared in-gate). FG2/MG2 untouched by the finding.

**B2 (MAJOR-1 — the LOG's foliation sentence, forward-corrected at
#370).** "The physics (winners, weights, amplitudes) is
foliation-invariant" overreached the program's own record ontology:
records ARE the physics, the initiator tag is record data, and the
tag propagates (post-arb proposal menus differ in the vname init
slot — referee-verified). The honest form: INITIATOR-ERASED content
is invariant; the residual foliation datum is ITSELF RECORD DATA.

**B3 (minors).** MG2-d relabeled: the absolute q' = (2/23, 1/23,
1/23) is depth-4-horizon-bound; the HORIZON-STABLE forced object is
the SECTOR CONDITIONAL (1/2, 1/4, 1/4) — the new MG2-d2 gates the
referee's depth-5 check in-receipt (family 6,471; join Z = 4;
early Z = 8; identical sector conditional; different absolute q');
MG2-f's depth bound labeled as the cited D44a constant; #369's
"committed anchors reproduced" clause covered new-in-receipt
q'/lambda values — corrected at #370.

**B4 (nits, recorded).** FG2-c gates 4 of the 8 exclusion pairs
(the referee verified all 8 block — referee-carried); amp slots
compared as strings with the numeric 1e-40 check backing; the
battery is real-only (a unitary complex-phase family would be
spuriously convicted — the scope is declared in MG1-e's label);
the MENU gate checks A's rows (B's join menu is the same object by
the committed symmetry — referee-verified).
