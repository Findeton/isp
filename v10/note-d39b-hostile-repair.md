# D39b — hostile repair of record-closed-law selection

**Status:** exact repair candidate, `PASS 10/10`; independent closing review
open.  Paper 28 remains held.
**Date:** 2026-07-15.
**Rejected parent:** D39 first receipt at commit `a9af380`.
**Hostile mandate:**
`reviews/d39-round1-selection-identifiability-hostile-review.md`.

## 1. Frozen artifacts

```text
source  = code/d39b_record_closed_law_selection_exact.py
stdout  = data/d39b_record_closed_law_selection_exact.out

source_sha256           = a90ce18b355a1168f442b140fbd702d40178d10b7a66f314f09dc2426c190704
stdout_body_sha256      = b0243bb4232901034ade4ca64ef92d9995ebae222c98d1ed84ad99267228d392
internal_science_sha256 = fba6b4a88bb4b7ae536cd92350e74a4b375b6699887a435acc669695089b95e0
complete_stdout_sha256  = 309aeec45af0ea337b97e916c897ebb1c7808a7e78421f808f64b92c5f9575ce
```

Seeds `0`, `1`, `17`, `101`, `999` and `314159` reproduce the complete stdout
hash.  The theorem rows use integer and `Fraction` arithmetic only.

## 2. Comparison result: a shared interface-schema span

D39's claimed direct D38b-to-D37 comparison was false: D37 was never imported,
and the local target retained a global witness hash.  D39b replaces that claim
with the object actually constructible now.

Both locked D37 regions and D38b record restrictions map into one typed
regional-interface category carrying internal, incoming, lateral and generated
rows.  The target contains no complete-witness field.  All 38 nonempty regions
of the six locked D37 carriers pass identity; all 72 strict three-level
restriction compositions commute.  The registered D38b chain passes identity,
two compositions, two direct/staged restrictions and update naturality.
Twelve pushed cylinders normalize, twelve prefix restrictions commute and
1,760 positive atoms remain.

This is an architecture-level comparison span through a shared interface
schema.  It is not a D37 conditional-kernel equivalence, a proof that D38b is a
D37/K-family member, or the Level-B generated-conflict bridge.

## 3. Level-B remains empty for chosen D34b

Five registered D34b histories generate no transaction carrier with a typed
participant base version, hence no contended proposal pair.  Alternative next
events cannot be relabeled as durable conflicts.  The locked D36 content bridge
would require three transaction-carrier kinds—BORN, dormant slot and TOKEN
activation—followed by the eight typed protocol roles

```text
PREPARE, GRANT, REJECT, DECISION_COMMIT, DECISION_ABORT,
APPLY, RELEASE, ACK.
```

No induced K member is claimed.

## 4. Typed causal H1 repair

H0 remains exactly rejected: a once-authentic static head statement verifies
after an unrelated birth has advanced that wire, while the complete oracle
rejects the stale event.

H1 is no longer a tuple-lock procedure advertised as a causal protocol.  It
creates authenticated records with the parent pattern

```text
HEAD_PREPARE
  -> HEAD_GRANT
  -> HEAD_DECISION_COMMIT or HEAD_DECISION_ABORT
  -> HEAD_APPLY or HEAD_RELEASE
  -> HEAD_ACK.
```

Every grant parents both its prepare and the certified current head.  An
interaction carries an incident-edge credential whose source must equal the
ledger's authenticated edge source.  A decision parents the complete grant
set; release is authenticated and all-or-none.  The protocol validator checks
parent closure, acyclicity, exact per-kind parent schema, payload binding and
ideal owner signatures.

The depth-zero-to-two registry contains 47 distinct histories and 410 valid
next birth/idle/interaction events.  H1 agrees with the complete witness and
produces the identical successor rows, heads and incidence on all 410.  The
24-case battery includes the round-one foreign-edge-source and forged-partial-
release attacks; all reject with byte-identical failed transactions.

This is an exhaustive theorem on the declared finite registry.  It is not a
global all-history certificate-locality theorem.

## 5. What the selection conditions actually classify

The finite action complex contains 155 canonical states, 401 local transition
variables and 344 distinct cocycle rows of exact rank 246.  The positive
unnormalized log-weight variety therefore has dimension 155; action
coboundaries have rank 154.  Chosen projected D38b satisfies 169 registered
commuting squares and violates 337.  The initial violation remains

```text
1/18 != 2/33.
```

Normalization is not silently inferred from that linear variety.  A separate
depth-three completion kernel supplies an exact normalized finite-horizon
representative: 21 kernel rows normalize and all 78 commuting-square products
agree across 179 terminal paths.  No stationary or infinite action-compatible
kernel is constructed.

The orbit-level mass-transport calculation uses one variable per rooted-
isomorphism orbit, rather than labeled-vertex variables.  Across three marked
cells, the complete reduced rank is `root-orbit count minus one`; uniform
vertex rooting passes three of three, degree bias fails three of three and
three noncovariant labeled controls are outside the domain.  Two arbitrary
mixture weights across unrooted classes remain free.  Infinite unimodular
completion remains open.

These two finite classifications do not select one physical record-closed law.
They leave a residual increment variety, free unrooted mixture weights and an
unconstructed stationary/infinite intersection.

## 6. Identification now uses typed causal DAGs

For each of 72 positive actor/mode packets, D39b enumerates two-event D38b
paths, executes their typed records and sums construction orders that yield the
same untimed causal DAG.  The quotient merges 288 serial paths across the
packet census.

The 72 packets have 49 projective actor/mode-ratio classes.  The typed O-U
pushforward has exactly 49 laws and zero residual projective collisions.  The
untyped projection has only 28 laws, 21 of which collide across distinct ratio
classes.  Common scale remains null.

The O-L control is no longer a self-equality.  Starting after B births B/1,
changing only B/1's silent actor rate changes the complete one-event O-U law
but leaves the first-retained root-star law unchanged.  Ten possible one-step
silent events preserve that retained law across the two rate packets.  This is
an exact finite thinning/collision result, not a global unlimited-history O-L
theorem.

## 7. Timed, D26 and operational conclusions

The two common-scale packets have construction hazards 12 and 36 but the same
untimed O-U law.  Rescaling the unknown clock conversion from two to six makes
both observed hazards six; a fixed conversion separates them.  Absolute
physical rate therefore remains clock-bridge-dependent.

The D26 row remains conditional on O-V.  If the bridge identifies N same-line
BORN records at `g=9/25`, visibility is `(4/5)^N`, including `64/125` at three
births; dormant TOKEN remains one.  No maintenance rate follows without the
bridge.

At the actual D28 active alphabet, all six preparation pairs are separated by
the `{Z,X}` response vector.  All 72 pair/depth attenuated distances through
depth twelve remain exactly nonzero.  At `epsilon=1/64`, the uniform
single-path and summable branching cutoffs are both six; all twelve
nonsummable shells remain above tolerance.  The D31B cancellation and
`576/625` intermediate influence reproduce.  No D38b quantum join, V6 seal or
ontological erasure is inferred.

## 8. Terminal noun

```text
SHARED D37 INTERFACE-SCHEMA SPAN CONSTRUCTED;
DIRECT CONDITIONAL EQUIVALENCE AND LEVEL-B CONFLICT GENERATION OPEN.

H0 REJECTED;
TYPED CAUSAL H1 SUFFICIENT ON THE EXHAUSTED FINITE REGISTRY.

RESIDUAL UNNORMALIZED ACTION-INCREMENT VARIETY;
ONE NORMALIZED FINITE-HORIZON REPRESENTATIVE;
FINITE UNIFORM-ROOT CLASSIFICATION;
REGISTERED PROJECTIVE O-U IDENTIFIABILITY.
```

Chosen D38b action membership, a stationary/infinite action-compatible law,
infinite unimodular completion, global identification, clock calibration,
generated K membership, the quantum join and physical sealing remain open.
Independent closing review must adjudicate this narrower result before Paper
28 begins.
