# The click law is the whole-history process

## Restoring sealed diamonds, proving dynamical underdetermination, and locating the actual primitive physics

**Relativistic ISP / SHARD v10 — Paper 13**  
**Status:** final after four hostile rounds, 2026-07-11

## Abstract

V10 asked for the final interactive rule that makes new records, lets records
interact, removes a global commit clock, and could eventually generate the V9
spacetime web.  The question was sharpened until a decisive distinction became
visible.  V6 had already described a record as a finite sealed diamond with a
whole-history law, lower and upper screens, an eventless collar, and a durable
commitment.  D11 tested a different terminal-port ontology in one global
equal-activity race; its exact extinction theorem is correct for that ablation
but is not a theorem about the V6 diamond architecture.

After restoring the diamond, this paper proves two no-go theorems.  First,
strictly positive projective non-Markov history laws can agree on every one-
and two-record shadow while giving different next-record probabilities.
Second, a continuum of two-leg interactions shares unitarity, exchange
symmetry, excitation conservation, entangling capacity, connected locality,
the same evidence clock, the same record instruments, seal-and-birth,
construction-order gauge, and indefinite continuation while giving different
durable records.  Exact representatives at `theta=pi/4` and `theta=pi/2`
predict `1/2` and `0` for the same later proposition.

The result identifies the answer to the generative question.  Once a complete
whole-history process measure is given, there is no second next-click
generator: the conditional measure is already that law.  If SHARD instead
aims to derive the measure, that derivation remains the missing physics.  In
an exactly decoherent-history representation the conditional law is
the conditional diagonal of a supplied decoherence functional; in an
operational multi-time representation it is a probability returned by a
supplied process tensor and inserted instruments.  Sealed-record
principles constrain how histories compose, restrict, transform, and are read
once a record instrument is supplied; they do not fix the universe's field content,
local action/Hamiltonian, coupling constants, initial state, bridge grammar,
or decohering record instrument.  Those are empirical dynamical physics.

An exact 145-gate replacement implementation—written after hostile round 1
rejected the original one-cell model—constructs durable records and output
collars, depth-indexed cylinder laws, an all-level classical cylinder family,
nontrivial disintegration, independent vertex frames and transported links,
canonical schedule fibers, overlap controls, local eligibility, and a genuine
continuation induction.  Two full packets pass the same gates while changing
one durable-record probability.  The final
verdict is `UNIVERSAL-FORM/PRIMITIVE-PROCESS-REMAINS`.  Geometry is correctly
withheld until that process is independently selected.

## 1. The question that survived V10

The program successively established:

1. a no-silent center filters a supplied candidate but does not create its
   carrier;
2. diamond amalgamation composes a supplied interface but does not choose the
   first interface;
3. profinite growth preserves an immutable past and hosts consistent measures
   but does not choose one;
4. bounded boundary summaries either import a supplied law or lose relevant
   history;
5. typed collar contraction composes supplied factors but does not create
   those factors;
6. a complete sealed-holonomy ledger identifies a supplied positive law but
   does not generate it;
7. the complete missing object is a null-inclusive extension law or a
   compatible full-history measure;
8. SCIR is a finite complete generative packet once its grammar and quantum
   couplings are treated as primitive;
9. Bell identification of one coupling does not justify equating that coupling
   with the V9 geometric transfer coefficient;
10. the complex qubit positive cone is exactly a `1+3` Lorentz cone, but this
    is kinematics, not interaction selection;
11. a globally raced terminal-port model almost surely dies.

The remaining question was therefore not “can a complete rulebook be
written?” D8 had done that.  It was:

> Do the deeper sealed-diamond, evidence, holonomy, quantum, locality,
> covariance, and whole-history principles select the rulebook's grammar and
> interactions, or is some dynamical measure irreducible?

## 2. The D11 correction

D11's exact population theorem concerns a model with `p` open ports, `p`
upward split tokens, `p+j` downward terminal seal/merge tokens, and one global
normalized race.  Its nonpositive drift and uniform death chance prove almost
sure extinction.  The calculation remains valid.

What failed was extrapolating that result to a continuing V6 architecture.
D11's live carrier is a disposable terminal port; the V6 record itself is a
sealed finite diamond.  An internal commitment leaves a durable record
and, when the law declares continuation, a typed upper output collar.  Deleting
a future from an observer's description is a cylinder projection, not a
physical annihilation.  D8 had also stated the internal operation as seal and
birth.

D11 is thus retained as architecture A:

```text
terminal-SEAL + global equal race = exact extinction negative control.
```

It cannot decide architectures in which local commitments emit continuing
collars or in which all finite conditionals come from one full-history measure.

## 3. Restored objects

Let a finite sealed diamond carry

```text
D = (Omega_D, mu_D, P_D^hist, C_D, h_D,
     lower screen, upper screen, eventless collar,
     local order unit, frame links, typed exposed interfaces).
```

`P_D^hist` is a law on complete finite history atoms, not merely a transition
matrix on the current boundary.  A complete contrast ledger `C_D` can identify
that law through its log Radon–Nikodym coefficients `h_D`.  The upper screen
may expose new typed collars.  A commit records a division and may birth those
collars; it need not terminate the lineage.

For a sealed past `H` with exposed boundary `B`, a supplied local grammar `G`
defines `Ext_G(B)`, the typed extensions whose lower screens match owned
connected collars in `B`.
An element contains at least:

```text
diamond type, participating collar owners, internal history/outcome,
local instrument or amplitude, upper output collars, frame links,
new owned opportunities, and durable record data.
```

A complete generative theory must determine both `Ext_G(B)` and the weights on
it.  A conditional exponential formula on a supplied finite `Ext_G(B)` with a
strictly positive reference measure solves only the
second half.

## 4. What V6 and V7 really fix

Three older results remain essential.

### 4.1 Evidence survival

When independent eventless evidence glues additively and survival multiplies,
the intrinsic evidence coordinate `I` gives

```math
S(I)=e^{-I}.
```

In a different content coordinate `chi`, the dense one-channel expression is
`S(chi)=e^{-kappa chi}`.  `kappa` is a conversion unless `chi=I`.  Sparse seals
leave an inter-seal coherence profile and actual spacing open.  This law says
how a supplied local opportunity survives accumulated evidence.  It does not
say which opportunity exists or which interaction follows firing.

### 4.2 Complete holonomy reconstruction

On a fixed finite positive history space, a complete set of contrasts spans
functions modulo constants.  Therefore

```text
positive history law P  <->  complete log-RN coefficient vector h.
```

This is lossless identifiability.  It is not generation: every positive
alternative law has its own complete `h`.

When an ambient Born law contains exact zeros, `h_D` is defined only after
restricting to its positive support; its nonconstant dimension is
`|support|-1`.  D12 never treats atomwise `-infinity` values as finite contrast
coordinates.  The exact receipt reconstructs support-relative RN ratios from
the stored law and reference measure and rejects mutated support or ratios.

### 4.3 Commitment fixed point

On a fixed support, reference measure, primitive oriented ledger, and mode
basis, strict convexity can select a coefficient vector through

```math
nabla psi(h)=e^{-h}.
```

The exact D12 witness reproduces the one-mode root

```text
h = 0.609377863436006231536803371168398695428539279312854...
```

at 120-decimal precision.  The same coefficient coexists with inequivalent
SWAP and CNOT interactions.  Convexity selects a point only after the physical
space and functional have been chosen; it does not choose the functional,
support, reference, orientation, operator packet, or field content.

## 5. Classical whole-history nonuniqueness theorem

### Theorem 1

There exist strictly positive non-Markov whole-history laws with identical
one- and two-record projective shadows and different next-record
conditionals.

### Construction

For `x,y,z in {-1,+1}` and rational `|r|<1`, let

```math
P_r(x,y,z)={1+rxyz\over8}.
```

Every one-variable marginal is uniform.  Every two-variable marginal is
uniform because summing over the third sign cancels the `rxyz` term.  Thus all
one- and two-record cylinder masses are independent of `r`.  Yet

```math
P_r(z=1\mid x,y)={1+rxy\over2}.
```

For `r=1/2` and `r=1/3`, the continuation after `x=y=1` is respectively `3/4`
and `2/3`.  Dependence on `x` remains after conditioning on `y`, so the law is
not first-order Markov.

Both exact laws are positive, normalized, projective under every pair
deletion, stable under neutral refinement, and normalizable under independent
diamond gluing.  A complete three-body ledger identifies `r`; the shared
lower-order record shadows do not derive it.  QED.

This finite witness extends to an explicit law at every prefix length.  For
`n=3q+s`, `s in {0,1,2}`, partition the first `3q` signs into triples and set

```math
P_r^{(n)}(x_1,\ldots,x_n)
=\left[\prod_{j=0}^{q-1}
 {1+r x_{3j+1}x_{3j+2}x_{3j+3}\over8}\right]2^{-s}.
```

Summing the last sign either removes a fair incomplete-block factor or turns a
complete `P_r` block into its uniform pair marginal, so
`(pi_(n+1,n))_*P_r^(n+1)=P_r^(n)` for every `n`.  The live collar need not copy
the past: it stores only the phase `s` and at most the previous two signs in
the current block.  Immutable earlier records retain the full history.  The
exact executable checks all bonding, conditional, and threshold-product cases
repeatedly through depth 9 for both `r` values; the formula proves arbitrary
depth.

### Consequence

Profinite history space does not remove this freedom.  A compatible cylinder
family extends a measure; it does not choose the cylinder family.  “Preserve
all earlier records” is a consistency condition, not a probability law.

## 6. Quantum interaction nonuniqueness theorem

The elementary SWAP/CNOT witness already proves that equal types, dimensions,
ownership, pointers, evidence clocks, gauge rules, and continuation do not
select an operator.  To remove the objection that those operators have
different symmetries, D12 uses one common continuous family.

Let `X_ex` exchange `|01>` and `|10>` and vanish on `|00>,|11>`.  Define

```math
U_theta=e^{i theta X_ex}=
\begin{pmatrix}
1&0&0&0\\
0&\cos\theta&i\sin\theta&0\\
0&i\sin\theta&\cos\theta&0\\
0&0&0&1
\end{pmatrix}.
```

### Theorem 2

The frozen structural principles do not uniquely select the two-leg
interaction, even after adding exchange symmetry, excitation conservation,
and entangling capacity.

### Proof

For every `theta`, `U_theta` is unitary.  It commutes with leg exchange and the
total excitation-number operator.  Except at a discrete set, it can entangle;
both exact representatives used below entangle `|++>`.  Every member acts on
the same connected owned two-leg collar, preserves the same input/output
types, accepts the same complete computational pointer, can use the same
`e^{-I}` evidence survival and commitment coefficient, and commutes with
operations on disjoint diamonds.

Take

```text
U_Q = U_(pi/4),     U_H = U_(pi/2).
```

All arithmetic lies in `Q(sqrt(2),i)`.  On input `|01>`, a later durable
second-leg-`1` record has probability

```math
p_Q=\cos^2(\pi/4)=1/2,
\qquad
p_H=\cos^2(\pi/2)=0.
```

The exact executable verifies every shared property and the unequal record
probabilities.  Therefore the shared principles admit at least two physically
inequivalent laws.  QED.

### Stronger meaning

The free variable is not an arbitrary simulation convenience.  Writing

```math
H_J=-J X_ex,\qquad \theta=J\tau/\hbar
```

shows it as an interaction coupling times a physical duration/evidence-scale
conversion.  Symmetry determines an allowed operator structure; it does not
determine its coefficient.

## 7. Why the proposed higher selectors fail

### 7.1 Least action

Hamilton's principle determines the trajectory or quantum evolution once an
action and boundary data are supplied.  It does not choose the action.  Every
member `U_theta` above has the same stationary-action form with a different
`J tau`.

For any positive finite classical history law `P` relative to `mu`, one can
also define

```math
A_P(omega)=-\log(P(omega)/\mu(omega)),
```

so that `P` is proportional to `mu exp(-A_P)`.  Existence of an action
representation therefore cannot select `P`.

### 7.2 Maximum entropy or caliber

For uniform binary marginals and correlation family

```math
p_r(x,y)=(1+rxy)/4,
```

the entropy obeys

```math
H'(r)={1\over2}\log{1-r\over1+r},
\qquad
H''(r)=-{1\over1-r^2}<0.
```

With no correlation constraint, maximum entropy selects `r=0`, independence.
To obtain an interaction one must supply a measured correlation, energy,
flux, or other constraint and its value.  The multiplier then represents that
input; it does not derive it.

### 7.3 Quantum/Born principles

Born normalization, no-signalling for disjoint laboratories, unitarity, and
decoherence constrain how a supplied process produces probabilities.  Both
D12 interactions use the same calculus.  The process remains different.

### 7.4 Covariance, locality, and symmetry

These principles determine admissible supports, representations, and operator
bases.  The exact common iSWAP family proves that an allowed commutant can
retain a continuous coupling.  In field theory, invariant local operators
likewise carry masses, couplings, mixing angles, and vacuum data.

### 7.5 Bootstrap, renormalization, and consistency

Analyticity, crossing, unitarity, gauge consistency, anomaly cancellation, and
renormalization can exclude large regions and occasionally isolate special
theories under extra assumptions.  They do not constitute a known universal
theorem selecting one interacting theory and its state.  Multiple consistent
fixed points and relevant trajectories exist.

### 7.6 Simplicity

Description length requires a representation language and prior.  Neither is
record-intrinsically fixed in V1–V10.  A recoding can exchange which member is
shortest.  Simplicity is a research heuristic, not a proved physical selector.

### 7.7 Round geometry

V9 cone shape is a downstream observable.  Choosing the interaction that
looks round after seeing the scores is parameter fitting.  It can select a law
only as an independently frozen empirical observation followed by new
holdouts.  It cannot derive the law from records.

## 8. The maximal conditional click representation

The central answer can now be stated without ambiguity.

### 8.1 Classical/ISP form

Let `G` be a typed extension grammar and `mu` a complete probability measure
on its compatible variable sealed histories.  When every level is finite and
the bonding maps have the stated inverse-limit properties, this is the
relevant profinite case; the conditional formula itself only needs measurable
cylinders.  Let `[H]` be the cylinder of histories extending a finite
sealed past `H`, and `[H e]` the cylinder that next contains a compatible local
extension `e`.  Whenever `mu([H])>0`,

```math
\boxed{
P(e\mid H)={\mu([He])\over\mu([H])}
}
```

is the interactive next-record law.  Its support is

```math
Ext_{G,\mu}(H)=\{e\in Ext_G(B_H):\mu([He])>0\}.
```

No Markov assumption appears.  The full sealed past can affect the ratio.
The grammar and path measure are already the missing generative data;
positive-mass support does not create candidate extensions.  Barandes's
indivisible-process program proposes a stochastic/quantum bridge relevant to
this setting, but it must not be used to infer a unique path measure from a
trajectory of instantaneous probabilities.  Egri et al. directly challenge
such identifications.  D12's disintegration statement is independent of that
dispute.

### 8.2 Quantum/decoherent-history form

Let `rho` be the initial boundary state.  Let each local diamond carry the
instrument/amplitude maps selected by the physical action or another supplied
quantum process representation.
For a record history `alpha`, compose overlapping local operations in physical
causal order, commute disjoint operations modulo construction gauge, and sum
indistinguishable internal alternatives at amplitude level to obtain the class
operator `C_alpha`.  Define

```math
D(\alpha,\beta)=\operatorname{Tr}
  (C_\alpha\rho C_\beta^\dagger).
```

For an exhaustive durable record partition with exact consistency/decoherence,
the history probability is

```math
p(\alpha)=D(\alpha,\alpha).
```

Given a sealed history `H`, the interactive click law is

```math
\boxed{
P(e\mid H)={D(He,He)\over D(H,H)}
}
```

Here `D(H,H)>0`.  Coherent unresolved alternatives are summed in the coarse
class operator **before** taking its diagonal.  Approximate decoherence needs
an explicit error analysis and is not promoted to exact probability here.
Process tensors and quantum combs are related operational multi-time
representations, not automatically the same object as a decoherence
functional; translation requires additional hypotheses.  D12 executes the
exact projector-history construction, not process-tensor tomography.

### 8.3 What a click is

A click is not the universe taking one turn on a global machine.  It is a
local alternative becoming a durable record relative to an owned diamond
collar.  Disjoint clicks have no physical relative order.  Overlapping clicks
do.  The “next” in the boxed formula is relative to a chosen local cylinder
refinement, not a universal time slice.

The exponential evidence clock can represent when a licensed local
opportunity commits.  It is not an additional law that creates the
opportunity, its interaction, or its output collars.

## 9. What is derived and what remains primitive

The restored result separates universal form from contingent content.

### Derived or structurally constrained

```text
finite sealed-diamond records;
typed connected ownership of multileg interactions;
whole-history rather than forced Markov conditioning;
projective deletion/coarse graining;
construction-order gauge for disjoint commits;
local evidence additivity and exponential survival in evidence units;
Born/decoherence readout once the quantum premises hold;
durable seal plus declared output birth;
conditional next records by disintegration;
conditional Herm_2(C) positive-cone / 1+3 Lorentz kinematics.
```

### Primitive or empirically selected

```text
configuration/field and record types;
root or boundary/initial state;
local action, Hamiltonian, process tensor, or equivalent full measure;
all interaction couplings, masses, mixing data, and vacuum parameters;
which local bridge/branch opportunities exist;
the physical record/decoherence instrument and coarse graining;
the evidence-to-proper-time and metric scale bridges;
the relation, if any, between interaction couplings and V9 geometric transfer.
```

The first list is a framework.  The second list distinguishes our universe
from other mathematically consistent universes.

## 10. Exact restored implementation and hostile repair

Hostile round 1 correctly rejected
`code/d12_restored_diamond_process_exact.py` as a one-cell precursor.  Its
projectors and marginalization were exact, but its 256-commit line was only an
arity counter and its gauge checks were too small for the frozen labels.

`code/d12_multidiamond_history_exact.py` is the replacement.  It constructs
two packets, immutable collars and records, screens, order units, endpoint
frames, transported links, emitted opportunities, local eligibility refusals,
quantum history towers, classical non-Markov towers, canonical construction
fibers, and an arbitrary-depth block-cylinder formula.

The receipt is:

```text
checks=145
minimum_diamond_packet_fields=PASS
support_relative_rn_reconstruction_and_mutation_refusal=PASS
explicit_durable_records_and_output_collars=PASS
record_repeat_read_and_persistence=PASS
local_type_owner_opportunity_stale_eligibility=PASS
depth_indexed_cylinder_projectivity=PASS_0_TO_4_PLUS_INDUCTION
nontrivial_next_record_disintegration=PASS
independent_vertex_frame_transport=PASS
lower_and_upper_unitary_frame_domain_refusal=PASS
canonical_disjoint_construction_fibers=PASS
overlapping_order_control=PASS
explicit_nonmarkov_sealed_history_process=PASS
all_level_classical_projective_family=PASS
finite_phase_two_sign_threshold_collar=PASS
equivalent_local_exponential_threshold_representation=PASS
two_full_models_shared_gates=PASS_WITH_DIFFERENT_RECORD_PROBABILITIES
interaction_selection=REFUTED_FROM_SHARED_GATES
semantic receipt=d48f9a161dd3e7f850726225d9ea3faad8433fe35ede0c3957cbbb0963e691c6
```

The implementation establishes that the restored finite packet architecture
and its all-depth induction work at the stated unitary-frame scope.  It does
not select the universe's packet.  Full nonunitary Lorentz-frame integration,
quantum gravity, cosmological bridge grammar, and physical scale remain open.

The supporting exact receipts are:

```text
42-gate diamond/history nonuniqueness:
1f9472d6ed613ad96fe875d68cf0a773062c077e9a44fc357f65ccfd93bd4b97

18-gate common-symmetry interaction family:
7da912f8deb705aaa1467d3428aacf7cc626b249bf16c324c5c365f376b89db9
```

Normal and optimized Python produce byte-identical output for the replacement
and the three precursor/countermodel executables.

The frozen architecture classes are all disposed rather than silently
dropped:

| class | executed disposition |
|---|---|
| A terminal global race | D11 exact extinction negative control |
| B continuing local-instrument SCIR | D8 exact 101-check complete-compressed packet, conditional on supplied grammar/couplings |
| C primitive whole-history law | D12 all-level `P_r` block family and explicit sealed prefixes |
| D amplitude/decoherent history | D12 two exact multi-diamond quantum towers |
| E local threshold representation | D12 exponential races proved equivalent to the same class-C conditionals |

Classes B–E work only after their process data are supplied.  The comparison
therefore closes the architecture census but does not select nature's class or
parameters.

The construction-order result also extends beyond the displayed two-event
cell at its stated scope.  Any two linear extensions of a finite partial order
are connected by adjacent swaps of incomparable elements.  Each such pair is
disjoint in the packet and its instrument maps commute, so one adjacent swap
preserves the canonical history and weight.  Induction along the swap sequence
proves presentation independence for every finite disjoint schedule.  The
exact `AB/BA` calculation is the generating swap; the same-collar control
shows why overlapping events are excluded and retain physical order.

Stale exclusion is likewise a constructor invariant, not only a Boolean
probe: a `QuantumHistory` exposes one live `open_collar`; `fire` returns a new
history whose live boundary is the born collar, while the consumed predecessor
survives only inside immutable provenance.  The API has no operation that
fires an ancestral collar in the same history.

## 11. Literature boundary

The conclusion aligns with established distinctions rather than claiming a
new universal formalism.

Barandes's [stochastic-quantum correspondence](https://arxiv.org/abs/2302.10778)
and [indivisible stochastic processes](https://arxiv.org/abs/2507.21192)
propose a broad bridge from stochastic dynamics to quantum representations;
the specific stochastic dynamics remains physical input.  The bridge is not
treated here as settled: [Egri et al.](https://arxiv.org/abs/2602.23491)
distinguish probability dynamics from probability on trajectories, prove
generic nonuniqueness of implementations, and directly challenge parts of the
stochastic-quantum correspondence.  D12 works in the path-measure category and
does not infer joint histories from one-time probabilities.
Quantum [combs](https://arxiv.org/abs/0904.4483) and operational
[process tensors](https://arxiv.org/abs/1801.09811) characterize general
multi-time dynamics and memory.  They do not select one process.

Jaynes's original [maximum-entropy paper](https://journals.aps.org/pr/abstract/10.1103/PhysRev.106.620)
states the principle relative to given information.  The modern
[S-matrix bootstrap review](https://arxiv.org/abs/2203.02421) describes an
infinite-dimensional allowed space under powerful consistency conditions.
Explicit gauge theories can have
[one or several interacting fixed points](https://arxiv.org/abs/1707.04217).
Current Particle Data Group reviews report interaction strengths, masses, and
mixing quantities as measured parameters, for example in the
[electroweak](https://pdg.lbl.gov/2025/reviews/rpp2025-rev-standard-model.pdf)
and [quark-mass](https://pdg.lbl.gov/2025/reviews/rpp2025-rev-quark-masses.pdf)
reviews.

The SHARD-specific contribution here is the exact restoration audit and the
explicit countermodels showing which sealed-record claims fail to select
dynamics.  No broader priority claim is made.

## 12. Consequences for spacetime, round cones, and `3+1`

The D10 identity

```text
Herm_2(C)_+  <->  a 1+3 Lorentz positive cone,
CP^1 = S^2 for normalized null directions,
```

remains relevant.  It says that if the primitive local factor is a complex
qubit and directional positive evaluations are the shared observables, then
few factors naturally carry a `1+3` Lorentz kinematic form.  It does not choose
which diamonds interact, the coupling, or the relational metric scale.

Many local evidence/proper clocks are compatible with this picture.  No
global commit order is required.  The spacetime web would be the large-scale
influence/decoherence structure of the selected whole-history process, much as
sound is a collective mode rather than an extra molecule.  But the process
must first be known.

D12 therefore runs no new `F` or dimension score.  D8's `g=0.18` shadow was
mixed, D9's Bell-frozen `g=1/2` cross-domain bridge failed, and D11's geometry
was population-starved.  Mapping the D12 angle to the V9 transfer dial would
repeat the already-refuted one-coupling assumption.  Geometry resumes only
after independent process selection and calibration.

## 13. Does this set `G` or physical metres and seconds?

No.  The universal conditional law is dimensionless.  An action contains
dimensionful scales, and ratios of physical clocks/rods can emerge from stable
record processes, but the numerical gravitational coupling and the bridge to
SI units are not fixed by the current record axioms.  D10 supplies a Lorentz
cone algebra, not Newton's constant.  A real theory must identify the
gravitational sector and its scale empirically or through a new independent
dynamical principle.

## 14. Final theorem and verdict

### Theorem 3 — relative independence of dynamics

Let `A_D12` be the exact shared packet principles executed here: finite sealed
history atoms and complete contrasts, common positive reference and fixed
evidence/commitment data, a recorded connected two-owner collar, typed local
eligibility, a complete Born pointer, immutable seal-and-birth records,
projective prefix laws with all-depth continuation induction, canonical
disjoint construction fibers with overlapping order retained, and independent
unitary vertex-frame transport of screens, links, states, effects, order units,
and records.  Then `A_D12` has at least two physically inequivalent continuing
models that disagree on a durable record probability.  Hence `A_D12` does not
entail a unique interaction coupling or induced history measure.  D7's
separate witnesses establish extension-grammar nonselection.  This theorem
does not claim an integrated nonunitary Lorentz gauge or a universe-specific
field theory.

### Proof

The 145-gate exact constructor inserts quarter-iSWAP and half-iSWAP into the
same packet grammar and audits both through the named gates.  They disagree on
a later record probability `1/2` versus `0`.  The all-level classical
`P_(1/2)` and `P_(1/3)` block families separately show that complete
projective non-Markov history structure does not remove the freedom.  A theory
with two inequivalent models cannot logically derive a unique law.  QED.

### Final verdict

```text
D11 TERMINAL/GLOBAL EXTINCTION = VALID NEGATIVE CONTROL
DECLARED CONTINUING SEAL-AND-BIRTH PACKET = EXECUTED, NOT UNIVERSALLY DERIVED
WHOLE-HISTORY CLICK DISINTEGRATION = EXACT
TYPED/LOCAL/CONSTRUCTION/UNITARY-FRAME/PROJECTIVE PACKET GATES = PASS
NON-MARKOV FULL-HISTORY SUPPORT = PASS
UNITARITY + SYMMETRY + CONSERVATION + ENTANGLING = NONSELECTING
EVIDENCE EXPONENTIAL + COMMITMENT FIXED POINT = NONSELECTING
LEAST ACTION WITHOUT ACTION = NONSELECTING
MAXIMUM ENTROPY WITHOUT PHYSICAL CONSTRAINTS = INDEPENDENCE
FINITE-LEVEL/PROFINITE COMPLETION WHEN HYPOTHESES HOLD = HOSTS, DOES NOT CHOOSE LAW
GEOMETRY-AFTER-LAW = CORRECTLY REFUSED
= UNIVERSAL-FORM/PRIMITIVE-PROCESS-REMAINS
```

## 15. The answer in one paragraph

The maximal universal statement is not an extra numerical formula built from
finite records alone.  Given a typed grammar and a complete compatible
whole-history process, its cylinder conditionals are the next-record law.  In
an exact quantum-history representation, supplied local amplitudes and record
instruments give decoherent Born weights, and conditioning those weights on a
positive sealed past gives the next local record.  This needs no preferred
global linear commit order.  It does **not** imply that an arbitrary path
measure is locally computable or causally local; those are additional gates on
the supplied process.  The sealed-record framework constrains architecture
and probability calculus but does not derive the action, fields, couplings,
initial state, grammar, or record coarse graining of our universe.  D12 has
therefore not found “the real law of nature”; it has proved why that law cannot
be extracted from the audited record principles alone.

## 16. What would constitute a deeper success

The current no-go is falsifiable by a new principle `Q` that:

1. is stated without naming the desired law or using V9 geometry after the
   fact;
2. rejects one of the exact `U_(pi/4)`/`U_(pi/2)` survivors for a physical
   reason;
3. also selects field/support/initial data rather than only one angle inside a
   hand-picked family;
4. produces a new quantitative prediction on data not used to formulate `Q`;
5. survives process tomography, spacetime influence, cone shape, dimension,
   and scale-ladder holdouts.

Until such a principle exists, declaring a numerical SCIR packet “the real
law” would hide empirical dynamics inside a derivation claim.  The correct
scientific endpoint is the proved boundary above.
