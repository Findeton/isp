# Relativistic ISP v10 — research plan

## D1: intrinsic no-silent center and support birth

**Status:** complete at the finite scoped theorem/no-go boundary, 2026-07-11.
Design note:
`note-d1-no-silent-support-birth.md`.

Question:

> Can record-visible boundary residue uniquely and covariantly force the birth
> of a joint support?

The campaign proceeded in this order:

1. freeze the finite probability arena, center order, exact independence
   criterion, nonreconstruction rule, restriction rule, and verdict gates;
2. enumerate every boundary-definable screen refinement in the frozen cells;
3. classify complete screens, unique minimal centers, competing centers,
   lookup-only closures, and no-boundary-center cases;
4. separate target marginalization, screen change, typed history restriction,
   and eligible-family naturality;
5. write Paper 2 at the honest theorem/no-go boundary;
6. run three independent hostile reviews, investigate every concrete opening,
   and repeat until the scoped result closes or is blocked.

Final D1 receipts: 45/45 and 28/28. Three independent hostile review streams
forced two major repair rounds and then accepted the finite scoped boundary.

The result is `CENTER-NONSELECTION + SUPPORT-OVERELIGIBILITY +
PROJECTIVITY-REFUSAL` for the naive universal birth rule. Marked history data
restrict path-independently in the tested subset lattices, but the derived
eligible-support family fails naturality on an exact parity/synergy history.

No large web or cone simulation is licensed by D1.

## D2: primitive direct-carrier birth from sealed diamonds

**Status:** complete at the finite marked-support-skeleton theorem/no-go
boundary, 2026-07-11.

Question:

> What record-intrinsic diamond operation proposes or creates a primitive
> direct carrier without presupposing a global construction order, emergent
> distance, or an already shared ancestor?

The leading route is a marked-diamond amalgamation/pushout census. D1's
no-silent rule becomes a downstream admissibility gate. Boundary work and a
positive `$h$` weight are investigated only after the carrier proposal and its
construction-order gauge law are well typed.

Final D2 receipt: 33/33 exact checks. Three independent hostile review streams
required one major repair round and then accepted the finite scoped result.

Result:

```text
CONDITIONAL-COMPOSITION
+ PROJECTIVE-PAIR-NO-BOOTSTRAP
+ HIGHER-SUPPORT-FAMILY
+ STOCHASTIC-PROJECTIVITY-REFUSAL
```

A supplied typed support span has a canonical pushout. The universal property
does not select the interface or create a first carrier. Under the frozen
support-skeleton axioms, pair birth is impossible; weakening restriction
semantics enlarges but does not select the higher-support family.

## D3: profinite variable-history extension dynamics

**Status:** complete at the finite unmarked common-future/prefix-tower claim
ceiling, 2026-07-11.

Design note:
`note-d3-profinite-variable-history-extension.md`.

Question:

> Can one-record variable-history growth preserve the complete old past while
> creating a new joint event across old components, and do full-history
> conditioning, construction-order gauge, diamonds, or the profinite history
> spaces select its probability law?

The program now has an explicit fork:

1. take a full non-Markovian probability measure on variable marked histories
   as primitive, in which case `P(next|H)` and `Ext_mu(H)` are already given;
2. carry an initial connected relational seed as boundary data;
3. derive or posit a genuinely new root/bridge-sector event whose proposition
   includes its participating ports, then derive its rate and outcome law.

D3 first audits the premise behind D2's phrase “add nothing.” It separates the
end-deletion inverse limit of finite prefixes from the Stone/covtree inverse
limit of covariant observable resolution. It then tests exact rational
extension kernels, cylinder consistency, common-future bridge-shadow extension
by a new maximal event,
and nonselection of the kernel. No marked-profinite, quantum, geometry, or
continuum promotion is allowed without further work.

Round 1 independently confirmed the immutable-past common-future extension
theorem but forced a sharper boundary. The exact global-prefix kernels are
end-deletion consistent yet fail ancestor-closed restriction naturality.
Actual finite canonical pushforwards remain inequivalent, and a controlled-
zero kernel shows prefix-level eligibility nonselection. The repaired receipt
is 27/27; no local marked interaction law is claimed.

Round 2 independently passed mathematics, reconstruction, and
ontology/locality review. Final stdout is byte-reproducible and the v10
self-containment audit remains 4/4.

## D4: no-silent boundary sufficiency

**Status:** complete at the strong unmarked/fixed-token theorem and no-go
boundary, 2026-07-11.

Design note:
`note-d4-no-silent-boundary-sufficiency.md`.

Question:

> Can a bounded no-silent boundary mark make a retained subsystem sufficient
> for a nontrivial restriction-natural extension law without encoding a
> law-dependent summary of the discarded universe?

D4 first classifies all unmarked all-restriction-natural down-set kernels,
then constructs the exact law-relative completion message for the D3 controls
and tests its uniform record-capacity and profinite scope. Geometry remains
downstream.

Round 1 independently confirmed the strong all-subset/convex collapse and
forced the cut-category, decoder-contract, canonical-quotient, operational-
capacity, and profinite repairs. The revised receipt is 23/23. Stem and
interval restrictions admit nontrivial controls; collar/direct-carrier cuts
remain untyped without a marked diamond construction.

Round 2 found and repaired one further load-bearing convex-proof opening: an
arbitrary comparable inside/outside pair need not be convex. The final proof
uses the first boundary-crossing cover along a saturated chain, or an
incomparable pair. The exact gate verifies 1,304/1,304 proper ideals, split
616 cover-chain and 688 antichain certificates.

Final D4 receipt: 23/23. Mathematics, independent reconstruction, and
ontology/locality reviews all PASS. The result does not select the interacting
click law; the next positive investigation must construct typed local collar
or direct-carrier messages, their encoder, and their composition law.

## D5: supplied factor partitions and separator-state limits

**Status:** complete at the supplied-factor-partition theorem/nonselection
boundary, 2026-07-11.

Design note:
`note-d5-typed-collar-message-algebra.md`.

Question:

> Does a supplied complete finite factor partition admit exact compositional
> separator states, and what still prevents that conditional theorem from
> becoming a sealed-record birth/click law?

D5 separates participant count, listed channels, screen-relative rank,
additive KL evidence, and exact table description. The repaired 34/34 receipt
proves exact separator contraction for a supplied factor partition,
elimination-schedule invariance, factorization-relative positive-ray
sufficiency, connected-domain closure given a seed, and scope/value
nonselection.

Round 1 independently confirmed the mathematical core but rejected the
physical nouns. The construction has unique allocated IDs, not typed directed
carriers; it predicts the state of an already supplied candidate, not
proposal/birth/seal; and it exhibits separator-state growth, not a distributed
physical encoding. Factor-cover completeness, ownership derivation, scopes,
values, evidence, seal/no-birth law, first joining token, and physical locality
remain open. Geometry remains downstream.

Round 2 independently passed mathematics, clean-room reconstruction, and
ontology/locality review. Final D5 receipt: 34/34 exact checks; byte-identical
stdout; v10 self-containment 4/4. D5 closes the conditional factor algebra,
not the interacting click law. The next investigation must derive or refuse
the physical factor token itself: proposal scope, evidence, direction,
ownership, value, eligibility, and seal/no-birth rule.

## D7: complete extension-rulebook characterization and search

**Status:** complete at the characterization, full V1–V9 audit, targeted
primary-literature comparison, and exact structural-nonselection boundary,
2026-07-11.

Question:

> What exact mathematical and physical object would constitute the final
> interacting click rulebook; does V1–V9 or established literature already
> provide or force it; and what remains after independent construction and
> originality checks?

D7 froze the target before reviewing the corpus: a normalized null-inclusive
kernel on typed marked finite extension packages, or a primitive full-history
measure, including root, continuation, bridges, marks, construction-order
gauge, recorded locality, projectivity, commitment, and ownership. It then
audited V1–V9, independently constructed seven candidate architectures, and
only afterward searched the primary literature for predecessors.

The strongest schema has four layers: typed extension grammar, record-local
transfer cocycle, positive harmonic/projective completion, and a
quantum-capable record instrument/seal. The 68/68 exact receipt proves that
flat transfer scores can lose diamond gauge after row normalization, a
positive completion can repair the sampler, and free activities still produce
inequivalent physical laws under every tested structural gate. Strict
support-local and defect-only rules cannot bootstrap roots or bridges;
projective full-history laws remain nonunique.

Literature comparison finds predecessors for each mathematical component.
The result is a SHARD-specific synthesis and exact refusal, not a discovered
unique law or a priority claim. Further work must introduce a primitive law or
a genuinely new physical selection postulate and then test its geometry and
quantum consequences.

## D8: complete Sealed Causal Instrument Rewrite rulebook

**Status:** complete at the `COMPLETE-COMPRESSED` theorem, exact finite gates,
literature comparison, and first V9 shadow replication, 2026-07-11.

Question:

> Can the D7 admissible class be replaced by one finite explicit generative
> law that supplies roots, locally born opportunities, bridges, quantum
> outcomes, seals, and full-history probabilities without a universe clock or
> one independent number per history?

SCIR's primitive packet is finite: typed ports/state spaces, one root
package/state, a finite local rewrite grammar, and local unitary/Kraus
couplings. The grammar emits candidate tokens locally. Tokens fire on the
unit-exponential integrated-evidence law; local instruments give Born outcomes
and durable pointer records. Disjoint events commute and quotient auxiliary
order; overlapping events acquire recorded physical order. Instrument
completeness gives cylinder projectivity, and bounded tokens/hazard/output give
non-explosion.

The exact receipts pass 101/101 and 19/19 in normal and optimized Python with
byte-identical output. The conservative partial-transfer shadow reproduces
V9's continuous leak form, preserves channel/monopole/dipole content, and
propagates influence through explicit local tokens. `c` is instrument data;
`g` is a measurable coupling.

The literature audit finds close predecessors for every component, especially
quantum Petri nets, quantum causal histories, relativistic flash processes,
stochastic rewriting, and quantum causal graph dynamics. No originality or
unique-coupling claim is made.

The fresh 24-seed `g=0.18` V9 shadow replication yields `t=-1.994` under the
dom convention and `t=-3.950` under m4, so the strict two-convention shape
conjunction fails; dimension refusals and S4 witnesses are 24/24. The unstable
dimension proxy is excluded from grading. SCIR is a complete rulebook but not
yet an empirically identified theory of our universe.

## D9: Bell identification and frozen geometry holdout

**Status:** complete with `REFUTED-ONE-COUPLING`, 2026-07-11.

Question:

> Does the smallest fully numeric SCIR packet permit the same two-port
> coupling to generate maximal Bell entanglement and the pregeometric
> diffusion needed for Lorentzian four-dimensional webs?

The frozen partial-iSWAP packet passes 126/126 exact checks. Within that packet
family, Bell saturation fixes `theta=pi/4`; local instruments generate exact
`2 sqrt(2)` CHSH, no-signalling, setting/source factorization, construction
gauge, cylinder projectivity, and gauge-relative recovery of Bob's two
measurement vectors. The one-coupling hypothesis then fixes `g=1/2` before
geometry.

The dimension repair passes 21/21 synthetic gates. Under injected drift 1.55,
the legacy ruler sends true `d=4` to its `d=6` ceiling; same-window matching
returns 4.013. Under drift 1.94 it returns 4.138.

The blind 24-seed `g=1/2` holdout gives `F_dom=1.3348` (`+3.15 SE` above its
line), `F_m4=1.2393` (`+1.44 SE`), corrected volume proxy `2.709`, refusals
24/24, and S4 witnesses 24/24. Influence reaches all 32 slots, but the scale
ladder worsens: `F_dom` by `+2.54 SE`, `F_m4` by `+4.18 SE`, and corrected
dimension from 2.73 to 2.19 between `N=2048` and `8192`.

Result: the same raw coupling cannot serve both roles in this mapping. SCIR is
not refuted. A viable next packet needs distinct matter/geometric couplings, a
predeclared RG map to a smaller effective `g`, another transfer shadow, or a
different geometry builder. D9 performs no post-hoc tuning sweep.

## D10: Bloch–celestial selection and the `3+1` bridge

**Status:** complete with unanimous round-3 `PASS` at the repaired finite
conditional `KINEMATICS-ONLY` scope, 2026-07-11.

Question:

> Does SCIR's minimal record algebra select the v9 celestial `S^2` and its
> `3+1` Lorentz cone, or does choosing a complex qubit insert the answer?

Result: conditional on the complex rank-two algebra, the bridge is exact:
`X=tI+x.sigma` is positive iff `t>=|x|`; rank-one null rays form `S^2`; and
many directional evaluations use four factors. Exact comparison witnesses
show that real, quaternionic, octonionic rank-two shadows and generic spin
factors give other Lorentz dimensions, so cone kinematics does not select
three-space. The current record principles do not derive the complex/local-
tomography choice.

Finite outer approximations, finite-alphabet projective refinement, a chosen
projective seal, disjoint-schedule control, bounded-forest intervention, and
supplied `SU(2)` connection covariance are receipted. Full `SL(2,C)` Born
gauge, event time/scale, physical link birth/ownership, per-record capacity,
joining sectors, and order/influence equivalence remain open.

Next investigation: construct an unnormalized event/effect SCIR packet with
dual `SL(2,C)` transport and invariant Born ratios, then compare a genuinely
marked continuation-measure influence front with the algebraic cone. Run the
real/complex/quaternionic selection tournament in parallel; if no existing
record principle distinguishes them, declare the complex operational factor
primitive rather than derived.

## D11: globally raced Bloch–Lorentz packet and extinction

**Status:** complete as a hostile-reviewed `INCOMPLETE-PACKET`, 2026-07-11.

D11 executed the D10 successor but did not close the requested law. The dual
`SL(2,C)` event/effect/instrument/link template, exact local instruments,
typed durable outcomes, owned sibling interaction, finite-prefix global
kernel, and algebraic positive-cone containment pass. The generated packet is
still root-gauge, globally raced, sibling-only, and lacks canonical projective
pushforward.

The decisive theorem is almost-sure extinction under unit activities:
`E[Delta p|H]=-j/(2p+j)`. All 72 frozen histories terminate early with zero
cone violations. The corrected downstream-seal interaction counts are
1/24, 4/24, and 5/24; direction/rank/shape claims refuse for population
degeneracy.

Next investigation, in order:

1. characterize the continuation boundary without tuning rates: terminal
   SEAL, continuing COMMIT, and separate local birth are distinct primitive
   packets;
2. freeze a selection principle, if any, before building one branch;
3. construct decentralized local clocks or a proof that global earliest-event
   order is pure gauge;
4. integrate vertex-local frames, dual effects, links, anchors, and
   instruments into generated histories;
5. execute canonical truncation/pushforward over construction-order fibers;
6. add a genuinely local eligibility/proposal law for cross-component bridge
   birth, or prove it impossible from current support locality;
7. only then rerun influence-envelope, round-cone, scale, and dimension tests.

## D12: restored diamonds and the generative-law theorem

**Status:** complete with four-round hostile PASS at the narrowed
`UNIVERSAL-FORM/PRIMITIVE-PROCESS-REMAINS` scope, 2026-07-11.

Question:

> Do V6–V10 sealed-diamond, evidence, holonomy, quantum, locality,
> projectivity, and gauge principles uniquely select the interactive click
> process; if not, what is the maximal exact law and which physics remains?

D12 corrected D11's scope, reconciled the corpus, froze five competing
architectures and ten gates, constructed exact classical and quantum
countermodels, audited every proposed selector, restored an explicit
multi-diamond implementation, and refused downstream geometry before
selection.

The final 145-check model has real collars, immutable records, screens, order
units, links, locally emitted opportunities, eligibility/refusal, depth-indexed
projective histories, independent unitary vertex frames, canonical
construction fibers, overlap controls, positive-support RN reconstruction,
an all-level `P_r` process, and a finite phase/two-sign threshold realization.
Quarter- and half-iSWAP packets pass the same stated gates and predict `1/2`
versus `0` for one durable record.

Result:

```text
NEXT CLICK GIVEN FULL PROCESS = EXACT DISINTEGRATION
SHARED RECORD PRINCIPLES = NONSELECTING
LEAST ACTION WITHOUT ACTION = NONSELECTING
MAXENT WITHOUT PHYSICAL CONSTRAINTS = INDEPENDENCE
PROFINITE COMPLETION = HOSTS, DOES NOT SELECT
GEOMETRY-AFTER-LAW = REFUSED
= UNIVERSAL-FORM/PRIMITIVE-PROCESS-REMAINS
```

The future program cannot honestly be “derive the same law again.” It must
find a genuinely new physical principle that distinguishes the exact
survivors and makes a holdout prediction, or empirically identify the
primitive process/action and then run V9 influence, cone, dimension, and scale
tests. Full nonunitary Lorentz integration, bridge/nucleation grammar, quantum
gravity, `G`, and the metric scale remain open.

## D13: the action behind the records

**Status:** three-round hostile PASS at the narrowed finite-kernel theorem;
formal protocol verdict `INCOMPLETE-INVESTIGATION`, 2026-07-11.

D13 audited every action-level route in the V1–V10-D12 antecedent corpus and
the primary literature.  A frozen dependency-free census covers 522 Markdown
files, 499 broad-relevant, with corpus SHA-256
`bc95bed456aca8d8e65af121ac2f4f5069bcd70b4bd6685e5d02a32b242d0c91`.
The human ledger includes the late V7 selector sequence and V4's standalone
Yang–Mills construction.

The first-round universal-action claim was rejected.  General-boundary
amplitudes remain the strongest candidate action-to-record bridge, not a
derived SHARD category or universal sewing theorem.  The final exact theorem
is narrower: on one fixed operational interval, quarter- and half-iSWAP
kernels share the tested finite local-unitary structure but predict `1/2`
versus `0`.  The dependency-free `Q(sqrt(2),i)` receipt passes 21/21 exact
checks, including an executed reversible memory circuit, interference,
no-signalling, Kraus/open limits, seal/birth, live collar, persistence and a
fresh-register repeat read.

D9 is the partial empirical exception: maximal Bell records identify
`theta=pi/4` inside its frozen partial-iSWAP preparation family.  Its fresh
geometry holdout then refutes treating `sin^2(theta)=1/2` as the geometric
diffusion coupling.  Thus one parameter can be identified within a model
without selecting the complete action.

Result:

```text
FINITE LOCAL UNITARY KERNEL NONUNIQUENESS = PROVED
GENERAL-BOUNDARY ACTION-TO-RECORD BRIDGE  = CANDIDATE
COMPLETE UV ACTION/FIELDS/STATE/RECORDS   = NOT SELECTED
V9 GEOMETRY HOLDOUT                       = CORRECTLY WITHHELD
FORMAL D13 VERDICT                        = INCOMPLETE-INVESTIGATION
```

The live selector routes are a derived causal-set/diamond quantum measure
with a stable `3+1` phase, a controlled asymptotically safe matter-gravity
trajectory, or new record-sensitive experimental evidence.  Until one
distinguishes the exact survivors and passes a held-out prediction, the
requested complete law is not known.

## D14: regional amplitudes and instruments to recorded histories

**Status:** hostile closure PASS at `BRIDGE-CONDITIONAL` scope, 2026-07-11.

D14 constructs the finite `FSDiam` bridge that D13 left as a proposal.  The
abstract source is a free strict symmetric-monoidal category on a supplied
finite typed signature; the executable tests its exact matrix image.  For a
fixed supplied DAG, all topological contraction schedules agree.  This is an
evaluation-schedule gauge, not a derivation of the diagram-generation law.

Hostile round 1 rejected the initial 30-check version because record
protection was diagnostic rather than enforced, live collars did not license
continuation, unowned joins were admitted, history records were appended
globally, memory deletion was absent, and the action-level headline outran
the supplied kernels and instruments.

The 42-check repair makes overwrite construction raise; propagates explicit
record identity through composition, tensor, symmetry and fresh insertion;
rejects missing/dead collars;
requires explicit component-join entitlement; builds depth-1..3 histories by
sequential local seals; integrates projectivity with a visible non-Markov
memory packet; and proves that a complete CPTP reset changes the process.

Result entering focused closure:

```text
SUPPLIED REGIONAL AMPLITUDES + STATE + RECORD INSTRUMENTS
  -> EXACT PROJECTIVE DURABLE RECORDED HISTORIES
FIXED-DAG EVALUATION ORDER = GAUGE
DIAGRAM GENERATION / JOIN-ENTITLEMENT ORIGIN = OPEN
PHYSICAL ACTION -> KERNELS / AUTONOMOUS RECORDS = OPEN
EXECUTABLE CORE = PASSED 42/42
PROTOCOL VERDICT = BRIDGE-CONDITIONAL
MAXIMAL ACTION-LEVEL GRADE = BRIDGE-CONDITIONAL
```

The next investigation must select or empirically identify a complete
physical action packet.  No V9 cone/dimension holdout is
licensed by D14 alone.

## D15: empirical action selector

**Status:** hostile round-2 repair review in progress; formal verdict
`INCOMPLETE-INVESTIGATION`, 2026-07-11.

D15 freezes the distinction between a low-energy effective rulebook and a
fundamental universe action.  Given observed `3+1` Lorentzian field content,
locality and gauge consistency select the Einstein–Hilbert plus Standard
Model EFT operator normal form, not all coefficient values, state, record
instrument or UV completion.  A separate ledger now prints the nonminimal
`xi_H H^dagger H R` term, operator-basis equivalences, truncation domain,
curvature tower and the measured/free/RG classification of `G`, `Lambda`,
gauge, Yukawa, Higgs, theta and Wilson coefficients.

The first exact regulated dictionary was rejected at 22 checks because it
only hash-read D14, hard-coded the seal/memory, conflated phase action with
measure/support and used a frame-equivalent matrix inequality.  The 28-check
repair now constructs CNOT support by an exact `Z2` multiplier sum; states the
Hadamard vertex measure separately; imports and uses D14's actual typed
`Obj`/`Port`/`Mor`/composition interface; derives the seal from environment
injection, CNOT interaction, explicit commit and live-collar emission; builds
memory from the same CNOT primitive; and distinguishes the phase-modified
action in a fixed-frame closed experiment (`1` versus `1/2`).

The UV ledger retains `EFT4`, causal-set/BD, asymptotic-safety, spin-foam and
primitive-history candidates.  Only `EFT4` is empirically selected as a broad
low-energy normal form; all fundamental candidates lack a complete common
packet and fixed differing prediction.  Therefore the V9 holdout remains
closed.

Current candidate result:

```text
LOW-ENERGY EFFECTIVE OPERATOR FORM = IDENTIFIED GIVEN OBSERVED INPUTS
FINITE ACTION -> D14 DICTIONARY     = EXACT WITNESS, NONGRAVITATIONAL
AUTONOMOUS UNIVERSAL RECORDS        = NOT DERIVED
G / MATTER COEFFICIENTS             = MEASURED OR FREE/RG DATA
3+1 / ROUND CONE                    = EFT4 INPUT, NOT EMERGENT RESULT
UNIQUE UV ACTION                    = NOT SELECTED
```

## D16: covariant causal-order action family

**Status:** finite theorem hostile-review PASS; formal D16 verdict
`INCOMPLETE-INVESTIGATION`, 2026-07-11.

D16 replaces the qubit carrier with finite strict causal orders and the full
interval-count action family `S=alpha N+sum beta_k N_k`.  The repaired exact
receipt passes 26/26: typed/owned extremal boundaries, exhaustive relabeling,
automorphisms, linear extensions, quotient gluing under independent regional
relabelings, cross intervals, exact orbit/normalization weights, and all
sixteen binary coefficient packets with sixteen distinct phase signatures.

The proved result is narrow but load-bearing:

```text
POSET RELABELING COVARIANCE + INTERVAL DEPENDENCE
  -/-> UNIQUE ACTION COEFFICIENTS

PURE exp(iS) PHASES
  -/-> NORMALIZED/PROJECTIVE QUANTUM HISTORY MEASURE

DIMENSION-SPECIFIC BDG COEFFICIENTS
  = CONTINUUM-MATCHED INPUT UNTIL A PHASE SELECTS DIMENSION
```

Typed quotient sewing can recompute the full action once, but a factorized
local boundary/corner law remains open.  The BDG provenance ledger keeps the
nonlocality/discreteness scale, `G`, quantum measure, records and `3+1` phase
explicitly unselected.

## D17: causal action to projective record measure

**Status:** protocol frozen before outcomes, 2026-07-11.

D17 now holds the causal action fixed and tests whether orbit measure,
boundary state, decoherence/record instrument, extension grammar and
projective cylinders follow—or whether the same action supports inequivalent
complete non-Markov record laws.  This is the next required layer before any
geometry holdout.

## D17-D20 final disposition — 2026-07-12

```text
D17 fixed causal action -> complete measure selection   REFUTED (finite exact)
D18 supplied finite (E,D) -> licensed conditionals      PROVED (sufficiency)
D19 finite lower shadows -> whole-history identification REFUTED (finite exact)
D20 full requested universe-law objective               NOT ACHIEVED
```

The next admissible work is not another selector slogan.  It requires at least
two complete inequivalent physical generator packets with the same observable
interface, independently fitted nuisance parameters, frozen differing
predictions, and a genuinely future untouched discriminator.  Existing V9
results remain `PARKED-AT-PROTOCOL` historical evidence and may not be reused
as that holdout.

## D21: two complete finite rulebooks at one record interface

**Status:** complete at the fixed-carrier finite-generator boundary,
2026-07-12.

The preregistered comparison constructs an interacting coherent source packet
Q and an objective hard-seal source packet C, then exposes both through the
same three-spacelike-record `X/Y/Z` interface.  Exact result:

```text
40/40 checks;
all 37 proper-support Pauli observables agree;
all one/two-record marginals agree;
XXX = 1 versus 0;
Mermin M = 4 versus 0.
```

The parameter-free microscopic C endpoint is incompatible with established
GHZ/Mermin evidence.  A live weak-collapse rival requires a named physical
`eta(M,Delta x,T,...)`, independent coefficient calibration and blind scale
points after multi-time environmental-memory characterization.

The D20 fundamental objective remains open: D21 does not grow carriers or
provide arbitrary record-record interaction, relativistic collapse ontology,
matter, gravity, units, cone predictions or an untouched holdout.

Next admissible work: lift Q and one independently motivated scale-dependent
event law to a common variable causal-carrier/process-tensor interface.  Freeze
the generator coefficients before designating a mass–separation–time–factor
coherence ladder.  Geometry remains gated until those completed survivors
print different untuned geometric predictions.
