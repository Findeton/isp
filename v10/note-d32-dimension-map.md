# D32 — the dimension map: card v2, the anchor, and the admissible kernel grid

**Status:** PIN (pre-receipt), 2026-07-12; committed before `code/d32a_card_v2_and_anchor.py` runs. Provenance labels per D20. Staged per the external plan review (`reviews/d31-plan-external-user-review.md`) and gated on D31's closure (LEDGER #139): **D32A runs now; D32B/D32C are pinned here and HELD until D32A's gates pass and its round closes.** Insulation: paper 19 untouched; the d̂ ≈ 1.76 result stays effective-order; the primary order = the D31-frozen port poset ≡ wire-DAG; covariance = a declared status column (bounded-domain-only for the none-absorbing arm, per #139); V9 discipline verbatim.

## 1. D32A — the deterministic card v2, the validated estimators, and the anchor

**(a) Card v2 [the O-E schema; the M1-class defect targeted]:** regenerate the full D29 validation suite (d ∈ {2,3,4} × N ∈ {256,512,1024} × 20 seeds) PLUS the controls at EVERY measured N (box-4 and percolation at 256/512/1024, with per-cell SDs for every carded statistic — the round-1 hardcoded-pseudo-SD defect dies here). Serialization: **fixed-decimal (12 significant digits), sorted keys** — platform-stable bytes; the card records the **numerical environment** (Python, numpy, platform triple) and its own generator hash. Determinism gate: the card string regenerated in-process is byte-identical. Card v2 gets a NEW hash and does not touch the frozen v1 (which D30's record consumed; v1 stays the historical freeze).

**(b) The order-only spatial estimator [filling the disclosed D29 vacancy]:** for a spacelike pair, the distance proxy is the **quantile over common-diamond pairs (u, w) of the longest-chain length through [u, w]** — chains only, no coordinates anywhere in the estimator (coordinates enter ONLY as validation ground truth). Gates: Spearman monotonicity vs true separation on d = 3, 4 sprinklings at N = 1024 (bar ≥ 0.60, pre-registered lower than the coordinate proxy's — chains are noisier); the **naive-min degeneracy measured and disclosed** (the RW failure mode: the minimum's drift with N printed); PASS makes the estimator card-carried as "spatial (order-only, validated)"; FAIL leaves the row vacant with the measurement printed — either outcome is a result.

**(c) The midpoint-scaling estimator in-receipt** (the review implemented it externally and found concordance; now receipt-carried): on the maximal interval, the midpoint m maximizes min(|I(u,m)|, |I(m,w)|); d̂_mid = −log₂(|I(u,m)|/|I(u,w)|) with the same-element convention pinned in-code. Validated on d = 2, 3, 4 sprinklings (bands carded); then the concordance gate at the anchor.

**(d) The anchor [the D30 collar cell reproduced under v2]:** regrow K_collar at its frozen D28 values (the D30 seeds, N = 512 events, the frozen primary order), measure d̂_MM and d̂_mid with card-v2 bands. Gates: d̂_MM within ±0.10 of the D30 record (1.76); the MM/midpoint concordance |d̂_MM − d̂_mid| ≤ 0.15; the M²-exclusion re-verdict under v2 bands (must remain excluded — a flip would be a card-v1/v2 inconsistency finding, not a physics change).

**(e) The determinized exception census [the D31 O5 debt]:** the ≥2-touch and ≥7-touch same-pair rates over 30 seeds at N = 256, mean ± SD — the honest exception bookkeeping D32B consumes.

## 2. D32B [HELD until D32A closes] — the admissible dimension map

One canonical seed (the D28 chain seed); the 3×3 kernel grid — collar radius r ∈ {1, 2, 3} × interact-weight ratio λ ∈ {½, 1, 2} — at N ∈ {256, 512}, **≥12 seeds per cell** (9 cells; 216+ growths); the anchor cell included. Per cell: d̂_MM, d̂_mid, matched-d chain constant, 3-chain ratio, α (from both sizes, with its two-point-slope SE printed), the REALIZED interaction fraction (λ is a proposal ratio), and the covariance-status column (all grid kernels are stationary + covariance-violating per D31; recorded, not filtered). Verdicts: the CORRECTED joint rule only (card-v2 bands; matched exponents; ratio3; the mimicry bar from the now-carded control SDs at matched N); **multiplicity: Bonferroni over the 9 cells** for any band-entry claim; **seed-forgetting**: the star and triangle seeds run at the anchor and the two grid extremes only — if d̂ remembers the seed at matched N beyond 2 SE, the framework result is seed-dependence, and the map is re-scoped. Fractional readings carry the effective-order scope verbatim.

## 3. D32C [HELD until D32B closes] — conditional refinement

Only if D32B shows a direction: a dense scan of the moving parameter (continuity assessed there, never from 3 points); fresh confirmation seeds for any band claim (the 45e/48d discipline); then the **S²-mechanism family as a separately pinned hypothesis** (direction-labeled registers; opportunity weights by direction compatibility over a discretized sphere) — a mechanism test, NOT a derivation of S² (the corpus's "why S²" stays open).

## 4. Round-1 fronts (pinned)

(F1) Card v2's 12-digit rounding: does any gate decision sit within rounding distance of its bar (decision-stability audit)? (F2) The order-only spatial estimator's chain-length granularity at small diamonds (integer chains quantize distance — bands honest?). (F3) The midpoint convention's bias at small |I|. (F4) The anchor's ±0.10 reproduction bar — is seed-matched regrowth genuinely identical-distribution (the determinized sampler changed the draw stream at #139: the D30 record's exact numbers are NOT expected to reproduce; the BAND is the claim — stated). (F5) The census's N-dependence (256 vs 512 rates). (F6) B's λ-axis realization (proposal-vs-realized divergence direction).

## 5. D32A outcomes (2026-07-12; LEDGER #140) — post-run addendum

**GREEN 6/6** (`code/d32a_card_v2_and_anchor.py`; attempt-1 crash recorded in LOG — estimator-domain pre-filter + pin-(b) vacancy handling, no bar changed). **Card v2 FROZEN at sha256/16 b5e77c0c3196fbd2** (env: python 3.8.20, numpy 1.23.5, macOS-26.5.1-x86_64; card v1 verified untouched). **The D29 spatial vacancy is FILLED:** CARD-CARRIED at both dimensions — Spearman 0.859 (d=3) / 0.717 (d=4), bar 0.60, naive-min means 9.2/6.0 disclosed. **Midpoint receipt-carried:** 1.992 ± 0.043 / 3.024 ± 0.104 / 4.009 ± 0.182 vs 2/3/4. **THE ANCHOR HOLDS:** d̂_MM = 1.756 — the D30 record reproduced EXACTLY; concordance 0.126 ≤ 0.15; M²-exclusion under v2 bands holds (0.244 > 0.120). **The census (O5 extended):** ≥2-touch 41.9 ± 3.4, ≥7-touch 1.00 ± 1.10 per web (30 seeds, N = 256).
**Fronts after the run:** F1 DISCHARGED in-receipt (P6; tightest decided margin 0.0238, the concordance) — with a disclosed coverage gap: P6's list omits the M²-exclusion margin (0.124 by hand; no flip risk; completeness fix owed to round 1). F2 OPEN (integer-chain granularity visible: naive-min means 9.2/6.0). F3 SIGNAL OBSERVED: the anchor midpoint reads 0.126 low vs MM while the N = 1024 validation cells are unbiased ≤ 0.024 — the pinned small-|I| bias direction; quantify in-round. F4 RESOLVED: exact reproduction — the D30 grower is array-deterministic; the determinization concern belonged to the census grower (a different object). F5 OPEN (census N-dependence unmeasured). F6 = D32B's. Also owed to round 1: anchor prints carry means only (no seed-spread SDs). **D32B stays HELD until round 1 closes.**

## 6. Round-1 repairs pinned (2026-07-12; report `reviews/d32a-round1-integrated-hostile-review.md`) — committed BEFORE the delta rerun

Round 1: **MAJOR-REVISION — 2 MAJOR / 7 MINOR / 5 NIT; zero pinned-gate flips; zero false printed numbers**; the reviewer's independent rerun reproduced every artifact byte-identically and the anchor bitwise (per-seed d̂ values equal to a verbatim D30 copy; seed formula/count/trim/weights all verified). Repairs, pinned here pre-rerun:

**(M1) P2 conventions DECLARED** (the pin left tie-handling and the truth column open; the frozen rho was index-tie-broken "Spearman" against frame-coordinate |dx| — the four convention readings at d = 4 straddle the bar: {0.584, 0.641, 0.717, 0.799}): the deciding statistic becomes **canonical tie-averaged Spearman against INVARIANT proper distance √(dx² − dt²), pooled over three fresh sprinklings × 40 pairs per d** (seeds BASE+777d+{1,2,3}); **bar 0.60 UNCHANGED**; pair-bootstrap CI (10k) and P(rho < 0.60) carried in-card; the frame-coordinate pooled reading disclosed alongside; the original 20-pair stream replayed bitwise (a gate — guards the estimator against unintended change) with its 2×2 convention audit (tie × truth) receipt-carried; vacancy admissible per pin (b). **D32B consumption rule: the spatial row is advisory-only if P(rho < 0.60) > 0.10.**

**(M2) The census record forward-corrected:** the receipt's grower is a RE-IMPLEMENTATION of d31b's (grown-acts-only N; z-padded child names) — same seed family, DIFFERENT webs at shared seeds ((≥2, ≥7) = [(40,4),(36,1),(42,3)] vs B4's [(41,3),(38,0),(41,1)]); the two published census datasets are non-commensurable at shared seeds. **#140's "consistent with #139's 0–3 range" is WITHDRAWN as a range statement — the honest per-web ≥7 range in this ensemble is 0–4** (max 4 at seed 0; 18/30 webs ≥ 1, so "present in most sampled webs" survives at 60%). **D32B is pinned to THIS ensemble definition** (grown-acts-only N = 256, padded names, ordered pairs, seed family 41260712+s).

**(m5 → the F3 CORRECTION; supersedes §5's F3 sentence:** the measured M²-calibrated instrument bias at matched interval size |I| ≈ 203–218 is **−0.017 ± 0.014** (the +1-endpoint-convention share −0.0155) — about one-eighth of the anchor's 0.126 MM/mid gap; the gap is **2.66 SE from zero: a REAL property of the non-manifold order at d̂ ≈ 1.76**, carried as a finding, not an estimator artifact. Concordance fragility stated: the 0.024 margin = 0.50 × SE(paired diff), so ≈30% of fresh-seed replications would fail the 0.15 bar — the pinned gate is deterministic on the frozen D30 seeds, and the fragility now travels with the record.)

**Instrument v2.1** (the card sha will supersede b5e77c0c3196fbd2; recorded in the LEDGER at closure): generator-code sha recorded IN the card (m1 — the pin clause the draft dropped); chain-constant SD fields (m2); the naive-min drift row across N ∈ {256, 512, 1024} (m3 — feeds F2); P1 relabeled to serialization-idempotence plus an in-process state-leakage recompute check, with cross-process byte-identity credited to the round's independent rerun (m4); anchor prints carry seed-spread SDs — 1.756 ± 0.101, 1.630 ± 0.127, paired diff 0.126 ± 0.150 (m5); spatial rows carry the domain qualifier ("common-diamond pairs"), convention fields, and CI (m6); the census ≥7 threshold scoped — exact for the grown single-angle alphabet (asin(3/5)+asin(4/5) = π/2 forces 8/7/6-interact minima, 7/6/5 with the birth rotation), and the necessary count drops to 6 (5 with birth) if any D32B kernel enters the third pinned gate (576/625 = the 2·asin(3/5) identity) — the ordered-pair convention stated, the unordered reading printed (m7); n1–n5 cosmetics (tally-line gate taxonomy; EPS/Q vestige removed; "%.12g = fixed-significant" wording with the honest platform-stability rationale; card file written only on P1-pass; N from M.shape).

## 7. External review #2 adoptions (2026-07-12; `reviews/d31-d32-external-user-review-2.md`) — the delta-2 pin, committed BEFORE the delta-2 rerun

Five adoptions from the second external user review (triage in the filed report's header; items already in the record — the O3 debt, the 2.66-SE gap, the covariance status column — confirmed, no action):

**(1) The P2 CLUSTER-DEPENDENCE defect [NEW; the delta-2 receipt change]:** the v2.1 certification's 120 pairs come from 3 sprinklings; the pair-level bootstrap treats them as independent, so P(<bar) = 0.000 is overconfident (the in-card per-sprinkling rhos are the honest spread). **Delta-2 design, pinned:** 12 FRESH sprinklings × 10 pairs per d at N = 1024 (seeds BASE + 20000 + 100·d + k, k = 0..11 — a fresh family, collision-checked); per-sprinkling canonical-invariant rho_k carried (12 values); the pooled 120-pair rho is the point estimate; **the interval and P(<bar) come from a SPRINKLING-level bootstrap (10k resamples of the 12 clusters, pairs pooled per resample) plus a jackknife-over-sprinklings SE — both carried**; bar 0.60 UNCHANGED, decided on the pooled rho; **the §6 advisory rule re-pinned onto the cluster-level P(<bar) > 0.10**; vacancy admissible; the v2.1 3×40 pooled readings (0.814/0.787) carried as provenance fields citing the v2.1 sha (superseded certification, not re-measured); the attempt-2 bitwise replay guard unchanged. Card schema v2.2 (same file path; v2.1 lives in history at 6090d55; supersession recorded at the LEDGER).
**(2) RANKING-vs-METRIC scope [wording, card-carried]:** the certification is rank MONOTONICITY over common-diamond pairs — an order-only spatial RANKING instrument. No absolute scale, no calibrated distance, no scaling-law claim. The card row gains a `certifies` field saying exactly this; "the spatial vacancy is FILLED" is henceforth read at this width (LOG/LEDGER wording aligned).
**(3) THE SIGNATURE PAIR [headline reframe]:** the anchor's deliverable is the ordered pair **(d̂_MM, d̂_mid) = (1.756 ± 0.101, 1.630 ± 0.127)** — a reproducible, decisively non-M², MULTI-EXPONENT order geometry (the 0.126 gap is real at 2.66 SE per §6; different scale-sensitive estimators see different effective exponents, and no single dimension is claimed that every manifold estimator must recover). Single-number narration ("the web has dimension 1.76") and D30's "MM/midpoint concordant" phrasing are SUPERSEDED (the D30 record's concordance-bar PASS stands as a bar pass; the agreement narration does not — LEDGER line owed). P4's prints carry the pair explicitly; D32B tracks the pair per cell.
**(4) O3 PROMOTED AHEAD OF D32B; D32B RENAMED:** the §2 grid is henceforth **"the kernel-mechanism map (PHENOMENOLOGICAL)"** — a geometry-mechanism experiment (how r and λ move the signature pair), with the covariance-status column as before; NO cell is presented as a candidate physical law. Before it runs, **O3 executes as its own receipt** (`code/d31c2_graded_feasibility_degree.py`): the D31C §3 pinned exact log-linear elimination (rational Gaussian elimination on path-covariance equations over the depth ≤ 3, u ≤ 5 reachable set), now for the OPEN families — simple-graph degree, distance-degree (family (ii)), motif, component-size — deliverable per family: EXISTS-candidate (sample rational solution) or OBSTRUCTION-instance (the inconsistent pair printed); reconnaissance scope verbatim (no unbounded-existence claim without an extension theorem). If a covariant interacting family EXISTS at this horizon, mapping ITS dimension signature becomes D32B's primary axis (the grid re-scopes to include it); if all OBSTRUCTED, the phenomenological grid proceeds as the only game, honestly labeled, and the covariance question escalates to the O2 unified lemma.
**(5) O7 NAMED OPEN — the covariance fork, record-theoretically:** path-covariance as a physical requirement presumes construction order is GAUGE; if accretion order is itself recorded physical history (flags-observable), the covariance quotient is a PHYSICAL HYPOTHESIS about what the record contains, not a mathematical necessity — the deepest remaining fork over the D31 no-go's reach. Carried alongside O2/O3; any D32B-class claim states which side of the fork it assumes.
**Bookkeeping note:** the review's "v2.1 uncommitted / no #141" was stale at receipt (committed at 6090d55); its remaining accurate point — the referee delta-verification outstanding — is in flight and lands before the delta-2 rerun.

## 8. The §7(4) fork RESOLVED + the D32B amendment (2026-07-12; post-O3; LEDGER #144) — pre-D32B

**The fork's condition fired on a technicality and is resolved in words (the round's M1 repair):** O3 found a covariant interacting family at the horizon — component-size, via the D5b witness (w_i = c < 1/(U(U−1)), w_b(s) = (1 − (s−1)(s−2)c)/(s−1), Z ≡ 1, every independent two-op pair exact). But the witness is EXACTLY D31A's priced bounded-growth escape: its interact rate must know the size cap U, i.e., it encodes a FINITE record universe — extra physics against the program's grammar, which is pinned none-free unbounded-growth (records keep being born; the click law must sustain unbounded record creation). **Under the unbounded-growth reading the §7(4) conclusion stands: D32B proceeds as the kernel-mechanism map (PHENOMENOLOGICAL) — now by argument, not by assertion.** The three genuinely open exits from the no-go remain: richer gradings (multi-radius/age-degree — exists-side undecided), the O2 unified lemma, and the O7 fork itself.
**D32B amendment [adopted]:** the grid gains ONE honestly-labeled **covariant bounded-domain comparison cell** — the D5b witness kernel run as a growth process at the D32B sizes (its covariance-status column reads COVARIANT-AT-CAP, unique among the cells) — so the map can measure whether horizon-covariance itself moves the signature pair (d̂_MM, d̂_mid). No cell, including this one, is presented as a candidate law; the witness cell's cap-dependence is its printed price.

## 9. D32B FINAL PIN (2026-07-12; pre-receipt; supersedes §2's held draft where they differ) — the kernel-mechanism map (PHENOMENOLOGICAL)

**Deliverable:** how kernel structure moves THE SIGNATURE PAIR (d̂_MM, d̂_mid) on the frozen primary order (port poset ≡ wire-DAG) — a MECHANISM map. No cell is a candidate law (§8; O3 terminal, LEDGER #145). Instruments: card v2.2 (e7bc80f0be08f537) bands + controls; the D30 corrected joint verdict rule verbatim; the census bookkeeping column per cell.
**The grid (10 cells):** r ∈ {1, 2, 3} × λ ∈ {½, 1, 2} with weights w_none = 1, w_birth = U, w_r-collar = λ·|{ordered unsealed pairs at d_cover ≤ r}| — so (r=1, λ=1) IS the anchor cell (K_collar exactly); PLUS the **COVARIANT-AT-CAP cell** (§8): the D5b witness kernel as a growth process — complete-graph interacts at w_i = c with c = 1/(4N²) (positive for every realizable component size at N acts), births at w_b(s) = (1 − (s−1)(s−2)c)/(s−1), no none op (Z ≡ 1 exactly). **Disclosed confound:** the witness cell differs from the grid in BOTH covariance status AND interaction range (complete-graph vs collar) — its reading informs the covariance axis, it does not isolate it (the collar-restricted comp-size kernel is NOT covariant; the witness formula needs the complete-graph opportunity count).
**Sizes/seeds:** N ∈ {256, 512} events (grown acts), **12 seeds per cell per size**; seed = 50260712 + 100000·r + 10000·li + 1000·(N/256) + s (li ∈ {0,1,2} indexes λ; the witness cell uses r = 9); s = 0..11 — disjoint from every prior family by construction. The D28 chain seed everywhere except the seed-forgetting arm.
**Per cell:** d̂_MM (max-interval sub-order at |I| ≥ 64; whole-order fallback counted and disclosed per cell), d̂_mid, the SIGNATURE PAIR with per-seed SDs and the paired gap, matched-d chain constant, ratio3, α from the 256→512 two-point slope (SE printed; two-point caveat pinned), REALIZED interaction fraction (λ is a proposal ratio — F6), the ≥7-touch ordered census rate, and the covariance-status column (VIOLATING for the 9 grid cells per D31/O3; COVARIANT-AT-CAP for the witness cell).
**Verdicts:** the corrected joint rule only (MM within 3·SD of an integer-d card cell AND matched-d chain constant in-band AND ratio3 in-band AND separated from box-4/percolation at matched N); **Bonferroni over the 10 cells** for any band-entry claim; fractional readings carry the effective-order scope verbatim; single-dimension narration BARRED (the pair is the object).
**Seed-forgetting arm:** star and triangle seeds at (r=1, λ=1), (r=1, λ=½), (r=3, λ=2) — 12 seeds each, N = 512 only; if any |Δd̂| between seed classes exceeds 2·SE at a matched cell, the framework result is SEED-DEPENDENCE and the map re-scopes.
**Fronts (pinned):** (BF1) the witness-cell confound above; (BF2) r = 3 at small n approaches the complete graph (K_tail-adjacent regime — the r-axis may saturate; report the realized mean d_cover of accepted interacts per cell); (BF3) the α two-point slope is descriptive, not a scaling claim; (BF4) F6 (λ proposal-vs-realized divergence direction); (BF5) the census ≥7 threshold is alphabet-scoped per §6 (m7).
