# D13 protocol — find the action behind the records

**Status:** frozen before D13 corpus extraction, new literature search,
executables, parameter fitting, or geometry outcomes, 2026-07-11.

## 1. Objective

Derive—or uniquely select on independent evidence—the simplest local,
generally covariant amplitude/action law for interacting sealed diamonds that:

```text
uses no preferred global commit clock;
generates a compatible non-Markov whole-history process;
creates durable records and continuing typed collars;
recovers quantum interference, entanglement, and Born records;
recovers relativistic causal structure;
fixes or explains its field content, couplings, and physical scales;
makes untouched predictions for influence, cones, dimension, and gravity.
```

## 2. Missing object after D12

D12 proves that a supplied grammar `G` and history measure `mu` give the
conditional click law by disintegration.  D13 must not rename either input an
explanation.

The action-level candidate packet is

```text
A = (T, G, K, S/Amp, rho_boundary, R, Q, U),
```

where:

```text
T             field/record/collar types;
G             locally owned extension grammar;
K             local diamond amplitude or instrument kernels;
S/Amp         action or equivalent whole-history amplitude functional;
rho_boundary  root/initial/boundary condition;
R             durable-record/decoherence rule;
Q             construction-order and frame gauge quotient;
U             conversion to physical clock, length, and coupling units.
```

For a finite canonical physical history `H`, an amplitude-first law must state
which unresolved constructions are coherently summed and which durable
records are exclusive:

```math
\mathcal A(\alpha)
 = \sum_{H\to\alpha}\prod_{D\in H}K_D,
\qquad
D(\alpha,\beta)
 = \operatorname{Tr}(C_\alpha\rho C_\beta^\dagger).
```

An action form `exp(iS/hbar)` is equivalent only after its measure, boundary
terms, contour, gauge fixing, and convergence/regularization are supplied.

## 3. Physical equivalence

Candidate actions are compared only modulo declared observational
equivalences:

```text
local invertible field/frame redefinitions;
pure gauge variables and gauge fixing;
overall history-independent phase/normalization;
boundary coboundaries that leave the fixed boundary experiment unchanged;
construction presentations inside one canonical partial-history fiber;
redundant operators proportional to equations of motion;
record relabelings that preserve every instrument probability.
```

Two actions are physically inequivalent when some allowed preparation,
intervention, durable record, scattering quantity, influence relation, or
scale prediction differs.

## 4. Candidate architecture classes

Every surviving class must be implemented or excluded by theorem.

### A — primitive path measure

`mu` is fundamental and `S=-log(dmu/dnu)` is a representation.  This is the
D12 endpoint, not an action derivation.

### B — finite local quantum rewrite action

A finite grammar plus local unitary/isometric/Kraus kernels generates class
operators and records.  D8/D12 establish completeness conditional on kernels.

### C — holonomy/topological action

Local closed-diamond holonomies, curvature defects, or cocycles supply the
phase/action.  Must distinguish measured holonomy from a fitted encoding of
the resulting law.

### D — evidence/information action

RN/KL/Fisher evidence, commitment, maximum caliber, or an information metric
supplies `S`.  Must derive rather than assume the physical sufficient
statistics, reference, constraints, and coefficients.

### E — symmetry/EFT action

All local operators allowed by types, gauge/Lorentz symmetry, unitarity, and
cluster/causal structure are included.  A selector must fix field content,
operator coefficients, vacuum, and relevant trajectory rather than merely
list them.

### F — geometric/gravitational action

Regge/Einstein-Hilbert/causal-set/diamond-curvature weights generate both
records and geometry.  The metric or order used by the action may not be
silently imported from the desired continuum.

### G — amplitude bootstrap

Locality, unitarity, analyticity/crossing, positivity, soft limits, and
factorization constrain observable amplitudes without a fundamental
Lagrangian.  It succeeds only if one equivalence class and its dimensionful
scale are selected.

### H — empirically identified effective action

Known quantum fields plus gravity are treated as the best measured low-energy
packet.  This is a legitimate empirical result only with a clear UV/record
boundary and untouched predictions; it is not a parameter-free derivation.

## 5. Frozen gates

### A0 — corpus recovery

Extract every V1–V10 action, amplitude, diamond, holonomy, evidence,
composition, field, scale, and gravitational claim with its hypotheses and
retractions.  No rediscovered older structure may remain outside the ledger.

### A1 — complete typed ontology

Every field, collar, screen, link, kernel, outcome, record, opportunity,
boundary datum, reference measure, and scale is explicit.  No silent center,
root, bridge, metric, frame, or regulator.

### A2 — locality without a global clock

Each elementary kernel is owned by a recorded connected collar.  Disjoint
firings commute/compose into one canonical fiber; overlapping firings retain
physical order.  A global linearization is calculation gauge only.

### A3 — amplitude/action gluing

Eventless gluing, diamond composition, coherent sums, exclusive records, and
boundary terms obey explicit associative/refinement laws.  Row-normalized
local weights may not destroy construction gauge.

### A4 — quantum consistency

The action yields normalized positive instrument probabilities, interference,
entanglement, no-signalling for disjoint laboratories, and the appropriate
unitary/open-system limits.  Born weights may not be inserted twice.

### A5 — record formation

Durability, repeat-read meaning, decoherence/coarse-graining, output birth,
and observational truncation are distinct.  The record rule is derived from
the dynamics or declared primitive and graded accordingly.

### A6 — covariance and gauge

Independent vertex frames transport states, effects, order units, screens,
links, kernels, anchors, and history amplitudes.  The claimed Lorentz/gauge
group is tested, not inferred from one global unitary basis change.

### A7 — uniqueness under equivalence

Enumerate or construct all candidates surviving A0–A6.  One pair with the
same fixed principles and a different observable refutes unique derivation.
“Simplest” requires a representation-invariant prior/complexity theorem.

### A8 — couplings and field content

Every mass, mixing, coupling, cosmological term, rank/factor choice, and
initial-state parameter is derived, independently measured, or explicitly
left primitive.  Symmetry-allowed coefficients cannot disappear by wording.

### A9 — physical units and gravity

Identify the evidence/proper-time bridge, length standard, `hbar`, light-cone
scale, and gravitational coupling.  Coordinate conventions or SI definitions
do not count as predictions.  Synthetic known-answer calibration precedes
unknown geometry.

### A10 — independent empirical selection

Any empirical selector is frozen before the selected candidate's prediction
set is opened.  Fitting and validation datasets are disjoint.  Existing
quantum/relativistic facts used to formulate the packet are not novel
predictions.

### A11 — spacetime holdouts

Only a uniquely derived or independently selected packet enters influence,
round-cone, scale-ladder, dimension, and gravitational tests.  Both V9 shape
conventions and a drift-safe dimension instrument are required.

### A12 — hostile closure

Independent mathematics, ontology/locality, and clean-room reconstruction
reviews investigate every opening before the next round.  Final claims and
hash manifests must match the reviewed bytes.

## 6. Verdicts

```text
UNIQUE-DERIVED-DIAMOND-ACTION
  A0–A9 leave one physical equivalence class and A10–A11 predict correctly;

EMPIRICALLY-IDENTIFIED-DIAMOND-ACTION
  structure leaves alternatives, independent data select one, and holdouts pass;

UNIVERSAL-ACTION-ARCHITECTURE/PRIMITIVE-COEFFICIENTS
  local amplitude form is fixed but fields/couplings/state/scale remain;

EFFECTIVE-ACTION-ONLY
  one tested low-energy packet works within a declared regime but UV/action
  selection and/or record ontology remain;

ACTION-SELECTION-REFUTED
  a proposed selector makes a frozen prediction that fails;

INCOMPLETE-INVESTIGATION
  a promised class, gate, calibration, review, or receipt is absent.
```

The words “the action of our universe” require empirical identification and a
novel holdout.  A mathematically complete toy action earns no such title.
