# D43c (N3) — the arbitration pincer vs the two escape classes

**Status:** CAMPAIGN PIN (strict), 2026-07-19. Parents: D43 #327
(import I2 [POSITED]); d42b4 E1 (the three-horn pincer: a lifted arb
step operator is cut-independent ⇒ the zero class / cut-dependent ⇒
carrier overlap / dilated ⇒ cut data returns); paper 30 residue 2.
Receipt: `v10/code/d43c_pincer_escapes_exact.py` (mp.dps 50 where
amplitudes enter; Fractions elsewhere).

## 1. The question, and why door-counting is the risk

The pincer classified CHANNELS ATTACHED TO CUTS. Two known escape
classes elsewhere evade FLP-type trichotomies: (E-A) RANDOMIZED
protocols (Ben-Or's escape from FLP — and the grammar's arbitration
is already click-randomized), and (E-B) HIGHER-ORDER operations
(quantum combs / process-matrix types: operators whose input
includes the causal structure, the natural type for an event that
CREATES structure). The pinned question: does either class contain
an object meeting the arb layer's requirements — and if not, the
pincer extends to a stronger no-go. Pre-registered open; either
answer is a result.

## 2. The requirements an escape must meet (from the record)

(R1) reproduce the pinned menus at both cuts: at [pA0] the
singleton-only sector (sum 1); at [pA0,pB1] the blind pair included
(sum 5/4) — the d42b4 pincer exhibit's own numbers. (R2) act only
on carriers its type licenses (no reading the blind wire except
through a licensed join input). (R3) preserve the kernel weights
(K1's 1/2-1/2 at the pair; the click-chain refinement d42b2-B1
compatible). (R4) NSE/D25/D27: the record-side maps are
distinguishability isometries. (R5) no cut-attached classical data
smuggled in ancilla form (the dilation horn's test, applied to the
escape candidate itself).

## 3. Gates

- **PG1 (the horns reproduced):** the three-horn exhibit re-run from
  the committed d42b4/d42a machinery (menus 2-sum-1 vs 4-sum-5/4;
  blind branches inadmissible early) — the baseline.
- **PG2 (E-A, randomized):** formalize the Ben-Or-shaped candidate:
  the arb step as a CLICK-CONDITIONED family (the operator applied
  depends on a recorded coin local to the initiator, with the join
  fired only on agreement-shaped click patterns). Test R1-R5
  mechanically on the pair fixture. The pre-registered structural
  hazard: the coin is initiator-local, so the JOIN's firing
  condition still needs pair-visibility — does the randomization
  relocate the cut-dependence into the firing predicate (then E-A
  fails R2/R5 the same way — exhibit exactly where), or does the
  d42b2 join-typed opening click (carriers = the component's
  proposers, ALREADY committed grammar) license the visibility
  (then E-A reduces to E-B's typed-input form)?
- **PG3 (E-B, comb-typed):** formalize the comb candidate: the arb
  operation's TYPE takes the component (the ckey, per A6 event data)
  as a classical input wire — i.e., the operation is a family
  {V_C} indexed by the join's own past-visible component, exactly
  the shape of the committed join-typed opening click (d42b2 B1).
  Test: with the component as a licensed input, do R1-R5 all pass
  on the pair fixture (the isometry V_C built from the d42b2 chain:
  opening click + continuations + acceptance, now as ONE typed
  higher-order operation)? If yes: THE FOURTH DOOR — the arb layer
  lifts as a comb, not a channel; state exactly what the comb's
  type carries that a cut-channel cannot (the component wire), and
  what remains open (the MEASURE side: the comb family's weights
  are still the classical q's — the completion question does not
  vanish, it relocates to the comb-family level; declare). If no:
  the pincer extends — print the failing requirement.
- **PG4 (honesty):** whatever passes, the verdict sentence must
  state the scope: fixture-level (the pair + path), grammar =
  d42a-terminal + the d42b2 click layer; no continuum claim;
  Hegerfeldt untouched.

## 4. Scope

This receipt DECIDES only whether the two named classes clear the
five requirements at fixture scale. A PG3 pass is an existence
exhibit for the fourth door, not the full quantum completion (the
weight/measure side stays with residue 1); a PG3 fail extends the
no-go to the strongest operation class the record can currently
type. [POSITED imports; the FLP/comb correspondences are named
analogies until a formalization receipt makes them exact.]

## 5. Round-1 amendments (2026-07-19; round frozen at LOG #339;
## report: reviews/d43bc-round1-hostile-review.md)

**C1 (F-C1, BLOCKER — owned): PG3 was extensional theater; no
operator existed.** The receipt never built the {V_C} family its
verdict announced: R1 was literally x == x (comb_menu and arb_menu
were the identical filter of the identical source — proven
extensionally on all 215 depth<=3 histories, and the substitution
mutant is byte-identical), R2/R5 were regs_of/admissible
restatements, and R4 was the d42b4-QG4 basis-copy toy rerun
verbatim, attributed to an object absent from the code. "The
arb-layer operator EXISTS at fixture scale" was claim-wider-than-
computation — the d42b4-F2 conviction class recurring at the same
front one round later. Owned.

**C2 (R-C1 adopted): the referee's V_single/V_pair construction is
now the receipt's PG3.** From committed machinery only: V_C =
Acceptance composed with OpeningClick (the d42b2-B1 join-typed
chain composed as ONE typed higher-order operation; for |C| = 2 the
chain is one click + acceptance, |rec> -> sum_w sqrt(1/2)
|rec, w, v(C,w)>). Gated mechanically: the matrix product; both V's
AND both factors isometric at 1e-40 (mp.dps 50); Born branch
weights = the committed K1 kernel (1/2-1/2 on the pair,
deterministic on the singleton, PK1 recomputed from the layer); the
MENU RECONSTRUCTION — (past-local admissible-ckey index x the
budget share 1/4 / #components, read from the layer's own past-view
machinery) x (the V_C Born splits) — equals the committed menus at
BOTH cuts exactly, in Fractions (the non-tautological form of R1);
CUT-INDEPENDENCE where it belongs — the SAME matrices serve at both
cuts, only the classical index set differs ({singleton} early;
{singleton, pair} at the join), and the index is computed by
admissible() from the event's own past cone.

**C3 (F-C2/F-C3): "the two residues are now ONE" and "residue 2's
operator question is answered" are WITHDRAWN.** Nothing was
answered without the operator; and even with it, "ONE" over-merges.
The honest statement: horn 2's explicitly-open arm is now TYPED AND
CONSTRUCTED at fixture scale — residue 2's operator-EXISTENCE face
is discharged there by the exhibited family; the foliation/
commuting-family face of horn 2 at overlapping visibility (V_pair
acts on BOTH proposers' records — licensed join carriers per A4/A6,
so commutation-by-disjointness is forfeited at joins; concurrent
arb operations commute only on disjoint components) and the MEASURE
question (residue 1: each V_C's Born split sums to 1, so the
family's weights remain the classical q's) stay OPEN and SEPARATE.
"The fourth door" = horn 2's open arm, typed past-locally and
exhibited — not a new door around three closed ones.

**C4 (F-C4): E-A's exclusion theorem STANDS, strengthened by the
round.** The two gated facts imply the DISTRIBUTIONAL form: any
family measurable in (own view, private randomness) has the same
output distribution at both cuts while the required supports differ
(the pair branches inadmissible early, required at weight 1/8 at
the join) — covering coins, local clocks, counters, and any data on
the initiator's wire (clock robustness: own views remain identical
with extra A-ticks inserted). Pin PG2's pre-registered second arm,
answered in one sentence: E-A's only repair route is the
join-licensed visibility — E-A REDUCES TO E-B's typed-input form,
which PG3 now adjudicates with the constructed family. The
FLP-partial scoping (obstruction = VISIBILITY, not adversarial
scheduling) is confirmed as printed.
