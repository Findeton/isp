# D70 — result: **CONTRACTION AT EVERY POOL AND DEPTH MEASURED.** The horizon-limit route is not closed by measurement: on the pin's own object the drift contracts at every off-root depth at **two, three and four actors** (in all five norms at two and three), and the four-actor row the first delivery fired the pin's negative on is a row in the *horizon-bound absolute kernel* that rises once and **falls at the very next horizon**, ending below where it started. The horn **lives off the root and shrinks there**; at the root it cannot separate at all, and that is a **theorem of the construction**. Properness extends to `r = 7` and the chained kernel is cut-additive — by identity. The **menu-exact atom route is closed** and the **σ-level renewal route is OPEN**, which makes operator-level minorization the campaign's surviving proof-engine candidate. The two "cheap aggregations" are **one closed route**, not two.

**Status: ROUND-1 REVIEWED AND REPAIRED, 2026-07-27.**  An independent
hostile review — `v10/reviews/d70-round1-hostile-review.md`, a worker
with no prior context and no loyalty to the unit, its own family
builder, its own backward recursion, its own kernels, its own five
norms, its own terminal conventions, its own renewal predicates, its
own hitting-probability chains and its own lumpability refinement —
returned **REVISE: 5 MAJOR / 4 MODERATE / 5 MINOR**.  It reproduced
**every single number of the first delivery exactly**: the census, all
seven potentials, properness at every `r`, the complete five-norm
depth-stratified drift table, both window columns, the `×1.92`
understatement, the root ratio sequence `0.7382 / 0.3985 / 0.0864 /
7.5091` identical in all three absolute norms, the three- and
four-actor matched cells, both horn tables entrywise, the renewal
counts `5,161 / 1,365 / 3,796`, the `0.7705`, the ladder cylinders, and
both HZ5 refinement tables including the S4 controls.  In its own
words: *"I broke nothing in the arithmetic.  I broke the readings."*

**Four of the five MAJORs were inference defects; the fifth was the
computation this unit named, priced, called "the cheapest thing in the
campaign and the one that would re-grade the headline" — and then
declined to run.  The review ran it.  It re-grades the headline.**

**THE ROUND-SUPPLIED COMPUTATIONS**, credited to the round and
**rebuilt here by this receipt's own code and gated like any other
number**: the four-actor depth-4 arm (§4.5), the `A ↔ B` / `0 ↔ 1`
automorphism and the root-orbit argument (§5.2), the two extra
terminals `C5` and `C4` (§5.2), the R-SIG re-entry census and its
N-step hitting probabilities (§6.5), and the merge-vacuity partition
identity behind HZ5 (§7).  **Every one of the review's figures
reproduced exactly in this receipt's own process.**

Pin `note-d70-horizon-limit-pin.md` (STRICT, frozen and committed
**before** any code was written; it named this note and the receipt
before either existed).  Receipt
`v10/code/d70_horizon_limit_exact.py`, output
`v10/data/d70_horizon_limit_exact.out` — run from the repository root,
**exit 0**, wall clock printed by the receipt itself.  Campaign: THE
MEASURE CAMPAIGN, opened at LOG #481, scoped by
`note-d69-measure-campaign-scoping.md`.

Every number below is quoted from the receipt's own stdout.  **Where
the receipt and this note disagree, the receipt is authoritative** (LOG
#477's standing rule).

---

## 0. The one-paragraph answer

D46b's relative-horizon kernels `k_r(e|h) = q(e|h)·G(h+e, r−1)/G(h, r)`
were extended from a 30,729-history family at `r = 1, 2` to a
**243,769-history** family at `r = 1..7`, at **three actor pools** — the
widest of them now at **332,697 histories to depth 4** — under **five
declared terminal conventions**, with a symmetry analysis, a renewal
analysis, an aggregation arm, four controls and a wall diagnostic.  The
kernels are **proper everywhere they are computable**, and the chained
measure is cut-additive at every cut — **by induction from properness,
not by measurement** — so HZ-IV does not fire.  **On the pinned object
the drift contracts over the computed horizons at every off-root depth,
in both conditional norms, at two, three AND four actors.**  The
four-actor `L = 1` row that fired the first delivery's negative was a
row in the *absolute* kernel with **two terms**; at depth 4 it has
three, and it falls by 11.28% / 4.87% / 4.87%, ending **below its first
term** in `L∞` and `L1`.  The pinned conditional row at that pool never
rose at all: `1/102 → 691/89913 → 902551/161966810`, −21.61% then
−27.49%.  So the honest verdict is **contraction at every pool and
depth measured**, with a two-term blip reported as a blip — the shape
the unit's own three-actor root row had already shown (a ×1.50 rise
followed by a fall).  Underneath it, four things: D46b's committed
statement that the **root** absolute drift contracts monotonically out
to `r = 6` is **false at `r = 7`**, rising by ×7.509; the truncation
horn **lives off the root**, where the pinned separation shrinks over
five terms, and **cannot** separate at the root, because the root menu
is one orbit per event kind under the layer's own automorphisms and
every equivariant terminal is therefore forced to the same uniform
conditional; the regenerative route splits in two, with the
**menu-exact atom closed** and the **σ-level class re-entered 3,796
times and carrying a strictly positive 4-step hitting probability**;
and HZ5's two sector maps are **one map on the decider's whole input**.
Delivered outcome: **`HZ-III (bound clause NOT satisfied)` + `HZ5-b`**
— the shape without the licence, since HZ4 exhibits no bound.

---

## 1. Gate-by-gate

| gate | verdict | what it settles |
|---|---|---|
| **HZ0** (15 anchor gates = 4 provenance + 11 committed numbers) | **all PASS** | every committed D46b / D56 / D57 / D65 number this unit stands on reproduces in-process.  Exit 1 was reachable only here and was not reached. |
| **HZ1-a/b/c** | PASS / PASS / PASS | properness extended from `r = 1, 2` family-wide to `r = 1..7`.  **HZ1-a and HZ1-c are theorem-passes** and are labelled so; **HZ1-b (strict positivity) is the substantive one**. |
| **HZ2-a** | PASS | drift contracts at every **off-root** depth, all five norms, two-actor arm. |
| **HZ2-b** | PASS | the family-uniform sup is **window-dependent**; both windows printed. |
| **HZ2-e** | PASS | the terminal `0`s of the conditional family-uniform rows are **window artifacts** (root-only window) and are struck from every contraction claim. |
| **HZ2-d** | PASS | D46b's five root steps reproduce and are norm-free; the **sixth reverses**, delivered as its own outcome. |
| **HZ2-POOL4** | PASS | the four-actor depth-4 level is **318,704** histories, enumerated, with its serial wall clock printed. |
| **HZ2-f** | PASS | the deepest-layer **leaf collapse changes no number** — controlled like-for-like on the three-actor pool, both ways, all five norms, every cell. |
| **HZ2-BLIP** | PASS | the four-actor rise is a **one-step blip**: up at `r = 2→3`, down at `r = 3→4`, below its first term in two of three absolute norms; the pinned rows never rose. |
| **HZ2-c** | **PASS** | pool dependence **taken on the pinned object**, one doctrine in one direction.  Off-root pinned rows contract at **every** pool. |
| **HZ3-a** | PASS | the conventions are what they are declared to be; C3 is gated to be a pure horizon shift — a gate that cannot fail, and one the first delivery already disclosed in its own label ("gated to be"). |
| **HZ3-s1/s2/s3** | all PASS | `A↔B` and `0↔1` are **exact automorphisms** (0 violations, `G` invariant); the root menu is **one orbit per kind**, so every equivariant terminal forces the uniform root conditional *(theorem-pass)*; a **non-equivariant** terminal breaks it at `r = 1`. |
| **HZ3-b0** | PASS | the horn's root leg is **the same theorem** *(theorem-pass)*. |
| **HZ3-b** | PASS | the conventions differ at **every** history; **off the root** the pinned separation shrinks at every fixed depth. |
| **HZ4-i/ii(a)/ii(b)/iii/iv/v** | all PASS | renewal predicate verified against the layer; **R-MENU absorbing-complement (0 re-entries), R-SIG re-entered 3,796 times**; departure census; ladder cylinders; **no bound exhibited, and the σ-level route left OPEN**; positive control finds d42a's renewal. |
| **HZ5-0/1/2/3/4** | all PASS | both coarser sector maps blow up, with the S4 lower-bound control and the S3 split witness; **HZ5-4 gates that they are the same map on the decider's whole input**; the finite-alphabet prerequisite left `[OPEN]`. |
| **HZ6-i/ii/iii/iv** | all PASS | the instrument can fail (**by a weight-law perturbation**, counted apart from the grammar one); the pipeline reproduces `Ẑ`; anti-vacuity scan clean with its defect named; hash-seed determinism, digest **widened past cap 3**. |
| **HZ7** | PASS | the B1 ladder does **not** survive the restricted enumerator — diagnostic only. |
| **HZ8-a/c/b** | all PASS | no infinite-volume claim (scanned); every normalization named; the lexical prohibition enforced on **all** gate labels — HZ8-b now runs **last** and folds in its own label, so coverage is **51 of 51**, where the first delivery's scan saw 40 of 42. |

**51 PASS / 0 FAIL — of which 7 are THEOREM-PASSES that cannot fail,
each named with its reason in the receipt's own summary block**
(`HZ0-7`, `HZ1-a`, `HZ1-c`, `HZ3-s2`, `HZ3-b0`, `HZ4-ii(a)`, `HZ5-1`;
`HZ3-a`'s C3-leg is an eighth unfailable gate and was already disclosed
as such in the first delivery's own label).  **So `51 PASS` is not 51
tests — 44 of the 51 are gates that could have returned False on this
family — and the receipt says so before anyone else has to.**  4
delivered outcomes, exit 0, **wall clock 1085.8 s** (two-actor depth 6:
252.1 s; three-actor depth 4: 45.2 s; **four-actor depth 4: 407.7 s
serial and single-core**, the round-1 MAJOR 1 arm and the reason this
is the campaign's longest receipt).  **Nothing was cut**; the one arm
the first delivery *did* cut — the four-actor depth-4 row — is in.

**The delivered outcome changed.**  The first delivery reported
**HZ-I** because it evaluated the pin's clause (c) on the **absolute**
kernel while enforcing the pinned-object doctrine at HZ3.  Applied in
one direction, the doctrine gives: clause (a) `False`, clause (b)
`False`, clause (c) `False` — **HZ-I does not fire on the pin's own
object at any pool or cap this unit or the round computed.**  The
outcome tag the receipt assembles from its own predicates is therefore
**`HZ-III (bound clause NOT satisfied)` + `HZ5-b`** — that is,
**HZ-III's shape without HZ-III's licence**: HZ4 exhibits no bound, so
the standing label is the pin's own *"root-free over the computed
horizons"*, explicitly not a boundary theorem, and no HZ2 label is
upgraded.  The letter-reading on the horizon-bound absolute kernel is
printed beside it and labelled context: on that object clause (a) is
still `False` and only clause (c) fires, on the one row whose rise
reverses at the next horizon.

---

## 2. HZ0 — the anchors: 15 anchor gates, reconciled

The first delivery's note said "14 anchors" over a list of ten
bullets while the receipt printed "15".  **The receipt is the source**
and the breakdown is now printed by it: **4 provenance gates**
(`HZ0-a(i)–(iv)`: the two exec'd layers, the AST kernel lift, the σ
text slice) plus **11 committed-number anchors** (`HZ0-1..HZ0-10` and
`HZ0-7b`).

Reproduced in this receipt's own process, from the committed layers:

* **ARM-1T census** `[1, 9, 69, 521, 3969, 30729]` and **243,769** to
  depth 6; per level `1, 8, 60, 452, 3448, 26760, 213040`.
* **the quarter-quantized ladder** `{2: 3757, 5/2: 212}` at depth ≤ 4
  and `{2: 29605, 5/2: 1124}` at depth ≤ 5; unchanged value set at
  depth ≤ 6.
* **the delivery-free partner** `[1, 7, 39, 215, 1191, 6471]` and
  **34,375**; census `{2: 5963, 5/2: 508}`.
* **the transport potentials** `G_1..G_6 = 2, 4, 257/32, 1035/64,
  4173/128, 134587/2048`; delivery-free `2, 4, 257/32, 1037/64,
  2101/64, 68313/1024`.
* **both ratio columns** exactly, with the sign and the turnover.
* **the root sector-normalized conditional**, drift exactly zero at
  `r = 1..6` — **now labelled `[THEOREM]`, see §5.2**.
* **the root absolute drift** in all three norms (D46b MB3-b).
* **the off-root conditional sup** `1/18, 4/171, 8/741, 176/32877` in
  D46b's own window, with `700, 140, 36, 4` drifting.
* **the family-uniform sup** `3/110, 3/253, 373/69230, 2333/1838829`,
  same window.
* **MB5**: the `1/256` witness, matched-horizon equality at
  `r = 1, 2, 3`, non-sufficiency census `8196/30728`, `1060/3968`,
  `104/520`.

**New**: `G_7 = 2168717/16384` at transport and `139065/1024`
delivery-free; transport ratio at `D = 7` = `2168717/1076696`.

---

## 3. HZ1 — properness, extended, with the substantive half named

`Σ_e k_r(e|h) = 1` is an **identity of the construction** and the
receipt says so before printing it.  What is new is the **extent**:

| `r` | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|---|---|---|---|---|---|---|---|
| histories | 243,769 | 30,729 | 3,969 | 521 | 69 | 9 | 1 |
| `Σ_e k_r = 1` | exactly | exactly | exactly | exactly | exactly | exactly | exactly |

The pin's weak POSITIVE lean on HZ1 at `r ≥ 3` family-wide is
**confirmed at these caps**.

**The correction the round forced.**  The first delivery called
**cut-additivity** "the substantive half".  It is not: given the
identity, the chained mass at cut `n` is 1 **by induction** — it is a
product of probability kernels — so HZ-IV's cut-additivity clause was
disposed of by construction before the receipt ran, and the contrast
against the raw weight's cut masses `1, 2, 4, 257/32, …` is the
observation that the raw weight is **unnormalized**.  The substantive
properness gate is **HZ1-b, strict positivity** — the only way the
identity can break is a zero denominator — and the substantive version
of "is the family consistent across `R`" is not cut-additivity at all:
it is exactly what HZ2's drift table measures.  HZ1-a and HZ1-c are
carried as **theorem-passes** and as reporting.

---

## 4. HZ2 — the Cauchy table

### 4.1 The window-controlled table (the one that matters)

Two-actor arm, `L∞` on the absolute kernel, exact:

| `L` | `r=1→2` | `r=2→3` | `r=3→4` | `r=4→5` | `r=5→6` | `r=6→7` |
|---|---|---|---|---|---|---|
| 0 | 0 | 1/1028 | 191/265995 | 412/1439685 | 4629/187210517 | **54193948/291881114879** |
| 1 | 3/208 | 1/156 | 1/356 | 2333/1838829 | 12494788/19065729755 | — |
| 2 | 3/110 | 3/253 | 373/69230 | 89364/36687385 | — | — |
| 3 | 3/110 | 3/253 | 373/69230 | — | — | — |
| 4 | 3/110 | 3/253 | — | — | — | — |
| 5 | 3/110 | — | — | — | — | — |

and the **pinned object**, `L∞` on the sector-normalized conditional:

| `L` | `r=1→2` | `r=2→3` | `r=3→4` | `r=4→5` | `r=5→6` |
|---|---|---|---|---|---|
| 0 | 0 | 0 | 0 | 0 | 0 |
| 1 | 1/18 | 4/171 | 8/741 | 176/32877 | 302623/103087098 |
| 2 | 1/18 | 4/171 | 8/741 | 176/32877 | — |
| 3 | 1/18 | 4/171 | 8/741 | — | — |
| 4 | 1/18 | 4/171 | — | — | — |
| 5 | 1/18 | — | — | — | — |

**Every off-root row contracts, in all five norms.**  The **root** row
of the three *absolute* norms does not — §4.3.  The root row of the
pinned object is identically zero at every horizon **and that is a
theorem, not a measurement** — §5.2.

### 4.2 The family-uniform row, its window dependence, and its artifact zeros

| norm | `r=1→2` | `r=2→3` | `r=3→4` | `r=4→5` | `r=5→6` | `r=6→7` |
|---|---|---|---|---|---|---|
| `L∞` abs | 3/110 | 3/253 | 373/69230 | 89364/36687385 | 12494788/19065729755 | 54193948/291881114879 |
| `L1` abs | 6/55 | 12/253 | 746/34615 | 357456/36687385 | 74555672/19065729755 | 325163688/291881114879 |
| sector-`L∞` | 3/55 | 6/253 | 373/34615 | 178728/36687385 | 24989576/19065729755 | 162581844/291881114879 |
| `L∞` cond | 1/18 | 4/171 | 8/741 | 176/32877 | 302623/103087098 | **0 [root only]** |
| `L1` cond | 1/9 | 8/171 | 16/741 | 352/32877 | 302623/51543549 | **0 [root only]** |

**The `r = 6` column is taken over a window that contains the root and
nothing else** (the receipt prints the depth list per column).  The
final `0` of the two conditional rows is therefore **neither
contraction nor shrinkage**: it is the window emptying of off-root
histories, and the value it leaves behind is the symmetry identity of
§5.2.  The first delivery quoted those zeros in a summary sentence and
in LOG #484 ("off-root separation shrinking to 0 at `r = 6`", which is
doubly wrong — the `r = 6` row contains **no off-root history at
all**).  **Every contraction claim in this note is taken on the
off-root-window prefix**, gated at `HZ2-e`.

The fourth term is **not** D46b's:

| `r` | D46b window (`len(h)+r ≤ 5`) | this receipt (`≤ 6`) | larger? |
|---|---|---|---|
| 1–3 | 3/110, 3/253, 373/69230 | identical | no |
| 4 | **2333/1838829** (~1.269e-3) | **89364/36687385** (~2.436e-3) | **yes, ×1.92** |

**D46b's committed four-term sequence is a LOWER BOUND on the sup over
the deeper family.**

### 4.3 The root reversal — outcome `HZ2-ROOT-REVERSAL`

```
root L-inf drift, r = 1..7:
  0,  1/1028,  191/265995,  412/1439685,  4629/187210517,
  54193948/291881114879
ratio sequence:  0.7382,  0.3985,  0.0864,  7.5091
```

It falls through D46b's five steps and **rises by ≈ ×7.509 at the
sixth**, exactly, identically in all three absolute norms (the three
ratio sequences are equal, rational for rational).  The round confirmed
this is **not a window artifact**: it is a fixed-history (root)
sequence, not a sup over a window, so no window or family semantics
changed between D46b's points and this unit's `r = 7` point.

> **THE FORWARD CORRECTION TO D46b MB3-e, stated exactly.**  *"The
> root's absolute drift contracts monotonically from `r = 2` through
> `r = 6`, and **rises by a factor ≈ 7.509 at `r = 6 → 7`**; the
> monotonicity claim was true of every step D46b could compute and is
> false at the next one.  The sector-normalized conditional at the root
> is exactly 0 at every horizon — **for the symmetry reason of §5.2,
> not as evidence of stability.**"*

**What it does not touch, said correctly this time.**  The first
delivery offered the pinned object's exact 0 as a consolation: *"the
pinned object … stays exactly 0 throughout, so nothing physical
moves."*  That is **not** a consolation.  The 0 is **forced** (§5.2),
so it would have stayed 0 whatever the absolute kernel did.  What the
reversal shows is only this: a monotonicity read off five
finite-horizon points turned at the sixth.

### 4.4 Actor pools — and the clause that no longer fires

```
2 actors (A,B)      depth 6, cumulative [1, 9, 69, 521, 3969, 30729, 243769]
3 actors (A,B,C)    depth 4, cumulative [1, 16, 235, 3424, 50617]
4 actors (A,B,C,D)  depth 4, cumulative [1, 25, 593, 13993, 332697]
```

The four-actor pool is now built to **depth 4**: **318,704** further
histories, **serial and single-core** (the choice is printed by the
receipt with its wall clock; the deepest layer is held as its exact
menu-weight total, a storage decision **controlled at `HZ2-f`** by
running the three-actor pool both ways and requiring byte-identical
drift cells in all five norms).

The matched-cell comparison is now printed **in all five norms**
across all three pools — the first delivery printed norm index 0 only,
so **the pinned object was never tabulated across actor pools anywhere
in the unit**, which is precisely what the pin's falsifier 4 asks
about.  The deeper four-actor cap also widens the matched set from
**5 cells to 10**: `(1,0..3), (2,0..2), (3,0..1), (4,0)`.  The drift
magnitude still falls with actor pool in every matched cell.

*(The leaf-collapse control, `HZ2-f`: 10 cells × 5 norms recomputed on
the three-actor pool with full depth-4 menus and with the collapse,
**0 disagreements**.)*

### 4.5 The four-actor depth-4 arm — **the blip**

The row the first delivery delivered its headline on, with its third
term:

| norm, 4 actors, `L = 1` | `r=1→2` | `r=2→3` | `r=3→4` | `t2/t1` | `t3/t2` | `t3/t1` |
|---|---|---|---|---|---|---|
| `L∞` abs | 29/8288 | 23117/6594399 | **33367649/10728807102** | +0.19% | **−11.28%** | **−11.12%** |
| `L1` abs | 87/4144 | 1160/53613 | **897664/43613037** | +3.06% | **−4.87%** | **−1.96%** |
| sector-`L∞` | 39/4144 | 580/53613 | **448832/43613037** | +14.95% | **−4.87%** | +9.35% |
| `L∞` **cond** | 1/102 | 691/89913 | **902551/161966810** | −21.61% | −27.49% | −43.16% |
| `L1` **cond** | 1/17 | 744465/15495007 | **39986658899218/1059396641001655** | −18.32% | −21.44% | −35.83% |

**The drift resumes falling in every norm at the next horizon, and in
`L∞` and `L1` the third term is below the first.**  The pinned rows —
the last two — never rose at all.

> **HZ-I's LETTER FIRED ON A TWO-TERM WINDOW AND ITS SUBSTANCE
> DISSOLVES AT THE THIRD TERM.**  The honest verdict is **contraction
> at every pool and depth measured**, with the two-term rise reported
> as a blip.  **The unit's own data already contained the
> counter-example**: at three actors the ROOT row does exactly the same
> thing — `0, 1/1734, 3496/4043977, 1014199/1191210097`, a **×1.50
> rise** at step 2 followed by a fall at step 3.  Truncated at two
> terms that row would have read as "stopped contracting" too.  A
> two-term rise was already known, **inside this receipt**, to be
> non-diagnostic in this family.

The first delivery's readings are therefore **withdrawn**, in the
round's own words:

* *"It does not contract at four actors"* → **withdrawn**.  At four
  actors the depth-1 **absolute** row is non-monotone: it rises by
  0.19–14.95% at `r = 2→3` and falls by 4.9–11.3% at `r = 3→4`.  The
  **pinned** row contracts strictly throughout.
* *"a drift that is small in magnitude and has stopped falling — and
  in the sector-norm, has turned up appreciably"* → **withdrawn**.  It
  resumes falling.
* *"Whether that is saturation or non-contraction cannot be decided at
  depth 3, and this unit does not decide it"* → it can be decided at
  depth 4, it now is, and the answer is **neither**: a single-step
  blip.
* **§10 residue 1 is DISCHARGED**, not open.

**And the doctrine.**  The pin binds twice and in terms: *"the pinned
object is the SECTOR-NORMALIZED CONDITIONAL; absolute completed weights
are horizon-bound (D44f) and are context"* (§6); *"the absolute weights
are carried as context"* (§2).  The first delivery enforced that at
HZ3-b in capitals, used it at §4.3 to decline counting the root
reversal as HZ-I — and then folded exactly those absolute rows into the
boolean that fired HZ-I at HZ2-c, i.e. **selected the object per gate
in the direction that maximised the delivered negative at HZ2 and
minimised it at HZ3**.  Here the doctrine runs in **one direction**:
the pinned conditional is the headline object everywhere, including in
the outcome decision, and the absolute kernel is context everywhere.
**On the pinned object, HZ-I clause (c) does not fire at any pool, at
any cap this unit or the round computed.**

---

## 5. HZ3 — the horn gate

### 5.1 The conventions

* **C1** (committed, D46b): `G(h, 0) = 1`.  Maximally coarse — so the
  pin's suggested S4 pattern inverts and a second convention must
  **refine**.
* **C2** (branch-count): `G(h, 0) = |menu(h)|`.
* **C3** (menu-mass): `G(h, 0) = Σ_e q(e|h)` — gated to be exactly a
  horizon shift of C1, carried so C2's separation cannot be mistaken
  for a reparametrization *(theorem-pass)*.
* **C5** (sector-weighted, equivariant): `G(h, 0) = 1 + |{kinds in
  menu(h)}|` — **this is precisely the first delivery's own §10
  residue 4**, added by the round.
* **C4** (actor-asymmetric, **non-equivariant**): `G(h, 0) = 2` if the
  last event's actor is `A`, else `1` — added by the round as the
  falsification test the pin's HZ3 asks for.

### 5.2 The root leg is a THEOREM — the round's central structural finding

`A ↔ B` is an **exact automorphism of the committed layer**: over all
**521** histories of depth ≤ 3, relabelling a history and relabelling
its menu give the same `(event, weight)` multiset — **0 violations** —
and the same holds for the `0 ↔ 1` proposal-value swap and for the two
together; the family is closed under all three; and `G(h, r) =
G(σh, r)` with **0 violations** over every (history, horizon) pair in
that range.

Under the group they generate, **each event kind in the root menu is a
single orbit**:

```
  {p(A,v0,0), p(A,v0,1), p(B,v0,0), p(B,v0,1)}   {d(A,B,v0), d(B,A,v0)}   {n(A), n(B)}
```

Therefore, for **any** terminal convention invariant under those
relabellings, the root sector-normalized conditional is the
uniform-within-kind distribution at every horizon:

```
  root conditional, EVERY r, under C1, C2, C3 and C5:
     p: 1/4, 1/4, 1/4, 1/4      d: 1/2, 1/2      n: 1/2, 1/2
```

So `HZ0-7` ("root drift exactly 0 at `r = 1..6`", D46b MB3-c) and the
root leg of `HZ3-b` ("convention separation exactly 0 at the root at
every horizon") are **the same identity**, and neither carries any
information about horizon stability or convention independence.  Both
are re-graded `[THEOREM of the construction: root-menu symmetry ×
equivariant terminal]`.

Two consequences, both gated:

* **C5 root conditional separation from C1: `0, 0, 0, 0, 0`.**  Its
  absolute separation is `1/68, 1/140, 50/14649, 2707/2122785,
  1317/34513492`.  **The first delivery's §10 residue 4 as written
  cannot sharpen the finding** — it reproduces the same tautology.  It
  is replaced (§10) by a **non-equivariant** terminal.
* **C4 root conditional separation: `1/6` at `r = 1`.**  The
  invariance breaks the moment equivariance is dropped, which is what
  makes §5.2 a statement about the **symmetry class of the terminal**
  and not about horizons.

### 5.3 The horn's genuine content — outcome `HZ3-HORN`

**(i)** Over the whole comparison (`len(h) + r ≤ 6`), the number of
histories with `k^{C1}_r = k^{C2}_r` is **zero**.  The truncation is a
CHOICE, not a formality.

**(ii) The horn lives off the root, and off the root it shrinks.**  The
pinned separation at fixed depth:

| `L` | `r=1` | `r=2` | `r=3` | `r=4` | `r=5` |
|---|---|---|---|---|---|
| 1 | 1/8 | 1/12 | 487/7790 | 40337/800358 | 109092211/2569013838 |
| 2 | 1/8 | 1/12 | 487/7790 | 7443521/132068175 | — |
| 3 | 1/8 | 1/12 | 487/7790 | — | — |

Five terms, roughly `1/(4r+4)`, over a **fixed eight-history window**
at the deepest rows — which is why the window caveat is attached in
place and why nothing is extrapolated past it.

**(iii)** On the **absolute** kernel the separation does not shrink at
2 of 6 depth rows — the root row and `L = 3`
(`7/200 → 559/16776 → 29255/822216`).

> **THE CORRECTED ONE-LINE STATEMENT.**  *The absolute kernel inherits
> the truncation convention; the pinned conditional is protected by
> symmetry at the root and measured to shrink off it.*  The first
> delivery's **"SO THE HORN IS OBJECT-DEPENDENT"** is **demoted**: half
> of that finding — the root half — was forced by construction and
> could not have come out otherwise, so the only measured content is
> the off-root table, and it is much weaker than the headline it
> carried.  Horn (I) does **not** fire as a non-shrinking separation in
> the pinned object over the computed horizons, and HZ-II is not the
> outcome.

---

## 6. HZ4 — the lemma slot: no bound, one route closed, **one route open**

### 6.1 The two ports

* **R-SIG** — every actor's **non-superseded** holdings is the same
  singleton `{v}`; no live proposals, components or merge pairs.  *The
  literal D62 row-R4 port — and what D69's route R5 actually names.*
* **R-MENU** — R-SIG **and** `holdings(a) = {v}` exactly for every `a`.
  *The menu-exact port.*

| | count | menu = root's under renaming | **re-entered (a prefix outside the class)** |
|---|---|---|---|
| **R-SIG** | **5,161** | 1,365 yes / **3,796 no** | **3,796** |
| **R-MENU** | **1,365** = `Σ_{n≤5} 4^n` | 1,365 / 0 | **0** |

The R-SIG points that are not menu-exact are **exactly** the re-entered
ones, and the receipt gates that identity.

### 6.2 The structural fact — and its exact scope

> **HZ4-ii(a): holdings are monotone along every transition** —
> exhaustively verified, **0 shrinking transitions**.

This is a **one-line theorem** of the layer (`View.holdings` is a union
over the view's arbs, deliveries and merges) and is carried as a
theorem-pass.  **Its scope is narrower than the first delivery
claimed**: the layer *does* have a shrinking set — `superseded` grows,
so **non-superseded** holdings shrink — which is exactly why the
absorbing-complement argument covers **R-MENU and nothing else**.

### 6.3–6.4 The departure census and the ladder

Unchanged and reproduced: the renewal count is exactly `4^n`, the raw-q
renewal mass exactly `(3/2)^n`, the horizon-completed mass **not** at a
renewal rises to **`1671053/2168717 ≈ 0.7705`** at depth 5 (closed
form: renewal mass at depth 5 `= (3/2)^5 · G_2 / G_7 = 497664/2168717`).
The single B1 ladder cylinder's weight vanishes at `1/32` per rung, so
falsifier 5 read narrowly does **not** fire; it does not help, because
the never-regenerating set is everything past the first creation event.

### 6.5 The minorization attempt, run on **both** ports

The first delivery ran this on `R-MENU` only, at `N = 1..3`, and
concluded *"D69 route R5 is CLOSED"*.  Both ports are now run to
`N = 5`, under two horizon normalizations (the receipt's own horizon-7
chain and the horizon-6 chain matched to this gate's depth-5 window);
the zero-sets are horizon-independent, since every kernel entry is
strictly positive and a zero hitting probability is a purely
combinatorial statement.

| `N` | tested | **R-MENU** zeros | **R-SIG** zeros | **R-SIG infimum**, `H6` window-matched | **R-SIG infimum**, `H7` this receipt's chain |
|---|---|---|---|---|---|
| 1 | 3,969 | 3,628 | **2,520** | 0 | 0 |
| 2 | 521 | 436 | **84** | 0 | 0 |
| 3 | 69 | 48 | **4** | 0 | 0 |
| 4 | **9** | 4 | **0** | **118/1455 ≈ 0.0811** | **134489/1661019 ≈ 0.0810** |
| 5 | **1** | 0 | **0** | **308834/403761 ≈ 0.7649** | **4979621/6506151 ≈ 0.7654** |

The zero-*sets* are horizon-independent — every kernel entry is
strictly positive (HZ1-b), so a hitting probability is zero iff **no**
`N`-step path reaches the class, a purely combinatorial fact — which is
why the two normalizations give the same counts and only the infima
differ.

**The R-SIG zero-set collapses with `N` and vanishes at `N = 4`, which
is the shape of a Doeblin condition, not of a closed route.**  And the
class is not obviously useless: over the whole window R-SIG carries
only **three** distinct menu shapes, with **3,788** of the 3,796
non-root-menu points sharing one (`4×d@1/8, 2×n@1/2, 4×p@1/8`).

**Stated against this unit:** the `N = 4` and `N = 5` windows are 9 and
1 histories.  **This is not a bound and none is claimed.**  It is a
demonstration that the route was never tested.

> **THE CLAIM, NARROWED.**  "D69 route R5 is CLOSED" → **"the
> MENU-EXACT ATOM route is closed"**: the class of histories whose menu
> is the root's under renaming is absorbing-complement and unreachable
> once left.  **The σ-level class (R-SIG, the literal D62-R4 port) is
> re-entered, was not tested for minorization, and its N-step hitting
> probability is not zero at `N = 4` on the window where it is
> computable.  THE SIGMA-LEVEL RENEWAL ROUTE IS OPEN.**  Separately:
> classical Doeblin minorization needs **no atom at all**
> (`P^N(x, ·) ≥ δ ν(·)`); the first delivery stated the *atom* version
> as if it were the general one.  **No operator-level minorization —
> Birkhoff / Hilbert-metric contraction of the positive backward
> recursion `G`, the natural instrument for this kernel — has been
> attempted anywhere.  That is now the campaign's named successor
> proof engine, and the only surviving candidate.**

Per the pin: **nothing in HZ4 upgrades any HZ2 label**, and outcome
HZ-III's bound clause is not satisfied.

---

## 7. HZ5 — the aggregation arm: **one** test, **one** inherited closure

D57's coarsest-lumpable fixpoint at TYPE-ONLY (5 sectors) and
BUDGET-ONLY (4 sectors).  **Not dropped.**

| depth | type-only cap3/4/5/6 | budget-only cap3/4/5/6 |
|---|---|---|
| 0–2 | 1,1,1,1 / 2,2,2,2 / 6,6,6,6 | identical |
| 3 | 5, 10, 10, **11** | 5, 10, 10, **11** |
| 4 | —, 6, 14, **16** | —, 6, 14, **16** |
| 5 | —, —, 7, **19** | —, —, 7, **19** |
| 6 | —, —, —, **10** | —, —, —, **9** |

`(depth, stable-at-last-two-caps) = [(0,T), (1,T), (2,T), (3,F), (4,F)]`
at **both** maps; the S4 trivial-boundary control transfers at
`C = 3, 4, 5` for both, so the counts are **LOWER BOUNDS** and the
blow-up reading is the conservative one; and at both maps a depth-3
class of **84** histories splits **64 / 20** between cap 5 and cap 6.

> **BUT THAT IS ONE CLOSED ROUTE, NOT TWO.**  `sec_budget` differs from
> `sec_type` only by merging `r` and `m`.  The census: menu entries of
> kind `m` occur **only at parent depth 5 and 6** (`{5: 72, 6: 2672}`),
> and only **72** of the 243,769 histories contain an `m` event at all.
> **No `m` event appears in any menu at parent depth ≤ 4.**  D57's
> decider — *"for every depth carrying at least three cap values, do
> the last two cap values agree?"* — uses **depths 0–4 only**.  At
> those depths `sec_budget` is a **bijective relabelling** of
> `sec_type`, so the two maps induce the identical initial signature
> and the identical fixpoint partition, and the receipt gates the
> partitions to be **identical as partitions** at every cap on the
> decider's whole input.  The one number that differs (depth 6: 10 vs
> 9) lies in the **cap layer** and never enters the decider.

**Answer to the two aggregation residues:** `[MEASURED, caps 3–6, two
actors, counts are LOWER BOUNDS]` — **route R1 closes on measurement;
route R2 is not a distinct test at these caps and its closure is
INHERITED, not measured.**  Outcome **HZ5-b**.  Extending R2 to a cap
where merges have support is the honest residue.

---

## 8. Controls, and the wall diagnostic

**HZ6-i — the instrument can fail.**

| control | kind | family-uniform `L∞` drift, `r = 1→2 … 5→6` | contracts? |
|---|---|---|---|
| **true law** | — | 3/110, 3/253, 373/69230, 2333/1838829, 4629/187210517 | yes |
| **NC1** propose ×3/×⅓ by parity | **weight law** | 4.31e-2, 2.32e-2, 7.85e-3, 7.09e-3, 1.86e-3 | **yes** (survives) |
| **NC2** no deliveries at even depth, no proposals at odd | **GRAMMAR** | 5.30e-2, 2.25e-2, 1.44e-2, 5.08e-3, **7.16e-3** | **no** |
| **NC3** idle ×100 at even depth only | **weight law** | 2.38e-2, **2.69e-2**, 4.94e-3, **6.24e-3**, 2.77e-5 | **no** |

The first delivery reported "2 of 3 break contraction".  **NC2 is a
perturbed GRAMMAR, not a perturbed weight law**: setting a weight to 0
removes the menu entry, so NC2's drift is taken over a *different
family*, and the pin asks for "a deliberately perturbed **weight
law**".  Restated: **1 of the 2 declared weight-law perturbations
(NC3) breaks contraction, and 1 of the 1 declared grammar perturbation
(NC2) breaks it.**  The pin's falsifier 1 is satisfied by the
weight-law breaker alone, and does not fire.

**HZ6-ii — the positive control.**  `ker(T − 2I)` one-dimensional with
a strictly positive generator taking `{1, 4/3, 7/3}` at `{29, 5, 2}` =
D49's `f`; `ker(T − I)` one-dimensional and mixed-sign; `ker(T − 5/2 I)`
and `ker(T − 9/4 I)` empty; `T f = 2 f` exactly; `G` a function of
`sigma` alone with 0 violations; `λ = 2` used only as an asymptotic
eigenvalue.

**HZ6-iii — anti-vacuity.**  All **51** `check()` predicates parsed, 0
flagged, with the hoisting defect named in the label — and, as the
round observed, "0 flagged" is compatible with a fraction of the passes
being unfailable, which is why the receipt now prints a **theorem-pass
count** beside the PASS count and names each one.

**HZ6-iv — determinism.**  Byte-identical digest under
`PYTHONHASHSEED` 0 / 7 / 999.  **The digest is widened past cap 3**:
it now carries the two-actor family at cap 4 under both terminal
conventions, the **three- and four-actor pools**, the **HZ4 renewal
predicate** through the layer's own `View`, and **both** HZ5 sector
maps' refinements — i.e. every arm the round found a defect in.  (A
by-product worth recording: probing this receipt showed that **two
equal `frozenset`s can carry different `repr`s in one process**, so
every menu comparison in HZ3-s1 and HZ4 was converted from a repr sort
to a set comparison.)

**HZ7 — the wall control, diagnostic only.**  With
`deliver_options_in_view` restricted to non-superseded holdings in a
separate namespace, the B1 ladder is still admissible at every rung and
`|holdings(A)|` still grows without bound, but the delivery menu
collapses to 1 option at `1/4` and the whole menu is constant at 8
options from rung 2 on.

> **MANDATED LABEL, printed by the gate itself: the committed layer is
> unchanged; no other number in this receipt uses the restricted
> enumerator; adopting it would be a different theory and is not
> proposed.**

---

## 9. What is licensed, horizon-scoped and pool-scoped (HZ8)

Every claim is scoped to **transport scope, the declared families and
caps only**, in exact `Fraction`s.  **No infinite-volume claim under
any outcome** (D46b MB6, binding, and scanned rather than asserted).

1. `[MEASURED, ARM-1T, depth ≤ 6, r ≤ 7]` The relative-horizon kernels
   are **proper at every computable `(h, r)`**.  D46b's `r = 1, 2`
   becomes `r = 1..7`.  `[THEOREM]` The chained measure is cut-additive
   at every cut — by induction from properness, not by measurement.
2. `[MEASURED, two-actor depth ≤ 6; three-actor depth ≤ 4; four-actor
   depth ≤ 4]` **On the pinned sector-normalized conditional the drift
   contracts over the computed horizons at every off-root depth, in
   both conditional norms, at all three actor pools.**  *This is a
   table.*  Not a rate, not a limit; no fit, extrapolation or threshold
   anywhere.
3. `[MEASURED, four-actor depth ≤ 4]` The four-actor `L = 1`
   **absolute** row is **non-monotone and not growing**: `L∞`
   `29/8288 → 23117/6594399 → 33367649/10728807102` (+0.19%, then
   **−11.28%**, ending **11.12% below its first term**); `L1`
   `87/4144 → 1160/53613 → 897664/43613037` (+3.06%, **−4.87%**,
   1.96% below the first); sector-`L∞` `39/4144 → 580/53613 →
   448832/43613037` (+14.95%, **−4.87%**).  **The pin's HZ-I clause
   (c) fires on neither the pinned object nor, in substance, on the
   absolute one: it fired in the first delivery on a two-term window.**
4. `[MEASURED]` The family-uniform sup is **window-dependent**; D46b's
   committed four-term sequence is a **lower bound** and its fourth
   term understates it by ×1.92.  The terminal `0`s of the conditional
   family-uniform rows are **root-only window artifacts** and are not
   contraction.
5. `[MEASURED, r ≤ 7]` **D46b MB3-e's root monotonicity is FALSE at the
   next horizon** — the root absolute drift rises by ×7.509 at
   `r = 6 → 7`, identically in all three norms.  `[THEOREM]` The pinned
   object at the root is 0 at every horizon **by symmetry**, which is
   not evidence of stability.
6. `[THEOREM, from the committed layer]` `A ↔ B` and `0 ↔ 1` are exact
   automorphisms (0 violations over 521 histories; `G` invariant); the
   root menu is one orbit per event kind; **every equivariant terminal
   is forced to the same root conditional**.  A non-equivariant
   terminal separates by `1/6` at `r = 1`.
7. `[MEASURED, len(h)+r ≤ 6]` The two declared terminal conventions are
   **different completions at every single history**.  **Off the root**
   the pinned separation shrinks at every fixed depth over five terms;
   the horizon-bound absolute kernel does not shrink at 2 of 6 depth
   rows.  **The absolute kernel inherits the convention; the pinned
   conditional is protected by symmetry at the root and measured off
   it.**
8. `[EXACT, from the committed layer]` **Holdings never shrink**, so
   the **menu-exact** transport renewal class is absorbing-complement
   (0 re-entries) and **that route is closed**.  `R-SIG` (the literal
   D62 R4 port, and what D69's R5 names) is **re-entered 3,796 times**,
   its zero-set collapses `2520 → 84 → 4 → 0`, and its 4-step hitting
   infimum over the computable window is `118/1455 > 0`.  **THE
   SIGMA-LEVEL RENEWAL ROUTE IS OPEN.**  No operator-level
   minorization has been attempted; it is the named successor.
9. `[MEASURED, caps 3–6, two actors, LOWER BOUNDS]` Type-only and
   budget-only aggregation both blow up — but **as one test**: they
   induce the identical partition on the decider's whole input.  D69
   route **R1 closes on measurement**; **R2's closure is inherited**.
   The finite-alphabet prerequisite stays `[OPEN]`.
10. `[DIAGNOSTIC ONLY]` The B1 ladder does not survive a restricted
    delivery enumerator.  Nothing in the corpus changes.

**What is NOT licensed.**  No claim that any of this is *the* click
law's measure — the identified law is reached only through the missing
map (D59/D65), untouched here.  No dimension claim, no typicality
claim, no quantum-layer claim.  (H1)/(H2) and the 36-state closure
remain **two-actor delivery-free d42a**.  Scale doctrine (LOG #440):
this unit certifies a **mechanism**, never an object.  **And no limit
is claimed**: HZ4 exhibited no bound, so what is delivered is HZ-III's
shape without HZ-III's licence — the standing label is *"root-free over
the computed horizons"*, explicitly not a boundary theorem.

---

## 10. Residues, named

1. ~~**The four-actor pool at depth 4.**~~ **DISCHARGED** (§4.5).  The
   deciding computation was run: 318,704 histories, serial, and the
   two-term rise reverses.
2. **The three-actor pool at depth 5** — 713,967 more histories; not
   affordable in this unit's budget and declared so rather than cut
   silently.  *(This is now the only pool residue, and it is the one to
   spend next if the pool question is reopened.)*
3. **The root reversal's mechanism.**  `r = 6 → 7` is the deepest
   horizon this cap can reach, so the reversal is a single step.
   Whether it is the onset of oscillation or one turn needs `G_8`, i.e.
   a depth-7 family (~1.9M histories).
4. **A NON-EQUIVARIANT terminal convention.**  *(Replaces the first
   delivery's residue 4, which proposed a sector-weighted terminal:
   §5.2 shows that is another equivariant terminal and returns exactly
   0 at the root, i.e. it reproduces the tautology rather than probing
   it.)*  What actually probes the root invariance is a terminal that
   is not invariant under the layer's relabellings — C4 is one, and a
   systematic family of them is the residue.
5. **The renewal window.**  R-SIG / R-MENU were evaluated at depth ≤ 5
   because the predicate needs full views.  The monotonicity argument
   is depth-free from the layer's own `holdings` clause, but the census
   — and, importantly, the R-SIG hitting probabilities at `N = 4, 5` —
   are window-bound, on 9 and 1 histories.
6. **THE SUCCESSOR PROOF ENGINE: operator-level minorization.**
   Birkhoff / Hilbert-metric contraction of the positive backward
   recursion `G`, and/or a genuine Doeblin minorization
   `P^N(x, ·) ≥ δ ν(·)` on the σ-level class, neither of which needs an
   atom.  **This is the campaign's surviving candidate and the first
   thing the successor unit should build.**
7. **Extending HZ5's route R2** to a cap where merge events have
   support, so that it is a distinct test rather than an inherited
   closure (§7).
8. **D57's own residues remain**: depth 7, and the actor-swap quotient.
   This unit did not touch them.
9. **The join-view lattice completeness** (D56 caveat C3) is not
   re-verified here and this unit does not rely on it — but any
   successor that builds a `sigma` on top of these kernels must.
10. **The pin's own gap, recorded.**  The pin set **no
    minimum-evidence bar on HZ-I clause (c)**.  A binary clause that
    can fire on a two-term row at the shallowest declared cap is a
    clause a unit can satisfy without learning anything, and the
    successor pin should price the evidence as well as the direction.

---

## 11. What this note does not do

* It does **not** claim the horizon-limit route is alive.  Every
  positive reading in §4 is a table over computed horizons; HZ4
  exhibited no bound; and HZ-III's licence is unavailable under the
  pin's own terms.  **"Contracts over the computed horizons" is the
  only wording used, and the pin's lexical prohibition is enforced on
  every gate label by a scan that is now total.**
* It does **not** claim the route is dead either.  The first delivery's
  HZ-I is withdrawn as a reading; what replaces it is a measurement
  that contracts, not a proof that it converges to anything.
* It does **not** claim a minorization bound from the R-SIG rows.
  Nine histories and one history are not a bound, and the note says so
  where it says the number.
* It does **not** propose adopting the restricted delivery enumerator,
  and it does not re-grade W-A.
* It takes **no** position on the missing map, on the TRIPLE-GRID
  construction, or on any of the four frontiers.

---

## 12. Round-1 disposition — every finding, and what changed

| finding | disposition |
|---|---|
| **MAJOR 1** four-actor depth-4 arm never run | **UPHELD.** Arm run (§4.5); rise reverses; residue 1 discharged; headline re-graded to contraction at every pool and depth measured. |
| **MAJOR 2** pinned-object doctrine applied in two directions | **UPHELD.** Doctrine now runs in one direction (§4.4); the outcome decision moves to the pinned object; the pinned object is tabulated across actor pools for the first time. |
| **MAJOR 3** the horn's root leg is a symmetry identity | **UPHELD.** Gated as a theorem (§5.2); `HZ0-7` and `HZ3-b0` re-graded; "object-dependent" demoted; residue 4 replaced. |
| **MAJOR 4** "R5 closed" over-claims a route family | **UPHELD.** Narrowed to the menu-exact atom (§6.5); R-SIG re-entry and hitting probabilities gated; **σ-level route declared OPEN** and operator-level minorization named as successor. |
| **MAJOR 5** HZ5's two routes are one computation | **UPHELD.** Partition identity gated (§7); R2's closure relabelled inherited. |
| **MODERATE 6** HZ1-c is an identity | **UPHELD.** Re-graded; HZ1-b named the substantive gate (§3). |
| **MODERATE 7** theorem-passes undisclosed | **UPHELD.** `check()` carries a theorem flag; the receipt prints a theorem-pass count and names each one. |
| **MODERATE 8** HZ5-1 cannot fail | **UPHELD.** Predicate strengthened to set-inclusion nesting (a real tripwire) and the unfailable half disclosed. |
| **MODERATE 9** window-artifact zeros | **UPHELD.** Gated at `HZ2-e`, marked `[root only]`, struck from every contraction claim (§4.2). |
| **MINOR 1** anchor count | Reconciled: 4 provenance + 11 numeric = 15 anchor gates, receipt is the source. |
| **MINOR 2** HZ8-b scanned 40 of 42 | Fixed: HZ8-b runs last and folds in its own label — coverage is total. |
| **MINOR 3** NC2 is a grammar perturbation | Relabelled; the count restated by kind (§8). |
| **MINOR 4** determinism digest covers cap 3 only | Widened to cap 4, both wider pools, the renewal predicate and both HZ5 maps. |
| **MINOR 5** residue 4 is not a route | Replaced by a non-equivariant terminal (§10.4). |

**Nothing in the review's arithmetic needed correcting, and nothing in
this repair contradicts a number the review published.**  Every figure
the round supplied was rebuilt here by this receipt's own code and
reproduced exactly.
