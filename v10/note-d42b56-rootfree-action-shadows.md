# D42b5/6 — root-free completion, the action-level ladder, and the
# continuum shadows (fronts 7-9, one unit on the decided TS structure)

**Status:** CAMPAIGN PIN (strict), 2026-07-18. Bases: d42b3 TERMINAL
#307 (the decided trilemma; the gradient completion; the 21/114
deformation; the residues), d42b4 TERMINAL #312 (the 1/k-boundary
identification; the arb-layer open problem), d42b7 TERMINAL #316.
Receipt: `v10/code/d42b56_rootfree_action_exact.py`.

## 1. Pinned claims

- **A1 (front 8 — the three-level flatness ladder, THEOREM-class).**
  On the 202 canonical diamonds: (i) the WEIGHT level is flat (mu
  factor-products diamond-equal — inherited gauge invariance, 0
  violations); (ii) the NAIVE CUT-NORMALIZED level is NOT flat
  (exactly the 36 N-mismatch diamonds violate — d42b3's census); (iii)
  the GRADIENT-COMPLETED level is flat again (0 violations, any
  boundary — the completion RESTORES flatness). Echoes paper 29's
  lesson on OUR object: flatness is LEVEL-RELATIVE; the completed
  measure passes the action-level check the naive normalization fails.
- **A2 (front 7 — boundary non-stationarity, exhibit).** The root and
  the post-arb fresh-base record point have ISOMORPHIC menus (same
  event shapes and weights — the renewal structure of the grammar);
  under BOTH canonical boundaries (unit and 1/k) the completed
  transfer q' DIFFERS at this isomorphic pair (exact witness) — the
  truncated completions are depth-non-stationary: they are ROOTED, as
  paper 28's uniform-rooting theorem anticipated.
- **A3 (front 7 — the reduction, PINNED).** Root-free/stationary
  completion = a positive eigenvector problem for the local transfer
  on menu-isomorphism state classes (Z(h) = f(state)·λ^{-depth});
  at finite depth the constraint system with free boundary is
  underdetermined, and the existence question IS the infinite-volume
  positive-harmonic residue that d42b3 already named. IDENTIFICATION:
  front 7's root-free question ≡ d42b3's infinite-volume residue —
  one open core, not two. No existence claim either way at this
  scope; the reduction and the finite-depth exhibits are the result.
- **A4 (front 9 — the continuum shadows, DECLARED + gated discretely).**
  v6 paper 1's gravity-sector residue (TS-integrability of the
  interacting reconstruction) has as its discrete form EXACTLY the
  decided dichotomy: ratio-preserving TS fails / gradient TS holds at
  finite volume at the price of within-cut ratio deformation, with
  the infinite-volume existence open (= A3's core). The second v6
  residue (invariant sprinkling of division events) has as its
  discrete precursor the SYMMETRY COVARIANCE of the generated
  opportunity complex: gates — (i) actor-exchange covariance of mu
  family-wide (A<->B relabeling is a measure isomorphism, exact);
  (ii) payload-relabeling covariance (0<->1); (iii) Z-class-constancy
  (gauge invariance of the completion — re-cited from d42b3). The
  continuum limits themselves are NOT claimed (Hegerfeldt
  pre-registered; the lift's arb-layer problem gates any operator
  continuum route — d42b4 E1).

## 2. Gates

S1 the three-level ladder on all 202 diamonds (0 / exactly-36 / 0).
S2 the isomorphic-menu exhibit (root vs post-arb: shapes and weights
equal as multisets) + the non-stationarity witness under both
boundaries (exact inequality of q' at the pair). S3 the reduction
printed with the eigen-form stated; no existence gate (declared
OPEN = the d42b3 residue). S4 actor-exchange and payload-relabeling
covariance of mu, family-wide exact; Z class-constancy cited.

## 3. Round-1 amendments (2026-07-18; round frozen at #319)

**B1 (F1).** The receipt's second boundary was 1/MENU-SIZE — an
unpinned probe mislabeled "canonical". The canonical 1/k (d42b4
D-M1) is 1/#linearizations-of-the-class: Z([]) = 325/64; the S2
witnesses under it are 21/325 vs 1/16. The repaired receipt runs the
CANONICAL class-1/k (linear-extension counts, gauge-invariant) and
retains the menu-reciprocal as a labeled extra probe.

**B2 (F2 — S1 re-stated as theorem + separating gate).** The
gradient leg's flatness is a TELESCOPING THEOREM: ANY cut-attached
(class-constant) Z gives flat diamond products identically — gated
by an arbitrary non-harmonic class-constant probe passing. The
SEPARATING content is d42b3-D2's lineage: class-constancy. Negative
control gated: a sequence-attached (non-class-constant) Z FAILS the
diamond check (referee: 198/202; the receipt gates > 0 with its own
deterministic sequence-Z). §1-A1's "any boundary" is qualified to
cut-attached class-constant Z; "completion RESTORES the check"
becomes "the gradient FORM restores it; the naive N fails because N
is not a gradient (D2)".

**B3 (F3 — the honest state space + one-way reduction).** The
transfer is NOT well-defined on menu shapes (4 shapes; one splits:
[pA0] vs [pA0, selfA, p-on-v1] have equal menus, different
successor profiles). The honest object is the BISIMULATION QUOTIENT:
17 states at depth <= 4 (refinement trajectory 4 -> 9 -> 14 -> 16 ->
17), on which the transfer IS well-defined (0/114 interior classes);
the finite system's rank 114/427 leaves 313 boundary freedoms
(referee anchors). A3's reduction is ONE-WAY: stationary completions
are a subclass of positive-harmonic solutions; the residue contains
front 7, not identically equals it.

**B4 (F4/F6).** The isomorphism gate is the STRUCTURAL EVENT-LEVEL
BIJECTION (type/payload-matched, v0<->v1 translated), not the
multiset proxy (331 histories share the root's shape — near-
information-free; 175 structural, 31 exact, censused). Citations:
paper 28 §5.3 + the D42 mandate carry the rootedness point (Thm 4
was atmospheric). Sprinkling's precursor scope NAMED: the internal
Z2 x Z2 (actor-exchange x payload-flip) plus foliation gauge — not
spacetime symmetry.
