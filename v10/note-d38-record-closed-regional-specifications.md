# D38 — record-closed regional specifications and predictive width

**Status:** D38 ROUND ONE REJECTED; D38b REPAIR `PASS 9/9`; focused hostile delta open.
**Date:** 2026-07-14.
**Parents:** terminal Papers 21--24, coordinator-lineage reviewed Paper 25,
independently reviewed Paper 26,
terminal D34b/D34e/D34f, terminal D35 and terminal D36.

## 1. Question

Paper 26 closes the supplied-carrier problem.  Given typed opportunity roles,
conflicts and boundary data, it constructs normalized regional conditionals,
records arbitration clicks and admits compatible global classical measures.
It leaves two statements deliberately open:

```text
the chosen D34b law is not proved to have the regional presentation;
the regional law does not generate the opportunity roles or their incidence.
```

D38 asks whether the generator and boundary language can be one object:

> Can a regional specification generate and authenticate the records that its
> own later conditionals require as boundary data?

The target name is **record-closed regional specification**.  “Self-
generating” will mean closure after a declared finite seed, not creation from
nothing and not unique law selection.

## 2. The correction that governs the campaign

The following proposed theorem is false and is excluded before receipt work:

> real birth or component growth obstructs every uniformly finite-range
> regional specification.

Papers 21--23 prove the opposite separation.  D34b has actor-star-local
generator rows and bounded touched/write arity.  Paper 22 gives a radius-one
distributed record boundary for the coarse and role-labeled A-wire futures.
What grows without a uniform bound is its width.  Paper 23 proves a stronger
obstruction only for the projective family of exact, unlimited-horizon,
full-ancestry futures: the minimal predictive quotient is the rooted marked
component class and has arbitrarily large information width.

D38 will keep three objects separate:

```text
local update collar       records read or written by one law event;
sufficient boundary       record carrier screening one declared future query;
predictive quotient       minimal equivalence class for that law and query.
```

The intended separation theorem is:

```text
bounded local update arity  DOES NOT IMPLY  bounded predictive width.
```

It is not a theorem that the local kernel reads the whole component before
each click, and it is not a no-go for finite collars.

## 3. Record-closed specification

Let `H` be a typed append-only record history, `D` a finite causal region and
`b_D(H)` its incoming/lateral boundary.  A record-closed specification consists
of regional kernels `gamma_D`, causal realizations `F_D` and boundary updaters
`U_D` satisfying all of the following.

### C1 — physical boundary realization

Every field read by `gamma_D(.|b_D)` is carried by a typed record or typed
incidence in the admitted record DAG.  A behavioral histogram or demand bit
may be used only when a distributed record realization and ownership map are
given.  No analyst-only oracle field receives physical credit.

### C2 — screening

For the declared query and stopping scope, conditional future laws factor
through `b_D(H)`.  Sufficiency is law-, query- and stopping-relative.  It is
not inferred from graph radius alone.

### C3 — recursive generation

Starting from the declared seed boundary, every later boundary record required
by the kernel is emitted by an admitted earlier law event.  The updater

```text
U_D : (b_D, local typed event) -> b'_D
```

uses only owned or authenticated incident records and emits a finite typed
delta.  Exogenous couplings and random marks may be part of the law; an
exogenous opportunity carrier after the seed may not be silently supplied.

### C4 — authentication and ancestry integrity

Boundary records bind type, owner, wire, causal parents, structural role and
payload under the D36 ideal-authentication discipline.  Missing-parent,
foreign-root, retargeting, omitted-field, forged-signature, replay and
disconnected-lookalike mutations reject before durable mutation.  D38 does
not claim Byzantine or cryptographic security.

### C5 — restriction and update cocycle

For `D contained in E contained in F`, record restriction is parent-total:
every retained parent is either retained internally or represented by a typed
`EXTERNAL(source_record_id)` boundary reference.  Boundary updates commute
with nested transport.  In insertion notation,

```text
U_b(eta_1 then eta_2) = U_(U_b(eta_1))(eta_2),
r_(F,D) = r_(E,D) o r_(F,E).
```

This is the record version of the insertion/transport cocycle.  Pairwise
agreement alone is not enough; Paper 26's triple-cover counterexample remains
a permanent gate.

### C6 — closure fixed point

The output record support of the law is an admitted input boundary support for
the next kernel.  Equivalently, after the seed the specification does not need
to import a new boundary ontology to continue.  This is the precise content of
“the boundary is made of records.”

### C7 — disclosed capacity and physical upkeep

Graph radius, actor/record/port count, parent arity, identifier cost,
continuous marks and total information width are reported separately.  A
finite record is not promoted to a uniform finite-capacity theorem.
Any boundary-maintenance record physically bridged to a D26 same-line BORN
carrier inherits D26's coherence cost; no such bridge is automatic.
Pre-existing coherence-neutral TOKEN capacity is a distinct conditional
control.

## 4. Positive target: an induced D34b presentation

D38 will use the chosen D34b law as an existence example, not as a law of
nature.  Its finite seed, Poisson construction marks and coefficients remain
supplied model data.  Birth, idle and interaction events then generate the
actor rows, edges, wire tips, event records and opportunity roles used by all
later updates.

At every positive finite cylinder, condition the chosen history measure on
the complete typed incoming record boundary.  These conditional laws are
normalized regional kernels.  Nested conditional expectation supplies the
tower law on the positive-cylinder algebra.  The local generator supplies the
recursive updater:

```text
birth        touches one parent and one new child;
idle         touches one actor wire;
interaction  touches two existing actor wires.
```

For the coarse and role-labeled A-wire queries, Paper 22's distributed typed
star is the physical boundary realization.  The root owns its row, counters
and ports; neighbors own their degree/birth data; edges carry incidence.  The
star is radius one and recursively updated, but its row/port/identifier count
is unbounded.

The theorem target is deliberately limited:

- positive finite cylinders, not an arbitrary pointwise version on null
  boundaries;
- the chosen timed D34b law, not a derivation of its rates;
- a regional presentation of that law, not identification with Paper 26's K1,
  K2, K3 or joint transaction family; and
- classical record histories only.

## 5. Predictive exhaustion target

For the full durable-ancestry Branch F query, Paper 23 supplies the analytical
endpoint at every licensed finite stop:

```text
finite-stop exact predictive quotient = current rooted marked component class.
```

D38 restates this as an unbounded-width theorem over reachable finite stops,
not as a timed or infinite predictive inverse-limit claim.  The receipt must
carry:

1. Paper 22's positive fixed-radius witnesses
   `p_r=[1/(8(r+3))]^(r+1)`;
2. Paper 23's fresh-anchor length `q=2n-1`;
3. the `2^M` positive-cylinder family and M-bit lower bound; and
4. the anchor discriminator against any proposed smaller exact quotient.

No uniform actor radius, record count, port count, identifier budget or bit
capacity screens this exact unlimited-horizon query over unbounded growth.
This does not imply one actor privately stores the component.

## 6. Criterion theorem and load-bearing assumptions

The obstruction will be stated conditionally.  A law/query pair enters the
criterion only when all of the following hold:

```text
P  typed old records persist immutably in wire ancestry;
G  reachable connected histories have unbounded distinguishable growth;
R  a finite positive-probability local transport path can return every old
   attachment to the distinguished future boundary;
A  for every finite legal K and nonisomorphic K', a Branch-F-measurable fresh
   anchored prefix U_K(Delta) has positive leading order q(K) under K and is
   zero or order at least q(K)+1 under K'; this includes first-unmatched,
   altered-record and missing-record catch-up cases;
Q  the query asks exact typed ancestry and elapsed future time at unlimited
   horizon, and the marked DAG reconstructs K;
S  no irreversible seal, exact horizon or zero-influence cut blocks return.
```

The criterion also requires component autonomy: the current component class
screens the declared future law.  Under these strengthened premises, anchored
prefix separation makes distinct component classes predictively distinct,
while autonomy makes the class sufficient.  The `2^M` family then gives
unbounded worst-case information width.  D38b verifies the chosen-D34b
instance only by hash-locked inheritance from D34f; the general criterion is
an analytical implication, not a finite receipt theorem.

The receipt must show that the assumptions are load-bearing, not decorative.

### Escape E1 — finite horizon

A horizon-capped query can ignore a witness whose first possible return lies
beyond the cap.  This can bound the exact operational boundary at fixed
horizon without deleting the old record.

### Escape E2 — irreversible operational seal

A hypothetical exact-zero operational cut may preserve the record
ontologically while forbidding every later read/transport at the declared
alphabet.  This breaks `R` or `S`.  D38b uses this only as an abstract query
variant.  It does not identify V6 dispersal or any existing D34b mechanism
with such a physical seal; Paper 23 leaves that construction open.

### Escape E3 — attenuation

Nonzero attenuation alone does **not** bound an exact infinite-precision
predictive quotient: arbitrarily small differences remain different.  It can
bound an epsilon-operational boundary after a declared alphabet/resolution
threshold.  The receipt must include both controls.

### Escape E4 — bounded growth

A cap on the complete reachable marked-history state space, total record count
or query horizon bounds capacity for that capped model.  A fixed actor
population alone does not: two actors can accumulate unbounded append-only
history.

## 7. Ontological and operational width

D38 will report two widths.

```text
W_ont   information required to identify the persistent rooted marked
        component history up to gauge;

W_op(A) information required to predict the future at a declared operational
        alphabet A, stopping rule and error tolerance.
```

Under exact unsealed Branch F, `W_ont=W_op` at the component quotient.  A
genuine seal or an epsilon attenuation threshold can make `W_op` smaller while
`W_ont` remains NSE-protected.  This mirrors D31's structural order versus
alphabet-relative operational influence.  Operational screening is not
physical deletion.

## 8. Boundary upkeep and the D26 falsifier

If a future physical bridge identifies `N` record-boundary maintenance events
with controlled BORN carriers on one specified coherent parent line at
coupling `g`, D26 gives

```text
V_N/V_0 = (1-g)^(N/2),
B_N = -ln(V_N/V_0) = N[-(1/2)ln(1-g)].
```

At `g=9/25`, the conditional visibility factor is `(4/5)^N`.  A measured
visibility budget could then constrain the identified birth count and
coupling.  A coherence-neutral dormant TOKEN contributes factor one but
requires pre-existing capacity.  D38b supplies no record-to-system dictionary,
coherent parent line or BORN classification, so generic record maintenance is
not priced by this table.

This is not a universal rate.  A rate needs a record-to-system dictionary,
monitored duration and environmental-decoherence control.

## 9. Selection and root-free balance

Record closure constrains coupling data because the law must sustain the
boundary records used by its own later conditionals.  It does not guarantee a
unique solution.  Paper 16's action-bridge warning and Paper 26's K1/K2/K3
nonselection remain in force.

A root-free random generated network needs more than local row normalization.
The mass-transport principle/unimodularity is the relevant invariance language:
expected mass sent from a random root equals expected mass received at it for
every nonnegative covariant transport.  D38 will include finite balance
controls and cite Aldous--Lyons, but it will not claim to derive a unique
unimodular completion or select D34b's coefficients.  That is the sharpened
selection target for a successor.

## 10. Frozen exact receipt plan

The first executable will be
`code/d38_record_closed_specification_exact.py`, with committed stdout in
`data/d38_record_closed_specification_exact.out`.  It must use exact integers
and `Fraction` wherever the claim is discrete or rational.  Analytic logarithms
may be printed symbolically; no floating value may carry a theorem.

### R0 — locked antecedents and scope

Hash-lock D34b, D34e, D34f, D26 and D36b artifacts.  Print the exact inherited
claim/scope rows.  Reject any lock mismatch.

### R1 — positive finite-cylinder regional kernels

Enumerate small chosen-D34b cells.  Gate normalization, positive-cylinder
restriction and nested tower identities.  Print that zero-probability boundary
versions and Paper 26 K-family membership are not proved.

### R2 — bounded local updates, growing boundary

Gate birth/idle/interaction touched arities, actor-star locality, distributed
ownership and a disconnected-insertion control.  Exhibit radius one with
unbounded row/port/identifier width; never print “bounded memory.”

### R3 — authenticated record-closed updater

Implement typed boundary records and a recursive star updater.  Gate closure
from one seed, ownership, parent closure, restriction, the insertion cocycle
and commutation of disjoint updates.  Run a D36-style negative battery of at
least fourteen forged/replayed/retargeted boundary mutations, all rejected
before durable mutation.

### R4 — predictive exhaustion

Gate exact `p_r` witnesses on fresh radii, anchor lengths `q=2n-1`, the `2^M`
family and M-bit lower bounds.  The receipt is evidence for the analytical
theorem, not a finite-size proof of all M.

### R5 — anchor discriminator

Reproduce the bare-sweep counterexample and fresh-anchor repair from D34f,
including the lower-order/equal-order separation and an attachment witness.
Use the anchor as a hostile discriminator against any claimed exact bounded
compression.

### R6 — load-bearing escape controls

Run horizon-capped, irreversibly sealed, nonzero-exact-attenuation,
epsilon-attenuation and bounded-growth variants.  Require the first, second,
fourth and fifth to evade the corresponding uniform obstruction at their
declared scopes; require nonzero exact attenuation **not** to evade it.

### R7 — D26 upkeep accounting

Gate BORN visibility factors `(4/5)^N`, coherence-neutral TOKEN controls and
the symbolic budget equation.  Print that no retry/boundary-maintenance rate
is inferred.

### R8 — balance and ceilings

Gate finite mass-transport balance on registered rooted cells, exhibit a
locally normalized but root-biased negative control, and leave infinite
unimodular completion/selection open.  Print the quantum, sealing-rate,
spacetime and action-bridge ceilings.

## 11. First exact execution and hostile rejection

The pinned executable and committed stdout now exist:

```text
code/d38_record_closed_specification_exact.py
data/d38_record_closed_specification_exact.out
```

The first implementation reported `PASS 9/9`.  Its frozen headline controls
were:

```text
R0  seven source/data antecedent locks and the three-object claim split;
R1  4/4 normalized star kernels, 29 positive atoms, 29 tower checks;
R2  258 local action rows, maximum two touched actors, width through 12;
R3  five recursive updates, 19 records, two cocycles, 14/14 attacks rejected;
R4  p_r through r=8, 2^M through M=10, q=2n-1 through q=19;
R5  bare 1/1152 vs 1/576 and anchored 1/192 vs 1/1536 controls;
R6  horizon, operational seal, exact/epsilon attenuation and growth controls;
R7  (4/5)^N through N=8 with coherence-neutral TOKEN controls;
R8  three finite mass-transport balances and one root-biased negative.
```

Six executions under hash seeds `0`, `1`, `2`, `17`, `101` and `999` were
byte-identical.  Reproducibility did not rescue the architecture.

```text
source SHA-256:        5e42616679dcebff464e25dffa62dad4d52a6f399e7bac420ce16ea3b236604c
stdout-body SHA-256:   d8517f7024f7c024872372cf8b076a01687a836e4f59d450ddbadd8c971f6ba7
science SHA-256:       540a05596b8c9286ae32ef4de4ac36cd95992756f4ae85a3b46e036071347522
complete-output SHA-256: b0e61b087451f216db1696ece836453f5ba403667fbdb5043a67ce17e070c3f6
```

`reviews/d38-round1-record-closed-specification-hostile-review.md` returns
`2B/7M/1m/0n` and rejects promotion.  Cached state/tips admitted forks,
rollbacks and fabricated current rows; no regional restriction map existed;
neighbor birth did not generate typed child incidence; the capacity ledger
was incomplete; the criterion was underhypothesized; the inverse-limit claim
exceeded D34f; toy escape and D26 tables were overpromoted; and the anchor gate
was inherited rather than recomputed.

## 12. D38b architectural repair

D38b replaces the blocked implementation rather than editing its receipt:

```text
code/d38b_record_closed_specification_exact.py
data/d38b_record_closed_specification_exact.out
```

It reports `PASS 9/9` with the following repaired controls:

```text
oriented depth-1/2/3 positive-cylinder family     12 normalizations,
                                                  12 direct restrictions,
                                                  1,760 positive atoms;
record-derived physical history                   no cached state or tips;
typed D34b births                                 child row, edge, two ports,
                                                  clock address in the event;
durable hostile battery                           18/18 rejected with an
                                                  unchanged immutable store;
nested regional transport                         2 direct/staged squares,
                                                  1 update-naturality square,
                                                  typed external parents;
predictive theorem                                finite-stop quotient only,
                                                  D34f inherited, inverse
                                                  limit open;
escape and D26 controls                           explicitly abstract and
                                                  conditional;
capacity                                           radius/actors/records/ports/
                                                  parent arity separated.
```

Six seed runs are byte-identical.  The exact hashes are:

```text
source SHA-256:          e82198adfc22c633ca0c68197efcbec2cc0407f3abd3d81717a6d430dbe9dece
stdout-body SHA-256:     6c11b1400ff6a82f1c02c12e96a9e73c354dda4da4a0565d9d5620664fb4b152
science SHA-256:         60d0c359923cbf501b425e9f7d43ab1577279ae148b3d46a1014d0aa95140182
complete-output SHA-256: b97b47e2b5c8375883c6093e0eda9b59978192c64e56989165211e08934e7d1e
```

The earned positive noun is deliberately narrower than round one:

```text
AUTHENTICATED RECORD-CLOSED ORIENTED FINITE-CYLINDER PRESENTATION /
CHOSEN D34b / NONSELECTING.
```

Spatial DLR, null-boundary versions, Paper 26 K-family membership, a generic
physical sealing model, the D26 updater bridge, timed inverse limits,
unimodular selection and quantum joins remain open.  A focused hostile delta
must close both round-one blockers before Paper 27 is drafted.

## 13. Promotion gates

D38 may support Paper 27 only if all nine receipt gates pass and independent
review closes the following attack lanes:

1. probability/projectivity/predictive quotient;
2. causal locality/record closure/authentication; and
3. corpus/D26/NSE/sealing/quantum scope.

The paper must lead with the positive local result before the predictive-width
obstruction.  It may not claim:

- a universal component obstruction to finite-collar kernels;
- a uniform bound in record hops;
- that the Paper 23 anchor is a low-cost online updater;
- that attenuation with nonzero influence erases exact predictive identity;
- that sealing deletes ontological records;
- that record closure uniquely selects couplings;
- that D34b is nature's law; or
- any quantum regional join.

## 14. Candidate outcome rows

```text
RECORD-CLOSED ORIENTED FINITE-CYLINDER PRESENTATION / CHOSEN-LAW EXISTENCE:
  positive-cylinder projective family and recursive typed record history pass.

LOCAL-UPDATE / PREDICTIVE-WIDTH SEPARATION:
  bounded touched arity coexists with unbounded exact Branch-F width.

CHOSEN-LAW PREDICTIVE-WIDTH OBSTRUCTION:
  finite-stop component quotient and M-bit family inherited from D34f.

SUPPLIED/UNSELECTED:
  seed, D34b coefficients, action bridge and root-free completion remain data.

REFUSAL/UNDEFINED:
  quantum record-closed specification and physical sealing rate.
```

## 15. Literature anchor

The DLR/specification background remains Dobrushin (1970), Lanford--Ruelle
(1969) and Georgii (2011), as cited by Paper 26.  The root-free balance language
is David Aldous and Russell Lyons, “Processes on Unimodular Random Networks,”
*Electronic Journal of Probability* 12 (2007), 1454--1508,
doi:`10.1214/EJP.v12-463`, arXiv:`math/0603062`.  Literature alignment supplies
definitions and comparison principles; it does not supply the SHARD-specific
record-closure or predictive-width theorems.
