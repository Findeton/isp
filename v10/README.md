# Relativistic ISP v10

## Latest — D38 / Paper 27 (campaign pin; receipt not started)

D38 asks when a regional specification generates and authenticates the records
that its own later conditionals require as boundary data.  The pinned object is
a **record-closed regional specification**: physical record boundary,
query-relative screening, recursive generation after a finite seed,
authentication, restriction/update cocycles, closure of output support into
the next input boundary, and an explicit capacity/coherence ledger.

The central theorem target is a separation, not a blanket no-go.  The chosen
D34b law has bounded touched/write arity and actor-star-local update rows, while
its exact unlimited-horizon Branch-F predictive quotient is the rooted marked
component class and has unbounded information width.  Finite horizon,
irreversible operational sealing, bounded growth and epsilon-thresholded
attenuation are explicit escape controls.  Nonzero attenuation alone does not
collapse an exact infinite-precision quotient.  D26 prices BORN boundary
maintenance by `(1-g)^(N/2)`; coherence-neutral TOKEN capacity remains a
distinct control.

The first D38 receipt is pinned at nine gates: locked antecedents,
positive-cylinder D34b kernels, local-update/growing-boundary separation,
authenticated record closure, predictive exhaustion, the fresh-anchor
discriminator, escape controls, D26 upkeep and finite mass-transport balance.
No receipt has run and no Paper 27 theorem is yet claimed.

- `note-d38-record-closed-regional-specifications.md`

## Prior — D37 / Paper 26 (independently reviewed exact-receipt candidate)

D37 constructs the classical regional object queued by Papers 24 and 25.
Finite typed opportunity regions carry normalized conditional kernels, and a
deterministic realization map pushes every K3, K2 and joint-family atom to an
immutable event DAG; K1 retains complete-carrier typed atoms but no typed
proper-region transport. Nested DLR composition, three-path marginal descent,
linear-extension covariance and causal anti-dilution are exact gates. The
receipt passes `9/9` and is byte-identical under six hash seeds. Its eight-graph
registry carries 38 checked incoming/lateral/generated interfaces; S0 realizes
six fixture histories with 120 typed events and 19 participant-accepted D36b
prepares, and rejects all nine registered illegal-history mutations.

The result is a family, not a unique selector. K3 passes 508 intrinsic
conditionals, 7,098 nested towers and 138 boundary mixtures; exact positive
feasible support and fixed feasible-addition odds force its vertex-activity
form, while its one-hop Markov boundary is a separate locality property. K2
nevertheless survives as a progressing independent-plus-dominating hard
constraint once its radius-two boundary carries accepted blockers and unmet
domination demands. Marked K1 composes finitely but fails one-hop output
locality on a retained five-path witness, so no infinite K1 completion is
claimed.

One joint law carries `NO_BIRTH/TOKEN/BORN` modes and selected/rejected
arbitration clicks. `BORN_CARRIER` and activation of an explicit pre-existing
coherence-neutral `DORMANT_TOKEN` are different event types. At the symmetric
path point the Q marginals are
`34/93,25/93,34/93` and selection `6/31` from the same normalized table. D26
makes the BORN sector observable on its represented parent line: at `g=9/25`,
the joint Q visibility factor is `431/465`. A compactness theorem supplies at
least one compatible global classical measure and typed-history pushforward
for K3, K2 and the joint family on each supplied countable typed carrier with
a locally finite pairwise conflict graph at the stated finite-range scope.
The countable typed-history statement covers the injectively identified causal
core through recorded selection; the exact D36b append adapter is a separate
finite receipt interface because its locked evidence identities are SHA-based.

The candidate noun is **CLASSICAL TYPED CAUSAL REGIONAL SPECIFICATION FAMILY /
FINITE SPECIFICATION AND COUNTABLE LOCALLY FINITE PAIRWISE-CONFLICT COMPLETION /
SUPPLIED OPPORTUNITY CARRIER AND COUPLINGS / NONSELECTING**. The law still does not generate the
opportunity carrier, select activities or mode weights, close the action
bridge, prove uniqueness, or supply the quantum join.

The D37 hostile stream closes at `0B/0M/0m/0n` after requiring complete
marked-K1 product equality, making the path-five witness fail-closed, stating
K2's radius-two boundary and removing Theorem 1's redundant Markov premise.
The subsequent independent Paper 26 review reproduced the arithmetic and
Theorems 1--3 but withheld promotion at `1B/5M/6m/3n`.  Its first constructive
repair implemented a typed causal event DAG. A focused closing delta then
returned `1B/3M/4m/0n`: K1 proper restriction orphaned its priority root, the
D26 line was label-only, the D36 tuple was not an actual imported adapter, and
finite digests could not carry the countable identity theorem. The second
authorship repair narrowed K1 to complete-carrier typed atoms, imported locked
D36b records/envelopes and
requires actual participant acceptance, and uses injective tagged tuples plus
arbitrary-precision actor encodings for theorem identities. A fresh delta on
that frozen repair returned `1B/1M/0m/1n`: its D26 line was implicitly ordered
by proposal enumeration and failed proper restriction. The present repair
uses one canonical `PARENT_LINE_ROOT` per declared line instead. Every
opportunity parent descends from it, restrictions retain it, both same-line
singleton squares pass, and declaration-order reversal leaves the DAG fixed.
All 96 linear extensions have one canonical DAG digest; D34 restriction maps
one 28-event history exactly to independent 15- and 13-event component
histories; S2 asserts every distinct feasible-addition edge; and S7 gates all
27 visibility histories. The frozen common-root audit returned
`0B/1M/1m/0n`; the only findings were a stale 140-event census and deleted
chain description in the D37 note. Both were repaired at `a1ba7c9`. Two
independent string-only lanes then returned `0B/0M/0m/0n`; the independent
Paper 26 stream is closed at its stated scope.

- `note-d37-regional-history-specifications.md`
- `relativistic-isp-v10-paper26-admissible-regional-history-specifications.md`
- `code/d37_regional_history_specification_exact.py`
- `data/d37_regional_history_specification_exact.out`
- `reviews/d37-round1-regional-specification-hostile-review.md`
- `reviews/d37-round2-regional-specification-closing-delta.md`
- `reviews/paper26-round1-independent-review.md`
- `reviews/paper26-round2-typed-carrier-closing-delta.md`
- `reviews/paper26-round3-common-root-closing-delta.md`
- `reviews/paper26-round4-common-root-string-closing-delta.md`

## Prior — D36 / Paper 25 (coordinator-lineage reviewed candidate; independent review open)

D36 supplies the first authenticated actor-record realization of overlapping
finite transaction attempts in V10.  Exclusive version-bound promises,
immediate typed rejection when busy or stale, one transaction decision, local
apply/release records, acknowledgements and a final close give a clock-free
append-only protocol.  Under supplied finite attempts, ideal authentication,
honest record-generating actors, reliable messages, failure-free operation and
fair complete servicing, every attempt closes commit or abort.  The theorem is
reservation-safe and deadlock-free at that scope; it is not crash tolerant,
starvation-free or a global history law.

The exact reference, actor and replay receipts are `PASS 22/22`, `PASS 14/14`
and `PASS 8/8`.  BORN and TOKEN independently reproduce the same coordination
quotients on pair, triangle, disjoint and partial-overlap fixtures, while their
full support-record algebras remain distinct.  Sparse structural-attempt state
and routing close a gapped tx2 without tx1, complete both local prepare orders
with `8/8` typed responses and preserve all 13 exact local records under a pure
disjoint transaction insertion.

Paper 25 synthesizes the positive and negative result: record birth is a real
durable carrier, but the protocol's coordination power comes from exclusive
fail-fast reservation and causal closure, while opportunity and arbitration
remain unselected.  K1 and K2 disagree on the path, K3 remains a supplied
regional family, raw restriction needs boundary data and pairwise overlap does
not guarantee a joint cover law.  Under D26's admitted quantum carrier bridge,
each nontrivial BORN carrier also contracts the initiator's coherence by
`sqrt(1-g)`, whereas a coherence-neutral dormant TOKEN control has no such
shadow.  Birth-bearing retries are therefore physically priced, although D26
does not supply a universal retry rate.

Corpus-conforming arbitration randomness must enter as durable click outcomes,
not an external coin or service order.  Whether `q_birth` and K1/K2/K3 descend
as conditionals of one complete action/history functional remains open.  A
future quantum join must also respect D23's in-degree-`>=2` identifiability
boundary and rerun D31/D32's alphabet-specific cancellation census.  Retry
fairness, crash recovery, participant discovery, quantum joining, root-free
completion and spacetime consequences remain open.

The coordinator authorship loop closed at `0B/0M/0m/0n`.  A subsequent
separate-session coordinator-lineage audit returned `0B/1M/4m/2n`, verified
every printed number and produced only wording and scope repairs, including
the D26 physical discriminator above.  Neither lane is represented as
independent paper acceptance.  Paper 25 is therefore a
**COORDINATOR-LINEAGE REVIEWED CANDIDATE**, not terminal accepted.

- `note-d36-record-birth-causal-coordination.md`
- `relativistic-isp-v10-paper25-record-birth-carries-coordination-but-does-not-select-it.md`
- `code/d36_birth_coordination_exact.py`
- `data/d36_birth_coordination_exact.out`
- `code/d36b_actor_record_refinement_exact.py`
- `data/d36b_actor_record_refinement_exact.out`
- `code/d36_replay_integrity_exact.py`
- `data/d36_replay_integrity_exact.out`
- `reviews/d36-round6-ancestry-terminal-delta.md`
- `reviews/paper25-round1-coordinator-hostile-review.md`
- `reviews/paper25-round2-coordinator-closing-delta.md`
- `reviews/paper25-round3-separate-session-review.md`

## Latest — D35 / Paper 24 (terminal accepted at rooted scope)

D35 answers the user's causal question without introducing a global commit
clock, a per-record proper-time variable or a Poisson race.  For A's current
record A1, A2 is its first wire successor and the upper seal of a finite
operational call diamond.  A datum reaches A2 exactly when a licensed adjacent
transfer chain enters A2's newly acquired causal past.  Reach is binary in a
realized history; before completion its probability is the measure of the
completed call diamonds containing that chain.

The exact executable is a supplied A-rooted nested-call family.  Logical actors
own tips, ports, mailboxes and call-bound return slots; one disclosed shared
engine evaluates the persistent event DAG and joint entangled carrier.  FIFO,
LIFO and canonical service produce the same 16 physical histories, and the
first law is the exact marginal of 408 second-call refinements.  D-origin
evidence reaches A2 in 6/16 support histories, with exact mass `1/16` in Q1 and
`3/40` in Q2; an isomorphic disconnected source gadget has no effect.  The
root-local classical-output instrument is exact from dimension 8 to 48, with
all five self Grams equal to identity and all ten cross Grams zero.  The
terminal receipt remains `PASS 18/18`.

Birth is now cleanly split into two layers.  D24 supplies an admitted
one-parent conditional newborn-content isometry `B_g`; it does not select the
birth opportunity, `g`, root, ownership or peer/join sectors.  Q1 and Q2 pass
every gate while disagreeing on birth (`1/4` vs `1/5`), visit (`1/4` vs
`3/10`) and causal reach, so the inherited SHARD/ISP principles still do not
select nature's numerical law.

The accepted noun is **TIMELESS ROOTED NESTED-CALL FAMILY / EXECUTABLE**, not a
root-free universe law.  Three independent paper lanes close after focused
deltas.  The next foundational target is to define—not merely name—an
overlap-consistent regional history architecture: oriented causal regions,
incoming/generated/lateral interfaces, restriction transport, conditional
kernel composition, coherent positive extensions on every finite cover and a
global completion theorem.  Only after candidate root-free laws pass that gate
should their grown webs be returned to the D28–D32 cone, dimension and `S^2`
many-clocks/few-factors tests.

- `note-d35-timeless-local-next-click-law.md`
- `relativistic-isp-v10-paper24-the-next-click-is-a-causal-diamond-not-a-clock-race.md`
- `code/d35d_typed_identity_terminal_exact.py`
- `data/d35d_typed_identity_terminal_exact.out`
- `code/d35_corpus_causal_inventory.py`
- `data/d35_corpus_causal_inventory.out`
- `reviews/paper24-round1-*`
- `reviews/paper24-round2-*`

## Prior — D34f / Paper 23 (terminal accepted)

D34f closes Paper 22's adaptive full-ancestry opening for the explicitly
chosen passive D34b law. Every old event in A's connected component is
returnable by a postorder interaction sweep. A bare sweep does not identify
the past: D34f found an exact equal-order alternative-cut counterexample before
the receipt. The repaired construction creates one fresh future A anchor,
broadcasts it outward and echoes it inward. Its `q=2n-1` target events all
contain the anchor. A different past either exposes an immutable first
attachment witness or needs at least one catch-up event, separating small-time
orders `q` and `q+1`.

Therefore the complete Branch-F future law identifies the whole rooted marked
component class. That class is the minimal exact predictive quotient; every
exact sufficient carrier determines it, although a nonminimal carrier may
retain extra data. A `2^M` positive-cylinder family proves an M-bit worst-case
lower bound for every M. The exact analytical ancestry boundary is thus
component-sized and unbounded over growth—not a small collar, a private record
database or a global execution algorithm.

The standard-library receipt passes `11/11` over 2,927 states, 20,148 wire
incidences, 2,927 anchored echoes and 7,410 registered emulator attempts. It
retains the false bare-sweep witness and the repaired first-attachment witness.
Three D34f closing deltas and all three Paper 23 hostile streams are
`0B/0M/0m/0n`. The result remains chosen-law and exact-query relative. Timed
profinite/v9, quantum, spacetime, dimension, units, `G` and derivation of the
real interactive click law remain open.

- `note-d34f-component-tomography-and-necessity.md`
- `relativistic-isp-v10-paper23-the-whole-component-is-the-ancestry-boundary.md`
- `code/d34f_component_tomography_exact.py`
- `data/d34f_component_tomography_exact.out`
- `reviews/d34f-round2-*`
- `reviews/paper23-round1-*`

The direct successor is no longer another exact adaptive-collar search inside
D34b. It is either a finite-horizon/approximate ancestry frontier or a physical
principle—sealing, attenuation or causal speed—that genuinely limits what old
information can return. Any surviving law must then rerun the v9 cone and
dimension diagnostics.

## Prior — the complete-record-law campaign: D24–D27 / Paper 19 (historical snapshot)

Per the user-set goal (LOG, 2026-07-12), Paper 19 assembles the complete
interactive record law (E_t, D, B) at the declared interface, at the
strength campaign round 1 certifies (two internal streams + one external
review; LEDGER #130): D24 exhibits the first exact birth kernel
(isometric, collar-local, isometry-grade ledger, construction-order
gauge — exhaustive over all 16 labeled rooted trees on 4 registers with
the general lemma proven; family click-identifiable with click-side-only
recovery; the kernel is extra physics — a family, not unique); D25 + D27
close F1 at family grade (ensemble injectivity proved TOO WEAK — the
C_half catch; NSE = distinguishability-isometry; admitted closures = the
Busch class, thermal receivers included; exclusions of C-class, weak
collapse and classical receivers stand, measure-independently); D26
carries the typed survivor structure — GIVEN the NSE selection [POSITED],
one class under the declared epsilon-convention (repaired UV exhibit:
full visible rho_AB identical, heavy click splits; dark-interior
invisibility; the sqrt(1-g) birth-decoherence bridge, proven for mixed
parents, bounds the kernel on probed systems) — while the record alone
additionally tolerates sub-bound intrinsic collapse and MOND-class
modified gravity, each with printed splitters. 23 substantive + 3 print
gates green, exit 0, pins pre-run. DONE audit: round 1 applied; the
delta pass is the remaining reviews gate.

- `note-d24-tree-birth-kernel-identifiability.md` + `code/d24_tree_birth_kernel_exact.py`
- `note-d25-reception-theorem.md` + `code/d25_reception_theorem_exact.py`
- `note-d26-interface-equivalence-closure.md` + `code/d26_interface_equivalence_exact.py`
- `note-d27-busch-closure-of-the-reception-theorem.md` + `code/d27_busch_thermal_receiver_exact.py`
- `relativistic-isp-v10-paper19-the-complete-interactive-record-law-at-the-declared-interface.md`

## Prior — D22/D23 / Paper 18 (hostile round 1 applied)

Paper 18 posits the candidate extra principle D20's warning names — D20
paired "inventing an extra principle" with "not a fabricated proof"; the
defense is that the posit is independently motivated, parameter-free-
falsifiable, and not tuned to the data that vets it — **No Silent
Erasure** (the corpus's no-silent discipline applied to dynamics: losses
must be received by records; sealing is dispersal only), selecting the
Q-class at Paper 17's coherence fork with a ladder-null falsifier, and
assembles the identification: the currently measured clicks identify
SM + effective gravity as the leading history generator over their
tested energy domain (not the complete law at every energy), with an
exact receipt (`code/d23_click_identifiability_exact.py`) proving click
statistics identify interaction graph and coupling on an exact witness
family (post-review: plus the tree class; boundary = boundary couplings,
silent edges, in-degree >= 2). Hostile round 1 (three streams) applied
2026-07-12: priority = strengthening-by-localization (Halliwell/GMH/
Zurek/CDP named); clause (iii) demoted to corollary; falsifier carries
the attribution rider; F1 (the reception theorem) is the open
load-bearing delta. Reports: `reviews/d22-d23-paper18-round1-*`.

- `note-d22-no-silent-erasure-principle.md`
- `note-d23-click-identifiability-and-the-real-interaction-graph.md`
- `relativistic-isp-v10-paper18-no-silent-erasure-and-the-identified-click-law.md`

## Prior investigation — D21 / Paper 17

D21 constructs the first two executable complete finite instrument
rulebooks at one sealed-record interface.  Q uses an interacting coherent GHZ
source; C applies an objective hard-seal source selection before the same
three spacelike `X/Y/Z` record instruments.  The exact receipt passes `40/40`
checks.  The candidates agree on all 37 proper-support Pauli observables,
every one/two-record marginal, and the complete `ZZZ` record law, but the
preregistered complete-support discriminator gives `M=4` versus `M=0`.

Existing GHZ evidence rejects the parameter-free microscopic hard-seal
endpoint, not every scale-dependent collapse generator.  The open physical
question is an independently calibrated mass–separation–time–factor ladder of
residual complete-history coherence.  Both D21 candidates use a fixed
interacting source carrier; arbitrary record birth/joining, cosmological
completion, cone geometry, dimension and `G` remain open.

- `note-d21-two-rulebook-comparison-protocol.md`
- `note-d21-two-rulebook-investigation.md`
- `note-d21-literature-audit-two-rulebook-discriminator.md`
- `relativistic-isp-v10-paper17-two-complete-history-laws-one-record-interface.md`
- `code/d21_two_rulebook_discriminator_exact.py`
- `data/d21-final-receipt.md`
- `reviews/d21-*`

V10 reopens the fundamental dynamics at the level of interacting sealed
record diamonds. Its starting correction is that a click law conditional on a
supplied evidence channel is not yet a law for the birth, support, parentage,
or interaction of new records.

The opening paper is:

- `relativistic-isp-v10-paper1-from-sealed-diamonds-to-record-birth.md` — a
  corpus-wide review of the diamond ontology, the v6–v7 forward-law candidate,
  the v9 builder correction, the proved underdetermination boundary, and the
  resulting research program.

Investigation 1 is complete:

- `note-d1-no-silent-support-birth.md` — preregistration plus the opening
  ledger across three hostile rounds;
- `relativistic-isp-v10-paper2-no-silent-centers-do-not-select-support-birth.md`
  — exact center census, marked direct-carrier locality, support nonselection,
  and the parity/synergy projectivity refusal;
- `code/d1_no_silent_center_exact.py` — 45/45 exact/high-precision checks;
- `code/d1b_marked_support_restriction_exact.py` — 28/28 exact marked-history
  checks;
- `reviews/` — independent mathematics, reconstruction, and ontology/locality
  hostile reviews and the round-1 disposition ledger.

Both D1 executables are self-contained Python standard-library programs under
`v10/code/`. No D1 source or runtime dependency lives in `.venv`, `.env`, or an
external source directory.

Investigation 2 is complete:

- `note-d2-primitive-carrier-amalgamation.md` — preregistration and hostile
  opening ledger;
- `relativistic-isp-v10-paper3-diamond-amalgamation-is-composition-not-carrier-birth.md`
  — marked-support pushout theorem, projective pair no-bootstrap, restriction
  alternatives, and deterministic/stochastic symmetry boundary;
- `code/d2_marked_diamond_amalgamation_exact.py` — 33/33 exact checks;
- `data/d2-final-receipt.md` — frozen hashes and theorem boundary;
- `reviews/d2-round2-*` — independent mathematics, reconstruction, and
  ontology/locality final reviews.

`code/v10_self_containment_audit.py` provides an executable 4/4 receipt that
all v10 investigation source is under `v10/code/`, imports only Python's
standard library, has no duplicate source elsewhere, and leaves no cache
artifact under `v10/`.

V10 does not inherit the v9 fleet builder as fundamental dynamics. Geometry
and cone measurements remain valid measurements of their stated builder
classes, but promotion to SHARD predictions is gated on a record-intrinsic
diamond birth and interaction law.

The D1 boundary is now sharp: no-silent closure is an exact admissibility
filter after a direct candidate carrier has been supplied. It neither derives
that carrier nor gives a projective eligible-support family. The next
investigation is therefore the record/diamond operation that creates or
proposes a primitive local carrier.

D2 sharpens that further. A supplied typed interface has a canonical
construction-order-gauge amalgam at the marked-support level, but neither the
pushout nor the frozen covariance/projectivity axioms create the first
carrier. The next foundational choice is explicit: take a full variable-history
measure as primitive, carry an initial connected seed, or derive/posit a new
root/bridge-sector nucleation law.

Investigation 3 is complete at its scoped finite boundary:

- `note-d3-profinite-variable-history-extension.md` — preregistration, exact
  results, and opening ledger;
- `relativistic-isp-v10-paper4-profinite-growth-preserves-past-not-law.md` —
  immutable-past common-future theorem, kernel/measure equivalence, two
  profinite filtrations, restriction-natural failure, and nonselection
  boundary;
- `code/d3_profinite_extension_exact.py` — 27/27 exact/high-precision checks
  after hostile-round-1 repairs.
- `data/d3-final-receipt.md` — frozen hashes, byte-identical stdout digest,
  theorem boundary, and three-stream hostile closure;
- `reviews/d3-round2-*` — final mathematics, reconstruction, and
  ontology/locality PASS reviews.

D3 corrects the scope of “add nothing.” End-deletion growth preserves every
old relation while allowing a newly born event to have ancestry in multiple
old components. This is a common-future bridge shadow, not yet an interaction.
It evades D2's fixed-vertex closure no-go but does not select a local marked
law. The positive global-prefix witnesses fail ancestor-closed restriction
naturality even though their temporal cylinder measures are consistent.
Their actual finite unlabeled pushforwards remain inequivalent. The
labeled-prefix inverse limit hosts sequential growth; covtree/Stone levels
refine covariant stem observables and are not themselves seal clicks or a
unique full-history lift.

Investigation 4 is complete at its stated unmarked/fixed-token theorem and
no-go boundary:

- `note-d4-no-silent-boundary-sufficiency.md` — preregistration, the failed
  signed-linear expectation, positivity repair, exact results, and openings;
- `relativistic-isp-v10-paper5-restriction-naturality-global-shock.md` — the
  unmarked global-collapse theorem, law-relative sufficient messages,
  fixed deterministic capacity, and profinite boundary;
- `code/d4_boundary_sufficiency_exact.py` — 23/23 exact checks after hostile
  repairs;
- `data/d4-final-receipt.md` — frozen hashes, theorem boundary, and
  three-stream hostile closure.

D4 shows that exact all-subset or convex autonomy on unmarked histories leaves
only an empty/full universal-precursor family with free parameter `p`, while
stem and interval restrictions admit nontrivial controls. Exact completion
quotients diagnose a supplied law, but no carrier computes them; their
deterministic token targets require unboundedly many states on growing chains
under the fixed-token contract. No bounded record-local interacting law is
selected.

Round 2 independently passed mathematics, clean-room reconstruction, and
ontology/locality review. The final convex proof uses a cover or incomparable
boundary pair and is audited on all 1,304 proper ideals through four vertices.
Two final stdout runs are byte-identical, and the v10 self-containment audit
remains 4/4.

Investigation 5 is complete at its supplied-factor-partition theorem and
nonselection boundary:

- `note-d5-typed-collar-message-algebra.md` — preregistration, initial result,
  and complete round-1 disposition ledger;
- `relativistic-isp-v10-paper6-typed-collar-composition.md` — conditional
  factor-partition theorem, screen-relative rank/KL limits, connected-domain
  closure, and selection refusals;
- `code/d5_typed_collar_exact.py` — 34/34 exact integer/rational checks;
- `reviews/d5-hostile-round1-opening-ledger.md` — every hostile opening and
  repair before round 2;
- `data/d5-final-receipt.md` — frozen hashes, theorem boundary, and
  three-stream hostile closure.

D5 proves that a supplied complete finite factor partition admits exact
separator contraction and a positive-ray sufficient state for arbitrary
positive full-separator tests. It does not construct a sealed factor, physical
typed carrier, distributed boundary implementation, proposal/no-birth law,
evidence increment, or seal. Bounded additive KL alone does not bound rank or
arity; exact separator tables grow in width and canonical rational
description. No final interacting click law or geometry result is selected.

Round 2 independently passed mathematics, clean-room reconstruction, and
ontology/locality review. The final receipt is 34/34; repeated stdout is
byte-identical; the v10 self-containment audit remains 4/4.

Investigation 6 is complete at its conditional ordered-law reconstruction and
nonselection boundary:

- `note-d6-sealed-holonomy-factor-token.md` — preregistration, corpus gates,
  repaired terminology, and the frozen claim ceiling;
- `relativistic-isp-v10-paper7-sealed-holonomy-reconstructs-change-not-birth.md`
  — conditional RN-field theorem, complete supplied log-density
  identifiability, normalized-mode commitment, end-deletion hosting, and the
  missing extension-opportunity law;
- `code/d6_sealed_holonomy_token_exact.py` — 84/84 exact/high-precision checks
  with fixed cardinality and optimization-safe failure;
- `reviews/d6-hostile-round1-opening-ledger.md` — every mathematics,
  reconstruction, and ontology opening plus its repair or sustained boundary;
- `data/d6-final-receipt.md` — frozen hashes, stdout digest, regressions, and
  three-stream hostile closure.

D6 proves that two supplied positive ordered finite laws canonically determine
a relative RN numerical field and presentation-relative coordinate dependence.
A supplied balanced normalized oriented mode admits the v6 conditional
commitment coefficient, and actual additive evidence would give numerical
survival/division weights. The executable does not construct a typed sealed
diamond, closed holonomy measurement, physical scope, proposal opportunity,
evidence carrier, ownership selector, realized seal, accepted birth, or final
interacting click law. The complete supplied log-density ledger identifies a
law but has the same nonconstant dimension as that law; end-deletion profinite
compatibility hosts inequivalent law families and selects none.

Round 2 independently passed mathematics, clean-room reconstruction, and
ontology/locality review at this repaired scope. Normal and optimized Python
both pass 84/84 with byte-identical output; all D1–D6 regressions and the v10
self-containment audit exit zero.

Investigation 7 is complete at the mathematical-characterization,
corpus/literature-audit, and exact nonselection boundary:

- `note-d7-extension-rulebook-characterization.md` — the problem and its
  physical/mathematical closure gates, frozen before the audit;
- `note-d7-independent-rulebook-architectures.md` — seven architectures and
  the four-layer synthesis, frozen before external literature search;
- `note-d7-literature-and-priority-audit.md` — primary-source predecessor and
  conservative originality audit;
- `relativistic-isp-v10-paper8-the-extension-rulebook-is-extra-physics.md` —
  full V1–V9 review, exact results, literature comparison, and final boundary;
- `code/d7_rulebook_nonselection_exact.py` — 68/68 exact/high-precision
  checks in normal and optimized Python with byte-identical output;
- `data/d7-final-receipt.md` — hashes, reproduction, and claim ceiling.

D7 identifies the missing object as a null-inclusive law on typed marked
extension packages, or equivalently a full variable-history measure plus an
initial law and off-support version choices. The strongest admissible schema
has four layers: extension grammar, local transfer cocycle, positive/projective
completion, and record instrument/seal. An exact witness proves that the
established covariance, projectivity, capacity, no-silent, diamond, and
full-history principles host inequivalent rulebooks and do not select their
activities or instrument.

Every major mathematical component has clear literature predecessors. No
priority or unique final-law claim is made. The next advance requires either
declaring a full covariant marked-history law primitive or adding a genuinely
new empirical/microscopic selection postulate, then testing its implications
in the V9 cone, dimension, scale-ladder, and finite-propagation pipelines.

Investigation 8 completes the constructive branch:

- `note-d8-complete-rulebook-selection-gates.md` — success ladder and ten
  frozen mathematical/physical gates;
- `note-d8-independent-sealed-causal-instrument-rewrite.md` — SCIR derived
  before the new literature search;
- `note-d8-literature-audit-scir.md` — quantum causal history, quantum Petri
  net, stochastic rewrite, flash-process, graph-dynamics, and RG comparison;
- `relativistic-isp-v10-paper9-a-complete-sealed-causal-instrument-rewrite-rulebook.md`
  — the complete generative law and claim boundary;
- `code/d8_scir_exact.py` — 101/101 exact/high-precision root, bridge,
  instrument, gauge, projectivity, no-signalling, and non-explosion checks;
- `code/d8_scir_diffusion_shadow_exact.py` — 19/19 exact/high-precision
  conservative-leak, semigroup, and propagation checks;
- `code/d8_scir_v9_shape_replication.py` — a fresh 24-seed strict V9 shadow
  replication against a hash-gated frozen builder;
- `data/d8-final-receipt.md` — reproduction, hashes, and the final boundary.

SCIR is a complete-compressed rulebook. Its finite primitive data are typed
record ports, one root package/state, a finite locally emitted opportunity
grammar, and finite local unitary/Kraus coupling packets. Exponential survival,
Born allocation, sealing, construction-order gauge, projective full-history
probabilities, operational no-signalling, and bounded-grammar non-explosion
then follow.

The exact grammar and coupling matrices used by nature are not derived. The
result identifies them as ordinary primitive measurable physics rather than
hiding them in one arbitrary number per history. In V9 language, shared
allocation `c` is absorbed into the local instrument; transfer `g` is a real
local coupling. The continuous conservative shadow matches the successful V9
leak mechanism. A fresh strict 24-seed test passes the `m4` shape leg strongly
but fails the `dom` leg; geometry support remains promising, not validated.

Investigation 9 performs the first frozen cross-domain identification and
falsification:

- `note-d9-minimal-packet-and-anti-tuning.md` — one-coupling packet and holdout
  semantics frozen before computation;
- `code/d9_minimal_bell_packet_exact.py` — 126/126 exact and reproducible
  finite-sample Bell/tomography checks over `Q(sqrt(2),i)`;
- `code/d9_drift_matched_dimension.py` — 21/21 validation gates on synthetic
  latent dimensions 2–6 under the measured nonstationarity class;
- `code/d9_frozen_packet_geometry.py` — Bell-frozen `g=1/2` primary geometry,
  influence, and scale holdouts;
- `note-d9-literature-and-hostile-self-audit.md` — self-testing/tomography
  comparison and seven hostile scope openings;
- `relativistic-isp-v10-paper10-bell-identifies-but-does-not-set-geometric-coupling.md`
  — complete D9 result;
- `data/d9-final-receipt.md` — hashes, reproduction, and claim boundary.

Within a one-partial-iSWAP family, maximal Bell entanglement fixes
`theta=pi/4` and blindly predicts `g=sin^2(theta)=1/2`. The exact packet gives
CHSH `2 sqrt(2)`, no-signalling, construction gauge, projectivity, and
gauge-relative observable recovery. The repaired dimension ruler recovers
synthetic `d=4` as 4.013 under the old 1.55-fold failure drift, where the
legacy ruler saturates at 6.

The frozen geometry holdout refutes the cross-domain identity. At `g=1/2`,
`F_dom` is above its line by `+3.15 SE`, corrected volume dimension is `2.709`,
and both shape conventions worsen significantly from `N=2048` to `8192`.
Influence reaches all 32 slots and structural dimension witnesses pass 24/24,
so coupling exists but has the wrong macroscopic strength. SCIR survives; the
one-coupling universe does not.

Investigation 10 tests the stronger post-D9 hypothesis that quantum records
and causal geometry share a local channel algebra rather than one raw
coupling:

- `note-d10-bloch-celestial-selection-protocol.md` — frozen layer questions,
  anti-circularity gates, comparison algebras, and verdicts;
- `code/d10_bloch_lorentz_exact.py` — 109 frozen exact checks for the
  conditional complex-qubit/Lorentz isomorphism, rank-two alternatives,
  supplied `SU(2)` connection covariance, and the `SL(2,C)` boost boundary;
- `code/d10_finite_clock_convergence.py` — 99 frozen 90-decimal checks for
  finite outer-cone support and its convex-hull completeness hypotheses;
- `code/d10_relational_scir_packet.py` — 43 frozen checks for finite-depth
  `H/T` projectors, a real projective seal, chosen-packet schedules and
  controls, and bounded-forest intervention probabilities;
- `note-d10-bloch-celestial-investigation.md` and
  `note-d10-literature-audit-bloch-celestial.md` — full result and literature
  boundary;
- `note-d10-round1-opening-repairs.md` — every hostile opening and repair;
- `relativistic-isp-v10-paper11-many-clocks-few-factors-is-an-exact-kinematic-bridge.md`
  — final paper;
- `data/d10-final-receipt.md` — reproduction, hashes, review closure, and
  final claim ceiling.

The exact positive result is conditional: `Herm_2(C)_+` is the `1+3` future
Lorentz cone, normalized null rays are `CP^1=S^2`, and any number of
directional positive evaluations depend on four real factors. Finite direction
sets are outer polyhedral shadows. A finite local alphabet can generate
increasing finite projective sets without an `S^2` oracle in the generator.

The result does not derive physical `3+1`. Existing sealed records do not
select the complex rank-two factor; normalized qubits do not supply the cone's
time scale; supplied `SU(2)` link covariance does not derive physical link
birth or boosts; per-record evidence capacity is open; and algebraic
positivity, ancestry, coordinate shadows, and spacetime influence have not
been identified. Three hostile rounds close unanimously at
`KINEMATICS-ONLY`.

Investigation 11 attempts the decisive integration and records why it fails:

- `note-d11-complete-bloch-lorentz-scir-protocol.md` — frozen complete-packet
  target plus post-review adjudication;
- `code/d11_complete_bloch_lorentz_exact.py` — 73 exact checks for the dual
  gauge template, typed instruments/outcomes, owned sibling interaction,
  finite-prefix kernel, selected schedule cell, and extinction theorem;
- `code/d11_generated_history_geometry.py` — 72 frozen paired histories at
  cutoffs 512--2048;
- `note-d11-complete-bloch-lorentz-scir-investigation.md` and
  `note-d11-literature-audit-complete-packet-and-extinction.md` — result,
  literature boundary, and next hypotheses;
- `note-d11-round1-opening-repairs.md` — all hostile openings, implemented
  repairs, and accepted downgrades;
- `relativistic-isp-v10-paper12-complete-lorentz-rulebook-that-cannot-grow-a-universe.md`
  — review-corrected paper (internal title: *A complete global record kernel
  that cannot grow a universe*);
- `data/d11-final-receipt.md` — final hashes, reproduction, and claim ceiling.

The globally raced sequential kernel is fully numeric once its primitive root,
global enabled-token race, qubit matrices, scale, and unit activities are
supplied. Its instruments are incidence-scoped; its separate `SL(2,C)`
event/effect template is exact; ancestry lies in the declared positive cone;
and an owned sibling-MERGE transfers a root intervention into a downstream
seal distribution.

But it is not the requested final local law. Generated histories remain in
one root frame, canonical projective pushforward is open, the event race is
globally normalized, sibling-MERGE cannot join independent components, and no
physical spacetime/influence equality is derived. The exact population result
is stronger: with `p` open ports and `j` sibling merges, the drift is
`-j/(2p+j)` and terminal-SEAL extinction occurs almost surely. All 72 frozen
histories die before their target; the longest lasts 171 clicks. Corrected
downstream-seal merge influence occurs in only 1/24, 4/24, and 5/24 histories.

Final D11 verdict: `INCOMPLETE-PACKET`. The next investigation must compare
predeclared continuation ontologies—continuing COMMIT, separate local birth,
and terminal observation—while also solving decentralized event selection,
generated multi-frame transport, canonical history pushforward, and general
bridge birth. `SEAL -> COMMIT` is a candidate, not a derivation.

Investigation 12 restores the V6 diamond and closes the derivation question:

- `note-d12-diamond-restoration-uniqueness-protocol.md` — frozen ontology,
  architecture classes, and U0–U10 gates;
- `note-d12-v6-v10-compatibility-ledger.md` and
  `note-d12-extension-law-characterization.md` — corpus reconciliation and
  exact missing object;
- `note-d12-selection-principle-audit.md` — action, MaxEnt, symmetry, RG,
  ISP, process, and literature adjudication;
- `code/d12_diamond_law_nonuniqueness_exact.py` — 42 exact/high-precision
  finite history and interaction nonselection checks;
- `code/d12_symmetric_interaction_family_exact.py` — 18 exact common-symmetry
  iSWAP checks;
- `code/d12_multidiamond_history_exact.py` — final 145-check exact typed
  multi-diamond, projective, gauge, locality, RN, finite-memory, and threshold
  implementation;
- `relativistic-isp-v10-paper13-the-click-law-is-the-whole-history-process.md`
  — final theorem and physical interpretation;
- `data/d12-final-receipt.md` — authoritative hashes, regressions, scope, and
  four-round hostile closure.

D12 proves that the shared finite-packet, connected-collar, Born,
projective, construction-order, unitary-frame, evidence, and durable-record
principles admit two complete continuing models with different record
probabilities (`1/2` versus `0`). Strictly positive all-level non-Markov
`P_r` families separately retain different conditionals under the same lower
shadows. Complete holonomy identifies a supplied law; it does not select it.

The maximal universal form is now sharp. For a supplied typed grammar `G` and
compatible full-history measure `mu`, the next local record is the positive
cylinder conditional `mu([He])/mu([H])`. This requires no physical global
commit order. It does not derive `G`, `mu`, action, fields, couplings, initial
state, record instrument, gravity, scale, or the V9 geometry map.

Four hostile rounds end unanimously at
`UNIVERSAL-FORM/PRIMITIVE-PROCESS-REMAINS`, explicitly limited to the audited
finite-packet/unitary-frame scope. Geometry is correctly withheld. D12 is an
underdetermination theorem, not the selected law of our universe.

## D13-D20: action, history functional, identifiability and final audit

D13-D16 move from supplied regional amplitudes to causal-order actions:

- Paper 14 and D13 prove finite local-unitary kernel nonuniqueness and retain
  general-boundary amplitudes as a candidate architecture, not a selected law.
- Paper 15 and D14 give the hostile-reviewed 42-check conditional bridge from
  supplied regional amplitudes/state/instruments to durable projective
  non-Markov records.
- D15 identifies the Einstein-Hilbert plus Standard Model EFT as the leading
  extensively tested low-energy baseline given observed 3+1 fields; its
  coefficients, state, records, units and UV completion remain inputs/open.
- D16 proves that finite poset relabeling covariance and interval dependence
  leave distinct causal-action coefficient packets.

D17-D19 isolate the remaining law exactly:

- `code/d17_integrated_causal_history_exact.py` passes 40/40.  One fixed
  causal action and supplied owner-safe filtration admit inequivalent positive
  kernels that reach actual causal cylinders, local memory and durable records.
  Final result: `CAUSAL-ACTION-TO-MEASURE-NONSELECTION`.
- `code/d18_minimal_history_rulebook_exact.py` passes 30/30.  On a finite typed
  event algebra, supplied strongly-positive `D` is sufficient for every
  licensed conditional; it is not derived, uniquely minimal or automatically
  a durable-record ontology.  Final exact result:
  `FINITE-DECOHERENCE-FUNCTIONAL-SUFFICIENCY`.
- `code/d19_empirical_identifiability_exact.py` passes 20/20.  A rank-seven
  training map on eight complete histories has an exact positive null
  direction, so all one/two-record shadows can agree while a future
  conditional differs.  This is
  `FINITE-HISTORY-LAW-EMPIRICAL-NONIDENTIFIABILITY`, not physical generator
  nonselection.

Paper 16 and D20 synthesize the answer.  The next-click law is the conditional
shadow of a supplied complete history functional.  A local explanatory
generator additionally needs domain/fields, measure/contour, action, state and
record dynamics.  SHARD principles and present evidence do not uniquely
select those data, their coefficients/scales, 3+1 emergence or gravity.  V9
is historical `PARKED-AT-PROTOCOL` evidence, not an untouched holdout, and may
not be reused to tune/rank incomplete candidates.

Full-objective verdict: `NOT ACHIEVED`.  See
`relativistic-isp-v10-paper16-the-rulebook-is-the-history-law.md` and
`note-d20-full-objective-closure-audit.md`.
