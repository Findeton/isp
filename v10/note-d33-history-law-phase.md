# D33–D36 — the history-law phase: measure first, kernel derived

**Status:** PHASE PIN (pre-receipt), 2026-07-13; committed before `code/d33_gauge_reaudit_exact.py` runs. Provenance: the external history-law proposal (user-relayed; filed verbatim at `reviews/d33-external-proposal-history-law.md` with the triage header) + the in-session assessment amendments, adopted by the user's "go ahead". Foundation: paper 20 TERMINAL (#153) — the campaign's one bought sentence: *the missing mechanism is not a parameter of the sequential lottery*. The primitive object changes: **stop treating the next-opportunity kernel K as fundamental; define the probability law μ of complete typed record histories first, and derive K by conditioning, K_μ(o|H) = μ(H+o|H).** The record's own artifacts already instantiate this: K_flat IS K_μ for the uniform horizon measure, and its "knows the final horizon" defect IS the failure of projective consistency (the D28b recurrence exhibits it). V9 discipline verbatim; paper 16's nonselection gate stands (boundary state, measure/contour, and any selection principle declared explicitly — never smuggled into normalization).

## 1. D33 — the O7 gauge theorem + the no-go re-audit [EXACT; runs now]

**The refined O7 resolution [adopted]:** *causal order is recorded physics; ordering between incomparable events is gauge.* The physical history = a typed causal partial order (events = clicks; record wires connect events locally; seals/channel marks type them; order along a shared record is physical; linearizations are bookkeeping). This REFINES the covariance we tested: our state-level convention also quotiented same-wire (comparable) swaps — demands that were TOO STRONG under the refined gauge.

**The re-audit, with pinned expectations from the record:** (a) DEG/DDEG kills SURVIVE — the raw disjoint-only single-row certificates (d31c2, the S8-probe class) use only incomparable pairs; (b) MOTIF survives at u ≥ 3 (disjoint Z-forcing + birth-inertness); (c) GRAPH-BLIND is RESTATED ASYMPTOTICALLY — the u = 2 seed-level forcing was an overlapping (comparable) pair and is dropped; the surviving theorem: under linear-extension covariance, a stationary birth-positive graph-blind kernel has interaction weight constant on u ≥ 3, hence w_i ≤ 1/(u(u−1)) → 0: **interaction DENSITY vanishes at unbounded growth** (the honest weakening — and the exact shape of the proposal's success condition 3); a seed-level-interacting covariant kernel is exhibitable and priced; (d) COMPONENT-SIZE unchanged (u-only reduction ⇒ the same asymptotic statement; the D5b witness was covariant under the STRONGER gauge, a fortiori under this one). **Receipt gates (d33):** the gauge instantiated (linear extensions of one typed causal history identified; a comparable swap exhibited as physical, not gauge); the disjoint-only kills re-verified as incomparable-pair-only; the motif restatement; the asymptotic graph-blind theorem's pieces (constancy on u ≥ 3 from incomparable pairs; the normalization bound; the seed-level escape exhibited exactly); the refined landscape printed. Net effect pinned in advance: **the no-go complex survives the gauge refinement in strengthened form — weaker hypotheses, same architecture verdict.**

## 2. D34 — the existence program: an unbounded, local, projectively consistent interacting history law [THE BOTTLENECK]

**The target:** compatible laws μ_n on finite marked histories 𝓗_n with (restriction)_*μ_{n+1} = μ_n, hence one measure μ on complete histories (the profinite space's proper use). **The seven success conditions [PINNED, verbatim from the filed proposal]:** (1) relabelling + linear-extension invariance; (2) restriction consistency; (3) positive birth AND interaction densities as the history grows without bound; (4) no final-horizon parameter or size cap; (5) no silent erasure; sealed records preserved; (6) local dependence on causal ancestry — a remote unrelated record cannot alter a local conditional (the anti-dilution property: no universe-wide denominator); (7) a proper quantum version (decoherence functional if a probability measure is insufficient — where incomparable swaps must commute as superoperators, not merely on labeled webs). **The named tension (in-session):** (5) is a GLOBAL coherence property (distinguishability-isometry over the total ontology) while (6) is local — proving a locally specified law satisfies NSE requires the paper-19 reception machinery, not just locality; this is where the interesting mathematics lives. **The constructive route [adopted]: the HARRIS CONSTRUCTION** — autonomous local record clocks (each unsealed record carries local hazards: birth, interact-with-locally-available-partner, idle) instead of one global opportunity lottery; the causal-history law of a race of local exponential clocks depends only on local rates (incomparable race order integrates out) — linear-extension invariance essentially by construction. **Failure is decisive:** if no such law exists, at least one of stationarity, covariance, the birth/interact grammar, unbounded growth, or persistent interaction must be abandoned — a theorem either way. **[LITERATURE anchor]:** Rideout–Sorkin classical sequential growth (discrete general covariance + Bell causality; pure-birth in our language — this program is CSG-with-interactions-and-marks); the Harris graphical construction for interacting particle systems. **Receipt d34a (first):** the Harris lemma at finite depth, exact rationals — (i) the induced law on typed causal histories is linearization-independent (computed two ways, equal); (ii) restriction consistency at small depths; (iii) persistence: birth and interact densities positive and non-vanishing as depth grows (exact small-n + sampled trend); (iv) no cap parameter anywhere; (v) remote-record locality: adding a causally unrelated record changes no local conditional (exact). d34b+ (the NSE gate; the quantum lift) pinned after d34a's round.

## 3. D35 — the empirical wings [pinned; run after D33's round]

**(A) The local-scheduler compression re-test [the decisive cheap diagnostic]:** re-run the D32B (r, λ) map under the local-hazard scheduler at FIXED PER-RECORD BUDGET. Pre-registered hypothesis: the compression regularity is partly a LOTTERY artifact (the global denominator makes interacts dominate as collar pairs multiply); under local clocks the fr-growth channel is closed by construction. Either outcome decides: compression is a law of interaction, or of the scheduler. Card v2.2, the D32B verdict machinery, per-cell disclosure discipline (fallbacks, minima, n_mid), signature pairs.
**(B) The fixed-budget aligned pilot [external review #3's standard, now a PILOT of the new architecture]:** aligned directional compatibility redistributing a FIXED local interaction budget (direction never changes the budget, only the partner distribution), with shuffled-direction and direction-blind controls bracketing any effect. Run under the local scheduler. The lift bar per the #152 standard (matched rate BY CONSTRUCTION; the D32C confound is structurally impossible here).

## 4. D36 — dynamic adjacency [HELD until d34a + D35A land]

Interactions must be allowed to CHANGE future relational geometry (diamond completion / adjacency modification), not merely add operations on a frozen birth tree — paper 20's evidence that metric-inert interactions only add comparability. The candidate history-weight terms (birth, diamond completion, sealed-holonomy consistency, channel compatibility, saturation/branching penalties) enter here — under the paper-16 gate (a local action does not select the measure; the selection data is declared).

## 5. Fronts (pinned)

(HF1) The refined gauge's quantum lift: comparable-swap physicality vs superoperator commutation for incomparable swaps — d34b territory. (HF2) The Harris race at simultaneous ties (measure-zero for continuous clocks; the discrete receipt must pin a tie convention and show it is gauge). (HF3) Persistence vs explosion: local budgets must not freeze out (all-idle absorbing) or explode; the d34a trend gate is the first look. (HF4) The D35A comparison's confounder: the local scheduler changes both the denominator AND the effective time parameterization — the pre-registered comparison is at matched realized event counts, stated. (HF5) NSE-locality tension (§2). (HF6) Whether D33's asymptotic restatement changes any paper-20 sentence (expected: no — paper 20 stated kills at their receipt scopes; a delta note if any width moves).

## 6. The d34b pin (2026-07-13; pre-receipt) — THE MEASURE-COMPLETING STEP: exponential clocks

**Provenance:** #158/#159 (D34a terminal as the weight-system receipt; the measure absent — reception placements unpriced; the discrete race's actor layer re-imports the census denominator). **The construction [the proposal's actual content]:** every unsealed record carries an INDEPENDENT UNIT-RATE EXPONENTIAL CLOCK; on its ring it draws from the SAME local conditional q (birth ¼; interact-total ¼ redistributed over unsealed collar neighbors; idle the remainder; initiator model; idles recorded). Memorylessness prices placement: the interleaving becomes the race of independent exponentials, and the causal-history σ-algebra forgets it.
**The mathematical claims to gate (exact, small depth):** (K1) the cylinder algebra is PER-RECORD (down-sets / per-record prefixes) — the #158 diagnosis: d34a's Σ = |present| came from the WRONG (global-event-count) cylinder algebra; on down-sets the weight system is already per-record normalized. (K2) THE HARRIS LEMMA PROPER: the exponential-clock path law (placement factors 1/k at k live records), marginalized onto any past-cone-closed down-set, equals Π q over the down-set's events — the placement factor integrates out of every local marginal. Finite-depth exact form: SANDWICH bounds — P(D complete by n) ≤ Πq ≤ P(D complete by n) + P(D-consistent-incomplete at n), gated to hold and NARROW with n (the tail is the unfinished-cone mass). (K3) full restriction consistency on down-sets (the object #158 said was missing), exact. (K4) HF2 discharged: continuous clocks have no ties (probability zero); the discrete computation uses strict orderings only — verified structurally. (K5) remote locality at the MEASURE level: local down-set marginals exactly unchanged under remote clusters. (K6) the scorecard at measure level; the projective-limit step stated as standard Kolmogorov extension over the per-record algebra, NOT claimed proven beyond the finite gates.
**DISCLOSED RE-SCOPE of #159's d34b gate list:** NSE/sealed-preservation (5) and the quantum decoherence-functional lift (7) move to **d34c** — the classical measure must exist before its NSE audit is well-posed; HF5 (the NSE-locality tension) is d34c's center. **Fronts:** (KF1) the sandwich's convergence rate (idle mass slows cone completion — geometric tails, rate printed); (KF2) the placement factor's k counts ALL live records including remote ones — K2/K5 must show it still integrates out locally (the crux); (KF3) the passive-reception cylinder subtlety (x's timeline contains events x never drew); (KF4) rate heterogeneity (unit rates here; record-dependent rates = a named variant).

## 7. The d34b repair pin (2026-07-13; post-round-1, pre-delta) — THE CLOCK CONSTRUCTION SURVIVES; THE RECEIPT DOES NOT

**Round-1 adjudication (report filed before this pin): REJECT AT STATED WIDTH, 4 MAJOR / 6 MINOR / 3 NIT.** Section 6 and LEDGER #160 are retained as the historical pre-registration and first claim, but their measure-level verdict is FORWARD-CORRECTED at #161. The decisive defect is global fresh-label matching: `w1` meant “the first birth anywhere”, so three advertised local events were not local. Their exact limits are 1/3, 1/12, and 1/5 rather than the printed 1/2, 1/8, and 1/2; the committed code fails its own brackets at depths 8, 29, and 12. The depth-4 brackets were too wide to diagnose this, the claimed convergence rate was not printed, K4 was a source-text grep, and no full mass/restriction gate was present. **What survives independently:** unit-rate exponential clocks are a legitimate local placement construction; the label-free interaction target is correct; and the renewal theorem gives the intended local initiated-prefix probabilities. The failure is in event definition, verification, and claim width—not yet a falsification of the clock object.

**The repaired mathematical object [PINNED]:** preassign every possible Ulam–Harris record address an independent rate-1 Poisson clock and independent mark stream. A record becomes active at birth. On each ring it draws birth 1/4; interaction-total 1/4 split over its currently eligible unsealed birth-tree neighbors; idle the complement. Birth creates a child whose address is `(parent address, parent-local birth ordinal)`—never a universe-wide counter. Interaction is one shared event touching initiator and receiver wires; the receiver is passive and its scheduled clock is not reset. The physical history is the typed wire-DAG obtained after auxiliary clock times and heap serialization are forgotten. Its law is the PUSHFORWARD of the independent clock/mark product measure. Individual scheduler linearizations need not have equal probabilities; gauge invariance means that all auxiliary realizations with the same typed causal history are summed by the pushforward. A single heap is an implementation of the independent clocks, not a primitive global chooser.

**The repaired gates [frozen before code changes]:**

1. **R1 — label-blind local cylinders, exact.** Fresh names use alpha-binding: `b(A,alpha)` binds alpha to A's actual child and later references dereference it. Stop on A's OWN rings/acts, never on a fixed number of global rings. Gate the idle-inclusive probabilities 1/4, 1/4, 1/2; the first-act probabilities 1/2, 1/2; and `birth(A,alpha)` then `interact(A,alpha)` = 1/8 at act level (= 1/32 for the two literal consecutive rings). Derive and print exact geometric partial sums/tails in A-LOCAL ring count. Explicitly exclude the convicted wrong limits 1/3, 1/12, 1/5 and exhibit that the old raw-label matcher fails after a remote birth while the binder passes. Do not transfer that geometric claim to GLOBAL jump depth: the exact killed-chain recurrence has a polynomial tail (asymptotically proportional to `n^-2` here) because births continually add competing clocks.
2. **R2 — normalization and projectivity, exact.** Gate the complete idle-inclusive and act-normalized per-record alphabets through prefix length 2; a two-record product partition; every one-step restriction sum; and a reception-containing multi-record cylinder. Distinguish the initiated-stream law from its pushforward onto the full wire-DAG. Do not call `prod q` alone the full-history measure. For a fixed-depth physical history H the embedded-chain mass is `prod_e q_e` times the SUM over its linear extensions of the placement factors `prod_j 1/k_j`; at fixed physical time the holding times are integrated as well.
3. **R3 — passive receptions and the physical DAG, exact.** An interaction `i(y,x)` appends one event to both wires while consuming only y's ring. Direct predecessors are the last events on every touched wire; an independent transitive-closure reconstruction must agree. A forced `i(B,A)` before `b(A,alpha)` proves that the A-birth alone is not past-cone-closed; its restricted cone includes the reception. Gate the minimal placement counterexample: A's first ring is `i(A,B)`, B's first ring is `b(B,alpha)`, and the interaction precedes the birth on B's wire has probability `(1/4)(1/4)P(T_A<T_B)=1/32`, not `prod q=1/16` (act level 1/8, not 1/4). Gate that the passive reception leaves A's already scheduled clock and A's private mark index unchanged.
4. **R4 — literal actor reference implementation, high precision plus an independent exact oracle.** Implement explicit actor objects with private ring counters, private lineage addresses, private keyed random streams, and private scheduled times. Compare a heap implementation with a scan-min implementation pathwise after stripping auxiliary serialization. Independently enumerate the embedded exponential race with exact rational `1/k` and mark weights at small depth, canonicalize typed DAGs, and compare masses/distributions. Use at least 100 decimal digits for exponential times; run under multiple hash salts. No OS threads are required—the proof concerns independent state and clocks, not simultaneous CPU execution.
5. **R5 — remote locality at the right stopping rule, pathwise.** Couple Seed2 and Seed2 disjoint-union (P--Q) with the same keyed streams for shared actors. At fixed physical T and at fixed A-local ring/act count, the complete shared-component projection—including local times, marks, lineage labels, and causal DAG—must be bit-identical. Print the negative control: at fixed GLOBAL event count it generally differs and is not a locality criterion.
6. **R6 — existence, nonexplosion, persistence, and scope.** Prove the population is a Yule process with per-capita birth rate 1/4: `E N(t)=N0 exp(t/4)` and `sum_n Exp(n/4)=infinity` almost surely, hence finitely many records and rings on every finite interval. Gate finite-time means/distributions at high precision as checks, never as substitutes for the proof. With the present seed every live record remains eligible, so ring kinds have persistent masses 1/4, 1/4, 1/2. Declare the current adjacency static (birth-tree only), disconnected components never join, and the coefficients 1/4 are a chosen exemplar—not yet THE universe's derived interactive law.

**Claim ladder frozen [historical pin; topology clause superseded by §9]:** passing R1–R3 earns the initiated-cylinder law and its deterministic causal-history map; passing R4–R6 plus the construction/nonexplosion proof earns a well-defined classical continuous-time history measure at finite physical time and its consistent projective family in time. This pin proposed a profinite finite-observation shadow; §9 withdraws that proposal because no inverse system was actually constructed. Real clock times make the full marked path space standard Borel. **d34c remains separate:** the classical law does not by itself establish NSE, sealed preservation, or a quantum/decoherence-functional lift. No D35/D36 physics run begins until the repaired d34b hostile delta closes.

## 8. d34b repaired receipt (2026-07-13; round pending) — THE CLASSICAL ACTOR-PROCESS MEASURE EXISTS

**Receipts:** `code/d34b_exponential_clocks_exact.py` (7/7; E1–E6 substantive, E7 dependent scorecard) and `code/d34b_actor_reference_high_precision.py` (8/8; A1–A7 substantive, A8 dependent scorecard), with byte-matched outputs in `data/`. The actor output is byte-identical under `PYTHONHASHSEED=0,77`. The exact receipt uses rational arithmetic throughout its probability layer and 100 decimal digits for the long killed-chain/Yule tables; the actor receipt uses 100-decimal exponential deadlines, exact 256-bit quartile marks, and unbiased partner selection.

**The repaired result:** the preallocated independent clock/mark tapes are a normalized product law. The live population is a Yule process with per-capita birth rate 1/4; the dyadic harmonic-block proof and finite expected integrated intensity establish nonexplosion. Therefore every finite physical-time restriction has finitely many marked events almost surely. The map from those tapes to the timed event stream and then to the typed wire-DAG is deterministic and measurable; pushing the source law through it gives the physical classical history measure. Time restrictions commute exactly with the construction. Disconnected source families factor, and common-random-number coupling makes the full local projection pathwise identical with or without a disconnected remote component. This earns success conditions (1)–(4) and (6) for the PRESENT STATIC-ADJACENCY EXEMPLAR at the classical level.

**The two algebra theorem [corrective center]:** own-record ring prefixes have `prod q`; own-record act prefixes have `prod qbar` after idles are integrated out. Physical reception-containing histories do not. The exact minimal split is `1/16 = 1/32 + 1/32` at ring level (`1/4 = 1/8 + 1/8` at act level): the two orders of A's interaction with B and B's birth share B's wire and are recorded physics. For an untimed fixed-depth physical history H, its mass is `prod_e q_e` times the linear-extension orbit sum of the placement factors; the three-extension receipt exhibit has unequal representative masses `1/2304, 1/3072, 1/3072` and class mass `5/4608`. At fixed time the analogous holding-time density is integrated. **Placement is part of the physical law whenever receptions make it comparable; it “integrates out” only after a declared coarse-graining.**

**The actor result:** the reference contains literal actor objects with private clocks, counters, Ulam addresses, mark tapes, and next deadlines. A heap and a full scan of private deadlines produce identical event streams and DAGs on 24 tapes/387 events; no OS threads are needed or claimed. Passive reception leaves the receiver's private ring state bit-identical while adding the proper causal predecessor. At fixed T and at fixed A-local rings, 40/40 Seed2 versus Seed2 disjoint-union (P--Q) couplings are bit-identical on the shared component; at fixed GLOBAL event depth, 40/40 differ—the intended negative control. Fixed-time restriction passes 24/24. An independently coded rational oracle has 40 two-event DAG classes and mass one; 12,000 high-precision actor worlds give chi-square 41.284 against the conservative 109.654 bar and max absolute z 2.204 < 6.

**Two forward-corrections beyond #160:** (i) section 6's global-depth “geometric” rate is withdrawn. In A-local time/ring count the usual exponential/geometric/Erlang laws hold and are remote-independent; in global jump count the exact killed chain has `S_n ~ 16 k0(k0-1)n^-2`, with the remote-dependent constant a computational census artifact. (ii) D34a #158/#159's inference from unequal representative weights was too strong. At fixed embedded depth, the uniform-actor `q/k` sampler is exactly the embedded chain of equal-rate exponentials, and it DOES induce quotient masses by summing representative weights on each causal-history class. What D34a correctly killed was the identification of that measure with bare `prod q` and the claim that a global-next-event conditional is local. D34b separately supplies the local-clock construction, physical-time projectivity, nonexplosion, and correct quotient/pushforward interpretation.

**Still open and explicitly not consumed [provisional §8 wording, superseded where noted in §9]:** the coefficients 1/4 are chosen, not derived; adjacency is the birth tree and interactions are metric-inert; disconnected components never join; heterogenous/state-dependent clock rates are untested; the timed path space is standard Borel; the proposed profinite shadow is withdrawn pending an explicit inverse system; and conditions (5)/(7)—global NSE, sealed preservation, and a genuine quantum/decoherence-functional lift—are d34c. Round-2 hostile review now attacks the repaired theorem and both receipts before d34c may begin.

## 9. d34b repaired hostile round APPLIED (2026-07-13; delta pending) — THREE OBJECTS, NOT ONE

**Independent streams:** measure/topology (3 MAJOR / 5 MINOR / 3 NIT), actor code (4 MAJOR / 6 MINOR / 3 NIT), and asymptotic/numerical (2 MAJOR / 5 MINOR / 2 NIT). Reports are filed separately in `reviews/`. All overlap on one verdict: **the ideal classical Harris law survives with zero false printed numbers and zero core probability falsifications; #162 overstates topology/projectivity and executable exactness.** The following repairs supersede the final paragraph of §8 and the live reading of §7 R2/R4/R6.

**The three objects [now disjointly named]:**

1. `Omega`, the countable product of ideal independent exponential/mark coordinates on all possible flat Ulam addresses. Its finite-coordinate cylinder law is normalized and projective.
2. `X_T`, the locally finite TIMED marked event process up to physical time T, produced measurably from `Omega`. The family `nu_T=Law(X_T)` is projective under the visible time restriction `r_{S,T}`. This is what the actor A5 gate tests.
3. `G_T=F_T(X_T)`, the Ulam-coded untimed typed wire-DAG after auxiliary times and incomparable serialization are forgotten. `mu_T=(F_T)_*nu_T` exists for every T, and the complete all-time typed-DAG pushforward exists. **No intrinsic map `G_T -> G_S` is claimed after timestamps are erased**: the later untimed graph does not reveal which events lay before S. A further quotient by arbitrary graph isomorphism is also not claimed; the current codomain retains relative Ulam lineage addresses. It is standard Borel as a countable-product encoding of locally finite typed relations, and the finite-T canonicalization is measurable.

**PROFINITE FORWARD-CORRECTION:** §7/§8's present-tense “finite-observation shadow is profinite” is WITHDRAWN. A profinite shadow may be constructible only after specifying an explicit directed system of finite stem/cylinder quotients and gauge-safe restriction maps. None has yet been supplied. The full timed law is standard Borel, not profinite. This is now a named topology open, not a delivered consequence.

**Nonexplosion and persistence proofs [now carried]:** if `X_n~Exp(n/4)` are the successive birth-stage holding times, `E X_n=4/n` diverges in sum while `sum Var X_n=16 sum 1/n^2<infinity`. Therefore the centered sum converges almost surely and `sum X_n=infinity` almost surely. Between births, total rings before the next birth are geometric with success 1/4, hence finite almost surely; the full process is nonexplosive. At every history in this static, no-sealing exemplar, every live record has an eligible parent/neighbor and the conditional next-kind law is exactly `(1/4,1/4,1/2)`. The bounded martingale SLLN therefore gives almost-sure empirical birth/interact/idle densities `(1/4,1/4,1/2)`. Partner identities are not iid, and the theorem is not transported to sealing/dynamic adjacency without a new proof. The exact receipt now gates the conditional type vector on all 304 depth-3 states and carries the variance step explicitly.

**Global-depth theorem [proof, not table]:** for `k0>=2`, let `M_t` be the non-target Yule population, initially `m=k0-1`. The Yule martingale `exp(-t/4)M_t` converges in L2 to `W~Gamma(m,1)`. Its ring count is asymptotic to `4W exp(t/4)`; target idles are lower order; the independent target-act time is `Exp(1/2)`. Inverting the exponential growth and applying dominated/regular-variation convergence yields `S_n ~ 16 E(W^2)n^-2 = 16 k0(k0-1)n^-2`. For `k0=1` the tail is geometric. The exact recurrence + 100-decimal table are now explicitly a cross-check of this theorem, not its proof.

**Relabeling scope:** the ideal source law and Harris map are equivariant under seed/type-preserving relabelings because the source coordinates are iid and the transition rule uses only typed incidence. The finite PRF reference is keyed by the structural address and is not pathwise invariant under renaming; it tests one deterministic coupling. `G_T` currently forgets global serialization but not all Ulam identities.

**Actor-reference repairs:** the executable now calls itself a finite BLAKE2-PRF/Decimal reference to the ideal continuous law. Finite-grid ties are possible and fail closed; no probability theorem about deterministic BLAKE2 coordinates is claimed, and A2 no longer attributes tie-zero to software. Ulam identifiers are flat paths with length-prefixed iterative encoding and pass depth 6000 (the hostile depth-998 `RecursionError` is closed). The DAG incidence checker no longer shares the producer's touched-wire helper; the rational oracle has a separately coded serializer and explicitly suppresses the event-inert sealed root `R`. A5 compares the full actor/neighbor/deadline snapshot, not events alone. A7 replaces ad hoc mixed bars with one frozen eight-SD implementation-diagnostic bar, the ideal Yule/negative-binomial moments and target distribution (2,000 fixed-time worlds), the joint-generator ring variance, and production-domain PRF quartile/partner diagnostics; these are not advertised as exact familywise tests or proof that the PRF is iid. A6 is labeled bounded statistical concordance between the finite PRF reference and the ideal depth-2 oracle, not exact continuous-law equivalence. The architecture is object-based autonomous clocks with one central atomic shared-event evaluator—not an OS-thread/mailbox implementation.

**Frozen-pin disposition:** §7 R2's initiated-prefix normalization is delivered exactly; its promised exact full physical-DAG restriction is narrowed to the source/timed projectivity theorem + A5 coupled reference check. The reception-containing physical algebra is delivered by E3/E4 (relative-order split + orbit-sum classes), but not by a finite untimed inter-T restriction map, which does not exist without retained times/a new observation functor. §7 R4's “literal” actor claim is narrowed to the ideal theorem plus high-precision reference. §7 R6 is now proof-complete at the static exemplar scope.

**Proposed terminal verdict for the delta referee:** **D34b CLASSICAL MEASURE PASS — the normalized, nonexplosive, persistent ideal timed Harris law and its Ulam-coded typed-DAG pushforwards are established; the PRF actor reference is concordant and pathwise local at its tested width; an intrinsic untimed projective/profinite architecture, dynamic adjacency/component joining, derived coefficients, NSE, and a quantum lift remain open.** d34c may open only if the hostile delta stamps this wording.

## 10. d34b TERMINAL delta (2026-07-13) — CLASSICAL MEASURE PASS

All three independent hostile streams returned **zero BLOCKER / zero MAJOR** on the frozen repaired target. The asymptotic stream returned DELTA-CLEAN outright. The measure/topology stream returned CORE DELTA-CLEAN with one minor and three nits; the actor stream returned SUBSTANTIVELY DELTA-CLEAN with two minors and two nits. Every conditional cleanup was applied rather than deferred:

- E5 no longer compares an expression with itself. It independently reconstructs the one- and two-act unfinished masses by a 100-decimal Poisson-ring/binomial-thinning series and matches the closed Exp/Erlang forms below `1e-90`; remote independence is attributed to source factorization, not to that numerical equality.
- A2 makes no probabilistic theorem about deterministic BLAKE2 outputs. Finite-grid ties are possible and fail closed; tie-zero belongs only to the ideal continuous source.
- The rational oracle's serializer is explicitly separate and its suppression of the event-inert sealed root `R` is disclosed.
- A7 audits the production `kind` domain, compares the finite PRF population with the **ideal law's** exact negative-binomial target, and uses one frozen eight-SD implementation-diagnostic bar. It is not advertised as an exact familywise test or as proof that the PRF is iid.
- The D34a `q/k` correction is scoped to fixed embedded depth. Physical-time projectivity belongs to the timed D34b construction.

Fresh executions are byte-identical to the committed data under `PYTHONHASHSEED=2026/31337`. Final output hashes are `47993cbcaf3d3a719ef868fd6a4d122b9b2d46e23555133d886185f79358740c` (exact receipt, 7/7) and `59d28bc5db03cca5e30a81eaed09c1c42d7e51541f6ea7c3d078c9d59a75c2a3` (actor reference, 8/8).

**TERMINAL VERDICT:** **D34b CLASSICAL MEASURE PASS — the normalized, nonexplosive, persistent ideal timed Harris law and its Ulam-coded typed-DAG pushforwards are established; the finite PRF actor reference is concordant and pathwise local at its tested width. An intrinsic untimed projective/profinite architecture, dynamic adjacency/component joining, derived coefficients, NSE, and a quantum lift remain open.**

This closes the classical measure-completion phase. It does **not** identify the universe's final click law: it proves that one explicitly chosen static-adjacency actor family is a coherent local history law. d34c may now ask whether this law can carry paper 19's no-shrinkage/sealed-record requirements and a strongly positive quantum history functional without turning the local clocks back into a hidden global scheduler.

## 11. d34c PIN (2026-07-13; before receipt) — THE NSE/QUANTUM COMPATIBILITY TEST

### 11.1 The question and the claim ceiling

D34b supplied a classical timed actor measure. Paper 19 supplied the **posited** No Silent Erasure (NSE) selector and the Busch characterization of trace-norm-isometric closures. D18 supplied finite/cylinder decoherence-functional sufficiency. None of those results yet proves that the same local actor architecture can carry all three structures at once.

The precise d34c question is:

> Can the D34b local-clock history law be realized as the durable-record sector of a strongly positive quantum history law whose total closure is NSE, whose sealed records are future-stable, and whose construction-order gauge requires no universe ledger?

This is a compatibility/existence test, not a selector derivation. Even a complete pass will **not** derive the `1/4,1/4,1/2` weights, the interaction unitary, adjacency, component joining, a preferred record basis, NSE itself, or nature's unique history functional. It also cannot by itself establish an infinite quantum-measure extension.

### 11.2 The candidate architecture, frozen

There are two distinct kinds of alternatives.

1. **Durable click alternatives:** every D34b local ring outcome is written to an event-local orthogonal flag on the touched record/environment sector. Different recorded click histories are therefore decoherent. The flags are distributed records, not one global history register; the single global ket `|h>` is notation for their tensor product.
2. **Unrecorded quantum alternatives between clicks:** these retain off-diagonal decoherence-functional terms and may interfere. When one becomes a durable record, the corresponding local flag makes the relevant partition decoherent.

At each local ring the mark distribution is preparation-independent. Idle is the identity; birth is a D24-style local isometry activating a fresh Ulam-addressed carrier; interaction is a local isometry/unitary on the touched carriers; and the typed outcome is appended as a durable orthogonal flag. Thus a finite branch has an isometry `V_h`. For a finite classical cylinder law `mu(h)`,

```text
Phi(rho) = sum_h mu(h) V_h rho V_h^dagger tensor |h><h|
```

is Busch form: preparation-independent weights and mutually orthogonal ranges. The general proof target is

```text
||Phi(Delta)||_1 = sum_h mu(h)||V_h Delta V_h^dagger||_1
                 = ||Delta||_1.
```

Erasing the flags is a negative control: an unflagged random-unitary average may contract trace distance and is not NSE merely because each branch separately is unitary. State-dependent scheduler weights are outside this construction; unless implemented as a larger recorded isometry, they threaten linearity/NSE and require a new theorem.

### 11.3 The exact operational diamond, frozen

Use four qubits `(S,R,P,O)` and exact arithmetic in `Q(sqrt(2))`. Start in

```text
|+>_S |0>_R |+>_P |0>_O.
```

The diamond is:

- `a = CNOT(S->R)`: create the durable receiver `R`;
- `b = Z_P`: an operation incomparable with `a` and disjoint from it;
- `c = CZ(S,P)`: the common future of `a,b`;
- `d = H_P` followed by `CNOT(P->O)`: commit the output to `O`.

The history alternatives are `h=(s,p,o)`: the durable `R` value after `a`, an **unrecorded** intermediate computational path `p` after `b`, and the durable output `O` after `d`. The predicted exact functional is

```text
D(h,h') = (1/8) delta(s,s') delta(o,o')
          (-1)^[p(1+s+o)+p'(1+s'+o')].
```

Each fixed `(s,o)` block is `1/8 [[1,eta],[eta,1]]`, `eta=(-1)^(1+s+o)`, with eigenvalues `(1/4,0)`. Coarse-graining over the unrecorded path predicts

```text
P(s,o) = (1/2) delta(o,1-s),
```

whereas reading fine diagonals alone would falsely give `1/4` for every `(s,o)`. This is the nontrivial-interference gate. Since `a` and `b` are disjoint, `ab` and `ba` must give the identical functional. A shared-carrier noncommuting reorder is the negative control.

### 11.4 Receipt gates

The receipt will be `code/d34c_nse_quantum_history_exact.py`, dependency-free, exact, and fail closed.

- **C0 — exact object typing:** separate the classical source law, durable click algebra, unrecorded path algebra, class operators, quantum functional, and NSE closure. No diagonal is read as a probability unless its partition is decoherent.
- **C1 — exact circuit/order gauge:** construct the circuit in `Q(sqrt(2))`; prove `a b = b a`; compute both orders and require byte-identical branch vectors/functionals. A noncommuting shared-carrier reorder must differ.
- **C2 — functional axioms:** derive all eight branch vectors and the displayed `D`; check Hermiticity, total normalization, exact Gram/strong positivity, rank/eigenvalue certificate, and additivity under incidence-matrix coarse graining.
- **C3 — genuine interference:** coarse `(s,o)` probabilities must be `(1/2,0;0,1/2)` in the appropriate ordering, while diagonal-only aggregation gives four `1/4`s. Recording `p` in an orthogonal receiver must remove the off-diagonals and give the diagonal law.
- **C4 — finite cylinder restriction:** directly compute the `s`, `(s,p)`, and `(s,p,o)` functionals and require the later functionals to push down by exact incidence matrices to the earlier ones.
- **C5 — seal durability:** after event `a`, every future operator acts as identity on `R`; gate the full `R` matrix algebra in Heisenberg form. Also gate preservation of the source value projectors while allowing its phase content to disperse. These are declared separately; “sealed” is not allowed to slide between them.
- **C6 — NSE/Busch lift:** prove the general orthogonal-isometry/direct-sum identity above and instantiate the D34b kind weights `(1/4,1/4,1/2)` with exact mutually orthogonal local flags. Gate trace-distance preservation on a noncommuting test family as regression, while labeling the all-state statement as the analytic block-norm theorem.
- **C7 — negative controls:** remove the flags from `1/2(I rho I + Z rho Z)` and require `|+>` versus `|->` to contract from distance one to zero. Make a preparation-dependent classical branch rule fail affine linearity unless its choice is retained in an enlarged isometric ontology.
- **C8 — actor/history sewing theorem:** for every finite D34b cylinder, compose only touched-carrier isometries and event-local flags. Show that classical restriction plus isometric exhaustiveness gives quantum-cylinder restriction, that disconnected actor families tensor-factor, and that no step queries a universe-wide live-record count. The finite diamond is the non-diagonal local factor; the durable D34b click sector remains diagonal.
- **C9 — scope scorecard:** a pass may earn `FINITE-CYLINDER NSE/QUANTUM COMPATIBILITY`. It may not earn coherent superposition across distinct recorded growth histories, infinite quantum-measure extension, a constructed profinite quantum space, dynamic joining, derived coefficients/operations, preferred-basis derivation, Lorentz cones/dimension, or `THE universe law`.

### 11.5 Hostile-review fronts, pre-registered

- **QF1:** a global `|h>` flag may hide a universe ledger. The proof must factor it into event-local durable flags and distinguish notation from architecture.
- **QF2:** a classical diagonal lift may be quantum in name only. C3 must exhibit nonzero off-diagonal history terms that change an observable coarse law.
- **QF3:** strong positivity at one matrix is not projectivity. C4 and C8 are separate gates.
- **QF4:** NSE of each branch does not imply NSE after forgetting which branch occurred. C6/C7 must make orthogonal durable flags load-bearing.
- **QF5:** exact seal durability may hold only because `R` is never touched again. That is an honest model clause, not a derived universal seal law; the report must say so.
- **QF6:** the construction can remain a compatibility family with primitive classical `mu`. It must not be narrated as a derivation of the quantum Born weights or of D34b's clock law.
- **QF7:** finite strong positivity need not extend to an infinite sigma algebra. The D18 extension warning is binding.

No D35 cone/dimension run may consume d34c unless the hostile delta accepts both the theorem and this claim ceiling.
