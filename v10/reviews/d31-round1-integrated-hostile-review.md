# D31 ROUND-1 INTEGRATED HOSTILE REVIEW (single stream) + the LEDGER #138 arc-delta fold-in — 2026-07-12

**VERDICT: PASS-WITH-FINDINGS — 2 MAJOR / 6 MINOR / 5 NIT. Zero gates flip; all three deliverables SURVIVE: the D31A no-go closes for ALL u once birth-positivity is inline (THEOREM grade); B1/B5 is UNDER-claimed (port poset == wire-DAG is a two-line theorem for total port maps — both objects are closures of the same next-op-on-wire generator; totality answered at the pinned couplings by the D28 kickback lemma); the C2 obstruction is exact. Both MAJORs are record-integrity/claim-scope class.**

## MAJOR (both applied same day; LEDGER #139)
**M1 — the d31b receipt was IRREPRODUCIBLE:** grow_collar consumed hash-salted set iteration in the sampling path (for x in adj[y]) — the committed .out could not be reproduced (census across hash seeds: 0-3 cancellation-capable pairs per web, webs with zero in most draws); the LOG/LEDGER #137 "1-2 per web" was one process's draw. FIX APPLIED: sorted(adj[y]); rerun — the determinized census [(41,3),(38,0),(41,1)] matches the review's prediction exactly; forward-corrections in LOG/LEDGER. No gate flipped in any draw (5/5 under every hash seed; B1 held on 15+ hash-scrambled webs — strengthened by the attack).
**M2 — the none-arm escape is BOUNDED-DOMAIN ONLY:** the exhibited constants fail idle-positivity at u* = 6 (active mass 31/30 > 1; n1 = 2(u-1) for tree growth); generally u*f_b > 1 at finite u, and Z == 1 re-imposes the bi forcing (interact weights constant) — at unbounded growth the none-absorbing multiplicity-insensitive class carries NO interactions: the arm IS the bounded-growth escape D31A prices, not a third route. FIX APPLIED: C4/C5 prints; the LOG/LEDGER landscape corrected; D32's covariance column carries the arm bounded-domain-only.

## MINOR (applied)
m1 the theorem sentence needed "birth-positive" inline (the birthless kernel w_b = 0, w_i = 1/(u(u-1)) is covariant WITH interactions — a genuine counterexample to the bare sentence); m2 the note's post-run pin promise unfulfilled — the §5 addendum now records THE FREEZE, the C-outcomes, and the descope; m3 family (ii) (distance-degree) received no deliverable — named debt (O3 = F3'); m4 C2's scope was ONE condition, needs TWO (multiplicity-insensitivity AND birth-inertness): simple-degree/motif/component-size move to the OPEN arm; m5 C4's covariance quantifier = active-subproduct on the none-free skeleton (none-padded products are not order-invariant: f_n(H+b) = 203/300 vs f_n(H+i) = 5/6); m6 B3's label scoped to the six pinned pairs (four active).

## NIT
n1 A4's label -> uniform product + the all-depth remark (applied); n2 dead rng_master (removed); n3 the LOG compressed the age-instance closed form (forward-noted); n4 ">= 7" is the conservative candidate cut (a non-parent-child pair needs >= 8) — next-touch; n5 "old-pair interact" now explicit in the proof.

## SURVIVES ATTACK
D31A re-derived and closed for all u (pair exhaustiveness; adjacent-transposition reduction valid — op multiset <=> final web; commutation canon-verified incl. parent-sharing pairs; opportunity counts = D28b grammar, cross-checked via Phi(seed,T=3) = 256); the front-(e) escape CLOSED (within-class uniformity is forced in the graph-blind class — label-covariance bars labels, blindness bars structure; label-covariance ALONE would not force it). B1 verified by an independent third construction on the battery + 200 adversarial random op sequences; B2's E5 numbers re-derived analytically (net = controlled(-I): I_end exactly 0, I_mid = (24/25)^2). C1's iff exact; C2's closed form recounted (+1 f_b, +2 f_i1, +2 f_i2); parallel edges legal; d_cover invariant under parallel edges and leaf births. Number fidelity: everything traces except the M1 census (fixed) and n3. DISCIPLINE: pin-before-run; D32 held; V9/D12 clean; the frozen order consistent with D30. THE #138 FOLD-IN: all six arc-delta residuals verified genuinely applied — the canon upgrade is real (the old canon FAILS the prescribed rename test; Phi tables byte-equal old-vs-new on the enumerated domain at T = 2, 3); the d29 card freeze held byte-identically across environments (numpy 1.23.5).

## OPENINGS
O1 the B1 theorem (recorded in the note §5 addendum). O2 the unified positivity-collapse lemma (A3 + bounded-growth + none-arm = one phenomenon: unboundedly growing per-class opportunity counts kill interactions under covariance) — the honest input to D32's covariance column. O3 family (ii)'s u <= 5 elimination (the named debt). O4 D31A relabeled THEOREM grade (applied). O5 the determinized 30+-seed census with spread before D32 consumes the exception bookkeeping. O6 the F4 transitivity exhibit (owed).
