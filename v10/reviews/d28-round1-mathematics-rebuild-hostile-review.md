# HOSTILE REVIEW — MATHEMATICS/REBUILD stream: v10 D28 (round 1)

**Reviewed:** 2026-07-12. Objects: note-d28 (pin 1fe7aa9), d28_influence_diamond_exact.py + .out (8/8), d28_opportunity_kernels_exact.py + .out (10/10), the LOG D28 entry. Rebuild artifacts (scratch): pure-state Fraction engine with swap-in ancilla replacements + reverse-sorted schedules (Engine A); complex Gaussian-rational density engine with channel interventions and Y-queries (Engine B); independent combinatorics enumerators — all green.

## 0. Provenance and rerun
Pin-before-run VERIFIED (1fe7aa9 note-only -> 30ad855 receipt1+out -> 94cf90c receipt2+out+LOG+paper-19 F12). Note byte-unchanged since pin. Both receipts rerun: 8/8 and 10/10, exit 0, outputs byte-identical; the as-run receipt-1 code reproduces its output byte-for-byte. One blemish: 94cf90c edits receipt-1 code after its run commit (dead-line deletion; verified behavior-neutral) — m6.

## 1. Clean-room rebuild — everything numeric reproduces exactly
210 pairs/19 trees (T1); I(A->X)=102544/390625, I(B->X)=212544/390625; ledger 192/625 -> 1344/15625; synergy 16/25 vs 468/625; P(X=1|1,1)=16/25=sin^2(thA+thB) (3-4-5 o 7-24-25 = 4/5 by hand); additive 976/625; 3 schedules (exhaustive over 24); 48 extensions, no 2-realizer; separating probability 2/405 (hand: two depth-1 birth nodes each carry conditional beyond-collar mass 1/81, reached w.p. 1/5 each; node normalization 8+1/10 checked). Every number in LOG/note traced.

## MAJOR

**M1 — N10b's "remaining pattern structural per the T1 theorem class" cites a tree theorem for a non-tree web.** T1 is proven for pure birth forests; the S_3 web has three interactions. Of 30 ordered pairs the receipt verifies 6 interventionally (with a 3-element SUB-alphabet); the other 24 rest on the citation. The zeros decompose into three classes: (i) wire-time support zeros (genuinely T1-class), (ii) blank-target zeros, (iii) REAL-FAMILY TRACE-IDENTITY zeros (b->a kickback rows: <w|Ry|w> = cos(theta) for every real w; tr[Ry X] = tr[Ry Z] = 0) — class (iii) is NOT T1-class and is exactly the class that fails under enrichment (M2). THE CONCLUSION SURVIVES: the rebuild gates the FULL 30-pair matrix at the complete 5-element pinned alphabet — equals S_3 exactly. **Correction:** add the full-matrix gate to the receipt (<1 s) and reword note §4(ii)/N10b print to "all 30 ordered pairs gated interventionally at the pinned alphabets"; if spot-verification is kept, replace the T1 citation with the three named lemmas.

**M2 — the closure-equality (T3a) and the "operational influence order" are real-family-relative in a direction §2's monotonicity remark hides; ANSWERS pinned F1 with exact witnesses.** Enrichment creates BACKWARD target->control edges through every interact gate (phase kickback; Ry nondegenerate): diamond I(X->B) = 9216/15625 under y+/- replacements + Y-query; a unitary S at X (an unconditioned TP CHANNEL intervention) shifts B's Y-marginal by 1769472/9765625; S_3 web I(b1->a3) = 288/625 — a 2-cycle, so the enriched relation is NOT a partial order. "Enrichment can only enlarge" is true for the RELATION but fatal for the ORDER reading that the dimension triage and D29/D30 instruments rest on. Meanwhile the zeros the structure needs are STRONGER than gated: A||B and every ->R row survive the full channel battery (complex replacements, unitaries, dephasing, amplitude damping) with full Z/X/Y tomography. **Corrections:** (a) §3 T3: append "at the pinned alphabets" to the closure claim; record the X->B witness as the complex-extension fact; (b) §2: after "can only enlarge," add "and can destroy antisymmetry — the (A,Q)-relative relation is an order only at alphabets where the kickback identity tr[Ry X] = tr[Ry Z] = 0 protects the backward entries"; (c) pin for D29/D30: instruments must fix the alphabets or measure the channel-robust subrelation.

## MINOR
m1 T3 conjunct/gate mismatch ("D durable" ungated/vacuous; the NSE-ledger conjunct lives in receipt 2) — add a durability print or strike; point the NSE conjunct at receipt 2. m2 S_3 spot checks use 3 settings not the pinned 5 and never intervene at b-sites — fixed by the M1 full-matrix gate. m3 T3c "nontrivial" overstated: schedule-varied pairs all commute; non-commuting pairs are causally order-pinned — reword. m4 name collision: the note's diamond apex D vs receipts' X (and D is a census label) — rename to X. m5 T0 is engine-tautological (w1 and w2 execute the same ops); the general A0a factorization is a correct standard theorem — gate a generic isometry's U∘J factorization or relabel "engine consistency exhibit". m6 the post-run receipt edit (94cf90c) — rerun-and-recommit with code changes, or never touch frozen receipts.

## NIT
n1 §4 "uniform over {∅} ∪ eligible" omits births. n2 N4 is implied by N1 (Ionescu–Tulcea) — label as implementation check (answers F4). n3 d_cover routes through sealed vertices (moot here; flag for sealed hubs). n4 §2's "joint product queries admitted where a gate says so" — no gate uses one. n5 receipt-2 never prints the spot-check settings.

## SURVIVES-ATTACK (with proofs found)
- T1's zero side is a THEOREM for every TP intervention (the wire-time lemma: Heisenberg support of O_v pulled back grows a connected subtree using only post-intervention edges; every path into u's subtree uses u's earlier birth edge; TP duals fix the identity). Numerically: 124 non-descendant pairs x 8 channel classes x full tomography, all invariant.
- T2's combinatorial core = one-line theorem (unique parent => unique root path); the operational no-go is ALPHABET-ROBUST (no TP intervention evades it).
- T3: all numbers reproduce; A||B channel-robust with full tomography — stronger than claimed; the 3 schedules exhaustive; label-canonical equality the right invariant; strengthened: every influence-matrix entry is schedule-invariant.
- N10a: the general mirror-DFS lemma is TRUE — F2 CLOSES AFFIRMATIVELY (proof: ancestor precedes subtree in every preorder; incomparables' LCA-children subtree blocks are contiguous, sigma-ordered in L1, reverse in L2 — exactly one flips; forests via reversed concatenation). Adversarially verified on 114 shapes (chains-200, star-201, caterpillar, binary-255, brooms, 60 random, 30 forests, census). The M4 exclusion then rests on the corpus S_n theorem [P].
- N10b refusal: 48 extensions recounted; no 2-realizer over 48x48; a 3-realizer exists => dim(S_3) = 3 exactly; the operational pattern full-matrix-verified at the pin (M1).
- N9: 2/405 by hand + enumeration; depth-1 collar/tail coincidence (separation purely depth-2); {Z,X}-completeness for the real family genuinely true; every in-play state real at the pin.

## OPENINGS
O1 promote the two robustness theorems (wire-time lemma; kickback dichotomy) into the record — they convert alphabet-relative statements into the theorem/exhibit split D29/D30 needs. O2 kickback as physics: characterize which interaction gates keep the influence relation an order (degenerate-spectrum controls; tr[U sigma] = 0) — the natural F1-closure receipt. O3 free exact number: K_tail(1/10) separating probability = 2/805. O4 F3 needs a design argument, not a rerun (N6 true by construction). O5 literature pins left to the physics stream.

**Bottom line:** no false exact result. Both MAJORs come with drop-in corrections; the conclusions were re-proven at full fidelity by the reviewing engines.
