# D34a ROUND-1 HOSTILE REVIEW — fresh referee, clean-room rebuild — 2026-07-13

**Target:** `code/d34a_harris_lemma_exact.py` + `data/d34a_harris_lemma_exact.out` (6/6) at bead278 — THE CENTRAL RECEIPT of the history-law phase: the first claimed lawful interacting history measure. Pins: note-d33 §2 (34e68e2, the seven success conditions + the Harris route + HF fronts); the adopted proposal `reviews/d33-external-proposal-history-law.md`; the D33-adjudicated conventions (`code/d33_gauge_reaudit_exact.py`, TERMINAL at #156); LEDGER #152/#154–#157; LOG 2026-07-13 D34a entry.
**Method:** the receipt was exec'd in-process with captured stdout (byte-compared to the committed .out) and its machinery rebuilt independently: an independent event poset (shared-register direct edges + Floyd–Warshall closure) and an independent, wire-local conditional (the collar built from the initiator's own wire history only), compared factor-by-factor on every enumerated history; the REAL linearization test the receipt does not run (resequence the act list along each extension, recompute every factor); an extended canon (the D33 construction lifted to the 'n'-op grammar, which D33's own canon cannot parse) for typed-causal-history grouping; **the exact race pushforward at depth ≤ 3** (all actor sequences and idle placements, exact rationals) confronted with μ class-by-class; the systematic remote-cluster sweep (3 cluster types × every history × every event); the lottery contrast re-derived by hand and against D30's committed conventions; reruns under PYTHONHASHSEED 1/99 (the LOG's salts were 0/17). Referee harness: `scratchpad/d34a_referee_harness.py` (session scratchpad; 17/17).

**VERDICT: MAJOR-REVISION — 3 MAJOR / 6 MINOR / 3 NIT. Zero gate flips; zero false printed numbers (2,872 / 1,448 / 109-111-180 / 1/8 / 1/320 / 1/440 / 1/8 → 1/11 all reproduce exactly); one false printed sentence (H2's restriction identity). The per-event local conditional q is genuinely well-defined on causal pasts (verified factor-by-factor by independent rebuild, 19,054 factors), and the REAL invariance content — μ constant across resequenced linearizations and across the relabeling gauge — is TRUE (verified here: 10,608 resequencings; 2,926 canon classes, all μ-constant). But the receipt's headline object does not survive at its printed width: μ = Πq is NOT a measure on the receipt's own space of typed causal histories (Σμ over depth-1 histories = 2; flat restriction fails by the exact factor |present| at every one of 451 histories; canon-DISTINCT reception placements each carry the full decision mass), the H1 gate is computationally vacuous as coded (the receipt's own global lottery — the object H5 convicts of non-invariance — passes it 2,872/2,872), and the race does not sample μ (exact pushforward at depth ≤ 3: equal-μ classes carry race masses in ratios 1/4 : 1/6 : 1/3; the race is not even linearization-covariant — 1/2304 vs 1/3072 on H5's own history). What exists at receipt grade is the conditional-decision HALF of a Harris factorization: a covariant, local, per-record-consistent weight system. The measure half — the clock/placement law that would make it a probability law on 𝓗 and that the pin's own Harris sentence names — is unbuilt, and #157's "a lawful ... history measure EXISTS" must be forward-corrected.**

---

## THE CENTRAL ADJUDICATIONS (as put to this round)

1. **Is μ(H) = Π q(e | causal-past(e)) well-defined? YES — and by a stronger, unstated lemma.** `event_poset` (lines 38-48) equals brute-force shared-register transitive closure on all 4,891 enumerated histories; `past_state`/`q_local` (50-76) were rebuilt independently with the conditional computed from the initiator's OWN wire history alone, and every one of 19,054 factors agrees exactly. The subtle spot the round flagged — an interact event's pred set imports the PARTNER's wire past — is real but benign, by **collar-locality**: A[y] changes only through births with parent or child = y, both events on y's own wire, so the eligible set is a function of y's causal cone regardless of what else pred feeds in. This lemma is load-bearing three times (well-definedness; the options_for/q eligible-set agreement in H2; H4's "by construction") and is stated nowhere (m3). μ is also constant on canon classes including relabeling (2,926 classes) — condition (1)'s relabel half, which no receipt gate tests.
2. **Probe 2 (top priority) — does the simulator's law equal μ? NO, decisively; and H1 is tautologous as computed.** `mu_along(acts, order)` (78-85) evaluates q_local(acts, pred, j) — a function of the LIST, never of `order` — so the product over any permutation of the same factor multiset is identical by commutativity of rational multiplication; the H1 "extension products" check (134-139) and the "factor-level" check (141-149, which permutes the ORDER argument, never the list) verify nothing about locality: **the global lottery passes the H1 gate as coded on all 2,872 multi-extension histories** while its genuinely resequenced products differ (1/320 vs 1/440) — the receipt's own H5 counterexample sails through its own H1 (M2). And the bridge from the race to μ is absent and FALSE: the exact race pushforward (all actor sequences and idle placements, exact rationals, total mass 1 at each depth) gives, on four canon-distinct depth-2 histories with μ = 1/16 each, race masses 1/64, 1/96, 1/48, 1/96, and on the two μ = 1/4 idle classes 1/16 vs 1/8 — 5 distinct race/μ ratios at depth 2, 16 at depth 3; no normalization reconciles them. Worse, the race's own path probabilities are not linearization-covariant: hc and hd — the receipt's OWN H5 pair, canon-same, μ = 1/128 each — get race paths 1/2304 vs 1/3072 (ratio 4/3); 42 classes at depth 3 (2 already at depth 2, e.g. n(A) ⊔ b(B,z1): 1/32 vs 1/48) have unequal path probabilities across their own gauge-equivalent linearizations. The census denominator deleted from q re-enters through the actor layer as 1/|present| — the H5(b) disease relocated, not removed (M3). μ is recoverable from the race only as the CONDITIONAL law given the actor sequence — and the actor sequence is not gauge.
3. **H2: arithmetic right, identity false.** The per-record sums are exactly 1 in all 1,448 cases (recounted), and there are no phantom or missing options — options_for's eligible set equals the causal-cone eligible set at every (history, record) by collar-locality. But the printed "(restriction)_* mu_{n+1} = mu_n at this depth" is FALSE on the pinned space 𝓗_n: the pushforward of μ_{n+1} under drop-one-event is |present(h)|·μ_n(h) — computed exactly at every one of the 451 histories of depth ≤ 3, factor 2 to 5, never 1. Full restriction-consistency would require Σ over ALL one-event extensions (all records) = μ_n(h); it does not follow and is exactly violated. What is gated is the per-record telescope — genuine, and it Kolmogorov-extends the PRODUCT measure on per-record decision trees — but that space forgets reception placements, which D33 certifies as physical (M1).
4. **The idle convention: locally clean, globally infected by the same defect.** q is step-count-free (the independent rebuild ignores step counts and matches everywhere; past_state's docstring promises a step count the code never computes — m4), so idles change nothing in any conditional and per-record idle marginalization is a clean geometric telescope (act-level conditionals 1/2 birth, 1/2 interact-total). But histories differing in an idle placement relative to a cross-wire reception are canon-DISTINCT comparable refinements ([n(B), i(A,B)] vs [i(A,B), n(B)]: μ = 1/8 EACH, shared decision content 1/8 total), so the act-history object inherits M1's non-additivity under idle marginalization; nothing about idles per se is at fault (m5).
5. **H4/H5 exhibits: arithmetic and structure all verified; H4's universal earned by this round's sweep, not by the receipt.** q_before = q_after = 1/8 with eligible {B, z1} — recomputed; the remote cluster is provably outside the interact's pred (verified). The systematic sweep (3 cluster types including a remote interact and a lone remote idle × every history × every event): 18,618 causally-unrelated cases, conditional exactly unchanged in ALL; 38,544 related cases (4,576 change, all via the initiator's own legitimately enlarged collar). H5: 1/320 = 1/5·1/8·1/8 and 1/440 = 1/5·1/8·1/11 re-derived by hand (w_tot = 5, 8, 8 and 5, 8, 11); 1/8 → 1/11 likewise; hc/hd are canon-SAME under the verbatim D33 canon (computed, not assumed) and their swapped pair (events 1, 2 of hc) is register-disjoint hence incomparable; μ(hc) = μ(hd) = 1/128 — the equality H1 should have exhibited and did not. lottery_prob is faithful to D30's committed conventions (`code/d30_frozen_cone_comparison.py` 47-60: w_none = 1, w_birth = U, w_collar = |argwhere(D == 1)| = ordered adjacent unsealed pairs; sealed = {R} only, interacts never seal; collar interacts distance-inert, so the static-A evolution is right), with two scope notes owed (m6): the tail sector (w_far) is omitted — the exhibit convicts the collar member of the lottery class — and D30's 'none' is a global register-free op where d34a prices per-record idles (hc/hd are idle-free, so H5 is unaffected).
6. **Scope honesty: no unearned projective-limit claim; the unearned words are "measure" and "exactly (2)".** H2/H6 are depth-scoped and the Kolmogorov step is nowhere claimed — correct. But H6's "(1) ... RECEIPT-GRADE at depth ≤ 4" rides on a vacuous gate; "(2) ... exact" is the per-record surrogate; #157's headline "a lawful, consistent, ... history measure EXISTS" and the LOG's "conditions (1)(2)(3)(4)(6) receipt-grade" overstate all three ways (m1). HF3 is disclosed at H3; **HF2 is not discharged or even cited at the only site using the race** — the pin says the discrete receipt "must pin a tie convention and show it is gauge"; the receipt's tie-resolution IS the uniform actor draw, and probe 2 shows it is NOT gauge (folded into M3). Persistence itself is safe: eligible is never empty at this scope, so every record's step law is exactly (1/4, 1/4, 1/2) i.i.d., and the race's 109/111/180 is within 2σ of the exact (100, 100, 200) — H3's trend claim survives independently of the actor law.
7. **Code/reproducibility: clean.** Hash-salt audit: every set that feeds output or sampling is sorted or reduced to len/membership (options_for sorts eligibility; lottery's pairs list is used only via len); the enumeration frontier's LIFO pop affects only listing order and nothing samples from it (H1/H2 iterate exhaustively); the race is seeded numpy with deterministic option order. Rerun byte-identical in-process and via subprocess; byte-identical under PYTHONHASHSEED 1 and 99; 2.1 s. D33 lineage reruns 6/6 byte-identical. Every printed number recomputed exactly (see FALSE-RESULT COUNT).

---

## MAJOR

**M1 — μ = Πq is not a measure on the receipt's own space of typed causal histories; H2's printed restriction identity is false as written; the missing datum is the reception-placement (actor/clock) law.**
Exact computations (harness P4): Σμ over the receipt's depth-1 histories = **2** (= one normalized decision law per seed record); over depth 2/3/4: 9/2, 93/8, 1113/32 — not 1 at any depth and not a constant inflation. Flat restriction: Σ over ALL one-event extensions of h of μ = **|present(h)| · μ(h)** at every one of the 451 histories of depth ≤ 3 (factor 2..5) — so "(restriction)_* mu_{n+1} = mu_n at this depth" (.out H2; label lines 173-178) is arithmetically false on 𝓗_n, the space the pin defines the μ_n on and the space H1 enumerates. The mechanism is not normalization pedantry but a physical double-count: [i(A,B), b(B,z1)] and [b(B,z1), i(A,B)] are canon-DISTINCT (computed under BOTH the verbatim D33 canon and the extended canon — the reception lands before vs after B's own birth on B's wire, exactly the comparable order D33 G1 certifies as recorded physics), their per-record decision content is identical (A's first decision: interact-with-B; B's first: birth), and μ assigns EACH the full decision mass 1/16. Mutually exclusive physical refinements of one decision event carry no subdivided probability — μ prices decisions and leaves placements unpriced. What IS true and gated (1,448 sums): the per-record telescope, i.e. μ is a genuine product probability law on per-record decision sequences, and on THAT space Kolmogorov extension is immediate. But that space cannot even express the placement events D33 declares physical. Full restriction-consistency on 𝓗_n would require an actor law α(y|h) with Σ_y α = 1 woven into the measure — and any such normalized α is a universe-wide denominator unless the clock half of the Harris construction (fixed-TIME, not fixed-depth, marginals; memoryless local clocks, under which the A-vs-B first-ring order is 1/2 : 1/2 independent of remote records) is actually built. Repair: restate the object honestly (a covariant local weight system / the conditional-decision half of a Harris factorization; a probability measure on per-record decision arrays); correct H2's sentence to the per-record telescope + print the computed |present| pushforward as the disclosed failure of flat restriction; pin the placement law as d34b-or-later's explicit target; forward-correct #157 (see m1).

**M2 — H1's gate is computationally vacuous, both halves: it verifies commutativity of multiplication, and the receipt's own convicted lottery passes it.**
`mu_along(acts, order)` (78-85) computes pred = event_poset(acts) and multiplies q_local(acts, pred, j) — no quantity depends on `order` except the multiplication sequence, so {mu_along(h, e) for e in exts} is a singleton for ANY factor function of (acts, j) whatsoever; likewise fac_ok (141-149) permutes the ORDER argument and compares the same product to itself. Demonstration (harness P3): substituting the receipt's own `lottery_prob` — the global-census object H5 proves is NOT linearization-invariant (1/320 vs 1/440) — into the H1 loop yields "extension-products singletons" on **2,872 of 2,872** histories. The 2,872 PASSes therefore carry zero evidential weight for "each event's conditional depends on its causal past only", and the .out's "with the #156-swept transposition lemma this closes full linearization invariance" claims a closure whose load-bearing middle premise (factor invariance across RESEQUENCED lists) the receipt never computes; the pin's own spec — "(i) ... linearization-independent (computed two ways, equal)" — is not implemented. The CONTENT is true and is now certified by this round's rebuild: resequencing the act list along every linear extension and recomputing every factor gives equal μ in all 10,608 cases, canon preserved; and μ is constant on all 2,926 canon classes (908 multi-member), which also delivers the untested relabel half of condition (1). Repair: replace H1's body with the resequence-and-recompute gate plus canon-class constancy (the canon must first be extended to the 'n' grammar — D33's `canon_history` indexes op[2] and cannot parse idle events; the receipt currently computes NO canon at all, making the enumerate docstring's "gauge-equal ones are identified by the measure gates" (113-115) false — nothing identifies them).

**M3 — The race does not sample μ, uniform-actor choice is not gauge, and the race itself is not linearization-covariant; HF2 is undischarged at the only site that uses the race.**
Exact pushforward of the race (all actor sequences and idle placements, Fractions; total mass 1 at each depth — harness P5): at depth 2, the four canon classes with μ = 1/16 carry race masses 1/64, 1/96, 1/48, 1/96 (reception-first chain, birth-first chain, two-parent antichain, same-parent chain); the two μ = 1/4 idle classes carry 1/16 vs 1/8. Five distinct race/μ ratios at depth 2, sixteen at depth 3 — the race's law on typed causal histories is not μ, not proportional to μ, and not a function of μ. The line-193 comment "# gauge choice only" is false: the actor draw sets the RELATIVE probabilities of canon-distinct (physical) histories — μ says 1:1 on the placement pair, the race says 3:2 — and the race splits the shared decision mass 1/16 across the two placements as 1/32 + 1/32 where μ writes 1/16 on each. Deeper: the race's own path probabilities differ across gauge-equivalent linearizations of ONE typed history — on the receipt's own H5 pair, P_race(hc) = 1/2304 vs P_race(hd) = 1/3072 (ratio 4/3; μ = 1/128 both) — 42 classes at depth 3, 2 already at depth 2 — i.e. the exhibited simulator fails Rideout–Sorkin discrete general covariance, the very property the phase demands, because 1/|present| is a global census factor: the denominator deleted from q was reinstated in the actor layer. Consequently H4's "no global size anywhere" is true of q and false of the only realization process in the receipt, and the pin's Harris sentence ("the causal-history law of a race of local exponential clocks depends only on local rates") is neither implemented (the discrete jump chain is not the exponential race: fixed-depth actor odds are census-diluted, while the exponential race's A-vs-B first-ring odds are 1/2 : 1/2 independent of bystanders by memorylessness) nor tested. HF2's pinned obligation — "the discrete receipt must pin a tie convention and show it is gauge" — is exactly this defect and is nowhere discharged or disclosed. Repair: either build the clock half (continuous-time/holding-time-weighted race; fixed-time marginals; show ITS causal-history law is local and covariant — the true Harris lemma, still unproven here) and derive the measure as clock ⊗ decision, or demote the race to "one auxiliary sampler, actor law declared and measure-relevant" wherever it appears; add the exact depth ≤ 3 race-vs-μ confrontation as a receipt gate so the gap is owned, not implied away. Note what survives: H3's persistence numbers are actor-law-insensitive (per-step decision law exactly (1/4, 1/4, 1/2) since eligibility is never empty at this scope), so the MEASURED trend stands.

## MINOR

**m1 — Grade inflation in H6/#157/LOG; forward-correction owed at #158.** H6 prints condition (1) "RECEIPT-GRADE at depth ≤ 4" (vacuous gate, M2) and (2) "exact" (per-record surrogate, M1); #157 declares "passes success conditions (1)(2)(3)(4)(6) EXACTLY" and headlines "a lawful, consistent, persistent, uncapped, local interacting history measure EXISTS"; the LOG echoes "receipt-grade". Honest scorecard after this round: (1) TRUE, certified by the referee's rebuild, not by the receipt's gate; (2) per-record consistency exact, 𝓗_n-restriction exactly false; (3) exact at law level and measured; (4) exact for q; (6) exact for q, false for the exhibited sampler; (5),(7) open as stated. The #154 → #155 forward-correction precedent applies verbatim.

**m2 — ok3a is the D33-M3 costume, second appearance of the defect class.** Lines 184-185: `all(q_local(...) == F(1,4) for _ in [0])` — one factor of one history, dressed as "at every local step of every record ... BY CONSTRUCTION". The claim is true and cheaply gateable: eligibility is never empty at this scope, so birth/interact-total/idle masses are exactly (1/4, 1/4, 1/2) at every step — the harness's factor census over all 19,054 factors (births 8,540 all exactly 1/4; interacts 3,580 × 1/4, 2,000 × 1/8, 258 × 1/12, 8 × 1/16; idles 4,668 all exactly 1/2) is the missing gate; adopt it. Disclose that "with a partner" is vacuous at this scope (the 3/4-idle branch, line 76, and the q = 0 phantom guard, line 73, never fire — unexercised, not dead-by-error).

**m3 — The collar-locality lemma is load-bearing three times and stated nowhere; H4 is a one-pair exhibit for a universal claim.** The lemma (A[y] changes only via births with parent or child = y, hence the eligible set is a function of the initiator's own wire history): it is why q is well-defined although pred imports the partner's past, why options_for's full-graph enumeration cannot produce phantom or missing options (H2's soundness), and why H4's "by construction" is true. State it in the receipt header or note-d33 §2 and adopt the systematic sweep (3 cluster types × every history × every event; 18,618 unrelated cases exactly unchanged) in place of the single pair.

**m4 — False in-code documentation (the D33-m6 class).** (i) past_state's docstring (50-53) promises "(adjacency ..., y's active-step count)"; the function returns adjacency only and no step count exists anywhere in the receipt — either the docstring or the intended law is wrong; as coded the law is step-count-free (verified: the independent step-free rebuild matches all 19,054 factors). (ii) enumerate_histories' docstring (113-115): "gauge-equal ones are identified by the measure gates" — no gate identifies anything (no canon is computed in d34a; M2's repair supplies one). (iii) line 193 "# gauge choice only" — false (M3). All three sit in the phase's central receipt.

**m5 — The idle adjudication (probe 4): per-record clean, cross-record infected.** Idles change no conditional (q is step-free) and per-record idle marginalization telescopes to act-level conditionals (1/2, 1/2) exactly. But idle placements relative to cross-wire receptions are canon-distinct comparable refinements sharing M1's defect ([n(B), i(A,B)] vs [i(A,B), n(B)]: μ = 1/8 each on identical decision content), so the act-history object obtained by summing over idle placements inherits the non-additivity; the pin's "remaining idle is a local click decision" is honored per-record and its cross-record price is unpaid. One disclosure clause owed wherever the act-level measure is discussed.

**m6 — H5 fidelity scope notes.** The lottery matches D30's committed conventions (ordered argwhere pairs; w_none = 1; w_birth = U; no sealing; collar interacts distance-inert — d30 lines 47-60, 35, 83-93) — verified. Owed in the docstring: (i) the tail sector (w_far = ε q^d) is omitted, so the conviction is of the collar/birth member of the lottery class (the global-denominator disease is class-wide, but say which member was tried); (ii) D30's 'none' is a global register-free op ('n',), while d34a prices per-record idles ('n', y) at 1/w_tot — a grammar mismatch that H5's idle-free exhibits never touch (disclose). The comment "pure-birth swaps leave the lottery's step weights unchanged" was verified true (pairs after k births = 2(k+1) regardless of tree shape), so the type-mixed exhibit was necessary and correctly chosen.

## NIT

**n1** — Fresh-label bookkeeping is linearization-dependent (z-names are assigned by global birth count at append time), so gauge-equal sequences carry different labels; harmless under canon, but the receipt never canonicalizes (cross-ref M2) — one comment line owed.
**n2** — Style: numpy imported mid-gate (186); the race's float() casts are sampling-only (several weights, e.g. 1/12, are non-dyadic — fine for a MEASURED gate, one comment owed); the `x <= c` cumulative boundary is measure-zero for the rng — immaterial.
**n3** — H6's "The engine sentence's positive half is now constructive (H5)" leans on "the local law passes both tests exactly (H1, H4)" — true in content, but the H1 half is certified by nothing in the receipt until M2's repair lands; re-point the sentence at the repaired gate.

## SURVIVES ATTACK

- **The local conditional q and its causal-past definition:** independent poset (FW closure) identical on all 4,891 histories; independent wire-local q identical on all 19,054 factors; collar-locality proven and exploited; no phantom/missing options in any of the 1,448 record-extensions.
- **The REAL condition (1), certified by this round:** resequence-and-recompute along every linear extension — 10,608 cases, μ invariant, canon preserved; μ constant on all 2,926 canon classes (relabel + linearization gauge), 908 multi-member.
- **H2's arithmetic:** all 1,448 per-record sums exactly 1 (the per-record telescope is real; it is the 𝓗_n label that is wrong).
- **H4's content:** the single pair verified (1/8 = 1/8, eligible {B, z1}) and the universal earned by the systematic sweep: 18,618 causally-unrelated (cluster, event) cases with the conditional exactly unchanged, across a birth cluster, a cluster with an internal remote interact, and a lone remote idle; 4,576 related cases change only via the initiator's own collar.
- **H5 in full:** 1/320, 1/440, 1/8 → 1/11 re-derived by hand and by code; hc/hd canon-same under the verbatim D33 canon; the swapped pair incomparable; D30 conventions matched; the lottery's linearization-dependence and remote dilution are genuine and correctly diagnosed — the negative half of the engine claim stands at receipt grade.
- **Persistence (H3):** per-step decision law exactly (1/4, 1/4, 1/2) at this scope, so the trend is actor-law-insensitive; 109/111/180 reproduces exactly under reruns and is within 2σ of the exact multinomial expectation (100, 100, 200).
- **Discipline:** pin 34e68e2 pre-committed; working tree clean at bead278; rerun byte-identical (in-process and subprocess); byte-identical under PYTHONHASHSEED 1/99 (LOG's 0/17 independently confirmed); D33 lineage 6/6 byte-identical; 2.1 s.

## GATES RE-VERIFIED

`python3 code/d34a_harris_lemma_exact.py` at bead278: **6/6 PASS as coded, 2.1 s, .out byte-identical to the committed artifact, and byte-identical across PYTHONHASHSEED ∈ {1, 99}** (the LOG's {0, 17} thereby extended). Lineage: `code/d33_gauge_reaudit_exact.py` 6/6, byte-identical. Referee harness 17/17 (rerun capture; independent poset + independent q; resequenced invariance; canon-class constancy; lottery-through-H1 vacuity; depth-1..4 mass sums; flat-restriction factor; placement pair; exact race law depths 2-3 with the exhibit table; race path-covariance failure; H2 recount; H1 count; remote sweep; H5 arithmetic + canon; factor census).

## FALSE-RESULT COUNT

**Printed numbers: 0 false** (2,872; 1,448; 109/111/180; 1/8; {B, z1} → 1/4 / 2; 1/320 vs 1/440; 1/8 → 1/11; 6/6 — every one recomputed exactly). **Gate flips: 0** (all six gates pass as coded — two of them, H1 and ok3a's half of H3, because they cannot fail). **False printed sentences: 1** — H2's "(restriction)_* mu_{n+1} = mu_n at this depth" (the exact pushforward is |present|·μ at every history; the gated telescope is per-record). **Verification-scope overstatements (content true, computed support nil): 2** — H1's "adjacent incomparable swaps change NO factor" and "this closes full linearization invariance" (both halves of the gate are order-of-multiplication identities; the lottery passes them). **False in-code comments: 3** — "# gauge choice only" (193); past_state's step-count docstring (50-53); "gauge-equal ones are identified by the measure gates" (113-115). **Misgrade-class:** H6's "(1) RECEIPT-GRADE / (2) exact", #157's "passes (1)(2) EXACTLY" and "history measure EXISTS", the LOG echo. No printed number is wrong; the receipt's arithmetic layer is clean and its claim layer is not.

## OWED REPAIRS (exact list)

1. **(M1)** Restate the object at its earned width: μ = Πq is a covariant local weight system = the conditional-decision half of a Harris factorization, and a probability measure on per-record decision arrays; NOT yet a measure on typed causal histories. Fix H2's sentence to the per-record telescope; print the computed flat-restriction factor |present| as a disclosed negative; pin the reception-placement/actor law as the named missing datum (d34b or a dedicated receipt).
2. **(M2)** Replace H1's body: resequence the act list along every linear extension and recompute every factor (equality of products AND factors); add canon-class constancy (extend the D33 canon to the 'n' grammar first — it currently cannot parse idles); gate the negative control (the lottery must FAIL the repaired gate).
3. **(M3)** Add the exact race-vs-μ confrontation at depth ≤ 3 as a gate that owns the gap (race pushforward ≠ μ; race path probabilities not covariant); delete or correct "# gauge choice only"; discharge HF2 explicitly (the actor draw is the tie convention and it is NOT gauge); pin the clock-half construction (exponential race, fixed-time marginals) as the route to an actual measure.
4. **(m1)** Forward-correct #157 at #158 (headline "measure EXISTS" → the earned object; scorecard grades per this round) and the LOG echo; re-grade H6's lines.
5. **(m2)** Replace ok3a's costume with the factor census gate (all birth factors 1/4; interact totals 1/4; idles 1/2 at this scope); disclose the vacuity of "with a partner" here and the unexercised 3/4/q=0 branches.
6. **(m3)** State the collar-locality lemma once, where q is defined; adopt the systematic remote sweep into H4.
7. **(m4, n1-n3)** Fix the three false comments/docstrings; one line each on fresh-label gauge, the float casts, and the numpy import; re-point H6's "constructive" sentence at the repaired H1.
8. **(m5, m6)** One clause on idle-placement non-additivity at the act level; the two H5 fidelity disclosures (tail sector omitted; global-'none' vs per-record idle).

## SUMMARY JUDGMENT

The receipt's genuinely new and correct content is the LOCAL LAW: a per-event conditional depending on the event's causal past alone — well-defined by collar-locality, exactly normalized per record, exactly size-free, exactly insensitive to causally unrelated additions, and (certified by this round's rebuild, not by the receipt's own H1) constant across linearizations and relabelings of the typed causal history. The lottery contrast is real and survives everything thrown at it: the old architecture is not a law on causal histories, and deleting its global denominator produces covariant local weights. But the phase's central claim — the first lawful interacting history MEASURE, the object the simulator samples — is not what was verified: H1 as coded proves commutativity of multiplication (the receipt's own convicted lottery passes it), H2 proves per-record consistency while its printed 𝓗_n-identity is exactly false (factor |present| at every history), and the exact depth ≤ 3 pushforward shows the race realizes a different, non-covariant, census-dependent law — the global denominator crept back in through the actor draw, and reception placements (physical, per D33's own G1) carry unpriced probability under μ. Probe 2's verdict: **the central object does not survive as claimed** — what exists at receipt grade is the decision half of a Harris factorization; the clock/placement half, which is what would make μ a measure on typed causal histories and what the pin's Harris sentence actually promises, is unbuilt, unpriced, and now precisely specified by the failure exhibits. That is a strong, publishable partial result wearing the wrong headline; the repairs are one honest restatement, two real gates, and one LEDGER forward-correction — and the mathematics they would buy (the exponential-clock pushforward and the NSE/locality tension at HF5) is exactly where the phase said the interesting mathematics lives.

---

# DELTA VERIFICATION — round-1 repairs at c1f17b8 (same referee) — 2026-07-13

**Scope:** commit c1f17b8 only (receipt + .out + LEDGER #158 + LOG; this report committed verbatim, confirmed unchanged above this line). Method: in-process rerun byte-compared to the committed .out; every new printed number independently recomputed (`scratchpad/d34a_delta_probe.py`); the H1/H2/H2b/H4 repair loops re-executed with decompositions the receipt does not print; byte-check under PYTHONHASHSEED 5/77 (extending the LOG's 0/23); dropped-item scan against the round-1 owed list.

**VERDICT: NOT YET DELTA-CLEAN — 0 MAJOR regressions / 3 Δ-m / 2 Δ-n (→ #159). All three round-1 MAJOR repairs are FAITHFUL and verified exact; zero gate flips; zero false new numbers (10,608 / 4,900 / Σ = 2 / 1-2304 / 1-3072 / ratio 3-4 / 1-128 / 278 all reproduce); the race numbers and all round-1 arithmetic unchanged. The residuals are the round-1 defect classes recurring inside the repair: one headline noun the receipt's own gates now contradict, one sweep whose "causally unrelated" label is false for 74 of its 278 cases (they pass by the stronger, still-unstated collar-locality lemma), and five round-1 m/n items dropped without a recorded disposition.**

## Item-by-item (as tasked)

**(1) The REAL H1 — FAITHFUL, count-exact.** The gate now rebuilds the act list in each extension's order and recomputes the poset and every factor (`h2 = [h[k] for k in e]`; `mu_of(h2)`), exactly the round-1 repair spec. nres = **10,608**, matching my certification count exactly (same domain: all extensions incl. identity of the 2,872 multi-extension histories). The lottery as failing control: **4,900** verified exact; reconciliation with my exhibit set: the 10,608 split as 2,872 identity + 7,736 non-identity resequencings; all 4,900 failures are non-identity; the other 2,836 non-identity cases preserve the lottery product (the count-invariant classes — pure-birth/pure-idle mixes whose w_tot sequences match, consistent with the verified "w_tot depends only on counts" fact); 2,268 of the 2,872 histories carry ≥ 1 failing extension. The vacuous draft gate is disclosed in the check text. The gate now has real discriminating power and its negative control fails in-receipt — round-1 M2's core repair, landed.
**(2) M1 repairs — FAITHFUL; #158's width is right; the receipt's own headline is not (Δ-m1).** The restriction identity is WITHDRAWN with the withdrawal printed; the flat pushforward = |present|·μ is gated over the 51 histories of depth ≤ 2 (recomputed: all exact; my ≤ 3-event verification review-carried); Σμ over depth-1 = 2 printed and gated; the per-record telescope retained (1,448); "the covariant WEIGHT SYSTEM (the decision half of a Harris factorization), not yet a measure" is the exactly right width, and #158/LOG carry it with correct referee attribution (19,054 / 2,926 / 18,618 all quoted accurately) and the correct d34b re-scope. BUT the receipt's docstring (line 7) and the .out's first line (the line-97 banner) still read "THE OBJECT: the product-of-local-conditionals **measure** on typed causal histories" — the noun H2 and H6 in the same file now withdraw. One-word repair at two sites; false-as-written at the receipt's most visible line (Δ-m1).
**(3) M3 repairs — FAITHFUL; the pair-level gate ADJUDICATED SUFFICIENT.** rc = 1/2304, rd = 1/3072 recomputed exactly from the committed acts (the printed ratio 3/4 is rd/rc — consistent with my round-1 rc/rd = 4/3); μ = 1/128 both; the relocated-denominator diagnosis, the fixed "# NOT gauge (H2b)" comment, and the HF2-undischarged sentence all present. Adjudication as tasked: the in-receipt pair gate suffices for the printed claims, because the receipt now owns a valid syllogism — the REAL H1 gates μ class-constant, H2b gates the race path law class-VARYING on the same class, hence the actor draw is not gauge and the race's path law ≠ μ. My depth-2/3 class-level pushforward tables (equal-μ classes at race masses 1/64 / 1/96 / 1/48; 16 distinct ratios at depth 3 — the fact that no normalization rescues the race even class-wise) may remain review-carried per the D33 Δ-m2 precedent; one pointer clause owed (Δ-n2), and "the race's induced law cannot equal any function of the causal history" should be scoped to the path law (the class-level pushforward is trivially a function on classes; what is proven in-receipt is path-level).
**(4) The minors — the sweep landed mis-labeled; five items silently dropped.** The systematic sweep is in-receipt (278 factors recomputed, all exactly unchanged — the gate is sound), and its size is adequate with my 18,618-case sweep review-carried and credited in #158. But its scope sentence is wrong: the filter excludes histories with op[1] ∈ {B, z8, z7} and op[2] ∈ {z8, z7} — **op[2] = 'B' is not excluded**, so interact-with-B histories pass the filter, and **74 of the 278 factors have the cluster IN their causal past** (exhibits: [('i','A','B')] event 0; [('n','A'),('i','A','B')] event 1 — the interact chains through wire B to b(B,z8)). The in-code "keep the cluster causally unrelated (A-side only)" is false for those, and the .out's framing presents all 278 as unrelated-cluster instances. The cases pass anyway by the STRONGER fact — collar-locality (eligibility is a function of the initiator's own wire history) — which is precisely the round-1 m3 lemma the receipt STILL does not state (Δ-m2). Dropped without recorded disposition (house style disallows silence): m2 (the ok3a `for _ in [0]` costume stands; no factor-census gate; the unexercised 3/4-idle and q = 0 branches undisclosed), m4(i) (**past_state's docstring still promises "y's active-step count" — a false in-code comment now surviving its second round in the phase's central receipt**), m6 (the two H5 lottery-fidelity disclosures: tail sector omitted; D30's global 'none' vs per-record idles), n1, n2 (Δ-m3). Cured or absorbed without separate action: m4(ii) (the enumerate docstring's "identified by the measure gates" is now half-true — the real H1 compares linearization-gauge pairs; relabel-gauge pairs still never compared, tighten when touched — Δ-n1), m4(iii) (fixed), m5 (absorbed by "reception PLACEMENTS are unpriced"), n3 (H5's "passes both tests exactly (H1, H4)" is now true with real support).
**(5) Rerun + numbers.** 7/7 PASS; .out byte-identical in-process and via subprocess; byte-identical under PYTHONHASHSEED 5 and 77; every new printed number recomputed exactly (10,608; 4,900; Σ = 2; 1/2304; 1/3072; 3/4; 1/128; 278); every round-1 number unchanged (109/111/180; 1/8; 1/320 vs 1/440; 1/8 → 1/11; 1,448); H6's honest scorecard and the H3 comment verified as claimed.

## Delta findings (→ #159)

**Δ-m1 (headline noun, printed):** docstring line 7 and .out line 1 still call μ "the product-of-local-conditionals measure on typed causal histories" — contradicted by the same receipt's H2/H6. Repair: "weight system" (or "weights") at both sites; the file's own H2 supplies the corrected noun.
**Δ-m2 (sweep scope false-as-written + the unstated lemma):** 74/278 sweep factors are cluster-RELATED (filter gap: op[2] = 'B' not excluded); the "causally unrelated" comment/framing is false for them. Repair (either): tighten the filter (exclude op[2] = 'B'; the sweep becomes all-unrelated) or re-word to the stronger true claim (204 unrelated + 74 related-but-collar-inert), AND state the collar-locality lemma in one sentence — it is the receipt's actual locality mechanism and is consumed silently by H2's options/q agreement, H4, and now this sweep.
**Δ-m3 (dropped dispositions):** record apply-or-defer-with-reason for m2, m4(i), m6, n1, n2. At minimum apply m4(i) — the one-line past_state docstring fix — a false in-code comment should not reach a third round.
**Δ-n1:** condition (1)'s relabel half is review-carried (2,926 canon classes), correctly attributed, with no in-receipt overclaim — acceptable; one pointer clause in H1's label would complete it; tighten the enumerate docstring when touched.
**Δ-n2:** one pointer clause in H2b for the review-carried class-level pushforward tables, and scope "induced law" to the path law.

## Conversion condition

Apply Δ-m1 (two one-word sites), Δ-m2 (filter fix or re-wording, + the one-sentence lemma), and Δ-m3 (dispositions; the docstring line at minimum); the Δ-n pointer clauses optional. Rerun with a two-salt byte check. On that rerun this review converts to **DELTA-CLEAN with no further round**: the three MAJOR repairs are faithful and verified exact (the REAL H1 with its failing control; the withdrawal + pushforward gate; the owned race confrontation with a valid in-receipt syllogism), #158's forward-correction of #157 is at exactly the earned width, and D34a closes terminal as THE WEIGHT-SYSTEM receipt — the covariant local weight system exists at receipt grade; the measure does not yet — with d34b (the exponential-clock measure-completing construction + full restriction + NSE + the quantum lift + HF2) correctly pinned as the next object.
