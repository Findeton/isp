# D15 ontology/physics review — round 2

**Date:** 2026-07-11  
**Review verdict:** **PASS FOR THE NARROWED FINITE DICTIONARY AND SCOPE LEDGERS**  
**Formal D15 verdict retained:** **`INCOMPLETE-INVESTIGATION`**

## Executive finding

Round 1 produced substantial repairs.

The regulated qubit witness now enters the actual D14 typed category.  Fresh
environment injection, CNOT interaction, explicit environment-to-record
commit, live-collar emission and a later protected future are composed as D14
`Obj`/`Mor` maps.  The target contains a sealed record with persistent identity
and a collar, and the later morphism is checked to preserve that record.

The action-weight layer is also more honest.  The Hadamard cell is now called
a local phase **plus supplied vertex measure**, while the CNOT support is
generated by an explicit finite `Z2` multiplier sum rather than written only
as a permutation.  A fixed-frame closed experiment distinguishes a
phase-modified alternative with probability `1/2` from the base value `1`, so
the receipt proves a physical difference while continuing to deny action
selection.

Most importantly, D15 does not promote the new typed success into autonomous
records.  The exact composition contains an explicit primitive named
`environment-to-protected-record`; the environment `|0>` state,
system/environment split, CNOT pointer interaction, trace/coarse graining,
commit interpretation and collar emission are supplied.  The action derives
the conditional unitary correlation and reduced decoherence once those data
are chosen.  It does not select a unique record ontology or universal seal.
S5 therefore remains correctly `ACTION-BRIDGE-CONDITIONAL`.

The new EFT parameter ledger and UV survivor audit repair most of the prior
scope omissions.  They state the derivative-expansion domain, operator-basis
equivalences, leading curved-space action, parameter classifications,
Planck-scale convention, all five candidate classes including PRIM, and the
difference between incomplete survival and selection.

No new major ontology/physics overclaim was found.  The exact witness remains
nongravitational and does not prove general covariance; S6 still lacks a
physical diagram-support/join law; the EFT domain remains schematic rather
than a numerical fit domain; and no frozen empirical fit or untouched
prediction exists.  The artifacts say these fields are absent or pending.
Consequently the finite cells pass, while the formal D15 investigation remains
incomplete.

## Exact reproduction

The repaired D15 source and frozen D13/D14 dependencies were copied to an
isolated temporary tree.  Normal and optimized Python produced byte-identical
stdout and byte-identical packets.  The regenerated packet matches the
primary artifact.

```text
checks                         = 26/26
normal stdout SHA-256          = 89bef6673905ceac8506cd2a6e9624a3265a31e087f7a69ead0f3d8a59152384
-O stdout SHA-256              = 89bef6673905ceac8506cd2a6e9624a3265a31e087f7a69ead0f3d8a59152384
generated JSON SHA-256         = d4ea94c6b727aa9ec7e20cad1304ad019e4711ffc1512d0f90532d75e01b3777
primary JSON SHA-256           = d4ea94c6b727aa9ec7e20cad1304ad019e4711ffc1512d0f90532d75e01b3777
semantic SHA-256               = f739c75352b8099b85836b4b7c471d131a173491217156e9d3da7d658ca1f1e3
source SHA-256                 = 78af9ed95a1ab5e27aa5edea2104c8f834688a1a1b3a06c6ef271b35afdb045a
D13 dependency SHA-256         = 1ea9969cb3e61b805e031ea7b3b025e3a1f88b56e268337824c6a6abcba1cf45
D14 dependency SHA-256         = e0db2c65a7305ca92bf19fcaa0384e0d825b1a04a5590c0ca243e6192bebc425
normal/-O stdout               = byte-identical
generated/primary JSON         = byte-identical
```

The packet ceiling remains explicit:

```text
finite Z2/qubit regional action; nongravitational;
does not select nature's action or establish generally covariant gravity.
```

## Round-1 closure ledger

| Round-1 opening | Round-2 adjudication |
|---|---|
| kernel relabelled as action | **SUBSTANTIALLY CLOSED for the finite witness.** Supplied measure is named and CNOT zeros come from an explicit multiplier sum.  No general action-selection claim remains. |
| D14 only hash-locked | **CLOSED.** Actual D14 objects/morphisms, protected record, collar and protected future compose. |
| record instrument called autonomous | **CLOSED BY CONDITIONAL GRADE.** Explicit commit, environment state/split and coarse graining remain supplied. |
| nongravitational toy used as EFT4/gravity dictionary | **CLOSED BY SCOPE.** The note calls it only a finite nongravitational S4 instance; continuum gravity remains formal/open. |
| no S6 ownership/diagram/global-clock law | **STILL OPEN, HONESTLY.** One disjoint tensor cell and a one-component D14 packet do not solve diagram generation or multi-component joining. |
| EFT domain/operator equivalence absent | **SUBSTANTIALLY CLOSED.** Schematic scale hierarchy, power-counting remainder and equivalence relations are stated; no numerical sector cutoff is claimed. |
| matter/parameter ledger absent | **CLOSED at classification scope.** Leading action, higher operators, neutrino caveat and parameter classes are explicit. |
| UV matrix incomplete/PRIM omitted | **CLOSED as a survivor audit.** All five classes appear and incompleteness is not confused with selection. |
| empirical fit/model-selection receipt absent | **STILL OPEN.** No coefficient dataset/likelihood/frozen uncertainty ledger is supplied. |
| 3+1/cone/units/`G` overread risk | **PASS.** Inputs and measured quantities remain separate from emergence. |
| V9 holdout risk | **PASS.** Holdout remains closed. |

## Finite action-weight layer

### Hadamard phase weight

The receipt now labels

```math
K_H(b,a)=2^{-1/2}\exp(i\pi ab)
```

correctly: `exp(i pi ab)` is the finite phase action and `2^-1/2` is a
supplied vertex measure.  This is a regulated zero-internal-label regional
weight.  It is not claimed to derive the measure from a continuum theory.

Some receipt labels still say “action-derived Hadamard” or “same local action”
where “declared finite action weight(s)” would be more exact.  The surrounding
source docstring, first check and packet ceiling remove the scientific
ambiguity, so this is nonblocking wording.

### CNOT constraint sum

The CNOT map is no longer merely typed in as four nonzero entries.  The source
evaluates two finite character sums,

```math
\frac12\sum_{\lambda=0}^1(-1)^{\lambda(b_c\oplus a_c)},
\qquad
\frac12\sum_{\lambda=0}^1(-1)^{\lambda(b_t\oplus a_t\oplus a_c)},
```

which are exact Kronecker constraints.  Their product yields precisely the
CNOT support.  This is a genuine finite action/constraint-weight-to-kernel
calculation.

### Nonselection control

The phase-modified alternative is unitary and changes a fixed-frame closed
experiment from probability `1` to `1/2`.  This closes the earlier weakness in
which the code only established matrix inequality.  It demonstrates that the
dictionary transports distinct supplied action weights to distinct
observables; it does not choose between them.

Action representatives remain nonunique under `2 pi` phase shifts and
boundary/measure conventions.  D15 only claims a finite representative and
an EFT operator normal form, not a unique bare action, so no further blocker
follows.

## Exact D14 typed composition

**PASS at the one-component finite scope.**

The executable now constructs:

```text
system q;
environment e;
protected record R with record_id="R";
collar C;

q -> q tensor e                    inject supplied |0> environment
q tensor e -> q tensor e           apply CNOT action kernel
q tensor e -> q tensor R           explicit commit/retyping
q tensor R -> q tensor R tensor C  emit live collar
```

D14 composition gives exactly the earlier `8 x 2` seal matrix.  The target is
the declared system/record/collar object.  A later D14 `Mor` acts on the system
and identities on record/collar; it composes with the seal and passes the
record-preservation predicate.

This is real semantic integration, not a source-hash receipt.  It closes the
finite S4 mapping to D14 carriers, protected records and collars.

The scope remains small:

- the ports are ownerless because the packet contains one component;
- no join entitlement is needed or derived;
- the collar is emitted explicitly rather than selected by a birth law;
- the future algebra is chosen to be system-only; and
- no gauge, gravity or continuum data enter.

Those limitations are printed in the working theorem and receipt.

## Autonomous-record audit

**PASS only as `ACTION-BRIDGE-CONDITIONAL`.**

The exact physics is:

```text
supplied |0> environment
+ supplied system/environment tensor split
+ supplied CNOT interaction in the Z pointer basis
-> orthogonal S/E correlation and exact reduced Z decoherence.
```

The next step is explicitly named `environment-to-protected-record`.  It is an
identity matrix between two differently typed objects.  Thus the action has
not caused protection by itself; D15 declares that this environment degree of
freedom counts as a durable record.  Live-collar emission is another explicit
primitive.

The coarse graining is likewise selected by tracing the environment.  A
different interaction/basis/split could define a different exact instrument.
No redundancy, macroscopic accessibility, stability time or universal
tolerance is derived.

The updated note states every one of these facts and concludes that the
environment initial state, split and protected-record interpretation remain
supplied.  This is the correct answer to S5.  The exact labels
“action-derived environment/record” should preferably become “CNOT-correlated
supplied environment” in the next receipt, but they do not override the formal
conditional grade.

## EFT domain and parameter ledger

**PASS as a scope/parameter-classification ledger; not yet an empirical fit.**

The new ledger defines the EFT regime schematically by

```math
E/\Lambda_{cut}\ll1,
\qquad |R|/\Lambda_{cut}^2\ll1,
```

and states that truncation error scales like the first omitted operator with
loop/log corrections.  It explicitly declines to invent one universal
numerical cutoff because thresholds are sector- and candidate-dependent.

It also lists the correct action-equivalence operations: integration by
parts, tensor/group identities, leading equations of motion for on-shell
observables, perturbative field redefinitions and fixed-topology four-
dimensional topological densities.

The representative leading curved-space action now exposes:

- Einstein gravity and cosmological term;
- gauge, fermion and Higgs kinetic sectors;
- Higgs potential and Yukawa matrices;
- nonminimal `H^dagger H R` coupling;
- theta/topological terms with anomaly/global caveats;
- neutrino-mass alternatives;
- curvature-squared and higher matter/gravity operators; and
- boundary/corner, state and record parameters as separate classes.

The classification of `G`, `Lambda`, Higgs/Yukawa/gauge parameters, theta
terms, `xi_H`, Wilson coefficients, state and record data is honest.  It
supports the operator-normal-form claim.

What remains absent is a particular analysis domain with a numerical
`Lambda_cut`, chosen truncation order, coefficient data release, covariance
matrix/likelihood and uncertainty propagation.  Therefore this ledger is not
yet S8 empirical model selection or a universal precision claim.  The formal
D15 status correctly remains incomplete.

## General covariance, gravity and S6

### General covariance/gravity

**Open in the exact receipt and honestly scoped.**

The qubit witness has no spacetime metric, diffeomorphism quotient,
gravitational constraints, boundary charges, edge modes, ghosts, corner terms
or continuum limit.  Nothing in its 26 checks is called a gravity theorem.

The working note gives the generally covariant EFT normal form and lists what
a regulated path-integral dictionary would still require.  That is a
literature-backed target architecture, not an executed generally covariant
regional amplitude.  S1's low-energy leading-form reasoning and the exact
nongravitational S4 cell should remain separate.

### S6 locality/no global clock

**Still open beyond inherited supplied-DAG evaluation gauge.**

The exact source verifies one tensor-factor commutator.  The D14 seal is a
single-component ownerless chain.  It does not construct multi-component
owned boundaries, a join entitlement, a complete-diagram amplitude or a local
extension-support law.

D14 may be inherited for the statement that contraction order is gauge once a
finite diagram is supplied.  It cannot convert the qubit action into a law
that generates diagrams without a global scheduler.  D15 should add a short
S6 ledger stating whether each UV candidate sums complete histories or
generates local extensions and where its ownership/join rule comes from.

This is a remaining protocol field, not an overclaim in the current result.

## UV survivor audit

**PASS as an incompleteness/survival ledger.**

The new matrix includes `EFT4`, `BDQ`, `ASQ`, `SFQ` and `PRIM`, and distinguishes
“not presently refuted” from “complete selected packet.”

- EFT4 is selected only as the observed low-energy normal form and assumes
  its continuum carrier.
- BDQ still lacks a selected quantum measure, matter/state/record packet,
  join law, calibrated scale and stable phase.
- ASQ remains a promising coefficient-selector program without a uniquely
  controlled realistic trajectory and record dictionary.
- SFQ supplies regional-amplitude architecture but not the selected
  refinement/matter/state/record packet.
- PRIM is mathematically admissible if posited and physically nonselecting.

The audit explicitly denies a theorem that two complete UV packets share all
observations.  Its sentence that it “establishes nonselection by missing
complete competitors” should be read as **current non-identification**, not a
no-go theorem; “records current nonselection because no complete competitor
has earned selection” would be clearer.

No candidate comparison produces a frozen non-tunable discriminator, so S9
correctly keeps V9 closed.

## 3+1, cones, units and `G`

**PASS.**

The repaired artifacts preserve the strongest round-1 scope boundary:

- EFT4 assumes observed `3+1` Lorentzian field content and signature.
- Its metric principal symbol gives the local cone; this is not emergent
  record-web roundness.
- Higher operators can alter propagation and are bounded/fitted rather than
  silently excluded.
- `G`/`M_Pl` is a measured action coefficient; the reduced-Planck convention
  is printed.
- `l_Pl` follows only after empirical `G` and calibrated `hbar,c` are supplied.
- converting record axes to metres/seconds does not change anisotropy.
- EFT4-to-V9 would be a recovery/calibration exercise, not an untouched test
  of dimension or cone emergence.

No cone/dimension holdout is opened.  This remains correct.

## Gate adjudication

| Gate | Round-2 result |
|---|---|
| S0 effective/fundamental scope | **PASS as schematic EFT scope; numerical sector domain/truncation remains future analysis data.** |
| S1 gravity normal form | **PASS at conditional leading-form/literature scope; exact gravity packet absent.** |
| S2 matter normal form | **PASS at representative operator-ledger scope.** |
| S3 coupling/scale ledger | **PASS for parameter classification; coefficient fit receipt still open.** |
| S4 action-to-D14 dictionary | **PASS for one finite nongravitational typed cell only.** |
| S5 autonomous records | **CONDITIONAL, correctly.** Environment state/split/coarse graining/commit supplied. |
| S6 locality/join/global clock | **OPEN beyond supplied-diagram evaluation gauge.** |
| S7 3+1/cones/influence | **PASS as assumption/recovery ledger; no emergence claimed.** |
| S8 empirical selection | **OPEN as a frozen data/fit ledger.** |
| S9 untouched prediction | **CORRECTLY WITHHELD.** |
| S10 UV survivor audit | **PASS as current survivor/incompleteness matrix; fundamental no-go not proved.** |
| S11 hostile closure | **Round-2 ontology cells pass narrowly; formal investigation still awaits full closure.** |

## Verdict

**PASS FOR THE NARROWED FINITE DICTIONARY AND SCOPE LEDGERS.**  The exact D14
typed composition is now real, the finite CNOT constraint has an explicit
multiplier sum, the EFT/parameter and UV ledgers are materially stronger, and
the record-instrument ceiling remains honest.

The formal D15 verdict should remain:

```text
INCOMPLETE-INVESTIGATION.
```

The accepted interim results are:

```text
FINITE Z2/QUBIT ACTION-WEIGHT -> D14 RECORD DICTIONARY   PASSED 26/26
EXPLICIT ENVIRONMENT COMMIT + POINTER COARSE GRAINING   SUPPLIED/CONDITIONAL
LOW-ENERGY EH+SM EFT OPERATOR NORMAL FORM               SUPPORTED GIVEN OBSERVED INPUTS
GENERAL-COVARIANT REGULATED GRAVITY DICTIONARY          NOT EXECUTED
LOCAL DIAGRAM/JOIN/GENERATION LAW                       OPEN
FROZEN EMPIRICAL COEFFICIENT/MODEL-SELECTION RECEIPT    OPEN
3+1, G AND ROUND-CONE EMERGENCE                         NOT DERIVED
V9 HOLDOUT                                               CORRECTLY WITHHELD
```

No primary claim needs retraction.  D15 needs the remaining S6/S8 and hostile
closure work before freezing `LOW-ENERGY-EFFECTIVE-RULEBOOK-IDENTIFIED` as its
formal protocol verdict.
