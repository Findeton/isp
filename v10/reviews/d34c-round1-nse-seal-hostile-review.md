# D34c round 1 — independent NSE/seal hostile review

**Target:** commit `9f92a95`. **Verdict:** CORE QUANTUM/NSE LEMMAS ACCEPTED; ACTOR/HISTORY SEWING REJECTED. **Count:** 0 BLOCKER / 4 MAJOR / 4 MINOR / 2 NIT.

The receipt reruns exactly with no false printed quantum number. C1–C7 survive at their mathematical scope.

## Major findings

1. C8 is precisely a spectator direct product; actor labels do not control quantum dynamics.
2. The pin's actor operations are not instantiated. Worse, the unused `zip((b,i,n),(I,X,Z))` suggests birth `I`, interaction `X`, idle `Z`, contradicting the frozen idle/birth clauses.
3. The “every finite timed cylinder” statement exceeds one/two iid local kind rings and omits the actual D34b process data.
4. Global direct-sum blocks do not establish distributed event flags. The Python tuples omit target, passive receiver, child, shared-event identity and predecessors.

## Scope repairs

The local `R` algebra is fixed only because modeled future support excludes `R`; relational observables involving `R` can change. Call this quarantine/local-algebra durability, not general sealed holonomy. Build the path receiver explicitly, check every future operation rather than only the product, and scope NSE to quantum preparations over a fixed classical actor/graph sector unless graph-controlled isometries are built.

To earn sewing, use preallocated Ulam carriers, D24 birth, idle `I`, a touched-pair interaction, actual targets/degree/passive reception, tensor-factor flags, click-dependent class operators, real within-actor interference, sequential restriction, remote quantum factorization, and incomparable-event commutation.
