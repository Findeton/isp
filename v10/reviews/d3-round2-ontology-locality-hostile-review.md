# D3 hostile review, round 2: final ontology, locality, Barandes, and profinite scope

**Referee:** independent hostile ontology/locality audit  
**Date:** 2026-07-11  
**Verdict:** **PASS WITH ONE MINOR NOTE CORRECTION**

The revised Paper 4 and executable pass at the stated finite unmarked
common-future/prefix-tower scope. One superseded sentence remains in the D3
note and should be corrected for internal consistency; it does not affect the
paper or any receipt.

## Frozen artifacts reviewed

- `v10/note-d3-profinite-variable-history-extension.md`
- `v10/code/d3_profinite_extension_exact.py`
- `v10/relativistic-isp-v10-paper4-profinite-growth-preserves-past-not-law.md`

Hashes executed in this review:

```text
ea49319b4fa17633fcd676b791c63577cd3a66bc0b6d7293fe36d22b2e3d4b45  note-d3-profinite-variable-history-extension.md
bdc93a27f66ba67d9100d6e76bfa0c5e4b776152373b32c08e97c2046e7b3f64  d3_profinite_extension_exact.py
72147070ee3423d940b24a433704cd2dbe6459910a1a6c491d889fe14a07b858  relativistic-isp-v10-paper4-profinite-growth-preserves-past-not-law.md
```

The repaired receipt reproduces **27/27** checks. The strengthened B6 gate
explicitly verifies the controlled-zero law's end-deletion cylinder
consistency and local isomorphism covariance; its eligibility conclusion
remains correctly limited to prefix-level gates.

## Round-1 findings: disposition

### 1. Common future versus interaction: PASS

The theorem object is now consistently a **common-future extension** or
**bridge shadow** in Paper 4 and in every executable verdict. The paper says
that a marked `A-B` interaction requires typed ports jointly affecting a
sealed outcome. It does not infer signaling, proximity, evidence sharing,
transport, or interaction from the unmarked causet order.

The final result is correctly:

$$
\text{immutable-past common-future extension is coherent,}
\qquad
\text{no local marked law is selected.}
$$

The historical preregistration portion of the note still records the stronger
question that was attacked, but its opening disposition explicitly reserves
`interaction`, `joint record`, `carrier birth`, and `bridge event` for a later
marked law. That historical wording is not promoted as a result.

### 2. Prefix consistency versus subsystem projectivity: PASS

This distinction is now executable rather than merely verbal.

- The deterministic incidence square
  $C[D]|_{K\cup\{e\}}=(C|_K)[D\cap K]$ passes all 6,064 audited old-subset
  cases.
- Both positive probability kernels fail autonomous ancestor-closed
  restriction naturality: 212 failures for `b=1` and 296 for `b=2` across 712
  audited squares.
- The chain-to-point witness prints the exact mismatch
  `(1/3,2/3)` versus `(1/2,1/2)`.

Paper 4 therefore says **end-deletion prefix consistency**, not arbitrary
record-subsystem projectivity. It explains that pushed-forward subsystem
predictions exist while the retained unmarked subsystem is not sufficient to
rerun the same global kernel autonomously. A marked no-silent boundary repair
remains open.

### 3. Finite unlabeled order-shadow quotient: PASS

The placeholder scheduler arithmetic is removed. For both witness laws the
receipt now:

- canonicalizes every audited naturally labeled order;
- sums every isomorphism-fiber mass exactly;
- verifies normalization of the finite unlabeled order-shadow pushforward;
- retains unequal raw labeling masses as presentation data;
- and exhibits different canonical probabilities, including three-antichain
  mass `1/8` versus `1/10`.

This is not yet a canonical marked-history quotient or a complete physical
pushforward. The paper now makes that limitation explicit. It also correctly
refuses an infinite unlabeled-prefix inverse limit because
“delete the last element” is not a canonical bonding map on an unlabeled
order. It also refuses to infer covtree consistency for these kernels. The
result is exactly a **finite quotient nonselection theorem**.

### 4. Covtree spectrum versus full-history lift: PASS in Paper 4

Paper 4 now states:

- a covtree random walk defines a stem-observable/spectrum measure;
- covtree rank is observable resolution, not click count;
- rogue fibers make a completed-history lift nonunique;
- surjectivity removes ghost spectrum points but does not select a lift;
- a SHARD next-record rule additionally needs a marked committed-history
  filtration or extension kernel.

The finite chain-plus-isolate witness is extended through nine events without
changing its exact-rank-two stem theory, while the infinite spectrum theorem is
explicitly imported from v9 Paper 7 rather than claimed from that finite
receipt.

### 5. Marked-support incidence versus sealed-diamond composition: PASS

Paper 4 now uses D2 only for supplied typed **marked-support incidence**. It
explicitly excludes probability, screen, collar, source, transport, and
holonomy composition and leaves full sealed-diamond gluing open. D1 is likewise
described as testing finite boundary logic rather than creating a sealed
diamond.

### 6. Universe-ledger witness versus locality: PASS

The `(a,b)` family is explicitly labeled a global-prefix/universe-ledger
control. It enumerates every legal down-set, inspects the whole component
decomposition, and globally normalizes one next event. It is used only to show
nonselection under broad prefix gates.

No local specification, decentralized realization, bounded record
neighborhood, relativistic locality, or no-signaling theorem is inferred.
The paper correctly says that a global measure might admit a local
realization, but D3 has not supplied one.

### 7. Barandes and primitive inputs: PASS

The revised Barandes section keeps separate:

- indivisibility of a supplied interval law;
- non-Markov dependence on the full path;
- disintegration of an already supplied path measure;
- and derivation/selection of that measure.

It explicitly treats the committed-history filtration, variable extension
domain, and conditional version as primitive rulebook data. Positive prefix
cylinders fix the displayed conditional ratio; zero-mass histories retain
version ambiguity. No Barandes correspondence principle is claimed to select
the law.

### 8. Geometry: PASS

Paper 4 makes no cone, dimension, stationarity, scale, continuum, absolute-
distance, or quantum inference. It retains the v9 diffusion result at
builder-class scope and requires the complete geometry suite to be rerun after
a marked law is selected or posited.

## One remaining minor correction

The note's historical initial-results section still says:

> Covtree can carry a manifestly covariant full-history measure.

That is precisely the round-1 overstatement repaired later in the note and in
Paper 4. Replace it with:

> Covtree can carry a manifestly covariant stem-observable/spectrum measure. A
> full-history measure requires a generally nonunique lift through the
> completed-history evaluation map.

Alternatively, label the sentence explicitly as superseded by O6. This is a
documentation correction only; Paper 4's theorem scope is already correct.

## Precise accepted claim ceiling

The strongest accepted D3 result is:

> In the unmarked naturally labeled causal-order shadow, a new maximal element
> may be placed above a down-set without changing any old-old order relation.
> Down-sets meeting multiple comparability components produce common-future
> bridge shadows. Global full-prefix kernels on this arena define exact
> end-deletion-consistent labeled measures, but need not be autonomous under
> old-subset restriction. Multiple locally isomorphism-covariant kernels and
> eligibility supports survive the prefix gates, and their canonical finite
> unlabeled order-shadow distributions remain inequivalent. The prefix inverse
> limit and covtree spectrum are different arenas; neither topology nor full-history
> access selects the marked local event law.

It is not a claim of:

- interaction or marked carrier birth;
- subsystem-projective locality;
- an infinite unlabeled-prefix measure;
- a unique lift of a covtree measure;
- a local/decentralized realization of the witness;
- sealed-diamond gluing or birth;
- a marked profinite completion;
- Barandes/quantum law selection;
- geometry, dimension, or cone roundness;
- or the final interacting click law.

## Final recommendation

Correct or mark superseded the one stale covtree sentence in the note. No
additional D3 receipt or Paper 4 revision is otherwise required for ontology,
locality, Barandes, diamond, profinite, or geometry scope.

At that point the D3 result should be accepted as:

```text
IMMUTABLE-PAST COMMON-FUTURE EXTENSION
+ LABELED-PREFIX CONSISTENT-FAMILY
+ FINITE UNLABELED ORDER-SHADOW NONSELECTION
+ UNMARKED RESTRICTION-NATURALITY FAILURE
+ PREFIX-LEVEL ELIGIBILITY NONSELECTION
```

The next investigation is genuinely new work: derive a record-readable marked
finite-boundary eligibility/weighting law, or explicitly posit a global or
nonlocal history measure.

## Closure confirmation

**Final recheck:** 2026-07-11  
**Final ontology verdict:** **PASS — D3 closed at the finite unmarked
common-future/prefix-tower scope**

The last stale note sentence is corrected. It now states that covtree carries
a stem-observable/spectrum measure, that a full-history measure requires a
generally nonunique lift through the completed-history evaluation map, and
that a SHARD next-record interpretation additionally needs a physical
filtration or marked extension kernel.

Reviewed final note hash:

```text
a080b9020c3f683e847aa225cdbc9bd0402155843c98a45c0d9b21b03fee7a6a  note-d3-profinite-variable-history-extension.md
```

This matches Paper 4, v9 Paper 7's rogue-fiber scope, and the accepted D3 claim
ceiling. No D3 ontology, locality, Barandes, diamond, profinite, or geometry
correction remains open. The next task is new physics rather than repair: a
record-readable marked finite-boundary eligibility/weighting law, or an
explicitly posited global/nonlocal history measure.
