# INTEGRATED HOSTILE REVIEW — v10 D28b/D29/D30 arc (round 1, single stream per the D29 F6 declaration; 2026-07-12)

**Reproduction:** all four receipts rerun byte-identical (4/4, 6/6, 8/8 + byte-identical regenerated card, 5/5), exit 0; card sha verified; pins precede runs; nothing edited post-run; the reviewer regrew all 60 webs independently — every W3 number reproduces digit-for-digit; the event-order builder equals an independent closure elementwise in 60/60 webs.

## MAJOR (all applied same day; LEDGER #135)
**M1 — D30's BRANCH-1 verdict does not survive like-for-like comparison; corrected verdict BRANCH 3.** Clause (ii) exponent-incoherent (c at measured d_hat vs card c at true d; matched-exponent collar c_2 = 2.91 vs 1.801 — +61.5%, ABOVE the m_2 = 2 asymptotic ceiling: structural exclusion); clause (i) used the 0.35 estimator-accuracy bar, 8.75x the card's per-cell SD (collar d_hat = 1.756 sits -6.3 SD below M^2); ratio3 measured and ignored (-10.1 SD). THE QUASI-FILAMENT ATTACK CLEARED: occupancy/width profiles distinguish the object from M^2, second/third intervals agree (no argmax bias — F1 answered), midpoint scaling 2^(-1.756) concordant with MM — a SELF-CONSISTENT NON-INTEGER d_hat ~ 1.76 order: genuinely not a filament, genuinely not M^2. Fix applied: the corrected rule (card bands; matched-d; ratio3; box-4) — reran BRANCH 3 with the positive residual printed; LOG/LEDGER headlines superseded.
**M2 — the measured order is the wire/circuit DAG, not D28b's directed relation** (31.5% comparable-pair gap; directed-relation MM sensitivity d_hat ~ 3.2). Fix: the object OWNED as declared physics (robust-zeros justification); the D28b attribution struck; the directed event poset = the named successor (O-C).
**M3 — the D28b conjecture false as pinned: the same-pair cancellation counterexample** (8 touches, half-angles summing to 2*pi: asin(3/5)+asin(4/5) = pi/2; reachability TRUE, I_ev = 0 exactly, mid-history 576/625). Fix: the NON-DEGENERACY hypothesis added; E5 gates the counterexample; battery results scope-true.
**M4 — D29's spatial ruler: pinned RW-2-link, delivered a coordinate-consuming quantile proxy** (violates the order-only header); no spatial/cone instrument exists for grown webs. Fix: the vacancy disclosed in the note; order-only repair + true 2-link = named refinements; LEDGER wording carries the proxy qualifier.
**M5 — K_flat's pinned battery under-implemented** (cylinder gate absent; label-covariance a presence check). Fix: the fixed-horizon marginalization gate + two relabeling classes added; §2(iii) scoped within-horizon (the {K_flat^T} family is not projective — R5's content).

## MINOR (applied): m1 clause-(iv) hardcoded pseudo-SDs (superseded by the corrected rule; card-schema upgrade = O-E); m2 the V3 disclosure quoted 10.4% — the true spread is 10.1%; m3 the K_tail far-rate mislabeled per-web (457 is per 3N-step growth RUN; ~100 per 512-event web); m4 D28b E1/E4 gates weaker than pin wording (E5 + the hypothesis carry the repair; exact-comparator upgrade named); m5 receipt-count conventions (d30 = 3 substantive + 2 print; d29 = 7 + 1); m6 alpha is a two-point slope with sub-SE margins on both verdict edges (branch-1-vs-2 unresolved at 10 seeds — moot under the corrected BRANCH 3, named); m7 D29 pin-scope gaps (midpoint cross-check unimplemented — review-implemented, concordant; V7 weaker than the pinned joint signature — named as criterion delta); m8 "card sha ok" printed without computing (now computed and gated); m9 window mismatch disclosed (interval-window comparison MORE exclusionary — cannot flatter).

## NIT: n1 provenance line (both openings = the physics stream's); n2 E3 "every tested web" = 3 of 4; n3 per-seed SD naming; n4 d3 chain_c non-monotone (the <=10% arm); n5 enters_band at N=512 only; n6 seeds formulaic in-code.

## SURVIVES ATTACK
Total reproducibility; kernel fidelity D28->D30 exact (line-by-line + full regrowth); the event-order builder correct for its declared object; no argmax bias; all D28b exact numbers verified by hand (1/40, 1/25, 2/81, Phi(seed) = 26, Phi(T3) = 256, 9/26, 51/128); path-covariance telescoping a genuine theorem; the functional equation right; canon correct; mm_f(d) confirmed independently (f(2) = 1/2, f(3) = 8/35, f(4) = 1/10); the V3 revision discipline held (only the quoted number wrong); V7's thin margin reproduces exactly and was self-disclosed; the pure-birth control real; percolation rejection reproduces; no v9 anywhere; growth genuinely classical; K_flat scope honest.

## OPENINGS
O-A THE REAL HEADLINE: the collar object's self-consistent non-integer dimension ~1.76 (MM + midpoint concordant) — name it, instrument it (abundance curves vs branching/fractal-order models; height-resolved d-profile); the quantitative F12 question: what moves d_hat -> 2 exactly; is d_hat(kernel) continuous. O-B the repaired conjecture (done at this round). O-C measure the directed (register, epoch) event poset at matched count. O-D K_flat's graph-blindness recurrence Phi(u,t) = u Phi(u+1, t+1) + u(u-1) Phi(u, t+1) — closes F3, makes the stationary-and-covariant question finite (done at this round, note §4). O-E card schema: per-cell SDs for every statistic + the controls at every measured N.
