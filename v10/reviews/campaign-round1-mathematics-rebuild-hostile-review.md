# HOSTILE REVIEW — MATHEMATICS/REBUILD stream: v10 D24/D25/D26 + paper 19 (campaign round 1)

**Verdict: MAJOR-REVISION (1 MAJOR, 6 MINOR, 8 NIT).** All three receipts rerun green and byte-identical; every checked number faithful; the highest-value attacks (tdist certificate, order gauge, recovery rule, mixed-parent bridge, dark-interior invariance) all FAIL — those five now carry proofs. The one MAJOR is the D26 E1 claim-vs-receipt mismatch; an exact repaired exhibit is supplied.

## Front 1 — receipts, git, disclosure: VERIFIED
Reruns: d24 7/7, d25 7/7, d26 6/6, exit 0; .out files byte-identical. Git chain linear, pins precede runs (pins commit the note only; code+output land in the run commit — standing corpus convention, D23 identical). The d26 E5 disclosure is accurate.

## MAJOR-1 — D26 E1's UV pair is split TODAY by a visible-sector coherence click.
Exact (three independent methods): (9/25,16/25) vs (16/25,9/25) give A-coherence 48/125 vs 36/125, B-coherence 1728/15625 vs 3072/15625. D26 §1 puts coherence records in the interface; E3/E4 use A-coherence as probed. The committed exhibit carries Z-population equivalence ONLY; the .out "future click" line is wrong for this pair; pinned F2 answered NO as committed. Loci: note E1, .out E1, paper 19 abstract/§4/§6, LEDGER #129, LOG. **Correction (either):** (i) repaired exhibit — chains A->H1->H2->H3->B, ends fixed (9/25 in, 16/25 out), heavy-INTERNAL couplings swapped (16/25, 576/625) vs (576/625, 16/25): FULL visible rho_AB identical (all Z and coherence statistics), heavy tables differ (P(H2=1) = 2304/15625 vs 82944/390625) — gate rho_AB equality; closes F2 affirmatively; or (ii) rescope to Z-population equivalence and print the light coherence split as a present-day splitter. Either way "the D15 low-energy shadow" must include coherence imprints.

## MINOR
1. Order-gauge "receipt-grade" covers one shape. The general lemma is TRUE — proof: adjacent transpositions of incomparable edge pairs; incomparable pairs share no register or only the control; disjoint edges commute; shared-control edges commute (block-diagonal in the control's Z basis, disjoint targets). Clean-room: all 141 rooted trees on 4-5 registers, all 612 causal extensions, amplitude-level equality, zero mismatches. Scope the §2 sentence; close F8 with theorem + receipt.
2. D24 G5 parent recovery truth-side-circular as coded (consults ancestors_of(pm, u)). Sound here (anc verified same loop) but the theorem means click-side-only. Fix: anc_rec[u] == anc_rec[ch] - {u}. Click-side rebuild: 864 recoveries exact (n=3,4,5; 144 trees; 3 coupling schemes incl. all-equal; 2 preps). Parent rule provable: nested ancestor sets along root paths.
3. "per-edge couplings from an interior grid" suggests a sweep; one fixed assignment used. Say so — or sweep (rebuild: all 27 assignments at n=4 + equal couplings, zero collisions).
4. D24 G3's ledger leg (psi_a != psi_b) certifies ensemble injectivity — the very form D25 kills. Fix: gate overlap invariance <psi_a|psi_b> at every birth (isometry-grade). Closes D24-F1 consonantly with D25.
5. D25-orbit wording: (a) note §2 "verifies the block structure" -> verifies trace conditions (cite the certificate lemma); (b) R5 computes a TWO-way split (value preserved, content killed); occurrence is definitional; (c) D22 §6 F1 "weak collapse -> D/2 at every rate" wrong as composed — contraction is (1-lambda)D at rate lambda; D/2 is the lambda=1/2 representative (paper 18 §8's wording correct; mirror it).
6. E2 receipt PASS-label carries an unscoped universal ("every dark-interior structure"); code tests 4. Add "tested" — or upgrade: the universal is a theorem (any dark-subtree-supported channel leaves rho_AB invariant); attack battery (8 structures incl. double-out register, g=1 boundary, interleaved schedule, depth-4) — all invariant.

## NIT
(1) "exact byte equality" = Fraction dict equality. (2) G4's "separate code path" reuses Web._cry — common-mode bug would pass (closed by two independent clean-room engines: classical Markov tree law == quantum diagonal on all 19). (3) G3 "preparation grid" = 2 preparations. (4) tdist never asserts input symmetry. (5) E5 "all 8 outcomes": 4 explicit + 4 via normalization. (6) "interior coupling swept" = two values. (7) schedule_of() infinite-loops on non-tree input (only fed trees). (8) .out E1 "future click" line — subsumed by MAJOR-1.

## SURVIVES-ATTACK (with proofs found)
- **tdist certificate PROVEN SOUND.** Theorem: D != 0 real symmetric, tr D^3 = 0, (tr D^2)^2 = 2 tr D^4 => nonzero spectrum exactly {x, -x}. Proof via max-eigenvalue extremal argument (equality case forces all nonzero |lambda| = M and exactly two; {M, M} killed by tr D^3 = 0). trD=0 redundant. 2e5 random rational spectra: no violation; 126 agreement checks vs independent Faddeev-LeVerrier eigendecomposition. ratsqrt correct; fail-closed.
- **D25 invariant table independently recomputed** (own closures, char-poly distances): all 55 distances match. R3/R4/R7 reproduced with own operators. NOTE: all five closures preserve the value pair — R6's separation is carried entirely by the eta-pairs (O7).
- **E4 bridge holds for MIXED parents — pinned F3 closes YES.** Proof: tracing the child gives parent-local Kraus pair diag(1, sqrt(1-g)), diag(0, sqrt(g)); coherence x sqrt(1-g), populations fixed, for arbitrary correlated/entangled parents; births elsewhere leave the parent marginal invariant. 8 exact cases verified.
- **19-law census**: rebuilt with two engines; zero collisions through n=5 (125 trees) and under adversarial equal couplings; the g=1 boundary genuinely breaks the zero-pattern criterion — the interior scope is honest and load-bearing.
- **Number fidelity clean** (every number in paper 19 §§2-4/6 + notes vs .out + recomputation; 20 gates = 7+7+6; neutrino inputs verbatim; Pedalino anchoring consistent).

## OPENINGS
O1 close F8 (theorem + exhaustive receipt). O2 all-n identifiability theorem (constructive; receipted through n=5). O3 E2 -> subtree-channel theorem + battery. O4 adopt the repaired UV exhibit (closes F2 affirmatively). O5 state the certificate lemma in the note. O6 G3 -> overlap-preservation gate. O7 the R6 value-pair sentence. O8 D25-F1 measure-independence closable in one paragraph: the rho_eta family commutes (shared eigenbasis (|000> ± |111>)/sqrt2, eigenvalues (1±eta)/2), and all five closures map it to commuting families — every monotone distinguishability measure reduces to the classical one: the R2 verdicts are functional-independent.
