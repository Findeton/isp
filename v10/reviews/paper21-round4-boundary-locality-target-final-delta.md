# Paper 21 round 4 — predictive-boundary/locality target final delta

**Target:** repaired commit `aa1a258`, audited against every `M1`–`M3`,
`m1`–`m5` and `n1`–`n2` in
`paper21-round3-boundary-locality-target-hostile-delta.md`.

**Exact verdict:** **SUBSTANTIVELY DELTA-CLEAN — 0 BLOCKER / 0 MAJOR /
2 MINOR / 1 NIT.**

All three previous majors and all five previous minors are closed. The repaired
D34e target is now mathematically and physically executable. Two new/local
scope precisions and one naming nit remain; none reopens D34d or changes the
target's scientific decision tree.

## Prior finding disposition

### M1 — screening versus NSE: CLOSED

Section 10.3 now carries two separate ledgers:

```text
omit a sealed record from B_A
    — licensed by predictive screening while the record persists globally,

physically delete/compress a sealed record
    — requires an independent NSE/isometric-carrier theorem preserving every
      protected distinguishability.
```

NSE receives a separate pass/fail/not-applicable flag and is not applicable
unless physical deletion/compression is proposed. Query-relative equality of
future probabilities is no longer presented as global evidence preservation.

### M2 — time/no-event/passive-crossing closure and stopping scopes: CLOSED

The recursive target now includes `Delta t`, survival/no-event data,
deterministic renewal-age flow, the event or batch, passive/cross-boundary
input, and touched/created boundary records. An embedded-chain candidate must
state which waiting-time data it quotients away.

The finite domain is typed as `D(N,H,Q,I,S)`. `S` separately registers fixed
construction time, A-own-ring count and A-wire-event count including passive
receptions. Fixed global event depth is explicitly an auxiliary enumeration
and locality-negative control, never regional-screening evidence. Every result
must print its stopping scope. Construction covariance is tested at that same
scope, so a typed-DAG swap cannot silently discard different survival data.

### M3 — outcome classes: CLOSED IN SUBSTANCE

Section 10.8 is a first-applicable decision list and separates:

- refusal/undefined input;
- all-future bounded carrier;
- all-future growing carrier;
- whole-component/global necessity;
- carrier-class exclusion theorem;
- tested-candidate obstruction;
- finite-domain-only survival.

It explicitly forbids finite enumeration from becoming an all-future result,
requires independent size and horizon theorems for promotion, and reports
covariance, composition, capacity and NSE separately. No partial score can
earn a bounded-collar headline.

### m1 — prefix tower: CLOSED

The global labeled prefix tower is now called an **auxiliary source-level
construction presentation** with census-dependent depth and auxiliary
serialization. A physical regional carrier must descend to timed past-closed
or typed-DAG/down-set data and pass construction covariance.

### m2 — D5 scope: CLOSED

D5 messages are now conditional candidates only. The target requires the
factor cover, scopes, values, interface embeddings and exactly-once ownership
to be physically constructed or explicitly supplied and audited. Contraction
alone earns neither record-native carrier nor causal-boundary status.

### m3 — fixed radius versus capacity: CLOSED

The capacity ledger separately prints graph radius, actor/record count,
open-port count, finite-state count where finite, continuous dimension and
precision, explicit unbounded fields, quantum widths and identifier cost.
Fixed radius is explicitly not bounded width when incident degree is
unbounded.

### m4 — causal meaning: CLOSED

The target is renamed **predictive record-DAG boundary** and immediately says
that causal means record/wire DAG, not light cone, Lorentz covariance, finite
propagation or proper time. Construction covariance is presentation gauge for
incomparable record-DAG updates, not spacetime coordinate covariance.

### m5 — intervention/output typing: CLOSED

Passive prediction and controlled/quantum operation are separate inputs. The
passive branch uses a regular conditional kernel on a measure-determining event
class. The operational branch requires a supplied causal kernel family indexed
by policies/instrument sequences and compares `P(r|I,h)` for typed durable
outcomes. Adaptive policies and entangled ancillas must be declared. A passive
D34b law is not claimed to generate intervention responses.

### n1/n2 — axes and carrier resources: CLOSED

Physical width and current-past/resolution are reported independently; v9 stem
rank is correlated only after a bridge. Finite-state count is now conditional
on finiteness, alongside continuous dimension/precision and explicit
unbounded-field entries.

## Fresh MINOR findings

### m1 — one outcome label exceeds its quantified carrier class

Outcome 5 is labeled

```text
NO RECORD-CARRIED EXACT REALIZATION
```

but its criterion proves exclusion only for “every member of the
**preregistered physical carrier class**.” That is the right theorem scope,
but the headline reads as exclusion of every conceivable record-carried
realization.

**Required repair:** rename it

```text
NO EXACT REALIZATION IN THE DECLARED CARRIER CLASS
```

and reserve universal `NO RECORD-CARRIED EXACT REALIZATION` for a theorem whose
quantifiers truly cover the full physically admitted record-carrier class.

The neighboring `WHOLE-COMPONENT/GLOBAL ONLY` row should also print which one
is proved necessary. A connected-component state is a distributed
record-carried object of unbounded width; a complete global state additionally
contains disconnected factors that D34b has already shown irrelevant to A.
Do not merge those physical widths in the emitted verdict.

### m2 — the proposed D34b-to-v9 forgetting map must itself be a bridge gate

Section 10.5 now gives the correct adapted posterior construction rather than
assigning a current finite past one completed-spectrum point:

```text
nu_tau = (phi o u)_* mu(d completion | F_tau).
```

However `u` is introduced as a mark-forgetting/causet map from completed D34b
marked histories without yet gating that map. Before the formula is a licensed
D34e object, prove or explicitly assume:

1. the completed D34b event/wire order lies in the past-finite completed-causet
   domain used by v9 after marks/types are forgotten;
2. `u` is well defined under the declared event/Ulam relabeling gauge;
3. `u` is Borel measurable for the D34b standard-Borel history coding and the
   v9 history space;
4. the chosen conditional completion law pushes forward through it.

**Required repair:** call `u` a **candidate map** and add these as gate zero of
the online-to-profinite branch. This does not assert that the bridge fails; it
prevents the notation from silently supplying the main domain/codomain map.

The rest of §10.5 is correctly scoped: `phi` is Borel rather than generally
continuous; online posterior factorization, completed-observable
factorization and a new adapted inverse limit are distinct claims; continuous
marks do not automatically give a profinite object; rogue fibers are not an
online state by themselves.

## Fresh NIT

The first-applicable outcome list is well defined only **per frozen registered
scope/branch** `(mu,A,Q,I,S,carrier class)`. Add that tuple to the sentence
introducing §10.8. Otherwise an undefined operational branch could appear to
force `REFUSAL` even while the separately registered passive branch has a
licensed result.

## Fresh-overclaim audit

No repaired text assumes:

- bounded actor degree;
- bounded graph-radius capacity;
- a pre-existing or finite collar;
- finite classical or quantum memory;
- relativistic locality or proper time;
- D5-derived physical transport/scope;
- an existing profinite predictive boundary;
- NSE from predictive screening;
- an all-future theorem from finite cells.

The maximum current statement remains exactly:

> **D34e TARGET IDENTIFIED; PREDICTIVE RECORD-DAG BOUNDARY UNCONSTRUCTED.**

Apply m1–m2 and the branch-index nit before freezing D34e's code pin. No D34d
receipt or Paper 21 terminal result requires scientific revision.
