# D33 ROUND-1 HOSTILE REVIEW — fresh referee, clean-room rebuild — 2026-07-13

**Target:** `code/d33_gauge_reaudit_exact.py` + `data/d33_gauge_reaudit_exact.out` (6/6) at c71f068 — the opening theorem-grade receipt of the history-law phase. Pins: note-d33 §1 (34e68e2, pre-committed); the adopted proposal `reviews/d33-external-proposal-history-law.md`; LEDGER #154; LOG 2026-07-13 D33 entry. Lineage consulted and rerun: d31a (5/5), d31c2 (7/7), d31b (the frozen-order generator), note-d31 §§1/5/6/7, paper 20 §2.
**Method:** the gauge machinery was rebuilt from scratch and tested exhaustively beyond the receipt's exhibits (independent event-poset closure; an independent equivalence oracle "fresh-relabeling ∘ linear extension" run against `canon_history` on 1,588 (history, permutation) cases and 256 length-3 histories); both G2 kill rows re-derived by hand and by an independent census engine; the G4 forcing identity re-derived symbolically for general u and probed at u = 3..30; the seed-escape kernel's covariance verified on **every** register-disjoint pair of the whole u ≤ 5 horizon (1,357 pair equations, exact rationals — the gate the receipt lacks); receipt + lineage rerun under three hash salts. Referee harness: `scratchpad/d33_referee_harness.py` (session scratchpad; 8/8 after correcting my own H2 oracle to the receipt's actual equivalence — see M2, which that correction exposed).

**VERDICT: MAJOR-REVISION — 3 MAJOR / 6 MINOR / 3 NIT. Zero receipt gates flip; zero false printed numbers; every printed row, bound, and count reproduces exactly (both G2 deltas re-derived digit-for-digit; A000081 = 1/2/4/9; 1/90; mean 1). The gauge definition is CORRECT and the no-go re-audit is SOUND: the kills survive the refined covariance, the seed-level escape is genuinely covariant (verified here far beyond the receipt's own gates), and the restated graph-blind theorem survives the clean-room rebuild — in fact it comes out STRONGER than the receipt claims, which is the central finding: the "exact-zero form is SUPERSEDED by the asymptotic form" framing is wrong-shaped, because exact zero SURVIVES on u ≥ 3 from incomparable pairs alone; only the seed weight w_i(2) is released. The MAJORs are claim-shape and receipt-integrity class (a false-as-written universal in G1's label; a tautological gate at G5); no mathematical result is false at its correct scope.**

---

## THE CENTRAL ADJUDICATIONS (as put to this round)

1. **The gauge definition (G1): RIGHT.** "Incomparable ⇔ disjoint wires at insertion" is exactly the D31B frozen order's comparability for freshly appended pairs: `event_poset` is the same next-op-on-wire closed generator as d31b's `wire_dag`/`port_poset` (verified against brute-force Floyd–Warshall closure on 3,774 enumerated histories), a fresh next-op pair sharing a register is forced adjacent on that wire (comparable), and a register-disjoint fresh pair admits no directed wire path in either order (incomparable, permanently — later events add descendants, never new relations between existing events). `canon_history` is a **complete** isomorphism invariant for these histories — the review's own suspicion ("direct predecessors only; collisions possible") is REFUTED: `event_poset` returns the *transitively closed* ancestor sets, the sig carries each event's full typed triple plus all ancestors' full typed triples, and per-wire event orders are recoverable from the sig (same-wire events are comparable, so ancestor-set sizes strictly increase along every wire chain; the poset is the closure of the wire chains). Confirmed exhaustively: canon-equality coincides with the true equivalence (fresh-relabeling ∘ linear extension) on all 1,588 tested cases, and 220 canon classes on 256 length-3 histories show zero collisions and zero splits. The two exhibits show exactly what the .out claims. **But G1's printed universal is false as written (M2), its code block carries an abandoned false start with a false in-code comment (m6), and the gate is exhibit-grade, matching the pin's promise — not a gated iff.**
2. **The re-audit (G2–G4): SOUND, with the theorem understated.** The exhibited rows use only disjoint pairs (rebuilt exactly; the DEG and DDEG first-sorted all-nonnegative rows are the u = 3 star's y = 2 birth, matching the .out digit-for-digit); the motif contradiction closes end-to-end from u ≥ 3 with **no** u = 2 input (walked below); the G4 forcing identity holds identically at general u; the seed-level escape verifies completely (H6). The honest full strength of the restated theorem is **w_i(u) = 0 for all u ≥ 3, w_i(2) ∈ [0, ½) free** — see M1 for why "asymptotic density" both under-claims and (in its supersession sentence) misdescribes.
3. **HF6 (G6): CONFIRMED — no paper-20 sentence moves.** Audited sentence-by-sentence at `relativistic-isp-v10-paper20-the-geometry-of-record-growth.md` §2: §2.1's theorem is true at its stated hypothesis (its "path-covariant" is the receipts' state-level convention, and §2.3's O7 paragraph is the in-paper warning that the whole complex presumes that quotient); §2.2's "obstructed at every horizon U ≥ 3" sentences now hold under *both* conventions (G2 strengthens them); falsifier (iii) is safe — the seed-escape kernel does **not** trigger it, because (iii)'s covariance is the paper's declared state-level axis, under which the escape fails the u = 2 overlapping-pair equation. Recommendation and the #154 supersession-wording finding at m5.
4. **Code audit:** closure correct (H1); canon complete (H2/H3) and factorially priced in fresh registers (fine at exhibit scale, n3); **G5's gate is a tautology (M3)**; sorted() discipline honored at every salt-exposed site (delta_census sorts keys — the #139/#145 class honored; canon sorts str-register sets; G2 iterates sorted; G3's unsorted `reps.values()` feeds only conjunctive, order-invariant gates over an insertion-deterministic dict); receipt rerun 0.1 s, byte-identical to the committed .out under PYTHONHASHSEED 0/7/12345.
5. **Pin fidelity: MET, clause by clause** — pinned (a) delivered at G2, (b) at G3, (c) at G4 *in the pin's own words* (M1 is therefore pin-inherited: the "(the honest weakening)" framing originates in note-d33 §1 and the receipt transcribed it faithfully — the repair is a forward-correction, not a fidelity breach), (d) at G5 (true content, sham gate). The phase's "obstruction engine = the global denominator" sentence is **not** licensed by this receipt at receipt grade — m4.

---

## MAJOR

**M1 — The restated theorem is STRONGER than claimed; the "SUPERSEDED by the asymptotic form" framing misdescribes what survives; "density" is undefined and its conclusion ungated.**
Independent rebuild (harness H7, exact rationals): the incomparable-pair forcing gives w_i(u+1) = w_i(u) for every u ≥ 3 (identity: lhs − rhs = w_b(u)·(w_i(u+1) − w_i(u)), zero iff constancy whenever w_b(u) > 0 — probed u = 3..30, four rational configurations each); constancy c on u ≥ 3 plus birth-positivity under normalization (w_b(u) = (1 − u(u−1)c)/u > 0) at every reachable u forces c < 1/(u(u−1)) for all u, hence **c = 0 exactly at unbounded growth**. So the surviving theorem is: *a stationary birth-positive graph-blind kernel under linear-extension covariance has w_i(u) = 0 for ALL u ≥ 3; only the seed weight w_i(2) ∈ [0, ½) is unconstrained.* The exact-zero conclusion is not superseded — it survives verbatim on u ≥ 3, derived from incomparable pairs alone; what the refined gauge releases is one number, the seed weight. The receipt's own escape kernel (w_i(u ≥ 3) = 0) *instantiates* the forced class, yet the forced conclusion is never printed. Consequences:
- G4's contrast sentence ("the OLD theorem's exact-zero conclusion used the comparable u = 2 forcing — dropped honestly") attributes the whole exact-zero conclusion to the u = 2 swap; that swap was load-bearing only for the seed component w_i(2) = 0. G6's "the exact-zero form needed a comparable swap and is SUPERSEDED by the asymptotic form" and #154's echo invite the reading that only a density statement survives — under which a reader would admit kernels with w_i(u) > 0 vanishing in u (e.g. ∼ 1/u³). Those are NOT covariant (constancy fails). The honest landscape line: *exact zero survives on u ≥ 3; the seed weight is released; hence interactions are confined to a geometric seed block and the interaction density along any unbounded history → 0.*
- "Interaction DENSITY" is defined nowhere (receipt, pin, or LEDGER). What is gated: constancy at one u (see m2), decreasing bounds, and the seed premise. The density corollary (fraction of interact events among the first N events → 0 a.s., because the interact count is geometric at the seed and identically zero after the first birth) is true and one paragraph — but it is prose-only, and the c = 0 step itself has no gate (d31a A3 computed its u*; d33 computes nothing analogous).
Repair: restate G4(b)/G6 with the per-opportunity form first (w_i(u ≥ 3) = 0 forced; w_i(2) free) and the density claim as a *defined* corollary; add a c = 0 gate (A3-style u* computation suffices); forward-correct #154's "RESTATED ASYMPTOTICALLY"/"SUPERSEDED" wording at #155. Zero gate flips; the phase's architecture verdict is unchanged and *sharpened* (D34's target must evade an exact-zero forcing, not outrun a density bound).

**M2 — G1's universal claim is false as written: not every comparable-pair swap yields a distinct history — same-parent birth swaps are absorbed by fresh relabeling.**
The .out states: "swapping COMPARABLE events (shared wire) yields a DIFFERENT history (physical — the order along a record is recorded)." Counterexample (harness H2, exhaustive): `[b(A,z1), b(A,z2)]` vs its swap `[b(A,z2), b(A,z1)]` are **canon-EQUAL** — the swap composed with the relabeling z1↔z2 is the identity on the isomorphism class. 82 of the 1,588 tested (history, permutation) cases are canon-equal without being plain linear extensions — absorbed by relabeling — and the elementary absorbed-swap class is exactly the same-parent birth pairs (interacts carry no fresh registers to relabel; birth-vs-interact and parent-child birth pairs change the ancestor structure). Three things to say precisely:
- **canon_history's behavior is CORRECT**, and this is the Rideout–Sorkin relabeling-invariance phenomenon showing up on schedule: the two sequences describe the same physics ("A births twice, in an order"), the event order along wire A is *preserved* by the isomorphism (first birth < second birth in both), and only the children's bookkeeping names swap. Rename ≠ reorder.
- The doctrine survives in corrected form: *no gauge transformation reverses the order of two comparable events; but a comparable-pair sequence swap yields a distinct history only when it is not absorbed by fresh relabeling* — same-parent birth swaps are absorbed; the receipt's birth/interact exhibit is correctly chosen from the non-absorbed class.
- **No demandable covariance equation is missed:** the absorbed swaps traverse identical state paths (b_y then b_y is one path; the "two orders" differ only in name assignment), so they impose only trivially-satisfied equations; the receipt's incomparable-only demand set remains the complete non-trivial set (with m3's lemma).
Repair: one clause in G1's label ("a comparable swap *not absorbed by fresh relabeling* yields a distinct history"); optionally convert the dead h-block (m6) into a third exhibit gating the absorbed case (`canon(b(A,z1),b(A,z2)) == canon(b(A,z2),b(A,z1))`) — which would turn this round's counterexample into receipt content. This matters forward: D34a's linearization-independence gate and HF2's tie conventions will consult G1's statement of what is gauge.

**M3 — G5's gate is a tautology: a print gate disguised as a computed verdict.**
Line 271: `ok5 = all(len({('b',)} | {('i',)}) == 2 for _ in [0])` — evaluates True with no input whatsoever from the component-size family (harness H8). Yet the closing line counts it among "G2-G5 re-audit verdicts", and unlike G6 (honestly `check(..., True)`), G5 wears a check's costume. The *content* is true and was independently verified here: register-disjoint pairs are state-level independent (both available, both orders defined, same final state), so the refined demand set is a subset of the state-level pair set d31c2's D5b verified exactly (558 pairs from u ≤ 4 starts; the u = 5-start caveats — ii order-identities, births exiting the guarded domain — carry over verbatim to the refined set), and d31c2 reruns green 7/7 byte-identical. But a re-audit receipt whose G5 verdict gates nothing is a receipt-integrity defect in the phase's opening theorem receipt. Repair (either): gate the reduction computationally (all 16 horizon states connected + `cls_COMP` factors through u — `connected` is a 6-line import from d31c2; d33 currently doesn't even define it) and/or verify the D5b witness kernel against the refined disjoint-only pair set directly; or demote G5 to a declared print gate and re-word the 6/6 line. The a-fortiori argument itself should appear as the subset lemma, not as "a fortiori" alone.

## MINOR

**m1 — G2/G3's kill chain silently consumes the disjoint-interact class-invariance lemma without re-gating it.** The Z(W+b) = Z(W) forcing needs cls(i_disjoint | W+b) = cls(i_disjoint | W) to cancel the interact's weight across the equation. G3 gates it for MOTIF (`inert`); G2 gates only `has_disjoint` for DEG/DDEG — the invariance half lives in d31c2's D1 (gated exhaustively there after its round-1 n1 flagged exactly this omission; this is the defect class's second appearance). The lemma is true (leaf births change no non-parent degree, no existing distance, no existing common-neighbor count — reverified here), and "the d31c2 machinery, verbatim conventions" declares the import loosely; but the receipt whose whole point is re-deriving the kills from a *named minimal equation set* should carry or gate-text-cite its full premise. Repair: extend G2's condition with the invariance check (three lines) or cite d31c2 D1 in the gate label.

**m2 — G4's gates are thin relative to their labels.** (a) "verified as an identity on a rational grid (u ≥ 3 …)": `covariant_pair` is invoked at u = 3 only, on a 2×2 grid of which only the two off-diagonal points test the forcing direction; `wb[4]` and `wi[5]` are built and never consumed (dead entries); d31a's A1 instantiated the same identity at u = 2..6 — the restated receipt instantiates *less* than its predecessor while claiming a u ≥ 3 theorem (the general identity is one line of algebra and my H7 probes u = 3..30; the gate should do one of the two). (b) `ok4b` verifies only that 1/(u(u−1)) decreases on u = 3..10 — a triviality standing in for the load-bearing "→ 0 forces c = 0" step (M1). (c) `ok4c` gates only `no_disjoint_at_seed`; the escape kernel's normalization ("2·¼ + 2·¼ = 1") and its covariance ("products 0 = 0 … pure-birth pairs") are comment-ware, never computed. The missing gate was built by this review (harness H6): the escape kernel is normalized at u = 2..11 and satisfies **all 1,357 register-disjoint pair equations on the u ≤ 5 horizon** (648 bi + 121 bb + 588 ii), exactly. And `exp_interacts = (F(1,2))/(F(1,2))` hardcodes both halves rather than deriving 2·w_i(2) and 2·w_b(2) from the kernel — the arithmetic is *right* (p_int = p_birth = ½; E[interacts before first birth] = q/p = 1, geometric; the printed quantity is what its name says), but a theorem receipt should compute it. Repair: adopt the H6-style gate; derive the race probabilities from the weights; widen the identity instantiation or state the one-line algebra in the gate label; delete the dead entries.

**m3 — The pairwise-equation completeness lemma is load-bearing and nowhere stated.** The receipt demands only adjacent incomparable-pair equations. For the kills this is a fortiori fine (each equation is a necessary condition). For the ESCAPE's "covariant" claim (and for the phase's standing choice to test pairs at all), sufficiency needs: any two linear extensions of a finite poset are connected by adjacent transpositions of incomparable pairs, and a swapped adjacent disjoint pair leaves prefix and suffix factors unchanged (disjoint supports commute at state level). Both are true (the first is classical; the second is the C1 bookkeeping), and together they promote "satisfies every incomparable-pair equation" to full linear-extension covariance. One sentence in note-d33 §1 or the receipt header owes this; D34a's "computed two ways, equal" gate implicitly leans on the same lemma.

**m4 — "The obstruction engine is the global-lottery denominator, not covariance" is printed at receipt altitude, and #154 calls it receipt-grade; only half of it is.** What this receipt establishes at receipt grade: the kills do NOT consume the covariance demand's strength — they survive its weakest honest form, so covariance-weakening does not evade the complex. The *positive* attribution (the engine IS the denominator) is diagnosis-by-elimination inside a hypothesis set the receipt did not vary: stationarity, positivity, the birth/interact grammar, and the global normalization all remain unweakened (and each has its own known priced escape — K_flat, D5b, now the seed). The mechanistic reading is well-motivated (every kill row is census growth entering through Z, the global sum a birth anywhere inflates), but no receipt yet exhibits that a locally-normalized law evades the kill — that is precisely D34a's existence claim. Repair: G6/#155 re-scope to "the global denominator is the surviving named lever the kill mechanism runs through; D34a's receipt is what would convert the attribution to receipt grade." D34a should cite the negative half as established and EARN the positive half, not inherit it.

**m5 — HF6 wording and the #154 supersession sentence (adjudicated as tasked, recommendation attached).** (i) Paper 20: no sentence moves — confirmed (see Central Adjudications §3). Silence is *defensible*: §2.3's O7 paragraph is the in-paper caveat, the paper is terminal, and supersession lives in the LEDGER by house convention. Recommendation nonetheless: at the paper's next touch, one clause in §2.3 — "path-covariant in §§2.1–2.2 = the receipts' state-level convention; the refined-gauge operative forms are D33's" — because falsifier (iii) is the one locus where a refined-gauge reader could misread the seed-escape kernel as a falsifier (it is not; (iii)'s covariance is the paper's declared axis). m-level either way; not a reopening. (ii) #154's "the old exact-zero conclusion … is SUPERSEDED at phase scope" sits badly beside d31a remaining green in-repo: d31a is a true theorem at its own (stronger) hypothesis and nothing marks it retracted — the honest relation is COEXISTENCE with a scope note ("d31a stands at its stated hypothesis; at phase scope the operative hypothesis weakens to linear-extension covariance and the operative form is D33's"), and per M1 the exact-zero form is not even superseded in substance on u ≥ 3. Fold into the #155 forward-correction.

**m6 — The G1 dead-code block, graded as tasked: an abandoned false start with a FALSE in-code comment, frozen into a theorem receipt.** Lines 60–66 build `h`, `po`, `incomp_02`, `h_gauge`; none feeds any gate. The comment at line 63 asserts "events 0 and 2 are incomparable (disjoint wires A/z1 vs B/z2)" — false, and instructively so: it applies the disjoint-wires criterion across an intermediate op, violating the gauge lemma's own *at-insertion* qualifier — event 1 = i(A,B) chains 0 < 1 < 2 through wires A and B (recomputed: pred = [{}, {0}, {0,1}]; `incomp_02` evaluates False). The meandering NOTE at 67–73 then discovers this in real time before switching to h2/h3. The gates that run are correct and well-chosen; the fossilized reasoning-with-question-marks is not receipt material. Repair: delete the block, or convert it into content — h is actually a *good* third exhibit (a 3-chain whose every adjacent pair is comparable: gate `po` as a chain), and the absorbed b-b pair (M2) makes a fourth. Same class: G4's dead `wb[4]`/`wi[5]` entries (m2).

## NIT

**n1** — G2's detail print renders the birth register after the word "Delta": "DEG: u = 3 birth, Delta = 2 -> {…}" — the "2" is y (the birth target), not the delta; reads as a value error until decoded. Print "birth at y = 2, Delta = {…}".
**n2** — G3's label "every u ≥ 3 birth" (and G2's search space) is horizon-scoped: u = 5 starts are guarded out (`st2[0]-1 > 5`), so the exhaustive quantifier ranges over u ∈ {3,4} births. The extension to all sizes is real (the pendant-vertex lemma, d31c2 round 1) but the label owes the qualifier — the #145 standard ("'every horizon' qualified at all sites") applies.
**n3** — `canon_history` is factorial in the number of fresh registers (fine at ≤ 2 here; my harness pushed 4). One comment line owed, since D34a will want this canon at depth; and the receipt docstring omits the u ≤ 5 horizon statement its predecessor carried.

## SURVIVES ATTACK

- **The gauge machinery, rebuilt and pushed past the exhibits:** `event_poset` == brute-force transitive closure of the D31B next-op-on-wire generator on all 3,774 enumerated histories (length ≤ 4, seeded A/B); `canon_history` sound AND complete — canon-equality ⇔ (fresh-relabeling ∘ linear extension) on all 1,588 (history, permutation) cases; 220 canon classes on 256 length-3 histories with all 44 in-group pairs equivalent and 300 cross-group samples inequivalent (zero collisions, zero splits). The collision worry (direct-predecessor-only sig) is refuted by construction: the sig carries transitively closed ancestor sets with full typed triples, and per-wire orders are recoverable (ancestor sizes strictly increase along wire chains).
- **"Incomparable ⇔ disjoint wires at insertion":** proven for freshly appended pairs (shared register ⇒ forced wire adjacency; disjoint registers ⇒ no directed path in either order, permanently) and consistent with the frozen primary order (same generator as d31b's `wire_dag`).
- **G2's kills:** both printed rows rebuilt independently and by hand — the u = 3 star (R–A with children B, z), birth at y = 2: DEG Δ = {('b',2):1, ('i',1,2):2, ('i',2,1):2, ('i',2,3):1, ('i',3,2):1}, DDEG Δ = {('b',):1, ('i',1,2):2, ('i',2,1):2, ('i',2,2):1, ('i',2,3):1} — digit-for-digit; each all-nonnegative with positive mass; the incomparable pair (b_2, i(1,3)) exists; the first-sorted search reproduces the same (state, y).
- **G3's contradiction, walked end to end with no u = 2 input:** at the u = 3 star, the disjoint interact i(1,3) has MOTIF class ('i',1,0) before and after the birth (inertness), so the single incomparable-pair equation forces Z(W+b) = Z(W); the delta census {('b',):+1, ('i',2,1):+2, ('i',1,0):+2, ('i',2,0):+2} is strictly positive mass; with all weights positive, contradiction. Reachability of u = 3 under the family's w > 0 convention is immediate. The seed contributes nothing: u = 2 has no incomparable bi pair (gated; re-derived — every interact touches both unsealed registers' set {1,2}, every birth touches one).
- **G4's restated theorem:** survives the clean-room rebuild, strengthened (M1): forcing identity at general u; c = 0 exactly at unbounded growth; the seed escape verified on ALL 1,357 register-disjoint pair equations of the horizon (bi/bb/ii), normalized at u = 2..11; the race arithmetic (p_int = 2·¼ = ½ = p_birth; geometric mean 1) correct and correctly named.
- **G5's content (not its gate):** refined pairs ⊂ state-level pairs (proven), so D5b's exact 558-pair verification covers every refined demand at its guarded scope; d31c2 reruns 7/7 byte-identical; the u-only reduction (connectivity) holds on the horizon.
- **HF6:** paper 20 §2 audited sentence-by-sentence — nothing moves; falsifier (iii) safe at the paper's declared convention.
- **Discipline:** pin 34e68e2 committed pre-run (parent of c71f068); working tree clean at HEAD; the receipt is deterministic by construction at every salt-exposed site and empirically byte-identical across PYTHONHASHSEED 0/7/12345.

## GATES RE-VERIFIED

`python3 code/d33_gauge_reaudit_exact.py` at c71f068: **6/6 PASS, 0.1 s, .out byte-identical to the committed artifact, and byte-identical across PYTHONHASHSEED ∈ {0, 7, 12345}.** Lineage: d31a 5/5 and d31c2 7/7 rerun, both byte-identical to their committed .outs. Referee harness 8/8 (closure; canon equivalence; collision scan; A000081 counts 1/2/4/9; G2 rows; the escape's full pair-equation verification; the general-u forcing + exact-zero consequence; the G5 tautology exhibit).

## FALSE-RESULT COUNT

**Printed numbers: 0 false. Gate flips: 0.** False-as-written sentences: **1 in the .out** — G1's universal "swapping COMPARABLE events (shared wire) yields a DIFFERENT history" (M2; counterexample class exhibited; the gate's own exhibits remain correct). **1 in code comments** (not printed): "events 0 and 2 are incomparable" in the dead block (m6). Misdescription-class (not false, wrong-shaped): the G4/G6 "exact-zero … SUPERSEDED by the asymptotic form" framing and its #154 echo (M1); #154's "receipt-grade" engine attribution (m4). No mathematical result is false at its correct scope; the re-audit's verdicts all stand, one of them stronger than claimed.

## OWED REPAIRS (exact list)

1. **(M1)** Restate G4(b)/G6 with the per-opportunity form first — *w_i(u) = 0 forced for all u ≥ 3 (exact zero survives on u ≥ 3 from incomparable pairs alone); w_i(2) alone is released* — and the density claim as a defined corollary (fraction of interact events → 0 a.s. via the geometric seed block); add a c = 0 gate (d31a-A3-style u* computation); forward-correct #154's "RESTATED ASYMPTOTICALLY"/"SUPERSEDED"/"dropped honestly" wording at #155.
2. **(M2)** G1 label: "a comparable swap not absorbed by fresh relabeling yields a distinct history"; add the absorbed same-parent birth-pair exhibit as a gated canon check (rename ≠ reorder; no equation missed — state in one clause).
3. **(M3)** Replace G5's tautology: gate connectivity + cls_COMP ≡ u-only on the horizon, and/or verify the D5b witness against the refined disjoint-only pair set; otherwise declare G5 a print gate and fix the 6/6 taxonomy line.
4. **(m1)** Gate (or gate-text-cite) the disjoint-interact class-invariance premise inside G2.
5. **(m2)** Adopt the escape-kernel full-covariance gate (normalization u = 2..11 + all register-disjoint pair equations on the horizon); derive the race probabilities from the kernel weights; instantiate the forcing over a u-range or state the one-line identity in the label; delete dead `wb[4]`/`wi[5]`.
6. **(m3)** State the adjacent-transposition completeness lemma (+ prefix/suffix invariance) once, in note-d33 §1 or the receipt header.
7. **(m4)** Re-scope the engine sentence in G6 and #155: negative half receipt-grade (weakest covariance still kills); positive attribution = the named lever D34a must convert.
8. **(m5)** #155 coexistence wording for d31a (stands at its own hypothesis; operative form at phase scope is D33's); paper 20 §2.3 one-clause scope pointer at next touch (recommendation, not a reopening).
9. **(m6, n1–n3)** Delete or convert the G1 dead block (false comment removed either way); fix the "Delta = 2" print to "birth at y = 2"; add the horizon qualifier to the "every u ≥ 3 birth" labels and the docstring; one comment line on canon's factorial cost.

## SUMMARY JUDGMENT

The physics deliverable of D33 is real and verified: the refined O7 gauge is correctly instantiated (its canon is a complete invariant, proven here well beyond the receipt's two exhibits), the no-go complex genuinely survives on incomparable-pair equations alone, the seed-level escape is exactly covariant and exactly priced, and the pinned net effect — weaker hypotheses, same architecture verdict — is CONFIRMED by clean-room rebuild. The round's findings are about the receipt saying less, and twice saying it wrongly: the restated theorem is *stronger* than its own headline (exact zero survives on u ≥ 3; the "asymptotic supersession" framing should be retired before D34a builds on it), one universal in the gauge gate's label is false as written (relabeling absorbs same-parent birth swaps — a fact the phase's own RS anchor predicts), and one of the six gates verifies nothing. Repairs are wording, gates, and one LEDGER forward-correction; no result moves, and the phase's opening claim survives at the corrected width: covariance-weakening does not evade the kills — what remains to be shown, by D34a and not by assertion, is that removing the global denominator does.
