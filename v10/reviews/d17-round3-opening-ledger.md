# D17 hostile round-3 opening ledger

**Round-3 verdict:** mathematical pass; ontology and rebuild major revision.  
**Repair status:** submitted to focused round 4, 2026-07-11.

| Opening | Exact repair |
|---|---|
| Foreign new-element owner admitted | Old owners must be preserved and the new element owner must equal the live-collar owner |
| Foreign collar, memory or carrier could reuse a declared raw edge | Declaration keys now include canonical typed orders and the complete collar type; explicit forged owner/collar/memory controls reject |
| Past-directed two-owner join escaped `precursor` test | Entitlement now counts every old element related to the insertion in either causal direction; a past insertion rejects without exact `A,B` entitlement |
| Raw relation matrices broke construction-order gauge | Typed order keys minimize over all relabelings; a consistently relabeled declared edge now admits |
| Node collars merely resembled a separate D14 carrier | Depth-one/two/three node carriers are exactly `net1.target`, `net2.target`, and `net3.target`; later nodes preserve the complete sealed boundary |
| Network isometry only checked on two vectors | Full matrix identity `V^dagger V=I` is exact |
| Reset completeness inferred | Integrated reset Kraus family now passes exact `sum K^dagger K=I` |
| Causal path marks conflated with visible records after reset | Normal and reset record maps are explicit separate functions of the same unchanged causal path/order |

The physical ceiling is unchanged: these repairs make the supplied finite
grammar owner-safe and gauge-covariant; they do not make the action select the
grammar, kernel, commit, or continuation.
