# D34b repaired round 1 — independent actor-code hostile review

**Target:** commit `59b0421`. **Verdict:** MAJOR REVISION AT EXECUTABLE/REFERENCE WIDTH; IDEAL CLASSICAL LAW SURVIVES. **Count:** 4 MAJOR / 6 MINOR / 3 NIT. **False results:** zero false printed fractions/statistics, zero rerun mismatches; four false-width headlines/subheadlines, two tautological subgates, one reproducible software-cap failure.

Reproduction: both receipts passed and byte-matched under fresh hash salts. Larger attacks preserved heap/scan and remote coupling. PRF kind/partner sweeps were clean; all 40 A6 expected cells exceeded 62.5.

## MAJOR

1. **Ideal continuous law and finite simulator conflated.** The BLAKE2 tape has 2^256 values; waits are discrete and bounded, natural ties have tiny nonzero probability, Decimal rounds, and the grid renewal process is not exactly memoryless. Separate the ideal iid Uniform/Exp theorem from the finite PRF reference. A2 is an implementation guard; A6 is shallow concordance, not equality.
2. **Verifier independence insufficient.** Producer and checker shared `touched()`; actor and oracle shared `physical_event_key()`. Separately code incidence and serialization, or narrow “independent.”
3. **E5/E6 exact width false.** E5's PASS consumed a 100-digit table and 4% tolerance without carrying the analytic asymptotic proof. The time-tail equality and source factorization subgates were tautological. Label the mixed exact/high-precision evidence honestly and carry structural proofs.
4. **Hidden software cap.** Nested Ulam IDs fail at lineage depth 998 through recursive `repr`/hash encoding. Use flat path IDs and iterative length-prefixed encoding; gate depth above 5000.

## MINOR / NIT

- State ideal-law relabeling covariance separately from label-keyed PRF coupling.
- A5 should compare full actor/neighbor/deadline state, not event rows only.
- Add a fixed-time distribution check; A6 is global-depth.
- Replace A7's ad hoc Yule bars with analytic variances and a negative-binomial population control.
- Add kind/partner diagnostics; disclose rejection semantics.
- Narrow “actual independent actors”: one central evaluator atomically updates a shared causal event; no OS mailboxes are implemented.
- Call the hash a counter-addressed BLAKE2 PRF, suppress inert R explicitly, and remove `repr` as ontology encoding.

## SURVIVES

Heap/scan pathwise equality; passive reception; parent-local naming at tested depth; fixed-T/local-ring remote coupling; timed restriction of event rows; rational oracle mass/classes; E1–E4 fractions; Yule formulas; and the ideal Harris/pushforward theorem.
