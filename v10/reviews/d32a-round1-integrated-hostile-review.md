# D32A ROUND-1 INTEGRATED HOSTILE REVIEW (single stream) — 2026-07-12

**VERDICT: MAJOR-REVISION — 2 MAJOR / 7 MINOR / 5 NIT. Zero pinned gates flip; zero false printed numbers (every statistic in the .out/card/LEDGER reproduced exactly, the anchor to the last float64 bit); the full receipt rerun is BYTE-IDENTICAL to the committed artifacts. Both MAJORs are certification-width/record-integrity class: (M1) the card-carried spatial verdict at d = 4 rests on a non-canonical tie-broken "Spearman" against a frame-coordinate ground truth — the frozen rho is not the Spearman of the data, and one coherent instantiation of the pin's own words reads the d = 4 row VACANT; (M2) the P5 census is a silent re-implementation of the d31b grower (different webs at shared seeds) and LEDGER #140's "consistent with #139's 0–3 range" is contradicted by the receipt's own per-web data (max = 4).**

Reviewed at HEAD 7707994. Everything below was rebuilt independently in a scratch driver that imports nothing from the repo (all functions verbatim-copied; receipt path and D30 path carried side by side). The receipt itself was rerun end-to-end from the repo root (928 s) under the card-recorded environment (python 3.8.20, numpy 1.23.5, macOS-26.5.1-x86_64).

## REPRODUCTION (probe results the findings rest on)

- **Full rerun byte-identity:** regenerated `data/d32_instrument_card_v2.json` sha256 = b5e77c0c3196fbd2dd6bcc15924ff8ef... (matches the frozen sha256/16); regenerated stdout byte-identical to `data/d32a_card_v2_and_anchor.out` including the env line; `git status --porcelain v10/data/` empty after the rerun. Card v1 untouched: af5f6b011d3c48f8 re-verified.
- **The anchor is a bit-exact D30 reproduction.** Seed formula: receipt `d32a_card_v2_and_anchor.py:311` = `30260712 + 100*512 + s`, s = 0..9; D30 `d30_frozen_cone_comparison.py:164-165` = `BASE_SEED + 100*N + s + 10**6*index('collar')` = the same integers (collar index 0), NSEEDS = 10 = 10. Kernel parameters: weight structure identical (none = 1, births = U, collar = |{d_cover = 1}| via the same `argwhere`); D30's `w_far` is 0.0 for collar and `tot = ... + 0.0` is IEEE-identical, so the two samplers consume the rng stream identically; EPS/Q are vestigial in the receipt (no far branch). Verbatim-copied D30 `grow`/`max_interval_dim` vs the receipt's `grow`/`max_interval` fallback logic: per-seed d-hat values **bitwise identical**, both means = 1.755910301205. "Reproduced EXACTLY" (LEDGER #140) is TRUE and now independently verified.
- **Order-only audit of `spatial_orderonly` (receipt:91-128):** the estimator column (`est`) is built from M alone — pair pre-filter (comp, common past `M[:,i]&M[:,j]`, common future, lines 98-105), (u,w) sampling, interior extraction, and the chain quantile all order-only; `ts` appears only as `len(ts)` (line 97) and `xs`/`ts` otherwise only in the ground-truth column (line 121). NO coordinate leakage into the estimate.
- **Anchor seed spread (the disclosed means-only gap, filled):** d_MM per seed = [1.8018, 1.9351, 1.7041, 1.7983, 1.7767, 1.7131, 1.7177, 1.5363, 1.7216, 1.8544], mean 1.7559, SD 0.1008 (= D30's printed ±0.10), SE 0.034; d_mid(+1) mean 1.6297, SD 0.1266, SE 0.042; paired diff (MM − mid) = 0.1262, SD 0.1501, SE 0.0475. Max-interval sizes [168, 206, 171, 249, 200, 183, 242, 129, 294, 192] (mean 203 = D30's "~203"; min 129, the size ≥ 64 branch never falls back).
- **P6 margins recomputed:** −0.095910 / −0.023809 / +0.2586 / +0.1173 / −0.3416 / −0.3261 / −0.3407 — all match the .out; the disclosed omitted M²-exclusion margin by hand = **0.123941** (claimed 0.124, correct). Coverage hunt: no OTHER decided verdict is missing from the list (P1's gate is boolean byte-equality, P5 is barless per pin (e), P2's n ≥ 8 domain floor and the |I| ≥ 64 / ≥ 16 estimator branch points are integer-valued and immune to 12-digit rounding). F1 discharge stands modulo the disclosed omission.
- **M²-exclusion rule (probe: verbatim or variant?):** receipt:322 `abs(d_anchor − 2) > 3*cell2["dhat_sd"]` IS D30's corrected clause (i) verbatim (`d30...py:219`), mean-of-10 vs 3× the card's single-sprinkling SD, with card v2 swapped in as pinned. Direction check: this comparison is LENIENT toward the kernel (the mean's own band would be 3·SD/√10 = 0.038); exclusion holds under both readings (0.244 > 0.120 > 0.038), and 9 of 10 individual webs are ≥ 0.120 from 2 on their own (the exception: seed 1 reads 1.9351). No flip under any defensible variant.
- **F4/§5 addendum ("the D30 grower is array-deterministic") verified:** no set/dict iteration feeds the sampler in `grow` (list `uns`, `argwhere` row-major collar pairs) or `event_order` (dict `last` is key-accessed only); confirmed operationally by the byte-identical rerun and the bitwise path equality above.
- **Seed-family collision audit:** cells / box4 / perc / spatial (BASE+777d) / anchor (30260712...) / census (41260712...) occupy disjoint integer ranges — no stream reuse.

## MAJOR

**M1 — the card-carried spatial certification at d = 4 is convention-fragile, and the frozen "rho" is not the Spearman of the data.** Two independent unpinned conventions sit under the P2 verdict:
(i) *Tie handling.* `ranks` (receipt:124-127) assigns tied values distinct ranks by argsort index order. The old D29 proxy was continuous so this never mattered; the NEW estimator's values are quantized integers-plus-tenths (d = 4: **7 distinct est values across 20 pairs** — pin front F2's granularity, now load-bearing). Canonical tie-averaged Spearman on the same 20 pairs: d = 3 **0.849** (frozen card: 0.859), d = 4 **0.799** (frozen card: 0.717). The frozen card rows (`d32_instrument_card_v2.json:224-237`) therefore carry a statistic that is not the named one, off by −0.08/+0.01 depending on cell.
(ii) *Ground truth.* `true` = |dx| (receipt:121-122), the frame-coordinate spatial separation — the D29 lineage convention (`d29_ruler_validation.py:137-138`) — not the Lorentz-invariant proper separation sqrt(dx² − dt²) of a spacelike pair, which is what an order-only (frame-free) instrument estimates. Against the invariant: d = 3 = 0.814/0.799 (canonical/receipt-tie), d = 4 = **0.641 (canonical) / 0.584 (receipt's own tie handling) — the latter BELOW the 0.60 bar**. The d = 4 verdict across the four convention pairs: {0.584, 0.641, 0.717, 0.799} — it straddles the bar, and the minimal-correction instantiation (receipt's code, honest truth column) flips CARD-CARRIED → VACANT.
(iii) *Width.* One sprinkling, 20 pairs: pair-bootstrap (20k) of the pinned statistic gives P(rho* < 0.60) = 0.040 at d = 3 and **0.098 at d = 4**, 95% CI [0.483, 0.917]; the permutation p vs rho = 0 is < 5e-5 / 4e-4 (monotonicity itself is real). The card row carries no CI, no convention declaration, and no domain qualifier (m6).
No pinned gate flips — the pre-registered procedure (lineage conventions) produced PASS and the pin's bar/scope were honored as written. But the instrument D32B will consume is certified more strongly than the data supports at d = 4. Repair owed: carry canonical tie-averaged Spearman; declare the ground-truth convention on the card row and disclose the invariant-truth reading; carry the bootstrap CI; re-validate with more pairs/sprinklings before D32B consumes the row (either outcome remains a result per pin (b)).

**M2 — the census is a silent re-implementation, and #140's range sentence is contradicted by its own data.** `grow_census` (receipt:337-355) differs from the d31b determinized grower (`d31b_two_causal_structures_exact.py:177-200`) in two undisclosed ways: (a) d31b counts the 3 seed ops toward N (253 grown acts at N = 256; the receipt grows 256 acts from an empty list); (b) child names `z{k:03d}` vs d31b's `z{k}` — zero-padding changes `sorted(adj[y])`, hence the option ordering fed to the SAME rng draws. Consequence, verified: at the shared seed family 41260712+s, the receipt's webs are DIFFERENT objects — s = 0..2 give (≥2, ≥7) = **[(40, 4), (36, 1), (42, 3)]** vs d31b's **[(41, 3), (38, 0), (41, 1)]** (my verbatim d31b copy reproduces #139's recorded triple exactly). "The census (O5 extended)" (LEDGER #140, `v8/LEDGER.md:311`) is therefore an extension of the seed FAMILY, not of the recorded census — the two published census datasets are non-commensurable at shared seeds, undisclosed. Worse: my exact reproduction of the receipt's 30-web ensemble (means/SDs match the .out to every printed digit: ≥2 = 41.9 ± 3.4, ≥7 = 1.00 ± 1.10) has per-web ≥7 counts [4, 1, 3, 1, 0, 3, 2, 2, 1, 0, 1, 0, 0, 0, 1, 0, 0, 1, 1, 3, 2, 1, 1, 0, 1, 0, 0, 0, 1, 0] — **max = 4, so #140's "consistent with #139's forward-corrected 0–3 range" is false as a range statement about this data** (18/30 webs have ≥ 1, so "present in most sampled webs" survives at 60%). No gate flips (P5 is barless bookkeeping) and no printed number is wrong; the defect is record integrity — precisely the class #139's M1 established as MAJOR. Repair owed: LEDGER/LOG forward-correction ("observed per-web range 0–4 at 30 seeds"; "re-implemented grower, same seed family, webs differ from B4 at shared seeds"), and D32B pinned to consume THIS ensemble definition (grown-acts-only N, padded names).

## MINOR

**m1 — pin (a) clause dropped: "the card records ... its own generator hash."** Card v2's schema (base_seed, cells, controls, env, schema, sig_digits, spatial_orderonly) contains no generator/code hash of any kind; the printed sha is the hash OF the card, not a recorded provenance field IN it (note-d32:7 vs `d32_instrument_card_v2.json:1-3,217-223`). Silently narrowed pin clause; no verdict consequence.

**m2 — pin (a) "per-cell SDs for every carded statistic" is not met for chain_c/chain_c4.** Every cell and control carries r/dhat/L/ratio3/dmid SDs, but the carded chain constants (receipt:242,254) carry none. Mitigation: SD(chain_c) = L_sd/N^(1/d) is exactly derivable per cell; D32B's mimicry bar must declare that derivation (or v2.1 adds the fields).

**m3 — pin (b) "the minimum's drift with N printed" was narrowed to single-N means.** The receipt measures the naive-min at N = 1024 only (9.15/6.00, printed 9.2/6.0 — verified); no drift-with-N was measured or printed anywhere. F2 is honestly OPEN in §5, but the pinned disclosure itself was not delivered. Repair: the 256/512/1024 naive-min row.

**m4 — P1's "determinism gate" tests serializer idempotence, not the pinned regeneration.** Pin (a): "the card string regenerated in-process is byte-identical." Implemented (receipt:265-267): blob2 = re-serialization of the PARSED blob1 — no recomputation occurs. The implemented check is near-vacuous: over 50k random floats, round12 idempotence never fails (0/50000), so the gate cannot detect a computation-order nondeterminism (the d31b M1 class it was named after). The REAL regeneration test — this review's independent rerun — passes byte-identically, so no consequence; the gate label overstates what ran. Repair: recompute-in-process (or re-word the gate).

**m5 — the anchor concordance PASS is a knife-edge under replication, and the F3 attribution is quantitatively unsupported on M² calibration.** Margin 0.0238 = 0.50 × SE(paired diff) (diff 0.1262 ± 0.0475 SE): approximately 30 percent of fresh-10-seed replications would FAIL the 0.15 bar; simultaneously the MM–mid gap is 2.66 SE from zero — a statistically significant disagreement, PASSed as "concordant". The gate as pinned is deterministic on the frozen D30 seeds (no dishonesty), but the record carries means only (disclosed). The §5/LOG F3 sentence attributes the low midpoint to "the pinned small-|I| bias direction": measured on TRUE d = 2 sprinklings at matched scale (mean |I| = 218 at N = 256 vs the anchor's 203), the total instrument bias is only **−0.017 ± 0.014**, and the +1 endpoint-convention share at the anchor is **−0.0155** (direct with/without comparison: 1.6297 vs 1.6452) — about one-eighth of the 0.126 gap. Direction consistent, magnitude insufficient: on current evidence the anchor's MM/midpoint disagreement is a property of the non-manifold order (or an uncalibrated estimator regime at d ≈ 1.76), not the validated M² bias. This IS the in-round quantification F3 owed; the record sentence should be corrected to carry it, plus the seed-spread SDs (1.756 ± 0.101, 1.630 ± 0.127).

**m6 — the card row schema under-carries the certification.** Pin (b) promised card-carrying as "spatial (order-only, validated)"; the frozen rows carry verdict "CARD-CARRIED" with no domain qualifier (common-diamond pairs — the pre-filter receipt:103-104, disclosed in LEDGER #140 but not in the instrument itself), no convention fields (tie handling, ground truth), no CI. The domain restriction is legitimate (the pin's own estimator definition is over common-diamond pairs — the selection is the estimator's domain, not a cherry-pick; sampling was not starved: 20/20 pairs at 40/86 tries), but it must travel WITH the instrument D32B consumes.

**m7 — the ≥ 7 "cancellation-capable" threshold is alphabet-relative; at the full pinned CS alphabet the necessary count drops to 6 (or 5 with the birth).** Verified identities: asin(3/5) + asin(4/5) = π/2 exactly and asin(24/25) = 2·asin(3/5) exactly, so k·a + j·b + m·(2a) = 2π forces j = 4 and k + 2m = 4 — minimal same-pair touch counts: 8/7/6 interacts, or 7/6/5 interacts for a parent-child pair whose birth carries one rotation. The E5 template (birth + 7 interacts, D28b CANCEL) makes ≥ 7 interact-touches the exact necessary cut at the single-angle grown alphabet (interacts all 9/25, births 16/25) — where, note, TRUE in-record cancellation is impossible (j = 4 b-rotations needed, only the one birth available), so the census is pure potential-alphabet bookkeeping, consistent with its D31 billing. But if any D32B kernel proposes the third pinned gate (576/625), pairs with 5–6 touches become capable and ≥ 7 UNDERCOUNTS — the dangerous direction for exception bookkeeping. Also convention-sensitivity worth one line in the record: the unordered-pair reading of the same 30 webs gives ≥7 = 4.17 ± 1.44 (4×); the ordered convention is the composition-relevant one (co-directed cRy's compose angles) and matches B4 — correct, but currently implicit. (Extends D31 round-1 n4.)

## NIT

**n1** — P5 hardcodes `check(..., True)` and P2 is presence-only; the tally line "ALL CHECKS PASS (6/6 ...)" does not separate substantive from record/print gates (D29/D30/D31 precedent: "3 substantive + 2 print gates"). **n2** — EPS, Q defined (receipt:156) but unused: vestigial D30 constants; harmless for stream identity (collar's far weight was exactly 0.0), worth a comment. **n3** — "fixed-decimal (12 significant digits)" is %.12g = fixed-significant (exponent form below 1e-4; no card value is — verified), and pin (a)'s "platform-stable bytes" overstates: measured 1-ulp boundary flip rate 1.1e-4/value (example: 0.007733080756645 vs nextafter → '...64'/'...65'); the honest rationale is "stable against sub-1e-12 relative cross-platform jitter except at rounding boundaries", and the card's env-recording is exactly the right mitigation (present). The probe's parsed-repr concern is empirically moot: json.dumps(float(12g-string)) == the 12g string in 50000/50000 samples. **n4** — P1 writes the card file (receipt:268-269) before its gate booleans evaluate; a FAIL would leave a stale uncommitted card on disk. **n5** — `N = len(ts)` inside the order-only estimator (receipt:97); M.shape[0] is the order-only source (cosmetic, no leakage).

## SURVIVES ATTACK

The rerun (6/6 PASS, byte-identical artifacts, env matched); the anchor reproduction claim at its strongest possible reading (bitwise per-seed equality of the receipt path against a verbatim D30 copy; seed formula, seed count, trim convention, fallback rule, and weight structure all verified identical); the order-only claim for the spatial estimator (line-audit clean); the M²-exclusion re-verdict under the verbatim D30 rule and under every stricter variant (3SD, 3SD/√10, 9/10 per-web); P6's margin arithmetic and its completeness modulo the disclosed omission (0.123941 by hand, confirmed); the midpoint validation numbers (card d-mid rows reproduced; the +1 convention's bias direction and small magnitude confirmed at −0.016 anchor-scale); the census means/SDs to every printed digit; the §5/F4 array-determinism claim; card v1's freeze; the attempt-1 crash disclosure (bars in the committed receipt match the pin verbatim; nothing frozen at the crashed sha); no seed-family collisions. The spatial rows' frozen rho values reproduce to all 12 serialized digits (0.858646616541 / 0.717293233083) — the numbers are exact; M1 is about what they are numbers OF.

## GATES RE-VERIFIED / FALSE RESULTS / OWED REPAIRS

**Gates re-verified:** 6/6 PASS on this reviewer's independent rerun from the repo root; `git status --porcelain v10/data/` empty afterward (card v2 and .out byte-identical to HEAD 7707994); card v1 sha re-verified af5f6b011d3c48f8.

**False-result count: 0.** Every printed number in the .out, the frozen card, the LOG entry, and LEDGER #140 reproduced exactly (the anchor bitwise). The findings are certification-width, convention, lineage-attribution, and record-wording defects.

**Owed repairs (exact list):**
1. (M1) Spatial rows: carry canonical tie-averaged Spearman (0.849 / 0.799); declare tie-handling and ground-truth conventions on the card row; disclose the invariant-proper-distance reading (0.814 / 0.641 canonical; 0.584 at d = 4 under the receipt's tie handling); carry the pair-bootstrap CI (d = 4: [0.483, 0.917], P(< bar) = 0.098); re-validate at larger n before D32B consumes the row — vacancy remains an admissible outcome per pin (b).
2. (M2) LEDGER #140 + LOG forward-correction: per-web ≥7 range is 0–4 (max 4 at seed 0; 18/30 webs ≥ 1); the census grower is a re-implementation (seed-op count and z-name padding) — same seed family, different webs than d31b B4 at shared seeds ([(40,4),(36,1),(42,3)] vs [(41,3),(38,0),(41,1)]); D32B pinned to the receipt's ensemble definition.
3. (m1) Generator (code) hash into card v2.1, or the pin-clause deviation recorded in the note.
4. (m2) chain_c/chain_c4 SD fields, or the L_sd derivation declared canonical for D32B's mimicry bar.
5. (m3) The naive-min drift measured across N ∈ {256, 512, 1024} and printed (feeds F2).
6. (m4) True in-process regeneration for the P1 determinism gate, or the gate label re-worded to "serialization idempotence".
7. (m5) Anchor seed-spread SDs into the record (1.756 ± 0.101, 1.630 ± 0.127, paired diff 0.126 ± 0.150); the F3 sentence corrected: measured M²-calibrated bias at |I| ≈ 203–218 is −0.017 (+1 share −0.0155) — the 0.126 gap is dominantly NOT the validated small-|I| bias; concordance replication fragility (margin = 0.5 SE) stated.
8. (m6) Card-row schema: domain qualifier ("common-diamond pairs"), convention fields, CI.
9. (m7) Census scope caveat for D32B: threshold exact for the grown single-angle alphabet; necessary count 6 (5 with birth) if the 576/625 gate enters; ordered-pair convention stated (unordered would read 4.17 ± 1.44).
10. (n1–n5) Cosmetics as listed.

D32B remains correctly HELD; nothing in this round licenses opening it before repairs 1–2 (its two direct inputs: the spatial instrument and the census bookkeeping) land.

---

# DELTA-VERIFICATION (round-1 repairs) — 2026-07-12, HEAD 6090d55

**VERDICT: DELTA-CLEAN over the round-1 repair scope — 0 MAJOR / 0 MINOR / 2 NIT (both prose, non-blocking). All ten owed repairs landed faithfully at 6090d55; nothing silently dropped or weakened; the reviewer's second end-to-end rerun is BYTE-IDENTICAL (exit 0, 1253 s; card v2.1 sha 0d33f81d67486a29 unchanged, .out diff empty, git clean on all tracked files); every new number recomputed independently and card-exact. D32B may open on this pass — subject to the separately queued delta-2 item noted at the end, which is outside this pass's scope.**

Scope: the two commits since round 1 — 533b22d (note §6 repair pin + this report, committed verbatim: blob sha 0984031f…) and 6090d55 (rewritten receipt + delta artifacts + LEDGER #141). Pin-before-run holds: §6 precedes the artifacts, and the §6 spec is what the receipt implements — deciding statistic = canonical tie-averaged Spearman vs invariant proper distance, pooled 3 fresh sprinklings × 40 pairs; seeds BASE+777d+{1,2,3} (receipt:310-311); bar 0.60 unchanged (receipt:342); advisory rule "advisory-only if p_below_bar > 0.10" carried on the card row; the replay guard gated inside ok2; vacancy admissible. The pooled domain floor 24 (= the attempt-2 floor, 8 per sprinkling) is an in-code choice — §6 is silent — disclosed in-code and immaterial at 120/120 pairs found.

## Repairs 1–10, verified one by one

1. **(M1) FULL.** Conventions declared on the card row (domain/tie/truth/advisory fields); the deciding statistic implemented exactly as pinned; CI95 + P(<bar) in-card (10k pair bootstrap, seeds BASE+555d); frame reading disclosed (0.824/0.786); the 2×2 convention audit receipt-carried; the attempt-2 stream replay gated. **Resolution by data, independently recomputed:** pooled rho d3 = 0.814058559751 [CI 0.7267, 0.8791], d4 = 0.786629123194 [CI 0.7095, 0.8455] — both card-exact from this reviewer's verbatim-copy driver, as are the per-sprinkling rhos, the CIs, and P(<bar). All six per-sprinkling readings individually clear the bar (min 0.758, d4 sprinkling 1). The round-1 d4 flip scenario is genuinely resolved as 20-pair sampling noise: the 120-pair invariant-canonical reading sits 0.19 above the bar and the old 20-pair readings lie inside the old bootstrap CI. On the certainty WIDTH of "P(<bar) = 0.000", see the queued external item below: the pair bootstrap (and this reviewer's round-1 Fisher cross-check) assume pair independence, but the 120 pairs cluster in 3 sprinklings; the cluster-honest reading is the per-sprinkling spread — d3 mean 0.8148 (SD 0.0311, t(2) = 12.0, one-sided p ≈ 0.0035), d4 mean 0.7909 (SD 0.0346, t(2) = 9.6, p ≈ 0.0054) — so the CARD-CARRIED direction survives the fully clustered reading with p < 0.01 at both dimensions; the overconfidence is confined to the "0.000" precision, not the verdict.
2. **(M2) FULL.** §6 withdraws #140's range sentence and states the observed 0–4; the re-implementation (grown-acts-only N, z-padded names) and the non-commensurability with d31b B4 at shared seeds are disclosed at §6, LEDGER #141, the P5 gate label, and the grower docstring; D32B is pinned to the receipt's ensemble definition; the per-web range 0–4 now prints in the .out; the unordered reading disclosed (4.17 ± 1.44 — matches this review's round-1 predicted value exactly).
3. **(m1) FULL.** generator_sha256_16 = 2112030a1e991fba in-card; verified equal to sha256/16 of the committed receipt file.
4. **(m2) FULL.** chain_c_sd / chain_c4_sd present in all 9 cells and 6 controls (= L_sd/N^(1/d), the correct constant-divisor propagation); P1 gates their presence.
5. **(m3) FULL.** F2 naive-min drift row at N ∈ {256, 512, 1024}: d3 6.80/7.85/9.25, d4 4.55/5.40/5.80 — recomputed exactly; in-card and printed; the LOG draws the honest conclusion (grows with density; not a stable proxy).
6. **(m4) FULL.** P1 relabeled serialization-idempotence + a state-leakage recompute (d2_N256 rebuilt in-process, byte-compared); cross-process byte-identity explicitly credited to the round-1 independent rerun; the in-code comment states what the gate cannot see.
7. **(m5) FULL.** Anchor prints carry seed spreads (1.756 ± 0.101, 1.630 ± 0.127, paired diff 0.126 ± 0.150, SE 0.047) and the fragility statement (margin = 0.50 SE) inline; §6 supersedes the §5 F3 sentence with this review's measured numbers (−0.017 ± 0.014 bias at matched |I|, +1 share −0.0155, the gap a real 2.66-SE property); P4's label and detail carry the corrected attribution.
8. **(m6) FULL.** Card row schema: domain qualifier, both convention fields, CI95, P(<bar), n_pairs/n_sprinklings, per-sprinkling rhos, the frame disclosure, the 2×2 audit, the advisory rule.
9. **(m7) FULL.** P5 label + §6 carry the alphabet scope (≥7 exact at the grown single-angle alphabet; necessary count 6, or 5 with the birth rotation, if the 576/625 gate enters) and the ordered-pair convention with the unordered reading printed.
10. **(n1–n5) FULL.** Tally-line gate taxonomy; EPS/Q vestige removed; %.12g named fixed-significant with cross-process identity credited rather than claimed; card written only on P1 pass; N = M.shape[0].

## Specific probes

- **FROZEN_LEGACY honesty:** 0.858646616541 / 0.717293233083 verified equal to the attempt-2 card rows at `git show 7707994:v10/data/d32_instrument_card_v2.json`. The replay path is genuinely the attempt-2 stream: `spatial_pairs` preserves the draw structure exactly (the only additions are non-rng arithmetic for the second truth column); the recomputed legacy statistic lands within 3.5e-13 (d3) / 2.9e-13 (d4) of the frozen 12-digit values — the 1e-9 gate tolerance is correctly scaled to the 12-digit freeze.
- **The 2×2 reconciliation (the "±0.001"):** NO discrepancy — all eight audit values are bit-identical to this review's round-1 scratch numbers (e.g. canonical_frame d4 = 0.79847083392, receipt_tie_invariant d4 = 0.583458646617). The apparent ±0.001 was print precision only (round-1 driver 4dp vs receipt 3dp). See n6.
- **Record chain:** #141's supersessions accurate and complete — card sha chain b5e77c0c3196fbd2 (7707994) → 0d33f81d67486a29 (recomputed on the committed file and on the regenerated one); census range withdrawal at §6 + #141; F3 supersession at §6 + #141 + P4; "cell/control statistics unchanged from v2" verified field-by-field (every shared field byte-equal; the delta is purely additive: chain SDs, generator sha, drift block, rewritten spatial rows). No overclaim found in #141 or the LOG entries, with one prose exception (n6).
- **New-defect hunt:** `avranks` is canonical average-ranks (bit-agreement with this review's independent implementation); `bootstrap_ci` is a faithful pair bootstrap of the deciding statistic; the new seed families (deciding BASE+777d+{1,2,3}, bootstrap BASE+555d, drift BASE+777d+10N) are collision-free against every existing family (cells, controls, legacy spatial, anchor, census) by direct arithmetic; `build_cell` reproduces the v2 cell statistics exactly; the write-only-on-pass path is correct (the in-memory card still feeds later gates; exit 1 preserved).

## Findings (both NIT, non-blocking)

**n6 — LOG prose mis-attribution:** the delta LOG entry says the 2×2 audit matches "the review's scratch numbers to ±0.001, their driver's rank-tie implementation detail". Wrong attribution: the values are bit-identical; the difference was 4dp-vs-3dp printing. No number is wrong anywhere; one sentence mischaracterizes the reviewer's driver. Correct at the next LOG touch (no supersession owed).

**n7 — "replayed bitwise" wording:** the replay gate compares the recomputed legacy rho to the FROZEN 12-DIGIT card value at 1e-9 (receipt:307), not bit-to-bit — necessarily, since the freeze stores 12 digits. The stream is structurally bitwise-identical and the measured gap (≤ 3.5e-13) confirms it; a one-clause tightening ("stream-identical; value equal to the 12-digit freeze within 4e-13, gated at 1e-9") would make the print exact. (Pedantic corollary: the replay verdict is a decided gate not in P6's margin list, but it is not serialization-flippable, so P6's stated scope does not owe it.)

## Externally raised, queued — NOT a finding of this pass

After this delta pass began, a second external review (filed as `reviews/d31-d32-external-user-review-2.md`, untracked at verification time) raised the P2 cluster-dependence point: the pair-level bootstrap treats the 120 pairs as independent although they cluster within 3 sprinklings, so "P(<bar) = 0.000" is overconfident. This reviewer's cluster-honest check (above) confirms the CRITIQUE is valid as a width statement and the VERDICT survives it (p ≈ 0.003–0.005 at df = 2). The coordinator has adopted a cluster-aware re-validation (12 fresh sprinklings × 10 pairs per d, sprinkling-level bootstrap, bar unchanged) as a delta-2 cycle to be pinned as note §7 — §7 is not yet present at HEAD 6090d55 and the delta-2 receipt has not run; neither is verified or graded here. The DELTA-CLEAN verdict above covers the ten round-1 repairs as committed at 6090d55; it does not pre-clear delta-2.

## Gates re-verified / false results / repairs owed

**Gates re-verified:** 6/6 PASS on this reviewer's second independent end-to-end rerun (exit 0, 1253 s, card-recorded environment); regenerated card v2.1 and .out byte-identical to HEAD 6090d55 (`git status` clean on tracked files); card v2.1 freeze verified at 0d33f81d67486a29 on both the committed and regenerated artifacts; card v1 lineage untouched.
**False-result count: 0** (again — every number in the .out, card v2.1, LOG, and #141 reproduced exactly; the two NITs are prose).
**Repairs owed: none blocking.** n6/n7 are next-touch cosmetics; the cluster-dependence item is queued as delta-2 per the coordinator (§7 pin pending).

**DELTA-CLEAN. D32B opens on this pass, with the delta-2 cycle running per its own pin.**
