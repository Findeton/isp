# HOSTILE REVIEW — PHYSICS/FOUNDATIONS stream: v10 D28 (round 1)

**Reviewed:** 2026-07-12. Objects: note-d28; both d28 receipts + outputs; LOG D28 entry; paper 19 §9 F12 pointer. Receipt integrity: both rerun, exit 0, byte-identical; every printed exact value independently reproduced in a clean-room complex-capable density-matrix engine. Zero false receipt results. The four MAJORs are definition-, scoping-, and wording-level, each with a receipt-checkable exhibit.

## MAJOR

**M1 — The pinned influence relation is not antisymmetric on kernel-generated webs, through its no-op arm; the two receipts already disagree about the alphabet.** The pin puts no-op in A and sups over all pairs. Exhibit (exact, real, {Z,X}): web r0 births c0 (g=576/625); interact(r0->c0, g=16/25). Querying r0: X = 53/250 under no-op at c0 vs 3629/6250 under EVERY replacement — I(c0->r0) = 1152/3125 > 0 with the no-op pair, 0 active-pair-only. Mechanism: replacement is marginal-invisible immediately (partial-trace theorem — why T1's zeros hold) but becomes visible when the parent RE-TOUCHES the child as a control (back-action reads the destroyed correlation). Both kernels re-touch generically (under K_collar every interact is a record-edge re-touch). Gives 2-cycles; J+ then not the up-set of any partial order; D29 rulers inherit an ill-posed input. Receipt 2's spot_influence quietly uses a no-op-free 3-prep SUB-alphabet — accidentally the right definition, formally a different relation from receipt 1's. Nothing gated is false (receipt webs never re-touch; full-alphabet recomputation confirms all zeros). **Correction:** re-pin I over ACTIVE pairs; carry no-op-vs-active separately as the DISTURBANCE relation (this exhibit as its receipt); harmonize receipt 2 with the pin.

**M2 — The real restriction is load-bearing for the influence ORDER; the complex extension destroys the load-bearing zeros (exact exhibits).** Complex preps (3/5, 4i/5), (5/13, 12i/13) + Y-query, active pairs: diamond I(X->B) = 110592/390625 > 0 (closure says X's row is empty); S_3 web I(b1->a3) = I(b2->a3) = 3456/15625, I(b3->a2) = 2592/15625 — 2-cycles with a3->b1: the complex relation is not a poset; the operational dim >= 3 reading does not survive enrichment. Mechanism (carry as the KICKBACK LEMMA): under controlled-R_theta the control's coherence block acquires Tr[rho_T R_theta†] = cos(theta)Tr[rho_T] - sin(theta)Tr[rho_T J]; real symmetric rho_T kills the J-term (SO(2) abelian => state-independent factor — also why T3b's ledger is exactly x7/25); complex targets restore it — a controlled gate is a two-way channel (BLO Def 4.1, universally quantified, already implies this). Trees immune in both families (targets born fresh in |0>) — T1/T2 are the alphabet-robust theorems; the diamond's A||B survives even complex-enriched (verified 0). **Correction:** kickback lemma added; T3a matrix-equals-closure and N10b's "operational influence order" scoped to (real family, {Z,X}); the asymmetry sentence: positive entries are alphabet-robust lower bounds, zero entries are alphabet-relative certificates; state which zeros are complex-robust (spectator, tree) and which are not (target->control).

**M3 — "Diamond-forming CROSS-BRANCH interaction is NECESSARY" is false as worded (K_collar-legal counterexample); T2's hypothesis clause admits a false reading.** Exhibit (exact, real, all zeros complex-robust): A root; z1, z2 born from A; interact(z1->A); interact(z2->A); z3 born from A. I(z1->z3) > 0, I(z2->z3) > 0, I(z1<->z2) = 0 both ways (also complex-enriched) — an operational diamond with ZERO cross-branch interactions: every interact is along an existing record edge (a MAILBOX ancestor: two siblings write on the parent, a later sibling reads). (i) The conclusion sentence at note §3 T2, the receipt-1 print, and the LOG is wrong — what T2 proves necessary is POST-BIRTH INTERACTION PER SE (the opportunity clause), not cross-branch coupling; (ii) T2's stated hypothesis "with no later cross-branch interactions" is refuted under the literal reading — the true hypothesis is "no post-birth interactions of any kind" (what the receipt actually tests). **Correction:** reword hypothesis + conclusion at all four loci; add the mailbox web as the second repair route; note K_collar (interacts provably never leave record edges) still repairs the obstruction.

**M4 — "Covariant" (the goal's word) is delivered as single-transposition relabeling invariance, while both kernels FAIL the Rideout-Sorkin covariance analog — the defining condition of the named priority neighbor; undisclosed.** RS discrete general covariance (verified from gr-qc/9904062): the net probability of forming a causet is independent of birth order (path-product equality). Exhibit (exact): under K_collar, birth(A) then interact(A,B): p = 1/40; interact(A,B) then birth(A) — same grown graph — p = 1/25 (K_tail: 2/81 vs 1/25). The kernels' growth measure depends on accretion order: the global step clock is physical in them. Also: RS births take arbitrary partial stems (MULTI-PARENT) — common futures are NATIVE in RS growth; D28's obstruction exists only relative to the corpus's one-parent birth clause; the priority paragraph must say this. **Correction:** define the note's covariance term against RS's; print the path-covariance failure as an honest delta — or argue flags-observable makes path-equality the wrong requirement (defensible; then say it); widen N2 beyond a single transposition; pin "does an NSE-compliant, RS-path-covariant opportunity kernel exist?" as the successor front (O2) — the honest first shot at the kernel-selector problem.

## MINOR

m1 T1/T2 are census-scoped receipts carrying general-forest assertions — add the 5-line general proofs (positive side: do(|1>) vs do(|0>) gives TV = prod(path g); zero side: fresh-|0> births give state-independent kickback + disjoint supports; T2: ancestor sets are chains in a forest). m2 "the corpus's S_n theorem" sources the M4-exclusion premise to v9-resident measurement-grade material, brushing the §0 insulation — carry the 5-line antipodal M3 construction as [EXACT-provable] or [LITERATURE], not "the corpus's". m3 Busch citation precision (root D25 F2/D27 §1; inherited by paper 19 §3, P18 §8 F1, D22 §6 F1): Busch Thm 1 (positive TP) = mixtures of unitary-or-ANTIUNITARY embeddings; the "mixtures of isometries" form is Thm 2 (the COMPLETELY POSITIVE case). The corpus states Thm 1's hypothesis with Thm 2's conclusion. Invisible on the real family; add "completely positive" (or the antiunitary caveat) at all five loci. m4 A0c: "or" -> "with" (Busch needs orthogonal ranges AND preparation-independent weights); N6 gates only the weight half; the recording/orthogonality half is declared, not constructed; the unrecorded-lottery claim needs its 3-line D25-style exhibit (a preparation-independent lottery over NON-orthogonal isometries strictly contracts a declared pair). m5 goal-word gap "influence laws distinguishable": N9 separates flag+click laws; add the STATIC-METRIC LEMMA (births add leaves, collar-interacts add parallel edges => d_cover never decreases => under K_collar influence at d >= 2 has probability 0 forever; under K_tail positive) — a theorem-grade separation of the influence laws proper (274/274 realized collar-interacts on record edges at depth 3). m6 priority-delta hygiene: delta (iii) is D26 E4's deliverable — attribute; delta (ii) is flag-carried — rename observable-history identifiability; pin click-only kernel identifiability as F7's substance + the on-support caveat. m7 N4 tautological given N1 (Ionescu-Tulcea) — label structural. m8 the g=0 mismatch never named: record edges at g=0 are influence-invisible — record graph and influence order differ on silent subgraphs; D29 rulers must declare which order they read. m9 K_self postponed but never defined — one defining sentence. m10 N10b zeros certified against a 3-prep no-op-free sub-alphabet (code-comment-disclosed only) — upgrade to the full 30-pair matrix at the pinned active alphabet.

## NIT

n1 bare "NECESSARY" -> "for common operational futures". n2 mirror-DFS: add the 4-line proof + the standard anchor (forest orders have dim <= 2); 300 random trees pass. n3 synergy: add the additive-mixture lemma (do-interventions sever switch correlations); scope as sufficient witness (tuned angles can false-negative). n4 "local-clock form" is a misnomer for N6 (preparation-swap invariance; the clock is in fact global per M4). n5 N3's nonzero-null is an unexplained modeling choice; the none op is the global tick made visible. n6 "both alphabets are printed in the receipts" — receipt 2 prints neither. n7 "minimal": state the content (zero interactions provably fail; one suffices).

## FRONT VERDICTS

F1 PROVEN with M2's scoping owed ({Z,X} complete for real states — 2-line proof; complex enters through interventions; what changes is the ORDER). F2 TRUE, proven (n2). F3 RESOLVED — recording is NOT an over-constraint: preparation-independence alone is not Busch-sufficient (non-orthogonal lottery contracts); orthogonal ranges = in-principle perfect recoverability of k, so "recorded" IS the orthogonality half; N6 matches D27 B3 exactly. F4 adequate but tautological (m7). F5 SURVIVES (confounders cannot produce the profile difference; do-severed switches are additive). F6 all four sources fetched: RS accurate but incomplete in two load-bearing ways (M4); BLO Def 4.1 = channel factorization universally quantified — deltas genuine, with the caveat that finite-alphabet TV is a LOWER BOUND on BLO-influence (D28-zeros do not imply BLO-no-influence); RW exactly as characterized ("naive spatial distance and its failure" is their own section title). F7 honest pin; add on-support caveat + click-only variant.

## FOUNDATIONS

(a) Unconditioned replacement without postselection is the right active class; the defects are the no-op arm (M1) and the unnamed g=0 mismatch (m8). Deep point: with end-of-history queries, registers are WORLDLINES, not events — re-interaction makes the register-level relation cyclic physically (c0->r0 is genuine signalling). Either resolve queries in time (O1) or quotient. (b) No "necessary for spacetime" overclaim found — the defect is "cross-branch" (M3). (c) Intrinsic graph locality consistently named; K_self debt recorded but undefined (m9). (d) Clean: no geometry claim, no V9 reuse (one sourcing brush, m2), D12 graveyard respected, paper 19 F12 accurate.

## GOAL COMPLIANCE

Conjunct 1 delivered (T2; general-proof debt m1). Conjunct 2 delivered at census+assertion grade (m1). Conjunct 3 delivered with M3's mischaracterization (mailbox route missing) + n7. Conjunct 4: constructed, complete, finite, distinguishable at observable-history grade; "covariant" carries M4's gap; "influence laws" carries m5's gap (repair supplied). Net: delivered-with-repairs; no conjunct falsely claimed; two goal words ("cross-branch" implicitly, "covariant" explicitly) not yet earned at exact grade.

## SURVIVES ATTACK

Both receipts byte-identical; every exact number independently confirmed; the full 6x6 S_3 matrix equals the standard example under the pinned alphabet; the diamond's A||B robust even complex-enriched; T1/T2 tree zeros alphabet-robust (fresh-target mechanism, both families); K_collar interacts provably never leave record edges; the mailbox diamond's incomparability complex-robust; {Z,X} completeness a theorem; mirror-DFS true; N9 2/405 exact; the triage LOGIC correctly the repaired v9-#119 form; insulation held everywhere except m2's sourcing; D29/D30 literature legs accurate; D27 B3 alignment exact.

## OPENINGS

O1 EVENT-RESOLVED influence: define I on (register, epoch) pairs; conjecture: real family + active alphabet + event resolution => influence = time-respecting op-chain reachability — repairs M1's cycles and gives D29 a well-posed order. O2 THE RS-COVARIANT NSE KERNEL: does any opportunity kernel satisfy RS path-independence + the D28 battery? The none op obstructs; without it path-product equality is a functional equation on weights — the D28 analog of RS's derivation and the honest first shot at F12; if flags-observable makes path-equality wrong, prove that instead — either outcome is a theorem. O3 the Bell-causality gate (K_collar trivially satisfies the RS ratio condition; K_tail's ratios growth-static — gate both). O4 click-only kernel identifiability (flags dropped) — F7's substance. O5 mailbox structure as a D30 discriminator (collar: all diamonds ancestor-mediated; tail: direct spacelike coupling too); open sub-question with triage stakes: is K_collar's dimension ceiling above 2? (Not pure birth, so N10a does not exclude it; nothing pins it either way.)

Sources: Rideout-Sorkin gr-qc/9904062; Barrett-Lorenz-Oreshkov 1906.10726; Rideout-Wallden 0810.1768 + 0905.0017; Busch 1999 (arXiv:1303.6624; Math. Phys. Anal. Geom. 2, 83).
