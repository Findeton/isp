# HOSTILE REVIEW — MATHEMATICS/REBUILD stream: v10 D23 / D22 / Paper 18 (round 1)

**Reviewed:** 2026-07-12.  Objects: `v10/note-d23-click-identifiability-and-the-real-interaction-graph.md`, `v10/code/d23_click_identifiability_exact.py`, `v10/relativistic-isp-v10-paper18-no-silent-erasure-and-the-identified-click-law.md` §2–§4. Repo untouched; all runs in scratch copies. Rebuild artifact (scratch): independent construction — sparse-dict states, per-edge couplings, closed-form tables; ALL SECTIONS PASS.

## Provenance and rerun (front 1)

- Commit order verified: `de85a64` (pin: note + code, pre-run) → `81c7d86` (run 5/5; touches **LOG.md only**) → `0298055` (paper 18 + README + LOG). Code and note at HEAD are **byte-identical** to the pin commit. Rerun in scratch: **5/5, exit 0**, output matches the committed claims.
- Clean-room rebuild: all 12 full 8-outcome distributions (2 graphs x 6 grid couplings) **identical** to the receipt's. Closed forms confirmed as *identities* by exact 6-point Lagrange interpolation (probabilities are polynomials of degree <= 2 in the coupling g = sin^2, so 6 points overdetermine): P(B=1) = (16/25)g for both graphs; P(C=1) = (16/25)g star, (16/25)g^2 chain. Bit conventions, `cry()` construction, and normalization independently verified correct.

## Findings

**MAJOR-1 (note §1, Leg A headline — false as written; receipt unaffected).** "the observation map (graph, coupling) -> click law is injective on an exact finite witness family" — with the family as §2 defines it (grid *including* s in {0,1}), injectivity **fails at both boundary points**: star(0) = chain(0) = {9/25 |000>, 16/25 |100>} and star(1) = chain(1) = {9/25 |000>, 16/25 |111>} (rebuild R3; necessarily so — s=0 is no interaction, s=1 a deterministic flip cascade). The receipt's own gates are correctly scoped (gate ii interior-only; gate iii within-graph), so this is claim-transcription class, zero false receipt results. **Exact correction:** "…injective on {star, chain} x (0,1) jointly, and within each graph on all of [0,1]; the two graphs necessarily coincide at s in {0,1}." (Rebuild also proves the *stronger* interior statement: star(g) != chain(g') for **all** interior pairs g != g', not just equal couplings.)

**MINOR-1 (s-convention slip, code print vs prose — the pre-flagged front, confirmed).** Note and paper use s = sin^2 throughout ("P(B=1) = s*16/25"). The receipt's gate (iii) **printed line** uses s = sine: "P(B=1) = s^2 * 16/25 … linear in the coupling s^2", while its gate (ii) line uses s = sin^2 ("star … = s*16/25"). The code's two printed messages therefore use "s" with two different meanings, and stdout-vs-note diffing shows s vs s^2 for the same statistic. Each is internally correct; the cross-artifact convention diverges. **Exact correction:** in the line-106–108 message, rename to the coupling variable: "P(B=1) = g*16/25, g = sin^2 (the note's coupling s)".

**MINOR-2 (receipt-carried vs prose-carried, gate ii).** Note: "for every s in (0,1), the full click distributions … differ"; paper §3: "differ at every interior coupling; the closed-form separator is verified exactly." The committed receipt verifies the full-table inequality at the **4 interior grid points** and the closed forms at **one** point (g = 16/25, line 89). The universal (0,1) statement rests on the closed forms as identities — true (3-line hand proof; pinned by the 6-point fit) but not receipt-carried. **Exact correction:** check the closed forms at all 6 grid points and print the degree-<=2 argument, or soften to "at every grid interior coupling; the closed form (elementary identity) extends to all of (0,1)".

**MINOR-3 (D19 reconciliation — right conclusion, mislabeled axis).** (a) Note §1 "reversing D19's nonidentifiability" — nothing is reversed; D19's theorem stands, and D23 instantiates D19's **own carve-out** (theorem note: "Independent evidence can still identify a restricted candidate family if the observation map is injective on that family. The restriction itself must be justified independently"). (b) Note §1 says "identifiability returns at **richer click data**" while §2(iv) says the identifying statistics are "**one/two-record** CLICK probabilities" — internal tension, and §2(iv) is the correct one: for the witness family the data order is the *same* as D19's shadows (one-record suffices at shared coupling; per-edge needs the two-record (B,C) joint — rebuild 4a). The operative mechanism is the **instrument-justified family restriction** ("instrumented" = the independent justification D19 demanded), not richer data; complete-support data is needed only for the coherence clause (D21). **Exact correction:** replace "identifiability returns at richer click data" with "identifiability returns on an instrument-justified restricted family (D19's own carve-out), at the same one/two-record order; only the coherence clause needs complete support (D21)"; change "reversing" -> "reconciling" (the paper's abstract already says "reconciling").

**MINOR-4 (paper §3 closing overreach).** "Identification of **every clause** of the click law is therefore an experimentally posed problem" — record semantics is stipulated/synthesis (D18/D22), carrier birth is open (D20, §6's own ceiling). **Exact correction:** "the interaction and coherence clauses".

**MINOR-5 (note §3 quantifier).** "clicks identify both (Leg A)" reads as if Leg A covered the real graph; Leg A licenses a witness-family CAN-claim. **Exact correction:** "clicks *can* identify such data (Leg A witness); the world's click record *has* identified them (Leg C)". (Paper 18's abstract is correctly bounded; the note is the loose one.)

**MINOR-6 (F4 status — front 3 verdict).** The two-line proof of "exactly one equivalence class at the coherence clause" **exists, but only after two definitional commitments**: (D1) clause-equivalence := equal intrinsic complete-support suppression (paper 17's eta/Mermin invariant — purity/spectrum change of the declared closure); (D2) "intrinsic" := non-unitary on the declared closure. Then: [L1] NSE(i) (closure unitary) => intrinsic complete-support suppression == 0; [L2] that is precisely the Q-class invariant — realized by Q (nonempty), violated by C and every intrinsic-collapse generator (paper 17 §7: <M> = 4 eta -> 0). Under (D1)–(D2) the selection is **quasi-definitional** (clause (i) nearly restates Q-class), and note the proof uses clause (i) *alone* — the "equivalently (i)-(iii)" framing overstates internal structure. The substantive open problem is **F1, not F4**: for a generator presented as a bare channel, "received by records" cannot mean "admits a dilation" (Stinespring makes that vacuous — *every* CPTP map dilates, including C's Delta_Z); the in-ontology record-sector dilation criterion must be formalized so C provably fails it (paper 17 §6's all-37-shadows-equal is the right raw material: a genuine in-ontology receiver would be a proper shadow that differs). The easy half (NSE does not distinguish the D12 dials) is genuinely one line each: both iSWAP angles are unitary closures; both P_r twins are realizable with dispersal-only sealing. **Exact correction:** in paper §2, state the class claim relative to the invariant: "…selects exactly one equivalence class *in the complete-support-suppression invariant* (NSE(i) forces it to zero; Q realizes it; C and collapse generators do not), with 'received by records' still to be formalized (F1) before the claim exceeds the invariant restatement."

**MINOR-7 (falsifier attribution asymmetry — D22 §3 / paper §2, F2-adjacent).** "Residual == 0, no new constants" sits in tension with the rehabilitation clause: an NSE-*compliant* rehabilitated collapse (physical receiver sector, e.g. gravitational) produces a nonzero residual until that sector is discovered and independently calibrated — so a "blind nonzero residual" falsifies NSE only after attribution to every physical receiver fails. One-sided as all collapse-search logic is; say so. **Exact correction:** "a nonzero residual **that survives attribution to every independently calibrated physical receiver sector** falsifies the principle."

**NITs.** (n1) Dead code: `kron()`, `I2` unused; `clicks()` local `n = 3` unused. (n2) Positivity gate vacuous (squares of rationals). (n3) "5/5" counts two print-only checks ((iv), (v)); substantive gates are 3 — per receipt-count hygiene, label "3 gates + 2 prints". (n4) "sufficient statistic" is nonstandard here; "identifying statistic". (n5) Paper abstract "the coupling map is exactly linear, hence injective" — the coupling->law map is polynomial (chain P(C=1) is *quadratic* in the coupling); only P(B=1) is linear; fix: "the coupling is pinned by an exactly linear statistic". (n6) The single closed-form check point g = 16/25 numerically equals the prep probability p = 16/25 — the one grid point where a p<->g role-swap bug would pass; add g = 9/25 (star 144/625, chain 1296/15625). (n7) "Graph" is really an *ordered* edge list: chain applied [B->C, A->B] gives a different law (== single edge A->B) — state "edges applied in the listed causal order" in note §2 (star is order-invariant; within the witness family the code pins the order, so this is definitional hygiene).

## Counterexample hunt and the honest identifiability class (front 2, F7/F1)

All receipt-demonstrated in the rebuild:

1. **The tuned fake fails at the joint, not the marginals.** Star(u, v=uw) matches chain(u, w) on **both** one-record marginals P(B=1) and P(C=1), yet the full tables differ: star P(A=1,B=0,C=1) = p(1-u)v = 20736/390625 > 0 vs chain == 0 — an algebraic support obstruction; an 11^4 dense per-edge rational search finds **no** interior fake. The s_star = s_chain^2 fake dies at the two-record (B,C) joint. Leg A for this pair is *stronger* than claimed.
2. **Silent-edge fake — the general claim's true boundary.** [A->B] and [B->C applied first, then A->B] are distinct edge sets with **identical click laws at all couplings** (the B->C control never leaves |0>; w is unidentifiable). So "clicks identify the interaction graph" is false for arbitrary circuits: clicks certify exactly the **activated** causal subgraph. Never-activated structure is unrecorded structure — No Silent Erasure's own contrapositive at the identification layer.
3. **The honest general statement (F7, receipt-ready).** For rooted controlled-Ry **trees** (root = the prepared register, each register targeted once, edges in topological order, per-edge couplings g_e in (0,1)): the Z-click law equals the classical percolation cascade exactly, and (tree, {g_e}) is recoverable — ancestors(v) = {u : P(X_v=1, X_u=0) = 0}, couplings from conditional ratios. Verified exhaustively: all rooted trees on 3 registers x all interior grid couplings per edge, and all 16 rooted trees on 4 registers, **zero collisions across the entire class**. Boundary is exactly threefold: boundary couplings (MAJOR-1), silent edges (item 2), and non-tree in-degree >= 2 targets, where angle addition breaks the cascade — triangle P(C=1|A=B=1) = sin^2(theta1+theta2) = 576/625 vs classical OR-cascade 369/625.

## Survives-attack

Pin-before-run commit order; byte-identical pin; exact rerun 5/5. All 12 distributions independently rebuilt and identical; closed forms hold as polynomial identities; within-graph coupling injectivity on [0,1] valid via the linear statistic; interior graph separation holds in the strengthened cross-coupling form; no per-edge fake exists for the pair. Leg B citations check out (paper 13 §8 boxed disintegration formulas; D18; D22 explicitly POSITED). Leg C wording ("identified, not derived"; PDG-class values; §6 ceiling; V9 untouched) holds — F3/F6 concern confirmed non-realized. The D23 receipt's mathematics is sound; every defect found is quantifier/convention/bookkeeping class.

## OPENINGS

1. **Tree theorem receipt (closes F1/F7):** promote Leg A from one witness family to the characterized class above — cascade reduction + ancestor-set recovery; the rebuild's R5 is adaptable as `d24_tree_identifiability_exact.py`.
2. **Silent-edge lemma as the §2<->§3 bridge:** paper 18's two halves currently never touch; "click-invisible = never-activated = unrecorded structure" makes the principle and the identifiability receipt one statement, and puts the honest quantifier on "the *real* interaction graph" (= the activated low-energy graph; UV openness becomes the special case).
3. **Non-tree/D24:** in-degree-2 targets expose relative coupling **signs** invisible to tree clicks (trees see only g = sin^2 theta — worth stating that the note's coupling coordinate is exactly the maximal tree-identifiable one); DAG identifiability open in both directions.
4. **F4 closure recipe** per MINOR-6 (define the invariant, two-line proof, move substance to F1's in-ontology dilation criterion) plus the falsifier attribution caveat (MINOR-7).
5. **Receipt upgrade batch:** closed forms at all grid points + degree-bound line; non-degenerate check point; print-convention fix; gate/print count relabel.
