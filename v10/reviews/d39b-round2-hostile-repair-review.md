# D39b round-two hostile repair review

**Frozen target:** commit `b3ce58c`.
**Receipt:** `PASS 10/10`, complete-output SHA-256
`309aeec45af0ea337b97e916c897ebb1c7808a7e78421f808f64b92c5f9575ce`.
**Lanes:** category/causal software; probability/selection; corpus/physics
scope.
**Verdict:** `0 BLOCKERS / 1 MAJOR / 2 MINOR / 1 NIT`.
**Core result:** survives.
**Promotion:** withheld for the finite comparison-object collision.

Six fresh hash-seed runs reproduce D39b byte for byte.  The round-one
foreign-edge-source and forged-partial-release exploits now reject and leave
their ledgers unchanged.  Independent modular elimination over primes
`1000003` and `1000033` gives rank 246 for the 344-by-401 action matrix,
matching exact rational elimination.  The depth-three normalized completion
kernel independently gives `(21 normalizations, 78/78 squares, 179 terminal
paths)`.  The 47-history/410-proposal certificate census, orbit-level
transport rows, 49 typed O-U laws, ten silent-step controls and pinned-alphabet
response arithmetic all reproduce.

## MAJOR M1 — the claimed D37 embedding collapses six source objects

D39b validates all 38 nonempty D37 regions and all 72 strict restriction
compositions, but `InterfaceObject` omits the carrier-cell namespace.  Hashing
the 38 images gives only 32 distinct targets:

```text
D37 source regions       38
distinct interface image 32
```

Regions with the same proposal labels, participants, parent lines and types in
different registered carriers become the same object.  A functor may identify
objects, but the repair note and stdout call this an “actual D37 embedding.”
That word is false, and a comparison proof cannot silently lose the carrier
identity when later restriction morphisms are carrier-relative.

**Required repair.**  Add a local carrier namespace to the shared interface
object—`D37:<cell.name>` on the D37 side and a fixed source-family namespace on
the D38b side—then gate 38/38 distinct D37 images.  This is not permission to
restore the complete D38b witness hash: the namespace identifies the finite
carrier type, not a global completion.

## MINOR m1 — certificate construction is not an asynchronous liveness theorem

The typed parent DAG is genuine and the 410 soundness/completeness comparisons
pass.  However `collect_h1` constructs the full prepare/grant set in one
checking transaction.  It does not model asynchronous partial arrival,
timeouts, fair delivery or recovery of a legitimately partial grant set.

**Repair.**  State in the receipt note and Paper 28 that H1 is a finite
admission-certificate existence/safety theorem.  It proves no distributed
liveness or asynchronous recovery theorem.

## MINOR m2 — keep the two action objects separate

The rank-246 variety is an unnormalized increment variety.  The normalized
representative is a separate horizon-three completion kernel, not a stationary
point proved to lie in a normalized version of the whole 155-dimensional
variety.  D39b's stdout and note mostly say this correctly.

**Repair.**  Preserve that separation in the terminal noun and manuscript;
never shorten it to “a residual family of selected laws.”

## NIT n1 — “parent closures” understates the H1 gate

The printed row `causal_protocol_parent_closures=410/410` actually gates parent
closure, acyclicity, per-kind parent schemas, payload binding and signatures.
Rename it `causal_protocol_validations` when the receipt is regenerated.

## Retained theorem surface

- The comparison object is a valid shared-interface functorial span once its
  missing local namespace is restored; it remains neither a D37 conditional
  equivalence nor Level-B conflict generation.
- H0 fails and typed H1 is sound/complete on the declared finite registry.
- The action matrix rank, chosen-D38b obstruction and finite-horizon normalized
  representative are correct.
- Finite all-transport rooting is uniform at orbit level; infinite completion
  is open.
- Typed O-U is projectively injective only on the registered 49-class grid;
  untyped and O-L collisions are real.
- Clock, D26, quantum-join and sealing conditions retain their declared scopes.

After the namespace repair and wording stitches, a narrow Paper 28 may begin.
