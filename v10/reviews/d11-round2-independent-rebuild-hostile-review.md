# D11 hostile review, round 2: independent repaired rebuild

**Referee:** independent clean-room reconstruction  
**Date:** 2026-07-11  
**Verdict:** **PASS at the narrowed `INCOMPLETE-PACKET` scope**

The repaired D11 package reproduces. The exact engine passes 73 frozen checks
with byte-identical normal and optimized output. The bundled-Python numerical
campaign is deterministic across two complete runs and enforces its internal
receipt hash. The original event/lifetime, support, covariance-rank, refusal,
and extinction summaries are unchanged; the corrected downstream-seal
sibling-MERGE influence prevalence is `1/24,4/24,5/24`.

The round-1 receipt defects are repaired at their finite declared scope:
tokens now contain exact matrix tuples, outcomes and multi-parent provenance
are explicitly typed, the owned `P0/P+` intervention reaches a merge and
changes a later P0/P1 law, and the selected disjoint schedules have exact
equal weight `1/140`. The stronger claims have been withdrawn. D11 now calls
itself a globally raced finite-prefix kernel with a separate covariance
template and explicitly leaves decentralized clicking, generated multi-frame
gauge, canonical projective pushforward, and general bridge birth open.

That downgrade is load-bearing. This review is a PASS only for the adjudicated
`INCOMPLETE-PACKET` result, not for the frozen protocol's former complete-local-
rulebook target.

## 1. Commands and hashes

Commands rerun:

```bash
PYTHONDONTWRITEBYTECODE=1 \
  python3 v10/code/d11_complete_bloch_lorentz_exact.py

PYTHONDONTWRITEBYTECODE=1 \
  python3 -O v10/code/d11_complete_bloch_lorentz_exact.py

PYTHONDONTWRITEBYTECODE=1 \
  /Users/felixrobles/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 \
  v10/code/d11_generated_history_geometry.py
```

I executed the numerical command twice.

Observed complete stdout hashes:

```text
exact normal       639154ac73e65adb1b528a2bc1d5f6fa1dc4ffc1bc8a1e94bb643742891021f4
exact python -O    639154ac73e65adb1b528a2bc1d5f6fa1dc4ffc1bc8a1e94bb643742891021f4
numerical run 1    56f5a51f928a1e26441455b5bbb996ac3c35ba911529a6d735ba27d0640e8ee9
numerical run 2    56f5a51f928a1e26441455b5bbb996ac3c35ba911529a6d735ba27d0640e8ee9
```

Current repaired source/result hashes:

```text
e66dc317764d1bd19229b98d446165c1ccc2a141b53572419fbc3de3115384dc  v10/code/d11_complete_bloch_lorentz_exact.py
60d7a905158dd9e08b7713b34e67e61a45214e7e7c6ab2e4c9b61979930e7c00  v10/code/d11_generated_history_geometry.py
6672fb35e320ebee70ea1b59d083eb17768c1018e30a5786e31b8980c633035e  v10/note-d11-complete-bloch-lorentz-scir-protocol.md
c2c754b816ceb692ba37b179de9be195ed6dc3731c9416f6d3d94036048e383c  v10/note-d11-round1-opening-repairs.md
efad9e513715c4defb0433a9805da127c02b8d8c0bfa4866b2cf5c67c7815bc7  v10/note-d11-complete-bloch-lorentz-scir-investigation.md
044154486930da0ddc0c00aac2bc0fbcb817d370497a68a4db365c5b0675486f  v10/relativistic-isp-v10-paper12-complete-lorentz-rulebook-that-cannot-grow-a-universe.md
```

The exact summary reports internal digest:

```text
c45eea0b4d50ec1644627a722bfa6f010f238ae581f66391eba7aeff4c32b62e
```

The numerical program computes and gates:

```text
f1ab9e04caa42f200c3af53adb295d8f78d547178d0b67fff0bcffd9af547224
```

Both programs fail on check-count or internal-receipt drift. Exact arithmetic
uses rational sign comparison in `Q(sqrt(2))`; Decimal/float geometry remains
confined to the declared external direction diagnostics.

## 2. Exact 73-check closure

The repaired exact output reports:

```text
checks=73
cutoff3_histories=117
independent_branch_word=H
depth12_orbit=113
depth12_support=0.914143429015
dual_sl2c_born_gauge=TEMPLATE_PASS_NOT_INTEGRATED_HISTORY
split_sibling_merge_terminal_seal_instruments=PASS
typed_complete_next_history_kernel=PASS
disjoint_split_state_probability_commutation=PASS_ONE_CELL
canonical_projective_pushforward=OPEN
decentralized_local_click_law=OPEN
ancestry_subset_positive_cone=PASS
naive_join_negative_control=FIRED
pairwise_positivity_equals_influence=FALSE
equal_activity_population=A_S_EXTINCTION_THEOREM
```

These labels now match the executed scope. In particular, the exact receipt no
longer promotes a static dual-covariance identity to an integrated generated-
history gauge theorem or one schedule cell to general construction-order
gauge.

## 3. Typed packet, outcomes, and parent provenance

The repaired data objects now contain:

- `Token.packet` as an immutable tuple of exact Kraus/isometry matrices;
- `DurableOutcome` fields for ID, record, owner ports, rule, value, recorded
  effect, and terminal/continuing status;
- `Port.parents` as a tuple rather than a single nullable parent;
- `JoinOpportunity` anchor position, anchor order unit, and anchor frame link.

Root token packets compare exactly with `(K_H,K_T)` and `(P0,P1)`. JOIN tokens
carry `(J0,J1)`. Terminal outcomes consume all recorded owner ports, while
SPLIT, terminal-SEAL, and sibling-MERGE outcomes remain separately typed.
Sibling-MERGE outputs retain both owner ports and inherit order-unit/frame data
from the recorded common anchor rather than from the arbitrary left input.

The recorded effects are now input-space instrument effects rather than
decorative pointer labels: SPLIT stores `K_g^dagger K_g=I/2`, terminal SEAL
stores `P_b`, and sibling-MERGE stores `J_b^dagger J_b` on the four-dimensional
input. A dedicated exact gate checks their dimensions and reconstructs each
Born weight from the stored effect.

The unbounded ancestry string was removed from live ports; immutable record
incidence now carries provenance. The manuscripts correctly leave identifier,
coordinate-shadow, and per-record capacity/compression questions open.

Two limitations remain visible and are compatible with `INCOMPLETE-PACKET`:

1. firing functions are still module-defined packet semantics rather than a
   decentralized interpreter derived from record-carried data; and
2. the generated campaign stays in one root frame, so the anchor fields do
   not yet demonstrate a transformed multi-frame history.

The paper explicitly lists both larger constructions as open.

## 4. Owned intervention and stricter sibling-MERGE influence

The new exact cell uses the registered worlds, not the former generic
`P0/P1` substitute:

```text
root P0 versus root P+
same owned H-SPLIT
same fresh P0 sibling
same partial-iSWAP sibling-MERGE
compare downstream P0/P1 seal law
```

Since `H P0 H = P+` and `H P+ H = P0`, merging each carrier with `P0` gives
different nonselective first-output P0 probabilities. The exact check is a
genuine owned-history seal-distribution witness.

The numerical counter now increments only when a changed MERGE input produces

```python
abs(Tr(P0 rho_out) - Tr(P0 rho_out_intervened)) > tolerance
```

It no longer treats an arbitrary density-matrix or coordinate difference as
seal influence. Two deterministic runs reproduce the corrected history counts:

```text
cutoff 512:   1/24
cutoff 1024:  4/24
cutoff 2048:  5/24
```

This strengthens failure of the registered `20/24` gate. Paper 12 and the
investigation note use these corrected numbers and call the mechanism
population-extinct / interaction-sparse rather than physically inert.

## 5. Schedule probability and prefix scope

For the selected two disjoint SPLITs, both canonical histories agree. The
repaired code also multiplies the token-race and outcome weights. Each order
has

$$
{1\over5}\,{1\over2}\,{1\over7}\,{1\over2}
={1\over140}.
$$

The overlap control still distinguishes sibling-MERGE-first from SPLIT-first
and verifies invalidation of the consumed sibling JOIN.

The cutoff-one through cutoff-three tests are now called finite-prefix mass
normalization, not canonical projectivity. Paper 12 explicitly says that a
general probability pushforward from sequential presentations to canonical
marked histories is not executed. This closes the round-1 overclaim by scope,
not by pretending the missing theorem was proved.

## 6. Numerical summaries and extinction

The repaired campaign leaves the topology and random draw sequence unchanged.
The clean-room topology-only simulator from round 1 continues to reproduce all
event/lifetime summaries:

| cutoff | reached | terminal | median clicks | maximum clicks | rank four |
|---:|---:|---:|---:|---:|---:|
| 512 | 0/24 | 24/24 | 1 | 49 | 3/24 |
| 1024 | 0/24 | 24/24 | 3 | 171 | 5/24 |
| 2048 | 0/24 | 24/24 | 1 | 47 | 4/24 |

The longest event counts remain:

```text
seed 20276009: S/J/Q = 24/6/19, max_open 7
seed 20277010: S/J/Q = 85/16/70, max_open 12
seed 20277013: S/J/Q = 85/15/71, max_open 11
seed 20278003: S/J/Q = 23/3/21, max_open 6
```

Other frozen summaries also match Paper 12:

```text
median direction support   -1.0000, -0.8515625, -1.0000
ancestry violations        0
influence violations       0
valid F_dom                1,3,1
valid F_m4                 1,0,0
M4 m4-axis controls        24/24 each rung
M4 dom-axis controls       0/24 each rung
M4 means                   1.085537373230, 1.079347373771, 1.078453579417
```

No NaN is averaged and no survivor conditioning occurs.

The independent extinction derivation remains unchanged:

$$
P(\Delta p=+1\mid H)={p\over2p+j},\qquad
P(\Delta p=-1\mid H)={p+j\over2p+j},
$$

with `0<=j<=p/2`, drift `-j/(2p+j)`, total SEAL probability at least `2/5`,
and immediate root extinction `1/2`. Optional stopping plus the uniform
bounded-block absorption chance proves almost-sure extinction.

## 7. Paper 12 and note scope audit

All reported numbers match the repaired programs. More importantly, the
headline and verdict have changed from the pre-review overclaim:

```text
INCOMPLETE-PACKET
+ complete globally raced finite-prefix kernel
+ incidence-scoped exact instruments
+ separate dual SL(2,C) covariance template
+ algebraic positive-cone containment
+ owned sibling-interaction witness
+ almost-sure terminal-SEAL extinction
- decentralized local click law
- generated multi-frame gauge
- canonical projective pushforward
- general bridge/join birth
- physical spacetime influence envelope
```

Paper 12 now calls JOIN a sibling-MERGE, says its global race is supplied,
limits schedule commutation to one tested cell, and distinguishes the frozen
registry label `INTERACTION-INERT` from the mechanism diagnosis. It does not
claim a physical influence cone, derived scale, general bridge birth, or final
COMMIT law.

The section heading “Complete local rewrite packet” is broader than the final
adjudication, but the section text immediately restricts it to the global
kernel and the paper's title/abstract/verdict are unambiguous. No result should
quote that heading without the `INCOMPLETE-PACKET` qualification.

## 8. Closure table

| Round-1 opening | Repair | Status |
|---|---|---|
| packet strings/silent matrices | exact matrix tuples stored in tokens | closed at global-kernel scope |
| untyped outcome log | immutable typed outcome records | closed |
| left-only JOIN provenance | both parents plus anchor frame/order-unit fields | closed at root-frame scope |
| growing ancestry word | removed; provenance in immutable records | closed, capacity still open |
| state/position proxy called influence | downstream P0/P1 probability difference | closed |
| generic P0/P1 JOIN witness substituted for P0/P+ intervention | owned H-SPLIT/MERGE witness added | closed |
| Decimal algebraic sign decisions | exact rational square comparison | closed |
| schedule probability omitted | exact `1/140` equality added | closed for one cell |
| numerical hash external only | internal expected receipt asserted | closed |
| finite mass called projectivity | renamed finite-prefix normalization; general pushforward open | closed by scope |
| complete/local headline | verdict downgraded to `INCOMPLETE-PACKET` | closed by scope |

## 9. Final determination

The repaired evidence supports:

$$
\boxed{
\text{a complete globally raced finite-prefix kernel with exact extinction,}
\quad
\text{not the final decentralized, gauge-integrated record click law.}
}
$$

The stricter interaction witness is real but sparse, the population extinction
result is unchanged, and every remaining construction gap is now named rather
than graded as a success.

**Round-2 independent-rebuild verdict: PASS at `INCOMPLETE-PACKET` scope.**
