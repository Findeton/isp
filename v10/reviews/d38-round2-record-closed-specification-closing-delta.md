# D38 round-two record-closed specification closing delta

**Frozen target:** commit `be310e2682eeb0920d40e8b1a073c50c174193ec`.
**Receipt:** D38b `PASS 9/9`, complete-output SHA-256
`28e76708b6c72cf874aedf9700a6bd1756220e1cb4bf8be3e096e632b66b4f7d`.
**Lanes:** probability/projectivity; causal record integrity and regional
transport; corpus/D34e/D34f/D26/V6/quantum scope.
**Verdict:** `0 BLOCKERS / 0 MAJOR / 0 MINOR / 0 NIT`.
**Promotion:** approved at the narrowed finite-witness scope.

## 1. Repair sequence audited

Round one at `7a3919b` returned `2B/7M/1m/0n`.  The replacement D38b
architecture at `dac44b6` removed authoritative caches, generated typed birth
incidence, installed exact positive-cylinder restrictions and implemented
nested record transport.  A first focused delta found one local projection
major: neighbor rows retained fields changed by thinned silent events.  Commit
`8170d41` replaced them with exact D34e B3 `(actor,degree,own-birth-count)`
rows, 220 exact competing-exponential integral checks and 30 elapsed/no-event
updates.

The causal lane then found one final blocker: `append_view()` could union two
individually authentic successors from the same prior head.  Commit `be310e2`
made the complete finite authenticated history an explicit regional admission
witness.  View validation re-derives the frontier and incidence from that
witness; append must be its exact next typed event and advances the witness.
A smaller localized admission certificate is printed `OPEN`.

## 2. Probability and projectivity lane — clean

Fresh evaluation returns:

```text
cylinder_checks =
  (12 normalizations,
   12 direct restrictions,
   1,760 positive path atoms,
   220 exact race-integral checks,
   30 elapsed/no-event updates,
   holding rates 3/2,3/2,2,15/8).
```

The D34e neighbor projection is now closed under silent neighbor idle and
off-root interaction events.  Direct depth `3->1` restriction equals staged
`3->2->1` restriction at all four reachable boundaries.  Spatial DLR and null
boundary versions remain unclaimed.  D34f is inherited only at its finite-stop
current-component quotient; timed or infinite predictive inverse limits stay
open.

Verdict: `0B/0M/0m/0n`.

## 3. Causal record and regional transport lane — clean

`authentication_checks()` returns `(18,18,18,1)`: all 18 registered candidate
and whole-history failures reject and return an equal immutable store.
Current rows and heads are derived from the event DAG, with no authoritative
cache.  Duplicate records, missing parents, disconnected events, cross-wire
parents, rollback and incompatible whole-history branches fail validation.

`restriction_checks()` returns `(2,1,1,5,1)`: two direct/staged nested
restriction squares, one legal update-naturality square, disjoint insertion
commutation, five external parent/frontier slots and one same-frontier branch
rejection.  Independent exhaustive enumeration of all 16,384 assignments
`D subset E subset F` on the seven-record specimen found zero restriction
mismatches and zero parent-totality failures.  After a seed-IDLE append advances
the witnessed A head, the competing seed-INTERACTION successor rejects and the
pre-attempt view remains valid.

Verdict: `0B/0M/0m/0n`.

## 4. Corpus and scope lane — clean

Typed birth generation matches the chosen D34b/D34e finite model: canonical
Ulam child, post-row, edge, two ports and child clock address.  Passive
reception leaves the receiver ring unchanged.  The README and campaign note
identify the complete finite witness explicitly and make no locality or
minimality claim for it.

The fresh-anchor numbers are labeled hash-locked D34f inheritance, not a new
recomputation.  The exact-zero operational cut is hypothetical and is not
identified with V6 dispersal.  D26 remains conditional on a missing same-line
BORN bridge; no maintenance rate follows.  All-transport unimodularity,
infinite completion, coupling selection and the quantum join remain
open/undefined.

Verdict: `0B/0M/0m/0n`.

## 5. Reproducibility

Executions under `PYTHONHASHSEED=0,1,2,17,101,999` are byte-identical.

```text
source SHA-256:          c48e317189a160d445af374346deb3199caed0ae222430260a55e2a6ef731eeb
stdout-body SHA-256:     35371f270a4ec0f018b4a386bf49694e99c136170e4ca9fb272c597cbc5d507d
science SHA-256:         1aa0201d20af064ab30173679b91d9437ceaf5f32a2aea1d19b72527ed593005
complete-output SHA-256: 28e76708b6c72cf874aedf9700a6bd1756220e1cb4bf8be3e096e632b66b4f7d
```

## 6. Accepted scope

The hostile-closed candidate noun is:

```text
AUTHENTICATED RECORD-CLOSED ORIENTED FINITE-CYLINDER PRESENTATION /
CHOSEN D34b / COMPLETE FINITE ADMISSION WITNESS /
NONSELECTING.
```

This does not prove spatial DLR, a localized minimal admission certificate,
null-boundary versions, Paper 26 K-family membership, a physical sealing law,
the D26 maintenance bridge, timed inverse-limit completion, all-transport
unimodularity, coupling selection or a quantum regional join.
