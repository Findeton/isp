# D31C2 (O3) ROUND-1 HOSTILE REVIEW — fresh referee, full clean-room rebuild — 2026-07-12

**Target:** `code/d31c2_graded_feasibility_degree.py` + `data/d31c2_graded_feasibility_degree.out` (6/6) at 316bce6; pins note-d31 §6 + note-d32 §7(4); LEDGER #143; LOG 2026-07-12 O3 entry.
**Method of this review:** the mathematics was rebuilt from scratch (independent enumeration, independent reduction, independent infeasibility decision by rational Gauss–Jordan + Stiemke certificates, independent exhaustive pair engine — nothing imported from the repo), then the code, the pins, and the record wording were audited against the rebuild.

**VERDICT: MAJOR-REVISION — 1 MAJOR / 4 MINOR / 4 NIT. Zero receipt gates flip; zero false printed numbers in the receipt; every receipt count reproduces exactly (16 states = 1/2/4/9; DEG 24 raw / 14 merges / 10 merged / 24 rows / 0 skipped; DDEG 11 / 7 / 4 / 24 / 0; both printed pair certificates re-derived verbatim). THREE of the four OBSTRUCTED verdicts (DEG, DDEG, MOTIF) survive the independent rebuild AND come out STRONGER than claimed (single-row kills; robust to the independence convention; extend a fortiori to every deeper horizon). The FOURTH (COMPONENT-SIZE) is mis-scoped in the record: it is obstructed ONLY at unbounded growth — at the u ≤ 5 horizon the family is FEASIBLE, and this review exhibits and machine-verifies an explicit witness kernel. The one false sentence is in the LEDGER/LOG lineage, not in the receipt.**

## MAJOR

**M1 — COMPONENT-SIZE is horizon-FEASIBLE; the #143/LOG headline scope is false as written, and the §7(4) fork condition is technically triggered.**
Evidence and recomputation:
- The D5 argument (code lines 438–445) is a connectivity gate plus a citation of the D31A theorem. The reduction itself is sound and grammar-wide (my rebuild: connectivity holds at every horizon state AND every successor; leaf births cannot disconnect; so the grading ≡ u+1 exactly). But the D31A theorem kills interactions only at UNBOUNDED growth; D31A's own printed limits (d31a A5: "bounded growth escapes — c > 0 admissible below a u-cap") assert the complementary horizon fact.
- **The witness (rebuilt, exact rationals):** c = 1/40 < 1/(5·4); w_i(s) = c for every interact at component size s; w_b(s) = (1 − (s−1)(s−2)c)/(s−1) for births (s = 3..6: 19/40, 17/60, 7/40, 1/10). All weights positive; Z(W) = 1 identically on the horizon and all successors; genuinely component-size-graded (w_b varies with s); stationary; normalized; interacting. **It passes ALL 2268 independent two-op pairs of the horizon exactly** (the receipt's own pinned state-level independence, both orders, exact Fraction equality), and passes 3-op same-multiset reorder covariance on spot-checked endpoint groups. The same construction works at EVERY finite horizon U (any c < 1/(U(U−1))); only unbounded u forces c = 0.
- Consequently: `v8/LEDGER.md:317` ("ALL FOUR families OBSTRUCTED at the u ≤ 5 horizon"), `LOG.md:1817` ("the covariant-family search at this horizon is EXHAUSTED-NEGATIVE"), and the 316bce6 commit subject are FALSE AS WRITTEN for component-size. The same #143 entry's landscape sentence ("no stationary path-covariant interacting kernel exists in ANY tested grading class **at unbounded growth**") has the scope right — the headline and the landscape contradict each other within one ledger entry.
- The pin's own deliverable dichotomy (note-d31 §6, line 37; note-d32 §7(4), line 53: "OBSTRUCTION-instance (certificate printed) or EXISTS-candidate (sample rational solution + a direct two-op covariance verification gate over the enumerated horizon)") was not honored for this family: D5 delivered a third, off-menu category (reduction citation), where the pin-conformant deliverable is **EXISTS-candidate at the horizon + obstructed at unbounded growth via D31A**.
- Downstream: §7(4)'s fork is horizon-worded ("If a covariant interacting family EXISTS at this horizon, mapping ITS dimension signature becomes D32B's primary axis... if all OBSTRUCTED, the phenomenological grid proceeds"). The witness triggers the first branch as written. The physical conclusion ("D32B phenomenological framing FORCED") SURVIVES under the unbounded-growth reading — the witness is exactly the bounded-growth escape D31A prices (its interact rate must know the size cap), and the grammar is pinned none-free unbounded-growth — but the record currently gets there by a mis-scoped sentence rather than by saying this. Note the branch is not vacuous: a covariant bounded-domain cell IS constructible at D32B sizes (c below the cap), so the reconciliation must be argued, not assumed.
Repair: #144 forward-correction re-scoping the #143 headline + LOG echo; D5/D6 gain the "at unbounded growth" qualifier and disclose the horizon witness class; §7(4)'s fork resolved explicitly under the unbounded-growth reading (or a covariant bounded-domain cell adopted into D32B, honestly labeled — either is coherent; silence is not).

## MINOR

**m1 — The printed certificates are valid but NON-MINIMAL, and the mechanism attribution misses the sharper (and more useful) fact.** My rebuild: after L2 merging, the deg-1-parent birth rows are ALL-NONNEGATIVE with positive entries — DDEG: {('b',): +1, D1: +2, D2: +2(u−1)}; DEG: {('b',2): +1, I: +2u} — so a SINGLE row already contradicts Z(W+b) = Z(W) with positive weights. Stronger still (my S8 probe): in RAW coordinates, with NO merges, NO seed rows, and DISJOINT-only pairs (the strictest, circuit-friendly independence reading), both systems are still single-row infeasible (e.g. path-state Q, birth at the far leaf: DDEG Δ = {b:+1,(1,2):+2,(2,1):+1,(2,2):+3} ≥ 0). Three consequences the record leaves unclaimed: (i) the true mechanism for DEG/DDEG is d31c's C2 "births strictly enlarge the class census" resurfacing after (indeed even before) merging — the same mechanism as MOTIF, which is exactly the O2 unified-lemma shape (#143 calls these "two new instances" of O2 without noticing they are already single-row C2 kills); (ii) the negative verdicts are ROBUST to the state-level overlap convention (they survive disjoint-only independence — the L2 layer is not load-bearing for infeasibility); (iii) the three negatives extend A FORTIORI to every deeper horizon (a deeper-horizon system contains the u ≤ 5 rows; MOTIF's inertness is a pendant-vertex lemma at all sizes — verified here through u=6 births). The "u-dependence trap" narration in #143 describes the pair certificate, not the mechanism. Certificate-pair claims themselves verified: my row-order mirror reproduces both prints exactly — (u=2 vs u=3, ('i',1,1)) for DEG, (u=2 vs u=3, ('i',2,1)) for DDEG; forced-zero classes by my own elimination: DEG {('b',2), ('i',1,1)-group}, DDEG {('i',2,1)-group}.

**m2 — Merged-class counts include out-of-horizon phantom classes; the DCAP sentence is wrong for the code's own class-collection domain.** The class loop (lines 257–261) collects from u=6 successor states with no guard. Recomputed: of DEG's "10 merged classes", FOUR (('i',2,5), ('i',3,4), ('i',4,3), ('i',5,2)) are realized ONLY at u=6 successors and are never constrained (free variables); of DDEG's "4", ONE (('i',2,5)). Honest in-horizon counts: DEG 6, DDEG 3. Equation-inert (u=6 states feed no rows or merges — verified), so no verdict moves, but the printed system sizes overstate the constrained content, and the docstring line 33 "DCAP = 5 (no capping distortion at this horizon)" is false at the domain the class loop actually consults: max degree at successors is 6 (recomputed), so the cap BINDS there (('b',5) and ('i',·,5) conflate deg-5 with deg-6 at u=6). Repair: one disclosure line (or guard the class loop to u ≤ 5 and let phantoms vanish).

**m3 — The #139 lineage correction is recorded but not named as a correction, and the stale false sentence survives uncorrected in two places.** The rebuild confirms #139's justification clause ("a leaf birth reclassifies the parent's old ops") was FALSE for motifs: leaf births change NO existing pairwise distance and NO existing common-neighbor count (verified exhaustively over every horizon state and every birth, including u=5→6). The receipt's D4 and #143 do record the supersession ("RESOLVED-OBSTRUCTED... never escaped birth-inertness" — not silent, good), but neither flags the #139 sentence as in error, and note-d31 §5 (line 32) still carries the unqualified clause, as does the frozen `data/d31c_graded_feasibility_exact.out` (lines 13–15). House style treats forward-corrections as first-class: a pointer sentence is owed in note-d31 §5 and in #144 ("#139's open-arm placement of motifs rested on a false premise for that family; degree and component-size were correctly placed").

**m4 — The two-op-closure limitation is presented as a live weakening of the negative verdicts; it is vacuous for them.** Lines 458–464 (.out 19–25) list "two-op covariance closure" among the reconnaissance limits with no direction statement. Decision (this review): every two-op equation imposed is a NECESSARY condition of full path-covariance (a genuine pair of length-2 paths to the same web), so infeasibility of the subset kills the full requirement at ANY closure depth — the limitation can only matter for EXISTS branches (where it is real: longer commuting sequences are extra constraints; my comp-size witness was therefore also checked at 3-op reorder depth). Same defect class: "deeper horizons are NOT decided here" is false-as-worded for the three killed maps (decided a fortiori, see m1(iii)); it is accurate only for the exists side and for richer maps. Repair: one scoping sentence in D6.

## NIT

**n1** — D1 (lines 355–363) gates only the EXISTENCE of a disjoint interact; the L1 derivation additionally needs that interact's class to be UNCHANGED by the birth. True — it follows from the leaf-birth invariance lemma (degrees of non-parents, distances, common-neighbor counts all invariant; verified exhaustively here for every state, birth, and disjoint op) — but it is the load-bearing half of the premise and is ungated.
**n2** — line 338: the same-parent birth-pair skip is dead code (each ('b', y) occurs once in `ops`; two distinct birth ops never share a parent).
**n3** — the EXISTS branch's witness grid (lines 236–245) is finite and its `(True, None)` fallback would print a FAILED gate for a genuinely feasible family — fail-safe in the right direction, unexercised this run; noted for the day an EXISTS verdict appears.
**n4** — pin-drift, resolved pre-run and verified consistent: §7(4) promised "exact log-linear elimination / rational Gaussian elimination", note-d31 §6 re-pinned the layered Δ-census + Fourier–Motzkin method (necessary: Z-sums make reclassifying families non-log-linear), committed at 78c03af before the run; the receipt matches §6 exactly (conventions, layers, seed-skip, deliverables — checked item by item). The state-level independence convention is disclosed (lines 23–27) and genuinely inherited: D31A's u=2 forcing pair is necessarily overlapping (re-read of d31a A1/A2 confirms), and per m1(ii) the verdicts do not depend on it anyway.

## SURVIVES ATTACK
- **Enumeration:** independently rebuilt; u → 1/2/4/9 (16 states) CONFIRMED, with an independent structural identification — states at count u are exactly rooted trees on u nodes (R stays a pendant marker on A forever), so the counts must be A000081(u) = 1, 2, 4, 9, and the u=6 successor count must be 20: both verified. An enumeration error would have poisoned everything; there is none.
- **The layered reduction as mathematics:** the covariance equation at line 29 re-derived from the stated path measure; L1 cancellation (disjoint interact, W+i = W, class-invariance ⇒ Z(W+b) = Z(W)) and L2 (w(c′)Z(W) = w(c)Z(W+b), then L1 ⇒ w(c′) = w(c)) both check; every merge pair used was verified independent under the pinned convention; the seed-skip logic is sound in the safe direction (only implied equations enter) and "0 skipped" reproduces — both seed births' reclassifications land in already-merged classes.
- **DDEG (the #139 debt):** full independent rebuild. Merge structure exactly {('b',)}, D1 = {('i',1,1..5)}, D2 = {('i',2,1..4)} (+ the phantom ('i',2,5)); merged rows are w_b + 2·D1 + 2(u−1)·D2 = 0 at u = 2,3,4; INFEASIBLE confirmed by my own elimination (rank 2, ('i',2,1) forced to 0) and by Stiemke certificate; the printed pair certificate reproduced exactly. **OBSTRUCTED at the horizon: CONFIRMED.**
- **DEG:** full independent rebuild; 5 birth classes never merge, all 15 in-horizon interact classes merge into one group with rep ('i',1,1); INFEASIBLE confirmed (rank 5; ('b',2) and ('i',1,1) forced to 0); printed certificate reproduced. **OBSTRUCTED at the horizon: CONFIRMED.**
- **MOTIF:** birth-inertness exhaustively re-verified (all states, all births, including u=5→6), census strictly grows, the seed pair already forces Z-equality directly — the C2 contradiction closes at the horizon and, by the pendant-vertex lemma, at every horizon. **RESOLVED-OBSTRUCTED: CONFIRMED**, and the lineage fact (#139's placement was wrong for this family) is real.
- **FM/rref code:** audited line-by-line; strict-inequality Fourier–Motzkin combinations are sound (positive multiples of strict rows); the empty-positivity-row exit is a genuine forced-zero detection; the zero-length-row contradiction after the last elimination is a genuine 0 > 0; pre-loop all-zero check correct; union-find with sorted-min reps correct (certificate class names are the group minima, as printed).
- **Discipline:** pins committed pre-run (78c03af) and held until D32A closed (69c0df8) per the §7 sequence; the receipt matches the §6 pin's conventions exactly; working tree clean at 316bce6.

## GATES RE-VERIFIED
`python3 code/d31c2_graded_feasibility_degree.py` rerun at 316bce6: **6/6 PASS, .out byte-identical, 0.18 s.** Clean-room rebuild runtime ~4 s; all receipt-printed counts reproduced (see verdict line).

## FALSE-RESULT COUNT
**In the receipt (gates + printed numbers): 0.** In the record lineage: **1 false scope sentence** — "#143: ALL FOUR families OBSTRUCTED at the u ≤ 5 horizon" (echoed at LOG.md:1817 and the commit subject), refuted for component-size by the witness in M1; the same entry's "at unbounded growth" landscape sentence is the correct one. No mathematical result is false at its correct scope.

## OWED REPAIRS (exact list)
1. **(M1)** #144 forward-correction: comp-size = OBSTRUCTED at unbounded growth ONLY (reduction + D31A); horizon-feasible witness class disclosed (c < 1/(U(U−1)); the exhibited c = 1/40 kernel); #143 headline + LOG.md:1817 re-scoped; §7(4) fork resolved in words (unbounded-growth reading, or an honestly-labeled covariant bounded-domain D32B cell).
2. **(M1)** D5's verdict line and D6's landscape line gain "at unbounded growth"; D6 optionally records the horizon-EXISTS fact for comp-size as the D31A bounded-growth escape.
3. **(m1)** Record the single-row minimal certificates (both families), the raw-coordinate/disjoint-only robustness, and the a-fortiori deeper-horizon extension of the three negatives; hand these to O2 as its instance data (they are the lemma's shape at this horizon).
4. **(m2)** Disclose the phantom-class counts (DEG 4/10, DDEG 1/4; in-horizon 6 and 3) or guard the class loop to u ≤ 5; fix the DCAP docstring sentence (cap binds at deg-6 successors).
5. **(m3)** Note-d31 §5 forward pointer: #139's motif clause was false-for-motifs (leaf births change no existing distances/common-neighbor counts); name the D4 outcome a correction of #139, not only a resolution.
6. **(m4)** One D6 sentence: the two-op-closure and horizon limits weaken only EXISTS-side claims; the negative verdicts are closure-depth-independent and horizon-monotone.
7. **(n1–n3)** Gate the class-invariance half of L1's premise; delete the dead line 338; keep the witness-grid fail-safe note for future EXISTS branches.

## SUMMARY JUDGMENT
The receipt's mathematics is real and, for three of the four families, stronger than advertised; the reduction method is sound and was executed exactly as pinned; the certificates check to the digit. The campaign-framing sentence it feeds ("all four obstructed at the horizon, phenomenological D32B forced") is however wider than the receipt licenses for component-size, and the pinned fork consumes exactly that width — hence MAJOR-REVISION rather than MINOR: the physics conclusion survives, but only after the record says why at the correct scope.

---

# DELTA-VERIFICATION PASS (same referee) — repairs at cae4ed4 — 2026-07-12

**VERDICT: NOT DELTA-CLEAN — 1 new MINOR / 3 new NIT (→ #145). All seven owed repairs are GENUINELY APPLIED, none dropped or weakened; zero gate flips (7/7 under every hash seed tested); zero content-level false numbers. The blocker is string-only: the committed .out is not byte-reproducible.**

**Repairs verified (a):** (1) #144 forward-corrects #143's headline exactly as owed, names the false sentence, credits the correct landscape sentence; D5 re-scoped in-receipt; **D5b verifies my witness** — weights (19/40, 17/60, 7/40, 1/10), Z ≡ 1, genuine s-grading all correct. (2) note-d32 §8's fork resolution is SOUND (the witness's c must know the cap ⇒ a finite record universe ⇒ extra physics against the pinned none-free unbounded-growth grammar ⇒ phenomenological BY ARGUMENT), and the COVARIANT-AT-CAP cell is honestly labeled with its cap-dependence as the printed price (note for the D32B pin: c must scale ~1/u_max² at grid sizes, so the cell's realized interaction fraction will be tiny — the pinned realized-fraction column will document it; a design consequence, not a defect). (3) The minimal single-row certificates gated in D2/D3 match my hand-derived rows exactly (DEG {('b',2):1, ('i',1,1):4}; DDEG {('b',):1, ('i',1,1):2, ('i',2,1):2}); the RAW disjoint-only kills at u = 3 are DIFFERENT exhibits from my S8 rows (theirs: birth at B on the u = 3 star P; mine: birth at the path tail of Q) — **both hand-recomputed entry-by-entry and valid** (any all-nonnegative row certifies; every departing class is exactly replenished by newborn ops). (4) Class loop guarded (line 275): merged counts now the honest 6 (DEG) / 3 (DDEG); merges/rows unchanged at 14/24 and 7/24 — as predicted, u = 6 fed neither; DCAP docstring now accurate. (5) §5 bracketed motif correction at the site, accurate including "degree and component-size were correctly placed"; §7 appended; #144 says CORRECTION. (6) D6's scoped-limits paragraph states exactly what round 1 proved. (7) D1 now gates BOTH halves of L1's premise for all three elimination families (correctly excluding COMP, whose premise genuinely fails — that is why it needed the reduction route); the dead line is removed; the fail-safe comment is present.

**Recomputation (c):** D5b's count independently CONFIRMED at the receipt's scope: **558 = 1·6 + 2·36 + 4·120** (per-state closed form C(u,2) + u²(u−1) + C(u(u−1),2) at u = 2,3,4), every pair exact under my own engine; the 1710 pairs my round-1 check additionally covered (u = 5 starts) are ALL interact-interact pairs — order-identities for ANY kernel (interacts are state-inert; both orders are literally the same product) — and 558 + 1710 = 2268. **Both scopes sound; the 558 subset suffices for D5b's claim as worded** given that one-line identity argument (see Δ-n1).

**New findings:**
**Δ-m1 — the repair introduced a hash-salted print: the committed .out is NOT byte-reproducible.** The RAW-kill dicts (D2/D3 details, print site line ~458) come straight from `delta_census`'s dict, built by iterating `set(c1) | set(c2)` (line ~169) over string-bearing tuples — PYTHONHASHSEED-dependent iteration order. Evidence: reruns under seeds 0/1/2/3 give four distinct output digests (34e07762, 959044e0, a5b83f19, fcbc2a8d), none matching the committed .out (9a2cae55); the diff is confined to the two RAW-kill lines' key ORDER (content identical as multisets; gates 7/7 under every seed). This is the d31b-M1 defect class (LEDGER #139) recurring inside a repair — content-stable this time (the exhibit CHOICE is deterministic via the sorted state iteration; only the rendering is salted), hence MINOR not MAJOR. Fix: print a sorted rendering (e.g. `dict(sorted(d.items()))`); house precedent is the D32A delta-3 treatment — string-only fix + full rerun with the pinned expectation: **diff vs the current committed .out non-empty ONLY on the two RAW-kill lines' orderings, and the new .out byte-identical under at least two distinct PYTHONHASHSEED values.**
**Δ-n1** — D5b's label claims covariance "on EVERY independent two-op pair of the horizon" while gating the 558 u ≤ 4-start pairs; the excluded in-horizon pairs (u = 5 starts) are exactly the ii identities above, so the claim is TRUE — but the closing argument lives in this review, not the record. One sentence in D5b's comment or the LOG line ("u = 5-start in-horizon pairs are interact-interact order-identities for any kernel") closes it.
**Δ-n2** — D5b's `comp_classes` loop (lines 528-531) lacks the m2 guard and collects ('b',7)/('i',7) from u = 6 successors: harmless surplus (positive anyway; never consulted — the 558 pairs only reach s = 6 weights), invisible in output, but stylistically inconsistent with the just-applied guard. Optional one-line fix.
**Δ-n3** — "at EVERY horizon" (the D6 landscape lines, note-d31 §7's heading sentence, #144) is false at the degenerate U = 2 horizon (seed-only reachable set: no in-horizon bi pairs, so any positive kernel is trivially covariant). Intended and licensed: every horizon containing a u = 3 web, i.e. U ≥ 3. Three-site wording touch.

**Gates re-verified:** rerun at cae4ed4 exit 0, 7/7 (and 7/7 under PYTHONHASHSEED = 0,1,2,3); .out diff = the two salted lines only; my round-1 report committed unmodified. **False-result count for the delta: 0 content-level; 1 string-level reproducibility defect (Δ-m1).**

**Owed for #145 (exact):** (1) the sorted-print fix at the RAW-kill detail + full rerun under the pinned numbers-identical-except-those-two-lines expectation, verified byte-identical across ≥ 2 hash seeds; (2) the Δ-n1 identity sentence in D5b's comment or the LOG; (3) optionally the Δ-n2 guard and the Δ-n3 "U ≥ 3" wording at its three sites. **On a green, seed-independent rerun with (1) and (2) applied, this pass converts to DELTA-CLEAN with no further round needed — the mathematics is settled; D32B (with the covariant-at-cap cell) is cleared to build on that condition.**
