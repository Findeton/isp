# D34e round 3 — ancestry/quantum closing delta

**Frozen target:** commit `6e6676b`.

**Comparison base:**
`d34e-round2-ancestry-quantum-hostile-delta.md` at `bd143fb`.

**Exact verdict:** **DELTA-CLEAN AND TERMINAL AT THE DECLARED CLAIM WIDTH —
0 BLOCKER / 0 MAJOR / 0 MINOR / 0 NIT.**

The endpoint-pinned Branch-F event closes the sole remaining major.  Full B4
event-history composition closes round-2 m1, and the independent-predicate,
tri-state-capacity decision engine closes round-2 m2.  Fresh-radius and
interloper attacks found no regression.  The complete finite-radius row-6
exclusion, B4 sufficient growing ceiling, stopping-scope separation and
intrinsic-quantum refusal are all accepted exactly as scoped.

“Terminal” here means that the ancestry/quantum D34e review stream has no
remaining defect at this claim width.  It does not construct the still-absent
timed controlled quantum process, prove a minimal Branch-F carrier, identify a
weak/timed predictive quotient, or connect the result to spacetime cones.

## 1. Independent reproduction

I ran the frozen receipt under fresh hash salts `0`, `123457`, `982451653`
with `-O`, and `424243`.  Every execution exited zero and printed `13/13` with
internal summary digest

```text
a53fa0c18a5905f282cea4c283ec3061c049ad7378a00f624906c1d68091d701.
```

The `424243` stdout was byte-identical to the committed output.  Both have
SHA-256

```text
3d12f6191883ee3790c78498bae4bb1971144765341a354df587d33188f54498.
```

Frozen artifact hashes independently reproduce as:

```text
note   2c6bed036361ce6bea4b671691de77886742acb552709bc885a69e6950cb2120
code   e66490560f7c38af746b6fea144a4356dfdb3630205eab9f46723ed8c830bff8
output 3d12f6191883ee3790c78498bae4bb1971144765341a354df587d33188f54498
```

The exact reproduced receipt ledger is:

```text
levels                         1,6,40,304,2576
reachable states               2927
coarse collisions              2816
registered strong classes      111,111,111
synthetic stress classes       106,110,110
B3 row updates                 35898
disjoint construction swaps   120276
full regional compositions    159734
corruption attacks             6/6 rejected
registered ancestry interlopers 16/16
radius lower bounds            1/24,1/1024,1/64000,1/5308416
unmarked classes               4,10
quantum                        REFUSAL
```

No discrete finding uses floating-point tolerance.  Decimal evaluation is
confined to explicitly named exponential, Poisson and optional Erlang
identities.

## 2. Round-2 finding disposition

### M1 — endpoint-pinned immutable Branch-F event: CLOSED

The repaired predicate accepts the remote structural path and the D-wire
ordinal frozen at the conditioning stop.  It constructs the exact selected
event ID

```text
selected_id = D#r(pre_stop_ordinal)
```

inside the future A-record ancestry.  It no longer follows D's immediate tip
at propagation time.

The paired current pasts have:

- the same structural D role;
- the same pre-stop wire ordinal;
- the same nominal event identity;
- different immutable event kinds: idle versus interaction;
- exactly equal complete `C_r` carriers.

The licensed event is therefore one common typed ancestry cylinder:

```text
the future A ancestry contains the selected inward interactions
and contains the exact pre-stop D event with kind idle.
```

From the idle past, the original consecutive inward sequence is a positive
subcylinder.  From the interaction past, the exact selected event already has
kind interaction, and persistence prevents any future update from changing
that fact.  Consequently

```text
P(E_r | h_idle) >= p_r > 0,
P(E_r | h_interaction) = 0.
```

This is now a genuine conditional-future-law distinction, not merely two
different outputs under one coupled realization.

### m1 — full B4 event/tip/predecessor composition: CLOSED

Regional messages now include and validate:

- exactly-once actor rows and endpoint ports;
- shared graph edges and external actor rows;
- persistent event records owned by their initiator;
- crossing event references with identical event contents;
- opaque predecessor references outside the visible event set;
- exactly-once wire-tip ownership.

Composition validates both inputs before union, promotes a referenced event to
owned when its initiator enters the union, recomputes opaque predecessor
references, validates the result and compares it with direct regional
projection.  All `159,734` registered ordered region pairs agree.

The six malformed-message attacks reject:

1. a missing owned endpoint port;
2. phantom ownership;
3. an inconsistent external actor row;
4. a corrupt shared edge;
5. corrupt shared event content;
6. duplicate event ownership.

B4 may therefore consume the typed-composition flag for its sufficient
growing-carrier verdict.  No necessity or minimality conclusion is introduced.

### m2 — first-applicable collisions and capacity tri-state: CLOSED

Capacity is now typed as exactly one of:

```text
BOUNDED_PROVED
UNBOUNDED_PROVED
UNKNOWN.
```

Rows 2 and 3 require proved boundedness and proved unboundedness respectively;
an unknown capacity cannot become a growing pass through a default false
Boolean.  Each Paper-21 predicate is calculated independently and the first
true row is selected without mutating earlier predicates in response to later
flags.

The engine tests all eight rows plus six overlapping-predicate cases:

- refusal over finite-domain evidence;
- bounded pass over whole-component necessity;
- growing pass over candidate obstruction;
- whole-component necessity over global necessity;
- universal class exclusion over candidate obstruction;
- unknown capacity refusing an otherwise complete pass.

The six registered branch verdicts agree with the frozen expected table.

## 3. Expanded endpoint attack

### 3.1 Registered interlopers: PASS

At each registered radius `r=0,1,2,3`, the receipt inserts four event families
between the conditioning stop and inward propagation:

- a later D idle;
- a later D interaction;
- an unrelated A event;
- several alternating/later D events.

All 16 cases retain

```text
E_r(h_idle future)        = True,
E_r(h_interaction future) = False,
```

and verify byte-for-byte immutability of the selected interaction record.
These are precisely the futures that defeated the round-2 moving-tip
predicate.

### 3.2 Fresh radii and new interlopers: PASS

I independently rebuilt the paired histories at unregistered radii `r=4` and
`r=7` and added four further families:

1. a D birth before propagation;
2. a leaf idle plus an inner-path-actor idle;
3. seven consecutive D idles;
4. alternating D interactions, leaf events and D idle.

For every family and both radii:

- the current complete `C_r` carriers remained equal;
- the idle-past pinned query remained true;
- the interaction-past pinned query remained false;
- the propagation mass was equal in the paired futures and positive;
- relabeling the entire graph/path preserved the idle query.

The exact positive combined interloper-plus-propagation masses were:

| `r` | event-family length | exact embedded mass |
|---:|---:|---:|
| 4 | 1 | `1/45097156608` |
| 4 | 2 | `1/107943428096` |
| 4 | 7 | `1/58054566272303104` |
| 4 | 4 | `1/338510590509056` |
| 7 | 1 | `1/215780714883317760` |
| 7 | 2 | `1/671088640000000000` |
| 7 | 7 | `1/2147483648000000000000000` |
| 7 | 4 | `1/4294967296000000000000` |

The zero is therefore not an artifact of forbidding later D events or of the
four registered radii.  It follows from selecting one immutable pre-stop event
by structural actor role and ordinal.

### 3.3 Relabeling and ordinal selection: PASS

Nominal relabeling maps the structural D actor and rewrites event IDs while
leaving its ring ordinal fixed.  The query reconstructs the selected ID from
the transported actor plus the same ordinal.  Fresh tests used a different
root name and new remote labels; all preserved the pinned event result.

No preferred literal Ulam spelling enters the theorem.  What is invariant is
the typed structural role and wire ordinal.

## 4. Probability and universal-quantifier audit

### 4.1 Exact embedded lower bound: PASS

For arbitrary finite `r`, the component has `r+3` actors along the forced
no-birth path.  Each selected child-to-parent interaction has embedded
probability

```text
1 / [8(r+3)].
```

The `r+1`-step subcylinder therefore has

```text
p_r = [1 / (8(r+3))]^(r+1) > 0.
```

The four printed values and the fresh-radius calls agree exactly with this
formula.  `p_r` remains correctly labeled a lower bound for the broader
Branch-F event, not its total probability.

### 4.2 Optional timed bound: PASS

During the exact path the component clock count stays `r+3`; no selected event
is a birth.  Conditional on the exact component action sequence, the first
`r+1` component-ring waiting times sum to an Erlang law with shape `r+1` and
rate `r+3`.  Thus for `Delta=1`

```text
p_r * F_Erlang(r+1, r+3; 1)
```

is a valid positive timed subcylinder bound.  The reproduced values

```text
3.959221E-2, 8.871307E-4, 1.367731E-5, 1.598963E-7
```

are correct.  Disconnected actor clocks do not alter this component-local
Poisson calculation.

### 4.3 Complete-radius universal class: PASS

`C_r` is frozen as the maximal physical record restriction owned by the closed
actor ball of radius `r`, including all events on owned wires and opaque
cross-boundary predecessor IDs but forbidding recursive dereference outside
the ball.  The differing pre-stop D event lies wholly outside every owned wire
and hence the paired `C_r` values coincide.

Because the complete `C_r` values coincide, every encoder that is a function
of that restriction also coincides.  The endpoint-pinned query distinguishes
the future laws for every finite `r`.  This proves nonexistence throughout the
declared class

```text
{C_r and its derived encoders : r finite},
```

not merely failure of four tested projections.  Paper-21 row 6,
`NO EXACT REALIZATION IN THE DECLARED CARRIER CLASS`, is therefore earned.
It does not exclude adaptive non-radius or whole-component carriers.

## 5. B4 and disconnected-scope audit

The complete A-component state is an all-future sufficient carrier because:

- the chosen law never creates cross-component edges;
- independent component Poisson sources factor at continuous construction
  time;
- the full component configuration is recursively Markov;
- relabeling and disjoint construction covariance pass;
- full actor/edge/event/tip/predecessor composition now passes;
- capacity is proved unbounded, not merely unknown.

The multi-step disconnected control preserves both the component restriction
and its component-local embedded path mass `1/24576`.  This result is scoped to
continuous construction time and component-local/A-local stops.  Fixed global
event depth remains a negative control because remote actors alter the global
event census.

No text or flag says that B4 is necessary.  The scorecard correctly emits

```text
ALL-FUTURE GROWING-CARRIER PASS
```

for B4, not `WHOLE-COMPONENT ONLY`.  Minimal full-ancestry encoding remains
open.

## 6. Intrinsic quantum refusal: PASS

E12 re-verifies the accepted finite D34c output hash

```text
9ce73a693b41f765eff163749ef769ca0cb4ce856ead66d690a63a20331a731a.
```

That dependency supplies finite strongly positive typed-DAG functionals and a
finite sewing result.  The auxiliary `P,E` object remains only a causal-break
negative control.  Neither supplies the missing timed controlled D34b-D34c
process or `P(r|I,h)` for the licensed all-instrument family.

The intrinsic branch therefore correctly emits `REFUSAL/UNDEFINED` and assigns
no SHARD `d_carrier`, `d_op` or `chi_cut`.  No existing finite result is erased
or promoted to the missing intrinsic process.

## 7. Terminal claim disposition

| Claim | Closing disposition |
|---|---|
| Complete `C_r` schema and paired equality | **PASS** |
| Endpoint-pinned common Branch-F event | **PASS** |
| Later idle/interaction/unrelated/multiple-event attacks | **PASS** |
| Fresh radii and relabeling | **PASS** |
| Exact embedded and optional timed bounds | **PASS** |
| Universal finite-radius carrier-class exclusion | **PASS — ROW 6** |
| B4 full event-history composition | **PASS** |
| B4 all-future growing sufficiency | **PASS** |
| B4 necessity/minimality | **OPEN; NOT CLAIMED** |
| Continuous/component-local disconnected invariance | **PASS** |
| Fixed-global-depth invariance | **correctly not claimed** |
| Capacity bounded/unbounded/unknown typing | **PASS** |
| First-applicable predicate collisions | **PASS** |
| Finite unmarked diagram | **PASS at finite source-prefix width** |
| Intrinsic timed quantum boundary | **REFUSAL/UNDEFINED — CORRECT** |
| Fresh regression | **NONE FOUND** |

The accepted maximum statement is:

> **QUERY-RELATIVE C/L ALL-FUTURE SUFFICIENT GROWING B3 CARRIER;
> COMPLETE-FINITE-RADIUS FULL-ANCESTRY CLASS EXCLUDED; WHOLE-COMPONENT B4
> SUFFICIENT GROWING CEILING; MINIMAL FULL-ANCESTRY, v9 POSTERIOR AND
> INTRINSIC QUANTUM BOUNDARIES OPEN/UNDEFINED.**

## 8. Scientific openings after closure

These are next investigations, not defects in D34e:

1. **Adaptive Branch-F frontier.** Search between fixed-radius `C_r` and B4
   for a recursively updating non-radius carrier of live tips and immutable
   ancestry summaries.
2. **Minimal/weak timed quotient.** B3 is sufficient and unbounded; the
   canonical weak/timed predictive quotient and the existence of another
   bounded physical carrier remain open.
3. **Finite quantum boundary.** A separately frozen finite D34c branch may
   measure finite-domain operational widths without pretending to be the
   intrinsic lift.
4. **Intrinsic quantum process.** Construct the timed controlled D34b-D34c
   process and all-instrument kernels before assigning SHARD quantum widths.
5. **v9 posterior bridge.** The finite unmarked source-prefix diagram is only a
   negative/control input to a future adapted posterior-factorization study.

## 9. Closing stamp

Every round-2 finding is closed.  The exact final count is:

> **0B / 0M / 0m / 0n.**

The ancestry/quantum hostile-review stream is **TERMINAL / DELTA-CLEAN** at the
frozen D34e claim width.  No further source repair is required by this stream.
