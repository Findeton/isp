# D34e round 3 — boundary/locality closing hostile delta

**Frozen target:** commit
`6e6676bc46f377139c91d76d28f9f0fa0b7f9074`.

**Comparison base:**
`d34e-round2-boundary-locality-hostile-delta.md` at `bd143fb`.

**Exact verdict:** **CLOSING DELTA CLEAN AND TERMINAL AT THE DECLARED
BOUNDARY/LOCALITY CEILING — 0 BLOCKER / 0 MAJOR / 0 MINOR / 0 NIT.**

The second replacement closes the round-2 output/gauge major and all three
minors.  Counter-bearing outputs now belong to the kernel actually tested;
the role generator is root-parameterized and its nominal transport includes
fresh children.  Full regional composition now carries actor rows, endpoint
ports, shared edges, persistent events, crossing event references, opaque
predecessor references and owned wire tips.  The capacity ledger and verdict
type are complete for the constructed B3.  Independent fresh-root,
multi-event, associativity, malformed-message, capacity and collision attacks
found no residual defect.

“Terminal” means that this hostile stream has no remaining issue at D34e's
stated claim width.  It does not mean that B3 is minimal or bounded, that A
privately stores the distributed collar, that the canonical weak timed
quotient is known, or that a controlled quantum or spacetime-cone law has been
constructed.

## 1. Frozen reproduction

The frozen artifact hashes independently reproduce as:

```text
code   e66490560f7c38af746b6fea144a4356dfdb3630205eab9f46723ed8c830bff8
stdout 3d12f6191883ee3790c78498bae4bb1971144765341a354df587d33188f54498
note   2c6bed036361ce6bea4b671691de77886742acb552709bc885a69e6950cb2120
```

Fresh executions under `PYTHONHASHSEED=17` and `65537` exit zero and are
byte-identical to the committed stdout.  The internal summary hash is

```text
a53fa0c18a5905f282cea4c283ec3061c049ad7378a00f624906c1d68091d701.
```

The exact receipt reports:

```text
gates                                      13/13
reachable levels                          1,6,40,304,2576
registered states                         2927
registered strong classes                 111,111,111
physical B3 row updates                    35898
role-output conjugacy states               2927
disjoint construction swaps               120276
full regional compositions                159734
malformed messages rejected               6/6
capacity probe                             5/1/5/5/9/112/60
```

`git diff --check` is clean.  No tracked source artifact was modified by this
review.

## 2. Round-2 finding disposition

### M1 — output alphabet and root/role conjugacy: CLOSED

The coarse output map now emits, on every non-silent row,

```text
kind : post-carrier : post-own-count : post-wire-count.
```

The role-labeled map additionally retains the relevant incident participant or
fresh child.  Silent neighbor births remain internal `None` transitions.  The
registered strong boundary-transition census is consequently the correct

```text
(111,111,111),
```

not the first replacement's `(29,29,29)`.  The receipt also stops calling this
strong refinement the canonical weak/timed quotient; it is used only as a
sufficient-carrier stress test.

The closed role generator now accepts its distinguished root as an argument.
Nominal transport acts on output handles, successor carrier roles and any
fresh Ulam child.  The executable checks

```text
T_g Rows_root = Rows_(g root) T_g
```

on all `2,927` registered states for the frozen `A -> R` chart.

I attacked this with a different namespace, every possible distinguished root
on a deterministic spread of registered states, and formulas closed directly
in each renamed chart.  All `95` fresh cases satisfied exact equality between:

1. transported original rows;
2. rows obtained by stepping the renamed state; and
3. the root-parameterized closed formula.

All `95` birth rows generated the transported fresh child `g(root)/n`.  The
round-2 `A/1` versus `R/1` counterexample is therefore eliminated rather than
hidden.

**Disposition:** **CLOSED.**

### m1 — incomplete message ownership and validation: CLOSED

The physical regional message now contains the entire declared composition
interface:

- exactly-once owned actor rows and endpoint ports;
- one shared edge object with the required owned endpoint ports;
- consistent external actor rows;
- persistent events owned by their initiator;
- crossing event references with checked contents;
- opaque predecessor references not visible in the region; and
- exactly-once owned wire tips whose event is visible and touches that wire.

Composition validates both inputs, unions disjoint ownership, promotes an
event reference to owned when its initiator enters the union, recomputes the
opaque predecessor set, validates the output and compares with direct regional
projection.  All `159,734` registered ordered pairs pass.

The six frozen malformed-message attacks all reject.  I additionally attacked
the new history fields with:

```text
phantom wire tip,
missing visible tip event,
phantom opaque predecessor,
event reference that no longer crosses the region,
owned port without its edge.
```

All five were rejected before union.  On event-bearing states I also compared
both parenthesizations of three singleton-region unions with direct projection;
all `52` fresh associativity cases agree exactly.

This proves composition for valid physical projections and fail-closed typing
for the declared interface.  It does not pretend that one region can detect a
lying owner by reconstructing that owner's entire history.

**Disposition:** **CLOSED.**

### m2 — incomplete B3 capacity ledger: CLOSED

The ledger now enumerates all fields of the constructed C/L carrier:

```text
radius and actor/reference count,
root carrier bit,
root own/birth/wire integers,
neighbor degree and birth-ordinal integers,
owned endpoint ports and shared incident edges,
nominal identifier/reference encoding,
port/edge handle encoding,
relative elapsed-time coordinate,
renewal-age scope,
bounded-alternative/minimality scope.
```

I independently recomputed the representative receipt as

```text
d/root-bit/degree-bits/neighbor-birth-bits/counter-bits/id-bits/handle-bits
= 5/1/5/5/9/112/60.
```

The bit counts use a declared reference representation and are properly
described as encoding-dependent.  The physical verdict does not depend on that
choice: A births occur with positive probability at every finite multiplicity,
so B3's incident actor/port count has unbounded support.  The note says only
that this concrete B3 is unbounded; a different bounded sufficient carrier and
minimality remain open.

**Disposition:** **CLOSED.**

### m3 — Boolean capacity conflated proved-unbounded with unknown: CLOSED

Capacity is now explicitly three-way:

```text
BOUNDED_PROVED / UNBOUNDED_PROVED / UNKNOWN.
```

Paper-21 row 2 consumes only `BOUNDED_PROVED`, row 3 only
`UNBOUNDED_PROVED`, and an otherwise complete row with `UNKNOWN` returns
`REFUSAL/UNDEFINED`.  My independent probe reproduced exactly those three
outcomes.

All eight decision rows and six frozen predicate collisions pass.  A fresh
specimen with every later predicate true plus missing required inputs selected
the first row, `REFUSAL/UNDEFINED`, confirming first-applicable priority rather
than accidental Boolean suppression.

**Disposition:** **CLOSED.**

## 3. Physical B3 updater and locality attack

The physical B3 carrier remains the distributed radius-one star, not an
independent centralized subsystem attached to A.  A owns its carrier/counters
and endpoint ports; each neighbor owns its degree/birth row; incident edges are
shared typed objects.  The updater consumes only:

```text
old B3 + elapsed increment + one typed local/passive/silent event view.
```

It never rereads the full global configuration.  The frozen exhaustive gate
matches direct projection after all `35,898` registered full-state rows.

I independently ran this seven-event history with unequal rational time
increments:

```text
B birth                  neighbor-owned silent update
B idle                   irrelevant event
B -> A                   passive incoming update
A birth                  root update and fresh A/1 port
A/1 birth                fresh neighbor-owned update
A -> A/1                 root outgoing update
P -> Q                   disconnected remote event
```

The typed views were

```text
neighbor-birth, None, incoming, root-birth,
neighbor-birth, root-outgoing, None,
```

and recursive B3 agreed with direct projection after every event, at final
elapsed time `28/17`.  This exercises a neighbor created during the trajectory,
passive reception, a silent collar change and a disconnected event in one
path.

Construction-order covariance remains separate from nominal covariance.  The
`120,276` disjoint write-support swaps agree on both the complete record DAG
and B3 projection.  The theorem scope is the correct one: disjoint actor/event
supports commute up to serialization gauge; events sharing a wire are not
declared independent.

The reference executable is centrally scheduled Python, but that is not a
physical global-state dependency in the law.  It is a serial implementation of
commuting typed actor updates.  Conversely, B3 is not claimed to be A's private
memory: it is a distributed collar readable by an analyst or regional
composition.

## 4. All-future and stopping-scope audit

The all-future claim is not inferred from depth-four enumeration.  For every
finite D34b configuration, the analytic generator partitions every row that
can change the coarse or role-labeled star.  Each aggregate rate and successor
depends only on B3.  The parent D34b nonexplosion theorem then gives a unique
projected pure-jump law for all future times.

The licensed stops remain exactly:

1. fixed finite construction time;
2. A's own-ring count hitting a finite level; and
3. A's wire-event count hitting a finite level, including passive incoming
   events.

Elapsed time is reset to zero at conditioning and accumulates relative
increments.  In the CTMC representation event time is the jump-time coordinate;
the string mark separately retains kind/direction, post carrier and the two
absolute counters.  Exponential memorylessness removes a renewal-age field for
this chosen law.  The two counts are monotone coordinates of the complete
boundary CTMC, so the inherited nonexplosive strong-Markov theorem applies at
their hitting times.  Fixed global event depth remains only an enumeration
device and locality-negative control.

Thus C and L retain the exact verdict

```text
ALL-FUTURE GROWING-CARRIER PASS / POINTWISE
```

for the chosen D34b law and declared passive relative-time queries.  No null-
history regular conditional, absolute global clock field or fixed-depth
locality inference is consumed.

## 5. Closing claim ceiling

This stream accepts the following and no stronger statement:

> For the chosen continuous-time D34b law, a distributed physical radius-one
> B3 star is a recursively updating, nominally covariant,
> construction-order-covariant, composable and all-future sufficient carrier
> for the declared coarse and role-labeled passive A-wire laws at fixed-time
> and local count stops.  This B3 has unbounded width.  Its finite strong
> boundary-transition census is a stress test, not a minimal weak/timed
> quotient.

The following remain open and are not defects in this scoped D34e result:

- a bounded sufficient carrier or a no-go theorem for all bounded carriers;
- minimality of B3 and the canonical weak timed observed-process quotient;
- a private-A implementation of a distributed collar;
- the missing controlled quantum/instrument process;
- a v9 posterior/profinite sufficiency bridge; and
- any implication for grown spacetime dimension or cone roundness.

**Final count: 0B / 0M / 0m / 0n.  Boundary/locality stream terminal at the
declared ceiling.**
