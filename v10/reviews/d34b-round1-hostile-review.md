# D34B ROUND-1 HOSTILE REVIEW — fresh referee, clean-room rebuild — 2026-07-13

**Target:** `code/d34b_exponential_clocks_exact.py` + `data/d34b_exponential_clocks_exact.out` (6/6) at 956d7dd — THE MEASURE-COMPLETING STEP of the history-law phase. Pins: note-d33 §6 (53e8fb9, pre-committed 13:38, receipt 13:42; the pin file verified UNMODIFIED since — empty diff 53e8fb9..HEAD). Lineage: d34a (bead278→c1f17b8→terminal at #159) and its round-1 review (M2 = the |present| pushforward failure; M3 = race-not-gauge); LEDGER #158–#160; LOG 2026-07-13 d34b entry (the first-run target-correction confession).
**Method:** receipt rerun and byte-compared (in-process; PYTHONHASHSEED 7/61 — fresh salts extending the LOG's 0/13); the machinery re-implemented faithfully and then attacked with tools the receipt does not have: (i) exact projected Markov chains for every gated event (state = live-count k × event phase, Fractions), validated against every printed lo/inc at depths 2–4 and against direct enumeration at depth 5, then run to depth 30; (ii) closed forms derived by hand for the label-frozen events; (iii) a label-blind `realizes` variant; (iv) sandwich-width quantification (inc/target) and wrong-value bracketing tables; (v) normalization probes on the per-record algebra (Σμ_act over act-alphabets and product partitions); (vi) misclassification exhibits for `realizes` (label collision; reception in the matched cone). Referee harness: `scratchpad/ref1_widths.py`, `ref2_chains.py`, `ref3_probes.py` (session scratchpad).

**VERDICT: REJECT AT STATED WIDTH — MAJOR-REVISION. 4 MAJOR / 6 MINOR / 3 NIT. Zero false printed numbers (all 14 printed fractions reproduce exactly, including 10179/65536 and 131193221/172032000); zero gate flips at the committed depth; but THREE OF THE FOUR SANDWICH TARGETS GATE THE WRONG EVENT, their sandwiches converge to 1/3, 1/12, and 1/5 — not the printed targets 1/2, 1/8, 1/2 — and the gates FAIL AS CODED at depths 8 (K2-T1), 29 (K2-T3), and 12 (K5) when the receipt's own machinery is run deeper. The depth-≤4 GREEN is entirely an artifact of sandwich width — the exact disease the receipt's own LOG confessed for the pre-fix run ("K5 had passed against the wrong target only because the 4-record sandwich is wide") and then did not cure: the fix changed the target and left the width. The mechanism is a fresh-label collision in `realizes` (d34a-n1, deferred at #159 as cosmetic, now load-bearing): `paths` assigns birth labels by GLOBAL birth count, so D = [('b','A','w1')] matches "A's first act is a birth AND it is the path's first birth" — a linearization-gauge-dependent event, not a causal down-set event. K5 — THE CRUX — is thereby ANTI-VERIFIED: the gated event's exact marginal moves from 1/3 (2-record world) to 1/5 (4-record world) when the remote pair is added, the precise OPPOSITE of "the placement factor integrates out of local marginals"; the pass at depth 4 is width alone (the depth-4 K5 sandwich brackets 1/4, 1/3, 3/8, 1/2, 5/8, 2/3, AND 3/4 — including the pre-fix convicted target and the true limit). What survives: T2 (the one label-free target — its sandwich genuinely brackets and converges to its target; chain-verified to depth 30), the construction itself (unit-rate exponentials ⇒ jump chain = uniform 1/k, the correct answer to d34a-M3 at the construction level), K1/K3's arithmetic, and the Harris-lemma CONTENT, which this review certifies is true and provable by a renewal argument the receipt never makes. The measure-completion claim does NOT survive at its stated width.**

---

## THE CENTRAL ADJUDICATIONS (as put to this round)

1. **Is the sandwich gate real evidence for the Harris lemma? NO — at 3 of 4 targets it is the d34a vacuity in a new costume.** Quantified at depth 4: inc/target = 0.706 (T1), 0.706 (T2), **4.43 (T3)**, **1.21 (K5)**. The gate's pass window at the committed depth is [lo(4), hi(4)] — width 0.35 to 0.61 in absolute probability — and "bracket + narrow" passes EVERY target value in that window: T3's window [0.0205, 0.574] contains its own pre-fix convicted target 1/32 as well as 1/16, 3/32, 1/4; K5's window [0.155, 0.763] contains the pre-fix convicted 1/4, and 1/3, 3/8, 5/8, 2/3, 3/4. Narrowing carries no target information: for prefix-cylinder events inc(n) is structurally monotone (an incomplete path can only complete or die), so the two strict comparisons can never fail for ANY target inside the depth-4 bracket. The demanded derivation: for T1 the true renewal answer is q_b/actmass = 1/2 (A's draws are iid (b¼, i¼, idle½) independent of the interleaving; first non-idle draw is a birth w.p. ½) — but the receipt's OWN lo follows MY closed form for a different event: **lo_T1(n) = (1/3)(1−(5/8)^n)** — 13/64, 129/512, 1157/4096 at n = 2, 3, 4, matching the printed values EXACTLY (and depth-5 direct enumeration = 9881/32768 = the formula) — a geometric series summing to **1/3, not 1/2**. The partial sums ARE series-exact — for the wrong series. hi(n) = lo(n) + P(A not yet acted) → 1/3, and the bracketing inequality fails at n = 8 (hi(8) = 0.48899 < 1/2). Same for T3 (limit (1/3)(1/4) = 1/12; fails at n = 29) and K5 (below). Honesty here required exactly what the task names: a series/rate gate (lo against independently derived partial sums, or midpoint convergence) — it was derivable (this review derives it), it was cheap, and its absence is what let the wrong event hide. Only T2 — the one target whose D carries no fresh label — gates a true causal event: its lo matches my independent k-inflation chain at every depth (41/192, 1277/4608, 89429/276480), converges to its target 1/2, and never fails to depth 30.
2. **Does the per-record algebra cure d34a's M2? NOT DEMONSTRATED.** There is NO normalization gate anywhere: nothing shows the measure of the whole space is 1 under the new algebra. I verify that it in fact HOLDS — Σμ_act over A's act-alphabet = 1 at lengths 1 and 2, and Σμ_act over the (A-first-act × B-first-act) product partition = 1 — so the repair is one cheap gate, but the receipt ASSERTS the #158 cure and gates only the old d34a telescope (K1, re-run on 3 histories) plus one single-step sum and one factorization pair (K3). μ vs μ_act is NOT cleanly separated where it counts: the docstring headline calls μ = Πq "THE MEASURE" on the per-record algebra, yet on the algebra the sandwiches actually match (act-prefixes) Σμ over the level-1 alphabet = **1/2** and over level 2 = **1/4** — the receipt's own K3 computes the 1/2 and prints it as "act mass" (μ is normalized only on idle-INCLUSIVE prefixes, which no sandwich ever gates). And `realizes` is NOT cone-closure: it matches the S-initiated wire-order subsequence. Constructed exhibit: path [('i','B','A'), ('b','A','w1')] — A receives passively, then births; `realizes` says 'yes' for D = [('b','A','w1')], but the birth's past-cone (pred[1] = {0}) contains the reception, so D is NOT cone-closed on that realizing path — the matched class is a wire-order-initiator SURROGATE for the pinned noun. For these targets the surrogate's aggregate value is saved by collar-locality (receptions never alter A's conditionals — a lemma THIS REVIEW supplies, via d34a-#159's in-code statement; the receipt is silent), and KF3 — the pin's own name for exactly this subtlety — appears NOWHERE in the receipt (grep: 0). A front may stay open; gating "cone-closed down-set" language on an undisclosed surrogate is a hole.
3. **K5's crux, derived independently: the receipt's own numbers refute its claim.** In the 4-record world the gated event is "A's first act is a birth AND no other record births first" (the label collision). Pre-absorption the live count is FROZEN at k = 4 (any other birth burns w1 and dooms the path; A's own act absorbs), so per ring: good = (1/4)(1/4) = 1/16, absorb = 5/16, neutral = 11/16, giving **lo_K5(n) = (1/5)(1−(11/16)^n)** — 27/256, 553/4096, and at depth 4 **10179/65536 EXACTLY as printed**. Limit: **1/5**. In the 2-record world the same D gives (1/3)(1−(5/8)^n) → **1/3**. So for the event actually gated, adding the remote pair CHANGES the local marginal from 1/3 to 1/5 — a 40% shift — which is the exact negation of "the 1/k placement factor integrates out of local marginals"; the printed pass exists only because the depth-4 sandwich (width 0.61, inc/target 1.21) brackets 1/5, 1/4, 1/3, 3/8, 1/2, 5/8, 2/3, and 3/4 alike. The gate as committed fails at depth 12 (hi = 0.4798 < 1/2). The TRUE claim is derivable and survives: label-blind, "A's first act = birth" has limit 1/2 in BOTH worlds (renewal; remote records slow the clock but not the draw), and my label-blind rerun gives blind-T1 ≡ T2 exactly at every depth (four exact equalities, depths 2–5) and blind-K5 = [0.196, 0.804] at depth 4 — converging to 1/2, slower (KF1). The receipt gated the wrong object; the right object was one binding-map away.
4. **K4 as labeled: the gate is cosmetic.** `'tie' not in src` is a string check: a tie rule named `resolve_simultaneous` would PASS it; an innocent comment containing the word "tie" would FAIL it (both demonstrated). The substantive facts — each `rec` step appends exactly one event (strict total orders by construction), continuous unit-rate clocks tie with probability zero, and the uniform 1/k actor draw IS the exponential race's embedded jump chain (the memorylessness theorem the docstring asserts) — are all TRUE, none gated; the discharge is acceptable in substance, and "audited" oversells a grep. The embedded-chain identity, which is the entire licence for the word "exponential" in the receipt's title, is asserted in the docstring and never stated as the (citable, standard) theorem it is.
5. **K3's "restriction": a single-record, single-step surrogate, over-labeled.** The gate sums A's next-act q's after one down-set (= actmass = 1/2 BY DEFINITION of q — it can fail only on enumeration bugs) and checks one factorization pair on μ (not μ_act) — which is near-tautological: μ is a product of per-event own-past factors, so register-disjoint concatenation factorizes for ANY per-event law (a deliberately wrong 1/3-per-event law passes the identical check). The pin's K3 — "full restriction consistency on down-sets... exact", i.e. (restriction)_*μ_{n+1} = μ_n on the per-record algebra — would be: for every down-set at per-record level vector L, the sum over one-act extensions ACROSS ALL PRESENT RECORDS of μ_act equals μ_act(D) (equivalently the product-partition mass-1 identity of adjudication 2). It is absent. The idle-complement renormalization IS disclosed (comment + LOG); the scope narrowness is not — the label says "restriction on down-sets [exact]" and #160 echoes "restriction on down-sets exact with DISJOINT-CONE FACTORIZATION".
6. **Pin fidelity §6, clause by clause.** K1: PARTIAL — the pinned per-record ALGEBRA is never constructed as an object; the gate re-runs d34a's telescope on 3 lineage histories (content right, label inflated). K2: CRITERION CHANGED — the pin says the marginal "equals Π q over the down-set's events" and pins the sandwich with Πq as the middle term; the receipt gates μ_act = Πq/actmass. The change is disclosed in the LOG and the K2 comment — honestly, as far as it goes — but the PASS LABEL still prints the pin's formula ("brackets mu(D) = prod q"), which is arithmetically FALSE at the receipt's own depth-4 numbers (Πq = 1/4 lies OUTSIDE [1157/4096, 351439/552960] and outside [89429/276480, 187051/276480]); worse, the pin's own Πq form is TRUE and directly gateable on idle-inclusive initiated prefixes (renewal: A's draw sequence is iid q), so the criterion change was unnecessary. K3: surrogate (above). K4: "verified structurally" → a grep. K5: the pin's "local down-set marginals exactly unchanged under remote clusters" is REFUTED for the event actually gated (1/3 → 1/5). K6: FAITHFUL — Kolmogorov stated-not-claimed, and the d34c re-scope of (5)/(7) is disclosed in the pin with reasons and carried verbatim into #160; no complaint. KF1: the pin says "geometric tails, rate printed" — the K5 label claims "(…rate cost of remote clocks, printed)" and NO rate is printed anywhere, only the intervals. Hash-salt audit: clean (every set feeding output is sorted or order-inert; `regs_of` iteration affects nothing printed) — byte-identical across 4 salts total (LOG 0/13; referee 7/61). First-run lesson: the disclosure is honest and the corrected targets (1/2, 1/2, 1/8) are the RIGHT values for the TRUE events — but the same paragraph's own diagnosis (pass-by-width) indicts the gate design that was then committed unchanged.

---

## MAJOR

**M1 — Three of four sandwich gates match a label-entangled event, not the pinned causal down-set; their sandwiches converge to 1/3 (K2-T1), 1/12 (K2-T3), 1/5 (K5) instead of the printed targets 1/2, 1/8, 1/2; the gates fail as coded at depths 8, 29, 12.**
`paths` assigns fresh birth labels by global birth count (`w{zctr}`, zctr shared across records), so on any path where another record births before A, A's own birth is labeled w2 and `realizes` returns 'no' against D = [('b','A','w1')] — exhibited: [('b','B','w1'), ('b','A','w2')] is classified 'no' although A's first act IS a birth (label-blind: 'yes'). The matched event is therefore "A's first act = birth AND it is the path-first birth" — a function of the linearization gauge (d33!), not of the typed causal history, and not local to A's cone. Exact consequences (projected chains validated against every printed lo/inc at depths 2–4 and against depth-5 direct enumeration): lo_T1(n) = (1/3)(1−(5/8)^n) → 1/3; lo_K5(n) = (1/5)(1−(11/16)^n) → 1/5; lo_T3 → 1/12; hi → the same limits; first bracketing FAILURE of the committed gate code at depth 8 (T1: hi = 0.48899 < 1/2), depth 29 (T3: hi = 0.12311 < 1/8), depth 12 (K5: hi = 0.47979 < 1/2). d34a-n1 ("fresh-label bookkeeping is linearization-dependent... harmless under canon"), deferred at #159 as receipt-internal cosmetics, became the load-bearing defect of the successor's central gates. Repair: label-blind `realizes` (bind D's fresh labels at first appearance; partner references resolve through the binding) — after which blind-T1 ≡ T2 exactly (verified, depths 2–5) and all limits are the μ_act targets.

**M2 — The sandwich criterion has no discriminating power at the committed depth, the LOG's own confession identified exactly this disease mid-build, and the pinned KF1 rate gate that would have caught it was claimed ("printed") but not implemented.**
Bracket-at-3-depths + strict narrowing passes EVERY target in [lo(4), hi(4)]: windows of width 0.35 (T1/T2), 0.55 (T3), 0.61 (K5); T3's window contains its pre-fix convicted target 1/32; K5's contains its pre-fix convicted 1/4 AND the true limit 1/5 AND every eighth from 1/4 to 3/4 except the endpoints. Narrowing is structural for prefix-cylinder events (inc monotone by construction), so it adds no evidence. The LOG records: "K5 had passed against the wrong target only because the 4-record sandwich is wide" — the committed fix changed the TARGET and kept the WIDTH, so the committed GREEN still rests on the same masking (now masking a wrong event rather than a wrong hand literal). The honest gate exists and is cheap: gate lo(n) SERIES-EXACT against independently derived partial sums (the closed forms above for frozen-k events; the k-inflation chain for the others — this review's chains reproduce every printed sandwich number), or gate |mid − target| decreasing with a printed geometric rate (KF1's pinned content). Note the pin itself pinned only "hold and NARROW" — the gate-design weakness is partly the pin's — but the receipt's label claims a rate was printed, and none is.

**M3 — μ vs μ_act: the printed PASS layer asserts the pin's formula while the code gates the changed one; two PASS sentences are false against the receipt's own printed numbers.**
(i) K2's label: "brackets mu(D) = prod q at every depth" — Πq = 1/4 for T1 and T2 lies OUTSIDE both printed depth-4 sandwiches (recomputed: 1/4 < 1157/4096 and 1/4 < 89429/276480); the bracketed object is μ_act = Πq/actmass. (ii) K5's label: "still brackets the SAME mu(D) = 1/4" — the gated value is μ_act = 1/2 (the same line prints "around 1/2"); 1/4 is the UNNORMALIZED μ, and "the SAME" is false in content for the gated event (1/3 vs 1/5, M1). (iii) The docstring headline "THE MEASURE... mu(D) = prod q" is unnormalized on the algebra the sandwiches match: Σμ over A's level-1 act-alphabet = 1/2 (the receipt's own K3 prints this number as "act mass") and 1/4 at level 2; μ is normalized on idle-INCLUSIVE prefixes, which no gate ever matches. The act-normalization criterion change vs the pin IS disclosed (LOG + K2 comment) — the false labels make the disclosure incoherent, and the pin's own Πq claim, which is true and directly gateable on idle-inclusive initiated prefixes with NO actmass division, is never gated. This is the d34a Δ-m1 noun disease (an unnormalized object printed as "measure"), third appearance in the phase, now inside PASS lines.

**M4 — The #158 cure (per-record algebra) is asserted, not demonstrated: no normalization gate, no full restriction, no multi-record or reception-containing D ever sandwich-gated.**
d34a died on Σμ = |present|·μ — a mass identity. The receipt's answer carries NO mass gate on the new algebra: nothing verifies Σμ_act = 1 over any complete partition at any level (I verify it holds: per-record act-alphabets at lengths 1–2 and the (1,1) two-record product partition all sum to 1 exactly — the gate is one loop); K1 re-gates d34a's telescope; K3 is a single-record single-step sum (= actmass by definition) plus one factorization pair that a deliberately wrong per-event law also passes (register-disjoint bookkeeping, not law content). The pin's K2 quantifier "ANY past-cone-closed down-set" is instantiated by four single-record, initiator-only, reception-free act-prefixes (three in SEED2, one in SEED4). The Harris-lemma gate never touches a multi-record down-set, a reception-containing down-set, or the path-law marginal of the factorization identity (K3's pair is gated on the weight system μ, not on path-law marginals at all). What the receipt calls "the measure-completing step" completes, at gate level, no mass identity of any kind.

## MINOR

**m1 — `realizes` is a wire-order initiator surrogate for "cone-closed down-set", and KF3 is silently absent.** Exhibit: [('i','B','A'), ('b','A','w1')] realizes D = [('b','A','w1')] as 'yes' while the birth's past-cone contains the reception — the matched D is not cone-closed on that path; passive receptions (events on A's wire A never drew — the pin's KF3, verbatim) are excluded from `sub` without a word in the receipt (grep KF3: 0 hits). The surrogate's VALUE coincides with the cone-marginal for these targets only by collar-locality (receptions never change A's eligible set) — a lemma d34a #159 stated for q and this review re-derives for the marginals; the receipt neither states nor cites it here.
**m2 — K4's gate is a grep dressed as an audit.** Demonstrated: a tie rule named `resolve_simultaneous` passes `'tie' not in src`; an innocent comment containing "tie" fails it. The substantive discharge (one event per `rec` step; a.s.-no-ties for continuous clocks; uniform 1/k = the exponential race's embedded jump chain) is right but lives entirely in prose; the embedded-chain identity — the licence for "exponential" in the receipt's name — is asserted in the docstring, never stated as a theorem or gated structurally.
**m3 — K1's label oversells.** "the PER-RECORD cylinder algebra... with mu(D) = prod q; the per-record telescope re-gated" — the gate constructs no algebra and gates no μ; it re-runs the d34a per-record telescope (idle-inclusive q sums to 1) on 3 lineage histories. Content correct, evidential weight ≈ lineage regression only.
**m4 — K3's factorization is near-tautological and gated on the wrong object.** Product-of-own-past-factors factorizes over register-disjoint lists for ANY per-event law (wrong-law demonstration passes); the gate is on μ where the phase's object is μ_act (the μ_act factorization also holds — unverified by the receipt); "DISJOINT-CONE down-sets FACTORIZE" as a PASS headline claims a theorem the gate cannot distinguish from bookkeeping.
**m5 — Grade inflation in K6 and #160.** "(1)(2)(3)(4)(6) at the measure level at this depth": (1)'s parenthetical "with the path law's placement integrating out (K2/K5)", (2) via K3, and (6) via K5 all rest on the broken gates; #160's headline "K5 crux: remote 1/k integrates out of local marginals" and "brackets μ_act... exactly... INCLUDING under remote clocks" carry M1's false content to the ledger; KF1 "rate... printed" — no rate exists in the .out. (3)/(4) honestly carried from d34a law level; the Kolmogorov disclaimer and the d34c re-scope are clean.
**m6 — Target-family narrowness undisclosed.** All four sandwich targets are single-record A-initiated act-prefixes of length ≤ 2 in worlds where A's eligible set never exceeds 2; "any past-cone-closed down-set" (pin) and "cone-closed down-sets" (docstring/labels) describe a class the gates sample at its four easiest points. One scope sentence owed (the d34a "at this scope" discipline).

## NIT

**n1** — K1 hardcodes `zctr = 9` (fresh label 'w9') for its option enumeration — collision-free on the 3 lineage histories only by inspection; one comment owed.
**n2** — "ALL CHECKS PASS (6/6…)" counts K6's `check(..., True)` freebie; with K4 a grep, substantive gates are 4 of 6 (the print line does disclose "K1-K5 substantive, K6 scorecard" — the d34a precedent — but K4's substance is prose, so the honest count is lower).
**n3** — `paths` stores every prefix and the caller filters `len(acts) != depth` (leaf-only use, O(all-prefixes) memory); `det2` reads the loop variables after the depth loop (correct "depth-4" by loop order — fragile); `import inspect` mid-file.

## SURVIVES ATTACK

- **T2 — the one honest gate:** D = [('i','A','B')] is label-free; my independent k-inflation chain reproduces its lo and inc EXACTLY at every printed depth (41/192, 1277/4608, 89429/276480; inc 55/96, 1027/2304, 48811/138240), the bracketing never fails to depth 30, and the limit is the target 1/2 by the renewal argument. Genuine finite-depth evidence for the Harris marginal on that event.
- **The construction:** unit-rate exponential clocks ⇒ the embedded jump chain is uniform 1/k over live records — d34a-M3's actor-layer objection is structurally answered AT THE CONSTRUCTION LEVEL (the 1/k draw is now the exponential race, not an ad hoc census); the failure moved to the verification layer, it is not in the object.
- **The Harris-lemma content (referee-derived, not receipt-established):** collar-locality ⇒ each record's draw sequence is iid q, independent of the interleaving and of remote records ⇒ P(y's initiated prefix = D) = Πq exactly for idle-inclusive prefixes and Πq/actmass for act-prefixes, in ANY world — the pin's K2/K5 claims are true and provable for initiator-prefix events. The corrected targets (1/2, 1/2, 1/8) are the right values for the true events; the label-blind rerun converges to them (blind-T1 ≡ T2 exactly; blind-K5 [0.196, 0.804] at depth 4, limit 1/2).
- **Arithmetic layer:** all 14 printed fractions recomputed exactly (13/64… through 10179/65536 and 131193221/172032000; act mass 1/2; factorization 1/16; targets 1/2, 1/2, 1/8); μ_act's mass-1 identities hold wherever probed (ungated, M4).
- **Discipline:** pin pre-committed 4 minutes before the receipt and unmodified since (empty diff); working tree clean at 956d7dd; rerun byte-identical; byte-identical under PYTHONHASHSEED 7/61 (extending LOG's 0/13); d34a lineage 7/7 byte-identical; ~1.0 s.
- **Disclosures that are real:** the first-run hand-literal confession (targets now formula-computed — true in code); the d34c re-scope of (5)/(7) pinned in §6 with reasons and carried to #160 verbatim; the K3 idle-complement renormalization comment; the Kolmogorov stated-not-claimed line.

## GATES RE-VERIFIED

`python3 code/d34b_exponential_clocks_exact.py` at 956d7dd: **6/6 PASS as coded, 1.0 s, .out byte-identical (in-process and subprocess), byte-identical under PYTHONHASHSEED ∈ {7, 61}** (LOG's {0, 13} extended). Lineage: `code/d34a_harris_lemma_exact.py` 7/7, byte-identical. Referee harness: sandwich rebuild identical at all printed depths; projected exact chains matching every printed lo/inc at depths 2–4 and depth-5 direct enumeration (9881/32768); closed forms (1/3)(1−(5/8)^n), (1/5)(1−(11/16)^n) matching the receipt's lo termwise; failure-depth scan to 30; label-blind variant; normalization probes; misclassification exhibits.

## FALSE-RESULT COUNT

**Printed numbers: 0 false** (every fraction in the .out recomputed exactly). **Gate flips at committed parameters: 0** (6/6 passes as coded at depth ≤ 4). **Gates refuted in extension: 3 of 4 sandwich targets** — the same committed code fails its own bracketing check at depth 8 (K2-T1), depth 29 (K2-T3), depth 12 (K5); the committed GREEN is depth-cherry-picked by width, not by intent (no rate gate existed to reveal it). **False printed sentences: 3** — K2's "brackets mu(D) = prod q at every depth" (Πq = 1/4 lies outside its own two printed depth-4 sandwiches); K5's "still brackets the SAME mu(D) = 1/4" (gated value μ_act = 1/2; "SAME" false in content, 1/3 vs 1/5); K5's "the placement factor integrates out of local marginals" (the gated event's marginal moves 1/3 → 1/5). **Misgrade class:** the docstring's "THE MEASURE... mu(D) = prod q" (unnormalized on the matched algebra — Σ = 1/2, the receipt's own K3 number); "(KF1... rate..., printed)" with no rate printed; K6's "(1)(2)(6) at the measure level"; #160's crux headline and "restriction on down-sets exact" echo. The receipt's arithmetic layer is clean; its event layer and its claim layer are not.

## OWED REPAIRS (exact list)

1. **(M1)** Make `realizes` label-blind: bind D's fresh labels to path labels at first appearance; partner references resolve through the binding. Certificates to add: blind-T1 ≡ T2 exactly at every depth (the birth/interact symmetry — free and exact); all limits become the μ_act targets. Never match on raw `w{n}` equality again (d34a-n1 is hereby promoted from cosmetic to structural).
2. **(M2)** Replace bracket+narrow with a gate that can fail: lo(n) SERIES-EXACT against independently derived partial sums (frozen-k closed forms where applicable; otherwise the projected k-chain — 20 lines, exact), or |mid − target| strictly decreasing PLUS a wrong-value exclusion (the depth run must exclude each target's pre-fix convicted value and the unnormalized Πq). Print the geometric rate (KF1's pinned content, currently claimed and absent).
3. **(M3)** Fix the three PASS labels and the docstring noun: μ_act everywhere μ_act is gated; either gate the pin's own Πq form on idle-inclusive initiated prefixes (exact, no actmass — the pin-faithful gate this review verified is available) or amend §6 to the act-prefix formulation explicitly; the criterion change must live in the pin or the receipt text, not only the LOG.
4. **(M4)** Add the mass gates: Σμ_act = 1 over per-record act-alphabets (lengths 1–2) and over at least one multi-record product partition; implement the pin's full restriction (all-records one-act pushforward = μ_act(D)) at depth ≤ 2; add one multi-record D and one reception-adjacent scope sentence (KF3 named in-receipt) to the sandwich family; gate the μ_act factorization (not only μ's).
5. **(m1, m2)** State collar-locality where the marginals are claimed (it is the actual mechanism, twice consumed silently); replace K4's grep with the structural fact (each rec step appends exactly one event) plus the embedded-chain identity stated as the standard exponentials theorem.
6. **(m3, m4, m6, n1–n3)** Re-word K1/K3 labels to earned scope; the target-family scope sentence; the zctr comment; recount the substantive-gate tally; memory/style lines.
7. **Forward-correct #160 at #161** (the crux sentence, "brackets... exactly... INCLUDING under remote clocks", "restriction on down-sets exact", "(1)(2)(6) at the measure level") — the #157→#158 precedent applies verbatim.

## SUMMARY JUDGMENT

The receipt's construction is right and its verification is not. Moving to exponential clocks genuinely answers d34a-M3 at the level of the object — the uniform 1/k draw is now the memoryless race's own embedded chain, not a smuggled census — and the Harris-lemma content the phase needs is true and provable by a renewal argument this review supplies: each record's draws are iid in its own conditional, so initiated-prefix marginals are exactly Πq (idle-inclusive) or Πq/actmass (act-level), in any world, remote records notwithstanding. But the receipt does not establish this. Its central gates match a label-entangled surrogate event (fresh labels assigned by global birth count — the deferred d34a nit turned structural), whose exact marginals are 1/3, 1/12, and 1/5 where the printed targets are 1/2, 1/8, and 1/2; the committed code fails its own gates at depths 8, 29, and 12; and the crux gate K5 in fact ANTI-verifies its headline — the gated event's marginal shifts 1/3 → 1/5 under the remote pair, hidden at depth 4 by a sandwich wide enough to bracket 1/4, 1/2, and 3/4 simultaneously. The LOG's first-run confession named the disease (pass-by-width) and the committed fix treated the symptom (the target literal) while keeping the pathogen (the width, the label matching, the absent rate gate). One target — T2 — survives at full receipt grade and shows what the repaired receipt looks like: label-free D, chain-checkable lo, limit = target. The repairs are mechanical (a binding map, a series gate, four noun fixes, one mass loop), and on their landing the measure-completing step would likely be earned in substance as well as in name. At 956d7dd it is not: **the measure-completion claim does not survive at its stated width — the phase still awaits its first gate-certified measure-level statement, and #160 owes a forward-correction.**
