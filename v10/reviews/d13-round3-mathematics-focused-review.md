# D13 focused round-3 mathematics review

**Date:** 2026-07-11  
**Verdict:** **PASS**  
**Scope:** post-round-2 hardenings only  
**D13 protocol verdict:** remains `INCOMPLETE-INVESTIGATION`

## Result

All requested hardenings are real and mathematically correct.  The visible
memory witness is now generated by an executed 16-dimensional permutation
unitary rather than a hard-coded history table.  Repeat-read uses a fresh
four-level target register and verifies perfect record/readout correlation.
The Born-once cell now equates the instrument trace directly with the sealed
branch mass.  The old SymPy file carries an explicit superseded banner.  The
21-check standard-library receipt remains byte-identical under normal and
optimized Python.

The additions concerning V4 Paper 39 and D9 are also correctly scoped.  V4
Paper 39 is treated as a substantial finite gauge-sector construction whose
continuum confinement/mass-gap endpoint remains conditional.  D9 is treated
as parameter identification inside a frozen partial-iSWAP preparation family,
followed by refutation of a proposed cross-sector geometry map—not as complete
action selection.

No new mathematical opening was found.

## Frozen hashes and reproduction

Reviewed hashes:

```text
1ea9969cb3e61b805e031ea7b3b025e3a1f88b56e268337824c6a6abcba1cf45  code/d13_finite_kernel_no_go_exact.py
60cbfe88baa4da37ab6a323cc3f73198caf614222a91c1158290f877e576efda  data/d13-finite-kernel-no-go-exact.json
b2370e270086ceec6a53e1cdd9f4a4d861ef00ab8ef642549ca16fdff4086f65  code/d13_local_action_family_exact.py
5eae7a2dcf0345f385853405b4ef604c64d3cf8734f473d3c9dec9c627b3150d  note-d13-maximal-action-theorem.md
575fdbb1c06933ed5da877030739e7ded421e197d3647ac17013cadfaf271f1f  relativistic-isp-v10-paper14-the-action-behind-the-records.md
```

Independent execution:

```text
checks                         = 21/21
normal stdout SHA-256          = 883e68627002aa33de6a8c3946c5d21f5d8771c257e3d0e1c1184110f26859c2
-O stdout SHA-256              = 883e68627002aa33de6a8c3946c5d21f5d8771c257e3d0e1c1184110f26859c2
semantic SHA-256               = 4eb19b0eb34bdc9cd910029cb3d4c22bb47d8d847e0fe12353a7b5eac69f2852
reported source SHA-256        = 1ea9969cb3e61b805e031ea7b3b025e3a1f88b56e268337824c6a6abcba1cf45
generated JSON SHA-256         = 60cbfe88baa4da37ab6a323cc3f73198caf614222a91c1158290f877e576efda
normal/-O stdout comparison    = byte-identical
normal/-O JSON comparison      = byte-identical
```

The semantic hash correctly stays unchanged because the narrowed semantic
claims and predictions are unchanged; the updated source hash binds the
hardened implementation bytes.

## 1. Executed reversible memory circuit

**PASS.**

The circuit acts on four bits ordered `X,M,Y,Z`.  On every computational basis
state it applies

```text
M <- M xor X
Z <- Z xor M
```

where the second line uses the updated memory.  This is the composition of
`CNOT X->M` and `CNOT M->Z`.  The map is a permutation of all 16 basis states,
and the executable verifies

$$
U_{\rm mem}^\dagger U_{\rm mem}=I_{16}.
$$

The input density matrix is the equal classical mixture of `|0000>` and
`|1000>`.  The outputs are respectively

```text
X,M,Y,Z = 0,0,0,0
X,M,Y,Z = 1,1,0,1.
```

Tracing the hidden memory gives

$$
P(0,0,0)=\frac12,
\qquad P(1,0,1)=\frac12,
$$

and hence

$$
P(Z=1\mid Y=0,X=1)=1,
\qquad
P(Z=1\mid Y=0,X=0)=0.
$$

The enlarged four-bit evolution is reversible and state-complete; the visible
`X,Y,Z` process is not first-order Markov.  This closes the round-two request
for an actually executed dilation rather than a stated realization of a
hard-coded table.

## 2. Controlled repeat-read

**PASS at the declared initialized-readout scope.**

After a later licensed system-only interaction, the receipt appends a fresh
four-level readout initialized to zero and evaluates controlled modular
addition from the sealed record.  On that initialized target it maps

$$
|r\rangle_{\rm record}|0\rangle_{\rm fresh}
\longmapsto
|r\rangle_{\rm record}|r\rangle_{\rm fresh}.
$$

The resulting joint distribution satisfies

$$
P(R=r,Q=q)=\delta_{rq}P(R=r)
$$

for every pair `r,q`.  This is a distinct operation on a new register, not a
second assertion that the old marginal equals itself.  It proves exact
repeat-read agreement within the declared future algebra.

The helper evaluates the controlled operation on the fresh `|0>` target
directly rather than constructing the entire 128-by-128 controlled-addition
matrix for arbitrary target inputs.  That is sufficient for the experiment
claimed; no broader arbitrary-target unitary theorem should be inferred.

## 3. Born trace versus sealed branch mass

**PASS.**

For input `|10>`, quarter-iSWAP, and effect `|10><10|`, the instrument trace is

$$
p=\operatorname{Tr}(E U\rho U^\dagger)=\frac12.
$$

The seal isometry copies the computational outcome into the record register.
The probability mass of record branch `r=2` is independently summed from the
sealed amplitudes and is also `1/2`.  The check now asserts

```text
instrument trace = sealed branch mass = 1/2
```

while additionally confirming that a second multiplication would give a
different value.  This is the correct finite Born-once consistency cell and
closes the round-two label weakness.

## 4. Superseded receipt provenance

**PASS.**

The old SymPy script begins with an unambiguous banner:

```text
SUPERSEDED ROUND-1 D13 WITNESS
Replacement: d13_finite_kernel_no_go_exact.py
```

It states that the replacement is dependency-free and carries the narrowed
21-check theorem.  Retaining the old bytes for provenance no longer presents
the old executable as the active D13 receipt.  The active paper and repair note
point to the standard-library artifact.

## 5. V4 Paper 39 scope

**PASS.**

The added ledger language accurately reflects the underlying paper.  V4 Paper
39 explicitly says it is not an unconditional proof of four-dimensional
Yang–Mills existence, confinement, or mass gap.  Its regulated Wilson/heat-
kernel construction is substantial, but the continuum identification,
nontrivial normalization, large-field/error control, fixed-scale bridge,
confinement, and spectral-gap endpoint remain conditional on named IR/bridge
obligations.

D13 therefore correctly treats the Wilson/heat-kernel action, gauge group,
sector, and trajectory data as supplied choices rather than a selector of the
universe's field content or coupling.  It neither erases the finite results nor
promotes the conditional continuum target.

## 6. D9 partial identification scope

**PASS.**

D9 freezes a one-parameter partial-iSWAP source/preparation family.  Within
that family, maximal Bell entanglement fixes `theta=pi/4`.  Bell self-testing
does not uniquely select the preparation circuit, grammar, complete action, or
record ontology.  D13 now states exactly this conditional scope.

D9 then makes the additional cross-sector identification

$$
g_{\rm geometry}=\sin^2\theta=\frac12
$$

and tests it on fresh geometry seeds.  The holdout rejects that one-coupling
map.  D13 correctly classifies the sequence as:

```text
parameter identified within a frozen model
+ proposed matter-to-geometry map empirically refuted
!= complete action empirically identified.
```

This addition is important because “no selector exists” would have understated
the corpus.  The revised wording preserves the partial empirical success
without mistaking it for a full action theorem.

## Regression ledger

```text
16x16 reversible memory unitary                  PASS
visible non-Markov conditionals                  PASS
fresh-register controlled repeat-read            PASS
instrument trace equals sealed branch mass       PASS
old SymPy source visibly superseded               PASS
updated source/stdout/JSON hashes                 PASS
21/21 normal/-O reproducibility                   PASS
V4 Paper 39 conditional scope                     PASS
D9 partial Bell identification scope              PASS
D13 formal INCOMPLETE-INVESTIGATION verdict       UNCHANGED AND CORRECT

new fatal/major/moderate mathematical opening    NONE
```

## Final verdict

**PASS.**  The three round-two receipt weaknesses are closed without expanding
the theorem beyond its evidence.  The exact result remains finite local-unitary
kernel nonuniqueness on a fixed operational interval.  The amplitude bridge is
still a candidate, D13 still lacks a selected complete action, and the formal
protocol grade correctly remains `INCOMPLETE-INVESTIGATION`.
