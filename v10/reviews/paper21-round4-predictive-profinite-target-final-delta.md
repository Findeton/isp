# Paper 21 round 4 — predictive/profinite target final delta

**Frozen target:** commit `aa1a258`, restricted to repaired section 10 and its
status/verdict cross-references in
`relativistic-isp-v10-paper21-local-generators-do-not-imply-local-memory.md`.

**Delta base:** commit `caaba6e`, audited in
`paper21-round3-predictive-profinite-target-hostile-delta.md`.

**Verdict:** **ALL PRIOR MAJORS AND MINORS CLOSED; TWO FRESH MINOR TYPING
REPAIRS — 0 BLOCKER / 0 MAJOR / 2 MINOR / 0 NIT.**

The D34d scientific core remains terminal and unchanged.  The repaired D34e
pin now has the correct passive/controlled law split, conditional-law scope,
measurable carrier factorization, finite audit ceiling, online posterior over
the v9 stem spectrum, and quantum positivity types.  No false profinite bridge,
continuity theorem, projective-limit existence theorem or all-horizon result is
claimed.  Two local phrases still need tightening before this focused stream
can stamp an unqualified clean delta.

I did not rerun the frozen numerical receipts.  Commit `aa1a258` changes only
paper/review prose, and the new target adds no numerical consequence.

## 1. Closure of the caaba6e findings

| Prior finding | Disposition at `aa1a258` |
|---|---|
| M1 — passive `mu` did not define interventions | **CLOSED.** Passive future-event kernels and supplied intervention-indexed causal families are separate branches. |
| M2 — finite prefix was conflated with a v9 spectrum point | **CLOSED.** The paper now uses an adapted posterior measure `nu_tau` on `X_stem`, not a future-leaking point assignment. |
| M3 — finite enumeration overclaimed bounded-horizon sufficiency | **CLOSED.** The exact domain is `D(N,H,Q,I,S)` and both current-size and future-horizon promotions require separate theorems. |
| m1 — null-history/version scope | **CLOSED in substance.** Regular conditional kernels are canonical only `mu`-a.s.; pointwise claims require a separately declared generator/kernel; witnesses must be reachable positive-cylinder or pointwise-kernel witnesses. |
| m2 — setwise quotient lacked measurable factorization | **CLOSED in substance.** `B_A`, `Kbar^I` and the factorization are explicitly measurable on finite/standard-Borel carriers. |
| m3 — “strongly positive restriction maps” | **CLOSED.** Strong positivity is assigned to finite history functionals; restriction consistency, comb positivity/causal traces, and CP causal encoder maps are typed separately. |

## 2. Passive versus controlled predictive laws — pass

Section 10.1 now correctly defines the passive kernel

```text
K_(tau,A)(h,E) = mu(future of A in E | F_tau)(h)
```

on a measure-determining future-event class.  It explicitly says that a
passive `mu` does not determine deliberate interventions.  The controlled
branch instead requires a supplied family `{K^I_(tau,A)}` and declares whether
adaptive instruments or entangled ancillas are licensed.  A future D34b–D34c
process object is listed as a possible supplier, not inferred by notation.

The stopping scope is also part of the quotient.  Fixed construction time,
A-own-ring count and A-wire-event count are separate; passive receptions are
included in wire stopping; global event depth remains an auxiliary
locality-negative control.  This closes the conditional-law and clock-scope
ambiguities without altering D34d.

## 3. Sufficiency and minimality — pass, subject to m1

The passive and controlled predictive equivalences have the correct domains.
The carrier criterion is now the measurable factorization

```text
K^I_(tau,A)(h,.) = Kbar^I_(tau,A)(B_A(h),.)  mu-a.s.
```

with `I` omitted passively.  This is the right probabilistic screening
statement.  The implication directions remain correct: boundary equality
implies predictive equality for sufficiency; the converse on the realized
support removes redundancy and earns an exact minimal realization.  Gauge and
carrier relabeling are declared measurable.

The anti-cheating conditions also remain: record-carried encoders must disclose
ports, ownership, continuous fields, unbounded resources and identifier cost;
fixed graph radius is not bounded width under unbounded degree; predictive
omission is not physical erasure; NSE is separately required only for actual
deletion/compression.

## 4. Finite-domain promotion — pass

The registered audit domain

```text
D(N,H,Q,I,S)
```

now bounds current-past size as well as future horizon/time, queries,
interventions and stopping algebra.  Enumeration earns only that cell's
sufficiency.  Nestedness is required before partitions are called refinements.
The two infinite promotions are correctly separated:

1. arbitrary reachable current size requires an induction/closure theorem;
2. arbitrary future horizon requires compatible limiting kernels or proved
   stabilization.

One reachable exact witness can refute a candidate, while survival through a
finite grid cannot prove it globally.  The frozen outcome table preserves this
as `FINITE-DOMAIN ONLY` and distinguishes candidate-class obstruction from a
theorem excluding an entire declared physical carrier class.

## 5. Online posterior and v9 profinite scope — pass, subject to m2

The repaired domains are correct:

```text
Omega_marked --u--> completed causets --phi--> X_stem --pi_n--> X_n.
```

The paper explicitly refuses a point of `X_stem` as the state of a current
finite past.  Instead it defines

```text
nu_tau(h)     = (phi o u)_* mu(completion in . | F_tau)(h),
nu_(tau,n)(h) = (pi_n)_* nu_tau(h).
```

Thus the online datum is an adapted conditional probability measure and its
finite covtree-resolution marginals.  The section then correctly distinguishes:

1. completed observables factoring through `phi`;
2. online regional prediction factoring through the posterior `nu_tau`;
3. an adapted online boundary possessing its own finite/profinite realization.

It states that the third construction may yield a different inverse limit and
requires commuting finite-level maps, compatibility and adaptedness before it
can be identified with v9 `X_stem`.  It also correctly records that v9's
evaluation map is Borel but generally noncontinuous, that measurable
almost-sure sufficiency is weaker than continuous realization, and that a
continuous real-valued function on a profinite space need not factor exactly
through one finite level.  Rogue fibers are confined to completed-history
lifts rather than treated as an online state.

No pushforward, lift, continuity or predictive-factor theorem is claimed to
exist.  These are now well-typed gates for D34e.

## 6. Quantum typing — pass

The quantum branch compares licensed durable outcome laws `P(r|I,h)` supplied
by a conditional process/decoherence object.  It does not use unlicensed fine-
history diagonals.  Retained joint operator states and eliminated-boundary
process tensors/combs remain alternatives rather than automatic SHARD
constructions.

The paper separates retained Hilbert memory dimension, operational functional-
span dimension and named-cut operator-Schmidt/bond rank, and refuses to replace
them silently by density rank, Choi/Kraus rank or ancilla dimension.  The
consistency ledger now assigns:

- normalization, Hermiticity and strong positivity to each `D_n`;
- exact projective equality to the restriction/incidence operation;
- positivity and causal trace/marginal constraints to comb Choi operators;
- complete positivity and causality to physical encoder/update channels.

This exactly closes prior m3.  The auxiliary `P,E` example remains a negative
control and is not promoted to a constructed or necessary SHARD process-memory
carrier.

## 7. Fresh MINOR findings

### m1 — use one common full-measure set, or choose the pointwise branch

Section 10.2 writes the kernel factorization `mu`-almost surely “for every
licensed `I`” and then says setwise that equal boundary states imply predictive
equivalence.  For one or countably many instruments this can be made true on a
common full-measure set by intersection.  For an uncountable instrument family,
separate `mu`-a.s. equalities need not share one full-measure set: the union of
their exceptional null sets can be non-null or everything.

The same scope seam reappears when section 10.4 asks for screening “on all
reachable histories” although the displayed factorization is only a.s., and
the all-future verdict rows say “all ... reachable pasts.”  The paper has
already named the two legitimate routes; it must keep one route through the
claim.

**Required repair:** state either:

- there is one declared `mu`-full measurable set `H_0` on which factorization
  holds simultaneously for every licensed instrument/query (with a countable
  determining family or joint regularity argument when needed), and all
  setwise equivalence/minimality statements are restricted to `H_0`; or
- the pointwise generator/controlled kernels are declared on every legal
  reachable state and the theorem is pointwise there.

Label the all-future outcome `mu`-A.S. or `POINTWISE` accordingly.  This is a
scope repair, not a reversal of the factorization or a new mathematical
program.

### m2 — compact marked does not imply profinite marked

Section 10.5 ends the continuity paragraph by allowing continuous/unbounded
marks to use “a separately declared compact marked topology.”  Compactness
alone does not preserve the profinite claim: `[0,1]` is compact and connected,
whereas a profinite space must be compact, Hausdorff and totally disconnected
(and the v9 light setting is additionally second countable/countably
presented).  An unbounded real mark with its ordinary topology is not compact
at all.

**Required repair:** say:

> Continuous or unbounded marks require explicit finite observable quotients
> or a separately declared marked topology.  If the resulting carrier is
> called profinite, that topology must itself be realized as an inverse limit
> of finite discrete quotients (equivalently, have the required Stone/light-
> profinite properties); a general compact marked topology yields a different
> compact, not automatically profinite, bridge.

This preserves the correct preceding refusal and prevents “compact” from
becoming a sufficient profiniteness condition.

## 8. Final disposition

Every `caaba6e` major and minor is substantively repaired.  The new section no
longer assumes an interventional law, a future-known spectrum point, finite-
level exact factorization, all-size finite-horizon sufficiency or strongly
positive maps.  No accepted D34d result is reopened.

After the two local scope/topology sentences above are tightened, this stream
can return unqualified `DELTA-CLEAN` without another mathematical or numerical
campaign.

**Final count: 0B / 0M / 2m / 0n.**
