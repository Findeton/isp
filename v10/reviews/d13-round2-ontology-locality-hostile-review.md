# D13 hostile ontology/locality review — round 2

**Date:** 2026-07-11  
**Review verdict:** **PASS AT NARROWED CLAIM SCOPE, WITH TWO NONBLOCKING REPAIRS**  
**Scientific verdict retained:** **`INCOMPLETE-INVESTIGATION`**

## Executive finding

The round-one overclaims are genuinely withdrawn.

Paper 14 and the repaired theorem note no longer claim that SHARD derives a
universal boundary-amplitude category or the action of nature. The
action-to-amplitude bridge is explicitly a candidate. The exact theorem is
limited to nonselection of a finite local unitary kernel on one fixed
operational diamond interval. Record formation, output birth, the licensed
future algebra, boundary state, grammar, physical units, and the amplitude
functor remain declared physical data. Construction gauge and covariance are
reported only at their executed/inherited scope. The non-Markov cell is called
a compatibility witness rather than a Barandes equivalence theorem. V9
geometry remains correctly closed.

Two nonblocking issues remain:

1. the “reversible hidden-memory circuit” cell is represented by its exact
   two-history probability table rather than executed reversible gates; and
2. the corpus adjudication should explicitly discuss D9's Bell-saturation
   identification of `theta=pi/4` inside the assumed partial-iSWAP preparation
   family. That is a genuine partial empirical selector, although it does not
   invalidate D13's structural theorem or select the complete action packet.

Neither issue reopens the proved finite-kernel nonuniqueness result.

## Reproduction

### Dependency-free finite receipt

The replacement standard-library executable reproduces under normal and
optimized Python with byte-identical stdout:

```text
checks=21/21
semantic_sha256=4eb19b0eb34bdc9cd910029cb3d4c22bb47d8d847e0fe12353a7b5eac69f2852
source_sha256=b11f8ffce91d803d991afe294be95e156a79461094edb833c8cf723743d8cb39
stdout_sha256=587f768d8f5b7c59b3858c4275b17273a41c7fbd9a28e80dd6c5e14add369fca
```

The regenerated JSON is byte-identical to
`data/d13-finite-kernel-no-go-exact.json`. The undeclared SymPy dependency is
therefore no longer part of the authoritative theorem path; the old script is
only provenance.

### Antecedent corpus census

The inventory also reproduces without writing the primary artifact:

```text
antecedent Markdown files=524
broad action-relevant files=501
corpus_stream_sha256=51d19c00e979ecfb796aba2c34e810cfbe3bd00586e8703c1ce54c7826877c6e
checks=5/5
```

D13 self-files are excluded, full headings/scope guards are retained, and the
machine counts now agree with the human ledger. The census remains a
high-recall inventory, while interpretation is correctly assigned to the
human candidate and version ledgers.

## Round-one blocker closure

| Round-one opening | Round-two adjudication |
|---|---|
| Undefined `Diam -> Hilb/CP` universal category | **CLOSED by downgrade.** It is now a candidate bridge; no universal category or sewing theorem is claimed. |
| No sealed-diamond runtime | **CLOSED by scope.** The proved theorem is a finite kernel theorem, not a full V6 diamond implementation. A1 remains open. |
| Record isometry imported and permanence absent | **CLOSED at declared finite scope.** The new isometry contains system, orthogonal record register, and live-collar flag; later licensed operations act on system only and preserve the record distribution. The instrument/future algebra remain primitive. |
| Construction gauge only one commutator | **CLOSED by precise scope.** Paper 14 states the exact cell and invokes D12's adjacent-swap theorem only under its explicit disjoint commuting hypotheses. Universal construction quotient remains open. |
| Covariance overclaim | **CLOSED.** The claim is endpoint unitary-frame covariance only; Lorentz/diffeomorphism/network gauge remains open. |
| Non-Markov/Barandes compatibility | **SUBSTANTIALLY CLOSED.** The receipt gives an exact visible-history memory table and the prose preserves the Egri distinction. The unexecuted gate realization is a minor label issue below. |
| “Maximal/universal architecture” theorem | **CLOSED by withdrawal.** The filename is retained for provenance, while the note and Paper 14 deny maximality/universality. |
| Primitive data mislabeled as coefficients | **CLOSED.** Types, grammar, action/process, state, record instrument, reference/sewing data, quotient, and units are listed as primitive physical packet data. |
| Corpus A0 and dependency receipt | **CLOSED at audit scope.** Frozen antecedent boundary, synchronized ledger, independent standard-library receipt, and hashes now reproduce. |
| Gravity/causal-set recovery implication | **CLOSED.** They are live candidate sectors, not recovered gravity or a complete universe action. |
| V9 holdout refusal | **CLOSED.** No full action-to-record packet has been independently selected, so A10–A11 correctly forbid opening geometry. |

## Detailed ontology and locality findings

### Action-to-amplitude bridge

**Honest candidate status.** The repaired theorem says only:

> if a supplied regional quantum action has a well-defined local sewing law,
> it is a natural candidate generator for sealed-diamond amplitudes.

It explicitly names the missing source objects, construction quotient,
associativity/coherence, edge modes, boundary charges, ghosts, anomalies,
corner terms, and theory-specific measure. This is the right relationship to
general-boundary quantum theory: relevant architecture, not a derivation from
record axioms.

### Record formation, output birth, and future algebra

**Accurately scoped.** The exact map is

```text
|j> -> |j>_system |j>_record |live>_collar.
```

It is an isometry; branches are exclusive; probabilities normalize; and the
record distribution is invariant under the explicitly licensed algebra that
acts only on the system. The live collar is a declared finite flag, not a full
typed V6 collar with ownership, screens, links, and opportunities. Paper 14
says exactly that the complete record instrument and physical future algebra
are not derived.

This is sufficient for the finite counterexample. It is not autonomous
environmental decoherence, redundant macroscopic inscription, collapse, or a
universal record ontology, and none is now claimed.

### No global clock, locality, and construction order

**Honest at conditional scope.** A supplied regional amplitude formalism can
avoid a preferred global time. It does not derive which regions are disjoint
or which collars own an interaction. Those relations remain grammar data.

The executable proves one disjoint tensor-factor commutator and one overlap
control. D12 supplies the broader finite linear-extension result only when
incomparable instruments commute. Paper 14 no longer turns those facts into a
universal scheduler-free quantum-gravity theorem.

### Covariance

**Honest.** The receipt transports one input and output endpoint by exact
unitary basis changes and preserves one probability. It is not called Lorentz
or diffeomorphism covariance. Full independent network frames, nonunitary
`SL(2,C)`, gravitational edge data, and amplitude sewing remain open.

### Primitive packet completeness

**Complete as a list of missing physical inputs, not as an implementation.**
Across the protocol, Paper 14, and the candidate-class note, the packet now
includes:

```text
field/record/collar types;
local extension grammar and ownership;
regional kernels/action or equivalent process law;
boundary/initial state;
record/decoherence instrument and future algebra;
construction/frame/gauge quotient and sewing data;
reference, regulator/contour/boundary data where required;
proper-unit and dimensionful scale bridge.
```

No missing item is silently demoted to a numerical coefficient. The formal
scientific verdict remains incomplete precisely because those inputs are not
selected.

## Non-Markov and literature scope

The visible table

```text
P(z=1|y=0,x=1)=1,
P(z=1|y=0,x=0)=0
```

is a valid finite non-Markov path law: the latest visible state `y=0` does not
screen off the earlier record. It demonstrates that a reversible enlarged
system can support non-Markov visible histories. It does not imply that every
regional amplitude is non-Markov or that one-time probability dynamics
selects a unique path measure.

Paper 14 explicitly retains the D12 Egri distinction and denies a general
Barandes-equivalence theorem. This is the correct literature boundary.

### Minor executable wording repair

The code comments and receipt call the table a “reversible hidden-memory
circuit,” but no reversible gate matrices are constructed. `histories` is
directly declared as the two nonzero paths. The table is plainly realizable by
copying `X` into an initialized hidden bit, retaining `X`, setting visible
`Y=0`, and later copying memory to `Z`; nevertheless, the executable proves
the path distribution, not the circuit implementation.

Either implement the reversible permutation/isometry or rename the cell
`EXPLICIT VISIBLE NON-MARKOV HISTORY LAW`. This is nonblocking because the
paper's compatibility conclusion follows from the exact path law alone.

## Could any corpus principle invalidate nonselection?

No inherited **structural** principle currently invalidates the theorem.

- V6 commitment fixes a coefficient only after support, reference, primitive
  ledger, orientation, and functional are supplied; no audited map turns that
  coefficient into `theta`.
- V4's GR branch and Deser/Lovelock results select structure only inside
  declared field/derivative sectors.
- V7's center, Einstein, manifold, and QFT-readiness gates require selected
  carriers, scales, tolerances, and identification data.
- V9 diffusion churn constrains desirable macroscopic behavior but is not an
  amplitude selector.
- D12 proves that compatible history/process architecture still leaves the
  interaction primitive.

### Important partial selector: D9 Bell saturation

D9 must be stated explicitly. Within a **preselected one-partial-iSWAP
preparation family** and its fixed preparation/readout map, maximal Bell
entanglement fixes `theta=pi/4`. Thus adding that empirical operational
constraint rejects the half-iSWAP member for that experiment.

This does not contradict D13:

- D13's theorem concerns selection by the listed shared structural premises,
  not by the extra Bell-saturation datum;
- Bell self-testing does not uniquely identify the underlying preparation
  circuit without the partial-iSWAP family assumption;
- the result selects one kernel parameter, not field content, grammar,
  boundary state, record ontology, UV action, gravity, or physical scales; and
- D9's attempted identification of that angle with the V9 geometric transfer
  coefficient failed its geometry holdout.

Nevertheless, calling A10 simply “no empirical selector” is too broad unless
it means **no independent selector of the complete action packet**. Add D9 to
the corpus/candidate adjudication as a conditional partial action-parameter
selector. It is the clearest existing example of the kind of additional
physical information D13 says is required.

### Remaining live selector programs

The repaired literature audit correctly preserves, rather than dismisses:

- asymptotic safety, conditional on a unique truncation-/scheme-stable fixed
  point and critical trajectory with matter;
- causal-set complex phases, conditional on a complete quantum measure and a
  stable manifoldlike `3+1` phase;
- amplitude bootstrap isolation in a supplied spectrum/dimension/symmetry
  sector;
- a representation-invariant simplicity principle, not presently known; and
- new record-sensitive experiments distinguishing collapse/actual-record
  dynamics from unitary decoherent histories.

None currently selects all components of the primitive packet or undermines
the exact structural counterexample.

## V9 holdout adjudication

**Correctly refused.** Even acknowledging D9's partial angle identification,
there is no selected action-to-record dictionary mapping Standard Model/GR or
a UV action into the V9 web. Feeding a continuum Lorentz cone into the test
would import roundness; retuning diffusion churn would test an ansatz already
used to motivate the search. A10–A11 therefore remain unfired for the complete
action packet.

## Gate adjudication

| Gate | Round-two result |
|---|---|
| A0 corpus recovery | **PASS at frozen high-recall plus human-ledger scope**, with D9 partial-selector note requested. |
| A1 complete typed ontology | **OPEN, honestly.** Primitive packet is listed; full diamond implementation is not claimed. |
| A2 locality/no global clock | **PARTIAL, honestly scoped.** One exact cell plus conditional D12 theorem; general region grammar remains primitive. |
| A3 action gluing | **PARTIAL, honestly scoped.** Exact matrix composition; universal sewing remains candidate only. |
| A4 quantum consistency | **PASS only for 21 named finite cells.** No universal quantum-process theorem claimed. |
| A5 record formation | **PASS for declared finite isometry/future algebra; derivation OPEN.** |
| A6 covariance/gauge | **PASS for endpoint unitary cell; physical Lorentz/network covariance OPEN.** |
| A7 uniqueness | **FINITE KERNEL SELECTION REFUTED; architecture universality OPEN.** |
| A8 fields/couplings | **OPEN and explicitly primitive/empirical.** |
| A9 units/gravity | **OPEN and explicitly primitive.** |
| A10 empirical selection | **No complete-action selector; D9 is a conditional partial angle selector.** |
| A11 spacetime holdouts | **CORRECTLY WITHHELD.** |
| A12 hostile closure | **Round-two narrowed claims pass; full investigation remains open by verdict.** |

## Verdict

**PASS AT NARROWED CLAIM SCOPE, WITH TWO NONBLOCKING REPAIRS.** Paper 14 now
makes the right scientific claim:

```text
FINITE-LOCAL-UNITARY-KERNEL-NONUNIQUENESS-PROVED
GENERAL-BOUNDARY AMPLITUDE BRIDGE = CANDIDATE
RECORD INSTRUMENT/FUTURE ALGEBRA = DECLARED PRIMITIVE
FULL ACTION, STATE, GRAMMAR, UNITS, AND GRAVITY = UNSELECTED
PROTOCOL VERDICT = INCOMPLETE-INVESTIGATION
```

Implement or relabel the non-Markov circuit cell and explicitly adjudicate
D9's conditional Bell selector. Neither repair changes the central theorem.
D13 is now an honest negative result and research map, not a claimed action of
our universe.
