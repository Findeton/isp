# EXTERNAL REVIEW (the user, 2026-07-12) — the D31/D32 plan + the arc record: MAJOR REVISION before pinning

Recorded per house style. Verdict: the corrected D28->D30 result is strong and the graph-blind no-go probably theorem-grade; D31's directed order not yet well-defined; D32 mixed inadmissible kernels, arithmetic inconsistencies, and over-design claims.

RECORD-LEVEL REPAIRS ORDERED (applied same day, LEDGER #136):
1. "non-integer-dimension order" -> "an order having a stable finite-scale effective dimension d_hat ~ 1.76".
2. "provably not a filament / provably not M^2" -> "strongly excluded as a filament or M^2 by the registered finite-sample diagnostics".
3. "arc closed at reviewed strength" -> "round-one repaired; delta verification pending" (no separate delta report existed for the arc round — the debt discharged by the delta pass launched at this round).
4. On the record verbatim: with D29's spatial row vacant, this arc has NOT measured an emergent spatial metric or light cone; it has measured the order geometry of a particular event DAG.

D31A ACCEPTED WITH NARROWING: the theorem is real — in the none-free, unbounded-growth grammar, a stationary path-covariant kernel whose per-labelled-opportunity weights depend only on u cannot assign positive weight to interactions (w_b(u)w_i(u+1) = w_i(u)w_b(u) with birth positivity forces w_i = c; normalization + unbounded u force c = 0; w_b = 1/u). The conclusion is RICHER STATE SENSITIVITY IS NECESSARY — graph sensitivity or degree/age/motif/global invariants — NOT specifically locality. Printed limits: none-ops excluded; bounded-growth escape; birth positivity; per-labelled-opportunity weights + stated counts; a u <= 5 search cannot establish unbounded solutions without an extension theorem.

D31B — THE MAIN CONCEPTUAL PROBLEM: the proposed "directed event poset" mixed structural causality with operational influence; operational influence need not be transitive (cancellation: A->B, B->C with net A->C zero), so no construction can be operational and transitive-by-recursion. REQUIRED ARCHITECTURE: (1) the PORT-DEPENDENCY POSET (explicit input/output ports; edge when an output supplies a later input; transitive closure — structural, acyclic, transitive by construction; also fixes the singular target()/source() ambiguity); (2) the OPERATIONAL-INFLUENCE GRAPH (edges = admissible-intervention effects; cancellations and alphabet-dependence recorded; NOT called a poset). Compare the wire-DAG, the port poset, and the influence graph; FREEZE ONE primary order on structural grounds before D32 (no undeclared pick-the-dimension choice).

D32 REDESIGN ORDERED: 3x3x3 = 27 cells (540 growths), not 12/240 — arithmetic corrected; one canonical seed for the main 3x3 kernel grid (9 cells x 2 sizes x >= 12 seeds); seed topology = a SEED-FORGETTING robustness test at the anchor and extremes, not a dial; 3 lambda values map-and-bracket only (continuity needs a conditional dense scan); d_hat crossing 2 is NOT an M^2 criterion (joint verdict: matched chain constant, 3-chain abundance, interval profile, controls); multiplicity handling across cells; print the REALIZED interaction fraction (lambda is a proposal ratio); the longest-chain-through-common-diamonds estimator must be VALIDATED on known geometries before being called spatial; the S^2 direction family is a MECHANISM TEST, not a derivation of S^2; card v2 must use fixed-decimal serialization + a recorded numerical environment (platform-dependent last-bit floats changed the raw JSON hash across runtimes while all gates passed).

REVISED SEQUENCE (adopted): D31A (narrowed theorem) -> D31B (two objects; primary frozen structurally) -> D31C (graded-feasibility reconnaissance) -> hostile round + separate delta -> D32A (deterministic card v2; validated estimators; the 1.76 anchor) -> D32B (admissible/declared 3x3; joint verdicts; multiplicity) -> D32C (conditional refinement; fresh confirmation seeds; then the S^2 family as a separately pinned hypothesis).

DISPOSITIONS BY THE MAIN LOOP (two nuances, adjudicated in-session): (a) the port-dependency poset for the cRy gate set is HYPOTHESIZED to coincide with the wire-DAG (entangling gates: both outputs depend on both inputs) — to be GATED in D31B, retroactively validating D30's object if confirmed and reclassifying "directed" as the alphabet-relative operational diagnostic; (b) path-covariance is not yet established as a physical admissibility requirement (the flags-observable reading) — D32 carries a covariance-status COLUMN rather than a covariance filter, with D31C's reconnaissance informing the choice.

The d_hat ~ 1.76 discovery survives as an EFFECTIVE-ORDER result until the spatial and operational layers are constructed independently.
