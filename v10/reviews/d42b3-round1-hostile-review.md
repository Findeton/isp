# D42b3 round 1 — hostile review (pin + receipt + out)

**Reviewer round:** 2026-07-18, against HEAD ac8e0a9. Objects: the pin
`v10/note-d42b3-placement-and-the-discrete-ts-condition.md` (#296), the
receipt `v10/code/d42b3_placement_exact.py`, the output
`v10/data/d42b3_placement_exact.out`, read against note-d42a (A1–A8 + A7'),
`reviews/d42a-round1-hostile-review.md` (round 1 + delta), LOG #296–#297,
the d34a-H5 lottery precedent (`code/d34a_harris_lemma_exact.py`, LOG
#157–#159), and v6 papers 1–2 (the TS-integrability residue). Method: 2
reruns (PYTHONHASHSEED 0, 61); a wider-universe independent family rebuild;
an independent poset algorithm (pairwise shared-register transitive closure
vs the receipt's last-writer chaining); my own chain-past/own-view
extraction, my own blind-group counter, my own cut products; a full
semantic side-by-side of the embedded layer against d42a's originals; two
exit-plumbing mutations; and — the commissioned centerpiece — the full
depth-4 TS system computed both ways (diamond census + gradient
completion). Scripts in the session scratchpad (`verify_d42b3.py`,
`followup_d42b3.py`, `semantic_diff.py`, `diff_layer.py`, `mutant1/2.py`).
Nothing in the repo modified except this file.

## VERDICT: 1 BLOCKER / 3 MAJOR / 2 minor / 2 nit (+2 notes)

Zero false numbers. Every printed quantity reproduces exactly under
independent recomputation (family 1,191 on a wider candidate universe;
T1 sums 1 and 5/4 by hand from the pinned law; T2's 1/256, 1/2048, 1/2560
and both N-sequences by hand; L2's 2,382 / 0 / {0: 2134, 1: 248} with my
own counter; L1's 28/0 replicated). The embedded d42a layer is
semantically verbatim (0 mismatches on all 1,191 candidate sets, canons,
mus, posets; 6,470 accepted + 1,200 mixed admissible probes agree). The
BLOCKER is the round's self-declared conceptual centerpiece: the "discrete
TS-condition" is ill-posed as pinned — its gate re-checks a fact already
gated in G-T2, and under every precise disambiguation the depth-4 status
is NOT "OPEN". I computed the system: **in the ratio-preserving class the
discrete TS-condition FAILS at depth 4 with 36 exact certificates; in the
gradient class it is solvable at every finite depth with an explicit
solution.** The fixture decides the question the pin left open.

---

## THE TS-SYSTEM RESULT (the commissioned computation, both ways)

Setup. The cut complex of the depth-4 family: 427 canonical classes
(1 + 6 + 23 + 84 + 313 by depth), 290 classes with >= 2 linearizations.
Verified first: the candidate (event, weight) set is invariant across all
764 cross-linearization points, hence N(c) = Sigma q is a well-defined
function of the cut (downset) — 0 exceptions over all 427 classes. Every
pair of gauge-equivalent chains decomposes into adjacent transpositions,
so the complete two-path constraint set is generated by DIAMONDS
(c; c+e; c+f; c+e+f with both orders admissible and equal top canon).
Sweep: 202 diamonds with top depth <= 4; mu-factor-product violations 0
(G1-type invariance reconfirmed).

**Class (a): no normalization required.** Any cut-attached Z with
chain-invariant products just needs Z constant on diamond-connected
components: 325 components — a 325-parameter solution family, Z == 1
included. Existence is TRIVIAL. ("Per-pair solvability" is likewise
universal and trivial: by factor invariance the mu-ratio across any
gauge-equivalent pair is 1, always — the receipt's G-T3 check
`ratio_needed == 1` is mathematically identical to `mu1 == mu2`, which
G-T2 already gates.)

**Class (b): ratio-preserving placement (the reading on which
"normalizer" means anything and the within-cut mu-ratios — L1's declared
invariant content — are preserved).** A per-cut uniform divisor with
Sigma_e q/Z(c) = 1 forces Z(c) = N(c), uniquely — Z == N is not "the
naive choice", it is the ONLY member of this class. Chain-invariance then
requires N equal across every diamond's two arms. Result: **INCONSISTENT
— 36 of 202 diamonds violate it** (all of the form N = 2 vs 5/2). The
violations organize into exactly 2 bad diamond-connected components: one
containing ALL 23 depth-2 classes (N takes both values 2 and 5/2 on a
single component — invariance forces one Z-value, normalization demands
two), one containing 76 of the 84 depth-3 classes. Witness diamonds
include the receipt's own T2 pair (base [pA0]; arms selfA vs pB1) AND
idle-armed ones (base [pA0]; arms ('n','A') vs pB1: N = 2 vs 5/2) — the
obstruction is not confined to the blind-self-arb arm; it fires on every
arm-pair where exactly one arm adds a conflicting proposal. **The
discrete TS-condition in the ratio-preserving class FAILS at depth 4.
This is a decided, receipt-grade THEOREM, not an open question — and
T2's witness was already one of its 36 certificates.**

**Class (c): gradient (h-transform) placement.** P(e|c) =
q(e|c) * Z(c)/Z(c+e). Chain products telescope to mu(H) * Z(0)/Z(H) —
extension-invariance is AUTOMATIC for any Z; the entire content moves to
per-cut normalization, the discrete harmonic equation Z(c) =
1 / Sigma_e q(e|c)/Z(c+e). On the finite truncation this solves by
backward recursion from ANY positive boundary values on the 313 depth-4
classes: with boundary Z == 1, Z(empty) = 64/1037, positivity everywhere,
per-cut normalization exact at all 215 interior histories, and the T2
witness pair's completed weights EQUALIZE at 1/2074 = 1/2074 (foliation
invariance restored). Under a second boundary the completed weight is
1/3802 — also invariant: the finite-depth solution set is a
313-parameter family. Price: within-cut ratios are deformed at 21 of the
114 interior cut-classes (boundary == 1), INCLUDING THE ROOT (proposals
get Z(succ) = 16/133, idles 32/257 at the empty cut), and no boundary
choice avoids all deformation (a globally ratio-preserving gradient
completion would collapse to class (b) — refuted). **Finite-depth
classical placement in the gradient class is UNOBSTRUCTED; the honest
open residues are (i) the infinite-volume limit (existence of a positive
solution consistent across all depths — a positive-harmonic/Martin-
boundary question) and (ii) whether ratio deformation is acceptable
given L1's canonization of mu-ratios as the invariant content.**

Undecided intermediate (posed, feasible, not computed this round): the
actor-factored class — preserve within-actor ratios, complete only the
actor choice by a cut-attached rule pi(a|c). Its constraint system is
small (114 interior classes) but nonlinear (sum-to-1 in pi); an exact
elimination at depth 4 is a natural round-2 target.

---

## F1 — BLOCKER (CONFIRMED): T3 is a declaration wearing a gate; the
## pinned "discrete TS-condition" is ill-posed and its "OPEN" status is
## wrong under every precise reading

Four convictions, each verified:

1. **The gate is a tautology.** `ratio_needed = (q1 q2 q3)/(q1' q2' q3')
   == 1` is exactly `mu_of(E1) == mu_of(E2)`, gated two checks earlier
   inside G-T2 (`mu1 == mu2 == F(1,256)`). G-T3 computes nothing new.
   And since factor invariance (d42a G1) makes the mu-ratio 1 for EVERY
   gauge-equivalent pair, "per-pair the constraint fixes only a
   normalizer ratio (= 1)" is a universal triviality, not a finding
   about the witness pair.
2. **The comment's equation is incoherent about where the unknowns
   live.** The prose names the two MIDDLE cuts ({pA0,selfA} vs
   {pA0,pB1}) as the unknowns — the coherent reading (they are distinct
   downsets; one equation, two unknowns, ratio forced to 1). The
   equation line instead gauge-fixes those very middle cuts
   ("fix Z0 = Za = Zc = 1") and puts the unknowns Zb1/Zb2 at the two
   paths' FINAL position — but the final cut is the SAME downset for
   both linearizations, so cut-attachment forces Zb1 = Zb2 and the
   "solve" collapses to the identity mu1 = mu2.
3. **The pinned definition never states the normalization constraint,
   and every disambiguation closes the question** (see THE TS-SYSTEM
   RESULT): no constraint => trivial-YES (Z == 1; 325-parameter family);
   ratio-preserving normalization => Z = N forced, decided-NO at depth 4
   (36 certificates — the receipt's own T2 numbers are one of them);
   gradient normalization => finite-depth-YES by backward recursion
   (explicit solution), with the open part relocated to infinity and to
   the ratio-deformation concession. "Status: ... global existence OPEN"
   is unsupportable in each reading; the coboundary formulation is only
   the linearization of reading (b) — and in (b) the answer is NO.
4. **The v6 identification is attached to the ill-posed object.** v6
   paper 1's TS-integrability residue exists as named (paper 1 §6/§10;
   paper 2 Part II carries it) and the structural analogy
   (foliation-change invariance of a completed transfer) is apt — but no
   computation bridges them, and the sentence sits inside a PASS gate.
   Under repair it attaches naturally to the (b)/(c) split: the
   ratio-preserving obstruction is the discrete integrability failure;
   the gradient Z is the discrete counterterm/re-phasing freedom.

**Classification:** the campaign's self-declared centerpiece (#296 "THE
DISCRETE TS-CONDITION identified as front 9's object") is not what the
round claims it is: not defined (missing normalization clause), not open
(decided both ways once defined), not gated (tautology). Zero false
numbers — the conviction is claims-vs-computation at the round's
foundation, the d42a-F1 severity class. Repairs fully pre-verified
(R1 below); both replacement results are theorem-class and computed.

## F2 — MAJOR (CONFIRMED): the .out's printed diagnosis "the naive N
## fails because N is NOT cut-attached data" is FALSE — N is exactly
## cut-attached; what it fails is chain-consistency

Verified: N is constant on every one of the 427 canonical classes (0
exceptions over 764 cross-linearization comparisons);
N([pA0,pB1]) = N([pB1,pA0]) = 5/2. N is therefore a well-defined
function of the cut — precisely the kind of object the pin's own T3
demands Z to be. The naive completion fails NOT because N lacks
cut-attachment but because the cut-attached function N is not a discrete
gradient: its chain-products depend on which intermediate cuts the
foliation visits (the 2 bad diamond components). The false clause
undersells the receipt's own theorem (the failure of an honestly
cut-attached normalizer is the STRONGER and correct statement — it is
what makes T2 genuinely the d34a-H5 pattern rather than a bookkeeping
artifact) and misdirects T3's framing. The second half of the printed
sentence ("double-counts the blind layer") is correct. One-sentence
repair (R2). T2's theorem itself SURVIVES and is STRENGTHENED: same
canonical DAG (my independent canon agrees), mu = 1/256 both orders
(hand: 1/8 * 1/4 * 1/8), cut products 1/2048 vs 1/2560 (hand: (1/256)/8
vs (1/256)/10), N-sequences (2,2,2) vs (2,2,5/2) (hand-derived from the
sector arithmetic), and the conviction generalizes to 36 diamonds.

## F3 — MAJOR (CONFIRMED): T1's claim as worded admits a counterexample;
## the witness proves the positive-reweighting class only

Pin T1: "No initiator-view-local re-weighting (counter-term) can restore
per-initiator normalization ... no function of that view can cancel it."
Three quantifier classes:

- **Uniform Z(own view):** the witness is airtight (equal own-view
  canons — my independent chain-past extraction concurs: both reduce to
  [pA0] — with sums 1 vs 5/4; one Z cannot divide both to 1).
- **Per-candidate strictly positive g(own view, e):** ALSO impossible,
  by an argument the receipt does not print but its data contains:
  machine-verified, A's candidate set at [pA0] is a SUBSET of A's set at
  [pA0,pB1] with identical weights on the shared part and extra strictly
  positive mass (the two pair-arb candidates, 1/8 each); equal own-view
  makes g agree on the shared part, so the two re-weighted sums differ
  by g(pair)*1/4 > 0 — they cannot both be 1.
- **Per-candidate with zeros allowed: FALSE.** The own-view component
  filter — weight 0 on every arb candidate whose ckey is not a component
  of the initiator's own view — is initiator-view-local, gauge-invariant
  (own view and candidate are DAG data, unlike the round-1 full-record
  filter), and restores per-initiator sums == 1 at every record point:
  the receipt's OWN G-L2 proves it (excess = k blind groups times
  exactly 1/4 each; deleting them leaves 1). It is the d42a round-1
  batch-close filter's own-view cousin, and its price is physical
  death: every join arbitration gets weight 0 (an initiator's pre-event
  own view never contains the pair), so conflicts never resolve.

The theorem is TRUE and important in the positive/support-preserving
class; the pinned wording quantifies over "re-weighting" simpliciter and
is refuted by an object the receipt's own L2 constructs. Repair R3:
state the positivity (support-preservation) hypothesis, add the
one-line nesting argument (pre-verified above), and NAME the degenerate
filter as the excluded completion — it is a good exhibit, not an
embarrassment: it shows the excess can only be cancelled by killing the
arbitration physics.

## F4 — MAJOR (CONFIRMED): the pre-registered dichotomy is too strong —
## cut-attached normalizers do not exhaust classical placement

Pin §2: "If the discrete TS-condition later FAILS globally, the
classical placement problem is UNSOLVABLE and the lift is the only
completion." Refuted as worded by this round's own computation: the
gradient completion of class (c) is classical, exact, per-cut
normalized, and foliation-invariant at every finite depth (explicitly
exhibited: Z(empty) = 64/1037, witness weights 1/2074 = 1/2074). Also
outside the cut-attached-uniform class: history-attached slice
normalizations (the d34a "flat pushforward" object), and clock
completions — d34b's exponential-clock construction, THE PROGRAM'S OWN
prior completion attempt, is not cut-attached; the pre-registration as
worded would rule the program's own scheme out of "classical placement"
by definition. The valid pre-registrable theorem (now decided, not
future): RATIO-PRESERVING classical placement is impossible from depth 2
on; any classical completion must either deform within-cut ratios
(gradient class — with the infinite-volume existence question open) or
attach data beyond cuts; the lift is the only completion that keeps the
weight system's ratios AND normalizes. Repair R4 re-words the
dichotomy with the ratio-preservation hypothesis named.

## F5 — minor (CONFIRMED): L1's sweep is a 28-case tautological corner;
## the pin's "swept deterministically over the family" is wider than the
## loop

Replicated exactly: tested = 28, violations = 0. Anatomy: h2 is
HARDCODED to two depth-1 comparators ([pA0], [pA1]); h ranges over the 7
A-only depth-2 histories; e over the 2 B-proposals on v0 — 7 x 2 x 2 =
28. Every tested pair has q1x == q2x (28/28), and all tested factors
equal the constant 1/8, so the ratio gate is equivalent to 1/8 == 1/8
and cannot fail short of layer breakage. No 'r' or 'n' extensions are
ever tested (the loop skips e[0] != 'p'); no negative control exists in
the domain — and cannot: the family-wide candidate q-spectrum is
degenerate by branch (p: 1/8 at all 2,420; r: 1/4 at 1,172 and 1/8 at
496; n: 3/4 at all 2,382), so at these depths ratio locality is
empirically indistinguishable from factor constancy. One dead filter:
`if tuple(h2) == key: continue` compares a 1-tuple to a 2-tuple — always
False. The law itself is TRUE at all depths as a one-paragraph COROLLARY
of factor past-locality (admissible() computes every q from the appended
event's own causal past, a function of its own-view canon; equal
own-view => equal factor => the mu-ratio identity by multiplication —
the d34a collar-locality lemma at the ratio level). Repair R5: upgrade
L1 to its theorem form with the census demoted to tripwire (the exact
A8/F7 precedent from the d42a round), and scope or widen the sweep.

## F6 — minor (CONFIRMED): L2's "exactly 1/4" and "component-additive"
## are stated wider than this receipt's domain

(i) A blind group's mass is (1/4) / (#components in the JOIN view): it
equals 1/4 exactly because the join-view denominator is 1 at these
depths — the d42a delta SAID so explicitly ("the join-view denominator
is always 1 at these depths"); the pin drops the scope and states
quarter-quantization as the cocycle's local form simpliciter. At deeper
records (or under d42b1 transport) a multi-component join view would
make a blind group carry 1/8 — the density's quantum is
depth-conditional, not yet a law. (ii) "Component-ADDITIVE" needs k >= 2;
this receipt's family has max k = 1 (spectrum {0: 2134, 1: 248} —
reconfirmed with my own poset, own components, own blind counter: 2,382
points, 0 ladder violations, 0 bad groups). Additivity's warrant is the
CITED d42a delta (ARM-2, k up to 3, every per-ckey group 1/4) — real,
referee-verified there, but not exercised by ONE point of this gate's
sweep. The blind-group construction itself is sound: the idle probe's
past excludes the probe, so own components and candidates are evaluated
at the same record point (verified — no off-by-one). Repair R6: scope
the pin's L2 sentence ("at depths where the join view is
single-component; d42a-delta-verified through k = 3") or import an
ARM-2 arm.

## F7 — nit: the embedded layer silently fixes the kernel to K1

d42a computes both supplied kernels; d42b3 drops PK2 and the `law`
parameter without a banner note. Provably immaterial HERE: PK1 == PK2 on
every one of the 1,048 components arising in this family (max component
size 2; the kernels first diverge on 3-chains, ARM-2 territory) — but
the pin's base declaration ("the d42a TERMINAL grammar") covers a
two-kernel object and the receipt should say one word about why K1-only
loses nothing.

## F8 — nit: the cited "32,884 actor-points" does not equal the sum of
## the anchored spectra

LOG #297 (and the pin's L2 warrant) cite the d42a delta's "ladder law
verified ... across 32,884 actor-points". The delta's own anchored
spectra sum to 12,942 (ARM-1: 11,926 + 1,016) + 19,767 (ARM-2: 16,539 +
1,824 + 936 + 468) = 32,709; families 6,471 x 2 + 6,589 x 3 = 32,709
also. Delta = 175, unexplained. Not this round's own number, but this
round propagates it; the d42a delta owner should reconcile the sweep
domain (one line).

**Notes (no conviction):** (i) Front-5 scope: the campaign order (#290)
pinned d42b3 as "placement/measure on the full grammar"; this round runs
the d42a base only, with the d42b1 extension "carried, not gated" —
declared in the pin header, and d42b1's P5 (deliver/merge sectors never
blind) is GREEN at #291, but that round is itself in flight: the carry
is provisional and should be re-gated when d42b1 settles. (ii) The L2
loop can increment `viol_l2` twice per actor-point (ladder + bad group)
— counting semantics only, moot at 0 violations.

---

## GATE-QUALITY AUDIT (the assigned attack points)

- **(a) T1 witness sufficiency / own-view canon adequacy:** the canon is
  ADEQUATE — holdings, superseded set, live components, and the idle's
  sector data are all functions of the chain-past typed DAG, which
  own_view_canon captures whole (my independent chain-past extraction
  agrees at the witness; the probe idle is correctly excluded from its
  own past). The witness is sufficient for the uniform class and — with
  the unprinted nesting argument — the positive per-candidate class; the
  claim as WORDED is wider than both (F3).
- **(b) T2 formalization:** N-as-prefix-function is in fact
  N-as-downset-function (verified, 0/427) — the honest formalization,
  and the STRONGEST form of the conviction: factors gauge-invariant,
  normalizer cut-attached, and the product still foliation-dependent
  because the visited-cut chain differs. The two middle prefixes are
  different downsets, so their N's differing is indeed unremarkable —
  the theorem correctly hangs on the PRODUCTS over the same final DAG
  (c1 == c2 gated; my independent canon concurs), exactly d34a-H5's
  same-history-two-linearizations pattern (there the per-step factors
  were state-built census weights; here the factors are clean and the
  census sits in the denominator — the same disease relocated, which is
  the receipt's own point). Genuine reconviction; only its printed
  diagnosis is wrong (F2).
- **(c) T3 content:** convicted (F1); the commissioned computation is
  delivered above, both ways, with certificates and explicit solutions.
- **(d) L1 selection/tautology:** convicted (F5); domain fully mapped
  (7 x 2 x 2), tautology quantified (28/28 equal q's; branch-constant
  spectrum family-wide).
- **(e) L2 off-by-one and additivity:** no off-by-one (verified same
  record point); additivity untested in-family (F6ii); quantization
  depth-scoped (F6i); numbers independently reproduced.
- **(f) pre-registration converse:** convicted (F4); explicit
  counterexample completion exhibited.

## PLUMBING, SEEDS, DETERMINISM, LOG

- **Reruns:** PYTHONHASHSEED 0 and 61 both byte-identical to the
  committed .out, exit 0, 0.38 s (LOG's "0.5 s", "0/61" consistent).
- **Exit-1 by design:** two mutations on scratch copies: G-T2
  expectation 2560 -> 2561 => 4 PASS / 1 FAIL, exit 1; G-L2 ladder
  k/4 -> k/5 => 4 PASS / 1 FAIL, exit 1.
- **Determinism:** audited — all prints are sorted or aggregate;
  candidate lists repr-sorted at the source; the inherited
  next(iter(frozenset)) base extraction is value-deterministic by the A8
  single-base theorem (d42a F9, on record).
- **Layer faithfulness:** semantic zero-mismatch against d42a's
  originals over the whole family (candidates, canon, mu, poset;
  admissible on 6,470 accepted + 1,200 mixed probes). Structural deltas
  are benign: `law` parameter dropped (F7), PK2/linear_extensions/
  gate-local helpers absent (unused here), regs_of branches merged,
  cosmetic reorderings.
- **LOG #296/#297 vs artifacts:** narratives match the pin/receipt/.out
  exactly; the defects they inherit are the artifacts' own (F1-F4), plus
  the propagated 32,884 (F8).

## WHAT SURVIVES (verified)

- **T2, strengthened:** the cut-normalized transfer is
  foliation-dependent on one canonical DAG — every number hand-derived
  and machine-reproduced; N downset-local (making the conviction clean);
  36 diamond certificates generalize the witness; genuinely the d34a-H5
  conviction on the generated grammar.
- **T1, in the honest class:** no uniform own-view normalizer, and no
  strictly positive own-view-local per-candidate re-weighting, can
  restore per-initiator normalization (witness + nesting,
  machine-verified).
- **L2's numbers as exact facts of this family:** 2,382 actor-points,
  excess = k/4 with every blind group exactly 1/4, k-spectrum
  {0: 2134, 1: 248} — independently reproduced (own poset, own
  components, own blind counter).
- **L1 as a law:** true, with a one-paragraph proof from factor
  past-locality (supplied in F5); the 28/0 census stands as a tripwire.
- Family = 1,191 (wider-universe rebuild, 0 candidate mismatches);
  candidate sets invariant across all 764 cross-linearization points;
  K1 == K2 on all 1,048 components (F7's immateriality); seeds,
  determinism, exit plumbing; the v6 residue exists as named (the
  identification remains a pinned declaration, not a computed bridge).
- **New, this round (referee-computed, offered to the program):** the
  depth-4 TS classification — ratio-preserving placement REFUTED (36
  certificates, 2 bad diamond components spanning all 23 depth-2
  classes and 76/84 depth-3 classes); gradient placement SOLVED at
  finite depth (Z(empty) = 64/1037 at unit boundary; 313-parameter
  solution family; 21/114 cuts ratio-deforming, root included; witness
  weights equalize at 1/2074).

## PRESCRIBED REPAIRS (pre-verified where stated)

- **R1 (F1, the blocker).** Re-pin T3 with the normalization equation
  stated, split by class, and replace the tautology gate with the two
  computed gates: (i) the diamond census — enumerate all 202 top-depth<=4
  diamonds, gate mu-factor-product invariance (0 violations) and the
  N-mismatch count == 36 with the 2-component structure => print
  "ratio-preserving discrete TS FAILS at depth 4" as a THEOREM; (ii) the
  gradient completion — backward recursion from unit boundary, gate
  positivity, per-cut normalization at all 215 interior histories,
  witness-pair equality (1/2074), and the ratio-deformation census
  (21/114 incl. the root) => print finite-depth solvability with the
  infinite-volume existence + ratio-deformation residues as the honest
  OPEN. Both computations run in ~2 s exact (pre-verified end to end;
  numbers above are anchors). Move the v6 identification to pin-
  declaration status attached to the repaired statement. Optionally pose
  the actor-factored intermediate class as round-2's decidable target.
- **R2 (F2).** Replace the false clause with: "N IS cut-attached
  (downset-local; gated) but is not a discrete gradient — its
  chain-products are foliation-dependent; it double-counts the blind
  layer along foliations that expose it." Optionally gate N's
  class-constancy (0/427, pre-verified).
- **R3 (F3).** Add the positivity/support-preservation hypothesis to T1;
  print the nesting argument (subset candidates + equal shared weights +
  positive extra mass, pre-verified); declare the own-view component
  filter as the excluded zero-class completion and note it kills all
  join arbitration (its normalization success is L2's k * 1/4).
- **R4 (F4).** Re-word the §2 pre-registration: "ratio-preserving
  classical placement is unsolvable (now decided at depth 4); classical
  completions must deform within-cut ratios or attach non-cut data;
  the lift is the only ratio-preserving normalized completion."
- **R5 (F5).** Record the L1 theorem (proof in F5); demote the census to
  tripwire; either widen the sweep to all types/pairs or print the
  branch-constant q-spectrum disclosure ("at depth <= 4 all same-branch
  factors are constant; the ratio law's empirical content begins where
  factors vary").
- **R6 (F6).** Scope L2's quantization to single-component join views
  (cite the d42a delta's parenthetical); label additivity as
  d42a-delta-carried (k <= 3 there, k <= 1 here) or import an ARM-2 arm.
- **R7 (F7).** One banner line: "kernel fixed to K1; PK1 == PK2 on every
  ARM-1 component (max size 2) — immaterial here (pre-verified)."
- **R8 (F8).** One-line reconciliation of 32,884 vs 32,709 in the d42a
  delta's ledger entry (owner: d42a).

## Reproduction inventory

- Reruns: `PYTHONHASHSEED={0,61} python3 v10/code/d42b3_placement_exact.py`
  -> byte-identical to `v10/data/d42b3_placement_exact.out`, exit 0.
- `verify_d42b3.py`: family rebuild (wider universe, 1,191, 0
  mismatches); independent poset (0/1,191); T1/T2 hand-law numbers +
  nesting; L1 replication (28, 28/28 equal q's) + q-spectrum; L2
  independent counter (2,382/0/{0:2134,1:248}); N class-constancy
  (0/427); diamond sweep (202/36); h-transform (Z(empty) = 64/1037,
  1/2074 = 1/2074); K1 == K2 (0/1,048).
- `followup_d42b3.py`: strict depth-4 components (2 bad: 23-at-depth-2,
  76-at-depth-3, N in {2, 5/2}); ratio-deformation census (21/114, root
  exhibit 16/133 vs 32/257); boundary freedom (313); second-boundary
  invariance (1/3802).
- `semantic_diff.py`: layer faithfulness (0 mismatches; 6,470 + 1,200
  probes). `diff_layer.py`: AST inventory (shared/dropped defs).
- `mutant1.py`/`mutant2.py`: exit-1 plumbing (both 4/1, exit 1).

---

# DELTA VERIFICATION (2026-07-18, against HEAD 229c34f: pin amendments
# D1-D6 at 5ac26b0/#305, repaired receipt GREEN 8/8 at 229c34f/#306)

**VERDICT: NOT CLEAN — 1 MAJOR / 1 minor / 4 nit (+3 notes). Every
number lands on the referee anchors** (all six commissioned points
verified below; reruns PYTHONHASHSEED 0 and 61 byte-identical to the
committed .out, exit 0). The MAJOR is a one-sentence defect with a
one-line repair: the receipt's final [VERDICT] line is STALE ROUND-1
TEXT that contradicts the repaired receipt's own banner and gates. On
that repair (plus, recommended in the same touch, the minor's
three-line de-vacuation) this delta is CLEAN and d42b3 is terminal-fit:
the decided-TS structure itself is safe — every constituent number was
independently reconfirmed this round.

## The six commissioned points

**(1) R1 — CONFIRMED on both gates; conventions verified honest.**
D1(i): the receipt's dedupe key `(canon(h), frozenset({e1, e2}))` IS my
counting convention — my round-1 key was
`(base-class, frozenset({repr(e), repr(f)}))`, identical semantics
(distinct events have distinct reprs here); its both-orders-IN-FAMILY
membership test is equivalent to my both-orders-admissible test because
the family is the admission relation's BFS closure at this depth.
202 diamonds / 36 violations land exactly. D1(ii): the receipt's
forward-mass recursion Z'(h) = Sigma q Z'(h+e), boundary 1, is the
EXACT reciprocal of my round-1 normalizer: verified Z'(h) * Z_mine(class(h))
== 1 at ALL 1,191 sequences (0 violations), so Z'(empty) = 1037/64 =
1/(64/1037) and the printed convention note ("every ratio-level
quantity identical") is TRUE — the completed kernels coincide
identically. Anchors land: interior histories 215, witness weights
1/2074 = 1/2074, interior classes 114, deformed 21 with the root
included. The two root exhibits are THE SAME COMPLETION displayed as
different quantities, reconciled exactly: my successor-normalizer
values (16/133 proposals, 32/257 idles; referee convention) map to the
receipt's extreme normalized weights via P = q * Z(c)/Z(c+e):
(1/8)(64/1037)/(16/133) = 133/2074 and (3/4)(64/1037)/(32/257) =
771/2074 — verified algebraically and numerically (root weights sum to
1: 4 x 133/2074 + 2 x 771/2074 = 1). No flag beyond the labeling nit
(D-n4 below).

**(2) R2 — CONFIRMED.** D2 gates N's class-constancy, 0 violations over
the 427 classes (anchor met; matches my round-1 Part D exactly), placed
BEFORE D1(i) so the diamond census's per-sequence N reads are licensed.

**(3) R3 — CONFIRMED.** The nesting gate checks exactly my
machine-verified argument: A's candidate set at [pA0] nests in the set
at [pA0,pB1], shared weights equal, extra mass exactly 1/4 over 2 blind
events (re-verified: both extras are 'r' candidates at 1/8). D3
declares the zero-class exclusion with the right reason (it abolishes
joint arbitration). The positivity hypothesis is now stated in the pin.

**(4) R5/R6/R7 — substantively landed, with two wording nits.** L1:
census demoted to tripwire, theorem status declared, tested-factor
disclosure printed (28/0; "every tested factor = 1/8" — exact). But see
D-n2: the label's compression "all same-branch factors are constant
1/8" is false as worded for the n-branch (3/4) and r-branch (1/4, 1/8),
D6 slightly misstates which fact my proof establishes, and the full
q-spectrum the pin promises is not printed. L2: scoped exactly per my
R6 (single-component join views; additivity d42a-delta-carried,
untested in this 2-actor family) — clean. K1 banner: present with the
max-component assert — but see D-n3 ("gated" overclaims; assert is not
a check and PK1 == PK2 itself is never computed in-receipt; it is true,
my round-1 0/1,048).

**(5) D4 and D1's v6 attachment — supported, one epistemic qualifier
recommended.** D4's first two clauses are precisely the decided
theorems (ratio-preserving UNSOLVABLE; classical completions exist but
deform ratios or attach non-cut data). The third clause ("the LIFT is
the only ratio-preserving normalized completion") matches my own R4
prescription verbatim — so no conviction — but on delta reflection it
should carry its epistemic status explicitly: what is DECIDED is the
negative half (no classical cut-attached sequential completion is
ratio-preserving + normalized); that the lift IS one, and is the ONLY
one across all non-cut-attached schemes, is the program's standing
expectation with the burden now assigned to d42b4. Recommended wording:
"the lift is the only CANDIDATE ratio-preserving normalized completion
left standing — d42b4's burden" (D-note-A). D1's v6 identification now
attaches to the decided two-class statement as a pin declaration
outside any gate — exactly my R1 prescription; correctly a declaration
(no computed bridge), correctly placed.

**(6) D5 (h12) — graded WELL-POSED as a pin obligation; provenance
verified; one attribution correction for the record.** Source
verification: d42b1's receipt reruns byte-identical, exit 0, 21/21;
the N1 exhibit gate prints h12 sums ['1', '23/24', '1'] with exactly
D5's semantics (a DEAD component inflates the live singleton's
view-relative arb denominator; the admission-based arb-D fix is
unavailable — it flattens the anchored 5/4 spectra; the general-depth
ladder is FALSE under current pricing). As a completion constraint for
the round-2 actor-factored target the obligation is well-formed and
correctly owned: note that h12's defect (sum 23/24, excess -1/24) is
BELOW 1 and NOT quarter-quantized — so on the extended grammar the
excess density is not the d42a ladder at all, which (i) independently
confirms my F6 scoping of L2, (ii) is automatically absorbed by the
gradient recursion (it normalizes ANY positive q-field — no ladder
assumption anywhere in it), and (iii) puts the real load on the
ratio-preserving/actor-factored analyses, which consume the actual
N-field. Round-2 execution is feasible: rerun the diamond census and
the gradient recursion on the 7,393-history transport family with
h12's cut in-domain. ATTRIBUTION, for the record: h12 is the
d42b1-delta referee's witness (N1, LOG #303/#304) — the pin and LOG
attribute it correctly; the hand-off message's phrase "your
h12-from-d42b1" conflates the two referee streams. No repo
defect; recorded so the provenance chain stays exact (D-note-C).

## Findings

**D-M1 — MAJOR (CONFIRMED): the .out's final [VERDICT] line is stale
round-1 text asserting the superseded status.** It reads "... the
discrete TS-condition defined with per-pair solvability and the global
cocycle question OPEN, identified with v6 paper 1's residue (T3) ..."
— contradicting, within the same file, the banner ("ROUND-1 REPAIRED
REVISION: T3 DECIDED (D1)"), gate D1(i) ("the ratio-preserving discrete
TS-condition FAILS"), gate D1(ii), and pin D1 ("supersedes §1-T3's
'OPEN'"). The verdict line is the receipt's highest-visibility
sentence — the one LOG entries and future readers quote — and it
currently reasserts the exact claim this round's repairs retired. This
is the false-printed-sentence class (my round-1 F2; the program's
zero-false-sentences standard), and it is precisely the claim-vs-artifact
class the fresh #304 lesson names. Repair R-D1 (one line), replacement
text supplied below.

**D-m1 — minor (CONFIRMED): D1(ii)'s ok_norm conjunct is vacuous, and
the load-bearing invariant it gestures at (Z's class-constancy = rep-
independence = foliation invariance beyond the witness) is ungated.**
The receipt defines Z[h] for interior h AS the sum
`Sigma q * Z[h+e]`, then ok_norm re-evaluates the same sum over the
same dict and compares — it passes by construction for ANY recursion
output. Probe: rebuilding the recursion from an arbitrary boundary
(7 everywhere) still passes the identical check. (That probe's witness
weight is also 1/2074 — a uniform boundary rescale is gauge, by
linearity of the forward recursion; my round-1 NON-uniform boundary
gave 1/3802 — so the conjunct distinguishes nothing either way.) What
IS substantive and currently unverified in-receipt: Z takes one value
per canonical class (rep-independence), which is what licenses the
class-keyed deformation census and extends foliation invariance beyond
the gated witness pair. The fact is SAFE — verified this delta: 0
violations over all 427 classes — but the receipt should own it.
Repair R-D2 (three lines, pre-verified: exactly my round-1
construction).

**D-n1 — nit: dead scaffold in the banner block.** `for _h in FAM:
_v = full_view(_h) if False else None` — `full_view` is undefined
(never evaluated; dead branch) and the loop assigns None 1,191 times.
The program's own dead-code hygiene class (d34a pre-commit standard).
Delete two lines.

**D-n2 — nit: G-L1's label compression is false as worded; D6's proof
attribution is slightly off; the pinned q-spectrum is not printed.**
The label says "all same-branch factors are constant 1/8" — the
n-branch is 3/4 and the r-branch takes 1/4 and 1/8; only the TESTED
(p) branch is 1/8, which the detail states correctly. Also D6 cites the
constancy as the referee proof's content; my F5 proof establishes L1 at
ALL depths from factor past-locality — branch-constancy is the separate
depth-4 empirical fact that makes the SWEEP tautological. And D6
promises "the q-spectrum disclosure printed" — the full spectrum
(p: 1/8 x 2,420; r: 1/4 x 1,172, 1/8 x 496; n: 3/4 x 2,382) appears
nowhere in the .out. One-line label fix + one print.

**D-n3 — nit: the banner's "PK1 == PK2 on every ARM-1 component (max
size 2; gated)" overstates.** Only max-component-size == 2 is checked,
via a bare `assert` (not a check(); silent under python -O); PK1 == PK2
itself is never computed in-receipt. The inference size<=2 => kernels
coincide is valid (and I verified 0/1,048 directly in round 1), but
"gated" should either become true (one check() comparing the kernels
over the component census — cheap) or the word should soften to
"asserted via max-size".

**D-n4 — nit: pin/receipt convention split is honest but unlabeled on
the pin side.** Pin D1 states Z(empty) = 64/1037 and the root exhibit
(16/133 vs 32/257) in the REFEREE convention without saying so; the
receipt computes the reciprocal (1037/64) and prints normalized weights
(133/2074, 771/2074), with its note. All four numbers are correct and
mutually consistent (verified); a reader moving pin -> .out meets two
unexplained mismatches. One parenthetical in D1 fixes it ("referee
convention; the receipt's forward-mass Z is the reciprocal; its root
exhibit shows the induced normalized weights").

**Notes (no conviction).** (A) D4's third clause: add the epistemic
qualifier (see point 5) — it is my own R4 wording, sharpened on
reflection. (B) The pin header's "the d42b1 extension inherits by the
same arguments — carried" is now superseded IN PART by D5 (the ladder
does NOT inherit — h12); acceptable under the append-only amendment
convention (the A2/A5' precedent), scope it in any future revision.
(C) The h12 attribution conflation in the hand-off message (see point
6) — repo artifacts are correct; recorded here so the referee-stream
provenance stays exact.

## Prescribed repairs (all pre-verified)

- **R-D1 (D-M1).** Replace the [VERDICT] line with decided-status text,
  e.g.: "d42b3 GREEN (round-1 repaired): actor-local counterterms
  impossible for support-preserving re-weightings (T1+D3);
  cut-normalization reconvicted, N cut-attached but not a gradient
  (T2+D2); THE DISCRETE TS-CONDITION DECIDED AT DEPTH 4 —
  ratio-preserving placement REFUTED (36/202 diamonds), gradient
  placement SOLVED at finite depth with unavoidable ratio deformation
  (21/114, root included) (D1); ratio locality a theorem at this depth
  (L1+D6); the obstruction density exact-1/4 scoped to
  single-component join views (L2+D6); h12 carried as the extended-
  grammar completion constraint (D5)." Rerun, re-commit .out.
- **R-D2 (D-m1).** Key Z by canonical class (or add the class-constancy
  check over all 1,191 sequences: 3 lines) and keep the all-histories
  normalization sweep — it becomes the non-vacuous rep-independence
  gate; anchors unchanged (0/427, pre-verified this delta).
- **R-D3..R-D6 (D-n1..D-n4).** The four one-liners above.

## Delta reproduction inventory

- Reruns: `PYTHONHASHSEED={0,61} python3
  v10/code/d42b3_placement_exact.py` -> byte-identical to the committed
  .out, exit 0, 8/8. `python3 v10/code/d42b1_transport_exact.py` ->
  byte-identical, exit 0, 21/21, h12 sums ['1','23/24','1'].
- Scratchpad delta script (inline): reciprocal identity 0/1,191;
  receipt-Z class-constancy 0/427; root-exhibit mapping (16/133,
  32/257) -> (133/2074, 771/2074) with weight sum 1; ok_norm vacuity
  probe (arbitrary boundary 7: conjunct still passes); nesting numbers
  (shared equal; extra 1/4 over 2 'r' events).

**Disposition:** all round-1 findings are repaired or correctly
declared on the anchors; the decided-TS structure is fully
reconfirmed; ONE new defect blocks: the stale verdict sentence (D-M1).
On R-D1 (+ recommended R-D2 in the same touch) and a green rerun, this
delta is CLEAN, d42b3 is fit to go terminal — front 5 closed with the
TS-condition DECIDED at depth 4 — and d42b5/d42b6 (fronts 7-9) open on
the decided structure, carrying D5's h12 constraint and the
actor-factored round-2 target.
