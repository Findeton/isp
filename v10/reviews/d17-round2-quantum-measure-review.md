# D17 round-2 quantum-measure mathematics review

**Date:** 2026-07-11  
**Verdict:** **PASS AT THE FINITE CONDITIONAL NONSELECTION SCOPE**  
**Formal status:** `INCOMPLETE-INVESTIGATION`

## Decision

The round-one mathematical blockers are repaired. Boundary envelope, orbit
square-root factor and fixed action phase are separate. Two positive normalized
envelopes and two orbit conventions propagate through the same D14 record
instrument and yield different exact predictions. The erasure observable
distinguishes coherent and recorded alternatives without a second Born rule.

The projective families now have actual finite causal-order extension
witnesses, validated depths 1–6 and a deterministic unique-child continuation
for induction. The non-Markov support is realized by a local D14 memory packet,
and a complete reset changes the visible process.

No blocker remains for the narrow theorem: one fixed action does not select
the boundary/orbit/projective recorded measure. The extension grammar, record
commit and towers remain supplied rather than action-derived, so the broader
formal status correctly remains incomplete.

## Reproduction

```text
source    305f532548db3734ed6d92896f98ea9803fbcc86a5786a402cfb6cff8a847d42
packet    15cfd44534c4b7de4d66e834a318a00eb666cfaffe7dccb86bc45c2891563cfe
theorem   a4974479024102f0ba503ac81adec44ffc5673e4f712ceba3db2104ded428806
receipt   86c23b29f71efe334b1d0bdb5cedae4e9bf0c081083d80bd5107464fb67858c2
stdout    bf5a54311daf639d612857c36cd40acc637f9a4246bd1cdecb816fa74b80b306
checks    26/26 normal and optimized
```

Normal and optimized output are byte-identical.

## H0–H7 audit

### H0/H7 — fixed action and varied packets

The code computes `A_j=Psi_j sqrt(mu_j) phase_j` explicitly. The action phases
`(-1,+1)`, two-order domain and D14 seal remain fixed.

The equal envelope gives `(1/2,1/2)`. The normalized positive envelope
`(3/5,4/5)` gives `(9/25,16/25)`. Neither relies on a null alternative.
Inverse-automorphism square roots propagate through the seal and normalize to
`(2/3,1/3)`, distinct from uniform `(1/2,1/2)`. This closes boundary and orbit
nonselection end to end.

### H2–H4 — interference, records and Born once

Opposite unrecorded amplitudes have zero erasure probability. After the D14
isometric seal creates orthogonal records, the same erasure test has probability
`1/2`. Branch norms give the recorded probabilities once; no extra click factor
is multiplied. The protected future composes, and the branch construction
implies a Hermitian positive normalized diagonal decoherence functional.

### H5 — projectivity

Both positive-support towers pass normalization, nonnegativity and every parent
sum through depth six. From depth four onward each positive cylinder has one
deterministic child, which supplies the stated all-later-depth induction.

The order witnesses are genuine one-element extensions:

```text
root -> chain2 or antichain2;
chain2 -> chain3;
antichain2 -> V3.
```

The mark tables are therefore tied to a finite causal-extension tree rather
than floating strings. The grammar is still supplied, which the theorem states.

### H6 — non-Markov memory

The equal tower gives exact conditionals

```text
P(z=1|y=0,x=1)=1;
P(z=1|y=0,x=0)=0.
```

The same `000/101` support is generated by the reviewed local D14 carried-memory
packet. Applying the complete reset before the final copy makes `z=0` on both
branches, so deletion changes the visible law. This closes the earlier
universe-ledger objection at the finite witness scope.

## Remaining scope

The result does not derive the extension tree, envelope, orbit convention,
record commit or memory architecture from the interval action. It also does
not provide an infinite causal-order measure, BDG continuum packet, geometry,
units or a V9 prediction. “Complete” must continue to mean the supplied finite
projective family plus its declared deterministic continuation, not a complete
law of all causal orders.

## Gate disposition

```text
H0 PASS; factors are explicit.
H1 PASS for two supplied exact orbit conventions.
H2 PASS for the finite D14 record cell.
H3 PASS; Born weighting occurs once.
H4 PASS on the recorded finite partition.
H5 PASS for depths 1-6 plus deterministic induction.
H6 PASS for one local memory/reset witness.
H7 PASS: fixed action/domain/instrument, inequivalent packets.
H8 PARTIAL: one supplied extension tree, not an action-derived grammar.
H9 PASS in narrowed whole-history scope.
H10 OPEN; V9 remains withheld.
H11 OPEN beyond this review stream.
```

## Final verdict

**PASS AT THE FINITE CONDITIONAL NONSELECTION SCOPE.** The fixed-action measure
nonselection theorem, D14 record cell, causal-extension projectivity and local
non-Markov compatibility are exact. Preserve `INCOMPLETE-INVESTIGATION` for
the still-supplied grammar/instrument and absent continuum/geometry gates.
