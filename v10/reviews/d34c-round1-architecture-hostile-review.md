# D34c round 1 — independent history/locality architecture hostile review

**Target:** commit `9f92a95`. **Verdict:** REJECT AT STATED WIDTH. **Count:** 1 BLOCKER / 4 MAJOR / 3 MINOR / 2 NIT.

## Blocker

**C8 tensors an unrelated interferometer onto a three-symbol toy process.** Any classical law, including a nonlocal global lottery, could pass by carrying the same spectator diamond. Initiator, target, local degree, Ulam child, passive reception, wire predecessors, times, actor state and actual event-isometry composition are absent. The `72->24` check only marginalizes an unused second iid kind.

## Major findings

1. **Timed projectivity/discrete flags unresolved.** D34b has timed `T->S` projectivity but no intrinsic untimed `G_T->G_S`. A finite discrete flag sum is not a timed direct-integral lift. Narrow to fixed `T`, a finite partition, or a discrete local-stopping algebra unless the direct integral/inverse system is built.
2. **Event-local flags asserted.** The executable uses a global block index and inert tuples; it does not build local tensor factors containing target, receiver, child and predecessor data.
3. **Sequential quantum restriction absent.** The second kind does not apply a second instrument. Actual class operators must compose, and summing real extensions must recover the earlier functional.
4. **Remote locality is scalar only.** The probability identity is not `Phi_(A disjoint B)=Phi_A tensor Phi_B` and does not prove quantum marginal invariance.

## Minimum rebuild

Enumerate genuine bounded D34b actor histories; construct local flags and touched-carrier birth/interact/idle maps; bind the diamond to an actual interaction and copy its output into the durable interaction record; compose class operators; recover the classical target/history shadow; gate a birth- and reception-containing cylinder, real extension restriction, degree-dependent partner completeness, remote quantum factorization, and disjoint-event gauge. Resolve or explicitly defer the timed/direct-integral fork.

**Surviving verdict:** `EXACT DIAMOND + ABSTRACT BUSCH LIFT + SPECTATOR PRODUCT COMPATIBILITY PASS`. D34c remains open.
