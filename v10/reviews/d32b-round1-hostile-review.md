# D32B ROUND-1 HOSTILE REVIEW — fresh referee — 2026-07-12

**Target:** `code/d32b_kernel_mechanism_map.py` + `data/d32b_kernel_mechanism_map.out` (7/7) at c4004ed; pin note-d32 §9 (8b7271c, pre-run); LEDGER #146; the LOG D32B entry; lineage D30/`d32a` P4; card v2.2 e7bc80f0be08f537.
**Method of this review:** the full receipt was independently rerun at HEAD (twice, under PYTHONHASHSEED 0 and 7) and byte-compared; the anchor-cell equivalence claim was tested BITWISE against a verbatim copy of D30's grower; every one of the 20 table rows was recomputed per-seed through a clean-room measurement engine (python-int bitset transitive closure, brute-force max-interval scan, independent root-finder for the MM inversion, independent midpoint/chain/ratio3) sharing no code with the repo's numpy chain; the joint rule was re-decided at three z conventions; a 402-seed collision audit was run; the pin, the LEDGER entry, and the LOG entry were audited against what the receipt licenses.

**VERDICT: MAJOR-REVISION — 2 MAJOR / 5 MINOR / 6 NIT. Zero gate flips; zero false printed numbers — every statistic in the table reproduces to all printed decimals through the independent engine, the committed .out is byte-identical on rerun under both hash seeds, and the working tree is clean after. THE MONOTONE-COMPRESSION HEADLINE SURVIVES AT FULL STRENGTH (all 36 adjacent (r, λ) comparisons monotone-decreasing at both N, for d̂_MM and — unclaimed but true — for d̂_mid as well). NO-BAND survives every defensible multiplicity convention with a minimum entry threshold of 10.4σ. Both MAJORs are record-width defects with one shared root cause: the receipt aggregates away the per-cell estimator-identity information §9 explicitly pinned ("whole-order fallback counted and disclosed per cell"), and two headline sentences over-read exactly where that information was hidden.**

## MAJOR

**M1 — The witness cell is 12/12 whole-order fallback at BOTH sizes and is a lightly-perturbed pure-birth tree; the aggregate fallback print conceals this, violating §9's per-cell disclosure clause, and the #146/LOG covariance-axis sentences over-read the cell.**
Evidence and recomputation:
- Per-cell fallback recount (clean room; the receipt prints only "31/240" at B2): **WITNESS N=256: 12/12; WITNESS N=512: 12/12; r=1 λ=½ N=256: 7/12; every other cell 0/12** (7+12+12 = 31 — the aggregate reproduces, and it is maximally uninformative). Witness max-interval sizes: |I| ∈ [18, 32] at 256 and [23, 50] at 512 — **the pinned |I| ≥ 64 interval estimator NEVER RAN on the witness cell**; its d̂_MM column is the web-level whole-order statistic in all 24 runs. §9 pinned "whole-order fallback counted and disclosed per cell"; D30's own table carried per-cell fallback tags ("10/10 web-level") — this receipt is a disclosure regression against both its pin and its lineage.
- The natural comparison the record already owns: the D30 pure-birth control (itself 10/10 web-level) reads 4.62 ± 0.10 (N=256) / 5.22 ± 0.10 (N=512). The witness reads 4.535 ± 0.093 / 5.133 ± 0.059 — **Δd̂ = −0.085 (z = −2.05) and −0.087 (z = −2.42)**; longest chain 16.1 vs 15.6 and 19.2 vs 19.6; ratio3 3.89 vs 3.88 and 4.58 vs 4.67. At the pinned coupling c = 1/(4N²) the kernel is 92.6% births, and the ENTIRE measurable effect of the covariant interact structure on the event order is a ≈ −0.09 shift of the whole-order d̂_MM off the birth tree plus a max-interval enlargement from ~18 to 23–50 (still < 64). The d31c2 delta pass predicted this exactly ("c must scale ~1/u_max² … the realized interaction fraction will be tiny; a design consequence") — the price was disclosed as a design note, but the conclusion sentence does not carry it.
- Consequently #146's "**horizon-covariance does not produce manifold-like order**" is licensed only as: the ONE priced covariant kernel, at its pinned near-zero coupling, run through a whole-order statistic, reads as a birth tree and is NO-BAND. The cell as run has essentially no power on the covariance axis (BF1 already concedes the range confound; the coupling-scale confound is worse and unstated). The LOG's mechanism clause "**a few complete-graph interacts … create massive comparability**" is contradicted by the receipt's own numbers — the interacts barely move any statistic, and the high d̂_MM = 5.13 is the birth tree's SPARSENESS, marginally reduced (−0.09) by the interacts, not comparability created by them.
- Not affected: the witness rows' NO-BAND verdicts (entry thresholds ≥ 14.2σ there); every printed number (reproduced exactly); B4's positivity margin 0.749 (reproduced); fr = 0.074/0.075, which I verified consistent with the mean-field expectation of the pinned birth-mass formula (predicted 0.0762/0.0773).
Repair: per-cell fallback counts printed (the §9 clause); the witness rows labeled whole-order-MM in-table; the #146/LOG covariance sentence re-scoped with the pure-birth comparison carried; the LOG "massive comparability" clause corrected.

**M2 — The receipt's own fresh-seed anchor data refute the d̂_MM leg of the standing M²-exclusion narration, and no record sentence says so.**
Evidence and recomputation:
- Fresh anchor (B3): d̂_MM = 1.908 ± 0.215 (per-seed values verified in the clean room). Applying d32a P4's own exclusion test to the fresh family: **|1.908 − 2| = 0.092 < 0.120 = 3×card-SD → NOT excluded** (the D30-seed value was 0.244 > 0.120). Against the card mean (2.006 ± 0.040): |Δ| = 0.098 ≤ 3.5σ = 0.140 — the anchor cell PASSES the B5 MM in-band clause for d = 2 (mm_z = 2.46). Its NO-BAND verdict is carried entirely by the matched-d chain constant (c₂ = 2.96 vs card 1.801 ± 0.104: **z = 11.2**) and ratio3 (**z = 9.6**).
- So the published exclusion mechanism — D30 W4's "K_collar is excluded from M² by d_hat at ~6 card-SD" and d32a P4's "M²-exclusion re-verdict … HOLDS (0.244 > 0.120)" — is now known to be **seed-family-contingent on its d̂ leg**: on fresh seeds the kernel's MM dimension is statistically indistinguishable from M² sprinklings, and "decisively non-M²" survives decisively but ONLY via the joint discriminants. #146's "the record's tightness was partly seed-family luck (kernel-level SD ≈ 0.2)" flags the SD and stops one sentence short of the consequence.
- B3's label "the signature pair replicated" is also generous: both legs moved UP ~2 SE (MM +0.152, z = 2.18; mid +0.149, z = 1.85) — replication at kernel-SD width only. And the luck attribution is directional: P(sample SD ≤ 0.101 | σ = 0.215, n = 10) = 0.008 and F = 4.53 on (11, 9) df — the honest statement is that the two families' spreads differ at ~2σ and the record's tightness does not transport; "the record was lucky" vs "the fresh family was unlucky" is not decided by this data.
- Not affected: B3 is ungated by design (§9 pins no bar there — pin-conformant); the fragility measurement 0.50 reproduces exactly; the anchor-cell IDENTITY is beyond doubt (see SURVIVES ATTACK — bitwise).
Repair: a forward-correction sentence at #147 rescoping the M²-exclusion of the collar kernel to the joint rule (chain_c + ratio3 + interval structure), with the fresh-family MM in-band fact carried; "replicated" qualified by the +2.2/+1.9 SE mean shifts.

## MINOR

**m1 — The corner cell (r=1, λ=½, N=256) is a two-estimator mixture.** 7/12 seeds fell back to whole-order MM; the per-seed d̂_MM is bimodal — fallback seeds 3.423 ± 0.114, interval seeds 1.965 ± 0.226 — and the printed 2.816 ± 0.739 averages incommensurables (the "huge SD" is mixture-driven, not kernel noise). Monotonicity survives stratification (interval-only 1.965 > 1.750 at the next cell; both strata fall along the row), and the #146 headline endpoint (r=1, λ=½, N=512 = 2.109 ± 0.350) is 0/12 fallback — clean. But the table gives the reader no way to see any of this. Repair: covered by M1's per-cell print plus one mixture flag at this cell.

**m2 — The joint rule's z choices are implementation, not pin, and the multiplicity universe is unstated; verdict-invariant, verified.** §9 pinned "MM within 3·SD … AND matched-d chain constant in-band AND ratio3 in-band AND separated …" plus "Bonferroni over the 10 cells for any band-entry claim". The code applies z = 3.5 to the three in-band clauses (disclosed in the .out, credit for that) and z = 3.0 to control separation; exact Bonferroni from a 3σ base over 10 cells is z = 3.643 (over the 20 printed verdict rows: 3.817; over 60 row×d tests: 4.080) — 3.5 matches none of these, and whether the family is 10 cells or 20 rows × 3 bands is never said. Consequence: none — I re-decided every (cell, d) at z = 3.0, 3.5, and 3.646: **NO-BAND everywhere at all three**; the minimum entry threshold over all (cell, d) is **10.4σ** (r=1, λ=1, N=256, d=2), i.e., the table survives any defensible convention by ≥ 3×. Nearest misses, for the record: r=1λ=½-512 at d=2 passes MM (2.55σ) AND chain (0.32σ) and dies on ratio3 alone (17.7σ); r=1λ=½-256 at d=3 dies on chain alone (13.1σ); the anchor-512 at d=2 passes MM (2.46σ) and dies on chain (11.2σ) + ratio3 (9.6σ). Observation worth carrying: the compressed cells read MM-CLOSE to the percolation control at matched N (r=2λ=1-512: −1.2 control SD; r=3λ=½-512: −1.5) — the compression is toward the non-manifold controls, reinforcing NO-BAND's physical reading. Repair: one reconciliation sentence (pin's 3·SD vs implemented 3.5/3.0; family size).

**m3 — "The signature gap positive at EVERY cell … UNIVERSAL" is a cell-MEAN claim; per-cell significance fails at 2 of 20 cells.** Clean-room, ddof=1 SEs: anchor-512 gap +0.129, z = 1.85 (p = 0.064); r=2λ=1-256 gap +0.047, z = 1.66 (p = 0.097); the other 18 cells are ≥ 3.08σ, and 20/20 means are positive (family-level evidence overwhelming). Per-seed, 12 of 20 cells contain negative gaps (up to 4/12 at the anchor cell). #146's "the multi-exponent character is UNIVERSAL in this family" and the LOG's "(MM > mid universally)" need one scope sentence: universal in cell means, not per-seed, and individually non-significant at two cells.

**m4 — The r-axis metric semantics were never pinned, and the code's silent choice deviates from the lineage's.** For r ≥ 2, `grow_grid` never updates `dist` after an accepted interact — d_cover is the STATIC genealogical (birth-tree + seed-edge) metric — whereas in the D28/D30 lineage every beyond-collar coupling is an edge insert that shortens d_cover (D30's K_tail runs incremental APSP on exactly this). §9 gives only the weight formula; the implemented family is self-consistent, pre-committed, and (r=1, λ=1) is exactly K_collar where the question vanishes — but 6 of 9 grid cells sit at r ≥ 2, so "interaction range" in every narration means static-tree radius, the edge-insert r-ball family (the lineage-faithful generalization, plausibly a different geometry since far interacts would densify the collar) is UNTESTED, and the F12 sentence inherits this scope. Repair: one semantic-disclosure sentence; D32C should name which family it scans.

**m5 — §9's per-cell deliverable block is under-delivered.** Pinned per cell but absent from the .out: the matched-d chain constant and ratio3 (computed and verdict-consumed, never printed — and they are the two statistics that DECIDE every NO-BAND, see m2's margins); α "from the 256→512 two-point slope (**SE printed**)" — no SE appears anywhere, and `stats()` discards L's SD so the receipt as written cannot compute one (recomputed here: α SEs 0.058–0.109, which makes most inter-cell α differences ≲ 2σ — worth knowing); the covariance-status column (the 9 grid cells' VIOLATING label appears nowhere in the .out; only the witness is tagged). Also: B6's 72 seed-forgetting growths sit outside every fallback ledger (recount: 1 fallback, star r=1λ=½; immaterial). Repair: print block per §9.

## NIT

**n1** — The realized-i-frac and mean-dcov columns are computed over the full 3N+64-act growth run, not the N measured acts (measured deltas: r=2λ=1-512 prints 0.854 vs 0.840 over the web; r=3λ=2-512 prints 0.951 vs 0.938). BF4's direction reading is unaffected; disclose the window.
**n2** — Gate taxonomy: B3, B5, B6, B7 cannot fail (`ok5` is vacuously true; `ok6 &= True` is dead; B3/B7 pass literal `True`) — 7/7 = 3 substantive gates (B1/B2/B4) + 4 report-gates. The d32a round's tally-line-taxonomy nit recurs: d32a's final line disclosed its split, d32b's does not.
**n3** — B4's "largest realizable s = 515": decision-time s never exceeds 514 (nreg ≤ 3 + 511 at the last decision); checking 515 is conservative, the label imprecise.
**n4** — The witness rows print mean-dcov "0.00" — an unmeasured quantity for the complete-graph kernel rendered as a measured zero; should read "—".
**n5** — §9-silent in-code conventions, both benign, both verified: the seed-forgetting arm's 900000/50000 offsets and the star/triangle seed GRAPHS exist only in code (the 402-seed collision audit is clean — grid, witness, seed-forgetting, D30, census, and card families all mutually disjoint). Pin them next time; the seed-forgetting formula also silently drops the 1000·(N/256) term (harmless at N=512-only).
**n6** — The MM bisection bracket floor (lo = 1.05) is 0.005 away from the data: the compressed cells' per-seed minima reach 1.054 (r=3λ=2-256). No seed clamped THIS run (verified per-seed via an unclamped solver), but any further compression (D32C's dense scan) will censor silently; widen the bracket or print floor distance.

## SURVIVES ATTACK
- **The anchor-cell equivalence (the receipt's load-bearing identity claim): VERIFIED BITWISE.** `grow_grid(1, 1.0, ·, ·, 'chain')` and a verbatim copy of D30's `grow('collar', ·)` produce identical act streams from shared seeds — tested on all 12 B3 seeds, all 10 D30 anchor seeds, 12 N=256 grid seeds, and 3 arbitrary seeds (37/37; full common prefix, not just first-N). The differing truncation conventions (receipt: 3N+64 acts → [:N]; D30: 3N ops → non-none[:N]) are provably immaterial: per-iteration rng consumption is identical whether the none is appended or skipped, so the act stream is the same, and D30's 3N ops always contained ≥ 1524 ≥ N acts. "(r=1, λ=1) IS K_collar" is exactly true.
- **Reproducibility (the #139/#145 defect class): CLEAN.** Static audit: no `set()` iteration anywhere; all dicts are insertion-ordered or consumed order-free. Empirical: two full reruns at HEAD under PYTHONHASHSEED 0 and 7 — both byte-identical to the committed .out (md5 22960fd34f7faae85c199eeae7edcc5f); working tree clean after; env matches the card's pinned environment exactly (python 3.8.20, numpy 1.23.5).
- **Every printed number: INDEPENDENTLY RECOMPUTED.** The clean-room engine reproduces all 20 rows' six statistics to all printed decimals (including the witness pairs, the corner cell, gaps and SDs), total fallback 31, the B6 line (0.52/0.92/0.60/0.67/0.59/0.66 SE — ddof=1 variant moves the max to 0.88; verdict unchanged; the pooled two-sample SE formula is sound), the B3 fragility 0.50, and — through the same independent engine — the D30/D32A anchor record (1.756 ± 0.101, 1.630 ± 0.127) exactly.
- **The monotone-compression headline: TRUE AT FULL TABULAR STRENGTH.** All 36 adjacent comparisons (6 lines × 2 axes × 2 sizes) are strictly monotone-decreasing in d̂_MM; the same holds for d̂_mid (unclaimed); the mixture cell (m1) does not carry the claim — stratified, the direction survives; the #146 endpoint values (2.109 ± 0.350 → 1.107 ± 0.027) are fallback-free cells.
- **NO-BAND: decided by ≥ 10.4σ under every multiplicity convention tested** (z = 3.0 / 3.5 / 3.646), with the deciding clauses being chain constant and ratio3 — the two statistics the corner's MM-passing cells fail by 11–18σ.
- **The witness kernel's implementation:** Z ≡ 1 exact (birth mass + (s−1)(s−2)c = 1 algebraically); the ordered-pair decode is uniform over u(u−1); c = 1/(4N²) per §9; positivity margin 0.749 reproduced; fr consistent with the pinned formula's mean-field expectation (0.0762/0.0773 vs 0.074/0.075).
- **Discipline:** §9 committed at 8b7271c pre-receipt and unchanged to HEAD; the receipt committed pre-run at 3d03546 and byte-identical at HEAD; the .out lands only at c4004ed; seeds disjoint from every prior family by construction and by audit.

## GATES RE-VERIFIED
`python3 v10/code/d32b_kernel_mechanism_map.py` at c4004ed: **7/7 PASS, .out byte-identical, twice (PYTHONHASHSEED 0 and 7), minutes-scale on the card's pinned environment; `git status` clean after.** Clean-room pass (all 240 receipt growths + 72 seed-forgetting + 10 D30-anchor regrowths through the independent measurement engine): every receipt-printed statistic reproduced.

## FALSE-RESULT COUNT
**In the receipt (gates + printed numbers): 0.** In the record lineage: **0 strictly false sentences; 2 over-wide headline sentences** owed re-scope (#146's "horizon-covariance does not produce manifold-like order" — the cell reads as a −0.09-perturbed pure-birth tree, M1; #146/LOG's "UNIVERSAL"/"universally" gap claim — mean-level only, m3); **1 unsupported mechanism clause** (LOG's "a few complete-graph interacts create massive comparability" — contradicted by the receipt's own numbers, M1); **1 material omission** (the fresh-anchor MM reading un-derpins the standing d̂-based M²-exclusion narration and nothing says so, M2). The §9 per-cell-fallback breach is a pin-conformance defect, not a falsity.

## OWED REPAIRS (exact list)
1. **(M1)** Print per-cell fallback counts (WIT 12/12 + 12/12, r=1λ=½-256 7/12, others 0; B6: star-r=1λ=½ 1/12); label the witness rows whole-order-MM; add the |I|max < 64 fact (32/50) to B4's detail.
2. **(M1)** Re-scope the #146/LOG covariance-axis sentence: the priced covariant kernel is birth-dominated at grid sizes (fr ≈ 0.074 by construction of c = 1/(4N²)) and reads as a lightly-perturbed pure-birth tree (Δd̂_MM ≈ −0.09 ≈ 2σ vs the D30 birth control; L and ratio3 within a few percent) — NO-BAND; the cell bounds covariance's effect at the priced coupling only. Delete or invert the LOG's "massive comparability" clause.
3. **(M2)** Forward-correction at #147: the d̂-leg of the M²-exclusion (D30 W4's "~6 card-SD"; d32a P4's re-verdict) does not transport to fresh seeds (|1.908 − 2| = 0.092 < 0.120; MM in-band at 2.46σ); the exclusion is henceforth carried by chain_c (11.2σ) + ratio3 (9.6σ) + the joint rule. Qualify B3's "replicated" with the +2.2/+1.9 SE mean shifts; state the luck attribution two-sidedly (P(SD ≤ 0.101 | σ = 0.215) = 0.008; F = 4.53).
4. **(m1)** Flag the corner cell as a 7/12 two-estimator mixture (strata: 3.423 ± 0.114 whole-order / 1.965 ± 0.226 interval); note monotonicity survives stratification.
5. **(m2)** One sentence reconciling z = 3.5/3.0 with §9's "3·SD + Bonferroni/10" and naming the family size; carry the 10.4σ minimum entry threshold (this review supplies it) so NO-BAND's robustness is in the record.
6. **(m3)** Scope the gap-universality sentence (cell means; 2 cells individually < 2σ; negative per-seed gaps in 12/20 cells).
7. **(m4)** Disclose the static-metric d_cover semantics for r ≥ 2 (interacts are metric-inert by CHOICE here, vs the D30 tail's edge-insert precedent); scope "this family" in the F12/#146 sentence accordingly; D32C names its family.
8. **(m5)** Print the §9 per-cell block: matched-d chain_c, ratio3, α ± SE (fix `stats()` to retain L's SD), covariance-status column.
9. **(n1–n6)** Window disclosure for fr/dc; tally-line gate taxonomy; "s = 515" label; witness dcov "—"; pin the seed-forgetting offsets and star/triangle graphs; widen or instrument the MM bracket floor.

## SUMMARY JUDGMENT
The map's computational content is fully real: every number reproduces two independent ways, the anchor identity holds bitwise, the reproducibility defect class that haunted d31b/O3 is absent, and the two headline negatives — monotone compression and NO-BAND — come out of independent recomputation STRONGER than printed (36/36 monotone comparisons; 10.4σ minimum entry threshold). What does not survive at printed width is the narration at the receipt's two edges: the covariant-at-cap row is a birth tree wearing a covariance label (and the one §9 clause that would have shown this per-cell was aggregated away), and the fresh-seed anchor quietly removed the d̂-leg from under the record's oldest exclusion sentence while the LEDGER credited only "seed-family luck". Both physics conclusions survive at correct scope; MAJOR-REVISION because, as with O3, the record must say why at the correct scope — and this time the receipt's own pinned disclosure rule, had it been followed, would have said most of it.
