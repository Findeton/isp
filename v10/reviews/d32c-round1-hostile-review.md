# D32C ROUND-1 HOSTILE REVIEW — fresh referee — 2026-07-13

**Target:** `code/d32c_s2_mechanism_test.py` + `data/d32c_s2_mechanism_test.out` (6/6) at 2dfa845; pin note-d32 §10 (b6d8a9e, pre-run); LEDGER #149; the LOG D32C entry; lineage D32B #146–#148; card v2.2 e7bc80f0be08f537. **The round's charter: the §10 pre-registered lift bar fired CONFIRMED-LIFT at all six live cells (discovery dz 5.3–41.5) and the coordinator declared the bar CONFOUNDED same-date, pre-round — interaction starvation, not directional geometry. That demotion is the central adjudication, prosecuted here in both directions.**

**Method of this review:** the full receipt was independently rerun at HEAD and byte-compared (fresh random hash salt; the card's pinned environment: python 3.8.20, numpy 1.23.5); a bit-identical reimplementation of the grower (incremental collar-pair bookkeeping in argwhere row-major order, proven act-stream-identical on probe growths across all 7 cells — 8/8) re-collected all 168 discovery growths plus the 84 distinct confirmation growths with per-seed instrumentation; every printed statistic (14 table rows across every printed column, 6 discovery dz, 6 confirmation dz, the global min, C4's z = 1.15) was recomputed to all printed decimals; a clean-room measurement engine (set-based transitive closure, explicit max-interval pair scan, independent MM root-finder, Σ|past|·|future| ratio3) re-measured the control and κ=2 p=0 cells per-seed (24 webs, worst |Δ| = 2.75e-12); **the matched-interaction-fraction control the bar lacked was BUILT AND RUN** (a direction-blind λ-scan of the same r=1 collar kernel, 12 seeds per matched cell at N=512, referee family 80260712+; λ=1 sanity 1.883±0.211 ≈ the anchor; pure-birth λ=0 anchor 5.286±0.071); two mechanism-isolating controls (seed-pair-only; thinned-eligibility) decomposed the residual; an interact-dot census ran at all κ cells (~400 further referee-family growths in total); the icosahedral construction, neighbor sets, sampling clamp, cross-κ stream identity, seed-collision ledger, CONF_C discipline, and the pin/commit chain were audited.

**VERDICT: MAJOR-REVISION — 3 MAJOR / 4 MINOR / 6 NIT. Zero gate flips; zero false printed numbers — the committed .out is byte-identical on independent rerun, and every statistic in it reproduces through two independent engines. THE COORDINATOR'S CONFOUND CALL IS CONFIRMED, QUANTITATIVELY, IN BOTH DIRECTIONS: (a) the direction-blind collar kernel, λ-tuned to each live cell's realized interaction fraction, predicts 101–126% of every cell's d̂_MM elevation over the control — the entire "lift" is the fr collapse; (b) the residual after conditioning on fr is small and NEGATIVE at all six cells (−0.02 to −0.55; slope-corrected mean z = −1.96, Stouffer ≈ −4.8) — at matched fr, direction weighting makes these webs slightly MORE ordered, so what survives the confound runs OPPOSITE to a lift; (c) the pre-round demotion was procedurally sound (formal outcome preserved verbatim; demotion argued from receipt-carried columns; handed to this round as the central question) — but the record under-diagnoses the failure: the confound was derivable pre-run from #146–#148 in two lines of mean-field arithmetic, matched fr is UNREACHABLE inside the pinned family, and the family algebraically reduces at r=1 to a starvation family that cannot express S² geometry in bulk; (d) the formal-outcome/interpretation split is coherent. The MAJORs are record-width; no physics conclusion moves.**

## THE CENTRAL ADJUDICATION (the charter's (a)–(d))

**(a) Starvation sufficiency: CONFIRMED — the missing matched-fr control was built, and it accounts for everything.**
- The direction-blind λ-scan (the §9 grid's own w = λ·|pairs| axis, extended below λ=½ into the starved regime; direction draws consumed identically; the receipt's measure chain verbatim) traces the collar kernel's fr → d̂_MM curve at N=512: 5.286±0.071 at fr 0 (pure birth), 5.234 at 0.003, 5.249 at 0.007 (the one adjacent inversion, 0.4 SE), 5.157 at 0.055, 5.090 at 0.082, 4.933 at 0.145, 4.570 at 0.211, down to 1.883±0.211 at 0.651 (λ=1 — statistically the anchor/control; machinery sanity).
- Per live cell, elevation accounting (blind curve interpolated to the cell's realized web-fr; control 2.017): k=1 p=0: observed +3.062, starvation predicts +3.228 (105%); k=1 p=¼: +2.789 vs +2.933 (105%); k=1 p=½: +2.001 vs +2.512 (126%; the 9/12-mixture cell, both sides mixtures — the weakest comparison); k=2 p=0: +3.195 vs +3.216 (101%); k=2 p=¼: +2.983 vs +3.156 (106%); k=2 p=½: +2.998 vs +3.088 (103%). **The starvation curve predicts every observed elevation within bands — and slightly OVER-predicts each one.**
- The starvation is exactly the pinned family's arithmetic (also M3's foreseeability evidence): at r=1 the interact candidates are precisely the tree edges plus the seed pair (1,2); a child's direction is its parent's, or one icosahedral hop with probability p, so every bulk candidate pair has dot ∈ {1, 1/√5} — weight exactly 0 (inherited) or exactly ((1−1/√5)/2)^κ = 0.276^κ (scattered). Mean-field fr = 2p·0.276^κ/(1+2p·0.276^κ): 0.121/0.217 (κ=1, p=¼/½) and 0.037/0.071 (κ=2) vs printed 0.130/0.217 and 0.041/0.071. Control: 2/3 exactly vs printed 0.667.
- The p=0 case behaves EXACTLY as the starvation account demands: the only positive-weight pair is the seed pair — the instrumented recount shows every realized interact at both κ, both N, all 48 webs is (1,2)/(2,1); per-seed interact counts track ((1−dot12)/2)^κ, including the dot12 = 1 seeds with exactly ZERO interacts (weight exactly 0.0 — the icosahedral self-dots are exactly 1.0, so the κ=1 negative-weight corner is empty); the κ=2 antipodal-seed web (dot12 = −1, weight 1) is that cell's interact maximum (12), as predicted.

**(b) Residual directional effect at matched fr: EXISTS, ALL-NEGATIVE (anti-lift), and non-geometric.**
- Residuals (live − blind at matched fr, slope-corrected): −0.166 (z −3.21), −0.144 (−2.10), −0.511 (−1.22), −0.021 (−0.53), −0.173 (−3.16), −0.090 (−1.54). All six negative; naive Stouffer ≈ −4.8. **A real second-order effect of the direction machinery survives conditioning on fr — and its sign is toward MORE comparability (lower d̂_MM), the opposite of the pinned lift.**
- Decomposition (two referee controls): (i) at p=0, a "pure-birth + λ-weighted seed-pair" blind control at matched fr reproduces the live cells (z = +0.93 at κ=1, +0.03 at κ=2) — the entire p=0 residual is interact PLACEMENT: every interact joins the two otherwise causally-disjoint seed clans, maximally connective, while blind interacts land on nearly-redundant parent–child pairs. (ii) at p>0, a thinned-eligibility blind control (each edge eligible with coin q=p at the uniform weight 0.276^κ, seed pair excluded) realizes the mean-field fr exactly and reads HIGHER than live by the seed-pair share — consistent with the algebraic identity: the live κ kernels ARE the thinned-uniform process plus the dot12-weighted seed pair. The residual is a seed-geometry (cross-clan channel) effect, not S² geometry.
- The interact-dot census nails the non-geometric reading: at the four p>0 cells the realized interact dots are +0.447 in bulk (scattered parent–child; e.g. 4144 of 4175 at κ=1 p=½), the only anti-aligned interacts are the ≤2% seed-pair events (−0.447/−1.0), and dot-1.0 pairs NEVER interact. **The pinned "anti-aligned (head-on) compatibility" never selects a head-on bulk pair, because none exists among r=1 candidates: the family cannot express S² geometry in bulk beyond the single scalar 0.276** — the 12-vertex discretization enters the bulk dynamics through that one number only (CF1's 48-point refinement would change the scalar, nothing structural, at r=1).
- Consequently the D32C axis does NOT sit exactly on "the one compression/starvation curve": the κ cells sit measurably BELOW the direction-blind curve at matched fr (above), and on the midpoint axis the starved-collar webs (d̂_mid 1.03–1.5) separate from the D32B witness (1.983) at comparable fr — "the witness/birth-tree signature" is a family, not a point. Neither deviation rescues any lift; both need scope words (M1).

**(c) Procedure and #149's width: the demotion is PROPER; the diagnosis is UNDER-SCOPED (M3), and one campaign clause re-widens a withdrawn sentence (M1).**
- Propriety: pre-registration binds the record to report the formal outcome and forbids silent suppression — both honored (the .out carries CONFIRMED-LIFT ×6 unedited; #149 opens with the fire before demoting it). The demotion resolves an internal instrument conflict: the OTHER pre-registered instrument (the card-v2.2 joint rule) reads NO-BAND at every cell at minimum entry threshold 11.2σ (this review's recomputation — the receipt dropped the number, m3), so the two pinned verdicts pointed opposite ways and the coordinator sided with the one carrying mechanism evidence (the fr and align columns, in-receipt). The demotion was declared before the round and handed to it as the central question; the incentive direction is self-skeptical (the coordinator destroyed his own positive). This is forward-correction discipline working as designed. What pre-registration does NOT license is treating "the bar lacked a matched-fr control" as the whole diagnosis — see M3.
- Width of "the S²-mechanism hypothesis as pinned is NEGATIVE": the parenthetical "(no manifold-band order from anti-aligned icosahedral compatibility on the r = 1 collar)" carries the family/sign/discretization scope correctly, and NO-BAND is bar-independent and confound-independent — that leg is licensed unconditionally. What the sentence under-states is the MODE of the negative: the family is negative because it is structurally incapable of running directional selection at healthy fr (max reachable fr at p=1: 0.356 for κ=1, 0.133 for κ=2, vs the control's 0.667 — in-family matched fr does not exist at the pinned λ=1), not because directional geometry was expressed and found inert. NEGATIVE-at-family stands; "tested and absent" would not. #149 never says the latter — but the design lesson it pins ("lift bars compare at MATCHED realized interaction fraction") is unexecutable inside this family, which the record should say (M3).

**(d) Record coherence: YES.** The .out is the instrument record (bar fired, formal, byte-reproducible); the LEDGER/LOG carry the demotion as interpretation, opening with the formal outcome and never editing the artifact; the hostile round adjudicates. The only blemish is internal to the receipt's gate taxonomy (C5 passes literally `True` in both branches — m2), not the outcome/interpretation split.

## MAJOR

**M1 — #149's campaign-verdict sentence re-widens the #147-WITHDRAWN covariance claim and over-states curve unity.** "Neither radius, weight, horizon-covariance, nor (this) directional structure produces manifold-like order — every axis lands on the one compression/starvation curve": (i) #147 explicitly withdrew "horizon-covariance does not produce manifold-like order" at that width ("the cell BOUNDS covariance's effect at the priced coupling only ... does NOT probe covariance at high interaction density") — the campaign clause re-asserts the withdrawn width, an internal contradiction in the authoritative map; (ii) the radius/weight legs drop #147's pinned family scope ("static-metric collar kernels at this grid"); (iii) "the one curve" is measurably approximate: the six D32C cells sit below the direction-blind curve at matched fr (all-negative residuals, Stouffer ≈ −4.8 — an anti-lift offset via the seed-pair channel), and the witness's midpoint leg (1.983) separates from the starved-collar midpoints (1.03–1.5) at comparable fr. The LOG's "the cells sit on the D32B fr-compression curve run backward" was, at writing, an extrapolation from a curve with no collar data below fr 0.5 (the only prior low-fr point was the complete-graph witness); this review measured it: true to within the all-negative residual above (and the witness point itself sits on the blind-collar curve, z ≈ +1.2). Both deviations REINFORCE the negative verdict, so no conclusion moves — but the map now contains one sentence wider than its own corrections. Repair: re-scope at #150 (covariance → the priced-coupling bound; radius/weight → the static-metric collar family; "one curve" → one starvation REGIME, with the measured anti-lift residual and the witness-midpoint separation carried).

**M2 — The starved regime silently broke the midpoint column, and the receipt dropped exactly the D32B stats field that would have disclosed it; one live cell is an undisclosed two-estimator mixture whose discovery/confirmation pair was measured under DIFFERENT estimator mixes.** Recomputation: at |I| < 16 `midpoint_dim` returns None, and the starved cells hit this hard — n_mid = 7/12 (κ=1 p=0 N=256), 11/12 (κ=1 p=0 512), 5/12 (κ=2 p=0 256), 8/12 (κ=2 p=0 512), 4/12 (κ=2 p=¼ 256), 9/12 (κ=2 p=½ 256), 8/12 (conf κ=2 p=0) — the printed d̂_mid ± sd at those cells averages an undisclosed, |I|-selected subset (down to a 4-seed mean behind a printed "1.044±0.044"), and the gap column inherits the same selection. D32B's `stats()` carried `n_mid`; D32C's `stats()` deleted the field — §10's "the D32B measurement/verdict machinery verbatim" is false in precisely the field that mattered, and the per-cell disclosure discipline (fallbacks AND minima) missed the new hole the starved regime opened. Same class: κ=1 p=½ N=512 is a 9/12 two-estimator mixture (whole-order stratum 4.747±0.192 vs interval stratum 1.829±0.115 at |I| = 68–72; the printed 4.018±1.276 averages incommensurables); the fb count is in C2 but the strata are not; the table rows carry no [whole-order-MM] tags (D32B's repair convention, dropped — 11 of 12 live cells are 12/12 whole-order fallback, correctly disclosed per-cell in C2, which answers the charter's disclosure question); and that cell's CONFIRMATION runs are 12/12 whole-order (|I|max 57) — the discovery dz = 5.33 vs confirmation dz = 31.01 spread there is largely an estimator-mixture artifact, not physics. Consequence audit: no verdict consumes d̂_mid (joint rule = MM/chain/ratio3; bar = MM), the missing-mid seeds are the MOST tree-like so "midpoint → 1" is directionally safe, and both dz values cleared the bar — zero conclusion flips. Concealment-class defect per the D32B M1 precedent. Repair: per-cell n_mid printed; the mixture cell's strata printed; [whole-order-MM] row tags restored; a C5 note that the κ=1 p=½ discovery/confirmation pair mixes estimator populations.

**M3 — The record under-diagnoses the bar failure: the confound was derivable PRE-RUN from #146–#148, matched fr is UNREACHABLE inside the pinned family, and the family reduces algebraically to a starvation family — so the pinned design lesson cannot be executed by any successor within it.** Evidence: (i) foreseeability — the two-line mean-field in (a) (candidates = tree edges; candidate dots ∈ {1, 1/√5}; fr = 2p·0.276^κ/(1+2p·0.276^κ)) uses nothing not in the record at pin time (#146's fr column, #147's witness/birth-tree lesson, §10's own family definition) and predicts the realized fr of all six cells to ≤0.009 absolute; the same arithmetic, run at CF2's pinning, would have fired the confound before a single growth. (ii) unreachability — max in-family fr at p=1 is 0.356 (κ=1) / 0.133 (κ=2) vs the control's 0.667, so "#149: lift bars compare at MATCHED realized interaction fraction" is not executable at the pinned λ=1: the matched-fr successor requires λ-retuning per (κ,p) (λ ≈ 3.6/p at κ=1), multi-hop/global scatter, r ≥ 2 collars (where multi-hop-misaligned candidates exist), or the CF4 aligned variant — which is the ONE in-family arm that runs at healthy fr (aligned weights give inherited edges weight 1: fr ≈ 0.65). (iii) reduction — the bulk dynamics carry exactly one scalar of S² content (0.276^κ), so CF1's "12-vertex coarseness" front is moot at r=1, and the receipt tested a two-parameter thinned-starvation family plus one seed-pair channel, not sphere geometry. None of #149's sentences is false; the omission is material because the design lesson and the successor fronts (CF1/CF4) are mis-aimed without it. Repair: a #150 forward-correction carrying the reduction, the reachable-fr bound, the re-aimed lesson, and the CF1/CF4 re-scopes.

## MINOR

**m1 — CF2 under-delivered: the pin ordered "the realized parent–child alignment distribution per cell"; the receipt prints a single interact-weighted mean that conflates three populations and zero-fills.** The al column is the mean dot over REALIZED INTERACTS (not the parent–child population), and webs with zero interacts contribute a placeholder 0.0 to the cell mean (at κ=2 p=0 N=512, 5 of 12 webs are interact-free; the printed −0.009 is mostly zero-fill averaged against a −1.0 antipodal-seed web). The actual distribution (this review's census): bulk interacts at +0.447 exactly, seed-pair events at ±0.447/±1, inherited pairs never — which is the mechanism evidence #149's "nearly-aligned population ⇒ weights collapse" clause needed. Repair: per-cell dot census (a three-value histogram suffices) or an al-semantics note (interact-weighted; zero-fill count).

**m2 — The C5 gate cannot fail, and the κ-monotonicity implementation is weaker than the pinned bar.** Both C5 branches pass literal `True`; `all_conf` is computed and never consumed — an UNREPLICATED outcome would still print "ALL CHECKS PASS (6/6)", with the failure visible only in detail text. Monotonicity is implemented as d̂(κ=2) ≥ d̂(κ=1) at fixed p, from DISCOVERY data only (a κ=2 fire would pass even with κ=1 below control; the confirmation family's monotonicity is never checked). Consequence-free this run — this review verifies confirmation-family monotonicity holds (5.084 ≤ 5.201; 4.832 ≤ 5.070; 4.564 ≤ 5.047) and every κ=1 mean sits far above the control — and the tally's "C5 [substantive]" (with "C3 [substantive]") mislabels report-gates: the substantive-capable set is C1/C2. Third recurrence of the D32B n2/Δ-n3 taxonomy defect, now on the headline gate. Repair: gate C5 on `all_conf` in the fire path (NO-LIFT stays a passing result); state the mono convention; honest tally taxonomy.

**m3 — The minimum joint entry threshold was dropped from the receipt (a #147-landed repair), un-carrying NO-BAND's robustness; this review supplies it: zmin = 11.2σ** (the control at N=256 vs d=2: legs mm 6.46σ / chain 11.15σ / ratio3 2.63σ; every live cell×d combination has a leg ≥ 12σ). NO-BAND therefore survives every defensible convention — z = 3.5 as implemented, the exact Bonferroni/7 point 3.55, and D32B's 3.0/3.646 variants — by ≥ 3×. Worth carrying because it is not obvious from the table: at N=256 several live-cell MM means sit INSIDE the d=4 MM band (e.g. 4.144 vs 3.974±0.147: 1.16σ) — NO-BAND at those cells is carried entirely by chain_c and ratio3, exactly the D32B m2 pattern, and with the zmin print absent the .out gives no way to see it. Repair: restore the zmin line.

**m4 — The fr/al window semantics are undisclosed and materially different from the measured web at the p=0 cells (D32B's n1 note, dropped).** The printed fr/al are computed over the full 3N+64-act growth window; the measured web is acts[:N]. At the starved p=0 cells interacts concentrate early (per-step odds ∝ 1/U), so the web's fr is 2–3× the printed window value (κ=1 p=0: web 0.021 vs printed 0.007 at N=256; 0.009 vs 0.004 at N=512). #149's "fr 0.667 → 0.001–0.22" quotes window values; the measured webs span ≈0.004–0.22. No conclusion moves (both collapse by 3–160×), but the mechanism narrative quotes the un-annotated number. Repair: restore the window note; print web-fr at least at the p=0 cells.

## NIT

**n1** — "(d̂_MM, d̂_mid) ≈ (4.1–5.2, ...)" (#149 and LOG): the live d̂_MM floor is 4.018 (κ=1 p=½ N=512) — "4.0–5.2"; or cite that cell's whole-order stratum 4.747, which "4.1–5.2" covers — say which.
**n2** — Exact Bonferroni/7 from a 3σ base is z = 3.55, not the implemented 3.5 (D32B Δ-n5 recurrence); verdict-irrelevant at zmin = 11.2σ.
**n3** — The bar and its confirmation run at N=512 only; §10 pins neither restriction. Consequence-free (all six cells also clear at N=256: elevations ≥ 2.4 over the 1.667±0.190 control at pooled SEs ≤ 0.1) — state the convention.
**n4** — The confirmation control (`run_cell(0, 0.0, 0, 512, CONF_C)`) is recomputed identically inside the fire loop for each of the six fired cells — deterministic, so harmless waste (72 redundant growths); hoist it.
**n5** — Sampling-clamp edge audit (the charter's question), graded: interior cumsum edges are handled CORRECTLY by `side='right'` (a zero-weight interior pair is unselectable; an x landing exactly on a boundary goes to the correct left-closed owner — the `side='left'` variant WOULD have been buggy). The sole corruption channel is the trailing edge: when floating-point pushes the post-subtraction x to ≥ cum[-1], `k = min(k, len−1)` selects the LAST row-major pair even if its weight is exactly 0 (possible at κ ≥ 1 whenever that pair is inherited) — probability ~2⁻⁵³ per interact draw; zero realized occurrences (byte-identity, plus the dot census contains no dot-1.0 interact and the p=0 webs contain no non-seed pair). Measure-ε defect class; a comment or assert suffices.
**n6** — C2's label says "per-cell fallbacks + minima disclosed" but prints one GLOBAL minimum (1.3860; floor margin 0.336 — and this review's per-cell audit finds no floor or ceiling adjacency anywhere, per-seed max 5.379 vs the 7.9 guard D32B ran and D32C silently dropped from its completeness gate); unlike D32B's B2 it also omits "all other cells 0" when suppressing zero-fallback cells. Label-width touches; restore the ceiling check.

## SURVIVES ATTACK
- **The demotion itself (the round's charter): the "lift" is starvation.** Rebuilt independently: the direction-blind matched-fr control curve explains 101–126% of every live cell's elevation; the p=0 cells are reproduced by pure-birth + a weighted seed pair (z ≤ 0.93); the realized fr of all six cells is predicted to ≤0.009 by two-line mean-field arithmetic from the pin; the residual at matched fr is all-negative (anti-lift). **No positive directional component exists at any tested cell.**
- **Byte-identity and the hash-salt class:** the committed .out (md5 8c08fa134707356be4148d5a848bb452) is byte-identical on this review's independent rerun at HEAD under a fresh random hash salt (python 3.8 salts per process, so with the coordinator's original run this spans two salts); static audit: no set iteration, no str-keyed dict iteration feeding output (`touch`/`last`/`data` are int/tuple-keyed; the card is consumed by direct key lookup only); tree clean before and after.
- **Every printed number:** all 14 rows' statistics, all six discovery dz (35.89/31.27/5.33/41.52/37.49/34.54), all six confirmation dz (36.50/28.13/31.01/41.16/38.15/38.66), the global min 1.3860, and C4's z = 1.15 reproduce to all printed decimals via the bit-identical grower + verbatim measure chain; the clean-room engine agrees to 2.75e-12 on 24 webs.
- **The grower vs the pin:** the 12-vertex construction is the exact icosahedron (dot spectrum exactly {±1, ±1/√5}; 12 distinct unit vertices); NBR is the exact 5-neighbor set at every vertex; weights w = ((1−u·u′)/2)^κ with λ = 1 as pinned; the κ=0 control's weights ≡ the D32B anchor cell's (the blind λ=1 rebuild reads 1.883±0.211 ≈ anchor 1.908±0.215 ≈ control 2.017±0.248); the seed formula matches §10 exactly; CONF_C appears only inside the fire branch — "touched only on a positive" is code-enforced — and the confirmation rows verifiably used 61260712+ through the same run_cell/grow/measure machinery at 12 seeds.
- **Cross-κ stream identity (the pinned CF3 design property):** verified — κ enters the weights only; at a shared seed the rng call-type sequence is identical across κ until the first acceptance decision the differing totals flip (immediately after which the webs diverge — which is why unpaired statistics are correct and C4 reports, rather than gates, the κ=0-vs-D32B comparison). One un-narrated second stream difference, for the record: D32B selects interact pairs by a fresh `integers` draw, D32C by searchsorted reuse of x — distributionally identical, stream-relevant only; C4's "different rng consumption via the direction draws" is incomplete attribution.
- **Seed hygiene:** discovery 60260712+ (max realized offset 60,482,723) < confirmation 61260712+ — disjoint by construction; both disjoint from every prior family in v10/code (50260712 D32B including its arms; 20260712; 30260712); no collisions.
- **NO-BAND:** minimum joint entry threshold 11.2σ (supplied by this review); robust under every multiplicity convention tested.
- **Discipline:** §10 committed pre-receipt at b6d8a9e (06:26:22) and byte-unchanged to HEAD; the receipt committed pre-run at a0b1a29 (06:27:57) and byte-unchanged to HEAD; the .out lands only at 2dfa845 (07:12:13 — consistent with the measured 30-minute runtime), whose LEDGER/LOG entry carries the formal outcome AND the demotion together.

## GATES RE-VERIFIED
`python3 v10/code/d32c_s2_mechanism_test.py` at 2dfa845: **6/6 PASS, .out byte-identical (md5 8c08fa134707356be4148d5a848bb452), 30.3 minutes on the card's pinned environment (python 3.8.20, numpy 1.23.5) under a fresh hash salt; `git status` clean after.** Referee engines: 8/8 probe growths act-stream-identical to the receipt's grower; 252 receipt-seed growths re-collected with per-seed instrumentation; 24 webs through the independent clean-room engine (worst |Δ| = 2.75e-12); the six-cell matched-fr rebuild, two decomposition controls, and the interact-dot census all run green on referee seed families (80260712+/9x260712+, collision-free).

## FALSE-RESULT COUNT
**In the receipt (gates + printed numbers): 0.** In the record: **0 false sentences; 1 over-wide campaign sentence** (#149's "neither ... horizon-covariance ... produces manifold-like order — every axis lands on the one compression/starvation curve" — re-widens #147's named withdrawal and asserts a curve unity the data measurably breaks, M1); **1 material omission** (the reduction/unreachability under-diagnosis of the bar failure, M3); **1 imprecise range** ("4.1–5.2" vs the 4.018 floor, n1). The six CONFIRMED-LIFT prints are formally true records of a confounded bar, demoted in the same LEDGER entry that reports them — no falsity.

## OWED REPAIRS (exact list)
1. **(M1)** #150 re-scopes the campaign sentence: covariance → "bounded at the priced coupling" (per #147); radius/weight → "static-metric collar kernels at this grid"; "the one curve" → one starvation REGIME, carrying the measured anti-lift residual (−0.02..−0.55, all six cells below the direction-blind curve at matched fr) and the witness-midpoint separation (1.983 vs 1.03–1.5).
2. **(M2)** Receipt repairs + rerun: per-cell n_mid printed; the κ=1 p=½ N=512 strata (4.747±0.192 whole-order / 1.829±0.115 interval); [whole-order-MM] row tags; a C5 note that this cell's discovery/confirmation pair mixes estimator populations (9/12 vs 12/12 whole-order).
3. **(M3)** #150 forward-correction: the reduction (bulk S² content = the single scalar 0.276^κ; candidates = tree edges; anti-aligned bulk pairs nonexistent at r=1), the reachable-fr bound (max 0.356/0.133 at p=1 vs control 0.667 — matched fr unreachable at λ=1), the re-aimed design lesson (successors need λ-retuning ≈ 3.6/p, multi-hop/global scatter, r ≥ 2, or the CF4 aligned variant, which runs at healthy fr ≈ 0.65), and the CF1 re-scope (48-point refinement structurally moot at r=1).
4. **(m1)** CF2 delivered: per-cell interact-dot census (three-value histogram) or an al-semantics note (interact-weighted; zero-fill count at the p=0 cells).
5. **(m2)** C5 gated on `all_conf` in the fire path; the mono convention stated; honest tally taxonomy (substantive-capable = C1/C2).
6. **(m3)** The zmin = 11.2σ line carried in the .out, with the N=256 MM-band-passing note (NO-BAND there decided by chain_c/ratio3).
7. **(m4)** The fr/al window note (D32B n1 convention) + web-fr at the p=0 cells.
8. **(n1–n6)** Range wording "4.0–5.2" (or cite the stratum); the exact /7 point 3.55; the N=512-only bar convention stated; hoist the confirmation control; clamp comment/assert; C2 minima-label + "all other cells 0" wording + restore the ceiling guard.

## SUMMARY JUDGMENT
The receipt's computational content is beyond reproach — byte-identical rerun, every number reproduced two independent ways, seed and confirmation discipline code-enforced — and the round's central question resolves FOR the coordinator on referee-built evidence he did not have: the matched-fr control confirms the entire lift is interaction starvation, and the only thing direction adds at matched fr is a small effect of the OPPOSITE sign, carried by the seed pair, not by sphere geometry. Demoting a fired pre-registered bar, with the formal outcome preserved and the interpretation handed straight to a hostile round, is forward-correction discipline working exactly as this corpus intends; the asymmetry worry (would a NO-LIFT have been scrutinized as hard?) is answered by the demotion running against the coordinator's own positive. What earns MAJOR-REVISION is the surrounding record: the authoritative map re-asserts a covariance width its own #147 withdrew; the starved regime quietly turned the midpoint column into undisclosed subset means and one cell into an estimator mixture — with "machinery verbatim" false in the one dropped field that would have said so; and the honest verdict's diagnosis stops short of what the arithmetic shows: this family could never test its own hypothesis at healthy interaction fraction, its bulk S² content is one scalar, and the pinned design lesson cannot be executed inside it. The physics all survives at correct scope; the record must again say why at correct scope — and, as in D32B, most of it was one dropped disclosure field away from saying itself.
---

# DELTA-VERIFICATION PASS (same referee) — repairs at 172df38 — 2026-07-13

**VERDICT: NOT DELTA-CLEAN — 2 MINOR residues inside applied repairs / 3 round-1 MINORs SILENTLY DROPPED / 2 new NIT + nit dispositions owed (→ #151). All three MAJOR repairs are genuinely applied at their core and none is weakened; the delta rerun is GREEN 6/6 with the committed .out byte-identical at HEAD (md5 8543351da19df7c1515ab6cddf961b8b, 30.3 min, fresh hash salt, tree clean); the numeric content of the .out is UNCHANGED from round 1 to the digit (diff-verified: the repair touched only the disclosure surface — header note, n_mid column, mixture flags, the C3 zmin detail, the C5 mixnotes). Zero new false numbers. The blockers are wording/disclosure-class and convert to DELTA-CLEAN on a single #151 entry — no further receipt rerun needed.**

**Repairs verified one-by-one (the charter's (1)–(3)):**
**(M1) APPLIED WITH ONE RESIDUE.** #150 restates the campaign verdict with the covariance leg correctly bounded ("BOUNDED ONLY AT THE PRICED COUPLING (#147)") and the curve claim correctly demoted ("the compression relation is APPROXIMATE (the matched-fr residuals are measurably nonzero), not one exact curve"), and carries the anti-lift finding, the 101–126% accounting, the mean-field law, and the bulk-scalar reduction — all quoted correctly against this review's values. Residue (Δ-m2 below): the radius/weight legs are restated as "neither locality radius, nor interaction weight ... produces manifold-band order" WITHOUT #147's pinned family scope ("static-metric collar kernels at this grid") — the one scope qualifier from owed-repair 1 that did not land; and the directional leg dropped #149's "(this)" deixis ("anti-aligned directional structure" — the sign scope survives, the family scope is context-only).
**(M2) APPLIED AT CORE WITH RESIDUES.** n_mid is restored in `stats()` (the D32B field, with the round-1 attribution comment) and printed per cell — the values match this review's instrumented recount at all 14 rows (7/12, 11/12, 5/12, 8/12, 4/12, 9/12 at the starved cells; 12/12 elsewhere); the map header carries the honest subset-mean semantics; mixture flags (*) mark every non-interval-pure cell; C5 carries mixture notes. Residues (Δ-m1 below): the k=1,p=½ N=512 strata (4.747±0.192 whole-order / 1.829±0.115 interval) are still printed nowhere; the C5 mixnote fires indiscriminately on all six pairs with a hedge ("may mix"), and the confirmation-side composition is unprinted — so the ONE pair that actually mixed (discovery 9/12 vs confirmation 12/12 whole-order, |I|max 57) is not identifiable from the .out alone; and the header's "cells with fb > 0 or n_mid < 12 are estimator MIXTURES" over-classifies the uniformly-whole-order cells (Δ-n1). #150's "estimator-mixing disclosed" is generous at exactly this width.
**(M3) VERIFIED IN FULL.** #150 carries foreseeability ("FORESEEABLE pre-run"), the unreachability bound ("saturates at 0.356/0.133 at p = 1 vs the control's 0.667" — this review's numbers), the in-family-unexecutable design-lesson consequence, and names the CF4 aligned variant as the executable successor. Nothing over-claims: the reduction is quoted at bulk scope ("cannot express S² geometry in bulk beyond the scalar 0.276^κ") with the seed-pair channel named.
**(2) zmin: CONFIRMED.** The receipt-carried 11.2σ matches this review's supplied value (11.15σ exact, control N=256 × d=2, chain leg deciding) under the same convention (min over cells × d of the max leg-z, cell means, card-v2.2 bands), recomputed independently from the instrumented per-seed data. The repair went one step beyond the owed item: C3 now GATES on zmin > 3.5 (ok3), making C3 genuinely substantive — its tally tag is now true. Inherited note (Δ-n2): the gate hard-codes NO-BAND as the passing outcome (the D32B B5 pattern, blessed there) — legitimate as a regression freeze on a terminal receipt whose verdict is adjudicated, but any fresh family reusing this code must demote it to a report-gate, since band ENTRY is pinned as a valid result.
**(3) Byte-identity: HOLDS.** Independent rerun at 172df38: 6/6 PASS, byte-identical (md5 8543351da19df7c1515ab6cddf961b8b), 30.3 minutes on the pinned environment under a fresh hash salt; `git status` clean after; this review's round-1 report is committed byte-unmodified at 6e086bd.

**(4) The m/n dispositions — three MINORs silently dropped, five nits undispositioned:**
- **m1 (CF2 distribution/al-semantics): SILENTLY DROPPED** — no dot census, no al-semantics note; C6 still claims "CF2 in-table (mean-align column)". The mechanism evidence lives in the review + #150, but the receipt's CF2 delivery remains the round-1 gap.
- **m2 (C5 vacuous gate / mono convention / tally taxonomy): SILENTLY DROPPED** — `all_conf` is still computed and never consumed (both C5 branches still pass literal True; an UNREPLICATED outcome would still print 6/6), the mono convention is still unstated, and the tally still tags C5 [substantive]. Fourth recurrence of the taxonomy defect; the repair even ADDED detail text to C5 while leaving the gate vacuous.
- **m4 (fr/al window note, web-fr at p=0 cells): SILENTLY DROPPED** — no window note anywhere; the fr column still prints 3N+64-window values un-annotated (web values 2–3× larger at the p=0 cells).
- **n1: MOOT** (#150 does not re-quote the "4.1–5.2" range; owed only if ever re-quoted). **n2, n3, n4, n5, n6: not applied, not dispositioned** (exact /7 point 3.55; the N=512-only bar convention; the 6× confirmation-control recompute; the clamp comment; the C2 minima-label + "all other cells 0" wording + the dropped ceiling guard). All remain consequence-free this run.

**(5) #150's wording width overall: correct except the two Δ-m2 clauses.** Every number in #150 traces to this review's round-1 values and reproduces (0.276^κ law, 0.356/0.133 bound, 101–126%, Stouffer ≈ −4.8, 4/12 subset floor, dz 5.33 vs 31.01, zmin 11.2σ); the covariance/curve/anti-lift restatements are at licensed width; the M2 sentence's "disclosed" is generous per Δ-m1; the radius/weight clause is the one remaining over-width.

**New findings:**
**Δ-m1 — The M2 repair under-delivers its disclosure leg:** strata absent; conf-side composition absent; the indiscriminate C5 mixnote makes the one true mixed pair unidentifiable from the .out. Fix (either): a #151 line carrying the strata (4.747±0.192 / 1.829±0.115), the confirmation-side composition (12/12 whole-order, |I|max 57), and the honest flag semantics — or one more receipt touch. The #151 route suffices; the numbers are review-carried.
**Δ-m2 — The M1 restatement's radius/weight legs drop the #147-pinned static-metric-collar scope** (and the directional leg's "(this)" deixis). Fix: one qualifier clause at #151.
**Δ-n1 — The map header's "are estimator MIXTURES" over-classifies** the uniformly-whole-order flagged cells (e.g., k=1 p=¼ both N: fb 12/12 AND n_mid 12/12 — no leg mixes); conservative direction, disambiguating columns printed; honest wording: "* = not interval-pure; the sole MM mixture is k=1,p=½,512".
**Δ-n2 — C3's new gate freezes NO-BAND as the passing outcome** (inherited D32B B5 pattern; fine on this terminal receipt, demote for any fresh family).

**CONVERSION CONDITION (no further receipt rerun needed — all items are wording/disclosure; zero numbers move; byte-identity stands):** a #151 entry carrying (i) the Δ-m1 disclosure line (strata + conf-side composition + flag semantics), (ii) the Δ-m2 scope qualifiers (static-metric collar grid on radius/weight; the family deixis on the directional leg), and (iii) named dispositions — applied, deferred-with-reason, or won't-fix — for m1, m2, m4 and n2–n6 (this referee accepts deferral of all of them on a terminal receipt; what it does not accept is silence). **On that entry this pass converts to DELTA-CLEAN, D32C closes terminal, and the campaign record + paper 20 can open.** The physics content needs nothing further: the starvation confound, the anti-lift residual, the bulk-scalar reduction, NO-BAND at 11.2σ, and the aligned-variant successor are all verified, on the record, and stable across both reruns.
