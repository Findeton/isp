# D34e closing hostile delta — predictive probability and profinite stream

**Frozen target:** commit `6e6676bc46f377139c91d76d28f9f0fa0b7f9074`  
**Compared against:**
`d34e-round2-predictive-profinite-hostile-delta.md`  
**Verdict:** **CLOSING DELTA CLEAN — 0 blockers / 0 majors / 0 minors / 0
nits.**

The second repair closes both findings from this stream.  Counter-bearing
non-silent marks now represent the declared durable output, silent neighbor
births are internal `None` transitions, and the finite object is honestly named
a strong boundary-transition bisimulation stress test rather than the
canonical weak/timed predictive quotient.  The exact registered counts are
`(111,111,111)`, the B1 horizon split is executable, and no same-signature/
different-declared-output witness survives.  The result is terminal for this
stream at the stated sufficient-carrier/profinite ceiling.  It does not claim
minimality, the weak timed quotient, a v9 posterior bridge, or an intrinsic
quantum boundary.

## 1. Frozen artifact and byte reproduction

The frozen artifact hashes reproduce as:

```text
code   e66490560f7c38af746b6fea144a4356dfdb3630205eab9f46723ed8c830bff8
stdout 3d12f6191883ee3790c78498bae4bb1971144765341a354df587d33188f54498
note   2c6bed036361ce6bea4b671691de77886742acb552709bc885a69e6950cb2120
```

Fresh executions under `PYTHONHASHSEED=83,104729,271828` exit zero and agree
byte for byte with the committed output.  The receipt prints:

```text
gates                                      13/13
reachable levels                          1,6,40,304,2576
cumulative registered full states         2927
coarse strong-lumpability collisions      2816
registered strong classes H=1,2,3         111,111,111
synthetic strong classes H=1,2,3          106,110,110
B1                                         H1 equal / H2 split
B3 physical row updates                    35898
disjoint construction swaps               120276
full regional compositions                159734
malformed compositions rejected           6/6
full-ancestry moving-tip interlopers       16/16
finite unmarked classes depth 3/4          4/10
```

The internal summary digest is

```text
a53fa0c18a5905f282cea4c283ec3061c049ad7378a00f624906c1d68091d701.
```

I also repeated the round-2 one-depth extension without executing the later
receipt gates.  Through global depth five the exact level counts remain

```text
(1,6,40,304,2576,23800), cumulative 26727.
```

Both coarse and role-labeled generator projections agree on all `26,727`
states.  There are `218` distinct scoped boundaries and the independently
recomputed strong class counts are `(218,218,218)`.

## 2. Round-2 major — closed

### 2.1 Counter-bearing observable marks

Every non-silent coarse mark now has the form

```text
kind:c(post-carrier):r(post-own):w(post-wire),
```

and the role-labeled marks additionally retain the licensed incident actor or
fresh-child role.  I parsed every non-silent projected row on all `2,927`
registered full states.  For the coarse kernel the parsed triple always equals

```text
(after carrier, after own count, after wire count),
```

and for the role-labeled kernel it always equals the corresponding fields of
the labeled successor.  Thus the round-2 witness with wire baselines `1` and
`3` now emits wire `2` versus `4` in its labels and cannot merge.

The exact coinductive class census on the `111` registered scoped states is

```text
H=1: 111, H=2: 111, H=3: 111.
```

I exhaustively grouped those states by their strong signatures at all three
horizons and searched each collision group for different declared non-silent
output kernels.  No witness exists; in fact every group is a singleton from
horizon one.  The same attack at the depth-five extension gives `218`
singletons.

### 2.2 Internal transitions and the exact noun

Neighbor births now carry the Python mark `None`, never an observable string
such as `tau`.  On every registered row a `None` transition changes the
boundary histogram/star and emits no A-wire record.  The signature still
distinguishes and recursively follows those transitions, exactly as a
**strong** CTMC boundary-transition bisimulation should.

Crucially, the output and note do not identify this strong refinement with the
canonical observed-process quotient.  They explicitly say that eliminating an
arbitrary number of hidden neighbor births to obtain the weak timed quotient is
open.  That is the correct scope: strong bisimulation is a sufficient-carrier
stress test, not a minimality theorem.  Branch L's role-bearing output kernel
and nominal transport are checked separately; no L class-count claim is
smuggled into the coarse `111` census.

This resolves the round-2 major without changing the underlying analytic B3
sufficiency theorem.

## 3. Round-2 minor — closed

The B1 pair is now an explicit conjunct of `e4_ok`, separate from the synthetic
class census:

```text
sig({2,3,6},1) = sig({2,4,4},1),
sig({2,3,6},2) != sig({2,4,4},2).
```

My independent probe reproduces `True/True`.  The note also states correctly
that the synthetic degree-`1..5` family does not contain the `{2,3,6}` member.
The former narration/receipt mismatch is gone.

## 4. Relative-time and count-stop all-future sufficiency

The elapsed-time convention remains coherent.  At a conditioning stop the
time origin is reset to zero; the future marked CTMC carries elapsed increments,
so histories related by a common construction-time translation are identified.
Exact absolute A-own and A-wire counters remain in B3 and in every non-silent
durable mark.

For arbitrary finite D34b configurations, only A's own rows, births by current
A-neighbors, and incoming interactions from current A-neighbors can change the
coarse boundary or its licensed output.  Their aggregate rows depend only on
the histogram/distributed-star carrier.  D34b's Yule theorem supplies
nonexplosion, hence unique all-future projected pure-jump laws.  The A-own and
A-wire counters are monotone coordinates of that projected strong-Markov CTMC,
so their finite hitting times inherit the stopped-process law.  The one printed
exponential is correctly described only as survival to the next full boundary
transition, not as the complete next-A-wire law.

The sufficient physical B3 therefore earns

```text
ALL-FUTURE GROWING-CARRIER PASS / POINTWISE
```

for C and L.  Its unbounded width is proved for this construction; neither a
bounded alternative no-go nor minimality is claimed.

## 5. E11 and the profinite ceiling

The finite diagram remains exact:

```text
(u3 o r4->3)_* mu4 = (u3)_* mu3,
```

with labeled committed-prefix truncation before marks are forgotten.  The
depth-three and depth-four path laws each normalize, their class counts remain
`4/10`, and the truncated depth-four pushforward equals the depth-three law.
There is still no canonical unmarked `4 -> 3` map asserted.

The note and scorecard correctly stop there.  They do not construct a
completed marked-history map, a covtree/profinite posterior, an adapted
posterior-sufficiency factorization, or a marked profinite topology.  The v9
posterior branch therefore remains `REFUSAL/UNDEFINED`, which is the honest
terminal disposition for D34e.

## 6. Dependent scorecard audit

E13 remains dependent on all twelve prior gates through `PASS == 12` and
`FAIL == 0`; E4 cannot silently fail while the terminal gate passes.  Its eight
outcome rows use the frozen first-applicable order, six overlap cases exercise
priority explicitly, and capacity is typed as

```text
BOUNDED_PROVED / UNBOUNDED_PROVED / UNKNOWN.
```

The scientific rows therefore remain correctly separated:

```text
C/L physical B3                 ALL-FUTURE GROWING-CARRIER PASS
F complete finite-radius C_r    NO EXACT REALIZATION IN DECLARED CLASS
F whole component B4            ALL-FUTURE GROWING-CARRIER PASS
                                 as a sufficient ceiling only
v9 posterior factor             REFUSAL/UNDEFINED
intrinsic quantum               REFUSAL/UNDEFINED
```

The output-mark repair changes no justified branch verdict.

## 7. Closing ceiling

This stream accepts the following and no stronger statement:

> For the chosen static D34b law and frozen passive relative-time queries, a
> distributed radius-one B3 star is an exact recursively updating,
> construction-covariant, composable and all-future sufficient carrier for the
> coarse and role-labeled A-wire laws at fixed-time and local count stops.  Its
> width is unbounded.  The finite `111,111,111` census is a strong
> boundary-transition bisimulation stress test, not the canonical weak/timed
> predictive quotient.  B0 and B1 fail; full ancestry defeats every complete
> finite actor radius; the whole component is sufficient but not proved
> necessary; and no v9 posterior or intrinsic timed quantum boundary has been
> constructed.

**Final count: 0B / 0M / 0m / 0n.  Predictive/profinite stream terminal at the
stated ceiling.**
