# D34b repaired round 1 — independent measure/topology hostile review

**Target:** commit `59b0421`. **Verdict:** CORE RESULT ACCEPTED; MAJOR REVISION AT TOPOLOGY/PROJECTIVITY WIDTH. **Count:** 3 MAJOR / 5 MINOR / 3 NIT. **False results:** zero false printed numbers, zero gate flips, zero false core probability identities; three overwide/unsupported claim-layer statements.

The referee independently reconstructed the marked Harris process, the Yule nonexplosion route, the initiated ring/act cylinders, the passive-reception split, the orbit-sum law, disconnected-component coupling, and the killed-chain asymptotic. The core is sound: ideal independent rate-1 clocks and marks define a normalized timed process; the birth process is Yule; finite time contains finitely many events almost surely; each fixed-time timed law has an Ulam-coded typed-DAG pushforward; disconnected components factor; and `q/k` representative weights are summed on quotient classes.

## MAJOR

1. **The profinite shadow is not constructed.** No explicit inverse system of finite discrete observation spaces or compatible restriction maps exists. Fixed-time history spaces have unbounded event count; after timestamps/global serialization are erased an untimed DAG has no canonical “last event.” Remove the present-tense claim or construct gauge-safe finite stem/cylinder quotients.
2. **Timed projectivity and untimed pushforward were conflated.** The source/timed laws are projective under time restriction. Each T has an untimed DAG pushforward, but the untimed T2 object does not intrinsically reveal its T1 subhistory after time is erased. Name these objects separately.
3. **Nonexplosion and persistence conclusions were true but proof carriage incomplete.** Divergence of `sum 1/n` alone does not prove the random holding-time sum diverges almost surely. Add the summable-variance/centered-series argument (and finite rings between births). Add the conditional-kind identity plus martingale SLLN for almost-sure densities 1/4,1/4,1/2, scoped to the live-neighbor invariant.

## MINOR / NIT

- State relabeling equivariance at theorem width; the fixed-seed PRF implementation is label-keyed.
- Declare the measurable codomain as Ulam-coded locally finite typed DAGs; arbitrary graph-isomorphism quotient is separate.
- The `1/32+1/32` split is a coarse first-ring relative-order cylinder, not necessarily an exact two-global-event DAG.
- Scope the D34a correction to normalized quotient masses at fixed embedded depth; timed D34b supplies path architecture/projectivity.
- Carry the analytic Yule-martingale proof of `S_n ~ 16 k0(k0-1)n^-2`; the table is a check, not proof.
- Remove the duplicate inequality, disclose the inert sealed R suppression in the oracle, and call the 256-bit actor tape a high-precision deterministic reference.

## SURVIVES

All core probabilities and exact fractions; the marked timed process; Yule and total-ring nonexplosion; alpha binding; initiated/physical algebra split; passive reception semantics; orbit sums; remote locality; the n^-2 constant; and the D34a unequal-representative correction.

**Stamped terminal wording after repair:** D34b CLASSICAL MEASURE PASS — timed Harris law and Ulam-coded typed-DAG pushforward established; profinite physical-history architecture still open.
