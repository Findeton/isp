# Paper 21 round 5 — predictive/profinite target closing delta

**Frozen target:** commit `befb10c`, restricted to the final repairs in section
10 and a fresh regression pass over that complete target section in
`relativistic-isp-v10-paper21-local-generators-do-not-imply-local-memory.md`.

**Delta base:** commit `aa1a258`, audited in
`paper21-round4-predictive-profinite-target-final-delta.md`.

**Verdict:** **DELTA-CLEAN / UNQUALIFIED CLOSURE — 0 BLOCKER / 0 MAJOR / 0
MINOR / 0 NIT.**

Both round-4 findings are exactly repaired.  The complete D34e target now
distinguishes almost-sure from pointwise screening on one declared branch
domain and distinguishes a genuinely profinite marked carrier from an
arbitrary compact marked topology.  The fresh end-to-end pass finds no new
predictive, conditional-law, projective, profinite, quantum-typing or verdict
regression.  The accepted D34d scientific core remains unchanged.

I did not rerun the frozen executables.  The delta is prose-only and changes no
theorem already proved, matrix, probability, gate count, output or hash.

## 1. Round-4 m1: common full-measure set and pointwise scope — closed

The repaired controlled-law section now handles all relevant cases.

### 1.1 Past-law scope

Future-only interventions may share the supplied passive past law up to the
conditioning stop.  If an intervention occurs earlier, the paper now requires
the appropriate controlled past law or a declared dominating measure.  It no
longer applies passive `mu` silently to histories generated under a different
policy.

### 1.2 Simultaneous almost-sure factorization

The almost-sure branch freezes one common `mu`-full reachable set `H_0` and
requires

```text
K^I_(tau,A)(h,.) = Kbar^I_(tau,A)(B_A(h),.)
    for every h in H_0 and every licensed I.
```

This is the simultaneous statement needed before boundary equality can imply
predictive equivalence for all licensed instruments.  The paper correctly
records how the common domain can be earned:

- finite/countable determining families permit intersection of their
  full-measure sets;
- uncountable families require a jointly regular version or a determining
  subfamily plus an extension theorem.

The passive branch omits `I` and uses the same declared-domain discipline.

### 1.3 Pointwise alternative and verdict scope

The alternative branch supplies generator/controlled kernels on every legal
reachable state.  Screening, equivalence and minimality are then pointwise on
that domain rather than inferred from an a.s. version.

Section 10.4 now states screening separately on `H_0` for an `A.S.` theorem and
on all legal reachable histories for a `POINTWISE` theorem.  Section 10.8
freezes the full branch `(mu,A,Q,I,S,C)` and requires every theorem/verdict to
carry `A.S.` or `POINTWISE`.  Its all-future pass rows quantify only over the
corresponding declared branch domain.  Thus no setwise or all-reachable claim
is extracted from incompatible instrument-specific null sets.

**Disposition:** round-4 m1 is closed without remainder.

## 2. Round-4 m2: marked profinite topology — closed

Section 10.5 now says exactly what topology permits.

- Continuous or unbounded marks require explicit finite observable quotients
  or a separately declared marked topology.
- A carrier may be called profinite only when its marked topology is itself an
  inverse limit of finite discrete quotients, equivalently when the required
  Stone/light-profinite properties are established.
- A general compact marked topology defines a different compact bridge and is
  explicitly **not** automatically profinite.

The repair therefore excludes both prior counterexamples: connected compact
mark spaces such as `[0,1]` are not mislabeled profinite, and ordinary
unbounded-real mark spaces are not mislabeled compact.

The surrounding bridge is also strengthened rather than widened.  The
mark-forgetting map `u` is now a **candidate** that must pass four gates before
use: the forgotten order must lie in v9's past-finite completed-causet domain;
`u` must respect the event/Ulam relabeling gauge; it must be Borel under the
declared codings; and the conditional completion law must admit the stated
pushforward.  Failure refuses only the v9 bridge branch, not passive predictive
analysis.

**Disposition:** round-4 m2 is closed without remainder.

## 3. Fresh predictive and measurable-factorization regression pass

The section still keeps passive and operational inputs separate:

- passive prediction uses a chosen regular conditional kernel under `mu` on a
  measure-determining future-event class;
- deliberate interventions use a supplied causal family `{K^I}`;
- the instrument family discloses adaptive choices and entangled ancillas;
- no D34b–D34c process object is manufactured by notation.

The predictive quotient remains law-, region-, query-, instrument-, stopping-
and version-relative.  Carrier sufficiency is measurable kernel
factorization; boundary equality implies predictive equality on the declared
branch domain; the converse removes redundancy only on the realized support
and up to measurable relabeling/gauge.  The implication directions remain
correct.

No arbitrary real encoding is counted as a finite record.  Predictive omission
of a sealed record remains distinct from physical erasure/compression, which
would separately require NSE/isometric preservation.  Fixed graph radius is
not treated as bounded capacity when actor degree is unbounded.  D5 typed
contraction remains conditional on a supplied/audited factor cover and
ownership ledger.

**Fresh disposition:** clean.

## 4. Fresh finite-domain and recursive-closure regression pass

The exact audit domain remains

```text
D(N,H,Q,I,S),
```

with current-past size, future depth/time, query family, intervention family
and stopping algebra all frozen.  Enumeration earns only cell-level
sufficiency.  Nestedness is required before predictive partitions are called
refinements.

Promotion remains two-theorem work:

1. arbitrary current size requires induction/closure across every reachable
   size;
2. arbitrary future horizon requires compatible limiting kernels or proved
   stabilization.

The recursive update gate now includes elapsed time, survival/no-event data,
renewal-age flow, passive/cross-boundary inputs and externally initiated
events.  Embedded-chain variants must say which waiting-time data they erase.
Construction covariance is restricted to one stopping scope and disjoint
incomparable record-DAG updates.  No finite search or update formula is
silently promoted to all-future screening.

The revised outcome table additionally freezes the physical carrier class
`C`, separates whole-component necessity from genuinely global necessity, and
reserves universal nonrealizability language for a theorem quantifying over
the full admitted carrier class.  Candidate-list failure remains only a
candidate-class obstruction.

**Fresh disposition:** clean.

## 5. Fresh online/stem-spectrum regression pass

The online past is still not assigned a future-known v9 spectrum point.  After
the candidate map gates above, the declared adapted object is the posterior

```text
nu_tau(h)     = (phi o u)_* mu(completion in . | F_tau)(h),
nu_(tau,n)(h) = (pi_n)_* nu_tau(h).
```

The paper continues to separate:

1. completed observables factoring measurably through `phi`;
2. online regional prediction factoring through the posterior `nu_tau`;
3. an adapted online boundary acquiring its own compatible finite/profinite
   realization.

A new predictive inverse limit is not identified with v9 `X_stem` without
commuting finite-level maps, compatibility and adaptedness.  Borel/a.s.
factorization is kept distinct from continuity, and continuity is not promoted
to exact factorization through a finite level.  Rogue fibers remain a concern
for completed-history lifts rather than an online state variable.

No existence, lift, continuity, projectivity or profinite-predictive theorem is
claimed.

**Fresh disposition:** clean.

## 6. Fresh quantum-typing regression pass

The additional quantum repairs are consistent with the predictive/profinite
target:

- `d_carrier` is minimized over record-native retained Hilbert carriers under
  ownership constraints;
- `d_op` is the real/complex span dimension of conditional operational
  boundary functionals;
- `chi_cut` is the operator-Schmidt rank of the comb Choi operator across one
  named tensor bipartition;
- density rank, Choi/Kraus rank and unrestricted ancilla-memory dimension
  remain separate.

Strong positivity remains a property of each history functional `D_n`.
Restriction maps satisfy exact consistency; comb Choi operators satisfy
positivity and causal trace/link constraints.  Deterministic channels are CP,
trace preserving and causal; instrument branches are CP and trace
nonincreasing with a trace-preserving causal sum; normalized conditional
updates are not misrepresented as linear channels.

The finite `P,E` witness remains only a negative control.  It neither
constructs nor proves the necessity of a SHARD process-memory boundary.

**Fresh disposition:** clean.

## 7. Exact terminal disposition

Commit `befb10c` applies both requested repairs exactly and introduces no fresh
error.  Section 10 is now a rigorously typed investigation target, not a claim
that the predictive boundary, marked profinite bridge or quantum process law
has already been constructed.

**DELTA-CLEAN / UNQUALIFIED CLOSURE — 0B / 0M / 0m / 0n.**  No further paper,
proof, executable, receipt or numerical repair is required by this stream.
