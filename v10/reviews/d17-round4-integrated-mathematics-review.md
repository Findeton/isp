# D17 focused round-4 integrated mathematics review

**Date:** 2026-07-12  
**Verdict:** **PASS AT THE INTEGRATED FINITE CONDITIONAL SCOPE**

## Decision

The round-three typed/gauge and operator-completeness openings are closed. The
growth grammar now keys declared edges by canonical typed poset data, complete
owner tuples, and the full collar memory/carrier type. Consistently relabeled
edges admit, while forged owner, collar, memory and carrier variants reject.

The causal-node collars are no longer parallel metadata: their depth-one,
depth-two and depth-three carriers equal the actual D14 network targets, and
later nodes preserve that sealed carrier. The complete record network satisfies
`V^dagger V=I`, and the reset Kraus family satisfies exact CPTP completeness.

All 38 checks reproduce. The fixed-action nonselection theorem is unchanged:
the grammar, record commit and kernels remain supplied, and multiple kernels
survive one fixed action.

## Reproduction

```text
source    1e934d2630aaae6ece9670b10132ab4f1c3f87d92c17c148ac699fd2209c0640
packet    397f35deafa549731b43c01886ecade5f8981651d13aca222b3a6430dcd84624
receipt   3ec1e1686e0463fe173af10948c3a1d575c8a93eb0dba602924ab0739c32501a
ledger    eed026feaaa7deebe92b95c38baf38093e973b482858ed5ce95cac0de3e2f1c2
stdout    1c520ddbfe0822a62c7e86ae7e80dc3305952bfb3a3aca66a5f28783242f18f4
checks    38/38 normal and optimized
```

Normal and optimized stdout are byte-identical.

## Canonical typed key

`canonical_typed_key` exhausts every relabeling, transports boundary ports,
flattens the relabeled order, and includes the correspondingly relabeled
element-owner tuple. The minimum therefore identifies presentation-equivalent
typed orders without discarding ownership. Declared edges additionally include
collar owner, carried memory and the complete ordered D14 carrier signature.

The exact controls demonstrate both directions: a consistently relabeled edge
is admitted; undeclared, foreign-owner, forged-collar, forged-memory and wrong-
carrier variants reject. This repairs the raw-relation-key bypass.

## Carrier propagation

The grammar computes the expected child carrier by appending `X`, `Y`, then
`Z` record/collar types at the first three depths and preserving the full
carrier thereafter. Exact identities

```text
node depth 1 carrier = net1.target
node depth 2 carrier = net2.target
node depth 3 carrier = net3.target
```

tie the causal tree directly to D14. Persistent record IDs and owners are part
of those carrier values, so a merely dimension-matching substitute cannot pass.

## Full operator identities

The record network is checked on its two basis branches and, more strongly,

```math
V^\dagger V=I_2.
```

Thus isometry is a full matrix theorem, not a two-output sample. For reset,
the two D14 Kraus morphisms obey

```math
K_0^\dagger K_0+K_1^\dagger K_1=I
```

on the entire depth-two carrier. Applying them before `Z` changes `000/101`
to `000/100` while preserving sealed earlier records.

## Nonselection and scope

The action is still evaluated on every actual causal node and gives opposite
phases on the size-four chain and diamond. Equal, positive-envelope and
inverse-orbit kernels remain projective and inequivalent under the same action,
grammar and carrier network. The groupoid ratio and D14 record ratio still
agree exactly.

Nothing in the repairs makes the action choose the extension grammar, kernel,
record commit, pointer basis or memory architecture. No continuum, geometry,
unit or `G` claim is added.

## Final verdict

**PASS AT THE INTEGRATED FINITE CONDITIONAL SCOPE.** Canonical typed relabeling,
exact D14 carrier propagation, full isometry and CPTP completeness all pass on
the frozen bytes. The nonselection theorem and its conditional ceiling remain
mathematically intact.
