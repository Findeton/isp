# D17 focused round-3 integrated mathematics review

**Date:** 2026-07-11  
**Verdict:** **PASS AT THE INTEGRATED FINITE CONDITIONAL SCOPE**

## Decision

The integrated executable closes the causal-tower opening. Every probability
cylinder is now an actual `CausalOrder`; every edge is an induced one-element
extension admitted by one supplied owned grammar; the fixed interval action is
evaluated on every node; and three positive kernels propagate through the same
causal tree and D14 record network.

The local record packet carries memory, seals `X,Y,Z`, emits collars, reproduces
the causal tower probabilities, and changes under a complete reset. Exact
groupoid/orbit counting agrees with the record probabilities. No mathematical
blocker was found.

## Reproduction

```text
source    6c12a6bab7edd24a530e294e6efd2f97484b67dc0f959b82f18b68a15695a422
packet    8791db408ace7751a89f4652eff202484aa867f2d54b6cc29beabcc93282e8aa
theorem   dd5356cec2e5742e5bba70e3faee1d46036956716543d92a872ec41ee7bf5bb0
receipt   adbc1c6a178f3cbe4d172a084351d0b927b3b67bfffc0c6db90eb70888111e60
stdout    663d456e8cc05c4137993fba0834084fee94ec46025ceddf23569d357d003b56
semantic  61aac50273a1cc01779b6bfea696fa6195f22bb4e0ce7cf8724da90a331e86bc
checks    32/32 normal and optimized
```

Normal and optimized output are byte-identical. D14, D16 and the prior D17
source hashes are pinned and reproduced.

## Actual causal tower

The graph contains one root and two nodes at every visible depth one through
six. The two initial branches are chain2 and antichain2; their next orders are
chain3 and `V3`; the size-four leaves are chain4 and a relabeled diamond whose
first three elements induce exactly `V3`. Later nodes add one maximal element.

The validator confirms every parent restriction equals the old order. Valid
but undeclared edges, nonowner requests and unentitled cross-owner joins
reject. The first branch bit is carried by the owned collar at every later
node. This is a genuine finite causal extension grammar, while its selection
remains supplied.

## Action and projectivity

One `N_0` action is evaluated on every node. Its two size-four leaf phases are
`-1` and `+1`. Equal, positive-envelope and inverse-orbit kernels are normalized
and projective through depth six. Every nonroot node has one declared child,
so deterministic continuation supplies the stated later-depth induction.

The three kernels remain inequivalent at depth six while action and grammar
are fixed. This proves kernel nonselection at the declared finite scope.

## Orbit/groupoid check

The chain and diamond have labeled orbit sizes `24/|Aut| = 24` and `12`.
Uniform labeled mass divided by `24` and normalized therefore gives exactly
`(2/3,1/3)`. The same ratio is independently obtained by applying amplitudes
`(-1,1/sqrt(2))` to the common D14 network and normalizing record masses.

Thus orbit weighting is not a detached table; it reaches the integrated
records. The convention remains supplied rather than action-selected.

## Records, memory and reset

The D14 network uses one owner and sequentially commits protected `X`, fixed
`Y=0`, and memory-revealing `Z`. It reproduces:

```text
equal kernel       000:1/2, 101:1/2
positive envelope 000:9/25, 101:16/25
inverse orbit      000:2/3, 101:1/3 after normalization.
```

The visible conditionals are exactly one and zero. The two reset Kraus maps
erase the local memory before the final commit, changing the equal law to
`000:1/2,100:1/2`. Earlier sealed records survive, so this is a real deletion
control rather than a replacement table.

## Scope

The result proves integrated finite causal-extension/projective-record
compatibility and fixed-action kernel nonselection. It does not derive the
extension grammar, kernel, record commit or memory architecture from the
action, nor an infinite measure, BDG continuum, geometry, units or `G`.

## Final verdict

**PASS AT THE INTEGRATED FINITE CONDITIONAL SCOPE.** All 32 checks and frozen
hashes reproduce. The earlier actual-order, projectivity, orbit, local-memory
and reset openings are closed; the theorem ceiling remains appropriately
conditional.
