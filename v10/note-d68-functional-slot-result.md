# D68 — result: **under the faithful paper-29 reading, CONSISTENCY DOES NOT STRUCTURE COHERENCE — and THE FIRST DYNAMICAL DEMAND ELIMINATES IT.** The record demands, read as paper 29 states them, permit within-class coherence *everywhere* and price it *nowhere* (`cohdim = coh` at every depth, in every variant, and **measure-independently**). Impose one dynamical demand — that the coherence excess be generated by the closed law's own state space — and the coherence dimension is **zero at every truncation tried** (at the block bound, depths 2–5). **At closed scope the quantum layer cannot be both state-generated and coherent: superposition must either break state-generation or enter at transport scope / a different joint.**

**Status: ROUND-1 REVIEWED AND REPAIRED, 2026-07-27.**  An independent
hostile review — `v10/reviews/d68-round1-hostile-review.md`, a worker
with no prior context and no loyalty to the unit, its own family
enumerator, its own σ normal form, its own constraint rows, its own
elimination (pivoting on the largest column where this receipt pivots
on the smallest) and its own coherence-dimension identity — returned
**REVISE: 1 BLOCKER / 5 MAJOR / 7 MINOR / 4 NIT**, and reproduced
**every published number of the first version exactly**: the census,
the record-class census, the 36 states, the λ = 2 spectrum, `μ_Ẑ`'s
descent, the entire 32-row dimension table including every
antisymmetric column and the full-chain split, the geography counts,
the pinned witness to its minor, all five control responses, and the
receipt's own 25 PASS / 0 FAIL under `PYTHONHASHSEED` variation.  *The
arithmetic was flawless.*  It then **broke the deliverable's two
headline sentences** — by computing the C1 reading the unit had
bracketed but never evaluated, and by spending the ten lines the unit's
own residue 1 had named and declined.  §9 is the corrections section
and names what is **WITHDRAWN**, verbatim.  **Both of the first
version's headline sentences were false, and their replacements are the
better result.**

**THE FOUR ROUND-SUPPLIED COMPUTATIONS**, credited to the round,
rebuilt here with this unit's own driver and **gated like any other
object**:

| object | round-1 finding | what it does |
|---|---|---|
| **the FAITHFUL C1 reading** (`D̄(r,r') = δ_{rr'}·μ_Ẑ(r)`) | BLOCKER 1 | paper 29 §4.3's actual decoherence condition — the one the unit's two readings *bracket*.  Under it **nothing is forced**: `cohdim = coh` everywhere, the depth-2 F-II cell evaporates, and the "killed" entries are exhibited |
| **the state-generated ansatz** (`E = μμ·K(σ,σ')`) | MAJOR 5 | the first *dynamical* demand.  It costs **everything**: `cohdim = 0` at depths 2, 3, 4 and 5 (imposed on the block bound) |
| **the second measure** `ν(h) = Π 1/|menu|` | MAJOR 2 | shows the coefficient matrix never sees the measure — the whole dimension table is a fact about `canon` and the prefix map |
| **the parent-swap group** | MAJOR 4 | an exact symmetry of the one-step system, of order ≥ 512 at depth 3 against C4's 4, whose orbits *are* the permitted coherences |

Every number below is machine-produced by
`v10/code/d68_functional_slot_exact.py`, output
`v10/data/d68_functional_slot_exact.out` — **39 PASS / 0 FAIL, exit 0,
271.2 s wall clock, 1,270,890,496 bytes (1,212 MiB) peak resident set
size — both now PRINTED BY THE RECEIPT ITSELF (round-1 MINOR 4)** (run
from the repo root; 25 gates before the repair, and **fourteen of the
39 are new**), byte-identical under `PYTHONHASHSEED` variation
(default / 7, verified by diff, the only differences being the
per-configuration `sec` column, the `t = …` lines and the two resource
figures).  Pin
`note-d68-functional-slot-pin.md` (STRICT, frozen and committed before
any computation ran); its question §1, gates F0–F6, pre-registered
outcomes F-I…F-IV and scope §4 are honoured as written and the pin's
declared lean — *none* — is respected.

Parents: paper 29 §3/§3.1 (refined cylinders, the pushforward
`(q_*μ)(ω̄) = Σ_{q(ω)=ω̄} μ(ω)`), §4.1–§4.3 (class operators, the Gram
functional `D(α,β) = ⟨v_α, v_β⟩`, strong positivity, **the five
durable-record hypotheses — number 2 is the one this repair is about**),
§5 (the D34c lesson); D65 (DC3(2): the generated line has **no**
functional level; `μ_Ẑ` genuinely descends; the repair-cone method);
D62/D61 (the closed law, and the state space the dynamical demand is
written on); d44a and d42b3 (the committed layers, text-sliced and
AST-gated, never re-implemented).

**Scope, non-negotiable and load-bearing in every sentence below:
TWO-ACTOR, DELIVERY-FREE, d42a, CLOSED SCOPE, the truncations D = 2…6
of the exhaustive depth-6 family** (census `[1, 6, 32, 176, 976, 5280,
27904]`, 36 σ states, 176 transition keys, **5,548 record classes**
`[1, 6, 23, 84, 313, 1138, 3983]`), **one record functor** (`canon`),
**one dynamical ansatz**.  Nothing here transfers to the identified
(action-line) click law: **no member computed here is claimed to BE the
action line's functional.**

---

## 1. What was actually computed

Over the depth-`D` history layer, the set of forms `D(h,h')` with

* **C1**, in **three** readings, of which the middle one is now the
  headline and the pin's two are its bounds:
  * **(medium — FAITHFUL, and the only one that is a stated condition
    of the parent paper)** paper 29 §4.3's load-bearing hypothesis 2 is
    *decoherence of the queried record algebra*, and §3.1 supplies the
    pushforward for the atom masses.  Written on the fine layer that
    pair of demands is exactly

    ```
        D̄(r,r')  =  Σ_{h∈r, h'∈r'} D(h,h')  =  δ_{rr'} · μ_Ẑ(r)
    ```

    — the coarse-grained functional on the record algebra is **diagonal**
    and its diagonal is the record measure.  This is medium decoherence
    in the ordinary consistent-histories sense, and it is the condition
    that licenses a scalar probability for a record class.
  * **(sum — the WEAKER bound)** `Σ_{h,h'∈r} D(h,h') = μ_Ẑ(r)` and
    nothing else: the `r = r'` half of the faithful condition.  It
    **drops decoherence entirely** — the queried algebra is never asked
    to decohere, so by §4.3's own bookkeeping no scalar record
    probability is licensed for the object it constrains, and its
    `10⁸`-dimensional solution spaces are exactly what a no-decoherence
    demand should produce.
  * **(block — the STRICTER bound)** `D(h,h') = 0` for **every fine
    pair** with `record(h) ≠ record(h')`, plus block traces `μ_Ẑ(r)`.
    It implies the faithful condition and is far stronger: it
    **forbids, by hand and one level down, the cancellations that
    coarse decoherence explicitly permits.**
* **C2** positive semidefiniteness (paper 29 §4.2's strong positivity).
* **C3**, operationalised and printed: `R^(d)(g,g') = Σ_{a ⊐ g, b ⊐ g'}
  D(a,b)`, and C3 demands that `R^(D−1)` satisfy **the same
  convention's C1** at depth `D−1`.  **Scope, and it is a repair:**
  paper 29 §4.2 asserts only that additive restrictions are *stable*
  ("future exhaustive alternatives do not change the earlier restricted
  functional"); the demand that a restricted functional **reproduce a
  lower-depth record measure** is *this unit's construction*, not paper
  29's.  It is defensible and it is gated (F1(a)/F1(b)) — and it is
  labelled a construction wherever it is used.
* **C4** equivariance under the **label-map group**, computed rather
  than assumed (F0(i)): the four maps (actor swap) × (value flip).
  **Scope, and it is a repair:** this is *not* the constraint system's
  symmetry group.  The relevant group for an equivariance demand is the
  automorphism group of the constraint data, and that group is far
  larger — see §5.2.  "C4 on" is a column, not a canonical quotient.
* **C5 — THE DYNAMICAL DEMAND (new, §4).**  `D = diag(μ_Ẑ) + E` with
  `E(h,h') = μ(h)μ(h')·K(σ(h), σ(h'))` for a symmetric kernel `K` on
  the 36 σ states, zero on the diagonal: *the coherence excess is
  generated by the closed law*.  The classical member is `K = 0`, so
  the demand excludes nothing this unit exhibits as existence.

Headline space real symmetric over **Q**; the **Hermitian extension**
(`D = S + iA`) is carried as a separate column throughout.  Dimensions
are `variables − EXACT rational rank` by sparse elimination over **Q**.
Positivity is certified by exact strict diagonal dominance and exact
2×2 determinants — never a float (F0(c) is an AST gate on this
receipt's own syntax tree).

### 1.1 The fact that gives C3 its content — and then leaves something over

**The record functor does not commute with taking prefixes** (F1(b)).
`canon` identifies two histories when they are two serialisations of
one *labelled event DAG*; two such serialisations have **different**
prefixes, so the depth-`D` record partition is not the pullback of the
depth-`(D−1)` one and C1 at depth `D` carries no C1 at depth `D−1`.
The splitting is **partial**.  Also gated: **no two siblings share a
record** (F1(c), zero exceptions).

---

## 2. F2/F3 — existence, the dimension, and what the dimension is a fact about

**F-I and F-IV are both EXCLUDED, and by the same one-line fact.**
The **classical diagonal functional** `D = diag(μ_Ẑ(h))` satisfies C1
in all three readings, C2, C3 in both readings and C4 — in every
configuration, verified by exact residual against every row — and it is
**positive definite** (smallest diagonal entry `1/16777216` at depth 6).
**C2 removes no dimension**, because the classical member is *interior*
(F3(a)).

**And existence is weaker than the first version claimed (F9).**  Every
coefficient in every row is a positive integer; the measure appears
**only** on the right-hand side.  Rebuilt from scratch with a second
measure `ν(h) = Π_k 1/|menu_k|` — strictly positive, exactly
cut-consistent, a probability measure on every cut, **not**
record-constant (4,156 of 5,548 classes carry two or more `ν` values),
differing from `μ_Ẑ` on 34,374 of 34,375 histories — the **coefficient
matrix is identical entry for entry** in every configuration while the
right-hand side moves, and `diag(ν)` satisfies every row.  So `diag(w)`
works for **any** strictly positive cut-consistent weight `w`.

> **Consequence, and it is a de-licensing.**  Rank, dimension,
> coherence dimension, the singleton census and every antisymmetric
> rank are functions of exactly three combinatorial objects — the
> truncated history tree, `canon`'s partition at depths `D` and `D−1`,
> and the prefix map.  D49's `λ = 2` completion and D65's descent are
> load-bearing for **no number in §2 or §3**.  The honest title of the
> dimension table is *what `canon` and the prefix map permit*, not
> *what the generated law admits*.

### 2.1 The faithful reading — the headline table

C4 off / C4 on, C3 one-step / full-chain.  `arank` is the constraint
rank on the antisymmetric block.

| D | C4 | C3 | vars | rows | rank | **dim** | coh | **cohdim** | asym | arank | acohdim |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2 | off | one | 528 | 297 | 296 | **232** | 9 | **9** | 496 | 268 | 9 |
| 2 | off | full | 528 | 298 | 296 | **232** | 9 | **9** | 496 | 268 | 9 |
| 2 | on | one | 146 | 297 | 88 | **58** | 4 | **4** | 114 | 60 | 1 |
| 2 | on | full | 146 | 298 | 88 | **58** | 4 | **4** | 114 | 60 | 1 |
| 3 | off | one | 15,576 | 3,846 | 3,845 | **11,731** | 134 | **134** | 15,400 | 3,739 | 134 |
| 3 | off | full | 15,576 | 3,868 | 3,865 | **11,711** | 134 | **134** | 15,400 | 3,754 | 134 |
| 3 | on | one | 3,968 | 3,846 | 1,006 | **2,962** | 35 | **35** | 3,792 | 900 | 35 |
| 3 | on | full | 3,968 | 3,868 | 1,013 | **2,955** | 35 | **35** | 3,792 | 902 | 35 |
| 4 | off | one | 476,776 | 52,711 | 52,666 | **424,110** | 1,491 | **1,491** | — | — | — |
| 4 | on | one | 119,592 | 52,711 | 13,329 | **106,263** | 390 | **390** | — | — | — |

**Every figure in the depth-2 and depth-3 rows reproduces the review's
own table exactly**, produced by two instruments that share only the
committed layer under test.  The depth-4 rows are new.

Beyond depth 4 the faithful system's row count is quadratic in the
record-class census (49,141 class-pair rows at depth 4, 648,091 at
depth 5), so it is measured there by an **exact structural identity**
instead of by elimination (F7(b)).  Every variable is an unordered pair
of histories; the C1 rows partition the variables by the pair's
**record pair** and the C3 rows partition them by the pair's
**parent-record pair**, so the coefficient matrix is the incidence
matrix of a bipartite graph whose vertices are the rows and whose edges
are the variables:

```
    rank  =  (#C1 rows) + (#C3 rows) − (#connected components)
```

exactly, and `dim(rowspace ∩ coherence-span)` is a second incidence
rank on the same components.  Integer arithmetic, no elimination, and
**cross-gated against the generic rational elimination at depths 2, 3
and 4, where both are computable — agreement everywhere.**

| D | vars | rows | components | rank | **dim** | coh | **cohdim** |
|---|---|---|---|---|---|---|---|
| 2 | 528 | 297 | 1 | 296 | **232** | 9 | **9** |
| 3 | 15,576 | 3,846 | 1 | 3,845 | **11,731** | 134 | **134** |
| 4 | 476,776 | 52,711 | 45 | 52,666 | **424,110** | 1,491 | **1,491** |
| 5 | 13,941,840 | 697,232 | 861 | 696,371 | **13,245,469** | 15,058 | **15,058** |

### 2.2 The two bounding readings — the first version's tables, unchanged

The pinned configuration (C4 on, C3 one-step):

| D | **sum**: vars / rank / **dim** | coh / **cohdim** | **block**: vars / rank / **dim** | coh / **cohdim** |
|---|---|---|---|---|
| 2 | 146 / 10 / **136** | 4 / **4** | 13 / 13 / **0** | 4 / **0** |
| 3 | 3,968 / 30 / **3,938** | 35 / **35** | 81 / 51 / **30** | 35 / **13** |
| 4 | 119,592 / 105 / **119,487** | 390 / **390** | 638 / 303 / **335** | 390 / **189** |
| 5 | 3,487,568 / 369 / **3,487,199** | 3,792 / **3,792** | 5,120 / 2,118 / **3,002** | 3,792 / **2,032** |
| 6 | 97,343,616 / 1,292 / **97,342,324** | 36,447 / **36,447** | 43,439 / 17,861 / **25,578** | 36,447 / **19,847** |

C4 off, on the full form space:

| D | **sum**: vars / rank / **dim** | coh / **cohdim** | **block**: vars / rank / **dim** | coh / **cohdim** |
|---|---|---|---|---|
| 2 | 528 / 29 / **499** | 9 / **9** | 41 / 37 / **4** | 9 / **0** |
| 3 | 15,576 / 107 / **15,469** | 134 / **134** | 310 / 190 / **120** | 134 / **50** |
| 4 | 476,776 / 397 / **476,379** | 1,491 / **1,491** | 2,467 / 1,135 / **1,332** | 1,491 / **744** |
| 5 | 13,941,840 / 1,451 / **13,940,389** | 15,058 / **15,058** | 20,338 / 8,394 / **11,944** | 15,058 / **8,074** |
| 6 | 389,330,560 / 5,121 / **389,325,439** | 145,018 / **145,018** | 172,922 / 70,850 / **102,072** | 145,018 / **79,168** |

**C3 full-chain** subtracts very little from the sum reading and about
3 % of the block reading's coherence: sum/C4-on `135 / 3,935 /
119,476`; sum/C4-off `498 / 15,462 / 476,349`; block/C4-on `0 / 25 /
301` with coherence `0 / 9 / 163`; block/C4-off `4 / 106 / 1,212` with
coherence `0 / 41 / 651`.  **That 3 % is not a rounding detail — it is
where the first version's headline identity breaks (§5.1).**

**Two structural facts, restated with their scope.**

1. **Under the sum reading with C4 off the constraint rows are
   linearly independent at every depth** — rank equals the row count
   (`29, 107, 397, 1451, 5121`).  That is exactly why that reading
   **cannot reject any right-hand side at all** (§6).
2. **The sum reading's dimension is essentially the whole space** —
   97,342,324 of 97,343,616 at depth 6.  A demand that does not ask the
   algebra to decohere is, dimensionally, almost nothing.

---

## 3. F4 / F7 — THE COHERENCE QUESTION: **permitted everywhere, priced nowhere**

**Under the faithful reading, the record demand forces NO within-class
coherence to zero — at any depth, in any variant** (F7(a), F7(b)).
`cohdim = coh` in every one of the **ten** generically-built faithful
configurations (depths 2, 3 and 4 × C4 on/off × C3 one-step/full-chain
where built) **and** at every structurally-measured depth 2–5.  The
coherence dimensions are `9 / 4` at depth 2, `134 / 35` at depth 3,
`1,491 / 390` at depth 4 and `15,058` at depth 5 — equal to the
coherence count in every cell.  Not the pairs whose parents carry
different records; not any.

Three things follow, and each of them kills a sentence of the first
version.

1. **The geography does not exist.**  The identity "cohdim = the
   parents-record-identical count" is a property of the **block
   convention**, not of the record instrument.
2. **The depth-2 F-II cell evaporates.**  `9/9` free with C4 off and
   `4/4` with C4 on, where the block reading gave `0/9` and `0/4`.
   "F-II fires at depth 2, and only there" was "F-II fires *under the
   block reading* at depth 2".
3. **The forbidden entries are explicitly realisable** (F7(c)).  At
   depth 3, faithful, C4 off, one-step, an exact member built by
   perturbing `diag(μ_Ẑ)` along a kernel direction at the **review's
   own displayed pair** has zero residual against all 3,846 rows and is
   positive definite by strict diagonal dominance:

```
   h  = ( ('p','A',v0,0), ('p','B',v0,0), ('r','A',{(A,v0,0)},{(A,v0,0)}) )
   h' = ( ('p','A',v0,0), ('r','A',{(A,v0,0)},{(A,v0,0)}), ('p','B',v0,0) )
   record(h) = record(h'),   mu_Zhat = 3/8192 each
   record(parent h) != record(parent h')     <-- the first version's "FORCED to 0"
   t = 1/81920,   D(h,h') = 1/81920 != 0
```

   **This is the first version's own §3 display with the verdict
   inverted.**  The two parents *are* distinguishable at the previous
   cut, and the coherence survives anyway.  The scale `t = 1/81920`
   reproduces the review's exactly, at the same pair, with the same
   derivation.

**What is NOT reversed.**  F2 stands: `diag(μ_Ẑ)` satisfies the
faithful condition too, so F-I and F-IV remain excluded and the
faithful reading is a genuine third data column, not a wall.  And the
*dimension* is genuinely priced there — rank 296 of 528 at depth 2
against the sum reading's 29 — so the faithful reading is the only one
of the three whose dimension is informative at all.

### 3.1 The Hermitian column — the phase claim, demoted and then reversed

**The lemma (F8(a)).**  Write `D = S + iA`.  Any linear row whose
coefficient function satisfies `c(i,j) = c(j,i)` annihilates `A`
identically, term by transposed term.  **Every** sum-reading row is a
sum over a product set `C × C`, hence swap-symmetric **by
construction**.  Therefore the constraint rank on the antisymmetric
block is zero at every depth, **for any partition, for any measure, on
any layer, without computing anything** — gated here by running the
same product-set rows over a partition with no relation to records
(histories grouped by index mod 7) and with `ν` on the right-hand side.
**F4(e)'s zero is a tautology of the row shape the first version chose.
It carries no information about records, about `μ_Ẑ`, or about this
layer.**

**And it is false the moment anything else is switched on.**

* **C2 sees `A`** (F8(b)): positive semidefiniteness bounds `|A_ij|²`
  by `D_ii D_jj − S_ij²`.  On the pinned witness's own 2×2 block, an
  imaginary entry of `9/2048` leaves determinant `3887/67108864 > 0`
  and one of `9/1024` gives `−1/67108864 < 0`.  *The linear system is
  phase-blind; the constraint set C1–C4 is not.*
* **The faithful reading sees `A`** (F8(c)): its rows have asymmetric
  supports (`r × r'` with `r ≠ r'`), and the constraint rank on the
  antisymmetric block is **268** at depth 2 and **3,739** at depth 3
  (C4 off, one-step; `3,754` full-chain, `900`/`902` with C4).  **A
  record demand of paper 29's shape does see a phase** — and still
  prices none of the within-class ones (`acohdim = ancoh` everywhere).
* Even inside the first version, the **block** reading's off-block rows
  have asymmetric support and see `A` exactly.

*(The one real construction bug found before first delivery stands and
is unchanged: keying the off-block rows by the **unordered** parent
pair sums `R(g,g')` and `R(g',g)`, which is twice the same constraint
on the real part but identically zero on the imaginary part.  The
receipt keys one row per **ordered** parent pair, and the faithful
reading's cross-class rows are keyed the same way for the same reason.)*

---

## 4. F11 — THE DYNAMICAL DEMAND, and what it costs: **everything**

The first version's residue 1 said the successor obligation is a
*dynamical* or *operator* demand "since the record demands demonstrably
do not suffice", and residue 7 named a composition/interchange demand
as "the first thing to try against these numbers".  The round spent the
ten lines.  **They reverse the outcome.**

**First, the ansatz-free half (F11(a)).**  Push the block/one-step split
into *permitted* (parents record-identical) and *forbidden* (parents'
records differ) within-class pairs through D62's closed state map `σ`:

| D | permitted pairs | on σ-state pairs | forbidden pairs | on σ-state pairs | permitted **and not** forbidden |
|---|---|---|---|---|---|
| 2 | 0 | 0 | 9 | 9 | **0** |
| 3 | 50 | 15 | 84 | 15 | **0** |
| 4 | 744 | 28 | 747 | 28 | **0** |
| 5 | 8,074 | 32 | 6,984 | 32 | **0** |

**Every σ-state pair that carries a permitted coherence also carries a
forbidden one.**  The first version's geography is *not a function of
the closed law's state variables at all*; it is a function of the
serialisation labels that `canon` identifies and the prefix map does
not.

**Then the demand (F11(b)).**  Write `D = diag(μ_Ẑ) + E` and impose

```
    E(h,h')  =  μ(h) μ(h') · K(σ(h), σ(h')),     K symmetric, K(s,s) = 0
```

— *the coherence excess is generated by the closed law*.  The classical
member is `K = 0`, so **the demand is fair**: it excludes nothing this
unit exhibits as existence.  The block trace rows and the C3 trace rows
are satisfied automatically (no two siblings share a record, F1(c));
every cross-class C1 entry forces `K = 0` on its σ-state pair; every
off-block singleton C3 row forces `K = 0` on its σ-state pair.  By the
census above, nothing is left:

```
    C1(block) + C3(one-step) + C5(state-generated coherence)
        cohdim = 0   at D = 2, 3, 4 AND 5     (C1–C4 alone: 0 / 50 / 744 / 8,074)
```

**SCOPE, and it is load-bearing.**  C5 is imposed here **on top of the
BLOCK reading** — the *stricter* of the two bounds — because that is
the reading whose entrywise rows force `K` pointwise, and it is the
reading the review's construction used.  Under the **faithful**
reading the cross-class rows are *sums*, not entrywise zeros, so they
do not force `K` pointwise and the C5 computation is a different one
that this unit has **not** performed.  The honest statement is
therefore: *the strongest of the three record readings, together with
one state-generation demand, leaves no coherence at all* — and the
faithful reading's C5 answer is open, and is part of residue 1.

**F-III does not survive the first dynamical demand the unit itself
named.**  This does not make F-III wrong — it is correct for C1–C4 —
but it makes "superposition is PERMITTED and priced" a statement about
a constraint set the unit's own residues call insufficient, and it
settles what such a demand costs.

**The sentence, with its scope.**  *At closed scope, on this layer, a
paper-29-shaped functional level cannot be both **state-generated** and
**coherent**.*  Superposition must therefore either break
state-generation — the excess is not a function of the closed law's own
variables — or enter somewhere else entirely: transport scope, a
different record functor, or a different joint in the map.  **One
demand, one functor, truncation-bound.  The demand's uniqueness is not
established, and that is this unit's new residue** (§8.1).

---

## 5. What the first version's mechanism actually was

Under the **block** reading with **one-step** C3 a within-class entry
`D(h,h')` survives into the restriction as a term of `R^(D−1)(g,g')`
with `g, g'` the two (distinct, by F1(c)) parents; when the parents
carry different records the block reading demands `R^(D−1)(g,g') = 0`,
and every one of those rows is a **singleton**.  The forced set is the
within-class pairs whose parents carry different records:

| D | within-class off-diagonals | parents' records **differ** | parents' records **agree** | **ALL ancestor records agree** |
|---|---|---|---|---|
| 2 | 9 | 9 | 0 | **0** |
| 3 | 134 | 84 | 50 | **0** |
| 4 | 1,491 | 747 | 744 | **0** |
| 5 | 15,058 | 6,984 | 8,074 | **0** |
| 6 | 145,018 | 65,850 | 79,168 | **0** |

That much is true and reproduces.  **Three scope facts strip it of the
physics the first version hung on it.**

### 5.1 The identity is one-step-specific, and the unit's own alternative C3 breaks it (F10(a))

Coordinate by coordinate, testing whether the solution space pins each
of the 134 within-class coordinates at depth 3 (block, C4 off):

```
  one-step C3 :  pinned 84,  unpinned 50,  cohdim 50   -> identity holds
  full-chain  :  pinned 84,  unpinned 50,  cohdim 41   -> identity FAILS
```

The nine missing dimensions are **not** coordinate forcings: the same
50 coordinates remain individually unpinned and the projection is a
proper 41-dimensional subspace of their 50-dimensional span, cut out by
linear relations **among free coherences** — which is exactly the
cancellation the first version's "one term, no cancellation, no
conspiracy" says does not happen.  Under full-chain C3 there is no
"IFF … by a singleton row" story at all.  The same test at depth 4
repeats it:

```
  one-step C3 :  pinned 747, unpinned 744, cohdim 744  -> identity holds
  full-chain  :  pinned 747, unpinned 744, cohdim 651  -> identity FAILS
```

### 5.2 The permitted coherence is the indiscernibility of the constraint data, and C4 is a tiny subgroup of the real symmetry group (F10(c))

For two depth-`(D−1)` histories `g, g'` with the same record, the map
that exchanges their children by matching records (well-defined: F1(c)
makes the matching unique, and same-record parents carry the same
child-record set — gated, zero exceptions) is an **exact symmetry of
the one-step system in all three conventions**:

```
  D = 3, 9 such transpositions, applied to every row of every convention:
     sum/one-step     : 0 non-symmetric rows      sum/full-chain    : 18
     block/one-step   : 0                          block/full-chain : 27
     medium/one-step  : 0                          medium/full-chain: 99
```

The nine transpositions have pairwise disjoint supports, so the
one-step constraint data has a symmetry group of order **at least
2⁹ = 512** at depth 3 alone, where the label-map group C4 has order
**4**.  "The renaming-invariant column" is therefore **not a canonical
quotient**, and the cone's advertised size is dominated by relabelings
the constraint system cannot see.  §3's "coherence survives exactly
between serialisations whose parents were already record-identical"
reads, without loss, as *the system is silent exactly where it cannot
tell the two histories apart* — and the full-chain columns are exactly
the reading in which that symmetry is broken and the dimension drops.
**That is the mechanism behind §5.1, and it is a deflation.**

### 5.3 No within-class pair agrees on its whole ancestor record chain (F10(b))

The last column of the table above is **0 at every depth**: two
serialisations of one DAG always differ in their first event, hence in
their depth-1 record.  So "coherence survives exactly where records
cannot reach" is **false** for the record instrument read as a
*filtration*.  What survives one-step C3 is coherence between histories
the record **could** tell apart, two cuts back.  **The geography tracks
how far down the record chain the convention looks, not the record
instrument.**

### 5.4 Dimension is not volume (F12)

The affine solution space is unbounded; the PSD body is not; every
number in §2 is a dimension.  The pinned witness carries
`D(h,h') = 1/8192` against an entrywise PSD ceiling of `9/1024` on that
entry — **1/72 of the maximum**.  **Nothing in this receipt measures
how much coherence a member may carry**, which is the quantity a reader
will take "a large cone" to be about.

---

## 6. F5 — the controls

**The perturbed measure** (F5(a)).  Each depth-`D` record class's mass
in turn multiplied by `101/100`, feasibility decided **exactly** by the
syzygies.  Three-sided, and all three sides reported:

* the true `μ_Ẑ` is **attainable in every configuration**;
* under the **block** reading **every** perturbation is **rejected as
  infeasible**, at every depth, with C4 on *and* off (23/23, 84/84,
  313/313) — and under the **faithful** reading likewise, at the
  depths where the syzygies are harvested (23/23 and 84/84, C4 on and
  off);
* under the **sum** reading with **C4 off** there are **no syzygies at
  all** (rank = rows), so **nothing whatever can be rejected**; with
  C4 on it rejects 22/23, 84/84, 312/313, the survivors being classes
  the renaming group fixes (orbit size 1).

**F5(a′) is repaired, not defended** (round-1 MINOR 1).  The first
version's disjointness predicate reduced to `MUC[D][c]·101/100 ≠
MUC[D][c]`, i.e. to `101/100 ≠ 1`.  It holds for any perturbation of
any consistent linear system and **cannot distinguish a discriminating
constraint system from one that accepts every measure** — and the
sum/C4-off system *is* the latter.  The statement is retained as a
statement; **the gate is now the faithful reading's control response**,
which is the one the unit wanted and did not compute: medium rejects
23/23 at depth 2 and 84/84 at depth 3, C4 on and off.

**The pure-diagonal comparator** (F5(b)).  `4 / 70 / 588` (C4 off,
one-step, D = 2/3/4) and `0 / 17 / 146` (C4 on).  **`μ_Ẑ` is one point
of a classical polytope before it is one point of a quantum cone**; the
record demands do not even pin the per-history weights.

---

## 7. Licensed claims — and the ones this unit forbids

**Licensed, at two-actor delivery-free d42a closed scope, at the
truncation depths 2–6 of the exhaustive depth-6 family, with `canon` as
the record functor:**

1. **A paper-29-shaped functional level exists over this layer** — the
   classical diagonal functional satisfies C1 (all three readings), C2,
   C3 (both readings) and C4 and is positive definite.  **F-I and F-IV
   are excluded.**  The claim is about the **layer**, not the generated
   law: `diag(w)` works for any strictly positive cut-consistent weight
   (F9).
2. **Under the faithful paper-29 reading, consistency does not
   structure coherence.**  `cohdim = coh` in every variant built —
   all four C4/C3 combinations at depths 2 and 3, one-step at depth 4,
   and structurally (C4 off, one-step) at depths 4 and 5 — and a PSD
   member carrying coherence between histories whose parents carry
   different records is exhibited exactly.  **Permitted everywhere,
   priced nowhere.**
3. **The whole dimension/geography table is measure-independent.**  The
   coefficient matrix never sees `μ_Ẑ`; it is a fact about the history
   tree, `canon`'s partition and the prefix map (F9).
4. **The first dynamical demand eliminates all coherence.**  Under
   C1(block) + C3(one-step) + C5, `cohdim = 0` at depths 2–5, and the
   classical member satisfies C5.  **At closed scope, at the block
   bound, the quantum layer cannot be both state-generated and
   coherent** (F11).  The faithful reading's C5 answer is a different
   computation and is **not** performed here (residue 1).
5. **F-III survives as one sentence: the consistency cone is large.**
   It is not evidence that the generated law is quantum.
6. **The first version's forcing mechanism is real and scoped**: under
   the **block** reading with **one-step** C3 a within-class coherence
   is killed iff the two histories' parents carry different records, by
   a singleton row.  The identity fails under the unit's own
   full-chain C3 (F10(a)); no within-class pair agrees on its whole
   ancestor chain (F10(b)); and what survives is the orbit structure of
   a symmetry group of order ≥ 512 that the first version did not look
   for (F10(c)).
7. **The sum reading's phase blindness is a tautology of its row
   shape** — true of any partition and any measure (F8(a)) — while C2
   (F8(b)) and the faithful reading (F8(c), rank 268 / 3,739) both see
   the antisymmetric part.
8. **Even the classical layer is under-determined** by C1 + C3 + C4
   (F5(b)).

**Explicitly NOT licensed (do not quote):**

* **"the record instrument is the exact boundary of permitted
  coherence"** — WITHDRAWN, §9;
* **"a record measure of this shape cannot see a phase"** —
  WITHDRAWN, §9;
* that any member computed here **is** the action line's decoherence
  functional — **the map is untouched**;
* any Hilbert-space ontology; **no class operators are constructed**;
* "the generated law is quantum", or "permits superposition", without
  the reading, the depth, the record-functor and the **C5** clauses;
* **"the generated law admits a functional level"** without the F9
  clause: the sentence is true of every strictly positive
  cut-consistent weight on this layer, including laws the corpus has
  spent D49–D65 excluding;
* the dimensions as a measure of **how much** coherence a member may
  carry — dimension is not volume (F12);
* the coherence dimensions as depth-free numbers; they are truncation
  dimensions;
* the `sum`-reading dimensions as evidence of anything *physical*;
* **"C3 is paper 29's demand"** — the lower-depth C1 reproduction is
  this unit's construction (MINOR 5);
* **"C4 is the layer's symmetry group"** — it is the label-map group,
  a subgroup of order 4 inside a group of order ≥ 512 (MAJOR 4);
* the **C5** result as depth-free, functor-free or ansatz-free;
* any of the above at three-actor scope, at transport scope, with
  delivery, or beyond depth 6;
* D65's DC3(2) as **discharged**.  It is not.

---

## 8. Residues

1. **THE DEMAND'S UNIQUENESS IS THE NEW RESIDUE.**  C5 is *one*
   natural dynamical demand — "the coherence excess is a function of
   the closed law's own state pair" — imposed on *one* of the three C1
   readings (the block bound, whose entrywise rows force the kernel
   pointwise).  It kills everything, at every depth tried, with a fair
   classical member.  **Two things are open and both are computations,
   not arguments:** whether the *faithful* reading plus C5 also
   collapses (its cross-class rows are sums, so they do not force the
   kernel pointwise, and this unit did not compute it), and whether
   *every* generation ansatz does what this one does.  This is now the
   sharpest successor question in the segment.
2. **The unit is a measurement, not a construction.**  D65's residue 1
   is *narrowed*, not closed: the generated line still has no derived
   class operators and no derived Gram functional.  **Building a
   functional level means deriving one, not measuring the space of
   candidates.**
3. **The C1 form choice is now DECIDED, not open.**  The first version
   filed "which C1 reading is the record demand" as an undecided form
   choice.  It is not: paper 29 §4.3 states one of the three, and this
   note is written at that one.  What remains a form choice is C3's
   lower-depth reproduction (MINOR 5) and the record functor.
4. **The record functor is interpretive, and ONE was tried.**  `canon`
   was *chosen*.  A coarser or finer functor changes which pairs are
   within-class at all.  The "transportable mechanism" of the first
   version was asserted on one functor and is downgraded to a
   conjecture; a coarsening control (records that forget the last
   event's actor) would settle it.
5. **The renaming group is the label-map group** — and the constraint
   system's real symmetry group is *much* larger (§5.2).  The honest
   quotient shrinks every headline number by 4–15×.  The full
   equivariance quotient was computed by the round and is *not*
   rebuilt here; it is a residue.
6. **C2 is untested where it could bite.**  Positivity removes no
   dimension here only because every record class has strictly positive
   mass.  At a scope with a null record class none of §2 transfers.
7. **Dimension is not volume.**  Nothing here measures how much
   coherence a member may carry (F12).  A volume/ceiling measurement is
   a separate unit.
8. **The truncation.**  Every dimension is a truncation dimension; the
   patterns held at every depth 2–6 with zero exceptions, but there is
   **no proof** and the pin declines to bet.
9. **Three actors, transport, delivery** — out of scope, as always.

---

## 9. Corrections — what round 1 withdrew, verbatim

Round 1 reproduced **every published figure** of the first version of
this note and of its receipt — the anchors, the whole 32-row dimension
table with every antisymmetric column, the geography counts, the
singleton census, the witness to its `5183/67108864` minor, all five
control responses, and the 25 PASS / 0 FAIL under three hash seeds —
using its own enumerator, its own σ normal form, its own constraint
rows and an elimination that pivots the other way.  **The arithmetic
was completely sound.**  It then broke the deliverable **by
computation rather than by argument**, twice at the headline and twice
more at the mechanism.  All four constructions are credited to the
round, rebuilt here with this unit's own driver and **gated**; every
figure of all four reproduces the review's exactly.

**WITHDRAWN 1 — the sharpest sentence of the deliverable (BLOCKER 1).**

> ~~"under the only reading that bites, coherence survives **exactly**
> between histories whose *parents* were already record-identical —
> killed everywhere else by a **singleton** cylinder-consistency
> row"~~ ; ~~"**The record instrument reaches back one step and
> decoheres precisely what it could already tell apart at the previous
> cut.**"~~ ; ~~"the record instrument is the exact boundary of
> permitted coherence"~~ ; ~~"**F-II fires at depth 2, and only
> there**"~~ ; ~~"the record instrument is part of the click law, and
> here it is the only thing that removes a phase"~~

are **false as statements about the record demand**.  They are
properties of the **block convention**, which forbids by hand — one
level down, before any row is written — the cancellations that paper 29
§4.3's actual condition permits.  Under the faithful condition
`D̄(r,r') = δ_{rr'}μ_Ẑ(r)` **nothing is forced at any depth**
(`cohdim = coh`, §3), the depth-2 F-II cell evaporates (`9/9` free),
and the entries the first version displayed as "killed by a singleton
row" are exhibited as an exact PSD member (§3, `t = 1/81920`).
Replaced by §3 and §4: **consistency does not structure coherence; the
first dynamical demand eliminates it.**  LOG #479's title
("COHERENCE SURVIVES EXACTLY WHERE RECORDS CANNOT REACH") is
**superseded by this note**.

**WITHDRAWN 2 — the phase claim (MAJOR 1).**

> ~~"**a record measure of that shape cannot see a phase**, so no
> amount of C1/C3 could ever select one"~~ ; ~~"A record measure
> cannot see a phase, so no amount of C1/C3 can select one"~~ ;
> ~~licensed claim 6, "**The record demands are blind to the imaginary
> part**"~~ ; ~~LOG #479's "RECORDS CANNOT SEE A PHASE"~~

are **false as physics sentences**.  The zero is a one-line theorem
about **row supports**: every sum-reading row is a sum over a product
set, hence swap-symmetric, hence annihilates the antisymmetric part —
for any partition, any measure, any layer, without computing anything
(F8(a)).  It says nothing about records.  And it reverses under the
faithful reading, whose asymmetric rows carry antisymmetric constraint
rank **268** at depth 2 and **3,739** at depth 3 (F8(c)), and under C2,
which bounds `|A_ij|²` by `D_ii D_jj − S_ij²` (F8(b)).  The honest
sentence is: *the linear part of the two bracketing readings cannot see
a phase because both were built out of product-set sums.*

**WITHDRAWN 3 — the identity's scope and the word "precisely"
(MAJOR 3, NIT 1).**

> ~~"`cohdim` reproduces the 'free' column exactly at every depth"~~ ;
> ~~"costs the block reading about 3 % of its coherence"~~ (as an
> aside) ; ~~"coherence survives exactly where records cannot reach"~~

The identity holds **only** for one-step C3.  The unit's own
full-chain column breaks it — 84 pinned, 50 unpinned, `cohdim` **41** —
and the unit printed the numbers without noticing (F10(a)).  And no
within-class pair agrees on its **whole** ancestor record chain at any
depth (F10(b)), so the filtration reading of the slogan is false.
D65's round flagged the same word "precisely" in the same position; it
is struck again.

**WITHDRAWN 4 — the reach of "the generated law" (MAJOR 2).**

> ~~"**the generated law** admits a paper-29-shaped functional
> level"~~ ; ~~title: "what quantum layers does **the generated law**
> admit?"~~ ; ~~"the space of paper-29-shaped functionals **over the
> generated law**"~~

Over-reach.  `μ_Ẑ` appears only on the right-hand side; the coefficient
matrix is identical under a second, non-record-constant measure (F9).
The statement is true of **every** strictly positive cut-consistent
weight on this layer, including laws the corpus has spent D49–D65
excluding.  The honest subject of the dimension table is `canon` and
the prefix map.

**CARRIED WITHOUT WITHDRAWAL** — the remaining findings, each repaired
in place: MAJOR 4 (C4 is the label-map group, a subgroup of order 4
inside one of order ≥ 512 — §5.2); MAJOR 5 (the dynamical demand — §4,
and it is now the note's second headline); MINOR 1 (the vacuous F5(a′),
relabelled and re-gated on the faithful reading — §6); MINOR 2
(dimension is not volume — §5.4); MINOR 3 (the two depth-2 headline
clauses, scoped); MINOR 4 (the runtime and the peak memory are now
printed by the receipt itself); MINOR 5 (C3's lower-depth reproduction
labelled a construction — §1); MINOR 6 (the "transportable mechanism"
downgraded to a one-functor conjecture — residue 4); MINOR 7 ("20
witnesses" relabelled as one algorithm at 20 configurations); NIT 2
(the witness display now prints its orbit expansion); NIT 3 (LOG #479's
unhedged title is superseded here); NIT 4 (the four findings the first
version's own residues half-admitted are promoted from residues to
findings — they are §3, §4, §5.1 and §5.2).

**One incidental non-match, recorded.**  The review's full-chain
variant of the §3 member reports `t = 1/98304`; this receipt's reports
`t = 1/81920` at the same pair.  Both are exact, both have zero
residual, both are positive definite.  The scale is derived from the
kernel *direction*, and the direction at a given coordinate depends on
the free/pivot split — the review pivots on the largest column, this
receipt on the smallest.  The one-step scale (`1/81920`) matches
exactly.  **No load-bearing figure differs.**

---

## 10. Gate ledger and where the evidence sits

**39 gates, 39 PASS, 0 FAIL, exit 0** — 25 before the
repair.  **A clean sheet is not evidence of a positive result**; the
negatives here (the dynamical demand's zero, the sum reading's
emptiness of content, the tautology behind the phase column, the
under-determined classical comparator, the measure-independence
de-licensing) are registered as *passing gates that assert them*.

* **anchors** F0(d)–F0(i) — unchanged, and every one of them was
  reproduced by the round;
* **definitions** F1(a) the marginal reduction, F1(b) prefix
  non-commutation, F1(c) no siblings share a record;
* **the bracketing readings** F2 existence, F3(a) interiority, F4(a)
  the sum reading's freedom [SCOPED], F4(b) the singleton mechanism,
  F4(c) the depth-2 negative [SCOPED], F4(d) the witnesses [relabelled
  one-algorithm], F4(e) the phase column [DEMOTED to a lemma];
* **THE FAITHFUL READING** F7(a) nothing forced, F7(b) the structural
  identity at depths 2–5 with its cross-gate, F7(c) the
  cross-parent-record PSD member;
* **THE PHASE** F8(a) the shape tautology, F8(b) C2 sees it, F8(c) the
  faithful reading sees it;
* **MEASURE INDEPENDENCE** F9;
* **SCOPE** F10(a) the identity is one-step-specific, F10(b) no whole
  ancestor chain agrees, F10(c) C4 is a small subgroup;
* **THE DYNAMICAL DEMAND** F11(a) invisibility, F11(b) `cohdim = 0`;
* **HONESTY** F12 dimension is not volume;
* **controls** F5(a), F5(a′) [REPAIRED], F5(b); **determinism** F6(a);
  **resources** F6(d).

Structural/hygiene gates on the file itself: F0(a) AST single sources,
F0(b) AST exit-freedom, F0(c) AST inexactness scan, F6(b) AST
hash-order scan.  **Entailed or declarative, and marked as such:**
F3(a) and F6(c).

Reproduce: `python3 v10/code/d68_functional_slot_exact.py` from the
repo root (optional argument = family depth, default 6).
