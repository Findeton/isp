# D28 — Diamond necessity and the opportunity law

**Status:** after hostile round 1, 2026-07-12 (two streams — mathematics/rebuild, physics/foundations; reports in `reviews/d28-round1-*`; 6 MAJOR / 16 MINOR / 12 NIT applied; both receipts upgraded and rerun green — 10/10 and 12/12; corrections at v8/LEDGER #132; outcomes in §6). Pin 1fe7aa9 preceded the receipts; the round-1 upgrades are disclosed per gate. Provenance labels per D20. Successors D29 (intrinsic causal rulers) and D30 (frozen cone comparison) remain OUTLINED, unopened.

## 0. The goal (user-approved wording, verbatim)

> Determine whether NSE-admissible one-parent growth can produce operational causal diamonds; prove the exact obstruction if it cannot; characterize the minimal post-birth opportunity process that repairs it; and construct at least two complete, covariant finite opportunity kernels whose induced influence laws are either exactly distinguishable or certified equivalent.

**The missing law, named:** the interaction-opportunity kernel K — the law for which existing records a newborn/young carrier may couple to, with what type and strength, given the sealed history. Paper 19 does not specify K; D28 is its characterization at exact finite grade.

**Insulation [PROTOCOL]:** paper 19's survivor structure claims nothing about cone emergence; every D28 outcome (including total failure) lands as a theorem about the growth clause's role. V9 discipline verbatim: no v9 measurement reused as evidence; v9's counting law and requirement list enter as design hints only, flagged; instruments validated on synthetic data before any grown web (D29); candidates frozen before the observed cone is consulted (D30). The observed low-energy cone is a COMPATIBILITY BENCHMARK, never a holdout — the untouched holdout is genuinely future scale-dependent influence-tail data.

**Priority [LITERATURE, pinned; round-1 verified by fetch, with two load-bearing caveats]:** neighbors are Rideout–Sorkin classical sequential growth (gr-qc/9904062: a growth family from discrete covariance + Bell causality — no NSE/isometric reception structure, no empirical bridge; **caveat 1: RS births take arbitrary partial stems — multi-parent — so common futures are NATIVE in RS growth: the D28 obstruction exists relative to the corpus's one-parent birth clause, not in RS; caveat 2: both D28 kernels FAIL the RS path-covariance analog — disclosed at receipt N11 (the same grown graph along two accretion orders: 1/40 vs 1/25 under collar, 2/81 vs 1/25 under tail; the global step clock is physical in these kernels)** — whether an NSE-compliant, RS-path-covariant opportunity kernel exists, or whether flags-observable makes path-equality the wrong requirement, is the successor front and the first exact shot at the kernel-selector problem, paper 19 F12) and interventionist quantum causal models (Barrett–Lorenz–Oreshkov, arXiv:1906.10726: their Def 4.1 no-influence = channel factorization, universally quantified over inputs — D28's finite-alphabet TV is therefore a LOWER BOUND on BLO-influence: D28 zeros do not imply BLO-no-influence, and BLO's own definition anticipates the kickback two-way channel). D28's deltas, round-1 hygiene applied: (i) the NSE/isometry constraint set on K; (ii) kernel-level OBSERVABLE-HISTORY identifiability (flag-carried; injectivity of K ↦ P_K — one level above D23's realized-graph recovery; click-only identifiability is F7's substance); (iii) the birth-visibility budget (D26 E4's deliverable, applied here as a constraint).

## 1. A0 — the Busch-aware normal form, with its claim reduced

- **A0a (extremal normal form) [EXACT + LITERATURE]:** every isometry V: H → H⊗K factors as V = U∘J with J|ψ⟩ = |ψ⟩|0⟩ and U unitary on the enlarged space — every pure isometric birth is fixed-blank activation followed by a unitary. KINEMATIC only: U may act globally; the factorization is not a locality theorem.
- **A0b (Busch normal form):** every admitted stochastic closure (D25/D27) is a preparation-independent orthogonal flag over such branches: T(ρ) = Σ_k p_k V_k ρ V_k† with mutually orthogonal ranges (Busch's completely positive case, Thm 2; the positive-only case, Thm 1, adds antiunitary branches — invisible on the real family; round-1 precision).
- **A0c (the locality debt, stated):** the support of each U_k is NOT fixed by factorization; constraining it to a declared collar is precisely part of K. Consequence: **K's random partner choice must itself appear as a recorded orthogonal branch WITH preparation-independent weights** (round-1 fix: Busch needs both halves — "or" was wrong) — **an unrecorded partner lottery is candidate silent erasure**, now receipt-grade: N12 exhibits a preparation-independent lottery over non-orthogonal isometries strictly contracting a declared pair (D 1/2 → 2/5), so preparation-independence alone is not Busch-sufficient and recording the flag (= range orthogonality = in-principle recoverability of k) is exactly what non-silence requires.

## 2. The influence relation — intervention- AND query-relative

For records A, B, a declared intervention alphabet 𝒜 and query alphabet 𝒬:

> I_{𝒜,𝒬}(A→B) = max over ACTIVE pairs α, α′ ∈ 𝒜_A \ {no-op}, max_{q∈𝒬_B} TV[ P(q | do α), P(q | do α′) ].

Pinned for the exact campaign (ROUND-1 REPIN, physics M1): interventions are UNCONDITIONED trace-preserving replacements applied at A's site immediately after A's birth (no postselection); the influence sup runs over **active pairs only** — the no-op arm enters the separate **DISTURBANCE relation** D(A→B) = max over (no-op, active) pairs, because a no-op-vs-active difference is back-action visibility through later re-touches, not influence: on a web where the parent re-touches its child, the no-op arm produces child→parent entries (receipt T4: disturbance 1152/3125 with active-pair influence exactly 0), breaking antisymmetry on kernel-generated webs. Active alphabet: {prep |0⟩, prep |1⟩, prep Ry(3/5,4/5), prep Ry(5/13,12/13)}. Queries: 𝒬_B = {Z, X} on B — tomographically complete for this campaign's REAL state family (2-line proof: real ρ = (I + r_x X + r_z Z)/2, both parameters read; TV over Y vanishes identically on real pairs). Both alphabets are printed in both receipts. **The enrichment asymmetry (round-1, both streams — the kickback lemma):** enrichment can only enlarge the detected relation AND can destroy antisymmetry — under controlled-R_θ the control's coherence block acquires Tr[ρ_T R_θ†] = cosθ·Tr[ρ_T] − sinθ·Tr[ρ_T J]; real symmetric targets kill the J-term (which is also why T3b's ledger is exactly ×7/25), while complex targets restore it, making every interact gate a two-way channel: with complex preparations and Y-queries the diamond acquires I(X→B) = 110592/390625 and the S_3 web acquires 2-cycles — the (𝒜,𝒬)-relative relation is an ORDER only at alphabets where the kickback identity tr[R_θX] = tr[R_θZ] = 0 protects the backward entries. Consequently: **positive entries are alphabet-robust lower bounds; zero entries are alphabet-relative certificates** — except the theorem-grade zeros: tree zeros (fresh-|0⟩ targets, immune in both families) and spectator zeros are robust under EVERY trace-preserving intervention (the wire-time lemma, round-1: the Heisenberg support of a query pulled back through post-intervention gates grows a connected subtree that never meets the intervened site). The record graph and the influence relation also differ on SILENT subgraphs (a g = 0 record edge is influence-invisible — receipt T1b): D29's rulers must declare which order they read, and must either fix the alphabets or measure the channel-robust subrelation. J⁺(A) = {B : I(A→B) > 0} is the SUPPORT cone; the WEIGHTED profile w_A(B) is carried alongside (D29/D30 report strict, ε-, and quantile fronts plus tails; never the bare maximum).

## 3. The exact theorems (receipt 1: `d28_influence_diamond_exact.py`)

- **T1 (tree influence theorem):** for the D24 census (interior couplings): I(u→v) > 0 ⟺ v is a descendant of u through nonzero couplings — activation-independent (the intervention sup dissolves state-dependence); the zero side (ancestors, incomparables) is quantum no-signalling, gated. A g = 0 edge blocks (exhibited). Construction-order independence of I: inherited from the law's gauge, gated.
- **T2 (the tree no-go; round-1 corrected hypothesis and conclusion):** in a pure one-parent birth FOREST with **no post-birth interactions of any kind**, incomparable records have DISJOINT descendant sets; hence B ∥ C ⇒ J⁺(B) ∩ J⁺(C) = ∅ — no operational diamond exists, exactly. General proof (round-1 m1, no longer census-only): in a forest every node's ancestor set is a chain, so a common descendant would place B and C on one root path, making them comparable; the positive side is TV = ∏(path couplings) between do(|1⟩) and do(|0⟩); the zero side is the wire-time lemma — alphabet-robust under every TP intervention. The continuum contrast (two spacelike Minkowski events share a future) is a separate [LITERATURE] statement; the finite-sprinkling caveat is printed and NOT conflated. **Conclusion (corrected): POST-BIRTH INTERACTION PER SE is necessary for common operational futures — cross-branch coupling is NOT:** the MAILBOX exhibit (receipt T2b) builds an operational diamond in which every interaction is a record-edge re-touch of the shared parent (two siblings write, a later sibling reads — K_collar-legal, zeros complex-robust). A0 shows the repair is representable after blank activation; A0c: representability is not locality.
- **T3 (the minimal operational diamond; apex renamed X to match the receipts; minimality = zero interactions provably fail by T2, one suffices):** a web with A ∥ B (I = 0 both ways, gated — and robust even under complex enrichment), I(A→X) > 0, I(B→X) > 0, satisfying ALL of: A's and B's sealed-value algebras never rewritten (Z-diagonals invariant; coherence dispersal disclosed as the ledger); the interaction via a control (nondemolition on the control's values); X durable under the declared instrument (the Z-click; no op follows — printed); NO influence outside the declared transitive closure **at the pinned alphabets** (the full influence matrix gated against the causal order; the complex-extension witness I(X→B) = 110592/390625 is recorded in §2 — the closure equality is a pinned-alphabet certificate, not an absolute); all causal-compatible schedules give the identical law (the schedule-varied pairs commute; the non-commuting pairs are order-pinned by causal compatibility — the gauge gate closes the residual commuting freedom); the NSE opportunity-ledger conjunct is receipt 2's battery; **the synergy gate** — X carries genuine joint dependence, witnessed by the A-influence profile on X differing exactly across fixed B-settings (a do-severed confounder cannot produce this: any (A,B)-independent mixture of single-mechanism laws gives setting-independent profiles; scope: a sufficient witness — tuned angles can false-negative).

## 4. The opportunity package and the two kernels (receipt 2: `d28_opportunity_kernels_exact.py`)

**The complete package:** K_H assigns a normalized exact-rational law to opportunities o = (∅ | birth(x; parent y) | interact(x, y, τ, g)) with the flag k = the realized choice, recorded; τ = controlled-Ry and the seal instrument 𝒥 = the Z-click, both declared for this campaign. Σ_o K_H(o) = 1 per reachable history.

**Intrinsic graph locality (no imported space):** d_cover = undirected cover-graph distance. **K_collar:** interactions eligible only at d_cover ≤ r = 1; weights uniform over {∅} ∪ eligible, exact rationals. **K_tail:** same, plus weight ε·q^{d_cover} for beyond-collar pairs, ε = 1/5, q = 1/2, renormalized. This is INTRINSIC GRAPH LOCALITY, named as such — not yet physical spatial locality. **K_self is POSTPONED** [defined for the debt to be auditable (round-1 m9): a kernel whose opportunity weights depend on the web's own influence/coupling history — e.g., attachment weighted by past interaction density between the candidate pair — so the weight function and the growth co-determine each other; it needs a fixed-point, deterministic-update, or uniqueness theorem before different update orders stop defining different laws]. **Covariance, defined against the RS term (round-1 M4):** this note's battery gates history-isomorphism covariance (label-independence, two relabeling classes) and realized-web construction-order gauge; it does NOT gate RS path-covariance, which both kernels FAIL (receipt N11, disclosed) — the accretion path is observable here because flags are declared records, so path-equality may be the wrong requirement; deciding that is the successor front. **The opportunity-law separation (round-1 m5; RESCOPED at round 2 — the first transcription overclaimed):** births add leaves and collar interacts add only parallel edges, so d_cover between existing registers never decreases; hence under K_collar a DIRECT interaction opportunity between registers at d_cover ≥ 2 has probability 0 at every step, forever, while under K_tail it is positive whenever such a pair exists — the N9 flag-law separation extended from the gated horizon to all times. The induced influence relations extend beyond the collar under BOTH kernels through mediated chains (T1's depth-≥2 descendants; T2b itself: I(z1→z3) = 109296/9765625 at d_cover = 2 in a K_collar-legal web), so the permanent separation is at opportunity/flag grade, not influence-relation grade.

**The gate battery (each kernel):** exact normalization per history; history-isomorphism covariance (label transposition gate); nonzero null-opportunity probability; projective/cylinder consistency (the T-step law marginalizes to the (T−1)-step law); non-explosion; local-clock form (weights depend on H only through the declared graph+flag data — gated by preparation-swap invariance); construction-order gauge on realized webs; sealed-algebra protection (zero weight on ops touching sealed registers, gated with a sealed seed register); **recorded, preparation-independent opportunity randomness (the A0c/Busch gate)**; no dependence on arbitrary labels.

**Kernel-level identifiability (the D23 distinction, honored):** D23 recovered a REALIZED graph; D28 gates injectivity of **K ↦ P_K(observable histories)** — flag sequences plus final click laws — on the exhibited family: P_{K_collar} ≠ P_{K_tail} exactly (the separating event: any beyond-collar interaction flag, probability 0 vs printed positive rational), and within-family K_tail(ε) ≠ K_tail(ε′). Scope stated: family-level injectivity; the general-K identifiability class is a pinned front.

**The dimension triage (the repaired logic — the v9 #119 lesson carried):** a kernel class is EXCLUDED for M⁴ only by a PROVEN scale-independent upper bound on order dimension; witnesses are lower bounds and growth evidence; witness ABSENCE is never evidence without a registered power calculation. Two theorem-grade gates: **(i)** the pure-birth (tree/forest) class carries the explicit upper bound **dim ≤ 2** — the mirror-DFS realizer, now with the general proof (round-1, both streams: an ancestor precedes its subtree in every preorder; incomparables' LCA-children subtree blocks are contiguous, σ-ordered in one preorder and reverse-σ-ordered in the mirror, so exactly one order flips; forests by reversed concatenation; adversarially verified on 114 + 300 shapes in review) ⇒ pure birth is excluded for M⁴, which requires unbounded order dimension — carried as the 5-line antipodal construction [EXACT-provable]: a_i on a unit circle at t = 0, b_j antipodal at radius R and t = R+1−δ embeds S_n in M³ ⊂ M⁴ for every n (round-1 m2: sourced here, not to v9-resident material). **(ii)** interactions PROVABLY BREAK the ceiling: the 6-record S_3 web — **all 30 ordered pairs now gated interventionally at the pinned active alphabet** (round-1 M1/m10: the earlier spot-verification cited the tree theorem outside its scope; the full-matrix gate replaces it) — with the exhaustive 2-realizer refusal ⇒ dim ≥ 3 > 2, at the pin (the complex-enrichment caveat of §2 applies: the operational ORDER reading is pinned-alphabet-relative). Open with triage stakes (round-1): K_collar is not pure birth, so (i) does not exclude it — nothing currently pins its dimension ceiling either way. Growth beyond S_3 is D29/D30 evidence-gathering, not an exact claim.

## 5. D29 / D30 — outlined successors [PROTOCOL]

**D29 (intrinsic causal rulers):** proper time (calibrated chain length), spatial distance via 2-link/n-link constructions (Rideout–Wallden arXiv:0810.1768, 0905.0017 — naive common-past/future minimization is known to misbehave and is NOT used as a ruler), validated dimension estimators, direction reconstruction, support-vs-occupancy cone instruments with certified error bands — every estimator validated FIRST on synthetic sprinklings varying dimension (2–5), density, window geometry, curvature, missing-record noise, plus polyhedral and non-manifoldlike random-DAG controls. **The S² separation:** a dimension-NEUTRAL arm (dimension/directions from causal order alone) strictly precedes a CONDITIONAL 3+1 arm (only if the inferred dimension is compatible with four does the recovered direction space get compared with the corpus's celestial bridge) — otherwise 3+1 is inserted, not derived. **D30 (frozen cone comparison):** completed kernels run through D29's validated instruments; F(L), d_eff(L), response fronts, tail laws printed with candidates and parameters frozen BEFORE comparison; the observed cone as the compatibility/selection benchmark; the four outcome branches pre-registered (bridge / discriminator-freeze / growth-law-insufficient / cone-was-assumed).

## 6. Round-1 outcomes (fronts pinned above; two streams, 2026-07-12; LEDGER #132)

**(F1) PROVEN, with the scoping the reviews forced now in §2:** {Z, X} is tomographically complete for real reduced states (2-line proof carried); complex amplitudes enter through the intervention alphabet, and what changes is not query power but the ORDER — the kickback lemma with exact witnesses (I(X→B) = 110592/390625; S_3 2-cycles), the positive-vs-zero asymmetry sentence, and the two theorem-grade robust zero classes (tree zeros; spectator zeros — the wire-time lemma, robust under every TP intervention). The no-op arm was additionally excised from influence into the DISTURBANCE relation (T4: 1152/3125 vs 0 — the round-1 repin). **(F2) TRUE — proven** (both streams: the general mirror-DFS lemma, adversarially verified on 114 + 300 shapes). **(F3) RESOLVED — recording is NOT an over-constraint:** preparation-independence alone is not Busch-sufficient (N12: a preparation-independent lottery over non-orthogonal isometries strictly contracts, D 1/2 → 2/5); orthogonal ranges = in-principle perfect recoverability of the flag, so "recorded" is exactly the orthogonality half; N6 gates the weight half, matching D27 B3. **(F4) ADEQUATE-BUT-STRUCTURAL:** cylinder consistency is implied by per-node normalization (Ionescu–Tulcea); relabeled a smoke test; the substantive refinement/clock question is the RS-covariance front, not N4. **(F5) SURVIVES:** a do-severed confounder cannot produce the setting-dependent profile (additive-mixture lemma); scoped as a sufficient witness. **(F6) VERIFIED BY FETCH, with two load-bearing caveats now in §0:** RS births are multi-parent (common futures native there; the obstruction is relative to the one-parent clause) and both kernels fail RS path-covariance — DISCLOSED at N11 (1/40 vs 1/25; 2/81 vs 1/25) with the successor front pinned (an NSE-compliant RS-path-covariant kernel, or a proof that flags-observable makes path-equality wrong — the first exact shot at F12); BLO Def 4.1 is universally quantified, so D28's finite-alphabet TV is a lower bound on BLO-influence (D28 zeros ≠ BLO-no-influence). **(F7) OPEN, sharpened:** family-level injectivity delivered (with 2/405 and 2/805 both gated); the substance is click-only identifiability (flags dropped) plus the on-support caveat (K is unidentifiable beyond its reachable support — the D23 activated-subgraph boundary one level up). **Round-1 wording repairs on the record:** T2's hypothesis and conclusion corrected (post-birth interaction per se, not cross-branch — the T2b mailbox exhibit); the apex renamed X; "covariant" defined against RS's term with the failure disclosed; the S_n premise sourced as the 5-line antipodal construction, not to v9-resident material; K_self defined; the Busch citation carries the completely-positive qualifier (the positive-only case adds antiunitary branches, invisible on the real family) — fixed here and at the five upstream loci.
