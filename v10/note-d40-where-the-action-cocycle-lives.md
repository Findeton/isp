# D40 — where the action cocycle lives

**Status:** PINNED BEFORE EXECUTION. No D40 executable, stdout receipt,
hostile review or Paper 29 draft exists at this commit.  
**Date:** 2026-07-15.  
**Parents:** Papers 15, 18, 19 and 28; D14, D26, D34c, D38b and D39c.  
**Held paper title:** *Where the action cocycle lives: amplitude consistency,
decoherent-record descent, and the first architecture audit of the identified
law*.

## 1. The question

Paper 28 reports that the chosen projected D38b star kernel fails 337 of 506
registered scalar action squares, including

```text
1/18 != 2/33.
```

That arithmetic is exact.  Its physical interpretation is not yet fixed.
Papers 15 and 18 do not assign an action directly to normalized click
transitions: an action supplies amplitudes or a decoherence functional,
unrecorded alternatives are summed coherently, and a supplied record
instrument produces durable click probabilities.  A scalar click cocycle is
therefore licensed only after the click algebra decoheres and the declared
regional boundary is sufficient for the descended conditional law.

D40 asks:

> On which object does commuting composition hold, under what exact
> conditions does it descend to durable-record probabilities, and what does
> the Paper 28 obstruction mean after serial histories, causal-DAG atoms and
> coarse regional boundaries are kept distinct?

The receipt is a finite level audit, not a claim to have executed the full
D15 Standard Model plus Einstein-Hilbert functional.  It confronts the
identified-law architecture with exact finite amplitude and Bell fixtures,
then records every dictionary still needed before the D15 law itself can be
called record-closed.

## 2. Frozen distinctions

The following objects may not be conflated:

```text
local operator or class-operator composition;
finite decoherence functional D(alpha,beta);
decoherent durable-record cylinder measure mu;
conditional click kernel mu([He])/mu([H]);
serial event history;
typed causal-DAG atom obtained by forgetting incomparable serialization;
regional boundary statistic supplied to a projected kernel;
classical increment potential on the D39 action complex.
```

For two incomparable operations `a,b`, operator equality

```text
K_b K_a = K_a K_b
```

is an action-level interchange law.  If durable refined records define one
decoherent cylinder `Hab`, then

```text
p(a|H) p(b|Ha) = mu(Hab)/mu(H)
                = p(b|H) p(a|Hb)
```

is a probability theorem.  It does not follow when `Hab` and `Hba` remain
distinct physical histories, when their common DAG atom is a sum over both
serializations, when interference prevents a scalar cylinder measure, or
when the displayed boundary is not sufficient for the full conditional.

## 3. Frozen antecedents

The receipt must byte-lock these exact parents before importing any theorem:

```text
D34c source  185d36b9cd3dea684afcbe6b8bc1250ced8745cd0d0d74f277351818d4825a12
D34c output  9ce73a693b41f765eff163749ef769ca0cb4ce856ead66d690a63a20331a731a
D39b source  22fbda6a9189a2f46cf64c0f33b943b952e702fce30828a37b6d462f5a1458d3
D39b output  3e3ec1b1ab0459bddc5106e1ba4fe459741af264bfb0c725c8d9edd41ddf85ea
D14 source   e0db2c65a7305ca92bf19fcaa0384e0d825b1a04a5590c0ca243e6192bebc425
Paper 15     7449e02f21c6d74a9899febb65ad7819460df21e43d793e36e6faba024862635
Paper 18     71fafc639e98ba387bd7a70305f24f266feb68704273d77d9849ed687321d4bc
Paper 19     e58b973ae70084d3c715965f94e0e54d97868a2021c363a6375f1fc3a07402c4
Paper 28     ce625405657e539e1cdb77c4d0e3713d73422f6094797a6b420ea7209f5e067f
```

Any mismatch fails closed.  D40 may import exact finite constructions from
these files but may not silently upgrade their theorem ceilings.

## 4. Exact gates

### R0 — lock and vocabulary gate

Verify every parent hash and assert that the receipt uses separate typed
constructors for operators, history pairs, record cylinders, serial paths,
causal-DAG atoms, regional boundary rows and action-complex increments.

### R1 — classical descent and boundary sufficiency

Prove by exact finite enumeration:

1. every strictly positive refined cylinder measure gives equal conditional
   products for two paths to the same refined cylinder;
2. a coarse boundary supports a well-defined Markov kernel exactly when all
   refined states in each coarse fibre have the same pushed next-record law;
3. a negative fibre with unequal pushed laws is retained.

The receipt calls the second property **finite lumpability/sufficiency**.  It
does not infer sufficiency from locality of the underlying update rule.

### R2 — action-level and decoherence-functional interchange

Reuse D34c's exact `Q(sqrt(2))` fixture.  Gate disjoint incomparable
`CNOT(S,R)` and `Z(P)` operators, their class-operator branch vectors and
their decoherence functional under both construction orders.  Retain the
shared-support `H(S)`/`CNOT(S,R)` reorder as a negative control.  Gate strong
positivity and additive restriction on the finite history algebra.

### R3 — refined durable-record descent

On D34c's decoherent durable fields, construct the scalar cylinder measure
from diagonal decoherence-functional blocks and gate the refined click
cocycle.  Then record the exact hypothesis list:

```text
exclusive exhaustive durable alternatives;
decoherence of the queried record algebra;
one common refined cylinder or a declared pushforward atom;
positive conditioning denominator;
sufficient declared boundary.
```

### R4 — interference and coarse-boundary controls

Retain D34c's coherent/unrecorded-path distribution and its orthogonally
recorded-path distribution as unequal exact controls.  Exhibit a coarse
boundary fibre whose hidden setting or path datum changes the next-record
conditional.  The gate must say `SCALAR DESCENT UNLICENSED` or
`BOUNDARY INSUFFICIENT`; it must not call either case an action inconsistency.

### R5 — adjudicate the Paper 28 square

Recompute the chosen D38b `1/18` and `2/33` products.  Also enumerate the
corresponding complete two-event serial histories, typed causal-DAG
pushforward and any relevant boundary augmentation.  The decision is made by
the following exhaustive table:

| Exact finding | Licensed conclusion |
|---|---|
| same refined record cylinder; sufficient boundary; unequal products | genuine classical conditional inconsistency |
| distinct serial histories; common DAG atom is their sum | no equality requirement between serial weights; test summed pushforward |
| same full conditional but unequal projected products | projected boundary is insufficient |
| nonzero off-diagonal decoherence terms | click-level scalar cocycle is unlicensed |
| equal products after the correct refinement | Paper 28 obstruction was a level/boundary artifact |

No quantum explanation may be chosen merely because the two displayed
rationals differ.

### R6 — completion-ratio theorem

For any positive terminal weight `H` on a finite acyclic completion tree,

```text
K_H(x,y) = H(y) / sum_(z:x->z) H(z)
```

telescopes to a normalized terminal law.  Gate three interpretations:

```text
H = continuation count;
H = positive classical terminal weight;
H = decoherent quantum/Born cylinder weight.
```

This proves that the K-flat shape is an `h`-ratio/Doob completion form.  It
does not identify that form uniquely with Born's rule or select `H`.

### R7 — exact Bell-class architecture fixture

Use the exact Bell state and CHSH settings

```text
A0=Z, A1=X,
B0=(Z+X)/sqrt(2), B1=(Z-X)/sqrt(2).
```

Gate strong positivity of its branch Gram form, no-signalling, exact
`CHSH=2*sqrt(2)`, spacelike operator interchange and refined record-cylinder
cocycles.  Erase a load-bearing setting record and gate the resulting boundary
insufficiency.  This fixture establishes that Bell-class quantum records are
compatible with record-cylinder consistency when their record algebra and
boundary are correctly declared.

It does **not** show that a join is necessary for entanglement.  Edge-local
gates on a tree can already produce Bell and GHZ correlations.  D23's
in-degree-at-least-two result remains an identifiability ceiling, not an
entanglement-generation theorem.  Generated joining remains relevant to
connecting previously disconnected components and to the dynamic-adjacency
geometry question.

### R8 — identified-law dictionary audit

Audit Papers 15, 18 and 19 line by line for the objects the finite fixtures
had to supply:

```text
boundary/cosmological state;
measure, contour and renormalization prescription;
record/environment instrument and preferred durable algebra;
action-to-finite-operator discretization;
D15 event/field slots on generated records;
record-closed birth, interaction and join grammar;
clock/units bridge.
```

Construct at least two exact record instruments over one operator fixture
whose click laws differ.  Therefore the identified low-energy action content
does not by itself execute a unique record-closed click law.  D40 may classify
the missing comparison map; it may not pretend to run the full D15 law.

### R9 — universality audit

Every D33--D39 result used in Paper 29 must enter exactly one row:

```text
grammar/information-theoretic;
classical-generator-specific;
decoherent-record universal;
quantum-functional-specific;
bridge-dependent or still open.
```

In particular, bounded event support versus unbounded finite-stop predictive
width is not erased by the quantum audit; the numerical D38b action-square
failure is not promoted beyond the exact R5 adjudication.

### R10 — D26 experimental handoff

At `g=9/25`, reproduce the conditional same-line BORN visibility factor
`sqrt(1-g)=4/5`, the `N`-birth factor `(4/5)^N`, and the three-birth value
`64/125`.  TOKEN activation has no birth isometry in the declared model.

Pre-register the lab-facing discriminator without claiming data:

```text
prepare a monitored parent-line coherence witness;
hold the visible receipt class fixed where possible;
compare authenticated BORN and dormant-TOKEN activation;
condition on attachment topology and initiator line;
fit visibility versus counted same-line physical births;
reject the bridge if the parent-selective 4/5 law fails outside declared noise.
```

This is the first experimental handoff since Paper 19, not an experimental
result and not a rate bound without the same-line BORN dictionary.

## 5. Decision outcomes

D40 must end at exactly one primary noun:

```text
LEVEL-MISMATCH:
  action/DF consistency survives; Paper 28 compared serial or coarse shadows
  that were not licensed to satisfy one scalar square;

BOUNDARY-INSUFFICIENCY:
  a refined classical record law exists but the D38b star boundary is not a
  sufficient statistic for the compared conditional;

GENUINE-CLASSICAL-OBSTRUCTION:
  the two paths are one refined decoherent cylinder with sufficient boundary
  and the unequal products persist;

UNRESOLVED-D15-DICTIONARY:
  finite level theorems close, but no unique finite identified-law fixture can
  be constructed from the corpus without adding state/instrument/grammar data.
```

The last noun can accompany one of the first three: the finite D38b
adjudication and the audit of the physical D15 dictionary are different
questions.  Paper 29 is drafted only after an independent hostile review of
the receipt.  A hostile central finding is repaired before authorship.

## 6. Paper 29 claim ceiling

If the receipt survives, Paper 29 may prove the finite level/descent theorem,
classify the Paper 28 square, execute the Bell architecture fixture, sort the
phase theorems by universality and expose the exact D15-to-record-closed-law
dictionary debt.  It may not claim:

```text
a full Standard Model plus gravity click-law execution;
that K-flat is uniquely quantum or selected;
that joins are necessary for entanglement;
that D23's ceiling is a dynamical no-go;
that record-native clocks determine SI seconds;
new laboratory evidence;
or a unique law of nature.
```

