# D43d (N4) — the canonical-D* two-clock instrument on the generated
# families

**Status:** CAMPAIGN PIN (strict), 2026-07-19. Parents: D43 #327
(audit A6 — the v8 p12/p13 instrument unexploited by the d42b7
pilot); the extraction record (scratchpad n4_dstar_extraction.md,
session-preserved; algorithms ported code-faithfully from the
committed v8/code/{g2,i1,i2,j1}). Receipt:
`v10/code/d43d_dstar_generated_exact.py`.

## 1. Honest scope (pinned BEFORE the run; the extraction's facts)

At n <= 5 the dim<=2 gate is VACUOUS (all labeled posets pass; the
smallest 3D poset is S3 on 6 elements) and D* is floor-dominated
(the full n=5 rank-embedding range is [0.25, 0.35], ~10x the N=512
faithful band); the statistical layer has zero qualifying pairs
(nan, disclosed). THEREFORE: the family sweep yields DESCRIPTORS +
same-n landscape ranking only — NO manifoldlikeness verdicts. The
genuinely verdict-capable content is G3: the CONSTRUCTED transport
chains (n = 8..11) are large enough to FAIL dim<=2 — the first
sharp dimension-capable question posed to the generated grammar:
**are the generated record orders two-dimensional (= two-clock
configurations, paper 13) or does transport generate dimension?**
Either answer is a result; neither is pre-committed.

## 2. Gates

- **NG1 (port fidelity):** the ported oracle + exact-D* reproduce
  the committed facts: S3 (6 elements) REJECTED by dim<=2; all
  labeled posets pass at n = 4 (219) and n = 5 (4,231); the full
  120-permutation n=5 D* range = [1/4, 7/20] exactly.
- **NG2 (the n<=4 landscape + family sweep):** the exact D*
  landscape over all 219 labeled 4-posets; the d42a depth-4
  family's mu-weighted D* distribution located in it (percentiles
  MEASURED; mu weights the FAMILY average only — D* itself stays
  unit-weight canonical, per the committed convention; declared).
- **NG3 (the chain suite — the verdict-capable gate):** for each
  committed constructed chain (SIG-FM 8, SIG-KR 4, h5 5, h11 11,
  h12 12, CH 10 events; posets from the committed d42b1 layer):
  dim<=2 verdict; if 2D: canonical D* + all-orientations min/max +
  the parity-invariance check (the relabeling lemma) + the
  two-clock certificate (r1, r2 = the b/chi ranks, paper 13);
  if 3D+: the REJECTION exhibited with a witness (the incomparable
  structure that blocks transitive orientation). The dimension
  outcome is PRE-REGISTERED as open.
- **NG4 (disclosure):** the statistical layer's nan printed per the
  canon's C2 convention; the no-verdict scope line in the output.
