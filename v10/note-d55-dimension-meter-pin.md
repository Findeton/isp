# D55 — THE DIMENSION METER: shatter-5, and the sphere calibrations (PIN)

**Status:** PIN, STRICT, 2026-07-26.  Committed BEFORE any receipt
code.  Parents: D54 TERMINAL (#427–#429: the Dilworth gate as an
unconditional theorem of the transport layer; the 42-event record
shattering at three depths; round-1's corrected accounting — width
prices sky size, the shatter offset prices dimension, and halfspaces
in R^d shatter d+1 so the ladder continues upward).  Review: an
independent Opus 5 hostile round after green; nothing citable before
it.

## 1. The meter

D54's round turned the shatter ladder into a two-sided instrument:
shatter-4 separates a sky from the CIRCLE (arcs stop at 3), and
shatter-5 would separate it from the SPHERE (caps stop at 4, VC
dimension 4, Radon-certified in the round).  So the maximum k the
transport layer can shatter is the framework's **dimension
signature**:

| max shatter | compatible with | actor cost (Dilworth gate) |
|---|---|---|
| 3 | circle (2+1) | >= 3 |
| 4 | sphere (3+1) | >= 6 |
| 5 | S^3 (4+1) | >= C(5,2) = 10 |
| k | S^(k-1) | >= C(k, floor(k/2)) |

This unit asks the meter two questions: **does transport admit
shatter-5** (A1), and **which spheres do the realized families
actually fit** (A2).

## 2. A1 — the GENERALIZED courier builder, and shatter-5

D54's courier architecture is not m-specific.  The general builder,
committed here: given the dBTK symmetric chain decomposition
`scd(m)` (the committed D54 recursion), take one minting actor X,
m direction-actors, one accumulator per SCD chain, and one clean
courier per contaminating step, where a chain
`[S1 ⊂ S2 ⊂ … ]` (with the ∅-chain's ∅ supplied by X's first
delivery and singletons by the directions themselves) costs
`(|S1| − 1) + (len − 1)` contaminating steps.  For m = 5: 10
chains (1 of length 6, 4 of length 4, 5 of length 2), 26 couriers,
**42 actors, 84 events**.  Schedule exactly as D54 §8 A2: clean
first receipts, then mints, then one courier send per step in chain
order.

Gates: the builder re-derives the **m = 4 record as an anchor**
(same trace family as D54's — the general machine must reproduce
the hand-built one); the m = 5 build with every event selected from
the committed layer's own menu; capacity by the SC5 analog
(>= 5 directions, >= 32 distinct traces, empty present); the
shatter-5 witness or the refusing step; Dilworth consistency
(>= 10 contributing actors; the realized family's min chain cover
= 10 exactly if all of B5 is realized).

**Tractability instrument, declared:** menus are enumerated
restricted to the step's initiator(s).  This cannot change
membership — `admissible(acts, e)` reads only `(acts, e)`, no actor
list — and the receipt GATES the equivalence on sampled steps
(full menu ∩ initiator = restricted menu).

**Pre-registered expectation: CONSTRUCTIBLE.**  Reasoning on the
record: nothing in the grammar caps deliveries; the courier method
is mechanical from `scd(m)`; the only failure mode D54 found
(backflow) is exactly what couriers price away.  Against it: five
of my seven pre-registrations this campaign were corrected by their
receipts.  **Pre-stated consequence of success:** the framework
does NOT cap at 3+1 via the ladder — the ladder measures capacity,
not selection, and dimension selection (if it exists anywhere) must
live in the MEASURE, which is the convergence question.  A refusal,
conversely, would be the framework's first genuine dimensional
selection and the more important result.

## 3. A2 — the sphere calibrations

- **A2a [expectation: REALIZABLE].**  The m = 4 family (all of B4)
  realized EXACTLY by caps on S²: d47a's own rational tetrahedron
  supplies the points; the receipt exhibits all 16 exact rational
  `(u, t)` cap certificates and verifies traces by Fraction dot
  products.  Upgrades D54's record from "not an arc system" to
  **sphere-compatible**.
- **A2b [THEOREM, to be certificated].**  NO five points admit cap
  shattering on S²: five points in R³ are affinely dependent, the
  dependence yields a Radon partition, and the positive-side subset
  is separated by no halfspace.  The receipt computes the exact
  dependence and the blocked subset for concrete rational 5-point
  configurations (including D54-round-1's) — the general argument
  is dimension counting and goes in the note.
- **A2c [expectation: REALIZABLE; runs only if A1 succeeds].**  The
  m = 5 family (all of B5) realized by caps on **S³**: five exact
  rational unit vectors in R^4 (e1..e4 and (−1/2,−1/2,−1/2,−1/2)),
  32 exact rational halfspace certificates by grid search over
  small integer normals.  Together with A2b: the m = 5 record's
  sky fits S³ and provably not S² — **the meter reading "beyond
  3+1" made concrete.**

## 4. What may and may not be concluded (pre-stated)

If A1 + A2 all land: transport admits skies of sphere rank >= 4
(S³-compatible, S²-impossible) — so **the admissibility layer does
not select 3+1**; the 3-vs-6-vs-10 actor costs are the ladder's
prices, and the selection question moves to the measure.  All
claims reading-relative to SKY-B with per-depth reporting (D54
K11's lesson: report every depth, the omission understates).  No
claim that any of this is *typical* — genericity under a measure is
not posable at transport scope (D52) and is not claimed.

## 5. Falsifiers / exits

A refusal in the m = 5 build: the refusing step and its menu are
the deliverable (exit 0) — and the blocking clause gets named.
The m = 4 anchor failing to reproduce D54's family: builder
breakage, exit 1.  A2a/A2c grid searches failing to find a
certificate: reported as NOT FOUND at the declared grid (never as
impossibility — one-sidedness); A2b's dependence failing to compute:
instrument breakage, exit 1.  Standard disciplines throughout (AST
scan per #403 MA-2; witness branches live and exercised per #354
F1; deterministic keys; no silent caps; depths printed).
