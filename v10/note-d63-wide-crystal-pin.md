# D63 — THE WIDE CRYSTAL: does tiling homogeneity compose with chart width? (PIN)

**Status:** PIN, STRICT, 2026-07-26.  Parents: D60 (the crystal —
the grammar tiles at sprinkling-band homogeneity with THIN charts;
residue 2 = the width ceiling, `max |D| = 3` at every `(M, R)` tried
at d = 2), D54 (the Dilworth gate — width is priced in ACTORS:
shatter-k needs ≥ C(k, ⌊k/2⌋) chains; the courier architecture with
backflow deliveries buys width), D58 (the repaired atlas — the sole
instrument; ω is a chart-size ratio along covers; the `|D| ≥ 4`
column is the sprinkling-width discriminant), the batch round
(comparators are RE-RUN, never re-typed).  User authorization: the
construction order "wide crystal → cocycle → descent-conditions"
(LOG #453 area).

## 1. The question

D60 left the sharp open question: **can ANY grammar record carry
sprinkling-grade chart WIDTH (`|D| ≥ 4` at d = 2) while keeping
sprinkling-band tiling homogeneity?**  D54 shows transport buys
width (its shatter records reach `|D| ≥ 4`+ but with homogeneity
0.357–0.386, far below the band); D60 shows tiling buys homogeneity
(0.77 band-interior, ceiling-free in rounds) but with `|D|` capped
at 3.  The unit asks whether the two MECHANISMS compose or exclude.

## 2. The objects

- **WIDE-BRICK(M, R, C):** the committed brick circuit (D60's
  blueprint, M ring actors, R rounds) spliced with C courier actors
  running D54-style cross-deliveries (backflow: deliveries join both
  ways) at a fixed printed cadence.  C = 0 must reproduce D60's
  brick EXACTLY (anchor).
- **DOUBLE-RING(M, R):** two coupled M-rings with inter-ring
  deliveries each round — the minimal 2D tiling with built-in
  cross-chains.
- Every record must be FORCED at D60's C1 grade: every event offered
  by the layer's own menu, every specification matching EXACTLY ONE
  candidate, all actors offered at every step.  A tie-broken or
  refused record is reported, not repaired silently.

## 3. Gates

- **W0** anchors: committed layers only (d42b1/d42b3 event grammar
  as used by D60's driver; D58's `atlas` imported by AST/text-slice
  from the committed receipt — no re-implementation); C = 0
  reproduces D60's published row (0.7692 / 0.6510 / 0 / 3) exactly.
- **W1** forcedness per §2, per record, gated.
- **W2** the atlas census per record: homogeneity, mean ω, `|D| ≥ 4`
  fraction, `max |D|` — at d = 2 AND d = 3, orderings reported both
  ways (the D60 lesson: the d = 2 and d = 3 orderings differ).
- **W3** comparators: D58's atlas re-run on the SAME eleven genuine
  sprinkling configurations (the committed band 0.6417–0.8000 at
  d = 2, `|D| ≥ 4` 0.425–0.650) in this receipt's own process —
  never re-typed.
- **W4 THE FRONTIER:** sweep C (and the double-ring coupling
  density) from 0 upward at fixed (M, R) and at two other (M, R)
  settings; print the (homogeneity, width-fraction) curve point by
  point.  No invented thresholds: "band" means the W3 band, "wide"
  means the D58 `|D| ≥ 4` column, nothing else.
- **W5** the interior control (D60's C7): boundary excision per
  record, so the frontier is read on the mechanism, not the prefix.
- **W6** sweeps' depth/caps printed; exit 0 for substantive
  negatives, exit 1 only on anchor breakage (W0).

## 4. Pre-registered expectations and falsifiers

**Lean (stated before the receipt exists):** couriers should raise
`max |D|` above 3 — D54's gate says width is an actor-count
resource and the couriers supply the chains — at SOME homogeneity
cost; the open question is whether the cost stays inside the band.

- **F1 (the grammar wall):** no configuration reaches `|D| ≥ 4` at
  d = 2 — then the width ceiling is a property of TILING records as
  such, and "transport generates dimension" (D45) does not
  transfer to crystals.  A first-class negative.
- **F2 (the trade-off wall):** width is reached only below the
  homogeneity band at every swept configuration — then homogeneity
  and width are competing resources at grammar layer; the frontier
  curve is the deliverable.
- **F3 (THE WIDE CRYSTAL EXISTS):** some configuration sits in the
  band AND carries `|D| ≥ 4` charts — then the tiling and width
  mechanisms COMPOSE, and this record becomes the substrate for the
  cocycle/tensor unit (non-identity transitions between wide
  overlapping charts — the next unit in the user's order).

Whichever lands is the result.  **No verdict sentence may exceed
the swept range** — the claim is about the swept (M, R, C) family,
not "all records" (F1's wall statement must carry "at every
configuration tried" unless a proof is supplied).

## 5. Scope

Grammar layer.  No measure claim (transport scope has no measure —
B1).  No physical-object claim; a crystal is a MECHANISM
certificate, never an object (#440, the scale doctrine).  Transfer
to the identified interactive click law runs through paper 29's
missing map (D59) and is not claimed.  ω is read as a chart-size
ratio along covers (D58 round-1 MAJOR 2), never as symmetric
overlap.
