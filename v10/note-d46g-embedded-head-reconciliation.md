# D46g — the embedded-head reconciliation (the #363 corpus obligation, discharged by formal versioning)

**Status:** RECONCILIATION NOTE, 2026-07-19 (D46 ladder step g; no
receipt needed — the resolution is record-level and edits NO frozen
artifact).  Parent: LOG #363 (the d44e round's discovery); the d44e
provenance labels (RG0b-iv) and the d44e round's verification.

## 1. The fact

The d42b2 click-refinement receipt embeds a copy of the d42b1
transport head that is PRE-#300 text: its merge-denominator rule is
the one d42b1's own round-1 F1 convicted and repaired.  The
divergence is real and exhibited (the D2H merge prices 1/16 under
the embedded head vs 1/24 under the terminal d42b1) and was never
reconciled post-#300 anywhere in the corpus.  The d44e round also
verified: the FORM-level census is unaffected (63/63 comparable
outcomes agree; the divergence is pricing-only).

## 2. The resolution: formal versioning

- The embedded head is henceforth the named layer version
  **d42b1@pre-#300** — a distinct, frozen, historical pricing layer.
  The terminal d42b2 receipt and every receipt that execs the
  embedded head gate that layer AS COMMITTED; the d44e RG0b-iv
  provenance label already marks this in-receipt.
- **Binding rule for all future work:** any NEW consumer of
  transport pricing MUST exec the terminal `d42b1_transport_exact.py`
  head (as d43d/d44b/d45b do).  The embedded head is quarantined to
  the frozen click-layer fixtures; citing a click-fixture merge
  PRICE requires the "@pre-#300" qualifier.
- **No frozen receipt is edited.**  The optional future receipt —
  re-pricing the click fixtures under the terminal head and gating
  which downstream numbers move — is REGISTERED as available but
  not scheduled: it becomes mandatory only if a pre-#300-priced
  number ever becomes load-bearing in a terminal claim (none is
  today; the d44e round's 63/63 form-level verification is the
  evidence, referee-carried).

## 3. Discharge

With this note committed and the binding rule in the LOG, the #363
standing obligation is DISCHARGED at the record level.  The
re-pricing receipt remains the named contingency.
