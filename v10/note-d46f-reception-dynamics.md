# D46f — reception dynamics (the census's typed-open arm)

**Status:** CAMPAIGN PIN (strict), 2026-07-19 (ladder step f, the
last).  Parents: D44e TERMINAL #364 (the per-type census is
complete, but the reception layer is ONE SHARED COPY-FORM TEMPLATE
— reception DYNAMICS typed and OPEN; the R6 obligation discharged
on its census arm only); D44f TERMINAL #372 (horn 2's operator
faces).  Receipt: `v10/code/d46f_reception_dynamics_exact.py`.
GREEN-UNREVIEWED per the D46 program pin.

## 1. The question

D44e gated the reception FORM per type (a copy-form template on
each type's own carrier/data) and honestly declared that this is
not layer-semantic reception DYNAMICS.  This unit asks the open
half: for each record type, what does the committed layer actually
DO to a receiver's state when that record is received — and is
that action (i) determined by the record's censused data, (ii)
isometric/NSE-compliant as the D25/D27 requirements demand, and
(iii) COMPOSABLE (receiving two records in either order)?

## 2. Gates (pre-registered)

- **RD0:** the D44e type inventory and carrier/data table re-run
  and re-anchored (11 types; 6,567 instances; the (actor,base)
  key) before any dynamics claim.
- **RD1 (the action map, per type):** for each type with a
  receiver, extract from the committed layer the ACTUAL state
  update (holdings/superseded/live-proposal changes) as a function
  of (receiver state, record) — mechanically, by applying the
  layer to enumerated instances; gate that it IS a function of the
  censused data (no hidden dependence).
- **RD2 (NSE/D25/D27 compliance of the DYNAMICS):** distinct
  pre-states must remain distinguishable after reception (an
  injectivity/isometry gate on the ACTION, not on a copy-form
  template), with a firing lossy control.
- **RD3 (COMPOSABILITY):** two receptions in either order compared
  exactly — commuting where the records touch disjoint structure,
  and the exact obstruction exhibited where they do not (the
  D44f foliation lesson at reception grain).
- **RD4:** honesty — anything not reachable at fixture scope is a
  printed declaration; purity; determinism; no unconditional gate.

## 3. Scope

Fixture scope (the committed families and click/SIG fixtures);
D25/D27 are cited as requirements, not re-proved.

## 4. Result (2026-07-19; GREEN-UNREVIEWED) — 29 PASS / 0 FAIL

**RD1 — the action map EXISTS and is a genuine function.** Over
23,069 reception instances and 228 distinct pre-states, the
committed layer's state update is a function of (record, receiver,
pre-state): 869 repeated keys, ZERO conflicts — no hidden
dependence on history, index, poset, or unseen events (the control
fires: 635/1107 conflicts on the layer's index-valued created
field). Per type: p adds a proposal; r resolves its ckey,
supersedes, creates, and endows only AUTHORS; n is the IDENTITY;
d endows the receiver in 44 instances and is IDENTITY for 112
bystanders; m supersedes two and creates one.
**DELIVERED FINDING RD1-d: the click records ko/kc/ka are the
EXACT IDENTITY on reception state** — view-transparent, their
content entering only the poset and the pricing. Unreachable from
D44e's copy-form template; this is what the dynamics arm was for.

**RD2 — injectivity, with two honest failures.** p, r, n, m, and
all three click types are injective. **(i) DELIVERY IS NOT
INJECTIVE**: the layer admits re-delivering an already-held
version (deliver_options_in_view filters the SENDER's holdings
only) and holdings is a set, so reception FORGETS delivery
multiplicity — exact witness printed. **(ii) version records act
by idempotent set insertion**, colliding on 72 v.arb fibers (v0
never — universally held at 228/228; v.mrg unreachable at this
scope, reason printed). Lossy control fires at 171 fibers.

**RD3 — RECEPTION IS COMMUTATIVE; THE OBSTRUCTION LIVES IN THE
MENUS.** All 7,163 co-receivable pairs commute — including the 63
with overlapping footprints: the reception state is an ABELIAN
MONOID under reception, strictly stronger than the pin's
disjointness gate. The order-dependence D44f found therefore does
NOT live in the state: the exhibited obstruction is that the SAME
three records in two orders give IDENTICAL sigma for both actors
while yielding components 1+1 vs 2 and arbitration menus
1 x 1/4 vs 2 x 1/8 per actor. The named datum: the CAUSAL
COMPARABILITY of the two proposal receptions. Gated asymmetry —
**the PROPOSE menu is state-determined; the ARBITRATE/MERGE menus
are causally determined.**

**RD4.** The three D44e residual grains re-declared and gated; the
receipt is FLOAT-FREE (D25 enforced in its exact classical form:
injectivity), purity walk 41,326 leaves / 0 impure with a firing
trip control; determinism by reversed-traversal hash identity;
self-scan needle concatenated. Deviations: RD2/RD3 outcomes are
stronger/other than the pin's phrasing anticipated (universal
commutation, so the obstruction was relocated honestly to the
derived menu layer and exhibited there); one new declared probe
fixture (PROBE-DD) to reach the re-delivery degeneracy; RD3's
second control is structurally vacuous and printed as a gated
declaration rather than dropped.

## 5. Round-1 amendments (2026-07-19; round frozen at
## reviews/d46ef-round1-hostile-review.md: REVISE, 2B/5M/5m/2n).
## THE ABELIAN-MONOID HEADLINE IS A CONSTRUCTION TAUTOLOGY.

**B1 (BLOCKER — the headline was never a discovery).** The
committed View(acts, pred, idxs) builds every field from a
DOWN-CLOSED INDEX SET, so order-independence is DEFINITIONAL; and
the extracted action ACT(s, e, a) = s join inc(e, a) is a JOIN
HOMOMORPHISM (referee probe: 255,840 tests, 0 violations).
Formally **RD3-b CANNOT FAIL once RD1-a passes** — mutant f1, a
demonstrably WRONG action map, still leaves RD3-b at 0
non-commuting, and no mutation separates the two.  ACT moreover
commutes on ALL 170,820 record pairs, not merely the 7,163
"co-receivable" ones, so the pairing rule hides nothing AND
constrains nothing.  **"ALL 7,163 CO-RECEIVABLE PAIRS COMMUTE — AN
ABELIAN MONOID" is WITHDRAWN as a delivered finding**: it is a
structural consequence of the layer's own state construction,
true and worth stating, but not discovered by this receipt.
RD1-b's "ZERO conflicts" is likewise ENTAILED by RD1-a (f1: RD1-a
fails, RD1-b passes).

**B2 (BLOCKER — the inference does not follow).** RD3-b
(intra-history concurrency) and RD3-e (two DIFFERENT histories
with different posets) are DIFFERENT NOTIONS OF ORDER.  **"D44f's
order-dependence therefore does not live in the state" is
WITHDRAWN** — the gates do not license it.  What each gate shows
must be stated separately.

**B3 (MAJORs).** RD1-d's click-identity "delivered finding" is the
View constructor's FILTER (View never mentions ko/kc/ka/n) — a
code reading, not a discovery; RD2-a's INJECTIVE verdict for r/m
is a POOL ARTEFACT (the referee exhibits an r collision as a map;
the receipt probed only d); 4,142 of the 7,163 pairs (58%) contain
a record whose action is the IDENTITY; RD3-c's 63/7163 control is
63 of 63 POSSIBLE; RD3-f's "gated asymmetry" is one example whose
real justification is an ungated code reading.

**B4 — WHAT SURVIVED.** RD1-a is genuine and valuable: the action
map IS a function of (record, receiver, pre-state), with the
control firing.  **RD2-b is ACQUITTED and the non-injectivity of
DELIVERY is REAL** — re-delivery is genuinely admissible
((True, 1/16) with B already holding v1) — and the receipt-built
PROBE-DD fixture is load-bearing (mutant f4 makes RD2-b fail).
Forward-corrected at LOG #401.
