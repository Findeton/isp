# The N-program synthesis: what D43 settled and what it opens

**v10 D43e — synthesis note (no new receipts; every claim below is
carried by a terminal unit's receipt and cited as such).**
Status: closes the D43 execution arc (LEDGER #327 pin; #340, #342,
#344 terminals). Successors listed in §4 are NAMED, not started.

## 0. What the program was

The D43 audit (note-d43-corpus-audit-and-the-next-step, #327) read
v1-v10 not from memory, graded the corpus's load-bearing joints, and
pinned four executable next steps plus far-field imports [I1]-[I5].
The four were run to terminal through the standard cycle (pin ->
receipt -> hostile round -> conversion -> delta):

- **N1 (d43a):** is the v1 collar generator an artifact of the
  Lie-Trotter splitting order — the oldest orphan in the corpus?
- **N2 (d43b):** does residue 1 (the root-free completion of the
  click law) survive on the generated grammar's own state chain?
- **N3 (d43c):** is the classical trilemma's quantum escape a real
  operator or a typed hole?
- **N4 (d43d):** does the canonical D* instrument see dimension in
  generated families, and what makes dimension appear?

## 1. The four settled statements

**S1 (d43a TERMINAL #340) — the foundation holds at the ray level.**
On the 1+1D lattice Dirac fixture, the collar bracket's operator RAY
K_par[beta] = (beta d_x + (1/2) d_x beta)(I - sigma_x) is
RULE-INDEPENDENT: EXC and Lie-Trotter admission rules produce the
same ray, differing only in per-mass constants — kappa(1/2) =
13/2304, kappa(1) = -1/72, exact [THEOREM at fixture scale, exact
order-12 series]. The sign flip between masses is itself a result:
no single c^2 > 0 shared generator serves both rules — rule-relative
data is CONSTANTS ONLY, never structure. The smeared transport
answer (referee T5) is adopted; tau is delta-odd in every channel.
v1's anchor survives its oldest unexamined dependency.

**S2 (d43b TERMINAL #344) — residue 1 is decided on the computed
window.** The generated grammar's own intrinsic state object (menu
shape refined by probabilistic bisimulation, horizon-uniform) closes
at SIX states at lookahead 2. On it, everything residue 1 needs is
EXACT and small: lambda = 2 with rational spectrum; one dominant
class; the positive 2-harmonic function f = (4,4,3,7,3,3)/3 unique
up to scale (existence AND uniqueness — the root-free certificate =
YES); root = renewal state at every computable lookahead;
mass-transport identity exact with pi = (1,1,2)/4 [EXACT, Fractions
throughout]. Scope stated honestly: decided on every computed
window at d42a scope; the all-depth statement awaits the
RENEWAL-PUMPING CLOSURE THEOREM, whose two ingredients are now both
receipt-gated (the pad-shift identity, NC3 12,942/0; the renewal
subtree isomorphism + the 144-point clean-slate census, MG6).

**S3 (d43c TERMINAL #344) — the arbitration operator exists.** The
quantum escape from the classical trilemma is no longer only typed:
V_single (2x1) and V_pair (4x1) are CONSTRUCTED as Acceptance o
OpeningClick — isometric at 1e-40, Born statistics = the committed
K1 law exactly, and the SAME cut-independent matrices reconstruct
the committed menus at both cuts through the past-local index
[EXACT at fixture scale]. The pincer stands for cut/initiator-
attached channels; E-A (initiator-local randomized coins) is
EXCLUDED by the visibility-not-scheduling theorem. Open and
separate: the foliation face and the measure side.

**S4 (d43d TERMINAL #342) — transport GENERATES dimension.** The
canonical D* instrument, ported digit-for-digit, plus the dim<=2
transitive-orientability oracle, deliver a mechanism: pure
transport at SIX actors realizes exactly S3 (the first
non-2-representable order), FOUR actors first break dim<=2 at 6
events, THREE actors stay 2D through 10 events, TWO actors never
leave it. Actor WIDTH, not event count, is the capability axis
[THEOREM at the tested scales; witnesses gated permanently].
Multi-author arbitrations are the provably minimal untested corner.

## 2. The meta-result

All four of the program's original headlines were CORRECTED by
their hostile rounds, three as positive reversals: N1's fit-floor
build was replaced by exact series and the orphan closed at the
bracket level; N2's 17/23/29 growth was convicted as horizon
stratification and the true six-state object decided what the first
build could not; N3's extensional identity was convicted as theater
and replaced by a real operator; N4's width-blind chain suite had
its headline inverted by the W6/W4 witnesses. The receipts now
carry the corrected content AND the convicted first attempts as
gated negative controls. This is the discipline working as
designed: the adversarial cycle is not overhead on the result — at
this depth of self-built formalism it IS the result-producing step.

## 3. What this changes for the corpus

The audit's grading is now materially different at four joints: the
v1 anchor's oldest dependency is closed (A2 discharged at ray
level); the click law's completion question has a decided core (the
Perron reduction holds on the grammar's own chain, not just on
hand-built fixtures); the trilemma's quantum horn has an exhibited
operator, so "quantum mechanics is where the classical completion
fails" is now constructive at fixture scale; and dimension — the
standing embarrassment of a 1+1D-only program — has a generative
mechanism with sharp thresholds instead of a hope. The [I1] import
(Martin boundary/unimodularity) landed partially (mass-transport
exact at state-chain scope) and RELOCATES to the d42b1 transport
grammar, where deliveries reopen the absorbing sector. [I2] landed
as the visibility-not-scheduling exclusion. [I3]-[I5] are unspent.

## 4. Named successors (not started)

1. **The renewal-pumping closure theorem** — both ingredients
   receipt-gated (NC3 + MG6); the natural next theorem; upgrades S2
   from decided-on-window to decided.
2. **[I1] at transport scope** — rerun the d43b program on the
   d42b1 grammar where deliveries reopen {2,4,5}; the class
   structure will change; this is where Martin machinery is needed.
3. **The multi-author-arbitration corner** (d43d residual, provably
   minimal) — the last untested dimension channel.
4. **The LT log-smeared finite-slab theorem + exact kappa(m) with
   its zero crossing** (d43a B4/B6 successors); the interacting p15
   cross-check.
5. **The d42b4 per-type reception census** — the carried obligation
   the d43c round re-confirmed as open.
6. **Horn 2's remaining arms** — the foliation face and the measure
   side (d43c, open and separate).
7. **The paper-31-class decision** — whether the D42/D43 arc
   (papers 30 + the N-program) becomes the next paper; the user's
   call, with the publication route for paper 30 still open.
