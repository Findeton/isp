# GEN — THE GENERALITY CHECK: THE NT GEOMETRY ON A SECOND BASE (PIN)

**Status:** PIN, STRICT, 2026-08-07.  **Immutable base:** commit
fceb614 (NT TERMINAL #211).  **Authorization:** the user's
STANDING LONG-HORIZON ORDER 2026-08-07 ("continue, keep going.
Until we have full gravity relativity, qft, qcd") — the ladder
runs cycle by cycle under the standing machinery; per-cycle halt
discipline retained (blocked outcomes and user-owned decisions
still halt; publication and pushing remain the user's).  Lean
NONE.

## The question

Is the NT geometry a property of THE THEORY or of the one
committed W6 base?  Construct a SECOND base — genuinely
different — and re-measure the five patterns.

## The second base (declared, all completions PINNED as data)

Same structural species (two wings, a preparation, commuting
local legs, records at the final division event) — different
flesh: different wing dimensions (one wing enlarged, e.g.
qutrit-or-higher system side or enlarged pointer side), a
DIFFERENT preparation with its completion DECLARED EXPLICITLY
as data (the completion-relativity lesson: no silent orthogonal
completions — the completion is part of the arena declaration),
its own six-setting family or a declared smaller family (size
computed).  The worker declares the full base as data BEFORE
any transport measurement (§13(4); receipt gate order proves
it).  §15: the arena (admitted relabellings, switchings,
scopes) declared as data with sizes computed.

## The five pre-registered patterns (each gated separately)

- **P1** a nontrivial holonomy group exists (some closed loop
  non-flat).
- **P2** the group COMPUTED, whatever it is (permutation-tuple
  counting, never labels; closure measured; order and structure
  named).  Klein-four reproduction is NOT required — a
  different group is a DISCOVERY (the group as a function of
  base data), not a failure.
- **P3** TWO curvature sources (identification multiplicity,
  and the preparation's swap-defect) — each exhibited or its
  absence measured.
- **P4** the preparation-defect is an ELEMENT of the holonomy
  group (the "preparation manufactures a generator" pattern).
- **P5** non-principality: the group escapes the declared and
  admitted scopes (membership of every element computed against
  every declared collection).

## Pre-registered outcomes (only these)

- **GEN-STRUCTURE-REPRODUCES** — all five patterns hold.
- **GEN-STRUCTURE-VARIES-⟨list⟩** — some hold, differences
  COMPUTED (e.g. a different group; one source absent) — the
  variation is the result.
- **GEN-STRUCTURE-ABSENT** — no nontrivial holonomy: the NT
  geometry is a property of the first base, not the theory;
  the downgrade recorded plainly.
- **GEN-BLOCKED-AT-⟨object⟩** — census discipline.

## Discipline

ALL terminal lessons binding: §14 with every addendum
(fresh-eval, zero-hit gates; no gate references mutant
identity; analytically-forced clauses are disclosures;
symmetry instruments self-tested under their own action w/
sign/orientation mutants), §15 with every addendum
(declared-arena; matched coordinates — read time a declared
coordinate in every cell), permutation-tuple counting,
complete sweeps wherever feasible (any [SAMP] declared w/
size), positive + negative controls w/ teeth, measurement-
founded verdicts w/ flip-test, counts computed never typed,
exact arithmetic, byte-identical delivery runs, stall
doctrine, NO git, FREEZE-ON-DELIVERY, deviations ship.
Deliverables: v13/paper-gen-generality-check.md +
v13/code/gen_generality_exact.py + _output.txt +
_receipt.json.  Then §13: protocol freeze → three-lens panel →
adjudication → STATUS.  Successor per the standing order: if
REPRODUCES or VARIES → overlaps/autonomous charts (the cocycle
question) on whichever base is richer; if ABSENT → the
downgrade returns to the user before any successor.
