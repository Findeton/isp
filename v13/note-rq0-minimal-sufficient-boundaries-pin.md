# Strict analytical pin — minimal sufficient quantum boundaries

**Status:** `PIN-FROZEN / ANALYTICAL-RQ0-L0-ONLY`

**Date:** 2026-08-01

**Antecedent HEAD:** `9436b0713f33ad0fa2db4435058efc08261d00bd`

**Primary artifact (provisional title):**

> **Minimal Sufficient Quantum Boundaries: W3 Records, Process
> Experiments, and the Classical Center of the Future**

---

## 1. Authorization and immutable history

This pin opens one bounded, forward-only analytical RQ0-L0 successor. It
preserves unchanged:

- the terminal W3 record-transport result through v13 ledger #76;
- every rejected seam, Morita, localization, and computational cycle;
- the operational W3 comb paper, all three hostile reports, and their joint
  adjudication, v13 ledgers #90--#96; and
- the adjudicated one-chart results

  $$
  \texttt{RQ0-L0-COMB-COMPLETE-W3},
  \qquad
  \texttt{RQ0-L0-TESTER-SEPARATED-W3}.
  $$

The preceding test-sheaf headline remains withdrawn. This cycle does not
repair it and does not construct another tester-coordinate sheaf.

The authorized work is mathematical physics. No estimator, hidden fixture,
scorer, adversarial software harness, scientific receipt, topology,
influence, causality, field, or gravity construction is authorized. Small
calculations may check already-derived finite arithmetic, but no program may
choose a definition, theorem, candidate boundary, or outcome.

---

## 2. Central question

> For a typed cut inside one complete finite operational quantum chart, can
> the dependence of every admitted future process on the admitted operational
> past be compressed to a minimal sufficient quantum experiment, and does a
> canonical classical record interface follow from that experiment rather
> than from a supplied PVM?

The proposed physical principle is:

$$
\boxed{
\text{a boundary is the least operational interface through which all
future-task-relevant dependence on the past factors.}
}
$$

The proposed classical record is not assumed. It may be obtained only if an
intrinsic, presentation-invariant classical center exists and is represented
by admitted physical maps.

This is a selection-principle test inside one chart. It is not yet spatial
locality. A cut is a typed laboratory composition boundary, not a spacetime
hypersurface.

---

## 3. Why minimal sufficiency is being tested

The previous analytical cycle established complete process semantics and
tester-visible preservation/recovery, but it did not select one record
interface. The nine inherited branch-memory partition seams were not
exhaustive: an additional exact complex rank-two seam also passed the frozen
law. A more carefully typed tester site would organize those candidates but
would not explain why one is physically minimal.

Finite quantum statistics provides two relevant antecedents, whose scopes
must not be conflated:

1. statistical experiments formed by normal states on a von Neumann algebra
   admit minimal sufficient representatives unique up to normal
   isomorphism, under the established CP/Schwarz coarse-graining hypotheses;
2. finite families of channels can be compared by a single superchannel and
   by complete operational decision tests.

Neither theorem by itself proves that a state-level minimal compression of
comb Choi operators is an admitted process superchannel, that the resulting
object is an ordinary comb boundary, or that its algebraic center is a
physically accessible W3 record. Those are the new obligations.

---

## 4. One-chart ontology firewall

Every object belongs to one finite operational chart $D$.

- A cut $c$ is one typed port decomposition inside $D$.
- A past intervention closes only the ports before $c$.
- A future task closes or exposes only the ports after $c$.
- A tester is complete only for its declared process profile inside $D$.
- A future-task family is not a cover of an atlas.

No experiment, tester, state, minimization, or sufficient statistic ranges
over several regional charts. No global event set, global state, global
present, or atlas-wide tester is introduced.

The laboratory order of a comb is composition typing only. It is not an
emergent causal order.

---

## 5. Typed future experiments

### 5.1 Cuts and process profiles

Let $\mathsf{Cut}_D$ be a finite declared family of eligible typed cuts in the
complete process law of $D$. For each cut $c$, declare:

$$
\tau_c^-
\quad\text{and}\quad
\tau_c^+
$$

for its past and future wire profiles. A heterogeneous family of future
outputs may be compared only after an explicit classical flag and tagged
direct sum put every member in one common process type.

Coincident carrier dimensions do not identify process profiles.

### 5.2 Complete past parameters

Let $\Theta_c$ be a candidate-independent family of admitted complete past
instrument settings. A parameter $\theta\in\Theta_c$ must retain every CP
branch, its probability weight, its classical outcome, and the quantum
system delivered at the cut.

The preferred first representation is a flagged deterministic process for
each complete past instrument setting. If individual probabilistic branches
are used as parameters, the paper must give a normalization-preserving
embedding that retains their weights. Dividing branches by their weights and
forgetting those weights is forbidden.

### 5.3 Future task classes

Let $\mathfrak F$ be an independently declared family of complete future
tasks of one common typed profile. Linking the complete past parameter
$\theta$ into the chart and retaining the complete future task interface
gives a conditional future comb

$$
F_{c,\mathfrak F,\theta}.
$$

The resulting experiment is

$$
\boxed{
\mathsf E_{c,\mathfrak F}
=
\left(
\tau_{c,\mathfrak F},
\Theta_c,
(F_{c,\mathfrak F,\theta})_{\theta\in\Theta_c}
\right).
}
$$

The paper must prove that every member is deterministic or is part of an
explicit complete flagged instrument of the displayed type.

### 5.4 Presentation covariance

Typed basis changes, Choi-convention changes, outcome-handle permutations,
Kraus changes that preserve the complete instrument, and reversible
presentation superchannels must transport the entire family together. The
experiment is the operational family, not its source-code grammar or one
matrix realization.

### 5.5 No selected record input

The construction of $\mathsf E_{c,\mathfrak F}$ may not read:

- a proposed record PVM;
- the nine inherited seam labels;
- a fine/coarse partition;
- a desired center;
- a tensor-factor name; or
- a declaration that a task is “preserving” because it preserves the center
  later produced.

Future task classes must be fixed by operational controls before their
minimal boundaries are computed.

---

## 6. Operational comparison and deficiency

For experiments with one parameter set $\Theta$ and compatible types, write

$$
\mathsf E\succeq_D\mathsf G
$$

only when one admitted deterministic superchannel $\Gamma$, independent of
$\theta$, satisfies

$$
\Gamma(F_\theta)=G_\theta
\qquad\forall\theta\in\Theta.
$$

A separate transformation for each parameter is forbidden.

Define exact operational equivalence by mutual simulation. Where an
approximate diagnostic is needed, define the one-way deficiency

$$
\delta_D(\mathsf E\Vert\mathsf G)
=
\inf_{\Gamma\in\mathsf{AdmSC}_D}
\sup_{\theta\in\Theta}
\left\|
\Gamma(F_\theta)-G_\theta
\right\|_{\mathsf{Test}_D},
$$

with every type, tester profile, compactness hypothesis, and normalization
printed. The first cycle remains exact: zero versus positive deficiency is
the substantive discriminator.

The paper must prove, at its declared finite scope:

1. reflexivity and transitivity of exact simulation;
2. invariance under reversible process presentation;
3. monotonicity under admitted downstream decision processing;
4. attainment of every infimum it uses, or else use infimum language
   without claiming a realizing map; and
5. the exact randomization/decision-test criterion actually required for
   the declared comb class.

Existing state or channel comparison theorems may be invoked only after
their hypotheses have been matched to the displayed process object.

---

## 7. Sufficient coarse-grainings

A coarse-graining

$$
\Gamma:\mathsf E\longrightarrow\mathsf G
$$

is sufficient only if an admitted recovery superchannel $\mathcal R$ obeys

$$
\mathcal R\Gamma(F_\theta)=F_\theta
\qquad\forall\theta\in\Theta.
$$

This recovers the entire conditional future comb family, not only a selected
terminal probability table.

The paper must distinguish:

- statistical sufficiency of normalized Choi states under arbitrary CP
  coarse-grainings;
- process sufficiency under admitted deterministic superchannels;
- tester-relative zero deficiency; and
- a physical split/retract that is itself an admitted operation.

No implication among these levels may be used without proof.

---

## 8. Minimal sufficient boundary theorem

### 8.1 Required category

The paper must specify a finite-dimensional convex category
$\mathsf{Proc}_D$ whose objects are the process types actually used and whose
morphisms are the admitted deterministic superchannels. The hom-sets used in
the theorem must be closed, convex, and stable under composition.

If an idempotent superchannel does not split as an ordinary comb type, the
paper may pass to the explicitly declared Karoubi completion

$$
\operatorname{Kar}(\mathsf{Proc}_D).
$$

A Karoubi object is a repeatable process coarse-graining. It is not thereby
an autonomous subsystem or spatial region.

### 8.2 Minimality

A sufficient representative $\mathsf M$ is minimal only if either of the
following equivalent conditions is proved at the declared scope:

1. every admitted endomorphism of $\mathsf M$ that fixes all parameter
   processes is the identity; or
2. $\mathsf M$ is a retract of every sufficient representative of
   $\mathsf E$.

The desired result is:

$$
\boxed{
\text{existence of }\mathsf M_{c,\mathfrak F}
\text{ and uniqueness up to reversible operational equivalence.}
}
$$

The proof may use finite-dimensional compactness, a minimum-rank admitted
idempotent, and a mean-ergodic/Cesàro argument, but every closure and
power-boundedness hypothesis must be checked. It may not quote the
von-Neumann-state theorem as if it were already a comb theorem.

### 8.3 Completeness scope

The paper must state separately whether the theorem gives:

- an ordinary typed comb boundary;
- only an idempotent-split process object;
- only a normalized-Choi statistical experiment; or
- no minimal object in the admitted physical category.

Only the first two can earn the registered process-boundary rung, and the
second must retain its coarse-graining-only interpretation.

---

## 9. Minimal sufficient algebra and classical center

### 9.1 State-level antecedent

For a normalized finite family of states on a matrix algebra, the paper may
use or reconstruct the finite Koashi--Imoto/minimal-sufficient decomposition

$$
H
\simeq
\bigoplus_r H_r^Q\otimes H_r^N,
$$

$$
\rho_\theta
\simeq
\bigoplus_r
p(r\mid\theta)\,
\rho^Q_{\theta,r}\otimes\omega^N_r.
$$

At that scope the minimal sufficient algebra is, up to isomorphism,

$$
M
\simeq
\bigoplus_r B(H_r^Q)\otimes I_{H_r^N},
$$

and its center is

$$
Z(M)
\simeq
\bigoplus_r\mathbb C I_r.
$$

This is a theorem about the specified state experiment. It is not yet a
theorem about the physical comb boundary.

### 9.2 Liftability gate

To earn a physical classical center, the paper must construct admitted
process maps realizing the relevant compression, recovery, central readout,
and presentation transport. In particular it must answer:

1. Does normalized Choi-state sufficiency lift to one admitted superchannel
   for the complete process family?
2. Does the minimal process retract carry a canonical finite-dimensional
   algebra or operator-system structure?
3. Is its center invariant under every reversible operational
   presentation, rather than only under a chosen matrix representation?
4. Is the center readable through an admitted complete instrument?

A center of a convenient ambient Choi matrix algebra cannot pass merely
because it is easy to calculate.

### 9.3 Redundant structure

The factors $H_r^N$ may be classified as statistically redundant only for
the displayed experiment and transformation class. They may not be deleted
from the physical process unless the sufficient compression and recovery are
admitted.

### 9.4 Ambiguity

If several nonisomorphic minimal process objects or several inequivalent
liftable centers survive, retain the complete groupoid or report the precise
failure. No basis, projector package, or lexical representative may be
chosen by fiat.

---

## 10. W3 as a future-task comparison

Fix two independently declared nested future-task classes at the same cut:

$$
\mathfrak F_{\mathrm{pres}}
\subseteq
\mathfrak F_{\mathrm{full}},
$$

where the second adds at least one complete eraser/recovery experiment. The
inclusion is a statement about admitted controls, not about a candidate
record.

Let $\mathsf M_{\mathrm{pres}}$ and $\mathsf M_{\mathrm{full}}$ be their
minimal sufficient process experiments when these exist. A candidate
classical boundary $R_c$ must be derived from the relevant minimal object,
not supplied.

### 10.1 Preservation

The paper must say exactly which claim is tested:

1. the classical center alone is sufficient for every preserving future
   process;
2. the center is sufficient only for a separately typed record-readout
   quotient while within-sector quantum information remains relevant; or
3. the preserving minimal experiment has a noncommutative quantum part and
   no center-only sufficiency theorem.

Only claim 1 can satisfy the proposed equation

$$
\delta_{\mathrm{pres}}(\mathsf E_c,R_c)=0.
$$

Claim 2 is a weaker record-interface theorem and must not be called full
future sufficiency. Claim 3 is a negative for the W3 Markov-boundary rung.

### 10.2 Coherent recovery

For the same derived center and an independently enlarged future-task class,
require

$$
\delta_{\mathrm{full}}(\mathsf E_c,R_c)>0.
$$

The positive deficiency must be witnessed by a complete operational decision
task or an exactly applicable randomization theorem. A matrix off-diagonal
coefficient is insufficient.

### 10.3 Write and no-write

The complete source/write instrument must induce a nontrivial center
statistic whose correlation with the declared operational past is stated
exactly. The matched complete no-write experiment must fail that same
condition or yield an inequivalent minimal experiment.

No fine rays, coarse sectors, or record values may be inserted solely to
make the discriminator pass.

### 10.4 Markov terminology

“Markov boundary” means exact operational screening-off inside one
laboratory-ordered chart and relative to the displayed future task class. It
does not mean spacetime causality, a Markov random field, or a relativistic
domain of dependence.

---

## 11. Future-task fibration

Let $\mathfrak{FutureTasks}_D(c)$ be the category or preorder generated by
independently declared task inclusion and admitted task simulation.

The paper may construct

$$
\pi:
\mathfrak{Bound}_{W3}(D,c)
\longrightarrow
\mathfrak{FutureTasks}_D(c)
$$

only after the fibers have been constructed. A richer task family should
require an interface at least as informative in the exact simulation order,
when a typed comparison map exists.

This is a fibration or indexed family of task-relative boundaries, not a
sheaf and not a demand that unrelated process profiles glue. Physical
symmetries and nonunique minima remain groupoid-valued.

---

## 12. Mandatory analytical controls

The paper must analyze the following by hand at small exact finite scope.

1. **Classical statistic.** A finite classical experiment whose ordinary
   minimal sufficient statistic and center are recovered.
2. **Koashi--Imoto family.** A state family with nontrivial classical,
   quantum, and redundant factors.
3. **Purely quantum family.** A minimal full matrix algebra with trivial
   center.
4. **Redundant spectator.** A parameter-independent factor disappears at
   the statistical level and is removed physically only if compression and
   recovery lift to admitted process maps.
5. **Nonliftable shadow.** A CP compression of normalized Choi states that is
   not shown to be an admitted superchannel cannot certify a process
   boundary.
6. **Task relativity.** One fixed process law with two independently declared
   future task classes yields different minimal sufficient objects or
   centers.
7. **Within-sector quantum dependence.** A noncommutative block remains
   future relevant even though the minimal algebra has a nontrivial center;
   test whether center-only preservation fails.
8. **Branch-memory census.** Reclassify the nine inherited partition seams
   and the additional exact complex rank-two seam by superchannel
   equivalence, minimality, center, and deficiency.
9. **Continuous rotated candidates.** Determine whether a nonseparating past
   or future family leaves continuous center ambiguity.
10. **Tomographically separating past extension.** Enlarge the admitted past
    family before inspecting the result and determine which distinctions
    become physical.
11. **Write/no-write.** Use complete source instruments and retain all
    weights and disturbances.
12. **Same normalized branches, different weights.** They remain different
    process experiments when the source flag is accessible.
13. **Symmetry/no-selection.** Symmetry-related minimal boundaries remain a
    groupoid unless an invariant discriminator exists.
14. **Terminal RQ0-A.** Recover its declared record interface only as a
    control with supplied regions and physical maps; do not infer new
    localization.
15. **Task-smuggling mutant.** Choosing the future task family by first
    choosing a desired PVM must be detected as circular.

The branch-memory outcome may be positive, negative, or underdetermined. No
candidate is protected from elimination.

---

## 13. Four-gate audit

Every new object must be audited explicitly.

| Object | Referent | Necessity | No-smuggling rule | Discriminator |
|---|---|---|---|---|
| future experiment | complete conditional future comb family produced by admitted past instruments | a record should summarize future dependence on the past | no PVM, factor, seam, or center enters | unequal weights and presentation controls |
| simulation order | one admitted parameter-independent superchannel | equal tester laws do not provide a physical compression | no parameter-dependent map | exact positive and negative simulation pairs |
| minimal sufficient boundary | least admitted recoverable process retract | many W3-positive interfaces need a selection test | state-shadow compression does not automatically count | redundant and irreducible controls |
| classical center | canonical classical direct-summand information of the minimal object | the boundary may retain both quantum and classical information | ambient matrix center cannot be silently promoted | KI, trivial-center, and nonliftable controls |
| W3 Markov boundary | task-relative screening interface for preserving but not eraser-enlarged futures | W3 needs the classical/quantum continuation split | future tasks are fixed before the center | zero/positive deficiencies and no-write |
| task fibration | indexed family over admitted future roles | quantum memory can be instrument relative | no gluing across unrelated types | task-enlargement and symmetry controls |

---

## 14. Registered outcome ladder

The positive outcomes are cumulative only at their stated scopes.

### `RQ0-L0-FUTURE-EXPERIMENT`

Earned only if every eligible cut used in the paper yields a correctly typed
family of complete conditional future combs indexed by complete admitted
past instruments, with weights, outcomes, disturbances, tester quotient, and
presentation covariance retained.

### `RQ0-L0-MINIMAL-SUFFICIENT-BOUNDARY`

Earned only if exact process simulation and recovery are defined by admitted
parameter-independent superchannels, a minimal sufficient object exists in
the declared ordinary or idempotent-completed process category, every other
sufficient object factors through it, and uniqueness up to reversible
operational equivalence is proved.

### `RQ0-L0-CANONICAL-CLASSICAL-CENTER`

Earned only if the minimal process object carries a canonically defined
classical center, the center is invariant under operational equivalence, its
readout/compression maps are admitted physical maps, redundant factors are
removed without presentation fiat, and a merely statistical Choi-state
center is not substituted for it.

### `RQ0-L0-W3-MARKOV-BOUNDARY`

Earned only if the previous rungs hold and one derived center satisfies an
exact complete-instrument write/no-write discriminator, zero deficiency for
an independently frozen preserving future class, and positive deficiency
for an independently enlarged eraser-capable future class. The task-relative
boundary assignment and all ambiguity must be retained.

### Precise blocked outcomes

Report the first applicable obstruction without erasing earlier positive
rungs:

- `RQ0-L0-BLOCKED-AT-FUTURE-EXPERIMENT-TYPING`;
- `RQ0-L0-BLOCKED-AT-SUPERCHANNEL-SUFFICIENCY`;
- `RQ0-L0-BLOCKED-AT-MINIMAL-BOUNDARY-EXISTENCE`;
- `RQ0-L0-BLOCKED-AT-CENTER-LIFT`;
- `RQ0-L0-BLOCKED-AT-TASK-SELECTION`; or
- `RQ0-L0-BLOCKED-AT-W3-MARKOV-BOUNDARY`.

No outcome name may be invented after the paper is written.

---

## 15. Claim ceiling

Even the highest rung would mean only:

> inside one finite operational chart and relative to independently declared
> past and future instrument classes, a minimal sufficient process boundary
> has a physically represented classical center that screens preserving
> tasks but not a larger eraser-capable task family.

It would not establish:

- an actual selected outcome;
- W6 fact or event-token co-reference;
- autonomous subsystem control;
- a physical overlap between independently reconstructed charts;
- intrinsic spatial localization;
- topology or a manifold shadow;
- influence or causal order;
- dimension, volume, Lorentzian geometry, or special relativity;
- quantum fields; or
- gravity.

`RQ0-T1`, `RQ0-C1`, and every later unit remain prohibited.

---

## 16. Artifact, review, and stopping discipline

1. Commit this pin before writing the paper.
2. Write one self-contained mathematical-physics paper at
   `v13/paper-rq0-minimal-sufficient-quantum-boundaries.md`.
3. Commit the paper immutably before dispatching reviews.
4. Dispatch three independent repo-read-only hostile reviews:
   - quantum statistics, sufficiency, and randomization;
   - quantum combs, superchannels, and process decision theory; and
   - operator algebras, Koashi--Imoto decomposition, and classical centers.
5. Reviewers may use temporary scratch space but may not edit the repository
   or repair the paper.
6. Freeze each complete report verbatim in a separate commit.
7. Jointly adjudicate all three against this pin and the unchanged paper.
8. Do not repair the paper during review or adjudication.
9. Halt after adjudication and request explicit authorization before any
   successor.

The paper must cite primary literature for inherited sufficiency,
Koashi--Imoto, channel/superchannel comparison, comb, tester, and quantum
Markov-order results. Every extension to process experiments must be proved,
not inferred by analogy.

---

## 17. Immediate work order

$$
\boxed{
\begin{array}{c}
\text{freeze this strict analytical pin}\\
\downarrow\\
\text{derive or refute minimal sufficient process boundaries}\\
\downarrow\\
\text{derive or block the physical classical center and W3 criterion}\\
\downarrow\\
\text{freeze three independent hostile reviews}\\
\downarrow\\
\text{joint adjudication and halt.}
\end{array}
}
$$

No stronger event is authorized.
