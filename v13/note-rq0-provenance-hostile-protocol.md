# BRANCH-A HOSTILE PROTOCOL (FROZEN BEFORE DISPATCH)

**Status:** FROZEN, 2026-08-07, per RUNBOOK §13(1).  **Object**
(commit 6ee172c): paper `b0eb273fa352`; code sha256
eb290aa77675c3c3…; output/receipt at their committed SHAs.
**Pin:** ce18eac + amendment v2 (a05e3d5).

## Verdict vocabulary

ACCEPT / ACCEPT-WITH-FIXES / REJECT — plus per-rung confirmations:
(a) REGRESS both variants; (b) LOSSY both variants incl. the
V-AMP rank-residue construction; (c) the delta-zero theorem; (d)
BLOCKED-AT-THE-DECLARATION as the correct census instantiation;
(e) the scissors disclosure honest; (f) the process deviations
(mutant repairs; budget) correctly recorded.

## The four kill-shots (all attempt all; primaries below)

K1 — **The delta-zero theorem.**  Single-valued law ⟹ cycle rank
0 (the Euler argument E = mn, V = (m+1)n, C ≤ n): re-prove
independently; probe its edge (multivalued steps; the declared
amplitude family's closure); verify the 2,880-path rank-0
measurement and the empty holonomy family.
K2 — **The REGRESS reduction.**  The decoupling (admissibility
120 vs generation 0); the declarable-history set as a quadruple
function (the reduction to stage-5 Thm 5.3 — audit the proof);
the 120→1 certificate collision; the 512-lift/4-holonomy/128-each
structure at amplitude scope.  Attack: is there ANY
provenance-reading that escapes the quadruple bound (e.g. reading
the carried path's internal structure in a way (P1) does not
factor)?
K3 — **The LOSSY constructions + the scissors.**  V-CL's
single-class witness (identity and deleted-rotation in one
class); V-AMP's rank-1 unread residue (checkpoint-local rank 2 vs
diagram rank 3; the two lifts with identical shadows, different
cross-checkpoint holonomy) — re-derive; adjudicate the
reading-relativity scissors honestly (is the "declared numerals"
reading legitimately admissible, and does the unit's both-readings
disclosure suffice, or does one reading actually bind?).
K4 — **The discriminator findings + process.**  (P1)/(P2) passing
at every patch (rejection carried by inherited clauses — verify
per-patch); (P2)-strong's collapse onto rigidity; the backwards
fan-in (120 vs 20); the amnesty 4,845-ties (failure prior to
amnesty); THE FREEZE + MUTANT-REPAIR AUDIT (two mutants repaired
mid-unit — verify the repairs are real and the freeze-on-delivery
was honored; the source scan's negative controls).

## Lenses (independent; no cross-reading; own exact code; repo
## read-only; ONE file each)

R1 operator → v13/review-rq0-provenance-operator.md — K1 + K2
primary; ≥10 numbers.
R2 effectus → v13/review-rq0-provenance-effectus.md — K3 primary
+ the reduction's order-theoretic side; ≥10 numbers.
R3 instrument → v13/review-rq0-provenance-instrument.md — K4
primary; the ten deviations adjudicated; anchor fidelity
(B″/stage-5/branch-C); ≥10 numbers.

## Common gates

Paper-vs-receipt sweep; scope tags (variant- and reading-
quantifiers on every kill sentence); name-blindness maintained;
forbidden vocabulary; deviations complete; mutants/determinism/
floats; single-threaded.

## Discipline

Exact arithmetic; /opt/homebrew/bin/python3.13; stall doctrine
(< 8 min silent, ≤ ~40 min); no child agents; no git mutations;
freeze-on-delivery; final report = compact verdict + top
findings.
