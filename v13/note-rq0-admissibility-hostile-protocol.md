# CYCLE B″ HOSTILE PROTOCOL (FROZEN BEFORE DISPATCH)

**Status:** FROZEN, 2026-08-06, per RUNBOOK §13(1).  **Object**
(commit 55c62d6; sha256 prefixes): paper `d08a761081a7`; code
`c81f7f5530d1`; output `4e2c7bf5e5fe`; receipt `dfd4f9435fd9`.
**Pin:** 9576aee.  Reviews judged against THIS protocol only.

## Verdict vocabulary

ACCEPT / ACCEPT-WITH-FIXES / REJECT — plus per-rung confirmations:
(a) the axiom's four verdicts on the discriminators incl. the
measured (i-a)+(ii-a) joint kill; (b) the RIGIDITY theorem and its
identity-free control; (c) JOINT UNFORGEABILITY (Thm 6.1); (d) the
Feynman gate positive form AND its honest half; (e) the cost tower
with the exact-|Obs| proof and the pair-level impossibility; (f)
the rung pair GENERATIVE-ATLAS-AXIOM + BLOCKED-AT-CARRIER as the
correct pre-registered instantiation.

## The four kill-shots (every review attempts all; primaries below)

K1 — **Rigidity's reach.**  "At any identity-containing law, (i)
admits only the carrier's algebra" — re-prove or break it; test
whether the identity-free control is genuinely a law of the
committed class (closure checked) or a bespoke escape; determine
what rigidity means for stage 5 (are coarse patches EVER
admissible in physical laws, or does the axiom need a relative/
approximate form? — adjudication input, answer required).
K2 — **Joint unforgeability.**  The Pres(discrete) ⊆ Pres(forged)
argument: valid at what quantifier?  Construct an escape if one
exists (a law where the inclusion fails).  Check the measured
verification (four committed laws + the altered law) supports the
"no admitted law whatsoever" wording, or force a rescope.
K3 — **The cost tower.**  The cost = |Obs| theorem (deletions
only; additions never help; complement-is-a-law lemma) — re-prove;
test additions-never-help against a constructed adversarial
addition; verify 120/360/1260/3120 independently; check the
"inside the committed class no forgery at any price" wording
matches what is proved (identity-containing class only?).
K4 — **The entailment and the measured kill.**  (i) ⟹ (ii-a):
re-prove; if it holds, is (ii) honestly presented as
occupancy-only content?  The colluding-pair kill: re-run the
verdicts natively; confirm the witness; confirm member 2's
admissibility is CORRECT (not a leak — the collusion living in
member 1 is the paper's claim; test the pair under the axiom's
JOINT reading).  The Feynman gate: recompute 1 vs 3/4 and 0 vs
1/16; verify the honest half's no-admissible-comparator claim.

## Lens assignments (independent; no cross-reading; own exact code
## in separate scratchpads; repo read-only; ONE file each)

**R1 operator** → v13/review-rq0-admissibility-operator.md —
primaries K2+K3: the inclusion argument; the cost proofs; the
tower numbers; the altered-law verification; ≥10 numbers.
**R2 effectus/order** → v13/review-rq0-admissibility-effectus.md —
primaries K1+K4(entailment): rigidity re-proved; the identity-free
control's closure; the (i)⟹(ii-a) proof; the two-sided Galois
conditions (i-a)/(i-b) against the terminal Cycle B machinery;
the minimal witness re-derived by hand; ≥10 numbers.
**R3 instrument** → v13/review-rq0-admissibility-instrument.md —
primary K4(measured verdicts + Feynman): all four discriminators
re-run natively; the killing-condition witnesses; the Feynman
statistics recomputed; anchor fidelity to B/B′/#103/#111
committed values; the ten deviations adjudicated; the stage-5
consequence question answered from the instrument side; ≥10
numbers.

## Common gates

Paper-vs-receipt sweep (≥10 each); scope tags (finite; the
5-configuration carrier; the law families named; exhaustive
strata); forbidden vocabulary (reachability carries no spacetime
reading — verify the scope line holds everywhere); prose vs gates;
deviations appendix complete; mutants/determinism/floats from the
receipt; single-threaded.

## Discipline

Exact arithmetic; /opt/homebrew/bin/python3.13; stall doctrine
(< 8 min silent, ≤ ~40 min); no child agents; no git mutations;
freeze-on-delivery; final report = compact verdict + top findings.
