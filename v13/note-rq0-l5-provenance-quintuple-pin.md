# RQ0-L5 BRANCH A — THE PROVENANCE QUINTUPLE (PIN)

**Status:** PIN, STRICT, 2026-08-06.  **Immutable base:** commit
483311c (Branch C TERMINAL — its measured verdict is this unit's
preamble: the coarse present cannot testify, proved where symmetry
acts, principled where it does not).  **Authorization:** the
user's plan ("C then A"), C now dead with the record purchased.
**Adjudicator's prior, recorded per honesty:** at least one of the
two pre-registered kills fires.  **Scope:** finite; the committed
carrier, laws, configurations; name-blindness binding throughout;
no spacetime vocabulary.  Lean NONE.

## The extension

The patch becomes a QUINTUPLE (boundary, family, preparation, law,
PROVENANCE), where provenance is a DECLARED generation-history
component: a finite path of admitted operations claimed to have
produced the patch's boundary and records, carried as a chain of
fine-grained B″-certificates at its checkpoints (the terminal
Atlas-Axiom machinery evaluated BEFORE coarse-graining, carried as
data).  The provenance-reading admissibility axiom: the terminal
B″ conditions + (P1) the carried path is admitted by the law
(every step an admitted operation, composable, endpoint = the
declared patch) and (P2) each carried fine-grained certificate
verifies against the law at its own checkpoint.

## THE TWO KILLS (pre-registered; the user's diamond-analogy
## engraved; each gets a constructed adversary)

- **RQ0-L5-PROVENANCE-REGRESS.**  Provenance is the SIXTH
  declaration.  Construct the forged-history adversary: the
  committed manufactured patch carrying a MANUFACTURED history
  (an admitted path that genuinely produces the forged boundary —
  it exists iff the forgery is reachable, which B″'s cost theory
  prices).  The regress question, gated: does anything distinguish
  carried-true from carried-forged provenance WITHOUT presupposing
  the certification provenance was meant to provide?  If the check
  chain does not bottom out in something the law itself enforces
  (fine-grained checkability at the checkpoints), the kill fires.
- **RQ0-L5-PROVENANCE-LOSSY.**  The carried history is classical;
  no-cloning and the recorded-but-phased limit (v12 terminal) make
  it necessarily lossy about the quantum layer.  Construct the
  lossiness adversary: two admitted histories agreeing on every
  carried classical certificate, differing in the un-carried part,
  one producing the legitimate patch and one the forged.  If they
  exist, the kill fires.

## Discriminators (mandatory)

1. The committed forged patches with their TRUE histories
   (manufacture-paths): the axiom must REJECT (P1/P2 verdicts per
   patch, witnesses printed).
2. The same patches with FORGED histories: caught by the regress
   gate, or the kill fires — measured, not assumed.
3. The legitimate patches with their true histories: PASS.
4. The amnesty sweep over carried-certificate configurations; the
   name-blindness gate on every provenance-reading statistic; the
   B″ anchors (cost tower, counter-law, dichotomy) exit-1-only.

## Pre-registered outcomes (combinable)

RQ0-L5-PROVENANCE-CERTIFIES (the quintuple separates robustly,
both kills survived) / RQ0-L5-PROVENANCE-REGRESS /
RQ0-L5-PROVENANCE-LOSSY / RQ0-L5-BLOCKED-AT-⟨object⟩.  If both
kills fire, branch B (coarse arena-relativity) is entered as the
proven residue — that adjudication returns to the user.

## Discipline

Exact arithmetic; anchors exit-1-only on every terminal value
reused (C's verdict rows; B″'s axiom and costs; stage-5's
statistics); candidates/constructions frozen before fixture truth
(SHA-first + source scan — the honest freeze form from #166);
mutants over anchors AND derivation gates; byte-identical runs;
stall doctrine (< 8 min silent, ≤ ~45 min); NO git;
FREEZE-ON-DELIVERY; deviations ship.  Deliverables:
v13/paper-rq0-provenance-quintuple.md +
v13/code/rq0_l5_provenance_exact.py + _output.txt +
_receipt.json.  Then RUNBOOK §13: protocol freeze → three-lens
panel → adjudication → STATUS.

## AMENDMENT v2 (2026-08-06, the user's order — binding)

**"Ensure that in A we force keeping the amplitudes/quantum bits,
so that we can more easily see if it really works."**

1. **Provenance carries the FULL AMPLITUDE-LEVEL generation
   data**, not classical certificates only: the admitted path is
   carried WITH the amplitude-level description of each step (the
   operations' lifts/amplitude data as DECLARED-DATA descriptions —
   legal to carry: no-cloning constrains copying unknown states,
   not writing down the declared amplitudes of known admitted
   operations) and the checkpoint B″-certificates are computed AT
   the amplitude level.
2. **BOTH VARIANTS RUN, mandatorily:** (V-CL) classical-only
   provenance (the original pin's form) and (V-AMP) the
   amplitude-carrying form — same discriminators, same kills,
   side by side.  THE DELTA between them is a first-class
   deliverable: it measures exactly what the quantum bits buy.
3. **The LOSSY kill rescoped to where it honestly bites:** with
   amplitudes carried, lossiness moves to VERIFICATION — the
   checks are record-producing operations, and the
   recorded-but-phased limit applies to the CHECKS: gate whether
   checkpoint verification can consume the carried amplitude data
   or necessarily collapses to its classical shadow.  The lossy
   adversary is now posed against V-AMP: two histories agreeing
   on everything any admitted VERIFICATION can read, differing in
   the carried amplitude data, one legitimate one forged — if
   they exist, LOSSY fires against V-AMP too; if V-AMP survives
   where V-CL dies, the delta is the result.
4. The REGRESS kill unchanged, posed against both variants.
   Name-blindness, the honest freeze, and all discriminators
   unchanged.
