# CYCLE B′ HOSTILE PROTOCOL (FROZEN BEFORE DISPATCH)

**Status:** FROZEN, 2026-08-06, per RUNBOOK §13(1).  **Object**
(commit e0216c0; sha256 prefixes): paper-rq0-composite-boundaries.md
`fc94524d6ef2`; rq0_l1_composite_exact.py `52809c240345`;
_output.txt `cb520b01c1df`; _receipt.json `73dbdc4a1d5f`.
**Pin:** 62cdf68.  Reviews judged against THIS protocol only.

## Verdict vocabulary

ACCEPT / ACCEPT-WITH-FIXES / REJECT — plus per-rung confirmations:
(a) the factorization/support-space lemma and its carrier-≤20 +
lemma-carried scope; (b) the corrected witness (gap = joint
readability; gap + exact atom classification = entanglement),
including the parity refutation of the naive form; (c) the
discriminator both ways at the declared arena; (d) the
top-of-lattice guard escape; (e) the measured limits (aligned
manufacture descends; coarser context withdraws 51→1) honestly
framed; (f) the verdict rungs as the correct pre-registered
instantiations.

## The four kill-shots (every review attempts all; primaries below)

K1 — **The lemma's reach.**  The support-space identity
(E_A⊗E_B) ∩ (Z_A⊗Z_B) = (E_A∩Z_A)⊗(E_B∩Z_B) carries the 16
excluded pairs.  Prove it independently or find its gap; test at
least one excluded pair directly (carrier > 20) if feasible; check
the product-law generation claim (joint class GENERATED by locals)
is what the proof actually uses.
K2 — **The corrected witness's own soundness.**  Attack "gap +
rank-one atom classification = entanglement witness" with
adversarial tasks at the committed dimensions: mixed-rank atoms;
separable-but-not-classical constructions; rank-one entangled vs
rank-one product edge cases.  Does the classification remain exact
where the paper claims it?
K3 — **Overlap neutrality and the next-level smuggling.**  The
manufactured records fail because their blocks collide AT THE
DECLARED OVERLAP; the aligned-manufacture control shows descent
certifies fact-content-at-overlap, not absence of manufacture.
Adjudicate: (i) is every de-smuggling sentence in the paper scoped
to the declared arena (no arena-independent claim leaks)?  (ii)
THE ADVERSARIAL-PAIR QUESTION: under which committed laws could an
adversary manufacture BOTH contexts so the pair passes the
independence gate AND the forged record descends?  The paper need
not solve this, but it must not foreclose it — verify it is left
open honestly, or supply the construction if one exists (that
would be a MAJOR finding).
K4 — **The independence gate's ground.**  Independence is
law-relative (the relabelled context passes under the counter-law).
What grounds the law choice for the gate?  Is the gate's meaning
scoped correctly everywhere?  Does any sentence quietly treat
measured-independence as law-independent?

## Lens assignments (three independent reviews; no cross-reading;
## own exact code in separate scratchpads; repo read-only; each
## writes exactly ONE file)

**R1 operator** → v13/review-rq0-composite-operator.md — H1 primary
(K1 + K2): the support-space identity re-proved; the 30 subspace
intersections and 65-pair sweep re-verified (≥10 pairs
independently); the 8-task gap table with atom classifications
recomputed; the hostile-wing 10 composites; the SR multisets;
≥10 load-bearing numbers.
**R2 effectus/order** → v13/review-rq0-composite-effectus.md — the
descent order and guard primary (K3): the 160-family closure
reverification; the certified-set-omits-⊤ argument; the
descent-carrier absence claim; the covariance-obstruction
non-transfer (the carrying map moves the overlap — recompute);
the certified counts 51/52, 1/15, 1/5, 1/52 independently;
≥10 numbers.
**R3 sufficiency/instrument** → v13/review-rq0-composite-instrument.md
— the arena primary (K4 + K3(ii)): the declared-before-both
overlap order; the 120-relabelling independence sweep re-run
natively; the relabelled control's 6 carrying isomorphisms; the
counter-law pass disclosed correctly; the aligned and coarser
sharpness controls re-run; anchor fidelity to Cycle B's
constructed boundaries (centres 4/3/5, atom maps) and #103/#111
fixtures; the deviations (1)-(10) each adjudicated; ≥10 numbers.

## Common gates

Paper-vs-receipt number sweep (≥10 each); scope tags everywhere
(finite; fixture list; carrier caps printed; exhaustive vs
lemma-carried vs declared); forbidden vocabulary (locality,
topology, causality, spacetime, QFT, gravity — negations in scope
box/non-claims permitted); no arena-independent selector claim
anywhere; prose vs gates; the deviations appendix present and
complete; mutants/determinism/floats from the receipt; the paper
single-threaded.

## Discipline

Exact arithmetic; /opt/homebrew/bin/python3.13; stall doctrine
(< 8 min silent, ≤ ~40 min each); no child agents; no git
mutations; freeze-on-delivery; final report = compact verdict +
top findings (the file is the review of record).
