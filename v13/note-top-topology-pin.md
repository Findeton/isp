# TOP — TOPOLOGY ON THE LADDER (PIN)

**Status:** PIN, STRICT, 2026-08-08.  **Immutable base:** commit
e82c647 (TB3 #299 terminal).  **Authorization:** the standing
order (#212) — the main ladder's next serial rung.  Lean NONE.

## The question

The atlas now has named groups.  What is its GLOBAL SHAPE?
Read topology off the terminal structures: (1) the overlap
graph of TB3's 36-chart atlas — components, cycle structure,
higher connectivity (the nerve as a cell complex; its Euler
characteristic and homology-like invariants over F₂, computed
exactly); (2) DIMENSION-like invariants — is there a
consistent local dimension reading (the first manifold-shaped
question, posed as a measurement); (3) **THE FANO-RUNG
SELECTOR** — what structural property of the defect-order-2
locus selects the exclusive GL(3,2) visit (the locus
characterized inside the completion family; the selector
declared as a candidate family before fixture truth, per the
corridor discipline); (4) the gauge-inclusive holonomy's
topological content — does the wing factor (⋊ S₃) act on the
nerve, and what are the quotient's invariants.

## Pre-registered outcomes (only these)

- **TOP-GLOBAL-STRUCTURE-⟨computed⟩** — the invariant table
  (components, χ, F₂-homology ranks, dimension reading,
  quotient invariants), each computed by two independent
  routes.
- **TOP-FANO-SELECTOR-⟨named|NOT-FOUND⟩** — the selector
  identified within the declared candidate family, or its
  absence recorded with the family's failure computed.
- **TOP-MANIFOLD-READING-⟨CONSISTENT|INCONSISTENT-⟨witness⟩⟩**
  — the local-dimension measurement's verdict.
- **TOP-BLOCKED-AT-⟨object⟩** — census discipline.

## Discipline

ALL engraved rules.  The TB3 terminal receipt hash-pinned;
the atlas rebuilt from its declaration, not imported.
Deliverables: v13/paper-top-topology.md +
v13/code/top_topology_exact.py + _output.txt + _receipt.json.
Then §13: freeze → panel → adjudication.  Successor: the
continuum/type-III rung on whatever TOP earns.
