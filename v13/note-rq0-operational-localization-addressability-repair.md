# RQ0-L0 addressability repair — second held-out fixture is invalid at runtime

**Status:** `RQ0-L0-INVALID`; no scientific outcome.

After estimator v2 was frozen, a genuinely unseen globally encoded
(S_3^3) amplitude instrument was constructed.  Each factor uses an exact
two-dimensional monomial representation over (mathbb Q(\zeta_{24})), and
each carries its own W3 record candidate.  The typed black-box input has an
eight-dimensional carrier, 216 raw operational classes and all 46,656 ordered
composition rows allowed by the pin.  It contains order-three operations and
nonabelian local composition without any causal, coordinate, metric or field
data.

The constructor passed its exact source and type anchors.  The complete
encoded call to frozen estimator v2 did not return within 360 seconds.  It was
interrupted after 442.41 seconds while repeatedly canonicalizing exact product
signatures.  An unencoded development diagnostic returned one finest
((6,6,6)) factorization and a positive six-object record atlas in 349.97
seconds.  That diagnostic is not the encoded delivery and, in any event,
leaves no time for the mandatory controls.  It is therefore not promoted.

Section 14 of the repair pin makes the encoded cap hit procedural
`RQ0-L0-INVALID`.  No positive or negative scientific rung is claimed, and no
partial result is used to weaken the runtime gate.  Estimator v2 remained
byte-identical to commit `36290ef`, SHA-256
`124cc1545f91982268efd0b14108b59da9465bd6c748517170110ff2897b7cde`.
The opened fixture is frozen at SHA-256
`107b7dc7d8d5b5f51020da5f3fc071631e1bd8930c3bb1dcf1d86a28665e18de`
and is permanently retired from positive scoring.

The measured generic obstruction is narrower than the first failure: equal
group products are being phase-canonicalized thousands of times, and zero
entries cannot distinguish phase candidates.  Any exact cache/zero-skip repair
must be committed and source-frozen before another newly encoded fixture is
created.  `RQ0-C1` and all causal, geometric, field and gravity work remain
prohibited.
