# RQ0-L0 addressability repair — first held-out fixture is invalid at runtime

**Status:** `RQ0-L0-INVALID`; no scientific outcome.

The first fixture constructed after the estimator freeze was an exact,
globally encoded (S_3\times V_4\times C_2) amplitude instrument.  The
(V_4) factor used a projective Pauli representation, while the (S_3)
factor supplied an order-three operation and a nonabelian local composition
component.  Two W3 record candidates were derived on the noncommutative
factors.  The estimator saw only the typed operations, full flat composition
table, amplitudes, preparations/readouts and gauge declaration.

The constructor passed its exact type and frozen-source anchors.  It produced
an eight-dimensional carrier, 48 raw operations, 2,304 ordered composition
rows and two frozen record candidates.  The complete frozen estimator did not
return within the pin's 360-second cap.  The observed run was interrupted at
419.71 seconds in the generic direct-complement search.

Under section 14 of the repair pin, a cap hit is procedural
`RQ0-L0-INVALID`.  It is neither `RQ0-L0-BLOCKED-AT-ADDRESS` nor a positive
localization result.  No partial internal state is promoted, no atlas count is
reported, and no heuristic shortcut is allowed.

The estimator file remained byte-identical to commit `760d921`, SHA-256
`79c8d493d29dbc80d8760984da58cd5a8276d757216e13d20abb4404e52ffed3`.
The opened fixture is frozen at SHA-256
`fb5b671bd1264122563867f71a1fbeaf1ca0616f7628702d35f6e9eba011cea6`
and is permanently retired from positive scoring.

The pin already specifies the only clean successor: a generic performance
repair must be committed and source-frozen using public calibrations only;
then genuinely unseen fixture contents must be constructed.  The failed
fixture may serve only as a timing/calibration benchmark.  `RQ0-C1` and all
causal, geometric, field and gravity work remain prohibited.
