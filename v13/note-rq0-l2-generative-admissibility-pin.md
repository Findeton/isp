# RQ0-L2 CYCLE B″ — GENERATIVE PATCH ADMISSIBILITY: THE ATLAS AXIOM (PIN)

**Status:** PIN, STRICT, 2026-08-06.  **Immutable base:** commit
efa7224 (Cycles B and B′ TERMINAL; #103/#111 artifacts; the v12
corpus read-only).  **Authorization:** the user's word ("go
ahead"), resolving the stage-4 fork BY SYNTHESIS: branches A and B
fused — the binding mechanism is the patch's own generating
dynamics, which is committed structure in every cycle and
therefore NO NEW PRIMITIVE; branch C remains the measured fallback,
adopted only on evidence.  **Scope:** finite; one law family per
context, declared; reachability is an order on configurations and
carries NO spatial, causal, or temporal reading (forbidden
vocabulary: locality, topology, causality, spacetime, QFT,
gravity).  Lean NONE.

## The candidate Atlas Axiom

A patch (context: boundary + declared task family + admitted law)
is **ADMISSIBLE** iff:

- **(i) Galois-minimality, both ways:** the boundary is the minimal
  sufficient boundary OF its own declared task family (#103's
  minimality, anchored), AND the family is the closure of the
  boundary under the terminal Cycle B Galois machinery — the
  two-sided fixed point, now grounded rather than free-floating.
- **(ii) Generativity:** the declared task family is GENERATED BY
  THE PATCH'S OWN ADMITTED DYNAMICS — each task is one the
  boundary's realized process actually writes (operationalized via
  the terminal instruments: realized legs, reachable-subprocess
  restriction, the counter-law reachability machinery).  A family
  containing a task whose distinctions the realized process never
  writes is INADMISSIBLE, with the offending task exhibited.

## Mandatory discriminators (each gated, both directions)

1. **The colluding pair** (both panel constructions, anchored at
   their committed values) **must FAIL admissibility** — report
   per member WHICH condition kills it and by what witness (the
   expectation: (ii), since the forged families declare rotated
   distinctions their dynamics never writes — but MEASURE, do not
   assume).
2. **The legitimate eraser context must PASS** both conditions.
3. **The relabelled context** and **the counter-law context**: the
   honest verdicts, law-relativity disclosed where it bites.
4. **The arena's own two contexts** (now known dependent): the
   axiom's verdict reported.

## THE FEYNMAN GATE (pre-registered; first-class either way)

Does admissibility ever change a NUMBER?  Exhibit either an
admitted tester statistic that differs between an admissible patch
and an inadmissible patch at the same boundary, OR a
proof/measurement of empirical idleness at declared scope
(RQ0-L2-EMPIRICALLY-IDLE — a first-class outcome that would say
the axiom organizes description, not prediction).  No softening
either way.

## THE MINIMAL WITNESS (mandatory)

A smallest hand-checkable model (target ≤ 5 configurations,
rational entries) exhibiting: one admissible patch (both
conditions passing), one patch failing (i) only, one failing (ii)
only.  The paper prints it whole; the receipts gate it.

## THE COLLUSION-COST CONJECTURE (pre-registered, with its honest
## negatives)

Formalize FORGING COST: the minimal amount of admitted law that
must be altered (count of transition columns / admitted operations
changed, exact) to make a given inadmissible patch admissible.
Target theorem at declared finite scope: the cost is strictly
positive for the committed forgeries and GROWS along the tower
(record → boundary → context-pair → law), with the limiting
statement: TOTAL forgery = a complete alternative admitted law —
no longer a forgery within a world but the specification of
another (the Borges bound).  Honest negatives, first-class:
**RQ0-L2-CHEAP-LAW-FORGERY** (a small alteration suffices — the
closure refuted; branch-C evidence) or BLOCKED-AT-⟨cost-referent⟩
(the cost cannot be typed — census is the deliverable).

## Pre-registered outcomes (only these; combinable)

- **RQ0-L2-GENERATIVE-ATLAS-AXIOM** — both conditions earn;
  discriminators pass as demanded.
- **RQ0-L2-EMPIRICALLY-IDLE** — the axiom works; no tester
  statistic differs (the Feynman gate's negative).
- **RQ0-L2-CHEAP-LAW-FORGERY** — the conjecture refuted with the
  cheap alteration exhibited.
- **RQ0-L2-BLOCKED-AT-⟨object⟩** — census discipline.

## Deliverables and discipline

Paper: v13/paper-rq0-generative-admissibility.md (RQ0 house form;
scope box with the no-spacetime-reading line; non-claims;
deviations appendix per the #121 rule).  Code:
v13/code/rq0_l2_admissibility_exact.py + _output.txt +
_receipt.json — exact arithmetic only; anchors exit-1-only on
every reused committed value (Cycle B/B′ constructed boundaries
and verdicts; #103 minimality; #111 instruments); substantive
negatives exit 0; mutant table over anchors AND derivation gates;
no wall-clock in receipt or rendered output (byte-identical runs);
progress stream only (< 8 min silent); total ≤ ~45 min; NO git
mutations; FREEZE-ON-DELIVERY (final run = delivery; zero edits
after; no child agents).  Then per RUNBOOK §13: protocol freeze
BEFORE reviews; three-lens panel; SHA freezes; joint adjudication;
STATUS in the same commit.
