# RQ0-L0 public architecture reset — hostile review dispatch

**Date:** 2026-07-31
**Status:** DISPATCHED
**Review target:** immutable commit `e55cb68`
**Governing pin:** `c0fab11`
**External reviewer:** `/root/rq0_archreset_hostile`

## Scope

This is the mandatory pre-freeze hostile review of the public-only
architecture reset.  It is not held-out scoring, scientific L0 adjudication or
authorization for successor physics.

The reviewer is repository-read-only.  Independent scripts and results may be
written only in a fresh `/tmp` directory.  The reviewer may inspect the
delivery source but may not import any of these modules into its independent
mathematical rebuild:

- `rq0_l0_archreset_exact.py`;
- `rq0_l0_archreset_verifier_exact.py`;
- `rq0_l0_archreset_public_models.py`;
- `rq0_l0_archreset_public_audit.py`.

The review must explicitly decide whether the shared kernel's indirect reuse
of the legacy addressability arithmetic weakens the asserted verifier
independence.

## Mandatory attacks

### 1. Strict raw schema

Audit every parsing path for coercion, missing/unknown keys, duplicate or
missing rows, mistyped/nonfinite caps, invalid dimensions, boundaries,
projectors, gauges and references.  Independently reproduce all named hostile
mutants.  Determine whether the 481 normalized mutations cover every distinct
raw field class and identify any skipped semantic mutation.

### 2. Total fail-closed boundary

Attack request parsing, cap construction, schema parsing, estimation,
verification, neutral resolution, serialization and fallback.  Exercise the
Unix hard alarm, main-thread restriction, pre-existing alarm state and every
timeout/exception exit.  Evaluate the order-192 hard-cap result and keep
performance separate from science.

### 3. Verifier independence

Trace imports, caches and shared arithmetic.  Search for trust in supplied
pass flags, result membership, attached atlases, obstruction prose, novelty
flags, handles, expected factors or constructor truth.

### 4. Direct-factor lemma

Independently reconstruct the public `C2 x C3` positive and `Q8` negative.
Prove or refute P1--P8 soundness and completeness only for decompositions into
two through eight proper normal direct factors under the declared caps.  Check
normal-subobject enumeration, selectability, two-order mixed implementation,
commutation, faithful multiplication, closure, scalar intersections, sparse
represented-algebra arithmetic, restriction stability and negative
exhaustion.

### 5. Full regional maps and triples

Verify that `RegAddr` carries and validates every row's endpoints, `tau`,
source/target status, source/target result, independently supplied law and
signature, plus every preparation, context, probe, readout, record/dynamics,
projector and gauge map.  Rebuild the coherent and twisted triples.  All three
pair maps must first be valid full-instrument isomorphisms; only the loop
equation may fail in the twisted control.

### 6. Overlap-first quaternion calibration

Independently prove `Q8` direct-product indecomposability, rebuild its exact
monomial representation and W3 witnesses, and verify that operational scopes
and record scopes are reconstructed independently.  Check the central core,
three order-four regions, twelve arrows, three pair pullbacks, one triple
pullback, handle invariance and mismatch negative.  Attack the richness of the
test category, constructor truth and hidden-label circularity while respecting
the calibration-only claim.

### 7. Audit and claim discipline

Verify neutral positive/finite-negative/invalid outcomes,
`PASS/FAIL/NOT-REACHED` accounting, timeout exclusion from schema-mutation
success, source authentication, no fixture-specific branching and absence of
new hidden/held-out/official/score artifacts.  Check every statement in the
notes, LOG #56, RUNBOOK §16 and public output.  The ceiling is only a public
architecture candidate with scientific outcome null.

## Required report

Return exactly one of:

- `ACCEPT`;
- `ACCEPT-WITH-FIXES`;
- `REJECT`.

Rank findings by severity, cite file/line evidence, distinguish executable
counterexamples from wording/scope corrections, describe independent scratch
methods, and state separately which of the following are secured:

- strict schema;
- total boundary;
- verifier independence;
- two-through-eight direct-factor lemma;
- full `RegAddr`;
- coherent/twisted triples;
- overlap-first `Q8` calibration;
- `OpSub/RecSub` agreement;
- public audit neutrality.

No repair is authorized during or after this review in the present cycle.
`RQ0-T1`, `RQ0-C1`, topology, influence, causality, geometry, spacetime,
fields and gravity remain prohibited.
