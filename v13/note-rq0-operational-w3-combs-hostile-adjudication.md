# Joint adjudication — operational W3 combs and complete testers

**Status:** `ADJUDICATED / CYCLE-CLOSED`

**Date:** 2026-08-01

**Frozen pin:** `3c958da`

**Frozen paper:** `fa02148`

**Frozen dispatch:** `28e4387`

**Frozen reviews:** `3cffef2`, `cf0efab`, `56146f3`

---

## 1. Joint disposition

All three independent reviews agree on the registered outcome:

$$
\boxed{\texttt{RQ0-L0-BLOCKED-AT-TESTER-DESCENT}.}
$$

The cumulative rung decision is

$$
\boxed{
\begin{array}{rcl}
\texttt{RQ0-L0-COMB-COMPLETE-W3}
&=&\text{EARNED AT NARROW FINITE SCOPE},\\
\texttt{RQ0-L0-TESTER-SEPARATED-W3}
&=&\text{EARNED},\\
\texttt{RQ0-L0-W3-TEST-SHEAF}
&=&\text{NOT EARNED}.
\end{array}
}
$$

The strongest secure new positive result is therefore

$$
\boxed{\texttt{RQ0-L0-TESTER-SEPARATED-W3}}
$$

at finite-dimensional, definite laboratory order, one-chart, law-relative
admitted-tester scope.

The frozen paper is not repaired. Its test-sheaf headline and its exhaustive
nine-candidate wording are withdrawn by this adjudication. Its sound
complete-comb, tester-quotient, exact-control, inherited-candidate, variance,
and one-chart results remain immutable evidence.

---

## 2. Frozen evidence

| Artifact | SHA-256 | Disposition |
|---|---|---|
| strict pin | `ec1e1c5e6ba6e003daf1cb0ccb88f13a7cd709fb26b55614ec2a8465ff9ef1f9` | binding |
| paper | `c2c8a27df423b417bcc8e11b68288f7c7169fee0b0f4b8c18b5ca3f440f1b74a` | immutable, headline downgraded |
| comb/process review | `f021f5466432577e2b6c9a50a48730a1a782d39e7235cf365f50d7edc0e9f312` | complete; blocked at tester descent |
| categorical-instrument review | `af044306d4e78d1765e1b3ad3bf47449be44219a2d5c400e72235022cd3faf46` | complete; headline downgrade |
| tester-site/stack review | `f466003aa1b5851c61e4178cae26caa53763fb5e4808c6fc52bb1f46e64d1a8b` | complete; blocked at tester descent |

The three reports were produced independently against the unchanged paper.
All independently reproduced the exact imaginary/real controls and the
inherited branch-memory values. All accepted the one-chart firewall. All
withheld the third rung.

---

## 3. Adjudication of the first rung

### 3.1 What survives

The flagged-instrument construction retains the physical data whose absence
blocked the preceding cycle:

- every source CP branch and its subnormalized weight;
- complete write and no-write operations;
- fine and coarse sharp intervention branches;
- every continuation CP branch and its classical outcome;
- output disturbance and surviving quantum output; and
- admitted terminal and tail testers.

For a finite instrument $(\mathcal I_i)_i$, the flagged channel

$$
\widehat{\mathcal I}(\rho)
=
\bigoplus_i\mathcal I_i(\rho)
$$

is CPTP exactly when the branch sum is trace preserving. Projection onto the
tagged summand recovers each CP branch. Thus weights, outcomes and
disturbance are not replaced by normalized-state or POVM shadows.

The explicit four-level and branch-memory networks are correctly typed
finite compositions, and their closed experiments are complete. This earns
`RQ0-L0-COMB-COMPLETE-W3` at that explicit finite network scope.

### 3.2 Bounded printed defects

The following general formulas in the frozen paper are not accepted
literally:

1. With ascending tensor order
   $H_0\otimes\cdots\otimes H_{2n-1}$, the comb recursion must read

   $$
   \operatorname{Tr}_{2n-1}C^{(n)}
   =
   C^{(n-1)}\otimes I_{2n-2},
   $$

   unless a reversed factor convention or canonical permutation is declared.
2. A flagged Choi operator requires the corresponding canonical permutation
   of input, output and classical-flag factors.
3. Outcome-dependent output systems $K_j$ require a tagged direct sum
   $\bigoplus_jK_j$ before they form one channel codomain.
4. The output instrument must be composed into the downstream tester law;
   merely listing it as a field is not enough in a general theorem.
5. Retaining a classical flag preserves every branch as part of the process
   object. Zero tester distance becomes branchwise algebraic equality only
   if the admitted tester family separates the flag-conditioned outputs.

These defects restrict the general notation. They do not refute the complete
explicit networks or erase the first rung.

### 3.3 Fine and coarse interventions

The effect algebras satisfy

$$
C^*(P_r)\subseteq C^*(Q_k),
\qquad
P_r=\sum_{k\in K_r}Q_k.
$$

The associated Lüders instruments do not generally coarse-grain:

$$
P_r\rho P_r
\ne
\sum_{k\in K_r}Q_k\rho Q_k.
$$

They are separately admitted counterfactual interventions with different
disturbance. Any claim that $\mathbf R\subseteq\mathbf F$ as CP instruments
is withdrawn. The W3 support calculations, which use their separately
declared branches, survive.

---

## 4. Adjudication of tester-separated W3

### 4.1 Operational quotient

For a fixed complete tester $\mathbf T=(T_t)_t$,

$$
q_{\mathbf T}(X)
=
\sum_t|\operatorname{Tr}(T_tX)|
$$

is a seminorm on the real Hermitian process space. The supremum over the
admitted tester family is finite at the declared finite-dimensional scope
and remains a seminorm. Hence

$$
\|C-C'\|_{\mathsf{Test}_D}=0
$$

is exactly equality of every admitted closed-experiment probability.

If an admitted deterministic supermap $S$ pulls every downstream tester back
to an admitted upstream tester, then

$$
\|S(X)\|_{\mathsf{Test}_D}
\leq
\|X\|_{\mathsf{Test}_D}.
$$

Therefore the operational kernel is a congruence for the compositions used
by the explicit W3 networks. Simultaneous basis/Choi presentation changes and
outcome permutations preserve all closed probabilities.

### 4.2 Preservation and recovery

The paper's decisive definitions survive:

$$
\text{preservation}
\iff
\|V\star W-V\star\mathcal D_R\star W\|_{\mathsf{Test}_D}=0,
$$

and

$$
\text{coherent recovery}
\iff
\|E\star W-E\star\mathcal D_R\star W\|_{\mathsf{Test}_D}>0.
$$

Thus a complex amplitude that changes no admitted probability is not an
eraser witness. The recoverable process coherence

$$
\mathcal C_R(W;E)
=
\|E\star(W-\mathcal D_R\star W)\|_{\mathsf{Test}_D}
$$

is presentation invariant, monotone under tester-family inclusion, and
contractive under admitted tester-pullback supermaps. It is distinct from the
Born composition defect and from record-algebra multiplicativity defect.

### 4.3 Exact controls

All reviews independently reproduce:

| Control | Exact result |
|---|---:|
| imaginary individual cross term | $-i/4$ |
| admitted probability contrast for that model | $0$ |
| real-phase recovered coherence | $1/2$ |
| Lüders versus reprepare tester distance | $2$ |
| unequal source weights | retain $\ell^1$ weight difference |
| restricted nonfaithful tester family | distinct processes may have distance $0$ |
| added separating tester | exposes the hidden difference |

The same-POVM/different-disturbance and same-normalized-state/different-weight
controls establish that complete instruments, not effects or normalized
branches, are load bearing.

### 4.4 Effect-level theorem

For admitted terminal effects and a Hermitian-spanning source family,

$$
\|V\star W-V\star\mathcal D_R\star W\|=0
$$

is equivalent to

$$
\mathcal D_RV^*(a)=V^*(a)
$$

on the admitted effect interval, hence by linearity on its operator-system
span. Without source separation the block identity remains sufficient but is
not necessary. The multiplicative-domain characterization of sharp
proposition transport remains a separate zero-defect theorem and is not
promoted to complete-instrument equality.

These results earn `RQ0-L0-TESTER-SEPARATED-W3`.

---

## 5. Branch-memory adjudication

### 5.1 The inherited nine candidates survive

Within the inherited subfamily whose coarse projectors are spans of blocks
of the basis $(v_0,v_1,v_2,v_3)$:

- the six $2+1+1$ candidates pass and have
  $\mathcal C_R=5/4$;
- the three $2+2$ candidates pass and have
  $\mathcal C_R=1$;
- three-element blocks fail the fine-ray condition; and
- the all-singleton partition lacks the matched no-write discriminator.

For a double block $B=\{i,j\}$, the fine projectors are the rays

$$
Q_{B,\pm}
=
\left|\frac{v_i\pm v_j}{\sqrt2}\right\rangle
\left\langle\frac{v_i\pm v_j}{\sqrt2}\right|.
$$

The exact return and recovery formulas are

$$
p_{\mathrm{return}}(\pi)
=
\frac1{16}\sum_{B\in\pi}|B|^2,
$$

and

$$
\mathcal C_{R_\pi}
=
2\left(1-p_{\mathrm{return}}(\pi)\right).
$$

### 5.2 Exhaustivity is refuted

The frozen source family is not tomographically separating, so terminal
probability preservation does not force the operator block identity. The
comb reviewer supplies an exact additional candidate.

Let $\psi_j=U|j\rangle$ and define the Hermitian unitary $S$ by

$$
\begin{aligned}
S\psi_0&=i\psi_1,&S\psi_1&=-i\psi_0,\\
S\psi_2&=i\psi_3,&S\psi_3&=-i\psi_2.
\end{aligned}
$$

Set

$$
P_\pm=\frac{I\pm S}{2},
$$

and use fine rays

$$
f_{0,\pm}=\frac{\psi_0\pm i\psi_1}{\sqrt2},
\qquad
f_{1,\pm}=\frac{\psi_2\pm i\psi_3}{\sqrt2}.
$$

Each write state has one live fine ray in each coarse sector. Every no-write
state has probability $1/4$ on every fine ray. Dephasing by $P_\pm$ preserves
all frozen terminal probabilities, while $E=U^*$ gives return probability
$1/2$ and recovery distance $1$.

This sharp record action is not a $v_i$-partition action. Therefore:

> Exactly nine inherited partition candidates pass, but the frozen
> nonseparating law does not make them an exhaustive census of all sharp W3
> record actions.

`RQ0-L0-BLOCKED-AT-BRANCH-CLASSIFICATION` is not selected because the binding
obligation was to reclassify the nine inherited candidates, which succeeds.
The stronger unregistered exhaustivity claim is withdrawn.

---

## 6. Why the test-sheaf rung fails

### 6.1 First registered obstruction: incompatible process types

The paper fixes one tester category $\mathsf{Tester}_D(\tau)$ and one master
set $A\subseteq\mathsf{Tester}_D(\tau)$. It then tries to form coordinates
$X_{s,a,\Xi}$ for the same $a\in A$ and every

$$
\Xi\in
\{W,N,V\star W,V\star\mathcal D_RW,
E\star W,E\star\mathcal D_RW\}.
$$

These processes do not generally have one type:

- $W$ and $N$ terminate at the cut or retain the continuation slot;
- the linked preserving and erasing processes terminate after the slot;
- continuation outcome flags alter the output profile; and
- output instruments may have tagged outcome-dependent codomains.

A complete tester for one profile cannot be applied to another merely because
some carrier dimensions coincide. No deterministic fillers into one common
outer type and no multi-sorted family

$$
\coprod_\Xi\mathsf{Tester}_D(\tau_\Xi)
$$

is constructed. Hence the proposed evaluation product is undefined at its
claimed general typed scope. This alone blocks the third rung.

### 6.2 The candidate action is absent

Even after a hypothetical type repair, the paper does not construct the
candidate presentation-groupoid action required for

$$
[\coprod_s\mathcal E_s/\mathfrak S]^\#.
$$

An action must transport tester contexts, outcome alphabets, all named
comparisons and restriction maps, and must obey identity, composition and
naturality. Presentation changes generally move both processes and dual
testers. Stability or equivariance of the finite master tester family is not
proved. The displayed quotient contains presentation arrows only, while
active physical-symmetry arrows are claimed but never supplied.

Stackification cannot manufacture this missing prestack.

### 6.3 Raw coordinates are not physical descent

For one correctly typed tester inventory, the rule

$$
U\longmapsto\prod_{a\in U}X_a
$$

is a sheaf under union covers. This coordinate-gluing lemma survives.

It does not impose tester-conversion relations or common-comb realizability.
For example, let $T'$ be a binary tester obtained from $T$ by swapping its
outcomes. The singleton tables

$$
p_T=(1,0),
\qquad
p_{T'}=(1,0)
$$

are separately realizable and agree on the empty overlap, so the raw product
sheaf glues them. No one process realizes the pair, because physical
conversion coherence requires $p_{T'}=(0,1)$.

The paper honestly defines the realized image only at the terminal tester
context. That chart-complete locus blocks a favorable singleton from becoming
a physical seam by itself, but it is a global predicate, not a presheaf or
descent theorem for physical realizability.

### 6.4 Tester conversions

Classical outcome postprocessing acts by a stochastic affine map on one
tester's probability vector. A general inserted quantum control does not.
If $T'=S^*T$, then

$$
p_{T'}(C)=p_T(S(C)),
$$

and no state-independent affine $K$ need recover this value from $p_T(C)$
alone. The site's later reduction to subset inclusions also discards these
physical conversion arrows.

### 6.5 Surviving context results

The following statements remain exact:

1. universal zero preservation is downward stable under tester restriction;
2. an existential positive eraser witness is upward stable and may disappear
   under restriction;
3. contextwise-valid W3 seams therefore do not form a presheaf;
4. union coverage on a finite subset poset is a Grothendieck pretopology and
   generates a Grothendieck topology;
5. correctly typed raw probability coordinates glue uniquely; and
6. an incomplete singleton becomes chart-complete only through a physically
   realized extension at $A_D$ satisfying the complete W3 predicate.

These are valuable constraints on a future analytical construction. They do
not earn the registered test-sheaf rung.

---

## 7. One-chart and ontological scope

All three reviews confirm that the paper does **not** reintroduce an atlas-wide
tester or global state.

Every tester, probability table and terminal context belongs to one finite
operational chart $D$. A “global section” means a section over $A_D$ inside
that chart's tester site. It does not mean a global event set, a state on all
regions, or a simultaneous experiment across an atlas.

The following separations remain binding:

$$
\boxed{
\text{W3 seam}
\ne
\text{actual outcome}
\ne
\text{fact co-reference}
\ne
\text{event-token identity}.
}
$$

The comb wire order is a declared laboratory composition order, not a causal
order. No locality, physical overlap, topology, manifold, Lorentzian
geometry, field or gravity conclusion follows.

---

## 8. Exact retained result

At the adjudicated scope, the cycle establishes:

> Inside one finite operational chart, a W3 candidate can be represented as
> a complete flagged quantum process retaining all CP branches, weights,
> disturbance and output systems. The candidate is preserved precisely when
> record dephasing is invisible to every admitted complete tester, and it
> exhibits coherent recovery precisely when at least one admitted complete
> tester sees a positive probability contrast.

It also establishes the law-relative invariant

$$
\mathcal C_R(W;E)
=
\|E\star(W-\mathcal D_R\star W)\|_{\mathsf{Test}_D},
$$

with the exact controls and scope qualifications adjudicated above.

This is real analytical progress over effect-only or coefficient-level W3.
It is not yet a descent theory across tester types, and it is not locality.

---

## 9. First unresolved obligation

The first unresolved mathematical object is a correctly typed physical
tester-evaluation descent construction. At minimum it would require:

1. multi-sorted tester contexts indexed by each process profile, or explicit
   deterministic embeddings into one common outer comb type;
2. correctly typed tester pullbacks for quantum controls;
3. an explicit candidate/group-symmetry action on contexts, outcomes,
   comparisons and restriction maps;
4. a presheaf or fibration of physically realizable common-comb evaluations,
   not only arbitrary probability coordinates; and
5. an exact statement of what physical gluing can and cannot mean.

This adjudication does not authorize that repair. It records the obstruction
and closes the cycle.

---

## 10. Terminal claim ceiling

The final cycle status is

$$
\boxed{
\begin{array}{c}
\text{highest secure new positive:}
\ \texttt{RQ0-L0-TESTER-SEPARATED-W3},\\[1mm]
\text{cycle disposition:}
\ \texttt{RQ0-L0-BLOCKED-AT-TESTER-DESCENT}.
\end{array}
}
$$

The antecedent terminal
`RQ0-L0-ADDRESSABLE-W3-OPERATOR-SYSTEM` result remains unchanged at its own
law-relative selected-seam scope. The two results answer different questions
and are not merged into a localization claim.

No actual outcome, W6 co-reference, autonomous chart, generic physical
overlap, intrinsic localization, topology, influence, causality, spacetime,
field or gravity result is conferred.

Halt. Any repair or successor requires new explicit authorization.
