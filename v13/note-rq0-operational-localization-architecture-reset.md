# RQ0-L0 public architecture reset

**Date:** 2026-07-31
**Status:** PUBLIC ARCHITECTURE CANDIDATE — AWAITING HOSTILE REVIEW
**Pin:** `c0fab11`
**Scientific outcome:** `null`

## 1. Result of this cycle

This cycle constructs a reviewable public engineering architecture.  It does
not freeze an estimator, create a hidden fixture, run an official score or
claim localization.

The architecture contains four new executable layers:

1. a small exact arithmetic kernel;
2. an untrusted public proposer and one total fail-closed entry point;
3. a separately imported trusted verifier that re-reads raw data and raw
   claims;
4. opened public direct-product and overlap-first calibration models plus an
   adversarial audit.

## 2. Corrected finite lemma

### Statement

At the declared finite group-like scope, subject to the parser bounds and
registered execution cap, the direct-factor search enumerates every
decomposition into **two through eight proper normal direct factors** that
satisfies P1--P8.

The statement excludes one-factor tuples and tuples of nine or more factors.
It excludes nonnormal, non-product and general operator-algebraic localizations.
It is a calibration lemma, not the definition of a region.

### Why the negative search is complete at that scope

For a finite group `G`, every normal subgroup is the join of the normal
closures of its elements.  The verifier starts from every element normal
closure and closes the finite set under joins.  It therefore obtains every
normal subgroup at its declared enumeration cap.

It discards the identity and ambient group, enumerates all unordered tuples of
two through eight remaining normal subgroups whose orders can multiply to
`|G|`, and recomputes P1--P8.  A claimed finite negative is accepted only if
that exhaustive search finds no passing tuple.  Supplied obstruction prose is
ignored.

### Soundness of a returned tuple

For every accepted tuple, the trusted verifier independently establishes:

- **P1:** each factor is generated by independently selectable members;
- **P2:** every mixed row in both orders is implemented;
- **P3:** mixed products and exact row laws commute operationally;
- **P4:** the multiplication map is injective and surjective;
- **P5:** each factor is closed and inverse-stable;
- **P6:** group and represented-algebra intersections are scalar;
- **P7:** the represented factor algebras commute and their dimension product
  equals the ambient represented-algebra dimension;
- **P8:** every subtuple restricts to a closed faithful implemented region.

The proposer-supplied predicate table and pass flag are compared with, rather
than used by, this recomputation.

## 3. Exact sparse represented-algebra backend

The verifier encodes a monomial law directly by its nonzero flattened matrix
entries and performs exact Gaussian elimination over `Q(zeta_24)`.  This is
not an approximation.

The sparse and dense encodings are the same vectors in the same
`d^2`-dimensional vector space; deleting explicit zero coordinates does not
change linear dependence.  Pivot normalization and elimination use exact
field arithmetic.  Therefore the sparse rank equals the dense matrix-span
rank.  The public audit compares both implementations on the small public
suite.  The sparse backend is generic and contains no fixture handle, order or
factor truth.

## 4. Strict raw schema

The trusted boundary accepts only exact mappings and arrays with frozen keys.
It does not coerce strings, numbers or booleans.  It validates:

- dataset version, handles, phase modulus, carrier and boundaries;
- exact monomial laws and recomputed signatures;
- operation classes and independent selectability;
- every ordered composition row exactly once;
- row type, status, result, independent law and signature;
- preparations, contexts, probes and readouts;
- projector partitions;
- W3 record candidates and complete dynamics;
- gauge carrier and boundary type;
- all references and boundary compatibility.

An unavailable row may not carry a synthetic result, law or signature.  An
implemented or collapsed row must carry all three, and its independently
serialized law must equal the exact physical composite before quotient gauge
comparison.

## 5. Independent trusted verifier

`rq0_l0_archreset_verifier_exact.py` imports neither the proposer nor the
public models.  Its only project-specific dependency is the exact arithmetic
kernel.  It accepts no novelty Boolean, expected factor list, expected atlas,
obstruction string or result membership as proof.

For direct-factor claims it reconstructs the finite composition group and
P1--P8.  For overlap claims it independently reconstructs operational scopes
from contexts and record scopes from W3-certified supports before comparing
them with the raw claim.

## 6. Full regional maps

A raw `RegAddr` map carries:

- a carrier gauge action;
- every operation-class map;
- every row endpoint and the source/target row's `tau`, status, result class,
  independently serialized law and observed signature;
- preparations;
- contexts and every context field;
- probes;
- readouts and projector resolutions;
- records, record dynamics and record projectors;
- gauges.

The verifier requires exact source coverage and injectivity; an isomorphism is
also target-surjective.  It verifies all transports from the endpoint raw
datasets, so the carried metadata is evidence to check, not data to trust.

Composition constructs every field map.  The full triple test first verifies
all three pairwise instrument isomorphisms, then compares the two-step map with
the direct map field by field.  In the twisted public control, every pair map
is valid and only the loop equation fails.

## 7. Overlap-first public calibration

The public quaternion ambient instrument has no proper normal direct-product
decomposition.  The direct-factor verifier therefore returns a finite public
negative.

Independently, the overlap proposer derives four proper operational scopes
from composition-closed contexts: a central order-two core and three cyclic
order-four regions.  It derives four record scopes from W3-passing record
dynamics.  The trusted verifier recomputes both collections, requires their
agreement, validates the five raw instruments (including the ambient), twelve
full regional maps, three pair pullbacks and one genuine triple pullback.

The public mismatch control removes one record reconstruction while leaving
the operational reconstruction intact.  It returns a finite public negative,
not a positive atlas.

This is a designed, opened calibration.  It cannot earn an RQ0 rung.

## 8. Total fail-closed boundary

One outer boundary contains request parsing, exact cap validation, dataset
parsing, proposal, JSON identity severance, independent verification, neutral
outcome resolution and response serialization.

Every invocation returns exactly one of:

- `ARCHRESET-PUBLIC-PASS`, exit 0;
- `ARCHRESET-PUBLIC-SCIENTIFIC-NEGATIVE`, exit 0;
- `ARCHRESET-INVALID`, exit 1.

The first two are public calibration classifications.  Neither is an RQ0
scientific outcome.  Exceptions, nonfinite clocks, malformed caps, cap
exhaustion and serializer failures close to `ARCHRESET-INVALID`.

On the supported command-line entry, a Unix real-time alarm surrounds the
entire post-cap boundary, including deep schema parsing.  Off-main-thread
invocations and pre-existing real-time alarms are rejected because they cannot
provide this hard-cap guarantee.  A cooperative deadline is also checked at
phase and exhaustive-search boundaries.

## 9. Public adversarial audit

The public harness includes the exact hostile counterexamples required by the
pin and systematic missing-key, unknown-key, wrong-container/scalar-type and
array-element-type mutations across normalized raw schema fields.  Every
mutation re-enters through the total boundary.

The harness reports `PASS`, `FAIL` and `NOT-REACHED` separately.  A
`NOT-REACHED` control is excluded from both the pass numerator and reached
denominator.

Opened order-144 and order-192 products are engineering regressions only.
Their outcome or runtime can neither earn nor refute overlap-first
localization.

In the stored public run, order 144 completed positively in 207.644 seconds
with factor orders `(2,3,4,6)`.  Order 192 was interrupted by the registered
360-second hard cap and returned `ARCHRESET-INVALID` during trusted
verification in 361.062 externally measured seconds.  Thus this candidate is
not performance-qualified for a future held-out cycle.  The timeout is a
procedural limitation, not a direct-factor negative and not an L0 result.

## 10. Claims and nonclaims

### Constructed engineering objects

- strict public raw schema;
- public proposer;
- independent trusted verifier;
- total fail-closed wrapper;
- full regional-map schema;
- public Q8 non-product overlap calibration;
- coherent and twisted triples;
- adversarial public harness.

### Narrow theorem implemented

- finite completeness/soundness for two through eight proper normal direct
  factors satisfying P1--P8, subject to declared caps and types.

### Public measurements

- outcomes of the opened calibration suite;
- exact dense/sparse equivalence checks on the small public suite;
- mutation rejection counts;
- noncanonical public runtimes.

### Postulates

- the declared finite preparation, control and tomography access;
- the chosen exact real/cyclotomic finite presentation scope.

### Not claimed

- no estimator freeze;
- no held-out generalization;
- no `RQ0-LOCAL-ATLAS` or `RQ0-LOCALIZATION-GROUPOID`;
- no scientific L0 negative;
- no topology, influence, causality, geometry, spacetime, field or gravity.

The only possible terminal statement for this cycle is whether this public
architecture candidate survives an external pre-freeze hostile review.
