# Strict analytical pin — operational classical cores of minimal quantum boundaries

**Status:** `PIN / FROZEN BEFORE DERIVATION`

**Date:** 2026-08-01

**Immutable predecessor HEAD:** `dba40d4a30e476ac0ed5e4fd1e1b484ddcbe6049`

**Authorized mode:** bounded, forward-only mathematical physics

**Forbidden mode:** estimator development, software certification, hidden
fixtures, numerical scoring, or implementation-led definition

---

## 1. Binding correction and question

The predecessor theorem constructs, for a fixed compatible one-chart future
experiment, a minimal sufficient process object

\[
B_{\mathfrak F}=(X,e)
\in\operatorname{Kar}(\mathsf{Proc}_D),
\]

unique up to reversible operational equivalence. It does not supply an
observable multiplication or physically admitted central readout.

This cycle must not write an undefined algebraic expression such as
\(Z(X,e)\) and then call it a record. It asks instead:

\[
\boxed{
\begin{minipage}{0.88\linewidth}
Can the complete ancillary tester data of an earned minimal sufficient
process boundary define a matrix-ordered operational dual, and does that
dual possess a canonical finest admitted instrument whose outcome can be
read repeatedly without disturbing the entire minimal experiment?
\end{minipage}}
\]

The target is a physical operational split, not a manufactured associative
center.

---

## 2. Immutable antecedents

The cycle inherits, without editing:

1. `RQ0-L0-FUTURE-EXPERIMENT` at narrow finite common-profile scope;
2. `RQ0-L0-MINIMAL-SUFFICIENT-BOUNDARY` at finite-dimensional,
   compact-convex, tester-quotiented Karoubi scope;
3. complete comb and tester-separated W3 at their one-chart scopes;
4. total effect transport and partial sharp-proposition transport under
   admitted UCP maps;
5. law-relative operator-system observability and admitted-idempotent
   coarse-graining;
6. the operator-system counterexample
   \[
   S=\operatorname{span}_{\mathbb C}
   \{(I,I),(Z,Z),(X,2X+Z)\}
   \subset M_2\oplus M_2;
   \]
7. the corrected branch-memory task minima
   \[
   \mathbb C,\qquad \mathbb C^5,\qquad M_4\oplus\mathbb C;
   \]
8. the task-smuggling theorem: a candidate-matched future task can plant a
   selected PVM center; and
9. the separation between a record proposition, an actual outcome, W6 fact
   co-reference and event-token identity.

The new paper may use state-level Koashi--Imoto and minimal-sufficiency
theorems only at their exact operator-algebraic scopes. It must prove every
new process-level statement.

---

## 3. Primitive operational data

Fix one finite operational chart \(D\), one compatible common-profile future
task \(\mathfrak F\), and its earned minimal Karoubi boundary
\(B_{\mathfrak F}=(X,e)\).

Primitive law data are limited to:

- the finite-dimensional typed process spaces;
- their deterministic and subnormalized admitted combs;
- the admitted deterministic superchannels and instrument branches;
- the admitted ancillary systems and complete ancillary testers;
- sequential composition, classical outcome retention and coarse-graining;
- the operational equivalence relation generated by those complete tests;
  and
- the minimum process idempotent \(e\) supplied by the predecessor theorem.

No observable algebra, PVM, center, record handle, tensor factor, circuit
wire, local region or spatial interpretation is primitive.

### 3.1 Complete-admission scope

The paper must state explicitly whether the admitted tester law is closed
under every matrix amplification used in the proof. Mathematical ancillas
that the physical law does not admit may not be silently added.

At every matrix level \(n\), operationally null process perturbations must be
defined from the admitted \(n\)-ancilla tester probabilities. These kernels
must be compatible with direct sums, scalar conjugation and superchannel
pullback.

If the predecessor idempotent is only scalar-idempotent modulo a larger
scalar kernel and does not remain idempotent modulo the complete kernels,
the matrix-ordered rung fails. The paper must include a scalar-positive but
non-completely-positive idempotent control.

---

## 4. Stage A — matrix-ordered tester dual

Construct from the admitted ancillary tester data a finite-dimensional
matrix-order-unit space or operator system

\[
\mathcal E(B_{\mathfrak F}).
\]

The construction must specify:

1. the underlying complex involutive vector space;
2. every matrix cone;
3. the deterministic order unit;
4. the operational null subspaces and quotient;
5. Archimedeanization if required;
6. the pairing with complete process states; and
7. covariance under reversible process presentations.

For an admitted process map \(F:B\to C\), tester pullback should induce a
map

\[
F^\sharp:\mathcal E(C)\longrightarrow\mathcal E(B).
\]

The paper must prove, not assume, when \(F^\sharp\) is UCP.

For the minimum idempotent, the target is

\[
(e^\sharp)^2=e^\sharp,
\qquad
e^\sharp\ \text{UCP}.
\]

The paper must distinguish:

- an idempotent in a scalar ordered quotient;
- an idempotent in the complete operational quotient; and
- a UCP idempotent on the dual operator system.

No Choi--Effros multiplication may be invoked unless all of its physical and
mathematical hypotheses are independently constructed.

---

## 5. Stage B — nondisturbing classical instruments

Work in the Karoubi object \(B=B_{\mathfrak F}\), whose identity morphism is
\(1_B=e\).

A finite complete nondisturbing repeatable instrument is a family of
physically admitted CP process branches

\[
\mathcal M=\{M_r:B\to B\}_{r\in\Omega}
\]

such that:

\[
\sum_{r\in\Omega}M_r=1_B,
\]

\[
M_sM_r=\delta_{sr}M_r,
\]

and the complete flagged process

\[
\widehat{\mathcal M}:B
\longrightarrow
\bigoplus_{r\in\Omega}B
\]

is admitted with its outcome flag retained.

The Heisenberg branches

\[
m_r=M_r^\sharp
\]

must be CP on every admitted matrix level. Their physical outcome effects
are

\[
q_r=m_r(1),
\qquad
q_r\geq0,
\qquad
\sum_rq_r=1.
\]

The Boolean logic belongs first to the retained finite outcome flag. The
paper must not declare multiplication among the \(q_r\) unless it has been
constructed.

The equations are required on the minimal boundary, not on an arbitrarily
larger raw matrix algebra. A full matrix factor may correctly have only the
one-outcome instrument.

---

## 6. Stage C — operational classical core

Define a two-outcome operational split projection as an admitted CP
idempotent \(P:B\to B\) for which \(1_B-P\) is also an admitted CP branch of
one complete instrument.

The paper must determine whether all such split projections:

- commute;
- are closed under complement, sequential meet and coarse-grained join;
- form a finite Boolean algebra at finite-dimensional scope; and
- possess a unique atomic decomposition of \(1_B\).

Sequential meet and classical coarse-graining count as physically admitted
only if they are constructed from the declared process-category closure.

If these statements hold, define

\[
\operatorname{Cl}_{\mathrm{op}}(B)
\]

to be the instrument formed by the nonzero atoms of that Boolean algebra.
It is the finest complete nondisturbing repeatable classical instrument,
unique up to outcome permutation.

The construction must retain a trivial one-atom core as a positive result.
It may not force a nontrivial classical variable.

Every reversible physical automorphism of \(B\) must act on the core by an
outcome permutation. No representative may be selected from a symmetry
orbit.

---

## 7. Required comparison theorems

The paper must prove or refute each statement below.

### 7.1 Finite \(C^*\)-algebra control

For

\[
A=\bigoplus_kM_{n_k},
\]

with the standard complete CP process law, nondisturbing split branches of
the identity must be classified. If the central sector instruments are
admitted, the operational core should be the minimal central projections.

### 7.2 State-experiment control

For a finite minimal sufficient state experiment in Koashi--Imoto form,
the construction must reproduce its classical part when the standard
nondemolition central instruments are admitted. Quantum within-sector data
and redundant tensor factors must remain correctly classified.

### 7.3 Factor control

For \(M_d\), the core should be trivial. The paper must prove this from
complete positivity, not from a chosen basis.

### 7.4 Hostile operator-system control

For

\[
S=\operatorname{span}_{\mathbb C}
\{(I,I),(Z,Z),(X,2X+Z)\},
\]

the operational core must be computed from admitted maps on \(S\). The
nontrivial center of \(C_e^*(S)=M_2\oplus M_2\) may not be imported. The
required discriminator is a trivial operational core unless an actual
nontrivial split instrument is constructed inside \(S\).

### 7.5 Admission control

An algebraic central projection whose instrument is not admitted must not
appear in the operational core. Conversely, an admitted central-sector
instrument must be recognized.

---

## 8. Required exact analytical controls

The paper must analyze by hand:

1. a finite classical algebra \(\mathbb C^n\);
2. a full quantum factor \(M_d\);
3. a mixed algebra such as \(M_2\oplus\mathbb C\);
4. a Koashi--Imoto family with classical, quantum and redundant parts;
5. the hostile operator system \(S\subset M_2\oplus M_2\);
6. a scalar-positive idempotent that fails complete positivity;
7. a physically unavailable algebraic central instrument;
8. a symmetry that permutes two classical sectors;
9. the corrected branch-memory preserving minimum \(\mathbb C\);
10. the corrected eraser minimum \(\mathbb C^5\); and
11. the corrected retained-output minimum \(M_4\oplus\mathbb C\).

The branch-memory control must retain the common \(3/4\) sink. It must not
claim that the \(\mathbb C^5\) minimum selects a memory seam. The
\(M_4\oplus\mathbb C\) center, if operationally readable, distinguishes only
success from failure and must be tested for source-record correlation.

Small arithmetic checks may verify a displayed matrix after the analytical
derivation exists. They may not determine the definitions or outcomes.

---

## 9. Four-gate declarations

| Object | Referent | Necessity | No-smuggling | Discriminator |
|---|---|---|---|---|
| ancillary matrix tester | physically admitted coherent ancilla-assisted experiment | scalar positivity does not imply CP | same tester doctrine for every candidate core | transpose/symmetrizer control |
| matrix-ordered effect dual | complete tester-visible effects of one minimal process boundary | CP branches require matrix order | cones come from admitted tests, not an envelope center | equivalent presentations give complete-order isomorphism |
| nondisturbing repeatable instrument | retained flagged experiment that leaves the whole minimum unchanged when the flag is forgotten | record must be physically readable and repeatable | branches are admitted before their classical interpretation | factor, direct-sum and unavailable-instrument controls |
| operational classical core | finest admitted split instrument | removes arbitrary choice among readable decompositions | built from all admitted split projections, no PVM input | classical algebra, factor and hostile operator-system controls |

The admitted ancillary closure and sequential-instrument closure are explicit
operational postulates at this rung. Their consequences may be proved; their
physical availability may not be silently inferred from abstract CP maps.

---

## 10. Kill conditions

The positive ceiling is killed if any of the following occurs:

1. scalar operational equivalence is silently substituted for complete
   ancillary equivalence;
2. the minimum idempotent fails complete-kernel compatibility;
3. a map is called CP without all declared matrix levels;
4. an arbitrary \(C^*\)-envelope center is promoted to a physical effect;
5. branch maps are mathematical CP maps but not admitted process operations;
6. forgetting the outcome does not equal \(1_B\) on the entire minimal
   boundary;
7. repeatability or exclusivity is checked only on the experiment points;
8. a nontrivial core is forced when the unique physical core is trivial;
9. maximality is asserted without proving common refinement and uniqueness;
10. a preferred outcome label is selected against a physical symmetry;
11. the branch-memory sink is discarded or conditioned away; or
12. task selection, W3, locality or a later claim is smuggled into the core.

Any failed theorem must be reported at the first registered obstruction. No
replacement definition may be introduced after seeing a hostile control.

---

## 11. Pre-registered outcomes

Only the following cumulative outcomes may be reported.

### `RQ0-L0-MATRIX-ORDERED-MINIMAL-BOUNDARY`

Earned only if the complete ancillary tester quotient is an explicitly typed
operator system or matrix-order-unit space and the minimal process
idempotent induces a UCP idempotent on it, at a precisely stated physical
admission scope.

### `RQ0-L0-OPERATIONAL-CLASSICAL-CORE`

Earned only if the complete category of admitted nondisturbing repeatable
instruments has a canonical finest object up to outcome permutation, with
physical effects and postmeasurement branches retained. The core may be
trivial.

### `RQ0-L0-BLOCKED-AT-OPERATIONAL-CENTER`

Selected if the matrix lift, physical instrument typing, common refinement,
maximality, uniqueness or required controls fail. The report must name the
first exact failure and retain any earlier theorem.

No W3 outcome may be registered in this cycle.

---

## 12. Claim ceiling

This cycle may establish only a matrix-ordered minimal process boundary and
its maximal physically admitted nondisturbing classical instrument.

It may not claim:

- a selected actual outcome;
- W6 fact co-reference or event-token identity;
- task-independent record selection;
- a W3 Markov boundary;
- independent preparation or autonomous subsystem control;
- an intrinsic local chart;
- physical overlap;
- topology, influence or causal order;
- spacetime or Lorentzian geometry;
- quantum fields, QCD or gravity.

The words “boundary” and “classical core” remain one-chart,
future-task-relative operational terms.

---

## 13. Deliverables and chronology

After this pin is committed, create one primary analytical paper:

`v13/paper-rq0-operational-classical-cores.md`

provisionally titled:

> **Operational Classical Cores of Minimal Quantum Boundaries: Matrix-Ordered
> Testers, Nondisturbing Instruments, and Split Process Sectors**

The paper must be self-contained mathematical physics. No estimator, large
test harness, hidden fixture, scorer or scientific software receipt is
authorized.

After the paper is committed and immutable, dispatch three independent
repo-read-only reviews:

1. operator systems and matrix-ordered duality;
2. quantum sufficiency and complete instruments; and
3. effectus/order theory and operational classicality.

Each report must be frozen verbatim in a separate commit. Then freeze one
joint adjudication in a separate commit without repairing the paper.

After adjudication, halt and request explicit authorization before any task
selection or successor work.
