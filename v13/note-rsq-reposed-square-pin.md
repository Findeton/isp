# RSQ — THE RE-POSED SQUARE AT THREE WINGS (PIN)

**Status:** PIN, STRICT, 2026-08-08.  **Immutable base:** commit
e82c647 (TB3 #299, LCB #297 terminal).  **Authorization:** the
standing order (#212); the LCB adjudication's successor
requirements (#293) and TB3's bequests (#299).  Lean NONE.

## The question

LCB proved the OLD square (records↔metric vs
completions↔commutators, scalar intertwining) EMPTY UNIVERSALLY
— by the fixed-point mismatch in its registered clauses.  The
from-question therefore moves to the FLESH: is there a
DIFFERENT, honestly-motivated encoding pairing at three wings
whose square is not stillborn — and if so, does a bridge exist
through it?

## The construction (all as data before fixture truth)

(1) **HA AT d = 3** — the deformation side rebuilt with
3-dimensional fronts (per R2-LCB's measured requirement:
V: F_p³ → F_p⁶, det 8, spectrum {1,1,1,2,2,2}) at declared
primes incl. 7 (where the S₃ spectral values MEET per R2-LCB's
F-10).  (2) **THE TRANSPORT SIDE AT THREE WINGS** — TB3's
terminal ladder machinery (the commutator encodings now
decided).  (3) **CANDIDATE SQUARES DECLARED AS A FAMILY** — at
minimum: the d=3 record-is-metric encoding against the
three-wing commutator encoding with MODULE-LEVEL intertwining
over F_p[S₃] (the Z/2-sign condition's proper generalization,
per R2-LCB), plus any candidate the worker declares before
fixture truth.  (4) **THE STILLBORN PRECHECK (the LCB lesson,
structural):** every candidate square FIRST passes the
fixed-point-compatibility precheck — δ's fixed-point structure
vs E's fixed-space structure at that pairing — BEFORE any
census runs; a candidate failing the precheck is recorded
stillborn with its mismatch computed.  (5) If a candidate
passes: the full strengthened test (census-not-expression;
held-out prediction for FOUND; two-way gates, both reachable
w/ mutants).  (6) **THE PRIME SECTION at the new pairing**:
the scale-threshold table recomputed; p = 7's spectral
meeting tested as a measurement.

## Pre-registered outcomes (only these)

- **RSQ-NO-COMPATIBLE-SQUARE** — every declared candidate
  stillborn at the precheck; the mismatches computed (a
  first-class negative: the flesh-question answered "none at
  this family").
- **RSQ-SQUARE-FOUND-BRIDGE-EMPTY** — a candidate passes the
  precheck; the census returns empty; the obstruction named.
- **RSQ-SQUARE-FOUND-BRIDGE-FOUND** — the commuting square
  exhibited + held-out verified (the sub-object embedding
  form per BRG's one-way theorem).
- **RSQ-BLOCKED-AT-⟨object⟩** — census discipline (incl. HA
  at d=3 failing to construct, honestly).

## Discipline

ALL engraved rules (§13/§14/§15 every addendum; computed
qualifiers; multi-source verdicts; covariant cell sweeps;
cell-completeness; genuinely independent routes; declination
precedent).  TB3/LCB/HA/BRG/PSI terminal receipts hash-pinned.
Deliverables: v13/paper-rsq-reposed-square.md +
v13/code/rsq_reposed_square_exact.py + _output.txt +
_receipt.json.  Then §13: freeze → panel → adjudication.
