# HA — THE RECORD-NATIVE H_a[N] SUCCESSOR (GRAVITY LINE) (PIN)

**Status:** PIN, STRICT, 2026-08-08.  **Immutable base:** commit
024fcd7.  **Authorization:** the standing order (#212) + the
user's parallelization order — the roadmap's own parallel
gravity branch, activated (fourth concurrent cycle; ha_* files
only).  Lean NONE.

## The question

GW1 (v13 #2–#5, TERMINAL NEGATIVE) died BLOCKED at the
kernel/deformation interface: its residual
R_{HH,a}[N,M] = H_a[N] H_a[M] H_a[N]⁻¹ H_a[M]⁻¹ D_a[−β_a]
could not be run because no record-native H_a[N] existed.  The
unit's own successor directive (note-gw1-metric-from-closure.md
§successor, "construct a record-native H_a[N]") is this pin's
order.  DOES A RECORD-NATIVE H_a[N] EXIST AT FINITE N, AND DOES
THE DEFORMATION-CLOSURE TEST THEN RUN?

## The instrument (frozen before fixture truth)

The worker READS AS BINDING: v13/note-gw1-metric-from-closure.md
(the terminal unit — its Definitions 2.x, the residual, the
detector W, the successor directive verbatim) +
v13/note-gw1-metric-from-closure-pin.md +
v13/relativistic-isp-v13-paper0-gravity.md (the charter).
Construct H_a[N] : V_a^tot → V_a^tot on total finite
matter-geometry records at small N, RECORD-NATIVE (built from
the corpus's own record/division structure, not imported
continuum machinery), invertible as the terminal unit declares;
then RUN the GW1 residual and detector at the committed finite
scope.  DECLARED SECONDARY (one section, gated separately):
does the measured transport/holonomy layer (NT #211 / GEN #222,
receipts hash-pinned) supply the interface object GW1 lacked —
i.e., can R_{HH} be expressed in terms of the stitching
geometry's data (the defect law / the bigon group) at any
committed coordinate?  A yes or no BY MEASUREMENT, not analogy.

## Pre-registered outcomes (only these)

- **HA-RUNNABLE** — a record-native H_a[N] constructed, all GW1
  gates it unblocks RUN; the closure result reported WHATEVER
  IT IS (closure / anomaly / kernel obstruction), numbers
  computed.
- **HA-STILL-BLOCKED-AT-⟨object⟩** — the construction fails at
  a named object (census discipline; the obstruction
  characterized).
- **HA-BRIDGE-⟨POSABLE|NOT-POSABLE⟩** — the secondary's verdict:
  the stitching-geometry expression of R_{HH} is posable (and
  its first value computed) or not (the obstruction named).
- **HA-BLOCKED-AT-⟨object⟩**.

## Discipline

All engraved lessons binding (§13 / §14-all / §15-all).  GW1
terminal values + NT/GEN receipts hash-pinned, exit-1.
Positive + negative controls w/ teeth; measurement-founded
verdicts w/ flip-test; permutation-tuple counting where groups
appear; exact arithmetic; byte-identical delivery runs; NO git;
FREEZE-ON-DELIVERY; deviations ship.  Deliverables:
v13/paper-ha-successor.md + v13/code/ha_successor_exact.py +
_output.txt + _receipt.json.  Then §13 freeze → panel →
adjudication.  Successor: whatever the verdict earns, per the
standing order.
