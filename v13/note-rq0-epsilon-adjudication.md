# STAGE-5 — JOINT ADJUDICATION OF THE THREE-LENS PANEL

**Status:** ADJUDICATED, 2026-08-06 (v13 LOG #154).  **Object:**
commit d5eca4e, protocol d670892.  **Frozen reviews:** R1
operator (#151), R2 effectus (#153), R3 instrument (#152) — SHAs
in the ledger line below.

## Panel result

3 × ACCEPT-WITH-FIXES; 143 independent recomputations; zero
false computed numbers.  Convergent, from three directions, on
one adjudicated fact:

> **At this scope, the legitimacy of a COARSE patch is
> undecidable from its own declared data at its declared
> configuration.**

- ε is the boundary's BAYES ERROR (R2's identity) — it measures
  resolution, and the failure is ordering: the forgeries refine
  the legitimate chart, so every refinement-monotone statistic
  ranks them better, at EVERY state (R1's theorem; forward
  separation empty 4845/4845; the selective-amnesty state
  certifies the frauds and rejects the honest chart at τ = 0).
- ω has the right shape (anti-monotone, 0/4590) and is inert
  where needed (identically 0 at the declared configuration,
  260/260, structural: Reach ⊇ declared preparation; a forger
  attains ω = 0 at 31/31 declared preparations; the one
  separating law convicts the legitimate patch — the amnesty
  again) — and ω IS a quadruple function (R2: proved + 6118
  keys, zero escapes).  BLOCKED-AT-PROVENANCE stands correctly
  instantiated.
- The overclaim is cut to its airtight core (R3's F1): for
  SAME-quadruple pairs the impossibility is total; for
  distinct-quadruple coarse pairs, ε, ω, and the entire
  refinement-monotone class fail, and the general question is
  open under now-sharp design constraints.
- The MIXED-STATE arena is the FIFTH declaration with no closure
  route (R1) — registered here as an open obstruction.

## Repair list (dispatched; mechanical)

R-1 Thm 3.1 (⟸) gains the single-valued hypothesis (R2's
countermodels gated); Lemma 2.3's converse rescoped to
deterministic laws (forward direction kept general).  R-2 Thm
4.2's determinism hypothesis restored (R1's counterexample
gated; the claimed [EXH-5] sweep actually run).  R-3 the
reduction re-gated with varied preparations (R2's 25,657
instances adopted natively; the 3,117 ω-alone rejections as the
two-component measurement).  R-4 the ε = Bayes-error identity
and the Stirling-row spectrum adopted (monotonicity as
corollary); R1's every-state inversion theorem stated and gated.
R-5 the structural-blindness argument rewritten around the
collision witness (ordering-not-resolution); §5's "any
statistic" cut to same-quadruple-same-configuration.  R-6 the
selective-amnesty state and the reverse-separation census
(4540/4845) gated and disclosed.  R-7 the mixed-STATE arena
registered as an open obstruction with its no-closure argument.
R-8 L3-23's predicate made to compute what it claims; Thm 8.4's
premise gated (R3's AST route adopted); the L3-09 exhibit
de-degenerated.  R-9 deviation 8 corrected (the escape stands =
FALSE at both routes — PLURALISM-PRICED's registration reworded
per R3's F5).  R-10 all three rewrite tables (9 + 14 + 10);
deviations completed.  R-11 receipts/mutants extended over the
new gates.

## THE FORK (the user's; the arc holds at stage 5's exit)

The fine-grained world is certifiable (B″, terminal).  The
coarse world — the only one inhabited — is not, from within, at
this scope.  Three ways forward:

**(A) PROVENANCE-AS-DATA.**  The patch becomes a QUINTUPLE:
generation history as a component the axiom may read.  Forced
form of branch B; the cost is the primitive (the reconstruction
consumes history rather than deriving it).  The B″ machinery
extends naturally; the colluding fixtures and the amnesty states
become its discriminators.
**(B) COARSE ARENA-RELATIVITY.**  Accept: coarse legitimacy is
audit-arena-relative, permanently.  The ontology takes its
groupoid form at the coarse level; objectivity of the
macroscopic = invariance across declared audit arenas (which
arenas exist becomes the physical question).
**(C) THE CONSTRAINED STATISTIC HUNT.**  One bounded cycle: does
ANY quadruple function separate the distinct-quadruple coarse
pairs?  The constraints are now sharp (not refinement-monotone;
not Bayes-error-like; anti-monotone shape with non-vanishing
content at declared configurations; the same-quadruple pairs
excluded by proof).  Cheapest; dies fastest; if it dies, (A) is
forced with a complete impossibility record — the Feynman
ordering.

Adjudicator's recommendation: **(C) then (A)** — with (C)'s pin
pre-registering the total-impossibility outcome as first-class,
so either result is a theorem.  No successor is authorized by
this adjudication; the repair proceeds; the choice is the
user's.
