# CYCLE B HOSTILE PROTOCOL (FROZEN BEFORE DISPATCH)

**Status:** FROZEN, 2026-08-02, per RUNBOOK §13(1).  **Object under
review** (commit 6fab072; sha256 prefixes):
paper-rq0-task-record-fixed-point.md `9afc0ad8578e`;
rq0_l0_fixed_point_exact.py `c77e94561d14`; _output.txt
`1d93073ff1e3`; _receipt.json `eef553549a38`.  **Pin:** 7a80e39.
Reviews are judged against THIS protocol only.

## Verdict vocabulary (each review must end with exactly one)

ACCEPT / ACCEPT-WITH-FIXES / REJECT — plus a per-rung confirmation
line: does the review independently confirm (a) the G0 collapse,
(b) the Galois connection for the availability reading, (c) the
cl = identity degeneracy, (d) the discriminator failure, (e) the
one-boundary impossibility theorem, (f) the verdict name
RQ0-L0-BLOCKED-AT-DE-SMUGGLING as the correct pre-registered
instantiation.

## The four kill-shots every review must attempt (lens-independent)

K1 — **The admitted-map premise.** The impossibility theorem rests
on the relabelling permutation being an ADMITTED reversible
operation and on #111 Cor 5.6 covariance.  Attack: is the
permutation genuinely inside the admitted class of the committed
law (not merely unitary over ℤ)?  If admissibility can be
consistently restricted to exclude it WITHOUT breaking the earned
#111 rungs, the theorem's scope collapses.  Decide which.
K2 — **The alternative D_M reading.** G0 kills the literal formula
via D_M = Σ m_r = id.  Attack: read D_M as measure-AND-BROADCAST
(flag retained), where F∘D_M acts on system+flag.  Does the
literal formula become nondegenerate under that reading, and if
so does it differ from the adopted availability form?  The
collapse claim must survive, be rescoped, or die.
K3 — **Law-family robustness of the degeneracy.** cl = identity is
proved UNDER REPREPARE CLOSURE.  Attack: recompute cl on the
committed law families withOUT reprepares (the identity-only
control already differs).  If cl is selective for a physically
motivated admitted family, the "degenerate closure" headline is an
artifact of an overly generous future class.  Decide, with the
family declared.
K4 — **The criterion-class quantifier.** "No criterion definable
from one-boundary data" — attack the formalization: what exactly
is the class of criteria quantified over?  Is the theorem stated
as isomorphism-covariance (sound but narrower) or as an
unrestricted impossibility (needs the class definition)?  The
paper's sentence must match what is proved.

## Lens assignments (three independent reviews; no reviewer reads
## another's output; own exact code in the session scratchpad only;
## repo read-only; each writes exactly ONE file)

**R1 — operator-system lens** → v13/review-rq0-fixed-point-operator.md
Multiplicative-domain/Choi machinery of the availability form; the
hostile S ⊂ M₂⊕M₂ control chain (s₂² = (I,5I), norms 5/1/10/8,
dim(S∩Z) = 1, φ_λ); the reading-B antitonicity refutation
(eraser 1→5 atoms) recomputed from the committed likelihoods; the
permutation-admissibility question K1 as primary; centre-dimension
anchors (4, 3; commutant solves); the transpose −1/2 and
Choi-rank-1 controls; ≥8 load-bearing numbers independently.

**R2 — effectus/order lens** → v13/review-rq0-fixed-point-effectus.md
The Galois core: antitonicity both sides, the two-sided adjunction,
closure laws, the collision-partition lemma (re-derive it; test it
beyond the exhaustive scope by construction if possible); the
fixed-point lattice; cl = identity (the 359 witness pairs; the
strata 1/2/5/15/52); the trivial-fixed-point correction Prop 4.1
both directions; K3 as primary; the closed-record ↔ closed-family
bijection; ≥8 numbers independently.

**R3 — sufficiency/instrument lens** → v13/review-rq0-fixed-point-instrument.md
G0 as primary, including K2 (the broadcast reading); the
availability form's claimed EXACT coincidence with v12 (H-avail)
(read v12/paper1-composition-defect.md §4 and
v12/note-w6-record-coreference.md; verify the correspondence is
literal, not analogical); the discriminator's fidelity to #103's
manufactured-PVM construction (re-read the #103 adjudication;
confirm the object run is THE committed example); the branch-memory
controls (minima ℂ/ℂ⁵/M₄⊕ℂ recomputed; the ℂ⁵ non-seam degenerate
satisfaction honestly framed); no-write deficit 1/4 and the
9-in/7-out stability table; K4 as secondary; ≥8 numbers
independently.

## Common gates (all three)

Paper-vs-receipt: every number in the paper carried by a receipt
row (spot ≥10 each); scope tags present on every result (finite,
one boundary, law family named, exhaustive-vs-declared strata);
forbidden vocabulary absent (composition/tensor, locality,
overlap, topology, causality, spacetime, QFT, gravity; "Markov"
only with the disclaimer); prose vs gates (no claim broader than
its measurement; the degenerate-satisfaction disclosures kept);
the deviations (1)-(8) each adjudicated fix-real/fix-cosmetic;
anchors' parent values against the committed #103/#111 artifacts;
determinism/mutants/floats re-confirmed from the receipt.

## Discipline

Exact arithmetic only in reviewer rebuilds; interpreter
/opt/homebrew/bin/python3.13; stall doctrine (< 8 min silent,
≤ ~40 min each); NO writes outside the single assigned review
file; no child agents; freeze-on-delivery.  The joint adjudication
(the adjudicator's) will SHA-pin all three reviews, decide the
verdict, apply the ROADMAP inversion and the premise correction,
and update STATUS — per RUNBOOK §13.
