# RQ0-SYNTH — HOSTILE-REVIEW PROTOCOL (FROZEN)

**Status:** FROZEN before panel dispatch, 2026-08-07 (v13 #181).
**Object under review:** commit b36ea87 (delivered as-is #180),
pin f74a8511b204 @ a14bda0.  Reviewers write ONLY their own
review file; no other edits; NO git.  Independent recomputation
mandatory; grades ACCEPT / ACCEPT-WITH-FIXES / REJECT with
per-finding evidence.

## Kill-shots (each reviewer addresses all five; lenses differ)

**K1 — THE FAMILY.**  The family is FIBERED (Deviation 1): the
admitted relabelling group depends on the law×state pair.
Recompute |𝒜| = 1,648,128 independently.  Attack: does the
fibering smuggle arena-dependence into the action itself — could
a quantity be "invariant" only because the fiber group is small
exactly where it would move?  Is the 1,073 right, and is the
65-pair fibration faithful to the pin's flat wording?

**K2 — VERDICT SOUNDNESS.**  Per quantity: recompute the orbit
counts (Q5 = 123 under LAW, Q6 = 14 under STATE), the invariance
sweeps (Q1 0/3,219; Q2 0/512; Q4 0/3,219 name-blind-first; Q8
fixed under law and state), and the INERT/INVARIANT boundary —
are the nondegeneracy (content) gates real or vacuous (the ω
lesson)?  Is any [SAMP] hiding inside a claimed exhaustive sweep?

**K3 — Q-OPT (Δᴮ TRANSPORT).**  The worker reports transport
SUCCEEDED exactly (BLOCKED-AT-IMPORT available and unused), then
verdict ARENA-INERT: 0 non-zero over 576 admitted reversible
pairs, obstruction named
BLOCKED-AT-THE-NON-MONOMIAL-ADMITTED-OPERATION (liftable ops
120/120/1/1/1; the non-monomial control non-zero on 384/512,
Deviation 5).  Attack all of it: is the transport genuinely
exact (anchor against v12 paper-1's committed values); are the
576 pairs a REAL census or vacuous given the obstruction; is the
verdict-vocabulary use compliant with the pin (INERT + a named
obstruction vs BLOCKED)?

**K4 — THE ASYMMETRY (the flagged headline).**  Q5 moves (123
certificates for one patch) while Q8 is fixed (seams 2/2/3
atoms, a chain).  This feeds the successor rung — scrutinize
hardest: is Q8's invariance CONTENTFUL (nondegenerate) or
secretly inert; do Q5 and Q8 face the SAME action (same sweep,
same fibration), or is the comparison apples-to-oranges; does
the chain structure trivialize the seam datum?

**K5 — INSTRUMENT INTEGRITY.**  §14 self-test (19,314/19,314
fixed; unclosed control moves 384/512; broken action fails
2,772/9,072; name-reader moves 728/3,219): verify the controls
have teeth.  Mutant table 20/20 exit-1 named kills: audit
per-mutant that the kill is the RIGHT gate.  READS-vs-ACTS
(Deviation 3): does the printed dependence profile actually
mitigate the pin's underdetermination, or does the declaration
choice FLIP any verdict?  The six-row §2 record table: verify
EVERY verdict name, number, and commit against the terminal
papers and receipts — the table must be verbatim-faithful.

## Frozen files (sha256-12)

See the #181 ledger line: paper / code / output / receipt SHAs
recorded there; reviewers verify SHA-first before reading.

## Deliverables

v13/review-rq0-synth-operator.md (operator lens),
v13/review-rq0-synth-effectus.md (effectus lens),
v13/review-rq0-synth-instrument.md (instrument lens): findings
ranked, independent recomputation counts stated, grade last.
