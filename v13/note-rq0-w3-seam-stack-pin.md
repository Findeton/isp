# RQ0-L0 analytical successor pin

## The W3 Seam Stack: Intrinsic Record Interfaces and Law-Relative Addressability

**Status:** `PIN`

**Date:** 2026-08-01

**Base:** `68d665e3b1e6ec92165e981d6e632f6300c8b714`

**Mode:** one bounded, forward-only analytical mathematical-physics cycle

**Scientific ceiling:** reconstruction of law-relative W3 seam moduli,
presentation invariance, and an admitted-idempotent addressability fibration;
pre-spatial RQ0-L0 only

---

## 1. Binding inheritance

Preserve the complete W3 record-transport cycle, commits `e994807` through
`68d665e`, unchanged. In particular:

- `RQ0-L0-W3-EFFECT-TRANSPORT` remains terminal at its declared scope;
- `RQ0-L0-SHARP-FACT-TRANSPORT` means Boolean transport of sharp record
  propositions, not selected truth, occurrence, W6 co-reference, or token
  identity;
- `RQ0-L0-ADDRESSABLE-W3-OPERATOR-SYSTEM` means admitted-idempotent
  repeatable coarse-graining, not autonomous subsystem control or space;
- the rejected Weld quantaloid/Isbell/stack headline remains withdrawn;
- terminal RQ0-A remains unchanged at its separate, declared-region scope;
  and
- every earlier negative, invalid, review, and adjudication remains immutable.

This cycle attacks the obstruction named by adjudication #76:

$$
\boxed{
\text{derive the family of W3 seams without supplying one cut and PVM as the
answer.}
}
$$

It does not repair any frozen paper.

---

## 2. Central question

Given a represented finite-dimensional operational quantum theory with an
admitted process and instrument law, which boundary cuts, fine alternatives,
and coarse record propositions satisfy the complete W3 write/preserve/erase/
no-write experiment?

The output must be the complete family of solutions, including stabilizers and
physical-symmetry ambiguity. It must not be one preferred memory basis selected
in advance.

The three cumulative questions are:

1. Can all valid seams be constructed as a typed moduli groupoid?
2. Does that groupoid depend only on the represented operational theory, rather
   than on basis, handles, Kraus lists, redundant grammar, inaccessible
   spectators, or a scoped dilation presentation?
3. Can the already-earned admitted-idempotent classification be assembled
   functorially over the reconstructed seam family?

---

## 3. Exact ambient scope

### 3.1 Represented process theory

Work with a represented operational theory $D$ having:

- finitely many boundary types in each worked control;
- a finite-dimensional complex Hilbert space $H_x$ at each boundary $x$;
- a finitely presented but generally infinite typed dagger-linear amplitude
  process language;
- admitted preparations and probes;
- admitted coherent amplitude arrows;
- admitted outcome-indexed instruments and UCP Heisenberg channels where used;
- exact composition and dagger;
- declared comparison relations identifying which write/no-write and
  continuation alternatives belong to one operational experiment; and
- an explicitly typed presentation gauge.

The arrow category must not be assumed finite. The finite-arrow obstruction
surviving from the Weld cycle is binding.

The process law is nomological input. A single realized history is
insufficient. This cycle asks whether the reconstruction depends only on that
represented law and not its syntax.

### 3.2 Admitted projective alternatives

A mathematical projector in $B(H_x)$ is not automatically an operational
alternative. A seam candidate may use a projector only when the represented
process theory supplies it as an actual typed effect/idempotent arrow, or when
an explicitly proved operational-equivalence construction supplies the same
effect.

No field named `access_operations`, support label, subsystem name, circuit
wire, tensor-factor name, or planted region membership may certify a seam.

### 3.3 Exact arithmetic and analysis

The paper is analytical. Small exact calculations may check already-derived
matrix identities, but may not choose definitions, search for a headline, or
serve as the proof of an invariance theorem. No hidden fixture, estimator,
large harness, score, or scientific software receipt is authorized.

---

## 4. Reconstruction variables

At every eligible boundary $x$, treat as variables two nested finite
commutative unital C*-algebras

$$
R\subseteq F\subseteq B(H_x).
$$

Their minimal projections are:

$$
F=C^*(Q_k:k\in K),
\qquad
R=C^*(P_r:r\in\Omega),
$$

with

$$
P_r=\sum_{k\in K_r}Q_k,
\qquad
K=\coprod_{r\in\Omega}K_r.
$$

Thus the inclusion $R\subseteq F$ determines the refinement map. It may not be
supplied again as an external list.

Every candidate must satisfy:

- at least two nonzero coarse sectors;
- exact orthogonality and resolution of the identity on the declared
  operational boundary support;
- operational nonvacuity of every retained fine atom and coarse sector;
- removal or quotienting of permanently inaccessible/dormant summands; and
- invariance under permutation of outcome handles.

For fixed boundary dimension, rank vector, and coarse partition type, begin
from a canonical nested decomposition and its full unitary orbit. Retain the
stabilizer. The total candidate family is the disjoint union over all permitted
rank and partition types and all eligible process tuples.

The paper must state whether this object is a finite union, a countable union,
or only a componentwise finite-dimensional real-analytic groupoid at the
declared scope. It may not call an arbitrary union a finite-dimensional
manifold.

---

## 5. Variable W3 experiment packets

A W3 packet is not selected once and for all. The raw construction ranges over
every eligible typed tuple in the admitted law:

$$
\omega=(x_0,x_1,x_2;\eta,U,N,V,E,f),
$$

or the corresponding finite families of preparations, write arrows, matched
no-write controls, preserving continuations, erasing continuations, and
probes.

Here:

- $U$ and $N$ are a law-declared matched operational comparison, not merely
  two arbitrary arrows with the same source and target;
- $V$ and $E$ are alternative admitted continuations of the same written
  process;
- $Q_k$ and $P_r$ are reconstruction variables; and
- every composite in the W3 equations is an actual typed arrow/effect of the
  represented theory.

The comparison relation is a declared part of the experimental grammar. It
must be carried by operational equivalences and cannot mention the candidate
record algebra it is later used to test.

---

## 6. Exact W3 equations on the candidate space

The paper must give coordinate-free equations and an equivalent exact matrix
form at finite Hilbert scope.

For every admitted preparation $\eta$ in the packet, define a fine alternative
as live for $U$ when

$$
Q_kU\eta\ne0.
$$

The candidate must pass all of the following.

### W1 — write correlation

For every admitted preparation and each coarse sector, at most one fine atom
inside that sector is live:

$$
k\ne\ell,\quad k,\ell\in K_r
\quad\Longrightarrow\quad
\|Q_kU\eta\|^2\,\|Q_\ell U\eta\|^2=0.
$$

### W2 — matched no-write failure

For at least one admitted preparation, the matched control $N$ has two live
fine alternatives in one coarse sector:

$$
\exists r,\ k\ne\ell\in K_r:
\|Q_kN\eta\|^2\,\|Q_\ell N\eta\|^2>0.
$$

This certifies failure of the W3 correlation test. It does not by itself prove
the metaphysical statement that “nothing happened.”

### W3 — preserving availability

For every declared preserving continuation $V$ and every admitted final probe
$f$, amplitudes reaching that probe may originate in at most one coarse record
sector. A coordinate-free formulation using the transported probe support
must be supplied; in rank-one amplitude notation the nonzero conditions are

$$
fVQ_k\ne0,
\qquad
fVQ_\ell\ne0
\quad\Longrightarrow\quad
k,\ell\in K_r\text{ for one }r.
$$

At least one preserving continuation must exist.

### W4 — coherent erasure

For at least one declared erasing continuation, preparation, probe, and pair
of fine alternatives from distinct coarse sectors,

$$
c_k=fEQ_kU\eta,
\qquad
c_\ell=fEQ_\ell U\eta,
$$

must satisfy

$$
k\in K_r,
\quad
\ell\in K_s,
\quad
r\ne s,
\qquad
c_k\overline{c_\ell}\ne0.
$$

The cross-sector term, not an accidentally nonzero or zero summed Born defect,
is the primary coherence witness.

### W5 — same-process and typing gate

Write, no-write, preserve, erase, preparations, probes, fine alternatives and
coarse propositions must all live in one represented process theory and obey
every source/target and composition equation. An externally attached W3
witness cannot certify a seam for an unrelated amplitude sector.

### W6 — operational minimality

Delete fine atoms, coarse sectors and boundary summands that are never live,
never distinguishable by admitted probes, or never enter any W3 composite.
Two candidates differing only on such permanently inaccessible structure must
be identified by the declared operational reduction, not counted as distinct
seams.

The exact form and existence of this reduction are theorem obligations. If it
cannot be defined invariantly, the presentation-invariance rung is blocked.

---

## 7. Raw seam locus and moduli object

Let

$$
\operatorname{Seam}^{\mathrm{raw}}_{W3}(D)
$$

be the full locus of eligible packets and nested algebra pairs satisfying W1
through W6.

The paper must prove at its stated finite scope that:

1. each fixed rank/partition/packet locus is cut out by explicit exact
   algebraic equalities together with declared open nonvanishing conditions;
2. the unitary presentation action preserves the locus;
3. stabilizers are retained; and
4. handle permutations change presentation but not the physical candidate.

The moduli object must be written explicitly as an action groupoid or quotient
stack,

$$
\mathfrak{Seam}_{W3}(D)
=
[\operatorname{Seam}^{\mathrm{raw}}_{W3}(D)/G_{\mathrm{pres}}(D)],
$$

with objects and arrows stated. Brackets without a specified action groupoid
do not count.

The word “stack” in this cycle refers only to this retained-stabilizer moduli
object. It does not restore the rejected Weld stack, assert descent over a
site, or construct physical overlaps.

---

## 8. Presentation gauge versus physical symmetry

Passive presentation gauge simultaneously changes boundary coordinates,
amplitudes, preparations, probes, instruments, and candidate algebras while
leaving the represented operational theory unchanged.

An active physical automorphism preserves the represented law as an
operational object and can permute distinct seam candidates inside the same
theory.

The two actions must be typed separately. After quotienting passive
presentation gauge, an active symmetry orbit is retained.

Prove the no-selection consequence:

> If the physical automorphism group acts transitively on more than one seam
> candidate and no invariant discriminator exists, no invariant rule selects
> one member.

The honest result is the orbit/action groupoid, not a lexical representative.

---

## 9. Operational-equivalence invariance

Define an equivalence of represented operational theories before proving an
invariance theorem. It must preserve:

- boundary typing;
- exact amplitude composition and dagger;
- preparation/probe pairings;
- admitted instrument and UCP maps;
- the matched comparison relation for write/no-write and continuations;
- operational probabilities and coherent W3 composites; and
- presentation gauge actions.

Then prove, or precisely block, an induced equivalence

$$
D\simeq D'
\quad\Longrightarrow\quad
\mathfrak{Seam}_{W3}(D)\simeq
\mathfrak{Seam}_{W3}(D').
$$

Mandatory scoped cases:

1. **basis and handle changes** — exact equivalence;
2. **redundant grammar** — presentations with the same represented completed
   process theory;
3. **Kraus equivalence** — invariance of CP maps and instruments under changes
   of Kraus representation, without applying Kraus language to a genuinely
   coherent amplitude arrow;
4. **Naimark equivalence** — only minimal instrument-preserving dilations with
   the system embedding and outcome instrument fixed; arbitrary dilations or
   equal POVMs with different disturbance do not qualify;
5. **inaccessible spectator extension** — no new operationally visible seam;
   and
6. **boundary/Karoubi presentation refinement** — only when a specified
   equivalence of the represented operational theories is supplied.

The theorem may be componentwise or conditional where the hypotheses truly
require it. It may not replace an operational-equivalence proof by equality of
probability tables.

---

## 10. Reuse of the terminal transport package

For each reconstructed seam $s$ with record algebra $R_s$, reuse without
repairing the earlier paper:

- total effect/POVM Heisenberg pullback;
- supplied-instrument precomposition;
- the record multiplicativity and sharpness defects;
- the multiplicative-domain criterion for sharp Boolean proposition
  transport; and
- the minimal law-relative observability operator system

$$
\mathcal S_s(y)
=
\operatorname{span}_{\mathbb C}
\{I,\Phi_a^*(P_r),\Phi_a^*(P_r)^*\}.
$$

These constructions are performed after $s$ is reconstructed. They may not be
used to select the seam that they later analyze.

No transported Boolean proposition is a selected actual outcome, W6
co-reference certificate, or occurrence-token identity.

---

## 11. Addressability fibration

For a seam $s$, define the complete admitted candidate category

$$
\mathfrak{Addr}_s(D)
=
\operatorname{Min}
\left\{
e:\ e^2=e,\ e\text{ admitted UCP},\
\mathcal S_s\subseteq\operatorname{Fix}(e)
\right\},
$$

where minimality uses

$$
e\preceq f
\quad\Longleftrightarrow\quad
ef=fe=e.
$$

Retain all morphisms induced by admitted Karoubi maps and physical
automorphisms. Do not choose one minimum by labels.

Construct, or precisely block, a Grothendieck-style category/fibration

$$
\pi:\mathfrak{AddrW3}(D)
\longrightarrow
\mathfrak{Seam}_{W3}(D),
$$

whose fiber over $s$ is $\mathfrak{Addr}_s(D)$.

The paper must distinguish:

- no proper admitted idempotent;
- one minimal proper candidate;
- several incomparable minima;
- one physical-symmetry orbit/groupoid of minima; and
- identity forced because the observability system is the whole boundary
  algebra.

“Identity-only because the grammar admits no proper map” must remain distinct
from “identity mathematically forced because the observability system is
global.”

The physical meaning remains repeatable coarse-graining only.

---

## 12. Mandatory analytical controls

### C1 — branch-memory seed without an inserted memory PVM

Fix only the represented branch-memory process law and the eligible W3
experiment grammar. Solve for every admissible nested pair $R\subseteq F$.
Report whether the familiar memory seam is unique up to presentation gauge,
one symmetry orbit among several, or part of a larger moduli component.

### C2 — rotated false seams

Construct rotations of candidate PVMs that fail at least one of correlation,
preservation, coherent erasure, or the no-write discriminator. A positive
branch-memory result without this negative control is insufficient.

### C3 — inaccessible spectator

Add a degree of freedom absent from every admitted preparation, intervention,
and probe. The reduced seam groupoid must remain equivalent. Arbitrary
spectator projectors must not generate new physical candidates.

### C4 — public Q8 support-smuggling negative

The externally attached two-level W3 witness must not become a quaternion
seam. Any W3 candidate belongs only to the process arrows that actually enter
its packet; no supplied support field may reassign it to Q8.

### C5 — equivalent grammars

Two syntactically different presentations of one represented process theory
must yield equivalent seam groupoids.

### C6 — symmetric copies

Two valid seam components exchanged by an active physical swap remain a
two-object symmetry orbit/groupoid after passive gauge quotienting.

### C7 — universal-control world

If the admitted continuation family destroys every proper candidate record
decomposition, return no proper stable seam or only a declared global seam.
Do not manufacture a preferred basis.

### C8 — addressability fibers

Exhibit, at small exact scope where possible, the distinct unique,
incomparable, symmetry-related, no-proper, and global/identity-forced fiber
outcomes.

---

## 13. Four-gate discipline

### Nested pair $(R\subseteq F)$

- **Referent:** admitted projective alternatives at one typed boundary.
- **Necessity:** the previous cycle supplied the PVM and refinement as input.
- **No-smuggling:** all eligible pairs are tested; no preferred pair is
  inserted.
- **Discriminator:** W1--W6 accept some pairs and reject rotated/control pairs.

### Seam moduli groupoid

- **Referent:** the complete family of exact W3-positive packets and algebra
  pairs.
- **Necessity:** exact symmetry forbids canonical selection.
- **No-smuggling:** passive gauge is specified independently and stabilizers
  are retained.
- **Discriminator:** unique, multiple, continuous, symmetric, and empty
  solution families remain different.

### Operational equivalence

- **Referent:** equivalence of represented process and instrument laws.
- **Necessity:** source grammar, Kraus lists, dilations, and spectators are not
  physical seams.
- **No-smuggling:** it cannot mention the desired seam answer.
- **Discriminator:** equivalent presentations agree; genuinely different
  counterfactual completions may disagree.

### Addressability fibration

- **Referent:** admitted repeatable coarse-grainings over each reconstructed
  seam.
- **Necessity:** idempotent classifications vary with the seam.
- **No-smuggling:** only admitted UCP idempotents enter; abstract expectations
  do not.
- **Discriminator:** the five registered fiber types are preserved.

---

## 14. Pre-registered cumulative outcomes

### `RQ0-L0-W3-SEAM-MODULI`

Earned only if the full candidate locus and retained-stabilizer moduli
groupoid are constructed, W3 equations are internally typed, dormant
structure is controlled, and the mandatory positive/negative seam controls
pass.

### `RQ0-L0-PRESENTATION-INVARIANT-W3-SEAMS`

Earned only if the previous rung holds and operational equivalences induce
equivalences of seam groupoids for every scoped mandatory case.

### `RQ0-L0-ADDRESSABILITY-FIBRATION`

Earned only if both earlier rungs hold and the complete admitted-idempotent
fiber assignment is typed, functorial, gauge covariant, and ambiguity
preserving.

These outcomes are cumulative. Failure at a later rung does not erase an
earlier theorem.

### Registered negative outcomes

- `RQ0-L0-BLOCKED-AT-SEAM-TYPING` — the W3 conditions cannot be imposed on
  internally typed variable algebra pairs in the represented process theory;
- `RQ0-L0-BLOCKED-AT-SEAM-INVARIANCE` — a seam moduli object exists but a
  mandatory operational equivalence changes it;
- `RQ0-L0-BLOCKED-AT-ADDRESSABILITY-FIBRATION` — invariant seams exist but
  admitted idempotent fibers cannot be assembled with the declared maps.

A negative requires an explicit theorem or counterexample, never absence from
the legacy corpus.

---

## 15. Kill conditions

The cycle is killed or downgraded if any of the following occurs:

1. one boundary, PVM, fine basis, memory label, tensor factor, or support list
   is supplied as the unique answer;
2. only the known branch-memory PVM is tested;
3. the no-write or continuation relation is defined using the candidate
   record algebra it is meant to test;
4. a projector is accepted solely because it exists in $B(H_x)$, despite no
   admitted operational referent;
5. dormant or inaccessible projectors create new seam components;
6. probability-law equality substitutes for process/instrument equivalence;
7. Kraus or Naimark invariance forgets outcome-specific disturbance;
8. passive presentation gauge and active physical symmetry are quotiented
   together;
9. stabilizers or symmetry-related candidates are discarded;
10. the addressability construction selects the seam it later classifies;
11. an abstract unadmitted expectation is counted as physical;
12. an action groupoid is called a physical overlap stack or spatial atlas;
13. a small script determines a definition or headline; or
14. any claim crosses into actualization, W6 co-reference, autonomous
    subtheories, overlap, localization, topology, causality, spacetime, fields,
    or gravity.

---

## 16. Primary-paper requirements

Write a self-contained mathematical-physics paper provisionally titled:

> **The W3 Seam Stack: Intrinsic Record Interfaces and Law-Relative
> Addressability**

The paper must:

- state every definition and theorem at exact finite scope;
- distinguish definitions, law inputs, postulates, theorems, constructions,
  counterexamples, interpretations, and conjectures;
- prove its finite claims in the paper;
- use external primary literature only for antecedents and comparison;
- include a theorem/control register and a four-gate audit;
- disclose every failure of existence, uniqueness, invariance, or
  functoriality;
- use “intrinsic” only relative to the represented operational theory and its
  admitted grammar; and
- state that outcome actualization and spatial locality are not solved.

The paper is frozen on commit before review dispatch. No review-time repair is
permitted.

---

## 17. Independent hostile reviews

After the paper is committed, obtain two independent repo-read-only reviews.

### Review A — operator algebra and quantum instruments

It must attack:

- nested commutative algebra/PVM typing;
- W3 equations and branch-memory classification;
- Kraus and Naimark scope;
- operational spectator reduction;
- reuse of effect/sharp transport;
- admitted-idempotent fibers; and
- every operator-algebraic theorem and exact control.

### Review B — moduli and higher geometry

It must attack:

- the candidate parameter spaces;
- analytic/algebraic locus claims;
- action-groupoid and stabilizer typing;
- presentation gauge versus physical symmetry;
- operational-equivalence invariance;
- no-selection;
- the fibration/Grothendieck construction; and
- any illicit return of Weld-stack or overlap claims.

Each reviewer must rebuild arguments independently, may use only scratch
outside the repository, and may not edit the paper. Freeze each complete
report verbatim in a separate commit, then adjudicate both jointly against
this unchanged pin and paper.

---

## 18. Stopping rule and exclusions

After review dispatch:

- do not repair the paper;
- freeze each report verbatim;
- adjudicate them jointly in a separate commit;
- retain only the highest preregistered rung actually surviving; and
- halt for explicit user authorization.

This cycle does not authorize:

- outcome actualization;
- W6 fact or event-token co-reference;
- autonomous subsystem control;
- generic physical overlap or a process cover;
- intrinsic spatial localization;
- `RQ0-T1` topology;
- `RQ0-C1` influence;
- causal order, dimension, volume, or Lorentzian geometry;
- scalar, Dirac, Maxwell, or other field reconstruction; or
- gravity and backreaction.

The immediate next event after this pin is committed is the analytical paper,
and nothing else.
