# RQ0-L4 BRANCH C — THE NOMOLOGICAL FINGERPRINT HUNT (PIN)

**Status:** PIN, STRICT, 2026-08-06.  **Immutable base:** commit
a5cb096 (Stage 5 TERMINAL; B″ TERMINAL; all fixtures at their
terminal values).  **Authorization:** the user's plan ("C then A"):
one bounded cycle deciding whether ANY function of declared data
separates the legitimate coarse patch from the forged ones at the
committed distinct-quadruple pairs — with the law's counterfactual
structure as the primary candidate resource.  **Scope:** finite;
the committed carrier, laws, and 687-law census; same-quadruple
pairs EXCLUDED by the terminal proof; no spacetime vocabulary.
Lean NONE.

## The question

Does the law itself remember what forgetting erased?  Formally:
does there exist a statistic on declared data — evaluated, where
the candidate demands it, at COUNTERFACTUAL arguments the law
admits (undeclared preparations; generation profiles) — that
separates the legitimate tomographic coarse patch from the forged
2+1+1 and 2+2 patches, robustly?

## THE CORRIDOR (binding; each an early gate on every candidate)

1. NOT refinement-monotone (the terminal every-state inversion
   theorem kills that entire class — gate each candidate's
   monotonicity type before evaluation).
2. NOT Bayes-error-like (must not factor through partition-size/
   resolution data — gate).
3. Content must survive the committed configuration (the ω failure
   mode: identically-zero-at-declared is a kill — gate
   nondegeneracy first).
4. AMNESTY CHECK mandatory (the ε failure mode): any separating
   candidate must be swept over the terminal state map (the
   4845-state simplex machinery, anchored) — separation that
   INVERTS at any admitted state is FINGERPRINT-AMNESTY, a kill
   for that candidate, reported per candidate.
5. Covariance: candidates must be admitted-isomorphism-covariant
   (the B″ discipline); the covariance analysis of WHY the
   committed pairs resist or admit separation is part of the
   deliverable.

## THE CANDIDATE FAMILY (FROZEN BEFORE FIXTURE TRUTH — RUNBOOK
## §13(4): all candidates declared and gated for corridor
## membership BEFORE any fixture value is computed; the receipt
## orders gates accordingly)

- **C1 — counterfactual occupancy profile:** Ω(P) = the profile
  of ω over ALL admitted preparations (not only declared);
  compare profiles, not points.  Known partial kill to confront:
  a DET forger attains ω = 0 at 31/31 declared preparations —
  the profile form must say whether sweeping changes that.
- **C2 — generation-profile mismatch:** per admitted preparation,
  the coarse structure the law actually generates (collision
  classes of the realized legs) vs the declared boundary; the
  mismatch profile as the statistic.
- **C3 — law-fan-in:** the terminal factoring result (175/874
  keys reachable from >1 law; one from 428) as a resource — does
  the Pres-fan-in structure distinguish the committed pairs?
- **C4 — worker-constructed candidates** inside the corridor:
  each DECLARED (definition + corridor gates) before its fixture
  values are computed; no post-hoc candidates.

## Fixtures (anchored at terminal values)

The committed distinct-quadruple pairs (forged 2+1+1, forged 2+2
vs legitimate tomographic); the selective-amnesty state
(1/2,0,1/2,0,0) and the 13 named states; the five committed laws
+ the 687-law census; the identity-free side (candidates must not
break the terminal dichotomy's ε = 0 side); the Bayes identity
and state-map machinery reused as instruments.

## Pre-registered outcomes (only these; per-candidate verdicts
## mandatory)

- **RQ0-L4-NOMOLOGICAL-FINGERPRINT** — a declared candidate
  separates the committed pairs, survives the amnesty sweep and
  the corridor gates.  (Coarse reality self-certifies; branch C
  wins.)
- **RQ0-L4-FINGERPRINT-AMNESTY** — candidates separate somewhere
  but invert at admitted states (the ε pattern recurring; a
  per-candidate kill, and if universal across the family, strong
  evidence for impossibility).
- **RQ0-L4-CLASS-IMPOSSIBILITY** — proved: no statistic in the
  declared corridor class separates the committed pairs (the
  theorem stated over the class honestly named, with the
  covariance analysis; this is the first-class negative the
  user's plan purchases — branch A is then entered with the
  record).
- **RQ0-L4-BLOCKED-AT-⟨object⟩** — census discipline.

## Discipline

Exact arithmetic; anchors exit-1-only on every terminal value
reused; candidates frozen before fixture truth (the paper lists
the full candidate family with corridor gates BEFORE the results
section; the receipt's gate order proves it); mutants over
anchors AND derivation gates; byte-identical runs; stall doctrine
(< 8 min silent, ≤ ~45 min); NO git; FREEZE-ON-DELIVERY; the
deviations appendix ships.  Deliverables:
v13/paper-rq0-nomological-fingerprint.md +
v13/code/rq0_l4_fingerprint_exact.py + _output.txt +
_receipt.json.  Then RUNBOOK §13: protocol freeze → three-lens
panel → adjudication → STATUS — and the A-or-onward decision
returns to the user with C's verdict in hand.
