# Verdict

\[
\boxed{\texttt{FACT-DESCENT BLOCKED AT PHYSICAL OVERLAP}}
\]

Preserve both commits. Commit `307c36f017d9d5587334d3b79421645ee5b54c61` remains a strong corrective pin, although its anti-padding and common-extension clauses need strengthening in a successor repair pin. Commit `1537b1475705a5def1d1d063459fa7d4fb534982` remains valuable immutable evidence.

The highest secure pre-registered rung is now:

\[
\boxed{\texttt{RQ0-REGIONS-CONSTRUCTED}}
\]

Its strongest uncontested descriptive result is “quantum amplitude regions with internally derived record seams.” Do not start `RQ0-C1`: the first unresolved obstruction is now a typed physical overlap between genuinely diverse regional instruments.

## FATAL — F1. The common extension is not structurally connected to the regions

The amendment requires an extension “carrying the three regional readouts” and regional restriction maps (`307c36f:v13/note-rq0-quantum-substrate-amendment-pin.md:191–207`). The implementation constructs something weaker:

- `common_extension()` has no regional arguments (`1537b14:v13/code/rq0_quantum_regions_exact.py:697`).
- Its “restrictions” are only projections
  \((v_1,v_2,v_3)\mapsto v_a\) on its own two-element joint support (`:719–746`).
- The sole connection to \(D_1,D_2,D_3\) is equality between extension marginals and independently calculated regional record laws (`:1227–1239`).
- No boundary-space map, amplitude intertwiner, channel, embedding, conditional expectation, subinstrument morphism, or readout pullback is defined.
- The note itself concedes that the extension does not identify the regional instruments, carriers, or tokens (`v13/note-rq0-relativistic-arena.md:323–326`).

The executable then derives `RQ0-FACT-DESCENT` solely from uniqueness of value maps plus diagonal support (`rq0_quantum_regions_exact.py:1338–1345`). That does not establish that the extension’s three internal bits are the three regional records.

Thus the extension certifies its own internal three-copy fact, not regional fact co-reference.

## FATAL — F2. The extension criterion is non-discriminating and circular

The amendment correctly states that equal alphabets and probabilities do not establish fact identity (`307c36f:...amendment-pin.md:191–195`). But because the extension constructor consumes no regional structure, any three regional records with the same fair binary marginal pass the implemented bridge.

I independently constructed two exact unitary, record-preserving extensions:

1. The delivered diagonal extension:

\[
\operatorname{supp}=\{(0,0,0),(1,1,1)\},
\qquad P=\left(\frac12,\frac12\right).
\]

2. The equally admissible amplitude extension \(U_{\mathrm{anti}}=X_2U_{\mathrm{ext}}\), flipping the second record after writing:

\[
\operatorname{supp}=\{(0,1,0),(1,0,1)\},
\qquad P=\left(\frac12,\frac12\right).
\]

Both are unitary, both are preserved by \(H_0\), and all three individual marginals are exactly fair. The first forces identity between records 1 and 2; the second forces complementation. The separately defined regional laws cannot select between them.

Therefore the relationship is inserted by the chosen extension. A physical admissibility class and structural region maps must be frozen before the fact comparison.

No equal-law accidental-agreement negative control exists. C2 instead compares one-step Born shadows with different composite laws (`rq0_quantum_regions_exact.py:1208–1211`), which does not test co-reference circularity.

## MAJOR — F3. \(D_2\) and \(D_3\) are exact spectator-padded copies

From the constructor (`rq0_quantum_regions_exact.py:377–417`), with the ordering \(0,1,2,\ldots,q-1\), I independently obtained:

\[
U_q^{\mathrm{write}}
=
U_2^{\mathrm{write}}\otimes I^{\otimes(q-2)},
\]

\[
V_q^{\mathrm{pres}}
=
V_2^{\mathrm{pres}}\otimes H^{\otimes(q-2)},
\]

\[
V_q^{\mathrm{erase}}
=
V_2^{\mathrm{erase}}\otimes H^{\otimes(q-2)}.
\]

The no-write control also factors as

\[
U_q^{\mathrm{no-write}}
=
U_2^{\mathrm{no-write}}\otimes I^{\otimes(q-2)}.
\]

These equalities hold exactly for \(q=3,4\). The auxiliary bits are accessible and actively transformed, as the note says (`note-rq0-relativistic-arena.md:165–168`), but they never couple to the branch-memory subsystem. “Active” does not imply “non-spectator.”

The unequal-dimensional carrier theorem is still mathematically correct: dimensions \(4,8,16\) forbid carrier bijections under the declared presentation type (`:151–163`). But it proves only formal non-isomorphism by product padding, not genuinely different regional amplitude dynamics.

The pin did not explicitly forbid this loophole: it required an accessible exact invariant (`307c36f:...amendment-pin.md:146–149`) but no product-irreducibility or same-dimension control. Genuine regional diversity is therefore not earned.

## MAJOR — F4. The “regional site” is only a fact-value groupoid

The clean pin requires regional morphisms to be typed embeddings, restrictions, interface maps, or process-preserving maps (`307c36f:v13/note-rq0-relativistic-arena-pin.md:109–129`). Its outcome definition requires regions, morphisms, overlaps, refinement, and triple instruments (`:190–203`).

Instead, `regional_fact_groupoid` has:

- objects: the strings `"D1"`, `"D2"`, `"D3"`;
- arrows: bijections of the binary value alphabet inferred from support (`rq0_quantum_regions_exact.py:761–814`).

The note acknowledges that these arrows are not morphisms of the amplitude instruments (`note-rq0-relativistic-arena.md:341–348`). Nevertheless, the executable awards `RQ0-REGIONAL-SITE` whenever this value-level groupoid satisfies its algebraic laws (`rq0_quantum_regions_exact.py:1341–1351`).

The counts are correct—3 labels, 9 arrows, 3 identity checks, 9 inverse checks, and 27 ordered composition checks—but the object is properly typed as a fact-interface groupoid, not \(\mathbf{Reg}\). `RQ0-REGIONAL-SITE` is not earned.

The same issue affects the token claim: the exact \(S_2\) symmetry is a symmetry of the extension’s internal record bits (`:1291–1306`). No map connects that symmetry to the separately named regional tokens (`:1279–1290`).

## MAJOR — F5. Refinement is only Born-shadow marginalization

The implementation first takes `born(fine_arrow)`, sums over an extra-output fibre, and compares the resulting stochastic matrix with `born(coarse_arrow)` (`rq0_quantum_regions_exact.py:832–881`). It constructs no amplitude map or instrument morphism.

The note is accurate when it says “Born-shadow level” (`note-rq0-relativistic-arena.md:165–168`, `:372–377`) but overstates the result as “operational instrument/refinement maps” (`:379–382`) and as maps between amplitude instruments (`:442–451`).

I independently reproduced all shadow equalities. They follow immediately from the exact tensor factorization. The result survives as:

> Exact Born-shadow coarse-graining over a product ancillary factor.

It is not yet the instrument-family refinement demanded by the amendment (`307c36f:...amendment-pin.md:237–244`).

## MINOR — F6. The implemented gauge is the real sign subgroup

The amplitudes lie in the real field \(\mathbb Q(\sqrt2)\), and the executable’s diagonal gauge uses only \(\{\pm1\}\) (`rq0_quantum_regions_exact.py:92–163`, `:589–603`). The receipt accurately says “exact +/- boundary rephasing” (`:1375`), while the report more broadly says boundary rephasing (`note-rq0-relativistic-arena.md:82–95`).

The earned scope is:

\[
G_D^{\mathbb R}
=
\text{configuration relabellings}
\times
\{\pm1\}\text{-boundary gauge}.
\]

The complete \(U(1)\) Paper 1 gauge has not been implemented.

## MINOR — F7. Preparation and tomography access are postulates

`build_region` declares every basis index preparable and installs a `configuration-probe` readout containing every configuration label (`rq0_quantum_regions_exact.py:389–415`). No preparation or measurement amplitude instrument constructs these capabilities.

This is permitted as a provisional operational-access assumption, but the accessible-dimension non-isomorphism result is conditional on that assumption. The report should classify “all basis preparations” and “full configuration tomography” explicitly as operational-access postulates, not derived operations.

## NOTE — 73/73 is a real receipt count, not an ontological adjudication

I independently counted 73 `[PASS]` entries in the committed text receipt. But the final outcome check literally compares the string `"RQ0-FACT-DESCENT"` with itself (`rq0_quantum_regions_exact.py:1353`), and the two preceding rung booleans encode the disputed value-level criteria (`:1341–1351`).

Thus “73/73” survives only as a correct count of internal assertions. It does not adjudicate physical overlap or regional typing.

## Independent algebra: claimed versus obtained

| Quantity | Commit #12 claim | Independent result |
|---|---:|---:|
| Carrier dimensions | \(4,8,16\) | \(4,8,16\) |
| All four regional arrows unitary | true for each region | true for each region |
| Write H-corr | true, true, true | true, true, true |
| Preserve H-avail | true, true, true | true, true, true |
| Erase H-avail | false, false, false | false, false, false |
| No-write H-corr | false, false, false | false, false, false |
| Preserve \((C_{\rm cross},C_{\rm within})\) | \((0,0)\) each | \((0,0)\) each |
| Preserve nonzero \(\Delta^B\) entries | \(0,0,0\) | \(0,0,0\) |
| Preserve record residual | defined and zero | defined and zero |
| Eraser cross-sector coherence counts | \(8,32,128\) | \(8,32,128\) |
| Eraser within-sector counts | zero | zero |
| Eraser nonzero \(\Delta^B\) entries | \(8,32,128\) | \(8,32,128\) |
| Eraser record quotient | undefined | undefined |
| Delivered extension support | \(000,111\) | \(000,111\) |
| Delivered extension probabilities | \(1/2,1/2\) | \(1/2,1/2\) |
| Delivered extension unitary/preserving | true/true | true/true |
| Fact maps inside delivered support | one per pair | one per pair |
| Fact-interface combinatorics | \(9,3,9,27\) | \(9,3,9,27\) |
| Token permutation family | \(2\) | \(2\) |
| Born-shadow refinements | all exact | all exact |
| Spectator factorization | not reported | exact for write, preserve, erase, and no-write |
| Alternative extension with same marginals | not tested | exact anti-diagonal extension exists |

The \(8,32,128\) growth is itself consistent with spectator padding: each added independent qubit multiplies the path/defect count by four.

## Rung adjudication

| Object or rung | Verdict |
|---|---|
| Quantum amplitude-region primitive | **Earned**, as definition plus operational postulates |
| Derived stable-record seam | **Earned** |
| Preserve/erase/no-write controls | **Earned** |
| Real \(\{\pm1\}\)-gauge first rung | **Earned at stated restricted scope** |
| Formal unequal-carrier non-isomorphism | **Earned but physically weak** |
| Genuine regional dynamical diversity | **Not earned** |
| Physical common overlap | **Not earned** |
| Fact-interface groupoid | **Earned as a value-level object** |
| Regional site \(\mathbf{Reg}\) | **Not earned** |
| Physical regional fact descent | **Blocked at overlap** |
| Internal fact descent inside the chosen extension | **Earned only as an extension-internal theorem** |
| Born-shadow refinement | **Earned** |
| Instrument-level refinement | **Not earned** |

## Claims from commit #12 that survive

The following should be preserved:

- the amplitude-native region definition;
- continuation-relative occurrence and availability;
- exact W3 write/preserve/erase/no-write results;
- vanishing preserving defect and record residual;
- eraser recoherence and nonzero defect;
- exact real sign-gauge and relabelling controls at their measured scope;
- the internal GHZ-like extension support and law;
- the internal extension’s \(S_2\) record-bit symmetry;
- exact Born-shadow product coarse-graining;
- all nonclaims concerning causality, spacetime, fields, and gravity.

The following headlines must be withdrawn or downgraded in a forward correction:

- “three genuinely non-isomorphic regional dynamics”;
- “physical common overlap”;
- “regional fact descent”;
- `RQ0-REGIONAL-SITE`;
- `RQ0-FACT-DESCENT`;
- “localized quantum subinstrument is the first obstruction.”

## Bounded repair specification

Do not rewrite either reviewed commit. Pin one repair unit, still stopping before `RQ0-C1`, with these requirements:

1. Construct three equal-dimensional instruments, or at minimum one same-dimensional pair, with exact non-gauge-isomorphism from accessible dynamics.
2. Add an anti-padding gate: every auxiliary factor must participate in an admitted interaction, and the operational instrument family must fail the declared product-decomposition test.
3. Define the regional morphism type before constructing the witness—amplitude intertwiners, isometric subinstrument embeddings, or quantum channels/conditional expectations.
4. Construct one master instrument \(\mathsf E\) and explicit maps \(F_{a,j}\) at each interface satisfying write and preserving-continuation diagrams, for example
   \[
   F_{a,1}U_{\mathsf E}^{\rm write}
   =
   U_{D_a}^{\rm write}F_{a,0},
   \qquad
   F_{a,2}V_{\mathsf E}^{\rm pres}
   =
   V_{D_a}^{\rm pres}F_{a,1}.
   \]
5. Require the regional record projector/algebra to be the actual pullback or restriction of the master record under \(F_a\).
6. Freeze the admissible-extension class and maps independently of the fact comparison.
7. Include both:
   - the diagonal-versus-anti-diagonal ambiguity control above;
   - an equal-law accidental-agreement negative with no admissible structural bridge.
8. Separate \(\mathsf{FactIface}\) from \(\mathbf{Reg}\). Award `RQ0-REGIONAL-SITE` only after regional process morphisms, overlap spans, covers, and triple diagrams exist.
9. Replace Born-shadow “refinement” with an explicitly typed amplitude/channel morphism, or rename it shadow refinement.
10. Label the gauge \(G_D^{\mathbb R}\) and the preparation/tomography scope as access postulates.

Suggested forward-correction sentence:

> Commit #12 constructs exact finite quantum amplitude instruments with internally derived stable-record seams and coherent erasure. Its common extension establishes an internal three-copy record fact but lacks structural maps to the three regional instruments. Therefore `RQ0-FACT-DESCENT` and `RQ0-REGIONAL-SITE` are withdrawn pending a physically typed overlap; the highest secure pre-registered rung is `RQ0-REGIONS-CONSTRUCTED`.

## Review freeze

- Reviewed immutable objects: `307c36f017d9d5587334d3b79421645ee5b54c61` and `1537b1475705a5def1d1d063459fa7d4fb534982`.
- Repository edits: none.
- Final repository status: clean.
- Independent exact rebuild imported no delivery code.
- Scratch derivation SHA-256: `0375b991fbfbae1995578e5bb0a6c6c90c5a606cc28805d88cc597e61af0d49f`.
- No scratch report was created; this response is the complete frozen hostile review.
