# O4 DISCRIMINATOR — UNRECORDED ACTUALITY ON THE W6 BASE (PIN)

**Status:** PIN, STRICT, 2026-08-07.  **Immutable base:** commit
71371ae (RQ0 line closed #188; W6 TERMINAL v12 #41; paper 0
v2.4 §O4 the charter text).  **Authorization:** the user's order
2026-08-07 ("ok work on O4 and transport"); this unit = the O4
discriminator; the full nomological-transport unit follows as
its own pin, informed by this verdict.  Lean NONE.

## The question (the charter's fork, made decidable)

O4-A (configuration realism): unrecorded configurations are
actual but ephemeral — REQUIRES a covariant co-reference rule
for unrecorded configurations (paper 0 §O4: BC2 shows the
slice-indexed formalism does not provide one; W5's LTP-forcing
lemma: under O4-A the composite has definite actuality at
intermediate times to which no probability law attaches).
O4-B (record actualism): only records are actual.  THE
DISCRIMINATOR: on W6's terminal co-reference base — where
RECORD facts provably descend (fact-descent; scoped set-amalgam;
groupoid at the symmetric duplicate) — construct the rule O4-A
needs, or prove its obstruction, AT THE COMMITTED FINITE SCOPE.

## §15 arena declaration (data, not prose)

Carrier, boundary, family, law, state: EXACTLY W6's terminal
committed configuration (its six settings, U_prep, the realized
legs, the wing swap; the worker enumerates them from
v12/note-w6-record-coreference.md + v12/paper2 + its code —
no new fixtures).  Arena: the admitted isomorphism group of
that base + the checkpoint-phase switchings where amplitude
data enters.  Every quantity's claims licensed per-coordinate
only (§15); any enforced symmetry self-tested under its own
action (§14, fresh-eval, cache-hit gated).

## The instrument (frozen before fixture truth)

TRANSPORT OF FACT-DATA along W6's co-reference maps, three
declared fact-classes, each with its own descent gates (the
SAME gates for all three — the comparison must be
like-for-like):
- **F-REC** record facts (division-event-anchored) — POSITIVE
  CONTROL: W6's terminal descent results must REPRODUCE under
  this instrument (anchored exit-1 against the W6/paper-2
  receipts).
- **F-CFG** unrecorded configuration facts ("the configuration
  between division events is X") — THE OBJECT: does F-CFG
  satisfy the same descent/amalgam/coherence gates?  Where it
  fails, the failing gate and the obstructing object NAMED.
  The LTP clause is a mandatory gate: any candidate rule must
  state what probability law (if any) attaches to the
  transported actuality — a rule assigning actuality with no
  law is recorded as LTP-BARE, per W5's lemma.
- **F-CTRL** a deliberately mis-conventioned fact-class
  (negative control with teeth: must FAIL the gates; if it
  passes, the instrument is dead — ACTION-TOO-WEAK analogue).
Candidate rules: the worker constructs candidates INSIDE the
declared corridor (covariant; name-blind; no global slice — any
candidate that smuggles a global now via a total order on
unrecorded facts is gated out by construction, with the gate
proving it bites); each candidate DECLARED with its gates
before its fixture values are computed (§13(4); receipt gate
order proves it).

## Pre-registered outcomes (only these)

- **O4-RULE-EXISTS** — a covariant co-reference rule for F-CFG
  passes all descent gates records pass, WITH a lawful
  probability assignment (no LTP-BARE).  (O4-A's price payable
  at this scope; the fork stays open but O4-A is live.)
- **O4-RULE-EXISTS-LTP-BARE** — the rule exists but actuality
  transports without any attaching law (W5's lemma instantiated;
  O4-A payable only at the price of lawless actuality — stated,
  not adjudicated).
- **O4-DISCRIMINATED-RECORD-ACTUALISM** — F-REC descends (control
  green) while F-CFG is obstructed BY THEOREM at this scope (the
  obstruction constructive and named; the strongest outcome).
- **O4-ARENA-RELATIVE** — F-CFG's truth-values move under the
  admitted arena action (the RQ0 census applied to unrecorded
  actuality): unrecorded actuality is arena-relative — a
  sharpened form of O4-B, distinct from both charter branches;
  reported with the measured orbit.
- **O4-BLOCKED-AT-⟨object⟩** — census discipline.
Per-candidate verdicts mandatory; the paper states plainly which
charter branch pays what price under the verdict; NO
adjudication of the fork itself is claimed — the fork's
resolution, if any, is paper 0's to record and THE USER'S to
route.

## Discipline

Exact arithmetic; anchors exit-1-only on every W6/RQ0 value
reused; positive AND negative controls; mutants over anchors
and gates incl. sign/orientation, a name-reader, a
global-now-smuggler (must be caught by the no-slice gate);
byte-identical delivery-mode runs (L5-style write guard); stall
doctrine; NO git; FREEZE-ON-DELIVERY; deviations ship.
Deliverables: v13/paper-o4-discriminator.md +
v13/code/o4_discriminator_exact.py + _output.txt +
_receipt.json.  Then §13: protocol freeze → three-lens panel →
adjudication → STATUS.  Successor: the nomological-transport
pin (law-data transport), written AFTER this unit's verdict.
