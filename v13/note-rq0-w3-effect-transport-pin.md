# RQ0-L0 analytical successor pin

## Sharp Facts and Unsharp Evidence: W3 Record Transport under Quantum Channels

**Status:** `PIN`  
**Date:** 2026-07-31  
**Base:** `874d5a1d8351297feb48fb961929a9ed75e50cd3`  
**Mode:** bounded forward-only analytical mathematical-physics cycle  
**Scientific ceiling:** law-relative record transport and admitted-idempotent
addressability; pre-spatial RQ0-L0 only

---

## 1. Binding inheritance and correction

Preserve commits `e056b06` through `874d5a1` and every artifact of the
Weld-stack cycle unchanged. In particular:

- `RQ0-L0-WELD-QUANTALOID-CHARTS` remains withdrawn;
- `RQ0-L0-REPRESENTABLE-W3-SKETCH` remains the strongest registered result
  inherited from that cycle;
- the two hostile reports and their joint adjudication remain immutable;
- terminal RQ0-A remains unchanged at its separate finite declared-map scope.

This successor starts from the physical correction

$$
\boxed{
\text{transport of evidence through a quantum channel}
\ne
\text{transport of the same sharp fact}.
}
$$

A general unital completely positive Heisenberg map sends effects to effects
and a PVM to a POVM. It need not send projectors to projectors. Boolean record
transport must therefore be a restricted zero-defect case, not the default.

This is not a patch to `Rec` inside the rejected Weld chart. The physical map
category, logical codomain and addressability object must be rebuilt before
any later quantaloid, Isbell, quotient-stack or overlap claim can be revisited.

---

## 2. Central question

Given an internally certified W3 record PVM and a declared law of admitted
quantum channels:

1. how does the record transform as a quantum instrument and as an
   effect/POVM-valued logical interface;
2. what exact compositional defect measures loss of sharpness;
3. exactly when does the channel restrict to homomorphic transport of the
   record algebra; and
4. can an admitted UCP idempotent give a type-correct, law-relative
   addressability object for the transported record-observability operator
   system?

The target is not intrinsic localization. Selected cuts, projector packages
and transport grammars remain nomological inputs in this cycle.

---

## 3. Prohibited work and claims

Do not edit or repair the immutable Weld paper or its reviews.

Do not construct or claim:

- an all-subspace physical quantaloid;
- a W3 Isbell concept;
- a quotient groupoid or Weld stack;
- generic physical overlap or general record descent;
- intrinsic support or intrinsic localization;
- `RQ0-T1` topology;
- `RQ0-C1` influence;
- causal order or cones;
- geometry, spacetime, fields or gravity;
- a hidden fixture, estimator, large adversarial harness, held-out score or
  scientific software receipt.

Small exact calculations may check arithmetic only after the analytical
derivation exists. They may not select definitions or determine outcomes.

---

## 4. Primary artifact

Write one self-contained mathematical-physics paper titled provisionally:

> **Sharp Facts and Unsharp Evidence: W3 Record Transport under Quantum
> Channels**

The paper must distinguish:

- definition;
- declared physical-law input;
- theorem;
- conditional theorem;
- construction;
- counterexample;
- interpretation;
- open obligation.

Every imported theorem must be checked against primary literature. Literature
is support, not a substitute for proofs at the finite scopes used here.

---

## 5. Fixed channel convention

Work in finite dimension.

For a physical Schrödinger channel from system \(A\) to system \(B\), use its
Heisenberg adjoint

$$
F:B\longrightarrow A.
$$

Here \(A\) and \(B\) denote finite-dimensional unital C*-algebras of
observables and \(F\) is unital completely positive. Composition is written:

$$
G:C\to B,
\qquad
F:B\to A,
\qquad
F\circ G:C\to A.
$$

Every functorial variance statement must use this convention consistently.
“Contravariant” refers to reversal relative to the corresponding
Schrödinger physical process, not reversal of the displayed Heisenberg arrows.

The admitted category

$$
\mathsf{Chan}_D
$$

must be specified by boundary algebras and the physically admitted UCP
Heisenberg maps. Abstractly existing UCP maps are not automatically admitted.

---

## 6. Stage A — effect and POVM transport

For a finite-dimensional unital C*-algebra \(A\), define

$$
\operatorname{Eff}(A)
=
\{a\in A_{\mathrm{sa}}:0\le a\le I_A\}.
$$

Prove that every UCP map \(F:B\to A\):

1. maps \(\operatorname{Eff}(B)\) into \(\operatorname{Eff}(A)\);
2. preserves zero, unit, orthocomplement and every defined finite partial
   sum of effects;
3. composes exactly; and
4. is covariant under unitary presentation changes.

For a finite W3 record PVM

$$
\mathbf P=(P_r)_{r\in\Omega},
\qquad
P_rP_s=\delta_{rs}P_r,
\qquad
\sum_rP_r=I_B,
$$

derive

$$
\mathbf E^F=(F(P_r))_{r\in\Omega}
$$

and prove it is a POVM. Do not call \(\mathbf E^F\) a PVM unless
idempotence is separately proved.

Define a total law-relative effect interface `Rec_eff`, or an equivalent
precisely typed functor/category of outcome-indexed POVMs, on all admitted UCP
maps. Its object and arrow actions, identities and composition must be
explicit. Record handles may not enter its mathematical identity.

Interpretation:

> `Rec_eff` transports quantitative evidence associated with a W3 record. It
> does not by itself certify transport of the same fact or occurrence token.

---

## 7. Stage B — instruments above effects

Define a finite-outcome quantum instrument in one variance convention.
Prefer Schrödinger CP trace-nonincreasing branches

$$
\mathcal I=(\mathcal I_r)_{r\in\Omega},
\qquad
\sum_r\mathcal I_r
\ \text{CPTP},
$$

with Heisenberg effects

$$
E_r=\mathcal I_r^*(I).
$$

Prove:

- the effects form a POVM;
- precomposition by an admitted channel yields another instrument;
- the effect assignment is a functorial shadow of the instrument;
- equal POVMs need not determine equal instruments or equal disturbance.

The primary hierarchy is:

$$
\boxed{
\text{record instrument}
\longrightarrow
\text{effect/POVM interface}
\longrightarrow
\text{sharp Boolean interface when eligible}.
}
$$

Do not invent an instrument from a bare POVM without an explicit construction
or postulate.

---

## 8. Stage C — record multiplicativity and sharpness defects

For a UCP map \(F:B\to A\), define

$$
\Delta_F^{\mathrm{rec}}(a,b)
=
F(ab)-F(a)F(b),
$$

and for self-adjoint \(a\in B\),

$$
\Sigma_F(a)
=
F(a^2)-F(a)^2.
$$

For a record projector \(P=P^2\), write

$$
\Sigma_F(P)=F(P)-F(P)^2.
$$

Prove at the stated UCP finite-dimensional scope:

1. \(\Sigma_F(a)\ge0\) for self-adjoint \(a\);
2. \(\Sigma_F(P)=0\) exactly when \(F(P)\) is a projection;
3. the exact chain law

   $$
   \Delta_{F\circ G}^{\mathrm{rec}}(a,b)
   =
   F\!\left(\Delta_G^{\mathrm{rec}}(a,b)\right)
   +
   \Delta_F^{\mathrm{rec}}\!\left(G(a),G(b)\right);
   $$

4. the self-adjoint specialization

   $$
   \Sigma_{F\circ G}(a)
   =
   F\!\left(\Sigma_G(a)\right)
   +
   \Sigma_F\!\left(G(a)\right);
   $$

5. covariance of the defects under unitary presentation changes.

State explicitly:

$$
\Delta^B\ne\Delta^{\mathrm{rec}}.
$$

The Born composition defect measures failure of stochastic shadows to
compose. The record multiplicativity defect measures failure of a UCP map to
act homomorphically on record observables.

---

## 9. Stage D — multiplicative domain and sharp Boolean transport

For a UCP map \(F:B\to A\), define its multiplicative domain

$$
\operatorname{MD}(F)
=
\left\{
x\in B:
F(x^*x)=F(x)^*F(x),\
F(xx^*)=F(x)F(x)^*
\right\}.
$$

Let

$$
R_W=C^*(P_r:r\in\Omega)
$$

be the finite commutative W3 record algebra.

Prove the exact equivalence:

$$
\begin{aligned}
R_W\subseteq\operatorname{MD}(F)
&\Longleftrightarrow
F|_{R_W}\text{ is a unital *-homomorphism}\\
&\Longleftrightarrow
F(P_r)\text{ is a projection for every }r\\
&\Longleftrightarrow
\Delta_F^{\mathrm{rec}}(a,b)=0
\quad\forall a,b\in R_W.
\end{aligned}
$$

The proof must not silently replace multiplicative-domain membership by
multiplicativity only on a set without establishing the equivalence.

Define the sharp record category with:

- objects carrying a finite record algebra;
- admitted UCP arrows \(F\) whose domain record algebra lies in
  \(\operatorname{MD}(F)\);
- explicit image-containment data for the target record interface.

Prove closure under identity and composition, including the condition

$$
R_C\subseteq\operatorname{MD}(G),
\qquad
G(R_C)\subseteq\operatorname{MD}(F)
\quad\Longrightarrow\quad
R_C\subseteq\operatorname{MD}(F\circ G).
$$

Define `Rec_sharp` as the resulting partial Boolean/*-homomorphic interface.
Its direct and composite maps must agree exactly.

Scope warning:

> A sharp Boolean pullback is necessary for exact proposition transport but
> does not by itself prove W6 fact co-reference or event-token identity.

---

## 10. Stage E — operator-system observability

Replace the first law-relative observability object by the minimal operator
system

$$
\mathcal S_W(y)
=
\operatorname{span}_{\mathbb C}
\left(
\{I_y\}
\cup
\{\Phi_a^*(P_r)\}_{a,r}
\cup
\{\Phi_a^*(P_r)^*\}_{a,r}
\right).
$$

Prove:

- it is a unital self-adjoint linear subspace;
- minimality among operator systems containing every transported record
  effect;
- monotonicity under enlargement of the admitted transport law;
- presentation covariance and record-handle invariance;
- the clean spectator law under the exact unitality hypothesis.

Keep distinct:

$$
\mathcal S_W(y),
\qquad
C^*(\mathcal S_W(y)),
\qquad
\operatorname{Eff}(\mathcal S_W(y)),
\qquad
\operatorname{Sharp}(\mathcal S_W(y)).
$$

Products in \(C^*(\mathcal S_W)\) are algebraic closure. They are not
automatically admitted joint experiments.

This stage remains explicitly law-relative and does not discharge
selected-cut/projector/transport no-smuggling.

---

## 11. Stage F — admitted idempotent completion and addressability

Fix the physical map category before introducing subobjects.

Let \(\mathsf{Chan}_D\) be the category generated by admitted UCP maps. Define
its admitted Karoubi envelope using objects

$$
(B,e),
\qquad
e:B\to B,
\qquad
e^2=e,
$$

where \(e\) is physically admitted. A morphism

$$
f:(B,e)\to(C,d)
$$

must be an admitted UCP map satisfying the correctly typed equation

$$
f=d\circ f\circ e.
$$

Prove identities and composition.

Do not assume the range of every UCP idempotent is a C*-subalgebra under the
ambient product. Treat its fixed range first as an operator system. State and
prove or cite exactly when:

- a Choi--Effros product supplies a C*-algebra;
- a conditional expectation has an ordinary subalgebra range; and
- a stronger multiplicative/sharp certificate is present.

An addressability candidate for \(\mathcal S_W\) is an admitted idempotent
object \((B,e)\) such that

$$
\mathcal S_W\subseteq\operatorname{Fix}(e).
$$

A sharp addressability candidate additionally satisfies the declared
multiplicative-domain condition for \(R_W\).

Order admitted idempotents by

$$
e\preceq f
\quad\Longleftrightarrow\quad
ef=fe=e.
$$

Determine, without arbitrary selection, whether the admitted law returns:

- no containing idempotent;
- one minimal containing idempotent;
- several incomparable minima;
- one physical-symmetry orbit/groupoid of minima; or
- only the identity, classified as global at this law-relative scope.

An abstract conditional expectation not admitted by the process grammar is a
negative control, not an addressability certificate.

---

## 12. Mandatory exact controls

Derive by hand at least these ten finite controls:

1. **Unitary transport:** a record PVM remains sharp; defect zero.
2. **Deterministic bit flip:** a nontrivial sharp Boolean automorphism.
3. **Dephasing:** the diagonal record algebra is preserved sharply.
4. **Random-unitary \(3/4\)--\(1/4\):** a commuting unsharp POVM with exact
   nonzero defect.
5. **Amplitude damping:** effect transport exists and is nonsharp at a
   declared exact parameter.
6. **Two noisy channels:** exact verification of the defect chain law.
7. **Branch-memory W3 seed:** native record seam sharp; transported
   interfaces classified without adding a classical memory.
8. **Terminal RQ0-A antecedent:** exact projector pullback survives only at
   its declared sharp-map scope.
9. **Same POVM, different instruments:** equal effects but different
   outcome-conditioned disturbance.
10. **Admitted versus abstract idempotent:** only the admitted map certifies
    addressability.

Also include:

- unitary gauge/presentation covariance;
- record-handle renaming;
- effect transport that is sharp but not a certificate of fact co-reference;
- an inaccessible spectator control;
- a symmetry case with several minimal candidates retained groupoid-valued;
- a global/identity-only case;
- a no-containing-idempotent case.

No control may be described as proving intrinsic locality.

---

## 13. Four-gate table

Every new primitive must be audited explicitly:

| Primitive | Required referent | Necessity | No-smuggling obligation | Discriminator |
|---|---|---|---|---|
| effect | yes/no propensity for an admitted measurement outcome | general channels do not preserve projectors | no fact identity inferred from its value | sharp versus unsharp |
| POVM | outcome probabilities of a declared measurement interface | total UCP transport | no instrument invented | same-POVM/different-instrument |
| instrument | outcome plus state/process update | W3 is dynamical | effects do not determine disturbance | Lüders versus reprepare |
| record defect | exact failure of homomorphic record transport | separates evidence from sharp transport | not the Born defect | unitary versus noisy chain |
| multiplicative domain | maximal domain of homomorphic action of one UCP map | types the sharp subcategory | no co-reference inferred | dephasing versus random-unitary |
| operator system | least direct linear/effect observability closure | avoids invented products | transport grammar remains declared | proper/global/noisy controls |
| admitted idempotent | physically executable compression/retraction endomorphism | types addressability without informal codomain | abstract expectations excluded | admitted versus unadmitted |
| Karoubi object | typed fixed subsystem of an admitted idempotent | repairs the prior split-category defect | no arbitrary algebra promoted | identity-only/proper/ambiguous |

Failure of a no-smuggling gate blocks intrinsic-support language even if every
algebraic theorem passes.

---

## 14. Outcome ladder

Report the highest cumulative rung established:

- `RQ0-L0-W3-EFFECT-TRANSPORT-BLOCKED` — after explicit construction, the
  total effect/instrument transport or its typing fails;
- `RQ0-L0-W3-EFFECT-TRANSPORT` — total UCP effect/POVM transport, instrument
  shadow, exact defect and chain laws are secure;
- `RQ0-L0-SHARP-FACT-TRANSPORT` — the multiplicative-domain characterization
  and partial sharp Boolean functor are additionally secure;
- `RQ0-L0-ADDRESSABLE-W3-OPERATOR-SYSTEM` — the law-relative operator system
  and admitted-idempotent/Karoubi addressability classifications are
  additionally secure;
- `RQ0-L0-ADDRESSABILITY-BLOCKED-AT-PHYSICAL-IDEMPOTENT` — earlier transport
  rungs survive, but no type-correct admitted-idempotent addressability object
  can be constructed at the declared scope.

Later-rung failure does not erase earlier theorems.

None of these outcomes means intrinsic localization, spatial locality,
topology, causality, geometry, spacetime, fields or gravity.

---

## 15. Kill conditions

The cycle must stop at the highest earlier rung if any later construction:

1. calls a generic UCP image of a projector a projector;
2. calls a POVM a quantum instrument without outcome maps;
3. equates equal outcome laws with the same fact;
4. conflates \(\Delta^B\) with \(\Delta^{\mathrm{rec}}\);
5. asserts multiplicative-domain equivalence without the required UCP and
   record-algebra hypotheses;
6. treats \(C^*(\mathcal S_W)\) products as admitted operations by default;
7. promotes an abstract but unadmitted idempotent;
8. uses a proper algebra as a channel-category object without constructing
   the containing category or completion;
9. chooses one symmetry-related minimal idempotent without an invariant
   discriminator;
10. claims intrinsic support despite the declared-cut/projector/grammar
    dependence;
11. restores the quantaloid, Isbell, overlap or stack headline; or
12. makes any spatial or later-physics claim.

---

## 16. Review protocol

After the paper is committed and byte-frozen, dispatch two independent
repo-read-only reviews:

1. **Operator algebra / effectus review**
   - UCP/effect typing;
   - Kadison--Schwarz and multiplicative-domain proofs;
   - operator systems;
   - CP idempotents, Choi--Effros scope and Karoubi completion;
   - every exact finite control.

2. **Quantum foundations / process-instrument review**
   - W3 occurrence/availability interpretation;
   - instrument versus POVM distinction;
   - evidence versus fact co-reference;
   - composition and gauge covariance;
   - admission and no-smuggling;
   - addressability and symmetry classifications.

Each reviewer must work independently, keep the repository read-only, and
return a complete durable report. Freeze each report verbatim in a separate
commit. Then adjudicate both against the immutable pin and paper.

No review-time repair is authorized.

---

## 17. Stopping rule

After the paper, both reviews and their joint adjudication are committed:

- halt;
- report the highest surviving pre-registered rung;
- preserve every negative result;
- request explicit user authorization before any successor.

No implementation, quantaloid/Isbell repair, overlap work, `RQ0-T1`,
`RQ0-C1` or later physics may begin in this cycle.
