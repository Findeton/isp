# BRANCH A — JOINT ADJUDICATION OF THE THREE-LENS PANEL

**Status:** ADJUDICATED, 2026-08-07 (v13 LOG #175).  **Object:**
commit 6ee172c, protocol 0e1ff17.  **Frozen reviews:** R1 (#173),
R2 (#174), R3 (this commit) — SHAs in the ledger line.

## Panel result and grade resolution

R1 ACCEPT-WITH-FIXES; R2 REJECT-OF-ARTIFACT (headline survives);
R3 ACCEPT-WITH-FIXES.  126 independent recomputations, zero false
classical numbers.  **Adjudicated grade:
ACCEPT-WITH-MAJOR-REPAIRS** — R2's artifact-reject recorded and
honored operationally: no terminal until the corrected kill-gates
re-run clean; the distinction from AWF is the mandatory full
re-derivation of the amplitude layer, not wording.

## The convergent defect and its corrections

The unit's holonomy was GAUGE-VARIANT (double-negated direction
flag; the loop never closed; 379-384/512 switchings move it, 0
move a closed loop).  Corrections ordered: **Prop 5.4 → the
CONSTANCY THEOREM** (the one-step holonomy is −1, forced by
unitarity; the delivered "4 × 128" was the gauge orbit relabelled
as invariant content); **Thm 6.3 → R2's corrected witness**
(constant checkpoint-local shadow vs the cross-checkpoint
invariant sweeping all 8 values — stronger); **L5-AMP-FREE
re-derived under the corrected invariant** (it FAILS as delivered
— the receipt's 0-must-pass-failures does not survive; the
repaired gate must carry the BLINDNESS form: the amplitude datum
is constant, hence unanchorable — REGRESS strengthened);
**THE SCISSORS SECTION REPLACED** — the numerals reading BINDS
(R2's three grounds); **the pin's recorded-but-phased antecedent
was MIS-TRANSPORTED (the adjudicator's error, owned): its
surviving phase is intra-sector at one time; the residue here is
cross-checkpoint — the corrected LOSSY-vs-V-AMP statement stands
on R2's witness, not on that antecedent.**

## What survives — the outcome box, whole and strengthened

REGRESS both variants (better reason: the declared amplitude
family is support-level and anchors no amplitude; the datum is
BLIND — constant); LOSSY-vs-V-CL now a THEOREM (the certificate
is a function of the written record alone); LOSSY-vs-V-AMP at the
committed scope reading-independent, at amplitude scope carried by
the corrected witness; the delta-zero theorem (re-proved shorter,
extended exhaustive); the reduction STRONGER (any predicate over
declared arguments; six escape attempts failed);
BLOCKED-AT-THE-DECLARATION upheld by measurement, with R3's
qualifier installed: **"a sixth declaration whose admissible
range is a function of the first four cannot bind them"** — the
exact form of the slogan.  (P2)-weak's vacuity stated (an
identity, not a measurement — WHY provenance adds no rejection
power).  true_histories brought under the source scan (inert,
verified, but scanned).  PROVENANCE-CERTIFIES does not occur;
**BRANCH B IS THE PROVEN RESIDUE, pending the repaired unit's
verification — the adoption decision is the user's.**

## Repair list (one pass): R-1 the holonomy fixed (closed loops;
gauge-covariance gate under Def 3.1's own criterion, 512-switching
sweep); R-2 Prop 5.4 → constancy theorem; R-3 Thm 6.3 → the
corrected witness + the omitted Gain-1 separation fixed in the
shadow function; R-4 L5-AMP-FREE re-derived (blindness form);
R-5 the scissors → the binding-reading resolution; the
mis-transported antecedent corrected with the ledger citation;
R-6 the §8 slogan qualified (R3's functionality form); R-7
(P2)-weak vacuity + true_histories under the scan; R-8 [EXH-1]
rescoped (1,680 distinct paths), "reduces identically" scoped to
moduli-uncovered; R-9 sign-convention mutants + the
gauge-covariance self-test added; all three rewrite tables;
deviations updated.  **RUNBOOK addendum (this commit): any
instrument enforcing a symmetry must be self-tested UNDER that
symmetry; sign/orientation mutants mandatory where a convention
enters.**
