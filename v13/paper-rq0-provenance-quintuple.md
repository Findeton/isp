# The Provenance Quintuple

## What a Carried Generation History Certifies, in Its Classical and Its Amplitude-Carrying Form — and Why the Same Two Kills Fire on Both

**Status:** `DELIVERED (v13 RQ0-L5, branch A)`

**Date:** 2026-08-06

**Frozen analytical pin:** `a05e3d5` (including AMENDMENT v2, binding)

**Immutable base:** `483311c` (branch C TERMINAL; B″ TERMINAL; stage-5
TERMINAL; all fixtures at their terminal values)

---

## Preamble — branch C's verdict, which is this unit's premise

Branch C closed with a measured negative and this unit inherits it whole. At a
full-group configuration every admitted-isomorphism-covariant statistic of the
declared patch data is a function of the pair (resolution profile, marked-block
excess), so **every covariant statistic is resolution-reading or
state-reading**; over the name-blind class at every committed configuration,
42 separating rows were measured and **none was unaccounted**. The block was
located one step in from provenance: `RQ0-L4-BLOCKED-AT-THE-REFINEMENT-ORDER`
— *the only structure the committed pairs differ by that any admitted
name-blind statistic can see is how much they resolve*. `RQ0-L4-NOMOLOGICAL-
FINGERPRINT` was **withdrawn**. The coarse present cannot testify: proved where
symmetry acts, principled where it does not.

Branch C also named the route this unit takes. Its closing sentence is that
what remains open is not a statistic on the patch but a **cross-patch
condition**. The provenance quintuple is the most direct such condition
available: attach the generation history itself, and check it.

The adjudicator's prior was recorded before this unit ran: **at least one of
the two pre-registered kills fires**. It is reported below as measured, neither
confirmed by construction nor dodged.

---

## Abstract

The patch becomes a **quintuple** $(B,\mathfrak F,X_0,L,H)$, where $H$ is a
declared generation history: a finite path of admitted operations claimed to
have produced the patch's boundary and records, carried as a chain of
fine-grained B″-certificates at its checkpoints. The provenance-reading axiom
is the terminal B″ conditions together with **(P1)** the carried path is
admitted by the law and its endpoint is the declared patch, and **(P2)** each
carried certificate verifies against the law at its own checkpoint.

The pin's amendment orders the amplitude bits kept, so **both variants run side
by side**: **V-CL**, in which the carried certificates are classical, and
**V-AMP**, in which the path is carried with the amplitude-level description of
every step and the checkpoint certificates are computed at the amplitude level.
Carrying is legal — no-cloning constrains copying unknown states, not writing
down the declared amplitudes of known admitted operations — and what is carried
is gauge-invariant content only, per the v12 W7 terminal form: the declared
pair gauge is vertex switching on the layered path graph, so raw per-edge
phases are gauge artifacts and the invariant content is exactly the closed-loop
holonomy family.

**Both pre-registered kills fire, on both variants.**

`RQ0-L5-PROVENANCE-REGRESS` fires with a constructed adversary and a proof.
B″'s cost theory prices making the forged $2{+}1{+}1$ boundary *admissible* at
$120$ deleted operations — unpayable inside the identity-containing class — and
prices *generating* it as a written record at **zero**: $120$ admitted
one-step paths already write it. Admissibility and generation are decoupled and
provenance sits on the cheap side. The declarable-history set is by (P1) a
function of $(B,X_0,L)$ and of nothing else, so carried provenance is a free
variable the adversary ranges over; hence every provenance-reading statistic is
bounded by a quadruple statistic, which is exactly the class stage-5's Theorem
5.3 proved cannot separate. The collision survives the extension literally: at
the carrier's own algebra, where the base supplies **both** provenance labels
for one patch, all $120$ admitted one-step histories carry **one** certificate
— in V-CL and in V-AMP alike.

`RQ0-L5-PROVENANCE-LOSSY` fires twice. Against V-CL, the pin's original
adversary is exhibited at the committed fixture: the $120$ histories producing
the carrier's own algebra fall into a **single** indistinguishable class
containing both the legitimate address context's history (the identity —
nothing was done) and the manufactured context's deleted rotation. Against
V-AMP the amendment's rescoped adversary is constructed and it fires for the
reason the amendment names: **(P2) is checkpoint-local by its own definition**,
so an admitted checkpoint verification reads step-local invariants only, while
the carried two-step diagram has cycle rank $3$ against a checkpoint-local rank
of $2$ — a rank-$1$ residue of gauge-invariant content permanently unread. Two
admitted lifts of one declared support word are exhibited with identical
checkpoint-local shadows, per-step holonomies $(\zeta_8^4)$ and $(\zeta_8^6)$,
and different cross-checkpoint holonomy, $(0,4,6)$ against $(1,4,5)$. This is
the v12 recorded-but-phased limit applied to the checks.

**The delta is the deliverable, and it has three parts.** At the **committed
scope it is exactly zero, and provably so**: every committed law is
single-valued, a single-valued step gives each vertex of a layer exactly one
upward edge, so the layered diagram has $E=mn$ edges, $V=(m+1)n$ vertices and
at most $n$ components, forcing cycle rank $0$; vertex switching then
trivializes every edge phase on a forest and there is *no gauge-invariant
amplitude content to carry*. Measured over $2{,}880$ carried paths at every
committed law: rank $0$, empty holonomy family, Euler identity holding in every
case. At a **declared amplitude scope** built at the same carrier the bits buy
two real things. First, **visible cancellation**: the word $(H_{01},H_{01})$
has a support composite writing the *forged* $2{+}1{+}1$ record while its
amplitude composite is the identity, writing the address chart — V-AMP
adjudicates an endpoint claim V-CL gets wrong, and B″'s own non-negativity
convention is exactly what the amplitude variant suspends. Second, **loop
holonomy**: cycle ranks $1,2,9$ at the words writing the two forged boundaries
and the legitimate coarse one, a strictly finer equivalence on declared
histories than V-CL's. And both are eaten, one by each kill: over the complete
family of $512$ admitted unitary lifts of one declared support step the carried
holonomy takes $4$ values with $128$ lifts realizing each, so the amplitude
datum is one further **free declaration**; and the cross-checkpoint part is
**unreadable** by the verification (P2) is defined to be. Measured against the
kills, **the delta is zero**.

The scissors are disclosed rather than resolved: under the reading in which a
verification is a classical computation on declared numerals it reads
everything and LOSSY does not fire against V-AMP — but then REGRESS eats the
amplitude datum; under the reading the amendment fixes, LOSSY fires. *The
amplitude bits are readable only under the reading that leaves them unanchored,
and anchored only under the reading that leaves them unread.*

The registered outcome is

$$
\boxed{
\begin{array}{l}
\texttt{RQ0-L5-PROVENANCE-REGRESS}\ \text{(V-CL and V-AMP)}\\
\texttt{RQ0-L5-PROVENANCE-LOSSY}\ \text{(V-CL and V-AMP)}\\
\texttt{RQ0-L5-BLOCKED-AT-THE-DECLARATION}\\
\texttt{RQ0-L5-PROVENANCE-CERTIFIES}\ \textbf{— does not occur, either variant}
\end{array}}
$$

Both kills fire on both variants, so **branch B — coarse arena-relativity — is
the proven residue**, and that adjudication returns to the user.

---

## Scope box

| Item | Value |
|---|---|
| Dimension | finite throughout |
| Carrier | ONE committed carrier of five configurations — four success addresses and the retained sink |
| Boundaries | the four committed contexts: the carrier's own algebra $\delta$ and the forged $2{+}1{+}1$, forged $2{+}2$ and legitimate tomographic minimum, with the base's provenance labels |
| Laws | the committed DET ($3125$), REV ($120$), counter-law ($120$) and funnel closure; every one single-valued, measured |
| Histories | finite paths of admitted operations; exhaustive over ALL one-step histories at DET; the two-step scope is the declared amplitude family and the declared lift family |
| Amplitudes | exact: $c\cdot 2^{-s/2}\cdot\zeta_8^e$ with $c$ rational, $s\in\{0,1\}$, $e\in\mathbb Z/8$ — the rational-times-root-of-unity family carrying the corpus's committed amplitude objects (Weyl clock-shift, Hadamard, DFT). Sums formed only between commensurable terms; commensurability gated |
| Gauge | vertex switching on the layered path graph, per the v12 W7 terminal form; invariant content = closed-loop holonomies, carried as a cycle basis |
| Amplitude scope | a DECLARED generator family $\{{\rm ID},{\rm SW}_{01},H_{01},H_{23},F_4\}$ at the committed carrier, carried as a declared family exactly as B″ carries FUNNEL; the law is its support-level composition closure, computed and gated closed ($7$ operations) |
| Arithmetic | exact throughout; no float in any substantive path |
| Amnesty | all $4845$ declared states at denominator $16$ |
| Name-blindness | all $120$ relabellings acting on the whole declared datum at once — boundary, law, preparation, state AND the carried history |
| Inherited scope | the declared #103 compact-convex Karoubi scope, the #111 ancilla-saturated instrument-complete scope, B″'s and stage-5's scopes, unchanged |
| Lean | none |
| Not decided here | where the carrier comes from; the mixed-law arena; whether any *cross-patch* condition other than provenance survives; general amplitude families beyond the declared one |

Scope tags: `[FIN]` the finite declared scope; `[FIX]` the committed fixture;
`[ARENA]` the predecessor's committed arena; `[EXH-1]` exhaustive over all
one-step histories at the committed law; `[AMP]` the declared amplitude scope;
`[SIMPLEX]` the $4845$-state grid.

The words *patch*, *path*, *history*, *step*, *checkpoint*, *carrier* and
*reachable* are operational vocabulary about admitted operations and declared
configurations. No region, locality, topology, manifold, causal order,
influence, spacetime, field, QCD or gravity object is constructed, used or
claimed. A *history* is a declared list of admitted operations; it has no
temporal, causal or spatial reading of any kind.

---

## 1. The question, and the result

### 1.1 What is inherited, and not re-derived

Four objects.

First, the **Atlas Axiom** (B″ Definition 2.2): a patch is the quadruple
$(B,\mathfrak F,X_0,L)$ and is admissible when (i-a) $A(B)=\ker(\mathfrak F)$,
(i-b) $\mathfrak F=\operatorname{Pres}_L(A(B))$, (ii-a) every declared task
writes exactly $A(B)$, and (ii-b) every atom is occupied. With it come
**rigidity** — at any identity-containing law the admissible set is the
singleton $\{\delta\}$ — and the **cost theory**: the obstruction lower bound,
the equality criterion, and the tower $120,360,1260,3120$.

Second, the **ε-machinery** and stage-5's Theorem 5.3: *forgery is not a
function of the quadruple*, witnessed by a collision — the adversary's
manufactured $1{+}1{+}1{+}1$ context and the legitimate address context present
the same patch while differing in provenance.

Third, **branch C's** verdict, the preamble above, together with its two
instruments this unit reuses: the **name-blindness gate** and the **honest
freeze form** — SHA-first registration plus an automated source scan with a
permanent negative control the scan must catch.

Fourth, from v12: the **recorded-but-phased limit** (W7 result 6 — a
record-grade block-diagonalization is *not* phase triviality of the composite)
and the **W7 gauge form** — the declared pair gauge is vertex switching on the
path graph, and the gauge-invariant content is the loop/seam holonomy family.
These are the antecedents amendment v2 names, and they are used exactly where
they bite.

### 1.2 The question

Does attaching the generation history certify what no statistic on the
declared data could? And — the amendment's question, which is the one the user
asked — **does forcing the amplitude bits into the carried datum change the
answer?**

### 1.3 The result in one paragraph

The quintuple is stated with (P1) and (P2) decidable, in both variants
(Section 2), and the amplitude layer is built with its gauge fixed by the W7
form (Section 3), where the **acyclicity theorem** shows the committed scope's
delta is zero before any discriminator runs. The four discriminators are then
run on both variants (Section 4): the forged patches carrying their true
manufacture-histories are rejected, but **(P1) and (P2) pass at every committed
patch**, so the rejection is the inherited B″ clauses' and the provenance
component adds no rejection power — and the *same* rejection falls on the
legitimate coarse chart, killed by the identical clause vector. The regress
adversary is constructed and the kill is proved by reduction (Section 5); the
lossy adversary is constructed twice, once per variant, and fires both times
(Section 6). Section 7 is the delta, stated exactly and gated. Section 8 is the
verdict and the residue.

### 1.4 Four-gate audit of the new object

**The provenance component, and the quintuple's admissibility predicate.**

| Gate | Disposition |
|---|---|
| Referent | a finite list of admitted operations together with the certificates recomputable at its checkpoints. Every element is committed laboratory data of the kind B″ already uses; nothing new is introduced. What is *declared* rather than measured is which list is carried — and that is the whole finding |
| Necessity | branch C closed the statistic-on-the-patch route and named the cross-patch condition as what remains; provenance is the most direct one, and the user's plan authorized it |
| No-smuggling | (P1) and (P2) mention no candidate record, no measure, no legitimacy predicate and no fixture boundary. Both are generic functions of $(\text{word},\text{part},\text{law},\text{prep},n)$, SHA-registered, and verified by the automated source scan against a negative control the scan catches |
| Discriminator | Section 4 exhibits patches the predicate certifies and patches it rejects; Section 6 exhibits histories it cannot separate; and both kills are pre-registered with constructed adversaries rather than asserted |

---

## 2. The quintuple, and the axiom in both variants

### Definition 2.1 — the quintuple `[FIN]`

A **patch** is the quintuple

$$
P=(B,\ \mathfrak F,\ X_0,\ L,\ H)
$$

with the first four components exactly B″'s, and $H=(g_1,\dots,g_m)$ a
**declared generation history**: a finite list of admitted operations. Its
**checkpoints** are the intermediate composites $C_t=g_t\circ\cdots\circ g_1$,
and the **carried certificate** at checkpoint $t$ is the fine-grained
B″-certificate at $C_t$ — the record $\operatorname{comp}(C_t)$ the composite
writes, the closure the law assigns it, the four clause bits, and the reachable
subprocess.

### Definition 2.2 — the axiom `[FIN]`

$P$ is **L5-admissible** iff the quadruple $(B,\mathfrak F,X_0,L)$ is
B″-admissible and

- **(P1)** every $g_t$ lies in $L$, the composites are formed, and
  $\operatorname{comp}(C_m)=A(B)$: the carried path is admitted and its
  endpoint is the declared patch;
- **(P2)** each carried certificate equals the certificate recomputed against
  the law at its own checkpoint.

Two readings of (P2) are carried, both computed, because the choice is
load-bearing and the pin does not fix it. **(P2)-weak** is the reading above:
the certificate must be *honest*. **(P2)-strong** additionally requires each
checkpoint to be B″-*admissible*. Section 4 measures both.

### Definition 2.3 — the two variants `[FIN]`, `[AMP]`

**V-CL** is Definition 2.2 with the carried certificates classical, as just
stated — the pin's original form.

**V-AMP** carries, in addition, the amplitude-level description of every step:
each $g_t$ is carried with a declared amplitude operation $U_t$ whose sector
support is $g_t$, and the checkpoint certificate is computed at the amplitude
level. It is the certificate of Definition 2.1 extended by three data: the
moduli profile; **the record the amplitude composite writes**, which may be
strictly finer than the support one; and **the gauge-invariant loop
holonomies** of the carried diagram.

**Why carrying is legal, and what may be carried.** No-cloning constrains
copying an unknown state; it says nothing against writing down the declared
amplitudes of *known admitted operations*, which is what a declared generation
history consists of. What may **not** be carried as content is anything
gauge-variant: by the v12 W7 terminal form the declared pair gauge is vertex
switching on the layered path graph, so a raw per-edge phase is an artifact of
the lift and carrying it would smuggle. Section 3 fixes the invariant content
accordingly.

---

## 3. The amplitude layer, and where its content is

### Definition 3.1 — the carried diagram and its gauge `[AMP]`

The carried path's **layered diagram** has vertices $(t,j)$ for $t=0,\dots,m$
and one edge per realized leg of each step, carrying that step's amplitude. The
**gauge** is vertex switching: a phase at each vertex, every edge amplitude
multiplied by the phase difference of its endpoints. A quantity is
gauge-invariant iff it is a product of edge amplitudes around a **closed loop**,
conjugated on reversed edges; a cycle basis therefore carries all of it and
nothing else does. This is the W7 form transported unchanged, and it is why the
holonomy family — and not the phases — is what V-AMP carries.

### Theorem 3.2 — the acyclicity theorem `[FIN]`, `[EXH-1]`

Let every step of a carried path be **single-valued**. Then the layered diagram
has cycle rank $0$, so the carried gauge-invariant content is **empty** and
V-AMP's certificate reduces to V-CL's identically.

**Proof.** Each vertex of a layer below the top has exactly one upward edge, so
$E=mn$ while $V=(m+1)n$. Following upward edges from any vertex reaches the top
layer, so every connected component meets it and $C\leq n$. Hence
$\operatorname{rank}=E-V+C=C-n\leq 0$; ranks are non-negative, so
$\operatorname{rank}=0$ and $C=n$ exactly. A connected acyclic graph admits a
vertex switching sending every edge amplitude to its modulus, so no phase
survives as an invariant. $\square$

Measured before it was stated, over $2{,}880$ one- and two-step carried paths
at DET, REV, the counter-law and the funnel closure: cycle rank $0$ and empty
holonomy family in **every** case, with $E=mn$ and $C=n$ holding in every case
(gate `L5-ACYC-M`). And **every committed law is single-valued** (anchor
`A11`, stage-5's own gate). So:

> **At the entire committed scope, V-AMP carries nothing V-CL does not — not
> because amplitudes were withheld, but because a deterministic step has no
> gauge-invariant phase content at all.**

That is the first half of the delta, and it is a theorem rather than a
measurement of the fixture.

### 3.3 The declared amplitude scope, where the content is not empty `[AMP]`

To pose the amendment's question honestly the amplitude layer must be run where
it can bite. A generator family is declared at the **same committed carrier**,
carried as a declared family exactly as B″ carries FUNNEL — the law is its
support-level composition closure, computed and gated closed at $7$ operations:

| generator | description | writes | cycle rank | holonomy phases |
|---|---|---|---|---|
| ${\rm ID}$ | the identity | $0\mid1\mid2\mid3\mid4$ | $0$ | — |
| ${\rm SW}_{01}$ | the transposition $(0\,1)$ | $0\mid1\mid2\mid3\mid4$ | $0$ | — |
| $H_{01}$ | Hadamard on $\{0,1\}$ | $01\mid2\mid3\mid4$ | $1$ | $\zeta_8^4$ |
| $(H_{01},H_{23})$ | two disjoint Hadamards | $01\mid23\mid4$ | $2$ | $\zeta_8^4,\zeta_8^4$ |
| $F_4$ | $\mathrm{DFT}_4$ on $\{0,1,2,3\}$ | $0123\mid4$ | $9$ | $0,2,2,4,4,4,4,6,6$ |
| $(H_{01},H_{01})$ | Hadamard twice | $01\mid2\mid3\mid4$ | $3$ | $0,4,4$ |

Every generator is exactly unitary (gate `L5-AMP-UNITARY`), every amplitude is
exact, and the family **generates every committed boundary as a written
record** (gate `L5-AMP-REACH`) — so the amplitude question is posed *at* the
fixture and not beside it.

### Proposition 3.4 — visible cancellation `[AMP]`

The last row is the finding. The word $(H_{01},H_{01})$ has a **support**
composite writing the forged $2{+}1{+}1$ record $01\mid2\mid3\mid4$, and an
**amplitude** composite equal to the identity, writing $0\mid1\mid2\mid3\mid4$.

B″ Definition 4.1 states the corpus's own convention and derives it: *"no entry
is lost to cancellation, so a leg is present exactly when the process can take
it."* The amplitude variant is exactly the suspension of that convention.
**This is the first thing the quantum bits buy, and it is real**: at this word,
V-AMP adjudicates the endpoint clause of (P1) correctly and V-CL gets it wrong.

---

## 4. The four discriminators, on both variants

All four are run per variant, and the honest verdict is reported first.

### 4.1 Discriminators 1 and 3 — the committed patches with their true histories `[FIX]`

The base supplies provenance labels but no generation paths, so the true
histories are declared (Deviation 1) in the one form the base's own vocabulary
fixes: the admitted operation that writes exactly the declared record — the
identity for the address chart, the block-minimum idempotent for each coarse
boundary, which B″ Proposition 8.6 already carries as an admitted operation.

| context | provenance | B″ | (i-a) | (i-b) | (ii-a) | (ii-b) | **(P1)** | **(P2)w** | (P2)s | **L5** | rank |
|---|---|---|---|---|---|---|---|---|---|---|---|
| $\delta$ address chart | LEGIT | yes | ✓ | ✓ | ✓ | ✓ | **✓** | **✓** | ✓ | **yes** | $0$ |
| forged $2{+}1{+}1$ | FORGED | no | ✗ | ✓ | ✗ | ✓ | **✓** | **✓** | ✗ | **no** | $0$ |
| forged $2{+}2$ | FORGED | no | ✗ | ✓ | ✗ | ✓ | **✓** | **✓** | ✗ | **no** | $0$ |
| legitimate tomographic min | LEGIT | no | ✗ | ✓ | ✗ | ✓ | **✓** | **✓** | ✗ | **no** | $0$ |

The table is identical in V-CL and in V-AMP, bit for bit, and every carried
diagram has cycle rank $0$ — Theorem 3.2 (gate `L5-D1-VARIANT`).

**Discriminator 1's letter is met and its content is not.** Both forged patches
are rejected (gate `L5-D1-REJECT`). But **(P1) and (P2) pass at every committed
patch, forged and legitimate alike** (gate `L5-D1-P1`): the true
manufacture-history of a forged boundary *is* an admitted path that genuinely
produces it, and its checkpoint certificates verify. The rejection is carried
entirely by the inherited B″ clauses. **The provenance component adds no
rejection power.**

**Discriminator 3 is where that costs something.** The legitimate tomographic
minimum, carrying its own true history, is rejected by the **identical clause
vector** as the forgeries — $(\text{i-a})$ and $(\text{ii-a})$ failing together,
exactly as B″'s Theorem 3.2 requires. Only the carrier's own algebra passes
(gate `L5-D1-COLLATERAL`). The quintuple inherits rigidity and does not repair
it: *the axiom's positive is one chart, and it is the same one chart B″ already
certified.*

**And the strong reading does not help.** (P2)-strong rejects every coarse
patch including the legitimate one and certifies only $\delta$ (gate
`L5-D1-STRONG`), so it collapses onto rigidity. Neither reading of (P2)
separates by provenance.

### 4.2 Discriminator 4 — name-blindness, separation, amnesty `[SIMPLEX]`

Six provenance-reading statistics were declared and SHA-registered before any
verdict was computed: manufacture depth (how many carried checkpoints are
B″-inadmissible), intermediate spread, coarsest-checkpoint defect, history
fan-in, path length, and carried cycle rank. Orientation is the corpus's:
admitted iff small.

**Name-blindness.** All six are name-blind: $0$ violations over
$6\times4\times120=2{,}880$ tests, with the relabelling acting on boundary,
law, preparation, state **and the carried history** at once. The gate is
**self-testing**: it passes only if the six are clean *and* a permanent
label-reading **negative control** — an indicator that the configuration named
$0$ forms a block of its own, branch C's label-reading class transported — is
**caught** by the same sweep, which it is (gate `L5-NB`).

**Separation, at the committed state, with the true histories carried.**

| statistic | $\delta$ | forged $2{+}1{+}1$ | forged $2{+}2$ | legit tomographic | separates |
|---|---|---|---|---|---|
| manufacture depth | $0$ | $1$ | $1$ | $1$ | no |
| intermediate spread | $1$ | $1$ | $1$ | $1$ | no |
| coarsest-checkpoint defect | $0$ | $1/16$ | $1/8$ | $3/16$ | no |
| history fan-in | $120$ | $120$ | $60$ | $20$ | no |
| path length | $1$ | $1$ | $1$ | $1$ | no |
| carried cycle rank | $0$ | $0$ | $0$ | $0$ | no |

**None separates** (gate `L5-SEP`), and the two informative rows say why.
Manufacture depth ranks the legitimate coarse chart *with* the forgeries,
because its true history *is* a manufacture-path in the only sense the base
defines: an admitted operation that writes the declared record. History fan-in
runs the **wrong way** — the forged $2{+}1{+}1$ has $120$ declarable histories
and the legitimate tomographic minimum only $20$ — so the forgery is the *more*
generable boundary, not the less.

**The amnesty sweep.** Over all $4845$ declared states, every statistic ties at
every state: $0$ separations, $0$ inversions, $4845$ ties apiece (gate
`L5-AMNESTY`). The sweep has nothing to amnesty. Branch C's
`FINGERPRINT-AMNESTY` pattern does **not** recur here, and the reason is worse
rather than better: the failure is *prior* to amnesty.

**Anchors.** Twelve terminal values are reproduced by this unit's own routes
and exit $1$ on mismatch: the record-lattice sizes $1,2,5,15,52$; DET and REV
at $3125$ and $120$; the counter-law's $120$ maps with exactly one reversible;
$\operatorname{Pres}$ under DET at the four committed boundaries,
$120,240,420,1280$; ε at the coarse triple, $1/16,1/8,3/16$; rigidity's
singleton admissible set at three laws; the cost tower's obstruction sizes
$120,360,1260,3120$; ω's three zeros; branch C's $24$ admitted isomorphisms and
$12$ orbits; the $4845$-state grid; single-valuedness at all four committed
laws; and B″ Theorem 6.1's zero jointly admissible comparable pairs.

---

## 5. The regress kill

### Theorem 5.1 — generation is free where admissibility is unpayable `[EXH-1]`

Under the committed deterministic law, for each committed boundary the number
of admitted one-step paths writing it, beside the B″ cost of making it
admissible:

| boundary | declarable one-step histories | cost of ADMISSIBILITY |
|---|---|---|
| $\delta$ | $120$ | $0$ |
| forged $2{+}1{+}1$ | $120$ | $120$ |
| forged $2{+}2$ | $60$ | $360$ |
| legitimate tomographic minimum | $20$ | $1260$ |

B″ prices making the forged $2{+}1{+}1$ *admissible* at $120$ deletions and
proves it unpayable inside the identity-containing class. It prices
**generating** it as a written record at **zero**: $120$ admitted operations
already write it, at no alteration of the law whatsoever. **Admissibility and
generation are decoupled, and provenance sits entirely on the generation
side — the cheap one** (gate `L5-D2-EXIST`).

This is the honest construction of the pin's forged-history adversary. The pin
asks whether an admitted path genuinely producing the forged boundary exists,
and prices it by B″'s cost theory. It exists, it is not rare, and it is free.

### Theorem 5.2 — the collision survives the extension `[ARENA]`, `[FIX]`

At the carrier's own algebra the base supplies **both** provenance labels for
**one** patch: B″ §6.5 returns the adversary's manufactured $1{+}1{+}1{+}1$
context and the legitimate address context as literally the same patch. Under
the extension, all $120$ admitted one-step histories there yield **one** carried
certificate in V-CL and **one** in V-AMP (gate `L5-D2-COLLISION`). Both
contexts may declare the same history; both verify; nothing distinguishes them.

### Theorem 5.3 — the reduction, and the kill `[FIN]`

By (P1), the set of declarable histories at a declaration is
$\mathcal H(B,X_0,L)=\{H: \text{every step admitted},\
\operatorname{comp}(C_m)=A(B)\}$ — **a function of $(B,X_0,L)$ and of nothing
else**. Let $S$ be *any* statistic on the quintuple. The range
$\{S(B,\mathfrak F,X_0,L,\rho,H):H\in\mathcal H(B,X_0,L)\}$ is therefore a
function of the quadruple, and an adversary may realize any element of it by
declaring the corresponding history. Hence **every provenance-reading statistic
is bounded by a quadruple statistic** — precisely the class stage-5's Theorem
5.3 proved cannot separate a forged from a legitimate declaration of the same
patch.

`RQ0-L5-PROVENANCE-REGRESS` **fires, on both variants** (gates `L5-D2-FUNCTION`,
`L5-D2-REDUCTION`). The pin's gated question — *does the check chain bottom out
in something the law itself enforces?* — is answered: it bottoms out in "is
this path admitted", which is a property of the declared path and the law and
**not** of what occurred. The law admits both the true path and the convenient
one, so verification against the law cannot prefer either.

### Proposition 5.4 — the regress at the amplitude scope `[AMP]`

The amendment's whole point is that the amplitude bits might anchor what the
classical certificates cannot. Measured over the **complete** family of admitted
unitary lifts of one declared support step — all $512$ of the form
$2^{-1/2}\left(\begin{smallmatrix}\zeta^a&\zeta^b\\ \zeta^c&\zeta^d\end{smallmatrix}\right)$
satisfying unitarity — the carried gauge-invariant holonomy takes exactly **4**
values, $\zeta_8^0,\zeta_8^2,\zeta_8^4,\zeta_8^6$, with **128 lifts realizing
each** (gate `L5-AMP-FREE`).

So an adversary declaring a forged boundary may declare *any* of them,
including whichever one a legitimate declaration carries. **The amplitude datum
is one further free declaration.** The regress moves one level deeper with the
quantum bits and does not bottom out.

---

## 6. The lossy kill, constructed twice

### Theorem 6.1 — LOSSY against V-CL, the pin's original form `[FIX]`, `[EXH-1]`

The $120$ admitted one-step histories producing the carrier's own algebra fall
into **one** indistinguishable class: a single distinct carried certificate,
class size $120$ (gate `L5-LOSSY-CL`). Among its members are

- the **identity** — the legitimate address context's history, *nothing was
  done to the address algebra*; and
- the **deleted rotation** $(1,2,3,0,4)$ — the manufactured context's, the very
  operation the predecessor's adversary removed to align the contexts.

Two admitted histories, one legitimate and one forged, agreeing on every
carried classical certificate and differing in the un-carried part. That is the
pin's adversary, constructed at the committed fixture. `RQ0-L5-PROVENANCE-
LOSSY` fires against V-CL.

### Theorem 6.2 — LOSSY against V-AMP at the committed scope `[FIX]`

The same witness works unchanged. The $120$ histories carry **one** amplitude
certificate as well (gate `L5-LOSSY-AMP-C`), because their diagrams are
monomial and a monomial diagram has no gauge-invariant loop content at all
(Theorem 3.2). **The amplitude bits are identically empty exactly where the
provenance question is sharpest** — at the one patch for which the base supplies
both labels.

### Theorem 6.3 — LOSSY against V-AMP where its content is not empty `[AMP]`

This is the amendment's rescoped adversary, and it fires for the reason the
amendment names.

**(P2) is checkpoint-local by its own definition** — it verifies the
certificate *at its own checkpoint*. So an admitted checkpoint verification
reads step-local invariants only. But the carried two-step diagram over one
declared support word has cycle rank $\mathbf 3$, while its two
checkpoint-local diagrams have rank $1$ each: **a rank-1 residue of
gauge-invariant content is permanently unread** (gate `L5-LOSSY-AMP-DIM`).

The witness is exhibited (gate `L5-LOSSY-AMP-A`): two admitted lifts of one
declared support word with **identical** checkpoint-local shadows — per-step
holonomies $\zeta_8^4$ and $\zeta_8^6$ in both — and **different**
cross-checkpoint holonomy, $(0,4,6)$ against $(1,4,5)$.

This is the v12 **recorded-but-phased limit** applied to the checks, in the
form W7 delivered it: block-local data is not phase-triviality of the
composite. The checks are record-producing; the record they produce is
block-local; the phase that survives it is exactly the residue above.

### 6.4 The scissors, disclosed rather than resolved `[AMP]`

There is an objection and it is answered by measurement rather than by
preference. If an admitted verification is a **classical computation on
declared numerals**, it reads everything — including the cross-checkpoint
holonomy — and Theorem 6.3 does not apply. Both readings are run:

| reading of "admitted verification" | LOSSY vs V-AMP | REGRESS vs V-AMP |
|---|---|---|
| a classical computation on declared numerals | does **not** fire | **fires** (Prop 5.4: the datum is free) |
| record-producing and checkpoint-local (the amendment's) | **fires** (Thm 6.3) | **fires** |

**The amplitude bits are readable only under the reading that leaves them
unanchored, and anchored only under the reading that leaves them unread**
(gate `L5-LOSSY-READING`). Neither reading escapes a kill, and the choice
between them does not change the verdict — which is why it is disclosed as a
scissors rather than settled by fiat.

---

## 7. THE DELTA — what the amplitude bits buy

This is what the amendment was ordered to produce, and it is stated exactly,
gated, and in three parts.

### 7.1 At the committed scope: exactly zero, and provably `[FIN]`, `[FIX]`

V-AMP and V-CL return bit-identical verdicts at every committed patch, and
every carried diagram has cycle rank $0$ (gate `L5-D1-VARIANT`). The reason is
Theorem 3.2 and it is a proof, not a fixture accident: every committed law is
single-valued; single-valued steps have acyclic layered diagrams; vertex
switching trivializes every edge phase on a forest. **There is no
gauge-invariant amplitude content to carry.**

The honest form of that sentence matters. It is *not* that the amplitudes were
withheld, or that carrying them is illegal, or that a shadow was taken. They
were carried in full, exactly, and the invariant content of what was carried is
empty. **A deterministic law has no quantum bits to force.**

### 7.2 At the declared amplitude scope: two real gains `[AMP]`

Where steps are not single-valued, V-AMP carries strictly more, and two of the
gains are worth naming.

**Gain 1 — visible cancellation.** At $(H_{01},H_{01})$ the support composite
writes the forged $2{+}1{+}1$ record and the amplitude composite is the
identity, writing the address chart (Proposition 3.4). V-AMP adjudicates an
endpoint claim that V-CL gets **wrong**. This is a genuine capability the
classical variant does not have, and it suspends a convention the corpus
otherwise derives.

**Gain 2 — loop holonomy.** Cycle ranks $1,2,9$ at the words writing the two
forged boundaries and the legitimate coarse one, with non-trivial phases. V-AMP
therefore **distinguishes carried histories that V-CL identifies**: a strictly
finer equivalence on declared provenance.

### 7.3 What neither gain buys, and this is the answer `[AMP]`

Both gains are eaten, one by each kill.

- **Gain 1 is eaten by REGRESS.** Which lift is carried is declared, and $128$
  admitted unitary lifts realize each of the $4$ holonomy classes (Prop 5.4).
  The forger declares the cancelling lift or the non-cancelling one as suits
  him; the endpoint verdict itself becomes a free declaration.
- **Gain 2 is eaten by LOSSY.** The cross-checkpoint part of the holonomy is
  unreadable by the verification (P2) is *defined* to be (Thm 6.3).
- **And at the carrier's own algebra — the one place the base supplies a
  genuine provenance collision — the content is identically empty** (Thm 6.2).

### 7.4 The delta, stated

| | V-CL | V-AMP |
|---|---|---|
| certifies at the committed scope | $\delta$ only | $\delta$ only, bit-identical |
| carried gauge-invariant content, committed scope | none | **none** (Thm 3.2) |
| carried content, declared amplitude scope | supports and moduli | **+ cancellation, + loop holonomy** |
| endpoint clause of (P1) at $(H_{01},H_{01})$ | **wrong** | **right** |
| REGRESS | fires | fires |
| LOSSY | fires | fires |
| what it certifies the other cannot | **nothing** | cancellation and holonomy — both un-anchored or un-read |

**In the reverse direction the delta is empty**: V-CL certifies nothing V-AMP
cannot, since the classical certificate is the support-and-modulus shadow of
the amplitude one, computed by the same routes. The delta is one-way.

**Measured against the kills, the delta is zero.** The same two kills fire on
both variants (gate `L5-DELTA`). Forcing the quantum bits does not rescue
provenance; it relocates the failure one level deeper, and at the committed
laws it is provably vacuous.

---

## 8. Verdict

$$
\boxed{
\begin{array}{l}
\texttt{RQ0-L5-PROVENANCE-REGRESS}\ \text{(V-CL and V-AMP)}\\
\texttt{RQ0-L5-PROVENANCE-LOSSY}\ \text{(V-CL and V-AMP)}\\
\texttt{RQ0-L5-BLOCKED-AT-THE-DECLARATION}\\
\texttt{RQ0-L5-PROVENANCE-CERTIFIES}\ \textbf{— does not occur, either variant}
\end{array}}
$$

- **`RQ0-L5-PROVENANCE-REGRESS` — occurs, on both variants.** The
  manufacture-path exists and is free where B″'s cost of admissibility is
  unpayable; the declarable-history set is a function of $(B,X_0,L)$, so every
  provenance-reading statistic is bounded by a quadruple statistic and
  stage-5's Theorem 5.3 applies; the committed collision survives the extension
  with one certificate for $120$ histories; and at the amplitude scope the
  carried holonomy is itself a free declaration, $128$ lifts per class.
  *Scope:* finite; one committed carrier; the committed laws; exhaustive over
  all one-step histories at DET; the declared amplitude family and the complete
  $512$-element lift family at the amplitude scope.

- **`RQ0-L5-PROVENANCE-LOSSY` — occurs, on both variants, twice constructed.**
  Against V-CL, $120$ histories in one indistinguishable class containing both
  the identity and the deleted rotation. Against V-AMP, the same witness at the
  committed scope (the content is empty there), and at the amplitude scope a
  rank-1 unread residue with an exhibited pair. *Scope:* the same; the V-AMP
  arm at the amplitude scope is relative to the checkpoint-local reading of
  admitted verification, with the alternative reading run and disclosed (§6.4).

- **`RQ0-L5-BLOCKED-AT-THE-DECLARATION` — occurs.** Branch C located the block
  at the refinement order; this unit locates it one step further out, and the
  location is sharper than "at provenance". Provenance is not un-measurable in
  principle here — (P1) and (P2) are perfectly decidable, and they *pass*. The
  block is that **the provenance component is declared by the same party that
  declares everything else, and the law admits every declaration that could be
  made.** Adding a sixth declaration to five cannot bind the five.

- **`RQ0-L5-PROVENANCE-CERTIFIES` — does not occur, in either variant.** The
  quintuple's positive is exactly B″'s: one chart, the carrier's own algebra.

**The adjudicator's recorded prior is confirmed, and over-confirmed.** The prior
was that at least one kill fires. Both fire, on both variants. That is recorded
as a measurement and not as a vindication: the prior was registered before the
run precisely so this sentence could be written honestly.

**Both kills fire on both variants, so branch B — coarse arena-relativity — is
the proven residue.** Stated plainly, as the pin requires: the provenance route
is closed at this scope, in its classical form and in its amplitude-carrying
form alike, and the adjudication of what branch B costs returns to the user.

**What is nevertheless not earned, and what this unit does not claim.** No
impossibility wider than the class run. Theorem 5.3 is a reduction and inherits
stage-5 Theorem 5.3's scope with it. The lossy arm against V-AMP at the
amplitude scope is relative to a declared reading of "admitted verification",
and the alternative reading is reported beside it. And the amplitude family is
a *declared* one: nothing here is claimed for amplitude families outside it.

**The next obstruction, named.** Not the carrier, and not the law: **the
declaration itself**. Every route this campaign has taken adds structure that
the same adversary declares. What has never been tried, and is what branch B
would have to confront, is a condition whose arguments the adversary does
*not* supply. No successor is authorized by this unit.

---

## 9. Non-claims

- **No spatial, temporal or causal reading of "history", "path" or "step".** A
  history is a declared list of admitted operations. It is not a trajectory,
  not a worldline, not a sequence of events in time, and not any spatiotemporal
  object.
- No locality, topology, atlas-as-place, manifold or geometric object; no
  influence, causal order, spacetime or Lorentzian object; no field, QFT, QCD
  or gravity object.
- **No claim that provenance is measurable.** (P1) and (P2) are decidable
  functions of the *declared* history. Nothing here reads what actually
  occurred, and Section 5 is the proof that nothing could at this scope.
- **No claim that the amplitude bits were withheld or shadowed.** They were
  carried in full and exactly; at the committed scope their gauge-invariant
  content is empty, which is Theorem 3.2 and not a modelling choice.
- **No claim that raw phases are content.** Carrying gauge-variant per-edge
  phases would smuggle; only closed-loop holonomies are carried, per the v12 W7
  terminal form.
- **No claim that V-AMP buys nothing.** It buys visible cancellation and a
  finer equivalence on histories, both measured (§7.2). The claim is that
  neither survives the two kills.
- **No claim that the declared true histories are the base's.** The base
  supplies labels, not paths; the paths are declared here and the declaration
  ships as Deviation 1.
- **No claim beyond the declared amplitude family**, the committed laws, the
  one committed carrier, and the one- and two-step history scopes actually run.
- No claim that the quintuple subsumes B″ or branch C. It extends B″ and
  inherits its rigidity unrepaired.

---

## 10. Reproduction

`v13/code/rq0_l5_provenance_exact.py` regenerates every number in this paper.
Exact arithmetic throughout; substantive negatives exit $0$; anchor mismatches
exit $1$. **$12$ anchors and $29$ gates, all passing, $0$ must-pass failures.**
`--falsification-selftest` runs every mutant — $6$ anchor mutants and $9$
derivation mutants — each of which must exit $1$. No wall-clock value enters
the receipt or the rendered output, so two runs of the same source produce
byte-identical artifacts.

Outputs: `rq0_l5_provenance_output.txt`, `rq0_l5_provenance_receipt.json`.

**The freeze.** Every definition of the quintuple, of the amplitude layer, and
every provenance-reading statistic is SHA-256 registered (`L5-00`), and the
automated **source scan** (`L5-SRC`) verifies that no definition mentions a
committed boundary — with the permanent negative control `TUNE`, which
special-cases the legitimate tomographic minimum by name, **caught by the same
scan**. This is branch C's honest freeze form, adopted natively.

**Two gates are self-testing rather than merely asserted**, and both carry a
permanent negative control the gate must catch: `L5-SRC` (the fixture-naming
control `TUNE`) and `L5-NB` (the label-reading control). Two mutants in the
first selftest pass failed to die — the cancellation mutant was a no-op and the
name-blindness mutant was too weak to move statistics that happen to be
invariant at the committed state. Both were repaired rather than retired: the
cancellation mutant now reimposes the non-negativity convention, and the
name-blindness mutant now collapses the relabelling group so the negative
control escapes. That correction is recorded here rather than smoothed over.

---

## Appendix A. Declared deviations from the frozen pin

Per the #121 rule, every deviation ships with the delivery.

1. **The true generation histories are declared, not inherited.** The immutable
   base supplies provenance *labels* for the four committed contexts and no
   generation *paths*. The paths are therefore declared here in the one form
   the base's own vocabulary fixes — the admitted operation writing exactly the
   declared record — and the declaration is load-bearing: it is why the
   legitimate coarse chart's true history has the same form as a
   manufacture-path (§4.1). A different declaration of the legitimate history
   would not change the verdict, because Theorem 5.3 quantifies over all
   declarable histories, but it would change the *table*, and that is said here
   rather than left implicit.

2. **(P2) is carried in two readings**, weak (the certificate is honest) and
   strong (the checkpoint is admissible). The pin's phrase — "each carried
   fine-grained certificate verifies against the law at its own checkpoint" —
   is the weak reading, and it is the one the axiom uses; the strong reading is
   run beside it because it is the natural repair a reader reaches for, and it
   is measured to collapse onto B″ rigidity (§4.1).

3. **The pin's lossy adversary for V-CL was expected to require the
   un-carried quantum layer.** The measurement is that it does not: the
   witness is entirely classical — $120$ permutations, one certificate — and
   the un-carried part that separates them is simply *which admitted operation
   was performed*. The kill is stronger than the pin's form and is reported as
   such.

4. **The amplitude scope is a DECLARED generator family, not a law.** It is
   carried exactly as B″ carries FUNNEL: the family is declared, the law is its
   support-level composition closure, and the closure is computed and gated
   (7 operations). Declaring the amplitude operations as a monoid closure of
   matrices was not attempted and is not claimed.

5. **Amplitudes are exact in a restricted family**: $c\cdot2^{-s/2}\zeta_8^e$
   with $c$ rational. This carries the corpus's committed amplitude objects
   (Weyl clock-shift, Hadamard, DFT) and no more; sums are formed only between
   commensurable terms and the commensurability is gated. Amplitude families
   outside it are not run and nothing is claimed for them.

6. **The lossy verdict against V-AMP at the amplitude scope is
   reading-relative**, and both readings are run rather than one being chosen
   (§6.4). The pin's amendment fixes the record-producing reading; the numeral
   reading is reported beside it, and under it LOSSY does not fire while
   REGRESS does. The verdict is invariant under the choice; the *kill that
   fires* is not.

7. **The two-step history scope is the declared amplitude family and the
   declared $512$-element lift family**, not an exhaustive two-step sweep of
   DET, which is $9.8$ million composites. One-step histories are exhaustive at
   every committed law. Theorem 5.3 is a proof and does not depend on the sweep
   depth; the counts in §5.1 are `[EXH-1]` and are tagged as such.

8. **A statistic family of six was declared where the pin named none.** The pin
   requires the amnesty sweep and name-blindness on "every provenance-reading
   statistic", which is vacuous without a declared family; six were declared and
   registered before any verdict, including two — history fan-in and carried
   cycle rank — that exist only because the quintuple does.

9. **The verdict tag `RQ0-L5-BLOCKED-AT-⟨object⟩` is instantiated as
   `BLOCKED-AT-THE-DECLARATION`**, not at the carrier and not at the law. The
   object named is the declaration act itself, and §8 states why that is the
   sharper location.

10. **Lean: none**, as pinned. **No new primitive**, as pinned: every object
    used is committed structure from B″, stage-5, branch C and v12's W7.
