# O4 DISCRIMINATOR — HOSTILE-REVIEW PROTOCOL (FROZEN)

**Status:** FROZEN before panel dispatch, 2026-08-07 (v13 #192).
**Object:** commit 9fcea62 (as-is #191), pin 2568bc528796 @
e1e8dcd.  Reviewers write ONLY their own review file; NO git;
independent recomputation mandatory; grades ACCEPT / AWF /
REJECT.

## Kill-shots

**K1 — THE OBSTRUCTION.**  "Disjoint occupied configuration
sets at the intermediate time, all six settings" carries the
headline.  Recompute the occupied sets independently.  Attack:
is disjointness PHYSICS or an artifact of the committed frame
pair / leg-order convention?  Sweep every admissible frame pair
the base admits: does ANY pair share occupied configurations?
If disjointness is choice-relative, DISCRIMINATED collapses to
BLOCKED-AT-FRAME-CHOICE — decide which.

**K2 — LIKE-FOR-LIKE.**  Verify the 10 gates are applied
IDENTICALLY to F-REC / F-CFG / F-CTRL (no per-class branches
beyond the declared carve-out D5); F-REC's positive control
reproduces W6's FORCED identity |Φ|=1×6 against the paper-2
receipts (27 anchors traced); F-CTRL's failures (NAMEBLIND,
CERT) are teeth not tautology; C2X's BLOCKED-AT-COVAR verified
under the switchings.

**K3 — THE LTP GATE.**  LTP-BARE computed not asserted: exhibit
the intermediate actuality and the absence of a conditioning
law yourself; residual ‖r‖₀ = (0,0,16,16,0,16) vs W5; verify
LTP-LAWFUL is GENUINELY REACHABLE by the selector (the Q-OPT
lesson — a verdict the code cannot emit is no verdict; a mutant
must prove reachability).

**K4 — THE ARENA TEST + D3.**  |arena| = 192 computed; D3 says
the acting group is the admitted 2, not the declared 8 (the
96-element extension not conjugation-closed) — attack: does the
group shrinkage hollow the orbit claims (QA1 orbit 2 / QA2
orbit 1)?  Recompute both orbits; verify "moves under FRAME,
not switching" per-coordinate; the census discipline (§15)
respected in the claims.

**K5 — INSTRUMENT + D1.**  §14 fresh-eval self-test (cache-hit
gates) verified; 23 mutants audited (right gates, named kills,
never-falsified EMPTY, D6's census exception legitimate); ALL
reused W6/W5/paper-2 values verbatim-checked.  D1 HARDEST:
the claim "W5's 576 recomputes to 288 because W5 chose a
different orthogonal completion of U_prep" indicts a terminal
unit's number — verify the diagnosis (j₀-column exact agreement
A25; the completion-independent anchor reproducing; construct
W5's completion if possible and confirm 576 there), or find
the alternative explanation.  A wrong 288 here is a false
number; a right 288 is a finding about W5 — decide which,
conclusively.

## Deliverables

v13/review-o4-operator.md / v13/review-o4-effectus.md /
v13/review-o4-instrument.md: findings ranked, recomputation
counts stated, grade last.
