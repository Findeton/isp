# RQ0-L1 CYCLE B′ — COMPOSITE BOUNDARIES AND THE DE-SMUGGLING ARENA (PIN)

**Status:** PIN, STRICT, 2026-08-06.  **Immutable base:** commit
0bc943c (Cycle B TERMINAL at RQ0-L0-BLOCKED-AT-DE-SMUGGLING; the
#103/#111 artifacts; the v12 corpus read-only).  **Authorization:**
the standing order (ROADMAP stage 4, amended #121).  **Scope:**
finite; TWO boundaries and one declared overlap/common-access
structure; "independence" is OPERATIONAL (which joint operations are
admitted), never spatial; FORBIDDEN vocabulary: locality, topology,
causality, spacetime, QFT, gravity.  Lean NONE.

## The two questions (one unit, two halves that need each other)

**H1 — the monoidal layer.**  How do two earned minimal boundaries
COMPOSE?  Declare the composite operational structure (local
admitted operations on each factor; the declared class of admitted
JOINT operations; product states and, where the committed fixtures
supply them, entangled admitted states).  Then measure:
- does the minimal sufficient boundary of the composite factorize?
- does the operational classical core distribute:
  Core(A ⊗ B) vs Core(A) ⊗ Core(B)?
**THE GAP between core-of-composite and composite-of-cores is the
pre-registered OPERATIONAL ENTANGLEMENT WITNESS** — antecedent:
v12's LTP-forcing lemma (composite division events fail exactly
under entanglement; paper 1 §9, cited as antecedent, its committed
Bell-model numbers as anchors where reused).  Controls: product
laws (the gap MUST vanish, gated); entangled fixtures (the gap MUST
open, gated); the hostile operator system as one wing (its trivial
core must not become nontrivial by composition with a readable
factor — gate it).

**H2 — the de-smuggling arena (relocated here by #121).**  Two
INDEPENDENTLY DECLARED task families 𝔉_A, 𝔉_B with a DECLARED
overlap (a common subsystem / common record algebra reachable from
both), each with its own earned boundary and core.  THE SELECTOR:
a record is CERTIFIED when it DESCENDS — its fact content
co-refers across both contexts under the committed co-reference
machinery (v12 paper 2's fact-certificates and W6's φ-map/descent
instrument, cited as antecedents, adapted operationally; the
fact/token split preserved).  **THE TOP-OF-LATTICE GUARD (from
Cycle B's terminal theorem, binding):** the selector must NOT be a
closure operator on any single record lattice containing the
candidate as its top — GATE that the selector's carrier is the
pair-plus-overlap structure (the candidate record is NOT maximal
in the descent-ordered structure by construction; exhibit the
order and the candidate's position).
**THE DISCRIMINATOR (mandatory, verdict-level):** Cycle B's
CONSTRUCTED manufactured boundary (the exact Givens PVM + matched
dephasing + A_P, anchored at its committed values) must FAIL
descent across an independently declared second context, while the
legitimate eraser record SURVIVES descent — both directions gated.
If the manufactured record ALSO descends, that is
RQ0-L1-SMUGGLING-SURVIVES-DESCENT: a VERDICT-LEVEL finding that
escalates the trilemma to its provenance/groupoid branches and
HALTS THE ARC FOR THE USER.  Independence of the second context
must itself be measured, not declared-only: gate that 𝔉_B is not a
relabelling image of 𝔉_A (no admitted isomorphism carries one to
the other — the Cycle B counter-law machinery reused).

## Pre-registered outcomes (only these; combinable)

- RQ0-L1-COMPOSITE-BOUNDARY — the monoidal layer earned (composite
  minimal boundary + distribution law characterized, at declared
  finite scope).
- RQ0-L1-ENTANGLEMENT-WITNESS — the core gap opens exactly on the
  entangled fixtures and vanishes on products (gated both ways).
- RQ0-L1-DESCENT-SELECTOR — the descent selector certifies the
  legitimate record and rejects the constructed manufactured one,
  with the top-of-lattice guard passing.
- RQ0-L1-SMUGGLING-SURVIVES-DESCENT — the escalation outcome
  (verdict-level; halts the arc).
- RQ0-L1-BLOCKED-AT-⟨object⟩ — census discipline (referent gate;
  the census is the deliverable).

## Controls (mandatory)

1. Product-law composite: gap zero; every factor record descends
   trivially along the identity overlap (positive control).
2. Entangled fixture (from the committed corpus): gap opens;
   report which factor records still descend.
3. The hostile wing (S ⊂ M₂⊕M₂ composed with a readable factor):
   composite core computed; no promotion of the trivial wing.
4. Relabelled second context (𝔉_B = an admitted-isomorphism image
   of 𝔉_A): the independence gate must FAIL it — this control
   proves the independence measurement bites.
5. The Cycle B counter-law as one context's law: descent posed
   there; report honestly what changes.

## Deliverables and discipline

Paper: v13/paper-rq0-composite-boundaries.md (RQ0 house form;
scope box; non-claims incl. NO spatial reading of "independence";
deviations list ships as an appendix per the #121 rule).  Code:
v13/code/rq0_l1_composite_*.py + _output.txt + _receipt.json —
exact arithmetic only; anchors exit-1-only on every reused
committed value (Cycle B's constructed-boundary values; #103/#111
fixtures; paper 1's Bell-model numbers where used); substantive
negatives exit 0; mutant table covering anchors AND derivation
gates; no wall-clock in receipt or rendered output (byte-identical
runs); progress prints (< 8 min silent); total ≤ ~45 min.  Then
per RUNBOOK §13: protocol freeze BEFORE reviews; three-lens panel
(operator-system / effectus-order / sufficiency-instrument); SHA
freezes; joint adjudication; STATUS in the same commit.
