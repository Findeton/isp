# RQ0-L0 computational certification — held-out delivery

**Status:** GREEN-UNREVIEWED, 2026-07-31.

**Binding pin:**
`note-rq0-operational-localization-computational-certification-pin.md`.

**Frozen estimator:** commit `d881a3e`, SHA-256
`a9f8f93a01d7bf84d7dfde1e43b5c14a0111e9722b42d8e3dc999de887630f8b`.

**Frozen proof:** SHA-256
`5839fedcb680cb24e0ba778aff6e00aa92ac4f98191753693abbad1a54bbcd2d`.

**Held-out fixture:** pre-run commit `b9e574a`, SHA-256
`d4a72c05f8755b4247ecac19f7ac9a49656c3c855a2fea08a77c73ce7b8d72ea`.

**Official receipt:** SHA-256
`476b0ce989811e602add8bda5b58fa1880adb74bc7cca0c6880e188c9500b1de`.

---

## 1. Outcome

The one authorized official held-out run returned

\[
\boxed{\texttt{RQ0-LOCAL-ATLAS}}
\]

with 35/35 registered held-out checks passing in 65.74 seconds under the
single complete 360-second cap.  This is a green, unreviewed finite L0 result.
It is not terminal before the required external hostile review and separate
adjudication.

The result is:

> A generic estimator frozen before the held-out composition law existed
> recovered four independently operable factors from one exact finite quantum
> amplitude instrument, then constructed a non-Boolean family of proper
> record-bearing operational regions, full restriction morphisms, and a
> coherent projector/Boolean stable-fact functor.

The word “local” remains operational.  No region is identified with a spatial
subset, causal diamond, manifold patch, or spacetime location.

---

## 2. Chronology and blindness

The successor pin was committed at `605761b`.  Public development used only
opened benchmarks.  Commit `d881a3e` froze the generic estimator, proof,
public runner and 48/48 public receipts.  Git-object inspection at `d881a3e`
proved that the future fixture, abstract multiplication law, factor truth,
contexts, records, scorer, receipt, output and this delivery path were absent.
That chronology was recorded at `59011af`.

Only afterward was the single fixture conceived.  Commit `b9e574a` froze its
constructor and scorer before execution.  The estimator and proof were not
edited.  The official scorer was then invoked exactly once.  No development
score, fixture repair, cap increase, replacement fixture or second hidden run
occurred.

The estimator received only the serialized operational dataset.  It did not
import the fixture module or read its factor labels, construction coordinates,
contexts-as-atom-sets, record truth, atlas truth or expected outcome.  The
scorer alone compared the returned objects with held-out truth after the
estimator returned.

---

## 3. The new held-out quantum process

The abstract composition object is

\[
C_2\times C_3\times C_4\times S_3,
\]

of order 144.  Its exact monomial amplitude representation acts on a
24-state carrier over \(\mathbb Q(\zeta_{24})\).  A nontrivial diagonal phase
frame conjugates the raw tensor representation, so the input retains exact
amplitude phase structure rather than being only an unsigned multiplication
table.

The actual quotient invariants measured from the composition object are:

- order 144;
- center order 24;
- derived-subgroup order 3;
- a noncommuting pair, hence genuine nonabelian content;
- element-order histogram
  \(1^1,2^{15},3^8,4^{16},6^{48},12^{56}\).

Order 144 proves nonisomorphism with both antecedent construction families:
the opened order-192 `C2 x C3 x C4 x D4` benchmark and order-216 `S3^3`.
Neither older fixture module is imported by the held-out constructor or
scorer.

The serialized instrument has:

- 144 quotient operation classes;
- 20,736 complete ordered composition rows;
- carrier dimension 24;
- 6 independently selectable classes, including identity;
- 138 composite-only classes;
- 4 fresh W3 record candidates;
- 4 selective operational contexts.

Independent selectability is substantive: setting every selectable flag to
false returns `RQ0-L0-BLOCKED-AT-ADDRESS` rather than the positive atlas.

---

## 4. Recovered address factors

The estimator validated every complete row at its separate type, status,
result, supplied-law, physical-composition, gauge-equivalence and observed-
signature layers.  It then found:

- 70 normal operational subobjects;
- 36 direct-complement pool members;
- 14 complete passing factor certificates;
- one finest factorization;
- recovered factor orders \((2,3,4,6)\).

Every returned factor passes the frozen P1–P8 certificate:

1. generation by independently selectable classes;
2. mixed implementation in both orders;
3. exact operational commutation;
4. faithful multiplication;
5. closure and inverses;
6. typed identity/scalar intersection;
7. exact represented-algebra product;
8. restriction stability.

The exact cache built the ambient represented algebra once.  Each of 14
canonical nonambient algebra keys was constructed once; 70 later requests
were cache hits, including certificate and regional/fact replay.

---

## 5. Irregular regional atlas

The four held-out operational context scopes were frozen as

\[
(0,1,2),\quad(0,2,3),\quad(0,1,3),\quad(1,2).
\]

Their exact meets yield these ten proper record-bearing regional scopes:

\[
\begin{aligned}
&(0),(1),(2),\\
&(0,1),(0,2),(0,3),(1,2),\\
&(0,1,2),(0,1,3),(0,2,3).
\end{aligned}
\]

This is not the complete proper-Boolean family.  It has no universal atom
shared by every context.  The estimator constructs:

- 10 regional amplitude subinstruments;
- 31 full `RegAddr` restriction arrows;
- 31 exact fact-interface maps;
- 64 coherent direct/composite regional diagrams;
- 64 coherent direct/composite factual diagrams;
- 3 nonvacuous triple overlaps.

Every regional arrow carries and replays operation classes, complete row
contexts and statuses, source/target results, independently supplied laws,
observed signatures, selectability declarations, preparations, contexts,
probes, readouts, records and gauges.

---

## 6. Stable records and `Rec`

All four record candidates pass the inherited exact W3 decision procedure.
They are derived from fresh write/preserve/erase amplitude witnesses and are
not primitive factual labels.

The contravariant factual assignment uses exact projector resolutions and
Boolean atom maps.  It checks projector pullback, identities, composition and
direct-versus-composite naturality.  Record handles are absent from the
scientific identity: renaming every record and witness handle while preserving
the dynamics/projectors returns the same structural result.

The result therefore concerns physical stable-fact descent between operational
quantum subinstruments, not equality of marginal record probabilities.

---

## 7. Hostile controls in the official run

The same official worker executed all mandatory controls before returning the
positive rung.

### Complete-row controls

- corrupt `tau` -> procedural invalid;
- declared `COLLAPSED` status -> scientific address block;
- corrupt result class -> procedural invalid;
- corrupt independently supplied row law -> procedural invalid;
- corrupt observed signature -> procedural invalid.

### Presentation controls

Full operation/field renaming, serialization reordering, exact carrier-gauge
conjugation and record-handle-only renaming each return the same structural
`RQ0-LOCAL-ATLAS` result.

### Scientific-map controls

Mutating a full regional row map, a complete context map, or a projector
pullback makes artifact replay return procedural invalid.  No structural hash
stands in for the executable map.

### Resolver controls

Malformed serialized input, zero runtime cap, injected timeout, exception,
missing outcome and multiple outcomes each return exactly one procedural
`RQ0-L0-INVALID` result with exit 1.

### Descent control

Three independently built full amplitude instruments have three valid
pairwise isomorphisms on operations, rows/laws, preparations, contexts,
probes, readouts, W3 records and gauges.  The two-step and direct maps disagree
only at the regional and record triple-loop equation, so the twisted triple is
rejected at the correct gate.

A deliberately corrupted frozen anchor likewise suppresses every positive
outcome.

---

## 8. Epistemic classification

### Definitions

- typed operational amplitude dataset and quotient composition object;
- independent-operability predicates P1–P8;
- operational regional restriction;
- `RegAddr`, `FactIface`, and projector/Boolean `Rec`;
- registered scientific and procedural outcomes.

### Postulates

- complete finite row tomography/access;
- supplied independent-selectability declarations;
- admitted preparations, controls, contexts, probes and readouts;
- exact finite monomial amplitude representation;
- the declared finite gauge and normal-direct-factor search scope.

### Theorems

- the frozen finite soundness and declared-scope completeness theorem;
- cheap-first pruning equivalence;
- exact sparse/dense represented-algebra equivalence;
- canonical-subobject cache equivalence;
- full regional/factual replay and total-resolver implications.

### Measurements

- the single held-out outcome and all counts above;
- runtime 65.74 seconds under the 360-second cap;
- actual quotient invariants;
- all 35 held-out control results.

---

## 9. Exact scope and first unresolved obstruction

The earned green claim is only:

\[
\boxed{
\text{finite exact amplitude-instrument scope}
\;\Longrightarrow\;
\texttt{RQ0-LOCAL-ATLAS}
}
\]

It does not show that arbitrary quantum laws admit such a localization, that
the recovered family is unique outside the declared normal-factor scope, or
that the regions are spatial.

The next scientific questions would be a topological overlap-shadow branch
and an independently frozen operational-influence branch.  They remain
strictly prohibited in this cycle.  Before either can start, this delivery
must survive a fresh external repo-read-only hostile reconstruction and a
separate adjudication.

No topology, manifold, dimension, volume, causal relation, cone, Lorentzian
metric, special relativity, field propagation, stress tensor, gravity or
Einstein equation is claimed.

**HALT for external hostile review.**
