# External hostile review dispatch — W3 seam stack

## Operator algebra and quantum instruments

**Dispatch date:** 2026-08-01

**Governing pin commit:**
`21d28d1c80a3a9518887c9b257475694fc5843e8`

**Immutable paper commit:**
`dc0a662099361355f0219602bdcab9520f4c25ff`

**Pin SHA-256:**
`d446e8d9ca6a10a06cb9f57a5056b8885581aa4e9a4422865e4352be70b60509`

**Paper SHA-256:**
`2a8c7d357346d9dcc8ddbde373f4d4fefbb28cebb8f08471b35106f18f31a91e`

## Review contract

Review the immutable pin and paper independently and adversarially. The
repository is read-only for this task. Do not edit, repair, stage, or commit
anything. Scratch work may be written only under `/private/tmp`. Return one
complete self-contained report in your final response so it can be frozen
verbatim.

Do not reject the paper merely because it does not construct actualization,
W6 co-reference, autonomous subsystems, overlap, spatial localization,
topology, causality, fields, or gravity. Those are explicit exclusions. Do
reject or downgrade any claim that silently depends on one of them.

## Required independent attacks

### 1. Types and physical referents

Audit the represented amplitude/instrument theory, eligible packet,
matched-control relation, admitted projective resolutions, nested
commutative algebras, and contextual operational quotient. Determine whether
every product and scalar in W1--W4 is typed and whether admission is physical
input rather than mere existence in `B(H)`.

Attack the claim that the contextual relation is a dagger-compatible
composition congruence and that it adequately controls dormant structure at
the paper's exact scope.

### 2. W3 equations

Rebuild write correlation, no-write failure, preserving availability, and
coherent erasure from the displayed definitions. Check that preservation
kills the required cross terms and that erasure really implies a failure of
record-sector availability. Look for vacuity, postselection, role tailoring,
or a packet definition that re-inserts the desired record.

### 3. Branch-memory classification

Independently recompute the four-dimensional branch-memory packet. Do not
trust the stated Hadamard matrix, partition table, or nine-seam count.
Verify or refute:

- availability discretizes every coarse algebra in the declared rank class;
- two-row restrictions yield exactly two orthogonal fine rays;
- three-row restrictions cannot satisfy write correlation;
- no-write and eraser tests pass for every `2+2` and `2+1+1` partition;
- the exact counts are three and six;
- operational reduction is satisfied; and
- the familiar memory PVM is only one of the three `2+2` seams.

Distinguish carefully between a reconstruction result conditional on the
full-projective-access postulate and a derivation of that postulate.

### 4. Invariance scopes

Audit Kraus invariance, minimal instrument-preserving Stinespring/Naimark
invariance, the POVM-only warning, formal inaccessible-spectator invariance,
and scoped Karoubi refinement. Supply counterexamples if any hypothesis is
insufficient. Equal POVMs with different disturbance must not be conflated.

### 5. Transport and addressability

Check that effect and sharp-proposition transport are reused only after seam
reconstruction. Audit the operator-system definition, UCP-idempotent order,
minimal-element proof, Karoubi morphisms, and the unique/unadmitted/
symmetry-related/global controls.

In particular, independently test the claim that the channel grammar
generated by two nonorthogonal auxiliary dephasings and their swapping
involution contains no additional idempotent. If the proof is incomplete,
state whether the conclusion is false or merely unproved.

### 6. Claim boundary and verdict

Classify separately whether each registered rung is earned:

- `RQ0-L0-W3-SEAM-MODULI`;
- `RQ0-L0-PRESENTATION-INVARIANT-W3-SEAMS`;
- `RQ0-L0-ADDRESSABILITY-FIBRATION`.

Use one overall verdict:

- `ACCEPT`;
- `ACCEPT-WITH-SCOPE-ENGRAVING`;
- `HEADLINE-DOWNGRADE`;
- `REJECT`.

Rank all findings by severity. Separate false theorems, missing hypotheses,
proof gaps, terminology inflation, and bibliographic issues. State the
highest exact result that survives. Preserve sound earlier rungs even if a
later cumulative rung fails.

Do not propose or begin successor mathematics beyond concise implications of
the findings.
