# RQ0-L0 public architecture reset: hostile-review adjudication

**Target snapshot:** `e55cb68`  
**Frozen report:** `f4d4e0b`  
**Review verdict:** `REJECT`  
**Adjudicated verdict:** `REJECT — ACCEPTED`  
**Scientific outcome:** null  
**Cycle state:** closed; no repair or rescore

## 1. Decision

The hostile verdict is accepted. The public architecture snapshot is not a sound overlap-first localization architecture and is not eligible for estimator freeze or a scientific RQ0-L0 claim.

The rejection does not negate the finite arithmetic that the reviewer independently rebuilt. The opened `Q8` instrument, its W3 write/preserve/erase/no-write seams, its declared order-four subinstruments with common order-two core, its absence of a normal direct-product pair, and the small direct-product calibration remain public mathematical fixtures. They do not establish intrinsic localization.

No file in the proposer, verifier, `Q8` model, public audit or frozen report is changed by this adjudication.

## 2. Independent checks of the load-bearing attacks

### A1. Empty-atlas acceptance — accepted, fatal

The adjudicator replaced the valid public `Q8` claim's arrows, pair intersections and triple intersections by empty arrays while retaining its five objects and reconstructed operational/record scopes. The trusted verifier returned:

```text
AtlasVerification(category='positive', operational_scopes=4,
                  record_scopes=4, objects=5, arrows=0,
                  pair_intersections=0, triple_intersections=0,
                  coherent_paths=0)
```

The positive gate therefore does not require a category, identities, executable overlap maps or any overlap. F1 is accepted as fatal.

### A2. Shared-handle intersections — accepted, fatal

The proposer forms pair and triple scopes by literal set intersection. The verifier requires each regional operation scope to be a subset of the ambient operation handles and again tests intersections by literal intersections of those handles. A simultaneous ambient renaming is not an independently presented regional isomorphism test.

Thus the implementation presupposes the very common support labelling that physical pullbacks were meant to establish. F2 is accepted as fatal.

### A3. W3/fact-algebra disconnect — accepted, major

The adjudicator replaced every valid binary `ambient_projector_resolution` in the ambient dataset and claimed objects by the valid one-atom resolution `[[0,1]]`, leaving each W3 witness's binary cut projectors unchanged. Verification still returned the original positive five-object, twelve-arrow, three-pair, one-triple result.

The transported Boolean algebra therefore need not be the algebra certified by W3. F3 is accepted as major.

### A4. Mutation-matrix scope — accepted, major

Inspection confirms that the reported 481 mutations systematically exercise normalized missing keys, wrong types, unknown keys and representative leaf types, not every registered semantic corruption for every applicable field. The `NOT-REACHED` example is supplied as a Boolean sequence rather than produced by an interrupted scientific pipeline. F4 is accepted; the 481/481 statement remains true only for the implemented mutation generator.

### A5. Unstructured-success path — accepted, major

The adjudicator supplied a serializer returning the exact string `not-json`. After a verified positive request, the total entry point returned exit code zero and `not-json\n`. It authenticates the serializer's return type but not one structured response agreeing with the resolved outcome. F5 is accepted as major.

### A6. Direct-factor and P8 scope — accepted, major

The internal search has a sound finite P1--P7 core for candidate tuples of two through eight proper normal factors, subject to its caps. The public result exposes only finest certificates, does not authenticate that all positive decompositions were returned, and treats P8 as algebraic subtuple closure rather than stability of every operational and record field under restriction. F6 is accepted with that exact scope. No general localization conclusion follows from the factor search.

### A7. Trusted-kernel scope — accepted, minor

The verifier is independent of the new proposer and public fixture truth, but its kernel wraps a broad legacy estimator dependency. This is not evidence of a circular `Q8` decision, but it defeats the description of a minimal standalone trusted arithmetic kernel. F7 is accepted as minor.

### A8. Twisted-triple coverage — accepted, minor

The implementation composes the listed map families, but the public twisted fixture does not independently mutate preparation, probe, readout and gauge loops. F8 is accepted as a limitation of adversarial coverage, not as a demonstrated defect in those composition functions.

## 3. What survives

The following statements survive only as opened finite calibration facts:

1. The exact `Q8` amplitude representation and stated finite group arithmetic reproduce.
2. The constructed W3 witnesses exhibit occurrence, preserving availability, erasing unavailability with cross coherence, and no-write failure.
3. The designer-supplied `Q8` presentations form the intended five-object inclusion diagram when global operation handles are retained.
4. `Q8` has no proper normal direct-product decomposition, while the small product control has the stated factors.
5. The exact sparse and dense monomial-span calculations agree at the checked public scope.

These facts establish neither a generic physical overlap nor an intrinsic process chart.

## 4. What does not survive

The cycle does not establish:

- a sound overlap-first verifier;
- handle-independent operational subinstruments;
- categorical physical pullbacks reconstructed from independently presented instruments;
- a record functor derived from the W3 projector algebra;
- an intrinsic localization atlas or localization groupoid;
- a no-go theorem for localization.

It also establishes no topology, influence, causality, geometry, spacetime, field or gravity result.

## 5. Final disposition

The public snapshot remains immutable as a failed architecture experiment. The final cycle disposition is:

```text
external review:     REJECT — ACCEPTED
engineering object:  PUBLIC-ARCHITECTURE-CANDIDATE REJECTED
scientific outcome:  null
Q8 status:           opened calibration evidence only
repairs/rescore:      prohibited in this cycle
```

The first unresolved scientific obligation is now explicit: define an intrinsic quantum process chart and its overlap without pairing a W3 witness with an independently supplied support label, shared operation handle or planted ambient intersection.

This closes the public architecture-reset cycle. Further work requires a separately pinned authorization. `RQ0-T1` and `RQ0-C1` remain closed.
