# COC — JOINT ADJUDICATION OF THE THREE-LENS PANEL

**Status:** ADJUDICATED, 2026-08-08 (v13 LOG #234).  **Object:**
commit 9c617e8, protocol ed968c5.  **Frozen reviews:** R2 #232,
R1 #233 (fd8fe8abf9d4), R3 #234a (88a011e1dc1c).

## Grade resolution

R1 AWF / R2 AWF (F1 fatal-if-unrepaired) / R3 AWF; ~289
recomputations, ZERO computed-number errors.  **Adjudicated:
REJECT-AS-DELIVERED — MANDATORY RE-DERIVATION.**  The delivered
closure verdict is analytically forced at its atlas (the
membership gate could not fail on any admissible input at any
completion — R2's telescoping equivariance lemma; R1's and
R3's coboundary mechanisms, three independent derivations);
its "576 free triangles" clauses are false; its verdict
derivation is ungated; its census routes are one identity.
AND the pin's genuine positive outcome was found BY THE PANEL:
**COC-HIGHER-OBSTRUCTION IS REACHABLE ON BASE G** — the
paper's own chart rule applied to the base's own declared
order-8 extension group yields 16 charts, 333,312 triangles,
32,256 defects outside the bigon group (two independent
constructions, identical counts), the escaping element NAMED:
W·τ_A·τ_B (order 2; inside the declared 216-scope, outside
the Klein four).  **THE CRITERION (the unit's true central
result): CLOSURE ⟺ THE CHART-GENERATING GROUP CENTRALISES THE
LEVEL-1 HOLONOMY** (the declared S abelian → forced closure;
the extension's 8-group non-abelian, 4/8 centralise → escapes).

## Re-derivation order (C-1..C-5, binding, one pass)

**C-1 THE VERDICT INVERTED:** unit verdict =
COC-HIGHER-OBSTRUCTION (reachable on base G, at the extension
atlas built by the unit's own chart rule from the base's own
declared group); the declared-atlas closure reclassified as an
analytically-forced DISCLOSURE (the lemma + coboundary stated
as theorems with the reviewers' proofs incorporated natively);
every "could have carried" sentence deleted; D-never restated
as analytically forced (D lies in no declared scope; the
scopes are groups).  **C-2 THE CRITERION CENTRAL:** stated and
GATED AT BOTH ATLASES (abelian → 0/1,824 forced;
non-abelian → 32,256/333,312 escapes; the centralizer
computation 4/8 gated); the mechanism stack native (R2's
equivariance lemma; R1's Z/2-coboundary per rule w/ f_FULL =
[σ=W]; R3's closed form δ = ∏_{REAL} w_X·w_Y).  **C-3 THE
EXTENSION CENSUS IN-DELIVERY:** 16 charts / 333,312 / 32,256
run and gated inside the delivery, the reviewers' counts as
external anchors to the frozen reviews (hash-pinned).
**C-4 INSTRUMENT:** the verdict DERIVED INSIDE A GATE
(recomputed from census counts; a verdict-flip mutant — R3's
one-character probe — must die by it); the census given a
genuinely independent second route or the identity disclosed,
PLUS a cell-completeness gate (the skip-a-cell mutant dies);
COC-MEMBERSHIP's tabulation tested (the false-zero probe a
mutant); the R1-route prose corrected (split charts glue to
each other — 368 identifications, 3,552 triangles, no escape —
measured and stated); §12's excluded-gate name fixed;
forcedness stated at link level.  **C-5** all three rewrite
tables row by row; deviations updated; single-threaded; the
one meta-sentence cites v13 LOG #234.  **RUNBOOK §13 ADDENDUM
(this commit): the printed verdict must itself be DERIVED
INSIDE A GATE from the measured counts, with a verdict-flip
mutant proving the derivation can fail; and a census's "two
independent routes" must be independent computations — an
algebraic identity of one route is one route.**
