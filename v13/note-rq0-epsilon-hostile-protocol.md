# STAGE-5 HOSTILE PROTOCOL (FROZEN BEFORE DISPATCH)

**Status:** FROZEN, 2026-08-06, per RUNBOOK §13(1).  **Object**
(commit d5eca4e): paper + code + output/receipt at their committed
SHAs (receipt-pinned source).  **Pin:** 6e1aa82.  Reviews judged
against THIS protocol only.

## Verdict vocabulary

ACCEPT / ACCEPT-WITH-FIXES / REJECT — plus per-rung confirmations:
(a) EPSILON-BLIND incl. the inverted ordering and the structural
proof; (b) the ε=0 reduction both directions; (c) the closed form
(identity-containing ⟹ ε law-independent); (d) the mixed-law
closures + the law-free-descent finding; (e) ω's separations; (f)
the rung set incl. the combined MIXED-LAW-CLOSED +
PLURALISM-PRICED registration.

## The four kill-shots (all reviews attempt all; primaries below)

K1 — **The inversion's robustness.**  The inverted ordering
(forgery = least positive defect) is measured at the committed
state.  Attack with other admitted states (the sink-only
state-relativity is disclosed — map the state-dependence; is
there an admitted state where ε DOES separate?).  If yes, the
blindness headline needs a state-quantifier rescope; if no at the
committed class, the structural proof gains a measured companion.
K2 — **The structural blindness proof.**  "ε is a function of the
quadruple; provenance is not a component" — audit the proof; test
whether ω (which reads the realized process) is ALSO a function
of the quadruple (the law is the fourth component — does ω
actually escape the structural argument, or does it read
something outside the quadruple?  This decides whether
BLOCKED-AT-PROVENANCE is correctly instantiated).
K3 — **THE UNASKED QUESTION, mandatory: does ω separate forged
from legitimate COARSE patches?**  The unit separated W1/W3 and
the counter-law preparations but never ran ω on the
forged-vs-legitimate coarse comparison that ε failed.  Run it.
If ω separates, the escalation softens (grading exists, on ω not
ε); if it does not, the provenance-as-data escalation hardens.
Either answer reshapes the adjudication.
K4 — **The law-free descent finding + the closures.**  Verify the
terminal descent selector reads no law (the attack-at-every-law
claim, 14/15); audit both closures' "imports the blockade"
adjudication (the forbidden-inference charge against shared
refinement; the constructed law-relative selector's 14→0 and
51→1); check MIXED-LAW-CLOSED + PLURALISM-PRICED as a combined
registration is honest rather than having-it-both-ways.

## Lenses (independent; no cross-reading; own exact code in
## separate scratchpads; repo read-only; ONE file each)

R1 operator → v13/review-rq0-epsilon-operator.md — K1 primary +
the closed form re-proved + the spectrum and tower welds (≥10
numbers).
R2 effectus → v13/review-rq0-epsilon-effectus.md — K2 primary +
the ε=0 reduction both directions + the entailment-converse lemma
+ monotonicity (≥10 numbers).
R3 instrument → v13/review-rq0-epsilon-instrument.md — K3 primary
(run ω on the coarse comparison) + K4 + the ten deviations
adjudicated + anchor fidelity (≥10 numbers).

## Common gates

Paper-vs-receipt sweep; scope tags (state-relativity named at
every ε claim); forbidden vocabulary; prose vs gates; deviations
complete; mutants/determinism/floats; single-threaded.

## Discipline

Exact arithmetic; /opt/homebrew/bin/python3.13; stall doctrine
(< 8 min silent, ≤ ~40 min); no child agents; no git mutations;
freeze-on-delivery; final report = compact verdict + top
findings.
