# RQ0-SYNTH — ARENA-RELATIVITY ADOPTED (MINIMAL FORM):
# THE SYNTHESIS + FIRST INVARIANCE CENSUS (PIN)

**Status:** PIN, STRICT, 2026-08-07.  **Immutable base:** commit
77b015e (the six-level tower terminal: B #123, B′ #134, B″ #145,
stage 5 #156, branch C #166, branch A #177).  **Authorization:**
the user adopted branch B in minimal form 2026-08-07 ("go ahead"
on the recommended plan): ONE bounded synthesis unit closing the
RQ0 line; then the critical path resumes.  No open-ended
arena-relativity research line is authorized.  Lean NONE.

## The thesis (prose, cited, not gated)

ADOPTED-ARENA-RELATIVITY: the legitimacy of a coarse patch is not
intrinsic (B: top-of-lattice), not certifiable by families of
contexts (B′: the adversarial pair), not law-structural at coarse
grain (B″: reversibility forbids coarseness), not state-carried
(stage 5: EPSILON-BLIND, inverted), not law-remembered (C: the
coarse present cannot testify), not provenance-carriable even at
amplitude level (A: the constant holonomy, blindness) — it is
relative to a DECLARED ARENA.  The paper states this
single-threaded with the six-unit theorem record (one row per
unit: ledger entry + commit + headline theorem).

## The census (the gated content)

An ADMISSIBLE ARENA is a declared tuple drawn EXACTLY from the
terminal fixtures (no new objects): patch declaration ∈
{legitimate tomographic, forged 2+1+1, forged 2+2} × law ∈ the
five committed laws (the 687-law census reused only where a
candidate demands it) × state ∈ the 13 named states × admitted
relabelling ∈ the admitted-isomorphism group × checkpoint-phase
switching ∈ the 512 (where amplitude data is in play).  The
family is enumerated finitely; its size COMPUTED, never typed.
The ACTION of arena-change on each quantity is DECLARED
per-quantity BEFORE any fixture value is computed (§13(4); the
receipt's gate order proves it).

Quantities (frozen; per-quantity verdicts mandatory):

- **Q1** the fine-grained law's transition data (expected
  positive control).
- **Q2** the one-step closed holonomy (the constancy theorem's
  ζ₈⁴ = −1; positive control at amplitude scope).
- **Q3** the rigidity classifier (B″'s identity-free flag).
- **Q4** the name-blind generation/collision profile (branch-C
  corridor machinery reused; name-blindness gated FIRST).
- **Q5** the legitimacy certificate functions (the V-CL
  certificate; cl) — expected ARENA-ARTIFACT (negative control:
  must move).
- **Q6** ε (the Bayes error, under its declared state+partition
  action) — expected artifact or inert.
- **Q7** ω — expected ARENA-INERT (fixed but degenerate; the
  inert/invariant distinction is first-class, ω taught it).
- **Q8** cross-arena overlap data: what two declared patches
  share on intersection (the RQ0-scope seam datum).
- **Q-OPT** Δᴮ (v12 paper 1): ONLY behind an explicit
  exact-transport gate to the committed carrier; failure →
  BLOCKED-AT-IMPORT recorded plainly, never fudged.
- Worker candidates admitted only if declared + action-typed
  before fixture truth (the branch-C C4 rule).

## Pre-registered outcomes (only these)

- Per quantity: **ARENA-INVARIANT-⟨Qk⟩** (fixed under the full
  action, WITH content — nondegeneracy gated) /
  **ARENA-INERT-⟨Qk⟩** (fixed but degenerate) /
  **ARENA-ARTIFACT-⟨Qk⟩** (moves) / **BLOCKED-AT-⟨object⟩**.
- **RQ0-SYNTH-CENSUS-COMPLETE** — every verdict entered; positive
  AND negative controls behaving.
- **ACTION-TOO-WEAK** — if the arena action fails to move ANY
  declared negative control, the family under-generates: a kill
  for the census as instrumented, reported plainly.

## Discipline

Exact arithmetic (Fraction / integer cyclotomic tuples; no
floats).  Anchors exit-1-only on every terminal value reused (the
tower's headline numbers re-anchored, never retyped).  **§14
MANDATORY:** the census instrument self-tested under the admitted
relabellings and the 512 switchings — applying an admitted
symmetry to the WHOLE configuration must fix every gated
invariant, and a deliberately mis-conventioned control must move.
Positive and negative controls both mandatory.  Mutants over
anchors AND gates, incl. sign/orientation mutants and a
stat-label-style name-reader; every mutant dies with named kills.
Byte-identical delivery-mode runs (write guard as in L5).  Stall
doctrine (< 8 min silent, ≤ ~45 min).  NO git.
FREEZE-ON-DELIVERY.  Deviations ship.  Deliverables:
v13/paper-rq0-arena-synthesis.md +
v13/code/rq0_synth_census_exact.py + _output.txt + _receipt.json.
Then §13: protocol freeze → three-lens panel → adjudication →
STATUS.  **THE UNIT CLOSES THE RQ0 LINE; the successor is the
roadmap's critical path (O4-on-W6-base / nomological transport),
NOT a further arena unit.**
