# Independent hostile review dispatch — operator algebra / effectus

**Status:** `DISPATCH`

**Date:** 2026-07-31

**Governing pin:** `e994807`

**Immutable paper commit:** `a57bed6`

**Review mode:** external, independent, repo-read-only, mathematical hostile
review

## 1. Object under review

Review the complete immutable paper

`v13/paper-rq0-sharp-facts-unsharp-evidence.md`

against

`v13/note-rq0-w3-effect-transport-pin.md`.

Read the Weld-stack paper, both Weld hostile reports and their joint
adjudication only as binding antecedent scope. Preserve the surviving
`RQ0-L0-REPRESENTABLE-W3-SKETCH` result and terminal RQ0-A at their exact
scopes unless a direct error in the present paper's use of them is found.

Do not edit the repository. Use `/private/tmp` only for scratch. Do not repair
the paper. Return one complete durable report in your final response.

## 2. Independent source and convention audit

Check the finite-dimensional UCP claims against primary operator-algebra and
effectus literature, including the cited sources where relevant. Do not accept
a citation in place of the displayed finite proof.

Audit the fixed variance convention:

$$
G:C\to B,
\qquad
F:B\to A,
\qquad
F\circ G:C\to A.
$$

Determine whether every object, arrow, undercategory, category of POVM
interfaces and functor uses this convention consistently.

## 3. Effect and instrument typing

Independently prove or refute:

1. UCP maps restrict to the claimed effect-module maps;
2. fixed-outcome POVM transport is a total functor;
3. `Rec_eff` is actually typed by the displayed undercategory and
   $\mathsf{POVMIfc}_\Omega(D)$;
4. the instrument precomposition and effect-shadow statements are correctly
   oriented and typed; and
5. the Lüders/reprepare maps are CP instruments with identical effects and
   genuinely different disturbance.

Reject any point where a POVM is silently promoted to an instrument or an
effect is called a projection without proof.

## 4. Defect theorem

Rebuild from raw algebra:

$$
\Delta_F^{\mathrm{rec}}(a,b)=F(ab)-F(a)F(b),
\qquad
\Sigma_F(a)=F(a^2)-F(a)^2,
$$

the Kadison--Schwarz positivity statement, the projection sharpness
criterion, both exact chain laws and unitary covariance. Check the statement
that both chain-law summands are positive and the qualification concerning a
nonfaithful outer channel.

Recompute exactly the random-unitary, amplitude-damping and two-noisy-channel
controls. Confirm that $\Delta^{\mathrm{rec}}$ is not being confused with the
Born composition defect $\Delta^B$.

## 5. Multiplicative domain and Boolean transport

Independently prove or find a counterexample to every implication in Theorem
6.2. In particular, check whether sharpness of every atom of a finite PVM is
sufficient to place the generated record algebra in the multiplicative
domain of a UCP map.

Audit $\mathsf{SharpRec}_D$:

- object and arrow typing;
- the image-containment condition;
- identity and composition closure;
- direct versus composite Boolean action;
- covariance versus contravariance language; and
- the limitation that Boolean transport does not establish W6 co-reference.

## 6. Operator-system theorem

Check that $\mathcal S_W(y)$ is the least unital self-adjoint linear space
containing exactly the admitted transported record effects. Audit minimality,
monotonicity, presentation covariance, handle invariance and the inaccessible
spectator law.

Recompute the branch-memory transport:

$$
N^*Z_mN=Z_m,
\qquad
U^*Z_mU=X_bZ_m,
$$

and the claimed dimensions three and four for the operator system and its
C*-closure. Decide whether any product is mislabeled as an admitted
experiment.

## 7. Karoubi and addressability audit

Audit the admitted Karoubi completion, including arrow direction,
$f=dfe$, identities and composition. Check precisely what a UCP idempotent
range inherits under the ambient product and under the Choi--Effros product.
Distinguish conditional expectations from general UCP projections.

Test the order

$$
e\preceq f\iff ef=fe=e
$$

and the fixed-range implication. Decide whether the paper correctly proves
that a literal no-containing-idempotent case is impossible because identity
is admitted, and whether replacing it by no-proper/identity-only is a valid
correction rather than noncompliance.

Independently attack every addressability control:

- unique dephasing candidate;
- abstract but unadmitted expectation;
- global/full-operator-system case;
- exact symmetry example with $c=1/2$;
- the normal-form claim that only $e_n,e_m$ are proper idempotents in the
  declared generated grammar;
- the action-groupoid/no-selection conclusion;
- inaccessible spectator; and
- the branch-memory joint-spectral conditional expectation.

Look especially for an unlisted finite word in the symmetry grammar that is
another idempotent below the stated minima.

## 8. Rung and no-smuggling audit

Grade separately:

- `RQ0-L0-W3-EFFECT-TRANSPORT`;
- `RQ0-L0-SHARP-FACT-TRANSPORT`;
- `RQ0-L0-ADDRESSABLE-W3-OPERATOR-SYSTEM`.

Later failure must not erase a secure earlier rung. Conversely, mathematical
existence of an unadmitted map cannot earn physical addressability.

Determine whether the paper consistently remains law-relative and whether
any wording implies intrinsic chart, support, overlap, localization or
spatial structure.

## 9. Required verdict

Return exactly one overall verdict:

- `ACCEPT`;
- `ACCEPT-WITH-FIXES`;
- `HEADLINE-DOWNGRADE`;
- `REJECT`.

Then state separately for every registered rung: `EARNED`, `WITHHELD`, or
`BLOCKED`, with the first exact obstruction. List surviving theorems even if
the cumulative headline fails. Distinguish fatal mathematics, scope repair,
terminology and optional strengthening.

No repair, implementation, topology, influence, causality, geometry, fields
or gravity work is permitted.
