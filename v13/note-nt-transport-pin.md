# NT — NOMOLOGICAL TRANSPORT OVER THE W6 BASE (PIN)

**Status:** PIN, STRICT, 2026-08-07.  **Immutable base:** commit
a264a06 (O4 TERMINAL #199).  **Authorization:** the user's order
2026-08-07 ("ok work on O4 and transport") — this is the second
half.  Lean NONE.

## The question

Can LAW-DATA — not facts, the law's own local data — be carried
between declared contexts along the base's structure, and is the
carrying PATH-DEPENDENT?  Two paths with the same endpoints
exist canonically (the two leg-orders of the O4 frames form a
loop).  Path-dependence of lawful transport = holonomy = the
first geometric structure this theory could EARN rather than
assume.

## Inherited anchors (exit-1 against the terminal receipts)

(1) **PREFIX-DECIDES — THE PANEL'S MANDATORY FIRST ANCHOR**: the
unit re-derives it independently ON THIS BASE before any
transport result is stated (it was panel-unseen at O4; its
independent re-derivation here is a pin clause, not optional).
(2) The wing-exchange orbit cause.  (3) K1 universality (36/36
cross-frame disjoint / 30/30 same-frame share at t=2; 0/36 at
t=1, t=3).  (4) LTP-LAWFUL at division events only.

## The instrument (frozen before fixture truth)

Paths = sequences of admitted moves (leg applications;
co-reference identifications where the O4-terminal instrument
certifies them).  Transported objects, each with its DECLARED
per-coordinate action (§15) and matched coordinates (§15
addendum — read time a declared coordinate in every cell, the
O4 lesson baked in):
- **T1** local transition data (the law's restriction to the
  context).
- **T2** the composition-defect datum Δᴮ — ONLY behind an
  exact-posability gate (the RQ0-SYNTH lesson): if the question
  cannot be posed at the committed laws, NT-BLOCKED honestly,
  never forced.
- **T3** the amplitude/phase layer in gauge-invariant closed-loop
  form (the L5 lesson: §14 gauge-covariance self-test under the
  512 switchings MANDATORY, sign/orientation mutants caught by
  it).

## The central hypothesis (pre-registered, falsifiable)

**THE PREFIX CRITERION IS THE FLATNESS CONDITION**: paths lying
in aligned-prefix corridors transport flatly; paths crossing
prefix divergence are obstructed or twisted.  The unit TESTS
this — it could come out otherwise (a flat crossing or a twisted
corridor kills it; both probed with teeth).

## Pre-registered outcomes (only these; per object)

- **NT-FLAT-⟨Tk⟩** — path-independent WITH content
  (nondegeneracy gated; a degenerate constant is NT-INERT-⟨Tk⟩).
- **NT-HOLONOMY-⟨Tk⟩** — path-dependent: the holonomy computed
  exactly, its value set and group structure named, gauge-fixed
  content separated from gauge orbit (the L5 disease gated
  against).
- **NT-OBSTRUCTED-AT-⟨object⟩** — transport fails lawfully; the
  obstructing object named (expected at prefix divergence if the
  hypothesis holds).
- **NT-PREFIX-FLATNESS-⟨CONFIRMED|REFUTED⟩** — the hypothesis'
  own verdict, from the matched table of paths.
- **NT-BLOCKED-AT-⟨object⟩** — census discipline.

## Discipline

Exact arithmetic; measurement-founded verdicts w/ the
declaration flip-test as permanent gate; §14 fresh-eval
self-tests (zero cache hits gated) incl. the gauge sweep;
positive control (a path pair KNOWN to agree — same-order
decompositions) and negative control with teeth (a
deliberately twisted comparator that MUST show holonomy);
mutants incl. prefix-lax, path-collapse, gauge-sign,
readtime-conflate; every must-pass gate falsified by some
mutant (never_falsified EMPTY, gated); counts computed never
typed; byte-identical delivery-mode runs; stall doctrine; NO
git; FREEZE-ON-DELIVERY; deviations ship.  Deliverables:
v13/paper-nt-nomological-transport.md +
v13/code/nt_transport_exact.py + _output.txt + _receipt.json.
Then §13: protocol freeze → three-lens panel (first anchor:
the independent PREFIX-DECIDES re-derivation checked FIRST) →
adjudication → STATUS.  Successor: overlaps/autonomous charts,
carrying whatever this unit earns.
