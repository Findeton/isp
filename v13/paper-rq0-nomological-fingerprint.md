# The Nomological Fingerprint Hunt

## Whether the Law Remembers What Forgetting Erased — and Why Every Separator It Offers Is Grading Resolution or Reading the State

**Status:** `DELIVERED (v13 RQ0-L4, branch C)`

**Date:** 2026-08-06

**Frozen analytical pin:** `b4fc87c`

**Immutable base:** `a5cb096` (stage 5 TERMINAL; B″ TERMINAL; all fixtures at
their terminal values)

---

## Abstract

The terminal cycle closed at a block one level out from the carrier and the
law: **forgery is not a function of the quadruple**, witnessed by a collision
— two declarations of opposite provenance presenting the same declared data —
so no statistic on declared data separates a forged declaration from a
legitimate declaration of the *same* patch. It left open the case the
committed fixtures actually present: coarse patches of **distinct** quadruple,
where ε, ω and the whole refinement-monotone class fail alike but where no
impossibility was proved. This unit runs that question as a bounded hunt with
the candidates frozen before the fixture truth, and then runs the hunt's own
corridor against nine further statistics built to defeat it.

Six candidates were declared and hashed before any fixture value existed: the
pin's three — the counterfactual occupancy profile C1, the generation-profile
mismatch C2, the law-fan-in C3 — and three worker constructions, a
state-symmetry alignment defect C4a, a law-reachability alignment defect C4b,
and C4c, the *maximal covariant candidate*, declared precisely so that the
impossibility, if it came, could not be a failure of imagination. Every
corridor gate was then decided on an **off-fixture population** — the 48
records of the committed carrier that are not one of the four committed
boundaries, plus the 745 identity-free census patches.

**Three candidates separate the committed pairs and none of them reads
provenance.** C2 is measured to *be* ε — the generated structure at an
identity-containing law is discrete, so the mismatch collapses onto the
concordance defect at all 240 off-fixture instances — and dies on the pin's
own non-Bayes gate. C3's fan-in is identically 1 at the committed carrier over
a declared population of 2847 laws: the resource that is real at the census
arity (874 keys, 175 reachable from more than one law, one from 428) does not
transport, and C3 is degenerate before it is anything else. C4b is a function
of ε off-fixture and separates nothing. What survives to the fixture is C1,
C4a and C4c — and what they are is the finding.

**C4a separates at every committed law, 0 against 1/4 and 1/4, and the amnesty
sweep kills it**: over the whole declared state simplex at denominator 16 the
separation holds at 1276 states, ties at 2017 and **inverts at 1552**. That is
`RQ0-L4-FINGERPRINT-AMNESTY`, the ε pattern recurring on a statistic built to
avoid it.

**C1 and C4c separate and survive the sweep, and they survive it for reasons
that disqualify them.** Both are measured *anti-monotone* in the refinement
order — off-fixture and, by the measured extension of §6, over the whole
52-record lattice; the committed triple is a **chain** whose coarsest member is
the legitimate patch; so their separation is entailed by the order alone, with
no reading of provenance. Both certify **total erasure** at their own
separating tolerance: the one-atom boundary, which forgets the entire carrier,
is admitted by the very threshold that admits the legitimate chart. And C1's
separating content is not new. C1 is the sum of ω over the 31 admitted
preparations, and the rows at which ω ranks the legitimate patch best are
exactly the rows the terminal cycle already counted and already dismissed —
**15 of 155**, 12 at FUNNEL and 3 at the counter-law, recomputed here — as *"a
grading of coarseness with the sign flipped rather than a reading of
provenance"*. The pin's letter is met at the counter-law and the letter is not
the finding: **`RQ0-L4-NOMOLOGICAL-FINGERPRINT` is withdrawn.**

**The class result is a theorem about the covariant quotient, and it is not
about resolution alone.** The admitted isomorphisms of the declared data are
the 24 relabellings fixing the retained sink and permuting the four success
addresses — measured, not assumed — and under them the 52 records fall into
exactly **12 orbits**, two records sharing an orbit if and only if they share
the *shape profile*. That invariant is strictly finer than resolution data: the
block-size profile has **7** classes, and exactly **5** of them split into two
orbits apiece (12 = 7 + 5). The extra datum is **how many configurations a
boundary merges with the one the declared state marks out** — a state datum,
not a resolution one; at the uniform state it vanishes identically, the group
is the whole 120 and the orbit invariant *is* the block-size profile, which is
the control that settles the point. So the honest class theorem is a
**factorization**: every admitted-isomorphism-covariant statistic is a function
of the pair (resolution profile, marked-block excess), hence every covariant
statistic is **resolution-reading or state-reading** — and the first arm is
what the pin's gate 2 excludes, while the second is what the amnesty sweep
exists to sweep. That is the fused form of `RQ0-L4-CLASS-IMPOSSIBILITY`, stated
over the class it covers and no wider: the full-group hypothesis is carried by
**25 of the 4845** declared states, and 1200 carry no symmetry at all.

**Where covariance has no content, a principle must do the work.** At a
configuration whose admitted-isomorphism group is trivial, gate 5 is satisfied
vacuously and *label-reading* statistics walk in — statistics that compare the
carrier's names. Two are exhibited here, from the panel that reviewed this
unit: an indicator of one named atom, and an indicator of the committed
legitimate boundary itself. Both separate, neither is order-entailed, and no
state inverts either. They are excluded by **name-blindness**, imposed here as
an explicit corridor gate: a statistic on declared data must be a function of
label-free invariants, so that relabelling the carrier throughout the declared
datum — boundary, law, preparation and state together — cannot move its value.
This is the corpus's own co-reference discipline, under which names are never
compared, and it has content exactly where covariance has none.

**Measured over the name-blind class at every committed configuration**, across
the six declared candidates and the nine constructions built against this unit:
**42 separating (statistic, law) rows are name-blind, and every one of them
falls to an arm — a function of the covariant shape invariant, or a verdict
entailed by the committed chain's order, or a separation the amnesty sweep
inverts. None is unaccounted. The only rows that escape every arm are the six
label-reading ones, and the name-blindness gate takes them.** There is no
robust name-blind separator at any committed law.

The freeze is claimed only where it holds. The definitions were SHA-256
registered before any fixture value existed, and an automated **source scan**
verifies that no candidate definition mentions a committed boundary — the check
that works, self-tested against a negative control that it catches. What is not
claimed is that the gate order proves anything: covariance and name-blindness
are orbit properties, so they evaluate candidates at relabelled arguments, and
**10,028** of those arguments are committed boundaries — 864 in the covariance
gate alone. Every such value is equality-tested against its own off-fixture
partner; none is retained, thresholded or used in any selection. The count is
disclosed rather than denied.

The registered outcome is

$$
\boxed{
\begin{array}{l}
\texttt{RQ0-L4-CLASS-IMPOSSIBILITY}\ \text{(fused)}\\
\texttt{RQ0-L4-FINGERPRINT-AMNESTY}\\
\texttt{RQ0-L4-BLOCKED-AT-THE-REFINEMENT-ORDER}\\
\texttt{RQ0-L4-NOMOLOGICAL-FINGERPRINT}\ \textbf{— WITHDRAWN}
\end{array}}
$$

---

## Scope box

| Item | Value |
|---|---|
| Dimension | finite throughout |
| Carrier | ONE committed carrier of five configurations — four success addresses and the retained sink — inherited unchanged |
| Patch | the inherited QUADRUPLE (boundary, declared task family, declared preparation, admitted law), read at a declared state |
| Committed pairs | the forged aligned manufactured $2{+}1{+}1$ $\{01\mid2\mid3\mid4\}$ and the forged aligned manufactured $2{+}2$ $\{01\mid23\mid4\}$, each against the LEGITIMATE corrected tomographic minimum $\{0123\mid4\}$ — of **distinct** quadruple. Same-quadruple pairs are excluded by the terminal proof and are not reopened |
| Laws | the committed DET (3125), FUNNEL (21, carried as a declared task family and not as a law), REV (120), FUNNEL-CLOSURE (3006) and the COUNTER-LAW (120) at five configurations; the 687-law census at three; a declared population of **2847** laws at five configurations for C3's fan-in, named as a declared population and never as a census |
| States | the committed $\rho=(1/16,1/16,1/16,1/16,3/4)$; the uniform state, as the control of §7.2; the selective-amnesty state $(1/2,0,1/2,0,0)$; and, for the amnesty sweep, the whole state simplex at denominator 16 — all $\binom{20}{4}=4845$ |
| Off-fixture population | the **48** records of the committed carrier that are not one of the four committed boundaries, and the **745** admissible patches at identity-free censused laws. The one-atom (total-erasure) boundary is *in* this population; it is not one of the four committed boundaries |
| Statistics gated | the **6** declared candidates, and the **9** constructions of the review panel, declared as post-fixture and gated by the same battery — 15 statistics × 5 committed laws |
| Orientation | fixed to the corpus's own defect semantics (stage-5 Def 2.2(b)): admitted iff the statistic is SMALL. A candidate wanting the other direction declares the negated form, which C4c does |
| "Coarse" | ALGEBRA-RELATIVE ONLY: a boundary properly coarser than the carrier's own configuration algebra. No spatial, causal, temporal, QFT or gravity reading |
| Arithmetic | exact rationals throughout; no float in any substantive path |
| Verification | corridor gates exhaustive over the 48 off-fixture records at every committed law and over the 745; the name-blindness gate exhaustive over the 48 records × all 120 relabellings at every committed law; the amnesty sweep exhaustive over all 4845 declared states; the orbit theorem and the 12-vs-7 decomposition exhaustive over all $52\times52$ record pairs; the full-group scope exhaustive over all 4845 states; the fan-in exhaustive over the 687-law census and over the declared 2847-law population |
| Lean | none |
| Not decided here | whether some statistic outside the 15 gated here separates; whether name-blindness is the *right* convention of legitimacy, as against a theorem (it is a principle, and it is declared as one); provenance as laboratory data; the atlas-level route the terminal cycle names as the other option; every later object |

Scope tags: `[FIN]` the finite declared scope above; `[OFF]` decided on the
off-fixture population alone; `[FIX]` on the committed fixture; `[SIMPLEX]`
over the whole state simplex at denominator 16; `[CEN]` over the 687-law
census; `[PANEL]` over the nine post-fixture constructions as well as the six
declared candidates.

The words *patch*, *carrier*, *coarse*, *reachable*, *occupied* and *shape* are
operational vocabulary about admitted operations and declared configurations.
No region, locality, topology, manifold, causal order, influence, spacetime,
field, QCD or gravity object is constructed, used or claimed.

---

## 1. The question, and what is inherited

### 1.1 Inherited, not re-derived

The **terminal dichotomy** and its rigidity trigger; the **concordance defect**
ε and its closed form, the boundary's Bayes error, equal to $(5-|\pi|)/16$ at
the committed state; the **occupancy defect** ω, its anti-monotonicity, and the
terminal cycle's verdict on it — *"where it is non-zero it therefore ranks the
coarsest patch best, which is a grading of coarseness with the sign flipped
rather than a reading of provenance"* — delivered on an explicitly counted set
of rows, **15 of the 155** (law, preparation) rows, 12 at FUNNEL and 3 at the
counter-law, six of them admitting the legitimate patch at tolerance zero; the
**every-state inversion theorem** — both forgeries are refinements of the
legitimate coarse chart and ε is monotone under refinement, so the forward
separating set is empty at all 4845 declared states; the **collision** — the
adversary's manufactured $1{+}1{+}1{+}1$ context and the legitimate address
context present identical declared data — and with it
`RQ0-L3-BLOCKED-AT-PROVENANCE`; the **factoring and fan-in** census, 874
(boundary, family) keys of which 175 are reachable from more than one declared
law and one from 428; and the **745** admissible patches at identity-free
censused laws, every one of them at ε exactly 0.

Nineteen anchors reproduce every one of those values by this unit's own routes
and exit 1 on mismatch (§11). Three of them are verbatim string matches against
the immutable paper (`A16`, `A17`, `A18`); each certifies the terminal cycle's
*words* and nothing about how this unit reads them, and where a reading is
this unit's own it is declared as such.

### 1.2 The question

Does the law itself remember what forgetting erased? Formally: is there a
statistic on declared data — evaluated, where the candidate demands it, at
**counterfactual** arguments the law admits, undeclared preparations and
generation profiles — that separates the legitimate tomographic coarse patch
from the forged $2{+}1{+}1$ and $2{+}2$ patches, robustly?

### 1.3 The result in one paragraph

Six candidates were frozen with their corridor gates before any fixture value
existed (§2, §3). Three die in the corridor on off-fixture evidence alone, one
of them by collapsing exactly onto ε (§4). Three separate the committed pairs
(§4.4). One of those, C4a, is the only declared separation not entailed by the
refinement order, and the amnesty sweep inverts it at 1552 of 4845 declared
states (§5). The other two survive the sweep, and are measured to survive it
because they are anti-monotone in the refinement order: their separation is the
chain's own order and their separating tolerance certifies total erasure (§6).
At a full-group configuration the admitted isomorphisms force every covariant
statistic through a 12-class invariant which is *resolution data refined by one
state datum*, so every covariant statistic is resolution-reading or
state-reading, and neither arm survives its own gate (§7). Where the group is
trivial that theorem is silent, label-reading statistics walk in, and
name-blindness is imposed as the principle that excludes them (§7.6). The
result is then measured over the whole name-blind class, the declared six and
the panel's nine together, at every committed configuration (§8).

---

## 2. The candidate family, as frozen

**This section is the pre-registration.** Every definition below, and every
gate below, was fixed and its source text hashed before a single fixture value
was computed; the receipt records the SHA-256 of each definition. Results begin
at §4. The nine further statistics of §8.3 are *not* part of this
pre-registration and are declared as post-fixture where they appear.

### 2.1 The corridor `[FIN]`

Six gates on every candidate, in the pin's order — nondegeneracy first, because
the ω failure mode is a statistic that is identically zero where the question
is asked — and the amnesty sweep after the fixture.

| gate | claim | why it is in the corridor |
|---|---|---|
| **G3** nondegeneracy | the candidate is non-constant on the 48 off-fixture records at the committed configuration | the ω failure mode: identically-zero-at-declared is a kill |
| **G1** monotonicity type | the candidate is **not refinement-monotone** | the terminal every-state inversion theorem kills that entire class: the forgeries sit *below* the legitimate chart in the refinement order |
| **G2a** non-Bayes factoring | the candidate is not a function of ε on the off-fixture population | ε is the boundary's Bayes error; a statistic factoring through it measures resolution |
| **G2b** non-resolution | the candidate is not a function of the covariant shape invariant on the off-fixture population | the strong reading of the same gate: partition-size *and* block-profile data are resolution data |
| **G5** covariance | the candidate is invariant under every admitted isomorphism of the declared data | the B″ discipline |
| **G-NB** name-blindness | the candidate is invariant under all 120 relabellings of the carrier acting on the whole declared datum at once | the corpus's co-reference discipline; the gate with content where the stabilizer is trivial and G5 has none (§7.6) |
| **G6** the identity-free side | the candidate is constant on the 745 admissible patches at identity-free censused laws, as ε is (exactly 0) | candidates must not break the dichotomy's ε = 0 side, where proper charts live |
| **G4** the amnesty sweep | *post-fixture*: any separator is swept over all 4845 declared states; separation that inverts anywhere is `FINGERPRINT-AMNESTY` | the ε failure mode |

**The two readings of G1, and whose reading each is.** The pin's G1 excludes
the monotone direction only. The terminal cycle states its own constraint on
any successor, carried here as a verbatim anchor (`A16`): *"no
refinement-monotone statistic whatever … Any successor must be non-monotone in
the refinement order, or must live on the atlas rather than on the patch."*
Read strictly that sentence is ambiguous, and the corpus's own vocabulary sets
*anti*-monotone in **contrast** with monotone rather than inside it, so the
either-direction reading is **this unit's extension and not an inheritance**.
It is nevertheless the right exclusion, on two warrants that are not the
wording. The first is the base's own adjudication of the anti-monotone case, on
the very rows this unit's C1 reuses: an anti-monotone grading *"ranks the
coarsest patch best, which is a grading of coarseness with the sign flipped
rather than a reading of provenance"* (`A17`, verbatim; the rows counted at
`A18`, recomputed at `A19`). The second is this unit's own erasure measurement
(§4.4): an anti-monotone separator's tolerance admits the boundary that forgets
the entire carrier. Both verdicts are computed for every candidate and both are
reported (§4.5). Nothing is re-gated after the fact.

### 2.2 The six candidates `[FIN]`

Orientation is fixed for all six: **admitted iff the statistic is small**.

**C1 — counterfactual occupancy profile.**
$$\Omega(P)\ =\ \sum_{X_0\neq\varnothing}\omega\bigl(A(B),\operatorname{Pres}_L(A(B)),X_0,\rho\bigr),$$
the profile of the occupancy defect over **all 31 admitted preparations**, not
only the declared one, summarised by its total; the underlying profile is also
read pointwise. This is the pin's answer to the DET forger who attains ω = 0
at all 31 declared preparations: compare profiles, not points.

**C2 — generation-profile mismatch.** Let $K=\ker(\operatorname{Pres}_L(A(B)))$
be the coarse structure the law actually *generates* — configurations merged
exactly when no declared task separates them — restricted to the patch's
realized process. Then
$$M(P)=\sum_{r\in A(B)}\Bigl[\Pr(r\cap\operatorname{Reach})-\max_{c\in K}\Pr(c\cap r\cap\operatorname{Reach})\Bigr],$$
the declared mass on which the generated structure resolves inside a declared
atom.

**C3 — law-fan-in.**
$$\mathrm{FI}(P)=\#\{L'\in\mathcal L_n:\operatorname{Pres}_{L'}(A(B))=\operatorname{Pres}_L(A(B))\},$$
the number of laws in the declared population presenting the same (boundary,
family) key — the terminal factoring result turned into a statistic on the
patch. $\mathcal L_3$ is the 687-law census; $\mathcal L_5$ is every
composition-closed law generated by one admitted single-valued operation
together with the committed laws, **2847** laws, named as a declared population
and never as a census.

**C4a — state-symmetry alignment defect.** Let $\lambda$ be the declared
state's level partition (configurations of equal declared mass). Then
$$A(P)=\sum_{r\in A(B)}\Pr(r)\cdot\mathbb 1[r\text{ is not a union of }\lambda\text{-classes}],$$
the declared mass carried by atoms that cut across the state's own symmetry.

**C4b — law-reachability alignment defect.** The same construction with the
law's own counterfactual structure in place of the state's: $\lambda$ replaced
by the reachability classes $j\approx k \iff
\operatorname{Reach}(\{j\})=\operatorname{Reach}(\{k\})$ of the declared
family. This is the candidate that asks the pin's question most directly —
does the law's own dynamics regenerate the declared atoms?

**C4c — the maximal covariant candidate.**
$$\mathrm{PR}(P)=-\sum_{r\in A(B)}|r|\,(|r|-1),$$
the negated count of ordered configuration pairs the boundary merges, declared
negated so that the coarsest boundary is the most admitted. It is declared for
one reason: §7 shows that every covariant statistic of the declared boundary
factors through an invariant of which C4c's is the resolution half, so if the
impossibility comes, it is not a failure of imagination.

---

## 3. The freeze, and what actually carries it

Three devices were declared. Two of them carry the freeze; the third does not,
and this section says which is which, because a methodological claim that
cannot be measured is worse than none.

1. **Definition hashes — carries.** `L4-00` registers all six candidates with
   the SHA-256 of each definition's source text, and asserts that no fixture
   value had been computed when it fired. The hashes bind this receipt to a
   source text a reader can inspect.
2. **The source scan — carries.** `L4-SRC` scans each of the eight declared
   definition functions, in its own source text, for any reference to a
   committed boundary (`PI1`, `P22`, `PTOMO`, `DISC5`, `ONEATOM5`, `FIXTURE`,
   `PROVENANCE`). Every one of the eight is a generic function of
   `(part, law, prep, rho, n)` and **none mentions one**. The gate is
   self-testing: it passes only if the eight are clean *and* the permanent
   **negative control** — `TUNE`, §8.3's statistic, which special-cases the
   committed legitimate boundary — **is caught by the same scan**. The freeze
   is checkable by reading the definitions, and this gate reads them.
3. **The barrier — does not carry, and no longer claims to.** Gate ordering
   establishes the order of *this run's* stages; it cannot establish authoring
   order, and it cannot certify that no candidate was evaluated at a committed
   boundary. That certification would be false. Covariance and name-blindness
   are **orbit** properties: they compare a candidate's value at a record with
   its value at the record's relabelled image, and the images of off-fixture
   records include committed boundaries — necessarily, since the forged
   $2{+}1{+}1$ and the forged $2{+}2$ lie in orbits of off-fixture records
   under the admitted isomorphisms, and the legitimate tomographic minimum lies
   in one under the full relabelling group. `L4-IMG` counts them exactly:
   **864** in the covariance gate (144 per declared candidate: 36 at each of
   the four symmetric laws, 20 at the forged $2{+}1{+}1$ and 16 at the forged
   $2{+}2$), and **9,164** in the name-blindness gate, **10,028** in all.

**Why the 10,028 are harmless, stated as a property and not as a promise.**
Each such evaluation is used in exactly one way: its value is tested for
*equality* with the value at an off-fixture record of the same orbit. No
ordering of fixture values is read, no fixture value is retained past the
comparison, none is compared to a threshold, and none enters any selection. A
candidate special-cased at a committed boundary does not survive this — it
breaks the very equality being tested, which is what happens to the negative
control (§8.4).

**A consolation the delivered instrument earns and should claim.** At the four
symmetric laws the covariance gate *would* catch fixture-special-casing at the
two forged boundaries, because a special case there breaks covariance. Under
the 24-element group the legitimate boundary's orbit is a singleton — **0** of
the off-fixture-record images reach it — so covariance alone leaves exactly one
tuning gap: the legitimate boundary, and the counter-law. **The name-blindness
gate closes it.** Under the full 120 relabellings the legitimate boundary *is*
the image of off-fixture records, and a statistic that special-cases it is
caught with an explicit witness — measured, at the record
$\{0124\mid3\}$ under the relabelling that exchanges 3 and 4 (§8.4).

The anchors of §1.1 are computed before the family is declared. They are
terminal values — immutable base, already published — and not this unit's
fixture truth.

---

## 4. The results, per candidate

### 4.1 Three candidates die on off-fixture evidence `[OFF]`

**C2 is ε.** Every committed law contains the identity; the identity preserves
every boundary and its collision partition is discrete; so
$\ker(\operatorname{Pres}_L(\pi))$ is discrete at every off-fixture record and
every committed law — **0 exceptions** in 240 instances (`L4-X2`) — and the
generated-mismatch statistic collapses to the concordance defect: **0
disagreements** over the same 240 instances (`L4-X3`). C2 is therefore
refinement-monotone and a function of ε, and dies at **G1** with **G2a** behind
it. The pin asked what the law generates; at an identity-containing law the
answer is *everything*, and the mismatch is the Bayes error again.

**C3's resource does not transport.** At the committed carrier the fan-in is
identically **1** over the 2847-law population, at every committed law and
every record: no other declared law presents the same (boundary, family) key.
C3 is constant off-fixture and dies at **G3**. The measurement is worth its
place because the resource is real one arity down — over the 687-law census
there are 874 keys, **175** reachable from more than one declared law, **699**
from exactly one, and the worst reachable from **428**. Fan-in is a census
phenomenon at three configurations and an empty one at five.

**C4b is a function of ε.** Under DET, REV and the funnel closure the declared
family contains every reversible operation, so every configuration reaches
every other and the law's reachability classes are the trivial one; the defect
is then 1 at every boundary but the one-atom one, which is a function of ε
off-fixture, and it dies at **G2a**. Under FUNNEL and the counter-law it is
constant and dies at **G3**. The candidate that asked the pin's question most
directly returns the same answer twice: at the committed laws, the law's own
counterfactual structure generates no coarse structure at all.

### 4.2 The corridor's survivor `[OFF]`

Exactly one (candidate, law) pair passes all of G3, G1, G2a, G2b and G5:
**C1 at the COUNTER-LAW**. C1 is degenerate at DET, REV and the funnel closure
— under DET, ω vanishes at all 52 records at all 31 preparations, 1612
instances, anchor `A07`, and C1 is measured constant on the off-fixture
population at REV and at the funnel closure as well — and at FUNNEL it is
nondegenerate but a function of the shape, failing G2b. At the counter-law it
is nondegenerate, anti-monotone, not a function of ε, not a function of the
shape, and covariant.

That last gate is where the honesty is, and its honest form is not that C1
failed something. §7 measures the admitted-isomorphism group of the declared
data at each committed law: 24 at DET, FUNNEL, REV and the funnel closure, and
**1 at the counter-law**. C1 passes G5 there **vacuously** — a vacuous
universal quantification is satisfied, not evaded, and nothing about C1 was
tested by it. The correct accounting is therefore **not that C1 failed a gate
but that C1 was screened by four gates, not five**. A symmetry-free
configuration is a perfectly admissible configuration; what disqualifies C1 is
§9's grounds 1 and 2, and they bite at every law, symmetric or not.

The one name-blindness failure among the declared six belongs here too, and it
is not a statistic reading names. **C3 at the counter-law** fails G-NB, with
the witness $\{0\mid1\mid2\mid34\}$ under the relabelling exchanging 3 and 4.
The cause is the declared population, not the statistic: C3 counts laws of a
population that is *not closed under relabelling*, because it adjoins the five
committed laws and one of them — the counter-law — is itself a label-reading
construction (§7.5), so a relabelled counter-law is not in the population. The
gate is measuring the population's non-closure. C3 is dead at G3 regardless,
its fan-in being identically 1.

### 4.3 The identity-free side `[CEN]`

| candidate | distinct values on the 745 | side intact |
|---|---|---|
| C1 | 3 | **no** |
| C2 | 1 | yes |
| C3 | 12 | **no** |
| C4a | 2 | **no** |
| C4b | 2 | **no** |
| C4c | 2 | **no** |

ε is constant (exactly 0) on the whole identity-free admissible side, so one
tolerance retains all 745. Only C2 — which is ε — keeps that property. Every
other candidate splits the side where *proper charts actually live*. This is
the collateral the pin asked to be gated, and it is not small: the
counterfactual sweep that gives C1 its content is exactly what makes it reject
patches the terminal axiom admits.

### 4.4 The fixture, plainly — separators first `[FIX]`

At the committed state, the committed preparation and each committed law:

| candidate | law | forged 2+1+1 | forged 2+2 | **legitimate** | total erasure | separates |
|---|---|---|---|---|---|---|
| **C1** | COUNTER-LAW | $95/8$ | $93/8$ | $\mathbf{45/4}$ | $0$ | **YES** |
| **C1** | FUNNEL | $14$ | $13$ | $\mathbf{23/2}$ | $0$ | **YES** |
| **C4a** | all five | $1/4$ | $1/4$ | $\mathbf{0}$ | $0$ | **YES** |
| **C4c** | all five | $-2$ | $-4$ | $\mathbf{-12}$ | $-20$ | **YES** |
| C1 | DET, REV, FUNNEL-CLOSURE | $0$ | $0$ | $0$ | $0$ | no |
| C2 | all five | $1/16$ | $1/8$ | $3/16$ | $1/4$ | no |
| C3 | all five | $1$ | $1$ | $1$ | $1$ | no |
| C4b | DET, REV, FUNNEL-CLOSURE | $1$ | $1$ | $1$ | $0$ | no |
| C4b | FUNNEL, COUNTER-LAW | $0$ | $0$ | $0$ | $0$ | no |

C2's row is the terminal spectrum, reproduced by an independent route: $1/16$,
$1/8$, $3/16$ — the inversion, unchanged. C1 read **pointwise** rather than as
a total separates at 12 of the 31 admitted preparations at FUNNEL and at 3 of
31 at the counter-law, and at none at DET, REV or the funnel closure; the
profile form does change the DET picture, and it changes it to zero.

**Every separator found here certifies total erasure at its own separating
tolerance** (`L4-ERASE`). The one-atom boundary — which forgets the whole
carrier, and which is itself an off-fixture record — sits at or below the
legitimate patch's value for C1 ($0$ against $45/4$), C4a ($0$ against $0$) and
C4c ($-20$ against $-12$). This is a measured property of these three, not a
property of separators: §8.3's D4 separates and puts total erasure *above* the
legitimate chart, and so does C5.

### 4.5 Per-candidate verdicts, both readings of G1

| candidate | pinned corridor | killing gate | strict corridor | amnesty sweep |
|---|---|---|---|---|
| **C1** | **SEPARATOR** (COUNTER-LAW) | none | KILLED | survives (0 inversions) |
| C2 | killed | G1 refinement-monotone | killed | not reached |
| C3 | killed | G3 nondegeneracy | killed | not reached |
| C4a | killed | G2b non-resolution | killed | **FINGERPRINT-AMNESTY**, 1552/4845 |
| C4b | killed | G2a function of ε | killed | not reached |
| C4c | killed | G2b non-resolution | killed | survives (0 inversions) |

Under the pin's literal G1 there is one survivor. Under the strict reading —
non-monotone in either direction — there are none: C1 and C4c are both measured
anti-monotone, and **an anti-monotone separator's verdict on the chain is
entailed by the order alone** (Theorem 6.1), which is what the terminal cycle
already ruled out on the anti-monotone case it measured (`A17`, `A18`, `A19`).
The strict reading is this unit's, on those warrants; it is not a paraphrase of
`A16`'s wording, and §2.1 says so.

Two limits on what that buys, both measured. The strict reading **empties the
declared six and nothing more** — it adds exactly the two anti-monotone
candidates to a list the other gates had nearly filled, since C4a is already
type "neither" and dies at G2b under either reading. And it does not empty the
*class*: §8.3's D3 is non-monotone under both readings, passes every other
pinned gate at the counter-law, separates, and no state inverts it. What
excludes D3 is name-blindness, not G1.

---

## 5. The amnesty sweep `[SIMPLEX]`

Every separator was swept over the whole declared state simplex at denominator
16, all 4845 states, exhaustively (`L4-AM-SUM` gates that each state is
classified exactly once).

| candidate | law | separating | tied | **inverting** | verdict |
|---|---|---|---|---|---|
| C1 | FUNNEL | 4844 | 1 | **0** | survives |
| C1 | COUNTER-LAW | 4692 | 153 | **0** | survives |
| C4a | each of the five | 1276 | 2017 | **1552** | `FINGERPRINT-AMNESTY` |
| C4c | each of the five | 4845 | 0 | **0** | survives |

**C4a is killed.** It separates at the committed state and inverts at 1552
states of 4845 — at those declared states the legitimate coarse chart carries a
*strictly larger* defect than a forgery, and a threshold reading the fingerprint
certifies the forgery and rejects the honest chart. This is the stage-5 pattern
exactly: an adversary who declares the state of his own patch turns the
fingerprint around. The mechanism is visible in the definition: C4a reads the
state through its level partition, and the level partition moves with the
state.

**Why the sweep cannot kill the other two — and what that reason is not.** C4c
is **state-blind**: its value does not depend on ρ at all, so its monotonicity
type is state-free by construction and no state can move its verdict on the
chain. C1 is **state-dependent** — ω reads ρ — so its immunity is not
structural but measured, and it is measured exhaustively here (`L4-C1STATE`):
C1 is linear in the declared state, $\Omega=\sum_j\rho_j c_j$ with $c_j$ the
number of admitted preparations at which $j$'s declared atom is unreached — two
routes, 0 disagreements at the committed state — and over the 224 comparable
off-fixture pairs it is anti-monotone at **4845 of 4845** declared states, at
FUNNEL and at the counter-law alike. That is why no state inverts it.

The general claim that would have covered both is false, and §8.3 exhibits the
counterexample: **D5**, a statistic of the declared boundary and the declared
mass, is measured *anti-monotone* at the committed state and the sweep inverts
it at **4350 of 4845** states (its name-blind completion D5N: **2380**). A
state-dependent statistic's monotonicity type is a measurement at the state it
is measured at, and nothing more. "A monotone separator passes the amnesty gate
by construction" is true of **state-blind** monotone separators only.

One more limit, in the sweep's disfavour: *surviving the sweep* is not
*separating at every state*. C1 ties at 153 of 4845 states at the counter-law
and at 1 at FUNNEL; C4a ties at 2017. And the sweep's power is not
co-extensive with state-reading: §8.3's **D2** reads the declared state — its
value *is* a declared mass — and the sweep never inverts it (4692 / 153 / 0).
The sweep catches state-reading **that inverts on the chain**, which is
strictly narrower.

---

## 6. Why each separation happens

### Theorem 6.1 — order-entailed separation `[FIN]`, `[OFF]`, `[FIX]`

Let $f$ be monotone in the refinement order in either direction **on the
comparable pairs of the whole 52-record lattice**. The committed triple is a
chain,
$$\{01\mid2\mid3\mid4\}\ \prec\ \{01\mid23\mid4\}\ \prec\ \{0123\mid4\},$$
(anchor `A13`) whose coarsest member is the legitimate patch. Then $f$'s
verdict on the triple is entailed by the order alone: if $f$ is anti-monotone
it separates, and if monotone it inverts, and in neither case does the verdict
depend on anything about the declarations other than their position in the
refinement lattice. $\square$

**The hypothesis, and where it is measured `[FIX]`.** The corridor measures
monotonicity type on the **224** comparable pairs of the off-fixture
population, and the committed triple is not in that population: off-fixture
monotonicity does not *entail* monotonicity across the fixture chain. The
extension is therefore **measured, not derived**, and it is measured here
(`L4-EXT`): over the **306** comparable pairs of the whole 52-record lattice
the monotonicity type is the same as the off-fixture type in **all nine**
(candidate, law) cases — C1, C4a and C4c at DET, FUNNEL and the counter-law.
No number moves; the inference does.

Measured per separating row (`L4-ENT`): C1 at FUNNEL and at the counter-law,
and C4c at all five laws, are order-entailed; **C4a is the only declared
separation that is not**. And C4a is the only declared separator the amnesty
sweep kills.

### 6.2 What an order-entailed separation is worth

Three consequences, all measured rather than argued.

**It ranks by forgetting.** The tolerance that admits the legitimate chart and
rejects both forgeries also admits the one-atom boundary, at every
order-entailed separator found here (§4.4). A statistic that prefers total
erasure to the legitimate chart is grading how much is forgotten.

**It is indifferent to provenance by construction.** Any declaration occupying
the same position on the chain receives the same verdict. The terminal cycle's
collision is the committed witness that position does not determine provenance:
the adversary's manufactured $1{+}1{+}1{+}1$ context and the legitimate address
context present *identical* declared data (anchor `A14`), hence identical
position, hence identical verdict from every statistic whatsoever.

**It survives the amnesty gate when it is state-blind**, per §5 — and only
then.

### 6.3 Does the law remember? What C1 actually reads

C1 is the only candidate that is not law-blind on the committed pairs: its
value triples differ across the five committed laws (3 distinct triples;
C4b has 2; C2, C3, C4a and C4c have 1 each — they are law-blind). So the
answer to the pin's question is *yes, the law supplies content* — ω is
identically zero under DET, REV and the funnel closure and non-zero under
FUNNEL and the counter-law, and that difference is the law's own reachability
structure speaking. But the content it supplies is **coarseness**: what the
law's counterfactual reachability grades is how much of the carrier a boundary
hides, and it grades total erasure best. The terminal cycle said so first, on
these very rows (§9, ground 1).

---

## 7. The covariance analysis, and the fused class theorem

### Theorem 7.1 — the admitted isomorphisms, measured `[FIN]`

The admitted isomorphisms of the declared data are the carrier relabellings
fixing the declared state, the declared law setwise, and the declared
preparation. Measured, never assumed:

| law | admitted isomorphisms | orbits of the 52 records | orbit invariant = shape profile |
|---|---|---|---|
| DET | **24** | **12** | yes |
| FUNNEL | 24 | 12 | yes |
| REV | 24 | 12 | yes |
| FUNNEL-CLOSURE | 24 | 12 | yes |
| COUNTER-LAW | **1** | 52 | — (vacuous) |

At the four symmetric laws the group is exactly $\operatorname{Sym}\{0,1,2,3\}$
— the relabellings fixing the retained sink, which the committed state marks
out by carrying $3/4$ there against $1/16$ elsewhere. Two records lie in the
same orbit **if and only if** they carry the same *shape profile*, the multiset
over declared atoms of the atom's profile across the state's level classes;
checked over all $52\times52$ pairs (`L4-COV1`). The orbit sizes are
$[1,1,1,3,4,4,4,4,6,6,6,12]$, summing to 52.

### 7.2 Shape is not resolution, and the gap is the state's `[FIN]`

The covariant invariant is **not** the boundary's resolution data, and the
difference is measurable (`L4-DEC`):

| invariant | classes on the 52 records |
|---|---|
| ε, the Bayes error, $=(5-\lvert\pi\rvert)/16$ at all 52 | **5** |
| the block-size profile — *resolution data*, which is what the pin's gate 2 names | **7** |
| the shape profile — the orbit invariant of Theorem 7.1 | **12** |
| the full record lattice | 52 |

Exactly **5** of the 7 resolution profiles split into two orbits apiece
($12 = 7 + 5$): the profiles $\{1,1,1,2\}$, $\{1,1,3\}$, $\{1,2,2\}$,
$\{1,4\}$ and $\{2,3\}$. The datum that separates each pair is $s$, **how many
configurations the boundary merges with the one the declared state marks out**
— the retained sink, at the committed state. Two boundaries can hide exactly as
much of the carrier and still differ covariantly.

Measured over all $52\times52$ pairs, two records share an orbit **if and only
if** they share the pair (resolution profile, $s$). A second route with no
group action agrees: an orbit is determined by $s$ together with the multiset
of the remaining block sizes, giving
$\sum_{s=0}^{4}p(4-s)=5+3+2+1+1=12$.

**The control that settles it `[FIN]`.** At the **uniform** state the declared
data marks no configuration out; the admitted-isomorphism group is the whole
**120**; the 52 records fall into exactly **7** orbits; and the orbit invariant
*is* the block-size profile exactly, measured over all $52\times52$ pairs
(`L4-UNIF`). The 12-vs-7 gap is produced entirely by the committed state's
$4{+}1$ level structure. A sentence identifying the covariant invariant with
resolution data is the uniform-state theorem carried where it fails.

### Theorem 7.3 — the fused class theorem `[FIN]`

*Let the declared data be a **full-group configuration**: the
five-configuration carrier, a declared state whose admitted-isomorphism group
is the full 24, the declared preparation the whole carrier, and any committed
law invariant under relabelling. Let $f$ be any statistic of the declared patch
data — including one evaluated at counterfactual admitted preparations or at
generation profiles, since those are themselves functions of the patch. Then*

1. ***(factorization)*** *$f$ covariant $\iff$ $f$ is a function of the pair
   $\bigl(R(\pi),\,s(\pi)\bigr)$, where $R$ is the boundary's resolution
   profile and $s$ its marked-block excess — a 12-class quotient of a
   52-element lattice;*
2. ***(the two arms)*** *hence every covariant statistic is
   **resolution-reading** — a function of $R$ alone — or **state-reading** —
   its value moves with $s$, a datum defined only through the declared state's
   level partition;*
3. ***(no non-resolution covariant statistic)*** *in particular no covariant
   $f$ satisfies corridor gate G2b: covariance and non-resolution in the strong
   sense are jointly unsatisfiable.*

**Proof.** By covariance $f$ is constant on the orbits of the
admitted-isomorphism group acting on declared boundaries; by Theorem 7.1 the
orbit invariant is the shape profile; by §7.2 the shape profile is exactly the
pair $(R,s)$, measured over all $52\times52$ pairs and re-derived without the
group action. Conversely any function of $(R,s)$ is manifestly covariant, both
components being relabelling-invariant. (2) is the definition of the two arms
applied to (1), and (3) is (1) restated, G2b being "not a function of the
covariant shape invariant". $\square$

**Corollary 7.4.** Every covariant statistic assigns identical verdicts to
declarations of identical $(R,s)$ and opposite provenance — witnessed by the
committed collision (`A14`). No covariant statistic reads provenance.

**What each arm meets.** The first arm dies at the pin's own gate 2, in the
pin's own words: a function of the resolution profile *is* a statistic
factoring through partition-size data, and grading resolution is what the
terminal cycle ruled out. The second arm is the arm the amnesty sweep exists
for — it reads a datum of the declared state, and the state is the adversary's
own declaration. Neither arm is *automatically* fatal, and §8 measures which of
them actually bites where; what the theorem delivers is that there is no third
place for a covariant statistic to live.

**Measured on the declared family** (`L4-COV4`): no declared candidate is both
covariant and a non-function of the shape invariant at DET. The intersection is
empty, exactly as the theorem requires.

### 7.5 The covariance analysis the pin demanded

*Are the committed pairs' declared data related by any admitted isomorphism?*
**No.** The three coarse boundaries lie in three distinct orbits, of sizes 6, 3
and 1, and no admitted isomorphism carries one to another — their block-size
profiles differ, so no relabelling whatever could (`L4-COV2`). Covariance does
**not** force them equal and does **not** block separation. Their shapes are

$$\{0123\mid4\}\mapsto\{(4,0),(0,1)\},\qquad
\{01\mid23\mid4\}\mapsto\{(2,0),(2,0),(0,1)\},\qquad
\{01\mid2\mid3\mid4\}\mapsto\{(2,0),(1,0),(1,0),(0,1)\}.$$

*Then what do the resisting invariants share?* The committed configuration's
own symmetry collapses the 52 declared boundaries to 12 distinguishable ones,
and the surviving invariant is the block-size profile **refined by the declared
state's level classes** — 12 classes against the 7 of the block-size profile
alone. All three committed boundaries carry the retained sink in a singleton
block, so $s=0$ at each of them and the extra covariant datum is constant on
the triple: *the only thing that distinguishes the three committed boundaries
covariantly is how much they resolve.* That is true of the triple, and it is
false of the quotient — off the triple, two boundaries of identical resolution
differ covariantly, and §8.3's C5 is built on exactly that gap.

### 7.6 Where the theorem is silent, and the principle that speaks there

Theorem 7.3 needs the full group. Two measurements bound what that costs.

**The scope, counted `[SIMPLEX]`.** Over the 4845 declared states of
denominator 16, the admitted-isomorphism group at a relabelling-invariant law
is the automorphism group of the state's level partition. Its sizes distribute

| group size | 1 | 2 | 4 | 6 | 12 | 24 |
|---|---|---|---|---|---|---|
| declared states | **1200** | 2640 | 510 | 440 | 30 | **25** |

so **25 of 4845** states carry the theorem's hypothesis and **1200** carry a
trivial group, where it has no content whatever; none carries the whole 120,
the uniform state not being of denominator 16 (`L4-SCOPE`). The emptiness the
theorem delivers is purchased by the committed state's degeneracy, not by the
carrier or the law.

**At the counter-law the quotient is trivial, and that is not an accident.**
There the group is 1, the quotient is the whole 52-record lattice, and
covariance imposes nothing — which is precisely why C1 passes G2b there and
nowhere else. The reason the counter-law is the asymmetric one is worth one
sentence and one measurement: it is the composition closure of the
block-minimum idempotents of all 52 partitions — maps defined by "send every
configuration to the *least* member of its block" — and "least" reads the
carrier's label order, so the family is not stable under relabelling. Measured:
DET, FUNNEL, REV and the funnel closure each have **1** distinct image under
the 120 relabellings — they are Sym(5)-invariant as families — and the
counter-law has **120**, its law-stabilizer being trivial. **The one law whose
gate 5 is vacuous is the one law defined by a label-reading rule**, and that is
exactly the door through which label-reading statistics walk.

**The name-blindness principle, imposed.** Covariance is invariance under the
*stabilizer* of the declared data; where the stabilizer is trivial it says
nothing. Name-blindness is invariance under *all* 120 relabellings acting on
the whole declared datum at once — boundary, law, preparation and state moved
together — equivalently: the statistic is a function of label-free invariants
of the declared structure. It has content at every configuration, including the
1200 with no symmetry.

It is imposed here as a **principle, and it is declared as one**: it is the
corpus's own co-reference discipline, under which names are never compared —
the discipline that carries the stage-5 and W6 antecedent that a statistic may
read what a declaration *says* and never which carrier element it says it
about. It is not a theorem of the committed structure, and this unit does not
present it as one. What is measured is its consequence, which is §8's result,
and what is measured about the gate itself is that it is **load-bearing**:
without it the class at an asymmetric law is not empty, and with it, it is.

---

## 8. The fused claim, measured against the constructions built to break it

### 8.1 What the declared family alone shows — a tally, not a theorem

Over the separating rows of the six declared candidates, every separation is
either order-entailed or inverted by the amnesty sweep, and none is both
(`L4-DICH`). C1 and C4c are of the first kind; C4a is of the second.

That is an **observation over twelve rows of one declared family**, and it is
not a property of statistics on declared data. Both arms fail as general
claims, and §8.3 exhibits the failures: **D1** is in *neither* arm — state-blind
and non-monotone, so no order entails it and no state inverts it — and **D5N**
is in *both*, anti-monotone at the committed state and inverted by the sweep at
2380 of 4845 states anyway. What carries the verdict is not this tally but
§8.5.

### 8.2 The hole in the pinned corridor, reported

The pin's gate 1 excludes refinement-**monotone** candidates because the
forgeries sit below the legitimate chart. It does not exclude the anti-monotone
ones, and the anti-monotone half of the same class separates *for the same
order-theoretic reason, with the sign flipped*. C1 and C4c walk through that
hole. The terminal cycle had already stated the constraint that closes it and
this unit anchors it verbatim rather than paraphrasing it, computes both
readings, reports both verdicts, and declares the strict reading as its own
(§2.1). The gate is not silently patched: §4.5 gives the pinned reading first.

There is a second hole, in the other direction, at gate 2. The delivered G2b is
strictly stronger than the pin's wording: the pin excludes statistics factoring
through *resolution* data (7 classes), and G2b excludes statistics factoring
through the *shape* invariant (12 classes). Under the pin's literal wording the
corridor at a full-group configuration is **not** empty — C4a inhabits it, and
so do §8.3's C5 and D2 — and the emptiness result depends on the strengthening.
Theorem 7.3 is stated so that this is visible: the covariant class is exactly
the functions of $(R,s)$, and which of those the pin's gate 2 excludes depends
on how one reads it. Both readings are reported, per candidate and per law.

### 8.3 The panel class `[PANEL]`

A corridor that only ever meets its author's constructions has not been tested.
Nine further statistics are carried here, each built against this unit by the
three-lens review panel with the fixture already known. They are **not**
candidates of the hunt and are declared as post-fixture, hashed like the six
(`L4-00B`), and gated by the same battery. Credit is inline: R1 is the operator
lens, R2 the effectus/order lens, R3 the instrument lens.

| | definition | separates (2+1+1, 2+2 \| **legit**) | erasure | type | G2b | name-blind | sweep (sep, tie, **INV**) |
|---|---|---|---|---|---|---|---|
| **C5** (R1) | $\mathrm{PR}(\pi)+1000\,s$ | $-2,\ -4\mid\mathbf{-12}$ | $3980$, **refuses** | neither | ✗ | ✓ | 870, 0, **3975** |
| **D1** (R2) | $\#\{r:\lvert r\rvert=2\}$ | $1,\ 2\mid\mathbf 0$ | $0$, certifies | neither | ✗ | ✓ | 4845, 0, **0** |
| **D2** (R2) | $\sum_{\lvert r\rvert=2}\Pr(r)$ | $1/8,\ 1/4\mid\mathbf 0$ | $0$, certifies | neither | ✗ | ✓ | 4692, 153, **0** |
| **D3** (R2) | $2\#\{\lvert r\rvert=2\}+\mathbb 1[\{0,1\}\in\pi]$ | $3,\ 5\mid\mathbf 0$ | $0$, certifies | neither | ✓ | **✗** | 4845, 0, **0** |
| **D4** (R2) | $\#\{r:\lvert r\rvert\notin\{1,4\}\}$ | $1,\ 2\mid\mathbf 0$ | $1$, **refuses** | neither | ✗ | ✓ | 4845, 0, **0** |
| **D5** (R2) | $(\tfrac12-\rho_4)\sum_r\lvert r\rvert(\lvert r\rvert-1)$ | $-1/2,\ -1\mid\mathbf{-3}$ | $-5$, certifies | **anti-mono** | ✗ | **✗** | 330, 165, **4350** |
| **D5N** | D5 with the marked mass for $\rho_4$ | $-1/2,\ -1\mid\mathbf{-3}$ | $-5$, certifies | **anti-mono** | ✗ | ✓ | 1650, 815, **2380** |
| **D** (R3) | $\mathrm{C4a}+\mathrm{C1}/1000$ | at the counter-law: $419/1600,\ 2093/8000\mid\mathbf{9/800}$ | $0$, certifies | neither | ✓ (counter-law) | ✓ | 3183, 110, **1552** |
| **TUNE** (R3) | $\mathrm D-1000\cdot\mathbb 1[\pi=\text{the legitimate boundary}]$ | $419/1600,\ 2093/8000\mid\mathbf{-799991/800}$ | $0$, **refuses** | neither | ✓ (counter-law) | **✗** | 4845, 0, **0** |

Four of these are the sharp ones, and each dies at a named gate (`L4-CORPSE`).

**C5 (R1) dies at the amnesty sweep.** It is the pair-resolution index
penalised by the marked block's excess — built to clear the pinned corridor,
the strict G1 *and* the erasure column at once, and it does: it is name-blind,
covariant, type "neither", it separates $-12$ against $-2$ and $-4$, and its
one-atom value $3980$ sits three thousand nine hundred and ninety-two above the
legitimate chart, so its tolerance **rejects total forgetting** — the only
construction here that does. It dies because the datum it adds is the declared
*state's*: relabel the state and the marked configuration moves, and at
$\rho'=(3/4,1/16,1/16,1/16,1/16)$ its values become 998, 996 and **2988** and
the separation inverts. Swept: **3975 of 4845** states invert. C5 is arm two of
Theorem 7.3 with an inhabitant, and arm two is what the sweep is for.

**D3 (R2) dies at name-blindness.** It passes the entire pinned corridor at the
counter-law under **both** readings of gate 1, separates 0 against 3 and 5,
takes different values on two boundaries of the *same* shape
($\{01\mid2\mid3\mid4\}\mapsto3$ against $\{02\mid1\mid3\mid4\}\mapsto2$), and
the sweep never touches it: 4845 / 0 / 0. Neither Theorem 6.1 nor Theorem 7.3
has any grip on it. It compares carrier names — the witness is the record
$\{01\mid2\mid34\}$ under the relabelling exchanging 1 and 2 — and the
name-blindness gate takes it at every committed law.

**D5 (R2) refutes the immunity mechanism.** It is covariant, it is a statistic
of the declared boundary and the declared mass, and the corridor's own
procedure — which measures monotonicity type at the committed state — labels it
**anti-monotone**, hence order-entailed, hence, on the mechanism §5 used to
assert, immune to the sweep by construction. It is not: the sweep inverts it at
**4350 of 4845** states (its name-blind completion at 2380). As written it
reads a configuration by name and the gate takes it; the structural point
survives the completion, and §5 is corrected accordingly.

**TUNE (R3) is the permanent negative control of the freeze audit.** It is D
with one explicit read of the committed legitimate boundary. It separates; it
is type "neither", so no order entails it; no state inverts it (4845 / 0 / 0);
and at the counter-law it passes every pinned and strict corridor gate. The
delivered machinery would have registered it as a fingerprint, and the
freeze barrier would have been green while it did — which is precisely why the
barrier is not what carries the freeze. It is caught twice, and by the two
checks that work: the **source scan** finds `PTOMO` in its definition (§3), and
the **name-blindness gate** finds that relabelling 3 and 4 moves its value at
the record $\{0124\mid3\}$ — whose image under that relabelling is the
legitimate boundary itself. The tuning gap covariance leaves open is exactly
the one name-blindness closes.

Two further readings, both in the panel's favour and both measured. **D4**
separates and puts total erasure *above* the legitimate chart, which is why
§4.4's erasure finding is a measured property of three declared separators and
not a law about separators. **D2** reads the declared state and the sweep never
inverts it, which is why §5's claim about the sweep's reach is corrected. And
**D** — a sum of two declared candidates, with no fixture reference whatever —
passes the full pinned *and* strict corridor at the counter-law, where the
declared family does not: the declared family is not closed under sums, which
bounds what "the declared family contains none" buys. It dies at the sweep,
1552 inversions inherited from C4a.

### 8.4 What the name-blindness gate is doing

Measured, across all 15 statistics × 5 committed laws: the gate rejects D3, D5
and TUNE at every committed law, each with an explicit relabelling witness, and
rejects nothing else except C3 at the counter-law, where — as §4.2 records — it
is measuring the declared population's non-closure and not the statistic. It is
not vacuous, and it is not doing hidden work: it removes exactly the statistics
that compare names.

### 8.5 The fused claim, measured `[PANEL]`, `[FIX]`, `[SIMPLEX]`

> **Over the name-blind class at every committed configuration — the six
> declared candidates and the nine panel constructions, at all five committed
> laws — every separating row falls to at least one of three arms:
> *shape-functional*, a function of the covariant shape invariant $(R,s)$,
> which is what gate G2b excludes and what Theorem 7.3 forces on every
> covariant statistic; *order-entailed*, its verdict fixed by the committed
> chain's order; or *swept*, inverted by the amnesty sweep at an admitted
> declared state. Of the 42 name-blind separating rows, none is unaccounted.
> There is no ROBUST name-blind separator — robust meaning: separating, and
> accounted for by no arm — at any committed law.**

The first arm is where Theorem 7.3's own two arms live: a shape-functional
statistic is *resolution-reading* if it is a function of $R$ alone — D1 and D4
are, and they are then exactly what the pin's gate 2 names — and
*state-reading* if it moves with $s$, which is where the sweep gets its grip
(C4a, C5) and where it sometimes does not (D2, which reads the state and never
inverts, and dies at G2b instead). The arms overlap by design; what matters is
that nothing name-blind falls outside all three.

`L4-FUSED`, and it is a measurement over a declared class, not a theorem about
all statistics. What makes it more than a tally is Theorem 7.3, which proves
the first arm inescapable for *every* covariant statistic at a full-group
configuration; and what makes it survive the asymmetric case is the
name-blindness principle, which is a convention of legitimacy and is declared
as one.

The gate's other half is the load-bearing check. **Six separating rows are
unaccounted by every arm — and all six are label-reading**: D3 at all five
committed laws, and TUNE at the counter-law. They separate, no order entails
them, and no declared state inverts them. Nothing in the committed structure
excludes them. Only the principle does.

---

## 9. Verdict

$$
\boxed{
\begin{array}{l}
\texttt{RQ0-L4-CLASS-IMPOSSIBILITY}\ \text{(fused: factorization + the two arms)}\\
\texttt{RQ0-L4-FINGERPRINT-AMNESTY}\\
\texttt{RQ0-L4-BLOCKED-AT-THE-REFINEMENT-ORDER}\\
\texttt{RQ0-L4-NOMOLOGICAL-FINGERPRINT}\ \textbf{— WITHDRAWN}
\end{array}}
$$

- **`RQ0-L4-CLASS-IMPOSSIBILITY` — occurs, in its fused form.** At a
  full-group configuration every admitted-isomorphism-covariant statistic of
  the declared patch data is a function of the pair (resolution profile,
  marked-block excess) and of nothing else — 12 classes of a 52-record lattice,
  proved and measured over all $52\times52$ pairs by two independent routes
  (Theorem 7.3). Hence every covariant statistic is resolution-reading or
  state-reading; the first arm is what the pin's gate 2 excludes and the second
  is what the amnesty sweep sweeps; and no covariant statistic is
  non-resolution in the strong sense. Measured over the name-blind class at
  **every** committed configuration, symmetric or not, and over 15 statistics
  rather than 6: 42 name-blind separating rows, **none unaccounted** (§8.5).
  *Scope:* the full-group hypothesis is carried by 25 of the 4845 declared
  states, 1200 carrying no symmetry at all; the measured half is over the
  declared six plus the nine panel constructions and is not claimed of
  statistics outside them; the name-blindness gate is a principle and is
  declared as one.

- **`RQ0-L4-FINGERPRINT-AMNESTY` — occurs, at C4a, and it is a kill.** The
  state-symmetry alignment defect separates the committed pairs at every
  committed law, $0$ against $1/4$ and $1/4$, and inverts at **1552 of 4845**
  declared states, ties at 2017 and separates at 1276. It is the only declared
  separation not entailed by the refinement order, and it is the only declared
  one the sweep can touch. Among the panel class the sweep also kills C5 (3975
  inversions), D5N (2380) and D (1552). C4a also fails G2b, being a function of
  the shape at the committed configuration, and splits the identity-free side;
  the amnesty is reported as the independent kill it is.

- **`RQ0-L4-NOMOLOGICAL-FINGERPRINT` — WITHDRAWN.** The pin's letter is met by
  C1 at the counter-law: it passes every pinned corridor gate there, separates
  ($45/4$ against $93/8$ and $95/8$) and survives the amnesty sweep with zero
  inversions. The letter is not the finding, and registering the tag would
  register an already-adjudicated negative as a positive. Four grounds, **in
  the order of their force** (`L4-C1-DQ`):

  1. **Order-entailment — independently sufficient.** C1 is measured
     anti-monotone off-fixture and, by the measured extension of §6, over the
     whole 52-record lattice; the committed triple is a chain whose coarsest
     member is the legitimate patch; so its verdict is the order's and reads no
     provenance. And the content is not new: C1 is the sum of ω over the same
     31 preparations whose rows the terminal cycle already counted — **15 of
     155**, 12 at FUNNEL and 3 at the counter-law, 6 admitting the legitimate
     patch at tolerance zero, recomputed here at `A19` — and already dismissed,
     as *"a grading of coarseness with the sign flipped rather than a reading
     of provenance"* (`A17`, verbatim). **C1's separating content is the
     immutable base's own pre-adjudicated ω grading, aggregated over
     preparations.**
  2. **Total erasure — independently sufficient.** The tolerance that admits
     the legitimate chart and rejects both forgeries also admits the one-atom
     boundary, which forgets the entire carrier.
  3. **The identity-free side.** C1 takes 3 distinct values on the 745 patches
     where ε takes one, splitting the side on which proper charts live.
  4. **The vacuous covariance gate — no information.** The counter-law's
     admitted-isomorphism group is trivial, so gate 5 there is satisfied
     vacuously. This is *not* a ground: nothing about C1 was tested by it, and
     a symmetry-free configuration is an admissible configuration. The correct
     accounting is that **C1 was screened by four gates, not five**. Grounds 1
     and 2 are what close the case, and they close it at every law.

  *Coarse reality does not self-certify here.*

- **`RQ0-L4-BLOCKED-AT-THE-REFINEMENT-ORDER` — occurs, census discipline.**
  The terminal cycle located the block at the declaration's provenance. This
  unit locates the *next* one, one step in: every separator **the name-blind
  class offers** is a function of the boundary's position in the refinement
  order — the shape profile at the full-group configurations, by Theorem 7.3,
  and the chain's own order or the state's own level data elsewhere. The block
  is not at the carrier, not at the law, and not only at provenance: at this
  scope, **the only structure the committed pairs differ by that any admitted
  name-blind statistic can see is how much they resolve.** Stated exactly: this
  is a claim about the class gated here, and at an asymmetric law it is
  underwritten by the name-blindness principle rather than by a theorem —
  drop the principle and D3 walks in.

**What is nevertheless not earned.** No fingerprint. No statistic that reads a
declaration's history. No impossibility wider than the class stated: Theorem
7.3 needs the configuration's symmetry, Theorem 6.1 needs the candidate's
monotonicity, and at an asymmetric law a non-monotone, non-covariant,
*name-blind* statistic is excluded by neither theorem — it is excluded by
measurement over the class gated here, and by nothing stronger. And the
name-blindness gate is a principle: it is warranted by the corpus's own
co-reference discipline, not derived from the committed structure.

**The ontological rider, recorded.** At symmetric configurations the law
provably does not remember: the covariant quotient has 12 classes and
provenance is in none of them. At asymmetric ones — 1200 of the 4845 declared
states — only the name-blindness principle stands between certification and
label-smuggling. That is a convention of legitimacy doing work a theorem does
not do, and it is the honest shape of the negative.

**The next obstruction, named.** Both routes the terminal cycle listed remain
the routes. This unit closes the "find a datum that distinguishes a
declaration's history" route at the level of *statistics on the patch*: at a
full-group configuration there is no such datum in the covariant quotient, and
elsewhere everything the name-blind class offers is the refinement order or the
declared state. What is not closed, and is now the sharper target, is the
**atlas** route — a cross-patch condition, which the inherited
comparable-boundaries theorem already shows is the shape such a condition would
take, and which no gate here touches.

---

## 10. Non-claims

- **No claim that a nomological fingerprint was found.** The tag is withdrawn
  (§9), not qualified: the one pin's-letter survivor is order-entailed and
  total-erasure-certifying, each independently disqualifying, and its content
  is the base's own already-dismissed ω rows.
- **No claim that the class impossibility covers all statistics.** It covers
  covariant statistics at a configuration whose admitted-isomorphism group is
  the full 24 — 25 of the 4845 declared states — and, as a measurement, the 15
  statistics gated here at all five committed laws. Nothing wider (§7.6).
- **No claim that the gate order proves the freeze.** It does not, and it never
  could. The freeze is carried by the definition hashes and the source scan;
  the orbit-forced evaluations at committed boundaries are counted and
  disclosed, 10,028 of them (§3).
- **No claim that name-blindness is a theorem.** It is a principle — the
  corpus's co-reference discipline imposed as a corridor gate — and it is
  load-bearing: six separating rows escape every arm without it (§8.5).
- **No claim that covariance forces the committed pairs equal.** It does not:
  they lie in three distinct orbits and no admitted isomorphism relates them
  (§7.5).
- **No claim that the covariant invariant is resolution data.** It is
  resolution data refined by the declared state's level classes: 12 classes
  against 7, with 5 resolution profiles splitting (§7.2). The identification
  holds exactly at the uniform state and at the committed triple, and nowhere
  else.
- **No claim that the amnesty sweep is a general filter.** It cannot kill a
  *state-blind* order-entailed separator; its power is co-extensive with
  state-reading **that inverts on the chain**, which is strictly narrower than
  state-reading — D2 reads the state and never inverts (§5).
- **No claim that monotonicity is a state-free property.** It is, for
  state-blind statistics; for state-dependent ones it is a measurement, and D5
  is anti-monotone at the committed state and inverted by the sweep at 4350 of
  4845 (§5, §8.3).
- **No claim that every separator certifies total erasure.** Measured of the
  three declared separators; false of separators — D4 and C5 refuse it (§4.4,
  §8.3).
- **No claim that C1 reads provenance.** It reads the law — it is the only
  candidate that is not law-blind on the committed pairs — and what the law
  supplies it is coarseness (§6.3).
- **No claim that C2, C3 or C4b failed for want of tuning.** C2 is proved and
  measured to *be* ε at every identity-containing law; C3's fan-in is
  identically 1 at the committed carrier over 2847 laws; C4b is a function of
  ε off-fixture. Each is a structural collapse with a stated cause.
- **No claim about same-quadruple pairs.** Excluded by the terminal proof and
  not reopened.
- **No claim that the pinned corridor is correct as pinned.** Its gate 1 has a
  measured hole — the anti-monotone half of the refinement-order class — and
  its gate 2, read literally, admits statistics G2b excludes; both are reported
  rather than patched (§8.2).
- **No claim that the identity-free side survives any candidate but C2.** Five
  of six split it, and the counts ship (§4.3).
- **No claim that the declared law population at five configurations is a
  census.** It is 2847 declared laws, named as a population — and it is not
  closed under relabelling, which is what C3's one name-blindness failure
  measures (§4.2).
- No spatial, causal, temporal or spacetime reading of reachability, of
  "coarse", of "patch", of "shape" or of "atlas". No locality, topology,
  manifold, field, QFT, QCD or gravity object is constructed, used or claimed.
- No Lean, as pinned.

---

## 11. Reproduction

`v13/code/rq0_l4_fingerprint_exact.py` regenerates every number in this paper.
Exact arithmetic throughout; substantive negatives exit 0; anchor mismatches
exit 1. **19 anchors and 57 gates, of which 25 are must-pass** (8 derivation,
8 covariance and decomposition, 3 freeze, 2 amnesty, 2 panel, 2
verdict-level), with 0 must-pass failures. Fifteen gates fail, every one of
them a substantive negative that exits 0: the corridor and fixture kills, the
five C4a amnesty rows, the erasure column, and the name-blindness gate's one
measured failure (§4.2). `--falsification-selftest` runs every mutant — 10
over anchors and 20 over derivation and gating steps, **30** in all — each of
which must exit 1. No wall-clock value enters the receipt or the rendered output, so
two runs of the same source produce byte-identical artifacts.

Outputs: `rq0_l4_fingerprint_output.txt`, `rq0_l4_fingerprint_receipt.json`.

**What the receipt proves, and what it does not.** It proves that this run
computed what it says it computed, in the order it says: the 19 anchors; the
candidate registration `L4-00` with per-definition SHA-256; the source scan
`L4-SRC` with its negative control; the corridor-membership gates, the
name-blindness gate and the identity-free-side gate, every one of them decided
on the 48 off-fixture records and the 745 census patches; the seven derivation
cross-checks; the disclosure `L4-IMG`; the barrier; and then the fixture,
amnesty, robustness, covariance, decomposition, scope, extension,
simplex-monotonicity, panel and fused gates. It does **not** prove authoring order — no in-script barrier can —
and it does not claim that no candidate was evaluated at a committed boundary,
because the orbit gates force 10,028 such evaluations and `L4-IMG` counts them.
The freeze rests on the definition hashes and on the source scan, both of which
a reader can check against the source text itself.

Anchors reproduce, by this unit's own routes, every terminal value it reuses:
the record-lattice sizes $1,2,5,15,52$; DET and REV at $3125$ and $120$; the
preservation families $240$, $420$, $1280$ and $120$; the committed defects
$1/16$, $1/8$, $3/16$; the ε spectrum and its Stirling multiplicities
$1,10,25,15,1$; the closed-form state map at the committed and
selective-amnesty states; ω's vanishing over all $1612$ DET instances; the
$4845$-state grid with ε's forward separating set empty on all of it; the
census counts $687$, $259$, $428$; the $745$ identity-free admissible patches
all at ε $=0$; the fan-in $874$/$175$/$428$; the cost tower
$120,360,1260,3120$; the refinement chain; the terminal collision's identical
declared data; the terminal §10 successor constraint, the §1 verdict on
anti-monotone gradings and the §9.1 row count, each quoted verbatim from the
immutable paper; the fifteen ω rows recomputed here, $15$ of $155$ with the
per-law split $0/12/0/0/3$ and $6$ at tolerance zero; and this unit's own Pres,
ε and ω routes gated equal to the terminal ones at all 52 records.

---

## Appendix A. Declared deviations from the frozen pin

Per the standing rule, every deviation ships with the delivery.

1. **Gate 2 is delivered in two readings, and both are gated.** The pin's gate
   2 is "not Bayes-error-like — must not factor through partition-size or
   resolution data". Delivered as **G2a** (not a function of ε on the
   off-fixture population) and **G2b** (not a function of the covariant shape
   invariant). The distinction is load-bearing, and it is a *strengthening*:
   the shape invariant has 12 classes against the resolution profile's 7
   (§7.2), so G2b excludes statistics the pin's wording admits — C4a, C5 and D2
   among them. Both columns are reported per candidate and per law, and §8.2
   states the strengthening as a hole in the other direction.

2. **Gate 1 is delivered in two readings, and the pin's is reported first.**
   The pin excludes refinement-monotone candidates. The strict reading —
   monotone in *either* direction — is **this unit's extension, not an
   inheritance**: `A16` is a string anchor certifying the terminal cycle's
   words, and those words are ambiguous, the corpus's own vocabulary setting
   *anti*-monotone in contrast with monotone. The extension is warranted
   instead by the base's own adjudication of the anti-monotone case on the very
   rows C1 reuses (`A17`, `A18`, `A19`) and by this unit's erasure measurement.
   §4.5 gives both verdicts, and the strict reading is shown to empty the
   declared six and nothing more.

3. **Orientation is fixed rather than free.** A defect is admitted when small,
   which is the corpus's own semantics (stage-5 Definition 2.2(b)). Without
   fixing it, ε itself would count as a "separator" of the committed triple in
   the reversed direction, which the terminal cycle's §5.3 explicitly warns
   against ("the failure … is one of ordering, not of resolution"). Candidates
   wanting the other direction declare the negated form; C4c does.

4. **The corridor is decided on an off-fixture population.** The pin requires
   corridor gates before fixture truth; it does not say on what data. The
   design is right and costs nothing — nondegeneracy, monotonicity type,
   factoring, covariance and name-blindness are properties of the statistic and
   not of the fixture. The rationale originally given for it was **false and is
   withdrawn**: deciding on the off-fixture population does *not* make the
   freeze checkable through recorded provenance flags, because the flag is an
   author-declared stage annotation and the orbit gates evaluate candidates at
   committed boundaries without setting it. What makes the freeze checkable is
   the definition hashes and the source scan (§3); what the off-fixture
   population buys is that the corridor's *decisions* are functions of
   non-fixture data, which is a real and separate virtue.

5. **Two diagnostics were added post-barrier, and they gate no kill but they
   carry the verdict.** The *order-entailment* test (`L4-ENT`) and the
   *total-erasure* column (`L4-ERASE`) are computed after the barrier from data
   already frozen. They eliminate no candidate — every kill in §4.5 is by a
   pinned gate — but `L4-ENT` is ground 1 of the withdrawal and `L4-ERASE` is
   ground 2, and both feed the arms of §8.5. Saying they "do not eliminate any
   candidate" would understate their role, so it is not said. (In the unit's
   favour: the one-atom boundary is itself an **off-fixture** record, so the
   erasure column is off-fixture data.)

6. **G6 is delivered as constancy on the identity-free side.** The pin asks
   that candidates not break the terminal ε $=0$ side. ε is *constant* there,
   so one tolerance retains all 745; the faithful analogue is that the
   candidate be constant too, and the number of distinct values is reported
   rather than a bare verdict. This is the strictest defensible reading and it
   is declared as such: five of six candidates fail it.

7. **C3's law population at five configurations is constructed, because the
   census is at three.** The terminal fan-in resource lives at three
   configurations. Transporting it required a declared population at five;
   2847 laws, every composition-closed law generated by one admitted
   single-valued operation together with the committed laws. It is named a
   declared population, never a census, and the negative it yields (fan-in
   identically 1) is reported with the census numbers that show the resource is
   real one arity down. **It is not closed under relabelling** — it adjoins the
   committed counter-law, a label-reading construction — which is why C3 fails
   the name-blindness gate there (§4.2); the failure measures the population,
   not the statistic.

8. **The counter-law's vacuous covariance is reported as a finding, not
   absorbed — and it is reported as carrying no information.** The pin's gate 5
   is stated as a requirement; at the counter-law it has no content, since the
   admitted-isomorphism group is trivial. `L4-COV0` measures the group at every
   committed law, and §4.2 and §9 state the consequence in its correct form:
   the single corridor survivor was screened by four gates, not five. The
   absence of symmetry is not itself disqualifying.

9. **A pre-registered outcome is WITHDRAWN rather than registered qualified.**
   `RQ0-L4-NOMOLOGICAL-FINGERPRINT`'s defining condition is literally met by C1
   at the counter-law. It is withdrawn because its separating content is the
   immutable base's own already-counted and already-dismissed ω rows (`A17`,
   `A18`, `A19`) and because it is disqualified on two independently sufficient
   grounds (§9). Registering a tag whose content the base has already
   adjudicated as a negative would import a false positive into the census. The
   pin's letter is recorded in the receipt — the pin's-letter survivor is named
   there — and the withdrawal is recorded with its reasoning; nothing is
   hidden, and the deviation is from the pin's *outcome menu*, not from its
   measurements.

10. **The census-discipline slot is instantiated as
    `RQ0-L4-BLOCKED-AT-THE-REFINEMENT-ORDER`.** The pin's slot is
    `RQ0-L4-BLOCKED-AT-⟨object⟩`; the object is measured to be the boundary's
    position in the refinement order — shape at the full-group configurations,
    chain order or the state's level datum elsewhere.

11. **Lean: none**, as pinned. **No new primitive**: every object is committed
    structure from Cycles B, B′, B″ and the stage-5 terminal unit, except the
    six declared candidates and the nine panel constructions, which are built
    from committed data only and are declared here rather than presented as
    inherited.

12. **The corridor's orbit gates evaluate candidates at committed boundaries,
    and the count is disclosed.** Covariance and name-blindness are orbit
    properties and cannot be tested without leaving the off-fixture set:
    **864** such evaluations in the covariance gate, **9,164** in the
    name-blindness gate, **10,028** in all, every one of them an equality test
    against the candidate's own off-fixture value, none retained, thresholded
    or used in any selection (`L4-IMG`). The barrier's earlier claim that no
    corridor gate evaluated any candidate at any committed boundary was false
    and is deleted, along with the claim that the gate order proves the freeze.

13. **A name-blindness gate is added to the pinned corridor.** The pin lists
    five gates and covariance is one of them; covariance has no content where
    the admitted-isomorphism group is trivial, which is 1200 of the 4845
    declared states and one of the five committed laws. The added gate is a
    principle, not a measurement of committed structure, and it is declared as
    one (§7.6). It is imposed because without it the corridor at an asymmetric
    law admits label-reading separators that no theorem here excludes, and it
    is reported as load-bearing rather than as free (§8.5).

14. **Nine statistics were declared and gated AFTER the fixture was known.**
    The panel class (§8.3) is not part of the pre-registration and could not
    be: each was constructed against this unit's delivered corridor. They are
    marked post-fixture in the receipt, hashed like the six, and used only to
    *attack* the unit's claims — never to support a positive. TUNE in
    particular is retained as the permanent negative control of the freeze
    audit.

15. **Two claims of the delivered analysis are corrected in place rather than
    footnoted.** The identification of the covariant invariant with resolution
    data (false off the committed triple: 12 classes against 7 — §7.2) and the
    fingerprint dichotomy as a theorem (a tally over twelve rows of one
    family; both arms have counterexamples — §8.1). Neither correction moves a
    computed number; both change what is claimed of them.
</content>
