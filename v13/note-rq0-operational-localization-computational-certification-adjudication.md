# RQ0-L0 computational-certification hostile adjudication

**Date:** 2026-07-31  
**Status:** FINAL FOR THIS AUTHORIZED CYCLE  
**Hostile report:** commit `da36baf`; SHA-256
`b6c6a7bd53ca28eac393009a784592f5af5a73d53b28d70f2ff1fd14e1b8e689`

## Verdict

The hostile verdict `REJECT` is accepted.  This successor cycle closes with

```text
procedural outcome = RQ0-L0-INVALID
scientific outcome = null
```

The provisional `GREEN-UNREVIEWED RQ0-LOCAL-ATLAS` headline from ledger #51
is withdrawn.  No terminal `RQ0-LOCAL-ATLAS`,
`RQ0-LOCALIZATION-GROUPOID`, `RQ0-L0-BLOCKED-AT-ADDRESS`, or
`RQ0-L0-BLOCKED-AT-REGIONAL-MAPS` result is conferred.

This disposition is forced by the frozen pin's novelty and robustness gates,
not by a failure of the finite order-144 algebraic calculation.

## Adjudicator verification

No held-out scorer was rerun.  The estimator, proof, fixture, scorer, receipt
and delivery remain byte-identical to their frozen hashes.

The adjudicator performed the following independent checks after the hostile
report was frozen:

1. A source diff between the opened order-192 fixture and the held-out
   constructor confirms the same first three cyclic factors, tensor-law
   architecture, selected-generator pattern, factor/scope helpers, complete
   row construction, per-factor record/gauge placement and atom-set context
   machinery.  The held-out constructor replaces the fourth `D4` factor by
   `S3`.  Different total order proves nonisomorphism, not non-derivation.
2. The hostile scratch rebuild was inspected to confirm that it imports no
   repository implementation and was executed again.  It reproduces order
   144; center order 24; derived order 3; the complete element-order
   histogram; 70 normal subobjects; 36 direct-pool members; 14 passing
   certificates; the unique finest `(2,3,4,6)` tuple; represented-algebra
   rank 40; and the `10/31/31/64/64/3` regional/factual counts.
3. On the unrelated public regional calibration, the adjudicator replaced a
   finest certificate by an otherwise identical object with
   `independently_generated=False`, synchronized it into the analysis and
   atlas, and invoked `adjudicate_resolution_artifacts`.  The result was
   still `RQ0-LOCAL-ATLAS`, exit 0, although `certificate.passes` was false.
4. Public serialized-input probes independently reproduced positive
   `RQ0-LOCAL-ATLAS` results after each of these malformed mutations:
   string `"false"` selectability values, a dimension-one gauge on the
   larger carrier, readout resolution `[[999]]`, an incompatible context
   boundary and an incompatible record boundary.  A `None` cap escaped as
   `TypeError`; a `NaN` cap returned a positive atlas.
5. The public full-instrument pair validator accepted a row map in which one
   source row was deleted and another duplicated.  Source inspection also
   confirms that the triple loop composes carrier actions and two singleton
   projector atoms, not every instrument field.
6. Direct inspection confirms that H005 is order-only, H035 is a literal
   true declaration, the anchor control constructs an outcome instead of
   rerunning a corrupted-anchor pipeline, positive metamorphic gates require
   positivity, and unreached negative-branch controls can be counted as
   passing.
7. Direct inspection confirms that the factor search excludes the ambient
   subgroup and emits tuples only from two through eight proper factors.
   The stated completeness theorem therefore needs that qualification.

These checks accept the load-bearing hostile counterexamples independently.

## Finding-by-finding adjudication

### F1 — novelty/provenance: accepted, critical

The binding pin required a fixture not isomorphic to, derived from, or a
re-encoding of either forbidden antecedent.  The held-out object is
nonisomorphic to both by order, but it is directly derived from the opened
order-192 product-template construction.  The scorer tested only order 144
against 192 and 216.  H005 therefore did not measure the registered novelty
predicate.

This failure alone prevents a held-out scientific rung.  The order-144 object
is reclassified permanently as an opened synthetic regression benchmark.

### F2 — adversarial artifact adjudication: accepted, high

The explicit false-certificate probe returns a positive atlas.  Membership
in a mutated `finest_certificates` tuple substitutes for replay of P1–P8, and
the artifact validator does not require `certificate.passes`.  The advertised
adversarial fail-closed theorem is false.

### F3 — serialized input and cap totality: accepted, high

The deserializer coerces rather than strictly types several fields, dataset
validation omits load-bearing carrier/projector/boundary checks, and cap
construction occurs outside the resolver boundary.  The reproduced malformed
positives and `None`/`NaN` cap behavior refute the universal untrusted-input
and total-invocation claims.

### F4 — official gate neutrality: accepted, high

H005 and H035 are not scientific measurements of their advertised claims;
the anchor mutant is not end-to-end; and the worker is biased toward a
positive main result.  Consequently “35/35 mandatory hostile controls” is
withdrawn as substantive certification evidence.  The stored 35 entries and
their literal pass bits remain historical receipt facts.

### F5 — full twisted triple: accepted, high

All three constructed pair maps pass the implementation's existing validator,
and their carrier/projector loop fails.  That limited calibration survives.
The validator is not row-bijective, record dynamics are not transported by
the carrier action, and the triple comparison does not compose the complete
instrument fields.  The claim that only the full-instrument triple equation
fails is withdrawn.

### F6 — completeness scope: accepted, medium

The pruning and enumeration remain sound for the actual search class.  The
completeness statement is narrowed to decompositions into two through eight
proper normal direct factors, subject to the registered caps.  It does not
cover a singleton ambient-factor tuple.

### F7 — delivery wording: accepted, medium

Forward corrections, without editing the frozen delivery, are:

- the four record candidates instantiate one pre-existing two-level Fourier
  witness helper with different supports/projectors; their dynamics are not
  four fresh mechanisms;
- row laws and signatures are separately serialized but co-generated by the
  fixture;
- the result concerns a synthetic exact operational model, not established
  physical stable facts;
- the 64 paths include identities; 12 are strict nonidentity two-step paths;
- not all mandatory controls were substantively implemented.

The delivery's explicit no-topology/no-causality ceiling remains correct.

## Evidence retained

The following survives as opened, non-scoring calibration evidence at the
declared finite exact scope:

- frozen chronology and source immutability;
- absence of estimator branches tailored to public or held-out handles,
  hashes, orders, contexts, records or intended truth;
- the exact order-144 group law and faithful 24-state monomial representation;
- the independently reproduced finite counts and finest `(2,3,4,6)`
  factorization;
- the actual ten-object synthetic regional atlas, its 31 region maps, 31
  fact maps, exact projector pullbacks and handle invariance;
- the four passing instances of the inherited W3 witness;
- sparse/dense equivalence and measured cache/performance behavior on the
  opened public suite;
- soundness of certificates produced by the normal estimator path;
- completeness only for two through eight proper normal direct factors,
  subject to caps.

The honest summary is:

> A byte-frozen generic estimator correctly reconstructed a finite
> normal-direct-product atlas on a post-freeze, nonisomorphic but
> source-derived synthetic benchmark.  This is useful opened regression
> evidence, but it does not satisfy the registered held-out novelty or
> robustness gates.

## Closure and successor discipline

The stopping rule forbids post-truth estimator repair, fixture replacement,
cap change, second held-out fixture or score, and hidden rescore.  Therefore
`ACCEPT-WITH-FIXES` is unavailable and this cycle is closed rather than
repaired.

No automatic successor L0 cycle is authorized.  `RQ0-T1`, `RQ0-C1`,
topology, influence, causality, geometry, spacetime, fields and gravity remain
prohibited because no terminal scientific L0 localization result exists.
