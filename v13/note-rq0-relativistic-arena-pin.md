# v13 RQ0 — the relativistic arena: clean-sheet construction pin

**Status:** PIN, STRICT, 2026-07-30.  **AUTHORIZED CONSTRUCTION UNIT.**
The construction begins only after this pin is committed.  It is bound to the
user's 2026-07-30 correction and to the immutable pre-pin checkpoint
`8adb460c9c5568d75ac73d4998e89d4c8de7ff64`.

> **CONSTRUCT RQ0; DO NOT SEARCH FOR A PRE-EXISTING RQ0.**

The prior `GW2-BLOCKED-AT-1` verdict is withdrawn in v13 LOG #10 as a
procedural misreading.  Absence of a finished relativistic arena in v10 or any
other legacy version is not an allowed block.  The committed corpus supplies
antecedent theorems, scopes, controls, fixtures and benchmarks; this unit is
authorized to define new v13 objects and explicitly labelled postulates under
the four-gate rule.

## 1. Central question and target

Can an atlas of finite local stochastic/amplitude processes, equipped with
independently certified stable records, generate an emergent regional
Lorentzian kinematical arena without presupposing a global event set, global
present, unique global token labels, background coordinates, a planted
metric, field propagation or Einstein dynamics?

The target is a newly constructed package

$$
\boxed{\mathfrak A_0=(\mathbf{Reg},\mathcal R,\prec,\mu,[g],\mathbf{Ref})}.
$$

- $\mathbf{Reg}$ is a category, site or groupoid of finite physical regions.
- $\mathcal R(D)$ is the independently W3-certified stable-record algebra of
  region $D$.
- $\prec$ is an intrinsic causal relation or local causal-cone structure.
- $\mu$ is an intrinsic count/volume measure.
- $[g]$ is a finite conformal Lorentzian structure reconstructed from the
  intrinsic order, with volume kept logically distinct.
- $\mathbf{Ref}$ is the refinement/coarse-graining structure.

$\mathbf{Reg}$ is not a family of subsets of one global event set.  An overlap
is not assumed to be a literal set intersection.  Token identity may remain
groupoid-valued.  RQ0 is kinematics, not gravity dynamics.

## 2. Binding antecedents and use classes

The receipt must lock and classify every reuse of these files:

| antecedent | SHA-256 | permitted use and exact scope |
|---|---|---|
| `v12/relativistic-isp-v12-paper0-the-weld.md` | `426633d8a9dfc85e24414ca4681ff5882a07110b410d34c6eed5d7051a4d67ef` | ontology discipline: local actuality; no global event set; O2 nomological composition; O3 record classicality; O4 open; four gates |
| `v12/paper1-composition-defect.md` | `81bdab5673fb67b63cd10c08fbb80870f8aa01088047718c5b4bf447e1669128` | W3 support predicates and decision theorem; local classical seams; composition-compatible boundary gauge; phase results only at declared one-chart scope |
| `v12/note-w6-record-coreference.md` | `2f18e863c1930ef2bfd52cf448a8baadd8ceac47696d80e6e4ae2ccf802d37c3` | fact/token distinction; erasure and duplicate controls; chart-level co-reference; five-valued discriminator |
| `v12/paper2-record-coreference.md` | `d6af0e6513fc7088407dc5a26c513ecc4e9e45b5a5ae71ffa8a9571f274ad670` | effective descent; set versus groupoid amalgamation; certificates sufficient under their adopted criterion; stability premise inherited, not derived |
| `v13/note-gw1-metric-from-closure.md` | `6f825ef6e1ced4842885a7356a860c7e55b16dc9d5dc4c15a3ef5da54da26627` | typed-object and no-smuggling discipline; geometry may not enter its estimator; negative gates require receipts |
| `v13/note-gw2-regional-descent-census-pin.md` | `48d35ea00c1388c9f67ac06ac12b729b9ad14d64359e704197acbe930957d462` | the seven required object types and the original “locate **or construct**” authorization; not its withdrawn execution |

v10 and earlier may be used only as explicitly labelled **lemma**, **fixture**,
**control** or **benchmark**.  They are not RQ0's required ontology.  No
legacy object is promoted merely because it exists; failure to locate one is
never an RQ0 verdict.

## 3. Four-gate worksheets

Every new region, morphism/overlap, causal relation, measure, conformal object
and refinement map must carry, in the report and receipt:

1. **Referent:** an exact constructor from declared inputs, or the label NEW
   POSTULATE with its independent data listed.
2. **Necessity:** the measured operation or obstruction requiring it.
3. **No-smuggling:** a machine-checkable forbidden-input list proving that the
   constructor does not contain the structure it is intended to recover.
4. **Discriminator:** independent positive and negative cases which could
   classify the proposal differently.

A syntactic name check is insufficient.  No-smuggling is a dataflow claim:
the estimator may consume only its declared intrinsic inputs and must be
frozen before held-out validation data are opened.

## 4. Stage 1 — construct a finite physical region

Construct at least one explicit candidate, preferably refining

$$
D=(X_D,P_D,\partial^-D,\partial^+D,\mathrm{Obs}_D,
\mathrm{Int}_D,\mathcal R(D)),
$$

where every component has a newly declared exact type.  $X_D$ is a finite
local process carrier, not a subset of a global event set; $P_D$ is the local
stochastic/amplitude process; the two interfaces are input/output ports, not
global-time slices; observables are locally accessible readouts; interventions
are admissible local process modifications; and $\mathcal R(D)$ is certified
by W3-grade H-corr/H-avail conditions rather than stipulated.

The regional gauge must answer when two presentations are the same region.
Configuration relabelling or a record-preserving process isomorphism is an
allowed candidate.  Object 1 is constructed only if:

- its full definition is explicit and typed;
- a nontrivial finite example is built;
- pure relabellings preserve all physical content;
- genuinely different process fragments are not falsely identified; and
- the record algebra passes the independent W3 support tests.

`RQ0-BLOCKED-AT-REGION` is allowed only after constructing an explicit
candidate class and proving a no-go or underdetermination result for the
declared gates.  “Not found” and grep/search absence are forbidden grounds.

## 5. Stage 2 — construct the regional site or groupoid

Define:

- objects: the new finite regions;
- morphisms: typed embeddings, restrictions, interface maps or
  process-preserving maps;
- automorphisms: physically irrelevant relabellings/symmetries;
- covering families, refinement and coarse-graining;
- overlap $D\wedge E$ from common accessible record facts and compatible
  process interfaces, never from global-set intersection.

Apply W6/Paper 2 at the record level: fact co-reference first; token identity
only when supported; groupoid-valued token descent counts as positive; phase
agreement is forbidden as a fact-identity criterion.

Build three genuinely distinct regions $D_1,D_2,D_3$ with a nontrivial common
fact algebra.  A relabelled copy is a gauge control, not the physical triple.
Test the comparison-map triangle on that common algebra modulo regional gauge.
Include a coherent triple and a twisted triple whose pairwise maps exist but
whose loop holonomy is nonidentity.

## 6. Stage 3 — intrinsic causal structure

Construct the causal relation or local cone from process/intervention data,
not coordinates.  The preferred candidate is operational influence:
$A\prec_D B$ when toggling an admissible intervention associated with record
proposition $A$ changes the accessible law of $B$, under a declared screening
rule.  An alternative is allowed if equally intrinsic and typed.

Measure or prove at the declared finite scope: irreflexivity (or the declared
finite analogue), acyclicity, transitive closure or a local-cone substitute,
gauge invariance, compatibility under restriction, and pair/triple descent.
Hidden coordinates may be used only for held-out scoring and may never enter
the estimator.  “No superluminal propagation” is a later comparison to the
reconstructed cone, not an input condition.

## 7. Stage 4 — intrinsic volume

Compare rather than silently choose among: elementary stable-fact count,
causal-atom count, stable-algebra information capacity and groupoid
cardinality.  The chosen $\mu$ must be gauge-invariant, distinguish facts from
tokens, avoid counting a redundant copy as a new fact unless occurrence-volume
is explicitly intended, respect refinement/coarse-graining, satisfy a declared
overlap law, and remain meaningful under token automorphisms.  A v10 event
count is not inherited as the answer.

## 8. Stage 5 — conformal Lorentzian reconstruction

Only after $(\mathbf{Reg},\mathcal R,\prec,\mu,\mathbf{Ref})$ exists may a
local $[g_D]$ be reconstructed from intrinsic causal/order data, with causal
class, volume density and absolute scale kept separate.  The estimator may not
read a Klein–Gordon propagator, Dirac operator, field dispersion, inserted unit
normal, hidden coordinate or embedding metric.

On every tested overlap require causal, measure and conformal comparison maps
to agree modulo regional gauge, and test the corresponding triple law.
Failure of absolute scale alone does not kill conformal RQ0.  An exact finite
low-dimensional model is an admissible first rung.  If a numerical diagnostic
is added, its hidden truth is scoring-only; seeds, tolerances, caps and failure
conditions are declared; the exact descent layer remains separate.

## 9. Mandatory controls

| ID | control | required classification |
|---|---|---|
| C1 | relabelled presentation of one region | all physical data descend |
| C2 | similar local probabilities but different process interfaces | no false regional identification |
| C3 | one fact represented by two redundant tokens | fact descent; token descent groupoid-valued or absent |
| C4 | record erasure | historical occurrence differs from present availability |
| C5 | three genuinely distinct regions with nonvacuous common fact algebra | coherent triple descent succeeds |
| C6 | pairwise maps with nontrivial loop | twisted triple descent fails |
| C7 | exact token automorphism | no arbitrary representative selected |
| C8 | facts descend while local causal relations disagree | `FACT-DESCENT-ONLY` control, not an arena |
| C9 | planted metric deliberately offered to an estimator | no-smuggling detector fires |
| C10 | two resolutions of one regional process | causal, volume and conformal data respect the declared refinement scope |
| C11 | attempted field-propagator input | field-independence guard fires |

Each mechanism has an independent positive and negative control.  Empty
overlaps are VACUOUS, never coherent successes.

## 10. Pre-registered outcome ladder

Report the highest earned rung and retain all lower earned rungs:

1. `RQ0-BLOCKED-AT-REGION` — only after a genuine construction/no-go;
2. `RQ0-REGIONS-CONSTRUCTED` — finite regions pass, no satisfactory atlas;
3. `RQ0-REGIONAL-SITE` — regions, morphisms, overlaps, refinement and triple
   instruments exist;
4. `RQ0-FACT-DESCENT` — independently stable shared facts descend;
5. `RQ0-GROUPOID-ARENA` — regional/geometric descent is positive only up to
   automorphism;
6. `RQ0-CAUSAL-ARENA` — intrinsic causal structure and measure descend;
7. `RQ0-CONFORMAL-ARENA` — the complete finite/refinement-scope package
   $(\mathbf{Reg},\mathcal R,\prec,\mu,[g],\mathbf{Ref})$ exists.

A negative at a later rung does not erase an earlier positive.  No unlisted
verdict may be invented.

## 11. Kill conditions

The unit stops at the highest prior rung if any of the following occurs:

- a region is individuated by a global event ID, coordinate, planted causal
  order, metric or spacetime location;
- the stable-record algebra is merely declared rather than independently
  passing W3-grade support conditions;
- overlap assumes literal set intersection or unique token identity;
- a relabelled copy is counted as the physical three-region control;
- phase agreement enters fact identity;
- the causal estimator reads hidden coordinates or a planted cone;
- the measure silently equates fact, token and occurrence counts;
- the conformal estimator reads a field, planted geometry, unit normal or
  held-out score;
- the exact path uses floats/tolerances or an anchor failure does not exit 1;
- a purported gate restates constructor input instead of testing it.

No kill authorizes H_a[N], gravity dynamics, Einstein equations, stress
tensors, backreaction, gravitons, scalar fields, Dirac fields or Maxwell.

## 12. Receipt and file discipline

New construction files are restricted to:

```text
v13/note-rq0-relativistic-arena.md
v13/code/rq0_regional_site_exact.py
v13/code/rq0_causal_geometry.py
v13/code/rq0_output.txt
v13/code/rq0_receipt.json
```

The adjudicator may append `v13/LOG.md` and update only RUNBOOK's live pointer.
No v10, v11, v12, old v13 result, or v13 Paper 0 file may be edited.

Exact arithmetic is mandatory for process laws, record tests, fact/token
co-reference, descent, automorphism/orbit classification, causal combinatorics,
volume bookkeeping and any exact conformal layer.  Numerical geometry, if
used, lives in the separate `rq0_causal_geometry.py`; hidden truth is read only
by its scorer.  The receipt states every postulate, inherited theorem,
constructor, search scope, cap, seed and legacy use class.  Counts are computed,
never typed.

Required verification:

- all antecedent hash locks and the frozen pin-commit ancestry check;
- normal master receipt exits 0 and matches `rq0_output.txt` exactly;
- two complete normal runs are byte-identical modulo no fields (timestamps are
  forbidden);
- deliberate anchor mutant exits 1 visibly;
- positive and negative controls for every claimed rung;
- `py_compile`, AST/grep float sweep and `git diff --check`;
- total normal runtime below 120 seconds, with no silent interval above 60
  seconds.

## 13. Required final report

The report must answer, with each claim tagged DEFINITION, POSTULATE, THEOREM,
MEASUREMENT or CONJECTURE: what a finite region is; why it is not a disguised
global subset; its local process; independent W3 record certification; physical
overlap and fact descent; set- versus groupoid-valued token descent; intrinsic
causality; the meaning of $\mu$; field-free geometry reconstruction; descent of
causal, volume and conformal data on the genuine triple; the first unresolved
obstruction; and the exact highest earned rung.

The final answer “the old corpus lacks object 1” is forbidden.

## 14. Successor discipline

This run stops after the RQ0 delivery.  The binding order remains:

$$
\text{W6/Paper 2 record descent}
\longrightarrow \text{RQ0 relativistic arena}
\longrightarrow \text{RQ1 scalar/Dirac independent tests}
\longrightarrow \text{gravity dynamics and universal backreaction}.
$$

The later field cone must be reconstructed independently and compared with
the RQ0 order cone.  No field propagator may be used to construct that cone in
this unit.
