# RQ0-L0 analytical foundations pin

## Operational Supports of Stable Records

**Cycle:** one bounded, forward-only analytical foundations cycle  
**Status:** `PIN`  
**Base:** `dceb524` (public architecture cycle closed; scientific outcome null)  
**Primary artifact:** `note-rq0-operational-supports-foundations.md`  
**Ceiling:** intrinsic quantum charts and their categorical overlaps only

## 1. Binding instruction

This is a mathematical-physics cycle, not a software-verification or held-out scoring cycle.

The cycle must derive its candidate regional objects from the internal amplitude-instrument structure. It must not recover a headline by attaching support labels to W3 witnesses, by intersecting common operation handles, by reading planted factor names, or by verifying a supplied finite atlas.

No estimator freeze, hidden fixture, hidden truth, large adversarial harness, scientific receipt, topology, influence, causality, geometry, spacetime, field theory or gravity work is authorized.

Small scripts may check arithmetic only after the relevant definitions and derivations have been written. They cannot define chart support, physical overlap, record descent or any outcome.

## 2. Question

Can a stable-record process determine an intrinsic quantum process chart, and can independently presented charts possess physical overlaps, using only the arrows, effects, compositions, operational equivalences and gauge structure of a finite amplitude instrument?

The target is not spatial locality. It is the strongest pre-spatial statement that can be justified:

1. an intrinsic quantum chart groupoid; or
2. a pullback-closed cover by intrinsic process charts.

If the supplied operational data underdetermine either object, the theory must retain the full ambiguity or state a no-go result.

## 3. Binding antecedents and scopes

The analytical note must use these antecedents only at their proved scopes:

1. **v12 Paper 0:** local actuality, no primitive global event set, nomological composition and the four-gate rule.
2. **v12 Paper 1:** typed amplitude composition, composition-compatible boundary gauge, W3 correlation/availability conditions, record-preserving classical seams and coherent erasure. Its phase result has one-chart scope.
3. **v12 Paper 2 / W6:** fact co-reference is not token co-reference; equal laws and phase agreement do not establish fact identity; descent may be set- or groupoid-valued.
4. **Terminal RQ0-A:** declared finite quantum regions can share a stable fact through typed amplitude-subinstrument maps and projector pullbacks. This is a positive control with regions already declared, not an intrinsic-localization theorem.
5. **Public architecture rejection #59:** a W3 witness plus `access_operations`, common handles or planted intersections does not derive localization. The opened `Q8` object is calibration evidence and a support-smuggling negative for the generic claim.

No conclusion may silently exceed one of these scopes.

## 4. Exact finite ambient language

The note must define a finite category or bicategory of admissible amplitude instruments before using the words subinstrument, chart or pullback.

At minimum, an ambient instrument must type:

- finite boundary state spaces;
- admissible amplitude arrows and their source/target boundaries;
- declared partial composition and identities;
- admissible preparations and effects/probes;
- accessible record readouts and their projectors;
- preserving, erasing and no-write continuations when a W3 diagram is claimed;
- operational equivalence under all declared preparation/effect contexts;
- composition-compatible boundary gauge.

The note must say which maps are admissible and what they preserve. A map called an embedding, restriction, quotient, channel, retract or isomorphism must have a separately stated type and equations.

Raw matrix-entry inclusion, shared names and common carrier coordinates are presentations, not physical morphisms.

## 5. Mandatory support-smuggling theorem

Define an **externally supported W3 schema** as an ambient instrument `D`, an internally evaluable W3 witness `W`, and an independently supplied label

```text
sigma(W) subset of displayed operation handles.
```

Prove a theorem of the following form at an explicit finite scope:

> If `sigma` is not determined naturally by the ambient operational structure and the internal W3 diagram, then there exist observationally identical inputs with the same amplitudes, compositions and W3 decisions but different admissible support labels. Any localization rule that follows `sigma` is therefore non-identifiable from the physical input and is not invariant under independent presentation changes.

The proof must distinguish:

- harmless relabelling of one already constructed chart;
- independent relabelling of separately presented candidate charts;
- genuinely different counterfactual completion;
- a support assignment invariant under every ambient automorphism.

The theorem must state its assumptions and its exact conclusion. It is not a no-go theorem against all intrinsic localization.

## 6. Internal W3 diagram

Construct a definition in which every element of a record witness is internal to the ambient amplitude instrument:

- preparation arrows;
- the write arrow;
- the cut projectors/effects defining the candidate record;
- preserving continuations;
- erasing continuations;
- a no-write control;
- availability probes/readouts;
- all composites used by H-corr and H-avail.

The record decision must be computed from those arrows and effects. No `access_operations`, support field, region label or external Boolean algebra may be paired with the witness.

Occurrence and continuation-relative availability must remain distinct. The Boolean record interface is derived from the W3-certified projector resolution only after the operational object containing that diagram has been constructed.

## 7. Minimal internal operational envelope

Given an internal W3 diagram `W -> D`, define and compare candidate closure operations that could produce its minimal operational envelope.

The analysis must include:

1. closure under every source/target identity and every composite needed to evaluate W3;
2. closure under the declared gauge action;
3. closure under operational equivalence rather than preferred representatives;
4. any continuation, preparation or effect needed to distinguish preserve from erase and write from no-write;
5. exclusion of arrows that are not forced by these closure requirements.

For the selected finite scope, prove existence. Prove presentation invariance and gauge covariance. Prove uniqueness up to the declared isomorphism if it holds. If intersections or closures of admissible subinstruments need not remain admissible, state the precise failure and return a groupoid or family of minimal envelopes rather than selecting one.

## 8. Competing chart notions

The note must compare, without prejudging the winner:

### 8.1 Closed subinstrument

A typed subobject closed under the required arrows, identities, compositions, preparations, effects and gauge action.

### 8.2 Split operational retract

An admissible pair

```text
i : A -> D,
p : D -> A,
p i = id_A,
```

with an explicit physical interpretation and preservation conditions. The note must identify what extra structure the splitting assumes and must not infer a split merely from a set-theoretic subset.

### 8.3 Operator-algebraic subsystem

A finite observable/effect algebra, or a represented algebra with a declared commutant/conditional expectation structure, tied back to executable preparations, effects and amplitude arrows. Algebra inclusion alone is insufficient if it has no instrument-level realization.

The comparison must cover existence, uniqueness, gauge covariance, closure under pullback and susceptibility to dormant completions.

## 9. Physical overlap

Physical overlap must be defined by a categorical universal property in an independently specified category of admissible amplitude instruments and maps.

For charts `A -> D` and `B -> D`, a candidate overlap `P` must come with maps

```text
P -> A,
P -> B
```

whose composites into `D` agree and which satisfy the pullback universal property against every admissible test instrument in the declared finite category.

The construction must not use:

- a supplied finite list of regional objects;
- equality of operation handles;
- literal intersection of planted atoms;
- equal record probabilities;
- equal phases;
- a common carrier basis without physical maps.

The note must distinguish a pullback in the chosen instrument category from a set-theoretic pullback of presentations. If the category lacks the required pullback, the result is `NO-PHYSICAL-PULLBACK` at that scope rather than an invented intersection.

## 10. Record assignment after overlap

Only after charts and their physical maps exist may the note define

```text
Rec : Chart(D)^op -> BoolAlg
```

or its groupoid/pseudofunctor analogue.

`Rec(A)` must be the Boolean algebra generated by W3-certified internal projectors of `A`. Its action on a chart map must be induced by exact projector/effect pullback. Handle equality is forbidden.

The note must state and prove, at the declared finite scope:

- identity;
- composition;
- gauge covariance;
- invariance under record-handle renaming;
- direct-versus-composite naturality;
- the relationship between fact descent and token descent.

## 11. Automorphism no-selection theorem

Prove:

> If an ambient instrument automorphism group acts transitively on a family of candidate intrinsic charts and the allowed operational data contain no invariant discriminator, no gauge-natural rule can select one member of that family.

The correct output is the action groupoid or orbit of candidate charts. Choosing a representative is a gauge fixing, not a physical localization result.

The theorem must also state the converse limitation: an invariant discriminator can break the underdetermination, but its physical admissibility must pass the referent and no-smuggling gates.

## 12. Localization-grounding theorem or counterexample

Analyze whether realized amplitudes and record laws determine localization, or whether counterfactual intervention/composition structure is indispensable.

Construct either a theorem or an exact counterexample with:

- the same realized preparation-to-readout amplitudes;
- the same W3 record laws on the realized history;
- different admissible counterfactual intervention or composition completions;
- different intrinsic chart or pullback structures.

If such a pair exists, engrave that localization is nomological rather than determined by realized history alone. State exactly which additional counterfactual data are necessary and which remain conventional.

## 13. Mandatory hand-worked controls

The note must analyze these without a large computational harness:

1. **Current public `Q8`: support-smuggling negative.** Its declared cyclic scopes and common central handles may illustrate a chart after declaration, but cannot prove intrinsic recovery.
2. **Terminal RQ0-A: positive physical-overlap control.** With regions and morphisms declared, its projector pullback establishes a shared fact at its terminal finite scope; it does not discover the regions.
3. **Clean tensor product.** Show what closed factors, split retracts and operator-algebraic subsystems do in a genuinely factorized instrument.
4. **Symmetry/groupoid.** Construct symmetry-related candidate charts with no invariant selector and retain their action groupoid.
5. **Fully symmetric no-localization control.** Show a case in which every proper candidate is moved by the ambient automorphism group and no invariant proper chart is justified.

Each control must identify definitions, assumptions, deductions and non-claims.

## 14. Four-gate table

Every new primitive or postulate must be classified under:

- **REFERENT:** what physical operation, preparation, effect or equivalence it denotes;
- **NECESSITY:** why the target theorem cannot be stated without it;
- **NO-SMUGGLING:** what desired chart or overlap is not encoded in it;
- **DISCRIMINATOR:** which positive and negative cases it separates.

The final note must label each major statement as definition, postulate, lemma, theorem, counterexample, measurement or conjecture.

## 15. Outcome ladder

Before external review, every result is analytical and provisional. The strongest permitted provisional outcomes are:

- `RQ0-L0-FOUNDATIONS-NO-INTRINSIC-SUPPORT` — the declared finite operational language cannot determine a proper chart;
- `RQ0-L0-INTRINSIC-CHART-GROUPOID` — intrinsic record-bearing charts exist only up to unresolved automorphism;
- `RQ0-L0-PULLBACK-CLOSED-PROCESS-COVER` — intrinsic charts and their physical pullbacks exist at the declared finite process scope.

A later rung does not erase a valid earlier theorem. None of these outcomes means spatial locality, a manifold cover, causal localization or spacetime.

No outcome may be terminal before a fresh external mathematical hostile review and separate adjudication.

## 16. Review protocol

After the analytical note is complete:

1. commit the note and append-only provenance separately;
2. dispatch one external repo-read-only mathematical hostile review;
3. require attacks on physical referents, necessity, no-smuggling, invariance, universal properties, automorphism arguments, counterexamples and theorem validity;
4. require the reviewer to reconstruct the hand examples independently;
5. freeze the complete report verbatim;
6. adjudicate it in a separate commit without silently repairing the reviewed note;
7. halt and request explicit user authorization before any implementation cycle.

`RQ0-T1` and `RQ0-C1` remain closed throughout.

## 17. Stopping rule

This is one analytical cycle. It ends after the hostile report is frozen and adjudicated. No post-review repair, implementation, hidden score or automatic successor is authorized.
