# TOP — JOINT ADJUDICATION (R1 #310, R2 #307, R3 #311)

**Adjudicator:** the coordinating agent.  **Inputs:** the three frozen
reviews (`review-top-operator.md` / `review-top-effectus.md` /
`review-top-instrument.md`), the frozen delivery (paper `ab09d091ed1d`,
code `e2d0200e4a06`, output `bd213b18d1b1`, receipt `0fb290cf4bfd`),
the frozen protocol (`note-top-hostile-protocol.md`).  **Standing:** all
three grades are ACCEPT-WITH-FIXES; no reviewer found a false theorem;
combined recomputation weight ~1,090 (R1 590 + R2 ~70 independent numbers
+ R3 434) with **zero instrument-vs-reviewer numerical disagreements**.
The single false number found is in the *paper*, not the instrument.

## 1. Resolved grade

**ACCEPT-WITH-FIXES**, unanimous.  The unit's verdicts survive as
*measured facts*; what the repair must change is their **epistemic
labels** (which clauses are measurements vs disclosures), their
**qualifiers** (which are P*-relative and instance-specific), and one
**false paper number**.

## 2. Cross-confirmed findings (the panel's agreements)

**A. The false paper number** (R1-F1 MAJOR ∥ R3-F3 MAJOR, independent
routes; R2-F6 the adjacent C8 prose).  §5.2 prints C7's clause-(c) cell
as **0**; the frozen output (line 136), the frozen receipt
(`c_completions_with_a_non_linear_K: 6`), R1's census and R3's census
(six named witnesses) all say **6**.  The prose "C4, C5, C7, C9 and C10
predict linearity perfectly" is false — C7 passes zero clauses.  R1-F2:
the same paragraph's C8 "holds on the locus" is false (192/384).  This
is the programme's **second false paper number** (after NT's).  Owned in
the ledger at freeze (#310, #311).

**B. The engraved-rule recurrence** (R3-F1 MAJOR).  Typed literal
qualifiers survive in **all three** verdict strings (b₁ 140→141 exits 0;
link b₁ 16998→16999; locus 384→385): the gates compare only the prefix
and `.split("<")` discards the qualifier.  This is TB3-R3's F1
recurring **against an already-engraved rule** (#257,
qualifiers-are-part-of-the-verdict), in a unit delivered after the
engraving.  Root cause: TOP was built in parallel with TB3's repair and
never diffed its gates against rules engraved since its pin froze.
R2-F1 is the same disease at the naming level: the citable verdict heads
carry none of their restrictions.

**C. The forced-clause cluster** (R2-F2 MAJOR, R2-F5, R1-F3, R3-MOD).
- R2-F2: `CONSISTENT` at the reference instance is **forced by an
  automorphism of the drawn table** (vertex-transitivity) — it is a
  disclosure, not a measurement, and §4.2 asserts the opposite.
- R1-F3 ∥ R2-F5: clause (c) has no measured content (every passer ⊆ C4;
  the count is |C \ C4|), and **C5 ≡ C10 extensionally** (PG(2,2)
  collineations = GL(3,2) over the prime field) — an undisclosed
  duplicate exactly parallel to the disclosed C1 ≡ C2.
- R3-MOD: TOP-SIMPLICIAL's χ clause is analytically forced (the
  alternating binomial sum is 1 for every k; verified k=1..40).
All fall under §14 addendum #208: forced clauses are disclosures.

**D. The temporal-b₁ demotion** (R1-F7/F15 ∥ R2-F3 MAJOR ∥ R2-F13).
b₁ = (T−1)(|V|−1) whenever each read time is simply connected; it
returns 140 for the reference, the W-class, **and the scrambled
negative control**.  The headline degree-1 invariant is **insensitive to
the identification data**; only b₂ sees it.  R2 adds: the temporal
reading is *forced* by deviation 2's same-checkpoint-only 2-cells, and
the one measurement that would decide whether it is physical — **do the
drawn maps for one pair agree across coordinate cells** — was never
taken.  R2-F13: the dimension estimator is extensive and cannot
stabilise under refinement.  R1's constructive contribution: the
checkpoint sub-nerves' b₀/b₁ equal those of the **block-incidence graph
of the two rules' partitions** (25/25) — the temporal b₁ is
partition-nesting structure at each read time, stated exactly.

**E. The P* relativity** (R1-F4).  P* is an undeclared arena coordinate
chosen by enumeration order.  R1 swept all five wing symmetries: the
three transpositions agree (locus 384, 48 on-locus); the two 3-cycles
give locus **270** and **0**, with order-7 defects appearing.  The
verdict's 384/48 are P*-relative.  §15 (declared-arena-as-data) applies.

**F. Falsifier gaps** (R3-F2 MAJOR; R1-F9 ∥ R2-F9; R3-MODs; R1 minors).
- R3-F2: the atlas's `link`/`star` have **no falsifier** — the bug class
  injected into `local_profiles` alone exits 0 while the load-bearing
  16,998 becomes 0 at all five instances; the declared mutant's death is
  carried entirely by the control path.
- R1-F9 ∥ R2-F9: the coherence "two independent routes" are one route
  (d₁,d₂,d₃ conjugate; the measured pattern is never mixed) — §13 #234.
- R3-MODs: three "external" anchors carry a source-typed declared side
  structurally unable to fail; `provenance-lax` is a waiver declared as
  a computation (both §10 denominators off by one); Burnside's floor
  division absorbs fixed-cell errors 1–5.
- R2-F8: no committed number anchors any rank ∂₂ computation though
  §7.1 claims one; R1 minors: sampled ∂₁∂₂ = 0 check, ungated
  pair-table symmetry, one anchor-provenance label, §7.2's unmeasured
  explanation; R2-F11: six candidates carry receipt provenance the pin
  does not support.

**G. What survived every attack** (all three reviewers).  The K₃₆
completeness ⟹ contractible nerve; the full invariant table at five
instances; both halves of the b₁ claim *as computations*; H₂-additivity;
the wing action, Burnside count and quotient (b₁ = 25, the −50); the
two- and three-wing controls against TB3's committed bytes; the entire
252/5,040 Fano-selector census with `{1:4,2:48,3:72,4:128}`; **the
never-at-5,6 theorem** (order 5 by Lagrange, order 6 by the spectrum —
R1 re-proved it); the OR-vs-XOR bug's discovery-and-fix confirmed with
the delta exact (R3 reconstructed it: quotient b₁ 24 vs 25, 240 loop
1-cells, rank ∂₁ 5→6); byte-identical delivery reruns by R1 (×2) and R3.
R2-F7's strictly finer order-theoretic condition *refutes* "and nothing
finer" — a gift, not a defect: the finer invariant exists and is
measured.

## 3. Binding repair orders (R-TOP-1 … R-TOP-12)

1. **Fix the false paper numbers** (A): §5.2 C7 clause-(c) 0 → 6 with
   the receipt as source; strike/correct the "predict linearity
   perfectly" sentence and the C8 on-locus sentence.  The corrected
   table must be regenerated from the receipt, not retyped.
2. **Gate the full verdict strings byte-for-byte**, qualifiers included,
   with the qualifiers *computed* (#257).  Verdict-flip mutants must
   prove each gate can fail on the qualifier alone.
3. **Rename per the qualifier precedent** (R2-F1): the manifold verdict
   head carries its restrictions — instance-specific 3/5, dimprofile
   {11,35}, links-not-circles — as computed qualifier text; same
   discipline for the other two verdicts (the locus one gains
   `P*-relative` per R-TOP-5).
4. **Reclassify the forced clauses as disclosures** (C): vertex-forced
   CONSISTENT (fix §4.2's opposite claim), clause (c) as |C \ C4|,
   C5 ≡ C10 disclosed beside C1 ≡ C2, the χ clause.  Forcing
   disclosures for C5/C9/C10 (σ_P all linear) stated like C4's.
5. **Declare P* as an arena datum and print the five-symmetry sweep**
   (E): the 3-cycle rows (locus 270 / 0, order-7 defects) enter the
   paper as measured relativity; the verdict qualifier records
   `P*-relative (transposition class)`.
6. **Restate the temporal-b₁ claim** (D): b₁ is a simple-connectivity /
   partition-nesting census — print the (T−1)(|V|−1) identity, the
   scrambled-control insensitivity, and the block-incidence route
   (R1's, 25/25) as the exact statement.  b₂ and N_coh are the
   identification-carrying invariants; §8's sentence fixed on N_coh
   (false by 21 intra-checkpoint classes — R1-F16 ∥ R2-F4).
7. **TAKE THE DECIDING MEASUREMENT** (R2-F3): for one pair, compute
   whether the drawn maps agree across coordinate cells.  This is an
   adjudication-ordered measurement (LCB precedent).  Whatever it
   returns, it is printed, gated, and enters the continuum-rung inputs.
8. **Add the link/star falsifier** (R3-F2): gate `local_profiles`
   through an independent comparator (not `control_links`), per §14.
9. **One-route disclosure for coherence** (F): the conjugacy identity
   stated; the external anchor named as the content-bearing check.
10. **Anchor and gate repairs**: a committed rank ∂₂ anchor (or fix
    §7.1); exact-division Burnside gate; the three pseudo-external
    anchors retyped as declared-side or given failure modes; waiver
    denominators fixed (§10: waiver-only is 3); pair-table symmetry
    gated; provenance labels corrected (R2-F11: six candidates).
11. **Boundary-parity gate class** (R1-F10, R3-K5(b)): a gate whose
    death certificate is the OR-vs-XOR delta (quotient b₁ 25 with the
    XOR boundary, 24 under the OR mutant), so the bug class that was
    caught only incidentally is now caught by construction.
12. **"Nothing finer" corrected** (R2-F7): the measured strictly finer
    order-theoretic condition named; the claim rescoped to what the
    census actually established.

## 4. RUNBOOK engravings (this adjudication)

- **§13 addendum (repair-propagation):** *Parallel-built units inherit
  repairs.*  Before delivery, a worker must diff its gates against every
  rule engraved since its pin froze (rules bind at delivery time, #246).
  A recurrence of an already-engraved disease is a MAJOR by default and
  is named as a recurrence in the review.
- **§14 addendum (boundary parity):** Boolean-connective boundaries in
  incidence/complex constructions carry a parity-witness gate whose
  death certificate is the measured delta of the alternative connective
  (the OR-vs-XOR class).

## 5. What the continuum rung inherits from TOP (consolidated)

R2 §3.3's five requirements stand, now sharpened by the panel:
(i) a **non-complete overlap graph** (K₃₆ ⟹ contractible nerve: no
simplicial topology at this scale); (ii) a **declared refinement
family**; (iii) a pre-registered **intensive** invariant (R2-F13: the
current estimator is extensive); (iv) **R-TOP-7's measurement settled**
(it decides whether temporal b₁ is physical or deviation-2 bookkeeping);
(v) **N_coh as the working object**, with b₂ — not b₁ — as the
identification-carrying Betti number.  The block-incidence identity
gives the successor its exact language: read-time topology is partition
nesting.

## 6. Adjudicator's own ledger

The "exclusive Fano visit" framing was already owned at #301.  This
panel adds nothing new to my account on TOP, but the R3-F1 recurrence is
a **process failure of the coordination layer** (I dispatched TB3's
repair and TOP's construction concurrently without ordering a rule-diff
at TOP's delivery): owned here, engraved in §4 above.
