# RQ0-L0 final certification-and-typing cycle — invalid delivery

**Status:** `RQ0-L0-INVALID`, frozen one-shot delivery, 2026-07-31.

**Scientific outcome:** none.

**Binding pin:**
`note-rq0-operational-localization-final-certification-pin.md`.

**Frozen estimator commit:** `3b9d88a`.

This delivery records the one permitted post-freeze score exactly as it
occurred.  The new abstract fixture constructed successfully, but the frozen
estimator did not return before the complete-run 360-second cap.  The cap was
enforced twice: first during the irreversible score opening, and again by the
official fail-closed scorer.  No estimator edit, fixture shrinkage, truth
change, replacement fixture, or positive verdict followed.

\[
\boxed{
\texttt{RQ0-L0-INVALID}
\quad\text{(registered runtime cap; no scientific L0 verdict).}
}
\]

---

## 1. What was frozen before the fixture

Commit `3b9d88a` froze:

- the generic estimator
  `code/rq0_l0_certification_estimator_exact.py`, SHA-256
  `c1d3c3b36df71ed80b9ca9152be3d5a97f4d4c1c88ab9f3b0defd360ed14a5c3`;
- the fixture-independent finite soundness/completeness proof
  `note-rq0-operational-localization-final-certification-soundness.md`,
  SHA-256
  `acb1ccbcf893f26424bc2868af9354fe9cb3d038ff18917c3602f9c32775ec1d`;
- a 29/29 deterministic public calibration receipt.

At that commit the new fixture, abstract multiplication law, factor truth,
access-context truth, atlas truth, scorer, and delivery files did not exist.
The official scorer verifies that absence through Git object lookup.

The estimator remained byte-identical through this delivery.

---

## 2. The genuinely new post-freeze object

After freeze, the fixture introduced the heterogeneous exact finite family

\[
C_2\times C_3\times C_4\times D_4,
\]

of order 192.  This is not `S3^3`, does not use the old `(6,6,6)` factor
truth, and does not reuse the old complete proper-Boolean atlas.

The estimator-facing serialized object has:

- 192 opaque operation classes;
- 36,864 complete ordered composition rows;
- exact row type
  `(a,b,tau,status,result_class,M,observed_signature)`;
- independently supplied exact `M` on every implemented row;
- a 32-state exact monomial carrier over `Q(zeta_24)`;
- six independently selectable generators, with all other nonidentity
  operations composite-only;
- four fresh W3 write/preserve/erase/no-write record candidates;
- five selective operational access contexts;
- no hidden factor or atlas truth in estimator input.

Its amplitudes use a nonuniform exact cyclotomic phase frame.  Separate fixture
constructors for a global-character gauge copy, a carrier relabelling, and a
physical phase contrast were defined, but were not scored because the main
pipeline never returned.

The hidden scorer target had factor orders `(2,3,4,8)` and an irregular
five-context family.  Its intended meet closure contained nine record-bearing
objects rather than the old automatic complete proper-Boolean lattice.  These
remain hidden fixture facts, not recovered measurements.

---

## 3. Exact point of failure

The first score built the complete fixture and printed:

```text
fixture-built 192 36864 32
```

It entered `analyze_addressability` but did not return by the registered cap.
The worker was interrupted at the cap while replaying a candidate certificate,
inside exact represented-algebra construction for the ambient 32-dimensional
carrier.  The frozen algorithm recomputes the ambient exact algebra during
candidate certification, and this fixture made that path exceed the bounded
run.

The official scorer then authenticated all inputs, ran the 29/29 public suite,
built the same serialized fixture, and launched the same frozen worker with
the remaining portion of the complete 360-second budget.  It again emitted
the exact fixture-built marker and timed out before an address result.

This is a procedural/runtime failure.  It does not show that the fixture has
no address factors.  It also does not provide a factor certificate from which
regional maps or record descent could be built.

Therefore neither a positive rung nor `RQ0-L0-BLOCKED-AT-ADDRESS` is allowed.

---

## 4. What survives

The following pre-main evidence remains exact:

1. The generic row validator carries an independently supplied law `M` and
   separates type, status, result, physical composition, gauge, and signature
   gates.
2. The independent-generation predicate reads the selectability flag.
3. The hostile `S3 x C2` composite-only coset is rejected, while its fully
   selectable control localizes.
4. Declared `COLLAPSED`, `UNAVAILABLE`, and a true multiplication collision
   are separate measured controls.
5. Every public returned factor certificate replays all eight soundness
   predicates.
6. A public record-bearing `V4` ambiguity object yields three finest
   factorizations, 18 exact arrows, and all nine source/target pairs.
7. The public regional calibration constructs full maps on operations, rows
   and row laws, selectability, preparations, contexts, probes, readouts,
   records, and gauges.
8. Its exact projector/Boolean `Rec` maps pass identities, composition, and
   direct-versus-composite naturality under handle renaming.
9. The twisted control has three valid regional pair maps and three valid
   record pair maps; only the regional and factual loop equations fail.
10. The new fixture constructor and chronology are real: 192 classes, 36,864
    rows, carrier 32, six independently selectable generators, post-freeze,
    and non-`S3^3`.

These are calibration and construction evidence.  They do not promote a main
scientific L0 outcome after the cap failure.

---

## 5. What was not reached

Because the main estimator did not return, this run did not measure:

- main normal-subobject or factor counts;
- main factor orders;
- main sound-certificate replay;
- main `RegAddr` objects or arrows;
- main `FactIface` or `Rec` maps;
- main coherent or twisted triple behavior;
- main gauge, physical-phase, handle, projector, or map mutants;
- deterministic regeneration of a positive main receipt.

The mandatory end-to-end positive-outcome mutants cannot be claimed: there
was no positive main outcome to falsify.  The official resolver nevertheless
fails closed—every positive prerequisite is false and every positive outcome
is suppressed.

---

## 6. Answers to the pin's final questions

### 1. What does row law `M` represent?

`M` is the exact amplitude law physically supplied for one ordered typed
composite.  The fixture constructs it from the independently tabulated result
law.  The estimator compares it with input-law composition and with the result
representative but never substitutes a product for a missing row law.

### 2. How are the failure modes distinguished?

- `UNAVAILABLE`: no result and no law;
- declared `COLLAPSED`: a typed status rejection;
- true collision: two distinct implemented factor tuples give one result;
- missing/malformed: invalid input;
- this delivery: the valid new main search exceeded the complete runtime cap.

### 3. Why are returned factors independently operable?

The generic theorem and public replays require each factor to equal the exact
closure of its independently selectable classes.  No main factor was returned
here, so this theorem was not instantiated on the new fixture.

### 4. What is the theorem's scope?

Finite unital associative invertible single-type quotient objects, normal
direct factors, exact monomial representations over `Q(zeta_24)`, up to eight
factors and the registered search caps.  It is not completeness for arbitrary
quantum subsystem notions.

### 5. Why is the fixture genuinely new?

It first exists after `3b9d88a`, has abstract order 192 and intended factor
orders `(2,3,4,8)`, uses `C2 x C3 x C4 x D4`, and has a selective irregular
context family.  Static and Git chronology checks show no import or reuse of
the prohibited old truth.

### 6. What fields would `RegAddr` map?

The frozen constructor maps operation classes, complete rows and supplied
laws, selectability, preparations, contexts, probes, readouts, record
candidates, and gauge actions.  The main run never returned the coordinates
needed to instantiate these maps.

### 7–8. Is `Rec` handle-invariant and natural?

Yes on the public exact calibration: record identities derive from projector,
dynamics, boundary, and quotient-support structure; handles are ignored.
Projector atoms and Boolean maps pass identities, composition, and naturality.
No main `Rec` object was constructed.

### 9. Is the twisted control genuine?

Yes publicly: all three pair maps are full typed isomorphisms, including exact
projector permutations.  The two-step identity action disagrees with the
direct swap, so rejection occurs only at the loop gate.

### 10. Is ambiguity record-bearing?

Yes in the public `V4` control.  It does not imply a main-fixture groupoid.

### 11. Was the new atlas reconstructed?

No.  Its selective contexts are valid fixture input, but the estimator did
not return factor coordinates, so the hidden irregular atlas was not opened
as recovered evidence.

### 12. Which end-to-end mutants ran?

The new-main mutant suite was not reached after the binding cap.  Claiming it
would be false.  The fail-closed official main worker and immutable-anchor
checks ran and suppressed all positives.

### 13. Epistemic labels

- **Definitions:** typed rows, factor certificates, regional arrows,
  `FactIface`, `Rec`, and outcome graph.
- **Postulates:** complete finite operational access, selectability, row laws,
  contexts, preparations/readouts, and finite gauge.
- **Theorems:** conditional finite soundness/completeness of the frozen
  estimator.
- **Measurements:** 29/29 public gates; fixture construction counts; two cap
  failures.
- **Hidden fixture truth:** intended factor and atlas data, unearned.
- **Conjectures:** none promoted.

### 14. Highest outcome and first obstruction

There is no scientific outcome.  The procedural outcome is
`RQ0-L0-INVALID`.  The first obstruction is the registered runtime cap during
exact represented-algebra certification.

---

## 7. Receipt and stopping rule

Canonical files:

- `code/rq0_l0_certification_receipt.json`, SHA-256
  `c3dfe72c199dd9036f3e4966035805a89d80713a6b788d32451d03fbe589e852`;
- `code/rq0_l0_certification_output.txt`, SHA-256
  `ce5c3bedd42a188bd12f409f55ea01f9e476fd9baae69e0e9dac6f066398cb8b`;
- fixture source SHA-256
  `b2901029d8533af2b90aa5eddbff6ed583c218bec8476a9dd55dc116ed581b14`;
- scorer source SHA-256
  `2fab9d403d7f740206e2a8c5cf4287cfb98cd5709257c82fd4b19db565f461a0`.

The receipt has 11/11 provenance/procedural checks and no scientific outcome.
Its worker marker authenticates successful full fixture construction before
timeout.

The pin's stopping rule now fires:

- estimator edit: forbidden;
- replacement/shrunk fixture: forbidden;
- automatic retry cycle: forbidden;
- positive L0 claim: forbidden;
- next event: external read-only hostile review, verbatim freeze, and separate
  adjudication.

---

## 8. Ceiling

No topology, spatial localization, influence, causal order, spacetime,
relativistic, field, stress, gravity, or deformation claim is made.
`RQ0-T1` and `RQ0-C1` remain prohibited.

This delivery closes construction honestly: the new certification design was
stronger, but the one permitted unseen fixture was too expensive for the
frozen exact implementation under its own cap.
