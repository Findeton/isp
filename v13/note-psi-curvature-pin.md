# PSI — THE ψ-SIDE CURVATURE HUNT (PIN)

**Status:** PIN, STRICT, 2026-08-08.  **Immutable base:** commit
1426984 (GEN TERMINAL #222 inherited; COC running in parallel on
disjoint files).  **Authorization:** the standing order (#212);
the user's parallelization order 2026-08-08.  Lean NONE.

## The question (the registered open §11.12 of GEN, promoted)

All measured curvature is declaration-side: D is a function of
the completion's Q alone (ψ cancels); W arises from the gluing
rules.  DOES ANY ψ-SIDE CURVATURE EXIST — a loop whose holonomy
CHANGES when the physical state ψ changes at FIXED declarations?

## The instrument (frozen before fixture truth)

On base G with the declared Q FIXED at GEN's pinned
transposition and all rules/scopes FIXED: sweep a DECLARED
family of preparations ψ (declared as data first: at minimum —
GEN's pinned ψ; 3 further exchange-invariant states of
differing Schmidt rank; 3 exchange-non-invariant states; sizes
computed).  For each ψ: (a) the admission tables (which
identifications are FORCED); (b) the loop space; (c) the
holonomy value of every loop class, permutation-tuple counted.
Compare across ψ at matched coordinates.

## Pre-registered outcomes (only these)

- **PSI-CURVATURE-EXISTS** — a witness pair (loop, ψ₁ vs ψ₂):
  same declarations, different holonomy — the first ψ-side
  geometry; characterized (which loops, how the value depends
  on ψ).
- **PSI-PATH-SPACE-DEPENDENCE** — ψ changes WHICH loops exist
  (admission/loop-space varies) while every common loop's
  holonomy is ψ-invariant: the state shapes the arena's
  connectivity but not its twists — a distinct, named result.
- **PSI-DECLARATION-ONLY** — the full swept family shows
  holonomy AND loop space invariant under ψ at fixed
  declarations: the declaration-side theorem strengthened to
  the family, stated at its honest scope.
- **PSI-BLOCKED-AT-⟨object⟩** — census discipline.

## Discipline

All engraved lessons binding (§13/§14-all/§15-all).  Inherited
GEN/NT values hash-pinned, exit-1.  Positive control: the GEN
pinned ψ must reproduce the terminal group.  Negative control
w/ teeth: a declaration change (a different Q) must move the
holonomy (the GEN law's prediction — if it does not, the
instrument is dead).  Deliverables: v13/paper-psi-curvature.md
+ v13/code/psi_curvature_exact.py + _output.txt +
_receipt.json.  Then §13 freeze → panel → adjudication.
