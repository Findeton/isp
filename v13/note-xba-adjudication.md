# XBA — JOINT ADJUDICATION OF THE THREE-LENS PANEL

**Status:** ADJUDICATED, 2026-08-08 (v13 LOG #249).  **Object:**
commit d1593ac, protocol 81cc240.  **Frozen reviews:** R2 #244
(4d730a9ccd1b, REJECT), R3 #245 (42a6fb67d999), R1 #249a.

## Grade resolution

R1 AWF / R2 REJECT / R3 AWF; ~738 recomputations across three
divergent instruments, ZERO numerical errors.  **Adjudicated:
REJECT-AS-DELIVERED — MANDATORY RE-DERIVATION.**  The
arithmetic is perfect; the meaning-layer fails (the unnamed
fifth premise; the misnomer; the one-bit prediction; the
hard-coded "derivation" — the #24 disease at the central
object) and the instrument repeats the #234 diseases (ungated
verdict; one-route census).

## Established by the panel

**THE UNIFICATION (R2, the arc's biggest structural result):
D = P·u⁻¹·P·u = [P, u] — the three declaration→geometry laws
are ONE: XBA's clauses are centralizer conditions (COC's
criterion with ⟨P⟩ as the chart-generating group), and the
commutator's ORDER is GEN's completion-selection.**  The
forcing is COMPLETION-FORCED, not species-forced (the
equivariant control is species-compliant with a different
profile; E1–E3 exclude nothing; E4 + involutivity do the
work).  The five-clause truth (involutivity the unnamed fifth
premise; at ord(D) = 3: six holonomies, profile
[42,46,46,72,78,80] — 18/36 transpositions; constructively
exhibited).  The third instance risked one bit; **R1's F12
supplies the genuine confirmation: a third SPECIES (dim-4
wings, 256 configurations) satisfying the clauses and
reproducing the profile.**  C2a's vacuity legitimate; the
cache-refusal mechanism real; all hash pins fire.

## Re-derivation order (X-1..X-6, binding, one pass)

**X-1 NAME AND CLAIM:** SPECIES-FORCED-SPLIT →
**COMPLETION-FORCED-SPLIT**; the involutivity premise NAMED
everywhere the clause-count appears (abstract, §7.1, §7.2,
§10); the §11.10 species-relocation contradiction fixed; the
verdict re-derived within the pin's vocabulary at the honest
name.  **X-2 THE DERIVATION REAL:** §7.1's commutation step
scoped (valid iff D² = 1; the general law SQ_REAL_1 = D^±¹ per
order stated); the hard-coded tuple REPLACED by a computed
derivation; the ord(D)=3 counter-class incorporated and gated
([42,46,46,72,78,80]).  **X-3 THE UNIFICATION CENTRAL:**
D = [P, u] stated and GATED as the unit's central result (the
commutator identity verified on the instances; the three
prior laws derived from it as corollaries — existence =
COC-centralizer, order = GEN-selection, forcing =
completion).  **X-4 THE PREDICTION HONEST:** the inert
parameters stated (one-bit risk owned); **R1's third species
run IN-DELIVERY** as the real different-flesh confirmation;
the steering residual restated in R1's sharper measurable
form (3,906/4,096).  **X-5 INSTRUMENT:** verdict-in-gate +
verdict-flip mutant (R3's swap probe dies); a genuinely
independent second route + cell-completeness at all 12
candidates (R3's sizes2 probe dies); the four tautological
waiver gates re-founded or reclassified with the vacuity
stated; the fail-closed attribution fixed (prep-lax); C2a's
vacuity reason corrected (7/13); A01 given a mutant; the 13
truncated kill-names fixed.  **X-6** all three rewrite tables
row by row; deviations updated (the hard-coded tuple owned by
name); single-threaded; the one meta-sentence cites v13 LOG
#249.
