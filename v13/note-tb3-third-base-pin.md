# TB3 — THE THIRD BASE: THREE WINGS (PIN)

**Status:** PIN, STRICT, 2026-08-08.  **Immutable base:** commit
d7fdcfb (COC #252, XBA #260, PSI #265, HA #262 all terminal).
**Authorization:** THE USER'S STANDING ORDER v13 #228 ("launch
the third base after COC/PSI/XBA report"), now fired — axes set
by the three verdicts as #228 prescribed.  Lean NONE.

## The design (the first PURPOSE-BUILT base)

**THREE WINGS**, each system dim 2 + pointer dim 2 (carrier
2³ × 2³ = 64 configurations) — the multipartite jump: the wing
symmetry is now **S₃** (non-abelian BY CONSTRUCTION), triple
overlaps are NATIVE (3! = 6 leg-orders = six frames), and
multipartite preparations (GHZ-class vs W-class) differ in
exchange structure in ways no bipartite ψ can.  All
declarations as data before fixture truth: the preparation
family (≥ 2 members per class: GHZ-type, W-type, one
non-symmetric), each completion PINNED AND PRINTED; the leg
family; the scopes; every completion chosen by a DECLARED RULE
(lex-first within a declared property class).

## The axes (per #228, set by the verdicts)

- **A1 (XBA/GEN — the ord-sweep):** completions chosen by rule
  to hit ord([P,u]) ∈ {1, 2, 3, and the maximum the carrier
  admits} for at least one wing-pair symmetry P; the group
  family COMPUTED at each (does it stay dihedral, or does S₃'s
  non-abelianity open a larger family? — the
  QCD-reachability probe at its first honest scale).
- **A2 (THE ONE LAW at S₃):** the law's generalization TESTED,
  not assumed — with P ranging over S₃'s elements, is the
  geometry generated by the commutators [P, u(ψ)] for all
  P ∈ S₃ (the commutator SUBGROUP), and does the cocycle
  mechanism δ_P(X) = [P, X] still factor state/declaration?
  Pre-registered: TB3-ONE-LAW-⟨HOLDS-VERBATIM |
  GENERALIZES-⟨computed form⟩ | FAILS-⟨witness⟩⟩.
- **A3 (COC — level 2 native):** the triangle census on the
  NATIVE six-frame atlas; chart groups including
  non-centralising ones BY DESIGN; the centralizer criterion
  re-tested where triangles are not engineered.
- **A4 (PSI — the ψ-family):** GHZ vs W vs non-symmetric at
  fixed Born shadow where constructible; the E(ψ)-factor per
  wing-pair; witnesses gated by the computed-qualifier
  standard.
- **A5 (XBA's residual — the graph broken on purpose):** three
  wings change the admission graph STRUCTURALLY (the 24-cell
  agreement cannot transfer); the unit measures WHICH
  graph-level quantities survive the wing-count change and
  which were two-wing artifacts.

## Pre-registered outcomes (only these; per axis + unit level)

Per axis: the named forms above, each
⟨CONFIRMED|REFUTED|BLOCKED-AT-⟨object⟩⟩ with computed content.
Unit: **TB3-BASE-ESTABLISHED** (the base runs; all five axes
return verdicts) / **TB3-BLOCKED-AT-⟨object⟩**.  Variation is
discovery; ABSENT results reported plainly.

## Discipline

ALL engraved rules (§13 incl. verdict-in-gate w/ computed
qualifiers, verdict-flip + no-witness mutants,
cell-completeness, independent routes; §14 all addenda; §15
all addenda; declination precedent).  Terminal receipts
hash-pinned (COC/XBA/PSI/HA/GEN/NT where anchored).  Positive
and negative controls w/ teeth per axis.  Exact arithmetic;
byte-identical delivery runs; stall doctrine (this unit is
large — declared [SAMP] w/ sizes wherever needed, never
silent; axes may be scoped to the symmetric settings first w/
the scope printed).  NO git; FREEZE-ON-DELIVERY; deviations
ship.  Deliverables: v13/paper-tb3-third-base.md +
v13/code/tb3_third_base_exact.py + _output.txt +
_receipt.json.  Then §13: freeze → panel → adjudication.
Successor per the standing order: topology on whatever TB3
earns.
