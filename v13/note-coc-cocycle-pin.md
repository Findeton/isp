# COC — OVERLAPS AND THE COCYCLE QUESTION ON BASE G (PIN)

**Status:** PIN, STRICT, 2026-08-08.  **Immutable base:** commit
20b8df2 (GEN TERMINAL #222).  **Authorization:** the standing
long-horizon order (v13 #212).  Lean NONE.

## The question

The measured geometry so far is TWO-chart: loops between a pair
of frames (bigons).  Real atlases have many charts glued
pairwise; the next structural floor is TRIPLE consistency.  For
charts A, B, C with admitted pairwise identifications g_AB,
g_BC, g_CA: the composite δ = g_CA ∘ g_BC ∘ g_AB (the
triangle loop) is the COCYCLE DEFECT.  The question with new
content beyond NT/GEN: **is every triangle defect generated by
the bigon holonomies (the connection story complete at level
2), or does some triangle carry structure OUTSIDE the
bigon-generated group (a genuine higher obstruction — the
gerbe signature)?**

## The third chart (declared before fixture truth)

Base G currently carries two frames per setting.  The worker
DECLARES the triple structure from the base's own admitted
material — candidates (each declared w/ its admission rule
before any fixture value): a third leg-order via a declared
sub-leg splitting of a committed leg; a relabelled frame via a
declared admitted isomorphism; or the two gluing RULES (FULL /
REAL) as parallel atlases making (frame1-FULL, frame2,
frame1-REAL) a triple.  Whatever is declared: three pairwise-
identifiable charts at declared coordinates, each identification
admitted by a declared rule, matched coordinates throughout
(read time a declared coordinate per cell).

## Pre-registered outcomes (only these)

- **COC-COCYCLE-CLOSES** — every admissible triangle defect
  lies in the bigon-generated holonomy group (computed
  membership, exhaustive over the declared triple family);
  level-2 adds nothing: the connection is the whole story.
- **COC-HIGHER-OBSTRUCTION** — some triangle defect OUTSIDE
  the bigon-generated group: a new structural level, the
  defect computed and characterized (its group, its
  declaration-dependence via the GEN defect law if applicable).
- **COC-TRIANGLES-ABSENT** — no admissible triple exists on
  base G (measured, both rules swept): the question needs a
  richer base; reported plainly, successor design noted.
- **COC-BLOCKED-AT-⟨object⟩** — census discipline.

## Inherited anchors (exit-1 against the GEN/NT terminal receipts)

The dihedral family + defect law (GEN); the bigon holonomy
group at the pinned completion (Klein four, the involutive
class); the escape result; the admission tables.  The declared
completion is FIXED at GEN's pinned V (one arena; completion-
variation is NOT this unit's question — one sentence scoping
it).

## Discipline

ALL engraved lessons binding (§13; §14 every addendum — fresh
eval w/ exercised-cache gating, no mutant identity in gates,
independent comparators, analytically-forced = disclosure,
symmetry self-tests w/ sign/orientation mutants; §15 every
addendum — declared arena as data, match-every-coordinate;
permutation-tuple counting; counts computed; complete sweeps
or declared [SAMP]; positive + negative controls w/ teeth;
measurement-founded verdicts w/ flip-test; exact arithmetic;
byte-identical delivery runs; NO git; FREEZE-ON-DELIVERY;
deviations ship).  Deliverables: v13/paper-coc-cocycle.md +
v13/code/coc_cocycle_exact.py + _output.txt + _receipt.json.
Then §13: protocol freeze → three-lens panel → adjudication →
STATUS.  Successor per the standing order: topology (the
overlap graph's global structure) on whatever this unit
earns.
