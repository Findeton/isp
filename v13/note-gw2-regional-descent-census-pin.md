# v13 GW2 — REGIONAL DESCENT: THE STEP-0 CENSUS (PIN)

**Status:** PIN, STRICT, 2026-07-29.  **NOT DISPATCHED — no run
authorized; queued behind W6 (v12 paper 0 v2.2 §5's successor
order).**  **Binding:** GW1's terminal verdict and discipline (v13
LOG #5: the five-condition statement §1.1, the no-smuggling
definition §1.2, the typed-object vocabulary §1.3); v12 paper 0
v2.2 §5 (the four-gate rule; the successor order).  Lean NONE.

## The re-posed successor

Per [REV3] (adjudicated at v12 LOG #18), GW1's successor target is
RE-POSED: **regional compatibility, not global chart identity** —
local descriptions need agree only on shared physical records,
modulo declared gauge.  The H_a[N] construction (GW1's constructive
successor: v4 p7 Def 1.3 with the record-read metric and the seven
§5 co-requisites) is PRESERVED as the later branch, not replaced:

    W6 co-reference  →  GW2 regional descent  →  possible emergent
                                                 deformation law H_a[N].

## STEP 0 — the census (first, and possibly the whole unit)

No descent condition may be written until the census locates in the
committed corpus, or constructs with referent-gate discipline, ALL
seven objects — cite file:line for every located one:

1. a declared notion of finite region D;
2. a local process object P_D at its committed type (Γ_D, amplitude
   map, history law, effect algebra — whatever the corpus actually
   carries, at its actual type);
3. a stable-record algebra 𝒜_R(D) (W3′-grade records only);
4. an overlap/common-access object for a pair of regions D, E;
5. restriction/comparison maps into that overlap;
6. the gauge relation under which the two restrictions are
   compared;
7. a discriminator between genuine compatibility and inserting the
   desired identification (GW1's kill, inherited verbatim).

**"Z_D" is CANDIDATE NOTATION ONLY** — not a committed typed
object; introducing it as an instrument without the census fails
the referent gate.  Only after STEP 0 passes may the condition

    P_D |_{𝒜_R(D∩E)}  ~  P_E |_{𝒜_R(D∩E)}   (modulo the declared gauge)

be posed as GW2's target.  Object 4 DEPENDS on W6's co-reference
definitions — hence the queue position; do not dispatch before W6
lands.

**Pre-registered outcomes:** GW2-CENSUS-COMPLETE (all seven located
or constructed; the descent condition is posable) /
GW2-BLOCKED-AT-⟨object⟩ (the census is the deliverable — a
first-class outcome that scopes the successor queue, exactly as
GW1's was).

Receipt rules: GW1's wholesale — anchors exit-1-only on every
committed number reused; substantive negatives exit 0; no-smuggling
gates on every construction; typed objects throughout; caps and
substrate declared; stall doctrine (< 8 min silent, progress
prints).  Files (when eventually run): v13/code/gw2_*,
v13/note-gw2-regional-descent.md.  The v13 draft paper 0 remains
[DRAFT] and untouched by this pin.
