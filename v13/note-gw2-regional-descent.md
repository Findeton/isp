# v13 GW2 — regional descent: the step-0 referent census

**Status:** GREEN-UNREVIEWED, STRICT, 2026-07-30.  Exact delivery against
the committed pin at v13 LOG #7 and the programme checkpoint
`e89a2e7c972ce3ff6c3a9d4fe5792bf0dac621b2`.  This is a verified negative,
not a terminal result; no external hostile round has yet attacked it.

## 1. Verdict

$$
\boxed{\texttt{GW2-BLOCKED-AT-1}}
$$

The first missing object is **a finite region in the live Barandes/W3–W6
record lineage**.  The distinction is essential:

- W3 supplies a dynamical criterion for record status through H-corr and
  H-avail (`v12/note-w3p-records-kill-defect.md:46-77`).
- W6 supplies finite dynamic **charts**, chart-local stable-record algebras,
  derived chart-overlap maps and a co-reference discriminator
  (`v12/note-w6-record-coreference.md:67-93,122-185,213-240`).
- v10 supplies finite **regions** and regional kernels, but only on an older,
  supplied classical opportunity carrier.  It does not derive those regions
  from the Barandes/W3–W6 process and it does not carry the H-corr/H-avail
  interface.

Consequently v10 is a structural antecedent and negative control for the
shape of a future regional theory.  It is not evidence that the required
Barandes-derived regional base already exists.

The pin therefore forbids proceeding to a descent equation.  In particular,
`Z_D` remains candidate notation only, and the pin's regional descent
expression is not instantiated here.  There is not yet a live `D` on which
that expression could be typed.

## 2. Why the v10 region does not discharge object 1

The older regional programme is mathematically useful and exact.  D37 defines,
for an oriented finite causal region `D`, boundary data `B_D`, histories
`Omega_D`, a regional kernel `gamma_D`, and restriction/transport maps
`r_E,D` (`v10/note-d37-regional-history-specifications.md:54-78`).  D38 then
asks for record-closed regional kernels and append-only histories
(`v10/note-d38-record-closed-regional-specifications.md:62-89`).

But the carrier precedes those constructions.  Paper 26 begins from

```text
C = (V, E_conf, Part, parentLine, proposalType)
```

with structurally identified opportunity roles, participant identities and
causal lines already supplied.  The paper calls “supplied” load-bearing and
states that D37 does not derive the opportunity roles or their incidence
(`v10/relativistic-isp-v10-paper26-admissible-regional-history-specifications.md:215-234`).
D37 also states that it and Paper 26 remain classical and defer a quantum lift
(`v10/note-d37-regional-history-specifications.md:404-417`); D38b leaves
quantum joins open (`v10/note-d38-record-closed-regional-specifications.md:461-477`).

That is not merely a missing refinement.  It is the wrong input type for this
census.  A classical authenticated click history on a supplied carrier is not
automatically a W3-grade record algebra, and a finite subset of supplied roles
is not automatically a spacetime region derived from a Barandes process.
Promoting either by renaming would insert precisely the identification GW2 is
meant to test.

## 3. What the live Barandes/W3–W6 lineage actually supplies

W3 makes the record predicate dynamical.  H-corr requires live alternatives at
the writing cut to occupy distinct record sectors, while H-avail requires all
later predecessors of a configuration to remain within one record sector
(`v12/note-w3p-records-kill-defect.md:65-77`).

W6 packages declared legs, an initial configuration and record tokens into
finite charts.  Its record algebra $\mathcal R_a$ is the finite Boolean algebra
of actual positive-probability token-value tuples, with occurrence and
availability computed at the W3 interfaces
(`v12/note-w6-record-coreference.md:67-120`).  It then derives:

- an overlap object by enumerating frame isomorphisms, not by assuming shared
  identities (`v12/note-w6-record-coreference.md:122-165`);
- token and Boolean-algebra comparison maps (`v12/note-w6-record-coreference.md:167-185`);
- configuration relabelling as its declared gauge
  (`v12/note-w6-record-coreference.md:213-222`);
- the five-valued ABSENT/FORCED/UNDERDETERMINED/VACUOUS/NO-INSTRUMENT
  discriminator (`v12/note-w6-record-coreference.md:224-240`).

These are chart-to-chart objects.  Neither W6 nor Paper 2 constructs a
spacetime-region functor, a region-indexed local process, or restriction maps
between region-indexed W3 record algebras.  Paper 2 expressly limits itself to
declared finite stochastic models and descent data, does not itself derive
token stability, and does not construct a universal event set
(`v12/paper2-record-coreference.md:64-66`).  Its dynamic chart is a tuple
$(X,T,x_0,\mathcal T)$, not a claimed spacetime region
(`v12/paper2-record-coreference.md:72-85`).

W6's handover sentence that “the base is built” must therefore be read at its
declared level: it has built the **chart-local co-reference base**—record
algebras, derived chart overlap, preservation filters and a descent solver
(`v12/note-w6-record-coreference.md:735-754`).  It has not thereby built RQ0's
regional causal site.  The present census corrects that possible over-reading.

## 4. Seven-object census

| # | Required object | Live Barandes/W3–W6 status | v10 antecedent | Census result |
|---:|---|---|---|---|
| 1 | finite region $D$ | W3/W6 exports `Chart`, not a spacetime region | finite regions of a supplied classical opportunity carrier | **BLOCKED** |
| 2 | local process $P_D$ | no live $D$ on which to index it | `gamma_D` / `Gamma_D` at the old classical type | **NOT REACHED** |
| 3 | W3-grade $\mathcal A_R(D)$ | $\mathcal R_a$ exists chart-locally, not region-indexed | durable typed clicks, with no H-corr/H-avail adapter | **SPLIT** |
| 4 | physical overlap/common access | derived chart overlap only | set overlap inherits supplied carrier identities | **SPLIT** |
| 5 | restriction/comparison maps | chart token/algebra maps, not maps between regional record algebras | `r_E,D` at the old type | **SPLIT** |
| 6 | declared gauge | configuration relabelling of charts | construction-order gauge | **SPLIT** |
| 7 | insertion discriminator | W6's five-valued discriminator applies | old labels furnish the negative case, not an identity rule | **LOCATED** |

The pre-registered rule stops at the first missing referent, hence
`GW2-BLOCKED-AT-1`, not `GW2-CENSUS-COMPLETE` and not a later-numbered block.
The `SPLIT` rows record useful halves; they do not repair object 1.

## 5. Four gates for the next construction

This unit constructs no replacement region.  A successor pin may introduce
one only by satisfying all four programme gates in writing:

1. **Referent.** Give an explicit finite-region constructor from committed
   W3/W6/Barandes process primitives, or label the regional site as a new
   postulate.  A setting, dynamic chart, supplied opportunity role or global
   event label is not a derived spacetime region merely by being renamed $D$.
2. **Necessity.** Show which RQ0 operation requires the object: at minimum the
   indexing of a local process and stable-record algebra, physical overlap,
   causal/spacelike comparison and refinement.  Chart co-reference alone does
   not provide spacetime locality.
3. **No-smuggling.** The constructor may not import v10's global role
   identities, a Minkowski region, a fitted field cone, or the overlap map it
   later claims to derive.  Its permitted Barandes/W3–W6 data must be frozen
   before regional comparisons are evaluated.
4. **Discriminator.** Exhibit a positive case in which the constructor yields
   $D$, $P_D$ and $\mathcal A_R(D)$ with a nonvacuous restriction, and a
   negative case in which a v10-style supplied label or a bare W6 chart fails.
   The receipt must distinguish derived, postulated, underdetermined and
   vacuous regional identity.

The correctly scoped successor is therefore a pinned **Barandes
regionalization / RQ0a** unit: construct the smallest regional site or
groupoid on which W3/W6 records can actually be restricted.  It is not yet a
field reconstruction and not a return to gravitational dynamics.  No such
successor is authorized or started by this note.

## 6. Exact receipt

Run:

```bash
PYTHONPYCACHEPREFIX=/tmp/isp-gw2-pycache \
  /opt/homebrew/bin/python3.13 v13/code/gw2_step0_census.py
```

The receipt locks the pin and ten antecedent files, verifies that programme
checkpoint `e89a2e7c972ce3ff6c3a9d4fe5792bf0dac621b2` is an ancestor, re-runs
the committed W3 (`129/129`), W6 (`120/120`), v10 D37 (`9/9`) and v10 D38b
(`9/9`) receipts, audits their Python interfaces, checks the explicit scope
sentences cited above, and searches the audited committed Python corpus for a
file co-locating the old regional and W3/W6 interfaces.  It finds none.

The exact result is:

```text
29 checks: 29 pass, 0 fail
GW2-BLOCKED-AT-1
```

Two complete normal runs are byte-identical and exit `0`.  The deliberate
mutant falsely asserts that a live `Region` class exists; it produces exactly
one failed check (`28 pass, 1 fail`) and exits `1`.  The committed stdout is
`v13/code/gw2_step0_output.txt`.

## 7. Scope and non-claims

This result is an exact census of the pinned interfaces and their committed
provenance at the audited checkpoint.  It does not prove that no
Barandes-derived notion of region can be constructed.  It proves that the
required object is not presently supplied by the audited W3/W6 interface and
that v10 cannot be promoted into it without an explicit adapter or postulate.
It does not test a descent law, causal cone, dimension, metric, scalar field,
Dirac field, stress tensor, Einstein equation or backreaction.  Those gates
remain downstream in the user-ordered RQ0 → RQ1 → GR1 sequence.
