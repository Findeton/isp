# XBA — THE CROSS-BASE AGREEMENT HUNT (PIN)

**Status:** PIN, STRICT, 2026-08-08.  **Immutable base:** commit
1426984.  **Authorization:** the standing order (#212); the
user's parallelization order 2026-08-08.  Lean NONE.

## The question (the OPEN lead of NT/GEN, promoted)

The two bases' holonomy class-count profiles agree
(82/86/90/106) though only ≈2.5% of Klein-four connections on
the common gauge-fixed graph reproduce that profile, and the
admission tables agree 24/24 cells.  WHAT SHARED STRUCTURE
FORCES IT?

## The instrument (candidate properties FROZEN before fixture
## truth — the branch-C corridor discipline)

On the common gauge-fixed graph (from R1-GEN's construction,
rebuilt independently): the space of Klein-four connections
(4,096).  A DECLARED family of candidate shared properties,
each a computable predicate on connections, frozen with its
gate before any profile is computed — at minimum: (C1) the
source-split property (identification links carry W, leg
loops generate the second generator); (C2) equivariance under
the graph's automorphism group (computed); (C3) the
per-setting admission pattern (the 24-cell table as a
constraint); (C4) worker candidates, declared before fixture
truth.  For each candidate: does the constraint-satisfying
subset force the profile (all members reproduce it), and do
BOTH realized connections satisfy it (membership computed)?

## Pre-registered outcomes (only these)

- **XBA-SHARED-STRUCTURE-IDENTIFIED** — a declared property
  both realized connections satisfy, whose satisfaction FORCES
  the profile (exhaustive over the connection space); the
  agreement explained, the property named.
- **XBA-PARTIAL** — properties found that both satisfy and
  that shrink the profile space (factors computed) without
  forcing it — the residual gap stated.
- **XBA-COINCIDENCE-NOT-EXCLUDED** — no declared candidate
  forces the profile: the agreement stays open, the candidate
  family's failure recorded honestly.
- **XBA-BLOCKED-AT-⟨object⟩** — census discipline.

## Discipline

All engraved lessons binding.  The gauge-fixing rebuilt
independently (not imported from the frozen review — the
review's numbers as external anchors, hash-pinned).  Positive
control: the two realized connections themselves must
reproduce the profile.  Negative control w/ teeth: a random
(declared-seed-free, constructed) connection violating each
candidate must break the profile.  Deliverables:
v13/paper-xba-crossbase.md + v13/code/xba_crossbase_exact.py +
_output.txt + _receipt.json.  Then §13 freeze → panel →
adjudication.
