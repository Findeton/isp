# EXTERNAL HOSTILE REVIEW — FINAL RQ0-L0 ADDRESSABILITY REPAIR

## Executive verdict

\[
\boxed{\texttt{REJECT}}
\]

Do not terminalize `RQ0-LOCAL-ATLAS` or `RQ0-LOCALIZATION-GROUPOID`.

The load-bearing finite mathematics is mostly correct and independently reproducible. In particular, the delivered \(S_3^3\) process really has the reported exact direct-product factorization, W3 seams, Boolean overlap incidence, ambiguity count, bridge discrimination and phase count.

The positive rung nevertheless fails the frozen pin for three independent reasons:

1. The supposedly unseen final fixture reuses the already opened #35 \(S_3^3\) quotient-composition object, factor truth and Boolean-atlas truth.
2. The estimator does not enforce independently selectable operations when accepting an address factor.
3. The delivered `RegAddr`/`Rec` types do not implement the full instrument and projector-algebra maps required by the pin.

The receipt also contains non-native “mutant” branches and duplicated evidence.

The exact surviving result is:

> A finite, fully accessible \(S_3^3\) amplitude representation has a faithful \((6,6,6)\) direct-product decomposition, three internally derived W3 record seams, and a six-object record-bearing proper-Boolean-product incidence structure.

That is strong calibration evidence, but it is not yet a terminal pre-registered L0 rung.

Do not begin T1, C1, causal reconstruction, fields or gravity.

---

## Immutable targets reviewed

| Target | Exact commit |
|---|---|
| Strict repair pin | `ffbcce6d597cbdc8bdb38ba8430ad92af347e629` |
| First runtime-invalid fixture | `39fb6b3dc635b8e02fa450dcf132093a0dce528a` |
| Estimator v2 freeze | `36290ef12a3fdf1e1ed88c1c87901fa6cce80859` |
| Second runtime-invalid fixture | `3a6cdedb008b68fda72af2e1ae5fb1000477c960` |
| Final estimator v3 freeze | `208a35473edf109dd4014cded33e3b7b22b0d6e3` |
| Final green delivery | `9d58eb0400cc4c282106e025cce7696a16e01716` |

The ancestry chain was independently verified:

\[
\texttt{ffbcce6}
\prec
\texttt{39fb6b3}
\prec
\texttt{36290ef}
\prec
\texttt{3a6cded}
\prec
\texttt{208a354}
\prec
\texttt{9d58eb0}.
\]

### Independently computed source hashes

| File | SHA-256 |
|---|---|
| Repair pin | `8135a0c9f358c86384d0d248a7cbf99131095656b5e61c06c8d49d0f62175fe3` |
| Frozen estimator v3 | `7c8571eac81692eeb87058b1162bf1b874179e93b3d11bc204f264877465b0e1` |
| Final fixture | `6b45e0671a16ea966634693a457ef090ae9ca15d0daabccee577ad4f6d527589` |
| Final scorer | `685971fa4dfa2bd418b3d0893c4074dd0a85fb1497c65b3d8639453e0b8da6f6` |
| Delivery note | `40333cd02a47e427293594958a34fa3faaf900052a53fe49ad1ac0c1c67db3a2` |
| Text receipt | `6613cac5ec531163b197ce5a07122739b07218b2fd57876412adb83454c853b7` |
| JSON receipt | `8ebefb2a11bd82fc19f21d9bbfabe91e32ceee0f9de3549a3bee19cd572154f1` |
| Retired #33 fixture | `fb5b671bd1264122563867f71a1fbeaf1ca0616f7628702d35f6e9eba011cea6` |
| Retired #35 fixture | `107b7dc7d8d5b5f51020da5f3fc071631e1bd8930c3bb1dcf1d86a28665e18de` |

The estimator blob is byte-identical at `208a354` and `9d58eb0`. It imports no fixture or scorer and contains no main-fixture partition or expected atlas.

---

## Methods and independent scratch work

Two separate scratch programs were used.

| Scratch file | Purpose | SHA-256 |
|---|---|---|
| `/tmp/rq0_l0_independent_rebuild.py` | Independent standard-library exact rebuild; imports no repository module | `8afcd412f6cf5ae6c26a9c8f978dd37072d0a816a6407f62346ec51b10a7e7f8` |
| `/tmp/rq0_l0_delivery_stress.py` | Explicit hostile mutants against the delivered API | `431a248d6a6dd63f78d5ba41acc93838aa6529abb7ee00b6627bd599caa437c9` |

The independent rebuild implemented afresh:

- \(\mathbb Q(\zeta_{24})\) as eight rational coefficients modulo
  \(\Phi_{24}(x)=x^8-x^4+1\);
- exact matrices and Gaussian spans;
- the three published \(S_3\) representations;
- the \(S_3^3\) composition table;
- all normal operational subobjects and direct factors;
- W3 occurrence, availability and erasing coherence;
- the proper-product atlas;
- \(V_4\) ambiguity arrows;
- \(V_4/C_4\) bridge searches.

It imported nothing from `v13/code`.

One diagnostic run was stopped at 7 minutes 50 seconds during an avoidably repeated phase calculation. Exact finite phase powers were then cached, and the complete rebuild finished in approximately 3 minutes 15 seconds. Total review computation remained below the 30-minute cap.

The official executable was also run separately. It returned exit 0 and reproduced `54/54`. A second official run wrote only to `/tmp`; its generated text and JSON hashes exactly matched the stored receipts.

No repository file was modified.

---

## Independent numbers

| Quantity | Delivery | Independent rebuild | Result |
|---|---:|---:|---|
| \( \zeta_{24}^{24}=1 \) | true | true | confirmed |
| \( \sqrt2^2=2 \) | true | true | confirmed |
| \( \sqrt3^2=3 \) | true | true | confirmed |
| Raw operation classes | 216 | 216 | confirmed |
| Quotient classes | 216 | 216 | confirmed |
| Typed composition rows | 46,656 | 46,656 | confirmed |
| Implemented main rows | 46,656 | 46,656 | confirmed |
| Independent composition-table checksum | not reported | `d8906d3cfb1cb18a1a4df0f9a2307dc98ad6ea07a867da1a705bbdb5e418302a` | new audit datum |
| Distinct amplitude signatures | 216 | 216 | confirmed |
| Normal operational subobjects | 38 | 38 | confirmed |
| Candidate factor tests | 19 | 19 | confirmed |
| Finest factorizations | 1 | 1 | confirmed |
| Factor orders | `(6,6,6)` | `(6,6,6)` | confirmed |
| Factor-algebra dimensions | `(4,4,4)` | `(4,4,4)` | confirmed |
| Ambient algebra dimension | 64 | 64 | confirmed |
| Direct-product image | 216 | 216 | confirmed |
| Direct-product collision | none | none | confirmed |
| W3 records | 3 positive | 3 positive | confirmed |
| Erasing coherence per record | 2 | 2 | confirmed |
| Record-bearing regions | 6 | 6 | confirmed |
| Algebra inclusions | 12 | 12 | confirmed |
| Pair overlaps | 9 | 9 | confirmed |
| Triple overlaps | 3 | 3 | confirmed |
| Universal atom core | empty | empty | confirmed |
| Remove-one-record result | 5 objects, not closed | 5 objects, not closed | confirmed structurally |
| Ambiguity factorizations | 3 | 3 | confirmed |
| Ambiguity arrows | 72 | 72 | confirmed |
| Ambiguity source/target pairs | all 9 | all 9 | confirmed |
| Positive structural bridges | 24 | 24 | confirmed |
| Record-compatible bridges | 1 | 1 | confirmed |
| \(V_4\to C_4\) bridges | 0 | 0 | confirmed |
| Physical-signature changes | 180/216 | 180/216 | confirmed |
| Stored receipt checks | 54/54 | official rerun 54/54 | reproduced |

All substantive source paths contain zero float literals and no NumPy or tolerance-based tests.

---

# Ranked findings

## FATAL F1 — The final fixture is not genuinely unseen at the composition-object level

The final estimator v3 was frozen after the #35 fixture had already exposed:

- the \(S_3^3\) group;
- its 216-element composition object;
- its six generators;
- the expected \((6,6,6)\) factorization;
- the six proper Boolean-product regions;
- the expected nine pair and three triple overlaps.

The final #37 fixture changes:

- the three local amplitude representations;
- the global encoding and finite phases;
- opaque handles;
- the phase-control implementation.

It does not change the abstract composition object that the addressability search factorizes.

The diff from #35 to #37 is only 41 insertions and 50 deletions. Independent AST comparison gives identical hashes in both commits for:

- `s3_elements`;
- `triple_elements`;
- `triple_multiply`;
- `MainTruth`.

The strict pin requires a genuinely unseen fixture whenever the estimator changes after truth is opened. The #36 ledger narrows this to “new encoding/amplitude contents,” but an append-only ledger cannot weaken the frozen pin silently.

This is especially consequential because v3’s factor search reads the quotient composition law directly. New carrier encoding does not make an already opened group decomposition a new addressability challenge.

### Required replacement sentence

> The final delivery changes the already opened #35 \(S_3^3\) fixture’s amplitude representations, global encoding, phases and handles; its abstract 216-class \(S_3^3\) composition law, factor truth and Boolean-atlas truth were already opened at #35. It is therefore not a genuinely unseen composition-object fixture relative to estimator v3.

---

## FATAL F2 — Independently selectable operations are not enforced

`OperationClass` stores `independently_selectable`, but the direct-factor search never reads it.

I constructed a new exact mutant from the public \(S_3\times C_2\) calibration:

- all six operations with \(C_2\) bit zero remain independently selectable;
- every bit-one operation is marked composite-only;
- the generator surface is reduced to the two selectable \(S_3\) generators.

The \(C_2\) address therefore has no independently selectable nonidentity operation.

Observed result:

- selectable operations: 6;
- finest factorizations: 2;
- returned factor orders: `(2,6)` and `(2,6)`;
- `blocked_at_address = false`.

Thus the estimator still returns the unavailable \(C_2\) factor.

This violates pin §5.1 and mandatory access discrimination. It also shows that the result is derived from the abstract total group law, not from the independently selectable intervention family.

The main fixture happens to mark all 216 group elements independently selectable, so its direct-product existence result survives. What fails is the estimator’s pinned status as an addressability discriminator and the mandatory negative-control coverage.

### Required replacement sentence

> At one exact \(S_3^3\) fixture whose access table postulates every group operation as independently selectable, the estimator reconstructs a faithful \((6,6,6)\) direct-product decomposition. The current estimator does not enforce independent selectability when testing arbitrary candidate factors.

---

## FATAL F3 — Full `RegAddr` and `Rec` maps are not constructed

The pin requires each `RegAddr` arrow to include executable maps on:

- operational classes;
- composite rows;
- represented algebra bases;
- preparations;
- contexts;
- readouts.

The delivered `AlgebraInclusion` has only:

```text
source_atoms
target_atoms
source_dimension
target_dimension
columns
```

The linear algebra map is real and its coordinate composition is checked. But there is no map on operational classes or composition rows and no preparation/context/readout compatibility object.

Likewise, `RecordPullback` has only:

```text
larger_atoms
smaller_atoms
record_maps
```

`build_record_pullback` accepts a record when the larger interface contains the same handle with the identical projector tuple. It does not construct a handle-independent Boolean/projector-algebra homomorphism.

A new presentation mutant with exactly identical projectors but renamed record handles was rejected:

```text
record projector is not an exact pullback
```

Therefore fact restriction remains partly handle-dependent.

The twisted control also reverses one projector family and is rejected while constructing one pair map. It does not construct pairwise-valid maps whose triple loop fails, as required by the pinned twisted-triple test.

These are not merely prose omissions. Pin kill condition 6 explicitly excludes handle/incidence representations in place of executable maps.

### Surviving result

Exact algebra-coordinate inclusions and literal shared-projector compatibility are earned.

### Required replacement sentences

> The delivery constructs exact linear inclusions among represented algebras. It does not yet construct the pin-required maps on operational classes and composition rows or preparation/context/readout compatibility for `RegAddr`.

> In the globally shared projector presentation of this fixture, identical record handles and identical projector tuples restrict coherently along subset inclusions. A handle-independent projector-algebra functor has not yet been constructed.

---

## MAJOR F4 — Receipt mutation tests are prewritten responses, not end-to-end mutants

The ordinary resolver is fail-closed in this implementation:

- every positive outcome requires `all_pass`;
- an exception is caught and emits `RQ0-L0-INVALID`;
- no scientific outcome exits 1.

But four advertised mutation tests do not mutate the full receipt.

`--mutate-anchor`, `--mutate-science` and `--no-outcome` directly print hard-coded invalid JSON and exit 1. They never alter an observed anchor or scientific check and rerun the prerequisite graph. The malformed mode does call one validator, but still bypasses the full receipt. Only `--negative-address` executes a substantive estimator path.

Consequently these rows demonstrate that dedicated invalid-response branches return invalid responses. They do not demonstrate that a real corrupted full receipt is suppressed.

There is also duplicated evidence:

- `A5-public-calibrations` aggregates 15 public checks;
- controls `C18`–`C22` recount booleans already included in A5;
- `S1-main-positive` aggregates atlas properties then separately counted again in `S10`–`S14`.

`C5-handle-branch-mutant-detected` does not run a contaminated handle-branch implementation. It merely observes that the two dataset handles differ and both normal runs succeed.

### Required replacement sentence

> The main receipt deterministically regenerates and its resolver is fail-closed when a measured check is false. The advertised quick invalid modes are prewritten invalid-response branches rather than end-to-end mutations of the full receipt.

---

## MAJOR F5 — The ambiguity control is recordless

The \(V_4\) control genuinely produces:

- three finest factorizations;
- 72 derived carrier-relabelled arrows;
- every one of the nine source/target pairs.

But it has zero record candidates.

Direct inspection gives:

- atlas object counts `(0,0,0)`;
- `has_positive_localization = false`.

The main fixture has one unique factorization and discharges its groupoid prerequisite with the identity arrow. Thus the receipt does not demonstrate that a genuinely ambiguous, record-bearing localization can earn `RQ0-LOCALIZATION-GROUPOID`.

### Required replacement sentence

> The recordless \(V_4\) control reconstructs three finest algebraic factorizations and 72 carrier-relabelled arrows. It does not instantiate a record-bearing localization groupoid.

---

## MAJOR F6 — The collapse control does not measure a multiplication collision

In both the public and main collapse controls, the selected result class is unchanged. Only the row status changes from `IMPLEMENTED` to `COLLAPSED`.

For the public calibration:

- changed rows: 1;
- positive and collapsed row have the same result;
- positive finest factorizations: 2;
- collapsed finest factorizations: 0.

The estimator rejects the dataset because `total_implemented` becomes false. It returns before testing direct-product multiplication faithfulness.

For the main fixture the same mechanism occurs in two commuting-order rows.

Therefore the delivery distinguishes a postulated `COLLAPSED` status, which is legitimate, but it does not measure a new multiplication-map collision or show that “faithfulness fails.”

### Required replacement sentence

> The collapse control changes the selected row statuses from `IMPLEMENTED` to `COLLAPSED` while leaving their result classes unchanged. The estimator blocks every non-total composition object before running the direct-product multiplication-faithfulness test.

---

## MAJOR F7 — `Comp_D` is narrower than the pinned exact type

The pin gives each row the type

\[
(a,b,\tau,s,c,M),
\]

including an exact composite amplitude law \(M\).

The implemented `CompositionRow` stores:

```text
left
right
context
status
result
```

For implemented and collapsed rows, the estimator recomputes the matrix product from the two input operations and checks its accessible finite-phase signature against the result class. This is substantive and all main rows pass.

It is nevertheless a narrower, matrix-product-derived first rung. It cannot represent a separately supplied operational composite amplitude law that is not identified with the raw matrix product.

### Required replacement sentence

> `CompositionRow` stores context, status and result. For implemented or collapsed rows the executable recomputes the matrix product and checks its accessible signature against the result class; it does not store an independent row-level amplitude law \(M\).

---

## MINOR F8 — “Non-star atlas” needs Boolean-lattice scope at the claim

The overlap incidence is genuinely computed after recovering the factors; it is not copied from held-out truth. Higher intersections are computed directly from atom-set meets rather than clique completion.

But `build_atlas` automatically constructs every nonempty proper subset of the three factors. The six-region incidence is therefore the complete proper Boolean-product lattice:

- three atoms;
- three two-factor products;
- nine nonempty pair meets;
- three nonempty triple meets;
- empty intersection across all six objects.

This is a valid non-star discriminator. It is not a selectively learned or irregular overlap nerve.

### Required replacement sentence

> The recovered three-factor composition object induces all six nonempty proper Boolean products. Their subset-meet incidence has nine nonempty pair meets, three triple meets and empty universal atom intersection.

---

## MINOR F9 — Gauge scope should distinguish two finite phase actions

The executable uses:

- global \(\mu_{24}\) phase normalization for operation signatures;
- configuration relabelling plus diagonal \(\mu_8\) phases in the full gauge-conjugacy fixture.

The receipt describes the whole gauge as “finite \(\mu_{24}\) boundary phase.” That conflates the two.

### Required replacement sentence

> The exact implemented scope uses global \(\mu_{24}\) normalization of operation signatures and tests full-dataset conjugacy under a carrier permutation with diagonal \(\mu_8\) phases. No full \(U(1)\) gauge is implemented.

---

## NOTE N1 — The core algebraic result survives

The independent rebuild found no counterexample to:

- exact \(\mathbb Q(\zeta_{24})\) arithmetic;
- 216 distinct main operation classes;
- all 46,656 implemented product rows;
- 38 normal subobjects;
- one finest faithful \((6,6,6)\) direct factorization;
- factor algebra dimensions `(4,4,4)`;
- scalar factor-algebra intersections;
- commuting factors;
- injective 216-element product image;
- exact order-three and nonabelian content.

This part is strong.

## NOTE N2 — W3 and bridge results survive

All three W3 rows independently reproduce:

\[
(\mathrm{occ},\mathrm{pres},\mathrm{erase},C,\mathrm{no\ write})
=
(\mathrm{true},(\mathrm{true}),(\mathrm{false}),(2),\mathrm{false}).
\]

The 24-permutation bridge searches also survive exactly:

- positive \(V_4\) pair: 24 structural bridges;
- complete record witness preserved by exactly one;
- equal-law \(V_4/C_4\): zero bridges.

The bridge criterion is structural and does not consult marginal-law equality.

## NOTE N3 — Exactness and official reproducibility pass

- No floats or tolerances occur in substantive paths.
- No NumPy occurs.
- The optimized phase implementation retains its exact reference comparison.
- Every composition row is matrix-multiplied before signature-cache lookup.
- A fresh official receipt regeneration exactly reproduced both stored hashes.

## NOTE N4 — The strong access postulate is honestly disclosed

The main access object supplies:

- full exact amplitude tomography;
- all 216 group operations as independently selectable;
- all 46,656 ordered product rows;
- three frozen record candidates.

The composition object therefore contains enough structure to expose its direct-product group factors. This is a legitimate first existence model, not a derivation of addressability from one bare unitary and not spatial locality.

The note generally discloses this limitation correctly.

---

# Claims earned separately

| Claim | Assessment |
|---|---|
| Exact quotient composition object | **Earned** for the published \(S_3^3\) fixture |
| Composition congruence | **Earned but vacuous on the alias-free main fixture**; substantive public alias controls pass |
| Faithful direct factorization | **Earned** |
| Generic operational addressability discriminator | **Not earned**; independent-selection mutant passes falsely |
| Three W3 seams | **Earned** |
| Six distinct record-bearing proper products | **Earned** |
| Full `RegAddr` maps | **Not earned**; only algebra-coordinate inclusions exist |
| Exact `FactIface` objects | **Partially earned** as projector tuples |
| Contravariant `Rec` functor | **Not earned at strict pin type**; handle-dependent identity-projector restrictions only |
| Pair/triple incidence | **Earned** as a proper Boolean-product lattice |
| Non-star discriminator | **Earned at that Boolean scope** |
| Genuine ambiguity of algebraic factorization | **Earned** |
| Record-bearing localization groupoid | **Not earned** |
| Bridge/no-bridge discrimination | **Earned over exhaustive \(S_4\) carrier permutations** |
| Gauge conjugacy | **Earned at finite permutation/diagonal-\(\mu_8\) scope** |
| Physical phase discrimination | **Earned: 180/216 signatures** |
| Fail-closed ordinary resolver | **Source-audit positive** |
| Advertised receipt mutants | **Not earned as end-to-end mutants** |
| Genuinely unseen final composition fixture | **Not earned** |
| Spatial, causal or relativistic locality | **Explicit nonclaim; not constructed** |

---

# Classification audit

## Definitions

Correctly introduced:

- finite quotient composition object;
- normal operational subobject;
- faithful direct product;
- record-bearing proper product;
- finite ambiguity arrows.

Mistyped or incomplete relative to the pin:

- `CompositionRow` lacks an independent \(M\);
- `AlgebraInclusion` is not the full required `RegAddr` arrow;
- `RecordPullback` is handle-dependent and not a general projector-algebra homomorphism.

## Postulates

Correctly disclosed:

- exact amplitude tomography;
- availability of the typed composition table;
- admitted preparations, interventions, probes and record candidates;
- finite phase and carrier-relabelling gauge scope.

The postulate is strong enough to encode the full finite group law and operational access. No spatial or causal data are inserted.

## Exact finite theorems or verified consequences

Secure:

- field identities;
- representation laws;
- direct-product factorization;
- algebra dimensions and intersections;
- W3 decisions;
- Boolean-product incidence;
- \(V_4\) ambiguity counts;
- bridge searches.

Overstated:

- generic independent addressability;
- full regional instrument maps;
- general `Rec` functor;
- multiplication collision in the collapse control;
- unseen-fixture status.

## Measurements

The reported class, row, subgroup, factor, algebra, region, overlap, ambiguity, bridge and phase numbers are correct.

## Conjectures

The note properly leaves open:

- scaling;
- robustness under changed access contracts;
- generic emergence;
- spatial or causal interpretation.

---

# Required repair

1. Freeze a new estimator before a genuinely new abstract composition fixture—not another representation or encoding of \(S_3^3\).

2. Enforce independent selection natively. A factor must be generated by independently selectable classes, and a composite-only factor must be rejected.

3. Implement the exact pinned row type or explicitly forward-pin a narrower replacement. If \(M\) remains derived from matrix multiplication, say so and test that exact scope.

4. Construct full `RegAddr` arrows with maps on:

   - operation classes;
   - composition rows;
   - represented algebras;
   - preparations;
   - contexts;
   - readouts.

5. Make record maps invariant under presentation-handle renaming. Construct projector/Boolean-algebra homomorphisms, composition, and a pairwise-valid twisted triple whose loop fails.

6. Give the ambiguity fixture W3 records on every counted object and overlap, or stop using it to discharge a record-bearing groupoid obligation.

7. Replace the collapse status-only control with a genuine declared multiplication-collapse witness, or narrow the claim to status discrimination.

8. Replace quick invalid branches with end-to-end receipt mutations that alter a real anchor/check and pass through the ordinary resolver.

9. Remove aggregate/duplicate receipt evidence and add native gates for:

   - independent-selection compliance;
   - full regional maps;
   - handle-independent `Rec`;
   - genuine unseen-composition provenance.

10. Keep T1 and C1 closed until the repaired L0 result survives another external round.

---

# Terminal-rung recommendation

\[
\boxed{\text{NO TERMINAL SCIENTIFIC L0 RUNG}}
\]

I do not recommend `RQ0-L0-BLOCKED-AT-ADDRESS`, because the fully accessible main fixture genuinely contains exact direct factors. The failure is certification, typing and anti-tailoring, not proof that no addressable model exists.

The correct current treatment is:

- preserve `9d58eb0` as immutable green historical evidence;
- reject terminalization;
- retain the exact direct-factor, W3, Boolean-incidence, ambiguity and bridge numbers as calibration results;
- run a forward-only repair with a genuinely new composition fixture.

Verbatim terminal-status sentence:

> `RQ0-LOCAL-ATLAS` is not terminalized. The exact \(S_3^3\) direct-factor, W3 record-seam and proper-Boolean-product measurements survive, but the final fixture was not unseen at the quotient-composition level, independent selectability is not enforced, and the pin-required regional and record maps are incomplete. No successor unit is authorized.
