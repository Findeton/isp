# RQ0-L0 CYCLE B — THE TASK–RECORD FIXED POINT (PIN)

**Status:** PIN, STRICT, 2026-08-02.  **Immutable base:** commit
6c2d7b8 (the #103 minimal-boundary and #111 operational-classical-core
artifacts and their reviews are read-only inputs).  **Authorization:**
the user's standing order (v13 #113, ROADMAP stage 3).  **Scope:**
finite, one-boundary, task-de-smuggling ONLY — no composition/tensor
claim (stage 4's), no locality, overlap, topology, causality, QFT or
gravity vocabulary.  Lean NONE.

## The question

The #111 core is relative to a chosen future task family 𝔉, and #103's
adjudication proved task selection can manufacture a desired PVM as
the resulting center.  Cycle B asks: **can the record and its
preserving future family determine each other**, so that a W3-grade
boundary is a *fixed point* rather than a task-relative artifact?

Target closure, up to operational equivalence:
    M ≃ Core(Pres(M)),
with both sides independently physical (complete admitted futures on
one side, complete nondisturbing repeatable classical instruments on
the other).

## G0 — THE DEFINITION GATE (first; the pin's known repair)

[REV5]'s literal formula Pres(M) = {F : ‖F − F∘D_M‖_Test = 0} must be
TESTED FOR COLLAPSE before use: for the earned exactly-nondisturbing
classical instruments (Σ_r m_r = id, m_r m_s = δ_rs m_r), exhibit
whether every admitted D_M-composite is the identity, making
Pres(M) = ALL futures and the closure degenerate.  If it collapses
(prove it, gate it), adopt and justify the AVAILABILITY form:

    Pres(M) = { admitted F : M's outcome remains recoverable after F —
                an admitted readout M′ exists whose flag-joint tester
                statistics reproduce M's record after F },

the operational H-avail (cite the v12 record hypotheses as
antecedent).  Gate its nondegeneracy: at least one admitted F inside,
at least one admitted F outside, on the earned fixtures.

## G1 — THE GALOIS GATES (before any fixed-point claim)

Prove or refute, as gates: Pres is antitone in instrument refinement;
Core is antitone in future-family inclusion; the pair forms a Galois
connection; hence Core∘Pres and Pres∘Core are closure operators and
fixed points are exactly closed elements.  Use the standard machinery
explicitly; do not hand-roll.

## G2 — FIXED POINTS

- Exhibit the TRIVIAL fixed point (the one-outcome instrument) and
  prove it is always fixed — the degeneracy the theorem must exceed.
- Characterize NONTRIVIAL fixed points on the earned finite scope;
  prove existence or nonexistence on the committed fixture set.
- **THE DE-SMUGGLING DISCRIMINATOR (mandatory):** the #103
  adjudication's manufactured-PVM example (the task matched to a
  preselected PVM) must NOT be a nontrivial fixed point — the
  closure must reject exactly the smuggled record.  If it survives,
  that is a finding, not a failure to hide.

## G3 — CONTROLS (carried forward, all mandatory)

1. The branch-memory triple with its corrected minima ℂ, ℂ⁵, M₄⊕ℂ:
   report which (if any) instruments are fixed; the ℂ⁵ common-sink
   task minimum must not be promoted to a W3 seam by the closure.
2. The hostile operator system S ⊂ M₂⊕M₂: trivial core ⟹ only the
   trivial fixed point; gate it.
3. Ordinary finite C*-controls: where central projections are
   physically readable (#111's sense), the fixed points must recover
   them; where not admitted, must not.
4. A no-write control and at least one admitted continuation outside
   Pres(M) with positive tester-visible recovery (the [REV5]
   stability requirements), each gated with positive and negative
   directions.

## Pre-registered outcomes (only these)

- RQ0-L0-W3-OPERATIONAL-MARKOV-BOUNDARY — nontrivial fixed points
  exist, characterized, smuggling rejected ("Markov" = operational
  screening-off inside one boundary; NO spacetime meaning).
- RQ0-L0-NO-NONTRIVIAL-FIXED-POINT — the closure admits only the
  trivial instrument at the earned scope (first-class; scopes stage 4).
- RQ0-L0-BLOCKED-AT-⟨object⟩ — a required object cannot be typed
  (referent-gate discipline; census is the deliverable).

## Deliverables and discipline

Paper: v13/paper-rq0-task-record-fixed-point.md (RQ0 house form:
scope box, non-claims, every number receipt-backed).  Code:
v13/code/rq0_l0_fixed_point_*.py + _output.txt + receipt JSON —
exact arithmetic only, anchors exit-1-only against #103/#111
committed values, substantive negatives exit 0, falsification
self-test, determinism, stall doctrine (< 8 min silent, ≤ ~45 min).
Then per RUNBOOK §13: protocol freeze BEFORE review dispatch;
three-lens panel (operator-system / effectus-order /
sufficiency-instrument); SHA-pinned frozen review files; joint
adjudication; STATUS.md updated in the same commit; next-obstruction
named; successor authorization = the ROADMAP's standing order.
