# LCB — THE LIVE-CELL BRIDGE AT THE STRENGTHENED STANDARD

**Status:** `GREEN-UNREVIEWED` — re-derivation delivery under
`v13/note-lcb-livecell-pin.md` (STRICT), not citable before its own external
hostile round.
**Pin:** `v13/note-lcb-livecell-pin.md`, frozen, sha256
`4f30880229e7…`, commit `d2f6104`.
**Binding:** BRG's terminal S1–S6 registry (its §2.6) verbatim; RUNBOOK §13
(verdict-in-gate with computed qualifiers, verdict-flip and no-witness mutants,
cell-completeness, genuinely independent routes), §14 (symmetry self-tests, all
addenda) and §15 (declared-arena discipline, all addenda).
**Sources, hash-pinned and anchored exit-1:** `v13/paper-brg-bridge.md` +
`brg_bridge_receipt.json`; `ha_successor_receipt.json`;
`gen_generality_receipt.json`; `xba_crossbase_receipt.json`;
`psi_curvature_receipt.json`.
**Verdicts:**
**`LCB-BRIDGE-EMPTY-AT-STRENGTHENED-STANDARD-UNIVERSAL-FOR-THIS-SQUARE`** — the
census is empty at every cell of the covariant twelve-cell family, in both
directions, at every declared prime and at every cell of the (prime,
defect-order) grid, and the operative obstruction is measured to cover every one
of those cells; and, separately, **`LCB-PRIME-DECLARED`** at the declared
nine-label arena, with **`LCB-PRIME-DERIVED`** measured at the sixteen-label
scale and reported beside it.
**Deliverables:** this note; `v13/code/lcb_livecell_exact.py`,
`v13/code/lcb_livecell_output.txt`, `v13/code/lcb_livecell_receipt.json`.

---

## Scope box

This unit does exactly one thing: it builds an ACTUAL pairing — a transport base
whose completion has $\mathrm{ord}(D) = 5$ against HA's own deformation arena
$C_{HA}(5)$ — and runs BRG's registered strengthened standard S1–S6 on it, with
S1's encoding intertwining as a **commuting square of measured matrices**. It is
a census at a declared finite scope, not a construction: nothing here builds a
spacetime, derives a field equation, or identifies any two of $\Delta^B$,
$\Omega_{\text{hypersurface}}$, $R^\rho{}_{\sigma\mu\nu}$.

**No claim is made that the two structures are unrelated.** What is measured
EMPTY is the set of candidate morphisms satisfying the full declared clause list,
at twelve covariant encoding cells, at seven declared primes, over the whole
declared (prime, defect-order) grid. What is measured NON-EMPTY, and reported
rather than counted, is the census at the **partial** clause list: 48 candidates
in each of six of the twelve encoding cells, every one of them rejected by a
single named clause at 100 of 125 record cells.

**The word UNIVERSAL in the verdict name is bounded and the bound is measured.**
The outcome is the pin's pre-registered
`LCB-BRIDGE-EMPTY-AT-STRENGTHENED-STANDARD` carrying an EARNED qualifier, not a
new outcome: the qualifier is entered only if the census is empty at every cell
of the covariant family, in both directions, at every declared prime and at every
grid cell, AND the obstruction is measured to cover every one of those cells; if
any of that fails the unqualified name is returned, and the qualifier's own
negative case is evaluated in the same gate. It says: at this square — this
transport encoding against this deformation re-encoding — the obstruction covers
every cell of the declared covariant family at every declared prime. It does
**not** say the obstruction is arena-free everywhere: it is arena-free at eight
of the twelve cells and arena-carried at the other four, and §4b measures which
is which.

Everything below is at ONE declared finite arena, set out as data in §2.

---

## 1. The question, and what the pin required

BRG measured `BRG-EMPTY-AT-CARRIER` at the committed carriers and registered, in
its §2.6, the standard a live cell would have to meet before the word *bridge*
is earned — S1–S6 — and recorded (its X11) that **no cell of any scope was
evaluated against it, in either direction**. Its §14 open 1 handed the successor
a dichotomy and the arithmetic to start from: 4,608 of the 40,320 declared
completions have $\mathrm{ord}(D) = 5$, and the smallest of them moves 3 labels.

The pin asks for the real test at the first arithmetically-possible carrier, and
for Open 1 to be addressed as a separate verdict. Where each requirement is
discharged:

| requirement | discharged |
|---|---|
| the pairing as data, gate order proving the freeze | §2; gates G01, G04, G05, G06, G09 |
| the live-cell condition verified, both group structures computed | §3; gate G07 |
| S1 as a commuting square of measured matrices, census not expression | §4, §5; gates G10, G11, G13 |
| the cell family swept COVARIANTLY under the admitted symmetry | §2.4, §4.2; gates G09, G10, G11, G27 |
| the operative obstruction identified and measured, not named by assertion | §4b; gates G35, G36, G37, G38, G39, G40 |
| S2–S6 per the registry | §6–§9; gates G14, G15, G16, G17, G18, G19, G20 |
| held-out predictive verification, on the pairing's own deformation side | §8; gates G18, G19, G41 |
| both outcomes reachable, mutants proving it | §10; gates G22, G23 |
| positive control with teeth, negative control with teeth | §10; gates G22, G24 |
| Open 1, measured not argued, at both declared scales | §12; gates G43, G44, G45, G29 |
| the verdicts derived in gate from computed qualifiers | §13; gates G46, G30, G31 |

---

## 2. The pairing as data

Everything in this section is **declaration**, recorded before any candidate is
evaluated. The instrument measures its candidate-evaluation counter to be
**zero** at the freeze point (G01), and the `freeze-lax` mutant, which evaluates
one candidate first, dies there.

### 2.1 The arena (RUNBOOK §15, declared as data)

| coordinate | declaration |
|---|---|
| **boundary** | one PAIRING. Transport side: base G's carrier (81 = the system pair $\times$ the pointer pair, 9 labels each), a completion $Q_0$ of the 9 system-pair labels fixing label 0, its defect $D = \delta(Q_0) = \Sigma Q_0^{\mathsf T}\Sigma Q_0$ (GEN §8.1), and the group $\langle W, D\rangle$ with $W = \Sigma\otimes\Sigma$. Deformation side: $C_{HA}(5) = \mathbb F_5^{k}\times\mathbb F_5^{d}$, $k = d = 2$, with $\langle R_{HH}\rangle\cong\mathbb Z/5$ acting by translation of the address register by $\rho \bmod 5$. **Encoding layer:** the record datum space $V = \mathbb F_p^{3}$ (the three link counts at a site) with HA's record$\leftrightarrow$metric readout $E$, and the completion group $G_C$ with GEN's completion$\to$commutator map $\delta$. |
| **family** | the COVARIANT ENCODING-CELL FAMILY = the orbit of the declared identifications under the metric-slot relabelling group $S_3$ (all $3! = 6$ slot identifications, the two declared ones among them) $\times$ 2 directions = **12 cells**, the count computed from the declared metric slot triple and never typed; 7 declared primes; 8 measured defect-order classes; the 4,608-member ord-5 completion class; HA's 9 admissible geometry records, **all of them swept at the base-point clause**, and its 2 declared negative controls; the dimensions $d = 2,3,4,5$ at the fixed-space sweep |
| **law** | S1–S6 as BRG's §2.6 registers them, operationalised clause by clause in §2.5; a candidate morphism is a map $\alpha : V\to G_C$ |
| **state** | the declared base geometry record $r_0 = $ `G-FLAT`'s count triple $(1,1,2)$; the declared base completion $Q_0$; the transport side's initial configuration $j_0 = 0$ |
| **arena action** | the metric-slot relabelling group $S_3$, whose orbit on the declared identifications is the covariant cell family; the DIRECTION sweep (the orientation convention RUNBOOK §14 requires to be swept); relabelling of the 9 completion labels by a permutation fixing label 0; change of basis of $V$ by an element of $GL_3(\mathbb F_p)$; the prime sweep; the choice of completion inside the declared ord-5 class; the choice of BASE RECORD among HA's 9 admissible records. **These seven are THIS UNIT'S OWN declared choices, and they are the ones the Open-1 declaration-freeness criterion measures invariance under.** The nine-label completion arena is INHERITED from GEN/BRG, not chosen here; its effect is not tested by re-declaration but reported separately, at both scales (§12) |
| **provenance** | BRG (terminal), HA, GEN, XBA, PSI — receipts and papers hash-pinned, every reused number read from them and recomputed here |
| **admission** | a candidate is ACCEPTED only if every clause of S1 holds and S2, S3 hold; the clause that fails FIRST in the declared order is the named obstruction of that cell |

### 2.2 The transport base, and the rule that selects it

**The selection rule, declared before evaluation:** $Q_0$ is the
**lexicographically first one-line notation** $(q_0,\dots,q_8)$ among all
permutations of the nine system-pair labels with $q_0 = 0$ whose defect
$\delta(Q) = \Sigma Q^{\mathsf T}\Sigma Q$ has order exactly 5. The class size is
computed by exhaustive sweep of the whole 40,320-member family, never typed.

| measured | value |
|---|---|
| the declared completion family | **40,320** members, both spectra summing to the swept count (G03) |
| the class $\mathrm{ord}(D) = 5$ | **4,608** members (anchored against BRG G38) |
| $Q_0$ | $[0,1,2,3,4,5,7,8,6]$ — and it **moves exactly 3 labels**, so the declared lex-first rule lands on BRG's own "smallest moves just 3 labels" witness |
| $D = \delta(Q_0)$ | $[0,1,8,3,4,2,7,5,6]$ |
| $\mathrm{ord}(D)$ / fixed configurations of 81 | **5** / **36** (GEN's class table's own ord-5 row) |
| $\lvert\langle W,D\rangle\rvert$ | **10** $= 2\,\mathrm{ord}(D)$, built as an explicit permutation group on the 81 configurations |
| element orders of $\langle W,D\rangle$ | $[1,2,2,2,2,2,5,5,5,5]$ |
| the dihedral form | $W^2 = 1$, $WDW = D^{-1}$, $W\notin\langle D\rangle$ — **measured, not cited** (G05) |

`select-lax`, which replaces the declared rule by an arbitrary member of the
class, dies at G04; `group-lax`, which closes under one generator only, dies at
G05.

### 2.3 The deformation arena $C_{HA}(5)$

HA's own construction at $p = 5$, rebuilt from its law (HA G29): the generator is
the translation of the address register by $\rho\bmod p$ with the front sector
fixed.

| measured | value |
|---|---|
| carrier $p^{k+d}$ | **625** |
| the register shift, read off the generator | $(1,1)$, and the same at every one of the 625 configurations |
| $\lvert\langle R_{HH}\rangle\rvert$ | **5** |
| orbit lengths / orbit count | $\{5\}$ / **125** — the action is measured free by an orbit walk |

`anchor-rho`, which perturbs the exact-to-$\mathbb F_p$ reduction, dies at the
shift anchor; `arena-lax`, which builds a non-free action of the same order, dies
at G06.

### 2.4 The encoding layer, and the covariant twelve cells

**The transport side's encoding** is GEN's defect law itself,
$\delta(Q) = \Sigma Q^{\mathsf T}\Sigma Q$. Written with $\sigma = $ conjugation
by $\Sigma$ it is the $\sigma$-twisted cocycle map

$$\delta(Q) \;=\; \sigma(Q)^{-1}\,Q ,\qquad
\delta(QQ') = \sigma(Q')^{-1}\delta(Q)\,Q' ,\qquad
\delta(\sigma(Q)) = \delta(Q)^{-1} .$$

Two properties of that map carry the whole unit, and both are measured, not
cited: it is label-exchange **ANTI-equivariant** (the last identity), and — §4b —
it has **exactly one fixed point**.

**The deformation side's encoding** is HA's readout, rebuilt exactly as
`ha_successor_exact.py` builds it — rows the declared links in HA's own sorted
order, columns HA's own `sym_index` — with determinant **2** exactly (HA G28,
rebuilt and re-measured here at G08).
That matrix is the coefficient matrix of the linear system $q_{ij}e^i e^j = n_\ell$,
so it runs $q\to$counts (X03). And $q$ reproduces every declared link count at
every one of the 11 declared records (9 admissible, 2 negative controls, all
determinants reproduced against HA's table).

$$q_{11} = n_{e_1},\qquad q_{22} = n_{e_2},\qquad
q_{12} = \tfrac12\bigl(n_{e_1+e_2} - n_{e_1} - n_{e_2}\bigr).$$

**Why the cell family is what it is, and why it is COVARIANT.** HA's datum is
*one datum in two coordinate systems*, so the readout is an endomorphism of one
space only after (i) an **identification** of the two coordinate triples and
(ii) a **direction**. Both are declarations, and both are swept — the direction
because HA's prose and HA's code place the determinant-2 matrix on opposite sides
of the arrow (X03), the identification because it is a **naming of the metric's
three slots**. RUNBOOK §15 makes the second consequence binding: a naming is an
arena action, so the declared identifications must be swept together with their
**whole orbit** under the slot-relabelling group $S_3$. That orbit is all $3!$
slot orders — the two declared ones, their chart-involution conjugates, and two
more that complete the orbit — and it is built here from the same declared metric
slot triple, adding nothing to the declaration. Twelve cells, the count computed
(G09); `cell-orbit-drop`, which truncates the family back to the two declared
identifications, and `encoding-drop`, which drops one cell, both die.

| identification | metric slot order | direction | $\det$ | $E \bmod 5$ | $\dim\mathrm{fix}(E)$ at $p=5$ | primes with $2\in\mathrm{spec}$, counts$\to q$ |
|---|---|---|---|---|---|---|
| natural | $(q_{11},q_{22},q_{12})$ | counts$\to q$ | $1/2$ | $[[1,0,0],[0,1,0],[2,2,3]]$ | 2 | $\{3\}$ |
| natural | | $q\to$counts | $2$ | $[[1,0,0],[0,1,0],[1,1,2]]$ | 2 | — |
| tau-natural | $(q_{22},q_{11},q_{12})$ | counts$\to q$ | $-1/2$ | $[[0,1,0],[1,0,0],[2,2,3]]$ | 1 | $\{3\}$ |
| tau-natural | | $q\to$counts | $-2$ | $[[0,1,0],[1,0,0],[1,1,2]]$ | 1 | — |
| index | $(q_{11},q_{12},q_{22})$ | counts$\to q$ | $-1/2$ | $[[1,0,0],[2,2,3],[0,1,0]]$ | 1 | $\{3\}$ |
| index | | $q\to$counts | $-2$ | $[[1,0,0],[0,0,1],[1,2,1]]$ | 1 | — |
| **tau-index** | $(q_{22},q_{12},q_{11})$ | counts$\to q$ | $1/2$ | $[[0,1,0],[2,2,3],[1,0,0]]$ | **0** | $\mathbf{\{3,7\}}$ |
| tau-index | | $q\to$counts | $2$ | $[[0,0,1],[1,0,0],[1,2,1]]$ | **0** | — |
| **alt** | $(q_{12},q_{11},q_{22})$ | counts$\to q$ | $1/2$ | $[[2,2,3],[1,0,0],[0,1,0]]$ | **0** | $\mathbf{\{3,7\}}$ |
| alt | | $q\to$counts | $2$ | $[[0,1,0],[0,0,1],[2,1,1]]$ | **0** | — |
| tau-alt | $(q_{12},q_{22},q_{11})$ | counts$\to q$ | $-1/2$ | $[[2,2,3],[0,1,0],[1,0,0]]$ | 1 | $\{3\}$ |
| tau-alt | | $q\to$counts | $-2$ | $[[0,0,1],[0,1,0],[2,1,1]]$ | 1 | — |

The **natural** identification pairs each link with the metric component it
determines; the **index** identification pairs the two triples slot by slot in
index order; `tau-` names the chart-involution conjugate of a cell and `alt`
the remaining orbit member. Two measurements taken here are used repeatedly
below. First, **exactly one identification and its conjugate carry the chart
involution**: in the natural identification the axis swap induces the same slot
permutation on counts and on the metric, in the index one it does not (G09) —
four of the twelve cells are chart-equivariant. Second, **the two identifications
whose slot order is a 3-cycle of the declared triple — `tau-index` and `alt` —
have a trivial fixed space and admit $2$ in the spectrum at $p = 7$ as well as at
$p = 3$**; the other four have a nonzero fixed space and admit 2 only at $p = 3$.
Those two facts are the same fact: a 3-cycle slot order puts primitive cube roots
of unity in the spectrum in place of the eigenvalue 1. `tau-lax` dies at G09.

The **registered** direction is counts$\to q$ — data $\to$ geometry — because
that is the direction that matches the transport side's declaration $\to$
curvature map $Q\mapsto\delta(Q)$.

### 2.5 The strengthened standard, operationalised

BRG's S1–S6, clause by clause, declared as data before any candidate is
evaluated.

| id | as operationalised here |
|---|---|
| **S1a** | $\delta(\alpha(r)) = \alpha(E r)$ at **every** record cell, compared as $9\times 9$ permutation MATRICES, entry by entry |
| **S1b** | $\alpha$ is a homomorphism of the record datum's additive structure into the completion group, at every ordered pair |
| **S1c** | $\alpha$ carries the chart involution to the label exchange: $\alpha(\tau r) = \Sigma\,\alpha(r)\,\Sigma$ at every record cell |
| **S1d** | BASE POINT: the declared base record $r_0$ goes to a completion whose defect reproduces the declared base completion's own measured order **and** fixed-configuration count |
| **S2** | (i) the census DETERMINES a candidate (size 1 up to the declared redundancy); (ii) the transport side's fixed-configuration stratification $\{9,18,27,36,45,54,81\}$ is CARRIED |
| **S3** | $\alpha$ is INJECTIVE — the form BRG's one-way theorem forces, since a FOUND could only exhibit the deformation side as a **sub-object** of the transport side, and a sub-object embedding is an injection |
| **S4** | the census over the whole declared (prime, defect-order) grid and over a base change inside the ord-5 completion class |
| **S5** | a declared FIT/HELD split frozen before any fitting; the square VERIFIED on HELD; two transport-side physical quantities predicted out of sample; two extensions declared IN ADVANCE to fail |
| **S6** | the prime declared a parameter, per-prime verdicts, and the intersection over the declared sweep |

**S1a, S1b, S1c and S1d are not all BRG's.** BRG's S1 registers the commuting
square and the two encodings. **S1a is BRG's**; S1b, S1c and S1d are this unit's
additions, declared as such in deviation 2, and §5 reports the census clause by
clause so that a reader who rejects any of them can read the alternative off the
table. §4b then removes the question's force: the verdict holds at
**S1a $\wedge$ S3 alone** — two clauses BRG registered — at every cell.

### 2.6 The held-out set, declared before any candidate is fitted

| id | quantity |
|---|---|
| split rule | **FIT** $=$ the single declared basis record $e_1 = (1,0,0)$; **HELD** $=$ every other element of $V$. Sizes computed. |
| fit rule | a candidate $(g,\lambda)$ is admitted if the square holds at the FIT cell **alone**; nothing on HELD is consulted |
| H1 | the square at every HELD cell, as $9\times 9$ permutation matrices |
| H2 | the **defect permutation** $\delta(\alpha(r))$ itself, entry by entry, at every HELD cell — a transport-side physical quantity, and the strictly stronger of the two readings (it implies the order) |
| H3 | $\mathrm{fix}_{81}(\delta(\alpha(r)))$ at every HELD cell — GEN's own stratification |
| chart map | the **pairing's own** deformation side, in the direction whose census is non-empty; the synthetic control map is run beside it and reported in a dedicated disclosure gate (§8, G41) |
| teeth (declared IN ADVANCE to fail) | **X-NOSQUARE**: predict that the defect map acts trivially, $\delta(\alpha(r)) = \alpha(r)$. **X-FLATSTRAT**: predict the identity-defect stratum $\mathrm{fix}_{81} = 81$ everywhere |

### 2.7 The controls, declared as data

| id | control |
|---|---|
| **F0-IDENT** | **DISCLOSURE, NOT A CONTROL** (RUNBOOK §14 addendum #208). With $\alpha$ the identity self-morphism every clause of the check reads $x = x$, for any $\delta$ and any comparison predicate; its 0 violations at all 40,320 cells record that the identity candidate is the identity and nothing about the square. Registered as an analytically-forced disclosure gate, carrying no mutant |
| **SYNTH-COMPATIBLE** | **THE POSITIVE CONTROL OF THIS UNIT.** The same transport side against a synthetic chart map whose 2-eigencovector is chart-ANTIsymmetric. The machinery must FIND candidates and they must pass S1a, S1b, S1c and S1d. **Its base-point clause is evaluated at the declared $\tau$-asymmetric record `G-ANISO`, and the reason is measured, not conventional:** S1c forces the exponent covector to be chart-antisymmetric, an antisymmetric covector annihilates any $\tau$-fixed record, and the declared base record `G-FLAT` is $\tau$-fixed — so S1c and S1d are jointly unsatisfiable at `G-FLAT` for **every** chart map whatsoever (§5, G42). The base record the control uses is recorded in its own gate detail |
| **SYNTH-EMPTY** | a declared synthetic incompatible pair: a scalar chart map with no eigenvalue 2. The machinery must report EMPTY |
| **BREAK-HOM** | NEGATIVE WITH TEETH: the accepted candidate's exponent $\lambda(r)$ replaced by $\lambda(r)\mu(r)^{p-1}$ with $\mu$ a declared 1-eigencovector. It satisfies the commuting square at EVERY cell — differing from an ACCEPTED candidate only in the linearity of its exponent — and must be rejected by **S1b alone** |

---

## 3. The pairing's basic compatibility, gated

The live-cell condition $p \mid 2\,\mathrm{ord}(D)$ is verified as a measurement
with its own negative case evaluated in gate: $5 \mid 10$ holds and $7 \mid 10$
is measured to fail against the same group, so a blinded clause cannot pass. The
homomorphism count is computed twice — by sweeping the element orders of the
built permutation group, and by $\gcd(\mathrm{ord}(D), p)$ — and the two agree:
$\lvert\mathrm{hom}(\mathbb Z/5, \langle W,D\rangle)\rvert = 5$, **4**
non-trivial, which is exactly BRG's own scope-2 entry for the cell
$(p = 5,\ \mathrm{ord}(D) = 5)$ (G07, anchor A17). `compat-lax` dies there.

---

## 4. S1: the commuting square, censused

### 4.1 The census, and its two routes

A candidate is a map $\alpha : V \to G_C$ from the record datum space to the
completion group, and the square

$$\begin{array}{ccc}
V & \xrightarrow{\;E\;} & V\\[2pt]
\downarrow{\scriptstyle\alpha} & & \downarrow{\scriptstyle\alpha}\\[2pt]
G_C & \xrightarrow{\;\delta\;} & G_C
\end{array}
\qquad\qquad \delta\circ\alpha \;=\; \alpha\circ E$$

is evaluated **as measured matrices**, entry by entry, at every one of the
$p^3 = 125$ record cells. The candidate space is enumerated, never solved for by
one expression, and the count is computed by two genuinely independent
computations:

- **Route B (exhaustive enumeration, no group theory).** Every homomorphism
  $(V,+)\to G_C$ is enumerated by pairwise-commuting basis images — the
  candidate set for each slot is the full set of completions of order dividing
  $p$, and commutation is tested, not assumed. **41,665** homomorphisms are
  enumerated, and their image-size spectrum is measured
  $\{1{:}1,\ 5{:}41{,}664\}$. Each is then run through the matrix square at every
  record cell.
- **Route A (linear algebra).** The kernel of $E^{\mathsf T} - 2I$ over
  $\mathbb F_p$ is solved by Gaussian elimination, and its nonzero covectors are
  multiplied by the measured count of label-exchange-anti-invariant elements of
  order $p$, divided by the declared redundancy.

Neither route reads the other: the taint counter is measured **0** and both
invocation counters are nonzero. The enumeration's **own size** is checked
against an independent count built from the measured order-$p$ element count
alone — the cyclic subgroups of order $p$ number (elements)$/(p-1) = 336$, each
carrying $p^3 - 1 = 124$ surjections, plus the one trivial map, giving
$\mathbf{41{,}665}$ — so a dropped basis image cannot pass silently (G11).
`route-alias`, `route-a-lax`, `square-lax`, `matrix-lax`, `enum-drop` and
`hom-lax` all die at G10, G11 or G24.

### 4.2 The census table, over the covariant family

| identification | counts$\to q$ (route A / route B / $\dim\ker(E^{\mathsf T}\!-2I)$) | $q\to$counts (route A / route B / $\dim\ker$) |
|---|---|---|
| natural | **0** / **0** / 0 | 48 / 48 / 1 |
| tau-natural | **0** / **0** / 0 | 48 / 48 / 1 |
| index | **0** / **0** / 0 | 48 / 48 / 1 |
| tau-index | **0** / **0** / 0 | 48 / 48 / 1 |
| alt | **0** / **0** / 0 | 48 / 48 / 1 |
| tau-alt | **0** / **0** / 0 | 48 / 48 / 1 |

$$\boxed{\text{S1a}+\text{S1b at the REGISTERED direction: } \mathbf{0}
\ \text{candidates, at all six identifications, at } p = 5.}$$

The direction pattern is uniform over the covariant family at the declared prime.
It is **not** uniform over primes: at $p = 7$ the registered direction is live at
the two identifications whose spectrum admits 2 there (§2.4, §4b), and that live
cell is exhibited rather than left as arithmetic (G38).

### 4.3 What the square forces at a live cell — measured, not cited

Every step below is a measurement in the run, not a citation:

1. **The image is cyclic of order 1 or $p$.** The completion group's order is
   $8! = 40{,}320$ and its $p$-part is computed by exact division to be $p^1$;
   independently, route B's own enumeration measures the image-size spectrum of
   **all 41,665** homomorphisms to be $\{1, 5\}$.
2. **The sign is forced to $-1$.** The square forces $\Sigma g\Sigma = g^{c}$
   with $c^2 \equiv 1$; the branch $c = +1$ requires an order-$p$ element
   commuting with the label exchange, and the count of those is measured **0**
   (against **48** anti-invariant ones).
3. **So the completion$\to$commutator map acts as SQUARING.** On the
   anti-invariant locus $\delta(Q) = \sigma(Q)^{-1}Q = Q^2$, verified element by
   element at **40 declared cells** — the first 8 of the 48 anti-invariant
   generators against all 5 of their powers, a named subset, not a draw.
4. **Hence the square reads $\lambda\circ E = 2\lambda$**, so a candidate exists
   at a cell only if $2\in\mathrm{spec}(E)$ there.

`theorem-lax`, which asserts the sign step instead of measuring it, dies at G12.

**What this is, and what it is not.** It is a per-cell diagnostic: it explains
which cells are empty at S1a and which are not, and it is measured coextensive
with that emptiness (G31). It is **not** the operative obstruction. The prime it
picks is identification-relative — $\{3\}$ at four identifications, $\{3,7\}$ at
`tau-index` and `alt` (X10) — and at $p = 7$ it is satisfiable, with a witness
exhibited in §4b. The wall that stands at every cell, including that one, is the
subject of the next section.

---

## 4b. The operative obstruction: THE FIXED-POINT MISMATCH

This is the section the verdict rests on, and it needs no census.

### 4b.1 The transport side's encoding has exactly one fixed point

$$\delta(x) = x \iff \sigma(x)^{-1}x = x \iff \sigma(x) = e \iff x = e ,$$

because $\sigma$ — conjugation by $\Sigma$ — is injective. There is **no
hypothesis on the arena** in that line. Measured at three arenas (G35):

| arena | members | $\lvert\mathrm{fix}(\delta)\rvert$ | the fixed point is the identity |
|---|---|---|---|
| 4 labels | 6 | **1** | yes |
| 9 labels (this pairing) | 40,320 | **1** | yes |
| 16 labels (inside G15's own order-125 witness subgroup) | 125 | **1** | yes |

`fixpoint-lax`, which reads the fixed-point set off the wrong law, dies there.

### 4b.2 The deformation side's re-encoding fixes a nonzero vector — at eight of the twelve cells

$\dim\ker(E - I)$ is solved over $\mathbb F_p$ at every cell and every declared
prime — **84 (cell, prime) measurements, by two routes, 0 disagreements** (G36).
It is nonzero at every declared prime at **8 of the 12 cells**: both directions
of `natural`, `tau-natural`, `index` and `tau-alt`. At the four cells of
`tau-index` and `alt` it is **zero** at every declared prime, for the reason §2.4
measures: their slot order is a 3-cycle, so the re-encoding carries primitive
cube roots of unity where the others carry 1.

The same measurement at general $d$, from HA's own §9 readout (G36), all eight
rows nonzero:

| $d$ | slot ordering | size | $\dim\mathrm{fix}(E)$ |
|---|---|---|---|
| 2 | natural | $3\times3$ | 2 |
| 2 | lex (`sym_index`) | $3\times3$ | 1 |
| 3 | natural | $6\times6$ | 3 |
| 3 | lex | $6\times6$ | 1 |
| 4 | natural | $10\times10$ | 4 |
| 4 | lex | $10\times10$ | 1 |
| 5 | natural | $15\times15$ | 5 |
| 5 | lex | $15\times15$ | 1 |

`fixspace-lax` and `dsweep-lax` die at G36.

### 4b.3 The mismatch, and what it forbids

The commuting square forces $\alpha(\mathrm{fix}\,E)\subseteq
\mathrm{fix}\,\delta = \{e\}$. So wherever $\dim\ker(E-I)\ge 1$, **every**
candidate satisfying S1a collapses at least $p \ge 3$ distinct record cells onto
the identity and cannot be injective:

> **S1a (BRG's registered commuting square) and S3 (BRG's registered injectivity
> horn) are jointly unsatisfiable at every arena, every prime, every $d$, both
> directions and every identification whose re-encoding fixes a nonzero vector.**
> No S1b, no S1c, no S1d, no cardinality argument, no census.

That is the **FIXED-POINT MISMATCH**, and it is the unit's operative obstruction.
It is measured live, not only argued: at `natural`$\vert q\to$counts — a cell
whose census is non-empty — all **25 of 25** records in $\mathrm{fix}(E)$ are
carried to the identity by every surviving candidate, whose image is 5 of 125
(G37).

**Where it does not reach, the arena does.** At the four cells with trivial fixed
space the mismatch is silent, and there the emptiness is carried by the arena's
own $p$-part ($p^1$ against $\lvert V\rvert = p^3$), which is arena-RELATIVE.
Coverage, gated (G37):

| branch | (cell, prime) pairs covered |
|---|---|
| arena-free: $\dim\ker(E-I)\ge 1$, so S1a $\wedge$ S3 is impossible | **56** |
| the arena's own $p$-part alone | **28** |
| **uncovered** | **0** of **84** |

That measured split is what the verdict qualifier
`UNIVERSAL-FOR-THIS-SQUARE` means and all it means (X11); `universal-lax`, which
asserts the qualifier instead of measuring it, dies at G30.

### 4b.4 The registered direction at $p = 7$, exhibited

Two cells of the covariant family have a live registered direction at an
**admissible** prime — `tau-index`$\vert$counts$\to q$ and
`alt`$\vert$counts$\to q$, both at $p = 7$, where 96 anti-invariant order-7
elements exist. The one that is the chart-involution conjugate of a declared
identification is built and run explicitly, and the row is reported as a
**diagnostic**, not as a FOUND (G38):

| quantity | measured |
|---|---|
| the generator | $g = [0,2,4,5,6,7,3,1,8]$, order **7**, $\Sigma$-anti-invariant |
| the 2-eigencovector | $\lambda = (3,4,1)$ |
| S1a violations | **0 of 343** |
| S1b violations | **0 of 117,649** |
| S1c violations | **294 of 343** |
| $\lambda(r_0)$ at the declared base record | **2** $\ne 0$ |
| $\mathrm{ord}(\delta(\alpha(r_0)))$ / $\mathrm{fix}_{81}$ | **7** / **18** |
| S1d against the declared ord-5 transport base | **fails** |
| S1d against an ord-7 transport base (class size **9,216**) | **passes** |
| $\dim\mathrm{fix}(E)$ at this cell | **0** — the arena-free branch is silent here |
| $\lvert\mathrm{image}\rvert$ | **7** of 343 |

So at that cell the census is non-empty at S1a and S1b, its base-point clause is
satisfiable against a transport base of matching order, the clause that empties it
is S1c, and S3 is failed by the arena's $p$-part rather than by the fixed-point
mismatch. `witness7-lax`, which perturbs the witness's exponent covector, dies
there.

### 4b.5 Two supporting measurements

**Why the geometry$\to$data direction is live at every identification and dies at
the same clause every time** (G39). In that direction every column of the readout
sums to **2** at **6 of 6** identifications, so the all-ones covector is a
2-eigencovector at every one of them; and the all-ones covector is
$\tau$-**symmetric**, while S1c demands $\tau$-antisymmetric. The property is
special to $d = 2$: at $d \ge 3$ the column sums are measured not all equal to 2.
`colsum-lax` dies there.

**What S1b carries** (G40). S1b — the homomorphism clause — is an addition of
this unit (deviation 2), and its load is measured rather than argued. Read as
BRG registers S1, the square is a **Set**-level statement ($\delta$ is a twisted
cocycle, not a homomorphism, so the square lives in no category of groups or
$\mathbb F_p$-modules), and the set-level census at the registered direction is
**not** empty. Enumerated exactly from the measured $E$-orbit spectrum
$\{1{:}25,\ 4{:}25\}$ and the measured $\delta^L$ fixed counts
$\{1{:}1,\ 4{:}189\}$:

$$1^{25}\cdot 189^{25} \;=\; 815{,}727{,}501{,}386{,}186{,}588{,}190{,}430{,}395{,}382{,}342{,}267{,}781{,}393{,}000{,}859{,}545{,}374{,}749
\;\approx\; 8.157\times10^{56}.$$

The constant-identity map alone is a witness. So the boxed **0** of §4.2 is
produced by S1b, and that is now visible rather than implicit — and it is why
§4b.3, which needs neither S1b nor S1c nor S1d, is the load-bearing result.
`setlevel-lax`, which types the per-orbit factor, dies at G40.

---

## 5. S1c and S1d

| identification | direction | S1a+S1b | S1c pass | S1d pass | **S1 (all clauses)** | S1c violations per candidate |
|---|---|---|---|---|---|---|
| natural | counts$\to q$ | 0 | 0 | 0 | **0** | — |
| natural | $q\to$counts | 48 | **0** | 48 | **0** | **100 of 125** |
| tau-natural | counts$\to q$ | 0 | 0 | 0 | **0** | — |
| tau-natural | $q\to$counts | 48 | **0** | 48 | **0** | **100 of 125** |
| index | counts$\to q$ | 0 | 0 | 0 | **0** | — |
| index | $q\to$counts | 48 | **0** | 48 | **0** | **100 of 125** |
| tau-index | counts$\to q$ | 0 | 0 | 0 | **0** | — |
| tau-index | $q\to$counts | 48 | **0** | 48 | **0** | **100 of 125** |
| alt | counts$\to q$ | 0 | 0 | 0 | **0** | — |
| alt | $q\to$counts | 48 | **0** | 48 | **0** | **100 of 125** |
| tau-alt | counts$\to q$ | 0 | 0 | 0 | **0** | — |
| tau-alt | $q\to$counts | 48 | **0** | 48 | **0** | **100 of 125** |

At the declared prime the two directions fail at **different clauses**, uniformly
over the covariant family. In the registered direction the census is already
empty at S1a: 2 is not in the spectrum at $p = 5$. In the reversed direction 2
**is** in the spectrum at every prime and every identification (§4b.5), and the
2-eigencovector is $\lambda = (1,1,1)$, chart-**symmetric**; the intertwining
needs it chart-**antisymmetric**, because $\delta$ is anti-equivariant for the
label exchange while $E$ is equivariant for the chart involution. All 48
candidates die at S1c, each at 100 of the 125 record cells.

S1d, by contrast, **passes** at all 48 in the reversed direction: the base record
`G-FLAT` has $\lambda(r_0) = 4 \ne 0$.

**The state coordinate is swept, and the collision it hides is measured** (G42).
S1d's 48 is base-record-relative, so all nine of HA's admissible records are run
(counts are summed over the six live cells, 48 each):

| record | counts | mod $p$ | $\tau$-fixed | candidates passing S1d |
|---|---|---|---|---|
| `G-FLAT` | $(1,1,2)$ | $(1,1,2)$ | yes | **288** |
| `G-DIAG2` | $(2,2,4)$ | $(2,2,4)$ | yes | **288** |
| `G-ANISO2` | $(4,9,13)$ | $(4,4,3)$ | yes | **288** |
| `G-CURVED` | $(1,1,2)$ | $(1,1,2)$ | yes | **288** |
| `G-OFFNEG` | $(3,5,4)$ | $(3,0,4)$ | no | **288** |
| `G-ANISO` | $(1,4,5)$ | $(1,4,0)$ | no | **0** |
| `G-OFFDIAG` | $(2,2,6)$ | $(2,2,1)$ | yes | **0** |
| `G-OFFDIAG2` | $(3,5,12)$ | $(3,0,2)$ | no | **0** |
| `G-CURVOFF` | $(2,2,6)$ | $(2,2,1)$ | yes | **0** |

**Five of the nine** admissible records pass; the declared one is among them, and
that is a measurement, not a convention. `records-lax`, which collapses the sweep
to the declared record, dies there.

**The base-point conflict, measured with no chart map in the computation at
all.** S1c forces the exponent covector to be chart-antisymmetric, and an
antisymmetric covector annihilates any $\tau$-fixed record. `G-FLAT` is
$\tau$-fixed. Over all $1{,}344\times124 = \mathbf{166{,}656}$ (generator,
covector) pairs: **192** satisfy S1c, and of those **0** satisfy S1d at `G-FLAT`
— while **192** satisfy it at the $\tau$-asymmetric `G-ANISO` (G42). So S1c and
S1d are jointly unsatisfiable at the declared base record **for every chart map
whatsoever**. That is why the positive control's base-point clause is evaluated
at `G-ANISO` (§2.7, §10) and why it is stated rather than smoothed: at the
declared base record, the full-clause-list emptiness is partly a property of the
clause list, and the result that does not depend on the clause list is §4b.3's.

**S1d's structural consequence.** For a candidate with cyclic image
$\delta(\alpha(r_0))$ has order $p$ or 1, so S1d forces
$\mathrm{ord}(D)\in\{1,p\}$ — not $p \mid 2\,\mathrm{ord}(D)$, and not
$\mathrm{ord}(D) = p$ alone. Since every one of BRG's live cells has
$p\mid\mathrm{ord}(D)$ with $p$ odd, the order-1 branch meets none of them:
measured on the grid of §7, BRG's **3** live $(p,\mathrm{ord}(D))$ cells become
**2** under the base-point clause and **0** under the full clause list, while the
grid's own live-cell count after the clause is **28** of 672 across **4** distinct
$(p,\mathrm{ord}D)$ pairs.

---

## 6. S2 and S3

| identification | direction | candidates | determined | strata hit | $\lvert\mathrm{image}\rvert$ | kernel | injective |
|---|---|---|---|---|---|---|---|
| each of the six | counts$\to q$ | 0 | no | — | — | — | no |
| each of the six | $q\to$counts | 48 | no | $\{36, 81\}$, **2 of 7** | **5** of 125 | 25 | **no** |

S2 fails on both clauses: 48 candidates is not a determination, and a cyclic
image reaches exactly two of the transport side's seven declared strata (its own
$p$-cycle stratum and the identity's).

S3 fails, and at most cells it fails twice over. At **eight** of the twelve cells
it fails by §4b.3's fixed-point mismatch, which needs no arithmetic about the
arena at all. At **every** cell it also fails by cardinality: the record datum
space has $p^3 = 125$ elements and the completion group's $p$-part is $p^1$. The
cardinality form is arena-relative and is disclosed as such (X02) — it is the
only branch that reaches the four cells of `tau-index` and `alt` — and it is
given a positive control with teeth:

> **The injectivity clause is satisfiable as a cardinality matter, and what
> forbids it here is the nine-label arena.** At the sixteen-label completion arena
> the $p$-part of the group's order is measured $p^3$, and three disjoint
> $p$-cycles are built whose generated subgroup is measured to have exactly
> $125 = \lvert V\rvert$ elements: an injective candidate exists there (G15). At
> nine labels the $p$-part is $p^1$ and none can.

**But the square does not follow it there.** $\mathrm{fix}(\delta)$ is measured
$\{e\}$ *inside that very witness subgroup* (§4b.1), so at every cell whose
re-encoding fixes a nonzero vector, S1a $\wedge$ S3 remains unsatisfiable at
sixteen labels exactly as it is at nine. What sixteen labels leaves open is
narrower than it looked: the four cells where neither branch bites (X07).

---

## 7. S4: functoriality in the family

**The grid.** 12 encoding cells $\times$ 7 declared primes $\times$ 8 measured
defect-order classes = **672** cells, each visited exactly once, the count
computed from the declared sets (G16); `grid-drop` dies there. The independent
content is smaller than the cell count and is stated as such: the kernel
computation is per (encoding cell, prime) — **84 distinct linear-algebra
decisions** — and the defect-order axis enters the clause list only through the
base-point condition $\mathrm{ord}(D)\in\{1,p\}$, so the grid is 84 measurements
replicated across 8 orders.

| stage | grid cells live | distinct $(p,\mathrm{ord}D)$ pairs |
|---|---|---|
| BRG's own live-cell condition $p \mid 2\,\mathrm{ord}(D)$ | — | **3** — exactly BRG's three, $(5,5)$, $(5,15)$, $(7,7)$, reproduced here and anchored, together with their non-trivial forward morphism counts $4$, $4$, $6$ |
| S1a+S1b | 112 | 16 |
| after the base-point clause S1d | 28 | **4** |
| the full S1 clause list | **0** | **0** |

The anti-invariant order-$p$ element counts by prime are $\{5{:}48,\ 7{:}96\}$
and **0** at every declared prime above 7 — measured, and the reason the grid can
only ever have been live at two primes.

**The base change inside the class.** A **declared 5-member subset** of the
4,608-member ord-5 class — the lex-first, lex-last, fewest-moved, most-moved and
three lexicographic quantile members, deduplicated, named rather than drawn — is
each rebuilt from its own completion; every one gives the same defect order, the
same fixed-configuration count and the same transport group order, so the pairing
is not an artifact of which member was declared (G17). The sweep is sub-total and
is stated at that scope. `completion-drop` dies there.

---

## 8. S5: the held-out verification, on the pairing's own deformation side

The split is declared before any candidate is fitted, and the fit uses the FIT
cell **alone**. The chart map is **the pairing's own** $E$, read in the direction
whose census is non-empty; the registered direction and the synthetic control map
are run beside it and reported, so that which deformation side carries which
number is visible rather than inferable (G41, a dedicated non-must-pass
disclosure gate on BRG's own G41 pattern).

| measured | value |
|---|---|
| FIT cells / HELD cells | **1** / **124** |
| candidates admitted by the FIT cell alone | **6,336** |
| of those, rejected out of sample by the HELD square | **6,144** |
| survivors | **192** |
| H2: the DEFECT PERMUTATION predicted correctly, entry by entry | **124 of 124** |
| H3: the fixed-configuration stratum predicted correctly | **124 of 124** |
| X-NOSQUARE violations (declared in advance to fail) | **99** |
| X-FLATSTRAT violations (declared in advance to fail) | **99** |

| deformation side | FIT-admitted | died out of sample | survived |
|---|---|---|---|
| **the pairing's own map, geometry$\to$data** | 6,336 | 6,144 | 192 |
| the pairing's own map, registered direction | 6,336 | **6,336** | **0** |
| the declared synthetic compatible map | 6,336 | 6,144 | 192 |

A protocol under which nothing died out of sample would verify nothing; here
**32 of every 33** FIT-admitted candidates (6,144 of 6,336) die on cells the fit
never saw (G18). `heldout-leak`, which fits on the held-out cells; `teeth-off`,
which silently makes the failing extension the accepted one; `quantity-lax`,
which reads the transported quantity off the candidate's image instead of off its
defect; and `s5map-lax`, which swaps the pairing's own map for the synthetic one,
all die.

---

## 9. S6: the prime as a parameter

| $p$ | 5 | 7 | 11 | 13 | 17 | 19 | 23 |
|---|---|---|---|---|---|---|---|
| grid cells live at S1a+S1b | 48 | 64 | 0 | 0 | 0 | 0 | 0 |
| live after the base-point clause | 12 | 16 | 0 | 0 | 0 | 0 | 0 |
| **live at the full S1 clause list** | **0** | **0** | **0** | **0** | **0** | **0** | **0** |
| anti-invariant order-$p$ elements | 48 | 96 | 0 | 0 | 0 | 0 | 0 |

The gate has teeth because the two readings differ: the **partial** clause list
is prime-DEPENDENT (live at 5 and 7 only, and at $p = 7$ live at eight cells
rather than six, because the registered direction opens at `tau-index` and `alt`)
while the **full** clause list is measured prime-UNIFORM. So the verdict does not
ride on the declared prime, and the difference between the two readings is itself
a measurement (G20). `prime-single` dies there.

---

## 10. Both outcomes reachable, and the controls

| control | measured |
|---|---|
| **F0-IDENT** (disclosure gate G21, not a control) | the identity self-morphism commutes with any encoding by construction; its **0** square violations at **40,320** cells record that the identity candidate is the identity, and nothing about the square. **The positive control of this unit is SYNTH-COMPATIBLE** |
| **SYNTH-COMPATIBLE** (positive control; FOUND reachable) | **192** $(g,\lambda)$ pairs, **48** distinct maps, all passing S1a, S1b, S1c **and** S1d — the base-point clause at the declared $\tau$-asymmetric record `G-ANISO`, for the measured reason of §5, recorded in the gate's own detail; the exhibited witness $g = [0,1,4,3,6,2,7,5,8]$, $\lambda = (1,4,0)$ is re-verified independently inside the gate at every cell |
| **SYNTH-EMPTY** (EMPTY reachable) | **0** candidates against a scalar chart map with no eigenvalue 2 |
| **BREAK-HOM** (negative with teeth) | S1a violations **0 of 125** — it satisfies the commuting square exactly — and S1b violations **6,000 of 15,625**; its accepted counterpart scores 0 and 0. **The rejecting clause is S1b, and it is named** |

`found-block`, `empty-block`, `witness-blank` (the no-witness mutant) and
`break-blind` each die at their own gate. The FOUND branch is not demonstrated
only synthetically: six of the twelve encoding cells return 48 candidates each at
the partial clause list, in-arena, at the declared prime (§5, X08), and a cell of
the **registered** direction returns a candidate passing S1a **and** S1b at
$p = 7$ (§4b.4).

---

## 11. The symmetry self-tests

| self-test | measured |
|---|---|
| relabelling the completion arena by a declared permutation fixing label 0 | the relabelling moves **4** labels; the defect map is measured equivariant at all **40,320** cells **inside the relabelled arena**, and the anti-invariant element count recounted there is **48**, the declared value |
| change of basis of the record datum space by a declared element of $GL_3(\mathbb F_p)$ | the census's own decision quantity — the dimension of the 2-eigenspace — recomputed inside the new basis is unchanged at every one of the twelve encoding cells |
| the tested set | fixed by DECLARATION — the **whole covariant family**, all twelve cells, including the six whose census is empty — and measured strictly larger than the set the verdicts would have selected (six cells) |
| cache discipline | every self-test evaluation BYPASSES the memo (**40,320** bypasses), self-test cache hits **0**, while the cache is measured to have been looked up **1,532,778** times and hit **1,492,458** times *before the self-tests begin* — so the zero-hit clause is not the signature of a cache nobody uses (run totals: 1,573,098 lookups, 1,532,778 hits, 40,320 misses) |

`relabel-lax`, `basis-lax`, `selftest-select`, `cache-lax` and `cache-unused` all
die (G25–G28). The two invariance clauses are analytically forced and are
reported at that strength; what the gates measure that is not forced is that the
action is non-trivial, that the recount is taken **inside** the transported arena,
and — G27 — that the tested set is the declaration and not the verdicts' own
selection.

---

## 12. Open 1: is the prime derivable on this pairing?

### 12.1 The criterion, computed rather than typed

Every candidate structure is declared before evaluation and then measured on two
properties.

**Uniqueness** — does its admitted prime set determine $p$?

**Declaration-freeness** — is the candidate free of this unit's own declarations?
This is **computed**, per candidate, by the declared criterion: each candidate is
a FUNCTION of the declaration record, and it is re-evaluated at every
counterfactual re-declaration of a choice this unit makes. A candidate is
declaration-free exactly when its admitted set does not move; the re-declaration
that moves it is recorded (G43). Seven counterfactuals, measured:

| re-declaration | candidates whose admitted set moves |
|---|---|
| $p := 7$ with the selection rule $\mathrm{ord}(D) := 7$ (class size 9,216, its lex-first member rebuilt here) | **P1, P3** |
| the DIRECTION $:=$ the reversed one | **P8, P9** |
| the IDENTIFICATION $:=$ the declared natural cell alone | **P8, P9** |
| the IDENTIFICATION $:=$ the $\tau$-conjugate of the declared index cell alone | — |
| the BASE RECORD $:=$ `G-ANISO` | — |
| the COMPLETION $:=$ the lex-last member of the ord-5 class | — |
| the RECORD-SPACE BASIS $:=$ the declared $GL_3$ element | — |

`open1-lax` (admit a carrier as free), `open1-free-flip` (flip exactly one
measured entry) and `unique-lax` (let a two-element set count as a determination)
all die.

### 12.2 The candidates, at both declared scales

The prime range swept is every prime below 60; the admissible primes are those
not dividing 6. Sets shown are admissible parts.

| id | candidate | 9 labels | 16 labels | role (9) | unique | declaration-free |
|---|---|---|---|---|---|---|
| P1 | the deformation carrier's size $p^{k+d}$ | $\{5\}$ | $\{5\}$ | narrowing | yes | **no** — moves under the prime |
| P2 | the deformation arena's front structure | all | all | narrowing | no | yes |
| P3 | the transport group's order $2\,\mathrm{ord}(D)$ | $\{5\}$ | $\{5\}$ | narrowing | no | **no** — moves under the prime's selection rule |
| P4 | the transport family's defect-order spectrum | $\{5,7\}$ | $\{5,7,11,13\}$ | narrowing | no | yes |
| P5 | the completion group's element orders | $\{5,7\}$ | $\{5,7,11,13\}$ | narrowing | no | yes |
| P6 | the record$\leftrightarrow$metric readout's determinant (raw: all but 2) | all | all | narrowing | no | yes |
| P7 | the denominator of $\rho$ (raw: all but 2 and 3) | all | all | narrowing | no | yes |
| P8 | **the S1 intertwining condition itself** | $\{7\}$ | $\{7\}$ | narrowing | no | **no** — moves under the direction and under the identification |
| P9 | the joint system P7 $\wedge$ P8 | $\{7\}$ | $\{7\}$ | narrowing | **yes** | **no** — inherits P8's |
| P10 | the count of anti-invariant elements of order $p$ | $\{5,7\}$ | $\{5,7,11,13\}$ | narrowing | no | yes |
| P11 | the $p$-part of the completion group's order against $\lvert V\rvert = p^3$ | $\varnothing$ | $\{\mathbf 5\}$ | no admissible prime | yes | yes |
| P12 | the intersection of every declaration-free narrowing | $\{\mathbf 5,\mathbf 7\}$ | — | narrowing | no | yes |

*Admissible parts are shown; the admissible primes are those not dividing 6, so
$p = 2$ and $p = 3$ never appear in this column even where a candidate admits
them. Every set is computed, none is typed: P11's raw admitted set at nine labels
is $\{2\}$ — the 2-part of $8!$ is $2^7 \ge 2^3$, so an injective candidate is
cardinality-admissible at $p = 2$ — which is why P11 is `unique` and yet has an
empty admissible part there.*

### 12.3 The verdict, from two computations that share no deciding variable

**Source 1** reads the candidate table, excludes the intersection candidate P12
from the uniqueness test (P12 *is* the intersection, not an independent
candidate), and forms the intersection. **Source 2** never forms an intersection
and never reads the table: it eliminates prime by prime, rebuilding each
candidate's membership from its arena's own primitive measurements with the memo
bypassed. The two agree at both scales (G44); `open1-inter`, which perturbs the
intersection at its own source, and `open1-scale`, which reads the sixteen-label
scale at the nine-label arena, both die. `open1-reach` — which makes the two
declared synthetic tables return the same verdict — dies at G45, where a table
with one declaration-free singleton returns `LCB-PRIME-DERIVED` and a table with
two survivors returns `LCB-PRIME-DECLARED` through the same function.

$$\boxed{\textbf{LCB-PRIME-DECLARED}\ \text{at the declared nine-label arena;}\quad
\textbf{LCB-PRIME-DERIVED}\ \text{at sixteen labels}}$$

| scale | tightest declaration-free narrowing | verdict |
|---|---|---|
| 9 labels — this pairing's declared arena, and where the unit's verdict is entered | $\{5,7\}$ | **`LCB-PRIME-DECLARED`** |
| 16 labels — the successor arena of G15 | $\{5\}$, from **P11** | **`LCB-PRIME-DERIVED`** |

**What is measured, at nine labels.** The tightest declaration-free narrowing is
$\{5,7\}$ — two primes, so nothing in the committed structure of this pairing
selects 5 over 7. The candidates that single out a prime all carry a declaration:
P1 and P3 move when the prime is re-declared; **P8 and P9 move when the direction
is re-declared and when the identification is** — and P8 is exactly the
intertwining condition. Its admissible part is $\{7\}$, not $\varnothing$: over
the covariant family the registered intertwining is solvable at $p = 3$ at every
identification and at $p = 7$ at two of them, and 7 is admissible. So the
intertwining does pick out an admissible prime — and the criterion measures that
what it picks is a function of two of this unit's own declarations, which is why
it is excluded from the intersection.

**What is measured, at sixteen labels, and how far it reaches.** At the
sixteen-label arena P11 — the injectivity-admissibility candidate, measured
declaration-free — narrows to $\{5\}$ and the same derivation returns
`LCB-PRIME-DERIVED`. This is reported, not adopted, and the reason is measured
beside it (G44): the arena that produces the tighter narrowing is itself a
function of the declared prime. The smallest arena admitting an injective
candidate has $3p+1$ labels — **16 at $p = 5$, 22 at $p = 7$** — and P11's
admissible part *at that arena* is $\{5\}$ at $p = 5$ and $\{5,7\}$ at $p = 7$.
A derivation of $p$ that must first be handed the arena $p$ chooses is not a
derivation of $p$; the unit's verdict is therefore entered at the declared arena,
by the declared verdict rule, and the sixteen-label reading ships with it as a
measured fact rather than as a conclusion.

**The standing interpretation, stated and not adjudicated.** With $p$ not derived
by any declared candidate structure at this pairing, any bridge statement made
here is declaration-relative: it answers the from-vs-alongside question only
relative to a declared reduction prime, and a from-claim built on it would
inherit that relativity.

---

## 13. The verdicts

$$\textbf{LCB-BRIDGE-EMPTY-AT-STRENGTHENED-STANDARD-UNIVERSAL-FOR-THIS-SQUARE}$$

derived **inside gate G30** from three sources that share no deciding variable:
the clause table's own survivor sum; the S4 grid's Gaussian-elimination decision,
which never touches the enumeration; and **the coverage argument of §4b, which
touches neither and reaches the verdict with no census at all** — at every one of
the 84 (cell, prime) pairs either the fixed-point mismatch forbids S1a together
with S3 outright (56 pairs) or the arena's own $p$-part does (28), and none is
uncovered. The `UNIVERSAL-FOR-THIS-SQUARE` qualifier is earned from the measured
coverage — 84 of 84 (cell, prime) pairs covered, 56 arena-free and 28 by the
arena's own $p$-part — and its own negative case is evaluated in the gate, so a
blinded measurement cannot pass. Every **numeric** qualifier below is recomputed
inside that gate from a different source than the one it was printed from (18 of
them, 0 disagreements). The nineteenth, the obstruction string, is not recomputed
by calling its own constructor again: it is **token-gated** in G46, where the
expected tokens are assembled from the measured booleans and each is required to
be present exactly when its own boolean holds. `verdict-flip`, `count-flip`,
`qualifier-typo`, `universal-lax`, `obstruction-misname` and
`obstruction-fabricate` all die there.

| qualifier | value |
|---|---|
| encoding cells | 12 |
| primes | 7 |
| grid cells | 672 |
| completion-family members | 40,320 |
| ord-5 class size | 4,608 |
| record cells per candidate | 125 |
| homomorphisms enumerated | 41,665 |
| candidates passing S1a+S1b | **288** |
| candidates passing all of S1 | **0** |
| encoding cells passing S2 | **0** |
| encoding cells passing S3 | **0** |
| accepted at the strengthened standard | **0** |
| live grid cells after the base-point clause | 28 |
| cells where the obstruction is arena-free | **8** of 12 |
| admissible records passing the base-point clause | **5** of 9 |
| set-level census absent S1b | $8.157\times10^{56}$ |
| Open-1 declaration-free narrowing, 9 labels | $\{5,7\}$ |
| Open-1 declaration-free narrowing, 16 labels | $\{5\}$ |
| obstruction | the FIXED-POINT MISMATCH (below) |

### The obstruction, named

> **THE FIXED-POINT MISMATCH — S1a AND S3, both of them clauses BRG registered.**
> The transport side's encoding has exactly **one** fixed point, at every arena,
> with no hypothesis on the arena: $\delta(x) = x \iff \sigma(x) = e \iff x = e$.
> The deformation side's re-encoding fixes a **nonzero** vector. The commuting
> square must therefore collapse the whole of $\mathrm{fix}(E)$ onto the identity,
> which injectivity forbids. **ARENA-FREE at 8 of the 12 covariant cells**, at
> every prime and every $d$ measured; at the remaining 4 — whose re-encoding
> carries primitive cube roots of unity in place of the eigenvalue 1 — the fixed
> space is trivial and the arena's own $p$-part ($p^1$ against $p^3$) decides
> instead, arena-relatively.
>
> **Per-cell diagnostics of the same wall, retained and measured coextensive with
> the clause failures they name (G31):** SPECTRAL in the data $\to$ geometry
> direction — $\delta$ acts as squaring, so the square demands
> $2\in\mathrm{spec}(E)$, which holds at $p = 3$ at every identification and at
> $p = 7$ at two of the six; and CHART-PARITY in the geometry $\to$ data direction
> — 2 is in the spectrum at every prime there, but the 2-eigencovector is
> chart-symmetric while the intertwining needs it chart-antisymmetric. Neither is
> the obstruction: the wall stands at cells where neither bites.

**Stated in the sub-object form the pin asks for:** at this pairing there is no
embedding of the deformation side's encoding into the transport side's encoding
at the strengthened standard — not because the group orders miss each other (they
do not: $5 \mid 10$, and 4 non-trivial group morphisms exist), and not, at eight
of the twelve cells, because the arena is too small — but because the two
encodings' fixed-point structures are incompatible: one map fixes exactly one
point, the other fixes a subspace, and a commuting square between them must
collapse the subspace onto the point.

### What this says, and at what strength

At the first arithmetically-possible carrier — the one BRG's own arithmetic
pointed at, chosen by a declared rule that lands on BRG's own smallest witness —
the group-level live cell **does not survive the encoding layer**, and it fails at
clauses BRG itself registered rather than at this unit's additions. BRG's live
cells were "a divisibility fact about one integer plus an unconstrained choice of
images" (its §5); this unit tests the standard BRG registered to make such a cell
mean something, the cell does not meet it, and the reason is structural rather
than arithmetic at two-thirds of the family. The census is exhaustive at its
declared scope and the machinery demonstrably returns FOUND on a compatible pair.

### What it does not say

- **No claim that the two encodings are unrelated.** Six of the twelve declared
  encoding cells return 48 candidates each at the partial clause list, and a cell
  of the registered direction returns a candidate passing S1a and S1b at $p = 7$.
- **No claim of universality beyond this square.** `UNIVERSAL-FOR-THIS-SQUARE`
  is bounded by measurement: the obstruction is arena-free at 8 of 12 cells and
  arena-carried at 4 (X11). Nothing here is a theorem about every completion
  arena or every readout.
- **No claim that $p=3$ is what the square wants.** That reading was carried by
  the two-of-six identification scope; over the covariant family the registered
  intertwining is solvable at $p = 3$ everywhere and at $p = 7$ at two
  identifications, and 7 is admissible (X10).
- **No claim beyond the nine-label arena.** At sixteen labels the cardinality
  branch dies and the arena-free branch does not; the four cells where neither
  bites are untested there (X07).
- **No claim about the corpus's from-vs-alongside question in general.** The
  verdict is at one pairing, at a declared scope, and the Open-1 verdict says the
  prime that indexes it is a declaration at that arena.
- **No repair, extension or criticism of BRG, HA, GEN, XBA or PSI.** Their
  numbers are reproduced, not adjusted.
- Nothing here is citable before an external hostile round confers TERMINAL.

---

## 14. The receipt

`v13/code/lcb_livecell_exact.py` emits `lcb_livecell_output.txt` and
`lcb_livecell_receipt.json`. Interpreter `/opt/homebrew/bin/python3.13`. Exact
arithmetic throughout: integers, `fractions.Fraction` and exact $\mathbb F_p$; no
float or complex literal and no `float()`/`complex()` call appears in the source,
and the scanner that measures it is validated by a synthetic injection it must
flag (G32). No function that registers a gate reads a mutant switch, a run-mode
boolean or `sys.argv`, measured by an AST guard validated by a synthetic sample
it must flag (G33). Anchor failures are exit-1, and the anchor policy is measured
(G34). **Nothing in this unit is randomised or sampled**; there is no seeded
stream and no `[SAMP]`. Two sweeps are declared sub-total rather than exhaustive
and say so at every claim: S4's base change runs at a **declared 5-member
subset** of the 4,608-member ord-5 class (lex-first, lex-last, fewest-moved,
most-moved and three lexicographic quantiles, deduplicated — named, not drawn),
and §4.3's squaring identity is verified at **40 declared cells** (the first 8
anti-invariant generators $\times$ all 5 powers) rather than all 240. Every other
sweep in the unit is exhaustive at its declared scope.

| item | value |
|---|---|
| anchors (exit-1-only) | **30**, all reproduced |
| gates | **46** registered, **44** must-pass, **0** failures |
| disclosures | **11** (X01–X11) |
| mutants | **69**, **0** survivors, `never_falsified` **empty** |
| runs | two full delivery runs, byte-identical output and receipt |

**Hash pins**, verified byte-exact against disk at G02, each with its own
corrupt-and-fire anchor mutant. `v13/note-lcb-livecell-pin.md`
`4f30880229e72cef28fb4c0891845ad3e258980fce2fc3b25fcf130b8612a822`;
`v13/paper-brg-bridge.md`
`371e38742059d14a93dd4dc916785a6c59a6ecd4e4b6cb882298ba8bdbc2ab33`;
`v13/code/brg_bridge_receipt.json`
`5c428afd5c58c8998b575b38b6a7808803805c2cb5c2d5bc0baca3bc10a989f9`;
`v13/code/ha_successor_receipt.json`
`542b8735daf0ebc6fc0063068e85c76f05cbca53b7f1174968f6ca79dc0068d4`;
`v13/code/gen_generality_receipt.json`
`e0b2f444f6a9b82861024f7733c7230583742dfd477d9ed6037a241e7b48d292`;
`v13/code/xba_crossbase_receipt.json`
`6015708df2a437a61955c1e194a0273b0eb712699844c9e6eb567cc3536db053`;
`v13/code/psi_curvature_receipt.json`
`7c7b91a9257e3888f3e1048366d728b5adead82b84cc9ef36175c0ba3e99fa75`.

### Disclosures

| id | disclosure |
|---|---|
| X01 | The structure of every S1a-admissible candidate is FORCED, and the forcing is MEASURED here rather than cited: the image is a $p$-subgroup whose order the arena's own $p$-part bounds by $p^1$; the square forces $\Sigma g\Sigma = g^{c}$ with $c^2 = 1$; $c = +1$ is measured empty; so $\delta(\alpha(r)) = \alpha(r)^2$ and the square reads $\lambda\circ E = 2\lambda$. |
| X02 | S3's cardinality failure at this carrier is FORCED — $\lvert V\rvert = p^3$ against a $p$-part of $p^1$ — and is disclosed, not used as a discriminating must-pass gate. Its positive control at sixteen labels (G15) is what makes the clause a measurement rather than a tautology. |
| X03 | The direction convention is SWEPT, not chosen. HA's prose calls the determinant-2 map "the map from the link counts to the components of $q$", while the matrix its code measures with determinant 2 is the linear system's coefficient matrix, which runs $q\to$counts. Both directions are declared cells and both are reported; the REGISTERED one is data $\to$ geometry. |
| X04 | The held-out square is PARTLY forced and is reported at that strength: once a candidate's exponent is linear and its generator anti-invariant, the square on HELD follows from the square on a spanning FIT set. What is NOT forced, and is what the protocol measures, is that 6,144 of the 6,336 FIT-admitted candidates die out of sample, that both transported quantities are predicted correctly at every held-out cell, and that both declared failing extensions fail. |
| X05 | S2's second clause is forced once the image is cyclic: a cyclic image of order $p$ reaches exactly two of the seven declared strata. Measured and disclosed; not used as a discriminating gate. |
| X06 | The square is compared as $9\times 9$ permutation matrices entry by entry, not as group elements; a mutant that compares element ORDERS instead is declared and dies. |
| X07 | What sixteen labels is and is not decided by: G15 measures that an injective candidate EXISTS there, so the CARDINALITY branch dies; the ARENA-FREE branch does not, since $\mathrm{fix}(\delta) = \{e\}$ is measured inside that arena's own order-$p^3$ witness subgroup. At the four cells whose fixed space is trivial neither branch bites at sixteen labels and the square there is NOT tested — that residue, not the whole arena, is what stays open. |
| X08 | Six of the twelve covariant encoding cells return a non-empty S1a/S1b census — 48 candidates each, the whole geometry$\to$data direction — every one rejected by S1c at exactly 100 of 125 record cells. Measured, reported, and not counted as a FOUND, because S1c is a declared clause of S1. |
| X09 | The enumeration route and the linear-algebra route share their input data but not their computations; they are disclosed as two computations over shared data, which is what they are. |
| X10 | The spectral condition is IDENTIFICATION-RELATIVE and the prime it picks is not always 3. In the registered direction 2 enters the chart map's spectrum at $p = 3$ at every identification of the covariant family — and at $p = 7$ as well at **2 of the 6**, `alt` and `tau-index`, the second being the chart-involution conjugate of the declared index cell. $p = 7$ IS admissible on the deformation side, so the registered census there is non-empty at S1a and S1b and is emptied by S1c (G38). Swept over every prime below 60. The delivered reading that the intertwining "wants $p = 3$" was carried by the two-of-six identification scope and does not survive the covariant one. |
| X11 | The obstruction is arena-free where the re-encoding fixes a nonzero vector, and that is not everywhere: 8 of the 12 cells at every declared prime; at the remaining 4 the fixed space is trivial and the emptiness is carried by the arena's own $p$-part, arena-RELATIVELY. The universality claimed is over this square's own cell family, not over arenas at those four cells. |

### The mutant table

69 declared mutants, every one exiting 1 with a named kill, no survivor, and no
must-pass gate left unfalsified. Twelve anchor mutants (each of the seven hash
pins, the rebuilt defect permutation, the family sweep, the $\mathbb F_p$
reduction of $\rho$, the readout matrix, and the anchor-failure policy); three
discipline mutants (freeze, float scanner, AST scanner); seven pairing mutants
(selection rule, group closure, compatibility clause, arena freeness, encoding
cell drop, **covariant-orbit truncation**, chart involution); seven census
mutants (square scope, homomorphism clause, enumeration drop, route A, route
aliasing, matrix comparison, forced-sign step); nine S-clause mutants (S1c scope,
base record, stratification source, image size, $p$-part arena, grid drop, prime
sweep, completion drop, **base-record sweep collapse**); **six arena-free
obstruction mutants** (fixed-point law, fixed-space blinding, dimension sweep,
the $p=7$ witness covector, the column-sum reading, the set-level per-orbit
factor); four held-out mutants (fit leak, teeth, transported quantity, **chart
map swap**); four control mutants (FOUND block, EMPTY block, **no-witness**,
break-blind); five self-test mutants (relabelling, basis change, verdict-selected
set, cache leak, cache disuse); **six Open-1 mutants** (declaration-free
wholesale, uniqueness, **single-entry freeness flip**, **intersection at its own
source**, **scale collapse**, **reachability collapse**); and six verdict mutants
(obstruction misnaming, **obstruction fabrication**, **verdict flip**, count
flip, **universality assertion**, qualifier typo).

Every one of the **44** must-pass gates is falsified by at least one mutant:
`never_falsified` is measured **empty**, and it is printed in the receipt.

---

## 15. Deviations and declared choices

1. **The encoding layer is posed as an endomorphism of one datum space, which
   requires an identification and a direction; the direction is swept and the
   identification is swept COVARIANTLY.** HA's own §3.2 says the record and the
   metric are "one datum in two coordinate systems", which is what licenses the
   naturality reading of S1 (one morphism, two charts) rather than two
   independent vertical arrows. **The cost of that reading is named rather than
   left implicit, and it is stated as the algebra it is** — not as a measurement
   of this delivery, which does not run it: under the alternative
   two-vertical-arrow reading $\delta\circ\alpha_1 = \alpha_2\circ E$ the square
   is solvable at every encoding cell, at every prime and in both directions,
   because $\lambda_2 := 2\lambda_1E^{-1}$ solves it for any invertible $E$. The
   SPECTRAL diagnostic is therefore a consequence of the one-morphism reading; the
   CHART-PARITY diagnostic and the FIXED-POINT MISMATCH of §4b survive both
   readings, and the verdict rests on the last of the three.
   **The identification scope is a correction this delivery carries by name.** An
   identification is a naming of the metric's three slots, so under RUNBOOK §15
   the declared pair does not bound its own declaration-relativity: the orbit
   does. Sweeping two identifications of six was wrong, and the two that were
   missing are not exotic — one of them is the chart-involution conjugate of this
   unit's own declared `index` cell, and it is the cell where the registered
   direction is live at an admissible prime (§4b.4). The whole orbit is swept
   here, the cell count is computed from the declared slot triple, and
   `cell-orbit-drop` falsifies the truncation.
   **One consequence of the chart-carriage measurement is recorded rather than
   smoothed:** only `natural` and its conjugate induce the same slot permutation
   on counts and on the metric (G09), so at the other eight cells S1c is
   evaluated with the COUNT chart's involution by declaration. It changes no
   number — those cells' counts are identical to the natural identification's,
   48 and 100 of 125 — but the S1c reading there is declaration-relative and is
   stated as such; the load-bearing cells are the chart-carrying ones, and the
   verdict does not rest on S1c at all (§4b.3).
2. **S1 is operationalised in four sub-clauses, and S1b, S1c and S1d are all
   additions of this unit.** BRG's S1 names the commuting square and the two
   encodings; it names neither the homomorphism clause, nor the involution
   clause, nor the base-point clause. S1b is the third addition and it is
   declared here as one: BRG's square is a **Set**-level statement, since
   $\delta$ is a twisted cocycle and not a homomorphism, and the set-level census
   at the registered direction is measured **not** empty —
   $8.157\times10^{56}$ solutions (§4b.5). Each addition ships with its reason
   (S1b: a candidate that ignores the record datum's additive structure carries
   almost no information and S2's "determined" clause has no content without it;
   S1c: the involution is the structure — PSI's one law, GEN's
   $\Sigma D\Sigma = D^{-1}$ and HA's chart involution are all the same
   involution; S1d: the base point is what makes the *pairing* rather than two
   abstract encodings the object under test), and the census is reported clause
   by clause so a reader who rejects any of them can read the alternative
   directly from §5. **A reader who rejects all three still lands on EMPTY**, by
   §4b.3, which uses only S1a and S3.
3. **S3 is read in the injectivity form.** BRG's S3 offers "injectivity on
   orbits, or at least a measured lower bound on $\lvert\mathrm{image}\rvert$".
   The injectivity form is chosen because BRG's one-way theorem restricts a FOUND
   to the sub-object embedding form, and an embedding is an injection; the
   measured image fraction (5 of 125) is reported as well.
4. **The exhaustive homomorphism enumeration runs at $p = 5$** — the pairing's
   own prime. At the other declared primes the census runs by the linear-algebra
   route together with the arena's own measured $p$-part bound, whose completeness
   is validated exhaustively at $p = 5$ by route B's measured image-size spectrum
   over all 41,665 homomorphisms. This is declared, not silent. One of the two
   registered-direction cells that are live at $p = 7$ is additionally built and
   run explicitly, candidate and all (G38).
5. **Nothing is sampled, and the two sub-total sweeps are named, not drawn.**
   There is no randomised stream anywhere in this unit and no `[SAMP]`. S4's
   base change runs at a declared 5-member subset of the 4,608-member ord-5
   class and §4.3's squaring identity at 40 declared cells of 240; both are
   enumerated by name and both are stated at that scope in §7 and §4.3.
6. **The emptiness decision has three sources, and the third is not a re-reading
   of the first.** The verdict's decision variable is taken at its own source,
   recomputed from the S4 grid's Gaussian-elimination decision (which never
   touches the enumeration), and established a third time by §4b's coverage
   argument, which touches neither and reaches the same verdict with no census
   at all. The Open-1 verdict likewise has two sources that share no
   deciding variable (§12.3). That is why `count-flip` and `open1-inter` cannot
   survive.
7. **S5 runs against the pairing's own deformation side, and the earlier
   synthetic-only reading is corrected rather than re-disclosed.** The held-out
   block is exercised on the pairing's own chart map in the direction whose
   census is non-empty; the registered direction and the synthetic control map
   are reported beside it in a dedicated disclosure gate (G41) so that no reader
   has to infer which deformation side carries which number. `s5map-lax` falsifies
   the substitution.
8. **The positive control is SYNTH-COMPATIBLE, and F0-IDENT is a disclosure.**
   The identity self-morphism commutes with any encoding by construction, so its
   0 violations at 40,320 cells are analytically forced and are registered as a
   disclosure with no mutant, per RUNBOOK §14 addendum #208. The unit's positive
   control is the synthetic compatible pair, whose base-point clause is evaluated
   at `G-ANISO` for the measured reason recorded in §5 and in its own gate detail.
9. **The state coordinate is declared and swept.** All nine of HA's admissible
   records are run at the base-point clause; five pass, and the declared one is
   among them (§5, G42).
10. **No Lean.** The pin says Lean NONE.
11. **No git.** The worker performed no repository mutation. This delivery
    completed a partially-written working tree left by an interrupted predecessor
    worker: the partial edits were inventoried against the binding order,
    verified item by item, completed, and re-run from scratch.

---

## 16. Opens

1. **The four cells where neither branch bites, at sixteen labels.** The
   cardinality obstruction dies at sixteen labels and the fixed-point mismatch
   does not — except at `tau-index` and `alt`, whose re-encoding fixes nothing.
   Whether the square is satisfiable there, at an arena large enough for
   injectivity, is the one place a FOUND could still live at this readout, and it
   is not tested here.
2. **Whether the fixed-point mismatch is a theorem about the whole one-law
   family.** $\mathrm{fix}(\delta) = \{e\}$ is proved for every arena by the
   twisted-cocycle form of the law. $\dim\ker(E - I)\ge 1$ is measured, at
   $d = 2,3,4,5$ and at six identifications; whether it holds for every
   admissible readout is open, and the two identifications where it fails are
   the measured counterexample to the naive universal form.
3. **A record readout with 2 in its spectrum at an admissible prime, from a
   different link set.** What this unit measures about the $d$-family is its
   fixed space and its column sums: at $d = 2,3,4,5$ the natural-order readout
   fixes exactly a $d$-dimensional space and the lex-order one fixes a line, so
   the fixed-point mismatch survives every $d$ measured; and the column-sum
   mechanism that makes the reversed direction live at every identification is
   measured special to $d = 2$. The open question is therefore a genuinely
   different **link set**, not a different $d$ — the full general-$d$ spectrum is
   not measured here, only its fixed part.
4. **The two remaining directions of Open 1.** Nothing measured here forces $p$
   at the declared arena; nothing measured here forbids a corpus-internal fixing
   of $p$ from some structure this unit did not declare. The candidate list is
   the frame, not the exhaustion. The sixteen-label `LCB-PRIME-DERIVED` reading
   and its arena-coupling ($3p+1$ labels) is the sharpest handle a successor
   inherits.
5. **The three-wing successor cannot pose S1 until its own $\delta$ is decided.**
   Everything here assumes the twisted-cocycle form $\delta(Q) = \sigma(Q)^{-1}Q$,
   and at three wings that form is not yet established. Two consequences are
   handed over as requirements rather than results, neither measured here: the
   parity clause has no $S_3$ analogue as stated — it becomes an
   $\mathbb F_p[S_3]$-module condition, since $S_3$ has a two-dimensional
   irreducible where $\mathbb Z/2$ has only the sign — while the fixed-point
   mismatch, being a consequence of the cocycle form alone, would transport
   unchanged if that form survives.
6. Inherited, untouched: BRG's §14 opens 2–5 and 7, and everything HA §14 lists
   beyond the morphism question.

---

*Method note (one sentence, per programme convention): this paper is the
re-derivation delivery ordered by the joint adjudication of the three-lens
hostile panel at v13 LOG #293, and every number it reports is the instrument's,
recomputed in this delivery rather than carried over.*
