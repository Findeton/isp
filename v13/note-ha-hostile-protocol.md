# HA — HOSTILE-REVIEW PROTOCOL (FROZEN)

**Status:** FROZEN before panel dispatch, 2026-08-08 (v13 #240).
**Object:** commit 3df5781 (as-is #236), pin dd978e3d10e1 @
9601899.  Reviewers write ONLY their own review file; NO git;
independent recomputation mandatory; grades ACCEPT / AWF /
REJECT.

## Kill-shots

**K1 — THE CONSTRUCTION.**  H_a[N](n,m) = (n+N, m+w[N,n]):
verify the exact bijection + closed-form inverse; verify the
transported-second-step property (GW1 condition 3) BY
CONSTRUCTION; the NO-SMUGGLING AUDIT — the drag uses front
tilts and eventwise lapses only (GW1 §1.2's permitted list):
scan the whole construction for anything metric-shaped
entering silently.

**K2 — THE TWO-SIDED CLOSURE.**  Recompute: the diagonal-sector
EXACT closure (R_HH = id at 132 pairs × 9 sites × 5 records,
d=2 AND d=3); the cross-term anomaly (96/132 at q₁₂≠0);
CLOSURE-IS-INSERTION (63 cells, 0 mismatches; the
"analytically forced given full rank" disclosure — verify the
forcing); **G09 derived or refuted** (no link-local weight can
close — det q a joint function; is that a theorem?); G08B
transport-necessity (frozen-front 0/9); the HHH detector
(81/108 — pair closure does not buy triple closure).

**K3 — THE READOUT DEVIATION (the largest).**  The directive
named v6_task2b; the worker REJECTED it citing GW1's own §2
no-smuggling grading and used the record-internal
interval-cardinality relation instead.  Audit both halves: was
the rejection correct by GW1's own text, and does the
substitute smuggle anything (the same audit as K1, applied to
the readout)?

**K4 — THE BRIDGE NEGATIVE.**  Order-5 elementary abelian
holonomy verified; ΣDΣ = D⁻¹ false for R_HH (17/24 a
coordinate coincidence) while the rebuilt-GEN positive control
satisfies it; "no committed carrier morphism" — is
NOT-POSABLE claimed at the right strength (an obstruction
theorem vs a search that found nothing — which is it, and does
the paper say)?

**K5 — INSTRUMENT.**  16 anchors (incl. the GW1/NT/GEN pins —
corrupt each in scratch, the pins must fire); 25 mutants
audited (≥3 reconstructed); never_falsified EMPTY; the chart
self-test 4,860/0 w/ the cache genuinely exercised (283,133
hits / 486 fresh bypasses — the §14 exercised-cache reading);
the density-weight convention-relativity (4 cells move at
w=1); the K_i/ε elimination legitimacy; the tangential-sector
abelian scoping (R_DD/R_DH out of scope — honest per the
pin?); §13-addendum verdict-gate compliance judged (built
before the addendum — flag if repair needed).

## Deliverables

v13/review-ha-operator.md / v13/review-ha-effectus.md /
v13/review-ha-instrument.md: findings ranked, recomputation
counts stated, grade last.
