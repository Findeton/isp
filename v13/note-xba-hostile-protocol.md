# XBA — HOSTILE-REVIEW PROTOCOL (FROZEN)

**Status:** FROZEN before panel dispatch, 2026-08-08 (v13 #238).
**Object:** commit d1593ac (as-is #237), pin 7cbde5da4280 @
095c6f7.  Reviewers write ONLY their own review file; NO git;
independent recomputation mandatory; grades ACCEPT / AWF /
REJECT.

## Kill-shots

**K1 — THE CHAIN.**  Recompute the species-clause chain
4,096 → 3,072 → 768 → 192 → 12 → 6 with your own predicates;
verify all six survivors carry 82/86/90/106; audit the honesty
of C4's subset-size-1 forcing (trivial as a subset statement —
is the paper's scoping of the content to
derivation+chain+prediction carried everywhere?).

**K2 — THE PREDICTION.**  The third instance (quaternion
(5,1,2,3), rank-2 ψ, different transposition) → the profile;
the equivariant control → 172/192.  Attack via D4/D5/D6: the
third instance shares base G's carrier shape and tests LABELS
on a declared graph (admission tables not recomputed) — does
that weaken the prediction to a label-level statement, and
does the paper claim it at the right strength?  Construct a
sharper predictive test if one exists within budget.

**K3 — THE RESIDUAL OPEN.**  The 24-cell admission-table
agreement is an INPUT, not explained — verify the scoping is
honest everywhere (no sentence claims the graph commonality
explained); C2a's measured vacuity and its missing violator
(the declared control gap) — legitimate or repairable?

**K4 — THE FREEZE (D1).**  Candidates were frozen before
PROFILES but the realized labels were knowable to the
designer — the steering audit: which candidate definitions
could have been reverse-engineered from the known answer, and
does the third-instance prediction adequately answer the
concern (the paper records it rather than arguing it away —
audit that choice).

**K5 — INSTRUMENT.**  36 anchors incl. THREE hash pins
(corrupt each in scratch — the pins must fire); 35 mutants
audited (right gates, named kills; ≥3 reconstructed from
prose); never_falsified EMPTY at 23 w/ both denominators; the
cache gating (0 hits vs 442,368 REFUSED lookups into a primed
cache — verify the refusal mechanism is real); the 11
declared violators (lex-first, seed-free — verify
constructions); the gauge sweep 16,384/0 w/ the
mis-convention control moving 12,288.

## Deliverables

v13/review-xba-operator.md / v13/review-xba-effectus.md /
v13/review-xba-instrument.md: findings ranked, recomputation
counts stated, grade last.
