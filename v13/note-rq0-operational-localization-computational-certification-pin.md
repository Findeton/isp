# v13 RQ0-L0 — computational-certification successor pin

**Status:** STRICT PIN, 2026-07-31.

**Authority:** the user's explicit authorization of one new bounded,
forward-only RQ0-L0 computational-certification repair cycle from commit
`7874daa`.

**Binding predecessor disposition:** the complete cycle
`4366531..7874daa` is immutable and remains permanently:

\[
\boxed{
\text{procedural outcome}=	exttt{RQ0-L0-INVALID},
\qquad
\text{scientific outcome}=\varnothing.
}
\]

No file, receipt, review, adjudication, estimator, fixture, or historical
wording from that cycle may be amended.  No intended factor or atlas truth
from it is promoted.  Its order-192 `C2 x C3 x C4 x D4` fixture is reclassified
only as an opened public performance/regression benchmark for this successor.
It can never earn a scientific rung in this cycle.

This pin authorizes exactly three stages:

1. mutable generic development and public qualification before freeze;
2. one estimator/proof/public-receipt freeze commit;
3. one genuinely held-out post-freeze fixture and one official score,
   followed by external hostile review and separate adjudication.

No additional held-out fixture or post-truth estimator repair is authorized.

---

## 1. Strict question and ceiling

> Can a performance-qualified, total, generic exact estimator reconstruct
> independently operable quantum address factors, full regional restriction
> morphisms, and handle-invariant stable-fact descent on one genuinely
> held-out finite amplitude process inside a pre-qualified complexity
> envelope?

The target remains operational localization only:

\[
\boxed{
\text{typed quantum composition/access}
\longrightarrow
\text{independently operable subinstruments}
\longrightarrow
\text{record-bearing regional atlas}.
}
\]

This unit may not construct or claim topology, spatial localization,
influence, causality, causal order, Lorentzian geometry, spacetime, fields,
stress, gravity, or deformation dynamics.  `RQ0-T1` and `RQ0-C1` remain
prohibited throughout.

---

## 2. Immutable antecedents and public reclassification

### 2.1 Immutable closed cycle

All commits and artifacts from `4366531` through `7874daa` remain unchanged,
including:

- the strict final-certification pin;
- the frozen estimator and proof;
- the order-192 fixture and scorer;
- invalid receipts and delivery note;
- hostile dispatch and verbatim hostile report;
- final adjudication and ledger wording.

The successor may import them only as explicitly classified public theorem,
lemma, adapter, calibration, control, benchmark, or provenance material.

### 2.2 Opened order-192 benchmark

The immutable `C2 x C3 x C4 x D4` object is an opened public benchmark.  It
may be used for:

- profiling;
- performance qualification;
- exact differential testing;
- cache and counter validation;
- presentation, rename, reorder and gauge metamorphisms;
- end-to-end regional-map, record-descent and mutant regression.

Its known factor orders, contexts, records, atlas truth, handles, serialized
fingerprint and receipt are public.  Consequently no result on it is blind
and no result on it can earn a scientific L0 rung.

The new estimator must not branch on:

- its source hash or serialized hash;
- order 192;
- factor orders `(2,3,4,8)`;
- its operation or record handles;
- its context inventory or record placement;
- its intended atlas truth;
- any equality with its known multiplication table.

Static checks and metamorphic transformations must test this prohibition.

---

## 3. Pre-registered paths and chronology

### 3.1 Mutable Stage-A paths

The successor generic implementation will use new paths:

- `code/rq0_l0_compcert_estimator_exact.py`;
- `code/rq0_l0_compcert_public_qualification.py`;
- `note-rq0-operational-localization-computational-certification-soundness.md`;
- `code/rq0_l0_compcert_public_functional_receipt.json`;
- `code/rq0_l0_compcert_public_functional_output.txt`;
- `code/rq0_l0_compcert_public_performance_receipt.json`;
- `code/rq0_l0_compcert_public_performance_output.txt`.

During Stage A these files are mutable development objects and confer no
status.  They become byte-immutable only in the Stage-B freeze commit.

### 3.2 Forbidden pre-freeze held-out paths

None of these paths may exist before the Stage-B freeze commit:

- `code/rq0_l0_compcert_heldout_fixture_exact.py`;
- `code/rq0_l0_compcert_heldout_score.py`;
- `code/rq0_l0_compcert_heldout_receipt.json`;
- `code/rq0_l0_compcert_heldout_output.txt`;
- `note-rq0-operational-localization-computational-certification.md`.

The Stage-B receipt must authenticate their absence through Git object
lookup.  It must also authenticate absence of any alternate future held-out
fixture, multiplication-table, truth, scorer, receipt or delivery path.

---

## 4. Stage A — public-benchmark development before freeze

Stage A deliberately permits iteration.  The estimator must demonstrate that
it can finish the declared public complexity envelope before it is frozen.
Compiling and passing small textbook controls are insufficient.

### 4.1 Semantics-preserving performance requirements

At minimum, the implementation must:

1. construct the ambient represented algebra once per dataset;
2. memoize exact represented algebras by canonical operational subobject;
3. evaluate cheap predicates before any represented-algebra construction:
   independent generation, intersections, mixed implementation,
   commutation, multiplication faithfulness, closure and typed status gates;
4. short-circuit a candidate immediately on the first failed cheap predicate;
5. reuse identical exact caches during discovery, certificate replay,
   regional restriction and mutant checks;
6. cache conversions from identical `MonomialLaw` objects to dense exact
   matrices;
7. optionally use an exact sparse monomial-algebra backend, provided it is
   proved or exhaustively checked equivalent to the dense `Q(zeta_24)`
   implementation on all public controls;
8. expose phase-level timing, candidate counts, rejection counts, algebra-
   cache hits/misses, dense-conversion counts and progress markers;
9. keep timing noncanonical: no scientific predicate depends on wall-clock
   time except the registered runtime caps.

Required instrumentation assertions on the opened benchmark:

- ambient represented-algebra constructions: exactly one;
- each canonical factor-algebra construction: at most one;
- repeated certificate and regional checks reuse the same cache;
- cache keys depend only on canonical operational subobjects, never fixture
  handles or truth labels.

### 4.2 Total outcome resolver

Every invocation must produce exactly one structured outcome:

- a registered scientific positive or negative result, exit `0`;
- a registered procedural invalid result, exit `1`.

Normal return without a resolved outcome, missing outcome, exception,
malformed input, failed immutable anchor, runtime cap, search cap and timeout
must all be caught and mapped to one outcome.  No branch may emit `None`,
`UNEXPECTED-RETURN`, an uncaught traceback as its only result, or exit `0`
without a registered outcome.

The public mutant suite must exercise every resolver branch, including the
previous latent normal-return/missing-outcome defect.

### 4.3 No-tailoring controls

The public qualification must include:

- static rejection of source conditions on fixture hashes, known handles,
  order 192, `(2,3,4,8)`, context names, record placement or serialized
  fingerprints;
- full operation-handle and record-handle renaming;
- arbitrary operation serialization reordering;
- context and record-candidate serialization reordering;
- exact gauge-conjugated presentation;
- redundant legal aliases/composites that preserve the operational object;
- semantically equivalent regenerated objects with different noncanonical
  identifiers;
- unrelated positive and negative objects whose classifications cannot be
  inferred from the order-192 benchmark.

Every renamed, reordered and gauge-conjugated benchmark copy must have the
same canonical structural result.

### 4.4 Full `RegAddr` repair

Every returned `RegAddr` row map must carry and validate:

- source and target operation classes;
- source and target composition contexts `tau`;
- source and target statuses;
- explicit source and target result classes;
- independently supplied source and target row laws;
- source and target observed signatures;
- executable source-to-target complete context maps.

The arrow must also carry executable maps on independently selectable versus
composite-only declarations, preparations, probes, readouts, record
candidates with dynamics/projectors, and gauge actions.  It must validate
identity, composition and direct-versus-composite naturality for every field.
Structural hashes may authenticate objects but cannot substitute for maps.

### 4.5 Full twisted-triple repair

The syntactic two-atom loop is retained only as legacy calibration.  The new
public hostile control must construct:

- three independently built full regional amplitude instruments;
- three full pairwise instrument isomorphisms;
- exact maps on operations, all complete rows and laws, preparations,
  contexts, probes, readouts, W3 record projectors/dynamics and gauges.

Each pair map must pass all field and composition checks independently.  The
only failure may be:

\[
\phi_{12}\phi_{23}\ne\phi_{13}
\]

on the full regional and record diagrams.  Rejection must occur only at the
triple-loop gate.

### 4.6 Provenance repair

The public qualification must:

- compare the estimator hash with a frozen expected estimator hash;
- compare the proof hash with a frozen expected proof hash;
- authenticate absence of every registered future fixture, scorer, receipt
  and delivery path at freeze;
- derive novelty from the actual composition object and exact invariants or
  an exhaustive declared-scope isomorphism test;
- never accept a fixture-supplied novelty Boolean.

Before the freeze hash exists, a development receipt may record provisional
hashes.  The Stage-B commit must regenerate the canonical receipt with the
committed frozen hashes and Git chronology.

### 4.7 Public outcome controls

The qualification suite must include unrelated fixtures receiving exactly
these registered outcomes:

- positive `RQ0-LOCALIZATION-GROUPOID` or `RQ0-LOCAL-ATLAS` at its declared
  public calibration scope;
- `RQ0-L0-BLOCKED-AT-ADDRESS` when no eligible independently operable factor
  certificate survives a complete search;
- `RQ0-L0-BLOCKED-AT-REGIONAL-MAPS` when address factors exist but mandatory
  regional or record maps fail;
- procedural `RQ0-L0-INVALID` for malformed input, cap failure, timeout,
  failed provenance or missing outcome.

The negative scientific outcomes require completed searches and explicit
receipts.  Absence, timeout or exception is never a negative theorem.

### 4.8 Public qualification threshold

The estimator cannot freeze until one public qualification receipt proves:

1. all inherited public gates and all new hostile gates pass;
2. the opened order-192 benchmark completes addressability, regional maps,
   record descent and the end-to-end mutant pipeline;
3. addressability completes within 120 seconds on the reference machine;
4. the complete benchmark completes within 240 seconds on the reference
   machine, leaving headroom under the 360-second held-out cap;
5. two canonical functional receipts are byte-identical;
6. noncanonical timing is logged separately from the functional receipt;
7. rename, reorder and gauge copies return the same structural result;
8. unrelated positive, address-blocked, regional-map-blocked and malformed
   controls return their registered outcomes;
9. ambient represented algebra is constructed once;
10. every canonical factor algebra is constructed at most once.

If any threshold fails, Stage A remains mutable and permits further generic,
semantics-preserving optimization.  The held-out fixture may not be created.

---

## 5. Stage B — estimator and public-qualification freeze

Only after section 4.8 passes may one separate freeze commit contain:

- the new estimator;
- its revised soundness, cache-equivalence and total-resolver proof;
- the public qualification runner;
- canonical public functional receipts;
- separate public performance receipts;
- append-only `v13/LOG.md` and `RUNBOOK.md` updates.

At this commit:

- all registered future held-out paths are absent;
- no held-out multiplication object, factor truth, contexts, records, atlas
  truth, scorer or delivery exists under any name;
- the estimator and proof hashes are recorded and authenticated;
- the estimator and proof become byte-immutable.

After this commit, no Stage-A optimization is permitted in this cycle.

### 5.1 Revised proof obligation

The proof must preserve the earlier finite P1–P8 soundness/completeness scope
and additionally show:

- cache keys are canonical operational subobjects;
- memoization changes evaluation multiplicity, not predicates or results;
- one ambient-algebra construction is sufficient because every factor basis
  is computed as a subalgebra of the same validated representation;
- sparse and dense exact backends agree at the stated scope, if sparse mode is
  enabled;
- cheap-predicate reordering is logically sound and cannot turn rejection into
  acceptance;
- regional arrows carry the complete registered data and commute fieldwise;
- `Rec` remains projector/Boolean-algebra based and handle-invariant;
- the resolver is total and assigns one outcome to every execution branch.

---

## 6. Stage C — exactly one genuinely held-out score

Only after Stage B is committed may one new hidden fixture be conceived and
written.  Before that commit, its group/composition family, multiplication
table, factor truth, contexts, record layout and atlas truth must not exist in
the repository, scratch notes, receipts or estimator source.

### 6.1 Frozen complexity envelope

The held-out object must have:

- at most 192 quotient classes;
- at most 36,864 complete ordered rows;
- carrier dimension at most 32;
- at least three heterogeneous independently operable factors;
- at least one nonabelian component;
- nontrivial composite-only classes;
- fresh W3 records;
- selective contexts with real pair and triple intersections;
- an irregular record-bearing regional family, not the automatic complete
  proper-Boolean lattice.

These constraints are frozen now.  The actual abstract family and
factorization are not.

### 6.2 Novelty gate

The held-out composition object must not be isomorphic to, derived from, or
re-encode either:

- `S3^3`;
- `C2 x C3 x C4 x D4`.

Novelty must be computed from the actual composition object using exact
invariants sufficient to prove nonisomorphism, or an exhaustive declared-
scope isomorphism search.  A fixture-supplied novelty flag is forbidden.
Differences only in handles, ordering, gauge, phases, records, contexts or
encoding do not establish novelty.

### 6.3 One official run and stopping rule

Run exactly one official held-out receipt under one complete 360-second cap.
Once the held-out multiplication truth first exists:

- no estimator or proof edit;
- no cap increase;
- no fixture shrinkage, repair or replacement;
- no second held-out fixture;
- no hidden rescore;
- no promotion of partial internal results.

Whatever registered positive, scientific negative or procedural invalid
result occurs is frozen.  The delivery is then halted for a fresh external
repo-read-only hostile review, verbatim report freeze and separate
adjudication.

---

## 7. Registered outcomes and exit codes

Exactly one of these outcomes must be emitted by every official run.

### Scientific outcomes — exit `0`

- `RQ0-LOCALIZATION-GROUPOID`;
- `RQ0-LOCAL-ATLAS`;
- `RQ0-L0-BLOCKED-AT-ADDRESS` after a complete eligible-factor search;
- `RQ0-L0-BLOCKED-AT-REGIONAL-MAPS` after positive address certificates but
  failed mandatory regional/fact maps.

### Procedural outcome — exit `1`

- `RQ0-L0-INVALID` for malformed input, exception, cap/search exhaustion,
  timeout, failed frozen anchor, missing outcome, multiple outcomes, or any
  unresolved normal-return branch.

A valid scientific negative does not become procedural failure.  A procedural
failure never becomes a no-go theorem.

---

## 8. Mandatory held-out controls

If the main addressability stage returns in time, the single official run must
execute end-to-end serialized-input or observed-map mutants for:

- immutable estimator and proof anchors;
- complete row `tau`, status, result, supplied law and signature;
- independent selectability;
- operation rename and serialization reorder;
- gauge conjugation;
- `RegAddr` complete row/context maps;
- projector pullback and Boolean `Rec`;
- handle renaming;
- full twisted triple;
- malformed input;
- runtime/search-cap state;
- outcome omission and outcome multiplicity.

Each mutant reruns the total resolver.  Every positive outcome must fail
closed when a mandatory scientific map is corrupted.  Controls not reached
after a binding invalid/negative outcome must be listed honestly as not
reached, never counted as passing.

---

## 9. Receipt discipline

The public and held-out receipts must separate:

- canonical functional data;
- noncanonical timing data;
- definitions and postulates;
- theorem certificates;
- measurements;
- provenance;
- hidden fixture truth;
- controls not reached;
- outcome prerequisites;
- nonclaims.

Every legacy import is classified.  Every cap and seed is recorded.  Exact
arithmetic is required for composition, amplitudes, group/subobject tests,
projectors, Boolean maps, gauge equivalence and descent.  Timing may use the
machine clock but cannot enter a scientific decision except through the
pre-registered caps.

---

## 10. Immediate and successor discipline

The immediate event after this pin is Stage-A generic estimator development
and public qualification.  It is not a held-out fixture, not a score of a
hidden object, and not a scientific promotion of the opened order-192
benchmark.

The Stage-B freeze may occur only after every public qualification threshold
passes.  Stage C may occur only after that separate freeze commit.  No
`RQ0-T1`, `RQ0-C1`, topology, influence, causality, geometry, spacetime,
fields or gravity work is authorized in this cycle.
