# BRG — THE MORPHISM-CENSUS BRIDGE (GRAVITY ⟷ TRANSPORT GEOMETRY) (PIN)

**Status:** PIN, STRICT, 2026-08-08.  **Immutable base:** commit
b632f59 (HA TERMINAL #262; NT #211; GEN #222; XBA #260).
**Authorization:** the standing order (#212) + the #246
adjudication registering this successor + HA §14's four
requirements (binding).  Lean NONE.

## The question

Does a STRUCTURE-PRESERVING MORPHISM exist between the two
measured geometric structures — the deformation side (the
R_HH family: translation actions ≅ Z/p on address registers
over fronts, at the declared primes, with the record-is-metric
encoding) and the transport side (the commutator-law family
⟨W, D⟩ = the dihedral/Klein geometries on wing carriers, with
the completion encoding)?  FOUND ⟹ this program's spacetime is
built FROM its quantum geometry; EMPTY ⟹ alongside, at the
committed carriers, with the obstruction named.

## HA §14's requirements (binding, verbatim in force)

(1) A CARRIER FUNCTOR declared as data — an explicit mapping
between the arenas (fronts/registers/lapses ↔ wings/pointers/
completions), declared before any morphism is evaluated, with
its non-triviality gated (a degenerate functor that collapses
either side is a named kill).  (2) A MORPHISM CENSUS — the
space of candidate structure-preserving maps enumerated at a
declared finite scope (never one expression); the
structure-preservation predicate declared as data (what must
be preserved: the group actions; the composition; the
arena-invariant content per §15).  (3) TWO-WAY GATES:
**BRG-MORPHISM-FOUND** requires PREDICTIVE VERIFICATION — the
found morphism must transport a computed quantity correctly
OUTSIDE its fitting sample (a declared held-out set);
**BRG-EMPTY-AT-CARRIER** requires exhaustiveness at the
declared scope with the obstruction NAMED (which preserved
structure cannot be matched).  Each outcome REACHABLE, with a
mutant proving each reachable.  (4) ARENA-INVARIANCE GATING
(§15): the morphism question posed only over arena-invariant
content on both sides; coordinate/arena artifacts (the prime
itself; naming) may not carry the verdict either way.

## Declared leads (tested, not assumed)

The record-is-metric identity (HA: counts→q linear) vs the
completion encoding (GEN/XBA: D from Q) — is there a declared
dictionary counts ↔ completion data under the functor?  The
spectrum overlap (order 5 in the defect spectrum) — shown at
#246 to be prime-tracking on the HA side: any
morphism-by-spectrum candidate must survive the prime sweep
(a candidate that works only at p = 5 is an arena artifact —
gated out by requirement 4).

## Pre-registered outcomes (only these)

- **BRG-MORPHISM-FOUND** — with the predictive held-out
  verification and the morphism exhibited as data.
- **BRG-EMPTY-AT-CARRIER** — exhaustive at the declared scope;
  the obstruction named.
- **BRG-BLOCKED-AT-⟨object⟩** — census discipline (incl. the
  functor's own non-constructibility, honestly).

## Discipline

ALL engraved rules (§13 incl. verdict-in-gate w/ computed
qualifiers + verdict-flip mutants + cell-completeness +
independent routes; §14 all addenda; §15 all addenda).
Anchors exit-1 against the HA/NT/GEN/XBA terminal receipts,
hash-pinned.  Positive control: a known-trivial
self-morphism on each side (identity functor within one
structure) must be FOUND by the census machinery.  Negative
control w/ teeth: a declared structure-breaking map must be
REJECTED by the preservation predicate.  Exact arithmetic;
byte-identical delivery runs; stall doctrine; NO git;
FREEZE-ON-DELIVERY; deviations ship.  Deliverables:
v13/paper-brg-bridge.md + v13/code/brg_bridge_exact.py +
_output.txt + _receipt.json.  Then §13: freeze → panel →
adjudication.  Successor: per the verdict and the standing
order.
