# Paper 04 construction audit

## Finite relational clocks from one constrained quantum parent

Date: 2026-08-23

Status: **RESULT-NEUTRAL AUTHOR AUDIT**

Disposition: **READY-FOR-INDEPENDENT-HOSTILE-REVIEW**

First exact semantic counterexample: **none found**

Scientific coordinates awarded: **none**

Paper 05, spacetime, and gravity: **closed**

This audit checks the sole frozen Paper-04 candidate against the exact model
pin, all three independent pre-construction audits, root adjudication, and the
v17 reality-first walls. It is not an independent scientific review or vote.

## 0. Authentication and chronology

The audit begins at committed candidate HEAD
`26ee05bc09d1fc2bc6b3caad391c25cbadcb00f1`, whose parent is terminal
model-pin adjudication HEAD
`260f8aafd6e5c9450410365911662cf9d019196c`.

| Artifact | SHA-256 | Size | Role |
|---|---|---:|---|
| `v17/paper-04-finite-relational-clock-parent.md` | `25d6771bd4bbfb658a54da7f65cdcc9620b0769f6d7be6075103ff5d44230dc6` | 1,197 LF / 45,211 bytes | frozen candidate |
| `v17/note-paper04-two-clock-parent-construction-pin.md` | `8adb5def4c927dd55eba4c2360782b1b6d9370fcf3f5d5c76f5458b1a0fbca4e` | 758 LF / 34,071 bytes | exact model contract |
| `v17/note-paper04-model-pin-audit-mathematics.md` | `a2fd150f75557c992a746e78b1e9b3b209bc7d0adca0ca8cee5bcb192d7a9ce9` | 881 LF / 41,768 bytes | finite reduction bindings |
| `v17/note-paper04-model-pin-audit-quantum-clocks.md` | `69c7deae38b115fd60d31006e5b5f79f5167e8e655bf9f3a0c7867ebbd809c1d` | 821 LF / 40,765 bytes | instrument bindings |
| `v17/note-paper04-model-pin-audit-ontology-relativity.md` | `ebd1fa98640accbe036d8097802f27e050875a09771f0d646ed7027e302bf0d6` | 866 LF / 52,036 bytes | ontology and geometry walls |
| `v17/note-paper04-model-pin-audit-adjudication.md` | `f0d2ae0142192683192a5e033c572d0c5906feb546d14268f7525f1b9a0a42cc` | 500 LF / 20,123 bytes | sole construction authority |

The candidate normalized self-hash independently reproduces
`5f5e2aebd8b7382df97e07914ee552a3d95ffebd9103f8da5ffbf7377fabe600`.
The unrelated untracked v16 handoff note was neither opened nor staged.

The mathematical construction froze before a bounded diagnostic verifier was
run. The private Rust source SHA-256 is
`89826fb844081993c7a86556cda40611eda61d705f0481dfc56b8c3084424c0f`.
It passed 118,749 integer assertions covering the representation character,
physical dimension, unique reductions, q=6 rank data, orbit/calibration
relations, adaptive history, response mutants, baseline values, and monotone
coordinate maps. The verifier selected no object and made no repository
change. The paper proofs, not the executable, are the scientific evidence.

## 1. Audit standard

The candidate passes construction fidelity only if:

1. every semantic input is byte-identical to #77;
2. the bare phase PVM obstruction is printed rather than hidden;
3. the retained clock records transform with complete frame lineage;
4. the A instrument remains coherent across Q sectors;
5. the q=6 rank loss is retained in every affected coordinate;
6. the adaptive context is one complete relational process, not two
   externally scheduled projections;
7. all first-record branches are retained and no hidden postselection occurs;
8. the full quantum map precedes any scalar sufficiency claim;
9. calibration and held-out values remain disjoint;
10. both response arms use frozen readers and the same deletion mutant;
11. the parent predictor does not consume $s$ or an encoded equivalent;
12. every finite resource and recurrence domain is printed;
13. all controls, attacks, and product coordinates are present; and
14. supplied order, geometry, ontology, gravity, time, and actuality walls
    remain explicit.

## 2. Parent and reduction reconstruction

The candidate proves $U_gU_h=U_{g+h}$ directly from the fixed character and
derives the projector through finite character orthogonality. It solves the
constraint for

$$
b=-4(a+m+q+aq),
$$

giving exactly 343 physical basis states. All nine projected seed norms are
$1/7$ because their B-phase orbit components are mutually orthogonal.

The B reduction maps each physical basis vector to one target basis vector
with a unit-modulus phase. The A reduction does the same exactly when
$q\ne6$. At $q=6$, seven allowed $(b,m)$ pairs each carry seven A charges,
and phase contraction has one-dimensional image per pair: rank seven from a
49-dimensional domain. The candidate never uses a q=6 A inverse.

These proofs satisfy adjudication B1, B2, and B6 without enumeration standing
in for the theorem.

## 3. Clock-record reconstruction

The candidate explicitly calculates

$$
P E_B(\beta)P=P/7
$$

and states that a bare Lüders branch leaves the physical subspace. It then
constructs the covariant recorded isometry and its normalized direct-sum
reduction form. The B pointer transforms by $\beta\mapsto\beta+2g$.

For A, the pointer action is coherently controlled by Q charge. This is
load-bearing: a fixed record permutation or a classical measurement of Q
would change UCOH. The good reduction branches sum to the good-sector
projector, and the complementary output is an explicit stopped flag. M and Q
records receive their corresponding transforming actions.

Thus the candidate does not turn a gauge-coordinate numeral into an invariant
beable. It constructs a frame-tagged record packet and retains all apparatus
memory. B3--B5 are met at the printed finite-model level.

## 4. Physical two-clock witness

The independent relative readers

$$
\Delta_A=\tau_A-\tau_M,
\qquad
\Delta_B=\tau_B-2\tau_M
$$

give UAO, UBO, and U0 the distinct values $(1,0)$, $(0,1)$, and $(0,0)$.
They are invariant in the q=0 control sector and separate the two physical
origin interventions after projection. This is stronger than separate tensor
labels and kills the copy/rename controls. It does not make the clocks
probabilistically independent.

## 5. Adaptive-history reconstruction

In the canonical B slice, USEQ gives $r_1=1,2$ with equal probabilities. The
frozen parity guard and two-step propagation yield histories $(1,3)$ and
$(2,5)$ with equal probabilities. The opposite guard yields $(1,4)$ and
$(2,4)$.

The candidate then supplies the missing covariance/nonpostselection theorem.
For arbitrary first B record $b$, it defines the frame shift
$h=(b-2)/2=4(b-2)$, invariant relative record
$\widetilde r_1=r_1-h$, and second opportunity $b+4$. Under gauge translation
both $h$ and $r_1$ shift equally. Hence every first-record branch is retained,
the parity predicate is transported, and the canonical table is one slice of
one complete direct sum. Program position and $s$ are absent.

The supplied Paper-03 frontier still orders the operations. The candidate
does not call the relational trigger an emergent arrow. B8 and B9 are met.

## 6. Quantum frame and sufficiency fork

The common-sector frame map is an explicit unitary permutation with a phase,
and its inverse gives exact round trips. Conjugation and direct-sum transport
extend it to the finite complete packets.

For UCOH at A record one, the A-relative state coherently spans two distinct
B branches. Every classical B-reading mixture is block diagonal across those
branches, while the normal effect onto their coherent sum gives probability
one for the exact state and one half for the mixture. This effect belongs to
the complete finite boundary algebra; it is a witness to the pin's universal
all-reader sufficiency question, not an added fitted calibration reader.

The candidate therefore correctly prints full quantum frame equivalence and
scalar insufficiency as separate coordinates. It does not dephase UCOH to
manufacture a scalar result. B10 is met.

## 7. Calibration, baselines, and responses

U0 gives $(\tau_A,\tau_B,\tau_M)=(s,2s,s)$. The two training points fix
$(a,b)=(2,0)$; points two and three remain untouched and pass. The complete
USEQ history is an additional held-out context.

The no-clock conditional matter law is uniform, at TV $6/7$ from the sharp
target. The mistuned B control disagrees sharply on held-out records and has
maximum TV one. The target finite loss is exactly zero, with no tolerance.

At B record two, U0/U1 give A records one/two and UA0/UA1 give Q records
one/two. Deleting $aq$ makes each pair equal. Both response arms therefore
have TV one and vanish under the same frozen deletion control. The candidate
labels this operational reciprocity, not causal or gravitational
backreaction. B11, B12, and B15 are met.

## 8. Hidden-time and covariance reconstruction

The parent predictor depends on the model bytes, physical source, record,
reduction, complete controller, posterior, and reader. It contains no runtime
$s$. The candidate uses $s$ only after prediction to identify the supplied
lab comparator component. The explicit $s$-record and hidden lookup mutants
have no physical record lineage and fail.

Both registered nonidentity reading maps push forward the full interface,
including guard subsets and record actions. This proves finite coordinate
covariance only. B13 and B14 are met.

## 9. Resources, divisions, and walls

The candidate prints all four microscopic dimensions, physical dimension,
record and controller sizes, charge support, exact charge moments, interaction
support, recurrence, stop sector, and zero comparison threshold. It does not
call modular charge positive energy or extend beyond the one-cycle window.

The four record/division cases use only Paper-03 interface memories allowed by
the pin; no memory is inserted into $D$ or the frozen source family. Their
future-sufficiency claims are scoped to licensed continuations and do not
select an actual outcome.

The candidate repeatedly states that the finite group is not a lattice or
event web, that the comparator path and order are supplied, that the
parent/comparator bridge is co-designed, and that spacetime, gravity,
fundamental time, ontology, and actuality remain unconstructed. B16--B20 are
met.

## 10. Census and continuity

| Surface | Required | Candidate | Audit disposition |
|---|---:|---:|---|
| theorem targets M1--M24 | 24 | 24 | each separately dispositioned |
| paired controls C1--C42 | 42 | 42 | both arms printed |
| generic attacks 1--68 | 68 | 68 | every integer appears once |
| model attacks X1--X24 | 24 | 24 | changed object and result printed |
| generic product | 28 | 28 | noncompensatory |
| model product | 14 | 14 | noncompensatory |
| permanent walls | 6 | 6 | all remain closed |

No attack is satisfied by a source-code mutation name. No failed scalar or
stopped-clock coordinate is hidden by the stronger full-frame result.

## 11. Physical-input immutability

The candidate changes none of the following after #77:

- group, factor dimensions, charge bases, phase convention, or polynomial;
- source, source coefficient, interaction, clock, or readout basis;
- training point, held-out point, sequential context, guard, or baseline;
- error functional, threshold, good window, recurrence, or stop sector;
- reparameterization map, outcome ladder, or claim ceiling.

The construction adds proofs, derived reductions, complete interface maps,
exact probabilities, and product outcomes only. The private diagnostic check
verified frozen formulas and did not define them.

## 12. Audit disposition

No pin-fidelity, mathematical-typing, arithmetic, product, or claim-ceiling
discrepancy was found. The exact candidate is ready for independent hostile
review.

This does not accept its scientific result. The hostile panel must test in
particular:

1. whether the covariant record dilation is truly complete and physical;
2. whether the all-branch USEQ construction removes rather than disguises
   external scheduling;
3. whether the UCOH coherence reader is legitimately inside the frozen
   complete-reader quantifier;
4. whether physical A/B independence survives all quotient identifications;
5. whether the parent-to-lab bridge is anything stronger than a formal
   wrapper; and
6. whether any language crosses the supplied-order or ontology walls.

Disposition:

```text
READY-FOR-INDEPENDENT-HOSTILE-REVIEW
```

Scientific coordinates awarded: **none**.

## 13. Authentication

Audit LF line count: `000283`

Audit byte count: `012759`

Audit ordinary SHA-256: reported externally after final bytes freeze;
embedding an ordinary self-hash would be circular.

Audit normalized self-SHA-256:
`c71a00c0ff08c75bea0f4881785b67ae4313215d60c8eb8171e18a6b0758f141`

Normalization rule: replace the six decimal digits on each count line and the
64 hexadecimal characters on the normalized-self line by ASCII zeroes,
preserve every other byte, and compute SHA-256. The file must use LF endings,
end in one LF, and contain no trailing horizontal whitespace.
