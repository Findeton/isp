# ISP v17 — Q-Cut future independent-review contract

**Status:** PRIVATE DRAFT / NOT FROZEN / NO REVIEW AUTHORIZED

**Date:** 2026-08-23

**Scientific result awarded:** none
**Authority created:** none

This contract describes how the private Q-Cut candidate pin could be reviewed
if the user later authorizes it. It does not itself appoint reviewers, launch
agents, freeze files, or permit construction, adjudication, repair, or a
successor.

---

## 1. Review object

The sole proposed review object is the exact future frozen version of:

`/private/tmp/v17_qcut_result_neutral_candidate_pin.md`.

The author-side theorem and audit are disclosed prior work, not part of the
review input during first-pass reconstruction:

- `/private/tmp/v17_continuous_cut_information_theorem_candidate.md`;
- `/private/tmp/v17_continuous_cut_source_quantifier_audit.md`.

If authorized, each reviewer initially receives only:

1. the frozen pin bytes and hash;
2. the exact primary-source versions printed in the pin; and
3. this frozen review contract.

The author-side proof may be released only after each first-pass report is
sealed. Reviewers then perform a second discrepancy pass comparing their
reconstruction with the disclosed derivation.

This is proof-blind verification, not blind discovery: reviewers know that a
candidate derivation exists but do not initially see its steps.

---

## 2. Three review seats

### Seat C — communication complexity and source fidelity

Seat C must independently reconstruct:

1. the exact \(\alpha=1/4\) Partial Matching promise task;
2. the natural hard input distribution;
3. the deterministic distributional lower bound at error \(1/3\);
4. the three-copy quantum upper bound and its exact error \(1/16<1/10\);
5. HJMR Result 1 and Lemma V.3;
6. the one-round, correlated-input specialization;
7. expected-length simulation, Markov truncation, fixed randomness, and final
   worst-case message length;
8. promise-relation applicability; and
9. every monotonicity and constant used in the pin.

Seat C must try to construct a finite-message private protocol for the natural
ensemble with error at most \(3/20\) and
\(I(X:Q)=o(\sqrt n)\). A source citation without reconstruction is an automatic
`REVISE`.

### Seat M — measure theory, kernels, and information

Seat M must independently reconstruct:

1. standard-Borel total-space and fiber typing;
2. joint measurability of the preparation kernel;
3. Borel measurability of every response coordinate;
4. measurability of the finite response-vector map;
5. the exact coordinatewise quantizer and \(1/40\) error bound;
6. existence and typing of \(I(X:\Lambda\mid S)\);
7. \(S\perp(X,Y)\) and the identity
   \(I(X:S,\Lambda)=I(X:\Lambda\mid S)\);
8. \(Q\perp Y\mid X\) and
   \(I(X,Y:Q)=I(X:Q)\);
9. data-processing direction; and
10. infinite-information and null-set branches.

Seat M must attack the proof with nonproduct bundles, nonatomic kernels,
overlapping conditional measures, redundant labels, and a one-real encoding.
It must state whether any hidden regular-conditional-probability assumption is
being used.

### Seat P — physics, ontology, and claim scope

Seat P must determine whether the theorem's premises and conclusions have been
physically represented honestly. It must reconstruct:

1. the operational meaning of the preparation--reader cut;
2. the independence premise for the later setting;
3. the difference between positive records and a positive sufficient cut;
4. the difference between indivisibility and non-Markovian hidden memory;
5. why a response-vector proof device is not an efficient physical model;
6. why mutual information is not automatically memory, energy, volume, or
   explanatory cost;
7. why the quantum comparator does not select complex amplitudes as ontology;
8. the whole-history, retrocausal, measurement-dependent, nonpositive, and
   empirical-deviation escape branches; and
9. the permanent chronology, spacetime, and gravity ceilings.

Seat P must supply at least one controller/lookup-table adversary and one
genuinely indivisible adversary. It must reject any wording that turns the
restricted cut theorem into a no-go for Barandes or for all positive-history
ontologies.

---

## 3. Common mandatory reconstructions

Every seat must independently verify these twelve coordinates:

1. \(n\in4\mathbb N\) and no rounding convention;
2. the exact natural distribution \(\pi_n^{\rm nat}\);
3. the order \(\pi_n^{\rm nat}\) first, all realizers second;
4. worst-case actual error \(1/10\);
5. worst-case screening error \(1/40\);
6. quantization error \(1/40\);
7. induced error \(3/20\);
8. compression slack \(1/20\);
9. final deterministic error \(1/5<1/3\);
10. the \(\Omega(\sqrt n)\) source lower bound;
11. the two data-processing/conditional-independence identities; and
12. the L3 claim ceiling.

Each report must include a literal error ledger and a literal quantifier
ledger. Prose assurances are insufficient.

---

## 4. Mutant battery

Each seat receives the following statements in randomized order and must label
each `VALID`, `INVALID`, or `OUTSIDE-SCOPE`, with a witness.

1. One continuous real is one bit of ontology.
2. Public randomness independent of \(X\) must be counted in
   \(I(X:\Lambda\mid S)\).
3. Public randomness correlated with \(Y\) is free because it carries no
   information about \(X\) marginally.
4. \(I(X:Q)\ge I(X:S,\Lambda)\) by data processing.
5. Correlated \(X,Y\) imply \(I(X,Y:Q)>I(X:Q)\) in every one-way protocol.
6. Expected communication directly upper-bounds deterministic worst-case
   communication.
7. A response label for the realized \(y\) suffices for the theorem.
8. A finite reader family makes the underlying ontology finite.
9. A nonmeasurable response table is an admitted stochastic model.
10. The theorem applies to an indivisible law with no sufficient cut.
11. A source-code lookup table of exponential size is ruled out by the mutual
    information lower bound.
12. The quantum protocol proves amplitudes are ontic.
13. The one-way experiment proves a physical arrow of time.
14. The result is an empirical deviation from quantum theory.
15. A counterexample outside the promise domain defeats the theorem.
16. A counterexample using \(S\not\perp Y\) defeats the theorem.
17. Changing \(\alpha\) after seeing a counterexample is an editorial repair.
18. A proof at error \(1/3\) can ignore the pin's accumulated error ledger.

Any common false positive on mutants 3, 4, 6, 7, 10, 11, 12, or 13 is a
review-quality blocker requiring a new seat, not a majority vote.

---

## 5. Report format

Every report must contain:

```text
FROZEN PIN SHA-256:
PRIMARY SOURCES RECONSTRUCTED:
ERROR LEDGER:
QUANTIFIER LEDGER:
PROOF OR COUNTEREXAMPLE SUMMARY:
MUTANT RESULTS:
SEMANTIC DEFECTS:
SCOPE DEFECTS:
VERDICT:
MAXIMUM EARNED RUNG:
```

Allowed verdicts are:

- `ACCEPT-L3`;
- `ACCEPT-L3-WITH-BINDING-SCOPE`;
- `COUNTEREXAMPLE`;
- `SOURCE-FAILURE`;
- `REVISE-PIN`; or
- `INCONCLUSIVE`.

No seat may recommend changing a threshold, task family, hard distribution,
carrier type, or theorem quantifier inside the same review cycle.

---

## 6. Root adjudication rule

Even three accepting seats do not award a result automatically. Root must
independently reproduce:

1. the source lower bound at the frozen constants;
2. the HJMR one-round inequality;
3. the conditional-independence identities;
4. the measurable finite quantizer;
5. the full error ledger; and
6. at least one attempted countermodel from every premise-escape branch.

`ACCEPT-L3` is available only if all three reports and root reconstruction
close every mandatory obligation. A semantic counterexample dominates seat
count. A citation, notation, or prose defect may be repaired only if it leaves
the frozen mathematical object unchanged and root orders the exact bounded
edit.

A change to any of the following is semantic and requires fresh user
authorization:

- task or promise;
- natural ensemble;
- \(n\) sequence or \(\alpha\);
- positive-cut class;
- standard-Borel or measurability assumptions;
- independence assumptions;
- error thresholds;
- information coordinate;
- theorem quantifiers; or
- claim ceiling.

There is no automatic v2 chain.

---

## 7. Independence limitation

Three isolated automated seats are strong adversarial quality assurance, not
external scientific replication. Before using Q-Cut as support for a new
physics claim, the proof should be reconstructed by at least:

- one communication-complexity specialist;
- one mathematical information theorist; and
- one quantum-foundations specialist not invested in ISP terminology.

External review must be able to read a standalone manuscript without the v17
ledger. Agreement among repository-local agents cannot establish community
acceptance or physical truth.

---

## 8. Contract disposition

```text
REVIEW CONTRACT:              PRIVATE DRAFT
PIN:                          NOT FROZEN
REVIEWERS:                    NOT APPOINTED
FIRST-PASS PROOF ISOLATION:   SPECIFIED
THREE-SEAT DUTIES:            SPECIFIED
MUTANT BATTERY:               SPECIFIED
ROOT ADJUDICATION:            SPECIFIED
AUTOMATIC REPAIR:             FORBIDDEN
EXTERNAL REPLICATION:         REQUIRED BEFORE NEW-PHYSICS USE
SCIENTIFIC RESULT:            NONE
DOWNSTREAM AUTHORITY:         NONE
```
