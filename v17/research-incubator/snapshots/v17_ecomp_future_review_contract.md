# ISP v17 — future review contract for E-Comp

**Status:** PRIVATE / NONBINDING / NOT DISPATCHED  
**Date:** 2026-08-23  
**Candidate:** `/private/tmp/v17_ecomp_result_neutral_candidate_pin.md`  
**Authority created:** none

---

## 1. Review objective

Determine whether the fixed primitive endpoint packet $\iota_G$ admits the
three printed uniform complete block laws and whether two fixed-unitary
nondivisible laws remain separated at $bb$ while agreeing on every base
primitive and physical measurement rule.

The review is logical and source-scoped. It must not select the quantum control
by empirical preference before testing the nonselection theorem.

---

## 2. Dispatch prerequisites

Before any future authorized review:

1. move the embedding, theorem candidate, source audit, candidate pin, and this
   contract to authorized paths;
2. regenerate SHA-256, LF line count, and byte size;
3. freeze every byte before reviewers inspect the objects;
4. print the exact Barandes source versions;
5. confirm no code or expected-output table is required;
6. confirm $Q$, $M$, $V$, and the algebraic $U_\theta$ classification were all
   registered before review; and
7. confirm no later v2 or replacement countermodel is authorized.

Any failure returns `INVALID-DISPATCH`.

---

## 3. Three blind seats

### Seat A — algebra, probability, and uniformity

Independently reconstruct:

1. $H^2=I_2$;
2. $G=|H|^{\odot2}$ and $G^2=G$;
3. normalization of $Q$, $M$, and $V$ for all block lengths;
4. primitive agreement at $n=0,1$;
5. values at $n=2,3,4,6,7,8,10$;
6. the two total-variation separators;
7. divisibility of $M$ and the rank-based nondivisibility of $Q,V$ against
   every stochastic continuation on the printed carrier at $bb$;
8. complete block/measurement record laws;
9. measured-middle equality;
10. relabeling covariance; and
11. unitarity of $U_\theta$, its common one-step endpoint, its two-step family,
    and the rank classification.

Seat A must derive values from formulas rather than import expected arrays.

### Seat S — stochastic-process and Barandes source scope

Independently reconstruct:

1. fixed configuration, contingent state, endpoint law, and division-event
   definitions from primary sources;
2. whether a fully divisible Markov process is admissible as a control;
3. whether the $V$ law is a valid fixed-unitary first-order whole-block law;
4. whether an unmeasured intermediate target requires a joint trajectory law;
5. whether the actual $m$ operation supplies a division event;
6. the difference between primitive endpoint input and a fixed law that already
   contains every $\Gamma_n$;
7. the Schur--Hadamard gauge scope; and
8. whether the candidate overstates any conclusion about Barandes.

Seat S must attempt the strongest source-faithful rejection of $V$, including
the objection that $H$ and $V$ are isolated-endpoint phase lifts. The report
must decide whether their different completed-record laws are one gauge orbit
or distinct completions of an incomplete base packet.

### Seat L — logic, identification, and physical meaning

Independently reconstruct:

1. the exact antecedent whose nonselection is claimed;
2. whether $Q,M,V$ share every datum in that antecedent;
3. whether any conclusion is hidden in the base embedding;
4. whether nondivision is merely a label or an exact failed factorization;
5. whether a complete reader genuinely separates the laws;
6. whether finite uniformity is confused with physical plausibility;
7. whether empirical adequacy and logical derivability are kept separate;
8. whether idle ontology or presentation changes affect the result;
9. the exact surviving inference if $M$ or $V$ fails;
10. the distinction between a continuum of mathematical laws and the
    finite-effective subclass; and
11. every forbidden promotion beyond the lemma grammar.

---

## 4. Blind protocol

Each seat:

1. receives the same frozen bytes;
2. reads no sibling report before freezing its own;
3. writes an independent derivation before consulting author-side commentary;
4. labels findings `STRUCTURAL`, `SEMANTIC`, `SOURCE`, `SCOPE`, or `EDITORIAL`;
5. supplies an explicit calculation or counterexample for every blocking
   finding;
6. prints the narrowest surviving rung; and
7. returns a normalized self-hash.

Automated seats are adversarial QA, not external human scientific review.

---

## 5. Seat A mutants

1. Replace $H$ by a nonunitary matrix while retaining the expected $bb$ output.
2. Replace $G$ by a matrix whose columns do not sum to one.
3. Use $G^2=I$ instead of the actual idempotence.
4. Store the $n=2,3,4,6,7,8,10$ answers rather than derive them.
5. Define $V^2=-iX$ but leave higher powers unspecified.
6. Use a nonunitary or incorrectly normalized $V$.
7. Compute total variation without the factor $1/2$.
8. Claim $Q$ is divisible because $H^2=I$.
9. Omit the retained middle bit from the measured program.
10. Apply a relabeling to transition matrices but not readers.
11. Test nondivision only against $G^2$ while ignoring another possible
    stochastic continuation $K$.
12. Evade the rank theorem with a larger hidden carrier while calling the
    ontology and antecedent unchanged.
13. Treat an arbitrary noncomputable $\theta$ as a finite-effective parameter.

---

## 6. Seat S mutants

1. Treat every syntax boundary as a division event.
2. Assign a joint trajectory through an unmeasured block and call it source
   data.
3. Treat an arbitrary Hilbert phase lift as physical without transforming the
   complete representation.
4. Reject the final-record separator as gauge despite different probabilities.
5. Assume Barandes derives every $\Gamma_n$ from $\Gamma_1$.
6. Assume Barandes never permits a Markov/divisible process.
7. Treat the $V$ completion as a claim that a fully calibrated Hadamard is
   physically $V$ rather than a countermodel to the bare endpoint packet.
8. Refute the entire fixed-system correspondence from primitive nonselection.
9. Infer internal time from the ordered word.
10. Infer a process tensor from the published conjecture.

---

## 7. Seat L mutants

1. Add the quantum whole-word rule to the antecedent and then call it derived.
2. Remove $V$ after seeing that it defeats nondivision selection.
3. Reject $M$ or $V$ by silently adding full Hadamard calibration to the
   antecedent.
4. Use a constant reader to erase the separator.
5. Hide a per-depth answer sequence in one exact real.
6. Call a finite rule ontologically true.
7. Call a wrong empirical theory logically invalid as a countermodel.
8. Promote the repeated-balanced-endpoint lemma to arbitrary process tensors.
9. Promote mutual nonselection to equal physical plausibility.
10. Claim a new empirical prediction from a comparison with known quantum
    behavior.

---

## 8. Required report format

Each report contains:

1. authentication;
2. independent theorem statement;
3. complete calculation;
4. source/premise ledger;
5. mutant results;
6. findings;
7. surviving rung;
8. verdict; and
9. self-hash.

Permitted verdicts:

```text
ACCEPT-ENDPOINT-NONSELECTION
ACCEPT-NONDIVISION-NONSELECTION
ACCEPT-COHERENT-ENDPOINT-FIBER-CLASSIFICATION
ACCEPT-ENDPOINT-ONLY
REVISE-BEFORE-FREEZE
REJECT-SEPARATOR
SOURCE-SCOPE-INVALID
INVALID-DISPATCH
```

---

## 9. Root adjudication

Root must independently:

1. rederive all matrices and distances;
2. rebuild the block/measurement semantics;
3. verify finite uniformity without relying on source-code length;
4. audit the exact base embedding;
5. verify the Barandes source scope;
6. distinguish gauge from different final record laws;
7. determine whether $V$ is structurally admissible and distinct at the
   complete-law level;
8. separate two-state carrier-relative nondivision from a no-dilation claim;
9. apply every mutant;
10. adjudicate one valid counterexample over any majority vote; and
11. print every nonclaim.

No implementation result can upgrade the mathematical rung.

---

## 10. Repair policy

After a future freeze, changing any matrix, grammar rule, held-out length,
reader, division declaration, control, verdict, or scope wall is semantic. It
requires a fresh user decision.

Only unambiguous typography, link, or hash-reporting defects may be repaired
under a separately frozen editorial protocol. No automatic v2 exists.

---

## 11. External-review wall

Before citing the result as a claim specifically about Barandes rather than a
general stochastic-composition lemma, obtain review from a stochastic-process
or foundations expert familiar with his formalism. The mathematical matrix
separation may stand independently at its narrow declared-input scope.

---

## 12. Present disposition

```text
CONTRACT DRAFTED:      YES
BYTES FROZEN:          NO
REVIEWS DISPATCHED:    NO
RESULT ACCEPTED:       NO
OFFICIAL AUTHORITY:    NONE
```
