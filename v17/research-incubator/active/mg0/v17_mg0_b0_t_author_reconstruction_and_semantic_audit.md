# ISP v17 — MG0 B0-T author reconstruction and semantic audit

**Status:** ACTIVE AUTHOR-SIDE AUDIT / NOT AN INDEPENDENT REVIEW / NOT A PIN
**Date:** 2026-08-24
**Scientific result awarded:** none
**Authority created:** none

---

## 0. Disposition

An exact reconstruction of the open B0-T witness preserves its core finite
quantum algebra but finds seven scope/typing defects that had to be corrected before
an independent review pin could be responsibly proposed.

1. The finite `R` qubit is a retained-register proxy, not an amplified stable
   physical record.
2. The original shared-nuisance two-by-two table was conditional on both
   readers registering; it was not itself a complete null-inclusive record
   law.
3. `W_X` is an idle external tag, not a physical controller/exchange carrier.
4. The general no-universal-matching theorem must separate states on the
   licensed physical algebra; unequal density matrices alone are insufficient
   under gauge or superselection restrictions.
5. The local-erasure theorem must type the full accessible carrier separately
   from the inaccessible record environment.
6. Complete records must be context-indexed: `X`, `Y`, and path reads on one
   source are alternative instruments, not simultaneous fields of one global
   joint law.
7. The public input packet—apparatus evidence plus registered comparison
   infrastructure—must be separated from the conventional B0 nomology and
   from the predicted output profile.

The active witness now prints those ceilings and lifts the nuisance example to
two exact normalized three-by-three tables. No massive apparatus, physical
stable record, exchange ledger, empirical transfer, gravity response, or
ontology is thereby obtained.

The author-side disposition is

$$
\boxed{
\text{FINITE ALGEBRA SURVIVES / SEVEN SCOPE-TYPING REPAIRS APPLIED / REVIEW STILL REQUIRED}.}
\tag{1}
$$

---

## 1. Audit boundary

This audit reconstructs only the finite witness and the propositions it is
used to illustrate. It is not one of the future independent mathematics,
physical-source/apparatus, or foundations/gravity-firewall seats.

It does not:

1. certify the author bundle;
2. freeze any byte;
3. award B0-L1 or B0-L2;
4. select a platform;
5. model a material reader;
6. discharge `P-B0-1`; or
7. authorize B0-E or an entrant.

---

## 2. Preparation reconstruction

Starting from `|0>` on `S`, the coherent map is

$$
R_z(\varphi)H|0\rangle
=
e^{-i\varphi/2}
\frac{|0\rangle+e^{i\varphi}|1\rangle}{\sqrt2}.
\tag{2}
$$

The prefactor is global, so the printed effective state is correct.

For the registered `X` and `Y` sign conventions,

$$
\begin{aligned}
p_X(+\mid E_\varphi)&=\frac{1+\cos\varphi}{2},\\
p_Y(+\mid E_\varphi)&=\frac{1+\sin\varphi}{2}.
\end{aligned}
\tag{3}
$$

At `varphi=pi/2`, the `X` distribution is uniform while the `Y` outcome is
deterministically plus. The claim that one quadrature can miss coherence and
a complementary quadrature can recover it is exact.

The mixed-source branch is globally

$$
\frac{|0,0\rangle_{S W_D}+|1,1\rangle_{S W_D}}{\sqrt2},
\tag{4}
$$

so it is not a global incoherent ontology. Tracing or ignoring `W_D` gives the
source-reduced mixture. The witness correctly keeps `W_D` in the complete
carrier; its `E_mix` label is therefore explicitly subsystem-relative.

---

## 3. Matched-algebra reconstruction

For

$$
A=a_0|0\rangle\langle0|+a_1|1\rangle\langle1|,
\tag{5}
$$

both the coherent source and its diagonal reduction give

$$
\operatorname{Tr}(A\rho)=\frac{a_0+a_1}{2}.
\tag{6}
$$

For

$$
X_\varphi
=
e^{-i\varphi}|0\rangle\langle1|
+e^{i\varphi}|1\rangle\langle0|,
\tag{7}
$$

the coherent expectation is one and the mixed expectation is zero. The
matched-diagonal versus coherence-sensitive distinction survives exactly.

This does not show that the complete physical preparations match. `W_D`,
controllers, supports, work, heat, and other records can distinguish them.
That ceiling is part of the result, not an experimental imperfection.

More generally, unequal matrices are physically separated only if their
restrictions to the licensed observable algebra differ. A gauge or
superselection quotient can identify matrix distinctions outside that
algebra. Here the explicitly registered complementary witness is in the
declared finite algebra and supplies the required separation.

---

## 4. Marker and eraser reconstruction

The marker is a controlled bit flip and therefore satisfies

$$
U_{\rm mark}^{-1}=U_{\rm mark},
\qquad
U_{\rm mark}^{-1}U_{\rm mark}=I.
\tag{8}
$$

Before any inaccessible copy forms, applying the inverse restores the entire
finite state and hence both quadrature laws. This is a genuine coherent
uncomputation control.

After copying the marker to `R`, the `S M R` state is

$$
\frac{|0,0,0\rangle+e^{i\varphi}|1,1,1\rangle}{\sqrt2}.
\tag{9}
$$

Tracing `M,R` gives `I/2` on `S`; a path read of `R` and an `X` read of `S`
gives four outcomes of probability `1/4`. Resetting only `R` while leaving
`M` correlated cannot restore source coherence.

Those equalities are correct. The earlier physical name was not. A single
finite qubit has no demonstrated amplification, redundancy, durability, or
stable-future category. Equation (9) supports only the phrase
"retained-register proxy." Physical record stability remains a B0-E gate.

The general erasure proposition is well typed only after the accessible
carrier `A` and inaccessible environment `E` are separated explicitly. With
orthogonal `E_L,E_R`, tracing `E` removes every phase-dependent cross term on
`A`; one fixed channel on `A` therefore cannot recover an entire family of
distinct phase states. A phase-conditioned repreparation is a different
operation, not erasure.

---

## 5. External-tag reconstruction

The two maps

$$
U^{(0)}=V_S\otimes I_{W_X},
\qquad
U^{(1)}=V_S\otimes X_{W_X}
\tag{10}
$$

produce the same reduced source unitary channel and different final `W_X`
values. This is mathematically exact.

It is also the idle-variable limit of channel nonidentifiability. `W_X` never
couples to `S`; no Hamiltonian, work functional, momentum, heat, recoil, or
boundary flux is assigned. The example establishes only

$$
\text{reduced source channel}
\not\Rightarrow
\text{complete external record}.
\tag{11}
$$

It does not positively realize Proposition B0-D's physical exchange ledger.
The witness and readiness audit now say so explicitly.

---

## 6. One-field reader reconstruction

Before reader noise is applied, one licensed context chooses at most one basis
for each role. Cross-role projectors commute because they act on separate
factors, but the witness never constructs a joint `X,Y` distribution for one
source instance. Its complete ideal law is the contextual instrument

$$
p_{\rm ideal}(z\mid b,c)
=
\operatorname{Tr}
\left[
\left(\bigotimes_{j\in J_c}\Pi_{z_j}^{(j,c)}\right)
\rho_{b,c}
\right],
\qquad z\in\mathcal Z_c.
\tag{12a}
$$

"Complete" refers to every outcome field for this `c`, not a hidden global
joint distribution over incompatible contexts.

For registration probability `eta=3/4` and conditional sign error
`epsilon=1/10`, the reader is

$$
\begin{aligned}
q(+\mid p)&=\frac3{40}+\frac35p,\\
q(-\mid p)&=\frac{27}{40}-\frac35p,\\
q(\varnothing\mid p)&=\frac14.
\end{aligned}
\tag{12}
$$

For `0<=p<=1`, the extrema are

$$
q(+)\in\left[\frac3{40},\frac{27}{40}\right],
\qquad
q(-)\in\left[\frac3{40},\frac{27}{40}\right],
\tag{13}
$$

and the sum is one. The printed coherent, antiphase, mixture, and quadrature
cases follow directly.

---

## 7. General joint-reader closure

One-field normalization does not establish that a multirecord reader is a
product. The corrected witness therefore uses a normalized stochastic kernel

$$
K_n(y\mid z),
\qquad
\sum_yK_n(y\mid z)=1,
\tag{14}
$$

where `z` is the complete ideal record, `n` is the nuisance value, and `y`
includes every registered null field. With normalized `mu(n|b,c)` and ideal
conditional law `p_ideal(z|n,b,c)`, the registered law is

$$
p_{\rm reg}(y\mid b,c)
=
\sum_{z,n}
K_n(y\mid z)\mu(n\mid b,c)p_{\rm ideal}(z\mid n,b,c).
\tag{15}
$$

Summing Equation (15) over `y` proves normalization without a fieldwise
independence assumption.

---

## 8. Null-inclusive nuisance reconstruction

Let each reader register independently with probability `3/4`. When a role
registers, let a common sign-flip bit equal one with probability `1/4`.
Starting from ideal `++`, the complete common-nuisance table is

$$
P_{\rm common}
=
\frac1{64}
\begin{pmatrix}
27&0&9\\
0&9&3\\
9&3&4
\end{pmatrix},
\tag{16}
$$

with rows and columns ordered `+, -, null`.

Replacing the common bit by two independent flip bits with the same one-role
law gives

$$
P_{\rm fact}
=
\frac1{256}
\begin{pmatrix}
81&27&36\\
27&9&12\\
36&12&16
\end{pmatrix}.
\tag{17}
$$

Both matrices sum to one. Their row and column marginals are identical:

$$
\left(\frac9{16},\frac3{16},\frac14\right).
\tag{18}
$$

Conditioning on both readers registering recovers the original two-by-two
tables:

$$
\begin{aligned}
P_{\rm common}(.\mid\mathrm{both})
&=
\begin{pmatrix}3/4&0\\0&1/4\end{pmatrix},\\
P_{\rm fact}(.\mid\mathrm{both})
&=
\begin{pmatrix}9/16&3/16\\3/16&1/16\end{pmatrix}.
\end{aligned}
\tag{19}
$$

Thus the scientific point survives and is now stated at complete-record
scope: equal one-role registered marginals do not determine the joint law.

---

### 8.1 Public input, conventional law, and output are distinct

The cross-file audit found that the earlier B0 packet placed `K_e`, the
conventional Hamiltonian/QFT/effective-kernel rule, beside measured apparatus
inputs. That conflated common input data with the law used to predict from
those data.

The corrected types are

$$
\Pi_e^{\rm in}
\ne
\mathcal B_{0,e}
\ne
\mathcal O_{0,e}.
\tag{21}
$$

`Pi_in` contains raw apparatus evidence plus the registered control, record,
nuisance, held-out, tolerance, and scoring contract. The B0 law
bundle contains the openly supplied conventional nomology and the states,
channels, and instruments it generates. `O_0,e` is the resulting contextual
record profile.

Future gravity entrants may consume the same public input packet and may independently
postulate standard quantum nomology as a charged part of their own law. They
may not receive `B_0,e` or `O_0,e` as an answer-bearing lookup field. The
reviewed B0 object remains a public recovery criterion, not forbidden
knowledge.

---

## 9. Proposition audit

| proposition | disposition | exact ceiling |
|---|---|---|
| B0-A normalization | survives | conditional on normalized seed, trace-preserving retained propagation, and complete instruments/kernels |
| B0-B no universal matching | survives after scope repair | state separation is on the licensed physical algebra; matrix inequality alone is insufficient under gauge or superselection restrictions |
| B0-C local erasure | survives | one fixed local channel without inaccessible environment or phase side input cannot restore the whole phase family |
| B0-D exchange ledger | theorem survives; witness is only a negative idle-tag control | no physical work or conservation closure in B0-T |
| B0-E residual nonidentifiability | survives as a model-class warning | unrestricted omitted kernels can mimic residuals; no claim that every physical nuisance can mimic every gravity law |

---

## 10. Remaining hostile questions for independent review

1. Is the complete ideal-record instrument specified for every context, or
   only inferred from unitary evolution plus later basis reads?
2. Does `E_mix` retain its full `W_D` provenance in every comparison that
   calls it a mixture?
3. Is the matched algebra fixed before any gravity-sensitive output exists?
4. Is the coherent eraser always prior to the stable-record boundary?
5. Does any prose still promote the retained-register proxy to a physical
   record?
6. Is the general joint kernel a genuine declared reader law rather than a
   license to choose arbitrary correlations after observing data?
7. Are nuisance kernels frozen from calibration-only evidence?
8. Does the future B0-E apparatus supply a positive controller/exchange
   realization rather than inheriting the idle tag?
9. Are sample spaces complete mutually exclusive run records rather than sums
   over simultaneous record coordinates, and are incompatible reads kept in
   different contexts?
10. Can any claimed B0-L2 coordinate be obtained only by importing an answer
    table rather than deriving it from the finite maps?
11. Are the public input packet, conventional B0 law, and predicted profile
    separate typed objects throughout the bundle, with apparatus evidence distinct
    from registered comparison infrastructure inside the packet?

---

## 11. Maximum legitimate author-side claim

> The open B0-T witness has been reconstructed exactly. Its coherent/mixed
> preparation algebra, complementary coherence reads, reversible marker,
> finite retained-register correlations, one-field noisy readers, general
> correlated-reader normalization, and common-versus-factorized nuisance
> countermodel are mathematically consistent. The complete nuisance tables
> are positive, normalized, and have identical one-role marginals. The audit
> corrects seven prior scope/typing errors: the original wording had not
> established a physical stable record or controller exchange; its joint
> nuisance reader, licensed-algebra separator, and accessible/inaccessible
> erasure carrier were incomplete; and incompatible source reads had not been
> explicitly separated into alternative contexts. It also conflated the
> public input packet with the conventional B0 nomology. These
> corrections remain unreviewed author-side
> work and earn no B0 level, apparatus, gravity, or ontology claim.
