# ISP v17 — U-Gen G1 exact controls and native-slot gate

**Status:** ACTIVE AUTHOR-SIDE CONTROL DOSSIER / NOT A PIN / NOT REVIEWED
**Date:** 2026-08-23
**Scientific result awarded:** none
**Authority created:** none

---

## 0. Purpose and bounded verdict

G1 proposes an ordinary-positive law for complete records,

$$
\Gamma_{\mathcal E}(r\mid x)
=\frac{|K_{\mathcal E}(r,x)|^2}
{\sum_{r'}|K_{\mathcal E}(r',x)|^2},
$$

with the coherent potential constructed from physical alternatives, magnitudes,
and a global action or holonomy character. This dossier executes the controls
that the candidate left open.

The result is deliberately mixed.

1. Presentation refinement, retained-record versus coherent-eraser typing,
   one connection-holonomy member, independent tensor products, and one
   recorded adaptive feed-forward process all have exact finite realizations.
2. Complex numbers are not forced as material ontology. The same scalar
   member can be written as an oriented real two-dimensional relational law.
3. The oriented relational datum cannot be removed. Positive endpoint laws
   alone admit no sequential composition rule that reconstructs held-out
   phase-sensitive continuations.
4. The scalar G1 packet is not closed under genuinely mixed or higher-rank
   boundaries. Treating a decomposition label as an actual classical source
   would add empirically unselected ontology.
5. A supplied interacting parent is easy; generating it uniformly from
   independently calibrated subsystem data is still absent.

Therefore G1 remains a bounded positive parent-law candidate and an exact
input-location result. It does not fill the native ISP slot.

---

## 1. Native-slot criterion

A candidate fills the v17 native slot only if one fixed physical rule maps a
bounded independently meaningful source packet to the complete operational
record law across held-out compositions. It may take measured masses,
couplings, fields, boundary conditions, and apparatus settings as inputs. It
may not take any of the following as disguised answers:

1. the target transition table;
2. the target amplitude kernel or decoherence functional;
3. a different fitted action or magnitude density for every held-out program;
4. a nonuniform response table whose size grows with the tested program;
5. future settings inside an earlier source packet; or
6. an interacting parent law supplied after subsystem calibration.

For a proposed rule $\mathcal F$ this is the distinction

$$
\text{physical controls}
\xrightarrow{\ \mathcal F\ }
\{\Gamma_{\mathcal E}\}_{\mathcal E}
$$

versus

$$
\{K_{\mathcal E}\}_{\mathcal E}
\xrightarrow{\ |\cdot|^2/Z\ }
\{\Gamma_{\mathcal E}\}_{\mathcal E}.
$$

The second arrow is a valid representation or compiler. Unless the family of
$K_{\mathcal E}$ is itself generated from narrower physical premises, it is
not explanatory reduction.

---

## 2. Proposition G1-G — complex notation is optional, orientation is not

Write each scalar history contribution as

$$
A_h=a_h e^{i\phi_h}.
$$

Associate the real oriented vector

$$
v_h=a_h
\begin{pmatrix}
\cos\phi_h\\
\sin\phi_h
\end{pmatrix}
\in\mathbb R^2.
$$

For all alternatives ending in one record $r$,

$$
\begin{aligned}
W_r
&=\left|\sum_{h\to r}A_h\right|^2\\
&=\left\|\sum_{h\to r}v_h\right\|^2\\
&=\sum_h a_h^2
+2\sum_{h<k}a_ha_k\cos(\phi_h-\phi_k).
\end{aligned}
$$

Thus the finite record law can be stated entirely with real magnitudes,
oriented two-vectors, inner products, and ordinary positive probabilities.
Calling a complex number material is unnecessary.

The orientation cannot be discarded, however. The cosine data of one isolated
readout do not determine how that system sits relative to a later quadrature
reference. Complex conjugation is the real-plane reflection

$$
(\cos\phi,\sin\phi)\longmapsto(\cos\phi,-\sin\phi).
$$

A simultaneous reflection of system and reference may be gauge. Reflecting
only one relative to a fixed reference can change complete probabilities.

### Corollary G1-G.1 — no endpoint-$\Gamma$ composition compiler

Let

$$
T_\theta=\operatorname{diag}(1,e^{i\theta}),
\qquad
H=\frac1{\sqrt2}
\begin{pmatrix}1&1\\1&-1\end{pmatrix}.
$$

Every $T_\theta$ has the same positive endpoint shadow,

$$
\Gamma_{T_\theta}=|T_\theta|^{\odot2}=I_2.
$$

But the fixed complete continuation $HT_\theta H$, prepared at input $0$ and
read in the computational basis, gives

$$
p(0\mid\theta)=\cos^2(\theta/2)
=\frac{1+\cos\theta}{2}.
$$

If a sequential composition operation depended only on the three endpoint
positive matrices $\Gamma_H,I_2,\Gamma_H$, its input would be identical for
all $\theta$ and its output would have to be identical. The displayed family
contradicts that conclusion.

Hence no rule on isolated endpoint $\Gamma$ matrices alone reconstructs all
phase-sensitive sequential composites. This does not forbid a direct holistic
law for each complete experiment. It proves that such a law needs relational
input absent from the isolated endpoint shadows.

---

## 3. Proposition G1-H — presentation refinement and label cloning

The physical alternative set is a quotient by pure presentation labels. If a
single physical alternative $h$ is displayed as clones $h_1,\ldots,h_n$, the
display is valid only when either all clones are identified in the quotient or
the proposed physical refinement obeys

$$
\sum_{j=1}^n a(h_j)\chi(h_j)=a(h)\chi(h).
$$

Under this condition the endpoint potential $K$, weight $W$, normalization
$Z$, and positive law $\Gamma$ are unchanged.

The hostile mutant copies the same term without quotienting or refinement:

$$
A_h\longmapsto A_{h_1}+A_{h_2}=2A_h.
$$

Its isolated intensity changes from $|A_h|^2$ to $4|A_h|^2$. That is a new
physical multiplicity, not a relabeling. G1 rejects it before normalization.

Likewise, splitting one displayed record label $r$ into $r_1,r_2$ leaves the
physical coarse record invariant only when the registered reader satisfies

$$
p(r)=p(r_1)+p(r_2).
$$

Creating two orthogonal, separately readable records is a different apparatus
and may not be smuggled in as bookkeeping.

---

## 4. Proposition G1-I — retained record and coherent eraser are distinct

Let $P$ be a two-path carrier and $M$ a memory bit. Start with

$$
|+\rangle_P|0\rangle_M
=\frac{|00\rangle+|10\rangle}{\sqrt2}.
$$

A path-controlled mark gives

$$
U_{\rm mark}:
\frac{|00\rangle+|10\rangle}{\sqrt2}
\longmapsto
\frac{|00\rangle+|11\rangle}{\sqrt2}.
$$

If $M$ is retained as a complete future-readable record and no coherent eraser
is licensed, a final $H_P$ produces a uniform path readout. The joint record
law factors through the recorded path value $y$:

$$
p(y,r)=\frac14,
\qquad y,r\in\{0,1\}.
$$

This is a valid positive division once the entire boundary is independently
certified sufficient.

If a second licensed $U_{\rm mark}$ coherently erases the mark before $H_P$,
then

$$
\frac{|00\rangle+|11\rangle}{\sqrt2}
\xrightarrow{U_{\rm mark}}
|+\rangle_P|0\rangle_M
\xrightarrow{H_P}
|0\rangle_P|0\rangle_M.
$$

The complete law gives

$$
p(P=0)=1.
$$

The false-division mutant replaces the marked carrier by the mixture

$$
\frac12|00\rangle\!\langle00|
+\frac12|11\rangle\!\langle11|.
$$

After the same eraser and readout it predicts

$$
p(P=0)=\frac12.
$$

Therefore a temporary mark is not a division merely because it can be read at
one intermediate seam. Stable record, complete sufficient boundary, and
licensed coherent eraser are separate types.

---

## 5. Proposition G1-J — exact connection-holonomy member

Consider two physical alternatives enclosing one loop. Let their relative
connection holonomy be

$$
e^{i\varphi},
\qquad
\varphi=\frac{q}{\kappa_A}\oint A
=\frac{q\Phi}{\kappa_A}
$$

in an electromagnetic member with enclosed flux $\Phi$. Choose the balanced
output potentials

$$
K_0(\varphi)=\frac{1+e^{i\varphi}}2,
\qquad
K_1(\varphi)=\frac{1-e^{i\varphi}}2.
$$

Then

$$
|K_0|^2=\frac{1+\cos\varphi}{2},
\qquad
|K_1|^2=\frac{1-\cos\varphi}{2},
$$

and their sum is exactly one. Local endpoint rephasings cancel from the loop
product. The fringe therefore depends on gauge-invariant relative holonomy,
not on an absolute potential representative.

This passes one global-sector source control conditional on the physical
connection, charge, loop, and action scale. It does not derive electromagnetism,
the charge spectrum, $\kappa_A$, or the apparatus alternatives. The loop is in
the declared experiment/configuration carrier; it is not emergent spacetime.

---

## 6. Proposition G1-K — products pass; interactions remain parent data

For independent experiments $\mathcal E_A,\mathcal E_B$, define

$$
\begin{aligned}
\mathcal H_{AB}&=\mathcal H_A\times\mathcal H_B,\\
a_{AB}(h_A,h_B)&=a_A(h_A)a_B(h_B),\\
\chi_{AB}(h_A,h_B)&=\chi_A(h_A)\chi_B(h_B).
\end{aligned}
$$

Then

$$
K_{AB}=K_A\otimes K_B,
\qquad
Z_{AB}=Z_AZ_B,
\qquad
\Gamma_{AB}=\Gamma_A\otimes\Gamma_B.
$$

Thus independently supplied G1 packets compose exactly, including in the
renormalized branch when the whole packet factorizes.

An interacting parent contains additional relational data. As an exact
control, prepare $|++\rangle$, apply

$$
C_\varphi=\operatorname{diag}(1,1,1,e^{i\varphi}),
$$

and read after $H\otimes H$. From input $00$ the four output probabilities are

$$
\begin{aligned}
p_{00}(\varphi)&=\frac{5+3\cos\varphi}{8},\\
p_{01}(\varphi)&=p_{10}(\varphi)=p_{11}(\varphi)
=\frac{1-\cos\varphi}{8}.
\end{aligned}
$$

They sum to one. The phase $\varphi$ is invisible in the isolated endpoint
shadows of the two subsystems and belongs to the interaction packet.

This constructs one supplied parent member. It does not provide a uniform rule
that derives interaction packets from subsystem $\Gamma$ laws. Supplying
$C_\varphi$ after seeing the target correlations is the tensor-oracle mutant.

---

## 7. Proposition G1-L — recorded adaptive feed-forward

At a certified recorded boundary $y$, a future instrument may depend on $y$
and on a freely supplied later setting $u$. The typed positive law is

$$
p(y,r\mid x,u)
=\Gamma_1(y\mid x)\Gamma_{2,g(y,u)}(r\mid y).
$$

No future setting appears in $\Gamma_1$.

For an exact bit control, let the first reader produce $y=0,1$ with probability
$1/2$, leaving the corresponding bit state $|y\rangle$. Apply $X^y$ and read
again. Then

$$
p(y,r)=\frac12\,\delta_{r0},
\qquad
p(r=0)=1.
$$

This passes ordinary recorded feed-forward. A coherent control whose memory is
later erased is not described by this division formula; it belongs to one
uninterrupted parent packet as in Section 4. The construction therefore does
not yet supply a general process tensor or arbitrary quantum comb.

---

## 8. Proposition G1-M — scalar rank-one ceiling

For one source and one record fiber, scalar G1 contributions induce the
fine-alternative kernel

$$
D_{hk}=A_h\overline{A_k}.
$$

Every nonzero such block has rank one. A genuinely mixed or environmentally
unresolved boundary generally has

$$
D=\sum_\alpha\lambda_\alpha
v_\alpha v_\alpha^\dagger,
\qquad
\lambda_\alpha>0,
$$

with rank greater than one.

The exact two-history example

$$
D_*=\frac12
\begin{pmatrix}1&0\\0&1\end{pmatrix}
$$

has determinant $1/4$ and rank two, so no scalar history list $A_h$ represents
it as $AA^\dagger$.

One can add a classical source label $\alpha$ and average scalar G1 members.
That is legitimate only when $\alpha$ is a physical division or contingent
classical variable. The maximally mixed qubit admits inequivalent ensemble
decompositions, so promoting an arbitrary decomposition label to actual
ontology violates representation independence.

A decomposition-independent extension needs a Gram-vector, operator, or
equivalent higher-rank boundary law. That extension is mathematically adjacent
to a decoherence functional, density operator, or process tensor and must be
charged accordingly.

### C1 relative-orientation witness

The C1 kernels

$$
D_\pm=
\begin{pmatrix}
1/2&\pm i/4\\
\mp i/4&1/2
\end{pmatrix}
$$

have determinant $3/16$ and rank two. They therefore lie outside scalar G1.
They agree on every isolated diagonal event propensity but differ after
composition with a fixed reference. A higher-rank G1 extension would have to
retain exactly this relational information; it could not reconstruct it from
the isolated positive diagonal.

This is an exact failure of the scalar mixed-boundary control, not an
implementation defect.

---

## 9. Control ledger

| Control | Exact status | Consequence |
|---|---|---|
| two-port nondivision | pass | finite interference with positive records |
| endpoint gauge | pass | absolute endpoint phase is representational |
| label cloning/refinement | pass with quotient law | presentation cannot manufacture intensity |
| retained record / coherent eraser | pass | record is not automatically a division |
| connection holonomy | conditional pass | global character has a typed physical source |
| independent tensor product | pass | factorized packets compose |
| interacting parent | supplied example only | uniform parent generation absent |
| recorded adaptation | bounded pass | classical feed-forward typed correctly |
| coherent general adaptation | open | no complete process compiler |
| mixed/higher-rank boundary | fail for scalar G1 | Gram/operator extension required |
| C1 relative orientation | fail for scalar G1 | isolated diagonals are insufficient |
| endpoint-positive no-signalling | fail for broad class | admissible local maps need stronger law |

No scientific level is awarded author-side. The evidence supports at most a
bounded G1-L1 construction plus conditional pieces of L2. G1-L3 is not met.

---

## 10. What the controls decide

The controls reject two extreme claims.

First, they reject:

> complex amplitudes must be material objects because interference uses
> complex notation.

The finite scalar law has an equivalent real oriented-plane form.

Second, they reject:

> ordinary endpoint probabilities already contain everything needed for
> composition.

They do not. The oriented relational carrier, or equivalent information, is
operationally load-bearing.

The live physical question is therefore not “real probability or complex
number?” It is:

> What covariant physical principle generates the oriented relational
> correlation law, its magnitudes, its interactions, and its actual record
> boundaries without receiving the target quantum process?

G1 has not answered that question.

---

## 11. Author-side adjudication

```text
ORDINARY POSITIVE COMPLETE RECORD LAW:       EXACT AT FINITE SCALAR SCOPE
COMPLEX MATERIAL ONTOLOGY:                   NOT REQUIRED
ORIENTED RELATIONAL COMPOSITION DATA:        REQUIRED BY HELD-OUT CONTINUATIONS
ENDPOINT-GAMMA-ONLY COMPOSITION:              IMPOSSIBLE FOR PRINTED FAMILY
PRESENTATION QUOTIENT:                       EXACT
RECORD / ERASER TYPING:                      EXACT FINITE CONTROL
CONNECTION HOLONOMY MEMBER:                  EXACT GIVEN DECLARED FIELD INPUT
INDEPENDENT PRODUCTS:                        EXACT
UNIFORM INTERACTING PARENT GENERATOR:        ABSENT
RECORDED FEED-FORWARD:                       EXACT BOUNDED CONTROL
MIXED / HIGHER-RANK BOUNDARIES:              SCALAR G1 FAILS
COMPLETE ADAPTIVE PROCESS:                   ABSENT
NATIVE ISP SLOT:                             NOT FILLED
QFT / SPACETIME / GRAVITY:                   NONE
OFFICIAL PIN / REVIEW / RESULT:              NONE
```

The next G1 move may not be another interferometer example. It must either:

1. derive at least one of $a$, $\chi$, the interaction packet, or the record
   grammar from narrower independently physical premises;
2. supply a representation-independent higher-rank and adaptive parent law
   without importing the target quantum process; or
3. state a bounded generator class and prove a class-wide no-go.

Otherwise G1 terminates as a useful zero-gain comparator and obstruction
dossier.
