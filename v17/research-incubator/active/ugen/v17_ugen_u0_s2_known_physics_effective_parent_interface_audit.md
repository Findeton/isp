# ISP v17 — U0 S2 known-physics effective-parent interface audit

**Status:** ACTIVE AUTHOR-SIDE CONDITIONAL CONTROL / NOT A NATIVE CANDIDATE / NOT REVIEWED
**Date:** 2026-08-23
**Scientific result awarded:** none
**Authority created:** none
**Implementation transfer performed:** no
**Official pin, target, Paper 04B successor, spacetime, or gravity model:** none

---

## 0. Executive result

The BFA-L2 audit proved that passive probabilities do not identify physical
interventions, system boundaries, or records, while a typed positive
complete-instrument law can have an operational quotient at genuine divisions.
This packet tests how far the promised known-physics control can actually be
constructed from the already audited R1 macroscopic-QED
source/interferometer/detector stack.

The result is deliberately weaker than a source-complete parent. One explicit
finite R1 record member can be extended to include controller, memory, eraser,
bath, null, and failure records. Exact conditional theorems then state what a
physical intervention, response port, stable record, eraser, division, and
operational quotient would require. The controller, microscopic detector,
memory, and material-erasure kernels themselves are not derived from one
autonomous physical model.

Six scoped results survive.

1. **Physical control has an exact conditional placement.** Given a physical
   controller preparation kernel, it selects real pump, arm, phase-element,
   detector-bias, reader-window, and eraser configurations. The “operation” is
   replacement of that controller source, not conditioning on its observed
   value. No microscopic controller kernel is constructed here.
2. **Intervention and conditioning are exactly separated.** A common hidden
   material/source coordinate generally makes the observational conditional
   differ from the response to a physical clamp. Equality has a precise
   covariance condition and is earned by independent randomization or a
   stronger mechanism audit.
3. **System ports have a conditional response criterion.** Independent
   preparation, block response, separate environments/readers, and
   no-interaction assumptions imply a product law. Switching on a physical
   cross-coupling requires a newly solved common parent. The assumptions are
   tests to discharge, not a derivation of ports from R1 labels.
4. **Stable records and erasers are typed at ideal effective scope.** Given a
   detector channel and identified memory sectors, stable futures preserve or
   transport them. An ideal eraser can transfer the distinction to a declared
   bath. No material eraser or event-level detector law is derived here.
5. **Divisions remain exceptional and unverified for R1.** A retained record is
   a division only when the declared boundary state is future sufficient for
   every licensed continuation. The packet states this exact criterion but
   does not establish it for a fabricated apparatus.
6. **The operational quotient is conditionally constructible, not physically
   completed.** If the controller, ports, records, erasers, and divisions pass
   the printed tests, preparations, transformations, effects, mixtures, and
   admissible compositions have an operational quotient. The quantum law and
   several interface kernels remain inputs.

The disposition is

$$
\boxed{
\begin{gathered}
\text{EFFECTIVE OUTCOME-COMPLETE FINITE MEMBER: CONSTRUCTED},\\
\text{INTERVENTION / CONDITIONING: EXACTLY SEPARATED},\\
\text{PORT / RECORD / ERASER / DIVISION: CONDITIONAL CRITERIA},\\
\text{OPERATIONAL QUOTIENT: CONDITIONALLY CONSTRUCTIBLE},\\
\text{MICROSCOPIC SOURCE-COMPLETE PARENT: NOT CONSTRUCTED},\\
\text{QUANTUM NOMOLOGY, SPACETIME, AND ACTUALITY: STILL SUPPLIED}.
\end{gathered}}
\tag{1}
$$

This reaches an exact effective-interface schema below BFA-L3. It identifies
what the known-physics control and any native proposal must still supply. It is
not evidence that either parent has already been found.

### Terminology firewall

“Outcome-complete” below means normalized over every declared click, no-click,
null, failure, controller, memory, and erasure record of the finite effective
member. It does **not** mean that microscopic controller, detector, memory,
bath, and eraser dynamics have been generated from one source-complete law.
The unqualified terms “closed parent” and “closure” are not awarded to this
packet.

### Claim-status ledger

| item | exact status |
|---|---|
| KPC-A | theorem about two laws once $\mu,K_C,P_{\rm R1}$ and clamp invariance are supplied |
| KPC-B | product implication under the complete factorization premises; no port-origin theorem |
| equations (10)--(14) | explicit normalized R1 effective finite member |
| equation (15) | instrument formula conditional on a CPTP detector channel and certified memory sectors |
| KPC-C | exact ideal dilation and typed-future lemma; no material-erasure derivation |
| KPC-D | definition and test for a division; no R1 division certified here |
| equation (19) | quotient existence conditional on physically discharged interfaces |
| equation (20) | explicit normalized record wrapper given supplied $p_C$ and $K_A$ |

No row may borrow the evidential status of another row.

---

## 1. Binding scientific boundary

1. The parent formulas are conditional effective known-physics schemas. A
   source-complete parent is not constructed, and none is the fundamental
   ontology of nature by declaration.
2. Standard quantum composition, the Born trace, $\hbar$, laboratory time,
   geometry, dimension, material response, and boundary preparations remain
   explicit inputs.
3. The controller is a physical subsystem or boundary preparation, not a
   free-floating agent or semantic `do` operator.
4. For the still-open BFA-L3 test, “autonomous” would mean no runtime external
   command after frozen initialization inside the declared laboratory
   schedule. It would not mean that external time, clocks, or cosmological
   boundary facts had been eliminated.
5. A phase plate position, arm blocker, pump waveform, detector bias, or eraser
   pulse is an admissible public fact only when independently measured and
   propagated through the fixed material rule.
6. A target unitary, transfer matrix, state, phase, POVM, click table, or
   process matrix remains forbidden input.
7. Controller values are not probabilities for mutually interfering quantum
   alternatives unless a physical controller record has made them classical.
8. No intermediate detector label is actual or divisible merely because an
   effective stochastic lift can name it.
9. Record actuality is stated conservatively: the complete parent predicts
   ordinary-positive retained-record chances. It does not derive why exactly
   one record rather than another actuality object occurs.
10. Form-neutral U0 is not replaced by a lattice, optical mode graph, field
    ontology, or detector cascade.
11. Probability identities are finite/countable at the explicit member scope;
    any continuous extension requires fixed dominating measures and regular
    conditional probabilities before equations (5)--(7) or (18) are used.
12. No implementation, empirical transfer, official pin, independent review,
    Paper 04B, chronology, spacetime, gravity, or unification claim follows.

---

## 2. Inherited source and mathematics boundary

This packet uses no new microscopic source. It inherits, without strengthening,
the version-bound primary-source claims and exact mathematics of:

1. `v17_ugen_u0_r1_macroscopic_qed_whole_boundary_member.md`;
2. `v17_ugen_u0_msource_qed_material_descent_audit.md`;
3. `v17_ugen_u0_msource_preparation_record_closure.md`;
4. `v17_ugen_u0_msource_microscopic_reader_descent.md`;
5. `v17_ugen_u0_source_to_record_common_parent_closure.md`;
6. `v17_ugen_u0_s2_configuration_to_operational_bridge_feasibility_audit.md`;
7. the Paper-03 accepted finite hybrid record semantics; and
8. the Paper-04/04B record-type and clock/controller counterexamples as hostile
   controls only.

R1 already proves exact normalization, affine mixtures, prefix autonomy,
certified independent products, the unconditioned multipair source law, and an
outcome-complete one-mode herald/interferometer/threshold record law. MRD types
the detector cascade, storage, stable-future grammar, eraser, and the exact
boundary between a final record instrument and an unjustified intermediate
classical history.

The new contribution is an exact placement schema for physical controls and
conditional criteria for the operational interfaces. It does not strengthen
MRD-L2 into microscopic reader closure.

---

## 3. Coordinates required for one fixed effective parent

The desired fixed effective nomology would be

$$
\mathcal N_{\rm KP}=
(\mathcal N_{\rm R1},\mathcal N_C,\mathcal N_D,
\mathcal N_M,\mathcal N_B),
\tag{2}
$$

where:

1. $\mathcal N_{\rm R1}$ is the Maxwell--macroscopic-QED--$\chi^{(2)}$--quantum
   operations stack already printed;
2. $\mathcal N_C$ is a fixed controller/source response-law slot;
3. $\mathcal N_D$ is a fixed field--detector--bath--circuit-law slot;
4. $\mathcal N_M$ is a memory, storage, overwrite, and read-law slot; and
5. $\mathcal N_B$ is the slot for every emitted, loss, controller, detector,
   memory, and erasure environment required for normalization.

Only the effective R1 coordinate and the finite record members below are
explicitly available. This packet does not supply source-complete microscopic
members for $\mathcal N_C,\mathcal N_D,\mathcal N_M,$ or $\mathcal N_B$.

The contingent boundary packet is

$$
B^{\rm KP}_e=
(B^{\rm R1}_e,X^D_e,\sigma_{C,e},\sigma_{M,e},\sigma_{B,e},
\alpha_e,\mathsf{Prov}_e,\mathsf{Tol}_e,\mathsf{Cost}_e).
\tag{3}
$$

Here $\alpha_e$ is a physical actuator preparation: wiring, voltage/current
source, mechanical support, switch initialization, and controller-memory state.
It is not the abstract operation's answer. A successful BFA-L3 parent would
generate all later evolution from these initialized physical coordinates.

Let $c$ denote the resulting complete controller configuration, including:

1. pump enabled/disabled and real waveform source;
2. arm open/blocked and physical blocker/loss port;
3. phase-element material position or drive;
4. detector bias, threshold, veto, and acquisition window;
5. storage retain/overwrite/erase mode; and
6. any retained-record-dependent later control policy.

Every field response, phase, loss, and reader channel is recomputed from the
physical configuration $c$. No lookup from $c$ to a target transfer matrix is
admitted.

---

## 4. Theorem KPC-A — intervention is mechanism replacement, not conditioning

Let $z$ collect common pump, material, thermal, fabrication, environment, and
controller-source coordinates present before the controller is prepared. Let
$K_C(c\mid z,\alpha)$ be the physical controller preparation kernel and let
$P_{\rm R1}(r\mid c,z)$ be the complete R1 record law for the realized
configuration. The common parent is

$$
P_{\rm KP}(c,r\mid\alpha)
=
\int \mu(dz)\,K_C(c\mid z,\alpha)
P_{\rm R1}(r\mid c,z).
\tag{4}
$$

### Observational conditional

Under a baseline actuator $\alpha_0$, for any $c$ with
$\bar K_C(c)>0$,

$$
P_{\rm obs}(r\mid c)
=
\frac{
\int\mu(dz)K_C(c\mid z,\alpha_0)P_{\rm R1}(r\mid c,z)
}{
\bar K_C(c)
},
\qquad
\bar K_C(c)=\int\mu(dz)K_C(c\mid z,\alpha_0).
\tag{5}
$$

### Physical clamp

Suppose a replacement actuator $\alpha_c$ prepares $c$ with probability one
without altering the upstream $z$ law or the downstream mechanism except
through $c$. Then

$$
P_{\rm clamp}(r\mid c)
=
\int\mu(dz)P_{\rm R1}(r\mid c,z).
\tag{6}
$$

The two laws agree for every record exactly when

$$
\int\mu(dz)
\bigl[K_C(c\mid z,\alpha_0)-\bar K_C(c)\bigr]
P_{\rm R1}(r\mid c,z)=0
\quad\forall r.
\tag{7}
$$

where the equivalence is asserted only on the observational support
$\bar K_C(c)>0$. Independent randomized controller preparation is a
sufficient, not necessary,
way to satisfy (7). A shared clock, pump, thermal controller, acquisition
channel, or feedback wire can violate it and must enter $z$. $\square$

### Physical meaning

Equation (7) is the exact BFA-A repair. The operation is not inferred from
$P(r\mid c)$. It is the new physical actuator preparation $\alpha_c$ under the
same nomology. If the actuator perturbs $z$, equation (6) must be replaced by
the full newly derived parent; silently holding the old conditional fixed is a
causal fiction.

---

## 5. Proposition KPC-B — physical port and product descent

Let two proposed modules $A$ and $B$ have independently prepared boundary
states, controller sources, environments, readers, memories, and acquisition
records. Suppose the assembled linear/material response and incoming state
factor as

$$
\mathcal L_{AB}=\mathcal L_A\oplus\mathcal L_B,
\qquad
\rho_{\partial,AB}=\rho_{\partial,A}\otimes\rho_{\partial,B},
\qquad
\sigma_{DMB,AB}=\sigma_{DMB,A}\otimes\sigma_{DMB,B}.
\tag{8}
$$

Suppose also that no pump, phase reference, bath, controller, feedback wire,
or record store is shared at the registered tolerance. Then the R1 complete
instrument factors and

$$
P_{AB}(r_A,r_B\mid c_A,c_B)
=P_A(r_A\mid c_A)P_B(r_B\mid c_B).
\tag{9}
$$

This is inherited from Corollary R1-A.2, now interpreted as a physical port
criterion. A port is an equivalence class of independently preparable boundary
conditions and separately addressable coupling/readout contexts, not a tensor
factor chosen after tomography.

If a controller switches on a cross-coupling, back-reflection, shared pump,
near field, or feedback path, equation (8) fails. The interacting operation is
obtained by solving the assembled $\mathcal L_{AB}^{\rm int}$ and its complete
environment/reader parent. Multiplying old module tables is not the parent.
$\square$

This gives a conditional physical answer to BFA-B. It does not derive why the
universe has these fields, material regions, or spacetime ports.

---

## 6. Exact one-mode controller/member law

Take the R1 one-Schmidt-mode source with $0\leq\lambda<1$ and threshold readers
$\mathcal D=\{h,0,1\}$. The controller has two physical arm configurations:

$$
\begin{array}{c|ccc}
c&q_0&q_1&q_{\rm loss}\\ \hline
O\ (\text{open},\phi=0)&1&0&0\\
B\ (\text{one arm blocked})&1/4&1/4&1/2.
\end{array}
\tag{10}
$$

The open phase is not a fitted abstract number; it is the material/geometry
setting whose Green/boundary response gives $\phi=0$ within the declared
tolerance.

For $A\subseteq\mathcal D$, define

$$
Q_c(A)
=
\left[\prod_{j\in A}(1-d_j)\right]
\frac{1-\lambda}{1-\lambda z_c(A)},
\tag{11}
$$

where

$$
z_c(A)=
(1-\eta_h)^{\mathbf1[h\in A]}
\left[
1-\eta_0q_0(c)\mathbf1[0\in A]
-\eta_1q_1(c)\mathbf1[1\in A]
\right].
\tag{12}
$$

For a click pattern $r\in\{0,1\}^{\mathcal D}$ with no-click set $Z(r)$ and
click set $C(r)$,

$$
P_c(r)
=
\sum_{A\subseteq C(r)}(-1)^{|A|}Q_c(Z(r)\cup A).
\tag{13}
$$

Theorem R1-B proves that each $P_c$ is positive and normalized. Hence every
physical controller distribution $p_C(c)$ gives one complete parent law

$$
P(c,r)=p_C(c)P_c(r),
\qquad
\sum_{c,r}P(c,r)=1.
\tag{14}
$$

Equation (14) includes vacuum, multipair, loss, dark, herald failure, double
clicks, and controller records. It also retains the darkening control: opening
the second arm changes the port-1 probability from positive to zero in the
ideal single-pair sector because the QED parent reconstructs the coherent
interaction before applying the record norm.

The controller mixture is classical only because its physical preparation and
record have been declared stable. A coherent controller must remain inside one
larger quantum parent and cannot be replaced by (14) before its own record
condition is met.

---

## 7. Conditional detector-to-memory instrument schema

If $\mathcal U_{D,c}$ is a CPTP field--detector--bath--memory channel
independently computed from a fixed detector family and controller
configuration, and if $\{\Pi_r\}$ are exhaustive orthogonal sectors on the
output memory $M$, the reduced effective instrument on the surviving field is

$$
\mathcal I_{c,r}(\rho_F)
=
\operatorname{Tr}_{D',E',M}
\left[
(I_{F'D'E'}\!\otimes\Pi_r)
\mathcal U_{D,c}(\rho_F\otimes\sigma_{D,E,M})
(I_{F'D'E'}\!\otimes\Pi_r)
\right],
\qquad
\sum_r\mathcal I_{c,r}\ \text{trace preserving}.
\tag{15}
$$

The record alphabet must include click, no click, timeout, veto, saturation,
invalid, timestamp, overwritten, erased, and status fields supported by the
physical apparatus. The full parent retains $M$; it is traced in (15) only to
display the induced field instrument. Given the inputs above, the effect
$E_{c,r}=\mathcal I_{c,r}^*(I)$ follows at effective scope; a
target-tomographed POVM earns no descent credit. Neither $\mathcal U_{D,c}$ nor
the stability of $\{\Pi_r\}$ is derived by this packet.

Equation (15) must remain unbroken across absorption, cascade, switching, and
waveform labels unless their intermediate classicalization passes the MRD
nondisturbance test for every licensed final record and future. The parent
therefore preserves the exact distinction between a calculation history and
an operational happening.

---

## 8. Lemma KPC-C — ideal stable-record and eraser typing

Let $M$ be a retained memory with orthogonal record states $|r\rangle_M$, and
let $B_E$ be an erasure bath initialized in $|0\rangle_{B_E}$. A complete
eraser dilation may act as

$$
U_E:
|r\rangle_M|0\rangle_{B_E}
\longmapsto
|e\rangle_M|r\rangle_{B_E}
\quad\text{for every }r.
\tag{16}
$$

The output states remain orthogonal globally, so $U_E$ extends to an isometry
and, with a sufficient complement, a unitary. The reduced memory channel maps
every $r$ to the erased sector $e$, while the complete parent retains the
distinction in $B_E$.

Let $P_r$ select record sector $r$. A licensed stable future $F:S\to T$ must
satisfy

$$
P^{(T)}_{\tau_F(r)}F=FP^{(S)}_r
\tag{17}
$$

for a declared transport map $\tau_F$. The eraser (16) is an executable arrow
in the broader category but is excluded from $\mathsf{Fut}_{\rm stable}$.

Therefore:

1. retention and erasure are different physical futures;
2. operational deletion does not imply global information destruction;
3. heat, bath, phonon, emitted-field, overwrite, and controller traces belong
   to the complete parent when relevant; and
4. stability is relative to a typed future grammar, not absolute metaphysical
   permanence. $\square$

This ideal typed construction avoids the illegal-endomorphism defect caught in
Paper 13C and the coherent-pointer/classical-record mismatch caught in Paper
04. It does not establish a material eraser or a thermodynamic cost law.

---

## 9. Criterion KPC-D — certified division without universal Markovization

Let $h$ be the complete past before a retained boundary and let
$m=(r,s_D,s_M,s_B)$ be the declared boundary state: stable record, detector
recovery state, memory state, and every future-accessible environment
coordinate. Define the boundary to be a certified division for a licensed
future family $\mathfrak F$ exactly when

$$
P(f\mid h,m,F)=P(f\mid m,F)
\quad
\text{for every }F\in\mathfrak F
\text{ and every positive-probability }(h,m).
\tag{18}
$$

When (18) holds, the later complete-record law composes through $m$ and the
stable record supports adaptive control. When it fails, one of the following
is true:

1. omitted past information remains future accessible;
2. the proposed detector/memory state is not sufficient;
3. a shared source or controller leak remains;
4. a coherent reversal accesses the alleged record environment; or
5. prefix autonomy itself fails after those controls are exhausted.

In cases 1--4 the correct repair is to enlarge the physical parent or deny the
division, not to insert a hidden Markov state and call it ontology. Equation
(18) licenses composition only at the proved boundary and makes no claim about
unrecorded absorption, path, cascade, or field alternatives. $\square$

---

## 10. Conditional operational quotient

Define the effective objects and arrows as follows.

| operational coordinate | required effective-parent origin |
|---|---|
| system/interface | independently preparable response port satisfying the KPC-B tests |
| preparation | physical boundary/controller source kernel under $\mathcal N_{\rm KP}$ |
| transformation | complete parent between typed ports, including all environment outputs |
| effect | stable record sector of an independently derived instrument |
| sequential composition | gluing only at a KPC-D certified division |
| parallel composition | KPC-B certified independent product |
| interacting composition | newly solved assembled material/QED parent |
| coarse-graining | union of physical record sectors |
| convex mixture | physical randomizer with provenance included |
| identity | certified transport/no-operation parent on the admitted interface |

If all rows in the table have been physically discharged, two such instruments
are equivalent exactly when every licensed
closing context gives the same complete record law. By Proposition BFA-E, the
quotient is an operational-probabilistic theory:

$$
\mathsf O_{\rm KP}
=
\mathfrak N_{\rm KP}/\!\sim_{\rm op}.
\tag{19}
$$

The quotient operation itself is not circular: its inputs must already be
physical controller, port, instrument, and record objects. This packet has not
derived every such input. Even a completed effective construction would remain
circular as a *native quantum explanation* because $\mathcal N_{\rm KP}$
contains quantum composition and the Born trace.

The CDP principles may be evaluated on $\mathsf O_{\rm KP}$, but they are not
claimed to have been derived from the positive record law. In a bounded finite
quantum domain the representation is quantum because the parent was quantum.

---

## 11. One exact controller--record--eraser law

Let $a\in\{R,E\}$ be a physical retain/erase controller outcome with a supplied
normalized effective kernel $K_A(a\mid c,r)$. Let $m$ be the final accessible
memory record and $b$ the erasure-bath label. The complete finite law is

$$
\begin{aligned}
P(c,r,a,m,b)
={}&p_C(c)P_c(r)K_A(a\mid c,r)\\
&\times
\begin{cases}
\mathbf1[m=r]\mathbf1[b=0],&a=R,\\
\mathbf1[m=e]\mathbf1[b=r],&a=E.
\end{cases}
\end{aligned}
\tag{20}
$$

Every factor is nonnegative. Summing over $m,b$ gives one, then summing $a$,
$r$, and $c$ successively gives one. Thus (20) is a normalized ordinary-
positive record law for the controller, detector, memory, and erasure bath.

If only the accessible memory is read, every erased branch has $m=e$. If the
complete bath is included, distinct pre-erasure records remain distinguishable.
This is not a paradox: “erased from the working memory” and “identical complete
physical history” are different claims.

Equation (20) is deliberately not a law on resolved optical paths. The
interferometer alternatives remain inside the R1 coherent parent until the
stable detector record is formed. Normalization of (20) does not derive the
material controller that realizes $K_A$.

---

## 12. Derived, assumed, and unselected ledger

### Explicitly derived at effective or mathematical scope

1. pump field from the real source/current and material Green response;
2. nonlinear pair kernel and unconditioned vacuum/multipair source;
3. blocked/open interaction and physical loss outputs;
4. the exact one-mode complete record law (10)--(14);
5. the intervention/conditioning equality criterion (7);
6. the product implication (9) under the complete factorization premises (8);
7. the ideal eraser dilation and stable-future typing (16)--(17);
8. the division criterion (18);
9. the normalized controller--record--eraser wrapper (20); and
10. existence of the operational quotient (19) conditional on every required
    interface object being supplied and certified.

### Assumed effective known-physics input

1. complex quantum state/channel composition, the Born trace, and $\hbar$;
2. field/operator framework, vacuum/thermal boundary, and regulator;
3. laboratory causal order, time, dimension, geometry, and distances;
4. material constitutive response, nonlinear susceptibility, and effective
   constants not descended from deeper matter physics;
5. $\mathcal N_C,\mathcal N_D,\mathcal N_M,$ and $\mathcal N_B$ or their
   effective controller, detector, memory, bath, and eraser kernels;
6. controller and detector calibration, tolerances, and initial states;
7. the physical factor/port candidates and assembled apparatus;
8. exact or effective record-sector identification and future grammar;
9. low-entropy preparation/reset facts; and
10. the experimental domain and resource budget.

### Still unselected

1. a form-neutral actual configuration carrier;
2. a target-independent ordinary-positive law generating quantum orientation;
3. why the quantum correlation boundary rather than another GPT is realized,
   unless the Q-source principles are separately adopted;
4. why exactly one retained record is actual;
5. a microscopic detector cascade uniquely identified by records;
6. fundamental time, spacetime, dimension, metric, or gravity coupling; and
7. any prospective deviation from standard QFT.

The generative direction remains

$$
\text{material packet + supplied quantum/spacetime nomology}
\longrightarrow
\text{effective outcome-complete members}
\longrightarrow
\text{typed quotient if interface criteria pass}
\longrightarrow
\text{positive stable-record law}.
\tag{21}
$$

The native programme must replace the first supplied arrow, not relabel the
last output.

---

## 13. Exact bridge score

| BFA gate | status in this control | ceiling |
|---|---|---|
| B1 source-complete parent | **not constructed** | only an effective finite member and named kernel slots |
| B2 intervention descent | KPC-A exact | controller mechanism and independence audit required |
| B3 system descent | KPC-B conditional | ports depend on supplied material/spacetime response |
| B4 record descent | equation (15) plus KPC-C conditional | channel, sectors, microscopic detector law, and empirical stability incomplete |
| B5 division discipline | KPC-D exact criterion | actual future-sufficiency must be tested |
| B6 operational quotient | existence conditional by equation (19) | interface objects and context domain supplied |
| B7 Q-layer origin | not earned | quantum nomology is input |
| B8 material source completion | R1 effective only | deeper matter/native map absent |
| B9 actuality/resources | explicit debt and ledger | one-record actuality unselected |
| B10 prospective exposure | protocol only | no implementation transfer run |

The result therefore remains below BFA-L3. It is an exact conditional
effective-interface audit, not a source-complete known-physics control, native
candidate, or empirical award.

---

## 14. Prospective controls and falsifiers

1. **Controller confounding:** observational conditioning and independently
   randomized physical clamping disagree beyond the frozen model.
2. **Actuator backaction:** the clamp changes pump, thermal, phase-reference,
   detector, or acquisition coordinates omitted from $z$.
3. **Port failure:** nominally independent modules retain shared-environment or
   cross-response signatures.
4. **Interaction failure:** assembled response is not predicted by the fixed
   material/QED parent.
5. **Record instability:** independent later readers disagree or lawful
   futures alter the record outside the transport map.
6. **Coherent reversal:** an admitted future makes pre-record alternatives
   interfere, defeating the proposed division.
7. **Hidden memory:** restart predictions fail until an undeclared detector,
   bath, or controller state is restored.
8. **Eraser leakage:** the purported complete law deletes a distinction without
   bath, emitted-field, heat, overwrite, or other output.
9. **Reader origin failure:** target-complete POVM tomography is required for
   prediction.
10. **Source origin failure:** target clicks are needed to fit $\lambda$, phase,
    mode shape, or brightness.
11. **Quantum-framework failure:** a complete-record deviation survives every
    source, reader, leakage, drift, and approximation audit.
12. **Transfer failure:** a new component order, pump/temperature point,
    detector lot, or controller implementation requires refitting.

No item has been tested by this packet. These are prospective requirements,
not claimed anomalies.

---

## 15. Hostile controls

1. call $P(r\mid c)$ the intervention response without equation (7);
2. hide a shared pump, clock, thermal controller, or acquisition channel;
3. treat the controller label as a transfer-matrix lookup key;
4. fit the physical clamp after target opening;
5. call a quantum-coherent control register a classical mixture;
6. use file order as controller chronology;
7. call a Hilbert tensor factor a physical port;
8. multiply module channels when an interacting common parent is required;
9. omit controller, null, invalid, timeout, or failure records;
10. insert a fitted phase or visibility instead of material/geometry response;
11. supply a heralded state rather than the unconditioned parent;
12. truncate vacuum or multipair branches;
13. supply a target-tomographed detector POVM;
14. call absorption, hotspot, switching, or threshold crossing a division
    without the all-futures test;
15. omit detector recovery, afterpulse, drift, or memory state;
16. declare a record stable under an untyped future set;
17. implement erasure as $r\mapsto e$ while deleting the environment output;
18. infer thermodynamic heat from logical erasure without a physical model;
19. infer one actual outcome from the Born record law;
20. call the exact positive wrapper a native stochastic derivation;
21. call quantum Darwinism a derivation of the quantum parent;
22. call a finite optical mode decomposition fundamental discreteness;
23. call scheduled laboratory time emergent time;
24. claim QP1--QP6 were derived because the parent is quantum;
25. use a new detector or component lot as an uncharged law index;
26. repair source or reader mechanisms after seeing the residual;
27. use an implementation simulation as empirical transfer;
28. infer Hilbert ontology from the success of known QFT;
29. infer failure of positive ontology from the supplied quantum inputs;
30. promote this control into an official pin, Paper 04B, spacetime, or gravity.

---

## 16. Outcome ladder

### KPC-L0 — ill-typed parent

The controller, detector, memory, eraser, or complete record law is not one
normalized typed parent.

### KPC-L1 — settings relabeled

Programme values are included as labels but physical intervention and
conditioning remain conflated.

### KPC-L2E — effective interface schema

One outcome-complete finite record member and controller--eraser wrapper are
normalized. Intervention, product, eraser, division, and quotient statements
are exact conditional criteria. Microscopic controller/reader/memory kernels,
physical port discharge, actual divisions, and transfer remain unproved.

### KPC-L3 — known-physics interface descent

One independently specified effective QFT/material parent supplies controller,
detector, memory, bath, and eraser kernels; passes the physical port and
division tests; and produces the complete operational quotient while every
quantum/spacetime input remains explicit.

### KPC-L4 — implementation transfer

The frozen parent predicts held-out complete records and a separately built
controller/detector implementation without target-complete fit.

### KPC-L5 — native replacement

A narrower form-neutral ordinary-positive indivisible law generates the
oriented quantum/material response, interfaces, and actuality rather than
receiving the quantum parent.

### KPC-L6 — relativistic/gravity-ready rival

The native law survives QFT/gauge/continuum controls and makes a distinct
reciprocal gravity-sensitive prediction while recovering established limits.

Present author-side ceiling:

$$
\boxed{\text{KPC-L2E EXACT EFFECTIVE-INTERFACE AUDIT / BFA-L3 OPEN}.}
\tag{22}
$$

---

## 17. Maximum legitimate claim

> The audited macroscopic-QED stack supplies an exact outcome-complete
> one-mode record member. Given declared controller and eraser kernels it also
> supplies a normalized controller--record--memory--bath wrapper. Exact
> theorems separate physical mechanism replacement from observational
> conditioning, derive product response under complete factorization premises,
> type ideal stable-record transport and information-preserving erasure, state
> the all-futures division criterion, and prove that certified interface
> objects admit an operational quotient without Markovizing unresolved
> interiors. The packet does not derive the controller, microscopic detector,
> memory, bath, or material-erasure laws, does not verify a division, and does
> not complete BFA-L3. Complex quantum composition, the Born rule, $\hbar$,
> laboratory spacetime, material response, boundary preparations, effective
> reader sectors, and record actuality remain inputs or debts. No
> implementation transfer, native configuration law, quantum-origin result,
> one-outcome derivation, candidate, pin, review, Paper 04B successor,
> spacetime, gravity, unification result, or scientific award follows.

---

## 18. Localized native replacement problem

The audit exposes one explicit object a native proposal must replace. No
minimality theorem is claimed. A proposal need not rediscover laboratory
nouns, but it must provide one fixed map

$$
\mathcal S_{\mathcal N}^{\rm native}:
(\text{source relations},\text{material couplings},\text{boundary facts})
\longmapsto
(P_{\rm parent},\mathsf{Ports},\mathsf{Records},\mathsf{Div})
\tag{23}
$$

such that:

1. the same law predicts the blocked/open darkening pair;
2. controller clamping obeys the KPC-A mechanism distinction;
3. independent and interacting parents obey KPC-B;
4. stable records and erasers obey KPC-C;
5. only KPC-D boundaries divide;
6. the operational quotient satisfies the selected Q-layer principles or
   predicts their failure;
7. no target wavefunction, Hamiltonian, action, phase, process, quantum
   current, POVM, or answer-equivalent sector enters;
8. one-run actuality and all resources are explicit; and
9. a held-out transfer or prospective complete-record deviation separates it
   from the completed known-physics KPC-L3 control.

The next investigation should therefore compare the irreducible inputs in
$\mathcal N_{\rm KP}$ against observed symmetry, conservation, response,
composition, and record facts and ask which smallest subset can determine the
cross-context quantum response, represented but not ontologically selected by
the oriented Gram envelope. It must not guess a lattice, path ontology, or
gravity action merely to fill equation (23).

---

## 19. Author-side adversarial reconstruction

The packet was rebuilt after an external review warned that effective record
closure had been conflated with microscopic physical closure. The following
checks were repeated from the printed premises.

1. Rearranging (5)--(6) gives (7) if and only if $\bar K_C(c)>0$; the missing
   support restriction is now explicit.
2. Equation (9) follows only after every R1-A.2 factor is supplied. It does not
   infer the factorization or ports from the observed product law.
3. Equations (11)--(13) are exactly the R1-B geometric-mixture and
   inclusion--exclusion construction; (14) adds only a supplied normalized
   controller distribution.
4. Equation (15) is CP and its sum is trace preserving when
   $\mathcal U_{D,c}$ is CPTP and the output-memory sectors are exhaustive. It
   is not a construction of either premise.
5. The images in (16) are mutually orthogonal, so the displayed map is an
   isometry on the declared input subspace and can be extended to a unitary
   with adequate complement. No material implementation follows.
6. Equation (18) is the certification definition; no R1 boundary was shown to
   satisfy it.
7. In (20), summing $m,b$, then $a$, then $r,c$ gives one. This proves wrapper
   normalization, not controller or eraser source descent.

The attempted KPC-L3 claim therefore fails by missing physical premises rather
than by a defect in the surviving conditional mathematics. The correct ceiling
is KPC-L2E.
