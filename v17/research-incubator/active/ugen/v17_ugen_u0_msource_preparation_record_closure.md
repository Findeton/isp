# ISP v17 — U0 M-source preparation and complete-record closure

**Status:** ACTIVE AUTHOR-SIDE PREPARATION/RECORD CONSTRUCTION / NOT A PIN / NOT REVIEWED
**Date:** 2026-08-23
**Scientific result awarded:** none
**Authority created:** none
**Implementation or target opened:** no
**Native U0 candidate constructed:** no

---

## 0. Executive verdict

The material-descent audit constructed a conditional map from physical
geometry, causal response, nonlinear susceptibility, and a real pump waveform
to a quantum optical source kernel. It left two conspicuous slots: the
incoming preparation and the detector-to-record map.

This packet closes a bounded part of those slots without fitting a density
matrix.

For an undepleted-pump SPDC source whose Green/QED kernel has a finite or
controlled Schmidt/Bloch--Messiah decomposition, the derived mode gains
\(r_k\) determine

$$
\lambda_k=\tanh^2 r_k,\qquad 0\leq\lambda_k<1.
\tag{1}
$$

The unconditioned pair-number vector has the exact normalized law

$$
P(\mathbf n)
=
\prod_{k=1}^{K}
(1-\lambda_k)\lambda_k^{n_k},
\qquad
\mathbf n=(n_1,\ldots,n_K)\in\mathbb N^K.
\tag{2}
$$

Thus vacuum, one-pair, multipair, and every herald failure remain in one
parent. No heralded state is supplied.

With independently calibrated threshold-reader efficiency
\(\eta_{j,k}\) and per-window dark probability \(d_j\), the full
herald/signal click table follows from the probability-generating function

$$
\mathcal G(\mathbf z)
=
\prod_{k=1}^{K}
\frac{1-\lambda_k}{1-\lambda_k z_k}.
\tag{3}
$$

For example, the joint no-click probability is

$$
P_{00}
=
(1-d_h)(1-d_s)
\prod_{k=1}^{K}
\frac{1-\lambda_k}
{1-\lambda_k(1-\eta_{h,k})(1-\eta_{s,k})}.
\tag{4}
$$

The other three outcomes follow by exact inclusion--exclusion and sum to one.
The heralded conditional law is derived only after this unconditioned parent
is printed.

This is a real preparation advance. The gain spectrum is generated by the
physical source kernel rather than target coincidence fitting, and the law
predicts multipair contamination and no-click records. It still has strict
ceilings:

1. the incoming signal/idler vacuum or thermal boundary is a QFT
   preparation premise;
2. the undepleted-pump Gaussian approximation is not universal;
3. a finite mode truncation needs an error bound;
4. reader efficiencies, dark behavior, and memory are local physical inputs;
5. threshold readers do not reveal the microscopic amplification law;
6. source and reader parameters are not identifiable from target clicks
   alone; and
7. the construction assumes background time, modes, and the quantum
   framework.

The disposition is

$$
\boxed{
\begin{gathered}
\text{UNCONDITIONED VACUUM+MULTIPAIR SOURCE LAW: DERIVED CONDITIONALLY,}\\
\text{HERALD/SIGNAL COMPLETE CLICK TABLE: EXACT GIVEN LOCAL READER INPUTS,}\\
\text{FITTED HERALDED STATE: NOT REQUIRED,}\\
\text{MICROSCOPIC PREPARATION AND DETECTOR ACTUALITY: NOT CLOSED.}
\end{gathered}}
\tag{5}
$$

This packet remains an effective-QED preparation/readout control. It is not a
native Barandes law or an empirical result.

---

## 1. Binding boundary

1. The Q-source and material-descent packets remain conditional controls.
2. No target state vector, density matrix, source gain spectrum, joint
   spectral amplitude, detector POVM, coincidence table, or visibility curve
   may enter.
3. Every \(\lambda_k\) must descend from the frozen physical
   Green/nonlinear/pump rule and not from held-out clicks.
4. Reader efficiency and noise may enter only as independently sourced local
   calibration with an explicit reader-origin debt.
5. All vacuum, no-pair, multipair, no-click, dark, invalid, saturation,
   timeout, and reset outcomes remain in the parent record alphabet.
6. Conditioning on a herald never erases the unconditioned parent.
7. A per-window threshold model is not silently extended across detector
   dead time or afterpulsing.
8. The finite mode model is an effective representation, not fundamental
   discreteness.
9. Complex squeezing phases may be derived and used for downstream
   interference, but they are not actual probabilities or primitive inputs.
10. No spacetime, QFT, ontology, or gravity claim follows.
11. No official pin, review, implementation, target, or scientific result is
    opened.

---

## 2. Primary-source boundary

The following primary sources were checked on 2026-08-23.

1. James Schneeloch et al.,
   [*Introduction to the Absolute Brightness and Number Statistics in
   Spontaneous Parametric Down-Conversion*](https://arxiv.org/abs/1807.10885),
   arXiv:1807.10885. It derives source brightness and treats multipair number
   statistics using the multimode squeezed-vacuum approximation, including
   pump-state and depletion limits.
2. James Schneeloch,
   [*Comments on: Introduction to the Absolute Brightness and Number
   Statistics in Spontaneous Parametric
   Down-Conversion*](https://arxiv.org/abs/2503.02062),
   arXiv:2503.02062. It corrects a quantization-dimension notation issue and
   an order-unity normalization factor in the focused-Gaussian absolute-rate
   comparison.
3. Morgan W. Mitchell,
   [*Parametric down-conversion from a wave-equations approach: geometry and
   absolute brightness*](https://arxiv.org/abs/0807.3533),
   arXiv:0807.3533v3.
4. Aleksa Krstić, Frank Setzpfandt, and Sina Saravi,
   [*Non-perturbative theory of spontaneous parametric down-conversion in
   open and dispersive optical systems*](https://arxiv.org/abs/2306.00781),
   arXiv:2306.00781v3.
5. Erik Fitzke et al.,
   [*Time-dependent POVM reconstruction for single-photon avalanche photo
   diodes using adaptive regularization*](https://arxiv.org/abs/2110.10484),
   arXiv:2110.10484.
6. Alejandro Simon et al.,
   [*Ab initio modeling of nonequilibrium dynamics in superconducting
   detectors and qubits*](https://arxiv.org/abs/2501.13791),
   arXiv:2501.13791v3.
7. The v17 Q-source and M-source material-descent audits, which bind the
   conditional operational framework and physical Green/QED source map.

The 2025 correction is scientifically material. No absolute-brightness
normalization from the earlier tutorial may be treated as immutable without
checking the corrected convention. The number-law derivation below is
independent of that particular order-unity rate convention once the physical
gain spectrum is fixed by a version-bound source rule.

---

## 3. Source kernel to independent mode pairs

Let the material-descent packet derive a finite-band pair kernel

$$
\Phi_e(\alpha,\beta)
\tag{6}
$$

between signal and herald/idler one-particle port spaces. At low gain it is
the Green/nonlinear/pump overlap. At higher undepleted gain, use the
Bogoliubov input-output kernel.

Under the admitted compact/truncated model, take a singular decomposition

$$
\Phi_e
=
\sum_{k=1}^{K}
\sigma_k\,
u_k\otimes v_k,
\tag{7}
$$

or the corresponding Bloch--Messiah decomposition of the full
Bogoliubov map. The physical source rule fixes the gain

$$
r_k
=
\mathcal R_{\rm QED}
(\sigma_k,J_p,\mathcal Q,\chi^{(2)},\mathsf{Bnd}),
\tag{8}
$$

with no target count fit. In the simplest normalized undepleted-pump
representation, the unconditioned output is

$$
\lvert\Psi_e\rangle
=
\bigotimes_{k=1}^{K}
\left[
\sqrt{1-\lambda_k}
\sum_{n=0}^{\infty}
\lambda_k^{n/2}e^{in\varphi_k}
\lvert n\rangle_{h,k}\lvert n\rangle_{s,k}
\right].
\tag{9}
$$

The phase \(\varphi_k\) descends from the same real pump/reference and
material response as the source kernel. Pair-number statistics depend only
on \(\lambda_k\); downstream coherent networks can remain sensitive to
relative \(\varphi_k\).

Equation (9) is a secondary Hilbert representation. The operational output
needed here is the positive law (2).

### Proposition PRC-A — unconditioned normalization

For finite \(K\) and \(0\leq\lambda_k<1\), equation (2) is a normalized
ordinary-positive probability law.

### Proof

Every factor is nonnegative and

$$
\sum_{n_k=0}^{\infty}
(1-\lambda_k)\lambda_k^{n_k}
=1.
$$

Finite products of normalized factors are normalized. \(\square\)

### Pair-number generating function

Let \(N=\sum_kn_k\). Then

$$
G_N(z)
=
\mathbb E[z^N]
=
\prod_{k=1}^{K}
\frac{1-\lambda_k}{1-\lambda_k z},
\qquad |z|<\min_k\lambda_k^{-1}.
\tag{10}
$$

Consequently,

$$
\mathbb E[N]
=
\sum_k\frac{\lambda_k}{1-\lambda_k},
\qquad
\operatorname{Var}(N)
=
\sum_k\frac{\lambda_k}{(1-\lambda_k)^2}.
\tag{11}
$$

These are source predictions if and only if the \(\lambda_k\) descend from
the physical source packet.

---

## 4. Vacuum, one-pair, and multipair are one law

The exact probabilities are:

$$
P(N=0)
=
\prod_k(1-\lambda_k),
\tag{12}
$$

$$
P(N=1)
=
\left[
\prod_k(1-\lambda_k)
\right]
\sum_j\lambda_j,
\tag{13}
$$

and

$$
P(N\geq2)
=
1-P(N=0)-P(N=1).
\tag{14}
$$

No low-gain truncation may silently renormalize the \(N=0,1\) sectors and
discard (14). A first-order biphoton amplitude can approximate conditional
shape while failing absolute multipair prediction.

### Low-gain expansion

If every \(\lambda_k\ll1\),

$$
P(N=0)
=
1-\sum_k\lambda_k+O(\lambda^2),
\qquad
P(N=1)
=
\sum_k\lambda_k+O(\lambda^2).
\tag{15}
$$

The \(O(\lambda^2)\) terms are physical multipair records, not numerical
noise.

### Pump-depletion wall

Equation (9) assumes the pump is classical/undepleted or otherwise supports
the printed Gaussian input-output map. When depletion, pump quantization, or
strong non-Gaussian dynamics matters, the source operation must be replaced
by the full fixed source law. Refitting \(\lambda_k\) at each pump power is
not a repair.

---

## 5. Threshold reader law

For reader \(j\in\{h,s\}\), define:

1. \(\eta_{j,k}\in[0,1]\), the independently calibrated probability that one
   photon in source mode \(k\) produces a registered event within the
   declared window, including admitted path loss; and
2. \(d_j\in[0,1]\), the independently calibrated probability of at least one
   dark registered event in that window when no source photon is registered.

Conditional on the pair vector \(\mathbf n\), the idealized independent
threshold law is

$$
q_j(\mathbf n)
\equiv
P(r_j=0\mid\mathbf n)
=
(1-d_j)
\prod_{k=1}^{K}
(1-\eta_{j,k})^{n_k}.
\tag{16}
$$

The click probability is \(1-q_j(\mathbf n)\).

Equation (16) is a local effective reader model. It is not a microscopic
derivation of absorption, avalanche/hotspot formation, electronics, or
timestamping.

### Marginal no-click probabilities

Average (16) over (2):

$$
P_{0*}
\equiv
P(r_h=0)
=
(1-d_h)
\prod_k
\frac{1-\lambda_k}
{1-\lambda_k(1-\eta_{h,k})},
\tag{17}
$$

$$
P_{*0}
\equiv
P(r_s=0)
=
(1-d_s)
\prod_k
\frac{1-\lambda_k}
{1-\lambda_k(1-\eta_{s,k})}.
\tag{18}
$$

Assuming conditionally independent local readers given \(\mathbf n\), the
joint no-click probability is equation (4).

### Complete four-outcome table

Define the first bit as herald and the second as signal. Then

$$
\begin{aligned}
P_{00}&=P(r_h=0,r_s=0),\\
P_{01}&=P_{0*}-P_{00},\\
P_{10}&=P_{*0}-P_{00},\\
P_{11}&=1-P_{0*}-P_{*0}+P_{00}.
\end{aligned}
\tag{19}
$$

### Proposition PRC-B — positive normalized click law

Under (2) and conditionally independent reader branches (16), all four
entries in (19) are nonnegative and sum to one.

### Proof

For each \(\mathbf n\), the two local Bernoulli reader laws define a positive
normalized four-outcome product distribution. Equation (19) is its convex
mixture with weights (2). Convex mixtures preserve positivity and
normalization. \(\square\)

The proof does not establish conditional reader independence in a real
apparatus. Shared electronics, crosstalk, common backgrounds, or source-reader
feedback require a joint reader parent.

---

## 6. Exact rational witness

Take one source mode with

$$
\lambda=\frac14,\quad
\eta_h=\frac34,\quad
\eta_s=\frac23,\quad
d_h=\frac1{100},\quad
d_s=\frac1{50}.
\tag{20}
$$

Equations (4), (17)--(19) give

$$
\begin{aligned}
P_{00}&=\frac{43659}{58750},\\
P_{01}&=\frac{2871}{58750},\\
P_{10}&=\frac{18963}{323125},\\
P_{11}&=\frac{48247}{323125}.
\end{aligned}
\tag{21}
$$

All four are strictly positive and their exact sum is one. This is an
ordinary-positive complete source/readout law with vacuum, multipair, dark,
loss, and click contributions implicitly summed rather than discarded.

The witness is arithmetic validation of the formula, not evidence about a
laboratory.

---

## 7. Herald conditioning without ontological deletion

The probability of a herald click is

$$
P(h=1)=1-P_{0*}.
\tag{22}
$$

For \(P(h=1)>0\), the conditional pair-vector law is

$$
P(\mathbf n\mid h=1)
=
\frac{
P(\mathbf n)
\left[
1-(1-d_h)
\prod_k(1-\eta_{h,k})^{n_k}
\right]
}{
1-P_{0*}
}.
\tag{23}
$$

The conditional probability of a signal click is

$$
P(s=1\mid h=1)
=
\frac{P_{11}}{1-P_{0*}}.
\tag{24}
$$

Equations (23)--(24) are derived from the parent. They do not say:

1. that exactly one pair was produced;
2. that the signal has a pure state;
3. that no unregistered herald photon existed;
4. that dark heralds are impossible;
5. that conditioning physically erased failed runs; or
6. that the herald boundary is fundamental.

Any claimed heralded purity must include the mode, multipair, loss, and dark
terms that survive (23).

---

## 8. Source-reader nonidentifiability theorem

The complete formula also proves why the physical source and reader cannot be
learned from one click rate alone.

For a one-mode source with \(d=0\), the no-click probability is

$$
q(\lambda,\eta)
=
\frac{1-\lambda}{1-\lambda(1-\eta)}.
\tag{25}
$$

### Proposition PRC-C — brightness/efficiency ambiguity

The distinct parameter pairs

$$
(\lambda,\eta)
=
\left(\frac15,1\right)
\quad\text{and}\quad
\left(\frac13,\frac12\right)
\tag{26}
$$

both give

$$
q=\frac45.
\tag{27}
$$

Therefore a target no-click rate does not identify source gain and detector
efficiency separately.

### Consequence

1. Fitting \(\lambda\) from the target after assuming \(\eta\) is not source
   descent.
2. Fitting \(\eta\) from the target after assuming \(\lambda\) is not reader
   descent.
3. Independent source and reader measurements, or a deeper joint physical
   model, are mandatory.
4. More target settings may shrink the fiber but do not by themselves prove
   source lineage.

Dark probability, mode number, spectral efficiency, afterpulsing, and
background correlations enlarge the ambiguity.

---

## 9. Mode truncation and unresolved structure

The finite \(K\) representation is admissible only with an error account.
Let the exact compact source kernel have singular gains
\(\{\lambda_k\}_{k\ge1}\), and retain the first \(K\). The omitted mean
pair number is

$$
\epsilon_N^{(K)}
=
\sum_{k>K}\frac{\lambda_k}{1-\lambda_k}.
\tag{28}
$$

For bounded reader efficiencies, \(\epsilon_N^{(K)}\) controls but does not
by itself equal the click-law error. A future executable packet must propagate
the omitted-mode contribution through the actual filters and readers.

### Three different unresolved cases

1. **Mathematical truncation:** the source kernel has known tail bounds and
   \(K\) is a numerical approximation.
2. **Physical coarse graining:** multiple source modes are intentionally
   aggregated by a reader with verified mode-insensitive response.
3. **Missing physics:** unmodeled spectral, spatial, polarization, temporal,
   pump, fluorescence, or background modes affect the records.

Only the first two can support a controlled prediction. Calling the third a
larger \(K\) after target opening is post-hoc repair.

### Mode-blind total-count special case

If \(\eta_{j,k}=\eta_j\) for every retained mode, the reader depends only on
total \(N\), and (3) reduces every threshold probability to evaluations of
\(G_N(z)\). This is a useful theorem control, not a default fact about real
detectors.

---

## 10. Temporal windows, detector memory, and afterpulsing

Equations (16)--(19) describe one window with no reader memory carried from
another window. A real complete record requires a typed state.

Let \(m_{j,t}\) be the physical state of reader \(j\) at the start of window
\(t\), including ready/recovery, bias, trapped carriers or quasiparticles,
electronics state, and any retained afterpulse variable. Let

$$
K_j(m'_{j},r_j\mid m_j,\mathbf n_t,c_t)
\tag{29}
$$

be a normalized local effective transition/readout kernel. With an initial
reader state \(\mu_j\), the full record law becomes

$$
\begin{aligned}
P(r_{h,1:T},r_{s,1:T}\mid\pi)
=
\sum_{\substack{\mathbf n_{1:T}\\z_{0:T}\\m_{h,0:T}\\m_{s,0:T}}}
&\mu_{\rm src}(z_0)\mu_h(m_{h,0})\mu_s(m_{s,0})\\
\times\prod_{t=1}^{T}
&K_{\rm src}(z_t,\mathbf n_t\mid z_{t-1},c_t)
K_h(m_{h,t},r_{h,t}\mid m_{h,t-1},\mathbf n_t,c_t)\\
\times&
K_s(m_{s,t},r_{s,t}\mid m_{s,t-1},\mathbf n_t,c_t).
\end{aligned}
\tag{30}
$$

Here \(z_t\) is the printed source/pump memory state and \(K_{\rm src}\) is
its normalized emission/transition kernel. If no finite or otherwise
controlled future-sufficient source state exists, the source must instead be
represented by one normalized whole-process law.

### Memory wall

Equation (30) is a legitimate effective Markov realization only if the
printed state is future sufficient. Failure of future sufficiency means:

1. the memory state is incomplete;
2. the reader/source law is non-Markov at that carrier; or
3. the window grammar is wrong.

Adding a hidden history table after observing failures is not physics.

### Division wall

A click becomes a candidate division only after amplification and retention
make the record future sufficient at the declared tolerance. Absorption,
hotspot formation, analog pulse rise, threshold crossing, and stable digital
storage are different typed stages. The existence of \(r_j\) in (29) does not
prove that an earlier stage was already a complete checkpoint.

---

## 11. Adaptive branch and prefix autonomy

Let an earlier stable herald/readout prefix \(h_{t-1}\) choose a later
physical setting

$$
c_t=\pi_t(h_{t-1}).
\tag{31}
$$

The setting changes the later pump, retarder, switch, or reader boundary
through a physical electronics waveform. It may not retroactively change
the marginal law of an already retained prefix:

$$
\sum_{r_{t:T}}
P(r_{1:T}\mid\pi)
=
P(r_{1:t-1}\mid\pi_{\leq t-1}).
\tag{32}
$$

This follows for the effective construction when:

1. source and reader transition kernels are normalized;
2. later settings are functions only of retained past records and fresh
   exogenous randomness;
3. no future setting generator is correlated with omitted source variables
   in a way excluded from the parent; and
4. the physical experiment semantics permit free later extension.

Equation (32) is operational prefix autonomy. It is not a derivation of a
fundamental time arrow.

---

## 12. The bounded preparation source map

Define the author-side map

$$
\mathcal S_{\rm PRC}^{\rm eff}:
(B_e^{\rm eff},\mathsf{Prep}_e,\mathsf{ReadCal}_e)
\longmapsto
\widehat P_e^{\rm src+read}
\tag{33}
$$

by:

1. deriving the finite-band Green/nonlinear kernel from the material packet;
2. deriving its singular/Bloch--Messiah gains \(r_k,\varphi_k\);
3. setting \(\lambda_k=\tanh^2r_k\);
4. generating the unconditioned pair law (2);
5. propagating source modes and loss through the held-out optical network;
6. applying only independent local reader calibration or a frozen reader
   memory kernel;
7. retaining every source and reader outcome; and
8. conditioning on heralds only as a secondary view.

### Preparation inputs allowed

1. real pump/reference waveform measured upstream;
2. physical pump power, spectrum, timing, and uncertainty;
3. source material/geometry/temperature/bias;
4. independently sourced constitutive response;
5. no-pump, blocked-source, and source-only backgrounds;
6. an explicitly declared incoming equilibrium/vacuum approximation;
7. local reader calibration independent of target network; and
8. a closed mode/truncation/augmentation budget.

### Inputs forbidden

1. fitted \(\lambda_k\) from target clicks;
2. target joint spectral amplitude;
3. heralded density matrix;
4. target pair-number histogram;
5. target brightness normalization;
6. target detector POVM;
7. assembled-network transfer matrix;
8. a per-pump-power refit; and
9. target-trained source or reader surrogate.

---

## 13. Absolute brightness and normalization firewall

The source kernel must predict both shape and absolute rate. But absolute-rate
formulas carry normalization, mode-volume, collection, and convention risks.

### Required freeze

Before any target is opened, freeze:

1. the exact source-paper/version and normalization convention;
2. the field and mode normalization;
3. quantization-volume cancellation;
4. refractive-index factors;
5. pump photon-flux conversion;
6. collection-mode normalization;
7. repetition/window duration;
8. source loss versus detector efficiency;
9. nonlinear-tensor convention; and
10. numerical quadrature and truncation error.

### Correction control

The 2025 correction to the 2019 tutorial demonstrates why a literature
formula cannot be treated as immutable merely because it is published. The
correction changes a dimensionless order-unity factor and improves agreement
with experiment. A future pin must either:

1. use the corrected formula;
2. derive the normalization independently from the Green/QED convention; or
3. demote absolute-rate prediction.

It may not choose among conventions after opening target brightness.

### Shape-only ceiling

A source law that predicts normalized spectra or fringes but fits one global
brightness factor has not completed preparation. The fitted factor may carry
\(\chi^{(2)}\), pump coupling, collection, loss, or reader efficiency.

---

## 14. Hostile control battery

### H1 — heralded-state import

Supply a fitted heralded density matrix. Prediction may pass; preparation
credit becomes zero.

### H2 — coincidence-fitted gain

Fit \(\lambda_k\) or a global squeezing parameter from sealed coincidences.
The source law becomes target calibrated.

### H3 — brightness/efficiency swap

Use the exact ambiguity (26)--(27). A valid protocol must not decide between
the two pairs from the no-click target alone.

### H4 — dark/source swap

Increase dark probability while decreasing source gain to preserve a click
rate. Independent blocked-source records must distinguish the mutant.

### H5 — mode-collapse mutant

Replace multiple modes with one effective mode at matched mean brightness.
Held-out \(g^{(2)}\), multipair, spectral, or detector-efficiency variation
must expose the difference.

### H6 — multipair erasure

Renormalize \(N=0,1\) and omit \(N\geq2\). The complete-law and high-pump
tests must fail.

### H7 — pump-refit mutant

Use a different gain constant at each pump setting. No-transfer credit.

### H8 — pump-depletion mutant

Extrapolate the undepleted formula into a regime with independently visible
pump depletion. The scope test must fail.

### H9 — full-target spectrum

Supply the sealed joint spectral amplitude under the name source
calibration. Source credit becomes zero.

### H10 — detector-POVM import

Supply target-complete detector tomography. Reader-origin credit becomes
zero even if the record law improves.

### H11 — dead-time omission

Use the window-independent formula at repetition periods comparable with
recovery. Inter-window records must expose the failure.

### H12 — afterpulse omission

Blocked-source conditional click rates after a previous click must expose
missing reader memory.

### H13 — shared-background mutant

Correlate herald and signal readers through common electronics, light leak,
or acquisition logic. The conditionally independent reader product must fail.

### H14 — source-memory mutant

Give successive pump windows correlated gain or phase drift while retaining
independent \(P_{\rm src}(\mathbf n_t)\). Drift monitors and repeated-window
statistics must expose it.

### H15 — truncation-tail mutant

Hide target-relevant modes beyond \(K\). A frozen tail bound or held-out
filter/reader change must detect them.

### H16 — normalization-convention mutant

Use an outdated or inconsistent absolute-rate prefactor. Independent
dimensional and limit checks must fail before target opening.

### H17 — herald deletion

Score only heralded successes. Complete-record normalization must reject it.

### H18 — record-before-amplification

Call absorption or an unamplified excitation a stable division. Typed reader
stages must reject the claim.

### H19 — order/table mutant

Attach different source gains to different downstream component orders
without a physical back-action relation. Source descent fails.

### H20 — implementation token

Use detector/source serial number to choose parameters beyond measured
descriptors. Transfer fails.

---

## 15. What has actually been closed

### Closed conditionally

1. Physical mode gains determine the unconditioned pair-number law.
2. Vacuum, one-pair, and multipair sectors are normalized jointly.
3. Given limited local threshold-reader inputs, the four complete click
   outcomes are exact and ordinary positive.
4. Herald conditioning is derived from the parent.
5. The brightness/efficiency ambiguity is proved exactly.
6. Detector/source memory has a typed extension rather than an ignored
   correction.

### Still open

1. Why the signal/idler input occupies the vacuum/thermal state.
2. Full pump quantum dynamics outside the admitted approximation.
3. Exact gain derivation for a real fabricated source at controlled
   uncertainty.
4. Microscopic detector absorption-to-stable-record probability.
5. Reader crosstalk, memory, and electronics from material law rather than
   calibration.
6. An implementation transfer.
7. The actual microscopic configuration/process.
8. Native indivisibility.
9. Background-independent chronology, QFT derivation, spacetime, and gravity.

The preparation gap has narrowed from "supply a state" to three explicit
physical debts: incoming boundary state, strong-pump/non-Gaussian dynamics,
and device-specific source uncertainty.

---

## 16. Barandes and actuality

The positive law (2) and complete records (19) can be stated without treating
the squeezed state (9) as ontic. That is compatible with the methodological
part of Barandes's programme: Hilbert representation can be secondary.

It is not yet Barandes-native because:

1. the \(\lambda_k\) are generated through a supplied QED process;
2. mode occupations are predictive coordinates, not an independently
   justified actual configuration space;
3. no stochastic transition law on physical detector/source configurations
   is printed;
4. only the final amplified records are conservatively declared actual; and
5. indivisibility is inherited operationally from the quantum source, not
   established on a complete positive carrier.

The exact positive record law is necessary but not sufficient for the desired
ontology.

---

## 17. Empirical packet and held-outs

No apparatus is opened. A future execution should freeze:

### Public

1. source geometry, materials, temperature, poling, and alignment;
2. upstream pump/reference waveform and power;
3. independent \(\chi^{(2)}\) and linear response;
4. filters and port geometry;
5. local reader efficiency versus mode, dark, jitter, and recovery;
6. blocked-source and no-pump records;
7. repetition rate and raw acquisition grammar;
8. numerical/mode tail bounds; and
9. correction/version provenance for absolute brightness.

### Held out

1. absolute pair/herald rate at new pump settings;
2. no-click, single-click, and coincidence table;
3. multipair-sensitive correlations;
4. a filter or collection-mode change;
5. a detector-bias/efficiency change;
6. a repetition-rate/dead-time change;
7. a downstream order/depth change;
8. a new source or component lot; and
9. every failure and drift record.

The same source map must predict all held-outs without refitting
\(\lambda_k\), brightness, or detector efficiency.

---

## 18. Outcome ladder

### PRC-L0 — fitted state/table

Target state, gains, spectra, POVM, or click table are supplied.

### PRC-L1 — unconditioned number theorem

The source kernel generates a normalized vacuum/one/multipair law.

### PRC-L2 — complete bounded click law

Independent local reader inputs generate the full herald/signal click table,
exact conditioning, and nonidentifiability controls.

### PRC-L3 — temporal reader/source closure

A frozen future-sufficient memory model predicts raw multiwindow records,
dead time, afterpulsing, and adaptive branches.

### PRC-L4 — no-refit implementation transfer

The physical source and reader maps predict a new implementation and held-out
absolute rates at controlled uncertainty.

### PRC-L5 — microscopic native closure

Preparation, source, reader amplification, actuality, and indivisibility
descend from one ordinary-positive native law.

Present author-side disposition:

$$
\boxed{\text{PRC-L2 AUTHOR-SIDE / NO SCIENTIFIC AWARD.}}
\tag{34}
$$

No temporal implementation, target, transfer, microscopic detector, or
native ontology is evaluated.

---

## 19. Next physical construction

The source-side law is now strong enough for cheap synthetic and prospective
tests. The sharp remaining construction is the reader:

> Can detector material, geometry, bias, temperature, incident field packet,
> and electronics—not a POVM or target clicks—generate the raw waveform,
> threshold, timestamp, recovery, and retained-record law?

The next author-side packet should:

1. type absorption, quasiparticle/carrier creation, avalanche/hotspot,
   circuit response, threshold, timestamp, and storage as distinct stages;
2. bind one fixed microscopic/effective detector model;
3. derive its stochastic kernel (29) from physical parameters;
4. distinguish irreducible quantum chance from uncontrolled microstate
   uncertainty;
5. prove normalization, prefix autonomy, and memory sufficiency or expose
   their failure;
6. test against ab initio SNSPD and SPAD-tomography controls; and
7. remain a detector recovery control, not a new ontology.

Only after both source and reader maps survive cheap controls would an
implementation-bound pin be scientifically justified.

---

## 20. Maximum legitimate claim

> In the finite/controlled multimode undepleted-pump SPDC model, physical
> mode gains derived from the Green/QED source kernel generate an exact
> unconditioned ordinary-positive distribution over vacuum, one-pair, and
> multipair sectors. Independently calibrated local threshold-reader
> efficiencies and dark probabilities then generate an exact normalized
> herald/signal complete-click law and the heralded conditional law without
> importing a fitted state. The same equations prove that source brightness
> and reader efficiency are not identifiable from target clicks alone.
> Incoming vacuum/thermal preparation, strong-pump dynamics, finite-mode
> error, detector amplification, memory, and implementation transfer remain
> open. This is PRC-L2 author-side effective-QED preparation progress, not a
> native stochastic ontology, experiment, spacetime/QFT derivation, gravity
> result, official pin, review, or scientific award.

---

## 21. Present disposition

~~~text
MATERIAL-DESCENT INPUT:                 GREEN/QED KERNEL / MSD-L2
MODE GAIN SOURCE:                       PHYSICAL KERNEL / NO TARGET FIT
UNCONDITIONED VACUUM+PAIR LAW:          EXACT UNDER PRINTED MODEL
MULTIPAIR STATISTICS:                   RETAINED
THRESHOLD COMPLETE CLICK TABLE:         EXACT GIVEN LOCAL INPUTS
HERALD CONDITIONING:                    DERIVED FROM PARENT
BRIGHTNESS/EFFICIENCY IDENTIFIABILITY:  REFUTED BY EXACT WITNESS
ABSOLUTE-RATE CONVENTION:               VERSION/ERROR FREEZE REQUIRED
PUMP DEPLETION:                         OUTSIDE SIMPLE MODEL / CONTROLLED WALL
MODE TRUNCATION:                        TAIL BOUND REQUIRED
DETECTOR MEMORY KERNEL:                 TYPED / NOT PHYSICALLY DERIVED
MICROSCOPIC AMPLIFICATION:              OPEN
IMPLEMENTATION TRANSFER:                NOT RUN
PRC AUTHOR-SIDE CEILING:                L2
NATIVE POSITIVE ONTOLOGY:               ABSENT
PAPER 17 EMERGENCE:                     NOT UNBLOCKED
QFT/SPACETIME/GRAVITY:                  NO PROGRESS
OFFICIAL PIN / REVIEW / RESULT:         NONE
~~~
