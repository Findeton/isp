# ISP v17 — Sufficient Physical Boundary Gate v3

## One final clean result-neutral temporal and regional contract

Date: 2026-08-24

Status: **AUTHOR-SIDE CANDIDATE / NOT YET FROZEN / NO SCIENTIFIC RESULT**

Authority: **one and only one clean successor to the terminal v2 contract**

Maximum author-side claim: **readiness for one immutable independent
contract-review pin**

Apparatus acquisition, pilot data, empirical scoring, threshold selection from
target data, boundary promotion, ontology selection, chronology, spacetime,
gravity, automatic repair, a second successor, or v4: **not authorized**

---

## 0. Exact question and non-negotiable scope

The contract asks:

> Does an independently identified material interface, operated through
> physically implemented interventions and readers, screen a complete
> controlled experiment and support direct held-out composition without
> encoding the connected target process?

There are two separately scored experiments.

1. **SPB-T** is a temporal system--memory experiment. It asks whether a
   material carrier at a causal break screens incompatible prepared histories
   from registered future instruments and supports direct three-interval
   composition.
2. **SPB-R** is a regional substitution experiment. It asks whether a fixed
   material port collar screens distinct interiors from registered exterior
   readers and supports direct two- and three-region assembly.

The contract is result-neutral. A compact carrier, an enlarged physical
carrier, a minimal passing antichain, failure of one or more registered duties,
no registered proper carrier, physical infeasibility, and underdetermination
are all admissible.

The following firewalls are binding.

1. A response coordinate, process tensor, state, `S` matrix, higher kernel, or
   lookup table is not a physical carrier merely because it predicts.
2. Literal programme or history labels are not evidence of a physical trace.
   A material trace accessible to the licensed future cannot be deleted as a
   label.
3. Observational conditioning is not an intervention.
4. A source inventory is not a causal-identification theorem.
5. Matching within a threshold is not an equivalence quotient.
6. A permissive threshold cannot make the experiment pass by definition.
7. Conditional kernels do not supply their own initial preparation law.
8. Local feedback kernels need not define a unique normalized joint law.
9. Pairwise composition is not a substitute for a direct triple.
10. Minimality in a registered candidate poset is not individual necessity.
11. A mathematical history coordinate is not a material memory device.
12. Simulation and algebra may test conformance but cannot supply apparatus
    records.
13. No result of either arm establishes a fundamental state, Markovian or
    indivisible universe, Hilbert or Barandes ontology, actuality, chronology,
    spacetime, gravity, or unification.

---

## 1. Supplied benchmark structure and physical scope

This is a fixed-background laboratory discriminator. It receives, rather than
derives:

- laboratory clocks and schedules;
- a selected apparatus and calibration epoch;
- finite physical preparations, intervention families, and readers;
- an operational system or port decomposition;
- standard quantum and classical-wave comparator calculations;
- a registered primary regime and a materially distinct transfer regime; and
- a finite candidate family, an explicitly registered physical partial order,
  and a licensed access graph.

These inputs have no fundamental ontological status. A successful temporal
arm does not derive time. A successful regional arm does not derive space or a
field ontology. The two arms are never averaged into one verdict.

“Complete” means complete on one frozen registered attempted-run schema:
issued attempts, noncompliance, failures, sentinels, boundary records, readers,
registered nuisance/reference variables, and declared inaccessible debt. It
does not mean microscopic omniscience over every bath or field degree of
freedom.

The exact scholarly source roles and ceilings remain those in the separately
frozen SPB source-receipt manifest and P0 platform audit. Later sources may
attack a claim but cannot repair a frozen contract.

---

## 2. Typed physical experiment and causal identification

### 2.1 Immutable context, assignment, execution, and observation

For either arm, one issued attempt is

$$
\omega=(\Xi,A,E,Y).
\tag{2.1}
$$

The immutable context is

$$
\Xi=(j,s,\rho,\ell),
\tag{2.2}
$$

where:

- `j` is the serial-labelled apparatus/configuration and calibration epoch;
- `s` is the immutable calibration, validation, training, primary-held-out,
  or transfer split;
- `rho` is the frozen primary or transfer regime; and
- `ell` is the comparison cell and common outcome schema.

No `do` operator acts on `j`, `s`, `rho`, or `ell`. If a regime or apparatus
change is physically assigned inside a larger experiment, that change must be
typed as an intervention in a new pin rather than hidden in context.

The assigned physical intervention is

$$
A=(h,i,f,z),
\tag{2.3}
$$

where `h` is the assigned past-history or interior preparation, `i` the
assigned boundary instrument or substitution, `f` the assigned future,
exterior, or reconnection policy, and `z` a randomized auxiliary assignment.

An adaptive `f` is a policy fixed before the attempt: a measurable map from
registered earlier outcomes to later licensed operations. If assignment is
staged, equation (2.11) is read as a sequential randomization law. Exogeneity
and positivity must then hold at every decision, conditional on the complete
registered history available before that decision. A policy learned, revised,
or selected from primary held-out outcomes is a new intervention and is not
admitted by this contract.

The literal execution transcript is

$$
E=(\widetilde h,\widetilde i,\widetilde f,e_C),
\tag{2.4}
$$

where `e_C` contains compiler/controller acknowledgement, physical settings,
timing, compliance, retries, and routing.

The physical observation is

$$
Y=(b,r,n,q),
\tag{2.5}
$$

where `b` is the boundary-side outcome and retained record, `r` the
future/exterior scientific reader transcript, `n` the registered nuisance,
reference, environment, and drift transcript, and `q` the complete attempt
quality/failure state.

### 2.2 Comparison-invariant execution coordinate

The complete archive retains `E`. A comparison uses the frozen physical
projection

$$
E^\star=\kappa_{\Xi,A}(E),
\tag{2.6}
$$

with common physical coordinates for compliance, deviations from the assigned
operation, latency, retries, acknowledgement, and failure. It does not copy
the literal treatment, programme, or history label. Correct execution of
either compared assignment maps to one common `COMPLIANT` state.

This removes syntax, not matter. Every controller log, seed carrier, pulse
residue, reservoir change, heat load, cable state, or other trace accessible
to a licensed future belongs in `b` or `n`. Omission requires a predeclared
physical inaccessibility certificate over the licensed access graph. Deleting
a database column is not physical inaccessibility.

The primary scored outcome is

$$
Y^\star=(E^\star,b,r,n^\star,q),
\tag{2.7}
$$

where `n_star` is one predeclared nuisance projection common to the comparison
cell. Noncompliance and failures are never postselected away.

### 2.3 One common measurable attempted-run space

Every cell `ell` has one measurable space

$$
(\mathcal Y_\ell,\mathcal F_\ell).
\tag{2.8}
$$

All admitted assignments map into this same schema. Branch-absent fields
receive typed sentinels such as `NOT-APPLICABLE`, `NO-RECORD`, `TIMEOUT`, or
`INACCESSIBLE`. Every issued attempt contributes exactly one outcome.

The complete parent law

$$
P(d\Xi,dA,dE,dY)
\tag{2.9}
$$

is normalized before any successful-run or protocol-conditioned secondary
analysis.

### 2.4 Admissible causal-identification routes

The primary intention-to-treat law is

$$
Q_{\xi,a}(dy)
=
P\!\left(Y^\star\in dy\mid
\Xi=\xi,\operatorname{do}(A=a)\right).
\tag{2.10}
$$

It is promotion-eligible only under one of two frozen routes.

#### Route `ID-R` — physically randomized intervention

The apparatus implements a target-independent assignment kernel

$$
g_\xi(da)=P(A\in da\mid\Xi=\xi),
\tag{2.11}
$$

using a physically identified randomizer whose commands, acknowledgements,
seed lineage, failures, and accessible carrier records are retained. For every
mandatory finite assignment cell,

$$
g_\xi(a)\ge p_{\min}>0.
\tag{2.12}
$$

For a continuous assigned coordinate, the evaluation pin instead freezes a
common dominating measure and a strictly positive lower-density condition on
the exact registered comparison domain. Randomization occurs before the
outcomes it is meant to identify. Allocation concealment from the device is
not assumed unless physically implemented.

The randomizer may consume `Xi` and its frozen allocation probabilities but
may not read the device state, candidate boundary, latent memory proxy, or
future outcome source. Its physical source and every credible common-cause
channel to the apparatus are audited. In potential-outcome notation the design
condition is assignment-source exogeneity,

$$
A\mathrel{\perp\!\!\!\perp}
\{Y^\star(a):a\in\mathcal A_\ell\}\mid\Xi,
\tag{2.13}
$$

as a physical design premise, not an inference from balanced observed
frequencies alone.

The physical consistency requirement is: on every issued attempt with
assignment `A=a`, the observed registered outcome is the corresponding
potential outcome `Y_star(a)`. This is not conditioned on successful
execution; execution deviations and noncompliance remain in `E_star` and `q`.
Cross-attempt carryover is either physically washed out and bounded or entered
as a preassignment block/context variable with cluster/block randomization.

#### Route `ID-T` — explicit causal transport

A nonrandom schedule can enter a primary claim only if the pin supplies all of:

1. a complete directed causal/transport model for the registered variables;
2. a physical intervention map distinguishing replacement from observation;
3. conditional exchangeability or an equivalent identification theorem;
4. consistency and a no-unregistered-interference/carryover condition;
5. positivity/overlap on the exact transported target population, or a
   separately bounded extrapolation domain;
6. only preassignment adjustment variables;
7. a frozen transport functional and uncertainty law; and
8. independent validation capable of rejecting the bridge.

Knowing the source schedule, hardware identity, or assignment log alone is
not `ID-T`. A descendant of treatment or latent physical memory cannot be
regressed away as drift. Failure of any item returns causal
underdetermination; it never defaults to observational conditioning.
Every `ID-T` scientific statement is printed conditional on the exact causal
model, transport population, positivity domain, and validation premises that
identify it. It is never described as a consequence of scheduling alone.

### 2.5 Disjoint causal status

Each mandatory comparison receives exactly one status in this priority order:

```text
ID-PROCEDURAL-INVALID
  source identity, schema, assignment record, or chronology is broken;

ID-ASSIGNMENT-INVALID
  the claimed physical randomizer/intervention was not implemented or its
  exogeneity/positivity/consistency conditions are physically refuted;

ID-UNIDENTIFIED
  the record is valid but randomizer exogeneity/positivity/consistency or a
  complete ID-T bridge cannot be certified;

ID-IDENTIFIED-R
  all ID-R conditions pass;

ID-IDENTIFIED-T
  all ID-T conditions pass.
```

Only the last two statuses permit a promotion-bearing distance. The first two
make the affected claim invalid; `ID-UNIDENTIFIED` makes it underdetermined.

### 2.6 Auxiliary randomization and standardization

If auxiliary `z` is averaged, one frozen assignment law is used:

$$
Q_{\xi,h,i,f}(dy)
=
\int Q_{\xi,(h,i,f,z)}(dy)\,\mu_\xi(dz).
\tag{2.14}
$$

History-specific empirical seed weights are forbidden. Comparisons across
drift blocks use randomized interleaving, one independently validated
preassignment standardization kernel, or return underdetermined. Every
standardization and causal bridge belongs to the simultaneous inference
family.

---

## 3. Physical boundary candidate and referent firewall

A candidate is

$$
\mathfrak B=
(\Sigma_B,\mathcal X_B,
 \mathsf{Prep}_B,\mathsf{Instr}_B,\mathsf{Read}_B,
 \mathcal G_{\rm pres},d_B,\delta_B,
 \mathsf K_B,\mathsf{Cost}_B).
\tag{3.1}
$$

The meanings are disjoint.

- `Sigma_B` is a serial-labelled material interface carrier with physical
  ports, storage medium where applicable, references, and calibration epoch.
- `X_B` is an operational coordinate on that carrier. It may be a density
  operator, response vector, finite history coordinate, or other predictive
  representation; it is not thereby ontic.
- `Prep_B`, `Instr_B`, and `Read_B` are implemented physical families acting
  on the carrier.
- `G_pres` is an exact presentation groupoid, not an empirical threshold.
- `d_B,delta_B` define validation matching and obey the nonvacuity conditions
  in Section 5.
- `K_B` is a normalized conditional response/instrument kernel used in
  composition.
- `Cost_B` records information, precision, energy/entropy, latency, storage,
  apparatus, references, calibration, and inaccessible debt.

A response history or higher kernel without a material `Sigma_B` and physical
preparation/instrument/readout grammar is a coordinate anti-control. It is
ineligible for physical descent and cannot be called a carrier.

The full target law, complete past, target-trained predictive state,
per-program table, and connected-fit composite remain anti-controls. Exact
prediction does not repair failed descent.

---

## 4. Duty vector

For each candidate:

$$
\begin{aligned}
D(\mathfrak B)&:\ \text{target-independent physical descent},\\
S(\mathfrak B)&:\ \text{primary complete-outcome screening},\\
C(\mathfrak B)&:\ \text{primary direct composition},\\
I(\mathfrak B)&:\ \text{implemented interface instruments/readers},\\
X_g(\mathfrak B)&:\ \text{second-regime no-refit transfer of duty }g,
\quad g\in\{D,S,C,I\}.
\end{aligned}
\tag{4.1}
$$

The complete decision vector is

$$
\mathcal V(\mathfrak B)
=(D,S,C,I;X_D,X_S,X_C,X_I).
\tag{4.2}
$$

No scalar transfer flag exists. A report names the candidate, exact duty,
regime, fixed quantities, and allowed standard recalibrations.

`D` passes only when material carrier identity and interface descent are
established from target-independent hardware/source facts. `I` passes only
when the physical preparations, interventions, and readers are executed with
their outcome, backaction, failures, and lineage retained. `S` and `C` are
defined below. Passing one coordinate never fills another.

For `D`, `I`, and every source/resource preflight, the status rule is:

```text
INVALID          a mandatory identity, source, or schema object is broken;
PASS             every mandatory physical certificate is established;
FAIL             no item is invalid and at least one mandatory proposition is
                 physically refuted;
UNDERDETERMINED  otherwise, including missing or unrun evidence.
```

The physical pin enumerates the mandatory propositions before acquisition.
An absent certificate is not silently a failure or a pass.

Primary composition uses component facts frozen without the direct target in
the same regime `rho_0`. Transfer uses a materially distinct predeclared
`rho_1` and permits only standard calibrations whose forms and roles were
frozen before that regime opens. Thus `C=PASS` and `X_C=FAIL` is coherent.

---

## 5. Exact presentation, physical matching, and nonvacuity

### 5.1 Exact presentation groupoid

An arrow in `G_pres` changes names, bases, reference phases, or coordinates in
every source, carrier, intervention, reader, record, and likelihood together,
without changing the serial-labelled material experiment. Exact pushforward
equality is required.

A physical adapter, cable permutation, pulse, reset, randomizer, state change,
or memory device is an instrument and is never quotiented. No statistical
threshold belongs to `G_pres`.

### 5.2 Boundary matching

Validation copies of histories/interiors `h,h'` generate common-space
boundary laws

$$
M_h(dx),\qquad M_{h'}(dx).
\tag{5.1}
$$

The primary matching distance is total variation on a frozen finite physical
schema or a predeclared measurable coarse-graining with validated coverage.
It has diameter one. Additional timing, phase, tail, or transport distances
are secondary and cannot replace it after residual inspection.

The pair is matched at resolution `delta_B` only when

$$
U_B[d_{\rm TV}(M_h,M_{h'})]\le\delta_B.
\tag{5.2}
$$

Matching is statistical, nontransitive, and never quotiented.

### 5.3 Nontrivial history/interior separation

Every screened pair must independently satisfy

$$
L_W[d_W(H_h,H_{h'})]>\eta_W>0
\tag{5.3}
$$

on an earlier/interior record. Duplicate programmes under two labels fail.
If no pair both separates and matches, screening is underdetermined.

### 5.4 Binding anti-vacuity conditions

For every candidate and comparison family, the evaluation pin freezes

$$
0<\delta_B<1,
\qquad
\Delta_B^- >0,
\qquad
\delta_B+\Delta_B^+<1,
\qquad
\delta_B-\Delta_B^->0,
\qquad
\Delta_B^+>0,
\tag{5.4}
$$

The pin also freezes a simultaneous family error target and two power errors,

$$
0<\alpha_B,\beta_B^-,\beta_B^+<\tfrac12,
\tag{5.5}
$$

with simultaneous coverage at least `1-alpha_B`, power at least
`1-beta_B^-` for certifying matching when the true boundary distance is at
most `delta_B-Delta_B^-`, and power at least `1-beta_B^+` for rejecting
matching whenever it is at least `delta_B+Delta_B^+`.

The pin also supplies a physical matching-resolution argument independent of
held-out future outcomes. A boundary reader must distinguish at least one
predeclared calibration anchor pair above `delta_B+Delta_B^+` with the stated
power. Otherwise matching is underdetermined, not automatically permissive.

Choosing `delta_B=1`, allowing every law to match, changing the threshold
after pair selection, or deriving it from the connected target is procedural
invalidity.

---

## 6. Screening, margins, power, and decisions

### 6.1 Screening family

For every predeclared matched separated pair, boundary instrument, licensed
future/exterior policy, candidate, and primary regime, compare

$$
Q^{B,\rho_0}_{\xi,h,i,f}(dy)
\quad\text{and}\quad
Q^{B,\rho_0}_{\xi,h',i,f}(dy)
\tag{6.1}
$$

on the same common space. Both must have an `ID-IDENTIFIED-*` status.

The primary distance is total variation on the registered complete outcome.
Continuous raw records require a frozen physical coarse-graining or a density
model with validated simultaneous coverage.

### 6.2 Scientific margin and two-sided power

Each primary comparison freezes a scientific negligibility margin before
validation-pair selection and held-out acquisition:

$$
0<\epsilon^{\rm sci}_c<1.
\tag{6.2}
$$

It must be derived from a declared downstream physical loss/decision
functional and independently justified physical resolution. Reader noise,
calibration uncertainty, and drift widen inference; they never enlarge the
margin.

The pin also freezes positive interior and exterior separations

$$
\Delta_c^- >0,
\qquad
\Delta_c^+ >0,
\qquad
\epsilon_c^{\rm sci}-\Delta_c^->0,
\qquad
\epsilon_c^{\rm sci}+\Delta_c^+<1.
\tag{6.3}
$$

One familywise error target and comparison-specific power errors are frozen,

$$
0<\alpha_{\rm fam},\beta_{c}^{\rm pass},
\beta_{c}^{\rm fail}<\tfrac12.
\tag{6.4}
$$

The simultaneous confidence family has coverage at least
`1-alpha_fam`. The design has power at least `1-beta_c^pass` to certify a
pass when the true distance is at most `epsilon_sci-Delta_minus`, and at least
`1-beta_c^fail` to certify a fail when it is at least
`epsilon_sci+Delta_plus`, under the predeclared model class. If such coverage
or power is physically unavailable, the comparison is underdetermined.

An empirical exact-zero margin is not promotion-eligible. Exact equality may
remain a deterministic/theorem control, but a finite empirical screening
claim uses the nonzero nonvacuous margin above.

### 6.3 Local and candidate decisions

Let `[L_c,U_c]` be a simultaneous confidence set. Exactly one local status is
printed:

```text
LOCAL-INVALID
  causal status, source identity, schema, or coverage is invalid;

LOCAL-PASS
  U_c <= epsilon_sci,c;

LOCAL-FAIL
  L_c > epsilon_sci,c;

LOCAL-UNDERDETERMINED
  neither inequality is established.
```

The priority is `INVALID`, `PASS`, `FAIL`, `UNDERDETERMINED`; a valid interval
cannot satisfy both pass and fail inequalities.

For one candidate and duty family:

```text
INVALID          iff any mandatory local item is invalid;
PASS             iff every mandatory local item passes;
FAIL             iff no mandatory item is invalid and at least one fails;
UNDERDETERMINED  otherwise.
```

Missing, infeasible, or unrun mandatory branches do not pass.

### 6.4 Multiplicity and stopping

One frozen simultaneous family includes causal-bridge validation,
validation-pair selection, all candidates, pairs, instruments, policies,
initial preparations, composition targets, regimes, frequency/time/power
bins, stochastic and deterministic classifier tests, bracketings, adaptive
candidate reporting, and stopping. No candidate, margin, bin, or classifier
is selected from the held-out residual.

---

## 7. Complete composition with an explicit initial preparation

### 7.1 Initial preparation kernel

Every composition prediction begins with an independently calibrated physical
preparation kernel

$$
\Pi_0(dx_0,dy_0\mid u_0,\lambda),
\tag{7.1}
$$

where `x_0` is the first boundary input coordinate, `y_0` contains preparation
outcomes/failures, `u_0` is the assigned preparation, and `lambda` is the
shared nuisance/reference parent. `Pi_0` is normalized and target-independent.
A fixed deterministic initial value is the explicit special case
`delta_x0`, not an omitted argument.

The direct connected target uses the same physical preparation instrument and
common outcome schema. Preparation failure remains in the attempted-run law.
Downstream kernels are defined on the preparation-failure/sentinel states or
on one explicit absorbing attempt state, so no failed branch falls outside the
composition domain.

### 7.2 Piece and seam kernels

Each independently calibrated piece `k` supplies

$$
\mathsf K_k(dy_k,dx_k^{\rm out}\mid
x_k^{\rm in},u_k,\lambda),
\tag{7.2}
$$

and each connector/intermediate operation supplies

$$
\mathsf V_k(dx_{k+1}^{\rm in},dy_{V_k}\mid
x_k^{\rm out},v_k,\lambda).
\tag{7.3}
$$

All kernels are normalized on success, failure, missing, invalid, and anomaly
outcomes. One independently calibrated joint law `Lambda(dlambda)` contains
registered shared references and correlations; independence is never silent.

### 7.3 Acyclic composition

For three pieces, the acyclic law is the explicit iterated integral

$$
\begin{aligned}
\widehat Q_{ABC}(d\mathbf y,dx_3^{\rm out})
=\int &\Lambda(d\lambda)\,
\Pi_0(dx_0,dy_0\mid u_0,\lambda)\\
&\mathsf K_A(dy_A,dx_A^{\rm out}\mid x_0,u_A,\lambda)\\
&\mathsf V_{AB}(dx_B^{\rm in},dy_{AB}\mid
x_A^{\rm out},v_{AB},\lambda)\\
&\mathsf K_B(dy_B,dx_B^{\rm out}\mid x_B^{\rm in},u_B,\lambda)\\
&\mathsf V_{BC}(dx_C^{\rm in},dy_{BC}\mid
x_B^{\rm out},v_{BC},\lambda)\\
&\mathsf K_C(dy_C,dx_3^{\rm out}\mid x_C^{\rm in},u_C,\lambda).
\end{aligned}
\tag{7.4}
$$

Here `mathbf y=(y_0,y_A,y_AB,y_B,y_BC,y_C)` is mapped by one frozen reader to
the same complete schema as the direct attempt. Ionescu--Tulcea applies because
an initial probability and every subsequent kernel are now explicit.

### 7.4 Feedback composition and disjoint status

For bidirectional or feedback wiring, local kernels do not automatically
define a joint law. The pin supplies a target-independent global construction
including `Pi_0`, finite state/noise variables, boundary conditions, and an
existence/uniqueness/normalization certificate.

Exactly one status is assigned in this priority order:

```text
COMP-PROCEDURAL-INVALID
  source, target separation, schema, or construction identity is broken;

COMP-MODEL-INVALID
  a required domain, instrument, stationarity, or kernel validation fails;

COMP-NONEXISTENT
  the valid exact frozen constraints certify that no joint law exists;

COMP-NONUNIQUE
  the valid exact frozen constraints certify multiple joint laws and no
  target-independent selector is supplied;

COMP-WELL-POSED
  exactly one normalized complete-attempt law is certified for every admitted
  assignment and nuisance stratum;

COMP-UNRESOLVED
  none of the preceding mathematical predicates is certified because
  existence, uniqueness, or normalization remains unresolved at registered
  resolution.
```

After procedural and model validity are certified, the remaining quantifiers
are global over every mandatory assignment and nuisance stratum.
`COMP-NONEXISTENT` is printed if at least one such instance is certified to
have no joint law. Conditional on existence for all instances,
`COMP-NONUNIQUE` is printed if at least one has multiple joint laws and no
target-independent selector. `COMP-WELL-POSED` is printed only if every
instance has exactly one normalized joint law. If those propositions cannot
be certified uniformly at registered resolution and no earlier decisive
predicate holds, the status is `COMP-UNRESOLVED`.

Earlier statuses control. A model-invalid instance is not also routed as
nonexistent; uncertainty spanning different exact cases is `UNRESOLVED`.
Only `COMP-WELL-POSED` can earn `C`.

### 7.5 Direct composition duty

Composition earns `C` only when:

1. `Pi_0`, every piece/seam kernel, parameter-sharing rule, and `Lambda` are
   frozen without the direct target;
2. one normalized complete prediction is certified;
3. all causal and statistical decisions pass their registered duties;
4. direct pair targets and one direct `ABC` target are physically executed;
5. at least one registered seam intervention or interior substitution occurs;
6. resource growth is sub-target under the frozen criterion; and
7. no connected-target tomography, de-embedding, residual fit,
   normalization, correlation, or threshold enters the prediction.

Target data may test the law but may not select among compatible joints.
Both algebraic bracketings are conformance calculations on one physical
network, not two empirical worlds.

---

## 8. SPB-T v3 — temporal material-memory arm

### 8.1 Apparatus and compact carriers

The complete effective apparatus is

$$
\mathcal A_T=(S,M,E_\perp,C,R,\mathcal L),
\tag{8.1}
$$

where `S` is the controlled system transmon, `M` the independently addressed
material memory transmon, `E_perp` declared residual environment/debt, `C` the
controller and reference registers, `R` the readout/reservoir stack, and
`L` leakage/crosstalk modes.

The primary material memory carrier is

$$
\Sigma_T^{(1)}=(S,M)
\tag{8.2}
$$

with serial-labelled control and reader ports. Density operators, quantum
instruments, and process tensors may be used as standard operational
coordinates; passing does not make them ontic.

### 8.2 A physically typed finite-history carrier

The only history-extended physical candidate is

$$
\Sigma_T^{(3)}=(S,M,H_{T,k}),
\tag{8.3}
$$

where `H_T,k` is a bounded material record subsystem with `k` predeclared
accessible slots, modes, or retention stages. Its record coordinate may be
finite or continuous under a frozen physical resolution; no fundamental
discreteness is presumed. The evaluation pin must bind:

- physical substrate, serials, topology, and clock/reference ports;
- record alphabet and write/read/hold/reset instruments;
- retention, overwrite, cross-talk, leakage, and failure laws;
- preparation and blank/reference distributions;
- the physical map from interval outcomes to record cells;
- access permissions for every licensed future;
- energy, entropy, latency, storage, controller, and reservoir costs; and
- a complete normalized response kernel.

An abstract vector of the last `k` responses, a reconstructed process tensor,
or a software cache without this material grammar is **not** `T-B3`. A
target-independent finite response-history model may still be tested as a
coordinate model on `T-B1`; it inherits only the already established physical
carrier and can never earn enlarged-carrier or material-history necessity.

At contract-review scope, equation (8.3) is an admission type, not evidence
that an `H_T,k` device exists. Before any acquisition, a separately authorized
physical pin must either instantiate exactly one member by all fields above or
remove `T-B3` from the scored roster. An uninstantiated row is ineligible and
cannot enter a minimality or necessity statement. Data may not choose between
instantiation and removal.

If `H_T,k` is actively added or written by the experimenter, the resulting
claim is explicitly about that augmented material experiment. It cannot be
used to claim that the unaugmented process already possessed the record bank.
The write channel and its disturbance are mandatory instruments and costs.

### 8.3 Frozen candidate family

| code | material carrier | operational coordinate | maximum interpretation |
|---|---|---|---|
| `T-B0` | freshly prepared `S` and its ports | system state/instrument | system-only effective cut |
| `T-B1` | `S+M` and direct ports on both | joint state/instrument | named-memory cut |
| `T-B2` | `S+M+C_B`, where `C_B` is a material controller/reference register | joint carrier coordinate | controller/reference-extended cut |
| `T-B3` | `S+M+H_T,k` as typed in Section 8.2 | bounded material-record coordinate | finite material-history cut |
| `T-COORD-RH` | the already named `T-B1` carrier only | target-independent finite response-history vector/kernel | coordinate-performance control; no enlarged-carrier claim |
| `T-ANTI-W` | no independently descended carrier | full reconstructed process | whole-process anti-control only |
| `T-ANTI-H` | target-built object | complete past or target-trained quotient | target-built anti-control only |

No `E_perp` degree is promoted after residual inspection. A different material
carrier is outside this one-successor contract.

The evaluation pin supplies explicit carrier embeddings. At minimum, the
physical inclusion `T-B0 prec T-B1` is required. `T-B1 prec T-B2` or
`T-B1 prec T-B3` is printed only if the exact smaller carrier and all its
instruments/readers are literally preserved by the larger apparatus. `T-B2`
and `T-B3` are unordered absent a physical embedding. Predictive or parameter
count alone never defines the order.

### 8.4 History-pair battery

The frozen temporal battery contains:

1. histories with the same fresh `S` preparation and independently different
   `M` preparations;
2. histories matched on `S+M` but differing in a physical controller/reference
   record where feasible;
3. histories differing in a predeclared material `H_T,k` record while compact
   carrier coordinates are matched;
4. depth mutants that agree through `k` registered intervals and differ
   earlier; and
5. duplicate-label controls that must fail history separation.

Validation copies establish boundary matching. The scored continuation is not
destructively measured to certify its own input.

### 8.5 Causal break and memory interventions

The system causal break is a physical instrument: measure or discard the
incoming `S` coordinate and prepare a randomized output independently of it,
while retaining the break outcome, latency, compiler/controller state,
preparation acknowledgement, leakage, failure, and measured backaction on
`M` and other registered carriers.

Matched-duration idle, sham-readout, and break-on-memory controls are
mandatory. Every compared break/future assignment uses `ID-R` unless a full
`ID-T` bridge is separately pinned.

The material memory grammar is:

| code | physical instrument | retained physical effects |
|---|---|---|
| `M-ID` | intended identity | drift and residual coupling |
| `M-READ` | informationally complete ensemble instrument on validation copies or randomized scored branches | outcome and post-read backaction |
| `M-X` | calibrated toggle/orthogonalization | pulse execution, leakage, crosstalk |
| `M-RAND-R` | random control with seed accessible to the licensed future | seed carrier and controller record |
| `M-RAND-U` | random control with seed physically inaccessible to the licensed future | source carrier, access proof, disposal/destination route |
| `M-RESET` | active reset to a reference distribution | retries, old record, reservoir, resonator, heat, latency, recovery |
| `M-ISO` | finite detuning/echo/decoupling budget | residual exchange, `ZZ`, bus, drive, readout, leakage bounds |
| `M-RESET-ISO` | reset followed by isolation | every reset and isolation record/failure |

No instrument destroys information from the universe. “Unavailable” is
relative to the frozen material access graph.

### 8.6 Temporal screening and composition

The complete temporal score is

$$
Y_T^\star=(E^\star,b_M,r_S,r_M,r_H,n_T^\star,q_T),
\tag{8.4}
$$

with typed sentinels on branches that do not read `M` or `H_T,k`.

Three interval kernels act on the same candidate carrier. Their initial
preparation is the explicit `Pi_0` of Section 7, their seam kernels include the
assigned memory/history-register instruments and records, and their training
set is disjoint from direct `AB`, `BC`, and `ABC` circuits. A separately fit
`ABC` process tensor is an anti-control.

The transfer regime `rho_1` materially changes at least one registered
duration, coupling, pulse family, preparation family, device family, or
controller regime. Each `X_g,T` names exactly which duty transfers and which
standard calibrations may be repeated.

---

## 9. SPB-R v3 — regional material port-collar arm

### 9.1 Apparatus and fixed collar

The effective apparatus is

$$
\mathcal A_R=(I,I',A,B,C,V,Q,G,N),
\tag{9.1}
$$

where `I,I'` are physically different interiors, `A,B,C` reusable modules,
`V` connectors/seams, `Q` physical adapters, `G` sources/readers/references,
and `N` registered noise/environment/debt.

The minimal material collar is

$$
\Sigma_R^{(0)}=
(\mathcal P_{\rm phys},\mathcal C_{\rm collar},
 \mathcal M_{\rm admitted},\mathcal R_{\rm plane}),
\tag{9.2}
$$

containing serial-labelled port cross-sections, a finite calibrated collar,
admitted modes/polarizations, physical reference planes, impedances, and
shared phase/frequency references. Topology, dimensions, connector
orientation, cable map, and calibration epoch bind its identity.

### 9.2 Deterministic and complete response coordinates

The deterministic coherent coordinate is

$$
b(\nu)=S_B(\nu)a(\nu),
\tag{9.3}
$$

inside a validated linear, stationary, admitted-mode regime. It is a nested
control only.

The complete coordinate is

$$
\mathsf K_B(db,dy_B,dq_B\mid a,u_B,\lambda),
\tag{9.4}
$$

including coherent response, emitted/thermal/noise-wave law, source and
receiver reference records, calibration/drift covariance, nonlinearity,
saturation, hysteresis, mode-conversion and stationarity classifiers,
missing/invalid/failure outcomes, and correlations with `lambda`. Gaussian or
independent noise is not assumed.

### 9.3 A physically typed regional memory carrier

The only memory-extended regional candidate is

$$
\Sigma_R^{(3)}=(\Sigma_R^{(0)},H_{R,k}),
\tag{9.5}
$$

where `H_R,k` is a predeclared additional material interface-memory device
attached to or incorporated into an enlarged collar and not already counted
inside `Sigma_R0`. It may have discrete or continuous physical state under a
frozen operational resolution; no fundamental discreteness is inferred. The
pin binds its substrate, serials, connection topology, state preparation,
write/hold/read/reset instruments, retention and failure law, reference
requirements, accessible records, energy/latency/storage costs, and complete
normalized kernel.

A target-independent Volterra, finite higher-response, or nonstationary kernel
without this additional physical referent may be tested as `R-COORD-HR` on the
already descended `R-B1` carrier. It is not `R-B3` and cannot earn a material
carrier extension. A hidden or target-built software state remains an
anti-control.

At contract-review scope, equation (9.5) is an admission type, not evidence
that an `H_R,k` device exists. Before any acquisition, a separately authorized
physical pin must either instantiate exactly one member by all fields above or
remove `R-B3` from the scored roster. An uninstantiated row is ineligible and
cannot enter a minimality or necessity statement. Data may not choose between
instantiation and removal.

If `H_R,k` is inserted into the collar, the candidate concerns the augmented
network. Its write/coupling channel, loading, disturbance, noise, and resource
costs remain in the complete law; it cannot establish that the original collar
contained the same memory.

### 9.4 Frozen candidate family

| code | material carrier | coordinate | maximum interpretation |
|---|---|---|---|
| `R-B0` | `Sigma_R0` | deterministic `S_B` only | coherent-response control, not complete boundary |
| `R-B1` | `Sigma_R0` | complete stochastic `K_B` | fixed-collar boundary |
| `R-B2` | enlarged collar with physical adapters, loads, ancillary ports, and reference register | complete stochastic kernel | collar/reference extension |
| `R-B3` | `Sigma_R0+H_R,k` as typed above | complete material-memory kernel | finite interface-memory extension |
| `R-ANTI-C` | no changed carrier | conjugacy, traces, spectra, poles | invariant anti-control |
| `R-COORD-HR` | the already named `R-B1` carrier only | target-independent higher-response/nonstationary kernel | coordinate-performance control; no enlarged-carrier claim |
| `R-ANTI-T` | target-built object | connected-fit/de-embedding table | target-built anti-control |

`R-B0` is not a complete-boundary predecessor. `R-B1 prec R-B2` or
`R-B1 prec R-B3` is printed only with a literal carrier embedding preserving
the smaller interface grammar. Otherwise candidates are incomparable.

### 9.5 Passive presentation and active apparatus

For fixed ports `p_1,...,p_m`, a passive arrow changes coordinates in every
source, response, reader, record, and likelihood:

$$
a\mapsto U_ga,\qquad
b\mapsto U_gb,\qquad
S\mapsto U_gSU_g^{-1}.
\tag{9.6}
$$

It leaves the serial-labelled port-to-cable map and material experiment fixed.
An active cable permutation is an assigned instrument. A mixing or
transplantation adapter is a physical channel with every ancillary port, load,
loss, gain, noise, phase, dynamic range, saturation, calibration, and resource
charge. Software conjugacy is never a physical adapter.

The known one-port ideal equality is a scalar deterministic control. The known
multiport transplantation is conjugacy, not same-labelled-port equality. The
printed `T_4` is not scalar-normalizable to a passive unitary mixer. The exact
generalized star-product theorem is a lossless self-adjoint graph control, not
a complete lossy apparatus law.

### 9.6 Regional screening, composition, and transfer

The complete regional score is

$$
Y_R^\star=(E^\star,b_{\rm collar},r_{\rm wave},r_H,n_R^\star,q_R).
\tag{9.7}
$$

Different interiors are randomized or transported under Section 2, validated
at the same material collar, and tested under a predeclared exterior policy
battery. Reusable modules, connectors, adapters, initial source/preparation,
and the shared nuisance/reference law are trained without connected targets.
Direct pair assemblies and one direct `ABC` assembly are mandatory, including
at least one held-out interior substitution.

The target cannot fit de-embedding, reference planes, adapters, connector
laws, cross-seam correlations, initial source state, conditioning thresholds,
or error covariance. Each `X_g,R` uses a materially distinct registered
frequency, power, time, module, or source regime and names the exact duty.

---

## 10. Deterministic seam law and exhaustive finite-resolution routing

### 10.1 Exact seam properties

For a deterministic coherent coordinate, partition

$$
\begin{pmatrix}b_e\\b_s\end{pmatrix}
=
\begin{pmatrix}S_{ee}&S_{es}\\S_{se}&S_{ss}\end{pmatrix}
\begin{pmatrix}a_e\\a_s\end{pmatrix},
\qquad
a_s=Vb_s,
\tag{10.1}
$$

and define

$$
K=I-VS_{ss}.
\tag{10.2}
$$

For admitted inputs:

- existence requires `Range(V S_se) subseteq Range(K)`;
- internal uniqueness requires `Ker(K)={0}`;
- exterior uniqueness requires `S_es Ker(K)={0}`; and
- useful prediction requires propagated uncertainty within the frozen
  conditioning/dynamic-range budget.

### 10.2 Primary seam status

The pin freezes a simultaneous admissible model/confidence set. Exactly one
primary status is printed in this priority order:

```text
SEAM-PROCEDURAL-INVALID
  source, schema, target split, or classifier identity is broken;

SEAM-MODEL-INVALID
  linearity, stationarity, admitted-mode, connector, or reference validation
  is rejected;

SEAM-NO-SOLUTION
  the valid admissible set uniformly certifies failure of existence for a
  registered input;

SEAM-EXTERIOR-NONUNIQUE
  existence is certified and exterior ambiguity is uniformly certified;

SEAM-INTERNAL-NONUNIQUE-EXTERIOR-UNIQUE
  existence and exterior uniqueness are certified while nonzero internal
  kernel freedom is certified;

SEAM-ILL-CONDITIONED
  existence and exterior uniqueness are certified but the propagated
  uncertainty/dynamic-range budget is uniformly exceeded;

SEAM-REGULAR
  existence, internal and exterior uniqueness, and the conditioning budget are
  all certified;

SEAM-UNRESOLVED
  none of the preceding mathematical predicates is certified because the
  admissible set spans different existence, exterior-uniqueness,
  internal-uniqueness, or conditioning cases.
```

The mathematical quantifiers range over every registered input and every
member of the simultaneous admissible model/confidence set.
`SEAM-NO-SOLUTION` requires at least one mandatory input for which
nonexistence is uniform over that set. Conditional on existence for all
mandatory inputs, `SEAM-EXTERIOR-NONUNIQUE` requires at least one input for
which exterior ambiguity is uniform. Conditional on exterior uniqueness for
all inputs, `SEAM-INTERNAL-NONUNIQUE-EXTERIOR-UNIQUE` requires at least one
input with uniformly certified nonzero internal kernel freedom. Conditional
on internal uniqueness for all inputs, `SEAM-ILL-CONDITIONED` requires at
least one input that uniformly exceeds the conditioning/dynamic-range budget;
`SEAM-REGULAR` requires every input to satisfy that budget. If none of these
global propositions is certified, the status is `SEAM-UNRESOLVED`.

An uncertainty set containing both singular and nonsingular cases is
`SEAM-UNRESOLVED`. Earlier statuses control; subordinate mathematical
properties may be reported but cannot create a second routing status.

### 10.3 Global solve and both bracketings

The global three-module solve is primary. Bracketing is represented by the
ordered triple

$$
\mathsf{Bracket}=(G,L,R),
\tag{10.3}
$$

with

```text
G in {GLOBAL-DEFINED, GLOBAL-UNDEFINED, GLOBAL-UNRESOLVED,
      GLOBAL-MODEL-INVALID}

L,R in {PIVOT-DEFINED, PIVOT-SINGULAR, PIVOT-UNRESOLVED,
        PIVOT-NOT-APPLICABLE}
```

Thus `(GLOBAL-DEFINED,PIVOT-SINGULAR,PIVOT-SINGULAR)` is explicitly typed.
A singular sequential pivot does not erase a valid global exterior prediction
and is never an empirical associativity or chronology result.

### 10.4 Stochastic seam status remains separate

Every regional prediction prints both one `SEAM-*` status and one `COMP-*`
status. A deterministic exterior mean does not select a stochastic noise law;
a stochastic regularization can define a measured distribution while
invalidating the deterministic model. Neither implication is assumed.

---

## 11. Source closure, physical resources, and immutable splits

### 11.1 Directed physical source inventory

Each arm publishes a directed inventory from assigned preparation through
retained record, including:

- serials, topology, materials, ports, cables, connectors, and dimensions;
- independently calibrated coupling and interface maps;
- firmware, compiler, pulse/VNA programmes, settings, and hashes;
- reference oscillators, clocks, synchronization, and scheduling;
- physical randomizer, seed, controller, and access graphs;
- initial preparation devices and distributions;
- readers, confusion/backaction, reservoirs, and recovery;
- residual environments and inaccessible debt;
- loss, leakage, noise, saturation, conversion, hysteresis, and drift;
- every issued attempt and data-loss/failure route;
- adaptive choices and stopping rules; and
- every fitted parameter and hyperparameter with its non-held-out source.

Source closure means this directed registered inventory is complete at the
claimed operational scope. It does not imply causal identification; Section 2
must independently pass.

### 11.2 Immutable data split

The evaluation pin separately freezes:

1. source identity and calibration;
2. causal bridge/randomizer validation;
3. boundary matching and anchor validation;
4. component/kernel/initial-law training;
5. primary held-out screening and direct composition; and
6. second-regime transfer.

No held-out record returns to training for the same claim. A material hardware
change invalidates the run or requires a new physical pin; no such new pin is
authorized by this contract review.

### 11.3 Resource ledger and target-size firewall

Every candidate reports

$$
\mathsf{Cost}=(I,P,E,T,M,A,R,K,D),
\tag{11.1}
$$

covering information, precision, energy/entropy, latency, memory/storage,
apparatus, physical references, calibration, and inaccessible debt. The
ledger includes initial preparation, randomization, seed disposal, history
depth, reset/isolation, adapter channels, loss/gain/noise, reference stability,
resolution, training attempts, raw data, and failure handling.

The physical pin defines a sub-target information/parameter/calibration
criterion before acquisition. A carrier coordinate whose resources grow like
the held-out response table is target-built even if it predicts.

---

## 12. Candidate posets, minimality, and necessity

### 12.1 Physical order only

The finite candidate relation `B prec B_prime` exists only when the physical
pin supplies an explicit injective carrier/interface embedding

$$
\iota_{B B'}:\Sigma_B\hookrightarrow\Sigma_{B'}
\tag{12.1}
$$

that preserves the smaller preparation, intervention, reader, access, and
outcome grammar, together with a monotone cost map. Predictive inclusion,
parameter count, history depth, or a suggestive name is not enough.

Minimality or necessity comparisons additionally require a commensurate duty:
the same external attempted-run outcome, licensed interventions/policies,
physical loss, scientific margin, power targets, regime, and resolution.
Candidates tested against different scientific questions are not ordered for
that claim.

### 12.2 Minimal passing antichain

For a named duty bundle `G`, define

$$
\operatorname{MinPass}_G
=\{B:B\text{ passes }G
\text{ and every applicable strict predecessor validly fails }G\}.
\tag{12.2}
$$

A failed predecessor can establish minimality of a passing candidate. An
underdetermined, invalid, infeasible, or unrun predecessor blocks that
minimality statement. Incomparable passing candidates are reported together
as an antichain. No member is called individually necessary merely because it
is minimal.

### 12.3 Registered necessity

A material augmentation `A_phys` is called **registered-necessary** for duty
bundle `G` only if:

1. at least one proper candidate passes `G`;
2. at least one registered proper competitor does not contain `A_phys`, and
   every such competitor validly runs and fails `G`; and
3. no relevant candidate is underdetermined, invalid, infeasible, or unrun.

This is necessity only inside the frozen candidate family, apparatus, policy,
regime, margin, and resolution. It is never fundamental necessity. A unique
minimal passing candidate is not sufficient for registered necessity unless
the competitor condition also holds.

---

## 13. Duty-specific outcome predicates

Every report prints the full local vector for every candidate. Headline flags
are parameterized predicates, not ambiguous prose.

### 13.1 Shared predicates

```text
ARM-INVALID[arm,reason]
  a procedural/source/schema requirement is invalid;

CANDIDATE-PASS[arm,B,G]
  candidate B passes the exact named primary duty bundle G;

CANDIDATE-MINIMAL[arm,B,G]
  B belongs to MinPass_G under Section 12.2;

MINIMAL-ANTICHAIN[arm,{B_i},G]
  the printed set is the complete registered minimal passing antichain;

AUGMENTATION-NECESSARY[arm,A_phys,G]
  all three Section 12.3 conditions pass;

DUTY-FAIL[arm,B,g,rho]
  the exact local duty g in regime rho validly fails for B;

DUTY-UNDERDETERMINED[arm,B,g,rho]
  the exact local duty is unresolved;

TRANSFER-FAIL[arm,B,X_g,rho_1]
  the named transfer coordinate X_g validly fails;

NO-PROPER-CANDIDATE-PASSES[arm,G]
  every registered proper candidate validly ran and failed at least one
  mandatory element of G;

ONLY-TARGET-BUILT-PREDICTS[arm,G]
  at least one target-built anti-control predicts and every registered proper
  candidate is valid, run, and fails G.
```

Any underdetermined, invalid, infeasible, or unrun proper candidate blocks the
last two universal/exclusion predicates. A candidate failure refutes only its
named duty at the frozen scope; no flag says the material carrier is
physically nonexistent or ontologically refuted.

### 13.2 Arm-specific informative predicates

Temporal reports may additionally print:

```text
TEMPORAL-SCREEN-PASS-COMP-FAIL[B]
TEMPORAL-PRIMARY-COMP-PASS-XC-FAIL[B,rho_1]
TEMPORAL-MATERIAL-HISTORY-MINIMAL[B3,G,cost]
```

Regional reports may additionally print:

```text
REGIONAL-COHERENT-CONTROL-PASS[B0]
REGIONAL-SCREEN-PASS-COMP-FAIL[B]
REGIONAL-PAIR-PASS-DIRECT-TRIPLE-FAIL[B]
REGIONAL-PRIMARY-COMP-PASS-XC-FAIL[B,rho_1]
REGIONAL-MODEL-FAIL[B,model_coordinate]
```

Every symbol names its exact duty. No aggregate “transfer fails,” “carrier
required,” “physically refuted,” or unguarded “only” outcome exists.

---

## 14. Cumulative scientific ladder

Pin creation is not a rung.

```text
SPB-L0  RESULT-NEUTRAL CONTRACT INDEPENDENTLY ACCEPTED
SPB-L1  EXACT APPARATUS/SOURCE/RANDOMIZATION/PREPARATION PACKET CLOSES
        FOR ONE ARM
SPB-L2  MATERIAL CARRIER AND INSTRUMENT GRAMMAR DESCEND; CAUSAL,
        SOURCE, NONVACUITY, AND RESOURCE PREFLIGHTS PASS
SPB-L3  PRIMARY COMPLETE-OUTCOME SCREENING AND POST-RUN AUDITS PASS
SPB-L4  SAME CARRIER PASSES PRIMARY DIRECT COMPOSITION AND POST-RUN AUDITS
SPB-L5  SECOND-REGIME PACKET CLOSES UNDER FROZEN NO-REFIT RULES
SPB-L6  ONE NAMED TRANSFER DUTY X_g PASSES WITHOUT REFIT
```

Rungs are cumulative and arm-specific. This author-side contract earns none.
Independent contract review can award at most `SPB-L0`. `L1+` requires a
separately authorized exact physical pin, which does not exist and is not
authorized here.

No rung establishes universal state sufficiency, fundamental Markovianity or
indivisibility, tensor/Hilbert/Barandes ontology, one-run actuality,
chronology, spacetime, gravity, or a final theory.

---

## 15. Binding hostile controls

Before a review pin, synthetic or exact mathematical instances must route all
controls below without using target data. Passing these controls earns no
physical result.

### 15.1 Causal and statistical controls

1. copied history label forces TV one although physical futures agree;
2. future-readable material log is projected away;
3. a known nonrandom source `A=U,Y=U` is mistaken for a do-law;
4. positivity fails inside a registered covariate/assignment cell;
5. cross-attempt memory violates an unregistered independence assumption;
6. a descendant of intervention is standardized away;
7. `delta_B=1` makes every boundary pair match;
8. `epsilon_sci=1` makes maximal future disagreement pass;
9. a reader cannot distinguish the predeclared above-margin anchor;
10. the confidence interval straddles the margin;
11. threshold closeness is treated as a transitive quotient;
12. duplicate programmes are called distinct histories;
13. noncompliance or failure is postselected away; and
14. a threshold, bin, candidate, or stopping rule is selected from held-out
    residuals.

### 15.2 Composition and classifier controls

15. normalized conditional kernels are composed without `Pi_0`;
16. incompatible binary feedback kernels are declared a joint law;
17. nonunique binary feedback kernels are assigned a target-selected mixture;
18. model-invalid and nonexistent feedback predicates both fire;
19. a seam confidence set spans no-solution and regular cases;
20. internal seam amplitudes are nonunique but the exterior is unique;
21. both sequential pivots are singular while the global solve is defined;
22. pairwise iid/even-parity marginals agree while the triple differs by TV
    one-half;
23. primary composition passes while `X_C` fails; and
24. `X_S` passes while `X_C` fails.

### 15.3 Carrier, minimality, and source controls

25. an abstract response-history vector is called `T-B3` without hardware;
26. a higher regional kernel is called `R-B3` without additional material
    memory, or its coordinate success is promoted to carrier enlargement;
27. two incomparable candidates pass after their common predecessor fails and
    both are called necessary;
28. a predecessor or competitor is underdetermined but an exclusion or
    necessity claim is printed;
29. target-built anti-control predicts while one proper candidate is unrun;
30. active cable permutation is called passive presentation;
31. equal coherent `S` hides unequal noise or timeout laws;
32. a nonunitary transplantation matrix is called a passive lossless adapter;
33. reset, randomization, or isolation exports a record that is omitted;
34. target-sized response table is called a descended carrier;
35. existing papers or simulations are substituted for an exact run packet;
36. finite registered success is promoted to universal or ontological
    sufficiency; and
37. laboratory schedule or bracketing is promoted to emergent chronology.

---

## 16. Frozen claim set for review

An immutable pin may submit only the following result-neutral claims.

- `V3-C1` — `(Xi,A,E,Y)` separates immutable context, physical assignment,
  execution, and observation without deleting accessible physical traces.
- `V3-C2` — every primary comparison has one common complete attempted-run
  space and an intention-to-treat estimand.
- `V3-C3` — `ID-R` and `ID-T` give explicit sufficient identification routes;
  source closure alone gives none.
- `V3-C4` — exact presentation, validation matching, and empirical screening
  are distinct relations.
- `V3-C5` — matching and screening margins are nonvacuous, target-independent,
  and paired with two-sided power duties.
- `V3-C6` — pass, fail, underdetermined, and invalid decisions are exhaustive
  and disjoint.
- `V3-C7` — `D,S,C,I` and each `X_g` remain logically distinct through the
  outcome layer.
- `V3-C8` — a physical carrier is distinct from its operational response
  coordinate and complete kernel.
- `V3-C9` — temporal memory instruments retain physical backaction, records,
  destinations, residuals, and failures.
- `V3-C10` — `T-B3` and `R-B3` denote only additional material memory carriers
  with explicit physical instrument grammars; abstract target-independent
  history/higher kernels may test coordinate performance but never material
  carrier enlargement.
- `V3-C11` — regional coherent response and complete stochastic response have
  distinct award scopes.
- `V3-C12` — composition includes an explicit normalized target-independent
  initial preparation law.
- `V3-C13` — feedback, seam, and bracketing classifiers are exhaustive,
  disjoint, and finite-resolution safe.
- `V3-C14` — direct pair and triple targets are mandatory and cannot train the
  prediction.
- `V3-C15` — candidate order uses physical embeddings; minimal passing
  antichains and registered necessity are distinct.
- `V3-C16` — every headline predicate names its exact candidate, duty, regime,
  and quantifier; unknown candidates block universal exclusions.
- `V3-C17` — source, randomization, preparation, resource, and inaccessible
  debt remain visible and simulation cannot replace apparatus.
- `V3-C18` — the ladder is cumulative and awards no ontology, actuality,
  chronology, spacetime, gravity, or unification conclusion.

No theorem that a sufficient boundary exists is submitted.

---

## 17. Immutable review and terminal dispositions

### 17.1 Pre-pin closure

Before review, root must freeze and rehash:

```text
[ ] this complete v3 contract
[ ] the terminal v2 pin, reports, and adjudication
[ ] the exact source receipt manifest and P0 audit
[ ] the complete V3-C1--C18 claim set
[ ] all causal statuses and identification assumptions
[ ] all nonvacuity, margin, power, and multiplicity rules
[ ] Pi_0 and the complete composition type
[ ] feedback, seam, and bracketing classifiers
[ ] temporal and regional material carrier families
[ ] physical embeddings, minimality, necessity, and outcome predicates
[ ] resource/source/evaluation firewalls and ladder
[ ] the hostile controls and allowed dispositions
```

Any semantic byte change after the pin invalidates the cycle. No repair review
or second successor is permitted.

### 17.2 Independent seats

Three mutually blind axes review the frozen object.

1. **Seat M — mathematics/statistics:** rebuild causal identification,
   positivity, common spaces, margins and power, initial-law normalization,
   feedback compatibility, seam/bracketing routing, candidate-order logic,
   quantifiers, and every hostile countermodel.
2. **Seat P — physical source/apparatus:** issue separate temporal and
   regional signed subreports plus one joint disposition; verify that every
   carrier, randomizer, preparation, memory device, adapter, reader, failure,
   and resource is physically coherent and that source ceilings remain exact.
3. **Seat O — foundations/ontology:** attack response/referent, intervention/
   conditioning, material-history/abstract-history, presentation/action,
   minimality/necessity, finite/universal, effective/fundamental, information
   destruction, chronology, spacetime, gravity, and actuality conflations.

Reports freeze verbatim and independently before one root adjudication. Root
must reproduce every load-bearing finding; one semantic counterexample
controls over a majority.

### 17.3 Allowed dispositions

```text
SPB-D0 — PROCEDURAL INVALID
  pin, chronology, source, report, or independence authentication fails;

SPB-D1 — REJECT
  the central test is circular, physically incoherent, or defeated in
  principle;

SPB-D2 — SEMANTIC REVIEW FAILED / HALT SPB CONTRACT ROUTE
  the physical question survives but this final contract has a semantic,
  mathematical, statistical, source-scope, or carrier defect;

SPB-D3 — ACCEPT-WITH-SCOPE
  the contract is valid only under a narrower scope already entailed by its
  frozen bytes;

SPB-D4 — ACCEPT
  all submitted claims survive as printed.
```

Only `D3` or `D4` awards `SPB-L0`. `D1` or `D2` terminates the SPB contract
route. No v4, repair, or second successor follows.

---

## 18. Present state and stopping rule

```text
SPB V1 CONTRACT REVIEW:       TERMINAL D2 / IMMUTABLE
SPB V2 CONTRACT REVIEW:       TERMINAL D2 / IMMUTABLE
SPB V3 CONTRACT:              AUTHOR-SIDE CANDIDATE / NOT FROZEN
V3 ROOT PRE-PIN AUDIT:        COMPLETE AUTHOR-SIDE / NOT INDEPENDENT
V3 IMMUTABLE REVIEW PIN:      ABSENT
V3 INDEPENDENT REVIEW:        NOT BEGUN
EXACT TEMPORAL RUN PACKET:    ABSENT
EXACT REGIONAL RUN PACKET:    ABSENT
PHYSICAL ACQUISITION:         NOT AUTHORIZED / NOT BEGUN
SCIENTIFIC BOUNDARY RESULT:   NONE
```

This is the final authorized contract successor. Author-side corrections may
occur only before its immutable pin and must be recorded in the root pre-pin
audit. After pinning, no semantic repair, threshold change, new candidate,
source substitution, apparatus assumption, pilot, acquisition, simulation,
new review, automatic successor, v4, boundary result, ontology selection,
chronology, spacetime, gravity, or unification claim is authorized.
