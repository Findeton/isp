# Paper 03 v3.2 hostile review — category, operator algebra, AQFT, covariance

Date: 2026-08-23

Seat: **A**

Status: **FROZEN INDEPENDENT HOSTILE REPORT**

Verdict: **ACCEPT-WITH-SCOPE**

First decisive semantic counterexample: **none**

Earliest supported positive rung:

```text
P03V32-CAUSAL-FRONTIER-PAIRED-DUAL-SEMANTICS-CONSTRUCTED
```

Strongest supported rung:

```text
P03V32-RELATIVISTIC-QUANTUM-OPERATIONAL-ADEQUACY-WITH-GLOBAL-ONTOLOGY-DEBT
```

Paper 04 status: **closed**

## 0. Corpus authentication, chronology, and blindness

I authenticated the exact protocol state before scientific judgment.

```text
protocol commit required and observed:
f39945272813aa3c1c5183994bbf065bd4609700

protocol parent required and observed:
a4ed54f071fd105dad2a27edd1e5269114a77cb1
```

The hostile protocol authenticated as follows.

```text
v17/note-paper03v32-hostile-review-protocol.md
ordinary SHA-256:
97c734b71f1ba43235f73e8d9c9f96e0362f062a83151dc78208d296bdd8bd85
LF lines: 551
bytes: 22705
normalized self-SHA-256:
6dc047f0405ec7a3fc0698b36b8d0506f0852fd7348df7bcd882706c3758066e
```

Every bound corpus artifact then authenticated byte for byte.

| Artifact | Observed SHA-256 | LF lines | Bytes |
|---|---:|---:|---:|
| `v17/note-paper03v32-complete-boundary-paired-semantics-pin.md` | `bf7b587c521f0b407a8df5c8c657ee69a5891d707c89d0ee3c96d6b6b3c07376` | 848 | 34048 |
| `v17/note-paper03v32-pin-audit-category.md` | `e9e3a7facff3ba7a5332e464c69b985e186fe705a148743b56e83e8c2f75fb8d` | 837 | 47446 |
| `v17/note-paper03v32-pin-audit-quantum.md` | `9d46f9ce90c97c066eb58b3820fb092ab7dcdbfad52a0bd1fdbac83d62902d07` | 752 | 42899 |
| `v17/note-paper03v32-pin-audit-adjudication.md` | `176e3f3e3e755c990464061c42db5ad27a858e915dbd07eed5813515f106322e` | 498 | 19471 |
| `v17/paper-03v32-complete-boundary-relativistic-adequacy.md` | `469ae61c849573c9fe7c70871ca6b60843a080082d07f6850b48213b86d6f7d6` | 1320 | 56462 |
| `v17/note-paper03v32-construction-audit.md` | `a28a921dbc87dd86e0b5e8dfdb7bc5b1712d13f069a14d955ddadc78d1ed90bb` | 528 | 24519 |
| `v17/note-paper03v31-hostile-review-adjudication.md` | `6eb5bc944a2ad255131e48a9fc6c44f583b482f9ea880bc34d748eafd8a34acc` | 632 | 31606 |
| `v17/note-paper03v2-hostile-review-adjudication.md` | `74303ddd93b4aac35d3368760da4a0ad3d442570cb16320467076aa5f93ea358` | 476 | 22617 |
| `v17/note-paper02v2-hostile-review-adjudication.md` | `37e1ada87f17723c248896f77ce03012d809f088632abb50ed01d1b166bed135` | 381 | 19166 |
| `v17/note-paper01-hostile-review-adjudication.md` | `3320414cb8161da33fbce3b1b8d3838cd3989d315de792c24cf24c0c322c2bb1` | 314 | 13844 |
| `v17/paper-00-reality-first-programme.md` | `a9f8456763447eaa25a6a840e8f221f51d961ecd9c3ced649ae35a8616457cbe` | 476 | 21268 |

I read the complete protocol, pin, both pre-construction audits, their
adjudication, frozen candidate, construction audit, upstream adjudications,
era charter, and relevant primary sources.  The author audit was treated as
a trace, never acceptance evidence.  I reconstructed the mathematics rather
than importing its verdict.

I did not open, identify, infer, contact, or discuss either sibling hostile
report.  This file is my sole writable path.  I did not edit a candidate,
authority, protocol, ledger, or prior report; did not stage or commit; did not
run an official evaluator; and used no code to define, repair, or select the
mathematical or physical object.

The authenticated chronology is:

```text
terminal v3.1 boundary-type failure
  -> frozen v3.2 complete-boundary pin
  -> two blind pre-construction audits
  -> independent pin adjudication
  -> one frozen mathematics candidate
  -> result-neutral author audit
  -> frozen hostile protocol
  -> this mutually blind Seat-A report
  -> independent joint adjudication
```

## 1. Verdict and binding reading

The candidate constructs a coherent category of **admitted** complete
predictive boundary packets.  Its exact mathematical improvement is real:

1. every licensed future-readable variable is either represented before the
   completeness test or makes the packet inadmissible;
2. normal integrated states are obtained from an `L1` predual field without
   diffuse point evaluation;
3. one physical arrow retains the exact kernel, normal Heisenberg map,
   source/target tags, interfaces, null ideals, and lineage;
4. the all-reader identity is derived for each physical constructor and then
   composes categorically; and
5. continuous outcomes are represented by an honest common-class,
   jointly-measurable, normal direct-integral construction, with a nonempty
   record-dependent witness.

No exact category, operator-algebra, AQFT, or covariance counterexample
survives the candidate's stated admission conditions.  The acceptance is
bindingly scoped:

- it is a constant-fiber theorem, not a theorem for arbitrary measurable
  fields of nonisomorphic von Neumann algebras;
- it is conditional on one complete frozen licensed interface, not a proof
  of ontic completeness;
- continuous duality requires joint decomposable normality, a common
  technical outcome/null class, target landing, same-instrument posterior
  intertwining, and ensemble closure in addition to fiberwise NEP;
- presentation descent is accepted only for the inherited registered
  equations that are already certified full-packet transports;
- operational congruence retains Paper 02 v2's quantification over all
  admissible source ensembles, compatible prefixes, ancillas, continuations,
  record operations, and readers; and
- all AQFT, covariance, split, Bell, and scheduling claims remain
  conditional on the supplied comparator and named hypotheses.

The two presentation/congruence qualifications make explicit the inherited
quantifier.  They do not change an object, arrow, probability, theorem
domain, product coordinate, or rung.

## 2. Independent mathematical reconstruction

### 2.1 Lower-set path category

Objects are tagged lower-set frontiers.  A primitive from `D` to `E` consumes
exactly `E minus D`, with prerequisites in `D` and matched ports/records.
Finite words compose by concatenation, the empty word is identity, and a
slot-consuming skip remains a nonempty word.  Associativity is literal word
associativity.  A timelike reverse fails typing because the corresponding
lower-set source object does not exist.

The category contains declared laboratory order.  It does not derive a clock
or microscopic chronology.  A topological sort and implementation order are
presentation data.

### 2.2 Complete constant-fiber boundary

The licensed future mechanisms, complete final reader algebras, retained
records, all future-readable memory variables, and presentation/lineage data
freeze before the object.  The nonrecord interface is represented in one
sigma-finite von Neumann algebra `B_D` with separable predual.  The hybrid
algebra is

```text
finite/atomic: O_D = B_D bar-tensor l-infinity(R_D),
integrated:    O_D = B_D bar-tensor L-infinity(R_D,nu_D).
```

The packet retains the boundary tag, regime, class, exact sample space,
ensemble class, record map, complete normal-state field, licensed futures,
and readers.  If two samples have the same record and complete state but a
licensed continuation gives different final-reader statistics, admission
fails.  This exactly excludes v3.1's external readable-bit countermodel.

The result is constant-fiber.  A family `r -> B_{D,r}` of nonisomorphic
algebras is neither represented by the displayed tensor product nor repaired
by padding unless the enlarged sectors and all interface guards are
physically present.  Such a family is refused or separately retyped.

### 2.3 Normal predual `Ens`

For an admitted law `Lambda`, write

```text
eta = (r_D)_* Lambda = h nu_D
```

and let `bar beta_r` be its strongly measurable conditional normal-state
barycentre.  Then

```text
g(r) = h(r) bar beta_r
```

is an `L1(R_D,nu_D;(B_D)_*)` field and

```text
Ens_D(Lambda)(F) = integral <g(r),F(r)> nu_D(dr)
```

is a normal state.  If two representatives of `F` differ on a `nu_D`-null
set, domination makes their difference invisible to `eta`; hence the
functional is representative independent.  Positivity and normalization
come from the state field, and normality comes from the direct-integral
predual.

An unbounded density is allowed when the complete predual field is
integrable.  No `L-infinity` density bound is needed.  A generic diffuse
point evaluation is not representative independent and is not normal.
Declared finite/countable atoms retain exact normal coordinate states.

The ensemble class must close under identity, each primitive, every finite
path, every registered finite preparation mixture, and every registered
positive central restriction.  Thus the v3.1 singleton-class counterexample
is excluded as a domain failure rather than hidden inside the proof.

### 2.4 Exact paired category

An admitted arrow is

```text
(g,D,E,K_a,Phi_a),
```

with unchanged normalized kernel `K_a`, normal UCP map
`Phi_a:O_E -> O_D`, exact tags, ensemble/interface/null/landing predicates,
lineage, and the proved identity

```text
Ens_E(Lambda K_a) = Ens_D(Lambda) o Phi_a
```

for every admitted `Lambda` and target reader.

For `a:D->E` and `b:E->F`, matched composition is

```text
(b o a,D,F,K_a K_b,Phi_a o Phi_b).
```

For every admitted source law,

```text
Ens_F(Lambda K_a K_b)
 = Ens_E(Lambda K_a) o Phi_b
 = Ens_D(Lambda) o Phi_a o Phi_b.
```

Kernel normalization, normal UCP closure, ensemble closure, null-ideal
preservation, endpoint/interface matching, target landing, and concatenated
lineage all compose.  Dirac kernels and identity UCP maps give two-sided
identities.  Kernel composition, map composition, and word concatenation are
associative.  Thus the certified pairs form a category and `Ev`/contravariant
`Heis` are projections, not competing definitions of a physical arrow.

There is one inherited descent qualification.  Because the path category is
already quotiented by registered presentation equations, the semantic
assignment must respect every such equation.  The candidate's full-packet
transport theorem supplies that equality for the inherited harmless
presentation equations.  If an equation lacked that certificate, the
assignment would exist on the free path category but would not descend.  Such
an equation is outside the admitted comparator; it is not licensed merely by
being written down.

### 2.5 Finite retained instruments

For branch maps `J_{r,s}` with normalized complete sum,

```text
[Phi(F)](r) = sum_s J_{r,s}(F(r,s))
```

is normal UCP.  Each branch is generally nonunital, while the nonselective
sum forgets `s`.  Neither substitutes for the record-bearing complete arrow.
The binary measurement followed by result-controlled `I/X` therefore gives

```text
J_0(A) + J_1(X A X),
```

not one branch and not the nonselective channel.  Existing records remain in
the target tuple `(r,s)` unless an explicit discard/coarse-graining changes
the interface.

### 2.6 Continuous decomposable assembly

For each old record `r`, the candidate begins with one common physical
instrument `I_r`, one state-independent normal UCP extension

```text
Ihat_r : B_E bar-tensor L-infinity(S,mu) -> B_D,
```

one frozen record update `j(r,s)`, same-instrument posterior states, and one
jointly Borel outcome/target kernel.  The target hybrid map is

```text
[Phi(F)](r) = Ihat_r(s |-> F(j(r,s))).
```

The condition

```text
j_*(nu_D tensor mu) << nu_E
```

or the printed equivalent instrument-weighted condition ensures that a
target-null representative difference is killed after pullback.  Joint
measurability makes the output a measurable `B_D` field.

The assembled normality claim reconstructs.  For a positive
`omega in L1(R_D,nu_D;(B_D)_*)`, the functional

```text
F |-> integral omega(r)
       (Ihat_r(s |-> F(j(r,s)))) nu_D(dr)
```

is normal: each fiber functional is normal, the family is jointly
measurable, the UCP bound gives domination by `||omega(r)||`, and joint null
transport makes the functional representative independent on `O_E`.
Equivalently, bounded increasing nets are preserved.  This supplies the
preadjoint route announced in Definition 8.2.

For rectangle readers the instrument gives

```text
integral_Delta rho_{r,s}(B) mu_{rho,r}(ds)
 = rho(I_r(B,Delta))
 = rho(Ihat_r(B tensor chi_Delta)).
```

The jointly Borel exact kernel has this outcome law, target record
`j(r,s)`, and same-instrument conditional state barycentre.  Hence the left
side is the sample-kernel expectation of the same reader.  Linearity handles
finite simple fields.  The span of rectangle tensors is ultraweakly dense,
and both sides are normal, so equality extends to all of `O_E`; ensemble
integration then gives the paired identity.  This ultraweak-density argument
is the precise reading of the candidate's shorter “monotone convergence and
normality” sentence.

Fiberwise NEP alone proves none of the common-class, joint-measurability,
assembled-normality, target-landing, exact-kernel, state-class, or record
retention clauses.  The candidate explicitly refuses every missing case.

### 2.7 Continuous nonvacuity witness

With old record `r in {0,1}`, outcome `s in [0,1]`, and

```text
E_{r,s} = I + (-1)^r epsilon cos(2 pi s) Z,
0 < epsilon < 1,
M_{r,s} = E_{r,s}^{1/2},
```

both eigenvalues of `E_{r,s}` are strictly positive and

```text
integral_0^1 E_{r,s} ds = I.
```

Thus

```text
I_r(B,Delta) = integral_Delta M_{r,s}^* B M_{r,s} ds
```

is a complete normal instrument and the corresponding operator integral is
normal UCP.  The outcome density is bounded below by `1-epsilon`, so the
posterior is jointly Borel without a null-outcome convention.  The target
sample explicitly contains `(r,s,rho_{r,s})`, and the target record is
`(r,s)`.  Counting measure times Lebesgue measure supplies the common class.
The old record is retained and the sample/Heisenberg routes agree reader by
reader.  This is a genuinely continuous, informative, record-dependent
example, not a finite lookup or an uninformative label.

### 2.8 Other constructors and finite paths

Preparation, deterministic channel, finite/continuous instrument, record
write, guard, trusted randomization, coarse-graining, discard, localized
coupling, ancillary probe, and explicit skip each derive `K` and `Phi` from
one frozen datum.  A row whose state class, null ideal, interface, landing,
or lineage fails is not admitted.

Primitive compatibility plus intermediate ensemble closure yields every
finite path by category composition.  This is a theorem for finite words,
not evidence for an infinite stream.

### 2.9 Future sufficiency, presentation, and congruence

If every licensed continuation is a paired arrow, equality of source `Ens`
states gives equality of every final-reader law.  This is future sufficiency
for the frozen represented family.  It is not injectivity on histories, a
native division, a selected state, or ontic Markov dynamics.

Full packet isomorphism transports comparator, boundary tags, algebras,
classes, samples, ensemble fields, futures/readers, both semantic layers,
sources, apparatus, record permutations, multiplicities, and certificates.
A proper `Loc` embedding gives observable transport and state pullback only.

The operational-congruence theorem is accepted with the inherited Paper 02
v2 meaning of “complete operational equivalence”: equality is tested for all
admitted source ensembles and compatible prefixes/ancillas as well as every
reachable continuation and final reader.  Without the prefix/source
quantifier, postcomposition would be immediate but precomposition would not
follow.  The inherited complete-context quantifier is already part of the
bound procedure quotient, so spelling it out changes no equivalence class.

### 2.10 AQFT and covariance

The Fewster--Verch system--probe scheme starts from supplied system/probe
theories, compact coupling region, scattering automorphism, and normal probe
state.  Positive probe effects induce CP maps; a complete probe observable
gives a normalized nonselective map.  Causal factorization supports ordered
composition and, for causally disjoint coupling regions, either order.  These
are comparator results on supplied spacetime and supplied couplings.

Nonselective no-signalling is operation-level and conditional on the complete
localized operation and source hypotheses.  Selected steering remains a
conditional comparison.  The Bell statement keeps measurement independence,
source lineage, parameter/outcome independence, and factorization distinct.

BFV covariance transports observables along supplied `Loc` morphisms and
pulls states back.  It does not select a natural contingent state or push one
forward canonically.  Split/tensor language is used only with named
separation/collar and gauge-compatible hypotheses.  Type-III local algebras
use normal functionals rather than a universal trace or density matrix.
Reeh--Schlieder statements retain norm, energy, success, postselection,
communication, and record costs.

No AQFT theorem used here derives its Lorentzian spacetime, field content,
state class, apparatus, ontology, or gravity.

## 3. Quantifier ledger

| Quantifier label | Exact candidate statements |
|---|---|
| `FOR-EVERY-ADMITTED-PACKET` | complete-boundary refusal test; normal `Ens`; paired category; finite-path duality; conditioning identity; full-packet transport |
| `CONDITIONAL-ON-NAMED-HYPOTHESES` | continuous instruments; decomposable feedback; localized system--probe schemes; causal factorization; no-signalling; steering; Bell; split; covariance; exchange certificates |
| `EXISTS-ONE-NAMED-COMPARATOR` | old-record continuous qubit witness; inherited finite relativistic laboratory comparators |
| `FINITE/ATOMIC-EXACT-CONTROL` | direct-sum complete instruments; exact atoms; binary `I/X`; explicit skip |
| `INTEGRATED-ENSEMBLE-ONLY` | diffuse record normal states, a.e. posteriors, normal all-reader duality, diffuse feedback |
| `REFUSAL/NONIMPLICATION` | hidden memory, late reader, variable fibers, false common class, non-NEP, ambient-only landing, singular update, nonmeasurable assembly, diffuse point restart, partial transport, ontology/time/gravity promotions |
| `UNCONSTRUCTED` | selected actuality, universal law/state, local microontology, internal clock, emergent spacetime, matter--geometry reciprocity, gravity, empirical ontology discriminator |

The technical reference class is never a physical prior.  Packet-relative
completeness is never ontic completeness.  Equality of endpoint predictors
never identifies earlier histories.

## 4. Target matrix V32-T1--V32-T24

| Target | Seat-A disposition | Exact boundary |
|---|---|---|
| V32-T1 | `CONSTRUCTED` | finite lower-set path category; no serialization clock |
| V32-T2 | `CONDITIONAL` | every licensed variable represented after freeze, otherwise packet refused |
| V32-T3 | `CONSTRUCTED` | finite/countable atomic hybrid algebra and normal point states |
| V32-T4 | `CONSTRUCTED` | normal integrated `Ens` from the `L1` predual field |
| V32-T5 | `REFUSED` | generic diffuse point evaluation/restart |
| V32-T6 | `CONDITIONAL` | admission requires identity/path/mixture/positive-conditioning closure |
| V32-T7 | `CONDITIONAL` | deterministic, stochastic, and joint-update null preservation required |
| V32-T8 | `CONSTRUCTED` | exact five-field paired hom-set |
| V32-T9 | `CONSTRUCTED` | identities, total matched composition, associativity |
| V32-T10 | `CONSTRUCTED` | `Ev` and contravariant `Heis` projections; registered presentation descent scoped as above |
| V32-T11 | `CONSTRUCTED` | complete finite arrow, branch, and nonselective map distinguished |
| V32-T12 | `CONDITIONAL` | continuous duality derived under the full Definitions 8.1--8.3 package |
| V32-T13 | `CONDITIONAL` | every admitted constructor has one common-data proof |
| V32-T14 | `CONSTRUCTED` | paired primitive identities compose along every admitted finite path |
| V32-T15 | `CONDITIONAL` | measurable decomposable feedback; no point restart |
| V32-T16 | `CONSTRUCTED` | positive central restriction/compression; null refusal |
| V32-T17 | `CONDITIONAL` | frozen-family representation-level sufficiency only |
| V32-T18 | `CONDITIONAL` | full packet transport and complete-context reachable congruence |
| V32-T19 | `CONDITIONAL` | exact two-layer equality in every reachable adaptive context |
| V32-T20 | `CONDITIONAL` | inherited localized no-signalling/steering/Bell scope only |
| V32-T21 | `CONSTRUCTED` | finite histories indexed by initial sample/law; prefix coherence |
| V32-T22 | `CONSTRUCTED` | record/conditioning/division/actuality distinctions |
| V32-T23 | `UNCONSTRUCTED` | no selected Barandes completion or actuality rule |
| V32-T24 | `UNCONSTRUCTED` | ontology, internal time, spacetime, reciprocity, gravity |

## 5. Two-way controls C1--C40

| ID | Positive arm reconstruction | Hostile arm reconstruction |
|---|---|---|
| C1 | complete `B_D` contains all licensed nonrecord memory | later-readable external factor fails completeness and admission |
| C2 | future/readers freeze first | late reader defines a new packet |
| C3 | one fully tagged object carries algebra, classes, fields, interfaces | abstractly equal algebra with different memory interface is a different object |
| C4 | normalized direct-sum map is one complete arrow | branch list is not one UCP complete arrow |
| C5 | record update retains old record | silent old-fiber loss is untyped absent licensed discard |
| C6 | retained binary result drives `I/X` | nonselective sum has forgotten the guard |
| C7 | declared atom gives normal coordinate state | generic diffuse evaluation is undefined/nonnormal |
| C8 | dominated predual ensemble gives normal state | diffuse Dirac restart is singular |
| C9 | each primitive/path preserves target ensemble class | class-escaping primitive is not an arrow |
| C10 | every registered positive restriction remains admitted | conditional outside class rejects packet |
| C11 | one common null ideal types a packet | route-indexed singular classes split schemas |
| C12 | nonsingular record map defines normal pullback | measurable singular pullback is undefined on quotient |
| C13 | full stochastic update preserves target null ideal | passing deterministic slices does not certify mixture |
| C14 | physical arrow contains exact `(K,Phi)` plus lineage | `Phi` alone is only a projection |
| C15 | all-reader identity is derived constructor by constructor | inserting it as a physical constructor axiom is forbidden |
| C16 | normal UCP extension lands in exact target | ambient-only extension is refused |
| C17 | exact continuous theorem uses NEP/stronger named result plus all extra gates | non-NEP or approximate-NEP promotion fails |
| C18 | extension/posterior family is jointly measurable | separate nonmeasurable statewise versions do not assemble |
| C19 | one jointly Borel frozen sample kernel | version chosen after outcome is not exact semantics |
| C20 | rectangles/simple tensors plus normal extension cover all readers | one scalar reader proves no universal identity |
| C21 | primitive certification plus category gives every finite path | depth checks and infinite extrapolation fail |
| C22 | present record guards later primitive | future outcome cannot guard past source slot |
| C23 | measurable decomposable feedback is a normal pair | nonmeasurable pointwise policy is not an arrow |
| C24 | discard removes fine reader from target | later fine read changes packet |
| C25 | explicit skip consumes a slot and retains nonempty path tag | semantic identity projections do not make it the empty path |
| C26 | exchange compares exact full pair | equality of one layer is insufficient |
| C27 | every reachable adaptive context is certified | initial-context certificate is insufficient |
| C28 | record permutation and occurrence multiplicity explicit | silent reorder/collapse changes arrow |
| C29 | product source and lineage exposed | hidden correlation changes mechanism/premises |
| C30 | full packet isomorphism transports contingent comparison | proper `Loc` embedding gives no canonical state pushforward |
| C31 | nonselective remote marginal unchanged under hypotheses | selected steering is not signalling |
| C32 | Bell premise/source ledger retained | no-signalling is not Bell locality |
| C33 | material/KMS/apparatus frame contingent | no law-level foliation follows |
| C34 | type-III sector uses normal functional | no universal density matrix or trace |
| C35 | split use names collar/separation/gauge hypotheses | touching or gauge-constrained regions are not automatic tensor factors |
| C36 | record retained/readable for frozen future | not permanent, selected, or actual |
| C37 | represented boundary future-sufficient for one family | frontier/sample is not automatically division |
| C38 | laboratory slots are comparator protocol | not microscopic time or event web |
| C39 | Lorentzian comparator supplied | not emergent or dynamically responding geometry |
| C40 | all v2 physical data unchanged | retuning probability/instrument to pass representation is forbidden |

## 6. Mandatory attacks 1--64

1. **External readable bit:** fails Definition 3.5 unless represented before
   testing; v3.1 M0 is absent from the admitted domain.
2. **Represented bit:** a central observable/state component in `B_D` makes
   the future read a reader on `O_D`.
3. **Late reader:** changes the frozen object and requires a new packet.
4. **Equal quantum/record marginals, different apparatus latch:** full state
   field must distinguish them or the packet is refused.
5. **Unlicensed idle bit:** ignored without proving its existence or absence.
6. **Same algebra, different interface:** tagged objects remain unequal.
7. **Branch for complete arrow:** generally nonunital and rejected.
8. **Nonselective for retained record:** loses the guard result and fails.
9. **Drop old record:** refused absent explicit licensed discard/coarse-grain.
10. **Binary `I/X`:** reconstructed exactly by the complete direct-sum map.
11. **Lebesgue-null point:** no representative-independent normal evaluation.
12. **Diffuse Dirac law:** singular to the admitted class and refused.
13. **Mixed atomic/diffuse:** exact restart at declared atoms only.
14. **Null representative change:** `Ens` unchanged; frozen exact kernel may
    remain distinct.
15. **No strongly measurable barycentre:** domination alone fails admission.
16. **Unbounded integrable density:** accepted at `L1`; no false `L-infinity`
    restriction.
17. **Singleton ensemble class:** positive-conditioning closure fails.
18. **Positive event in closed class:** restriction, central compression, and
    normalization agree.
19. **Null event:** normalization and restart refused.
20. **Primitive exits class:** pair and longer paths are not typed.
21. **Uncountably singular routes:** common packet splits or is refused.
22. **Singular deterministic map:** no map on target `L-infinity` quotient.
23. **Stochastic singularization:** whole-kernel null test independently
    required.
24. **Equivalent then singular class:** first is gauge; second changes type.
25. **Class as physical prior:** interpretation rejected.
26. **Good kernel/map, unequal readers:** tuple fails hom predicate.
27. **Good map, class-escaping kernel:** tuple fails hom predicate.
28. **Two valid pairs:** every hom predicate composes; all-reader identity
    recomputes by the two-step equation.
29. **Mismatched tags:** no categorical composition.
30. **Skip collapsed to empty word:** physical path tag prevents collapse.
31. **Continuous non-NEP instrument:** outside exact category.
32. **Approximate NEP:** supplies no exact comparator equality.
33. **Ambient-only extension:** target landing fails.
34. **Statewise posteriors without joint kernel:** no exact `Ev` arrow.
35. **Post-outcome version choice:** violates packet freeze.
36. **One effect checked:** another reader can separate the semantics.
37. **Simple readers, no normal extension:** proof incomplete.
38. **Rectangles plus normal extension:** reconstructed as the correct route.
39. **Continuous nonvacuity:** qubit witness is normalized, informative,
    jointly Borel, and retains old record.
40. **Weakly repeatable non-NEP:** exact retained representation refused.
41. **Future guard on past slot:** source/target typing fails.
42. **Nonmeasurable policy:** no decomposable normal arrow.
43. **Read discarded fine value:** target interface changes.
44. **Singular revealing preparation:** new class/schema; cannot reveal an old
    null representative inside the admitted theorem.
45. **Infinite stream from finite induction:** no such inference.
46. **Integrated equality as exact kernel equality:** exact/a.e. firewall
    rejects it.
47. **One state/reader exchange:** insufficient.
48. **One semantic layer exchange:** insufficient.
49. **Missing record permutation/multiplicity:** certificate incomplete.
50. **Hidden source/apparatus correlation:** lineage/premise failure.
51. **Initial-only exchange:** reachable adaptive context may distinguish it.
52. **Uncertified incomparable swap:** no schedule theorem.
53. **Partial transport:** no full packet isomorphism.
54. **Forward contingent state on proper embedding:** no canonical map.
55. **Selected steering as remote marginal:** conditional/nonselective error.
56. **No-signalling as Bell locality:** premise error.
57. **Universal type-III density/tensor:** operator-algebra type error.
58. **Cost-free Reeh--Schlieder:** physical resource ledger omitted.
59. **KMS frame as foliation:** state/material structure is not law structure.
60. **Record as permanent/actual/division:** none follows.
61. **Slots/records as time/volume atoms:** interpretation unsupported.
62. **Complete predictor as beable algebra:** ontology selection absent.
63. **Relative Cauchy evolution as gravity:** supplied-background response is
    not backreaction.
64. **Premature Paper 04:** prohibited pending terminal adjudication and a new
    result-neutral pin.

## 7. Pin-audit countermodels A1--A18

| ID | Independent hostile disposition |
|---|---|
| A1 | Nonisomorphic record fibers lie outside constant-fiber scope; split or refuse. |
| A2 | Padding into one large fiber introduces false sectors unless exact guards/states prove them operationally absent; otherwise reject. |
| A3 | Exact diffuse memory cannot be made a normal point state by hiding it in `L-infinity`; retype or refuse. |
| A4 | Uncountably many deterministic Dirac outcome classes need not admit one sigma-finite dominator; split/refuse. |
| A5 | A nonmeasurable choice of normal extensions gives no hybrid field; reject. |
| A6 | Fiber normality alone is insufficient; the reconstructed predual/monotone proof is required. |
| A7 | Joint update mapping positive source mass into target-null set destroys representative independence; reject. |
| A8 | Bit-flip kernel paired with identity UCP map fails a bit reader; not a morphism. |
| A9 | Two valid pairs pass compatibility and all other predicates under matched composition. |
| A10 | Explicit skip and empty word retain different physical path tags despite equal projections. |
| A11 | Path-closed but conditioning-open class fails Definition 4.1. |
| A12 | Continuous randomization does not follow from finite convex closure; register its source/pair/class or refuse. |
| A13 | Equal displayed state with different licensed future profile fails completeness. |
| A14 | Null-version kernel difference is invisible integrated but blocks exact exchange. |
| A15 | `j(r,s)=s` erases old `r`; reject absent licensed discard. |
| A16 | Algebra transport omitting class/state/interface is not packet isomorphism. |
| A17 | Proper local inclusion admits many state extensions; no canonical forward state. |
| A18 | Nonisomorphic outcome branch algebras lie outside the constant-fiber theorem. |

## 8. Pin-audit countermodels Q1--Q18

| ID | Independent hostile disposition |
|---|---|
| Q1 | Same complete displayed state but different future kernel fails sample-level completeness. |
| Q2 | Path closure without conditioning closure rejects the packet. |
| Q3 | Conditioning closure without registered mixture closure rejects the packet. |
| Q4 | Fiberwise NEP with nonmeasurable record dependence gives no assembled arrow. |
| Q5 | No common operator-control outcome class forces schema split/refusal. |
| Q6 | Statewise Borel versions selected nonmeasurably in input give no Markov kernel. |
| Q7 | Old record overwrite fails interface unless explicitly typed discard. |
| Q8 | Update violating target null ideal gives representative-dependent `Phi`. |
| Q9 | Posterior from a second instrument fails quantum target readers. |
| Q10 | Simple-reader equality without normality is incomplete. |
| Q11 | Outcome-dependent nonisomorphic target algebras are outside theorem. |
| Q12 | Normal posterior outside physical state/ensemble class fails closure. |
| Q13 | Feedback targeting null-version difference preserves integrated law but cannot certify exact exchange. |
| Q14 | Equal final hybrid state does not identify unequal registered histories. |
| Q15 | History without initial sample/law is a kernel, not one probability measure. |
| Q16 | Steering branch outside ensemble class cannot seed a continuation. |
| Q17 | Source correlated with settings removes Bell premise even if no-signalling survives. |
| Q18 | Freeze covers the entire reader algebra/interface, not a finite test list. |

## 9. Fresh hostile Seat-A attacks HA1--HA18

These attacks were constructed for this hostile review.  HA1--HA12 cover the
protocol's mandatory Seat-A families; HA13--HA18 probe additional descent,
normality, center, and covariance boundaries.

### HA1 — future-readable variable absent from `B_D`

Let `X_D=[0,1]x{0,1}`, let record be the first coordinate, and let `b_D` be
independent of the bit.  License a later bit reader.  Equal represented
boundaries then have probabilities zero and one.  Definition 3.5 refuses the
packet.  The attack confirms the v3.1 defect is not admitted.

### HA2 — nonconstant measurable field

Take `B_0=C` and `B_1=M_2(C)` over two record outcomes.  There is no one
constant fiber with the exact intended interfaces.  The direct sum
`C direct-sum M_2` is a varying-fiber hybrid, not the displayed
`B bar-tensor l-infinity({0,1})`.  The candidate scopes it out exactly.

### HA3 — no strongly measurable conditional predual field

Give a dominated record marginal but a state assignment with no strongly
measurable conditional barycentre in the declared predual type.  Scalar
domination alone cannot define the Bochner `L1` field.  The law is excluded
from `E_D`, so Theorem 4.3 is not falsely applied.

### HA4 — unbounded integrable field

On `(0,1]`, take scalar density `h(r)=1/(2 sqrt(r))`, normalized in `L1` but
unbounded.  With a fixed normal state `beta`, `h beta` is a valid predual
field.  The candidate admits it; an `L-infinity` density requirement would
have been a false exclusion.

### HA5 — null representatives

Let `F` and `G` differ only at one Lebesgue-null point.  Every admitted
dominated `Ens` gives equal values.  An exact frozen sample kernel may still
distinguish chosen representatives at that point, so integrated equality
does not certify exact exchange.  Both candidate firewalls behave correctly.

### HA6 — false common class for singular routes

Index uncountably many incoming routes by `r` and assign record law
`delta_r`.  A sigma-finite measure cannot give positive mass to uncountably
many atoms.  No common class dominates all routes.  The packet must split or
be refused; the candidate does not call the missing class a uniform prior.

### HA7 — target-null graph append

Let a deterministic append map `r` to `(r,s_0)` while the target carries
product diffuse measure in the new coordinate.  The image graph is target
null but has full source mass.  Pullback of its zero class is nonzero, so no
map on the quotient exists.  Candidate Lemma 9.3 requires a graph-compatible
target class or refusal.

### HA8 — normal fibers, no measurable assembly

Choose two normal UCP maps and select between them by a nonmeasurable subset
of the retained-record space.  Each fiber is normal, but a separating target
observable yields a nonmeasurable output field.  Definition 8.1 rejects the
family.  Fiberwise NEP is not promoted to a decomposable arrow.

### HA9 — valid kernel with incompatible map

On one classical bit, pair the deterministic flip kernel with the identity
Heisenberg map.  Both projections are individually valid channels, but the
bit reader has opposite expectations.  The exact tuple fails the all-reader
predicate.

### HA10 — closure of every hom predicate

Compose two admitted pairs and test tags, ensembles, null ideals, memory,
landing, lineage, and reader equality separately.  Source/target matching
concatenates tags; ensemble/null/interface implications compose; the range
of the first Heisenberg map is the second source algebra; lineage words
concatenate; and the two-step `Ens` calculation proves compatibility.  The
composite remains in the hom-set.

### HA11 — partial packet transport

Transport `O_D` by a normal star-isomorphism but leave `nu_D`, `E_D`,
`b_D`, a source tag, or occurrence multiplicity fixed.  Admissible null
events or reader predictions can change.  The candidate correctly refuses
this as a packet isomorphism.

### HA12 — type-III/gauge fake tensor factor

Replace a type-III or gauge-constrained regional algebra by a density matrix
on a Hilbert tensor factor without a verified split/gauge interface.  This
changes algebra type and admissible observables.  Propositions 18.1--18.2
reject it.

### HA13 — presentation equation without semantic descent

In a free path category, register a formal equation `p=q` while their exact
kernels differ.  Both paths may separately be certified, but no functor from
the quotient exists.  This attack is excluded only when every registered
presentation equation is a certified full-packet transport.  That is the
binding inherited reading of Definitions 2.2 and 12.1--12.2.  An arbitrary
post-hoc equation is not admitted.

### HA14 — postcomposition-only equivalence

Define two arrows to agree for one default source and every continuation but
to differ after a compatible prefix prepares another admitted state.  This
relation is not a congruence under precomposition.  The candidate's theorem
passes only with Paper 02 v2's complete-context quantifier over all admitted
prefixes/source ensembles.  This is the binding reading recorded in Section
2.9.

### HA15 — center omitted under algebra isomorphism

A normal star-isomorphism automatically transports the center, but a mere
linear UCP bijection need not preserve the record projections or their
multiplicity.  Presentation equivalence therefore requires the printed full
star-isomorphism/packet data, not a channel isomorphism.  Candidate scope
passes.

### HA16 — sequence-normal but not proven net-normal

Checking only increasing sequences is insufficient in a general von Neumann
algebra unless sigma-finiteness/separability is used appropriately.  The
candidate's normal UCP hypothesis and predual construction establish full
normality for bounded increasing nets.  A numerical or sequential test alone
would fail.

### HA17 — nonfaithful calibration hides a null operator effect

Let one calibration state assign zero probability to a nonzero positive
effect.  Scalar null preservation in that state does not make the effect zero
or define an algebraic null ideal.  The candidate requires state-independent
operator/measure-class preservation and all-reader identity, so the pair is
not certified from that calibration alone.

### HA18 — covariance with a selected state extension

For a proper inclusion of local algebras, choose two different normal state
extensions of the same smaller-algebra state.  Both pull back identically,
so covariance cannot select one forward extension.  The candidate retains
observable covariance and contravariant pullback only, and the attack passes.

No fresh attack changes the verdict.  HA13 and HA14 produce the two binding
scope clarifications; neither changes the inherited mathematical object.

## 10. Full 41-coordinate product

```text
input: BOUND-UNCHANGED
slot-skeleton: CONSTRUCTED-INHERITED-LABORATORY-PROTOCOL; NOT TIME
frontier: CONSTRUCTED-TYPED-LOWER-SET; NOT AUTOMATIC DIVISION
licensed-future-interface: CONSTRUCTED-AS-FROZEN-PACKET-DATA
complete-predictive-algebra: CONSTRUCTED-CONDITIONALLY-CONSTANT-FIBER
boundary: CONSTRUCTED-FULLY-TAGGED-PREDICTIVE-OBJECT
sample-semantics: CONSTRUCTED/INHERITED-EXACT-FROZEN-KERNELS
ensemble-semantics: CONSTRUCTED-NORMAL-PREDUAL-ENS
ensemble-closure: CONSTRUCTED-AS-ADMISSION-GATE
paired-arrow: CONSTRUCTED
paired-category: CONSTRUCTED
ev-projection: CONSTRUCTED/INHERITED
heis-projection: CONSTRUCTED-CONTRAVARIANT
continuous-derivation: CONSTRUCTED-CONDITIONALLY-NONEMPTY
integrated-compatibility: CONSTRUCTED-AS-DERIVED-CONCLUSION
point-restart: FINITE/ATOMIC-CONSTRUCTED;
  NONATOMIC-GENERIC-REFUSED
presentation: CONSTRUCTED-FULL-PACKET;
  DESCENT-ONLY-FOR-CERTIFIED-REGISTERED-EQUATIONS
quotient: CONSTRUCTED-REACHABLE-COMPLETE-CONTEXT-SCOPED
covariance: CONSTRUCTED/INHERITED-CONDITIONALLY
instrument: FINITE-CONSTRUCTED;
  CONTINUOUS-CONSTRUCTED-CONDITIONALLY
causal-factorization: INHERITED-CONDITIONAL-ON-NAMED-SCHEMES
certified-schedule: CONSTRUCTED-CONDITIONALLY-ON-ALL-REACHABLE-CONTEXTS
no-signalling: INHERITED-SCOPED-NONSELECTIVE
steering: INHERITED-SELECTIVE-CONTROL
bell: INHERITED-PREMISE-INDEXED-COMPATIBILITY
positive-model: CONSTRUCTED/INHERITED-WITH-COSTS
context: EXPLICIT-REGISTERED-COMPLETE-INTERFACE
fibers: CONSTANT-FIBER-SCOPE; IDLE-ONTOLOGY-UNSELECTED
type-III: NORMAL-FUNCTIONAL-SCOPE
split: NAMED-HYPOTHESIS-ONLY
gauge: TYPED-UNSELECTED
particles: TYPED-UNSELECTED
continuum: SUPPLIED-COMPARATOR; NO DERIVATION
UV: UNCONSTRUCTED
preferred-frame: NO-LAW-LEVEL-FOLIATION;
  CONTINGENT-MATERIAL/KMS/APPARATUS-FRAMES-ALLOWED
record: OPERATIONALLY-CONSTRUCTED/RETAINED;
  NOT PERMANENT OR ACTUAL
division: REPRESENTATION-LEVEL-FUTURE-SUFFICIENCY-CONDITIONAL;
  NATIVE-DIVISION-UNCONSTRUCTED
actuality: UNCONSTRUCTED
barandes: COMPATIBLE-BUT-INCOMPLETE
ontology: GLOBAL-PREDICTIVE-REPRESENTATION-UNSELECTED
downstream: CLOSED
```

The product is noncompensatory.  Constant-fiber, normality, same-instrument,
landing, closure, presentation, or reachable-context certification failure
would demote its exact coordinate even if all other rows survived.

## 11. Outcome ladder

| Rung | Seat-A adjudication |
|---|---|
| `P03V32-COMPLETE-BOUNDARY-TYPE-FAILURE` | surpassed: v3.1 M0 is refused by the frozen object domain |
| `P03V32-ENSEMBLE-NORMALITY-OR-CLOSURE-FAILURE` | surpassed on admitted packets: normal predual and all closure gates reconstruct |
| `P03V32-PAIRED-OPERATION-CATEGORY-UNCONSTRUCTED` | surpassed: exact pair hom-set and category laws reconstruct |
| `P03V32-CONTINUOUS-SAME-INSTRUMENT-DUALITY-INCOMPATIBLE` | surpassed conditionally: theorem and genuine continuous witness reconstruct |
| `P03V32-CERTIFIED-CONCURRENCY-UNCONSTRUCTED` | surpassed only on the fully certified reachable domain |
| `P03V32-CAUSAL-FRONTIER-PAIRED-DUAL-SEMANTICS-CONSTRUCTED` | **earliest supported positive rung** |
| `P03V32-RELATIVISTIC-QUANTUM-OPERATIONAL-ADEQUACY-WITH-GLOBAL-ONTOLOGY-DEBT` | **strongest supported rung** |
| `P03V32-RELATIVISTIC-LOCAL-MICROONTOLOGY-CONSTRUCTED-WITH-COSTS` | impossible to award; no selected local beables or actuality |
| `P03V32-EMPIRICALLY-DISCRIMINATED-RELATIVISTIC-ONTOLOGY` | impossible to award; no empirical discriminator |

Rung 7 is conditional on the supplied Lorentzian/AQFT comparator and every
packet admission gate.  It is not a universal QFT, ontology, or spacetime
result.

## 12. Bounded clarifications versus semantic repairs

### Bounded clarifications

1. State explicitly beside Theorem 6.3 that every registered presentation
   equation is one of the inherited full-packet transports of Theorem 12.2;
   otherwise functor descent is not automatic.
2. State explicitly in Definition 12.3 that complete operational equivalence
   quantifies over all admitted source ensembles and compatible prefixes and
   ancillas, as already bound by Paper 02 v2.  This makes the precomposition
   half of congruence visible.
3. Replace the informal “monotone convergence” wording in Theorem 8.4 by the
   precise statement that rectangle tensors are ultraweakly dense and both
   sides are normal, or print the equivalent full predual argument.
4. In every general continuous use, repeat that joint measurability means
   enough measurability to define the decomposable normal map and its
   preadjoint; fiberwise measurability alone is not intended.

These clarifications alter no object, arrow, kernel, instrument, probability,
quantifier domain, product coordinate, or supported rung.

### Changes that would be semantic, not bounded

- adding outcome-dependent nonisomorphic algebra fields;
- choosing a common measure class after observing a failed construction;
- replacing exact frozen kernels by almost-everywhere classes;
- dropping old records without an already licensed discard;
- weakening all-reader identity to one state or outcome marginal;
- adding a future reader after completeness evaluation;
- extending finite path induction to an infinite law;
- retuning v2 instruments, states, or probabilities; or
- promoting the complete predictor to ontology, division, time, geometry, or
  gravity.

No such repair is needed for this verdict and none is authorized.

## 13. Primary-source scope

The primary sources support only the following exact uses.

1. [Okamura--Ozawa, *Measurement theory in local quantum physics*](https://arxiv.org/abs/1501.00239)
   establishes NEP/measuring-process representation and its connection to
   strongly measurable posterior families under the paper's hypotheses.  It
   also supplies non-NEP examples and only approximate realization for the
   broader injective case.  It does not supply joint old-record dependence,
   the candidate's common class, target packet, exact sample kernel, ensemble
   closure, or paired identity automatically.
2. [Fewster--Verch, *Quantum fields and local measurements*](https://arxiv.org/abs/1810.06512)
   constructs localized system--probe instruments and proves causal-order
   composition under a verified causal-factorization property.  Causally
   disjoint schemes may be composed in either order under those hypotheses.
   The source does not localize arbitrary CP maps, derive the spacetime, or
   solve measurement actuality.
3. [Fewster--Verch, *Measurement in Quantum Field Theory*](https://arxiv.org/abs/2304.13356)
   reviews the same localized-coupling route and its selective/nonselective
   causal scope; it does not strengthen the candidate's ontology claim.
4. [Brunetti--Fredenhagen--Verch, *The generally covariant locality principle*](https://arxiv.org/abs/math-ph/0112041)
   supplies covariant observable functors and natural fields on a supplied
   category of spacetimes.  Contingent states pull back; no canonical forward
   extension or emergent metric follows.
5. [Coecke--Heunen--Kissinger, *Categories of Quantum and Classical Channels*](https://arxiv.org/abs/1305.3821)
   supports finite classical/quantum CP-channel coexistence.  It does not
   prove this direct-integral W-star theorem.
6. [Fewster, *The split property for quantum field theories in flat and curved spacetimes*](https://arxiv.org/abs/1601.06936)
   supports split inclusions under named hypotheses; it does not make every
   pair of local or gauge-constrained regions a tensor product.
7. [Fewster--Verch, *Dynamical locality and covariance*](https://arxiv.org/abs/1106.4785)
   supports the covariance/natural-state firewall and relative Cauchy
   evolution on supplied geometries; it does not select a contingent state or
   turn background response into gravity.
8. [Summers--Werner, *Maximal violation of Bell's inequalities is generic in quantum field theory*](https://doi.org/10.1007/BF01207366)
   is a hypothesis-specific QFT Bell existence result.  It neither supplies
   universal localized probe realizations nor Bell-local causation.

None of these sources selects actuality, a universal stochastic law, a local
microontology, internal time, emergent spacetime, or gravity.

## 14. Final verdict

The candidate's mathematics survives the Seat-A hostile lens.  The complete
boundary excludes hidden licensed memory; the direct-integral `Ens` state is
normal and representative independent; finite and continuous hybrid arrows
are coherently typed; all matched pair compositions form a category; exact
and almost-everywhere levels remain separate; and AQFT/covariance claims stay
inside their supplied-background hypotheses.

The two binding clarifications concern inherited presentation and operational
context quantifiers, not a new mathematical object.  Therefore:

```text
VERDICT: ACCEPT-WITH-SCOPE
FIRST DECISIVE SEMANTIC COUNTEREXAMPLE: none
EARLIEST SUPPORTED POSITIVE RUNG:
P03V32-CAUSAL-FRONTIER-PAIRED-DUAL-SEMANTICS-CONSTRUCTED
STRONGEST SUPPORTED RUNG:
P03V32-RELATIVISTIC-QUANTUM-OPERATIONAL-ADEQUACY-WITH-GLOBAL-ONTOLOGY-DEBT
```

No implementation, selected ontology, actuality, clock, spacetime, or gravity
result follows.  Paper 04 remains closed pending independent joint
adjudication and a separately frozen successor question.

## 15. Freeze authentication

Final bound-corpus reauthentication: **PASS**

Report LF line count: `000980`

Report byte count: `050322`

Report ordinary SHA-256: reported externally after the final byte is fixed;
an ordinary self-hash cannot be embedded in the file it hashes.

Report normalized self-SHA-256: `a0019dd9938ded3f5d5279398b5d51899a4cf1ea9082c16ecfbd71f551b68d6a`

Normalization rule: replace the six decimal digits on each count line and
the 64 hexadecimal characters on the normalized-self line by ASCII zeroes,
preserve every other byte, and compute SHA-256.  The report ends in one LF,
contains no trailing horizontal whitespace, and remains unstaged and
uncommitted.
