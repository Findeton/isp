# Paper 03 v3.1 result-neutral repair pin

## Atomic restart states, integrated continuous records, and certified concurrency

Date: 2026-08-22

Status: **FROZEN BEFORE CONSTRUCTION — AWARDS NO RESULT**

This is the sole corrected pin authorized by the terminal Paper 03 v3
pin-audit adjudication. It repairs one exact mathematical overreach in the v3
pin: a nonatomic point record was incorrectly required to be a normal state
on an $L^\infty$ hybrid algebra.

The corrected architecture has two related semantic layers:

1. `Ev` carries point-valued stochastic samples and posterior versions; and
2. `Heis` carries boundary-tagged hybrid observable algebras, paired with
   normal ensemble states through an explicit integrated map `Ens`.

They agree by an integrated expectation theorem, not by identifying every
sample point with a normal state. Finite and atomic records retain exact
point-state semantics.

No probability, instrument, state update, source, comparator spacetime,
physical parameter, or ontology claim may change. No automatic v3.2 or v4
repair chain is authorized.

## 0. Frozen authority and chronology

The construction must bind these exact authorities:

| Artifact | SHA-256 | Role |
|---|---|---|
| `v17/note-paper03v3-pin-audit-adjudication.md` | `a4cba0b98ceafa65888bca1a57b8b8205b26c186f264a807a37daf4c5eecb087` | sole v3.1 repair authority |
| `v17/note-paper03v3-hybrid-instrument-semantics-pin.md` | `ada49694c66911455c2980c896ea10f8741d668ebb8af909e2f061c9d6e6d9af` | superseded v3 pin; unchanged evidence |
| `v17/note-paper03v3-pin-audit-category.md` | `5430ece42ca9e09c82442a9ea2abfa77127b579e06cc9be6b023cc7de780f3fc` | point-state and pullback counterexamples |
| `v17/note-paper03v3-pin-audit-quantum.md` | `ca5d9d8e2e1c97c86862f813709cd1c42b3e0b4e74a67a4c8f764f6285190f28` | finite and integrated positive theorem |
| `v17/note-paper03v2-hostile-review-adjudication.md` | `74303ddd93b4aac35d3368760da4a0ad3d442570cb16320467076aa5f93ea358` | immutable v2 physical boundary |
| `v17/paper-03v2-causal-frontier-relativistic-adequacy.md` | `93eaa95fba10831618512ab95447d3527ff5d8877ab5119237f73bb8c30e0181` | immutable v2 construction |
| `v17/paper-00-reality-first-programme.md` | `a9f8456763447eaa25a6a840e8f221f51d961ecd9c3ced649ae35a8616457cbe` | reality-first and downstream walls |

This pin must pass two independent audits on the same bytes before any
construction. A failed semantic audit returns to adjudication. It cannot be
patched during construction.

## 1. Scientific question

> Can the unchanged relativistic state/record kernel and localized quantum
> instruments be represented by a genuine hybrid Heisenberg category that
> composes finite retained outcomes exactly, treats continuous outcomes by
> normal integrated ensemble semantics and almost-everywhere posterior
> fields, and identifies schedules only through certified full-operation
> exchanges?

This is an operational representation question. It is not a new stochastic
law, an actualization rule, an internal clock, an emergent spacetime, or a
gravity model.

## 2. Preserved physics and forbidden changes

### 2.1 Preserved exactly

The successor inherits without retuning:

1. the finite causal-frontier/path category;
2. every v2 standard-Borel state/record space and Markov kernel;
3. every branch CP map, complete instrument, POVM, state update, source, and
   localized scattering map;
4. every finite and continuous probability law;
5. the fixed-packet no-signalling, steering, Bell, and positive-history
   package at its adjudicated quantifiers;
6. the presentation and complete-context operational quotient scope;
7. full packet-isomorphism and proper-embedding refusals;
8. physical KMS/material/apparatus rest-frame scope;
9. type-III, split, gauge, particle, continuum, and UV firewalls; and
10. the distinction among record, conditioning, division, actuality,
    ontology, spacetime, and gravity.

### 2.2 Forbidden moves

The construction may not:

- change a v2 probability or physical mechanism;
- discretize a continuous outcome merely to make the theorem pass;
- treat a technical reference measure as a physical state or prior;
- choose pointwise posterior representatives after seeing a desired result;
- call a null singleton a positive-support event;
- identify a point sample with an $L^\infty$ normal state;
- hide future-relevant memory outside the boundary interface;
- force uncertified operations to commute;
- interpret laboratory slots as microscopic events or time; or
- add an ontology, actualization, metric, geometry, or gravitational law.

## 3. The three record statuses

Every boundary record coordinate must be assigned exactly one status.

### 3.1 Finite retained record

For finite $R_D$ and a unital quantum observable algebra $\mathcal A_D$,

$$
\mathcal O_D
=\ell^\infty(R_D)\otimes\mathcal A_D
\cong\bigoplus_{r\in R_D}\mathcal A_D.
$$

Every point $r$ is a central atom. A normal quantum state $\rho$ and exact
record $r$ define the normal hybrid state

$$
\omega_{\rho,r}((A_s)_s)=\rho(A_r).
$$

This is the mandatory exact restart and adaptive-control regime.

### 3.2 Countable atomic retained record

A countable record space may use $\ell^\infty(R_D)$ only when the admitted
normal laws lie in $\ell^1(R_D)$ and every exact retained point used as a
restart state is a declared atom. Point states are then normal. The finite
regime remains the mandatory positive construction; countable scope is
conditional.

### 3.3 Standard-Borel integrated record

For a standard-Borel $R_D$, an integrated boundary packet supplies:

1. a represented sigma-finite von Neumann algebra $\mathcal A_D$ with
   separable predual;
2. one sigma-finite measure class $[\nu_D]$ on $R_D$;
3. a standard-Borel sample space $X_D$ of quantum predictive values and exact
   retained record samples;
4. a class $\mathfrak E_D$ of admitted ensemble laws on $X_D$ whose record
   marginals are absolutely continuous with respect to $\nu_D$;
5. a strongly measurable predual-valued posterior field for every admitted
   ensemble; and
6. every future-relevant quantum and classical memory coordinate.

Define

$$
\mathcal O_D
=\mathcal A_D\,\overline\otimes
L^\infty(R_D,\nu_D).
$$

The measure class is technical typing data. It is not a physical ensemble,
cosmological state, selector, or actual history.

### 3.4 Generic nonatomic point restart is refused

For a point $r$ with $\nu_D(\{r\})=0$, evaluation at $r$ is not a
well-defined functional on $L^\infty(R_D,\nu_D)$ and is not normal. The
construction must therefore print:

```text
P03V31-NONATOMIC-POINT-RESTART-UNCONSTRUCTED
```

unless a separately declared atom or independently proved pointwise
regularity theorem changes the boundary type. No such theorem is part of
v3.1.

An exact sampled value may remain in `Ev`. It is not thereby a normal hybrid
state or a complete division.

## 4. Boundary-tagged hybrid objects

The object assigned to a boundary is the tagged tuple

$$
\mathbf O_D=
(B_{\Xi,D},\mathcal O_D,\mathsf{Reg}_D,[\nu_D]),
$$

where the measure class is omitted in the finite regime and
$\mathsf{Reg}_D$ records the finite, atomic, or integrated status.

The tag is structural typing, not an observable. Two frontiers with equal or
isomorphic underlying algebras remain different category objects when their
boundary schemas differ. Thus an explicit skip may have an identity
underlying CP map while remaining a nonidentity physical arrow between
different tagged objects.

Different quantum systems, retained record schemas, or future memory ports
give different objects. Mechanisms with different target schemas are not
parallel arrows unless a common typed sum or measurable-field interface is
constructed explicitly.

## 5. Normal ensemble states and the `Ens` bridge

### 5.1 Integrated state

Let $\Lambda_D\in\mathfrak E_D$. Writing a sample as $(\rho,r)$, define

$$
\operatorname{Ens}_D(\Lambda_D)(F)
=\int_{X_D}\rho(F(r))\,\Lambda_D(d\rho,dr),
\qquad F\in\mathcal O_D.
$$

The construction must prove that the predual-valued integrand is strongly
measurable, integrable, representative independent, positive, normalized,
and normal. `Ens` is a map from admitted **laws** to normal states. It is not
a map from every point of $X_D$.

In the finite/atomic regime, $\operatorname{Ens}_D(\delta_{(\rho,r)})$ is the
point state of Section 3. In the nonatomic regime, the Dirac law generally
lies outside $\mathfrak E_D$.

### 5.2 No hidden ensemble collapse

Two `Ev` sample descriptions may induce the same normal ensemble state while
differing only on a null set. The integrated operational theorem identifies
their predictions at this scope. It does not assert microscopic identity or
choose an actual sample.

If a licensed future can operationally distinguish the alleged null-set
difference under an admitted preparation, then the common domination or
boundary schema is incomplete and the packet must be refined or refused.

## 6. Common domination and nonsingular maps

### 6.1 Common boundary measure class

Every integrated boundary must prove that one $[\nu_D]$ dominates the record
marginal of every admitted ensemble reaching that exact boundary schema.
Instrument-specific classes do not define one object.

A finite or countable registered family may construct a common class from a
strictly positive weighted sum of route measures. An arbitrary uncountable
family receives no such inference. The family

$$
\{\delta_x:x\in[0,1]\}
$$

is the mandatory refusal: no sigma-finite measure dominates all members.

If common domination fails, the packet must restrict the family, refine the
boundary schema, or use an atomic/coarse-grained interface. It may not choose
a measure after inspecting the desired prediction.

### 6.2 Nonsingular deterministic maps

A deterministic record map

$$
f:(R_D,[\nu_D])\to(R_E,[\nu_E])
$$

induces the Heisenberg pullback

$$
f^*([g])=[g\circ f]
$$

only if

$$
f_*\nu_D\ll\nu_E.
$$

This condition is independent of representative and invariant under
equivalent measures. It composes.

Record append may require a graph-supported target class. Coarse-graining,
discard, presentation transport, and packet isomorphism must each print the
correct source/target class and prove nonsingularity or equivalence. Mere
measurability is insufficient.

### 6.3 Technical faithful states

For one fixed normal instrument, a faithful normal source state may induce a
dominating outcome measure. This is a construction tool. It does not by
itself prove common domination across several incoming instruments and may
not enter a physical probability except through an equivalent null ideal.

## 7. The hybrid operation category

Let $\mathbf{Hyb}_\Xi$ have the tagged objects $\mathbf O_D$.

An arrow is a tagged triple

$$
(D,E,\Phi),
\qquad
\Phi:\mathcal O_E\to\mathcal O_D,
$$

where $\Phi$ is unital CP in a $C^*$ packet and normal unital CP in a
represented $W^*$ packet. It must preserve the declared quantum state class,
record measure classes, and future-memory interface.

Composition is

$$
(D,E,\Phi)\circ(E,F,\Psi)
=(D,F,\Phi\circ\Psi).
$$

Identity is $(D,D,\operatorname{id}_{\mathcal O_D})$. The construction must
prove closure under composition and refuse every mismatched tag or schema.

Underlying map equality does not erase the tagged endpoints.

## 8. Complete instruments

### 8.1 Finite instrument

For branch maps $\mathcal J_s:\mathcal A_E\to\mathcal A_D$,

$$
\widehat{\mathcal J}:
\bigoplus_s\mathcal A_E\to\mathcal A_D,
\qquad
\widehat{\mathcal J}((A_s)_s)
=\sum_s\mathcal J_s(A_s).
$$

The construction must prove CP and unitality. With an existing record $r$ it
must use the fiberwise map

$$
(A_{r,s})_{r,s}
\longmapsto
\left(\sum_s\mathcal J_{r,s}(A_{r,s})\right)_r.
$$

Branch insertion is generally nonunital and is not an arrow in the normalized
category. The nonselective map is the diagonal restriction.

### 8.2 Standard-Borel instrument

Every admitted continuous instrument must supply:

1. a countably additive CP instrument;
2. a normal unital complete extension on the exact hybrid source/target
   algebras;
3. NEP or an explicitly stronger equivalent construction;
4. the property that the extension lands in the declared target algebra, not
   merely an ambient $\mathcal B(\mathcal H)$;
5. a strongly measurable posterior/state-record kernel for `Ev`;
6. closure of the admitted ensemble and quantum state classes; and
7. compatibility with the common boundary measure class.

Approximate NEP earns only an approximate comparator theorem. A continuous
outcome may instead remain a terminal reader or be finitely coarse-grained
before retained control.

## 9. Primitive constructor schema

Every primitive must provide its exact physical arrow, `Ev` kernel, `Heis`
map, and regime/measure-class proof.

| Primitive | Mandatory hybrid semantics |
|---|---|
| identity | tagged identity |
| explicit skip | consumes the enabled slot; exact distinct endpoint tag; underlying map may be identity |
| preparation | normalized CP map with source/preparation exposed |
| deterministic quantum channel | unital or normal unital CP map |
| finite instrument | one complete direct-sum arrow |
| continuous instrument | one admitted normal complete extension plus `Ev` posterior kernel |
| record write/append | nonsingular deterministic pullback and exact target class |
| record read/guard | block or decomposable map over an already present record |
| trusted randomization | complete classical--quantum channel; retained randomizer exposed |
| coarse-graining | nonsingular pullback along the declared record map |
| discard | inclusion of observables independent of the discarded coordinate |
| localized coupling | exact system--probe complete instrument with frozen scattering convention |
| ancillary probe | complete source/tensor interface; correlations typed as joint source |

A false guard uses explicit skip. A future record cannot control a past slot.
Discarded or coarse-grained information cannot be read later unless a
separate retained memory port is explicitly present.

## 10. `Ev`, `Heis`, and integrated compatibility

### 10.1 Point-sample semantics

The inherited functor

$$
\operatorname{Ev}_\Xi:
\mathcal P_\Xi\to\mathbf{Kern}
$$

acts on the exact standard-Borel sample spaces $X_D$. It retains the v2
kernels byte-for-byte as mathematics. Posterior versions are part of packet
data only up to their declared almost-everywhere equivalence.

### 10.2 Heisenberg functor

For physical $p:D\to E$ define

$$
\operatorname{Heis}_\Xi(p):
\mathbf O_E\to\mathbf O_D
$$

by reverse chronological composition of the primitive hybrid maps. Prove

$$
\operatorname{Heis}_\Xi(\operatorname{id}_D)
=\operatorname{id}_{\mathbf O_D},
$$

and

$$
\operatorname{Heis}_\Xi(q\circ p)
=\operatorname{Heis}_\Xi(p)
 \circ\operatorname{Heis}_\Xi(q).
$$

The proof must include records, guards, discard, coarse-graining, and memory.

### 10.3 Integrated duality theorem

For every admitted ensemble $\Lambda_D\in\mathfrak E_D$, finite path
$p:D\to E$, and complete reader $F\in\mathcal O_E$, prove

$$
\operatorname{Ens}_E
\bigl(\Lambda_D\operatorname{Ev}_\Xi(p)\bigr)(F)
=
\operatorname{Ens}_D(\Lambda_D)
\bigl(\operatorname{Heis}_\Xi(p)(F)\bigr).
$$

The left side must be expanded as the quantum-state evaluation integral, not
as an undefined integral of an operator-valued symbol. One-step agreement
and the tower property must prove every finite path.

In the finite/atomic regime this includes exact point laws. In the nonatomic
regime it is an ensemble theorem and does not quantify over inadmissible Dirac
initial laws.

### 10.4 Conditioning

A central event $\Delta$ with positive ensemble probability defines a
normalized conditional ensemble and normal hybrid state. A finite/atomic
positive point may define an exact branch state. A null singleton does not.

Conditioning changes prediction given a record. It does not select actuality.

## 11. Continuous adaptive control

A continuous guard is an almost-everywhere defined measurable field of
admitted operations $r\mapsto\Phi_r$ whose decomposable action gives one
normal UCP hybrid arrow. The construction must prove measurability,
essential boundedness/normality, exact endpoints, and closure of the ensemble
class.

Two versions equal $\nu_D$-almost everywhere are the same integrated
operation. A pointwise difference on a null set is not promoted to a physical
distinction unless an admitted preparation or reader gives that set positive
support; in that case the packet's domination/schema is incomplete.

This supports continuous feedback inside a whole program. It does not make
the intermediate exact point a generic division or restart state.

## 12. Certified concurrency

An exchange certificate for co-enabled incomparable primitives must contain:

1. equality of the complete `Heis` composites;
2. equality of the complete `Ev` kernel composites;
3. the exact output-record permutation;
4. source, apparatus, and memory lineage;
5. identical tagged source and target boundary schemas; and
6. measure-class compatibility in integrated regimes.

Let $\sim_{\rm ex}$ be the least category congruence generated by these
squares. Only paths connected by certified adjacent exchanges have equal
complete semantics.

A protocol is fully exchange certified only when every reachable co-enabled
incomparable pair has a certificate in every reachable record context. Only
then do all finite linear extensions agree. Uncertified serializations remain
distinct procedures or must be refined into an ordered/joint mechanism.

No microscopic clock, foliation, or causal order is derived from this
laboratory scheduling theorem.

## 13. Presentation, quotient, and covariance

The presentation groupoid must transport:

- the tagged boundary;
- quantum algebra and classical center;
- regime and measure class;
- sample space and ensemble class;
- every primitive `Ev` kernel and `Heis` map;
- posterior/control fields modulo the transported null ideal; and
- exact record and occurrence multiplicity.

A continuous record map must be a measure-class isomorphism for packet
isomorphism. Coordinate bijection alone is insufficient.

Complete operational equivalence remains quantified over every compatible
prefix, ancillary probe, adaptive continuation, record operation, and reader.
The construction must prove congruence using both semantic layers. The
reachable quotient remains packet scoped.

A proper `Loc` embedding still gives no canonical forward state extension or
complete experiment transport.

## 14. Relativistic and ontological scope

The localized system--probe, causal-factorization, no-signalling, steering,
Bell, type-III, split, gauge, particle, continuum, UV, and frame results retain
their v2 hypotheses and conclusions exactly.

In particular:

- nonselective remote operations may no-signal while selected records steer;
- causally disjoint instruments exchange only under the named complete
  factorization certificate;
- a physical KMS/material/apparatus state may select a rest frame;
- covariance is not fixed-state invariance;
- a retained record is not automatically stable, permanent, or complete;
- an ensemble boundary is not automatically a division;
- an exact sample is not an actuality mechanism; and
- no boundary, record, algebra, slot, or measure class is a microscopic event,
  clock, spacetime cell, volume, entropy, or gravitational source.

## 15. Target theorem package

The construction must prove or refuse each target independently.

| ID | Target | Required scope |
|---|---|---|
| V31-T1 | v2 causal-frontier/path category unchanged | every admitted packet |
| V31-T2 | one tagged hybrid object per exact boundary schema | every admitted packet |
| V31-T3 | finite direct-sum complete instrument is normalized CP | every finite packet |
| V31-T4 | finite/atomic point state and exact guard compose | every admitted finite packet; conditional atomic scope |
| V31-T5 | integrated boundary supplies common domination and normal `Ens` | conditional on proved hypotheses |
| V31-T6 | generic nonatomic point normal state is refused | every integrated packet |
| V31-T7 | deterministic classical maps are nonsingular | every integrated primitive |
| V31-T8 | continuous complete arrow has NEP/landing/posterior data | each admitted instrument |
| V31-T9 | tagged `Hyb_Xi` is a category | every admitted packet |
| V31-T10 | contravariant `Heis_Xi` is a functor | every admitted packet |
| V31-T11 | integrated `Ens`/`Ev`/`Heis` identity holds | every admitted ensemble/path/reader |
| V31-T12 | continuous adaptive controls compose a.e. | conditional on measurable decomposable field |
| V31-T13 | discard/coarse-graining remove unavailable controls | every admitted packet |
| V31-T14 | positive event conditioning is exact; null point refused | every admitted packet |
| V31-T15 | presentation and packet transport move all semantic data | every admitted packet/isomorphism |
| V31-T16 | complete operational equivalence is a congruence | constructor-complete contexts |
| V31-T17 | one certified exchange commutes in both layers | conditional on full certificate |
| V31-T18 | all linear extensions agree only when fully certified | conditional on reachable-context certification |
| V31-T19 | localized no-signalling/steering/Bell package unchanged | inherited quantifiers |
| V31-T20 | positive histories/prefix coherence unchanged | inherited quantifiers |
| V31-T21 | frame/type-III/split/gauge/continuum walls unchanged | refusal or named hypotheses |
| V31-T22 | record, conditioning, division, actuality remain distinct | refusal/nonimplication |
| V31-T23 | Barandes compatibility remains incomplete | no selected configuration/law/trajectory |
| V31-T24 | ontology, time, spacetime, gravity remain unconstructed | unconstructed |

## 16. Two-way controls

| ID | Positive control | Hostile control |
|---|---|---|
| C1 | finite direct-sum record | branch list alone |
| C2 | binary result drives later $I/X$ | nonselective sum drives no guard |
| C3 | complete arrow unital | individual branch may be nonunital |
| C4 | atomic point state normal | nonatomic point evaluation refused |
| C5 | normal joint continuous law | Dirac law outside dominated ensemble class |
| C6 | a.e. posterior field | canonical null-singleton posterior refused |
| C7 | one common boundary class proved | per-instrument unrelated classes refused |
| C8 | nonsingular record pullback | merely measurable constant-map pullback refused |
| C9 | graph-compatible append class | blind product-class append refused |
| C10 | equivalent measures same object | singular replacement changes object |
| C11 | technical faithful state supplies domination tool | no physical prior/selector |
| C12 | boundary-tagged skip | empty path remains different arrow |
| C13 | existing record controls future | future record cannot control past |
| C14 | continuous decomposable guard | nonmeasurable/unbounded field refused |
| C15 | full memory exposed | hidden cache cannot affect future |
| C16 | discard removes coordinate | later read is untyped |
| C17 | coarse event positive conditioning | null point normalization refused |
| C18 | complete `Ens` evaluation identity | scalar outcome law alone insufficient |
| C19 | full-map/kernel exchange | equality on one state/reader refused |
| C20 | all reachable swaps certified | one initial square not global theorem |
| C21 | output permutation explicit | reordered records not silently identified |
| C22 | product source exposed | correlated source is joint mechanism |
| C23 | presentation moves null ideal and multiplicity | coordinate-only relabel refused |
| C24 | packet isomorphism moves both semantics | proper embedding state push refused |
| C25 | nonselective no-signalling | selected conditional may steer |
| C26 | KMS/material frame physical | covariance is not invariant state |
| C27 | type-III normal functional scope | generic density matrix/trace refused |
| C28 | split with collar/hypothesis | touching/gauge factorization refused |
| C29 | record retained under typed interface | erasure/discard removes it |
| C30 | future-sufficient boundary division | lower set or sampled point alone is not |
| C31 | prediction over possible records | no actuality selection |
| C32 | laboratory slot protocol | no microscopic event web or global time |
| C33 | comparator spacetime declared | no emergent metric claim |
| C34 | v2 probabilities byte-for-byte mathematical inputs | no retuning |

## 17. Mandatory hostile attacks

Construction and review must attack at least:

1. the uniform $[0,1]$ point-evaluation counterexample;
2. a null singleton represented by two different function representatives;
3. a Dirac initial law in a nonatomic dominated ensemble class;
4. a positive-measure coarse event and its normalized conditional law;
5. an atomic point and its exact normal state;
6. an uncountable mutually singular incoming family;
7. a merely measurable constant pullback between Lebesgue classes;
8. deterministic append into a null graph under product measure;
9. equivalent versus singular reference classes;
10. a technical faithful state used illicitly as a physical prior;
11. a non-NEP instrument;
12. approximate NEP promoted to exact;
13. an NEP extension that lands only in an ambient algebra;
14. an outcome kernel without a strongly measurable posterior field;
15. same ensemble state but future-relevant hidden memory;
16. scalar record-law agreement with different quantum posterior fields;
17. finite measurement followed by `I/X` guard;
18. branch-as-complete-arrow substitution;
19. nonselective-sum-as-record-bearing substitution;
20. measurement after an existing record with the old fiber dropped;
21. skip collapsed to empty path because underlying maps match;
22. mismatched target schemas called parallel arrows;
23. future-result guard;
24. read after discard;
25. fine-value read after coarse-graining;
26. nonmeasurable continuous guard;
27. null-set version changed and then made operational by an inadmissible
    singular preparation;
28. `Ens` integral lacking quantum-state evaluation;
29. duality proved on one reader only;
30. presentation collapsing two record occurrences;
31. packet transport omitting measure class;
32. proper-embedding state pushforward;
33. one-state scalar exchange certificate;
34. output permutation omitted;
35. source correlation hidden;
36. initial-context certificates used for later adaptive contexts;
37. uncertified incomparable pair exchanged;
38. arbitrary antichain promoted to schedule independence;
39. selected steering called signalling;
40. commutation alone called no-signalling;
41. QFT Bell observable called exact probe realization;
42. every local state called a density matrix;
43. arbitrary gauge/touching split;
44. KMS frame called preferred fundamental foliation;
45. retained record called permanent;
46. continuous point sample called division;
47. conditioning called actualization;
48. slot/record counted as duration or volume;
49. hybrid algebra called the ontology;
50. import of v16 order, metric, FLRW, selector, fusion, or gravity;
51. any v2 probability or physical parameter change; and
52. opening Paper 04 before terminal adjudication.

Independent pin reviewers must add at least ten fresh attacks per lens.

## 18. Product and outcome ladder

The product remains coordinatewise:

```text
input
slot-skeleton
frontier
boundary
sample-semantics
ensemble-semantics
hybrid-object
heisenberg-functor
integrated-compatibility
point-restart
presentation
quotient
covariance
state-class
instrument
causal-factorization
certified-schedule
no-signalling
steering
bell
positive-model
context
fibers
type-III
split
gauge
particles
continuum
UV
preferred-frame
record
division
actuality
barandes
ontology
downstream
```

Outcome rungs:

```text
P03V31-HYBRID-BOUNDARY-TYPE-FAILURE
P03V31-COMMON-DOMINATION-FAILURE
P03V31-HYBRID-OPERATION-CATEGORY-UNCONSTRUCTED
P03V31-HEISENBERG-FUNCTOR-UNCONSTRUCTED
P03V31-INTEGRATED-DUAL-SEMANTICS-INCOMPATIBLE
P03V31-CERTIFIED-CONCURRENCY-UNCONSTRUCTED
P03V31-CAUSAL-FRONTIER-INTEGRATED-DUAL-SEMANTICS-CONSTRUCTED
P03V31-RELATIVISTIC-QUANTUM-OPERATIONAL-ADEQUACY-WITH-GLOBAL-ONTOLOGY-DEBT
P03V31-RELATIVISTIC-LOCAL-MICROONTOLOGY-CONSTRUCTED-WITH-COSTS
P03V31-EMPIRICALLY-DISCRIMINATED-RELATIVISTIC-ONTOLOGY
```

The strongest pin-permitted result is rung 8. It is conditional operational
adequacy on declared AQFT comparator packets. It does not mean that the
hybrid algebra is the ontology, one outcome is actualized, spacetime is
derived, or gravity is constructed.

The point-restart product may simultaneously read:

```text
FINITE/ATOMIC-CONSTRUCTED
NONATOMIC-GENERIC-UNCONSTRUCTED
```

without lowering a correctly integrated continuous theorem.

## 19. Construction and review chronology

1. Commit this immutable pin.
2. Run the same two independent category/mathematics and
   quantum/instrument audits on these exact bytes.
3. If and only if both accept, freeze one self-contained mathematics-only
   construction.
4. Freeze a construction audit.
5. Freeze a three-lens hostile-review protocol.
6. Run mutually blind category/operator-algebra, quantum/probability, and
   ontology/relativity reviews.
7. Commit each report separately and adjudicate independently.

There is no implementation stage in this unit. Paper 04, internal clocks,
background-free spacetime, matter--geometry dynamics, and gravity remain
closed until terminal Paper 03 v3.1 adjudication.

## 20. Primary-source anchors

- Okamura--Ozawa, *Measurement theory in local quantum physics*:
  https://arxiv.org/abs/1501.00239
- Fewster--Verch, *Quantum fields and local measurements*:
  https://arxiv.org/abs/1810.06512
- Brunetti--Fredenhagen--Verch, *The generally covariant locality principle*:
  https://arxiv.org/abs/math-ph/0112041
- Coecke--Heunen--Kissinger, *Categories of Quantum and Classical Channels*:
  https://arxiv.org/abs/1305.3821

These sources support operational instrument and representation mathematics.
They do not select a microscopic configuration ontology, actual trajectory,
background-independent law, internal time, or gravitational dynamics.
