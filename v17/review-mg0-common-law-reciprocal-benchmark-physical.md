# ISP v17 MG0 common-law reciprocal benchmark review

## Seat P — physical experiment, massive matter, and gravity controls

Date: 2026-08-24

Status: COMPLETE INDEPENDENT SEAT-P REPORT

Scientific result awarded: none

Candidate construction, candidate completion, numerical evaluation, entrant
admission, comparison, ontology selection, and gravity verdict: not performed
and not authorized.

## 0. Review boundary and method

I reviewed only the frozen protocol and its Section 2 bundle. I worked
read-only in the repository, did not inspect another current review seat's
prompt, draft, finding, or report, and did not construct a mechanical baseline
or any entrant. No external literature was needed: every conclusion below is
supported by the frozen clauses, so literature has not been used to repair or
supplement them.

Artifact abbreviations used below are:

- `Prot`: `v17/note-mg0-common-law-reciprocal-benchmark-review-protocol.md`;
- `C`: `v17/research-incubator/active/mg0/v17_mg0_common_matter_geometry_contract.md`;
- `B`: `v17/research-incubator/active/mg0/v17_mg0_selector_controls_and_benchmark.md`;
- `Pin`: `v17/note-mg0-common-law-reciprocal-benchmark-pin.md`; and
- `R1`: `v17/note-r1-native-source-gap-independent-review-adjudication.md`.

Line references are one-based LF line numbers in the authenticated files.

## 1. Chain of custody and authentication

Repository `HEAD` was exactly
`c54b54ffacb777e1cc98d3e12be320a8eb7c2183`, the required protocol commit,
with parent `7c96afca5c616624135526f2ae1fea858546aafd`. Direct hashing of the working
bytes and `git show` of the committed protocol produced the same ordinary
SHA-256.

| object | provenance checked | LF lines | bytes | ordinary SHA-256 | result |
|---|---|---:|---:|---|---|
| `Prot` | `c54b54ffacb777e1cc98d3e12be320a8eb7c2183` | 439 | 16950 | `11ee64b5da11dbad3833cb43097b8130a91f95c27d494ce6a58b197ce3e00217` | exact |
| `C` | current bytes and `0ed5f3c3388c46ca14642c3075a4b3f41a38eddd` | 699 | 24600 | `3552e4d00806c1a17294b9e57a4a1d693e72bfb0fdff0356411f6af2fa12d475` | exact |
| `B` | current bytes and `0ed5f3c3388c46ca14642c3075a4b3f41a38eddd` | 518 | 19557 | `7d527e68df9ab788433d3d953b607181d1624e46aea40b50b630e675c70d8482` | exact |
| `Pin` | current bytes and `f844666a26104d6503f41aeb4375a7e20146de8d` | 427 | 16227 | `2355b2f6809b1ddaed8ec4a2dc8792980bcb051f075383d11ba240b21a27ade8` | exact |
| `R1` | current bytes and `1eebddef46a677c67c465deb352d022c01e15fca` | 646 | 26718 | `53f38d9bdda430f41c857cb73d44f48c13be2f79f5432277fd217718a0f03668` | exact |

I independently applied the printed normalization rules. The protocol
normalized self-SHA-256 reproduced as
`7f8aad7ac09ecdbb90f7c17897a2d66d78c938f3c78ea34ea89c215496ed0e0b`.
The pin and R1 normalized self-signatures also reproduced as
`e36bf6032d79cd03a1cf4be481f84f9eae4c60cac1da493529eefad21590faa2`
and
`1a6b3fc32de563a1b8d82c7f1fa5d7e7fdac4f0acb67b6be2d3a3ceb7655b786`.
All five files use LF, end in one LF, and contain neither CR bytes nor trailing
horizontal whitespace. A restricted diff against the protocol commit found no
change in any review-bundle path.

**Finding P-01 — severity NONE / PROCEDURAL PASS.** The governing bytes,
historical provenance, exact sizes, and all required digests authenticate. No
procedural mismatch bars substantive review.

## 2. Physical reconstruction

### 2.1 Source preparation family

The preparation family is physically purposeful and nonredundant (`B`
69--85):

1. `E_L` and `E_R` establish localized source-response controls.
2. `E_mix` supplies the registered incoherent mixture.
3. `E_+` and `E_-` hold equal weights while reversing a controlled relative
   phase.
4. `E_phi` is a held-out relative phase and therefore tests propagation rather
   than phase-specific fitting.
5. `E_erase` and `E_retain` distinguish an erasable temporary path mark from a
   retained physical record.

The clause that separated equal packets *may* share a one-body diagonal mass
density while differing in coherence is correctly limited: it makes a
source-functional discriminator and explicitly does not promise a
gravity-sensitive difference (`B` 82--85). The source declaration separately
requires each law to distinguish coherent state from incoherent mixture,
sample from ensemble, conditioned from unconditioned expectation, and record
from unrecorded alternative (`C` 342--363). Thus diagonal mass density is not
silently equated with source coherence or with a universal stress-energy
source.

### 2.2 Probe, apparatus, output, and calibration stages

The probe family contains a gravity-insensitive null, a calibrated
fixed-background matter response, a direct gravity-sensitive reader when the
law admits one, a two-source mediator-entanglement setting, retained/erased
path information, and a held-out geometry or scale (`B` 87--96). The output is
the full joint profile over source, gravity-sensitive, probe, and apparatus
records, or an exactly typed non-Kolmogorov precursor plus all licensed record
probabilities (`B` 98--111; `Pin` 170--183). It is not one selected fringe.

The staged logic is:

- Stage A closes traps, electromagnetic and Casimir forces, vibrations,
  thermal noise, measurement backaction, and cross-talk; an unexplained
  residual earns no gravitational label (`B` 115--121).
- Stage B tests the one-way fixed-background gravity-to-matter control with an
  effectively classical source (`B` 123--132).
- Stage C removes or desensitizes the probe and varies the source family to
  locate the source functional (`B` 134--138).
- Stage D runs source and probe together under the same law and parameters;
  separately fitted conditional channels fail (`B` 140--144).

The apparatus role explicitly includes traps, supports, clocks, shields,
lasers, environmental controls, and retained apparatus records (`B` 55--67).
Changing a source configuration also charges supports, work, momentum
exchange, and records, and forbids an invisible external conservation repair
(`B` 264--271). Before any later comparison, one packet must freeze actual
preparation values, apparatus, controls, calibrations, nuisance model,
held-outs, tolerances, uncertainty propagation, and record interface for every
entrant (`Pin` 185--189).

### 2.3 PG, CP, ME, and DD

The four discriminators are conditional classifiers, not ontology theorems:

- PG tests whether the registered response follows an ensemble expectation,
  actual record, stochastic signal, or other typed source. It targets only the
  simplistic expectation-sourced averaged-field inference and expressly does
  not reject every semiclassical or classical--quantum law (`B` 169--188).
- CP matches declared diagonal source data while varying relational coherence.
  Its five printed null explanations include decoherence, inadequate
  resolution, and deliberate diagonal sourcing; a null does not prove
  classical gravity (`B` 190--204).
- ME requires initially separable matter, an admitted gravitational-only
  channel, and the complete joint record. Positive entanglement excludes only
  the local-classical-communication mediator in the frozen class and does not
  select a unique microscopic gravity ontology without its independence and
  locality assumptions (`B` 206--214).
- DD applies to a classical--quantum hybrid and jointly charges decoherence,
  gravitational diffusion/noise, applicable heating or momentum diffusion,
  their shared kernel dependence, and bounds. Independent tuning is forbidden
  when the candidate's own consistency conditions relate them (`B` 216--227).

### 2.4 Reciprocal parent, conservation, causality, and limits

The contract requires operationally nonzero matter-to-gravity and
gravity-to-matter response members only after nongravitational channels,
common causes, control leakage, and postselection are represented or bounded
(`C` 284--319). Both responses must be conditionals or marginals of one
admissible parent (`C` 321--325). The benchmark strengthens this with fixed
controls, parameter matching, erased/retained records, an entangled input,
no-signalling, actuality-conditioned records, and rejection of lookup-table
responses (`B` 231--260). One-way controls remain allowed approximations but
cannot pass as fundamental reciprocity (`C` 334--338).

The laboratory `do` order is only a supplied causal control and derives no
fundamental chronology (`C` 327--332). Gauge representatives must agree on
complete records, while transformations that change a relational record may
not be discarded (`C` 367--406). Conservation requires exact, stochastic,
boundary-charge, anomaly, prospectively bounded, or limit-derived closure;
metric-regime sources must respect the relevant Bianchi-to-source identity,
including compensating degrees of freedom for collapse, trajectory, or noisy
sources (`C` 410--440). Entangled preparations, retained/erased records,
postselection, decomposition independence, no future setting, and no hidden
foliation make the causal check genuinely multichannel (`C` 444--463).

Both the fixed-geometry quantum and classical-gravity limits must descend from
the same immutable law, with controlled errors. The latter includes reciprocal
backreaction, conservation, and universal coupling or a typed violation at
the claimed scope; shared parameters are calibrated once (`C` 467--501).

### 2.5 Parameter provenance, uncertainty, and R1 scope

Every input is classified as nomological, contingent, controlled,
independently calibrated, derived, or target-imported. Per-experiment amplitude
tables, solved metrics, result-chosen kernels, future settings, and
postselected actual branches are presumptive answer imports (`C` 553--575).
Every adjustable parameter receives an independent calibration; equations,
masses, separations, coherence settings, clocks, nuisance model, held-outs,
tolerances, uncertainty propagation, and outcome rules freeze before the joint
evaluation (`B` 442--456). Candidate replacement and held-out refitting are
barred.

R1 is used faithfully but narrowly. `C` 63--82 and `B` 30--46 bind the exact
R1 adjudication as the fixed-background quantum recovery target and import
ledger, not as an entrant, actuality law, ontology, source prescription, or
gravity result. `Pin` 119--140 preserves its material ceilings, especially
that no-refit source/apparatus transfer is not established. R1 itself states
that no separately built no-refit transfer was run (`R1` 339--351), no pinned
hardware certified the complete reader chain (`R1` 353--376), and a complete
microscopic detector, source/apparatus transfer, gravity, and reciprocal
matter--geometry parent are not earned (`R1` 598--614).

## 3. Findings with severity and exact evidence

### P-02 — Neutral roles and common interface

**Severity: NONE / FAVORABLE.** `C` 86--109 forbids fundamental metric,
continuum, dimension, foliation, classical/quantum gravity, tensor split, and
actuality assumptions. `C` 121--157 makes matter- and gravity-sensitive labels
operational rather than microscopic factors; `B` 52--67 repeats that the
low-energy scaffold is a control and `G` asserts no metric, field type, or
factorization. The complete operational profile permits ordinary-positive,
pair-history, and quantum-plus-actualization laws without equating their
microscopic prediction semantics (`C` 199--244; `Pin` 170--183).

### P-03 — Massive-apparatus closure is typed but not yet discharged

**Severity: MAJOR / BINDING PRE-ENTRANT SCOPE.** The benchmark requires a
massive source, coherence manipulation, an independent probe, traps, supports,
clocks, shields, lasers, environmental controls, and their records (`B`
55--96). Stage A and the full-apparatus firewall require calibrated nuisance
closure and work/momentum accounting (`B` 115--121, 264--271). The common
packet and entrant gate require apparatus, nuisance controls, calibration
data, uncertainty, no-refit obligations, and a complete reader/record map
(`B` 389--407; `Pin` 185--214). R1 expressly lacks no-refit source/apparatus
transfer (`Pin` 119--128; `R1` 598--614). Those facts force the separate
baseline decision in Section 6 of this report.

### P-04 — Same-parent reciprocity and intervention firewall

**Severity: NONE / FAVORABLE.** The two directions must be operationally
nonzero after leakage/confound closure and arise from one parent (`C`
284--338). Stage D requires the Stage-B/Stage-C law and parameters, while
channel splicing, postselected field retrofitting, and solved per-source
metrics are forbidden (`B` 140--144, 264--294). This rejects two
independently calibrated laws without rejecting controlled one-way limits.

### P-05 — Discriminators are valid at class-relative scope

**Severity: NONE / FAVORABLE.** PG, CP, ME, and DD each print their premises and
ceilings (`B` 169--227). None turns Page--Geilker, coherence sensitivity,
mediator entanglement, or decoherence--diffusion into a universal ontology
theorem. The complete-record, nuisance, and uncertainty duties prevent a
formal witness alone from earning a physical claim.

### P-06 — Constraint, conservation, gauge, and causal closure

**Severity: NONE / FAVORABLE.** Exact alternatives for constraint closure,
metric-limit conservation, empirical gauge quotienting, conditional gluing,
entangled no-signalling controls, and immutable two-limit descent are printed
at `C` 367--501. `B` 266--271 expressly blocks conservation repair by an
invisible agent. The duties are scoped to structures a candidate uses and do
not impose a classical constraint algebra before an arena exists.

### P-07 — No-answer, no-refit, held-out, and uncertainty firewall

**Severity: NONE / FAVORABLE.** `C` 532--575 admits only predeclared,
independently calibrated differences and charges target imports. `B` 442--456
freezes all adjustable parameters, held-out source and probe settings,
tolerances, uncertainty propagation, outcome rules, and failed-feasibility
publication. `Pin` 259--285 requires one common packet and forbids target
processes, solved responses, private calibrations, future settings, and
postselected histories.

### P-08 — R1 is faithful but cannot stand in for massive mechanics

**Severity: MAJOR / BINDING CEILING, not a semantic defect.** The exact R1
artifact, commit, digest, rung, award, and ceilings are reproduced in `C`
63--82, `B` 30--46, and `Pin` 108--140. It prevents degradation of scoped
fixed-background quantum physics and is never counted as a law or ontology.
Its explicit no-transfer, incomplete reader, no-gravity, and no-reciprocal-
parent ceilings mean it cannot certify the massive source--apparatus--probe
chain.

### P-09 — Entrant gate, nonselection, outcome scope, and authority

**Severity: NONE / FAVORABLE.** A narrow entrant is permitted only if it covers
the entire registered experiment and prints all outside-scope debt (`Pin`
193--229). Sketches, master equations without parents, solved metrics,
compilers, conditional channels, hidden tables, and principles without laws
fail. Genuine distinctness excludes gauge, basis, relabelling, parameter, and
decoder duplicates; an objective unravelling counts only with a physical
referent, occurrence law, source, and reciprocal coupling (`Pin` 234--255).
The hard conjunction requires two complete distinct laws, one packet, and one
output interface (`Pin` 259--287). Agreement is nonselection; null,
nonidentifying, constraint/limit failure, confounded and infeasible outcomes
remain available (`B` 414--418, 442--456; `Pin` 351--369). `Pin` 390--416 and
`Prot` 388--418 bar every stronger successor or scientific verdict.

No blocking procedural or semantic finding survives. P-03 and P-08 are the
single material scope chain.

## 4. Mandatory physical attacks

1. **Coherence replaced by diagonal density — defeated.** The preparation
   family and exact source declaration distinguish mixture, phase, actual
   sample, record conditioning, pair object, and stochastic signal (`B`
   69--85; `C` 342--363). The equality of selected diagonal data is only a
   controlled contrast, not a source theorem.
2. **Preparation/readout discrepancy called gravity — defeated subject to the
   binding baseline.** The null probe, Stage A, full apparatus records, and
   unexplained-residual rule block the label (`B` 87--121). Actual discharge
   of those duties is required before admission.
3. **Supports, traps, clocks, lasers, feedback, or work omitted — defeated
   subject to the binding baseline.** `C`, `R_C`, supports, work, momentum,
   backaction, cross-talk, and complete nuisance records are mandatory (`B`
   55--64, 115--121, 266--271; `Pin` 185--214). Feedback forces fall within
   complete apparatus/control/backaction and may not remain an unregistered
   channel.
4. **Electromagnetic, Casimir, thermal, seismic, vibrational, or cross-talk
   channels omitted — defeated subject to the binding baseline.** Stage A
   names all but seismic explicitly; the hostile mutant names seismic and
   apparatus-mediated channels (`B` 117--121, 351--361). The common nuisance
   packet and complete record interface make the list enforceable.
5. **Page--Geilker universalized — defeated.** PG rejects only a simplistic
   expectation-sourced averaged field when records disagree and expressly
   spares other semiclassical/hybrid laws (`B` 169--188).
6. **Mediator entanglement universalized — defeated.** ME's conclusion is
   limited to the frozen local-classical-communication class under stated
   independence/locality assumptions (`B` 206--214).
7. **Decoherence and diffusion fitted independently — defeated.** DD requires
   their joint kernel dependence and forbids independent tuning when the law's
   consistency conditions relate them (`B` 216--227).
8. **Conservation repaired by an invisible bath or carrier — defeated.** The
   parent must include compensating degrees of freedom and apparatus exchange;
   an invisible external agent is expressly barred (`C` 410--440; `B`
   266--271).
9. **Fixed-background probe phase supplied as the target answer — defeated.**
   An independently calibrated Stage-B control is allowed, but a target
   process, per-experiment amplitude, solved response, or solved metric is
   zero-credit or disallowed (`C` 553--575; `B` 290--294; `Pin` 277--285). It
   cannot substitute for the Stage-D parent prediction.
10. **Source and probe directions use different laws — defeated.** Stage D,
    the no-splicing firewall, and the immutable-law two-limit rule require the
    same parent and once-calibrated parameters (`B` 140--144, 273--281; `C`
    497--501).
11. **Held-out settings select parameters — defeated.** The phase and probe
    geometry are frozen held-outs; every adjustable parameter has an
    independent calibration, and coupling changes after revealing the phase
    are hostile mutants (`B` 71--96, 315--325, 442--456).
12. **Mass position promoted to universal stress-energy sourcing — defeated.**
    Each entrant must type the exact source object and nearby inequivalents;
    the classical-gravity limit charges universal coupling or its explicit
    violation, and a held-out probe species tests a mere Newtonian fit (`C`
    342--363, 483--501; `B` 340--349).

## 5. Answers to Q1--Q12

### Q1 — neutrality

**Yes, at the stated operational scope.** Fundamental metric, continuum,
dimension, topology, foliation, matter/gravity factorization, classical or
quantized gravity, history ontology, and actuality are not assumed. The
low-energy laboratory scaffold is a declared control, not a claimed
derivation (`C` 86--109; `B` 50--67).

### Q2 — common interface

**Yes.** All rivals owe complete registered predictions, but an
ordinary-positive law is not forced to adopt Hilbert ontology and a
pair-history law is not forced into a positive fine-history measure. A typed
non-Kolmogorov precursor is allowed only together with every licensed record
probability and comparison map (`C` 199--244; `Pin` 170--183).

### Q3 — role typing

**Yes.** Law, state, control, record, and optional actual referent are distinct;
source, gravity-sensitive role, probe, apparatus, interventions, readers, and
records are operationally typed without a microscopic tensor split (`C`
132--195; `B` 55--67; `Pin` 193--214).

### Q4 — same-parent reciprocity

**Yes.** Independently fitted directions fail, the two responses must descend
from one parent, and one-way limits remain permissible approximations. The lab
schedule supplies no fundamental chronology (`C` 284--338; `B` 231--288).

### Q5 — complete experiment

**Yes, conditional on completing the binding pre-entrant mechanical control.**
The schema fixes common preparations, probe settings, apparatus roles, full
records, calibration/nuisance contents, held-outs, comparison map, and one
packet for all entrants (`B` 55--145, 389--418; `Pin` 145--214, 259--287).
Numerical values and physical closure are deliberately future packet duties,
not present results.

### Q6 — entrant completeness

**Yes.** Scope may be narrow, including a finite-mode weak-field arena, if the
whole registered experiment is covered and every debt is printed. The gate
still excludes sketches, compilers, appended source prescriptions, separately
solved metrics, isolated channels, and incomplete master equations (`Pin`
193--229).

### Q7 — genuine distinctness

**Yes.** Gauge, basis, notation, relabelling, parameter choice, coarse-graining,
decoder, and answer-table duplicates fail. An objective unravelling counts
only when its referent, occurrence law, source functional, and reciprocal
coupling are physical frozen nomology (`Pin` 234--255).

### Q8 — fixed-background control

**Yes, within R1's exact award.** R1 prevents degradation of scoped
fixed-background quantum physics and supplies neither an entrant nor ontology.
It does not close massive mechanical preparation/readout or no-refit apparatus
transfer; that separate duty is resolved below (`Pin` 108--140; `R1`
598--614).

### Q9 — massive-apparatus completeness

**The schema is adequate, but physical completion is a mandatory pre-entrant
condition.** It types source preparation and coherence records, traps,
supports, work, clocks, lasers, shields, feedback/backaction, probe readers,
retained/erased records, electromagnetic/Casimir/thermal/seismic/vibrational
and cross-talk channels, failure outcomes, tolerances, and uncertainty (`B`
55--121, 264--271, 351--361, 442--456; `Pin` 185--214). Because R1 has no
massive-apparatus no-refit transfer, paper duties alone cannot certify the
experiment. The frozen text already types the missing control, so no semantic
revision is required.

### Q10 — discriminator validity

**Yes.** PG, CP, ME, and DD distinguish typed source functionals and reciprocal
laws with explicit class-relative premises and null alternatives; none is a
universal Page--Geilker, coherence, mediator, or diffusion ontology theorem
(`B` 169--227).

### Q11 — nonselection and outcome scope

**Yes.** Exact agreement between genuinely distinct laws is nonselection.
Null, infeasible, confounded, excluded, constraint/limit-failing, and
nonidentifying results are preserved and cannot be promoted through a failed
nuisance or uncertainty ledger (`B` 414--418, 442--456; `Pin` 351--369).

### Q12 — authority

**Yes.** The pin and protocol bar candidate work, entrant admission,
comparison, ontology selection, spacetime claims, gravity verdicts, and
automatic successors in this cycle (`Pin` 332--347, 377--416; `Prot`
388--418).

## 6. Separate massive-mechanical baseline disposition

### P-B0-1  A SEPARATE CANDIDATE-NEUTRAL MECHANICAL BASELINE IS MANDATORY BEFORE ENTRANT ADMISSION, BUT THE FROZEN BENCHMARK ALREADY TYPES IT

This follows from one exact clause chain:

1. R1 is a conditional Maxwell--macroscopic-QED--quantum-instrument recovery
   target whose binding ceiling says no-refit source/apparatus transfer is not
   established (`Pin` 119--140; `R1` 339--376, 598--614).
2. The MG0 experiment adds a massive localized/coherent source, an independent
   probe, traps, supports, clocks, lasers, shields, source-moving work and
   momentum exchange, and complete apparatus records (`B` 55--96, 266--271).
   R1 therefore cannot establish that a preparation, transport, support,
   coherence-witness, feedback, or reader discrepancy is not masquerading as
   a gravity-sensitive response.
3. Stage A already requires candidate-independent calibration of traps,
   electromagnetic and Casimir forces, vibration, thermal noise, backaction,
   and cross-talk, with no gravity credit for an unexplained residual (`B`
   115--121). The public-packet clauses require the actual apparatus,
   calibrations, nuisance model, held-outs, tolerances, uncertainty propagation,
   and record interface for every entrant (`Pin` 185--189; `B` 404--407).
   Entrant completeness additionally requires the full apparatus/reader/record
   map and no-refit obligations (`Pin` 193--214).
4. If those mechanical source-to-record duties remained merely aspirational at
   entrant admission, two entrants could use the same nominal packet while
   absorbing different preparation, support, feedback, coherence, or readout
   physics. A resulting difference would not identify the source functional or
   reciprocal gravity law. The common-experiment and uncertainty gates would
   be vacuous at the physical layer.
5. Completion of the already required candidate-neutral mechanical control is
   therefore a condition of authenticating the shared packet before entrant
   admission (`Pin` 332--344). It is a binding control, not an MG0 entrant, not
   a replacement for R1's quantum scope, not a gravitational solution or
   target response table, and not permission to repair candidate conservation
   with an external bath.

The phrase “sole bound fixed-background baseline” in `Pin` 108--140 identifies
R1 as the sole already bound reviewed quantum artifact. It does not erase the
separately typed but not-yet-completed Stage-A mechanical control: the protocol
itself asks this exact question, and `Prot` 370--381 permits root to bind a
pre-entrant mechanical baseline when the existing benchmark already types the
duty. No frozen semantic clause needs amendment.

I have not designed, specified dynamics for, or constructed that baseline.

## 7. Recommendation and maximum surviving claim

**Recommendation: MG0P-D3 — ACCEPT-WITH-SCOPE.**

Reason: the frozen contract, experiment, source classifiers, reciprocal-parent
firewall, conservation/causality duties, two limits, no-answer ledger,
uncertainty protocol, genuine-distinctness rule, and hard two-law gate are
coherent and result-neutral. The material condition is binding and
pre-entrant, but it is already required by the frozen Stage-A, full-apparatus,
shared-packet, uncertainty, and no-refit clauses. It therefore does not require
a semantic revision.

The maximum surviving claim is:

> The authenticated frozen MG0 objects define a coherent, ontology-neutral and
> result-neutral comparison protocol at the declared bounded scope, with a
> nonvacuous gate requiring at least two complete and physically distinct
> reciprocal parent laws on one complete public experiment; before any entrant
> can be admitted, the already typed candidate-neutral mechanical
> source-to-record control must be completed, while R1RR-L4 remains only the
> scoped fixed-background quantum recovery target and import ledger.

This claim supplies no mechanical baseline, entrant, prediction, feasible
apparatus, empirical wedge, ontology, classical/quantum gravity verdict,
spacetime, or unification.

## 8. Report signature

Report LF line count: `000520`

Report byte count: `028265`

Report ordinary SHA-256: reported externally after the final byte is fixed;
embedding an ordinary self-hash would be recursive.

Report normalized self-SHA-256:
`ee6fa1ce6463440c01d11e1c59bc1e406b7a96f1e807e45b6409529146035135`

Normalization rule: replace the six decimal digits on both report count lines
and the 64 hexadecimal characters on the normalized-self line with ASCII
zeroes, preserve every other byte, and compute SHA-256. This report uses LF,
ends in one LF, and contains no trailing horizontal whitespace.
