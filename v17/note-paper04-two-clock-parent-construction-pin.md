# Paper 04 model-specific pre-fit construction pin

## A finite cyclic two-clock parent with reciprocal clock--matter coupling

Date: 2026-08-23

Status: **FROZEN RESULT-NEUTRAL MODEL SELECTION — NO DERIVATION OR EVALUATION**

Scientific result awarded: **none**

Parent constructed as a theorem: **no**

Clock reductions proved: **no**

External parameter shown redundant: **no**

Fundamental time, chronology, spacetime, and gravity: **unconstructed**

This is the sole model-specific pre-fit pin authorized by Paper-04 root
adjudication #76. It fixes the mathematical parent, physical subsystems,
sources, instruments, calibration, held-out tests, resources, comparison
metrics, and failure branches before any result is derived. It is not a
repair successor and does not select this model as nature's ontology.

## 0. Immutable authority and chronology

Every audit and construction must authenticate these exact inputs.

| Artifact | SHA-256 | Role |
|---|---|---|
| `v17/note-paper04-pin-audit-adjudication.md` | `d683901fb1fba9da1b21839839cd955270f1c7c7e2405e7948b7b9b3d0b106b5` | terminal generic-boundary ruling and B1--B18 |
| `v17/note-paper04-relational-clock-external-time-pin.md` | `da48bc95bf02c93393697ad6b447605ab89879ff45a1be6896abf6ce6a276b0c` | generic physical question, controls, attacks, product, and walls |
| `v17/note-paper04-pin-audit-mathematics.md` | `2151bee5c6ac9b93f315047f2164c995a7fdbc6726a431ebd992814071d0d204` | constrained-dynamics and probability scope |
| `v17/note-paper04-pin-audit-quantum-clocks.md` | `3440dd49b51ae8245070c23963c7a51fc43fe5c02a5c54d4308b4cefd71ec8f2` | quantum-clock, instrument, and resource scope |
| `v17/note-paper04-pin-audit-ontology-relativity.md` | `76c2a5d412f56b031ff4e0bbce87a22f9c7ea65a4fe24f62289fbcc12be6defd` | ontology, relativity, Barandes, and hidden-time scope |
| `v17/note-paper03v32-hostile-review-adjudication.md` | `b42fcf6201e249f03772ae2f1e037c2c945e98e4221c89a629d744de937e6104` | terminal complete-boundary operational scope |
| `v17/paper-03v32-complete-boundary-relativistic-adequacy.md` | `469ae61c849573c9fe7c70871ca6b60843a080082d07f6850b48213b86d6f7d6` | accepted paired sample/ensemble/Heisenberg interface |
| `v17/paper-00-reality-first-programme.md` | `a9f8456763447eaa25a6a840e8f221f51d961ecd9c3ced649ae35a8616457cbe` | reality-first and gravity walls |

The permitted chronology is now:

```text
terminal generic Paper-04 boundary (#76)
  -> this exact model-specific pre-fit pin
  -> three independent model-pin audits
  -> root model-pin adjudication
  -> at most one mathematics/physics construction
  -> result-neutral construction audit
  -> three mutually blind hostile reviews
  -> independent terminal scientific adjudication
```

No derivation, fit, exact-value check, source program, numerical experiment,
or result may occur before the three model-pin audits and root adjudication.
Any semantic rejection halts this sole model selection. There is no automatic
Paper-04 v2/v3 model chain.

## 1. Why this model is selected before calculation

The model is not selected because it is known to pass. It is selected by the
following pre-result criteria.

1. A finite Abelian constraint gives an exact normalizable physical Hilbert
   space and exact reduction maps, avoiding divergent group averaging.
2. Two clock factors are explicit and separately addressable rather than two
   aliases of one pointer.
3. One lowest-degree bilinear charge term supplies reciprocal clock--matter
   influence instead of inserting an inert coupling.
4. A finite model necessarily contains recurrence, a stopped-clock sector,
   finite resolution, and non-global temporal frames; these are tested rather
   than idealized away.
5. The same parent admits a laboratory gauge-fixed description and two
   internal-clock descriptions without importing a metric or causal order as
   an output.
6. The model is small enough for complete symbolic proof over every state
   sector and complete registered reader, but not so small that two-point
   calibration is the only possible test.

The prime $p=7$ is fixed as the smallest odd prime in this family that gives:

- invertible clock charges $1$ and $2$;
- two calibration points and two additional nonwrapping held-out points for
  the rate-two clock;
- a nontrivial stopped-clock sector;
- distinct reciprocal-coupling source sectors; and
- at least two separated relational intervention opportunities.

Changing $p$, a charge coefficient, the interaction polynomial, or the
registered window after this freeze is semantic model replacement.

## 2. Kinematic systems and finite clock observables

Let

$$
\mathbb F=\mathbb Z_7,
\qquad
\omega=e^{2\pi i/7}.
$$

The kinematic Hilbert space is

$$
\mathcal H_{\rm kin}
=
\mathcal H_A\otimes\mathcal H_B\otimes
\mathcal H_M\otimes\mathcal H_Q,
\qquad
\mathcal H_J\cong\mathbb C^7.
$$

$A$ and $B$ are the two candidate clocks. $M$ is a time-sensitive matter
readout system. $Q$ is a matter/control system used to make reciprocal
clock--matter response visible without conflating that response with the
held-out $M$ task.

For every $J\in\{A,B,M,Q\}$, freeze an orthonormal charge basis
$\{|k\rangle_J:k\in\mathbb F\}$ and the conjugate phase basis

$$
|\tau\rangle_J
=
\frac1{\sqrt7}\sum_{k\in\mathbb F}\omega^{k\tau}|k\rangle_J,
\qquad
\tau\in\mathbb F.
$$

Let $K_J|k\rangle_J=k|k\rangle_J$ in the declared integer representatives
$0,\ldots,6$. The physical reading instrument for each clock is the exact
phase PVM

$$
E_J(\tau)=|\tau\rangle\langle\tau|_J
$$

with its Lüders update and a retained seven-valued classical record. The
construction must type the complete readout dilation and later guards through
the accepted Paper-03 paired semantics; a bare conditional amplitude is not
enough.

The two clocks have distinct subsystem identities, distinct charge
coefficients in the parent, separately addressable phase instruments, and
independently variable local origin/charge preparations. Correlation under
the parent constraint does not make them probabilistically independent and
must not be replaced by a product law.

No absolute phase outcome is declared a gauge-invariant beable. A phase value
is a clock-frame coordinate carried by the reduced Paper-03 boundary; its
physical content lies in complete correlations and transforms under a change
of frame. The construction owes a covariant dilation or reduction/lift theorem
for any retained record. A classical register that silently fixes an external
phase origin is the hidden-reference mutant and must be refused.

## 3. The clock-neutral parent

Define the fixed charge polynomial

$$
D(k_A,k_B,k_M,k_Q)
=
k_A+2k_B+k_M+k_Q+k_Ak_Q
\pmod 7.
$$

The one-parameter finite gauge representation is

$$
U_g|k_A,k_B,k_M,k_Q\rangle
=
\omega^{gD(k_A,k_B,k_M,k_Q)}
|k_A,k_B,k_M,k_Q\rangle,
\qquad g\in\mathbb F.
$$

All factors in the exponent commute, so $U_gU_h=U_{g+h}$ exactly. The
physical projector and physical Hilbert space are

$$
P_{\rm phys}=\frac17\sum_{g\in\mathbb F}U_g,
\qquad
\mathcal H_{\rm phys}=P_{\rm phys}\mathcal H_{\rm kin}.
$$

The physical inner product is the inherited Hilbert inner product on this
finite subspace. No kinematic distribution, divergent gauge volume, or
unprinted rigging normalization is allowed.

Because $2$ is invertible modulo $7$, every triple $(k_A,k_M,k_Q)$ fixes one
$k_B$ on $D=0$. The construction must prove, rather than merely state,

$$
\dim\mathcal H_{\rm phys}=7^3.
$$

The bilinear term $k_Ak_Q$ is the unique selected interaction. Its coefficient
is exactly one by the frozen charge-unit convention. Removing that term is the
registered no-interaction mutant; changing its coefficient is forbidden.

This finite constraint is the primary mathematical object. The laboratory
orbit family defined later is a gauge-fixed comparator derived from these
same frozen bytes, not the source from which the constraint may be fitted.

## 4. What the gauge action is expected to test, not assume

For a $Q$ charge eigenstate $|q\rangle_Q$, the gauge action changes the phase
labels according to the formal identities

$$
\tau_A\mapsto\tau_A+g(1+q),
\qquad
\tau_B\mapsto\tau_B+2g,
\qquad
\tau_M\mapsto\tau_M+g.
$$

For an $A$ charge eigenstate $|a\rangle_A$, it similarly gives

$$
\tau_Q\mapsto\tau_Q+g(1+a).
$$

These displayed relations identify what the construction must derive from
$U_g$ and the phase basis. They are not pre-awarded results. They expose the
physical design:

- changing $Q$ charge can alter the rate of clock $A$;
- changing clock-$A$ charge can alter the phase response of $Q$;
- clock $B$ remains a separately addressable rate-two reference;
- the $q=6$ sector is a candidate stopped-clock sector for $A$; and
- recurrence occurs after seven gauge steps.

If any identity is false under the printed convention, the construction must
stop at the first affected coordinate. The convention may not be repaired
after evaluation.

## 5. Physical reductions and their exact domains

Freeze the candidate clock reductions

$$
\mathcal R_B(\beta)
=
\sqrt7\,{}_B\langle\beta|:
\mathcal H_{\rm phys}\longrightarrow
\mathcal H_A\otimes\mathcal H_M\otimes\mathcal H_Q,
$$

and

$$
\mathcal R_A(\alpha)
=
\sqrt7\,{}_A\langle\alpha|:
\mathcal H_{\rm phys}^{Q\ne6}\longrightarrow
\mathcal H_B\otimes\mathcal H_M\otimes
\mathcal H_Q^{Q\ne6}.
$$

Here $\mathcal H_{\rm phys}^{Q\ne6}$ is the physical direct sum of the
$k_Q\ne6$ charge sectors. The construction must prove:

1. $\mathcal R_B(\beta)$ is a unitary coordinate map onto its printed target;
2. $\mathcal R_A(\alpha)$ is unitary on the printed good sector;
3. the $q=6$ map loses rank or otherwise fails the same theorem and is
   classified as clock stoppage, not silently deleted;
4. all density-operator, observable, instrument, record, and reader maps are
   induced from these state maps and the Paper-03 complete packet; and
5. the temporal-frame map

   $$
   \mathcal S_{A\leftarrow B}(\alpha,\beta)
   =\mathcal R_A(\alpha)\mathcal R_B(\beta)^{-1}
   $$

   is claimed only on the common $Q\ne6$ domain.

No global A-frame inverse is presumed. No scalar Markov kernel is presumed to
carry the full quantum frame transformation.

## 6. Predeclared source family

All source contexts below freeze now. $|\tau\rangle$ denotes the phase state;
$|k\rangle$ denotes the charge state. Every physical source is

$$
\rho_u^{\rm phys}
=
\frac{P_{\rm phys}|\phi_u\rangle\langle\phi_u|P_{\rm phys}}
{\langle\phi_u|P_{\rm phys}|\phi_u\rangle},
$$

provided the denominator is proved positive.

| ID | Kinematic seed $|\phi_u\rangle$ | Frozen role |
|---|---|---|
| U0 | $|0\rangle_A^{\tau}|0\rangle_B^{\tau}|0\rangle_M^{\tau}|0\rangle_Q^{k}$ | primary calibration and time-sensitive matter route |
| U1 | $|0\rangle_A^{\tau}|0\rangle_B^{\tau}|0\rangle_M^{\tau}|1\rangle_Q^{k}$ | system-to-clock response |
| USTOP | $|0\rangle_A^{\tau}|0\rangle_B^{\tau}|0\rangle_M^{\tau}|6\rangle_Q^{k}$ | stopped-A control |
| UA0 | $|0\rangle_A^{k}|0\rangle_B^{\tau}|0\rangle_M^{\tau}|0\rangle_Q^{\tau}$ | A-to-Q response baseline |
| UA1 | $|1\rangle_A^{k}|0\rangle_B^{\tau}|0\rangle_M^{\tau}|0\rangle_Q^{\tau}$ | A-to-Q response intervention |
| USEQ | $|0\rangle_A^{\tau}|0\rangle_B^{\tau}(|0\rangle_M^{\tau}+|1\rangle_M^{\tau})/\sqrt2\,|0\rangle_Q^{k}$ | sequential/adaptive nontriviality |
| UCOH | $|0\rangle_A^{\tau}|0\rangle_B^{\tau}|0\rangle_M^{\tau}(|0\rangle_Q^{k}+|1\rangle_Q^{k})/\sqrt2$ | coherent full-frame and scalar-sufficiency control |
| UAO | $|1\rangle_A^{\tau}|0\rangle_B^{\tau}|0\rangle_M^{\tau}|0\rangle_Q^{k}$ | independently shifted A-origin control |
| UBO | $|0\rangle_A^{\tau}|1\rangle_B^{\tau}|0\rangle_M^{\tau}|0\rangle_Q^{k}$ | independently shifted B-origin control |

No source may be deleted after seeing a failure. No additional source may be
added to rescue a positive coordinate.

## 7. Externally parameterized comparator and hidden-time boundary

For each frozen seed, define the declared laboratory comparator

$$
\rho_u^{\rm lab}(s)
=
U_s|\phi_u\rangle\langle\phi_u|U_s^\dagger,
\qquad s\in\mathbb F.
$$

The integer $s$ is an external scheduling/calibration label only in this
comparator. It may be used to define the training and held-out benchmark
probabilities. It is forbidden from every parent-relative held-out predictor.

The supplied Paper-03 laboratory packet declares A and B co-located on one
comparator path for the primary affine test. That path, its orientation, and
its laboratory causal frontier are inputs. The finite parent does not derive
co-location, a worldline, proper time, or geometry.

The construction must produce the same registered predictions from
$\rho_u^{\rm phys}$ and physical clock records, or report the exact failure.
The dependency graph must inspect the parent projector, seed, constraint,
controllers, instruments, records, reductions, and model bytes. Merely
removing the symbol $s$ from a function signature is not evidence.

The required hidden-time mutant replaces the physical B record by the orbit
index $s$ while leaving the ordinary calibration table unchanged. It must
pass the superficial fit and fail the lineage gate. A second mutant encodes
the table $s\mapsto U_s$ in a controller lookup object while hiding its
provenance; it must receive the same refusal.

## 8. Clock readings, retained records, and relational instruments

The construction must type finite complete instruments for both clock phase
PVMs. Each outcome writes a seven-valued retained record and preserves all
future-readable apparatus memory in the Paper-03 complete boundary. The
instrument includes its Lüders disturbance; replacing it by its POVM effects
or nonselective channel is an attack.

All matter readouts use the $M$ or $Q$ phase PVM and retained classical
records. Relationally triggered instruments must be constructed as physical
paired arrows on $\mathcal H_{\rm phys}$—for example through proved reduction,
application, and lift maps or an equivalent gauge-invariant dilation. The pin
does not assume that every gauge-fixed instrument has such a lift.

The clock record in an A- or B-relative boundary is coordinate bearing. It
must transform with the complete temporal frame and may not be copied into a
gauge-trivial register as though its absolute numeral were invariant. What is
future readable and physical is the retained relational record together with
its frame lineage. Failure to construct that lineage stops M4/M5/M10.

Absolute program position may not trigger an operation. A context may name a
physical clock record and a complete relational operation, just as a
laboratory context may name a preparation or instrument setting.

## 9. Frozen sequential and adaptive context

The nontrivial sequence uses source USEQ.

1. At clock-B record $\beta_1=2$, perform the complete $M$-phase instrument
   and retain $r_1\in\mathbb F$.
2. A physical record-controlled guard applies one $M$ phase translation when
   $r_1\in\{0,2,4,6\}$ and applies the identity when
   $r_1\in\{1,3,5\}$.
3. At clock-B record $\beta_2=6$, perform the second complete $M$-phase
   instrument and retain $r_2$.
4. Compare the complete joint law of $(\beta_1,r_1,\text{guard},\beta_2,r_2)$,
   posterior boundary, and every registered later reader—not merely its
   one-time marginals.

The two opportunities correspond in the comparator to $s=1$ and $s=3$, but
the parent-relative instrument may consume only the physical B records. The
guard's memory, source, operation, and readout cost are part of the complete
packet.

The frozen sequential hostile control replaces the guard by the opposite
parity rule while preserving both one-time $M$ marginals wherever possible.
The complete joint reader must distinguish the laws or the sequential target
fails.

## 10. Calibration, held-out data, and informative error gate

The primary calibration source is U0. Use integer representatives before
the first recurrence.

| Role | Gauge steps $s$ | Clock-A readings | Clock-B readings |
|---|---|---|---|
| training | $0,1$ | $0,1$ | $0,2$ |
| held out | $2,3$ | $2,3$ | $4,6$ |

The only fitted affine parameters are $a,b$ in

$$
\tau_B=a\tau_A+b.
$$

They are fitted on the two training points. The two held-out points test the
relation; neither may be reused or dropped. The expected values, if any, are
not printed here as results and must be derived by the construction.

The time-sensitive matter comparator is the complete $M$-phase reader for U0
at all four steps. The sequential USEQ context is an additional held-out task.
The primary loss is maximum total-variation distance over all registered
complete finite record laws, sources, and contexts. A positive exact
equivalence coordinate requires loss exactly zero. Approximate success is not
registered in this finite model.

Two frozen baselines must be evaluated:

1. `NO-CLOCK`: replace the candidate clock record by an independent uniform
   seven-valued record;
2. `MISTUNED-B`: replace the coefficient $2k_B$ in the comparator-only mutant
   by $3k_B$ while leaving calibration and held-out interpretation fixed.

At least one predeclared time-sensitive reader must separate each baseline
from the target for ordinary clock adequacy to be informative.

## 11. Full frame transport and the sufficiency fork

The primary temporal-frame object is the complete quantum map
$\mathcal S_{A\leftarrow B}$ on the common $Q\ne6$ sector. It must transform:

- states and coherent source superpositions;
- $M$ and $Q$ observables;
- both clock records;
- complete instruments and posteriors;
- guard memory and adaptive contexts; and
- all registered later readers.

For U0, U1, and UCOH, separately test whether a scalar stochastic kernel on
the clock readings suffices for every complete reader. The scalar mixture
formula may be awarded only if conditional sufficiency is proved. If UCOH
retains phase information that a reading-only kernel loses, print the failure
and retain the full quantum frame map. Quantum coherence may not be discarded
to manufacture a classical change of clock.

Round-trip equivalence is tested on every source and complete context in the
common sector. The USTOP sector is outside the A-frame domain and must remain
an explicit loss/stoppage result.

## 12. Nonidentity reading-coordinate transformations

On the positive-support nonwrapping real reading windows, freeze exactly:

$$
f_{\rm aff}(x)=3x+1,
\qquad
f_{\rm nl}(x)=x+\frac{x^3}{100}.
$$

Both are strictly increasing Borel isomorphisms from each finite declared
window to its image. The construction must push forward the phase instrument
labels, retained records, probability/reference measure, null class,
calibration points, error rule, conditionals, and complete readers. It must
prove prediction equality after both transformations. The identity map does
not count as this test.

No orientation-reversing or noninjective map is registered as gauge. The
latter is a lossy physical coarse-graining control.

## 13. Finite resource and domain ledger

The following resources and domains freeze before evaluation.

| Object | Frozen resource/domain |
|---|---|
| each of A, B, M, Q | dimension $7$ |
| kinematic parent | dimension $7^4$ |
| candidate physical parent | expected dimension $7^3$, to be proved |
| bare charge representatives | $0,1,\ldots,6$ in one declared charge unit |
| each bare charge bandwidth | six charge units |
| clock phase resolution | one of seven orthogonal outcomes |
| primary good window | gauge steps $0,1,2,3$ only |
| first recurrence | seven gauge steps for the bare phase representations |
| readout | exact Lüders phase instrument plus retained record |
| calibration success | no postselection; every positive-support run retained |
| parent normalization | exact finite physical inner product |

The construction must additionally print expectation/variance of the bare
charges on each source, interaction-generator support, record dimensions,
guard memory, and every preparation/readout operation used. It may not call
the modular charge a semibounded physical energy without a separate theorem.

No large-$p$, continuum, infinite-runtime, ideal-clock, or gravitational
limit is part of this model.

## 14. Reciprocal interaction tests

The interaction is tested by the following fixed comparisons.

### System-to-clock arm

Compare U0 and U1 at the same B records and measure the complete A-record
law. The only changed source field is the $Q$ charge. Removing $k_Ak_Q$ is the
paired mutant. A positive arm requires a nonzero complete-reader difference
under the frozen law and its disappearance or exact alteration in the mutant.

### Clock-to-system arm

Compare UA0 and UA1 at the same B records and measure the complete $Q$-phase
law. The only changed source field is the A charge. The same interaction-
deletion mutant is used. A positive arm requires a nonzero complete-reader
difference without any retuning.

Both directions must pass. A nonzero phase in the generator, global phase,
or response visible only after changing the reader is insufficient. The
readers are frozen above.

## 15. Stoppage, recurrence, and multiple-choice controls

USTOP tests the $q=6$ sector. The construction must determine whether A is
stopped, its reduction loses rank, or another exact failure occurs. It may
not delete the sector or infer that M, Q, or B stop.

U1 tests a different A/B rate relation from U0. UCOH tests a coherent
superposition of such sectors. These may show that no single scalar affine or
stochastic clock transformation covers all sources. Such a result is
`P04-CLOCK-CHOICE-DEPENDENT-DYNAMICS`, not a defect to repair.

The full seven-step recurrence must be printed. The primary theorem window
ends at step $3$. No winding counter is present. Adding one is forbidden.

## 16. Record, division, and occurrence controls

The construction must instantiate four distinct complete-boundary cases:

1. readable record and a proved future-sufficient cut;
2. readable clock record with retained future-relevant apparatus memory, so
   the record alone is not a division;
3. a complete reset/future-sufficient cut with no clock record; and
4. an unresolved intermediate operation with neither property.

If the finite parent cannot realize one case, the corresponding coordinate
remains unconstructed; it may not be asserted abstractly. None of these cases
selects one actual outcome history.

## 17. Model-specific theorem targets

No target below is pre-awarded.

| ID | Frozen target |
|---|---|
| M1 | authenticate all authority and exact model-pin bytes |
| M2 | prove $U_g$ is a unitary representation and $P_{\rm phys}$ an orthogonal projector |
| M3 | prove physical dimension and normalization for every source |
| M4 | construct complete physical clock-A and clock-B packets |
| M5 | construct the common finite joint law with all records and contexts |
| M6 | derive the laboratory orbit comparator without retuning the parent |
| M7 | prove the B reduction and its complete paired semantics |
| M8 | prove the A reduction exactly on $Q\ne6$ and classify $Q=6$ |
| M9 | reproduce U0 time-sensitive held-out $M$ laws from each valid clock |
| M10 | reproduce the USEQ complete sequential/adaptive joint law |
| M11 | fit only training points and pass both held-out affine points |
| M12 | construct the full A/B temporal-frame map and its domain |
| M13 | prove scalar sufficiency or print exact reading-only information loss |
| M14 | test full-context round trip on every registered common-sector source |
| M15 | pass both nonidentity reparameterization pushforwards |
| M16 | exclude runtime and encoded external $s$ through the dependency graph |
| M17 | separate both no-clock and mistuned-clock baselines |
| M18 | print the complete finite resource, recurrence, and stoppage ledger |
| M19 | demonstrate or refute reciprocal operational backreaction in both arms |
| M20 | classify U0/U1/UCOH multiple-choice dependence without retuning |
| M21 | instantiate all four record/division combinations or print failures |
| M22 | classify the parent as independent pre-fit, formal-only, or failed |
| M23 | print the complete 28-coordinate generic Paper-04 product |
| M24 | preserve every ontology, supplied-order, spacetime, gravity, and actuality wall |

## 18. Model-specific hostile controls

The construction and later protocol must run the generic 68 attacks and at
least these 24 changed-object controls continuously.

| ID | Changed object | Required disposition |
|---|---|---|
| X1 | replace $p=7$ by a post-result larger prime | semantic retuning; refuse |
| X2 | change coefficient $2$ after calibration | different clock law; refuse |
| X3 | delete $k_Ak_Q$ | paired no-interaction mutant; backreaction must change |
| X4 | replace $k_Ak_Q$ by $cI$ | inert interaction cannot pass |
| X5 | remove USTOP | stopped-sector censoring; fail completeness |
| X6 | project UCOH to a classical mixture | coherence changed; full-frame claim fails |
| X7 | use only U0 | reciprocal and multiple-choice tests remain unconstructed |
| X8 | identify A and B phase records | subsystem-lineage collision; one clock only |
| X9 | copy A record into B | record channel, not independent clock |
| X10 | fit affine parameters on $s=0,1,2,3$ | held-out leakage; fail M11 |
| X11 | drop either held-out point | overidentification gate fails |
| X12 | set TV tolerance to a positive post-result value | unregistered approximate theory; refuse |
| X13 | replace B record by $s$ | superficial fit survives; hidden-time gate must fail |
| X14 | encode $s\mapsto U_s$ in hidden controller bytes | dependency gate must fail |
| X15 | trigger USEQ by program position | relational-instrument gate fails |
| X16 | drop the first retained record before the guard | adaptive arrow untyped |
| X17 | replace complete instruments by POVM effects | sequential physics changed |
| X18 | call $Q=6$ a zero-probability state and omit it | source is registered; refuse |
| X19 | append a winding count after recurrence | hidden external record; refuse |
| X20 | test only identity reparameterization | M15 unconstructed |
| X21 | push labels but not records/measures/readers | covariance fails |
| X22 | infer a causal arrow from increasing U0 readings | supplied-order firewall kills |
| X23 | call the modular charge ordinary positive energy | resource/ontology overclaim |
| X24 | promote conditional finite success to timeless ontology or gravity | terminal walls kill |

Each control must preserve all unchanged objects byte for byte in any later
formal verification. A control is scientific only through its changed
mathematical or physical object, not through a source-code mutation name.

## 19. Product-valued outcome

The candidate must print the original 28 Paper-04 coordinates plus these
model-selection coordinates:

```text
P04M-MODEL-PIN-AUTHENTIC
P04M-FINITE-GROUP-PARENT-CONSTRUCTED
P04M-PHYSICAL-SOURCE-FAMILY-NONEMPTY
P04M-B-REDUCTION-GLOBAL
P04M-A-REDUCTION-Q-NOT-6
P04M-A-STOPPED-Q-6
P04M-COMPLETE-RELATIONAL-INSTRUMENTS-CONSTRUCTED
P04M-HELDOUT-SEQUENTIAL-LAW-REPRODUCED
P04M-FULL-QUANTUM-FRAME-MAP-CONSTRUCTED
P04M-READING-ONLY-SUFFICIENCY-PASS-OR-FAIL
P04M-RECIPROCAL-INTERACTION-PASS-OR-FAIL
P04M-MULTIPLE-CHOICE-PASS-OR-DEPENDENT
P04M-HIDDEN-TIME-EXCLUDED-OR-DETECTED
P04M-FORMAL-ONLY-OR-OPERATIONAL-REDUNDANCY
```

No scalar success word may replace the product. The earliest failed generic
or model-specific rung controls.

Regardless of every positive finite-model result, the candidate must print:

```text
P04-CAUSAL-ORDER-STILL-SUPPLIED
P04-ONTOLOGY-SELECTION-UNCONSTRUCTED
P04-SPACETIME-CHRONOLOGY-UNCONSTRUCTED
P04-GRAVITY-UNCONSTRUCTED
P04-FUNDAMENTAL-TIME-STATUS-UNSELECTED
P04-ACTUALIZATION-UNCONSTRUCTED
```

## 20. Interpretation and claim ceiling

The construction can at most establish that this one finite constrained
parent gives a clock-relative representation and perhaps removes the external
orbit label from a bounded family of complete predictions. It cannot establish
that:

- the universe has seven-state clocks or a finite cyclic ontology;
- the finite gauge group is microscopic time;
- the parent is uniquely selected;
- all Paper-03 or QFT processes admit this parent;
- the world is fundamentally timeless;
- causal direction follows from the gauge parameter or clock readings;
- a discrete lattice or event web is real;
- spacetime dimension, metric, curvature, or Einstein dynamics emerges; or
- one outcome history becomes actual.

Because the laboratory orbit and the constraint share the same pre-frozen
charge polynomial, exact agreement is first a structural consistency theorem.
It rises above a target-fitted movie strip only if the complete pre-fit lineage,
nontrivial held-out contexts, baselines, hidden-time mutant, and independently
specified minimality criteria all survive. Even then the result is conditional
model-level operational redundancy, not empirical selection of the parent.

The model is an exact discriminator between formal wrapping, clock-relative
adequacy, full parent reduction, scalar sufficiency, multiple choice, and
bounded operational redundancy. Its value survives a negative result.

## 21. Primary-source scope

The construction and audits may use these primary sources only at their stated
hypotheses.

1. [Page and Wootters, *Evolution without evolution*](https://doi.org/10.1103/PhysRevD.27.2885): stationary parent and conditional dynamics; no unique factorization or ontology.
2. [Höhn, Smith, and Lock, *The Trinity of Relational Quantum Dynamics*](https://arxiv.org/abs/1912.00033): exact physical-inner-product and reduction-map equivalence under its constraint and clock hypotheses.
3. [Höhn, Krumm, and Müller, *Internal quantum reference frames for finite Abelian groups*](https://arxiv.org/abs/2107.07545): finite physical Hilbert spaces, group averaging, relational observables, and reduction maps; no automatic temporal or ontological interpretation.
4. [Smith and Ahmadi, *Quantizing time: Interacting clocks and systems*](https://arxiv.org/abs/1712.00081): interaction can change clock-relative dynamics and generate temporal nonlocality; no permission to hide the interaction.
5. [Woods, Silva, and Oppenheim, *Autonomous quantum machines and the finite sized Quasi-Ideal clock*](https://arxiv.org/abs/1607.04591): finite size, energy, error, runtime, and backreaction must be priced.
6. [Giacomini, Castro-Ruiz, and Brukner, *Quantum mechanics and the covariance of physical laws in quantum reference frames*](https://arxiv.org/abs/1712.07207): frame transformations act on complete states, measurements, and dynamics.
7. [Chataignier et al., *Relational Dynamics with Periodic Clocks*](https://arxiv.org/abs/2409.06479): periodic clocks generally have cycle-relative validity; winding is not free.
8. [Barandes, *Quantum Systems as Indivisible Stochastic Processes*](https://arxiv.org/abs/2507.21192): an external target-time index cannot become a physical clock by renaming; no Barandes ontology is inherited here.

No source selects the printed polynomial, $p=7$, the physical state family, a
fundamental clock, or a timeless ontology. Those are declared finite-model
choices under test.

## 22. Independent model-pin review protocol

Because this file selects the semantic parent rather than merely formatting a
generic question, three mutually independent audits must accept these exact
bytes before construction.

1. **Seat M — finite constraint and reductions:** representation law,
   projector, physical dimension, source normalization, B/A reduction domains,
   frame map, sufficiency fork, calibration, recurrence, and exact feasibility.
2. **Seat Q — instruments and quantum clocks:** complete clock instruments,
   relational lift, sequential/adaptive law, coherent-source transport,
   resource ledger, reciprocal response, and all quantum controls.
3. **Seat O — ontology, time, and relativity:** model-selection rationale,
   hidden schedule, physical-clock independence, record/division separation,
   supplied causal/geometry inputs, and every claim wall.

Each seat must:

- authenticate the complete authority corpus;
- read this file completely;
- reconstruct M1--M24;
- disposition all generic 68 attacks and X1--X24;
- audit the full product and outcome ceiling;
- add at least twelve fresh semantic countermodels;
- return `ACCEPT-FOR-ONE-CONSTRUCTION-WITH-BINDING-SCOPE` or
  `REVISE-MODEL-BEFORE-CONSTRUCTION`; and
- write only its assigned report without inspecting sibling work.

Reports are evidence, not votes. Root must independently adjudicate every
finding. No model construction exists until that adjudication accepts.

## 23. Halt rule

The model pin must be rejected before construction if:

- the constraint or source family is inconsistent or empty;
- either clock is only an alias or hidden readout of the other;
- the physical reduction domains cannot be typed;
- a relational complete instrument cannot in principle be constructed;
- the training/held-out split is algebraically vacuous;
- the interaction cannot in principle produce reciprocal operational effects;
- the parent necessarily consumes the external schedule;
- the result depends on deleting USTOP, UCOH, or another frozen failure source;
- an ontology, chronology, spacetime, or gravity conclusion is built into the
  definitions; or
- finite feasibility cannot be established without implementation selecting
  the physics.

If the pin passes, root may authorize one construction of these exact objects.
Candidate failure is a scientific result. No semantic repair is automatic.

## 24. Authentication

Pin LF line count: `000758`

Pin byte count: `034071`

Normalized self-SHA-256:
`29e14feae49cc7a88f1da878623c83fead437d82381fe7ce003f10a877480f85`

Normalization rule: replace the six decimal digits on each count line and the
64 hexadecimal characters on the normalized-self line by ASCII zeroes,
preserve every other byte, and compute SHA-256. The file must use LF endings,
end in one LF, and contain no trailing horizontal whitespace.
