# Gravity from Sealed Records: the derived equation of state, the unified no-go on Newton's constant, and the open frontiers

**Author:** Felix Robles Elvira (ORCID: 0009-0009-2017-4394; independent researcher)

**Status:** preprint, not peer reviewed, version 2026-06-14. A synthesis paper. Every load-bearing line is tagged by epistemic status: **[DERIVED]** = established (symbolic or high-precision numeric); **[NO-GO]** = a proven obstruction; **[OBSTRUCTED]** = blocked in practice, with the obstruction characterized; **[OPEN]** = an unsolved obligation. All numeric claims were verified at mpmath dps ≥ 80 on every modular-kernel / near-vacuum / covariance / small-χ quantity (the corpus's own audit shows float64 produces a 25–32% artifact on these), and symbolically (sympy) where exact; float-safe cases are stated as such.

## Abstract

We investigate exhaustively whether gravity emerges from the SHARD (Sealed Holonomy And Record Dynamics) sealed-records / entropic ontology, route by route, at high precision. The result is sharp and two-sided. **SHARD derives gravity's equation of state but provably not its scale.** Concretely: the Unruh–Hawking temperature `T = a/2π` is a KMS theorem [DERIVED, modulo the one named axiom (R) that fixes the value `β = 2π`]; the pure-geometry content of the area law is geometry-universal (the ball/planar capacity ratio is `U_G = 1` to ~10⁻³ — straddling unity, a consistency rather than a sign-determination — and the ball area coefficient Richardson-extrapolates to Srednicki's 0.295 with no fitted tail) [DERIVED]; the horizon record entropy is structurally **pure-area** — a curvature-free Wald–Noether charge of the Einstein sector (`dσ_A/dR = 0`), so the Clausius "no Wald corrections" precondition is discharged, not assumed [DERIVED]; the modular first law `δS = δ⟨K⟩` closes and the Jacobson Clausius relation `δQ = TδS` closes, yielding the Einstein field equations in form [DERIVED, modulo axiom (R), the continuum focusing gate, and Jacobson's `θ=σ=0` local-equilibrium premise — all isolated in §1]; and the unimodular fork demotes the cosmological constant to a non-sourced integration constant while rerouting the Bianchi obstruction exactly [DERIVED]. **But SHARD cannot fix the single absolute scale `σ_A` (which has length-weight −2 and is in bijection with Newton's `G` of weight +2, via `G·σ_A = 1/4` — linked, not equal), and this is one unified no-go theorem, not a list of failures:** every candidate lever — Tomita–Takesaki/Connes, Benincasa–Dowker causal-set normalization, fluid/gravity, entanglement-wedge/JLMS, Barbour/shape-dynamics, the Chamseddine–Connes spectral action — collapses the same way — onto a fixed weight-zero invariant (`κ·σ_A = 2π`, and likewise `G·Λ²` a pure number; the two are *separate* invariants, not numerically equal) with the absolute scale `σ_A`/`G` left as a free modulus, because sealed records carry only dimensionless, ratio, and conformal data and never an absolute record-length-to-geometric-length conversion; any route that appears to derive `1/G` provably trades it for an inverse-length cutoff `Λ` that is the same missing label [NO-GO]. (The unified obstruction is sharpened by an exact weight-counting lemma — every intrinsic record functional is weight-zero while `σ_A` is weight −2 — which closes the second-scale attacks; the Chamseddine–Connes spectral action *might* make the dimensionless content of `G·Λ²` record-intrinsic, but this is speculative — the one explicit profile already gives `f₂ = 1/(2π)²`, missing the conjectured `1/(2π)` by a factor `2π` — and in any case it leaves `G` free.) Two frontiers remain open (neither bears on `σ_A`): the **graviton/spin-2 sector** — where a distinction is essential. The *linearized spin-2 (traceless) Einstein equation itself is derived* as a geometric identity (the §1.4 null-cone lemma fixes all 9 traceless components; the traceless projection removes the free proportionality coefficient `c`/`Λ`, so the *shape* of the spin-2 equation is scale-free, while `G` itself remains explicitly as the coupling `8πG T_⟨ab⟩`) [DERIVED, modulo the §1.4 conditionals — the continuum focusing gate I1 and the `θ=σ=0` local-equilibrium premise]; what is [OBSTRUCTED] is the matter modular *charge* that would independently price `T_⟨ij⟩` at a universal capacity — it reaches the traceless stress only at a non-universal, boost-dependent, second-order coupling (reinforced by type-I, Berry, and half-sided-inclusion obstructions) — i.e. the propagating *graviton* / quantum spin-2 operator, **not** the equation; and the **covariance of the dynamics** — where discreteness done the causal-set way covariantizes the *kinematics* outright (Lorentz-invariant sprinkling, the arrow living in the frame-invariant causal order, Hegerfeldt dissolved for the free flash) and the interacting-*distinguishable* dynamics is solved (Tumulka 2020), but the interacting-*field* (variable particle number) dynamics is the [OBSTRUCTED-leaning] residue. The result places SHARD squarely in Jacobson's thermodynamic tradition, with `G` an honest calibration, and adds the theorem (at audited corpus scope) that what is missing is exactly *one overall dimensionful unit* — not the dimensionless gravitational coupling-per-species `c_m = Gm²/ℏc`, which is weight-zero and intrinsic and therefore *eligible* to be a record output (the open hierarchy question). So the records cannot fix the absolute scale of gravity; whether they fix its dimensionless strength is a separate, still-open question.

---

## 1. What is derived: the equation of state

The thermodynamic/entropic derivation of the gravitational field equations assembles from the record ontology in full, and we verified each piece.

**1.1 The temperature `T = a/2π`. [DERIVED]** The division/flash rate of records along an accelerated worldline supplies the Unruh temperature via the KMS condition on the accelerated Wightman function: `W(τ − iβ) = W(−τ)` at `β = 2π/a` holds to the i-epsilon floor (residual ≈ 3.75×10⁻⁴⁰), and detailed balance `F(E)/F(−E) = e^{−E/T}` is machine-exact (≈ 10⁻¹⁰¹) at `T = a/2π`. The Rindler interval identity `Δt² − Δx² = (4/a²)sinh²(aΔτ/2)` is exact (sympy). Källén–Lehmann thermality survives interactions *for fields with a positive free-field spectral representation* (the Unruh temperature is mass-independent, so a positive spectral superposition preserves stationarity and KMS at the same `T`); genuinely interacting sectors without a KL representation (confining / anomalous-dimension) are a separate conditional, not covered by this argument. *Scope:* Gibbs thermality at *some* `β` is unconditional (record passivity / a finite Lenard theorem); the *value* `β = 2π` — hence `T = a/2π` and the Clausius coefficient `η = 1/4G` — rests on the one named, unproven axiom (R) (the Euclidean-rotation / modular-period identification, the residue of Bisognano–Wichmann). The no-go of §2 does not depend on (R).

**1.2 Geometry-universal area density. [DERIVED]** A `3+1` massless scalar partial-wave-decomposes into radial chains; the ball entropy `S(R) = Σ_l (2l+1) S_l(R)` gives an area coefficient that converges to **Srednicki's 0.295** (`a(l_c=5120) = 0.295307`, Richardson `a_∞ = 0.295417`, grid- and box-robust to ~10⁻⁵) with **no fitted tail** — a genuine universal constant. The ball/planar capacity ratio `U_G = ν_sph/ν_planar = 1` to ~10⁻³ (it straddles unity, cutoff-limited). Crucially, the absolute scale `c = σ_A = G` *cancels* in `U_G` (sympy-confirmed: `ν = c·f_geom`, `U_G = f_sph/f_pl`), so `U_G = 1` is a pure-geometry statement orthogonal to the scale. The *geometry factor* `f_geom` of the area law is therefore SHARD-derived; only the overall scale is free. *Two honest caveats:* `U_G = 1` *straddles* unity at the ~10⁻³ level (the sign of `U_G − 1` is undetermined, cutoff-limited — a consistency, not a precision determination); and flat-screen universality is *broken in the angular sector* — the partial-wave first-law ratio `q_l` departs from 1 (down to ≈0.83 for the in-window `l=1`), an mp-converged effect attributed to the centrifugal matter profile, not to the area coefficient. So `[DERIVED]` covers the area-coefficient ratio and the `l=0` (scalar) channel; the angular sector is a measured departure, not a closed universality.

**1.3 Pure-area Wald–Noether charge. [DERIVED]** The record entropy is screen-cell counting, `S = σ_A·A + o(A)`, with the curvature pieces explicitly subleading. The apparent curvature-running of the marginal area density shrinks monotonically with the mode cutoff (`|Δa| = 3.2×10⁻³ → 6.9×10⁻⁴` over `l_max = 40→200`), *consistent with* an `l`-truncation artifact rather than a Wald coupling (the load-bearing element is the corpus's floor-converged `l_c=5120` sweep, with this cutoff-shrinking trend corroborative); the Wald density is then `s = σ_A` with `ds/dR = 0` by construction (the screen-cell capacity does not couple to a curvature scalar), so this is the *statement* that `σ_A` is curvature-free, supported by — not independent of — the convergence evidence. The `l=0` (no-centrifugal) channel completes at `q = 1.000`. So `η = 1/4G` is well-defined (not running): the Clausius route stays in the Jacobson-1995 *equilibrium* branch (pure Einstein), not the 2006 nonequilibrium `f(R)`/Lovelock branch. The record entropy is the von Neumann entropy of the boost-thermal reference state `σ_x = e^{−H_x/T_x}` with `H_x` the wedge-boost Noether charge, satisfying the modular first law `δS_A = δ⟨K_A⟩` — the algebraic shadow of the Wald first law. This **discharges** the "no Wald corrections" assumption *within the screen-cell entropy ontology* — it is a structural property of that ontology, corroborated by the convergence sweep, not a derivation that an *arbitrary* higher-curvature gravitational action generates no Wald corrections.

**1.4 The field equations in form. [DERIVED]** The Jacobson Clausius derivation is exact (sympy): Raychaudhuri `θ = −R_kk λ + O(λ²)`; the heat- and area-side affine moments cancel identically; the null-cone lemma forces a pure-trace tensor (`S_ab = Φ g_ab`) — fixing **all 9 traceless components in one geometric step** (the same geometric content underlies Jacobson's 2016 entanglement-equilibrium derivation; the correct timelike statement is that a symmetric `E_ab` whose contraction `E_ab u^a u^b` is the *same* for every unit timelike `u` must be `c g_ab` — *not* the vanishing condition, which forces `E=0`), so the *spin-2 (traceless)* part of the Einstein equation `G_⟨ab⟩ = 8πG T_⟨ab⟩` is delivered geometrically: the traceless projection fixes away the free proportionality coefficient (the `c` that would multiply `g_ab`, tied to the cutoff `Λ`), so the *shape* of the spin-2 equation is scale-free — `G` of course remains, explicitly, as the coupling on the right-hand side `8πG T_⟨ab⟩` (consistent with the G no-go living in the trace/scale sector, not the traceless shape); contracted Bianchi closes (checked on explicit Schwarzschild and an anisotropic fluid with genuine traceless stress); the coefficient match `2π/η = 8πG` forces `η = 1/4G`. The modular first law `δS = δ⟨K⟩` (Casini relative entropy verified, `S_rel ≥ 0` strictly second order) yields the linearized Einstein equation. So the *form* `G_μν = 8πG T_μν` is the equation of state of sealed records — with `η = 1/4G` the one input. *Scope and conditionality:* the **Clausius route delivers the full *nonlinear* equation** (the pointwise null relation `2π T_kk = η R_kk` holds for the actual Ricci tensor — the Schwarzschild and anisotropic-fluid checks are nonlinear); the modular first law gives its *linearized* form (the cleaner setting for the spin-2 / traceless argument, §3). Both rest on three named conditionals: axiom (R) (the temperature value, §1.1); **Jacobson's local-equilibrium premise** `θ = σ = 0` at the bifurcation — *presumed, not derived* — which is what selects pure Einstein over the *nonequilibrium* `f(R)`/Lovelock branch (the pure-area Wald result of §1.3 keeps the *entropy* in that branch, but the kinematic `θ=σ=0` premise is separate); and the continuum focusing input `θ' = −R_kk`, the one imported continuum gate (now reduced to two *native* conditional gates — a finite double-null affine-pair readout and a cofinal-tightness / no-silent-refinement condition — whose discharge would make the chain fully native).

**1.5 The unimodular fork. [DERIVED]** Trace-free Einstein `R_ab − ¼g_ab R = 8πG(T_ab − ¼g_ab T)` is a genuine 9-component equation (traceless on both sides only in `d = 4`). Its divergence plus contracted Bianchi gives, with `Λ := (R + 8πG T)/4`, exactly `∇_b Λ = 8πG η_b` (residual identically 0, sympy) — so the conservation/Bianchi obstruction is *rerouted into a dynamical `Λ`*, not evaded. The same `8πG` is forced into both the traceless equation and the `Λ`-flow (no second coupling); the traceless RHS is homogeneous degree-1 in `G` (dps-100: `|RHS(3G) − 3RHS(G)| ≈ 10⁻¹⁰⁹`). The cosmological constant becomes a non-sourced integration constant `Λ₀` — dissolving the `Λ`-value pressure on the records ontology at the cost of typing `Λ₀` as a boundary datum.

**The path of §1 is real and substantive: SHARD reaches the Einstein field equations as the thermodynamics of sealed records**, deriving more than vanilla Jacobson (the geometry-universality and the pure-area property are *derived*, not assumed). The remainder of the paper establishes precisely where, and why, the path stops.

---

## 2. The unified no-go on Newton's constant `G`

**2.1 The result. [NO-GO]** SHARD does not, and provably cannot, fix the absolute scale `σ_A` — which has length-weight −2 and is in bijection with Newton's `G` (weight +2) via `G·σ_A = 1/4`, so fixing one fixes the other (they are linked, never equal). This is not an absence of success but a single structural theorem. Under the gauge `A_rec → λ·A_rec` holding all sealed data fixed, the records carry only weight-zero (dimensionless / ratio / conformal) invariants: `κ → λκ`, `σ_A → λ⁻¹σ_A`, and the product

  **`κ·σ_A = 2π`  and (separately)  `G·Λ² = const`**  (two weight-zero invariants — *each* a fixed pure number, not numerically equal to one another; what they share is the structure, not a value; paper6 Corollary 2; the structural lemma *invariant ⟺ weight-zero*, not the implementation-audit monomial count; here `Λ` is the inverse-length UV/spectral cutoff, distinct from the unimodular cosmological constant `Λ₀` of §1.5)

is the only thing fixed. The SIGMA-SPLIT theorem makes it concrete: the *same* sealed horizon data are consistent with `A_rec = 1` *or* `A_rec = 3`, giving `κ = 1` *or* `3` (Einstein residuals `0.0` for both). The G-rescaling test confirms it at dps-100: `s_unit → λ·s_unit` sends `G → G/λ` while *every* sealed/order/modular observable is invariant. *The single load-bearing premise — gate G1.* The no-go rests on exactly one premise, isolated here as cleanly as axiom (R) isolates the temperature value: **gate G1** — *no sealed law consumes the record area `A_rec` (a weight-`±k` dimensionful datum) except through the continuum labeling map `ℓ`.* G1 is what makes the corpus factor as `(R, ℓ)` with `g_λ` the identity on the record sector `R`, so that Theorem G forces every intrinsic record functional to weight 0 while `σ_A` is weight −2 — the no-go is airtight *iff* G1 holds. The sharpest candidate counterexample, an intrinsic *seal rate* `Γ` (divisions per proper time) carrying an absolute scale, does **not** open G1 but closes it (sympy-exact + dps-100): a record-internal `Γ` is divisions-per-seal `= 1` (weight 0, no scale); any geometric `Γ` is the seal density `1/l_step` (weight −1 — the gauge/spacing direction, verified `g_λ: Γ → Γ/μ` to ~10⁻¹⁰¹); and a putative weight-(−2) `Γ² = (#²)/A_rec` is a free record number times `1/A_rec` (a relabeling, never the absolute `σ_A`) — an intrinsic absolute scale would need weight −2 *and* weight 0 at once, which Theorem G forbids. (A second record-internal clock collapses identically: its rate against the seal count is a weight-0 count ratio, the same collapse the corpus already proved for the mass-gap correlation lengths.) So the seal-rate attack closes G1 rather than opening it, establishing the `n²`-covariance lemma outright: the seal-spacing length `l_step` is provably the unique dimensionful primitive and the sole gauge direction. *Scope:* G1's audit is proved-finite for the executed corpus and interface-level for the v1–v3 texts (and the `n²` chart convention is proved to be the unique convention site); an un-imported law consuming `A_rec` outside `ℓ` would need re-auditing — the seal-rate primitive does not, consuming only the record count plus the gauge length `l_step`.

**2.2 The mechanism: sealed records carry no absolute length. [NO-GO]** `σ_A` has length-unit weight −2; it cannot equal any weight-0 intrinsic record functional. The discreteness length `l = A_rec^{1/2}` is the "hidden length unit" — and it is itself only fixable against `G`. This is *why* every route fails the same way, captured by an exact weight-counting lemma (sympy): *every dimensionful record quantity is `(pure number) × l^{±k}` (one record length), and every intrinsic record functional factors through the record sector, hence is `g_λ`-invariant (paper6 Theorem G), hence weight-zero; so no intrinsic quantity can have the weight −2 that `σ_A` needs.* The lemma settles the *second-scale* attacks at once: a mass-gap correlation length (`ξ_{1d} = 1/η = 1.641…`, `ξ_{2d} = 23.355` — both *lattice units*, `ξ = (#)·l`, not an independent length), a transmuted scale from a second marginal sector (a fixed-point coupling is dimensionless → weight-zero, while the reference scale is forced to be `1/l` → the ratio ties sectors to each other, never to a geometric length), and an asymptotic-safety UV fixed point (`g_* = Gμ² ⇒ G = g_* l²`, reproducing SIGMA-SPLIT) all collapse onto the same fixed weight-zero invariant (`κ·σ_A = 2π`, with `G·Λ²` likewise a fixed pure number — separate invariants, not numerically equal).

**2.3 Every lever collapses onto the same invariant. [NO-GO]** We tested each candidate route to fix `G` and found a unified obstruction:

- **Tomita–Takesaki / Connes-cocycle** (modular flow as time): the boost-relevant record algebra is forced *commutative* (finite-dimensional, diagonal Gibbs state) → modular flow is trivial; the order-evidence `σ = D(P_AB‖P_BA)` is a *classical* KL (Araki relative entropy = KL exactly, |diff| = 0); the modular-time↔boost-rapidity slope is reparametrization-free (dps-80: invariant to ~10⁻⁸¹) — it *is* the free modulus. The only setting where Tomita gives a canonical modular time is the type III₁ continuum local algebra, which is GAP-1 itself and would import the QFT vacuum. [NO-GO]
- **Benincasa–Dowker causal-set action**: circular on three counts. The BD continuum constants are pure numbers (verified: d=4 prefactor `4/√6` to 80 digits, moment `M₀ = O(10⁻⁸⁵)` = the matching condition); EH-matching is one scalar equation `l²/G = 16π ξ₄` (a pure number) that fixes `l` only against `G` and supplies no absolute value; smearing introduces only a dimensionless ratio; modulus deficit unchanged (BD adds one equation *and* one unknown `l`, which is the gauge direction). Additionally pathological (action fluctuation std grows as `ρ^{+1.2}`). [NO-GO]
- **Fluid/gravity** (Navier–Stokes ↔ Einstein): `η` and `s` both scale as `1/G`, so `η/s = 1/4π` cancels `G` (verified to 10⁻⁷⁰); the BKLS map is a PDE correspondence carrying no absolute scale. [NO-GO]
- **Entanglement-wedge / JLMS**: `1/4G` is the *shared* coefficient of the area law and the bulk-boundary modular map (a consistency, not a derivation); Brown–Henneaux `c = 3L/2G` needs the AdS radius in record units = the same missing label. Structurally, the JLMS area operator *is* the spin-2/metric-fluctuation operator the currency is blind to (§3). [NO-GO]
- **Barbour / shape-dynamics**: conformal/relational structure generates no length scale, and Weyl invariance actively *removes* the absolute-area datum `σ_A` needs. [NO-GO]
- **Cosmological / de-Sitter horizon count** (the one route that is not an *obvious* collapse — the sharpest test of the lemma): the unimodular `Λ₀` (§1.5) is a genuine weight-(−2) datum — *identical in weight to `σ_A`, its weight-twin*. The de Sitter entropy `S_dS = π/(G·Λ₀) = N_dS` fixes only the weight-zero product `G·Λ₀ = π/N_dS`, and `N_dS = A_horizon/A_rec = 4π(σ_A/Λ₀)` is the ratio of two weight-(−2) data, so the discreteness length cancels (`l`-independent, sympy-exact) — it adds no gauge-eligible datum. The records may carry `G·Λ₀` and `Λ₀/σ_A` but *never* the absolute weight-(−2) value of `Λ₀`, so `G = (G·Λ₀)/Λ₀` stays free for exactly the reason `σ_A` does. The only escape — that the record *dynamics* intrinsically *selects* `Λ₀`'s value — is precisely what Theorem G forbids (every intrinsic functional is weight-0) and what the corpus's own honesty confirms: `Λ₀` is a non-sourced integration constant (paper42's undersourcing table — collapse heating undersources `ρ_Λ` by 10–17 orders; only the *drift* `dΛ = 8πG η` is sourced, never the value). So the de Sitter route is not a second channel — it is the same weight collapse with `σ_A` replaced by its weight-twin `Λ₀`. [NO-GO]
- **Chamseddine–Connes spectral action**: the `a₂` heat-kernel coefficient gives `1/(16πG) ∝ f₂ Λ²`. The spectral cutoff moments are *plausibly* record-intrinsic — the eventless-collar Gibbs profile at the proved `β = 2π` is a natural candidate test function, which would make the *dimensionless content* of `G·Λ²` a record number. But the specific value is **[CONJECTURED]**, not established: the naive collar profile `e^{−2πu}` gives `f₂ = ∫u·e^{−2πu}du = 1/(2π)²`, not `1/(2π)`, so the often-quoted `G·Λ² = 3π²` does not follow without specifying the exact measure; and identifying the Chamseddine–Connes spectral Dirac test function with the wedge-boost modular profile is an unjustified ansatz (the two live in different structures). Either way the no-go is untouched: the inverse-length cutoff `Λ` is the same missing label — sourcing it from a profile scale `τ_G = ℏ/E_G` only pins `G` to an *external configuration*, not to the records. [NO-GO]

Further levers collapse identically: the conformal trace-anomaly coefficients `(a, c)` are weight-zero (they fix dimensionless higher-curvature couplings, never `G`), and Sakharov-induced gravity gives `G·Λ² = 192π²/N` with `Λ` cancelling. Even the strongest *record-unit* attack fails: measuring a particle mass `m` in record units to claim `G = c_m/m_rec²` smuggles in the record-length↔lab-length conversion that the gauge `g_λ` acts on — `c_m`, `m_rec`, `G_rec` are all weight-zero, and the absolute unit lives *outside* every record number in the gauge orbit. The unification is the decisive content: not coincidental failures but one fact — *deriving `1/G` always trades it for a labeling-equivalent cutoff; the missing datum is exactly one absolute length unit, which no weight-zero record functional can be.*

---

## 3. The tensor sector: the graviton is not a matter modular charge

*The distinction this section turns on:* the linearized spin-2 Einstein *equation* `G_⟨ab⟩ = 8πG T_⟨ab⟩` is derived geometrically in §1.4 (the null-cone lemma fixes all 9 traceless components), modulo the §1.4 conditionals — the continuum focusing gate I1 and the `θ=σ=0` local-equilibrium premise. What is obstructed below is the *distinct* claim that the matter modular *currency* independently **prices** `T_⟨ij⟩` as a universal first-law charge — i.e. the propagating graviton / quantum spin-2 operator, *not* the equation.

**3.1 Charge-pricing status: 4 of 9. [OBSTRUCTED]** The modular currency prices the 4 energy-momentum stress components: `T_00` (energy, boost-axis first law) and `T_0i` (momentum, moving-probe first law), both at first-law scope with capacity coupling `1/(4ν)`. The trace-free Einstein equation needs all 9 traceless components; the 5 not priced are the spatial-stress components `T_⟨ij⟩` (anisotropic pressure differences and shears).

**3.2 The obstruction is non-universality, not orthogonality. [OBSTRUCTED]** A wedge modular Hamiltonian measures an energy density `K = 2π∫(boost weight)·(T_μν u^μ u^ν)` (Bisognano–Wichmann). A *single* static wedge cannot isolate a traceless spatial component (a pure spatial rotation leaves `T_00` invariant). But the 5 traceless components are **not** orthogonal to *every* boost-energy integrand: a boosted wedge reads `T'_00 = cosh²η·ρ + sinh²η·(n_i n_j T_⟨ij⟩)` (sympy-exact), and a congruence of boosted wedges over directions `n ∈ S²` has **rank 5 — it spans the entire traceless spatial-stress space** (verified: traceless family rank 5; augmented `[ρ | 5 TL]` rank 6, cleanly separated from the energy density). So the components *are* reachable. The obstruction is that the recovered coupling is **`χ·sinh²η` — observer-boost-dependent and second-order** (`sinh²η = η² + O(η⁴)`, the paper55 centered-disk susceptibility signature), *never* the single universal `1/(4ν)` that prices `T_00`/`T_0i`. On the lattice the traceless charge is non-universal: it sign-flips and swings two orders of magnitude with source shape and lattice spacing at fixed dimensionless geometry (`δK(T_xx − T_yy)` ranging `−3.3×10⁻³ → +2.0×10⁻⁴` under matched-depth refinement — a *non-universality* signature, distinct from paper52's canonical 14.6× *matched-depth modular-charge* collapse, which is a separate quantity). So GAP-3 is **4/9** for the correct, stronger reason: the energy-momentum components are priced at a universal `1/(4ν)`; the 5 spatial-stress components are spanned but only at a non-universal, second-order, boost-dependent coupling — no universal capacity prices them.

**3.3 Reading: gravity as geometry, not as a spin-2 charge. [OBSTRUCTED]** This is the Jacobson division of labour: spin-2 is *not* a matter modular charge, and four independent obstructions converge — and reduce to a single root. (i) *Non-universality* (§3.2): the traceless coupling is a boost-dependent second-order susceptibility, not a capacity. (ii) *Type I, no area operator*: the record lattice is a type-I (finite) algebra with no center-valued area operator, so the entropy fluctuation `δÂ` is a c-number (the matter side of JLMS), not the quantum area operator (the graviton DOF); and the CLPW crossed-product-by-modular-flow — the standard way to manufacture a fluctuating area operator from an algebra lacking one — is structurally blocked, requiring a type III₁ seed with an *outer* modular flow (the record order-evidence algebra is commutative, its flow inner/trivial: `‖[Â, a]‖ = 0` vs 0.8 for an outer control). The SHARD-native alternative — crossing instead by the *entropy-production σ-arrow* flow (`σ = D(P_AB‖P_BA)`, which *is* genuinely outer: `‖[L, a]‖ = 1.76 ≠ 0`, so it passes the very test that kills the modular flow) — fails for a sharper reason: the σ-arrow is the entropy production of a *stationary* ordered transport (it preserves an invariant measure `πP = π`), hence a stationary *current*, not a measure-class change; its Connes–Radon–Nikodym cocycle is trivial (ratio set `{1}`), so the crossed product is *semifinite* (a faithful normal trace exists — tracial residual `≈10⁻¹⁷`, center one-dimensional), i.e. **type I**, with a c-number area and a spin-2-blind isotropic capacity (`c₂/c₀ ≈ 10⁻¹⁶`). Seeding type III₁ would require an essentially *dissipative* arrow with no invariant measure — which the corpus's σ, defined only at stationarity, structurally is not; any such non-stationary flow would itself need the continuum collar, i.e. it is GAP-1 again. (iii) *Berry*: the first-order relative modular charge (Connes cocycle) between differently-boosted wedges has **zero spin-2 (`cos 2θ`) overlap**. (iv) *Null-cut*: the light-ray route is the strongest card for 9/9, because the null contraction `T_kk` carries the traceless stress at `O(1)` (coefficient ½, *not* `sinh²η`-suppressed, sympy-exact) — but the `O(1)` *integrand* does not survive into a universal *charge*. The universal-2π null-cut pricing **is** the half-sided modular inclusion `[K, P] = iP` (Borchers/Wiesbrock/Ceyhan–Faulkner), which has **no finite-dimensional representation** (Hermitian `K` would force an eigenvalue `k + i` off the real axis, so `P = 0`); on the lattice the null-direction traceless charge is non-universal (sign-flips `−0.19 → +0.58`, 6.3× shape spread, floor-converged) while the trace channel stays one-signed. **The single root:** every spin-2 / area-operator route (non-universality, type-I, Berry, null-cut, CLPW, Tomita, JLMS) needs the universal structure — outer modular flow, type III₁, a half-sided inclusion — that lives *only* in the continuum local algebra (GAP-1), which the finite record lattice cannot host intrinsically. So "9/9 in principle" is *empty*: the principle is exactly the type-III₁ continuum the lattice provably lacks. SHARD's emergent geometry is thus spin-2-*active* but not-a-graviton (no universal `G`, 3.4× shape spread, axis-locked, refinement-unstable); it evades Weinberg–Witten the way induced gravity does — no flat-space graviton S-matrix — but no derived graviton exists. A genuine propagating spin-2 (the real-time TT sector, an emergent holographic bulk `S = Area/4G`) is gated corpus-wide and remains [OPEN].

---

## 4. The indivisibility (Tier-1) question

The companion Level-3 program (Paper 56) asks whether the gravitational record process is genuinely Barandes-indivisible (its one-time transition law fails Chapman–Kolmogorov) rather than a classical-noise shadow. We sharpen the status here.

**4.1 The reduced channel cannot decide it — to all orders. [NO-GO for the observable route]** The divisible (continuous OU noise) and indivisible (sparse-seal) ontologies produce the bit-identical reduced coherence `D_off(T) = e^{−χ(T)}`. The OU channel is CP-divisible — its equivalent hazard `λ_OU(t) = χ'_OU(t) = (σ²τ_c/ℏ²)(1 − e^{−t/τ_c})` is globally ≥ 0 — hence `λ_OU` is itself a legitimate state-independent irreversible-seal hazard, and a sparse seal with that hazard reproduces the OU coherence curve *to all orders* (residual = machine 0 at every `T`). The "order of first disagreement" between a truncated seal and OU (O(T³) for a ramp, O(Tⁿ) for an n-term seal) is a free truncation knob, not physics. So no finite-order coherence measurement separates the two ontologies — confirming, airtight, the do-delete undecidability (the intervention structure is not an observational statistic).

**4.2 The functor is constructible; the open content is sparseness. [OPEN]** The functor (sealed-record history → Barandes `Γ` via the Gell-Mann–Hartle decoherence functional) is the consistent-histories bridge: "Chapman–Kolmogorov ⟺ refinable" is the medium-decoherence consistency condition, so *SHARD-unrefinable ≡ Barandes-indivisible* is constructible at that level (the functor is exhibited; the gravitational realization is the open part). The one genuinely open question is whether the gravitational decoherence functional has non-trivial off-diagonal support *between* sparse seals (Tier-1 indivisible) or is diagonal at all times (continuous sealing → classical/divisible) — a computation on `D`, not on `|ρ_01|`. An O(1) interference window exists at the physical closure `κ ~ 1`, but that closure is an ansatz, not forced. This does not bear on `σ_A`.

---

## 5. The two open walls (neither touches `σ_A`)

**5.1 Covariance of the dynamics. [OBSTRUCTED-leaning]** This is the binding wall, orthogonal to the scale no-go, and the analysis splits into a covariantized *kinematics* and an obstructed *dynamics*.

*Discreteness covariantizes the kinematics* (verified). Done the causal-set way, SHARD inherits Tumulka's escape — and discreteness is the asset that continuous collapse models (GRW/CSL/pilot-wave) lack. (i) A Poisson sprinkling of division events is statistically Lorentz-invariant (BHS: nearest-neighbour spacing CV flat across boosts, no recoverable frame; a regular lattice is frame-recoverable) — verified, with `s²` boost-invariant to `10⁻⁹⁷`. (ii) The entropy-production arrow `σ = D(P_AB‖P_BA)`, defined as "what is committed at an event = its causal past," reads the **frame-invariant causal partial order**, not a preferred slicing (the causal past is identical in every boost — `0/3200` mismatches; a constant-time-slice past changes for *every* event). So the arrow needs no foliation. (iii) Hegerfeldt *dissolves* for the free flash: a flash localizes a point-*event* sampled from a density, not a positive-energy *state*, and Hegerfeldt forbids only the latter.

*The dynamical completion is where it stops.* Two sharp results harden the wall. First, curl-freeness of `η` is *frame-invariant* (`dη` is a 2-form) — the issue is not the frame in which the curl is taken. The physical current `η_μ = w·u_μ` is an exact gradient **iff the flow `u` is irrotational (`u∧du = 0`) iff a global preferred foliation *exists*** (sympy-exact; a vortical flow has `u∧du ≠ 0` in every frame). So η-exactness does not *remove* a preferred foliation — it is *equivalent to having one*; for vortical matter the `Λ`-flow is genuinely multivalued. Second, a new obstruction: by Hodge, only the exact `dχ` part of `η` feeds `Λ`, while the decoherence source (the program's sole empirical falsifier, the Gaussian-onset gravitational decoherence) is the *co-exact* transverse part — and exact ⊥ co-exact in `L²`, so *forcing `η` exact zeroes the decoherence power identically* (the two are mutually exclusive). 

*Coverage by regime:* free / single-particle-distinguishable — covariant (Tumulka 2006); **interacting-distinguishable — solved (Tumulka 2020)**; the genuine open residue is a **covariant interacting *field* (variable particle number) indivisible beable**: a Branch-B *candidate* exists (paper1 §5: a microcausal `Q̂ = ½α:φ²:` collapse model; cf. Bedingham's mediating-field covariant QFT collapse), so the wall reduces to the sharp C2+C5 "dangerous pair" — whether the state-dependent mass-localizing seal can be covariant *and* localized enough to source point-gravity without re-triggering the nonlinear-collapse / Hegerfeldt no-go at spacelike separation. So the honest status is: discreteness covariantizes the kinematics outright, the interacting-distinguishable dynamics is done, and the interacting-field dynamics is the [OBSTRUCTED-leaning] wall — SHARD has the *thermodynamics* of gravity and a covariant kinematics, not yet a covariant interacting-field *dynamics*.

**5.2 The propagating graviton. [OPEN]** Beyond the static modular-currency program lies the real-time transverse-traceless sector and an emergent holographic bulk that would realize `S = Area/4G` as an *emergent* dictionary rather than an input. These are the only named routes that could reach a genuine graviton, and they are gated.

---

## 6. Status ledger

| Object | Status | Statement |
|---|---|---|
| Thermality at some `β` | **[DERIVED]** | record passivity / Lenard; exact |
| Temperature *value* `T = a/2π` | **[DERIVED, mod (R)]** | conditional on the one named axiom (R) (BW residue) |
| Geometry factor `f_geom` (area coeff. + `l=0`) | **[DERIVED]** | `U_G = 1` to ~10⁻³ (straddles); Srednicki 0.295, no fit |
| Angular first-law universality | **[OPEN]** | `q_l` departs (≈0.83 at `l=1`); measured, not closed |
| Horizon entropy = pure-area Wald charge | **[DERIVED, structural]** | `ds/dR = 0` by construction (curvature-free screen cell), corroborated by the floor-converged `l_c` sweep (finite-`l` running shrinking, not zero) |
| Modular first law / Clausius → Einstein *form* | **[DERIVED, mod (R)]** | `δS = δ⟨K⟩`, `δQ = TδS`; coefficient `η = 1/4G` |
| Cosmological constant `Λ₀` (unimodular) | **[DERIVED]** | non-sourced integration constant; Bianchi rerouted |
| Absolute scale `σ_A` ↔ `G` | **[NO-GO]** | `κ·σ_A` and `G·Λ²` are fixed weight-zero invariants with the scale free; weight-counting lemma; SIGMA-SPLIT; single load-bearing premise gate G1 (seal-rate attack *closes* it — seal spacing the unique dimensionful primitive); at audited scope |
| Dimensionless content of `G·Λ²` (spectral) | **[CONJECTURED]** | record-intrinsic *if* the seal profile is the CC test function; the value `3π²` is unverified (the naive profile gives `f₂ = 1/(2π)²`) |
| Spatial-stress pricing (5 of 9 components) | **[OBSTRUCTED]** | spanned by the rank-5 boosted congruence, but only at a non-universal, boost-dependent, second-order coupling `χ·sinh²η` — no universal `1/(4ν)` |
| Linearized spin-2 (traceless) Einstein *equation* | **[DERIVED, mod (R) + gates]** | §1.4 null-cone lemma fixes all 9 traceless comps; traceless *shape* is scale-free (`G` remains on the RHS) |
| Spin-2 matter *charge* / propagating graviton | **[OBSTRUCTED→OPEN]** | non-universal + type-I (no center area op) + Berry `cos2θ=0` + half-sided-inclusion; TT sector gated |
| Tier-1 observable indivisibility | **[NO-GO]** | OU/seal identical to all orders; not observationally decidable |
| Covariant *kinematics* (substrate, arrow, free flash) | **[DERIVED]** | BHS LI sprinkling; arrow in causal order; Hegerfeldt dissolved |
| Covariant interacting *field* dynamics | **[OBSTRUCTED-leaning]** | interacting-distinguishable solved (Tumulka 2020); field case open; η-exact ⟺ foliation, and ⊥ the decoherence falsifier |

---

## 7. What this paper does and does not claim

It claims: a high-precision demonstration, across every gravity-sector route we could identify, that the SHARD sealed-record ontology derives the gravitational field equations as an equation of state (temperature + pure-area geometry-universal entropy + modular/Clausius first law), and a unified no-go (at audited corpus scope) that the absolute coupling `G` is not encoded in the records (`κ·σ_A` and `G·Λ²` are fixed weight-zero invariants; the weight-counting lemma). The route survey is of the gravity-sector levers; the matter-sector hierarchy (a dimensionless `A_matter/A_grav` ratio) is a separate, intrinsic — and still-open — quantity that the no-go does *not* forbid (it is weight-zero), and it is not engaged here. The paper *does* claim to derive the linearized spin-2 (traceless) Einstein *equation* as geometry (§1.4, modulo the I1 continuum-focusing gate and the `θ=σ=0` local-equilibrium premise); it does **not** claim to derive Newton's `G`, a propagating graviton / quantum spin-2 *charge*, an emergent holographic bulk, or a covariant interacting-field dynamics — these are, respectively, proven impossible (`G`, modulo the audit scope), obstructed (the spin-2 *charge*: non-universal + type-I + Berry + half-sided-inclusion — distinct from the derived equation), or open (bulk; interacting-field covariance, with the *kinematics* covariantized by discreteness and interacting-distinguishable solved). Structurally, the many obstructions reduce to **two roots**: a *representation-theoretic* one — every spin-2 / quantum-area / canonical-modular route (graviton, JLMS, CLPW, null-cut, Tomita) needs the universal structure (outer modular flow, type III₁, a half-sided inclusion) that exists only in the continuum local algebra the finite record lattice cannot host intrinsically (GAP-1); and a *dimensional* one — `σ_A` ↔ `G` (weights −2 and +2) while every intrinsic record functional is weight-0, so the absolute unit is outside the gauge orbit. The framing is Jacobson's: gravity as thermodynamics, with the entropy-area density a calibration — now sharpened by the theorem (at audited scope) that the missing datum is one absolute unit, and by the derivation of the geometry factor and the pure-area property that vanilla Jacobson assumes. The honest one-line summary: **SHARD gives gravity's form for free and proves it cannot give gravity's scale.**

## 8. Precision discipline

Every modular-kernel `F(ν) = log((ν+½)/(ν−½))`, near-vacuum, covariance, spectral, and small-χ quantity was computed in mpmath at dps ≥ 80 (commonly 100), with explicit floor sweeps — never float64, which the corpus audit and our own checks show produces a 25–32% artifact on these (e.g. the traceless charge `−3.30×10⁻³` is floor-converged across `10⁻³⁰…10⁻⁷⁸` but float64 returns a clip-dependent `−4.1×10⁻³`, a 26% error; the OU exponent `χ` returns float64 0 at `x = 10⁻¹⁰` where the true value is `5×10⁻²¹`). Exact algebra (Bianchi, null-cone lemma, trace-free identities, BW boost decompositions, the weight-zero invariant ring, the all-orders OU/seal identity) is sympy-symbolic. Float-safe quantities (the entanglement entropy itself — no `F(ν)` divergence; integer combinatorial counts; BW energy-moment weights; homogeneity identities) are stated as such and were used only there.

---

## References

**Internal (corpus).** [1] *Indivisible Causal-Set Gravity* (`relativistic-isp-v6-paper1-…`). [3] *Modular Record Diamonds and the Sealed-Deletion Equivalence Principle* (`…-paper3-…`). [5] *SHARD Born Rule and GR Verdict* (`…-paper5-…`; SIGMA-SPLIT). [6] *A_rec Gauge Closure* (`…-paper6-…`; Theorem G, Corollary 2 weight-zero invariant ring). [10] *The Arrow-Positivity Theorem and the Record Renormalization Group* (`…-paper10-…`; `σ = D(P_AB‖P_BA)`). [42] *…-paper42* (unimodular fork, W2). [48] *The Hydrodynamic Completion* (`…-paper48-…`; energy first law). [49] *…-paper49* (momentum first law). [50] *…-paper50* (spherical balls; geometry-universal `ν`; Srednicki 0.295). [52] *…-paper52* (stress currency spin-2-blind; the float64 audit). [55] *…-paper55* (emergent geometry spin-2-active-not-a-graviton). [56] *Indivisible Gravitational Channel* (`…-paper56-…`; the Level-3 program, CP-divisibility no-go, Tier-1 functor). [Va] *Quantum theory from sealed records I* (`v6/publishable/paper-Va-…`). [VIII] *…paper-VIII-horizons* (`v6/publishable/`). [U] *Unimodular gravity from non-conserved matter* (`isp/publishable/paper4-unimodular-…`); [U5] `isp/v5/…-paper3-unimodular-beable-gravity-…` (the covariance obstruction, §7).

**External.** R. Penrose, *On gravity's role in quantum state reduction*, Gen. Relativ. Gravit. **28**, 581 (1996). T. Jacobson, *Thermodynamics of spacetime: the Einstein equation of state*, Phys. Rev. Lett. **75**, 1260 (1995); *Entanglement equilibrium and the Einstein equation*, Phys. Rev. Lett. **116**, 201101 (2016). C. Eling, R. Guedens, T. Jacobson, *Nonequilibrium thermodynamics of spacetime*, Phys. Rev. Lett. **96**, 121301 (2006). E. Verlinde, *On the origin of gravity and the laws of Newton*, JHEP **04**, 029 (2011). A. D. Sakharov, *Vacuum quantum fluctuations in curved space and the theory of gravitation*, Dokl. Akad. Nauk SSSR **177**, 70 (1967). D. Benincasa, F. Dowker, *The scalar curvature of a causal set*, Phys. Rev. Lett. **104**, 181301 (2010). M. Srednicki, *Entropy and area*, Phys. Rev. Lett. **71**, 666 (1993). H. Casini, M. Huerta, *Entanglement entropy in free quantum field theory*, J. Phys. A **42**, 504007 (2009). T. Faulkner, M. Guica, T. Hartman, R. Myers, M. Van Raamsdonk, *Gravitation from entanglement in holographic CFTs*, JHEP **03**, 051 (2014). A. Chamseddine, A. Connes, *The spectral action principle*, Commun. Math. Phys. **186**, 731 (1997). R. Wald, *Black hole entropy is the Noether charge*, Phys. Rev. D **48**, R3427 (1993). J. A. Barandes, *The stochastic-quantum correspondence*, arXiv:2302.10778 (2023).
