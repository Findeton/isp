# Paper 42 (v6) - SHARD: The Einstein Corrections - QNEC Re-Registration, the Unimodular Fork, the Rotation No-Go, and the Binarity of the Constant

Preprint, not peer reviewed, version 2026-06-11.

Author: Felix Robles Elvira

Subtitle:

```text
The response campaign to the Einstein-lens audit of Papers 40-41
(eight adversarial referees, eighteen verification verdicts; three
findings survived as HUGE).  Four work packages, two fired kills,
one no-go theorem, and one quiet upgrade of the program's honesty
about its own constant.  W0 (THE CONSTANT'S PEDIGREE): the
exponential-consistency fixed point is ALPHABET-DEPENDENT - binary
records give theta = 0.5437 (the program's constant), spin-1 gives
0.4656, spin-3/2 gives 0.5984 - so binarity of sealed distinctions
(S-atomicity, P37's named identification) is part of theta's
pedigree and is now stated as the selector: the JUNO registration
tests SHARD-WITH-BINARY-RECORDS.  Meanwhile the axiom-C FAMILY is
scheme-free (heat-kernel closure under scale-2 AND scale-3
refinement, residual 1e-16): families from smoothness, constants
from binary marginality - the two-pillar split survives with its
selector premise explicit.  W1 (QNEC RE-REGISTRATION): the
classical pointwise focusing identity - withdrawn in P41 v3 as
wrong-object-class - is replaced by the QUANTUM FOCUSING class,
and the exact modular kernel earns its license as a QNEC
instrument: on the massless chain the 2d Wall combination
Y = S'' + (6/c)(S')^2 is position-INDEPENDENT across the window
(spread 8e-8) and equals the strip-Casimir null energy
2 pi T_kk = -pi^2/(6N^2) to ratio 0.995 - the vacuum SATURATES
the 2d QNEC, receipted.  An excited bump state obeys the
inequality within the declared tolerance; and a symplectic
dilation of the vacuum exhibits a NEGATIVE-energy region
(delta T00 down to -1.2e-5 with total energy +5.9e-2 above
vacuum, energy cross-check exact to 4 digits) - the one-signed-
source premise of the withdrawn registration is constructively
dead, exactly as the audit demanded.  One scope correction found
in the receipts and kept visible: the spatial-derivative
surrogate is null-QNEC only for STATIONARY states; the
time-extended instrument is the registered next step.  W2 (THE
UNIMODULAR FORK): adopted - the corpus's own theorem (v5p3:
nabla T = eta != 0, REQUIRED by the decoherence falsifier) makes
conserved-coupling GR inconsistent, and the unique repair keeping
the falsifier is trace-free Einstein with dynamical Lambda,
nabla Lambda = 8 pi G eta (Josset-Perez-Sudarsky; the corpus's
unimodular paper).  The Lambda budget table is printed honestly:
at every benchmark (DP at two cutoffs, CSL at GRW and Adler
couplings) collapse heating UNDERSOURCES rho_Lambda by 10 to 17
orders - the unification registered is STRUCTURAL (Lambda's
drift is sourced by the falsifier's own eta; its value remains
the integration datum), no magnitude claim is made, and the same
table read backwards is a Lambda-drift bound on the memory
class.  Einstein anchor: his own 1919 trace-free field
equations.  W3 (THE ROTATION NO-GO): the audit's third finding -
the staticity ceiling - is now a THEOREM, not a gap: the twisted
bond kernel exp(x^T J R y) has J R symmetric but det(J R) = -1
for every rotation and every orientation-reversing involution,
so one eigenvalue is always negative and the kernel is never
PSD: REAL twisted mirrors provably cannot restore reflection
positivity for circulating sectors (kill K-W3 fired; receipts:
plain -0.14, twisted -0.64, static control -9e-18).  Stationary
g_0i physics (Kerr, frame dragging, Newton's bucket) is
re-routed through discrete COMPLEX allowability (the
Kontsevich-Segal route), registered, with the shift vector's
record meaning ported from the causal-set arc's flow-drift
observable.  Two kills fired in one campaign and both made the
program stronger: that is what the kill discipline is for.
```

## 0. Provenance, design block, receipts

This campaign responds to the first-principles Einstein-lens
audit of Papers 40–41 (eight independent referee perspectives —
background independence, equivalence principle, gravitational
radiation, the cosmological constant, energy conditions,
constraint structure, Mach, methodology — triaged and
adversarially verified, three verdicts per candidate).  Findings
adopted: the wrong-object-class registration (energy-conditions
lens), the conserved-stress contradiction (cosmological-constant
lens), the staticity ceiling (Mach lens); findings answered at
corpus level and cross-referenced rather than re-fought: the
hole-argument/constraint arc (the causal-set line's
hypersurface-deformation campaign), the gravitational-wave cell
(state-data reading), equivalence-principle universality (corpus
paper 6, H1) — all now wired into Papers 40 (§9b) and 41 (v3).

Receipts: `code/v6_p42_einstein_corrections_campaign.py`,
canonical `/tmp/v6_p42_campaign.out`, bit-identical rerun
verified.  The design block (declared targets, formulas-before-
values for W2, either-branch clauses) is printed at the top of
the canonical output; outcomes that contradicted the declared
expectations are reported as fired kills, kept fired.

## 1. W0 — the constant is alphabet-bound; the families are scheme-free

The audit's residue on the marginality pillar: θ and ε are
computed on one scaffold; is the pedigree scaffold-dependent?
Receipts, two halves:

**Alphabet dependence (the constant).**  The exponential-
consistency fixed point ⟨χ⟩ = e^{-h} for three record alphabets:
binary (χ = ±1): e^{-h} = **0.543689** = θ exactly
(θ³+θ²+θ−1 = 0 to 30 digits); spin-1 (χ ∈ {−1,0,1}):
**0.465571**; spin-3/2: **0.598359**.  Three alphabets, three
constants — the constant is alphabet-dependent, and **binarity
of sealed distinctions** (S-atomicity, already the named
identification of P37) is thereby part of θ's pedigree.  Stated
plainly in the registration's terms: **the JUNO confrontation
tests SHARD-with-binary-records.**  This is the honest sharpening
the program's own discipline demands — the selector premise was
implicit; it is now explicit and killable (a derivation of
binarity from axiom S, or its refutation, would resolve it).

**Scheme independence (the families).**  On gauge towers (P39),
the axiom-C family must be closed under *any* refinement scale if
it is to be scheme-free: heat-kernel closure under conv-4
(scale 2) *and* conv-9 (scale 3) holds at max residual
$1.1\times 10^{-16}$.  The two-pillar architecture survives with
its fine print visible: **families from smoothness (scheme-free);
constants from marginality (alphabet-bound, binary).**

## 2. W1 — the QNEC re-registration

**The withdrawal (binding; recorded in P41 v3 and here).**  The
classical pointwise focusing identity is withdrawn: with the law
layer being quantum mechanics in record form, a one-signed
pointwise deficit-to-source dictionary would prove pointwise NEC
— pre-falsified physics (Epstein–Glaser–Jaffe; Casimir; Hawking
evaporation requires NEC violation exactly where the program's
Page-curve check lives).

**The replacement (registered).**  The program's dynamics core is
the **quantum focusing class**: generalized entropy
$S_{\rm gen} = A/4G + S_{\rm out}$; QNEC,
$\langle T_{kk}\rangle \ge \frac{1}{2\pi}\bigl[S'' +
\frac{6}{c}(S')^2\bigr]$ (2d Wall form), as the matter-side
bound; the exact modular kernel as the instrument.  Coefficient
candidates deferred; the ten-digit rule carries over; no hunt
performed.

**The instrument's license (receipted).**  On the massless chain
($N = 480$, Dirichlet, $c = 1$, null vector $k = \partial_t +
\partial_x$ so $T_{kk} = 2T_{00}$ for stationary states):

- **Vacuum saturation — the 2d Wall identity.**  $Y(\ell) = S'' +
  6(S')^2$ is position-independent across $\ell \in [64, 200]$
  (mean $-7.101\times 10^{-6}$, spread $8.4\times 10^{-8}$) and
  equals the Dirichlet-strip Casimir null energy
  $2\pi T_{kk} = -\pi^2/(6N^2) = -7.139\times 10^{-6}$ at ratio
  **0.995**.  The vacuum *saturates* the 2d QNEC, and the
  combination's exact $\ell$-independence — divergent pieces of
  $S''$ and $(S')^2$ cancelling pointwise — is the Wall identity
  on the record lattice.  The modular instrument is a QNEC
  instrument; this receipt is its license.
- **Excited inequality.**  A bump state ($G_X$ + rank-one, width
  16; energy cross-check $\sum \delta T_{00} = \delta\langle H
  \rangle$ to four digits — a first-run bond-halving bug in the
  energy profile was caught by exactly this cross-check and is
  documented in the receipt) obeys the inequality at every probed
  edge within the declared tolerance ($2.1\times 10^{-7}$ =
  saturation bias + twice the spread), strictly inside the bump.
- **The two-signed source, constructively.**  A symplectic
  dilation of the vacuum (valid, pure; both signs probed as
  declared) has $\delta T_{00}$ ranging $[-1.2\times 10^{-5},
  +3.4\times 10^{-3}]$ with total energy $+5.9\times 10^{-2}$
  above vacuum: **a locally negative-energy region in a valid
  record-law state.**  The one-signed-source premise of the
  withdrawn registration is dead by construction — the audit's
  central demand, discharged.
- **Scope correction (found in the receipts, kept visible).**
  The spatial-derivative surrogate equals null QNEC only for
  *stationary* states; the dilation state is not stationary, and
  its surrogate margin exceeds tolerance by exactly the $O(\epsilon)$
  scale of the missing $\partial_t S$ terms.  Kill K-W1b is
  scoped accordingly, and the time-extended null instrument
  (evolved covariances; $G_{XP} \neq 0$ modular machinery) is the
  registered next step.

## 3. W2 — the unimodular fork

**Why forced.**  P41 v2's dependency ledger required a conserved
record stress tensor; the corpus's own theorem (v5 paper 3) says
the record source obeys $\nabla^\mu T_{\mu\nu} = \eta_\nu \neq 0$
— and the non-conservation is *required* by the program's
flagship falsifier (the Gaussian-onset decoherence channel).
Conserved-coupling GR is therefore inconsistent with the
program's own matter sector.  The unique repair that keeps the
falsifier: **trace-free (unimodular) Einstein equations**, where
Bianchi converts the non-conservation into a *dynamical*
cosmological term, $\nabla_\nu \Lambda = 8\pi G\,\eta_\nu$
(Josset–Perez–Sudarsky; the corpus's dedicated unimodular paper
derives exactly this).  The Jacobson route is natively trace-free
anyway; the fork is a tightening, not a concession.  Einstein
anchor, with the irony noted: his own 1919 trace-free field
equations.

**The Λ budget, printed honestly (formulas declared before
values).**  $\rho_{\Lambda,\rm obs} = 5.30\times 10^{-10}$
J/m³.  Collapse-heating contribution $\rho_{\rm heat} = n_b
\,(dE/dt)\,t_H$ at four benchmarks:

```text
  DP  R0 = 0.5 Angstrom   2.7e-41 W/nucleon   ratio 5.5e-15
  DP  R0 = 1e-12 m        3.3e-36 W           ratio 6.8e-10
  CSL (GRW)               5.0e-44 W           ratio 1.0e-17
  CSL (Adler)             5.0e-38 W           ratio 1.0e-11
```

At every benchmark the heating **undersources** $\rho_\Lambda$ by
10–17 orders.  The registered claim is therefore *structural*,
not magnitude: Λ's drift is sourced by the same $\eta$ the
decoherence falsifier requires — collapse and dark-energy
dynamics share one current — while Λ's *value* remains the
integration datum.  No magnitude claim is registered; the table
read backwards is a Λ-drift bound on the memory class.
(Perez–Sudarsky-type magnitude successes use Planckian-
discreteness rates — a different mechanism; cited, not adopted.)
What this buys Paper 40's ledger: the worst "against interest"
disclaimer in the program (Λ unpredicted) becomes a typed
statement: value = state datum (initial condition), drift =
law-layer noise — the two-layer split applied to Λ itself.

## 4. W3 — the rotation no-go

The audit's staticity-ceiling finding, upgraded from a gap to a
theorem.  The design block declared the twisted mirror (time
reflection composed with $J = \mathrm{diag}(1,-1)$, which
reverses circulation: $JR_\phi J = R_{-\phi}$, receipt exact)
*should* restore RP on the circulating two-component chain.  **It
does not — kill K-W3 fired** (plain mirror $-0.14$; twisted
$-0.64$; static control $-9\times 10^{-18}$), and the reason is a
one-line no-go:

> **No-go (real twisted mirrors).**  The twisted bond kernel is
> $\exp(x^{\mathsf T} (JR)\, y)$ with $JR$ symmetric (since
> $JRJ = R^{\mathsf T}$); but $\det(JR) = \det J \cdot \det R =
> -1$ for *every* rotation and *every* orientation-reversing
> involution, so $JR$ always has a negative eigenvalue — and
> $e^{\mu xy}$ is not a positive-definite kernel for $\mu < 0$
> ($2\times 2$ witness: determinant $e^{-2} - e^{2} < 0$).  Real
> twisted mirrors can never restore reflection positivity for
> circulation.  The obstruction is orientation-topological.

This matches continuum physics — rotating ensembles carry sign
problems; Kerr's Euclidean section is genuinely complex — and it
*corrects this campaign's own design expectation in the open*.
Consequences, registered: stationary ($g_{0i} \neq 0$) sectors
enter the ledger-native Wick framework only through **discrete
complex allowability** (the Kontsevich–Segal route, with the KS
bound as the positivity surrogate); the shift vector's record
meaning is the causal-set arc's **flow-drift observable**, ported
as the dictionary entry; P41 v3's KS-conjecture wording is
finalized accordingly.  Newton's bucket, in record terms, is now
a *named* problem with a named route — not an unasked question.

## 5. Kill status and verdict

```text
  K-W0   alphabet roots coincide      -> did not fire (three
         distinct constants; binarity selector named)
  K-W1a  vacuum saturation fails      -> did not fire (ratio
         0.995, spread 8e-8: the instrument license)
  K-W1b  inequality violated          -> did not fire within its
         (now explicitly scoped) stationary-surrogate domain;
         the non-stationary extension is registered
  K-W3   twisted Gram fails           -> FIRED, upgraded to the
         det(JR) = -1 NO-GO; rotation re-routed to complex
         allowability
  plus:  the two-signed-source receipt retires the one-signed
         premise CONSTRUCTIVELY (the audit's central demand)
```

Two kills fired across this campaign and its parent review cycle,
and both strengthened the program: the finite-box-sine kill (P41)
moved the deep modular weight to the open ledger where it
belongs, and the twisted-mirror kill became a theorem that
correctly routes rotation to complex methods.  The Einstein
corrections leave the program with: a constant whose selector
premise (binarity) is finally explicit; a dynamics core
registered in the object class that quantum matter actually
permits, with its instrument licensed by an exact saturation
receipt; field equations targeted at the trace-free form its own
matter theorem forces, with Λ typed by the two-layer split
(value = state, drift = law); and a rotation sector that is no
longer invisible but named, no-go'd for real methods, and routed.
What Einstein's audit found was not that the program was wrong —
it was that the program had not yet read its own corpus closely
enough.  Now it has.

## Receipts

```text
code/v6_p42_einstein_corrections_campaign.py   the campaign
/tmp/v6_p42_campaign.out             canonical (BIT-IDENTICAL)
W0  alphabet roots 0.543689... (= theta, residual 0 to 30
    digits) / 0.465571 / 0.598359; heat-kernel scheme closure
    (conv-4, conv-9) residual 1.1e-16
W1  Y(l) mean -7.101e-6, spread 8.4e-8, target -7.139e-6, ratio
    0.995; tolerance 2.1e-7; bump margins min -9.7e-8 (pass),
    strict max +1.45e-6; energy cross-check 5.8565e-4 both
    routes; dilation delta T00 in [-1.2e-5, +3.4e-3], total
    +5.9e-2, negative region TRUE; surrogate scope correction
    documented (first-run bond-halving bug also documented,
    caught by the <H> cross-check)
W2  rho_Lambda = 5.30e-10 J/m^3; benchmark table as printed
    (ratios 1.0e-17 .. 6.8e-10)
W3  plain -1.395e-1, twisted -6.381e-1, control -9.0e-18;
    J R J - R^T = 0; eig(sym JR) = {-1, +1}; det argument
    one-line in the receipt
Literature: Wall (2d QNEC identity); Bousso-Fisher-Leichenauer-
Wall (QFC/QNEC); Epstein-Glaser-Jaffe; Casini; Josset-Perez-
Sudarsky; Perez-Sudarsky; Ellis et al. (unimodular); Einstein
1919 (trace-free); Diosi; Penrose; Ghirardi-Rimini-Weber; Adler;
Kontsevich-Segal; Witten (complex metrics); Jaffe-Ritter;
Gravity Probe B (frame dragging, the W3 stake); Rideout-Sorkin
(discrete general covariance, the W0 stake's neighbor).
```
