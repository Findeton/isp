# R6 / p=ε and the O35.1 bridge — the neutrino prediction's physics content (2026-06-14)

R6 gates whether the SHARD neutrino mass-step prediction has genuine physics content or is a fitted
post-diction. Read papers 34–37 directly. Two findings: an **audit correction**, and a precise
localization of the residue.

## Audit correction (important)
The corpus audit reported the neutrino prediction as "an evidence-free post-diction at 2.5σ tension,
expected to die at JUNO." That is **the bare/undressed point** (P34: bare rung `C=1` is +1.45σ today,
+5.62σ at JUNO — to be *excluded*). The **dressed** prediction is the live one:
`ε_eff = ε(1−ε)` (the dressed ladder base), and **P35 reports JUNO's first data landed within 0.05σ of
it** (registered point off by 0.23σ at JUNO-final precision). So the dressed prediction is **consistent
with current/JUNO-class data, not dying** — and the discriminating power is real (it would *exclude* the
bare point and several rival bridge forms: P35 "3 of 12 one-factor forms even at JUNO-final"). The audit
conflated the bare and dressed points; corrected here.

## The two ingredients, and where the residue actually is
The prediction `ε_eff = ε(1−ε)` factors into:

1. **p = ε (the seam weight = binding marginality) — DERIVED.** P35: the exact binding-defect functional
   of P8 is reconstructed (40-digit table), and its **second order** gives the marginality `ε`; the
   `w=3` defect flips sign at second order (binding is second-order, as P8 proved), so the marginality
   `ε` is a *derived* corpus quantity, not a fit. This ingredient is in good shape.

2. **The (1−ε) per-rung dressing factor (O35.1) — IDENTIFIED, NOT YET DERIVED.** This is the residue.
   P36 ("Seven Doors to the Per-Rung Factor, and the One Left Standing") is a full assault on O35.1:
   derive the per-rung factor `(1−ε)` from the corpus' own binding theory. Of seven standpoints, **one
   survives** — the `|coherence|² = ε(1−ε)` at purity / geometric-leakage `(1−ε)^x` form, exact at
   half-rungs — under the load-bearing structural constraint **"the rung PHASE is silent (rephasing
   gauge, axiom-S) while its MODULUS is physical."** But P37 states plainly: this is **"identified rather
   than derived"** — the surviving candidate is data-consistent and structurally constrained, *not*
   uniquely forced from first principles.

## The precise residue
> **(R6) O35.1.** Upgrade the per-rung dressing factor `(1−ε)` (giving `ε_eff = ε(1−ε)`) from "the
> surviving data-consistent candidate" to **"uniquely derived from the binding cluster expansion"** —
> i.e. prove that the binding theory FORCES a per-unit-rung multiplicative factor `(1−ε)` in the
> surviving cluster that is (a) phase-silent / modulus-physical, (b) exact at half-rungs, (c) sign-flip-
> free at all orders, with (d) a consistent charged-vs-neutrino story. The marginality `ε` (ingredient 1)
> is already derived; the residue is *the rung-dressing*, not the marginality.

## Tractability and verdict
**Hard-but-doable, not Clay-hard.** Unlike R3/R4 this is a finite, *algebraic/combinatorial* claim about
the P8 binding-defect functional and the cluster expansion — no continuum limit, no relativistic-collapse.
P36 already reduced it to a single surviving mechanism with a sharp structural requirement (phase-silent
modulus); the open step is the *uniqueness/derivation* proof that the cluster expansion forces exactly
`(1−ε)` per rung. The most promising route: take the surviving `|coherence|²=ε(1−ε)`-at-purity object and
prove it is the *forced* per-rung factor (not one of several) from the second-order binding structure +
the axiom-S phase-silence — closing the "identified → derived" gap.

## UPDATE — the dossier's decisive tension (p=ε is at genuine risk)
The full R6 dossier sharpens — and partly *undercuts* — ingredient 1. It verified the corpus' own
self-consistent **commit probability** is `⟨s⟩ = e^{−η} = θ = 0.5437` (Bernoulli variance
`θ(1−θ)=0.2481`), whereas the prediction uses `p = ε = 0.0318` (variance `ε_eff=0.0308`). So the **natural
Bernoulli parameter of the marginal triangle is θ, not ε** — `p=ε` is therefore *not* the obvious commit
probability; it is the claim that the relevant Bernoulli weight is the **seam binding-marginality** (ε),
a structural identification that is **not yet forced**. The dossier rates R6 "genuinely at risk of being
FALSE." The decisive test it specifies: take the marginal-triangle binding fixed point `h*`, form its
rank-one sealed reduction as PSD letters, and compute the *unique* Bernoulli weight `p` making that seam
stationary in the RP form — **`p=ε` ⇒ derived (prediction resurrected, 0 free parameters); `p≈θ` (or other)
⇒ `p=ε` is a choice and the prediction is a fit.** This is the genuine R6 crux, more decisive than O35.1.

**Honest status (revised):** the dressed prediction is **live and data-consistent** (audit corrected),
but `p=ε` itself is **not securely derived** — the natural commit probability is θ=0.544, so the residue
is two-layered: (1) the sharper, *prior* question — is the relevant Bernoulli weight ε (seam) or θ
(commit)? compute the stationary seam weight to decide; and (2) O35.1 — derive the `(1−ε)` rung factor.
Both are **bounded, non-Clay, falsifiable** (JUNO-final at 7.3σ), and genuinely at risk of a clean kill —
which is good science. R6 is closable *either way* (derivation or refutation), and the stationary-seam-
weight computation is the single five-minute-scale probe that would settle the `p=ε` layer.
