# Gravity and the continuum from the record order: what the order owns, what the count buys, and the two gates

**Author:** Felix Robles Elvira (ORCID: 0009-0009-2017-4394; independent researcher)

**Status:** preprint, not peer reviewed, version 2026-07-01. Fourth paper of the v8 consolidation (fully supersedes v7 Papers 7, 10, 11, 15 and note-C2; consolidates the v6 causal-set-gravity and discreteness-covariance lines, which remain citable). Tags as in paper 1 (**[THEOREM] / [DERIVED] / [DEMONSTRATED] / [CONDITIONAL(…)] / [IMPORT] / [POSITED] / [NO-GO] / [OPEN]**). Numerics mpmath (`dps = 60` for the sprinkling/combinatorics receipts, `100–120` for the kernel/Λ chain) / sympy-exact / exact combinatorics; receipts in `code/`.

## Abstract

The records are a causal set decorated by the seal dynamics (paper 1); this paper consolidates what that buys toward spacetime and gravity, in four graded steps and two gates, with each step's *derivation* carried in-paper. **(I) Order → conformal structure [THEOREM for manifold-like orders; DEMONSTRATED on the record order].** Null cones, dimension, and scale-free *directions* are recovered from the pure partial order (Malament/HKMM **[IMPORT]**; the Myrheim–Meyer estimator with its exact fractions), demonstrated with a held-out-coordinates methodology: dimension `1+1 → 2.01`, `2+1 → 3.01` from order alone; the link skeleton concentrating on the null directions; order and skeleton boost- and dilation-invariant bit-identically. A propagation **direction is order-owned and scale-free** — reclassifying the spin-2 analysis's missing direction from "unbuilt" to *order-determined, gated by manifoldlikeness, not by the scale wall*. **(II) Number → volume, up to the wall [NO-GO].** The seal count is an unbiased Poisson volume counter: the volume *measure* is recovered, ratios owned, the absolute walled at weight `+d` — closed only by inputting `G`. **(III) Spin-2, priced in three questions [DERIVED + IMPORT + NO-GO].** "Spin-2" splits into tensor structure (owned: the multipole selection rules from the conservation hierarchy), coupling (an owned conserved source fed to Weinberg's soft-graviton theorem **[IMPORT]**, with the Weinberg–Witten evasion stated), and polarization content (record-blind: the TT `5 → 2` counting needs the metric continuum for the direction — now order-owned in principle via (I) — and the mode-canonicalization wall for the `+/×` frame; the complete 9-component ledger is given, and the blindness is *re-attributed* from geometric guesses to the two named walls). **(IV) The everpresent Λ [DERIVED, weight-classified; one import, structurally resisted].** The records natively reproduce the *robust falsifiable content* of Sorkin's everpresent Λ — the scaling `δΛ ∼ ±V^{−1/2}`, exponent exactly `−1/2` — as a CLT on the native Poisson seals, by two routes proved sympy-exactly identical (unimodular conjugacy; the corpus's own drift, random-walked); the weight classification places the fluctuation handle at weight `0` (no-go-*allowed*) and the whole mean sector on walled weights — the everpresent is the **variance**, the `G` no-go is the **mean**, a genuinely third category — with de Sitter self-consistency exact in the exponent. The magnitude rests on one irreducible import, the **zero-mean centering** of the per-seal Λ-kick, and the records' own physics *points away from supplying it* (**the curl-free obstruction**: a three-way-equivalent reduction shows exactly what is needed, and the substrate delivers sign-balance and Λ-eligibility on *different objects*). The numerology is deflationary and shown: the physical `δΛ ∼ H²` is `l_p`-free; `10⁻¹²⁰` is one measured ratio squared, consistent with (not forcing) `d = 4`. A full provenance table closes the section: *every exponent-fixing ingredient is native except the centering; every magnitude-fixing ingredient is imported.* **Gate one — manifoldlikeness [OPEN, field-shared].** Steps I–II are theorems only for manifold-like orders; Kleitman–Rothschild orders dominate (`2^{n²/4}`), fool the dimension estimator (`d_MM = 2.38`, manifold-plausible, unmasked only by height `3` vs `46`), and out-count manifold data by orders of magnitude in bits; whether the click-law dynamics suppresses them is the field's central open problem, the click-law offers no proven lever, and an interference-phase probe (the Benincasa–Dowker-action mechanism) returned a clean artifact/null — with the honest note that the suppression mechanism itself *runs on the complex weight* `e^{iS}` — a locus structurally parallel to the complex-over-real bit paper 2 proves record-un-forceable (path-weight ℂ and composition-rule ℂ are analogous, not one object). **Gate two — covariance [CONDITIONAL, with the derivation in-paper].** The decoherence layer *conditionally inherits* the 2025 all-orders microcausality theorem: the seal is a self-adjoint scalar with a Lorentz-invariant positive-type colored kernel, and the inheritance holds **iff** the holonomy diamond coarse-grains to a *point-local* scalar seal density — the locality⟺spacelike-commutation hinge, operationalized (point-local commutes to all orders tested; region-smeared fails at first order); the coloring of the noise is *structural* (white noise diverges — the infinite-heating catastrophe — the quartic template converges); the statevector layer stalls at the **Haag-type wall** (`B = B1 × B2`; Tomonaga–Schwinger disproven in `d > 2`), shared with every collapse program and scoped to a flash/event ontology; and the premise decomposes into three legs — (P-Lor) reduces to owned machinery, (P-point) is **BHS-blocked for any frame-fixed finite-valency collar** (a sharp no-go with the escape routes named), (P-scalar) the open emergence core. Net: **the continuum is buildable up to `l_step` given manifoldlikeness; the conformal direction is owned; Λ's scaling is owned and its magnitude provably needs one structurally-resisted import; and the covariance frontier is reduced to one named hinge plus one shared wall.**

## 1. Order owns the conformal structure [THEOREM (manifold-like) / DEMONSTRATED]

**The theorem [IMPORT].** Malament/HKMM: the causal order of a past- and future-distinguishing Lorentzian manifold determines its conformal geometry. **The estimator.** Myrheim–Meyer inverts the ordering fraction via `f(d) = Γ(d+1)Γ(d/2) / [2Γ(3d/2)]` — exact values `f(1) = 1`, `f(2) = 1/2`, `f(3) = 8/35`, `f(4) = 1/10` (to `10⁻⁵⁰`; inversion self-consistent to `10⁻²⁰`).

**The methodology guard (load-bearing).** The receipts build the record order from a coordinate sprinkling *only to test* order-only reconstruction: every reconstructed quantity is computed from the order (plus count) alone, and the coordinates are **held out** purely as scoring ground truth — never fed to the estimator. What the demonstration shows is therefore the *conditional*: manifold-like order ⇒ recoverable conformal data; it does not manufacture manifoldlikeness (that is §5's gate).

**The demonstration [DEMONSTRATED].** On the record order: measured ordering fractions `0.4963` against `f(2) = 1/2` and `0.2259` against `8/35 = 0.228571`; recovered dimension `1+1 → 2.01`, `2+1 → 3.01` with no length input. The order's **link relation concentrates on the null directions** — the scored link slope rises `0.83 (N = 400) → 0.93 (N = 3000)` with density, the discrete skeleton of the light cone. **Invariances, bit-identical:** a boost at rapidity `0.8` and dilations at `λ = 0.137` and `7.3` leave the order and its link skeleton unchanged; a representative reconstructed direction drifts `0.0` (scored against the held-out geometry) while the associated length scales `2.70 → 19.70` under the dilation.

**The reclassification.** A propagation direction is therefore an **order-owned, scale-free member of the conformal class**: it is *not* blocked by the `l_step` wall (a direction carries no scale) — the missing input of §3's polarization question is order-determined *in principle*, gated only by §5's manifoldlikeness. What the order does **not** deliver is the `+/×` polarization *frame* (that is the mode wall; §3.3). Grade: conditional on manifoldlikeness, exactly Malament's hypothesis. Receipt `r1_order_to_conformal_direction.py` (9/9).

## 2. Number buys the volume measure, up to the wall [NO-GO]

`E[N] = V / l_step^d` (Poisson: bias `0.5%`, `Var/E = 0.97`): the seal count is an unbiased volume counter. **The μ-rescaling ledger makes the wall explicit:** under `l_step → μ·l_step` the *count* is invariant (`49` for every `μ`) while the *physical volume* it denotes scales — `49 → 196 → 534` at `l_step = 1, 2, 3.3` — so the count fixes the volume **measure** but not its unit; the absolute volume carries weight `+d` (here `+2`, the two-dimensional demonstration) and is walled, while volume *ratios* are weight-`0` and owned (`0.355` recovered against a true `0.36` — the `1.5%`). The one external input that closes the unit is `G ∼ l_step²` (natural units) — the paper-3 scale no-go recurring on the volume/conformal-factor axis. This is the continuum-side face of the v6 result that order + number give GR up to `G` and `Λ` (v6 Paper 1, citable). Receipt `r2_number_volume_lstep.py` (6/6).

## 3. Spin-2, priced in three questions [DERIVED + IMPORT + NO-GO]

"The graviton is spin-2" is not one claim but three — **tensor structure** (does the source organize as a transverse quadrupole?), **coupling** (does anything force helicity-2 to couple to it?), **polarization content** (are the two propagating modes carried?) — and the record substrate prices them differently.

### 3.1 The tensor structure [DERIVED]

The multipole selection rules hold on the record source with their *reason*: the mass monopole is the conserved total mass and the dipole the conserved momentum — **neither radiates** (`Σ m·x = 0` enforced; dipole radiation forbidden); the first radiating moment is the **traceless quadrupole** (`tr Q = 0` sympy-exact), pure `l ≥ 2`. On the illustrative circular binary, `Q_xx = 1 + 3cos(2t)` — **twice the orbital frequency, period `π`** — the standard gravitational-wave signature, obtained from the record stress bookkeeping. Honesty guard: the binary is an illustrative Tier-B slice in an assumed Euclidean 3-geometry; the *rules* are properties of the index structure and the conservation hierarchy, not of the toy.

### 3.2 The coupling [DERIVED source + IMPORT theorem]

The record scalar sector carries `T_μν = ∂_μφ ∂_νφ − ½η_μν(∂φ)²`, symmetric with `∂_μT^{μν} = 0` **on-shell** (`ω² = |k|²`), sympy-exact — exactly the conserved symmetric source to which Weinberg's soft-graviton uniqueness argument couples helicity-2 universally. The theorem is external and borrowed, never re-derived; the substrate's contribution is *owning the source*. **The Weinberg–Witten evasion, stated:** the record stress is an *emergent internal modular current*, not the fundamental Poincaré charge of a Lorentz-covariant QFT — the substrate evades the Weinberg–Witten obstruction exactly as every emergent-gravity program does, and says so. Two-slices honesty: the binary slice carries the multipoles and the massless slice the conserved-symmetric algebra; neither toy is claimed to carry both.

### 3.3 The polarization content [NO-GO against two named walls]

A symmetric-traceless spatial tensor has `5` degrees of freedom; TT-projection along a direction `n̂` leaves exactly `2` (`S^{tt}_{xx} = −S^{tt}_{yy}`, `S^{tt}_{xy}`) — the counting is float-safe integer-rank arithmetic (numpy, tolerance `10⁻¹²`, declared). The two inputs the projection needs, anatomized: **(i) the direction `n̂`** — *not* the scale wall (a direction is scale-free) but the metric continuum; by §1 this input is **order-owned in principle and manifoldlikeness-gated**; **(ii) the canonical mode frame** — the `+/×` labelling is pure gauge *given* a metric, and a genuine second input only because the continuum is unbuilt: the mode-canonicalization wall (paper 3). So the polarization blindness is largely **one missing structure seen through two walls**, and the correct attribution is to those walls — not to 3+1-dimensionality, lattice shape, or holography (the re-attribution this pricing performs).

### 3.4 The nine-component ledger [DERIVED bookkeeping]

The complete pricing of the stress components: the **4 static universal charges** (`T_00`, `T_0i`, at `1/4ν` — v6 Paper 52, citable) are the conserved energy-momentum on which the Weinberg source rests; the **5-component traceless spatial stress** (static-blind, per v6 Paper 52) is the *source* whose TT projection (`5 → 2`) yields the propagating polarizations. The alignment is **by sector, not by identity of objects** — the same traceless spin-2 sector appears once as static charge and once as propagating source; the counts are not the same objects. The v6 dynamical traceless channel (Paper 55-B) is consistent: the structure is carried, the modes are not selected. Net claims ledger: no graviton constructed; no equivalence principle derived (an owned source is *fed* to Weinberg); the polarizations not fixed; **no new gravitational no-go** — the blindness *is* the existing walls, and the contribution is the pricing plus the re-attribution. Receipt `p10_spin2_pricing.py` (dipole `= 0`, `tr Q = 0`, period `π`, on-shell conservation exact, transverse trace `< 10⁻¹²`; the [NO-GO] *attribution* is an argument, not itself a receipted computation — stated).

## 4. The everpresent Λ: scaling owned, magnitude gated [DERIVED + the one import]

Three scope guards first. (1) This section reproduces and *classifies* the **scaling** — never the value; the numerology is reproduced, not improved. (2) The heuristic spine is Sorkin's and Ahmed–Dodelson–Greene–Sorkin's, cited as such; what the records own is the *native status* of the inputs, the *weight classification*, and the *curl-free obstruction*. (3) The everpresent object is the absolute fluctuation of a **zero-mean** Λ (`std ∼ V^{−1/2}`); the nonzero-mean `1/V`-decay object (exponent `−3/2`) is the *excluded wrong target* — the exponent gap of `1` is the precision trap, honored throughout.

### 4.1 Sorkin's four inputs, and their native counterparts

The everpresent argument runs on four inputs: **(i)** a conjugacy `Λ ↔ V` (so `δΛ ∼ ℏ/δV`); **(ii)** number = volume (`V = N·l_step^d`); **(iii)** Poisson statistics (`δN = √N`); **(iv)** a zero-mean Λ (`⟨Λ⟩ = 0`), the ADGS reading `Λ = S(N)/N` — *everpresent* because the fluctuation never decays relative to the mean. Native counterparts: the seals *are* the causal set, literally (v6 Paper 1); (ii) and (iii) are §2 of *this* paper (internal); Λ enters natively as a **non-sourced integration constant** of the Clausius/Bianchi route with the sourced drift `∇_ν Λ = 8πG·η_ν` (v6 Paper 57 §1.5, citable) — Henneaux–Teitelboim gives one rigorous packaging, Jacobson another, both imported. **Only input (iv) has no native counterpart** — the pivot of the section.

### 4.2 Two routes, one statement [DERIVED]

**Route A (conjugacy).** Taking (i) as the one import: `δΛ·l_step² = 1/√N`, exponent `−1/2` sympy-exact; Monte-Carlo slope `−0.49985` (a *flagged numerical estimate with the exact target printed* — the receipt discipline throughout). Honesty items: the conjugacy bracket is **grep-confirmed absent from the corpus (0 hits)** — the one native canonical pair leaves Λ undetermined — so route A is *conditional on that one import*; and the wrong target `V^{−3/2}` is computed and excluded, not ignored. **Route B (the corpus's own drift).** Random-walk the sourced drift `∇_ν Λ = 8πG·η_ν` (the Josset–Perez–Sudarsky form; v6 Papers 40/42): reading (a), the bare one-signed drift, gives a *non-decaying mean* (`N⁰`, slope `≈ 0`) — precisely the object the v6 pricing shows undersources by `10^{9.2}`–`10^{17}`; reading (b), the zero-mean fluctuation, gives slope `−0.49539` (flagged) with the normalization `V·δΛ² = N·std²` flat (max/min `1.10`). **The reduction:** `δΛ_B − δV/V = 0` **sympy-exactly** — route B *is* route A's conjugate reading; the two are one statement, the CLT on the already-verified Poisson seals. Route B's value is the internal link: the corpus *holds* the sign-balanced `δη` — and routes it to decoherence (§4.5).

### 4.3 The weight classification, and de Sitter [DERIVED]

Under the `g_λ` grading (the tool is proved in paper 1 §4.4; the instances in paper 3): the fluctuation handle `δΛ·l_step² = N^{−1/2}` is a **pure number — weight `0`**, exactly the class the scale no-go *permits*; the mean sector sits at walled weights — `Λ₀` at `−2` (the **de Sitter twin** of `σ_A`/`G`: `S_dS = π/(G·Λ₀) = N_dS` fixes only the weight-`0` product), `∇Λ` at `−3`, `η` at `−5`, `G` at `+2` — all disjoint from `0`. Hence the `G` no-go *and* the drift pricing are both **silent** on the variance: the everpresent is the **variance**, the no-go is the **mean** — a genuinely third category. *Sorkin asserts the value; the records grade it.* **De Sitter self-consistency** (five lines, whole): `r_dS ∼ Λ₀^{−1/2}` ⇒ `V_dS ∼ Λ₀^{−2}` ⇒ `Λ₀ ∼ V_dS^{−1/2}` — the mean scales with the *same* exponent as the fluctuation (difference exactly `0`), fluctuation/mean `O(1)`: self-consistency, not tension; the match is of *exponents*, both absolutes stay weight-`−2` imports.

### 4.4 The sign of the exponent is classical and universal [DERIVED]

Why `−1/2` and not `+1/2`: the extensive reading `Λ_ext = S` grows (`slope +1/2`); the intensive reading `Λ_int = S/N` shrinks (`−1/2`). What selects *intensive* is the universal `−ΛV` coupling — Λ multiplies the 4-volume in **every** GR action, not specifically the unimodular one (present natively via the v6 Paper 57 equation of state) — making Λ degree-`0` in `V`. And the exponent's **free-symbol set is `{N}` alone: `ℏ` is literally absent** — the `−1/2` is classical discreteness statistics. This selection is independent of the sign distribution of the increments and **silent on the centering**: the clean hand-off to the obstruction. Conclusion: unimodular gravity is *not load-bearing* for the sign or for the weight-`0` structure — it is one packaging of the residual posit `⟨Λ⟩ = 0`.

### 4.5 The curl-free obstruction [NO-GO on self-derivation]

**The reduction (three-way equivalence, residuals `0`):** `[δΛ·δV ∼ O(1)] ⟺ [δS ∼ √N] ⟺ [zero-mean, O(1)-variance per-seal increment + CLT]` — and **both conditions are necessary** (a bias `m ≠ 0` gives `N⁰`, no decay; a variance `v ∼ N^p` gives exponent `(p−1)/2`). So the entire residual import is one fact about the per-seal Λ-kick. **The routing (restated self-containedly, with citable homes):** (1) because Λ is a scalar, only the **curl-free** part of the flux can source it, and the curl-free *mean* `w(t)` is **one-signed** (heating, `w > 0`) — exactly the drift the corpus prices as undersourcing; (2) the **sign-balanced** fluctuation `δη` is *not* curl-free — it cannot be absorbed into Λ — and is routed to gravitational *decoherence* (the Einstein–Langevin sector; the publishable v6 decoherence line is its citable home); (3) the seal itself is **one-signed** (`dχ = σ ≥ 0`, paper 1; hazard `λ ≥ 0`, CP-divisible — v6 Paper 56), and "decoherence rate = seal rate" is a dissolved conjecture, not a bridge (paper 6 §1). **Therefore `{sign-balanced}` and `{Λ-eligible}` are delivered on *different objects* — mutually exclusive in the substrate's own construction.** The everpresent *magnitude* is structurally, not accidentally, un-self-derivable; the obstruction is a proved routing fact, not an unbuilt gap.

### 4.6 The numerology, deflated with the numbers shown [DEMONSTRATED]

The physical fluctuation is `δΛ_phys = 1/√V_phys ∼ 1/R_H² ∼ H²` — and the Planck length **cancels**: `δΛ_phys = (1/l_step²)(l_step²/√V_phys)`, needing only the cosmic size `R_H`. The dimensionless `10⁻¹²⁰` is the *only* place `l_step (= G)` enters, and there it is a tautology: `Λ·l_step² = (l_step/R_H)²`, one measured ratio `R_H/l_step ∼ 10^{60.93}` squared (`N ∼ 10^{243.7}`, `δΛ ∼ 10^{−121.86}`); the match `log₁₀(pred/obs) = 0.0` (`|·| < 10⁻⁹⁰`) is algebraically **consistent with `d = 4` alone** *given* the everpresent coincidence as exact input — both sides are by construction `∝ R_H^{−2}` — "forces" would overstate a tautology. **Two independent scales, two different jobs:** `R_H` fixes the physical `δΛ ∼ H²`; `l_step = G` only converts it to Planck units (the no-go forbids *deriving* `G`, never *importing* it) — and importing `G` fixes the unit, never the sign of the kick: **orthogonal to the §4.5 obstruction.**

### 4.7 Provenance — native vs imported, exponent vs magnitude

| Ingredient | Status | Fixes | Receipt / citable source |
|---|---|---|---|
| Seals are the causet | NATIVE | — | v6 Paper 1 (literal) |
| Number = volume `V = N·l_step^d` | NATIVE | magnitude | §2 (receipt `r2`) |
| Poisson `δN = √N` | NATIVE | **exponent** | §2 (`Var/E = 0.97`) |
| Λ a non-sourced integration constant | NATIVE (derived) | — | v6 Paper 57 §1.5 |
| `−ΛV` coupling → Λ intensive (the *sign*) | NATIVE | **exponent** | §4.4 (`p15f`) |
| Sign-balanced *Λ-eligible* η-fluctuation | **[OPEN]** (the flip) | — | not curl-free; routed to decoherence (§4.5) |
| Weight classification (variance wt-0 / mean walled) | NATIVE | — (value-add) | §4.3 (`p15c`) |
| de Sitter `−1/2` self-consistency | NATIVE | — | §4.3 (`p15d`) |
| **Zero-mean centering of the Λ-kick** | **IMPORT** | **exponent** | §4.5 (`p15g`) |
| `O(1)` per-seal action quantum (bracket coefficient) | IMPORT | magnitude | `p15b` (`c² = N·std²`) |
| Cosmic size `R_H ∼ 1/H₀` (the physical `δΛ ∼ H²`) | IMPORT | magnitude | §4.6 (`p15e`) |
| `l_step = `Planck (`= G`; only the dimensionless `10⁻¹²⁰`) | IMPORT | magnitude | §4.6 (`p15e`; the paper-3 no-go) |

**One-line reading: every exponent-fixing ingredient is native except the zero-mean centering — which the records structurally resist (§4.5); every magnitude-fixing ingredient is imported (the one NATIVE magnitude row, number = volume, converts count to volume only through the imported unit `l_step^d`).**

## 5. Gate one: manifoldlikeness [OPEN, field-shared]

Steps §1–2 are theorems **only for manifold-like causal sets**, and generic orders are not: Kleitman–Rothschild 3-layer orders dominate the uniform measure (`2^{n²/4 + o(n²)}`, the Kleitman–Rothschild asymptotic, OEIS A001035, cited), and the dominance is crushing in information terms — at `n = 10⁴`, roughly `2.5 × 10⁷` bits of dominant-family entropy against `∼5 × 10⁵` bits of manifold-like embedding data. **The estimator trap, with its contrast:** genuine sprinklings recover their dimension (`2d → 1.991`, `3d → 3.001`) — that is what makes the trap meaningful — while a KR 3-layer order returns a *manifold-plausible* `d_MM = 2.38`, unmasked only by a second, height-based estimator (longest chain `3` for any `N`, against a `2d` sprinkling's `46`). Certification needs *agreeing* estimators, and the dominant family is exactly where they disagree. Two open layers, kept apart: whether the click-law dynamics *produces* manifold-like orders is the causal-set field's central open problem (the v6 record of a "non-discriminating finite-sprinkling drift" reads as its first symptom in this program — v6 Paper 1); and even for manifold-like orders the **Hauptvermutung** — uniqueness of the embedding — is a separate open conjecture. **The categorical split (load-bearing):** `l_step` is the program's *own* wall — a proved scale impossibility, closed by *inputting* `G` — while manifoldlikeness is the *field's* open emergence problem: not proved either way, and **not closeable by inputting a constant**. The frontier question — does the forced click-law give any lever on KR suppression — has nothing in the corpus suggesting a lever; an interference-phase probe (weighting orders by `e^{iS_BD}` with the Benincasa–Dowker action, the Carlip-style suppression mechanism **[IMPORT]**) returned a clean **artifact/null** at reachable sizes: the probe enumerated *all* posets exactly to `n = 8` (counts agreeing with OEIS A000112), ran both unlabeled- and labeled-uniform measures (via `|Aut(P)|`), tested GEOMETRIC = order-dimension ≤ 2 exactly (Dushnik–Miller / transitive orientation — a *generous* class: necessary, not sufficient, for faithful embeddability), and pre-declared its artifact control — 40 random action-shuffles, with the verdict logic *selection-seen ∧ within-shuffle-band ⇒ artifact*; `R_bulk` landed inside the shuffle band, so "artifact/null" is a designed outcome, not a shrug. The one route the probe leaves open is the **`n`-trend**: asymptotic cancellation requires the bulk-action spread `std(S_bulk)` to *grow* with `n` (KR dominance is asymptotic; enumerable `n` cannot exhibit it) — the gate is an analytic problem, not a computational one. One honest cross-connection: the suppression mechanism itself runs on the **complex weight** `e^{iS}` — positive (Boltzmann) real weights provably cannot produce the phase cancellation — signed real weights are a different, un-run class — so the field's proposed lever runs structurally parallel to the complex-over-real bit that paper 2 proves the records cannot force (path-weight ℂ and composition-rule ℂ are analogous loci, not one object); if the mechanism is right, the gate's key is one of the substrate's three blind imports. Receipts `r3_manifoldlikeness_myrheim_meyer.py` (17/17), `pE_phase_causalset.py` (the null; `PE_DPS = 120`).

## 6. Gate two: covariance [CONDITIONAL — the derivation]

### 6.1 The two-layer frame

Covariance splits into two layers with different demands: the **decoherence/master-equation layer** (microcausality of the seal noise; the *spacelike* half of Tomonaga–Schwinger integrability) and the **statevector/beable layer** (a frame-independent collapsing state). The claims below are layer-specific — the first is conditionally inherited, the second stalls at a shared wall. The program sits in the GRW/CSL family for this comparison and is pre-geometric in discipline: Lorentz structure enters only as the *emergent* structure being tested against.

### 6.2 The two supplied ingredients [DERIVED]

**(1) The seal is a self-adjoint scalar.** `E = E² = E*`, the reflection `q = 2E − 1` an involution, the content `χ` a weight-`0` relabel-invariant KL number — *scalar under substrate automorphisms* is established; *scalar under the emergent Lorentz action* is part of the §6.3 premise, not an independent gap (the internal-vs-emergent caveat, stated). **(2) The noise kernel is Lorentz-invariant and positive-type.** `f(s²)` a function of the interval alone *by construction*; the working template `ĝ(q) = exp(−q⁴/β⁴)` is positive-type **exactly, by Bochner**; the receipt's Gram minimum eigenvalue `+2.77752268119 × 10⁻⁹` is a *finite-sample certificate, not an intrinsic margin* (declared); quartic UV decay. Positive-type is this program's strengthening, not a condition of the imported theorem; Tier-B status declared.

### 6.3 The conditional inheritance — the hinge [CONDITIONAL(point-local scalar emergence)]

The 2025 all-orders microcausality theorem for collapse noise (Gundhi–Diósi–Carlesso **[IMPORT]**) needs one input beyond §6.2: the collapse-driving density must be **point-local**. That is exactly where the record primitive differs — a finite holonomy *diamond* with a collar, not a point — so point-locality is the one *new* load-bearing gap (the emergent-Lorentz-scalar property is the pre-existing covariance-frontier premise, carried, not introduced). **The hinge, operationalized** on a finite causal-lattice model: a *point-local* scalar seal density commutes with every spacelike-separated probe — the induced commutator **vanishes to all orders tested**; a *region-smeared* seal density (the diamond with finite collar) **fails microcausality at first order** — the collar straddles the spacelike gap. This is the locality⟺spacelike-commutation hinge on which the imported recursion turns — *not* a re-derivation of it (for a single point-local density the higher orders vanish trivially once the first does; the published all-orders result, nesting distinct multi-point operators, is what is borrowed). **The inheritance:** the decoherence layer inherits all-orders microcausality — and with it the spacelike half `[H(x), H(y)] = 0` of TS-integrability — **iff** the holonomy diamond coarse-grains, under an emergent Lorentz action, to a point-local self-adjoint Lorentz-scalar seal density (collar → 0). Only the **microcausality half** is inherited (the theorem's finite-energy half needs its normal-ordering, which the substrate does not carry). The premise is motivated but is an emergence statement, not a theorem — **[CONDITIONAL]**, with a clean failure mode (region-smearing breaks it at first order): the open frontier reduced to one sharp, named, borrowable hinge.

### 6.4 The coloring is structural, the kernel is not derived [DERIVED, scoped]

Two counts keep this honest. The kernel is **not derived**: `χ` is a weight-`0` scalar — the wrong *type* for a two-point function — and the quartic template is one admissible profile among many (the same status as the literature's choices). But the **coloring is forced**: discreteness is a built-in UV cutoff, and white noise (`ĝ ≡ 1`) makes the heating integral `∫q²ĝ(q)dq` **diverge** — the infinite-heating catastrophe, the collapse literature's hardest ad-hoc repair — while the quartic converges (`∫q²e^{−q⁴}dq = Γ(3/4)/4`). Honest claim: *the coloring is structural*; never *the kernel is derived*.

### 6.5 The statevector wall [NO-GO, field-shared; scoped]

The wall anatomy: interacting field representations on different Cauchy surfaces are **unitarily inequivalent** (Haag-type), and Tomonaga–Schwinger functional evolution is **disproven in dimension `> 2`** (Torre–Varadarajan); contemporary work corroborates the spacelike side *assuming* the equivalence and acknowledges the precondition fails generically (Fedida). The factorization: `B = B1 × B2` — `B1` the *unconditional shared* Haag wall (blocks GRW-flash and CSL equally; the substrate is no worse off, and strictly better at the decoherence layer), `B2` *conditional* on the §6.3 premise. Two disciplines travel with the layer: the dynamics stays **stochastic-linear** (a deterministic nonlinear term violates the no-signalling theorem), and the beable is scoped to a **flash/event ontology** — the seals are the flashes; covariant flash constructions are known to be free (Tumulka 2006) and interacting-distinguishable (Tumulka 2020) — never an interacting wavefunctional.

### 6.6 The premise, decomposed — and the finite-collar no-go [DERIVED + NO-GO]

The §6.3 premise splits into three legs. **(P-Lor)** *reduces to owned machinery*: the causal order is boost-invariant on the record arena (interval and light-cone sets invariant at rapidity `0.9` — explicitly an illustration on the owned order, not a proof of the full distributional theorem). **(P-point) is BHS-blocked:** a Poisson sprinkling admits **no Lorentz-invariant finite-valency graph** (Bombelli–Henson–Sorkin), and the receipt exhibits it — the `k = 5` nearest neighbours of a site overlap the boosted choice in only `2 of 5` while the *order* is fixed: **collar → 0 does not commute with the emergent Lorentz action for any frame-fixed sharp collar** [NO-GO, narrow: finite-valency collars only]. **(P-scalar)** is the open emergence core, with the circularity note: asserting it *presupposes* the emergent group that (P-point) shows the discreteness alone does not supply. **The open routes** (the frontier map): covariant infinite-valency or order-theoretic smearings; causal-set d'Alembertian-style layered kernels; terminal actions; a matter sector supplying the emergent Lorentz group — *but not through a frame-fixed finite-neighbour collar*. Verdict: the conditional grade of §6.3 is correct and **not improvable by a finite collar from discreteness alone**. **How this sits against the v6 residues:** the v6 discreteness-covariance line's two named covariant residues — matter irrotationality and interacting-field TS-integrability — remain the frame (citable); the §6.3 inheritance delivers the *spacelike half* of TS-integrability, and §6.5's wall is where the rest stalls.

## 7. Honest scope

Steps I–II inherit manifoldlikeness (and the **Hauptvermutung** remains open even granted it); the sprinkling in the receipts is scaffolding, never a claim. Step III constructs no graviton, derives no equivalence principle, and fixes no polarizations; its no-go is an *attribution* to existing walls, not a new impossibility. Step IV reproduces Sorkin without sharpening him; the **[OPEN] flip** that would make the everpresent fully native needs *both* (i) sign-balance of the curl-free Λ-sourcing projection of the `w`-mode fluctuation (currently *contradicted* by the routing, not merely unbuilt) *and* (ii) a genuine CLT delivering `δΛ ∼ V^{−1/2}` from it; and one further premise is disclosed — the Poisson `Var = E` kinematics under the exponent itself assumes manifold-like sprinkling (KR-dominated orders would modify it), tying §4 to §5's gate. The §4.1 integration-constant leg carries the v6 equation-of-state conditionals (axiom (R) `β = 2π`; local equilibrium; the focusing gate — v6 Paper 57 §1.4). §6 claims no full covariant collapse theory: the kernel is not derived (only its coloring is structural), the point-local-scalar premise is a motivated, named, unproved hypothesis, the imported theorem is external, and Lorentz invariance enters only as the emergent structure tested against — consistent with paper 1's covariance frontier.

## 8. Receipts

All in `code/`, re-run green: `r1_order_to_conformal_direction.py` (9/9 — fractions, dimension recovery, link slopes, boost/dilation invariance, direction drift `0.0`), `r2_number_volume_lstep.py` (6/6 — Poisson counter, the μ-ledger, ratio recovery), `r3_manifoldlikeness_myrheim_meyer.py` (17/17 — exact `f(d)` values, the sprinkling contrast, the KR trap and height unmasking, the dominance growth), `p10_spin2_pricing.py` (dipole `0`, `tr Q = 0` sympy-exact, period `π`, on-shell conservation, TT `5 → 2` with transverse trace `< 10⁻¹²` — float-safe integer-rank counting, numpy, declared), `p15a`–`p15g` (13/13, 13/13, 27/27, 18/18, 14/14, 23/23, 19/19 = 127 — routes A/B with the one flagged MC slope per scaling receipt and the exact target printed, the weight classification, de Sitter, the ℏ-absence, the three-way equivalence and necessity, the numerology; `dps = 120`), `pE_phase_causalset.py` (the interference-probe null; `PE_DPS = 120`), `c1_lorentz_scalar_seal_mcc.py` (the scalar seal + the point-local vs region-smeared hinge; all-orders-tested vs first-order failure), `c2_derived_noise_kernel.py` (the kernel: Bochner-exact positive-type, quartic template, `Γ(3/4)/4`) and `c2_bhs_collar_nogo.py` (the `2/5` boosted-neighbour exhibit; the finite-valency no-go), with the Gram finite-sample certificate `2.77752268119 × 10⁻⁹` in `f1_kernel_positive_type.py`.

## References

**This series.** Paper 1 (the substrate; the Tier-B regularity condition; the covariance frontier declaration); Paper 2 (the complex-over-real bit that this paper's §5 cross-connection consumes); Paper 3 (the scale wall of §2/§4.6; the mode wall of §3.3; the `g_λ` instances); Paper 6 (the dissolved decoherence-rate conjecture §4.5 leans on; the blindness results). Ledger: `LEDGER.md`.

**Frozen log.** v7 Papers 7, 10, 11, 15 and note-C2 — fully superseded by this paper.

**v6 (kept, citable).** Paper 1 (order + number = geometry; the finite-sprinkling drift); Papers 40/42 (the drift `∇_ν Λ = 8πG·η_ν` and its pricing); Paper 52 (the `4/9` static charges; static-blind stress); Paper 55-B (the dynamical traceless channel); Paper 56 (the one-signed hazard); Paper 57 (the equation of state; the integration-constant Λ; §1.4's conditionals); the discreteness-covariance line (the two named covariant residues); the publishable decoherence line (the Einstein–Langevin routing home of §4.5).

**External.** D. Malament, J. Math. Phys. **18**, 1399 (1977); S. W. Hawking, A. R. King, P. J. McCarthy, J. Math. Phys. **17**, 174 (1976). J. Myrheim, CERN TH-2538 (1978); D. A. Meyer, MIT thesis (1988). L. Bombelli, J. Lee, D. Meyer, R. D. Sorkin, PRL **59**, 521 (1987); L. Bombelli, J. Henson, R. D. Sorkin, Mod. Phys. Lett. A **24**, 2579 (2009). L. Bombelli, J. Noldus, Class. Quantum Grav. **21**, 4429 (2004) — the Hauptvermutung context. D. Rideout, R. D. Sorkin, PRD **61**, 024002 (2000) — the dynamics arena for the selection question. D. Kleitman, B. Rothschild, Trans. Am. Math. Soc. **205**, 205 (1975). D. Benincasa, F. Dowker, PRL **104**, 181301 (2010); S. Carlip, Class. Quantum Grav. **32**, 232001 (2015); S. Carlip, arXiv:2405.14059 (2024) — the complex-phase suppression of the non-manifold bulk that §5's texture probes (with S. Loomis, S. Carlip, Class. Quantum Grav. **35**, 024002 (2018)). R. D. Sorkin, Int. J. Theor. Phys. **36**, 2759 (1997); M. Ahmed, S. Dodelson, P. B. Greene, R. D. Sorkin, PRD **69**, 103523 (2004). M. Henneaux, C. Teitelboim, Phys. Lett. B **222**, 195 (1989). T. Josset, A. Perez, D. Sudarsky, PRL **118**, 021102 (2017). T. Jacobson, PRL **75**, 1260 (1995). S. Weinberg, Phys. Rev. **135**, B1049 (1964); Phys. Rev. **138**, B988 (1965); S. Weinberg, E. Witten, Phys. Lett. B **96**, 59 (1980); R. M. Wald, PRD **33**, 3613 (1986); S. Deser, Gen. Rel. Grav. **1**, 9 (1970). A. Gundhi, L. Diósi, M. Carlesso, arXiv:2507.06954 — the all-orders microcausality theorem §6.3 inherits. R. Haag, *Local Quantum Physics* (Springer, 1996); R. F. Streater, A. S. Wightman, *PCT, Spin and Statistics, and All That*. C. G. Torre, M. Varadarajan, Class. Quantum Grav. **16**, 2651 (1999). S. Fedida, arXiv:2506.14693. R. Tumulka, J. Stat. Phys. **125**, 821 (2006); R. Tumulka, arXiv:2002.00482 (2020) — the interacting relativistic flash process. G. C. Ghirardi, A. Rimini, T. Weber, PRD **34**, 470 (1986); P. Pearle, PRA **39**, 2277 (1989).
