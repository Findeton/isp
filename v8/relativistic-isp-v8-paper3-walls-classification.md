# The three walls, classified: what the records measure, import, and cannot decide

**Author:** Felix Robles Elvira (ORCID: 0009-0009-2017-4394; independent researcher)

**Status:** preprint, not peer reviewed, version 2026-07-01. Third paper of the v8 consolidation (fully supersedes v7 Papers 5, 17, the wall half of Paper 8, and the mode-relocation no-go of Paper 14 §3; the scale material of v6 Paper 57 / publishable paper-XI remains citable). Tags as in paper 1 (**[THEOREM] / [DERIVED] / [DEMONSTRATED] / [CONDITIONAL(…)] / [IMPORT] / [POSITED] / [NO-GO] / [OPEN]**), plus **[POSITED-STRUCTURAL]** = a structural identification whose no-go content is sound but whose presentation as a clean theorem instance would conflate distinct objects. Numerics mpmath `dps ≥ 60` for the CODATA calibration receipt, `≥ 100` (up to 140) for the wall receipts / sympy-exact; receipts in `code/`.

## Abstract

The click-law substrate of papers 1–2 delivers *relative* structure — forms, envelopes, ratios, spectra — and is barred from exactly three last inputs: the absolute **scale** (`l_step`, hence Newton's `G`), the **tensor product** (the entangling content `χ_AB` of paper 2), and the canonical **mode** (which-mass-is-which). This paper consolidates the three walls *with their derivations*, and classifies them. **SCALE [NO-GO, theorem modulo one named gate].** The equation-of-state route derives the Einstein *form* **[CONDITIONAL]** on four named premises (§1.1, with the internal asymmetry stated: the weight no-go itself rides none of them); the no-go proper is the `g_λ` grading argument (§1.2): every intrinsic record functional is weight-`0`, absolute lengths are not, and the record-scale invariants — `κ_grav·σ_A = 2π`, `G·σ_A = 1/4`, `G·Λ_uv²` (each separately constant under `l → μl`; *linked, never numerically equal to one another*) — absorb all six second-scale levers; a dedicated second-scale attack collapses every route onto the one record length. `G` is **measured**. **MODE [NO-GO on selection; import-fixed].** A seal mode is a ledger of orthogonal parity characters whose **rank `2ⁿ − 1` is a superselection invariant** (Gram `= I`, preserved by every record-internal move); cross-sector selection needs a common energy zero, but each sector measures content only against its own reference — the re-referencing shift is `(n′ − n)·ln 2`, state-independent (`H(P)` cancels exactly) — and a mode-selecting Hamiltonian **relocates** the choice into its own free coupling (the argmin switches rank at level-crossings for *every* strictly increasing multiplicity reward — the log reward's crossings sit at `α = 0.168/0.207/0.247`, none within `0.012` of any forced constant). Three internal routes fail in unison (§2.2); the matter coupling `c_m = Gm²/ℏc` is weight-`0`, `g_λ`-invariant, **blind to every executed gravity identity** (toy values `12` and `1200` both pass), factorizes as `c_m = γ_G·ν_m²` with the record density cancelling identically, and its smallness has a *derived form* (a `1/N` mode-count ratio) with an *imported magnitude* (`N ∼ 10³⁸` not corpus-forced): the strength-axis twin of the free unit, sharing the mode-canonicalization gate. **The classification [THEOREM for two of three; POSITED-STRUCTURAL for the third].** For SCALE and TENSOR a single internal symmetry group (`G₁ = g_λ`; `G₂ = R`) makes the self-consistency principle invariant, the records carry *only* invariants (the proved inclusion), and they are blind to *at least* the orbit parameter — the absolute length (continuous) and the local-tomography bit (binary, in `ker R`). MODE realizes the same *shape* but as the **weaker member**: the re-referencing group moves the *continuous* zeros while provably *fixing* the discrete rank label — the label⟺orbit-parameter identification is **[POSITED-STRUCTURAL]**, and the demotion does not touch the underlying no-go. Three residual types (continuous / binary / label); three epistemic statuses — scale *measured*, mode *import-fixed*, tensor a *contested convention* (paper 2 §6): **one no-go shape, three times; two structural no-gos plus one conditional derivation; fence forced, filling free, three ways.** Novelty is the unification, the grading, and the placement of `c_m`; the individual walls are convergent-not-novel (§4), and the hard mathematics is imported with attribution.

## 1. The scale wall

### 1.1 What is derived, and on what [CONDITIONAL — four named premises]

The equation-of-state route — local Rindler temperature, pure-area Wald entropy, `δQ = T δS` — yields the Einstein *form* on the record substrate, conditional on four named premises: **(i)** the Jacobson–Clausius premise itself (the locus of Padmanabhan's "interpretation, not derivation" critique); **(ii)** axiom (R), with the honest split — that the record state is Gibbs-thermal at *some* `β` is **unconditional** (record passivity, a finite Lenard-type theorem); the *value* `β = 2π` — hence `T = a/2π` and `η = 1/(4G)` — is the axiom; **(iii)** local equilibrium `θ = σ = 0` (Eling–Guedens–Jacobson: dropping it forces the dissipative `f(R)`/Lovelock branch — the premise is load-bearing, not decorative); **(iv)** the continuum focusing gate `θ′ = −R_{kk}` (reduced to two native conditional gates: a finite double-null affine-pair readout, and cofinal tightness / no silent refinement). All four have citable v6 Paper 57 §1 homes. **The internal asymmetry (load-bearing):** the §1.2 weight-counting no-go depends on *none* of these premises — it is the structural half; what rides the Jacobson route is only the *identification* of the missing weight-carrying unit with gravity's `G`. So "the G-*leg of the classification* is conditional" is precise: the wall stands unconditionally, the `G`-naming of it is conditional.

### 1.2 What is walled [NO-GO, modulo gate G1]

**Theorem (scale no-go).** Every intrinsic record functional factors through the record structure `R` and is therefore `g_λ`-invariant (weight `0`); an absolute scale carries weight `≠ 0`; hence no record output fixes `l_step`, `G`, the seal spacing `d`, the metric normalization, or the 4-volume unit. The grading tool (`g_λ` as a one-parameter group; weight an additive homomorphism — v6 Paper 6, Theorem G) is stated and proved in paper 1 §4.4; here it is instantiated on the wall (§3.1 gives the receipt-anchored instance). Gate G1 — *no sealed law consumes the record area outside the continuum labeling map* — is the single named premise, stated at its true strength in paper 1 §4.4 (proved-finite for the executed corpus; not a closure theorem).

**The invariants and the six levers.** The record-scale invariants are `κ_grav·σ_A = 2π` and `G·σ_A = 1/4` — and, distinct from both, the spectral-action ratio `G·Λ_uv²` — **each separately constant under `l → μl` (verified at `μ = 1, 1.7, 3`; receipt `p3_d_nogo.py`, shared with paper 1); linked by the construction, never numerically equal to one another.** All six second-scale levers tried against the wall (Tomita–Takesaki/Connes, Benincasa–Dowker, fluid-gravity, JLMS, shape dynamics, spectral action) collapse onto these weight-`0` invariants — fixing ratios to `l_step`, never an absolute. The dedicated second-scale attack (mass gaps: `ξ = 1.641017929928` exact and Onsager `23.355`, both pure number × spacing; marginal gauge sectors: fixed-point couplings dimensionless, transmuted scales `∝ 1/a`; asymptotic safety: `G = g_*a²` tracks the record spacing) found no independent length: *intrinsic ⇒ weight-`0`* and *length-weighted ⇒ not intrinsic* are mutually exclusive **[NO-GO]**. Consequence: **`G` is measured** — one experimental datum, imported exactly as any coupling is.

## 2. The mode wall

### 2.1 The object and the obstruction [THEOREM on the arena]

**The mode, defined.** A seal mode is a **ledger of orthogonal primitive characters of the parity group `(ℤ/2)ⁿ`**; its rank is `2ⁿ − 1` (`1/3/7` for `n = 1, 2, 3`). The characters are orthonormal (Gram `= I`, sympy-exact), and the rank is preserved by **every record-internal move** — sign-flips, index permutations, the firing map — so distinct ranks are **gauge-inequivalent superselection sectors** (the projective-symmetry hallmark; Wen). What self-consistency *does* fix is the firing form: `∇ψ(h) = e^{−h}` holds identically in every mode and sector.

**The common-zero obstruction.** Selecting a mode cross-sector requires a **common energy zero** across gauge-inequivalent sectors. But each sector measures content only against **its own uniform reference `U_r` of dimension `2ⁿ`** (the `D(P_r‖U_r)` normalization), and re-referencing across sectors shifts content by exactly `(n′ − n)·ln 2` — **state-independent: `H(P)` cancels identically** (sympy-exact). Each sector's zero is therefore free up to an independent additive constant the records never fix: the comparison the selector needs does not exist record-internally **[NO-GO]**.

### 2.2 The three routes fail in unison, and the relocation is decisive [NO-GO]

**Route 1 — completeness / no-silent-refinement, both horns.** If the mode label is *recorded*, completeness pushes toward the maximal ledger — down the **dilution ladder** `η: 0.6094 → 0.4951 → 0.3680` (1/3/7 characters), *away* from any distinguished member. If the label were *silent*, a dissolution template would apply — but the modes are **recorded-distinct**: `D(P_{η_B}‖P_{h_*}) = 0.00479 > 0`. Neither horn selects. **Route 2 — an external structural identity.** The candidate `1/η_B = 1.6410… = ξ` (the 1d mass-gap length) is an algebraic **tautology** — `ξ = 1/η` *is* the seal equation rewritten (symbolic residual `0`) — and the mass-gap length is mode-*covariant* (`ξ(h_*) = 1.2814 ≠ ξ(η_B) = 1.6410`), so "has a mass-gap length" selects nothing; `1/η_B` is already filed weight-`0` record-internal. **Route 3 — a variational principle.** Maximal content selects the *least*-coupled ledger while completeness drives toward the dilution *limit* — opposite ends of the ladder; the firing clock is **mode-blind** (`E[I] = 1` in every mode); and any in-class extremum is circular — the admissibility gate *is* the mode choice (dropping the count-dual base to `μ(+) = 0.2` overshoots the capacity, content `0.526 > W_* = 0.365`). Here `W = η·θ − log cosh η = 0.1561092…` (with `η` the seal root `e^{−η} = tanh η` and `θ` the commitment-cubic root, `θ³ + θ² + θ − 1 = 0`) is the forced constant the crossings below are measured against.

**The relocation, proved decisive.** The sole escape — a mode-selecting matter Hamiltonian — *relocates* rather than closes. Construction: `F_α = C_ext − α·ln(modes + 1)` with `C_ext` the extensive (summed) content — explicitly distinct from §2.1's `D(P_r‖U_r)`; the common-zero core is independent of which is used. The argmin **switches rank at level-crossings** as `α` sweeps: crossings at `α = 0.168 / 0.207 / 0.247`; **no crossing coincides with any forced click-law constant** — the nearest is `W = 0.1561` at distance `0.012` (`η_B = 0.6094` sits `0.36` from its nearest crossing). The switching is **structural**: for *any* strictly increasing multiplicity reward `R(m)`, the argmin of `C − α·R(m)` moves from the least-content rank at small `α` to the largest ledger at large `α`, with crossings at `α_ij = (C_i − C_j)/(R_i − R_j)` — an algebraic fact of the family, not a rigged functional form (the quoted crossing *values* are the log reward's, per the receipt); and **even `α = 0` is convention-dependent** — extensive, intensive, action-like, and binding-energy forms of the `α = 0` functional pick *different* ranks. Conclusion: a mode-selecting Hamiltonian does not break the wall; it **relocates the undetermined choice into its own free coupling** **[NO-GO]**. Between-species mass ratios are thereby **singly gated** — by the rank *assignment* alone (the chirality-bridge and PSG-cohomology gates collapse *into* the assignment: they fix the species *name*, never the gap *value*); the gap values and within-pair ratios the records *do* force are paper 5's. The precise closer: no mass ratio is computable *not because the gap is unknown* (a forced pure number to `100+` digits) but because *which gap belongs to which particle* is the one import. Status: the mode is **import-fixed** by the measured spectra — mode-relative throughout, per the scope guard carried from the matter sector.

### 2.3 The strength axis: `c_m`, complete-and-blind [DERIVED + NO-GO + IMPORT]

**The permitted lane.** `c_m = Gm²/ℏc = (m/M_Planck)²` — the gravitational analog of the fine-structure constant: `c_m(e) = 1.7518 × 10⁻⁴⁵`, `c_m(p) = 5.9061 × 10⁻³⁹` (gravity `∼10⁻⁴³` weaker than electric repulsion for two electrons). A derivation of `c_m` would be a statement about *why gravity is weak* — the hierarchy problem, which is what this block prices. Weight arithmetic: under `l → μl`, `G ∼ l²` (weight `+2`), `m ∼ 1/l` (weight `−1`), `ℏc` weight `0`, so `weight(c_m) = +2 + 2(−1) = 0`; gauge invariance *verified*, `c_m(μl)/c_m(l) = 1` to `< 10⁻⁴⁰` at `μ = 1.7, 5`; equivalently `c_m` is a mass-*ratio* squared — the absolute scale cancels. Contrast class: `σ_A`, `G`, the absolute `d` sit at weight `±2` and are forbidden; `c_m` is the legitimate intrinsic-eligible candidate.

**Complete-and-blind — two results, not conflated.** (a) The **record equivalence principle** (a quotient-functoriality theorem — v6 Paper 7, citable) makes "the" matter ratio *well-defined*: the record footprint is universal. (b) The **blindness proper**: the gravity sector is *complete* with respect to that ratio — no executed gravity-sector identity constrains its value. The clean exhibition: toy values `c_m = 12` and `c_m = 1200` **both pass every executed gravity identity** — `κ_grav·σ_A/2π = 1.000000000000` in all four cells of the inherited scan (the invariants are functions of `G, σ_A` alone) — NOT-SELECTED. The orienting analogy: *the Einstein equations do not determine the electron mass* — a gravity sector that fixed `c_m` would have geometry determine the matter spectrum, which neither GR nor record-gravity does. Two guards travel with this: the **role-identity-across-unit-systems guard** — the same hierarchy invariant reads `m̂ ≈ 0.156` nats on the content axis and `c_m ≈ 10⁻⁴⁵` on the SI-strength axis, bridged only by the unbuilt matter calibration (the axes must not be conflated; the toy floor `C(η_B) = 0.156` bounds nothing physical — physical `c_m(e)` sits ∼44 orders below, and an oriented ledger forfeits the floor anyway); and the **free-κ disambiguation** — `c_m` is the strength-axis analog of the *Tier-A conversion* `κ` (the click-law's free unit), distinct from the gravitational `κ_grav = 8πG` in `κ_grav·σ_A = 2π`.

**The factorization and the shared gate.** `c_m = γ_G·ν_m² = (G·σ_A)·(m²/σ_A) = Gm²` — the record area density `σ_A` **cancels identically** (sympy-exact): `γ_G = G·σ_A = 1/4` is a ratio-of-twins *convention* fixed by the gauge, and the matter content sits entirely in `ν_m = m/√σ_A` (physical `ν_m(e) ≈ 8.4 × 10⁻²³`, unbuilt; the mode-dependence illustration `|η_B − h_*| = 0.114` shows structure, its toy values ∼22 orders above physical). **The precise shared-gate statement:** `c_m` and the spacing `d` share exactly *one* sub-problem — the mode-canonicalization of §2.1–2.2 (a single canonicalization principle would pin both) — while `c_m` additionally carries the matter-sector and hierarchy-selection burdens `d` does not: categorically distinct roles, one common gate.

**The smallness: form derived, magnitude imported.** The record(slow)/substrate(fast) split makes the smallness a **`1/N` mode-count ratio** — structurally a near-isomorph of Sakharov induced gravity, the Dvali species bound, and the matrix slow-fast split, all weight-`0` — but the controlling count `N = 1/c_m ∼ 10³⁸` is itself weight-`0` and **not corpus-forced** (natural record gaps are `O(1)–O(10)`, ∼37 orders short). The mechanism fixes that `c_m` *is* a `1/N` ratio without fixing `N` — exactly one free per-species calibration **[IMPORT]**. **What a derivation would require** (the three-item ladder): a constructed matter sector (the toy gaps `1.641`, `23.355` are not the proton); the mode-canonicalization (the shared gate); a hierarchy-selection principle. The spectral-action candidate fails the ladder: Chamseddine–Connes yields only the weight-`0` invariant `G·Λ_uv²` (scheme-dependent pure number), which fixes the *ratio* `G/l_step²` — not `c_m = Gm²`, which needs a separate `(m/Λ)²` — and rests on an unverified profile identification: a candidate, not a prediction. **The G1-free/G1-dependent claims split (blindness is unconditional):** the blindness result leans only on G1-*free* sub-results — the equivalence-principle theorem plus the algebraic `c_m`-independence of the executed invariants — so it is **independent of any open premise**; it is the weight-`0` classification and the no-record-coefficient closure that rest, like the scale no-go, on gate G1. Anti-numerology guard: the strongest record-number coincidence, `θ_B² = 0.295598` vs Srednicki's area coefficient `a = 0.295417`, is refuted *algebraically* (`θ_B²` is a root of the irreducible cubic `s³ + s² + 3s − 1` — a cubic irrational, distinct from `a`, and on the wrong axis; the full analysis is paper 1 §4.4).

## 3. The classification

### 3.1 The schema, and the SCALE instance [THEOREM]

**Schema.** Let `P` be the self-consistency principle and `G_i` an internal symmetry with `P` `G_i`-invariant. Then every record readout is a `G_i`-invariant — the records carry *only* invariants (the proved inclusion; not every invariant is supplied — `c_m` and `χ_AB` are weight-`0` invariants the records demonstrably do not fix) — and the records are blind to *at least* the orbit parameter, which the same `G_i` moves. **The per-wall `P`, named** (without which the schema floats): for SCALE, `P` = the gravitational equation of state together with the click-law's gauge triviality; for TENSOR, `P` = the transverse moment-positivity `Γ ⪰ 0` (paper 2 §2); for MODE, `P` = the within-sector firing-form fixing (`∇ψ = e^{−h}` in every sector). Three claims, kept distinct: that the schema *holds* is receipt-verified; that it is *novel as a unification* is a positioning claim; that any single wall is novel *in isolation* is disclaimed (§4).

**SCALE.** `G₁ = g_λ`, **defined**: the unit-relabeling automorphism `A_rec ↦ λ·A_rec`, the *identity on the record sector* `R`; lengths `l = A_rec^{1/2}` transform as `l ↦ μ·l` with `μ = λ^{1/2}`, and a weight-`w` quantity as `g_λ(X_w) = μ^w·X_w`. It is a **one-parameter group** — `g_{μ₁} ∘ g_{μ₂} = g_{μ₁μ₂}`, identity `μ = 1`, inverse `1/μ`, sympy-exact — which is what makes weight an additive grading homomorphism (paper 1 §4.4). Paradigm gradings, with the control: a pure number `c₀` has weight `0`; a genuine ratio `l³/l³` weight `0`; the unequal-weight control `l³/l` weight `2` (the grading detects fakes). Instantiated wall weights: `weight(l_step) = +1`, `weight(G) = +2`, `weight(σ_A) = −2`. **The two-Λ distinction (disambiguated once, here):** `weight(G·Λ₀) = 0` with `Λ₀` the weight-`(−2)` **cosmological constant** (an inverse area; `S_dS = π/(G·Λ₀)` fixes only the weight-`0` product) — and, a *distinct weight-`0` object*, `weight(G·Λ_uv²) = 0` with `Λ_uv` the weight-`(−1)` **inverse-length UV/spectral cutoff**. The two `Λ`s are different quantities; every record-scale-invariant statement in §1.2 reads with this split, and the twins (`G·σ_A = 1/4`, `κ_grav·σ_A = 2π`, `G·Λ₀`, `G·Λ_uv²`) are *linked by the construction, never numerically equal to one another*. The `σ_A ↔ G` bijection through `G·σ_A = 1/4` is the same object the everpresent-Λ factorization uses (paper 4 §4.3, its home). Residual: the absolute length — **continuous** `ℝ₊`.

### 3.2 The TENSOR instance [THEOREM]

`G₂ = R`, the complex→real field reduction (qubit-pair ↔ rebit-pair), a `ℤ₂` involution. The invariants: `R` preserves the marginals, the cross-moments, the click-law joint form `p(a, b|x, y) = (1 + ab·E_xy)/4`, and the sealing scalar `I_σ`. What `R` changes: the information-dimension counts — `K_single = 3/4` (`d(d+1)/2` vs `d²`), `K_AB = 10/16` (`D(D+1)/2` vs `D²`) — the local-tomography deficit `+1` over ℝ (`10 ≠ 9`) vs `0` over ℂ; the residual lies in `ker R`, **binary**. The mechanisms, the `Q̃ ∖ Q` permanence, and the contested empirical status live in paper 2 (the intended division: *derived* there, *classified* here). **One honesty note, carried:** the `I_σ` equality across `R` is **true-by-design at the shared Tsirelson point** — both correlators live in the `X–Z` plane `R` preserves — so the load-bearing field-distinguishing computation is the `K`-deficit, never that equality (it is not independent evidence). Cross-check: the envelope receipt, live (`p6`, 43/43, 8 SDP digits flagged solver-tolerance).

### 3.3 The MODE instance [POSITED-STRUCTURAL]

**The honest framing first:** for SCALE and TENSOR *one group does both schema jobs on the same object* — it fixes the invariants *and* moves the named residual. The MODE group does not, and the difference is load-bearing. The group: the cross-sector re-referencing torsor — per-sector additive energy-zeros `c = (c_r) ∈ ℝ³`, composition adding zeros, identity `c = 0`, inverse `−c`, **no distinguished basepoint** (exactly "no common energy zero"). Clause (A) as a derivation: the within-sector invariant — the ledger rank `2ⁿ − 1` — is read off the **parity-character Gram `= I`** (orthonormal characters, sympy-exact), and a symbolic offset `c_r` is *absent from the Gram* (recomputed after applying it). Clause (B): what `G₃` genuinely *moves* is the **continuous** zero `c_r`. **The stabilizer analysis:** sector permutations carry rank-`r` ledgers to rank-`r` ledgers; within-sector sign-flips and index permutations fix the Gram, the rank, *and* the label — so the discrete multiset `{1, 3, 7}` sits in the **stabilizer** of `G₃`. The named residual (the label) is therefore *downstream of* the missing reference — selected *by* it, not itself an orbit coordinate — and identifying the discrete label with the continuous orbit parameter would be a continuous-vs-discrete conflation: the identification is **[POSITED-STRUCTURAL]**, never [THEOREM]. The demotion does not touch §2's no-go (records select neither the rank nor `c_m`); per-clause receipt tags: (A) [THEOREM], (B) [THEOREM] *on the zero*, the label-fixing check [STRUCTURAL].

### 3.4 The roll-up

| Wall | `G_i` | Invariant carried | Residual (type) | Grade |
|---|---|---|---|---|
| SCALE | `g_λ` (1-param group, `ℝ₊`) | the weight-`0` functionals | the absolute length (**continuous** `ℝ₊`) | **[THEOREM]** |
| TENSOR | `R` (`ℤ₂` involution) | the moment algebra `M` | the local-tomography bit (**binary**, in `ker R`) | **[THEOREM]** |
| MODE | re-referencing torsor (`ℝ³`) | within-sector spectrum / rank `2ⁿ − 1` (`G₃`-*fixed*) | which-mass-is-which (**discrete label** `{1,3,7}`; the continuous zero is what moves) | **[POSITED-STRUCTURAL]** |

*Annotations:* `c_m` rides the MODE row, not SCALE's (§2.3); the asymmetry between the rows is stated, not smoothed (one group does both jobs for rows 1–2; not for row 3). Epistemic statuses per leg: SCALE **measured** (the residual an experimental datum — with the *route to naming it `G`* conditional per §1.1); MODE **import-fixed** (by measured spectra — the intermediate case); TENSOR **contested convention** (possibly a residual *the world itself may not carry* — paper 2 §6). There is no clean "three-experiment-fixed" symmetry, and the series does not claim one. **The moduli reading:** the records compute on the quotient `R/G_i`; the residual is the fibre coordinate the quotient discards — self-consistency fixes the fence (the orbit) and never the filling (the position on it), the same split paper 2 proves transversely, here the *shape of all three walls at once*.

## 4. Honest scope, convergences, and imports

**The orthogonal-axes guard.** `G` is a *scale* (a weight-`+1`-direction grading obstruction — `l_step` at `+1`, `G` at `+2`); the masses are a *spectrum anchor* (a mode); the tensor product is a *field/composition* bit (in `ker R`). Conflating "all three are imports" with "all three are the same epistemic object" is exactly the error the grading corrects. **The three convergent lineages, by name:** `G`-underivability is the Jacobson/Verlinde-tradition conclusion; local-tomography-as-the-least-motivated-selector is the Hardy / Chiribella–D'Ariano–Perinotti / Masanes–Müller / Barnum–Müller–Ududec / Höhn lineage; masses-are-measured is Standard-Model orthodoxy (`∼19+` measured parameters). Each wall is convergent-not-novel in isolation; the owned content is the **unification** (one quotient shape, three realizations), the **grading** (two theorems + one structural analogy; measured / import-fixed / contested), and the **placement of `c_m`** on the mode axis. **What is not owned (the imports list):** MIP\* = RE (Ji–Natarajan–Vidick–Wright–Yuen) and Slofstra — the `Q̃ ∖ Q` permanence rides them verbatim; the reconstruction lineage above (the `R`-instance is a re-localization, not the origin); the entropic-`G` tradition with its critiques — noting the attribution datum that Visser's and Kobakhidze's critiques target *Verlinde*, leaving the Jacobson route untouched (Visser explicitly so) (A. Kobakhidze, PRD **83**, 021502 (2011), *Gravity is not an entropic force* — not to be confused with S. Gao's similarly-aimed *Is gravity an entropic force?*, Entropy **13**, 936 (2011)), so the G-leg's conditionality is underwritten by Padmanabhan and Eling–Guedens–Jacobson, not by them; Tsirelson/NPA (paper 2); the 2026 real-vs-complex constructions (paper 2). Non-claims carried from the sources: no PSG cohomology group `H²(SG, IGG)` is derived (the §2.1 map is to PSG-*protection*, phenomenological); ledger-chirality ↔ spinor-chirality is unproved (paper 5's frontier); no `c_m` value is produced (any number would smuggle the `1/4` convention plus a mode choice — the illustrative `γ_G·η_B² = 0.093` is ∼44 orders off, and says so); the hierarchy problem is *relocated into one free count*, not solved.

## 5. Receipts

All in `code/`, re-run green: `p17_classification.py` (23/23 — the three triples with SCALE/TENSOR as genuine single-group quotients and MODE's label-fixing explicit; the two-Λ instance weights; the field-blindness cross-check `|ΔI_σ| = 0` to `< 10⁻¹³⁰`; **purely algebraic — no SDP, no solver-tolerance digit, no float64 anywhere**), `p15c_weight_classification_nogo.py` (27/27 — the `g_λ` grading; `σ_A` weight `−2` ↔ `G` weight `+2` via `G·σ_A = 1/4`), `s_mode_import_wall.py` (16/16 — 2 sympy-exact / 11 mpmath `dps = 120` / 3 structural; anchors `η_B = 0.6093778634360062` (residual `9.2 × 10⁻¹²⁸`), `W = 0.1561092001572401`; the re-referencing shift, the `F_α` crossings, the rank invariance), `m2_mode_canonicalization.py` (10/10, `dps = 100` — the dilution ladder, the tautology residual `0`, the `ξ` mode-covariance `1.2814 ≠ 1.6410`, the recorded-distinctness `D = 0.00479`, the skewed-base overshoot `0.526`, the ranks `[1,3,7]`), `p5_cm_calibration.py` (10 — CODATA-2018 at `dps ≥ 60`, sympy-exact where structural: the invariants' symbolic `c_m`-independence, the `γ_G·ν_m²` factorization with `σ_A` cancelling, the weight verification; the four-cell `12`/`1200` NOT-SELECTED scan itself is v6 Paper 6 §9's, citable), the v6 Paper 57 / paper-XI scale receipts (the record-scale invariants and the six-lever collapse; citable), and `p2a`/`f4` (the weight bookkeeping shared with paper 1). Envelope cross-check where cited: `p6` at **43/43** (8 [SOLVER-TOL]).

## References

**This series.** Paper 1 (the grading tool §1.2/§3.1 instantiates; gate G1 stated at strength; the free conversion `κ` this wall protects); Paper 2 (the TENSOR mechanisms, the contested-bit status, the `Q̃ ∖ Q` machinery); Paper 4 (the equation-of-state use; the `σ_A ↔ G` bijection's everpresent home); Paper 5 (the gap values and ratios the mode wall gates; the chirality no-gos). Ledger: `LEDGER.md`.

**Frozen log.** v7 Papers 5, 8 (the wall half), 14 §3, 17 — fully superseded here.

**v6 (kept, citable).** Paper 6 (Theorem G; gate G1; the four-cell toy-value scan), Paper 7 (the record equivalence principle, Thm 6.1; the `W`-theorem home), Paper 9/14 (the constructive matter homes §2.2 points away from), Paper 57 and publishable paper-XI (the scale no-go, the invariants, the §1.1 premises).

**External.** T. Jacobson, PRL **75**, 1260 (1995); T. Padmanabhan, Rep. Prog. Phys. **73**, 046901 (2010); C. Eling, R. Guedens, T. Jacobson, PRL **96**, 121301 (2006); E. Verlinde, JHEP **04**, 029 (2011); M. Visser, JHEP **10**, 140 (2011), arXiv:1108.5240 — the critique that exempts Jacobson; R. M. Wald, PRD **48**, R3427 (1993). X.-G. Wen, PRB **65**, 165113 (2002) — the superselection/PSG hallmark. A. D. Sakharov, Sov. Phys. Dokl. **12**, 1040 (1968); G. Dvali, Fortschr. Phys. **58**, 528 (2010) — the `1/N` form isomorphs. M. Srednicki, PRL **71**, 666 (1993); A. H. Chamseddine, A. Connes, Commun. Math. Phys. **186**, 731 (1997). W. Slofstra, Forum Math. Pi **7**, e1 (2019); Z. Ji et al., arXiv:2001.04383. L. Hardy, quant-ph/0101012; G. Chiribella, G. M. D'Ariano, P. Perinotti, PRA **84**, 012311 (2011); L. Masanes, M. Müller, New J. Phys. **13**, 063001 (2011); H. Barnum, M. Müller, C. Ududec, New J. Phys. **16**, 123029 (2014); P. Höhn, Quantum **1**, 38 (2017) — the reconstruction lineage.
