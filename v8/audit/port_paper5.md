# V8 self-containment audit — destination paper 5 (matter)

**Sources swept section-by-section:** v7 Paper 9 (`relativistic-isp-v7-paper9-chiral-gap-and-chirality-nogos.md`), v7 Paper 14 (`relativistic-isp-v7-paper14-matter-sector-gap-mechanism-mode-wall.md`, **constructive half only** — the GW flow §§1–2 + the values/ratios sharpening §4; the §3 mode no-go is destination paper 3), v7 Paper 18 (`relativistic-isp-v7-paper18-chiral-gap-global-optimality-lemma.md`).
**Target:** `../relativistic-isp-v8-paper5-matter.md` (41 lines, ~10.5 KB; read 2026-07-01).
**Rule applied:** v8 must be the *better, self-contained* version — v7 goes log-only (nothing load-bearing may live only there), v6 stays citable, and self-containment does **not** mean compression. Corrections respected (LEDGER correction #6 + audit brief): `[L*]` in **margin form** `(Lμ)`; surrogate ratios relabeled (true `n = 4 ≈ 5.1`); two-level closed **modulo the `n ≤ 20`-verified step**; corrected multiset parenthetical; `0.235/0.2436` reconciled; count-vs-index + `γ₅` check; scope large-`N`/2d.

## Overall verdict

The v8 paper is a **correct-in-outline digest at the right grades** — every headline result, every corrected label, and the honest scoping survive — but it is exactly what the objective forbids: **compressed**. It carries the *statements* of the three sources while the load-bearing constructions and proofs remain only in the frozen log:

1. **The extremal problem is not defined in-paper.** No characters `χ_a`, no `T_ε(s) = Σ_{a≠0} ε_a χ_a(s)`, no per-mode seal fixed point `E_P[ε_a χ_a] = e^{−h_a}`, no gap definition `m̂(ε) = D(P*‖U)`, no general definition of the seal mean `mT = E_{P*}[T] = Σ_a e^{−h*_a}` — so the paper's *named open problem* `(Lμ)` is stated over undefined symbols (v8 Paper 1 has the unoriented seal calculus; the oriented extension exists nowhere in v8).
2. **The two §4 no-gos have no argument and no receipt.** The `K̂ ⋊ Aut(K)` construction, the `168 = GL(3,2)` computation, the split-extension/no-cocycle obstruction, `H²(SG, IGG)`, and the whole parity-odd-on-parity-symmetric-data bridge argument are absent; `p9b`/`p9c` are missing from §6.
3. **The GW flow is asserted, not built.** No `D(M) = D + M(1 − D/2)`, no `γ̂₅`, no gap equation `M = g²Σ(M)`, no residuals/thresholds, no O8-closure statement, and **zero v6 citations** (the record GW operator, the index theorem, and the quenched prior art are v6 Papers 14/22 — citable and uncited).
4. **One factual bug:** §1 says achievable-multiset counts `2/4/14/156` at `n = 1…4` — the correct range is **`n = 2…5`** (paper 18 §3.2, receipt `pD2_achievability.py` P1).

All seven checked corrections are carried (details in the checklist below); the bug in (4) is a *fresh regression in exactly the corrected-parenthetical class*. Required: expand §§1–4 with the ports below (roughly: §1 setup + proofs, §2 formulas + witnesses, §3 construction, §4 the two no-go arguments), fix the bug, add 5 receipts and ~9 references.

---

## Port table

Class key: **KEEP-FULL** = must be ported (not adequately in v8); **KEEP-RESULT** = statement + receipt adequately in v8, no action beyond noted nits; **DROP** = superseded / process / other destination, with reason.

### A. v7 Paper 9 — chiral-gap law + orientation group + two chirality no-gos

| # | Source | Content (one line) | Class | Receipts | v8 destination / status |
|---|--------|--------------------|-------|----------|--------------------------|
| A1 | Status/tags | v7 process metadata, tag glossary, dps ledger | **DROP** | — | v8 has its own status block and tag system (paper-1 tags + `[SUB-THEOREM]/[REDUCTION]/[CORROBORATION]`) |
| A2 | Abstract | Three-prerequisite narrative (Paper VIII §8 list settled) | **DROP** | — | v7-internal history; the surviving substance is redistributed to A4–A8. v8's own abstract already carries the corrected grades |
| A3 | §1 | Intro: the §8(a)(b)(c) list, "sharpened boundary" framing, single-threaded discipline note | **DROP** | — | v7 narrative. One clause worth keeping (see A8): of Paper IX's three named opens, the interacting-flow item is *closed by this v8 paper's §3* — v8 never says so explicitly |
| A4 | §2 | **The closed gap law's physics** (expected big item — PARTIAL in v8): the oriented branch drives the gap *below the unoriented floor* `W = η·θ − log cosh η = 0.156109200157240` for all `n ≥ 3`; the two-branch verdict "protection broken ⇒ gapped at `W`; protection exact ⇒ asymptotically gapless"; the **three load-bearing properties** — strictly positive at every finite `n` (`δ_n` never reaches the leading term), vanishing **only** as `n → ∞` (asymptotically gapless, *never* finite-`n` gapless), prefactor exactly 1 (`1.068, 1.033, 1.016, …, 1.0000000149` at `n = 25`); brute-force base counts `8 / 128 / 32768` at `n = 2/3/4`; the `n = 5` corpus sampled-bound-coincides-with-closed-form confirmation | **KEEP-FULL** | `p9a_chiral_gap_closed.py` | §1 — v8 has the closed form + `n = 2…5` anchors + `m̂·2ⁿ → 1` but **none of the physics**: no `W`, no two-branch statement, no "never finite-`n` gapless", no exhaustion counts. Port the two-value-collapse *derivation* via C5–C6 (paper 18's sharper Walsh version is the proof of record); port the `W`-floor/two-branch/three-properties paragraph from here. Cite v6 Paper 7 for `W` (and note v8 Paper 1 §"three-object" carries `C(η_B) = 0.156109…` — cross-ref, don't re-derive) |
| A5 | §3 | **The orientation-class group** (expected big item — MISSING): `K̂ = Hom(K, ℤ₂)` (orientation classes = characters of the relation code, v6 Paper 8 Thm 5.1, citable), `Aut(K)` = position-permutations preserving `K`, `K^⊥` spin-flip gauge redundancy; the assembled `K̂ ⋊ Aut(K)` with `Aut(K)` acting by a well-defined permutation representation (verified on the five smallest ledgers); the exact signature `|Aut(K)| = 168 = |GL(3,2)| = (2³−1)(2³−2)(2³−4)` reproducing the `1 + 7 + 7 + 1` orientation-class multiplet | **KEEP-FULL** | `p9b_psg_ingredient_functor.py` (finite-group-exact) | §4 — v8 carries only the string "the ingredient group `168 = GL(3,2)` exists". The object itself (what group, of what, acting how) must be in-paper; `K`/`K̂` may lean on a v6 Paper 8 citation. Receipt currently absent from §6 |
| A6 | §4 | **No-Wen-PSG-without-geometry no-go** (expected big item — MISSING): Wen's PSG = extension `1 → IGG → PSG → SG → 1` with physical content in `H²(SG, IGG)`, `SG` a **space group**; the two-bullet obstruction — (i) `Aut(K)` is a finite permutation group (no translations, no lattice momentum, no projective spatial phases), (ii) the assembled product is split by construction and *nothing in the record data can force a nontrivial cocycle* (orientation data strictly `ℤ₂`-valued, honest permutations, no `U(1)`-valued 2-cocycle from `K` alone); a genuine 2-cocycle needs `SG` needs emergent geometry = the `l_step` wall; so the functor delivers PSG *ingredients*, not Wen's group — "the matter-sector face of the scale wall" | **KEEP-FULL** | `p9b` | §4 — v8 states the verdict ("no Wen-style projective construction without geometry **[NO-GO]**") with zero argument. Port the extension/cohomology localization + the two bullets; keep the ingredient/group distinction verbatim in spirit |
| A7 | §5 | **Chirality-bridge no-go construction** (expected big item — MISSING): two layers — orientation `σ : K → ℤ₂` on the ledger (dim `K` bits) vs Weyl handedness `m` on the `SO(3)` rotation/dilation fiber (one bit, v6 Paper 9); **handedness-blindness** (gap, code, orientation group all functions of `σ` alone — `m` never enters); parity as *joint* symmetry `P : (σ, m) ↦ (σ̄, −m)` with the gap parity-invariant `m̂(σ) = m̂(σ̄)` (automatic: `σ̄` realizes `s → −s`, a measure-preserving bijection under which `D(P‖U)` is invariant); ⇒ a bridge `f : σ ↦ m` would be parity-**odd** on parity-**symmetric** data, hence undetermined — choosing it *is* the coupling `g` (an `(M)`-sector input, v6 Paper 9 §9); consistency: self-mirror (achiral) orientations carry `m = 0` (matching the vanishing Weyl seed), handedness magnitude `g`-dependent (vanishes at `g = 0`) | **KEEP-FULL** | `p9c_chirality_bridge_nogo.py` (exact) | §4 — v8 says only "the chirality bridge is a no-go". The argument is short, structural, and the *reason* the mode gate is single (B9); port it whole. Receipt currently absent from §6 |
| A8 | §6 | What-this-leaves ledger: two prerequisites became impossibilities; the strictly-smaller open set (Wen-PSG cohomology → geometry-gated; mode Hamiltonian → fifth wall; **interacting GW flow → v6 Paper 14 O8**) | KEEP-RESULT | — | §§3–5 — substantially present, with one gap: v8 should state that the third open item (the interacting flow / O8) **is closed by its own §3** (see B3). One clause |
| A9 | §7 | Claims / non-claims: no mass or mass ratio produced or predicted; `K̂ ⋊ Aut(K)` is *not* claimed to be Wen's PSG; global-optimality lemma not proved all-`n`; frameworks (PSG, chiral-fermion no-gos, frustrated-Ising mechanism) external | KEEP-RESULT (one add) | — | §5 — mostly carried ("nothing here claims the full theorem", "the proton is far"). Add the explicit **"no particle mass or mass ratio is produced or predicted"** sentence — it is the paper's sharpest non-claim and currently only implicit |
| A10 | §8 | Precision specifics: two-value identity exact `n = 2…7`; closed form vs corpus anchors `< 10⁻¹¹`; `δ_n` magnitudes (`~4×10⁻⁷` at `n = 3`, `~3×10⁻¹¹⁵` at `n = 6`); prefactor to `10⁻⁹` by `n = 25`; exhaustion counts; finite-group-exact `p9b`; exact `p9c` | **KEEP-FULL** (small) | `p9a`, `p9b`, `p9c` | §6 — fold into the receipt lines as the A4/A5/A7 ports land (the `δ_n` figures also arrive via C20) |
| A11 | Refs | v6 Papers 7 (`W`), 8 (`K̂` Thm 5.1, `GL(3,2)` multiplet), 9 (Weyl seed, fiber handedness, coupling `g`), 14 (GW operator, index theorem, anomaly filter, O8); external Wen 2002 + Wen 2013 (SPT/anomaly), Levin–Wen, Nielsen–Ninomiya, Ginsparg–Wilson, Griffiths + Toulouse (frustration mechanism of the oriented collapse) | **KEEP-FULL** (refs) | — | References — v8 currently cites **zero v6 papers**; of the externals it has Wen 2002 + GW but lacks Nielsen–Ninomiya, Griffiths/Toulouse, Wen 2013, Levin–Wen (the last two optional if the anomaly filter is only cited, not restated) |

### B. v7 Paper 14 — matter sector (constructive half + values/ratios; §3 belongs to paper 3)

| # | Source | Content (one line) | Class | Receipts | v8 destination / status |
|---|--------|--------------------|-------|----------|--------------------------|
| B1 | Status/tags | v7 process metadata; **plus** the precision-split declaration: structural/algebraic identities mpmath `dps ≥ 120`/sympy-exact, lattice eigenvalue spectra and gap-equation traces float64/extended, *every such digit flagged lattice-numeric* | **KEEP-FULL** (the split only; small) | — | Status block or §6 — v8's status line declares only the mpmath side; the float64-flagged lattice-numeric honesty split is part of the program's precision discipline and must be stated where the `M(g)` digits are quoted |
| B2 | Scope block | Four hard guards: (1) large-`N` Gross–Neveu *dynamical fermion mass*, **not** the Clay/pure-gauge YM gap; (2) large-`N` controlled, finite-`N` open; (3) **2d, quenched / uniform-flux background, small lattices `≤ 10×10`** (within the v6 Paper 14 ceiling); (4) mass mode-relative, no absolute number | KEEP-RESULT (two small adds) | — | Abstract + §3 + §5 — carried ("large-`N`, `d = 2`, quenched-class — not the Clay problem; mode-relative"; finite-`N` in §5). Missing details: the `≤ 10×10` lattice ceiling and the *uniform-flux, non-dynamical gauge background* specification — restore both (they bound the claim) |
| B3 | §1 | **What is owned and what is new** (MISSING): the v6 Paper 14 recall — exact record GW (overlap) operator `D = 1 + γ₅ sign(H)`, `H = γ₅ D_W` (GW identity residual `4.0×10⁻¹⁵`, gauge-covariant, exponentially local, single species — Nielsen–Ninomiya evaded record-natively, 1 zero vs the naive 4), lattice index theorem `index(D) = Q` exact 14/14 flux sectors, anomaly filter (`index = qQ`, minimal chiral stack `{1,4,4|2,2,5}`); **the O8 remainder named and closed here**; the exact novelty over v6 Paper 22 (quenched condensate *signal* → real self-consistent interaction + proved Lüscher survival + `M(g)` mapped) | **KEEP-FULL** | — (v6 receipts; cite) | §3 opening — v8 says "the record Ginsparg–Wilson flow" without ever defining the operator, citing v6 Paper 14/22, or saying **"closes v6 Paper 14's O8 remainder"**. Since v6 is citable this is a *recall + citation* paragraph, not a re-derivation — but it must exist, or §3's subject is undefined and its novelty unstated |
| B4 | §2.1 | **Lüscher survival** (expected big item — MISSING): mass the Lüscher-correct way `D(M) = D + M(1 − D/2)`, deformed generator `γ̂₅ = γ₅(1 − D)`; sympy-exact backbone (`{γ₅, D} − Dγ₅D = 0`; Lüscher Ward `Dγ̂₅ + γ̂₅†D = 0` at `M = 0`; massive breaking `= M(γ₅ − s)`); residuals `< 10⁻⁹⁰`–`10⁻¹²¹` in all three flux sectors `Q = 0, 1, 2` (scaled-Newton matrix sign, `‖s² − 1‖ < 10⁻¹²¹`; naive eig also `< 10⁻⁹⁰` at dps 120); breaking **strictly `O(M)`** with `M`-independent `C = ‖γ₅ − s‖ = 0.507/0.582/0.648` — soft, **no hard doubler** | **KEEP-FULL** | `s_igw_production_gap_flow.py` (9/9) | §3 — v8 has the one clause "preserves the exact Lüscher chiral symmetry". The deformation, the generator, the three identities, and the soft-breaking/no-doubler quantification are the *proof content* of that clause |
| B5 | §2.2 | **The gap equation** (expected big item — PARTIAL): `M = g²·Σ(M)`, `Σ(M) = (1/V)·Tr[(1 − D/2)·D(M)⁻¹]` (root at `dps ≥ 110`; trace lattice-numeric/float64-flagged); `M(g) = 0` identically below a sector-dependent `g_c` (`M(g² = 10⁻⁴) = 0` exactly — no anomaly-free-lunch), monotone above (`Q = 0`: `0.297, 0.762, 1.385, 1.907` at `g² = 3, 4, 6, 8`); anchors `M(g² = 4) = 0.762447/0.885544/0.786694` for `Q = 0/1/2`; threshold trend `g_c² = 3.63 → 2.79 → 2.35` for `L = 6, 8, 10` (the expected 2d continuum trend toward breaking at any `g > 0`) | **KEEP-FULL** | `s_igw_production_gap_flow.py` | §3 — v8 quotes the rounded anchors `0.762/0.886/0.787` and `M = 0` at `g = 0` but **never writes the gap equation** it claims to solve, nor the threshold structure/trend that makes "interaction-driven, not free/Wilson-remnant" a checked statement |
| B6 | §2.3 | **Topology in the order parameter** (PARTIAL): the divergence law `M·V·Σ(M) → n_zero` as `M → 0` (residual `~10⁻⁵⁴`), `n_zero` the total **count** `n₊ + n₋ = 2/1/2`; the **index** is the signed part `n₋ − n₊ = Q = 0/1/2`, equal to the count only in mono-chiral sectors; `Q = 0` an accidental vector-like pair (count 2 > index 0), exactly as v6 Paper 14 records; **explicitly *not* the continuum Banks–Casher `ρ(0)`** (which excludes zero modes) — it is the finite-volume topological zero-mode term in a fixed sector; the zero-snap is the index theorem, a precision device (unsnapped trace already gives `n_zero` to eight digits at `M = 10⁻⁶`) | **KEEP-FULL** | `s_igw_production_gap_flow.py` | §3 — v8 carries the corrected count-vs-index split with the `γ₅` check (good — see corrections checklist) but not the divergence law it lives in, not the Banks–Casher disclaimer (a correctness guard against re-importing the original error), and not the snap-is-not-the-source note |
| B7 | §3 (§3.1–3.3) | Mode wall: rank superselection invariant, common-zero obstruction (`(n′−n)ln 2` state-independent), relocation theorem (crossings `0.168/0.207/0.247` vs `W = 0.1561` at `0.012`; every monotone reward) | **DROP here — destination paper 3** | `s_mode_import_wall.py` (16/16) | **Verified ported**: v8 Paper 3 §2 carries the full no-go (relocation, crossings, rank invariance, sympy-exact shift) and §6 the receipt. Cross-refs are consistent both ways (paper 3 §2: "the gap values and within-pair ratios … live in paper 5"; paper 5 §4 gates on paper 3) |
| B8 | §3.4 | The import family: three last inputs (scale / `χ_AB` / mode), weight classes, three epistemic statuses | **DROP here — destination paper 3** | — | **Verified ported**: v8 Paper 3 abstract + §3 carry the classification in its corrected three-statuses form (measured / contested convention / import-fixed) |
| B9 | §4 | **Values forced, ratios singly gated** (PARTIAL): gap values pure numbers in nats, functions of `n` alone, free of all three gates (`l_step` dimensionless; `χ_AB` single-party KL; handedness-blind *in value* — the bridge enters only the species *name*), 100+ digits; prefactor sequence decreasing to 1 and adjacent ratios decreasing to 2 (`2.0690, 2.0328, …`); within-pair ratios forced; between-species ratio gated by the **rank assignment alone** — and the assignment is load-bearing because the menu is `n`-dependent, **spread `0.067`** (it changes the number); the doubly→singly collapse *mechanism*: the chirality bridge and the Wen-PSG cohomology both fix the species *name*, not the gap *value*, so they collapse into the assignment; the only assignment-free numbers are the limits (`ratio → 2`, prefactor `→ 1`) | **KEEP-FULL** (the missing halves) | `s_chiral_gap_ratios.py` (14/14, dps 140) | §4 — v8 has the core (pure to 100+ digits; free of the three gates; singly gated; two routes closed). Missing: the **`0.067` menu spread** (the quantitative reason the assignment is load-bearing), the name-vs-value collapse mechanism (which is *why* the gate is single — pairs with A7), and the "assignment-free numbers are limits only" sharpening |
| B10 | §5 | The matter-sector verdict ("own the gap mechanism, import the mode-selector"; why no physical mass ratio is computable *now*, stated precisely) | KEEP-RESULT | — | Abstract "Net:" + §5 — carried in substance. Optional: the one-sentence "not because the gap is unknown … but because *which* gap belongs to *which* particle is the one import" formulation is sharper than v8's and worth lifting verbatim |
| B11 | §6 | Open residues: finite `N`; 2d/quenched (4d, unquenched, confinement untouched); the Clay gap explicitly not addressed; the mode import (with the cross-wall epistemic comparison) | KEEP-RESULT | — | §5 — items 1–3 carried; item 4's epistemic comparison is paper 3's (verified there). No action |
| B12 | Reproducibility | Per-receipt check ledgers: `s_igw` 9/9 (backbone, residuals, gap equation, topology split), `s_mode_import_wall` 16/16 (2 sympy / 11 mpmath / 3 structural), `s_chiral_gap_ratios` 14/14 (dps 140; `δ_n > 0` structural with the `n ≥ 7` underflow honestly flagged, strict positivity verified `n ≤ 6`) | KEEP-RESULT (one add) | all three | §6 — receipts listed with counts. Add the **`δ_n` positivity flag** (structural for all `n`, verified `n ≤ 6`, underflow `~10⁻¹³⁰` at `n ≥ 7`) — it qualifies a `100+`-digit claim and lands with C20 |
| B13 | Refs | v6 Paper 14 (operator/index/filter/O8), v6 Paper 22 (prior art), Neuberger PLB **417**, 141 (1998), Nielsen–Ninomiya NPB **185**, 20 (1981); GW/Lüscher/Gross–Neveu/Banks–Casher/Wen already in v8 | **KEEP-FULL** (refs) | — | References — the two v6 citations are mandatory (B3); Neuberger belongs with the overlap operator; Nielsen–Ninomiya with the single-species/no-doubler claim (B4) |

### C. v7 Paper 18 — global-optimality residue (structural core, certificate, hunt)

| # | Source | Content (one line) | Class | Receipts | v8 destination / status |
|---|--------|--------------------|-------|----------|--------------------------|
| C1 | Status | `PARTIAL_REDUCTION` grade + tag definitions + receipt roster | KEEP-RESULT | — | v8 abstract carries the equivalent grade ("[THEOREM at `n ≤ 4`; all-`n` modulo one named inequality]") and defines `[SUB-THEOREM]/[REDUCTION]/[CORROBORATION]` — no action |
| C2 | Guard 2 | **Scope correction**: gap = pure `T`-multiset functional **only `n ≤ 4`**; at `n ≥ 5` the seal tilts over the full `M`-vector, `P*` not constant on `T`-level sets; witness: same multiset (`minT = −7`, values `{−7, +1, +9}`), gaps `0.7960484195…` vs `1.1251170042…`; searches must run at orientation level | KEEP-RESULT (present §1) | `pD` checks D, D2 | §1 — carried with the witness gaps `0.796` vs `1.125` and the orientation-level consequence. Optional: quote the witness multiset values (use the frozen v7 corrected parenthetical `{−7, +1, +9}`) |
| C3 | Guard 3 | **Surrogate caveat**: scalar-tilt certificate exact on two-level, **overshoots** the vector-seal pushforward on multi-level (`2.23` vs true `0.328` at `n = 4`; all 10 multi-level cases) ⇒ not a lower bound at `n ≥ 5`; the `n ≤ 4` theorem is by **exhaustion**, not the certificate chain | KEEP-RESULT (one small add) | `pD2_minimization_proof.py` | §§1–2 — carried in corrected compressed form ("exhaustion closes the law"; the surrogate "overshoots … and is used nowhere load-bearing"). Add the overshoot witness figures (`2.23` vs `0.328`, 10/10 cases) — one parenthetical that makes "overshoots" checkable |
| C4 | Guard 4 | Precision split: float64 vector-seal solver for the combinatorial *landscape* (convexity-guarded, degeneracy cap, non-spurious near the minimum); every asserted 30+-digit *value*, witness, and margin mpmath `dps ≥ 120` (set 140) | **KEEP-FULL** (small) | — | §6 — missing; pairs with B1 as the paper's precision-discipline sentence ("a search gets double precision; an asserted value gets mpmath") |
| C5 | §2 | **The setup** (BIG — MISSING; the paper's self-containment floor): states `s ∈ {±1}ⁿ`, `N = 2ⁿ`, characters `χ_a(s) = ∏_{i∈a} s_i` orthogonal under `U`; orientations `ε_a ∈ {±1}` (`2^(2ⁿ−1)` classes), alternating-by-weight `ε_a = (−1)^{|a|−1}`; the tilted seal `P(s) ∝ exp(Σ_a h_a ε_a χ_a(s))` with per-mode fixed point `E_P[ε_a χ_a] = e^{−h_a}` = the unique stationary point of the strictly convex `G(h) = ψ(h) + Σ_a e^{−h_a}` (`∇²G ≻ 0`); the gap `m̂(ε) = D(P*‖U)` in nats; the Walsh frame: `T_ε(s) = Σ_{a≠0} ε_a χ_a(s)` is exactly the function with WH coefficients `(0, ε_1, …, ε_M)` — zero mean, all nonzero coefficients modulus 1; `μ*` = the delta case `T = 1 − N·𝟙_{s*}`, `ε_a = −χ_a(s*)` = alternating up to spin-flip; the two-value collapse → scalar seal → closed form | **KEEP-FULL** (BIG) | `pD` (A–E) | **New §1 opening block.** v8's §1 compresses this to two sentences with `T_ε` and `μ*` undefined; `(Lμ)` (§2) then rides on `mT`, `h*_a`, `I_T` — none defined. v8 Paper 1's seal calculus is the unoriented ψ′-balance; the oriented per-mode extension exists nowhere in v8. This block is the port's highest priority |
| C6 | §3.1(A) | Two-value identity + one-line proof (`−(∏(1−s_i) − 1)` = binomial expansion minus empty term; `∏(1−s_i) = 2ⁿ` on all-minus else 0) + **self-propagation** (`(1 − s_{n+1})`-factor recursion: the minimizer's *structure* carries inductively even where its *optimality* does not); exact `n = 2…7` | **KEEP-FULL** | `pD` check A (sympy) | §1 — missing entirely; three lines of proof that make the closed form a derivation instead of an assertion |
| C7 | §3.1(B) | Moment hierarchy identities: `Σ_s T = 0`, `Σ_s T² = N(N−1)` (orthogonality + `ε_a² = 1`), values odd integers in `[−(N−1), N−1]`, never 0; `Σ_s Tᵏ = N·(signed k-XOR-tuple count)` (`Σ T³ = 6N·S₃` Fano-signed; `Σ T⁴ = N(3M² − 2M + S₄)` with the diagonal `ε`-independent); `μ*` saturates the first two — the **maximally negatively-concentrated** achievable multiset | **KEEP-FULL** | `pD` check B, `pD2_achievability` P2 (sympy) | §1 — v8 has the two words "the moment hierarchy". The two displayed identities + odd-valuedness are used by C11 and C14 and must be in-paper |
| C8 | §3.1(C) | Spin-flip invariance: `(R_g ε)_a = ε_a χ_a(g)` acts by the measure-preserving `s ↦ g·s`, so `m̂` is an **orbit** functional — licenses gauge-fixing (`ε_{e_i} = +1`, kills `2ⁿ`), while the orbit is *finer* than the multiset at `n ≥ 5` (so deduplication is unsound) | **KEEP-FULL** (small) | `pD` check C | §1 — missing; one sentence that makes the search protocol (and A7's parity-invariance, same mechanism) sound |
| C9 | §3.1(D) | The engine: data-processing `m̂(ε) ≥ KL_{1D}(ε) := D(law_{P*}(T)‖law_U(T))` for all `ε, n`, with **equality on the `μ*` orbit** (and exhaustively every orbit `n ≤ 4`, max spread `< 10⁻⁹`; DPI verified over `2×10⁴` random `n = 5` orientations); reduction logic: optimality ⇔ `KL_{1D}(ε) ≥ KL_{1D}(μ*)` over achievable multisets, searches at orientation level | **KEEP-FULL** | `pD` checks D, D2 | §1 — v8 carries the *consequence* (multiset-functional scope) but not the inequality-plus-equality-on-`μ*` pair, which is the stated logic of the whole reduction |
| C10 | §3.1(E) | Closed form on `μ*` (scalar two-value seal) | KEEP-RESULT | `pD` check E (dps 140) | §1 + abstract — present with anchors; see C20 for the table/`δ_n` |
| C11 | §3.2 | **Achievability characterization** (BUG in v8): achievable multisets = value-histograms of `±1`-Walsh-spectrum functions mod spin-flip; gauge-fixed enumeration `2^(2ⁿ−1) → 2^(2ⁿ−1−n)`; counts `2, 4, 14, 156` at **`n = 2, 3, 4, 5`**; spectral/rank test **strictly stronger** than moments — explicit witness `(−5,−3,−1,1,1,1,3,3)` passes `Σ T = 0`, `Σ T² = N(N−1)`, odd-valued, yet non-achievable; `μ*` unique `D_{1D}`-minimizer over the achievable set `n = 2…5` (to `> 60` digits); no multi-level multiset reaches `minT = −(N−1)` | **KEEP-FULL** (+ **FIX**) | `pD2_achievability.py` (10/10) | §1 — present-compressed but with the **wrong range `n = 1…4`** (must be `n = 2…5`; at `n = 1` there is exactly one multiset `{−1, +1}`, not 2, and `156` is the `n = 5` count contradicted by v8's own `n ≤ 4` exhaustion story). Also add the explicit non-achievable witness multiset — "is exhibited" currently points at nothing |
| C12 | §3.3 | **Unique-deepest-`minT` lemma** + its one-line proof: `T(s*) = −(N−1)` forces `ε_a χ_a(s*) = −1` for every mask ⇒ `ε_a = −χ_a(s*)` ⇒ the `μ*` orbit — `μ*` alone at the bottom of the concentration order, all `n` | **KEEP-FULL** (small) | `pD` B2, `pD2_achievability` P3c | §1 — statement present, proof absent; it is one line and it is the isolation lever |
| C13 | §3.4 | **Entropy reformulation**: `D_{1D} = ln N − H(P*)` ⇒ min gap = max seal-tilted entropy; on `μ*`, `H(P*) = ln(N−1) + δ_n`, giving the closed form; companions: scalar-tilt closed form (R1, the surrogate, exact on two-level) and **the seal forbids the uniform law** (`E_U[T] = 0` vs seal `E_P[T] = M e^{−h} > 0`; `μ*` uniquely minimizes `E_P[T]`, R3) | **KEEP-FULL** (small) | `pD2_minimization_proof.py` (6/6) | §1 — v8 has the slogan "(min gap = max seal-tilted entropy)"; port the identity, the `μ*` entropy value, and R3 (R3 foreshadows why `mT` is the right 1-D variable for `(Lμ)`) |
| C14 | §3.5 | **Two-level family**: moments force `a·b = N−1`, `k = Nb/(a+b)` (sympy-exact, all `n`) ⇒ one-parameter divisor family `a | (N−1)`; gap strictly decreasing in `a` over divisor candidates ⇒ `μ*` (`a = N−1`) unique two-level minimizer; grade split honest — reduction all-`n`, monotonicity verified `n = 2…12` + `13, 15, 16, 18, 20`, not analytic (naive continuous interpolation non-monotone near `a ≈ 1`, and non-achievable there anyway) | **KEEP-FULL** | `pD` check F | §2 — v8 carries the corrected grade ("[SUB-THEOREM] … verified to `n = 20`") but not the two formulas that *are* the sympy-exact half, nor the divisor-family structure |
| C15 | §4 | Reduction logic + **exhaustive base table** (`8 / 128 / 32768` classes; global min = closed form at `n = 2, 3, 4`, reproducing `p9a`); the scalar-tilt best-cell certificate (R4/Q/V1) with the `n = 4` bottom-cell-fails/top-cell-rescues example | **KEEP-FULL** (table) / KEEP-RESULT (certificate detail) | `pD` check G; `pD2_minimization_proof` V1 | §1 or §2 — the 3-row table is small and is the entire evidentiary base of "THEOREM at `n ≤ 4`"; port it. The best-cell machinery stays receipt-level (it is the superseded surrogate track — keep only the §2 caveat clause, per C3) |
| C16 | §4.1 | **The DV certificate** (expected big item — PARTIAL): the chain `m̂(ε) = D(P*‖U) ≥ D(law_{P*}(T)‖law_U(T)) ≥ I_T(mT) := sup_t [t·mT − log E_U[e^{tT}]]` with the **true seal mean** `mT = E_{P*}[T] = Σ_a e^{−h*_a}`; never overshoots (`I_T(mT) ≤ KL_{1D} ≤ m̂` over all orientations checked, exhaustive `n ≤ 4`, random+deep `n = 5, 6`); exactly tight on `μ*` (`= m̂_min(n)` to `> 100` digits, `n = 2…8`); isolates `μ*` through `n ≤ 6` (runner-up LB `≈ 0.51/0.29` at `n = 3, 4` vs `μ* ∼ 2⁻ⁿ`) | **KEEP-FULL** | `pB_variational.py` (9/9) | §2 — v8 has the right prose (valid all-`n`, never overshoots, tight on `μ*`) but no formulas: `I_T` and `mT` are never defined, yet `(Lμ)` is stated in terms of `mT`. Port the displayed chain + the `mT` definition; keep v8's `mT(μ*) = (N−1)e^{−h} → 1` as the specialization |
| C17 | §4.1 | Three uncertainty-principle tools **ruled out with named reasons**: Donoho–Stark (bounds support size — wrong functional; both supports constant across orientations, zero separating power), Maassen–Uffink (wrong *direction* — lower-bounds the entropy sum ⇒ upper-bounds the gap), Bonami–Beckner hypercontractivity (spectrum-blind — every orientation's spectrum is flat; `μ*`'s distinction is value concentration) | **KEEP-FULL** (small) | `pB_donoho_stark.py` (8/8), `pB_entropic.py` (11/11), `pB_hypercontractive.py` (10/10) — each a documented NO | §2 — v8 says "three uncertainty-principle tools ruled out for principled reasons" without names, reasons, or receipts. Name all three + one-clause reasons; add the three receipts to §6 (documented-NO receipts are part of the honest record) |
| C18 | §5 | `[L*]` + the margin reduction `(Lμ)` + margin-essential + **refuted closers**: (1) not Schur-monotone in the `T²` profile (44/83 comparable pairs flip at `n = 4`; 3/6 at `n = 3`); (2) not monotone in `minT` — witness floors `0.328` (`minT = −7`) `< 0.480` (`−11`) `< 0.725` (`−13`), so the deep-band search focus is a *heuristic*, not a (B)-consequence; (3) within-level minimizer not always two-level; blocked routes: surrogate not a lower bound; greedy single-flip landscape has many local minima (`n = 5` restarts bottom near `0.235 ≫ 0.0317`, never reaching the `μ*` basin) ⇒ no swap/exchange argument; the domination is **global only** (no intermediate two-level dominator, no monotone explaining parameter); equivalent entropy form `H(P*) ≤ ln(N−1) + δ_n` | **KEEP-FULL** (the missing witnesses) | `pD` checks I, I2; `pD2_achievability` P3b, P3d | §2 — the core is carried well (margin form, `c ≥ 0.5`, floor-mean `0.026 < 0.0645` bare-min refutation, three closers named with 44/83). Missing: the `minT` witness triple (`0.328/0.480/0.725` — makes "non-monotone" checkable), the greedy-landscape route block, the "domination is global only" observation, and the equivalent entropy-bound form of `[L*]` |
| C19 | §6 | Counterexample hunt architecture: `n = 5` deepest-tier exact enumeration (gauge-fixed `2²⁶`; tier runner-up `0.2436`); `n = 6` two-phase (random breadth `~2×10⁶` bottoms at `minT = −43`; greedy-deepen reaches `−63` where the unique orientation is `μ*` — gap `0.015748356968` vs next-best `0.7165` at `−61`, an isolated deep basin); `n = 7–9` four strategy families with `dps = 120` vector-seal re-checks of best competitors, every margin strictly positive; `n = 10` float probe, flagged | KEEP-RESULT (two small adds) | `pD_chiral_global_optimality.py` (17/17), `pD2_counterexample_hunt.py` | §§1–2 + §6 — carried at result level ("orientation-level searched at `n = 5, 6`, counterexample-hunted to `n = 9`", true-ratio isolation evidence). Worth adding: the **`n = 6` isolation pair `0.0157` vs `0.7165`** (the single most vivid isolation datum) and the `n = 10` float-probe flag. The strategy-family roster stays receipt-level. The `0.235/0.2436` pair is carried in reconciled form — see corrections checklist |
| C20 | §7 | **Anchor table + `δ_n` analysis** (expected big item — PARTIAL): `m̂_min(n)` at dps 140 for `n = 2…8` (30 digits), `δ_n` column (`2.10×10⁻²`, `4.11×10⁻⁷`, `4.50×10⁻¹⁹`, `6.76×10⁻⁴⁸`, `2.94×10⁻¹¹⁵`, below-floor `n ≥ 7`), prefactor column; `δ_n` sign-definite positive (structural, all `n`), doubly-exponential `δ_n ~ 2^{−n(2ⁿ−1)}`; prefactor `1.0000000149` at `n = 25`; the `n = 5` sampled-bound-was-exact confirmation | **KEEP-FULL** | `pD` check E; `s_chiral_gap_ratios.py` | New anchors block (end of §1 or §6) — v8 has four 16-digit anchors in the abstract and the bare symbol `δ_n`. The `δ_n` structure (sign-definite, doubly-exponential, where it underflows) is an expected big item and currently absent. Nit: v8's 16-digit anchors are *rounded at the last digit but written with a trailing ellipsis* (e.g. `0.2666533651797381…` vs true `0.266653365179738098…`) — truncate or extend when porting the table |
| C21 | §8 | Literature placement: Donoho–Stark/K. T. Smith equality case (spectral-spread/spatial-peak extreme, wrong functional), bent functions (opposite extreme), flat-Littlewood/merit-factor (the hard family; why no clean certificate exists) | KEEP-RESULT | — | §2 + References — carried ("Donoho–Stark-adjacent … bent … flat-Littlewood the hard neighbor") with Donoho–Stark/Rothaus/Balister et al. cited. Optional refs: K. T. Smith equality case, Jedwab–Katz–Schmidt (merit factors) |
| C22 | §9 | Conclusion recap ("reclassified, not slain") | **DROP** | — | Summary duplication; v8's abstract "Net" + §2 closing sentence carry the grade |
| C23 | Receipts block | Per-receipt check inventories (pD 17/17 with the A–I2 map; pD2 6/6, 10/10; hunt; pB_variational 9/9; the three documented-NO receipts) | KEEP-RESULT (+ 3 adds) | — | §6 — pD/pD2/hunt/pB_variational present with counts; **add `pB_donoho_stark.py` (8/8), `pB_entropic.py` (11/11), `pB_hypercontractive.py` (10/10)** (all verified present in `../v7/code/`), per C17 |
| C24 | Refs | Cover–Thomas (DPI), Marshall–Olkin–Arnold (majorization — the route that *fails*), Griffiths/Toulouse, Smith, Jedwab–Katz–Schmidt | **KEEP-FULL** (refs, small) | — | References — Cover–Thomas and Marshall–Olkin become apt the moment C9/C18 are ported (v8 asserts the DPI bound and the Schur-refutation); Griffiths/Toulouse per A11 |

---

## Corrections checklist (7 items; 6 carried, 1 regressed-in-class)

| # | Correction | Status in v8 paper 5 |
|---|-----------|----------------------|
| 1 | `[L*]` in **margin form** `(Lμ)` | **CARRIED** — abstract + §2: `mT(ε) ≥ mT(μ*) + c`, `c > 0` `n`-independent, `c ≥ 0.5` verified, per-orientation rate functions, "bare minimization would not suffice" with the floor-mean witness `I_T(·,1) ≈ 0.026 < 0.0645`. (But `mT`/`I_T` undefined in-paper — C5/C16) |
| 2 | Surrogate ratios relabeled (true `n = 4 ≈ 5.1`) | **CARRIED** — §2 quotes *only* the true vector-seal ratios `≈ 5.1` (`n = 4`) and `≈ 7.4–7.7` (`n = 5`), tagged `[CORROBORATION]`; the surrogate figures `6.1/7.3/21.8` are not carried at all, so no mislabeling is possible |
| 3 | Two-level closed **modulo the `n ≤ 20`-verified step** | **CARRIED** — abstract ("closed modulo one numerically-verified (`n ≤ 20`) monotonicity step") + §2 `[SUB-THEOREM]` with the sympy-exact/numeric split stated |
| 4 | Corrected multiset parenthetical | **REGRESSED IN CLASS** — v8 does not quote the `n = 5` witness parenthetical (vacuously safe; frozen v7 carries the corrected `{−7, +1, +9}`), but §1 introduces a **fresh error of exactly this kind**: "counts `2/4/14/156` at `n = 1…4`" — must read **`n = 2…5`** (see Bugs) |
| 5 | `0.235/0.2436` reconciled | **CARRIED IN SUBSTANCE** — the two probes (greedy-descent `0.235`; `n = 5` tier-enumeration `0.2436`) appear only as the single reconciled range "`≈ 7.4–7.7` at `n = 5`" (= `0.235–0.2436` over `0.0317`); no figure quoted, no contradiction possible. If C19's figures are ported, keep the tier-enumeration vs greedy-probe labels from frozen v7 §6 |
| 6 | Count-vs-index + `γ₅` check | **CARRIED** — abstract + §3: count `n₊ + n₋` vs index `n₋ − n₊ = Q` distinguished, the `γ₅`-chirality check named, the vector-like `Q = 0` pair explicit (`2/0`, `1/1`, `2/2`); Banks–Casher cited "with the count/index distinction explicit". (The *not-continuum-`ρ(0)`* disclaimer still worth porting — B6) |
| 7 | Scope large-`N`/2d | **CARRIED** — abstract (bold), §3 scope guard, §5 (finite-`N`/4d/confinement non-claims). Minor restorations: the `≤ 10×10` lattice ceiling and the uniform-flux non-dynamical background (B2) |

## Bugs and mandatory small fixes in the current v8 paper 5

1. **§1 factual error:** "counts `2/4/14/156` at `n = 1…4`" → **`n = 2…5`**. (Paper 18 §3.2 / receipt `pD2_achievability.py` P1. At `n = 1` there is one achievable multiset; `156` is the `n = 5` count — as written it also contradicts the paper's own "exhaustive at `n ≤ 4`" narrative, since 14, not 156, is the `n = 4` count.)
2. **§6 missing receipts:** `p9b_psg_ingredient_functor.py` and `p9c_chirality_bridge_nogo.py` — the *only* receipts for the two §4 no-gos — plus the three documented-NO receipts `pB_donoho_stark.py` (8/8), `pB_entropic.py` (11/11), `pB_hypercontractive.py` (10/10). All five verified present in `../v7/code/`.
3. **References:** zero v6 citations. Add v6 Papers 7, 8, 9, 14, 22 (A11, B13); external: Neuberger, Nielsen–Ninomiya, Griffiths + Toulouse, and (with the C-ports) Cover–Thomas, Marshall–Olkin–Arnold; optional: Wen 2013, Levin–Wen, K. T. Smith, Jedwab–Katz–Schmidt.
4. **Anchor-ellipsis nit:** the four abstract anchors are rounded at digit 16 but written with a trailing "…" (3 of 4 affected, e.g. `…7381…` vs true `…738098…`); truncate instead, or port the full C20 table.

## Expected big items — verification

| Item | Status in v8 | Rows |
|------|--------------|------|
| Paper 9 gap-law proof structure | **PARTIAL** — closed form, minimizer, anchors, `2^(2ⁿ−1)`, exhaustion-at-`n ≤ 4` present; two-value-collapse identity + derivation, seal setup, three load-bearing properties, `W`-floor two-branch physics, exhaustion counts absent | A4, C5, C6 |
| Paper 9 `δ_n` analysis | **MISSING** beyond the symbol — sign-definiteness (structural all `n`, verified `n ≤ 6`), doubly-exponential magnitudes, underflow flag, table | A4, A10, B12, C20 |
| Paper 9 `168 = GL(3,2)` PSG analysis | **MISSING** — result string only; no `K̂ ⋊ Aut(K)`, no order computation, no `1+7+7+1`, no split/cocycle obstruction, no `H²(SG, IGG)`, no receipt | A5, A6 |
| Paper 9 chirality-bridge no-go construction | **MISSING** — verdict only; no two-layer split, no handedness-blindness, no parity-odd-on-parity-symmetric argument, no receipt | A7 |
| Paper 14 GW-flow construction | **PARTIAL** — Lüscher-survival claim, rounded anchors, count/index bookkeeping present; operator recall + O8 closure statement, `D(M)`/`γ̂₅`, gap equation, residuals, thresholds/trend, divergence law + Banks–Casher disclaimer absent; zero v6 citations | B3–B6 |
| Paper 18 structural core proofs | **PARTIAL** — all five core items *named* with key witnesses (0.796/1.125; 44/83; floor-mean 0.026; `c ≥ 0.5`); but the setup, the one-line proofs (A, B, §3.3), the identities (`D_{1D} = ln N − H(P*)`, `a·b = N−1`), the DV chain + `mT` definition, the achievability witness multiset, and the exhaustion table absent; achievability count range **wrong** | C5–C9, C11–C16, C18 |

## Cross-cutting actions (the port plan)

1. **§1 rebuild** (highest priority): open with the C5 setup block (characters → orientations → per-mode seal → gap → Walsh frame → `μ*`), then the gap law as a *derivation* (C6 two-value identity + one-line proof; scalar seal → closed form), the A4 physics paragraph (`W`-floor, two branches, three properties), and the structural core with its one-line proofs and identities (C7, C8, C9, C11 **fixed**, C12, C13), closing with the C15 exhaustion table and the C20 anchors/`δ_n` table (or the latter as a §6-adjacent block).
2. **§2 additions** (the residue): C16 DV chain + `mT` definition; C17 three named ruled-out tools with reasons; C18 missing witnesses (`0.328/0.480/0.725`, greedy-landscape block, domination-global-only, entropy form of `[L*]`); C3 overshoot figures; C19's `n = 6` isolation pair + `n = 10` probe flag.
3. **§3 rebuild** (the mechanism): B3 recall + O8-closure + novelty-over-v6-22 paragraph (with v6 citations); B4 Lüscher-survival construction; B5 gap equation + thresholds; B6 divergence law + Banks–Casher disclaimer; B1 precision split.
4. **§4 expansion** (the gate): A5 group construction; A6 no-Wen-PSG argument; A7 bridge no-go argument; B9's `0.067` menu spread + name-vs-value collapse + limits-only sharpening.
5. **§5:** A9's explicit "no particle mass or mass ratio is produced or predicted"; A8's "Paper IX's third open item is closed by §3" clause.
6. **§6 + References:** bug-fix list above (receipts #2, refs #3); B12 `δ_n` positivity flag; C4/B1 precision-split sentence.
7. **Guard rails (do NOT reintroduce):** the scalar-tilt certificate as a proof route (it overshoots — keep it explicitly non-load-bearing); surrogate isolation ratios (`6.1/7.3/21.8`) unlabeled; "doubly gated" (superseded by singly); count/index conflation (`n_zero = Q` is false at `Q = 0`); any claim that `(Lμ)` or the all-`n` lemma is closed; any continuum Banks–Casher `ρ(0)` reading of the zero-mode term; "`PARTIAL_REDUCTION`" upgraded in either direction.

## Counts

- **KEEP-FULL: 29** rows — A4, A5, A6, A7, A10, A11; B1, B3, B4, B5, B6, B9, B13; C4, C5, C6, C7, C8, C9, C11 (+fix), C12, C13, C14, C15 (table half), C16, C17, C18, C20, C24 → of which **6 BIG clusters** (C5 setup; A5+A6 PSG; A7 bridge; B4+B5 GW construction; C16 DV; A4+C20 gap-law physics/`δ_n`).
- **KEEP-RESULT: 13** rows — A8, A9, B2, B10, B11, B12, C1, C2, C3, C10, C19, C21, C23 (several with one-clause adds noted).
- **DROP: 6** rows — A1, A2, A3, C22 (process/recap), B7, B8 (destination paper 3 — **verified ported there**); plus the certificate-machinery half of C15 (superseded surrogate track, kept receipt-level only).
- Corrections: **6/7 carried**, 1 regressed-in-class (the `n = 1…4` counts bug). Receipts on disk: all 14 referenced files verified present in `../v7/code/`.
