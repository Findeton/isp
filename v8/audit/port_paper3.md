# V8 self-containment audit — destination paper 3 (walls classification)

**Date:** 2026-07-01. **Rule:** v7 is log-only (NOT citable) — every load-bearing argument, construction, number, and honesty guard the v8 paper's claims rest on must live *inside* v8. v6 IS citable (v6 paper 57 / publishable paper-XI may stay citation-level for the scale material, per the audit brief). "Self-contained ≠ compressed": a verdict stated without its derivation fails the audit.

**Target:** `/Users/felixrobles/workspace/isp/v8/relativistic-isp-v8-paper3-walls-classification.md` (45 lines, 5 sections + abstract).
**Sources:** v7 paper5 (c_m free calibration, 99 ln), paper8 (mode wall, 95 ln), paper17 (classification theorem, 224 ln), paper14 §3 only (mode-relocation no-go half, of 130 ln; the constructive half is v8 paper 5's — audited there).

## Overall verdict

The v8 skeleton is correct — the abstract is strong, correctly graded, and **all seven required corrections are verified present** (checklist below) — but the paper currently states its three walls at headline strength with most of the *derivations* still only in the frozen log. Ranked by severity:

1. **§2 mode wall — the worst gap.** One paragraph currently carries what three source papers derive. Missing: paper8 §4's full three-route failure apparatus (gauge-inequivalence/superselection framing via the parity-character ledger; the dilution ladder `0.6094 → 0.4951 → 0.3680`; the recorded-distinct check `D = 0.00479 > 0`; the `ξ = 1/η` tautology + mode-covariance `1.2814 ≠ 1.6410`; the mode-blind clock `E[I] = 1` and the gate-circularity overshoot `0.526 > W_*`), paper8 §5's hierarchy form-only mechanism (the `1/N` mode-count ratio, `N ~ 10³⁸` not corpus-forced), and paper14 §3's decisive relocation construction (the `F_α` form, the distance quantification `0.012` / `0.44`, the every-monotone-reward robustness, the `α = 0` form-dependence). v8 has only the crossings numbers and the `(n′−n)·ln 2` shift.
2. **The c_m block (paper5) — placement present, analysis absent.** v8 correctly places `c_m` on the mode axis with the weight-`0` arithmetic, but omits the entire calibration analysis: the proved complete-and-blind result with its **G1-free/G1-dependent claims split** (blindness rides only G1-*free* sub-results; the weight-`0` classification and the closure are the G1-gated claims), the toy-value test (`12`/`1200` both pass, NOT-SELECTED), the exact factorization `c_m = γ_G·ν_m²` with `σ_A` cancelling, the content-vs-strength axis guard (`m̂ ≈ 0.156` nats ≠ `c_m ≈ 10⁻⁴⁵`; the toy floor pins nothing physical), the free-κ disambiguation, and the θ_B²-vs-Srednicki numerology refutation.
3. **§3 classification — triples asserted, receipt-anchored instances compressed out.** Missing: the `g_λ` definition and 1-parameter-group composition law; the paradigm gradings (ratio vs control); **the two-Λ distinction** — v8 §1.2 lists `weight(G·Λ₀) = 0` and writes the unified invariant `κ·σ_A = G·Λ² = const` with *no disambiguation* between the weight-`(−2)` de Sitter `Λ₀` and the weight-`(−1)` spectral/UV cutoff `Λ_uv` (paper17 §3.1's explicit "distinct weight-`0` objects, not the same `Λ`"); the TENSOR K-counts `3/4/10/16` in the instance; the MODE Gram/torsor/stabilizer analysis; the roll-up table; the C.1–C.4 common-structure checks.
4. **§§1/4 G-leg + statuses — texture absent.** Missing: paper17 §4.1's four named premises with the axiom-(R) honest split (Gibbs at *some* β unconditional; the *value* `2π` is the axiom) and the **internal asymmetry** (the weight-counting no-go does not ride axiom (R); what rides Jacobson is the *identification* of the missing unit with `G`); the orthogonal-axes guard (§4.3); the three convergent lineages by name (§5.1); the imports list with the Visser-exempts-Jacobson attribution datum (§6).

Receipts: v8 §5 is missing `m2_mode_canonicalization.py` (10/10 — paper8's three-routes receipt) and `p5_cm_calibration.py` (10 checks), plus the "no SDP / no solver-tolerance digit in p17" declaration; the p6 cross-check, if named, must use the **corrected count 43/43** (LEDGER 2026-07-01), not paper17's 42/42.

---

## Corrections checklist (all seven verified in the v8 target)

| Correction | Source state | v8 state |
|---|---|---|
| MODE = [POSITED-STRUCTURAL] | paper17 tags it [STRUCTURAL]/[ANALOGY] (§§2.1, 3.3, 3.4) | v8 ✓ (abstract, §3, §4) — but `[POSITED-STRUCTURAL]` is absent from the status-line tag list (only `[POSITED]`); add it or define it there |
| only-invariants / at-least-residual | paper17 §2.1 has the guarded form ✓; §5.2(A) and §7 say "exactly the `G_i`-invariants" — the known overclaim | v8 ✓ ("the inclusion actually proved", "blind to *at least* the orbit parameter"); v8 refs even flag paper17's "'exactly'-form" as superseded. Port §2.1's form; DROP the §5.2/§7 phrasings |
| G-leg Jacobson-CONDITIONAL | paper17 guard 2, §4.1, §6.3 ✓ | v8 ✓ (abstract, §1.1, §3, §4) — but the four-premise enumeration + internal asymmetry still need porting (Source-3 table) |
| scale weight +1 | paper17 receipt `weight(l_step)=1`, §4.3 "the scale a weight-(+1) grading obstruction"; paper14 §3.4 same | v8 ✓ (`weight(l_step) = +1`, `G` `+2`, `σ_A` `−2`); on porting §4.3's guard keep l_step (+1) vs `G` (+2) distinct |
| three statuses | paper17 §4 (anchored "Paper XVI"); paper14 §3.4/§6.4 | v8 ✓ (measured / import-fixed / contested convention; abstract + §3 + §4). LEDGER: *derived* in v8 paper 2 §5, *classified* here — intentional double home. The retired overclaim ("Renou fixed the bit like G") appears nowhere ✓ |
| c_m on the mode axis | paper5 §§2–5; paper17 §3.1 parenthetical + §3.3 | v8 ✓ (abstract, §1.2 `weight(c_m) = 0`, §2 "Where c_m lives", §4 "placement of c_m") — the *placement* is done; the paper5 *analysis* is not (Source-1 table) |
| drop 'fifth wall' ordinals | **P6-vs-P8 collision confirmed:** v7 paper6 §6 heading gives "the fifth wall" to the TRANSVERSE/composite-state-space structure; v7 paper8 §§4/7/8 gives the same ordinal to MODE-canonicalization; paper14 §3.4 already demurs ("third last input") | v8 ✓ (0 hits for "fifth"). On port: DROP paper8's "fifth instance/fifth wall" and paper14 §3.4's "(Paper VIII named it the 'fifth wall'…)" parenthetical |

**Ledger-driven update on port (2026-07-01):** `p6_transverse_nogo.py` = **43/43** after the monogamy-sharpness extension (paper17's reproducibility says 42/42, of which 7 solver-tolerance — update the count, keep the 7-SDP-flagged caveat); `f5` = 21/21 if ever cited; full-level-2 `I3322` comparator quoted as `0.2509397` with no eighth digit.

---

## Source 1: v7 paper5 — c_m free calibration → v8 §2 (the c_m block) + §1.2

LEDGER routing confirmed: "v7 paper 5 (c_m calibration) → v8 paper 3 (walls; the c_m/mode axis)".

| Section | Class | Destination / notes |
|---|---|---|
| Status header + tags | DROP | v7 bookkeeping. Tag mapping: [FORCED]→[THEOREM]/[DERIVED]; [PERMITTED]→[DERIVED] (weight-`0` eligible); [BLIND]→[NO-GO] (gravity-sector blindness); [FREE]→[IMPORT] (one free per-species calibration); [OPEN]→[OPEN]. |
| Abstract | KEEP-RESULT | Superseded by porting §§1–7; the one-line verdict ("permitted but not gravity's to give — the matter sector's free per-species calibration, the strength-axis twin of the free unit, sharing the spacing's mode-canonicalization gate") survives as the c_m block's opener. |
| §1 the permitted lane | KEEP-FULL → v8 §2 | v8 has only the formula. Port: `c_m = Gm²/ℏc = (m/M_Planck)²` as the gravitational analog of the fine-structure constant; the hierarchy numbers `c_m(electron) = 1.7518 × 10⁻⁴⁵`, `c_m(proton) = 5.9061 × 10⁻³⁹` (gravity `~10⁻⁴³` weaker than electric repulsion for two electrons); "a derivation of `c_m` would be a statement about *why gravity is weak*" — the hierarchy-problem identification that motivates the whole block. |
| §2 weight-zero / intrinsic-eligible | KEEP-FULL → v8 §§1.2/2 (partially present) | v8 has the weight arithmetic `+2 + 2(−1) − 0 = 0` ✓. Port the rest: the generator weights under `l → μl` (`G ~ l²`, `m ~ 1/l`, `ℏc` weight `0`); the gauge-invariance *verification* `c_m(μl)/c_m(l) = 1` to `< 10⁻⁴⁰` at `μ = 1.7, 5`; the mass-ratio-squared reading (absolute scale cancels); the contrast class (`σ_A`, `G`, absolute `d` at weight `±2`, forbidden — `c_m` the legitimate candidate). |
| §3 complete-and-blind [BLIND] | KEEP-FULL → v8 §2 | Entirely absent from v8. The two-results split that must NOT be conflated: (a) the **record equivalence principle** (H1, universal record footprint — a quotient-functoriality/covariance theorem) makes "the" matter ratio *well-defined* — the theorem itself is v6 paper 7 (citable, KEEP-RESULT with citation); (b) the **blindness proper** — the gravity sector is complete with respect to that ratio, no gravity-sector identity constrains its value. Plus the **role-identity-across-unit-systems guard**: the same hierarchy invariant reads `m̂ ≈ 0.156` nats in record-content units and `c_m ≈ 10⁻⁴⁵` in SI-strength units, bridged only by the unbuilt matter calibration — the content axis and the strength axis must not be conflated. Plus the orienting analogy: "the Einstein equations do not determine the electron mass" — a gravity sector that fixed `c_m` would have geometry determine the matter spectrum, which neither GR nor record-gravity does. |
| §4 the toy-value test [FREE] | KEEP-FULL → v8 §2 | The clean exhibition of blindness: `c_m = 12` and `c_m = 1200` **both** pass every executed gravity identity — `κ·σ_A/2π = 1.000000000000` in all four cells (NOT-SELECTED; the four-cell scan inherited from v6 paper 6 §9, citable; the receipt reproduces its *structural* basis — the invariants `κ·σ_A = 2π`, `G·σ_A = 1/4` are functions of `G, σ_A` alone). Plus the **free-κ disambiguation**, load-bearing (two κ's in the corpus): `c_m` is the strength-axis analog of the scale no-go's free unit `κ` — the Tier-A content→commit-order conversion *rate* — **distinct** from the gravitational `κ = 8πG` in `κ·σ_A = 2π`. v8 currently gestures at this only in its references ("the one free `κ` this wall protects"). |
| §5 factorization + shared gate [OPEN] | KEEP-FULL → v8 §2 | The centerpiece, absent from v8: `c_m = γ_G·ν_m² = (G·σ_A)·(m²/σ_A) = Gm²` — the record area density `σ_A` **cancels identically** (sympy-exact); `γ_G = G·σ_A = 1/4` a ratio-of-twins *convention* fixed by the gauge; the matter content entirely in `ν_m = m/√σ_A`; physical `ν_m(e) ≈ 8.4 × 10⁻²³`, unbuilt. The mode-dependence illustration: `η_B = 0.609` (one-mode) vs `h_* = 0.495` (coupled), `|η_B − h_*| = 0.114`, no canonical member — toy values `~22` orders above physical, *structure not value*. The **precise shared-gate statement**: `c_m` and the spacing `d` share exactly ONE sub-problem — the mode-canonicalization (a single canonicalization principle would pin both) — while `c_m` *additionally* carries the matter-sector + hierarchy-selection burden `d` does not (the two remain categorically distinct in role). The **toy-floor caveat** keeping "free" honest both ways: the toy gap `m̂` is floored at `C(η_B) = 0.156` nats for the unoriented class, yet physical `c_m(e)` sits ~44 orders below — the toy record gap bounds nothing physical (and an oriented/chiral ledger forfeits the floor anyway). |
| §6 what a derivation would require [OPEN] | KEEP-FULL → v8 §2 or §4 | The three-item ladder: a constructed matter sector (toy gaps `1/η_B = 1.641`, Onsager `23.355` are not the proton); a mode-canonicalization (the shared gate); a hierarchy-selection principle. Plus the **spectral-action candidate analysis**: Chamseddine–Connes only ever yields the weight-`0` invariant `G·Λ²` (scheme-dependent pure number — `3π²`, Sakharov `192π²/N`), which fixes the *ratio* `G/l_step²`, **not** `c_m = Gm²` (that needs a separate `(m/Λ)²`), and rests on an unverified profile identification — a candidate, not a prediction. Feeds §3's two-Λ distinction (Source 3). |
| §7 claims/non-claims | KEEP-FULL (split) → v8 §§2/4 | **The G1-free/G1-dependent claims split (the audit brief's expected big item), entirely absent from v8:** the blindness result leans only on G1-*free* sub-results (the equivalence-principle theorem + the algebraic `c_m`-independence of the executed gravity invariants) — so blindness is **independent of any open premise**; it is the weight-`0` classification (§2) and the no-record-coefficient closure (§5) that rest, like the scale no-go, on the single open premise **gate G1** — which v8 §1.2 *names* but never *states*: "no sealed law consumes the record area outside the continuum labeling map". Port the statement. Plus the **anti-numerology guard**: the strongest record-number coincidence, `θ_B² = 0.295598` vs the Srednicki area coefficient `a = 0.295417`, refuted *algebraically* (`θ_B²` is a root of the irreducible `s³ + s² + 3s − 1`, a cubic irrational, distinct from `a` and on the wrong axis). Non-claims (no value — any number smuggles the `1/4` convention + a mode choice; the illustrative `γ_G·η_B² = 0.093` is ~44 orders off; H2 relocated, not solved) → v8 §4 scope sentences. |
| §8 precision | KEEP-RESULT → v8 §5 | `p5_cm_calibration.py` (10 checks, mpmath `dps ≥ 60` CODATA-2018, sympy-exact where structural) — **currently missing from v8 §5's receipt list; add.** |
| References | KEEP-RESULT | v6 homes citable and mostly present in v8 already (paper 6 Theorem G / H1–H2 / toy-value scan; paper 7 EP theorem / Thm 6.1; paper 57; paper-XI). **Paper5 carries no external refs at all — on port, v8 must add:** Srednicki PRL **71**, 666 (1993) (the area coefficient `a` in the refuted numerology); Chamseddine–Connes, Commun. Math. Phys. **186**, 731 (1997) + Sakharov (1967) (the §6 candidate analysis). |

## Source 2: v7 paper8 — the mode wall → v8 §2 (constructive Part A routes to v8 paper 5)

| Section | Class | Destination / notes |
|---|---|---|
| Status header + tags | DROP | v7 bookkeeping. Tag mapping: [SYNTHESIS]→positioning prose (paper 5); [THEOREM]→[THEOREM]; [CONJECTURE]→superseded (v8 paper 5's closed law); [NO-GO]→[NO-GO]; [FORM-ONLY]→[DERIVED (form)] + [IMPORT (magnitude)]; [GATED]→[NO-GO]-gated. |
| Abstract | KEEP-RESULT | Superseded by the ported body. **DROP "fifth instance of the program's under-determination" / "fifth wall"** (the P6-vs-P8 ordinal collision — checklist above); the shape statement survives ordinal-free as v8 §3's classification. |
| §1 intro | DROP / KEEP-RESULT | Framing superseded by v8 papers 1/3 abstracts. One sentence survives → v8 §2: the mode-canonicalization as *the shared gate behind the spacing `d`, the coupling `c_m`, and any particle mass*. |
| §2 three constructive pieces [SYNTHESIS] | DROP for paper 3 — **cross-paper coherence flag** | Out of destination scope (constructive matter). BUT: v8 paper 3's status line claims to supersede v7 paper 8 *entire*, while v8 paper 5 absorbs only "9/14-constructive/18" and (grep-verified) carries none of: the exact record GW operator as Nielsen–Ninomiya evasion (single species, 1 Brillouin zero vs 4), `index(D) = Q` 14/14 flux sectors, the anomaly filter `{1,4,4|2,2,5}`, or the "SHARD ahead on chiral fermions" positioning vs Levin–Wen / Wen-PSG. The pieces themselves have citable v6 homes (v6 papers 9, 14 — so no rule violation), but the *synthesis/positioning claim* is currently unrouted in v8. Recommend: one compressed synthesis paragraph in v8 paper 5 §2 with v6 citations, or amend paper 3's supersession line to "the wall half of 8". |
| §3 mass = gap when protection breaks | DROP for paper 3 (→ v8 paper 5) / KEEP-RESULT (one line here) | The W-theorem has a citable v6 home (paper 7 Thm 9.4); the oriented descent table is superseded by the closed law `m̂_min(n)` already in v8 paper 5. **What paper 3 must carry (one line):** the definition `W = η·θ − log cosh η = 0.1561092…` (`η` the seal root `e⁻η = tanh η`; `θ` the commitment-cubic root `θ³+θ²+θ−1 = 0`) — otherwise §2's "nearest forced constant `0.156`" is a dangling number. The balanced-KL caution (`0.00382 ≠ W`) → paper 5. |
| §4 the mode-canonicalization wall [NO-GO] | KEEP-FULL → v8 §2 | **The audit brief's expected big item — v8 has none of the apparatus.** Port whole: form-fixed-in-every-mode (`∇ψ(h) = e⁻ʰ` holds identically across modes/sectors — what self-consistency DOES fix); the structural reason — modes differ by **ledger rank** `[1,3,7]` (orthogonal primitive characters), an invariant no record-internal map changes ⇒ **distinct superselection sectors**, the Wen-PSG hallmark (needs the Wen 2002 ref); the per-mode coefficients `0.609 / 0.495 / 0.368` monotone down the dilution ladder; then the **three-route failure in unison**: Route 1 completeness/no-silent-refinement, both horns (recorded label ⇒ maximal complete ledger pushes the coefficient *down* the ladder `0.6094 → 0.4951 → 0.3680`, *away* from `η_B`; silent label ⇒ the dissolution template would apply but the modes are recorded-distinct, `D(P_{η_B}‖P_{h_*}) = 0.00479 > 0`); Route 2 external structural identity (`1/η_B = 1.6410… = ξ` is an algebraic **tautology** — `ξ = 1/η` IS the seal equation rewritten, symbolic residual `0`; and the mass-gap length is mode-*covariant*, `ξ(h_*) = 1.2814 ≠ ξ(η_B) = 1.6410`, so "has a mass-gap length" selects nothing; `1/η_B` already filed weight-`0` record-internal); Route 3 variational (max content selects the *least*-coupled ledger, completeness the dilution *limit* — **opposite** ends; the firing clock is mode-blind, `E[I] = 1` in every mode; any in-class extremum circular — the admissibility gate IS the mode choice: dropping the count-dual base to `p = 0.2` overshoots, content `0.526 > W_* = 0.365`). The sole-escape paragraph (a matter Hamiltonian relocates, never closes) merges with paper14 §3.3's decisive proof. **DROP the "fifth wall/fifth instance" sentences.** |
| §5 hierarchy form-only [FORM-ONLY] | KEEP-FULL → v8 §2 (c_m block) | The *positive* content about c_m's smallness, absent from v8: the record(slow)/substrate(fast) split makes the smallness a **`1/N` mode-count ratio** — structurally a near-isomorph of Sakharov induced gravity / the Dvali species scale / the matrix slow-fast split (refs needed), all weight-`0`; but the controlling count `N = 1/c_m ~ 10³⁸` is itself weight-`0` and **not corpus-forced** (natural record gaps `O(1)–O(10)`, ~37 orders short) — the mechanism fixes that `c_m` IS a `1/N` ratio without fixing `N`, reproducing exactly paper5's one free per-species calibration. Form derived, magnitude imported. |
| §6 no mass ratio computable [GATED] | KEEP-RESULT → v8 §2 | Superseded by paper14 §4's sharpening (doubly → **singly** gated). State the wall side only: between-species ratios gated by the rank *assignment* alone (= this wall); values and within-pair ratios → v8 paper 5 (v8 §2 already points there ✓ — add the singly-gated clause). |
| §7 claims/non-claims | KEEP-RESULT → v8 §4 | Surviving non-claims: no PSG *group* `H²(SG, IGG)` derived (the map is to PSG-*protection*, phenomenological); ledger-chirality ↔ spinor-chirality unproved. Items about the biconditional/conjecture superseded by v8 paper 5's closed law. **DROP the fifth-wall ordinal in claim (3).** |
| §8 the prerequisite / escape [OPEN] | KEEP-RESULT (split) | Route (c) — a constructed matter Hamiltonian selecting a rank = the relocation move — → v8 §2 (paper14 §3.3 proves it decisive). Routes (a) Tier-1 PSG functor and (b) chirality bridge → v8 paper 5 §4 ✓ (verified present there as the two closed routes: chirality-bridge no-go; `168 = GL(3,2)` but no Wen-PSG without geometry). |
| §9 precision | KEEP-RESULT → v8 §5 | `m2_mode_canonicalization.py` (10/10, `dps = 100`: dilution ladder; tautology residual `0`; `ξ` mode-covariance; `D = 0.00479`; skewed-base overshoot; ranks `[1,3,7]`) — **the receipt for the three-routes content; currently missing from v8 §5. Add.** (`v7_m1_psg_gap_chiral_receipt.py` → paper 5's concern.) |
| References | KEEP-FULL (subset) → v8 refs | If §§4–5 port, v8 paper 3 must add: **Wen, PRB 65, 165113 (2002)** (superselection/PSG hallmark); **Sakharov (1967)** + **Dvali, Fortsch. Phys. 58, 528 (2010)** (the form-only isomorphs). Levin–Wen, Nielsen–Ninomiya, Ginsparg–Wilson, Wen 2013 → v8 paper 5's ref set (its own audit's business; note NN and Levin–Wen currently absent there too). |

## Source 3: v7 paper17 — the classification theorem → v8 §§3–4 (+ §1)

| Section | Class | Destination / notes |
|---|---|---|
| Status header + tags | DROP | v7 bookkeeping. Tag mapping: [OWNED]→[DERIVED]; [STRUCTURAL]/[ANALOGY]→**[POSITED-STRUCTURAL]** (v8's tag — add to the status-line tag list); [CONVERGENT]→"convergent-not-novel" prose; [CONTESTED]→reported (v8 paper 2 owns); [IMPORT]/[NO-GO]/[CONDITIONAL] unchanged. |
| Scope guards 1–4 | KEEP-FULL (1, 4) / KEEP-RESULT (2, 3) → v8 §4 | Guard 2 (G-leg conditional) and guard 3 (residual types genuinely differ) are already in v8 ✓. Guard 1's **three convergent lineages by name** are not: `G`-un-derivable = the Jacobson/Verlinde tradition; local-tomography-least-motivated (the complex-over-real selector) = the Hardy / Barnum–Müller–Ududec / Masanes–Müller / **Höhn** lineage; mass-spectra-measured = SM orthodoxy (`~19+` measured parameters). Guard 4 (hard theorems not owned: MIP\*=RE, Slofstra, the reconstruction lineage, Tsirelson/NPA, the 2026 constructions) ports with §6. v8 §4's "field-convergent pieces … are imports" clause is the compression — expand. |
| §1 abstract | KEEP-RESULT | Superseded — v8's abstract already carries the full result at the corrected grade ([POSITED-STRUCTURAL] demotion, three statuses, "fence forced, filling free, three ways"). |
| §2.1 the schema | KEEP-FULL → v8 §3 | v8 has the (A)/(B)/(C) content compressed ✓ and the theorem statement at the only-invariants strength ✓. **Missing: the per-wall instantiation of `P`** — for SCALE `P` = the gravitational equation of state / the click-law's gauge-triviality; for TENSOR `P` = the transverse moment-positivity `Γ ⪰ 0`; for MODE `P` = the within-sector firing-form fixing. v8 §3 says "Let `P` be the self-consistency principle" without ever saying which principle each instance uses — the schema floats without it. |
| §2.2 what the schema is / is not | KEEP-RESULT → v8 §§3/4 | The moduli reading is in v8 §3 ✓ (verbatim-grade). Port one sentence of the three-claims discipline: that the schema *holds* is verified (receipt); that it is *novel as a unification* is a positioning claim; that any single wall is novel *in isolation* is disclaimed. |
| §3.1 SCALE instance | KEEP-FULL → v8 §§1.2/3 | **The receipt-anchored instance (audit brief's expected big item).** Missing from v8: (i) the `g_λ` **definition** — the unit-relabeling automorphism `A_rec ↦ λ·A_rec`, *identity on the record sector `R`*, `l = A_rec^{1/2}` so `l ↦ μ·l` with `μ = λ^{1/2}`, weight-`w` transform `g_λ(X_w) = μ^w·X_w` — without it "every intrinsic functional factors through `R` hence is `g_λ`-invariant" is an assertion, not a derivation (Theorem G itself stays a v6 paper 6 citation, fine); (ii) the **1-parameter-group composition law** `g_{μ₁} ∘ g_{μ₂} = g_{μ₁μ₂}`, identity `μ = 1`, inverse `1/μ`, sympy-exact; (iii) the paradigm gradings incl. the **control**: pure number `c₀` weight `0`; genuine ratio `l³/l³` weight `0` vs unequal-weight control `l³/l` weight `2`; (iv) **the two-Λ distinction** — `weight(G·Λ₀) = 0` with `Λ₀` the weight-`(−2)` cosmological constant (inverse area; `S_dS = π/(G·Λ₀)` fixes only the *product*) vs the **distinct** `weight(G·Λ_uv²) = 0` with `Λ_uv` the weight-`(−1)` inverse-length UV/spectral cutoff — "a distinct weight-`0` object … not the same `Λ`", linked-never-equal alongside `G·σ_A = 1/4`. **v8 §1.2 currently lists `weight(G·Λ₀) = 0` in its grading list and writes the unified invariant as `κ·σ_A = G·Λ² = const` with no disambiguation — a reader cannot tell the two Λ's apart; this is both a self-containment gap and a live confusion risk.** (v) the p15c cross-check content that belongs to this wall: `σ_A` weight `−2` in **bijection** with `G` weight `+2` via `G·σ_A = 1/4` (the everpresent-Λ factorization half → v8 paper 4 §4, its home). The residual list (`l_step` +1, `G` +2, `d`/metric/4-vol all weight ≠ 0) ✓ in v8; the c_m not-in-this-orbit parenthetical ✓ in v8 §2; the six-lever collapse ✓ in v8 §1.2 at citation level (v6 57/XI — acceptable per audit rule). |
| §3.2 TENSOR instance | KEEP-FULL-lite → v8 §3 | **The R instance (expected big item).** The mechanisms (M1/M2 proofs, `Q̃∖Q` permanence, the 2026 controversy) live in v8 paper 2 ✓ (verified: §M2 carries `K = 3/4/10/16`, deficit `+1`/`0`, `ker R`, Wootters ref, `|ΔI_σ| < 10⁻¹³⁰`; §5 the contested status) — series-internal cross-ref is the intended design. **But paper 3's own classification row must still state the triple with its numbers** (its receipt `p17` re-derives them sympy-exact): `R` = qubit-pair ↔ rebit-pair; preserves marginals, cross-moments, the click-law `p(a,b|x,y) = (1+ab·E_xy)/4`, and `I_σ`; changes the info-dimension count `K_single = 3/4` (`d(d+1)/2` vs `d²`), `K_AB = 10/16` (`D(D+1)/2` vs `D²`), deficit `+1` rebit (local tomography fails, `10 ≠ 9`) / `0` qubit; residual in `ker R`. **Port the true-by-design honesty note** (carried from v7 paper 12): the `I_σ` equality is true-by-design at the shared Tsirelson point — both correlators live in the `X–Z` plane `R` preserves — the load-bearing field-distinguishing computation is the `K`-deficit, not this equality. **Grep-verified this caveat is currently in NEITHER v8 paper 2 nor paper 3 — port it here (or add to paper 2).** Cross-check receipt count on port: `p6` = 43/43 (ledger), 7 SDP-flagged. |
| §3.3 MODE instance | KEEP-FULL → v8 §3 | **The G₃ torsor analysis (expected big item).** v8 §3 has the correct compressed conclusion (moves continuous zeros, fixes the label, [POSITED-STRUCTURAL]) ✓. Missing: (i) the **honest-up-front framing** — for SCALE/TENSOR one group does both schema jobs *on the same object*; `G_3` does not, and the difference is load-bearing; (ii) clause (A) as a derivation: the rank `2ⁿ − 1` is read off the **parity-character Gram = I** (orthonormal characters, sympy-exact), and a symbolic offset `c_r` is **absent from the Gram** (the receipt recomputes it after applying `c_r`) — v8 has only the one-clause parenthetical; (iii) the **affine-torsor structure**: composition adds zeros, identity `c = 0`, inverse `−c`, **no distinguished basepoint** = exactly "no common energy zero"; (iv) the **stabilizer analysis**: sector-permutations carry rank-`r` to rank-`r` ledgers; within-sector sign-flips/index-permutations also fix Gram, rank, AND label — so they too are disqualified as residual-movers; the multiset `{1,3,7}` sits in the stabilizer of `G_3`; (v) the continuous-vs-discrete **conflation argument** spelled out (what `G_3` moves is `c = (c_r) ∈ ℝ³`, a *different object*; the label is *downstream of* — selected *by* — the missing reference, not an orbit coordinate; the identification would be a continuous-vs-discrete conflation, hence [POSITED-STRUCTURAL], never [THEOREM]); (vi) the receipt's per-clause tags ((A) [THEOREM], (B) [THEOREM] on the *zero*, (3.3) the label-fixing check [STRUCTURAL]). The `s_mode` cross-check items (argmin switching; `α = 0` form-dependent — extensive/intensive/action/binding pick different ranks; freedom relocates to the *choice of Hamiltonian itself*) port with Source 4 §3.3. |
| §3.4 roll-up table + tag footnote + C-checks | KEEP-FULL → v8 §§3/5 | **The classification deliverable — v8 is prose-only.** Port: the 3-row table (`G_i` / invariant / residual / residual type / tag), with the annotations that matter (c_m rides the MODE row, not SCALE's; the MODE residual cell carries "`G_3`-fixed; the continuous zero is what moves"); the theorem-vs-structural footnote (one `g_λ` does both jobs on the same object; one `R` does both; `G_3` does not — the stated asymmetry); the four `[CLASSIFICATION]` checks C.1 fence-not-filling per instance (with MODE's caveat inline), C.2 residual types distinct, C.3 the grading two+one, C.4 all triples instantiated → fold into v8 §5's description of `p17`. |
| §4 status table + intro | KEEP-FULL → v8 §§3/4 | The 3-row epistemic-status table (leg / residual / status / status word). v8 has the statuses in prose ✓ and "no clean three-experiment-fixed symmetry" ✓; the table form carries the per-leg qualifiers (MEASURED-but-route-CONDITIONAL; IMPORT-FIXED intermediate in contestedness; CONTESTED possibly-unfixable, "a residual *the world itself may not carry*"). |
| §4.1 the G-leg caveat | KEEP-FULL → v8 §§1.1/4 | **The four named premises + the internal asymmetry — the conditional grade's actual content.** v8 §1.1's CONDITIONAL(...) tag names three items compressed; port: **(i)** the Jacobson–Clausius premise itself (the Padmanabhan "interpretation-not-derivation" locus); **(ii)** axiom (R) with the honest split — Gibbs thermality at *some* β is **unconditional** (record passivity / a finite Lenard theorem); the *value* `β = 2π` — hence `T = a/2π` and `η = 1/4G` — is the axiom; **(iii)** local equilibrium `θ = σ = 0` (Eling–Guedens–Jacobson: dropping it forces the dissipative `f(R)`/Lovelock branch — the premise is load-bearing); **(iv)** the continuum focusing gate `θ' = −R_{kk}` (reduced to two native conditional gates: finite double-null affine-pair readout + cofinal-tightness/no-silent-refinement). Then the **internal asymmetry, absent from v8 and load-bearing**: the §1.2 weight-counting no-go does **not** depend on axiom (R) — that is the structural half the receipt verifies; what rides the Jacobson route is the *identification* of the missing weight-carrying unit with gravity's `G`. So "the G *leg of the classification* is conditional" is precise, and v8's §1.1/§1.2 split implicitly encodes it but never states it. (All four premises have citable v6 paper 57 §1 homes — the *enumeration and asymmetry* must still appear in v8 prose.) |
| §4.2 mode/tensor statuses | KEEP-RESULT → v8 §§3/4 | Mode import-fixed ✓ in v8. Tensor contested: v8 defers to paper 2 §5 ✓ (verified paper 2 carries the 2021/2026 material and the vindication-never-proof framing; LEDGER confirms the intentional double home: *derived* in paper 2, *classified* here). Keep the deferral; no port needed beyond the status-table row. |
| §4.3 headline + orthogonal-axes guard | KEEP-FULL (guard) → v8 §§3/4 | Headline ✓ in v8 abstract. **The orthogonal-axes guard is absent:** `G` is a *scale* (a weight-`(+1)` grading obstruction along the `1/l_step` direction — keep l_step +1 / `G` +2 exact on port); the masses a *spectrum anchor* (a mode); the tensor product a *field/composition* bit (in `ker R`) — conflating "all three are imports" with "all three are the same epistemic object" is exactly the error the grading corrects. One short paragraph; port. |
| §5.1 convergent walls | KEEP-FULL → v8 §4 | The three lineages with names and one-line content each (see scope-guard-1 row). v8's single compressed clause fails "self-contained ≠ compressed" for a claim ("convergent-not-novel") that does real work in the novelty accounting. |
| §5.2 the novel content | KEEP-RESULT → v8 §4 | v8 ✓ has the (A) unification + (B) grading + (C) placement-of-c_m triple. Note: v8 intentionally swaps paper17's (C) (un-forceability-of-the-filling — now v8 paper 2's own result) for the c_m placement — coherent with the LEDGER routing; no action. |
| §6 what SHARD does not own | KEEP-FULL-lite → v8 §4 + refs | The five-item imports list, absent from v8 as a list: (1) MIP\*=RE (Ji–Natarajan–Vidick–Wright–Yuen 2020) + Slofstra 2019 — the `Q̃∖Q` permanence rides on them verbatim; (2) the Hardy/BMU/Masanes–Müller/**Höhn** lineage (SHARD's `M2` a re-localisation, not the origin); (3) the entropic-`G` argument field-shared and premise-conditional — **with the Visser attribution datum**: Visser (JHEP **10** (2011) 140, arXiv:1108.5240) and Kobakhidze target *Verlinde* and explicitly exempt Jacobson ("do not directly impact on either Jacobson's or Padmanabhan's versions") — so they do NOT underwrite this leg's conditionality (Padmanabhan + EGJ do); include the title disambiguation footnote (Gao vs Kobakhidze) only if the exemption note ports; (4) Tsirelson 1980 / NPA (+ the Barandes–Hasan–Kagan re-derivation mention — already in v8 paper 2); (5) the 2026 real-vs-complex constructions (stay in paper 2 ✓). Close with the owned-list sentence (unification, grading; un-forceability → paper 2). |
| §7 conclusion | DROP | Superseded by v8 abstract; contains the "exactly the `G_i`-invariants" phrasing — the known overclaim the v8 target already corrects (only-invariants form). |
| Reproducibility | KEEP-RESULT → v8 §5 | v8 has p17 23/23, p15c 27/27, s_mode 16/16 ✓. **Add:** the declaration "**no SDP and no solver-tolerance digit in `p17` — purely algebraic; no float64 anywhere**"; the re-run-not-trusted / numbers-re-derived-live discipline sentence; the `p6` cross-check at the corrected **43/43** (7 cvxpy/SCS solver-tolerance `~10⁻⁹`, flagged) or an explicit deferral of `p6` to v8 paper 2's receipt list. |
| References | KEEP-FULL (subset) → v8 refs | **Missing from v8 paper 3** (currently prose-mentions without citations, or absent): Padmanabhan, Rep. Prog. Phys. **73**, 046901 (2010); Eling–Guedens–Jacobson, PRL **96**, 121301 (2006); Verlinde, JHEP **04**, 029 (2011); Visser, JHEP **10** (2011) 140, arXiv:1108.5240; **Höhn** (add to the reconstruction-lineage list); Slofstra, Forum Math. Pi **7**, e1 (2019) + Ji et al., arXiv:2001.04383 (full cites here, or keep the explicit "as in paper 2" deferral — but §6's imports list reads better with them). The 2026 quartet (Hoffreumon–Woods; Maioli–Curado–Gazeau; Renou Comment; Renou 2021) stays in v8 paper 2 ✓. Wald, Jacobson, Hardy/CDP/MM/BMU already in v8 ✓. |

## Source 4: v7 paper14 — §3 mode-relocation NO-GO half only → v8 §2

(Per the audit brief: §§1–2 and §4's constructive/values content are v8 paper 5's — verified present there: the iGW flow with `M(g²=4)` anchors and count-vs-index, the singly-gated ratios, the closed-route no-gos, `s_mode_import_wall.py` listed "shared with paper 3".)

| Section | Class | Destination / notes |
|---|---|---|
| Scope guards | KEEP-RESULT (guard 4) → v8 §2 | One clause: the mass is *mode-relative* — the coupling `g` and the absolute mode are imported; the mechanism is owned, no absolute number is produced, and this paper *proves the wall genuine*. Guards 1–3 (large-`N`, 2d, quenched; not the Clay gap) → v8 paper 5 ✓ (present). |
| §§1–2 (constructive iGW flow) | OUT OF SCOPE | Destination v8 paper 5 (its audit's business). |
| §3.1 rank = superselection invariant | KEEP-FULL → v8 §2 | v8 has only "(the ledger rank `2ⁿ − 1`)" + the Gram-orthogonality parenthetical. Port the definition and the theorem: a seal mode is a **ledger of orthogonal primitive characters of the parity group `(ℤ/2)ⁿ`**; its rank `= 2ⁿ − 1` (`1/3/7` for `n = 1,2,3`); the characters are orthonormal (Gram `= I`, sympy-exact); the rank is preserved by **every record-internal move** (sign-flip, permutation, the firing map) ⇒ distinct ranks are gauge-inequivalent sectors — the Wen-PSG hallmark. This is the object the whole §2 wall is about; without it "ledger rank" floats. |
| §3.2 the common-zero obstruction | KEEP-FULL → v8 §2 | v8 has the shift + state-independence ✓. Port the derivation frame: the escape route needs a **cross-sector ground-energy comparison**, hence a common energy zero across gauge-inequivalent sectors; each sector measures content only against **its own uniform reference `U_r` of dimension `2ⁿ`** (the `D(P_r‖U_r)` normalization); re-referencing across sectors shifts content by exactly `(n′ − n)·ln 2`, state-independent (**`H(P)` cancels identically**, sympy-exact); so each sector's zero is free up to an independent additive constant the records never fix — the comparison the selector needs does not exist record-internally. |
| §3.3 the choice relocates, never closes | KEEP-FULL → v8 §2 | **The relocation-crossings analysis (audit brief's expected big item) — v8 has the headline numbers only.** Port: the construction `F_α = C_ext − α·ln(modes + 1)` with `C_ext = m·C` the *extensive* (summed) content — explicitly distinct from §3.2's `D(P_r‖U_r)`, the common-zero core independent of which is used; the argmin **switches rank across level-crossings** as `α` sweeps (`α = 0.168 / 0.207 / 0.247`); **no crossing equals any forced click-law constant** — nearest is `W = 0.1561` at distance `0.012` (`η_B = 0.6094` is `0.44` away) [v8 states "0.156 — none coincide" but not the distances that make it checkable]; the switching is **structural** — it happens for *every* monotone multiplicity reward (log, linear, spin, `√`, squared), not a rigged form; **even `α = 0` is convention/Hamiltonian-form dependent** (per the `s_mode`/`p17` cross-check: extensive / intensive / action / binding forms pick *different* ranks); conclusion — a mode-selecting Hamiltonian does not break the wall, it **relocates the undetermined choice into its own free coupling** (v8's "the argmin is coupling-dependent" is the compression). |
| §3.4 the import-family membership | KEEP-RESULT → v8 §§2/3 | The three-last-inputs grading sentence (scale a weight-`(+1)` grading obstruction, genuinely measured; `χ_AB` weight-`0` field/symmetry obstruction, contested convention; mode weight-`0` *relocation* obstruction, import-fixed) — v8 §3 carries this ✓. **DROP the parenthetical "(Paper VIII named it the 'fifth wall' …)"** — the ordinal correction; v8 correctly carries no ordinal. |
| §4 values forced / ratios singly gated | KEEP-RESULT (wall side only) → v8 §2 | One sentence: between-species mass ratios are **singly** gated — by the rank *assignment* alone (= this wall); the chirality-bridge and PSG-cohomology gates collapse *into the assignment* (they fix the species *name*, never the gap *value*). Values, within-pair ratios, assignment-free limits → v8 paper 5 ✓ (verified present, including the sharpening). |
| §5 the matter-sector verdict | KEEP-RESULT → v8 §2 | The boxed line's wall half: "own the gap mechanism, import the mode-selector"; and the precise closer — no ratio is computable *not because the gap is unknown* (a forced number to 100+ digits) but because *which gap belongs to which particle* is the one import. v8 §2's closing status sentence covers the first half; add the second. |
| §6 open residues | KEEP-RESULT | Item 4 (the mode import: closing requires an external input — like `G`, genuinely measured; unlike `χ_AB`, whose closure is itself contested) ✓ already in v8 via the three statuses. Items 1–3 → v8 paper 5 ✓ (present as its scope guards). |
| Reproducibility | KEEP-RESULT → v8 §5 | `s_mode_import_wall.py` 16/16 ✓ listed in v8. **Add the composition + anchors** that back §2's numbers: 2 sympy-exact / 11 mpmath `dps = 120` / 3 structural / 0 lattice-numeric; `η_B = 0.6093778634360062` (residual `9.2 × 10⁻¹²⁸`), `W = 0.1561092001572401`. |
| References | KEEP-RESULT | Wen, PRB **65**, 165113 (2002) → add to v8 paper 3 with §3.1's port (same ref as Source 2 §4). GW/Neuberger/Lüscher/Gross–Neveu/Banks–Casher/NN → v8 paper 5's set (Neuberger and NN currently absent there — note for that audit). Companion v7 refs → v8-internal (papers 1/2/5) or v6 (papers 9/14/22, 57). |

---

## Cross-cutting

- **Tag normalization on port:** [FORCED]→[THEOREM]/[DERIVED]; [PERMITTED]→[DERIVED (weight-`0` eligible)]; [BLIND]→[NO-GO]; [FREE]→[IMPORT (one free calibration)]; [SYNTHESIS]→positioning prose (paper 5); [FORM-ONLY]→[DERIVED (form)] + [IMPORT (magnitude)]; [GATED]→[NO-GO]-gated; [OWNED]→[DERIVED]; [STRUCTURAL]/[ANALOGY]→**[POSITED-STRUCTURAL]**; [CONVERGENT]→"convergent-not-novel" prose; [CONTESTED]→reported (paper 2 owns). **Add `[POSITED-STRUCTURAL]` to the status-line tag list** (currently only `[POSITED]` is enumerated while the body uses the compound tag).
- **v7-pointer hygiene:** all "Paper III/V/VI/VIII/XII/XIII/XIV/XVI §n" cross-refs become v8-internal section refs (papers 1/2/3/5) or v6/publishable citations; paper17's in-corpus anchor "Paper XVI" for the grading → v8 paper 2 §5 (its v8 home, LEDGER-confirmed). Receipts stay physically in `../v7/code/` (v8 §5 already declares this) — code paths are not citations.
- **DROP class (uniform reasons):** v7 status headers and paper-count framing ("fifth/eighth/fourteenth/seventeenth paper of the v7 program"); per-source abstracts and conclusions (superseded by the ported bodies); **all "fifth wall"/"fifth instance" ordinals** (the P6-vs-P8 collision — v7 paper6 §6 and paper8 §4 assign the same ordinal to different walls); paper17 §5.2(A)/§7's "exactly the `G_i`-invariants" phrasings (the corrected overclaim; the §2.1 only-invariants form is what ports); priority narratives.
- **Cross-paper coherence items (not paper-3 ports, but exposed by this audit):** (a) **paper8 Part A is currently unrouted in v8** — paper 3's status line claims to supersede v7 paper 8 entire, but its constructive/synthesis half (three pieces + "ahead on chiral fermions" positioning) is in neither paper 3 (correctly — out of scope) nor v8 paper 5 (grep-verified); recommend a compressed synthesis paragraph in v8 paper 5 with v6 papers 9/14 citations, or narrowing paper 3's supersession claim to "the wall half of 8". (b) The **true-by-design `I_σ` caveat** (v7 paper 12 via paper17 §3.2) is in neither v8 paper 2 nor 3 — port with the TENSOR instance here or add to paper 2. (c) v8 §1.2's `G·Λ²` (unified invariant) vs `weight(G·Λ₀) = 0` (grading list) currently read as one `Λ` — the §3.1 port resolves it; until then the target is internally ambiguous.
- **Receipts to add to v8 §5:** `p5_cm_calibration.py` (10 checks, dps ≥ 60 CODATA + sympy-exact); `m2_mode_canonicalization.py` (10/10, dps = 100); the `p17` "no SDP / no solver-tolerance digit / no float64" declaration; the `s_mode` composition + `η_B`/`W` anchors; `p6` at the corrected **43/43** (7 SDP-flagged) or an explicit deferral to paper 2.
- **References to add (consolidated):** Padmanabhan RPP **73**, 046901 (2010); Eling–Guedens–Jacobson PRL **96**, 121301 (2006); Verlinde JHEP **04**, 029 (2011); Visser JHEP **10** (2011) 140 / arXiv:1108.5240; Höhn (lineage); Wen PRB **65**, 165113 (2002); Sakharov (1967); Dvali Fortsch. Phys. **58**, 528 (2010); Srednicki PRL **71**, 666 (1993); Chamseddine–Connes CMP **186**, 731 (1997); Slofstra Forum Math. Pi **7**, e1 (2019); Ji et al. arXiv:2001.04383 (or explicit paper-2 deferral for the last two).
- **Size estimate:** the ports take v8 paper 3 from ~13 KB to roughly **40–50 KB** — in line with "self-contained, NOT compressed". §2 grows the most (one paragraph → the full mode-wall apparatus + the c_m calibration block); §3 second (the three receipt-anchored instances + the roll-up and status tables); §§1/4 gain the four-premise enumeration, the internal asymmetry, the guards, and the imports list.
