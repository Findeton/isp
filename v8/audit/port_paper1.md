# V8 self-containment audit — destination: v8 Paper 1 (foundations / click-law)

**Sources swept section-by-section:** v7 Paper 1 (`relativistic-isp-v7-paper1-record-click-law.md`), Paper 2 (`relativistic-isp-v7-paper2-content-supply.md`), Paper 3 (`relativistic-isp-v7-paper3-seal-spacing-no-go.md`).
**Target:** `../relativistic-isp-v8-paper1-foundations-click-law.md` (read 2026-07-01).
**Rule applied:** v8 must be the *better*, self-contained version (v7 → log-only; v6 stays citable). Corrections respected: v8 (a)/(b1)/(b2)+GMH+q2 premise-ledger style is canonical; χ-additivity **[DEFINITIONAL]**; T3 = premise-dependent **dissolution** (not used); `d = W_*` **denied**; `f5 = 21/21`; v6 citations allowed.

**Headline finding.** The v8 paper fully and correctly contains v7 Paper 1's *forced-shape* chain (§§3.1–3.6, ledger, receipts) — but it contains essentially **nothing of v7 Papers 2–3** beyond three asserted clauses (ceiling `d ≤ W_*`, "denied by the sub-capacity firing law", "d is [OPEN]"). The coboundary content-supply theorem, the `W_* = max min(C,J)` capacity derivation, the sub-capacity firing law (the actual denial mechanism), the three-object discipline for `d`, and the grading-trichotomy cross-tier no-go (with G1 and the open-doors ledger) must be ported, or v8 Paper 1 asserts results whose proofs will live only in the frozen log. Recommended vehicle: a **new top-level section** (between current §3 and §4): *"The content supply and the seal spacing"* with 4 subsections (S1 coboundary clock; S2 capacity + ceiling; S3 firing law / three objects; S4 cross-tier no-go + open-doors ledger), plus rewrites of §6.1/§6.3 and additions to §7 receipts and References.

---

## Port table

Class key: **KEEP-FULL** = must be ported (not adequately in v8); **KEEP-RESULT** = statement+receipt already in v8, no action; **DROP** = superseded/wrong/pure-process, with reason.

### A. v7 Paper 1 — record click-law

| # | Source | Content (one line) | Class | Receipts | v8 destination |
|---|--------|--------------------|-------|----------|----------------|
| A1 | §1 | Record primitive spelled out: `D = (Ω_D, μ_D, screens, collar, transports)`, holonomy defect `Δ_AB = T_B T_A − T_A T_B`; do-delete no-go attributed specifically to **v6 Paper 56**; the "quarantine" rationale (tiering isolates the hard relativistic-localization problem into Tier B as an *emergence* question) and the explicit list of geometry-presupposing constraints excluded as axioms (microcausality, LI, local kernel `f(s²)`, equilibrium premise) | **KEEP-FULL** (small) | — | §1 (one added sentence each: the tuple + `Δ_AB`; "v6 Paper 56" for do-delete; the quarantine sentence) |
| A2 | §2 | One-diamond self-consistency law **KL content = Fisher capacity**: `η·tanh η − log cosh η = 1 − tanh²η`, root `η_* = 1.090344354879492…`, `θ_* = tanh η_* = 0.797003794162878…`, `W_* = 1 − θ_*² = 0.364784952089976…`, balance residual `9.7×10⁻¹²²` (dps 120; re-verified `1.3×10⁻¹⁴¹` at dps 140 in p2a) | **KEEP-FULL** | `f3_self_consistency.py`, `p2a` | New supply/spacing section, subsection S2 — v8 currently *uses* `W_*` (§3.4) and even cites the `9.7×10⁻¹²²` residual (§7) without ever defining the equation or constants |
| A3 | §3.1 | Seal = orthogonal projection: result + oblique-idempotent discriminator + receipt | KEEP-RESULT | `f1_born_projection_q2.py` | already §3.1 |
| A4 | §3.1 | The *mechanism* line: in a 2-norm an idempotent is norm-non-increasing **iff** self-adjoint; hence `q = 2E−1` is a self-adjoint reflection, `q² = 1`; purely algebraic, no geometry | **KEEP-FULL** (small) | `f1` | §3.1 (one sentence — gives the *why*, not just the *that*) |
| A5 | §3.2 (bridge para) | CK⟺interference kinematic identity (defect = cross-term entrywise, `Σ|·|²` vs `|Σ·|²`), gap `6.6×10⁻¹⁴²`; `0.329` unsealed / `0.0` at a seal | KEEP-RESULT | `f3d` | already §2 row (iv) + §3.2 (optionally add the entrywise `Σ|·|²` vs `|Σ·|²` formula) |
| A6 | §3.2, §5.0 | Bridge "**itself-forced**" claim (ingredients `Γ=|U|²` + orthogonal pointers "Tier-A-forced", so the bridge is not a posit) and "collar Markov-presentability *forced* by T3+T4, not a separate assumption" | **DROP** | — | Superseded by the v8 corrections: GMH bridge is **[POSITED]** model class (biconditional forced *within* it); **T3 = premise-dependent dissolution, not used**; collar Markov-presentability **[CONDITIONAL]** on the T4 finite-primitive-collar arena. v8 §2 row (iv) + §3.2 already carry the corrected form |
| A7 | §3.2 (odometer) | "Additivity is a **theorem of arrow-positivity**" status | **DROP** | — | Superseded: **χ-additivity is [DEFINITIONAL]** (a defined accumulated sum telescopes), with the segment-level identity wired to the [CONDITIONAL] collar clause — exactly as v8 §3.2 states. Do not port the old status |
| A8 | §3.2 (odometer), §7 | Sequential-vs-**parallel** additivity distinction: the tensor cocycle `A_{D₁×D₂} = A_{D₁} + A_{D₂}` holds machine-zero for *parallel* diamonds but is "the wrong additivity here" — distinct from the commit-order telescope | **KEEP-FULL** (small) | `f3c` (telescope `7.7×10⁻¹²¹`; parallel cocycle gap `0.0`) | §3.2 or §7 (one clause; prevents a real conflation and is already receipted) |
| A9 | §3.2 (dense) | Cauchy equation, Aczél uniqueness, `p = 1`-only scan (`2^p − 2 = 0`), product limit from any stationary contraction; (b1)/(b2) counterexample gaps `0.317`/`0.18` | KEEP-RESULT | `f3`, `f3e` | already §2 + §3.3 (optionally add the worked example `f = 1/(1+dχ) → exp`) |
| A10 | §3.2 (sparse) | Skeleton `S(nd) = S(d)ⁿ` by finite induction; free profile = the genuine-quantum content; `d→0` recovery | KEEP-RESULT | `f3b` | already §3.4 |
| A11 | §3.2 (sparse) | The **two-regimes-one-limit identity**: `S(d)^{χ/d} = exp(−κχ)` exactly on the lattice for every `d`, with `κ = −log S(d)/d`; squeeze amplitude `B(d) = s^k(1−s) → 0`, `∼ κd·s^k` small-`d` | **KEEP-FULL** (small) | `f3b` | §3.4 (the explicit formula unifying dense/sparse — currently only prose "recovered as d→0") |
| A12 | §3.2, §5.3 | `d = W_*` as a "plausible but unforced candidate" | **DROP** | — | Superseded: **`d = W_*` is denied** by the sub-capacity firing law (v7 Papers 2–3); v8 §3.4 already carries the denial. Port the *denial mechanism* (rows B5–B7, C2–C4), never the candidate framing |
| A13 | §3.3 | No-revival core: non-composing point is not a seal; `κ ≥ 0` | KEEP-RESULT | — | already §3.5 (v8 adds the correct "unfalsifiable-by-construction" honesty + Tier-B deferral) |
| A14 | §3.3 + §5.3 | The **committed-survival vs inter-seal-coherence** distinction: between seals the coherence may rise transiently, and that non-monotone freedom **is the same object** as the revival fingerprint of the decoherence program (free profile ≡ observable revival signature); plus §5.3's sharp form: sparse-vs-dense is a decidable **computation on the decoherence functional `D`** (off-diagonal support between commitments or not) | **KEEP-FULL** (small) | — | §3.5 (one sentence) + §6.3 (the computation-on-`D` criterion; currently v8 only says "deferred to the phenomenology paper") |
| A15 | §3.4 | One free scale `κ`; weight-0 `χ`; κ↔gravity identification a structural analogy, tagged | KEEP-RESULT | `f4` | already §3.6 |
| A16 | §3.5 | Entanglement guardrail normative sentence: **locality must never be imposed on the Tier-A correlation structure**; only emergent Tier-B dynamics is local, and only as no-signaling; the Bell address named (keep PI / drop OI) | **KEEP-FULL** (small) | — | §4 or §6 item 2 (v8 has the OI/PI *silence* result + `f5 = 21/21` but not the guardrail; keep `21/21`, do not port any other count) |
| A17 | §4 | **Tier-B detail** (expected big item — confirmed missing): (a) the regularity condition *named* (tame-class / cofinal-stability / Lorentz-invariant-sprinkling); (b) explicit parametrizations `χ(T) = λ₀T → exp(−κλ₀T)` vs `χ(T) = aT²/2 → Gaussian onset`, sympy-exact, residuals `~10⁻¹²²`; (c) the tie to v6 Paper X: the exp/Gaussian "shape is not a mechanism signature" result *re-derived* as Tier-A reparametrization; (d) kernel note "white-noise forbidden"; (e) the framing "the program's thermodynamics is the spacetime **limit** of the click-law — a consequence, not an assumption" | **KEEP-FULL** | `f3` (reparametrization identities), `f1_kernel_positive_type.py` | §5 (expand the single paragraph to carry (a)–(e); cite v6 Paper X for (c)) |
| A18 | §5.0–§5.3 | Residue ledger core items (supply / entanglement / placement+sparseness) | KEEP-RESULT (as corrected) | — | already §6 — **but §6.1 must be rewritten after the Paper-2 port (see row B8)** and §6.3 extended (row A14) |
| A19 | §6 | Claims / does-not-claim recap + one-line summary | **DROP** | — | Pure summary duplication; v8 abstract + §6 already carry the corrected versions (old [FORCED]-tag vocabulary superseded by the v8 tag system) |
| A20 | §7 | Precision discipline extras: float-safe items enumerated (integer composition counts, `p`-scan, projection booleans); parallel-cocycle machine-zero note | **KEEP-FULL** (tiny) | — | §7 (half a sentence each; v8 currently says only "float-safe items stated as such") |

### B. v7 Paper 2 — content supply

| # | Source | Content (one line) | Class | Receipts | v8 destination |
|---|--------|--------------------|-------|----------|----------------|
| B1 | §1 | Framing "leading candidate `d = W_*` flagged plausible" | **DROP** | — | Superseded (`d = W_*` denied); process framing |
| B2 | §2 | **The coboundary clock theorem** (expected big item — confirmed missing): `dχᵢ = ρᵢ = h_{i+1} − h_i` with exact telescope `Σρᵢ = h_b − h_a`; the discrete Poincaré lemma (`H¹ = 0` on a path) makes "concatenation-additive content ⟺ coboundary of a scalar potential" an *identity*; the corpus's source-as-coboundary law instantiates it; **two layers kept apart** — the form (lemma-level) vs the identification of `{hᵢ}` with the closed-holonomy cochain (the physical-input layer); corpus potential/increment numbers reproduced `< 10⁻¹⁰⁰` | **KEEP-FULL** (BIG) | `p2a_content_supply.py` (13 checks) | New section, subsection S1. **Status re-tagging required:** under the v8 correction the additive⟺coboundary equivalence sits on the [DEFINITIONAL] odometer (a lemma-level identity, not "a theorem of arrow-positivity"); the cochain identification is the [POSITED]/physical layer |
| B3 | §2 (end) + §4 | **The magnitude is [PHYSICAL]:** the law constrains only *differences* (coboundary form), never absolute size — a configuration and its tenfold rescaling both satisfy every constraint (verified); `κ` stays the untouched no-go floor | **KEEP-FULL** | `p2a` | Subsection S1 + **rewrite of §6.1**: v8 currently presents the supply as wholly "[OPEN — the decisive next question]", which is the *pre-Paper-2* state. Corrected residue: the clock's **form is forced** (coboundary), the **magnitude is physical configuration data** (not a law), `κ` measured — the open frontier moves to the *spacing* (row C10) |
| B4 | §3.1 | **`W_*` is a genuine capacity** (expected big item — confirmed missing): monotone opposition — accumulated content `C(η)` rises `0 → log 2` while Fisher capacity `J(η) = sech²η` falls `1 → 0`, single crossing at `η_*`; hence `W_* = max_η min(C, J)`; the scalar-`min` reduction *inherits (b1)* (the q=2 one-contrast screen collapses the matrix capacity to a scalar; multi-channel ⇒ vector capacity ⇒ Pareto frontier) | **KEEP-FULL** (BIG) | `p2a` (13), `p2b` (13) | Subsection S2 (with A2's constants). The (b1) tie-in belongs in the v8 premise-ledger style |
| B5 | §3.2 | **The ceiling `d ≤ W_*` derivation:** premise **(no-over-sat)** stated ("a committed record is self-consistent: `C(η) ≤ J(η)`"); composition-self-consistency gives *no* bound (`d ∈ (0,∞)` reconfirmed — it only evaluates at seal points); asymmetry: the ceiling needs only self-consistency of what commits, the *equality* would need (P-sat) | **KEEP-FULL** (BIG) | `p2a`, `p2b` | Subsection S2. v8 §3.4 keeps the bare bound + qualifier (KEEP-RESULT for the statement); the derivation and the (no-over-sat) premise must be in-paper |
| B6 | §3.3 | **The sub-capacity firing law** (expected big item — THE denial of `d = W_*`, currently only asserted in v8 §3.4): capacity law (v6 paper4 §5, `ψ″`-balance `C = J`, root `η_A`) and division-event firing law (§71–76, `ψ′`-balance `∂ψ/∂h = e^{−h}` on the complete primitive ledger) are **distinct and mutually consistent**; firing runs on a *random* evidence clock `S(I) = e^{−I}`, `I ~ Exp(1)`; roots **mode-dependent**: one-mode-parity `tanh η = e^{−η}` ⇒ `η_B = 0.609377863436006…`, `C(η_B) = 0.156109…`; coupled `{x,y,xy}` ⇒ `h_* = 0.495053…`, `C(h_*) = 0.109004…` — both `< W_*` | **KEEP-FULL** (BIG) | `p2b` (13), `p2c` (25) | Subsection S3 |
| B7 | §3.3 (structural argument) | **Sub-capacity is structural for the admissible class**, not per-case: `C′ = η·sech²η > 0`; reaching `W_*` needs the inadmissible cochain unit `λ ≈ 0.208 < 1` (same argument that rejects the `λ = 2` double-count); coupling only *dilutes* (`η_B: 0.609 → 0.495 → 0.368` over 1/3/7 characters) so one-mode `C(η_B) = 0.428·W_*` is the **supremum**; admissibility gates load-bearing: skewed base `μ(+) = 0.2` overshoots to `0.526 > W_*`, excluded by count-dual/orthogonality — the *same* primitive count-symmetry that fixes the §5 constants; `(P-sat)` "seal at capacity" is what the firing law contradicts (not §5); v6 paper4 §69 fixed-content threshold non-canonical, §71 closes the renewal-shape drift (Poisson shape, `r = 1`) leaving **mode-dependence** as the live non-canonicity; three objects never conflated (tilt `η_B` / evidence `I ~ Exp(1)` / content `C(η_B)`) | **KEEP-FULL** (BIG) | `p2c` TASK 6 | Subsection S3. This is the strict-for-the-gated-class content behind v8 §3.4's parenthetical "strict for the gated admissible class — the qualifier travels with the bound" |
| B8 | §5 | The honest spacing ledger: `composition → d ∈ (0,∞)`; `+ no-over-sat → d ∈ (0, W_*]`; firing commits strictly inside (sub-capacity, mode-dependent, Poisson clock); three quantities on three footings (bound / mode-set / random-and-κ-blocked clock — the evidence clock is in *evidence* units and converting to content spacing needs the no-go-walled `κ`); the `1/η_B = 1.641018 = ξ_1d` mass-gap thread (suggestive, weight-zero) | **KEEP-FULL** | `p2b`, `p2c`, `p3` | Subsections S3/S4 close + §6 residue. **Port route 2 in its post-Paper-3 form only** (see B9) |
| B9 | §5 route 2 | "The cross-tier bridge **remains a candidate**… a derived Tier-B gravitational dimensionless coefficient would pin the spacing" | **DROP** (as stated) | — | Superseded by Paper 3's **[NO-GO]**: the record-derived absolute-scale bridge is closed (trichotomy); what survives is only the *external-structural-identity* door + the `c_m` strength lane (rows C5–C9). Port the corrected door-status, not the candidate hope |
| B10 | §6 | Claims / does-not-claim recap | **DROP** | — | Summary duplication (content covered by B2–B8 ports) |
| B11 | §7 | Precision specifics: dps = 140; `W_*` residual `1.3×10⁻¹⁴¹`; p2a = 13 checks, p2b = 13, p2c = 25; roots to 12+ digits; skewed-base exhibit | **KEEP-FULL** | `p2a`, `p2b`, `p2c` | §7 receipts (v8 §7 currently lists **no** p2*/p3 receipts at all) |

### C. v7 Paper 3 — seal-spacing no-go

| # | Source | Content (one line) | Class | Receipts | v8 destination |
|---|--------|--------------------|-------|----------|----------------|
| C1 | §1 | **The three-object discipline** (content `C(h)` / capacity `W_*` / evidence clock `I`) + the four naive readings (a) fixed scalar, (b) fixed mean + fluctuations, (c) mode-dependent set, (d) irreducible distribution, sorted onto the objects; layered verdict **(c)+(b)/(d)** | **KEEP-FULL** | — | Subsection S3 opening (the taxonomy is the reusable discipline; v8 currently has none of it) |
| C2 | §2 | Single-fixed-scalar reading fails; the corpus-robust quantitative statement is the strict inequality **`d < W_*` in every admissible mode** [FORCED]; `d = W_*` is the one reading the firing law denies | **KEEP-FULL** | `p2b`, `p2c` | Subsection S3 (merge with B6/B7) |
| C3 | §3 | Content axis **[MODE]**: per-mode deterministic — the fixed point is the Euler equation of the strictly convex commitment potential `Φ(h) = ψ(h) + Σⱼ e^{−hⱼ}` (unique minimizer, Hessian > 0); the random draw does **not** enter it, so `Var[content | mode] = 0`; mode-indexed set monotone-decreasing under coupling, supremum `0.428·W_*`, no canonical member | **KEEP-FULL** | `p2c` | Subsection S3 |
| C4 | §4 | Evidence axis **[RANDOM]**: `S(I) = e^{−I}` derived from eventless RN/KL gluing `S(I+J) = S(I)S(J)` + exact self-accounting `−log S(I) = I` (fixes rate `λ = 1` and commitment ratio `r = 1`); `E[I] = Var[I] = 1` (exact quadrature, dps 140); evidence units ≠ content units — conversion needs `κ`, the no-go floor, so the clock cannot fix a content `d` either | **KEEP-FULL** | `p3_d_nogo.py` | Subsection S3 |
| C5 | §5 | **The grading trichotomy theorem** (expected big item — confirmed absent from v8; reusable tool): record-length gauge `g_λ` is a one-parameter group ⇒ weight is an additive grading homomorphism (v6 paper6 Theorem G); monomial `W_*^a κ^b l_step^c σ_A^e` has weight `2b + c − 2e`; dimensionless ⟺ `c = 2(e−b)` — the length power is carried entirely by cancelling `κ`/`σ_A` powers; irrational-weight leak closed (`a + b√2 = 0` over ℤ ⇒ `a = b = 0`); transcendental functionals of weight-0 ratios stay weight-0; **exhaustive trichotomy** for record-derived dimensionless coefficients: (1) *ratio-of-twins* (circular — `1/4 = G·σ_A`, `2π = κ·σ_A`, `G·Λ²`, `η = 1/4G`; SIGMA-SPLIT invariance verified under `l → μl`, `μ = 1, 1.7, 3`), (2) *scale-blind* (vacuous — Srednicki `a = 0.295417`, `U_G = 1`, `η/s = 1/4π`, `c/6`, graviton `4/9`), (3) *Tier-A-intrinsic* (wrong tier **and wrong axis** — `W_*` is the paper4 source-*amplitude* coefficient, not a spacing); the three requirements mutually exclusive, no fourth bin. Includes the **three-κ disambiguation** (SIGMA-SPLIT `κ = 8πG`, weight +2 ≠ Tier-A conversion `κ` ≠ paper4 amplitude `κ = W_*`) — port it, it prevents a genuine symbol collision inside v8 Paper 1 | **KEEP-FULL** (BIG) | `p3_d_nogo.py` (9 checks, sympy-exact) | Subsection S4. Cross-ref note: v8 Paper 3 (walls) owns the general scale no-go — state the trichotomy *tool* here where it is proved and used on `d`, and let Paper 3 cite/restate it; do not leave it only in Paper 3, since the `d`-application is a foundations result |
| C6 | §5 | **Algebraic numerology refutation** (honest negative, reusable method): `θ_B² = 0.295597742522…` vs Srednicki `a = 0.295417…` (gap `1.8×10⁻⁴`) is not an identity — `θ_B` is a root of `t³ + t² + t − 1` (`e^{η_B}` = the **tribonacci constant**, root of `x³ − x² − x − 1`), `θ_B²` a root of `s³ + s² + 3s − 1`, both irreducible over ℚ; the partial-wave sequence converges *toward* `a` and *away* from `θ_B²`; also `1/η_B = 1.641018` vs `π²/6 = 1.644934` (gap `3.9×10⁻³`); the `ξ_1d = 1/η_B` mass-gap identity is real but weight-zero record-internal (the second-scale attack collapses onto the invariant) | **KEEP-FULL** | `p3` | Subsection S4 |
| C7 | §5 | **Spectral-action route closed:** Chamseddine–Connes/Sakharov lands on the invariant `G·Λ²` (`Λ = 1/l_step` *is* the record-scale label) ⇒ fixes only the ratio `G/l_step²`, never absolute `G` — independently of the coefficient value in every scheme (`3π²`, `192π²/N`, …); the conjectured "factor-2π gap" is *plausibly* a **moment-labelling ambiguity** (`M0 = ∫e^{−2πu}du = 1/(2π)`, `M1 = ∫u·e^{−2πu}du = 1/(2π)²`, `M0/M1 = 2π`) — a candidate correction to the corpus's `[CONJECTURED]` value, unverified, convention-dependent, and **immaterial to the no-go** | **KEEP-FULL** | `p3` | Subsection S4 (keep the careful "plausibly/candidate/unverified" hedging verbatim in spirit) |
| C8 | §6 | The permitted **weight-zero strength lane**: `c_m = Gm²/ℏc` is weight-0, intrinsic-eligible, but a *strength/hierarchy*, categorically not the spacing (`c_m·σ_A` inherits the free scale, never produces it); spectral action = one unverified candidate generator | **KEEP-FULL** (brief) | — | Subsection S4 / open-doors ledger row; cross-ref the v8 paper that owns `c_m` calibration (v7 Paper 5's successor — gravity/matter volume) |
| C9 | §7 | **Gate G1 + completeness sweep** (the no-go's honest scope — mandatory if the no-go is ported): G1 = "no sealed law consumes `A_rec` outside the labeling map `ℓ`" — the *single* load-bearing premise; proved-finite for the executed corpus (clause table, worst gap 0), interface-level for un-imported texts, **not** a closure theorem against future laws; the seal-rate primitive refutes the sharpest counterexample (intrinsic rate with absolute scale, killed by the weight trichotomy `{0, −1, −2}`) but does **not** discharge the quantifier — never describe it as "closing G1". Sweep closing every other crack: grading anomaly → G1; negative-weight primitives are all chart/label images of `ℓ`; fractional roots stay labels (`weight(A_rec^{−3/7}) = −6/7`); `ℏ = c = 1` hides no second length (the residual length *is* `l_step`); log-running/transmutation ⇒ weight −1 = `l_step` again (reproduces SIGMA-SPLIT); every record mass gap = pure number × `l_step` | **KEEP-FULL** | `p3` | Subsection S4 (condensed but complete — the quantifier caveat is load-bearing honesty) |
| C10 | §8 | **The open-doors ledger** (5 rows: absolute-scale bridge CLOSED; spectral-action absolute route CLOSED; G1 OPEN scope assumption; `c_m` lane OPEN-permitted; external structural identity PERMITTED-undiscovered) + the sharpened obligation: *what fixes `d`* is well-posed and open, three live non-circular routes (completeness/canonicalization principle; external structural identity, `1/η_B` thread as suggestive entry; non-circular variational principle on the inter-seal stretch) | **KEEP-FULL** | — | Subsection S4 close (keep the table form) + **§6.3 rewrite** (v8's "Placement and sparseness [OPEN]" should absorb the spacing ledger or point to the in-paper section instead of only deferring) |
| C11 | §9 | Claims / does-not-claim recap + one-liner | **DROP** | — | Summary duplication of C1–C10 |
| C12 | §10 | Precision specifics: p3 = 9 machine checks; the tribonacci polynomials; spectral moments; SIGMA-SPLIT invariance values; `E[I] = Var[I] = 1`; dps 140 | **KEEP-FULL** | `p3` | §7 receipts |

---

## Cross-cutting actions (implied by the ports)

1. **New section** "The content supply and the seal spacing" (S1–S4 above), inserted after current §3 (renumber §4–§7). Every ported status re-expressed in the v8 tag system: coboundary form [DEFINITIONAL/lemma] + cochain identification [POSITED-physical]; ceiling [CONDITIONAL(no-over-sat; b1)]; firing-law facts [DERIVED] within the v6-paper4 arena; mode-dependence [MODE-qualified DERIVED]; evidence clock [DERIVED, RANDOM]; cross-tier bridge [NO-GO modulo G1]; `d` [OPEN].
2. **§6.1 rewrite** (row B3): supply no longer wholly open — form forced / magnitude [PHYSICAL] / `κ` measured; the open frontier is the spacing (point to the new section's ledger).
3. **§6.3 extension** (rows A14, C10): computation-on-`D` criterion + the open-doors ledger pointer.
4. **§7 receipts additions**: `p2a_content_supply.py` (13), `p2b_event_law_saturation.py` (13), `p2c_vector_ledger_roots.py` (25), `p3_d_nogo.py` (9) — all verified present in `../v7/code/` (live, version-independent per v8's own convention). Add the dps-140 `1.3×10⁻¹⁴¹` residual, float-safe enumeration, parallel-cocycle note.
5. **References additions**: v6 Paper 4 (§5 constants; §69 non-canonicity; §71–76 firing law), v6 Paper 6 (Theorem G, gate G1), v6 Paper 56 (do-delete; GMH bridge home), v6 Paper 57 / paper-XI (scale no-go, SIGMA-SPLIT), v6 Paper X (exp/Gaussian non-signature); external: M. Srednicki, PRL **71**, 666 (1993); A. Chamseddine & A. Connes, CMP **186**, 731 (1997). All v6 citations allowed; v7 Papers 1–3 remain only in the frozen-log line of the Status block.
6. **Guard rails for the port** (do NOT reintroduce): old [FORCED]-tag vocabulary; "additivity = theorem of arrow-positivity"; "bridge itself-forced" / "T3 forces the collar"; "`d = W_*` plausible candidate"; "cross-tier bridge remains a candidate" (pre-Paper-3 form); any `f5` count other than 21/21.

## Counts

- **KEEP-FULL: 24** rows (A1, A2, A4, A8, A11, A14, A16, A17, A20; B2–B8, B11; C1–C10, C12) — of which 7 BIG (B2, B4, B5, B6, B7, C5, A17).
- **KEEP-RESULT: 8** rows (A3, A5, A9, A10, A13, A15, A18-core, plus v8 §3.4's bare ceiling statement noted in B5).
- **DROP: 8** rows (A6, A7, A12, A19, B1, B9, B10, C11) — each with the superseding correction cited.
