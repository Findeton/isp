# V8 self-containment audit — destination: v8 Paper 6 (phenomenology / experimental reach)

**Sources swept:** v7 Paper 19 (`relativistic-isp-v7-paper19-seal-swerve-norevival.md`, section by section) + the un-papered receipt analyses `pK1_seal_information_rate.py`, `pRUN_kappa_exact.py`, `p19c_swerve_fdr.py`, `pFDT_seal_influence.py`, `pPRIN_seal_record.py`; routing checks for `pE_phase_causalset.py` (→ v8 Paper 4) and `f5_oi_pi_consistency.py` (→ v8 Papers 1/2).
**Target:** `../relativistic-isp-v8-paper6-phenomenology.md` (read 2026-07-01).
**Rule applied:** v8 self-contained vs v7 (v7 → log-only; v6 citable — v6 Papers 26/56/X may stay citation-level); NOT compressed. Corrections checked: tie POSITED; `κ_G` disambiguation; Donadi cite; species caveat; σ-bound OPEN; `N_BLP` scoping; quarter law + dissolution central.

**Headline finding.** v8 Paper 6 carries **all seven mandated corrections** and the full two-channel skeleton + quarter-law §1 in corrected form; nothing in it is *wrong* against the sources — the findings are omissions plus two stale cross-references. The three biggest gaps: **(1)** Channel-A derivation texture that makes §2 self-contained (the **weight-(−3) = mass³ grading of the magnitude** — the actual mechanism of SCALE-GATED and the site of the v7 review's dimensional fix — plus the kick-accumulation line, `1/6 = 1/(2(d−1))`, the species-caveat second half, and the un-cited Javed–Wilson-Ewing template); **(2)** the receipt-docstring analyses that survive nowhere but the log: p19c's **Einstein construction + tautology caveat + completion-owns-the-mass-dependence** (D3–D5), pK1's **asymmetry note and punted seal-is-record bridge** (B3–B4 — load-bearing: without it §1 silently conflates the which-path KL with the arrow σ, and paper 1's "free κ" would contradict §1's "forced ¼"), pRUN's **`724ε⁶/945` term, component closed forms, and asymmetric-slope exhibit** (C2/C3/C6), and pPRIN's **surviving overlap anatomy** (noise-even/phase-odd, `dφ` = unsealed holonomy, phase-cannot-revive; F1–F3) — while the pFDT **arg-F-vs-modulus failure verdict is verified already present** in §1; **(3)** two stale pointers: "paper 1 §6.1 / the open supply law" (now §7.1, magnitude-[PHYSICAL]) and the un-honored paper 1 §7.3 deferral of the functor/sparse-vs-dense discussion *to this paper*. Routing: `pE_phase_causalset` → Paper 4 landed as the null verdict but its **real-vs-complex analysis** (real weights provably cannot cancel; the needed `i` = the complex-over-real/tensor-product bit) is missing there and from `port_paper4.md` — flagged; `f5` → Papers 1/2 verified adequate. See per-section tables.

---

## Port table

Class key: **KEEP-FULL** = must be ported (not adequately in v8); **KEEP-RESULT** = statement+receipt already in v8, no action; **DROP** = superseded/duplicative/pure-process, with reason.

### A. v7 Paper 19 — seal-swerve and no-revival (section by section)

| # | Source | Content (one line) | Class | Receipts | v8 destination |
|---|--------|--------------------|-------|----------|----------------|
| A1 | Status + Scope guards 1–3 | Constrains-not-predicts; swerve inherited / tie posited; Channel B a plain negative | KEEP-RESULT | — | v8 abstract + §2 + §3 carry all three guards in corrected (tie-POSITED) form |
| A2 | §1 abstract | Two-channel summary | **DROP** | — | Duplication; v8 abstract is the corrected, fuller version (adds §1 quarter law, which v7 P19 lacked) |
| A3 | §2 | The two-axes framing: Channel A **geometric/kinematic** (momentum diffusion; particle spectra/heating experiments) vs Channel B **thermodynamic/open-system** (coherence decay; interferometric revival tests); one substrate root (records *are* the causal set), orthogonal axes = why one paper, two verdicts | **KEEP-FULL** (small) | — | §2/§3 openers or §4 — one sentence each; v8 nowhere names the axes or the experiment types; it is the organizing contrast of the paper |
| A4 | §3.1 | Uniqueness structure: the only LI second-order generator on `H_m` is `a·Δ_{H_m} + b·𝟙`, probability conservation forces `b = 0` (DPS theorem: single phenomenological `κ`) | KEEP-RESULT (mostly) | `p19a` 1b | v8 §2 has "(probability conservation kills the zeroth-order term)" — the enumeration premise ("the only invariant second-order generator is `aΔ + b·1`") is worth its half-sentence for self-containment |
| A5 | §3.1 | Heating-law detail: per-component `⟨p_i²⟩ = 2κτ`; `⟨p²⟩ = 2(d−1)κτ = 6κτ`; rest-frame isotropy at leading short-`τ` (the `H³` curvature enters only at higher order near `χ = 0`); linearity check `⟨p²⟩(2τ)/⟨p²⟩(τ) = 2.0` | **KEEP-FULL** (small) | `p19a` 1c | v8 §2 has `2(d−1)κ_sw τ = 6κ_sw τ` and the "leading short-τ" label but not *why* (curvature = higher-order) nor the per-component form; one sentence |
| A6 | §3.2 | The tie's derivation spelled out: worldline threads `1/l_step` cells per unit proper time → crossing rate `ν = σ/l_step`; per-seal mean-zero kick of variance `l_step⁻²`; **accumulation `⟨p²⟩(τ) = (στ/l_step)·l_step⁻² = σ·l_step⁻³·τ`**, hence `κ = (1/6)σ·l_step⁻³` with **`1/6 = 1/(2(d−1))` kinematic**, coefficient model-dependent `O(1)` | **KEEP-FULL** (small) | `p19a` 2 | v8 §2 lists the two ansätze + result but not the one-line accumulation nor the `1/(2(d−1))` identity — the two lines that make the tie's arithmetic self-contained (and show exactly *where* the posit sits) |
| A7 | §3.2 | Framing: in plain causal sets `κ` is a free Planck-suppressed constant; SHARD fixes its *structure* (proportionality to `σ` in cell units) — the tie is the new content | KEEP-RESULT | — | v8 abstract "the record-specific content is the tie"; adequate |
| A8 | §3.3 | **Weight analysis of the magnitude:** the relation `κ·l_step³/σ = 1/6` is weight-0 (`g_λ`-invariant, record-intrinsic); the absolute `κ` carries **length-weight −3 = mass³, matching its physical dimension** — the only dimensionful input is `l_step`; this is *how* the scale wall gates the channel | **KEEP-FULL** (small) | `p19a` 4 | v8 §2 states weight-0 for the relation but never grades the magnitude; the −3 grading is the actual mechanism of "SCALE-GATED" (and the corrected v7 XIX review fix — `l_step⁻²→l_step⁻³` — lives exactly here) |
| A9 | §3.3 | Species caveat, second half: the species-independent **molecular/cosmic-ray limits (DPS) corroborate more weakly**; a mass-dependent `κ(m)` would **relax the translation** | **KEEP-FULL** (small) | — | v8 §2 has the first half (single-species bound, universality supplied by the tie) but drops the corroborating-limits sentence and the `κ(m)` relaxation — both are part of the honest reach assessment |
| A10 | §3.3 + §7 | The ER=EPR template: the constrain-don't-predict move is the analog of *Testing ER=EPR with Hydrogen* (Javed–Wilson-Ewing, arXiv:2512.02156) | **KEEP-FULL** (tiny) | — | v8 §2 *uses* the move ("the analog of turning a structural quantum-gravity claim into a precision amplitude constraint") but **omits the citation** — add Javed–Wilson-Ewing to References |
| A11 | §3.3 | σ-bound consistency left OPEN (bites vs vacuous; needs the supply law) | KEEP-RESULT | — | v8 §2 + abstract carry it verbatim in spirit (correction satisfied). NOTE: v8 points at "paper 1 §6.1" — if Paper 1 is renumbered per `port_paper1.md` (new section inserted after §3), this cross-reference must be updated |
| A12 | §3.4 | Not-blind: `κ_continuum = 0`; the swerve escapes Paper-X blindness **by being a momentum-space, not a decoherence-channel, signature** | KEEP-RESULT + **KEEP-FULL** (tiny) | `p19a` 3 | v8 §2 has the theorem; the *momentum-space-vs-decoherence-channel* reason is the one clause missing (it reappears in row A16) |
| A13 | §4.1 | Seal kernel `|ρ₀₁| = exp(−∫λ)`, `λ ≥ 0`, CP-divisible/monotone/revival-free; Gaussian onset; `κ_G` disambiguation | KEEP-RESULT | `p19b` 1–2 | v8 §3 carries all, including `κ_G ≠ κ_sw` (correction satisfied) |
| A14 | §4.1 | **"CP-divisibility is incompatible with Barandes-indivisibility — a genuine structural fingerprint of the seal"** | **KEEP-FULL** (one line) — *unless owned by v8 Paper 2* | — | Absent from v8 Paper 6; v6 P56 is citable but v7 P19 is log-only and this line is the seal-fingerprint framing that motivates §3. Verify whether v8 Paper 2 (envelope/Barandes) states it; if yes, a cross-ref in §3 suffices — see §G below |
| A15 | §4.2 | Escape attempt: the **three built dynamics** (seal / QM CP-divisible / revival model with `λ` dipping negative), `N[seal] = N[QM] = 0 < N[revival]`; **higher-order/multi-time probes and the specific `λ(t)` shape checked**; identical to all orders; `BLIND_NO_ESCAPE` | KEEP-RESULT + **KEEP-FULL** (tiny) | `p19b` 3 | v8 §3 has the three dynamics + all-orders identity + verdict; missing only the clause that higher-order/multi-time probes were explicitly checked (the exhaustiveness of the escape hunt is the content) |
| A16 | §5 | Comparison table + **the decisive difference**: the swerve predicts something the continuum/QM *does not have* → mere presence informative; the no-revival predicts something QM *also has* → presence uninformative | **KEEP-FULL** (small) | — | v8 §4 states the bottom line but not the structural *reason* for the channels' opposite fates; one sentence (the table itself is optional presentation — DROP the table, keep the sentence) |
| A17 | §4.2 note | `N_BLP` scoping: CP-divisibility certified from `λ ≥ 0` directly, converse false (eternal-non-Markovian counterexamples), RHP the certifying measure, lines coincide for the pure-dephasing family | KEEP-RESULT | `p19b` | v8 §3 carries the full corrected note (correction satisfied) |
| A18 | §6 | Honest-scope table rows; plus "the no-revival has no open route — **the blindness is structural, not unbuilt**"; tightening the bound never converts it to a prediction without the scale import | KEEP-RESULT + **KEEP-FULL** (tiny) | — | Statuses all present in v8 prose; the "structural, not unbuilt" clause and the tightening-never-converts clause are worth their half-sentences in §3/§4 |
| A19 | §7 conclusion | One-line summary | **DROP** | — | Duplication; v8 §4 is the corrected version |
| A20 | Receipts + References | `p19a` 23/23, `p19b` 22/22, `p19c` 19/19 with dps; DHS/DPS/KM/BLP/RHP/Donadi cites | KEEP-RESULT | — | v8 §5 + References carry all (BLP/RHP now with full journal refs — better than v7); only Javed–Wilson-Ewing missing (row A10) |

### B. `pK1_seal_information_rate.py` (11/11, dps=120) — the κ=1 crux / Rényi-order forcing

| # | Source | Content (one line) | Class | v8 destination |
|---|--------|--------------------|-------|----------------|
| B1 | header + Step 1 | The crux setup (κ = −d log\|ρ₀₁\|/dχ; paper56 [TARGET] "decoherence = seal rate" ⇔ saturation κ=1) and the three-way resolution: κ forced distribution-independently, value set by the Rényi order of the chosen σ — `D_B → (1/8)Js²`, `D_KL → (1/2)Js²`, `JSD → (1/8)Js²`, so κ_KL = ¼, κ_JSD = 1 | KEEP-RESULT | v8 §1 carries the full structure (quarter law + Rényi-order ratio + κ=1-iff-mutual-information); abstract carries the dissolution framing |
| B2 | header | κ_symKL = ¼ too (symmetrizing the KL does *not* change the order) | **KEEP-FULL** (tiny) | §1 — half a clause; closes the "maybe the symmetrized σ saturates" escape |
| B3 | header (PRIOR ART item 3) | **The asymmetry note:** SHARD's `σ = D(P_AB‖P_BA)` is a *genuine asymmetric pairwise KL* — the forward-reverse structure does **not** symmetrize it to a JSD — so its Rényi order is 1 and ¼ *is* the right number for SHARD's σ | **KEEP-FULL** (small) | §1 — v8 asserts "σ … is a KL (order 1)" but never closes the natural objection that a forward-vs-reverse comparison might be secretly JSD-like; one sentence |
| B4 | header (PRIOR ART item 3) | **The punted bridge:** ¼ is right *provided σ governs the logical-coherence decay at all* — the unbuilt seal-history functor (v6 Paper 56 §2.2); the receipt dissolves the equality but **punts** that bridge | **KEEP-FULL** (small) | §1 — v8 References name "the structural-functor obligation" but the §1 body's [POSITED] clause covers only the de Bruijn/entropy-production bridge (p19c), not this logically prior one; add the clause (or fold both bridges into one labeled [POSITED] sentence) |
| B5 | header (PRIOR ART) | Modern citation for the complementarity: *Decoherence Implies Information Gain*, Phys. Rev. Research **7**, L012040 (2024) / arXiv:2402.16740 (alongside Englert 1996, Zurek 2003) | **KEEP-FULL** (tiny) | References — Englert/Zurek are cited; the 2024 statement is the receipt's actual modern anchor |
| B6 | Step 1 | Distribution-independence mechanism: the base `a` *cancels* from κ_KL and κ_JSD (sympy-exact) — forced, not free | KEEP-RESULT | v8 §1 "distribution-independent, sympy-exact" ✓ |
| B7 | Step 3 | Finite-s: κ runs and is distribution-dependent; the universal value is a *dense-limit* statement (honest scope) | KEEP-RESULT | superseded in detail by pRUN [B]/[D], which v8 §1 carries |
| B8 | verdict | κ is weight-0/dimensionless, separable from the scale no-go — the dissolution does not need the un-fixable absolute rate | KEEP-RESULT | v8 §1 dissolution ("dimensionfully… only λ = κ·σ̇ survives") ✓ |
| B9 | verdict | Publishability paragraph (JSD-saturation = known complementarity, modest novelty; SHARD claim refuted) | **DROP** | process/strategy prose; the content survives as v8 §1's [IMPORT] grading |

### C. `pRUN_kappa_exact.py` (17/17, dps=140) — the exact running κ(ε) and the master Rényi structure

| # | Source | Content (one line) | Class | v8 destination |
|---|--------|--------------------|-------|----------------|
| C1 | [A] | All-order Gaussian identity: `N(0,1)` vs `N(μ,1)` gives `D_B = μ²/8`, `D_KL = μ²/2`, κ = ¼ **exactly at every μ** — a theorem, not an expansion; no running on the Gaussian family; honest novelty note (ingredients textbook, the exact-ratio packaging new) | KEEP-RESULT | v8 abstract + §1 carry all of it, including the "elementary/textbook-ingredients" honesty and the Kailath cite |
| C2 | [B] | The running's **component closed forms**: `BC(ε) = √(1−4ε²)`, `D_B(ε) = −½log(1−4ε²)`, `D_KL(ε) = 4ε·artanh(2ε) = 2ε·log((1+2ε)/(1−2ε))` | **KEEP-FULL** (small) | §1 — v8 gives only the ratio κ(ε); the pair of closed forms is what makes the ratio checkable by hand (self-containment; two formulae) |
| C3 | [B] | The ascending series **through e⁶**: `κ(ε) = ¼ + ε²/6 + 14ε⁴/45 + 724ε⁶/945 + O(ε⁸)` (sympy-exact) | **KEEP-FULL** (tiny) | §1/abstract truncate at `14ε⁴/45 + …` — add the `724ε⁶/945` term (the prompt-flagged completeness item; costs four characters of honesty about how far the exact series was pushed) |
| C4 | [B] | Monotone running UP from ¼; dense limit recovers ¼; closed form matches direct computation to dps=140 | KEEP-RESULT | v8 §1 "monotonically increasing from ¼ (strong records decohere faster per nat of arrow)" ✓ |
| C5 | [C] | Master structure: `σ = D_{α=1}`, `D_B = ½D_{α=½}` (exact identity, not just weak-limit), `D_α → (α/2)Js²` verified for α ∈ {½, 1, 2, 3, 4}, hence `κ_α → 1/(4α)`; **saturation κ=1 ⇔ α=¼**, which the JSD/mutual-information measure supplies | KEEP-RESULT + **KEEP-FULL** (tiny) | v8 §1 has the structure and the ratio-of-orders reading; add the explicit "κ=1 would be α=¼" clause and (optionally, §5) the verified α-grid — this is the κ_α table the prompt asks after; its content is present, its explicit tabular/grid form is not |
| C6 | [D] | **Asymmetric-base exhibit with numbers:** the O(s) slope of κ above ¼ depends on the base — `a = 0.3` slope ≈ 0.0794, `a = 0.45` slope ≈ 0.0168, `a = ½` slope ≈ 0 (pure even series) — so "κ runs" is distribution-dependent off the symmetric point; only the dense-limit ¼ and the Gaussian all-order ¼ are distribution-free | **KEEP-FULL** (small) | §1 — v8 has the qualitative sentence; the three-number exhibit is the receipt's proof of distribution-dependence and the prompt-flagged completeness item |
| C7 | header/verdict | NEW-vs-KNOWN carving: NEW = [A] all-order Gaussian + [B] exact closed-form running; KNOWN/IMPORTED = divergence formulae (textbook), Rényi/Fisher weak-limit geometry (standard), quarter leading order (v6 paper26), κ=1 complementarity (Englert/Zurek), absolute scale (imported); FDT interpretation = a reading, not a theorem | KEEP-RESULT (verify wording) | v8 §1 distributes all six gradings correctly ([THEOREM]/[DERIVED]/[IMPORT]/labeled analogy); no single block needed if C2/C3/C6 land |

### D. `p19c_swerve_fdr.py` (19/19, dps=100) — the de Bruijn grounding and the Einstein tension

| # | Source | Content (one line) | Class | v8 destination |
|---|--------|--------------------|-------|----------------|
| D1 | Step 1 | The de Bruijn identity on H³ spelled out: `S = −∫f ln f dV_g`, `I_F = ∫ |∇ln f|² f dV_g ≥ 0`, integration by parts (boundary terms vanish; `∫Δf dV = 0`), verified on **two families** (Jüttner `e^{−a cosh χ}`, a=1.3; Gaussian-in-χ `e^{−bχ²}`, b=0.7) so the identity is general, not Jüttner-specific | KEEP-RESULT (mostly) | v8 §1 "holds exactly (verified on two families)" ✓; optionally name the families in §5 receipts |
| D2 | header/Step 2 | Second law: `dS/dτ = κ·I_F ≥ 0`, vanishing iff f uniform — the swerve monotonically heats structured distributions; κ = entropy-production-per-Fisher-information | KEEP-RESULT | v8 §1 "[DERIVED] a genuine second law" ✓ |
| D3 | header (CAVEAT) | **The tautology caveat:** de Bruijn holds for *any* heat flow — every κ, every manifold — so it gives κ a thermodynamic *name* but does **not determine or constrain** it | **KEEP-FULL** (small) | §1 — v8 says the grounding "motivates but does not derive" the tie (§2), but never states *why the grounding is cheap*; this is the sentence that stops a reader over-crediting the de Bruijn leg |
| D4 | Step 3 | **The Einstein construction:** complete the swerve to a relativistic Ornstein–Uhlenbeck process (Dunkel–Hänggi); Jüttner stationarity `f_J ∝ e^{−E/T_sub}` forces zero-current drift `A(p) = (D/T)·p/E`, friction `ν = dA/dp|₀ = D/(TM)`, hence the relativistic Einstein relation `D = νMT` ⇒ `κ = ν·M·T_sub` (sympy-exact; non-relativistic OU limit checked); the free-streaming swerve = the friction-negligible limit (`T_sub ~ 1/l_step` Planckian: the equilibrium sits at energies never reached, so lab physics sees pure heating) | **KEEP-FULL** (medium) | §2 — v8 states only the conclusion "(κ = νMT_sub) would make κ_sw mass-dependent"; the 2–3-sentence construction is what makes the tension *checkable* and self-contained, and the friction-negligible reading is why the completion is physically natural |
| D5 | header/Step 5 | **The tension's honest anatomy:** the mass-dependence is a property of the *completion* (which adds friction + a Jüttner equilibrium the free swerve does not have) — a flagged tension with the mass-independent tie and with the single-mass relic bound, **not a delivered swerve prediction**; a real, falsifiable-in-principle difference (different species-scaling) | **KEEP-FULL** (small) | §2 — v8 has "a flagged, unresolved tension"; add the one clause locating the mass-dependence in the completion (this also feeds the §2 species caveat, row A9: a mass-dependent κ(m) is exactly what the Einstein completion would give) |
| D6 | Step 4 | **σ vs σ̇ disambiguation:** the entropy-current identification is `σ̇ = dS/dτ = κ·I_F` — an identification between two *rates* (σ̇ has dim mass), *not* p19a's dimensionless per-cell σ; solved: `κ = σ̇/I_F`; still one posited bridge doing all the record-specific work; scale still imported (`T_sub ~ 1/l_step`) | **KEEP-FULL** (tiny) | §1/§2 — v8 §1 already writes `λ = κ·σ̇` and §2 uses per-cell σ; one clause flagging that the two σ's differ by the (no-go-walled) cell-rate conversion prevents a genuine symbol trap |
| D7 | Step 4 | Dimensional soundness: `[σ̇] = mass`, `[I_F] = mass⁻²` ⇒ `[κ] = mass³` — matches the weight-(−3) grading; convention note (I_F on unit-radius rapidity H³; physical Fisher info = I_F/M²; the M² cancels, identity convention-free) | **KEEP-FULL** (tiny, merged with A8) | §2 — lands automatically if A8's weight-(−3) sentence is ported with the dimensional check attached |
| D8 | header (STANDARD) | Citations for the standard legs: de Bruijn / entropy-dissipation / Bakry–Émery; **Dunkel–Hänggi, Phys. Rep. 471, 1 (2009)**; Debbasch–Mallick–Rivet (relativistic Brownian motion) | **KEEP-FULL** (tiny) | References — v8 cites nothing for the de Bruijn identity or the Einstein relation it uses in §§1–2; add at least Dunkel–Hänggi |
| D9 | header (PURPOSE) | The hostile-review origin story ("dimensional bookkeeping posing as a derivation") | **DROP** | process; papers are single-threaded (standing style rule) — the *content* (tie = posited kick-count ansätze; 1/6 kinematic) is already in v8 §2 |
| D10 | Step 5 | Comparison table p19a-bare-tie vs p19c-FDR-grounded (σ as count vs σ as entropy current; no temperature vs T_sub; both import one scale) | KEEP-RESULT (spirit) | v8 §1+§2 carry the graded conclusion; the explicit two-column contrast is optional |

### E. `pFDT_seal_influence.py` (21/21, dps=140) — the retired FDT framing, audited

| # | Source | Content (one line) | Class | v8 destination |
|---|--------|--------------------|-------|----------------|
| E1 | header verdict block | **Why the FDT framing fails (the prompt's big item):** in genuine Feynman–Vernon the friction is the *phase* kernel (`arg F`) and the noise the *modulus-damping* kernel; but `σ = KL` is a **second modulus-level (noise-sector) divergence** — nonzero even when `arg F = 0` — so `−ln BC` and `σ` are *two noise-sector divergences*; the `coth(ω/2T)` FDT does no work, no `T` enters the ¼, the ratio is pure information geometry `(1/8)/(1/2)` | **KEEP-RESULT — VERIFIED PRESENT** | v8 §1 carries exactly this, compressed but correct: "in a genuine influence functional the friction is the imaginary/phase kernel, while σ is a second modulus-level divergence — and no coth(ω/2T) enters the ¼, which is pure information geometry" |
| E2 | verdict + Step 1 | **The narrow reading that survives:** the seal overlap *is* readable as a discrete GMH/FV influence functional — `F = ⟨e₁\|e₀⟩ = Σ_b √(P₀P₁) e^{iΔθ(b)}`, with `\|F\| = BC` exactly when the recorded phases align and `\|F\| ≤ BC` in general (recorded phases only *add* dephasing — the modulus carries the full noise); the seal multiplies `ρ₀₁` by `F` (the Caldeira–Leggett law, record-instantiated) | **KEEP-FULL** (small) | §1 — v8 grades the framing analogy-only but never states the *positive* part that survives (the modulus-is-Bhattacharyya identification and `\|F\| ≤ BC`); two sentences rescue it from the log |
| E3 | Steps 2–3 | The retired scaffolding: Gaussian-bath kernel split, Callen–Welton `ν(ω) = coth(ω/2T)η(ω)`, high-T Nyquist limit, athermal `coth → 1` floor as "the regime" | **DROP** | retired by the receipt's own adversarial verdict (the coth structure does no work for the record pair); v8's "no coth enters the ¼" is the correct residue |
| E4 | Step 2 | Additivity of both faces over independent seals (`−ln BC^n = n(−ln BC)`; `nσ`) | KEEP-RESULT | paper 1's odometer/telescope already owns additivity; nothing paper-6-specific |
| E5 | novelty block | Citations behind the analogy grading: Hu–Paz–Zhang PRD **45**, 2843 (1992); Callen–Welton (1951); Halliwell PRD **60**, 105031 (1999); GMH PRD **47**, 3345 (1993) | **KEEP-FULL** (tiny, optional) | References — v8 has FV + Caldeira–Leggett; if the coth clause stays in §1, Callen–Welton earns its cite; GMH is already cited in paper 1 |
| E6 | receipt line | 21/21, header carries the analogy-only verdict | KEEP-RESULT | v8 §5 says exactly this ("its own header carries the analogy-only verdict") ✓ |

### F. `pPRIN_seal_record.py` (21/21, dps=140) — the seal-is-record principle, audited

| # | Source | Content (one line) | Class | v8 destination |
|---|--------|--------------------|-------|----------------|
| F1 | header | **The named postulate** (equivalence-principle style, elevated not derived): *"a seal is the event whose time-asymmetry IS its which-path readout"* — irreversibility (`σ`, arrow) and decoherence (which-path) as two faces of **one record event sharing one Fisher object `J`** (which is *why* the ratio is distribution-free) | **KEEP-FULL** (small) | §1 — this is the clean Tier-A form of THE one [POSITED] bridge (it unifies B4's "does σ govern coherence at all" and D6's `σ̇ = dS/dτ`); one tagged sentence; without it §1's "σ = D(P_AB‖P_BA) is a KL (order 1)" + "κ = D_B/D_KL" silently conflates the *which-path* KL (what the theorem computes) with the *arrow* KL (what σ is) — see the consistency note below the tables |
| F2 | Step B | **The genuine overlap anatomy** (survives the FDT retirement): the per-seal relative phase `dφ` is the *unsealed gravitational which-path holonomy* (v6 P56 §3 object); under path swap `0↔1` the noise `−ln\|F\|` is **even** (no arrow) while the phase `arg F` is **odd** (carries the arrow) — the FV even/odd kernel signature realized on one record event; nonzero `dφ` strictly adds decoherence | **KEEP-FULL** (small) | §1 — none of it is in v8; the even/odd split is the *correct* structural content the metaphor was gesturing at, and the `dφ` = unsealed-holonomy identification is the §1 ↔ gravity-thread link |
| F3 | Step C | **The phase cannot revive coherence** (friction/Im face leaves `\|ρ₀₁\|` untouched); noise-as-nonnegative-rate reproduces the CP-divisible monotone kernel — the principle *adds no revivals* | **KEEP-FULL** (tiny) | §3 — one clause; an independent strut under Channel B (the only face that could have manufactured a revival provably cannot) |
| F4 | Step D | Epistemic shape: forced weight-0 ratio (¼) + one imported scale — "a complete honest law, not an incomplete derivation," the same shape as Newton's `G` | KEEP-RESULT | v8 §1 dissolution + §4 bottom line carry the substance; the "like G" simile optional |
| F5 | Step E | The dissolution as **category error**: the equality set an arrow-of-time KL (Rényi-1) equal to a which-path Bhattacharyya quantity (Rényi-½); equality holds only after switching the measure (JSD = complementarity, known) | KEEP-RESULT (+ optional half-clause) | v8 §1 has both prongs; the *arrow-vs-which-path axis naming* (why it was a category error, not an arithmetic accident) is worth its half-clause |
| F6 | novelty block | "Framing/consolidation move, not a new theorem; internally consistent; proves no new physics" | KEEP-RESULT | v8 §5 grades pPRIN as a labeled-analogy audit ✓ |

**Consistency note (auditor's, for the port):** paper 1 presents `S(χ) = exp(−κχ)` with `κ` *the one free scale*; paper 6 §1 presents `κ = D_B/D_KL → ¼` *forced*. These are consistent **only because** the quarter-law theorem relates the which-path pair's own two divergences, while paper 1's `κ` converts the *arrow*-odometer `χ` into coherence loss — and the identification of the two (which-path KL ↔ arrow σ) is exactly the seal-is-record [POSITED] bridge (F1/B4). Paper 6 should say this in one sentence, and should add a **κ-glossary clause**: `κ` (quarter ratio, §1) / `κ_sw` (swerve) / `κ_G` (onset curvature) / paper 1's `κ` (the free rate conversion) — the paper already disambiguates the middle two; the outer two collide across papers of the same series.

### G. Routing checks (other receipts, other destinations)

**G1. `pE_phase_causalset.py` → v8 Paper 4 (gravity/continuum) — partially landed; analysis gap flagged.** Paper 4 §5 carries the probe's existence, the Carlip [IMPORT], the clean **artifact/null** verdict (`R_bulk` inside the random-shuffle band), the "gate is analytic, not computational" conclusion, the receipt listing, and the Benincasa–Dowker/Carlip/KR references — the *verdict* is self-contained. **Missing from Paper 4 (and from `port_paper4.md`, which has no row for it):**
1. **The real-vs-complex mechanism contrast** (the receipt's SHARD-specific analysis, verdict block + header): a REAL click-law-shaped weight `e^{−βS}` is a sum of positive terms — it provably **cannot cancel**, only monotonically reweight toward low `|S|`; the Carlip selection *requires the `i`* — and that `i` is plausibly the **same complex-over-real bit** the program proves un-forceable (the tensor-product import, v8 Paper 2). This ties the manifoldlikeness gate to the χ_AB wall — cross-paper connective tissue that currently survives nowhere but the docstring. One-to-two sentences in Paper 4 §5 (tagged as a reading, "plausibly").
2. The **artifact-control design** (40 random action-shuffles; verdict logic `selection_seen ∧ within-shuffle ⇒ ARTIFACT`) — the reason "artifact/null" is an honest word and not a shrug.
3. The **n-trend diagnostic** (bulk action spread `std(S_bulk)` must grow with `n` for asymptotic cancellation — the one route left open; KR dominance is asymptotic, enumerable `n` cannot show it).
4. Methodology texture: exact enumeration of all posets `n ≤ 8` with OEIS A000112 agreement; both unlabeled- and labeled-uniform measures via `|Aut(P)|`; GEOMETRIC = order-dim ≤ 2 tested exactly (Dushnik–Miller/Golumbic transitive orientation) **with the honest caveat that this class is generous** (necessary, not sufficient, for faithful sprinkling); the pre-declared "a clean null is a valid, reportable outcome."
→ Action: append these to Paper 4 §5 (or a receipts appendix) and add a row to `port_paper4.md`.

**G2. `f5_oi_pi_consistency.py` → v8 Papers 1/2 — verified ADEQUATE.** Paper 1 §7.2 carries the silence result with the receipt at the corrected count (21/21), the PI-yes/OI-no law at `CHSH = 2√2`, the normative guardrail (locality never imposed on Tier-A; Tier-B local only as no-signaling), and the Bell address (keep PI, drop OI); Paper 2 carries the "OI ⊥ skeleton lemma" ownership note + receipt. Optional one-line upgrades to Paper 1 (not blocking): (i) the explicit law `p(a,b|x,y) = (1 + ab·E)/4`, `E = −cos(x−y)`; (ii) the *mechanism* clause — attaching per-chain survival preserves CHSH and no-signaling because the common seal-gate cancels in conditional outcome statistics; (iii) the converse leg (an OI-*respecting* law carries the same skeleton — the "neither forces" direction, currently implicit in "neither forces nor forbids").

---

## Corrections checklist (the seven mandated items, verified against the target)

| Correction | Status in v8 Paper 6 |
|---|---|
| **Tie POSITED** | ✓ Abstract ("a posited identification… whose owned core is the proportionality") + §2 "[POSITED]" + References ("the tie at its POSITED grade"). Nothing to do. |
| **κ_G disambiguation** | ✓ Abstract + §3 ("κ_G the onset curvature, distinct from κ_sw"); the swerve constant is consistently `κ_sw`. **New flag:** the third collision — §1's forced quarter `κ` vs paper 1's *free* conversion `κ` — is un-flagged; add the κ-glossary clause (see the consistency note, §F). |
| **Donadi cite** | ✓ References: S. Donadi et al., Nat. Phys. **17**, 74 (2021), used for the adjacent collapse class. |
| **Species caveat** | ✓ first half (single-species relic-ν bound; universality supplied by the tie — abstract + §2). **Missing second half** (row A9): DPS molecular/cosmic-ray limits corroborate more weakly; a mass-dependent `κ(m)` would relax the translation — which also connects to the Einstein-completion tension (D5). |
| **σ-bound OPEN** | ✓ present (abstract "[OPEN]… stated rather than presumed"; §2 "bites vs vacuous"). **But the cross-reference is stale twice:** §2 and References point at "paper 1 §6.1" / "the open supply law" — paper 1's residue is now **§7.1**, and its corrected content is *form forced / magnitude [PHYSICAL] configuration data* ("not an open law waiting to be found"). Reword: "awaits a reading of the substrate's physical σ configuration along massive worldlines (paper 1 §7.1: supply magnitude [PHYSICAL]) and the spacing ledger" — same openness, right address. |
| **N_BLP scoping** | ✓ §3 carries the full corrected note: CP-divisibility certified directly from `λ ≥ 0`; converse false in general (eternal-non-Markovian counterexamples); RHP the certifying measure; lines coincide for the pure-dephasing family. |
| **Quarter law + dissolution central** | ✓ §1 is first and the abstract leads with it. Completeness sub-audit: closed form ✓, `ε²/6` ✓, `14ε⁴/45` ✓, **`724ε⁶/945` missing** (C3); asymmetric-base dependence qualitative ✓, **the three-number exhibit missing** (slopes ≈ `0.0794` at `a=0.3` / `0.0168` at `a=0.45` / `~0` at `a=½`; C6); `κ_α → 1/(4α)` + ratio-of-orders ✓, the verified grid `α ∈ {½,1,2,3,4}` and the explicit "saturation `κ=1 ⇔ α=¼`" clause missing (C5, tiny); component closed forms `D_B(ε)`, `D_KL(ε)`, `BC(ε)` missing (C2). |

**Expected-big-items verdict (from the prompt):** paper 19's Channel-A skeleton and Channel-B construction are in v8 in corrected form (A1, A13, A15, A17 ✓); the *derivation texture* is the gap (A4–A8: generator enumeration, per-component/curvature qualifier, kick-accumulation line, `1/6 = 1/(2(d−1))`, **weight-(−3) = mass³ grading of the magnitude** — the last being the actual mechanism of SCALE-GATED and the locus of the v7-review dimensional fix). pK1/pRUN's [A]–[D] carving is mostly present (B1, C1, C4, C7 ✓; gaps B2–B5, C2–C3, C5–C6). p19c: two-family de Bruijn ✓ *present*; the **Einstein construction (D4), the tautology caveat (D3), and the completion-owns-the-mass-dependence anatomy (D5)** are the missing big pieces. pFDT/pPRIN: the arg-F-vs-modulus failure anatomy is **verified present** in §1 (E1); the *surviving positive anatomy* (E2, F1–F3) is the gap.

---

## Cross-cutting actions (in port order)

1. **§1 (seal-decoherence law):** add B3 (the asymmetry note — forward-reverse KL does not symmetrize to JSD), B4+F1 (the punted bridge, named: the seal-is-record [POSITED] postulate — which-path KL ↔ arrow σ; also discharges paper 1 §7.3's promise that the functor obligation is treated here), C2 (component closed forms), C3 (`724ε⁶/945`), C5 (α=¼ clause; α-grid to §5), C6 (asymmetric slopes exhibit), D3 (de Bruijn tautology caveat), E2+F2 (the surviving influence-functional anatomy: modulus-is-Bhattacharyya, `|F| ≤ BC`, noise-even/phase-odd, `dφ` = unsealed holonomy), κ-glossary clause, B2 (symKL = ¼, half-clause).
2. **§2 (Channel A):** A3 (axes framing), A4–A6 (generator premise; per-component + curvature qualifier; accumulation line + `1/(2(d−1))`), A8+D7 (magnitude weight −3 = mass³ + the dimensional check), A9 (species-caveat second half), A10 (Javed–Wilson-Ewing citation), D4 (Einstein construction, 2–3 sentences), D5 (mass-dependence belongs to the completion; ties to A9), D6 (σ vs σ̇ clause), fix the stale "paper 1 §6.1"/"open supply law" cross-reference (→ §7.1, [PHYSICAL] wording) in §2 *and* References. |
3. **§3 (Channel B):** A14 (the Barandes-indivisibility incompatibility line — verified absent from Paper 2 as well; one line + cross-ref to Paper 2's division-event treatment), A15 (higher-order/multi-time probes clause), F3 (phase-cannot-revive strut), A18 ("blindness structural, not unbuilt"), + one honest-scope sentence acknowledging the deferred sparse-vs-dense computation-on-`D` (paper 1 §7.3 defers it *to this paper*; currently only the References mention the functor). |
4. **§4:** A16 (the decisive-difference sentence: presence-informative vs presence-uninformative), A18 (tightening-never-converts clause).
5. **§5 + References:** add Javed–Wilson-Ewing arXiv:2512.02156; Dunkel–Hänggi Phys. Rep. **471**, 1 (2009) (+ optionally Debbasch–Mallick–Rivet, Callen–Welton, Hu–Paz–Zhang, Halliwell, PRR **7** L012040 (2024)); name the two de Bruijn families (Jüttner, Gaussian-in-χ) in the p19c receipt line; carry v7 P19's "bound translations are arithmetic at dps=140 and flagged" discipline note.
6. **Out-of-paper flags:** G1 → Paper 4 §5 + `port_paper4.md` (the pE real-vs-complex analysis and texture); G2 → Paper 1 (three optional one-liners; construction otherwise adequate).

**What was NOT ported, and why (summary of DROPs):** v7 P19 §1/§7 and the receipt verdict summaries (duplication — v8 abstract/§4 are the corrected versions); the §5 comparison *table* as a table (its decisive-difference sentence ports instead); pFDT's coth/FDT scaffolding (retired by its own adversarial verdict — v8 correctly keeps only the graded analogy + the "no coth enters ¼" residue); pPRIN's FDT-lock framing (same reason; the named postulate and overlap anatomy port *without* the noise/friction labels); all hostile-review origin stories (single-threaded paper style); pK1's publishability paragraph (process). No content was found in v7 P19 or the five receipts that is *wrong* in v8 — the v8 paper's statements are all correct; the audit's findings are omissions and two stale cross-references, not errors.
