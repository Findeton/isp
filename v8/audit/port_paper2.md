# V8 self-containment audit — destination: v8 Paper 2 (envelope / unforceability)

**Date:** 2026-07-01. **Sources swept section-by-section:** v7 Paper 4 (`relativistic-isp-v7-paper4-entanglement-graph-not-metric.md`), Paper 6 (`…paper6-transverse-almost-quantum.md`), Paper 12 (`…paper12-transverse-nogo-envelope-forced.md`), Paper 13 (`…paper13-feynman-map-chi-ab-bottoms-at-qtilde.md`), Paper 16 (`…paper16-seal-indivisibility-bridge-real-quantum-reopening.md`).
**Target:** `/Users/felixrobles/workspace/isp/v8/relativistic-isp-v8-paper2-envelope-unforceability.md` (69 lines, 8 sections; read 2026-07-01).
**Rule:** v8 must be the *better*, self-contained version — v7 → log-only (NOT citable for load-bearing content); v6 stays citable; "self-contained ≠ compressed" (a verdict stated without its derivation fails the audit). Corrections respected: `I3322` nesting `0.2514709 > 0.2509397 (full level-2, no 8th digit) > 0.2508754`; `8.748` and `0.2513864` retired; Renou → contested-convention; three-status meta-result; only-invariants direction (no "exactly" biconditional); `p6 = 43/43`; `f5 = 21/21`.

**Headline finding.** The v8 paper's *verdict layer* is fully correct — all seven listed corrections are in place, and no retired number or framing survives. What is missing is the *derivation layer* underneath the verdicts, plus one entire headline result: **(1)** v7 Paper 4's constructive half — **entanglement sources the record graph but not its metric** (the pinch, the hop-metric-up-to-one-scale, the weight-split no-go, the circularity audit) — is absent from *all six* v8 papers (grep-verified) and unclaimed by any other port audit; the joint-law groundwork (skeleton-transfer conditional, additivity-not-forced telescope, the `I_σ = 0` counterexample and the `E_cl` contrast) is compressed to two clauses. **(2)** v7 Paper 6's arena argument ((C-form)/(C-level), level-independence) and its entire Information-Causality static core (`t3`, 17 checks) are missing; the crossing-template constants (`η_* = 1.0903…`, `W_* = 0.3648…`) are invoked but never defined (recap + cross-ref to v8 Paper 1 needed). **(3)** v7 Paper 12's M1/M2 derivations (the forcing checklist, the max-ent construction with flatness numbers, the capacity-robustness sweep with the canonicity criterion, the rebit realization) and the three closed escapes (holonomy (a)/(b)/(c), sign, magnitude) exist in v8 as roughly one clause each. **(4)** v7 Paper 13's witness self-contained certification (level-2 as in-house `⪰ Q` bound), sector push values, covariance gates (prime-dim-3 anti-smuggling witness, sub-Tsirelson `1.339`), order-feedback three-grade closure, honest-pinch connectivity floor, KR-quantified manifoldlikeness obligation, and do-not-conflate list are summarized in a sentence. **(5)** v7 Paper 16 is the best-preserved source, but the Doukas boundary, the Jarlskog `ker R` tie-in, the M2 true-by-design honesty note, the worry→undercut vindication argument, and Höhn are missing. **Two verification flags:** the arXiv:2604.07425 author initials (v8 disagrees with both v7 sources); and — out of destination scope but caught here — v8 Paper 3's "`κ·σ_A = G·Λ² = const`" equality-chain wording, which Paper 16 §5 explicitly corrects to *separate, not numerically equal* invariants. Recommended vehicle: grow §2 (joint law + arena), §3 (IC core), restructure §4 into subsections (checklist / M1 / M2 / escapes / sweep / new constraints), grow §5 (vindication argument) and §6 (Barandes–Doukas–BHK detail), and add one new section for the graph/metric constructive half — or explicitly re-home it in v8 Paper 4 with a pointer; it must land in exactly one place.

---

## Port table

Class key: **KEEP-FULL** = must be ported (not adequately in v8; give destination); **KEEP-RESULT** = statement + receipt already adequately in v8, no action; **DROP** = superseded / wrong / pure-process, with reason.

### A. v7 Paper 4 — entanglement sources the graph, not the metric

Status note: v8 Paper 2's own status line declares it "supersedes v7 Papers 4, 6, 12, 13 and the positioning content of 16" — so Paper 4's *geometry* half must either be carried here or explicitly re-homed (v8 Paper 4, gravity/continuum, is the alternative); the existing `audit/port_paper4.md` swept only v7 papers 7/10/11/15/C2, so nothing else claims it. Grep across all six v8 papers for Van Raamsdonk / hop-metric / pinch / `E_cl` / graph: **zero hits** (the "connectivity floor" in v8 §4 is the *different*, Paper-12/13 result).

| # | Source | Content (one line) | Class | Receipts | v8 destination |
|---|--------|--------------------|-------|----------|----------------|
| A1 | §3 (opening) | The exact joint-odometer decomposition **`σ_AB = D(P_AB‖P_BA) = σ_A + σ_B + I_σ`** over the product state space, with the two anchor numbers: product ledger `I_σ = 5×10⁻¹⁴¹` (additive, no entanglement), correlated ledger `I_σ = −0.0712` (non-additive) | **KEEP-FULL** (small) | `p4a_joint_clicklaw.py` (24 checks) | §2 — v8 names `I_σ` "the interaction information of the joint seal statistics" but never writes the defining decomposition; one display + the two gaps makes the definition self-contained |
| A2 | §3 ("the skeleton is forced") | **Skeleton-transfer conditional** (expected big item — confirmed missing): the exponential survival *shape* transfers verbatim to the joint scalar argument `X = σ_AB` because the single-chain premise — survival a function of one accumulated scalar — *is* satisfied jointly (Cauchy residual `0`, `p = 1` only, a single `κ`, product limit `2×10⁻⁶`), **under the same single-channel + stationarity premises of paper 1** (dense regime; a genuinely-sparse joint regime inherits the geometric-progression skeleton instead); the genuinely new degree of freedom is *how the scalar decomposes* across the `A|B` cut | **KEEP-FULL** | `p4a` | §2 — this is the load-bearing bridge from paper 1's sequential law to this paper's transverse arena; v8 currently jumps straight to the moment set without saying why the joint survival law has the form `exp(−κσ_AB)` at all. Re-tag in the v8 system: [CONDITIONAL(paper-1 §3.2/§3.3 premises)] |
| A3 | §3 ("the additivity is not") | **Additivity-not-forced theorem** — the ancestor of the whole unforceability result: the *sequential* commit-order refinement telescopes **exactly (gap `0`) while `I_σ = −0.0712 ≠ 0`** — provably blind to the mutual term; additivity `X = χ_A + χ_B` is *equivalent to* the separate `A|B`-cut (parallel) factorization, which the sequential Cauchy argument never invokes | **KEEP-FULL** | `p4a` | §2 — currently v8 asserts `χ_AB` is "the filling variable" without the theorem that made it free in the first place; two sentences + the telescope gap |
| A4 | §3 ("the complement is free") | Free-complement characterization: `χ_AB` is **two-sided in sign** (`−0.0712` correlated / `+0.0182` no-signaling), a **continuum at fixed marginals** (marginal spread `10⁻¹⁴¹`), with parameter-independence exact (`PI` gap `10⁻¹⁴¹`) while `I_σ ≠ 0`; the complement becomes forced/unique only as a functional of the *full* bipartite kernel (nearest-product reference the unique minimizer) | **KEEP-FULL** (small) | `p4a`, `nonadditive_entangling_complement.py` | §2 ("the filling variable of this paper") — the two-sidedness and fixed-marginal continuum are the *evidence* of freeness; NOTE the sign convention: `χ_AB` signed (it is `I_σ`), distinct from any `MI ≥ 0` intuition |
| A5 | §3 + §8 | **`I_σ = 0` necessary-not-sufficient + the counterexample + the `E_cl` contrast** (expected big item — confirmed only half-present): `{factorized} ⊊ {I_σ = 0}` — a convex mixture exhibits `I_σ ≈ 0` at factorization-defect `≈ 0.09` (signed quantity cancels on a correlated kernel); only the **non-negative kinematic `E_cl`** (nearest-product KL of `Γ_AB`; `E_cl(Bell) = ln 2`, `E_cl(product) = 0`) vanishes **iff** factorized; the signed dynamical `I_σ` and the non-negative kinematic `E_cl` are *distinct functionals* sharing only the factorized ⟹ zero direction | **KEEP-FULL** | `p4a` (counterexample), `nonadditive_entangling_complement.py` (`E_cl`, nearest-product uniqueness) | §2 — v8 has the bare clause "necessary but not sufficient for product structure" with no counterexample and no `E_cl`; without `E_cl` the reader has no zero-iff-factorized object at all |
| A6 | §2 | The Tier-A entanglement objects this paper 2 presupposes: `E_cl(A:B)` defined (column-averaged KL of the joint transition law `Γ_AB` from its nearest *product* law; non-negative, gauge-invariant, locally monotone, Bell-state `ln 2`, zero iff product); the ontology (*finite same-process nonfactorization* — joint ledger not replaceable by local ledgers + shared classical records); the Bell address (Jarrett `LC = PI ∧ OI`; keep PI + no-signaling + single outcomes, drop OI) | **KEEP-FULL** (small, travels with A5) | — | §2 footnote-grade definitions. **Citation hazard:** v7 sourced these to v1 paper21 / v5 paper5 / v5 paper14 — under the v8 rule only v6 is declared citable, so port the *definitions inline* (self-contained) rather than the v1/v5 citations; the Bell-address sentence may cross-ref v8 paper 1's guardrail if ported there (port_paper1 row A16) |
| A7 | §4 | **The connectivity/metric weight-split [NO-GO on the metric]** (expected big item — confirmed absent from ALL of v8): entanglement content (`I(i:j)`, the whole joint KL) is **weight-0** (gauge-invariant under `l → μl`); a distance is **weight +1** (`#hops × l_step`); by the grading trichotomy a weight-0 record functional can never supply the one absolute `l_step` ⇒ **entanglement sources connectivity + the hop-metric up to one overall scale, provably not the absolute metric** — the third appearance of the same wall (spacing `d`, Newton's `G`, now the metric) | **KEEP-FULL** (BIG) | `p4b_connectivity_metric_split.py` (5 checks) | New v8 §: "What `χ_AB` sources downstream: the graph and its one missing unit" (natural seat: after current §4, before §5 — it is the *constructive* complement of the no-go: the filling is free, but *any* filling sources weight-0 geometry). Alternative seat: v8 Paper 4 (gravity/continuum) §1–2; if so, Paper 2 must carry a one-paragraph statement + pointer. The grading-trichotomy *tool* itself is ported to v8 Paper 1 S4 / Paper 3 SCALE leg (port_paper1 row C5) — cite in-v8, not v7 |
| A8 | §5 | **The pinch and the hop-metric detail** (the weight-0 half is real and non-circular): `I(A:B) > 0` = edge, `I = 0` = no edge ⇒ reducing entanglement disconnects (the Van Raamsdonk pinch; three-node exhibit `A–B` entangled, `C` product pinches off); integer shortest-path hop distances with **ratios `d_ij/d_kl` invariant to `10⁻¹⁰⁰`** under `l_step → μl_step`; honesty guards ported verbatim in spirit — the result is *coarse* (unweighted graph only, no Riemannian similarity class, no curvature), **not** a `−log I` conformal claim (that carries an additive zero-point gauge under `I → sI`, fixing distance *differences* not ratios, and one overall scale ≠ local-Weyl class), and "entanglement = area" survives only as "= area *up to the one free unit*", never the absolute RT coefficient | **KEEP-FULL** (BIG) | `p4b` | Same new § as A7 — the pinch is also the natural *companion* of v8 §4's connectivity floor (`χ_AB ≠ 0` for a connected space): pinch = zero-correlation ⇒ disconnection; floor = connection ⇒ nonzero `χ_AB`; state them side by side |
| A9 | §6 | **The circularity audit**: every existing geometry-from-entanglement in the corpus runs the *opposite* (Jacobson) direction — geometry assumed, entanglement–area read off (induced-gravity-grade, not vicious); the one genuinely **vicious trap** named — scoring recovered coordinates against the *true simulation coordinates* of a metric-generated sprinkling (recovering a metric that went in is consistency, not emergence); clean-construction criteria: (i) correlation defined on `Γ` alone (no Hilbert/tensor arena silently re-imported as "pre-geometric"), (ii) reconstruct only the weight-0 *relative* metric, blind, (iii) never source geometry from a weight-bearing coefficient | **KEEP-FULL** | — | Same new § (closing paragraph), or v8 Paper 4 §5 if the geometry half re-homes there. Overlap note: `port_paper4.md` lists a "held-out-coordinates methodology guard" gap (from v7 paper 11) — same *trap*, different source; port once, cross-reference. Criterion (i) is also a live honesty guard for Paper 2's own §2 premise (an operator algebra is being *posited*, not smuggled) — worth one sentence in §2 |
| A10 | §7 | Does-not-claim ledger for the geometry half: no absolute metric, no RT area-coefficient `1/4G`, no ER=EPR wormhole *length* (all weight +1, no-go-blocked); full bulk-emergence premature (needs `χ_AB` for the gravitational ledger *and* the matter sector); "particle = stable long entanglement link" NOT in the corpus (named future program, gated behind matter) | **KEEP-FULL** (small, travels with A7/A8) | — | Same new § + one line in §7 (honest scope). The Tsirelson-is-external attribution from the same list is already v8 scope guard 1 (KEEP-RESULT there) |
| A11 | §3/§7 | "Constrained only by no-signaling and Tsirelson" as the *total* constraint on `χ_AB`, and the closing open question "whether a transverse self-consistency principle … could convert `χ_AB` from free to forced — untested, the natural next step" | **DROP** (as stated) | — | Superseded: the constraint set is now the full almost-quantum fence (v8 §3, five elements — strictly sharper than "no-signaling + Tsirelson"), and the open question was *answered* by v7 Papers 6/12/13 (fence-not-filling) — v8's abstract already carries the settled form. Keep only as narrative ancestry if desired (one clause) |
| A12 | §3 (OI paragraph) | Forced skeleton permits OI-violation: a full OI-violating, PI-yes, no-signaling, Tsirelson-*saturating* (`CHSH = 2√2`) correlation rides the forced skeleton; PR-box excluded | KEEP-RESULT | `f5` (21/21) | Already v8 §6 (OI ⊥ skeleton lemma) + §3 item 2 (click-law realizes the Tsirelson point, `5.2×10⁻¹⁴¹`) — count `21/21` correct per the corrections list |
| A13 | Abstract/§1 | Status-upgrade narrative (Paper I §5.2 `[OPEN]` → "shape forced, content free"); the two-directions framing (constructive vs Jacobson) | **DROP** | — | Process/narrative; content carried by A2–A4; the constructive-direction framing survives inside the A7/A8 port as one sentence |
| A14 | header | Old tag system `[FORCED]/[FREE]/[PERMITTED]/[NO-GO]/[OPEN]` | **DROP** | — | Superseded by the v8 tag system; re-tag on port: A1/A3/A5 [THEOREM]-grade on the arena, A2 [CONDITIONAL(paper-1 premises)], A7 [THEOREM given the grading tool, cited in-v8], A8 [DEMONSTRATED] |
| A15 | §8 | Precision/receipt specifics not yet in v8: `p4a` = **24 checks** (v8 lists the file but no count), `nonadditive_entangling_complement.py` (not listed in v8 at all), `p4b` = **5 checks** (not listed), product-limit residual `2×10⁻⁶`, "no spacetime, metric, or `s²` used as input anywhere" | **KEEP-FULL** (small) | — | §8 receipts + the no-geometric-input sentence into §2 or §7 (it is the pre-geometric honesty guard for the whole arena) |

### B. v7 Paper 6 — transverse almost-quantum (the arena, the level, Tsirelson inherited)

Status note: Paper 6 is the *arena-construction* paper — it defines what a transverse self-consistency principle can even be, fixes the level, and proves the envelope necessary-not-sufficient. v8 §2 carries the arena in compressed premise form; the structural argument for the arena's *exhaustiveness*, the two-claim (C-form)/(C-level) discipline, the level-independence theorem, and the entire Information-Causality static core are missing. Paper 6's SDP digits (`t1`: `2.8284271250`, gap `2.78×10⁻¹⁰`) are superseded by v8's own fresher `p6` run (`2.8284271251`, `3.16×10⁻¹⁰`) — do not port stale digits.

| # | Source | Content (one line) | Class | Receipts | v8 destination |
|---|--------|--------------------|-------|----------|----------------|
| B1 | §2 | **The (C-form) structural argument** — *why any* self-consistency principle the substrate can express is a moment-positivity condition: a principle is a closure condition on the records' *own* correlations; the chains meet only through `⟨A_xB_y⟩` (commutation on the state, no global state space); hence every expressible principle is a balance/extremum **over the PSD cone of `Γ`** | **KEEP-FULL** | `t1` (superseded by `p6`) | §2 — v8 posits the premise but never argues that the arena *exhausts* what a record principle can be; without (C-form) the no-go quantifier ("any principle the records can host") is unsupported. One paragraph |
| B2 | §2 | **The (C-form)/(C-level) two-claim separation, by name**: (C-form) = robust structural claim; (C-level) = flagged substrate-modeling premise — a record interface commits a single joint outcome pair, furnishing degree-`(1,1)` cross moments only (a higher cross-party word `⟨A_xA_{x'}B_y⟩` would bind two non-commuting `A`-seals to a `B`-seal and reach a higher level) | **KEEP-FULL** (small) | — | §2 + scope guard 2 — matches the v8 series' premise-ledger style; v8 currently has the premise [POSITED] but not the named two-layer split or the higher-word mechanism explaining *why* the level is `1+AB` |
| B3 | §3 | **Level-independence of the qualitative no-go**: at *every* finite NPA level and in the commuting-operator limit the records still furnish no tensor factorization (Slofstra 2019; Ji–Natarajan–Vidick–Wright–Yuen `MIP* = RE`), so `χ_AB` is free *strictly above `Q`* regardless of (C-level) — the premise controls only *how far* above `Q` (`Q̃` at `1+AB`, tighter supraquantum sets for higher words), never *whether* the gap exists | **KEEP-FULL** (small) | — | §2 end (v8 has one clause: "the commuting-vs-tensor distinction survives every finite level") and §4's [OPEN] paragraph — the [OPEN] "higher-word substrates not audited" should be paired with this theorem: higher words shrink `Q̃` toward `Q` but *never reach it*; the residue is the `I3322`-specific separation level, not the headline no-go |
| B4 | §3 | Envelope SDP entailments: `Γ ⪰ 0` caps `CHSH` at `2√2`, PR-box `E = (1,1,1,−1)` infeasible, sharp boundary (Tsirelson point feasible, just-past point `CHSH = 2.9084` infeasible) | KEEP-RESULT (mostly) | `t1` 8 checks → superseded by `p6` 43/43 | Already v8 §3 items 1–3 with fresher digits. Optional one clause: the **just-past-Tsirelson infeasibility** — v8 verifies sharpness for the monogamy facet but not for the CHSH ceiling |
| B5 | §3 | `Q̃ ⊋ Q` witnessed on `I3322`, quantum boundary needs relaxation level ≥ 4 | KEEP-RESULT | — | Already v8 §3 item 4 in *corrected, strengthened* form (the explicit nesting `0.2514709 > 0.2509397 > 0.2508754`, Pál–Vértesi cited, retired numbers quarantined) — v8 is strictly better here |
| B6 | §3 verdict | "Necessary-not-sufficient": the transverse principle reproduces the envelope but leaves `χ_AB` free across `Q̃`; the parallel-chain analog of `C = J` exists only as envelope-pinning, never content-pinning | KEEP-RESULT | — | Already v8 abstract + §4 ("fence forced, filling free") |
| B7 | §4 | **One source, two faces — the tensor product ≠ local tomography distinction** (collapsed in v8 to the slash-phrase "tensor-product / local-tomography / complex-over-real choice"): the **global tensor product** closes `Q̃ ∖ Q` (the *moment-form* gap); **local tomography** `K_AB = K_A·K_B` selects ℂ over ℝ *within* a tensor-product world (the *state-space-form* gap); these are **distinct axioms** — the rebit theory *has* a tensor product yet *fails* local tomography — sharing one origin: no composite state space at all | **KEEP-FULL** | `t2` (superseded by `p6` K-deficit part) | §5 opening — the conceptual anatomy of "the residual bit"; without it the v8 slash-phrase reads as three names for one axiom, which Paper 6 explicitly proves false |
| B8 | §4 | The K-count arithmetic: `K_qubit = 4`, `K_2qubit = 16 = 4·4` (LT holds over ℂ); `K_rebit = 3`, `K_2rebit = 10 ≠ 9` (deficit `+1`, joint parameters invisible locally) | KEEP-RESULT | `p6` | Already v8 §4 M2 (`K_single = 3/4`, `K_AB = 10/16`, `10 ≠ 9`, Wootters cited) |
| B9 | §4 | Seal field-blindness: the single-chain forcing (`E = E² = E*`, the `q = 2` Banach–Lamperti screen) holds *identically* over ℝ and ℂ; the rebit foil has the same marginals and the same `2√2` to full precision but a different `χ_AB` | KEEP-RESULT | `t2` → `p6` | Already v8 §4 M2 in the sharper Paper-12 form (identical marginals/cross-moments/`q=2` seal/sealing scalar at the Tsirelson correlators) |
| B10 | §5 | Tsirelson upgraded from *respected* (Paper IV) to **inherited** — a theorem of the records' own `Γ ⪰ 0` | KEEP-RESULT | — | Already v8 §3 (fence element 2) + scope guard 1 (attribution to Tsirelson/Landau, no priority claim) |
| B11 | §5 | **The Information-Causality static core** (confirmed absent from ALL of v8; genuine second witness that the ceiling is Tsirelson's): the record KL mutual information owns the four PPKSWŻ properties — non-negativity, consistency (`I = 0` iff factorized), data-processing, chain rule (residual `3.63×10⁻¹²²`); the IC facet `V(E) = E² − ½ ≤ 0` saturates *exactly* at the Tsirelson point (`V = −9.7×10⁻¹²²` at `E = 1/√2`; `V < 0` sub-, `V > 0` super-Tsirelson, PR maximally violating); **honesty guard travels**: one operational lever short of full IC — the random-access-code protocol is *not* forced by the seal-law, so the claim is "static core owned", never "IC derived" | **KEEP-FULL** | `t3` (17 checks, dps ≥ 100) | §3 closing paragraph ("an independent witness") + §7 non-claim + §8 receipts (`t3` must be listed) + References (Pawłowski–Paterek–Kaszlikowski–Scarani–Winter–Żukowski, Nature **461**, 1101 (2009) — currently missing from v8 refs) |
| B12 | §6 | The fifth-wall shape ("self-consistency fixes the envelope, never the last record-blind input"; scale ↔ composition rule) and the posture sentence ("the records do not evade the `Q`/`Q̃` boundary — they reproduce it from the inside") | KEEP-RESULT | — | v8 title + abstract carry "from inside"; the shape unification is v8 Paper 3's job (walls classification), already cross-referenced there |
| B13 | §7 | Non-claim guards: `Q̃` itself is an external construction (NGHA) whose definition the records *instantiate rather than invent*; the paper proves the *absence* of ℂ-QM/LT/tensor is the obstruction, it does not derive them | KEEP-RESULT (one optional clause) | — | v8 §6 imports NGHA and disclaims derivation; the "instantiate rather than invent" formula is a compact attribution guard worth one clause in §6 |
| B14 | §8 + header | Old receipts `t1` (8 checks) / `t2` / `t3` (17); old tag system `[NECESSARY]/[INHERITED]` | **DROP** (except `t3`) | — | `t1`/`t2` superseded by `p6` 43/43 (envelope, K-deficit, field-blindness re-verified fresher); `t3` survives with B11. Tags re-map: [NECESSARY]→fence [CONDITIONAL], [INHERITED]→fence element, [FORCED] one-source→[THEOREM on the arena] |
| B15 | §1 | **The single-chain crossing template recap** (expected big item): the transverse question is defined *against* the one-diamond `C(η) = J(η)` balance — accumulated KL content vs Fisher capacity, unique crossing `η_* = 1.0903443548…`, `W_* = 0.3648…` — "the template for everything the records fix without external input" | **KEEP-FULL** (small, cross-ref form) | `f3_self_consistency.py`, `p2a` (paper-1 ports) | §4 M1 — v8 invokes "the transverse analog of the single-chain capacity crossing" without ever saying what the single-chain crossing *is*; one defining sentence with both constants + explicit cross-ref to v8 Paper 1's supply/spacing section (port_paper1 rows A2/B4 put the full derivation there; Paper 2 needs only the recap sentence) |

### C. v7 Paper 12 — the transverse no-go, envelope forced from inside (the core source)

Status note: Paper 12 is the paper v8 Paper 2 most directly consolidates, and it is *post-correction* (its §8 already carries `0.2514709 > 0.2509397 > 0.2508754` with `8.748` quarantined) — its content is safe to port except the stale receipt count (`42/42` → now `43/43`, v8 correct). v8 carries every Paper-12 *verdict* but compresses each *mechanism* to roughly one clause; the rows below are the derivation layer.

| # | Source | Content (one line) | Class | Receipts | v8 destination |
|---|--------|--------------------|-------|----------|----------------|
| C1 | §1 (close) | **The mechanism contrast that makes this "a new kind of no-go"**: the scale/`G` no-go is a *grading-homomorphism* obstruction (weight `+1` object can never be a weight-`0` functional — a degree count); here `χ_AB` is weight-`0` — *the right weight to be forced* — and the obstruction is instead **invariance / capacity-degeneracy at weight-0** (self-consistency constant along the `R`-orbit; capacity flat in the entangling direction) | **KEEP-FULL** (small) | — | §4 opening paragraph — v8 labels the mechanisms "weight-0" but never states the contrast with the scale wall, which is what earns the tensor wall separate existence; one short paragraph + cross-ref to v8 Paper 3 (which carries the `G₁ = ℝ₊` vs `G₂ = ℤ₂` group-level version) |
| C2 | §2 | **The transverse-forcing checklist (i)–(vii)** — the test harness of the whole no-go: (i) two record-internal scalar functionals of one shared transverse parameter; (ii) *proved* monotone opposition; (iii) supply-vs-capacity at weight-0; (iv) *scalar* capacity (else Pareto only); (v) crossing at an *intrinsic* value, not marginal-drifting; (vi) max-min attained analytically; (vii) no imported quantum axiom — **with the failure map: the transverse principle fails (ii), (iv), (v), (vii)**. Plus the **LAW-A vs LAW-B discipline**: only a max-min *crossing* (LAW-A) forces a canonical number; a *firing*-type fixed point (LAW-B, `tanh η = e^{−η}`, `η_B = 0.6093778634…`, committing `0.428·W_*`) is mode-dependent and forces nothing canonical — a transverse analog mimicking the firing inherits that | **KEEP-FULL** | `p6` PART 0 | New §4.0 ("what a transverse forcing would have to be") — v8 §4 currently *asserts* M1/M2 without the criterion they are measured against; the checklist is what turns "we tried and failed" into "every principle of the expressible class fails at named items". Template constants: one recap sentence + cross-ref v8 Paper 1's supply/spacing section (see row B15; full derivation ports there per port_paper1 A2/B4) |
| C3 | §3 | **M1 construction in full** (expected big item — v8 has one sentence): the joint record law is the max-ent family `P(a,b) ∝ exp(η_A a + η_B b + λ ab)` on sufficient statistics `(a, b, ab)`; the faithful capacity leg is the marginal-tilt Fisher `Var(a) = 1 − ⟨a⟩²` (exactly the single-chain `J = Var(q)`); the fixed-marginal protocol (re-solve `(η_A, η_B)` as functions of `λ` to pin `⟨a⟩ = ⟨b⟩ = m`; marginal residual `< 10⁻¹⁴⁰`); **flatness numbers**: `Var(a)` spread `1.3×10⁻¹⁴¹` across `λ ∈ [−0.6, 0.6]`, central-difference `dJ/dλ` at the `~10⁻¹¹²` round-off floor; **and no rising leg either**: `C_AB(λ)` is U-shaped at fixed marginals (at `m = ½`: centre `0.26162407`, wings `0.37125` / `0.31128`), reflecting the signed two-sidedness — so `max_λ min(C_AB, J)` is not even well-posed | **KEEP-FULL** (BIG) | `p6` PARTs 1–2 | §4 M1 expanded to a subsection — the flat-direction computation *is* mechanism M1; v8 asserts "computed flat to `1.3×10⁻¹⁴¹`" without the family, the protocol, or the U-shape |
| C4 | §3.2 | **The circularity exhibit**: a forced-*looking* balance (`C_AB = Cov(a,b)`) has a root that **moves with the marginals** (`λ_* = 0.1155` at `m = 0.3`, `0.6189` at `m = ½`, non-robust at `m = 0.7`) — the diagnostic signature of a non-forcing balance; structural reason: at fixed marginals the only free level-`(1+AB)` coordinate is `E_ab`, and the only responsive Fisher entry is `Cov(a,b) = E_ab − ⟨a⟩⟨b⟩` — the entangling content *itself*; balancing content against the entangling content is circular | **KEEP-FULL** | `p6` PART 2 | §4 M1 subsection — v8's parenthetical "(alternative capacity definitions drift with the marginals — the tautological-crossing exhibit)" is the only trace |
| C5 | §4 | **M1 robustness sweep over capacity definitions** (expected big item): entangling-parameter Fisher `Var(ab) = 1 − E_ab²` is a **hump** (sympy-exact `sign(dJ_λ/dλ) = −sign(E_ab)`; turning point `E_ab = 0` marginal-dependent, pushed to the boundary for `m ≥ ½`); the `A|B`-cut mutual information *and* the Blahut–Arimoto channel capacity are **U-shaped** (and the channel reading re-optimises the input, moving the marginals); `E_cl` is U-shaped, sign-blind; **the exhaustive three-way classification**: any record-internal capacity is marginal-only (blind) / even-in-the-signed-correlation (U/hump, sign-blind) / `E_ab` itself (tautological) — none is the strictly-monotone-opposed, marginal-independent-crossing pair that forced `W_*` | **KEEP-FULL** (BIG) | `p6c` (narrative; cross-checked in `p6`) | §4 M1 subsection — this is what backs v8's "not an artifact of one capacity choice"; the three-way classification is the quantifier over definitions |
| C6 | §4 (end) | **The canonicity criterion + the golden-crossing counterfeit**: one *can* manufacture a monotone-opposed pair (`E_ab` rising vs `1 − E_ab²` falling), crossing at the golden moment `E_ab^* = (√5−1)/2 = 0.6180339887…` — but at a marginal-dependent `λ` (`0.685 / 0.602 / 0.377` at `m = 0.3 / ½ / 0.7`) and carrying a **free relative dial** `c` under which the root moves (`0.414 / 0.618 / 0.781` at `c = 0.5 / 1 / 2`); the criterion separating the genuine `W_*`: the forcing pair must be the KL content and Fisher capacity **of one canonically-normalised exponential family** (`C = ηψ′ − ψ`, `J = ψ″`, same log-partition `ψ = log cosh η`, `q = 2` screen fixing the units — no free relative scale available); honesty note travels: this is *not* a claim that `W_*` is invariant under independent rescaling of `C` and `J` (the crossing would move; the point is the records offer no such dial) | **KEEP-FULL** | `p6c` | §4 M1 subsection close — the criterion is the *positive* characterization of what a genuine forcing is, reused nowhere else in v8; without it "tautological" is name-calling |
| C7 | §5 | **M2 construction in full** (expected big item — v8 has the verdict paragraph): at the Tsirelson correlators `E = (s, s, s, −s)`, `s = 1/√2`, the joint law `p(a,b|x,y) = (1 + ab·E_xy)/4` is realised by *both* a qubit singlet and a rebit Bell pair; `R` preserves every element of `M` with numbers — identical marginals `(½, ½)`, cross-moments (`CHSH = 2√2` to `< 10⁻¹¹⁸`), orthogonal-projection seal (`|E² − E| ~ 6.6×10⁻¹⁴²` over *both* fields), `q = 2` norm, Tsirelson point, and the decisive check **`I_σ(rebit) − I_σ(qubit) = 0` exactly (both `= 0.27665…`)** — while flipping the LT-deficit `+1/0`; the selecting bit lies in the **kernel of the records' factorisation through `M`** | **KEEP-FULL** | `p6` PARTs 3–4 | §4 M2 expanded — v8 M2 asserts the invariances ("identical marginals, cross-moments, `q = 2` seal, sealing scalar") without the two realizations, the correlator table, or the joint-law formula; the construction is the proof. NOTE v8 quotes the `I_σ` gap as "`0` exactly; `< 10⁻¹³⁰` numerically" — keep v8's phrasing, add the shared value `0.27665` |
| C8 | §5 (end) | **"Two faces of one wall"** — M1 *is* M2 seen information-geometrically: the faithful capacity is a function of the `R`-invariant marginals, hence flat in the entangling direction; and the M2 wall is "unreachable not by weight but by symmetry" | **KEEP-FULL** (tiny) | — | §4 — one sentence unifying the two mechanisms; currently only the v8 *abstract* hints at it ("a function of the `R`-invariant marginals alone"), the body never joins them |
| C9 | §6 | **Escape 1 closed — the retained-holonomy phase** (expected big item — v8 has one clause): the closure schema — a breaking resource must be *simultaneously* (a) forced by sealing self-consistency, (b) not a functional of `p(a,b|x,y)`, (c) field-distinguishing; three realizations of the Tsirelson point (`R1` X–Z qubit singlet, `R2` Y-phase qubit, `R3` rebit pair; identical correlator tables, gap `0`); **(a) fails**: the Y-phase is pure local gauge (`R1`/`R2` identical full moment tables, gap `2.2×10⁻¹⁴¹`; identical Bargmann invariant, `7.4×10⁻¹⁴³`), and the forced `q = 2` calculus is **real-closed** (holds over ℝ — the seal never manufactures a phase); **(b) fails**: the composite seal-loop invariant *is* the degree-`≤3` moment polynomial `z = (1 + Σ⟨O_k⟩ + Σ⟨O_iO_j⟩ + ⟨O₁O₂O₃⟩)/8`, verified **non-vacuously** over generic loops/states with `|z|` up to `0.73` (reconstruction gap `1.3×10⁻¹⁴¹`); **(c) fails**: `Im(z) = 0` on all shared X–Z seals over both fields (real matrices ⇒ Bargmann phase ∈ `{0, π}`); the only genuinely complex value needs an out-of-plane Y-seal (single-party Pancharatnam `γ_Y = π/3 = 1.0472`), which the rebit substrate cannot host, self-consistency does not force, and a local unitary fixing all of `M` removes; **positive control**: `Tr(P_XP_YP_Z) = 0.25 + 0.25j` (`Im = 0.25`, the octant solid angle) — the probe *would* see a phase if the substrate carried one | **KEEP-FULL** (BIG) | `p6b` (21 checks) | §4 escapes subsection — v8's single sentence names the polynomial, `Im z = 0`, and the positive control but loses the (a)/(b)/(c) schema (the *logic* of the closure), the gauge-invariance step, the real-closedness of the seal calculus, and `γ_Y = π/3`; also resolves the corpus's flagged "retained-holonomy phase enters composition without proven content" — that resolution sentence should port |
| C10 | §7 (sign) | **Escape 2 closed — the sign is permanently free**: the outcome-relabel involution `τ` flips `E → −E` (hence `I_σ`'s sign) while leaving `E_cl`, `E²`, the marginal Fisher, and the CHSH-magnitude **exactly invariant** (sympy-exact); witness pair `E = ±0.68`: identical `E_cl = 0.253477301159…` (gap `0`), signed interactions summing to exactly `0`; the sign lives in a quotient every magnitude functional is blind to — free *independently* of whether any magnitude is ever forced | **KEEP-FULL** | `p6d` (20 checks) | §4 escapes subsection — v8 has six words ("the outcome-relabeling involution kills the sign") |
| C11 | §7 (magnitude) | **Escape 3 closed — no magnitude selected**: `E_cl` is a strictly-monotone continuum `0 → ln 2` realising every interior value at fixed marginals (`ln 2/2` at `t = 0.194986…`, residual `< 10⁻¹³⁶`); the single-chain forcing has no transverse instance (capacity flat ⇒ balance degenerate); any candidate balance (`E_cl = c·Cov`) drifts with the marginals (`0.131 → 0.170`, drift `0.038` — the M1 circularity signature again); `E_cl` is even in the signed coupling with its only internal stationary point the trivial product `0` | **KEEP-FULL** | `p6d` | §4 escapes subsection — v8 has one clause ("spans `0 → ln 2` without selecting") |
| C12 | §8 | The envelope from inside: `Γ ⪰ 0` forced as the Gram matrix of the records' own observables (with the Gram caveat — off-diagonal completion entries not table-measurable; commuting-operator relaxation strictly weaker than the missing tensor product); the five envelope elements incl. the corrected `I3322` nesting and the monogamy sharp scan | KEEP-RESULT | `p6` PART 5 (all SDP digits [SOLVER-TOL]) | Already v8 §2 (premise + caveat, guard 2) and §3 (all five elements, corrected numbers, monogamy sharpness *plus* the extra `(2√2, 0.30)` direction) — v8 is at parity or better here |
| C13 | §8 (cocycle) | **Cocycle-triviality detail**: the associator `((s₁s₂)s₃)/(s₁(s₂s₃)) ≡ 1` **sympy-exact** (because `exp` is a homomorphism to `(ℝ, ×)`), grouped survival grouping-independent over `2200` random assignments (gap `1.3×10⁻¹⁴¹`); scope guard: this certifies the *scalar* skeleton only — **the nontrivial associator that *would* force structure is an `F`-symbol of a fusion category, which needs exactly the composite tensor product the seal is blind to — the same wall by a different road** | **KEEP-FULL** (small) | `p6` PART 6 | §4 (the cocycle sentence) — v8 has the 2200-assignment fact; the associator identity and the F-symbol scoping remark (a genuinely clarifying honesty guard) are absent |
| C14 | §9 | **[OPEN] residue detail**: (a) the moment-polynomial result of §6 is **field-agnostic** — it covers *any* field reproducing the same real `E_xy` (this *softens* the un-audited-quaternionic-foil gap and should be stated next to it); (b) the two honestly-unexhausted capacity classes — capacities via a *preferred tensor split* (import the very `χ_AB` they would force) and off-state / higher-moment algebras (escalate to a different arena) — are named *with the reason they are not record-internal Tier-A principles* | **KEEP-FULL** (small) | — | §4 [OPEN] paragraph + §7 — v8 names the two residues (higher words; no quaternionic foil) but not the field-agnostic mitigation nor the two excluded capacity classes |
| C15 | Reproducibility | Receipt texture: `p6` PART 0 (the single-chain template) is missing from v8 §8's parts list (v8 lists six of seven parts); the **η_*-root honesty note** — any "two-formula `|J − C|` agreement" is a root-finder residual, *not* independent corroboration (the load-bearing content is `W_*`'s value + the sympy-exact sign opposition); the "strict `Q̃ ⊋ Q` rests on NGHA + Pál–Vértesi + Slofstra as field-established, not re-derived" provenance sentence | **KEEP-FULL** (tiny) | — | §8 — add "single-chain template" to the p6 parts list *if* row C2 ports; the root-finder note travels with any port of the template; the provenance sentence fits §6 |
| C16 | header/§9 | Stale receipt count `p6 = 42/42`; old tags `[FORCED]/[CONSTRAINED]`; "three SHARD-specific last inputs" phrasing implying symmetric status | **DROP** | — | Superseded: `p6 = 43/43` (the CG-`I3322` fix added a check — v8 §8 correct); tags re-map to v8 system; the three-inputs list survives only in the corrected **three-different-statuses** form (v8 §5 has it) |
| C17 | §8–9 | "The records force the fence but never the filling" meta-line; the upgrade ledger (Paper IV *respected* → Paper VI *located* → here *derived-from-inside*) | KEEP-RESULT | — | v8 abstract/§§3–4 carry the meta-line; the upgrade ledger is v7-internal narrative — one optional clause in §6 if ancestry is wanted |

### D. v7 Paper 13 — the Feynman map: `χ_AB` bottoms out at `Q̃`

Status note: the connectivity floor and manifoldlikeness obligation live *here* (§5), not in Paper 12 — v8 §4 already attributes them correctly as sweep-fallout. Paper 13 is post-correction (CG-projector witness, `0.2509397` full level-2, `8.748` quarantined, Renou-contested paragraph). v8 carries the headline and the receipt list; the sector-by-sector pushes, the witness-certification logic, the covariance gates, the order-feedback three-grade closure, and both new constraints' *derivations* are each compressed to a clause or a word.

| # | Source | Content (one line) | Class | Receipts | v8 destination |
|---|--------|--------------------|-------|----------|----------------|
| D1 | §1 | **The Feynman-probe logic + the four dial positions**: sweep `(a)` `χ_AB = 0` (product), `(b)` generic `P_Q ∈ Q`, `(c)` the super-quantum witness `P* ∈ Q̃ ∖ Q`, `(d)` supra-`Q̃` PR-box (already `Γ ⪰ 0`-excluded) — with the decisive-probe statement: *if anything downstream breaks at `P*`, global consistency would select `Q`, recovering the tensor product as derived rather than postulated* | **KEEP-FULL** (small) | — | §4 "global consistency" paragraph — v8 states the outcome ("nothing breaks") without the test's stakes; the counterfactual is what makes the null result a theorem-shaped finding |
| D2 | §2 | **The witness construction + self-contained certification** (expected big item): `P*` is the Collins–Gisin projector `I3322` optimum over the *true* level-`(1+AB)` matrix — base words **plus all nine cross-products** `P_A[x]P_B[y]` (not the bare first NPA level; the Paper-XIII round-1 lesson) — `0.2514709`, exceeding the **full level-2 rigorous upper bound on `Q`** (`0.2509397`) by `+5.3×10⁻⁴`, **certifying `P*` outside `Q` self-containedly, without relying on any external digit** (Pál–Vértesi's level-`≥4` `0.2508754` then corroborates); extracted as a fully-specified no-signaling probability table and pushed *by its own probabilities* | **KEEP-FULL** | `s_probe_qtilde_minus_q.py` (26/26) | §3.4 + §4 — v8 has the nesting and "extracted as a … table" but **not** the all-nine-cross-products construction detail nor the self-contained-certification reading (level-2 as an in-house `⪰ Q` bound), which is what frees the claim from external digits |
| D3 | §2 | **The sector push table with values** (the non-vacuity of "nothing breaks"): geometry — the entanglement graph read off `P*`'s own per-setting `I(A_x:B_y)` (a moment functional that *moves* across settings, connected graph); correlation — static IC core chain-rule residual `0` on `P*`'s marginals, macroscopic locality, local orthogonality all satisfied; covariance — no-signaling residual `0`, a boost preserves `386 = 386` causal relations; matter — mode fixed point `η_B = 0.6093778634…`, gap `W = 0.1561092001…`, chiral `m̂ = ln 2`, all flat under the `χ_AB` sweep (`< 10⁻¹⁰⁰`) | **KEEP-FULL** | `s_probe` | §4 — v8 lists the six sector names with zero values; the values are what show each sector was *actually computed*, not waved at |
| D4 | §2 (honesty block) | **The by-construction honesty that controls the whole result**: the numeric sector-by-sector identity is true *by construction* (same level-`(1+AB)` data, differing only in the kernel flag) — the load-bearing claim is the **structural** one (each sector *is* an `M`-functional), and the numbers are illustration, not independent evidence; plus the **macroscopic-locality caveat**: ML reaches only `Q¹`, so it cannot exclude `P*` — only "satisfies" is asserted | **KEEP-FULL** (small) | — | §4 — v8 states "every sector is an `M`-functional" as fact but not the honesty inversion (structure load-bearing, numerics illustrative); omitting it invites the strongest referee objection the source already answered |
| D5 | §3 | **The covariance gates** (expected big item — v8 has the word "covariance"): (1) no-signaling is a **Lorentz scalar** (frame-independent count; exact, any box incl. PR); (2) **microcausality *is* the on-state commutation that defines `Q̃`** — two faces of one constraint, which is *why* covariance recovers `Q̃` and never `Q`; witnessed **without smuggling the tensor product**: abstractly (free non-commutative algebra, `[A_x,B_y] = 0` alone collapses the cross-moment) and on a **single Hilbert space of prime dimension 3** where no factorisation `H_A ⊗ H_B` exists (tensor realisation sufficient-not-necessary); (3) `P*` passes — no-signaling, commuting, **sub-Tsirelson CHSH block `1.339`** so the van Dam/Brassard communication-complexity collapse (needs `CHSH > 2√2`) does not bite; `P*` is a `Q̃`-**boundary** point (native moment matrix singular; hard-equality pin at solver floor `≈ −2×10⁻⁹`, margin `+1.1×10⁻⁷` under a small moment band — boundary membership is membership); the comm-complexity/relativistic teeth act only *outside* `Q̃`; Tier-B premises (emergent-Lorentz action, point locality) quarantined as Paper-VII gates, conditional not new | **KEEP-FULL** (BIG) | `c3_covariance_chi_ab_probe.py` (15/15) | §4 covariance subsection — also resolves a likely reader worry v8 never addresses (super-quantum boxes are famous for causality pathologies); the prime-dim-3 witness is the cleanest anti-smuggling exhibit in the corpus. Cross-note: the *general* covariance layer is v8 Paper 4 §6 (port_paper4 severity-1 gap); this χ_AB-specific probe belongs in Paper 2 with a pointer |
| D6 | §4 | **The order-feedback closure at three grades** (expected big item — v8 has "(σ ⊥ χ_AB, sympy-exact)"): *structural* — `σ = D(P_AB‖P_BA)` is longitudinal (variables `{p_ij}`), `χ_AB` transverse (variables `{λ, η_a, η_b}`), sets **disjoint** sympy-exactly, `∂σ/∂χ_AB ≡ 0`, and a coupled generator `σ + g·F(χ)` has `g = 0` forced by the corpus definition; *feedback numeric* — 200 random graph re-wirings permute the `σ`-ranking of `K = 6` distinct seals **zero** times; `σ`-spread exactly `0` while `χ_AB` moves `0 → 0.3657` and `E_xy` by `1.295` at pinned marginals; *non-vacuity* — an adversarial surrogate (`χ`-incidence rank) genuinely produces a different ordering (`[0,4,3,5,1,2]` vs `[4,3,0,2,1,5]`), so a `χ`-driven order is constructible — it is just **not the SHARD generator**; honest residual is **definitional**: re-opens only under redefinition to an unbuilt `χ`-driven dynamics that paper 1's forced odometer excludes | **KEEP-FULL** | `s_order_feedback_probe.py` (18/18) | §4 — the closed "genuine frontier where a break could have hidden"; the adversarial non-vacuity check is the part that makes "closed exactly" meaningful |
| D7 | §5 (1) | **The connectivity floor, honest form** (expected item — v8 has the six-word clause): edge ⟺ `I(i:j) > 0`; edge weight sign-blind (`I(±0.85) = 0.42676…`, sympy-exact via the relabel involution); **the honest pinch** — a *single* zero bond does **not** disconnect (one edge removed, node attached through other bonds; verified one-component), genuine isolation needs a **globally product node** (all bonds zero → two components, the Van Raamsdonk pinch); hence *a connected emergent geometry requires `χ_AB ≠ 0`* — but this carves the **measure-zero face** `{χ_AB = 0}`, **not** the positive-measure `Q/Q̃` gap (quantum vs almost-quantum give the *identical* graph at equal edge weight, `I = 0.19274…`, gap `0`); genuinely new vs the device-independent literature (single-scenario, geometry-blind) | **KEEP-FULL** | `s_geom_chi_sweep.py` (20/20, zero solver-tol) | §4 (the floor sentence → a short paragraph) — CRITICAL interaction with row A8: paper 13's *honest pinch* refines paper 4's bare pinch (single-bond vs globally-product-node); when the Paper-4 geometry half ports, use **this** sharpened form, not the Paper-4 original |
| D8 | §5 (2) | **The manifoldlikeness obligation, derived** (v8 has four words): the multi-record `χ_AB` *magnitude pattern* must be manifold-like, but Kleitman–Rothschild dominance makes that rare — at `n = 10⁴`: `~n²/4 = 2.5×10⁷` bits of generic partial orders vs `~n log n = 5.3×10⁵` bits of manifold-embedding data; the `R`-invariance of the diagnostics is **structural, not numeric** (Myrheim–Meyer functionals read the magnitude-induced *order* alone and `R` preserves every edge magnitude exactly — a literal no-op, invariant *by construction*); **non-vacuous** (`d_MM = 1.9453` for a 2d sprinkle, in the manifold window `[1.7, 2.3]`, `r = 0.52086`; a 1d-like pattern gives `d_MM ≈ 1.0`); the obligation carves along the **dynamical-selection axis** (does the click-law beat KR-dominance — the field-shared open), **orthogonal** to the complex-over-real axis | **KEEP-FULL** | `s_geom` | §4 (the obligation clause → a short paragraph) — coordinate with v8 Paper 4 (the KR gate itself is the continuum paper's; port_paper4 lists the KR bit-count missing there too): Paper 2 carries the *obligation-on-the-`χ_AB`-pattern* + axis-orthogonality, Paper 4 the gate; cross-reference both ways, port the numbers once |
| D9 | §5 (end) | **The do-not-conflate list** (five policed conflations): tensor product ≠ local tomography (rebit has one, fails the other); manifoldlikeness selection-bit ≠ complex-over-real bit (different axes); multi-node ≠ higher field-resource (more nodes, same per-pair commitment); `CHSH` alone does not witness `Q̃ ∖ Q` (need `I3322`, level ≥ 4); a supra-`Q̃` monogamy-violating star's pathology is not a new carving (already `Γ ⪰ 0`-excluded) | **KEEP-FULL** (small) | — | §4 close or §7 — a consolidated paper is exactly where these five conflations would re-enter; five one-line bullets |
| D10 | §6 | **The tripartite closure**: on the valid degree-`(1,1)` tripartite no-signaling law `p(abc) = ⅛(1 + ab·E_AB + ac·E_AC + bc·E_BC + abc·E_ABC)`, the interaction information `II(A:B:C)` and triangle invariant `Tri` **factor through `M`** — bit-identical (gap `0`) across qubit/rebit realisations; **non-vacuous** (`II` moves `−0.00201 → 0.00962` as `E_ABC: 0.15 → 0.05`); honest scope: closes the tripartite path **only at degree `(1,1)`** — more *nodes* at the same commitment do not open the gap; the actual `Q/Q̃` separation lives at degree `> (1,1)` | **KEEP-FULL** | `s_geom` part (iv) | §4 — v8 says "the tripartite invariant" (three words) in the sector list; the formula, the invariants, the non-vacuity, and the degree-scope all missing |
| D11 | §7 + §8.1 | The residual-bit consolidation + the **open-locus mitigations**: the commitment premise is where the actual `Q/Q̃` gap lives (degree `> (1,1)`), *mitigated* by (a) the holonomy result being field-agnostic (a moment polynomial for any joint state) and (b) the order/graph arguments never touching the correlator's would-be imaginary content — higher-word case untested but the named routes don't reach it | KEEP-RESULT (verdicts) / **KEEP-FULL** (mitigations, small) | — | §4 [OPEN] + §7 — v8 names both residues; the two mitigations (which shrink the residue honestly) are absent (overlaps row C14 — port once) |
| D12 | §8.2 | **The unbuilt two-party matter-sector locus**: the no-link `χ_AB ↔ mode` is tautological at the executed single-party level (mode reads marginals only); a genuine two-party interaction (interacting GW flow / mode-selecting Hamiltonian) is the one place a coupling could arise — unbuilt; the *a fortiori* field-blindness argument (any such link must escape `R`-invariance, thereby importing the tensor/LT axiom — circular) is sound **but is about an unbuilt sector — flagged, not closed** | **KEEP-FULL** (small) | — | §7 — entirely absent from v8 Paper 2; also cross-ref v8 Paper 5 (matter), which owns the interacting-GW-flow construction |
| D13 | §7 | The three-input map on three orthogonal axes with the mode-orthogonality number (`W` flat under the `χ_AB` sweep to `< 10⁻¹⁰⁰`) | KEEP-RESULT (map) | `s_probe` | The corrected three-status form is v8 §5 + Paper 3; the `W`-flatness number travels with row D3's matter entry |
| D14 | §7/§8 | Renou-contested reporting (2021 stands in-framing; Hoffreumon–Woods; Maioli–Curado–Gazeau; Renou Comment; "vindicates, not proves") | KEEP-RESULT | — | Already v8 §5 verbatim-grade. **DISCREPANCY TO VERIFY:** paper 13's reference list names the Comment authors "M. Moradi Kalarde, B. Xu, M.-O. Renou"; v8 has "F. Moradi Kalarde, X. Xu, M.-O. Renou" (arXiv:2604.07425) — initials differ between sources; check the arXiv listing before freezing v8's reference block |
| D15 | Reproducibility | Receipt counts and texture v8 omits: `c3` = **15/15**, `s_geom` = **20/20 (zero solver-tol)**, `s_order` = **18/18 (zero solver-tol)** (v8 lists these three files with *no* counts); 79 checks total, 17 solver-tol across the four; the **surrogate-vs-witness distinction** (c3's Tsirelson-facet surrogate with min-eig `−2.2×10⁻¹⁶` at `CHSH = 2√2` is *distinct* from the spine's `P*` re-completion `+1.1×10⁻⁷` — two different objects, easy to conflate); the flagged benign SCS "solution may be inaccurate" landing within `3.2×10⁻¹⁰` of `2√2` | **KEEP-FULL** (small) | — | §8 — add the three counts; the surrogate-vs-witness note prevents a real receipt-reading error |
| D16 | §1/abstract | "Thrice-proven free (transverse, downstream, holonomy)" tripling; the Feynman framing ("the way Feynman would: pick fully specified values, push them through the machine, watch what breaks") | KEEP-RESULT (tripling) / optional flavor | — | v8 §7 has the tripling as "three independent attempts … failed in the specific, receipted ways"; the Feynman-method sentence is good prose for the §4 opening if wanted — flavor, not load-bearing |

### E. v7 Paper 16 — seal-indivisibility bridge, BHK positioning, the real-quantum reopening

Status note: Paper 16 is a positioning-and-correction note; v8 Paper 2 absorbed its *verdicts* well (guard 1, §5, §6 all carry corrected forms; the retired "experiment-fixed" framing is gone). Missing are the positioning *details*: the Barandes/Doukas mechanics, the Jarlskog tie-in, two honesty guards, and the full vindication argument. Its receipt count (`p6 = 42/42`, 7 solver-tol) is stale — v8's `43/43`, 8 [SOLVER-TOL] is current. One cross-paper correctness flag falls out of §5 (see row E10).

| # | Source | Content (one line) | Class | Receipts | v8 destination |
|---|--------|--------------------|-------|----------|----------------|
| E1 | Scope guards 1–4 | Owns framing + the multi-scenario object, not the hard theorems; `Γ ⪰ 0` strictly weaker than tensor; SDP digits corroboration-not-proof; side-neutral on real-vs-complex | KEEP-RESULT | — | Already v8 guards 1–5 + status-line [SOLVER-TOL] discipline + §5 side-neutrality |
| E2 | §2 (opening) | **What Barandes's correspondence *is* and the slot argument** (expected big item, first half): a quantum system as an indivisible (non-Markovian) stochastic process, Hilbert space a *secondary* tool, configuration-space description only at division events; the correspondence is deliberately kinematic — it does not say *what physically distinguishes* a division event — and the forced multiplicative survival `S = exp(−κχ)` / `S(nd) = S(d)ⁿ` supplies exactly that slot natively (non-sealing stretch `S = 1`); kin-in-spirit (multiplicativity along refinement is where indivisibility lives), a separate construction, no claim on the correspondence | **KEEP-FULL** (small) | — | §6 — v8's "supplies the family in which paper 1's seal is a candidate division-event mechanism" presumes the reader knows the family and why the seal fits the slot; three sentences |
| E3 | §2 (boundary) | **The Doukas boundary** (confirmed absent from v8, including References): arXiv:2602.22095 is a *divisibility/emergence* result — Chapman–Kolmogorov divisibility of the lifted `B(H)` family is the decisive extra constraint (CK-consistent CPTP family ⇒ Lindblad form; the off-diagonal phase a *compressed carrier of history* not fixed by transition kernels) — **not** a unistochastic-from-first-principles theorem; the seal-as-mechanism reading is consistent with all three adjacent results and inherits none of their theorems | **KEEP-FULL** (small) | — | §6 + References (add Doukas) — this is the boundary that keeps the [POSITED] mechanism claim honest against the nearest emergence theorem |
| E4 | §2 (probe) | **The divisibility-probe anatomy** (v8 has the verdict + receipts only): (1) the `q = 2` screen collapses the CK matrix to the scalar survival — the seal forces only `S = exp(−κχ)`, *not* matrix-level CK-divisibility (Doukas's decisive constraint) and *not* the doubly-stochastic/unistochastic class (Barandes's assumed envelope); (2) **the unitary phase that would promote doubly-stochastic → unistochastic is the *same* `ker R` bit that fixes `χ_AB`** — a real orthogonal and a phase-dressed complex unitary give identical record moments `\|U_ij\|²` to `< 10⁻¹²¹` yet opposite Jarlskog sign `±0.0521` — *not a new wall*; (3) the seal forces a forward-stochastic kernel but not double-stochasticity (explicit sealed semigroup with a **non-uniform stationary law**), so Barandes's doubly-stochastic envelope is only partly seal-motivated — the divisibility half not forced, the unbiased-prior half a further import; slogan: **"the seal motivates the survival, never the kernel"** | **KEEP-FULL** | `p18_seal_divisibility.py` (26/26), `f6_unistochastic_record_blind_probe.py` | §6 — item (2) is genuinely Paper-2 content (it ties the adjacent-literature question back to the paper's own residual bit: one `ker R` bit, seen twice); items (1)/(3) are the derivation behind v8's existing clause |
| E5 | §3 | BHK described (CHSH-game-specific Tsirelson from causally-local indivisible processes) + the [OVERLAP]/[OWNED] split (2√2 co-derived, no priority; owned = multi-scenario envelope from one object + the no-go + framing) | KEEP-RESULT | — | Already v8 guard 1 + §6 ("the value is co-derived and no priority is claimed; the non-overlap is …") |
| E6 | §3 (boundary) | **The honest comparison boundary**: SHARD does *not* prove the BHK framework cannot reach other scenarios or the broader envelope — that would be a claim about their *unbuilt extensions*; the defensible statement is only that SHARD already exhibits what their *published CHSH-specific* paper does not | **KEEP-FULL** (small) | — | §6 — one sentence; without it the non-overlap claim quietly overreaches into their future work |
| E7 | §3 (end) | **The M2 internal honesty note** (absent from v8, and v8's M2 currently *needs* it): the `I_σ(rebit) = I_σ(qubit)` equality is **true-by-design at the shared Tsirelson point** (both correlators live in the X–Z plane that `R` preserves); the genuinely field-distinguishing computation is the `p6b` holonomy audit; **the no-go rests on the K-deficit / orbit argument, not on the `I_σ` equality** — do not present that equality as independent evidence of field-blindness | **KEEP-FULL** (small) | `p6` Part 3 note, `p6b` | §4 M2 — v8 lists "identical sealing scalar" third in the invariance list with no caveat; porting this note pre-empts the one referee objection the source already answered |
| E8 | §3 (table + anchors) | The five-row envelope table; the exact (non-solver) anchors — marginals `(½,½)` to `< 10⁻¹¹⁸`, `CHSH = 2.8284271247…`, `\|CHSH − 2√2\| = 5.248×10⁻¹⁴¹` | KEEP-RESULT | `p6` | v8 §3 carries all five elements with the *sharper* monogamy scan and the `5.2×10⁻¹⁴¹` anchor; nothing lost |
| E9 | §4 | **The reopening, in detail + the full vindication argument** (expected big item, second half — v8 §5 compresses to one sentence each): Hoffreumon–Woods — the Kronecker composition rule under **operational (not product-state) independence** reproduces all finite-network correlations (the postulate *choice* is exactly the composition-rule locus SHARD names); Maioli–Curado–Gazeau — symplectic Kähler rule, reaches maximal `CHSH3 = 6√2` with real variables; the Renou Comment — the HW postulate **fails in Fermionic Information Theory**, not a general physical postulate; **the vindication argument's structure**: the natural worry ("surely experiment fixes the bit from outside, so 'records can't' is mere bookkeeping") is exactly what the controversy undercuts — if the bit is a possibly-unfixable convention, then *neither the records nor a clean experiment may carry it*, which is the strongest available corroboration of the un-forceable-import reading; "SHARD did not predict the controversy; but the controversy is exactly what 'un-forceable from inside' looks like from the outside" | **KEEP-FULL** | — | §5 — v8 has the citations, the framing-scoped standing of Renou 2021, and "vindication, not proof"; it lacks the postulate-locus detail (operational vs product-state independence), the Comment's content, and the worry→undercut logic that makes the vindication an *argument* rather than an assertion |
| E10 | §5 | The three-status meta-result table + the **over-symmetry guard** ("the three legs are on orthogonal axes, *not the same kind of thing wearing three hats*: a scale / a spectrum anchor / a composition bit — conflating 'all three are imports' with 'all three are the same epistemic object' is the error the revision corrects") + the leg-(1) precision: **`κ·σ_A = 2π` and `G·Λ² = (pure number)` are *separate* weight-0 invariants, NOT numerically equal — linked via `G·σ_A = 1/4`, never equal** | KEEP-RESULT for Paper 2 (its §5 one-sentence meta-result is the right altitude) — **but two flags for v8 Paper 3**: (a) the over-symmetry guard is only partially carried there (types + statuses present; the "three hats" warning prose is worth porting); (b) **CORRECTNESS FLAG:** v8 Paper 3's abstract writes "the unified invariant `κ·σ_A = G·Λ² = const`" — the equality-chain notation Paper 16 explicitly corrects as *not numerically equal*; verify Paper 3's wording against Paper 16 §5 / Paper 57 before freeze | — | v8 Paper 3 (walls) — out of this audit's destination but in its sources; recorded here so it is not lost |
| E11 | §6 | The imports ledger — one gap: **Höhn** is part of the local-tomography-least-motivated lineage (Hardy; Barnum–Müller–Ududec; Höhn) and is absent from v8's §6/References (v8 has Hardy, CDP, Masanes–Müller, BMU) | **KEEP-FULL** (tiny) | — | §6 + References — one name; also keep Paper 16's "M2 is a *re-localisation* of this pre-existing insight, not its origin" honesty, which v8 carries in spirit ("the reconstruction lineage that already located…") — no action beyond the name |
| E12 | §7 + Note + Reproducibility | Conclusion recap; the in-corpus correction-sites list (retired "experiment-fixed" loci); stale `p6 = 42/42` (7 solver-tol) | **DROP** | — | Narrative duplication; pure process log (the corrections are *applied* in v8, the site list belongs to the frozen log); count superseded by `43/43` / 8 [SOLVER-TOL] (v8 §8 correct) |
| E13 | References | The Renou-Comment author line — Papers 13 **and** 16 both print "M. Moradi Kalarde, B. Xu, M.-O. Renou" (arXiv:2604.07425); v8 prints "F. Moradi Kalarde, X. Xu, M.-O. Renou" | **VERIFY** (flag) | — | v8 References — two v7 sources agree against v8; check the arXiv listing and fix whichever is wrong (same flag as D14) |

---

## Cross-cutting actions (implied by the ports)

1. **New section — the constructive half** ("What `χ_AB` sources downstream: the graph and its one missing unit"; rows A7–A10), seated after current §4: the pinch in its Paper-13 honest form (D7), the hop-metric-up-to-one-scale with its honesty guards (A8), the weight-split metric no-go (A7, citing the grading tool at its in-v8 home: Paper 1's spacing section / Paper 3's SCALE leg), the circularity audit (A9), and the does-not-claim ledger (A10). Alternative: re-home in v8 Paper 4 with a one-paragraph statement + pointer here — but it must land in exactly one place, and `port_paper4.md` currently claims none of it.
2. **§2 growth — the joint-law groundwork** (A1–A6): the `σ_AB` decomposition display, the skeleton-transfer conditional, the additivity-not-forced telescope, the two-sided/fixed-marginal freeness, the `I_σ = 0` counterexample + `E_cl` contrast (definitions inlined, v1/v5 citation hazard noted), and Paper 6's (C-form)/(C-level) named split + level-independence (B1–B3).
3. **§3 additions**: the IC static core paragraph + `t3` receipt + Pawłowski reference (B11); optional CHSH just-past-point sharpness clause (B4).
4. **§4 restructure into subsections**: §4.0 the forcing checklist + LAW-A/LAW-B discipline (C2, with the crossing-template recap sentence B15 and the mechanism-contrast paragraph C1); §4.1 M1 in full (C3–C6, C8); §4.2 M2 in full (C7, E7); §4.3 the three escapes (C9–C11) + cocycle detail (C13); §4.4 the Feynman sweep — probe logic (D1), witness certification (D2), sector values (D3), by-construction honesty (D4), covariance gates (D5), order-feedback three grades (D6), tripartite closure (D10); §4.5 the two new constraints (D7, D8) + do-not-conflate list (D9); [OPEN] paragraph upgraded with the mitigations and excluded-capacity-classes (C14, D11) and the unbuilt-matter locus (D12).
5. **§5 growth**: the reopening detail + the worry→undercut vindication argument (E9).
6. **§6 growth**: Barandes slot argument (E2), Doukas boundary (E3), divisibility-probe anatomy with the Jarlskog `ker R` tie-in (E4), BHK comparison boundary (E6), Höhn (E11).
7. **§8 receipts**: add counts `c3 = 15/15`, `s_geom = 20/20`, `s_order = 18/18` (D15), `t3` (17) if B11 ports, `p4b` (5) + `nonadditive_entangling_complement.py` + `p4a = 24` (A15); the surrogate-vs-witness note (D15); "single-chain template" added to the `p6` parts list + the η_*-root honesty note (C15).
8. **References**: add Pawłowski et al. 2009 (B11), Doukas arXiv:2602.22095 (E3), Höhn (E11), Van Raamsdonk + Ryu–Takayanagi if the geometry half lands here (A7–A8); **verify** the Moradi Kalarde/Xu initials on arXiv:2604.07425 (D14/E13).
9. **Cross-paper flags recorded, out of destination scope**: v8 Paper 3's "`κ·σ_A = G·Λ² = const`" equality-chain wording vs Paper 16 §5's "separate invariants, not numerically equal" (E10); the manifoldlikeness KR numbers to be ported once between Papers 2 and 4 with cross-references (D8); the covariance layer split — general layer in Paper 4, the `χ_AB`-probe in Paper 2 (D5).

## Bottom line

Nothing in the five sources is *wrong* in v8 — all seven corrections are in place and no retired number or framing survives. The gap is uniformly one of **depth**: v8 Paper 2 states every verdict but carries the derivations for almost none of them, and v7 Paper 4's constructive geometry half (graph YES / metric NO) is missing from v8 entirely. KEEP-FULL rows: 50 (A1–A10, A15; B1–B3, B7, B11, B15; C1–C11, C13–C15; D1–D10, D12, D15, plus the mitigation half of D11; E2–E4, E6–E7, E9, E11). KEEP-RESULT rows: 18. DROP rows: 6 (all superseded framings, stale counts, or process). Two verification flags (E13/D14 reference initials; E10 Paper-3 invariant wording). Estimated growth: ~21 KB → ~55–65 KB, consistent with "self-contained, NOT compressed."
