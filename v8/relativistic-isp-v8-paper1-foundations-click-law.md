# The sealed-record click-law, consolidated: what is forced, given what

**Author:** Felix Robles Elvira (ORCID: 0009-0009-2017-4394; independent researcher)

**Status:** preprint, not peer reviewed, version 2026-07-01. First paper of the v8 consolidation (supersedes v7 Papers 1–3 and the corresponding foundations companions; the frozen v6/v7 research log and the result ledger are at `LEDGER.md`). Every load-bearing line is tagged: **[THEOREM]** = proved within the stated arena; **[DERIVED]** = established from tagged inputs; **[DEFINITIONAL]** = true by construction of the named object; **[CONDITIONAL(…)]** = proved *given* the named premises; **[IMPORT]** = an external result or datum used, not claimed; **[POSITED]** = a modeling identification, not a derivation; **[NO-GO]** = a proved impossibility; **[OPEN]** = an unsolved obligation; **[PHYSICAL]** = configuration data the law constrains only differentially — an input, not an open law. All numeric claims are verified at mpmath `dps ≥ 100` (commonly 120–140) and sympy-exact where the structure permits; receipts in `code/`.

## Abstract

This paper states the fundamental SHARD click-law — the pre-geometric rule for *when* and *how* a record commits — **together with its complete premise ledger, in the headline rather than the fine print**. The honest claim is conditional and we state the condition first. **Given** four imports — (i) the seal-content clock `σ = D(P_AB‖P_BA)`, the forward-vs-reverse relative entropy of the holonomy-transport law (the Schnakenberg entropy-production functional of stochastic thermodynamics — **[IMPORT]**, not discovered here); (ii) the `q = 2` screen calculus (Born weights via Banach–Lamperti — **[IMPORT]**, itself **[CONDITIONAL]** on a continuous-reversibility premise that remains **[OPEN]**); (iii) the Aczél memorylessness theorem for the Cauchy functional equation (**[IMPORT]**); (iv) the projective Gell-Mann–Hartle seal-history bridge (**[POSITED]** as the model class; its seal⟺composition biconditional is then a forced kinematic identity *within* that class) — **and given** three premises — **(a)** dense seals (the divisible regime; the sparse regime is treated separately and is weaker), **(b1)** a single coherence channel (forced by the `q = 2` one-contrast screen; a multi-channel screen breaks multiplicativity, receipt gap `0.317`), **(b2)** screen stationarity along the commit order (**assumed**: a single channel whose contraction varies with content-location breaks multiplicativity, receipt gap `0.18`; its discharge is an open obligation) — **then** the click-law is forced up to one calibration: the seal `E` is an orthogonal projection (`E = E* = E²`, by norm preservation — **[THEOREM given (ii)]**); the content `χ` := accumulated `σ` is an odometer whose additivity is **[DEFINITIONAL]** (a sum telescopes) and whose non-negativity `dχ ≥ 0` is a **[THEOREM]** (Gibbs/arrow-positivity); survival is the unique exponential `S(χ) = exp(−κχ)` in the dense regime and the geometric lattice skeleton `S(nd) = S(d)ⁿ` in the sparse regime, with the inter-seal profile and spacing free; no-revival is the **[DEFINITIONAL]** corollary that a non-composing point is not a seal; and the single conversion `κ` is the one free scale the records provably cannot fix. **The supply and the spacing** (§4): the content clock's *form* is forced (a coboundary — the discrete Poincaré lemma on the odometer), its *magnitude* is physical configuration data (the law constrains differences only), the capacity `W_* = max_η min(C, J) = 0.364784952…` is derived and the ceiling `d ≤ W_*` holds given no-over-saturation — while the firing law commits **strictly sub-capacity in every admissible mode** (`d < W_*`; the closure `d = W_*` is *denied*, not merely unforced), and a grading-trichotomy theorem closes every record-derived cross-tier route to fixing `d` (modulo one named gate), leaving *what fixes the spacing* as a well-posed open with an explicit doors ledger. **Not delivered** — stated as prominently as what is: the cochain identification behind the supply (posited, not derived), the entanglement/joint law across parallel chains, the seal placement and the dense-vs-sparse realization, the values of `κ` and `d`, and any relativistic content (Lorentz invariance and microcausality are not claimed). In one sentence: **a conditional-uniqueness result — the *shape* of the law is unique modulo one no-go-protected scale, relative to three named premises and four typed imports (the bridge among them) — and every one of those qualifiers is part of the theorem, not a footnote to it.**

## 1. The objects, with their statuses

A **record** is a sealed finite holonomy diamond `D = (Ω_D, μ_D, screens, collar, transports)` (the SHARD primitive — **[POSITED]**, v6 Paper 4): a self-contained parcel whose physical content is the closed exchange-defect holonomy `Δ_AB = T_B T_A − T_A T_B`, and which becomes an *event* when a count-symmetric idempotent proposition `E` commits. Two constraints fix the method. First, the **do-delete no-go** (**[THEOREM]**, v6 Paper 3 §19, carried as constraint C6 in Paper 56's ledger): distinct commitment rules can produce identical observed histories, so the click-law is not an observational statistic — it must be pinned by self-consistency, or accepted as an input. Second, SHARD is **pre-geometric**: time is the commit order; space, metric, light cones and `s²` are emergent. The fundamental law is therefore Tier-A (record-level, no spacetime input); geometric and thermodynamic statements are Tier-B *conditional consequences*. The tiering is a deliberate quarantine: the hard relativistic-localization problem is isolated into Tier B as an *emergence* question, and the geometry-presupposing constraints — microcausality, Lorentz invariance, a local kernel `f(s²)`, the thermodynamic-equilibrium premise — are excluded as axioms of the fundamental law rather than smuggled in.

The only Tier-A state variable is the **content**

> `dχ = σ_step = D(P_AB ‖ P_BA)` per commit-order step,  `χ` its accumulated sum,

the relative entropy between the forward and reverse holonomy-transport laws, summed along the commit order — a pure number (nats), weight-`0`, the intrinsic clock. **Status: [IMPORT].** This functional is the entropy-production of stochastic thermodynamics (Schnakenberg; the trajectory-level `⟨σ⟩ = D(P_F‖P_B)` identity); the corpus's own contribution is the *identity* `σ = 𝔼[A_D]` on the record arena (**[THEOREM]**, v6 Paper 10 T2) and the decision to make this the seal's argument. The **survival functional** `S(χ) ∈ [0,1]` is the probability the holonomy is uncommitted after accumulating `χ`; the **hazard** is `λ(χ) = −d log S/dχ`, dimensionless per-content.

## 2. The premise ledger

The forcing chain below uses exactly this list; nothing else is load-bearing. The receipts quantify what breaks when each premise is dropped.

| # | Premise / import | Status | If dropped |
|---|---|---|---|
| (i) | `σ = D(P_AB‖P_BA)` as the clock | [IMPORT] (Schnakenberg) + [THEOREM] `σ=𝔼[A_D]` on the arena | no intrinsic Tier-A variable |
| (ii) | `q = 2` screen calculus (Born) | [IMPORT], [CONDITIONAL] on continuous reversibility — [OPEN] | §3.1 and (b1) lose their forcing |
| (iii) | Aczél memorylessness theorem | [IMPORT] (textbook) | multiplicativity would not yield uniqueness |
| (iv) | GMH projective seal-history bridge | [POSITED] model class; seal⟺CK-composition a forced kinematic identity within it (receipt `f3d`: CK defect = interference cross-term, gap `6.6×10⁻¹⁴²`; `0.329` across an unsealed interval, `0.0` at a seal) | "refinement" undefined |
| (a) | dense seals | regime choice (the divisible branch) | only the sparse skeleton (§3.4) survives |
| (b1) | single coherence channel | forced by (ii)'s one-contrast screen | multiplicativity breaks (gap `0.317`, receipt `f3e`) |
| (b2) | screen stationarity along the commit order | **assumed**; discharge [OPEN] | multiplicativity breaks (gap `0.18`, receipt `f3e`) |

**(b2) is the soft joint of the entire construction and we say so here, not in an appendix.** "Survival is a function of accumulated content alone" — the claim that history enters only through `χ` — *is* (b1) + (b2). (b1) is forced; (b2) is supplied only by the observation that the screen is the same `q = 2` one-contrast object at each seal, which is a plausibility argument, not a proof. Its counterexample-if-dropped is exhibited by the corpus's own receipt. A proof of (b2) from the count-symmetry of the screen (the route: the same primitive symmetry that fixes the constants of the content-supply analysis — v6 Paper 4 §5, whose capacity form is §4.2–4.3 below — run through the collar machinery of v6 Paper 10 T4) would be the single most valuable strengthening of this paper; it is **[OPEN]** and listed in §7.

## 3. The forced structure

### 3.1 The seal is an orthogonal projection [THEOREM given (ii)]

Norm preservation in the `q = 2` calculus forces the committing proposition to be self-adjoint idempotent, `E = E* = E²` — an oblique idempotent also satisfies `q² = 1`, so the projection property is genuinely forced by norm preservation, not by idempotence alone. The mechanism is purely algebraic: in a 2-norm an idempotent is norm-non-increasing *iff* it is self-adjoint, whence `q = 2E − 1` is a self-adjoint reflection (`q² = 1`) — no geometry enters. Receipt `f1_born_projection_q2.py`, sympy-exact. This is the cleanest step in the chain — its only premise is the imported `q = 2` calculus, whose own continuity premise remains open (§2, row ii).

### 3.2 The odometer [DEFINITIONAL + THEOREM]

`χ` is concatenation-additive along the commit order and its increment is non-negative. The two halves have different statuses and we refuse to blend them: **additivity is [DEFINITIONAL]** — `χ` is *defined* as an accumulated sum, and a sum telescopes (receipt `f3c`: the trajectory action telescopes on a driven chain, residual `7.7×10⁻¹²¹`; this verifies the definition's consistency, it does not derive structure); that the accumulated sum equals the *segment-level* forward/reverse KL uses the Markov chain rule at junctions — i.e. the [DEFINITIONAL] tag is wired to the [CONDITIONAL] collar clause below, not free-standing; and this sequential (commit-order) additivity is distinct from the *parallel*/tensor cocycle `A_{D₁×D₂} = A_{D₁} + A_{D₂}` (machine-zero for parallel diamonds, receipt `f3c` — the *wrong* additivity here, kept apart); **`dχ ≥ 0` is a [THEOREM]** — Gibbs/arrow-positivity, equality only at a detailed-balance (eventless) step. The collar between seals is Markov-presentable **[CONDITIONAL]** on the finite-primitive-collar arena (v6 Paper 10 T4; T3 is a premise-dependent dissolution, not a dynamical theorem, and is not used here; infinite-memory collars remain [OPEN]). The compatibility is exact: *Markov where the process is order-blind, non-Markov where it commits* — the collar's content-transport telescopes while the seal order's coherent one-time law fails Chapman–Kolmogorov across unsealed intervals and composes only at seals (receipt `f3d`).

### 3.3 The dense regime: the unique exponential [CONDITIONAL(a, b1, b2, iv, (ii) via b1); uniqueness by (iii)]

Under (a) + (b1) + (b2), inserting an intermediate seal must compose, giving the Cauchy equation `S(χ₁+χ₂) = S(χ₁)S(χ₂)`; with monotonicity and measurability the Aczél theorem yields the unique

> `S(χ) = exp(−κχ)`,  constant hazard `λ(χ) = κ ≥ 0`.

The exponent is not assumed: among `S = exp(−κχ^p)` only `p = 1` is refinement-consistent (`2^p − 2 = 0 ⟺ p = 1`; receipt `f3_self_consistency.py`, `dps = 120`), and the product limit `limₘ (f(χ/m))^m = exp(χ·(log f)′(0))` forces the exponential from *any* stationary per-step contraction with `(log f)′(0)` finite (the regularity is a premise, stated). The uniqueness theorem is the import (iii); the corpus's content is that the record arena instantiates its hypotheses *given the premise ledger*.

### 3.4 The sparse regime: skeleton only [CONDITIONAL(b2-on-the-lattice); profile FREE]

If seals are sparse — the decoherence functional carrying off-diagonal support *between* commitments — composition is available only on the seal lattice `L = {nd}`, and finite induction (no regularity needed) forces the geometric skeleton

> `S(nd) = S(d)ⁿ`  (geometric in the *sequence* sense),

with the **inter-seal coherence profile and the spacing `d` free**. That free profile *is* the genuine-quantum content the dense limit annihilates; the dense exponential is recovered as `d → 0` for monotone continuations (receipt `f3b`; the lattice-skeleton forcing holds to residual `1.2×10⁻¹²²`, and the recovery is bounded by the squeeze `B(d) → 0`). The two regimes are one law *exactly*: on the lattice `S(d)^{χ/d} = exp(−κχ)` for every `d`, with `κ = −log S(d)/d`, and the inter-seal squeeze amplitude `B(d) = s^k(1−s) → 0` (`∼ κ·d·s^k` at small `d`) — the dense exponential is the `d → 0` *face* of the sparse skeleton, not a separate postulate. The candidate closure `d = W_* = 0.364784952…` is **denied** by the sub-capacity firing law (§4.3): `d` is [OPEN], bounded above by the capacity ceiling `d ≤ W_*` (a bound itself [CONDITIONAL] on no over-saturation, strict for the gated admissible class — the qualifier travels with the bound; the derivation, the firing law that makes it strict, and the doors ledger are §4.2–4.4).

### 3.5 No-revival [DEFINITIONAL corollary]

`κ ≥ 0` and monotone `S`: a point where survival composes non-multiplicatively is, by the definition of a seal, *not a seal*. This is exclusion-by-definition — Tier-A no-revival is unfalsifiable by construction. One distinction keeps it honest: *committed survival* is monotone by definition, while the *inter-seal coherence* of the sparse regime may **rise transiently** — and that non-monotone freedom is the same object as the revival fingerprint of the decoherence literature: the free sparse profile *is* the observable signature the dense limit annihilates. The operational (Tier-B) no-revival statement lives in the phenomenology paper of this series (paper 6), where its blindness versus standard CP-divisible decoherence is proved rather than assumed.

### 3.6 The one free scale [NO-GO]

`χ` is weight-`0` (a KL number); a *rate* per emergent time carries weight; converting one to the other needs one absolute unit the records provably do not carry — the same scale no-go that walls Newton's `G` (this series, paper 3; the proved no-go's frozen-log home is v6 Paper 57 / v7 Papers XI and XVII). So `κ` is the single free calibration: **the shape of the law is unique; its absolute rate is measured, never derived.** The identification of `κ`'s scale with the gravitational one is a structural analogy, not a theorem, and is tagged so wherever used. The dimensionless per-content structure downstream of `κ` (e.g. the quarter law relating coherence loss to `σ` — paper 6 of this series; frozen-log home v6 Paper 26) is no-go-eligible and is derived there.

## 4. The content supply and the seal spacing

The click-law of §3 answers *when given content*; this section carries what the substrate says about the content itself and the spacing of seals — the form is forced, the magnitude is physical data, the spacing is bounded and mode-disciplined but not fixed, and no record-derived cross-tier coefficient can fix it.

### 4.1 The coboundary clock [DEFINITIONAL lemma + POSITED identification]

The per-step increment is the coboundary of a scalar potential: `dχᵢ = ρᵢ = h_{i+1} − h_i`, telescoping exactly (`Σρᵢ = h_b − h_a`). On a one-dimensional commit path `H¹ = 0`, so "concatenation-additive content ⟺ coboundary of a potential" is an *identity* — a lemma sitting on the [DEFINITIONAL] odometer of §3.2, not new dynamics. Two layers are kept apart: the **form** (any additive content is a coboundary — lemma-level) and the **identification** of the potential `{hᵢ}` with the closed-holonomy cochain (the physical layer — **[POSITED]**; the corpus's potential/increment values are reproduced to `< 10⁻¹⁰⁰`). **The magnitude is [PHYSICAL]:** the law constrains *differences* only — a configuration and its tenfold rescaling both satisfy every constraint (verified) — so the size of `dχ` is configuration data, not a law, and `κ` remains the untouched no-go floor of §3.6. Receipt `p2a_content_supply.py` (13).

### 4.2 The capacity and the ceiling [DERIVED + CONDITIONAL(no-over-sat, b1)]

The one-diamond self-consistency law — **KL content = Fisher capacity** — is `η·tanh η − log cosh η = 1 − tanh²η`, with root `η_* = 1.090344354879492…`, `θ_* = tanh η_* = 0.797003794162878…`, and

> `W_* = 1 − θ_*² = 0.364784952089976…`  (balance residual `9.7×10⁻¹²²` at `dps = 120`; `1.3×10⁻¹⁴¹` at `140`).

`W_*` is a genuine **capacity**: the accumulated content `C(η)` rises `0 → log 2` while the Fisher capacity `J(η) = sech²η` falls `1 → 0`, with a *single* crossing at `η_*` — hence `W_* = max_η min(C, J)`. The scalar-`min` reduction *inherits premise (b1)*: the `q = 2` one-contrast screen collapses the matrix capacity to a scalar; a multi-channel screen gives a vector capacity and a Pareto frontier, no scalar crossing. **The ceiling** `d ≤ W_*` follows **[CONDITIONAL(no-over-sat: a committed record is self-consistent, `C(η) ≤ J(η)`; and (b1))]** — composition alone gives *no* bound (`d ∈ (0, ∞)`; it only evaluates at seal points). The asymmetry matters: the ceiling needs only self-consistency of what commits; the *equality* `d = W_*` would need a saturation premise (P-sat), which is precisely what §4.3's firing law denies. Receipts `p2a`, `p2b`.

### 4.3 The firing law, the three objects, and the strict bound [DERIVED within the v6 Paper 4 arena]

**The three-object discipline.** Content `C(h)` / capacity `W_*` / evidence clock `I` are three different objects, and the four naive readings of "the seal spacing" — a fixed scalar, a fixed mean with fluctuations, a mode-dependent set, an irreducible distribution — sort onto them; the layered verdict is *mode-dependent set* for the content with *random clock* on the evidence axis. **The two laws are distinct and consistent:** the capacity law (the `ψ″`-balance `C = J`, root `η_A ≡ η_*` of §4.2, which fixes the constants) and the division-event **firing law** (the `ψ′`-balance `∂ψ/∂h = e^{−h}` on the complete primitive ledger) — the seal fires on a *random evidence clock* `S(I) = e^{−I}`, `I ∼ Exp(1)`, itself derived from eventless RN/KL gluing (`S(I+J) = S(I)S(J)`) plus exact self-accounting (`−log S(I) = I`, fixing rate `λ = 1` and commitment ratio `r = 1`); `E[I] = Var[I] = 1` exactly. **The roots are mode-dependent and strictly sub-capacity:** one-mode parity `tanh η = e^{−η}` gives `η_B = 0.609377863436006…`, `C(η_B) = 0.156109…`; the coupled `{x, y, xy}` ledger gives `h_* = 0.495053…`, `C(h_*) = 0.109004…` — both `< W_*`. Hence

> **`d < W_*` strictly, in every admissible mode [DERIVED]** — the closure `d = W_*` is *denied*, not merely unforced.

Sub-capacity is **structural** for the admissible class, not per-case: `C′ = η·sech²η > 0`, and reaching `W_*` would need the inadmissible cochain unit `λ ≈ 0.208 < 1` (the same argument that rejects the `λ = 2` double-count); coupling only *dilutes* (`η: 0.609 → 0.495 → 0.368` over 1/3/7 characters), so the one-mode `C(η_B) = 0.428·W_*` is the class **supremum**; and the admissibility gate is load-bearing — a skewed base `μ(+) = 0.2` overshoots to `0.526 > W_*` and is excluded by the same count-dual/orthogonality symmetry that fixes the constants. Per mode the content is *deterministic* — the fixed point is the Euler equation of the strictly convex commitment potential `Φ(h) = ψ(h) + Σⱼ e^{−hⱼ}` (unique minimizer; `Var[content | mode] = 0`) — so all randomness lives on the evidence axis, and evidence units convert to a content spacing only through the no-go-walled `κ`. Three objects, never conflated: the tilt `η_B`, the evidence `I ∼ Exp(1)`, the content `C(η_B)`. The lineage honesty travels: a *fixed-content* commitment threshold was already found non-canonical (v6 Paper 4 §69), and §71's renewal analysis closes the shape drift (Poisson, `r = 1`), leaving mode-dependence as the one live non-canonicity — exactly what the trichotomy of §4.4 prices. Receipts `p2b_event_law_saturation.py` (13), `p2c_vector_ledger_roots.py` (25), `p3_d_nogo.py` (9).

### 4.4 The cross-tier no-go and the open doors [NO-GO modulo gate G1]

**The grading trichotomy theorem** (the reusable tool). `g_λ` is a one-parameter group, so weight is an additive grading homomorphism (v6 Paper 6, Theorem G): a monomial `W_*^a κ_grav^b l_step^c σ_A^e` has weight `2b + c − 2e`; it is dimensionless iff `c = 2(e − b)` — the length power is carried entirely by cancelling `κ_grav`/`σ_A` powers; the irrational-weight leak is closed (`a + b√2 = 0` over `ℤ` forces `a = b = 0`), and transcendental functionals of weight-`0` ratios stay weight-`0`. **Every record-derived dimensionless coefficient falls in exactly one of three bins:** (1) **ratio-of-twins** — circular (`1/4 = G·σ_A`, `2π = κ_grav·σ_A`, `G·Λ²`, `η = 1/(4G)`; `G·σ_A` and `κ_grav·σ_A` verified invariant under `l → μl` at `μ = 1, 1.7, 3` in `p3_d_nogo.py`, `G·Λ²` in `p15c`/`p17`); (2) **scale-blind** — vacuous (Srednicki `a = 0.295417`, `U_G = 1`, `η/s = 1/4π`, `c/6`, the graviton `4/9`); (3) **Tier-A-intrinsic** — wrong tier *and wrong axis* (`W_*` is a source-*amplitude* coefficient, not a spacing). The three requirements are mutually exclusive and there is no fourth bin, so **no record-derived cross-tier bridge fixes `d`** **[NO-GO]**. A three-`κ` disambiguation travels with this (it prevents a real symbol collision): the gravitational coupling `κ_grav = 8πG` (weight `+2`) ≠ the Tier-A conversion `κ` of §3.6 (the no-go floor) ≠ the source amplitude `κ_amp = W_*`.

**Honest negatives, kept.** `θ_B² = 0.295597742522…` vs Srednicki's `a = 0.295417…` (gap `1.8×10⁻⁴`) is numerology, not an identity: `e^{η_B}` is the **tribonacci constant** (the root of `x³ − x² − x − 1`; `θ_B` is the root of `t³ + t² + t − 1` and `θ_B²` of `s³ + s² + 3s − 1`, both irreducible over `ℚ`), and the partial-wave sequence converges *toward* `a` and *away from* `θ_B²`; likewise `1/η_B = 1.641018` vs `π²/6 = 1.644934` fails. The `ξ_1d = 1/η_B` mass-gap identity is real but weight-`0` record-internal (no second scale — the dedicated attack collapses onto the invariant). **The spectral-action route is closed:** Chamseddine–Connes/Sakharov lands on the invariant `G·Λ²` with `Λ = 1/l_step` the record-scale label — it fixes only the ratio `G/l_step²`, never absolute `G`, in every scheme; the conjectured "factor-`2π` gap" is *plausibly* a moment-labelling ambiguity (`M₀ = 1/(2π)`, `M₁ = 1/(2π)²`, `M₀/M₁ = 2π`) — a candidate, unverified, convention-dependent, and immaterial to the no-go.

**Gate G1 — the single load-bearing premise, stated at its true strength.** G1: *no sealed law consumes `A_rec` outside the labeling map `ℓ`.* Proved-finite for the executed corpus (clause table, worst gap `0`); interface-level for un-imported texts; **not** a closure theorem against future laws — it is never described as closed. The sharpest counterexample class (an intrinsic rate carrying an absolute scale) is killed by the weight trichotomy `{0, −1, −2}`, but that does not discharge the quantifier. The completeness sweep closes every other crack: an additive grading anomaly collapses into G1; negative-weight primitives are chart images of `ℓ`; fractional roots stay labels (`weight(A_rec^{−3/7}) = −6/7`); `ℏ = c = 1` hides no second length (the residual length *is* `l_step`); log-running/dimensional transmutation gives a weight-`−1` transmuted scale — a pure number × `1/l_step`, the record scale again — merely reproducing the `σ_A ↔ G` bijection; and every record mass-gap *length* is a pure number × `l_step` (weight `+1`), including `ξ_1d = 1/η_B`. (The sweep's clause table is v6 Paper 6 §3.1's; the seal-rate weight trichotomy `{0, −1, −2}` is v6 Paper 57 §2's — both citable; the bins and invariances live in `p3_d_nogo.py`.)

**The open-doors ledger.**

| Door | Status |
|---|---|
| Record-derived absolute-scale bridge (any bin) | **CLOSED** (the trichotomy) |
| Spectral-action absolute route | **CLOSED** (lands on the invariant) |
| Gate G1 | **OPEN** — a scope assumption, honestly quantified |
| The `c_m` weight-`0` strength lane | **OPEN-permitted** — a *strength*, categorically not a spacing (`c_m·σ_A` inherits the free scale); owned by paper 3 |
| An external structural identity | **PERMITTED, undiscovered** (the `1/η_B` thread a suggestive entry) |

*What fixes `d`* is well-posed and open; the three live non-circular routes: a completeness/canonicalization principle; an external structural identity; a non-circular variational principle on the inter-seal stretch.

## 5. What "accounting for history" means, exactly

The click-law is a *whole-history* rule in one precise sense: all history enters through the scalar odometer `χ`. That sentence is exactly premise (b2) plus theorem (b1), no more: **it is well-defined if history acts on survival only through accumulated content, and that "if" is assumed, counterexampled-if-dropped, and not yet proven.** The law is therefore a **conditional hazard law** — given the content stream, it answers *when* (constant hazard per nat) and *how* (an orthogonal projection) — and **not an autonomous dynamics**: it does not generate the content stream (§4.1, supply; residue §7 item 1), does not place the seals, does not fix outcomes beyond the imported `q = 2` weights, and does not correlate parallel chains.

## 6. Tier-B: conditional consequences [CONDITIONAL(emergent spacetime)]

*If* the records coarse-grain into a spacetime satisfying the program's regularity condition — the tame-class / cofinal-stability / Lorentz-invariant-sprinkling condition of the frozen v6 line, named here so the conditional has a referent — then the content lays down along an emergent parameter as a profile `χ(T)`, and:

- **One law, two onsets [DERIVED].** The exponential and Gaussian decoherence onsets are the *same* Tier-A law read through two parametrizations: `χ(T) = λ₀T` gives `S = exp(−κλ₀T)`; `χ(T) = aT²/2` gives the Gaussian onset `exp(−κaT²/2)` — sympy-exact reparametrization identities, residuals `∼10⁻¹²²` (receipt `f3`). This *re-derives*, as pure Tier-A reparametrization, the frozen-log result that the exp-vs-Gaussian decoherence *shape is not a mechanism signature* (v6 Paper X): the shape distinguishes content *profiles*, never sealing from any other CP-divisible mechanism.
- **The locality kernel [DERIVED, Tier-B].** The kernel `f(s²)` is positive-type with quartic UV decay, and white noise is *forbidden* (receipt `f1_kernel_positive_type.py`) — the Tier-B face of the seal memory.
- **The thermodynamics as a limit.** The equation of state and the scale no-go follow in the emergent limit (papers 3–4 of this series): the program's thermodynamics is the spacetime *limit* of the click-law — a consequence, not an assumption.

**Lorentz invariance and microcausality are not claimed** — they are the open covariance frontier, and no Tier-A statement here presupposes them.

## 7. What is not claimed — the honest residue

1. **The supply — form forced, magnitude physical, spacing open.** The clock's *form* is forced (the coboundary lemma, §4.1) and the *magnitude* of `dχ` is **[PHYSICAL]** — configuration data the law constrains only differentially, not an open law waiting to be found. What remains genuinely open on this axis: whether the cochain *identification* (§4.1's [POSITED] layer) can be upgraded to a derivation — where `κ`'s internal definition would, or would not, be further pinned (the no-go guarantees `κ` cannot be removed; its internal form may still be forced) — and *what fixes the spacing `d`* (§4.4's open-doors ledger).
2. **The entanglement/joint law [OPEN].** Sequential multiplicativity is silent on outcome-independence: the sealing skeleton neither forces nor forbids OI-violation (receipt `f5_oi_pi_consistency.py`, 20/20 — the explicit law `p(a, b|x, y) = (1 + ab·E)/4`, `E = −cos(x − y)`, is PI-yes/OI-no at `CHSH = 2√2` and carries the same skeleton; the mechanism: attaching per-chain survival preserves CHSH and no-signaling because the common seal-gate cancels in conditional outcome statistics; and the converse leg — an OI-*respecting* law carries the same skeleton too — closes the "neither forces" direction). A normative guardrail travels with this: **locality must never be *imposed* on the Tier-A correlation structure** — only the emergent Tier-B dynamics is local, and only as no-signaling (the Bell address: keep parameter-independence, drop outcome-independence). The correlation content `χ_AB` is treated in paper 2 of this series: the almost-quantum fence `Q̃` is (conditionally) forced from inside, while the filling — *which* `χ_AB` the world realizes within it — is shown un-forceable, up to `Q̃`, on the degree-`(1,1)` arena.
3. **Placement and sparseness [OPEN].** Which `E` commits, where, and whether gravitational sealing is sparse (genuinely indivisible) or dense (classical shadow). Sparse-vs-dense is a *decidable computation* on the decoherence functional `D` — whether `D` carries off-diagonal support *between* commitments — not a matter of taste; the computation, and the gravitational structural-functor obligation it belongs to (v6 Paper 56's Tier-1 target; "Tier" there is that paper's grading axis, distinct from this paper's Tier-A/B), are deferred to the phenomenology paper. The seal *spacing* within the sparse regime has its own ledger (§4.4).
4. **(b2) stationarity [OPEN].** The named soft joint (§2).
5. **The `q = 2` continuity premise [OPEN, imported].** The Hilbert-space layer is organized, not reconstructed, here.
6. **`κ` [IMPORT — measured].** One number, like `G`.

## 8. Receipts

All in `code/`, all pass on re-run (2026-07-01): `f1_born_projection_q2.py` (sympy-exact projection forcing), `f3_self_consistency.py` (Cauchy uniqueness, `p = 1` scan, balance residual `9.7×10⁻¹²²`; the exp/Gaussian reparametrization identities of §6), `f3b_sparse_seal_shape.py` (skeleton `1.2×10⁻¹²²`, the `S(d)^{χ/d}` identity, `d→0` squeeze), `f3c_sequential_odometer.py` (telescope `7.7×10⁻¹²¹`, `σ_step ≥ 0`; the parallel/tensor cocycle gap machine-zero `0.0`, distinct from the `10⁻¹²¹`-class telescope residual), `f3d_foundational_supports.py` (CK⟺interference identity; collar Markov-presentability), `f3e_history_independence.py` (the (b1)/(b2) counterexamples, gaps `0.317` / `0.18`; the product limit forcing exp for any stationary per-step law), `f4_variational_rate.py` (weight bookkeeping: `weight(χ) = 0`, the rate conversion walled), `f5_oi_pi_consistency.py` (20/20, OI/PI orthogonality), `f1_kernel_positive_type.py` (Tier-B kernel), and the §4 supply/spacing set: `p2a_content_supply.py` (13 — the coboundary telescope `< 10⁻¹⁰⁰`, the `W_*` crossing at `dps = 140` residual `1.3×10⁻¹⁴¹`, magnitude-rescaling invariance, weight bookkeeping), `p2b_event_law_saturation.py` (13 — sub-saturated firing, `C(η_B) ≠ W_*`, the evidence-clock mean `1`, the capacity/firing separation), `p2c_vector_ledger_roots.py` (25 — `C(η_A) = W_*` exact, the `λ* ≈ 0.208` inadmissibility, the dilution chain `0.609 → 0.495 → 0.368`, the skewed-base `0.526 > W_*` gate exhibit), `p3_d_nogo.py` (9 — the grading homomorphism, the trichotomy bins, the irrational-leak closure, the tribonacci irreducibility, the spectral moments, the invariance of the twins under `l → μl`). Never float64 for cancellation-heavy or near-`S = 1` quantities; float-safe items (integer composition counts, the `p`-exponent scan, the projection booleans) stated as such.

## References

**This series.** Paper 2 (the correlation envelope and the un-forceable filling); Paper 3 (the walls classification, incl. the scale no-go this paper's §3.6 uses); Paper 6 (phenomenology: the quarter law, the swerve, the blindness results). Ledger: `LEDGER.md`.

**Frozen log.** v7 Papers 1–3 (the click-law campaign, fully superseded here). **v6 (kept, citable):** Paper 4 (the primitive; the §5 constants and the §71–76 firing law that §4.3 runs on; conditional Born selection), Paper 6 (Theorem G — the grading homomorphism — and gate G1), Paper 10 (arrow-positivity T2; the collar arena T4), Paper 3 §19 (the do-delete no-go's proof home), Paper 56 (its constraint ledger C6; the GMH-bridge home), Paper 57 / paper-XI (the scale no-go and the twins invariant §4.4 leans on), Paper X (the exp/Gaussian non-signature result §6 re-derives), and the publishable-batch foundations (paper-Va; companion-A, whose Aczél import framing this paper adopts).

**External.** J. Aczél, *Lectures on Functional Equations and Their Applications* (Academic Press, 1966) — the memorylessness/Cauchy uniqueness. S. Banach, *Théorie des opérations linéaires* (Warsaw, 1932); J. Lamperti, Pacific J. Math. **8**, 459 (1958) — the `L^p`-isometry classification behind the `q = 2` selection; S. Aaronson, quant-ph/0401062 — the norm-preservation + continuous-reversibility reading of it used here. M. Gell-Mann, J. B. Hartle, Phys. Rev. D **47**, 3345 (1993) — the decoherence-functional history calculus. J. Schnakenberg, Rev. Mod. Phys. **48**, 571 (1976); U. Seifert, Rep. Prog. Phys. **75**, 126001 (2012) — entropy production as the forward/reverse KL. J. A. Barandes, arXiv:2507.21192 — the indivisible-stochastic family in which the seal supplies a candidate division-event mechanism (placed in paper 2). M. Srednicki, Phys. Rev. Lett. **71**, 666 (1993) — the entanglement-entropy coefficient of §4.4's numerology refutation. A. H. Chamseddine, A. Connes, Commun. Math. Phys. **186**, 731 (1997) — the spectral action whose absolute-scale route §4.4 closes.
