# The record correlation envelope: the almost-quantum fence from inside, and the one un-forceable bit

**Author:** Felix Robles Elvira (ORCID: 0009-0009-2017-4394; independent researcher)

**Status:** preprint, not peer reviewed, version 2026-07-02. Second paper of the v8 consolidation (supersedes v7 Papers 4 — both its joint-law and its geometry halves — 6, 12, 13 and the positioning content of 16; the frozen v6/v7 log and result ledger are at `LEDGER.md`). Tags as in paper 1 of this series: **[THEOREM] / [DERIVED] / [DEMONSTRATED] / [CONDITIONAL(…)] / [IMPORT] / [POSITED] / [OPEN]**, with SDP digits additionally flagged **[SOLVER-TOL]** (cvxpy/SCS `~10⁻⁹`; the PSD logic is exact, the optimizer digits are corroboration). Numerics mpmath `dps = 100–140` / sympy-exact; receipts in `code/`. Pre-geometric throughout: no spacetime, metric, light-cone, or interval `s²` is used as input anywhere; every quantity is a record-internal probability, KL number, or moment of the substrate's own observables — the *absence* of the composite structures (the tensor product; the absolute length) is precisely the result.

## Abstract

The sealed-record substrate of paper 1 carries, transversely to its commit order, exactly one kind of two-party data: the **moments of its own seal observables**, `M = {1, A_x, B_y, A_xB_y}` — marginals and cross-moments, nothing else. This paper consolidates everything that fact does, in four layers. **The joint law [THEOREM + CONDITIONAL].** The joint odometer decomposes exactly, `σ_AB = D(P_AB‖P_BA) = σ_A + σ_B + I_σ`; the exponential survival *shape* transfers verbatim to the joint scalar (under paper 1's own dense-regime premises), but the *additivity* of the content is provably not forced — the sequential refinement telescopes exactly (gap `0`) while blind to the mutual term — so the entangling content `χ_AB = I_σ` is a **free** Tier-A input: signed, two-sided, a continuum at fixed marginals, with `I_σ = 0` necessary but *not* sufficient for product structure (only the non-negative kinematic `E_cl` vanishes iff factorized). **The fence [CONDITIONAL].** *Given* the records modeled as an operator algebra on a state committing level-`(1+AB)` words (the premise; §2.3), the Gram matrix `Γ` of those observables is positive semidefinite *by construction*, and `Γ ⪰ 0` — the commuting-operator / NPA level-`(1+AB)` relaxation, strictly weaker than the tensor product it does not possess — **entails the almost-quantum envelope**: no-signaling, the Tsirelson ceiling `CHSH ≤ 2√2`, PR-box exclusion, the Collins–Gisin `I3322` strict-outer nesting (level-`(1+AB)` `0.2514709` > full level-2 `0.2509397` > the quantum bound `0.2508754`), and the Toner–Verstraete monogamy facet verified *sharp* — all from one construction, with the static core of Information Causality as an independent inside-witness. **The filling [NO-GO].** The same self-consistency **provably cannot select the quantum set inside that envelope**: `χ_AB` is free up to `Q̃` by two independent weight-`0` mechanisms — capacity blindness (the faithful Fisher leg is marginal-only, computed flat in the entangling direction to `1.3×10⁻¹⁴¹`, and every alternative capacity is marginal-blind, sign-even, or tautological) and field blindness (the moment algebra `M` is invariant under the complex→real reduction `R`, while the selecting datum — the local-tomography deficit `K_AB − K_A K_B ∈ {+1, 0}` — lies in `ker R`) — the three named escapes (the retained-holonomy phase, the sign, the magnitude) are closed, and pushing *any* `Q̃`-value, including an explicit super-quantum witness, through every downstream sector (geometry, correlation, covariance, matter, tripartite invariants, order-feedback) breaks nothing: every sector is an `M`-functional, and `σ ⊥ χ_AB` exactly. Global consistency *does* add two genuinely-new constraints, both orthogonal to the residual bit: a **connectivity floor** and a **manifoldlikeness obligation**. **The constructive half [THEOREM + DEMONSTRATED].** What the free filling *sources* downstream is carved by the weight grading: the entanglement content is weight-`0`, so it sources the record graph's **connectivity** (the Van Raamsdonk pinch) and the graph's **hop-metric up to one overall scale** (distance ratios invariant to `10⁻¹⁰⁰`), but provably **not the absolute metric** (weight `+1` — the same missing `l_step` that blocks the spacing and Newton's `G`; the wall's third appearance). **The residual bit.** The datum that would carry `Q̃` to `Q` is the tensor-product / local-tomography / complex-over-real choice — and its empirical status is currently **contested**: the 2021 real-QM falsification stands within its independence-postulate framing, while 2026 results exhibit real composition rules reproducing all network correlations, making the bit a possibly-unfixable composition-rule convention. That controversy **vindicates, without proving**, the no-go: the one bit the records cannot fix from inside is the one bit the field is currently unable to fix from outside. The meta-result of the series follows: the records' three blind inputs carry **three different epistemic statuses** — scale *measured*, mode *import-fixed*, tensor product *contested convention*. Novelty is claimed narrowly (§7): every hard mathematical ingredient here is imported and attributed (Tsirelson–Landau; NPA; the almost-quantum set; Pál–Vértesi; Toner–Verstraete; Slofstra and MIP\* = RE; the reconstruction lineage that already located local tomography as the selecting axiom); what this paper owns is the **substrate reading** (a pre-geometric record process possesses `Γ ⪰ 0` natively, as the Gram matrix of its own forced seals), the **two blindness mechanisms**, the **multi-scenario envelope from one object**, the **localization of the un-forceable bit to `ker R`**, and the **graph-not-metric split** — fence forced, filling free, and the free filling sources everything relative.

## 1. Scope guards (before any result)

1. **The `2√2` ceiling is not claimed as new.** It is Tsirelson's bound, obtainable from commuting ±1 observables since 1980 (Tsirelson; Landau), and at NPA level 1 since 2007. Where a contemporary derivation from causal-locality postulates exists for the CHSH game (Barandes–Hasan–Kagan), the value is co-derived and no priority is claimed; the non-overlap is the *multi-scenario* envelope from one substrate-owned object plus the un-forceability half.
2. **The envelope forcing is conditional and the premise is named.** "Γ is a Gram matrix by construction" requires the records modeled as an operator algebra on a state committing level-`(1+AB)` words. Probability tables alone do not force `Q̃` (the PR box is a no-signaling table); the off-diagonal completion entries are not table-measurable. §2.3 states the premise; every "entails" below is relative to it.
3. **The no-go does not assert that experiment fixes the bit.** The 2021–2026 literature is reported as a live controversy (§6); the paper takes no side and needs none: *contested outside* strengthens *un-forceable inside* only as vindication.
4. **Blindness mechanisms are stated at their proved strength.** The records carry *only* `R`-invariants (the direction actually proved) and are blind to *at least* the selecting bit; no "exactly" biconditional is asserted.
5. **The seal supplies the mechanism, never the kernel.** The sequential click-law (paper 1) forces the scalar survival skeleton; it does not force the matrix-level transition structure, and nothing here pretends otherwise (§7).

## 2. The substrate's transverse data: the joint law, then the arena

### 2.1 The joint click-law: forced skeleton, free complement [THEOREM + CONDITIONAL(paper 1 §2 premises (a)/(b1)/(b2))]

Two parallel record chains `A`, `B` (settings `x`, `y`; outcomes ±1 committed by orthogonal-projection seals, paper 1 §3.1) carry a joint constraint ledger. The joint odometer decomposes **exactly** over the product state space:

  `σ_AB = D(P_AB‖P_BA) = σ_A + σ_B + I_σ`,

with `σ_A, σ_B` the coarse-grained marginal odometers and `I_σ` the mutual term — the **interaction information of the joint seal statistics** [THEOREM; receipt `p4a_joint_clicklaw.py`]. The two anchors: a product (factorized) ledger gives `I_σ = 5×10⁻¹⁴¹` (zero at working precision — additive, `S_AB = S_A S_B`, no entanglement); a correlated ledger gives `I_σ = −0.0712` (non-additive).

**The skeleton transfers; the additivity does not.** Two refinement directions must be distinguished. The single-chain premise — survival a function of *one accumulated scalar*, which paper 1 §2 identifies as its premises (b1) + (b2) — is satisfied *jointly* for `X = σ_AB` (itself one number), so in the dense regime (premise (a)) the exponential shape transfers verbatim to the joint scalar argument via paper 1 §3.3's Cauchy step: Cauchy residual `0`, `p = 1` the only refinement-consistent exponent, a single `κ`, product limit `2×10⁻⁶` **[CONDITIONAL on exactly those premises; a genuinely-sparse joint regime inherits the geometric-progression skeleton of paper 1 §3.4 instead]**. But the *sequential* refinement (concatenation along the joint commit order) — the only direction the Cauchy argument uses — **telescopes exactly (gap `0`) while `I_σ = −0.0712 ≠ 0`**: it is provably *blind* to the mutual term. Additivity `X = χ_A + χ_B` is equivalent to the *separate* `A|B`-cut (parallel) factorization, which the sequential argument never invokes. So the genuinely new degree of freedom is *how the one scalar decomposes* across the `A|B` cut, and the machine-verified identity is

  `S_AB / (S_A · S_B) = exp(−κ · χ_AB)` with `χ_AB = I_σ`  (gap `0`, `p4a`):

the multiplicative factorization defect *is* the exponential of the mutual content. This theorem — the **additivity-not-forced telescope** — is the ancestor of the whole unforceability result: it is what made `χ_AB` free in the first place.

**The complement is free, and its freeness is exhibited.** `χ_AB` is **two-sided in sign** (`−0.0712` on a correlated ledger, `+0.0182` on a no-signaling one — a signed, interaction-information-type quantity, *not* a static `MI ≥ 0`), a **continuum at fixed marginals** (marginal spread `10⁻¹⁴¹` across distinct nonzero values), with parameter-independence exact (`PI` gap `10⁻¹⁴¹`) while `I_σ ≠ 0`. It becomes forced/unique only as a functional of the *full* bipartite kernel (the nearest-product reference is the unique minimizer; receipt `nonadditive_entangling_complement.py`).

**`I_σ = 0` is necessary, not sufficient — and the zero-iff object is `E_cl`.** `{factorized} ⊊ {I_σ = 0}`: being a *difference* of entropy productions, `I_σ` can cancel on a genuinely correlated kernel — `p4a` exhibits a convex mixture with `I_σ ≈ 0` at factorization-defect `≈ 0.09`. The object that vanishes **iff** factorized is the **non-negative kinematic** `E_cl` (§2.2): `E_cl(Bell) = ln 2`, `E_cl(product) = 0`, `E_cl > 0` on every correlated ledger. The signed *dynamical* `I_σ` (what enters the survival law) and the non-negative *kinematic* `E_cl` (the rigorous nonfactorization measure) are **distinct functionals** sharing only the factorized ⟹ zero direction — a zero-set containment, not an equality of numbers.

No spacetime, metric, or `s²` is used as input in any of this; the honesty guard of §5's circularity audit applies already here — the operator algebra of §2.3 is *posited* as the substrate model, never smuggled in as a "pre-geometric" Hilbert arena.

### 2.2 The Tier-A objects this paper presupposes [definitions inlined]

**`E_cl(A:B)`** — the column-averaged Kullback–Leibler divergence of the joint transition law `Γ_AB` from its nearest *product* law, defined entirely on `Γ_AB` (non-negative entries, unit column sums): non-negative, gauge-invariant, locally monotone, `E_cl(Bell) = ln 2`, zero iff product; the nearest-product law is the unique minimizer (receipt `nonadditive_entangling_complement.py`). **The ontology** — *finite same-process nonfactorization*: a joint interface is entangled iff its joint constraint ledger cannot be replaced by independent local ledgers plus shared classical records without changing the joint statistics. **The Bell address** — the Jarrett decomposition `LC = PI ∧ OI`: any single-history theory reproducing CHSH keeps parameter-independence, no-signaling, and single outcomes, and drops *outcome*-independence; the forced sequential skeleton is provably *silent* on OI (the OI ⊥ skeleton lemma, paper 1 §7.2, receipt `f5`, 20 checks) — which is what makes this paper's transverse axis a genuinely separate question rather than a corollary of paper 1. These definitions are carried inline (their frozen-log homes are v7-and-earlier); nothing below imports more of them than stated here.

### 2.3 The arena: every record principle is a moment functional [(C-form) THEOREM-grade structure + (C-level) POSITED]

The two chains meet only at shared cross-moments: commutation *on the state*, not a Hilbert tensor factorization — the substrate has no tensor product to assume. **The (C-form) argument for why the arena is exhaustive:** a self-consistency principle is a *closure condition on the records' own correlations* — the single-chain principle (`C = J`, paper 1 §4.2) is a balance between two functionals of the one-diamond law, and its transverse analog must be a condition on the joint correlations of the two chains. But the chains furnish no global state space; they couple *only* through the joint moments `⟨A_x B_y⟩` and the on-state symmetry `B_y A_x = A_x B_y`. So every principle the substrate can express is a balance or extremum **over the PSD cone of the Gram/moment matrix `Γ`** of its own statistical observables — a moment-positivity condition. That is (C-form), the robust structural claim: without it the no-go's quantifier ("*any* principle the records can host") would be unsupported.

**(C-level) is a separate, flagged premise [POSITED]:** a record interface commits a *single joint outcome pair* `(a, b)`, furnishing degree-`(1,1)` cross-moments only — a higher cross-party word such as `⟨A_x A_{x′} B_y⟩` would bind two non-commuting `A`-seals to one `B`-seal and reach a higher relaxation level. Under (C-level) the moment set is `M = {1, A_x, B_y, A_xB_y}` and `Γ_ij = ⟨ψ| O_i^† O_j |ψ⟩` is a Gram matrix, hence `Γ ⪰ 0` **by construction** [DERIVED, given the premise], with no-signaling encoded structurally (a single `⟨A_x⟩` entry, `y`-independent). This is the commuting-operator relaxation at level `(1+AB)` — the almost-quantum set `Q̃` of Navascués–Guryanova–Hoban–Acín [IMPORT] — strictly weaker than the tensor product it lacks.

**The qualitative no-go is level-independent [IMPORT-backed].** At *every* finite NPA level, and in the commuting-operator limit, the records still furnish no tensor factorization: commutation on the state is strictly weaker than a tensor product — the negative resolution of Tsirelson's problem (Slofstra 2019; Ji–Natarajan–Vidick–Wright–Yuen, MIP\* = RE). So (C-level) controls only *how far* above `Q` the feasible set sits — `Q̃` at `(1+AB)`, tighter supraquantum sets for higher words, never `Q` itself — not *whether* the gap exists. The premise ledger of this paper is therefore short: (C-form) structural, (C-level) posited, and everything below is relative to them.

Two transverse quantities recur below: the **entangling content** `χ_AB = I_σ` of §2.1 — the *filling* variable of this paper, which value the world realizes inside the fence — and the moment algebra `M`, whose invariance properties are the fence's and the no-go's shared engine.

## 3. The fence [CONDITIONAL(§2.3 premise); receipts `p6` 43/43, `s_probe` 26/26]

From the one object `Γ ⪰ 0`, verified end-to-end at `dps = 140` with the eight SDP digits flagged [SOLVER-TOL]:

1. **No-signaling** — structural (§2.3); the marginals are single entries, independent of the far setting.
2. **The Tsirelson ceiling** — NPA level-1 maximum `CHSH = 2.8284271251` against `2√2` (`|·−2√2| = 3.16×10⁻¹⁰`, solver-tol); the substrate's own click-law realizes the Tsirelson point with `|CHSH − 2√2| = 5.248×10⁻¹⁴¹` at `dps = 140` (an exact anchor, not a solver digit).
3. **PR-box exclusion** — the correlator `E = (1,1,1,−1)` is `Γ`-infeasible.
4. **The `I3322` strict-outer nesting** — the Collins–Gisin projector form (Collins–Gisin [IMPORT]): level-`(1+AB)` optimum `0.2514709`, strictly above the **full (both-orders, 28-word) level-2** value `0.2509397` — consistent with independent full-level-2 computations; no eighth digit is quoted, the SDP being solver-tolerance `~10⁻⁹` — itself strictly above the converged quantum bound `0.2508754` (Pál–Vértesi, NPA level ≥ 4 [IMPORT]); gap to `Q` `= 5.955×10⁻⁴`. So level-`(1+AB)` is a *genuine* `Q̃ ∖ Q` outer relaxation. (The ±1-correlator `I3322` encoding is *not* used: for it, classical = quantum = `Q̃` = `8.0` — no gap — and its bare-`Q¹` value `8.748` sits above `Q̃` and is records-inadmissible. The partial `x<x'`-only level-2 word list, value `0.2513864`, is an intermediate relaxation and is not quoted as level 2.)
5. **The monogamy facet** — `CHSH_AB² + CHSH_AC² ≤ 8` (Toner–Verstraete [IMPORT]) recovered over the three-party moment matrix `{1, A, B, C, AB, AC}` with product-operator generators (which bind `B`, `C` through `A`), and verified **sharp**: boundary points `(2√2, 0)` and `(2, 2)` (`sum² = 8`) feasible, just-inside `(1.9, 1.9)` (`7.22`) feasible, just-outside `(2.05, 2.05)` (`8.41`) and `(2√2, 0.30)` (`8.09`) both infeasible — the flip is at `sum² = 8`, a genuine SDP facet, not a hardcoded binding. (Sharpness at the CHSH ceiling itself was verified in the frozen `t1` run — a just-past point `CHSH = 2.9084` infeasible; the live sharpness scan here is the monogamy facet's, in two directions.)

**An independent inside-witness: the records own the static core of Information Causality.** In the Pawłowski–Paterek–Kaszlikowski–Scarani–Winter–Żukowski derivation, `CHSH ≤ 2√2` follows from a mutual-information functional with four properties, and the record KL mutual information owns all four — non-negativity, consistency (`I = 0` iff factorized), the data-processing inequality, and the chain rule (residual `3.63×10⁻¹²²`) — with the IC facet `V(E) = E² − ½ ≤ 0` saturating *exactly* at the Tsirelson point (`V = −9.7×10⁻¹²²` at `E = 1/√2`; `V < 0` sub-Tsirelson, `V > 0` super-Tsirelson, the PR box maximally violating). Receipt `t3_tsirelson_derivation.py` (16 checks, `dps ≥ 100`). **The honesty guard travels with it:** this is one operational lever short of full Information Causality — the random-access-code *protocol* that turns the static functional into the dynamical IC statement is not forced by the seal-law — so the claim is "static core owned, an independent witness that the ceiling is Tsirelson's," never "IC derived."

One construction, five envelope elements plus an independent witness: that is the fence, and it is the *almost-quantum* fence, not the quantum set.

## 4. The filling is un-forceable [NO-GO; receipts `p6` 43/43, `p6b` 21, `p6c`, `p6d` 20, `s_probe` 26/26, `c3`, `s_geom` 20/20, `s_order` 18/18]

### 4.1 What a transverse forcing would have to be [the criterion]

The single-chain template this paper's question is defined against (full derivation: paper 1 §4.2): the accumulated KL content `C(η) = η tanh η − log cosh η` **strictly rises** (`C′ = η sech²η > 0`, sympy-exact) and the contrast's Fisher capacity `J(η) = sech²η` **strictly falls** (`J′ = −2 tanh η sech²η < 0`), so they cross exactly once, at `η_* = 1.0903443548…`, and the max-min identity `W_* = max_η min(C, J) = J(η_*) = 0.3647849520…` makes the crossing a genuine *forcing* — the largest content a diamond can simultaneously accumulate and hold. The reduction to a *scalar* `min(C, J)` is itself forced by the `q = 2` single-channel screen. That crossing (LAW-A) is the engine of every forced number in the sequential theory. The distinct **seal-firing root** (LAW-B, `tanh η = e^{−η}`, `η_B = 0.6093778634…`, committing `0.428·W_*` — paper 1 §4.3) is a survival fixed point, *mode-dependent*, and forces nothing canonical; a transverse analog that mimics the *firing* inherits that. Only a LAW-A-type max-min crossing forces.

**The checklist a transverse analog must satisfy to force `χ_AB`:** (i) two record-internal scalar functionals of one shared transverse parameter; (ii) *proved* monotone opposition (one strictly rising, one strictly falling); (iii) a supply-vs-capacity reading at the right weight (`χ_AB` is weight-`0` — good); (iv) a *scalar* capacity (else only a Pareto constraint); (v) a crossing at an *intrinsic* parameter value, not one drifting with the marginals; (vi) the max-min attained analytically; (vii) **no imported quantum axiom** — no tensor product, local tomography, Tsirelson bound, or composite Born rule. The transverse principle **fails at (ii), (iv), (v), and (vii)** — and the failures are the two mechanisms below.

**Why this is a new *kind* of no-go.** The scale/`G` no-go (paper 3, SCALE leg) is a *grading-homomorphism* obstruction: an absolute length is weight `+1`, and a weight-`+1` object can never be a weight-`0` record functional — a degree count. Here `χ_AB` is weight-`0` — *the right weight to be forced* — and the obstruction is instead **invariance / capacity-degeneracy at weight-`0`**: the records' self-consistency is constant along the `R`-orbit (M2) and its capacity leg is flat in the entangling direction (M1). Different mechanism, same shape of conclusion; paper 3 carries the group-level version (`G₁ = ℝ₊` vs `G₂ = ℤ₂`).

### 4.2 M1 — capacity blindness [THEOREM on the arena]

The literal transverse analog is the **parallel-diamond capacity crossing**. The joint record law is the maximum-entropy family on the sufficient statistics `(a, b, ab)`,

  `P(a, b) ∝ exp(η_A a + η_B b + λ ab)`,

with `(η_A, η_B)` the per-chain content knobs and `λ` the entangling parameter that, at fixed marginals, controls `χ_AB = I_σ`. The faithful capacity leg is exactly what the single-chain crossing used — the marginal-tilt Fisher `Var(a) = 1 − ⟨a⟩²` (the analog of `J = Var(q)`). To test a forcing of `χ_AB` the marginals must be held fixed (otherwise "capacity" merely tracks the marginals — the circularity below), so `(η_A, η_B)` are re-solved as functions of `λ` to pin `⟨a⟩ = ⟨b⟩ = m` (marginal residual `< 10⁻¹⁴⁰`).

**The capacity leg is exactly blind to the entangling parameter.** `Var(a) = 1 − m²` is a function of the marginals alone, so at fixed `m` it is constant in `λ`: across `λ ∈ [−0.6, 0.6]` the spread of `Var(a)` is `1.312×10⁻¹⁴¹` (one unit in the last place at `dps = 140`), and the central-difference `dJ_marg/dλ` sits at the round-off floor `≈ 6.6×10⁻¹¹²`. The crossing re-fixes only the marginals (recovering the per-chain `η_*`) and says nothing about the entangling content. Checklist item (ii) fails on the capacity side: there is no falling leg in `λ`.

**There is no single rising leg either.** The joint KL content `C_AB(λ)` is **U-shaped** at fixed marginals — minimized at the product `λ = 0`, rising on *both* signs (at `m = ½`: centre `0.26162407`, wings `0.37125` at `λ = +0.6` and `0.31128` at `λ = −0.6`) — reflecting the signed two-sidedness of `I_σ`: departure from independence in either direction raises the joint content. So `max_λ min(C_AB, J)` is not even well-posed as one crossing.

**Any forced-looking balance is circular and marginal-dependent.** One can still write a balance — e.g. `C_AB(λ) = Cov(a, b)` — and solve it. Its root **moves with the marginals**: `λ_* = 0.115493` at `m = 0.3`, `0.618918` at `m = ½`, non-robust (no genuine crossing) at `m = 0.7` — the diagnostic signature of a non-forcing balance (the single-chain `W_*` is marginal-independent). The structural reason previews M2: at fixed marginals the only free level-`(1+AB)` coordinate is the cross-moment `E_ab`, and the only Fisher entry that responds to `λ` is `Cov(a, b) = E_ab − ⟨a⟩⟨b⟩` — the entangling content *itself*. Balancing content against the entangling content is circular, not a content-vs-capacity opposition.

**Robustness over the definition of "capacity."** The no-go survives replacing the faithful leg; every defensible record-internal alternative was tested at fixed marginals (`dps = 140`, sympy-exact signs):

- **Entangling-parameter Fisher `J_λ = Var(ab) = 1 − E_ab²`** — genuinely `λ`-dependent, but sympy-exact `sign(dJ_λ/dλ) = −sign(E_ab)`: a **hump** (rises while `E_ab < 0`, peaks at `E_ab = 0`, falls after), not a monotone-falling capacity; and its turning point is marginal-dependent (`λ* ≈ −0.112` at `m = 0.3`, pushed to the boundary for `m ≥ ½`). Items (ii) and (v) fail.
- **The `A|B`-cut capacities** — the record-rate mutual information `I(A:B)` *and* the Blahut–Arimoto channel capacity across the parallel cut: both **U-shaped**, minimized at `λ = 0`; the channel reading additionally re-optimizes the input, moving the marginals. Item (ii) fails.
- **The nearest-product KL `E_cl`** — U-shaped, minimum `0` at `λ = 0`, sign-blind. Item (ii) fails.

**The exhaustive three-way classification:** at fixed marginals, any record-internal capacity is (a) *marginal-only* — blind; (b) an *even* function of the signed correlation about independence — U-shape or hump, a magnitude-of-departure, sign-blind; or (c) `E_ab` *itself* — tautological. None is the strictly-monotone-opposed pair with a marginal-independent crossing that forced `W_*`. This classification is the quantifier over definitions: the blindness is not an artifact of one capacity choice.

**The canonicity criterion, and the golden counterfeit.** One *can* manufacture a monotone-opposed pair — `E_ab` (rising) vs `1 − E_ab²` (falling) — crossing at the golden moment `E_ab^* = (√5−1)/2 = 0.6180339887…`. But the crossing sits at a **marginal-dependent** `λ` (`0.685 / 0.602 / 0.377` at `m = 0.3 / ½ / 0.7`, spread `0.31`), and the pair carries a **free relative dial** `c` (`1 − E_ab² → c(1 − E_ab²)`) under which the root moves (`0.414 / 0.618 / 0.781` at `c = 0.5 / 1 / 2`) — the "forced" value is an artifact of `c`. The criterion separating the genuine `W_*`: the forcing pair must be the **KL content and Fisher capacity of one canonically-normalised exponential family** — `C = ηψ′ − ψ` and `J = ψ″`, both derived from the *same* log-partition `ψ = log cosh η`, with the `q = 2` screen fixing the units so **no free relative scale is available**. (This is *not* a claim that `W_*` is invariant under independent rescaling of `C` and `J` — the crossing would move; the point is the records offer no such dial.) Without this criterion, "tautological" would be name-calling; with it, the golden crossing is a diagnosed counterfeit.

### 4.3 M2 — field blindness [THEOREM on the arena]

At the Tsirelson correlators `E = (s, s, s, −s)`, `s = 1/√2`, the joint law `p(a, b|x, y) = (1 + ab·E_xy)/4` is realised by *both* a qubit singlet and a rebit Bell pair, and the field reduction `R` (qubit-pair → rebit-pair, complex → real) preserves **every element of `M`**, with numbers: identical marginals `(½, ½)` (to `< 10⁻¹¹⁸`), identical cross-moments (`CHSH = 2√2` to `< 10⁻¹¹⁸`), identical orthogonal-projection seal (`|E² − E| ~ 6.6×10⁻¹⁴²` over *both* fields), identical `q = 2` norm, identical Tsirelson point, and identical sealing scalar — `I_σ(rebit) − I_σ(qubit) = 0` exactly (`< 10⁻¹³⁰` numerically; both `= 0.27665…`). What `R` changes is the local-tomography count: `K_single = 3/4` (rebit/qubit), `K_AB = 10/16`, so the deficit `K_AB − K_A K_B` is `+1` over ℝ (local tomography fails, `10 ≠ 9` — the classic rebit deficit, Wootters [IMPORT]) and `0` over ℂ (`16 = 16`), a sympy-exact integer fact. The one bit that would select a unique complex `χ_AB` — carrying the feasible set from `Q̃` to `Q` — is exactly this deficit, and it lies in the **kernel of the records' factorisation through `M`**: every record-internal principle is constant on the `R`-orbit while the selecting bit varies across it. The records carry *only* invariants; the bit is invisible. `χ_AB` is unreachable **not by weight but by symmetry**.

**An internal honesty note that controls how M2 is read:** the `I_σ(rebit) = I_σ(qubit)` equality is **true-by-design at the shared Tsirelson point** — both correlators live in the `X`–`Z` plane that `R` preserves — so it is *not* presented as independent evidence of field-blindness. The genuinely field-distinguishing computation is the holonomy audit of §4.4 (`p6b`); the no-go rests on the **`K`-deficit / orbit argument**, not on that equality.

**Two faces of one wall.** M1 *is* M2 seen information-geometrically: the faithful capacity is a function of the `R`-invariant marginals, hence flat in the entangling direction. One blindness, two exposures.

### 4.4 The three escapes, closed, and the cocycle [THEOREM/DEMONSTRATED]

The no-go rests on the premise that every transverse principle is a functional of `M`. Three named resources could have broken it; all three die.

**Escape 1 — the retained-holonomy phase [closed; `p6b` 21 checks].** A breaking resource would have to be *simultaneously* (a) forced by the sealing self-consistency, (b) not a functional of `p(a, b|x, y)`, and (c) field-distinguishing. Construct three realisations of the Tsirelson point — `R1` a qubit singlet in the `X`–`Z` plane, `R2` a qubit carrying a genuine `Y`/phase component, `R3` a rebit pair — all with the identical correlator table (gap `0`). Then: **(a) fails** — the `Y`-phase is pure local gauge (`R2` reproduces `R1`'s correlators, gap `1.3×10⁻¹⁴¹`; identical *full* moment table, gap `2.2×10⁻¹⁴¹`; identical gauge-invariant Bargmann invariant, `7.4×10⁻¹⁴³`), and the forced `q = 2` orthogonal-projection calculus (`E = E² = E*`, `q² = 1`) is **real-closed** — it holds over ℝ with no imaginary unit, so the seal never manufactures a phase. **(b) fails** — on the records' shared (real-direction) seals, the composite seal-loop invariant *is* the degree-`≤3` moment polynomial `z = (1 + Σ⟨O_k⟩ + Σ⟨O_iO_j⟩ + ⟨O₁O₂O₃⟩)/8` — an algebraic identity for any joint state, verified **non-vacuously** over generic loops and states with `|z|` up to `0.73` (reconstruction gap `1.3×10⁻¹⁴¹`), not just at the degenerate Tsirelson point. **(c) fails** — `Im(z) = 0` on all shared `X`–`Z` seals over *both* fields (real matrices ⇒ Bargmann phase ∈ `{0, π}`, a sign already fixed by the real `E_xy`); the only genuinely complex value needs an out-of-plane `Y`-seal (single-party Pancharatnam loop, `γ_Y = π/3 = 1.0472`), which the rebit substrate cannot host, self-consistency does not force, and a local unitary fixing all of `M` removes. **The positive control:** `Tr(P_X P_Y P_Z) = 0.25 + 0.25i` (`Im = 0.25`, the octant solid angle) — the probe *would* see a phase if the substrate carried one, so "`Im = 0` on shared seals" is a result, not a blind instrument. This also resolves the corpus's old flag that "the retained-holonomy phase enters composition without proven content": its content is exactly the moment polynomial.

**Escape 2 — the sign [permanently free; `p6d` 20 checks].** The outcome-relabel involution `τ` flips `E → −E` (hence `I_σ`'s sign) while leaving `E_cl`, `E²`, the marginal Fisher (`= 1`), and the CHSH-magnitude **exactly invariant** (sympy-exact). Witness pair `E = ±0.68`: identical `E_cl = 0.253477301159…` (gap `0`), signed interactions summing to exactly `0`. The sign lives in a quotient every magnitude functional is blind to — free *independently* of whether any magnitude is ever forced.

**Escape 3 — the magnitude [no value selected; `p6d`].** `E_cl` is a strictly-monotone continuum `0 → ln 2`, realising every interior value at fixed marginals (`ln 2/2` at `t = 0.194986…`, residual `< 10⁻¹³⁶`); the single-chain forcing has no transverse instance (the capacity is flat, so the balance is degenerate); any candidate balance (`E_cl = c·Cov`) drifts with the marginals (`0.131 → 0.170`, drift `0.038` — the M1 circularity signature again); and `E_cl` is even in the signed coupling, its only internal stationary point the trivial product `0`.

**The cocycle is trivial — and its scope is stated.** The associator `((s₁s₂)s₃)/(s₁(s₂s₃)) ≡ 1` **sympy-exact** (because `exp` is a homomorphism to `(ℝ, ×)`), and grouped survival is grouping-independent over `2200` random assignments of the free mutual terms (gap `1.3×10⁻¹⁴¹`) — no transverse *phase* obstruction hides in the sequential skeleton [DEMONSTRATED; `p6` PART 6]. Scope guard: this certifies the *scalar* skeleton only — the nontrivial associator that *would* force structure is an `F`-symbol of a fusion category, which needs exactly the composite tensor product the seal is blind to. The same wall, by a different road.

### 4.5 Global consistency does not rescue it: the sweep [DEMONSTRATED]

**The probe logic, and the stakes.** Treat `χ_AB` as a dial with four positions: `(a)` `χ_AB = 0` (product); `(b)` a generic quantum point `P_Q ∈ Q`; `(c)` the decisive **super-quantum witness** `P* ∈ Q̃ ∖ Q`; `(d)` a supra-`Q̃` PR box — already `Γ ⪰ 0`-excluded. The counterfactual is what gives the null result its force: *if anything downstream had broken at `P*`, global consistency would have selected `Q` — recovering the tensor product as derived rather than postulated.* It does not break.

**The witness, self-certified.** `P*` is the Collins–Gisin projector `I3322` optimum over the *true* level-`(1+AB)` moment matrix — the base words **plus all nine cross-products** `P_A[x]P_B[y]`, not the bare first NPA level — value `0.2514709`, which exceeds the **full level-2 value `0.2509397`** by `+5.3×10⁻⁴`. Since every finite NPA level is a rigorous *upper* bound on `Q`, the level-2 value is an **in-house `⪰ Q` bound**, so `P*` is certified outside `Q` *self-containedly, without relying on any external digit*; Pál–Vértesi's level-`≥4` value `0.2508754` then corroborates. `P*` is extracted as a fully-specified no-signaling probability table and pushed through the sectors *by its own probabilities* (receipt `s_probe_qtilde_minus_q.py`, 26/26).

**The sector push, with values** (each sector *computed*, not waved at):

- **Geometry** — the entanglement graph read off `P*`'s own per-setting mutual informations `I(A_x:B_y)`: a moment functional that genuinely *moves* across settings, yielding a connected graph — identical for any `M`-equivalent quantum point.
- **Correlation** — the static IC core's chain rule holds with residual `0` on a ledger built from `P*`'s own no-signaling marginals; macroscopic locality and local orthogonality are satisfied.
- **Covariance** — no-signaling residual `0`; a boost (an order-automorphism) preserves `386 = 386` causal relations (the gates below).
- **Matter** — the mode fixed point `η_B = 0.6093778634…`, the gap `W = 0.1561092001…`, the chiral `m̂ = ln 2`: all **flat under the `χ_AB` sweep to `< 10⁻¹⁰⁰`** (the mode reads marginals alone — the orthogonality datum the series' three-input map uses).

**The by-construction honesty that controls the whole sweep:** the numeric sector-by-sector identity is true **by construction** — `P*` and `P_Q` share the same level-`(1+AB)` data, differing only in the kernel flag, so any `M`-functional must return the same value. The load-bearing claim is the **structural** one — each sector *is* an `M`-functional, argued sector by sector — and the numbers are illustration, not independent evidence. One caveat is not overstated: **macroscopic locality reaches only `Q¹`** (Navascués–Wunderlich), so it cannot exclude `P*`; only "satisfies" is asserted, consistent with the field result that no device-independent principle reaches `Q`.

**The covariance gates [receipt `c3_covariance_chi_ab_probe.py`, 15 labeled checks, each echoed once in the run's summary block].** The relativistic sector is where a super-quantum correlation might plausibly break — super-quantum boxes are famous for communication-complexity collapse and causality pathologies. It does not, and the reasons are exact: **(1)** no-signaling is a **Lorentz scalar** — the single-party marginal is a frame-independent count, so no-signaling-static ⟺ no-signaling-under-boosts for *any* box, including the PR box (gap `0`, exact); covariance imposes nothing beyond no-signaling. **(2)** **Microcausality *is* the on-state commutation that defines `Q̃`** — the spacelike condition reduces (sympy-exact) to `[A_x, B_y] = 0` on the state — two faces of one constraint, which is *why* covariance recovers `Q̃` and never `Q`. The commutation is witnessed **without smuggling the tensor product it is weaker than**: abstractly, in the free non-commutative algebra (the relation `[A_x, B_y] = 0` alone collapses the cross-moment), and concretely on a **single Hilbert space of prime dimension 3**, where *no* factorisation `H_A ⊗ H_B` exists at all — a tensor realisation is sufficient, not necessary. **(3)** `P*` passes every gate: no-signaling, on-state commutation by construction, and **sub-Tsirelson** — its CHSH block is `1.339 ≤ 2√2`, computed on `P*`'s own table in `s_probe` (`c3`'s own ceiling gate is exercised on the Tsirelson-facet surrogate; the §9 receipt-reading note keeps the two objects apart) — so the van Dam / Brassard communication-complexity collapse (which needs `CHSH > 2√2`) does not bite. `P*` is a `Q̃`-**boundary** point: its native optimiser matrix is singular (min-eig `−1.3×10⁻⁹`, the solver floor [SOLVER-TOL]); re-pinned within a `10⁻⁷` moment band it completes with margin `+1.1×10⁻⁷` — boundary membership is membership. The communication-complexity and relativistic-causality teeth act only *outside* `Q̃`, which `Γ ⪰ 0` already excludes. (The emergent-Lorentz action and point-locality premises are the same Tier-B gates paper 4 §6 quarantines; this sector is conditional on them, on nothing new.)

**The order-feedback frontier, closed at three grades [receipt `s_order_feedback_probe.py`, 18/18].** The one place a break could genuinely have hidden is a *feedback*: if the dynamics generating the causal order were driven by the transverse `χ_AB` pattern, a super-quantum pattern could imprint a pathological order the static tests would miss. *Structural (sympy-exact):* the order's generator `σ = D(P_AB‖P_BA)` is a longitudinal transport functional of the commit-order entries `{p_ij}`; `χ_AB` is a transverse functional of the joint-outcome variables `{λ, η_a, η_b}`; the variable sets are **disjoint**, every cross-partial `∂σ/∂χ_AB ≡ 0`, and a coupled generator `σ + g·F(χ)` has `g = 0` *forced* by the corpus definition of `σ`. *Feedback (numeric):* across `200` random entanglement-graph re-wirings the `σ`-ranking of `K = 6` distinct seals is permuted **zero** times; the `σ`-spread is exactly `0` across a sweep in which `χ_AB` moves `0 → 0.3657` and `E_xy` by `1.295` at pinned marginals `(0.3, 0.2)`. *Non-vacuity (the critical check):* an adversarial surrogate generator (a `χ`-incidence rank) genuinely produces a *different* ordering (`[0,4,3,5,1,2]` vs `σ`'s `[4,3,0,2,1,5]`) — a `χ`-driven order **is** constructible; it is simply **not** the SHARD generator. The honest residual is **definitional, not a tolerance gap**: the closure is theorem-grade for the built generator; it would re-open only under redefinition to an unbuilt `χ`-driven dynamics that paper 1's forced odometer excludes.

**The tripartite closure [premise-scoped; `s_geom` part (iv)].** Could a *higher-order* invariant — three records at once — see the bit the pairwise data hides? Not at the commitment the records host: on the valid degree-`(1,1)` tripartite no-signaling law `p(abc) = ⅛(1 + ab·E_AB + ac·E_AC + bc·E_BC + abc·E_ABC)`, the interaction information `II(A:B:C)` and the triangle invariant `Tri = I(AB) + I(AC) + I(BC)` **factor through `M`** — bit-identical (gap `0`) across the qubit and rebit realisations — and the check is **non-vacuous**: `II` genuinely moves (`−0.00201 → 0.00962` as `E_ABC: 0.15 → 0.05`). Honest scope: this closes the tripartite path **only at degree `(1,1)`** — more *nodes* at the same per-pair commitment do not open the gap; the actual `Q/Q̃` separation lives at degree `> (1,1)` (§4.7).

### 4.6 What global consistency *does* add: two new constraints [DEMONSTRATED; `s_geom` 20/20, zero solver-tol]

Global consistency is not toothless — it carves `Q̃` from above at two places the device-independent literature cannot reach (that literature being single-scenario and geometry-blind). Both are orthogonal to the residual field bit.

**(1) The connectivity floor.** Edges of the emergent geometry are mutual-information bonds: `edge ⟺ I(i:j) > 0`, with the edge weight sign-blind (`I(+0.85) = I(−0.85) = 0.42676…`, sympy-exact via the relabel involution). The pinch is stated *honestly*: a **single** zero bond does **not** disconnect — one edge is removed and the node stays attached through its other bonds (verified: `E_02 = 0` alone gives **one** component); genuine isolation needs a **globally product node** — all bonds zero — which does pinch off (**two** components; the Van Raamsdonk pinch). Hence: **a connected emergent geometry requires `χ_AB ≠ 0`.** This is a constraint neither the transverse no-go (which *permits* `χ_AB = 0`) nor the device-independent program (silent on geometry) carries. But it carves the **measure-zero face** `{χ_AB = 0}`, *not* the positive-measure `Q/Q̃` gap: at equal edge weight a quantum and an almost-quantum point give the *identical* graph (`I = 0.19274…`, gap `0`).

**(2) The manifoldlikeness obligation.** For a smooth spacetime to emerge, the multi-record `χ_AB` **magnitude pattern** must be manifold-like — and Kleitman–Rothschild dominance makes that rare (at `n = 10⁴`: `~n²/4 = 2.5×10⁷` bits of generic partial orders vs `~n log n = 5.3×10⁵` bits of manifold-embedding data; the KR gate itself, with the field-shared dynamical-selection open, is paper 4 §5's). The `R`-invariance of the manifoldlikeness diagnostics is **structural, not numeric**: the Myrheim–Meyer functionals read the magnitude-induced *order* alone, and `R` preserves every edge magnitude exactly — a literal no-op, invariant *by construction* (by which inputs they read), not by feeding identical arrays. And the diagnostic is **non-vacuous**: a 2d sprinkle gives `d_MM = 1.9453` (in the manifold window `[1.7, 2.3]`; ordering fraction `r = 0.52086`), while a 1d-like near-totally-ordered pattern gives `d_MM ≈ 1.0`. The obligation carves along the **dynamical-selection axis** (does the click-law beat KR dominance? — the field-shared open), **orthogonal** to the complex-over-real axis.

**Do-not-conflate (five conflations, policed).** (1) The tensor product ≠ local tomography — the rebit theory *has* a tensor product yet *fails* local tomography; two distinct axioms, one origin (no composite state space at all). (2) The manifoldlikeness selection-bit ≠ the complex-over-real bit — different axes (longitudinal order-combinatorics vs transverse correlator). (3) Multi-node ≠ higher field-resource — more nodes, same per-pair commitment (§4.5, tripartite). (4) `CHSH` alone does not witness `Q̃ ∖ Q` — one needs `I3322` at level ≥ 4 (at CHSH, `Q̃` and `Q` coincide at `2√2`). (5) A supra-`Q̃` monogamy-violating star's pathology is not a new carving — it is already `Γ ⪰ 0`-excluded.

### 4.7 The disclosed residue [OPEN]

**The commitment premise (the deeper locus).** Field-blindness is proved for degree-`(1,1)` single-joint-outcome-pair words; the actual `Q/Q̃` separation lives at degree `> (1,1)` (`I3322` needs level ≥ 4). A substrate committing *higher* cross-party words is not audited here — and by §2.3's level-independence theorem, higher words shrink `Q̃` *toward* `Q` but never reach it: the premise controls the size of the overshoot, never the existence of the gap. Two mitigations shrink the residue honestly: the holonomy result is **field-agnostic** (the composite invariant is a moment polynomial for *any* field reproducing the same real `E_xy` — which also softens the un-audited-quaternionic-foil gap), and the order/graph arguments never touch the correlator's would-be imaginary content. Both named, neither closes the higher-word case.

**The two honestly-unexhausted capacity classes** are named *with the reason they are not record-internal Tier-A principles*: capacities defined via a *preferred tensor split* import the very `χ_AB` they would force; off-state / higher-moment algebras escalate to a different arena. Neither is a forcing route; both are the same disclosed premise.

**The unbuilt two-party matter locus.** The no-link `χ_AB ↔ mode` is, at the executed single-party level, tautological (the mode reads marginals only; the `W`-flatness `< 10⁻¹⁰⁰` above). A genuine two-party matter interaction — the interacting Ginsparg–Wilson flow (paper 5) or a mode-selecting Hamiltonian — is the one place a coupling could arise, and it is unbuilt. The *a fortiori* field-blindness argument (any such link must escape `R`-invariance, thereby importing the tensor/LT axiom — circular) is sound **but is about an unbuilt sector — flagged, not closed**.

## 5. What `χ_AB` sources downstream: the graph and its one missing unit [THEOREM + DEMONSTRATED; receipt `p4b` 5 checks]

The filling is free (§4); this section is its constructive complement — what *any* realized filling sources. The answer is carved exactly by the weight grading (the grading tool lives at paper 1 §4.4 and paper 3's SCALE leg; it is cited in-series, not re-derived).

**The weight-split [THEOREM given the grading tool].** The entanglement content — the mutual information `I(i:j)`, and indeed the whole joint KL content — is **weight-`0`**: a pure KL number in nats, gauge-invariant under the record-length regrading `l → μl`. A graph **distance is weight `+1`**: a length `= #hops × l_step`. By the grading trichotomy, a weight-`0` record functional can never supply the one absolute `l_step` a metric needs. Therefore: **entanglement sources weight-`0` structure — connectivity, and the graph's hop-metric up to one overall scale — but provably not the absolute metric.** This is the **third appearance of the same wall**: the missing absolute `l_step` blocks the spacing `d` (paper 1 §4.4), Newton's `G` (paper 3/paper 4), and now the emergent metric. The Van Raamsdonk direction is exactly **half-open**: topology yes, distance no.

**The pinch, in its honest form [DEMONSTRATED].** With edges defined by mutual information, an entangled pair (`I > 0`) is an edge and a product pair (`I = 0`) no edge — and the honest statement of the pinch is §4.6's: a *single* zero bond does not disconnect; a **globally product node** does. Reducing all of a region's entanglement bonds to zero pinches it off — connectivity from entanglement, as a purely weight-`0`, non-circular statement. The floor (§4.6: connection ⇒ `χ_AB ≠ 0`) and the pinch (zero correlation ⇒ disconnection) are the two directions of one weight-`0` fact, stated side by side.

**The hop-metric, up to one scale [DEMONSTRATED].** The weight-`0` connectivity graph carries integer shortest-path (hop) distances; the physical metric is `hop-distance × l_step`, and under `l_step → μ·l_step` the distance **ratios** `d_ij/d_kl` are invariant to `10⁻¹⁰⁰` while absolute lengths scale by `μ`. So the records deliver the relative hop-metric — missing only the single multiplicative unit. **The honesty guards:** the result is *coarse* — the unweighted graph only, no continuous Riemannian similarity class, no curvature exhibited. It is **not** a `−log I` "conformal" claim: a `−log I` edge length carries an *additive zero-point gauge* under `I → sI`, fixing distance *differences* rather than the ratios `d_ij/d_kl` (which are not invariant there), and one overall scale is not a local-Weyl conformal class. And "entanglement = area" survives only as "= area *up to the one free unit*" — never the absolute Ryu–Takayanagi coefficient.

**The circularity audit.** Every existing geometry-from-entanglement in the corpus runs the *opposite* (Jacobson) direction — geometry assumed, the entanglement–area relation read off as an equation of state. That is induced-gravity-grade, not vicious (no metric is inserted and recovered). The one genuinely **vicious trap** is named: scoring recovered coordinates against the *true simulation coordinates* of a metric-generated sprinkling — recovering a metric that went in is consistency, not emergence. A clean entanglement→geometry construction must (i) define the correlation on `Γ` alone — no Hilbert/tensor arena silently re-imported as "pre-geometric" (the same honesty §2.3 applies to this paper's own premise: the operator algebra is *posited*, and said so); (ii) reconstruct only the weight-`0` *relative* metric, blind — no access to true coordinates; (iii) never source geometry from a weight-bearing coefficient. (Paper 4 carries the same trap as a held-out-coordinates methodology guard for the continuum program; one trap, two exposures, cross-referenced.)

**Does-not-claim ledger for this half:** no absolute metric, no RT area-coefficient `1/4G`, no ER=EPR wormhole *length* (all weight `+1`, no-go-blocked); no full bulk-emergence (premature — it needs the realized `χ_AB` for the gravitational ledger *and* the matter sector); "particle = stable long entanglement link" is *not* in the corpus (a named future program, gated behind matter).

## 6. The residual bit, and its contested status [reported, not owned]

The one datum that would carry `Q̃` to `Q` is the tensor-product / local-tomography / complex-over-real choice. The slash-phrase names *distinct axioms mapping onto distinct gaps*: the **global tensor product** closes `Q̃ ∖ Q` (the *moment-form* gap of §2.3); **local tomography** selects ℂ over ℝ *within* a tensor-product world (the *state-space-form* gap of §4.3) — the rebit theory has the first and fails the second (§4.6, conflation 1) — sharing one origin, no composite state space at all. It is the composition-rule axiom the reconstruction lineage had already isolated as the selecting one (Hardy; Chiribella–D'Ariano–Perinotti; Masanes–Müller [IMPORT]); Barnum–Müller–Ududec reach ℂ by a route that *bypasses composition axioms entirely* (single-system postulates plus energy observability), which independently underlines that the selecting datum is an axiom choice, not a forced structure [IMPORT].

**Its empirical status, in detail.** Renou et al. (Nature **600**, 625 (2021)), with the Chen et al. and Li et al. realizations, rule out real quantum mechanics *within their independence-postulate framing* — in which the result stands. The 2026 reopening targets exactly the composition-rule locus this paper's no-go names: **Hoffreumon–Woods** (arXiv:2603.19208) show real quantum theory under the **Kronecker composition rule**, with *operational* (not product-state) **independence**, reproduces **all finite-network correlations** — the postulate *choice* is precisely the locus; **Maioli–Curado–Gazeau** (arXiv:2604.19482) exhibit a real Kähler framework with a **symplectic composition rule** reaching the maximal `CHSH3 = 6√2` with real variables; the **Renou et al. Comment** (arXiv:2604.07425) responds that the Hoffreumon–Woods postulate **fails in Fermionic Information Theory** and is not a general physical postulate. The debate is live; this paper takes no side (guard 3).

**The vindication argument, as an argument.** The natural worry about the no-go was: *"surely experiment fixes the bit from outside — as a measurement fixes `G` — so 'the records can't' is mere bookkeeping."* The controversy undercuts exactly that worry: if the complex-over-real bit is a composition-rule **convention** that may be experimentally **unfixable**, then *neither the records nor a clean experiment may carry it* — which is the strongest available corroboration of the un-forceable-import reading. This paper did not predict the controversy; but the controversy is exactly what "un-forceable from inside" looks like when viewed from the outside. **Vindication, never proof**; the physics question remains open.

**The series meta-result follows [DERIVED (classification)]:** the three blind inputs of the record substrate divide as **scale — measured** (one datum, like `G`); **mode/masses — import-fixed** by measured spectra; **tensor product — contested convention**, possibly unfixable in principle. The three legs sit on orthogonal axes — a scale, a spectrum anchor, a composition bit — not one kind of thing wearing three hats; there is no clean "three experiment-fixed inputs" symmetry, and the corpus no longer claims one (the full classification is paper 3's).

## 7. Relation to the literature — what is owned, what is not

**Imported, with attribution (the mathematics):** Tsirelson's bound from commuting observables (Tsirelson 1980; Landau 1988); the NPA hierarchy and level-1 CHSH cap (Navascués–Pironio–Acín 2007/2008); the almost-quantum set `Q̃ = Q^{1+AB}` and the field-wide result that natural device-independent principles reach `Q̃` and stop (NGHA 2015) — `Q̃` itself is an external construction whose definition the records *instantiate rather than invent*; the `I3322` quantum bound and level analysis (Pál–Vértesi 2010); monogamy (Toner–Verstraete 2006); Information Causality (Pawłowski et al. 2009 — only its *static core* is owned, §3); macroscopic locality (Navascués–Wunderlich) and local orthogonality (Fritz et al.) as the comparison principles; the non-closure of `Q` and its level-independence (Slofstra 2019; MIP\* = RE 2020); local tomography as the selecting axiom (the reconstruction lineage: Hardy; Chiribella–D'Ariano–Perinotti; Masanes–Müller; Barnum–Müller–Ududec; Höhn) — M2's `ker R` localization is a *re-localisation* of that pre-existing insight onto the record substrate, not its origin; the Van Raamsdonk pinch picture and Ryu–Takayanagi as the target the weight-split halves (§5); Kleitman–Rothschild dominance.

**Owned (the reading and the proofs on this arena):** that a pre-geometric record substrate possesses `Γ ⪰ 0` *natively* — the Gram matrix of its own forced seals, under the §2.3 premise — so the almost-quantum envelope is structural rather than postulated; the joint-law split (skeleton transfers, additivity provably not forced — the telescope theorem); the two blindness mechanisms M1/M2 with the `ker R` localization and the canonicity criterion; the three closed escapes; the multi-scenario envelope from the single object; the downstream-sector sweep with the `σ ⊥ χ_AB` closure at three grades; the connectivity floor and the manifoldlikeness-pattern obligation; the graph-not-metric weight-split with its circularity audit; and the honest split *fence forced, filling free*.

**Adjacent, distinguished — the indivisible-stochastic family.** Barandes's correspondence reconstructs a quantum system as an **indivisible (non-Markovian) stochastic process** — Hilbert space a secondary tool, a configuration-space description available only at *division events*. The correspondence is deliberately kinematic: it does not say *what physically distinguishes* a division event from a non-division stretch. That is exactly the slot paper 1's forced survival skeleton `S = exp(−κχ)` / `S(nd) = S(d)ⁿ` occupies natively (a non-sealing stretch carries `S = 1`; the seal *is* the event that breaks divisibility, at the forced rate) — kin in spirit (multiplicativity along refinement is where indivisibility lives), a **separate construction**, no claim on the correspondence [POSITED as mechanism]. **The Doukas boundary keeps the mechanism claim honest:** Doukas (arXiv:2602.22095) is a *divisibility/emergence* result — Chapman–Kolmogorov divisibility of the lifted `B(H)` family is the decisive extra constraint (a CK-consistent CPTP family admits a Lindblad form; the off-diagonal phase is a *compressed carrier of history* not fixed by the transition kernels) — **not** a unistochastic-from-first-principles theorem; the seal reading is consistent with all three adjacent results and inherits none of their theorems. **The divisibility probe settles what the seal does and does not force** (receipts `p18_seal_divisibility.py` 26/26, `f6_unistochastic_record_blind_probe.py`): (1) the `q = 2` screen collapses the CK matrix to the scalar survival — the seal forces `S = exp(−κχ)` only, *not* matrix-level CK-divisibility (Doukas's constraint) and *not* the doubly-stochastic/unistochastic class (Barandes's assumed envelope); (2) **the unitary phase that would promote doubly-stochastic → unistochastic is the *same* `ker R` bit that fixes `χ_AB`** — a real orthogonal and a complex unitary give identical record moments `|U_ij|²` (gap `0` exact at `n = 2`; `1.5×10⁻¹²¹` for the phase-dressed `n = 2` pair; `1.3×10⁻¹⁴¹` for the rephased `n = 3` CKM pair), and a conjugate pair `U, U*` with *identical* records (gap `0` exact) carries opposite Jarlskog sign `±0.0521` (a real orthogonal has `J = 0` identically — the sign lives entirely in the complex preimage the records cannot read) — one bit, seen twice, not a new wall; (3) the seal forces a forward-stochastic kernel but *not* double-stochasticity (an explicit sealed semigroup carries a non-uniform stationary law), so the doubly-stochastic envelope is only partly seal-motivated. **The seal motivates the survival, never the kernel** (guard 5). Where a causal-locality Tsirelson derivation exists for the CHSH game (Barandes–Hasan–Kagan), the `2√2` is co-derived with no priority claim; the non-overlap is the multi-scenario envelope from one object plus the no-go — **and the comparison boundary is kept honest: nothing here proves the BHK framework *cannot* reach other scenarios or the broader envelope; that would be a claim about their unbuilt extensions. The defensible statement is only that this substrate already exhibits what their published CHSH-specific result does not.**

## 8. Honest scope and the opens

The envelope forcing is **[CONDITIONAL]** on the §2.3 operator-algebra premise (guard 2). The no-go is proved for degree-`(1,1)` commitments and the complex→real reduction; higher-word substrates and non-rebit foils are **[OPEN]** (§4.7, with the field-agnostic mitigation stated there). The entanglement *law* itself — which `χ_AB` the world realizes inside `Q̃` — remains the corpus's standing open (paper 1 §7): this paper proves the records cannot answer it, not that it has no answer; and §5 shows any realized answer sources the graph and the relative hop-metric, never the absolute unit. The un-forceability verdict is falsifiable in principle: a derivation of the quantum set from record-internal principles alone would refute M1/M2; three independent attempts (the transverse crossing, the global/Feynman sweep, the order-feedback path) failed in the specific, receipted ways recorded here.

## 9. Receipts

All in `code/`, re-run green 2026-07-01/02:

- `p6_transverse_nogo.py` (**43/43**; 8 [SOLVER-TOL]; parts: the single-chain template — `η_*`, `W_*`, the sympy-exact opposition, the firing root; M1 capacity blindness — the fixed-marginal solver, the `λ`-flatness, the U-shape; M1 circularity — the marginal-drifting root; M2 field blindness — qubit/rebit at Tsirelson, `I_σ` gap `0`; the `K`-counts and deficit `+1/0`; the envelope with the monogamy sharpness scan; cocycle triviality).
- `p6b_holonomy_phase_audit.py` (**21** — the holonomy escape: the moment polynomial verified non-vacuously, the gauge invariance, `Im z = 0` on shared seals, the phase-sensitive positive control).
- `p6c_alternative_capacity.py` (narrative verdict blocks — the capacity-definition robustness sweep and the golden-crossing counterfeit; conclusions cross-checked in `p6`).
- `p6d_sign_magnitude.py` (**20** — the sign involution; the `E_cl` continuum; the drifting balance).
- `s_probe_qtilde_minus_q.py` (**26/26** — the explicit `Q̃ ∖ Q` witness with the full-level-2 nesting and its self-contained `⪰ Q` certification; the four-sector push incl. the `CHSH(P*) = 1.339` block; the `P*` re-completion margin `+1.1×10⁻⁷` and the native-optimum floor `−1.3×10⁻⁹`).
- `p4a_joint_clicklaw.py` (**24** — the `σ_AB` decomposition, the telescope blind to `I_σ`, the `χ_AB = I_σ` identity gap `0`, the exp-shape transfer, the `I_σ = 0` counterexample, the free-complement continuum; "no spacetime, metric, or `s²` used as input anywhere").
- `nonadditive_entangling_complement.py` (the `E_cl` complement: Bell `ln 2`, product `0`, nearest-product uniqueness).
- `p4b_connectivity_metric_split.py` (**5** — the weight-split: content weight-`0` vs distance weight-`+1`; the pinch; hop-ratio invariance to `10⁻¹⁰⁰`; the `−log I` additive-gauge contrast).
- `t3_tsirelson_derivation.py` (**16** — the IC static core: the four properties, the chain-rule residual `3.63×10⁻¹²²`, the facet saturating at `E = 1/√2`; the operational RAC lever *not* entailed).
- `c3_covariance_chi_ab_probe.py` (**15** labeled checks, printed twice per run — the covariance gates: no-signaling as a Lorentz scalar; microcausality = on-state commutation, witnessed on the free algebra and on prime dimension 3; the ceiling gate on the facet surrogate; the lever localized to `CHSH > 2√2`).
- `s_geom_chi_sweep.py` (**20/20**, zero solver-tol — edge sign-blindness; the honest pinch; quantum vs almost-quantum identical at equal weight; the structurally-`R`-invariant, non-vacuous manifoldlikeness diagnostics; KR dominance; the tripartite closure).
- `s_order_feedback_probe.py` (**18/18**, zero solver-tol — the `σ ⊥ χ_AB` closure at three grades; the adversarial surrogate non-vacuity).
- `p18_seal_divisibility.py` (**26/26**) / `f6_unistochastic_record_blind_probe.py` (the divisibility probe and the Jarlskog `ker R` tie-in of §7).
- `f5_oi_pi_consistency.py` (**20/20** — the sequential skeleton silent on outcome-independence: the transverse content is genuinely a separate axis; home exposition paper 1 §7.2).

Precision: `p6`/`p6b`/`p6c`/`s_probe` at `dps = 140`, `c3` at `130`, `f5`/`s_geom`/`s_order` at `120`, `p4a`/`p4b`/`nonadditive` at `120–140`, `t3` at `≥ 100`; the cancellation-heavy invariance gaps (`~10⁻¹⁴¹`) all live in the `dps = 140` receipts; never float64 for them. Three receipt-reading notes. (1) `η_*` is *defined* as the root of `C − J`, so any "two-formula `|J − C|` agreement" is a root-finder residual, not independent corroboration; the load-bearing content is `W_*`'s value plus the sympy-exact sign opposition. (2) **Surrogate vs witness:** `c3`'s Tsirelson-facet surrogate (min-eig `−2.2×10⁻¹⁶` at `CHSH = 2√2`, used to exhibit the boundary; one benign SCS "solution may be inaccurate" status on its level-2 CG-`I3322` solve (`0.2509397`), flagged; its CHSH-cap solve returns clean status within `3.2×10⁻¹⁰` of `2√2`) is a *different object* from the spine's `P*` re-completion (`+1.1×10⁻⁷`, in `s_probe`) — easy to conflate, distinct. (3) The strict `Q̃ ⊋ Q` statement rests on NGHA 2015 + Pál–Vértesi (level ≥ 4) + Slofstra 2019 as field-established, corroborated (not re-derived) by the receipts' own nesting; all SDP digits are solver-tolerance `~10⁻⁹` with the PSD/Gram logic exact.

## References

**This series.** Paper 1 (the click-law and its premise ledger §2 — the seal observables §3.1, the dense/sparse regimes §3.3–3.4, the capacity crossing §4.2, the firing law §4.3, the grading trichotomy §4.4, the Bell-address guardrail §7.2); Paper 3 (the walls classification: this paper's no-go is its TENSOR leg, and the mechanism contrast of §4.1 its group-level form); Paper 4 (gravity/continuum: the KR gate and the covariance layer's Tier-B gates); Paper 5 (matter: the interacting GW flow the unbuilt-two-party locus of §4.7 waits on); Paper 6 (phenomenology). Ledger: `LEDGER.md`.

**Frozen log.** v7 Papers 4 (the joint click-law and the graph-not-metric split, both absorbed here), 6, 12 (the mechanisms and the envelope), 13 (the global sweep and the two new constraints), 16 (the positioning content this consolidation absorbs).

**External.** B. S. Tsirelson, Lett. Math. Phys. **4**, 93 (1980); L. J. Landau, Found. Phys. **18**, 449 (1988). M. Navascués, S. Pironio, A. Acín, PRL **98**, 010401 (2007); New J. Phys. **10**, 073013 (2008). M. Navascués, Y. Guryanova, M. Hoban, A. Acín, Nat. Commun. **6**, 6288 (2015). K. F. Pál, T. Vértesi, Phys. Rev. A **82**, 022116 (2010). B. Toner, F. Verstraete, quant-ph/0611001 (2006). D. Collins, N. Gisin, J. Phys. A: Math. Gen. **37**, 1775 (2004) — the `I3322` inequality (introduced by M. Froissart, Nuovo Cim. B **64**, 241 (1981)). M. Pawłowski, T. Paterek, D. Kaszlikowski, V. Scarani, A. Winter, M. Żukowski, Nature **461**, 1101 (2009) — Information Causality. M. Navascués, H. Wunderlich, Proc. R. Soc. A **466**, 881 (2010) — macroscopic locality (`Q¹`). T. Fritz et al., Nat. Commun. **4**, 2263 (2013) — local orthogonality. W. K. Wootters, *Local accessibility of quantum states*, in *Complexity, Entropy and the Physics of Information*, ed. W. H. Zurek (Addison-Wesley, 1990) — the rebit local-tomography deficit. W. Slofstra, Forum Math. Pi **7**, e1 (2019); Z. Ji et al., *MIP\* = RE*, arXiv:2001.04383. J. P. Jarrett, Noûs **18**, 569 (1984) — the `LC = PI ∧ OI` decomposition. L. Hardy, quant-ph/0101012; G. Chiribella, G. M. D'Ariano, P. Perinotti, PRA **84**, 012311 (2011); L. Masanes, M. Müller, New J. Phys. **13**, 063001 (2011); H. Barnum, M. Müller, C. Ududec, New J. Phys. **16**, 123029 (2014); P. A. Höhn, Quantum **1**, 38 (2017) — the reconstruction lineage. W. van Dam, quant-ph/0501159; G. Brassard et al., PRL **96**, 250401 (2006) — the communication-complexity collapse above Tsirelson. M. Van Raamsdonk, Gen. Rel. Grav. **42**, 2323 (2010); S. Ryu, T. Takayanagi, PRL **96**, 181602 (2006). D. J. Kleitman, B. L. Rothschild, Trans. Amer. Math. Soc. **205**, 205 (1975) — the partial-order dominance count. M.-O. Renou et al., Nature **600**, 625 (2021); M.-C. Chen et al., PRL **128**, 040403 (2022); Z.-D. Li et al., PRL **128**, 040402 (2022); T. Hoffreumon, M. P. Woods, arXiv:2603.19208; A. Maioli, E. Curado, J.-P. Gazeau, arXiv:2604.19482; F. Moradi Kalarde, X. Xu, M.-O. Renou, arXiv:2604.07425. J. A. Barandes, arXiv:2507.21192; J. A. Barandes, M. Hasan, D. Kagan, arXiv:2512.18105; M. Doukas, arXiv:2602.22095 — the divisibility/emergence boundary of §7.
