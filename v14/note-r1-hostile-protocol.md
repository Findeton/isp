# R1 — HOSTILE REVIEW PROTOCOL (FROZEN)

**Frozen:** 2026-08-09, v14 ledger #6.  **Object:** the R1 delivery as
committed (paper `2c75772098eb`, code `7c04500ec178`, output
`624ab5236ea1`, receipt `2130abe58b9e`; commit 473a841), verified
byte-identical (#5).  **Pin:** `v14/note-r1-continuum-pin.md`
(`27c9f1144ffa`).  **Base:** the R0 founding pin + the #4 erratum.
**Reviewers:** three, independent, no file writes outside
`v14/review-r1-{operator,effectus,instrument}.md`; scratch-only
recomputation; no imports from the unit's code; read-only git.

## Kill-shots (binding on all three)

**K1 — THE COPYING QUESTION (the verdict's meaning).**  The measured
mechanism is disjoint block addition with per-block counts equal on
the tail.  Adjudicate: given b₀ = blocks+1 and isomorphic blocks, is
the constancy of N_coh density and b₂ density an *algebraic
consequence of the rule* — i.e., a forced clause that #208 demands be
a disclosure, not a measured stabilization?  Separate precisely: what
is measured (that the blocks ARE isomorphic? that the rule copies?)
from what is forced given that.  Then the sharpest form: does ANY
registered or natural quantity that is NOT per-block-copied stabilize?
If the honest content is "the rule is a copying rule, measured," say
so and propose the verdict wording that says it.

**K2 — THE FAMILY'S LEGITIMACY.**  The family is heterogeneous before
A₃ (two of four steps non-functorial, different generator rules).  Is
gluing three constructions into one declared "family" faithful to the
pin's §2, and does the verdict survive restriction to the homogeneous
tail alone (A₃–A₅ + the L₁₀/L₁₂ probes)?  The stabilization window
K=3 covers exactly the homogeneous members: is the claim then "3
members + 2 probes of one rule," and is that enough to say
STABILIZES, or does the qualifier need the tail's homogeneity named?
Recompute the trajectory table independently at every member.

**K3 — THE INVARIANT DEFINITIONS.**  Are the five registered
invariants well-defined on their declared domains?  b₂ density's
denominator vanishes under scramble (recorded UNDEFINED) — is the
invariant partial, and does the verdict's derivation handle the
partial case by measurement or by accident?  Is the normalized
spectral profile's eigenvalue-1 anchor chain actually confirming
I2's wall (0 ∈ spec(I−E), never unit-circle) or a weaker surrogate?
Is N_coh density per drawn pair the pin's quantity or a variant?
Verify the DIMENSION_PROFILE exclusion of the raw estimator was
honored everywhere.

**K4 — ATLAS-RELATIVITY.**  The atlas (charts = labels; cells =
(block, FULL/REAL); drawn iff unique transport element) is the unit's
own declaration, disclosed in §12.  Measure the sensitivity: at least
one alternative declared atlas over the same arenas (reviewer's
choice, stated precisely), same five invariants at the tail — do
37/27 and 20/37 move?  If they move, the verdict qualifier must carry
the atlas declaration; adjudicate whether §12's disclosure suffices
or the P*-precedent (v13 TOP) demands the sweep in-unit.

**K5 — INSTRUMENT.**  (a) Full independent falsifier audit: every
declared mutant's death claim tested against the census; injections
of the bug classes the gates claim to catch (at minimum: a
per-invariant corruption the declared mutants do NOT cover, the
X-REAL-EMPTY forced clause's gating status per #208, the G33
denominator self-exclusion, the boundary-parity degenerate probe's
legitimacy as a death certificate, cache-exercise gating).  (b) The
verdict string: rebuild it from the receipt alone; every qualifier
segment (values, window, cap, functoriality count, gateway) must be
derivable and flippable.  (c) The 28 scale-threshold anchors and the
I6 width cross-anchor: trace to the pinned bytes.  (d) Paper↔output↔
receipt: every number in the paper matched to its receipt path; false
numbers reported by value.  (e) Two-run byte-identity reproduced.

## Standing rules

Grades: ACCEPT / ACCEPT-WITH-FIXES / REJECT with the era's meanings.
Findings ranked, each with severity, the measured evidence, and —
where a repair is definite — the repair's exact form.  Zero numerical
claims without recomputation; every recomputation counted.  The
reviews are frozen as delivered; disagreements between reviewers are
the adjudicator's to resolve, not smoothed by the reviewers.
