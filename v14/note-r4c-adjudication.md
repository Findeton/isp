# PAPER-22 (R4C — STATISTICS AS A MEASUREMENT) — JOINT ADJUDICATION (K1+K2+K3)

**Adjudicator:** the orchestrator.  **Date:** 2026-08-11 (v14
ledger #217).  **Inputs:** review-r4c-operator.md
(b5d0e3b0dfcc, AWF, 88 recomps), review-r4c-effectus.md
(df7e9d1b209c, AWF, 65 recomps + ~302k instances),
review-r4c-instrument.md (9a3b43680b6e v3, AWF, 96 recomps,
byte confirmed three ways).  Object at 4f89135: 1b4ac134e727
/ deb0c1c83a76 / 45866a3ed5e3 / 5c058006db78; pin
162553b03ca9.

## VERDICT ON THE VERDICT

**R4C-STATISTICS-BOTH-ADMITTED SURVIVES ALL THREE SEATS —
UPGRADED FROM CENSUS TO THEOREM.**  Zero false computed
numbers across ~249 seat recomputations.  Three seats
independently converge on the same strengthening: the
selection mechanism (leak ⟺ non-monomial) is a ONE-LINE
THEOREM for every unitary (K2's 240 witnesses; K1's 3364
out-of-family composites; K3's set equality) — the delivered
"whose split is not forced" waiver is FALSE and dies.  The
ceiling remains a DECLARATION, genuinely open (the 6-and-12
argument dies with it — the registers were link-sweep bounds,
not site capacities); OCC (user-adopted, #209) carries the
forcing question with K2's exact conditional.

## THE HEADLINE CORRECTION (K1 M1, governing)

The delivered "everything that can see the doubly-occupied
channel sees it exactly where the substrate already
interferes" rested on a VALUE-MULTISET comparison the paper
never disclosed.  Entrywise, the two shapes' defects differ
at 1764/3364 — exactly the two-excitation nonzero set —
witness (C000, C012).  **The repaired law is SIMPLER and
stronger: the shapes differ cell-by-cell exactly where both
legs are non-monomial; multiset-level discrimination is
confined to the 588.**  The triple identity survives at BOTH
granularities (K3's 43-element coin-level set; K2's
588-pair-level set) — the repair states both with their
granularities labeled.

## SEAT-CONFLICT RULINGS

None substantive.  K2's "circulant-stratum scope + 360
undischarged rows" and K1's entrywise correction compose (the
360 rows' discrimination is TAKEN in repair; the scope stamp
stays).  K3's phantom-consumer finding is not contradicted by
any seat.  The one-parameter fibre of the derivation law
(K1 MINOR: Δ⊗Y+X⊗Δ holds equally) is adopted as a disclosure,
not a defect.

## REPAIR ORDERS (binding; one worker; the four files in place)

- **T1 (K2 M1 ≡ K1 M2 ≡ K3):** the A2 waiver corrected — the
  leak ⟺ non-monomial THEOREM gated for every unitary (the
  240-witness census + the 3364 composites in-code); "whose
  split is not forced" deleted; the head gains the
  THEOREM stamp.
- **T2 (K1 M1):** the discrimination section rewritten — the
  entrywise law computed and gated (1764; joint-zero 1600);
  the (C000,C012) witness rendered; the multiset statement
  confined to the 588 WITH the gate text quoted; both
  granularities of the triple identity stated (43-element
  coin-level; 588 pair-level).
- **T3 (K2 M2):** the 6-and-12 sentence replaced by the
  positive gate (27 stage declaration keys, 0
  occupancy-shaped); the ceiling stated open.
- **T4 (K2 M3):** the UNIVERSAL indistinguishability theorem
  stated, stamped, and gated (B(U⊗U)=B(U)⊗B(U);
  Δ₂ = Δ⊗X + Y⊗Δ; X⊗X=Y⊗Y ⟺ X=Y; the telescoping to all n)
  — with K1's one-parameter fibre disclosed beside it.
- **T5 (K2 M4):** the 360 overlap-2 rows' shape
  discrimination TAKEN (raw_overlap compares); the
  circulant-stratum scope stamped.
- **T6 (K2 M5):** the braid/topology stamp (S₂ ≠ B₂; d=2; no
  anyon claim possible or made).
- **T7 (K2 M6):** the ten-row choice inventory published
  (two rows verdict-determining).
- **T8 (K3 M1):** the twelve phantom consumers eliminated —
  every named consumer/sealed_at_gate exists in the ledger
  (G-ANCHOR-CONSUMERS per the sibling's pattern); **the
  no-particle-naming wall gets its instrument** — the
  inverted-wall injection must die.
- **T9 (K3 M2):** claim licensing rebuilt — occurrence-
  counted (multiset) claims; the 39 sha-digest members
  removed from the licensing pool; the forged-second-fence,
  every table perturbation, and the inverted headline must
  die (E-22).
- **T10 (K3 M4 + MINOR-5):** the seal window closed at ALL 30
  seals; restore() failures fatal; os.replace AFTER the
  disk-vs-seal check; INJ-11 must die.
- **T11 (K3 M3):** G-CACHE-UNPOLLUTED bound to all 7 caches'
  full field sets with a real (object-corrupting) falsifier.
- **T12 (K1 M3 + the MINOR sweep):** the 39 symmetric values
  stamped and gated (G-DEFECT-RATIONAL reaches them); the
  ordered-sector forcing disclosed; A4's measurement
  published; the fixed-point gate strengthened to the claim
  (both ceilings' restrictions compared as objects); the
  velocity attribution corrected (failures exactly (2,2) and
  (6,6); the (4,4) cells excluded by measurement); §7's
  mechanism sentence replaced by the 1536-row sweep's honest
  statement; the two false §11 prose claims corrected;
  E-24 stamps on the delivered fractions (birth-fair,
  lifted); K3's twelve MINORs as written.

## SUCCESSOR REGISTER (adopted)

OCC (user-adopted #209) owns the ceiling-forcing question
with K2's exact conditional and the one-way asymmetry as its
organizing principle.  FCK inherits: the ceiling as a priced
fibre-2 item (until OCC rules), the leak mechanism at every
n, the labelled-sector theorem at every n, and the sector
count that does NOT lift.  SPT inherits the
defect-as-discriminator WITH its blind set and the
independent spectral channel.  The contact handle stays
one-operator-scoped.

## AFTER REPAIR

Worker re-runs plain ×2 byte, selftest, the full mutant sweep
(incl. the new wall/licensing/seal falsifiers), the K3
injection replays (all 12 previously-surviving must die),
hostile argv; my battery; then paper-22 → TERMINAL —
unlocking the OCC pin.
