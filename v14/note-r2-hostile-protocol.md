# R2 — HOSTILE REVIEW PROTOCOL (FROZEN)

**Frozen:** 2026-08-09, v14 ledger #14.  **Object:** the R2 delivery as
committed (paper `cc78e9373bbe`, code `36a10324b93f`, output
`cb8493a13c39`, receipt `7b128499b246`; commit c129ab5), verified
byte-identical (#13).  **Pin:** `v14/note-r2-manifold-pin.md`
(`76d42dfbc900`).  **Reviewers:** three, independent; single-file
writes (`v14/review-r2-{operator,effectus,instrument}.md`);
scratch-only; no imports from the unit; read-only git.  NOTE: the R1
repair worker is concurrently editing `v14/paper-01-*` and
`v14/code/r1_*` — those files are OFF-LIMITS and their current state
is NOT review evidence.

## Kill-shots (binding on all three)

**K1 — THE CIRCULANT QUESTION (is the census a theorem?).**  The
delivery reports the non-complete component is always the transport's
regular orbit with "missing pairs = those no cyclic window of length c
covers," and locality vanishes at wide windows.  Derive the exact
statement: for a cyclic transport of order n with sliding coset-union
windows of width c, the component's overlap graph should be a
circulant graph with a computable completeness threshold and a
computable cycle rank.  Prove or refute: (a) non-completeness holds
iff a stated inequality in (n, c); (b) the measured b₁ values
(0, 6, 12; 1, 8; 0, 2 across the 14 rules) equal the circulant
formula's output; (c) the entire 14-of-109 census is then derivable
from the grid declaration without running the atlas.  If yes: per
#208 the census clauses reclassify (measured = the grid + the drawing
relation; forced = which rules are local), and the verdict must say
so.  The locality itself is NOT killed by this — but its epistemic
label changes.  Say exactly which.

**K2 — THE MOTIVATION QUESTION (the RSQ precedent).**  R1's panel
demoted "found squares" because every survivor came from arbitrary
relabellings selected by the property under test.  Apply the same
standard here: the 14 locality rules were *declared* into the grid by
a mechanism chosen because it produces locality (R2-A's recipe).  Is
there a MOTIVATED selection principle — anything in the substrate's
own inherited structure (the click law, division events, the one law
D(ψ)=[P,u(ψ)], the ladder, the record layer) that singles out sliding
windows or a particular width — or is "locality" here purely an
artifact of instrument design?  Answer with measurements where
possible (e.g., is any of the 14 rules' cell structure expressible
from I6's own declared objects — Σ-orbits, the ladder subgroups, the
defect law — without new choices?).  Then state what the verdict may
honestly claim: LOCALITY-DECLARABLE (an existence statement about
atlas space) vs LOCALITY-FOUND (a statement about the substrate).
This is the unit's deepest question.

**K3 — THE STANDARDS.**  Verify the link profiles, the 1-of-80
circle-link, the INCONSISTENT dimension readings, and the local
dimensions 1/2/3 tracking window width.  Then write the successor
requirement: what, concretely, would a rule (or arena) have to
measure to EARN a consistent dimension — and is that reachable
inside this grid class or provably not (another circulant
consequence?).

**K4 — SCOPE.**  Adjudicate the worker's transport decision (two
transports as arena data vs the pin's singular wording; the strict
T7-only sub-census as the literal reading); the word-generated
Σ-lattice scoping; the 5 REFUSES; the B₂ persistence claims
(components double exactly — verify, and check "copies locality"
against a case where blocks could interact); the coherence corollary
(N_coh = N identically — forced disclosure status per #208, and what
it means that the cocycle condition is free at this drawing
relation).

**K5 — INSTRUMENT.**  42 gates vs 14 mutants: audit coverage (which
gates have no falsifier?  the era's standard: every must-pass gate
falsified by some mutant or explicitly waived with the waiver
censused).  Re-run the R1 injection classes against THIS unit
(name↔value swap, typed segment, table-cell corruption vs
render-from-gated-object, filtered recount, qualifier append) — the
#10 engravings bind it at birth; verify they hold under attack, not
just under declared mutants.  Anchor audit: 5 anchors is thin — the
unit reads I6 values by JSON path; are the READ VALUES anchored
(byte-hash + value equality), and does corrupting one die?  Paper↔
output↔receipt: every number traced; false numbers by value.
Two-run byte-identity reproduced.  The verdict string: rebuild from
receipt alone; every one of the nine segments flippable.

## Standing rules

Grades ACCEPT / ACCEPT-WITH-FIXES / REJECT.  Findings ranked with
severity, measured evidence, exact repair where definite.  No
numerical claim without recomputation; count recomputations.
Reviews frozen as delivered; reviewer disagreements are the
adjudicator's to resolve.
