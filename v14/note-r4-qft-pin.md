# v14 R4 — THE QFT RUNG: THE DEFECT ON THE STAGE (pin)

**Frozen:** 2026-08-09, v14 ledger #47, per the charter (R4) and the
R3 adjudication's formalized ruling (#46).  **Paper number:**
`paper-10` — deliverables `v14/paper-10-defect-on-the-stage.md`,
`v14/code/r4_defect_stage_exact.py` + `_output.txt` + `_receipt.json`.
**Discipline:** RUNBOOK complete, all eight 2026-08-09 engravings
(incl. #46 no-unanchored-runtime-inputs) with injection-falsifiers at
birth; exact arithmetic (cyclotomic/rational — the v12 fields).

## 1. Sources (hash-pinned; pedigrees carried)

- **THE SEED — Δᴮ:** `v12/paper1-composition-defect.md`
  (`81bdab5673fb`, TERMINAL-PUBLISHABLE, the user's never-split
  order standing) + `v12/paper1_code/exact.py` (`8e90f6435922`) —
  the composition defect, the corpus's terminal quantum object:
  indivisible families, the defect algebra, Born readouts.
  Reference-only for recipes; REIMPLEMENT.
- **THE STAGE:** I7 (R0 row, `542b8735daf0`) — the lattice, sites,
  links, translation structure (d=2, L ≥ 4 gated per the R2
  terminal; anchor the locality facts to the R2 terminal receipt
  `08b2140f46ae`).
- **THE GATE DESIGN:** `v14/note-r3-adjudication.md` (hash at
  freeze) — the realization-census gate (mandatory) and FR1–FR11
  where applicable.  R3's own receipt is NOT anchored (it is
  mid-repair); the gate is self-contained on this unit's generators.

## 2. The question (falsifiable, the programme's first QFT-shaped)

**Does a spatially structured indivisible family on the record stage
exhibit a nonzero composition defect — and does the defect carry
excitation structure?**  "Spatially structured" means: the family's
transition matrices act on lattice configurations, moves are local
(adjacency-respecting), and the family is translation-covariant.
"Excitation structure" means measured two-point content and
transformation-type classes — not a field-theory claim; the word
"field" appears only in "free-field ANALOG."

## 3. Registered measurements

1. **The family construction**: minimal spatially structured
   indivisible families built from the v12 recipe on lattice
   configurations (occupation labels on sites; declared local-move
   generators; the division-event times declared).  The construction
   choices run through the CHOICE INVENTORY (the era standard) — the
   declared family is data (§15), its freedoms classed and printed.
2. **THE DEFECT CENSUS**: Δᴮ for composed segments, exactly as v12
   defines it, now with spatial structure — computed per generator
   pair and per separation; the Markovian sub-family MUST return
   Δᴮ = 0 (the v12 control, gated); the defect's dependence on
   locality (local vs non-local generators) measured.
3. **TWO-POINT STRUCTURE** (the free-field-analog benchmark):
   equal-time and composed-time correlations across lattice
   separation, exact; decay/periodicity profiles per family; the
   translation-covariance of the two-point table gated.
4. **TRANSFORMATION-TYPE CLASSES** (the Wigner move at finite
   scale): orbits of the family's generators under the lattice
   translation group (+ the declared point symmetries); the class
   census printed — the first excitation-type table of the
   programme.  No "particle" language: the paper says
   transformation-type.
5. **THE REALIZATION-CENSUS GATE** (mandatory, from #46): every
   generator declares which fields it transports (front / register /
   geometry-analog labels as applicable to this unit's
   configurations); defects enter verdict segments only at the
   maximal declared transport; the gate's classifications printed.
6. **The state-motion check** (FR2's spirit): whether any measured
   coefficient/defect moves with the prepared state vs being
   background — measured, labelled.

## 4. Verdicts (first-class, composable, computed in-gate,
independent comparator, all segments flippable)

`R4-DEFECT-PRESENT-<structure: the two-point + class-census
qualifiers; the locality dependence; scope>` / `R4-DEFECT-ABSENT-<the
Markovian-collapse characterization>` / `R4-BLOCKED-AT-<named fact>`.
Scope honesty: finite lattice, declared family, no continuum claim,
no interacting-theory claim beyond the composed-segment defect
itself.

## 5. Controls, falsifiers, discipline

Markovian control (Δᴮ = 0, gated both directions); scrambled-lattice
negative control (translation covariance must break measurably); a
non-local generator control (locality dependence measured, not
assumed); era-minimum mutants + the five verdict classes + the
defect-zeroing class (the R3 Y1 lesson: defect gates bound to the
value census) + path/verbatim drifts per pinned row; never-falsified
census with verified waivers (#34) from delivery one; no unanchored
runtime inputs (#46).  Two plain runs byte-identical; full
compliance sweep with computed statuses.
