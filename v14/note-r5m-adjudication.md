# PAPER-23 (THE CONFIGURATION MEASURE) — JOINT ADJUDICATION (K1+K2+K3)

**Adjudicator:** the orchestrator.  **Date:** 2026-08-11 (v14
ledger #192).  **Inputs:** review-r5m-operator.md
(7740b5d01f07, AWF, 61/61 exact), review-r5m-effectus.md
(eb14a5a64374, AWF, 55 recomps 0 moved),
review-r5m-instrument.md (0890efbf3071, AWF, 33/47 injections
caught).  Object at 33da839: 9249dda1c0a3 / f7de59960fe6 /
8ee12d000bad / 1e794bd7f5fb; pin e5e09f65f83b.

## VERDICT ON THE VERDICT

**MEASURE-DECLARATION-REQUIRED SURVIVES ALL THREE SEATS** —
zero delivered numbers moved across ~159 seat recomputations
(61+55+41; both 85/86- and 89/88-digit integers
character-exact twice).  **TWO DEMOTIONS, both honesty-scoped:
(i)** "forced shut" is CENSUS-RELATIVE — the stationary-measure
candidate is absent and currently unownable (no pinned dynamics
acts on configurations; the missing ingredient is
IRREDUCIBILITY, which no symmetry supplies); **(ii)** the
DERIVE arm of the two-way design never could have fired (typed
free_items; G-FIBRE-PRICED refuses transitive arenas) — the
head stands because this arena is genuinely non-transitive,
but the instrument must be made honest.  **ONE PROMOTION:** the
census gains its own organizing principle, stated by K2 and
verified on all 8 rows — *a canonical measure exists exactly
where something acts transitively.*

## SEAT-CONFLICT RULINGS

1. **K2's seal-manifest flag vs K3's totality measurement:**
   K3 governs — totality closes exactly (42 = 31+10+1); the
   exempt key is DECLARED, not silent.  No defect.
2. **The title sentence:** K1 and K2 killed it independently
   and identically (a 2×2 doubly stochastic kernel on a
   domino, not a distribution over 16 states).  The licensed
   replacement is K2's: **the asymmetry is LAW-vs-NOTHING** —
   the substrate hands a kernel (dynamics), never a measure;
   pressed for one it fixes none (stationary simplex dim 15
   diagonal / 14 others, to be computed in-code).
3. **The 27/130 tie-break:** found by K1 and K2 independently
   (#91).  Both attaining sets will be printed.
4. The monomial⇔no-quantum biconditional (my #181 ledger
   gloss) is DEAD (K2: 128 interfering defect-free coins);
   the licensed cross-unit sentence is
   same-species-different-modality, citing only the repaired
   instruments.

## THE R5 REGISTER (adopted verbatim from K2)

**NO erratum; NO number annotation.**  One NOTE recording a
STRENGTHENING: R5's declared uniform window is now DERIVED —
it IS the chart-fixed locus (chart-declaration-relative,
655360/655360).  R5 never made a probability reading of
576/640 (swept: zero probability-words).  The corpus-wide
caution is engraved this ledger as E-24.

## REPAIR ORDERS (binding; one worker; the four files in place)

- **Q1 (K1 M1):** the derive arm made REACHABLE — free_items
  measured, never typed; G-FIBRE-PRICED handles transitive
  arenas (0-simplex ⟹ the head emits MEASURE-DERIVED); the
  synthetic-transitive arena becomes an in-code CONTROL ARM
  demonstrating the derive head is emittable; a mutant kills
  the reachability.
- **Q2 (K1 MINOR-5 ≡ K2 MAJOR):** the Born segment rewritten —
  kernel/law language throughout; the LAW-vs-NOTHING licensed
  sentence; the stationary simplex dims (15/14) computed
  in-code and gated; the verdict word unchanged.
- **Q3 (K2 rows 1+8):** the transitivity criterion STATED as
  the census's organizing principle (all 8 rows labeled as
  instances, gated); maxent given a census row (gated:
  collapses to the unconstrained null, priced); the stationary
  candidate added as a NAMED-ABSENT row —
  NOT-CENSUSABLE-NO-PINNED-DYNAMICS-ON-CONFIGURATIONS with the
  irreducibility requirement — and "forced shut" rescoped to
  census-relative in head and §1.
- **Q4 (K1 M2):** the two FOUND-candidate controls named in
  paper and receipt (CRYSTAL/DOUBLE-GRID(3,2),
  DECLARED-PROBE/CAYLEY-AT-I7); each measured dying at the
  site-count blade; the one-dictionary gate strengthened to
  test the enumeration.
- **Q5 (K3 M1):** the head derivation made honest — the
  comparator re-derives the head from primitives (a true
  second route); K3's one-character-corruption injection dies.
- **Q6 (K3 M2):** the withholding gate made deep and
  name-robust — recursive over all receipt depths; AST
  extended to lambdas and assignments; MUST_NOT gains the
  wilson/expectation prose words (#125-normalized); the
  slash-adjacent-numeral regex fixed; K3's W7/W8 injections
  become mutants.
- **Q7 (K3 M3):** the §7 null-dependence table rendered from
  the receipt and gated — all six fractions bound (it is the
  unit's own not-innocuous claim).
- **Q8 (K3 M4):** registry-totality closed against mut(var)
  and comparison forms.
- **Q9 (K3 M5):** the choice inventory binds ROWS, not counts
  (#87); the 4th verdict-determining row (candidate-sources
  count, per K2) added.
- **Q10 (K2 row 7):** the chart-group fibre corrected — the
  translations-only contrast (2 link orbits; locus 409600)
  measured in-code; the disclosure: the positive result is
  carried by the direction relabelling.
- **Q11 (K1+K2 tie-break):** both 27/130-attaining sets
  printed; the label carries a declared tie-break or none.
- **Q12 (MINOR sweep):** docstring 8 candidates; late_gates
  derived; 58 vs 56 labeled (registry vs snapshot); the
  unreachable waiver class removed; the exit-0 conventions
  disclosed in USAGE; the interfering-pairs sentence corrected
  (131072/262144); the no-chart-reversal fact gated with a
  mutant; PV-W3-DIVISIONS consumer prints the right name; the
  head price segment gains the word INVARIANT; K2's prose
  slips (pin's-plus-five; four-classes→three); the four
  load-bearing fragments' claim gates located, not
  presence-anywhere; the weld-2-silence scope sentence gains
  the L-PARITY stamp (bipartite iff L even — fires at odd L).

## ENGRAVING BOUGHT (appended to RUNBOOK this ledger)

- **E-24 (measure-relativity of counts; bought by K2's [0,1]
  demonstration):** *no count becomes a probability without a
  declared measure.*  A headline fraction over a configuration
  space is measure-relative; the admissible mass of paper-23's
  headline sets spans exactly [0,1] over invariant measures.
  Units must either declare the measure with the count or
  stamp the count as counting only.

## SUCCESSOR REGISTER

The STATIONARY-MEASURE UNIT (new; unownable today): requires a
declared dynamics ON configuration space + an irreducibility
argument; registered as the QCD road's next gate.  The
L-PARITY lead to R6 (the weld-2 blade fires at odd L).  The
Born-fibre question re-posed at plaquette grain (K2 row 8).
The action/coupling gaps (R5's register) remain behind the
measure.

## AFTER REPAIR

Worker re-runs plain ×2 byte, selftest, the full mutant sweep,
the K3 injection replays (the 14 delivered ones must die),
hostile argv; my battery; then paper-23 → TERMINAL.
