# GDL (paper-25) — THE GRAVITATIONAL-DECOHERENCE LAW — PIN (FROZEN)

**Frozen:** 2026-08-11, v14 ledger #205.  **Authority:** THE
STRATEGIC PLAN (#193, Wave A); prediction-ledger entry (c).
**Parents (terminal):** paper-20 at #204 (paper 4824d190af73,
code 72e7b299f66e, receipt 55273f6b6068 — the coupled machine:
the walk, the emission law, the update semantics, the frozen
control, G-COIN-INVARIANCE with all four hidden coins, the
exit census) and the Γ-iteration terminal at 2895a9a (the
law-native normaliser k₁ = q/M).  **Historical shape citation
(cite-not-inherit):** the corpus's V5P2 gravitational-
decoherence arc (Diósi–Penrose reconstruction; the
Gaussian-onset falsifier; onset mechanism-blindness) — shape
only, no number and no claim inherited.  **Unit:**
paper-25-gdl; code v14/code/gdl_exact.py (+ output + receipt).

## THE QUESTION

The substrate identity — ONE event is simultaneously a record,
a decoherence event, and a metric increment — makes a
quantitative relation between decoherence and geometry growth
POSSIBLE.  This unit measures whether the relation EXISTS and
whether it is FORCED:

1. **THE DECOHERENCE FUNCTIONALS (declared, fiber priced,
   every member run):** at minimum — (a) the site-basis
   inverse participation of the Born menu (the paper-20
   observable, inherited); (b) the off-diagonal coherence mass
   in the site basis (ℓ₁-shaped, exactly computed); (c) the
   record-menu/Born-menu divergence (the Δᴮ-shaped candidate).
   None privileged; the fiber priced.
2. **THE METRIC-GROWTH FUNCTIONALS:** the division-event
   emission rate (per step, per site, per branch — exact,
   from the coupled machine's own emission law); the n-growth
   profile.
3. **THE RELATION, exactly:** per step at the declared
   horizon, is each decoherence functional a FUNCTION of the
   record-growth functional (per-step, per-site, per-branch
   resolution)?  Fitted forms are BARRED — only exact
   relations (equalities, monotone dominations with exact
   witnesses, or exact functional dependence gated by
   equality) may be claimed.  Partial relations reported as
   partial with the failure set censused.
4. **FORCEDNESS (the two-way core):** the relation censused
   across the COIN FIBER (all four hidden coins + ±Grover, the
   paper-20 invariance machinery inherited verbatim) — a
   relation invariant across the fiber is FORCED-AT-THIS-
   ARENA; a coin-relative relation is priced.  THE FROZEN
   CONTROL is mandatory: on the frozen stage records never
   grow — the decoherence functionals' behavior there is the
   it-can-differ arm (a relation that holds identically frozen
   is NOT a gravitational-decoherence relation).
5. **THE PREDICTION ROW:** if a forced relation exists, it is
   entered in the structural-prediction ledger in
   substrate-native form (counts and exact rationals — NO SI
   numbers, NO experimental-value claims; the DP citation
   stays shape-only).

## OUTCOMES (pre-registered)

`GDL-LAW-FORCED-<the relation>` /
`GDL-LAW-COIN-RELATIVE-<spread>` /
`GDL-PARTIAL-<the failure census>` / `GDL-DECOUPLED` (no
functional relation — first-class and informative) /
`GDL-BLOCKED-AT-<object>`.

## STANDARDS

The full era per HANDOFF-PROMPT.md §4 + E-22 + E-23 + E-24;
description-stamps on every quantum-character claim; exact
arithmetic; the four walls; declared windows licensed; the
derived+rendered head; the total seal; failing runs write
nothing; byte ×2.  Candidate readings until adjudication.
