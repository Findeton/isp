# THE CONFIGURATION MEASURE (paper-23) — R5'S OPENING OBLIGATION — PIN (FROZEN)

**Frozen:** 2026-08-11, v14 ledger #174.  **Authority:** R5's
terminal successor register (#156): "the confinement-shaped
follow-on REQUIRES A CONFIGURATION MEASURE FIRST — its pin's
opening obligation."  **Parent:** R5 terminal (commit 987cd73;
the 640-coin family; the link-indexed configurations; the
alternating-profile fingerprint).  The Γ-iteration terminal
(2895a9a) supplies the transition layer's own stochasticity as
the candidate derivation source.  **Unit:** paper-23-measure;
code v14/code/r5m_measure_exact.py (+ output + receipt).

## THE QUESTION: DERIVE OR DECLARE

Does the substrate DERIVE a probability measure on the gauge
configurations (the link-indexed coin assignments of R5's
declared family), or must one be declared?

1. **THE DERIVATION CENSUS:** the candidate sources, each
   measured at the RSQ standard (zero free items = derived):
   (a) the transition layer's own law — histories carry exact
   probabilities (Γ's kernels); does the history measure
   PUSH FORWARD along any pinned correspondence to a measure
   on coin configurations?  (b) the counting/uniform measure —
   the null, carrying no information (declared, fiber
   printed); (c) any invariance-characterised measure — is
   there a measure UNIQUELY fixed by the measured symmetries
   (the chart group; the family covariance identity; the
   gauge action) — uniqueness gated, not asserted.
2. **IF ANY SOURCE DERIVES:** compute the first Wilson-loop
   EXPECTATIONS under it (the R5 plaquette loops; exact) —
   REPORTED AS EXPECTATIONS ONLY.  **The confinement word
   stays behind its gate: this unit builds the measure; it
   makes NO area-law, string-tension, or potential claim**
   (the R5 must-nots inherited verbatim; the prose gate
   paper-effective per #125 with markdown normalization).
3. **IF NONE DERIVES:** `MEASURE-DECLARATION-REQUIRED-<the
   minimal fiber>` — the honest form, pricing exactly what a
   declared measure must add (the BRG dichotomy pattern).

## OUTCOMES (pre-registered)

`MEASURE-DERIVED-<source;uniqueness>` /
`MEASURE-DECLARATION-REQUIRED-<fiber>` /
`MEASURE-BLOCKED-AT-<object>`; the Wilson-expectation segment
only in the DERIVED case.

## STANDARDS

The full era per HANDOFF-PROMPT.md §4; the R5 walls verbatim;
description-stamps; exact arithmetic; declared windows
licensed; the derived+rendered head; the total seal.
Candidate readings until adjudication.
