# SEC (paper-32) — THE SECTOR/ATLAS UNIT: GLUED WELDED WORLDS — PIN (FROZEN)

**Frozen:** 2026-08-11, v14 ledger #258.  **Authority:** the
user's order (#254: "register it and investigate it when you
see fit") — the orchestrator schedules it here.  **The
question's ancestry:** R1's copy theorem (disjoint sectors are
sterile — coupling must be SHARED PARTICIPATION); R2's
sliding-window locality; paper-0's registered cross-chart
open; the 9-works/16-failed weldability pattern.  **Parents
(terminal):** paper-19 at 559e162 (the forced dictionary and
weld machinery), paper-21 at #232 (ef4a8c35a0c4 — the
detector, the ladder law, block quantisation).  **Read-only
context:** paper-30/LOR's delivered extended-carrier lesson
(2369290: SITE ← ACTOR ⊕ PAIR; abstract ≠ dictionary) —
cite-not-inherit until its terminal.  **Unit:** paper-32-sec;
code v14/code/sec_exact.py (+ output + receipt).

## THE QUESTION (the user's: can the universe be n actors per
sector?)

Two welded 9-actor sectors, glued by k SHARED actors — does
geography compose?

1. **THE ARENA FAMILY (k a declared axis, every member run):**
   two copies of the R=3 welded arena (paper-19's (1,1,1))
   sharing k boundary actors, k ∈ {1, 2, 3} at minimum (k=3 —
   one full tripartite class — the lead); the union carrier
   (18−k actors); the gluing's combinatorial type declared
   (which tripartite classes align — the fiber priced).
2. **THE UNION'S DICTIONARY:** the weld detector on the
   union — does a FORCED dictionary exist for the glued world
   (readings, fibers, free items — the full paper-19/21
   standard)?  Reading-stamped; the bare-union and
   extended-carrier readings both run (the LOR lesson: the
   carrier may need pairs).
3. **THE GLUING FIBER:** where the two sectors' geometries
   meet — do the shared actors' links carry consistent counts
   from both sides (the compatibility census); what freedom
   the gluing itself carries (priced).
4. **CURVATURE OF THE GLUING:** a division event SPANNING
   sectors (one actor from each side beyond the shared set —
   if the grammar admits it; if it does not, THAT is a
   finding) — does it write geometry in both sectors; is
   there geometric content AT the seam that neither sector
   carries alone (the q-form of the union vs the direct sum —
   the defect measured exactly).
5. **THE FORCED-OVERLAP QUESTION:** is any k selected by
   weldability (the union welds at some k and not others —
   the first test of the cardinality-selection hint)?  The
   9-works/16-failed pattern cited as motivation, not
   evidence.
6. **THE STERILITY CONTROL (two-way, mandatory):** the k=0
   disjoint union run as the it-can-fail arm — R1's theorem
   predicts NO new structure; the union detector's behavior
   there gated (the contrast is the unit's license to speak).

## OUTCOMES (pre-registered)

`SEC-COMPOSES-<k; the union dictionary; the gluing fiber>` /
`SEC-SEAM-CURVATURE-<the defect>` /
`SEC-NEVER-WELDS-<the obstruction — geography irreducibly
single-sector, a theorem>` / `SEC-K-SELECTED-<the forced
overlap>` / `SEC-BLOCKED-AT-<object>`.

## WALLS

No cosmological reading (sectors are pinned arenas, not a
universe narrative); the new-places/expansion language barred
except as measured structure; description-stamps; E-22 +
E-23 + E-24 in full; the four walls inherited.

## STANDARDS

Full era per HANDOFF-PROMPT.md §4; exact arithmetic; windows
declared and licensed (the union censuses may need windows —
the saturation-schema lesson: re-prove any exhaustiveness
license per arena); all seals at value-close; read-back
before replace; derived head with de-twinned comparator
routes; #91 off-tree; failing runs write nothing and promote
nothing; byte ×2.  Candidate readings until adjudication.
