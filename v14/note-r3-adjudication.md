# R3 — JOINT ADJUDICATION (effectus #39, operator #40, instrument #45)

**Inputs:** the three frozen reviews, the frozen delivery (paper
`00850cc796d0`, code `bbcc9a1aa7de`, output `d54142292980`, receipt
`1c8beb16c8a2`), protocol `c575340216fc`, pin `a2ac89687a65`.
**Standing:** three ACCEPT-WITH-FIXES; ~197,000+ recomputations +
~10⁶-value sweeps; **zero false numbers by value** (two false
mechanism claims); the verdict rebuilt byte-identically by two
reviewers.  Resolved grade: **ACCEPT-WITH-FIXES**.

## 1. The settled physics (cross-confirmed)

**A. The census is analytic** (three derivations: residual = (W−B)Ω;
CLOSES ⟺ W ≡ B; the coefficient class a pure function of (rule,
readout)).  Per #208 the census clauses carry FORCED labels.  **What
is genuinely measured:** the spanning hypothesis (full rank 460/460 —
measured by the operator, NOT by the delivery: to be measured
in-unit), the sector arithmetic, the residual magnitudes, the {D,H}
half, and the literal-composition agreements.

**B. The positive claim restated at true strength.**  32/120
decomposes as 16 control + 16 diagonal-sector identity; **no cell
exists where a non-metric rule yields a site-varying metric
coefficient**.  The HDA correspondence: relation (I) **exact in form,
analogical in status** (a central extension with a background
coefficient — the coefficient cannot move with the state); relation
(II) partial; relation (III) contentless at this arena (abelian
subfamily only); the three relations not simultaneously realizable
here (99.3%/99.75% of {H,H} generators admit no D-TOT generator).
The honest name: **fixed-background covariance with GR's bracket
form** — not the HDA.

**C. The defect's final anatomy.**  Absolute: **IN-CONSTRAINT at 0 of
73,872 — no declared-expressible realization closes {D,H} strictly**
(and it survives the reverse bracket order and non-constant bijective
tangential fields).  Relative: the OUTSIDE status dissolves at D-FULL;
**the covariance theorem** (2,064/2,064): conjugation by full
transport maps H_g[N] to H_{S·g}[S·N] — **the surviving obstruction is
that the record does not transport: the geometry is a fixed
background**.  At maximal transport the residue is
**curvature-supported** (the resisting cells are exactly the
inhomogeneous records).  §6's "transport, not curvature" reverses at
the full realization.  Cross-unit: this is the same missing object as
CR-A's geometry-update law — the fourth naming.

**D. Adjudicator's own account:** the pin's RIGID branch was
unreachable by construction (closure defined as equality with the
metric-inserted generator) — my second pin-design error of v14, same
class as R1's gateway; owned.  Also mine: the "candidate
deviation-from-GR object" framing for the register defect, retired by
F4 (the sixth such retirement).

**E. Instrument.**  M1 — a NEW disease class: the delivery reads
unanchored, mutable `v14/LOG.md` and a verdict segment is a function
of it (one appended sentence flips the verdict at exit 0).  Ten
surviving injection classes (defect-zeroing inverting all four §6
findings; post-gate receipt corruption; L-sweep truncation;
shared-component route corruption; two self-compares; class flips;
row drops).  The waiver ledger: 27 of 29 falsifiable (all killed),
false waivers by name, a stale falsifier map.  Two false mechanism
claims (the architecture-B misattribution — truly B-all, 36/108; the
nonexistent "degenerate probe" typed as True).  Denominator
inflation ×3; duplicate rules (A-axis≡B-axis, A-chart≡B-chart,
A-insert≡A-notransport).

## 2. Binding repair orders (R-R3-1 … R-R3-10)

1. **Verdict rewrite**: keep the DEFECT head; segments recomposed to
   carry — the FORCED labels on the analytic census; the measured
   spanning hypothesis (in-unit, gated); the restated positive claim
   (fixed-background covariance; the correspondence map per relation);
   the realization census (27 realizations; the absolute 0-of-73,872;
   D-FULL absorption; the curvature-supported residue); the
   covariance theorem as a gated in-unit measurement; scope
   qualifiers (rule-scoping, realization-scoping, the duplicate-rule
   disclosure → 10 distinct rules or the distinct rules implemented).
2. **The rigid branch made reachable**: closure re-defined against
   the declared generator basis with the residual measured against
   it (a rigid outcome that can win — FR9); re-censused.
3. **M1 eliminated**: no unanchored runtime input — the L-gate
   fractions anchored to the R2 terminal receipt (path-value), the
   LOG read removed; per the new engraving (§4).
4. **The ten injection classes hardened** (defect gates bound to the
   value census — Y1 dies; post-write render gating; L-sweep
   cell-completeness vs a derived arena count; independent
   comparators for gap_matrix / RECOVERY / CONTROLS (#219); class
   census guards; row-count derivations) — each with a new mutant.
5. **The waiver ledger rebuilt** at the #34 standard (the 27
   killable gates get declared falsifiers — the reviewer's kills
   adopted; the six file-byte and twenty path-value waivers
   corrected; the tautology gate rebuilt or demoted; the stale
   falsifier map regenerated in-gate).
6. **The two false mechanism claims fixed** (B-all, with X05 and the
   verdict segment corrected; the degenerate probe implemented via
   the constant lapse — a real zero-defect probe — replacing the
   typed Boolean).
7. **Denominators honest** (the convention sweep printed at its true
   685-probe base; 62-of-238; the covariance non-vacuity count).
8. **FR1–FR11 recorded** in the paper as the successor requirements
   (verbatim from the effectus review), with FR3 stated as the
   unit's own conclusion: the arena is a declared fixed background
   and the paper does not use the unqualified name "HDA".
9. **Paper rewrite** single-threaded; the finding = the analytic
   structure theorem + the covariance theorem + the curvature-
   supported residue + the realization census; no correction
   narrative.
10. **The R4 ruling formalized** (from the effectus recommendation,
    adopted): R4 seeds from Δᴮ (v12 paper 1); the register defect
    becomes R4's mandatory **realization-census gate** (every
    interaction generator declares which fields it transports; its
    defect enters a verdict only at the maximal declared transport)
    plus a background detector; R3's numbers are the gate's controls.

## 3. RUNBOOK engraving (this adjudication)

- **§14 addendum (no unanchored runtime inputs):** every input a unit
  reads at run time is either a hash-pinned artifact (byte +
  path-value anchored) or the unit's own frozen declaration; reading
  mutable repo state (ledgers, STATUS, working-tree files of other
  units) is forbidden — a verdict segment that depends on an
  unanchored read is an ungated verdict (the R3 M1 class, found by
  one appended ledger sentence flipping a verdict at exit 0).

## 4. What R4 inherits (consolidated)

The arena: I7's lattice with R2's locality/dimension facts and R3's
covariance machinery (the full-transport conjugation law).  The seed:
Δᴮ — v12 paper 1's composition defect, the corpus's terminal quantum
object, a defect of composition (which is what an interaction is).
The gate: the realization census.  The requirements: FR1–FR11 where
applicable, chiefly — coefficients that move with the state; the
record must transport (or the unit declares its background); one
algebra, one realization; a rigid outcome that can win.
