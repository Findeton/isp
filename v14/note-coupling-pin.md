# THE COUPLING UNIT (paper-20) — "QFT REQUIRES GRAVITY" AS A THEOREM — PIN (FROZEN)

**Frozen:** 2026-08-11, v14 ledger #173.  **Parents (both
terminal):** the gravity law (Γ-iteration, #161, commit
1b11f43-era artifacts at 2895a9a: paper `5c1df50673d4`, receipt
`42255f50328a`) and the weld (paper-19, #172, commit 559e162:
paper `50bb81e67942`, code `f95a26a1764b`, receipt
`dfea664f2408`).  **Authority for the scope:** the paper-19
adjudication (#168).  **Unit:** paper-20-coupling; code
v14/code/coupling_exact.py (+ output + receipt).

## THE QUESTION

On the welded arena — the R=3 saturating record (n ≡ 1, det 3/4,
posdef 9/9) with the FORCED dictionary [ACTOR→SITE |
CO-DIVISION-PAIR→LINK | DIVISION-COUNT→n_ℓ(x)] — run a quantum
dynamics whose interactions EMIT DIVISION EVENTS THROUGH THE
CONFIRMED LAW, updating the very counts the dynamics propagates
on.  Then gate three things:

- **G-CONSISTENCY:** the coupled object is well-defined — the
  walk's unitarity and the law's column-stochasticity COMPOSE
  (exactly, at every step of a declared horizon).
- **G-NONTRIVIALITY (two-way, the frozen control mandatory):**
  back-reaction measurably shifts a declared observable set
  (the dispersion/defect census adapted to this lattice) against
  the FROZEN-STAGE control — the same dynamics on never-updating
  counts.  Identical ⟹ the coupling is inert (an honest
  first-class outcome).
- **G-REQUIREMENT (the theorem, two-way):** exhibit a CLOSURE
  CONDITION of the quantum dynamics — a conservation,
  consistency, or composition identity internal to the quantum
  side — that FAILS on the frozen stage and PASSES exactly when
  the counts update per the law (or the reverse, equally
  reportable).  The frozen control is the it-can-fail arm; a
  mutant demonstrates each direction.

## DECLARED DATA (§15; every fiber priced)

1. **The quantum dynamics:** a unitary coin walk ON THE WELDED
   RECORD LATTICE — the 9 sites and the 3 co-division link
   classes of the R=3 arena (adapt the R4b family shape; the
   coin alphabet derived from the parents where derivable,
   declared where not; the fiber printed).  Exact cyclotomic
   arithmetic.
2. **The emission rule:** which walk events count as
   INTERACTIONS and emit division events — DECLARED, bound to
   the law's LAW-NATIVE normaliser (k₁ = q/M is
   partition-independent — G(h,1)=M(h) by the terminal
   condition — and therefore transportable; its transport to
   this arena must be GATED, not assumed).
3. **The update semantics:** counts increment per emission; the
   walk's next step reads the updated n (the metric-dependence
   of the walk DECLARED: which of its ingredients consume n).
4. **The horizon:** a declared finite number of coupled steps
   (sedimentary — one pass, no returns, per the every-cap
   theorem; NO regeneration assumptions anywhere).

## HONEST OUTCOMES (pre-registered)

`COUPLING-CONSISTENT-AND-REQUIRED-<the closure identity named>`
/ `COUPLING-CONSISTENT-NOT-REQUIRED` (the loop composes but the
closure holds frozen too — an honest negative) /
`COUPLING-INERT` (no measurable back-reaction) /
`COUPLING-INCONSISTENT-<witness>` /
`COUPLING-BLOCKED-AT-<object>` — **including, first-class,
BLOCKED-AT-THE-LAW-TRANSPORT**: the law was confirmed on the
(A,B) 2-actor carrier; if its kernel provably cannot be carried
to the 9-actor welded arena, that gap is the finding (named,
measured, with what a transport would require).

## SCOPE (binding, from #168)

**THIS UNIT REACHES A RECORD, NOT YET A LAW OVER RECORDS** — the
welded record is unsplittable (split fiber 0 at all 27
intervals); papers 04/06/09's refinement laws are empty on it;
the head carries this scope row verbatim.  The R=4 arena
(G-FLAT reachable at 276 quadruples) is the registered
follow-on where a law-over-records may become posable.  No
continuum claim; no claim beyond the declared horizon;
cosmological readings barred (the four walls inherited); every
quantum-character claim DESCRIPTION-STAMPED (the
carrier-relativity discipline); the support-overlap law cited
where it binds.

## INHERITANCES (cite, do not re-derive)

The forced dictionary and the 72-stratum (paper-19 terminal);
the law at its values with the LAW-NATIVE normaliser
(Γ-iteration terminal); the price law (R=2 budget-binds → R=3
matching-binds); sedimentary-by-theorem-at-every-cap; the
description-property sentence; the support-overlap law (R5
terminal); the fragility contrast (crystallinity ⊥ geometry).

## STANDARDS AT CONSTRUCTION (the full era, all 21 engravings)

The #82 CLI; the head DERIVED (the comparator types its own
templates and re-derives every segment from primitives) AND
rendered-from-receipt with string equality; **the TOTAL seal
(#119 + totality + the vouching layer + chained
ledger/transcript)**; #20 WITH FENCED-BLOCK coverage and
polarity, in-run; #34 WITH REACHABILITY; #125 with whitespace
AND markdown-prefix normalization; #62 floors + consumer
binding; #87 per-object; #91 off-tree AND git-less tested (root
paths at __file__; no subprocess; the v10 tie-break gate stated
where a v10 layer is driven); #24 counts computed; failing runs
write nothing; byte-reproducible ×2.  Between delivery and
adjudication every headline reading is a candidate reading —
maximally so for the REQUIREMENT gate.
