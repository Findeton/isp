# R6a — HOSTILE REVIEW PROTOCOL (FROZEN)

**Frozen:** 2026-08-09, v14 ledger #28.  **Object:** the R6a delivery
as committed (paper `af5b7f26e427`, code `ea914c6b55aa`, output
`a04b97d2b7bc`, receipt `022c3f488a93`; commit b0087a9), verified
byte-identical (#27).  **Pin:** `v14/note-r6a-refinement-grammar-pin.md`
(`a22582f67168`).  **Reviewers:** three, independent; single-file
writes (`v14/review-r6a-{operator,effectus,instrument}.md`);
scratch-only; no imports from the unit; read-only git.  NOTE: the R3
construction worker owns `v14/paper-03-*` and `v14/code/r3_*` — those
files are OFF-LIMITS and not evidence.

## Kill-shots (binding on all three)

**K1 — THE COBOUNDARY THEOREM (the mechanism).**  The unit's center:
"no record's counts are a coboundary — the datum a split needs is
exactly the datum the record does not carry."  Verify the statement
and its proof at the pinned scope.  Then press its universality: is it
a theorem about the GRAMMAR (every admissible record — strictly
positive counts, periodicity, the min-cycle-sum bound) or about the
declared record family?  Could an admissible record within I7's
grammar carry coboundary counts (construct one or prove none exists)?
And is the theorem genuinely the *mechanism* — does it imply the
split-fiber counts — or a parallel restatement?

**K2 — THE CLASSIFICATION HUNT (the verdict's hinge).**  The verdict
rests on four freedoms classed genuinely-free.  A missed stabilizer or
missed forcing declaration flips NO-MOTIVATED-SPLIT.  Hunt structures
that could fix each: for THE-SPLIT — symmetries of the pinned objects
beyond chart-equivariance (the Σ action, lattice reflections, the
front's own structure), extremal/variational selections derivable from
pinned declarations, the dynamics itself; for FREE-TRANSVERSE-LINKS —
restriction-style constraints from d-dimensional consistency; for
NEW-FRONT-VALUES — the front's semantics (committed events at a site
that "already existed" unlabelled?).  Recompute the fibers (the
19,683..~1.26e21 span; the equivariant min 3; the 1-of-361
lattice-forced with (2,2,2) outside the family; the 64-witness
infinite family).  This is the mirror of R2's motivation hunt — run it
with the same exhaustiveness.

**K3 — THE DYNAMICS CLAIMS.**  Verify: the commutation-defect census
(7,112 of 11,088; the identically-zero 3,976 as positive control); the
exact closed form at coarse images under the left lift (D(ι(x)) = the
whole coarse drag); lift-relativity at images; rule-blindness.  Then
the interpretive hinge: the "dynamics-FORCED front lift" (the
count-weighted interpolation, non-integral at 30/81, n|n₁ at 0/207).
DERIVE what forces it: is the count-weighted interpolation the unique
lift compatible with a pinned declaration (name it), or one natural
choice among several (in which case "the dynamics forces an
inadmissible value" overclaims and the honest statement is "this lift
class fails")?  The distinction moves a verdict segment.

**K4 — THE MOVE CENSUS AND THE HEAD.**  Verify: HYPERPLANE's block
(the 3 cut diagonals, 2 decompositions, the 16-completion disagreement
at 12 — and whether an incidence rule is genuinely underivable from
the pinned sources); SINGLE-INTERVAL's refusal (the translation-forced
cycle argument); the d=3 coverage gap (27/216, no body diagonal); the
count-1 floor (3 of 9 records unrefinable — including G-FLAT) and its
correct scope; the iteration ceiling (⌊log₂ min count⌋ = 2 attained)
and inventory growth.  Adjudicate the head composition: the pin's §4
allows mixed outcomes — should BLOCKED-AT-GRAMMAR-SOURCE appear in
the head for the hyperplane branch beside NO-MOTIVATED-SPLIT, or is
the delivered per-class table the right carrier?  Also: the R1-copy
control's discriminating-failure claim (different failure mode, not
just failure) — verify both fingerprints.

**K5 — INSTRUMENT.**  (a) The NEW ANCHOR KIND — 10 verbatim-text
anchors quoting load-bearing grammar sentences: audit the mechanism
(what dies if the quoted sentence drifts? does the anchor bind the
sentence to a USE, or just to existence?); this is its first
deployment and the era adopts or rejects it here.  (b) Coverage: 48
gates vs 34 mutants; never-falsified 20 of 48 ALL-WAIVED — verify
every waiver's stated forcing is genuine (a false waiver is a false
gate claim).  (c) The five verdict injection classes + the birth
compliance of all five 2026-08-09 engravings — test under attack, not
by declaration.  (d) The split-fiber count recomputed in-gate (#24
compliance) — verify the independent recomputation is genuinely
independent.  (e) Paper↔output↔receipt: every number traced (the
programme's four false paper numbers all lived in prose; the unit
claims 15/15 prose claims rendered — verify).  (f) Two-run
byte-identity reproduced; repo hashes unchanged after all work.

## Standing rules

Grades ACCEPT / ACCEPT-WITH-FIXES / REJECT.  Findings ranked with
severity, measured evidence, exact repair where definite.  No
numerical claim without recomputation; count recomputations.  Reviews
frozen as delivered; disagreements are the adjudicator's to resolve.
