# GDL (PAPER-25 — THE GRAVITATIONAL-DECOHERENCE LAW) — JOINT ADJUDICATION (K1+K2+K3)

**Adjudicator:** the orchestrator.  **Date:** 2026-08-11 (v14
ledger #230).  **Inputs:** review-gdl-operator.md
(f3db1f15c9ea, AWF, 348 recomps), review-gdl-effectus.md
(0043ca529ec1, AWF, 243 recomps), review-gdl-instrument.md
(1565d161354f, AWF, 613 recomps + 111 executions).  Object at
4c85ca4: e98003841378 / 81595d600575 / 39128fafc7bf /
b87016e96285; pin fe9533371046.

## VERDICT ON THE VERDICT

**THE HEAD MOVES: GDL-PARTIAL →
GDL-DECOUPLED-AT-THE-GRAVITATIONAL-BAR** (K2's three legs
adopted: textual — the pin's G scopes "functional relation"
to gravitational and gravitational_count = 0; instrumental —
the delivered selector could NEVER emit DECOUPLED (both
conditions gate-forbidden), so the truer word lost by
construction, the paper-23 disease's second instance;
measured — the frozen control earns the identical PARTIAL
word at 26 of the 28 coupled cells).  **The zero is FORCED
twice over:** the single exact cell relates a record-blind
decoherence functional to a record-blind growth functional
(K1's G5 measurement) — it could not have failed frozen from
either side.  Zero false computed numbers across ~1,204 seat
recomputations; the blindness mechanism verified AS ALGEBRA
(K1: 4,374 symbolic identities) and STRENGTHENED (K2: the
correct hypothesis is BLOCK-DIAGONALITY, not site-uniformity
— any arena, graph, dimension, horizon; site-dependent-coin
demonstration in hand).

## THE NEW OBLIGATION (K2's discovery, ordered into repair)

**THE MOD-3 CHANNEL:** the coin reads the record only mod 3,
and the max-cell ladder [2,2,3,3,4] wraps (4 ≡ 1) INSIDE the
delivered horizon — a third geometry→matter channel the unit
never analyzed.  V4 measures it.

## SEAT-CONFLICT RULINGS

None substantive.  K1's M2 ≡ K3's M5 (the denominator), found
independently.  K1's per-pair 36× measurement and G5-blindness
are adopted in-code.  K3's Row-2 affirmation (the second pass
genuinely independent) stands beside its M1 (the one number
it never recounts) — both true, the repair closes the gap.

## REPAIR ORDERS (binding; one worker; the four files in place)

- **V1:** the head → GDL-DECOUPLED-AT-THE-GRAVITATIONAL-BAR;
  the selector made genuinely two-way (DECOUPLED emittable —
  a control arm demonstrates it, the paper-23 pattern); §13's
  false availability sentence corrected; the comparator
  re-derives the new head from primitives.
- **V2:** the frozen-control cell table published (1/38/6;
  the 26-of-28 identical-word fact); the 11 total-failure
  cells stamped as failures, not PARTIAL.
- **V3:** the blindness theorem restated at the correct
  hypothesis (block-diagonal ⟹ site-marginal record-blind,
  any arena/graph/dimension/horizon; site-uniformity only
  for the off-diagonal Theorem B, which makes
  co-occupancy-necessity a theorem); the site-dependent-coin
  demonstration and K1's symbolic verification in-code; the
  exportable statement with exact hypotheses.
- **V4:** THE MOD-3 CHANNEL measured — the residue reading
  named; the wraparound events censused at the delivered
  horizon; whether any functional distinguishes
  congruent-but-different records gated (a THIRD-CHANNEL
  segment in the head, whatever it says).
- **V5:** the occupancy predicate corrected (the described
  one fires at 3, the measured one at 4 — both named, all 6
  coins); §7's false universal scoped (D2 moves); the D1
  denominator fixed (54,770); §5's inverted framing
  corrected (a 92% failure, not success).
- **V6:** the prediction row rescoped — the threshold (the
  invariant) into the sealed digest; the co-occupancy COUNT
  disclosed coin-relative (16/24/33); falsification
  conditions stated.
- **V7:** K1's two gifts adopted in-code: the per-pair
  mechanism census (0/452,745 at overlap ≤1;
  840,889/1,518,975 at co-occupancy) and G5's own
  record-blindness (the doubly-blind exact cell stated as
  the frozen exclusion's sharpest ground); the ε=p row
  strengthened per K1.
- **V8:** the kernel-check classes (1,479,176 with its
  split) stamped definitional or given falsifiers (the
  paper-20 precedent).
- **V9 (K3 M1):** the VACUOUS clause gains
  failing_classes == 0; nonsingleton_classes recounted by
  the second pass; K3's restamp injection dies.
- **V10 (K3 M2):** verdict fences by TRUE multiset; all 8
  tables rendered as claims (the §5/§2/§9 swap injections
  die).
- **V11 (K3 M3):** E-23 made semantic (description-vs-code;
  the inverted-description injection dies); G-SWEEP-BOUND
  binds actual executions (the fabricated sweep dies).
- **V12 (K3 M4):** the coverage allowlist cleaned (the
  self-description tokens and the falsifier's own needle
  out; the planted-numeral injection dies).
- **V13 (sweep):** the choice inventory gains the bar-scope
  row (verdict-determining); K1's six MINORs (incl. the ℓ₁
  denominator's rational zeros) and K2's remainder as
  written.

## SUCCESSOR REGISTER (adopted)

**GDL-1a** (the two-time rate functional — cheaper, first)
then **GDL-2** (metric-consuming coins) carrying K2's no-go:
raising consumption strength m cannot rescue the blind
functional AT ANY m — the live route is breaking
block-diagonality, which re-gates paper-20's transport; the
no-go has publishable shape.  **SIG inherits Theorem A as a
pin constraint** (no fiber on site-marginal observables) —
already adopted at #224, binding at pin time beside the
reachability floors.  PER-R inherits the delay-law shape,
not the counts.  The structural-prediction ledger gains the
separation ladder AT ITS RESCOPED FORM (threshold invariant;
count coin-relative) on terminal.

## AFTER REPAIR

Worker re-runs plain ×2 byte, selftest, the full mutant
sweep, ALL K3 injection replays (the restamp, the forged
fence, the table swaps, the inverted description, the
fabricated sweep, the planted numeral — every one dies),
hostile argv, off-tree byte ×2; my battery; then paper-25 →
TERMINAL.
