# WELD 2 — THE REFERENT-WELD SCOUT REPORT (of record)

**Date:** 2026-08-10.  **Question (ledger #80):** does any pinned
source tie the spatial lattice (I7's sites/links/counts) to grammar
objects (histories, actors, deliveries, division events)?  **Two
independent read-only agents:** the scout (direct-read seed
inventory) and a full-corpus sweep (every lattice/site/adjacency ↔
grammar claim, both directions).  **Zero repo writes by either.**
Scout read at HEAD `8b89460`; all load-bearing hashes re-verified
against the tree at commit time by the orchestrator (all match).

## THE VERDICT (convergent, both agents)

> **NO-SEED-AT-THE-CARRIER — THE LATTICE IS A FRESH DECLARATION;
> SEED-EXISTS-AT-THE-COUNT-SEMANTICS-ONLY.**
>
> The corpus pins **what the counts count** — grammar division
> events, tied to renewals by v11 paper 0 §4's `[POSIT]`
> (`37a428321f46`) — and pins **nothing about where**.  Sites,
> links, direction labels, the chart group and the entire record
> family are authored literals with no grammar provenance anywhere.

The sweep's independent formulation agrees on all three legs:
positive grammar→geometry derivations exist ONLY in the v10
crystal/sky line; everywhere quantitative (HA/I7, R3, R6a, R6b′)
the site set, links and direction labels are DECLARED and the
papers say so about themselves (HA §12 item 3; review-ha-effectus;
note-r3-adjudication); the impossibility/open register is uniform —
GW1-NOT-RUNNABLE, GW2-BLOCKED-AT-1, CRA-BLOCKED-AT-STATIC-GEOMETRY,
R6BP-TRANSPORT-UNMOTIVATED 0-of-5, "combinatorial spatial position,
an open problem" (v6_task2d `047dc6a7bb2e`), D29's vacant spatial
row, D20's "3+1 Lorentzian emergence from records not derived".

**The absence is instrument-carried, not prose:** `MUT-CRA-BRIDGE`
in `gmain_exact.py` — a mutant whose entire content is asserting
the grammar↔lattice map — DIES at T6-CRA (gmain output
`b2b45be500b7` line 349).  Weld 2's negative control is pre-built.

## (a) THE SEED INVENTORY (ranked)

1. **SEED-1, the count semantics — the one real tie; type-level,
   load-bearing.**  HA §3.1–3.2 (`f286ba10d2d9`): n_ℓ(x) = the
   number of division events in the record interval between x and
   x+ℓ; v11 paper 0 §4 `[POSIT]`: division events ARE the renewal
   events.  Already forced two results in gates: R6a's
   ADDITIVITY-972-OF-972 (`paper-04`, `dfa5090f26b1`) and CR-C's
   merge-summing.  Carries the *what*, never the *where* — R6a's
   own mechanism verdict:
   THE-RECORD-CARRIES-INTERVAL-TOTALS-NOT-EVENT-POSITIONS.
2. **SEED-2, the crystal line (D60/D63/D66/D67) — the corpus's only
   lattice-shaped objects living ON grammar records.**  Brick-wall
   and grid records FORCED by the layer's own menus (65 events / 8
   ring actors, exactly-one-candidate; 46 events / 9 actors no
   refusal — `19e50d34635f`), sprinkling-grade homogeneity,
   width ceiling k·b ≤ k² saturated at k=3..6.  DISQUALIFIED as a
   seed as-is (three pinned reasons: the actor arrangement is a
   declared blueprint; the direction set is a construction choice —
   d47 pin `baf226e4f006` §3 verbatim; the transfer runs through
   paper 29's named MISSING MAP, d59 `98336bd48ae2`).  **Residual
   value: the natural FOUND-side positive control** — a grammar
   record that demonstrably carries a lattice.
3. **SEED-3, the actor-width law (D47/D66/D67)** — the only
   measured grammar→geometry relation: sky size bounded by ACTOR
   WIDTH, depth-independent (`bc8b13f7fbca`).  A bound, not a map.
4. **SEED-4, the birth-tree/Ulam adjacency — DEAD as seed, live as
   warning**: the one adjacency the grammar hands over is lineage,
   and paper 20 (`3e9768fd1d2b`) measured its induced geometry
   chain/tree-like, never lattice-like (no cell within 10.4σ of a
   manifold band; the two dimension estimators genuinely disagree).
5. **SEED-5, "record adjacency" on GW1 §1.2's permitted list
   (`6f825ef6e1ce`)** — swept: NEVER DEFINED anywhere; its only
   instantiation is HA's declared link set, priced by HA itself
   (§12 item 3: "declared, not derived … the mitigation cannot
   certify the frame").

**The sweep's addition — U4 names the route.**  v11 paper 0 §7
(`37a428321f46`): **"the division events of a crystal form a
crystal"** — rebuild the crystals with renewal-only records; the
renewal sublattice is periodic; "order + number = geometry" becomes
posable on a GENERATED carrier for the first time.  U4 is pinned
and NEVER RUN.  It is the corpus's sharpest lattice-from-grammar
statement and the strongest form of the FOUND-side control.

## (b) THE DEAD LIST (pre-registered; a weld-2 unit may not re-run)

- **D1**: R6b′'s five interval-identification candidates, all
  UNMOTIVATED (`006f96aaa2ff` §9; free items 6/5/1/4/1).  Binding
  mechanism facts: C1 equates a division-event count with a
  grammar-event count (type error); **C3, the least-unmotivated
  (1 free item), is degenerate** — admissible only at count 1
  (29/201 intervals, every one with split fiber 0).
- **D2**: BRG-EMPTY-AT-CARRIER (`371e38742059`) — the group-action
  census EMPTY both directions; the component dictionary explicitly
  NOT POSED (§14 open 4).  Weld 2 is the question BRG named and
  declined.
- **D3**: order-only spatial instruments — GW1 §2's table grades
  every one (spectral frame unoriented; MDS scored after
  Procrustes; all on sprinkled backgrounds).
- **D4**: v12's Γ objects — arena-free by pinned declaration
  (r6bp pin `17111fd19022`).
- **D5**: naive 9↔9 (CRYSTAL-2D's 9 actors ↔ I7's 9 sites) — R2
  measured d=2, L=3 DEGENERATE; L≥4 is a measured requirement
  (`paper-02` §11).

## (c) THE TWO ROUTES (the user's call, per #80)

**Route A (scout-recommended) — THE CARRIER CENSUS**, a BRG-shaped
unit at the level BRG declined, posed to return EMPTY well.  Pin
rows R1–R7 as drafted in the scout's report: the two carriers as
declared data; the count semantics as the only motivated ingredient
(any candidate must send division-event sets to link counts and
reproduce ADDITIVITY-972-OF-972; needing a position inside a leg =
dead on arrival); the RSQ choice inventory with motivated ⟺ zero
free items; two-way gates with the **crystal records as the
FOUND-side positive control** (per HA §14 requirement 3: a
predicate that cannot return its other value is not a measurement)
and the generic 2-actor walk as the negative; the sharpened
no-smuggling gate (at I7 record↔metric is an invertible linear
re-encoding — G28, det 2 — so the test is WHICH FUNCTION of grammar
data the map computes, not what it can see); §15 arena-invariance;
the D1–D5 dead list pre-registered.
Outcomes: `WELD2-CARRIER-MAP-FOUND-<map>` /
`WELD2-EMPTY-AT-CARRIER-<obstruction>` / `WELD2-BLOCKED-AT-<object>`.

**Route B — THE DECLARED WELD**: declare the map (e.g. site ←
actor, link ← delivery channel, n_ℓ ← division-event count on that
channel), name it a declaration in the verdict string, price it as
BRG §14 open 1 prices its dichotomy.  Cost: every downstream
QFT-rung result inherits `DECLARATION-RELATIVE`, permanently.

## (d) CARRIER AMENDMENT (post-#82)

The scout's R1 row named the MENU quotient as the grammar-side
carrier.  The Γ-main adjudication (#82) has since ruled **CONG-185
supersedes MENU+G** for the law, and stamped carrier-relativity
(lumpable at CONG-185 / indivisible at MENU) a first-class open.
**The weld-2 pin, when ordered, therefore poses the census at BOTH
carriers and carrier-stamps its verdict** — the same discipline the
Γ iteration now carries.  The scout's absence verdict is
carrier-robust (it quantifies over ALL grammar-side objects: the
five evidence kinds are quotient-independent), so no scout finding
is invalidated by the carrier change.

## (e) THE SCOPED FINDING (publishable form)

The gravity line and the quantum line are built on **two arenas
that share one word and no object** — the record carries interval
totals without event positions; the process carries event positions
without intervals; each identification so far had to invent the
missing half, and all five died UNMOTIVATED.  The honest sentence:
**the theory derives what its geometry counts, and declares where
the counting happens.**
