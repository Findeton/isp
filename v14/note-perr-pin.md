# PER-R (paper-29) — THE R-LADDER PERSISTENCE CENSUS — PIN (FROZEN)

**Frozen:** 2026-08-11, v14 ledger #233.  **Authority:** the
persistence directive (#196) + the paper-21 adjudication
(#211: the saturation theorem AS A SCHEMA; the three
falsifiable predictions; the price sequence CLOSES — R=5 is
the SLACK census) + the PER-L adjudication (#228: the
interference census on the R-ladder is CLOSED BY THEOREM —
LINK is Sidon at every L ≥ 3; cite, do not re-run).
**Parents (terminal):** paper-21 at #232 (the R=4 row
complete; the covering machinery; G-R6-DOOR), paper-19
(R=3), U4b (R=2), I7 (the declared record box).  **Unit:**
paper-29-perr; code v14/code/perr_exact.py (+ output +
receipt).

## THE QUESTIONS

1. **THE THREE PREDICTIONS AT R=5 (falsifiable, from #211):**
   (a) NO declared I7 record has Σn = 5 (check the committed
   box); (b) zero-free-items is IMPOSSIBLE at R=5 (5 ≢ 0 mod
   3 — prove in-arena via the ladder law, not by citation
   alone); (c) R=5 is STRICTLY WEAKER than R=4 (the
   covering-class comparison at a declared window).  Each
   PASSES or FAILS with exact witnesses.
2. **THE SLACK CENSUS AT R=5:** the price catalogue is
   closed; measure the slack's structure (paper-21's 9(R−3)
   formula predicts 18 — verify; the binding constraint's
   identity at R=5; the covering-class census at the
   declared window with the saturation SCHEMA's license
   re-proved at R=5, not assumed).
3. **THE DICTIONARY ROW:** the weld fibers along the ladder —
   R=3 (1/1/1, cite), R=4 (36/3/1, cite), **R=6 (1/1/1 —
   CITE paper-21's G-R6-DOOR; extend to the full (2,2,2)
   stratum census if the window licenses)**; the R=5
   non-rung's weld status measured (the ladder law predicts
   imperfect — the fibers priced).
4. **THE RECORD-CLASS STRUCTURE AT R=5:** the
   posdef/I7-strict/det-spectrum distribution at a declared
   window; **G-SINGULAR and G-INDEF static reachability at
   R=5 — THE SIG FEED (this row is SIG's Stage-0 input;
   compute it first and state it separately).**
5. **THE DIA ROW:** is the diagonal parallel class still
   compulsory at R=5/R=6 (the covering analysis extended)?
6. **NOT RUN (closed by theorem, cited):** the interference
   census (PER-L's #228 corollary).

## OUTCOMES

The per-invariant table (PERSISTS / BREAKS-AT / TRANSFORMS
with witnesses) + the three prediction verdicts + the SIG
feed stated separately.  `PERR-BLOCKED-AT-<object>`
first-class.

## STANDARDS

Full era + E-22/E-23/E-24; the saturation schema re-proved
per rung before any exhaustiveness claim; windows declared
in-string and licensed; exact arithmetic; the derived+
rendered head; the total seal; failing runs write nothing;
byte ×2.  Candidate readings until adjudication.
