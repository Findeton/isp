# R4b — MOMENTUM: READING THE DISPERSIONS (paper-15) — PIN (FROZEN)

**Frozen:** 2026-08-10, v14 ledger #106.  **Authority:** the R4
terminal (#103; papers/receipts at commit 583cae7 — paper
`1063401c7bb5`, code `2959c5a6a84b`, output `ffd069ff3eb4`,
receipt `3dc1393b0df8`) and its adjudicated successor register
(note-r4-adjudication.md `3b00a9481b28`, R-R4-11); the frozen
panel reviews for the dispersion rows (operator K3; effectus
"charge without momentum" section).  **Unit:**
paper-15-momentum; code v14/code/r4b_momentum_exact.py
(+ output + receipt).

## THE QUESTION

R4's terminal registers MOTION NOT FORBIDDEN: 57 of 58 circulant
families carry non-constant eigenphase — the dispersions exist
and have never been read.  R4b READS them: the exact Bloch
dispersion per family, the induced group velocities, which
classes MOVE, and what effective propagation bound (if any)
replaces R4's light-cone segment (vacuous at L=4;
BOUND-HAS-CONTENT-ONLY-AT-RADIUS-0), including the terminal
observation "the unique scale is where the propagator cannot be
resolved" made precise.

## R1 — THE ARENA (inherited, declared)

R4's terminal family verbatim: the 64 translation-covariant
generators on the L=4, d=2 torus, alphabet 25, field Q(ζ₈),
single-occupation sector, the 22 extended / 38 anchored classes
with the 14 invariant labels; the connective clause INHERITED
VERBATIM into SCOPE: CONNECTIVE=MAX-NORM(FORCED-BY-ANCHORED-
LINK-(1,1)).  **The momentum lattice is the dual torus (16
momenta), DECLARED as data.**  No continuum claim; no
interacting claim beyond the inherited composed-segment defect.

## R2 — THE READINGS, EXACT

- **The dispersion census:** eigenphases per family per momentum
  in EXACT cyclotomic form (Q(ζ₈) arithmetic or an exact
  extension the unit constructs and gates; NEVER floats).  The
  57/58 claim RE-DERIVED in-unit (its 1/58 constant-eigenphase
  family identified, gated, and used as the NO-MOTION control —
  the motion head must return its other value there).
- **Group velocity:** the discrete derivative of the Bloch
  phase on the dual torus — the DEFINITION DECLARED as data
  (fiber printed if alternatives exist); computed exactly.
- **The motion head per class:** MOVES / STATIC, bound to the
  per-class gate (#87 — every one of the families bound to its
  own computed dispersion; no aggregate predicates).
- **The propagation bound:** whether the maximal exact group
  speed bounds the measured defect radius / two-point content
  (R4's SEPARATIONS=16-OF-16-CEILING and MAX-DEFECT-RADIUS=2-OF-
  2-CEILING rows inherited at their ceilings — the ceiling
  disclosure carried); the propagator-resolution observation
  stated as a measured relation between L=4 and the dispersion
  support, or demoted to a register row if not derivable.

## R3 — CHOICE INVENTORY + SCOPE

RSQ standard throughout; expected declared items: the momentum
lattice, the velocity definition, the eigenphase branch
convention.  SCOPE segment inherits R4's verbatim (D=2; L=4;
FIELD; ALPHABET; GENERATORS; STENCIL=3-TERM-AXIS;
SECTOR=SINGLE-OCCUPATION; INDIVISIBILITY=DECLARED-BY-DIVISION-
EVENT-TIMES; FINITE-LATTICE-ONLY; NO-CONTINUUM-CLAIM).  The
charge-without-momentum tension (R4 K3) addressed as a measured
statement: do the translation-trivial classes acquire motion
through the dispersion, or is motion confined to specific
classes — candidate readings named.

## R4 — STANDARDS AT CONSTRUCTION

Full era: the #82 CLI contract; #87 per-object gates; #91
pinned-sha reads + off-tree/git-less byte-reproduction TESTED;
§15; #24/#20/#34/#62; the head DERIVED by an independent
reconstruction; failing runs write nothing; a final integrity
gate re-reads written artifacts; verify-paper inside the plain
run covering every numeral; exact arithmetic only.  Concurrent
workers exist — touch only this unit's four files.

## OUTCOMES (pre-registered)

`R4B-DISPERSION-READ-<MOVING=n-OF-58;VMAX=…;BOUND=…>` /
`R4B-NO-MOTION-<witness>` / `R4B-BLOCKED-AT-<object>`.
Between delivery and adjudication every headline reading is a
candidate reading.
