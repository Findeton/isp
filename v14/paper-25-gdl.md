# The gravitational-decoherence law: what a record can and cannot tell you about a decoherence

**GDL / paper-25.** Pin `v14/note-gdl-pin.md` (FROZEN, sha256-12 `fe9533371046`,
ledger #205). Parent, terminal and cited at its pinned bytes: the coupling
(paper-20, paper `4824d190af73`, code `72e7b299f66e`, output `42b103eeec14`,
receipt `55273f6b6068`), with the gravity law's normaliser taken from the
Γ-iteration terminal at commit `2895a9a`. Code `v14/code/gdl_exact.py`;
artifacts `v14/code/gdl_output.txt`, `v14/code/gdl_receipt.json`.

**SCOPE (binding, carried from the pin).** This unit measures a relation at
ONE arena at ONE declared horizon. Fitted forms are barred; only exact
relations are claimed. No SI quantity, no rate in any physical unit and no
experimental value appears anywhere, and the corpus's Diósi–Penrose
gravitational-decoherence arc is cited for SHAPE ONLY. No continuum reading and
no cosmological reading is taken. The *measurements* are arena-bound; the
*theorem* of §7 is not, and §7 says exactly which is which.

**Verdict**, in four segments.

```
GDL-ARENA-[THE COUPLED MACHINE RE-IMPLEMENTED FROM THE COMMITTED SPEC AND ANCHORED AT THE PARENT'S OWN COMMITTED BYTES: 14 OF 14 PUBLISHED VALUES OF PAPER-20 RE-DERIVED AT EQUALITY BY AN IMPLEMENTATION THAT IMPORTS NO MODULE OF IT AND SUBPROCESSES NOTHING, AND ALL 14 LOCATED VERBATIM IN THOSE BYTES; THE WELDED RECORD n = 1 AT 27 OF 27 CELLS DRIVEN TO HORIZON 5 ON 3 ARMS; 11,044 BRANCH-STEPS ON THE COUPLED ARM COLLAPSING TO 10,954 DISTINCT MEASUREMENTS AND 9,751 ON THE FROZEN CONTROL COLLAPSING TO 5 -- ONE PER LEVEL, BECAUSE A FROZEN STAGE CARRIES ONE QUANTUM HISTORY AND THE BRANCHING IS CLASSICAL BOOKKEEPING OVER IT; UNITARY AT 296,784 SITE-BRANCH-STEPS AND LAW-NATIVE AT 1,479,176 KERNEL CHECKS WITH 0 VIOLATIONS]@PAPER-20-TERMINAL-55273f6b6068
```

```
GDL-FUNCTIONALS-AND-MECHANISM-[3 DECOHERENCE FUNCTIONALS DECLARED, NONE PRIVILEGED, ALL RUN ON EVERY OBJECT OF EVERY ARM OF EVERY FIBER MEMBER; THE l_1 DECLARATION PRICED -- 278,318 OF 388,024 OFF-DIAGONAL MODULI ARE IRRATIONAL AND OUTSIDE Q, WHICH IS WHY D2 IS CARRIED IN SQUARED MODULUS OVER A POPULATION OF 394,344 OF WHICH 6,320 ARE EXACT ZEROS | 5 GROWTH FUNCTIONALS FROM THE MACHINE'S OWN EMISSION LAW: THE SITE RATE IS THE BORN SITE MASS SUMMED THROUGH THE KERNEL AND THE TOTAL RATE IS EXACTLY ONE DIVISION EVENT PER COUPLED STEP, 0 VIOLATIONS | THE BLINDNESS MECHANISM, MEASURED ON 5 DECLARED FOREIGN COUNT FIELDS AT 164,310 CHECKS OVER THREE FUNCTIONALS, 54,770 OBJECT-AND-FIELD PAIRS EACH: D1 MOVES 0 TIMES, D2 MOVES 54,593 TIMES AND AT NOT ONE OBJECT WHOSE OCCUPIED-LINK SETS MEET IN AT MOST ONE LINK, D3 MOVES 54,700 TIMES, AND THE LAW'S OWN GROWTH FUNCTIONAL G5 MOVES 0 TIMES -- THEOREM A: A SITE-BLOCK-DIAGONAL UNITARY STEP LEAVES THE SITE MARGINAL RECORD-BLIND AT ANY ARENA, GRAPH, DIMENSION AND HORIZON, MEASURED UNDER A SITE-DEPENDENT COIN AT 54,770 PAIRS WITH THE MARGINAL MOVING 0 TIMES AND D2 MOVING 54,641; THEOREM B: SITE-UNIFORMITY REDUCES THE OFF-DIAGONAL AND MAKES CO-OCCUPANCY-NECESSITY A THEOREM, VERIFIED COEFFICIENT BY COEFFICIENT AT 4,374 IDENTITIES AND 39,366 CHECKS, AND CONFIRMED AT THE PAIR GRAIN: 452,745 OF 1,971,720 PAIR-CHECKS AT OVERLAP <= 1 MOVE 0 TIMES WHILE 1,518,975 AT CO-OCCUPANCY MOVE 840,889 | THE TWO OCCUPANCY THRESHOLDS: A SITE ON TWO LINKS AT STEP 3, A PAIR SHARING TWO LINKS AT STEP 4, BOTH IDENTICAL ON BOTH ARMS | THE SEPARATION LADDER D3 AT 3, D1 AND D2 AT 4, IDENTICAL ACROSS ALL 7 EXECUTED FIBER MEMBERS]
```

```
GDL-DECOUPLED-AT-THE-GRAVITATIONAL-BAR-<THE RELATION=MEASURED-NOT-FITTED(45 CELLS PER ARM -- 3 DECOHERENCE x 5 GROWTH x 3 RESOLUTIONS -- DECIDED BY EQUALITY INSIDE A GROWTH CLASS AND BY NOTHING ELSE: ON THE DELIVERED ARM 2 EXACT, 28 PARTIAL WITH EVERY FAILURE SET CENSUSED AND RECOUNTED, 15 VACUOUS AND STAMPED VACUOUS RATHER THAN PASSED; ON THE FROZEN CONTROL 1, 38 AND 6) -- THE ONLY EXACT CELL IS THE LAW'S OWN IDENTITY(D1 = SUM_x eps(x)^2, THE SITE-BASIS INVERSE PARTICIPATION OF THE BORN MENU IS THE SUM OF THE SQUARED SITE EMISSION RATES, AT EVERY OBJECT OF BOTH ARMS -- A CONSEQUENCE OF THE LAW-NATIVE NORMALISER'S COLUMN-STOCHASTICITY, STAMPED DEFINITIONAL-THROUGH-THE-LAW) -- FROZEN-EXCLUSION=2 OF 2 EXACT CELLS EXCLUDED(THEY HOLD IDENTICALLY ON THE FROZEN CONTROL, WHOSE RECORD NEVER GROWS AT 9,751 BRANCH-STEPS, SO THEY ARE FACTS ABOUT THE WALK AND THE LAW RATHER THAN ABOUT THE COUPLING), AND FORCED: BOTH SIDES OF THE ONE EXACT IDENTITY ARE FUNCTIONS OF THE SITE MARGINAL, WHICH IS RECORD-BLIND BY THEOREM A -- D1 MOVES 0 TIMES AND G5 MOVES 0 TIMES OVER THE SAME 54,770 OBJECT-AND-FIELD PAIRS -- SO IT COULD NOT HAVE FAILED ON THE CONTROL FROM EITHER SIDE, AND THE 4 GENUINE RECORD FUNCTIONALS G1..G4 PRODUCE NO EXACT CELL AT ANY RESOLUTION -- GRAVITATIONAL D=f(G) CELLS=0 -- THE 28 PARTIAL CELLS ARE NOT ADJUDICATED GRAVITATIONAL BY ANY GATE: THE BAR IS DECLARED(F12) TO APPLY TO EXACT CELLS ONLY AND IS INAPPLICABLE TO A RELATION THAT DOES NOT HOLD, 26 OF THE 28 CARRY THE IDENTICAL WORD ON THE FROZEN CONTROL, AND 11 OF THE 28 HAVE ZERO PASSING NON-SINGLETON CLASSES AND ARE STAMPED TOTAL-FAILURE(THE HONEST STAMP CENSUS OF THE DELIVERED ARM IS 2 EXACT, 17 PARTIAL, 11 TOTAL-FAILURE, 15 VACUOUS) -- THE RECORD DOES NOT DETERMINE THE DECOHERENCE(AT THE FINEST RESOLUTION THE CELL-GRAIN RECORD LEAVES 7,498 OF 10,954 OBJECTS IN CLASSES CARRYING MORE THAN ONE D1-VALUE, 8,166 FOR D2 AND 8,100 FOR D3; ON THE FROZEN CONTROL THE RECORD IS ONE CLASS CARRYING EVERY VALUE, SO IT CANNOT TESTIFY AT ALL) -- WHAT SURVIVES THE BAR IS BETWEEN-ARM, NOT WITHIN-ARM: THE SEPARATION LADDER D3 AT 3 / D1 AND D2 AT 4 IDENTICAL ACROSS 7 OF 7 FIBER MEMBERS, AND D1 COUPLED-DOMINATES AT BOTH ITS SEPARATING STEPS [4, 5] WITH EXACT WITNESSES AND IS PUBLISHED HORIZON-LOCAL -- DOMINATION=D1 COUPLED-DOMINATES; D2 NO-DOMINATION; D3 NO-DOMINATION -- OUTCOME-REACHABILITY=4 OF 4 ARMS OF THE DECLARED GRAMMAR EMITTED BY A WITNESS CONFIGURATION THROUGH THE SAME SELECTOR AND THE SAME COMPARATOR, SO THE WORD IS A MEASUREMENT AND NOT A DEFAULT -- FORCEDNESS=7 OF 7 FIBER MEMBERS AGREE ON THE WHOLE RELATION SIGNATURE, THRESHOLDS INCLUDED(+/- GROVER MEASURED IDENTICAL AS A GLOBAL-PHASE PAIR) -- PREDICTION-ROW=ENTERED -- SCOPE=THIS UNIT MEASURES A RELATION AT ONE ARENA AT ONE DECLARED HORIZON; NO CONTINUUM, NO SI QUANTITY, NO EXPERIMENTAL VALUE, AND THE DIOSI-PENROSE ARC CITED FOR SHAPE ONLY>
```

```
GDL-THIRD-CHANNEL-[THE RECORD THE MACHINE WRITES IS UNBOUNDED; THE RECORD THE COIN READS IS Z/3 -- THE STEP READS w^(n mod 3) AND w IS A PRIMITIVE CUBE ROOT OF UNITY, SO EVERY psi-INTERNAL FUNCTIONAL SEES AT MOST A Z/3 SHADOW OF THE METRIC.  MEASURED ON 2 DECLARED CONGRUENT-BUT-DIFFERENT RECORD FIELDS -- THE SAME RESIDUE AT EVERY ONE OF THE 27 CELLS, A DIFFERENT RECORD AT EVERY OBJECT -- AT 65,724 CHECKS: THE TWO STATE-INTERNAL FUNCTIONALS MOVE 0 TIMES AND THE RECORD-READING ONE MOVES 21,856, SO TWO OF THE THREE CANNOT DISTINGUISH CONGRUENT RECORDS AT ALL AND ONE CAN | THE WRAPAROUND, CENSUSED AT BOTH GRAINS: WRITTEN INSIDE THE HORIZON -- THE ANCHORED MAXIMUM-CELL LADDER REACHES 4 AT LEVEL 5 AND 1,316 CELLS THERE CARRY A COUNT ABOVE 3, ALL 1,316 OF THEM CONGRUENT TO THE WELDED VALUE n = 1 -- AND READ BY NO OBJECT OF IT: AN OBJECT OF STEP t READS THE RECORD OF LEVEL t-1, SO 0 WRAPPED CELLS ARE READ ANYWHERE INSIDE THE DECLARED HORIZON AND THE FIRST STEP THAT WOULD READ ONE IS 6, ONE BEYOND IT | VERDICT=THIRD-CHANNEL-PRESENT-AND-NOT-EXERCISED]
```

Between delivery and adjudication every headline reading here is a **candidate
reading** — maximally so for the mechanism of §7, which is this unit's deepest
finding and its most falsifiable one.

---

## 1. The question, and what would have counted as an answer

The substrate identity says that one event is simultaneously a record, a
decoherence event and a metric increment. If that is true, a quantitative
relation between decoherence and geometry growth is not merely possible, it
ought to be forced. This unit asks whether it exists.

The pin makes the answer a measurement rather than a target, and it does so by
barring the one move that would have manufactured a positive. **Fitted forms
are barred.** Nothing here is regressed, correlated, approximated or estimated;
the only admissible claims are equalities, monotone dominations with exact
witnesses, and exact functional dependence gated by equality. The test used
throughout is therefore the simplest one there is: *D is a function of G
exactly when every G-class carries one D-value.* A test with no non-singleton
class decides nothing and is stamped VACUOUS rather than passed.

And the pin supplies the arm that makes an answer meaningful. **A relation that
holds identically on the frozen stage is not a gravitational-decoherence
relation**, because a frozen stage's record never grows, so anything still true
there is a fact about the walk and about the law rather than about the
coupling. That exclusion is implemented as a gate, not as a remark.

**What the outcome grammar means, and why the word is a measurement.** The
pin's outcome arms all carry the `GDL-` prefix, and the pin defines the G in
GDL itself: a relation that holds identically frozen is not a
gravitational-decoherence relation. The prefix therefore scopes every arm. The
DECOUPLED arm does not say *no relation*; it says **no gravitational-decoherence
functional relation survives the bar** — and that is what was measured, at
`0` surviving cells.

A head word is only a finding if its alternatives could have been emitted, so
the alternatives are **run** rather than advertised. In the plain delivery run
a control arm hands the same selector and the same independent comparator four
witness configurations, each differing from this run's own measured blocks in
one patched primitive, and requires each to emit its named word: the delivered
measurement emits `GDL-DECOUPLED-AT-THE-GRAVITATIONAL-BAR`; one exact cell
surviving the bar emits `GDL-LAW-FORCED`; the same with one fiber member's
signature moved emits `GDL-LAW-COIN-RELATIVE`; a run whose mandatory control
did not execute emits `GDL-BLOCKED-AT-THE-FROZEN-CONTROL`. Measured: **4 of 4
arms of the declared outcome grammar are emitted by a witness configuration
through the same selector**. The word this unit publishes is selected by
whether the control ran, by how many exact cells survived the bar, and by
whether the fiber agrees — and by nothing else.

An exact relation does exist, it is beautiful, and it is excluded by the pin's
own gate — and §6 shows the exclusion was not merely observed but compelled.
What replaces it is a mechanism, measured rather than argued, that says why the
decoherence functionals a physicist would reach for cannot see the metric at
all.

## 2. The machine, rebuilt rather than cited

paper-20's driver is read as a **specification** and never imported and never
subprocessed: #91 forbids the second and the whole claim to independence
forbids the first. The walk (`C(x) = G · D(x)` with `D(x) = diag(ω^{n_l(x)})`,
site-block-diagonal, and the shift `|x, l⟩ → |x + l, l⟩`), the emission law
(both declared readings, with the law-native kernel `k_1(l|x) = q(l|x)/M(x)`)
and the update semantics (a division event on `(x, l)` increments `n_l(x)` by
one, and admissibility is a property of the record rather than a precondition
of the step) are re-implemented here from that description.

An independent re-implementation is worth nothing unless it is anchored, so it
is: **the rebuild reproduces 14 of 14 published values of paper-20 at equality,
and all 14 are located verbatim in the parent's committed receipt bytes**. The
anchor set is not a spot check, and the whole of it is published rather than
sampled. Location is by discriminating token where the row has one; the two
rows whose every token is a single digit are located against their own receipt
key instead, in order, so a perturbed ladder fails to locate.

| anchored quantity | value |
|---|---|
| A-COUPLED branches per level | `[3, 27, 486, 10527, 284078]` |
| A-FROZEN branches per level | `[3, 27, 486, 9234, 212382]` |
| B-COUPLED branches per level | `[3, 27, 486, 11664, 314928]` |
| coupled Born-menu inverse participation at the horizon | `35971074413334039128803/239299329230617529590083` |
| frozen Born-menu inverse participation at the horizon | `2306155/14348907` |
| coupled admissibility-exit probability, Born menu | `927415552/847288609443` |
| coupled admissibility-exit probability, record menu | `37440224/5811307335` |
| frozen admissibility-exit probability | `0` |
| coupled link-class marginal at the horizon | `[1484731802239/847288609443, 1326536376376/847288609443, 1425174868600/847288609443]` |
| coupled site distribution at the horizon | `[175233895520/847288609443, 175071388384/847288609443, 42793914260/847288609443, 136317227872/847288609443, 61354232756/847288609443, 4919647264/94143178827, 43253844785/282429536481, 31797021556/847288609443, 5631396596/94143178827]` |
| frozen site distribution at the horizon | `[352/2187, 352/2187, 416/6561, 352/2187, 160/6561, 544/6561, 1697/6561, 160/6561, 416/6561]` |
| maximum cell count on the ladder | `[2, 2, 3, 3, 4]` |
| branch-step count of the coupled arm | `11044` |
| branch-step count of the frozen arm | `9751` |

The measured arena is the parent's: **the welded record is n = 1 at 27 of 27
cells**, driven to the declared horizon on three arms, unitary at `296,784`
site-branch-steps and law-native at `1,479,176` kernel checks with `0`
violations.

**That second number is stamped, because it is not evidence of the kind its
size suggests.** The `1,479,176` kernel checks split exactly as `296784`
law-native, `295598` kernel and `886794` kernel-entry, and **every one of the
three classes is an identity in the definition of the kernel: none of them is a
row that could have failed**, for any coin, menu or record. The contentful
sibling is the `296,784` site-branch-step unitarity row, which is site-mass
preservation and is stamped CONTINGENT — a coin that moved amplitude between
sites breaks it, and §7 exhibits one that does not.

**One number in that table is a measurement this unit adds rather than
inherits.** The objects of every relation test below are BRANCH-STEPS — a
branch together with the Born menu it reads and the record it reads that menu
on — and they are deduplicated by their own content, because two branch-steps
carrying the same state on the same record are one measurement counted twice.
Deduplicated: **the coupled arm's 11,044 branch-steps collapse to 10,954
distinct measurements**, and **the frozen control's 9,751 branch-steps collapse
to 5 distinct measurements, one per level**. The raw count is a measurement;
the collapse to exactly one object per level is *forced* by the frozen update
semantics — a constant record under a deterministic unitary step gives a unique
`(t, ψ, n)` — and it is stamped DEFINITIONAL-THROUGH-THE-FROZEN-SEMANTICS. The
unit carries its own evidence for the distinction: across the fiber the frozen
*raw* count varies while the *distinct* count is `5` at all seven members. What
the collapse says is what a frozen stage *is*: one quantum history, with the
branching a classical ledger written beside it. Every honest denominator in
this paper is the deduplicated one.

## 3. The three decoherence functionals, declared and priced

None is privileged and all three run on every object of every arm of every
fiber member.

- **`D1-IPR-BORN-MENU-SITE`** — paper-20's own observable, inherited: the
  site-basis inverse participation of the Born menu, `D1 = sum_x p(x)^2`, with
  `p(x)` the Born menu's site mass.
- **`D2-OFFDIAG-SITE-MASS`** — the off-diagonal coherence mass in the site
  basis: with `rho_xy = sum_l (C psi)(x,l) conj((C psi)(y,l))` the site-reduced
  density matrix of the Born menu, `D2 = sum_{x != y} |rho_xy|^2 / 9^{2t}`,
  which is exactly the squared Hilbert–Schmidt distance from the site-dephased
  state.
- **`D3-RECORD-BORN-DIVERGENCE`** — the record-menu/Born-menu divergence, the
  Δᴮ-shaped candidate and the only one of the three that reads the record: the
  Born-weighted total variation between the Born menu's own kernel and the
  record menu's, `D3 = sum_x p(x) · TV(k_A(.|x), k_B(.|x))`.

Each specification is re-derived from its own published formula at every object
of both delivered arms, not on a probe object.

**The `D2` declaration is priced rather than waved through.** The exact ℓ₁
form is not computable in Q on this arena: an amplitude's modulus is the square
root of the rational integer `a^2 - a b + b^2` and that is usually not a
square. So the squared modulus is the exact carrier, and the price is measured
in the same run — **278,318 of 388,024 off-diagonal moduli are irrational**,
over `547` distinct values. The honest denominator of that fraction is the full
unordered off-diagonal population, `394344` entries over `10954` objects: the
`6320` entries the numerator drops are exact zeros, which are rational, so the
rational moduli number `116026` of `394344`. `D3` needs no such declaration:
the absolute value of a rational is a rational, so it is exactly ℓ₁.

**And nothing measured about `D2` transfers to the ℓ₁ form.** `D2` is
`Σ_{x≠y}|ρ_xy|²`, which is not a monotone function of `Σ_{x≠y}|ρ_xy|`, so
neither `D2`'s domination flip nor its separating step is inherited by the ℓ₁
functional it stands in for. The declaration is disclosed and its consequence
is disclosed with it.

An exact identity ties two of the three together and is gated per object:
`D1` is the diagonal of the site-basis purity and `D2` is its complement,
`D1 + D2 = Tr(rho^2)` at `10954` coupled and `5` frozen objects with `0`
violations.

## 4. The growth functionals, taken from the machine's own law

Five, and the distinction between them is the two-arm core. `G1-RECORD-CELL` is
the record itself — the metric — at the cell grain; `G2-RECORD-SITE` and
`G3-RECORD-LINK` are its site and link-class totals; `G4-GROWTH-TOTAL` is the
accumulated growth. Those four are the record's **actual** growth, and on the
frozen control they are identically constant: not one of the `5` distinct
frozen objects carries a record different from the welded one.

The fifth, `G5-RATE-SITE`, is the **law's** rate: `eps(x)`, the expected number
of division events at site `x` at this step, read off the machine's own
emission weights and never re-derived here. It is computed by the same code on
both arms, and it is not constant on the frozen one. That asymmetry is what
makes the frozen stage a real control rather than a null.

Two rows are measured about the rate itself, and both are properties of the
law-native normaliser rather than of this unit's arithmetic: `eps(x) = p(x)` at
every site of every object of the delivered arm (`98586` checks, `0`
violations), and `sum_x eps(x) = 1` — exactly one division event per coupled
step — at every object. The rate is **summed through the kernel** rather than
assigned, `eps(x) = sum_l p(x) k_1(l|x)`, so the first of those rows genuinely
tests the normaliser's column-stochasticity at the site grain instead of
comparing a value with itself. Both are re-taken on every fiber member, the
record menu included, in §10.

The n-growth profile of the coupled arm is the record's own: total growth
`0, 1, 2, 3, 4` at steps `1` to `5`, with the maximum cell count running
`2, 2, 3, 3, 4`; the frozen arm's total growth is `0` at every step.

## 5. The relation, exactly

`45` cells per arm — 3 decoherence functionals × 5 growth functionals × 3
resolutions — each decided by equality inside a growth class and by nothing
else, on both arms, with every cell's published word required to agree with its
own censused counts and every failure set recounted by a second pass that
shares no intermediate with the first. That second pass now recounts three
numbers per cell rather than two: the failing classes, the objects inside them,
**and the non-singleton count the VACUOUS stamp rests on**. **45 cells per arm
are decided by equality inside a growth class: on the delivered arm 2 exact, 28
partial and 15 vacuous; on the control 1, 38 and 6.**

At the finest resolution, `RES-BRANCH`, with each cell's number the count of
**failing** classes out of its **non-singleton** classes:

| growth | `D1` | `D2` | `D3` |
|---|---|---|---|
| `G1-RECORD-CELL` | PARTIAL, `3740` of `4080` | PARTIAL, `4074` of `4080` | PARTIAL, `4041` of `4080` |
| `G2-RECORD-SITE` | PARTIAL, `104` of `120` | PARTIAL, `116` of `120` | PARTIAL, `117` of `120` |
| `G3-RECORD-LINK` | PARTIAL, `24` of `31` | PARTIAL, `25` of `31` | TOTAL-FAILURE, `31` of `31` |
| `G4-GROWTH-TOTAL` | PARTIAL, `2` of `4` | PARTIAL, `2` of `4` | PARTIAL, `3` of `4` |
| `G5-RATE-SITE` | **EXACT**, `0` of `130` | PARTIAL, `128` of `130` | PARTIAL, `129` of `130` |

Read the column label and not the shape: `3740` of `4080` is a **failure at
3740 of 4080 classes**, not a success at any of them. Only `340` of that cell's
non-singleton classes carry a single D-value.

**Eleven of the twenty-eight partial cells carry no partial relation at all.**
Every non-singleton class in them fails, and the verdict grammar the pin
declares has no word for that, so PARTIAL absorbs it. This unit therefore
publishes a STAMP beside the equality-gated word: a cell whose failing count
equals its non-singleton count is stamped TOTAL-FAILURE, and the honest stamp
census of the delivered arm is `2` exact, `17` partial, `11` total-failure and
`15` vacuous, against the verdict census of `2`, `28` and `15`. The best
passing fraction anywhere among the partial cells is `2` of `4`, on a
four-class test. **26 of the 28 coupled partial cells carry the identical
verdict word on the frozen control, and 11 of them have zero passing
non-singleton classes** — the two exceptions are the `G5` column's `D2` and
`D3` cells at `RES-BRANCH`, which are VACUOUS there.

The `RES-SITE` column reproduces the shape on `55140` site-rows, with the same
single exact cell (`63` non-singleton classes, `0` failing, of `64`). The
`RES-STEP` column is `15` of `15` VACUOUS on the delivered arm — five ensemble
objects, five distinct growth values, no class with two members — and it is
stamped vacuous rather than reported as fifteen passes. That stamp is the
honest denominator of this section. On the control the same column is `12`
partial and `3` vacuous, which is why the control's census is published beside
the delivered one rather than standing for it.

**The record does not determine the decoherence.** At the cell grain — the
finest record there is — **the cell-grain record leaves 7,498 of 10,954 objects
in classes carrying more than one value of the inverse participation**, `8166`
for the coherence mass and `8100` for the divergence. Two branch-steps can
carry the identical record and different decoherence, because the record is a
multiset of events while the state depends on the *order* in which they were
written. Coarsening the record does not help and cannot: `G2`, `G3` and `G4`
are functions of `G1`, so a class that already carried two `D`-values can only
absorb more, and the census measures exactly that — `7498`, `10599`, `10905`
and `10923` objects in failing classes for `D1` as the record is coarsened from
cells to sites to link classes to a single total.

**And there is one exact cell.** `D1 = sum_x eps(x)^2`: **the site-basis
inverse participation of the Born menu is the sum of the squared site emission
rates, D1 = sum_x eps(x)^2, at every one of the 10,959 objects of both arms**.
It is exact, it is per-object, it survives every coin and both readings — and
it is stamped `DEFINITIONAL-THROUGH-THE-LAW`, because it follows from the
law-native normaliser's column-stochasticity together with `eps(x) = p(x)` and
from nothing else. It is a true relation between a decoherence functional and a
metric-growth functional. Whether it is a *gravitational* one is decided in §6.

## 6. The frozen exclusion, and why the zero is forced

The rule is the pin's and it is a gate: a relation that holds identically on
the frozen stage is not a gravitational-decoherence relation. Every EXACT cell
of the coupled arm is re-evaluated on the control.

**The bar's scope is a declared choice and it is the choice that decides the
head.** It is applied to EXACT cells only, because a relation that does not
*hold* cannot hold *identically frozen*. Its consequence must be stated with
it: the `28` partial cells' gravitational status is decided by **no gate at
all**, and the control cannot decide it either, being a degenerate one-class
grid of `5` objects. That is item `F12` of the choice inventory, and the head
carries it.

Both exact cells hold on the control. The identity `D1 = sum_x eps(x)^2` is
checked per object on the frozen arm and holds at all `5` of them; at
`RES-SITE` the partition test itself returns EXACT there, while at `RES-BRANCH`
the frozen partition test is VACUOUS and decides nothing, so that leg rests on
the per-object identity instead — the receipt says so and so does this
sentence. So **2 of 2 exact cells are excluded because they hold identically on
the frozen control, and 0 survive as gravitational-decoherence relations**.

**And the zero is forced, not contingent.** The one exact cell relates `D1` to
`G5`, and *both sides are functions of the site marginal alone*: `D1 = sum_x
p(x)^2`, and `eps(x) = sum_l p(x) k_1(l|x) = p(x)`. The site marginal is
record-blind by Theorem A of §7, and this unit measures both halves on the same
fields: `D1` does not move once, and **the law's own rate does not move once in
54,770 object-and-field pairs, so the single exact cell relates a record-blind
decoherence functional to a record-blind growth functional**. The identity
could not have failed on the frozen stage from either side. The exclusion was
not a measurement that came out one way; it was compelled — and the four
genuine record functionals `G1`–`G4` produce no exact cell at any resolution,
on either arm.

A reader who wanted "decoherence equals geometry growth" would have got it —
and the control says it would have been reading the law back to itself.

## 7. Why the functionals cannot see the metric: the mechanism

The negative of §5 and §6 is not an absence of evidence. It has a mechanism,
and the mechanism is exact. It comes in three theorems with three different
hypotheses, and keeping them apart is the whole content.

> **THEOREM A (site-marginal blindness).** Let the state live on sites × fiber
> and let the record-dependent step act as `U(n) = ⊕_x U_x(n)` — **block-diagonal
> in the site index**, each block **unitary** on that site's fiber. Then for
> every ψ and every `n` the site marginal `p(x) = ‖(U(n)ψ)_x‖²` is independent
> of `n`, so **every functional of the site marginal alone** — `D1`, the Born
> site menu, the emission rate `eps(x)`, any Rényi entropy or inverse
> participation of `p` — is record-blind at that step. **Site-uniformity is not
> a hypothesis**, and neither is the arena, the graph, the fiber dimension, the
> horizon, the number of sites, nor the order of ω.

> **THEOREM B (the off-diagonal reduction).** If in addition `U_x(n) = G·D_x(n)`
> with `G` **site-uniform** unitary and `D_x(n) = diag(ω^{n_l(x)})` unimodular
> diagonal in the fiber basis, then
> `rho_xy = sum_l w^{n_l(x) - n_l(y)} psi(x,l) conj(psi(y,l))`,
> so any pair of sites meeting in at most one occupied link contributes a sum
> with at most one term and `|rho_xy|` is record-independent. **Co-occupancy
> necessity is a theorem, not only a measurement**, and the census below
> confirms it rather than establishing it.

> **THEOREM C (the residue reading).** The step reads `ω^{n mod 3}` and ω is a
> primitive cube root of unity, so the record enters the dynamics only as
> `n mod 3`. §8 measures what that costs.

Theorem A is where the exportable statement lives, and it is the strongest
thing in this unit. Note what its scope is: the *measurements* below are bound
to this arena at this horizon by the SCOPE box, but Theorem A is a statement
about the **form** of the step and is arena-free. That is why the blindness is
a scope limit on the programme and not on this arena alone.

Three consequences follow, and each is measured on `5` declared foreign count
fields — fields this run never generates, two of them not even admissible —
re-read at every coupled object. Their foreignness is measured too: none of the
five is among the records the coupled arm generates.

On the diagonal `x = y` every phase difference is zero, so `D1` cannot depend on
the record at all: **the inverse participation does not move once in the 54,770
object-and-field pairs at which it is checked, of 164,310 checks over the three
functionals and 5 declared foreign count fields**. The inherited observable is
record-blind by theorem. Off the diagonal, a pair of sites whose occupied-link
sets meet in at most one link contributes a pure global phase and is equally
blind: **the off-diagonal mass moves at 54,593 of the 54,770 object-and-field
pairs, and at not one object no two of whose sites share two occupied links**.
Co-occupancy is *necessary* and not sufficient.

| foreign field | `D1` moves | `D2` moves | `D3` moves |
|---|---|---|---|
| `STALE` | 0 | 10,911 | 10,953 |
| `ALL-TWO` | 0 | 10,922 | 10,929 |
| `INADMISSIBLE-ONE-CELL` | 0 | 10,922 | 10,929 |
| `LADDERED` | 0 | 10,922 | 10,954 |
| `ZERO-AT-ONE-CELL` | 0 | 10,916 | 10,935 |

The instrument is two-way, which is what makes the blindness a measurement
rather than a property of the census: on the same fields and the same objects
`D3` moves `54700` times. A census in which nothing moved would have decided
nothing.

**The mechanism holds at the grain its own algebra predicts, thirty-six times
finer.** The table above counts an object as blind only when none of its `36`
site-pairs co-occupies; Theorem B predicts something finer, namely that
`|rho_xy|²` is record-independent for *every* pair meeting in at most one
occupied link, whatever the rest of the object does. Measured over `10954`
objects × `5` fields × `36` site-pairs: **at the finest grain the mechanism
holds at 1,971,720 pair-checks: 452,745 site pairs meeting in at most one
occupied link move 0 times**, while `1518975` pair-checks at co-occupancy move
`840889` times and stay static `678086` times. Necessary, and not sufficient,
at the finest grain too.

**The algebra is verified as algebra.** With `M = 3C` the coin numerator the
claim is `rho_xy = 9 · sum_j w^{n_j(x) - n_j(y)} psi(x,j) conj(psi(y,j))`, and
both sides are bilinear in `(psi_x, conj psi_y)`, so checking them on the `9`
monomial pairs that span that form proves the identity for every ψ. Measured
coefficient by coefficient in Z[ω] over all `729` residue pairs for each of the
`6` declared coins: `4374` identities at `39366` coefficient checks, `0`
mismatches.

**And the hypothesis is isolated, not assumed.** The same objects and the same
foreign fields are re-read under a coin that is block-diagonal in the site
index and exactly unitary on every block but **different at every site** — the
declared fiber cycled over the nine sites, which is not a member of this unit's
fiber and about which nothing concerning this arena is claimed. Measured:
**under a coin that is block-diagonal and unitary on every block but different
at every site, the site marginal moves 0 times and the inverse participation
moves 0 times in 54,770 object-and-field pairs, while the off-diagonal mass
moves 54,641**. At the horizon that coin's `D1` takes `101` values and its `D2`
takes `1007`. The diagonal blindness survives site-variation; the off-diagonal
reduction does not need to.

**Two occupancy thresholds govern this unit and they differ by one step.**
Measured: **a site occupied on two links first appears at step 3 and a pair of
sites sharing two occupied links first appears at step 4**. From step `3` the record
moves the *cell masses*, so the arms' states differ from step `4` — and `D1`
and `D2` separate at exactly `4`. The *pair* predicate is what `D2`'s
off-diagonal blindness turns on, and it is the one the table above measures.
Both are identical on both arms, and the co-occupancy ladder locates the second
by measuring every step rather than by assertion:

| step | objects | with a co-occupancy pair |
|---|---|---|
| 1 | 1 | 0 |
| 2 | 3 | 0 |
| 3 | 27 | 0 |
| 4 | 486 | 486 |
| 5 | 10,437 | 10,437 |

Measured: **the co-occupancy threshold is step 4 on both arms**. It is a
property of the walk and not of the coupling, which is exactly why the frozen
control reaches it at the same step.

This is the unit's deepest finding and it is a scope limit on the whole
programme, not on this arena alone: **the DIAGONAL of a decoherence functional
built from the state at a single time, in a basis the coin preserves each
site's mass in, is blind to the record it is supposed to be a function of; its
OFF-DIAGONAL is blind exactly on the site-pairs whose occupied-link sets meet
in at most one link.** paper-20's staleness-blindness theorem is about the
*form* of closures: a single-time ψ-internal closure holding uniformly in `n`
holds on both stages. This unit measures the *values*, and the value-level
statement is strictly finer, because it separates two functionals the closure
theorem does not separate. The ψ-internal value built from the diagonal is
blind outright, at `0` of `54770`; the one built from the off-diagonal is blind
exactly off the co-occupancy support and moves at `54593` of `54770`; the
record-reading functional is never blind. **Blindness is graded, and the
grading is what the closure theorem cannot see.**

## 8. The third channel: the record the coin reads is Z/3

The record the machine **writes** is unbounded. The record the coin **reads**
is a residue: the step reads `ω^{n mod 3}` and ω is a primitive cube root of
unity, so every ψ-internal functional sees at most a `Z/3` shadow of the
metric. That is a third geometry-to-matter channel beside the diagonal and the
off-diagonal ones, and it is measured here.

Two record fields are declared for it, both **congruent but different**: the
same residue at every one of the `27` cells, a different record at every one of
the `10954` objects. Measured: **the two state-internal functionals move 0
times on 2 declared congruent-but-different record fields and the record-reading
one moves 21,856**, over `65724` checks — `10924` and `10932` moves of `D3` on
the two fields, against `0` for `D1` and `0` for `D2` on both. The census is
two-way for the same reason the blindness census is: a functional that saw the
metric rather than its residue would move here, and two of the three cannot.

The wraparound is not hypothetical, and it has to be censused at **both
grains**, because the two differ by exactly one step and the difference is the
finding. The record the machine writes reaches `4` at the top of the anchored
maximum-cell ladder `[2, 2, 3, 3, 4]`: **1,316 cells at level 5 carry a count
above 3 and every one of them is congruent to the welded value, while 0 wrapped
cells are read anywhere inside the declared horizon**. An object of step `t`
reads the record of level `t-1`, so the first step that would read a wrapped
cell is `6`, one step beyond the horizon this unit ran.

The verdict of the channel is therefore `THIRD-CHANNEL-PRESENT-AND-NOT-EXERCISED`
and it is worth stating in both directions. The channel is real and one-sided:
a cell that reaches `n = 4` enters the dynamics exactly as if it had never
grown, because `4 ≡ 1` is the welded value, and no ψ-internal functional could
tell the difference. And it is not exercised by any measurement in this unit:
the wrap is written inside the horizon and read outside it. Nothing published
here depends on the residue reading, and nothing published here escapes it.

## 9. The separation ladder, and the dominations that fail

The ψ-internal functionals are not blind forever: the record feeds back into the
state through the coin's phases, so their *values* do eventually move. When?

| functional | first separating step |
|---|---|
| `D1` | 4 |
| `D2` | 4 |
| `D3` | 3 |

Measured: **the record-reading functional separates the arms at step 3, one
step before either state-internal functional, which separate at step 4**. The
row is per-object, not an average: it compares the whole VALUE SET of each
functional against the control's, and at steps `1`, `2` and `3` the coupled
arm's `D1` and `D2` take a single value which is the frozen one — `1`, `11/27`, `619/2187` for
`D1` and `0`, `0`, `128/729` for `D2`. At step `3` the coupled `D3` already
takes `3` distinct values against the control's `1`. The mechanism of the ladder
is §7's: `D3` separates when the *records* differ, and the ψ-internal
functionals separate one step after the first double-occupied site.

The dominations are reported with their witnesses and two of the three fail:

| functional | separating steps | verdict |
|---|---|---|
| `D1` | [4, 5] | COUPLED-DOMINATES |
| `D2` | [4, 5] | NO-DOMINATION |
| `D3` | [3, 4, 5] | NO-DOMINATION |

The coupling raises the inverse participation at both steps where it moves it:
`3822155/14348907` against `40411/177147` at step `4` and
`51476944417/282429536481` against `81857/531441` at step `5` — the record
localises the walk. But the coherence mass rises at step `4`
(`2176942528/10460353203` against `33920/177147`) and **falls** at step `5`
(`5743648327744/22876792454961` against `184192/531441`), and the divergence
does the same. A monotone claim would have been available at horizon `4` and is
false at horizon `5`. That is the exact witness, and it is the reason this unit
publishes no monotone law.

It is also the reason `D1`'s surviving domination is published HORIZON-LOCAL
and never as a law: it is tested at exactly the two steps at which it moves,
and the two functionals that fail teach precisely that a two-step monotone
claim can break at the next horizon.

## 10. Forcedness

The fibers are run as declared **axes**, not as a product, and this unit says
so: **the fibers are run as declared axes, not as a product** — all `6` members
of the coin fiber (±Grover and the four hidden S₃-covariant classes, paper-20's
own witnesses, each verified exactly unitary and exactly S₃-covariant here) at
the delivered reading, and both members of the emission-reading fiber at the
delivered coin. The cross-product is not claimed.

Each member is required to reproduce the delivered member's **entire relation
signature**: all `15` grid cells' verdict words on both arms, the separation
ladder, every identity, and **both occupancy thresholds on both arms**. The
signature is a digest, so a single moved word or a single moved threshold would
break it.

| coin | coupled objects | frozen objects | site threshold | pair threshold | signature |
|---|---|---|---|---|---|
| `GROVER` | `10954` | `5` | 3 | 4 | `e78a2f782896` |
| `-GROVER` | `10954` | `5` | 3 | 4 | `e78a2f782896` |
| `w/3` | `11935` | `5` | 3 | 4 | `e78a2f782896` |
| `(-1+w)/3` | `10045` | `5` | 3 | 4 | `e78a2f782896` |
| `(-1-w)/3` | `11935` | `5` | 3 | 4 | `e78a2f782896` |
| `(-2-w)/3` | `9043` | `5` | 3 | 4 | `e78a2f782896` |
| `GROVER`, record menu | `11941` | `5` | 3 | 4 | `e78a2f782896` |

Seven of seven agree. The object counts are coin-specific; the verdict is not.
±Grover differ by a global phase, and this unit measures that every published
row is identical between them rather than assuming it.

Measured: **the separation ladder is identical across all 7 executed fiber
members**, and so is each occupancy threshold on each arm.

**What is not invariant is the co-occupancy *count*.** The control's own
step-`5` object carries `16` co-occupying pairs under ±Grover and the record
menu, `24` under `(-1+w)/3` and `(-2-w)/3`, and `33` under `w/3` and
`(-1-w)/3`. The threshold is a fiber invariant; the count is coin-relative, and
the prediction row below is written to the first and not the second.

## 11. The prediction row

A prediction is entered only if its own fiber census carries it. This one does,
and it is registered as two rows because its two clauses had different
standing until both were censused across the fiber.

> **`GDL-PRED-1a` (the separation ladder). FORCED-AT-THIS-ARENA.** At this
> arena, at the declared horizon 5 and for every member of the declared coin
> and reading fibers, the record-reading decoherence functional separates the
> coupled stage from the frozen control at step 3, exactly one step before
> either state-internal functional, which separate at step 4.
> **Falsified by:** any fiber member — any coin, any declared reading — whose
> ladder is not `(D3, D1, D2) = (3, 4, 4)`; any run at this arena and horizon
> in which the ladder moves. **Not falsified by:** a change of arena, of
> horizon, of the observable set, or of the fiber shape — those are outside the
> stamp and license nothing against it. **Mechanism, and therefore the shape of
> a refutation:** `D3` separates when the records differ, and `D1`/`D2`
> separate one step after a site is first occupied on two links. A refutation
> should attack the support geometry, not the coin.

> **`GDL-PRED-1b` (the co-occupancy threshold). FORCED-AT-THIS-ARENA.** The
> pair co-occupancy threshold that governs the middle functional's off-diagonal
> blindness is step 4 on BOTH arms, at every executed fiber member; the
> threshold now sits inside the same sealed signature as the ladder.
> **Falsified by:** any member whose threshold is not 4 on both arms. **Not
> falsified by:** a change of arena or horizon. **Already known to fail in its
> stronger form:** the co-occupancy *count* is coin-relative — `16`, `24` and
> `33` on the control's own step-`5` object — so the prediction is about the
> threshold only, and says so.

Both rows are rendered from the receipt and their units are substrate-native:
step indices and object counts, nothing else. The corpus's Diósi–Penrose
gravitational-decoherence arc is cited for SHAPE ONLY: this unit inherits no
number, no rate, no mass and no experimental claim from it, states nothing in SI
units, and every quantity published here is a count or an exact rational of the
substrate's own.

## 12. The walls

**L-1 — argued before any test, then declined.** L-1 records order-level
covariance as a fourth form outside paper 8's three. Admissibility would
require a group declared to act on the generated causal order and a reason to
read it as a covariance group; this unit constructs no such bridge. **The
fourth form is not tested here.** The sentence retracted in 2026 is not
reproduced, and the gate that enforces its absence whitespace-normalises,
ASCII-folds and strips markdown prefixes from both sides.

**BHS, Kleitman–Rothschild, the continuum, and SI.** No sprinkling-grade test is
run, no dimension estimate is taken, no continuum or cosmological reading
appears, and no SI quantity or experimental value is stated. All four
abstentions are **measured** rather than declared: the gates scan this run's
whole measurement layer — every measured receipt key together with the
statement and evidence of every non-wall gate evaluated — and the SI scan
covers this paper's own text as well, since the pin bars SI numbers from the
published row. Each carries a falsifier that writes the forbidden reading into
that layer and dies there. Three needles are declared out with their reasons
printed: `horizon`, `rate` and `mass` are this unit's and paper-16's own
vocabulary, so a scan firing on them would fire on the pin.

**The two resonances are named, both carried from the parent.** The
determinant that reaches 0 in the parent and is read again here is NAMED AND
NOT READ: it is the exact boundary of I7's own admissibility criterion on a
nine-site Euclidean lattice, it is not a signature and not a light cone, no
indefinite form is reached at this horizon, and no Lorentzian, causal or
signature-change reading of it is taken here or licensed by anything measured
here. The second resonance is NAMED before it is heard: q = [[1, -1/2], [-1/2,
1]] is the Gram matrix a reader will recognise as hexagonal, unit lengths
meeting at one hundred and twenty degrees, and this unit takes no triangular,
hexagonal, crystallographic or lattice-geometry reading of it whatever.

## 13. Choice inventory

| # | item | status | fiber | note |
|---|---|---|---|---|
| 1 | `F1-MACHINE` | **INHERITED** | 1 | paper-20's walk, emission law and update semantics, re-implemented from the committed spec and anchored at its bytes |
| 2 | `F2-COIN` | **DECLARED, WHOLE FIBER RUN** | 6 | +/- Grover and the four hidden S_3-covariant classes; all six executed |
| 3 | `F3-READING` | **DECLARED, WHOLE FIBER RUN** | 2 | Born menu against record menu; both executed |
| 4 | `F4-HORIZON` | **DECLARED** | 1 | T = 5, the parent's; the whole ladder published |
| 5 | `F5-D-SET` | **DECLARED** | 3 | the three decoherence functionals; none privileged, all run |
| 6 | `F6-D2-UNITS` | **DECLARED UNDER AN EXACTNESS CONDITION** | 1 | squared modulus, because the exact l_1 is not in Q here -- priced by measurement, and no D2 verdict transfers to the l_1 form |
| 7 | `F7-G-SET` | **DECLARED** | 5 | four record functionals and the law's own rate |
| 8 | `F8-RESOLUTION` | **DECLARED** | 3 | branch, site, step; the step column measured VACUOUS and stamped |
| 9 | `F9-OBJECT-IDENTITY` | **MEASURED** | 1 | branch-steps deduplicated by (step, state, record); the collapse 9751 -> 5 is forced by the frozen update semantics and stamped DEFINITIONAL-THROUGH-THE-FROZEN-SEMANTICS |
| 10 | `F10-FOREIGN-FIELDS` | **CITED** | 5 | paper-20's own declared set, adopted unchanged, and measured to be foreign |
| 11 | `F11-FIBER-SHAPE` | **DECLARED** | 1 | axes, not a product; stated in the receipt and in the paper |
| 12 | `F12-BAR-SCOPE` | **DECLARED** | 1 | the frozen bar is applied to EXACT cells only; partial cells are not adjudicated gravitational, and the control is structurally unable to adjudicate them.  THIS IS THE CHOICE THAT DECIDES THE HEAD |
| 13 | `F13-COOCCUPANCY-PREDICATE` | **DECLARED** | 2 | pairs sharing at least two occupied links, evaluated on the pre-coin psi; the single-site predicate is declared and measured beside it and they differ by one step |
| 14 | `F14-DOMINATION-TEST` | **DECLARED** | 1 | per-step value-set comparison; COUPLED-DOMINATES iff coupled exceeds frozen at every separating step, and the result is HORIZON-LOCAL |
| 15 | `F15-CONGRUENT-FIELDS` | **DECLARED** | 2 | the third channel's own declaration: two record perturbations congruent mod 3 at every cell and different at every object |
| 16 | `F16-SITE-DEPENDENT-COIN` | **DECLARED** | 1 | the THEOREM A demonstration coin -- block-diagonal, unitary per block, not site-uniform; not a member of this unit's fiber and nothing about this arena is claimed from it |

Every declared item has each member of its fiber executed. For `F2` and `F3`
the execution is bound by set equality against the declared member ids; for the
others it is bound by a declared cardinality, and the two bindings are named
rather than conflated.

## 14. Scope, and what a successor would have to change

This unit measures a relation at one arena at one declared horizon, on a
rebuild of one parent's machine. It licenses nothing about any other arena, any
other coin, any horizon it did not run, or any continuum. Theorem A is the one
exception and it is an exception of the right kind: it is a statement about the
form of the step and carries no number, so it constrains successors without
licensing anything numerical about them.

What this unit hands forward is a sharpened question. The relation is not
absent because the substrate identity is false; it is absent because **the
decoherence functionals available at a single time in a site-local basis are
blind to the record by construction**, and the one functional that is not blind
gives a partial dependence with a censused failure set rather than a law. A
successor that wants a gravitational-decoherence law at this substrate must
therefore change one of three things, and each is now named and sized:

1. **Make the observable read the history, not the state.** `D1` and `D2` are
   functions of the state at one time; the record enters them only through the
   walk's own trajectory, with a delay measured here as one step. A
   two-time functional — a decoherence *rate* between consecutive menus rather
   than a decoherence *value* at one — is the obvious next object, it is
   untouched by Theorem A, and it is not built here.
2. **Break the block-diagonality of the step.** Theorem A's hypothesis is
   block-diagonality, not site-uniformity, and this unit measures that a
   site-dependent coin changes nothing about the diagonal. So raising the order
   of the record phase cannot rescue the inherited observable at any order: the
   only route to a record-sensitive site-marginal functional is a step that
   moves amplitude between sites before the menu is read — which is the same
   property paper-20's law transport consumes, and would have to re-gate it.
3. **Reach a record the order-blindness cannot reach.** The partial dependence
   fails exactly where two orders of the same events give different states.
   Whether some coarsening of the record is order-blind *and* still determines
   the decoherence is a question this unit poses and does not answer: the
   cell-grain record and all three coarsenings it declared fail, and their
   failure sets are censused.

There is a fourth thing a successor should know, and §8 is where it is
measured: any exactly computable unimodular record consumption of finite order
is periodic in the record, so unbounded metric-sensitivity in a ψ-internal
functional and exact arithmetic are in tension at this substrate. At this
horizon the tension is latent rather than active — the wrap is written and not
read — which is precisely the kind of fact a successor at a longer horizon must
re-take rather than inherit.

There is structure here — an exact identity, an exact mechanism, a forced
ladder, a residue channel — and none of it is a gravitational-decoherence law.
