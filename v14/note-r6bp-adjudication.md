# R6b′ — JOINT ADJUDICATION (operator #59, effectus #60, instrument — this entry)

**Inputs:** the three frozen reviews, the frozen delivery (paper
`68c20d1fdae4`, code `8e188dd3ab70`, output `42a39fcaf194`, receipt
`50f63b3ba362`), protocol `1cf5fc8b3272`, pin `17111fd19022`.
**Standing:** three ACCEPT-WITH-FIXES (operator's fixes BLOCKING);
~1,114 recomputations; **zero numerical divergences — every kill is
definitional, referential, or instrumental**.  Resolved grade:
**ACCEPT-WITH-FIXES**.

## 1. The settled corrections (cross-confirmed)

**A. The renewal convention** (operator M1/M2): undeclared, and it
contradicts the source's own `REN` (class 0 *carrying an
arbitration*).  Under the source's convention the corrected numbers
are return 1/4, defect 3/4, visits 4/3, holes {1,2}, cost 79/201 —
AND the corrected convention **reproduces S2's delivery-free census
exactly from S1 alone** (the weld hiding in the kill).  The repair
re-derives everything under the source convention, with the bare-
state-0 reading printed as the disclosed alternative.

**B. The coverage upgrade** (operator M3 ∥ effectus qualifier
grading): count-4-only is a route artifact.  The uniform positional
law is derivable at every n ≥ 4 from S1 alone (C(n−1,2) equiprobable
configurations; position marginal uniform on n−1); count-3 emptiness
is a LAW ((p,p,r) forced); the honest denominators exclude the three
unrefinable records (10 of the 37 sat there).  The repair delivers
the all-n law with the corrected coverage on the honest denominator,
and the one residual freedom (filler-set → position reduction) in
the inventory.

**C. The seam, resolved at both referents** (operator "avoidable" ∥
effectus "genuine, cause mis-named"): as an obstruction to THIS
unit's question it was avoidable (the single-scope delivery-free row
set S1+S4+I7 supplies both layers); as a measurement it is real and
its cause is THE ESCAPE (no bounded transport abstraction), not row
provenance.  The head re-grades accordingly; the "delivery-free"
stamp corrects to NO-DELIVERY-CONDITIONAL-OF-THE-TRANSPORT-CENSUS
(different conditioning events, masses 3/7 vs 1/3, printed).

**D. F1 — a false result stricken**: NEW-FRONT-VALUES → fiber-1 is
wrong (the left-anchored rule's mirror is equally expressible and
disagrees at 63/63 cycles; the terminal R6a paper's own §5.3 forbids
the forcing).  The segment corrects to the measured two-rule
disagreement.

**E. F8 — the mechanism enters the record**: the entire transport
position-dependence has one exact cause — **(p,d,p,r) does not
occur: no delivery in the middle interior slot**; delivery
multiplicity 2→3 drives 3/7→4/9.  Printed, gated, and pre-registered
as **Γ-main's first target**: reproduce (3/7,1/7,3/7) at leg 1 and
(4/9,1/9,4/9) at leg 2.

**F. Smaller strikes**: det-blindness unsound (both reviewers — det q
is record-intrinsic; the DIES-AT-THE-BAR verdict stands on
VARIATIONAL-ROWS-0-OF-6 as the decisive clause, with the
non-uniqueness census as constructive corollary); the S1 "two
routes" are two transcriptions of an authored literal (#219 —
disclosed, not claimed); five uncarried restrictions enter segments.

## 2. The instrument findings

Five surviving injection classes — headlined by **the T1
meaning-inversion STILL surviving**, because all three #34
verbatim-anchor modifications were implemented inert (order without
short-circuit; consumer bindings as unread labels — 7 literal-True
consumers, 12 never-falsified; windows below the intended class).
The waiver census FAILS the #34 standard (21 of 46 false
FALSIFIER-REACHES-IT claims; 18 unfalsified gates including the one
carrying TERMINATES-A-S; 6 literal-True must-pass predicates; dead
WAIVED-VERIFIED code).  **Two MAJOR recurrences**: the #46 class (a
must-pass gate reading another unit's UNCOMMITTED working file; no
#46 compliance row; "seven engravings" where the RUNBOOK has eight)
and R6a-M5's existence-binding anchor.  **The provenance pattern:
good stability core, broken addressing** — `git show HEAD:` is
mutable (the unit exits 2 today against R6a's terminal receipt; the
disclosed worktree blob is unrecoverable).

## 3. Binding repair orders (R-R6BP-1 … R-R6BP-9)

1. Re-derive everything under the SOURCE renewal convention (A),
   with the weld (S1 reproduces S2 delivery-free) measured and
   gated; the alternative reading a disclosure.
2. The all-n positional law delivered (B): the C(n−1,2) law, the
   uniform position marginal, coverage on the honest denominators
   (both printed), the filler-set→position freedom in the inventory.
3. The head recomposition (C): the seam head re-graded
   (cause = the escape; single-scope availability stated); the
   conditioning stamp corrected; the four heads made
   convention-consistent (M2).
4. F1 stricken and corrected; F8's mechanism printed + gated + the
   Γ-main target pre-registered; the det-blindness leg replaced;
   the #219 route disclosure; the five uncarried restrictions
   carried.
5. The verbatim-anchor implementation made real per the amended
   standard (§4): paper-quotation binding; consumer gates exist,
   non-literal, mutant-falsified; genuine evaluation-order
   short-circuit.  The A2/A5/A6b/consumer-unbind/additivity-garbage
   classes each get a dying mutant.
6. The waiver ledger rebuilt at the #34 standard (the 21 false
   claims; the 18 unfalsified gates get falsifiers or verified
   forcings; the 6 literal-True predicates rebuilt; the dead branch
   removed; 48/48 coverage).
7. The #46 recurrence eliminated: no read of any other unit's
   uncommitted state; the provenance addressing fixed per §4
   (commit-sha declared in frozen text; only committed objects
   disclosed); the unit re-runnable today (the R6a reference
   updated to the TERMINAL receipt by declared sha with values
   re-anchored); the compliance sweep carries all eight engravings.
8. Paper rewrite single-threaded; Status GREEN-REPAIRED.
9. The Γ-main input register (a paper section): the effectus
   four-head structure; the kernel as Γ's DELIVERY-FREE SHADOW
   (13/16 → 1/4 under the corrected convention — a control, never a
   target); the position-law targets; the holonomy-vs-⟨2,3⟩
   pre-registered gate requirement; the don't-inherit list.

## 4. RUNBOOK engravings (this adjudication)

- **§14 amendment (verbatim-text anchors, corrected spec):** the
  anchor kind binds QUOTE FIDELITY (the paper's quotations against
  the source's committed bytes); every consumer gate must exist, be
  non-literal, and be falsified by a declared mutant; anchor
  evaluation genuinely short-circuits before byte anchors.  An
  anchor whose consumer is an unread label binds existence, not
  meaning (the R6b′ instrument audit; supersedes the #34 wording).
- **§14 addendum (provenance by committed sha):** a unit reading
  another unit's artifacts declares the source COMMIT SHA in its own
  frozen text and reads via that sha; `git show HEAD:` and worktree
  bytes are mutable state (the #46 class); only committed objects
  may be disclosed.  The path-value stability gate across declared
  shas is the adopted core.

## 5. What Γ-main inherits (consolidated; supersedes prior notes)

The corrected kernel under the source convention + the weld; the
all-n positional law; the F8 mechanism + the two position-law
targets; the single-scope row set; the escape as the seam's cause;
the holonomy-vs-⟨2,3⟩ gate as a pin requirement; the don't-inherit
list (the stale stamps, fiber-1, det-blindness, the old
denominators).
