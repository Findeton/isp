# Γ-MAIN + R4 — HOSTILE REVIEW PROTOCOLS (FROZEN, one document, two panels)

**Frozen:** 2026-08-10, v14 ledger #75.  Reviewers: three per unit,
independent, single-file writes, scratch-only, no imports, read-only
git; all mutant/injection work on scratch copies (both units' plain
runs WRITE artifacts).  Standing rules as always (grades A/AWF/R;
recomputations counted; findings ranked with exact repairs).

## PANEL A — Γ-MAIN (paper-12; the settlement construction)

**Object:** paper `d85a629a9378`, code `51c3b4cf3f3c`, output
`b2b45be500b7`, receipt `974f36b1251a` (commit 822bb15, verified
#73).  Pin `8529ddc4a319`.  Reviews:
`v14/review-gmain-{operator,effectus,instrument}.md`.

- **K1 THE CONSTRUCTION'S FIDELITY:** rebuild Γ on the 113-class
  quotient from the pinned kernels independently; verify the
  column-stochastic exactness, the cut dims [1,5,13,45,113], the
  class law as the exact conditional (w(h)·k_r = w(h+e)); the
  23-sha provenance; the non-descent measurement (multi-valued on
  4/13) that forced the readout.
- **K2 THE TARGETS UNDER READOUT-RELATIVITY (decisive):** the COUNT
  readout hits both targets exactly; the OCCUPANCY readout (the
  process's own law) gives (3/8,1/4,3/8) at both legs; I-READOUT
  (fiber 2) is genuinely-free AND target-selecting.  Rule: is
  TARGETS-HIT an honest head at any motivated reading, or is the
  count readout selected BY the targets (the RSQ shape at the
  summit)?  Is there any pinned fact that forces either readout?
  The settlement's "targets TRUE" survives or dies here.
- **K3 THE HOLONOMY DEVIATION:** verify q reproducing ⟨2,3⟩ exactly;
  the k-enlargement ({5,13} via the 8 forced self-loops — re-derive
  them); Γ's rank-7 group; REC flat at all readings; the
  scramble control's caveat (the q-reading's group is not a
  discriminating statistic).  Then the successor question: does the
  quotient lattice between MENU and REC contain a carrier with BOTH
  descent and holonomy — derivable from the pinned artifacts, or a
  new construction?  Your answer shapes the next Γ iteration.
- **K4 THE QUANTUM-SHAPE CLAIMS:** the eq-22 refutation (negative
  entries — convention-dependent via the padding, or robust?); the
  non-Markov census (4/10 triples); the U3 one-pass being the known
  degenerate; the 44-squares result (curvature half non-unit at all
  44; the descent half not-a-loop).  What may the paper claim about
  Γ's quantum character at citable scope?
- **K5 INSTRUMENT:** 88 gates vs 36 mutants — the coverage audit at
  the #34/#62 standards; the never-falsified 2 (theorem-pass
  waivers verified); the era injection classes (verdict, table,
  render, path-drift, quotation-meaning); the ten-engraving
  compliance claims tested under attack; paper↔output↔receipt
  sweep; byte-identity; repo hashes unchanged after all work.

## PANEL B — R4 (paper-10; the defect on the stage)

**Object:** paper `f3e8cc1618f8`, code `b079bb3b8d55`, output
`58ec08893526`, receipt `3214f4da3af2` (commit 264cb54, verified
#55).  Pin `1582cea5df51`.  Reviews:
`v14/review-r4-{operator,effectus,instrument}.md`.

- **K1 THE DEFECT CENSUS:** rebuild the family and the 3,364-pair
  census independently (588 nonzero; the 8 exact values; column
  sums zero; Markov 0/1,792 both directions).  THE DERIVABILITY
  QUESTION (the era pattern): is the census analytic from the
  stencil/autocorrelation algebra — state what remains measured.
- **K2 THE L=4 UNIQUENESS THEOREM (decisive):** verify both halves
  (locality ⟺ L≥4 from the R2 anchor; non-monomial local axis
  unitaries ⟺ L≤4 with the ord≥5 monomial collapse); its
  stencil-class and alphabet scopes (the order-3 emptiness is
  declared alphabet-relative — verify); would a 5-point or Moore
  stencil reopen it (analysis, not sweep)?  The theorem's honest
  generality is the verdict's spine.
- **K3 THE TRANSFORMATION-TYPE CENSUS:** 22/38 classes; TRANSLATIONS
  ACT TRIVIALLY on the whole circulant family — is that a real
  finding or forced by circulant structure (derivable)?  The
  charge-without-momentum tension with K2's unique scale: state
  what a successor needs for motion-carrying types.
- **K4 THE GATES:** the realization-census gate's bite (150 nonzero
  excluded — verify the classifications and that the gate's
  exclusion is principled); the state-motion verdict
  (BACKGROUND-COEFFICIENT-OBSERVABLE-MOVES — the reconstruction
  from 16 point-mass responses verified); the light-cone and
  two-point tables; the projective-period gauge-invariance
  self-test.
- **K5 INSTRUMENT:** 77 gates vs 82 mutants at the #34/#62
  standards; the never-falsified 2 verified; injections incl.
  defect-zeroing (the Y1 class the unit claims to kill), class
  flips, table corruption, path drifts; the ten-engraving
  compliance under attack; paper↔output↔receipt; byte-identity;
  repo hashes unchanged.
