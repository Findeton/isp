# CR-A — HOSTILE REVIEW, EFFECTUS LENS (MEANING / SCOPE / MOTIVATION)

**Unit:** CR-A, refinement by accumulation — `v14/paper-05-accumulation.md`.
**Protocol:** v14 ledger #174 (frozen in-entry), **K2 decisive** — the
verdict's meaning POST-Γ.
**Pin:** `v14/note-cr-batch-pins.md`, section CR-A (frozen #30).
**Object:** commit `94df5ad` (CR-A delivered, v14 ledger #41, 2026-08-09).
**Lens:** what the measured objects are entitled to mean, at the standards
of CR-A's own era (birth-date fairness, the U4b precedent).

**Hashes verified at the start and re-verified at the end of this review
(sha256-12, the corpus convention):**

| artifact | expected | measured |
|---|---|---|
| `v14/paper-05-accumulation.md` | `af0058432b79` | `af0058432b79` ✓ |
| `v14/code/cra_accumulation_exact.py` | `e289d3afc852` | `e289d3afc852` ✓ |
| `v14/code/cra_accumulation_output.txt` | `f398959da079` | `f398959da079` ✓ |
| `v14/code/cra_accumulation_receipt.json` | `5f68bac811bd` | `5f68bac811bd` ✓ |
| `v14/note-cr-batch-pins.md` | `1cfee4fc0891` | `1cfee4fc0891` ✓ |

Five for five, working tree byte-identical to the commit. No drift.

**Grade: ACCEPT-WITH-FIXES.**

**Recomputations: 826.** 61 delivered aggregate quantities independently
reproduced and **756 per-cell trajectory verdicts** independently validated —
**817 exact matches, zero mismatches** — plus 9 probe quantities the delivery
does not print. My reimplementation shares no code, no predicate and no
short-circuit with the delivery: the readout written from the paper's own
equation, ADVANCING/ADMISSIBLE written from §4's prose, the probe-cell
dedup rebuilt from the (record, lapse, front, site) product, and convergence
decided by a **shrinking-residual test against the delivered limit** rather
than by any reimplementation of the POLY/EXPC window classifier. Only the
declared arena data (the nine records, the lapse family, the fronts, the
schedules) is read as data.

**I could not move a single delivered number.** The census reproduces to the
unit — 8192 / 2976 / 1232, LIN 1752 / 1209, BIL 1224 / 23 — the chart orbit
census reproduces (262144 → 133120 orbits, 4096 fixed, 4096 equivariant), the
closure fiber reproduces at 4 with its four named members, the
static-geometry theorem's algebraic core reproduces (the only coefficient
vector annihilating a single link's atom set is zero, in **both** families),
and all 531 exact limits, 117 divergences and 108 undefined normalizations
validate.

**The findings are entirely about meaning.** Three of them are MAJOR, and
all three are instances of one disease: **a quantity that varies across the
unit's own declared free axes is reported as a property of the pinned layer.**
That is a §15 violation, and §15 with its match-every-coordinate addendum was
engraved 2026-08-07 — two days before CR-A's pin froze, and the pin invokes
it by name ("RUNBOOK §13/§14/§15 with every addendum"). The findings are
era-fair.

---

## 1. K2.1 — DOES THE VERDICT STAND? **YES, unreservedly.**

### 1.1 The scope wording, checked line by line

`CRA-BLOCKED-AT-STATIC-GEOMETRY-<MISSING=A-GEOMETRY-UPDATE-LAW; …>` is a
statement about **the pinned layer**, and the paper says so at three
independent places, each of which I verified against the object:

1. **§2, the scope box** — "at ONE declared finite arena, inherited from I7
   and rebuilt here: d = 2, L = 3, the declared link set, the nine admissible
   geometry records, the twelve declared lapse profiles, the eleven declared
   drag rules, w = 0."
2. **§14, non-claim 2** — "No claim that any mover measured here is the
   geometry-update law, or that one exists. The census measures what the
   pinned grammar can express and what it can select; **it does not search
   outside the pinned grammar, which the pin forbids.**"
3. **§15, second paragraph** — "whether a geometry-writing move exists in any
   grammar deeper than the pinned layer (**the v10/v11 renewal grammar is
   outside this pin and was not read**)."

Point 3 is the decisive one and it is better than adequate: CR-A did not
merely scope itself away from the deeper grammar, **it named the place a
successor would have to look**, and the successor looked exactly there.
Γ-main builds Γ on D74's committed MENU quotient and Γ-iteration rebuilds it
on CONG-185 — a v10-lineage carrier, i.e. the grammar CR-A registered as
unread. Nothing the Γ campaign measured is about the object CR-A censused,
and nothing CR-A measured is contradicted.

**The layer CR-A censused was static, and it still is.** The three
box-independent statements — the pinned clauses force the identity and
nothing else; the stabilizer reduces but does not select; no pinned selector
cuts the fiber to one — are statements about the pinned clause table, not
about the coefficient box, and I confirm each: the clause table's only
constraint on Δ is `C-NOLAW` ("The geometry sector is frozen under H_a[N]"),
so the forced set is {Δ ≡ 0} whatever the box; the censused uniform space is
fixed pointwise by the chart action (64 points, all fixed), so no stabilizer
can select in it; and the strongest **pinned** selector leaves 1232.

**Ruling: the verdict STANDS as delivered. It requires no scope repair, no
erratum, and no weakening. The word BLOCKED is correct and the missing
object is correctly named — and, as §10 says and I verified against
`v13/paper-ha-successor.md`, it is named by the pinned source itself
("**No geometry-update law is constructed**"), not coined here.**

### 1.2 One thing the verdict string does not carry — and why it is not a finding

The string carries no arena coordinate (no `@…` scope segment of the kind
paper-19's carries). At CR-A's era that was standard: R6a's terminal verdict
is the bare token `R6A-NO-MOTIVATED-SPLIT`, and CR-B's is of the same shape.
Scope-in-the-string is a later practice. **Not a finding.** It is a register
item (§8, S-CRA-EF-7) for the adjudication, which should read the string
against the §2 box rather than alone.

---

## 2. K2.2 — THE LICENSED HISTORICAL SENTENCE, AND WHERE IT BELONGS

### 2.1 The offered sentence is true but is the weakest true one

"CR-A named the gap the Γ campaign filled" is defensible and I do not
contest it. But it is weak in two ways and misleading in one:

- CR-A did not **name** the gap first. The pinned HA source named it,
  verbatim, twice; CR-A **measured** it — that the pinned grammar can express
  8192 candidate movers, that 1232 of them survive every pinned criterion, and
  that no pinned clause picks one. Naming was quotation; measuring was the
  work.
- The Γ campaign did not fill the gap **at CR-A's layer**. Γ is a
  column-stochastic family between depth cuts on a menu/congruence carrier;
  CR-A's missing object is a writer into I7's interval-cardinality record
  s = {n_ℓ(x)}. Those become one object only through the welds, and the weld
  is what supplies the identification — paper-19's forced dictionary
  `[ACTOR→SITE | CO-DIVISION-ACTOR-PAIR→LINK | DIVISION-COUNT→n_ℓ(x)]`.

### 2.2 The sentence I can license, because I measured it

There is a much sharper historical fact available, and it is exact:

> **CR-A's accumulation limit and the programme's weld arena are the same
> matrix.** CR-A measured that the rescaled geometry of every link-uniform
> accumulation at this arena converges to the metric that makes all three
> declared links the same length — (1/27, −1/54, 1/27), the hexagonal Gram
> matrix normalized by |X|·|L| = 27. Paper-19's weld induces the record
> (1,1,1), q = [[1, −1/2], [−1/2, 1]], det = 3/4. **q(1,1,1)/27 =
> (1/27, −1/54, 1/27) exactly.** The record the programme welded on is the
> fixed point of the accumulation route CR-A censused — reached from two
> unrelated directions, CR-A's as a rescaled limit and U4b/paper-19's as the
> minimal budget-saturating configuration. CR-A also recorded, before the
> weld existed, that this limit lies **outside its own declared record
> family**; paper-19 independently records the same thing
> ("NOT-ONE-OF-ITS-11-DECLARED-RECORDS").

And the caution that must travel with it, which is CR-A's own §14 bar:

> The coincidence is **forced by the link declaration, not by physics.**
> CR-A's mechanism sentence — "the limit metric is the one that makes all
> three declared links the same length … a statement about the declared link
> set rather than about any mover" — is the reason the two derivations meet.
> Any reading of the weld's arena as a *selected* geometry is barred by
> CR-A's non-claim 5, and that bar transfers.

### 2.3 Where it belongs: **NOT a scope-annotation note.**

The paper-12 precedent does not apply, for two independent reasons, and the
U4b precedent (§4.2, "no erratum, no scope-annotation note … what is owed is
one cross-reference line") does.

1. **Paper-12's note exists because a later unit gave a different answer to an
   adjacent question at the same cell** — eq-22 negativity at MENU-113 (1,2,4)
   against rectangular feasibility. Nothing of that shape happened here.
   Nothing the Γ campaign or the welds measured narrows, contradicts or
   re-reads a single CR-A claim. There is no divergence cell to annotate.
2. **Paper-12 was TERMINAL and therefore untouchable. CR-A is
   `GREEN-UNREVIEWED`.** The whole machinery of a separate annotation note
   exists to avoid editing a frozen paper. CR-A is not frozen; this panel is
   its first review, and §3–§5 below require paper repairs anyway.

**Ruling: no note. The historical sentence of §2.2 goes in TWO places —**
(i) one row of the **CR-A adjudication's register**, as the cross-reference,
and (ii) **inside the repaired paper's §15**, where it belongs as a
successor-facing fact, since §15 is already the section that states what a
successor must carry. Writing a third file for a paper that is about to be
edited would be over-machinery, exactly as U4b ruled.

---

## 3. K2.3 — WHAT RETRO-BINDS THE COUPLING UNIT (paper-20, in construction)

The coupling pin's update semantics are, verbatim: "**counts increment per
emission; the walk's next step reads the updated n**". That is a
geometry-record-advancing move in CR-A's exact sense — the object CR-A
censused. CR-A therefore binds paper-20 in four measured ways. **Register
rows, ready to lift:**

**R-CRA→20-1 — the arena is CR-A's accumulation fixed point, and its
hexagonality is declared, not found.** Per §2.2. The coupling unit's arena
q = [[1,−1/2],[−1/2,1]] is (up to the factor 27) CR-A's mover-blind limit, and
CR-A's measured mechanism says it is a function of the declared link set
{e₁, e₂, e₁+e₂} and of nothing else. **paper-20 may not read the hexagonal
form of its stage as physically selected.** CR-A's non-claim 5 is the
governing sentence and should be cited by paper-20's walls.

**R-CRA→20-2 — the admissibility margin at the weld arena is three events
wide, and I computed it.** CR-A's admissible-record test is exactly the
instrument paper-20 needs per step, because the weld's arena sits at the edge
of it. From n ≡ 1, three co-division events deposited on the **diagonal link
alone** and none on the axes gives the record (1, 1, 4) — q = [[1,1],[1,1]],
**det = 0** — which is literally `G-SINGULAR`, one of the two records I7
itself rejects. **paper-20's update semantics must gate positive-definiteness
per emission, not assume it**; an emission rule that favours the diagonal
class leaves I7's admissible cone in three steps. CR-A's §6 mechanism (the
cross term driven by the diagonal against the axis sum) is the failure mode,
and it is now a concrete number rather than a caution.

**R-CRA→20-3 — the commutation theorem is why paper-20's REQUIREMENT gate has
the shape it has.** CR-A's static-geometry theorem — verified here at its
algebraic core — says that at the pinned layer, demanding the geometry move
commute with the dynamics forces the move to zero. The coupling pin's
`G-REQUIREMENT` asks for the *opposite*: a closure condition that FAILS
frozen and PASSES only when the counts update. **CR-A's theorem is the
positive reason that gate is well-posed** — commutation is precisely what a
geometry writer cannot have, so a coupled dynamics must be non-commuting or
inert. CR-A's open #4 then states the debt paper-20 currently does not
discharge: *"a successor that wants motion must say what it puts in
commutation's place and characterize the resulting defect."* The coupling pin
names no substitute compatibility notion. **This is the row that actually
binds a unit in flight**, and it should reach paper-20 before its head is
written.

**R-CRA→20-4 — CR-A's census cannot speak to paper-20's emission rule, and
must not be cited as if it could.** Per MAJOR-3 below: CR-A's bilinear atom
family is {N(x), N(x+ℓ)} × {n(x), n(x+ℓ), n_ℓ(x)} and contains **no
lapse×lapse product**. The forced dictionary defines n_ℓ(x) as the division
events on a **co-division pair** — events whose footprint contains *both*
endpoints, an AND. CR-A's four boundary conventions are all endpoint sums, an
OR. **The convention the corpus actually adopted is not in CR-A's design
space**, so no CR-A count constrains it. Any citation of "1232 unmotivated
movers" against paper-20's emission rule would be a category error.

---

## 4. K2.4 — THE FINDINGS

### MAJOR-1 — the mover-blind hexagonal limit is a property of ONE schedule of six, and the paper generalizes it to the accumulation route

This is the unit's most quotable positive result and the ledger #41 headline
("the mover-blind hexagonal limit"). The delivered numbers are all correct. The
reading is not.

The 90-cell class decomposes, exactly, as:

| mover | schedules reaching (1/27, −1/54, 1/27) | cells |
|---|---|---|
| `M-FIAT` | **6 of 6** | 54 |
| `M-CLOSED` | **1 of 6** — `SCH-CONST` only | 9 |
| `M-FRONT` | **1 of 6** — `SCH-CONST` only | 9 |
| `M-SOURCE` | **1 of 6** — `SCH-CONST` only | 9 |
| `M-TARGET` | **1 of 6** — `SCH-CONST` only | 9 |

§7's sentence "That class holds 90 of the 972 cells, **at all six
schedules**" is true *of the class* and false of every mover but the foreign
one. What follows it is not scoped at all:

> "So the accumulation route, run to its limit, produces a geometry fixed by
> the declaration of which links exist — and it produces the same one whether
> the events arrive from the lapse at both endpoints, from one endpoint, from
> the front, or from nothing at all by fiat."

**At the other five schedules it does not**, and the delivery's own
trajectory table says so cell by cell. At `SCH-RAMP0` the transfer movers
converge to **degenerate** forms — `M-CLOSED` at `G-ANISO` → (1/54, 0, 0),
`M-TARGET` at `G-FLAT` → (1/27, 0, 0), `M-SOURCE` at `G-FLAT` → (0, 0, 0) —
each with a vanishing q₂₂ or a vanishing form altogether, so the limit is not
a metric at all. At `SCH-DELTA-FIX` they converge to forms that are
**proportional to the hexagonal one but not equal to it** — `M-CLOSED` →
(1/6, −1/12, 1/6), `M-SOURCE` → (1/3, −1/6, 1/3), i.e. 4.5× and 9× the
claimed limit. The conformal class is often preserved; **the value, which is
what the paper claims is shared, is not.**

**And the mechanism at `SCH-CONST` is a degeneracy, which I measured:** at the
constant lapse, all five movers write a **site- and link-uniform** increment
at every step, at every record — `M-CLOSED` writes 2, `M-SOURCE`, `M-TARGET`
and `M-FIAT` write 1, and `M-FRONT` writes the front value, uniform because a
constant lapse keeps the front uniform. Under `NORM-INT` the scalar cancels
identically. So the "blindness" at `SCH-CONST` is the statement that four
movers which are *literally the same function up to a constant there* have
the same rescaled limit. It is close to a tautology at the one schedule where
it is measured.

*Repair (exact). Replace the two sentences with:*

> That class holds 90 of the 972 cells, at `NORM-INT`, and it is independent
> of the starting record at every one of them. It is **not** independent of
> the schedule, and the split is the point: the foreign move reaches the limit
> at all six schedules and the four transfer moves reach it at **one**,
> `SCH-CONST`. The mechanism there is measured and is a degeneracy of the
> constant lapse — at `SCH-CONST` all five write a site- and link-uniform
> increment at every step, so under a per-event denominator their scalars
> cancel and the limit is forced. At the five non-constant schedules the
> transfer movers separate: some limits are degenerate — a vanishing
> component, hence not a metric — and some are proportional to the hexagonal
> form at a different scale, but none of them is the value.
> **So the licensed statement is the existential one:
> there is a declared schedule at which the rescaled limit is reached by the
> foreign move and by four coupled ones alike, at every record. Convergence of
> the rescaled geometry is therefore not evidence that the mover is right —
> one schedule of six suffices to show that, and it is not claimed at the
> other five.**

The final inference — *"convergence is not evidence that the mover is right;
it is compatible with the mover being foreign"* — **SURVIVES**, and the repair
above preserves it. It only ever needed one witness schedule, and it has one
at all nine records.

*Verdict-string consequence:* the segment `MOVER-BLIND-LIMIT=YES` is
unqualified. It should read `MOVER-BLIND-LIMIT=YES@1-OF-6-SCHEDULES`, or the
adjudication must read the token as "blind at the constant schedule". The
ledger #41 headline needs the same qualifier.

### MAJOR-2 — §9's discriminator is measured at ONE undeclared coordinate of 3888, and the pinned readout in fact separates the movers at 207 of them

§9 asks the right question — "If the pin cannot forbid a mover, the next
question is whether it can at least tell two movers apart" — and answers it at
a single coordinate: record `G-OFFDIAG`, lapse `one`, front `n-ramp`, site
(0,0). I reproduce that coordinate exactly: 29 of 36 pairs separated, and the
identical seven indistinguishable pairs. **The number is right. The
conclusion drawn from it is a coordinate artifact.**

I swept the readout over all **3888 declared** (record, lapse, front, site)
coordinates — the unit's own probe product, no enlargement:

| indistinguishable pairs at the coordinate | coordinates |
|---|---|
| **1** | 90 |
| 2 | 63 |
| 3 | 81 |
| 4 | 126 |
| 5 | 405 |
| **7 (the delivered coordinate's value)** | 378 |
| 8 … 21 | 2745 |

- The **best declared coordinate** (`G-ANISO`, `ramp0`, `n-ramp`, site (2,0))
  separates **35 of 36** pairs, leaving only `M-FRONT|M-SOURCE`.
- **207 declared coordinates separate all six pairs inside
  {`M-FIAT`, `M-FRONT`, `M-SOURCE`, `M-TARGET`}** — the exact set §9 reports
  as inseparable.

So the sentence *"At that coordinate, the readout the pin supplies cannot see
the difference between a move that reads the lapse, a move that reads the
front, and a move that reads nothing"* is true and correctly hedged — but the
paragraph it supports, and **§15's open #3**, are not:

> "A successor claiming its law from a limit must exhibit a readout that
> separates its law from the fiat move."

**The pinned layer already supplies one, at 207 of its own declared
coordinates.** Open #3 as written demands of a successor something the pin is
measured to have. This is a §15 breach at CR-A's own birth standard: a
quantity that ranges from 1 to 21 across the declared free axes is used as a
conclusion, and §15 admits arena-artifacts "as instruments but never as
conclusions".

Compounding it: **the discriminator's coordinate appears in no deviation and
no disclosure.** §12's ten deviations price the box, the frozen probe, the
ADVANCING operationalization, the four selectors, the normalizations, the
horizon, the nine movers, the binary chart box and two falsifier scopes. The
one choice that determines §9's headline — which of 3888 coordinates to read
it at — is unlisted, with a measured fiber of 3888. Under the pin's own
imported discipline ("the R6a choice-inventory discipline applied to
advancing moves") that is a missing inventory row.

*Repair (exact), §9, replacing the section's conclusion:*

> Of 36 pairs of named movers, 29 are separated by the one-step record readout
> at the declared coordinate, and the seven that are not are
> `M-DILATE`/`M-LAPDIL` and every pair inside {`M-FIAT`, `M-FRONT`,
> `M-SOURCE`, `M-TARGET`}. **That count is a property of the coordinate, and
> the coordinate is a free choice with a fiber of 3888.** Over the whole
> declared probe product the separation count ranges from 15 of 36 to
> **35 of 36**, and **207 declared coordinates separate every pair inside the
> four-set above.** The licensed statement is therefore the opposite of a
> blindness result: **the pinned readout does discriminate these movers — it
> simply does not do so everywhere, and a claim read at one coordinate is a
> coordinate effect until the invariance is gated.** What the delivered
> coordinate shows is that a *badly chosen* readout cannot separate a foreign
> move from three coupled ones, which is a caution about readouts, not a
> deficiency of the pin.

*Repair, §15 open #3 — rewrite as a demand that is not already met:*

> 3. **A discriminator the limit can see, and a stated coordinate.** The
>    rescaled limit measured here is shared by five movers including a foreign
>    one at the constant schedule. The one-step readout separates them at 207
>    of the 3888 declared coordinates and fails to at others. A successor
>    claiming its law from a limit must therefore exhibit a separating readout
>    **and gate its separation as invariant across the declared free axes** —
>    the failure mode is not that no discriminator exists, but that one can be
>    read at a coordinate where it is blind.

*Add an inventory row (§12):* *the discriminator coordinate | **free** | fiber
3888 | selected first-in-enumeration; the separation count ranges 15–35 of 36
across it.*

### MAJOR-3 — the design space cannot express the boundary convention the corpus went on to force, and the omission is undeclared

§3 licenses twelve atoms from two verbatim quotes and declares a thirteenth as
a negative control, "so the gate admits 12 of 13 and can fail". The bilinear
family is exactly

  BIL = {N(x), N(x+ℓ)} × {n(x), n(x+ℓ), n_ℓ(x)}

— lapse × (front or interval count). **There is no lapse × lapse atom.** No
move in the 8192 can be sensitive to an event occurring at *both* endpoints of
a link at once.

That is precisely the relation the programme's terminal weld uses. Paper-19's
forced dictionary defines the count as "the division events **on that pair**
inside the declared window" — events whose footprint contains both x and
x+ℓ, an AND. CR-A's four boundary conventions (`M-CLOSED` = N(x) + N(x+ℓ),
`M-SOURCE`, `M-TARGET`, `M-FIAT`) are all endpoint **sums**, an OR.
`M-CLOSED`'s own gloss — "an event at *either* endpoint is in it" — makes the
difference explicit.

The consequence is scoped and precise, and it does **not** touch the verdict:

- The census counts and the class-(iii) fiber of 1232 are relative to a box
  that excludes the endpoint-coincidence atom. Deviation 1 declares the box as
  a box, which is honest; it does not declare **the shape of the bilinear
  family**, which is the load-bearing part.
- **§15's open #2 is understated.** It calls the boundary convention "a
  four-way choice the pinned semantics does not fix". It is at least
  five-way, and the fifth is the one the corpus adopted.
- The three box-independent statements are unaffected — they are about the
  clause table, not the atoms — and so is `CRA-BLOCKED-AT-STATIC-GEOMETRY`.

*Repair (exact), §3, after the atom table:*

> The bilinear family pairs each endpoint lapse with each of the front and the
> interval count. It contains no product of the two endpoint lapse values, so
> **no candidate in this design space is sensitive to an event committed at
> both endpoints of a link at once.** That is a declared restriction of the
> box and it is named because it is not innocuous: a semantics in which
> n_ℓ(x) counts events whose footprint *covers* the link — a co-incidence
> reading rather than an endpoint-sum reading — is outside this census, and
> nothing measured here constrains it.

*Repair, §15 open #2:*

> 2. **A resolution of the boundary convention.** Whether an event committed
>    at an endpoint lies in the interval is at least a five-way choice the
>    pinned semantics does not fix: the four endpoint-sum conventions censused
>    here — both, source, target, neither — and the **co-incidence** reading,
>    in which an event counts for (x, ℓ) only if it is committed at both
>    endpoints. The four censused ones are not equivalent, and the fifth is
>    outside this unit's declared atom family.

*Add an inventory row (§12):* *the bilinear family's shape | **declared** |
lapse × (front, count) only; no lapse × lapse product | the co-incidence
convention is thereby inexpressible.*

### MINOR-1 — §7's mechanism for the 108 undefined cells is false at 45 of them

> "The 108 undefined cells are `M-NONE` and `M-TILT` at `NORM-INT`, **where
> nothing is written into any interval** and the denominator is zero."

True for `M-NONE` (54 cells) and for `M-TILT` at `SCH-CONST` (9 cells, where a
constant lapse makes the front uniform and the tilt vanishes). **False at the
other 45.** I measured it: at each of the five non-constant schedules
`M-TILT` writes into 6 to 22 of the 27 (site, link) cells over the horizon,
at every record — yet the `NORM-INT` denominator is exactly 0 at every one.
The real mechanism is better than the stated one: on (ℤ₃)² the tilt
telescopes, Σₓ [n(x+ℓ) − n(x)] = 0 for every link, so the writes **cancel in
the total while the individual counts move**. I verified the net is exactly 0
at all 54 cells.

*Repair (exact):* *"The 108 undefined cells are `M-NONE` and `M-TILT` at
`NORM-INT`. For `M-NONE`, and for `M-TILT` at the constant schedule, nothing
is written and the denominator is zero. At the other 45 cells `M-TILT` writes
into as many as 22 of the 27 interval slots and the denominator is still zero,
for a better reason: on a periodic arena the front tilt telescopes, so the
writes sum to zero exactly. In both cases the normalization is undefined, not
the limit — but in the second case the geometry is genuinely moving under a
per-event denominator that cannot see it, which is itself a caution about
`NORM-INT`."*

### MINOR-2 — four of the nine named movers are not in the 1232

§7 opens: *"The pin asks the accumulation question for every admissible mover,
and the census leaves 1232 of them. Nine are named as the census's structural
classes."* That reads as a selection **from** the 1232. I measured the
membership:

| mover | advancing | admissible-advancing | in the 1232 |
|---|---|---|---|
| `M-SOURCE`, `M-FRONT`, `M-DILATE`, `M-LAPDIL`, `M-FIAT` | yes | yes | **yes** |
| `M-CLOSED`, `M-TARGET` | yes | **no** | no |
| `M-NONE`, `M-TILT` | **no** | no | no |

`M-NONE` is transparently handled (§8 says "blind, but not advancing").
`M-TILT` is not: it takes negative values wherever the front is non-uniform,
so it is not an advancing move at all. And §8's summary sentence — *"reports 7
coupled movers, **every one of them class-(iii) unmotivated**"* — is a
category error for the three of those seven outside the 1232; class (iii) is
defined as the residue of the admissible-advancing census.

The delivery is not hiding this: the receipt carries `admissible_throughout`
per trajectory cell and records 162 of 972 cells leaving admissibility (18
`M-CLOSED`, 54 `M-TARGET`, 90 `M-TILT`). **That field is never mentioned in
the paper**, and the convergence counts do not separate by it. For the record,
all 90 hexagonal-limit cells are admissible throughout — the headline is not
contaminated.

*Repair (exact), §7:* *"Nine movers are named as the design space's
structural classes and carried through the trajectory measurement. Five of
them lie in the admissible advancing census of 1232 — `M-SOURCE`, `M-FRONT`,
`M-DILATE`, `M-LAPDIL`, `M-FIAT`. Four are carried deliberately from outside
it: `M-CLOSED` and `M-TARGET` advance but fail the frozen-probe admissibility
filter, `M-NONE` is the pinned identity and does not advance, and `M-TILT`
takes negative values wherever the front is non-uniform. They are carried
because they are the boundary conventions and the drag's own ingredient, and
their exclusion from the census is itself a measurement. The receipt records
per-cell admissibility along the trajectory: 162 of the 972 cells leave the
positive-definite cone, all of them at those three movers, and none of them in
any class named in this section's conclusions."*

*Repair, §8:* replace *"every one of them class-(iii) unmotivated"* with
*"four of them class-(iii) unmotivated members of the census, and three
carried from outside it; none of them motivated by anything pinned"*.

### MINOR-3 — §1's control value 5 is forced arithmetic and reads as a measurement

*"the s-image of the whole pinned move monoid is a single point at depth 4,
against **5 points** when one geometry-writing generator is added."* The
singleton is a real measurement and the paper defends it correctly. The 5 is
not: the added generator writes +1 into every interval count, so at breadth-4
expansion it can be applied 0,1,2,3,4 times and the s-image has depth+1 = 5
points by construction. (The 341 visited nodes are likewise the full word
count over 4 generators, (4⁵−1)/3 = 341 — correctly reported as "no
truncation".)

*Repair:* *"…against 5 points when one geometry-writing generator is added —
which is depth + 1, the most that one uniform generator can produce, and is
reported as a positive control showing the instrument can move s at all, not
as a magnitude."*

### MINOR-4 — §7 contains two sentences that never meet

*"the four give different geometries"* (of the boundary conventions) and *"the
exact rescaled limit … is reached by `M-CLOSED`, `M-FIAT`, `M-FRONT`,
`M-SOURCE`, `M-TARGET`"* sit four paragraphs apart and are never reconciled.
Both are true; MAJOR-1's repair reconciles them (they differ at five
schedules of six and coincide at one, for a measured reason). Adding the
schedule qualifier closes this too — no separate repair needed.

---

## 5. K2.5 — THE CHOICE INVENTORY AND THE PROSE↔RECEIPT SWEEP, AT CR-A'S ERA

### 5.1 The prose↔receipt sweep: **clean, and better than its claim**

I extracted every numeral from the paper and tested it against every scalar
the receipt carries. **56 distinct numerals, 0 not carried.** All 11
receipt-rendered prose sentences appear verbatim in the file. §11's claim —
"every numeric token appearing anywhere in this file is required to be a
number the receipt object carries" — holds under my independent sweep, and
the #20 engraving (prose renders from the receipt, engraved at v14 #20, four
days before delivery) is met. **No finding.**

The three false statements I do report (MAJOR-1's generalization, MAJOR-2's
conclusion, MINOR-1's mechanism) are **not numbers** — they are mechanism and
scope sentences, the surface no sweep covers. That is worth registering: the
prose-numeral gate is doing its job, and this unit's failures live one level
up, in the sentences that say what the numbers mean.

### 5.2 The choice inventory at the era standard

The pin imports "the R6a choice-inventory discipline applied to advancing
moves", so every freedom must be classified forced / symmetry-fixed / free
with a counted fiber. §5's three-class table does this **for the coefficient
vector** and does it well. §12 prices ten further items. Four rows are
missing or mispriced:

| row | status | fiber | finding |
|---|---|---|---|
| the discriminator coordinate | **unlisted** | **3888** | MAJOR-2 |
| the bilinear family's shape | **unlisted** | ≥ 2 (endpoint-sum vs co-incidence) | MAJOR-3 |
| the six declared schedules | listed only implicitly, as arena data | 6 | MAJOR-1 — the mover-blind result is a function of this axis and the axis is not priced as one |
| `SCH-CONST` as the schedule at which the headline is read | **unlisted** | 6 | MAJOR-1 |

Deviations 1–10 are otherwise accurate and I sustain each: the box, the
frozen-probe filter, the ADVANCING operationalization, the four
unit-declared selectors, the two normalizations (correctly flagged as
disagreeing at 216 cells), the horizon with its t = 40 recheck, the nine
movers, the binary chart box, and the two falsifier-scope prices. Disclosures
X-PRECHECK (the #314 precheck doctrine, engraved 2026-08-09 and correctly
honoured), X-FORCED-CLAUSES (the #208 analytically-forced-clauses rule,
correctly honoured with a per-link positive control), X-BOX, X-HORIZON,
X-SELECTORS-NOT-PINNED and X-COMPANION-HASHES are all in order.

### 5.3 Birth-date fairness, stated explicitly

CR-A delivered at ledger #41. Engravings through #34 bound it; #46 (no
unanchored runtime inputs) and #62 (verbatim-anchor corrected spec) came
after and are **not** applied. §15 and its match-every-coordinate addendum
were engraved 2026-08-07, before the pin froze, and the pin names §15
explicitly — so MAJOR-1 and MAJOR-2, which are §15 findings, are in scope.
The scope-in-the-verdict-string practice is **later** and is not charged
(§1.2). The RSQ full-inventory standard is charged only through the pin's own
imported R6a discipline, which is what §5.2 does.

---

## 6. WHAT SURVIVES, IN ONE PARAGRAPH EACH

**The BLOCKED verdict — SUSTAINED, unchanged, at full strength.** The census
reproduces exactly, the clause table is verbatim and correctly read, the
missing object is the pinned source's own words, and the three
box-independent statements are true independently of every finding above. The
Γ campaign and both welds leave it untouched, because CR-A registered the
deeper grammar as unread and the deeper grammar is exactly where Γ was built.
This is the cleanest of the three: **nothing in this review moves it.**

**The static-geometry theorem — SUSTAINED, and its core verified
independently.** I confirmed the algebraic step the paper states rather than
measures: over the declared records, lapses and fronts, the only coefficient
vector annihilating a single link's atom set is zero — in **both** families,
at all three links. So requiring Δ ≡ 0 on any read link forces Δ ≡ 0
everywhere, the commuting fiber is {identity}, and 0 commuting advancing
movers at all 9 count-reading rules follows. The 9-rule scope via the
read-link-set argument is sound: the 11 rules realise exactly three read-link
sets and both nonempty ones are measured.

**The choice inventory and the foreign control — SUSTAINED.** The
FOREIGN/UNMOTIVATED distinction is real, correctly kept apart, and the
control does what a control must: `M-FIAT` is flagged by a measured property
(blind to every pinned field while advancing) and `M-NONE` is excluded by
another measured property (it does not advance), never by name. The audit's
disclosed price — that the foreign move is the best-behaved mover in the
trajectory census — is exactly the right thing to have printed.

**The mover-blind limit — SUSTAINED ONLY AS AN EXISTENTIAL, at one schedule.**
Per MAJOR-1. The inference it was built to support survives intact; the
generalization to "the accumulation route" does not.

**§9's discriminator — REVERSED.** Per MAJOR-2. The measurement is right and
the conclusion inverts: the pinned readout separates these movers at 207 of
its own 3888 declared coordinates, and the delivered coordinate is one of the
blinder ones. The unit's own open #3 is written against a deficiency the pin
does not have.

---

## 7. THE SUCCESSOR REGISTER

- **S-CRA-EF-1 — the four repairs above, then re-run.** MAJOR-1, MAJOR-2,
  MAJOR-3 and MINOR-1..3 are all prose-and-scope; none requires new
  computation except the two numbers I hand over (the 207 fully-separating
  coordinates and the 90-cell schedule decomposition), both of which the
  instrument can emit from objects it already builds. The verdict string gains
  one qualifier: `MOVER-BLIND-LIMIT=YES@1-OF-6-SCHEDULES`.
- **S-CRA-EF-2 — the historical row, per §2.2/§2.3.** One row in the CR-A
  adjudication's register and one paragraph in the repaired §15. **No
  scope-annotation note.** The sentence is the measured one — CR-A's
  accumulation fixed point *is* the weld's arena, q(1,1,1)/27 =
  (1/27, −1/54, 1/27) — carrying CR-A's own bar that the coincidence is
  forced by the link declaration.
- **S-CRA-EF-3 — the four coupling-unit rows, R-CRA→20-1..4 (§3).** Row 3
  (the commutation substitute) is the one that binds a unit **in flight** and
  should reach paper-20 before its head is written; row 2 (the three-event
  margin to `G-SINGULAR`) is a concrete per-step gate it can adopt today; row
  4 bars a citation that would otherwise look natural and be wrong.
- **S-CRA-EF-4 — the co-incidence convention as its own question (open).**
  CR-A censused the endpoint-sum family; paper-19 forced the co-incidence one.
  Nobody has censused the co-incidence family for a geometry-writing move.
  The natural successor is CR-A's census re-run over
  BIL′ = {N(x)N(x+ℓ)} ∪ BIL — one atom added, the same 47 gates — and the
  question is whether the added atom changes the class-(iii) fiber or the
  commuting fiber. My prediction is that it does not change the theorem (the
  single-link annihilator argument is atom-agnostic), which is precisely why
  it is worth measuring rather than assuming.
- **S-CRA-EF-5 — the `NORM-INT` blindness (open, from MINOR-1).** A
  normalization whose denominator is identically zero while the geometry moves
  is not merely undefined — it is a per-event accountancy that a conservation
  law can defeat. On this arena the tilt telescopes; on any arena with a
  periodic direction, some writer will. A successor adopting a per-event
  denominator must gate that its denominator is nonzero **for the reason it
  thinks** — i.e. that no write occurred — rather than by cancellation.
- **S-CRA-EF-6 — the trajectory-admissibility field (housekeeping).** The
  receipt carries `admissible_throughout` and the paper never reads it. Either
  surface it (MINOR-2's repair does) or drop it; a receipt field that no
  sentence consumes is an ungated measurement.
- **S-CRA-EF-7 — how the adjudication should read the string.** The verdict
  carries no arena coordinate, which is era-correct. The adjudication must
  therefore read it against §2's scope box, and must read
  `MOVER-BLIND-LIMIT=YES` as `@SCH-CONST` and the discriminator's absence
  from the string as fortunate.

---

## 8. RECOMPUTATION LEDGER

| block | quantities | result |
|---|---|---|
| records: 9 admissible (named), 2 rejected (named), 5 diagonal (named) | 3 | exact |
| arena: 12 lapses, 132 ordered pairs, 3888 probe product, 1010 / 809 dedup cells | 5 | exact |
| census: 4096/1752/1209, 4096/1224/23, 8192/2976/1232 | 9 | exact |
| choice inventory: 262144, 133120 orbits, 4096 fixed, 4096 equivariant, 64 uniform, forced 2, forced-advancing 0, classes i/ii/iii = 0/0/1232 | 10 | exact |
| selectors: fibers 2976 / 1232 / 4096 / 4 / 0 / 0 / 0, the 4 closure members, 11 rules, 9 count-reading, 2 count-blind (named), 2 nonempty read-link sets | 15 | exact |
| the static-geometry theorem's core: single-link annihilator = {0} at 3 links × 2 families | 6 | exact |
| arithmetic anchors: 2376, 341, 972, 486, 36, 81, 54 | 7 | exact |
| trajectory aggregates: 972, 531, 117, 108, 216, 27 limit classes, 216 flips, 90 hex cells, the 5 hex movers, M-FIAT 108/108 | 10 | exact |
| discriminator at the delivered coordinate: 36 pairs, 29 separated, the 7 named pairs, the 9 readout quadruples | 4 | exact |
| **delivered aggregate subtotal** | **61** | **61 exact, 0 mismatches** |
| **per-cell trajectory verdicts**: 531 CONVERGES rows each validated against its exact delivered limit by shrinking residual, 117 DIVERGES, 108 UNDEFINED | **756** | **756 exact, 0 mismatches** |
| probes not in the delivery: the 3888-coordinate discriminator sweep; the 207 fully-separating coordinates; the 35-of-36 best coordinate; the hex class's (mover, schedule) decomposition; the `SCH-CONST` uniform-writer lemma; the tilt's exact net-zero conservation at 54 of 54 cells; the 4-of-9 named movers outside the 1232; the 162 non-admissible trajectory cells; q(1,1,1)/27 = the hexagonal limit | 9 | — |

The 216 UNCLASSIFIED cells are not tested — my method decides convergence and
divergence but, by design, does not reproduce the delivery's declination
criterion. The delivery's characterization of them (the two non-stationary
schedules and the lapse-modulated dilation) is consistent with everything I
measured and I raise no finding against it.

Interpreter `/opt/homebrew/bin/python3.13`, exact `int`/`Fraction`
throughout; scratch at `…/scratchpad/cra-ef/`. Trajectories run to t = 512
for the residual test, against the delivery's t = 20.

**Closing hash re-verification:** paper `af0058432b79` ✓, code
`e289d3afc852` ✓, output `f398959da079` ✓, receipt `5f68bac811bd` ✓, pin
`1cfee4fc0891` ✓. Unchanged across the review.
