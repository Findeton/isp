# R6a — JOINT ADJUDICATION (operator #32, effectus #31, instrument #33)

**Adjudicator:** the coordinating agent.  **Inputs:** the three frozen
reviews (`dc01972c34f1` / `e1da7d3125bb` / `8d05097f3d0f`), the frozen
delivery (paper `af5b7f26e427`, code `ea914c6b55aa`, output
`a04b97d2b7bc`, receipt `022c3f488a93`), protocol (`02d249f22f6f`),
pin (`a22582f67168`).  **Standing:** three ACCEPT-WITH-FIXES; ~975
recomputations + ~61M sweep evaluations; zero numerical
disagreements; **zero false numbers — the campaign's first clean prose
surface**.  Resolved grade: **ACCEPT-WITH-FIXES**.

## 1. The verdict: HOLDS, with its anatomy rewritten

**NO-MOTIVATED-SPLIT survives every hunt.**  Operator: nine fixing
structures tested, none derivable from the pinned sources.  Effectus:
no missed forcing (symmetry, admissibility 0-of-20,356, arithmetic,
extremal, dynamics, front semantics); the split freedom is NOT gauge
(split↦defect injective over the full rule family).  Instrument: the
comparator genuinely independent, both alternative heads reachable.
The trichotomy is sound at pinned scope, with "fixed in distribution"
empty-by-declaration (to be disclosed) and asymptotic fixing blocked
by the unit's own ceiling.

**But the mechanism story was wrong in three ways, all cross-confirmed:**

1. **The coboundary theorem is not the split's mechanism** (effectus
   F3 ∥ operator ∥ instrument): the split fiber is exactly
   ∏(n_ℓ−1) and a granted potential leaves it IDENTICAL (verified at
   a 23-digit value); the theorem is the mechanism of
   FREE-TRANSVERSE-LINKS (potential ⟹ fiber INFINITE → 1).
2. **The theorem itself is grammar-wide on the torus but
   torus-scoped** (operator's characterization: coboundary ⟺ zero
   axis-cycle sums ∧ triangle relations; positivity forbids the
   former for every record — AND instrument's discovery: **five of
   nine records — the q₁₂ = 0 sector, G-FLAT and G-DIAG2 included —
   ARE coboundaries on the universal cover ℤ²**, where G-DIAG2's
   cover-potential, interpolated by the very rule §6.1 called
   dynamics-forced, yields a fully admissible canonical refinement
   with split fiber 1.  On that sector the obstruction is **periodic
   holonomy**, not information.  §2's "mechanism of everything"
   sentence retires; the three-wall anatomy replaces it: the split's
   wall is combinatorial multiplicity; the transverse links' wall is
   the (torus) coboundary theorem; the q₁₂=0 sector's wall is
   periodicity alone.
3. **"The dynamics forces an inadmissible value" is rule-relative**
   (operator ∥ effectus, the counterexample unreported in the
   delivered census): A-chart/B-chart force the declared RIGHT lift —
   integral, admissible, exact image agreement 4,536/4,536; B-all is
   underdetermined; and A-notransport is implemented identically to
   A-insert (10 distinct rules, not 11).

Also dead: the count-1-floor reading (a scale artifact — flat
records at other scales refine to ceiling 2); LATTICE-FORCED-1-OF-361
(structurally empty: uniqueness ⟺ all counts 2).  K4 resolved: BLOCKED
stays out of the head (the hyperplane branch ends at the same wall);
the per-class table is the right carrier.

## 2. The instrument findings

Twelve injection classes survive (post-gate receipt cells; a dropped
move class under a self-comparing completeness gate — #219 at
G-FIBER-COMPUTED and G-MOVE-CENSUS-CELL-COMPLETE; zeroed defect
values under green defect gates; four #24 typed constants; false
waiver text; a semantic inversion under green verbatim anchors).
**The waiver audit: 12 false waivers by name — 10 gates SHADOWED
(never evaluated on any path), 11 of 20 waived gates killed by
targeted injections.**  The verbatim-text anchor kind is ADOPTED WITH
MODIFICATION (evaluate before byte anchors; bind each row to a named
consumer gate; anchor context windows, not fragments).

## 3. Binding repair orders (R-R6A-1 … R-R6A-10)

1. **The mechanism rewrite**: §2 and the MECHANISM segment carry the
   three-wall anatomy (split = ∏(n_ℓ−1) multiplicity; transverse
   links = the torus coboundary theorem, stated with its
   characterization; q₁₂=0 sector = periodic holonomy, with the
   universal-cover canonical refinement measured in-unit as a
   disclosure — cover objects are NOT pinned, so it cannot flip the
   verdict, and the paper says exactly that); the potential
   counterfactual corrected (effectus F3's arithmetic).
2. **Head per effectus minimum**: keep NO-MOTIVATED-SPLIT, add the
   corrected MECHANISM segment and the five measured relativities
   (d=3 gap, lift-, family-, completion-, rule-relativity) as
   computed segments.
3. **The dynamics claims rule-scoped**: the forced-lift table printed
   per rule (count-weighted at 2; RIGHT at A-chart/B-chart with the
   4,536/4,536 agreement; underdetermined at B-all; third form at
   six); the veto sentence replaced; A-notransport's duplication
   fixed (10 rules) or the distinct rule implemented.
4. **The twelve injection classes hardened**: render/receipt cells
   gated post-write; the move-census completeness gate rebuilt
   against an independent constructor (#219); fiber recomputation
   genuinely independent; defect gates bound to the value census;
   the four typed constants computed; waiver text rendered from the
   census.
5. **The waiver ledger rebuilt**: shadowed gates either wired into
   the execution path with real falsifiers or removed as dead code
   (a shadowed gate is not a waived gate); every surviving waiver's
   forcing verified by the unit itself (waiver claims are gate
   claims); the instrument's 11 successful kills become declared
   mutants where applicable.
6. **Verbatim anchors per the adopted modification** (order,
   consumer-binding, context windows).
7. **Scope/denominator fixes**: the 207→650 family scope; the typed
   "six records"; "30 of 81" scoped to its record×rule cell;
   effectus F5/F6 and operator F8's unrendered sentence brought
   under the prose gate.
8. **Weak-control fixes**: the 3,976 zero-cell control named as the
   both-drags-vanish sector (operator F4); the restriction test
   disclosed as a corollary of additivity (operator F6) with the
   independent-comparator claim withdrawn; the 16-completion sweep
   restated via its f₀≠f₁ criterion with the wider census printed.
9. **Paper rewrite** (single-threaded): the finding is the sound
   verdict + the three-wall anatomy + the reopening leads recorded
   as successor inputs; no correction narrative.
10. **The reopening-leads register** (a section of the paper and of
    this note, for the charter): (i) **R6b′ — the record-type
    discriminator** (pin the v10/v11 renewal/event-position row;
    re-run this audit verbatim); (ii) **the extremal principle**
    (max-det selects uniquely 9-of-9 on all six splittable records —
    motivated iff a deeper row declares a variational principle);
    (iii) **the universal-cover route** (the q₁₂=0 sector refines
    canonically on ℤ² — motivated iff a deeper row de-periodizes the
    arena or pins cover objects).  All three are charter decisions —
    the user's.

## 4. RUNBOOK engravings (this adjudication)

- **§14 addendum (waiver claims are gate claims):** a never-falsified
  waiver naming a mutant or a forcing is itself a claim requiring
  verification — the named mutant must reach and be killed by the
  gate, or the forcing must be machine-checked; a gate no execution
  path evaluates is dead code and may not appear as waived.
- **§14 addendum (verbatim-text anchors, adopted):** the anchor kind
  is standard, with the three modifications binding: evaluated before
  byte anchors, each row bound to a named consumer gate, context
  windows anchored rather than fragments.

## 5. The programme statement (corrected, and the R6b ruling)

Earned: **CONTINUUM-NOT-POSABLE-AT-THE-PINNED-GRAMMAR-ROWS (2 of 2)**
— and the two walls are different walls (R1: the declared family
degenerates to copying; R6a: the pinned record type is extensive).
"The continuum is at most effective" is narrative and is retired from
adjudicator language.  **R6b: RE-SCOPED, not retired** — R6b-as-written
is unrunnable for a measured reason (ceiling 2 attained; the free part
grows); its successor design is R6b′ (the record-type discriminator),
standing beside the operator's extremal lead, the instrument's cover
lead, and the four CR units in flight.  Charter changes are the
user's call.
