# The renewal-grain transport

**v14 R6b′ — paper-09.**  Does a motivated identification carry the
deep corpus's renewal-grain positional structure to the spatial
record-interval arena, and does the derived kernel collapse the R6a
split fiber?

**Status:** GREEN-UNREVIEWED.
**Pin:** `v14/note-r6bprime-transport-pin.md` (frozen, v14 ledger #43).
**Instrument:** `v14/code/r6bp_transport_exact.py` with
`_output.txt` and `_receipt.json`; exact `int`/`Fraction` arithmetic
under an AST float guard; two plain runs byte-identical.
**Discipline:** RUNBOOK §13/§14/§15 with every addendum, all seven
2026-08-09 engravings gated at birth, each compliance claim shipping
its injection-falsifier.

---

## 1. The question, and why it is a transport question

R6a proved that the pinned record grammar admits no motivated
interval-subdivision move, and located the mechanism: the datum a
split needs — where inside an interval the division events sit — is
exactly the datum the record does not carry.  CR-B then asked whether
a motivated split *distribution* exists and returned
`CRB-BLOCKED-AT-NO-PINNED-STOCHASTIC-LAW`, naming the missing object
in its own receipt: *the interval positional law — the transition
kernel between an interval's endpoints whose renewal count is n* —
and observing that R0 carries the record layer and no transition
layer.

The deep corpus does carry a transition layer, at renewal grain.  So
the question this unit asks is not whether the stratum exists.  It is
whether the stratum **transports**: whether a motivated identification
carries the renewal-grain positional structure of v10/v11 to I7's
spatial interval arena, and if so whether the induced law collapses
R6a's split fiber to a forced value or a forced distribution.

The answer is composite, and every part of it is first-class.

---

## 2. What is assumed

Six deep rows, hash-pinned by this unit's pin, each carried with its
pedigree; plus I7, the baseline arena row of the founding pin.

| row | supplies | pedigree carried |
|---|---|---|
| S1 the transition matrix | the exact `6`×`6` transfer, λ = `2`, the completed chain q′, the named renewal state, root = renewal | TERMINAL (v10 #349) |
| S2 the positional census | the exhaustive intervening-pattern census inside inter-renewal intervals at lengths `3` and `4`, with exact leaf counts | TERMINAL (v11 #20–#21); **interval lengths are ENSEMBLE DATA (declared)** |
| S3 the type declaration | division event ≡ renewal; "the bridges" = the interval; Γ(cut′←cut) declared TO BE CONSTRUCTED | founding paper; v11 frozen |
| S4 the renewal theorem | every pair arbitration is a renewal to the root; the elementary-click refinement | TERMINAL (v10 #326, ten hostile rounds; its header's paper-level-review-open note carried) |
| S5 the declared positional law | the continuous positional layer, **with its own verbatim disclaimer that the waiting law and rates are chosen, not derived** | D34b TERMINAL delta |
| S6 the extremal standard | the frozen bar any extremal selector must clear | audit (completed pre-hostile-review — carried) + finite nonselection theorem |
| I7 | the record layer: sites, links, and n_ℓ(x) — the number of division events in the record interval between x and x+ℓ | v14 R0 founding pin |

R6a's split-fiber values and CR-B's simplex dimensions are anchored at
their receipts.  R6a is on the terminal track with its repair in
flight: this unit consumes the delivered-committed receipt bytes the
pin names, discloses the working tree's in-flight bytes beside them,
and gates that every consumed path-value is identical across the two.

**Named exclusions, binding and honoured.**  `u1c` is NOT CITABLE and
appears only as a registered lead with its status printed;
THE-THEORY-SO-FAR is index-only; v12's Γ objects are arena-free and are
not a source; d70 is excluded.  The instrument gates that no
named-excluded artifact is cited: the count is `0`.

---

## 3. The derived kernel, and the defective-renewal finding

S1's chain is rebuilt by two genuinely independent routes inside S1's
own artifacts — route P parses the `6`×`6` transfer out of paper 31's
fenced prose block, route C AST-extracts the referee matrix and the
harmonic vector from d43b's Python source objects.  The two artifacts
are distinct files in distinct formats with distinct hashes, and the
routes agree entry by entry.  The completed transfer
q′(i→j) = T_ij f_j / (2 f_i) normalises exactly on all six rows and
reproduces S1's own printed conflict row.

**The renewal state is transient.**  The completed chain has exactly
one closed communicating class, `{2, 4, 5}`; the renewal state lies in
the transient part, and `3 → 0` is the renewal loop.  The exact
hitting probabilities are `0` on the closed class, `1/4` from the
one-live-proposal state and `4/7` from the conflict state, so

> **P(return to the renewal state | at the renewal state) = `13/16`,
> defect `3/16`.**

The number of renewals after any renewal is therefore geometric, hence
almost surely finite: **the delivery-free renewal process terminates
almost surely.**  The expected total number of visits to the renewal
state is `16/3`, and the mean inter-renewal length conditional on
return is `21/13`.

**The inter-renewal law, exact, and its hole.**  Computed two ways —
in closed form and by an exact taboo iteration of q′ to `400` terms,
agreeing termwise —

> f(1) = `3/4`,  f(2) = `0`,  f(n) = (n−2)(3/4)^(n−3)/`256` for n ≥ `3`,

with total mass `13/16` and a residual tail below `1e-40` beyond the
cap.  So f(3) = `1/256` and f(4) = `3/512`.  **The support has a hole
at 2**: no inter-renewal leg of length two exists, exactly.

This is stated at **delivery-free scope**, which is S1's and S4's own
scope — S4's theorem reads verbatim *"every pair arbitration is a
renewal to the root state [THEOREM at two-actor delivery-free
scope]"*.  S1's own artifact declares what changes at transport scope:

> *"At transport scope the picture will change and is declared:
> deliveries reopen the absorbing sector (diverged holdings can
> reconverge), the class structure of §3.2 is not stable under the
> transport grammar, and the Martin-boundary machinery that this
> section did not need is expected to become load-bearing exactly
> there."*

Every headline below therefore carries the delivery-free qualifier.

---

## 4. The positional census, and the scope seam

S2 censuses the intervening patterns of an inter-renewal leg
exhaustively.  At leg length `3` there is exactly one pattern class;
at leg length `4` there are five, each carrying exactly two proposals
plus one delivery or one idle, so the pattern class determines a
distinguished **interior position** in {1, 2, 3}.  The leaf counts are
`1024`/`512`/`512`/`1024`/`512` at chain position one (total `3584`)
and `24576`/`8192`/`8192`/`24576`/`8192` at chain position two (total
`73728`).

Read as a law on the interior position:

| scope | chain position one | chain position two | total variation |
|---|---|---|---|
| transport | (`3/7`, `1/7`, `3/7`) | (`4/9`, `1/9`, `4/9`) | `2/63` |
| delivery-free | (`1/3`, `1/3`, `1/3`) | (`1/3`, `1/3`, `1/3`) | `0` |

**At transport scope the positional law is chain-position dependent.
At delivery-free scope it is exactly uniform at both censused
positions.**  The entire position dependence is carried by the
delivery-bearing patterns — and deliveries lie outside the scope at
which the kernel of §3 is derived.  Deleting them retains `3/7` of
S2's leg-one mass and `1/3` of its leg-two mass.

That is the seam.  S1 and S4 are delivery-free; S2 declares itself
*"Transport scope (d42b1) only"*; and S1 declares the class structure
is not stable across the change.  A composite kernel must choose a
scope, the two scopes give measurably different laws, and no pinned
row chooses.

S2's leg lengths are **ensemble data**: the renewal positions are
declared by the ensembles, not sampled from any kernel.  No frequency
in this paper is read as a kernel probability.

---

## 5. The identification census

`5` identification candidates are expressible from the pinned rows.
Each carries a choice inventory whose items are classed forced /
stabilizer-fixed / genuinely free with exact fibers, and the
motivation qualifier is computed from the inventory: motivated iff
zero free items.

| candidate | forced | stabilizer | free | qualifier |
|---|---|---|---|---|
| C1 count-match-length (the pin's literal reading) | 3 | 1 | 3 | UNMOTIVATED |
| C2 count-match-legs (the type-honest reading) | 1 | 0 | 5 | UNMOTIVATED |
| C3 the bridges reading (S3's verbatim declaration) | 2 | 0 | 1 | UNMOTIVATED |
| C4 the elementary-click refinement | 1 | 0 | 3 | UNMOTIVATED |
| C5 the continuous comparator (S5, labelled) | 0 | 1 | 1 | UNMOTIVATED |

**Motivated identifications: `0` of `5`.**

Three findings inside the inventory are worth naming.

**The type census.**  n_ℓ counts division events; an inter-renewal leg
contains exactly one division event.  C1 — the pin's literal
"n_ℓ events ↔ a length-n_ℓ leg" — therefore equates a division-event
count with a grammar-event count.  C2 is the type-honest repair, and
it pays for the repair with four extra free items, because the record
fixes only the number of legs and neither their lengths nor the
halving rule.

**The bridges reading lands where there is nothing to split.**  S3
declares verbatim that *"The bridges are the conflict windows between
renewals"*.  A bridge's interior carries no division event, so the
reading is admissible exactly at count one — and `29` of the `201`
censused intervals carry count one, every one of them with split fiber
`0`.  The one type-honest reading of S3's own declaration reaches only
intervals that cannot be subdivided.

**The first stabilizer-fixed items in this line.**  R6a's inventory
had zero stabilizer-fixed entries.  Two appear here, both measured:
the interval's orientation, because the induced position law is
reversal-invariant at both scopes and both censused positions; and
S5's clock rate, because conditioned on the event count the positions
of a Poisson process are uniform order statistics and the rate cancels.

---

## 6. The fiber collapse

Classify every censused interval by what the derived kernel can say
about it.  The arena carries `201` intervals over `9` admissible
records — the seven homogeneous ones at all `9` sites, the two
inhomogeneous ones at the two sites the R6a receipt prints, a declared
cap.

| class | intervals |
|---|---|
| no split (count 1; R6a fiber 0) | `29` |
| kernel hole (count 2; f(2) = `0` exactly) | `50` |
| stratum empty (count 3; one pattern class) | `20` |
| **collapsed in distribution (count 4)** | **`37`** |
| uncensused by S2 (count ≥ 5) | `65` |

> **The derived kernel collapses the R6a split fiber on `37` of the
> `201` censused intervals — `37` of the `122` that carry a
> non-trivial fiber — and the collapse is IN DISTRIBUTION, never to a
> value.**

At delivery-free scope the induced split law at count four is uniform,
with `3` maximisers; at transport scope it is (`3/7`, `1/7`, `3/7`),
with `2`.  No functional of the derived law names a split.

Stated in CR-B's own coordinates: at count four CR-B measured a fiber
of `3` and an invariant-measure simplex of dimension `2` under the
pinned symmetry.  **The derived kernel selects one point of that
simplex: dimension `2` → `0`, at count four, at delivery-free scope.**

Two further measurements sharpen it.

**One record is completely covered.**  Exactly `1` record of the
family, G-DIAG2, has every splittable interval at count four and every
other interval at fiber one.  On it the derived kernel supplies a
complete law on R6a's entire split fiber: the uniform law on `19683`
elements, whose raw and admissible-at-images sizes coincide.  That is
the law CR-B classed REFUTED-AS-FORCING for want of a declared
support — and the support is now derived rather than chosen.

**The two positional layers disagree.**  S5's continuous layer places
interior positions as uniform order statistics, giving the binomial
split law (`2/7`, `3/7`, `2/7`) at count four, separated from the
derived delivery-free law by total variation `2/21`.  That number
reproduces CR-B's own anchored uniform-versus-binomial total variation
at count four, computed by a different unit on a different route.  One
layer is derived and one is chosen; they do not agree; and S5's own
disclaimer — *"the coefficients 1/4 are chosen, not derived"* — is
carried wherever the continuous layer is touched.

---

## 7. The R6a audit, re-run at the enriched type

R6a's four genuinely-free freedoms, re-classed with the six rows in
hand.

| freedom | R6a | at the enriched type |
|---|---|---|
| THE-SPLIT | class (iii), fiber `19683` … `1257565061957837936381` | class (ii) **in distribution**, at count four only, at delivery-free scope only — `37` of `201` intervals |
| FREE-TRANSVERSE-LINKS | class (iii), fiber infinite, `54` of `108` | **unchanged**: the kernel is count-indexed and these links carry no count — `0` of `54` forced |
| NEW-FRONT-VALUES | class (iii), fiber infinite | **forced relative to the split**, fiber `1`: front(new) = front(x) + n₁, integral by construction |
| THE-LIFT-PAIR | class (iii), fiber `2` | **unchanged, and the freedom grows**: the transported rule is a third rule |

The forced part is re-verified unchanged.  Count additivity holds on
`647` split constraints of this unit's own enumeration with `0`
violations, and the readout recovers every censused count from the
rebuilt form, `67` of `67`.  R6a's own figures (`972` additivity checks
with `0` violations; `324` of `324` restriction checks) are anchored
separately as path-values; the two enumerations are different objects
and no ratio is formed between them.

Two of these re-classifications deserve their names.

The **front rule** is the unit's one clear gain.  The same count
semantics that forces additivity also forces the front at a new site
once the split is fixed, and it forces an integer.  R6a's
dynamics-forced count-weighted lift is non-integral at `30` of its
`81` cells; the transported rule is integral everywhere on this unit's
grid.  But it coincides with the count-weighted interpolation only
where the record's counts are a coboundary, and R6a's gated theorem is
that no record's counts are — so the enriched type does not select
inside R6a's lift pair.  **It adds a third admissible rule to a fiber
of `2`.**  Freedom grows where it was expected to shrink.

The **transverse links** are untouched for a reason that is structural
rather than accidental: an enrichment indexed by interval counts has
no index at all for a link that lies on no interval.

Also independently rebuilt here, from the count data alone and not
routed through R6a's own fiber code: R6a's raw split fibers on the `8`
records the printed-site data determines, and its equivariant fibers on
the `5` homogeneous splittable records.  All agree.  The rebuild is
the product over links of (n_ℓ − 1), which is the adjudication's
mechanism statement made constructive.

---

## 8. The extremal lead against the S6 bar

S6's bar: *"A proposed selector Q derives a unique law only if the
frozen SHARD premises plus Q have exactly one physical equivalence
class of models"*, and it is not enough that Q chooses a coefficient
after its support, reference measure and constraints have been
supplied, or selects one member of a preselected toy family.

Max-det does select uniquely.  So do three other functionals, and they
do not agree.  On the pinned arena's own count-four interval, `4`
record-intrinsic functionals — each computed from the record alone,
none supplied a base measure — return `3` distinct selections:
max-det and max-balance pick the middle split, max-left-count picks
the last, and max-|q₁₂| ties on the two ends.  That is the
countermodel, and it is D12's own structure: twins the selector cannot
separate, separated instead by the choice of selector.

The record-intrinsic test fails at a second, independent point.  The
arena's declared readout is `I = q⁻¹(det q)^w at w = 0` — the
determinant enters at exponent zero, so **the declared readout is
det-blind**.  Max-det optimises a functional the declared record does
not read.

And the deep rows do not rescue it: `0` of the `6` pinned rows declare
a variational principle (S6 refutes the class outright), and the
derived delivery-free law assigns max-det's selection probability
`1/3`, not `1`.

**Outcome: DIES-AT-THE-BAR.**

---

## 9. The cover lead, dissolved

R6a's universal-cover route is motivated iff a deeper row de-periodizes
the declared arena or pins cover objects.  Of the `6` pinned rows, `0`
pin a cover object and `0` de-periodize the arena.  The deep arenas are
rooted generated objects, not periodic lattices: S4 declares
*"Truncated completions are therefore depth-non-stationary: rooted"*
and S1 declares *"its state count grows with depth (17, 23, 29 at
depths 4, 5, 6)"*.  A rooted, depth-non-stationary arena carries no
deck group, so it supplies no cover object for a torus it never
mentions.  Recorded DISSOLVED; no hunt is run.

---

## 10. Controls

Three negative controls, all failing the audit by distinct measured
modes.  The R1 copying move re-posed as an identification forces no
constraint and leaves `6` intervals unrepresented.  An identification
reaching a named-excluded artifact is refused at source, and the
instrument gates that `0` such artifacts are cited.  And the census's
own control — identify an interval of count n with a leg of length
n+1 — fails on a measured cardinality mismatch: the leg offers n
interior positions against n−1 admissible splits, so the induced map
is not a bijection and additivity is not represented.

An audit that could not fail a move would not be an instrument.

---

## 11. The verdict

```
R6BP-KERNEL-DERIVED<IDENT=C1-COUNT-MATCH-LENGTH|LAW=FIRST-RETURN-f1-3/4-f2-0-fn-(n-2)(3/4)^(n-3)/256|COLLAPSE=DISTRIBUTION-UNIFORM-AT-COUNT-4|COVERAGE=37-OF-201-INTERVALS|CRB-SIMPLEX-DIM-2-TO-0|SCOPE=DELIVERY-FREE>
R6BP-TRANSPORT-UNMOTIVATED<CANDIDATES=5|MOTIVATED=0|FREE-ITEMS=C1-COUNT-MATCH-LENGTH:3+C2-COUNT-MATCH-LEGS:5+C3-THE-BRIDGES-READING:1+C4-ELEMENTARY-CLICK-REFINEMENT:3+C5-CONTINUOUS-PLACEMENT-COMPARATOR:1|DECISIVE=I-SCOPE-AND-I-POSITION|TV-BETWEEN-CHAIN-POSITIONS-TRANSPORT=2/63|TV-DELIVERY-FREE=0|BRIDGES-READING-REACHES-ONLY-COUNT-1-29-OF-201-WITH-FIBER-0>
R6BP-KERNEL-DEFECTIVE<RETURN=13/16|DEFECT=3/16|CLOSED-CLASS={2,4,5}|TERMINATES-A-S=TRUE|VISITS=16/3|SUPPORT-HOLE-AT-2-COSTS-50-OF-201-INTERVALS|SCOPE=DELIVERY-FREE>
R6BP-BLOCKED-AT-THE-SCOPE-SEAM<S1-DELIVERY-FREE|S2-TRANSPORT|DELIVERY-FREE-MASS-LEG1=3/7|LEG2=1/3|S1-DECLARES-THE-CLASS-STRUCTURE-NOT-STABLE-ACROSS-IT>
EXTREMAL-BAR=DIES-AT-THE-BAR<FUNCTIONALS=4|DISTINCT-SELECTIONS=3|MAX-DET-UNIQUE=TRUE|READOUT-DET-BLIND-AT-W-0=TRUE|VARIATIONAL-ROWS-0-OF-6|DERIVED-LAW-RATIFIES-AT-1/3>
COVER=DISSOLVED<ROWS-PINNING-COVER-OBJECTS-0-OF-6|ROWS-DE-PERIODIZING-0-OF-6|DEEP-ARENAS-ROOTED-AND-DEPTH-NON-STATIONARY|NO-HUNT>
CONTROLS=R1-COPY-UNMOTIVATED|EXTERNAL-REFUSED-AT-SOURCE|COUNT-MISMATCH-FAILS-THE-CENSUS|EXCLUDED-CITED-0>
```

Read plainly: **the kernel exists and is derivable; it is defective;
where it speaks it forces a distribution and never a value; it speaks
on a minority of the arena; and no identification that carries it
there is motivated, because the two rows that would jointly supply the
positional law live at different scopes and the pinned corpus declares
that seam is not innocuous.**

The stratum exists.  Its transport does not.

---

## 12. Scope, caps, and what this unit does not do

Scope qualifiers are mandatory and are carried at every claim.  S1's
chain and S4's renewal theorem are two-actor delivery-free; S2's
census is transport scope, and its interval lengths are ensemble data;
S5's continuous layer is chosen, not derived, and is used only as a
labelled comparator.

Declared caps.  The two inhomogeneous records are censused at the two
sites the R6a receipt prints; the remaining seven sites of each are
uncensused, and one record's fiber is consequently not rebuilt.  The
first-return law is iterated to `400` terms with the residual tail
bounded exactly.  S2 censuses chain positions one and two only, so
position-independence at delivery-free scope is measured at two
positions, not proved at all.

Not done.  This unit does not construct Γ(cut′←cut) — S3 declares it
TO BE CONSTRUCTED and building it is a future unit.  It runs no
scaling census.  It does not touch the transport-scope renewal arm.
It claims nothing about the continuum beyond what the fiber-collapse
measurement earns.

**Registered lead, status printed:** `v11/note-u1c-depth15-two-sided.md`
is GREEN-UNREVIEWED and NOT CITABLE per its own status line.  No value
in this paper reads from it.  It is registered because S2 names depth
fifteen as the first depth at which its own question is askable, and a
terminal row there would extend the positional census past chain
position two — the single cap that most constrains §4's measurement.

---

## 13. The instrument

`75` anchors — `16` file-bytes (one of them read at its committed
bytes), `34` path-value, `24` verbatim-text context windows each bound
to a named consumer gate and evaluated before the byte anchors, and
`1` disclosure.  `48` must-pass gates, `0` failures.  `31` declared
mutants, every one dying by its named gate or anchor in the selftest.
`15` compliance rows, `0` missing, each row's status computed by
checking that its named injection-falsifier is a mutant the selftest
runs.  Never-falsified census: `0` unwaived.  The verdict string is
compared for complete equality against a comparator rebuilt segment by
segment from the measured values; the paper's own verdict block is
compared against the emitted one segment by segment, `7` of `7`
verbatim; and three verdict mutants — a typed
segment, an appended segment, and a class swap — die on it.
