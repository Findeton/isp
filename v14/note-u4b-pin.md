# U4B — THE SCHEDULE CENSUS (paper-17) — PIN (FROZEN)

**Frozen:** 2026-08-10, v14 ledger #126.  **Authority:** the U4
adjudication §4 (`fa991e19ae54`, #125).  **Unit:**
paper-17-schedule-census; code v14/code/u4b_schedule_exact.py
(+ output + receipt).

## THE QUESTION

U4 measured the division-event crystal TRUE — and the panel
proved its periodicity CONSTRUCTOR-INHERITED (the affine
mechanism: n = c + m·1_S, S a union of ⟨(1,1)⟩ cosets seated by
d66's seed rule).  U4B turns the constructor into a variable:
over the DECLARED FAMILY of admissible arbitration schedules —
the committed diagonal-coset seed ONE POINT in it, not its
generator — does crystallinity (a) survive only at coset-union
seeds (INHERITED, closed honestly), or (b) appear at
beyond-coset schedules (emergence's first foothold)?  And does
ANY schedule induce a NON-DEGENERATE metric (the weld-arena
scout)?

## R1 — THE FAMILY, DECLARED AS DATA (§15)

Base structure: CONFLICT-GRID(g=3, R=2) — the committed
constructor's grid, round count, and per-round event budget
(from the pinned v10 originals: d66 `3d0516ab106e`; the v14
rebuild routes may be READ at commit 58195da for cross-check).
THE FAMILY: all grammar-admissible choices of which cell-pairs
arbitrate per round, at the committed budget — admissibility
determined by the layer's own menus (the d42b1 source
`576275d55ecf` driven directly, per the operator's route), never
by fiat.  The family's size COMPUTED (#24).  If exhaustive
enumeration is infeasible, a DECLARED WINDOW with pinned
precedent (the d74 dep−2 shape or a declared sub-family),
disclosed in the head — never sampled silently (no-silent-caps).

## R2 — MEASURED PER SCHEDULE (per-object gates, #87)

1. **Constructibility**: FORCED (maxhits=1) / BRANCHING /
   REFUSED — each a counted fate (refusal locations printed).
2. **The stabilizer**, both site-readings (initiator /
   footprint), exact on Z₃² — by TWO independent routes minimum
   (direct + Fourier-annihilator; the panel's
   subgroup-lattice route available as a third).
3. **THE DETERMINANT COLUMN**: the induced I7-form per schedule
   (through HA §3.2's readout, as the U4 effectus evaluated it);
   det recorded exactly; **DET-NONZERO-EXISTS is a first-class
   outcome segment** — any schedule with det ≠ 0 at all sites is
   the corpus's first non-degenerate grammar-generated geometry
   carrier and is REPORTED BY NAME.
4. **Fragility** (for the crystalline schedules): the number of
   admissible single-event edits that break the stabilizer —
   computed, not sampled, within the declared window.

## R3 — THE NULL HYPOTHESIS (the affine mechanism, inherited)

Any schedule whose seed set is a union of cosets of a subgroup
H ≤ Z₃² yields Stab ⊇ H by the affine law — these schedules are
the PREDICTED-CRYSTALLINE class, and their crystallinity carries
NO emergence information.  The census's information lives in the
beyond-coset schedules; the classification
(coset-union vs beyond-coset) is computed per schedule and the
verdict stratifies over it.  Pre-registered outcomes:
`U4B-CRYSTAL-SEEDED-<the-locus>` (only coset-union schedules
crystalline) / `U4B-CRYSTAL-GENERIC-<fraction;subgroups>`
(beyond-coset crystallinity measured) — plus the independent
`DET-NONZERO-<EXISTS-<witness>|EMPTY>` segment and the
constructibility census.  Between delivery and adjudication
every headline reading is a candidate reading.

## R4 — WALLS AND STANDARDS

The four U4 walls INHERITED VERBATIM (L-1 argued-first with the
whitespace-normalised prohibition per the #125 engraving; BHS;
KR — any dimension reading carries the longest-chain height
control, correctly named per the adjudication; the diagonal
counterpoint may be MEASURED here — that is this unit's point —
but cosmological readings stay barred).  Full era standards AT
CONSTRUCTION: the #82 CLI; the head DERIVED independently;
per-object gates; **THE GATE-TO-DISK SEAL (#119) disciplined
from birth** — gate-time digests, write from the sealed object,
integrity = disk-vs-seal; #91 off-tree/git-less tested; #24;
#34 (no cannot-fail gates without waivers-with-forcings); #62
with the length floor; text gates whitespace-normalised
(#125); verify-paper in-run with polarity; exact arithmetic;
byte-reproducible ×2.  Registered siblings (cite, do not run):
the law-side stochastic-crystallization unit; the
diagonal-populating constructor (K₉/S1).
