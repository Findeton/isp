# U4 (paper-14, renewal-only crystals) — HOSTILE REVIEW PROTOCOL (FROZEN)

**Frozen:** 2026-08-10, v14 ledger #114.  Three reviewers,
independent, single-file writes, scratch-only, no imports,
read-only git; mutant/injection work on scratch copies (the
plain run WRITES artifacts).  Grades A/AWF/R; recomputations
counted; findings ranked with exact repairs.  **Panel queued
behind the R4b panel; launch on its completion.**

**Object (committed as-is, 06b89fe):** paper
v14/paper-14-u4-renewal-crystals.md (`7e6db49f0e6e`), code
v14/code/u4_crystals_exact.py (`c1ae8ec7fdbe`), output
(`d1bfbbca40c9`), receipt (`ae7a4ce48538`).  Pin `06b62ecb60a9`
(v14/note-u4-pin.md).  The scout of record
(note-routeA-successor-scout.md) — whose PRELIMINARY this unit
partly refutes.  Reviews:
`v14/review-u4-{operator,effectus,instrument}.md`.
Candidate-reading rule in force.

- **K1 THE CRYSTALS AND THE MARKING:** rebuild all five records
  from the v10 original constructors at their pinned shas (the
  unit's #91-forced substitute route — verify the substitution
  was faithful: 42 committed-number anchors); gate FORCED per
  crystal; re-derive the division marking (61 events;
  class-0-carrying-an-arb; S4's content 61/61 with the pair
  hypothesis VACUOUS here — verify both).
- **K2 THE STABILIZER TABLE (decisive):** all ten cells, both
  derivation routes (direct translation AND the
  Fourier-annihilator in Z[ω]) — rebuild with YOUR OWN third
  route if possible; the CONFLICT-GRID footprint cells' rise to
  full Z₃² (constant field — verify); the control's trivial
  stabilizer; the two planted mutants (aperiodic division;
  full-period control).  The scout-refutation: verify the
  divergence at 2 of 4 crystals and its one-directionality.
- **K3 GEOMETRY + HEIGHT PURITY:** chart-width invariance at
  both depths per crystal (the exclusively-at-division-events
  sharpening 3/3, 6/6, 1/1, 3/3 vs control 0/8); **HEIGHT
  PURITY — division events fill whole height layers sharing
  with nothing, all five records: verify, and rule what it
  forbids** (the unit says the KR control is empty BY THEOREM,
  forbidding homogeneity/ω readings — is that the honest
  strength?).  The renewal-only rebuild refusal: all five at
  the first delivery, locations printed; the idle-free rebuild
  event-identical (the delivery isolated as the sole blocking
  tag); paper 0 §10's third falsifier correctly NOT fired.
- **K4 SCOPE AND THE WALLS (effectus decisive):** did the unit
  respect all four pin walls (L-1 argued-before-tested or not
  tested; no sprinkling-LI test; the KR wall honored by the
  BLOCKED verdict; the q₁₂/diagonal counterpoint NAMED AND NOT
  READ)?  The verdict pair's honesty
  (TRUE-crystal + GEOMETRY-INVARIANT-AT-THE-CONTROLLED-ROW-
  REST-BLOCKED): candidate readings; what the result hands the
  weld line (the FOUND-side arena now exists at the renewal
  sublattice — what may Route A claim next, at citable scope?);
  the successor register rows.
- **K5 INSTRUMENT** at the full era (the twelve-injection
  playbook): 147 gates vs 12 mutants at #34 honest denominators
  (12 mutants for 147 gates — name every uncovered gate); the
  22 waivers each backed; the fixed MUT-NOT-FORCED (the
  worker's non-silent defect fix — verify the fix and the
  re-run); per-object gates (#87 — ten cells each bound); the
  head derived independently; #91 (off-tree loud failure at
  G-PROV-ROOT as designed; git-less byte-identity); verbatim
  anchors #62; verify-paper coverage; paper↔output↔receipt;
  byte-identity; repo hashes unchanged (concurrent workers
  disclaimed).
