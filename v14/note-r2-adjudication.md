# R2 — JOINT ADJUDICATION (operator #17, effectus #19, instrument #18)

**Adjudicator:** the coordinating agent.  **Inputs:** the three frozen
reviews (`1edcf53f79dd` / `199ca156dbed` / `7eabff507110`), the frozen
delivery (paper `cc78e9373bbe`, code `36a10324b93f`, output
`cb8493a13c39`, receipt `7b128499b246`), the protocol
(`3768846aebe9`), the pin (`76d42dfbc900`).  **Standing:** three
ACCEPT-WITH-FIXES; combined weight ~204,402 recomputations +
~4.25M sweep computations; the receipt is CLEAN (2,703/2,703 and
3,770/3,770 independent cell checks); **two false paper numbers, both
in prose**; the instrument is the weakest of v14 (11 of 13 injections
survive).  Resolved grade: **ACCEPT-WITH-FIXES**, unanimous.

## 1. The settled mathematics (cross-confirmed)

**A. The census is a theorem** (operator's circulant theorem R2-OP-1 ∥
instrument's closed form ∥ effectus's width law, three independent
derivations).  The drawn graph on the regular orbit is a clique
blow-up of a circulant; locality ⟺ window width ≤ the coset-support
diameter; |E| and b₁ in closed form; all fourteen rule IDs derivable
from the declaration — the operator predicted them before measuring.
The two transports' thresholds have **different causes** (missing
point vs contiguous arc): the paper's one "window-width effect" is
two.  Per #208 the census clauses reclassify as forced; what remains
measured: the grid, the drawing relation, the density values.

**B. LOCALITY-DECLARABLE, not FOUND** (operator ∥ effectus,
independent routes).  Zero of fourteen rules has a transport-stable
cell family; all 45 γ-stable atlases and **all 4,140 set partitions ×
2 transports** are clique-only — disqualifying every inherited group
at once.  **I6's own seven Fano lines — the corpus's one motivated
partial-overlap cover — are CLIQUE-ONLY: a 2-design covers every
pair.  The motivated cover is a locality-destroyer.**  The sliding
mechanism's essential ingredient is the integer labelling (in neither
⟨γ⟩ nor ⟨γ,Σ⟩); the rule list is a coordinate of the numbering (11
distinct rule sets over 40,320 numberings).  Credit, cross-confirmed:
the declared numbering's 14 is **modal** (34.29%) — the unit did not
cherry-pick.

**C. Dimension and locality are disjoint here — and why** (operator
F2 ∥ instrument K3 ∥ effectus F3, three proofs).  CONSISTENT ∧
NON-COMPLETE is impossible under the cell-indexed reading; consistent
readings exist at 22 rules, none local; the basepoint breaks
vertex-transitivity.  **And the escape is constructive** (effectus
F6): a fixed-point-free transport + the same sliding class yields a
triangulated 1-manifold with a CONSISTENT reading.  §11's closing
claim is refuted; the successor exists and ships as a control.

**D. The forced remainder.**  B₂-persistence and B→B₂ constancy are
forced by block-locality (the blocks-can-interact control does not
exist in this construction; the reviewer-run straddling and
non-block-local variants move the values).  LOCAL-DIMENSIONS is an
identity.  The link numbers are convention-relative (cell-multiplicity
vs simple-graph: 1-of-80 vs 8-of-80; triangulated circles 0-of-80).
The admissibility proxy excluded 60 constructible rules, one
locality-bearing (16/21, unattained in-grid).  NULL's denominators
mislabel measurements as rules.

**E. The instrument findings** (instrument, 11 surviving injections):
the verdict-equality gate compares the build function against itself
— **vacuous compliance with the day-old #10 engraving, plus a false
gate claim describing it**; render_check covers 10 of ~40 fields;
REFUSES/RULES/B1 counts unguarded (one injection yields an internally
contradictory receipt); a dropped window leaves "grid complete";
**JSON-path reads are not value-anchored** (a one-key drift delivers a
115-rule grid, a different verdict, five green anchors); the
"exhaustive" sweep truncates silently; never-falsified is 30 of 42
with four gates that cannot fail on any input and **no never-falsified
census in the receipt** — a regression against R1.

**F. Two false paper numbers** (operator F5/F6 ∥ instrument, same
values): "0/1 to 25/18" for a true range of 0 to 5/3 (contradicted by
the paper's own table), and "two charts" where three carry one link
edge.  The programme's third and fourth — all four to date live in
prose, the one surface not rendered from the gated object.

## 2. Rulings

**The transport scope decision: ADOPTED.**  The worker's §15 reading
(transport as arena data, both values censused) is vindicated by
finding A — the two transports have different threshold causes, which
a single-transport census would have hidden.  The pin's singular
wording was the adjudicator's, owned.  The strict sub-census remains
separable in the tables.

**The R1 side finding: ledger note, no reopening.**  N_coh = N is
forced at the shared drawing relation, so R1's "coherent 2-cells"
numerator qualifier is extensionally vacuous there.  Recorded here as
the note of record for terminal paper-01; no artifact changes.

**The R3 handoff: I7's arena, L ≥ 4.**  The 14 components are
inadequate (forced-inconsistent dimension, ≤20 2-cells, no direction
structure, numbering-relative membership).  The effectus measurement
stands as the discovery of record: **the gravity record layer's own
declared lattice satisfies the locality criterion, is
translation-covariant, and carries a CONSISTENT chart-intrinsic
dimension with a single link profile at every site** — (8,12,1,5) at
d=2, (24,96,1,73) at d=3 — failing only at d=2, L=3.  The R3 pin
poses the deformation questions on I7's own sites and **gates L ≥ 4
as a measured requirement**.

## 3. Binding repair orders (R-R2-1 … R-R2-12)

1. **Verdict rewrite**: head `R2-LOCALITY-DECLARABLE-AT<...>` with the
   effectus's segment set — DRAWING (verdict-determining; the ⟨γ,Σ⟩
   probe's 0 pairs), MOTIVATION (0-of-8,280 partitions; the Fano
   2-design result; order-relativity 11–16 with 11 rule sets and the
   modal statement), WIDTH-LAW (the circulant theorem with BOTH
   threshold causes; census-derivable at 109/109), forced labels on
   DIMREAD / B₂-PERSISTENCE / densities / LOCAL-DIMENSIONS,
   LINK-CONVENTION (both conventions printed; triangulated-circles
   0-of-80), GRID-BOUNDARY (the 60 excluded; the one locality-bearing
   excluded rule measured at 16/21 and printed), VALUES-GRID-DEPENDENT
   — every segment computed in-gate and flippable.
2. **The width/circulant theorem in-unit**: derivation, closed forms,
   the 109/109 prediction gate, the two-cause statement; census
   clauses carried at their forced label (#208).
3. **The motivation census in-unit**: the 4,140×2 partition sweep, the
   Fano-lines measurement, the order-relativity sweep with the modal
   count — MOTIVATION becomes a measured segment.
4. **Fix the false prose numbers** (both, plus the NULL denominators);
   regenerate every numeric prose claim from the receipt object or
   mark it derived-in-text at its derivation site.
5. **Rebuild the verdict gate** with a comparator that can disagree
   (independent reconstruction from the receipt payload, not the
   build function), plus injection-mutants proving non-vacuity — all
   five R1 classes must die; fix §8's false gate claim.
6. **Anchor (path, value) pairs** for every JSON-path read from
   pinned artifacts; the path-drift injection must die.
7. **Extend render_check to every rendered field**; the seven
   escaping cells die; add an internal-consistency gate (a receipt
   asserting clique-only while listing a non-complete component must
   fail).
8. **Guard the counts**: REFUSES / RULES / B1 / grid-size derived and
   gated; the dropped-window and truncated-sweep injections die
   (exhaustiveness gated by count derivation, not assertion).
9. **Never-falsified census restored** to the receipt with an honest
   denominator; the four cannot-fail gates rebuilt as falsifiable or
   demoted to disclosures; waivers censused.
10. **Ship the constructive successor control** (effectus F6): the
    fixed-point-free triangulated 1-manifold with its CONSISTENT
    reading, gated — the grid's boundary made visible.
11. **Paper rewrite** (single-threaded): the finding is
    LOCALITY-DECLARABLE + the width law + the motivation walls + the
    I7 discovery as the successor input; §11's claim corrected by
    F6's construction; no correction narrative.
12. **Scoping**: the unit claims existence-in-atlas-space and the
    walls around it; nothing about the substrate's own locality —
    that question moves to I7's arena (R3).

## 4. RUNBOOK engravings (this adjudication)

- **§13 addendum (prose renders from the receipt):** every numeric
  claim in a paper renders from the receipt object or is explicitly
  marked derived-in-text at its derivation site; all four of the
  programme's false paper numbers to date lived in hand-written
  prose.
- **§14 addendum (compliance claims are gate claims):** a gate
  asserting compliance with an engraved rule ships with an
  injection-falsifier demonstrating the gate can fail; a compliance
  gate whose comparator cannot disagree with the object under test is
  vacuous by construction (the R2 verdict-gate recurrence of #219, at
  the gate the #10 engraving created).
- **§14 addendum (path-value anchoring):** a read-by-path from a
  pinned artifact anchors the (path, value) pair, not only the file
  bytes; a path drift that changes the arena or verdict must die by
  anchor.

## 5. What R3 inherits (consolidated)

I7's declared lattice as the arena (motivated locality; consistent
dimension; single per-site link profile; translation covariance);
**L ≥ 4 gated**; the deformation questions posed record-natively
(H_a[N] on I7's own sites); the grid line closed as
existence-in-atlas-space with its walls proved; the fixed-point-free
1-manifold as the minimal consistent-dimension example.
