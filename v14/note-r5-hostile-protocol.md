# R5-GAUGE (paper-18, the gauge rung) — HOSTILE REVIEW PROTOCOL (FROZEN)

**Frozen:** 2026-08-10, v14 ledger #138.  Three reviewers,
independent, single-file writes, scratch-only, no imports,
read-only git; mutant/injection work on scratch copies (the
plain run WRITES artifacts).  Grades A/AWF/R; recomputations
counted; findings ranked with exact repairs.

**Object (committed as-is, a504243):** paper
v14/paper-18-gauge-rung.md (`3800959353b4`), code
v14/code/r5_gauge_exact.py (`37c232de91a6`), output
(`e86be9a581a7`), receipt (`1c072956ac7b`).  Pin `b53adba0eee0`
(v14/note-r5-gauge-pin.md; G1–G7 lifted from the frozen
R4-effectus recommendation).  Parents at terminal commits: R4
(583cae7), R4b (6d32993).  Reviews:
`v14/review-r5-{operator,effectus,instrument}.md`.
Candidate-reading rule in force on R5-NON-ABELIAN and every
segment.

- **K1 THE GROUP (decisive):** rebuild the 640-coin family (the
  derivation from R4's 25 elements — is it a derivation or a
  choice?), the plaquette holonomies, and the isomorphism-class
  certification (even-on-every-orbit + order = the alternating
  product — is set-equality certification sound for A_n on its
  own support? verify the local ladder A₃<A₅<A₃×A₃<A₃×A₃<A₇<A₈
  and the global A₁₆ with YOUR OWN group machinery); the rank-8
  arena-relativity; the 576/640 commutator census; **the flat
  control** (58 circulants, 0/3364, trivial — rebuilt
  independently again); THE CR-D COMPARISON (the form match
  "full alternating on its own support"; A₅ at the five-point
  stencil — verify against the pinned CR-D artifacts; is the
  match structural or numerological?).
- **K2 THE GATE RESULT:** the inherited per-generator gate
  admits 0/52 at FULL (all at NONE — "translating a link
  operator moves the link"): verify the emptiness and its
  mechanism; the FAMILY-covariance replacement (4096/4096) —
  is it the right instrument, and is
  R5-BLOCKED-AT-THE-GATE-AT-THAT-READING honestly segmented
  beside R5-NON-ABELIAN (two verdicts, one string — do they
  compose or contradict)?
- **K3 THE EXCLUSION THEOREM:** curvature/defect MUTUALLY
  EXCLUSIVE at link grain by the single-path lemma (0/1920
  both; 576/384 split) — verify the lemma's proof and its
  scope; the plaquette-grain table (all four cells, 384 both —
  reconcile the two grains' opposite-looking statements); the
  two-excitation negative (0/18); the parent baseline; **the
  infinite-order sector** (512/512 by trace non-integrality —
  verify the certification route; what does
  defect ⟹ infinite-holonomy license at citable scope?).
- **K4 REFINEMENT + SEPARATION (effectus decisive):**
  LOCAL-STABLE-GLOBAL-EXTENSIVE (6/6 at L=4 vs L=8; A₁₆→A₆₄ —
  is "extensive" the licensed reading of a growing global
  group?); G7's split verdict (local separates 12/12, global
  separates 0/2 — A₁₆ measured-but-not-discriminating: is
  entering it in the head licensed?); the must-nots audit (no
  confinement language — sweep; no curvature⟹quantum);
  candidate readings of R5-NON-ABELIAN as such (what may the
  programme now claim about "the QCD shape" at citable scope?);
  the choice inventory at the RSQ standard (the coin alphabet's
  derivation status; the parity strata; the declared gate);
  THE SUCCESSOR REGISTER (R5-QCD-shaped follow-ons; what the
  coupling unit inherits; what may NOT be claimed).
- **K5 INSTRUMENT** at the full era (a born-#119 unit — the
  second): the seal audited under the R4b playbook (21 sealed
  objects; post-gate and post-write injections); the #82 CLI
  under hostile argv; 61 gates vs 24 mutants at #34 (the 3
  forcings machine-checked); #125 text gates; #91 re-tested at
  your own hands (off-tree/git-less/foreign-CWD/new seed);
  anchors at #62 (floor 20 — verify consumer binding);
  comparator independence; paper↔output↔receipt sweep;
  byte-identity; repo hashes unchanged (concurrent workers
  disclaimed: giter reviews, u4b_*).
