# WELD 2 — JOINT ADJUDICATION (operator #96, effectus #97, instrument #100)

**Inputs:** the three frozen reviews (operator `64ddbfb0c2f6`,
effectus `da37c5b9a11f`, instrument `e83855bd5e74`); the frozen
delivery (paper `535e288ff412`, code `290149118b9d`, output
`5e35e7a0115f`, receipt `bacdb7a5e985`, commit 58195da); pin
`9d19515cb3ae`; protocol `e7b99e05557d`; the successor scout of
record (note-routeA-successor-scout.md, #101) as REGISTERED
INPUT.  **Standing:** three AWF; 115+110+~179 recomputations
with ZERO disagreements against delivered numbers; off-tree
byte-identity ×3 (two seats + foreign-seed).  One false paper
claim (prose, the abstract's "strictly positive" — the
programme's 6th; computed-number record still zero).  Resolved
grade: **ACCEPT-WITH-FIXES**.

## 1. THE CORRECTED SETTLEMENT

**EMPTY STANDS.**  All three seats attacked it — including a
constructed counter-candidate (effectus) and a
charitable-reconstruction sweep (operator) — and it held at
every cell.  What is corrected is the ACCOUNT of why:

1. **The carrier axis was inert** (operator MAJOR-1 ⇄ effectus
   MAJOR-3, independent): all 30 @MENU rows byte-identical to
   the 30 @CONG rows; the code selects by SITE generator and
   never consults the carrier; CANDIDATES=60 is 30 counted
   twice; §5's "built separately on each carrier's own class
   graph" is false of the code; §9's "silence is a result" is a
   non-measurement.  The substance of @BOTH survives via the
   site axis; the head and §5/§9 must say what ran.
2. **The mechanism is READING-STRATIFIED** (effectus MAJOR-1,
   decisive): the code silently demanded bijection +
   induced-subgraph isomorphism (embedding) where the pin says
   "a map."  Corrected account, both readings carried:
   - EMBEDDING reading: the scissors — with its second blade
     now **THE GRADING THEOREM** (operator strengthening 3;
     scout S2 convergent): every extension edge raises history
     length by exactly 1, so the class graphs are graded ⟹
     acyclic with no computation; CONG-185 length-homogeneous
     at d≤4 AND d≤5; MENU's 45 self-loops = exactly its 45
     multi-length classes.  The scissors survives at d≤5
     (operator ran the registered experiment: 265/462
     reproduced; actor-pair still 2).
   - QUOTIENT reading (admissible under the pin's words):
     acyclicity is no obstruction; **MENU dies EXACTLY**
     (arc-consistency wipeout on its 45 self-loops — the
     effectus-supplied second blade, valid under both
     readings); **CONG dies by COUNT-POSITIVITY** (the
     effectus's explicit Z₃-grading witness φ: 185→9 reaches at
     best 18/27 positive cells over 40 solutions).
   40/60 cells are reading-robust; the head must carry the
   stratification and the free-item accounting must state that
   the pre-registered items were never reached.
3. **The FOUND control was demonstrated at the wrong target**
   (effectus MAJOR-2): both FOUND-side controls ran at TGT_CRY
   (2 links, arena 288) while the census judges at TGT_I7
   (3 links, arena 15,552); the pin-named generator (the
   record's own cover structure) returns STRUCT-DEAD at both
   targets, unreported.  The control claim must be re-scoped or
   re-run at the verdict's target.
4. **ADOPTED SHARPENINGS:** det = 0 at every site of every
   crystal (effectus: q₁₂ = −k through HA §3.2 ⟹ no committed
   crystal induces an admissible I7 record AT ALL — a third,
   independent STRUCT-DEAD route); the R4-resonance sentence
   fixed at its licensed form: "the declared link that forces
   R4's connective is the one link no committed grammar record
   writes."  The scout's S1 REGISTERED alongside it: the
   9-actor co-division graph is Cayley(Z₃²,4 gens) with 9!
   isomorphisms — the deep obstruction is a LINK-COUNT
   MISMATCH (4 offered vs 3 declared), and the diagonal is
   absent from the CRYSTALS, not from the grammar.

## 2. The instrument findings (born-compliant, not era-compliant)

CLI COMPLIANT under attack — the first born-compliant unit —
but 12/12 reviewer injections survived: the verdict head is a
TYPED LITERAL no gate compares (a FOUND head delivers at 32/32,
exit 0 — R4's F1 recurring with no reconstruction at all); one
raw worktree read bypasses read_pinned (#91; G-PROVENANCE
passes its claim while G-I7-ARENA consumes the corrupted copy);
no per-cell fate gates and no carrier-agreement gate (#87);
nothing re-reads what was written; ZERO code reads the paper
(#20 claim uninstrumented); "each falsified by a mutant" false
at 1-of-11; five verdict-bearing numbers ungated; failing runs
still overwrite artifacts.  Honest coverage 11/32 (34%) with
all 21 uncovered gates named; the 3 waivers = the 3
literal-True gates (to be re-gated as theorem-passes with
forcings per the grading theorem).

## 3. Binding repair orders (R-W2-1 … R-W2-10)

1. **The head recomposed**: the reading-stratified mechanism
   (§1.2 verbatim in structure — EMBEDDING: THE-GRADING-THEOREM
   + arity; QUOTIENT: MENU-SELF-LOOP-WIPEOUT-EXACT +
   CONG-COUNT-POSITIVITY-18-OF-27-MAX); the carrier stamp
   corrected to what ran (@SITE-AXIS with both carrier graphs
   built) or made genuinely per-carrier with a
   carrier-agreement gate; CANDIDATES counted honestly.
2. **The head DERIVED** inside a complete-string equality gate
   by an independent reconstruction (no shared code/inputs/
   typed literals, #82) — the FOUND-head injection dies.
3. **The grading theorem adopted unit-side** (the operator's
   proof carried; the acyclicity branch's literal True re-gated
   as a THEOREM-PASS with a machine-checked forcing; SCC at
   every length replacing the 2–6 bound).
4. **The quotient-reading census added**: the admissibility
   axis declared as data (EMBEDDING | QUOTIENT), both run; the
   MENU wipeout exact; the CONG count-positivity witness
   computed; the pre-registered free items reached and printed
   where the reading permits.
5. **The FOUND control at the verdict's target**: re-run at
   TGT_I7 (or the control claim scoped to TGT_CRY explicitly in
   the head); the pin-named cover-structure generator's
   STRUCT-DEAD at both targets REPORTED; the directed-vs-
   undirected criterion asymmetry (operator MINOR-2 =
   instrument handoff) resolved by using one declared criterion
   on both branches or pricing the difference.
6. **read_pinned everywhere** (#91): the L1016 raw read routed;
   off-tree AND git-less byte-reproduction re-verified; the
   git-less leg made to reproduce (or its loud failure declared
   as designed and gated).
7. **Per-cell fate gates + the carrier-agreement gate** (#87):
   every one of the cells bound to its own computed fate; the
   two carriers' tables compared by a gate.
8. **Artifact integrity closed**: failing runs write nothing; a
   final integrity gate re-reads what was written (killing
   INJ05/10/11's class); the five ungated verdict numbers
   gated; verify-paper built and run inside the plain run
   (#20), covering every numeral (incl. 265/462, currently
   uncited); the "each falsified by a mutant" sentence corrected
   to the measured 1-of-11 → repaired coverage; the degenerate
   additivity comparator rebuilt independent.
9. **Prose corrections**: the 6th false paper claim (abstract
   "strictly positive" — D60-GRID has axis counts 0) corrected
   with the delivery-vs-arbitration crystal distinction carried;
   §8.2's false waiver-census citation fixed; the receipt's
   subsets_excluded powers → the binomials the paper correctly
   prints; SMUGGLED=0 stamped STRUCTURAL-NOT-MEASURED (or made
   reachable); "336 in each direction" de-doubled; argparse
   abbreviation disabled; --help exits 0.
10. **Paper rewrite single-threaded**; THE SUCCESSOR REGISTER
    as the closing section, adjudication-ruled (§4 below), with
    the effectus's four-consumer register + the scout's
    ingredient tables cited by sha.

## 4. THE SUCCESSOR RULING (with the scout of record)

- **The ≥9-actor unit is NOT launched as posed** — the scout
  proves the symmetric-family form is pre-determined (K₉ by
  equivariance; 0 isomorphisms into I7's 3-link lattice; count
  field constant): it would re-derive EMPTY by over-connection.
  Its content is REGISTERED instead: S1 (the link-count
  mismatch 4-vs-3; the grammar CAN write diagonals; the first
  grammar-side q₁₂ is reachable only through a declared channel
  sub-grammar or Route B) becomes a register row in paper-13
  and a standing datum for any Route-B pricing.
- **U4 IS the next Route-A unit** (effectus + scout convergent).
  Its pin (to be frozen on this adjudication's completion)
  inherits: the scout's ingredient table; renewal =
  class-0-carrying-an-arb SOURCE-FORCED; the site reading
  DECLARED as data (initiator vs footprint); the renewal-only
  operationalization DECLARED (three candidates, choice-
  inventoried); scope = ARBITRATION crystals (the delivery
  crystal is the named counterexample); the preliminary
  ⟨(1,1)⟩-stabilizer measurement to be made corpus-grade
  two-way; THE WALLS engraved in the pin (L-1 fourth-form
  argued before testing — the retracted wording banned; BHS —
  no sprinkling-grade-LI test; KR height control; q₁₂ ≡ 0
  inherited from the rook's graph).
- The Γ-iteration, R4b-momentum, R5-gauge pins are unaffected
  by this unit and proceed on their own parents' terminals.

## 5. Engravings

None new.  Recurrences logged under existing rules: the typed
head (#234/#10 + the R4 derived-head order — second recurrence);
the raw worktree read (#91 — first post-engraving violation,
in an otherwise born-compliant unit); per-object gates (#87).
The repair-propagation addendum (§15) applies: the repair
diffs its gates against every rule engraved since pin-freeze.
