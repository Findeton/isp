# ACT (paper-34) — THE ACTION UNIT: THE QCD ROAD'S FUNNEL — PIN (FROZEN)

**Frozen:** 2026-08-11, v14 ledger #279.  **Authority:** THE
STRATEGIC PLAN (Wave C, first rung) + the SMU adjudication
(#242: ROUTE C THE ONLY LIVE ROUTE; the inheritance row —
the law-native π enters as a CONTROL, never spent as
derived; the 207/639 conserved-price frame; the cheap
falsifier = exhibit a gauge observable whose expectation
covariance pins; POT's confinement gate UNASKABLE until this
unit lands) + paper-23's terminal (the Gibbs census row
blocked-at-no-action — this unit supplies the missing
ingredient).  **Parents (terminal):** SMU at #278 (paper
6df0db523d32: the dynamics census, the surjection, the
conserved price, the invariant simplexes), paper-23 at #201
(the 640-configuration space, the withholding machinery, the
transitivity criterion), R5 at 987cd73 (the plaquette
structure, the holonomy, the chart and gauge groups),
Γ-iteration at 2895a9a (the law's exact structure).  **Unit:**
paper-34-act; code v14/code/act_exact.py (+ output +
receipt).

## THE QUESTION

Construct the action on gauge configurations and price every
part of it forced-vs-declared.  **Framing binding for exact
arithmetic: the action is carried MULTIPLICATIVELY — local
weight systems w (positive exact rationals per local datum),
the configuration weight W(c) = Π w(local parts), the
"action" S = −log W nominal only.  No logs, no floats,
anywhere.**

1. **THE FORM CENSUS (the heart):** characterize ALL weight
   systems satisfying the measured constraints — (i)
   GAUGE-INVARIANCE (under the measured gauge group action);
   (ii) LOCALITY (w a function of the declared plaquette
   data — the locality grain itself a declared axis: per-link
   / per-plaquette / per-site, every member run); (iii)
   COVARIANCE (under the measured chart group).  The allowed
   space computed EXACTLY (its dimension / generating set /
   orbit structure).  **The dimension modulo normalization =
   THE COUPLING COUNT — this universe's gauge-constant
   inventory (the α-analogue).  Dimension 0 ⟹ ACT-FORM-FORCED
   (the strongest possible outcome).  Finite d > 0 ⟹ the
   couplings ARE the priced fiber, enumerated.**
2. **THE CANDIDATE ROWS (each measured):** (a) the
   Wilson-shape (plaquette-trace weights — is it IN the
   allowed space?  is it a distinguished point — extremal,
   symmetric, minimal-support?); (b) the LAW-NATIVE row —
   the weight system whose Gibbs measure is SMU's law-native
   π, AS THE CONTROL (its stamp
   LAW-RATED-CONSTRUCTION-DECLARED-AT-AN-UNPINNED-
   IDENTIFICATION carried verbatim; never spent as derived);
   (c) the NULL (w ≡ 1 → counting).
3. **THE GIBBS MAP:** from the allowed weight space to the
   invariant simplex — image, fibers, surjectivity-or-not
   ONTO the invariant measures.  **THE CONSERVED-PRICE
   BINDING: does choosing a weight system evade SMU's
   207/639?  The map's fiber dimensions answer it exactly —
   measure, do not argue.**
4. **WILSON EXPECTATIONS under each candidate row** —
   stamped CONDITIONAL-ON-THE-DECLARED-WEIGHTS; the
   [0,4]-range fact cited; **the cheap falsifier run: is
   there ANY gauge observable whose expectation the
   covariance constraints pin to a proper sub-range?**
5. **THE POT HANDOFF:** what the potential unit inherits —
   the allowed space, the coupling inventory, the
   distinguished points; **the confinement vocabulary REMAINS
   BEHIND POT'S GATE — this unit makes no area-law,
   string-tension, or potential claim** (the withholding
   machinery inherited verbatim from paper-23's repaired
   gate).

## OUTCOMES (pre-registered)

`ACT-FORM-FORCED-<the characterization>` /
`ACT-FORM-RELATIVE-<dimension; the coupling inventory>` /
`ACT-GIBBS-<image; the price verdict>` /
`ACT-BLOCKED-AT-<object>`.

## WALLS

No confinement vocabulary (POT's gate); no SI numbers; the
four walls; E-22 + E-23 + E-24; **the #267 template
checklist** (paper-scanning walls; all tables rendered; no
typed counts; full-transcript integrity; digest-free
whitelists; spelled numerals); REFERENT-BOUND gates (the OCC
lesson); genuinely multi-way outcome selectors with control
arms (the paper-23/SIG lesson).

## STANDARDS

Full era per HANDOFF-PROMPT.md §4; exact arithmetic
(multiplicative weights — no logs); windows declared
in-string and licensed; all seals at value-close; read-back
before replace; derived head, de-twinned comparators;
#91 off-tree; failing runs write nothing and promote
nothing; byte ×2.  Candidate readings until adjudication.
