# FAC (PAPER-35) — THE FACTORIZATION UNIT: ARE ACTORS THREADS, AND IS THE NINE-FOLD DIVISION FORCED? — PIN (FROZEN)

**Frozen:** 2026-08-12, v14 ledger #300.  **Authority:** the
AID adjudication (note-aid-adjudication.md, 04cd41ed858b) +
the AID effectus review §10 (the licensed claims) and §11
rows 1–2 (the groupoid grain; the decomposition grain,
previously unregistered and owed — "the grain Barandes'
worry actually names") + OCC terminal #276 (the 27-cell
carrier typing; MISTYPED-AT-THE-CARRIER as precedent) +
PER-L #251 / PER-R (persistence along R; panel-verified
numbers) + paper-20 (the walk, under the #293 coin-order
duty).  **Unit:** paper-35-fac; code v14/code/fac_exact.py
(+ output + receipt).

**READ DISCIPLINE:** AID's four object files are under
repair in a sibling worktree — do NOT read them from the
worktree.  Cite AID content from the committed reviews and
adjudication (stable), or via git show at the delivery
commit.  No delivered AID number may be re-derived from
in-flight files.

## THE QUESTION

AID answered NAMING: on the granted nine-actor grain,
history forces every label (5,852/5,856 trivial
stabilizers; the Young-subgroup theorem).  It explicitly
did NOT answer FACTORIZATION: whether the nine-thread
decomposition is itself distinguished by the law, or is one
chart among many.  This unit asks factorization, at two
grains the successor register names:

1. **THE DECOMPOSITION CENSUS (the heart).**  Enumerate
   candidate factorizations of the arena — partitions at
   the actor grain (the lattice over 9) and at the carrier
   grain (OCC's 27 cells) — and for each, apply a DECLARED
   law-compatibility criterion (covariance of the committed
   dynamics/record structure under the partition's induced
   identifications; the criterion itself specified exactly
   in-code before any census row runs, and gated).  Count
   the admissible set exactly.  Cardinality 1 → the
   division is FORCED; > 1 → the division is DECLARED, with
   the full inventory enumerated and priced.
2. **THE GROUPOID CRYSTALLIZATION.**  Replace the global
   symmetric group with the groupoid of partial,
   locally-consistent identifications.  The atom theorem is
   THE OBJECT TO BREAK: group-level triviality does NOT
   imply groupoid-level rigidity — the 5,852 must be
   RE-EARNED at the groupoid grain or lost there, measured.
3. **THE GRAIN TRIANGLE.**  The stabilizer census at S₂₇
   (cells) against the site grain against S₉ (actors) —
   which grain does the law type its own objects at?
   (OCC's MISTYPED-AT-THE-CARRIER is the live precedent;
   the carrier answer need not be the actor answer.)
4. **THE PERSISTENCE PRESUPPOSITION.**  The global grain
   PRESUPPOSES thread persistence; this unit is the first
   that can pose "are actors threads" at all.  State the
   presupposition as a measured fact, then test: does
   crystallized identity TRANSPORT without it (along the
   walk / the R-ladder), or does thread-hood enter only by
   declaration?  (PER-L/PER-R rows cited as parents,
   panel-verified.)

## STANDING DUTIES (inherited engravings)

- **The test-declaration duty (AID MAJOR-1's lesson, now
  mandatory):** every blindness/sightedness probe states
  which test it applies — transported reading or fixed
  attribution — and where the contrast is the claim, BOTH
  tests run on BOTH objects.
- **The coin-order duty (#293):** if the walk enters, the
  coin order is declared and both declared orders published,
  or the fiber stamped.
- **Sentence-level referent binding** (the three-seat AID
  convergence): no sentence pairs numerals from different
  universes; gated.
- **Class/predicate binding (#295):** every census row's
  printed class-words gate-bound to the computed predicate.

## OUTCOMES (pre-registered, WITH FEASIBILITY — the #299 engraving)

- `FAC-FACTORIZATION-FORCED-<witness>` — reachable: the
  admissible set of the decomposition census can have
  cardinality 1; nothing in the arena pre-decides it — the
  census ranges over partition lattices (B₉ = 21,147 at the
  actor grain alone) and the compatibility criterion is a
  per-partition predicate, so counts 1 and > 1 are both
  arithmetically attainable.  No pigeonhole forbids 1.
- `FAC-FACTORIZATION-DECLARED-<inventory>` — reachable: any
  second admissible partition realizes it; the trivial and
  discrete partitions are candidate members whose
  admissibility the criterion decides, not the arena.
- `FAC-STRATIFIED-<by-grain>` — reachable: the actor-grain
  and cell-grain censuses are independent measurements; one
  returning 1 while the other returns > 1 realizes it.
- `FAC-ATOM-<HOLDS/BREAKS>-AT-THE-GROUPOID-GRAIN` —
  reachable both ways: the groupoid census either re-earns
  the 5,852 (holds) or exhibits a nontrivial groupoid
  stabilizer at some admissible history (breaks); both are
  decidable outcomes of the same enumeration.
- `FAC-BLOCKED-AT-<object>` — instrument fault only.
The head is DERIVED, multi-way, with control arms
demonstrating every outcome word emittable (synthetic-arena
controls through the real criterion, per ACT Z1's pattern).

## WALLS

No unscoped "actors are/aren't real" slogans — the AID
not-licensed list (adjudication §2) is a WALL, paraphrase-
hardened.  No persistence claims beyond the measured
transport rows.  The four walls; E-22 + E-23 + E-24; the
#267 checklist (paper-scanning walls; ALL table rows AND
COLUMN HEADERS rendered as claims; no typed counts;
full-transcript integrity; digest-free whitelists; spelled
numerals scanned).  REFERENT-BOUND gates (names bound to
objects measured).  Windows declared in-string and
licensed; costs disclosed; any census truncation named with
its bound (no silent caps).

## STANDARDS

Full era per HANDOFF-PROMPT.md §4/§9; exact arithmetic;
seals at value-close; read-back before os.replace (nothing
promoted on refusal); derived head with de-twinned
comparator routes; #91 off-tree; failing runs write
nothing; byte ×2.  Candidate readings until adjudication.
