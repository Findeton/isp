# SMU (paper-27) — THE STATIONARY MEASURE — PIN (FROZEN)

**Frozen:** 2026-08-11, v14 ledger #202.  **Authority:** THE
STRATEGIC PLAN (#193, Wave B) + the paper-23 adjudication
(cc85b7d98f6e) successor register + paper-23's terminal head
row: "A COVARIANT CHAIN DERIVES IFF IT IS IRREDUCIBLE."
**Parents (terminal):** paper-23 at #201 (paper 79cc67b4f6cd,
code faf353385905, receipt c9edf97a5533 — the invariant
simplexes, the transitivity criterion, the withholding
machinery), R5 at 987cd73 (the 640-coin link-indexed
configuration space, the chart and gauge actions), the
Γ-iteration terminal at 2895a9a (the law-native normaliser as
a candidate rate source).  **Unit:** paper-27-smu; code
v14/code/smu_exact.py (+ output + receipt).

## THE QUESTION

Paper-23 proved no measure derives from the STATIC census and
named the one absent candidate: the stationary measure of a
dynamics ON configuration space — currently unownable because
no pinned dynamics acts there.  SMU declares the dynamics
(the fiber priced, per candidate) and asks whether the
stationary measure DERIVES:

1. **THE DECLARED-DYNAMICS CENSUS (every member run):** at
   minimum — (a) the chart-group random walk (uniform steps
   on the measured chart group); (b) the gauge-action walk;
   (c) a local resampling dynamics bound to the law-native
   normaliser (the Γ-shaped candidate); each DECLARED with
   its fiber, none privileged.
2. **IRREDUCIBILITY, per dynamics (the iff):** the chain's
   communicating classes computed exactly; paper-23's theorem
   applies — derives iff irreducible.  Reducible ⟹ the
   decomposition is the finding.
3. **THE STATIONARY MEASURE, exactly, where it exists:**
   unique?  One of the named nulls (counting; orbit-uniform)
   or NEW?  Computed by exact linear algebra, gated.
4. **THE DYNAMICS-RELATIVITY CENSUS (E-24's teeth):** does
   the stationary measure MOVE across the declared-dynamics
   fiber?  All-candidates-agree ⟹ quasi-derivation (the
   strongest honest form); disagreement ⟹ the spread is
   priced and the declaration-relativity stamped.
5. **WILSON EXPECTATIONS — gated, not assumed:** paper-23's
   withholding was scoped to the DECLARATION-REQUIRED
   outcome.  HERE: expectations are computable ONLY under a
   measure that derives at the RSQ standard GIVEN the declared
   dynamics, and every expectation is stamped
   CONDITIONAL-ON-THE-DECLARED-DYNAMICS.  **The confinement
   vocabulary stays behind its gate: no area-law,
   string-tension, or potential claim; expectations reported
   as expectations** (the MUST_NOT list inherited verbatim
   from paper-23's repaired gate, #125-normalized).

## OUTCOMES (pre-registered)

`SMU-DERIVED-<dynamics;measure;UNIQUE>` /
`SMU-QUASI-DERIVED-<all-candidates-agree>` /
`SMU-REDUCIBLE-<decomposition>` /
`SMU-DYNAMICS-RELATIVE-<spread>` / `SMU-BLOCKED-AT-<object>`.

## STANDARDS

The full era per HANDOFF-PROMPT.md §4 + E-22 + E-23 + E-24
(every fraction measure-labeled or COUNTING-ONLY); exact
arithmetic; declared windows licensed; the derived+rendered
head; the total seal; failing runs write nothing; byte ×2.
Candidate readings until adjudication.
