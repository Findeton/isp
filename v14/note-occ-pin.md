# OCC (paper-31) — THE OCCUPANCY-CEILING FORCING UNIT — PIN (FROZEN)

**Frozen:** 2026-08-11, v14 ledger #249.  **Authority:** the
user's order (#209: OCC before FCK; FCK inherits the ceiling)
+ the paper-22 adjudication (#217: OCC owns the ceiling) +
paper-22's terminal head (#248: FORCING-THE-CEILING=OPEN;
STAGE-DECLARES-NO-OCCUPANCY-KEY-IN-27).  **The seed
conditional (paper-22 K2, #206, verbatim):** weld ACTOR→SITE
bijection + excitation↔actor injection + closure-required +
the full 64-pool ⟹ hard-core forced ⟹ **FERMIONIC-SHAPE IS A
THEOREM OF THE COUPLED THEORY.**  **The organizing
principle:** exclusion can select a shape; permission cannot
(the one-way asymmetry).  **Parents (terminal):** paper-22 at
#248 (paper fb05cc2a376a: the leak theorem, the two ceilings
as objects, the head), paper-19 at #172/559e162 (the forced
dictionary — ACTOR→SITE), paper-20 at #204 (the coupled
machine — how excitations ride actors; the emission
grammar).  **Unit:** paper-31-occ; code v14/code/occ_exact.py
(+ output + receipt).

## THE QUESTION

Does any committed deeper layer FORCE the occupancy ceiling
that selects statistics?  The seed conditional is measured
PREMISE BY PREMISE on the committed layers — never assumed:

1. **P1 — the bijection:** the weld's ACTOR→SITE map at the
   committed fibers (paper-19: 1/1/1) — is it a bijection ON
   THE ARENA THE EXCITATIONS USE (the coupled machine's 9
   sites)?  Measure/cite; any gap priced.
2. **P2 — the injection:** in the coupled representation
   (paper-20's walk), is a DOUBLY-OCCUPIED state
   constructible at all — can two excitations ride one actor
   in the committed grammar, or does the representation
   exclude it structurally?  (This is the premise the whole
   conditional turns on: measure the state space's
   construction, not its usage.)
3. **P3 — closure:** the process semantics — division events
   involve co-division PAIRS of distinct actors; can an actor
   carrying two excitations still divide per the committed
   grammar (d42b1/paper-19's driver)?  If the grammar is
   silent, that silence is a FINDING (the premise needs a
   declaration; price it).
4. **P4 — the pool:** the full 64-generator pool's role
   (paper-22's leak theorem gives the 48/16 split — cite).
5. **THE VERDICT:** all premises hold on committed layers ⟹
   `OCC-CEILING-FORCED-<the premise chain>` and the licensed
   sentence "fermionic-shape is a theorem of the coupled
   theory" (shape-words only; the wall gate inherited from
   paper-22's repaired instrument).  Any premise failing or
   needing declaration ⟹ `OCC-CEILING-OPEN-<the named
   premise; the fiber priced>` — first-class and honest.
6. **THE TWO-WAY CONTROL:** an arena where the conditional
   demonstrably FAILS — the bare stage without the weld
   (paper-22's ceiling-2 arm: double occupancy constructible
   there) — run as the it-can-fail arm; the contrast gated.
7. **THE ASYMMETRY ROW:** the one-way principle (exclusion
   selects, permission cannot) restated at whatever verdict
   emerges, gated against the measured arms.

## OUTCOMES (pre-registered)

`OCC-CEILING-FORCED-<chain>` /
`OCC-CEILING-OPEN-<premise;fiber>` /
`OCC-CEILING-PARTIAL-<which premises hold>` /
`OCC-BLOCKED-AT-<object>`.

## WALLS

Shape-words only (the no-particle wall AS AN INSTRUMENT —
inherit paper-22's gate pattern); no spin/relativity claims;
description-stamps; the four walls; E-22 + E-23 + E-24 in
full (post-engraving-native; no excuses).

## STANDARDS

Full era per HANDOFF-PROMPT.md §4; exact arithmetic; windows
declared and licensed; the derived+rendered head with
DE-TWINNED comparator routes (the SMU lesson); all seals at
value-close with read-back-before-replace (the era's closed
diseases stay closed); failing runs write nothing and promote
nothing; byte ×2.  Candidate readings until adjudication.
