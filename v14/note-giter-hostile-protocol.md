# Γ-ITERATION (paper-16, the law on the ruled carrier) — HOSTILE REVIEW PROTOCOL (FROZEN)

**Frozen:** 2026-08-10, v14 ledger #131.  Three reviewers,
independent, single-file writes, scratch-only, no imports,
read-only git; mutant/injection work on scratch copies (the
plain run WRITES artifacts and is heavy — budget accordingly).
Grades A/AWF/R; recomputations counted; findings ranked with
exact repairs.

**Object (committed as-is, 9e481db):** paper
v14/paper-16-gamma-iteration.md (`fd2f25d40002`), code
v14/code/giter_exact.py (`fab2cdc1893e`), output
(`58ddd86a52f2`), receipt (`8d28b5f2f807`).  Pin `aa161f8f8e9d`
(v14/note-giter-pin.md).  Parents at their terminal commits:
Γ-main (4747b47) and Γ-prep (2a859a1).  Reviews:
`v14/review-giter-{operator,effectus,instrument}.md`.
Candidate-reading rule in force on GITER-LAW-CONFIRMED and
every segment.

- **K1 THE CARRIER:** re-derive CONG-185 from the D74 artifacts
  with YOUR OWN congruence computation; verify all six ruling
  properties on their own objects and the MENU-113 2-of-6
  contrast (descent horizons; the 0-of-572 multi-valued edges
  vs 4-of-368; the 44-square set identity; both holonomies
  [2,3] rank 2 with the enlargement's disappearance; CK 0/10
  vs 4/10); the dims 1×5×17×49×113; no-mismatch-with-D74.
- **K2 THE TARGETS (decisive):** the step-normaliser proof on
  THIS carrier (k₁ = q/M at 0/30728; k₂ failing 1340/3968 —
  verify both, and rule whether the proof is carrier-native or
  inherited); the law values (15/38, 5/19, 13/38) at BOTH legs
  independently recomputed; leg-independence and left–right
  asymmetry; the census shadow's external-control status
  (token scan 0 hits — re-run the scan; the #82 disease must
  be provably absent).
- **K3 HOLONOMY:** r_k = r_q at 1362/1362 with YOUR OWN square
  enumeration; the deviation identity as must-pass 1546/1546;
  the 8 correction factors closing at MENU and 0 at CONG; REC
  flat both readings; is AGREES the honest head given the
  adjudicated expectation, and what would a deviation have
  looked like (the gate's two-way demonstration)?
- **K4 THE THREE ANSWERS (share effectus-decisive):**
  (a) QUANTUM=CARRIER-RELATIVE with the mechanism — verify the
  entire signature is carried by MENU's 4 multi-target edges on
  the 45 recurring classes (the mechanism's OWN gate); the
  eq-22 carrier stamps (2/4 speaking vs silent — the negatives
  36/104/108/164); candidate readings of "carrier-relative"
  (is it licensed as stated, or does it need
  the-coarser-carrier-is-Markov phrasing?); (b) ANCHOR=
  SEDIMENTARY — verify path (a)'s failure at the class grain
  (0/185 recurring; prefix-class returns 0 vs 1900; the root's
  class depth profile); **audit the CAP-DRIVEN stamp hard**
  (purity at round 4-of-5; spanning counts 17→5→1→0→0): is
  every negative honestly scoped to this cap, and is
  SEDIMENTARY the licensed head or does it need
  AT-THIS-CAP qualification in the string itself?
  (c) the surprises: the [B3] column-sum-coupling isolation
  (772 rows 0 infeasible; coupled 4/4 @CONG with witness vs
  3/4 @MENU Farkas) — **verify the correction to Γ-main's
  4-of-4 and the ONE interpolant cell's witness**; the atom's
  death (δ* 1→0; the 72 trivial atoms); the (1,1)-block
  5-class split (256/64/16/4/1).
- **K5 INSTRUMENT** at the full era incl. the two newest
  engravings THIS UNIT PREDATES: **the gate-to-disk seal
  (#119)** — run the seam battery (post-gate head flip;
  published-row corruption; post-write receipt corruption) and
  rule how wide the seam is here; **text gates (#125)** if any
  prose-matching gates exist; the #82 CLI under hostile argv;
  58 gates vs 54 falsifiers at #34 (the 3 waivers'
  machine-checked forcings); the paper-13
  declared-and-not-read exclusion (correct per #46/#91 —
  verify nothing consumed it); G-REFINE-DETERMINISTIC (the
  found-and-fixed bug — verify the fix and hunt the CLASS:
  any other set-iteration-order dependence); comparator
  independence (#82-strengthened); verify-paper coverage (750
  numerals); byte-identity + off-tree/git-less at your own
  hands; repo hashes unchanged (concurrent workers disclaimed:
  u4_*, u4b_*, r5_*).
