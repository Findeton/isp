# Γ-PREP (paper-11, the transport foundation) — HOSTILE REVIEW PROTOCOL (FROZEN)

**Frozen:** 2026-08-10, v14 ledger #84.  Three reviewers,
independent, single-file writes, scratch-only, no imports,
read-only git; all mutant/injection work on scratch copies (the
plain run WRITES artifacts).  Grades A/AWF/R; recomputations
counted; findings ranked with exact repairs.

**Object:** paper `09482eb080cc`
(v14/paper-11-transport-foundation.md), code `9a4f0529b840`
(v14/code/gprep_foundation_exact.py), output `097c08a0229d`,
receipt `dd86ad1a80d7`; committed 0f5d57e (#63).  Pin
`42ce06e6be8a` (v14/note-gprep-transport-foundation-pin.md).
Reviews: `v14/review-gprep-{operator,effectus,instrument}.md`.

**Standing context the panel MUST carry:**
- **The #66 correction of record:** my #65 verification of this
  unit was VACUOUS — its CLI silently ignored unknown flags, so the
  `--selftest` invocation ran a plain delivery.  The CLI-whitelist
  repair item is REGISTERED for this panel's adjudication.
- **The #82 engravings apply as review lenses:** the CLI-contract
  minimum (this unit is a NAMED recurrence) and
  comparator-independence-strengthened (no shared code, inputs, or
  typed literals between a verdict comparator and its builder).
- The #34/#62 standards (honest coverage denominators; verbatim
  anchors that bind meaning, with consumer gates and mutant
  falsifiers).
- **Downstream fact:** Γ-main (#68–#82) consumed this unit's rows;
  its instrument review found an unresolved probe of Γ-prep's
  receipt "silently swallowed" (R-GM-9).  What the probe should
  have found lives on THIS unit's side.

- **K1 THE SEVEN FACTS:** rebuild each transport-foundation fact
  independently from the pinned sources (the unit claims zero
  mismatches); verify exactness end-to-end, the provenance shas,
  and that each "fact" is a recomputation of a pinned row rather
  than a re-declaration of it.
- **K2 ARM B — THE ATOMS (decisive):** verify the Birkhoff route's
  death (unique parenthood — is the proof airtight and correctly
  scoped?); rebuild the R-SIG holdings-profile block decomposition
  and the δ*=1 EXACT ATOMS claim independently; verify
  BLOCKED-AT-THE-MONOTONE-HOLDINGS-LADDER is the honest head — is
  there any pinned fact that unblocks the ladder, and is "blocked"
  a statement about the corpus or about this construction?
- **K3 THE FEED-FORWARD BINDING:** enumerate exactly which rows
  Γ-main consumed (kernels, atoms, receipt keys); verify consumed
  = delivered (values and shas); resolve what R-GM-9's swallowed
  probe SHOULD have read (the flat receipt's actual key structure);
  flag any row Γ-main read that this unit does not actually pin.
- **K4 SCOPE AND MOTIVATION (effectus lens):** what the seven
  facts + atoms license and do not; candidate readings of the
  headline; the choice inventory at the RSQ standard (declared vs
  forced vs free); whether the atoms' exactness is
  arena-dependent; what the foundation means for the Γ iteration
  now that #82 ruled CONG-185 the carrier — do the atoms survive
  the carrier change, and is that measurable here or a successor
  question?
- **K5 INSTRUMENT:** the CLI audit FIRST, at the #82 contract
  minimum (argv parsing; unknown flags exit 2; a real `--selftest`
  that corrupts one anchor, exits 1, writes nothing; a `--mutant
  NAME` harness) — this unit's silent flag-ignoring is the
  registered disease; then gates-vs-mutants coverage at #34
  (honest denominators, assert-unmutated and tautological mutants
  named), injection classes (verdict, table, render, path-drift,
  quotation-meaning), waiver census at #34, verbatim anchors at
  #62, comparator independence at the strengthened standard,
  paper↔output↔receipt sweep, byte-identity of the plain run, repo
  hashes unchanged after all work.
