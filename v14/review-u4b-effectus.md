# U4b — HOSTILE REVIEW, EFFECTUS LENS (MEANING / SCOPE / MOTIVATION)

**Unit:** U4b, the schedule census — `v14/paper-17-schedule-census.md`.
**Protocol:** v14 ledger #149 (frozen in-entry), **K2 decisive**.
**Pin:** `v14/note-u4b-pin.md` (`d2cff9a274a8`), R1–R4, the four U4 walls inherited.
**Object:** commit `8c2dd98`.  **Lens:** what the measured objects are entitled to mean.

**Hashes verified at the start and re-verified at the end of this review
(sha256-12, the corpus convention):**

| artifact | expected | measured |
|---|---|---|
| `v14/paper-17-schedule-census.md` | `2bcba873d78e` | `2bcba873d78e` ✓ |
| `v14/code/u4b_schedule_exact.py` | `2c9b999dea31` | `2c9b999dea31` ✓ |
| `v14/code/u4b_schedule_output.txt` | `8715c46a7b7f` | `8715c46a7b7f` ✓ |
| `v14/code/u4b_schedule_receipt.json` | `c4a4e8223b64` | `c4a4e8223b64` ✓ |
| `v14/note-u4b-pin.md` | `d2cff9a274a8` | `d2cff9a274a8` ✓ |

Five for five, working tree byte-identical to the commit. No drift.

**Grade: ACCEPT-WITH-FIXES.**

**Recomputations: 88.**  77 delivered quantities independently reproduced,
**77 exact matches, zero mismatches**; plus 11 probe quantities the delivery
does not print.  The reimplementation shares no code, no constant and no
definition-by-reference with the delivery: partitions generated by my own
recursion, the co-division adjacency and HA 3.2's readout written from the
paper's prose, stabilizers by direct translation, all arithmetic in
`Fraction`.  Blocks inside that count: all 280 partitions and the closed
form; all 7,056 ordered seed-set pairs; all 78,400 partition pairs with the
full nine-site form at each; the 16-row resolvable table row for row; the
19,791-schedule joint cell by bitset contraction; the 540 window crystals
with all 6,480 of their edits; a 9,072-case family-scope fragility probe;
the 20-stratum structure; an R = 3 successor probe.

**The arithmetic is clean and I could not move a single delivered number.**
The findings below are about words, scopes, one false sentence in §8, and
one gate whose printed claim exceeds its own test.

---

## 1. K2.1 — CRYSTAL-GENERIC: what "emergence" is licensed to mean

### 1.1 What the census actually established

The pin pre-registered the null on **the seed set**: a seed set that is a
coset union yields `Stab ⊇ H` by the affine law, "and their crystallinity
carries NO emergence information."  The delivery emitted CRYSTAL-GENERIC
correctly against that pre-registration — I reproduce every number in it.
But the pre-registered variable is not the variable the period lives on,
and the census itself shows this.  My probe, over all 252 crystalline seed
pairs:

| shape of the field `1_{S₀} + 1_{S₁}` | pairs | structure |
|---|---|---|
| value 2 on one H-coset, 0 elsewhere | 12 | `S₀ = S₁` = a coset |
| value 1 on two H-cosets, 0 on the third | 240 | `S₀ ⊔ S₁` = those two cosets |

**252 of 252 crystalline seed pairs have `supp(n)` equal to a union of
cosets of the period subgroup** — including all 216 beyond-coset pairs,
whose seed sets are always *disjoint* and, my probe adds, in which
**neither** part is ever a coset (the crystalline classes by
(S₀ is a line, S₁ is a line) are exactly `(T,T)` 36 and `(F,F)` 216; the
mixed cell is empty).  So the affine law `n = c + m·1_S` explains every
crystal in this census — with `S` the union.  Nothing in the family is
crystalline outside the inherited mechanism.  The delivery's §6 is honest
about the structure ("the field takes the value 1 on a union of two
H-cosets … while neither seed set is an H-coset"); what is not said is the
consequence: **the null was stated on the wrong carrier, and the census
re-seats it rather than defeating it.**

The mechanism is also closed in closed form, which the delivery reports as
measured: for each of the 4 subgroups and each of its 3 cosets to exclude,
the remaining 6 sites split into ordered (S₀,S₁) in C(6,3) = 20 ways, 2 of
them coset-to-coset; 12 × 18 = **216**, 12 × 2 + 12 = **36**, total 252 =
4 × 63.  Every one of those numbers is the delivery's.

### 1.2 The exact sentence (the licensed claim, head 1)

> Over the declared family of 57,153,600 arbitration schedules on
> CONFLICT-GRID(3, 2), the division-event field is crystalline at the
> initiator reading at 2,041,200 schedules — exactly 1/28 — in all four
> order-3 directions with exactly equal weight (63 seed pairs each).
> Crystallinity is **not confined to the pre-registered inherited locus**:
> 216 of the 6,912 beyond-coset seed pairs, 1,749,600 of 55,987,200
> beyond-coset schedules, exactly 1/32, are crystalline; they are **six
> sevenths of all crystallinity in the family**.  In every one of them the
> period is carried by the two seed sets jointly — the seed sets are
> disjoint, neither is a coset, and their union is exactly two cosets of
> the period subgroup.  The null's affine law is therefore not violated
> anywhere in the census: at 252 of 252 crystalline seed pairs the field is
> a non-negative integer combination of the period's coset indicators.
> What is measured is that **the constructor-inheritance argument was
> stated on the wrong variable** — the carrier of the period is the summed
> division field's support, not either seed set — not that a mechanism
> outside the affine law exists on this arena.  Scope: this grid, this
> budget, R = 2, the initiator reading, the declared family.

`6/7` is exact (1,749,600 / 2,041,200) and the delivery does not print it.
It is the strongest true statement available on this head and I recommend
it be added: the inherited locus contributes one seventh of the
crystallinity it was supposed to explain.

### 1.3 Word calibration, and the over-readings killed

- **"emergence"** — licensed *only* as "outside the pre-registered per-seed
  locus".  **Not licensed** as "outside the inherited mechanism", "a new
  mechanism", or "the affine law fails".  Repair, §10: replace *"That is
  the emergence question's first positive datum on this arena"* with
  *"That is the first crystallinity on this arena that the pre-registered
  per-seed locus does not contain — and it is still the affine law, taken
  on the union of the two seed sets rather than on either one."*
- **"GENERIC"** in the verdict string is a **pre-registered branch name**,
  not a genericity claim.  The measured rate is 1/32 of the beyond-coset
  class and 1/28 of the family — a minority.  The string carries its own
  fraction, so it does not mislead; the adjudication should read the token
  as NOT-CONFINED-TO-THE-SEEDED-LOCUS.
- **"the period belongs to the PAIR"** (ledger #146 headline) — true as
  written, and the delivery's own §6 wording ("a property of the pair, not
  of either seed … precisely what the affine reading of a *single* seed set
  cannot produce") is correctly hedged.  Sustained.
- **"relational order" as a measured category — NOT LICENSED, killed.**
  `Stab(n)` is by construction a function of the aggregate field
  `n = 1_{S₀} + 1_{S₁}`; a category of order that is "in the relation and
  in neither part" would require a carrier that is *not* a function of the
  aggregate.  This census exhibits the opposite: the carrier is the
  aggregate, and it is a coset union in 252 of 252 cases.  The finding is a
  statement about **which variable was pre-registered**, i.e. bookkeeping,
  and it may not be promoted to an ontological category.  The honest
  category name is *the carrier is the sum, not the summands*.

---

## 2. K2.2 — the DET segment, and what the weld line may now claim

### 2.1 Verified

747 of 78,400 partition pairs non-degenerate at all nine sites; 544,563 of
57,153,600 schedules; the ROW|ROW/DIAG witness `q = [[0,−1],[−1,2]]`,
det = −1 at 9 of 9; the whole 16-row resolvable table, homogeneous in every
row; 6,561 of 11,664 window schedules non-degenerate; POSDEF ceiling 3;
**I7-STRICT-EMPTY at 0 of 78,400**; and the budget arithmetic — my census
finds the maximum total link-incidence count over the whole family is
**exactly 18** (attained by 1,296 pairs).  All exact.

### 2.2 The licensed claim (head 2)

> A non-degenerate grammar-generated induced form **exists**: 747 of 78,400
> groupings, 544,563 of 57,153,600 schedules, with the named witness
> ROW|ROW/DIAG — in the declared window, FORCED — inducing
> `q = [[0,−1],[−1,2]]`, det = −1 at every one of the nine sites.  It is
> the corpus's first non-degenerate grammar-generated geometry carrier, in
> the pin's own words, **and it is nothing more than that**: no schedule in
> the family is positive definite at all nine sites, and no schedule
> satisfies I7's strict-positivity criterion — both by one counting
> theorem.  Six division events on three-cell footprints deposit at most
> **18** incidences over I7's 27 (link, site) cells, while all-sites
> positive definiteness needs at least **27**.  The renewal-crystal weld
> census the U4 effectus predicted EMPTY is EMPTY, and **the obstruction is
> the budget, not the diagonal**: the wall survives every redistribution of
> those 18 incidences and holds at all 78,400 groupings, whereas the
> diagonal explanation held only at the committed point.

### 2.3 "Engineering, not mystery" — licensed, in exactly this form

> The emptiness is a **resource deficit in the committed cycle**, not a
> structural impossibility of the arena.  Nothing measured here excludes a
> constructor whose budget deposits ≥ 27 link-incidences, and the minimal
> saturating configuration is exhibited: three rounds grouped on the three
> link-direction parallel classes give `n_ℓ(x) = 1` at all 27 cells,
> `q = [[1, −1/2], [−1/2, 1]]`, det = 3/4 > 0, **positive definite at all
> nine sites**, and I7's strict criterion satisfied for the first time.

(That probe is mine, exact, and is the successor register's first row.
It is not in the delivery.)

**Not licensed**, and the adjudication should bar all three:

1. that such a constructor is **grammar-admissible** — no R = 3 record is
   driven anywhere in this unit, and at R = 3 the round-2 conflict-supply
   question is new;
2. that clearing the count **clears the weld** — I7 strict positivity is
   one of the census's conditions, not all of them;
3. that the wall explains **the measured ceiling of three** positive-definite
   sites.  It does not: 18 incidences at 3 per posdef site permit **six**.
   The ceiling of 3 is a measurement with no derivation (see MINOR-2).

---

## 3. K2.3 — the signature surprise, the walls, and the resonance

### 3.1 The paper sweep: no breach

I swept the paper for every wall-adjacent stem (`lorentz`, `cosmolog`,
`spacetime`, `causal`, `boost`, `rapidity`, `frame`, `signature`,
`riemann`, `metric`, `universe`, `expansion`, `minkowski`, `hubble`,
`light-cone`, `de sitter`, `arrow of time`).  Result: **every occurrence of
a barred word is inside §8, in the declaration of the wall it belongs to,
or is the pin's own quoted text.**  `metric` occurs twice, both times
carrying the pin's own question.  `time` occurs only as "run time" /
"gate-time".  No cosmological claim phrase occurs anywhere.  The four walls
are argued before the tests, not after.  **No breach.**

### 3.2 The failure is the opposite one: the resonance is neither named nor read

The protocol requires the Lorentzian resonance **NAMED-NOT-READ**.  The
delivery achieves NOT-READ by silence — §5.3 reports 261 pairs with det < 0
at all nine sites and 486 with both signs, and never says the word.  A
two-by-two symmetric form with det < 0 is an indefinite (1,1) form, and in
this corpus that is a loaded observation.  Leaving it unnamed leaves the
inference **ungoverned**: the reader supplies it and nothing in the paper
stops them.  That is MAJOR-2 below.

### 3.3 What IS licensed about indefinite signature here

1. The induced form is **generically indefinite and never definite
   everywhere** — a theorem, from the 18 < 27 wall.
2. The non-degeneracy locus and the signature are functions of **the
   groupings alone**; the seeds move initiators, not footprints.
3. **The signature is not even a schedule-level invariant.**  486 of the
   747 non-degenerate pairs carry both signs across their own nine sites.
   So "this arena has signature X" is not merely unlicensed — it is
   **ill-posed for the majority of the carriers**.  This is the strongest
   anti-over-reading datum in the unit and it is currently buried in a
   sentence about counts.
4. The two coordinates are **link directions e₁, e₂ of Z₃²**, not tangent
   directions and not a time direction; no causal order is defined on them,
   no group acts but the translation group of nine sites.
5. "Never Riemannian" = "never positive definite at all nine sites", a
   statement about a rational 2 × 2 matrix read out of counts by HA 3.2.
   It carries no manifold.

---

## 4. K2.4 — the window's honesty, the footprint reclassification, fragility, d60

### 4.1 The window: sufficient, with one clause added

1/4900, named **inside** the constructibility verdict string, containing
the committed schedule, with the two exhaustive columns licensed by the
per-object equality gate (11,664 driven fields = 11,664 combinatorial
fields, zero mismatches) and 20 out-of-window stratum witnesses.  I
verified the window is exactly 16 × 729 = 11,664 and the fraction exactly
1/4900, and I verified independently that **the stratification has exactly
20 non-empty cells** — 10 (stabilizer × affine class) combinations, each
realized both non-degenerate and degenerate.  The disclosure discipline is
met.

One clause is owed (MINOR-7): the equality gate is verified *on the
window*, so the exhaustive columns are exhaustive over the **declared**
family, while constructibility is established only on the window plus the
20 witnesses.  Every rate in §4–§6 is therefore a rate over declared
schedules.  The risk is low (the cycle imposes no geometric condition on a
triple, and the witnesses cover every census cell) but the denominator
should say what it is.

The window is also **the most symmetric window available** — all-parallel-class
groupings are exactly the resolvable device the constructor itself uses, so
FORCED-everywhere is measured in the most favorable place.  The 20 witnesses
are what price that, and §11.1 should say so in those words.

### 4.2 The footprint reclassification: cross-reference only, no erratum

U4's paper-14 (frozen, `06b89fe`) already prints the constant footprint
fields `2,2,…` and `4,4,…` at both CONFLICT-GRIDs, and the U4 adjudication
already says "the two constant-field cells are vacuous positives, now
said".  U4b adds the **cause and its scope**: the constancy is a property
of the budget — every round's three groups partition the nine cells, so
every cell lies in exactly one footprint per round — and therefore holds at
**every schedule the committed budget admits**, not at those two crystals.

Ruling: **no erratum, no scope-annotation note.**  Nothing in U4 is false;
its verdict string's two `Z3^2` entries are true measurements that were
already annotated vacuous.  What is owed is **one cross-reference line** in
the U4 adjudication's register: *"U4b (#146) measured the constant-field
vacuity at family scope: the footprint field is ≡ R at every schedule the
committed budget admits, so the two Z3^2 entries are budget artifacts, as
this adjudication said."*  A scope-annotation note in the paper-12 style
would be over-machinery for a claim that is not narrowed.

### 4.3 Fragility: the delivery under-claims, and I can hand it the stronger fact

§7 measures 12 of 12 admissible re-seatings breaking the stabilizer at all
540 window crystals — verified, all 6,480 edits.  But the delivery's stated
mechanism ("a difference of two distinct point masses is never constant on
the cosets of an order-3 subgroup") proves only that **the original period
is not preserved**; it leaves open that an edit lands on a *different*
nontrivial period, in which case the crystal would be rotated, not
destroyed, and "maximally fragile" would be the wrong word.

I closed that gap at **family scope**, not window scope.  Over all 252
crystalline seed pairs, every single-point re-seating of either seed to any
other site — **9,072 cases** — lands on the **trivial** stabilizer.  Zero
land on any nontrivial subgroup.

So the licensed fragility claim is stronger than the one delivered:

> No crystal in the family survives a single re-seating: at all 9,072
> one-point moves of one seed of a crystalline pair, the edited field's
> stabilizer is trivial.  Crystallinity here is not merely un-preserved
> under the minimal admissible edit — it is destroyed by it.

What "order is brittle here" may mean: **the period is not robust to the
minimal admissible perturbation, at every crystal, in the whole family.**
It may not mean that the crystalline *class* is unstable, small or special
— the class is 1/28 of the family, exactly uniform across the four
directions, and closed under nothing that was tested.  Brittleness is a
property of each crystal, not of the phenomenon.

### 4.4 The d60 seed-dependence: the registration this owes the corpus

The delivery found, live, that d60's `pick` breaks ties with
`sorted(key=repr)` and that the sorted objects are event tuples embedding
frozensets produced by d42b1's `triples()`.  **I confirmed the mechanism
empirically**: a `frozenset` of `(str, str, int)` tuples reprs in four
different orders under four `PYTHONHASHSEED` values.  The defect is real
and the delivery's disclosure of it is correct.

It must be registered **scoped**, or it will generate a corpus-wide false
alarm.  `key=repr` appears in v7 (12 files), `bc`, `v14/w2_census_exact.py`
and the v10 originals, but the hash-randomized case requires sorting
objects that **embed a set/frozenset of strings**.  Sorting strings, ints
or tuples of them by `repr` is deterministic.  The exposure is therefore:

1. **The site.** `v10/code/d60_crystal_exact.py:131` (`pick`), and any
   re-implementation of it — reached only when **more than one candidate
   matches**, i.e. `maxhits > 1`.
2. **The immunity clause.**  A unit whose every event is specified by its
   full tuple never consults the tie-break.  U4b states it and is immune;
   I verified **U4 (paper-14) is immune too** — its `G-FORCED` gates record
   `maxhits = 1` at all five crystals, so no terminal U4 number is
   process-local.
3. **The demand.**  (a) a LOG registration of the defect with the
   `PYTHONHASHSEED` demonstration; (b) one RUNBOOK line — *a
   byte-reproducibility claim by any unit driving a v10 grammar layer must
   state the `maxhits == 1` immunity or price the tie-break*; (c) a scoped
   audit of the units that carry a `pick`/`maxhits` mechanism —
   `d60`, `d63`, `d66`, `d67`, `u4_crystals`, `w2_census`, `u4b` — each to
   record immunity or exposure.  Not a corpus-wide `key=repr` sweep.

---

## 5. K2.5 — the choice inventory at the RSQ standard

The standard (weld-2 pin R4): every choice classified **declared / forced
(forcing exhibited) / free (fiber measured and printed)**; MOTIVATED ⟺
zero free items.  Ten rows are declared; the deviations §11 prices six
items, and items 2 and 3 (U4 and weld-2 cited-not-read under rule #91) are
correctly priced — the substitute anchors are strictly older and the
committed d66 function object is re-run in process.  That pricing is
sustained.

Four findings against the standard:

**(a) A load-bearing choice is missing entirely: the co-division adjacency.**
"For a link l and a site x, the number of division events whose footprint
contains both x and x + l" is what makes the determinant column exist.  It
carries the det census, the budget wall, POSDEF-EMPTY and I7-STRICT-EMPTY.
It appears in **no inventory row**; item 6 covers HA 3.2's readout
(`q₁₁ = n_{e₁}` …), which is a different object — the readout maps link
counts to a form, the adjacency says what a link count *is*.  Its verbatim
statement exists in U4's paper-14 (frozen at `06b89fe`), the artifact this
unit declines to read; here it is anchored only **operationally**, by
reproducing one committed row.  Repair: add a row — *the co-division
adjacency | **declared** | fiber ≥ 2 | U4's own definition, anchored here
by one-row reproduction* — and add to §11 that the whole determinant column
and both wall theorems are relative to it.  Name I7's link set
{(1,0), (0,1), (1,1)} in the same row: the wall's arithmetic is |L| = 3 and
*which* 3 (the antidiagonal class deposits nothing).

**(b) Item 7, the window, has fiber 1.**  The pin names at least two
admissible window shapes ("the d74 dep−2 shape or a declared sub-family").
Fiber ≥ 2; price as in §4.1 above.

**(c) Item 9, the fragility edit, is `free` with fiber "—".**  The RSQ
standard requires a free item's fiber to be **measured and printed**; a dash
is a deviation.  Two clean repairs, and the second is an upgrade the
delivery left on the table: exhibit the forcing that §7's own first
sentence already contains — *any edit that moves exactly one division event
is a re-seating within its group, and there are 12* — and reclassify the
row as **declared / 2** (event-level re-seating vs. group-level
re-partition), pricing that a partition-level fragility is a different
quantity (§11.4 half-says this).  That leaves **zero free items**, i.e.
**MOTIVATED at the RSQ standard**, which the unit does not currently claim.

**(d) Unlisted but instrument-side:** the 20-witness stratification variable
and its "first in a fixed enumeration" selection rule.  Fold into the window
row or add a row; deterministic, no fiber consequence.

---

## 6. Findings

### MAJOR-1 — §8's independence claim is measurably false, in the wall section

§8 reads: *"The two objects that looked like one cause at the committed
point are **independent** across the family."*  The marginal half is exactly
right and I verify it: the period direction is uniform, 63 seed pairs per
subgroup, no direction preferred.  The joint is not.  Measured, exact, over
all crystalline schedules:

| period | schedules | diagonal link populated | rate |
|---|---|---|---|
| ⟨(0,1)⟩ | 510,300 | 503,388 | 4661/4725 |
| ⟨(1,0)⟩ | 510,300 | 503,388 | 4661/4725 |
| ⟨(1,1)⟩ | 510,300 | **490,860** | **101/105** |
| ⟨(1,2)⟩ | 510,300 | 503,388 | 4661/4725 |

The diagonal period is the one direction that behaves differently, and the
coupling is large in the conditional direction: among crystalline schedules
whose **diagonal link count is empty**, the period is the diagonal at
19,440 of 40,176 = **15/31**, against 1/4 under independence — a factor
1.94.  So the two objects U4's panel found jointly forced are *still*
coupled across the family, and coupled **exactly at (1,1)**, in the section
whose job is to govern the diagonal reading.

*Repair (exact).*  Replace the sentence with: *"The two objects that looked
like one cause at the committed point are **separately carried and neither
determines the other** — the period is a seed property, the diagonal link
count a grouping property.  They are not independent: conditional on the
diagonal link count being empty, the period is the diagonal at 15/31 of
crystalline schedules against 1/4 under independence, the only direction
that departs from uniformity in the joint.  The marginal is exactly uniform
(63 seed pairs per subgroup); the residual coupling is a transversal
constraint between seed sets and groupings, and it is read as nothing
else."*  This strengthens the wall rather than weakening it: the diagonal is
still special in the joint, and saying so is what keeps the bar honest.

### MAJOR-2 — the Lorentzian resonance is neither named nor read

Per §3.2.  *Repair (exact), one sentence at the end of §5.3:* *"In two
dimensions a form with det < 0 is indefinite, of signature (1,1).  The
corpus's Lorentzian vocabulary is not in play: the two coordinates are the
link directions e₁ and e₂ of a nine-site lattice, no causal order is
defined on them, no boost group acts, and the sign is not even constant
within a schedule at 486 of the 747 non-degenerate groupings — so a
signature reading of this arena is not merely barred, it is ill-posed.  The
resonance is named here so that it is not read."*  I checked that this
sentence trips no gate: `G-WALL-DIAGONAL` tests claim phrases
(`COSMO_CLAIMS`), not the stems, and `G-WALL-BHS` does not scan the paper.

### MAJOR-3 — a wall gate's printed sentence claims a measurement it does not make

`G-WALL-BHS` prints *"Measured on this program's own source: no sprinkling,
no boost, no rapidity and no frame is computed anywhere"*.  The gate's
value is `bhs_run = pick("MUT-WALL-BHS", False, True)` — a hard-coded
literal, flipped only by the mutant.  **No scan of the source occurs.**  The
claim happens to be true (I grepped: those stems appear only in gate prose,
mutant descriptions and the verbatim anchor), which is exactly why the
sentence must not assert a measurement.  `SELF` is already available at
line 119, so the scan is a two-line repair.  *Repair:* perform the source
scan and report its hit list, or rewrite the sentence to *"No sprinkling,
boost, rapidity or frame construct occurs in this program; the gate carries
the declaration and the mutant falsifies it."*  **Shared finding — routed
to K3 (instrument) as owner and K1 for the wall-as-theorem reading.**

### MAJOR-4 — the co-division adjacency: unlisted, and anchored only up to a sign

Per §5(a).  Compounding it: the delivery's anchor `A-EFF-I7` is declared in
code as the tuple `(n_(1,0), n_(0,1), n_(1,1), q_11, q_22, |q_12|, det)` —
**the absolute value**.  So the reproduction that validates the route does
not check the sign of q₁₂, and the paper's §5.1 statement `q12 = -1` is not
the anchored quantity.  Harmless for every number in the unit (det and
positive-definiteness depend on q₁₂²), but §2.3's claim that the route "is
validated by reproducing the effectus's committed row **exactly**" should
read "exactly, up to the sign of q₁₂, which the ASCII fold does not carry".

### MAJOR-5 — the CRYSTAL head's §10 sentence over-reads

Per §1.3.  Repair given there verbatim.

### MINOR-1 — §2.3's anchor list is unlabeled

*"the U4 effectus review's CONFLICT-GRID(3,2) row in I7 coordinates,
1, 1, 0, 1, 1, 1, 0"* — the source table's sixth entry is **−1**; the
delivery's sixth entry is |q₁₂|.  A reader who checks the source finds a
mismatch that is not one.  *Repair:* write the tuple's schema, as the code
already does: *"(n_(1,0), n_(0,1), n_(1,1), q₁₁, q₂₂, |q₁₂|, det) =
1, 1, 0, 1, 1, 1, 0"*.

### MINOR-2 — §5.3 lets the wall reach further than it does

The 18 < 27 argument proves **POSDEF-EMPTY at all nine sites** and
**I7-STRICT-EMPTY**.  It does not prove the measured ceiling of 3: at 3
incidences per positive-definite site, 18 incidences permit **6**.  *Repair:*
after "18 < 27, and the exhaustive census agrees", add *"The same count
permits as many as six positive-definite sites; the measured maximum is
three, attained at 252 partition pairs, and that finer ceiling is a
measurement here, not a consequence of the wall."*  Register the gap as an
open (successor row 6).

### MINOR-3 — `G-WALL-DIAGONAL`'s printed sentence is falsified by the paper

It prints *"No cosmological word appears in the paper"*; the word
"cosmological" appears twice in §8 (the pin quote, and the sentence that
respects it).  The gate's code comment is correct — *"the wall bars a
cosmological READING, not the word"* — and the test uses claim phrases.
*Repair:* print what the gate tests: *"No cosmological claim phrase appears
in the paper; the bar is on the reading, not on the word."*  Routed to K3.

### MINOR-4 — window fiber 1 should be ≥ 2.  Per §5(b).

### MINOR-5 — the free item's fiber is unprinted; the forcing is available.  Per §5(c).

### MINOR-6 — the named witness's two unremarked properties

ROW|ROW/DIAG has `q₁₁ = n_{(1,0)} = 0`: the e₁ coordinate direction is
**null** in the very form the paper calls the corpus's first non-degenerate
carrier.  And it is a same-class-twice schedule, hence one of the 2,916
records of **24** events — its round-1 groups repeat round 0, so no conflict
supply is needed.  Both facts are the *reason* it is non-degenerate: both
rounds pile their incidences onto one link, giving `n_{(0,1)} = 2` and
det = −1.  §3 and §5.2 both contain the ingredients and never meet.
*Repair, §5.2:* *"The witness is non-degenerate for a specific reason: never
rotating concentrates both rounds' incidences on a single link, which is
also why its record is one of the 2,916 short ones (24 events, no round-1
conflict supply) and why q₁₁ = 0 — the e₁ direction of the resulting form
is null.  Non-degenerate, here, is a consequence of dynamical poverty, not
of richness."*  This is the caution the coupling unit needs.

### MINOR-7 — the denominator's scope.  Per §4.1.

### MINOR-8 — an under-claim: 6/7 is not printed

1,749,600 / 2,041,200 = **6/7** exactly.  Six of every seven crystalline
schedules in the family lie outside the inherited locus.  Recommend adding
to §6 and to the adjudication's reading; it is the head's strongest true
number.

---

## 7. What survives, in one paragraph each

**CRYSTAL head — SUSTAINED, re-worded.**  The verdict string stands as
emitted; the numbers are exact; the branch was correctly selected against
its pre-registration; the mechanism sentence in §6 is honest.  The single
required change is that "emergence" be calibrated to "outside the
pre-registered per-seed locus", because every crystal in the census is the
affine law taken on the union.  With that change the head is the strongest
claim in the unit: **the null's locus contains one seventh of the
crystallinity it predicted.**

**DET head — SUSTAINED, and its two negatives are the real result.**
DET-NONZERO-EXISTS is true and named; POSDEF-EMPTY and I7-STRICT-EMPTY are
theorems from one counting bound, and that bound **displaces the diagonal**
as the weld's obstruction — the deepest thing this unit found.  The
"engineering, not mystery" reading is licensed only in the exact form of
§2.3 above, with the three barred inferences barred.

**CONSTRUCTIBILITY head — SUSTAINED as scoped.**  FORCED 11,664 of 11,664
with both other fates exhibited by declared controls in the same run is a
measurement, not a tautology, and the scope is inside the string.  One
clause on the denominator (MINOR-7) and it is complete.

---

## 8. The successor register

- **S-U4B-EF-1 — the bigger-budget constructor (the weld route).**  Its pin
  must demand, as an **entry criterion and not an outcome**, a budget
  depositing **≥ 27 link-incidences** on I7's link set.  The minimal
  saturating structure is exhibited and must be its first object: **R = 3,
  the three rounds grouped on the three link-direction parallel classes
  (ROW, COL, DIA)** — computed here exactly: `n_ℓ(x) = 1` at all 27 cells,
  `q = [[1, −1/2], [−1/2, 1]]`, det = 3/4 at all nine sites, positive
  definite at all nine, I7 strict positivity satisfied.  Four demands on
  top: (i) constructibility **driven**, since the round-2 conflict-supply
  question is new; (ii) the affine null **re-pre-registered**, because at
  R = 3 the field sums to 9 over 9 sites so the shape (1,1,1) exists and
  **the full group Z₃² becomes reachable** — U4b's "the full group never
  occurs" is a budget fact that dies at R = 3; (iii) the null stated on the
  **summed field**, per §1; (iv) an explicit statement that ≥ 27
  incidences is necessary, not sufficient, for the weld.
- **S-U4B-EF-2 — the law-side stochastic sibling (registered, not run).**
  It inherits three numbers as its null: the family rate **1/28**, the
  beyond-coset rate **1/32**, and the exact uniformity across the four
  directions (63 seed pairs each) — a stochastic crystallization claim on
  this arena must beat a base rate, not merely exhibit crystals.  It also
  inherits the carrier finding: randomizing seeds and randomizing the field
  are different experiments, and only the second addresses the period.  And
  it must survive §7: no deterministic crystal here survives one edit.
- **S-U4B-EF-3 — what THE COUPLING UNIT inherits.**  Taking ROW|ROW/DIAG as
  the first candidate shared arena is **PREMATURE**, on three measured
  grounds: 486 of 747 non-degenerate groupings have site-varying
  determinant sign, so "the arena's signature" is undefined for the
  majority; the witness has a null coordinate direction (q₁₁ = 0) and is
  the dynamically poorest schedule in the family (never rotates, short
  record); and no selection criterion among the 747 has been declared.  What
  the coupling unit **should** inherit: (i) the **budget wall as a resource
  criterion** on any shared arena it proposes; (ii) the structural fact that
  non-degeneracy is a function of the groupings alone, so seeds remain free
  inside it; (iii) the co-satisfiability cell — **19,791** schedules
  crystalline *and* non-degenerate at all nine sites, **17,118** of them
  beyond-coset — which is the honest candidate pool, not one witness.
- **S-U4B-EF-4 — no pair-mechanism unit; register a theorem instead.**  The
  mechanism is closed in closed form (§1.1: 12 × 18 = 216, 12 × 2 + 12 = 36,
  252 = 4 × 63) and needs no census.  The open worth pinning is the
  **general-(g, R) law**: does the union-carrier statement, the exact
  uniformity across directions, and a closed-form rate survive other
  budgets?
- **S-U4B-EF-5 — the d60 registration.**  Scoped exactly as §4.4: the site,
  the immunity clause, the seven-unit audit, one RUNBOOK line.  U4 and U4b
  are both immune; that must be recorded, or the defect will read as
  retroactive doubt on a terminal unit.
- **S-U4B-EF-6 — the positive-definite ceiling (open).**  The wall permits
  six positive-definite sites; the measured maximum is three, attained at
  exactly 252 partition pairs.  Either derive the finer obstruction or
  register the ceiling as measurement-only.  Anyone reading "never
  Riemannian" as fully explained by 18 < 27 is reading past this gap.
- **S-U4B-EF-7 — the U4 cross-reference.**  One line in the U4
  adjudication's register per §4.2.  No erratum, no annotation note.

---

## 9. Recomputation ledger

| block | quantities | result |
|---|---|---|
| family count: 280 partitions, closed form, 7,560, 57,153,600 | 5 | exact |
| window: 11,664 and 1/4900 | 2 | exact |
| record-length dichotomy 2,916 / 8,748 | 2 | exact |
| footprint field constant 2, stabilizer Z₃² (as a family theorem) | 2 | exact |
| stabilizer census: 84 subsets, weight 90, 7,056 pairs, 6,804 + 63×4, 55,112,400 + 510,300×4, 2,041,200, 1/28 | 12 | exact |
| committed schedule q₁₁, q₂₂, q₁₂, det | 4 | exact |
| determinant census: 747, 78,400, 544,563, witness form ×4, 16-row table, 6,561 | 24 | exact |
| §5.3: ceiling 3, 18, 27, 9 pairs, 261, 486, 855, 5,238, 108, 522, I7-strict 0 | 11 | exact |
| §6: 291,600 / 874,800 / 55,987,200, 36 / 108 / 216 / 6,912, 1,749,600, 1/32, four subgroups, 19,791, 17,118 | 12 | exact |
| §7: 12 edits, 540 crystals, 12/12 at all | 3 | exact |
| **delivered subtotal** | **77** | **77 exact, 0 mismatches** |
| probes not in the delivery: 6/7 share; 15/31 conditional; four period-vs-diagonal rates; 9,072-case family fragility; 252-pair union structure; 20-stratum count; max incidences 18; det>0 ⇒ posdef; R = 3 saturating form; frozenset repr under 4 hash seeds | 11 | — |

Interpreter `/opt/homebrew/bin/python3.13`, exact `Fraction` throughout;
scratch at `…/scratchpad/u4b-ef/`.

**Closing hash re-verification:** paper `2bcba873d78e` ✓, code
`2c9b999dea31` ✓, output `8715c46a7b7f` ✓, receipt `c4a4e8223b64` ✓, pin
`d2cff9a274a8` ✓.  Unchanged across the review.
