# R3 — HOSTILE REVIEW PROTOCOL (FROZEN)

**Frozen:** 2026-08-09, v14 ledger #36.  **Object:** the R3 delivery as
committed (paper `00850cc796d0`, code `bbcc9a1aa7de`, output
`d54142292980`, receipt `1c8beb16c8a2`; commit cc069d5), verified
byte-identical (#35).  **Pin:** `v14/note-r3-relativity-pin.md`
(`a2ac89687a65`).  **Reviewers:** three, independent; single-file
writes (`v14/review-r3-{operator,effectus,instrument}.md`);
scratch-only; no imports; read-only git.  OFF-LIMITS: every other live
unit's files (paper-04..08, r6a_*/cra_*/crb_*/crc_*/crd_*, other
reviews) — a repair worker and four constructions are concurrent.

## Kill-shots (binding on all three)

**K1 — WHAT CARRIES THE HDA CLAIM (the forced-closure question).**
{H,H} closure is labelled FORCED (w linear in the front) — so the
positive finding rests entirely on the COEFFICIENT typing
(METRIC-READING-SITE-VARYING at 32/120).  Attack it: (a) is the
solve-for-c step well-posed and its over-determination a real
existence test; (b) is the metric-typing comparator genuinely
independent (#219) of the readout used to build the drag; (c) derive
whether the coefficient classes are THEMSELVES forced — HA's own
closed form ρ = (Λ−I)ω is in the pinned inheritance: does it
determine c analytically per rule, making the coefficient census a
theorem (the R2/R6a pattern)?  State exactly what remains measured if
so; (d) is SITE-VARYING a real algebra property or trivially
inherited from record inhomogeneity (would ANY inhomogeneous record
force it)?  The 32/120 count's robustness across the lapse family and
records.

**K2 — THE DEFECT'S REALITY (realization-relativity).**  The register
defect is measured at D-TOT, with D-REG trivial — two declared
realizations, one vacuous.  Hunt the realization space: enumerate the
D_a[v] realizations expressible from the pinned declarations (the
R6a-style census discipline); could any declared-expressible
realization absorb the register defect the way the BACKWARD
convention absorbed the front sector?  Verify: the closed form; the
not-a-boundary-term claim (the lattice-sum argument and the
degenerate probe); no-sector-vanishing; L/d-independence; the
convention sweep's 1-of-4 exactness.  If the defect is
realization-relative, the honest verdict segment changes; if it
survives the census, it is the strongest object in the unit.

**K3 — THE HDA CORRESPONDENCE.**  β is GW1's bracket covector
(q⁻¹-weighted), so the SHAPE matches Dirac/ADM.  Adjudicate the
correspondence: what would distinguish "the discrete HDA" from "a
deformation algebra with metric coefficients"?  The
metric-reading-CONSTANT vs SITE-VARYING split's sharpness; whether
the closing 26/99 sector is the HDA-analog sector or a subalgebra
artifact; what the rigid (constant-coefficient) cells mean.  Then the
forward statement: what would a successor need to EARN "GR's algebra
at scale" (the R4-relevant requirements).

**K4 — SCOPE.**  The five worker flags: w=1 anchored-not-swept; the
d=3 dense-route sparsity; the {D,H} d=3 probe convention (X07); the
non-constant tangential exclusion (bijection constraint — is the
restriction to translations load-bearing for the defect claim?); the
TRANSLATES enlargement.  Plus: is the L-sweep's constancy (closure
counts and classes constant in L) forced by the same linearity that
forces closure — i.e., is L-stability a theorem, not a measurement?

**K5 — INSTRUMENT.**  (a) Full falsifier audit: 60 mutants vs 81
gates; **the waiver audit at the #34 standard** (waiver claims are
gate claims — the 29 never-falsified rows' named forcings verified
one by one; the R6a audit found 12 false waivers by this method;
note: R3 delivered before the #34 engraving, so findings here bind
the REPAIR, not the delivery's good standing).  (b) Injections: the
five verdict classes; census-cell drops per bracket table;
coefficient-class corruption; defect-value zeroing (the R6a Y1
class); typed-count hunt (#24); path-value drifts.  (c) Verdict
rebuild from the receipt; all 14 segments flippable.  (d)
Paper↔output↔receipt number sweep (the campaign's prose standard).
(e) Two-run byte-identity; repo hashes unchanged after all work.

## Standing rules

Grades ACCEPT / ACCEPT-WITH-FIXES / REJECT.  Findings ranked with
severity, measured evidence, exact repair where definite.  No
numerical claim without recomputation; count recomputations.
Reviews frozen as delivered; disagreements are the adjudicator's.
