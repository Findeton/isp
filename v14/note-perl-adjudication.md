# PER-L (PAPER-28 — THE L-LADDER PERSISTENCE CENSUS) — JOINT ADJUDICATION (K1+K2+K3)

**Adjudicator:** the orchestrator.  **Date:** 2026-08-11 (v14
ledger #228).  **Inputs:** review-perl-operator.md
(d7a410b1c442, AWF, 741 recomps), review-perl-effectus.md
(d4945b1b5e3f, AWF, ~300 recomps), review-perl-instrument.md
(a624d1a5211a, AWF, ~360 recomps + 118 executions).  Object
at 9fcc081: bd0298e2a482 / 976d5b9e4ac8 / e4ff37a7a13e /
54ec5a9e9b72; pin 973b160d52ed.

## VERDICT ON THE VERDICT

**PERL-SIDON-SUFFICIENT-NOT-NECESSARY SURVIVES** — zero
delivered numbers moved across ~1,401 seat recomputations.
**THE SCALE SEGMENT MUST MOVE (both content seats,
independent):** the delivered band ("admitted sizes ARE the
even L in [2r+2,4r]") is falsified twice — K2's odd-L
alphabet-extended witness (L=9, width 3, THIRDS-19) and,
decisively, **K1's in-alphabet witness (L=7, width 2, the
(7,4,2) difference set — a witness species the unit's
involution-pair search was structurally blind to; Z₇² has no
involutions).**  The corrected law is BETTER than the
delivered one: K1 proved the absence half FORCED AT EVERY
WIDTH (the injectivity theorem L ≥ 4r+1 ⟹ DDS-free ball,
plus the alphabet's support-size ceiling 4) and ran the
census to completion — **the true in-alphabet sections are
{4} / {6,7,8} / {8,10,12}, two-sided, with evenness deleted
and the alphabet-relativity stamped.**  Also promoted: the
five persistence breaks are ONE mechanism (K2: monomial ⟹
integer velocity, one line); the width=interior-radius join
is FORCED (both = {1,…,diam−1}); the DDS theorem's
field-freeness carries K1's nine-characteristic evidence.

## RULINGS

1. **The prediction ledger records THREE PARTS, not a
   fraction** (K2, adopted verbatim): mechanism CONFIRMED;
   transport to the R-ladder CLOSED BY THEOREM (LINK is
   Sidon at every L ≥ 3 — PER-R inherits this as a
   corollary, one census cancelled); control
   REFUTED-AS-STATED, with the compression note (the false
   universal entered between S-EF-1 and the register).
2. **The successor prediction is the CONVERSE** (K2 M4):
   DDS-carrying ⟹ interference — measured 5/5 in the
   delivered receipt, already falsified ALPHABET-RELATIVELY
   by the width-3 witness; registered with both facts and
   its falsification conditions.  "DDS-free ⟹ no
   interference" is a THEOREM and moves to the theorem
   column.
3. **R4 RECEIVES A SCOPE-ANNOTATION NOTE** (K2, the paper-12
   precedent): its head's SCALE=L=4-UNIQUE and
   LOCALITY-IFF-L≥4 are width-1-relative (its body
   self-scopes twice; the head does not); the trichotomies
   gain the fourth branch WIDEN-THE-WINDOW.  The terminal
   paper is UNTOUCHED; the note is a separate file.
4. **Paper-20 needs NO edit** (K2: both its register rows
   reproduce exactly; the 78-vs-72 reconciled).
5. K3's seam named of record: the unit MEASURES HONESTLY and
   DESCRIBES ITS INSTRUMENT OPTIMISTICALLY — all ten K3
   MAJORs are liftable instances of engraved families (E-22,
   E-23, #119, #34); NO new engraving.
6. The L=3 control rung (28% of the scan) must be NAMED
   (K3 M5); "first counterexample" wording de-ordered
   (K1); the field-with-involution category fix (K1).

## REPAIR ORDERS (binding; one worker; the four files in
place + ONE new note file)

- **U1 (SCALE):** the band rewritten as the completed
  two-sided law — the L=7 difference-set witness verified
  in-code (autocorrelation + full 49×49 unitarity); the
  injectivity theorem + ceiling in-code as the absence half;
  the sections {4}/{6,7,8}/{8,10,12} gated; evenness
  deleted; alphabet-relativity stamped on the SCALE clause
  itself; the head's SCALE segment re-derived (U12 binds).
- **U2:** the odd-L/THIRDS-19 disclosure coherent across
  SCALE and CONTROL clauses.
- **U3:** the one-mechanism unification (monomial ⟹ integer
  velocity gated); the DDS persistence row renamed; the
  table recounted honestly (four BREAKS earned).
- **U4:** the successor-register section WRITTEN (it was
  absent); the three-part ledger record; the converse
  prediction registered per ruling 2.
- **U5:** attributions — the Moore-ball collapse cited (DDS
  subsumes FOUR results); the fingerprint's L=4/L=8 rows
  credited to R5's published table (only L=6 new).
- **U6:** K1's completed absence census + the
  nine-characteristic scans in-code; the
  field-with-involution fix.
- **U7 (K3 M1):** the DDS ball census RUN and gated
  (G-DDS-WINDOW gains its ledger row).
- **U8 (K3 M2):** SEAL-TOTALS at value-close;
  G-TOTALS-REDERIVED binds all 9 keys.
- **U9 (K3 M3):** STRUCTURAL made conditional — the 4
  unfailable waivers re-classed with binding predicates;
  the 2 mislabelled fixed.
- **U10 (K3 M4):** E-23 description-vs-code verified
  in-run; the 6 mismatches corrected.
- **U11 (K3 M5):** the L=3 control rung named in SCOPE and
  §control.
- **U12 (K3 M6):** the head DERIVED — typed literals
  removed; the comparator re-derives all 7 parts from
  primitives.
- **U13 (K3 M7–M10):** the fraction stamps; 18-OF-18 split
  and stamped; the band table and all 69 rows bound; the
  span whitelist scoped (the 0–24 blanket removed).
- **U14 (sweep):** the failing-path promotion fixed (nothing
  left promoted); G-PARTITION-COROLLARY made a real
  measurement; K2's 15 and K1's remaining MINORs as
  written.
- **U15 (the note):** v14/note-r4-scope-annotation.md
  written per ruling 3, K2's register row verbatim; R4's
  paper untouched.

## AFTER REPAIR

Worker re-runs plain ×2 byte, selftest, the full mutant
sweep, the K3 injection replays (all six survivors must
die), hostile argv, off-tree byte ×2; my battery; then
paper-28 → TERMINAL, and the prediction ledger in PLAN.md
updates (the orchestrator's edit, at terminal).
