# R1 — JOINT ADJUDICATION (operator #7, effectus #8, instrument #9)

**Adjudicator:** the coordinating agent.  **Inputs:** the three frozen
reviews (`f869eee604a7` / `27756b4751cb` / `d3685698da65`), the frozen
delivery (paper `2c75772098eb`, code `7c04500ec178`, output
`624ab5236ea1`, receipt `2130abe58b9e`), the frozen protocol
(`0647aeba4d9e`), the pin (`27c9f1144ffa`).  **Standing:** three
ACCEPT-WITH-FIXES; combined weight ~1,878 recomputations + ~64,000
sweep/algebraic checks + 220 process executions; **zero numerical
disagreements, zero false numbers** — and the largest meaning
correction of v14 so far.

## 1. Resolved grade

**ACCEPT-WITH-FIXES**, unanimous.  Every number stands.  What falls is
the verdict's reading: the stabilization is *algebraically forced by
isomorphic copying*, the locality gateway is *forced by the basepoint*,
and the atlas is *verdict-determining*.  The repair rewrites what the
unit claims, hardens the gates the injections defeated, and keeps
every measurement.

## 2. The panel's settled mathematics (cross-confirmed)

**A. The copy-forcing theorem** (operator ∥ R2-B, proved independently;
instrument corroborates 27b/37b/20b at b = 6,7,8,10,12).  The declared
rule yields Σ-stable isomorphic blocks (measured intertwiner β_k);
transports never cross blocks; hence N and N_coh are m disjoint copies
plus an isolated basepoint, and every ratio of component-additive,
point-vanishing quantities is m-independent.  **37/27 and 20/37 hold
at m = 1.  Nothing converges; the K=3 window carries one block census
(26/26 tail points = L₁ × m).**  Converse (R2-B′): the profile drifts
are O(1/B), entirely the basepoint's share.

**B. The paper's mechanism sentence is false as written** (operator F1
MAJOR).  "Disjoint addition ⟹ density invariance" has a measured
counterexample (MX_m, mixed block sizes: densities move
115/84 → 89/65 → 152/111).  The true hypothesis is *isomorphic*
copying; the widening control varied block size and never tested it.

**C. The atlas is verdict-determining** (effectus F1 MAJOR ∥ operator
F2 MAJOR).  Atlas-C (non-block-local, the unit's own drawing rules
verbatim) returns 0-of-5 stabilizing over the same arenas — the head
flips to NO-CONTINUUM-LIMIT.  ALT-D refuses to draw and both headline
invariants are UNDEFINED at the tail.  Three transport-convention
re-declarations leave the values fixed (favourable, unclaimed).  Per
the TOP P*-precedent: the atlas becomes a named verdict coordinate
with the sweep in-unit.

**D. The gateway was never a measurement** (effectus R2-C ∥ F3 MAJOR).
φ < 1 is forced (the basepoint lies in no cell's support); "first
member with φ < 1" degenerates to "first member"; A₁ *attains the
forced maximum* — it is the least local member (K₈ ⊔ {pt}).
Componentwise overlap completeness = 1 at every member and both probes
(R2-F4): each component is a complete clique — the inherited
non-complete-overlap requirement was met in letter, defeated in
purpose.  **The gateway is withdrawn: R2-GATEWAY = NONE-EARNED.**
The successor's recipe is proved (R2-A, 43,537 actions, 0
counterexamples): with regular-orbit drawing the overlap graph is
always a union of cliques — locality is earned only by declaring
cells whose orbits overlap *partially*.

**E. The five injection survivals** (instrument, each proven executed
at exit 0): the headline is silently rewritable (G16 recounts a
filtered list — the #219 disease); the two stabilized values swap
between names (G22 checks containment, not pairing); the gateway
segment is typed-acceptable; a corrupted trajectory cell reaches the
delivered receipt while gates pass (the rendered object is not the
gated object); appended qualifier text survives.  Plus #208 findings:
the eigenvalue-1 chain and degree check are permutation identities in
must-pass gates; two LCB-fix anchors are forced.

**F. Secondary corrections.**  The eigenvalue-1 chain is *strictly
weaker* than I2's wall and about a *different operator* (HA's F_p
readout vs this unit's chart-permutation over Q) — the walls-ride-along
claim must be restated at its true strength (operator F4 ∥ effectus
R2-F6).  A sixth stabilizer exists and was wrongly excluded
(b₁(N_coh)/|F| = 4/37; the exclusion rationale contradicted by the
unit's own b₁(N_coh) = 24, 28, 32).  Basepoint-deleted, 4 of 5
invariants are constant — the 2-of-5 split is a basepoint artefact.
The N_coh denominator is a variant of the pin's wording (37/27
per-incidence vs 37/21 per-drawn-pair; both constant; both printed
henceforth).  UNDEFINED-AT-A-MEMBER is dead code (partiality handled
by exclusion — make the path live or state the exclusion).  The R0
hash literals are typed against a docstring claiming otherwise.  Tail
restriction gives FAMILY-FUNCTORIAL and would name A₃ (operator F6) —
printed as the tail-restricted reading.  Credit recorded (R2-F11):
the b₂-partiality failure mode was pre-declared — a clean pass.

## 3. Adjudicator's own account (owned here and in the ledger)

(a) **The pin's gateway criterion was degenerate by design** — "first
member with φ < 1" cannot fail (R2-C); that is my pin-authoring error.
(b) **The pin's invariant wording was ambiguous** (per-pair vs
per-incidence) — two reviewers hit it independently.  (c) **My reports
narrated "locality everywhere" and "the substrate's first stabilized
constants"** before the panel ruled; both readings are dead (forced,
and copying).  This is the fifth instance of coordinator narrative
outrunning measurement (#241, #267, exclusive-Fano, the scale-thesis,
this) — the pattern's lesson is now engraved as a standing sentence in
my reporting: *between delivery and adjudication, headline readings
are candidate readings.*

## 4. Binding repair orders (R-R1-1 … R-R1-12)

1. **Verdict rewrite** (effectus's proposal adopted verbatim in
   structure): head `R1-STABILIZES-BY-DISJOINT-COPYING-AT-<...>`, with
   computed segments: values (both denominator conventions), MECHANISM
   (b₀ = blocks+1; per-block census constant; ratio-of-additives
   forced), INDEPENDENT-CONTENT = ONE-BLOCK-CENSUS(26-OF-26-FORCED),
   DIVERGENT (with the basepoint-share cause named), BASEPOINT-DELETED
   set 4-of-5 (+ the sixth stabilizer included; the grid printed),
   WINDOW (tail homogeneity named), FUNCTORIALITY (+ the
   tail-restricted FAMILY-FUNCTORIAL reading), **ATLAS as a named
   coordinate with the in-unit sweep** (block-local conventions ×3,
   Atlas-C flip, ALT-D UNDEFINED), **R2-GATEWAY = NONE-EARNED** with
   R2-C's forcing stated and R2-A's successor recipe recorded.  Every
   segment derived in-gate and flippable.
2. **The copy-forcing theorem enters the unit** (statement, in-code
   verification at m = 1..12, the m = 1 base case printed as the
   claim's true content) and **the false mechanism sentence is
   replaced** by the isomorphic-copying hypothesis with **MX_m as a
   shipped negative control** (mixed blocks must move the densities).
3. **The stabilization clauses reclassify** per #208: measured facts =
   block isomorphism (the intertwiner), the per-block census, the
   atlas sweep; forced consequences = the constancy; the verdict's
   STABILIZES-BY-COPYING wording carries exactly this split.
4. **Gate hardening for the five injections**: G16 recount from the
   unfiltered source through an independent path (#219); the verdict
   gate compares the full emitted string `==` a string rebuilt
   segment-by-segment from measured values (containment abolished);
   the gateway and DIVERGENT segments rebuilt and compared exactly;
   **the receipt and paper render from the gated object** (one object,
   single source of truth); five new mutants, one per injection class,
   each dying on its new gate.
5. **Forced clauses → disclosures** (#208): the eigenvalue-1 chain,
   the degree check, the two LCB-fix anchors — each demoted with its
   forcing stated; the spectral chain's true strength restated and
   its relation to I2's wall (different operator) disclosed; the
   walls-ride-along claim rescoped to what is actually confirmed.
6. **The sixth stabilizer** measured, included, and the score
   restated; the full 24-quantity grid (operator's) printed as the
   non-copied hunt's record.
7. **Both denominator conventions** printed with the pin-literal value
   named (37/21 / 20/73), the delivered convention disclosed as such.
8. **UNDEFINED path**: made live (a shipped input reaches it) or the
   exclusion stated in the verdict derivation; dead code removed.
9. **R0 hashes**: read from the pin file at run time (implement the
   docstring) or the docstring corrected; the #4 erratum values used
   for the two paper companions.
10. **Tail-restricted reading** printed (FAMILY-FUNCTORIAL; A₃) beside
    the full-family reading.
11. **Paper rewrite** (single-threaded): abstract/§9 carry the
    copying mechanism as the finding; R2-A/R2-B/R2-C stated as the
    unit's theorems with in-unit verification; the successor recipe
    (partial orbit overlap) recorded as the R2 pin's input; no
    correction narrative.
12. **Scoping note**: the continuum question itself is untouched — the
    unit's honest deliverable is "this family cannot answer it, for a
    proved reason, and here is what a family that could must do."

## 5. RUNBOOK engravings (this adjudication)

- **§14 addendum (containment is not equality):** a verdict gate
  compares the complete emitted string for equality against a string
  rebuilt segment-by-segment from the measured values; substring,
  prefix, or containment checks are not verdict gates (the INJ_B/C/E
  class).
- **§13 addendum (render from the gated object):** the object the
  gates check must be the object the receipt and paper render from —
  one object, one source of truth; a rendering path that bypasses the
  gated object is an ungated verdict (the INJ_D class).

## 6. What the manifold rung actually inherits

Not an arena — a criterion and a recipe.  Criterion: the first
*component* with a non-complete overlap graph (currently none exists
anywhere in the family).  Recipe (R2-A, proved): partial orbit
overlap must be *declared* — with regular-orbit drawing, cliques are
all there is.  The R2 pin therefore starts from atlas design, not
arena choice; the copying family remains available as R2's null
control (a family guaranteed to produce no locality).
