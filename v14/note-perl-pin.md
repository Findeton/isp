# PER-L (paper-28) — THE L-LADDER PERSISTENCE CENSUS — PIN (FROZEN)

**Frozen:** 2026-08-11, v14 ledger #196.  **Authority:** the
user's persistence directive (this ledger) amending THE
STRATEGIC PLAN (#193).  **Parents (all terminal or frozen):**
R4 (583cae7: the stage family, L-parameterised), R4b (6d32993:
the dispersions; the even-L theorem VMAX=diameter, verified
4–12 by its effectus; INTERIOR-RADII=1-at-L-4, 3-at-L-8 in its
register), R5 (987cd73: the 640-coin family and the
(order,support) PROFILE fingerprint at L=4), the paper-20
adjudication (159200e: **the registered Sidon prediction** —
the monomial-only theorem is a Sidon property of the offset
set, transports verbatim where offsets are Sidon, dies at any
declared fourth direction with 54 non-monomial unitaries), R2
(paper-02 terminal: the window-width locality effect, cited).
**Unit:** paper-28-perl; code v14/code/perl_exact.py (+ output
+ receipt).

## THE QUESTION

Do the forced results of the L=4 arc PERSIST along the
L-ladder (L = 6, 8)?  Per-invariant verdicts, each with exact
witnesses: `PERSISTS` / `BREAKS-AT-L=<n>-<witness>` /
`TRANSFORMS-<law>`.

1. **THE SIDON TEST (decisive — the programme's first test of
   a terminal unit's registered prediction):** for each arena
   (the L=6 and L=8 link/axis stencils, the generalisation
   rule DECLARED with its fiber), measure whether the offset
   set is Sidon (each nonzero difference realised exactly
   once) and whether the monomial-only conclusion follows or
   fails EXACTLY as the prediction says; the fourth-direction
   death (54 non-monomial unitaries appear) run as the
   control.  The prediction PASSES/FAILS per arena — a
   prediction-ledger entry either way.
2. **VMAX=DIAMETER EXTENDED:** the even-L theorem re-derived
   at L=6/8 in-unit (cite R4b's 4–12 verification, do not
   re-run it silently); the interior-radii resolution
   parameter measured (the register's 3-at-L-8 claim tested).
3. **THE GAUGE FINGERPRINT ALONG L:** the declared family
   generalised to L=6/8 (rule declared, fiber priced); the
   (order,support) profile computed; does the
   alternating-on-orbits character persist?  (GGS's first
   datum; no group-selection LAW is claimed here — that is
   GGS's question.)
4. **LOCALITY WINDOWS:** R2's window-width effect at L=6/8 at
   the declared reading — cite R2's machinery, recompute only
   the new rows.
5. **THE PERSISTENCE TABLE:** one row per invariant per L,
   rendered from the receipt and gated (E-22 multiset; every
   fraction E-24-stamped).

## WINDOWS AND HONESTY

Every generalisation rule (family extension, stencil choice)
DECLARED with its fiber priced; windows licensed by equality
gates, never silent; exhaustive where feasible, the window
declared in-string where not.  The four walls inherited; no
continuum claim — this unit measures PERSISTENCE AT DECLARED
FINITE RUNGS, nothing more.  NO transport numbers inherited
(the R4b scope stamp).  Between delivery and adjudication
every headline is a candidate reading.

## OUTCOMES (pre-registered)

`PERL-<table>` with the per-invariant verdicts;
`PERL-BLOCKED-AT-<object>` first-class; the Sidon
prediction's PASS/FAIL is reported in the head regardless of
the rest.

## STANDARDS

The full era per HANDOFF-PROMPT.md §4 PLUS E-22 (inline spans;
blocks by multiset; tables as claims), E-23 (falsifier
honesty), E-24 (measure-relativity of counts).  Exact
arithmetic; the derived+rendered head; the total seal;
failing runs write nothing; byte ×2.
