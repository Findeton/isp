# R4B — JOINT ADJUDICATION (effectus #116, operator #117, instrument #118)

**Inputs:** the three frozen reviews (effectus `f756f42469c7`,
operator `7a24064c835f`, instrument `29c75e73ede9`); the frozen
delivery (paper `5f8ec142319c`, code `53a10e87ec19`, output
`d33412225949`, receipt `00e7f6ea0f90`, commit 10c8d17); pin
`bcd12bbe6fd8`; protocol `b9cd2133d961`.  **Standing:** three
AWF; 541 seat recomputations (234+92+215) with ZERO false
computed numbers — every census row reproduced in two
independent field bases plus symbol-free routes; off-tree
git-less byte-identity ×3 (two seeds, foreign CWD).  Resolved
grade: **ACCEPT-WITH-FIXES**.

## 1. THE CORRECTED SETTLEMENT

1. **The convention stratified** (effectus MAJOR-1 ⇄ operator
   MAJOR-1, independent; one resolution): the delivered
   "fiber 9, one member selected" conflated two different nines.
   Corrected: the STENCIL is FORCED to {forward, backward} by
   the unit's own stated normalisation (monomial shift by o has
   velocity o — 384/384; currently prose, must be GATED); the
   drift=winding identity selects the TIE only (3→1) — winding
   is provably BLIND to the stencil; the residual DECLARED
   fiber is 2, disclosed, differing at 768/1856 cells.  **The
   identity itself is an ARENA ARTIFACT** (it fails exactly on
   the widened-alphabet generator, winding −1 vs drift −1/2,
   computed in Q(i,√3)) — it may serve as an instrument, never
   as "the velocity convention is not free."  §12 rewritten;
   every BOUND-segment quantity either carries an invariance
   gate over the residual fiber or is stamped
   READING-RELATIVE (effectus MAJOR-2's §15 violation closed).
2. **VMAX = diameter is a STRUCTURAL THEOREM, not an L=4
   fact** (effectus MAJOR-3): |v| ≤ L/2 = the max-norm diameter
   by construction, saturated by the antipodal shift at EVERY
   even L (verified L∈{4,6,8,10,12}); the one-step cone is
   vacuous at every even L and **L=8 cannot fix it**; §11's
   founding premise corrected; the genuine resolution
   parameter is the INTERIOR-RADIUS COUNT (1 at L=4 → 3 at
   L=8); the L=8 successor re-posed on it.  A scale-relative
   negative upgraded into a theorem.
3. **The cancellation stratified, licensed as arithmetic
   only** (operator MAJOR-2 + effectus K4): of the 42
   zero-net families, 24 are zero-drift under ALL THREE tie
   readings (18 averaged-only) and 30 zero-winding (12
   averaged-only) — the robustness split is REPORTED;
   "cancellation" is licensed as nonzero-summands-zero-sum and
   killed as a mechanism claim (nothing propagates in this
   arena); "interfering" corrected to its definition
   (non-monomial) and each of the 42 bound to the parent's
   diagonal-defect rows.
4. **The like-for-like separation adopted** (operator MAJOR-3,
   a strengthening): the delivered 58-vs-14 compared per-family
   with per-class; the corrected statement — the MULTISET of
   member dispersions separates 19 OF 19 circulant classes vs
   14 labels for 22 — replaces it.
5. **The first in-artifact LABEL defect, classified**:
   `REACH-BOUND-FALSE-AT=14-OF-58` misassigns meaning (an upper
   bound is falsified by the 8 overshooters, not the 14
   undershooters).  The numbers are correct; the label is
   wrong.  Register category opened: label defects in computed
   artifacts — count 1, programme-wide.  (The computed-NUMBER
   record remains zero.)
6. **Closed in the delivery's favor by the panel**: the
   Kronecker integrality step verified (all 928 symbols
   algebraic integers — the μ₈ result stands); the drift-table
   "only under it" clause verified true (the negative reading
   also gives 16|15, 18|10, 24|8 — now to be gated, not
   asserted).

## 2. THE SEAM (instrument) — and the era rule it buys

Ten of ten surviving injections live between the last gate and
the bytes on disk: the head is derived twice and gated — then
the payload is serialized 50 lines later, unguarded; the
terminal integrity gate re-derives FROM THE DISK BYTES and so
confirms any post-gate corruption.  Verbatim windows mostly
decorative (the parent's window_sha256_12 dropped); the
PUBLISHED rows unbound; #20 numeral-complete but
polarity-blind; --verify-paper a documented no-op.  All
bounded; one mechanism fixes the class: **seal at gate time,
compare the disk against the seal.**

## 3. Binding repair orders (R-R4B-1 … R-R4B-9)

1. **The head recomposed** per §1: the stratified convention
   segment (STENCIL=FORCED-BY-NORMALISATION(gated);
   TIE=SELECTED-BY-DRIFT-WINDING(arena-instrument);
   RESIDUAL-FIBER=2); the BOUND segment invariance-gated or
   READING-RELATIVE-stamped; the structural-theorem form of
   VMAX=diameter with the interior-radius resolution row; the
   cancellation robustness split (24/18, 30/12); the corrected
   REACH label (the 8 overshooters); the 19/19 multiset
   separation.
2. **THE GATE-TO-DISK SEAL** (the one-mechanism fix,
   instrument MAJOR-1+3): the verdict payload and every
   published row digested AT GATE TIME; write happens from the
   sealed object; the terminal integrity gate compares the
   DISK BYTES against the GATE-TIME SEAL (never a
   re-derivation from disk); the ten injections become dead
   mutants.
3. **The published rows bound** (#87 extended to emission):
   every published census/control row carries its own gate
   against the sealed object; control rows cannot ship under
   census labels.
4. **Verbatim windows made meaningful** (#62): the parent's
   window_sha256_12 restored; the 12 decorative windows
   consumer-gated or reclassified; the truncation class dies.
5. **#20 given polarity**: the prose instrument extended so
   claim-direction inversions are caught (sentence-level
   anchors on polarity-bearing claims, or polarity rendered
   from the receipt); the three surviving prose inversions
   become dead mutants.
6. **--verify-paper made real** (or removed): it must run the
   #20 instrument and fail on drift; a nonexistent path errors
   (exit 2).
7. **The forced-stencil normalisation GATED** (384/384); the
   drift-table negative-reading clause gated; the 67/67-vs-66
   counting defect fixed; the head law's third outcome
   exercised by a mutant.
8. **Prose corrections**: §12 rewritten; §11's premise
   corrected; "interfering"→non-monomial binding; both MINOR
   sheets applied as written.
9. **Paper rewrite single-threaded**; the successor register
   adjudication-ruled: R4c-multi inherits NO transport number
   (single-occupation uniform-average scope stamped);
   R5-gauge receives NOT-BLOCH-DIAGONAL as a THEOREM
   (index-two stabiliser); the R4b follow-on re-posed on
   interior radii (not L=8-fixes-the-cone); the
   convention-selection's domain of validity registered as the
   unit's most consequential open.

## 4. RUNBOOK engraving (this adjudication)

- **§14 addendum (the gate-to-disk seal):** a verdict or
  published row is sealed (digested) at the moment its gate
  passes, artifacts are written from the sealed object, and
  the terminal integrity check compares disk bytes against the
  gate-time seal.  A re-derivation from disk is not an
  integrity check — it confirms corruption.  Demonstrations:
  R4b INJ01 ("#234 satisfied at gate time, vacated at delivery
  time") and the weld-2 INJ05/10/11 class.
