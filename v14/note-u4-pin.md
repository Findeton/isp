# U4 — RENEWAL-ONLY CRYSTALS (paper-14) — PIN (FROZEN)

**Frozen:** 2026-08-10, v14 ledger #105.  **Authority:** the
weld-2 adjudication §4 (`7213d26ea4d4`, #102) ruling U4 the next
Route-A unit; the successor scout of record
(note-routeA-successor-scout.md, #101); the founding spec.
**Unit:** paper-14-u4-renewal-crystals; code
v14/code/u4_crystals_exact.py (+ output + receipt).

## THE QUESTION (v11 paper 0 §7, verbatim; `37a428321f46`)

"U4 — SPARSE RECORDS ON THE CRYSTALS.  The conflict crystals
rebuilt with renewal-only records: geometry should be invariant
(it is kinematic — paper 0 §10's third falsifier if not); the
bridges between the renewal sublattice — itself periodic: *the
division events of a crystal form a crystal* — are probed for
indivisible structure."  Never run; registered-unrun in the
committed gate G-U4-REGISTERED (w2 `290149118b9d` L2307–16).

## R1 — THE ARENA, DECLARED (§15)

The four ARBITRATION crystals from the committed constructors —
DOUBLE-GRID(3,2), DOUBLE-GRID(3,3), CONFLICT-GRID(3,2),
CONFLICT-GRID(3,4) — rebuilt from the v14 self-contained routes
(w2 `290149118b9d` SEC 6) cross-checked against the v10
originals (d60 `684cdb76552b`, d66 `3d0516ae106e` — NOTE: use
the scout-verified sha `3d0516ab106e`; abort on mismatch and
report — d67 `e80edf851d93`, d63 `89e170f40579`).  All records
FORCED (maxhits=1, no refusal) — gate that.  **The delivery
crystal D60-GRID(3,12) is the DECLARED COUNTEREXAMPLE CONTROL**:
every periodicity claim must return its other value there
(1 division event, stabilizer order 1 — the scout's preliminary;
HA §14 requirement 3 in force).

## R2 — THE RENEWAL MARKING (source-forced, not chosen)

Division events = renewal events (the POSIT, v11 paper 0 §4
`37a428321f46`); renewal = class-0-carrying-an-arb,
SOURCE-FORCED (paper-09 §3 `006f96aaa2ff`; the r6bp verdict
tag; the weld-2 ingredient row: 1536 instances tag-forced).
The unit re-derives the marking on each crystal record and
gates it against the source rows.

## R3 — DECLARED DATA (choice-inventoried, fibers printed)

- **The SITE READING** — BOTH run as declared arms: (a)
  initiator (`op[1]`); (b) register footprint (`regs_of`).
  The scout's preliminary: stabilizers AGREE, supports differ
  (6/9 vs 9/9).  Agreement/divergence is a measured output, not
  an assumption.
- **The RENEWAL-ONLY OPERATIONALIZATION** — nowhere pinned;
  DECLARED here with fiber 3: (a) FILTER the forced record to
  its arbitration events (primary arm — no new grammar law);
  (b) re-run the Builder on a restricted candidate stream
  (second arm, run if (a) completes; its restriction is a
  declared sub-grammar per the d74 shapes `bb852161aced`
  L1406–18); (c) quotient by non-arbitration events (registered,
  not run — say so).  Every choice classified declared/forced/
  free at the RSQ standard.

## R4 — THE CLAIMS, EACH TWO-WAY

1. **SUBLATTICE PERIODICITY (the headline claim):** the
   division-event field's translation stabilizer on Z₃²,
   computed per crystal per site-reading, exactly.  The scout's
   PRELIMINARY (to be made corpus-grade or refuted): stabilizer
   = ⟨(1,1)⟩ (order 3) on all four arbitration crystals, order
   1 on the delivery control.  Mutants: a planted aperiodic
   division; a planted full-period field on the control.
2. **GEOMETRY INVARIANCE under the renewal-only rebuild** (arm
   (b)): the crystal's geometry rows (forcing, homogeneity,
   chart width where posable from the committed specs) at
   renewal-only vs full — INVARIANT is the kinematic
   prediction; **VARIES-<witness> is paper 0 §10's third
   falsifier and must be emittable** (a mutant demonstrates the
   VARIES path).
3. **THE BRIDGES, scoped:** the induced counts/adjacency of the
   renewal sublattice vs the full record — what structure the
   bridges carry, at declared scope only (no indivisibility
   claim beyond what the arena can measure; candidate readings
   named).

## R5 — THE WALLS (engraved in this pin; violations are
construction errors, not findings)

- **L-1:** only ORDER-LEVEL COVARIANCE may be tested, a fourth
  form whose admissibility must be ARGUED in the paper BEFORE
  any test; the retracted "weaker form" sentence (retracted
  2026-07-28) is BANNED from this unit.
- **BHS:** no sprinkling-grade Lorentz-invariance test (finite
  valency makes it provably unavailable; testing it
  manufactures a false negative — catalog §1.6 "Engrave in the
  U4 pin").
- **KR:** any dimension reading carries a height control
  ("a dimension reading without a height control is
  worthless" — catalog §1.7).
- **THE DIAGONAL:** q₁₂ ≡ 0 is INHERITED (the co-division graph
  is the rook's graph; diagonal pairs share neither row nor
  column).  The diagonal question is NOT answered here; the
  paper says so.

## R6 — STANDARDS AT CONSTRUCTION

Full era: the #82 CLI contract; #87 per-object gates
(per-crystal × per-reading × per-arm); #91 pinned-sha reads
with products gated + off-tree AND git-less byte-reproduction
TESTED; §15 declared-arena; #24/#20/#34/#62; **the head
DERIVED inside a complete-string equality gate by an
independent reconstruction (no shared code/inputs/typed
literals)**; failing runs write nothing; a final integrity gate
re-reads written artifacts; exact arithmetic only.

## OUTCOMES (pre-registered)

`U4-THE-DIVISION-EVENTS-FORM-A-CRYSTAL-<stabilizers-per-
crystal-per-reading>` /
`U4-SUBLATTICE-NOT-A-CRYSTAL-<witness>` /
`U4-BLOCKED-AT-<object>`; a separate GEOMETRY segment
(INVARIANT / VARIES-<witness>); the dead list cited never
re-run (C1–C5 + blankets, per the weld-2 pin R4/scout S6).
Between delivery and adjudication every headline reading is a
candidate reading.
