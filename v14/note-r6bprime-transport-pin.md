# v14 R6b′ — THE RENEWAL-GRAIN TRANSPORT (pin + R0 supplement)

**Frozen:** 2026-08-09, v14 ledger #43, under the user's standing order
(#42).  **Paper number:** `paper-09` — deliverables
`v14/paper-09-renewal-transport.md`, `v14/code/r6bp_transport_exact.py`
+ `_output.txt` + `_receipt.json`.  **Discipline:** RUNBOOK
§13/§14/§15, every addendum, all seven 2026-08-09 engravings, each
compliance gate shipping its injection-falsifier; exact arithmetic.

## 1. The R0 supplement — six deep-corpus rows, hash-pinned

Per the charter's history mechanism (anything of v10–v12 is usable
only via a unit pin with hash + reason).  Every citation carries the
row's PEDIGREE verbatim; the unit gates every read (path-value +
verbatim-context anchors per the #34 standard).

| row | artifacts (sha256-12) | pedigree | supplies |
|---|---|---|---|
| S1 THE TRANSITION MATRIX | `v10/relativistic-isp-v10-paper31-four-decisions-at-the-joints.md` `7ac66f3fe74d` + `v10/code/d43b_state_chain_exact.py` `5f91f0190b4c` | TERMINAL (v10 #349) | the exact 6×6 transfer; λ=2; the completed stochastic chain q′; the named renewal state (class 0); root = renewal |
| S2 THE POSITIONAL CENSUS | `v11/note-u1b-renewal-class-sweep.md` `47f001fad828` + `v11/code/u1b_output.txt` `a955b8484465` + `v11/code/u1b_renewal_class_sweep_exact.py` `5adb205a33d6` | TERMINAL (v11 #20–#21) | the exhaustive intervening-pattern census inside inter-renewal intervals (lengths 3, 4) with exact leaf counts; interval lengths are ENSEMBLE DATA (declared) — carried as such |
| S3 THE TYPE DECLARATION | `v11/relativistic-isp-v11-paper0-the-indivisible-record-law.md` `37a428321f46` | founding paper; v11 FROZEN | division-event ≡ renewal (with its engraved scope tags); "the bridges" = the interval; Γ(cut′←cut) declared TO BE CONSTRUCTED |
| S4 THE RENEWAL THEOREM | `v10/relativistic-isp-v10-paper30-the-generated-record-and-its-completion.md` `e431a7c48f76` | TERMINAL (v10 #326; ten hostile rounds; its header's "paper-level review open" note carried) | every pair arbitration is a renewal to the root [THEOREM at its scope]; the elementary-click refinement licensing event-level positions |
| S5 THE DECLARED POSITIONAL LAW | `v10/note-d33-history-law-phase.md` `bad952ee5849` + `v10/relativistic-isp-v10-paper21-local-generators-do-not-imply-local-memory.md` `038a424a8843` + `v10/code/d34b_exponential_clocks_exact.py` `dee1cc968268` | D34b TERMINAL delta (3 hostile streams) | the continuous positional layer (exponential clocks, placement factors, next-winner law) **with its own verbatim disclaimers: the waiting law and rates are CHOSEN, NOT DERIVED** — pinned as positive control + over-claim bound |
| S6 THE EXTREMAL STANDARD | `v10/note-d12-selection-principle-audit.md` `2670a2ea7644` (status: completed pre-hostile-review — carried) + `v10/relativistic-isp-v10-paper16-the-rulebook-is-the-history-law.md` `dbf027b2fbc9` | audit + finite nonselection theorem | the frozen bar any extremal selector must clear (record-intrinsic or dead) |

**Named exclusions (binding):** `v11/note-u1c-depth15-two-sided.md`
(GREEN-UNREVIEWED, NOT CITABLE per STATUS — may appear only as a
registered lead with status printed); `v10/THE-THEORY-SO-FAR.md`
(index only, never primary); v12's Γ objects (arena-free — no
interval/position/count content); `v10/note-d70-horizon-limit-result.md`
(round-1-repaired, NOT terminal — excluded from this unit; the
transport-scope arm is a possible follow-up pin).

## 2. The question (the scout's reframing, binding)

The stratum named missing four times EXISTS at renewal grain.  What
does not exist is its transport: nothing derives positions inside a
SPATIAL link interval n_ℓ(x).  **R6b′ therefore asks a transport
question between two arenas:** can a MOTIVATED identification carry
the renewal-grain positional structure (S1's chain; S2's census) to
HA's interval arena (I7, the baseline row) — and if so, does the
derived interval-positional kernel collapse the R6a split fiber, as
a forced value or a forced distribution?

## 3. Registered measurements

1. **The identification census (the core, R6a discipline).**
   Enumerate the identification candidates between "inter-renewal
   interval" (temporal event-chain; S2/S3's object) and "record
   interval n_ℓ(x)" (spatial link; I7's object) that are expressible
   from the pinned rows — at minimum: count-matching (n_ℓ events ↔ a
   length-n_ℓ inter-renewal leg), the bridges reading (S3's
   declaration), and any construction the rows themselves state.  Run
   the CHOICE INVENTORY on each identification (forced /
   stabilizer-fixed / genuinely-free with exact fibers).  An
   identification with free choices is UNMOTIVATED and says so (the
   RSQ standard); zero motivated identifications is a first-class
   outcome.
2. **The derived kernel.**  At each identification (motivated or not
   — unmotivated ones measured and labelled): derive the
   interval-positional law the pinned structures induce — from S1,
   the first-return/inter-renewal law of the completed chain q′
   (verify the scout's derivation in-unit: the renewal state's
   return probability, the closed class, the defectiveness); from
   S2, the intervening-pattern profiles at lengths 3 and 4 (anchor
   the counts 1024/512/512/1024/512 and the leg-2 profile).  Then
   the decisive test: does the derived law collapse the R6a split
   fiber — to a unique value, or to a UNIQUE distribution (the
   invariant-measure question CR-B posed, now with a pinned law to
   answer it)?  Anchor the R6a fiber values (receipt `022c3f488a93`)
   and CR-B's simplex dimensions (receipt `5ebeec141303`), both at
   DELIVERED/TERMINAL status as applicable.
3. **THE DEFECTIVE-RENEWAL FINDING (first-class either way).**
   In-unit, from S1's pinned chain: the renewal state's recurrence
   arithmetic (the scout's flagged-as-his recomputation, now made an
   artifact claim or refuted): P(return to 0 | at 0), the closed
   class {2,4,5}, the a.s.-termination statement at delivery-free
   scope, with paper 31's own transport-scope caveat quoted.  If the
   delivery-free renewal process terminates, every headline in this
   unit carries that scope qualifier.
4. **The R6a audit re-run at the enriched type.**  With the six rows
   available: re-class the four R6a freedoms (does THE-SPLIT become
   class-(i)/(ii) in value or in distribution? do the transverse
   links or front values gain forcings?).  Additivity and
   metric-restriction re-verified unchanged.
5. **The extremal lead vs the S6 standard.**  Is max-det
   record-intrinsic in D12's sense (computed from the record alone,
   no supplied base measure or constraints)?  Test the selector
   against S6's countermodel discipline; the outcome is a measured
   segment (CLEARS-THE-BAR / DIES-AT-THE-BAR with the countermodel).
6. **The cover lead: recorded DISSOLVED** (the deep arenas are
   non-periodic — one gate quoting the non-periodicity declarations;
   no hunt).

## 4. Verdicts (first-class, composable, computed in-gate,
independent comparator, every segment flippable)

`R6BP-KERNEL-DERIVED-<identification; law; fiber-collapse: VALUE |
DISTRIBUTION; scope>` / `R6BP-TRANSPORT-UNMOTIVATED-<the inventory>`
/ `R6BP-KERNEL-DEFECTIVE-<the termination characterization + scope>`
/ `R6BP-BLOCKED-AT-<named fact>`.  Segments for the extremal-bar
outcome and the dissolved cover lead.  Scope qualifiers mandatory:
delivery-free vs transport scope (S1/S4's own tags); ensemble-data
status of S2's interval lengths; S5's chosen-not-derived disclaimer
wherever the continuous layer is touched.

## 5. Controls and falsifiers (minimum set)

The R1-copy and R6a-external moves as audit negative controls (the
inventory must fail them); a deliberately count-mismatched
identification as the identification census's negative control; S1's
chain re-derived by a second route (the 36-state quotient claim of
paper 32 is NOT pinned — do not reach for it; two routes within S1's
own artifacts); mutants per the era minimum + the five verdict
classes + path/verbatim-context drifts per row + fiber-anchor drift
(R6a/CR-B receipts) + defectiveness-arithmetic corruption; the
never-falsified census with verified waivers (#34 standard) from
delivery one.

## 6. Scope honesty

This unit transports and derives; it does not construct Γ(cut′←cut)
(S3 says TO BE CONSTRUCTED — building it is a future unit), does not
run the scaling census (R6b-proper, gated on this unit's outcome),
does not touch the transport-scope renewal arm (d70 excluded), and
claims nothing about the continuum beyond what the fiber-collapse
measurement earns.  KERNEL-DEFECTIVE and TRANSPORT-UNMOTIVATED are
exactly as terminal-grade as KERNEL-DERIVED.
