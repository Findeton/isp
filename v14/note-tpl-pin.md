# TPL — THE #267 TEMPLATE SWEEP (CHARTERED AT #371, PER #362; FOUR PANELS CONVERGENT + ONE REALIZED HARM)
**Not a physics unit.** Three deliverables, no seal is reopened:
1. **THE HARDENED TEMPLATE** (v14/TEMPLATE.md + v14/code/era_template.py): reference implementations + spec for the nine recurring disease families — (a) seal integrity per #119-as-amended (gate-time seals verified at promotion; adds, edits, and post-close edits all die; totality recomputed at promotion); (b) transcript bound to the ledger by content (forged rows/verdicts die; counts reconciled); (c) walls SEMANTIC (voice-normalised patterns + positive legs + self-sealing; no literal-string traps; no self-licensing token sets); (d) verbatim anchors CONSUMED (subscripted by their named gates, gated); (e) claims by EQUALITY, two-way, table-sighted (headers + cells + row-multisets; exact occurrence counts); (f) referent binding (sentence-level, cross-universe plants die, aggregates excluded); (g) NO TYPED COUNTS (every numeral in gate statements, templates, and testimony leaves interpolated from live registries — PER-R's six templates and EPR's two testimony leaves are the registered exemplars); (h) falsifiers that poison MEASUREMENTS (no sentinel/verdict-variable poisoning; descriptions code-matched; recipes must prove they changed the object); (i) read sets recorded at the I/O accessor, order-insensitive, with vacuous-pass modes killed.
2. **THE EXPOSURE CENSUS** (v14/tpl_census.md + code/receipt): for each sealed unit's instrument (the ~16 v14 units), measure read-only in mirrors which families it carries, using the panels' own published injection recipes; publish the matrix with zero edits to sealed objects; registered residuals listed per unit.
3. **RUNBOOK ADDENDA** drafted (not applied — the orchestrator engraves): one addendum per family, each citing its convergent panel evidence.
Standards: exact; no seal reopened; git read-only; the census instrument itself built ON the hardened template (self-hosting = the acceptance demonstration).
