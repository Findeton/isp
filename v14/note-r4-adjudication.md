# R4 — JOINT ADJUDICATION (operator, effectus, instrument — #77/#78/#86)

**Inputs:** the three frozen reviews (operator, effectus,
instrument `7fa287a04367`); the frozen delivery (paper
`f3e8cc1618f8`, code `b079bb3b8d55`, output `58ec08893526`,
receipt `3214f4da3af2`, commit 264cb54); pin `1582cea5df51`;
protocol `a3a39813e5b5`.  **Standing:** three ACCEPT-WITH-FIXES;
operator 136/136 recomputations + byte-identical scratch replay;
instrument 178 executions + 329 recomputations zero
disagreements + byte-identity ×3 (one from a different repo
root) + 82/82 mutants externally confirmed; effectus ledger
clean.  **Zero false computed numbers anywhere** — every census
cell, both tables, all identities.  Resolved grade:
**ACCEPT-WITH-FIXES**, and the principal repair STRENGTHENS the
unit (both content reviews state this independently).

## 1. THE RELOCATED HEADLINE (the decisive convergence)

Operator MAJOR-1 and effectus F1, independent routes, one fact:
**the L=4 uniqueness is connective-relative** — under the
von Neumann (sum-norm) connective the locality thresholds are 2
at d=2,3 (the delivery's own receipt prints
`von_neumann_threshold_d2 = 2`), and the admissible set is
[2,4], so uniqueness FAILS there.  The paper's line-177 sentence
("measured, both connectives, three dimensions") and the §2
caption are **false — the programme's 5th false paper claim
(prose species, receipt-contradicted, load-bearing for the
title)**.  The resolution is (B), fixed by the unit's own code:
`G-LINKS-IN-BALL` checks the anchored links against a hard-coded
max-norm ball, and the anchored diagonal link (1,1) has sum-norm
2 — **the connective is FORCED by I7's declared link set**.  The
§3.4 row "GENUINELY-FREE | 2" is a false classification; the
corrected row is FORCED (anchored), fiber 1.  Consequence,
carried verbatim into the paper: **the unique scale is a theorem
about I7's declared link set, not a law of the substrate.**  The
SCALE segment gains `CONNECTIVE=MAX-NORM(FORCED-BY-ANCHORED-
LINK-(1,1))`; the false biconditional becomes the ONLY-IF form
with `PRESENT-AT-L-IN-{2,4}` derived in-gate (effectus F2).
Note the resonance with #83: this is now the SECOND result
resting on the declared lattice — the weld-2 census (paper-13)
carries exactly that weight.

## 2. What GROWS

The theorem is adopted at its strengthened scope: the
**Moore-ball collapse** (L≥5 ⟹ every radius-1-ball unitary
monomial, ANY field) and the **alphabet-independence argument**
(effectus §K2) close the two declared scope holes.  The
abelian-stratum content is promoted into §7: **the
verdict-bearing stratum is abelian — 0 of 3,364 commutators
nonzero; every Wilson loop is the identity by theorem; the only
non-commuting generators are the 4 brickwork generators the
mandatory realization gate excludes** (effectus F6/F10;
principled bite 36/150).  This is THE R5 PIN DATUM: a gauge rung
on R4's FULL stratum is pre-committed to flat abelian holonomy
and could not be falsified — R5 builds on the excluded
non-abelian stratum, opening with this fact.

## 3. The instrument findings (34 surviving injections)

The head sits OUTSIDE the complete-string equality gate (the
reconstruction copies it; a full 77/77-gate run emitting
R4-BLOCKED-AT-NOTHING exits 0); the realization gate verifies no
individual classification (a ≥3-distinct-names predicate); the
Y1 lesson binds 12/588 rows; 18/27 verdict values lack flip
probes with D/L/FIELD typed in both paths (the #82 twin,
independently re-derived by the reviewer with colleague reviews
unopened); #208 laundering proven by clause removal
(MUT-PRECHECK-NAMES dies on a mutant-only flag); verify-paper
renders 13 strings for a 336-numeral paper (operator MAJOR-5's
independent twin — and the exact surface the false connective
sentence entered through); **the CLI disease, FOURTH instance**
(--selftest silently ignored AND the plain run it triggers
writes both artifacts); 616-OF-1024 resolves to 25 distinct
non-local comparisons (value-multiset matching, not
gauge-class); G-MARKOV-ZERO keys on a count, not the object.
Best on-target mutant discipline of the campaign; both
never-falsified waivers verified genuine; 34 anchor
perturbations all fatal.

## 4. Binding repair orders (R-R4-1 … R-R4-12)

1. **The headline relocated** (§1 verbatim): retitle per effectus
   ("One Lattice Size Admits a Local … Family", with
   declared-indivisible folded per order 10); the false sentence
   + §2 caption corrected; the vN threshold table printed; the
   connective row FORCED (anchored, fiber 1, reason printed); the
   SCALE segment carries the connective; a routing mutant (sum-
   norm rows into the admissibility loop) dying at the named
   forcing gate.
2. **The biconditional corrected** to ONLY-IF-L<=4 with
   PRESENT-AT-L-IN-{2,4}, derived in-gate from the ord census.
3. **The two strengthenings adopted** (Moore-ball collapse
   any-field; alphabet-independence), proofs carried, gated where
   computable.
4. **The head DERIVED, not copied**: the reconstruction rebuilds
   the complete verdict head from its own gated inputs, sharing
   no code, inputs, or typed literals (#82); the head flips
   become dead mutants.
5. **The real CLI** at the #82 contract (fourth recurrence):
   argv parsing; unknown flags exit 2; --selftest
   corrupt-one-anchor/exit-1/WRITES-NOTHING; --mutant NAME with
   unknown names exit 2 (never "SURVIVED").
6. **Gates bind objects**: the realization gate verifies every
   individual generator classification against its computed
   invariant; G-MARKOV-ZERO keyed on the object; the Y1 binding
   extended to the full census (zeroing any row dies).
7. **Flip probes for all 27 verdict values**; D/L/FIELD computed
   once, consumed via the receipt; L computed upstream of every
   consumer.
8. **The #208 laundering removed**: MUT-PRECHECK-NAMES dies by
   the real mechanism; no mutant-only flags anywhere (the
   clause-removal probe becomes a standing self-check).
9. **The compliance sweep gated** (operator MAJOR-5 + instrument
   F6): every status a computed predicate with a declared
   injection-falsifier; paper_claims covers every numeric token
   (66 distinct/321 occurrences) or the residue is marked
   derived-in-text at its derivation site; --verify-paper runs
   inside the plain run as a gate.
10. **The scope block**: abelian-stratum §7 content; STATE /
    translations-trivial / splitting identity re-registered
    FORCED with #208 disclosures; "indivisible" →
    declared-indivisible in SCOPE (or d_div measured and
    printed); light-cone vacuity + SEPARATIONS=16 /
    MAX-DEFECT-RADIUS=2 ceilings + the
    propagator-cannot-be-resolved observation carried;
    616-OF-1024 restated as 25 distinct comparisons with
    multiplicities and the matching species named; class-label
    collisions printed (22 classes, 14 labels; "conjugacy type"
    terminology); the state-motion probe count printed; both
    MINOR sheets + effectus MODERATE + N1 naming applied as
    written; the instrument's two prose fixes (one verbatim
    replacement).
11. **Paper rewrite single-threaded**; THE SUCCESSOR REGISTER as
    the closing section: R4b-momentum (the unread dispersions;
    the propagator observation), R5-gauge (§2's pin datum
    VERBATIM as the pin's opening fact; the excluded non-abelian
    stratum; effectus G1–G7), the connective-forced fact carried
    into both successor pins.
12. **The false-claim register**: the programme count rises to 5
    (all prose; zero in computed artifacts) — stated in the
    paper's deviations section per the single-threaded rule
    (present-tense scope statement, no correction narrative).

## 5. RUNBOOK engraving (this adjudication)

- **§14 addendum (gates bind objects, not cardinalities):** a
  gate discharging a per-object obligation must evaluate each
  object against its own computed invariant; a predicate on an
  aggregate (a count, a distinct-name tally, a sampled subset
  where the property is not forced) is vacuous at the per-object
  level.  Three demonstrations in one unit: the realization
  gate's ≥3-names predicate; G-MARKOV-ZERO keying on a count;
  the Y1 lesson binding 12 of 588 rows.
- Under the existing #82 CLI engraving: the FOURTH named
  recurrence logged (Γ-prep, R6b′-partial, Γ-main, R4).
