# v14 Γ-MAIN — THE GEOMETRY-UPDATE LAW (pin)

**Frozen:** 2026-08-09, v14 ledger #64, per the standing order (#56):
both preconditions met — Γ-prep delivered (committed at sha
`0f5d57eef77f`, ledger #63) and the R6b′ panel adjudicated (#62, the
input register).  **Paper number:** `paper-12` — deliverables
`v14/paper-12-gamma-main.md`, `v14/code/gmain_exact.py` +
`_output.txt` + `_receipt.json`.  **Discipline:** RUNBOOK complete —
ALL TEN 2026-08-09 engravings, including the #62 pair (the corrected
verbatim-anchor spec; provenance by declared COMMIT SHA — every
cross-unit read in this unit uses the shas declared here).

## 1. Sources (by declared commit sha where v14; hashes for frozen trees)

- **Γ-PREP (the foundation):** the four artifacts AT COMMIT
  `0f5d57eef77f` (paper `09482eb080cc`, code `9a4f0529b840`, output
  `097c08a0229d`, receipt `dd86ad1a80d7`; status
  DELIVERED-COMMITTED-VERIFICATION-IN-FLIGHT, carried).  Supplies:
  the grammar facts; the kernels k_r proper; **the B2 atoms (δ*=1 on
  the R-SIG holdings-profile blocks — the renewal-block structure)**;
  the escape at both grains; the monotone-holdings ladder.
- **THE R6b′ ADJUDICATION REGISTER:** `v14/note-r6bp-adjudication.md`
  at commit `d042ef1` — the four-head structure; the kernel as Γ's
  DELIVERY-FREE SHADOW; **the two position-law targets**; the
  holonomy-gate requirement; the don't-inherit list.  (The R6b′
  artifacts themselves are MID-REPAIR and are NOT anchored; the
  adjudication note is the frozen carrier of the targets.)
- **THE QUOTIENT + CURVATURE (the carrier):**
  `v10/note-d74-transport-holonomy-result.md` `0180e21c7127` +
  `v10/code/d74_transport_holonomy_exact.py` `bb852161aced` +
  `v10/data/d74_transport_holonomy_exact.out` `b5a9d50f9573`
  (TERMINAL v10 #495): the six abstractions; MENU (113 classes,
  menu-descent 113/113); REC (2,477, flat); **the measured holonomy
  ⟨2,3⟩ + the flatness predicate J**.
- **THE v11 BOARDS:** `v11/code/u1_indivisibility_census_exact.py`
  `63a0808fafbe` + `v11/note-u1-indivisibility-census.md`
  `13a2430927ad` (gamma_of + its three engraved caveats — never-
  square supports; the identity-padding CONVENTION; the 44 squares);
  `v11/note-u1b-renewal-class-sweep.md` `47f001fad828` +
  `v11/code/u1b_output.txt` `a955b8484465` (S2: the positional
  census — the TARGETS' source); `v11/note-u3-unistochasticity-
  screen.md` `ad48306d41e5` + `v11/code/u3_unistochasticity_
  screen_exact.py` `ddc08bc2f83b` (the screen; its input contract —
  square, doubly-stochastic, the n=3 discriminant, EXCLUDED-BY-CAP
  honesty); `v11/note-u2-three-address-weld.md` `26d6690a7b23`
  (W-CROSS — the constraint on any curvature⇒quantum claim);
  `v11/relativistic-isp-v11-paper0-the-indivisible-record-law.md`
  `37a428321f46` (the type declaration; the construction route).
- **THE WELD-BATTERY ANCHORS (v14, by receipt):** CR-B
  `5ebeec141303` (the missing-kernel naming + the simplex data);
  CR-A `5f68bac811bd` (the mover census: 1,232 admissible, 0
  forced); R4 `3214f4da3af2` (the defect on the stage; the
  realization-gate design).  Pedigrees carried.
- **Named exclusions:** u1c (NOT CITABLE); d56 (via d57's pin only);
  the indexes; all mutable state.

## 2. The construction (the corpus's own named route)

**Γ := the transport process read on D74's committed MENU quotient
(113 classes at the (A,B) d≤4 cap), as an exact rational
column-stochastic family Γ(cut′←cut) between declared depth cuts**,
built from the pinned kernels k_r (the horizon normalization is the
enabling step — U1's G1 fact).  The REC quotient (2,477 classes,
measured flat) is the mandatory NEGATIVE control (trivial holonomy
required); renewal cuts are the POSITIVE control only (U1b's
column-constancy wall — any indivisibility claim there is dead at
citable scope and the unit says so).  The B2 atom structure from
Γ-prep is available as the block decomposition.

## 3. The registered test battery (each a gate, each flippable)

1. **THE POSITION-LAW TARGETS (pre-registered, the R6b′ register):**
   the constructed Γ must reproduce the transport positional laws —
   **(3/7, 1/7, 3/7) at leg 1 and (4/9, 1/9, 4/9) at leg 2** — and
   exhibit the F8 mechanism (the middle-slot delivery absence) as a
   property of Γ, not an import.
2. **THE HOLONOMY GATE (pre-registered):** the holonomy of the
   constructed family compared against d74's measured **⟨2,3⟩** —
   agreement or the measured deviation, exactly; REC flat.  Without
   this gate "Γ-main lands" is unfalsifiable (the effectus ruling).
3. **THE U3 SCREEN** on Γ (the owed, never-run test): shape;
   double-stochasticity with exact deficits; the n=3 discriminant
   where applicable; EXCLUDED-BY-CAP honesty at n≥4 unless a unitary
   is supplied.  The screen verdict is a segment.
4. **THE INTERPOLANT/[B3] TEST** with the never-square-supports
   caveat and the identity-padding CONVENTION declared as a
   convention (U1's discipline verbatim).
5. **CR-B's KERNEL QUESTION:** does Γ induce the interval-positional
   law (the law for where n events fall), and what does it do to the
   measured simplex (anchored values)?
6. **CR-A's MOVER QUESTION:** does Γ force an advancing mover
   (against the anchored 1,232-admissible-0-forced census), and what
   is its commutation status with H_a[N]?
7. **THE W-CROSS CONSTRAINT:** any curvature⇒quantum reading tested
   against U2's measured crossings (the three loci do not coincide
   at cut grain); the record-grain form flagged as the open one.
8. **THE MOTIVATION INVENTORY** on Γ's OWN construction (the RSQ
   standard; including an I-READOUT-class item per the effectus;
   the cut choice, the cap, the grain — each classed with exact
   fibers).  **Zero motivated construction = a first-class head.**
9. **THE 44 SQUARES:** the corpus's named prime suspects — their
   status under the constructed Γ (do they carry the
   descent-obstruction/indivisibility signature?), at whatever
   scope the cap affords, EXCLUDED-BY-CAP printed otherwise.

## 4. Verdicts (the effectus four-head structure, composable,
computed in-gate, independent comparator, every segment flippable)

`GMAIN-CONSTRUCTED-<the carrier, the family's dimensions, the
provenance>` / `GMAIN-BLOCKED-AT-<named fact>` — composed with
`REQUIREMENTS-<the battery's per-test results: targets hit/missed;
holonomy agree/deviate; screen verdict; kernel/mover answers>` —
`MOTIVATION-<the inventory>` — `SCOPE-<caps, grains, conventions,
the delivery-free-shadow relation (the corrected 1/4-return as
CONTROL)>`.  **A Γ that constructs but misses the targets, or hits
them with an unmotivated construction, says so in its head.**  The
QFT-needs-gravity stake is settled ONLY by: constructed ∧ targets
hit ∧ holonomy consistent ∧ motivation non-empty — anything less is
partial and says which link failed.

## 5. Controls, falsifiers, discipline

REC-flat negative control; renewal-cut positive control; a
mis-normalized Γ failing the screen; a scrambled quotient breaking
the holonomy gate; era-minimum mutants + the five verdict classes +
target-value drifts + screen-verdict flips + per-battery-test
corruption; never-falsified census at the #34 standard (real
falsifiers, verified forcings); the #62 sha-provenance for every
cross-unit read; two plain runs byte-identical; the full ten-
engraving compliance sweep with computed statuses.  Compute honesty:
the 113-class carrier at the d≤4 cap is the declared arena; deeper
caps printed-and-gated if attempted, EXCLUDED-BY-CAP otherwise.
