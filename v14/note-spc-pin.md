# SPC (PAPER-37) — THE SPECIES TABLE, KINEMATIC HALF: THE QUANTUM-NUMBER INVENTORY — PIN (FROZEN)

**Frozen:** 2026-08-12, v14 ledger #309.  **Authority:** the
ACT terminal #307 (the measured gauge-image and chart
groups, the acting groups at the declared grains, the 136
carrier classes, the coupling inventory) + the AID terminal
#304 (the Young-subgroup identity structure; naming forced
at the admissibility axis; the stabilizer reads the SET) +
OCC #276 (the 27-cell carrier typing; the occupancy
ceiling's statistics selection at the carrier/pair grain) +
SMU #278 (the invariant simplexes) + R5 (the arena's
symmetry inventory).  **Unit:** paper-37-spc; code
v14/code/spc_exact.py (+ output + receipt).

**SCOPE DECLARATION:** this is the KINEMATIC half only —
which species CAN exist (labels, selection rules,
statistics compatibility).  Which species ARE realized,
with what spectral values, is SPC-D's question and waits on
POT's spectral door.  No dynamic claim is licensed here.

## THE QUESTION

The exact irreducible-representation census of the measured
symmetry structure — this universe's quantum-number
inventory.  All representation theory computed EXACTLY
(integer/cyclotomic-rational character entries; the groups
are finite and small — S₉'s character table is
integer-valued with 30 irreps; the gauge/chart/acting
groups are orders 8/16/108 and kin; no floats anywhere).

1. **THE GROUP INVENTORY (gated):** every measured symmetry
   group of the terminal corpus assembled with its
   provenance digest — gauge images, chart groups, acting
   groups at the three grains, the arena group, the
   stabilizer lattice (Young subgroups of S₉), the torus
   translations.  Each group's order re-derived, not typed.
2. **THE IRREP CENSUS:** for each inventory group, the full
   character table computed exactly and gated by two routes
   (column orthogonality AND row orthogonality as separate
   gates; class equation verified).  The species labels =
   the irreps, published with dimensions and multiplicities.
3. **THE CARRIER DECOMPOSITION (the heart):** the 136
   carrier classes (80 at the extension) as permutation
   modules over the acting groups — decomposed into irreps
   EXACTLY.  Which species the carrier can host, with what
   multiplicity; which irreps of the abstract groups have
   NO carrier realization (label-possible but
   carrier-homeless — measured, not argued).
4. **THE IDENTITY LAYER:** S₉'s irreps restricted along the
   measured stabilizer lattice (the Young structure AID
   sealed) — which species survive identity crystallization
   at the admissibility axis; the branching computed.
5. **SELECTION RULES:** tensor-product decompositions among
   the carrier-realized species — which composites are
   reachable (the interaction selection rules), rendered as
   an exact table.  E-24: counting-only; no rate claims.
6. **THE STATISTICS TIE-IN:** OCC's ceiling selected
   exclusion at the carrier/pair grain — which species are
   compatible with the selected statistics at that grain;
   the compatibility census exact.

## OUTCOMES (pre-registered, WITH FEASIBILITY — the #299 engraving)

- `SPC-INVENTORY-<the counts>` — always reachable: the
  census is a total exact computation over finite groups;
  its numbers are its outcome.
- `SPC-CARRIER-SELECTS-<k-of-n>` — reachable both ways: the
  carrier decomposition either realizes every abstract
  irrep (k = n, attainable if the permutation module is
  rich enough — dimension 136 exceeds the largest candidate
  irrep dimensions) or misses some (k < n, attainable since
  faithfulness of the module is not guaranteed a priori);
  both are decidable outcomes of measurement 3.
- `SPC-SELECTION-CLOSED-<rules>` / `SPC-SELECTION-OPEN` —
  reachable: the tensor census either closes on the
  carrier-realized set or exits it; both decidable.
- `SPC-STATISTICS-SPLITS-<partition>` — reachable: the
  compatibility census can return all-compatible or a
  proper split; both decidable from measurement 6.
- `SPC-BLOCKED-AT-<object>` — instrument fault only.
The head DERIVED, multi-way, control arms demonstrating
every word emittable through the real census machinery
(synthetic small-group controls through the SAME character
and decomposition functions — the ACT Z1 pattern).

## WALLS

**No dynamic vocabulary:** no masses, no spectra, no
stability, no realized-particle claims — those are SPC-D's,
behind POT's door; a wall gate kills them (plants must die
on the paper leg).  **No Standard-Model identification:**
no sentence may name an SM particle as an identification
(electron/quark/photon-as-this-species); comparisons are
licensed only as structural analogies stamped ANALOGY.  No
SI numbers; no continuum claims; the four walls; E-22 +
E-23 + E-24; the #267 checklist (headers bound; sentence
referent binding; spelled numerals; no typed counts;
digest-free whitelists; full-byte transcripts);
REFERENT-BOUND gates; class words gate-bound (#295); the
test-declaration duty where a blindness contrast appears.

## STANDARDS

Full era per HANDOFF-PROMPT.md §4/§9; exact arithmetic
(cyclotomic-rational characters; integer S₉ table); windows
declared in-string; seals at value-close; read-back before
os.replace; derived head, de-twinned comparator; failing
runs write nothing; byte ×2; #91 off-tree; scratch under
5G with absolute paths (the #306 incident's lesson).
Candidate readings until adjudication.

**SUCCESSOR NOTE (registered, not measured here):** the
glued-world species question — SEC's k=0 union carries
|Aut| = 1296² × 2 (the sector-swap element), which at the
irrep level implies an EXCHANGE PARITY no single sector
has; shared-actor unions (smaller Aut) imply
splitting/merging of sector species.  That census belongs
to a SEC×SPC successor chartered after BOTH panels rule.
