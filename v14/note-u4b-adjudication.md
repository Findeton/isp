# U4B — JOINT ADJUDICATION (effectus #150, operator #151, instrument #152)

**Inputs:** the three frozen reviews (effectus `dc37768d323b`,
operator `2f197645d498`, instrument `ebcfbb49fa88`); the frozen
delivery (paper `2bcba873d78e`, code `2c9b999dea31`, output
`8715c46a7b7f`, receipt `c4a4e8223b64`, commit 8c2dd98); pin
`d2cff9a274a8`; the protocol in-ledger at #149.  **Standing:**
three AWF; ~788 seat recomputations; ZERO false measured
numbers; TWO new prose claims in the register (the 9th and
10th).  Resolved grade: **ACCEPT-WITH-FIXES**.

## 1. THE CORRECTED SETTLEMENT

1. **CRYSTAL-GENERIC sustained; "emergence" re-worded.**  All
   252 crystalline seed pairs obey the affine law ON THE UNION
   of the two seed sets (252/252; the closed form 216 = 4×3×18
   operator-derived).  The null was stated on the wrong
   carrier, not defeated; RELATIONAL ORDER as a measured
   category is KILLED; the honest headline quantity adopted:
   **6/7 exactly of all crystallinity lies outside the
   inherited locus** — one law, seven times the territory.
   Fragility STRENGTHENED to family scope (9,072/9,072
   re-seatings land trivial: crystals are destroyed, never
   rotated).
2. **BOTH COUNTING CLAIMS ADOPTED AS THEOREMS** (operator: the
   wall airtight, max incidence exactly 18, tight at 1,296
   pairs; never-Riemannian follows, with the omitted
   n₍₁,₁₎ ≥ 1 step added to the proof).  F1's two-word repair
   ("at every site") applied — the 10th register claim; the
   36.2%-posdef-site fact and the third determinant case
   (0 pairs all-positive) added; the false independence
   sentence corrected by the effectus's stronger form (the
   9th register claim); the false exhaustiveness bolding
   scoped (the fragility column is window-scoped).
3. **THE R=3 SATURATION enters the successor register as the
   weld route's exact demand** (reviewer-grade: R=3 on the
   three link classes → n ≡ 1 at all 27 cells, det = 3/4,
   posdef 9/9 — to be made unit-grade by the successor); the
   coupling unit inherits THE POOL AND THE WALL
   (19,791/17,118), not the single witness; the Lorentzian
   resonance NAMED with the effectus's checked sentence
   (silence → governance); no pair-mechanism unit (the closed
   form registered as a theorem); the posdef ceiling open
   (wall permits 6, measured 3).
4. U4's two vacuous cells: ONE CROSS-REFERENCE LINE (no
   erratum, no annotation note).  The inventory upgrade TAKEN:
   the free item discharged via §7's own forcing — the unit
   becomes MOTIVATED at the RSQ standard.

## 2. The instrument findings

The seam ten wide with the era's clearest character: the
measured layer sealed and attack-proof; the VOUCHING layer
(schema, paper, provenance, claims, coverage, polarity)
unsealed — two corruptions leave the receipt byte-identical.
TOTALITY ruled a birth-date-excused miss (born #146, the
addendum #148) — ordered in repair, not held against the
delivery.  The untaken-seal invisibility (operator's hand-off,
instrument-measured; two-line fix).  #34 dishonest at three
rows (the honest denominator 51; two coinciding miscounts).
#125's normalization CLARIFIED by this adjudication to include
MARKDOWN PREFIXES (blockquote/list-item) — a clarification of
the existing engraving, not a new rule.  D60's defects
REGISTERED corpus-wide (the tie-break class; C2's missing
maxhits conjunct; the CWD-relative read) — a register note, no
retro-edits; the effectus's 7-unit audit scope carried.  CLI
residues (the directory/empty --verify-paper traceback; the
inert flag combination).

## 3. Binding repair orders (R-U4B-1 … R-U4B-10)

1. The heads recomposed per §1.1–1.2 (the union-carrier
   restatement; the 6/7 headline; the two-word and
   independence fixes; the theorem proofs completed; rendered
   from the receipt, string-equality gated).
2. THE SEAL TOTALIZED (the six keys; gate-time seals; the
   droppable-seal fix; G-SEAL-COMPLETE compares the manifest
   against DECLARED, not taken).
3. The vouching layer sealed (paper_claims/coverage/polarity
   inside the manifest; the transcript head bound — the
   byte-identical-receipt corruptions die).
4. #34 honesty (denominator 51; G-WAIVERS-VERIFIED's dead
   waiver resolved; the two mis-credited mutants re-pointed;
   reachability verified per the R5 lesson).
5. #125 with markdown-prefix normalization (the blockquote and
   list-item evasions die).
6. The Lorentzian naming sentence inserted; the successor
   register per §1.3; the cross-reference line per §1.4; the
   inventory upgrade executed (MOTIVATED).
7. The d60 defect register written as
   v14/note-d60-defect-register.md (the one extra file
   permitted): the class, the two gaps, the 7-unit audit
   scope, no retro-edits.
8. The CLI residues fixed (bad --verify-paper args exit 2; the
   inert combination rejected or honored).
9. The control-row-under-label fix (#87 at emission).
10. Paper single-threaded; deviations re-priced (the window
    licence renamed CONSTRUCTIBILITY per the operator; the
    BRANCHING-0 structural note; §6's "neither seed is a
    LINE" under-claim adopted).

## 4. Engravings

None new.  The #125 markdown-prefix CLARIFICATION recorded
above; the totality addendum's first application ruled with
birth-date fairness; recurrences logged (#34-reachability
second instance; the seal-coverage class third instance).
