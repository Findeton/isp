# Two welded worlds, glued

**SEC / paper-32.** Pin `v14/note-sec-pin.md` (FROZEN, sha256-12
`c46a9927f2a8`, ledger #258). Code `v14/code/sec_exact.py`; artifacts
`v14/code/sec_output.txt`, `v14/code/sec_receipt.json`.

**Verdict**, in four segments.

```
SEC-ARENA-[TWO DRIVEN R=3 SATURATING SECTORS; 45010 GLUINGS IN 16 COMBINATORIAL TYPES OVER k IN {0,1,2,3}; UNION CARRIERS 15..18; FORCED 16 OF 16 DRIVEN WINDOW RECORDS AT THE SHARED-SEED RULE, AND THAT FATE IS A PROPERTY OF THE RULE (ALL 45010 GLUINGS CARRY THE BASE INTO THE SECOND SECTOR UNDER IT); REFUSED 6 OF 16 AT PAPER-19'S CANONICAL RULE, WHERE THE FATE IS A PROPERTY OF THE DRIVEN GLUING AND NOT OF ITS TYPE -- THE CANONICAL RULE SPLITS 10 OF THE 16 TYPES AND ONLY 8740 OF THE 45010 GLUINGS CARRY; DRIVEN=COMBINATORIAL AT 26 OF 26]@WINDOW-32=16-TYPE-TRANSVERSAL-x-2-SEED-RULES-OF-45010-GLUINGS
```

```
SEC-COMPOSES-[ACTOR->SITE|CO-DIVISION-ACTOR-PAIR->LINK|DIVISION-COUNT->n_l(x)]@EMBEDDING+QUOTIENT<THE-UNION-DICTIONARY-EXISTS-AT-EVERY-TYPE:STRUCT-ALIVE-16-OF-16(INHERITED:THE-UNION'S-REALISED-PAIR-SET-IS-THE-AMALGAM'S-INCIDENCE-SET),COUNT-POSITIVE-16-OF-16|MOTIVATED-AT-10-OF-16-TYPES(4186-OF-45010-GLUINGS)|SITE-FIBER=1-AT-16-OF-16-TWO-ROUTES:THE-GLUING-BREAKS-EDGE-TRANSITIVITY-SO-THE-SEAM-IS-VISIBLE-IN-THE-STRUCTURE-ALONE|I-SITE-ASSIGNMENT-NEVER-FREE(0-OF-6-DOUBLED-TYPES,A-TYPE-ROW);AT-A-DOUBLED-TYPE'S-DECLARED-REPRESENTATIVE-THE-FREE-ITEMS-ARE-I-DIRECTION-LABEL(9-AT-6-OF-6)-AND-I-ORIENT(4-AT-5-OF-6,1-AT-1-OF-6)-BOTH-REPRESENTATIVE-ROWS-NOT-TYPE-ROWS(THE-PAIR-MOVES-INSIDE-4-OF-16-TYPES-ON-THE-DECLARED-SAMPLE)|EXTENDED-CARRIERS:BOTH-RETURN-FOUND-STRUCTURAL-AT-16-OF-16-TYPES-AND-64-OF-64-ROWS,EXT-PAIR-UNDER-SIMPLE-AND-EXT-INCIDENCE-UNDER-CHARTED;THE-INDUCED-COUNT-FIELD-IS-CARRIER-INDEPENDENT-AND-UNCHANGED;THE-RSQ-INVENTORY-IS-READ-AT-BARE-ONLY-SO-NO-REPAIR-OF-THE-FREE-ITEM-IS-MEASURED>
```

```
SEC-SEAM-CURVATURE-[THE-DIRECT-SUM-IS-A-DECLARATION-NOT-A-MEASUREMENT: AT EVERY SHARED SITE THE SIX DECLARED LINKS GIVE RANK 6 ON THE 10 ENTRIES OF Sym^2(Q^4), KERNEL 4 -- BY THEOREM ON THE ROW SPACE, WHICH MENTIONS NO COUNT, AND MEASURED AT 39 OF 39 SHARED SITES; THE DIRECT SUM IS THE CHOICE C=0 AND IS POSITIVE DEFINITE, AND AN EXACT RATIONAL COMPLETION REPRODUCING ALL SIX COUNTS IS NEGATIVE ON AN EXHIBITED VECTOR] -- SEAM-CONFINED COMPOSITIONALITY: EVERY CELL AT WHICH THE UNION'S COUNT DIFFERS FROM THE OWNING SECTOR'S HAS BOTH ENDPOINTS SHARED, 22 OF 22 AT 16 OF 16 TYPES, AND 0 PAIRS OFF COUNT 1 TOUCH AN UNSHARED ACTOR ANYWHERE IN THE UNION ARENA -- NO SECTOR-PRIVATE LINK EVER MOVES (FIXED-ATTRIBUTION READING) -- CROSS-SECTOR DIVISION EVENTS ARE ADMITTED BY THE COMMITTED GRAMMAR (2 OF 3 SPECIFICATIONS DRIVEN, THE OTHERS REFUSED ON THE VERSION LINEAGE) AND EACH CROSS LINK REMOVES EXACTLY ONE UNDETERMINED ENTRY (4,3,2,1,0) -- EVERY CROSS-SECTOR EVENT THAT WOULD FIX A SEAM ENTRY LEAVES THE TARGET BY THEOREM ON THE AMALGAM'S LINK SET (2 OF 2 ADMITTED WITNESSES DIE AT STRUCTURE); WHETHER ANY OTHER COMMITTED STRUCTURE FIXES THEM IS OPEN AT S-3
```

```
SEC-OVERLAP-TYPE-SELECTED-NOT-k-SELECTED-[EVERY k IN {2,3} CARRIES BOTH A WELDING AND A NON-WELDING TYPE, k IN {0,1} ONLY WELDING TYPES AND k=1 HAS NO PAIR TO DOUBLE; THE CRITERION IS ALIGNMENT: THE UNION WELDS IFF NO SHARED PAIR IS ADJACENT IN BOTH SECTORS, i.e. IFF EVERY SHARED PAIR SHARES A TRIPARTITE CLASS ON AT LEAST ONE SIDE; AT k=3 THIS IS 'ALL THREE IN ONE CLASS ON ONE SIDE', 2970 OF 42336 GLUINGS; THE WORD IS TYPED BY A COMPARATOR THAT STATES BOTH HYPOTHESES AND IS EXERCISED ON 4 DECLARED CONTROL FAMILIES, ONE OF WHICH RETURNS SEC-K-SELECTED] -- STERILITY-CONTROL=k=0-CONSISTENT-WITH-R1'S-COPYING-LESSON-AT-A-DIFFERENT-OBJECT(NOT-AN-INSTANCE-OF-THEOREM-B): |Aut|=3359232=|Aut(SECTOR)|^2 x 2 BY TWO ROUTES, 0 NEW PAIRS, 0 DOUBLED, 0 SEAM CELLS, THE DICTIONARY THE DIRECT SUM OF THE TWO -- AGAINST k=3-ALIGNED WHERE |Aut|=62208 AND THE SEAM CARRIES 12 UNDETERMINED ENTRIES
```

This is the REPAIRED object. The three-seat panel returned three
ACCEPT-WITH-FIXES, recomputing every published quantity independently and
finding **no published number false**; the repair order it produced (`v14/note-sec-adjudication.md`) is
executed here in full. Every reading below now carries the scope the panel
measured for it, and the two readings the panel refused -- a leak between
sectors, and a repair at the extended carrier -- are gone.

---

## 1. The question

Paper-19 drove one nine-actor arena whose co-division relation **is** I7's
own Cayley incidence, and welded it at both readings of "a map" with zero
free items:

> 1,296 site assignments carry the record's co-division incidence onto

Paper-21 drove the next budget and turned the reason into a theorem:

> The theorem is that **zero free items holds exactly at the link-constant
> records, and I7 declares none of them.**

Both are ONE SECTOR. The registered question is whether geography
COMPOSES -- whether two such arenas, glued, carry one geometry between
them. R1 answers half of it already, and in the negative direction: a
refinement family that answers R1's own question

> must **divide** a block, not copy one

so a disjoint juxtaposition of two sectors is sterile by that lesson, and
any coupling must be SHARED PARTICIPATION. This unit glues two copies of
the driven R = 3 saturating record by k shared actors, k in {0, 1, 2, 3},
and asks the weld question of the union. LOR's delivered lesson enters as
a METHOD and not as a result. This unit's own pin states it:

> the bare-union and extended-carrier readings both run (the LOR
> lesson: the carrier may need pairs)

which is why the union is put to the detector at three carriers rather
than one. **No sentence of LOR is quoted or read anywhere in this unit.**
Its delivered text was under concurrent rewrite in the working tree while
this unit ran, so rule #91 forbade reading it. It is cited as **the
delivered version at v14 ledger #252**, whose sha256-12 was `f3e9e9df2c70`
at that entry and which has since moved on disk under its own repair --
which is why the citation names the ledger entry and not a digest that no
longer resolves. The lesson is anchored on the frozen pin instead, and the
abstention is gated on this run's own READ REGISTER: every file this
instrument opens is recorded inside the accessor that opens it, and the
gate reads that register rather than a list the source loop keeps about
itself.

## 2. The arena, declared as data (RUNBOOK section 15)

### 2.1 The base object and the variables

The base object is TWO copies of d66's `CONFLICT-GRID(g = 3, R = 3)`, each
running the saturating collinear arrangement -- three rounds grouped on the
three link-direction parallel classes of AG(2, 3) -- which is paper-19's own
driven witness. Driven here through the committed menus,
one sector runs to 48 events with 9 division events, its link field is 1 at all 27 of 27 cells, det = 3/4 at all nine sites and the form is positive definite at 9 of 9.

Three variables are declared.

**THE GLUING.** k actors of sector A are identified with k actors of
sector B by an injection. Every one of the **45010** gluings is enumerated
and classified; the family's size is counted a second time from the closed
form C(9,k) x 9!/(9-k)!, which returns **45010** from a five-line
independent route.

**THE COMBINATORIAL TYPE**, and it is exactly *which tripartite classes
align*. The realised relation of one sector is the complete tripartite
graph with three parts of three, and its parts are the three lines of the
one direction I7 does not declare. The type of a gluing is the bipartite
part-incidence matrix of its shared set -- how many shared actors sit in
A-part i and B-part j -- taken up to permutation of each sector's parts.
There are **16** of them.

**THE SEED RULE.** In each conflict group one member seeds. Two rules are
declared and BOTH are driven: `first`, paper-19's canonical transversal,
and `shared`, which seeds on a shared actor whenever the group has one.
The seed rule is a variable of the DRIVER, not of the gluing, and no
geometric claim in this paper reads it: the dictionary, type, seam,
compatibility and alignment censuses are all computed from the schedule
alone, by a route that never consults a seed.

### 2.2 The target, and the one declaration the record cannot make

The target is the AMALGAM T(k, gamma): two copies of I7's lattice glued
along the same k sites, built from the lattice and the gluing alone. It
never sees the record, which is what makes the structural test of Stage 2 a
measurement rather than a construction.

The target carries one declared axis of its own, and it is load-bearing.
Under **SIMPLE** individuation a link IS an unordered pair of sites, which
is weld 2's own definition carried unchanged; two sites shared by both
sectors then carry ONE link. Under **CHARTED** individuation a link is a
(chart, site pair), so the two charts keep their links distinct even where
they join the same two sites. **A division event knows an actor pair and
nothing else, so the record can only ever make the SIMPLE distinction:**
CHARTED is a declaration, and the paper says where it is spent -- at the
extended carrier of section 4.3, and again at the seam chart of section 6.1
wherever a shared site is incident to a doubled pair.

**The test is declared, and so is what it would have shown (AID section
6.2).** The question "does the record distinguish the two charts' links at a
shared site" is posed under FIXED ATTRIBUTION: a link is named by the actor
pair the division event names, and under that naming the record is BLIND to
the chart -- it returns the same object for both, which is the SIMPLE
reading. Under the TRANSPORTED reading the question is not posed at all,
because nothing this unit declares carries one chart's links onto the
other's. Had the record been SIGHTED here -- had any division event carried
a chart index -- the CHARTED individuation would have been a measurement
and section 4.3's extended carrier would have been a repair rather than a
declaration. It is not, and that is the whole content of the two places
where CHARTED is spent.

### 2.3 The carriers and the readings

| carrier | site objects | target |
|---|---|---|
| `BARE` | ACTOR | the amalgam, SIMPLE |
| `EXT-PAIR` | ACTOR + CO-DIVISION PAIR | the amalgam's subdivision, SIMPLE |
| `EXT-INCIDENCE` | ACTOR + CO-DIVISION INCIDENCE | the amalgam's subdivision, CHARTED |

Both of weld 2's readings run at every carrier: EMBEDDING, a bijection
carrying the realised relation ONTO the target's incidence, and QUOTIENT,
one carrying it INTO. Every row below is stamped with the reading and the
carrier it was decided under.

### 2.4 The declared driven window, disclosed here and in the head

Driving 45010 unions is not affordable. The **declared driven window** is
**32** records -- one canonical representative of every one of the 16
combinatorial types, at BOTH declared seed rules -- and the head names not
only its size but what it is: a complete transversal of the 16 types at
two seed rules, so no reader can meet the number without meeting its scope.

**Every other column of this paper is exhaustive over an object the window
does not cap**, and the licence is re-proved here rather than inherited.
The union's co-division relation is computable from the schedule alone; the
driven route and the combinatorial route are compared pair for pair on
every FORCED window record, and they agree at **26 of 26**. The type census
then runs over all 45010 gluings, and the doubled-free criterion is checked at
every one of them.

The licence itself is now gated. Every 16-type column speaks about 45010
gluings only if the union arena is a function of the type, and that is
measured on the type's own members rather than assumed: at every type
a declared sample of up to twelve of its gluings is compared against the type's representative, each sampled member's WEIGHTED union is carried onto the
representative's by an EXHIBITED isomorphism, and a Weisfeiler-Leman
invariant computed by refinement alone -- sharing no code with the
isomorphism search -- is constant across the sample. The mechanism is that
two gluings of one type are related by an element of Aut(K3,3,3) x
Aut(K3,3,3), which is *not* the affine group: it carries a collinear
transversal to a non-collinear one. That is exactly why the ABSTRACT arena
is a type invariant while everything read against the target's CHART is
finer than the type -- see section 5.2, where every published row is put on
one side of that line or the other.

One further item is measured rather than assumed, and by exhaustion rather
than by one witness. The saturating arrangement each sector runs is a
declared choice, and its fiber is 1: all **280** partitions of the nine
sites into triples are enumerated, the **36** that are I7-STRICT saturating
are found among them, and every one of the **12** unordered admissible
arrangement triples -- **72** ordered -- realises 27 pairs at count 1 on an
arena isomorphic to the collinear one. A DRIVEN non-collinear I7-STRICT
triple, found by search and never typed, exhibits it at the record.

## 3. Stage 1 -- the arena family

### 3.1 The census of types

Exhaustive over the whole family: **the family is 45010 gluings in 16 combinatorial types**.

In the table `n` is the union's carrier count,
`E` is its realised pair count and `dbl` the number of pairs carrying two
divisions.

| type | gluings | n | E | dbl | \|Aut\| | \|Aut_w\| | site fiber |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `(0,)` | 1 | 18 | 54 | 0 | 3359232 | 3359232 | 1 |
| `(1, (0, 0, 1))` | 81 | 17 | 54 | 0 | 41472 | 41472 | 1 |
| `(2, (0, 0, 1), (0, 1, 1))` | 486 | 16 | 54 | 0 | 3456 | 3456 | 1 |
| `(2, (0, 0, 1), (1, 0, 1))` | 486 | 16 | 54 | 0 | 3456 | 3456 | 1 |
| `(2, (0, 0, 1), (1, 1, 1))` | 1458 | 16 | 53 | 1 | 2304 | 2304 | 1 |
| `(2, (0, 0, 2))` | 162 | 16 | 54 | 0 | 20736 | 20736 | 1 |
| `(3, (0, 0, 1), (0, 1, 1), (0, 2, 1))` | 486 | 15 | 54 | 0 | 3456 | 3456 | 1 |
| `(3, (0, 0, 1), (0, 1, 1), (1, 0, 1))` | 11664 | 15 | 53 | 1 | 288 | 288 | 1 |
| `(3, (0, 0, 1), (0, 1, 1), (1, 2, 1))` | 8748 | 15 | 52 | 2 | 192 | 192 | 1 |
| `(3, (0, 0, 1), (0, 1, 2))` | 972 | 15 | 54 | 0 | 1728 | 1728 | 1 |
| `(3, (0, 0, 1), (1, 0, 1), (2, 0, 1))` | 486 | 15 | 54 | 0 | 3456 | 3456 | 1 |
| `(3, (0, 0, 1), (1, 0, 1), (2, 1, 1))` | 8748 | 15 | 52 | 2 | 192 | 192 | 1 |
| `(3, (0, 0, 1), (1, 0, 2))` | 972 | 15 | 54 | 0 | 1728 | 1728 | 1 |
| `(3, (0, 0, 1), (1, 1, 1), (2, 2, 1))` | 4374 | 15 | 51 | 3 | 768 | 768 | 1 |
| `(3, (0, 0, 1), (1, 1, 2))` | 5832 | 15 | 52 | 2 | 576 | 576 | 1 |
| `(3, (0, 0, 3))` | 54 | 15 | 54 | 0 | 62208 | 62208 | 1 |

Read the last column first: **the site-assignment fiber is 1 at every one of the 16 types**. The
two routes are the index of the weight-stabilizer in Aut and the orbit of
the doubled-edge set under Aut's own generators, and they agree everywhere
-- but the reader should know where the agreement has content. At the 10
doubled-free types the count field is constantly 1, so the weighted and
unweighted orders coincide and the doubled-edge set is empty: both routes
read a trivial object and the fiber is 1 by inspection. The measurement has
content at the 6 doubled types, where the field is NOT constant and the
fiber is 1 all the same.

The site assignment never goes free in this family. Paper-21's theorem --
zero free items exactly at the link-constant records -- is committed at its
own saturating stratum, a nine-actor R = 4 arena, and asserts nothing about
a glued arena of fifteen to eighteen actors; it was never claimed there,
and nothing here finds it wanting. What carries it there is measured absent
here. At R = 4 the realised relation is edge-transitive, so a count field
invariant under all 1296 of its automorphisms is constant on all 27 edges,
and the site fiber is 1 exactly at the link-constant records.

**The gluing destroys edge transitivity**, and the degree law is measured
rather than stated. A shared actor carries both sectors' links at its site:
its degree is 12 where no shared pair is adjacent in both sectors, and 10
or 11 where doubled pairs cost it one degree apiece -- a pair realised in
both sectors is still ONE link of the union -- against 6 at every unshared
actor. So the shared set is fixed setwise by every automorphism of the bare
structure, the seam is visible without the weights, and the weight function
is stabilised whether or not it is constant. The census exhibits it: at the
6 doubled types the field takes the values 1 and 2 and the site fiber is 1
all the same, by two routes. Read as a universal the biconditional would
forbid that; read at its declared scope it does not reach it. The free item
moves from the map to the labelling.

`STRUCT-ALIVE` at 16 of 16 is INHERITED and the head says so. Under SIMPLE
individuation the amalgam's incidence set IS the union's realised pair set
at every type -- necessarily, since each sector's realised relation is I7's
own link incidence and the union and the amalgam are formed by the same
identification -- and that identity is now a gate. The structural test
cannot fail on this family; it is not a vacuous detector, because the
declared dead arms of section 4.4 die at structure, but the 16-of-16 is a
corollary of the single-sector identity rather than news at the union.

### 3.2 The driven window, and the seed rule that decides it

| type | seed rule | fate | events | divisions | driven = combinatorial |
|---|---|---|---|---|---|
| `(0,)` | first | FORCED | 96 | 18 | yes |
| `(0,)` | shared | FORCED | 96 | 18 | yes |
| `(1, (0, 0, 1))` | first | FORCED | 98 | 18 | yes |
| `(1, (0, 0, 1))` | shared | FORCED | 98 | 18 | yes |
| `(2, (0, 0, 1), (0, 1, 1))` | first | FORCED | 98 | 18 | yes |
| `(2, (0, 0, 1), (0, 1, 1))` | shared | FORCED | 98 | 18 | yes |
| `(2, (0, 0, 1), (1, 0, 1))` | first | REFUSED | 49 | 9 | -- |
| `(2, (0, 0, 1), (1, 0, 1))` | shared | FORCED | 100 | 18 | yes |
| `(2, (0, 0, 1), (1, 1, 1))` | first | FORCED | 98 | 18 | yes |
| `(2, (0, 0, 1), (1, 1, 1))` | shared | FORCED | 98 | 18 | yes |
| `(2, (0, 0, 2))` | first | REFUSED | 49 | 9 | -- |
| `(2, (0, 0, 2))` | shared | FORCED | 100 | 18 | yes |
| `(3, (0, 0, 1), (0, 1, 1), (0, 2, 1))` | first | FORCED | 98 | 18 | yes |
| `(3, (0, 0, 1), (0, 1, 1), (0, 2, 1))` | shared | FORCED | 98 | 18 | yes |
| `(3, (0, 0, 1), (0, 1, 1), (1, 0, 1))` | first | REFUSED | 49 | 9 | -- |
| `(3, (0, 0, 1), (0, 1, 1), (1, 0, 1))` | shared | FORCED | 100 | 18 | yes |
| `(3, (0, 0, 1), (0, 1, 1), (1, 2, 1))` | first | FORCED | 98 | 18 | yes |
| `(3, (0, 0, 1), (0, 1, 1), (1, 2, 1))` | shared | FORCED | 98 | 18 | yes |
| `(3, (0, 0, 1), (0, 1, 2))` | first | FORCED | 100 | 18 | yes |
| `(3, (0, 0, 1), (0, 1, 2))` | shared | FORCED | 100 | 18 | yes |
| `(3, (0, 0, 1), (1, 0, 1), (2, 0, 1))` | first | REFUSED | 49 | 9 | -- |
| `(3, (0, 0, 1), (1, 0, 1), (2, 0, 1))` | shared | FORCED | 102 | 18 | yes |
| `(3, (0, 0, 1), (1, 0, 1), (2, 1, 1))` | first | FORCED | 100 | 18 | yes |
| `(3, (0, 0, 1), (1, 0, 1), (2, 1, 1))` | shared | FORCED | 100 | 18 | yes |
| `(3, (0, 0, 1), (1, 0, 2))` | first | REFUSED | 49 | 9 | -- |
| `(3, (0, 0, 1), (1, 0, 2))` | shared | FORCED | 102 | 18 | yes |
| `(3, (0, 0, 1), (1, 1, 1), (2, 2, 1))` | first | FORCED | 98 | 18 | yes |
| `(3, (0, 0, 1), (1, 1, 1), (2, 2, 1))` | shared | FORCED | 98 | 18 | yes |
| `(3, (0, 0, 1), (1, 1, 2))` | first | FORCED | 100 | 18 | yes |
| `(3, (0, 0, 1), (1, 1, 2))` | shared | FORCED | 100 | 18 | yes |
| `(3, (0, 0, 3))` | first | REFUSED | 49 | 9 | -- |
| `(3, (0, 0, 3))` | shared | FORCED | 102 | 18 | yes |

**The seed rule is the driver's own freedom, and it is priced.** Under the
shared-seed rule the committed grammar drives every one of the 16 driven
window records. Under paper-19's canonical rule the grammar refuses at
**6** of the 16 driven window records, and the refusals are recorded and
never patched.

**The two clauses do not have the same standing, and the difference is
measured.** The window drives ONE representative per type, and that
representative is this unit's free choice. At the shared-seed rule the
choice is inert: a shared actor seeds wherever a group holds one, so the
predicate that decides drivability holds at every one of the 45010 gluings
of the family, and the FORCED column is a property of the RULE. At
paper-19's canonical rule it is not: the seed reads the actors' NAMES,
which the combinatorial type does not fix, and **the fate is a property of
the driven gluing and not of its type** -- the canonical-rule predicate
SPLITS 10 of the 16 types, and only 8740 of the 45010 gluings carry the
base into the second sector under it. So "6 of 16" is a count of driven
window RECORDS and never of types, and a different choice of representative
returns a different number. Choice-inventory item 16 is classed
VERDICT-DETERMINING for that clause accordingly.

The predicate is not asserted, it is validated before it is used. It is a
property of the SCHEDULE -- in the second sector's first round, is every
group holding a shared actor seeded by a shared actor -- and it is compared
against the DRIVEN fate at every one of the 32 window records AND at 6
further records driven OFF the window, at types the predicate separates
from their own representative, with both fates occurring among them. Only
then is the family-wide count published, and it is published as a
prediction of a validated predicate rather than as 45010 drives.

The mechanism is measured and it is the version lineage, not the geography.
A group seeded by a sector-B actor that contains a shared actor must supply
its base to that actor; the delivery puts sector A's whole past into the
seed's causal past; the base it is about to propose on has been superseded
there; and the layer's own menu offers nothing but idle. At every refusal
in this window the same event refuses -- `propose ('B', (1, 0))` at prefix
49, at 6 of 6. Whether that mechanism is the *only* obstruction to
drivability is not measured here.

## 4. Stage 2 -- the union's dictionary

### 4.1 The census

At every one of the 16 types the union's dictionary EXISTS: the structural
test passes at both readings and at all three carriers, and the induced
count is strictly positive everywhere. **the union welds at 10 of the 16 types**
-- at the bare carrier, where the RSQ inventory is read. The fiber column
is the whole inventory triple, in the parent's own order.

| bare fate | types | free items | site, label, orient |
|---|---|---|---|
| `FOUND-candidate` | 10 of 16 | none | 1, 1, 1 |
| `UNMOTIVATED` | 5 of 16 | I-DIRECTION-LABEL, I-ORIENT | 1, 9, 4 |
| `UNMOTIVATED` | 1 of 16 | I-DIRECTION-LABEL | 1, 9, 1 |

**This table is of two kinds and the difference is measured, not stylistic.**
The bare fate and the type counts are TYPE ROWS: the fate is a function of
the doubled count, which is a type invariant. The free-item list and the
label and orient entries of the fiber triple are read at each type's
declared representative and are REPRESENTATIVE ROWS, exactly as section 4.2
says of them and as section 5.2 measures; only the site entry of that
triple is a type row. A reader who carries the label and orient figures
across a type's fiber carries them further than this unit measured them.

The two readings AGREE at every row of this census, and that agreement is
the same datum paper-19 reported: the readings are opposite inclusions, so
they meet exactly when the realised relation IS the target's incidence.
The gluing does not break that equality; it breaks the COUNT.

### 4.2 What the free items are, and what they are not

At a doubled type `I-SITE-ASSIGNMENT` is never free -- at 0 of the 6. The
free items are `I-DIRECTION-LABEL`, at fiber 9 at every one of the six, and
`I-ORIENT`, at fiber 4 at five of the six. A doubled pair sits on one
direction of the A-chart and one of the B-chart; permuting either chart's
labels moves which cell reads 2; and since the two charts relabel
independently the label fiber is 3 x 3. The orientation is the mirror of
the same mechanism: a chart's flip reads the doubled pair at the other
endpoint's cell, so the field moves unless the doubled-cell set is
invariant under (x, d) to (x + d, d) -- which happens only when the doubled
cells fill a whole line, and that is exactly what the triangle
representative's collinear shared triple does, which is why its orientation
fiber is 1. The map is forced and the LABELLING is not -- the opposite of
the failure mode paper-21 measured, where the map was free and the symmetry
forced the field.

These two fibers are rows of the DRIVEN GLUING, not of its type; they are
read at the type's declared representative and stamped `AT-THE-REPRESENTATIVE`
in section 5.2, where the split is measured.

### 4.3 The extended carriers, and the LOR lesson's exact price

`EXT-PAIR` adds one site object per realised pair and changes no count the
record makes: a pair carrying two divisions is still one object.
`EXT-INCIDENCE` adds one object per (division event, pair), so a doubled
pair becomes two site objects and the carrier's own incidence relation is
identically 1 by construction. Both extended carriers return
`FOUND-STRUCTURAL` at all 16 types and at both readings -- the structural
question only, at the scope stamped in the row.

**What was measured, and what was not.** The dictionary's induced count
field is read from the ACTOR PAIR, and it is therefore the same at all
three carriers: a gate compares the three carriers' field values and cell
counts at every type and both readings, and they are identical, so the
field still takes the values 1 and 2 at the six doubled types under
`EXT-INCIDENCE` exactly as under `BARE`. Whether the extended carrier
removes the free item is **not measured**: the RSQ inventory is a statement
about the map to the target's cells, those cells are the bare carrier's,
the inventory is read there and stamped `READ-AT-BARE` -- so **no repair of
the free item is measured** anywhere in this run, and no head clause claims
one.

**The test is declared, and so is what it would have shown.** The test that
was run at the extended carriers is the STRUCTURAL one, and it is posed
under FIXED ATTRIBUTION at the carrier's own cells. The test that would
decide the repair -- count the free items of the map into the SUBDIVIDED
target's cells -- was not posed, so this run is BLIND to it by construction
and not by outcome. Had it been posed and had it returned zero free items
at the doubled types, the extended carrier would have been a repair; had it
returned the same fibers as the bare carrier, the carrier would have been
inert. This unit distinguishes neither, and the head says so rather than
picking the flattering branch.

**The price is stated and it is not small.** That carrier only has a target
to weld onto under CHARTED individuation, because the amalgam under SIMPLE
individuation has one link where the record now has two objects. So the
structural pass is bought by declaring that the two sectors' links stay
distinct at the seam -- a distinction the record cannot draw, since a
division event knows an actor pair and nothing else. **The extended carrier
repairs the seam by declaring away the thing the seam measures.**

### 4.4 The controls, two-way

HA requirement 3 is carried and honoured: every value this detector can
return is exhibited in this run, and the gate is bound to the EXACT fate
string of every arm, so the two readings' signature cannot be swapped.

| arena | reading | fate |
|---|---|---|
| SEC-UNION(3,aligned)@I7-SINGLE | EMBEDDING | ARITY-DEAD |
| SEC-UNION(3,aligned)@T(0) | EMBEDDING | ARITY-DEAD |
| SEC-UNION(3,triangle)@T(3,ALIGNED) | EMBEDDING | STRUCT-DEAD |
| SEC-UNION(3,aligned)-FALSIFIER | EMBEDDING | STRUCT-DEAD |
| SEC-UNION(3,aligned)-FALSIFIER | QUOTIENT | COUNT-DEAD |

The union against a single sector's target is ARITY-DEAD at 15 objects
against 9; the k = 3 union against the disjoint target is ARITY-DEAD at
15 against 18; a triangle-glued union against the ALIGNED amalgam is
STRUCT-DEAD, so the type is not a label but a shape; and the declared
falsifier -- one arbitration withheld from a driven union, after which the
layer refuses the remainder, leaving a 13-event stump with **2** divisions
and **6** realised pairs -- is STRUCT-DEAD under the embedding reading and
COUNT-DEAD under the quotient reading, which is paper-19's own signature
for the two readings differing. The stump's own shape is published beside
the row rather than left to be imagined.

## 5. Stage 3 -- the gluing fiber

### 5.1 The compatibility census

For every shared actor and every one of its links, the union's count is
compared against the count that actor's OWN sector carries -- read from
this run's own driven sector field, cell by cell, and not from a literal.

**The test is declared (AID section 6.2).** A cell is named by its ACTOR
PAIR, at the naming the gluing declares: this is the FIXED-ATTRIBUTION
reading, and it is the only reading under which the comparison is posed at
all. Under the TRANSPORTED reading nothing carries the union's cells to a
sector's, because the two objects have different automorphism groups and no
relabelling between them is declared. Every row of this census is a
fixed-attribution row and says so.

| type | shared cells | cells where the union differs | of those, both endpoints shared | pairs off count 1 touching an unshared actor | verdict |
|---|---|---|---|---|---|
| `(0,)` | 0 | 0 | 0 | 0 | COMPATIBLE |
| `(1, (0, 0, 1))` | 6 | 0 | 0 | 0 | COMPATIBLE |
| `(2, (0, 0, 1), (0, 1, 1))` | 12 | 0 | 0 | 0 | COMPATIBLE |
| `(2, (0, 0, 1), (1, 0, 1))` | 12 | 0 | 0 | 0 | COMPATIBLE |
| `(2, (0, 0, 1), (1, 1, 1))` | 12 | 2 | 2 | 0 | SEAM-DEFORMED |
| `(2, (0, 0, 2))` | 12 | 0 | 0 | 0 | COMPATIBLE |
| `(3, (0, 0, 1), (0, 1, 1), (0, 2, 1))` | 18 | 0 | 0 | 0 | COMPATIBLE |
| `(3, (0, 0, 1), (0, 1, 1), (1, 0, 1))` | 18 | 2 | 2 | 0 | SEAM-DEFORMED |
| `(3, (0, 0, 1), (0, 1, 1), (1, 2, 1))` | 18 | 4 | 4 | 0 | SEAM-DEFORMED |
| `(3, (0, 0, 1), (0, 1, 2))` | 18 | 0 | 0 | 0 | COMPATIBLE |
| `(3, (0, 0, 1), (1, 0, 1), (2, 0, 1))` | 18 | 0 | 0 | 0 | COMPATIBLE |
| `(3, (0, 0, 1), (1, 0, 1), (2, 1, 1))` | 18 | 4 | 4 | 0 | SEAM-DEFORMED |
| `(3, (0, 0, 1), (1, 0, 2))` | 18 | 0 | 0 | 0 | COMPATIBLE |
| `(3, (0, 0, 1), (1, 1, 1), (2, 2, 1))` | 18 | 6 | 6 | 0 | SEAM-DEFORMED |
| `(3, (0, 0, 1), (1, 1, 2))` | 18 | 4 | 4 | 0 | SEAM-DEFORMED |
| `(3, (0, 0, 3))` | 18 | 0 | 0 | 0 | COMPATIBLE |

A gluing at k shared actors puts 6k cells into this census, so the second
column runs from none at k = 0 to 18 shared cells at k = 3.

Two sectors that share nothing realise fifty-four pairs between them, and
the union of two sectors that DO share never realises more: a shared pair
adjacent in both sectors costs the union a pair rather than earning it one.

**This is the sharpest thing in the unit that nobody asked for, and it is a
locality result.** At a doubled type the union reads 2 at exactly the cells
of the pairs that are adjacent in BOTH sectors, where each sector alone
reads 1. Both endpoints of every such pair are shared actors, necessarily:
a pair realised in both sectors has both its actors in both. Measured over
the whole union arena and not only over the census's shared cells, **no pair carrying a count other than 1 touches an unshared actor at any of the 16 types**.
So the deformation is confined to the seam in the strongest sense
available: what moves is the count on the links the two sectors hold in
common, and it moves because the union counts the division events of both.
**Nothing private to either sector moves, and no sector-private link ever
moves** -- there is no event of one sector reaching a link the other does
not also carry. The union's geometry is the sectors' everywhere except on
the links they jointly own, and there it is the sum of theirs. That is a
compositionality statement, not a leak.

### 5.2 The gluing's freedom, priced -- and which rows are rows of a type

The gluing carries 45010 choices collapsing onto 16 combinatorial types
and, up to isomorphism of the weighted union, onto 12 arenas: **the 16 types carry 12 union arenas up to isomorphism**.
Four pairs of types are exchanged by the A-to-B swap, which the type does
not quotient by, and each such pair carries one arena -- measured by an
isomorphism search between the representatives rather than read off the
census's equal columns.

| type | mirror type | gluings | arena fiber |
|---|---|---|---|
| `(2, (0, 0, 1), (0, 1, 1))` | `(2, (0, 0, 1), (1, 0, 1))` | 486 + 486 | 972 |
| `(3, (0, 0, 1), (0, 1, 1), (0, 2, 1))` | `(3, (0, 0, 1), (1, 0, 1), (2, 0, 1))` | 486 + 486 | 972 |
| `(3, (0, 0, 1), (0, 1, 1), (1, 2, 1))` | `(3, (0, 0, 1), (1, 0, 1), (2, 1, 1))` | 8748 + 8748 | 17496 |
| `(3, (0, 0, 1), (0, 1, 2))` | `(3, (0, 0, 1), (1, 0, 2))` | 972 + 972 | 1944 |

The type map's fiber is the gluings-per-type column of section 3.1; the
arena map's fiber is that column summed over each mirror pair.

**Every published row is now typed, and the two classes are not the same.**
The ABSTRACT union arena is a function of the type: carriers, realised
pairs, doubled pairs, |Aut|, |Aut_w| and the site-assignment fiber are
constant on every type's fiber, and so is the FOUND / UNMOTIVATED fate,
because that fate is a function of the doubled count. Those rows -- the
whole of section 3.1, section 7, and section 4.1's fate and type columns
together with the site entry of its fiber triple -- are TYPE ROWS. The
chart-borne rows are not: the `I-DIRECTION-LABEL` and `I-ORIENT` fibers of
sections 4.1 and 4.2, the seam's per-site (n_A, n_B) profile with its
direct-sum minors in section 6.1, and drivability under the canonical seed
rule in section 3.2 are functions of the GLUING. Each of those is read at
that type's declared representative and is a REPRESENTATIVE ROW. The split
is gated, and for both chart-borne rows rather than for one: on the same
declared sample per type, the (I-DIRECTION-LABEL, I-ORIENT) pair takes more than one value at 4 of the 16 types, and **the per-site seam profile takes more than one value at 6 of the 16 types**,
while the abstract
arena does not move at any of them. That is a floor and not a census: the
sample is a declared twelve members of each type, and the panel's own
wider probe moved the pair at more types still.

The freedom the pin's Stage 3 asks about at the seam is answered not here
but in section 6.1: four undetermined entries per shared site.

## 6. Stage 4 -- curvature of the gluing

### 6.1 The union's form against the direct sum

At a shared site the union carries six links: three of the A-chart and
three of the B-chart. I7's readout says the count on a link is the squared
length of its direction, so the union's form at that site is the solution
of an exact linear system on Sym^2 of the direct-sum chart -- ten unknowns,
six equations, solved by exact row reduction over the rationals.

| seam | n(A) | n(B) | equations | unknowns | rank | kernel | Q(v) at the witness |
|---|---|---|---|---|---|---|---|
| ALIGNED(k=3) | [1, 1, 1] | [1, 1, 1] | 6 | 10 | 6 | 4 | -2 |
| TRIANGLE(k=3) | [1, 2, 1] | [1, 2, 1] | 6 | 10 | 6 | 4 | -1 |
| k=1 | [1, 1, 1] | [1, 1, 1] | 6 | 10 | 6 | 4 | -2 |

**the seam system has rank 6 on 10 unknowns, so its kernel is 4**, and the quantifier is earned twice
rather than by three witnesses. The six rows are the quadratic rows of the
six declared directions a1, a2, a1+a2, b1, b2, b1+b2, and they mention no
count: the coefficient matrix alone has rank 6 on the ten entries whatever
the right-hand side, which is a theorem about the declared directions and
is computed here on the coefficients and re-computed at five declared count
vectors. And it is a census: **the seam system is solved at every one of the 39 shared sites of the 16 type representatives**,
one system per site, with rank 6 and kernel 4 at every one of them, and no
cross-site consistency condition imposed or measured. The twelve
undetermined entries the head reports at the aligned k = 3 seam are that
census's own per-site sum, not a product of a kernel and a site count.

The direct sum -- the completion that sets every cross entry to zero -- is
one point of a four-parameter affine family, and it is positive definite
by the exact Sylvester criterion, with leading minors 1, 3/4, 3/4, 9/16 at the aligned
seam and 1, 1, 1, 1 at the
triangle seam. But the family is not confined to that cone: at every seam
an exact rational completion is exhibited which reproduces all six measured
counts and is NEGATIVE on an exhibited rational vector.

So **the direct sum is a declaration, not a measurement.** The record fixes
six of the ten entries of the seam's form and says nothing whatever about
the other four.

The indefinite completion is NAMED AND NOT READ: that the seam's undetermined block admits completions of mixed sign is a statement about what the record fails to fix, it is not a signature, and no Lorentzian, causal or dimensional reading of it is taken here or licensed by anything measured here.

Two scope notes travel with the table, and both are the CHARTED
declaration again. First, the seam rows are read at each type's declared
representative and are REPRESENTATIVE ROWS, and that is measured on the
same declared sample as the fibers of section 4.2 rather than asserted:
**the per-site seam profile takes more than one value at 6 of the 16 types**
-- the six doubled ones, the triangle among them. Second, at a shared site
incident to a doubled pair the direct-sum chart gives ONE union link two
direction vectors, one per chart, so the system there is read under CHARTED
individuation. Read no further than the arithmetic: the triangle seam's
direct-sum minors are all 1 where the aligned seam's are 1, 3/4, 3/4, 9/16,
so at this representative the doubled link does not merely relabel the
seam -- it moves the determinant of the charted form. That is a comparison
of two charted seams and not of two records.

### 6.2 What would determine the four entries

A co-division pair joining an A-neighbour of the shared site to a
B-neighbour of it has difference b - a, so its count is one linear equation
on one cross entry. Measured on the exact system, one row at a time:

| cross links | rank | kernel |
|---|---|---|
| 0 | 6 | 4 |
| 1 | 7 | 3 |
| 2 | 8 | 2 |
| 3 | 9 | 1 |
| 4 | 10 | 0 |

### 6.3 The cross-sector division event, driven

Whether the committed grammar admits a conflict group spanning the two
sectors is not decided by fiat here. It is driven.

| specification | seed | fate | new pairs | foreign pairs | dictionary after |
|---|---|---|---|---|---|
| SHARED-SEEDED | ('S', 0) | ADMITTED | 1 | 1 | STRUCT-DEAD |
| A-SEEDED | ('A', (1, 1)) | REFUSED | 0 | 0 | -- |
| B-SEEDED-PURE | ('B', (1, 1)) | ADMITTED | 2 | 2 | STRUCT-DEAD |

**2 of the 3 cross-sector specifications are ADMITTED by the committed grammar**.

The refusal is the same
version-lineage mechanism as section 3.2 and not a prohibition on crossing:
a seed whose base has been superseded in the receiver's past is offered no
proposal. Seeded from a shared actor, or from within the second sector, the
group is admitted and the division event is written.

**And every admitted one kills the dictionary.** Put beside section 6.2
that is the unit's seam answer, and it is two-sided:

> the seam's four undetermined entries are exactly what a cross-sector
> division event would fix -- one entry per cross link, measured 4, 3, 2,
> 1, 0 on the exact system -- and fixing them that way leaves the declared
> target BY THEOREM on the amalgam's link set: a cross-sector event in the
> pin's sense realises a pair between an A-only and a B-only actor, and the
> amalgam carries no link between an A-only and a B-only site, so the
> detector returns STRUCT-DEAD. Both admitted specifications do exactly
> that. Whether any structure OTHER than a cross-sector division event
> fixes those four entries is not decided here, and is registered open at
> S-3.

## 7. Stage 5 -- the forced-overlap question

| type | k | gluings | doubled | bare fate | welds |
|---|---|---|---|---|---|
| `(0,)` | 0 | 1 | 0 | FOUND-candidate | YES |
| `(1, (0, 0, 1))` | 1 | 81 | 0 | FOUND-candidate | YES |
| `(2, (0, 0, 1), (0, 1, 1))` | 2 | 486 | 0 | FOUND-candidate | YES |
| `(2, (0, 0, 1), (1, 0, 1))` | 2 | 486 | 0 | FOUND-candidate | YES |
| `(2, (0, 0, 1), (1, 1, 1))` | 2 | 1458 | 1 | UNMOTIVATED | no |
| `(2, (0, 0, 2))` | 2 | 162 | 0 | FOUND-candidate | YES |
| `(3, (0, 0, 1), (0, 1, 1), (0, 2, 1))` | 3 | 486 | 0 | FOUND-candidate | YES |
| `(3, (0, 0, 1), (0, 1, 1), (1, 0, 1))` | 3 | 11664 | 1 | UNMOTIVATED | no |
| `(3, (0, 0, 1), (0, 1, 1), (1, 2, 1))` | 3 | 8748 | 2 | UNMOTIVATED | no |
| `(3, (0, 0, 1), (0, 1, 2))` | 3 | 972 | 0 | FOUND-candidate | YES |
| `(3, (0, 0, 1), (1, 0, 1), (2, 0, 1))` | 3 | 486 | 0 | FOUND-candidate | YES |
| `(3, (0, 0, 1), (1, 0, 1), (2, 1, 1))` | 3 | 8748 | 2 | UNMOTIVATED | no |
| `(3, (0, 0, 1), (1, 0, 2))` | 3 | 972 | 0 | FOUND-candidate | YES |
| `(3, (0, 0, 1), (1, 1, 1), (2, 2, 1))` | 3 | 4374 | 3 | UNMOTIVATED | no |
| `(3, (0, 0, 1), (1, 1, 2))` | 3 | 5832 | 2 | UNMOTIVATED | no |
| `(3, (0, 0, 3))` | 3 | 54 | 0 | FOUND-candidate | YES |

**k is not what selects.** Every k in {2, 3} carries both a welding and a
non-welding type; k = 1 welds always, having no pair of shared actors to
double; k = 0 welds and means nothing, which is section 8. The pin
pre-registered `SEC-K-SELECTED` as one possible outcome and the measurement
declines it: what selects is the ALIGNMENT.

The declination is itself a measurement and not an assertion. Segment 4's
outcome word is typed by a COMPARATOR that states both rival hypotheses --
k selects, if the set of k carrying a welding type and the set carrying a
non-welding type are disjoint; the alignment selects, if a type welds
exactly when no shared pair is adjacent in both sectors -- and decides
between them from a weld column measured a second time through the real
detector at the real amalgam target. The same comparator is exercised on
four declared families measured through the same machinery: the real
family, the k <= 1 types together with the doubled types, the doubled types
alone, and the doubled-free types alone. It emits four DIFFERENT words, and
one of them is `SEC-K-SELECTED`. So a family exists on which this
instrument emits the pre-registered word; this one is not it.

> **THE ALIGNMENT CRITERION.** The union welds -- zero free items at the
> bare carrier -- if and only if no shared pair is adjacent in BOTH
> sectors; and adjacency in a sector is exactly "different tripartite
> classes". Equivalently: every shared pair must share a tripartite class
> on at least one side.

The criterion is EVALUATED per PAIR at every one of the 45010 gluings, on
the part profile that determines every pair's answer, and it is CHECKED
against a gluing's own realised relation at the 16 type representatives.
**the doubled-free gluings number 4186 of 45010**, by k:

| k | gluings | doubled-free |
|---|---|---|
| 0 | 1 | 1 |
| 1 | 81 | 81 |
| 2 | 2592 | 1134 |
| 3 | 42336 | 2970 |

At k = 3 the criterion has a closed reading: all three shared actors must
lie in one tripartite class on the A side or one on the B side. **at k = 3 the criterion is met by 2970 of the 42336 gluings**,
spread over five types, no one of which carries more than 972;
and the maximal case -- one full tripartite class aligned with one full
tripartite class, which is the pin's own lead -- is the type `(3, (0, 0,
3))`, carrying 54 gluings and the largest automorphism group in the family
at 62208.

## 8. Stage 6 -- the k = 0 sterility control

The pin makes this arm mandatory and calls it the unit's licence to speak.
R1's lesson is that disjoint copying cannot generate new structure, which
is the requirement quoted in section 1 and the only thing this arm takes
from it. This unit's k = 0 arm re-poses that lesson at a different object,
and
the numbers are measured object by object:

| quantity | k = 0 |
|---|---|
| carriers | 18 |
| realised pairs | 54 |
| pairs beyond the two sectors' own | 0 |
| doubled pairs | 0 |
| seam cells | 0 |
| shared actors | 0 |
| \|Aut\|, chain route | 3359232 |
| \|Aut\|, component-product route | 3359232 |
| the direct-sum prediction | 3359232 |
| dictionary | FOUND-candidate |

**at k = 0 the automorphism group is 3359232, which is |Aut(sector)|^2 times 2** -- the sector's own 1296 squared, times the
sector swap and nothing else, by two routes that share no code. There is no
pair the two sectors did not already carry, no doubled pair, no seam cell
and no shared actor, and the dictionary is FOUND and is the direct sum of
the two sectors' dictionaries. **This is a fresh sterility measurement at
SEC's own arena and consistent with R1's copying lesson: the disjoint arm
adds nothing.**

**It is an ANALOGY and not an instance, and the paper says so.** R1's
Theorem B is stated under an isomorphic-copying hypothesis -- a refinement
family with Sigma-stable blocks and an order-carrying intertwiner -- which
this arena does not instantiate: there is no refinement family, no nerve,
no atlas, no Sigma, no cyclic order and no m here. Its conclusions concern
ratios of quantities ADDITIVE over connected components; the quantity this
arm gates, |Aut|, is MULTIPLICATIVE, and |Aut(C + C)| = |Aut(C)|^2 x 2 is a
wreath-product fact independent of R1. The ten rows above account for
themselves. Six are forced by the empty gluing before any measurement --
carriers 18 = 9 + 9, realised pairs 54 = 27 + 27, and the four zeros. Three
more are that wreath-product theorem: the two |Aut| routes and the
direct-sum prediction they are compared against. Exactly one row is
genuinely contingent: the dictionary's fate, which required the amalgam
T(0) to weld, and it did. **On nine of its ten rows the it-can-fail arm
cannot fail**, which is the honest price of the control and is stated here
rather than left for a reader to compute.

**And the contrast is what licenses the rest of the paper.** Against that
sterile arm, the aligned k = 3 union has 15 carriers instead of 18, an
automorphism group of 62208 instead of 3359232, and a seam carrying 12 entries the
record does not fix; and the triangle-glued k = 3 union changes the count
field itself, from a field taking the single value 1 to one taking 1 and 2.
Shared participation does what
disjoint copying provably cannot.

## 9. The walls

**L-1.** The sentence L-1 retracted is not reproduced anywhere in this
paper, and the gate that enforces its absence whitespace-normalises,
ASCII-folds and strips markdown prefixes from both sides, so a line-wrapped
or blockquoted injection dies too.

**The seam resonance, NAMED.** A reader arriving from the relativity line
will hear something in an indefinite completion. The naming sentence in
section 6.1 is mandatory and a gate requires it to be present: silence is
how a resonance becomes governance.

**No sector narrative.** Sectors here are pinned arenas and nothing else.
No reading of the union as anything larger, and no new-places or growth
language, is taken or licensed by anything measured here -- and the
abstention is MEASURED on two
surfaces rather than declared: this run's whole measurement layer AND this
paper are scanned for the 14 terms whose presence would mean the reading
was taken, and none occurs in either. The receipt-side scan is taken TWICE:
once inside the run, and once at the writer over the COMPLETE payload,
because the first one can only see the keys that exist when it runs. The
provenance surface -- the source block, the read register and the three
gates whose evidence is that surface -- is excluded and named, because it
carries other units' file names, which are citations of their titles and
not readings taken here.

**BHS and Kleitman-Rothschild.** No dimension reading of any kind is taken,
so no height control is owed; and the catalog's

> no Lorentz-invariant finite-valency graph

clause is why no invariance test is run on these finite-valency records.

## 10. Every ratio here is COUNTING-ONLY

E-24 binds any unit that publishes a fraction over a configuration space.
**This unit publishes no probability and no percentage.** Every ratio in
this paper -- welding types of types, welding gluings of gluings,
doubled-free gluings of gluings by k, FORCED records of window records,
admitted cross-sector specifications of specifications -- is a COUNT over
an exhaustive enumeration with its denominator written beside it, and it
is stamped **COUNTING-ONLY** in the receipt. No measure is declared on the
gluing family, and nothing here may be read as a likelihood that a gluing
welds: the gluings are not equiprobable under anything this unit declares,
and the type populations differ by more than two orders of magnitude.

Each fraction names what it counts. "10 of 16" counts TYPES; "4186 of
45010" counts GLUINGS; "6 of 16" counts DRIVEN WINDOW RECORDS and never
types; "2970 of 42336" counts the gluings at k = 3; "26 of 26" counts
FORCED window records. Two of those denominators are different collections
and a ratio over one is not a ratio over the other.

## 11. Choice inventory

| # | item | class | fiber | where it binds |
|---|---|---|---|---|
| 1 | the base object: two CONFLICT-GRID(3, 3) | forced | 1 | paper-19's, driven |
| 2 | the saturating arrangement per sector | measured | 1 | section 2.4 |
| 3 | the site carrier: actors to the lattice | forced | 1 | the constructor's own naming |
| 4 | admissibility: the layer's own menu | forced | 1 | d42b1 driven directly |
| 5 | the I7 readout | forced | 1 | HA, matched verbatim |
| 6 | k, and the gluing | declared, the axis | every member run | section 2.1 |
| 7 | the seed rule | declared | 2 | both driven, section 3.2 |
| 8 | the link individuation | declared, VERDICT-DETERMINING for EXT-INCIDENCE and for the seam chart at a doubled site | 2 | sections 4.3 and 6.1 |
| 9 | the carrier | declared | 3 | section 2.3 |
| 10 | the reading | declared | 2 | weld 2's, every row stamped |
| 11 | the seam chart: the direct-sum chart | declared | 1 | section 6.1 |
| 12 | I-SITE-ASSIGNMENT | measured | 1 | section 3.1, two routes |
| 13 | I-DIRECTION-LABEL | measured | 1 or 9 | section 4.2 |
| 14 | I-ORIENT | measured | 1 or 4 | section 4.2 |
| 15 | the three cross-sector specifications | free | -- | this unit's; section 6.3 |
| 16 | the window's type representative | free, VERDICT-DETERMINING for the canonical-rule fate clause | -- | section 3.2; the abstract arena is a type invariant, section 5.2 |

Two INSTRUMENT-SIDE FREE CHOICES, and the phrase is not the RSQ inventory's
"free item". Item 15 chooses which cross-sector groups are attempted, and
both fates are exhibited. item 16 chooses which gluing of a type is driven:
the abstract arena is a function of the type and is gated as such, so item
16 is inert for every type column, but the canonical-rule fate is a
function of the gluing, so item 16 is VERDICT-DETERMINING for that one
clause of the head and is classed that way.

## 12. Verdict

```
SEC-ARENA-[TWO DRIVEN R=3 SATURATING SECTORS; 45010 GLUINGS IN 16 COMBINATORIAL TYPES OVER k IN {0,1,2,3}; UNION CARRIERS 15..18; FORCED 16 OF 16 DRIVEN WINDOW RECORDS AT THE SHARED-SEED RULE, AND THAT FATE IS A PROPERTY OF THE RULE (ALL 45010 GLUINGS CARRY THE BASE INTO THE SECOND SECTOR UNDER IT); REFUSED 6 OF 16 AT PAPER-19'S CANONICAL RULE, WHERE THE FATE IS A PROPERTY OF THE DRIVEN GLUING AND NOT OF ITS TYPE -- THE CANONICAL RULE SPLITS 10 OF THE 16 TYPES AND ONLY 8740 OF THE 45010 GLUINGS CARRY; DRIVEN=COMBINATORIAL AT 26 OF 26]@WINDOW-32=16-TYPE-TRANSVERSAL-x-2-SEED-RULES-OF-45010-GLUINGS
```

```
SEC-COMPOSES-[ACTOR->SITE|CO-DIVISION-ACTOR-PAIR->LINK|DIVISION-COUNT->n_l(x)]@EMBEDDING+QUOTIENT<THE-UNION-DICTIONARY-EXISTS-AT-EVERY-TYPE:STRUCT-ALIVE-16-OF-16(INHERITED:THE-UNION'S-REALISED-PAIR-SET-IS-THE-AMALGAM'S-INCIDENCE-SET),COUNT-POSITIVE-16-OF-16|MOTIVATED-AT-10-OF-16-TYPES(4186-OF-45010-GLUINGS)|SITE-FIBER=1-AT-16-OF-16-TWO-ROUTES:THE-GLUING-BREAKS-EDGE-TRANSITIVITY-SO-THE-SEAM-IS-VISIBLE-IN-THE-STRUCTURE-ALONE|I-SITE-ASSIGNMENT-NEVER-FREE(0-OF-6-DOUBLED-TYPES,A-TYPE-ROW);AT-A-DOUBLED-TYPE'S-DECLARED-REPRESENTATIVE-THE-FREE-ITEMS-ARE-I-DIRECTION-LABEL(9-AT-6-OF-6)-AND-I-ORIENT(4-AT-5-OF-6,1-AT-1-OF-6)-BOTH-REPRESENTATIVE-ROWS-NOT-TYPE-ROWS(THE-PAIR-MOVES-INSIDE-4-OF-16-TYPES-ON-THE-DECLARED-SAMPLE)|EXTENDED-CARRIERS:BOTH-RETURN-FOUND-STRUCTURAL-AT-16-OF-16-TYPES-AND-64-OF-64-ROWS,EXT-PAIR-UNDER-SIMPLE-AND-EXT-INCIDENCE-UNDER-CHARTED;THE-INDUCED-COUNT-FIELD-IS-CARRIER-INDEPENDENT-AND-UNCHANGED;THE-RSQ-INVENTORY-IS-READ-AT-BARE-ONLY-SO-NO-REPAIR-OF-THE-FREE-ITEM-IS-MEASURED>
```

```
SEC-SEAM-CURVATURE-[THE-DIRECT-SUM-IS-A-DECLARATION-NOT-A-MEASUREMENT: AT EVERY SHARED SITE THE SIX DECLARED LINKS GIVE RANK 6 ON THE 10 ENTRIES OF Sym^2(Q^4), KERNEL 4 -- BY THEOREM ON THE ROW SPACE, WHICH MENTIONS NO COUNT, AND MEASURED AT 39 OF 39 SHARED SITES; THE DIRECT SUM IS THE CHOICE C=0 AND IS POSITIVE DEFINITE, AND AN EXACT RATIONAL COMPLETION REPRODUCING ALL SIX COUNTS IS NEGATIVE ON AN EXHIBITED VECTOR] -- SEAM-CONFINED COMPOSITIONALITY: EVERY CELL AT WHICH THE UNION'S COUNT DIFFERS FROM THE OWNING SECTOR'S HAS BOTH ENDPOINTS SHARED, 22 OF 22 AT 16 OF 16 TYPES, AND 0 PAIRS OFF COUNT 1 TOUCH AN UNSHARED ACTOR ANYWHERE IN THE UNION ARENA -- NO SECTOR-PRIVATE LINK EVER MOVES (FIXED-ATTRIBUTION READING) -- CROSS-SECTOR DIVISION EVENTS ARE ADMITTED BY THE COMMITTED GRAMMAR (2 OF 3 SPECIFICATIONS DRIVEN, THE OTHERS REFUSED ON THE VERSION LINEAGE) AND EACH CROSS LINK REMOVES EXACTLY ONE UNDETERMINED ENTRY (4,3,2,1,0) -- EVERY CROSS-SECTOR EVENT THAT WOULD FIX A SEAM ENTRY LEAVES THE TARGET BY THEOREM ON THE AMALGAM'S LINK SET (2 OF 2 ADMITTED WITNESSES DIE AT STRUCTURE); WHETHER ANY OTHER COMMITTED STRUCTURE FIXES THEM IS OPEN AT S-3
```

```
SEC-OVERLAP-TYPE-SELECTED-NOT-k-SELECTED-[EVERY k IN {2,3} CARRIES BOTH A WELDING AND A NON-WELDING TYPE, k IN {0,1} ONLY WELDING TYPES AND k=1 HAS NO PAIR TO DOUBLE; THE CRITERION IS ALIGNMENT: THE UNION WELDS IFF NO SHARED PAIR IS ADJACENT IN BOTH SECTORS, i.e. IFF EVERY SHARED PAIR SHARES A TRIPARTITE CLASS ON AT LEAST ONE SIDE; AT k=3 THIS IS 'ALL THREE IN ONE CLASS ON ONE SIDE', 2970 OF 42336 GLUINGS; THE WORD IS TYPED BY A COMPARATOR THAT STATES BOTH HYPOTHESES AND IS EXERCISED ON 4 DECLARED CONTROL FAMILIES, ONE OF WHICH RETURNS SEC-K-SELECTED] -- STERILITY-CONTROL=k=0-CONSISTENT-WITH-R1'S-COPYING-LESSON-AT-A-DIFFERENT-OBJECT(NOT-AN-INSTANCE-OF-THEOREM-B): |Aut|=3359232=|Aut(SECTOR)|^2 x 2 BY TWO ROUTES, 0 NEW PAIRS, 0 DOUBLED, 0 SEAM CELLS, THE DICTIONARY THE DIRECT SUM OF THE TWO -- AGAINST k=3-ALIGNED WHERE |Aut|=62208 AND THE SEAM CARRIES 12 UNDETERMINED ENTRIES
```

Read out. **Two welded worlds glued by shared actors compose**: the union
carries a dictionary at every one of the 16 types, at both readings, and
**the union welds -- zero free items -- at 10 of them, which are 4186 of the 45010 gluings**; the two fractions count different collections and neither is
a likelihood. What decides is not how many actors are shared but
how they are aligned, and the criterion is a statement about tripartite
classes rather than about cardinality.

Three qualifications travel with it. The union's geometry is not the direct
sum of its sectors' -- but the difference is SEAM-CONFINED: at a doubled
gluing the union's count exceeds the owning sector's on exactly the links
both sectors realise, both of whose endpoints are shared actors, and no
cell touching an unshared actor moves at any type. The seam's own form is
not measured at all -- six of ten entries fixed, four free -- and the
direct sum is a declaration. And the one thing that would fix them, a
division event spanning the sectors, is admitted by the committed grammar
and destroys the dictionary when taken.

The sterility arm is what makes those statements mean anything. At k = 0
every measured invariant is the direct sum, exactly as R1's copying lesson
says, and the detector returns FOUND while carrying no new content at all.

## 13. Deviations, priced

1. **The driven window is 32 of 45010 gluings.** Price: the FORCED reading is
   exhaustive on the window, not on the family. Mitigation: the window
   contains a representative of every type at both seed rules, the window
   is named in the arena verdict string together with what it is, every
   other column is exhaustive over an object the window does not cap, the
   arena-is-a-function-of-the-type licence is gated on the types' own
   members, and the driven-versus-combinatorial equality that licenses
   those columns is measured on every FORCED record.
2. **The type is this unit's object, not the pin's.** The pin asks for the
   gluing's combinatorial type with its fiber; the census measures that the
   ABSTRACT union arena -- carriers, pairs, doubled pairs and automorphism
   order -- is a function of the type, and reports gluings-per-type as the
   fiber. The chart-borne quantities of sections 4.2 and 6.1 are NOT
   functions of the type, and are stamped as representative rows.
3. **`SEC-K-SELECTED` is declined, and the declination is comparator-derived.**
   The pin pre-registered it; the measurement says alignment selects and k
   does not; the head's word is emitted by a comparator that could have
   emitted the pre-registered one and does emit it on a declared control
   family. The head names the outcome `TYPE-SELECTED-NOT-k-SELECTED` rather
   than forcing the result into the pre-registered word.
4. **The extended carriers are a STRUCTURAL reading and say so in the
   row.** The RSQ inventory is a statement about the map to the target's
   cells, and those cells are the bare carrier's; the inventory and the map
   count are read on the BARE row and stamped `READ-AT-BARE` rather than
   duplicated with numbers that would be the same by construction. No
   repair of a free item is measured at an extended carrier, and the head
   claims none.
5. **The seam chart is declared, and at a doubled site it is CHARTED.**
   Reading the union's form at a shared site in the direct-sum chart is a
   declaration; a reader who identifies the two charts instead gets an
   over-determined system rather than an under-determined one. At a site
   incident to a doubled pair the direct-sum chart gives one union link two
   direction vectors, which is the CHARTED individuation of section 2.2.
   Both are declarations, neither is measured, and that is the content of
   section 6.1 rather than a gap in it.
6. **The cross-sector specifications are three, chosen by this unit.** They
   exhibit both fates and the dictionary consequence; no census of all
   cross-sector groups is run and none is claimed.
7. **The per-type fiber and seam rows are representative rows.** Price: the
   published (`I-DIRECTION-LABEL`, `I-ORIENT`) pair and the per-site seam
   profile are read at one gluing of the type and do not hold across the
   type's fiber. Mitigation: the fate, the site fiber and every census
   column are type invariants and are gated as such, the split is measured
   on a declared sample at every type, and every row is stamped on the side
   the measurement puts it.
8. **The shared-seed rule is this unit's, not paper-19's.** Paper-19's
   canonical rule refuses at 6 of the 16 driven window records; the shared-seed rule drives all 16. Price: the FORCED column is reported at
   a rule the parent does not declare, and the refusals are recorded and
   never patched. Mitigation: no geometric claim in this paper reads the
   seed rule -- the dictionary, type, seam, compatibility and alignment
   censuses are all computed from the schedule alone, and the driven and combinatorial routes agree at 26 of 26 FORCED window records.
9. **`SEC-COMPOSES` would have fired at one motivated type.** The
   comparator's threshold for that word is "the union dictionary exists and
   at least one type is motivated", which is the pin's own definition of
   the word. The bracket carries the actual count so a reader meets 10 of
   16 with the word.

## 14. The instrument

`v14/code/sec_exact.py`, with the #82 CLI contract: a delivery run that is
the only writer, `--no-write`, `--numbers`, `--selftest`, `--mutant NAME`,
`--break-anchor NAME`, `--verify-paper [PATH]`, `--list-gates` and
`--list-mutants`; every unknown flag, unknown flag argument, missing flag
argument and second mode flag exits 2. A mutant that dies at its declared
gate exits 0, so a wrapper reading shell truth reads a clean sweep as clean.

**15 sources are read at run time**, sha-pinned, and the read set is an
OBSERVATION rather than a declaration: every file this instrument opens
goes through one recorded accessor, which registers the path and its
category before the bytes are returned, and an AST scan of the file is a
gate that there are exactly two `open` call sites -- that accessor, and the
staging writer. The gate then compares the observed register against the
declared set category by category, so a stray read taken anywhere in the
file is visible to it and the cited-and-not-read abstention is a
measurement. No subprocess is invoked and no network or subprocess module
is imported, so the run is correct off-tree and with no version control
present. Arithmetic is exact end to end -- an AST scan of the file is a
gate. The driver is anchored against the object it generalizes: at d66's
own schedule it and d66's `conflict_grid(3, 4)`, re-run in this process,
emit identical event lists, and that record's profile reproduces d66's own
committed row, read from its pinned output at run time rather than
re-typed. The menu memo is gated, not trusted: four window members are
re-driven with the memo BYPASSED -- calling the committed function directly
-- and compared event for event.

Automorphism orders are computed by an orbit-stabilizer chain that returns
an order and generators without enumerating the group, and are checked
against two independent routes wherever those can afford the answer: plain
lexicographic enumeration under a declared cap, and the component product
on the disconnected arm. The site fiber is computed twice more, as the
index of the weight-stabilizer and as an orbit under the group's own
generators, and the two agree at every type.

Coverage is honest. Every gate is falsified by one of the 64 declared mutants, each
dying at its own named gate, or waived with a forcing that is TRUE of it --
the delivered object carried six waivers naming a mutant that died at an
earlier gate and never reached the gate it was said to falsify, and each of
those six now has a mutant of its own; five gates remain waived and each
states why an object corruption cannot reach it. The sweep's EXECUTION is
bound ON ITS OWN ROWS: the delivery mode requires one on-target row per
declared mutant, named as the registry names it, and every other mode
promotes nothing and claims no sweep, so a caller cannot hand the writer a
sweep it did not take. Every falsifier reaches its gate, and the gates
emitted after the coverage gate are named DECLARED-LATER and their presence
verified at the last gate.

The seal is total and it is VERIFIED. Every published receipt key is sealed
at the moment its gate passes or listed as DECLARED-UNSEALED with its
forcing; before a byte is written every sealed key is re-digested FROM THE
PAYLOAD and compared against the digest taken at gate time, so a value that
moved after its gate dies before the disk. The artifacts are then STAGED
beside their destinations, read back through the same recorded accessor and
compared, with a corruption probe that is a REAL corruption of a REAL
staged file and is required to be detected in the same run; only then are
they promoted through `os.replace`, and the promoted bytes are re-read and
compared again. A run that fails writes nothing, promotes nothing and
leaves no staging behind. The ledger and its chain head are closed together
at the writer, so the published chain is the head of the published rows.

The head's second segment is derived a second time by a comparator sharing
neither code nor input nor typed literal with the builder: it re-derives
that segment's outcome word from the receipt's own fate rows and re-checks
the seam kernel and the sterility identity. The head's FOURTH segment has a
comparator of its own, which types the two rival hypotheses itself and is
exercised on four declared control families through the real weld
machinery. Segments 1 and 3 are rendered from the receipt and gated
verbatim against this run's own verdict strings; their words are not
independently re-derived, and the paper says so rather than claiming four.

The paper under test is checked in the same run, and the paper leg BINDS.
**Every data row of every table above is a rendered claim of the receipt**
-- all of them, across every table, where the delivered object bound six
table families and left seven uncovered -- and **every table's column header
is a rendered claim too**, so a swapped cell and a swapped pair of column
labels both die.
Numeral coverage includes fenced blocks, inline code spans, a numeral
followed by a full stop and a numeral carrying a leading minus. Fenced
blocks are matched by MULTISET EQUALITY against this run's own verdict
strings whatever the info string carries, so a forged head hidden behind a
non-letter fence prefix is a stray. Claim polarity runs on every
direction-bearing sentence the head asserts rather than on three of them. The five block quotations
are verified ON THE QUOTE SIDE: each is a fragment shown verbatim inside
its pinned parent AND required in the paper, so an inverted quotation dies.
Spelled numerals are scanned against a GENERATED number-word grammar at
every magnitude, and the spelled numerals that carry a measurement are
rendered from the measured value. The numeral registry is built from the
measured layer only: no sha256, ledger-chain or seal-manifest token can
back a numeral in this paper.

## 15. The successor register

Registered, not claimed.

**S-1 -- the atlas with three or more sectors.** This unit glues two. The
alignment criterion is a condition on pairs of shared actors, so it has an
immediate reading at more sectors, and the question of whether alignment
can be satisfied pairwise but not globally is exactly the question a chart
atlas asks. Nothing here answers it.

**S-2 -- the cross-sector census.** Three specifications are driven. Which
cross-sector groups the committed grammar admits, as a census over the
version lineage rather than three witnesses, is cheap and open; and the
target that WOULD accommodate them -- a lattice with the cross links -- is
a declaration nobody has priced.

**S-3 -- the seam's four entries as a physical question.** The record fixes
six of ten. Whether any further committed structure fixes the other four,
or whether they are permanently a declaration, is the sharpest open
question this unit creates, and the head defers to this line rather than
closing it.

**S-4 -- the doubled link at other budgets.** At R = 3 a doubled seam link
carries count 2 because each sector spends one incidence on it. What the
seam does when the sectors run different budgets, and whether the alignment
criterion survives, is not run.

**S-5 -- the canonical-rule fate over the family.** The fate at paper-19's
canonical rule is a property of the driven gluing; a validated predicate
counts it at 8740 of 45010, but the drive itself is run at 38 records. A
census of driven fates over a declared sample per type is cheap and is not
run here.

**What may not be inherited**, as a standing row: the extended-carrier
structural pass as a repair rather than a declaration; the alignment
criterion at any other generator; the type census as a statement about
gluings rather than about types; the chart-borne fibers and seam profiles
as statements about types; the canonical-rule refusal count as a statement
about types; the seam's positive-definite direct sum as a measurement; and
any reading of a sector as anything but a pinned arena.
