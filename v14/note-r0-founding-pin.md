# v14 R0 — THE FOUNDING PIN

**Frozen:** 2026-08-09, v14 ledger #1.  **Charter:** `v14/PLAN.md`
(recorded at v13 #315, user-ordered).  **Predecessor:** v13, CLOSED AND
FROZEN at its ledger #320 (commit 4ac4f76); synthesis at
`v13/note-v13-terminal-synthesis.md`.  **Discipline:** RUNBOOK
§13/§14/§15 with every addendum binds every v14 unit; papers are
numbered (`v14/paper-NN-<slug>.md`, NN assigned at pin time); v14
assumes exactly what this pin lists and nothing else.

## 1. The inheritance, hash-pinned

Every item below is citable in v14 only through these artifacts; a v14
unit that uses one MUST verify the sha256-12 at run time (the external
anchor mechanism).

| # | inherited result | carrying artifact | sha256-12 |
|---|---|---|---|
| I1 | The master equation I−E = α⁻¹ρα; the three walls as readings of ρ (fixed-point / order / permutation-module); module obstruction independent (35/35) | `v13/code/rsq_reposed_square_receipt.json` | `85f3cf809544` |
| I2 | THE SPECTRAL CARRY: eigenvalue 1 at 112/112 readout rows ⟹ 0 ∈ spec(I−E) on no unit circle — the bridge obstruction dimension-independent, in proof form; the wall UNIVERSAL-BY-THEOREM-AT-EVERY-PRIME-GE-5 (2,880 integer witnesses, gcds {1,2}); the criterion an iff at measured scope (all 64 diagonal patterns, p≡1 mod 3 caveat); the motivated family: RSQ-NO-COMPATIBLE-SQUARE | same receipt (+ output `a5266012ebd3`, paper `07bea42728a2`, code `8c7705f55fa6`) | `85f3cf809544` |
| I3 | The topology base: K₃₆ ⟹ contractible nerve (no simplicial topology at this scale); **TRIVIAL H₁ by the ordered measurement** (b₁ 140→0 under coherent cross-read-time digons); b₂/N_coh carry the identification data; the dimension estimator EXTENSIVE (excluded as intensive candidate); P* an arena datum (locus P*-relative, transposition class) | `v13/code/top_topology_receipt.json` (+ output `109302d0d036`, paper `4e4cd4f11bab`, code `81d07ffebd82`) | `65bb1fc5231f` |
| I4 | The successor requirements: R2-TOP's five (non-complete overlap graph; declared refinement family; pre-registered INTENSIVE invariant; the deciding measurement settled — now settled by I3; N_coh primary) and R2-RSQ's S-1..S-5 (headlined: drop thresholds, carry obstructions spectrally) | `v13/note-top-adjudication.md` / `v13/note-rsq-adjudication.md` | `e4934f2525b0` / `31b70406c6e8` |
| I5 | The measured scale table, FACTS ONLY: the prime DECLARED@9 / DERIVED-[5]@16 labels, arena-coupled, growth inference refused by LCB; the 43-label control at rung 2; torsion at 3 wings a group-level fact.  **The scale-convergence thesis is retired (v13 #314); no v14 document may re-assert it.** | `v13/code/lcb_livecell_receipt.json` | `3e502f685ab3` |
| I6 | The group tower: THE LADDER 1<A₄<GL(3,2)<A₆<A₇ by set equality; the ceiling theorem (15,120 attained); ×7=[A₇:A₆] with the embedding constructed; the defect law D=(ΣVᵀΣV)⊗I₉ (ψ-independent); THE ONE LAW D(ψ)=[P,u(ψ)] with the 1-cocycle mechanism | `v13/code/tb3_third_base_receipt.json` / `v13/code/gen_generality_receipt.json` / `v13/code/psi_curvature_receipt.json` | `c9bc956fe751` / `e0b2f444f6a9` / `7c7b91a9257e` |
| I7 | Gravity's record layer: H_a[N] record-native; diagonal-sector exact closure from counting; record-IS-metric (invertible linear re-encoding); G29 (R_HH = translation by ρ mod p) verified at 7/7 primes and verbatim at d=3 | `v13/code/ha_successor_receipt.json` | `542b8735daf0` |

The charter itself is part of the base: `v14/PLAN.md`, whose hash is
recorded in this pin's commit.  Anything of v13 not listed here is
HISTORY, not base — usable only by adding it to a unit's own pin with
its own hash and a stated reason.

## 2. R1 — THE CONTINUUM RUNG, posed

**Question (falsifiable, two-sided):** does a declared refinement
family over the v13 substrate admit a pre-registered **intensive**
invariant that stabilizes under refinement?

Binding structure for the R1 pin (to be frozen as its own document):

- **The family must be DECLARED, not discovered**: a sequence of
  arenas with declared maps between them, starting from the measured
  line the corpus already owns (the 9- and 16-label arenas of I5, the
  43-label grown arena of I2's control, the 3-wing base of I6), with
  the growth rule stated as data (§15).
- **Intensive candidates must be pre-registered before any is
  computed** (the I3 estimator is excluded as provably extensive).
  Candidate classes from the inheritance: N_coh density, spectral data
  of I−E along the family (I2 guarantees the obstruction persists —
  the question is what else the spectrum does), per-volume dimension
  profiles, overlap-graph completeness fraction (I3: locality exists
  iff this leaves 1).
- **Both verdicts first-class**: STABILIZES-AT-<invariant> and
  NO-CONTINUUM-LIMIT-AT-THIS-SUBSTRATE are equally publishable
  outcomes; the pin must carry gates for both.
- **The walls ride along spectrally** (I2), never re-censused; any
  bridge-adjacent claim cites I1/I2 as pinned anchors.
- **Paper number: `paper-01`** — assigned here per the numbering
  convention; the R1 pin's deliverable set is
  `v14/paper-01-<slug>.md` + `v14/code/<slug>_exact.py` +
  `_output.txt` + `_receipt.json`.

## 3. What R0 does not do

No physics, no census, no code.  This pin exists so that every later
claim has a floor: the base is these seven rows, the charter, and the
engraved discipline — nothing else.
