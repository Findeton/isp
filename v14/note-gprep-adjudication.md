# Γ-PREP — JOINT ADJUDICATION (operator #90, effectus #88, instrument #88)

**Inputs:** the three frozen reviews (operator `08b5f1f8ddd9`,
effectus `b1a29e33ad2a`, instrument `aa9b0362fbed`); the frozen
delivery (paper `09482eb080cc`, code `9a4f0529b840`, output
`097c08a0229d`, receipt `dd86ad1a80d7`, commit 0f5d57e); pin
`42ce06e6be8a`; protocol `b00a0be5c1ce`.  **Standing:** three
ACCEPT-WITH-FIXES; 164+508+670 = 1,342 seat recomputations;
**zero false numbers in the delivered artifacts** (128 paper
tokens clean; 553 machine-counted exact comparisons; in-tree
byte-identity ×2 by two seats).  Resolved grade:
**ACCEPT-WITH-FIXES**.

## 1. THE CORRECTED SETTLEMENT

**The mechanism stands, strengthened beyond the delivery.**  The
monotone-holdings ladder holds at 8× the delivered coverage (ALL
243,768 transitions: 0 profile decreases, 0 holdings-shrinking —
operator); a FIFTH holdings-profile block (3,3), 424 points at
depth 6, undeclared and untested in the delivery, CONFIRMS the
ladder when measured (N=1 hits infimum 0; 30,545/30,729 zeros);
and the coarsening lemma (δ* monotone under coarsening — derived
independently by BOTH content seats) proves no pinned abstraction
unblocks it: all six T5 abstractions are finer than the primary
grain, hence strictly worse.

**The head is corrected, two ways.**  (i) GRAIN-QUALIFIED
(operator MAJOR-4): at Ψ = the R-SIG indicator the same class has
δ* = 1 on the same widest windows (P¹(x,R-SIG) = 3/4 at every
R-SIG point) — BLOCKED is Ψ-family-relative and must carry the
qualifier the FOUND verdict already carries.  (ii) SPLIT
(effectus): the LADDER is a corpus theorem (A4-MONO); the
BLOCKING is a construction fact — the small-set family was
declared as the level sets of the very monotone coordinate, and
Arm B carries FOUR free items, failing the RSQ zero-free
standard.  Corrected head shape:
`ARM-B-BLOCKED-AT-THE-MONOTONE-HOLDINGS-LADDER<GRAIN=…;
PSI-FAMILY=MENU-SHAPE-ONLY;FREE-ITEMS=4;LADDER=CORPUS-THEOREM;
BLOCKING=CONSTRUCTION-FACT>`.  Arm A stands motivated; the seven
facts verified at zero mismatches.

**The carrier fact (decisive for the Γ-iteration).**  At
CONG-185 — the ruled carrier — 4 of the 6 delivered atom rows
have δ* = 0: **the atoms' live content is the (1,1) block only**
(effectus MAJOR-2; chain CONG-185 ⊑ MENU-113 ⊑ KIND-13 verified,
0 exceptions/3,969).  Three of four delivered blocks are not
statable on the d≤4 window; d74's committed d≤5 row (265/462)
must join the T5 supply.  The Γ-iteration pin inherits: the
(1,1)-block atoms as the live [B3] content, the d≤5 row, and the
fifth block.

## 2. The instrument findings

CLI-CONTRACT-FAILED 0/4 — the registered #66 disease with two
aggravations (`--no-writ` typo WRITES; exit-1-on-dead-falsifier
still writes) and under-reporting registries (40/44, 112/118).
The verdict comparator IS its builder (all three #82 axes; the
headline-flip injection passed V-VERDICT).  The moving-HEAD read
(`git show HEAD:v10/LOG.md`): unanchored, product read by no
gate, "LEDGER #None" delivers 44/44 PASS at exit 0 (both seats
independently), **and the committed byte-identity does not
reproduce off-tree**.  B0-SUCCESSOR-NAMED — the consumer
licensing all of ARM B — is not among the 44 gates.  Honest
denominators: 82/189 receipt keys falsifier-free; 8/18 compliance
rows typed True (both content seats independently); the waiver
loop closes on the author's own KIND_THM declaration.  Post-gate
receipt corruption survives (INJ-3); a 41-char verbatim anchor
truncated to "the " passes (INJ-6).  Flat-receipt consumer traps
documented (Fractions as strings; _gates a list; samples
truncated; no code sha) — the R-GM-9 resolution, relayed to the
Γ-main repair in-flight (B2_best_delta = STRING "1").

## 3. Binding repair orders (R-GP-1 … R-GP-12)

1. **The head recomposed** per §1: grain qualifier; the
   theorem/construction split; FREE-ITEMS=4 printed with fibers;
   the Ψ=R-SIG-indicator δ*=1 counter-reading printed as the
   FOUND-reachability witness.
2. **The fifth block declared and measured unit-side** (424
   points, depth 6); the four false universals corrected; the
   ladder census five rungs.
3. **Full-coverage monotonicity unit-side** (all 243,768; the
   12.6% scope retired; dependent sentences re-anchored).
4. **The carrier section added**: the CONG-185 collapse measured
   unit-side (4/6 rows δ*→0; the (1,1) block live; the chain
   verified in-unit); the d≤5 265/462 row added to the T5
   supply; per-block statability stamped.
5. **The real CLI** at the #82 contract (discharging the
   registered #66 item): argv whitelist, unknown flags exit 2
   (killing the --no-writ hazard); --selftest
   corrupt-one-anchor/exit-1/WRITES NOTHING; --mutant NAME;
   every failure path writes nothing; registries complete
   (44/44, 118/118).
6. **V-VERDICT rebuilt independent** (#82: no shared code,
   inputs, or typed literals); the field-swap and headline-flip
   injections become dead mutants.
7. **The moving-HEAD read killed**: pinned-sha read with the
   product GATED, or the ledger number moved to declared data;
   the plain run byte-reproduces off-tree and git-less.
8. **B0-SUCCESSOR-NAMED enters the gate registry**; all verbatim
   anchors re-audited at the full #62 spec (quote fidelity over
   meaningful spans — the "the " truncation dies;
   M-VERBATIMDRIFT made honest).
9. **The compliance sweep computed** (8/18 typed rows replaced
   by predicates with injection-falsifiers; the #46 row made
   true by order 7; the #219 row made true by order 6).
10. **Coverage honesty**: the 82 falsifier-free keys addressed
    (#34 — falsifiers or reclassification); the waiver loop
    classifies independently of the author's KIND_THM tag (the
    objects-not-cardinalities engraving); the two typed birkhoff
    literals computed or reclassified-with-anchors; the Birkhoff
    witness zeros assigned by a computed route (no always-firing
    else); the Birkhoff/Hilbert quotient-scope disclosure added;
    the theorem-pass census recomputed mechanically with
    per-gate two-way demonstrations (B1-TREE, A4-PRED,
    B3-MONOTONE classified with forcing exhibited; the
    operator's 38/5/1 the reference).
11. **The receipt upgraded**: per-block δ* published; value
    typing declared (or typed emission); _gates addressable;
    samples untruncated or the truncation stated; the code's own
    sha emitted; a final artifact-integrity gate re-reads what
    was written (killing INJ-3's class).
12. **Paper rewrite single-threaded**: A1-KINDS five (the label
    inherited my owned pin error); "eight times" → 6.96×;
    A4-PORTS set-identity gated; the output-sentence/table
    contradiction fixed; typed #494/#492 computed; 3/4
    block-re-entry rows printed with definitions and
    reclassified; the closing successor register incorporating
    the effectus's 11 items, the fifth block, the
    (1,1)-block-at-CONG-185 fact, and the licensed-claim
    sentence.

## 4. RUNBOOK engraving (this adjudication)

- **§13/#46 addendum (no moving refs):** delivery code reads the
  repository only at pinned shas; every such read's product is
  consumed by a gate; the plain run must byte-reproduce off-tree
  and git-less.  Demonstrations: Γ-prep's `t5_ledger_number`
  ("LEDGER #None" at 44/44 PASS, found independently by two
  seats; the off-tree hash break).
