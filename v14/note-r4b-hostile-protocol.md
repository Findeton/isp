# R4B (paper-15, momentum) — HOSTILE REVIEW PROTOCOL (FROZEN)

**Frozen:** 2026-08-10, v14 ledger #111.  Three reviewers,
independent, single-file writes, scratch-only, no imports,
read-only git; all mutant/injection work on scratch copies (the
plain run WRITES artifacts).  Grades A/AWF/R; recomputations
counted; findings ranked with exact repairs.

**Object (committed as-is, 10c8d17):** paper
v14/paper-15-momentum.md (`5f8ec142319c`), code
v14/code/r4b_momentum_exact.py (`53a10e87ec19`), output
(`d33412225949`), receipt (`00e7f6ea0f90`).  Pin
`bcd12bbe6fd8` (v14/note-r4b-momentum-pin.md).  Parent: the R4
terminal (#103, commit 583cae7; paper `1063401c7bb5`).
Reviews: `v14/review-r4b-{operator,effectus,instrument}.md`.
Candidate-reading rule in force on every delivered headline.

- **K1 THE CENSUS:** rebuild the 64-generator family and the
  928-cell eigenphase census independently (different
  diagonalization route); verify the μ₈ claim (every eigenvalue
  an 8th root — the Kronecker argument's finite legs); the
  Bloch-diagonalization scope (the 3 NOT-BLOCH-DIAGONAL classes
  measured, not assumed — verify all 6 controls); parity
  invariance 58/58; the 58-distinct-reduced-profiles vs
  14-labels separation.
- **K2 THE MOTION CENSUS (decisive):** MOVING 57/58 with C004
  the identity (verify matrix = I up to global phase; the
  two-way control); per-class v_max (the v=1 and v=2 lists);
  the aliased-cells accounting (320/1856 in exactly the 19
  v_max=2-adjacent families); **THE CONVENTION-SELECTION
  CLAIM:** of 9 antipodal-tie readings exactly one makes
  drift = winding at 58/58 (best other 39/58) AND reproduces
  the frozen R4-effectus drift table exactly — verify both
  halves against the frozen review file; is
  "selected-not-declared" the honest classification of that
  fiber-9 choice, and what does §15 require the SCOPE segment
  to carry?
- **K3 THE BOUND:** VMAX=2 = the max-norm diameter; NO-CONTENT
  two ways; the 8/14/36 overshoot/below/saturate partition
  (the "false as a reach bound at 14/58" claim — verify the
  per-family reaches); the inherited ceilings
  (SEPARATIONS=16-OF-16, MAX-DEFECT-RADIUS=2-OF-2) carried with
  their disclosure; the propagator relation as delivered
  (one interior radius; the dual-side 320/1856) — measured
  relation or renamed register row?
- **K4 THE CANCELLATION CLAIM:** "charge-without-momentum is
  cancellation, not absence" — all 42 interfering families MOVE
  with zero net transport in BOTH spaces; the 12
  nonzero-winding families all monomial; the Markov binding
  (1792 = 64²−48²).  Rule the licensed sentence at citable
  scope; kill over-readings (does "cancellation" import an
  interference ontology the arena doesn't measure?); what does
  this hand the R4c-multi (Fock/statistics) successor?
- **K5 INSTRUMENT** at the FULL era (this unit claims era-level
  born-compliance — test it with the weld-2 twelve-injection
  playbook): the 67 gates vs 66 mutants at #34 honest
  denominators; the 3 registered forcings genuinely forced; the
  head derived twice — truly independent (no shared
  code/inputs/typed literals, #82); per-object gates (#87 —
  each of the 58 families bound to its own dispersion); #91
  off-tree/git-less re-tested; post-gate artifact corruption
  (the integrity re-read); verify-paper coverage over every
  numeral; verbatim anchors at #62; paper↔output↔receipt sweep;
  byte-identity; repo hashes unchanged after all work
  (concurrent workers exist — list and disclaim their files).
