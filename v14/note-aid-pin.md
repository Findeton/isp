# AID (paper-33) — ARE ACTORS DERIVED? THE IDENTITY-RECONSTRUCTION CENSUS — PIN (FROZEN)

**Frozen:** 2026-08-11, v14 ledger #268.  **Authority:** the
user's order ("investigate the AID proposal") on the
orchestrator's design.  **The question's ancestry:** the
corpus definition "an actor is an identity that recurs in the
record"; the Barandes-orthodox observation that a subsystem
decomposition is a factorization choice unless the law forces
it; v13's relabelling-independence machinery (the
adversarial-pair precedents — cite the instruments, do not
re-run); LOR's delivered carrier lesson (structural
isomorphism ≠ the dictionary).  **Parents (terminal):**
paper-19 at 559e162 (the committed driven records; the weld),
paper-21 at #232 (the 600 driven schedules; G-R6-DOOR's 72
triples and their 5,184 ordered concatenations).  **Unit:**
paper-33-aid; code v14/code/aid_exact.py (+ output +
receipt).

## THE QUESTION

Is the actor LABELING forced by participation structure — are
actors real threads, or a chart?

1. **THE FORMAL OBJECT:** a committed history = a sequence of
   division events, each an actor-subset.  The de-labeled
   history = its isomorphism class under S₉ relabelling.  THE
   STABILIZER of a history = the subgroup of S₉ preserving
   the event sequence.  Identity is FORCED on a history iff
   its stabilizer is trivial; the FIBER = the stabilizer's
   structure (order, orbits).
2. **THE STABILIZER CENSUS:** over the committed corpora —
   the 72 R=3 triples (short histories); the 5,184 R=6
   concatenations; a declared window of paper-21's 600 driven
   schedules — the stabilizer computed EXACTLY per history
   (order, orbit partition, generators' cycle types).
3. **THE CRYSTALLIZATION TIME:** per history, the earliest
   prefix length at which the stabilizer becomes trivial
   (identity "crystallizes") — the distribution over each
   corpus; histories that NEVER crystallize censused and
   characterized.
4. **THE PHYSICS-INVARIANCE CENSUS (the deep half):** where
   stabilizers are nontrivial — is any COMMITTED observable
   sensitive to the un-forced part of identity?  The record
   n_ℓ(x), the weld dictionary's readings, and the walk's
   Born menu evaluated under stabilizer elements: invariant ⟹
   the physics never needed the full labeling (identity
   partially CHART); any non-invariance ⟹ the observable
   NAMES threads the structure does not force (a finding
   either way, exact).
5. **THE GRAIN NOTE:** global relabelling only (the
   stabilizer grain); mid-history thread-swaps (the groupoid
   grain) are REGISTERED as the successor question, not run —
   scoped honestly.

## OUTCOMES (pre-registered)

`AID-IDENTITY-FORCED-<crystallization data>` /
`AID-IDENTITY-CHART-<the stabilizer census; the invariance
verdict>` / `AID-STRATIFIED-<by history class>` /
`AID-BLOCKED-AT-<object>`.

## WALLS

The reality language confined to the measured form ("forced /
chart / stratified" — no metaphysical assertion beyond the
stabilizer and invariance data, description-stamped);
E-22 + E-23 + E-24 in full; **the #267 template-disease
checklist in the closing battery** (paper-scanning walls;
all tables rendered; no typed counts; full-transcript
integrity; digest-free whitelists; spelled numerals).

## STANDARDS

Full era per HANDOFF-PROMPT.md §4; exact arithmetic
(stabilizers by explicit group computation, never sampled
without a declared window); windows declared in-string and
licensed; all seals at value-close; read-back before
replace; derived head, de-twinned comparators,
REFERENT-BOUND gates (the OCC lesson: names bound to the
objects measured); #91 off-tree; failing runs write nothing
and promote nothing; byte ×2.  Candidate readings until
adjudication.
