# v14 R3 — THE RELATIVITY RUNG (pin)

**Frozen:** 2026-08-09, v14 ledger #23.  **Base:** the R0 founding pin
(+ #4 erratum), the R2 adjudication's handoff ruling (#20, terminal at
#22), and I7 — nothing else.  **Paper number:** `paper-03` —
deliverables `v14/paper-03-relativity-rung.md`,
`v14/code/r3_relativity_exact.py`, `_output.txt`, `_receipt.json`.
**Discipline:** RUNBOOK §13/§14/§15 with every addendum, including all
five 2026-08-09 engravings (containment-is-not-equality;
render-from-the-gated-object; prose-renders-from-the-receipt;
compliance-claims-are-gate-claims; path-value anchoring — every
compliance gate ships its injection-falsifier at birth).  Exact
arithmetic only.

## 1. The arena (data, §15 — the R2 handoff)

**I7's own declared lattice** — the record layer's sites, links
(`links_d2`/`links_d3`), chart group, and `lapse_family`, rebuilt from
the pinned HA receipt (R0 row I7, `542b8735daf0`) with path-value
anchors for every read.  Both declared dimensions d=2 and d=3.
**L ≥ 4 is GATED** (R2's measured requirement: the d=2, L=3 lattice
fails the locality criterion at 9 sites); L=3 is excluded with the
measured reason printed.  The lattice's locality, consistent
chart-intrinsic dimension, single per-site link profile, and
translation covariance are INHERITED FACTS (the R2 terminal receipt is
the anchor) — re-confirmed as anchors, never re-derived.

## 2. The question (falsifiable, two-sided)

**Does the record-native constraint family H_a[N] close as an algebra
over the declared lapse family — and in what form?**  The
hypersurface-deformation signature to test for: commutators of
constraints at different lapses land back in the constraint family
with coefficients that are READINGS OF THE RECORD METRIC
(record-IS-metric composed) — structure *functions*, not constants.

Verdicts, both first-class, derived in-gate with flip mutants:

- `R3-DEFORMATION-CLOSES-<computed form>` — naming the closure form
  (which generator basis, which coefficient class: constant /
  metric-reading / other), its measured scope (d, L, lapse family);
- `R3-DEFORMATION-DEFECT-AT-<computed>` — **a nonzero defect is a
  measured object, not a failure**: characterized exactly (its
  generator decomposition, its L- and d-dependence, whether it is a
  boundary term, whether it vanishes on any declared sector).  The
  v12 precedent binds the design: the composition defect WAS the
  physics.  If the algebra does not close, the defect's structure is
  the deliverable.

## 3. Registered measurements (fixed here)

1. **The closure census**: for every ordered pair (N, M) in the
   declared lapse family, the commutator [H_a[N], H_b[M]] computed
   exactly and decomposed in the declared generator basis (the
   constraint family + the lattice's translation generators + a
   residual channel).  The residual measured exactly, cell-complete.
2. **Structure-coefficient extraction**: where the commutator lands in
   the family, the coefficients extracted and tested against
   record-metric readings (via I7's record-IS-metric re-encoding) —
   the HDA-signature test: constant vs metric-dependent, measured.
3. **The diagonal-sector anchor**: I7's exact diagonal closure
   reproduced in-unit as a positive control (anchored to the pinned
   receipt's values); the unit's machinery must recover it before any
   new measurement counts.
4. **The translation control**: the lattice's own covariance group
   must close exactly (positive control for the commutator
   machinery); a scrambled-lattice negative control must break
   closure (measured, not assumed).
5. **The L-sweep**: everything at L = 4 and 5 minimum (both d where
   compute permits); any cap printed and gated (no silent caps);
   L-dependence of defects/coefficients recorded — the first
   deformation data along a refinement direction.
6. **The lapse-family scope**: the declared `lapse_family` plus its
   lattice translates; any enlargement is a new declaration (printed
   as arena data).  The lapse family IS a named verdict coordinate
   (the R1/R2 atlas lesson applied at birth).

## 4. Controls, falsifiers, discipline (minimum set)

Mutants: commutator-machinery corruption (the translation control must
flip); decomposition-basis drop (residual misattribution dies on the
cell-complete census); coefficient-typing corruption (metric-reading
vs constant conflation dies); diagonal-anchor drift; lapse-family
drop; L-gate violation (an L=3 run must die by gate, not silently
run); verdict name↔value and segment classes (the five R1 classes at
birth, each with its injection-falsifier); path-drift per anchor row.
The verdict comparator is an independent reconstruction from the
receipt payload.  The never-falsified census ships in the receipt with
an honest denominator from delivery one.  Prose numbers render from
the receipt.

## 5. Scope honesty

This unit measures the algebra at finite L on I7's declared lattice.
It claims nothing about a continuum limit (that door is closed until a
non-copying refinement exists — R1's terminal), nothing about
manifoldhood (R2's terminal holds the standards), and nothing about
the walls (I1/I2 ride as anchors).  If the deformation algebra closes
with metric-reading coefficients at measured scope, that is the
discrete hypersurface-deformation signature ON THE RECORD LAYER — the
first relativity-shaped statement of the programme.  If it closes with
constant coefficients, the result is a *rigid* algebra (named as
such — a different, weaker geometry).  If it does not close, the
defect is the finding.  All three outcomes are terminal-grade.
