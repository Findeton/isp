# R2 — HOSTILE REVIEW, REVIEWER R2 (EFFECTUS / STRUCTURAL LENS)

**Reviewer:** the effectus/structural lens — the MEANING audit.
**Protocol:** `v14/note-r2-hostile-protocol.md` (`3768846aebe9`, FROZEN).
**Pin:** `v14/note-r2-manifold-pin.md` (`76d42dfbc900`).
**Base:** `v14/note-r0-founding-pin.md` (`e9d2bedff244`) with the LOG #4
companion-hash erratum; the R1 joint adjudication (`4115dcd83cfa`); the RSQ
adjudication precedent (`v13/note-rsq-adjudication.md`).
**Object (verified before and after this run, unchanged):**

| artifact | sha256-12 |
|---|---|
| `v14/paper-02-manifold-rung.md` | `cc78e9373bbe` |
| `v14/code/r2_manifold_exact.py` | `36a10324b93f` |
| `v14/code/r2_manifold_output.txt` | `cb8493a13c39` |
| `v14/code/r2_manifold_receipt.json` | `7b128499b246` |

**Recomputation: 3,503 distinct delivered-or-derived quantities rebuilt from
scratch, plus seven exhaustive sweeps totalling 4,247,080 further
computations.** Nothing was imported from the unit's code: the grid, the
cells, the drawn relation, the complexes, every Betti number, every density
and every standards profile were rebuilt in scratch from the paper's
declarations and from the hash-pinned I6/I7 receipts alone.
**Numerical disagreements with the frozen delivery: zero. False numbers: zero.
False theorems: zero.**

**GRADE: ACCEPT-WITH-FIXES.**

---

## 1. Resolved position

Every number in this delivery is right. I reproduced all 109 grid coordinates
in the delivered order, all 545 census coordinates, all 80 chart standards
profiles, all 109×2 block-constant rows, all 14 b₁ rows, the 28 UNDEFINED
rows, the 5 REFUSES, the 40,320-action R2-A sweep and the closed
positive/scramble/parity controls — **zero discrepancies at any coordinate.**
Theorem R2-A, the partition corollary and the coherence corollary are all
true as stated and correctly proved. The grid is exactly what §2.4 says it
is. The coherence corollary is already correctly demoted to a disclosure.
The instrument is sound and the paper's §10 non-claims are unusually honest.

What the unit did **not** earn is what its verdict **says**, and the gap is
this era's characteristic one. Six measurements decide it, all mine, all
taken in scratch:

1. **Nothing in the inheritance motivates the cover.** Every orbit family of
   every subgroup — Σ, ⟨γ,Σ⟩, the ladder, GL(3,2), A₄/A₆/A₇, the seven
   declared words — is a partition, and **0 of 4,140 set partitions of the
   eight labels produces a non-complete component at either transport**
   (8,280 measurements). I6's *own declared* partial-overlap cover, the
   **seven Fano lines** (I6 receipt: `it_permutes_the_seven_Fano_lines`,
   `the_non_zero_labels_of_F2_cubed = [1..7]`), returns **clique-only at both
   transports** — because it is a 2-design and covers all 21 pairs. So does
   the γ-generated Fano plane. The one motivated cover the corpus owns is
   precisely a locality-*destroyer*.
2. **The census is a coordinate of I6's numbering.** Over all 40,320
   relabellings of the carrier (1,209,600 exact censuses) the locality count
   ranges **11 to 16** and **11 distinct locality-bearing rule sets** are
   realised. The delivered 14 is the *modal* value (34.29%) — the unit is
   **not** cherry-picking — but `RULES=14-OF-109:<list>`, the verdict's head
   segment, is numbering-relative, and two rules (R069, R094) that are local
   at 77.1% of their own coset orderings are non-local at the declared one.
3. **The census is derivable from the declaration without building an atlas.**
   Closed form, verified at **109 of 109**: with R the regular orbit, k the
   coset count and D the maximum cyclic coset-distance inside R,
   ALL/BLOCKWISE/orbit rules are forced complete-or-REFUSES, and a SLIDING
   rule is non-complete **iff c ≤ D**.
4. **`DIMREAD=INCONSISTENT` is forced, not measured.** Under the unit's own
   reading (which compares a *cell-indexed* dimprofile vector), CONSISTENT
   and NON-COMPLETE are **mutually exclusive** — proved below and verified
   over 19,948 cover families with 12,928 non-complete components and **zero**
   consistent readings. The 14-of-14 is a theorem about the reading.
5. **The drawing relation is verdict-determining and unnamed.** The unit
   *measured* it (`G-ALT-DRAW-PROBE`: ⟨γ,Σ⟩ as drawing group draws 0 pairs at
   both transports, so the head flips to `R2-NO-LOCALITY-IN-THE-DECLARED-GRID`)
   and left it in §7.5 prose. This is the R1 atlas finding recurring at a new
   coordinate — a MAJOR by the #313 recurrence rule.
6. **A consistent, chart-independent locality is reachable — and the corpus
   already owns one.** I7's record layer declares a site lattice with a
   declared link set and a chart group (`links_d2`, `links_d3`,
   `chart_group`, `lapse_family`). Its link-star cover **satisfies R2's own
   criterion** (non-complete at d=2/L≥4 and d=3/L≥3), is translation-covariant,
   and returns a **CONSISTENT** chart-intrinsic reading with a **single link
   profile at every site** — (8,12,1,5) at d=2, (24,96,1,73) at d=3. The
   unit's closing sentence, "Everything measured here says width-based overlap
   will not do it", is refuted constructively.

Under the RSQ precedent (a FOUND half demoted by a computed qualifier naming
the class it rests on) and the R1 precedent (a verdict-determining declaration
promoted to a named verdict coordinate), this is a verdict-language repair of
the same size as R1's, plus two adjudication-ordered measurements. The
mathematics stands. The head does not.

---

## 2. Findings, ranked

| # | severity | finding |
|---|---|---|
| **F1** | **MAJOR** | **No motivated selection principle exists in the inheritance.** 0 of 4,140 partitions (×2 transports) yields locality, so no subgroup-orbit family can; and I6's own declared partial-overlap cover — the seven Fano lines — is measured **clique-only** at both transports because it is a 2-design. The head must become `LOCALITY-DECLARABLE`, not `LOCALITY-AT`. (§3) |
| **F2** | **MAJOR** | **The rule list is a coordinate of I6's numbering.** 40,320 relabellings, 1,209,600 exact censuses: counts 11–16, **11 distinct rule sets**, R069/R094 local at other orders. The declared 14 is *modal* (not cherry-picked, and the unit deserves that credit) but the string names a numbering-relative set as if it were substrate. (§3.4) |
| **F3** | **MAJOR** | **`DIMREAD=INCONSISTENT` at 14 of 14 is analytically FORCED** — CONSISTENT ∧ NON-COMPLETE is impossible under the delivered cell-indexed reading (theorem + 19,948-family exhaustive check, 12,928 non-complete components, 0 consistent). Per #208 a disclosure, delivered as a measurement. (§5.1) |
| **F4** | **MAJOR** | **The drawing relation is a verdict-determining coordinate absent from the string.** Measured by the unit itself (0 pairs at ⟨γ,Σ⟩ ⟹ head flips), disclosed only in §7.5 prose. Recurrence of the R1 atlas disease engraved at #10 → MAJOR by the §13 #313 addendum. (§7) |
| **F5** | **MAJOR** | **The whole census is derivable from the declaration alone** (closed form, 109 of 109, no atlas built). Per #208 the census clauses reclassify: *measured* = the grid + the drawing relation + the two transports; *forced* = which rules are local, at what widths, with what b₁. (§4) |
| **F6** | **MODERATE** | **§11's closing claim is refuted constructively.** The same sliding-window class at a **fixed-point-free** transport (the 8-cycle) yields a non-complete component, a CONSISTENT chart-intrinsic reading, and at c=2 a genuine triangulated 1-manifold (the 8-cycle; every link = S⁰, the correct link for dimension 1). The obstruction is the two transports' **fixed points**, not width-based overlap. (§5.2) |
| **F7** | **MODERATE** | **An undisclosed, headline-moving link convention.** The delivered link counts 2-cells **with cell multiplicity** while counting vertices *without* it (verified exactly, 0 of 160 mismatches). Under the simple-graph convention the headline `LINK-CIRCLES=1-OF-80` becomes **8 of 80**. No parity-witness gate covers it — the #313 boundary-parity class, recurring. (§5.3) |
| **F8** | **MODERATE** | **The one "circle" is not a triangulated circle.** R051 chart 4's link has degree sequence (1,2,2,2,3) — a triangle with a two-edge tail, homotopy-equivalent to S¹ but not a manifold link. Triangulated-circle links in the delivery: **0 of 80**. (§5.3) |
| **F9** | **MODERATE** | **`B2-PERSISTENCE` and the `CONSTANTS` constancy are forced** by the block-local declaration plus copy-forcing — zero degrees of freedom. Measured control: a non-block-local B₂ over the same 17 labels gives components `[16,1]`, no doubling. The pin's own request ("check copies-locality where blocks could interact") is unanswerable inside the delivered declaration. (§6.2) |
| **F10** | **MODERATE** | **The grid boundary is set by the wrong quantity.** Admissibility is `c·|H| < 8` on the **group order**, while cells are H's **orbits**. 60 rules constructible from the same declared words are excluded; **one of them is locality-bearing** — T7, ⟨gsgs⟩ (order 5, four cosets), c=2, SLIDING, completeness **16/21**, a value no censused rule attains. "14 of 109" is an artefact of that arithmetic. (§6.3) |
| **F11** | **MODERATE** | **`LOCAL-DIMENSIONS=1+2+3` is an identity, not a measurement**: the top local dimension equals `max|cell ∩ regular orbit| − 1` at **14 of 14**. "The reading tracks the declaration" is right; the verdict carries it as a finding. (§5.4) |
| **F12** | **MODERATE** | **The criterion's base rate is unreported.** "Some component non-complete" ⟺ the overlap graph contains an induced P₃ (verified exhaustively at V=5: 52 = Bell(5), 0 failures). **99.958%** of labelled graphs on 7 vertices and **76.6%** on 4 vertices satisfy it. The 14-of-109 rarity is a property of the *grid's composition* (partitions + pair-complete ALL-covers), not of the substrate. (§5.5) |
| **F13** | **MINOR** | **The `NULL` segment's counts have undeclared units and conflate REFUSES.** `G0-CLIQUE-ONLY-AT-4-OF-4` and `38-OF-38` are rule×**arena** measurements (2 G0 rules, 19 orbit rules), yet §3.2 calls them "rules" while §4's table says G0 has 2 rules at B. And **10 of the 38** are REFUSES, a status the unit elsewhere insists is distinct. (§6.4) |
| **F14** | **MINOR** | The `MECHANISM` segment names `MODES=SLIDING` but not the **width bound** (`c ≤ D`) that is the mechanism's actual content, nor the measured disappearance at c ≥ 5 (T7) / c ≥ 4 (T4). |
| **F15** | **MINOR, CREDIT** | **The grid is a named verdict coordinate at birth** — the R1 atlas lesson applied before it was ordered. The two-route gates, the REFUSES discipline, the positive control, the 40,320-action R2-A sweep, the demotion of the coherence corollary to a disclosure, and §10's five non-claims are all clean and reproduce exactly. |
| **F16** | **NON-FINDING, recorded** | §2.4's "plus the whole group" generates no G3-ORBITS rule at either transport. I checked: this is **correct de-duplication** — ⟨γ,Σ⟩'s orbits equal ⟨g⟩'s at T7 and ⟨gs⟩/⟨sg⟩'s at T4. No defect. |

---

## 3. K2 — THE MOTIVATION QUESTION (primary)

The RSQ standard, verbatim: a FOUND result is demoted when *every survivor is
an arbitrary relabelling selected by the property under test*, and the repair
is to compute the verdict **on the motivated family** and name the class the
positive half rests on. Applying it here needs three things: (a) an
enumeration of what the inheritance actually supplies, (b) a measurement of
each candidate, (c) a measurement of how much the survivors owe to naming.

### 3.1 The structural fact that organises the whole question

A cell structure produces locality iff the co-cell graph, restricted to the
transport's regular orbit, has a connected component that is not a clique.
Two consequences fix the candidate space:

> **(i) Every group-orbit family is disqualified in advance.** Orbits
> partition; a partition's co-cell graph is a disjoint union of cliques
> (R2-partition, the unit's own corollary). **Measured at full generality:**
> all **4,140** set partitions of the eight labels, at both transports —
> **8,280 measurements, 0 non-complete components.** Since the orbit family of
> *any* subgroup of S₈ is one of those 4,140 partitions, this settles Σ,
> ⟨γ,Σ⟩, the seven declared words, the ladder 1<A₄<GL(3,2)<A₆<A₇, and every
> subgroup of every one of them, at once. No inherited **group** can motivate
> a locality-bearing cover. This is not a limitation of the grid; it is a
> theorem about groups.
>
> **(ii) What is needed instead is a proximity datum** — a cover that is
> neither a partition nor pair-complete. The inheritance supplies groups
> (partitions) and one design (pair-complete). It supplies no order, no
> metric and no neighbourhood on I6's eight labels.

### 3.2 The candidates, tested

| candidate | where it comes from | measured result |
|---|---|---|
| Σ-orbits; ⟨γ,Σ⟩-orbits; the seven declared words' orbits | I6 arena + the grid's own G1/G3-ORBITS | partitions → **clique-only** (19 rules × 2 arenas, 0 non-complete) |
| the ladder subgroups' orbits (A₄, GL(3,2), A₆, A₇), and every subgroup of S₈ | I6 `the_ladder` | partitions → covered by the **4,140-partition sweep**: 0 of 8,280 |
| **the seven Fano lines** | **I6's own receipt**: `it_permutes_the_seven_Fano_lines`; `the_non_zero_labels_of_F2_cubed=[1..7]`; `THE_TYPE = GL(3,2) … acting F₂-linearly on the seven Fano points` | **CLIQUE-ONLY at both transports.** The lines cover **21 of 21** pairs — a 2-design — so the overlap graph is complete. |
| the Fano complements (the seven 4-sets) | dual of the above | **clique-only** at both transports |
| the γ-compatible Fano plane (translates of a (7,3,1) difference set) | the transport's own cyclic order | **clique-only** — and the six clique-only 3-shapes at T7 are *exactly* the planar difference sets |
| transport-generated covers of shape S (the only cyclic order the inheritance supplies) | γ acting on its regular orbit | T7: |S|=2 → 6/6 local; |S|=3 → 9/15 local, the 6 exceptions being the Fano shapes; **|S|≥4 → 0 local (20+15+6 shapes)**. T4: |S|=2 → 2/3; |S|=3 → 0/3. |
| the defect law D=(ΣVᵀΣV)⊗I₉, the one law D(ψ)=[P,u(ψ)] | I6 `gen`/`psi` receipts | live on the 9·k-dimensional matrix space; supply **no cover of the eight labels** without a fresh choice. Not testable as a cell structure; recorded as such. |
| division events / the record layer H_a[N] | I7 | supplies a proximity datum — but **on its own site lattice, not on I6's eight labels** (see §8, and F16 of the R3 recommendation) |

**The single positive result, and it is negative for the unit.** The one
genuinely inherited partial-overlap cover in the whole base — the Fano lines,
which I6's receipt names in so many words — fails the criterion, and fails it
for a *reason*: it is a 2-design. A design covers every pair, so its overlap
graph is complete. The corpus's own geometry is exactly the wrong shape to
produce this unit's locality.

### 3.3 Where the 14 rules' cover actually comes from

`SLIDING` means "every cyclic window of c consecutive cosets", and cosets are
"ordered canonically by minimum label". **The rule reads the label order
twice** — once to name the cosets, once to define "consecutive". A cyclic
order is precisely the datum §3.1(ii) says the inheritance does not supply.
The two transports do not rescue it: T7's γ *is* the increasing cycle on
1..7 (so the label order and the γ-order coincide **by construction of the
numbering**), and T4 is `lex_first_Q_per_order['4']` — *lexicographically*
first, i.e. selected **by the same label order**. The transports and the
windows are not two independent declarations; they are the numbering used
twice.

### 3.4 How much the survivors owe to naming — the RSQ test, run

Two sweeps, both exact (no predictor: I found and discarded a closed-form
shortcut of my own that mis-handles far pairs in different components).

**Per rule, over every ordering of that rule's own cosets:**

| rules | orderings | local at |
|---|---|---|
| R005/R008/R011/R030/R033/R036 (T7, H trivial) | 40,320 each | **100.0%** |
| R048/R051 (T7, H=⟨Σ⟩) | 5,040 each | **100.0%** |
| R063/R088 (T4, c=2) | 40,320 each | 45.7% |
| R066/R091 (T4, c=3) | 40,320 each | 82.9% |
| **R069/R094 (T4, c=4)** | 40,320 each | **77.1% — and the declared order is in the other 22.9%** |
| R106 (T4, ⟨Σ⟩, c=2) | 5,040 | 60.0% |
| every ALL and every BLOCKWISE rule | all orderings | **0.0%** |

**Globally, over all 40,320 numberings of I6's carrier** (1,209,600 exact
censuses; only the 30 SLIDING rules can move — ALL is order-free and
BLOCKWISE and the orbit classes are partitions at every order):

| locality count | orders | share |
|---|---|---|
| 11 | 4,032 | 10.00% |
| 12 | 2,880 | 7.14% |
| 13 | 10,368 | 25.71% |
| **14 — the declared numbering** | **13,824** | **34.29%** |
| 15 | 1,728 | 4.29% |
| 16 | 7,488 | 18.57% |

**11 distinct locality-bearing rule sets** are realised; **16 rules** are local
at some order; R069 and R094 are local at some orders but not the declared one.

**Adjudication of the RSQ standard.** This is **not** RSQ's disease. RSQ's
survivors were a measure-thin set of relabellings selected *upward* by the
property under test, and the motivated family returned nothing. Here the
declared numbering returns the **modal** count and the mechanism (SLIDING vs
ALL/BLOCKWISE) is **completely order-independent** — a real robustness result
the unit did not claim and deserves credit for. But the *specific* claim the
verdict head makes — `RULES=14-OF-109:R005,…,R109` — is a coordinate of the
numbering, taking 11 distinct values across relabellings of a carrier whose
numbering carries no structure. The RSQ repair therefore applies in its
weaker, correct form: **not "demote the finding", but "name the class the
finding is stated in".**

### 3.5 (b) What the verdict may honestly claim

> **LOCALITY-DECLARABLE**, not LOCALITY-FOUND.

Precisely: *the R1 criterion is satisfiable; the mechanism that satisfies it
is declared partial coset overlap along a cyclic order that no inherited
object supplies; the inheritance's own partial-overlap cover (the Fano lines)
fails the criterion because it is a 2-design; and the rule list is a
coordinate of the carrier's numbering.* This is an existence statement about
atlas space. It is not a statement about the substrate, and the paper's §10
already says so in prose — the repair is to move that sentence into the string.

### 3.6 The proposed verdict wording

Head and every segment computed in-gate and flippable; new material in bold.

```
R2-LOCALITY-DECLARABLE-AT<
 RULES=14-OF-109:R005,R008,R011,R030,R033,R036,R048,R051,R063,R066,R088,R091,
 R106,R109
|GRID=T4:G0=1;G1=2;G2=24;G3-ORBITS=7;G3-UNIONS=24|T7:G0=1;G1=1;G2=18;
 G3-ORBITS=7;G3-UNIONS=24|TOTAL=109
|**DRAWING=R1-RELATION-AT-<GAMMA>-VERDICT-DETERMINING(ALT-<GAMMA,SIGMA>:
 0-PAIRS-AT-2-OF-2-TRANSPORTS=>NO-LOCALITY-IN-THE-DECLARED-GRID)**
|MECHANISM=DECLARED-PARTIAL-COSET-OVERLAP(CLASSES=G2+G3-UNIONS;MODES=SLIDING)
 **;WIDTH-LAW=NONCOMPLETE-IFF-C<=D(D=MAX-CYCLIC-COSET-DISTANCE-IN-THE-REGULAR-
 ORBIT;D=4-AT-T7,3-AT-T4-TRIVIAL);CENSUS-DERIVABLE-FROM-THE-DECLARATION-AT-
 109-OF-109**
|**MOTIVATION=NONE-INHERITED:PARTITION-COVERS-NONCOMPLETE-AT-0-OF-8280
 (ALL-4140-PARTITIONS-X-2-TRANSPORTS);I6-FANO-LINES-COMPLETE-AT-2-OF-2
 (2-DESIGN:21-OF-21-PAIRS-COVERED);ORDER-RELATIVE(COUNT=11..16-OVER-40320-
 NUMBERINGS;DECLARED=14-MODAL-34.29%;DISTINCT-RULE-SETS=11)**
|COMPONENTS=NONCOMPLETE-COMPONENT-SIZES=4+7;RULES-WITH-NONTRIVIAL-B1=9
|STANDARDS=LINK-CIRCLES=1-OF-80-CHARTS**(CONVENTION=2-CELLS-WITH-CELL-
 MULTIPLICITY;SIMPLE-GRAPH-CONVENTION=8-OF-80;TRIANGULATED-CIRCLES=0-OF-80)**
 ;DIMREAD=INCONSISTENT**-FORCED(CELL-INDEXED-READING:CONSISTENT-EXCLUDES-
 NONCOMPLETE-BY-THEOREM)**;LOCAL-DIMENSIONS=1+2+3**=MAX|CELL-CAP-REGULAR-
 ORBIT|-1-AT-14-OF-14**;DIMPROFILE=EXTENSIVE-EXCLUDED
|B2-PERSISTENCE=**FORCED-BY-BLOCK-LOCALITY:**SURVIVES-AT-14-OF-14;
 COMPONENTS-DOUBLE-AT-14**(NON-BLOCK-LOCAL-B2:NO-DOUBLING,COMPONENTS=[16,1])**
|NULL=**ORBIT-PARTITION-CLASSES-NONCOMPLETE-AT-0-OF-38-RULE-ARENA-MEASUREMENTS
 (19-RULES-X-2-ARENAS;10-OF-38-REFUSE);G0=0-OF-4**
 (R2-A-VERIFIED-10-UNIT-ACTIONS-AND-40320-SWEPT-ACTIONS-0-COUNTEREXAMPLES)
|REFUSES=5-OF-109**;GRID-BOUNDARY=(H,C)-ADMITTED-BY-GROUP-ORDER-NOT-ORBIT-
 COUNT:60-CONSTRUCTIBLE-RULES-EXCLUDED,1-OF-THEM-NONCOMPLETE(T7-<GSGS>-C2-
 SLIDING-16/21)**
|BLOCK-CONSTANTS=DENSITIES-CONSTANT-B-TO-B2-AT-109-OF-109**(FORCED-BY-
 COPYING);VALUES-GRID-DEPENDENT=0/1..25/18**;UNDEFINED-B2-DENSITY-AT-28>
```

Each new segment can fail: `DRAWING` dies if the alternative group draws a
pair; `WIDTH-LAW` dies if any rule violates `c ≤ D`; `MOTIVATION`'s first two
clauses die if any partition or the Fano cover returns non-complete, and the
third dies if the order sweep is constant; `STANDARDS`' convention clause dies
if the two conventions agree; `B2-PERSISTENCE`'s parenthetical dies if the
non-block-local B₂ doubles; `GRID-BOUNDARY` dies if the excluded set is empty
or all-complete.

---

## 4. K1 — THE STRUCTURAL SIDE: forced vs measured, and the real contribution

I did not derive the circulant formula (that is the operator lens's charge),
but I derived and verified the **classification** it implies, which is what
#208 needs.

> **Closed form (verified at 109 of 109, no atlas built).** Let R be the
> transport's regular orbit, k the number of cosets, i(x) the coset index in
> the declared cyclic order, and D = max over pairs a,b ∈ R of the cyclic
> distance |i(a)−i(b)|ₖ. Then
> - **ALL** — every two cosets share a cell ⟹ R is a clique ⟹ complete;
> - **BLOCKWISE**, **G0/G1/G3-ORBITS** — the cells partition ⟹ R2-partition ⟹
>   complete or REFUSES;
> - **SLIDING** — a,b co-celled iff their cyclic coset distance ≤ c−1; hence
>   **non-complete iff c ≤ D**, and REFUSES iff no pair of R is within c−1.
>
> Measured D: 4 at T7/H-trivial (k=8), 3 at T7/⟨Σ⟩ (k=7), 3 at T4/H-trivial,
> 1 at T4/C2, 3 at T4/⟨Σ⟩ — reproducing the paper's c≥5 (T7) and c≥4 (T4)
> thresholds and every one of the 30 SLIDING statuses.

**Classification per #208.**

**MEASURED (genuine content):**
- **The grid itself** — its exhaustive enumeration, its 109 coordinates and
  the no-duplicates/no-truncation gates. This is the unit's real object.
- **The drawing relation and the two transports**, read from I6 by exact path
  (I verified both against `the_witness_system_part` and
  `lex_first_Q_per_order['4']`).
- **D at each rule** — a property of how the regular orbit sits in the declared
  coset order. Everything else follows from D.
- **The alt-drawing-group probe** (0 pairs) and the **positive control**.
- The **standards' calibration** against the three rebuilt I3 controls.

**FORCED (⇒ disclosures):**
- *Which rules are local* — a function of (mode, c, D).
- *The b₁ values* — circulant edge counts minus vertices plus components.
- *`DIMREAD=INCONSISTENT` at 14 of 14* — §5.1's theorem, no degrees of freedom.
- *`LOCAL-DIMENSIONS` = 1+2+3* — equals max|cell ∩ R| − 1 at 14 of 14.
- *`B2-PERSISTENCE` and every B→B₂ constancy* — block-local declaration +
  copy-forcing (§6.2).
- *`N_coh = N`* — already correctly disclosed by the unit.
- *`ORBIT-PARTITION-CLIQUE-ONLY-AT-38-OF-38`* — the partition corollary, and
  the unit proves it.

**The unit's real contribution, stated exactly.** Not the theorem (R2-A came
in with the R1 adjudication and is elementary), and not "locality exists"
(forced by the mechanism the recipe already named). It is **the grid design
and the standards port**: an exhaustively enumerated, printed, gated,
named-as-a-verdict-coordinate family of 109 atlas declarations, with a
criterion applied uniformly, controls that bite, and a REFUSES discipline —
plus the honest negative that the ported manifold standards return nothing at
the rules the criterion selects. That is a genuine and reusable instrument.
The census it produced is a corollary of the design.

---

## 5. K3 — THE STANDARDS AS EFFECTS

### 5.1 `INCONSISTENT` is a theorem about the reading

The reading compares the triple (link, star, **dimprofile**), where dimprofile
is a vector **indexed by the rule's cell list** — an external coordinate, not
chart-intrinsic. That single choice decides the result.

> **THEOREM (R2-eff-1).** Under the delivered reading, a component whose
> overlap graph is non-complete can never return `CONSISTENT`.
>
> *Proof.* Suppose C is non-complete and all charts of C share one dimprofile.
> C has ≥3 charts and ≥1 drawn edge, so some entry i is ≥1 at some chart,
> hence — by constancy — at every chart of C. An entry ≥1 requires the chart
> to lie in cell i, so C ⊆ cell i. A component lies in a single regular orbit,
> so every pair of C is drawn inside cell i and C is a clique — complete.
> Contradiction. (The alternative, every entry −1 at every chart, forces C to
> be a singleton.) ∎

*Verification, independent of the proof:* exhaustive over cover space at
n = 4 (all 2,047 non-empty families of subsets of size ≥2, 433 non-complete
components) and n = 5 (all 17,901 families of ≤4 cells, 12,495 non-complete
components) — **12,928 non-complete components, 0 consistent readings.**

So `DIMREAD=INCONSISTENT` at 14 of 14 is not a finding about the arenas; it is
a restatement of the fact that those 14 rules are the locality-bearing ones.
The two headline segments are logically welded. Per #208 → disclosure.

### 5.2 What WOULD earn a consistent dimension — and it is reachable

The obstruction above is the **reading**, not the geometry. Repair the reading
to chart-intrinsic data (the sorted multiset of local dimensions, the star,
the link's profile) and the question becomes live. Measured:

| construction | status | delivered reading | chart-intrinsic reading | link |
|---|---|---|---|---|
| 8-cycle transport, sliding c=2 | NON-COMPLETE | INCONSISTENT | **CONSISTENT** | (2,0,2,0) = **S⁰ — the correct link for dimension 1** |
| 8-cycle transport, c=3 | NON-COMPLETE | INCONSISTENT | **CONSISTENT** | (4,3,1,0) |
| 8-cycle transport, c=4 | NON-COMPLETE | INCONSISTENT | **CONSISTENT** | (6,9,1,4) |
| I7 link-star, d=2, L=4 | NON-COMPLETE | INCONSISTENT | **CONSISTENT** | (8,12,1,5), identical at all 16 sites |
| I7 link-star, d=3, L=4 | NON-COMPLETE | INCONSISTENT | **CONSISTENT** | (24,96,1,73), identical at all 64 sites |

At c=2 the 8-cycle transport gives a drawn graph that **is** an
8-cycle: a triangulated 1-manifold, every link two points, non-complete. The
sufficient condition is plain: **a transport acting regularly on the whole
label set, with the cover permuted transitively.** Both declared transports
have fixed points (T7 fixes 0; T4 fixes 0 and 1 and carries a 2-cycle), and
that — not "width-based overlap" — is the entire obstruction at R2's scale.

**§11's closing sentence is therefore an over-reach and must be withdrawn:**
"Everything measured here says width-based overlap will not do it" is refuted
by a construction in the same class.

### 5.3 The link convention, and the "circle"

The delivered link counts its **edges** as 2-cells **with cell multiplicity**
but its **vertices** as distinct neighbours. I confirmed this exactly: the
multiplicity reading matches the delivery at **160 of 160** chart-quantities;
the simple-graph reading disagrees at 17. The convention is internally mixed
and it moves the headline:

| convention | charts with (b₀,b₁) = (1,1) |
|---|---|
| delivered (2-cells with cell multiplicity) | **1 of 80** |
| simple graph (distinct neighbour pairs) | **8 of 80** |
| triangulated circle (a cycle graph) | **0 of 80** |

And the single delivered "circle" — R051 chart 4, link (5,5,1,1) — has degree
sequence (1,2,2,2,3): a triangle with a two-edge tail. It is homotopy-
equivalent to S¹ and is **not** a manifold link. The honest statement is
*zero* sphere-like links, with the Betti coincidence disclosed.

### 5.4 "Local dimensions track window width"

An identity, measured at **14 of 14**: the top local dimension equals
`max|cell ∩ regular orbit| − 1`. The paper says as much ("which is what an
extensive estimator does") and then puts it in the verdict as
`LOCAL-DIMENSIONS=1+2+3`. It is a restatement of the declaration, and the
string should say so.

### 5.5 What the criterion establishes at all

Locality as defined ⟺ the overlap graph contains an induced P₃ (a graph is
"clique-only" iff it is a disjoint union of cliques iff P₃-free; verified
exhaustively over all 1,024 labelled graphs at V=5, count 52 = Bell(5), 0
failures). Base rates:

- on T7's 7-chart component: 877 of 2,097,152 graphs are clique-only ⟹
  **99.958% of graphs satisfy the criterion**;
- on T4's 4-chart component: 15 of 64 ⟹ **76.6%**.

So "some component is not complete" is close to vacuous as a property of
graphs; the 14-of-109 rarity is a property of the **grid's composition** (the
grid is dominated by partitions and by ALL-covers, which are pair-complete),
not a discovery about the substrate. A successor claim must beat this base
rate or measure something stronger.

---

## 6. K4 — SCOPE

### 6.1 The transport-scope decision — RULE FOR THE TWO-TRANSPORT READING

The pin's §4 says "the transport action ⟨γ⟩" in the singular, and the worker
declared two. **The adjudication should adopt the worker's reading.** Three
reasons, in force order:

1. **§15 requires it.** Declared-arena discipline says the arena — "boundary,
   family, law, state, arena" — is declared **as data**, and that claims of
   significance are entered only for quantities gated across the unit's
   admissible arenas. The transport is arena data. A census taken at one
   transport and reported as a fact about the grid would be exactly the
   arena-artifact-as-conclusion §15 was engraved to stop.
2. **It is load-bearing here, not decorative.** The two transports have
   different regular-orbit sizes (7 and 4) and different D (4 and 3), hence
   different width bounds and different component sizes — the verdict's
   `COMPONENTS=…SIZES=4+7` exists *only* because both were run. A T7-only
   census would have reported component size 7 and threshold c≥5 as if they
   were the mechanism's content.
3. **The strict reading is available and costs nothing.** As a fallback the
   adjudication can order the **T7-only sub-census printed beside the full
   one**: 51 rules, 8 locality-bearing (R005, R008, R011, R030, R033, R036,
   R048, R051), 2 REFUSES (R002, R021), **one** component size (7), and the
   width bound D = 4 in the trivial-subgroup family (locality gone at c ≥ 5),
   D = 3 at H = ⟨Σ⟩. I have computed it; it is a strict sub-table of the
   delivered census and adds no new number. Printing it satisfies the pin's
   letter at the cost of one table.

**Ruling: two transports as §15 arena data, with the T7-only sub-census
printed as the pin-literal reading** — the same shape as R1's repair, where
both denominator conventions were printed and the pin-literal one named.

### 6.2 B₂ persistence, and the interaction test the pin asked for

The pin declares B₂ **block-local**: cells copied into each block, the
basepoint in no cell, transports never crossing blocks. Under exactly that
declaration the drawn graph of B₂ **is** two disjoint isomorphic copies of B's
plus an isolated vertex. Therefore "locality survives", "components double",
"completeness unchanged" and "densities constant B→B₂" are **forced**, with
zero degrees of freedom — R1's copy-forcing theorem applied verbatim. I
verified the doubling at 14 of 14 and the constancy at 109 of 109 (0
disagreements), and confirm they carry no information beyond the declaration.

The pin also asked to "check 'copies locality' against a case where blocks
could interact." **Inside the delivered declaration they cannot.** I built the
control the pin was reaching for — a non-block-local B₂ over the same 17
labels (one sliding cover over all of them, one transport cycling the whole
moved set, the R1 Atlas-C shape) — and measured components `[16, 1]` at
c = 2,3,4,5: **no doubling, and a different census object.** The B₂ result is
a property of block-locality, not of block addition, and the verdict segment
must say so.

### 6.3 The Σ-lattice word-generated scoping, and the grid boundary

The word-generation itself is clean: the seven declared words `e,g,s,gs,sg,
ggs,gsgs` are used consistently, and §2.4's "plus the whole group" is correct
de-duplication (F16). The defect is one level down:

**The admissibility test `c·|H| < 8` uses the group order, while the
construction uses H's orbits.** ⟨gs⟩ and ⟨sg⟩ have order 10 on eight labels;
⟨gsgs⟩ has order 5 with only four orbits. Their orbit structures are perfectly
usable and are excluded by an arithmetic that refers to a quantity the
construction never touches. Measured: **60 rules** constructible from the same
declared words under a coset-count test are excluded, and **one is
locality-bearing** —

> T7, H = ⟨gsgs⟩ (order 5; orbits {0}, {1,4,5,6,7}, {2}, {3}), c = 2, SLIDING:
> cells {0,1,4,5,6,7}, {1,2,4,5,6,7}, {2,3}, {0,3}; component 7, **16 of 21
> pairs, NON-COMPLETE.**

16/21 is a completeness value **no censused rule attains** (the 14 realise
2/7, 4/7, 6/7, 1/3, 2/3, 1/2, 5/6), so this is a genuinely new census row, not
a duplicate. "14 of 109" is therefore true at the declared arithmetic boundary
and at no other, and the boundary should be stated in the verdict or the test
restated in terms of cosets.

### 6.4 The coherence corollary — what it means that the cocycle is free

`N_coh = N` identically, verified at all 109 rules × 2 arenas. The unit
handles this correctly: it is disclosed, not claimed, and the scramble control
shows the instrument is not vacuous.

**What it means, stated for the successor.** The cocycle condition is free
*because the drawing relation forces trivial stabilisers* (R2-A): a drawn pair
admits exactly one transport element, so the three maps of a 2-cell compose to
the identity with nothing to check. This is not a fact about the substrate; it
is a fact about **the R1 drawing relation**, which selects only regular orbits.
Any relation that admitted a pair via more than one group element would have a
real cocycle condition.

**Does it trivialise R1's N_coh density?** Yes, in the following exact sense,
and this needs saying. R1's headline constant was `NCOH_DENSITY = |F(N_coh)|/
|E(N)| = 37/27`. Since N_coh = N is forced by the shared drawing relation, that
constant is **|F(N)|/|E(N)|** — a ratio of two counts of the *same* complex,
with the word "coherent" doing no work at all. R1's already-demoted headline
loses its remaining distinctness: it is not "coherence per drawn pair" but
"2-cells per 1-cell". The R1 repair should record this (the adjudication's
order 7 already prints both denominators; this adds that the *numerator* is
not what its name says). Measured across the whole grid, the density ranges
from 0/1 to 25/18 — it is a number about a declaration, at both rungs.

---

## 7. K5 — CARRIAGE: what the nine segments do not carry

Measured or forced restrictions, against the delivered string:

**Carried, correctly:** REFUSES = 5 of 109; UNDEFINED b₂ density at 28; the
grid as a named coordinate with its full per-class breakdown (the R1 lesson
applied at birth — real credit); both transports; the mechanism's class and
mode; the component sizes; DIMPROFILE = EXTENSIVE-EXCLUDED.

**Measured and NOT carried:**

1. **The drawing relation** — the unit's own `G-ALT-DRAW-PROBE` measured that
   ⟨γ,Σ⟩ as drawing group draws **0 pairs at both transports**, so the head
   flips to `R2-NO-LOCALITY-IN-THE-DECLARED-GRID`. A verdict-determining
   declaration disclosed only in §7.5 prose. This is R1's atlas finding
   recurring at a new coordinate; #313 makes a recurrence a MAJOR by default.
2. **The atlas-dependence of the constants' values** (0/1 to 25/18, §6.1
   fact 2). The `BLOCK-CONSTANTS` segment carries only the *forced* half
   (constancy) and omits the *measured* half (the values move by more than an
   order of magnitude across the grid).
3. **The width bound** — the mechanism's actual content (`c ≤ D`; c ≥ 5 at T7,
   c ≥ 4 at T4), stated in §4 and §11, absent from `MECHANISM`.
4. **The link convention** — 1-of-80 is convention-relative (8-of-80 the other
   way), and 0-of-80 links are triangulated circles.
5. **The numbering-relativity of the rule list** — not measured by the unit at
   all; measured here (11 rule sets, counts 11–16).
6. **The grid boundary's arithmetic** — 60 constructible rules excluded, one
   of them locality-bearing.

**Forced but carried as if measured:** `DIMREAD=INCONSISTENT`;
`LOCAL-DIMENSIONS=1+2+3`; `B2-PERSISTENCE` and `COMPONENTS-DOUBLE`;
`DENSITIES-CONSTANT-B-TO-B2`; `ORBIT-PARTITION-…-38-OF-38`.

**Legitimately outside:** the positive control, the scramble control, the
parity witness, the symmetry self-test, the cache counts.

**Unit error inside a carried segment (F13):** `G0-CLIQUE-ONLY-AT-4-OF-4` and
`38-OF-38` count rule×**arena** measurements (2 G0 rules; 19 orbit rules), but
§3.2 calls them "rules" against §4's own table, and **10 of the 38 REFUSE** —
a status the unit insists elsewhere is distinct from clique-only. The counts
are arithmetically right; their units and their conflation are not.

---

## 8. THE R3 QUESTION — are the 14 components adequate arenas?

**Recommendation: NO — and the corpus already owns the arena R3 needs.**

**Why the 14 components cannot carry hypersurface-deformation questions.**

1. **No dimension to deform in.** All 14 are `INCONSISTENT` — and by
   §5.1's theorem they *must* be, so no repair inside the grid changes it. A
   hypersurface has a codimension; a component with no chart-independent
   dimension has none.
2. **No local model.** Every chart of every component has a different link
   (12 of 14 rules give a distinct reading at every chart; the best case is 6
   distinct over 7). H_a[N]'s constraint algebra is written on a *repeating*
   local model; there is nothing here to repeat.
3. **No interior.** Five of the 14 carry **no 2-cells at all** — R005, R030,
   R063, R088, R106, F(N)=0 (and F(N)=0 at 28 of 109 rules overall) — and a
   sixth, R048, carries exactly one. The largest 2-cell count at any
   locality-bearing rule is **20** (R011 and R036), on 7 charts. There is no
   bulk to deform and no boundary to deform it against.
4. **No direction structure.** H_a[N] needs lapse profiles, per-direction link
   counts and a chart group. The R2 components have a cyclic order borrowed
   from a numbering and nothing else — no directions, no sites, no lapse.
5. **The locality is numbering-relative** (§3.4), so any deformation result
   would inherit the relativity and be un-defendable at the next round.

**What R3 should use instead.** I7 (`v13/code/ha_successor_receipt.json`,
`542b8735daf0`) *declares*, as data: a site lattice X with `d = 2`,
`d_ext = 3`, `L = L_ext = 3`; a link set `links_d2 = {e₁, e₂, e₁+e₂}` and
`links_d3` = the six positive links; a `chart_group` = "the |X| chart
translations and the d! direction relabellings, acting on sites, on the
record's link counts, on the lapse profiles and on every tensor index"; and a
`lapse_family`. That is a **motivated locality**: the proximity relation is
inherited, the cells are its link-stars, and a declared group permutes them
transitively. Measured here, against R2's own criterion and R2's own
standards:

| arena | R2 criterion | chart-intrinsic reading | link profile |
|---|---|---|---|
| I7 as declared, d=2, **L=3** | **clique-only** (9 sites too few: every difference is a link combination) | — | — |
| I7 links, d=2, L=4 | **NON-COMPLETE** (64/120) | **CONSISTENT** | (8,12,1,5) at all 16 sites |
| I7 links, d=2, L=5 | **NON-COMPLETE** (100/300) | **CONSISTENT** | (8,12,1,5) at all 25 sites |
| I7 `links_d3`, d=3, L=3 | **NON-COMPLETE** (324/351) | — | — |
| I7 `links_d3`, d=3, L=4 | **NON-COMPLETE** (768/2016) | **CONSISTENT** | (24,96,1,73) at all 64 sites |

**The R3 pin should therefore:**

- **(R3-a)** declare the arena to be I7's site lattice with its declared link
  set and chart group — *not* a component of the R2 grid;
- **(R3-b)** carry **L ≥ 4** as a *measured* requirement with the L=3
  degeneracy printed as its own control (I7's declared L=3 fails R2's
  criterion at d=2 and passes at d=3 — the size dependence is the first thing
  the pin must gate);
- **(R3-c)** use R2's criterion as an **entry gate** on that arena, and the
  **repaired chart-intrinsic reading** (§9) as the dimension standard;
- **(R3-d)** keep the R2 grid as the **null control** — a declared,
  numbering-relative locality on which the standards demonstrably fail, which
  is exactly the negative baseline a deformation claim needs;
- **(R3-e)** state up front that a consistent dimension is *necessary but far
  from sufficient*: even at I7's lattice the link is (8,12,1,5), not a sphere.
  R3 must not read "CONSISTENT" as "manifold".

---

## 9. SUCCESSOR REQUIREMENTS

### 9.1 MOTIVATED LOCALITY (the analog of RSQ's motivated family)

A cover C of a declared arena A earns the word *locality* only if all five hold
and each is gated:

- **M1 — inherited carrier.** The cells are orbits, stars or blocks of a
  structure **named in the unit's own hash-pinned inheritance** (e.g. I7's
  `links_d2`/`links_d3`), never of the arena's numbering. A cover that reads a
  label order is a declaration, and the verdict says `DECLARABLE`.
- **M2 — symmetry-carried.** A declared group Γ acts on A, permutes C, and is
  **transitive on the cells**. Then no cell is a chosen one and locality is
  Γ-covariant.
- **M3 — relabelling-invariance, gated and printed.** The locality verdict
  *and its rule list* are invariant under every relabelling of A that fixes the
  inherited structure. The sweep is printed. R2's 40,320-order sweep is the
  template — and R2 **fails** it: 11 distinct rule sets, counts 11–16.
- **M4 — not a design.** The cover must not be pair-complete (a 2-design
  covers every pair and its overlap graph is complete). This is not a
  technicality: it is the exact reason I6's Fano lines fail, measured.
- **M5 — beat the base rate.** "Some component non-complete" is satisfied by
  99.958% of graphs on 7 vertices. A locality claim must report the base rate
  at its component sizes and either beat it by a stated margin or measure a
  strictly stronger invariant than P₃-freeness.

### 9.2 EARNED DIMENSION

- **D1 — chart-intrinsic reading.** Chart-independence must compare
  chart-intrinsic data (sorted multiset of local dimensions, star, link
  isomorphism type), never a vector indexed by the external cell list. As
  delivered, `CONSISTENT ∧ NON-COMPLETE` is impossible (theorem R2-eff-1,
  19,948 families, 12,928 non-complete components, 0 hits).
- **D2 — vertex-transitivity is the cheap sufficient condition.** A transport
  acting **regularly on the whole arena**, with the cover permuted
  transitively, gives CONSISTENT for free. Both R2 transports have fixed
  points; that is the sole obstruction at this scale, and it is removable.
- **D3 — a manifold link, not a Betti coincidence.** Report whether the link
  is a triangulated sphere (in dimension 2, a cycle graph), not merely
  (b₀,b₁)=(1,1). Measured: **0 of 80** R2 charts have a triangulated-circle
  link; the delivered "1 of 80" is a triangle with a tail.
- **D4 — the convention is gated.** Coordinate-resolved vs simple link edges
  must carry a parity-witness gate with the measured delta (here 1-of-80 vs
  8-of-80), exactly as the #313 boundary-parity engraving requires for the
  2-cell connective. Vertices and edges must use the **same** convention.
- **D5 — the dimension must not equal the declaration.** Report the measured
  dimension against `max|cell ∩ regular orbit| − 1`. Equal at every rule (14
  of 14 here) ⟹ the reading is a restatement of the width and must be labelled
  one.

---

## 10. What survived every attack

- **Every number.** 3,503 quantities rebuilt from the declarations — the whole
  109-row census, all 80 chart profiles, all 218 block-constant rows, every
  Betti number, every density, every control — **zero disagreements**. Two
  apparent discrepancies were **mine**, and both resolved in the delivery's
  favour: BLOCKWISE keeps a short trailing window (I had dropped it), and
  `b₁(N)` is the *complex's* first Betti number, not the drawn graph's cycle
  rank (18−8+2−(5−0)=7 at R008, exactly as delivered).
- **Theorem R2-A** is true and correctly proved; my independent sweep over all
  **40,320** cyclic actions on the eight labels found **0 counterexamples**,
  reproducing the delivered figure exactly.
- **The partition corollary** is true and I strengthened it: 0 of 4,140
  partitions, at both transports.
- **The grid is exactly as declared** — all 109 coordinates in the delivered
  order, no duplicates, no truncation.
- **The coherence corollary is already a disclosure** — the unit reached the
  right #208 classification unprompted, and backed it with a scramble control
  that moves at 81 of 81. Model behaviour.
- **The grid as a named verdict coordinate at birth**, before any reviewer
  ordered it. This is the R1 atlas lesson absorbed correctly and it is the
  best thing in the delivery.
- **§10's five non-claims are correct**, including "No claim that locality
  here is substrate content", which anticipates F1's direction — the paper's
  prose is ahead of its verdict string, and the repair is largely a matter of
  moving §10 into the string.
- **The mode probe's coincidence** (SLIDING = ALL at c = |cosets| − 1, at
  R020/R045/R078/R103) is correct: windows of 7 over 8 cosets are the
  complements of singletons, and so is C(8,7).
- **The alt-drawing-group reason is right in general form**: |⟨γ,Σ⟩| exceeds
  the label count, so no orbit can be regular and nothing is drawable.

---

## 11. Repair orders (this reviewer's proposals; the adjudicator's to resolve)

1. **Rename the head** to `R2-LOCALITY-DECLARABLE-AT` and adopt §3.6's segment
   set: add `DRAWING`, `MOTIVATION`, the `WIDTH-LAW`/derivability clause, the
   standards' convention clause, the forced-status labels on `DIMREAD`,
   `B2-PERSISTENCE` and the densities, the `GRID-BOUNDARY` clause, and the
   `VALUES-GRID-DEPENDENT` half of the constants. Every segment computed
   in-gate, every one flippable (#234/#257, the #10 equality addendum).
2. **ADJUDICATION-ORDERED MEASUREMENT — the motivation census, in-unit** (the
   RSQ motivated-family precedent and the TOP P* precedent together): run
   (a) the 4,140-partition sweep at both transports, (b) I6's seven Fano
   lines and their complements, (c) the transport-generated shape sweep. Print
   the table. Whatever it returns enters the paper and the `MOTIVATION`
   segment. My measurements are offered as the three.
3. **ADJUDICATION-ORDERED MEASUREMENT — the numbering sweep, in-unit**: the
   40,320-order census with its count distribution, its 11 rule sets, and the
   credit that the declared numbering is *modal* and the mechanism
   order-independent. This is the §14 symmetry self-test done at the
   coordinate that actually moves — the delivered self-test conjugates the
   transport *and* transports the cells, which is covariance, not
   order-invariance, and cannot detect this.
4. **Reclassify the forced clauses as disclosures** (#208): `DIMREAD`,
   `LOCAL-DIMENSIONS`, `B2-PERSISTENCE`, the B→B₂ constancy, the 38-of-38
   null. Each keeps its gate as an *instrument* gate; its epistemic class for
   the substrate claim becomes disclosure.
5. **State theorem R2-eff-1 in the paper** (CONSISTENT excludes NON-COMPLETE
   under a cell-indexed reading) and **repair the reading** to chart-intrinsic
   data, re-reporting the 14 rules under it.
6. **Withdraw §11's closing sentence** and replace it with the measured
   statement: a consistent, manifold-correct reading *is* reachable in this
   grid class at a fixed-point-free transport (the 8-cycle at c=2 is a
   triangulated circle), and the obstruction at the declared transports is
   their fixed points.
7. **Disclose the link convention** and print both counts (1-of-80 and
   8-of-80), plus **0 of 80 triangulated circles**; give it a parity-witness
   gate per the #313 engraving; make the link's vertex and edge conventions
   agree.
8. **Print the non-block-local B₂ control** (`components [16,1]`, no doubling)
   as the pin's interaction test, and label the delivered B₂ result as forced.
9. **Fix the grid boundary**: either restate admissibility in terms of coset
   counts (adding the 60 rules, of which one — T7 ⟨gsgs⟩ c=2 SLIDING, 16/21 —
   is locality-bearing) or carry the exclusion and its one casualty as a
   verdict clause.
10. **Fix the `NULL` counts' units** (rule×arena, not rules) and separate
    REFUSES from clique-only in the 38.
11. **Print the T7-only sub-census** (51 rules, 8 local, 2 REFUSES, sizes {7},
    threshold c ≥ 5) beside the two-transport census as the pin-literal
    reading, and state the §15 ground for keeping both transports (§6.1).
12. **Record the R1 consequence** (§6.4): since `N_coh = N` is forced by the
    shared drawing relation, R1's `NCOH_DENSITY` numerator is not "coherent
    2-cells" but simply "2-cells"; the R1 repair should say so.
13. **Report the criterion's base rate** (99.958% / 76.6%) beside the
    14-of-109, so the census's rarity is attributed to the grid rather than to
    the substrate.
14. **Carry the successor requirements** of §9 into the R3 pin, together with
    the R3 arena recommendation of §8.

---

## 12. Recomputation ledger and hash re-verification

Scratch only (`…/scratchpad/r2eff/`): `rebuild.py`, `motivation.py`,
`standards.py`, `linkconv.py`, `k1k4.py`, `b1fix.py`, `final.py`,
`ordersweep.py`, `i7lattice.py`, `i7stand.py`. Interpreter
`/opt/homebrew/bin/python3.13`. Exact arithmetic (`int`, `Fraction`, F₂
elimination by bitmask); **no import from the unit's code**; read-only git.

| block | distinct quantities |
|---|---|
| Grid coordinates (109) + census coordinates (545) | 654 |
| Standards: 80 charts × (link, star, dimprofile) + readings | 327 |
| Link-convention resolution, 80 charts × 2 conventions | 160 |
| Block constants, 109 rules × 2 arenas × 8; closed-form predictor (218); B₂ (28); grid-scoping (60) | 2,036 |
| b₁ per component, 14 rules × 4 | 56 |
| R2-A null classes, controls, alt-drawing probe | 65 |
| Fano / dual / transport-shape / base-rate checks | 83 |
| I7 lattice configurations (8) + its standards (114) | 122 |
| **Total distinct quantities** | **3,503** |

| exhaustive sweep | computations |
|---|---|
| All 4,140 set partitions × 2 transports (the orbit-cover class) | 8,280 |
| All cyclic actions on the eight labels (theorem R2-A) | 40,320 |
| Cover families: n=4 exhaustive (2,047) + n=5 to four cells (17,901) | 19,948 |
| All labelled graphs on 5 vertices (the P₃ characterisation) | 1,024 |
| Per-rule coset-ordering sweeps, 30 SLIDING rules | 2,967,840 |
| Global label-order sweep, 40,320 orders × 30 rules, exact censuses | 1,209,600 |
| Transport-generated cover shapes | 68 |
| **Total** | **4,247,080** |

**Numerical disagreements with the frozen delivery: 0. False numbers: 0.
False theorems: 0.**

**Reviewer errors, owned:** (a) I first implemented BLOCKWISE without its
short trailing window and reported five spurious mismatches — the delivery is
right. (b) I first computed `b₁(N)` as the drawn graph's cycle rank rather
than the complex's first Betti number — the delivery is right. (c) I built a
closed-form shortcut for the global order sweep that mis-classifies a far pair
lying in a different component; it inflated the sweep's maximum from 16 to 18.
Caught by cross-checking against the per-rule exact sweep, discarded, and the
sweep re-run exactly (1,209,600 censuses). Only the corrected numbers appear
above.

**Hashes re-verified after this run** (all unchanged from §0):
`cc78e9373bbe` / `36a10324b93f` / `cb8493a13c39` / `7b128499b246`.
Governing documents re-verified: protocol `3768846aebe9`, pin `76d42dfbc900`,
R1 adjudication `4115dcd83cfa`, R0 founding pin `e9d2bedff244`.

**Repo writes by this reviewer: exactly one — this file.**
