# THE R=4 ARENA: THE DECLARED-RECORD WELD + THE SPLITTABLE QUESTION (paper-21) — PIN (FROZEN)

**Frozen:** 2026-08-11, v14 ledger #174.  **Authority:** the
paper-19 adjudication (#168) successor register + the effectus's
exhaustive discovery (G-FLAT (1,1,2) — one of I7's ELEVEN
DECLARED records — reachable at R=4 at 276 ordered quadruples,
over 36⁴ = 1,679,616).  **Parents:** paper-19 terminal (559e162)
and the Γ-iteration terminal (2895a9a).  **Unit:**
paper-21-r4dec; code v14/code/r4dec_exact.py (+ output +
receipt).

## THE QUESTIONS (staged, each gated)

1. **THE 276, UNIT-GRADE:** rebuild the R=4 grouping census
   (exhaustive over 36⁴ where the effectus was exhaustive;
   grammar-driven at a declared window per the paper-19
   licensing pattern); confirm the 276 G-FLAT-inducing
   quadruples; gate FORCED/BRANCHING/REFUSED; the record
   induced EXACTLY (1,1,2) per I7's committed G-FLAT row.
2. **THE DECLARED-RECORD WELD:** the paper-19 census machinery
   (both readings; the RSQ zero-free-items standard; the
   controls re-established) pointed at the R=4 G-FLAT records
   vs I7 — with the target now ONE OF THE ELEVEN DECLARED
   records, retiring the undeclared-target residue entirely if
   FOUND.  Outcomes: `R4W-DECLARED-WELD-FOUND-<map>` /
   `-EMPTY-<obstruction>` / `-BLOCKED-AT-<object>`,
   reading-stamped.
3. **THE SPLITTABLE QUESTION (the law-over-records door):** is
   the R=4 welded record splittable (split fiber > 0 at any
   interval — the (1,1,2) record has a count-2 link)?  If YES:
   measure WHICH of the terminal refinement laws (papers
   04/06/09) become non-empty on it — the first arena where "a
   law over records" is posable on a welded world.  If NO:
   the mechanism named.
4. **THE PRICE-LAW ROW:** the R=4 budget (36 ≥ 27) census at
   the declared window — the posdef/I7-STRICT distribution row
   extending the R=2/R=3 sequence (budget-binds →
   matching-binds → ?).

## STANDARDS

The full era per HANDOFF-PROMPT.md §4 (all 21 engravings —
total seal, fenced coverage, reachability, #125 with markdown
prefixes, #91 off-tree/git-less, the derived+rendered head);
the four walls inherited; sedimentary (no anchors); every
quantum-character claim description-stamped; windows declared
in-string, licensed by equality gates, never silent; the dead
lists cited never re-run.  Between delivery and adjudication
every headline is a candidate reading.
