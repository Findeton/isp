# THE CONTINUUM-ROUTES BATCH — FOUR PINS (CR-A / CR-B / CR-C / CR-D)

**Frozen:** 2026-08-09, v14 ledger #30, by user order.  **Shared
discipline for all four:** RUNBOOK §13/§14/§15 with every addendum;
all five 2026-08-09 engravings at birth with injection-falsifiers;
exact arithmetic; verdicts computed in-gate with independent
comparators; never-falsified census in the receipt from delivery one;
declared arenas as data; BLOCKED verdicts first-class.  Sources cited
from R6a are its DELIVERED artifacts (committed #26, verified #27,
UNDER PANEL — status carried in every citation; the units gate reads
by hash 022c3f488a93 for the receipt).  Cross-unit file surfaces are
disjoint; none reads another CR unit or R3.

---

## CR-A — REFINEMENT BY ACCUMULATION (paper-05)

Deliverables: `v14/paper-05-accumulation.md`,
`v14/code/cra_accumulation_exact.py` + `_output.txt` + `_receipt.json`.
Sources: I7 (`542b8735daf0`), HA paper (`f286ba10d2d9`), HA code
(`d44cb72f8ee9`).

**The question.**  H_a[N] advances the FRONT n(x) (events committed at
sites) but the geometry record s = {n_ℓ} (events in intervals) is
background.  (1) Does the pinned grammar contain any
geometry-record-advancing move — an analog of front advance that
writes new interval events?  Enumerate the candidates expressible from
the pinned declarations (the census as data); if none exists,
`CRA-BLOCKED-AT-STATIC-GEOMETRY` — the pinned layer treats geometry as
frozen background, and accumulation-refinement needs a
geometry-writing dynamics the pin does not carry (name the missing
object).  (2) For each admissible advancing move: along declared
schedules, does the RESCALED geometry (q normalized per accumulated
event, exact rationals) converge (eventually constant / eventually
periodic / divergent — each characterized)?  Verdicts:
`CRA-RESCALED-CONVERGES-<move, schedule, limit>` /
`CRA-DIVERGES-<mode>` / `CRA-BLOCKED-AT-STATIC-GEOMETRY`.  Controls: a
hand-declared external growth move must be flagged foreign by the same
audit (the R6a choice-inventory discipline applied to advancing
moves); mutants per the shared minimum set.

## CR-B — STOCHASTIC REFINEMENT: THE SPLIT IN DISTRIBUTION (paper-06)

Deliverables: `v14/paper-06-stochastic-split.md`,
`v14/code/crb_stochastic_exact.py` + `_output.txt` + `_receipt.json`.
Sources: the R6a receipt (`022c3f488a93`, UNDER-PANEL status carried),
I7, HA paper/code.

**The question.**  R6a proved no motivated split VALUE.  Is there a
motivated split DISTRIBUTION — a conditional law over the split fiber
forced by pinned structure?  Registered: (1) rebuild the split fibers
(anchor against R6a's counts); (2) the ORBIT DECOMPOSITION of each
fiber under every pinned symmetry (chart group, Σ, lattice
reflections — the symmetry inventory as data); (3) the invariant-
measure census: unique invariant distribution ⟺ transitivity —
measured per fiber; where not transitive, the orbit-simplex dimension
counted exactly; (4) whether any pinned object supplies a further
selection (the dynamics' weights, the record's own counts as biases —
each candidate tested and its forcing named or refuted); (5) the
iteration question in distribution: do orbit-simplex dimensions
grow/stabilize under repeated refinement.  Verdicts:
`CRB-DISTRIBUTION-MOTIVATED-<law, forcing>` /
`CRB-ORBIT-SIMPLEX-<dims — not unique>` /
`CRB-BLOCKED-AT-NO-PINNED-STOCHASTIC-LAW-<the missing object>`.
Control: a fiber with a transitive action constructed as positive
control; an asymmetric fiber as negative.

## CR-C — THE COARSE-GRAINING SEMIGROUP (paper-07)

Deliverables: `v14/paper-07-coarse-graining.md`,
`v14/code/crc_coarsegrain_exact.py` + `_output.txt` + `_receipt.json`.
Sources: I7, HA paper/code; the R6a receipt for the additivity dual
(UNDER-PANEL status carried).

**The question.**  Merging is the direction the grammar licenses
(counts SUM — the R6a dual).  (1) Construct the merge moves (2:1
axis block-merge; full dyadic coarsening) and run the CHOICE INVENTORY
on the merge direction for the FULL record (s, n, m): interval counts
sum (forced — verify); front values sum (forced by counting semantics
— verify); the matter register m does NOT add — class every merge
freedom on m exactly as R6a classed the split freedoms.  (2) The
transformation law: how q transforms under merge (exact); which
quantities are merge-covariant.  (3) THE FIXED-POINT CENSUS: which
records are merge-self-similar (fixed up to the measured rescaling) —
the RG-fixed-point analog, over the declared record family and a
declared enlargement.  (4) Merge-dynamics compatibility:
coarsen-then-H vs H-then-coarsen — the commutation census, defect
characterized if nonzero.  Verdicts:
`CRC-MERGE-CANONICAL-ON-<sectors>-FIXED-POINTS-<census>` /
`CRC-MERGE-CHOICE-AT-<register, inventory>` — plus the commutation
segment.  Control: the R6a split move run backwards must reproduce
the merge on its overlap (consistency).

## CR-D — THE SYMMETRY-TOWER LIMIT: FOUR WINGS (paper-08)

Deliverables: `v14/paper-08-tower-four-wings.md`,
`v14/code/crd_tower_exact.py` + `_output.txt` + `_receipt.json`.
Sources: I6 (`c9bc956fe751` — TB3's three-wing base, the ladder, the
ceiling theorem), GEN (`e0b2f444f6a9`), PSI (`7c7b91a9257e`).

**The question.**  The one measured non-copying growth in the corpus
is the group tower.  Construct the FOUR-wing analog of TB3's base by
TB3's own construction rule (read as data from the pinned receipt:
the wing structure, S₃ → S₄ on the wing factor); compute the
holonomy/system groups at four wings at the declared completions;
test: (1) does the three-wing ladder EMBED in the four-wing structure
(set-equality standard — the ladder-extension question); (2) the
four-wing ceiling (the 15,120 analog) — attained or bounded; (3) the
growth law of the tower (|Hol| as wings grow — the first two points
of the sequence); (4) whether the new groups stay in the alternating/
linear families or leave them (the Lie-limit direction's first datum).
Compute honesty: the four-wing censuses may be large — caps printed
and gated, sampled sweeps labelled as samples, BLOCKED-AT-SCALE
first-class with the reached scope printed.  Verdicts:
`CRD-TOWER-EXTENDS-<embedding, ceiling, families>` /
`CRD-TOWER-BREAKS-<mode>` / `CRD-BLOCKED-AT-SCALE-<reached>`.
Control: the three-wing values re-derived and anchored against I6
before any four-wing number counts.

---

**Shared mutant minimum** (each unit): source-hash + path-value + (for
CR-B) the R6a-receipt anchor drifts; census-cell drop; forced-clause
demotion checks (#208); the five verdict injection classes; verdict
comparator independence; fiber/census-count corruption against the
in-gate independent recomputation (#24/#219); cache-exercise; the
unit's own decisive-claim flip (e.g. CR-C's fixed-point census bit,
CR-D's embedding bit).
