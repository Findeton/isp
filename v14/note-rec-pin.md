# REC (PAPER-41) — THE RECONSTRUCTION UNIT: THE CAST DERIVED FROM THE RECORD — PIN (FROZEN #373, user-raised at #354)
**Question:** can the theory be reconstructed from the record alone, the actors DERIVED rather than declared? Authority: the #354 registration; FAC terminal (LEG-2 ⟺ participation-signature — the existing evidence the cast is record-recoverable); EPR terminal (the record is the complete description); AID terminal (naming forced).
**Measurements:** (1) THE RECONSTRUCTION MAP: from the bare record field (the 27-cell histories at HEAD's committed corpus, stripped of every actor label) derive: the site set, the link structure, the cast size, the partition menu, the naming — each by an explicit algorithm reading ONLY record bytes; gate each against the declared arena (agreement = the cast is a theorem; disagreement located = the extra structure actors smuggle). (2) THE INVERSE DIRECTION: which record properties are NOT derivable from the cast+grammar alone (the record's own surplus). (3) THE MINIMALITY CENSUS: over the committed histories, how much record suffices (prefix depth for full reconstruction — the w* connection measured, not assumed). (4) THE OBSTRUCTION, if any, named exactly (which datum resists — e.g. the coin orientation, the torus closure).
**Outcomes (feasibility at the committed corpus per #299-as-extended):** REC-CAST-DERIVED (all reconstruction gates agree) / REC-CAST-DERIVED-UP-TO-<the residue> (the honest middle: agreement modulo a named declared datum) / REC-OBSTRUCTED-AT-<datum> / REC-BLOCKED-AT. Control arms through the real reconstructor (scrambled records must fail; synthetic minimal records must succeed).
**Walls + standards:** the FULL E-25…E-33 TEMPLATE (this is the first unit built on era_template.py — the adoption's first exemplar); S-1 addressed by construction (the comparator must not be the builder: reconstruction and verification by disjoint code); no seal reopened; exact arithmetic; scratch <5G. Unit: paper-41-rec + rec_exact.py/output/receipt.
