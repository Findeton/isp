# POT (PAPER-36) — THE POTENTIAL UNIT: THE CONFINEMENT GATE OPENS — PIN (FROZEN)

**Frozen:** 2026-08-12, v14 ledger #308.  **Authority:** the
ACT terminal #307 (paper-34 d933221780ed: the allowed
weight-system space, the 135-coupling inventory at link,
the distinguished points — 64 vertices + 72 edge midpoints,
the odd-twist prohibition, the merging-index law
8/gcd(L,8), the arithmetic obstruction over the declared
field) + the SMU adjudication #242 (ROUTE C; the law-native
π control stamp carried verbatim, never spent) + paper-23
(the withholding machinery — **the confinement gate it held
shut OPENS FOR THIS UNIT ONLY**; the transitivity
criterion) + R5 (the plaquette structure, holonomy, the
torus).  **Unit:** paper-36-pot; code v14/code/pot_exact.py
(+ output + receipt).

## THE QUESTION

The static potential on the record-borne gauge arena,
priced and family-swept.  Exact arithmetic only:
expectations are ratios of exact rational sums over the
declared carrier; no logs, no floats — "potential" nominal,
carried multiplicatively as loop-expectation ratios.

1. **THE LOOP FAMILY (the missing R5 object, constructed
   first):** enumerate the loop content of the declared
   arena exactly — contractible rectangles by size, AND the
   winding (torus-wrapping) family.  ACT recorded this
   object STILL ABSENT; POT builds it, gates it, and
   publishes the family's exact extent at the declared L
   (the window named in-string: which sizes exist, which
   are distinct under the acting group).  No silent caps.
2. **LOOP EXPECTATIONS, TWO QUANTIFIER MODES:**
   (a) CONDITIONAL — under declared weight systems: the
   conjugation-symmetric Wilson row (ACT's repaired
   membership object), the law-native control (stamp
   verbatim, never spent as derived), the null w ≡ 1.
   Stamped CONDITIONAL-ON-THE-DECLARED-WEIGHTS.
   (b) FAMILY-SWEPT — over the allowed space's measured
   extreme points (the 64 vertices and 72 edge midpoints)
   and any exactly-parameterized family slices: which loop
   observables are FAMILY-INVARIANT and which are
   coupling-dependent.  E-24: counts are counting-only;
   no measure over the family is declared.
3. **THE DISCRIMINATOR (defined before any row runs, and
   gated):** the exact finite analogue of area-vs-perimeter
   — ratio tests over the available rectangle ladder (the
   Creutz-ratio analogue in exact rationals), plus the
   winding-loop expectation as the deconfinement-order
   analogue.  The discriminator's own well-definedness
   (nonzero denominators; enough ladder rungs at the
   declared L) is itself a gated measurement.  **The
   L-BOUNDARY STATED: the merging-index law 8/gcd(L,8)
   means family-level pinning dies at L ≡ 0 mod 8 — every
   family-invariant claim carries its lattice scope.**
4. **THE PRICE BINDING:** does the potential's behavior
   partition the coupling inventory?  If confinement-like
   vs not splits the family, the split is measured exactly
   (which couplings, which fraction of the 135) — the
   inventory partitioned, not argued.
5. **THE SPECTRAL DOOR (named, not forced):** the transfer
   object at declared weights exists as an exact matrix on
   the carrier; its gap is a computable exact quantity.
   Measure it at the declared rows IF the cost fits the
   window; otherwise name the door and its cost honestly.
   (The mass-gap question's finite form — SPC inherits
   whatever this row measures.)

## OUTCOMES (pre-registered, WITH FEASIBILITY — the #299 engraving)

- `POT-CONFINES-AT-<rows>` — reachable: a weight system
  concentrating on flat configurations makes large-loop
  expectations decay faster than perimeter (the counting
  measure's ladder is computable; steep decay is attainable
  within positive rationals).  A control arm demonstrates
  the word on a synthetic row through the REAL
  discriminator.
- `POT-DECONFINES-AT-<rows>` — reachable: the null w ≡ 1
  row's ladder is computable and its ratio behavior is a
  legitimate perimeter-shaped candidate; the control arm
  demonstrates the word likewise.
- `POT-SPLIT-BY-WEIGHT-<the partition>` — reachable: modes
  (a)+(b) measure ≥ 3 rows and 136 extreme points; any
  disagreement between two rows realizes the split.
- `POT-DISCRIMINATOR-DEGENERATE-AT-<L>` — reachable: the
  declared L's rectangle ladder is finite; if the gated
  well-definedness row fails (too few rungs, zero
  denominators), this word is the honest verdict.
- `POT-BLOCKED-AT-<object>` — instrument fault only.
The head is DERIVED, multi-way, every word demonstrated
emittable via control arms through the real machinery (the
ACT Z1 pattern; no forged rows).

## WALLS

**Confinement vocabulary is licensed FOR THIS UNIT ONLY
and only in measured sentences** — every confinement word
in prose must sit in a sentence carrying its measured
discriminator value; unmeasured confinement talk dies at a
wall gate (paper-23's withholding machinery inverted: from
ban to license-with-teeth).  No SI numbers; no continuum
claims (the R4 rung's, not this unit's); the four walls;
E-22 + E-23 + E-24; the #267 checklist (headers bound as
claims; sentence-level referent binding; spelled numerals;
no typed counts; digest-free whitelists; transcript
full-byte integrity); REFERENT-BOUND gates; the
test-declaration duty; class words gate-bound to computed
predicates (#295).

## STANDARDS

Full era per HANDOFF-PROMPT.md §4/§9; exact arithmetic;
windows declared in-string and licensed; seals at
value-close; read-back before os.replace; derived head with
de-twinned comparator; failing runs write nothing; byte ×2;
#91 off-tree.  Candidate readings until adjudication.
