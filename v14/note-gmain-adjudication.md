# Γ-MAIN — JOINT ADJUDICATION (effectus #76, operator #79, instrument #81)

**Inputs:** the three frozen reviews; the frozen delivery (paper
`d85a629a9378`, code `51c3b4cf3f3c`, output `b2b45be500b7`, receipt
`974f36b1251a`); pin `8529ddc4a319`; protocol `a3a39813e5b5`.
**Standing:** three ACCEPT-WITH-FIXES; ~843 recomputations + 18
executions; zero false numbers anywhere; the delivery byte-reproduced
by two reviewers.  Resolved grade: **ACCEPT-WITH-FIXES**.

## 1. THE CORRECTED SETTLEMENT (three-review convergent)

- **constructed: TRUE** — Γ exists, exact, column-stochastic, rebuilt
  twice by disjoint routes (11,989/11,989).
- **targets hit: FALSE** — triple-confirmed: the targets were census
  statistics at birth (their frozen source defines them by leaf
  counts); the target test is Γ-free and off-carrier (token-scan
  proof); the count readout breaks the mandatory negative control
  (REC gains holonomy; CK fails; 0/544 columns stochastic — not a
  law).  The paper named the wrong failed link.
- **holonomy consistent: TRUE under the well-posed form** — my
  settlement condition hardened the pin's "agreement OR the measured
  deviation" into an agreement demand that is unsatisfiable by an
  identity (r_k = r_q·G/G at 1,546/1,546, deviation forced exactly at
  non-descent).  My pin error, owned.  Under
  REPRODUCED-AND-LOCATED the unit passes.
- **motivation non-empty: TRUE but mismeasured** — I-READOUT's fiber
  is ≥3 (the third, step-normalised law measured: 15/38, 5/19,
  13/38); the inventory rows carry the wrong content.

**SETTLEMENT: PARTIAL — FAILED LINK = TARGETS** (not holonomy).  The
honest statement: the law is constructed, its geometry is reproduced
and its deviation located, its motivation is non-empty — and the
census numbers it was asked to hit were never values of any law.

## 2. THE CARRIER RULING

**CONG-185 supersedes MENU+G**: d74's own coarsest weighted
congruence has descent at every horizon, zero multi-valued edges,
all 44 curvature squares intact, q-holonomy ⟨2,3⟩, **k-holonomy
collapsing back to ⟨2,3⟩** (the enlargement disappears), and exact
lumpability — with the operator's theorem (descent ⟹ r_k = r_q on
every closing square, 2,756/0) as its foundation.  MENU+G descends
but keeps 4 bad targets and CK 8/10.  **Carrier-stamped open, first
class:** on CONG-185 the chain is exactly lumpable (Markov at that
level) while the indivisibility signature lives at MENU — the
quantum character of Γ is carrier-relative, and the next iteration
must measure it at both levels and stamp every quantum-shape claim.

## 3. The instrument findings

Zero input channels (no argv — no selftest, no mutant harness;
unknown flags unreadable; my #73 verification vacuous, owned at
#81); coverage 15/88 honest; 18/36 mutants genuine;
G-VERDICT-EQUALITY the same concatenation twice (the #219 disease at
the #10 gate — the R4 MAJOR-6 twin); four surviving injections
(eq-22 headline flip; motivation reclass; the T1 quotation inversion
— the #62 compliance claim false; prose numbers); INJ12 — the
instrument cannot emit SETTLED (the clean sheet contingent on
PARTIAL); the unresolved Γ-prep probe silently swallowed; the
worktree-paper unanchored read; three false instrument claims in
prose.

## 4. Binding repair orders (R-GM-1 … R-GM-11)

1. **The verdict recomposed** to the corrected settlement (§1
   verbatim as the settlement segment; the failed link TARGETS; the
   readout segments stamped with fiber ≥3 and the third law; the
   B2-block claim scoped 9/113; five missing restriction stamps).
2. **The target section rebuilt**: the census numbers demoted to a
   declared census shadow (control); the law-value comparison run at
   the properly normalised primary readout ON the carrier; TEST 1
   made Γ-dependent and on-carrier or renamed as the external-census
   control it is.
3. **The holonomy gate re-posed as REPRODUCED-AND-LOCATED** (the
   deviation identity a killable must-pass; T2 rebuilt fallible).
4. **A real CLI built**: argv parsing, unknown-flag rejection,
   `--selftest` (corrupt-one-anchor, exit 1, write nothing),
   `--mutant NAME`; the run never overwrites without a channel.
5. **G-VERDICT-EQUALITY rebuilt** with a reconstruction sharing no
   code AND no typed literals with the builder (the #62-strengthened
   standard, §5).
6. **The four surviving injections killed** with declared mutants
   (eq-22 value census; motivation-inventory binding; the paper
   quotation anchors per the corrected #62 spec — the paper itself
   hash-anchored, closing the worktree-read residue; prose-number
   rendering).
7. **The coverage ledger rebuilt** at the #34 standard: honest
   denominators; the 10 assert-unmutated and 8 tautological mutants
   replaced or reclassified; `mutant()`'s hard-coded reaches_target
   computed; anchor falsifiers genuine.
8. **INJ12 closed**: the instrument must be able to emit a SETTLED
   verdict on synthetic payloads with the mutant sheet staying
   clean; the settlement conjuncts each demonstrably two-way.
9. **The Γ-prep probe resolved** (the flat receipt's actual key;
   unresolvable probes must abort, never swallow — per #46's
   spirit).
10. **The three false instrument claims + the output line
    corrected**; the two vacuous mutants replaced; N1's flow-identity
    scope (r = 4−|h|) stated.
11. **Paper rewrite single-threaded**; the next-iteration register
    (CONG-185; the disintegration-criterion readout; law-value
    targets pre-registered from the law; REPRODUCED-AND-LOCATED;
    the [B3] feasibility LP; the lumpability/indivisibility
    carrier-stamp) as the closing section.

## 5. RUNBOOK engravings (this adjudication)

- **§14 addendum (the CLI-contract minimum):** every unit ships an
  argv-parsed CLI that rejects unknown flags (exit 2), a
  `--selftest` that corrupts one anchor, confirms exit 1, and writes
  nothing, and a `--mutant NAME` harness; a runner with no input
  channels or with silent flag-ignoring is undeliverable (third
  recurrence: Γ-prep, R6b′-partial, Γ-main).
- **§14 amendment (comparator independence, strengthened):** a
  verdict comparator shares NOTHING with its builder — neither code,
  nor inputs, nor typed literals; "the same concatenation written
  twice" and "the same literal in both paths" are the measured
  failure modes (Γ-main G-VERDICT-EQUALITY; R4 MAJOR-6).
