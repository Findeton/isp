# R6b′ — HOSTILE REVIEW PROTOCOL (FROZEN)

**Frozen:** 2026-08-09, v14 ledger #58.  **Object:** the R6b′ delivery
as committed (paper `68c20d1fdae4`, code `8e188dd3ab70`, output
`42a39fcaf194`, receipt `50f63b3ba362`; commit 264980a), verified
byte-identical (#49).  **Pin:** `v14/note-r6bprime-transport-pin.md`
(`17111fd19022`).  **Reviewers:** three, independent; single-file
writes (`v14/review-r6bp-{operator,effectus,instrument}.md`);
scratch-only; no imports; read-only git.  OFF-LIMITS: every live
unit's files (paper-11, gprep_*, r3_*, crd_* worktrees; other
reviews).  R6a is TERMINAL (#53) — its terminal receipt
`856f6e810ab5` is the current reference; the unit under review
consumed the #26 committed bytes `022c3f488a93` via git show with
drift disclosed — both hashes are legitimate at their stated
provenance.

## Kill-shots (binding on all three)

**K1 — THE DERIVED KERNEL.**  The first-return law (f(1)=3/4,
f(2)=0, f(n)=(n−2)(3/4)^(n−3)/256, mass 13/16) — re-derive from S1's
pinned chain by your own route; verify the two-route independence
claim; the 400-term tail bound.  Then the derivation's purity: does
anything in the kernel import an undeclared choice (state ordering,
the completed-chain convention, the first-return definition)?

**K2 — THE FIBER-COLLAPSE CLAIM (the campaign-critical number).**
37-of-201, DISTRIBUTION-only, count-4-only, delivery-free-only;
G-DIAG2's complete coverage (the uniform law on 19,683); CR-B's
simplex 2→0; the cross-unit TV = 2/21 reproduction.  Verify each
independently.  Then press the scope: is count-4-only a LAW or an
artifact of S2's censused lengths (3 and 4 only, positions 1 and 2
only — the u1c cap)?  Would the collapse extend or die at count 5?
State what is measurable at citable scope vs blocked.

**K3 — THE DEFECTIVE RENEWAL + THE SEAM.**  The termination
arithmetic (return 13/16; the closed class {2,4,5}; visits 16/3; the
exact support hole at 2 costing 50-of-201).  The BLOCKED-AT-THE-
SCOPE-SEAM head: is the seam genuinely non-innocuous (S1
delivery-free vs S2 transport; the TV 2/63-vs-0 split) or an
artifact of the pin's row selection?  Could a single-scope row set
have been pinned instead — and does the Γ-prep unit now in flight
(which terminalizes the transport-scope foundation) dissolve or
confirm the seam?  Your answer shapes Γ-main's pin.

**K4 — THE COMPOSED HEADS + THE RE-CLASSIFICATIONS.**  Four heads
composing: verify their mutual consistency and the per-head scope
qualifiers.  The R6a re-classification claims: THE-SPLIT →
(ii)-in-distribution (count-4, delivery-free); NEW-FRONT-VALUES →
forced-relative-to-the-split fiber 1; the lift freedom GROWS (a
third rule).  The extremal bar's death (4 record-intrinsic
functionals, 3 distinct selections; the det-blind readout at w=0) —
verify; is non-uniqueness across functionals the right kill
criterion per the D12 standard?  The identification census: the
C1 type error (division-event vs grammar-event count); whether any
identification could be motivated at the pinned rows; the
stabilizer-fixed firsts (orientation, clock rate).

**K5 — INSTRUMENT.**  (a) The 24 verbatim-context anchors — the
first at-scale deployment of the #34-modified kind: corrupt quoted
contexts and their USES; do the consumer-gate bindings carry weight?
(b) The never-falsified census (46 rows, 0 unwaived) audited at the
#34 standard — verify every declared falsifier reaches its gate;
kill any waived gate you can.  (c) Injections: the five verdict
classes; fiber-anchor drifts (the R6a/CR-B receipts); defectiveness-
arithmetic corruption; census-cell drops; the S1-chain two-route
claim (#219).  (d) The provenance handling of the R6a mid-repair
drift (committed-bytes-via-git-show + 12/12 stability gate) —
audit as the candidate STANDARD for concurrent-unit reads.
(e) Paper↔output↔receipt sweep; false numbers by value.  (f)
Two-run byte-identity; repo hashes unchanged after all work.

## Standing rules

Grades ACCEPT / ACCEPT-WITH-FIXES / REJECT.  Findings ranked with
severity, measured evidence, exact repair where definite.  No
numerical claim without recomputation; count recomputations.
Reviews frozen as delivered; disagreements are the adjudicator's.
