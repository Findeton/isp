# SCOUT-T PIN ADDENDUM (FROZEN) — THE #87 CORRECTIONS

**Frozen:** 2026-08-17, v15 ledger #87.  Amends the pin 3f35573d88d8.

1. W-REPRESENTATION IN ITS #86 FORK-NEUTRAL FORM binds (replacing
   any #84-form citation): no supplied object promoted to ontology
   by convenience; an ontic trigger/trace remains ADMISSIBLE if
   independently declared, operationally distinguished, mapped to
   the beables.  Your unit tests whether a trace COULD be real — it
   must not presuppose either answer.
2. THE MINIMALITY LADDER (replacing the single full-trace test):
   arm 1 runs the kernel family at FIVE trace grains, ordered by
   information content — (a) no trace (the T-blind parent
   reproduction); (b) previous trigger only; (c) unordered trigger
   counts; (d) finite suffixes (length 2 if distinct from (b));
   (e) the full ordered trace.  Publish the feasibility verdict PER
   GRAIN with certificates/dimensions; the honest headline names
   the COARSEST feasible grain.  "Full trace sufficient" is never
   called minimal; SCOUTT-TRACE-SUFFICIENT carries its grain:
   SCOUTT-TRACE-SUFFICIENT-<grain>-<dim>-AT-<depth>.
3. Verdict language: TRACE-SUFFICIENT at any grain is FEASIBILITY
   (a bridge exists), never nature — already your wall; restated
   here against the ladder.
