# SCOUT-PSI — THE EQUAL-DENSITY / DIFFERENT-PREPARATION TEST — PIN (FROZEN)

**Frozen:** 2026-08-17, v15 ledger #65 (ordered by the tenth external
review via the user; decisive computation (ii) of the #64 program).
Scout class: note + code + output + receipt; battery + ONE hostile
verifier seat.

## SOURCES (binding anchors)
- v15/note-scout-bridge.md + v15/code/scout_receipt.json (committed
  #58; under repair for OTHER sections — consume the S4 apparatus by
  anchor at the committed digests 34f10a6fd494/12bdb7a58909, or their
  repaired successors if the repair lands first; state which): the S4
  formalization of paper-20's record-Born-selected-cell-while-
  uncollapsed rule, the nonlinearity witness and its closed form
  -(1/4)(w_c(rho0)-w_c(rho1))(P0-P1), the Q(omega) arithmetic.
- v14/paper-20-coupling.md:633 region — the delivered rule itself,
  quoted verbatim.
- v15/LOG.md #64 — the sharpened fork (binding language: the
  ontic-psi branch is an ORDINARY stochastic process over
  configurations; distributions over ontic psi evolve linearly;
  nonlinearity lives only in the compressed rho description).

## THE QUESTION
Two preparations, one rho: does the DELIVERED dynamics predict
different future record distributions?  This is the operational
content of the rho-complete-vs-incomplete dichotomy (#64 item 2).

## THE METHOD
1. Build >= 2 distinct pure-state decompositions of ONE committed
   rho (start from the S4 witness family; add a second rho for
   robustness — decompositions must be genuinely distinct as
   ensembles, stated exactly).
2. Propagate the DELIVERED update exactly (per-branch: the rule as
   paper-20 declares it, applied to each pure member with its weight)
   at windows of 1, 2, 3 steps (deeper registered, not claimed).
3. Compare the resulting RECORD DISTRIBUTIONS exactly over Q(omega):
   equal at every window, or a first divergence exhibited (the
   witness pair, the window, the distributions, the difference).
4. CONTROLS through the real machinery: (a) NULL — a genuinely
   linear CPTP completion (any declared one, e.g. the selective
   collapse reading paper-20 names as not-run, or an
   outcome-dependent instrument) run on the SAME decompositions must
   be BLIND at every window; (b) POSITIVE — a synthetic
   decomposition-sensitive map must read SENSITIVE.  Both gated.

## OUTCOMES (#299 feasibility against the committed S4 rows)
- SCOUTPSI-DECOMPOSITION-SENSITIVE-AT-<window>: reachable — the S4
  nonlinearity witness makes sensitivity the expected case; the
  outcome carries the exact witness pair and first divergence.
- SCOUTPSI-DECOMPOSITION-BLIND-AT-<window>: reachable if the
  nonlinearity cancels in the record marginal at the tested windows
  — would be a major surprise, stated as such with the cancellation
  mechanism exhibited.
- SCOUTPSI-BLOCKED-AT-<object>: instrument fault only.

## WALLS
THIS UNIT DECIDES NO ONTOLOGY: it measures a property of the
delivered rule; the fork's resolution (replace the rule vs declare
rho incomplete) is a program decision informed by this measurement —
candidate language gated; no sentence may claim psi IS ontic or rho
IS incomplete.  W4-TYPE-IDENTITY with term-binding table (CELL-HIT /
division event / preparation / ensemble / record distribution); the
sample-space spine per claim; NUMERAL-FIELD BINDING from birth; the
receipt hazard (no environment-dependent bytes in either artifact;
byte-stable across PYTHONHASHSEED 0/1/424242 and alien CWDs in a
git-less mirror from deleted artifacts); iteration-order determinism
(sorted() discipline; no bare set-iteration or listdir into anything
serialized); the four prior species (atomic slash-rationals,
NEG-guard robustness, structural licence ids, W1 subject policing) +
hyphen-robust wall patterns + heading-aware sentence splitting;
E-34; exact arithmetic only; write-nothing failures; registry
falsifiers with move proofs; hostile argv rc 2; object-under-test
digest in both artifacts; candidate readings until the verifier
seat + adjudication.

## STANDARDS
Era template at scout scale.  Deliverables: v15/note-scoutpsi.md,
v15/code/scoutpsi_exact.py, v15/code/scoutpsi_output.txt,
v15/code/scoutpsi_receipt.json.
