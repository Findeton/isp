# SCOUT-T — THE TRIGGER-TRACE FORK: IS THE MISSING DATUM THE ORDERED TRIGGER HISTORY? — PIN (FROZEN)

**Frozen:** 2026-08-17, v15 ledger #82 (thirteenth external review via
the user).  Scout class: note + code + output + receipt; battery + one
verifier seat.  Runs fork arms (1) and (2) of the #82 four-way fork,
AHEAD of any add-psi conclusion.

## SOURCES
- v15/LOG.md #74/#81/#82 — SCOUT-K's sealed-pending result, its
  verifier, and THE TRIGGER-ERASURE MECHANISM (binding: the bridge's
  triple record forgets which cell triggered; the walk remembers;
  scoutk_exact.py:1010 region anchored).
- SCOUT-K's committed apparatus (code 38c3f6cb288e at #74, or its
  micro-repaired successor if landed — snapshot which, per the #58
  precedent) — the arena, group, walk, orbit machinery, conditional
  consistency mode: REUSED AS PARENT by anchor.
- The #68 SCOUT-K addendum (3a1e5a649537) — the consistency-mode
  freeze binds here identically.

## THE QUESTION
SCOUT-K measured that K(e|c,G,R) fails because the record erases the
trigger identity.  Two repairs are computable now:
ARM 1 (TRIGGER REAL): does a minimal covariant kernel
K(e|c,G,R,T) — T the ORDERED TRIGGER TRACE (the sequence of past
trigger cells, exactly what the bridge erased) — preserve the
delivered walk at the committed windows?  T enters the orbit space
(orbits of (G,R,T,c,e) under simultaneous relabelling); one variable
per orbit; conditional consistency mode; depths 2 and 3.  If
NONVACUOUS/UNIQUE: the hidden memory is PRICED ONTOLOGICALLY (a new
state component, counted as a declaration, its scope stated).
ARM 2 (TRIGGER BOOKKEEPING): per-latent-history equality may be too
strong — build the MARGINAL-HISTORY comparison: an exact factorizing
history-level process (Markov in the complete successor, latent
triggers summed out) compared to the walk's OBSERVABLE record
distributions only, at the same windows; exact polynomial method
with certificates per the #68 freeze (the linear-relaxation refusal
binds).
CROSS-CHECK (mandatory): the T-blind subfamily of arm 1 must
reproduce SCOUT-K's refusal (binds to the parent).

## OUTCOMES (#299 feasibility argued against SCOUT-K's committed rows)
- SCOUTT-TRACE-SUFFICIENT-<dim>-AT-<depth> (arm 1 nonvacuous — the
  minimal memory suffices; psi/rho NOT forced by this axis) /
  SCOUTT-TRACE-UNIQUE-AT-<depth> (a candidate law, candidate-marked)
- SCOUTT-TRACE-INSUFFICIENT-AT-<depth> (arm 1 empty with
  certificates — the erasure reading is NOT the whole story; the
  state-slot question reopens toward process/quantum state)
- SCOUTT-MARGINAL-AGREES-AT-<depth> / SCOUTT-MARGINAL-DISAGREES-AT-
  <depth> (arm 2 — whether the distinction disappears after correct
  marginalization)
- SCOUTT-BLOCKED-AT-<object> — instrument fault only.
Controls: forced both ways through the real solver; the T-blind
reproduction of the parent refusal.

## WALLS
The #82 precise-formulation language for the parent result (never
wider); trigger stays CANDIDATE-MARKED (arm 1 tests a bridge, it
does not establish physical triggering — even TRACE-SUFFICIENT is a
FEASIBILITY fact, not an ontology decision; gated); W4 term-binding
(CELL-HIT / division event / trigger / trace / successor);
sample-space spine; numeral per-occurrence TOTALITY from birth;
G-ENV-EXCLUSION + G-AST-DETERMINISM + G-KERNEL-WALL ported from
birth; the four newest species + hyphen robustness + heading-aware
splitting; E-34; exact arithmetic; byte x2 deleted-artifact
git-less alien-CWD 3 seeds incl. the LOG-append probe; registry
falsifiers with move proofs; hostile argv rc 2; object-under-test
digest; candidate readings until the verifier seat.

## STANDARDS
Era template at scout scale.  Deliverables: v15/note-scoutt.md,
v15/code/scoutt_exact.py, v15/code/scoutt_output.txt,
v15/code/scoutt_receipt.json.
