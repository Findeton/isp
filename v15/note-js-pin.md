# JS — THE JOINT-SUCCESSOR UNIT (PHASE ONE: STEPS 1-5) — PIN (FROZEN)

**Frozen:** 2026-08-17, v15 ledger #101 (the nineteenth review via the
user; the user's instruction: investigate in a new runner).  Unit
class: scout-plus (note + code + output + receipt; battery + one
hostile verifier seat; phase two = steps 6-8 registered, not run).

## THE OBJECT
One covariant boundary-to-boundary law making one complete
commitment: (R,G,S) -> {p_alpha; R'_alpha, G'_alpha, S'_alpha,
record_alpha} over MUTUALLY EXCLUSIVE COMPLETE SUCCESSORS (an atomic
transaction: which pair relations change, what permanent record is
created, how geometry changes, how the process state transports —
together).  The step = a finite causally complete relational region
between GENUINE division boundaries; never a global tick.

## SOURCES (anchored)
LOG #98/#101 (the diagnosis, the fork, the caveats); the sealed
scouts (BRIDGE/K/PAIR/PSI) for every constraint they measured; the
DC addenda (causality v2 ca713e89633b; ontology v3 7df6120f585f);
the w5 Barandes recast; the v13 gravity prospectus (the Double Weld
target) and the HA transport lesson — by anchor where cited.
External context anchors (quoted, not load-bearing): Quantum Causal
Histories (hep-th/0302111 — the acknowledged fixed-causal-set gap);
Rideout-Sorkin (gr-qc/9904062 — birth-order gauge; measure on
histories; Bell causality NOT importable); Oeckl (hep-th/0509122);
Simon-Buzek-Gisin (quant-ph/0102125); Barandes (2302.10778).

## THE CHARGES (the review's steps 1-5, with the #101 caveats frozen)

S1 — BOUNDARY AND STATE DEFINITIONS + THE COMPRESSION MEASUREMENT.
Define exactly: relational region, incoming/outgoing boundary,
GENUINE division boundary (where conditioning is physically valid),
complete outcome, allowed preparations.  THE PREDICTIVE-EQUIVALENCE
STATE, PASSIVE-FIRST (caveat C1): h ~ h' iff every future
record-and-geometry observation distribution agrees at every
DECLARED PREPARATION (the intervention extension REGISTERED as
successor, never smuggled).  COMPUTE the equivalence classes at the
committed arena over the declared window range; THE STABILIZATION
PREDICATE FROZEN BEFORE MEASUREMENT (caveat C2): class count at
window k for k in the declared range; stabilized iff the count and
the classification of every reached history are k-to-k+1 invariant
over the declared tail.  Verdict: JS-STATE-RG-SUFFICIENT /
JS-STATE-FINITE-MEMORY-<dim> / JS-STATE-RHO-LABELLED /
JS-STATE-DECOMPOSITION-SPLIT (the ontic-psi branch, owing the
steering theorem — cite PSI) / JS-STATE-NO-FINITE-COMPRESSION-AT-
<range> (with the growth curve published).  Expected per the sealed
corpus: NOT RG-sufficient (PAIR S4); the psi-separation of collision
classes (PAIR S3) bears on the split branch — re-derive, never copy.

S2 — THE PAIR SEED ON UNLABELED MOTIFS.  On 2-, 3-, and 4-actor
motifs (NOT the nine-actor chart first — arena naturality is a
requirement, restriction 3): enumerate the most general local,
relabeling-covariant pair rewrite updating relation + geometry +
state TOGETHER; parameterize exactly; publish the family dimension
per motif.  THE NATURALITY TENSION (caveat C3) exhibited: the
delivered walk is anchored (SCOUT-K) and violates naturality — a
result, stated.

S3 — THE VARIABLE CARRIER.  Complete successors represented with
direct-sum output spaces and outcome-dependent transport
J_alpha: A_{R,G} -> A_{R',G'}; probability = trace of output;
normalization over the direct sum; positivity/Choi constraints
imposed OPERATIONALLY on the complete process (restriction 8's CP
discipline — never blindly on an arbitrary reduced subsystem).
This is the OPERATIONAL ENVELOPE, not an ontic commitment
(W-REPRESENTATION binds).

S4 — THE DISJOINT DIAMOND.  Four actors, AB and CD: both
construction orders must yield the IDENTICAL complete output after
canonical transport (construction-order gauge, restriction 5);
probabilities need NOT factor (Bell-aware).  Exact; certificates.

S5 — THE OVERLAP-FACTORIZATION TEST (the decider).  Three actors,
AB and BC: determine whether the joint map (a) FACTORS through a
genuine intermediate division boundary (two pair processes), (b)
COMMUTES as a simultaneous compatible collection without a boundary,
or (c) REQUIRES an irreducible triple map.  This is the first honest
experiment separating pair-native / triple-native / adaptive arity —
the law-selected arity definition applied: process arity = the
smallest support whose law cannot factor through any genuine
boundary.  Verdicts: JS-OVERLAP-FACTORS / JS-OVERLAP-COMMUTES /
JS-OVERLAP-IRREDUCIBLE-<support>, each with certificates, each per
declared state grain.

## THE REQUIREMENT SET (the 13 restrictions, all gated where testable at this scale)
1 complete-outcome normalization (E-34.4); 2 relabeling covariance
(declared tie-breaks counted); 3 arena naturality (the same motif,
the same law at every embedding — no global n/q/chart dependence
unless locally physically available); 4 input-relative locality (a
created relation cannot retroactively justify its own reach); 5
construction-order gauge; 6 PHYSICAL factorization only (Kraus
decompositions and inserted clock slices are NOT subdivisions); 7
predictive-state minimality (the S1 stabilization predicate); 8
operational CP + the causality addendum's no-signalling gate; 9
record permanence (isometric dilation ideal; irreversibility =
dispersal into records); 10 geometry consistency
G_order/count = G_composition (phase-two target, REGISTERED); 11
transport and constraint preservation (the v13 HA lesson anchored);
12 universal backreaction (phase-two, REGISTERED); 13 empirical
regression (the walk recoverable as fixed-background representation;
its incompatible record update NOT frozen in — gated language).

## WALLS
W-REPRESENTATION (fork-neutral, #86 form); the #98 narrowings (the
clocked-refusal scope; SCOUT-T language); the sample-space spine;
E-34 + the full term-binding table (region / boundary / division
event / complete successor / process state / preparation); numeral
per-occurrence totality from birth; the receipt-wide hex inventory
+ G-AST-DETERMINISM + G-KERNEL-WALL from birth; the newest species
(all registered TPL-2 items); exact arithmetic; byte x2
deleted-artifact git-less alien-CWD 3 seeds + the LOG-append probe;
registry falsifiers with move proofs; hostile argv rc 2;
object-under-test digest; candidate readings until the verifier
seat.  NO sentence decides the ontology forks; the compression
verdict is a MEASUREMENT feeding the user's decision.

## STANDARDS
Era template.  Deliverables: v15/note-js.md, v15/code/js_exact.py,
v15/code/js_output.txt, v15/code/js_receipt.json (+ disclosed
snapshots for any mid-repair source).  Phase two (geometry weld /
quantum-composite gate incl. HJW / scaling gate) REGISTERED.
