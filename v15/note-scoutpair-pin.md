# SCOUT-PAIR — RECORD-ARITY vs PROCESS-ARITY: THE RE-TYPING AUDIT, THE ORDER-DEFECT CURVATURE CENSUS, AND THE PAIR-SEQUENTIAL FACTORIZATION TEST — PIN (FROZEN)

**Frozen:** 2026-08-17, v15 ledger #84.  Scout class: note + code +
output + receipt; battery + one verifier seat.  The computable core
of the #84 hybrid ("pairwise actuality, n-body indivisibility,
geometry as the memory of realized relations").

## SOURCES
v15/LOG.md #84 (the framework, binding language); the DC ontology
addendum (digest at commit); the committed corpus wherever read, by
anchor: paper-19/paper-41 (the triple event and its three-cell
footprint), paper-20 (CELL-HIT), the ARITY/ARITY-16 sealed tables
(note: ARITY-16's files are under repair — bind its COMMITTED #47
digests or the repaired successors, state which), SEC-2 (the seam
algebra), SCOUT-K's committed apparatus (arena, group, walk — by
anchor or snapshot).

## THE CHARGES
S1 — THE RE-TYPING AUDIT: formalize RECORD-ARITY (=2: every record
atom is a pair-relation datum — verify this against the committed
cell structure: cells as (site,link) pair-incidences; the triple
event as a joint write of three pair atoms) and PROCESS-ARITY (=3
committed; ARITY's dial re-read as the PROCESS dial).  Sweep the
live corpus (papers 19/20/33/35/39/40/44/50 + the v15 scouts) for
every load-bearing use of "arity"/"event size"/"a" and classify
which arity each means; report any site where the two are conflated
BEYOND the already-diagnosed type-error arc (expected: the known
sites; any NEW conflation is a finding).  Deliver the E-34
term-table entry for the split.
S2 — THE ORDER-DEFECT CURVATURE CENSUS (the smallest curvature
probe, measured): at the committed arena, enumerate overlapping
pair-change compositions (AB then BC vs BC then AB at a shared
actor, over the committed update algebra — the walk step and the
record write, both) and measure the ORDER DEFECT exactly: which
pairs commute (order physically irrelevant), which do not (order =
causal structure), and the defect's value where nonzero.  Disjoint
pairs: verify commutation (the causal-layer/concurrency structure).
Publish the full commutation table and the defect census; W3
labels: which results are arena-general vs committed-arena facts.
S3 — THE PAIR-SEQUENTIAL FACTORIZATION TEST: does ANY divisible
Markovian pair-sequential law (a sequence of independently
conditioned single-pair-cell transitions, arbitrary order policies)
reproduce the committed triple-write statistics at the declared
windows?  Exact; certificates for every refusal; the #68
consistency-mode discipline binds (conditional mode primary; no
linear relaxation over histories).  Expected refusal per the
indivisibility thesis — but measured, not assumed; a SURPRISE
feasibility would be a major finding against the n-ary-process
reading.

## OUTCOMES (#299 against the committed rows)
- SPAIR-RETYPING-CONSISTENT (+ the term table; any new conflation
  sites listed) / SPAIR-RETYPING-BREAKS-AT-<site>
- SPAIR-DEFECT-CENSUS (the table itself is the outcome: commuting /
  non-commuting / defect values; the three-actor curvature-probe
  reading SUPPORTED or REFUTED at this arena — supported iff
  nonzero defects exist and require a third actor)
- SPAIR-SEQUENTIAL-REFUSED-AT-<window> (certificates) /
  SPAIR-SEQUENTIAL-FEASIBLE-<policy> (a major surprise, stated so)
- SPAIR-BLOCKED-AT-<object>.
Controls: forced both ways where solvable; synthetic commuting and
non-commuting algebras through the real census machinery.

## WALLS
W-REPRESENTATION (no promotion of K/psi/H to ontology; the census
measures STRUCTURE, decides no ontology — the hybrid remains the
user's adopted reading, candidate-marked); the #84 language for the
framework (three levels never conflated); W4 term-binding (CELL-HIT
/ division event / pair atom / process event / trigger / trace);
sample-space spine; numeral per-occurrence TOTALITY from birth;
G-ENV-EXCLUSION + G-AST-DETERMINISM + G-KERNEL-WALL from birth; the
newest species (slash-rationals, NEG-guard, structural licences, W1
subjects, hyphen robustness, heading-aware splitting); E-34; exact
arithmetic; byte x2 deleted-artifact git-less alien-CWD 3 seeds +
the LOG-append probe; registry falsifiers with move proofs; hostile
argv rc 2; object-under-test digest; candidate readings until the
verifier seat.

## STANDARDS
Era template at scout scale.  Deliverables: v15/note-scoutpair.md,
v15/code/scoutpair_exact.py, v15/code/scoutpair_output.txt,
v15/code/scoutpair_receipt.json.
