# SCOUT-K — THE RECORD-DEPENDENT LOCALLY COVARIANT KERNEL CENSUS — PIN (FROZEN)

**Frozen:** 2026-08-17, v15 ledger #60 (ordered by the ninth external
review via the user; the calculation that decides whether the kernel
route survives backreaction).  Scout class: note + code + output +
receipt; battery + ONE hostile verifier seat.

## SOURCES
- v15/LOG.md #58/#59 — the fixed-alpha result and ITS HONEST SCOPE
  (binding: "no record-blind, fixed-alpha, affine-equivariant kernel
  preserves the delivered three-step walk statistics" — cite only
  this form; the general no-go is OPEN and is THIS unit's question).
- v15/code/scout_exact.py (committed, edb60bccd22e) — the walk
  builder, the relabelling group (order 108), the three-step
  consistency apparatus: REUSE AS PARENTS, by anchor, never silently.
- The scout's ECC snapshots (61d330d13fe0 / ea24c1fc2340) for any LP
  reads; v14/paper-20-coupling.md for the walk law; the pinned
  committed arena.

## THE QUESTION
Does any LOCAL, RECORD-DEPENDENT kernel K(e|c,G,R), equivariant in
the true sense K(ge|gc,gG,gR)=K(e|c,G,R) under SIMULTANEOUS
relabelling of all four slots, preserve the delivered walk statistics
at exact multi-step consistency — or is the emptiness of #58's
narrow family in fact general?

## THE METHOD (normative)
1. Enumerate the LOCAL RECORD TYPES actually reached at the committed
   windows (depth 2 and 3 from the delivered start; the reach census
   published — how many types, which).
2. Form the ORBITS of (G,R,c,e) under simultaneous relabelling; ONE
   FREE VARIABLE PER ORBIT.  Publish the orbit count per depth — this
   is the honest dimension of the covariant kernel family, vs the
   fixed-alpha family's 1.
3. Impose, as separate legs: support (c in e); locality (K reads only
   the declared neighborhood — see 4); record-dependence THROUGH
   ORBIT STRUCTURE ONLY (that is what covariance means here);
   normalization per trigger; exact multi-step walk consistency at
   depth 2, then 3 (deeper depths registered as successors, not
   claimed).
4. PROXIMITY AS A DECLARED FORK, each arm run or refused with its
   cost counted as a declaration: (a) shared-actor (the scout's
   minimal choice); (b) record-distance; (c) metric/count distance;
   (d) causal neighborhood.  No silent choice; if arms coincide at
   this arena, the coincidence is measured and said.
5. Solve each system EXACTLY (rational LP / linear algebra; Farkas
   certificates for every emptiness; polytope dimension for every
   non-emptiness; uniqueness checked when dim 0).

## OUTCOMES (#299 feasibility argued against the committed rows)
- SCOUTK-COVARIANT-EMPTY-AT-<depth>-<arm> (certificates published) —
  would generalize the fixed-alpha refusal to the real family;
- SCOUTK-NONVACUOUS-<dim>-AT-<depth>-<arm> — the kernel route LIVES;
  the family published, nothing selected within (EQUIVARIANT-OR-
  DECLARED holds for any tie-break);
- SCOUTK-UNIQUE-AT-<depth>-<arm> — a candidate event-selection LAW,
  the first in the corpus; stated as candidate, not adopted;
- SCOUTK-BLOCKED-AT-<object> — instrument fault only.
Controls: forced-empty and forced-nonvacuous synthetic systems
through the real solver; the fixed-alpha family re-derived as a
1-dimensional SUBFAMILY and shown to reproduce #58's refusal (the
consistency check binding this unit to its parent).

## WALLS
The #59 downgrade language (gated verbatim); W4-TYPE-IDENTITY with
the term-binding table (CELL-HIT / division-event / trigger /
successor bound); the sample-space spine (every probability claim
declares CELLS / TRIPLE-EVENTS / COMPLETE-SUCCESSOR-CONFIGURATIONS);
NUMERAL-FIELD BINDING FROM BIRTH: every load-bearing prose numeral
bound to its SPECIFIC receipt field by a gate; the four newest
species (atomic slash-rationals, NEG-guard robustness, structural
licence ids, W1 subject policing); E-34; exact arithmetic, no
floats, no builtin hash; byte x2 from deleted artifacts in a
git-less mirror at alien CWDs and 2+ seeds — THE RECEIPT INCLUDED
(the scout's double_build_digest defect is the named hazard: no
environment-dependent value may enter either artifact); write-nothing
failures; registry falsifiers with move proofs; hostile argv rc 2;
object-under-test digest in both artifacts; candidate readings until
the verifier seat + adjudication.

## STANDARDS
Era template at scout scale.  Deliverables: v15/note-scoutk.md,
v15/code/scoutk_exact.py, v15/code/scoutk_output.txt,
v15/code/scoutk_receipt.json.
