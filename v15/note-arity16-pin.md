# ARITY-16 (PAPER-50) — THE a-AXIS AT THE SECOND ARENA: n=16, THE F4-LINEAR PRE-REGISTRATION — PIN (FROZEN)

**Frozen:** 2026-08-16, v15 ledger #43.  Unit paper-50-arity16; code
v15/code/arity16_exact.py (+ output + receipt).  A SIDE UNIT beside
ECC (not on the ECC→DC→EXCITATION spine; consumes nothing from ECC).

## PARENTS (sealed)

- paper-44-arity.md 0d677a4cbe97 (TERMINAL #39): the a-only rule and
  its two-level aggregate; THE MODULUS THEOREM nL/gcd(nL,n)=L with
  mod-a iff a=L; a=q assumption-selected via the subgroup lattice
  1|3|9; the a=2 obstruction-vanishing theorem C(a,2)−maxcut(K_a).
- paper-39-ndep.md e2293b8c3858 (v14 TERMINAL #352): the transport
  machinery, the n=16 window (505,920/0/49,536), the three
  numeral-free laws verbatim at n=16; ndep_receipt.json 29216cea946f
  read by PATH for every n=16 substrate count consumed.
- v15/LOG.md #11 (the SHARPENED registration, quoted verbatim in the
  unit): the additive group of AG(2,4) is C2^4 with subgroup orders
  1,2,4,8,16 = ALL divisors of 16 — n=16 does NOT separate
  divisibility from abstract-subgroup availability; it separates
  divisibility from F4-LINEAR-subspace cosets (sizes 1,4,16); the
  successor must pre-register WHICH "subgroup" it means.

## THE QUESTION

ARITY's two theorems and its a=q conditional, run at the ONE other
arena the corpus owns.  PRE-REGISTERED READING (primary): "subgroup"
means F4-LINEAR SUBSPACE of AG(2,4) — coset sizes 1|4|16, the exact
analogue of the parent's F3 route (1|3|9).  DISCLOSED ALTERNATIVE
(reported beside, never merged): abstract C2^4 subgroups — sizes
1|2|4|8|16.  Both readings run; their DISAGREEMENT is a result.

1. THE a=q CONDITIONAL AT q=4: under the F4-linear reading the only
   nontrivial coset order is 4 — the conditional predicts a=4.  Under
   the abstract reading the lattice offers 2|4|8.  Measure which
   assumption set selects what, exactly as the parent's §-principles
   census did (5 principles, per-size admission table at all 16
   candidate sizes).
2. THE MODULUS THEOREM AT THE NEW ARENA: nL/gcd(nL,n)=L claims
   arena-independence and was verified at 320 declared pairs — here
   the theorem's L is AG(2,4)'s declared link count; the budget
   reading's ladder measured directly; mod-a iff a=L tested where
   a and L can differ.
3. THE a-ONLY RULE: the parent's 6 statements + 7 numerals
   transported n=9→n=16 at matched arities where both arenas admit
   the object (a=2 and a=4 exist as cosets under readings; a=3 at
   n=16 is PURE EXTENSION FAMILY — 3 does not divide 16, one idle
   actor MINIMUM — the committed grammar cannot run there and that
   refusal is itself a measurement, stamped, never patched).
4. THE OBSTRUCTION FORM C(a,2)−maxcut(K_a) = 0|1|2|4 at a=2|3|4|5:
   arena-independent by inspection — VERIFIED at the new arena's
   realised seam census, not re-derived.

## OUTCOMES (#299: feasibility against NDEP's committed n=16 window and ARITY's sealed tables)

- A16-CONDITIONAL-TRACKS-THE-FIELD (the F4 reading admits a=4 alone
  — reachable: the lattice arithmetic is forced) /
  A16-CONDITIONAL-READING-DEPENDENT (the two readings select
  different sizes — reachable and the sharpened registration's
  expected case) / A16-CONDITIONAL-FAILS-<row>
- A16-MODULUS-THEOREM-HOLDS / A16-MODULUS-THEOREM-FALSIFIED-AT-<arena>
  (either is a headline; the theorem is the parent's, tested here)
- A16-TRANSPORT-<table> (statements/numerals two-level, the parent's
  own aggregate discipline, rule-relative with every rule published)
- A16-BLOCKED-AT-<object> — instrument fault only.
Scope stamps: extension-family wherever the packing rule is the
unit's own declaration (a=3 at n=16 ALWAYS; a=5 windows); window caps
declared up front; NDEP substrate counts consumed by PATH with
consumer gates, never recomputed silently.

## WALLS

The parent's eight walls verbatim with live licence legs (gated, NEG
guard); W3 instrumented (family-level vs member-specific
auto-labelled — n now takes two values, so EVERY headline says which
n it holds at); no continuum claims; no SI; exact arithmetic; no
builtin hash; E-25…E-33; #295; #119; #299 row-list feasibility;
sentence referent binding; test-declaration duty; the TPL-2 disease
gates as in the ECC pin; the spelled-numeral gate with the parent's
declared exclusion; candidate readings until adjudication.

## STANDARDS

As the ECC pin's STANDARDS section, verbatim era template.
Deliverables: paper-50-arity16.md, code/arity16_exact.py,
code/arity16_output.txt, code/arity16_receipt.json.
