# SCOUT-BRIDGE — THE PRE-DC SCOUT: SAMPLE SPACES, THE PUSHFORWARD CEILING, THE KERNEL, AND THE Delta-B GATE — PIN (FROZEN)

**Frozen:** 2026-08-16, v15 ledger #53 (user-ordered at #52; SMALL by
order — a scout, not a paper).  Unit: v15/note-scout-bridge.md (report
NOTE, no paper number) + v15/code/scout_exact.py (+ output + receipt).
Review plan: orchestrator battery + ONE hostile verifier seat (declared
here; not a three-seat panel — the smallness is the user's order).

## SOURCES (read before writing anything)

- ECC delivered artifacts (paper-46 61d330d13fe0, receipt ea24c1fc2340
  — the LP rows, the FOUR COMMITTED-CLASS DUAL/FARKAS CERTIFICATES
  with gaps 4|4|3|7/3, the 156-row census, the incidence matrices).
- v12/note-w1p-three-class.md:344 — Delta-B is NOT a divisibility
  measure (WALL: no sentence may treat it as one).
- v12/note-w5-barandes-recast.md:120 — the corpus's careful Barandes
  recast; his framework retains ordinary probability, definite
  configurations, single-time distributions; sparse = allowed
  CONDITIONING TIMES.  The scout must not repeat the corrected
  mischaracterization (#52 item 3).
- v10/note-d44-d45-campaign-synthesis.md:60 — the sector conditional's
  actual scope (horizon-stable, between completion sectors, at a
  fixture) — precedent for a different object; cite it as such.
- v14/paper-38-epr.md:275 — record-completeness is by-construction for
  the censused catalogue (do not inflate).

## THE FRAMING SPINE (S0, mandatory)

EVERY route's every probability claim DECLARES ITS SAMPLE SPACE FIRST:
CELLS / TRIPLE-EVENTS / COMPLETE-SUCCESSOR-CONFIGURATIONS.  The report
note's summary is a route x sample-space x verdict table.  A claim
that changes sample space mid-argument is the disease this scout
exists to prevent (gated: W-SAMPLE-SPACE-DECLARED).  This is DC's
first question (#52 order d) being answered in advance.

## THE CHARGES

S1 — THE PUSHFORWARD THEOREM.  Formalize: an ordinary positive
probability law over triple-writing histories, pushed forward at the
committed read point to "which triple event occurred," is an event
distribution p(e); IF the bridge form requires occupancy marginals
Mp = 3q THEN the 1/3 ceiling applies unchanged.  Prove exactly at the
committed arena (the ECC incidence matrix, exact arithmetic); state
the general form.  Then the ESCAPE MENU, each formalized minimally as
what-changes-in-the-LP: (a) no instantaneous triple-event variable;
(b) Born weights as ENDPOINT statistics, not occupancy; (c) a
non-additive history measure (quantum measure / decoherence
functional) replacing ordinary probability; (d) record-writing only at
selected division events.  Outcomes:
SCOUT-PUSHFORWARD-CEILING-INHERITED (with the per-escape formal menu)
/ SCOUT-PUSHFORWARD-ESCAPE-EXHIBITED-AT-<assumption> (if one of (a-d)
formalized at this arena actually breaks the theorem, shown by
construction).

S2 — THE KERNEL.  K(e|c,G,R), support c in e, on the committed arena.
Impose, in this order, each a declared leg: (i) EQUIVARIANCE under the
relabelling group (or a declared tie-break per
EQUIVARIANT-OR-DECLARED — declared means counted); (ii) LOCALITY (K
reads only the declared neighborhood of c in (G,R) — the committed
adjacency, stated); (iii) FULL RECORD-UPDATE CONSISTENCY: the other
two cells the selected event writes get their statistics FACED — a
DECLARED FORK with no silent arm: trigger-cell-only-quantum /
all-write-one-measured / full-statistics-agreement — each arm's
constraint set built and tested.  MUST show the constraint set is
strictly stronger than ECC's disclosed vacuous free-two-stage (that
vacuity is the null).  Outcomes: SCOUT-KERNEL-EMPTY-AT-<arm> /
SCOUT-KERNEL-NONVACUOUS-<arm>-<dim> / SCOUT-KERNEL-UNIQUE-AT-<arm>.

S3 — THE Delta-B GATE (conditional charge).  The identification
"the ECC excess is Delta-B" is ATTEMPTED ONLY IF all four
pre-conditions can be built: (1) the map targets THE FULL DUAL
CERTIFICATE of each committed class (4|4|3|7/3), not the 1/9
threshold excess; (2) invariance under the allowed factorization/cut
choices (the I*U degenerate cut included — Delta-B=0 there while the
obstruction stands, so any surviving identification must not depend
on a cut); (3) the two COUNTERCONTROLS run and survived: the
basis-state permutation (qmax=1, zero interference — ceiling
violation WITHOUT Delta-B) and F3,F3 (nonzero Delta-B, uniform
intermediate at the cap — Delta-B WITHOUT excess); (4) a feasibility
PREDICTION across the complete 156-row census, scored.  If ANY
pre-condition fails to build or any control kills:
SCOUT-DELTA-B-IDENTIFICATION-KILLED-AT-<condition>, retired on the
record.  Language wall: killable-conjecture register only — the words
"probably," "likely," "explains" are refused in S3 sentences (gated).

## WALLS

W-SAMPLE-SPACE-DECLARED (S0, gated per claim); the Delta-B-not-
divisibility wall (source line anchored, consumer gate); the Barandes
recast wall (any sentence about indivisibility must be consistent with
the w5 recast — conditioning-time sparsity, NOT missing single-time
distributions; gated by verbatim anchor consumption); no route claims
to EVADE the no-go without naming which sample-space change or escape
assumption it purchased (gated); the ECC hold respected — this scout
CONSUMES ECC's committed artifacts as delivered-candidate values
(their panel-verified status stated, their seal status stated as
HELD); no DC pin material beyond the report's fold-in recommendations;
era standard walls (exact arithmetic, no hash, no floats, byte x2,
write-nothing failures, registry falsifiers with move proofs, hostile
argv rc 2, object-under-test digest, atomic slash-rational scanning —
the four newest species gated).  Candidate readings until the verifier
seat + user read it.

## STANDARDS

Era template as in the ECC pin, scaled to scout size: every claimed
number gated to the receipt; falsifiers registry-named; the battery
full-strength even though the unit is small.  Deliverables:
v15/note-scout-bridge.md, v15/code/scout_exact.py,
v15/code/scout_output.txt, v15/code/scout_receipt.json.
