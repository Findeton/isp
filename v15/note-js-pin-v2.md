# JS PIN v2 (FROZEN) — THE CONSOLIDATED JOINT-SUCCESSOR CHARTER

**Frozen:** 2026-08-17, v15 ledger #104.  SUPERSEDES AND CONSOLIDATES
the pin 93ef35b48811 + addendum 1 6cb7438d461c + addendum 2
7992e47ba5d9 (all retained as history; where any clause conflicts,
THIS DOCUMENT GOVERNS).  Adds the #104 requirements (twenty-second
review): cross-arity coherence; the four-notion support taxonomy;
robust irreducibility; S-rho mixture consistency.

## THE OBJECT
One covariant boundary-to-boundary law: (R,G,S) -> {p_alpha;
R'_alpha, G'_alpha, S'_alpha, record_alpha} over mutually exclusive
COMPLETE SUCCESSORS.  The step = a finite causally complete
relational region between GENUINE division boundaries (a property of
the law, never assumed); no global tick.

## SOURCES
As the v1 pin (the sealed scouts by anchor; the DC addenda; the w5
recast; the external context anchors).  The sealed constraints bind
every construction.

## THE CHARGES (order normative)

S1a — THE REGRESSION BENCHMARK: the predictive quotient OF THE
DELIVERED DYNAMICS ONLY, passive-first (declared preparations; the
intervention extension registered), labelled a benchmark.  The
stabilization predicate frozen BEFORE measurement, on TWO AXES (past
supplied x future horizon), holding the history set fixed and
refining its PARTITION as the horizon grows.  The no-compression
word is JS-NO-STABILIZATION-WITHIN-<range>.

S1b — THE STATE-LAW FIXED POINT, AS AN ALGORITHM (specified before
running): the finite candidate-law family; the exact
quotient/refinement operator; enumerate-all-fixed-points or a
declared selection criterion; CYCLE and NON-CONVERGENCE outcome
words; state-relabelling identification; a termination argument
within the ansatz.  "Compression does not select truth" — gated.

S2 — TYPES AND SUPPORT (before any enumeration): the relation and
geometry alphabets; the state representation; the successor grammar;
operational input/output spaces; AND THE FOUR-NOTION SUPPORT
TAXONOMY, each declared separately and non-circularly:
DEPENDENCY SUPPORT (whose incoming data the law reads); WRITE
SUPPORT (whose relations/records change); GEOMETRY HALO (nearby
geometry affected by backreaction); PROCESS ARITY (the smallest
complete causal region that cannot physically factor — a LAW
property, measured last).  Whether an AB process touching C-adjacent
geometry counts as 2- or 3-support is FIXED HERE, never decided
opportunistically.  PHASE-ONE MINIMAL GEOMETRY mandatory: a finite
structural geometry grammar; validity gates on every G'; a minimal
order/composition consistency gate; input-relative causal support —
else every arity verdict is LABELLED KINEMATIC-AND-PROVISIONAL.

S3 — THE INDEPENDENT FAMILIES + COHERENCE: construct L2, L3, L4
independently (most general admissible complete-successor families
at each support, typed, finite, exactly parameterized; direct-sum
outputs; trace-nonincreasing CP maps T(H_{R,G}) -> T(H_{R',G'}),
direct sum trace-preserving; Choi positivity semidefinite with
method honesty).  THEN THE COHERENCE CONDITIONS (#104 — independent
is not yet unified; without these they are three model families,
not one law), each tested with certificates:
(a) IDLE-SPECTATOR: an L2 process on AB embedded in ABC reproduces
the same local process when C is causally irrelevant;
(b) RESTRICTION: an L4 law restricted to a closed three-actor
boundary lands in L3;
(c) DISJOINT UNION: separated instruments combine consistently
(common-past correlations allowed);
(d) OVERLAP GLUING: laws agreeing on a shared boundary admit a
well-typed joint extension;
(e) RELABELLING NATURALITY: all embedding/restriction maps commute
with actor relabelling.
Naturality throughout compares the ISOMORPHISM CLASS of complete
boundary data (R,G,S) — never the bare actor graph.

S4 — THE DISJOINT DIAMOND: AB and CD with SEPARATION DEFINED via
the input geometry and causal past (actor disjointness is not
separation); both construction orders yield the identical complete
output up to a SPECIFIED boundary isomorphism and transport;
probabilities need not factor.

S5 — ROBUST IRREDUCIBILITY (the decider, unbiased and robust):
test L3 members with THREE DISTINCT TESTS: (1) not exactly
factorable; (2) not in the CLOSURE of factorable laws (closure
defined exactly in the pin's declared topology); (3) POSITIVELY
SEPARATED from that closure in a declared OPERATIONAL NORM on
direct-sum geometry-changing instruments (the norm defined; the
non-membership certificate form defined; whether positive distance
is certifiable stated).  Only test 3 is experimentally stable — a
map failing exact factorization while approximable by pair
processes is NOT called fundamentally triple-native.  Verdict space:
EMPTY / ENTIRELY-FACTORIZING / ENTIRELY-IRREDUCIBLE / MIXED /
STATE-DEPENDENT / METHOD-INCONCLUSIVE (renamed from
undecidable-within-method — these finite semialgebraic questions may
be decidable even when the chosen solver cannot finish).  "Commutes"
means a compatible joint representation exists, nothing more.

S6 — THE S-RHO BRIDGE WITH MIXTURE CONSISTENCY: the bridge declared
explicitly (S -> rho_S, or S = (S_causal, rho)) with redundancy
tests per component; PLUS: (a) affinity under operational mixing,
or the explicit departure stated; (b) a preparation-independence
test; (c) the steering/no-signalling gate ENGAGED wherever hidden
S-information affects evolution (the sealed SCOUT-PSI fork cited —
this is where JS meets it); (d) transition closure: updating S and
rho does not double-count state.

## THE REQUIREMENT SET
The 13 restrictions as in v1 (complete-outcome normalization;
relabeling covariance; arena naturality over (R,G,S) classes;
input-relative locality; construction-order gauge; PHYSICAL
factorization only; predictive-state minimality; operational CP +
the causality addendum; record permanence; geometry consistency
[phase two]; transport; universal backreaction [phase two];
empirical regression — the walk as representation, its record
update not frozen in).

## PHASE TWO (REGISTERED, NOT RUN)
The geometry weld (G_order/count = G_composition); universal
backreaction; the quantum/composite gate incl. the HJW protocol;
the scaling gate; AND (#104) THE EXTENSION QUESTION: an extension
theorem generating every L_n from the local law, or a measured
minimum-n obstruction — local consistency through L4 does not
guarantee L_n.

## WALLS AND STANDARDS
As v1 (W-REPRESENTATION fork-neutral; no ontology decided; the #98
narrowings; W4 term-binding incl. the support taxonomy terms;
sample-space spine; numeral per-occurrence totality; the receipt-wide
hex inventory + G-AST-DETERMINISM + G-KERNEL-WALL from birth; exact
arithmetic; byte x2 deleted-artifact git-less alien-CWD 3 seeds +
LOG-append probe; registry falsifiers with move proofs; argv rc 2;
object-under-test digest; candidate readings until the verifier
seat).  Deliverables: v15/note-js.md, v15/code/js_exact.py,
v15/code/js_output.txt, v15/code/js_receipt.json.
