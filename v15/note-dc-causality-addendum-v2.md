# DC BIPARTITE-CAUSALITY ADDENDUM v2 (FROZEN; supersedes v1 4c7282b2a518 per ledger #82) — MANDATORY COMPONENT OF THE EVENTUAL DYNAMICS-CLOSURE PIN

**Frozen:** 2026-08-17, v15 ledger #80 (twelfth external review via
the user).  DC remains PARKED per the standing holds; this addendum
pre-freezes obligations that bind DC's pin whenever the user unparks
it.  No unit may cite "steering-unphrasable" as safety.

## THE OBLIGATIONS

1. THE COMPOSITE STATE: define X_AB — the complete composite state,
   the subsystem factorization, RELATIONAL separation (a condition on
   the evolving geometry, not fixed light cones: the
   Barandes-Hasan-Kagan causal-locality construction assumes a fixed
   factorization and fixed cones and CANNOT be inherited under
   cell-creating backreaction), and the allowed local instruments
   (preparation and intervention semantics — currently unbuilt, per
   the registry).
2. THE CANONICAL STEERING IMPLEMENTATION: a Bell-pair analogue on
   separated regions whose remote basis choice places the far
   subsystem into EXACTLY the SCOUT-PSI pair (the Z/X decompositions
   of the shared rho) — the Hughston-Jozsa-Wootters construction
   realized in ISP terms, or its impossibility exhibited
   constructively.
3. THE UNCONDITIONAL TEST: Bob's record-AND-geometry distribution
   after summing over Alice's outcomes, compared across Alice's
   choices.
4. EVERY GRAIN, EVERY WINDOW: the comparison runs at every
   operational grain (ordered histories, count fields, every
   physically meaningful label quotient — the grain-indexed lesson
   of #76) and at ALL pre-contact future windows (backreaction can
   amplify an initially hidden difference; a window-2 blindness is
   not safety).  O_B includes records, Bob-local geometry, carrier
   availability, and event rates.
5. BOTH BRANCHES: the test runs against (a) an instrument completion
   whose outcome maps are COMPLETELY POSITIVE and trace-nonincreasing
   with CPTP sum (mere density-matrix linearity is insufficient once
   ancillas and bipartite systems exist) and (b) the ontic-state
   dynamics — neither assumed.  The canonical Z/X steering test is a
   FALSIFIER, not the proof: the positive theorem must cover EVERY
   allowed joint preparation and EVERY allowed local instrument.
6. THE OUTCOME WORDS (nothing else):
   - UNPHRASABLE-BECAUSE-COMPOSITE-DYNAMICS-UNBUILT — recorded as
     INCOMPLETENESS, never as safety;
   - PHRASABLE-NO-SIGNALLING-PROVED — the positive theorem, with
     the relational condition proven at every grain and window;
   - PHRASABLE-SIGNALLING-WITNESS — the delivered rule is REJECTED
     by the corpus's own sealed no-signalling commitments;
   - REMOTE-DECOMPOSITIONS-ONLY-EPISTEMIC — admissible only with a
     concrete joint-law proof that Alice's operation does not move
     Bob's local ontic ensemble, PLUS reproduction of the actual
     steering correlations;
   - COMPOSITE-DYNAMICS-COMPLETE-BUT-STEERING-UNREPRODUCED — the
     dynamics is finished and FORBIDS the observed steering
     correlations: an EMPIRICAL FAILURE of the theory, never
     recorded as safety.

## THE STANDING CONTEXT (anchored when used)

SCOUT-PSI (sensitivity at the canonical steerable pair; verdict
grain-indexed); SCOUT-K — SCOPED PRECISELY (#82): at the committed
arena and window, no normalized covariant conditional kernel
K(e|c,G,R) with FIXED geometry and the TRIGGER FACTORIZATION
P(c,e)=q(c)K(e|c,G,R) preserves the delivered cell-walk's
conditional statistics through window 3, even with the entire
record; the mechanism is trigger-erasure (the triple record forgets
which cell triggered; the walk remembers) — it does NOT kill
non-trigger-factorized bridges, kernels with additional state (the
trigger trace; SCOUT-T), history-level processes, or
observable-history-level agreement; the
Polchinski / Simon-Buzek-Gisin line (no-signalling drives toward
linear CP maps under the usual assumptions); paper-38's zero scoped
as reading-not-intervention (paper-38:429 — not a wall); v5's
record-nonfactorization as vocabulary, not implementation.  The
twelfth review's strategic recommendation, decision the user's: the
rho-linear branch is the safer default until the positive theorem
exists.
