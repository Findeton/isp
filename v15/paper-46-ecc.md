# Event-and-carrier closure: the state contract, the seam decision, and the Born-marginal LP

**ECC / paper-46, v15.** Pin `v15/note-ecc-pin.md` (FROZEN, sha256-12
`04874b01e241`, v15 ledger #43). Registry questions Q2, Q75, Q73, Q79 and Q5,
with Q1, Q4, Q87, Q121 and the Q34 typing as secondary legs. Instrument
`v15/code/ecc_exact.py`; artifacts `v15/code/ecc_output.txt` and
`v15/code/ecc_receipt.json`. Parents, all four sealed terminal and read at
pinned digests by no other route: CONTRACT (paper-43), ARITY (paper-44),
AUTOGLUE (paper-45), DISC (paper-47). This unit is a COMPATIBILITY/NO-GO
paper, not a forced construction, and its class is bound to the pin's own
class sentence by a gate.

**THE DEFENSIBLE SENTENCE.** The four parents can be put on one interface
table with every object typed and every free declaration carried with its
type; they cannot be unified as written at the committed row: the walk's own
start state produces Born weights that no probability over the committed
event classes can carry as inclusion marginals, and the obstruction at that
row is an exact arithmetic ceiling.

**SCOPE (binding).** One arena at each fork arm -- the committed chart of
CONTRACT's machine and the aligned two-sector union of AUTOGLUE's -- at the
committed windows only. Every census here is finite and no continuum claim
is made. No number here carries a laboratory unit. Every fraction is a
counting fraction or an exact rational Born weight, and no fraction is read
as how often anything happens. Exact arithmetic throughout: Python integers
and `fractions.Fraction`; an abstract-syntax scan of the instrument bans
floats, typed numerals in statements, orderings keyed on a bare repr, and
every call to the builtin hash.

**Verdict**, in five segments, quoted exactly as the instrument emits them.

```
ECC-STATE-CONTRACT-CLOSED-AT-THE-COMMITTED-WINDOWS-UNDER-THE-DECLARED-SEAM-READING<OBJECTS=23; COMPUTED-HERE=18; CITED=5; SENSE-WORDS=6; FREE-DECLARATIONS-CARRIED=15-OF-15-WITH-6-TYPE-CATEGORIES; EVENT-FORK=DECLARED-2-ARMS-NO-COMPROMISE; COSET-PRINCIPLE=DECLARED-THE-SELECTION-LAW; MAPS=3-TYPED-COMPOSED-NOWHERE; STATE-COMPONENTS=2-PLUS-BOOKKEEPING=1; SEAM-COMPONENT=READING-CONDITIONAL; ACTOR-RECORD-CYCLE=4-EDGES-CARRIED-OPEN-AT-FULL-DYNAMICS>
```

```
SEAM-DECISION-UNDERDETERMINED-AT-THE-COMMITTED-OBSERVABLE-MENU<OBSERVABLES=8; COMPLETION-BLIND=8-OF-8; PROBE-STATES=11; THE-MODAL-RELATION-SEPARATES=2-VALUES-OVER-THE-SAME-PROBES; READING-FAMILY=3; TWO-STEP-CENSUS-FROM-ALL-108-FIRST-CROSSINGS: RE-SOLVED-FIBER-25-TO-43; PERSIST-FIT-DIFFERS-AT-108-OF-108; PERSIST-KEPT-DIFFERS-AT-108-OF-108; CHOSEN-READING=RE-SOLVED-DECLARED-NOT-MEASURED; DOWNSTREAM-STAMPED=215-ROWS>
```

```
PSI-STATUS-INDEPENDENT-AT-EVERY-DELIVERED-ROW<FACES=3; ROWS=18; FUNCTIONAL-AGREEMENTS=18-OF-18; SEPARATING-ROWS=0; THE-CONSUMERS-REACH-THE-BORN-FUNCTIONAL-ALONE=AUDITED; THE-TRILEMMA-IS-UNRESOLVED-AND-EVERY-DELIVERED-RESULT-IS-PROVEN-BLIND-TO-IT>
```

```
ECC-LP-INFEASIBLE-AT-THE-COMMITTED-ROW-AND-THE-CEILING-IS-A-THEOREM<CLASSES=4-PLUS-THE-PAIR-EVENT-BRANCH; DISTINCT-TARGETS=39; ROWS=156; INFEASIBLE=136; UNIQUE=8; MANY=6-MAX-DIM-8; FIBER-FEASIBLE=6; BRANCH-DEGENERATE-IDENTITY=39-OF-39; TARGET-UNDEFINED-AMPLITUDES=2; COMMITTED-ROW=SINGLE-CELL-AMPLITUDE-AT-THE-INITIAL-RECORD-AT-THE-DELIVERED-ORDER-QMAX-4/9-ABOVE-THE-CEILING-INFEASIBLE-AT-ALL-4-COMMITTED-CLASSES; CEILING=FEASIBILITY-FORCES-QMAX-AT-MOST-ONE-THIRD-CHECKED-AT-156-ROWS-EXCEPTIONS-0; CONTROLS=FORCED-BOTH-WAYS-THROUGH-THE-REAL-PREDICATES; NORMALIZATION=PRE-REGISTERED-PAIR-CELL-FORM-INCLUSION-EQUALS-UNIFORM-TWO-STAGE-WHERE-EVERY-EVENT-WRITES-3-CELLS; FREE-TWO-STAGE=VACUOUS-AT-27-OF-27-CELLS-COVERED>
```

```
ECC-CARRIER-FAMILY-UNSELECTED-AND-THE-DEBT-DECIDED<CANDIDATES=4; HOST-THE-STATE=4-OF-4; EXPRESS-THE-COMMITTED-BRANCHING=2-OF-4; EVOLVE-ACROSS-CREATION=0-OF-4; NOTHING-SELECTED-THE-FAMILY-PUBLISHED; DEBT=THE-WALK-CONTINUES-ON-ITS-OWN-MENU-WHERE-CREATION-REFUSES: MENU-CELLS-IN-THE-CROSS-CLASS=0-OF-36; FROZEN-CREATION-ADMITS=0-OF-288-SEAM-SPANNING; UNIT-MASS-AMPLITUDES=4-OF-4; CLASS=COMPATIBILITY-NO-GO; SCOPE=ONE-ARENA-COMMITTED-WINDOWS-COUNTING-ONLY; W-THREE-LABELS=4-MEMBER-SPECIFIC-PLUS-1-FAMILY-LEVEL-FIBRES-DISCLOSED>
```

Between delivery and adjudication every headline reading here is a
**candidate reading**.

---

## 1. The short of it

Four sealed units left four different vocabularies on the table: a census of
declarations, an arity dial, a transition relation on a seam, and an
ablation benchmark. The pin sends this unit to put them on ONE table --
every object typed, every word fixed to one sense -- and then to ask the
question the corpus has never asked in one place: can the quantum layer's
Born weights and the event layer's grammar be carried by a single
probability assignment, at the state the corpus itself commits?

Five things came out.

First, the table closes. Every object of the four parents takes one of the
four classes -- declared, generated, law-selected, reconstructed -- and no
row needs a fifth word. The fifteen free declarations arrive with six
heterogeneous types, and the event class is a declared fork with its two
arms kept apart.

Second, the seam decision the pin puts ahead of any Born weight comes back
UNDERDETERMINED, with teeth. The committed observable menu is blind to the
seam completion at every observable probed, while the modal allowed-event
relation separates the same probe family -- and the three-member reading
family separates the two-step record census at every one of the 108 first
crossings. The corpus carries a discrimination schema and no discriminating
measurement.

Third, the psi-status trilemma does not need to be resolved for anything
this unit delivers: the three faces are built as three carriers and emit
byte-identical Born functionals at every delivered row.

Fourth, the LP. At the committed row -- the walk's own start state, the
initial record, the delivered coin order -- Born-marginal feasibility fails
at all four committed event classes, and the failure at that row is an
arithmetic ceiling: an inclusion marginal can never exceed the total
probability, so feasibility forces every Born weight to one third or
below, and the committed start carries a weight of 4/9. Away from the committed
row every outcome word fires on real rows: unique at the line classes,
an eight-dimensional family at the block class, and a branch that
degenerates to the identity at pair-events.

Fifth, the carrier family is measured and nothing is selected within it,
and the admissible-class debt is decided: the walk and the creation
relation never contradict each other, because they act on disjoint menus.

---

## 2. The interface table (step 0)

Every repository read is at a pinned digest; the read set is recorded at
the audit hook, classified rather than filtered, and checked before the
first gate, after the last, and after the artifacts are written.

| id | path | sha256-12 |
|---|---|---|
| E-ARI | v15/paper-44-arity.md | 0d677a4cbe97 |
| E-AUT | v15/paper-45-autoglue.md | fa0268d99524 |
| E-CON | v15/paper-43-contract.md | 0c8d1a687b14 |
| E-DIS | v15/paper-47-disc.md | 5da53943c6f7 |
| E-PIN | v15/note-ecc-pin.md | 04874b01e241 |
| E-TPL | v14/code/era_template.py | d04a3eb58fbc |

The table below is this unit's own: one row per object the four parents
jointly put in play, each classed by the four words, each carrying its
extent, its backing, its carrier kind and the sense word it binds to. It is
not CONTRACT's census re-typed -- CONTRACT named its own object list at its
own unit, and the two lists differ; every extent marked COMPUTED-HERE is
rebuilt from constructors in this instrument and gated against the sealed
parent by a consumer gate, and every extent marked SEALED-CITATION is
parsed out of the parent's own located sentence, never typed.

| object | class | extent | backing | carrier | sense |
|---|---|---|---|---|---|
| ACTOR | DECLARED | 9 | COMPUTED-HERE | ONE-STATE | event |
| CELL | GENERATED | 27 | COMPUTED-HERE | ONE-STATE | cell |
| DIRECTION | DECLARED | 3 | COMPUTED-HERE | ONE-STATE | cell |
| PARALLEL-CLASS | GENERATED | 4 | COMPUTED-HERE | ONE-STATE | geometry |
| LINE | GENERATED | 12 | COMPUTED-HERE | ONE-STATE | event |
| DIVISION-EVENT | GENERATED | 84 | COMPUTED-HERE | ONE-STATE | event |
| REALISED-EVENT | GENERATED | 30 | SEALED-CITATION | ONE-STATE | event |
| GROUPING | GENERATED | 280 | COMPUTED-HERE | ONE-STATE | event |
| ADMISSIBLE-ROUND | LAW-SELECTED | 36 | COMPUTED-HERE | ONE-STATE | event |
| RECORD-BLOCK | GENERATED | 27 | COMPUTED-HERE | ONE-STATE | record |
| HISTORY | GENERATED | 5784 | SEALED-CITATION | ONE-STATE | record |
| COUNT-FIELD | GENERATED | 36 | SEALED-CITATION | ONE-STATE | record |
| QUANTUM-STATE | DECLARED | 27 | COMPUTED-HERE | ONE-STATE | state |
| BRANCH-WEIGHT | GENERATED | 1 | COMPUTED-HERE | ENSEMBLE-SIDE | state |
| MENU | LAW-SELECTED | 6 | COMPUTED-HERE | ONE-STATE | geometry |
| NAMING | RECONSTRUCTED | 1296 | SEALED-CITATION | ONE-STATE | record |
| COIN | DECLARED | 6 | COMPUTED-HERE | ONE-STATE | state |
| UNION-CARRIER | GENERATED | 15 | COMPUTED-HERE | ONE-STATE | geometry |
| UNION-PAIR | GENERATED | 54 | COMPUTED-HERE | ONE-STATE | geometry |
| CONFLICT-GROUP | GENERATED | 455 | COMPUTED-HERE | ONE-STATE | event |
| SEAM-COMPLETION | DECLARED | 4 | COMPUTED-HERE | READING-RELATIVE | state |
| SEAM-STATE-SPACE | GENERATED | 29791 | COMPUTED-HERE | READING-RELATIVE | state |
| METRIC-READOUT | LAW-SELECTED | 1 | SEALED-CITATION | ONE-STATE | metric |

No row is classed PRIMITIVE, which carries CONTRACT's own finding forward:
at this corpus nothing is primitive, and the table's typing is total. The
two READING-RELATIVE rows are the seam completion and its state space: they
are components of the state under the persistent reading and bookkeeping
under the re-solved one, and section 3 is the census that decides what can
be said about that choice. The branch weight is carried on CONTRACT's own
classification, quoted from its bytes and consumed by the table's gate:

> ensemble-side bookkeeping: the weight the emission law multiplies along a
> branch, carried to compare runs and never read by the one-instant update

**The six words, fixed.** Each of the six words the pin names is fixed to
one sense by one sentence, and the instrument requires each sentence to
stand in this paper verbatim.

| word | the one sense |
|---|---|
| event | an event is a set of actors of the declared arity dividing together, and nothing else is called one |
| cell | a cell is an unordered co-division pair of actors carrying one declared direction, and nothing else is called one |
| geometry | the geometry is the cell set with its incidence, the object the weld reads, and nothing else is called one |
| record | the record is the co-division relation with its multiplicities, and nothing else is called one |
| state | the state is the instantaneous data the declared update reads, listed reading-relative in this table, and nothing else is called one |
| metric | the metric is the division count read as the squared length of its link direction, and nothing else is called one |

**The fifteen free declarations, carried with their types.** CONTRACT's
sealed census prices the corpus at a family verdict; its free rows are
carried here with a REQUIRED type column, because they are heterogeneous
categories -- arena choices, schedules, conventions, readings, a completion
datum and a measure declaration -- and never a count of physical constants.

| declaration | what it fixes | type |
|---|---|---|
| d | the spatial dimension | ARENA-CHOICE |
| q | the field order | ARENA-CHOICE |
| a | the division-event arity | ARENA-CHOICE |
| R | the depth in rounds | ARENA-CHOICE |
| the ceiling | the occupancy ceiling | ARENA-CHOICE |
| the chart | the two-sector overlap type | ARENA-CHOICE |
| the window | the driven schedule set | SCHEDULE |
| the horizon | the number of coupled steps | SCHEDULE |
| the tick | the scheduling convention | SCHEDULE |
| the coin | the S_3-covariant unitary | DYNAMICAL-CONVENTION |
| the coin order | coin before or after the residue | DYNAMICAL-CONVENTION |
| the orientation | the sign of the shift | DYNAMICAL-CONVENTION |
| the reading | the Born menu or the record menu | READING |
| the seam | the completion at a shared site | COMPLETION-DATUM |
| the measure | the measure over configurations | MEASURE-DECLARATION |

**The event fork, declared.** No compromise event class is constructed
anywhere in this unit: the declared fork keeps its two arms apart, and the
instrument's own event-class registry is gated at exactly two arms.

| arm | event class | grain |
|---|---|---|
| COMMITTED | three-actor conflict groups | 3 |
| BRANCH | pair-events | 2 |

The committed arm is the corpus's own: a division event is a group of
three actors, and which triples the grammar admits is governed by the
affine structure. The coset principle behind that selection is labelled
declared (the selection law), pending the registered
COSET-FROM-COMPATIBILITY test, on ARITY's sealed conditional, quoted from
its bytes:

> IF every round must be a complete partition of the actors into proper
> nontrivial coset blocks of the arena's translation group, THEN the event
> size is the field order

The branch arm is ARITY's a = 2 extension family, an extension of the
committed theory and never a reading of it, on that unit's own declaration:

> the packing rule is a declaration this unit makes, it is carried as its
> own row of the arena table

The two arms never mix: every measurement below names its arm, and the LP's
pair-event rows are labelled BRANCH at every row and share no vocabulary
with the committed classes.

**The circularity, carried.** The dependency graph of CONTRACT's census is
rebuilt here as data and its two cycles recomputed: the actors-and-records
circle closes in 4 edges and the dynamical loop in 3, at the parent's own
lengths. The three conditional maps of section 4 thread that circle without
composing, and whether the coupled dynamics run forward from the
reconstructed cast returns the record it started from stays open at the
full-dynamics scope and is routed to DYNAMICS-CLOSURE.

---

## 3. The seam decision (step 0b), ahead of any Born weight

The pin orders this decision before any Born weight is used, and the
instrument enforces the order on its own ledger: the seam-decision gate
fires before the first emission functional is computed. The launching row
is AUTOGLUE's sealed sentence, quoted from its bytes and consumed by this
section's gate:

> a state restricts the allowed transitions if and only if it persists, and
> persistence is a reading this unit declares and measures, not a fact it
> found

and the question this unit must pose is the one AUTOGLUE registered as
unposed:

> Whether that predictive content shows in the record, and whether two
> states differing only in it are separable by any measurement this corpus
> defines, is unposed

**The substrate, rebuilt.** The aligned union is reconstructed from the
gluing: 15 carriers, 54 realised pairs, 455 conflict groups, 288 of them
seam-spanning, with the footprint census landing on the parent's profile
table object by object and the completion lattice at the committed counts
carrying 31 points with kernel 4. The every-leg standard returns the
parent's 162 lawful events with 108 lawful crossings; the successor
multiset is 4-valued at 81 seam slots and 8-valued at 243, over 324 slots
in all, and 1-valued at none; the state need not move at exactly 27 of the
crossings;
and the advance-state census over all 29791 completion triples reproduces
the parent's histogram point by point, with the best states ready for 9
crossings, 8 such states, and 20100 states ready for none. Every one of
those numbers is bound to its sealed source by a consumer gate, so this
rebuild is a gated reproduction, reported as one.

**Leg one: the committed observable menu is completion-blind.** Eight
observables -- every record functional the committed corpus defines at the
union window, from the count field itself down to the total-mass residue --
are implemented as functions whose whole domain is the geometry and the
record; the declared domain audit, an abstract-syntax gate, proves none can
even receive a completion.
Evaluated over a declared probe family of 11 union states that share one
geometry and one record and differ ONLY in the seam completion -- the 8
best states, the direct-sum triple, and the first and last lattice points
in the canonical key order -- every observable takes exactly one value.

| observable | values over the probe family |
|---|---|
| OBS-COUNT-FIELD | 1 |
| OBS-RESIDUE-FIELD | 1 |
| OBS-CELL-COUNT | 1 |
| OBS-SEAM-COUNT-VECTORS | 1 |
| OBS-PROFILE-CENSUS | 1 |
| OBS-FROZEN-ALIVE | 1 |
| OBS-A2-CROSS-REALISED | 1 |
| OBS-TOTAL-MASS-RESIDUE | 1 |

That blindness is structural and the paper says so: the menu's members are
functions of data the completion is not part of, so the sweep could not
have read otherwise -- which is exactly the certificate the verdict word
needs, established by the domain audit and exhibited by the sweep. What is
NOT structural is the other half: the modal allowed-event relation -- which
crossings a state's own predictions fit -- takes 2 distinct values over the
same probe family. The one completion-sensitive quantity the corpus carries
at this window is a lawfulness relation, and the corpus defines no
observable that reads a lawfulness relation.

**Leg two: the readings separate the two-step record census.** Three
readings of the seam datum are declared as exact predicates and run from
every one of the 108 lawful first crossings, over every second event that
deposits a cross pair.

| reading | status | first crossings where its second-step set leaves the re-solved one |
|---|---|---|
| RE-SOLVED | the base member | 0 |
| PERSIST-FIT | declared | 108 |
| PERSIST-KEPT | declared | 108 |

The base member is AUTOGLUE's own: the successor census takes only the
post-record, and its second-step fiber reproduces the parent's published
table exactly --

| allowed second crossings | first crossings |
|---|---|
| 25 | 9 |
| 26 | 36 |
| 27 | 24 |
| 28 | 12 |
| 41 | 9 |
| 43 | 18 |

-- the allowed set falling from 108 to between 25 and 43 depending on which
crossing goes first. The persist members allow DIFFERENT second events: the
declared fit member requires the persisted completions to predict the
single incidence each deposited cross pair carries; the declared kept
member requires some completion of the first successor set to survive the
second event unchanged; and each of them leaves the re-solved set at every
one of the 108 first crossings. The separation is real and it is
everywhere.

**The verdict.** A discriminating measurement inside the committed corpus
would need an observable that takes different values under the readings.
The menu is blind at every observable while only the modal relation
separates, the corpus runs no autonomous multi-step seam dynamics that
could turn the relation difference into an occurrence, and so the honest
outcome is the pre-registered word:
SEAM-DECISION-UNDERDETERMINED-AT-THE-COMMITTED-OBSERVABLE-MENU. What the
census adds to AUTOGLUE's refusal is the certificate and the schema: no
committed observable separates the completion-only probe family, and the
modal relation does. Persistence is a declared reading here and every
downstream row is stamped with it. The chosen reading is RE-SOLVED --
matching the relation's own definition -- and it is a declaration, not a
measurement; the stamp is discharged by a reach audit proving that no
downstream consumer touches the completion machinery at all, so every LP,
psi, carrier and debt row below is reading-independent by construction,
215 stamped rows in all.

---

## 4. The three conditional maps (step 0c)

The dynamics is decomposed as three separate conditional maps, typed in
the table below and composed nowhere in this unit -- an abstract-syntax
reach audit proves that no map reaches another through any chain of this
instrument's functions. Their composition is DYNAMICS-CLOSURE's chartered
object, not this unit's.

| map | kind | domain | codomain |
|---|---|---|---|
| event selection | STOCHASTIC-KERNEL | record x born-functional | distributions over the declared event class |
| seam completion | RELATION | the post-record | sets of seam completions |
| quantum evolution | FUNCTION | amplitude x record residue | amplitude |

The kinds are measured, not asserted. The seam-completion map is a
relation whose witness sizes at the committed windows are the full lattice
at the pre-state and the four- and eight-valued successor sets at a lawful
crossing, one-valued nowhere. The quantum-evolution map is a function and
its unitarity is exact at the tripled scale, checked by norm conservation.
The event-selection map is distribution-valued, and the committed corpus
carries it at two grains -- the grammar's round admission and the walk's
cell emission -- which is the fork of section 2 seen from the dynamics
side, and this unit blends them nowhere.

---

## 5. The psi-status trilemma (step 1)

The pin's trilemma -- ontic stochastic wave, CPTP-instrumented quantum
state, Barandes-representational -- is built here as three disjoint code
regions carrying three different state objects: the amplitude vector; the
density operator evolved by conjugation, with the site-block route checked
against a full-matrix evolution at the committed row; and the
configuration-level process law carrying the transition-amplitude table
with the initial configuration data. A syntax audit proves the regions
share only declared plumbing and never call one another.

At every delivered row -- the five declared amplitudes at the initial
record under both coin orders, and the uniform and single-cell amplitudes
at two round-generated records under both orders -- the three faces emit
byte-identical functionals at every delivered row: 18 rows, agreements at
every one of the 18, separating rows none. No committed row separates the
faces, so the trilemma is UNRESOLVED at this corpus's one-step windows,
and every result this unit delivers is proven independent of it: the reach
audit shows every downstream consumer takes the Born functional through
one accessor and touches no face region. That is the pin's second arm --
independence proven row by row -- and it is the honest one, because a
measurement that separates the faces would need more than the one-step
windows the corpus commits.

---

## 6. The Born-marginal feasibility LP (step 2)

**The normalization, pre-registered.** An a-actor event writes its pairs:
at the committed arity every admissible event writes 3 pair-cells, so
inclusion marginals sum to 3; at the branch arity a pair-event writes 1
cell and the problem degenerates. The registered general form is consumed
from the pin's own bytes, its digest is sealed at the normalization gate,
and the instrument's ledger proves that gate fires before any feasibility
row. The form is class-relative and measured so: at the block class and
the declared-line class every event writes 3 cells and the marginal-sum
identity holds; at the coset-line class 3 events write no cell at all, and
at the full triple class 54 events write 2 -- so the identity is a
property of the class, not of the notation. Three readings of the
normalization were on the pin's table and the choice is explicit:
INCLUSION MARGINALS is the registered primary; the uniform two-stage
reading builds the SAME constraint system wherever every event writes 3
cells, measured as matrix equality; and the free two-stage reading -- a
per-event cell-selection weight chosen freely -- constrains nothing, since
every one of the 27 cells is covered and any target becomes expressible,
so it is disclosed as vacuous and carries no verdict.

**The LP.** For an event class with incidence matrix M and a Born target
q, the feasibility question is whether a probability vector p over the
class's events exists with M p equal to 3 q. Four committed classes are
built from the chart's own constructors -- E-BLOCK, the 27 record blocks;
E-LINE-DECLARED, the 9 declared lines; E-LINE-COSET, the 12 affine-line
cosets with their 3 zero-writers exposed; E-TRIPLE, all 84 triples -- and
the branch class E-PAIR beside them, never merged. The structural counts
are gated: every cell lies in exactly 3 blocks, 1 declared line and 7
triples. The target family is declared: the five declared amplitudes of
CONTRACT's screening at the initial record under both coin orders, and the
round-generated record window -- the field each of the 36 admissible
rounds writes -- at the uniform amplitude under both orders. Born language
is licensed only downstream of the LP row it cites. The emission read
point is the parent's own, quoted from DISC's bytes:

> a division event emitted on a cell with that cell's post-coin Born
> weight, every branch of the emission tree carried with no sampling and
> no pruning

and the residue discipline is CONTRACT's: two count fields differing by
the field order on one cell give byte-identical functionals, measured at
both orders. The coin-order fiber is measured before the LP consumes a
single target: under the alternative order the functional is record-blind
at every amplitude and every record of the window, under the delivered
order it moves with the record, and at the initial record the two members
coincide -- which is DISC's sealed mechanism, quoted from its bytes:

> Under the alternative order the count phase lands after the coin, so it
> cannot enter that step's Born weights at all

**The census.** 39 distinct targets by 4 classes give 156 rows, and every
row is solved by one exact two-phase procedure over Fractions.

| word | rows |
|---|---|
| FEASIBLE-AT-THE-FIBER-ROW | 6 |
| INFEASIBLE | 136 |
| MANY | 6 |
| UNIQUE | 8 |

**The committed row.** The walk's own start state is DISC's PR5 -- one
basis vector -- at the initial record under the delivered order, and the
committed row is the walk's own start state and it is infeasible at every
committed event class:

| class | word | qmax |
|---|---|---|
| E-BLOCK | INFEASIBLE | 4/9 |
| E-LINE-DECLARED | INFEASIBLE | 4/9 |
| E-LINE-COSET | INFEASIBLE | 4/9 |
| E-TRIPLE | INFEASIBLE | 4/9 |

with exact infeasibility gaps 4, 4, 3 and 7/3 in the receipt. The
mechanism at this row is a theorem and the instrument checks it at all 156
rows with exceptions none: an inclusion marginal is a sum of probabilities
and can never exceed the total, so feasibility forces every Born weight to
one third or below -- and the committed start concentrates a post-coin
weight of 4/9 on each of two cells at its own site. The ceiling leg is the
one family-level statement of this paper: it is arithmetic about the
pre-registered normalization, checked at every row and carried by no
fibre.

**Away from the committed row every outcome word is substantive and every
word fires on a real row.** At the declared-line and coset-line classes,
the uniform, one-direction and alternating amplitudes are feasible and
UNIQUE: the line-event probabilities are pinned outright, because each
cell lies in exactly 1 declared line. At the block class the same targets
are feasible with an 8-dimensional polytope -- MANY, a new free
declaration, published as a family with nothing selected -- and at the
full triple class the same targets carry 57 implicit zeroes, one per
deficient writer, and collapse onto the block family at the same
dimension 8: the marginal-sum identity forces every event that writes
fewer than 3 cells to probability zero, so the free class buys no freedom.
The record fiber is where feasibility dies wholesale: at 2 round fields
the uniform amplitude stays feasible under the delivered order, and at
every other member of the window it does not -- which is why 136 of the
156 rows read infeasible. The zero amplitude defines no target and its 2
target rows -- one per coin order -- are stamped TARGET-UNDEFINED. At the branch, all 39
targets degenerate to the identity -- the pair-event marginal map IS the
Born functional -- and the branch word DEGENERATE-IDENTITY is kept apart
from the committed vocabulary at every row.

**The controls.** Both arms run through the real predicates, the ACT
pattern: a synthetic target built as a known mixture comes back feasible,
and a synthetic single-cell target comes back infeasible with the ceiling
witness.

| arm | word |
|---|---|
| forced feasible | MANY |
| forced infeasible | INFEASIBLE |

---

## 7. The variable carrier (step 3)

The candidate the pin names -- a classical-quantum instrument with
classical register G and quantum state on H_G -- is measured against three
alternatives through the same predicates, and nothing is assumed.

| candidate | classical register | hosts the state | expresses the branching | evolves across creation |
|---|---|---|---|---|
| CQ-INSTRUMENT | True | True | True | False |
| DIRECT-SUM | True | True | True | False |
| FIXED-CARRIER | False | True | False | False |
| AMPLITUDE-ON-RECORD | False | True | False | False |

Hosting is the dimension bookkeeping: the creation window takes the
carrier from 27 cells to 28, and the canonical inclusion is verified an
exact isometry, so the growing carriers host the created cell and the
fixed carriers host the fixed-background state. Expressing is the
committed branching: one step of the walk leaves 3 branches with 3
distinct records, so a carrier without a classical register cannot express
the committed ensemble -- measured at 2 of the 4 candidates -- and the
corpus never exercises a coherence between branches, so the census does
not separate the cq-instrument from the direct sum: the family is
published, nothing is selected. Evolving is the sharp gap, at 0 of the 4
candidates: every cross pair the union admits is hosted by neither chart
-- all 36 are directionless -- while the committed coin family is typed on
three-direction site blocks, so no member of the family extends the
committed evolution to a created cell. The carrier question closes as a
family with a measured boundary: the state can be hosted, the branching
can be expressed, and the committed quantum evolution stops at the edge of
the committed carrier.

---

## 8. The admissible-class debt (step 4)

CONTRACT recorded the grain tension in its own voice, quoted from its
bytes and consumed by the debt gate:

> the coupled dynamics writes its own record: one emitted cell per step,
> which is a co-division pair rather than a group of the declared arity

AUTOGLUE refuses; the walk steps anyway; the pin orders the debt decided.
Here is the decision, measured at both arms of the fork.

| row | value |
|---|---|
| the walk's menu at the chart, in cells | 27 |
| the walk's menu at the union, in realised pairs | 54 |
| menu cells in the cross class | 0 |
| cross pairs the union admits | 36 |
| seam-spanning events the frozen creation admits | 0 |
| seam-spanning events the arena admits | 288 |
| declared amplitudes with unit menu mass | 4 |
| declared amplitudes that are non-zero | 4 |

The walk's emission menu is its own carrier: every weight lands on a
realised cell, and a realised cell is never a cross pair -- the menu meets
the cross class at 0 of the 36 cross pairs. The creation relation at the
frozen geometry admits 0 of the 288 seam-spanning events. And the walk
still steps wherever the relation refuses: the menu carries unit mass at
every non-zero declared amplitude, 4 at each side of the count. At the
branch arm the same disjointness holds with ARITY's own number, quoted
from its bytes:

> At two actors every one of the 36 seam-spanning groups opens no pair
> inside a sector and doubles nothing.

-- the crossing configuration is every pair-event's whole footprint there,
and still no crossing pair is in the walk's menu, because an unrealised
pair carries no amplitude. So the debt is decided: the two grains never
conflict, and the walk cannot express the crossing at either arity. The
compatibility is bought by confinement -- the walk is a dynamics of the
standing carrier, and growth is a transition the walk cannot phrase --
which is the carrier boundary of section 7 seen from the dynamics side.

---

## 9. The walls

**Reconstruction is never promoted to derivation**: every parent number
recomputed here is gated as a reproduction and cited to its seal, and the
agreement of two routes is reported as agreement, nothing more.

**Invariance is never promoted to gauge**: the word is withheld until
operational observables exist, and this unit defines none.

**Every headline is auto-labelled member-specific or family-level** from
the varied-fibre data, with the fibres disclosed in section 11; the labels
are computed, never typed.

**No compromise event class is constructed**: the declared fork keeps its
two arms apart at every row of this unit.

**Born language is licensed only downstream of the LP row it cites**, and
before the first Born weight the seam decision is already on the ledger.

**Every census here is finite and no continuum claim is made. No number
here carries a laboratory unit.**

**Persistence is a declared reading here and every downstream row is
stamped with it**; the stamp is a reach audit, not a sentence.

**The state listed here is reading-relative and scoped to the committed
windows**: a unit that declared a different dynamics would owe its own
table.

## 10. Every ratio here is COUNTING-ONLY

No measure is declared on events, states, completions, targets or rows.
Every fraction in this paper is a count over an exhaustive enumeration
with its denominator beside it, or an exact rational Born weight named as
one, and nothing here may be read as how often anything happens.

## 11. Choice inventory, labels, and the pre-registered outcomes

Every member-selecting choice is either proved equivariant or declared and
counted -- a silent tie-break is an offence the wall refuses.

| choice | class |
|---|---|
| the record window | EQUIVARIANT-PROVED |
| the probe-family extras | DECLARED |
| the canonical container order | DECLARED |
| the committed coin order | DECLARED |

The record window is the whole round-generated family and a translation
generator permutes it onto itself, measured; the probe-family extras, the
canonical container order and the committed coin order are declarations,
carried as three declared rows, with the alternative coin order run beside
the committed one at every target.

| head segment | label |
|---|---|
| the interface table and the state contract | MEMBER-SPECIFIC |
| the seam decision | MEMBER-SPECIFIC |
| the psi-status trilemma | MEMBER-SPECIFIC |
| the born-marginal feasibility lp | FAMILY-LEVEL-ACROSS-THE-TARGET-FAMILY |
| the carrier and the admissible-class debt | MEMBER-SPECIFIC |

The labels are rendered from the receipt: four segments are
member-specific and one carries the family label, which its ceiling
theorem earns by holding at every row with a derivation leg. Nothing else
in this paper is licensed beyond its arena.

| segment | reached | not reached | witness | other arm's witness |
|---|---|---|---|---|
| the interface table and the state contract | ECC-STATE-CONTRACT-CLOSED-AT-THE-COMMITTED-WINDOWS-UNDER-THE-DECLARED-SEAM-READING | ECC-CIRCULARITY-UNTYPABLE-AT-THE-TABLE | 23 | 0 |
| the seam decision | SEAM-DECISION-UNDERDETERMINED-AT-THE-COMMITTED-OBSERVABLE-MENU | SEAM-PERSISTENT-SUPPORTED | 0 | 2 |
| the psi-status trilemma | PSI-STATUS-INDEPENDENT | PSI-ONTIC | 0 | 18 |
| the born-marginal feasibility lp | ECC-LP-INFEASIBLE-AT-THE-COMMITTED-ROW | ECC-LP-UNIQUE | INFEASIBLE | 8 |
| the carrier and the admissible-class debt | ECC-CARRIER-FAMILY-UNSELECTED-AND-THE-DEBT-DECIDED | ECC-BLOCKED-AT-THE-CARRIER | 12 | 0 |

Every reached word is built on a stem of the pin's own bytes, located in
the frozen pin by the outcome gate; every unreached word carries the
predicate that would have reached it and the measured witness of the arm
that fired; and all four LP words -- infeasible, unique, many, degenerate
-- fire through the real predicates on real or control rows, so no outcome
of the pre-registration is a word this instrument cannot emit.

## 12. The instrument

`v15/code/ecc_exact.py`, with the era's CLI: a delivery run that is the
only writer, `--no-write`, `--numbers`, `--selftest`, `--mutant NAME`,
`--verify-paper PATH`, `--list-gates`, `--list-mutants` and
`--list-families`; every unknown flag, repeated flag, missing operand and
conflicting mode pair exits with the usage code, and a mutant name
combined with a listing or sweep mode is refused rather than discarded.

The nine template families are implemented natively under the template's
own check ids, and the conformance gate parses the pinned era template and
requires id equality. Seals are taken at gate time over JSON-native
values, the seal window is closed after the last sealing gate, the
manifest is re-derived at promotion time, the serialised bytes and the
staged files and the promoted files are each re-verified against the
gate-time digests, and a failing run leaves the tree exactly as it found
it. The transcript's gate rows are reconciled with the ledger as a
multiset; every value-bearing narrative line is declared with the receipt
path of each numeral; and the object under test -- this paper -- is
digested into both artifacts, so a reader holding the committed pair can
tell which text they certify.

The head is a positional field list rebuilt by a comparator that shares no
code and no literal with the builder and reads no summary scalar: every
count in every segment is re-derived from the receipt's primitive row
tables, and the two renderings are compared whole and numeral by numeral.
Every falsifier recipe names the measured object it corrupts and the gate
it must die at; the self-test sweep runs every recipe out of the builder's
control flow, requires the death at the declared gate, proves the move by
digest against the clean run's register, and verifies the artifacts
byte-unchanged; and the falsifier table itself is audited against this
file's own source, so a recipe with no injection site cannot be declared.
The coverage gate recomputes its denominator at the gate over the ledgered
gates and the door checks together, and every gate carries at least one
recipe.

## 13. Deviations, priced

1. **One arena per fork arm.** Every census runs at the committed chart
   and the aligned union. Price: the counts are properties of those
   arenas. Mitigation: within them nothing is sampled -- every event,
   every state, every completion, every target of the declared windows.
2. **The record window is round-generated.** CONTRACT's own 36 corpus
   count fields are a driver product this unit does not rebuild; the LP's
   record fiber is the field each admissible round writes, declared as
   this unit's window and proved closed under a translation generator.
   Price: the fiber is a window, not the parent's corpus. Mitigation: the
   window is generated by the law-selected round set itself, so no
   tie-break selects a member.
3. **The persist readings are declared predicates.** AUTOGLUE sealed the
   persistence question as a reading; the two persist members here are
   exact predicates this unit declares so that the separation could be
   measured at all. Price: the separation numbers are relative to the
   declared family. Mitigation: the family is published, the base member
   is the parent's own, and the verdict word does not depend on the
   members' details.
4. **The dimension leg is computed at the primary rows.** The
   record-fiber rows carry feasibility words without a polytope
   dimension, stamped FEASIBLE-AT-THE-FIBER-ROW and disclosed. Price: six
   rows carry no dimension. Mitigation: every primary feasible row
   carries its dimension with an implicit-equality census and a
   second-point certificate.
5. **The debt is decided at the committed windows.** Whether a successor
   theory should let the walk's menu grow is not decided here; what is
   decided is that the committed walk and the committed creation relation
   never contradict each other. Price: the word decided is scoped.
   Mitigation: the scope is stamped in the verdict segment itself.

## 14. The successor register

Registered, not claimed.

**S-1 -- the LP at the persistent state.** If a successor unit declares
the persistent reading, the seam completion joins the state and the LP's
variables range over a larger object. Nothing here anticipates the result;
what is registered is that the re-solved stamp on every LP row is exactly
what a persistent successor would have to re-derive.

**S-2 -- the feasible pocket of the record fiber.** Exactly 2 round
fields keep the uniform amplitude feasible under the delivered order. What
distinguishes them -- and whether the pocket survives at deeper records --
is unmeasured.

**S-3 -- the composition.** The three maps are typed here and composed
nowhere; DYNAMICS-CLOSURE owes the normalized kernel, the behavioural
equivalence standard, and the a = 2 branch as its own theory with its own
grammar declaration.

**S-4 -- the coin at a grown carrier.** The committed covariant family is
typed on three-direction site blocks and stops at the created cell. A
successor that wants evolution across creation owes a coin family for the
grown block, and the exclusion here is type-level, not a search.

**S-5 -- COSET-FROM-COMPATIBILITY.** The coset principle is carried as
declared (the selection law) through every row of the interface table; the
registered test of the charts successor decides whether it upgrades.

**What may not be inherited**: the LP's MANY families as selected laws;
the fiber-feasible pocket as anything but a count; the carrier family's
unselected members as a chosen formalism; the debt's no-conflict verdict
at any window where the menu can grow; and the seam verdict as a decision
between the readings -- it is the measured certificate that the committed
corpus cannot make that decision.
