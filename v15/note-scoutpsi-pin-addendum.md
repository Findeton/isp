# SCOUT-PSI PIN ADDENDUM (FROZEN) — THE #68 FREEZES

**Frozen:** 2026-08-17, v15 ledger #68 (eleventh external review).
Amends (never relaxes) v15/note-scoutpsi-pin.md (8e9fe2448b00).
Routed to the in-flight worker with the required
already-inspected disclosure.

1. THE PREPARATIONS — PRE-REGISTERED EXACTLY, nothing selected
   after behavior is visible:
   PRIMARY rho* = (1/2)|a><a| + (1/2)|b><b| where |a>,|b> are the
   two LEXICOGRAPHICALLY FIRST computational (cell-coin) basis
   states in the S4 witness's support, read from the committed
   scout receipt by the deterministic rule (anchor the field).
   D1 = {1/2 |a>, 1/2 |b>}.  D2 = the plus/minus ensemble
   {1/2, |a>+|b>; 1/2, |a>-|b>} given as UNNORMALIZED vectors with
   norms absorbed into the Born functionals — every record
   probability is a squared modulus, computed exactly in Q(omega);
   no radicals enter.
   SECONDARY rho** = the maximally mixed state on the S4 carrier.
   D1 = the computational basis, uniform.  D2 = the F3-character
   (Fourier) basis, uniform (characters native to Q(omega)).
   Equality of rho across each D1/D2 pair is PROVEN in-run as a
   gate before any propagation.
2. THE RECORD DISTRIBUTION — FOUR FROZEN COMPARISON ROWS, each its
   own verdict word, at windows 1|2|3:
   {complete ordered CELL-HIT emission histories, final count
   field} x {raw labels, relabelling-quotiented}.
   Sensitivity vanishing under coarse-graining or quotient is
   verdict-relevant and published per row; no row is silently
   dropped.
3. THE NULL INSTRUMENT — ONE explicit frozen object: the selective
   Born-collapse instrument paper-20 names as not-run.  Outcome c
   with probability w_c(psi); post-state P_c psi renormalized
   (P_c = the S4 formalization's cell-component projector);
   record += c.  On mixtures: rho -> P_c rho P_c / tr, outcome
   weight tr(P_c rho).  Its effects and post-states are stated in
   the note; it must be BLIND at all four rows and all windows
   (the null gate).
4. OPERATIONAL QUALIFICATION (gated wall sentences): a SENSITIVE
   verdict proves rho is not a sufficient state descriptor FOR THE
   DELIVERED CELL-HIT RULE on pure-state ensembles; it becomes an
   experimental distinction only if ISP can operationally prepare
   both decompositions — preparation/intervention protocols are
   UNBUILT; and a future triple-event law with a different state
   update may remove or change the sensitivity.  No sentence may
   read the verdict as deciding the psi-ontology.
5. NUMERAL TOTALITY (from birth): every numeral occurrence in the
   note classified BOUND (specific receipt field) or NON-CLAIM
   (reason class), the classification gated and total.
