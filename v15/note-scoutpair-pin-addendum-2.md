# SCOUT-PAIR PIN ADDENDUM 2 (FROZEN) — THE #87 CORRECTIONS (SEVENTEENTH REVIEW)

**Frozen:** 2026-08-17, v15 ledger #87.  Amends the pin (67e6082b445a)
and addendum 1 (b3aa0f973ae1).  EXPLICITLY REPLACES outcome
vocabulary — no prose-precedence:

1. S2 OUTCOME WORDS REPLACED: the base pin's "curvature-probe
   reading SUPPORTED/REFUTED" words are RETIRED.  The outcomes are:
   SPAIR-ORDER-DEFECT-NONZERO / SPAIR-ORDER-DEFECT-ZERO, always
   accompanied by GEOMETRIC-INTERPRETATION-UNTESTED-FIXED-G.  The
   three-actor curvature reading appears only as a CANDIDATE
   interpretation under the pair-update hypothesis.
2. S4 OUTCOMES MADE ASYMMETRIC: insufficiency is provable by one
   witness; sufficiency is NOT provable by finite census.  The
   positive word is SPAIR-NO-INSUFFICIENCY-WITNESS-THROUGH-<window>
   (never "SUFFICIENT"); all-future sufficiency would need a
   closure/bisimulation theorem, named as the successor.  The
   geometry leg is BLOCKED-AT-FIXED-G (record sufficiency under the
   fixed-background walk is what is testable; say so).
3. S4 PREMISE GATE (mandatory, before any future-comparison): prove
   (R1,G1)=(R2,G2) as RAW recorded states, or equality under ONE
   explicitly declared physical quotient (named, counted).  SCOUT-K's
   clash rows have identical COVARIANT COEFFICIENT VECTORS — that is
   NOT raw record equality; using them without the gate would repeat
   the type error this unit exists to catch.  BETTER SEEDS: the REC
   collision classes (different histories, byte-identical records —
   paper-41's censused non-injectivity); bind them by anchor.
4. S3 OUTCOMES SPLIT (replacing the single verdict): 
   SPAIR-CK-DIVISIBLE-AT-TESTED-CUTS /
   SPAIR-PROCESS-TENSOR-DIVISIBLE-AT-TESTED-CUTS /
   SPAIR-INTERVENTION-SEMANTICS-UNBUILT (the honest word when only
   passive CK divisibility was testable) — plus the refusal forms
   scoped as before.  TOMOGRAPHIC SUFFICIENCY: prove the supplied
   preparation set spans the state space the transition maps act on
   (or publish the deficiency — a map agreeing on tested
   preparations may fail elsewhere; the gate says which).  STATE
   GRAINS: "the declared configuration space" is ontology-dependent
   — run the divisibility question separately at the grains
   (record+geometry only / +trigger trace / +psi / +full history) or
   prove grain-independence; each verdict names its grain.
5. THE INTERPRETATION WALL (binding on the note's conclusions): an
   S4 insufficiency witness shows ONLY that current pair-record +
   geometry is not a sufficient INSTANTANEOUS state; in a
   non-Markovian process two identical presents may lawfully differ
   by HISTORY.  It does NOT show beables must be higher-arity.  The
   distinct explanations (realized history / trigger memory / ontic
   psi / n-body beable) stay distinct and unchosen — gated.
