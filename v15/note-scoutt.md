# SCOUT-T — THE TRIGGER-TRACE FORK: THE TRACE DOES NOT RESCUE THE COVARIANT KERNEL, AND MARGINALIZATION DOES NOT DISSOLVE THE DISTINCTION (REPORT NOTE)

**Delivered:** 2026-08-17.  Scout class: note + code + output + receipt;
battery + one verifier seat.  CANDIDATE READINGS until the verifier seat.

**Pin:** v15/note-scoutt-pin.md (FROZEN 3f35573d88d8, v15 ledger #82),
amended by the #87 SCOUT-T addendum v15/note-scoutt-pin-addendum.md
(15d763633293) and bound by the #68 SCOUT-K addendum
v15/note-scoutk-pin-addendum.md (3a1e5a649537).
**Instrument:** v15/code/scoutt_exact.py; artifacts
v15/code/scoutt_output.txt and v15/code/scoutt_receipt.json.
**Parent binding (the #58 snapshot precedent):** the live
v15/code/scoutk_exact.py is held by the concurrent SCOUT-K micro-repair
worker and its working-tree bytes differ from the #74 commit, so this
unit binds SCOUT-K's committed apparatus SNAPSHOT-ONLY:
v15/code/scoutt_parent_scoutk74.py (38c3f6cb288e, byte-equal to the #74
commit of scoutk_exact.py) and
v15/code/scoutt_parent_scoutk74_receipt.json (5af53face093, the
delivered receipt), plus the frozen walk snapshot
v15/code/scoutk_parent_delivered.py (edb60bccd22e) and
v14/paper-20-coupling.md (4824d190af73).  LOG.md and v15/PLAN.md are
anchor-only and never digest-recorded (the receipt hazard; checked
in-run). [LIC:G-PIN-DIGESTS] [LIC:G-ENV-EXCLUSION]
**Timing disclosure:** the mid-flight state described above is the
state AT THIS UNIT'S BINDING; while this unit was in construction the
SCOUT-K micro-repair committed (v15 ledger #93, stated there as moving
zero measured values) and SCOUT-K closed-sealed (#94).  This unit's
binding stays at the #74 delivered apparatus through the pinned
snapshots, per the pin's own instruction and the #58 precedent; the
delivered numbers the T-blind leg binds byte-exactly are the #74
receipt's, which the #93 entry states are unmoved.

**Order-of-events disclosure (the #77 routing discipline):** the #68
addendum was in the birth orders, before any construction.  The #87
addendum arrived by routed message while instrument sections were being
drafted and BEFORE any feasibility row was solved or any solver output
inspected; the single-full-trace arm-1 design was replaced by the
five-grain minimality ladder, and the #84-form W-REPRESENTATION citation
was replaced by the #86 fork-neutral form, prior to any computation.
Computations inspected before receipt: NONE (syntax checks only).
The #98 addendum (eighteenth external review, routed per #77, ledger
#98) was received AFTER this unit's artifacts were first sealed and
while the self-battery was mid-run; all solver results had been
computed and inspected at receipt; its items change instrument auditing
(the receipt-wide digest inventory, the measurement-subtree numeral
inventory, the hostile plants) and claim language (the
event-selection-kernel narrowing, the joint-successor-law diagnosis,
the four-class model space) and NO measured value; the delivery was
re-run and the battery restarted on the folded instrument.
The #102 diagnosis (twentieth external review, ledger #102, routed per
#77) was received while the folded battery's selftest leg was in
flight: the arm-1 status/certificate mutants targeted the INHERITED
full grain (dead code under the ladder collapse) and the SAMPLES gates
validated status words only.  Both wired defects are fixed — the
injection now targets the SOLVED PREV1 grain, and the SAMPLES gates
validate every published status word against the solver object
(INFEASIBLE exactly when the solver's gap is positive; the chosen
repair of the two the review offered) — the scope sentence is
engraved, and delivery, selftest and the full battery were re-run
fresh.  Audit wiring only; no measured value moved.

## VERDICTS (candidate readings; every verdict names its mode)

SCOUT-T verdicts:

SCOUTT-TRACE-CENSUS-PUBLISHED<81-CONTEXTS-243-TUPLES-9-TRIGGER-EVENT-PAIRS; FULL-GRAIN-ORBIT-VARIABLES-37-37-46-43-46-PER-ARM-VS-PARENT-16-16-25-19-25; EVERY-TRACE-PARTITION-REFINES-THE-PARENT>

SCOUTT-TRACE-SUFFICIENT-1-AT-2<CONDITIONAL-MODE; VACUOUS-BY-BLINDNESS; TRACE-EMPTY-AT-STEP-ONE>; SCOUTT-MARGINAL-AGREES-AT-2-ALL-GRAINS<MARGINAL-HISTORY-MODE-EXACT-FACTORIZATION; VACUOUS-BY-BLINDNESS: the window-2 observable-history distribution is q1 x q2 for EVERY normalized kernel, equal to the walk's>

SCOUTT-TRACE-INSUFFICIENT-AT-3-SA<ALL-FIVE-GRAINS-EMPTY; CONDITIONAL-MODE; UNIFORM-IN-THE-FIRST-STEP-LINE-WEIGHT; CERTIFICATES-PUBLISHED-AND-VERIFIED>; SCOUTT-TRACE-INSUFFICIENT-AT-3-MC<ALL-FIVE-GRAINS-EMPTY; CONDITIONAL-MODE; UNIFORM-IN-THE-FIRST-STEP-LINE-WEIGHT; CERTIFICATES-PUBLISHED-AND-VERIFIED>; SCOUTT-TRACE-INSUFFICIENT-AT-3-CN<ALL-FIVE-GRAINS-EMPTY; CONDITIONAL-MODE; UNIFORM-IN-THE-FIRST-STEP-LINE-WEIGHT; CERTIFICATES-PUBLISHED-AND-VERIFIED>

SCOUTT-MARGINAL-DISAGREES-AT-3-SA-RD-ORD-RAW; SCOUTT-MARGINAL-DISAGREES-AT-3-SA-RD-CNT-RAW; SCOUTT-MARGINAL-DISAGREES-AT-3-SA-RD-ORD-GAM; SCOUTT-MARGINAL-DISAGREES-AT-3-SA-RD-CNT-GAM; SCOUTT-MARGINAL-DISAGREES-AT-3-CN-ORD-RAW; SCOUTT-MARGINAL-DISAGREES-AT-3-CN-CNT-RAW; SCOUTT-MARGINAL-DISAGREES-AT-3-CN-ORD-GAM; SCOUTT-MARGINAL-DISAGREES-AT-3-CN-CNT-GAM; SCOUTT-MARGINAL-DISAGREES-AT-3-MC-GLOBAL-ORD-RAW; SCOUTT-MARGINAL-DISAGREES-AT-3-MC-GLOBAL-CNT-RAW; SCOUTT-MARGINAL-DISAGREES-AT-3-MC-GLOBAL-ORD-GAM; SCOUTT-MARGINAL-DISAGREES-AT-3-MC-GLOBAL-CNT-GAM<MARGINAL-HISTORY-MODE-EXACT-FACTORIZATION; ORDERED-RAW-COLLAPSES-ONTO-THE-CONDITIONAL-SYSTEM; NO-LINEAR-RELAXATION-OVER-HISTORY-FLOWS>

SCOUTT-TBLIND-REPRODUCES-PARENT<18-OF-18-BYTE-EQUAL-GAPS; TIED-ROUTE-REFUSED; MANDATORY-CROSS-CHECK-BOUND>

SCOUTT-TRIGGER-AND-TRACE-STAY-CANDIDATE-MARKED<FEASIBILITY-NEVER-NATURE; FORK-NEUTRAL-PER-87; EVENT-SELECTION-ONLY-TRANSPORT-STILL-OPEN>

## 1. THE QUESTION AND ITS WALLS

The pin's arm 1, quoted:

> does a minimal covariant kernel K(e|c,G,R,T) — T the ORDERED TRIGGER
> TRACE (the sequence of past trigger cells, exactly what the bridge
> erased) — preserve the delivered walk at the committed windows?

> T enters the orbit space (orbits of (G,R,T,c,e) under simultaneous
> relabelling); one variable per orbit; conditional consistency mode;
> depths 2 and 3.

The pin's arm 2, quoted:

> build the MARGINAL-HISTORY comparison: an exact factorizing
> history-level process (Markov in the complete successor, latent
> triggers summed out) compared to the walk's OBSERVABLE record
> distributions only, at the same windows; exact polynomial method with
> certificates per the #68 freeze (the linear-relaxation refusal binds).

The mandatory cross-check, quoted:

> CROSS-CHECK (mandatory): the T-blind subfamily of arm 1 must
> reproduce SCOUT-K's refusal (binds to the parent).

The #68 consistency-mode freeze binds identically here, quoted:

> PRIMARY = CONDITIONAL transition agreement at every reached state
> (each constraint linear in exactly one kernel factor; LP/Farkas
> valid).

> SECONDARY (only if attempted, disclosed) = marginal-history agreement
> WITH exact Markov factorization preserved, decided by an exact
> polynomial method with certificates.

> A linear relaxation over history flows is REFUSED as an
> outcome-bearing method: it can certify "kernels" that do not factor
> into one reusable K.  Every verdict names its mode.

The #82 trigger-erasure mechanism this fork answers to, quoted:

> the kernel construction builds the available record from the WHOLE
> TRIPLE FOOTPRINT (R1 = nfield(BLOCK_OF[e1])) while the target walk
> depends on the individual CELL-TRIGGER history (q3 over [c1],[c2]).

> The bridge lets one cell trigger a three-cell event, AND THE TRIPLE
> RECORD FORGETS WHICH CELL WAS THE TRIGGER; the walk remembers and
> uses it.

The parent result stays at its #82 precise formulation and never
wider ([SS:TRIPLE-EVENTS] for the kernel's outcome space;
[LIC:G-TBLIND]):

> at this arena, no normalized covariant conditional kernel K(e|c,G,R)
> with fixed geometry, the trigger factorization P(c,e)=q(c)K(e|c,G,R),
> and exact preservation of the delivered cell-walk's conditional
> statistics, works through window 3 — even with the entire record.

The honest parent scope sentence is preserved: no record-blind,
fixed-alpha, affine-equivariant kernel preserves the delivered
three-step walk statistics. [LIC:G-KREACH]

The scope wall, quoted from the #68 addendum and gated:

> G is FIXED throughout — this unit tests RECORD backreaction only;
> success does not establish quantum transport across AUTOGLUE-created
> cells or any topology change G→G'; those remain independent missing
> laws.

G is FIXED throughout — this unit tests RECORD backreaction only;
success does not establish quantum transport across AUTOGLUE-created
cells or any topology change G→G'; those remain independent missing
laws. [LIC:G-KERNEL-WALL]

The #87 fork-neutral representation wall (replacing any #84-form
citation), quoted from v15/PLAN.md:

> no currently supplied mathematical object (K, psi, rho, H, a field, a
> trigger) may be promoted to ontology merely because the equations
> need it; an ontic psi, field, or trigger remains admissible if
> independently declared, operationally distinguished, and mapped to
> the beables.

and from the #87 addendum:

> Your unit tests whether a trace COULD be real — it must not
> presuppose either answer.

> Publish the feasibility verdict PER GRAIN with
> certificates/dimensions; the honest headline names the COARSEST
> feasible grain.

> SCOUTT-TRACE-SUFFICIENT carries its grain:
> SCOUTT-TRACE-SUFFICIENT-<grain>-<dim>-AT-<depth>.

Accordingly: an ontic trigger or trace remains admissible if
independently declared, operationally distinguished, and mapped to the
beables.  TRACE-SUFFICIENT at any grain is a feasibility fact, never
nature.  the honest headline names the COARSEST feasible grain.  this
unit tests whether a trace COULD be real and presupposes neither
answer; whether the trigger is real or bookkeeping is the fork's open
question. [LIC:G-REP-WALL]

> Even TRACE-SUFFICIENT (SCOUT-T) is feasibility, not nature.

The pin's own wall sentence, quoted:

> even TRACE-SUFFICIENT is a FEASIBILITY fact, not an ontology
> decision; gated

The #84 arity split binds the vocabulary (see the term table, section
9), quoted:

> RECORD-ARITY (two actors per elementary relational fact — the atomic
> beable is a pair relation) vs PROCESS-ARITY (any number of actors may
> participate in ONE indivisible boundary-to-boundary transition).

The #82 fork context for arm 2, quoted:

> TRIGGER BOOKKEEPING — per-latent-history equality may be too strong;
> test the marginal-history mode (the frozen secondary the #68 addendum
> already names): an exact factorizing history-level process compared
> AFTER latent triggers are marginalized.

The paper-20 anchors (the committed identification of the walk's
observables), quoted:

> A division event on cell (x, l) increments n_l(x) by one.

> The menu at site x is the three link traversals and the weight q(l|x)
> is the post-coin Born weight |(Cψ)(x,l)|².

> The record accumulates the law's own weights and the state is not
> collapsed onto the emitted cell, so the walk stays coherent between
> division events.

The parent orbit-count context, quoted from the #74 ledger entry:

> 16 to 25 orbit variables per arm against the fixed-alpha family's one

## 2. THE APPARATUS

The committed arena, relabelling group and delivered walk are rebuilt
from constructors and gated: 27 cells in bijection with linked actor
pairs, 27 triangles among 84 triples, 9 declared lines of 12, the
group of order 108 closed on the block class, the first-trigger weights
(1/9, 4/9, 4/9) on cells 0, 1, 2 and the 11-variant depth-2 blindness.
[SS:CELLS] [LIC:G-ARENA] [LIC:G-GAMMA] [LIC:G-WALK] [LIC:G-BLIND]

The four proximity arms and the global reference are the PARENT'S
pre-registered, digest-sealed predicates, reused unchanged by anchor
from the pinned snapshot and re-measured relabelling-covariant at all
108 elements; no new proximity predicate is introduced by this unit.
The trace slot T is carried WHOLE at every arm (a locality-restricted
trace is a registered successor, never an improvised predicate).
[SS:TRIPLE-EVENTS] [LIC:G-ARMS-COVARIANT]

Arm 1 runs in the #68 PRIMARY mode: CONDITIONAL transition agreement at
every reached state.  The step-one kernel factor at the blank record
and empty trace is the measured 2-orbit, dim-1 line-weight family (an
exact census fact, not a relaxation), so every depth-3 system is linear
in the second kernel factor at each sampled line weight a in {0, 1/6,
1/3, 1/2, 2/3, 1}; emptiness at every sample is closed over the whole
segment by one uniform-in-a Farkas certificate per system (the
affine-endpoint argument). [SS:TRIPLE-EVENTS] [LIC:G-STEP1T]

Arm 2 runs in the #68 DISCLOSED SECONDARY mode: the exact factorizing
history-level process is Markov in the complete successor
configuration (G, R, psi-evolution under the written triple record);
the trigger is NOT carried in the state; the latent event histories are
summed inside each exactly factorizing Markov step; the observable
compared is the delivered walk's record observable — the CELL-HIT
emission history and its final count field, under paper-20's anchored
emission-weight identification of the trigger — at four disclosed
grains.  No linear relaxation over history flows is used
anywhere; every system is decided by the same exact LP/Farkas method
that the conditional linearization licenses, plus fiber-summed rows per
grain. [SS:COMPLETE-SUCCESSOR-CONFIGURATIONS] [LIC:G-A2-SAMPLES]

Solver: exact rational arithmetic end to end.  Feasibility is decided
by rref-first reduction (a linear inconsistency yields a Farkas
certificate over the PUBLISHED rows by transpose solve; a consistent
system goes to phase-one simplex on the reduced rows, with witnesses
verified against the original rows and reduced certificates lifted
back to the original rows).  Every certificate satisfies y.A <= 0
columnwise, y <= 1, y.b = gap > 0; every witness is nonnegative and
satisfies the equalities exactly; every published polytope dimension is
recomputed from its proven always-zero set and interior point.
[LIC:G-A1-CERT] [LIC:G-A2-CERT] [LIC:G-A1-DIM]

## 3. THE TRACE REACH CENSUS

the trace reach census against the parent: the full ordered trace lifts
the per-arm depth-3 orbit counts from 16/16/25/19/25 to 37/37/46/43/46,
over 243 raw tuples in 81 raw contexts (9 trigger-event pairs); every
trace partition refines the parent partition [SS:TRIPLE-EVENTS]
[LIC:G-TREACH]

The parent's own T-blind census is reproduced inside this instrument
and matches the pinned delivered receipt field-by-field: 63 contexts,
189 tuples, 7 distinct first events, orbit variables 16/16/25/19/25.
The measured coincidences carry over to the trace level: SA and RD
induce the same trace partition, MC and GLOBAL induce the same trace
partition, CN sits apart; one representative is solved per distinct
partition (SA, MC, CN). [LIC:G-KREACH] [LIC:G-TCOINCIDE]

Step one, published separately (the #64 and #68 first-event requirements):
the trace is EMPTY before the first trigger, so the step-one census
with the trace slot collapses to the parent's 2 orbit variables (line /
non-line), dim 1 after normalization — the trace adds nothing at the
first event, and the tie-break trilemma stands exactly as the parent
measured it. [SS:TRIPLE-EVENTS] [LIC:G-STEP1T]

Depth 2: consistency is vacuous under the measured blindness at both
arms and every grain; the covariant family is the one-parameter
line-weight segment, dim 1. [SS:CELLS] [LIC:G-D2T]

## 4. THE MINIMALITY LADDER (the #87 replacement for the single
full-trace test)

Five trace grains, ordered by information content: NONE (the T-blind
parent family), PREV1 (previous trigger only), COUNTS (unordered
trigger counts), SUFFIX2 (length-2 suffix), FULL (the full ordered
trace).  Feasibility is published PER GRAIN; an inherited verdict is
licensed only by a byte-level partition-and-rows identity, gated.
[LIC:G-LADDER]

the minimality ladder collapses at this window by measurement: every
trace-bearing invocation sees a length-1 trace, so PREV1, COUNTS,
SUFFIX2 and FULL induce byte-identical systems; their verdicts are one
verdict, and the honest headline names the COARSEST feasible grain,
never the full trace

Measured refinement structure: every trace grain's partition refines
the NONE partition; SUFFIX2 refines PREV1; FULL refines every other
grain; and at this window the four trace-grain partitions are pairwise
equal.  The per-grain variable counts per representative: SA 16 at
NONE and 37 at every trace grain; MC 25 at NONE and 46 at every trace
grain; CN 19 at NONE and 43 at every trace grain. [LIC:G-LADDER]

## 5. ARM 1 — THE TRACE DOES NOT RESCUE THE COVARIANT KERNEL

Every ladder grain of every representative system is INFEASIBLE at all
6 sampled line weights: 5 grains x 3 systems x 6 samples, every status
decided, every refusal certificated, and every solved all-empty system
closed over the whole segment a in [0,1] by one verified uniform-in-a
Farkas certificate (supports 3 to 7 rows).  The branchwise lemma is
also refused at every grain: even conditioning on each written first
triple separately, no covariant trace-carrying second-invocation
kernel matches the delivered third-step profiles at the committed
window. [SS:CELLS] [LIC:G-A1-SAMPLES] [LIC:G-A1-UNIFORM]

The headline is therefore SCOUTT-TRACE-INSUFFICIENT-AT-3 at every arm:
there is NO coarsest feasible grain because no grain is feasible, and
by the measured ladder collapse the refusal of one trace grain is the
refusal of all four.  At depth 3 the trace available to the second
invocation has only ONE previous trigger, so FULL, PREV1, COUNTS and
SUFFIX2 COINCIDE — a measured, publishable fact — and the scope is
exact: this verdict defeats THIS trace repair AT THIS WINDOW; it does
not rule out longer-memory distinctions at later windows (ledger
#102).  The two candidate repairs of the #82 mechanism
both fail at this window: remembering the trigger (arm 1) does not
restore the covariant bridge, and marginalizing the trigger (arm 2,
section 6) does not dissolve the mismatch.

THE MECHANISM, measured at two levels:

the clash-T probe: the trace refinement does NOT separate the parent's
mechanism witness rows (0,5,14) and (1,11,21) at a = 0; the parent
identification survives the trace [SS:CELLS] [LIC:G-CLASH-T]

The reason is exact: the relabelling that identifies the two branches
maps the trace along with everything else — the trace transforms
covariantly, so the identification the parent measured (with delivered
walk values 16/729 against 64/729) persists in the trace-orbit space.
Covariance identifies what the anchored start state distinguishes, and
the trace, being covariant data, cannot break that identification.

the free-relaxation lemma splits the mechanism in two: at a = 0 every
one of the 27 branch pairs is feasible once covariance is dropped --
there the refusal is COVARIANCE ITSELF, the relabelling identification
the clash-T probe shows surviving the trace -- while at all five other
sampled line weights 25 of 27 branch pairs are refused even with
covariance dropped entirely, so there no trace-informed
second-invocation kernel of any kind matches the delivered walk at the
committed window: the record-evolution mismatch itself is the
obstruction, and no TRIGGER-TRACE information added to THIS NORMALIZED
EVENT-SELECTION KERNEL repairs it — other state variables and
DIFFERENT UPDATE LAWS remain open [SS:CELLS] [LIC:G-A1-FREE]

Consistency across relaxation levels is gated: wherever the covariant
trace system were feasible, every branch pair of the free relaxation
would have to be feasible too; no violation is observed.
[LIC:G-A1-CONSIST]

## 6. ARM 2 — MARGINALIZATION DOES NOT DISSOLVE THE DISTINCTION

arm 2's ordered-raw grain collapses onto the conditional system by the
measured window-2 blindness (every prefix marginal is q1 x q2 for every
normalized kernel), so the marginal-history question lives entirely at
the count-field and relabelling-quotient grains, each published with
its own verdict and certificates [SS:CELL-HIT-HISTORIES]
[LIC:G-A2-COLLAPSE]

The four disclosed grains (the #76 grain lesson), each with its own
verdict at windows 2 and 3:

| grain | observable | rows at window 3 | fiber structure |
| ORD-RAW | the complete ordered CELL-HIT emission history, raw | 729 | all fibers singletons |
| CNT-RAW | the final count field, raw | 594 | 459 singleton fibers, 135 doubleton fibers |
| ORD-GAM | the emission history quotiented by the relabelling group | 138 | fibers of size 2 to 8 |
| CNT-GAM | the count field quotiented by the relabelling group | 45 | fibers of size 2 to 28 |

Window 2: MARGINAL-AGREES at every grain, vacuously — the bridge's
window-2 observable-history distribution equals the walk's for every
normalized kernel, by the measured blindness. [SS:CELL-HIT-HISTORIES]
[LIC:G-BLIND]

Window 3: MARGINAL-DISAGREES at every grain and every system — all
four grains at all three distinct systems are INFEASIBLE at all 6
sampled line weights, each refusal certificated and each grain-system
closed over the whole segment by a verified uniform-in-a certificate
(supports 5 to 19 rows).  Even the coarsest grain — the count field
quotiented by the full relabelling group, 45 rows — refuses every
exactly factorizing trace-free process. [SS:COUNT-FIELDS]
[SS:GAMMA-ORBIT-HISTORIES] [SS:GAMMA-ORBIT-COUNT-FIELDS]
[LIC:G-A2-SAMPLES] [LIC:G-A2-UNIFORM]

Reading (candidate): the distinction the conditional mode measures does
NOT disappear after correct marginalization at any disclosed grain, at
this window, in the trace-free factorizing class.  Sensitivity survives
the order-forgetting quotient and the relabelling quotient.

## 7. THE MANDATORY T-BLIND CROSS-CHECK AND THE CONTROLS

The T-blind subfamily is realized twice. [LIC:G-TBLIND]

First, byte-exactly: the parent's three systems are rebuilt and solved
by the parent's own pipeline, and all 18 runs match the pinned
delivered receipt in status, row count and gap string — the depth-3
refusal reproduced identically, with every Farkas certificate
re-verified by this instrument's own checker independently of the
parent's. [SS:CELLS] [LIC:G-TBLIND-CERT]

Second, literally: the trace-carrying systems with tying equalities
across trace orbits inside one parent orbit (for example 21 tie rows on
the SA system at 117 rows) are refused at every sampled line weight —
the T-blind subfamily of the trace family reproduces the parent
refusal from inside this unit's own variable space. [SS:CELLS]

Controls, forced both ways through the real builders and the real
solver: the synthetic covariant trace-kernel target is FEASIBLE with a
nonnegative witness and a recomputed polytope dimension of 16; the
shifted target is INFEASIBLE with a verified certificate; the same
pair pushed through the real count-field grain aggregator gives
FEASIBLE and INFEASIBLE again.  Four controls, four expected outcomes,
zero silent legs. [SS:COUNT-FIELDS] [LIC:G-CONTROLS]

## 8. WHAT THIS DOES AND DOES NOT SETTLE

this unit tests whether a trace COULD be real and presupposes neither
answer; whether the trigger is real or bookkeeping is the fork's open
question

At this arena and these windows, the #82 fork's two scout-computable
arms both close NEGATIVE: the ordered trigger trace — at every grain of
the minimality ladder — does not restore a covariant conditional
kernel, and exact marginalization does not make the factorizing
trace-free process match the walk's observable record distributions at
any disclosed grain.  The state-slot question the pin names therefore
reopens toward the remaining fork arms (probabilities over complete
triple successors; a triple-native dynamics): the erasure reading is
NOT the whole story, because even un-erasing the trigger (arm 1) or
refusing to condition on it (arm 2) leaves the delivered walk
unmatched. [SS:TRIPLE-EVENTS]

The free lemma sharpens where the obstruction lives: at a = 0 it is
covariance alone (a non-covariant trace kernel exists there,
branch-pair by branch-pair); at every other sampled line weight it is
the record-evolution mismatch (triple-record phases against
cell-record phases) — and there, no TRIGGER-TRACE information added to
THIS NORMALIZED EVENT-SELECTION KERNEL repairs it — other state
variables and DIFFERENT UPDATE LAWS remain open, explicitly.  Pricing
the covariance exit is registered as a successor, not claimed.
[SS:CELLS] [LIC:G-A1-FREE]

THE #98 DIAGNOSIS, verified against this unit's own construction: the
delivered walk evolves its state on the INDIVIDUAL CELL-HIT history
(the target side conditions the state update on the cell record of
[c1] and then [c1,c2]), while the candidate event layer evolves its
state on WHOLE TRIPLE FOOTPRINTS (the bridge side conditions the same
update on the block records of e1 and then e2) — the two q3 call forms
of the instrument are exactly these two rules.  A probability kernel
over candidate events was being asked to make two INCOMPATIBLE
STATE-UPDATE RULES agree; no kernel should be expected to repair that.
The missing object is the JOINT SUCCESSOR LAW: (relations, geometry,
process state) to (new relations, new geometry, new process state) —
one law updating all three together (ledger #98). [SS:TRIPLE-EVENTS]

THE #98 MODEL SPACE (replacing any narrower fork): the live successor
space is FOUR dynamics classes, to be compared by the SAME
complete-successor observable, never by making one model's event
kernel compensate another model's state evolution (ledger #98):
pair-native — one pair changes and geometry and state backreact
locally, with substep generators, fractional evolution and
simultaneous compatible changes untested by SCOUT-PAIR's clocked test,
which only proved three-full-old-steps is not one-old-step; joint
rewrite over compatible collections — the field analogue;
triple-native; and n-adaptive — effective arity a function of the
local relational situation. [SS:COMPLETE-SUCCESSOR-CONFIGURATIONS]

TRACE-SUFFICIENT at any grain is a feasibility fact, never nature —
and no TRACE-SUFFICIENT was earned here at window 3.  an ontic trigger
or trace remains admissible if independently declared, operationally
distinguished, and mapped to the beables; nothing in this unit
declares, distinguishes or maps one.

Caps, declared honestly: windows are depths 2 and 3 only; at these
windows every trace-bearing invocation sees a length-1 trace, so the
four trace grains coincide by measurement and genuinely separating the
ladder needs longer windows; G is FIXED (record backreaction only);
one representative is solved per measured distinct partition with
inherited verdicts licensed by gated byte-level identity; and every
verdict is a feasibility statement about a candidate bridge — no ontic
status is decided either way.

Registered successors: the depth-4 window; a locality-restricted
trace; longer windows where the ladder grains genuinely separate;
sub-normalized kernels; the quantum transport law rho'_e onto created
cells (open regardless); the operational protocol of the #87
admissibility route; and the NON-COVARIANT trace bridge at a = 0
(pricing that covariance exit — trilemma arm (b) territory — is a
successor unit, not claimed here).

## 9. TERM BINDING (E-34 + the #84 arity split; W4)

| CELL-HIT | paper-20's primitive: one Born-selected pair-cell increment per step (the mandatory rename); the walk's record-arity emission |
| PAIR ATOM | the record's atomic datum: one pair-relation of two actors (one cell of the chart); the count field counts pair-incidences (the #84 record-arity side) |
| DIVISION EVENT | paper-19's three-actor conflict group whose footprint writes all three pair-relations; the only object this note calls a division event |
| PROCESS EVENT | a division event carried as ONE indivisible boundary-to-boundary transition (the #84 process-arity side); same referent as division event, arity-typed |
| TRIPLE-EVENT | a division event carried as one probabilistic alternative |
| TRIGGER | the cell the quantum menu selects; the conditional seat of q(c) under the ADOPTED CANDIDATE bridge P(c,e|X) = q(c|X) K(e|c,G,R,T) -- a candidate this census tests, never an established physical mechanism |
| TRIGGER TRACE | T: the ordered tuple of past trigger cells; a candidate conditioning datum tested at five grains; whether it could be real is what arm 1 measures, and no outcome here decides that it is |
| SUCCESSOR | a COMPLETE-SUCCESSOR-CONFIGURATION X'_e = (G'_e, R'_e, rho'_e, event data): one outcome of the E-34 normalization rule, clause 4 |
| RECORD | the co-division relation with its multiplicities (ECC's sense, unchanged) |
| KERNEL CONTEXT | the localized triple (R restricted to the arm's neighborhood of the trigger, the trace at its grain, the trigger cell), up to simultaneous relabelling |
| ORBIT VARIABLE | one free kernel weight per relabelling orbit of localized (record, trace, trigger, candidate event) tuples |

## 10. NUMERAL BINDING (per-occurrence totality, the #68 standard)

Every numeral occurrence in this note is classified BOUND (specific
receipt field) or NON-CLAIM (reason class: heading, quotation, date,
digest, ledger reference, corpus label, file path, symbol name,
verdict token); the classification is total, gated at note
verification, and published in the receipt; no blanket layout
whitelist exists.  The load-bearing bindings:

| 108 | gamma/order |
| 27 | arena/cells |
| 243 | treach/raw_tuples |
| 81 | treach/raw_contexts |
| 9 | treach/first_event_pairs |
| 189 | kreach/raw_tuples |
| 63 | kreach/raw_contexts |
| 7 | kreach/distinct_first_events |
| 16 | kreach/depth3/SA/tuple_orbit_variables |
| 25 | kreach/depth3/GLOBAL/tuple_orbit_variables |
| 19 | kreach/depth3/CN/tuple_orbit_variables |
| 37 | treach/depth3/SA/tuple_orbit_variables |
| 37 | treach/depth3/RD/tuple_orbit_variables |
| 46 | treach/depth3/GLOBAL/tuple_orbit_variables |
| 43 | treach/depth3/CN/tuple_orbit_variables |
| 46 | treach/depth3/MC/tuple_orbit_variables |
| 2 | step1t/orbit_variables |
| 1 | d2t/polytope_dim |
| 11 | blindness/variants |
| 18 | tblind/refusals |
| 16/729 | a1/clash_t/parent_rhs[0] |
| 64/729 | a1/clash_t/parent_rhs[1] |
| 16 | controls/arm1_forced_feasible/polytope_dim |
| 248 | sample_spaces/declared |
| 35 | registry/falsifiers_registered |
| 37 | registry/gates_total |

## 11. THE INSTRUMENT

37 gates; 35 registered falsifiers, each dying at its declared gate
with a move proof on its named receipt object; 248 sample-space
declarations from the seven declared names; the double build compared
by digest; hostile argv refused with usage; --selftest writes nothing
and proves artifact bytes untouched; every failure writes nothing.
The #98 legs: the receipt-wide hex-digest inventory (the SCOUT-PAIR
port) classifies every hex-12 token in the serialized receipt
pinned or self, re-scans to a fixed point, re-runs on the FINAL
receipt bytes at delivery, and dies on an arbitrary unpinned-read
plant by registered mutant; the numeral-totality sweep draws its
inventory from MEASUREMENT subtrees only (claims about audit values
route through the claim-specific binding table), and permanent
hostile plants of invented numerals are refused on every build.
[LIC:G-DIGEST-INVENTORY] [LIC:G-NUM-TOTAL]
The kernel-scope wall and the fork-neutral representation wall run on
every build with permanent dead controls (verbatim, hyphen, paraphrase,
unscoped-widening and doubt-consumed-negation variants) and permanent
alive twins; both walls also police this note's own prose at
verification.  Environment exclusion is checked in-run: no digest of
any unpinned live read (LOG.md, v15/PLAN.md) occurs in the receipt
payload.  The AST determinism leg refuses bare set-iteration and raw
listdir at the source, at every hash seed. [LIC:G-SRC-CLEAN]
[LIC:G-AST-DETERMINISM] [LIC:G-SAMPLE-SPACE] [LIC:G-NUM-BIND]

Mode names in every verdict; conditional-mode results are labelled
CONDITIONAL-MODE, marginal-history results
MARGINAL-HISTORY-MODE-EXACT-FACTORIZATION, per the #68 freeze.
