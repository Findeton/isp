# SCOUT-K PIN ADDENDUM (FROZEN) — THE #68 FREEZES

**Frozen:** 2026-08-17, v15 ledger #68 (eleventh external review).
Amends (never relaxes) v15/note-scoutk-pin.md (a1a6ccc61bd4).
Routed to the in-flight worker with the required
already-inspected disclosure.

1. CONSISTENCY MODE — FROZEN CHOICE: PRIMARY = CONDITIONAL
   transition agreement at every reached state (each constraint
   linear in exactly one kernel factor; LP/Farkas valid).
   SECONDARY (only if attempted, disclosed) = marginal-history
   agreement WITH exact Markov factorization preserved, decided by
   an exact polynomial method with certificates.  A linear
   relaxation over history flows is REFUSED as an outcome-bearing
   method: it can certify "kernels" that do not factor into one
   reusable K.  Every verdict names its mode.
2. PROXIMITY PREDICATES — each arm's exact admissibility predicate
   pre-registered and digest-sealed BEFORE any feasibility row
   runs, stated only in committed objects (actor sharing,
   count-field values, the declared adjacency, event order);
   radii/thresholds fixed in the predicate; an arm that cannot be
   so formalized at this arena is REFUSED with the refusal
   published.  No predicate may be altered after any solver output
   is seen.
3. THE FIRST-EVENT REQUIREMENTS (formalizing #64): (a) the
   step-one orbit census published SEPARATELY from step-2+; (b)
   whether a unique SYMMETRIC stochastic kernel exists at the
   blank record — decided, not assumed; (c) which tie-break, if
   any, the family purchases (symmetric stochastic / declared
   initial asymmetry / extra state variable) — counted as
   declarations; (d) step-2+ record-dependent selection reported
   independently of the step-one answer.
4. SCOPE WALL (gated): G is FIXED throughout — this unit tests
   RECORD backreaction only; success does not establish quantum
   transport across AUTOGLUE-created cells or any topology change
   G→G'; those remain independent missing laws.
5. NUMERAL TOTALITY (from birth): every numeral occurrence in the
   note classified BOUND (specific receipt field) or NON-CLAIM
   (reason class), the classification gated and total.
