# K2 EFFECTUS — hostile review of THEORY-CONTRACT (paper-43)

**Seat:** K2 of the three-seat panel — the six ordered corrections, the two
standing walls, and the licensure of every claimed word. **Object:**
`v15/paper-43-contract.md`. **Authority:** `v15/note-contract-pin.md`;
`v15/PLAN.md` walls W1/W2; `v15/LOG.md` #2 (the user's six orders) and #5;
RUNBOOK through E-33. **Status: CANDIDATE UNTIL ADJUDICATION.**

**Object hashes, verified at open AND at close, five for five:**

| object | sha256-12 | open | close |
|---|---|---|---|
| v15/paper-43-contract.md | a0caabea6566 | MATCH | MATCH |
| v15/code/contract_exact.py | d7f4c15e7704 | MATCH | MATCH |
| v15/code/contract_output.txt | 2f1933c09b8f | MATCH | MATCH |
| v15/code/contract_receipt.json | 9b3638b796ae | MATCH | MATCH |
| v15/note-contract-pin.md (the pin) | 438586c11db5 | MATCH | MATCH |

---

## GRADE: ACCEPT-WITH-FIXES

No head numeral is wrong. The unit reproduces off-tree from a mirror carrying
only its pinned sources: 34/34 gates, all four verdict segments byte-equal to
the sealed `contract_output.txt`. The four verdict WORDS are correctly
licensed by their measurements. Four of the six ordered corrections are folded
not once but at every use, two of them inside the comparator grammar and the
head segment name where prose cannot undo them — this is the best order-folding
the corpus has delivered.

Against that: **one sealed claim is false in both of its halves**, refuted by
the unit's own reconstructor; **the two standing walls do not stand** — sixteen
planted paraphrases and four full-battery plants survive at exit 0, and the
wall's own falsifier is copied from the wall's pattern list; **one head field is
a single-amplitude fact** that is false elsewhere in its own declared state
space; **the "every declaration" totality is contradicted by the paper's own
census**; and **two table cells promote reconstruction to derivation** at the
exact object the orders name.

The grade is ACCEPT-WITH-FIXES and not REJECT because no measured number moves
under any repair below, and the three verdict words the user's defensible
sentence turns on (family; identifiability-within-a-class;
unique-in-the-triangular-class-up-to-the-direction-declaration) all survive.
**The adjudicator holds the lever:** if a false GATED claim is classed as a
false result under the programme's zero-false-results standard, MAJOR-1
converts this to REJECT.

---

## WHAT HOLDS — the orders, at every use (with the measurements)

**Order (c), GAUGE-WORD=WITHHELD — CLEAN, and the standard is applied per
row.** Thirteen occurrences of gauge/redundancy/unphysical/convention swept.
Every one is a withholding (head `GAUGE-WORD=WITHHELD`, line 16 unresolved
list), a quotation of the engraving (559), Q6's own question wording (226), or
an explicit denial (502-505). **No invariance-only fact anywhere is called
gauge, redundancy, unphysical, or pure convention.** The upgrade standard is
stated twice and, at 283-287, bound to *every row of the table* — "for this
group and for every other row" — not once at the top. Order folded at use.

**Order (d), direction-blindness substrate-scoped — CLEAN, and carried in the
class word itself.** Six mentions (79-84, 252-264, 236-238, 441, 457-459, head).
The scope is inside the class name `INVARIANT-IN-THE-SUBSTRATE-CENSUS`, inside
the status column `SUBSTRATE-CENSUS-INVARIANT`, and inside a gated claim
(262-264) that names the four things outside the sweep. A prose edit cannot
strip it.

**Order (e), the state scoped to the committed machine — CLEAN, and carried in
the head segment name.** The head reads
`CONTRACT-STATE-AT-FIXED-BACKGROUND-AND-INVARIANCE`. Seven mentions, all
scoped; `THE-UNIVERSAL-STATE` sits in the head's UNRESOLVED list; §7 (496-499)
states what a different dynamics would owe. No universal-state reading is
available anywhere.

**Order (f), CARRIER-CANDIDATE with no excitation vocabulary — CLEAN.** Swept
excitation / particle / quanta / propagate / localized / mode / stable. Every
occurrence of "excitation" is a withholding; "particle" appears exactly once, in
a denial (131). "Quantum" appears only as the corpus's own object name
QUANTUM-STATE. Zero leaks.

**Order (b), the fixed point scoped to the triangular class — CLEAN, and folded
into the comparator's GRAMMAR.** `k_circularity_word` carries no word that could
name a full-dynamics fixed point, and says so in its own docstring; the emitted
word is `CIRCULAR-CAST-UNIQUE-IN-THE-TRIANGULAR-CLASS-UP-TO-THE-DIRECTION-DECLARATION`;
the paper's 346-352 denies the dynamical reading explicitly and it is a gated
claim. The head, gates and prose agree.

**The defensible sentence is verbatim and gated.** Lines 14-16 carry the user's
three clauses; the string is `CLAIM_TEXTS[0]`, so G-CLAIMS-EQUAL requires it in
the paper at exact multiplicity. Deleting it fails the run.

**Ledger #5 panel-brief item 7 — NOT CONFIRMED.** The alleged SEAM census
duplication and duplicated parent quotation do not exist. SEAM appears once in
the object census (§1) and once in the parameters table (§6) — different tables
with different universes, not a double count. Quotations: 19 quote lines
resolving to 13 distinct blocks = exactly the 13 declared anchors, zero
duplicates. The item is cleared. (A different and real census-inflation issue is
MINOR-6 below; the reviewer's instinct was sound, the location was not.)

**E-24 satisfied.** The counting-only stamp is a gated claim (`CLAIM_TEXTS[12]`)
and a W-MEASURE positive leg; no fraction is offered as a probability.

**Other structure verified:** S-1 disjointness is a real transitive
reachability audit over the module call graph, not a promise; the receipt binds
the paper's exact digest (`paper_digest = a0caabea6566`), so #119's gate-to-disk
seal holds; `--render` writes nothing and cannot deliver.

---

## MAJOR-1 — a SEALED CLAIM is false in both halves, refuted by the unit's own reconstructor

**Paper 395-397, and `CLAIM_TEXTS[4]` — a gated, sealed sentence:**

> The reconstruction is not generator-independent: it holds across every
> mechanism that writes triangles and refuses on every mechanism that does not.

Both quantifiers are false. Measured with the unit's own `r_reconstruct` and its
own `is_triangle` predicate, in its own arena:

| block set | all-triangular | cover | certificate | cast recovered |
|---|---|---|---|---|
| the 27 triangles (the paper's arm 5) | yes | 27 | CERTIFIED | **yes** |
| smallest covering prefix, 23 triangles | yes | **27** | TOKEN-IN-NO-ACTOR | **no** |
| 27 triangles minus one (three variants) | yes | **27** | TOKEN-IN-NO-ACTOR | **no** |
| first 20 / 18 / 13 / 9 triangles | yes | 25/25/21/15 | TOKEN-IN-NO-ACTOR | **no** |
| 27 triangles **+ one 2-cell block** | **no** | 27 | **CERTIFIED** | **yes** |

Ten triangle-writing mechanisms fail to recover, four of them **at total
cover** — so the paper's own stated criterion (triangularity with every token
covered, 399-400) is not sufficient either. And one non-triangular mechanism
recovers and certifies, killing the second quantifier.

This is also the mandate's scope failure. §7 (512-514) correctly says "The
mechanism arms are a domain probe, not a classification of mechanisms." The
sealed claim is exactly the classification §7 disclaims, and the gate cannot
catch it: `q58_split_is_triangularity` evaluates the predicate on the nine arms
only, so a criterion that fails off the arm list passes green. The scope is
carried in §7 and in 399-400 ("it decides all nine arms") and **dropped in the
one sentence that is sealed.**

**Licensed replacement (paper 395-397 and `CLAIM_TEXTS[4]`), verbatim:**

> The reconstruction is not generator-independent. Of the nine mechanisms the
> corpus supplies, the five whose blocks are triangles covering every token
> recover the cast and the other four do not, so triangularity with total cover
> decides all nine. It is not a sufficient condition beyond them: the hostile
> round exhibited triangle-writing block sets at total cover on which the rule
> does not return the declared cast, so the criterion is stated as what
> separates these nine and not as a property of mechanisms.

No measured number moves. ARMS=9, RECOVERING=5, REFUSING=4 and the verdict word
`Q58-IDENTIFIABILITY-WITHIN-A-GENERATIVE-CLASS` are untouched.

---

## MAJOR-2 — the two standing walls do not stand, and their falsifier is copied from the wall

The user engraved W1 and W2 for the whole programme. This unit builds them as
seven `ET.SemanticWall` objects, 53 negative patterns, 11 required sentences,
and the paper (551-555) says they are "built as walls and not as prose".

**Measured: they are literal-phrase traps.** Sixteen paraphrases were planted
one at a time into the delivered paper and scanned through the gate's own call
path (`w.scan(CTX["paper"], CTX["claim_texts"])`):

| family | planted | died |
|---|---|---|
| W1 → derivation / selection / output / emergence | 5 | **0** |
| W2 → unphysical / pure convention / redundant labelling / symmetry | 4 | **0** |
| n → DERIVED | 2 | **0** |
| fixed point → full dynamics | 2 | **0** |
| state → universal | 1 | **0** |
| carrier → excitation / particle content | 2 | **0** |
| **total** | **16** | **0** |

Control: the literal banned string "the cast is derived" dies at
W-RECONSTRUCTION-IS-NOT-DERIVATION, so the wall is not vacuous — it is exact.

**End-to-end, through all 34 gates, four separate single-plant delivery runs,
each at exit 0 with every paper gate green:**

- `The record therefore fixes the cast: the nine actors follow from the record blocks alone, and nothing else was needed to obtain them.` → **exit 0**
- `The 1,296 relabellings the round law fixes are therefore without physical content: nothing an experiment could ever see distinguishes them.` → **exit 0** (G-REFERENTS-BOUND even bound the new numeral, 22 occurrences)
- `The actor count is therefore a derived quantity of the construction.` → **exit 0**
- `The construction therefore has a fixed point: actors and records determine one another uniquely.` → **exit 0**

The other three paper gates cannot help: G-CLAIMS-EQUAL gates the 15 licensed
claim texts at exact multiplicity and every paper table against a rendered one —
added prose is outside it; G-REFERENTS-BOUND binds numerals to universes, and a
violating sentence with a legal numeral binds.

**The mechanism.** `MUT-WALL` plants `"The actors emerge from the record."` —
which is the wall's own negative pattern `\bactors emerge from\b` spelled out as
a sentence. **The falsifier is written from the pattern list it is supposed to
test.** That is the corpus's registered S-1 family (the comparator is the
builder) reappearing at the one gate the user's orders depend on, and it is why
the wall passed a green badge while blind to every paraphrase.

**MAJOR-2b — the §8 description of that falsifier is false (E-23).** Paper
551-555 says "their positive controls are written as a paper would write the
violation rather than derived from the patterns." Two falsehoods: (i) the
`positive` leg of `SemanticWall` holds *required standing sentences*, not
violations — the 11 counted are the sentences the paper must carry; (ii) the
unit's only violation control, MUT-WALL, **is** derived from the patterns. E-23
makes a falsifier's published description part of the sealed surface.

**Repair — the leg already shipped and unused.** All seven walls pass
`policed=()` and `licences=()`, so `SemanticWall._licence_leg` never runs. That
leg — every sentence carrying a policed word must also carry a rendered claim —
is precisely what kills all sixteen plants. Turn it on for the two standing
walls with `policed = ("deriv", "select", "emerg", "gauge", "redundan",
"unphysical", "physical content", "convention")` and `licences = CLAIM_TEXTS`,
and write MUT-WALL as a sentence sharing no substring with any pattern.

**Licensed replacement for 551-555, verbatim:**

> The two standing walls are built as walls and not as prose: their patterns are
> regular expressions over the canonicalised paper, and their standing sentences
> must be carried or the run fails. Their reach is the reach of a pattern list —
> the hostile round planted sixteen paraphrases of both walls that the patterns
> do not match — so every sentence carrying a policed word is additionally
> required to carry a rendered claim, and the wall's own falsifier shares no
> substring with any pattern.

**Programme-wide:** the walls are engraved for all v15 units. This finding
should be routed to ARITY, AUTOGLUE and DISC before their panels close.

---

## MAJOR-3 — `DISTINCT-RESIDUE-COLLISIONS=0-OF-606` is a one-amplitude fact, false elsewhere in its own state space

The screening measurement fixes `psi_uniform = [B_Z1] * DIM` and varies only the
count field. The paper discloses "a common amplitude" (193-194), which says the
amplitude is shared across the 36 fields — not that the result is specific to it.
Measured over the declared state space (one ring element per cell, §2's own
THE-AMPLITUDE component):

| amplitude | one-step successors | equal-residue agree | **distinct-residue collisions** | Born-menu values |
|---|---|---|---|---|
| uniform (**the unit's**) | 24 | 24/24 | **0 / 606** | 24 |
| one cell occupied (e_0) | 3 | 24/24 | **205 / 606** | 3 |
| one link direction only | 3 | 24/24 | **205 / 606** | 3 |
| alternating 1, w | 24 | 24/24 | 0 / 606 | 24 |
| zero (degenerate control) | 1 | 24/24 | 606 / 606 | 1 |

The two legs part company, and the paper does not:

- **Sufficiency** (equal residue ⇒ equal successor) is **structural** —
  `b_coin_apply` reads `n[...] % B_Q` — and holds at every amplitude, including
  the degenerate ones. The sentence "The count field enters the quantum update
  only through its residue" (208) is safe, and the anchored parent sentence
  backs it.
- **Minimality** (distinct residue ⇒ distinct successor) is **amplitude-
  relative**. At a single-cell amplitude, 205 of 606 distinct-residue pairs share
  a successor, so a coarser state would suffice there. The paper's word
  "minimal" (218) and the head's `0-OF-606` rest on this leg.

The qualitative conclusion survives everywhere (record menu 36 separates more
than the Born menu at every amplitude tested), so §2's headline is not lost —
only its universe is unstated, and §7 does not disclose it.

**Licensed addition to §2 after line 217, verbatim:**

> Both legs are measured at one amplitude, the uniform one. The first is
> structural — the coin reads the count only as n mod 3 — and holds at every
> amplitude. The second is not: at an amplitude occupying a single cell, 205 of
> the 606 distinct-residue pairs share a successor. So the residue's sufficiency
> is general and its minimality is a fact about the uniform amplitude, and the
> head's zero-of-606 is stamped there.

**Head field:** `DISTINCT-RESIDUE-COLLISIONS=0-OF-606-AT-THE-UNIFORM-AMPLITUDE`.

---

## MAJOR-4 — two table cells promote reconstruction to derivation, at the object the orders name

Order (a) is that n reads RECONSTRUCTED-CONDITIONALLY everywhere, with no
DERIVED reading survivable. The parameters table (443) and the prose (453-456)
obey. Two cells do not:

- **101, the census ACTOR row** — "declared as the points of the arena, **and
  derived back from the record**". This is the forbidden promotion, in the
  reading column of the first row of the census, on the exact datum.
- **330, the dependency-graph edge** — "| RECORD-BLOCK | ACTOR | **the
  reconstruction derives the cast** |".

Two occurrences is a class, not a slip, and neither is caught: the wall's
pattern is `\bthe actors are derived from the record\b`, which "derived back
from the record" does not match. The repair prose at 156-160 is fifty-five lines
away and does not travel with the table; a reader who reads the census reads
that the actors are derived from the record.

**Licensed replacements, verbatim:**

- 101, reading column: `declared as the points of the arena, and reconstructed from the record inside the triangular class of section five`
- 330, edge column: `the reconstruction reads a cast off the blocks`

**Related, lower:** "the derived cast" (117), "the derived structure" (75), "the
derived link structure" (118, 355) apply the same verb to record-obtained
objects in this paper's own voice. It is the parent's house word (licensed
inside the quotation at 249) but it is the wrong word in the paper that exists
to separate reconstruction from derivation. Recommend "the reconstructed cast /
structure" throughout. Note also that **the paper's own line 73 — "The record
fixes the cast and the link structure as sets" — is within one word of a
paraphrase this seat planted as a W1 violation.** Reading it in context it is
defensible; that the wall cannot tell it from the plant is the point of MAJOR-2.

---

## MAJOR-5 — "every declaration the corpus makes" is contradicted by the paper's own census, and the 13/0 count is a reading of a typed table

**(a) The totality is false.** §6 (422-424) says the table lists "every
declaration the corpus makes". §1 classes nine objects DECLARED. **TICK** ("one
scheduling convention") and **CHART** ("the combinatorial types of a two-sector
overlap") are DECLARED in §1 and have no row in §6. So DECLARATIONS=21 is not
the corpus's declarations; it is the twenty-one this unit priced. There is no
completeness gate and there cannot be one over a hand-written tuple.

**(b) The class and selection columns are typed, not measured.**
`PARAMETER_ROWS` is a literal tuple; `param_free` and
`param_free_selected_by_a_law` are sums over its typed 4th and 5th fields.
`g_parameters` checks that the classes partition the list and that no row is
both FREE and YES — but FREE *means* "no measured law selects it", so
`FREE-ROWS-A-LAW-SELECTS=0` is a consistency check on the typing, published as a
result. Twenty of the twenty-one rows' statuses rest on an unverified citation
label in the last column; exactly one anchor (NDEP-CARRIED) is consumed by the
gate. This is #295's disease — class words not gate-bound to computed
predicates.

**(c) #299-extended feasibility fails for two of the four emitted head words.**
`BACKING_ALPHABET = ("COMPUTED-HERE", "SEALED-CITATION")` and `census_rows`
never emits "NONE", so `CENSUS-PARTIAL` **cannot fire at any corpus this builder
can produce**: `CENSUS-TOTAL` is unfalsifiable and `census_backed ==
census_row_count` is a tautology. `ISP-IS-ONE-THEORY` likewise requires every
row typed non-FREE. The §8 control table gives all eleven words the identical
string "a declared input handed to the comparator" — which shows the comparator
function is total, not that the corpus could have produced the other word. The
engraving asks for feasibility argued *against the committed corpus*, per row.
(The circularity word and the Q58 word ARE genuinely measurement-determined —
`Q58-EMERGENCE-GENERATOR-INDEPENDENT` was live and would have fired had all nine
arms recovered. Two of four is a real result, and the honest one to publish.)

**(d) A cross-table class conflict.** §1 classes CARRIER-CANDIDATE **DECLARED**;
§6 classes the carrier size **DERIVED**. Both are the 27 cells, both use the
pin's five-word alphabet, and no sentence reconciles object with cardinality.

**Licensed replacement for 422-424, verbatim:**

> Here is the clean saying: every declaration this census reaches — twenty-one
> of them, the scheduling convention and the chart type being classed declared
> in section one and not priced here — its fiber where a fiber has been
> measured, its class, and whether any measured law selects it.

**Licensed addition after 467, verbatim:**

> The class column is this unit's own reading of the sealed units named in the
> last column, and the count thirteen is a count of that reading. That no free
> row is law-selected is the reading's consistency check and not a second
> measurement.

---

## MINORS

1. **The C1-only width sweep is not disclosed where it bites.** The window table
   (271-279) is measured on `c1hist[0]` — one history of the 5,784. The gate
   additionally verifies the threshold and global count are uniform across all
   **72** C1 histories, which the paper never says; and the sweep never reaches
   the C2/C3 corpora. `LOCAL-COLLAPSE-WIDTH=4` rides in the head under a blanket
   scope of "the committed corpus". Add: *"The table is one C1 history; the
   collapse width and the global count are the same at all seventy-two, and the
   concatenation and window corpora are outside the sweep."*
2. **`--render` is not disclosed.** It skips the four paper gates including
   G-WALLS. It writes nothing and cannot deliver, so the residual is contained —
   but §8's "or the run fails" is unqualified and the mode is shipped. One
   clause in §7.
3. **"Exactly the automorphism group" is backed by order equality only.**
   `inv_law_blind_to_direction = (len(stab) == len(autos))` compares
   cardinalities of two permutation sets on different index sets, while 243-245
   asserts group identity. **This seat verified the claim is TRUE** — set
   equality holds, both inclusions, `|stab \ Aut| = |Aut \ stab| = 0`, and
   `Aut(arena) ⊂ Aut(link)` with index exactly 12. It is a true claim carried by
   a coincidence of orders; one line (`set(map(tuple, stab)) ==
   set(map(tuple, autos_site))`) makes it earned. No physical-symmetry reading
   is available anywhere for the 1,296 — the paper stops at "an invariance
   statement, and the paper stops there" (84) and §7 denies the quotient. Order
   (c) holds here.
4. **The instrument's own docstring carries a stale count.** Line 32: "it
   refuses four of **eight** declared generating mechanisms". There are nine, as
   the head, the arms table and §8 all say. A false numeral inside a sealed
   artifact.
5. **§8 attributes the refusals to the wrong region.** "the comparator is shown
   to have teeth by refusing four of the nine mechanisms" (524-526) — but
   `r_reconstruct` is the **reconstructor** (`r_*`), and §5 (402) says so
   correctly. The comparator's genuine tooth is the second one named, rebuilding
   the verdict from the serialized receipt. The S-1 answer is a little weaker
   than §8 states. Replace with: *"and the reconstructor is shown to have teeth
   by refusing four of the nine mechanisms, the comparator by rebuilding the
   verdict from the serialized receipt alone."*
6. **OBJECTS=23 counts names, not objects.** `g_census` gates
   `len({r["object"]}) == len(rows)` — distinct *names*. The reading column
   declares ACTOR ≡ SITE ("the same objects as the actors under the weld's
   dictionary") and CELL ≡ CO-DIVISION-PAIR ("the cell is the unordered pair").
   Two identifications, disclosed and not deducted. This is the real form of the
   duplication ledger #5 asked about. Stamp the head field, or add: *"Twenty-three
   named rows; the weld's dictionary identifies ACTOR with SITE and the carrier
   typing identifies CELL with CO-DIVISION-PAIR, so twenty-one objects carry
   twenty-three names."*
7. **"Refuses at four" conflates two different failures.** Two arms
   (TOKEN-NOT-IN-EXACTLY-TWO) had the rule *run* and its answer rejected by its
   own certificate; two (THRESHOLD-UNDETERMINED — both grains of the coupled
   walk, a=2) had the rule *decline*, because 27 singleton blocks carry no
   co-writing at all. **Ruling on the licensed word:** "refuse" is licensed for
   the a=2 rows, because it is the reconstructor's own declared branch
   (`r_reconstruct`'s docstring: "the rule REFUSES rather than choosing one") and
   the code returns the empty cast — but it is NOT licensed to read as "the cast
   is not identifiable from the coupled walk's record", since no reconstructor
   could act on a record with no co-writing. §5 (402-406) distinguishes all three
   legs correctly and 411-412's "The corpus's one autonomous dynamics is outside
   it" is the right word. **Only the summary at 88 under-scopes.** Replace with:
   *"Run through nine, the cast comes back at five and does not at four: at two
   the rule's own membership certificate rejects the cast it built, and at two —
   both grains of the coupled walk's own emission law — the record carries no
   co-writing to threshold, so the rule declines and returns none."* The verdict
   word is robust to this re-ruling (`refusing` would still be > 0 under any
   reading), and REFUSING=4 in the head may stand as the count of arms that did
   not return the declared cast.
8. **A typed numeral reaches a rendered table.** `("TICK", "DECLARED", S, 1, …)`
   — the only census cardinality not drawn from a measured key, published as
   "| TICK | DECLARED | 1 |". `census_rows` is not in `TYPED_CALLERS`, so E-31's
   scan does not see it.

---

## NOTES

- **PRIMITIVE is empty and the paper never says so.** The pin's alphabet has
  five words; the census uses four. That ISP's contract declares *nothing*
  primitive is a substantive result of this census and is left unstated. One
  sentence would earn it.
- The referent registry's `base = {1, 2, 3, 9, 27}` is admitted into all six
  universes. Permissive, but they are the arena's own constants; noted, not
  charged.
- `COVERAGE_WAIVER` is a single waiver whose forcing is machine-checked
  (`_coverage_forcing`) — compliant with the #34 addendum, verified by reading.
- MUT-CLAIM and MUT-REFERENT are genuine independent paper corruptions. The
  circularity in MAJOR-2 is specific to MUT-WALL, not general to the suite.

---

## RECOMPUTATION COUNT: 126

Object hashes 10 (5 open + 5 close) · off-tree baseline reproduction 38 (34 gate
rows + 4 verdict segments, from a mirror carrying only the pinned sources) ·
wall-leg paraphrase plants 16 + 1 vacuity control · full-battery single-plant
delivery runs 4 · full-battery multi-plant run 1 · triangle-claim probes 12 ·
stabilizer set-equality and subgroup-chain checks 9 · residue screening across
amplitudes 14 · census hand-recount 6 (23 rows; 9/10/2/2; 20/3) · parameters
hand-recount 7 (21 rows; 13/2/1/3/2; 0) · vocabulary sweeps 5 (gauge, W1,
excitation, seam, render/C1) · quotation-duplication check 3.

**Zero false numerals found in the head, the tables, or the receipt.** Every
falsity found in this review is a quantifier, a scope, or a description.

---

## WHAT THIS SEAT DID NOT AUDIT

The nine template families on a second exemplar and the full mutant sweep (K3);
the independent re-derivation of the arena, corpus, quotient, coin-fiber and
seam-rank numbers (K1). Where this seat recomputed such numbers it did so only
to establish a licensure finding, and every one it touched agreed with the
receipt.

**CANDIDATE UNTIL ADJUDICATION.**
