# SCOUT-BRIDGE PIN ADDENDUM (FROZEN) — THE #55 SHARPENING

**Frozen:** 2026-08-16, v15 ledger #55.  Amends (never relaxes) the
frozen pin v15/note-scout-pin.md (c57a0afffd58).  Source: the user's
type-error diagnosis, engraved verbatim-faithfully at LOG #55; RUNBOOK
E-34.

## THE REFRAME (binding on every scout sentence)

The ECC no-go is a TYPE ERROR surfaced, not nonclassicality: Mp=3q
demands inclusion probability 3*(4/9)=4/3 at the committed cell.  The
scout's S0 sample-space spine is the cure applied forward.  The
language: paper-20's primitive is CELL-HIT (the rename is mandatory in
this note's prose; "division event" refers only to the three-actor
grammar object).  The confusion to never repeat: mutually exclusive
alternatives (which cell triggered) vs simultaneous consequences
(which three relations were written).

## THE SHARPENED ORDERS (the user's five + one)

1. The note formally adopts the CELL-HIT rename with a terminology
   box: paper-20:277 quoted, paper-19:289 quoted, the incompatibility
   stated in one paragraph.
2. THE PRIMITIVE SELECTION IS THE DELIVERABLE QUESTION: CELL-HIT
   primitive vs TRIPLE-EVENT primitive — both ontologies built at the
   committed arena, the decision criteria measured (which supports
   the local-sector/backreaction reading; which preserves the
   walk unchanged; what each costs).  The user RECOMMENDS
   triple-primitive; the scout tests both and says which survives
   what.
3. Under triple-primitive: Mp=3q is ABANDONED, not repaired.  The
   object is P(X'_e | X) over MUTUALLY EXCLUSIVE COMPLETE LOCAL
   SUCCESSORS X'_e = (G'_e, R'_e, rho'_e, ...); sum_e P = 1; the
   three pair-cells are correlated consequences.  S1's pushforward
   theorem stands as the proof of why anything less re-inherits the
   ceiling.
4. S2 SHARPENED: test whether locality + proximity + overlap/
   relabelling symmetry + record consistency UNIQUELY determine
   K(e|c,G,R) (the candidate bridge P(c,e|X)=q(c|X)K(e|c,G,R)); an
   arbitrary K is a hidden free law and is refused as an outcome
   word.  Overlapping triples are compatible with exclusivity (which
   successor happens, not which actors are shared) — say so, gated.
5. NEW: the successor specification must include rho'_e — the
   event-conditioned quantum update INCLUDING transport onto newly
   created cells (ECC paper-46:529 measured 0/4 candidates evolve
   across creation; the scout states what a successor law must
   supply there, as constraints, not as a construction).
6. NEW CHARGE S4 (the uncollapsed-record problem): paper-20 records a
   Born-selected cell while leaving the quantum state UNCOLLAPSED
   (paper-20:633).  If that is a standard quantum operation, its
   LINEARITY must be tested — build the map, test linearity exactly;
   if it fails, state what non-linear object it actually is.  The
   joint successor law must specify both the recorded event and the
   corresponding state update — the scout formalizes the requirement
   and measures whether paper-20's delivered rule satisfies it.

## THE BARANDES-COMPATIBLE FORM (S1 escape (a), named)

The universal configuration space Omega = disjoint union over G of
{(G, R, rho, event data, ...)}: the OUTER space fixed, G changing
between configurations (w5-recast:68 — his per-model space is fixed;
a fixed Omega does not require a fixed geometry).  The scout
formalizes S1-escape-(a) as exactly this construction and states what
it demands of the transition law.

## WALLS (additions)

W4-TYPE-IDENTITY (E-34) in full: the term-binding table is mandatory
in the note; the alternatives-vs-consequences audit per bridge
equation, gated.  Everything else per the base pin.
