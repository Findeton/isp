# ECC (PAPER-46) — EVENT-AND-CARRIER-CLOSURE: THE STATE CONTRACT, THE SEAM DECISION, THE BORN-MARGINAL LP — PIN (FROZEN)

**Frozen:** 2026-08-16, v15 ledger #43.  Unit paper-46-ecc; code
v15/code/ecc_exact.py (+ output + receipt).  Charter authority:
v15/PLAN.md as amended through ledger #40 (reviews four through seven
folded).  A COMPATIBILITY/NO-GO paper, not a forced construction.

## PARENTS (all four wave-1 terminals, sealed digests)

- paper-43-contract.md 0c8d1a687b14 (TERMINAL #35): the 23-declaration
  census with types; state = 2 components + branch-weight bookkeeping,
  READING-RELATIVE; the gauge word withheld; Q58
  identifiability-within-a-generative-class.
- paper-47-disc.md 5da53943c6f7 (TERMINAL #37): backreaction non-inert
  under specified declarations; the coin-order tick fiber; the
  demotion census 6-of-9; the record-free null ≡ paper-20's control.
- paper-44-arity.md 0d677a4cbe97 (TERMINAL #39): no parent numeral
  follows the a-only rule; the modulus theorem nL/gcd(nL,n)=L; a=q
  assumption-selected via the subgroup lattice 1|3|9; the a=2
  obstruction-vanishing theorem.
- paper-45-autoglue.md fa0268d99524 (TERMINAL #42): a TRANSITION
  RELATION on the seam-subsystem state, not an evolution law; "a state
  restricts the allowed transitions only if it persists, which is a
  reading and not a measurement"; motivated∩lawful=∅; no seam
  principle derived; event-conditioned creation 108/288.
Standing corpus wherever read: by PATH + sha256-12 anchors with
consumer gates (paper-20 coin; SEC/SEC-2 seam algebra; SPC menu; POT
gap; EPR shadow theorems).

## REGISTRY QUESTIONS (commit at pin)

Primary: Q2 (completion of the sealed partial: the COMPLETE state),
Q75 (the state-space commitment — Hilbert / algebraic / probabilistic
/ other, decided AT THE INTERFACE TABLE), Q73 (what defines the
probability measure), Q79 (Born derived or declared — THE LP), Q5
(the actors↔records fixed point at the full-dynamics scope CONTRACT
left open).  Secondary legs: Q1/Q4 (circularity), Q87 (the ψ-status
trilemma face), Q121 (amplitude attachment, seed), Q34-typing (the
three maps feed DC).  Every status change cites #<seal-entry> and
lands in the seal commit.

## THE STEPS (order is normative)

(0) THE INTERFACE TABLE: every object typed
    (declared/generated/reconstructed/law-selected; one-state vs
    ensemble; update rule; free-parameter dependence); the words
    event/cell/geometry/record/state/metric fixed to ONE sense each;
    CONTRACT's 15 free declarations carried WITH THEIR TYPES
    (heterogeneous categories, never a count of constants); the EVENT
    CLASS row an explicit DECLARED FORK — affine-line coset triples
    (committed) vs pair-events (the a=2 extension branch), NO silent
    compromise event class; the coset principle labelled "declared
    (the selection law)" pending COSET-FROM-COMPATIBILITY.
(0b) THE SEAM DECISION, ahead of any Born weight: is seam data
    PERSISTENT STATE between events or RE-SOLVED at each event?
    Launching measurement = AUTOGLUE's sealed row ("a state restricts
    the allowed transitions only if it persists, which is a reading
    and not a measurement").  The unit must either EXHIBIT a
    discriminating measurement inside the committed corpus or return
    the honest UNDERDETERMINED verdict with the chosen reading
    DECLARED and every downstream result stamped with it.
(0c) THE DYNAMICS DECOMPOSED AS THREE SEPARATE CONDITIONAL MAPS —
    event selection, seam completion, quantum evolution — typed
    separately in the table; composed only in DC; never blended.
(1) THE ψ-STATUS DECISION before any Born weight is used: ontic
    stochastic wave / CPTP-instrumented quantum state /
    Barandes-representational with configuration-level reformulation —
    resolved by measurement, or every subsequent result PROVEN
    independent of the choice.
(2) THE BORN-MARGINAL FEASIBILITY LP, normalization pre-registered in
    its general form: an a-actor event writes C(a,2) pair-cells, so
    inclusion marginals sum to C(a,2) — 3 at a=3; at a=2 the problem
    DEGENERATES (measured, not assumed; the branch reported, never
    merged).  Inclusion probabilities vs a 1/3 factor vs two-stage
    cell selection chosen EXPLICITLY.  ALL outcomes substantive; NO
    silent uniform/max-entropy selection among many.
(3) THE VARIABLE CARRIER beyond a symbol: the basis of H_G; how
    directionless cells acquire quantum degrees of freedom; amplitude
    entry; coin change; norm; channel-vs-isometry.  CANDIDATE (to be
    MEASURED against alternatives, not assumed): a classical–quantum
    instrument {p_G, rho_G} with outcome maps G→G′.
(4) The admissible-class consistency debt: the coupled walk continues
    where AUTOGLUE refuses — decided, not narrated.

## OUTCOMES (#299: feasibility argued against the committed rows named above)

- ECC-STATE-CONTRACT-CLOSED-AT-<scope> / ECC-CIRCULARITY-<verdict>
- SEAM-PERSISTENT-SUPPORTED / SEAM-RE-SOLVED-SUPPORTED /
  SEAM-DECISION-UNDERDETERMINED-AT-<row> (reachable: AUTOGLUE's row
  is already a measured refusal to decide — the census either finds a
  discriminator or certifies none exists at the committed windows)
- PSI-ONTIC / PSI-INSTRUMENT / PSI-REPRESENTATIONAL — or
  PSI-STATUS-INDEPENDENT-<results list> (independence proven row by row)
- ECC-LP-INFEASIBLE-AT-<scope> (the parents cannot unify as written —
  a major result, not a failure) / ECC-LP-UNIQUE (a derivation) /
  ECC-LP-MANY-<dimension> (a new free declaration; the family
  published, nothing selected) / ECC-LP-DEGENERATE-AT-a=2 (the branch)
- ECC-BLOCKED-AT-<object> — instrument fault only.
Control arms demonstrate every outcome word through the real
predicates (synthetic forced-feasible / forced-infeasible LPs; the
ACT Z1 pattern).

## WALLS

W1 reconstruction≠derivation; W2 invariance≠gauge before operational
observables; W3 INSTRUMENTED — every headline auto-labelled
member-specific or family-level with varied fibres disclosed;
EQUIVARIANT-OR-DECLARED for any selection/tie-break rule (a
non-equivariant tie-break = a NEW DECLARATION added to the family
count in-paper); the event fork DECLARED (no compromise class); the
heterogeneous-type carry; Born LANGUAGE only downstream of the LP row
it cites; no continuum claims; no SI numbers; exact arithmetic only
(no floats; no builtin hash — the MUT-HASH AST leg); E-25…E-33
template engravings; #295 class binding; #119 promotion totality;
sentence referent binding + role-aware referent legs; the
test-declaration duty; scope stamps everywhere (one arena, committed
windows, extension-family labels where ARITY's dial moves); the
TPL-2 disease list gated: post-final-gate seal window, reflexive
N-of-N pairs, bare-negation/hedge licence tokens (NEG guard with
re-assertion and other-clause exclusions), period-regex numeral blind
spot, spelled numerals AND spelled fractions/proportions
(sentence-scoped, hedge-aware), object-under-test digest in BOTH
artifacts, seed-dependent-receipt-fixed-transcript (MUT-HASH
species).  Candidate readings until adjudication.

## STANDARDS

Era template (v15 era_template.py / TEMPLATE.md); gates named,
consumers named, every anchor consumed; windows declared; seals at
value-close with promotion-time manifest re-derivation; read-back
before os.replace; derived head with de-twinned comparator reading NO
summary scalar (all head positions derived from primitive receipt
tables); failing runs write nothing (whole-tree hash); byte x2 from
deleted artifacts at alien CWDs; PYTHONHASHSEED-invariant; hostile
argv rc 2; --selftest write-nothing with per-recipe
moved-the-measurement proof; registry-named --mutant dead at declared
gate; #91 off-tree; scratch <5G absolute paths; one process per
probe; kill only your own PIDs.  Deliverables: paper-46-ecc.md,
code/ecc_exact.py, code/ecc_output.txt, code/ecc_receipt.json.
