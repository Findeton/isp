# JS PIN ADDENDUM (FROZEN) — THE #102 CORRECTIONS (TWENTIETH REVIEW)

**Frozen:** 2026-08-17, v15 ledger #102.  Amends (never relaxes) the
pin 93ef35b48811.  Routed to the worker by confirmed send; the worker
folds before any verdict-bearing computation.

1. THE CIRCULARITY REPAIRED — STATE AND LAW ARE A FIXED POINT.
   Predictive equivalence is defined by future distributions, which
   come from the law being sought.  S1 is RESCOPED: (S1a) compute the
   predictive quotient OF THE DELIVERED DYNAMICS ONLY, explicitly
   labelled a REGRESSION BENCHMARK — it is not automatically the new
   law's S; (S1b) the actual construction is the FIXED-POINT LOOP:
   start from full histories or an explicit finite-memory candidate
   -> construct a candidate joint law -> compute THAT LAW's
   predictive quotient -> minimize and verify transition closure ->
   iterate until law and state agree.  Likewise: candidate cuts may
   be declared, but GENUINE-division-boundary status is a property
   OF THE LAW (does it condition and compose there) — never assumed.
2. THE NATURALITY CLAIM CORRECTED (the pin's C3 overclaimed): a
   symmetric law can evolve an asymmetric initial state; the
   anchored walk does NOT establish an unnatural law.  The frozen
   test: does the anchor transform COVARIANTLY as part of S or the
   boundary data, or does the law reference an ABSOLUTE chart label
   that cannot be transformed?  SCOUT-K showed K(e|c,G,R) lacks
   state — not that the underlying law is unnatural.  The tension is
   a QUESTION to test, not a result to exhibit.
3. TYPE BEFORE ENUMERATION — S2/S3 REORDERED: first the
   carrier/type boundary (relation and geometry alphabets; the state
   representation; local radius/support; the allowed successor
   grammar; operational input/output spaces — all declared exactly);
   THEN the pair-law enumeration, finite and well-defined under
   those declarations (or S2 becomes a purely combinatorial
   successor-grammar census, stated as such).
4. COMPRESSION VERDICTS TIGHTENED: the no-compression outcome is
   RENAMED JS-NO-STABILIZATION-WITHIN-<range> (finite windows cannot
   prove nonexistence).  The stabilization test runs on TWO
   INDEPENDENT AXES — the amount of past supplied AND the future
   prediction horizon — holding the history set fixed and REFINING
   ITS PARTITION as the horizon grows (class counts alone hide
   splits, mergers, and newly appearing histories; the partition
   refinement is the object).
5. CP TYPING AND METHOD HONESTY: one convention, declared —
   trace-nonincreasing CP maps J_alpha: T(H_{R,G}) ->
   T(H_{R',G'}) with the direct sum trace-preserving.  The
   certificate methods declared per constraint class: Choi
   positivity is SEMIDEFINITE (rational witnesses where available,
   the method and its limits disclosed where not); factorization
   constraints can be polynomial and non-convex — the LP/Farkas
   machinery is NOT automatically sufficient and no verdict may
   silently assume it is; UNDECIDABLE-WITHIN-METHOD is an honest
   outcome word.
6. S5'S VERDICT SPACE EXPANDED: the admissible family may be EMPTY /
   ENTIRELY-FACTORIZING / ENTIRELY-IRREDUCIBLE / MIXED /
   STATE-DEPENDENT / UNDECIDABLE-WITHIN-THE-ANSATZ — all six are
   outcome words.  "Commutes without a boundary" means a COMPATIBLE
   JOINT REPRESENTATION exists — it does not automatically mean two
   physically real subevents.
