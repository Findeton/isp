# JS PIN ADDENDUM 2 (FROZEN) — THE #103 CORRECTIONS (TWENTY-FIRST REVIEW)

**Frozen:** 2026-08-17, v15 ledger #103.  Amends the pin
(93ef35b48811) and addendum 1 (6cb7438d461c).  Routed by confirmed
send; folded before any verdict-bearing computation.

1. THE UNBIASED ARITY COMPARISON (replacing the pair-seeded S5): a
   pair-seeded enumeration cannot fairly decide pair-native vs
   triple-native — a failed factorization would yield only
   "no pair-generated law," never a positive irreducible triple law.
   CONSTRUCT INDEPENDENTLY: L2, L3, L4 = the most general admissible
   complete-successor law families with support on 2, 3, 4 actors
   (each under the declared types, each finite, each parameterized
   exactly).  THEN test whether members of L3: (a) belong to the
   closure/composition of L2; (b) admit a compatible simultaneous
   pair representation; (c) remain GENUINELY OUTSIDE every
   pair-generated family — making triple irreducibility a POSITIVE
   mathematical result, with certificates, not the residue of pair
   failure.  The S5 six-way verdict space applies per family
   membership question.
2. PHASE-ONE MINIMAL GEOMETRY (structural backreaction cannot wait):
   (a) a FINITE structural geometry grammar declared; (b) validity
   constraints on every G' (gated); (c) a MINIMAL order/composition
   consistency gate (the full G_order = G_composition weld stays
   phase two); (d) input-relative causal support enforced.  IF the
   minimal geometry layer is not built, every phase-one arity
   verdict is LABELLED KINEMATIC-AND-PROVISIONAL — gated language,
   no exceptions.
3. THE FIXED-POINT LOOP AS MATHEMATICS (S1b made exact, before it
   runs): declare the finite candidate-law family searched; the
   exact quotient/refinement operator; whether ALL fixed points are
   enumerated or one is selected (and by what declared criterion);
   CYCLE and NON-CONVERGENCE as outcome words; state-relabelling
   identification (two fixed points related by a relabelling of S
   are one); a TERMINATION argument within the declared finite
   ansatz.  Predictive minimization COMPRESSES a given law; it does
   not select which law is true — gated sentence.
4. S AND THE QUANTUM STATE KEPT DISTINCT (E-34 applied to the
   framework's own central symbol): an explicit declared bridge —
   either S -> rho_S (the map stated) or S = (S_causal, rho) — with
   REDUNDANCY TESTS for each component; no construction may hide
   the psi/rho/record type question inside the single symbol S.
5. TWO PRECISION FIXES: (a) arena naturality's "same motif, same
   law" means the same ISOMORPHISM CLASS OF THE COMPLETE LOCAL
   BOUNDARY DATA (R,G,S) — never the bare actor graph (different
   memory or local state may lawfully differ); (b) S4's separation
   is DEFINED via the input geometry and causal past — actor
   disjointness is NOT causal separation — and the diamond compares
   complete instruments up to a SPECIFIED boundary isomorphism and
   transport.
