# DISC — the discriminator: a model-ablation benchmark, and what the null already reproduces

**Status:** `DELIVERED` — built against the frozen pin `v15/note-disc-pin.md`
(sha256-12 `dbe7b26bb0d0`, v15 ledger #1), revised under the binding user review
at v15 ledger #2, which renamed the head, reclassed the unit and promoted the
reproduction census to the primary result, and repaired under the three-seat
panel and the adjudication at v15 ledger #19. Verified to run: the plain run
byte-identical twice from artifact-deleted trees, every gate passed, every
declared recipe dead at its declared gate with the measured key it names
actually moved, the falsification self-test fatal at every leg and writing
nothing. Between delivery and adjudication every headline below is a
**candidate reading**.

## The Record-Free Null Reproduces 6 of 9 Parent Results Exactly, and What the Record Layer Does Buy Is Not Inert but Is Declaration-Relative

**Unit:** DISC, v15, paper-47. **Questions:** Q147, Q148, Q149, Q150, Q155.
**Class:** this unit is a model-ablation benchmark.
**Instrument:** `v15/code/disc_exact.py`.
**Artifacts:** `v15/code/disc_output.txt`, `v15/code/disc_receipt.json`.
**Inheritance, hash-verified at run time and by no other route:** the coupled
walk, its coin, its connection, its declared fibers and its frozen-stage control
come from the coupling unit, `v14/paper-20-coupling.md` (`4824d190af73`); the
pinned observable and its gauge behaviour come from ACT, `v14/paper-34-act.md`
(`d933221780ed`); the loop family, the perimeter law, the closed form and the
spectral row come from POT, `v14/paper-36-pot.md` (`1e495318252d`); the
untested connection-modulus test comes from NDEP, `v14/paper-39-ndep.md`
(`e2293b8c3858`); the nine template families are imported from
`v14/code/era_template.py` (`d04a3eb58fbc`) and used rather than copied.
Every object below is **reimplemented** from those definitions; no
measurement is taken from any other unit's program.

**What kind of result this is.** The null built here is this theory with one
layer removed: same arena, same internal register, same coin, same shift,
same initial state, same horizon, and no record. That is why the class word above is the one it is: every comparison below
measures what the record layer does, and no comparison here reaches beyond
the two programs it compares. Three consequences are load-bearing and are
stated at the top rather than in a caveat. First, the reproductions are the
primary result: they say that most of the corpus's own sealed results do not
distinguish this theory from a plain coined walk. Second, the record-free null
is the parent's own frozen-stage control up to a global phase, so the bare fact
that the layer is not inert is the parent's already-sealed result and not this
unit's; what is this unit's is the tick, the exact magnitudes and the
universality. Third, the tick is order-relative: it moves by one on a fiber the
parent declared and stamped verdict-relevant, which is why the head word carries
no tick and carries the declarations instead.

**Exactness.** Integers and `fractions.Fraction` only. Walk amplitudes are
carried as integer four-tuples over the basis of the twelfth cyclotomic ring
reduced modulo its own minimal polynomial, so every Born weight is an exact
rational and equality of states is equality of tuples; the lattice leg's link
operators are carried in doubled eighth-root coordinates exactly as their
parent carries them. An abstract-syntax-tree scan of this instrument's own
source and a recursive type scan of the receipt are both gates: no
floating-point literal occurs in the source and no floating-point value
occurs in the receipt.

**S-1 structurally.** The registered-unimplemented family the era template
names first — *the comparator is the builder* — is met here structurally
rather than promised, and the repair made it stricter than the delivered
version was. Four code regions are disjoint by machine check: the null's, the
ISP model's, the head builder's and the comparator's. **Every** cross-region
call edge is an offence, in both directions; callees are resolved through the
module's own binding table, so an alias is not a way past; a function in no
region that is called from more than one region is a shared component and is an
offence unless it is one of the declared shared-ground names, each of which is
required to be genuinely shared. No function of the null's region names any word
of the declared record vocabulary in any identifier or argument, so the null's
lack of a record is a property of the source and not a sentence in this paper.

**The seal.** Every published object is digested at the moment the gate that
vouches its own values passes; totality is recomputed from the payload's live
key set at the door rather than from a snapshot; the artifacts are staged,
read back, compared against the gate-time digests, promoted and re-verified
from the promoted path, with no staging residue left on any exit.

**The verdict, quoted exactly as the instrument emits it.** The delivered head
is rendered by the head builder and rebuilt by the comparator over the receipt
payload alone, and the two routes' segments are compared as a multiset; an
abstract-syntax-tree read of the source requires the two sides of that
comparison to be two different functions in two different regions, neither
calling the other. The paper's fenced blocks are compared as a multiset against
the single block this run licenses:

```
RECORD-BACKREACTION-NON-INERT-UNDER-SPECIFIED-DECLARATIONS -- CLASS=MODEL-ABLATION-BENCHMARK;NULL=PLAIN-COINED-WALK;RULES=7;SHARED-WITH-ISP=ARENA-REGISTER-COIN-SHIFT-STATE-HORIZON;ABLATED=RECORD-LAYER-CONSERVED-PRICE-STRUCTURE -- PRIMARY=THE-RECORD-FREE-NULL-REPRODUCES-6-OF-9-PARENT-RESULTS;7-OF-10-TESTED-ROWS;2-NOT-REPRODUCED;1-NOT-EXPRESSIBLE;REPRODUCED=PERIMETER-LAW+CLOSED-FORM+GAP+PLAQUETTE+QUARTIC-SIGN+COIN-REGISTER-RESTRICTION+THE-FIRST-TWO-TICKS -- LATTICE=PERIMETER-REPRODUCED-BY-THE-NULL;CLOSED-FORM-REPRODUCED-BY-THE-NULL;GAP-REPRODUCED-BY-THE-NULL;PLAQUETTE-REPRODUCED-BY-THE-NULL;QUARTIC-REPRODUCED-BY-THE-NULL -- BASE-FINDING-IS-THE-PARENTS=THE-NULL-IS-PAPER-20S-OWN-FROZEN-STAGE-CONTROL-UP-TO-A-GLOBAL-PHASE;EQUAL-AT-1116-OF-1116-SITE-DISTRIBUTION-CHECKS;THIS-UNITS-NEW-CONTENT=THE-TICK+THE-EXACT-MAGNITUDES+THE-UNIVERSALITY -- AGREEMENT=6216-OF-6216-SITE-BY-TICK-CHECKS-EQUAL-THROUGH-TICK-2 -- ABLATION-EFFECT=FIRST-AT-TICK-3-AT-294-OF-294-NON-TRIVIAL-FIBER-POINTS;NEVER-AT-78-TRIVIAL-COIN-POINTS -- COIN-ORDER-FIBER=DELIVERED-G-DOT-D-TICK-3;ALTERNATIVE-D-DOT-G-TICK-4-WITH-THE-TICK-3-LAW-EQUAL-TO-THE-NULLS-AT-BOTH-PLANES;THE-TICK-IS-ORDER-RELATIVE;ORIENTATION-FIBER-INERT -- WIDER-NULL-SWEEP=10380-MEMORYLESS-CONFIGURATIONS;0-EXACT-REPRODUCTIONS;CLOSEST-AT-AG-2-2=8200/59049-BY-A-NON-COVARIANT-COIN-AGAINST-THE-PR3-NULLS-10144/59049;PR3-IS-THE-BEST-AT-AG-2-3 -- IPR-AT-AG-2-3=ISP-33596579/129140163-VS-NULL-40411/177147;TV=1024/19683 -- IPR-AT-AG-2-2=ISP-43392899/129140163-VS-NULL-58235/177147;TV=10144/59049 -- M-EQUALS-Q=FORCED-AND-OBSERVABLE;INTERNAL-CHECK-CLOSING-THE-PARENTS-REGISTERED-TEST-NOT-AN-EXTERNAL-PREDICTION;CONSTRUCTION-DEPENDENT;OBSERVABLE-AT-5-OF-5-DECLARED-MODULI -- PRICE=ISP-CARRIES-5-STRUCTURES-THE-NULL-DOES-NOT-AND-1-FREE-NUMBER;THE-FREE-NUMBER-IS-THE-PHASE-MODULUS-AND-IT-IS-FIXED-ONLY-BY-THE-CONSTRUCTION-DEPENDENT-DESCENT-FORCING;PARAMETER-FREE-IS-NOT-STRUCTURE-FREE -- FALSIFIER=COMPUTATIONAL-REGRESSION-TEST-ON-A-PROPOSED-REALIZATION;SHOTS-IF-EVER-REALIZED=36948-AT-AG-2-3-AND-5186-AT-AG-2-2-AGAINST-THE-CLOSEST-MEMORYLESS-WALK -- FEASIBILITY=BOTH-WAYS-BY-A-DECLARED-RECIPE;PIN-OUTCOME-WORDS=2-OF-3-REACHABLE;DISC-BLOCKED-RETIRED-AS-UNREACHABLE-BY-CONSTRUCTION -- SUCCESSOR=DISC-2-THE-SIMPLEST-MEMORY-BEARING-NULL -- AXES=ARENA+COIN-CLASS+COIN-DIRECTION+COIN-ORDER+EMISSION-READING+MODULUS+SHIFT-ORIENTATION+START-SITE -- SCOPE=ONE-PRE-REGISTERED-NULL-PR1-TO-PR7-WHICH-IS-THE-PARENTS-FROZEN-STAGE-CONTROL;PLUS-A-SWEPT-MEMORYLESS-CLASS-OF-10380-CONFIGURATIONS;NO-MEMORY-BEARING-NULL-TESTED-AT-ALL;D=2;ARENAS=AG-2-2-AND-AG-2-3;READINGS=A-AND-B;AT-FINITE-HORIZON-A-RECORD-IS-ABSORBABLE-INTO-AN-ENLARGED-STATE;NO-OPERATIONAL-UNITS;NO-LABORATORY-CLAIM;NOT-AN-EXTERNAL-PREDICTION
```

(The string is one line; the gate compares that complete string.)

---

## 1. The question, and what the answer turned out to be

The pin asks the corpus's most uncomfortable question. Its own words:

> a plain d=2 discrete-time quantum walk / lattice model with the same arena
> size, no record layer, no conserved-price structure

is to be built honestly, given its own full-era instrument, and then run
against the candidate discriminants already in hand. If it reproduces
everything, the honest outcome is the null's, and every reproduction is a
demotion candidate.

It reproduces most of them, and the record-free arm compared here carries no
memory at all, which bounds what that reproduction means. At the grain of parent results the census
puts 9 rows to the record-free arm and 6 of 9 parent results come back exactly.
Among them are the results the corpus has quoted most often about itself: the
perimeter-only law, its three-term closed form, the gap that closed form
carries, the plaquette expectation, the pinned sign observable, and the theorem
that restricts the coin register. None of those distinguishes this theory from a
plain coined walk. That is the primary result of this unit and it is reported
first because it is the one that changes what the corpus may say about its own
work.

What the record layer does buy is measured too, and it is smaller and
sharper than the reproductions are large — and it is smaller than the delivered
version of this paper said it was. With the layer removed and everything else
held fixed, the two models agree exactly through the second tick and differ from the third on, at every interfering point of a swept fiber. That is an ablation effect: it says the feedback layer is not
inert. Two things bound it, and both are new here. The record-free arm is
provably the parent's own mandatory control on the compared observable, so the
bare non-inertness is the parent's already-sealed row; and the tick at which the
parting starts moves by one when the parent's own declared coin order is
exchanged for its alternative. What is left, and what the head word says, is
that the layer is not inert under the declarations this unit states. It leaves
entirely open what a different model might do with the same numbers. What this
unit put against the record-carrying arm is a pre-registered null obeying the
seven pre-registration rules set out below, which turns out to be the parent's own frozen-stage
control, together with the swept memoryless class the wider sweep covers; no memory-bearing null is tested here, and the closing scope section says exactly why that matters.

## 2. The primary result: what the record-free null already reproduces

Every result this unit put to the record-free arm is listed with its provenance,
its ruling and the measurement that earned it. A reproduction is not a defeat of
the parent that proved it; it is a statement that the parent's result does not
distinguish this theory from a plain walk, and each one is a demotion candidate
for the adjudicator to price.

| parent result | provenance | ruling | evidence |
|---|---|---|---|
| paper-36 the rectangle ladder is a function of the perimeter alone | sealed parent result | REPRODUCED | per-configuration at 3200 equal-perimeter comparisons, 0 disagreements; the record-free lattice arm's own coin is in the family |
| paper-36 the three-term closed form at every coin | sealed parent result | REPRODUCED | 0 closed-form failures over 640 coins |
| paper-36 the halving mode and its gap | sealed parent result | REPRODUCED | present at 512 of 640 coins, the record-free lattice arm's own among them with coefficients 2 -1 1 |
| paper-36 the plaquette counting expectation | sealed parent result | REPRODUCED | 11 distinct values, expectation 13/10, 56 non-flat diagonal coins |
| paper-34 the off-diagonal quartic sign pinned to a single value | sealed parent result | REPRODUCED | the record-free arm's counting measure returns 0; the odd twists reverse the observable at every coin |
| paper-20 the coin register is forced on this offset set | sealed parent result | REPRODUCED | 18 unitary and 0 non-monomial over 50653 maps: arena-carried |
| paper-20 the walk's distribution at the first two ticks | carved here from the same parent observable as the row below | REPRODUCED | 6216 site-by-tick comparisons, 0 violations, over all 372 points of the sweep |
| paper-20 the walk's distribution from the third tick on | sealed parent result | NOT-REPRODUCED | unequal at both arenas; the first difference arrives at the same tick at 294 of 294 interfering points of the sweep; at the parent's alternative coin order it arrives one tick later |
| paper-39 the connection modulus read at the smaller arena | parent's registered-and-unrun test | NOT-REPRODUCED | the record-free arm is the modulus-one point of the family; the tick-three distribution takes 5 distinct values over 5 moduli; internal, construction-dependent |
| paper-20 the record-side observable set | definitional bundle | NOT-EXPRESSIBLE | 6 of 12 observables of the classification are formable only where a record exists |

**The census is stated at two grains, and the head carries both.** At the grain
of tested rows there are 10 rows and 7 of 10 tested rows are reproduced. At the
grain of parent results there are 9, because the seventh row — the walk's
distribution at the first two ticks — is not a result any parent states: it is
carved here out of the same parent observable as the row beneath it, the walk's
own site-occupation distribution. Counting that observable once gives 6 of 9
parent results reproduced, 2 not reproduced and 1 not expressible, and the
head leads with the parent-result grain. Both fractions are counting
fractions and are declared as such: a count of rows over a count of rows, taken
on the declared list itself, with no weight system anywhere in either of them. Neither of the two rows the
record-carrying model wins is a sealed reproduction claim of a parent: one is
this unit's own finding and the other is a test a parent registered and never
ran. The provenance column says so per row, and a gate binds each ruling to the
measurement in its own evidence cell — a reproduction to a measured equality and
a non-reproduction to a measured inequality — so a ruling cannot be flipped in
either direction and leave the gate satisfied.

The last row is not a victory: a definitional absence is not a discriminant, and it is listed so that the rulings above it are read as the only ones that carry
weight. Its own fraction is a bundle: 6 observables collapsed into 1 census row,
while POT's results are counted separately.

The honest reading of the reproductions is that they follow from the shared
ground — from a uniform configuration, from a counting measure, from an
offset set — and not from the record layer at all. Sections 11 and 12 give
each one its measurement. Five of them are measured against a record-free arm on
a different carrier from the walk's, which section 12 names, and the honest
reading of the two non-reproductions is that they are one fact seen twice: the
record's back-reaction on the walk that writes it.

## 3. The null, declared before any row runs, and the pre-registration sealed

A null tuned to lose proves nothing, so the null's parameters are fixed by
seven pre-registration rules written into the instrument's own header before any measurement
is taken. Each pre-registration rule hands the null a parity with the model it
is compared against; only two of the seven take anything away, and the two they
take away are exactly the ones the pin's own sentence names.

| rule | datum | the null's value |
|---|---|---|
| PR1 | ARENA | the null runs on the same site set as the ISP model it is compared against -- the points of AG(2,q) with periodic identification, d=2 |
| PR2 | REGISTER | the null's internal register has the same dimension as the ISP model's -- one basis vector per declared link direction |
| PR3 | COIN | the null carries the SAME coin as the ISP model it is compared against, taken from the same S_3-covariant census over the arena's own ring; the discriminator may not be won by changing the coin |
| PR4 | SHIFT | the null's shift is the same conditional translation, carrying the amplitude at a site along its own direction and leaving the direction alone |
| PR5 | STATE | the null starts in the same state as the ISP model -- one basis vector, at the same site and the same direction |
| PR6 | STRUCTURE | the null has NO record layer: its coin is constant in space and in time, it emits nothing, and no count field exists in its region |
| PR7 | MEASURE | where a census over configurations needs a measure the null uses the uniform counting measure -- it has no conserved-price structure and declares no weight system |

The reading to hold on to is PR3. The null is not given a textbook coin of
its own and then beaten with a coin chosen afterwards: it is given the very
coin the ISP model uses, drawn from the very census the ISP model draws
from, at every point of the sweep. Nothing in what follows is bought by
changing the arena, the alphabet, the coin, the state or the horizon — and that
sentence is measured rather than asserted, in section 8, where the null's coin
is varied against a fixed record-carrying arm over a class far wider than PR3's
own. That parity is what makes the comparison an ablation rather than a contest
between two theories.

**The pre-registration is sealed, and the seal is the repair.** The delivered
instrument bound only the CARDINALITY of this table, so PR3 could be rewritten
after the fact — from the same coin to a coin of the null's own choosing, picked
after the sweep — and the run still passed. Three things bind it now. The rule
table digests to a value declared beside it, so any rewrite of any rule's words
moves that digest; that leg alone would not be enough, because the declared
value sits beside the table and an author who rewrites a rule can re-declare
it in the same edit, which is why the third leg below exists and why a recipe
of this unit's own battery performs exactly that edit. Every rule carries a content
leg over objects this run actually built: the sweep binds the arena, the horizon,
the coin, the start site and the internal direction to one set of locals and
hands **those** to both arms, which an abstract-syntax-tree read of the source
checks, so PR3's parity is a property of the code; the rule that takes the record
away is checked by the region scan; and the rule that declares the counting
measure is checked by asking the record-free arm for a weighted expectation and
requiring it to refuse. And every rule's text is bound to the parity that
content leg measures: each rule must still **say** the thing its leg measures —
PR3 must still assert the same coin — and no rule may admit, in any voice, that
its value was chosen once the answer was known. That third leg is the one an
author who re-declares the digest still has to defeat, and it is what turns the
rewrite above from a survivor into a death. The list of data the two models share is derived from
this table's own datum words rather than typed beside it, so the head cannot say
the coin is shared while PR3 says the null picks its own.

## 4. The shared ground, and the coin the arena allows

The arena is shared and is declared shared: the points of an affine plane
with three declared link directions, the conditional translation that moves
an amplitude along its own direction, and the ring the amplitudes live in.
Everything else is one model's or the other's. The ring the covariant coins are
enumerated over is the register's own, one third of the arena's cyclotomic
integers; at the smaller plane it reduces to the rationals, where it coincides
with the parent's own real-rational census.

The coin family is the arena's own. Over that ring the covariant
unitary coins are enumerated exhaustively and sorted into classes up to a
global phase.

| arena | solutions | classes up to phase | trivial classes |
|---|---|---|---|
| AG(2, 2) | 4 | 2 | 1 |
| AG(2, 3) | 36 | 6 | 1 |

At the larger plane this recomputes the parent's own two numbers from the
definition rather than quoting them. At the smaller plane the same census
returns a fiber that has collapsed: apart from the scalar class there is one
class and it is the Grover coin, so at that arena the coin is not a
declaration with a fiber but the only interfering choice the arena admits.

## 5. The ISP arm, its parent, and the null's identity with the parent's own control

The coupled walk is rebuilt here from its parent's definitions — the coin
multiplied by the record's own diagonal phase, the shift, a division event
emitted on a cell with that cell's post-coin Born weight, every branch of the
emission tree carried with no sampling and no pruning — and it is checked
against the parent before it is used for anything. The parent's control is
quoted in its own words:

> The frozen-stage control is the same walk, the same emission rule and the same branching, on counts that never update.

and that sentence enters the gate as a value twice over: the parities it lists
are counted out of it and required to hold between these two arms, and the
control's own record is measured to stand at its initial value everywhere while
the coupled arm's does not.

| quantity | this rebuild | the parent's sealed value |
|---|---|---|
| coupled branch ladder | 3 27 486 10527 284078 | 3 27 486 10527 284078 |
| control branch ladder | 3 27 486 9234 212382 | 3 27 486 9234 212382 |
| coupled inverse participation | 35971074413334039128803/239299329230617529590083 | 35971074413334039128803/239299329230617529590083 |
| control inverse participation | 2306155/14348907 | 2306155/14348907 |

Four sealed quantities of the parent, reproduced exactly by an independent
implementation. This is what licenses the ISP arm below; it is not a new
result and it is not counted as one.

**And the record-free arm is that control.** On a record whose counts never
update the diagonal is a scalar multiple of the identity at every site, so the
frozen coin is the plain walk's coin times a global phase, every frozen branch
carries the plain walk's state up to that phase, and the two site-occupation
ladders coincide at every tick. Measured over every tick of every declared
point of the sweep: 1116 equal, 0 unequal. So the record-free null is the parent's own frozen-stage control up to a global phase, and that cuts both ways.

In this unit's favour, decisively: the opponent was not invented here. It is the
parent's own **mandatory** control, on the very observable being compared, which
is a far stronger anti-strawman statement than the seven pre-registration rules make
on their own.

Against the delivered version's framing, and this is the correction: the bare
statement that the record layer moves the walk's distribution is then the
parent's already-sealed non-triviality row restricted to the site-occupation
row, and it is not new. What is new, and is genuinely this unit's, is **when**
the parting starts, **by how much** the two distributions differ at both planes,
and **how universally** it happens across the swept fiber. The base finding is
re-attributed to the parent's control accordingly, in the head and here.

## 6. The ablation effect

The sweep is a product of every declared axis: both arenas, every coin class
of each arena's own census, every start site, every internal direction, and
both emission readings. At each point the two models are run to the swept
horizon and their site occupations compared site by site and tick by tick.

| arena | coin | first difference | points |
|---|---|---|---|
| AG(2, 2) | non-trivial | 3 | 24 |
| AG(2, 2) | trivial | none | 24 |
| AG(2, 3) | non-trivial | 3 | 270 |
| AG(2, 3) | trivial | none | 54 |

Two readings come out of that table and neither is a count of anything else.
The first is the agreement: through the second tick the two models are equal
at every comparison taken, at every point, so nothing before the third tick
separates them. The second is the parting: at every point whose coin carries
interference the site occupations differ for the first time at the third
tick, and at every point whose coin is a scalar they never differ at all.
The discriminant requires interference: at the scalar coins the two models never separate.

At the canonical declaration — the Grover coin, the origin, the first internal
direction, the Born-menu emission — the third-tick **inverse participation
ratio**, the sum of the squared site probabilities, takes these values on the two
sides, with the total variation between the two full site distributions beside
them.

| arena | tick | ISP | the null | total variation |
|---|---|---|---|---|
| AG(2, 3) | 3 | 33596579/129140163 | 40411/177147 | 1024/19683 |
| AG(2, 2) | 3 | 43392899/129140163 | 58235/177147 | 10144/59049 |

Site by site at the larger plane:

| site | ISP | the null |
|---|---|---|
| (0, 0) | 1/81 | 1/81 |
| (0, 1) | 68/729 | 68/729 |
| (0, 2) | 116/729 | 116/729 |
| (1, 0) | 32/729 | 32/729 |
| (1, 1) | 0 | 0 |
| (1, 2) | 2620/19683 | 116/729 |
| (2, 0) | 32/729 | 32/729 |
| (2, 1) | 1324/19683 | 68/729 |
| (2, 2) | 8800/19683 | 32/81 |

and at the smaller:

| site | ISP | the null |
|---|---|---|
| (0, 0) | 9824/19683 | 32/81 |
| (0, 1) | 8480/59049 | 80/729 |
| (1, 0) | 12617/59049 | 281/729 |
| (1, 1) | 8480/59049 | 80/729 |

The two distributions are not merely unequal in aggregate. At the larger
plane they agree at six of the nine sites, the parting is confined to the
remaining three, and the probability it moves among those three sums to zero,
as a probability must. The total-variation column above and the largest-site-gap
column of section 13 carry the same values at each plane under different names,
because at each plane one site absorbs the whole probability movement; they are
one measurement seen twice, not two.

**Q147 and Q148, answered at this scope and no further.** The observable on
which a record-carrying model and its record-free ablation differ is the
site-occupation distribution at the third tick. Both models express it; both
are handed the same arena, alphabet, coin, initial state and horizon; the
values above are exact rationals on both sides. The prediction is free of every parameter the null does not also carry. What it is not is a first
prediction of the theory: this unit is a model-ablation benchmark, and the value
above is the measured effect of one layer under one coin order, and the class of
models that could reproduce it is not swept here. The sharper limit is that the null compared here is memoryless — at a finite horizon a record is always
absorbable into an enlarged state description, so nothing here excludes a
memory-bearing model from reproducing these same numbers.

## 7. The coin-order fiber, and why the head word carries no tick

The parent declares twelve items of its own construction and stamps one of them
verdict-relevant: the order in which the coin and the record's diagonal are
applied. The delivered version of this unit inherited one member of that fiber
silently. Both members are run here, at both planes.

| arena | coin order | first difference | tick-3 law equals the null's |
|---|---|---|---|
| AG(2, 2) | G.D | 3 | no |
| AG(2, 2) | D.G | 4 | yes |
| AG(2, 3) | G.D | 3 | no |
| AG(2, 3) | D.G | 4 | yes |

Under the alternative order the count phase lands after the coin, so it cannot
enter that step's Born weights at all — which is the parent's own observation
about that member — and the whole effect is displaced by exactly one tick. At
the alternative order the third-tick law is the record-free arm's own, exactly,
and the parting starts at the fourth. So the tick is order-relative, and a head
word naming a tick would have been a statement about one member of a declared
fiber wearing the clothes of a statement about the theory. The head word names
what survives both members — that the layer is not inert — and stamps the
declarations it is said under.

The neighbouring fiber, the sense in which the shift carries an amplitude along
its own direction, moves neither the tick nor the third-tick inverse
participation.

| arena | shift orientation | first difference | tick-3 inverse participation |
|---|---|---|---|
| AG(2, 2) | +l | 3 | 43392899/129140163 |
| AG(2, 2) | -l | 3 | 43392899/129140163 |
| AG(2, 3) | +l | 3 | 33596579/129140163 |
| AG(2, 3) | -l | 3 | 33596579/129140163 |

## 8. The wider memoryless sweep: measuring the parity sentence

Section 3's parity sentence was unmeasured in the delivered version, and it is
measured here. The sweep of section 6 varies the coin of **both** arms together;
it never varies the record-free arm's coin against a fixed record-carrying arm.
Two classes are swept that way against the canonical declaration. The first is
every solution of the arena's own covariant census, at every start site and
every internal direction. The second is every integral coin whose rows are
orthogonal and carry the same squared norm as the coin the record-carrying arm
itself uses, with the arena's covariance dropped entirely; it is far wider than
the first.

| arena | null class | configurations | distinct tick-3 laws | exact reproductions | smallest single-site gap |
|---|---|---|---|---|---|
| AG(2, 2) | the arena's own covariant census | 48 | 16 | 0 | 10144/59049 |
| AG(2, 2) | all integral coins, covariance dropped | 2880 | 76 | 0 | 8200/59049 |
| AG(2, 3) | the arena's own covariant census | 972 | 90 | 0 | 1024/19683 |
| AG(2, 3) | all integral coins, covariance dropped | 6480 | 171 | 0 | 1024/19683 |

Two rulings follow and both are adopted into the unit. The first strengthens the
finding: over 10380 memoryless configurations — same arena, same register
dimension, same shift, same one-basis-vector start, same horizon, no record —
no memoryless walk in either swept class reproduces the third-tick law at either plane, and over the census sub-sweep no
free arm's third-tick inverse participation equals the record-carrying arm's
either. The second corrects a published cost. At the larger plane the
anti-strawman coin is already the best any of these reaches; at the smaller a
non-covariant integral coin comes strictly closer, and the deciding cost below
is priced against **that** one rather than against the anti-strawman
coin alone. That is the one number this repair moved, and it moved to the truth.

## 9. Parameter-free is not structure-free

The prediction is free of every parameter the null does not also carry, and
that is worth stating precisely rather than letting it stand for more than it
is. The record-carrying model carries structures the record-free arm does not,
and each of them is a commitment; five of them introduce no free number at all,
and the sixth row of the table below is not a structure but the one number this
comparison does carry as a dial.

| structure | what it is | free numbers | how it is fixed |
|---|---|---|---|
| the count field | one non-negative integer per cell, initialised uniform and monotone thereafter | 0 | UNCONDITIONAL |
| the feedback rule | the coin at a site is multiplied by the diagonal of phases the site's own counts determine | 0 | UNCONDITIONAL |
| the emission rule | one division event per step, on a cell drawn with that cell's own post-coin weight | 0 | UNCONDITIONAL |
| the branch structure | the state is an ensemble over emission histories rather than a single trajectory | 0 | UNCONDITIONAL |
| the phase character | the map from a count to a phase, taken to be a character of the arena's own scalar group | 0 | UNCONDITIONAL |
| the phase modulus m | the modulus at which that character reads a count; a swept axis of this run, measured moving the headline observable | 1 | FIXED-TO-THE-FIELD-ORDER-BY-THE-DESCENT-FORCING-WHICH-IS-CONSTRUCTION-DEPENDENT |

The delivered version of this paper said zero adjustable numbers, flatly. That
contradicted this run's own sealed axis list, which names the phase modulus as a
declared free axis, and section 11's own table, which measures that modulus
taking 5 distinct values and moving the headline observable at every one of
them. The honest statement is the conditional one: 5 structures and 1 free
number, and the free number is fixed to the field order only by a forcing that
is construction-dependent in the parent's own sense. The absence of a fitted
dial is what makes the ablation clean; and a successor that wants to argue the record layer is
necessary rather than merely effective has to argue against models that carry
some of them too — which is exactly the successor the closing sections
register.

## 10. Why the third tick, and why not the second

That the two models agree exactly through the second tick and differ from the third on is a measurement, and the measurement is the sweep of section 6.
What follows is an account of it. It explains the tick number, it is not gated
as an explanation, and section 17 registers it as unproved — but it is no longer
bare narrative: the case that discriminates it is run.

At the initial record every cell carries the same count, so the record's
diagonal is a scalar multiple of the identity at every site and the coupled
walk is the plain walk times a global phase. The two are therefore equal at
the first tick, and no probability can tell them apart.

The first division event lands on a cell of the start site. After the shift
no amplitude remains at the start site, so no branch's coin reads a modified
cell at the second step, and the second tick is again equal on every branch —
which is why the agreement leg reaches two ticks and not one.

The third step is the first at which a coin can read a cell an earlier step
modified, and the reason is the arena's own closure. The three declared
directions are closed under addition — the third is the sum of the first two
— so a two-shift path can land on a site that a one-shift path had already
reached and whose cells the second step's emission had already touched. The
tick at which the coupling becomes visible is the closure time of that
elementary triangle, and it is the same at both planes because both planes
declare the same three directions.

**The corroboration.** If that account is right then a declared direction set
whose two-shift paths cannot return to a site one shift had already reached must
move the parting one tick later. Three sets are run at the larger plane.

| declared directions | closed under addition | first difference |
|---|---|---|
| (1, 0) (0, 1) (1, 1) | yes | 3 |
| (1, 0) (0, 1) (1, 2) | yes | 3 |
| (1, 0) (0, 1) (2, 2) | no | 4 |

The parting arrives at the third tick exactly at the sets closed under addition
and at the fourth at the set that is not. That is the account's discriminating
case and it comes out the account's way; it is evidence for the mechanism rather
than a proof of it, and section 17 still asks a successor to prove it or find
the arena where it fails.

A scalar coin never mixes the directions, so the record's diagonal stays a
diagonal and no probability moves. That is why the discriminant requires interference: at the scalar coins the two models never separate.

## 11. The connection modulus: an internal check, closed

NDEP registered a successor test and did not run it. Its words:

> The concrete successor test is to build the connection at AG(2,2) over F_2 and see whether it forces m = 2, and to find an observable the modulus does move.

and its reading of the parent's derivation:

> AG(2,2) is over F_2, so read there it predicts m = 2, a determinate and q-carried answer

The derivation being read is the coupling unit's own:

> The link connection the record defines is therefore valued in the arena's own scalar group Z_3, and the walk's phase alphabet is the cube roots of unity.

That sentence has content, and the content is a forcing **given its own
construction**. A record cell's value is an arena scalar; the parent takes
the phase to be a character of the arena's scalar group; such a map is a
function of that scalar exactly when it is constant on residues modulo the
field order, and it tells the arena's own scalars apart exactly when it is
injective on them. Both conditions are decided by integer residues alone, and
both reduce to a two-line lemma rather than to a search: the first holds exactly
when the modulus divides the field order, the second exactly when the modulus is
at least the field order, and the two together leave the field order itself. The
sweep below is the lemma's instances, and the run verifies each equivalence
against its own computed predicate at every swept point rather than reading the
table as an empirical finding.

| field order | admissible moduli |
|---|---|
| 2 | 2 |
| 3 | 3 |
| 5 | 5 |
| 7 | 7 |

At every prime field order swept, one modulus survives both conditions and it
is the field order itself. Read at the smaller plane the parent's derivation
therefore fixes the modulus two, which is what NDEP said it fixed and what
this unit's descent leg now derives rather than repeats.

This closes an obligation the corpus wrote for itself. It is an internal check
and it is construction-dependent, and the reason is in the construction: the
forcing holds for the phase-character reading the parent chose, and a different
reading of what a connection is would carry a different forcing. What the leg
establishes is that the parent's own choice is determinate where it had been
untested.

The second half of the registered test is the observable. NDEP could not see
the modulus because its census read only the count field; the parent
disclosed the reason in its own voice —

> the walk consumes the count residue n mod 3, not the count

— so an observable that moves with the modulus has to be one the walk's
amplitudes carry. The site occupation at the third tick is such an
observable, and at the smaller plane it separates the whole declared window.

| modulus | tick-3 inverse participation | branches |
|---|---|---|
| 1 | 58235/177147 | 270 |
| 2 | 43392899/129140163 | 306 |
| 3 | 4680635/14348907 | 324 |
| 4 | 41546723/129140163 | 324 |
| 6 | 41655923/129140163 | 324 |

Every value in that column is distinct, so the modulus is not invisible at
the smaller plane and the registered test is decidable there. The row at the
first modulus is the record-free arm: a phase alphabet with a single element is
no connection at all, and that arm sits inside this family as the point the
parent's construction excludes. The remaining member of the declared window, the
twelfth, leaves the rationals — its Born weights land in the real quadratic
subfield rather than in the rationals — and the run records that rather than
dropping it.

**The register's own opening.** The descent sweep runs at prime field orders,
where the additive group of the field is cyclic. At a prime power it is not,
and the same argument would have to be made with a character of an
elementary abelian group; the corpus's own trouble at the fourth order is
the same trouble, and this unit does not close it.

## 12. The reproductions, measured rather than conceded

The three results the corpus quotes most often are lattice results, and all
three are put to a record-free arm on the shared carrier with that arm's own
declared measure. That arm is a different object from the coined walk of the
earlier sections and is called the record-free lattice arm throughout this
section: a uniform configuration plus the counting measure on the parent's own
carrier. The anti-strawman rule cannot select its coin, because that carrier has
no record-carrying coin to copy; its coin is declared to be the Hadamard link
coin and that fiber is left unswept. POT's perimeter law in its own words:

> At every coin of the carrier and at every pair of ladder shapes of equal perimeter, the loop observable takes the same value

is a per-configuration statement about a uniform configuration, and a uniform
configuration is a thing a plain lattice model has. The quantity that sentence
groups by is pulled out of the sentence itself and selects the grouping the run
uses. Its spectral row:

> The ladder's own transfer content is the closed form of section 5: spectrum {1,1,1/2} and gap 1/2, verified at every coin of the carrier and unchanged at every declared row.

names a number that this unit pulls out of the sentence and compares against
the ratio the fitted halving term actually carries between consecutive
perimeters at the record-free lattice arm's own coin. That ratio is the ansatz's
own basis function and is definitional once the halving coefficient is non-zero;
the measured content of the row is the coefficient's presence, at the coins
counted below and at that arm's own coin. Its plaquette row:

> the plaquette's trace takes 11 distinct values on the carrier, its counting expectation is 13/10

names a counting expectation, and a counting expectation is that arm's own
measure by rule PR7. The parent reports one such expectation per declared weight
system and stamps them all conditional on those weights; what is reproduced here
is the one taken at the counting measure. And ACT's pinned observable —

> the sign of the fourth power of the two off-diagonal entries, added

— whose expectation ACT reports as follows:

> Its expectation under every admissible weight system is therefore the single value zero

is pinned by a pairing under the odd twists, and the pairing is a property of
the coin family rather than of any weight system. The value the parent pins it
to is read out of that sentence's own last word rather than typed into the gate.

| parent's result | measured on the shared carrier | the null's own coin |
|---|---|---|
| perimeter-only law | 3200 comparisons, 0 disagreements | in the family: True |
| three-term closed form | 0 failures over 640 coins | coefficients 2 -1 1 |
| halving mode | present at 512 of 640 coins | present: True |
| plaquette counting expectation | 11 distinct values | 13/10 |
| off-diagonal quartic sign | 352 zero, 144 plus, 144 minus | 0 |

All five rows reproduce. The record-free lattice arm's coin is a member of the
shared family; its fitted ladder carries a constant term, a term linear in the
perimeter and a halving term with the coefficients shown, so the mode that
carries the parent's gap is present at that coin and the gap is that arm's too.
The fraction in that row is a counting fraction: a count of coins over a count
of coins on the shared family, with no weight system.
The counting expectation of the pinned observable is the parent's single value
exactly. These five rows are the ones measured against a different record-free
arm on a different carrier from the walk's, and the head's single null token
names the walk's arm; the split is stated here and in section 17.

The coupling unit's coin-register theorem is in the same position, and the
reason is worth separating from the result.

| stencil | nonzero differences | multiplicities | unitary | non-monomial |
|---|---|---|---|---|
| the arena's link offsets | 6 | 1 | 18 | 0 |
| a collinear stencil | 2 | 3 | 216 | 198 |

The theorem is carried by the arena's offset set, not by the record: on a set
every one of whose nonzero differences is realised once, no unitary
coefficient map is anything but a monomial, while a collinear set admits
many. The record-free arm shares the arena, so it inherits the theorem. A result
that follows from the shared ground is not evidence for either model over the
other, and this unit says so.

## 13. What each model can express, and the regression test

The pin is explicit that an absence is not an argument:

> the record-side observables the null lacks by construction (state which are definitional vs measurable)

so every observable this unit touches is classed before it is used, in a class
vocabulary read out of that sentence rather than typed into the gate. The
classes are two: whether the quantity can be formed in both models, and
whether its unavailability in the record-free arm is a measured disagreement or
a definitional absence.

| observable | expressible in | class | why |
|---|---|---|---|
| site-occupation distribution p_t(x) | BOTH | MEASURABLE | a function of the state alone |
| inverse participation ratio | BOTH | MEASURABLE | a function of the site distribution |
| total variation between two ticks' distributions | BOTH | MEASURABLE | a function of distributions |
| loop observable on a uniform configuration | BOTH | MEASURABLE | a function of the link operators |
| off-diagonal quartic sign | BOTH | MEASURABLE | a function of the coin |
| plaquette counting expectation | BOTH | MEASURABLE | a function of the coin family and a measure |
| division-count field n_l(x) | ISP-ONLY | DEFINITIONAL | the null has no count field |
| emission field and its link-class marginal | ISP-ONLY | DEFINITIONAL | the null emits nothing |
| branch count of the emission tree | ISP-ONLY | DEFINITIONAL | the null has one trajectory |
| record curvature field on the elementary triangle | ISP-ONLY | DEFINITIONAL | a function of the count field |
| maximum cell count | ISP-ONLY | DEFINITIONAL | a function of the count field |
| admissibility exit probability | ISP-ONLY | DEFINITIONAL | admissibility is a property of the record |

Of the 12 declared observables, 6 are formable in both models and 6 are
formable only where a record exists, and that fraction too is a counting
fraction over the declared list. A definitional absence is not a discriminant. The observable of section 6 is deliberately drawn from the
first half: it is a function of the walk's own state, which the record-free arm
has, and the two models disagree about its value rather than about its
existence.

**Q149 and Q150, at the class this unit actually has.** The falsifier is a
**computational regression test on a proposed realization**, not an empirical
test of nature. A program that claims to realize this model is refuted at
either declared plane if its site occupation at the third tick takes any value
other than the one published in section 6, and the case where it takes the
record-free arm's own value is the diagnostic one, the one that says the record
layer was not implemented at all. The statement is sharp because the value is
exact on both sides, and the sweep shows that the tick at which the two models
part does not move with any axis this unit sweeps except the coin order, where
it moves by one and the declared fiber above says so. The values themselves are stated at the
canonical declaration and are not claimed invariant across the axes; what the
sweep gates is the parting, not the number.

The test is finite twice over.

| arena | largest site gap against the closest memoryless walk | shots | branches |
|---|---|---|---|
| AG(2, 3) | 1024/19683 | 36948 | 486 |
| AG(2, 2) | 8200/59049 | 5186 | 306 |

The second column is the largest single-site gap between the record-carrying
model and the closest memoryless walk section 8 found, which is the quantity a
two-outcome test measures. The third column is the number of repeated
preparations that would separate the two values, computed by Chebyshev's
inequality on that gap in exact rational arithmetic at a declared confidence
denominator of 100, with no logarithm and no float anywhere in the derivation.
The fourth is the cost of the deciding simulation: the emission tree is
exhausted at that many branches, so the regression test is decidable on a
laptop. Against the anti-strawman null alone the smaller plane would cost 3389
rather than 5186; the larger figure is the one published, because the smaller
one is the cost against an opponent that is not the closest. The shot count is
what an experiment would cost **if** something realised the declared register and
the declared tick. No operational mapping to laboratory units exists in this corpus, so the shot column is a scale and not an experimental proposal.

## 14. Feasibility, both ways

The programme's standards bind a feasibility argument against the committed
corpus at the declared row list, and the pin pre-registers three outcome words.
The delivered version carried neither. Both are here, and the feasibility half
is machine-checked rather than narrated: the alternative outcome is reached by a
declared recipe of this unit's own battery, which switches the coupling off in
the headline arm, drives the payload to the negative verdict word and dies at
the gate that vouches the values.

| the pin's outcome word | status |
|---|---|
| DISC-FOUND | REACHED-UNDER-THE-RENAMED-POSITIVE-WORD |
| DISC-NULL-REPRODUCES-ALL-TESTED | REACHABLE: the census's own limit at 10-OF-10-TESTED-ROWS, and the negative verdict word RECORD-BACKREACTION-NOT-DETECTED-IN-THE-SWEPT-WINDOW is driven by the declared recipe MUT-VALUES, which switches the coupling off and dies at G-DISCRIMINANT-VALUES |
| DISC-BLOCKED | RETIRED-AS-UNREACHABLE: no route in this instrument produces it -- the two arms are exhaustive and exact, so the run either separates them or does not, and there is no third exit; the word is declared retired rather than left silent |

The pin's positive word survives under the name the binding review gave it. Its
negative word is reachable and its reachability is demonstrated by a recipe
rather than argued. Its third word has no route in this instrument at all — the
two arms are exhaustive and exact, so a run either separates them or does not,
and there is no third exit — and it is declared retired rather than left silent.

## 15. What this decides, what it does not, and the successor

**Decided, at the declared scope.**

- 6 of 9 parent results are reproduced exactly by a model with no record and no
  price, on the parents' own carriers, with the parents' own numbers, and none
  of them distinguishes this theory from a plain coined walk.
- Over 10380 memoryless configurations, no memoryless walk in either swept class reproduces the third-tick law at either plane.
- The record-free arm is provably the coupling unit's own mandatory
  frozen-stage control on the compared observable. The bare statement that the
  record layer is not inert is therefore that parent's already-sealed result;
  what this unit adds is the tick, the exact magnitudes at both planes, and the
  universality over the fiber.
- Removing the record layer and holding everything else fixed changes an
  observable both models form: the site occupation at the third tick. The
  tick is the same at both planes, at every interfering coin, every start
  site, every internal direction and both emission readings.
- The connection modulus the coupling unit's construction fixes is
  determinate at every prime field order swept, and it moves an observable at
  the plane where it had never been tested. That closes an internal
  obligation.
- The model's extra structures are 5 and the free numbers among them are 1.
  Parameter-free is not structure-free.

**Not decided, and named.**

- The tick is declaration-relative. At the parent's alternative coin order the
  parting arrives one tick later and the third-tick law is the record-free
  arm's own exactly. Nothing here decides which member of that fiber a physical
  reading would select, and the head word carries none.
- The null compared here is memoryless: its coin is constant in space and in
  time. At a finite horizon a record is always absorbable into an enlarged
  state description, so nothing measured here excludes a memory-bearing model
  from reproducing the third-tick numbers as well.
- What was compared is a pre-registered null obeying the seven pre-registration
  rules — which is the parent's own frozen-stage control up to a global
  phase — together with the swept memoryless class, and no
  memory-bearing null is tested here. That is the shape of the comparison and
  it should not be read as one opponent standing for every simpler model: the
  swept class is wide but it is a class of memoryless walks, the space of
  models carrying memory is not entered at all, and no statement about that
  space is made here.
- The difference is measured at two planes, at a sweep horizon of three ticks
  with a horizon of five for the headline arm at the smaller plane and for
  the fidelity arm at the larger, and at two emission readings. Whether it
  persists at larger planes, longer horizons or other arities is untested.
- Nothing here is an experimental number. No operational mapping to
  laboratory units exists in this corpus.
- The reproductions of section 2 are rulings about what those parents'
  results distinguish, not about whether they are true. Every one of them
  stands as its parent proved it.

**The successor, registered.** DISC-2 is the next opponent: the simplest
**memory-bearing** null — a finite-memory coin, a state-dependent coin, a
dynamically updated phase field, or an enlarged internal register — built to
the same parity rules and put to the same third-tick observable. That is the
model this unit's result does not touch, and it is named here rather than
attempted here.

## 16. The instrument

The instrument is a single file with one entry point and a whitelisted
argument list; anything outside the list exits two. Every declared mode runs the
whole gate battery. The plain run writes both
artifacts and nothing else; the no-write run writes nothing; the render mode
emits exactly the tables, claims and fenced block this paper carries; the
self-test corrupts a transcript row, adds a key after the seal and edits a
sealed value, requires each to be refused, and writes nothing.

Every gate carries a recipe that names the measured key it must move, and the
harness digests that key before and after the recipe runs: a recipe that
leaves its key identical is a sentinel and dies as one, whatever it says
about itself. No gate carries a waiver: the two the delivered version waived —
the scope census and the read set — now carry measured predicates and recipes of
their own. The recipes are corruptions of measurements rather than of
flags — the coupling switched off in the headline arm, the sweep stopped one
tick short of its own finding, the ladder shapes grouped by their longer side
instead of by the quantity the parent's sentence names, the exponent misread out
of the parent's own sentence, the expectation taken over one sector instead of
the carrier, the modulus read at the wrong plane, the two routes given different
coins, the anti-strawman rule rewritten after the fact, the same rewrite by an
author who also re-declares the digest beside the table so that only the text
leg is left to catch it, the fiber's second
member run at the first member's order, the wider class never swept, the head
rebuilt by its own builder, a census ruling flipped in each direction, a gate row
renamed in the text that would be promoted, and an undeclared read taken after
the read gate has passed.

The coverage denominator is the declared gate list, fixed before any gate fires,
so a gate that fires after the coverage gate is inside the denominator rather
than outside it by ordering; the last gate reconciles that list with the gates
that actually fired, itself included, so the row count, the mode that lists the
gates and every published total are one number. That last gate binds the
transcript to the ledger by content, recomputes the ledger's own chain, and
re-checks the read log at the close; it carries two recipes of its own, which is
the first closed instance of that family the era has recorded.

The verbatim anchors are read through one accessor that records the read, and
each is required to occur exactly once in its pinned parent's own bytes and
once in this paper's rendering under the same canonicalisation. Each is then
consumed by the gate its own column names, and consumed for its content: the
field order comes out of the coupling unit's sentence, the modulus out of
NDEP's two sentences, the gap out of POT's spectral sentence, the trace count
and the counting expectation out of POT's plaquette sentence, the exponent and
the pinned value out of ACT's two sentences, the spatial dimension out of the
pin's own description of the null, the grouping quantity out of POT's perimeter
sentence, the parity count out of the coupling unit's control sentence, and the
class vocabulary out of the pin's own parenthetical.

The paper's tables, claims and fenced block are compared against this text by
multiset equality in both directions, keyed by the table each row was
rendered into; every numeral, every exact fraction and every spelled number
in this text is required to be a value the run measured, with the declared
identifier exemptions removed first and every exemption required to occur;
and every numeral of every prose sentence whose subject noun names a declared
universe is resolved against that universe alone, per occurrence, with the
fenced blocks and the table rows removed — the tables are bound row for row by
the claims gate, which is a stronger binding than a universe — so the run's own
verdict cannot discharge the paper's obligations. Every universe is required to
bind at least one sentence of this paper, the count of resolved occurrences is
recomputed by a second route, and that route also resolves the spelled numbers
the template's own pattern cannot see, which is the grain at which a headline
inversion lives. Three semantic walls run against the canonicalised text, one of
them bought by this unit's own review: it bans the reading in which an
ablation effect is reported as a prediction of nature, in the voices a paper
would use, it requires this paper to carry the three sentences that keep the
class straight, and its licence leg requires every sentence that names the banned
reading to carry one of this run's own rendered claims. Every wall carries
control sentences written from the disease rather than from the patterns, and
every control is required to be caught by its own wall.

## 17. The successor register

Eight things this unit leaves named.

DISC-2, the memory-bearing null of section 15, is the first and the largest.

The account of section 10 is an account. The tick number is measured at every
point of the sweep and the case that discriminates the account is run, but that
the arena's elementary triangle is what fixes it is an explanation the
instrument does not gate, and a successor should either prove it or find the
arena where it fails.

The coin order is a declaration this unit inherits and does not decide. Both
members are run and reported; which of them a physical reading would select is
a question for a successor with an operational criterion, and this unit has none.

The sweep's horizon is three ticks. The parting is established there and the
account explains it, but whether the two models' distributions ever
re-converge at a later tick is not measured, and a re-convergence would be a
finding rather than a repair.

The descent sweep runs at prime field orders. The prime-power case needs the
character of an elementary abelian group and is the corpus's own standing
trouble at the fourth order.

The emission reading is swept over the parent's two. A reading in which the
emission is not a function of the walk's amplitudes at all would change what
back-reaction means, and no such reading is built.

The lattice legs and the walk leg are run on different carriers, because
their parents are, and the record-free arm therefore denotes two objects. A
single carrier carrying both the loop observable and the walk would let the
perimeter law and the third tick be measured against one another, and would be
the natural successor object.

And the shot counts assume a preparation that this corpus has no mapping for.
Until the mapping exists the test is a regression test on a program,
decidable and cheap, and it is stated as one.
