# AUTOGLUE (paper-45) — K2 EFFECTUS review

**Seat:** K2 (EFFECTUS), three-seat hostile panel, v15 wave 1.
**Object:** `v15/paper-45-autoglue.md` `09ee568a4bba` · `v15/code/autoglue_exact.py`
`d750a64f153c` · `v15/code/autoglue_output.txt` `1a287adcaead` ·
`v15/code/autoglue_receipt.json` `34606049f1ef` · pin `v15/note-autoglue-pin.md`
`88c04df52c19`. **All five re-verified at open and at close; none moved.**
**Authority:** the pin; `v15/PLAN.md` W1/W2/W3; `v15/LOG.md` #2/#5/#11/#17;
RUNBOOK through E-33.
**Status:** CANDIDATE UNTIL ADJUDICATION.

---

## GRADE

**ACCEPT-WITH-MAJOR-REPAIRS. No reject lever.**

Every measured quantity in this unit reproduces. I rebuilt the arena, the event
family, the incidence census at all sixteen window cells, the seam system, the
26-row form census verbatim, the successor multiplicity, the whole-state fiber,
the fourth-direction table, the preparedness census over all 29,791 states, the
eight extremal functionals before and after every one of the eighteen cuts, the
Q50 chain link by link, the two-step census and the price table — from my own
construction, importing nothing from the instrument. **232 quantity checks, zero
disagreements on any delivered physics number.** Three tables I could not match
on the first attempt (the form census's boolean columns, the fourth-direction
split, the preparedness histogram) turned out to be my own modelling gaps, and
all three reproduce exactly once the unit's actual legs are used. That is a
strong instrument and an unusually honest paper: its four concessions —
event-conditioned and not autonomous, a relation and not a law, an equivalence
class and not a member, nothing derived — are the result, and they are all
carried in the head where they cannot be lost.

The repairs are nine, and they are not cosmetic. One is a **false number in the
paper's prose**; one is a **false scope word in a head string**, wrong at a third
of its own witnesses and wrong in the direction that collides with the unit's
central object; one reports **a single measurement as two**; one **re-attributes
the second head segment's mechanism to the parent**, where it already sits with
its own object-by-object check; three **engraved disciplines are carried in prose
with no gate**, and all six paraphrases I wrote against them survive the
whole delivery.

---

## THE RECOMPUTATION LEDGER

436 independent recomputations, plus six full deliveries under planted
paraphrases.

| leg | checks | result |
|---|---|---|
| the five object hashes, at open and at close | 10 | all five verify twice, unmoved |
| the six pinned sources, re-hashed | 6 | 6/6 verify |
| the nine verbatim anchors, located under the #125 normaliser | 9 | 9/9 locate exactly once |
| arena, automorphisms, events, orbits, incidence at all 16 cells, seam system | 72 | exact (re-run 6×) |
| the 26-row form census, successor multiplicity, whole-state fiber, fourth direction, preparedness, price | 65 | exact, row by row |
| the 8 extremal functionals, the 18 cuts, the one-sided identity, the two-sided constancy, the Q50 chain link by link, the two-step | 29 | exact |
| the mechanism sentences (the 27 no-move crossings, the [2,0,1] failure, the 216 identity, the equivalence class, the feasibility predicate) | 14 | exact |
| the first pass's superseded form-leg checks | 52 | resolved as my modelling gaps |
| *— quantity-check subtotal* | **232** | **zero disagreements on a delivered number** |
| the 42 gates re-run at the delivered paper | 42 | 42/42 PASS, artifacts untouched |
| the unit's own 30 wall controls, re-read from the receipt | 30 | 30/30 caught |
| my six planted paraphrases × six walls | 36 | **0/36 fired** |
| the `ds_in_cut ≡ ds_realises` identity, at all 31 lattice points | 31 | identical everywhere |
| the 17 declared parent values, audited for consumption | 17 | 16 consumed, 1 dead |
| the referent gate's binding rate over the paper's prose | 18 | 18 pairs bound of 234 prose numerals |
| the extremal family's uniqueness count, five readings | 5 | none gives 3 |

**Zero delivered physics numbers wrong.** The two mismatches that survive are a
prose count (M1) and a representative-carried sentence (M9); neither moves a
measurement.

---

## MAJORS

### M1 — A FALSE NUMBER IN THE PAPER'S PROSE. §6: "Three of the eight functionals do select uniquely somewhere."

**Establishing measurement.** Recomputed from the eight functionals' own
definitions over the 31-completion lattice and over each of the 18 cuts:

| reading of "selects uniquely somewhere" | measured |
|---|---|
| selects a single completion **before** the event | **2** — maximum determinant, minimum cross coupling |
| selects a single completion at **some cut** after it | **4** — min/max cross coupling, min/max one-sided price |
| does so **before or after** | **5** |
| does so **before and after** | **1** — minimum cross coupling |
| does so at **every** cut | **0** |
| distinct underlying functionals (min/max pairs collapsed) | **5** |

No reading returns 3. The instrument's own gate agrees with the first row:
`G-NOTHING-DERIVED` asserts that the only functionals with `selects before == 1`
are maximum determinant and minimum cross coupling — two, not three.

**Why it shipped.** The numeral is spelled. `G-PAPER-COVERAGE`'s spelled leg
requires only that the integer 3 be measured *somewhere* in the run — it is, many
times over — and `G-PAPER-REFERENT` binds only the literal digit form `N of M`.
A spelled numeral with a stated denominator ("of the eight") is bound by nothing.

**Licensed replacement (§6):**

> So: **nothing is derived.** Two of the eight functionals select a single
> completion before the event and four select one at some cut after it, and each
> such selection is an addition to the corpus rather than a consequence of it.

**Instrument order.** Extend the spelled-numeral leg to spelled `N of M` pairs
and route them through the referent gate's universes; the family here is
`n_functionals`.

---

### M2 — THE OBSTRUCTION THEOREM'S THIRD LEG IS SCOPED BY A WORD THAT IS FALSE AT 156 OF ITS OWN 468 WITNESSES.

The head (segment 2), §4.3 and §6.1 leg 3 all say the same thing:

> AN UNSHARED SITE'S FORM HAS KERNEL ZERO AND FORCES THE FOURTH DIRECTION'S
> COUNT AGAINST THE ONE THE EVENT DEPOSITS AT 468 OF 468 READINGS

**Establishing measurement.** I classified all 468 readings by the site the
instrument reads them at:

| the base site of the reading | readings |
|---|---|
| an unshared (chart-private) site | 312 |
| **a SHARED (seam) site** | **156** |

The three shared–shared within cells are the glue-line pairs; each is read in
both charts at both endpoints, contributing 156 of the 468. At a shared site the
form is not the kernel-zero object: it is precisely the rank-6-on-10-unknowns,
**kernel-four** object this unit builds its entire state around (§4, verified:
rank 6, kernel 4, 31 completions). The head therefore attaches a kernel-zero
property to exactly the sites the paper elsewhere proves have kernel four.

**The finding survives the repair.** The mechanism is sound — a *single chart's*
restriction of the form at *any* site has kernel zero over that chart's three
declared directions, which is why the arithmetic `2n₁ + 2n₂ − n₃` reproduces
468 readings with 0 agreements at every one. Only the scope word is wrong, and
§6.1's own first clause already uses the right one ("refused by **the sector's
own form**").

**Licensed replacement (head segment 2 and §4.3/§6.1):**

> A SECTOR'S OWN FORM AT A SITE HAS KERNEL ZERO OVER ITS THREE DECLARED
> DIRECTIONS AND FORCES THE FOURTH DIRECTION'S COUNT AGAINST THE ONE THE EVENT
> DEPOSITS AT 468 OF 468 READINGS — 312 OF THEM AT CHART-PRIVATE SITES AND 156
> AT THE SHARED SITES, WHERE THE TWO-CHART FORM'S KERNEL IS FOUR AND THE SINGLE
> CHART'S IS STILL ZERO

This is the closest thing in the unit to a reject lever, and it does not reach
one: no number moves, and the disclosure *strengthens* the leg by showing it
holds at the seam sites too.

---

### M3 — ONE MEASUREMENT REPORTED AS TWO (family S-2, registered-unimplemented in the RUNBOOK).

Head segment 3 and §6:

> THE DIRECT SUM REALISES 0 OF THE ARENA'S CROSSINGS AND SURVIVES 0 OF THE CUTS
> … And the dynamics refutes it **twice over**.

**Establishing measurement.** By construction `cuts[k] = {U : ccs(U)[k] == 1}`,
so "U lies in cut k" and "U realises direction k" are the *same predicate*. The
two sealed receipt keys are the same function:

```
ds_in_cut   = |{k : (0,0,0,0) ∈ cuts[k]}|      = |{k : ccs(0)[k] == 1}|
ds_realises = |{k : ccs(0)[k] == 1}|
```

Tested at **all 31 lattice points**, not only at the direct sum: **0 completions
where the two counts differ.** The distribution over the lattice is
{4: 18, 6: 12, 0: 1} for both. It is an identity, not a coincidence of this
arena.

Head segment 3 therefore reports **seven** independent numbers as eight, and §6's
"refutes it twice over" is one refutation counted twice.

**Licensed replacement (§6 and head segment 3):**

> MAXIMUM DETERMINANT SELECTS THE DIRECT SUM UNIQUELY, AND THE DIRECT SUM
> REALISES 0 OF THE ARENA'S 18 CROSS DIRECTIONS — WHICH IS THE SAME STATEMENT AS
> SURVIVING NONE OF THE 18 CUTS, SINCE A CUT IS THE SET OF COMPLETIONS THAT
> REALISE ITS DIRECTION — AND ON THE POST-EVENT LATTICE THE SAME CRITERION IS
> 4-VALUED

and in §6, "the dynamics refutes it at the one place that matters" rather than
"twice over". Drop one of the two receipt keys or seal them as one row with the
identity stated.

---

### M4 — THE Q50 MECHANISM IS THE PARENT'S, WITH ITS OWN OBJECT-BY-OBJECT CHECK, AND IS TAKEN IN THIS UNIT'S VOICE.

§5.1 and head segment 3 present the disjointness mechanism as this unit's:

> and the mechanism generalises to a two-sided statement, checked at every
> object. An event leaves the weld forced exactly when it doubles nothing …
> Three actors give three pairs and at most two of them can cross the seam,
> because a triangle admits no proper two-colouring …

and §14 S-1 claims ownership — "the disjointness theorem of section 5.1 is
scoped to a = 3 **by its own proof**".

**Establishing measurement.** `v14/paper-40-sec2.md` §4.5 — a source this unit
reads at a pinned sha and quotes from — already contains, in the sentence
immediately following the one AUTOGLUE quotes:

> Three actors give three pairs, at most two of which can join the sectors,
> since a triangle admits no proper two-colouring; so a group that opens no pair
> inside a sector must double at least one link the union already carries; and
> the free items are exactly what a doubling buys. **The hypothesis is checked at
> every one of the 455 groups and the conclusion at every one of the 216 lawful
> ones, object by object.**

Every link of AUTOGLUE's chain except the last is the parent's, including the
object-by-object check at the same 455. AUTOGLUE quotes the parent's *conclusion*
and re-voices the parent's *mechanism*.

**What is genuinely new here, and it is real:** (a) the two-sidedness — the parent
says no lawful event is motivated; AUTOGLUE measures the motivated set at 77 and
the lawful-at-every-leg set at 162 and their intersection at 0 (I verified: the
77 are exactly the zero-doubling events, profiles [0,3,0] 5 and [2,1,0] 72; the
162 are [0,0,3] 54 and [1,0,2] 108; disjoint by profile); and (b) the closing
link the parent does not have — *and that is the one cell the sector's own form
refuses*, at 468 of 468 readings. That last link is the whole reason the theorem
becomes a statement about geometry rather than about counting.

**Licensed replacement (§5.1, and §8's reproduction list):**

> The triangle step and the doubling↔free-items step are SEC-2's, checked there
> at every one of the 455 groups; this unit reproduces them and adds two things
> the parent does not have: the two-sided form — 77 motivated, 162 lawful at
> every leg, intersection 0 — and the closing link, that the pair a
> doubling-free event must open inside a sector is the one cell the sector's own
> form refuses, at 468 of 468 readings. **The events that pin the weld down are
> the events the geometry cannot host** is this unit's sentence; the arithmetic
> under it is the parent's.

§8's list of reproduced parent results ("the 216, the two crossing inventories,
the automorphism order of 62208") must gain the Q50 mechanism.

---

### M5 — W2, W3 AND THE AUTONOMY RENAME ARE PROSE-CARRIED WITH NO GATE; SIX OF SIX PARAPHRASES I WROTE SURVIVE THE WHOLE DELIVERY.

The unit's own count is 6 walls, 30 controls, 30/30 caught — I re-read all 30
from the receipt and confirm it. But those controls were written by the unit.
I wrote six against the *disciplines*, having read the wall source, appended each
to the paper and pushed it through the full delivery in process (`WRITE` forced
false; artifact digests compared before and after; artifacts untouched).

| plant | flavour | walls that fired | full delivery |
|---|---|---|---|
| K2-AUTONOMY-A "the update settles which conflict group fires next … the crossing arrives without anything outside the process handing it over" | autonomy (#11) | none | PASSED |
| K2-AUTONOMY-B "what section three exhibits is therefore autonomous dynamics … the process moves itself" | autonomy (#11) | none | PASSED |
| K2-GAUGE-C "a gauge redundancy of the theory: two states related by one of them are the same physical state, and no experiment could ever tell them apart" | W2 | none | PASSED |
| K2-FAMILY-D "a prediction of the ISP family as such, holding at every arena the family admits" | W3 | none | PASSED |
| K2-STATE-E "the three objects this unit tracks are the whole of what the process carries at a moment" | seam-subsystem naming (#11) | none | PASSED |
| K2-METRIC-F "what the cross block records is how far apart the two sectors stand" | no-metrical-reading | none | PASSED |

**0 of 36 wall scans fired, and all six deliveries ran to the end of the gate
set — 42 of 42 PASS, exit 0, artifacts byte-unmoved.** Not one of the six was
stopped by anything. The diagnosis is not that the walls are badly
written — WALL-RELATION-NOT-LAW, WALL-RECONSTRUCTION-NOT-DERIVATION and
WALL-COUNTING-ONLY each have a real licence leg and kill their own paraphrases.
The diagnosis is **three of the engraved disciplines have no wall at all**:

- **W2** (invariance never promoted to gauge or physical meaning) — carried only
  in §8's prose. There is no pattern anywhere in `WALLS` for *gauge*,
  *redundancy*, *physical state*, or *no experiment could tell them apart*.
- **W3** (family-level predictions only), engraved at #17, i.e. **before** this
  unit's delivery at #20 — carried nowhere. The words *fibre*, *family-level* and
  *still-free* do not occur in the paper at all; the only occurrence of "fiber"
  is a column header in §10.
- **the #11 autonomy rename** — the head is correctly re-worded (I verified:
  "autonomous" occurs four times and every one is a negation), but nothing
  prevents the word being re-asserted.

K2-METRIC-F is different in kind: WALL-NO-METRICAL-READING exists and is simply
porous — it policies the words *metric/distance/geometry of space/causal
structure/signature/interval* and a paraphrase built from "how far apart … stand"
walks through. That is the CONTRACT K2 disease in its second generation: the
licence leg is on, and the *policed* leg is still a word list.

**Orders.** (Z-a) Add `WALL-INVARIANCE-NOT-GAUGE` (policed: gauge, redundancy,
physical state, same state, no experiment/observable could distinguish; licence:
the every-observable standard, *invariance of the quantities measured*, *at the
arena it measures them on*, *has not yet defined an observable*; must-carry: §8's
own sentence). (Z-b) Add `WALL-FAMILY-PREDICTION` (policed: *of the ISP family*,
*at every arena*, *holds generally*, *a prediction of the theory*; licence:
*at this arena*, *fibre-invariant*, *independently selected*, *declared*;
must-carry: a new §8 sentence stating W3 and the unit's fibre status — see M7,
which supplies the measurement that discharges it). (Z-c) Add
`WALL-EVENT-CONDITIONED` (policed: *selects the event*, *moves itself*,
*without anything outside*, *autonomous*; licence: *supplied*, *the grammar is
not re-driven*, *event-conditioned*, *not*). (Z-d) Widen
WALL-NO-METRICAL-READING's policed leg to distance-without-the-word forms
(*how far apart*, *separation*, *nearer/farther*, *the gap between*), and add all
six of my plants to the control set so the repair is proved on them.

---

### M6 — THE PRE-REGISTERED OUTCOME'S TRUTH PREDICATE CONTAINS NO CREATION; NO-RULE-EXISTS WAS NOT REACHABLE THROUGH IT.

§2.3 pre-registers two outcome words and reaches **both**. The first,
TARGET-FREE-EVENT-CONDITIONED-CREATION-EXISTS, is reached iff
`len(full_alive["CROSS-ONLY"]) > 0`.

**Establishing measurement.** Of the 162 events in that set:

| | events |
|---|---|
| events at which CROSS-ONLY creates **no cell at all** | **54** |
| events at which it creates a cross cell | 108 |
| the lawful set of the **NONE** rule — the rule that creates nothing | **54, the same set** |

So the predicate that certifies "target-free creation exists" is already
satisfied, at this arena, by 54 events at which nothing is created, and is
satisfied equally by the rule that creates nothing at all. It could not have
returned false while any all-doubled triple existed. **NO-RULE-EXISTS was not on
the table.**

Three further defects in the same gate:

1. The published **witness** for outcome 1 is 455 = `alive["ALL-NEW"]` — a
   different quantity from the predicate that sets `reached`.
2. `G-OUTCOME-FEASIBILITY`'s substantive conditions are cardinality identities:
   `outcomes[0].witness == n_groups` and `outcomes[1].witness == span`; the second
   holds only because `span - span_alive["NONE"] == span - 0`. No ruling is bound
   to its evidence — the DISC K2 M8 shape.
3. The head has **four** segments and only **one** of them has a pre-registered
   outcome pair. AN-EVOLUTION-LAW (segment 2's alternative),
   MOTIVATION-AND-LAWFULNESS-OVERLAP (segment 3's) and A-PRINCIPLE-DERIVED
   (segment 4's) are pre-registered nowhere, and the 108 is pre-registered at no
   threshold.

**Licensed repair.** Replace outcome 1's predicate with a contentful one that was
available and could have failed: `n_form_cross > 0` — some event at which the rule
*creates a cross cell* is lawful at every leg. Add the three missing outcome pairs
with their own predicates (`1 in mult` for AN-EVOLUTION-LAW; `overlap > 0` for the
overlap word; `any(r["selects before"] == 1 and r derived)` for the derivation
word) and argue each one's feasibility against the committed corpus at the
declared row list, per #299-extended.

---

### M7 — THE WINDOW'S ADVERTISED SIZE IS FOUR TIMES ITS MEASURED CONTENT, AND THE INERTNESS IS BOTH PROVABLE AND UNDISCLOSED.

The head opens:

> FOUR LINK-CREATION RULES BY TWO READINGS BY TWO COUNT LEGS AT EVERY ONE OF 455
> THREE-ACTOR EVENTS AND AT BOTH ENDS OF EVERY TRANSITION

**Establishing measurement.** I ran the weld at all 4 × 2 × 2 = 16 cells at every
one of the 455 events. Every one of the 16 cells returns **identical** counts:

| rule | alive (all four cells) | seam-spanning alive (all four cells) |
|---|---|---|
| NONE | 54 | 0 |
| CROSS-ONLY | 270 | 216 |
| WITHIN-ONLY | 167 | 0 |
| ALL-NEW | 455 | 288 |

Further: only **two of the weld's four fates ever occur** across all 16 cells —
ALIVE and STRUCT-DEAD. `COUNT-DEAD` never fires anywhere, and it cannot: under
EMBEDDING the isomorphism forces every geometry cell to carry an incidence, and
every cell the rules create is a pair the event has just deposited, so the field's
minimum is never below 1. The READING and COUNT LEG axes are **inert by
construction** in this window. Sixteen cells, four distinct results.

This is not a defect of the measurement — it is the largest invariance the unit
found, and it is missing from §8's invariance list, from §10's choice inventory
commentary, and from the head. It is also **exactly what W3 asks for**: two of the
window's still-free fibres are measured not to move the verdict.

**Licensed replacement (head segment 4 opening, and a new §8 paragraph):**

> FOUR LINK-CREATION RULES AT EVERY ONE OF 455 THREE-ACTOR EVENTS AND AT BOTH
> ENDS OF EVERY TRANSITION, RUN AT EVERY ONE OF THE WINDOW'S 16 CELLS AND
> MEASURED INERT ON TWO OF ITS AXES: THE READING AND THE COUNT LEG MOVE NO
> NUMBER — ONE OF THE WELD'S FOUR FATES NEVER FIRES AT ALL — SO THE WINDOW'S 16
> CELLS CARRY 4 DISTINCT RESULTS

---

### M8 — THE COMPARATOR READS SEALED SUMMARY SCALARS, CONTRADICTING §13 AND ITS OWN DOCSTRING (family S-1).

§13:

> it reads only the receipt's per-object censuses, joins them on the event
> profile, re-derives every number in the head by its own arithmetic

and `reconstruct`'s docstring: "nothing it needs is taken from a summary key."

**Establishing measurement.** Eight of the head's numbers are re-typed from
sealed summary scalars, not re-derived:

| head number | source in `reconstruct` | kind |
|---|---|---|
| 29791 | `prep["states"]` | sealed scalar |
| 9 | `prep["best"]` | sealed scalar |
| 31 | `seam["lattice"]` | sealed scalar |
| 8 (the cut) | `R["cut_size"]` | sealed scalar |
| 0 (realises) | `R["ds_realises"]` | sealed scalar |
| 0 (survives) | `R["ds_in_cut"]` | sealed scalar |
| 1 (two-sided) | `R["two_sided_values"]` | sealed scalar |
| 1 (one-sided) | `R["one_sided_matches"]` | sealed scalar |

That is the entire numeric content of head segment 3 apart from the row count and
the post-event 4, plus two of the three preparedness numbers in segment 2. The
counts the comparator *does* re-derive — events, spanning, alive_all, parent,
full_cross, frozen, unique, reads, motivated, lawful, overlap — are genuinely
independent, joined on the profile from `incidence_census`, `form_census`,
`inventory_census` and `fourth_direction`; that half of the claim is true and is
good work. The claim as written is not.

**Compounding:** no falsifier moves a summary scalar between sealing and the
comparator. MUT-EXTREMAL, MUT-ONESIDED and MUT-PREP all move the *upstream*
computation, so builder and comparator move together and the comparator cannot
part from the head. The leg is untested exactly where it is weakest.

**Licensed replacement (§13):**

> The head is rebuilt by a routine that shares no function, no template and no
> literal with the builder. Eleven of the head's numbers it re-derives from the
> receipt's per-object censuses alone, joining them on the event profile by its
> own arithmetic; eight — the state space, the best state's coverage, the
> lattice, the cut, the two direct-sum counts and the two price counts — it reads
> from sealed scalars and re-types, and those eight are covered by their own
> gates and not by this comparator.

Add a falsifier that edits one of the eight scalars after its seal and requires
death at `G-INTEGRITY` (the mechanism already exists as MUT-PROMOTE — declare it
against a scalar the comparator re-types).

---

### M9 — §4.4's BEST STATE IS AN ARGMAX REPRESENTATIVE, IN A UNIT WHOSE METHOD SENTENCE IS "NEVER BY ORBIT".

> The best state the census exhibits carries the same completion at all three
> seams.

**Establishing measurement.** The paper's own table says 8 states absorb 9
crossings. I enumerated them:

| the 8 states attaining 9 | |
|---|---|
| carry the same completion at all three seams | **2** |
| mix the two completions (1,−1,0,1) and (1,0,−1,1) across seams | **6** |

The receipt publishes a single `best_state`, and the code selects it with a
strict-greater argmax (`if n > bestn`), i.e. the first state reached in the
iteration order over the 31³ lattice. §4.1 opens with "nothing here is taken at an
orbit representative: all 455 events are censused object by object" — and §4.4
then publishes a property of one representative as a property of "the best state".

**A stronger true fact the census already contains and does not state:** of the
108 lawful crossings, **81 can be absorbed by no advance state whatsoever** —
their successor set at the doubled seam contains no member of the pre-event
lattice. Only 27 are absorbable at all, and the best state gets 9 of those 27.
That sharpens §6.1 leg 2 considerably.

**Licensed replacement (§4.4):**

> The best states are eight, and they are ready for 9 of the 108. Two of the
> eight carry the same completion at all three seams; the other six mix the two
> completions the census exhibits. And the shortfall is sharper than the ratio
> suggests: 81 of the 108 lawful crossings can be absorbed by no state declared
> in advance at all, because the doubling their footprint carries moves the
> seam's own counts out of the pre-event lattice; only 27 are absorbable by any
> state, and the best state takes 9 of those 27.

---

## MINORS

**m1 — §1 mis-states its own anchor's ratio.** "of the seam-spanning conflict
groups the arena admits, a little under half leave the dictionary alive". The
anchor the instrument parses out of `v14/paper-40-sec2.md` reads 216 of 288 =
three quarters. 216 is a little under half of **455** — the wrong denominator.
Word-fractions carry no numerals, so neither paper gate can see it. Licensed:
"three of every four leave the dictionary alive".

**m2 — the Q34 seed is addressed to one successor, at the pre-amendment number.**
§7: "the successor unit, DYNAMICS-CLOSURE at paper-46" and "handed … to
DYNAMICS-CLOSURE". Per #17 (adopted, PLAN.md amended) paper-46 is
EVENT-AND-CARRIER-CLOSURE and DYNAMICS-CLOSURE is paper-48. LOG #20 records the
seed as "handed to ECC/DC"; the paper hands it to DC alone and never names ECC —
although §7's own clause "the cell carrying no direction label of its own" is
precisely ECC's chartered problem (#17: "AUTOGLUE creates DIRECTION-LESS cells the
quantum carrier cannot host"). Fix the numbering and route the direction-less-cell
clause explicitly to ECC.

**m3 — the STATE axis is declared "not posed" and is verdict-bearing.** §2.2's
window table gives `STATE | FROZEN, RE-SOLVED | not posed`. §4.2 and §7 measure
*both* values (162 under re-solved, 9691/29791 under persisting) and head segment
2 ends on it: "SO A STATE RESTRICTS THE ALLOWED TRANSITIONS ONLY IF IT PERSISTS".
#17 routed "persistent-vs-resolved verdict-bearing" to this unit. §10's choice
inventory names two verdict-bearing declarations and this is a third. Move the row
to "both run; verdict-bearing" and add it to §10.

**m4 — the memoryless half of the totality is a re-label, not a measurement.**
`memoryless = len(full_alive["CROSS-ONLY"])`. The paper states it correctly as a
consequence ("the successor set is a function of the post-record, so…"); LOG #20's
"totality measured both readings" overstates it. It is true by the definition of
`successors`, which takes only the post-record. Say "by construction" in the seed
section.

**m5 — `G-RULE-BLIND` is a tautology at the delivered rules.** `anon` is the
footprint with the `pairs` key removed; the four delivered rule bodies read only
`cross` and `within`, so `apply_rule(nm, anon)` and `apply_rule(nm, fp)` evaluate
identical expressions on identical objects at all 455 events. What the leg proves
is "no rule reads the `pairs` key" — real, and it is what kills MUT-RULE-BLIND —
but §3's "recomputed from a footprint stripped of every name of the event that
made it" claims much more: the footprint's `cross`/`within`/`doubled` entries *are*
named actor pairs and are handed over unchanged. The name-blindness claim is
carried by the *equivariance* leg, not this one. Re-word §3.

**m6 — `G-RULE-EQUIVARIANT` binds "index two" to an expression that computes no
index.** The gate condition is
`len(KINDPERM) * len(RULE_ORDER) // len(READINGS) == len(perms)` — 31104 × 4 ÷ 2 =
62208. It passes by a coincidence of unrelated cardinalities: change the number of
creation rules and the group theory is unchanged but the gate fails. The sentence
it guards says "the chart-preserving ones sit at index two". Write
`len(perms) == 2 * len(KINDPERM)`.

**m7 — "the sector exchange and nothing else" describes 31,104 relabellings.**
§3 and the gate sentence. The chart-preserving subgroup has index two (verified:
62208 / 31104), so the *coset* is generated by the sector exchange; there is not a
single such relabelling. Also, the sector-mixing sweep is itself a 64-element
stride sample of the 31,104, and §12's deviation 5 discloses only the
chart-preserving sample. Disclose both.

**m8 — the price table's "objects the route declares: 1" is bound to a different
predicate.** `n_laws = len({frozenset(full_alive[nm]) for nm in ("CROSS-ONLY",
"ALL-NEW")})` measures how many *distinct lawful-event sets the two creating rules
produce* — the §3 equivalence — not how many objects the route declares. Had the
two rules disagreed, the row would read "this one declares 2 laws". `G-PRICE`
hard-requires `distinct_laws == 1`, so the cell is the equivalence measurement
wearing the declaration count's label. Split the row.

**m9 — §3's "the head's number belongs to an equivalence class of rules and not to
CROSS-ONLY" is false if read of the number §3 has just put in bold.** The
paragraph is preceded by "**CROSS-ONLY reaches 216 of 288.**" The 216 is
CROSS-ONLY's alone — at the incidence leg ALL-NEW reaches 288 (measured). The
equivalence holds only after the form leg, i.e. of the 162/108. Licensed: "the
head's 108 belongs to an equivalence class of rules; its 216 does not — at the
incidence leg ALL-NEW reaches 288, and the two rules coincide only once the form
leg binds."

**m10 — a carried-not-used declared value.** `DECL["sec2.declaration_price_per_seam"]
= 4` is declared and consumed nowhere (all 16 other DECL keys are consumed;
`sec2.lawful_at_matched` is bound by a clean triple equality, computed == parsed
from the anchor == declared). The v15 standards forbid carried-not-used families;
no gate iterates DECL to require consumption. Either bind it against the measured
kernel or remove it.

**m11 — `G-NO-POSTHOC` does not cover the function that computes the rule's
output.** The scan pattern is `def (rule_\w+)\(fp\):`, which finds the four rule
bodies but not `apply_rule`, the dispatcher that actually produces the cells and
that carries the mutant special-cases (including one keyed on a named actor).
Inert in delivery; a coverage hole of the argv[1]-only shape. Extend the pattern.

**m12 — the second step's lawfulness predicate omits the fourth-direction leg.**
§4.5's `form_lawful` column is `all(s > 0 for s in sizes)` only, while the first
step's is that *and* "creates no within cell". I tested whether it bites: of the
25 second-step crossings, **0 open a within pair**, so the two standards coincide
on the surviving set and the 108 → 25 comparison is sound. Nothing gates that
coincidence. Add the leg, or state the measurement.

**m13 — §14 S-1's provenance sentence.** "Its arity was not available before this
unit closed." ARITY (paper-44) was delivered and committed at LOG #13, before this
unit's #20, and its lead result *is* the a=2 obstruction-vanishing theorem. What is
true is that this instrument does not read it (its read set is six sources, none of
them ARITY) and derives the mechanism independently — which is better than
unavailability. Re-word.

**m14 — the #17 note "a=2 removes the obstruction, NOT seam underdetermination" is
half-folded.** S-1 scopes §5.1's disjointness theorem to a=3, correctly. It does
not say that §6.1's legs 1 and 2 — the never-unique successor and the
no-advance-state result, which turn on the seam system's kernel and not on the
event size at all — are untouched by a. A reader of S-1 may take a=2 to dissolve
the unit's headline obstruction. Add the sentence.

**m15 — §6's "Reverse the convention and it becomes another direction's cut" is
true and ungated.** I measured it: the minimum one-sided price's minimiser equals
the cut of index (2,2,+1); the maximum one-sided price's equals the cut of
(2,2,−1); and the minimiser under the opposite sign convention equals (2,2,−1)
too. Bind it — `matches` is currently computed for the minimiser only.

**m16 — the two coincident 108s.** §4.1 uses "the 108" for the surviving [1,0,2]
crossings and "The 108 with two crossings" for the [2,0,1] events. Both are 108,
both live in the referent gate's "events" universe, so a swap would not be caught.
The prose distinguishes them correctly; a reader may not. Consider naming them.

---

## WHAT STANDS — CREDITS

These are not courtesies; each is a leg I attacked and could not move.

1. **Every delivered physics number.** 232 quantity checks against my own
   construction, zero disagreements. The 26-row form census reproduces *verbatim*,
   including the 103/49/31 successor sizes and both boolean columns; the
   preparedness histogram reproduces exactly (20100, 6804, 2034, 622, 153, 18, 52,
   8); the fourth-direction table reproduces exactly (360/36/36/36, 0 agreements);
   the whole-state fiber (256 at 81, 512 at 27) and the 324 seam slots (81 fours,
   243 eights) reproduce; **no successor set anywhere in the census takes the size
   1** — the sizes that occur across all 455 events × 3 seams are {0, 4, 8, 31, 49,
   103}, so head segment 2's "UNIQUE AT 0" is exact and exhaustive.

2. **The mechanism sentences are true at every object, and I checked them
   adversarially.** "27 of the 108 need no state move, which is the case where no
   doubling lands on a seam's own forward cell" — an exact biconditional, 27/27 and
   81/81. "[2,0,1] events die at exactly one seam, and which one depends on where
   the actors sit" — exact, and the three failure patterns all occur. The Q50
   chain's four measurable links each hold at all 455.

3. **The count-datum order (#5.6) is folded cleanly.** "metric datum" occurs
   nowhere; "count datum" carries §6.1; "no metrical reading of it is taken here"
   closes the sentence; §8 carries the standing paragraph; WALL-NO-METRICAL-READING
   has a licence leg. Only the paraphrase hole of M5 remains.

4. **The whole-state fiber referent (#11) is clean at every use.** I checked all of
   them: §4.2's table, §7's successor relation, §10's inventory row all say per-seam
   4-or-8 and whole-state 256/512, and the one place an 8 appears alone (§5's price
   row) is labelled "completions **the seam** may carry". No per-seam-8-as-global
   anywhere.

5. **The equivalence class is a set equality, not a cardinality coincidence.** I
   verified `full_alive["CROSS-ONLY"] == full_alive["ALL-NEW"]` as *sets*, not just
   162 == 162. The head carries the equivalence and does not celebrate a member.

6. **The provenance is sound.** All six pinned source shas verify; all nine
   verbatim anchors locate exactly once under the #125 normaliser; 16 of 17 declared
   parent values are consumed, and the 216 is bound by a triple equality —
   computed == parsed out of the parent's own sentence == declared.

7. **E-24 is satisfied properly.** §9 stamps every ratio COUNTING-ONLY with the
   denominators beside them, the receipt carries the stamp, WALL-COUNTING-ONLY
   requires the words and kills all five of its controls, and §9 volunteers the
   fact that would be abused ("the event profiles differ in size by more than an
   order of magnitude").

8. **The numeral coverage leg is the best in v15 so far.** 755 numerals scanned
   including inline spans and table cells, spelled numerals *generated* rather than
   whitelisted, exemptions required to occur. This closes CONTRACT K3's "92 spelled
   numerals ungated". M1 escapes it only because a spelled *fraction* with a stated
   denominator is a different object from a spelled numeral.

9. **The referent gate is 17× better bound than DISC's.** 18 pairs bound over 234
   prose numerals (15.4%, against DISC's 0.89%), four declared universes, and I
   checked every prose occurrence of 455/288/270/216/185/162/108/77/324/29791
   by hand: **every one is attached to a correct denominator.** The only referent
   error I found is m1, which carries no numeral at all.

10. **§12's deviations are real deviations, priced.** The one arena, the
    un-re-driven grammar, the declared second-step rule, the readout carried to
    undeclared cells, the equivariance sample, the single first event. Nothing there
    is decoration.

---

## THE MANDATE, LEG BY LEG

**(1) The ordered items, verified at every use.**

| order | source | status |
|---|---|---|
| event-conditioned, never autonomous | #11 | **head FOLDED** (4 occurrences, all negations; §1, §7 explicit). No wall → M5. |
| the window in-head | pin, #11 | FOLDED — head segment 4 opens with the window; but see M7 on its advertised size. |
| transition-relation, not evolution law | #5.3 | **FOLDED** — head, §4.2, §7, WALL-RELATION-NOT-LAW, G-TRANSITION-RELATION; and the measurement is exhaustive (never 1-valued anywhere). |
| SEAM-SUBSYSTEM state naming; amplitude/branch/coin/emission named absent | #11 | FOLDED, 6 uses; the absent components named once (§7). No wall → M5 (K2-STATE-E). |
| whole-state fiber referents 256/512 | #11 | **FOLDED, clean at every use** (credit 4). |
| the equivalence-class sentence, no CROSS-ONLY favouritism | #11 | FOLDED in the head; §3's own sentence is ambiguous → m9. |
| "count datum" not "metric datum" | #5.6 | **FOLDED** (credit 3). |
| the a=2 successor registration with its mechanism | #5.4 | FOLDED — S-1 carries the mechanism (an event *is* a pair at a=2; doubling-nothing without a within pair becomes possible). Two wordings need repair → m13, m14. |
| the Q34 seed addressed to ECC/DC, not EXCITATION | #17 | **PARTIAL** — EXCITATION correctly absent; DC named at the *pre-amendment* number; ECC never named → m2. |
| W1 — reconstruction never derivation | #2 | FOLDED — wall present with licence leg, 5/5 controls die; §8 carries it. But M4 is a W1-shaped attribution gap the wall cannot see. |
| W2 — invariance never gauge/meaning | #2 | **NOT GATED** → M5. |
| W3 — family-level predictions only | #17 | **NOT CARRIED AT ALL** → M5; M7 supplies the measurement that would discharge it. |
| possibilities-not-probabilities; uniform weighting a declaration | #17 | FOLDED (§9 + wall + E-24). |
| persistent-vs-resolved verdict-bearing | #17 | folded in prose; contradicted by the window table → m3. |

**(2) The wall sweeps.** The unit's 30/30 confirmed from the receipt. My six
plants: 0 of 36 wall scans fired, six of six deliveries passed all 42 gates. See
M5.

**(3) The obstruction theorem, Q50, the derivation attempt.**
- The theorem's *licensed form* — "the datum that cannot precede the event is the
  seam's cross block" — is earned by legs 1 and 2 (both exhaustive, both
  reproduced exactly) and by leg 3 with the scope word repaired (M2). Scope is
  carried: §7's "THE SCOPE, GATED" names the arena, the arity, the field order,
  the link directions and the dimension, and §14's "What may not be inherited"
  is a genuine standing row.
- **Q50's chain, link by link:** link 1 (forced ⟺ no doubling) measured at all
  455 and reproduced; link 2 (no doubling ⟺ three new pairs) measured at all 455,
  reproduced; link 3 (three new ⟹ a within pair) measured at all 455, reproduced;
  link 4 (at most two cross) measured at all 455 *and* proved; link 5 (the form
  refuses that cell) measured at 468/468. **Every link is measured or proved.**
  The provenance of links 1–4, however, is the parent's → M4.
- **The derivation attempt's honesty.** Max-det's self-defeat is rendered
  *fairly in substance* — the criterion is exhibited selecting a state that
  realises none of the arena's crossings, and the paper says plainly that nothing
  is derived and that maximum determinant "has lost even the claim to be the
  natural candidate". Two blemishes: the self-defeat is one fact counted twice
  (M3), and the count of selective functionals is wrong (M1). The mirror
  observation the paper makes about the one-sided price — "the event's own
  equation wearing a criterion's clothes" — is not made about max-det, where it
  would also apply: the direct sum realises no crossing because a crossing needs a
  non-zero cross entry, which is the direct sum's definition. Worth a sentence.

**(4) Referents, E-24, #299-extended.** Referents: credit 9 and m1. E-24:
credit 7. #299-extended: M6 — **NO-RULE-EXISTS was not reachable**, the predicate
that certifies the positive word contains no creation, and three of four head
segments have no pre-registered outcome at all.

---

## ORDERS, CONSOLIDATED

**Numbers that must move:** none of the physics. §6's "Three" → the measured
counts (M1). §4.4's best state → eight, two diagonal, and the 81-of-108 sharpening
(M9). Head segment 3's eight numbers → seven, with the identity stated (M3).

**Head strings that must move:** segment 2's "AN UNSHARED SITE'S FORM" → "A
SECTOR'S OWN FORM AT A SITE", with the 312/156 split published (M2). Segment 3's
double count (M3). Segment 4's window opening → the inertness disclosure (M7).

**Prose that must move:** §1's fraction (m1); §5.1's attribution (M4); §13's
comparator claim (M8); §3's blindness sentence (m5) and its equivalence sentence
(m9); §7's successor-unit numbering (m2); §14's S-1 provenance (m13) and the
a-independence sentence (m14).

**Instrument:** the three new walls and the widened metric wall with my six plants
as controls (M5); the feasibility predicate and the three missing outcome pairs
(M6); the summary-scalar falsifier (M8); the index-two condition (m6); the
spelled-`N of M` referent leg (M1); the price row split (m8); the `apply_rule`
scan (m11); the second-step fourth leg (m12); the reversal binding (m15); the dead
DECL key (m10).

---

## FOR THE ADJUDICATOR

The panel will want to weigh M2 against a reject lever. My reading is that it does
not reach one: the arithmetic is right at all 468 readings, no number moves, the
mechanism is sound at both kinds of site, and the repair *strengthens* the leg.
But it is a head string that is false at a third of its own witnesses, and it is
false about the exact object — the seam's kernel-four form — that the unit was
pinned to. If the panel disagrees with me, that is where the lever is.

M4 is the finding I expect the other seats to weigh most heavily, and it has the
DISC precedent behind it (#8: "the null is provably identical to paper-20's
frozen-stage control — undisclosed"). The parent supplies the triangle step, the
doubling↔free-items step, and the same object-by-object check at the same 455.
What AUTOGLUE adds is real and should be stated *as* the addition: the two-sided
form, and the closing link to the form's refusal. Re-attributed, the segment gets
smaller and much harder to argue with.

M6 and M5 are the two that generalise past this unit. M6 says the #299 machinery
in v15 is being satisfied by predicates that cannot fail; M5 says two of the three
standing walls have never been given a gate in any wave-1 unit. Both belong in the
wave-2 charter, not only in this repair.

Recomputation count: **436**, plus six full deliveries under planted paraphrases.
Object hashes re-verified at close: `paper-45-autoglue.md 09ee568a4bba`,
`autoglue_exact.py d750a64f153c`, `autoglue_output.txt 1a287adcaead`,
`autoglue_receipt.json 34606049f1ef`, `note-autoglue-pin.md 88c04df52c19` — all
five unmoved. No repo file other than this review was written; no process other
than my own was signalled.
