# ARITY (paper-44) — K2 EFFECTUS review

**Seat:** K2 (EFFECTUS), three-seat hostile panel, v15 wave 1.
**Object:** `v15/paper-44-arity.md` `177560920b33` · `v15/code/arity_exact.py`
`d0044766fcd8` · `v15/code/arity_output.txt` `95414a8d2824` ·
`v15/code/arity_receipt.json` `c1354b632733` · pin `v15/note-arity-pin.md`
`89b35dad3219`. **All five re-verified at open and at close; none moved.**
**Authority:** the pin; `v15/PLAN.md` W1/W2/W3; `v15/LOG.md` #2/#5/#11 (the
ordered renames); RUNBOOK through E-33.
**Status:** CANDIDATE UNTIL ADJUDICATION.

---

## GRADE

**ACCEPT-WITH-MAJOR-REPAIRS. No reject lever.**

**444 recomputations across 11 full deliveries. Not one delivered physics number
is wrong.** I rebuilt the packing substrate at all four arities from a closed
form and from a from-scratch enumerator, the complete actor lattice, the leg-1
survivors and their set-equality with the coset partitions, the attained
information floor over every complete event universe, the offsets, the ladder
moduli, the principle census, the division-forcing identities and — with a
second hand, independently — the whole SEC-2 union census including the
`0|1|2|4` obstruction column that is this unit's lead. Every one agrees. The
eight verbatim anchors are byte-exact in their pinned sources and every one is
genuinely consumed. The three renames ordered at #11 are folded **verbatim and
at every use**: the conditional selection form stands word-for-word in the head
and in §10; `coset_events` is gone from the unit entirely and its replacement
carries the honest weaker-predicate sentence; the n=16 successor is
pre-registered F₄-LINEAR with the correct mathematics for why the abstract
subgroup reading cannot separate anything there. The extension-scope discipline
ordered at #5 is carried **structurally, not in prose**: all thirteen per-law
rows carry the scope word in the row, the packing rule and the extension family
are two separate declared rows of the arena table, and one sweep over all 191
prose sentences finds **no sentence that reads an a=2|4|5 result as a fact about
committed ISP**. The two-level engraving is intact and the banned universal
("nothing is carried by event size") appears nowhere; the head says
`WHAT NO PARENT NUMERAL FOLLOWS IS THE DECLARED UNIFORM a-ONLY RULE`, which is
exactly the ordered form.

This is the most disciplined paper of wave 1 on the licensure axis. Its repairs
are seven, and three of them are serious. One is **a head numeral bound to the
wrong noun** — the unit's own sealed receipt publishes the column the sentence
names with a value of 2 while the sentence says 0. One is **the wall set: none
of the six paraphrases I wrote survives contact with the disease, and all six
ship at exit 0 through full delivery** — and four of the unit's own six plants
die too once a single bare negation is added. One is **a numeral-scanner blind
spot that lets a false number stand in the LEAD's own sentence at 34/34 gates
green**. The remaining four are disclosure debts, and one of them moves the
headline aggregate under a rule the unit itself derives two sections earlier.

I also hand the unit **one strengthening it did not claim**: its §7 mechanism is
not a one-arena measurement but a two-line theorem, arena-independent.

---

## WHAT HOLDS (established, not asserted)

| leg | my route | result |
|---|---|---|
| substrate 945\|280\|315\|126 | from-scratch enumerator **and** closed forms `9!/(2⁴·4!)`, `9!/(6³·3!)`, `C(9,4)C(5,4)/2`, `C(9,5)` | exact, both routes |
| idle 1\|0\|1\|4, max weight 4\|9\|10\|8, both saturation columns, weight sets | independent `round_vec` over the 27 cells | exact, 28 cells |
| actor lattice 21147 | Bell(9) by direct set-partition enumeration | exact |
| menu: 6 survivors, 6 coset partitions, **set equality** | independent descent test + independent subgroup/coset construction | exact; set-equality TRUE |
| attained floor 6\|4\|4\|4 | brute-force minimum separating signature over `C(9,a)` events | exact, all four |
| offsets 0\|1\|1\|0 | by subtraction from the delivered pair | exact |
| ladder modulus `27/gcd(27,mass)`, mod-a never appears | independent gcd arithmetic on all 8 rows | exact, 8/8 |
| SEC-2 census: 105\|455\|1365\|3003, seam-spanning 36\|288\|1113\|2751, 36\|216\|405\|243, 36\|0\|0\|0, max cut ⌊a²/4⌋, forced-inside `C(a,2)−⌊a²/4⌋` | second hand rebuilt the union from the arrangement alone (15 carriers, 54 realised pairs, 0 doubled); **I additionally recovered the sector geometry from the delivered counts alone** — two 9-carrier sectors sharing a 3-carrier seam, `C(15,a) − (2C(9,a) − C(3,a))` — and it reproduces all four seam-spanning counts | exact, every cell |
| principle census, `round_completeness` = divisors of 9, `subgroup_order_available` = subgroup orders {1,3,9} | independent divisor and subgroup enumeration | exact; the two coincide, as §10 says |
| division-forcing 945\|276\|315\|126, non-unique = rounds that are parallel classes | independent identity checks per arity | exact |
| 8 verbatim anchors | re-located byte-exact in the pinned sources under the declared normalizer, uniqueness checked | 8/8, all consumed |
| the SEC-2 free-item attribution | SEC-2's own zero-free-item control row located and quoted; the referent of "no three-actor group can produce it" is unambiguously that row | **attribution exact** |
| naming window-leg bookkeeping | `204 × 19778 = 4,034,712 = 52,778 + 3,981,934` | exact identity |
| byte-reproduction | delivery re-run from an alien CWD in a fresh mirror | receipt `c1354b632733`, transcript `95414a8d2824` — **byte-identical** |

**On the a=2 lead's licensure specifically.** The claim is scoped correctly. The
union is rebuilt from the arrangement and its parent-arity census is reproduced
object by object before the a=2 row is read; the free-item counts are *not*
recomputed and the paper says so three times (§9, §12, the SCOPE line); §13
explicitly refuses to say what replaces the obstruction. SEC-2's own §12 carries
a standing "may not be inherited" row that names *the crossing-alone control as
a driven object*, and §9's closing sentence ("the arity at which a driven event
can realise the row it could only construct by hand") comes closer to that line
than anything else in the paper — but it does not cross it, because the price is
never imported and the head never calls the a=2 event motivated. **The
attribution is clean.** A one-sentence acknowledgement of SEC-2's standing row
would close the gap entirely, and AUTOGLUE consumes this row, so I ask for it as
a minor rather than leaving it to be discovered downstream.

---

## MAJORS

### M1 — A HEAD NUMERAL IS BOUND TO THE WRONG NOUN; THE UNIT'S OWN RECEIPT CONTRADICTS THE SENTENCE

The paper (§7, bolded), the head (segment 5) and the gate statement all carry:

> **The number of rows whose first rung is the event size is 0.**

The receipt publishes a column with exactly that name. It reads **TRUE at 2 of
the 8 rows**:

| a | reading | achievable | `first_rung_is_the_event_size` | `mod_a_appears` |
|---|---|---|---|---|
| 3 | LITERAL | [3, 6, 9] | **true** | false |
| 3 | MAXIMAL | [3, 6, 9] | **true** | false |

`arity_exact.py:2548` defines `first_rung_is_the_event_size = min(ach) == a`
(2 rows true). `:2554` defines `mod_a_appears = min(ach) == a and a != A.L`
(0 rows true). The published sentence at `:2573` and the registered prose claim
at `:3633` both take their **number from `mod_a_appears`** while their **noun
names `first_rung_is_the_event_size`**. A reader with the receipt open finds the
named column true twice and a claim that it is true zero times.

The *reasoning* is right and is disclosed in the adjacent sentence — at the
parent's arity `a = L = 3`, so that row is a coincidence and only `a ≠ L` rows
discriminate — and the code's own inline comment says so. The **discriminating**
count is 0 and my independent gcd rebuild confirms it. Nothing in the physics
moves. But the sentence as written is false against the unit's own seal, in the
head, and it is one of only six prose claims the claims gate binds. This is the
E-30 disease at the highest-stakes position: **referent binding per occurrence.**

**Establishing measurement:** 8 ladder rows, both columns, recomputed;
`sum(first_rung_is_the_event_size) = 2`, `sum(mod_a_appears) = 0`.

**LICENSED SENTENCE (repair — use one, in §7, in the head, and at
`arity_exact.py:2573`/`:3633`):**

> The number of rows at which the event size and the declared link count differ
> and the first rung is nonetheless the event size is 0.

or, equivalently and shorter:

> mod-a appears in 0 of the 8 rows: at the parent's arity the first rung is the
> event size, but there the event size and the link count are the same number,
> and only a row at which they differ can discriminate.

### M2 — THE WALLS DO NOT STAND: 0 OF 6 FRESH PARAPHRASES CAUGHT, ALL SIX SHIP AT EXIT 0 THROUGH FULL DELIVERY

The unit's `G-WALL-PARAPHRASE` statement says: *"A wall that only matches the
sentences its own patterns were written from is not a wall."* The unit's own six
plants are caught, and honourably — five by the **licence leg**, not by a
hard-coded pattern. That is real work and it is the CONTRACT-K2 fix applied.

I wrote six fresh ones against the disease, two per wall family, reusing no
wall's wording. **Not one is caught by any of the seven walls, and all six pass
the entire 34-gate delivery at exit 0** with the plant present in the scanned
text and `unlicensed_sentences: []` on every wall:

| plant | flavour | sentence | caught | full delivery |
|---|---|---|---|---|
| K2-S1 | selection | "Only one block size lets every actor take part in every round, and that is the size the corpus already uses." | **none** | exit 0, 34 PASS |
| K2-S2 | selection | "The event size at which the grammar closes is three, and the arena admits no rival." | **none** | exit 0, 34 PASS |
| K2-G1 | gauge | "A count that reads the same at every event size is descriptive bookkeeping, carrying no content an experiment could reach." | **none** | exit 0, 34 PASS |
| K2-G2 | gauge | "The admissible-partition count is inert under the sweep, so it belongs to the description and not to the world." | **none** | exit 0, 34 PASS |
| K2-F1 | family-prediction | "Across the ISP family the ladder's first rung is always the number of declared link directions." | **none** | exit 0, 34 PASS |
| K2-F2 | family-prediction | "Any ISP model whatever gives one redundant event between the schedule and the floor precisely when the event size divides the actor count." | **none** | exit 0, 34 PASS |

**The mechanism, measured.** The licence leg fires only when a sentence matches
one of the wall's `subject` regexes *and* carries a `policed` form within 80
characters. Both lists are narrow keyword lists, so **any synonym walks
through**: `W-NO-SELECTION`'s subject list is
`arity|arities|event size|three actors|three-actor` — "block size" is not in it
(K2-S1); its policed list has sixteen verbs and "admits no rival" is none of
them (K2-S2). `W-NO-INVARIANCE-AS-GAUGE`'s subject list has `a-inert` but not
bare `inert` (K2-G2) and nothing matching "reads the same at every event size"
(K2-G1). The licence leg converted a pattern list into a claim detector; it did
not make the claim detector general.

**The bare-negation recurrence (AUTOGLUE K3 major 3, confirmed here).**
`W-NO-SELECTION` licences include `\bnot\b`, `\bnever\b`, `\bif\b`, `\bwould\b`,
`\bthen\b`; `W-NO-RECONSTRUCTION-AS-DERIVATION` licences `\bnot\b`, `\bnever\b`;
`W-EXTENSION-SCOPE` licences `\bnot\b`, `\bnever\b`, `\bwould\b`. Appending one
inert clause containing a bare "not" to each of the unit's **own** six plants
kills **4 of 6**:

| unit plant + ", and this is not a small point." | caught before | caught after |
|---|---|---|
| P-NINE-LEAVES-NO-CHOICE | W-NO-SELECTION | **none** |
| P-ARENA-DEMANDS | W-NO-SELECTION | **none** |
| P-CONSTRUCTOR-SETTLES | W-NO-SELECTION | **none** |
| P-RECONSTRUCTION-ESTABLISHES | 2 walls | **none** |
| P-MENU-IS-PHYSICS | W-NO-INVARIANCE-AS-GAUGE | still caught |
| P-ISP-AT-TWO | W-EXTENSION-SCOPE | still caught |

The two survivors are instructive. `W-NO-INVARIANCE-AS-GAUGE` survives because
it is the **one wall whose author saw this coming** — `arity_exact.py:3503-3505`
carries the comment *"a bare negation is NOT a licence here"* and its licence set
is made of specific disclaimers. **The cure is already in the file; it was
applied to one wall of seven.** `P-ISP-AT-TWO` survives only via its hard-coded
negative pattern, i.e. exactly the failure mode the gate statement disclaims.

**Repair:** strip bare `\bnot\b` / `\bnever\b` / `\bif\b` / `\bwould\b` /
`\bthen\b` from every licence set and replace them with specific disclaimers, as
`W-NO-INVARIANCE-AS-GAUGE` already does; widen every `subject` list to the
semantic class (the object being talked about) rather than five spellings of it;
and re-run with all six plants above plus the six negated ones as permanent
falsifier rows. Twelve controls, all of which must die.

### M3 — THE NUMERAL SCANNER IS BLIND TO ANY NUMERAL FOLLOWED BY A PERIOD; THREE FALSE NUMBERS SHIP AT 34/34, ONE OF THEM IN THE LEAD'S OWN SENTENCE

Both scanners — `ReferentRegistry.NUM` (`:876`) and the coverage scan
(`:3338`) — use a trailing guard `(?![\w.])`. **A numeral immediately followed by
`.` is never scanned.** 19 numerals in the paper sit there; the declared section-
heading strip covers most, but four are load-bearing claims. I planted a
falsehood at each and ran the full delivery:

| # | sentence, corrupted | ships? |
|---|---|---|
| C1 | §3 "…the maximum, which is one higher than the budget, is reached by **999**." (truth: 162) | **exit 0, 34 PASS, `uncovered: []`** |
| C2 | §5 "…the offset is not constant: it reads 0, 1, 1, **7**." (truth: 0; a head numeral) | **exit 0, 34 PASS, `uncovered: []`** |
| C4 | §9 "That complement reads 0, 1, 2 and **8**." (truth: 4; **the lead's own closed form**) | **exit 0, 34 PASS, `uncovered: []`** |
| C3 | §7 "…first rung is the event size is **7**." | died at `G-PAPER-CLAIMS` |

Only C3 dies, and only because that sentence happens to be one of the six
registered prose claims. C4 is the sharpest: the paper's headline
generalisation of SEC-2's obstruction — the measurement AUTOGLUE is ordered to
consume — can be published with a false final value and every gate stays green.

This is the E-22 / #20 family recurring at the regex level: *coverage includes
inline spans and fenced blocks* was bought at v14 #187 precisely because a
scanner blind exactly where the highest-stakes numerals live is not a scanner.

**Repair:** change both guards to `(?![\w])` (or `(?!\d)` plus an explicit
decimal-point exclusion), keep the declared section-heading strip, and add a
falsifier that plants a corrupted sentence-final numeral and must die at
`G-PAPER-COVERAGE`.

### M4 — THE HEADLINE AGGREGATE IS RULE-RELATIVE, AND THE RULE THAT MOVES IT IS THE UNIT'S OWN §9 CLOSED FORM

The head's second segment returns `0 LAW-IN-A, 2 NEEDS-3 and 5 BREAKS`, with
`NO WORD MOVES UNDER EITHER DECLARED ALTERNATIVE a-ONLY RULE`. That sentence is
true, and the head is correctly scoped to *the declared* rule — the #11 rename
is properly folded. The problem is the **sensitivity leg's adequacy**.

The declared rule is `C(a,2) + v − 3`; the two alternatives are `a + v − 3` and
`blocks + v − 3` (`:342-347`). All three are simple monotone functions of `a`,
and under all three the seven words are identical. But §9 of this very paper
derives, measures and publishes an **exact closed form in `a`** for one of the
seven scored numerals: the pairs forced inside a sector are
`C(a,2) − ⌊a²/4⌋ = 0|1|2|4`, and §9 calls it *"generalises exactly"*. Declared as
a third uniform a-only rule, `C(a,2) − ⌊a²/4⌋ + v − 1`, and run through the
unit's own decision procedure:

| rule | menu | ladder | sched | floor | offset | naming | sec2 | aggregate |
|---|---|---|---|---|---|---|---|---|
| declared `C(a,2)+v−3` | NEEDS-3 | NEEDS-3 | BREAKS | BREAKS | BREAKS | BREAKS | BREAKS | **0 / 2 / 5** |
| alt-1 `a+v−3` | NEEDS-3 | NEEDS-3 | BREAKS | BREAKS | BREAKS | BREAKS | BREAKS | 0 / 2 / 5 |
| alt-2 `blocks+v−3` | NEEDS-3 | NEEDS-3 | BREAKS | BREAKS | BREAKS | BREAKS | BREAKS | 0 / 2 / 5 |
| **K2 third rule** | NEEDS-3 | NEEDS-3 | BREAKS | BREAKS | BREAKS | BREAKS | **LAW-IN-A** | **1 / 2 / 4** |

**Exactly one word moves, and it moves the headline from "0 LAW-IN-A" to
"1 LAW-IN-A".** The third rule is not a friendlier reading — the other six words
are untouched — it is a genuinely competitive uniform rule, and the unit
supplied it itself.

The head's mitigation clause names the *sharpened floor* as the thing that does
move with event size. But the sharpened floor is not one of the seven scored
numerals; the scored numeral that actually carries an exact arity law is scored
BREAKS with no cross-reference to §9.

**LICENSED SENTENCE (repair — §11, after the sensitivity paragraph):**

> The declared rule is a choice, and one of the seven numerals has an exact
> arity law that the declared rule does not happen to be: §9's pairs forced
> inside a sector follow `C(a,2) − ⌊a²/4⌋` at every arity measured. Run as a
> third uniform rule it returns LAW-IN-A on that row and leaves the other six
> words untouched, so the aggregate reads one LAW-IN-A rather than none. The
> declared aggregate is therefore a statement about the declared rule, which is
> why the head names it; what the sweep establishes without reference to any
> rule is that no parent numeral is constant across the arities and none follows
> the corpus's own co-division-pair count.

### M5 — W3 HAS NO WALL AT ALL, AND ONE SENTENCE IS EXPOSED TO IT

W3 was engraved at #17: *a result is an ISP-family prediction only if it holds
across every still-free fibre or the fibre is independently selected.* The unit
carries **seven** walls: `W-NO-SELECTION`, `W-NO-PROBABILITY`,
`W-NO-CARRIER-OVERREACH`, `W-NO-RECONSTRUCTION-AS-DERIVATION`,
`W-NO-INVARIANCE-AS-GAUGE`, `W-EXTENSION-SCOPE`, `W-NO-MOTIVATION-CLAIM`. **None
of them is about family-level prediction.** The words "fibre", "fiber",
"still-free" and "family-level" occur **0 times** in the paper. This is the
AUTOGLUE K2 M5 finding, reproduced here by the same probe.

**In fairness to the worker:** W3 was engraved at #17, *after* this unit's #13
delivery, and the only post-delivery change (#22/#24) was path hygiene. The
worker is not culpable; the repair is owed all the same.

**The exposure is real but small.** ARITY fixes `n = 9`, `q = 3`, `L = 3` at
every row, and all three are still-free declarations by CONTRACT's own census —
so every result here is a single point of a three-dimensional free fibre. One
sentence generalises past it (§7, last line):

> the event size is not a modulus of this theory

and the head carries `MOD-a NEVER APPEARS`. That is a claim about *the theory*
from one `(n, q, L)` point. **Fortunately it is discharged rather than repaired**
— see the bonus below, which turns it into a theorem for the budget reading.
Under the MAXIMAL reading it remains fibre-relative and should say so.

**Repair:** add a `W-NO-FAMILY-PREDICTION` wall with a licence leg (subject: the
theory / ISP / every arena / any model; policed: predicts, always, at every
arena, of this theory, in general; licences: at this arena, at the declared
arena, one point, fibre) plus the three family-flavoured controls above; and
scope §7's last sentence.

### M6 — THE PARENT'S CLOSED FORM IS FALSE AT 3 OF 4 ARITIES, AND THE SENTENCE THAT LEANS ON IT IS A NON-SEQUITUR

§5 quotes NDEP's `> it evaluates to 2(q - 1) at q = 2 through 7` and concludes:

> which returns the parent's own floor at the parent's arity, and is what makes
> the reading used here a transport of the parent's formula rather than a new
> one.

Measured against this unit's own floors:

| a | `2(a−1)` | measured floor |
|---|---|---|
| 2 | 2 | **6** ✗ |
| 3 | 4 | 4 ✓ |
| 4 | 6 | **4** ✗ |
| 5 | 8 | **4** ✗ |

The parent's published closed form **fails at three of four arities**, and the
paper does not report it. The gate is correspondingly hollow: `:2416` computes
`2 * (A.q - 1)` — using the **field order**, not the arity — and compares it to
the a=3 floor, the one arity at which every candidate reading agrees by
construction (the a-only rule is *defined* to reproduce the parent's numeral
there). The anchor does no discriminating work and the inference it supports
does not follow.

The unit's actual claim — that the **two-condition construction** reproduces the
measurement at 4 of 4 arities while the counting bound reproduces it at 3 — is
true, independently verified, and generous to the parent (NDEP left attainment
open and ARITY correctly says its evidence is not a proof). **A second scope
point the paper owes:** the whole event-size-vs-field-order separation for the
floor rests on the **single row a=2**; at a=3, 4 and 5 the two readings are
indistinguishable.

**LICENSED SENTENCE (repair — §5, replacing the quoted clause):**

> The parent also gave that formula a closed form on its own axis, where the
> arena moves along the diagonal `n = q²`. Off that diagonal the closed form does
> not transport — it returns 2, 4, 6 and 8 against measured floors of 6, 4, 4 and
> 4 — so what is transported here is the parent's two-condition construction and
> not its closed form; the closed form is cited only to fix which variable the
> construction's incidence term names. The two readings of that variable are
> separated by one row: at a=3, 4 and 5 the event-size and field-order readings
> agree, and only a=2 tells them apart.

### M7 — THE PRE-REGISTRATION IS SEALED AND NEVER CONSUMED; 3 OF 9 SEGMENTS ARE OUTCOME-LESS; ONE PRE-REGISTERED WORD NEVER FIRES

`PREREGISTERED` (`:283`) carries five outcome words with #299-extended
feasibility arguments — the best-formed pre-registration in wave 1. An AST scan
finds it **referenced at exactly two lines: its definition (283) and its seal
(2155)**. No gate consumes it. Nothing checks that the words the run emits are
drawn from it; nothing checks a head segment against it. That is E-28 ("an
anchor is consumed or it is decoration") applied to the pre-registration itself.

Three consequences, measured:

1. **`INFEASIBLE-CARRIED` occurs exactly once in the whole receipt — in its own
   declaration.** Its feasibility text says it applies when *"under the LITERAL
   saturation reading no grouping attains the budget at a = 2 or a = 5, so the
   ladder's rows there have no object to score."* **That situation occurred** —
   both rows carry `"feasible": false` — and the word was never emitted. A
   pre-registered outcome whose own stated trigger fired and which was never
   used is not pre-registration, it is a list.
2. **3 of the 9 delivered segments carry no pre-registered outcome word:**
   `ARITY-NO-PARENT-NUMERAL-FOLLOWS-THE-DECLARED-a-ONLY-RULE` (the top-level
   headline), `ARITY-SUBSTRATE-THE-ROUND-STOPS-BEING-A-PARTITION`, and
   `ARITY-ADDED-ASSUMPTIONS-WOULD-SELECT-a-EQUALS-q-AND-PRESENT-ISP-DOES-NOT`.
   This is better than DISC (absent entirely) and AUTOGLUE (3 of 4), but the
   headline is among the three.
3. **Segment 2's outcome was not contingent.** The declared arity window is
   `(2, 3, 4, 5)` and only `1, 3, 9` divide 9, so
   `THE-ROUND-STOPS-BEING-A-PARTITION` was forced by the window's own arithmetic
   before any measurement ran. The complementary outcome had no route. Segment 8
   is half-contingent: which principles select was measured and could have come
   out otherwise; *"PRESENT ISP DOES NOT"* was knowable from the corpus before
   the run.

**On the mandate's question — was a LAW-IN-A-EVERYWHERE outcome reachable?**
Yes for the *word*: the LAW-IN-A feasibility argument is sound (`C(a,2)+v−3`
differs from the constant reading at a=2, 4 and 5, so the discrimination leg can
fire), and M4 shows a rule under which the word is actually returned. Yes for
the *statement* slot, where four of six do transport. But the aggregate headline
segment itself has no pre-registered alternative and no feasibility argument.

**Repair:** bind the pre-registration to the run — a gate that (i) asserts every
emitted word is in the list, (ii) asserts every declared word either fires or
carries a measured reason why its trigger did not occur, and (iii) attaches a
pre-registered outcome plus feasibility to **each** head segment, including the
aggregate and the substrate segment. Stamp the two ladder rows
`INFEASIBLE-CARRIED` where they are already `"feasible": false`.

---

## MINORS

**m1 — the scope word's stated ground does not cover two of the rows it is
attached to.** §2.1 defines the extension family as *"the arities at which the
packing rule leaves an idle remainder"*, and the arena table's `extension family`
row lists 2, 4, 5 on that ground. But the crystallization **floor** leg
enumerates every a-subset of the nine actors (`event_universe = C(9,a)`) and the
**SEC-2** leg enumerates every a-subset of the fifteen union carriers — neither
uses the packing rule. Both rows still carry `WITHIN THE DECLARED EXTENSION
FAMILY`. The scoping is *conservative* (over-scoped, never under-scoped) and the
rows are still extension results because the **event size** is off-parent — but
the definition given is packing-based and does not reach them. Since AUTOGLUE is
ordered to consume the a=2 SEC-2 row, say which ground each row stands on.

**m2 — `build_walls`'s docstring says "FOUR READING WALLS"; it returns seven**
(`:3407`, sealed as `n_walls = 7`). A sealed object misdescribed by its own
docstring is the E-23 shape.

**m3 — three of the seven walls have no licence leg at all.**
`W-NO-PROBABILITY`, `W-NO-CARRIER-OVERREACH` and `W-NO-MOTIVATION-CLAIM` declare
`subject=()` and `policed=()`, so `licence_leg` returns immediately and they are
**pure pattern walls**. `W-NO-CARRIER-OVERREACH` is the wall that guards the
head's own carrier-separation sentence; it should be the strongest of the seven.

**m4 — the comparator's independence claim is half true.** §11 says the
comparator *"re-decid[es] the verdict words by its own arithmetic without calling
the decision procedure."* The **numeral** half genuinely does (`:3254`
re-implements `math.comb(aa,2) + pv - math.comb(PARENT_ARITY,2)`). The
**statement** half does not: `:3246` reads `ev.get("fails_at")` straight out of
the builder's own evidence dict and re-labels it. That is the S-2 family (two
"independent" routes through one shared component), and the sentence should say
which half is which.

**m5 — a dead check inside the comparator.** `:3265-3268` builds `pat`, evaluates
a condition on it, and then executes `pass`. It is a written check that does
nothing — the carried-not-used shape TPL-2 forbids and CONTRACT K1 M3 found in
the sibling unit.

**m6 — the referent gate's reach should be stated.** It binds **44 of 52 prose
numerals (84.6%)** across 23 sentences — far and away the best in wave 1, and
worth saying so. But it is **9.3% of the paper's 473 numerals**, the remainder
living in tables and fences that `G-PAPER-CLAIMS` binds by equality instead.
Six prose sentences carrying numerals select no universe, including *"That
complement reads 0, 1, 2 and 4."* — the lead's own generalisation, which selects
no universe because "complement" is in no noun list. Add it to the `union` nouns.

**m7 — five reflexive referent pairs are licensed.** `factorization` carries
`[126,126]`, `[315,315]`, `[945,945]`; `union` carries `[36,36]`;
`crystallization` carries `[4,4]`. Each is a genuine measurement here, unlike
AUTOGLUE's, so this is not the K3 M1 defect — but a reflexive pair licenses
"X of X" anywhere in its universe, and the register should mark them as measured
identities rather than free pairs.

**m8 — SEC-2's standing prohibition should be named.** SEC-2 §12 lists *"the
crossing-alone control as a driven object"* among what may not be inherited.
ARITY does not inherit it — but §9's closing sentence and the segment name
`ARITY-BREAKS-SEC2-AND-THE-OBSTRUCTION-IS-EMPTY-AT-TWO` invite the reader to
complete an inference ARITY declines to make. One sentence acknowledging the
standing row settles it, and AUTOGLUE will thank the paper for it.

**m9 — "reproduces that unit's own delivered census" (§9) reproduces counts, not
verdicts.** ARITY's column is a pair-combinatorial predicate; SEC-2's 216/72 are
liveness verdicts. They coincide because SEC-2 itself asserts the identity. The
word "census" is doing double duty.

**m10 — a LOG-vs-artifact discrepancy the adjudication should record (not the
paper's defect).** #22 and #24 both state the ledger head as `3ea48cfe71ef3cc9`.
The delivered transcript's last line and the receipt's `ledger_head` key both
read **`7ff8a990f4b541c5`**. I reproduced both: `3ea48cfe71ef3cc9` is
`LD.head` printed to stdout after all 34 rows; `7ff8a990f4b541c5` is
`LD.recompute_chain()` taken at `:4551`, **two gates early** — it does not cover
`G-SEAL-TOTALITY` or `G-CLOSE`. The artifacts therefore publish a "ledger head"
that is not the head of the ledger. Either move the recomputation after the last
row or rename the published key.

---

## BONUS FOR THE UNIT — §7'S MECHANISM IS A THEOREM, NOT A ONE-ARENA MEASUREMENT

§7 says *"The mechanism is measured rather than argued"* and publishes the
modulus law `cells / gcd(cells, mass)` as a sweep result at this arena. It is
stronger than that, and the paper can have the stronger form for free.

Under the **budget** (LITERAL) reading, the round mass is *by definition of that
reading* the budget, and the budget is the actor count: `mass = n`. The cell
field is the sites times the declared links: `cells = n·L` (verified:
`27 = 9 × 3`). So

> `modulus = cells / gcd(cells, mass) = nL / gcd(nL, n) = nL / n = L`

**at every arena, with no reference to n, q or a.** Hence, under the budget
reading, **the ladder's first rung is the declared link count always, and mod-a
appears if and only if `a = L`.** Verified on both live rows (a=3: mass 9 →
modulus 3 = L; a=4: mass 9 → modulus 3 = L).

Three things follow. (i) §7's conclusion is not evidence, it is a two-line proof,
and the parent's `R = L` rule is explained rather than merely re-observed. (ii)
The W3 exposure in M5 is **discharged for the budget reading** — "the event size
is not a modulus" is arena-independent there, and the paper is entitled to say
so. (iii) Under the MAXIMAL reading `mass ≠ n` and the modulus is genuinely
arena-specific (27 here), so that half stays fibre-relative and should be
labelled as such. This is liftable evidence the unit did not have, and it makes
its most-repeated-coincidence section the strongest in the paper.

---

## MANDATE DISPOSITION

| item | verdict |
|---|---|
| extension-scope discipline at every per-law word | **FOLDED.** 13/13 rows carry it in-row; packing rule and extension family are separate declared arena rows; 191-sentence sweep finds no a=2/4/5 result read as committed ISP. See m1 for the one definitional gap. |
| the conditional selection form, verbatim | **FOLDED VERBATIM**, head segment 8 and §10:511. "NONE OF THEM PRE-REGISTERED ANYWHERE IN THE CORPUS" is said plainly; "In present ISP the event size remains an INDEPENDENT FREE DECLARATION" is said plainly. |
| the two-level headline | **FOLDED.** Both slots defined in §1, two tables in §11, head segment 2 declares the split. The banned universal appears nowhere; every carrier sentence is scoped by "here". |
| W1 / W2 paraphrase sweeps, 6 fresh plants | **FAILS — M2.** 0 of 6 caught; 6 of 6 ship at exit 0 through full delivery; 4 of the unit's own 6 die to one bare negation. |
| does W3 have a wall at all | **NO — M5.** 7 walls, none family-level; 0 occurrences of fibre/still-free/family-level. One exposed sentence, discharged by the bonus for the budget reading. |
| the a=2 lead's licensure; SEC-2 free-items not recomputed | **CLEAN.** Attribution exact against SEC-2's own control row and its own sentence; the price is never imported; §13 refuses the successor claim. See m8, m9. |
| the mod-a mechanism sentence's scope | **UNDER-CLAIMED — see the bonus.** True as stated, provable in general under the budget reading, fibre-relative under the maximal one. |
| the NDEP floor bonus's fairness to the sealed parent | **FAIR, with one non-sequitur — M6.** ARITY correctly reports NDEP's attainment as open and its own result as evidence not proof; the closed-form inference does not follow and the single-row basis of the separation is unstated. |
| referent binding across the many universes | **STRONG — 84.6% of prose numerals, 7 universes with measured pairs.** See m6, m7. |
| spelled fractions (the AUTOGLUE M1 disease) | **CLEAN.** No spelled fraction is false. "5 of 5", "three of four", "3 of the 5", "4 of 4", "one higher than the budget", "one for each parallel class" all recompute exactly. No percentage and no `a/b` fraction anywhere. |
| E-24 | **SATISFIED.** `MEASURE=COUNTING-ONLY (E-24)` in the SCOPE line, `COUNTING-ONLY` in head segment 6, §8 and §12; no count anywhere is converted to a probability. |
| #299-extended feasibility of every head word | **PARTIAL — M7.** Five words with feasibility, none consumed; 3 of 9 segments outcome-less; one word never fires though its trigger occurred; segment 2's outcome was not contingent. |

---

## RECOMPUTATION COUNT

**444 discrete recomputations across 11 full deliveries**, sharing no code with
the instrument: 98 arena/law rebuilds from scratch and from closed forms; 54
provenance and SEC-2 rebuild checks by a second hand; 28 transport-word
re-decisions across four rules; 42 fresh-plant wall scans; 42 negated-plant
scans; 42 own-plant leg attributions; 52 prose numerals classified against the
referent universes; 19 sentence-final numerals located; 16 ladder-column
recomputations; 10 source-hash verifications; 10 pre-registration consumption
checks; 9 head-segment feasibility rulings; 8 spelled-claim verifications; 6
registered prose claims verified; 7 modulus-law derivations; 1 window-leg
identity. Deliveries: 1 baseline (byte-identical, receipt and transcript both
matching the committed digests), 6 fresh-paraphrase, 4 numeral-corruption.

**Zero delivered physics numbers are wrong.** The three false numbers reported in
M3 are ones I planted to prove the scanner blind; they are not in the delivered
paper.

---

## STATUS

**CANDIDATE UNTIL ADJUDICATION.** Seven majors, ten minors, one bonus. No reject
lever: no theorem here is false, no measured quantity disagrees with an
independent rebuild, and the three ordered renames are folded verbatim at every
use. M1 and M3 move sentences, not numbers. M2, M5 and M7 are the instrument
debts wave 1 keeps re-buying, and M2's cure is already written inside this unit's
own file, applied to one wall of seven.
