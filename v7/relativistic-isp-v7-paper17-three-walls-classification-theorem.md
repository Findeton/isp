# The record click-law, XVII: the three record-blind inputs are one no-go shape — a quotient-by-internal-symmetry classification theorem, graded by epistemic status

**Author:** Felix Robles Elvira (ORCID: 0009-0009-2017-4394; independent researcher)

**Status:** preprint, not peer reviewed, version 2026-06-16. Seventeenth paper of the v7 program. It is a **classification / structural-unification** result — a consolidation, not new foundational mathematics and not a derivation of any of the three inputs. It proves that the three things the record click-law provably cannot supply — the absolute **scale** (`l_step`, hence Newton `G`, the seal spacing, the metric, the 4-volume unit), the **tensor product** (the entangling content `χ_AB` / the complex-over-real composition bit), and the canonical **mode** (which-mass-is-which, the ledger rank label — and with it the matter hierarchy `c_m = Gm²/ℏc`, a weight-`0` quantity whose value-freedom is the mode/matter-sector residual, *not* the scale orbit) — are not three coincidences but **three instances of a single quotient-by-internal-symmetry shape**, and that the three residuals carry **three distinct epistemic statuses**. Tags: **[OWNED]** = a SHARD-intrinsic construction or framing; **[IMPORT]** = an external result invoked, not re-derived; **[CONVERGENT]** = a wall the foundations / quantum-gravity field already treats this way, on which SHARD claims no priority in isolation; **[NO-GO]** = a proved impossibility in scope; **[CONDITIONAL]** = a no-go that rides a named, contestable premise; **[CONTESTED]** = a genuinely open 2026 question on which this paper takes no side. The unifying receipt is re-run, not trusted: `v7/code/p17_classification.py`, **23/23 checks pass**, every structural/algebraic identity **sympy-exact**, every numeric quantity at mpmath `dps = 140`; **no SDP and no solver-tolerance digit in this receipt**. The three cross-checked source receipts are also re-run: `p15c_weight_classification_nogo.py` (27/27), `p6_transverse_nogo.py` (42/42, of which 7 are cvxpy solver-tolerance `~10⁻⁹`, flagged), `s_mode_import_wall.py` (16/16).

## Scope — four guards, stated first [scope discipline]

Because a "one shape, three times" claim invites both overreach and false symmetry, the boundaries come before the theorem.

1. **This is consolidation, not new heavy mathematics.** The contribution is *seeing one structure where the field sees three unrelated facts*, and *grading the residuals by epistemic status*. The individual walls are, in isolation, **convergent-not-novel**: that the absolute scale / `G` is un-derivable from within an emergent-gravity ontology is the Jacobson / Verlinde tradition; that local tomography is the least-motivated reconstruction axiom — the one whose removal admits real (rebit) theory, i.e. the selector of complex over real — is the Hardy / Barnum–Müller–Ududec / Masanes–Müller / Höhn lineage; that mass spectra are measured inputs is Standard-Model orthodoxy (`~19+` measured parameters). SHARD is not first on any wall alone.

2. **The G leg is a CONDITIONAL no-go, not on par with the other two.** The residual of the scale leg — the absolute length, hence `G` — is genuinely **measured**; but the SHARD *route* establishing that it is record-un-derivable rides the entropic equation-of-state derivation (Jacobson's Clausius `δQ = TδS` ⇒ Einstein form), and so inherits the Jacobson-targeting critiques (Padmanabhan's "interpretation-not-derivation"; Eling–Guedens–Jacobson's nonequilibrium `f(R)` branch) and the three further Paper 57 conditionals (§4). The honest headline is **two structural no-gos + one conditional**, never a clean threefold all-experiment-fixed symmetry.

3. **The residual TYPES genuinely differ; this is a classification across three kinds, not a coincidence of one kind.** SCALE's residual is a *continuous* positive real (`ℝ₊`); TENSOR's is a *discrete binary* bit (`{0,+1}`, in `ker R`); MODE's is a *discrete label* (`{1,3,7}`). The unification is the common quotient-by-symmetry *shape*. It is a genuine single-group quotient for SCALE and TENSOR (one `G_i` fixes the invariant *and* moves the named residual, sympy-exact); for MODE it is the **weaker, analogical** member — the named discrete residual `{1,3,7}` is `G_3`-*fixed*, and the object `G_3` moves is the *continuous* zero `c_r ∈ ℝ³`, so the discrete-residual↔orbit-parameter identification is [STRUCTURAL]/[ANALOGY], honestly scoped in §3.3. Not a claim that the three residuals are the same object, and not a claim that MODE is a quotient on a par with the other two.

4. **SHARD does not own the hard theorems underneath.** The permanence and level-independence of the `Q̃ ∖ Q` gap is **MIP\*=RE** (Ji–Natarajan–Vidick–Wright–Yuen 2020) and **Slofstra 2019**; the local-tomography-least-motivated lineage is Hardy / BMU / Höhn; the Tsirelson bound `2√2` and the NPA / commuting-operator relaxation are Tsirelson 1980 / Navascués–Pironio–Acín; the entropic-gravity `G` argument is field-shared and premise-conditional. The 2026 real-vs-complex constructions and falsification framing are external and the paper takes **no side**. These are cited as imports (§6).

Inside those guards, the classification is machine-verified.

---

## 1. Abstract

The SHARD record click-law carries everything **relative**: every quantity it forces is a record-internal pure number — a KL content, a Fisher capacity, a survival ratio `S = exp(−κχ)`, a moment `⟨ψ| O_i^† O_j |ψ⟩` — all weight-`0`, all in `ker R`. Across Papers I–XVI it was shown to be barred from exactly **three** last inputs: the absolute **scale** `l_step` (hence Newton `G`, the seal spacing `d`, the metric, the 4-volume unit — all `length^w` with `w ≠ 0`); the **tensor product** (the entangling content `χ_AB`, equivalently the complex-over-real composition bit); and the canonical **mode** (which-mass-is-which, the ledger-rank label — carrying with it the matter hierarchy `c_m = Gm²/ℏc`, which is itself weight-`0` and so *not* a scale-orbit datum, but whose value is import-fixed through the same mode-canonicalization the rank label awaits, per Papers 5/VIII). This paper proves these three are **one no-go shape**. For each wall `i` there is an *internal symmetry group* `G_i` such that **(A)** the self-consistency principle is `G_i`-invariant, so the records carry *only* `G_i`-invariants (everything relative / weight-`0` / the form / the envelope / the ratios / the within-sector spectrum — the ⊆ direction; not every invariant is supplied, e.g. `c_m`, `χ_AB`); **(B)** the records are blind to (at least) the **orbit parameter** — the position within the `G_i`-orbit — which is the record-blind residual; **(C)** self-consistency fixes the **fence** (the orbit) and never the **filling** (the parameter). The three instances: SCALE — `G_1 = g_λ`, the length-relabeling automorphism `l_step ↦ μ·l_step` (the multiplicative group of `ℝ₊`); invariant = the weight-`0` functionals (Paper 6 Theorem G); residual = the absolute length, **continuous** `ℝ₊`. TENSOR PRODUCT — `G_2 = R`, the field-reduction (qubit-pair ↔ rebit-pair, complex ↔ real); invariant = the moment algebra `M = {1, A_x, B_y, A_x B_y}` (Paper 12 `M2`); residual = the local-tomography bit `K_AB − K_A K_B ∈ {0,+1}` in `ker R`, **discrete binary**. MODE — `G_3 = ` the **cross-sector re-referencing group** (the affine `ℝ`-torsor of per-sector additive energy-zeros `c_r`, `E_r ↦ E_r + c_r`; no common energy zero; carrying the which-is-which sector-permutation); invariant = the within-sector spectrum / ledger rank `2ⁿ − 1` (an additive offset of a sector's zero is orthogonal to its character Gram, so the rank is fixed); residual = the cross-sector which-mass-is-which label `∈ {1,3,7}`, a **discrete label**, which `G_3` *fixes* (it is in the stabilizer) — what `G_3` genuinely *moves* is the *continuous* zero `c = (c_r) ∈ ℝ³`, a different object, so the discrete-label↔orbit-parameter identification is [STRUCTURAL], not a theorem. (For MODE the schema's `G_3 =` cross-sector re-referencing does **not** sit on a par with `g_λ` and `R`: the named residual is the *discrete* label `{1,3,7}`, which `G_3` provably **fixes** (it carries a rank-`r` ledger to a rank-`r` ledger); what `G_3` genuinely *moves* is the *continuous* per-sector zero `c = (c_r) ∈ ℝ³`. The MODE instance is therefore the **weaker, analogical** member of the classification — the underlying no-go (records cannot select the rank) is sound and sympy-exact, but its presentation as a clean single-group quotient on a par with SCALE and TENSOR is a **[STRUCTURAL]** identification, *not* a theorem; see §3.3.) The classification carries a **three-epistemic-status grading** (the Paper XVI grading; in-corpus anchor): scale/`G` genuinely **MEASURED** but the SHARD route is **CONDITIONAL** on the Jacobson–Clausius premise (the Padmanabhan / Eling–Guedens–Jacobson critiques targeting Jacobson's derivation; the Verlinde-targeting Visser critique explicitly exempts it); mode/masses **IMPORT-FIXED** by measured spectra; tensor product a **CONTESTED** composition-rule convention possibly experimentally unfixable (the 2026 Hoffreumon–Woods / Maioli / Renou-Comment reopening). The honest headline is therefore **one shape, three times; three residual TYPES; three epistemic statuses; two structural no-gos + one conditional**. SHARD's novelty is the **unification** into one quotient-by-symmetry shape plus the **grading**; the individual walls are convergent-not-novel, and SHARD owns neither the undecidability math nor the reconstruction lineage. The unifying receipt `p17_classification.py` (**23/23**, sympy-exact + mpmath `dps = 140`, no SDP) instantiates and verifies all three triples — SCALE and TENSOR as genuine single-group quotients, MODE as the weaker analogue with the `G_3`-fixing of `{1,3,7}` made explicit; the field-blindness cross-check confirms `|ΔI_σ| = 0` to `< 10⁻¹³⁰`. Framed as a positioning and structural-classification result, receipt-anchored, not a new impossibility theorem.

---

## 2. The common structure, stated as a theorem [OWNED]

The classification rests on a single schema. We state it precisely, then instantiate it three times in §3.

### 2.1 The quotient-by-internal-symmetry schema

Let the SHARD records be a structure `R` (the relative ring of record-internal readouts), and let a *self-consistency principle* `P` be a constraint the records impose — for the three walls `P` is, respectively, the gravitational equation of state / the record click-law's gauge-triviality (SCALE), the transverse moment-positivity `Γ ⪰ 0` (TENSOR), and the within-sector firing-form fixing (MODE). Suppose an **internal symmetry group** `G_i` acts on the ambient theory in which the records are embedded, such that:

> **(A) Invariance.** The principle `P` is `G_i`-invariant: `P` holds of a configuration iff it holds of every `G_i`-translate. Consequently every record readout — every datum `P` actually fixes — is a `G_i`-**invariant**, i.e. a function constant on `G_i`-orbits. The records carry *only* invariants (the inclusion actually proved; whether they carry *all* invariants is not claimed — `c_m` and `χ_AB` are weight-`0` invariants the records demonstrably do *not* supply).
>
> **(B) Orbit-blindness.** There is a coordinate on the `G_i`-orbits — the **orbit parameter**, the position within an orbit — that no `G_i`-invariant can resolve. This parameter is the **residual**: the records are blind to it precisely because they see only invariants.
>
> **(C) Fence, not filling.** `P` fixes the **fence** (the orbit — the equivalence class, the form, the envelope, the ratios) and never the **filling** (the orbit parameter). Formally: each invariant is a fixed point of `G_i`; each residual is moved by `G_i`.

**Theorem (classification).** *For SCALE and TENSOR there is a single group `G_i` and a triple `(G_i, ` invariant`, ` residual`)` for which (A), (B), (C) all hold with the residual itself the `G_i`-orbit parameter: the records carry *only* `G_i`-invariants and are blind to *at least* the named residual, which the same `G_i` moves (the ⊆ direction is what is proved; the records do not supply every invariant — `c_m`, `χ_AB` — and are blind to more than the residual alone); self-consistency fixes the fence and never the filling. These two are genuine quotient-by-internal-symmetry instances, sympy-exact. MODE realizes the same no-go **shape** — an internal re-referencing symmetry `G_3` the records cannot see past, leaving the cross-sector mass label un-selectable — but as the **weaker, analogical** member: `G_3` fixes the within-sector invariant (A) and moves a `G_3`-orbit parameter (the* continuous *per-sector zero `c_r ∈ ℝ³`) (B), yet the* named *residual is the* discrete *label `{1,3,7}`, which `G_3` **fixes** (it lies in the stabilizer), so the identification of the named discrete residual with the orbit parameter is [STRUCTURAL]/[ANALOGY], not a theorem. The three instances carry residuals of three different **types** (continuous `ℝ₊` / discrete binary `{0,1}` / discrete label `{1,3,7}`) and three different **epistemic statuses** (measured-but-Jacobson-conditional / import-fixed-by-measured-spectra / contested-convention-possibly-unfixable).*

The proof is the instantiation: §3 exhibits each triple, and the receipt `p17_classification.py` verifies (A)–(C) hold for SCALE and TENSOR as genuine single-group quotients and for MODE as the weaker analogue (with the `G_3`-fixing of the discrete `{1,3,7}` made explicit) — `23/23`, every structural identity sympy-exact. What is a *theorem* and what is a *structural/analogical modelling claim within SHARD* is tagged explicitly in §3.4.

### 2.2 What the schema is and is not

The schema is a **gauge-orbit / moduli** statement: the records compute on the quotient `R / G_i`, and the residual is the fibre coordinate the quotient discards. This is the precise sense in which "the records carry everything relative." It is **not** a derivation that any residual takes a particular value; on the contrary, the content of the theorem is that the residual is *exactly* the datum self-consistency cannot reach — the moduli the principle leaves free. The unification's value-add is structural: three superficially unrelated open inputs (a length, a composition rule, a spectrum label) are shown to be the *same kind of un-fixable* — orbit parameters of an internal symmetry the self-consistency principle respects — differing only in residual type and epistemic status.

A point of discipline that controls §4–§6: that the schema *holds* for all three is verified (the receipt); that the schema is *novel* as a unification is a positioning claim (§5); and that any single wall is novel *in isolation* is **disclaimed** (the walls are convergent-not-novel, §5.1). The three claims are kept separate throughout.

---

## 3. The three instances [OWNED unification · CONVERGENT walls]

We give the three triples, each with its receipt and key numbers, then the roll-up table.

### 3.1 SCALE — `G_1 = g_λ`, the length-relabeling automorphism

**The symmetry.** Paper 6 introduces `g_λ`, the unit-relabeling automorphism `A_rec ↦ λ·A_rec` that is the **identity on the record sector `R`**. A length `l = A_rec^{1/2}` scales by `μ := λ^{1/2}`: `l ↦ μ·l`. A quantity of length-weight `w` transforms as `g_λ(X_w) = μ^w · X_w`. The orbit is the **multiplicative group of `ℝ₊`**: a one-parameter Lie group with `g_{μ₁} ∘ g_{μ₂} = g_{μ₁μ₂}`, identity `μ = 1`, inverse `1/μ` — verified sympy-exact in the receipt (`g_of(g_of(l_step, μ₂), μ₁) − μ₁μ₂·l_step = 0`).

**The invariant the records carry.** Paper 6 **Theorem G** (gauge triviality): every intrinsic record readout factors through `R`, hence is `g_λ`-invariant, hence **weight-`0`**. The receipt confirms the paradigm cases sympy-exact: `g_λ(c₀) = c₀` for a pure-number coefficient (weight `0`); a genuine length **ratio** `l³/l³` is weight `0` while the unequal-weight control `l³/l` is weight `2`; and the de Sitter **product** `G·Λ₀` is weight `0` — `Λ₀` the weight-`(−2)` cosmological constant (an inverse area), `G` weight `+2`, so the product is weight `0`, and Paper 57 (`S_dS = π/(G·Λ₀)`) fixes only that product, never the absolute `Λ₀`. (The separate spectral-action invariant `G·Λ² = (`pure number`)` of Paper 57 §2 uses a *different* `Λ`: there `Λ` is the inverse-**length** UV/spectral cutoff of weight `−1`, so `G·Λ²` is weight `+2 − 2 = 0` — a distinct weight-`0` object from `G·Λ₀`, not the same `Λ`.) Self-consistency fixes these weight-`0` invariants — `G·Λ₀`, `G·Λ²`, and `κ·σ_A = 2π` — never the absolute scale.

**The residual.** The **absolute length / scale**, a positive real `∈ ℝ₊`: `g_λ(l_step) = μ·l_step` (weight `1`, non-fixed), `g_λ(G) = μ²·G` (weight `+2`, non-fixed). The seal spacing `d`, the metric, and the 4-volume unit are all `length^w` with `w ≠ 0`, hence orbit-carried. This is the **continuous** residual: the orbit parameter ranges over `ℝ₊`.

*(What is* not *in this orbit: the matter coupling-per-species `c_m = Gm²/ℏc = (m/M_Planck)²` is `g_λ`-**invariant** — `g_λ(G) = μ²G`, `g_λ(m) = m/μ`, so `g_λ(c_m) = c_m`, weight `+2 + 2(−1) − 0 = 0` exactly (Paper 5 §2; Paper 57). Being weight-`0`, `c_m` is **eligible** to be a record output and is **not** a `g_λ`-orbit parameter; it is therefore* not *part of the SCALE residual. Its value-freedom is the **MODE / matter-sector** residual — the same mode-canonicalization bottleneck that leaves the rank label free (§3.3) — and it is placed there, not here.)*

**Receipt.** `p17_classification.py` INSTANCE (1), eight checks, all `[THEOREM]`, sympy-exact: the 1-parameter-group composition law; the weight grading `weight(c₀)=0, weight(l_step)=1, weight(G)=2`; and — instantiated with their explicit `Λ` weights so the prose is receipt-anchored — `weight(G·Λ₀)=0` (de Sitter; `Λ₀` the weight-`(−2)` cosmological constant / inverse area) and the *distinct* `weight(G·Λ_uv²)=0` (spectral action; `Λ_uv` the weight-`(−1)` inverse-length cutoff). The `c_m` instance is also run: `weight(c_m = Gm²)=+2+2(−1)=0` and `g_λ(c_m)=c_m`, confirming `c_m` is `g_λ`-invariant and **not** a scale-orbit residual. Cross-checked against `p15c_weight_classification_nogo.py` (27/27, `σ_A` weight `−2` in bijection with `G` weight `+2` via `G·σ_A = 1/4`; the everpresent `Λ` factors exactly into a weight-`0`-eligible `N^{−1/2}` and a weight-`(−2)`-walled `1/l_step²`, residual `0`). Paper 57's unified no-go: all six second-scale levers (Tomita–Takesaki/Connes, Benincasa–Dowker, fluid-gravity, JLMS, shape-dynamics, spectral action) collapse onto the same fixed weight-`0` invariants; the absolute scale is a free modulus.

### 3.2 TENSOR PRODUCT — `G_2 = R`, the field-reduction map

**The symmetry.** `R` is the field-reduction qubit-pair ↔ rebit-pair (complex ↔ real). It **preserves** every marginal, every cross-moment `E_xy`, the click-law `p(a,b|x,y) = (1 + ab·E_xy)/4`, and the sealing scalar `I_σ`; it **changes** the local-tomography info-dimension count.

**The invariant the records carry.** Paper 12 `M2`: the chains meet only at shared cross-moments (commutation *on the state*, not a Hilbert tensor factorisation), so every transverse principle factors through the level-`(1+AB)` **moment algebra** `M = {1, A_x, B_y, A_x B_y}` — the marginals plus the cross-moments. `M` is `R`-**invariant**: the qubit-singlet and the rebit-Bell-pair realizations carry **identical** `M`-data. The receipt encodes `M` as the symbolic data vector `(1, m_A, m_B, E_xy)` and verifies `R` is the identity on it; a numeric cross-check at `dps = 140` confirms both realizations reach `|E| = 1/√2` at the Tsirelson angle with `I_σ` identical to `< 10⁻¹³⁰` (`|ΔI_σ| = 0` exactly in the receipt). The click-law datum is **field-blind**. *(Honest note, carried from Paper 12: the `I_σ` equality is true-by-design at the shared Tsirelson point — both correlators live in the `X`–`Z` plane `R` preserves; the load-bearing field-distinguishing computation is the `K`-deficit / orbit argument below, not this equality, which is not presented as independent evidence.)*

**The residual.** The **local-tomography / complex-over-real bit** `K_AB − K_A·K_B`, which lives in `ker R` (`R` erases it). Re-running the Paper 6 PART 4 info-dimension counts sympy-exact and cross-checking them live against the source receipt: `K_single = 3` real (`d(d+1)/2`) / `4` complex (`d²`); `K_AB = 10` 2-rebit (`D(D+1)/2`, real symmetric `4×4`) / `16` 2-qubit (`D²`). The deficit is `+1` for the rebit (local tomography **fails**: `10 ≠ 9`) and `0` for the qubit (holds: `16 = 16`). Two orbit values `{0,+1}`, flipped by `R`, invisible to `M`. This is the **discrete binary** residual — equivalently the `Q`-vs-`Q̃` tensor-product choice.

**Receipt.** `p17_classification.py` INSTANCE (2), seven checks (six `[THEOREM]`, one `dps=140` numeric), sympy-exact `K`-counts `3/4/10/16` and `M`-invariance; the field-blindness cross-check `|ΔI_σ| = 0 < 10⁻¹³⁰`. Cross-checked against `p6_transverse_nogo.py` (42/42; PART 4 the exact `K`-counts; PART 5 the almost-quantum envelope `Γ ⪰ 0` ⇒ `CHSH ≤ 2√2`, no-signaling, PR-exclusion, the `I3322` strict-outer relaxation `0.2514709 >` `Q`-bound `0.2508754`, monogamy `CHSH_{AB}² + CHSH_{AC}² ≤ 8` — the **7 SDP digits flagged solver-tolerance `~10⁻⁹`**, never relied on here). The un-forceability no-go (Papers XII/XIII): `χ_AB` is free up to `Q̃` by two independent weight-`0` mechanisms — `M1` **capacity blindness** (the faithful Fisher leg is marginal-only / `λ`-flat) and `M2` **field blindness** (the `K`-deficit / orbit argument). The `Q̃ ∖ Q` gap is permanent and level-independent (Slofstra 2019 / MIP\*=RE — an **import**, §6).

### 3.3 MODE — `G_3 = ` the cross-sector re-referencing group (affine zeros `c_r`)

**MODE is the weaker, analogical member of the classification — stated honestly up front.** For SCALE and TENSOR a *single* group does both schema jobs on the *same* object: one `g_λ` both fixes the weight-`0` invariants (A) and moves the absolute length residual (B); one `R` both fixes `M` (A) and flips the residual bit (B). MODE does **not** fit this way, and the difference is load-bearing, so it is faced directly rather than papered over.

The named MODE residual is the **discrete which-mass-is-which label** `∈ {1,3,7}`. The candidate group `G_3 =` the **cross-sector re-referencing group** — the affine `ℝ`-torsor of per-sector additive energy-zeros `c = (c_r)_r`, acting by `E_r ↦ E_r + c_r`, realized on the dimension labels by `n ↦ n'` (shift `(n'−n)·ln 2`) and carrying the which-is-which sector-permutation — **fixes** the within-sector rank `2ⁿ − 1` (clause (A), proved below) but it **fixes** the discrete label `{1,3,7}` too: a sector-permutation carries a rank-`r` ledger to a rank-`r` ledger, and the additive offset `c_r` leaves every rank where it was, so the multiset `{1,3,7}` is in the **stabilizer** of `G_3`, not in its moved orbit. (The within-sector sign-flip/permutation relabel likewise fixes the rank and the label and so is also disqualified as the residual-mover.) What `G_3` genuinely *moves* is the **continuous** zero `c = (c_r) ∈ ℝ³` — a *different object* than the named discrete residual `{1,3,7}`. Identifying the two — reading the discrete label as "the orbit parameter `c`" — is a **continuous-vs-discrete conflation**, and we do **not** assert it as a theorem. The honest position: **MODE is an analogy to the schema, not a clean instance of it on a par with SCALE and TENSOR.** The named residual is `G_3`-*fixed*; the genuine `G_3`-orbit parameter (the continuous zero) is not the named residual. This is tagged **[STRUCTURAL]/[ANALOGY]**, never [THEOREM]. What *is* a theorem is the underlying no-go: the records cannot select the cross-sector rank.

**The sound part — clause (A), sympy-exact [THEOREM].** The **within-sector spectrum / the ledger rank `2ⁿ − 1`** (`n = 1,2,3` ⇒ ranks `1/3/7`) is **invariant under cross-sector re-referencing**. The rank is read off the parity-character Gram, which the receipt verifies sympy-exact to be the identity of size `2ⁿ − 1` (the characters are orthonormal); an additive offset `c_r` of a sector's energy zero is a constant *orthogonal to the character spectrum* — it enters the energy, never the Gram, so the rank is a fixed point of `G_3` (the receipt recomputes the Gram after applying a symbolic `c_r` and confirms `c_r` is absent from it and rank `= 2ⁿ − 1` is unchanged). The sector-permutation half of `G_3` carries a rank-`r` ledger to a rank-`r` ledger, fixing the multiset `{1,3,7}`. The within-sector stabilizer (sign-flips and index-permutations) likewise leaves Gram `= I` and rank `= r`. The three ranks `[1,3,7]` are distinct integers: three gauge-inequivalent sectors.

**What `G_3` genuinely moves — clause (B), and the honest caveat.** The cross-sector re-referencing shifts the content by exactly `D(P‖U_{2^{n'}}) − D(P‖U_{2ⁿ}) = (n'−n)·ln 2`, **state-independent** (`H(P)` cancels: `d/dH` of the shift `= 0`, sympy-exact). The additive zeros `c_r` form an affine `ℝ`-torsor (composition adds the zeros, identity `c = 0`, inverse `−c`) with **no distinguished basepoint** — exactly "no common energy zero." So each sector's free-energy zero is free up to the unfixed *continuous* `c_r ∈ ℝ` the records do not supply, no within-sector invariant resolves which `c_r` is "the" zero, and the cross-sector argmin requires a reference **import**. This `c_r` is what `G_3` moves — a continuous `ℝ³` datum. The *discrete* label `{1,3,7}` is the thing the records still cannot select once that import is missing, but it is downstream of (selected *by*) the choice of reference, not itself a coordinate of the `G_3`-orbit; calling it "the orbit parameter `c`" is the **[STRUCTURAL]** identification flagged above, not part of the proof. The matter hierarchy `c_m = Gm²/ℏc` (weight-`0`, Paper 5) sits on this same MODE/matter-sector axis: its value-freedom is the very mode-canonicalization that leaves which-mass-is-which open, *not* the `g_λ` scale orbit. The no-go that does stand, structurally, is: **records carry no cross-sector reference, so they select neither the rank label nor `c_m`.**

**Receipt.** `p17_classification.py` INSTANCE (3), four checks, sympy-exact: **(A)** [THEOREM] the parity-character Gram `= I`, ranks `[1,3,7]`, the within-sector stabilizer's rank-preservation, **and** the explicit recomputation showing a symbolic offset `c_r` is absent from the Gram so rank `= 2ⁿ − 1` survives re-referencing; **(B)** [THEOREM] the cross-sector shift `(n'−n)ln 2` with `H(P)` cancellation and the affine-`ℝ`-torsor composition law (no basepoint) — verifying that `G_3` moves the *continuous* zero `c_r`; **(3.3)** [STRUCTURAL] the explicit check that every sector-permutation and every offset `c_r` **fixes** the discrete multiset `{1,3,7}` (it is in the stabilizer of `G_3`), so the discrete-label↔orbit-parameter identification is a continuous-vs-discrete modelling identification, explicitly *not* a theorem; and the records-blindness of the rank label (the sound no-go), tagged [STRUCTURAL]. Cross-checked against `s_mode_import_wall.py` (16/16): rank `= 2ⁿ−1` a superselection invariant (1a); a mode-selecting Hamiltonian needs a cross-sector common energy zero the records do not supply (1b, only a per-sector reference); the cross-sector argmin switches rank across crossings and lands on no forced click-law constant, with even the `α = 0` convention form-dependent (extensive / intensive / action / binding pick different ranks) — so the freedom relocates to the **choice of Hamiltonian itself**. SHARD owns the gap *mechanism* (large-`N` Gross–Neveu dynamical mass `M(g²=4) = 0.762/0.886/0.787`, chiral-SB without Higgs) and the gap *values* (`m_min(n) = −ln(1 − 2⁻ⁿ) − δ_n`, 100+ digits) but **imports** the canonical mode.

### 3.4 The roll-up table

| wall | symmetry `G_i` | invariant (records carry) | residual (blind to) | residual type | tag |
|---|---|---|---|---|---|
| **SCALE** | `g_λ` : `l_step ↦ μ·l_step` (length-relabel automorphism; the multiplicative group of `ℝ₊`) | weight-`0` functionals (Paper 6 Theorem G) — incl. weight-`0` `c_m = Gm²/ℏc` (eligible, **not** a residual) | absolute length `l_step`, hence `G`, `d`, metric, 4-vol (all weight `≠ 0`) | **continuous `ℝ₊`** | [THEOREM] |
| **TENSOR** | `R` : qubit-pair ↔ rebit-pair (complex ↔ real) field-reduction | moment algebra `M = {1, A_x, B_y, A_x B_y}` (Paper 12 `M2`) | local-tomography bit `K_AB − K_A K_B ∈ {0,+1}`, in `ker R` | **discrete binary `{0,1}`** | [THEOREM] |
| **MODE** | cross-sector re-referencing group: the affine `ℝ`-torsor of per-sector zeros `c_r`, `E_r ↦ E_r + c_r` (no common energy zero) | within-sector spectrum / ledger rank `2ⁿ − 1`; the matter `c_m` rides this axis (weight-`0`, mode-canonicalization-fixed) | cross-sector which-mass-is-which label `∈ {1,3,7}` (`G_3`-**fixed**; the continuous zero `c=(c_r) ∈ ℝ³` is what `G_3` moves — a *different* object) | **discrete label `{1,3,7}`** | [THEOREM] (no-go) + [STRUCTURAL]/[ANALOGY]\* (schema fit) |

\* **What is a theorem vs a structural/analogical modelling claim** (tagged per check in the receipt). SCALE — invariance + residual identification is a **theorem** (sympy-exact; Theorem G is a proved gauge-triviality statement; the single `g_λ` fixes the weight-`0` invariants *and* moves the same absolute-length residual). TENSOR — invariance of `M` + residual `=` the local-tomography bit in `ker R` is a **theorem** (sympy-exact info-dimension count; `R` is an explicit map; the single `R` fixes `M` *and* flips the same bit). MODE — the no-go is a **theorem** (sympy-exact: rank `= 2ⁿ−1` is a superselection invariant fixed by `G_3`, the offset `c_r` absent from the character Gram, and the cross-sector argmin needs a reference import the records do not carry). But the *schema fit* is **weaker than SCALE/TENSOR and is not a theorem**: the named residual is the *discrete* label `{1,3,7}`, which `G_3` **fixes** (it is in the stabilizer), while the object `G_3` genuinely *moves* is the *continuous* zero `c=(c_r) ∈ ℝ³` — a different object. Identifying the two (reading the discrete label as `G_3`'s orbit parameter) is a **[STRUCTURAL]/[ANALOGY]** continuous-vs-discrete identification, flagged as such, *not* the [THEOREM] tag. So MODE is the **analogical** member: same no-go *shape* (an internal symmetry the records can't see past), but not a clean single-group quotient on a par with the other two. The **common-shape** statement — all three are quotients by `G_i` with the records carrying the invariants and blind to the symmetry-hidden datum — is a **classification** verified to hold for SCALE and TENSOR as genuine instances and for MODE as the weaker analogue, a unification, not a derivation of new physics.

**The common-structure verification.** The receipt closes with four `[CLASSIFICATION]` checks confirming the schema (A)–(C) holds across all three (genuinely for SCALE/TENSOR, analogically for MODE): (C.1) **fence-not-filling** — each invariant is `G_i`-fixed (`g_λ(c₀)=c₀`; `R` fixes `M`; the rank is fixed by the cross-sector re-referencing `G_3`, the offset `c_r` being absent from the character Gram), and `G_i` moves a non-invariant datum the records cannot supply: for SCALE the residual itself (`g_λ(l_step) ≠ l_step`), for TENSOR the residual itself (the bit flips `0 ↔ +1`), and for MODE the *continuous* zero (`G_3` shifts the cross-sector zero `(n'−n)ln2`) — with the caveat (tagged [STRUCTURAL]) that for MODE the *named discrete residual* `{1,3,7}` is `G_3`-**fixed**, not the moved object, so MODE is the weaker analogical fit; (C.2) the three residual **types** are genuinely distinct (SCALE a 1-parameter group, TENSOR two values, MODE three labels); (C.3) the **epistemic grading** — exactly two structural + one conditional; (C.4) all three `(G_i, ` invariant`, ` residual`)` triples instantiated and verified. **`23/23`, sympy-exact + `dps = 140`, no SDP, no solver-tolerance digit.**

---

## 4. The three epistemic statuses, and the Jacobson-conditional caveat on the G leg [OWNED grading · CONDITIONAL · CONTESTED]

The classification of §3 is structural — it says the three residuals are the same *kind* of un-fixable. The **grading** — §4 — says they are *not* the same *epistemic* object: not "three experiment-fixed inputs" in a clean threefold all-experiment-fixed symmetry, but three distinct statuses. (In-corpus anchor: Paper XVI, "three record-blind inputs of distinct epistemic status.")

| leg | residual | epistemic status | status word |
|---|---|---|---|
| **(1) SCALE** | absolute length / `G`, `d`, metric, 4-vol — continuous `ℝ₊` (the weight-`0` `c_m` is *not* here; it rides the MODE axis) | genuinely **MEASURED** (an experimental datum the records provably cannot supply; the *least contested*) — **but** the SHARD route to its un-derivability is **CONDITIONAL** on the Jacobson–Clausius premise | **MEASURED but route-CONDITIONAL** |
| **(2) MODE** | cross-sector which-mass-is-which label — discrete `{1,3,7}`; and the weight-`0` matter hierarchy `c_m = Gm²/ℏc` (value-free through the same mode-canonicalization) | **IMPORT-FIXED** by measured spectra (a structural no-go); *intermediate* in contestedness | **IMPORT-FIXED by measured spectra** |
| **(3) TENSOR** | local-tomography / complex-over-real bit — discrete `{0,+1}` in `ker R` | a **NOW-CONTESTED** composition-rule / local-tomography convention, possibly experimentally **unfixable** in principle (the *most epistemically unusual*); a structural no-go whose residual *the world itself may not carry* | **CONTESTED CONVENTION, possibly unfixable** |

### 4.1 The G leg carries a caveat the other two do not [CONDITIONAL]

The mode and tensor legs are **structural** no-gos: the records cannot supply the residual, and that fact stands on sympy-exact structure (the superselection rank invariance; the `M`-invariance with the bit in `ker R`). The scale leg is **not** on a par. While the residual itself — the absolute length, hence Newton `G` — is genuinely **measured**, the SHARD *derivation* that `G` is record-un-derivable does **not** stand on its own. It rides the entropic equation-of-state route: Jacobson's Clausius relation `δQ = T dS` applied to local Rindler horizons yields the Einstein field equations *in form*, and only against that derived form does the weight-counting / `g_λ` no-go locate `G` as the missing absolute scale. That route inherits the **Jacobson premise**. The critiques that target Jacobson's derivation specifically are Padmanabhan's **"interpretation, not derivation"** reading (Padmanabhan 2010) and **Eling–Guedens–Jacobson 2006** (the nonequilibrium `f(R)` / Lovelock branch, which shows the local-equilibrium premise `θ = σ = 0` is load-bearing — drop it and pure Einstein gives way to a dissipative branch), together with the local-equilibrium-breakdown / finite-heat-capacity / observer-temperature-ambiguity scope limits — the standing objection that the thermodynamic-to-geometric inference is not premise-free. (The Verlinde-targeting critiques — Visser, *Conservative entropic forces* arXiv:1108.5240; Kobakhidze — **exempt** Jacobson by their own statements and are *not* the locus of this conditionality; see §6.)

Concretely the `G` leg is conditional on **four** named premises, all isolated in Paper 57 §1:

- **(i) the Jacobson–Clausius premise itself** — that `δQ = T dS` over all local horizons *is* the equation of state of spacetime; the locus of the Padmanabhan "interpretation-not-derivation" critique;
- **(ii) AXIOM (R)** — the modular / Euclidean-rotation identification fixing the temperature *value* `β = 2π`. Gibbs thermality at *some* `β` is unconditional (record passivity / a finite Lenard theorem), but the value `2π` — hence `T = a/2π` and the Clausius coefficient `η = 1/4G` — rests on this unproven axiom;
- **(iii) Jacobson's LOCAL-EQUILIBRIUM premise** `θ = σ = 0` at the bifurcation surface — presumed, not derived — which selects pure Einstein over the nonequilibrium `f(R)` / Lovelock branch (Eling–Guedens–Jacobson 2006: dropping it forces a dissipative treatment);
- **(iv) the CONTINUUM FOCUSING GATE** `θ' = −R_{kk}`, the one imported continuum input (now reduced to two native conditional gates: a finite double-null affine-pair readout and a cofinal-tightness / no-silent-refinement condition).

**The internal asymmetry, stated honestly.** Paper 57's §2 **scale no-go proper** — the weight-counting lemma that every intrinsic record functional is weight-`0` while `σ_A` is weight `−2`, so the absolute unit is outside the `g_λ` gauge orbit — does **not** itself depend on axiom (R). That part is the structural fact the receipt verifies. But the classification *claim* that this missing weight-`(−2)` unit **is gravity's Newton `G`** is delivered through the Jacobson / Clausius Einstein-form route. So the `G` **leg of the classification** is honestly a **conditional no-go** — conditional on the Jacobson–Clausius premise plus the three Paper 57 conditionals. We do **not** present it as a clean structural no-go on a par with tensor and mode. The headline is **two structural no-gos + one conditional**, never a clean threefold all-experiment-fixed symmetry — a load-bearing honesty constraint of this paper.

### 4.2 The mode and tensor statuses [IMPORT-FIXED · CONTESTED]

**Mode (import-fixed).** The cross-sector label is fixed by a *measured spectrum fed in from outside* — the Standard-Model orthodoxy that masses are inputs. SHARD owns the gap mechanism and values but imports the canonical mode; the residual is carried by the world, measured, structural. Intermediate in contestedness between the measured `G` and the contested tensor product.

**Tensor (contested convention, possibly unfixable).** The single bit SHARD cannot supply from inside — the one in `ker R` — *is* the complex-over-real / local-tomography / composition-rule choice. Until 2026 the natural reading was that an experiment fixes it from outside (Renou et al., Nature **600**, 625 (2021), read as ruling out real QM). That reading is now **contested** by three 2026 papers that change the composition rule — the precise locus SHARD names as the un-forceable bit: **Hoffreumon–Woods** (arXiv:2603.19208, real QT with the Kronecker rule reproduces all finite-network correlations under operational independence); **Maioli–Curado–Gazeau** (arXiv:2604.19482, a real Kähler / symplectic-composition framework reproducing all of QM, reaching `CHSH3 = 6√2`); and the **Renou et al. Comment** (arXiv:2604.07425, the Hoffreumon–Woods postulate fails in Fermionic Information Theory and is not a general physical postulate).

**SHARD takes no side.** It is **never** claimed that real QM is correct, that Renou et al. are wrong, or that the tensor product is "really" real. Renou et al. 2021 **stands in its own framing** (its product-state-independence postulate, in which real QM is excluded). The only SHARD claim is structural and side-neutral: *whichever way the community resolves the debate, the bit is being decided by a choice of postulate / convention — not by a record-internal derivation.* That contested, possibly-unfixable status **vindicates (does not prove)** the un-forceable-import verdict: "the records cannot supply the bit, and the world may carry no clean fact that fixes it" is exactly what a field-blind import in `ker R` looks like from the outside. Framed as **vindication, never proof**; the physics question remains open.

### 4.3 The honest headline

> **ONE SHAPE, THREE TIMES; THREE RESIDUAL TYPES; THREE EPISTEMIC STATUSES; TWO STRUCTURAL NO-GOS + ONE CONDITIONAL.**

The records carry everything relative / weight-`0` and are barred from three last inputs — **two structural no-gos** (tensor product, mode) **plus one conditional no-go** (scale/`G`, riding the Jacobson–Clausius premise) — **not** a clean threefold all-experiment-fixed symmetry. A guard against the tempting over-symmetry, policed here: the three legs are on **orthogonal axes** and are not the same kind of thing wearing three hats. `G` is a *scale* (a weight-`+1` grading obstruction along the `1/l_step` direction); the masses are a *spectrum anchor* (a mode); the tensor product is a *field / composition* bit (in `ker R`). Conflating "all three are imports" with "all three are the same epistemic object" is exactly the error this grading corrects.

---

## 5. Positioning versus the reconstruction literature [CONVERGENT walls · OWNED unification]

### 5.1 The three walls are individually convergent-not-novel [CONVERGENT]

Taken in isolation, every wall agrees with how the foundations / quantum-gravity field already treats these inputs — SHARD is not first on any of them alone.

1. **SCALE / `G`.** That the absolute scale / Newton's `G` is not derivable from within an emergent-gravity ontology is **field-shared**, not SHARD-novel. It is the standard reading of the Jacobson thermodynamic / entropic-gravity tradition (Jacobson 1995; Verlinde 2011), where `G` enters only as a calibration constant `η = 1/4G`; Paper 57 itself places SHARD "squarely in Jacobson's thermodynamic tradition, with `G` an honest calibration." The SHARD derivation that `G` is record-un-derivable rides the same entropic equation-of-state route, so it inherits the field's known premise-dependence (the Padmanabhan "interpretation-not-derivation" reading and the Eling–Guedens–Jacobson local-equilibrium critique).

2. **TENSOR PRODUCT / complex-over-real.** That **local tomography** is the least physically motivated of the operational reconstruction axioms — the very axiom whose removal admits real (rebit) quantum theory, i.e. the axiom that *selects complex over real* — **predates SHARD** and is the explicit subject of the operational-reconstruction lineage: **Hardy 2001** (the original five-axiom reconstruction with tomographic locality as a postulate), **Barnum–Müller–Ududec**, **Masanes–Müller**, **Höhn**. SHARD's `M2` field-blindness ("the complex-over-real bit lives in `ker R`, weight-`0`") is a **re-localisation** of this pre-existing insight onto the records, not its origin. The permanence of the resulting `Q̃ ∖ Q` gap is likewise an imported theorem.

3. **MODE / masses.** That particle masses / the mass spectrum are **measured inputs, not derived**, is completely standard — the Standard Model carries `~19+` independently measured free parameters (the fermion masses / Yukawa couplings among them), and no accepted theory derives them. Treating the canonical mode / mass spectrum as an import is the field consensus, not a SHARD discovery.

### 5.2 The SHARD-novel content is the unification plus the grading [OWNED]

The SHARD-novel content is **not** any single wall but their **unification** plus the **epistemic grading**. Three genuinely new pieces:

**(A) The one-shape classification.** The proof that all three record-blind inputs are instances of a *single* quotient-by-internal-symmetry structure (§2): a self-consistency principle invariant under `G_i`; the records carrying exactly the `G_i`-invariants; the one datum they cannot supply being the symmetry-hidden residual — self-consistency fixing the fence, never the filling. SCALE and TENSOR are genuine instances (one group fixes the invariant *and* moves the named residual, receipt-verified, sympy-exact); MODE is the **weaker analogical** member (the same no-go shape, but the named discrete residual `{1,3,7}` is `G_3`-fixed, not its orbit parameter — §3.3, tagged [STRUCTURAL]). The three carry residuals of three different **types** (continuous `ℝ₊` / discrete binary / discrete label) — receipt-verified.

**(B) The three-epistemic-status grading** (§4; in-corpus anchor Paper XVI): the three residuals are *not* a clean all-experiment-fixed trio. Scale/`G` genuinely **measured** but the SHARD route to its un-derivability **conditional** on the Jacobson–Clausius premise (so two structural + one conditional); mode/masses **import-fixed** by measured spectra; tensor product a **contested** composition-rule convention possibly unfixable (the live 2026 reopening).

**(C) The un-forceability-of-the-filling no-go for the tensor leg** (Papers XII/XIII): `χ_AB` is free up to `Q̃` and un-forceable from inside the records by two independent weight-`0` mechanisms (`M1` capacity blindness, `M2` field blindness), the selecting bit isolated to `ker R` — i.e. self-consistency provably cannot supply that orbit parameter, even in principle, from within.

The defensible headline: **seeing one shape where the field sees three unrelated facts, and grading the residuals by epistemic status** — a positioning and structural-classification result, receipt-anchored, not a new impossibility theorem.

---

## 6. What SHARD does not own [IMPORT — stated plainly]

SHARD owns the unification and the grading, **not** the hard theorems underneath. These are imported and cited as such.

1. **The undecidability core.** The permanence and level-independence of the `Q̃ ∖ Q` gap (the non-closability of the commuting-operator vs tensor-product correlation sets) is **MIP\*=RE** (Ji–Natarajan–Vidick–Wright–Yuen 2020) and **Slofstra 2019** ("the set of quantum correlations is not closed" / Tsirelson's problem has a negative answer). SHARD imports this verbatim; the "permanent, level-independent" character of `χ_AB`'s freedom rides entirely on it.

2. **The local-tomography-least-motivated lineage.** That local tomography (equivalently the tensor-product / complex-over-real bit) is the least physically motivated reconstruction axiom — the selector of complex over real — predates SHARD: **Hardy 2001, Barnum–Müller–Ududec, Masanes–Müller, Höhn**. SHARD's `M2` is a re-localisation, not the origin.

3. **The entropic-gravity `G` argument is field-shared and premise-conditional.** That `G` is a calibration not fixed from within is the **Jacobson / Verlinde** tradition; the SHARD route to it carries the Jacobson–Clausius premise (axiom (R) that `β = 2π`, the `θ = σ = 0` local-equilibrium premise, the continuum focusing gate — all named in Paper 57). The Jacobson-targeting critiques are **Padmanabhan** ("interpretation, not derivation") and **Eling–Guedens–Jacobson 2006** (the nonequilibrium `f(R)` branch showing local equilibrium is load-bearing). The **Verlinde**-targeting critiques (Visser, *Conservative entropic forces*, JHEP **10** (2011) 140, arXiv:1108.5240; Kobakhidze) **explicitly exempt** Jacobson — Visser's abstract states his constraints "do not directly impact on either Jacobson's or Padmanabhan's versions" — so they bound the **Verlinde** branch, not this leg's conditionality. So the `G` leg is honestly **conditional** — and must **not** be presented as an unconditional structural no-go on par with the other two.

4. **The Tsirelson bound `2√2` and the NPA / commuting-operator relaxation** are not SHARD-originated (**Tsirelson 1980; Navascués–Pironio–Acín**; the fresh process-theoretic re-derivation in Barandes–Hasan–Kagan). SHARD's `Γ ⪰ 0` instantiates the relaxation; it does not invent it.

5. **The 2026 real-vs-complex constructions and the falsification framing** (Hoffreumon–Woods, Maioli–Curado–Gazeau, Renou et al. 2021 + the Renou Comment) are external; SHARD cites them as the live controversy that contextualises (vindicates, never proves) leg (3)'s contested status, owning none and **taking no side**.

**What SHARD *does* own:** the **unification** into one quotient-by-internal-symmetry shape (§2–§3), the **three-epistemic-status grading** (§4), and the **un-forceability-of-the-filling no-go** for the tensor leg (Papers XII/XIII) — receipt-anchored in `p17_classification.py`.

---

## 7. Conclusion

The three things the SHARD record click-law provably cannot supply — the absolute **scale** (`l_step`, hence Newton `G`, the seal spacing, the metric, the 4-volume unit — all `length^w`, `w ≠ 0`), the **tensor product** (the entangling content `χ_AB` / the complex-over-real bit), and the canonical **mode** (which-mass-is-which — and with it the matter hierarchy `c_m = Gm²/ℏc`, a weight-`0` quantity that is *not* a scale-orbit datum but is value-fixed by the same mode-canonicalization) — are **one no-go shape**, not three coincidences. Each is an instance of a single quotient-by-internal-symmetry structure: a self-consistency principle invariant under an internal symmetry group `G_i`, the records carrying exactly the `G_i`-invariants (everything relative / weight-`0` / the form / the envelope / the ratios / the within-sector spectrum), the one datum they cannot supply being the orbit parameter — the position within the orbit — and self-consistency fixing the **fence** never the **filling**. The three instances are the same shape with residuals of three different **types** — continuous `ℝ₊` (scale, `g_λ`), discrete binary `{0,+1}` in `ker R` (tensor, the field-reduction `R`), discrete label `{1,3,7}` (mode, the cross-sector re-referencing group `G_3` — the affine `ℝ`-torsor of per-sector energy-zeros `c_r`) — where SCALE and TENSOR are genuine single-group quotients (one `G_i` fixes the invariant *and* moves the named residual) while MODE is the **weaker analogical** member: `G_3` fixes the rank and the discrete label `{1,3,7}` and moves only the *continuous* zero `c_r ∈ ℝ³`, so the discrete-label↔orbit-parameter identification is [STRUCTURAL], not a theorem (§3.3). The three carry three different **epistemic statuses**: scale/`G` genuinely **measured** but the SHARD route to it **conditional** on the Jacobson–Clausius premise (Padmanabhan / Eling–Guedens–Jacobson, the Jacobson-targeting critiques; the Verlinde-targeting Visser critique explicitly exempts Jacobson); mode/masses **import-fixed** by measured spectra; tensor product a **contested** composition-rule convention possibly unfixable (the 2026 Hoffreumon–Woods / Maioli / Renou-Comment reopening). The honest headline is **one shape, three times; three residual types; three epistemic statuses; two structural no-gos plus one conditional** — not a clean threefold all-experiment-fixed symmetry.

The contribution is exactly that: **seeing one structure where the field sees three unrelated facts, and grading the residuals by epistemic status.** The individual walls are convergent-not-novel — `G` un-derivable-from-within is the Jacobson / entropic tradition; local tomography as the least-motivated complex-over-real selector is the Hardy / Barnum–Müller–Ududec / Höhn lineage; mass spectra as measured inputs is Standard-Model orthodoxy. SHARD owns neither the undecidability math (MIP\*=RE / Slofstra), the reconstruction lineage, the Tsirelson bound / NPA relaxation, nor the entropic-gravity `G` argument. The `G` leg is kept **conditional**, the tensor leg kept **balanced** (contested, never "Renou is wrong"), the whole framed as **vindication, never proof** — SHARD remains side-neutral and the physics questions remain open. The unifying receipt `p17_classification.py` instantiates and verifies all three triples and the common schema (A)–(C): **23/23 checks pass**, every structural identity sympy-exact, every numeric quantity at mpmath `dps = 140`, the field-blindness cross-check `|ΔI_σ| = 0 < 10⁻¹³⁰`, **no SDP and no solver-tolerance digit** in this receipt.

---

## Reproducibility

One unifying receipt, re-run (not trusted), deterministic, single-threaded, exit `0`: `v7/code/p17_classification.py`, **23/23 checks pass** — every structural/algebraic identity **sympy-exact** (the `g_λ` weight grading `weight(c₀)=0, weight(l_step)=1, weight(G)=2`, the two distinct de Sitter / spectral-action invariants `weight(G·Λ₀)=0` (`Λ₀` weight `−2`) and `weight(G·Λ_uv²)=0` (`Λ_uv` weight `−1`), the weight-`0` `c_m=Gm²` with `g_λ(c_m)=c_m`, and the 1-parameter-group composition law `g_{μ₁}∘g_{μ₂}=g_{μ₁μ₂}`; the `K`-count info-dimensions `3/4/10/16` with deficits `0/+1`; the parity-character Gram `= I` and ranks `[1,3,7]`; the `(n'−n)ln2` cross-sector shift with `H(P)` cancellation; the explicit `G_3`-fixing of the discrete `{1,3,7}` multiset) and every numeric quantity at mpmath `mp.dps = 140` (the TENSOR field-blindness cross-check `|ΔI_σ| = 0 < 10⁻¹³⁰`). **No SDP and no solver-tolerance digit in this receipt** — it is purely algebraic. No float64 anywhere.

The three cross-checked source receipts are re-run, not trusted, with their numbers re-derived live (not copied): `p15c_weight_classification_nogo.py` (**27/27**; `σ_A` weight `−2` in bijection with `G` weight `+2` via `G·σ_A = 1/4`; the everpresent-`Λ` factorisation, residual `0`); `p6_transverse_nogo.py` (**42/42**, of which **7 are cvxpy/SCS solver-tolerance `~10⁻⁹`**, every such digit flagged — PART 4 the exact `K`-counts `3/4/10/16`, PART 5 the `Γ ⪰ 0` envelope at solver tolerance, not relied on here); `s_mode_import_wall.py` (**16/16**; rank `= 2ⁿ−1` superselection invariant; the `(n'−n)ln2` shift; the cross-sector argmin relocating the freedom to the choice of Hamiltonian). Reused numbers were re-run and cross-checked live: the Paper 6 PART 4 `K`-counts and the `s_mode_import_wall` ranks both match exactly; `p15c`'s `g_λ` + `weight()` machinery is re-derived identically inside `p17`.

## References

**This program.**
- *The record click-law, I* (v7 paper 1) — the forced multiplicative sealing skeleton `S = exp(−κχ)`.
- *The record click-law, VI* (v7 paper 6) — the transverse almost-quantum envelope; the field-reduction `R`; the moment algebra `M`.
- *The record click-law, VIII* / *XIV* (v7 papers 8, 14) — the mode wall; superselection sectors; the parity-character ledger of ranks `2ⁿ−1`; the Gross–Neveu gap mechanism and values.
- *The record click-law, XII* (v7 paper 12) — the transverse no-go: `Γ ⪰ 0` forces the almost-quantum envelope; `χ_AB` un-forceable by `M1` capacity-blindness and `M2` field-blindness; the `K`-deficit `K_AB − K_A K_B = +1` real / `0` complex.
- *The record click-law, XIII* (v7 paper 13) — the Feynman map: `χ_AB` bottoms out at `Q̃` downstream; the un-forceable bit in `ker R`.
- *The record click-law, XV* (v7 paper 15) — the everpresent-`Λ` scaling; the weight classification; `p15c`.
- *The record click-law, XVI* (v7 paper 16) — the seal as a physical mechanism; the three-epistemic-status revision (the in-corpus anchor for §4's grading; "three record-blind inputs of distinct epistemic status").
- *Emergent Einstein equations without an emergent Newton constant* (Paper 57) — the weight-`+1` scale / `G` no-go: `κ·σ_A = 2π`, the de Sitter product `G·Λ₀` (`Λ₀` the weight-`(−2)` cosmological constant; `S_dS = π/(G·Λ₀)` fixes the product) and the spectral-action `G·Λ²` (`Λ` the weight-`(−1)` inverse-length cutoff) — distinct fixed weight-`0` invariants, never numerically equal — with `G·σ_A = 1/4` (linked, never equal) and the absolute scale a free modulus; the four named conditionals (axiom (R), `θ=σ=0`, the continuum focusing gate, the Jacobson–Clausius premise); and the explicit statement that the matter coupling-per-species `c_m = Gm²/ℏc` is weight-`0` and intrinsic, **eligible** to be a record output (not part of the scale no-go).

**External (invoked, not re-derived).**
- T. Jacobson, *Thermodynamics of spacetime: the Einstein equation of state*, Phys. Rev. Lett. **75**, 1260 (1995); E. Verlinde, *On the origin of gravity and the laws of Newton*, JHEP **04**, 029 (2011) — the entropic / thermodynamic-gravity tradition; `G` a calibration constant.
- C. Eling, R. Guedens, T. Jacobson, *Nonequilibrium thermodynamics of spacetime*, Phys. Rev. Lett. **96**, 121301 (2006) — the `f(R)` / Lovelock **nonequilibrium** branch: deriving Einstein form from `δQ = T dS` over local horizons relies on the local-equilibrium premise `θ = σ = 0`; curvature corrections demand a nonequilibrium treatment. This is the Jacobson-specific evidence that the local-equilibrium premise is **load-bearing** (the locus of the `G`-leg conditionality, with Padmanabhan's "interpretation-not-derivation" reading).
- T. Padmanabhan, *Thermodynamical aspects of gravity: new insights*, Rep. Prog. Phys. **73**, 046901 (2010) — the "interpretation, not derivation" reading of the thermodynamic route, and the standing observer-temperature / local-equilibrium scope questions on Jacobson 1995.
- M. Visser, *Conservative entropic forces*, JHEP **10** (2011) 140, arXiv:1108.5240 — a constraint on the entropy / temperature functions that presents real problems for **Verlinde's** entropic gravity; Visser's abstract states explicitly that these constraints *"do not directly impact on either Jacobson's or Padmanabhan's versions of entropic gravity."* Cited here as a critique of the **Verlinde branch only** — it does **not** underwrite the Jacobson-route conditionality and **exempts** Jacobson; the Jacobson-route conditionality rests instead on Padmanabhan and Eling–Guedens–Jacobson above. (The title *"Why gravity is not an entropic force"* is S. Gao's, not Visser's; *"Gravity is not an entropic force"* is Kobakhidze, arXiv:1009.5414 / 1108.4161 — both also target Verlinde, not Jacobson.)
- L. Hardy, *Quantum theory from five reasonable axioms* (2001); H. Barnum, M. P. Müller, C. Ududec; Ll. Masanes, M. P. Müller; P. A. Höhn — local tomography as the least-motivated (complex-over-real) reconstruction axiom.
- T. Hoffreumon, M. P. Woods, *Quantum theory based on real numbers cannot be experimentally falsified*, arXiv:2603.19208 (2026); A. C. Maioli, E. M. F. Curado, J.-P. Gazeau, *Quantum mechanics over real numbers fully reproduces standard quantum theory*, arXiv:2604.19482 (2026); F. Moradi Kalarde, X. Xu, M.-O. Renou, *Comment on "Quantum theory based on real numbers cannot be experimentally falsified"*, arXiv:2604.07425 (2026); M.-O. Renou et al., *Quantum theory based on real numbers can be experimentally falsified*, Nature **600**, 625 (2021) — the live 2026 real-vs-complex reopening; SHARD cites side-neutrally and takes no side; Renou et al. 2021 stands in its own framing.
- Z. Ji, A. Natarajan, T. Vidick, J. Wright, H. Yuen, *MIP\*=RE* (2020); W. Słofstra, *The set of quantum correlations is not closed*, Forum Math. Pi **7**, e1 (2019) — the undecidability core; the `Q̃ ∖ Q` gap permanent and level-independent.
- B. S. Tsirelson (1980); M. Navascués, S. Pironio, A. Acín — the bound `2√2`; the NPA / commuting-operator relaxation that `Γ ⪰ 0` instantiates.
