# Relativistic ISP v7 Paper XXXII: V6-V7 Diamond Law Recovery Audit

**Status:** working audit, not peer reviewed, version 2026-06-29.

This paper rereads the v6-v7 corpus for pieces of the click record law that
may have already been found under different names. The result is not that the
universal law is closed. The result is that the older papers contain a
surprisingly specific skeleton for the current frontier:

> the click law is the positive h-transform shadow of a minimal no-silent
> diamond-boundary center, with the center generated by intrinsic
> boundary/collar residue and the shadow selected by projective recurrence,
> reflection positivity, coarseness, and a non-reconstruction budget.

The flag system is therefore not fundamental. The flags are coordinates for a
shadow. The diamond center is the older invariant that v6 was already trying
to define.

## 1. Reading Receipt

This audit close-read the solution-bearing portions of:

- v6 Paper 3, *Modular Record Diamonds and the Sealed-Deletion Equivalence
  Principle*: sections 14, 20, 24-27, 38-42, 55-66.
- v6 Paper 4, *SHARD: Sealed Holonomy and Record Dynamics*: sections 0-7,
  11-13, 71-77.
- v6 Papers 56 and 57 on gravitational channels and sealed-record gravity.
- v7 Papers 1, 2, 20, 21, 28, 29, 30, and 31.

The audit also used targeted searches over the v6-v7 corpus for diamonds,
screens, collars, no-silent seams, commitment, h-transforms, sufficiency,
projection, and boundary centers.

## 2. Main Finding

The previous papers do give part of the solution.

They do not give a universal closed formula for the click law, but they give
the architecture:

1. The scalar law is only the visible count face:

   $$
   N_\chi-\kappa\chi
   \quad\text{is the record martingale}
   $$

   in the dense simple regime.

2. A committed record is not a point click. It is a sealed finite diamond with
   boundary, collar, screens, and internal transport.

3. Hidden structure is admissible only through its record-projected likelihood:

   $$
   L_N(R)=\frac{dQ_N}{dP_N}(R).
   $$

4. If boundary data leave a residual, the residual is not ignored. It either
   washes out or becomes a licensed center.

5. The forward law is an h-transform:

   $$
   \Pr(P\mid C)
   =
   \frac{D(P,C)h_{N+1}(P)}
   {\sum_{P'}D(P',C)h_{N+1}(P')}.
   $$

Here `C` is the current committed record, `P` is a possible next/parent
record, `D(P,C)` is the deletion multiplicity, and `h_{N+1}(P)` is the
positive effective weight seen by the admissible record shadow.

The real missing theorem is not a better scalar. It is the theorem deriving
the admissible center and its h-shadow from record data alone.

## 3. What V6 Already Had

### 3.1 Scalar Deletion Profiles Are Not Complete

v6 Paper 3 explicitly falsified the idea that a scalar deletion profile is a
complete invariant. Two finite laws can share the same modular deletion profile
while differing in higher shell correlations.

The complete local event object was therefore not:

$$
M_x(k)
$$

alone, but:

$$
G_x=(P_x,P_{\setminus x},\mathcal F_{x,k},
\text{role maps},\text{fixed units},\text{action}).
$$

This is the ancestor of the v7 lesson that scalar compensators and finite
feature actions are only projections of the record law.

### 3.2 Do-Delete Was Already a Boundary Repair Problem

v6 Paper 3 rejected conditioning, label erasure, unconstrained KL repair, and
Markov repair as definitions of deletion. The surviving target was:

> deletion is the unique boundary-preserving minimum-disturbance repair to a
> fixed null state at a division-event atom.

This is close to the current "backward-then-forward" idea, but with an
important correction: deletion is not literal time reversal. It is a record
operation that asks what boundary-preserving repair makes the eventless
diamond consistent.

### 3.3 The Intrinsic Collar Separator Is the Old Boundary Center

v6 Paper 3 then attacked the origin of the null target. The key chain was:

$$
B_x
\to
T_x
\to
R_x
\to
N_x(B_x)
\to
\text{do-delete repair}.
$$

Here:

- `B_x` is the sealed boundary/collar data of the candidate event;
- `T_x(i,j)` is the intrinsic conditional-information transport:

  $$
  T_x(i,j)=I(Y_i;Y_j\mid Y_{\partial x\setminus\{i,j\}});
  $$

- `R_x` is the separator relation obtained from an isolated gap in `T_x`;
- `N_x(B_x)` is the null factorization target over the connected components
  of `R_x`.

This is almost exactly the missing "record-intrinsic center" problem in modern
language. The current `\bar H` bridge object in Paper 29/30 is a teacher for a
boundary center. The v6 collar-separator chain says what the teacher should be
replaced by: intrinsic eventless boundary transport and a minimal complete
record closure.

### 3.4 Two-Screen Bridges Give The Composition Test

v6 Paper 3 showed that a sealed diamond should be an ordered two-screen bridge.
The bridge is:

$$
Q_D(a,b)=P_D(\text{lower}=a,\text{upper}=b).
$$

Adjacent bridges compose exactly when the shared screen is a complete RN
separator:

$$
D\!\left(P_{XYZ}\middle\Vert \frac{P_{XY}P_{YZ}}{P_Y}\right)
=
I(X;Z\mid Y)
=0.
$$

If this fails, there is hidden bypass, missing memory, or event residue.

This is the old form of the v7 projection-sufficiency rule:

> no hidden residue may cross a committed seam for free.

### 3.5 Minimal Complete Screen And No-Silent-Seam

v6 Paper 3 already had the decisive dichotomy:

$$
\epsilon_*=
\min_{Y\in\mathcal C_x}I(X;Z\mid Y).
$$

The law should not say every seam is already complete. It should say:

- eventless seam:

  $$
  \epsilon_*=0
  $$

  with a unique minimal role-blind complete screen;

- eventful seam:

  $$
  \epsilon_*\ge \epsilon_0>0
  $$

  with a stable role-blind residual event/source defect.

This is the cleanest older statement of the current center principle. The
residual diamond-boundary center in Paper 30 is the finite modern version of
the minimal complete RN record closure.

### 3.6 Primitive Event Identity Fixes The One-Diamond Constant

v6 Papers 3 and 4 also closed the primitive one-diamond event law:

$$
E^2=E,
\qquad
\operatorname{Var}_{\mu_D}(q_{\rm raw}\mid E)=0,
\qquad
E\leftrightarrow 1-E.
$$

This makes the primitive contrast count-dual and Boolean:

$$
q=2E-1.
$$

Then Diamond Work-Balance selects the one-diamond constant:

$$
D(P_\eta\Vert\mu_D)=\operatorname{Var}_{P_\eta}(q),
$$

with:

$$
\eta_* = 1.090344354879492,
\qquad
\theta_* = 0.797003794162878,
\qquad
W_*=J_* = 0.364784952089976.
$$

This is not the full click law. It is the primitive event law inside one sealed
diamond.

### 3.7 Division-Event Commitment Gives A Second Exponential

v6 Paper 4 later derived a different exponential law. In additive RN/KL
evidence `I`, eventless gluing forces:

$$
S(I+J)=S(I)S(J),
$$

and exact self-accounting fixes the rate:

$$
S(I)=e^{-I}.
$$

On a complete primitive oriented closed-history ledger, the coefficient law is:

$$
\nabla\psi_G(h_G)=e^{-h_G},
$$

equivalently:

$$
h_G=\operatorname*{argmin}_h
\left(\psi_G(h)+\sum_j e^{-h_j}\right).
$$

This is extremely relevant to the v7 h-transform frontier. Paper 31 still
uses hidden presentation counts as a teacher for the effective h-weight. v6
Paper 4 says what the intrinsic replacement should look like: a primitive
RN/KL quotient ledger with the commitment potential above.

The caveat is equally important. The ledger must be primitive and quotient out
duplicates, serial subdivisions, and cofinally silent vertical modes. Otherwise
the commitment law double-counts evidence or becomes cover-dependent.

## 4. What V7 Added

### 4.1 Scalar Click Law And Martingale Face

v7 Papers 1 and 21 clarified the scalar face:

$$
S(\chi)=e^{-\kappa\chi}
$$

in the dense simple regime, equivalently:

$$
N_\chi-\kappa\chi
$$

is the record martingale.

Paper 21 is the cleaner formulation: the law-level object is the predictable
compensator in the committed-record filtration, not primitive randomness.

### 4.2 Content Supply Is A Coboundary, But Spacing Is Open

v7 Paper 2 showed:

$$
d\chi_i=h_{i+1}-h_i.
$$

So the clock's form is forced as a coboundary, but its magnitude and the seal
spacing are not. The one-diamond capacity gives:

$$
d\le W_*,
$$

but the corpus firing law commits below capacity and does not force
`d=W_*`.

### 4.3 Hidden Machines Are Allowed Only Through Record Projection

v7 Paper 20 showed that deterministic hidden machines can realize the same
record-facing law. This does not weaken the law. It strengthens the projection
principle:

> hidden mechanisms are allowed only if their committed-record projection has
> the right compensator.

This is the deterministic version of the later projected-likelihood rule.

### 4.4 Record Sufficiency Is The Organizing Principle

v7 Paper 28 named the general principle:

$$
\text{contiguous pushforward}
\quad\text{or}\quad
\text{committed residue}.
$$

If a hidden refinement has bounded record second moment, it washes out as
representation. If not, the divergent likelihood residue is a missing sector,
mark, action, or boundary center.

### 4.5 Projected Likelihood Is The Effective Law

v7 Paper 29 made the exact finite statement. If:

$$
\widetilde L_N
=
\frac{d\widetilde Q_N}{d\widetilde P_N},
$$

then the record-visible likelihood is:

$$
L_N(\pi_N(\omega))
=
\mathbb E_{\widetilde P_N}
\left[
\widetilde L_N\mid \pi_N
\right](\omega).
$$

Hidden residue orthogonal to this projection is not a law-level object unless
it is promoted to committed records.

For any proposed record-sector filtration `G`, the predictor is forced:

$$
L_{N,G}
=
\mathbb E_{P_N}[L_N\mid G].
$$

The remaining problem is not choosing a statistic. It is finding the projective
filtration `G` whose increments are controlled and non-reconstructive.

### 4.6 Paper 30-31 Found The Finite Diamond-Shadow Form

Paper 30 rebased the v7 flag work onto the older diamond idea:

> the click law is the positive h-transform shadow of the minimal licensed
> diamond-boundary center.

Paper 31 then closed the exact audited finite law:

```text
base atoms=131514/131526
diamond_center_atoms=131518/131526
center_conflicts=0
selected_shadow_atoms=65521/131526
TV9=0
rec9=0
qrec9=0
```

The finite shadow at `N=9` is generated by selected dual sectors:

$$
\mathcal G_9
=
\{(912,25104),(17288,525076),(24576,540672)\},
$$

and selected odd metric:

$$
M_9=\operatorname{diag}(5,5,3).
$$

But Paper 31 also proved the guardrail: finite evidence through `N<=9` cannot
prove universal uniqueness. A formal law can agree through the audited window
and diverge at `N=10`.

## 5. The Recovered Architecture

The pieces now align as follows.

### 5.1 Ontology

The primitive event is a sealed finite record diamond, not a bare point, not a
flag count, and not a hidden presentation.

### 5.2 Center

The center is the minimal boundary/collar refinement required by no-silent
residue:

$$
\mathcal C_N
=
\text{minimal complete diamond-boundary center}.
$$

In v6 language this is the minimal complete RN record closure. In v7 language
it is the residual diamond-boundary center.

### 5.3 Shadow

The physical law cannot use the full center if the full center reconstructs too
much. It uses a positive non-reconstructive shadow:

$$
\mathcal P_N\preceq \mathcal C_N.
$$

The shadow is selected by recurrence, reflection positivity, dual symmetry,
coarseness, density/regularity, and a reconstruction budget.

### 5.4 Weight

The hidden teacher is presentation count:

$$
\operatorname{pres}(R).
$$

The physical replacement should be an intrinsic h-weight:

$$
h_{\mathcal P_N}(R)
$$

defined on the selected shadow. v6 Paper 4 suggests the intrinsic generation
law should be an RN/KL commitment potential:

$$
\nabla\psi_{\mathcal P_N}(h)=e^{-h},
\qquad
h=\operatorname*{argmin}\left(\psi+\sum_j e^{-h_j}\right),
$$

but v7 has not proved that this construction equals the effective h-weight
needed for forward record growth.

### 5.5 Transition

Given `h`, forward growth is:

$$
\Pr(P\mid C)
\propto
D(P,C)h_{\mathcal P_{N+1}}(P).
$$

This is the h-transform form of the click law.

## 6. What This Means For The Search

The older papers change the next target.

The target is not:

$$
\text{find the right finite flag list}.
$$

The target is:

$$
\text{derive the minimal no-silent diamond center and its admissible positive shadow}.
$$

The older v6 screen/collar machinery likely supplies the intrinsic definition
of the center. The newer v7 h-transform machinery supplies the forward law.
The missing bridge is proving that the v6 center generates the v7 shadow and
the v7 h-weight without hidden presentation teachers.

## 7. What Is Still Missing

### 7.1 Intrinsic Center Theorem

Prove, for the physical record class, that every diamond has a record-intrinsic
candidate closure lattice and satisfies the no-silent dichotomy:

$$
\epsilon_*=0
\quad\text{or}\quad
\epsilon_*\ge\epsilon_0>0.
$$

Then define:

$$
\mathcal C_N
$$

without hidden presentation labels, hidden scores, or post-hoc flags.

### 7.2 Projective Shadow Theorem

Prove that the centers admit a projectively stable family of positive shadows:

$$
\mathcal P_N\preceq\mathcal C_N
$$

with controlled deletion/insertion drift and a non-reconstruction budget.

### 7.3 Intrinsic h-Weight Theorem

Replace hidden presentation counts by a record-intrinsic h-weight. The strongest
candidate is the v6 commitment law:

$$
\nabla\psi(h)=e^{-h}.
$$

The theorem must show that this h-weight is the same object, or the correct
replacement, for the effective forward weight in the h-transform.

### 7.4 Physical Breadth

The finite proof is currently in the audited 2D permutation-order sector. The
universal law must survive other dimensions, curved/manifoldlike regimes,
marked records, entanglement sectors, and gravity/stress sectors.

### 7.5 Non-Uniqueness Beyond Finite Windows

Finite exactness through `N<=9` is not enough. Paper 31 explicitly constructs
the finite-window guardrail. The universal theorem has to be structural, not
empirical over a finite table.

## 8. Proposed Next Campaign

The most direct campaign is:

1. Translate v6 minimal complete RN closure into the exact v7 boundary-center
   language.
2. Replace `\bar H` as a hidden-score teacher with an intrinsic boundary
   transport, likely a conditional-information/collar-separator object.
3. Test whether the resulting center reproduces the Paper 30 `N=9` center
   without hidden presentations.
4. Build the shadow by the Paper 31 admissibility gates.
5. Attempt to generate the shadow h-weight from the v6 commitment potential
   instead of presentation counts.
6. Stress-test against the formal `N=10` divergence guardrail.

If this succeeds, the click law becomes:

> construct the minimal no-silent diamond-boundary center; choose the coarsest
> reflection-positive non-reconstructive recurrent shadow; generate its
> h-weight by primitive RN/KL commitment; use the resulting h-transform.

If it fails, the failure should say exactly which part of the old v6
architecture cannot be made intrinsic in the v7 record-order setting.

## 9. Bottom Line

Yes: previous v6-v7 papers give a large part of the solution.

The solution was split across two languages:

- v6 had the diamond, screen, collar, no-silent seam, primitive event, and
  RN/KL commitment machinery.
- v7 had the scalar martingale, projected likelihood, controlled filtration,
  h-transform, and finite diamond-shadow witness.

The missing law is the fusion:

$$
\boxed{
\text{click law}
=
\text{positive h-transform shadow of the minimal no-silent diamond center}
}
$$

with the h-weight generated intrinsically rather than by hidden presentation
counts.

That is not yet proved universally. But it is now a much better target than
"find the right flags" or "fit another scalar action."

## 10. Bridge Campaign: No-Silent Center To Positive Shadow

Receipt:

```text
isp/v7/code/p32_v6_v7_diamond_law_bridge.py
```

The campaign tested the fusion target directly:

$$
\text{click law}
=
\text{positive h-transform shadow of the minimal no-silent diamond center}.
$$

It did not try to rerun the expensive `N=9` enumeration. Paper 30 already did
that. Instead it audited the missing conceptual bridge:

1. Can the v6 no-silent seam be represented as a concrete
   conditional-information center?
2. Is the v7 finite diamond shadow compatible with that center picture?
3. Does v6 RN/KL commitment supply a plausible h-weight while remaining
   distinct from the one-diamond Diamond Work-Balance constant?

All finite probabilities were exact rational numbers. Logs, exponentials, and
roots used Decimal precision `140`, which is above the requested 80-bit floor.

### 10.1 Exact No-Silent Seam Model

The receipt constructed a finite law with four variables:

- `X`: the lower-side record bit;
- `S`: the visible seam/screen bit;
- `H`: a hidden-but-boundary-definable center bit;
- `Z=(S,H)`: the upper-side boundary readout.

The visible seam `S` is incomplete:

$$
I(X;Z\mid S)
=
0.085111494415824107925552963695511919514815453187793769080502507292894721897523629449941688461405475243354406988840293826900794688794059111355.
$$

But the minimal center `(S,H)` closes the seam:

$$
I(X;Z\mid S,H)=0.
$$

This is exactly the v6 no-silent idea in miniature. If a boundary leaves
residue, the law is not allowed to pretend the residue is absent. It must
either wash out or become a licensed center. Here it becomes a licensed center.

The important feature is that `(S,H)` is not an external label oracle. It is a
larger boundary partition: `S` has two cells, while `(S,H)` has four intrinsic
boundary cells.

### 10.2 Compatibility With The V7 Diamond Shadow

The receipt then imported the exact audited facts from Papers 30 and 31:

```text
records_9              = 131526
base_atoms_9           = 131514
diamond_center_atoms_9 = 131518
selected_shadow_atoms_9= 65521
residual_edges_9       = 4
shadow_edges_covered   = 2
licensed_orbits        = 11/24
selected metric        = (5,5,3)
TV9 = rec9 = qrec9    = 0
```

This says:

- the diamond center is a proper residual closure:

  $$
  131514 < 131518 < 131526;
  $$

- the selected shadow is nonreconstructive:

  $$
  65521 < 131518;
  $$

- the shadow does not merely encode the whole residual graph:

  $$
  0 < 2 < 4;
  $$

- the selected positive shadow still gives the exact held-out forward law:

  $$
  TV9=rec9=qrec9=0.
  $$

So the finite v7 result is compatible with the v6 no-silent-center reading:
the law does not need the whole center as a state variable. It needs a
positive, recurrent, nonreconstructive shadow of the center.

### 10.3 Commitment Exponential Is Not The Same Constant

The receipt also tested the v6 RN/KL commitment exponential:

$$
S(I+J)=S(I)S(J),
\qquad
S(I)=e^{-I}.
$$

At precision `140`, the gluing residual was:

$$
0E-140.
$$

It then solved the commitment h-root:

$$
\tanh(h)=e^{-h},
$$

giving:

$$
h_{\rm commit}
=
0.6093778634360062315368033711683986954285392793128541477762892366225152051203195768397057073419585497249233347078073628047232738007518566486.
$$

It separately solved the one-diamond Diamond Work-Balance equation:

$$
C(\eta)=J(\eta),
$$

giving:

$$
\eta_*
=
1.0903443548794921962205077083509499533582197857502947304430449706378495187012211801530749701632256720938879002552211976806351593056361869318,
$$

and:

$$
W_*
=
0.36478495208997636225717716051224804565748308899359420899463605906130941182160023387193116674509464583937207258828893058723287776927312987592.
$$

The gap is:

$$
|\eta_*-h_{\rm commit}|
=
0.4809664914434859646837043371825512579296805064374405826667557340153343135809016033133692628212671223689645655474138348759118855048843302832.
$$

This matters. The old papers contain two different exponentials:

- one-diamond event saturation / work-balance;
- RN/KL commitment survival across record evidence.

The bridge campaign confirms they should not be collapsed into one constant.
The candidate h-weight should come from commitment/survival, while the
one-diamond constant constrains the primitive event capacity.

## 11. Hostile Review Of The Bridge Campaign

### Objection 1: The Seam Example Is Only A Toy

Sustained.

The finite `X,S,H,Z` model proves logical compatibility, not the physical
center theorem. It shows what a no-silent center is supposed to do. It does not
prove that the `N=9` diamond center is generated by a record-intrinsic collar
separator.

Required theorem:

$$
\mathcal C_N
=
\text{minimal record-intrinsic closure that kills no-silent seam residue}.
$$

### Objection 2: The N=9 Facts Still Import A Hidden Teacher

Sustained.

The finite diamond center and shadow facts still rely on the audited Paper 30
teacher construction. The bridge campaign shows that this teacher has the right
shape, but it has not replaced it by an intrinsic v6-style transport:

$$
B_x\to T_x\to R_x\to N_x(B_x).
$$

Required theorem:

$$
\bar H_N
\quad\text{is recoverable from intrinsic boundary/collar transport}.
$$

### Objection 3: The Commitment Root Does Not Yet Produce The Forward Weight

Sustained.

The receipt shows that commitment gives a clean h-root and gluing law. It does
not show that this h is the effective forward likelihood needed by:

$$
\Pr(P\mid C)
\propto
D(P,C)h(P).
$$

Required theorem:

$$
h_{\rm commitment,\mathcal P_N}
=
h_{\rm effective,\mathcal P_N}.
$$

### Objection 4: Exactness At N=9 Still Cannot Prove Universality

Sustained.

Paper 31 already proved the finite-window guardrail: two formal laws can agree
through `N<=9` and diverge at `N=10`. The universal result must be a structural
admissibility theorem, not another finite fit.

## 12. Updated Law Target

The strongest current candidate is now:

1. Given a committed finite record diamond, compute its boundary/collar
   transport.
2. Generate the minimal no-silent center:

   $$
   \mathcal C_N.
   $$

3. Select the coarsest admissible positive shadow:

   $$
   \mathcal P_N\preceq\mathcal C_N,
   $$

   subject to:

   - controlled deletion/insertion drift;
   - reflection positivity;
   - order-dual compatibility;
   - recurrence/projective stability;
   - non-reconstruction;
   - density/regularity.

4. Generate the h-weight on that shadow from RN/KL commitment:

   $$
   S(I)=e^{-I},
   \qquad
   \nabla\psi(h)=e^{-h}.
   $$

5. Use the h-transform:

   $$
   \Pr(P\mid C)
   =
   \frac{D(P,C)h_{\mathcal P_{N+1}}(P)}
   {\sum_{P'}D(P',C)h_{\mathcal P_{N+1}}(P')}.
   $$

This is not yet a theorem. It is now a precise theorem-shaped target.

## 13. What The Campaign Actually Advanced

The advance is not "we found the final click law."

The advance is:

1. The old v6 no-silent seam machinery can be read as the missing
   record-intrinsic center principle.
2. The v7 diamond-shadow result is a nonreconstructive quotient of that kind
   of center, not a replacement for it.
3. The old v6 RN/KL commitment exponential is the right kind of h-weight
   generator, but it is not the same as the primitive Diamond Work-Balance
   constant.
4. The exact missing bridge is now narrower:

   $$
   \boxed{
   \text{intrinsic boundary/collar transport}
   \Rightarrow
   \mathcal C_N
   \Rightarrow
   \mathcal P_N
   \Rightarrow
   h_{\mathcal P_N}
   }
   $$

   with no hidden presentation counts.

That is the real next proof target.
