# The record click-law, XIII: the Feynman map of the entangling input bottoms out at the almost-quantum envelope — a downstream no-go for `χ_AB`, with a connectivity floor and a manifoldlikeness obligation

**Author:** Felix Robles Elvira (ORCID: 0009-0009-2017-4394; independent researcher)

**Status:** preprint, not peer reviewed, version 2026-06-15. Thirteenth paper of the v7 program; the *downstream* sequel to Paper XII, which proved that no *transverse* self-consistency principle forces the entangling content `χ_AB`. Here we ask the operational ("Feynman") question Paper XII left open: dial `χ_AB` across the almost-quantum envelope `Q̃`, push each value through the *whole* machine — emergent geometry, the correlation principles, covariance, the matter sector, higher-order graph invariants, and the causal-order dynamics — and find every physical and logical constraint it must meet. Pre-geometric throughout: no spacetime, metric, light-cone, or interval `s²` is assumed; every quantity is a record-internal probability / Kullback–Leibler number. Tags: **[FORCED]** = uniquely required; **[CONSTRAINED]** = bounded but not pinned; **[NO-GO]** = a proved impossibility within the stated scope; **[OPEN]** = a disclosed, unproved obligation; **[EMPIRICAL]** = fixed by experiment, not by the records. All numeric claims at mpmath `dps ≥ 120` (140 where cancellation-heavy) and sympy-exact where structural; semidefinite-program corroborations are at solver (SCS) tolerance `~10⁻⁹`, every such digit flagged and the underlying PSD/Gram logic exact. Receipts `v7/code/s_probe_qtilde_minus_q.py`, `s_geom_chi_sweep.py`, `c3_covariance_chi_ab_probe.py`, `s_order_feedback_probe.py` (79 machine checks total).

## Abstract

Paper XII proved a *scoped* no-go: no *transverse* self-consistency principle — a parallel-chain analog of the single-chain capacity crossing — forces the entangling content `χ_AB = I_σ`, which stays free up to the almost-quantum envelope `Q̃`. That left the operational question open: does the *rest of the machine* constrain `χ_AB`? Treat `χ_AB` as a **dial**, sweep it across `Q̃`, and push every value through the full downstream apparatus. The decisive probe is a **super-quantum witness** `P*` in `Q̃ ∖ Q` (an `I3322` correlation feasible at the records' level-`(1+AB)` but provably outside the quantum set `Q`): if anything downstream breaks at `P*`, global consistency would *select* `Q` — recovering the tensor product as derived rather than postulated. **It does not.** Pushed through geometry (the entanglement graph, the hop-metric, manifoldlikeness diagnostics), the correlation principles (no-signaling, the static Information-Causality core, macroscopic locality, local orthogonality), covariance (no-signaling-under-boosts, microcausality), the matter sector (the mode fixed point, the chiral gap), and a higher-order *tripartite* invariant, `P*` is **indistinguishable** from a quantum point with the same level-`(1+AB)` data. The structural spine: **every downstream sector is a functional of the field-blind moment algebra** `M = ⟨{1, A_x, B_y, A_x B_y}⟩_ψ`, an *invariant* of the qubit→rebit field-reduction `R`; the local-tomography (complex-over-real) selecting bit — the deficit `K_AB − K_A·K_B` (`+1` over `ℝ`, `0` over `ℂ`, sympy-exact) — lives in the **kernel** of `M`, consumed by no sector. We also close the genuine frontier where a break could have hidden: the **order-feedback** path. The causal order's generator `σ = D(P_AB‖P_BA)` is a *longitudinal* per-chain transport functional whose variable set is sympy-exactly **disjoint** from `χ_AB`'s *transverse* joint-outcome variables; across 200 entanglement-graph re-wirings the causal order never permutes; and an adversarial surrogate confirms a `χ`-driven order is *constructible* but is a different, unbuilt generator the click-law's forced odometer excludes. So the Feynman map **bottoms out at `Q̃`**, and `χ_AB` is thrice-proven free (transverse, downstream, holonomy). Two genuinely-new constraints survive, both **orthogonal** to the residual bit and both absent from the device-independent literature (which is single-scenario and geometry-blind): a **connectivity floor** — `χ_AB = 0` forces a disconnected graph, so a connected emergent geometry *requires* entanglement (a measure-zero face, not the `Q/Q̃` gap) — and a **manifoldlikeness obligation** — the multi-record `χ_AB` *magnitude pattern* must be manifold-like, but Kleitman–Rothschild dominance (`2^{n²/4}` orders) makes that an unmet *dynamical-selection* rule on a different axis. The single residual is **Tsirelson's problem**: the tensor product + local tomography = complex-over-real Hilbert space, the field-wide `Q`-vs-`Q̃` wall that *no* device-independent principle reaches. The records do not derive it; they **isolate exactly the one bit that must come from outside**, and whether even experiment fixes it is now a live 2026 controversy: Renou et al. (Nature **600**, 625 (2021)) read in its own framing as ruling out real QM, but Hoffreumon–Woods (arXiv:2603.19208) and Maioli–Curado–Gazeau (arXiv:2604.19482) argue real quantum theory under a Kronecker/symplectic composition rule reproduces all network correlations (Renou et al. defend their postulate in arXiv:2604.07425) — so the complex-over-real bit is plausibly an un-fixable composition-rule convention, which **vindicates rather than threatens** the SHARD verdict that the tensor product is the un-forceable field-blind import (in `\ker R`, weight-`0`). Honestly carried open loci (scope, not breaks): the level-`(1+AB)` single-joint-outcome-pair commitment premise (where the actual `Q/Q̃` gap lives, degree `> (1,1)`), and the unbuilt two-party interacting-matter sector (field-blind *a fortiori*).

---

## 1. The Feynman question, and the headline [FORCED · setup]

Paper XII forced a *transverse* result: the single-chain capacity crossing that pins `W_*` has **no** parallel-chain instance that fixes `χ_AB = I_σ`, by two weight-`0` mechanisms (capacity blindness; field blindness). `χ_AB` therefore stays free up to the almost-quantum envelope `Q̃`, which the records force *from inside* via moment-positivity `Γ ⪰ 0`. That settled the question of *deriving* `χ_AB` from self-consistency. It did not settle the operational question: of all the values `χ_AB` is *permitted* to take, which survive coupling to **the rest of the program** — the emergent geometry, the matter sector, covariance, the dynamics? This paper runs that test in the most concrete way available, the way Feynman would: pick *fully specified* values, push them through the machine, and watch what breaks.

The arena (Paper VI / companion to Paper VI). Two record chains commit binary contrasts `a, b ∈ {−1, +1}`; they meet **only at shared cross-moments** — marginals `⟨A_x⟩, ⟨B_y⟩` and correlators `⟨A_x B_y⟩ = E_xy` — coupled by the on-state commutation `B_y A_x = A_x B_y`, *not* a Hilbert tensor factorisation. Every record-internal observable is therefore a functional of the **level-`(1+AB)` moment algebra** `M = ⟨{1, A_x, B_y, A_x B_y}⟩_ψ`. The transverse self-consistency `Γ ⪰ 0` on `M` *is* the almost-quantum set `Q̃ = Q^{1+AB}` (Navascués–Guryanova–Hoban–Acín 2015), which strictly contains the quantum set `Q`. The **field-reduction** `R` maps a complex-qubit realisation to a real-rebit one; it is the symmetry that exposes what the records can and cannot see.

**The dial positions.** We sweep four fully-specified `χ_AB` values: `(a)` `χ_AB = 0` (product); `(b)` a generic quantum point `P_Q ∈ Q`; `(c)` the decisive **super-quantum witness** `P* ∈ Q̃ ∖ Q` — the **Collins–Gisin `I3322`** correlation (projector/probability form; 3 inputs / 2 outcomes per party; local bound `0`) whose level-`(1+AB)` optimum `0.2514709` strictly *exceeds* the converged quantum value `Q ≈ 0.2508754` (NPA level `≥ 4`, Pál–Vértesi 2010), so the optimal correlation is feasible at the records' level (in `Q̃`) yet outside `Q`, extracted as an explicit probability table `p(a,b|x,y)`; and `(d)` a supra-`Q̃` PR-box (`CHSH > 2√2`), which `Γ ⪰ 0` already excludes. The headline:

> **The Feynman map bottoms out at `Q̃`.** Pushed through every downstream sector, the super-quantum witness `P*` is indistinguishable from a quantum point `P_Q` carrying the same level-`(1+AB)` data. Global consistency adds two genuinely-new constraints (a connectivity floor, a manifoldlikeness obligation), both orthogonal to the `Q/Q̃` bit, and reaches no further. The one residual bit is the field-wide tensor-product / local-tomography wall, whose experimental fixability is itself now contested (Hoffreumon–Woods arXiv:2603.19208, Maioli et al. arXiv:2604.19482; Renou Comment arXiv:2604.07425; Renou 2021 stands in its own framing) — plausibly an un-fixable composition-rule convention, which vindicates (not proves) the un-forceable-import verdict.

The remainder proves this sector by sector, and — crucially — closes the one frontier where a downstream break could have hidden (§4).

---

## 2. The spine: every sector reads only the field-blind algebra `M` [NO-GO]

The result is, at bottom, one structural fact, and the numbers only illustrate it. **`M` is an invariant of the field-reduction `R`.** At the relevant correlators `R` preserves every element of `M` — the marginals `(½, ½)`, every cross-moment, the orthogonal-projection seal `E = E² = E*`, the Tsirelson point, and the sealing scalar `I_σ` itself (`I_σ(rebit) − I_σ(qubit) = 0`, verified to `< 10⁻¹¹⁸`, Paper XII §5) — while changing **only** the local-tomography count: the information-dimension deficit `K_AB − K_A·K_B` is `+1` over `ℝ` (local tomography fails: `K_{2\text{-rebit}} = 10 ≠ 9`) and `0` over `ℂ` (`K_{2\text{-qubit}} = 16 = 4·4`), a sympy-exact integer fact. The one bit that distinguishes `Q̃` from `Q` lies in the **kernel of every record functional's factorisation through `M`**.

Consequently any downstream sector that is *itself* a functional of `M` is blind to that bit and cannot separate `P*` from `P_Q`. Receipt `s_probe_qtilde_minus_q.py` (26/26) makes this concrete on the explicit witness. The super-quantum `P*` is the **Collins–Gisin `I3322`** level-`(1+AB)` optimum: building the *true* level-`(1+AB)` moment matrix over `{1, P_A[x], P_B[y], P_A[x]P_B[y]}` (the base words **plus all nine cross-products** — not the bare first NPA level) and maximizing the CG functional yields `0.2514709`, which strictly *exceeds* the converged quantum value `Q ≈ 0.2508753845` (NPA level `≥ 4`; the full level-2 relaxation already caps it at `0.2509397`, itself a rigorous upper bound on `Q` — so `P*` exceeds even that *self-contained* outer bound by `+5.3 × 10⁻⁴`, certifying it outside `Q` without relying on any external digit). So `P*` is feasible at the records' level — `Γ ⪰ 0`, hence in `Q̃`, records-*admissible* — yet attains a value above the quantum maximum, hence outside `Q`: a genuine, fully-specified `Q̃ ∖ Q` correlation, extracted as a probability table and pushed *by its own probabilities* through the sectors. [These SDP digits are SCS solver-tolerance `~10⁻⁹`, flagged; the PSD/Gram logic, the level-2 `⪰ Q` upper bound, and the `I3322` level-`≥ 4` fact (Pál–Vértesi 2010) are field-established, not re-derived. An honest one-line contrast in the receipt: the *±1-correlator* `I3322` has classical max `=` quantum max `=` level-`(1+AB)` `= 8.0` — **no** quantum-over-classical gap — which is why the projector form is the right witness, and why the bare first-level value `8.748` of that encoding sits *above* `Q̃` and is records-inadmissible.] Pushed through the four sectors, every `M`-functional returns a value a quantum `P_Q` with the same level-`(1+AB)` data must also return:

- **Geometry** — the entanglement graph read off `P*`'s own per-setting mutual informations `I(A_x:B_y)` (a non-vacuous moment functional that moves across settings; a connected graph): identical for any `M`-equivalent quantum point.
- **Correlation** — the static Information-Causality core (chain-rule residual `0` on a ledger built from `P*`'s own no-signaling marginals), macroscopic locality, local orthogonality: all satisfied at `P*`.
- **Covariance** — no-signaling residual `0`; a boost (an order-automorphism) preserves `386 = 386` causal relations (full §3).
- **Matter** — the mode fixed point `η_B = 0.609377863436…`, the gap `W = 0.156109200157…`, the chiral `m̂ = \ln 2`: independent of `χ_AB`.

**An honesty that controls the whole paper.** The *numeric* sector-by-sector identity is true **by construction** — `P*` and `P_Q` are realised by the same level-`(1+AB)` data, differing only in the kernel flag, so any `M`-functional must return the same value. The load-bearing claim is therefore not the numbers; it is the **structural** statement that each sector *is* an `M`-functional, made good sector by sector below. The numbers are the illustration that confirms it, not independent evidence. One further caveat we do not overstate: **macroscopic locality reaches only `Q¹`** (the first NPA level), so it cannot exclude `P*`; we assert only that `P*` *satisfies* it, consistent with the field result that no device-independent principle reaches `Q`.

---

## 3. Covariance recovers `Q̃` and breaks nothing [NO-GO · conditional on the Tier-B gates]

The relativistic sector is the one place a super-quantum correlation might plausibly break — super-quantum boxes are famous for trivialising communication complexity and for relativistic-causality pathologies. It does not, and the reason is exact. Receipt `c3_covariance_chi_ab_probe.py` (15/15):

1. **No-signaling is a Lorentz scalar.** The single-party marginal is a frame-independent count, so no-signaling-static `⟺` no-signaling-under-boosts for *any* box, including the super-quantum PR-box (gap `0`, exact). Covariance imposes nothing on a box beyond no-signaling.
2. **Microcausality *is* the on-state commutation that defines `Q̃`.** The spacelike condition `[h_A(x), h_B(y)] = 0` reduces (sympy-exact) to `[A_0, B_0]`, and the commuting realisation has `⟨ψ| A_0 B_0 |ψ⟩ − ⟨ψ| B_0 A_0 |ψ⟩ = 0`. We witness this commutation **without smuggling the tensor product** it is *weaker* than: abstractly, in the free non-commutative algebra (the relation `[A_x, B_y] = 0` alone makes the cross-moment single-valued), and concretely on a **single Hilbert space of prime dimension `3`** — where *no* tensor factorisation `H_A ⊗ H_B` exists at all — flagging that a tensor realisation is sufficient but not necessary. So covariance and `Γ ⪰ 0` are **two faces of the same constraint** — which is exactly why covariance recovers `Q̃` and not `Q` (commutation-on-the-state `≠` tensor factorisation; Tsirelson's problem).
3. **The witness passes.** `P*` is no-signaling, satisfies on-state commutation by construction, and is **sub-Tsirelson** (its `CHSH` block is `1.339 ≤ 2√2`), so the van Dam / Brassard communication-complexity collapse — which needs `CHSH > 2√2` — does not bite. And `P*` is records-*admissible*: as the level-`(1+AB)` linear-functional optimiser it is a `Q̃`-*boundary* point (its native moment matrix is singular), feasible under a hard-equality pin to the solver floor (`≈ −2 × 10⁻⁹`) and with a `+1.1 × 10⁻⁷` margin under a small moment band — so it lies in `Q̃` (boundary membership is membership) and covariance breaks nothing on it.

The communication-complexity and relativistic-causality teeth therefore act only *outside* `Q̃` (`CHSH > 2√2`), which `Γ ⪰ 0` already excludes. (We quarantine the emergent-Lorentz-action and point-locality premises as the same Tier-B gates Paper VII carries; this section is conditional on them, not on anything new.)

---

## 4. The frontier closed: the causal order is independent of `χ_AB` [NO-GO for the SHARD generator · OPEN under redefinition]

The one place a downstream break could genuinely have hidden is a **feedback**: if the dynamics generating the causal *order* were driven by the transverse `χ_AB` pattern — entanglement-graph connectivity feeding back into which seals are causally ordered — then a super-quantum pattern could imprint a pathological order that the static sector-by-sector test would miss. We close this at three grades (`s_order_feedback_probe.py`, 18/18):

- **Structural (sympy-exact).** The causal order's generator is the longitudinal entropy-production arrow `σ = D(P_AB‖P_BA)`, a per-chain transport functional of the commit-order transition entries `{p_ij}`. The entangling content `χ_AB` is a transverse functional of the joint-outcome variables `{λ, η_a, η_b}`. **The two variable sets are disjoint**, so every cross-partial `∂σ/∂χ_AB` is identically zero; a coupled generator `σ + g·F(χ)` has `g = 0` *forced* by the corpus definition `σ := D(P_AB‖P_BA)`.
- **Feedback (numeric).** Across 200 random transverse entanglement-graph re-wirings (with `χ_AB` up to `O(1)`), the `σ`-ranking of `K = 6` seals with distinct `σ` is permuted **zero** times. And `σ`-spread is exactly `0` across a `χ_AB` sweep in which `χ_AB` moves `0 → 0.3657` and `E_xy` moves by `1.295` at *fixed* single-chain marginals `(0.3, 0.2)`.
- **Non-vacuity (the critical check).** The test is not vacuous: an adversarial surrogate generator (a `χ`-incidence rank) genuinely produces a *different* ordering (`[0,4,3,5,1,2]` versus `σ`'s `[4,3,0,2,1,5]`). So a `χ`-driven causal order **is** mathematically constructible — it is simply **not** the SHARD generator.

The honest residual is **definitional, not a tolerance gap**: the closure is theorem-grade for the *built* generator `σ = D(P_AB‖P_BA)`; it would re-open under a redefinition to a `χ_AB`-driven generator, which is a *different, unbuilt* dynamics that Paper I §3.2 already excludes by forcing the odometer. So the order-feedback path is closed for the records' own order, and the one escape is an unbuilt alternative dynamics — scope, not an open numerical gap.

---

## 5. What global consistency *does* add: two new constraints [NEW]

Global consistency is not toothless — it carves `Q̃` from above at two places the device-independent literature cannot reach (because that literature is single-scenario and geometry-blind). Both are real, and both are **orthogonal** to the `Q/Q̃` field bit (`s_geom_chi_sweep.py`, 20/20).

**(1) The connectivity floor [lower bound].** Edges of the emergent geometry are mutual-information bonds (`edge ⟺ I(i:j) > 0`). The edge weight is sign-blind — `I(+0.85) = I(−0.85) = 0.42676…` (sympy-exact via the outcome-relabel involution) — and `I` rises from `0` (product) to `\ln 2` (PR-box). The pinch is stated *honestly*: a **single** zero bond does **not** disconnect — it removes one edge, and the node stays attached through its other bonds (verified: `E_{02} = 0` only, with `E_{01}, E_{12} ≠ 0`, gives **one** component). Genuine isolation requires `χ_AB = 0` on **all** of a node's bonds — a *globally product node* — which does pinch off (`E_{02} = E_{12} = 0` gives **two** components, the **Van Raamsdonk pinch**). **A connected emergent geometry therefore requires `χ_AB ≠ 0`** — a constraint that the transverse-only no-go (Paper XII *permitted* `χ_AB = 0`) and the device-independent program (silent on geometry) both lack. But it carves the **measure-zero face** `{χ_AB = 0}`, *not* the positive-measure `Q/Q̃` gap: at equal edge weight a quantum and an almost-quantum point give the identical graph (`I(0:1) = 0.19274…`, gap `0`).

**(2) The manifoldlikeness obligation [global].** For a *smooth* spacetime to emerge, the multi-record `χ_AB` **magnitude pattern** must be manifold-like — but Kleitman–Rothschild dominance makes that rare: at `n = 10⁴` the number of partial orders carries `~n²/4 = 2.5 × 10⁷` bits against `~n\log n = 5.3 × 10⁵` bits of manifold-embedding data, so almost all patterns are non-manifold "junk." This is a genuinely-new lever the single-scenario device-independent program cannot have. Its `R`-invariance is **structural, not a numeric coincidence**: the Myrheim–Meyer diagnostics are functionals of the magnitude-induced *order* alone (`r = 0.52086`, `d_MM = 1.9453`, manifold-like in `[1.7, 2.3]`, longest-chain height `44` for one sprinkle, reported as decorative), and Paper XII §5 / Paper VI §3 prove `R` preserves every edge magnitude exactly — so `R` is a literal no-op on the magnitude pattern and the diagnostics are `R`-invariant *by construction* (which inputs they read), not by feeding identical arrays. The diagnostic is **non-vacuous** — a genuinely different magnitude pattern (a 1d-like, near-totally-ordered sprinkle) gives a different `d_MM ≈ 1.0` — so the invariance is informative. So manifoldlikeness carves along the **dynamical-selection** axis (does the click-law beat KR-dominance? — the field-shared open problem of Paper XI §4, on which the click-law has no proven lever), which is **orthogonal** to the transverse complex-over-real axis.

**Do-not-conflate (policed throughout).** The tensor product `≠` local tomography (a rebit theory *has* a tensor product yet *fails* local tomography — two distinct axioms). The manifoldlikeness selection-bit `≠` the complex-over-real local-tomography bit (different axes: longitudinal order-combinatorics vs transverse correlator). A "multi-record / multi-node" invariant is not a "higher field-resource" (more *nodes*, same per-pair commitment). `CHSH` alone does not witness `Q̃ ∖ Q` (one needs `I3322` at level `≥ 4`). And a supra-`Q̃` monogamy-violating star's graph pathology is **not** a new carving — it is already `Γ ⪰ 0`-excluded.

---

## 6. The tripartite closure: more nodes do not open the gap [NO-GO · premise-scoped]

A natural worry: perhaps a *higher-order* graph invariant — one reading three records at once — sees the local-tomography bit that the pairwise data hides. It does not, at the commitment level the records host. Receipt `s_geom_chi_sweep.py` part (iv): on a valid degree-`(1,1)` tripartite no-signaling click-law `p(abc) = \tfrac18(1 + ab\,E_{AB} + ac\,E_{AC} + bc\,E_{BC} + abc\,E_{ABC})`, the **interaction information** `II(A{:}B{:}C) = I(A{:}B) − I(A{:}B|C)` and the triangle invariant `Tri = I(AB)+I(AC)+I(BC)` **factor through `M`**: they are bit-identical (gap `0`) across the qubit (complex) and rebit (real) realisations of the same joint moments. The check is **non-vacuous** — `II` is a genuine moment functional that *moves* (`−0.00201 → 0.00962` when `E_{ABC}: 0.15 → 0.05`), so the field-invariance is a real coincidence, not `0 = 0`.

The honest scope, stated plainly: this closes the tripartite path **only at degree `(1,1)`** — it shows that more *nodes* at the same single-joint-outcome-pair commitment do not open the gap. It does **not** show that a higher cross-party *word* (degree `> (1,1)` seal projectors) cannot host non-reduced structure — and the *actual* `Q/Q̃` separation lives precisely there (`I3322` needs NPA level `≥ 4`). This is the one place the no-go is conditional on a premise the corpus has not discharged (Paper XII §9): the level-`(1+AB)` single-joint-outcome-pair commitment premise. Multi-node `≠` higher field-resource; we carry the premise openly rather than claim it closed.

---

## 7. The residual is one bit, and whether experiment fixes it is now contested [OPEN]

Every internal lever — the four downstream sectors (§2–3), the order-feedback dynamics (§4), and the higher-order invariant (§6) — bottoms out at `Q̃`. So **no internal SHARD principle selects `Q`**; the residual `Q`-vs-`Q̃` gap is a single bit, and it is the right one: the **tensor product + local tomography = complex-over-real Hilbert space = Tsirelson's problem**. This is not a SHARD-specific weakness — it is the **field-wide wall**: device-independent principles (Information Causality, here owned only as its static core and numerically established for `Q̃`; macroscopic locality, which reaches only `Q¹`; local orthogonality) all reach `Q̃` and stop (NGHA 2015). SHARD sits exactly at that frontier, owning the `Q̃` envelope from inside.

What SHARD adds is the honest localisation: it proves the residual bit is field-blind (weight-`0`, in `\ker R`) and consumed by no internal sector — so the bit **must come from outside**. And whether even experiment closes it is now disputed: Renou et al. (Nature **600**, 625 (2021)), with the Chen et al. (PRL **128**, 040403 (2022)) and Li et al. (2022) realisations, rule out real quantum mechanics within their independence-postulate framing — in which it stands — but Hoffreumon–Woods (arXiv:2603.19208) and Maioli–Curado–Gazeau (arXiv:2604.19482) show real quantum theory under a Kronecker/symplectic composition rule reproduces every network correlation (Renou et al. reply in arXiv:2604.07425), so the complex-over-real bit may be a composition-rule convention no experiment cleanly fixes — which is exactly the un-forceable field-blind import SHARD predicts, **vindicating (not proving)** the verdict.

This completes the **three-input map** — all three SHARD-specific last inputs share the meta-shape *force the fence, never the filling* and are each field-blind/weight-obstructed, but they have **different epistemic status**: the scale `l_step` / `G` is genuinely measured and the mode / masses are import-fixed by measured spectra, while the tensor product / complex-over-real bit is a now-contested composition-rule convention that may be experimentally unfixable (Hoffreumon–Woods arXiv:2603.19208, Maioli et al. arXiv:2604.19482 reopen; Renou Comment arXiv:2604.07425 defends; Renou 2021 stands in its own framing). On three orthogonal axes:

- **`χ_AB`** (the tensor product) — weight-`0` field obstruction; carved from above to `Q̃` by global consistency, the residual complex-over-real bit a now-contested composition-rule convention (Renou 2021 stands in its framing; reopened by Hoffreumon–Woods arXiv:2603.19208 and Maioli et al. arXiv:2604.19482, Renou Comment arXiv:2604.07425) — plausibly experimentally unfixable, which vindicates its status as the un-forceable field-blind import.
- **`l_step` / `G`** (the absolute scale) — weight-`(+1)` grading obstruction; downstream geometry only fixes volume *up to* `l_step` (Paper XI), `G` the input that closes it; genuinely measured.
- **the mode / masses** — single-party mode-canonicalisation (the "fifth wall"), proven orthogonal to `χ_AB` (the mode reads marginals alone, the gap `W` flat under the `χ_AB` sweep to `< 10⁻¹⁰⁰`); needs an external matter Hamiltonian; import-fixed by the measured spectra.

---

## 8. Disclosed open loci [OPEN]

Two residual loci are **scope, not breaks**, and are carried into the result rather than claimed closed.

1. **The commitment premise (the deeper one).** Field-blindness is proven only for level-`(1+AB)` single-joint-outcome-pair words. The actual `Q/Q̃` separation lives at degree `> (1,1)` (`I3322` needs NPA level `≥ 4`; `CHSH` alone does not witness it). §6 closes the tripartite path *at degree `(1,1)`* — more nodes at the same commitment do not open the gap — but not that a higher cross-party word cannot host non-reduced structure. This is the one premise the downstream no-go is conditional on; the mitigation is that the holonomy result (Paper XII §6) is field-agnostic (the composite invariant is a moment polynomial for any joint state) and the order/graph arguments never touch the correlator's would-be imaginary content — but the higher-word case is untested.
2. **The unbuilt two-party matter sector.** The no-link `χ_AB ↔` mode is, at the executed single-party level, tautological (the mode reads marginals only). A genuine two-party matter interaction — the interacting Ginsparg–Wilson flow (Paper IX, the O8 residue) or a mode-selecting Hamiltonian — is the one place a `χ_AB ↔` mode coupling could arise, and it is genuinely unbuilt. The argument that it is field-blind *a fortiori* (any such link must escape `R`-invariance and thereby import the tensor-product / local-tomography axiom — circular) is sound but is an argument about an unbuilt sector, flagged not closed.

Neither is an opened path; both are the honest boundary of the result. The verdict stands: the Feynman map of `χ_AB` bottoms out at `Q̃`; global consistency adds a connectivity floor and a manifoldlikeness obligation, both orthogonal to the residual; and the residual complex-over-real bit is the field-wide wall whose experimental fixability is itself now contested (Hoffreumon–Woods arXiv:2603.19208, Maioli et al. arXiv:2604.19482; Renou Comment arXiv:2604.07425; Renou 2021 stands in its framing) — a controversy that vindicates the un-forceable-import verdict rather than threatening the structural result.

---

## Reproducibility

Four receipts in `v7/code/`, all deterministic, single-threaded, exit `0`, **79 machine checks total**, of which **17 are cvxpy/SCS solver-tolerance `~10⁻⁹`** (every such digit flagged; the rest mpmath `dps = 120–140` or sympy-exact).

- **`s_probe_qtilde_minus_q.py`** (26/26). The spine. The explicit super-quantum witness `P*` — the **Collins–Gisin `I3322`** level-`(1+AB)` optimum `0.2514709` (true level: base words plus all nine cross-products `P_A[x]P_B[y]`), strictly above the full level-2 `⪰ Q` upper bound `0.2509397` and the converged `Q ≈ 0.2508754` (Pál–Vértesi 2010, level `≥ 4`, cited; solver-tolerance) — extracted as a fully-specified, no-signaling probability table and pushed by its own probabilities through all four sectors; the field-blindness spine (`I_σ(rebit) − I_σ(qubit) = 0`; deficit `+1/0`, sympy-exact). Carries the honest one-line contrast that the *±1-correlator* `I3322` has `cl = Q = Q̃ = 8.0` (no gap; the bare-`Q¹` `8.748` is records-inadmissible). Honest that the sector identity is structural (each sector is an `M`-functional); ML stated to reach only `Q¹`.
- **`s_geom_chi_sweep.py`** (20/20, zero solver-tolerance). Edge sign-blindness (sympy-exact); the connectivity floor — *honest pinch*: a single zero bond stays connected (one component), only a **globally product node** (all bonds zero) disconnects (two components); quantum vs almost-quantum identical at equal weight; the manifoldlikeness diagnostics **structurally** `R`-invariant (they read the magnitude-induced order; `R` preserves magnitudes — not a `0==0` array-copy) and **non-vacuous** (`d_MM` moves `1.9453` (2d sprinkle) → `1.0` (1d-like); `r = 0.52086`, height `44`; `f(2) = ½`, `f(3) = 8/35`); KR-dominance `n²/4 ≫ n\log n`; and the tripartite closure (`II`, `Tri` factor through `M`, gap `0`, non-vacuous).
- **`c3_covariance_chi_ab_probe.py`** (15/15). No-signaling a Lorentz scalar (gaps `0`, exact); microcausality `=` on-state commutation (sympy-exact two-faces), witnessed **without smuggling the tensor product** — the on-state commutation is exhibited abstractly (free non-commutative symbols, the relation `[A_x,B_y]=0` alone collapsing the cross-moment) and on a **single Hilbert space of prime dimension 3** (where no tensor factorisation exists at all), with an explicit flag that a tensor realisation is *sufficient but not necessary*; the super-quantum `Q̃ ∖ Q` witness is the **Collins–Gisin `I3322`** level-`(1+AB)` optimum `0.2514709 > Q ≈ 0.2508754`, passing all covariance gates (NS, `Γ ⪰ 0`, sub-Tsirelson CHSH block); the communication-complexity lever localised to `CHSH > 2√2`. (This receipt's own gate min-eigenvalue `−2.2 × 10⁻¹⁶` and `CHSH = 2√2` belong to a *Tsirelson-facet surrogate* used to exhibit the boundary, distinct from the spine's `P*` re-completion `+1.1 × 10⁻⁷` in `s_probe`.) One benign SCS "solution may be inaccurate" on the `CHSH`-max SDP, landing within `3.2 × 10⁻¹⁰` of `2√2` (flagged).
- **`s_order_feedback_probe.py`** (18/18, zero solver-tolerance). The order-feedback closure: `σ`-spread exactly `0` across a live `χ_AB` sweep (`χ` `0 → 0.3657`, `E` spread `1.295`, marginals pinned); 200 re-wirings permute nothing; `\mathrm{Var}(σ)` and `\mathrm{Var}(χ_AB)` disjoint (sympy-exact); the adversarial surrogate ranking genuinely distinct (non-vacuity).

Precision-honesty notes. The numeric "identical observables on `P*` and `P_Q`" is true by construction (same `M`-data); the load-bearing content is the structural `M`-functional argument. The strict `Q̃ ⊋ Q` and the `I3322` level-`≥ 4` fact are field-established (NGHA 2015; Pál–Vértesi 2010; Slofstra 2019), invoked not re-derived; the SDP digits that locate them are solver-tolerance. Never float64 for the cancellation-heavy KL/field-invariance quantities (gaps `~10⁻¹⁴¹` at `dps = 140`).

## References

**This program.**
- *The record click-law, XII* (v7 paper 12) — the transverse no-go: no transverse self-consistency forces `χ_AB`; the field-blind moment algebra `M` and the qubit→rebit reduction `R`; the envelope `Q̃` forced from inside.
- *The record click-law, IV* (v7 paper 4) — the joint click-law forced-skeleton / free-complement; `χ_AB = I_σ`; the entanglement graph and the connectivity/metric split (the Van Raamsdonk pinch).
- *The record click-law, VI* (v7 paper 6) and the companion *Transverse record correlations land on the almost-quantum set* — the moment-algebra arena, the static Information-Causality core, the local-tomography deficit `K_AB − K_A·K_B`.
- *The record click-law, VII* (v7 paper 7) — the covariant decoherence layer and its Tier-B gates (point-local-scalar seal density, microcausality).
- *The record click-law, XI* (v7 paper 11) — manifoldlikeness and the Kleitman–Rothschild dominance gate; the dynamical-selection axis.
- *Emergent Einstein equations without an emergent Newton constant* (Paper 57) — the weight-`(+1)` scale / `G` no-go (the second of the three orthogonal walls).

**External (invoked, not re-derived).**
- M. Navascués, Y. Guryanova, M. Hoban, A. Acín, *Almost quantum correlations*, Nat. Commun. **6**, 6288 (2015) — `Q̃ = Q^{1+AB} ⊋ Q`; device-independent principles reach `Q̃` not `Q`.
- M. Pawłowski et al., *Information causality as a physical principle*, Nature **461**, 1101 (2009); M. Navascués, H. Wunderlich, *A glance beyond the quantum model*, Proc. R. Soc. A **466**, 881 (2010) (macroscopic locality, `Q¹`); T. Fritz et al., *Local orthogonality as a multipartite principle for quantum correlations*, Nat. Commun. **4**, 2263 (2013).
- B. S. Tsirelson (1980); K. F. Pál, T. Vértesi, *Maximal violation of a bipartite three-setting, two-outcome Bell inequality using infinite-dimensional quantum systems*, Phys. Rev. A **82**, 022116 (2010) — the `I3322` quantum value (`Q ≈ 0.2508753845`, NPA level `≥ 4`); W. Słofstra, Forum Math. Pi **7**, e1 (2019) — `Q` not closed.
- L. Hardy (2001); G. Chiribella, G. M. D'Ariano, P. Perinotti, Phys. Rev. A **84**, 012311 (2011); W. Wootters — local tomography as the complex-over-real selector.
- M.-O. Renou et al., *Quantum theory based on real numbers can be experimentally falsified*, Nature **600**, 625 (2021); M.-C. Chen et al., Phys. Rev. Lett. **128**, 040403 (2022); Z.-D. Li et al. (2022) — ruling out real QM within their independence-postulate framing, now contested by R. Hoffreumon, M. P. Woods, *Quantum theory based on real numbers cannot be experimentally falsified*, arXiv:2603.19208 and F. Maioli, E. M. F. Curado, J.-P. Gazeau, *Quantum mechanics over real numbers fully reproduces standard quantum theory*, arXiv:2604.19482 (real QT reproduces network correlations under a Kronecker/symplectic composition rule), with the reply M. Moradi Kalarde, B. Xu, M.-O. Renou, *Comment on …*, arXiv:2604.07425; debate live.
- M. Van Raamsdonk, *Building up spacetime with quantum entanglement*, Gen. Relativ. Gravit. **42**, 2323 (2010) — the connectivity/pinch picture.
