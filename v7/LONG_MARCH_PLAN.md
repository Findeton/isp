# The Long March: deriving the record click-law (v7 Paper 1)

> **[FROZEN 2026-07-01.]** This plan tracked the campaign through Paper XI and was not updated for
> XII–XIX, the foundations campaign, or the λ=σ dissolution. It is superseded by `../v8/LEDGER.md`
> (the authoritative result/receipt/status map) and frozen with the rest of v7 (`ARCHIVE-STATUS.md`).

**Goal.** Derive — or constrain to a uniqueness-up-to-the-one-no-go-scale — the SHARD **record click-law**: the whole-history law `P^hist` that fixes *when, where, and how* a record commits (a seal/division event fires). The target is a triple:

- **`λ`** — the seal *rate* functional (when a seal fires);
- **`f(s²)`** — the *locality* kernel (how the seal is spread in spacetime);
- **`E`** — the seal *projection* (which proposition `E²=E`, `q=2E−1` commits).

This is the open **Branch-A** problem (derive the seal law) vs **Branch-B** (postulate it). The **do-delete no-go (C6)** proves observation alone cannot fix it, so the law must be pinned by a **self-consistency principle**, not data. Strategy: pile up every constraint and intersect.

## The five filters (the method)

1. **Hard kinematics fix the *form*.** C8 (Born/`q=2`) → seal `E` is a projection in a 2-norm structure. C3 (no white noise) + reflection positivity (theorem) → kernel `f(s²)` smooth, Lorentz-invariant, positive-type, quartic falloff. C10 (all discreteness ledgered).
2. **Covariance — the tightest filter.** Poisson sprinkling (BHS, Lorentz-invariant discreteness) + C4 (Tomonaga–Schwinger microcausality) + C1 (Bell: keep PI, drop OI, no signaling) + C5 (state-independent rate, keys to a c-number `E_G`) + C2 (Hegerfeldt: free-flash localization). Recovers Tumulka 2006/2020 for free + interacting-distinguishable; interacting-field is the likely wall.
3. **Self-consistency under refinement — the SHARD-native closer (highest-value bet).** Axiom C fixed point. paper4 already fixed the one-diamond constants `η_*=1.0903…`, `θ_*=0.7970…` by an internal KL=Fisher consistency condition. Extend to the **two-diamond sealing dynamics**: does the refinement fixed-point *force* `λ`? Seals = the refinement-consistency points (where the Gell-Mann–Hartle decoherence functional `D` diagonalizes). This is the Branch-A uniqueness principle the do-delete no-go demands.
4. **Variational rate.** Extremize entropy production `σ = D(P_AB‖P_BA)` (MaxEnt-production / stationary-action on `P^hist`) → *derive* `λ`, replacing the postulated hazards (`λ=at`, `λ_OU`).
5. **Uniqueness count.** After 1–4, how much freedom remains? The scale no-go guarantees **one** parameter must stay free (calibration). Best possible outcome = **unique modulo the one no-go scale** = clean Branch-A.

## Two-tier structure (LOCKED — the R1.5 correction)

SHARD is **pre-geometric**: space and time *emerge* (time from the commit order, space from screens/holonomy). The click-law must be derived at the **record level without presupposing spacetime**. Geometric/thermodynamic constraints are *conditional consequences*, not axioms.

**Tier A — the fundamental, pre-geometric click-law (define + derive here).** Records only (holonomy diamonds, commit order, exchange-defect). Constraints are purely combinatorial / algebraic / informational:
- `q=2` projective seal (C8); reflection positivity (process-level);
- **self-consistency under refinement (F3)** — the core uniqueness principle (fixed `η_*, θ_*` with no geometry);
- entropy production / arrow (F4) **per commit-step** (the commit order is the only "time" here);
- **Bell at the record-correlation level (C1)** — keep parameter-independence, drop outcome-independence; *this carries entanglement*;
- do-delete (C6), Markovization (C9), all-discreteness-ledgered (C10), magic≠indivisibility (C7) — process-level.

**Tier B — the conditional spacetime / thermodynamic regime.** *If* the records coarse-grain into a spacetime satisfying the corpus regularity condition (tame-class / cofinal stability / BHS sprinkling), *then* these **emerge as theorems**: Lorentz invariance, microcausality / Tomonaga–Schwinger (C4), Hegerfeldt localization (C2), the local kernel `f(s²)` (C3 geometric part), the thermodynamic equation of state + the scale no-go (Papers X, XI). **Not** imposed on the fundamental law.

**Entanglement guardrail.** Entanglement = non-local record *correlation* = **Tier A** (pre-geometric; needs no spacetime). Tier-B locality constrains the **dynamics** (no spacelike *signaling*), **not** the correlation structure. *Never* impose locality/factorization on the Tier-A correlations. Two distinct "localities": dynamics-locality (Tier B, no-signaling) vs correlation-non-locality (Tier A, entanglement persists). C1 (PI yes / OI no) is the exact line.

**Payoff.** This *isolates the wall.* The Tier-A law is purely combinatorial → plausibly derivable. The hard relativistic-collapse / covariance problem becomes a **Tier-B emergence question** ("does the Tier-A law's spacetime limit satisfy microcausality?"), not a starting axiom.

**Filter re-map.** F3 + F4 = Tier-A core. F1 splits (`q=2`/projection/RP = Tier A; kernel-`f(s²)` = Tier B). F2 = Tier B (reframed from "force the law" to "if spacetime, then microcausality/Lorentz/thermodynamics — and does the Tier-A limit satisfy it?"). F5 counts the **Tier-A** residual freedom (best = unique-mod-one-no-go-scale).

**Paper 1 v7 structure.** §A fundamental pre-geometric click-law (Tier A) → §B conditional spacetime/thermodynamic regime (Tier B, with X/XI as consequences) → entanglement caveat explicit.

## Recovery limits (a derived law must pass all)

- dense-seal → decoherence phenomenology (C12);
- free / distinguishable → Tumulka flash law;
- coarse-grained → Born rule + Einstein equation of state (already in hand);
- must not leak the do-delete / Bell / Markovization no-gos.

## Constraint ledger (C1–C12, from v6 paper56 — every record law must satisfy)

C1 Bell non-evasion · C2 Hegerfeldt/covariant localization · C3 no white-noise catastrophe · C4 Tomonaga–Schwinger integrability · C5 nonlinear-collapse no-go (state-independent) · C6 do-delete (not an observational statistic) · C7 magic ≠ indivisibility · C8 Born composition (`q=2`) · C9 Markovization no-go · C10 all discreteness ledgered · C11 operational fingerprint (optional) · C12 recover the decoherence phenomenology.

## Campaign discipline (the march)

- **Iterate:** investigate (apply/refine filters, high-precision) → write Paper 1 v7 → hostile review → integrate fixes as authorship → **if a review opens a new path, investigate it before the next review round** → repeat to terminal.
- **High precision throughout:** mpmath dps ≥ 80 (commonly 100), sympy-exact; never float64 for modular/near-vacuum/cancellation-heavy quantities. Receipt scripts in `v7/code/`.
- **Single-threaded paper style:** Paper 1 v7 reads written-once; no version/round/campaign narration; bare date stamps OK.
- **Honest scope:** this is the hardest open problem in the program. Tag every line [DERIVED]/[FORCED]/[CONSTRAINED]/[CONJECTURED]/[OPEN]/[OBSTRUCTED]. Do not claim Branch A closed unless the filters genuinely force the law.

## Status log

- **R0 (setup):** v7 folder created; plan written. First investigation campaign (Filters 1–4 in parallel + uniqueness synthesis) launched.
- **R1.5 (correction, LOCKED):** two-tier structure adopted (pre-geometric Tier-A fundamental law vs conditional Tier-B spacetime regime); entanglement is Tier-A correlation, never over-localized.
- **R1 (the 5-agent fan-out DIED mid-run** — all agents froze simultaneously, output never written; a systemic stall. Partial receipt scripts salvaged from `v7/code/` and re-run by hand:
  - **F1 form [Tier-A, FORCED]:** `f1_born_projection_q2.py` — in the 2-norm screen the seal `E` is forced to an **orthogonal projection** `E²=E=E*`, `q=2E−1` a reflection `q²=1`; oblique idempotents are not screen-norm-preserving.
  - **F1 kernel [Tier-B]:** `f1_kernel_positive_type.py` — positive-type, quartic UV decay, white-noise forbidden (needs `s²` ⇒ Tier B).
  - **F4 rate [Tier-A, lands on the no-go floor]:** `f4_variational_rate.py` — the entropy production `χ=∫λ=D(P_AB‖P_BA)` is **weight-0 / record-intrinsic** (a KL divergence the records fix); turning it into a *rate* needs one absolute time unit ⇒ **rate SHAPE forced, exactly ONE scale free** = the no-go floor.
  - F3 (self-consistency) agent never wrote its script; synthesis never ran.
- **R1-redo (F3) — DONE, the Tier-A SHAPE is FORCED.** `f3_self_consistency.py` (dps=120, runs clean): one-diamond constants reproduced (balance residual 9.7e-122); refinement-consistency = the Cauchy multiplicative eqn `S(χ₁+χ₂)=S(χ₁)S(χ₂)` ⟹ **unique `S(χ)=exp(−κχ)`** (constant hazard per unit holonomy content); only `p=1` consistent; content-additivity = the paper4 §34 cocycle (1e-81); `κ≥0` no-revival is structural (a non-composing point is not a seal — re-derives CP-divisibility purely combinatorially, no geometry); exp vs Gaussian = same law under `χ(T)` linear/quadratic (Tier-B profile). **Forced up to the one no-go scale κ** = clean Branch-A for the shape.
- **Paper 1 v7 WRITTEN:** `relativistic-isp-v7-paper1-record-click-law.md`. §A Tier-A (F1 seal=orthogonal projection; F3 `S=exp(−κχ)` forced; no-revival structural; one free κ; entanglement guardrail [OPEN]) + §B Tier-B conditional (exp/Gaussian reparametrization; X/XI as the spacetime limit, sharing κ). Honest residue: (i) intrinsic supply of `dχ` (the closed-holonomy cochain `h_C`), (ii) entanglement correlation, (iii) sparsity. Receipts: f1/f3/f4 in v7/code/.
- **Paper 1 v7 review round 1 — 3 referees, all minor-revision, NO pre-geometric violations (the discipline held), all math verified.** The major catch (all 3 converge, and it *improves* the paper): the for-all-splits `exp(−κχ)` is the **divisible (dense-seal) / classical-side** shape; the genuinely-indivisible (sparse) case is the open frontier and is the *same* question as the density of seals; and §3.2 conflated **parallel/tensor** content-additivity (the cocycle, verified ~1e-102) with the **sequential/concatenation** additivity (an odometer) the Cauchy eqn actually needs (the open §5.1). Plus: regularity qualifier (monotone+measurable/Aczél), no-revival→corollary, `q²=1` doesn't force orthogonality (norm-preservation does), receipt-attribution fixes, shared-κ softened to analogy, Lorentz/microcausality NOT claimed. **All integrated** (paper reframed: exponential = divisible-regime shape, sequential additivity assumed/open, sparse = open frontier).
- **R2 (review-opened paths, investigated BEFORE round 2) — two robust background agents:** (A) `f3b_sparse_seal_shape.py` — does the SPARSE/indivisible regime force a shape (geometric-on-the-lattice S(nd)=S(d)^n?), is the inter-seal profile free, is the spacing self-consistently fixed, does d→0 recover exp? (B) `f3c_sequential_odometer.py` — does Arrow-Positivity (paper10 σ=D(P_AB‖P_BA), accumulated entropy production) FORCE the sequential odometer (concatenation-additive χ, dχ≥0), closing the §5.1 additivity premise? Then integrate R2 → review round 2.
- **R2 DONE + INTEGRATED.** (A) `f3b_sparse_seal_shape.py`: the SPARSE/indivisible regime forces the seal-lattice skeleton geometric `S(nd)=S(d)^n` (Cauchy on ℕ, rigid, residual 1.2e-122) but the **inter-seal coherence profile (∞-dim) + the spacing `d` are FREE** — that free profile IS the genuine-quantum content; dense exp = `d→0` limit (B(d)→0); spacing genuinely open (`d=W_*=0.36478` a plausible but unforced closure). §5.3 upgraded [OPEN]→[CONSTRAINED]. (B) `f3c_sequential_odometer.py`: the SEQUENTIAL content-odometer (concatenation-additivity + `dχ≥0`) is **DISCHARGED — a theorem of arrow-positivity** (trajectory action telescopes, gap 7.7e-121 on a driven chain; `σ_step≥0`), distinct from the parallel/tensor cocycle. §3.2 drops from TWO premises to ONE (only dense-vs-sparse). Both folded into Paper 1 §3.2 (new structure: forced odometer → divisible exp → indivisible geometric skeleton), §5 (residue now: dχ-magnitude, entanglement, inter-seal profile+spacing), §6/§7/abstract. Receipts verified clean.
- **Review round 2 — 3 referees, all minor-revision, math verified dps-150, no pre-geom violations, no overclaims.** Fixes (all integrated): odometer additivity caveat named (Markov-presentable + χ:=σ); "geometric"→"geometric-progression" (vs spacetime); sparse per-seal-constancy premise stated; cocycle gap→0.0 machine-zero; d→0 squeeze→monotone; §3.3 no-revival scoped to committed survival + **inter-seal coherence-rise = the companion non-Gaussian REVIVAL fingerprint** (nice connection). Opened the two deepest supports.
- **R3 (deepest supports, before round 3) — BOTH RESOLVED as legitimate, not holes.** `f3d_foundational_supports.py` (dps=140). (A) The seal⟺composition bridge is **FORCED at the kinematic level as an ALGEBRAIC IDENTITY** — CK defect = interference cross-term entrywise (gap 6.6e-142); its ingredients (Γ=|U|², orthogonal projectors) Tier-A-forced by §3.1; intrinsic supply splits into Tier-A-open rate (κ/dχ) + Tier-B-deferred placement, NOT in the forcing chain; the only geometry-risk (projector ordering) is the COMMIT order, now stated. (B) Collar **Markov-presentability is FORCED** (paper10 T3: eventless⇒detailed-balance reversible; T4: ⇒finite primitive Markov presentation); Markov(collar)/non-Markov(seal-order) **consistent** — different objects (content-transport telescopes gap 0.0; seal-order Γ=|U|² CK-gap 0.329 across unsealed, 0 at a seal). §3.2 caveat (b) DISCHARGED; only paper10's inherited infinite-collar residue remains. Integrated (§3.2 bridge+odometer, §5 item 0). 
- **Review round 3 — all 3 minor-revision, all math re-verified dps-140, NO pre-geom violations, NO overclaims.** Terminal-quality. Two minor premise-naming fixes: (1) the dense Cauchy step has a SECOND premise — survival a function of accumulated content ALONE (single scalar channel), supplied by the q=2 one-contrast screen of §3.1 (a multi-channel screen breaks multiplicativity, gap 0.317 — NEW receipt `f3e_history_independence.py`); (2) abstract "(q²=1)" misleading → "(E=E*=E², by norm-preservation)". Plus citation fixes (p=1 as 2^p−2=0; [10] T3/T4; [X]/[XI] file handles; B(d)=s^k(1−s) exact). All integrated. Round-3 "new paths" were future-paper notes only (κ↔gravity proof, non-uniform spacing) — no investigation needed.
- **Review round 4 — 2 minor-revision + 1 ACCEPT. TERMINAL reached.** All math re-verified dps-150, no pre-geom violations, no overclaims, no single-threaded violations. The referee found one *strengthening* refinement: the dense premise (b) splits into (b1) single channel [FORCED by §3.1's q=2 one-contrast screen, matrix→scalar collapse] and (b2) stationarity [a SEPARATE premise — same contrast at every seal; even one channel with location-dependent contraction breaks multiplicativity, gap 0.18]; and given (a)+(b1)+(b2) the exponential is forced via the **product limit** `lim_m (f(χ/m))^m = exp(χ(log f)′(0))` for ANY stationary per-step law (e.g. f=1/(1+dχ)→exp, verified to 1.8e-6) — so exp is forced *without* assuming an exponential finite-step law (a cleaner, stronger route). Plus 2 citation tidies (§7 p-scan; abstract κ softened to analogy). All integrated; f3e upgraded to receipt (b1)+(b2)+product-limit. Round-4 "new paths" all future-paper (κ↔gravity proof, dense-vs-sparse realization).
- **PAPER 1 OF THE LONG MARCH — TERMINAL (accept-with-minor-revisions).** Convergence: 4 review rounds (3 minor → 2-minor+1-accept), 3 investigation rounds (R2-A/B, R3), zero drift toward major, math fully verified every round (dps 120-150), the pre-geometric discipline held throughout, no overclaims ever. RESULT (all FORCED, pre-geometric, receipted): seal=orthogonal projection; the seal⟺composition bridge=a forced kinematic identity (CK-defect=interference cross-term, 6.6e-142); content=a forced odometer (dχ≥0 unconditional, additivity from arrow-positivity; collar Markov-presentability forced T3+T4); the **divisible law=exp(−κχ)** (modulo dense seals + single-channel + stationarity, all §3.1-forced or named); the **indivisible law=a power-law skeleton S(nd)=S(d)^n + a free inter-seal quantum profile** (= the non-Gaussian revival fingerprint); all up to the one no-go scale κ. Honest residue: the MAGNITUDE of dχ/κ (the h_C cochain), the entanglement correlation, the inter-seal profile+spacing/sparsity, + paper10's inherited infinite-collar residue. Receipts f3/f3b/f3c/f3d/f3e + f1/f4 all run clean.
---

## Paper 2 — the content supply (the clock from the holonomy cochain). [STARTED 2026-06-15]

**Roadmap (user, 2026-06-15):** Paper 2 (content/`d`) → **Paper 3** (cross-tier bridge: get `d` from the Tier-B gravitational *dimensionless* coefficients `1/4` / `0.295` / `W_*` — the no-go ALLOWS this since `d` is weight-0; consistency Tier-A↔Tier-B; circularity caveat = must use a *derived* coefficient not the scale) → **Paper 4** (entanglement, derived *into* spacetime — direction is entanglement→spacetime à la Van Raamsdonk/ER=EPR, NOT spacetime→entanglement; uses Paper 3's bridge).

**Target (Paper 2, pure Tier-A):** Paper 1 forced the click-law's *shape in content* but left the **magnitude** (`dχ` size, `κ`) and the **spacing `d`** open. Paper 2 asks whether the record's own closed-holonomy ledger generates its clock:
- **Q1** — is `dχ`'s FORM forced = the cochain increment `ρᵢ = h_{i+1}−h_i` (paper4 §44 telescope `Σρᵢ = h_b − h_a`, already used in Paper 1 R2-B)?
- **Q2 (CENTERPIECE)** — does a per-diamond **capacity/saturation** self-consistency principle FORCE the spacing `d = W_* = 1−θ_*² = 0.36478…` (one seal per one-diamond's worth of content)? This is a NEW principle, *distinct* from Paper 1's composition-self-consistency (which R2-A showed leaves `d` free): a diamond holds at most `W_*` of coherent content at self-consistency (its KL=Fisher capacity `J(η_*)`); beyond that it must commit. Is that a rigorous forcing of `d=W_*`, or still a choice?
- **Q3** — is the accumulation rate (size of `ρᵢ`) forced or physical input?
- **Q4** — confirm `κ` (content→commit-order conversion) is the one no-go floor (weight-0 content → rate needs one absolute unit).
Pre-geometric throughout; mpmath dps≥100/sympy; receipts `v7/code/`. Same discipline: investigate → write Paper 2 → hostile review → investigate opened paths BEFORE next round → loop. LEAN agents (single robust background / small workflows — the 5-agent fan-out stalled in Paper 1).

- **P2 R1 investigation launched** (single robust agent: Q1 cochain→`dχ` form + Q2 the saturation-forcing of `d=W_*` + Q3 rate + Q4 κ).
- **P2 R1 DONE + Paper 2 WRITTEN** (`relativistic-isp-v7-paper2-content-supply.md`, receipt `p2a_content_supply.py`, dps-140, 14 checks). Q1: `dχᵢ = ρᵢ = h_{i+1}−h_i` is **forced** three ways (Paper I odometer; discrete Poincaré lemma `H¹=0` ⟹ "concatenation-additive content ⟺ coboundary of a scalar potential"; corpus source-as-coboundary law). Q2: `W_* = max_η min(C(η), J(η)) = 0.36478…` is a genuine **forced capacity** (content `C(η)` rises, Fisher `J(η)=sech²η` falls, single crossing at `η_A=1.0903`); over-saturation self-inconsistent ⟹ **`d ≤ W_*` forced** (a ceiling Paper I's composition-self-consistency could *not* give). Q3/Q4: magnitude PHYSICAL, `κ` the one no-go floor.
- **Review round 1 (Paper 2) — all minor, math verified dps-120, no pre-geom/single-threaded violations.** Catches (integrated): `d≤W_*` rests on an unstated **no-over-saturation** premise (symmetric partner of P-sat); `min(C,J)` tacitly imports Paper I §3.1's single-channel screen. Insight surfaced: `d=W_*` ⟺ the seal fires *exactly* at `C=J`.
- **R2 (the decisive investigation, before round 2) — THE TWO-ROOTS FINDING.** `p2b_event_law_saturation.py` (dps-140, 13 checks). The agent was told NOT to manufacture a closure, and honestly found **no closure** — and worse for `d=W_*`: paper4 carries **two distinct laws with two distinct roots** that the `d=W_*` candidate silently conflated. **LAW-A** (paper4 §5 self-consistency, `D(P_η‖μ_D)=J(η)`) fixes the diamond *constants* — root `η_A=1.0903…`, capacity `W_*=0.36478…`; it says nothing about when a seal fires. **LAW-B** (paper4 §71 primitive division-event/seal-firing) is a survival/Poisson commitment `S(I)=e^{−I}` with fixed point `tanh η = e^{−η}` — a **different** root `η_B=0.60938…` (verified residual 0), at which the seal commits content `C(η_B)=0.156 < W_*` — **under-saturated, 43% of capacity**, the committed evidence a random Poisson variable (mean 1 in RN units), not `W_*`. So paper4's *own* commitment law fires the seal at `η_B` **under** the ceiling and therefore **contradicts** `(P-sat)` rather than supplying it. `d=W_*` is not merely unforced — closing it needs *overriding* §71 with a completeness postulate. Suggestive thread: `1/η_B=1.641…` = the corpus's 1d mass-gap correlation length (a hook for the Paper-3 cross-tier bridge).
- **Paper 2 HONEST PIVOT integrated** (abstract part 2, §3 header, §2 form-vs-identification layer, §3.1 single-channel credit, §3.2 named no-over-sat premise, §3.3 two roots + §71 contradiction, §5 two-roots reconciliation as the genuine open obligation, §6 claims, §7 + p2b receipt). The open obligation is now sharp: **reconcile (or deliberately choose between) `η_A` and `η_B`** — is the under-saturated commitment law final, or is there a completeness principle overriding §71? (paper4 §69 already found a fixed-content threshold non-canonical, weighing against a naïve "complete the diamond" stipulation.)
- **Review round 2 — 3 referees, all minor-revision; ALL numerics verified independently dps 120–150 (both receipts pass); pre-geom + single-threaded clean.** Decisive CONVERGENCE catch (all 3): the word **"contradicted" is OVERSTATED** + a **units conflation**. (a) §71's `tanh η=e^{−η}` is verbatim a *fixed-point eq selecting the TILT coefficient* `h_G` — it has no `d`, no spacing, no per-seal content; getting "d=W_* contradicted" needs first *identifying* per-seal content with `C(η_B)`, a modeling step §71 never states → honest status is "in tension / not supplied," matching p2b's own verdict. (b) `C(η_B)=0.156` is DETERMINISTIC, not "a random Poisson variable" — three objects: tilt `η_B`, random evidence `I~Exp(1)` (mean 1), deterministic content `C(η_B)`. (c) promote §69 (fixed-content threshold non-canonical) as the SOUND reason `d≠W_*`. (d) new path: it's really THREE unit systems, and §71's one-mode parity slice sits in a richer VECTOR ledger §73–76 with a coupled root ≈0.495. Plus `η_A=W_*` typo, §3.3 heading too strong, 43%→~43%, p2a check count 14→13.
- **R-invest (BEFORE round 3) — two investigations, both done, both high-value:**
  - **Compatibility workflow `w8eccehq7` (HIGH CONFIDENCE): §5 and §71 are NOT incompatible — `compatible-different-roles`.** Verbatim-grounded + dps-150. Same `η`-axis (identical parity family `P_η(q)=e^{ηq}/2cosh η`, same `ψ`), so the comparison is well-typed (NOT apples-to-oranges), but DIFFERENT laws: §5 = `ψ″`-balance `C=J` → `η_A=1.0903` (structural constant, fixes `W_*`, supplies κ; §7: single diamond "has no memory, no previous/next diamond" = no firing dynamics); §71 = `ψ′`-balance `tanh η=e^{−η}` → `η_B=0.6094` (dynamical firing threshold, with the actual survival on a SEPARATE evidence clock `S(I)=e^{−I}`). Two eqns contradict only if they constrain the same quantity at the same stage — these don't. What's in tension is ONLY (P-sat)/(d=W_*) ⊥ §71 (both speak to firing; §71 fires under capacity; §71 wins). Recommended framing: lead with "§5⊥§71 consistent — no corpus contradiction," scope "contradicts" to P-sat, frame the open obligation as "what fixes d," not "reconcile the two laws."
  - **Vector-ledger agent (receipt `p2c`, 17 checks): "THE commitment root" is MODE-DEPENDENT.** Four distinct values (ascending): `η_DC=0.4407` (rejected double-counting) < `h_*=0.49505` (coupled `{x,y,xy}` vector root, all components equal, Φ strictly convex) < `η_B=0.6094` (one-mode-parity slice) < `η_A=1.0903` (self-consistency — NOT a seal root). The seal law is the VECTOR fixed point `grad ψ(h)=e^{−h}` on the complete ledger; different ledgers → different roots, so `η_B` is the one-mode slice, NOT a corpus-unique canonical seal root. ROBUST mode-independent fact: **seals fire STRICTLY under `W_*` in every mode** (`C(η_B)=0.156`, `C(h_*)=0.109`, coupled total `0.460 < 3W_*`). §69 reading reconciled: a FIXED-CONTENT threshold (= what d=W_* is) IS non-canonical (selected η drifts with shape/unit); the earlier "opposite" read was about the *likelihood-zero* threshold (a different object). Recommendation: don't call η_B "the" root; LEAD with the inequality.
- **Paper 2 UNIFIED REWRITE integrated** (abstract part 2, §3 header, §3.3 full rewrite, §5 recast, §6 claims, §7 + p2c, ref [4] + §71–76/§69, p2b prose). New honest spine: §5⊥§71 **consistent (no corpus contradiction)**; the seal law is a **mode-dependent vector fixed point**, robust fact = **committed content < W_* in every mode** (lead with the inequality, not a magic number); **only (P-sat) is contradicted** (scoped, never widened to §5/corpus); **three objects** kept apart (tilt/evidence-random/content-deterministic); §69 fixed-content-threshold non-canonicity cited; open obligation = **what fixes `d`** (not the already-consistent §5↔§71). All three receipts pass (p2a 13, p2b 13, p2c 17).
- **Review round 3 — 1 ACCEPT + 2 minor-revision; ALL numerics verified dps 100–160; pre-geom + single-threaded clean; the de-escalation declared FAITHFUL and correctly scoped (one referee: "the de-escalated centerpiece is faithful, robust, and honestly scoped").** All catches cosmetic: (1) `C(η)` is bounded at **log 2**, not ∞ (§3.1 factual slip — crossing/W_* unaffected); (2) "three independent routes" oversells Q1 → it's one theorem in two framings (Poincaré lemma = odometer) + the corpus instantiation (§2); (3) §69 citation: §71 *closes* §69's shape/`r`-drifts (r=1, Poisson forced), so drop "and unit" — the *surviving* non-canonicity is the **ledger/mode dependence** (§3.3/§5); (4) p2a printed verdict stale (pre-de-escalation prose). PLUS an honest **under-statement** catch (referee 3): the inequality is *structural*, not "every mode checked" — seal content is monotone in the coefficient, the forced cochain unit `λ=1` caps the one-mode value at `0.428 W_*` (reaching `W_*` needs the inadmissible `λ≈0.208`), and coupling only dilutes, so `C(η_B)=0.428 W_*` is the **supremum over admissible modes**; also the evidence-clock candidate is **κ-blocked** (mean-1 in *evidence* units → needs κ to become a content `d`).
- **R3 integration + de-risk (BEFORE round 4):** all minor fixes folded in (§3.1 log2; §2 one-theorem; §3.3 + §5 §69-sharpen; §3.3 + abstract + §7 the structural supremum; §5 κ-block; p2a prose refreshed). The **supremum claim de-risked by hand**: solved `grad ψ(h)=e^{−h}` on `(x,y)`, `(x,xy)`, `(y,xy)`, `(x,y,xy)` — uncoupled/2-mode hit `η_B` *exactly*, full coupling dilutes to 0.495, **no mode exceeds `η_B`** → `C(η_B)=0.428 W_*` is genuinely the supremum. Locked into **p2c as TASK 6 + an explicit supremum scan (now 24 checks)**. All receipts pass (p2a 13, p2b 13, p2c 24).
- **Review round 4 — 3× minor-revision; ALL numerics verified dps 100–160; pre-geom + single-threaded CLEAN; de-escalation declared FAITHFUL in both directions.** One substantive catch (the rest trivial): the "supremum over admissible modes" was **over-precise** — referee 3 (tagged major) found that *widening* the ledger class beyond *orthogonal* parity modes on the *count-dual* base lets the seal coefficient/content EXCEED `η_B`, even exceed `W_*` (a skewed/correlated ledger gives content `0.371`–`1.046 > W_* = 0.365`). The supremum is a theorem only OF the admissible class (complete primitive ORTHOGONAL parity ledgers, paper4 §73–76, on the COUNT-DUAL base μ=(½,½), §5–6; skewed/mixed-basis excluded by §75 cover-independence). Referee 3 exhaustively verified the gated claim (127+112 orthogonal ledgers on {±1}^≤3, max component = `η_B` in all). Also: a leftover stale "(17 checks)" for p2c in §7 (the other citation already said 24).
- **R4 integration (verified by hand first):** confirmed the admissible-class cap (orthogonal {x,y,z} uniform → each `η_B`), the **monotone character-group dilution** (1/3/7-char → `0.609/0.495/0.368`), and a clean **load-bearing-gate counterexample** (skewed base μ(+)=0.2 → content `0.526 > W_*`). All integrated: §3.3 + abstract + §7 GATE the supremum to the admissible class (orthogonal parity + count-dual base; cite §73–76, §5–6, §75) and add the note that the count-symmetry (`q²=1`, `μ=½`) bounding the firing content is the *same* structure that fixes the §5 constants — bound and constants on one footing. p2c extended with the dilution table + the skewed counterexample (**now 25 checks**); §7 stale count fixed. Receipts p2a 13, p2b 13, p2c 25 all pass.
- **Confirming review — ACCEPT (TERMINAL).** Receipts 13/13/25 all pass; every numeric reproduced independently dps 120–140 (W_* balance 1.3e-141; η_B residual 0.0; coupled h_* residual 3e-161). The gated supremum held under an EXHAUSTIVE search: all 135 orthogonal-character ledgers on {±1}^≤3 + m=4 (single chars + 400 random asymmetric seeds) → max per-mode coefficient = EXACTLY η_B, none exceeds; skewed base breaches W_* (gate load-bearing). The §5–6/§73–76/§75 gating verified FAITHFUL to paper4 verbatim (line 360 μ=(½,½); line 364 "forced by q²=1 and count-duality"; line 367 unbalanced-binary FAILS-UNIVERSALITY; §75-76 REJECTS-MIXED-PRIMITIVE/REFUTES-GL-INVARIANCE). Clean-read: no overclaim, pre-geom clean, single-threaded clean, numeric-consistency clean. **No substantive defect remains.**

- **PAPER 2 OF THE LONG MARCH — TERMINAL (2026-06-15).** `isp/v7/relativistic-isp-v7-paper2-content-supply.md`. Convergence: 4 hostile review rounds (R1 minor → R2 minor [the de-escalation] → R3 1-accept+2-minor → R4 3-minor [the supremum gating]) + 2 deep investigations (compatibility workflow; vector-ledger agent) + a confirming review = ACCEPT. All math verified every round dps 100–160; pre-geometric discipline held throughout; no overclaim survived. RESULT, all receipted (`v7/code/` p2a 13, p2b 13, p2c 25):
  - **(Q1) FORCED:** the content increment is the **coboundary** `dχᵢ = h_{i+1}−h_i` (one theorem two framings: discrete Poincaré lemma `H¹=0` = Paper I's odometer; the corpus source-as-coboundary law instantiates it). Only *which* potential (the magnitude) is left = physical input; `κ` stays the one no-go floor.
  - **(Q2) FORCED CEILING:** `d ≤ W_* = 0.36478` (`W_* = max_η min(C,J)`, `C↑` to log 2 / `J↓`, single crossing at `η_A=1.0903`; given a committed record is non-over-saturated). A bound composition-self-consistency could not give.
  - **THE HONEST SPINE:** paper4 §5 (capacity constant `η_A`) and §71–76 (seal-firing **vector** fixed point `∂ψ/∂h=e^{−h}`) are **distinct & mutually consistent — NO corpus contradiction**. The firing law commits **strictly under** `W_*` in every admissible mode; the firing root is **mode-dependent** (`η_B=0.6094` one-mode, `h_*=0.4951` coupled), so the robust fact is the **inequality**, not a number. `C(η_B)=0.428 W_*` is the **supremum over the admissible class** (orthogonal parity ledgers on the count-dual base; the gates are §5–6/§75, the *same* count-symmetry that fixes the §5 constants). Three objects kept apart (tilt `η_B` / random evidence `I~Exp(1)` / deterministic content `C(η_B)`). Only the *postulate* `(P-sat)`/`d=W_*` is contradicted, never §5/the corpus.
  - **OPEN obligation:** *what fixes the spacing `d`* (not the already-consistent §5↔§71). All three candidates fail to deliver a forced deterministic `d`: `W_*` is a bound; the firing content is mode-dependent; the evidence clock is random AND κ-blocked. → Paper 3 (cross-tier bridge) is the live route.
  - NEXT per user roadmap: **Paper 3** (fix `d` from a *derived* Tier-B gravitational dimensionless coefficient — no-go permits it since `d` is weight-0; suggestive thread `1/η_B=1.641` = 1d mass-gap correlation length), then **Paper 4** (entanglement → spacetime, à la Van Raamsdonk). User flagged the roadmap as "ideas to test, not gospel."

## Paper 3 SCOUT (cross-tier bridge) — VERDICT: BLOCKED-CIRCULAR, high confidence (2026-06-15)

Workflow `wynk5kzhf` (inventory + circularity + numerical-thread + adjudication). **The cross-tier bridge to fix `d` does NOT exist non-circularly → Paper 3 should be a SECOND NO-GO, the `d`-analog of Paper 57's G no-go.** The elegance: the *same* weight-counting lemma (paper6 **Theorem G**) that makes `d` weight-0 (hence eligible to be fixed by a pure number) FORCES every record-derived dimensionless coefficient into an exhaustive **trichotomy**, and the three requirements (record-derived ∧ scale-independent ∧ per-seal/length-meaningful) are **mutually exclusive**:
- **Bin 1 — ratio-of-twins (CIRCULAR):** `1/4 = G·σ_A`, `κ·σ_A = 2π`, `G·Λ²`, `η=1/4G` — dimensionless only because the record scale appears and cancels; consistent with *every* record scale (SIGMA-SPLIT: A_rec=1→G=1/4 vs A_rec=3→G=3/4, both Einstein-residual 0). Circular.
- **Bin 2 — scale-blind (VACUOUS):** Srednicki `a=0.29542`, `U_G=1`, `η/s=1/4π`, `c/6=1/6`, graviton `4/9`, 14.66/decade — scale cancels, so they carry ZERO information about `d`.
- **Bin 3 — Tier-A-intrinsic (WRONG TIER + WRONG AXIS):** `W_*=0.36478` IS genuine per-seal content but is a *Tier-A* record constant (same tier as `d`), not a Tier-B emergent coefficient; and on the wrong axis (amplitude `κ=W_*`, not spacing — the geometric scale lives in `L_n=n²(D−A)=h⁻²(D−A)`, orthogonal to `W_*`; paper4 free-amplitude-attack: varying `κ=W_*` changes gravity WORK but never the lattice length `h`).
- **All three numerical threads are NUMEROLOGY (dps-120):** (1) `θ_B²=0.295598` vs Srednicki `a=0.295417` — gap `+1.8e-4`, and the partial-wave sequence converges *toward* Srednicki and *away* from `θ_B²`; no identity (and `θ_B²` is record-side → circular even if exact). (2) `1/4`: no clean factor (`C(η_B)/(1/4)=0.624` vs `5/8=0.625`, a 5.6e-4 near-miss). (3) `1/η_B=1.641`: nearest pure numbers loose (`π²/6=1.6449`, φ); no independently-derived Tier-B quantity equals it; the corpus already lists `ξ_1d` as a *second-scale attack* that collapses onto the weight-0 invariant (record-internal, not spacetime).
- **Constructive residue (a DIFFERENT open question, not the d-bridge):** the dimensionless gravitational coupling-per-species `c_m = Gm²/(ℏc)` IS weight-0, intrinsic, record-eligible — but fixes a dimensionless STRENGTH/hierarchy, not the absolute spacing `d`.
- Aligns with the existing MEMORY **second-scale-G-fix verdict** (same mutual exclusion `κ·σ_A=G·Λ²=const`). Recommended Paper-3 structure (the no-go): (i) `d`'s weight-0 eligibility, (ii) Theorem-G trichotomy/exhaustion, (iii) the three bins with verified witnesses, (iv) the three numerology threads catalogued with dps≥100 receipts, (v) the single load-bearing premise (gate G1, same as Paper 57; seal-rate primitive closes it), (vi) the `c_m` hierarchy residue.

## Paper 3 d-no-go STRESS-TEST — SURVIVES (high confidence), correctly scoped (2026-06-15)

Workflow `weru2t5e2` (3 adversaries try to BREAK the no-go + skeptical judge). **No attacker found a non-circular bridge; the no-go SURVIVES.** Decisive new findings:
- **The trichotomy is FORCED by a grading homomorphism, not asserted** (paper6 = `relativistic-isp-v6-paper6-arec-gauge-closure-v6.1.md`, Theorem G §2.1-2.2, cofinal-equivariance proof §3.4). `g_λ` is a one-parameter group → weight(XY)=weight(X)+weight(Y); generic monomial `W^a κ^b l_step^c σ_A^e` has weight `2b+c−2e`; `=0` forces `c=2(e−b)` so any `l_step` power is *even* and tied to κ/σ_A powers that *exactly cancel* the absolute length. **ROUTE B (weight-0 yet leaks length) is structurally impossible**; no fourth bin of record-derived coefficient exists (verified incl. irrational/anomalous weights: `a+b√2=0` over ℤ only `a=b=0`).
- **Clean ALGEBRAIC refutation of the numerology:** `θ_B` is the real root of `t³+t²+t−1=0` (`e^{η_B}` = the **tribonacci constant**), `θ_B²` of `s³+s²+3s−1=0` — degree-3 algebraic irrationals, **provably ≠ Srednicki's transcendental partial-wave integral** `a=0.295417` (stable gap 1.8e-4, ~96 orders above any identity floor). Kills the strongest near-miss for good.
- **The RG-running/transmutation route** (the only genuine "dimensionless yet scale-carrying" escape, via `ln μ`) fails correctly: dimensional transmutation delivers a RATIO `μ_*/μ0` (the `c_m` hierarchy residue), never the absolute `l_step` (the only reference scale IS `1/l_step`).
- **CRUCIAL SCOPE REFINEMENT (the honest residual):** "NO Tier-B coefficient CAN fix `d`" *overstates* it. The trichotomy rigorously closes only the **record-derived** route. The "scale-blind ⟹ vacuous-for-`d`" step is rigorous ONLY for *record-internal* scale-blind coefficients (Buckingham-π / exponential-family value-blindness). It is **NOT** rigorous — and the corpus does NOT claim it — for a **genuinely EXTERNAL, independently-derived** gravity number: since `d` is weight-0, two weight-0 numbers can be equated without the scale, so an external structural identity `d = (derived Tier-B coefficient)` is **permitted, only undiscovered** (paper-XI §2.4; v7 paper2 §5 route 2). Two corpus-disclosed scope doors remain un-attacked: **(i) gate G1** (an un-imported v1-v3 law consuming A_rec outside ℓ); **(ii) the Chamseddine–Connes spectral-action route** making a gravity coefficient record-intrinsic (its one explicit profile gives `f2=1/(2π)²`, missing the conjectured `1/(2π)` by 2π, and leaves G free — not a counterexample, not closed).
- **Judge: write-the-nogo (correctly scoped).** Honest Paper-3 thesis = the no-go closes the *record-derived/internal* bridge (forced trichotomy + tribonacci-algebraic numerology refutation), while *cataloguing* the permitted-but-undiscovered external-identity route and the two open doors.

## DEEPER, PRIOR QUESTION (user, 2026-06-15): "is `d` a fixed quantity?"
Answer from Papers 1–2: **NO — `d` is not known to be fixed, and Paper 2 gives positive reasons it ISN'T a single scalar.** Paper 1 left `d` *free* (the regular lattice `L={nd}` was an idealization). Paper 2: the corpus's firing law commits at a **mode-dependent** content (`C(η_B)=0.156` one-mode, `C(h_*)=0.109` coupled) on a **random Poisson clock** (`I~Exp(1)`). FIXED/forced = the **ceiling** `d≤W_*` + the **form** (coboundary, power-law skeleton); within one mode the tilt/content are deterministic. NOT fixed = a single universal deterministic `d` (mode-dependent) and the realized spacing (random). → The two results COMPOSE into the strongest Paper-3 thesis: **`d` is not a forced fixed scalar (random + mode-dependent + `W_*`-bounded), AND even were it fixed, no record-derived Tier-B coefficient can pin it (forced trichotomy); an external structural identity is permitted but undiscovered.**

## Paper 3 EXHAUSTION SWEEP — complete (2026-06-15)
Workflow `wufgr39hn` (d-status + spectral-action door + G1/completeness + synthesis). VERDICT: no-go **airtight-modulo-disclosed-doors**, high confidence.
- **(A) Is `d` fixed? NO — layered (c)+(b)/(d).** Content `C(h)` deterministic given a mode (`Var=0`) but MODE-dependent (finite set `{C(η_B)=0.156, C(h_*)=0.109,…}`, no canonical member, all `< W_*`); `W_*` a fixed scalar but a BOUND not the spacing; the firing CLOCK an irreducibly random `Exp(1)` (`E[I]=Var[I]=1`) in evidence units (unconvertible to a content `d` without `κ`). Only corpus-robust quantitative statement: the **inequality `d < W_*`**.
- **(B) Spectral-action door — CLOSED for absolute scale; the M0/M1 "resolution" was itself an OVERCLAIM (corrected in Paper-3 R1).** The route lands on `G·Λ²` and fixes only the ratio `G/l_step²`, never absolute G (`Λ=1/l_step` IS the record-scale label) — this is the load-bearing, robust fact. The exhaustion agent's claim that the corpus's "factor-2π gap" is a *resolved* M0-vs-M1 mislabel was UNVERIFIED: the arithmetic (`M0=∫e^{−2πu}=1/(2π)`, `M1=∫u e^{−2πu}=1/(2π)²`, differ by 2π) is right, but WHICH moment carries the EH coefficient is convention-dependent and the corpus computed it as the first moment `[CONJECTURED]`. So it is a **candidate** correction, not settled — and **immaterial to the no-go**. ⇒ NO corrigenda to v6 (the earlier "flag corrigenda" was based on the overclaim); Paper 3 carries the hedged observation instead.
- **(C) Gate G1 — the SOLE load-bearing premise, NOT a closure theorem.** Proved-finite for the executed corpus, interface-level for un-imported v1-v3; the seal-rate primitive **refutes the sharpest counterexample** (weight trichotomy `{0,−1,−2}`) but does NOT discharge the universal quantifier — must NOT be called "closing G1." Every OTHER candidate crack closed sympy-exact (additive anomaly, fractional/irrational roots, ℏ=c hidden scale, log-running transmutation, non-monomial functionals, second-length/mass-gaps).

## PAPER 3 DRAFTED (2026-06-15) — `isp/v7/relativistic-isp-v7-paper3-seal-spacing-no-go.md` + receipt `v7/code/p3_d_nogo.py` (9 checks pass)
10 sections: §1 three-object discipline; §2 what `d` is NOT (W_* a bound); §3 content axis (mode-dependent); §4 evidence axis (random Exp(1)); §5 the no-go (grading trichotomy + tribonacci-algebraic numerology refutation + spectral M0/M1); §6 the permitted weight-0 strength lane (`c_m`); §7 G1 + completeness sweep; §8 open-doors ledger; §9 claims; §10 precision. Title thesis: "the seal spacing is mode-dependent and bound-only, and its cross-tier scale-bridge is a no-go."
- **Review round 1 — 3× minor-revision; ALL numerics verified dps 120 (every receipt passes); pre-geom + single-threaded CLEAN; NO manufactured impossibility, NO buried crack (G1 correctly scoped, seal-rate "refutes not closes", c_m/external correctly open).** Convergent MAJOR catch (all 3): the spectral **M0/M1 "factor-2π gap resolved as a moment mislabel"** was an OVERCLAIM — a NEW unverified claim presented as faithful, contradicting the corpus's honest `[CONJECTURED]` first-moment computation. INTEGRATED: hedged M0/M1 to a *candidate/plausible* labelling ambiguity (convention-dependent, unverified), made "fixes only the ratio `G/l_step²`, never absolute G, regardless" the load-bearing point; receipt `p3` §(5) reframed to compute the moment *integrals* and assert only "they differ by 2π / labeling convention-dependent / no-go independent" (no EH-is-M0 claim). Minor fixes: dropped the unproven "Srednicki a is transcendental" (refutation now rests on algebraicity + converging-away sequence); disambiguated coefficient-dilution `0.609/0.495/0.368` from contents `0.156/0.109/0.063`; §5 "closed within the weight grading (modulo G1)"; ref [57] "distinct invariants, not numerically equal". Receipt 9 checks pass.
- **Review round 2 — 3× ACCEPT (unanimous).** All numerics re-verified dps 120 (E[I]=Var[I]=1; W_*, η_B/C(η_B), h_*/C(h_*); tribonacci cubics PROVED irreducible /ℚ, θ_B² min-poly by resultant; M0/M1; SIGMA-SPLIT); pre-geom + single-threaded CLEAN; **referee 2 explicitly hunted and found NO surviving overclaim** (M0/M1 hedge correct, G1 "refutes not closes" verbatim, tribonacci on algebraicity, unconditionality disclaimed); a referee's own canonical-`d` probe CONFIRMED "only robust statement is `d<W_*`, no canonical `d` wrongly denied." Cosmetic minors integrated: corpus-grounded scheme value (`3π²`, Sakharov `192π²/N` — dropped unsourced `6π²`/`3π²/2`); softened "EH coeff is the zeroth moment" → "under one reading of the CC moment indexing … carried by the zeroth (rather than first) u-moment"; disambiguated the three `κ`s (Tier-A rate / `8πG` / source `W_*`); "corpus partial-wave sequence" qualifier in abstract+§9; §2 strict `d<W_*` tagged to the gated class. Receipts p3 9 / p2b 13 / p2c 25 all pass.

- **PAPER 3 OF THE LONG MARCH — TERMINAL (2026-06-15).** `isp/v7/relativistic-isp-v7-paper3-seal-spacing-no-go.md` + receipt `v7/code/p3_d_nogo.py`. Convergence: R1 (3× minor — the M0/M1 overclaim, fixed) → R2 (3× accept). RESULT: **`d` is NOT a fixed quantity** — mode-dependent content `{0.156, 0.109, …}` (no canonical member, all `< W_*`), a *bound* `W_*` that isn't the spacing, a random `Exp(1)` firing clock; only robust statement `d < W_*`. **The record-derived cross-tier absolute-scale bridge is a NO-GO** — forced grading-homomorphism trichotomy (`c = 2(e−b)`, length always cancels), tribonacci-algebraic numerology refutation (`θ_B` cubic-irrational ≠ Srednicki `a`), spectral-action lands on `G·Λ²`/fixes only the ratio (the `2π` gap a hedged candidate labelling correction, immaterial). **Airtight modulo the single isolated premise G1** (seal-rate refutes the sharpest counterexample but does not discharge it); every other crack closed sympy-exact. Open + permitted: the weight-0 *strength* `c_m = Gm²/ℏc` (a hierarchy, Paper 5) and an undiscovered external structural identity (`1/η_B = 1.641` thread).
- **USER REORDER (2026-06-15): do Paper 5 (`c_m`) BEFORE Paper 4 (entanglement).**

## Paper 5 (`c_m`) SCOUT — verdict: FREE-CALIBRATION, high confidence (2026-06-15)
Workflow `wiejxqz9x` (what-is + derivable/free/blocked + mechanism + adjudication). **`c_m = Gm²/ℏc = (m/M_Planck)²` is the gravity sector's FREE per-species calibration — the strength-axis analog of the scale no-go's free unit `κ`.**
- **WEIGHT-0 / PERMITTED:** `weight(c_m) = +2 + 2(−1) − 0 = 0`, gauge-invariant under `l→μl`; the scale no-go does NOT forbid it (it carries no absolute length).
- **NOT a gravity output (the load-bearing result, paper7 Theorem 6.1):** given the equivalence principle, `c_m` = the matter ledger's spectral gap, and the gravity sector is **complete-and-blind** w.r.t. it — "no gravity-sector identity constrains it… exactly as the Einstein equations do not determine the electron mass. H2 is not a hole in record gravity; it is the matter sector's spectrum problem."
- **Currently FREE:** the toy-value receipt (paper6 §9) — `c_m = 12` AND `c_m = 1200` both pass every executed identity (`κ·σ_A/2π = 1.000000000000`), NOT-SELECTED.
- **THE UNIFICATION (the nice positive contribution):** `c_m = γ_G·ν_m²` (area-norm convention × matter spectral gap), and `ν_m` is **mode-dependent** (`η_B=0.609` vs `h_*=0.495`) — the SAME mode-canonicalization bottleneck as the spacing `d`. So `d` and `c_m` are ONE open problem (matter sector + mode canonicalization), not two. It COMPLETES the weight-0 ledger (the last permitted lane is also not gravity's to give).
- **NOT a third no-go** (nothing forbids it); **NOT derivable now** (needs a matter sector — "beyond all current corpus technology"). Numerics: `c_m(e)=1.75e-45`, `c_m(p)=5.9e-39`, ratio `(m_p/m_e)²`. Gates: H1(EP) proved-conditional (paper7); H2(value) NOT-SELECTED. Rests on gate G1.
- **Is it a paper? YES — as a FREE-CALIBRATION / permission-structure result, NOT a derivation** (must not pin a value).

## PAPER 5 DRAFTED (2026-06-15) — `isp/v7/relativistic-isp-v7-paper5-cm-free-calibration.md` + receipt `v7/code/p5_cm_calibration.py` (7 checks pass)
8 sections: §1 the permitted lane / what `c_m` is; §2 weight-0/eligible; §3 gravity complete-and-blind (Thm 6.1); §4 currently free (toy-value); §5 factorization + shared gate with `d` (the unification); §6 what a derivation requires (matter sector absent); §7 claims (no value pinned; numerology refuted; gate G1); §8 precision. Title thesis: "the gravitational coupling per species is a free calibration — and it shares the spacing's one open gate."
- **Review round 1 — 3× minor-revision; ALL numerics verified dps 80; NO overclaims, NO faithfulness violations; the thesis CORRECT and faithful on both legs.** Catches (integrated): (1) the UNIFICATION was overstated — narrowed to "`c_m` and `d` share ONLY the mode-canonicalization sub-problem; `c_m` additionally needs a matter sector + hierarchy selection," reconciled with Paper III's "categorically distinct in role." (2) Separated H1/EP (makes "the" ratio well-defined) from Thm 6.1 blindness (the p6d scan / toy-value), and scoped G1: the §3 **blindness is PROVED** (EP theorem + scan), NOT modulo-G1; only §2 classification + §5 closure rest on G1. (3) `ν_m`/spectral-gap slip fixed — `ν_m` is the open matter mass (physical `ν_m(e) ≈ 8.4e-23`), the toy `η_B=0.609`/`h_*=0.495` tagged ILLUSTRATIVE (not candidate masses). (4) Toy-value `12/1200` attributed to paper6 §9 (inherited). STRENGTHENINGS added: the factorization is EXACT — `c_m = γ_G·ν_m² = Gm²`, `σ_A` cancels, `γ_G = G·σ_A = 1/4` (Bekenstein, a ratio-of-twins convention); `c_m` is free **within a derived window** (matter gap bounded below by the one-diamond minimum `0.156` nats, paper7 §9.6); the illustrative `0.093` is ~44 orders above the physical `c_m` ⇒ self-evidently not a prediction. Receipt grew to **10 checks** (exact factorization + `σ_A` cancellation + `c_m`-independence of gravity invariants + physical-`ν_m` + illustrative-scale-gap). All pass.
- **Review round 2 — 1× ACCEPT + 2× minor-revision; all numerics verified; the thesis CORRECT/faithful both legs.** The 2 minor catches both targeted the **"derived window"** I added in R1 — and it was a genuine **UNITS ERROR**: the gap theorem bounds the *toy* record evidence-content `m̂` (in nats, `O(0.1)`), NOT the physical `c_m = 1.75e-45`, which sits ~44 orders *below* that toy floor (and the lower bound is anyway unoriented-class-only). Also caught: `ν_m` is NOT `m̂` — `c_m = m̂ = γ_G·ν_m²` is the *content/coupling*, `ν_m = √(m̂/γ_G)` the *mass-amplitude* (different axes). INTEGRATED: **dropped the window claim** (replaced with the honest point — the 44-order toy↔physical chasm IS the unbuilt matter sector, reinforcing "awaits the matter sector"); **relabeled `ν_m`** (mass-amplitude; `m̂ = c_m` the content); added the G1-free-sub-results note (§3 blindness uses Thm 5.1 + algebraic `c_m`-independence, not the pinned-sector Thm 6.1); fixed the receipt print label. Receipt 10 checks pass.
- **Review round 3 — 1× ACCEPT + 2× minor-revision (pure NOTATION); NO math errors, NO overclaims, NO faithfulness violations; window removal + `ν_m`/`m̂` relabel confirmed CORRECT.** Catches (all integrated): (1) `c_m`/`ν_m` symbol-overloading — the inline `c_m = m̂` could be misread as a false 44-order numerical identity; recast as a **role identity across unit systems** (the hierarchy invariant in record-content units `m̂ ≈ 0.156` nats vs SI-strength units `c_m ≈ 10⁻⁴⁵`, bridged only by the unbuilt matter sector, NOT numerically equal); `ν_m` = mass-amplitude (not "the spectral gap"). (2) The two `κ`s disambiguated (the fixed gravitational `8πG` in `κ·σ_A = 2π` vs the free Tier-A conversion-rate `κ` that `c_m` is the strength-axis analog of — Paper III's note). (3) "narrowed unification" → "precise" (the temporal word broke single-threaded style). Receipt 10 checks pass.

- **PAPER 5 OF THE LONG MARCH — TERMINAL (2026-06-15)**, `isp/v7/relativistic-isp-v7-paper5-cm-free-calibration.md` + receipt `v7/code/p5_cm_calibration.py` (10 checks). Convergence: R1 (3× minor, faithfulness + unification narrowing) → R2 (1 accept + 2 minor, the window units-error I introduced, fixed) → R3 (1 accept + 2 minor, notation). RESULT: **`c_m = Gm²/ℏc` is the gravity sector's free per-species calibration** — weight-0/PERMITTED (the scale no-go does not forbid it); provably **NOT a gravity output** (complete-and-blind, Thm 6.1, *proved* via the G1-free EP theorem + algebraic `c_m`-independence, exactly as GR doesn't fix `m_e`); **currently free** (toy `12`/`1200` both pass, the gravity invariants are `c_m`-independent); factors *exactly* `c_m = γ_G·ν_m² = Gm²` (`σ_A` cancels, `γ_G = G·σ_A = 1/4`); shares the spacing `d`'s **mode-canonicalization** sub-problem while additionally needing a constructed matter sector; pins NO value (the physical `c_m ≈ 10⁻⁴⁵` is ~44 orders below the toy record gaps; numerology refuted algebraically). The strength-axis twin of the no-go floor `κ`. **FOUR of the five v7 papers now terminal (I, II, III, V).** NEXT: **Paper 4 (entanglement → spacetime)** — the last & most constructive, and the on-ramp to the matter sector that `c_m`/`d`/masses all wait on.

## Paper 4 (entanglement → spacetime) SCOUT — verdict: CONJECTURAL-WITH-REQUIREMENTS / half-open, high confidence (2026-06-15)
Workflow `wrd9jwa20` (Tier-A entanglement + bridge/circularity + geometry/matter-link + adjudication). **THE BEAUTIFUL UNIFYING RESULT: entanglement → spacetime is exactly HALF-OPEN — non-circular for CONNECTIVITY (weight-0, Van-Raamsdonk-pinch eligible) but no-go-BLOCKED for the METRIC (weight +1 = the SAME forbidden scale as Newton's G and the spacing d).**
- **The load-bearing insight (sympy-exact dps-140 vs the Papers 3/6 grading homomorphism):** entanglement content `χ = D(P_AB‖P_BA)` is **weight-0** (a pure KL number); any record graph DISTANCE/metric is **weight +1** (a length = #hops × `l_step`). The trichotomy proves the records cannot supply the absolute `l_step`. ⟹ entanglement sources **topology/connectivity** (weight-0, non-circular: zero mutual info ⇒ no edge ⇒ pinch-off) and the **conformal class** (Barbour-eligible, de Boer–Czech kinematic-metric form/curvature up to scale) — but provably **NOT a metric** (the distance half is the **G no-go again**). The v7 no-go (the missing `l_step`) is UNIVERSAL: it blocks `d`, `G`, the matter scale, AND the emergent metric.
- **What SHARD HAS (Tier-A, derivable now, non-circular):** a closed-form gauge-invariant spacetime-free bipartite correlation `E_cl` on the transition law `Γ` alone (v1 paper21 Thm 1; Bell-state `E_cl=ln2`, CHSH recovery); the exact Bell address (PI-yes/OI-no/no-signaling/single-outcome, forced by Tsirelson — leans on *external* Jarrett + Barandes); the nonfactorization ontology (v5 paper5); the record graph from the frame-invariant causal order (v6 paper2, boost-inv 1e-97); the Van-Raamsdonk PINCH-OFF as a weight-0 statement.
- **Circularity verdict:** all existing "geometry from entanglement" (de Boer–Czech paper55-B, area-law paper50/57) runs the OPPOSITE (Jacobson) way — geometry assumed, entanglement-area read off as an equation of state, on an already-spatial lattice. NOT vicious (no metric inserted-then-recovered) but induced-gravity-grade. ONE vicious trap to avoid: paper2 §2 scores recovered coords against "the TRUE simulation coordinates" from a metric-generated sprinkling. The weight no-go IS the anti-circularity guardrail (stay weight-0 = legitimate; smuggle `l_step` = circular).
- **THE DECISIVE OPEN PREREQUISITE:** the **joint Tier-A click-law** on parallel entangled chains (carrying PI-yes/OI-no) is UNWRITTEN and unforced — Paper I §3.5/§5.2 leaves it verbatim **[OPEN]**. Single-chain sealing was forced to `S=exp(−κχ)` by refinement-multiplicativity; the parallel/correlation direction has NO analogous forcing, and the parallel cocycle `A_{D1×D2}=A_{D1}+A_{D2}` is ADDITIVE = the WRONG (factorizing) object — the entangling law must be its **non-additive complement**, unidentified. Without it, "entanglement" has no derived dynamics to source anything.
- **Matter-sector link:** partial (shared modular-first-law gate, not a shared mechanism). The "particle = stable long-range entanglement link, mass = gap" ontology the user floated is **NOT in the corpus** (zero hits) — a brand-new program, premature; gated behind the same matter sector as `d`/`c_m`.
- **Is it a paper? YES as a SCOPED structural / no-go-mirror result NOW; PREMATURE for the full bulk-emergence claim.** Honest thesis/title: *"Entanglement sources the record graph but not its metric — the connectivity half of Van Raamsdonk is weight-0 and non-circular; the distance half is the Newton-constant no-go again."* Contents: (1) the connectivity/metric weight-split theorem; (2) the circularity audit (Jacobson-direction diagnosis + the paper2 §2 trap named); (3) optionally a *blind* abstract-entanglement → conformal-class reconstruction (`d_ij ~ −log I(i:j)`, scale free) if doable without a metric-generated substrate.
- **Scout's recommended SEQUENCE:** (1) FIRST attack the dynamical gate; (2) THEN write the scoped Paper 4; (3) optional blind conformal-reconstruction test; (4) do NOT manufacture a metric / pursue "particle=long link" yet. **USER: "Both — gate then fold into Paper 4."**

## Paper 4 DYNAMICAL GATE — CLOSED IN STATUS: FORCED-SKELETON-FREE-COMPLEMENT, high confidence (2026-06-15)
Workflow `wcvj1tclf` (forcing + non-additive complement + OI/PI consistency + synthesis; 3 receipts, **67 asserts** total, dps 120-140). **The joint Tier-A click-law on parallel entangled chains = FORCED-SKELETON, FREE-COMPLEMENT.**
- **Skeleton FORCED:** the multiplicative survival SHAPE `exp(−κχ)` transfers verbatim to the joint scalar argument (Cauchy residual 0, `p=1` only, single `κ`, product-limit forces exp) — content-blind.
- **Additivity NOT forced (the crux):** the joint odometer splits EXACTLY `σ_AB = σ_A + σ_B + I_σ` over the product space. Two refinement directions: (R-seq) commit-order concatenation = the only one the single-chain Cauchy uses — **telescopes EXACTLY (gap 0) while `I_σ = −0.0712 ≠ 0`**, i.e. provably BLIND to the mutual term; additivity ≡ (R-par) the separate `A|B`-cut factorization, never invoked. So **entanglement is PERMITTED, never forced, never forbidden**. Exact: `S_AB/(S_A·S_B) = exp(−κ·χ_AB) = exp(−κ·I_σ)` (gap 0) — the factorization defect IS the exp of the mutual content.
- **The entangling complement = ONE object, FREE:** `χ_AB = I_σ` (interaction information) = `E_cl` (mutual-info deficit = nearest-product KL; `E_cl(Bell)=ln2`, `E_cl(product)=0`) = the OI-factorization defect. FREE given marginals (a continuum at fixed marginals), TWO-SIDED in sign; constrained ONLY by no-signaling (PI gap 1e-141) + Bell/Tsirelson (`CHSH ≤ 2√2` saturable, PR-box `4` excluded). Forced/unique only as a functional of the full bipartite kernel.
- **OI/PI:** the forced skeleton PERMITS (≠ forces, ≠ forbids) OI-violation — survival-multiplicativity ⊥ outcome-factorization; supports a full OI-violating/PI/no-signaling/`CHSH=2√2` correlation, realizing the Paper-14 keep-PI/drop-OI address. **Upgrades Paper I §5.2 from bare [OPEN] → "shape forced, no-signaling the only extra constraint, entanglement content FREE."**
- **Half-open verdict CONFIRMED & refined (not flipped):** metric-no stands (κ, dχ untouched, orthogonal by design); connectivity-yes sharpened (the relational content is forced-skeleton with a free complement). Open residues: (1) what `χ_AB` IS for the gravitational record process (needs the joint ledger — same intrinsic-supply question as the single-chain dχ); (2) whether a TRANSVERSE self-consistency principle (parallel-chain analog of the one-diamond KL=Fisher) could FORCE `χ_AB` (the next investigation, the only free→forced route); (3) the full Bell realizability (continuum exhibited, PR-box excluded, but 2√2 not derived from the records); (4) E_cl measure properties (inherited).

## PAPER 4 DRAFTED (2026-06-15) — `isp/v7/relativistic-isp-v7-paper4-entanglement-graph-not-metric.md` + receipts `p4a_joint_clicklaw.py`(23), `nonadditive_entangling_complement.py`, `f5_oi_pi_consistency.py`(22), `p4b_connectivity_metric_split.py`(4) — all pass.
8 sections: §1 the constructive direction; §2 the Tier-A entanglement SHARD has (E_cl on Γ, nonfactorization, Bell address); §3 the joint click-law = forced-skeleton-free-complement (the gate); §4 the connectivity/metric WEIGHT-SPLIT (χ weight-0 / metric weight+1 = the l_step no-go a 3rd time); §5 what entanglement DOES source (pinch-off + conformal class); §6 the circularity audit (Jacobson-direction diagnosis; the score-vs-true-coords trap); §7 claims; §8 precision. Title thesis: "entanglement sources the record graph but not its metric — Van Raamsdonk's connectivity half holds at weight-0; his distance half is the Newton-constant no-go again." THE UNIFYING CAPSTONE: the missing `l_step` no-go is UNIVERSAL — it blocks `d` (II–III), `G` (57), the matter scale (V), AND the emergent metric (IV).
- **Review round 1 — 1× ACCEPT + 2× minor-revision; all numerics verified dps 120; pre-geom + single-threaded clean; no manufactured-geometry overclaims; the half-open thesis + the Paper-III trichotomy reuse ("Newton-G no-go a third time") declared faithful.** Two substantive catches (ref 2): **CRITICAL** — `χ_AB = I_σ = E_cl` "one object" was sign-inconsistent: `I_σ` (entropy-production interaction information, the dynamical defect in the survival law) is TWO-SIDED (`−0.0712`), `E_cl` (nearest-product KL, kinematic) is `≥ 0` (`ln2` Bell); they share a zero-set but are DISTINCT functionals → **decoupled** (`χ_AB = I_σ` signed/dynamical; `E_cl` ≥0/kinematic). **MAJOR** — "conformal class" MISLABELED: the `d_ij = −log I` construction carries an ADDITIVE zero-point gauge under `I→sI` (fixes distance *differences*, not the ratios `d_ij/d_kl`) → corrected to **"the metric up to the ONE overall scale"** (the physical modulus is `l_step`, multiplicative; under `l_step→μl_step` the RATIOS are invariant = the relative/similarity class); curvature/Barbour-conformal dropped; p4b revised (now **5 checks**: ratios-invariant-under-l_step + the `−log I`-additive-gauge contrast). Minors: `χ` vs `χ_AB` label (entanglement content = the mutual term); f5 `22→21`; `[21]/[5']/[14]` citation markers. All integrated.
- **Review round 2 — 2× ACCEPT + 1× major-revision; all numerics verified (incl. an independent 2nd kernel); pre-geom + single-threaded clean; the gate declared FAITHFUL and "more careful than its own scout note."** Referee 2's **CRITICAL** (real): `χ_AB = I_σ` is NOT "zero iff factorized" — the *signed* `I_σ` (a difference of entropy productions) can **cancel at a genuinely correlated kernel** (referee built `I_σ ≈ 0` at fact-defect `0.09`, `E_cl > 0`); only the *non-negative* `E_cl` is zero-iff-factorized (paper21 Prop 1a). + **MAJOR**: "full relative/similarity geometry" over-states — the `−log I` distance carries an *additive* gauge; what's actually delivered is the *unweighted connectivity graph's hop-metric* up to one scale. INTEGRATED: corrected `I_σ = 0` to **necessary-not-sufficient** (`{factorized} ⊊ {I_σ=0}`; only `E_cl` iff), **reproduced + machine-backed the counterexample** (convex mixture, `I_σ = 1.45e-62` at fact-defect `0.094`, added to p4a → **24 checks**); scoped the positive claim to **"the connectivity graph's hop-metric up to one overall scale"** (integer shortest-path; ratios `l_step`-invariant; *not* a continuous similarity class / curvature / conformal class); fixed the p4b prose/banner ("conformal"→hop-metric). Both fixes are in the HONEST direction (the critical makes the complement *freer*). Abstract/§3/§4/§5/§7/§8 all updated. Receipts p4a 24 / nonadditive / f5 21 / p4b 5 all pass.
- **Review round 3 — 3× ACCEPT (unanimous).** All numerics re-derived with FRESH kernels at dps 100-140; gate FAITHFUL; the `I_σ`/`E_cl` decoupling + necessary-not-sufficient + the counterexample correct; OI-permission + weight-split + "Newton-G no-go a third time" all faithful (not manufactured); pre-geom + single-threaded clean; every overclaim-disavowal present. Minor writing folds integrated: §3 scoped the transferred exp shape to the divisible/dense-seal regime + the single-channel screen (Paper I §3.2) and clarified the single-scalar premise IS satisfied for `X=σ_AB` (the new freedom is in `X`'s *decomposition* across the A|B cut); §7 disclaimer that the Tsirelson ceiling `CHSH≤2√2` / PR-box exclusion is an EXTERNAL bound (Tsirelson; Barandes-Hasan-Kagan), respected not derived; p4b hop-distances clarified as illustrative. Receipts all exit 0.

- **PAPER IV OF THE LONG MARCH — TERMINAL (2026-06-15)**, `isp/v7/relativistic-isp-v7-paper4-entanglement-graph-not-metric.md` + receipts `p4a_joint_clicklaw.py`(24)/`nonadditive_entangling_complement.py`/`f5_oi_pi_consistency.py`(21)/`p4b_connectivity_metric_split.py`(5). Convergence: R1 (1 accept + 2 minor incl. critical+major) → R2 (2 accept + 1 critical) → R3 (3× accept). RESULT: the joint click-law is **forced-skeleton-free-complement** (exp shape forced on the joint scalar `X=σ_AB`; the entangling content `χ_AB = I_σ` is a FREE Tier-A input — `I_σ=0` necessary-not-sufficient, only the non-negative `E_cl` is zero-iff-factorized; the forced skeleton PERMITS the OI-violating Tsirelson-saturating Bell correlation); and **entanglement → spacetime is HALF-OPEN** — it sources connectivity + the graph's hop-metric up to one scale (weight-0, non-circular) but provably NOT the absolute metric (weight +1 = the Newton-G `l_step` no-go a third time).

## POST-PENTALOGY FRONTIER PROGRAM (2026-06-15) — user: "address all 3 (covariance / matter / transverse); first review v6-v7 + my ideas + literature"

### LANDSCAPE (workflow `w147ml8ei`, 9 agents, high conf): the 3 frontiers + the 2025 literature levers
The `l_step` no-go is the verified capstone — DO NOT re-attack. Three orthogonal open frontiers, each with a sharp literature anchor:
- **Transverse (`χ_AB` free→forced?):** likely PROVABLY FREE — the almost-quantum set **Q̃** (Navascués-Guryanova-Hoban-Acin 2015, arXiv:1403.4621) is a pure self-consistency/SDP set that strictly contains quantum Q yet satisfies every info principle. Rescues: CDP purification; Tsirelson is principle-derivable.
- **Covariance:** the master-equation layer was SOLVED 2025 (**Gundhi-Diósi-Carlesso**, arXiv:2507.06954: local-Lorentz-scalar collapse + LI colored noise → microcausal all-orders + finite energy). The objective interacting-field STATEVECTOR is stuck field-wide (**Fedida 2025**, arXiv:2506.14693: needs unitarily-equiv Hilbert spaces across Cauchy surfaces, which interacting QFT lacks). Borrow the master eq; scope the statevector to the field wall.
- **Matter sector:** H1(EP) closed, H2(hierarchy) open ("beyond current tech"). SHARD has a forced gap `m̂≥0.156` + an exact record Ginsparg-Wilson op + lattice index theorem + anomaly cancellation + 3-generations (v6 YM line) — maybe AHEAD on chiral fermions. Template = **Levin-Wen string-nets** + the **PSG masslessness-protection** tool (converts "mass=record gap" into a precise claim). The whole field stops at massless; one mass RATIO as a record gap = past the field. Shared bottleneck = mode-canonicalization (d, c_m, masses are ONE problem).
- **Recommended order:** VI transverse (fastest) → VII covariance (borrow 2025) → VIII matter (the moonshot arc). Biggest risk: 2 of 3 are likely no-gos + the 3rd a multi-paper moonshot; mode-canonicalization may be a 5th `l_step`-type under-determination. SHARD novel on: pre-geometric DYNAMICAL derivation, no-go honesty, attempted mass-as-gap, exact record GW. Borrow: 2025 master eq, Q̃+NPA, PSG, CDP purification, slow/fast hierarchy.

### PAPER VI — TRANSVERSE: RESOLVED = SHARP NO-GO + Tsirelson positive (workflow `wkz0l9pt5`, high conf, 3 receipts)
**`χ_AB` is FREE-UP-TO-Q̃ — a sharp no-go with a real positive corollary.** Receipts `v7/code/t1_npa_q_vs_qtilde.py`(8/8, cvxpy SDP), `t2_purification_uniqueness.py`, `t3_tsirelson_derivation.py`(17/17), all exit 0.
- **NO-GO (T1, the spine):** any transverse self-consistency principle the record substrate can host is a moment-matrix-PSD/SDP condition (the two chains meet ONLY via the shared joint moment `⟨A_x B_y⟩` = commutation ON THE STATE, not a global Hilbert tensor product) → lands EXACTLY on **Q̃ = Q^{1+AB}** (strictly ⊋ Q). NECESSARY (reproduces the whole Paper IV envelope: no-signaling + Tsirelson + PR-box exclusion — NPA level-1 caps CHSH at `2√2` to 2.8e-10, PR-box min-eig −0.414 infeasible, Tsirelson saturated min-eig 0) but NOT SUFFICIENT (I3322 needs NPA level≥4; 1+AB overshoots). So `χ_AB` free within Q̃; the supraquantum gap Q̃\Q is PERMANENT for any self-consistency principle.
- **THE GAP HAS ONE NAME (T1+T2 unified):** the missing input is the **global tensor product / local tomography `K_AB=K_A·K_B`** (the complex-over-real selector). SHARD's seal is provably FIELD-BLIND (`E=E*=E²` and the q=2 Banach-Lamperti screen hold identically over ℝ and ℂ; real-vector/rebit QT is the explicit Q̃-foil: same marginals, same Tsirelson `2√2` to 30 digits, but DIFFERENT `χ_AB`). Global-tensor-product (T1) and local-tomography (T2) are ONE obstruction, two faces.
- **POSITIVE COROLLARY (T3): Tsirelson is inheritable** — `CHSH≤2√2` falls out of the moment-matrix self-consistency AND SHARD owns the full static Information-Causality substrate (KL mutual info: non-negativity, no-signaling-consistency, DPI, chain rule — 17/17, chain-rule residual 3.6e-122). So SHARD promotes Tsirelson from external postulate to THEOREM (bounds `χ_AB`, doesn't pin it — Tsirelson is field-blind). One lever short: the operational RAC usage for full IC-as-theorem.
- **THE FIFTH WALL:** same no-go SHAPE as `l_step`/Newton-G — self-consistency fixes the ENVELOPE, never the last record-blind distinguishing input (here: the global tensor product). SHARD REPRODUCES the field's Q-vs-Q̃ wall from first principles rather than evading it. Paper VI = a sharp scoping no-go (4 sections), publishable; mirrors paper40/57.
### PAPER VII — COVARIANCE: RESOLVED = master-equation covariantized (conditional) + statevector scoped to field wall (workflow `wy7cg1v18`, high conf, receipts c1/c2 exit 0)
**A disciplined Tier-B emergence theorem (partial/conditional positive, not a full covariance solution).**
- **WIN (C1, headline):** the SHARD seal is the RIGHT KIND of object for the 2025 microcausality proof — algebraically self-adjoint weight-0 Lorentz SCALAR (S, Tier-A: `E=E²=E*`, χ relabel-invariant KL) + an already-LI positive-type colored kernel `f(s²)` (N, Tier-B: Gram min-eig +2.78e-9). THEREFORE **IF** the diamond coarse-grains so the seal-firing density is **POINT-LOCAL (collar→0)** under an emergent Lorentz action, SHARD **INHERITS VERBATIM** Gundhi-Diósi-Carlesso 2025's all-orders microcausality + the spacelike `[H(x),H(y)]=0` half of TS-integrability — the master-equation/decoherence layer is **covariantized**. The move RE-EXPRESSES the open microcausality frontier as ONE sharp borrowable premise: **"point-local-scalar emergent seal density"** (region-smeared seals provably break MCC at n=1 — verified on a finite causal lattice). A real reduction of the open surface.
- **C2 (honest partial):** SHARD does NOT derive the noise KERNEL (inter-seal profile is wrong-type + free; ≥3 kernels share one cutoff). But principled discreteness makes the noise being **COLORED structural-by-construction** (built-in UV cutoff → avoids the white-noise/infinite-heating catastrophe BY CONSTRUCTION — the literature's hardest ad-hoc move). Write "colored noise is structural, not ad hoc" — NOT a derived kernel.
- **C3 (honest wall):** the objective interacting-FIELD STATEVECTOR (variable particle number) is the **FIELD'S SHARED WALL** — Fedida 2025 unitary-inequivalence (generic interacting QFT lacks unitarily-equiv Hilbert spaces across Cauchy surfaces). Residue B = B1(does an interaction-local TS field unitary exist) × B2(insert collapse given B1); every factor except B1(field) discharged. B1 blocks Tumulka/CSL/GRW/Fedida equally — SHARD is NOT worse off. Stay stochastic-LINEAR (g_nl=0) to avoid the Hsu deterministic-nonlinear no-go.
- **Paper VII = the Tier-B covariant-decoherence theorem** (conditional master-eq covariantization + colored-noise-structural + honest statevector scoping). Publishable. Receipts c1/c2 + the C3 factorization. Cite GDC 2507.06954, Fedida 2506.14693, Tumulka, Hsu 2511.15935, Diósi 2602.06845.
- **Paper VIII (matter sector) scout launched (`wg1wxaxj8`)** — M1 PSG/gap/chiral, M2 mode-canonicalization (THE LINCHPIN: 5th wall or closeable?), M3 hierarchy mechanism. The moonshot.

### PAPER VIII — MATTER SECTOR: RESOLVED = synthesis-plus-FIFTH-no-go arc (workflow `wg1wxaxj8`, high conf, receipts `v7_m1_psg_gap_chiral_receipt.py` + `m2_mode_canonicalization.py`, both exit 0)
**SHARD derives the matter sector's FORM + mode RATIOS but is STRUCTURALLY BARRED from a canonical mode — so NO single physical mass ratio is computable as a record gap now. A real paper, but a no-go-plus-arc, not a mass-prediction moonshot. Do NOT manufacture a mass prediction.**
- **M1 (PSG / gap / chiral) — ATTACKABLE NOW (partial):** the Levin-Wen/PSG recast is a DEFENSIBLE synthesis (not a metaphor). SHARD owns the **three constructive pieces the entire it-from-qubit field assumes but never builds**: (1) an **exact record Ginsparg-Wilson operator** (paper14 Prop 2.1: GW identity 4.0e-15, covariant 1.1e-15, exp-local 0.97/unit — single species, 1 vs 4 Brillouin zeros → **evades Nielsen-Ninomiya record-natively**); (2) a **lattice index theorem** that holds exactly (index(D)=Q in 14/14 flux sectors, integer even on rough sectors with no continuum field, paper14 §3); (3) **anomaly cancellation as a theorem-grade filter** (paper14 Thm 6.1, minimal stack `{1,4,4|2,2,5}` — Wen's anomaly-free GATE, but PROVEN). The mass mechanism: **"mass = the record gap when PSG-protection breaks"** — THEOREM-grade via Thm 9.4: the **unoriented (vector-like)** floor `m̂ ≥ W = η·θ − log cosh η = 0.156109200157240` (Griffiths/ferromagnet mechanism, `h_a ≤ η_hist`; "one constant three roles" Δ = m̂ = W to <1e-40), while **oriented (chiral)** ledgers BREAK Griffiths (`h_a=1.984 > η`) and **collapse below W as ~2^−n** (n3=0.1335 exact, n4=0.0645 exact, n5/n6 sampled UBs; ratios 1.17/2.07/2.03). This unoriented-gapped / oriented-gapless dichotomy IS the PSG dictionary made literal (vector-like = unprotected = gapped at W; chiral = protected = asymptotically massless). **Caveat:** the gapped half is a theorem; the **protected⟹gapless half is a CONJECTURE** (O2: data through n=6, asymptotics open — does the oriented minimum hit 0 at finite n or only as n→∞?). Ship as theorem + computable-conjecture; do NOT claim a finished biconditional. Open: ledger-chirality ↔ Weyl/PSG-chirality bridge unbuilt; quartic/interacting GW flow (O8) open; the PSG group H¹(SG,IGG) not yet reconstructed from record data. **NOTE: the prompt's arXiv:2507.21996 is UNRELATED (Sahakian entropic gravity) — transposed ID; M3 used Sakharov/Dvali/matrix instead.**
- **M2 (mode-canonicalization) — FIFTH WALL (the decisive verdict, 10/10 dps=100):** NOT closeable by any of the three non-circular routes — a **fifth instance of the `l_step`-type under-determination**. Self-consistency fixes the FORM (the firing fixed point `∇ψ(h)=e^{−h}` holds in EVERY mode/sector) but never WHICH ledger/mode is physical. **The one structural reason** (verified): the seal modes are **gauge-INEQUIVALENT** — they differ by **ledger rank** (number of orthogonal primitive characters: 1/3/7), an invariant no record-internal gauge map can change, so they sit in **distinct superselection sectors** (the Wen-PSG hallmark of genuinely distinct quantum orders that NO internal principle canonicalizes). All three routes fail in unison: **Route 1** (completeness axiom, paper10 T3/T4 template) — both horns fail: if the mode-label is RECORDED, completeness forces the MAXIMAL ledger → pushes the coefficient DOWN the dilution ladder (0.6094→0.4951→0.3680→limit), AWAY from η_B; if SILENT, the modes are recorded-distinct (`D(P_ηB‖P_h*)=0.00479>0`) so the T3 dissolution does not apply. **Route 2** (external identity `1/η_B=ξ_1d`) — sympy-exact TAUTOLOGY: `ξ=1/η` IS the seal equation `tanh η=e^{−η}` restated, and the mass-gap length is mode-COVARIANT (`ξ(h_*)=1.2814 ≠ ξ(η_B)=1.6410`), each mode owns its own ξ; Paper III already files `1/η_B` as weight-0 record-INTERNAL. **Route 3** (variational) — max-content→η_B vs completeness→dilution-limit point in OPPOSITE directions; the firing clock is mode-blind (`E[I]=1` in every mode); any in-class extremum is circular (the admissibility gate IS the mode-choice). **The sole escape is OUTSIDE the three routes and does NOT close the wall:** a constructed matter Hamiltonian that energetically selects one ledger rank (the PSG "import external data" escape) RELOCATES the choice to a sector that does not yet exist — it changes WHERE the choice lives, not WHETHER it is free.
- **M3 (hierarchy mechanism) — PARTIAL (FORM-only):** the record(slow)/substrate(fast) split DOES supply a genuine MECHANISM for the smallness `c_m=Gm²/ℏc ~ 1e-39` — but in FORM only: structurally a **1/N mode-separation RATIO** (large mode/species/event count), a near-isomorph of Sakharov induced gravity / Dvali / the matrix slow-fast split (all weight-0, running on SHARD's record/substrate two-mode split). But the controlling count `N=1/c_m~1e38` is **weight-0 and NOT corpus-forced** (natural record gaps are O(1)..O(10) — ~37 orders short) → one free per-species calibration survives, **exactly Paper V's `c_m=γ_G·ν_m²`**.
- **A MASS RATIO NOW? GATED — not reachable.** Computing even ONE physical mass ratio as a record-gap pure number is DOUBLY gated: (a) by the **fifth wall** (which mode/ledger-rank = the electron vs the proton — record-internally undecidable: the three observables a ratio would draw on — `d`, `c_m`, the per-mode masses `ν_m` — are ALL firing-fixed-point outputs and the fixed point is rank-degenerate, no canonical member); (b) by the **unbuilt ledger-chirality↔Weyl-chirality identification** + the conjectural protected⟹gapless asymptotics (O2). SHARD computes mode-RELATIVE gaps + the chiral 2^−n collapse RATIOS + the hierarchy FORM, but NOT the absolute "this mode = the electron." **Honesty mandate: no mass prediction is licensed by the executed corpus.**
- **Paper VIII = a two-part synthesis-plus-fifth-no-go arc** (NOT a moonshot). Title around "the record matter sector and the mode-canonicalization wall." PART A (attackable now): the PSG/Levin-Wen recast (SHARD owns the 3 constructive pieces) + "mass = the record gap when PSG-protection breaks" (Thm 9.4 + the O2 conjecture). PART B (the honest wall): mode-canonicalization is the **fifth named `l_step`-type wall** (gauge-inequivalence diagnosis) → form + ratios YES, canonical mode / absolute mass ratio NO; the hierarchy smallness is FORM-only (1/N, one calibration survives). Papers III/V should formally register M2 as the **fifth named wall**. PREREQUISITE for a future moonshot (NOT this paper): a Tier-1 functor record-orientation-classes → PSG group, the ledger-chirality↔Weyl-chirality bridge, and a constructed matter Hamiltonian that energetically selects one ledger rank. Cite Levin-Wen cond-mat/0404617, Wen cond-mat/0107071, Nielsen-Ninomiya Nucl.Phys.B185, Wen arXiv:1305.1045.

## ★★★ ALL THREE POST-PENTALOGY FRONTIERS RESOLVED (2026-06-15) — the research phase is COMPLETE ★★★
**The unifying meta-result: SHARD's records carry everything FORM / RELATIVE / weight-0 (forced laws, shapes, ratios, the connectivity/conformal structure) but are STRUCTURALLY BARRED from exactly THREE "last distinguishing inputs," each a self-consistency-fixes-the-envelope-never-the-last-choice wall:**
- **(I) the absolute SCALE `l_step`** — appears FOUR times: the spacing `d` (II–III), Newton's `G` (57), the matter-hierarchy value `c_m` (V), the emergent metric (IV). [the pentalogy capstone]
- **(II) the global TENSOR PRODUCT / local tomography** (`K_AB=K_A·K_B`, the ℂ-over-ℝ selector) — the transverse `χ_AB` free-up-to-Q̃ wall (VI). [the chains meet only via the shared joint moment, never a global Hilbert tensor product]
- **(III) the canonical MODE / ledger rank** — mode-canonicalization (VIII), the gauge-inequivalent superselection sectors barring "which mass is which." [the matter-sector wall]
Each is the SAME no-go SHAPE three times over: a self-consistency principle fixes the form/envelope/ratios, never the final record-blind input. **Covariance (VII) is the lone CONDITIONAL POSITIVE** — the master-equation layer covariantizes now (on one sharp emergence premise), with the objective interacting-field statevector honestly scoped to the field-wide Fedida wall (shared by the whole collapse field).
- **Frontier verdicts:** VI transverse = sharp no-go (tensor-product wall) + Tsirelson inherited as theorem; VII covariance = conditional master-eq covariantization + colored-noise-structural + statevector→field wall; VIII matter = synthesis-plus-fifth-no-go (mode wall) + SHARD genuinely ahead on chiral fermions (exact record GW).
- **NEXT = the WRITING PHASE:** write Papers VI, VII, VIII, each from its resolved verdict + receipts, via the same discipline (write → hostile-review-until-terminal → integrate). VI = sharp scoping no-go (4 sections, mirrors paper40/57); VII = disciplined Tier-B covariant-decoherence theorem; VIII = synthesis-plus-fifth-no-go arc. Papers III/V get a one-line registration of M2 as the fifth named wall.

## ★★★ WRITING PHASE COMPLETE (2026-06-15) — Papers VI, VII, VIII all TERMINAL ★★★
All three frontier papers written and driven to terminal via the standard discipline (write → hostile-review → integrate → confirming review). Each: math independently reproduced by referees at dps 60–140, pre-geometric + single-threaded clean, overclaims hunted both directions. **8 of 8 v7 papers now terminal (I–VIII).**
- **Paper VI — `relativistic-isp-v7-paper6-transverse-almost-quantum.md`** (receipts t1/t2/t3). R1 (1 accept + 2 minor) → R2 confirming (3× minor, all fixes confirmed). RESULT: the transverse entangling content `χ_AB` is **free up to the almost-quantum set Q̃**, a sharp no-go; the missing structure is the **composite state space** (the global tensor product, and local tomography within it — two distinct axioms, one origin); **Tsirelson is inherited as a THEOREM** via moment-positivity (`Γ⪰0` caps CHSH at 2√2), witnessed independently by the static IC core (one operational lever short). R1 catches integrated: (C-form)/(C-level) split + the **level-independent fallback** (commutation≠tensor at every level — Tsirelson's problem, Slofstra/MIP*=RE — so the no-go does NOT rest on the single-pair-moment premise); "global tensor product ⟺ local tomography" de-conflated (rebit has tensor product, fails local tomography); Tsirelson re-anchored from the IC route to moment-positivity. R2 residual: Tsirelson's-problem citation added.
- **Paper VII — `relativistic-isp-v7-paper7-covariant-decoherence.md`** (receipts c1/c2). R1 (1 accept + 2 minor) → R2 confirming (**3× ACCEPT, unanimous**). RESULT: the decoherence layer is **covariantizable conditionally** — SHARD supplies the scalar seal + LI positive-type kernel at Tier B, and **inherits GDC-2025 all-orders microcausality IFF** the diamond coarse-grains to a point-local scalar seal density (the locality⟺spacelike-commutation hinge; region-smeared fails at order 1); the **colored noise is structural-by-construction** (NOT a derived kernel); the objective interacting-field statevector is the **field-shared Haag wall**. R1 **major faithfulness fix**: the wall re-attributed from Fedida (mis-cited as proving it) to the **Haag obstruction** — Fedida 2025 actually derives positive Einstein-causality assuming the equivalence, now cited as corroborating the microcausality side (verified against the live arXiv abstract by R2); + the inheritance scoped to the microcausality half (not finite-energy), "recursion"→"hinge", kernel→Tier-B, positive-type→SHARD-strengthening (Bochner-exact, +2.78e-9 a finite-sample certificate), B2-conditional, GDC fabricated title removed. R2 residual: Torre–Varadarajan 1999 citation added for the d>2 TS clause.
- **Paper VIII — `relativistic-isp-v7-paper8-matter-sector-mode-wall.md`** (receipts m1/m2). R1 (2 minor + 1 accept) → R2 confirming (**ACCEPT**). RESULT: a synthesis-plus-fifth-no-go arc — SHARD owns the three constructive pieces (exact record GW evading Nielsen-Ninomiya, lattice index theorem, anomaly filter) and is **ahead on chiral fermions**; "mass = the record gap when protection breaks" is a **theorem** on the unoriented branch (W=0.156109200157240, three roles to <1e-40) + a **conjecture** on the oriented branch (2⁻ⁿ, exact n=3,4); **mode-canonicalization is the fifth wall** (gauge-inequivalent superselection sectors, ranks 1/3/7; all 3 routes fail); the hierarchy is **form-only** (1/N, one calibration = Paper V); **no mass ratio computable now** (doubly gated). R1 **major math fix**: the false/superseded oriented prefactor `c≈0.27` dropped (actual c≈1.0; the honest m(n)·2ⁿ≈1.0 + ratios 2.069/2.033/2.016 *strengthen* the conjecture) — fix also propagated into the m1 receipt (corrected the cosmetic ratio-label off-by-one + the superseded-0.27 print); + fifth-wall framing clarified (same shape as l_step, distinct missing input = the mode); orphan Barandes ref dropped. NO mass prediction manufactured.
- **All 7 frontier receipts pass (exit 0): t1/t2/t3 (transverse), c1/c2 (covariance), m1/m2 (matter).** The post-pentalogy program is COMPLETE: three frontiers resolved (VI no-go + VII conditional-positive + VIII synthesis-plus-no-go) and three papers terminal. The SHARD v7 corpus stands at 8 terminal papers (I–VIII) + the meta-result (records carry everything relative; barred from exactly three last inputs — scale, tensor product, mode). Remaining genuinely-open frontier = the matter-sector PREREQUISITE for a future mass ratio (the Tier-1 PSG functor + ledger↔Weyl chirality bridge + a mode-selecting matter Hamiltonian), explicitly named in Paper VIII §8.

## ★★★ v7 PENTALOGY COMPLETE (2026-06-15) — all five papers TERMINAL (I, II, III, IV, V) ★★★
The Long March is complete. Unified close: **the SHARD record click-law is FORCED up to a single absolute scale `l_step`, that scale is provably NOT the records' to supply, and its absence is the ONE wall behind every open number** — the seal spacing `d` (II–III), Newton's `G` (57), the matter hierarchy `c_m` (V), and the emergent spacetime metric (IV). The records carry everything *relative*/weight-0 (shapes, ratios, the conformal/connectivity structure, the forced laws) but never one absolute unit. Genuine open frontier remaining (cross-cutting, not a hole in any one paper): a **record matter sector** with a canonical mode/mass spectrum (the shared gate behind `d`, `c_m`, the masses, and full bulk-emergence), and the **transverse self-consistency principle** that could convert the entangling content `χ_AB` from free to forced. 14 receipts across `v7/code/` (f1–f5, p2a–c, p3, p4a/b, p5, nonadditive, etc.), all high-precision, all pass.

## POST-FRONTIER PROGRAM — Paper IX (2026-06-15), user: "do C1–C4 in whatever order you see fit"
After the three frontier papers (VI/VII/VIII), a landscape scout (`wjultbdvp`, 4 investigators + adjudicator) ranked four candidate next-papers and fixed the sequence: **IX = C1+C4 folded** (do first); C3 (graviton spin-2) deferred to a possible Paper X; **C2 (discharge VII's covariance premise) PREMATURE/BLOCKED** — the corpus's own BHS no-finite-valency lemma forbids point-locality of the seal collar from discreteness alone, so it would import the unbuilt matter sector; only a premise-sharpening note is honest.

### PAPER IX — TERMINAL (2026-06-15): `relativistic-isp-v7-paper9-chiral-gap-and-chirality-nogos.md` + receipts `p9a_chiral_gap_closed.py`, `p9b_psg_ingredient_functor.py`, `p9c_chirality_bridge_nogo.py` (all pass).
Settles the three matter-sector prerequisites Paper VIII §8 left open — **one conjecture closed into a theorem, two open gaps converted to proved no-gos** (the program's signature "sharper boundary, not a mass"). Convergence: R1 (3× minor — all math independently verified CORRECT by referees, incl. the exhaustive n=4 they ran) → R2 confirming (2 accept + 1 minor, the 1-minor's two docstring residuals already fixed by the concurrent referee).
- **[THEOREM] the closed chiral-gap law (C4).** The oriented (chiral) minimum over all `2^(2ⁿ−1)` orientation classes is attained at the **alternating-by-weight** orientation `ε_a=(−1)^{|a|−1}`, which collapses the tilted law to two values (`T(s)=−[∏ᵢ(1−sᵢ)−1] ∈ {−(2ⁿ−1) on all-minus, +1 on the other 2ⁿ−1}`), giving the scalar fixed point and **`m̂_min(n) = −ln(1−2⁻ⁿ) − δ_n`** (`δ_n ~ 2^{−n(2ⁿ−1)}`, doubly-exp small, sign-definite): **strictly positive at finite n, asymptotically gapless as n→∞, prefactor EXACTLY 1**. Verified to corpus anchors at dps=130 (n=5 `0.031748698…`, a corpus sampled-bound, is the closed-form value) + EXHAUSTIVE brute force at n=2,3,4 (all 32768 at n=4 confirms global min). Closes VIII's "protection-exact⇒gapless" conjecture into a theorem **mod one finite global-optimality lemma** (exhaustive through n=4; closed form for n≥5). VIII's "theorem+conjecture" → **theorem+theorem**.
- **[SYNTHESIS+NO-GO] no Wen-PSG without geometry (C1a).** Orientation classes `K̂` + relabeling `Aut(K)` build the record group `K̂⋊Aut(K)` (`|Aut(K)|=168=GL(3,2)` at n=3, the 1+7+7+1 multiplet) — but it's the PSG-INGREDIENT group: nothing forces a nontrivial cocycle (Z2-valued orientation + permutation action; split by construction), and Wen's PSG content `H²(SG,IGG)` needs a SPACE GROUP = emergent geometry (l_step-walled). [VIII corrected H¹→H² for consistency.]
- **[NO-GO] no record-forced chirality bridge (C1b).** The ledger is HANDEDNESS-BLIND (the gap is a pure function of the orientation σ; the Weyl handedness m lives on a separate SO(3) fiber). Parity is JOINT `(σ,m)↦(σ̄,−m)` with a parity-INVARIANT gap (structurally automatic), so a bridge `σ↦m` would be parity-ODD on parity-SYMMETRIC data — undetermined, supplied only by the coupling g. Upgrades paper8 open-item-2 / VIII's "different layer" non-claim from open gap to PROVED impossibility.
- **NET:** the matter sector ends MORE resolved; what remains gated (now sharply named): the Wen-PSG cohomology (emergent geometry), the mode-selecting Hamiltonian (the fifth wall), the interacting GW flow (paper14 O8). NO mass ratio produced or claimed. **9/9 v7 papers terminal (I–IX).** NEXT: Paper X = C3 (graviton spin-2 three-layer pricing, partial/confirmatory); C2 = honest deferral note (BHS-blocked).

### PAPER X (C3 graviton spin-2) — TERMINAL (2026-06-15): `relativistic-isp-v7-paper10-spin2-carried-helicity2-modeblind.md` + receipt `p10_spin2_pricing.py`.
Three-layer pricing of the graviton's spin (paper57's "spin-2-blind" residue). R1 (2 accept + 1 minor) → R2 confirming (2 accept + 1 minor, the residual stale receipt-comment fixed). **(Q-tensor) [OWNED]** the record stress carries the spin-2 tensor structure (binary: dipole=0, traceless quadrupole `Q_xx=1+3cos2t` period-π, pure l≥2) — the selection rules are the conserved-stress index structure + conservation hierarchy (reproduces paper55-A). **(Q-coupling) [LEVER]** the record stress is conserved+symmetric (`∂_μT^μν=0` on the massless shell) = the Weinberg soft-graviton source (theorem EXTERNAL; emergent stress evades Weinberg-Witten as an internal current). **(Q-polarization) [BLIND]** the 2 physical helicities are record-blind: the TT projection (5→2 dof) needs a propagation DIRECTION (the unbuilt emergent metric continuum — NOT the l_step scale; a direction is scale-free) + a canonical mode FRAME (the fifth wall) — largely ONE missing structure seen through two walls. Honest grade: mostly confirmatory; new = the Weinberg lever + the wall re-attribution. NO graviton constructed, NO EP derived, NO new no-go.

### C2 DEFERRAL NOTE — CLEAN (2026-06-15): `note-C2-covariance-premise-deferral.md` + receipt `c2_bhs_collar_nogo.py`. Reviewed alongside Paper X; ACCEPT across the board (no fixes). Discharging Paper VII's covariance premise is PREMATURE: the premise splits into 3 legs — (P-Lor) the causal order descends LI (REDUCES, verified s² + past/future sets boost-invariant); (P-point) the finite-valency collar is FRAME-DEPENDENT (BHS no-go, k-nearest overlap 2/5 under boost) = BLOCKED on discreteness alone; (P-scalar) the emergence-of-Lorentz core. Honest premise-sharpening note, NOT a paper; Paper VII stays correctly conditional.

## METRIC-CONTINUUM INVESTIGATION + PAPER XI (2026-06-15) — user: "can it be built? / is there a paper where this is defined? / write paper 11"
User-driven from the Paper X "emergent metric continuum" residue. Investigation `wbewsu86z` (3 routes + adjudicator; receipts `r1_order_to_conformal_direction.py` 9/9, `r2_number_volume_lstep.py` 6/6, `r3_manifoldlikeness_myrheim_meyer.py` 18/18). **Can the metric continuum be built? CONDITIONAL — buildable UP TO l_step GIVEN manifoldlikeness; manifoldlikeness is the causal-set FIELD's open problem, not SHARD's wall.** Found the foundation = v6 paper1 (Indivisible Causal-Set Gravity: division-event→causal set→order+number=geometry→HKT→GR up to G,Λ; the "non-discriminating finite-sprinkling drift"); manifoldlikeness NAMED NOWHERE in v7 (zero hits).
### PAPER XI — `relativistic-isp-v7-paper11-order-owns-direction-manifoldlikeness-gate.md` + receipts r1/r2/r3. R1 (3× minor, all math verified; 2 real receipt math-errors caught) → R2 confirming (running). Three tiers:
- **[THEOREM/DEMONSTRATED] order → conformal/direction (r1):** the LI causal order delivers the conformal class — null cones, dimension (Myrheim-Meyer canonical `f(d)=Γ(d+1)Γ(d/2)/(2Γ(3d/2))`: 1+1→2.01, 2+1→3.01, f(2)=1/2/f(3)=8/35/f(4)=1/10 exact), scale-free DIRECTIONS (order bit-identical under boost+dilation; a scored direction dilation-invariant, drift 0.0, length scales by λ). Reclassifies Paper X's missing direction from "premature" → order-determined, scale-free, gated by manifoldlikeness NOT l_step. (Malament-HKMM, conditional on manifoldlikeness.)
- **[NO-GO] number → volume up to l_step (r2):** the seal count is an unbiased Poisson volume counter `E[N]=V/l_step^d`; absolute volume weight +d WALLED, ratios weight-0 owned (1.5%). A RECURRENCE of the same l_step no-go (behind d/G/c_m/metric) on the volume axis; G is the input that closes it (= v6 paper1's "GR up to G,Λ" from the continuum side).
- **[WALLED, field-shared] manifoldlikeness (r3):** Malament/MM hold ONLY for manifold-like sets; Kleitman-Rothschild orders dominate `2^{n²/4}` (vs OEIS A001035); a single MM estimate is FOOLED by KR (d_MM=2.38, unmasked by height: longest chain 3 vs sprinkle 46). The click-law offers NO proven lever (v6 paper1's non-discriminating drift = first symptom). Hauptvermutung open. Field-shared open problem, NOT SHARD-specific; named for the first time in the corpus.
- **R1 fixes integrated:** the canonical MM formula (was denom 4→2; r2 f(4) was 1/5→1/10); the KR exponent (was (3/16)n²→n²/4); dropped the contested "sixth appearance" ordinal → "recurrence of the same l_step wall"; weight +2→+d; Malament→Malament-HKMM; n̂ reframed (order-only = the bit-identical dilation-invariance, the vector a held-out SCORE); v6 paper1 drift softened to "symptom consistent with". **KEY conceptual close (user Qs): G ≠ manifoldlikeness (orthogonal axes — G is a scale, manifoldlikeness scale-free/structural; G is downstream, only meaningful AFTER manifoldlikeness); manifoldlikeness ≠ the YM gap (different sectors, shared SHAPE = selection/dominance) ≠ the 10^20 hierarchy (the scale axis). Manifoldlikeness is a NEW category — the program's first dynamical-SELECTION/dominance problem, field-shared.**
