# Relativistic ISP v7 Paper XXXI: The Diamond-Shadow Click Law

**Author:** Felix Robles Elvira (ORCID: 0009-0009-2017-4394; independent researcher)

**Status:** preprint, not peer reviewed, version 2026-06-29. Thirty-first paper of the v7 program. This paper attempts to close the click-law search after Paper XXX's diamond-boundary result. It distinguishes the strongest finite closure from the still-unproved universal law. The finite closure is the **diamond-shadow h-transform**: construct the residual diamond-boundary center, then select the coarsest recurrent, reflection-positive, non-reconstructive positive shadow. The universal closure is not proved, because exact finite evidence through `N<=9` cannot by itself imply uniqueness beyond the audited window. The receipt is `v7/code/p31_diamond_shadow_closure_receipt.py`; it uses exact integer/Fraction arithmetic and Decimal reporting at precision `140`, above the requested 80-bit threshold.

---

## Abstract

Paper XXX changed the search. The selected flag system is not the law, and it
does not reconstruct the whole diamond center. The better picture is:

1. a committed record is a sealed finite diamond;
2. the current boundary packet may leave a residual conflict graph;
3. the **diamond center** is the coarsest boundary refinement that closes that
   residual;
4. the physical click law uses only a **positive non-reconstructive shadow** of
   that center;
5. the shadow is selected by recurrence, reflection positivity, dual symmetry,
   coarseness, and a reconstruction budget.

In the exact `N=9` audited window this gives:

```text
base atoms=131514/131526
diamond_center_atoms=131518/131526
center_conflicts=0
selected_shadow_atoms=65521/131526
TV9=0
rec9=0
qrec9=0
```

So the finite click law is closed in this scoped sense:

> **Finite diamond-shadow law.** Given the audited boundary packet, the forward
> click weights are generated by the coarsest recurrent reflection-positive
> positive h-transform shadow of the residual diamond center.

The universal law is not closed:

> finite receipts through `N<=9` cannot prove that no different law agrees on
> the audited window and diverges at `N=10`.

So the honest status is not "we found the final universal law." It is:

> we found the law's likely form, proved it in the current finite audited
> window, and isolated the exact missing universal principle.

---

## 1. Why this paper exists

The previous papers narrowed the search repeatedly:

- Paper XXVIII: hidden structure is admissible only through its
  record-projected effective likelihood.
- Paper XXIX: the key bridge object is the compressed boundary histogram
  `\bar H`, not raw hidden presentation data.
- Paper XXX: the residual diamond boundary generates a nonlookup center, and
  the selected sector/metric law is a positive h-transform shadow of that
  center.

The open question is now:

> is this the click law, or only another finite witness?

This paper answers:

- it is a closed finite law in the audited `N<=9` model;
- it is not a proved universal law;
- the missing theorem is now sharply stated.

---

## 2. Nontechnical summary

A **diamond** is a sealed finite record-event. It has an inside, a boundary,
and consistency constraints. The outside does not get to inspect the whole
inside.

A **center** is the smallest extra internal summary the boundary needs when its
current description fails.

A **shadow** is the part of that center that the forward click law is allowed to
use. The shadow must not reconstruct the whole center, because that would sneak
hidden structure back into the law.

So the law should not say:

> use these flags.

It should say:

> build the diamond center forced by boundary residue, then use the coarsest
> positive shadow that predicts forward growth and stays non-reconstructive.

That is the conceptual closure.

---

## 3. The finite law

Let `R_N` be a committed record order with `N` records.

Let:

$$
\mathcal B_N(R_N)
$$

be the current record-boundary packet. In Paper XXX, at `N=9`, this packet is:

$$
\mathcal B_9
=
\left(
\hbox{known record features},
\texttt{flag3\_36},
\texttt{flag5\_549376},
\texttt{flag4\_2176},
\texttt{flag4\_206},
\texttt{flag5\_24576},
\texttt{flag5\_912}+\texttt{flag5\_664}
\right).
$$

This packet leaves a residual multi-hole boundary conflict graph. The exact
receipt gives:

```text
residual_edges=4
graph_vertices=8
components=4
component_sizes=(2,2,2,2)
```

Define the diamond center:

$$
\mathcal C_N(R_N)
=
\mathcal B_N(R_N)
\vee
\left(
\bar H_N^{(1)}(R_N),
\bar H_N^{(2)}(R_N),
\ldots,
\bar H_N^{(k_{\max})}(R_N)
\right),
$$

where:

- `\mathcal C_N` is the center;
- `\mathcal B_N` is the boundary packet;
- `\vee` means "join the information in both partitions";
- `\bar H_N^{(k)}` is the compressed `k`-hole boundary histogram;
- `k_{\max}` is the finite deletion depth audited at this scale.

At `N=9`, with `k_{\max}=4`:

```text
base atoms=131514/131526
diamond_center_atoms=131518/131526
center_conflicts=0
```

So `\mathcal C_9` is the smallest tested diamond-center refinement that closes
the residual without becoming lookup.

The positive shadow is a quotient:

$$
\mathcal P_N
\preceq
\mathcal C_N.
$$

Here:

- `\mathcal P_N` is the physical shadow partition;
- `\preceq` means `\mathcal P_N` is coarser than the center;
- the shadow forgets some center data and therefore does not reconstruct the
  full diamond center.

The finite selected shadow is:

$$
\mathcal P_9(R)
\sim
\left(
\sum_{(F,F^\ast)\in\mathcal G_9}(F+F^\ast)(R),
Q_{M_9}(O(R))
\right),
$$

where:

$$
\mathcal G_9
=
\{(912,25104),(17288,525076),(24576,540672)\},
$$

$$
M_9=\operatorname{diag}(5,5,3),
$$

and:

$$
Q_{M_9}(O)
=
5O_1^2+5O_2^2+3O_3^2.
$$

Here:

- `F` is a five-record local count;
- `F^\ast` is its order-dual partner;
- `F+F^\ast` is the dual-even channel;
- `O_j=F_j-F_j^\ast` is the `j`th dual-odd channel;
- `M_9` is the selected odd quadratic metric.

The click transition is then the h-transform:

$$
\Pr(P\mid C)
=
\frac{
D(P,C)\,h_{\mathcal P_{N+1}}(P)
}{
\sum_{P'}D(P',C)\,h_{\mathcal P_{N+1}}(P')
}.
$$

Here:

- `C` is the current committed record;
- `P` is a possible parent/next record;
- `D(P,C)` is the deletion multiplicity: how many deletions of `P` give `C`;
- `h_{\mathcal P_{N+1}}(P)` is the positive effective weight of the shadow atom
  containing `P`;
- the denominator normalizes probabilities.

This is the finite diamond-shadow click law.

---

## 4. What the exact receipt proves

The receipt is:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 v7/code/p31_diamond_shadow_closure_receipt.py
```

It proves seven exact checks.

### 4.1 Center closure

```text
PASS: diamond center closes the audited residual without lookup
131514->131518/131526, conflicts=0
```

The center is not a slogan. It is an exact finite partition.

### 4.2 Proper shadow

```text
PASS: selected shadow is a proper nonreconstructive quotient of the center scale
shadow=65521, center=131518, records=131526
```

The shadow is much coarser than the center. This matters: the law is not
reconstructing hidden structure.

### 4.3 The flag library does not reconstruct the center

```text
PASS: selected shadow does not reconstruct the whole diamond residual graph
covered=2/4
```

This is a positive result, not a failure. It means the flag/sector law is a
physical shadow, not a full hidden coordinate system.

### 4.4 Boundary activity is only a gate

```text
PASS: boundary activity is a gate, not a selector
orbits=11/24, triples=4<64<165
```

Boundary residue tells us what is admissible. It does not uniquely pick the
physical quotient. Recurrence and coarseness are still needed.

### 4.5 Exact finite prediction

```text
PASS: selected positive shadow gives exact audited forward law
TV9=0 rec9=0 qrec9=0
```

This is the finite closure.

### 4.6 Metric selection

```text
PASS: selected odd metric is the non-Euclidean finite metric from Campaign U
metric=(5,5,3)
```

The selected metric is not the Euclidean metric. It is chosen by endpoint
h-collapse plus recurrence plus coarseness.

### 4.7 No finite-window uniqueness

```text
PASS: finite N<=9 receipts cannot by themselves prove universal uniqueness
two formal laws agree on the full audited window and diverge at N=10
```

This is the guardrail. Even exact finite closure does not prove the universal
law. A universal principle is required.

---

## 5. The actual candidate law

The best current law is:

> **Diamond-shadow click law.** A click transition is the positive h-transform
> of the coarsest admissible, reflection-positive, non-reconstructive shadow of
> the residual diamond-boundary center.

More explicitly:

1. Start with the committed record boundary packet `\mathcal B_N`.
2. Compute the residual diamond center `\mathcal C_N`.
3. Search only shadows `\mathcal P_N\preceq\mathcal C_N`.
4. Reject lookup/reconstructive shadows.
5. Require dual symmetry and reflection positivity.
6. Require projective h-recurrence:

   $$
   \sum_P h_{\mathcal P_{N+1}}(P)D(P,C)
   \approx
   (N+1)^2h_{\mathcal P_N}(C).
   $$

7. Select the coarsest shadow satisfying the admissibility constraints.
8. Use the h-transform transition:

   $$
   \Pr(P\mid C)
   \propto
   D(P,C)h_{\mathcal P_{N+1}}(P).
   $$

In the exact `N=9` audited sector, the approximation signs become equalities
for the selected shadow.

---

## 6. Hostile review

### Review 1: "This still uses hidden presentation counts."

The exact finite h-weight is still calibrated against presentation counts. That
is why the universal law is not closed. The finite law shows which kind of
object the weight must be: a positive h-transform shadow of the diamond center.
The universal theorem must construct `h_{\mathcal P}` intrinsically.

### Review 2: "The center uses `\bar H`, which was itself a bridge object."

Correct. `\bar H` is a teacher object. The final law needs an intrinsic
definition of the same center, or an admissibility theorem licensing the
projection. Paper XXXI does not hide this; it names it as the remaining bridge.

### Review 3: "Exact `N=9` is too finite."

Correct. The receipt proves finite closure and finite non-uniqueness at the
same time. A law can agree through `N<=9` and differ at `N=10`. So exact finite
evidence alone cannot prove the universal law.

### Review 4: "Why not use the full diamond center as the law?"

Because the center is almost lookup at `N=9`:

```text
131518/131526 atoms
```

The physical law needs the center's predictive shadow, not its full
reconstruction.

### Review 5: "Why trust the flag shadow?"

We should not trust it because it is a flag shadow. We trust it only because it
is:

- boundary-active;
- recurrent;
- reflection-positive;
- coarser than the center;
- nonlookup;
- exact on held-out `N=9`.

The law is the selection principle, not the flag names.

### Review 6: "Did this close the universal law?"

No. It closed the finite audited law and isolated the missing universal
principle. The universal law is still open until `\mathcal C_N` and
`h_{\mathcal P_N}` are defined without hidden presentation teachers for all
relevant `N`.

---

## 7. What is actually missing

The missing theorem is now:

> For record diamonds generated by the physical theory, the residual
> diamond-boundary center has a projectively stable family of admissible
> positive shadows, and the coarsest recurrent reflection-positive
> non-reconstructive shadow is unique enough to define the click h-transform.

This theorem must prove:

1. **Intrinsic center:** `\mathcal C_N` can be defined from record data, not
   hidden presentation labels.
2. **Projective stability:** deletion/insertion moves the centers with
   controlled drift.
3. **Shadow existence:** a positive recurrent shadow exists below lookup.
4. **Shadow selection:** recurrence, reflection positivity, coarseness, and
   density/regularity select the shadow.
5. **Asymptotic control:** the rule does not collapse into lookup as `N` grows.
6. **Physical breadth:** the rule survives beyond the 2D permutation-order
   audit sector.

---

## 8. Bottom line

The search did not end with a universal closed formula.

It did end with a credible finite law:

> the click law is the positive h-transform shadow of the residual
> diamond-boundary center.

The reason this is a real advance is that it explains why the flags work
without making the flags fundamental:

- diamonds are the record objects;
- centers are the boundary-forced missing summaries;
- shadows are the admissible predictive quotients;
- h-transforms are the forward click mechanism.

So the actual law is no longer expected to be a scalar penalty, a fixed flag
list, or a raw stochastic clock. It is expected to be:

> a projectively stable, reflection-positive, non-reconstructive h-transform
> shadow of diamond centers.

That is the current frontier.
