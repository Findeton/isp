# Paper 14 premetric construction and source freeze

Date: 2026-08-20

Status: **STAGE B CONSTRUCTION / SOURCE FROZEN / RESULT UNKNOWN**

## 1. Binding authority

The result-neutral analytical pin is committed at
`9687d59c2faa29efd53993643deb92b3f5c5a025`:

```text
path:       v16/note-paper14-stable-happenings-premetric-pin.md
ordinary:   0dc92112e5db39bb9e1a8c51a018119e783f347505d3e5bf03debf47fe31ef44
normalized: 5054cfd38e89b66d1dc2b82dd510e7bb1616253c84cfd7ec18760db69e19878d
```

The construction follows the pin's Stage B whitelist. It does not read,
import, execute, or cite Paper 13 source. The seven hash-bound shared-worktree
exceptions remain untouched and out of scope.

## 2. Frozen candidate bytes

```text
v16/code/p14_premetric_exact.py
SHA-256 1a1d9a7acc3ef4ba62a9e9b0f6101263dde3b72dd9d407918ea0be653d5d628e
1,411 lines; 56,229 bytes; 1,267 nonblank lines

v16/paper-14-stable-happenings-and-premetric-order.md
SHA-256 ffc3dca2863bf9f36c9fe62e8dff80628c59c8837b3d96006e449329dee05ec1
1,748 lines; 62,489 bytes
```

The source is now frozen. Stage C may create the fresh-case, output, receipt,
and verification-note paths named by the pin. It may not edit either frozen
candidate path.

## 3. Law provenance and ontology boundary

The instrument prints

```text
DECLARED-NEW-LAW-POSTULATE
```

No terminal parent supplies the Paper 14 `Gamma`. The finite reciprocal law
and uniform fresh-port family are new declared mathematical model laws. A
positive result can therefore establish physical referents inside that
declared model, not selection of Nature's law.

The paper now separates:

- complete possible histories governed by `Gamma`;
- the external actualization postulate `rho`;
- semantic record transport `T`;
- onset germs and possible stable-happening types;
- actual happenings conditional on `rho`;
- complete division frontiers;
- dependency bundles;
- unit and descended unequal valuations; and
- labels, addresses, hashes, and serializations as nonphysical provenance.

No clock, coordinate, graph, dimension, metric, curvature, stress tensor, or
gravity variable enters the candidate law.

## 4. Exact construction surface

The source constructs rather than reads:

1. the rational orthogonal writer matrix and its two record-sector branch
   maps;
2. six record-preserving exact future generators and one excluded record
   eraser;
3. the minimal four-fact, five-state, seven-word, five-trace-history frame;
4. the mutual-dependence quotient with one two-component onset bundle;
5. the complete 16-history reciprocal probability law;
6. direct-versus-cut equality on all parity/output inputs;
7. stable/complete, stable/incomplete, nonstable/complete, and
   nonstable/incomplete frontier controls;
8. the weak-only correlated antichain and strong screened-fork controls;
9. every nonisomorphic rooted shape through four cells at branching bounds
   one, two, and three, for both root seeds;
10. exact normalization and counts on 38 finite uniform-family rows; and
11. all 26 preregistered scientific changed objects.

The paper, not the finite instrument, carries the all-word induction,
complete-frontier kernel theorem, dependency-quotient proof, no-hidden-clock
proof, fixed-memory obstruction, strong-diamond equivalence, countable atomic
measure theorem, and all-size local-finiteness proof.

## 5. Development selftest boundary

The source-frozen selftest reports:

```text
scientific checks:       16 / 16
registered attacks:      26 / 26 killed
uniform finite rows:     38
shape-census rows:       12
fresh_cases_evaluated:   false
outcome status:          FRESH-CONTROLS-NOT-EVALUATED
normalized payload:      6c5a3c10615cb7a14ecb679c70856222cf24a86600a17186c43eb03a69e0efed
canonical stdout SHA:    36bda0de588ce4ffdd37b549b581a1b436466f4351e6ddd5714c857a789110d2
```

Repository-root, alien-CWD, and true one-file/no-`.git` runs produced the same
canonical stdout SHA. Measured wall times were 10.54, 13.15, and 13.02 seconds,
respectively, below the 60-second cap.

The named `H22-SCREENED-COMMON-CAUSE` mode completed and emitted SHA-256
`e7461fc373973db0e78c947a5ace9d91d65e26b28a98819ed053273089673c33`.
The deliberate anchor-failure mode exited 1 with
`SCIENTIFIC-FAILURE: DELIBERATE-ANCHOR-FAILURE`. An unknown mode exited 2.

These are source-construction checks, not the official scientific result.
The official coordinate remains unknown until fresh cases are generated once,
the frozen source evaluates them, publication artifacts are created, and an
independent verifier reconstructs the receipt.

## 6. Static audit

The frozen source passes Python AST parsing. Its substantive path contains:

```text
float literals:                    0
numpy/scipy/network imports:       0
subprocess/socket imports:         0
randomness:                        0
Paper 13 imports:                  0
Git calls or literals:             0
typed expected finite answers:     0 acceptance tables
```

Fractions are serialized as exact numerator/denominator pairs. Unequal
weights are represented as symbolic formal-log reciprocals; no logarithm is
evaluated numerically.

## 7. Fresh-case interface frozen before contents

The source accepts one future file with schema `P14-FRESH-1`. It requires:

- the frozen source SHA;
- one lowercase 256-bit nonce;
- at least six unique cases;
- at least five distinct scientific case kinds; and
- exact schemas and rational probabilities.

The six generic case kinds are frontier-profile insufficiency, correlated
antichain, screened common-cause fork, fixed-memory capacity, presentation
permutation, and mutual-dependence bundle. The source knows their semantic
predicates but no future parameters, nonce, or case list. Those contents do
not exist at source freeze.

## 8. Publication and receipt boundary

Official `--run` refuses unless all three Stage C paths are exact and absent:

```text
v16/p14_premetric_fresh_cases.json
v16/p14_premetric_output.json
v16/p14_premetric_receipt.json
```

It binds the pin, frozen source, paper, construction note, and fresh file;
evaluates fresh and registered controls; derives product outcomes; seals every
top-level scientific key and every receipt-core field; and writes output and
receipt transactionally. The attempted pre-freeze official run refused at the
absent fresh file and left both publication paths absent.

## 9. Current stopping boundary

No scientific outcome is awarded at source freeze. The candidate coordinates
remain exactly those preregistered in the pin, with ceiling

```text
P14-PREMETRIC-ORDER-AND-INTERVAL-MEASURE
```

only inside a declared law if every prerequisite survives Stage C and the
later hostile round. No chronology, spacetime, metric, curvature, gravity,
actualization, or law-selection coordinate is open.

The next and only authorized event is Stage C: generate one fresh file after
this source hash is committed, execute the frozen source once in official
mode, independently verify the artifacts, and commit the result. No source
repair or Paper 15 construction is authorized.
