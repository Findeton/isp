# PPR panel adjudication — the exact fixtures survive; the joint event algebra does not

**Date:** 2026-08-18

**Frozen candidate target:** `0f7a4b9bfc55be224143b2bc56bd4a5f5f51e5ee`

**Panel grades:** OPERATOR `REJECT`; HISTORIES `ACCEPT-WITH-FIXES`;
GRAVITY `REJECT`.

## 1. Frozen independent evidence

The three reports froze independently before this adjudication:

| seat | report | ordinary SHA-256 | normalized self-SHA-256 |
|---|---|---|---|
| operator / representation | `v16/review-ppr-operator.md` | `ab00e911ffb1146f7fb7b3859ded47c0d0dd2943e2d4957e30c840dd76f824a8` | `871d12e0fba5ebf72159f75f7fc55d4ccf93ff7c873b8777c09e705ea74b571b` |
| histories / quotient / records | `v16/review-ppr-histories.md` | `244862b719bb5ab4c8649e75075fb53983ee4928a3166ffdaaaf9f754cbd47a4` | `30c8877b6509f18a26123cd54be60f2265f5ec5b69f2b3f70569ef863fad772c` |
| relational geometry / gravity | `v16/review-ppr-gravity.md` | `f36837c8326b3db7205e4a1c10897007266a82857193f7c02033f1ec89e17fb3` | `ae323c91cb8641893d9a7eed1d07f13a4ecb34fbeb5ba60d17d5fce566530a9b` |

All three authenticate the frozen artifacts and chronology. They independently
reproduce the load-bearing finite arithmetic: Kraus/channel rotation,
history split, stable-null ranks and two strict rounds, contextual pullbacks,
record preserving/erasing controls, interference and completeness, Bell/fixed-
Bob controls, loop screens, and the two rival-law held-out screens. No
registered rational number requires correction.

## 2. Joint ruling

The frozen positive primary is rejected. The earliest applicable outcome in
the pin's ordered classifier is:

```text
PPR-BLOCKED-AT-EVENT-ALGEBRA
```

The reason is prior to law selection. PPR never constructs one typed object in
which graph-individuated event atoms map to the displayed history/class
operators, those operators generate the stable-null continuation family, and
the record instrument is a coarse graining of the same event algebra. The
primary conjoins correct results from neighboring fixtures.

The histories seat judged that outcome 9 could survive if every fixture were
read only at its own case-specific grammar. That is a valid scope reading of
the individual theorems, but it does not defeat registered outcome 2: the pin
asks whether the relational history/event referent is independently typed.
It is not. The operator seat's missing representation map and the histories
seat's object-separation table establish the same structural fact.

The gravity seat supplies a second, downstream kill. Even if an event algebra
were supplied, the delivered relational wedge is exactly replaceable by two
minimal port-labelled circuits carrying the contracted path products. The
declared carrier dimensions never type the transported state. That would
trigger outcome 7, `PPR-BLOCKED-AT-RELATIONAL-REWRITE`; outcome 2 wins because
the frozen ordering requires the first applicable block.

## 3. Exact content that survives

The following may be cited as finite mathematical results, with the stated
scope:

1. The descending null construction gives the greatest null subfamily
   invariant under the supplied finite two-arrow continuation graph. Its raw
   cut dimensions are `3 -> 2` in that presentation; the invariant content is
   quotient rank two. A pure null dilation moves the raw dimensions to
   `4 -> 3` without moving the quotient.
2. A newly licensed continuation may reactivate a presently null direction.
   “Permanent” therefore always means continuation-stable relative to a
   closed declared grammar, never absolute darkness.
3. Once a common future inner-product/effect context and its continuation maps
   are supplied, `U_alpha^dagger U_beta` is the unique Gram pullback on the
   reached subspaces. The future context is law data; the identity does not
   select it.
4. The preserving and erasing support fixtures correctly implement the v12
   co-live/co-merge record criterion. The clean positive is recoverability
   under the one-generator preserving grammar. Actualization is not derived.
5. The registered three comparison paths form one commuting finite diagram.
   This is not a general overlap/cocycle theorem.
6. The exact interference, completeness, local-Bell, and fixed-Bob
   unconditional no-signalling calculations survive at fixed-factor scope.
7. Two complete two-port laws move the registered screen:
   `(49/625,576/625)` versus `(0,1)`. This proves nonselection on that port
   interface, not nonselection among complete relational-history laws.
8. The separately declared loop phase moves `p_0` from `1` to `49/625`. It is
   a phase-sensitive finite transport witness; the phase is not generated by
   the rewrite and is not gravitational curvature.

## 4. Claims withdrawn

The following readings are not licensed by the frozen candidate:

- one complete history law jointly generating event algebra, null quotient,
  pullbacks, records, graph rewrite, and either rival law;
- a classification of Kraus mixing as relational-history gauge, because no
  event-algebra representation is supplied;
- invariance under arbitrary history refinement rather than one coarse
  class-operator split identity;
- permanence under an untyped union of every continuation appearing in the
  paper;
- graph incidence or the declared `2 -> 3` metadata as an irreducible changing
  process carrier;
- a refinement-invariant matter-to-geometry response;
- rewrite-generated curvature, gravity, continuum/Lorentz structure, QFT,
  particles/species, Hamiltonian reconstruction, constants, or empirical
  deviations; and
- a record-generated comparison/law fixed point.

## 5. Eliminability ruling

The gravity countermodel is adopted at exactly its measured class-relative
scope. It does not claim that every finite geometry is absolutely eliminable.
It exhibits one explicit nongeometric adversary allowed by the candidate's
own two-history interface: replace each graph path by its complete port map.
That adversary preserves every registered wedge prediction. Therefore this
fixture supplies no evidence that graph incidence is load-bearing.

A future irreducibility experiment must freeze a graph family, held-out
members, one uniform graph-reading law, and a resource-matched graph-blind
adversary class. That is new construction, not a wording repair to PPR.

## 6. Disposition and repair boundary

No bounded forward repair can turn the current neighboring fixtures into the
missing joint event algebra or make the current graph metadata dynamically
load-bearing. Doing so requires new physical fixture data and changes the
scientific question after review. It therefore belongs to a separately pinned
successor.

For the frozen Paper 3 investigation the adjudicated result is negative and
complete:

```text
PPR-BLOCKED-AT-EVENT-ALGEBRA

SURVIVES:
  exact finite operator, quotient, pullback, record, and two-port controls.

MISSING:
  one relational event algebra represented by one complete history law;
  one carrier rewrite that graph-blind port circuits cannot reproduce.
```

Papers 4--7 may use the surviving operator mathematics only conditionally.
They may not cite PPR as having supplied a joint relational-history law.
