# JCV terminal verification

Date: 2026-08-17

Ledger target: v16 #32

Repaired artifact commit:
`126caa4b3f6c53b8043458f7a95f59f5216b2a6d`.

## Terminal disposition

The scoped terminal verdict is:

`LOCAL-COMPATIBILITY-VARIETY-CONFIRMED-BUT-RECORD-GENERATED-FIXED-POINT-UNBUILT`.

The frozen scorer words remain:

- global algebraic classifier: `JCV-STRATIFIED`;
- registered active-locus classifier:
  `JCV-PAIRING-SELECTED-WEIGHTS-FREE`.

Their terminal interpretation is narrower than their names when read without
the pin.  Inside one declared real, isometric, nondegenerately calibrated
two-channel chart doctrine, full support plus two local cut equalities forces
flat holonomy modulo chart-sign gauge.  It does not derive a physical
cross-carrier comparison.  The active boundary-instrument law remains a real
two-dimensional family.  Six nontrivial-holonomy sectors survive only in
one-step law-null columns; whether future continuations distinguish them is
open.

## Frozen repaired bytes

- scorer SHA-256:
  `84649be8a56b8248304ba54065b4f4cfb9aae9cc33b3d59f0c76eafa38843b66`;
- paper SHA-256:
  `e06e7ce1ec59397e05ccafa1e51d3bf78e888d0c0b72c214144deb9ba92e9e39`;
- transcript SHA-256:
  `1ea26ef8b14f0cc915edb9098ff835a5733e1bd31080b9a293dc36c4a8901e02`;
- receipt SHA-256:
  `b472a4281e40e6aa8b0dfe7a6fcde871cc808acd79bdcbe123d907e2bf12894a`.

## Verification performed

1. A worktree `--replay` from committed bytes returned `REPLAY
   byte-identical`.  It changed no result artifact.
2. `--selftest` killed `ANCHOR_CORRUPT` at `P-ANCHORS`.  All 16 explicit named
   mutants returned their required exit code, moved their primitive datum, and
   died at their registered gate.  The aggregate artifact digest was unchanged
   across the complete mutant battery.
3. A plain second-official invocation refused at the existing-artifact guard
   with exit one.  Unknown flags and incomplete mutant syntax refused with exit
   two.  Every refusal left the artifacts unchanged.
4. An independent JSON/hash audit recomputed all 19 receipt payload seals,
   seven source anchors, paper/output digests, 20 gates, 16 mutant proofs, and
   18 paper-result bindings.  It independently recomputed the registered
   unconditioned channel, the post-review witness normalization,
   orthogonality, determinant, and coherence eigenvalue `-119/169`, the
   direct-`03` defect, the law-null quotient statement, all scope walls, and
   the absence of the removed hard-coded chronology gate.
5. Independent AST inspection found zero floating-point literals.  The exact
   physical algebra remains rational plus the frozen `Q(sqrt(2))` comparison
   backend.
6. A `git archive` of the exact runtime/read/result set at repaired commit
   `126caa4` was extracted to
   `/private/tmp/jcv-terminal-offtree.2niQf9`.  It contained no `.git`
   directory.  Invoking the archived scorer by absolute path from alien
   working directory `/private/tmp` returned byte-identical replay; its
   selftest also passed.  All three archived artifact hashes equal the
   committed hashes above.
7. The tracked JCV source, fixture, solver, paper, transcript, and receipt are
   unchanged from the repaired commit.  `git diff --check` is clean.  The
   unrelated untracked v15 SCOUT-T files were not read, edited, staged,
   archived, cited, or committed.

## What is terminal at this scope

- the `1,024` raw assignments, `64`-element gauge orbits, and `16` holonomy
  sectors for the declared chart-sign family;
- the seven shared-law nonempty sectors, one active dimension-two component,
  six sector-level dark circles, nine independent-law control sectors, and two
  homogeneity-excluded mixed handoffs;
- the exact active real-locus form `S^1 x S^1` and nonconstant calibrated
  outcome probability;
- a valid all-input/ancilla fixed-boundary two-outcome CPTP instrument;
- the fact that the two frozen active witnesses change the outcome-resolved
  instrument while sharing one unconditioned channel;
- the post-review exact corollary that another active direction changes the
  unconditioned channel;
- active flatness being independent of numeric shared-law homogeneity, while
  the two dark mixed handoffs are homogeneity-dependent;
- the direct-`03` countermodel proving that the local solution does not
  determine its next overlap; and
- the theorem that `WQ=W` makes comparison trivial on `H/ker W`, so every
  surviving dark mismatch is null for the delivered one-step law.

## What remains unconstructed or unselected

- a physical, rewrite-generated cross-carrier boundary identification;
- the complete null ideal tested against all future continuations and proved a
  congruence under gluing, spectators, and refinement;
- a record-generated self-map whose stable records reproduce that quotient and
  whose induced coarse law reproduces itself;
- a selected complex history functional or boundary-instrument weight law;
- arbitrary overlap/atlas coherence and all-finite-arity composition;
- stable record permanence and objective actualization;
- changing-factorization subsystem algebras, steering, and no-signalling;
- a relation/geometry rewrite, nonfactorizing backreaction, or continuum
  constraint algebra;
- a Hamiltonian/generator selection, vacuum, particles/species, scattering,
  constants, scale, Lorentz behavior, or any QFT/GR deviation.

The refined successor target is precise but not authorized: construct a
strongly positive complete-history law whose continuation-stable null quotient
generates the physical boundary comparison, and whose own durable records
reproduce that quotient across at least three overlapping refinements.
Strong positivity alone supplies representation, not weight selection or
actualization.

JCV and Paper 2 now halt at this scoped terminal verdict.  No successor unit is
authorized without a new explicit user instruction and a fresh pin.
