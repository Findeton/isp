# APR Paper 12 scorer freeze — before first scientific evaluation

**Date:** 2026-08-18

**Status:** FROZEN PRE-TRUTH IMPLEMENTATION. No official APR fixture score,
transcript, receipt, candidate paper, or outcome word existed when this note
was written.

## 1. Frozen scorer

| path | SHA-256 |
|---|---|
| `v16/code/apr_score.py` | `a145bf619781705b439defaf62177896a069c34a81b90b22e29522cf77528444` |

The scorer is 4,095 lines and imports only the frozen generic APR core after
binding its location to the scorer's own directory. It authenticates the pin,
both pin addenda, the parent scorer protocol, both forward corrections, core,
fixture module, and canonical fixture payload before scientific work.

## 2. Result-neutral architecture

The scorer reconstructs exact Boolean regions and valuation restrictions,
semantic branch bits, arbitrary-support decision trees, finite cospan
pushouts, three quotient types, record readers, three replacement closures,
relative support spaces, comparison and predictive-boundary controls,
overlap marginals, static contact/causal controls, E-37 blind projections,
and separate law/preparation/control roots.

Missing constructions are emitted explicitly. In particular, the scorer does
not manufacture arbitrary-frontier forest composition, identities above B0,
tensor, nontrivial vertical naturality, mixed intrinsic trees, regional
overlap gluing, generated complete probes, physical post-quotient
atomlessness, operational comparison, a law-generated predictive boundary,
causal schedules, regional E-37 dynamics, or an unseen-family generator.

The #160 and #161 corrections are binding: `p` is preparation data; branch
bits come from `Q_C^0/Q_C^1`; numeric controls use unit support while the
theorem is arbitrary-support; replacement closures and raw/relative fixed
spaces remain distinct; `rw_002` refuses as a sequential word;
`mx_007`/`mx_008` refuse their undeclared constructor; and total identity is
not inferred from the B0 row.

## 3. Generic verification before truth

The following checks passed without invoking `score_dataset`, `--run`, any
mutant's scientific fixture evaluation, or any official artifact path:

- Python AST parse;
- nine-suite `--selftest`, explicitly reporting
  `scientific_fixture_evaluated:false`;
- exact Boolean/valuation, semantic tree/zero-port, undeclared-constructor,
  finite-pushout, corrected four-coordinate stable-null, three-replacement,
  strict-reader-typing, mutant-registry, and transactional-publication tests;
- all 56 registered transformation identifiers are present: M01–M35,
  P1–P8, L1–L6, and G1–G7;
- all eight immutable file hashes and the canonical fixture-payload hash bind;
- the same generic selftest passes from a true off-tree directory containing
  only `apr_score.py` and `apr_core.py`, with no `.git`;
- missing mode, unknown option, missing run destinations, relative run
  destinations, and selftest-with-destinations refuse;
- output-only, receipt-only, both-existing, and synthetic failure after first
  publication preserve/roll back the paired artifacts;
- no APR scorer bytecode, float literal, random scientific path, runtime Git,
  network, current-working-directory, result table, or fixture-side
  measurement call is present.

Registry coverage is not mutant success. The complete transformation battery
is evaluated only by the official run and later reconstructed independently
by hostile reviewers.

## 4. Authorized next event

The only next scientific event is one official scorer execution with these
previously absent destinations:

```text
v16/apr_output.txt
v16/apr_receipt.json
```

The run must publish the pair transactionally, followed by a clean second run
to temporary destinations for byte comparison, all refusal/overwrite checks,
and true off-tree replay. The official artifacts freeze before candidate
prose. Any implementation change requires a forward repair and refreeze; it
may not be made after seeing the result.

No APR primary, physical regional referent, causal structure, geometry,
backreaction, metric, curvature, gravity, GR, QFT, or actualization result is
licensed by this implementation freeze.
