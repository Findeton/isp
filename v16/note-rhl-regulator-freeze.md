# RHL result-neutral regulator/control freeze

**Date:** 2026-08-18

**Pin:** `05d8107`

**Generic core:** `24c65b1`

**Status:** result-neutral regulator and countercontrol family frozen before
the RHL scorer, theorem dispositions, outcome, or candidate paper exists.

## Frozen artifacts

| artifact | SHA-256 |
|---|---|
| `v16/code/rhl_regulator.py` | `d6d520a6a43451a889bfc40e2dd8df2f9afc08f032c202c3d8c206c02b4a3db9` |
| `v16/code/rhl_regulator_output.txt` | `1f020e41989007048cf2d864f966b90184e332622230f26028c8e90225befb68` |
| `v16/code/rhl_regulator_receipt.json` | `cea302918c101c7b2bd5973167776baa587681f4b83b8ebff041e38e0bfee944` |

## Frozen control family

The regulator contains ten exact structural controls.

1. Two nonisomorphic finite presentations with three and four intermediate
   rows descend to the same boundary rotation.
2. A tampered refinement embedding changes that boundary map.
3. The committed route amplitudes `9/25` and `-16/25` give coherent weight
   `49/625`, diagonal classical weight `337/625`, and interference defect
   `-288/625`.
4. A single CNOT flag is erased exactly when its licensed writer fires again.
5. A second flag retains the branch correlation after the first is erased;
   the final support is exactly `[0,5]`.
6. Four exact character twists satisfy 196 of 196 registered gluing
   identities.
7. Those equally compositional character laws move one boundary weight across
   `1,1/2,0,1/2`; structural gluing alone therefore does not select the law.
8. A fixed-factor local dephasing leaves Bob's unconditioned marginal exactly
   `I/2`.
9. Two candidate identifications of a changed Bob boundary move a calibrated
   probability from `3/4` to `1/4`.
10. Removing the upstream cross term kills the registered interference control
    in the explicit self-test.

## Interpretation guard

The three- and four-row presentations are not spacetime lattices and their row
counts are not physical scales. They are finite receipts for the general
directed-colimit/descent theorem still to be written. Likewise the character
integer is not called curvature or geometry. The counterfamily proves only
that composition/refinement axioms permit more than one law unless additional
physical content selects the character.

The fixed-factor locality control proves no dynamic-subsystem theorem. Its
purpose is to show that the algebra can pass the ordinary theorem while the
changed-boundary comparison remains untyped: without a law-selected transport
of Bob's operational algebra, `3/4` and `1/4` are both available readings.

The flag fixtures distinguish an erasable correlation from a redundantly
retained one over the registered continuations. They do not prove absolute
record permanence, because the complete future continuation catalogue is not
known.

## Verification

Two fresh generations in `/private/tmp/rhl-regulator.KNe53I` are byte-identical
to each other and to the frozen output and receipt. Python 3.13 compilation and
the cross-term self-test pass. All substantive arithmetic is exact `Q(i)`.

The next event is the frozen scorer. It must consume these upstream data and
the general analytical theorem certificates. It may not turn a missing object
into a Boolean result, infer an arbitrary-refinement theorem from the two
presentations, or promote the character counterfamily to geometry.
