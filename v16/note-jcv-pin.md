# JCV PIN — the joint comparison/law solution variety

Status: **PIN**.  This document must be committed before the generic solver or
physical fixture exists.

Authorized by the user's explicit 2026-08-17 instruction to continue v16 with
the next investigation.  Immutable base commit:
`7adc7cbf4897b303077bbfba4e11a7268274a10f`.

## 1. The question — one box

> At the smallest calibrated changing-carrier overlap with a neighboring
> cocycle extension, do cut equality, comparison coherence, all-input quantum
> completeness, durable outcome typing, and a nonfactorizing/full-rank law
> select a joint equivalence class of **cross-carrier comparison structure plus
> history-weight law**; leave physical freedom; force a classical/dark law; or
> admit no solution?

The deliverable is the exact algebraic solution variety and its gauge quotient,
not a census of hand-picked pairings or weights.  The unit may decide the
declared finite arena only.

## 2. Referent census and term binding

Every inherited term is bound to one source definition.  Same spelling never
supplies identity by itself.

| term in this unit | binding | use here |
|---|---|---|
| boundary type | `v16/paper-01-joint-relational-history-law.md`, common typed boundary and continuation algebra | a declared two-dimensional calibrated interface; no metric or spacetime meaning |
| durable record | the stable orthogonal output-sector usage of that same paper | the two outcome flags only; no claim that the carrier alternatives are records |
| record event / legitimate division | `v12/paper1-composition-defect.md`, theorem-level record structure | comparison precedent only; this unit does not identify its outcome flag with the older event object |
| cross-carrier comparison | **new postulated referent below** | isometric intertwiner between two calibrated interface algebras |
| history-weight law | **new postulated referent below** | one coefficient matrix reused at both overlap triangles |
| actualization | `v16/paper-01-joint-relational-history-law.md`, explicit stochastic postulate | outside the solution variety; no emergence or derivation claim |
| geometry / backreaction | v16's repaired negative scope | not constructed here; carrier charts are interface encodings, not a metric or Einstein response |

Sample space declaration: the two rows of the weight matrix are mutually
exclusive durable **outcome alternatives**.  Its two columns are simultaneous
coherent **history contributions within each outcome**, not probabilities and
not mutually exclusive classical events.  Normalization is over the complete
outcome rows after coherent column addition.

## 3. Four-gate declarations for the new objects

### 3.1 Cross-carrier comparison doctrine

- **Referent:** for a boundary chart `B_i` with a nondegenerate calibrated
  binary observable algebra, a comparison `P_ij` is a real isometry satisfying
  `P_ij O_i = O_j P_ij`.  It compares encodings of the same declared unread
  boundary fact.  It is not a beable.
- **Necessity:** without a common codomain/observable identification, coherent
  sums across distinct carrier histories and changing-factorization locality
  are untyped.
- **No-smuggling:** the intertwining, isometry, gauge action, and overlap graph
  are frozen before the amplitudes or fixture result.  Neither desired
  interference nor a verdict word occurs in the definition.
- **Discriminator:** triangle holonomies, their neighboring extension, and
  calibrated output probabilities distinguish gauge from physical pairing
  freedom.

### 3.2 History-weight law

- **Referent:** one real matrix
  `W=[[x,y],[u,v]]`; rows label the two durable outcomes and columns label the
  two coherent carrier-history channels.  It is the real exact slice of a
  possible complex law, not the general law.
- **Necessity:** comparison maps say which terms may be added but do not by
  definition assign their amplitudes.
- **No-smuggling:** `x,y,u,v` are variables.  No fitted value, probability, or
  inherited Z/X verdict is supplied.
- **Discriminator:** the gauge-invariant rank determinant, internal
  interference term, and calibrated probabilities for two held-out input
  eigenstates distinguish weight laws on a fixed comparison class.

### 3.3 Joint solution variety

- **Referent:** the zero set over the exact real algebraic closure of the
  polynomial system in §5, stratified by the declared nonzero predicates and
  quotiented by the declared sign gauge.
- **Necessity:** both comparison and weights are continuous/algebraic families;
  a finite row list cannot prove their selection.
- **No-smuggling:** a generic exact polynomial solver is frozen on public
  calibrations before the physical fixture path exists.  The scorer is frozen
  with the fixture before the sole official solve.
- **Discriminator:** emptiness, quotient dimension, gauge-invariant holonomy,
  determinant locus, and observable-function constancy give the registered
  outcome taxonomy.

### 3.4 Actualization

- **Referent:** a separate stochastic ontology postulate inherited at its
  explicit status.
- **Necessity:** a probability law does not entail that one outcome occurs.
- **No-smuggling:** actualization supplies no polynomial equation and cannot
  select a comparison or weight.
- **Discriminator:** none in this unit.  Any sentence saying actualization
  emerges is a wall failure.

## 4. Declared arena

- **Boundary:** four interface charts `B0,B1,B2,B3`, each carrying a real
  two-dimensional process space and one calibrated nondegenerate binary
  observable.  Their encodings alternate `Z,X,Z,X`.
- **Family:** real orthogonal intertwiners on five comparison edges and real
  two-outcome/two-history coefficient laws.  A real-slice nonselection result
  refutes uniqueness in any containing complex family; a real-slice selection
  result does not establish complex uniqueness.
- **Law:** the same `W` is used at both overlapping triangles.  An
  independently weighted-triangle control measures the price of that
  homogeneity declaration.
- **State:** quantum completeness is operator/all-input.  The two calibrated
  eigenstates used for the response discriminator are diagnostics, not the
  normalization sample space.
- **Arena:** comparison edges `01,12,02,23,13`, making triangles `012` and
  `123` share edge `12`.  This is the smallest neighboring-cocycle extension
  in the unit; no arbitrary atlas or all-n theorem is claimed.
- **Provenance:** all corpus anchors are read only at the immutable base commit
  and byte/path-value checked.  External literature is attribution only.

The four charts are an operational interface model for alternative carrier
rewrites.  They are not a constructed graph rewrite, geometry, causal set,
metric, gravitational field, or backreaction experiment.

## 5. Frozen algebraic system

Each comparison edge has two calibrated eigenchannel signs:

`(a,b)` on `01`, `(c,d)` on `12`, `(e,f)` on `02`, `(g,h)` on `23`, and
`(j,k)` on `13`.

Every sign variable `s` obeys `s^2-1=0`.  In matrix coordinates the comparison
maps are the exactly normalized Hadamard-type intertwiners between `Z` and `X`
charts or the corresponding signed diagonal map between equal charts.  The
exact matrix backend uses the field `Q(sqrt(2))`; the polynomial sign and
weight system itself has rational coefficients.

The gauge-invariant channel holonomies are

`q012+ = a*c*e`, `q012- = b*d*f`,

`q123+ = c*g*j`, `q123- = d*h*k`.

The unweighted comparison defects are

`r012+ = a*c-e`, `r012- = b*d-f`,

`r123+ = c*g-j`, `r123- = d*h-k`.

With `W=[[x,y],[u,v]]`, amplitude-level cut equality at both triangles is

`x*r_t+ = u*r_t+ = y*r_t- = v*r_t- = 0`

for `t` in `{012,123}`.  This deliberately does not impose structural cocycle
equality in a direction the law never probes.

After comparison into the calibrated final chart, the two branch operators
are

`K0=x*I+y*Z`, `K1=u*I+v*Z`.

All-input trace preservation is exactly the pair

`x^2+y^2+u^2+v^2-1 = 0`,

`x*y+u*v = 0`.

The first equation is the identity coefficient of
`K0^dagger*K0+K1^dagger*K1=I`; the second is its `Z` coefficient.  Complete
positivity is branchwise by the Kraus form and is not used as a selector.

Predeclared diagnostics, never constraints, are:

- nonfactorization/full-rank discriminator `Delta=x*v-y*u`;
- outcome-zero probabilities `p_plus=(x+y)^2` and
  `p_minus=(x-y)^2` on the two calibrated input eigenstates;
- interference response `p_plus-p_minus=4*x*y`;
- the four holonomies `q012+`, `q012-`, `q123+`, `q123-`.

The gauge group is independent sign choice on each eigenchannel at each chart.
The four `q` values generate the cycle-holonomy quotient at this graph.  Gauge
freedom is never counted as physical law freedom.

## 6. Operational order

1. **Anchor and referent census.**  Reproduce every committed anchor and reject
   any same-name/cross-type identification not proved here.
2. **Generic solver freeze.**  Implement exact rational multivariate
   polynomials, Buchberger reduction, leading-ideal dimension, exact sign-sector
   substitution, and polynomial-remainder tests.  Freeze it on public EMPTY,
   POINT, POSITIVE-DIMENSIONAL, REDUCIBLE, and GAUGE-SECTOR calibrations.  No
   physical fixture, scorer, receipt, output, or paper path may exist.
3. **Fixture/scorer freeze.**  Encode exactly §4–§5 and a verdict-neutral
   scorer.  Commit both before execution.  The scorer classifies from the
   computed quotient and verified certificates; it contains no expected
   dimension, sector count, witness value, or verdict.
4. **One official solve.**  Produce the exact bases/sector strata, gauge
   quotient, determinant/interference/observable diagnoses, paper, transcript,
   and totally sealed receipt.  No post-truth edit precedes commit-as-is.
5. **Post-commit verification.**  Rerun from committed bytes, then from a true
   off-tree/no-git copy in an alien CWD.  Run every named mutant and CLI mode.
6. **Hostile protocol and panel.**  Freeze the protocol before any reviewer.
   The three independent lenses are algebraic-geometry/quantifier,
   operator/instrument/gauge, and gravity/covariance/ontology.  Reviews are
   frozen separately and jointly adjudicated before repair or terminalization.

## 7. Registered primary outcomes

Exactly one primary word is emitted by the frozen scorer, with a separate
stratum table retained even when the primary word compresses it.

1. `JCV-EMPTY` — no joint solution exists.
2. `JCV-POINT-MOD-GAUGE` — one zero-dimensional physical class remains and
   every registered observable is fixed there.
3. `JCV-PAIRING-SELECTED-WEIGHTS-FREE` — comparison holonomy is unique modulo
   gauge on the registered nonfactorizing locus, while a positive-dimensional
   weight direction moves a calibrated observable.
4. `JCV-GAUGE-FAMILY-OBSERVABLE-INVARIANT` — multiple coordinate solutions
   survive but all remaining directions are certified gauge or leave every
   registered observable fixed.
5. `JCV-DECLARATION-INDEXED` — more than one gauge-invariant comparison class
   survives on the same registered physical locus and moves a calibrated
   observable.
6. `JCV-CLASSICAL-ONLY` — every solution lies on the rank-deficient,
   zero-internal-interference locus: the semialgebraic witness
   `Delta*x*y != 0` is empty (with `u*v=-x*y` on the completeness surface).
7. `JCV-STRATIFIED` — different nonempty algebraic strata earn different words
   above; no single unqualified selection statement is licensed.

Feasibility is explicit.  Polynomial inconsistency makes outcome 1 reachable;
isolated roots make 2 reachable; a fixed holonomy with a continuous normalized
weight conic makes 3 reachable; gauge orbits make 4 reachable; multiple
holonomy sectors make 5 reachable; determinant/interference ideals make 6
reachable; reducible ideals with full-rank and dark components make 7
reachable.  The scorer's priority and compound-stratum rules are frozen before
truth.

Secondary rungs are reported independently:

- `JCV-REFERENT-TYPED` or `JCV-BLOCKED-AT-COMPARISON-REFERENT`;
- `JCV-BASE-TRIANGLE-COHERENT`;
- `JCV-NEIGHBOR-EXTENSION-COHERENT`;
- `JCV-ALL-INPUT-INSTRUMENT`;
- `JCV-NONFACTORIZING-LOCUS-NONEMPTY`;
- `JCV-CALIBRATED-OBSERVABLE-MOVES` or `...-FIXED`;
- `JCV-HOMOGENEITY-DEPENDENT` or `...-INDEPENDENT`.

## 8. Kill conditions

The unit is refused or demoted if any of the following occurs:

- the solver, fixture, or scorer exists before its authorized freeze stage;
- any substantive float, tolerance, numerical root, finite rational sampling,
  or hand-listed solution is used to classify the variety;
- the matrix intertwiners fail exact isometry or observable calibration;
- gauge quotienting is omitted, inferred from counts, or performed after
  inspecting the desired observable;
- only triangle `012` is tested and the neighboring triangle is absent;
- cut equality is imposed structurally in dark directions instead of using the
  frozen amplitude-level equations;
- the Z/X weak-surface result from paper 01 is imported as evidence for weight
  freedom in this calibrated arena;
- completeness is checked on one state rather than as an operator identity;
- `Delta != 0` is silently treated as an equation or a sampled condition;
- actualization, geometry, backreaction, QFT/GR recovery, species, or continuum
  physics is promoted from this interface fixture;
- a positive selection claim is inferred from the real slice as though the
  full complex variety had been solved;
- a local extension result is generalized to arbitrary atlases or all arities;
- any verdict string, paper numeral, receipt key, read, wall, or falsifier
  bypasses the current RUNBOOK engravings.

## 9. Frozen anchors

All corpus files below are anchored at base commit
`7adc7cbf4897b303077bbfba4e11a7268274a10f`:

| path | SHA-256 | required meaning |
|---|---|---|
| `v16/paper-01-joint-relational-history-law.md` | `98489edb6a83919199c11b14b92c423965d1a08ad7652a1c1915d5402f9e6003` | common-boundary candidate, rival-law scope, and open pairing/weight debts |
| `v16/note-jrh-delta-adjudication.md` | `cc4f4d11abd77287f645dfd34af438630629c1bfcb2a52212bfd90fd48c0660c` | geometry-sector dilemma, completeness gap, and nonfactorization requirement |
| `v16/note-jrh-terminal-verification.md` | `223e12ccca014947b6fe8a0dcbd584c72ff0ff83a81f630a14a89ff512047824` | immutable terminal boundary; paper 01 is not reopened |
| `v15/paper-43-contract.md` | `0c8d1a687b148a6cc5a0c1bfc199d2caa30db85f1e9ce4bbcf50fbe91ffe057f` | declaration-family precedent only, not a theorem about this arena |
| `v13/paper-coc-cocycle.md` | `44c50aa1faf956b2e814c82a35d0aac4427eaa8b83061c6a6772d7391681cb82` | local-versus-extension cocycle precedent |
| `v12/paper1-composition-defect.md` | `81bdab5673fb67b63cd10c08fbb80870f8aa01088047718c5b4bf447e1669128` | records/interference precedent only; no object identity imported |
| `v14/code/era_template.py` | `d04a3eb58fbcfe3d093b98126ca23c1610a9cc7cec96c9b9097eed515516f2b9` | current receipt/seal discipline |

Primary antecedents for attribution, not runtime data: Dittrich's
cylindrical-consistency programme (arXiv:1205.6127), Hawkins–Markopoulou–
Sahlmann's completely-positive quantum causal histories
(arXiv:hep-th/0302111), Dowker–Johnston–Surya on extension of strongly positive
quantum measures (arXiv:1007.2725), and Levin–Wen on pentagon/coherence data in
string-net models (arXiv:cond-mat/0404617).  This unit does not claim to
instantiate any of those theories.

## 10. Receipt, CLI, runtime, and whitelist

- Exact arithmetic only; the algebraic matrix backend is `Q(sqrt(2))` and the
  polynomial backend is `Q`.
- Runtime caps: 120 seconds for every public calibration and 240 seconds for
  the sole physical solve.  A timeout is procedural `JCV-INVALID`, scientific
  outcome null.
- CLI minimum: plain run, `--selftest`, `--mutant NAME`, rejection of unknown
  flags at exit 2, and zero artifact writes by test modes.
- All emitted numbers render from the sealed receipt.  Seals are total and
  recomputed before and after atomic promotion.  The transcript is reconciled
  to the ledger by row multiset and whole-byte digest.
- Required mutants include anchor corruption, sign/isometry corruption,
  calibration swap, dropped triangle, cut-equality weakening, completeness
  state-only substitution, gauge-collapse, dimension forgery, determinant-locus
  inversion, observable-constancy forgery, homogeneity smuggling, verdict flip,
  paper-number corruption, transcript contradiction, and post-seal add/edit.

Pin-stage whitelist:

- `v16/note-jcv-pin.md`, `v16/LOG.md`, `v16/README.md`, `v16/PLAN.md`,
  `v16/QUESTIONS.md`, and `STATUS.md`.

Solver-freeze whitelist:

- `v16/code/jcv_variety.py`, `v16/code/jcv_public_calibrations.json`,
  `v16/code/jcv_public_output.txt`, `v16/code/jcv_public_receipt.json`,
  `v16/note-jcv-solver-freeze.md`, plus the v16/root ledgers.

The following physical paths must be absent from the solver-freeze commit:

- `v16/code/jcv_fixture.json`, `v16/code/jcv_score.py`,
  `v16/code/jcv_output.txt`, `v16/code/jcv_receipt.json`, and
  `v16/paper-02-joint-comparison-fixed-point.md`.

Fixture-freeze and delivery may touch only those five reserved paths plus
separately named ledger/status files.  Staging is always by explicit path.  The
untracked v15 SCOUT-T paths remain outside every read list and whitelist.

## 11. Scope engravings

This unit does **not** derive or decide:

- a general complex comparison variety;
- arbitrary-dimensional boundaries or arbitrary atlases;
- an all-n interaction law;
- a graph/causal/metric carrier rewrite;
- gravitational backreaction or a GR limit;
- EPR steering on growing subsystems;
- actualization;
- Hamiltonian uniqueness;
- fields, particle species, statistics, scattering, constants, scale, Lorentz
  behavior, or deviations from QFT/GR.

Any of those words in a positive conclusion is a wall violation.  At every
stage the unit halts at its authorized boundary.
