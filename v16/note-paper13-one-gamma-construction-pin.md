# Paper 13 — ONE-GAMMA construction pin

**Status:** PIN. Result-neutral construction contract. No Paper 13 scientific
outcome is awarded by this file.

**Immutable base:** commit `45fccbcf8a9fce35c5abc08d56531fa6111c8f62`.

**Binding predecessor:** `v16/note-apr-one-gamma-paper-review-gate.md`,
SHA-256
`06d171a3eea8109e177e2dfa3cb5536fe3785043e676f735c36e91d03834cb51`.

## 1. Authorization, exposure, and stopping line

Paper 12 is terminal at `APR-BLOCKED-AT-BOUNDARY-GLUING`.  This pin
authorizes one Paper 13 construction cycle whose first scientific object is a
complete point-free transition-law family.  It does not authorize metric,
curvature, continuum, gravity, GR, QFT, phenomenology, or publication work.

Extensive private analytic design preceded this freeze.  Consequently every
formula, exact number, local reciprocal fixture, finite matching row, attack,
and proposed positive rung written below is **RESULT-EXPOSED-BEFORE-PIN**.
Private scratch files are not project evidence and are not inputs to the
authoritative construction.  The construction must rebuild every object from
this committed pin, publish a complete receipt, and survive independent
no-import reconstruction.  The only unexposed objects will be the explicitly
post-source-freeze matching confirmations in Section 15; those confirmations
test uniformity, covariance, and oracle resistance, not discovery of the
analytic theorem.

The immediate v4/v5 weld failures were ordinary one-law typing and dataflow
failures.  They did not require quantum mechanics or indivisibility to diagnose.
Passing this unit would prove only that one executable finite candidate has the
registered properties.  It would not establish the physical ISP law, select a
coupling, derive actualization, or reach metric geometry.

## 2. Boxed question

> Does there exist one total, normalized, point-free transition-law family on
> typed complete configurations such that regional contact, coherent
> nondivision, a record-bearing division, raw relational support change,
> reciprocal matter--relation response, and a resource-matched family assay
> are all computed shadows of that same law rather than neighboring modules?

The highest outcome eligible in this cycle is
`P13-RELATIONAL-GAMMA-CLASS-RELATIVE-EVENT-GRAMMAR-PRICED`.  It is not
pre-awarded.  The coupling, event/filling selection, division doctrine, and
actualization remain unselected, priced, or postulated even if every eligible
gate passes.

## 3. Term-binding and sample-space table

This table is binding under RUNBOOK E-34.  A same-named object outside these
definitions is split unless an explicit isomorphism is measured.

| term in this unit | binding definition | explicitly not identified with |
|---|---|---|
| complete typed configuration | the full contextual, matter, and port-status state in Sections 4--5; its lawful source use is typed by the boundary and filling in the complete key; `v16/note-apr-one-gamma-paper-review-gate.md:49-68` | APR prefix syntax, an optional native/history-context payload, a maximally fine trajectory, a Hilbert state, or Barandes's fixed configuration catalogue |
| law-sufficient | equal complete typed source arguments at a lawful source boundary have equal future profiles for every licensed filling | Markov sufficiency at every intermediate cut |
| `Gamma_g` | the one whole-filling amplitude evaluator plus the postulated endpoint-squaring clause in Section 6; `v16/note-apr-one-gamma-paper-review-gate.md:140-180` | a family of separately supplied two-boundary kernels, response tables, instruments, or a cached shadow container |
| admissible stochastic division boundary | this unit's typed boundary at which same-law ordinary conditionalization, alternate-cut equality, and registered future-record recovery hold | RUNBOOK `CELL-HIT`, the inherited three-actor “division event,” or every Barandes conditioning time |
| Barandes division event/time | only the source-backed notion described at `v16/note-apr-one-gamma-paper-review-gate.md:80-86` | this unit's record boundary by definition |
| record | a carried Boolean branch effect recovered for every word of the frozen continuation grammar | append-only syntax, a label, or a universal permanent fact |
| region/role | a typed Boolean element in a finite zero-pattern context | APR `PrefixRegion`, a point-set region, a metric neighborhood, or a spacetime region |
| contact | nonzero Boolean meet for a typed query | causal contact, metric adjacency, distance, or interaction by itself |
| support change | horizontal activation or inverse merge of a typed Boolean role | motion in a pre-existing space, expansion, topology change, or geometry |
| amplitude/path | a representation used by the one whole-filling law | an ontic wavefunction, simultaneous actuality of alternatives, or a hidden phase beable |
| actual configuration | one candidate configuration may be definite at any cut | permission to restart autonomously at that cut |
| actualization | an orthogonal postulate-coordinate not constructed here | normalization, branching, division, or record writing |

Probability normalization is always over mutually exclusive **complete target
configurations at the declared output boundary**.  Internal amplitude terms are
simultaneous contributions to one endpoint amplitude, not mutually exclusive
ordinary-probability outcomes.  Record branches become ordinary alternatives
only at the registered division boundary after the division gates pass.

## 4. Declared arena and point-free configuration referent

The primitive arena is

```text
(C_pres, G, C, B, Div, {Gamma_g : g in Q intersect [1/3,1/2]}).
```

- `C_pres` is the finite typed zero-pattern presentation catalogue embedded in
  the countable atomless Boolean algebra.
- `G` is the explicit groupoid of type-preserving finite Boolean
  isomorphisms/relabelings.  Vertical regulator refinements are localized
  before this groupoid is formed; horizontal role activation is not localized.
- `C` is the candidate physical source groupoid, implemented by explicit
  transport witnesses rather than equality of serialization hashes.
- `B` is the exact source/target boundary grammar in Section 5.
- `Div` is the priced boundary typing and continuation doctrine in Section 9.
- `Gamma_g` is the unique nomological transition root in Section 6.

A finite context consists of typed Boolean roles and the zero/nonzero pattern
of all generated Venn cells.  Raw Venn cells and raw role names are
presentation data, not ontic points.  Boolean formulas use extensional truth
tables on essential support: variables whose flip cannot change the Boolean
element are erased.  The construction must therefore identify, through every
consumer,

```text
A and not A = 0
A or not A = 1
A and (B or not B) = A.
```

For a nonzero typed formula `P`, horizontal extension introduces one fresh role
`N` and splits every nonzero Venn cell satisfying `P`; forgetting `N` is the
inverse merge.  No component count, valuation, coordinate, mesh depth,
distance, dimension, or metric enters this definition.

The complete source groupoid action must transport boundaries, fillings,
source and target states, the class operator, and endpoint probabilities.
Presentation hashes bind bytes and provenance only.  A canonical hash is not
physical equality.

## 5. Exact boundaries, ports, and public language

A port is exact data

```text
(name, child role, parent formula P0, parent formula P1),
```

where `P0` and `P1` are nonzero, disjoint, and partition the unit on every
admissible base context.  Its sectors are `empty`, `branch0`, and `branch1`.
Its modes are `ACTIVE` and `CARRIED`.  Only an active port may be selected by an
occurrence; a carried port may be read but not selected.

An atomic boundary declares exactly:

```text
(matter-role signature, unique pre-port base context,
 complete port declarations and status sets,
 full coefficient-independent state catalogue).
```

Every port parent may use only roles of the recovered pre-port base context,
never the child of another active port.  Port children are unique.  The basis
is the full product licensed by the declarations; a hand-selected partial
catalogue is not a boundary.  Status sets use the canonical order
`empty < branch0 < branch1`.  Neutral labels and presentation order are
semantically inert.

The public exact language is closed over exact, recursively revalidated,
frozen and slotted primitive nodes for formulas, contexts, ports, port
declarations, configurations, occurrences, boundaries, states, arrows,
linear maps, continuation grammars, matching presentations, source-groupoid
witnesses, and law implementations.  Foreign objects, duck types, subclasses,
instance-method shadows, non-Boolean seals, malformed declared fields, and
undeclared instance fields refuse before operator evaluation.  Any valid
low-level replacement of a declared field must move the canonical complete
law/source identity before it can move an operator.

Every arrow returns the complete coefficient-independent target catalogue and
an exact coefficient for every target, including zeros.  A query must be total
on every source context before its generator AST is accepted.  Every public
identity, generator, composition, tensor, symmetry, associator, and unitor is
recursively retyped from primitive operands; caller-supplied outer boundaries
are not evidence.

The complete presentation key is

```text
(law identity and g, source boundary, derived target boundary,
 complete filling AST, complete source configuration).
```

Equal keys may never produce unequal operators or future profiles.  Explicitly
isomorphic sources may have different presentation hashes, but their groupoid
witness must carry the operator and endpoint law exactly.

## 6. The one primitive law

Freeze

$$
g\in\mathbb{Q}\cap\left[\frac13,\frac12\right].
$$

For a typed Boolean query `Q`, let `chi_X(Q)` be one when `Q` is nonzero in
`X`, and zero otherwise.  Define

$$
R_x=
\frac{1}{1+x^2}
\begin{pmatrix}
1-x^2 & -2x\\
2x & 1-x^2
\end{pmatrix}.
$$

For an occurrence selecting matter bit `c` and one active port, the sole
matrix-element rule is

$$
T_g(Y,c'\mid X,c;Q,p)
=R_{g\,\chi_X(Q)}[c',c]\,
\mathbf{1}\!\left[Y=\rho_{c'}(X,p)\right].
$$

Here `rho_0` toggles `empty <-> branch0` while fixing `branch1`, and `rho_1`
toggles `empty <-> branch1` while fixing `branch0`.  Each is the formula-based
horizontal extension/inverse merge above and must be an involution on every
admissible port domain.

A filling supplies boundary types, incidence, formulas, occurrence identities,
seal annotations, and composition order only.  It supplies no coefficient,
update, response, member identity, or target outcome.  Filling/event selection
is priced kinematics, not emergent time.

The class operator is recursively derived for identity, generator,
composition, tensor, symmetry, associator, and unitors.  Endpoint probability
is fixed by the postulated nomological clause

$$
\Gamma_g(y\mid x;U)=\left|K_g(U)_{y,x}\right|^2.
$$

The unit does not derive the Born clause.  Individual amplitude coordinates and
path labels are representational.  Whole-filling relative phase and
composition are nomological because changing them changes endpoint laws.  Two
amplitude realizations are equivalent here only if a frozen transformation
preserves every typed whole-filling endpoint law and composition, not merely a
one-leg Born matrix.

`Gamma_g` must be total and exactly normalized on every declared source, or the
earliest outcome is no higher than `P13-GAMMA-UNCONSTRUCTED`.

## 7. Ordered measurement programme

The evaluator performs these gates in order; the earliest failed rung governs.

1. **Referent and source-language census.** Recompute essential Boolean
   support, context admissibility, exact primitive closure, boundary totality,
   zero-target retention, complete source identity, and groupoid transport.
   The groupoid self-test runs uncached on a predeclared identity,
   nontrivial relabeling, inverse, and composable pair; raw-role and
   orientation mutations must fail or transport the measured object rather
   than a memoized copy.
2. **COMPLETE-GAMMA.** Build the one primitive recursively and verify exact
   totality, normalization, identity, composition, contextual tensor,
   symmetry, associator, unitors, and one nontrivial interchange square.
3. **Anti-wrapper lineage.** Prove every later shadow's backward arithmetic and
   type slice ends at this primitive.  A shared carrier, container, hash, or
   dependency edge is insufficient.
4. **Changing support.** Derive active-role creation and inverse merge from the
   primitive while treating the fixed global basis only as a meta-catalogue.
5. **Reciprocal chain.** Compose matter-to-relation writing and a later
   relation-sensitive matter probe through the literal output context.
6. **Lawful division.** Derive writer branches, all-input completeness,
   retained zero ports, alternate-cut equality, and delayed record recovery
   for every word of the frozen continuation grammar.
7. **Native nondivision.** Compute the native intermediate and endpoint laws
   and give an exact infeasibility certificate for every positive
   source-independent restart kernel on the declared cut carrier.
8. **Family and blind-class assay.** Evaluate one global resource-matched
   matching context per member with one `Gamma_g` call; derive the blind
   projection from physical resources and test the frozen transducer class.
9. **Covariance and root mutations.** Recompute every affected pre-quotient
   shadow under groupoid transport and registered root/context mutations.
10. **Outcome and scope rendering.** Derive the full exact outcome string and
    every orthogonal coordinate from the gated receipt object.

No gate may read a mutant name.  No expected value, analytic helper, cached
shadow, constructor argument, or result label may serve as the measured value.

## 8. Exposed exact controls

At `g=1/2` and contact one, the authoritative construction must derive, rather
than load, the following exposed controls:

$$
R=
\begin{pmatrix}
3/5 & -4/5\\
4/5 & 3/5
\end{pmatrix},
\qquad
B=|R|^2=
\frac1{25}
\begin{pmatrix}
9 & 16\\
16 & 9
\end{pmatrix}.
$$

For two coherent create/erase pairs,

$$
C=|R^2|^2=
\frac1{625}
\begin{pmatrix}
49 & 576\\
576 & 49
\end{pmatrix}.
$$

For a recorded ordinary division,

$$
B^2=
\frac1{625}
\begin{pmatrix}
337 & 288\\
288 & 337
\end{pmatrix}.
$$

The unique native algebraic factor at the unrecorded cut is

$$
K=CB^{-1}=
\frac1{175}
\begin{pmatrix}
351 & -176\\
-176 & 351
\end{pmatrix}.
$$

For every rational `g` in the frozen interval, the nontrivial eigenvalue proof
must derive the lower bound `527/175` on its absolute magnitude.  Anchor
assertions are exit-1-only; the scientific negative exits zero with its scoped
outcome printed.

## 9. Native nondivision and lawful record division

At the unrecorded active cut, the exact conclusion is:

> The cut is not a lawful stochastic division on the declared configuration
> space: the complete endpoint law admits no positive source-independent
> factorization through it. A definite configuration may still be actual
> there; what is forbidden is an autonomous Markov restart conditioned only on
> that configuration.

This conclusion is native and configuration-relative.  The construction must
also supply the positive history-conditioned joint
`P(a,b|q)=B(a|q)C(b|q)` as a scope control.  History or phase enlargement may
Markovize the representation; that does not make the added variables ontic.
Only an undeclared variable that changes futures at a lawful source division is
a source-sufficiency defect.  Context needed only to condition across this
registered nondivision is not automatically missing classical state.

The record writer is the same primitive typed from a blank active source port
to a carried target port.  The exact registered continuation alphabet is:

```text
matter roles: {source, probe}
selectable target port: active
query formulas: {0, L_active, L_record and N_record}.
```

The continuation language is the free finite-word language over its complete
typed boundary closure.  Each generator must intertwine the record projector;
the checked composition lemma must prove preservation for every finite licensed
word.  Direct evaluation and the complete sum over record-projected alternate
cuts agree for every input.  Retyping the carried port as active must expose the
inverse toggle and erase the record.  This earns a division only relative to
the printed grammar and priced division doctrine.

## 10. Reciprocal raw relational response

Use two matter bits, one record port, and one fresh probe port.  The writer
changes the literal complete context; the later occurrence of the same law on
the fresh probe queries `L_record and N_record` on that exact output context.
At `g=1/2`, derive the exposed joint endpoint law

```text
(source,probe): 00 -> 9/25
                01 -> 0
                10 -> 144/625
                11 -> 256/625.
```

The same typed boundary includes a zero-row counterfactual with the opposite
child incidence and every nonrelational coordinate fixed; it gives
`P(probe=1)=0`.  This is calibrated source sensitivity, not a new eraser law or
autonomous control.  A positive result earns raw relation-mediated reciprocal
response or proto-backreaction only.  It does not earn distance, metric,
curvature, gravity, or geometry.

## 11. Same-root perfect-matching family

For size `m` and permutation `pi`, construct one global context with roles
`L_i,N_j` and exactly:

1. one all-zero cell;
2. one private `L_i` cell for each `i`;
3. one private `N_j` cell for each `j`;
4. one shared cell with `L_i=N_{pi(i)}=1` for each `i`.

Every member therefore has `3m+1` cells, `4m` incidences, `2m` roles, support
two for each role, and degree-one `L--N` contact graph.  The query at slot `i`
is `L_i and N_i`, nonzero exactly when `pi(i)=i`.

For each registered queried subset, allocate distinct matter bits and active
ports in this one global context and compose one filling.  The evaluator must
call the same `Gamma_g` once on that global object.  At `g=1/2`, each marginal
is `16/25` on a fixed point and zero otherwise.  The product formula may be
proved against the direct call; it may not replace it or be evaluated as a
tuple of local gadgets.

The blind projection is computed from the literal catalogue.  It retains size,
resource counts, cell-arity histogram, support/degree multisets, calibrated
query roles, port/coin counts, schedule, and prior lawful record tokens.  It
erases only cross-interface co-reference, `pi`, raw names, IDs, hashes, and
contact bits.  For paired members with equal blind prefixes and common
initialization, an exact induction covers any stochastic blind transducer,
including unbounded memory.  Member-correlated initialization, incidence
leakage, or a member oracle voids the class rather than defeating the theorem.

The result is class-relative.  “Incidence cycle” is not topology.  A finite
family never establishes absolute relational irreducibility or law selection.

## 12. Pre-registered outcome ladder

The exact earliest-outcome order is:

```text
P13-SPECIFICATION-INCONSISTENT
P13-REFERENT-PRESENTATION-ONLY
P13-GAMMA-UNCONSTRUCTED
P13-LAWFUL-SOURCE-SUFFICIENCY-UNPROVEN
P13-WRAPPER-OR-LOOKUP
P13-SHADOW-WELD-FAILS
P13-FIXED-CARRIER-ONLY
P13-SUPPORT-CHANGE-UNPROVEN
P13-RECIPROCAL-CHAIN-UNINSTANTIATED
P13-DIVISION-RECOVERY-UNPROVEN
P13-NATIVE-NONDIVISION-UNRESOLVED
P13-BLIND-CLASS-UNRESOLVED
P13-RELATIONAL-GAMMA-CLASS-RELATIVE-EVENT-GRAMMAR-PRICED
P13-RELATIONAL-GAMMA-LAW-UNSELECTED
P13-LAW-SELECTED
```

Only outcomes through
`P13-RELATIONAL-GAMMA-CLASS-RELATIVE-EVENT-GRAMMAR-PRICED` are eligible in
this cycle.  The final two labels remain registered future rungs but are capped
because this pin contains no law selector.  The receipt must print the cap and
cannot silently promote through it.

Orthogonal coordinates always print:

```text
event_filling_selection = PRICED-KINEMATICS
division_doctrine       = TYPED-CANDIDATE-AND-GRAMMAR-RELATIVE
actualization           = POSTULATED
valuation               = UNCONSTRUCTED
metric                  = UNCONSTRUCTED
curvature               = UNCONSTRUCTED
continuum               = UNCONSTRUCTED
GR                      = UNCONSTRUCTED
```

## 13. Mandatory kills and controls

Every row is a changed object or source mutation with a reconstructible payload.
No row passes because its name is known.

### 13.1 Domain, identity, and primitive-language kills

- `PORT-PARENT-NONTOTAL`, `ACTIVE-ROLE-ZERO`, `TWO-BASE-CATALOG`,
  `CROSS-CHILD-PARENT`, `BOUNDARY-SIGNATURE-MISSING-SPECTATOR`,
  `PARTIAL-BOUNDARY-CATALOG`, `DEPENDENT-FRESH-CHILD-COLLISION`, and
  `DUPLICATE-ID` must refuse at the exact construction boundary.
- `GENERATOR-UNDECLARED-QUERY-ROLE`, `ALIEN-TENSOR-OPERAND`,
  `ALIEN-IDENTITY-BOUNDARY`, `FORGED-IDENTITY-TARGET`,
  `FORGED-COMPOSE-TARGET`, `FORGED-TENSOR-TARGET`, and
  `FORGED-GENERATOR-TARGET` must refuse before numerical evaluation.
- `FORMULA-SUBCLASS-PROXY`, `CONTEXT-SUBCLASS-PROXY`,
  `LAW-SUBCLASS-PROXY`, `FOREIGN-RHO-LEAF`, `ISOMETRIC-SWAPPED-RHO`,
  `FORMULA-INSTANCE-METHOD-SHADOW`, `PORT-RHO-OVERRIDE`,
  `MUTABLE-LAW-METHOD-SHADOW`, and `NONBOOLEAN-SEAL` must not share candidate
  identity or reach an operator.
- `POSTINIT-LAW-IDENTITY`: an ordinary assignment refuses; a permitted
  low-level declared-field change must move the complete identity before any
  changed operator is evaluated.
- `INESSENTIAL-BOOLEAN-ROLE` must preserve semantic identity, operator,
  endpoint law, and source identity for tautology/contradiction dummy syntax.
- `STATUS-ORDER-PRESENTATION` and neutral label changes must be invariant.
- `SOURCE-KEY-OMITS-FILLING` must demonstrate that distinct complete fillings
  have distinct keys even when the configuration component agrees.

### 13.2 One-law and arithmetic kills

- `WRAPPER-GAMMA`, `OCCURRENCE-SEVER`,
  `ALIEN-CARRIER-OR-CLONE-ROUTING`, `BARE-KRON-AS-CONTEXT-TENSOR`, and an
  incompatible composition must fail the one-root/type slice.
- `RESET-ONE`, `TARGET-CONTACT`, `COIN-CONTACT`, `NONINJECTIVE-RHO`, and a
  changed-source wrong-branch `Port.rho` must fail isometry, normalization,
  target provenance, or a native shadow.
- `ZERO-TARGET-DROPPED` and `SUPPORT-FILTERED-TARGET` must fail the exact typed
  target-catalogue gate.
- `BORN-TABLE-AS-GAMMA` and the same-one-leg-Born reflection control must prove
  that a supplied probability table does not determine the coherent whole law.
- `BRANCH-SUM-ONLY` must show that total instrument normalization alone cannot
  establish branch/record semantics.
- `CACHED-G-OR-SHADOW`, `QUERY-FORMULA-MUTATION`, phase/reflection mutation,
  and `MOVE-DOWNSTREAM` must recompute the affected native, record, reciprocal,
  and family objects from the changed root.

### 13.3 Division, family, and ontology kills

- `CARRIED-PORT-STILL-ACTIVE`, `RESET-WRITER-CHAIN`, hidden eraser,
  return-to-old-port, and `DELAYED-READER-SEVER` must kill record recovery or
  refuse by type.
- `HELDOUT-SHADOW-DUPLICATED`, `RESULT-EXPOSED-AS-HOLDOUT`,
  `REROLL-OR-EXPOSED-AS-HOLDOUT`, a hand-entered resource table, constant blind
  token, unequal blind schedule/prior-record law, and a leaked incidence bit
  must kill or void the claimed assay for the registered reason.
- `HISTORY-PASSED-OFF-AS-NATIVE-K`, `NONDIVISION-AS-STATE-DEFECT`, and
  `ABSOLUTE-NONDIVISION` must fail the exact scope renderer.  The positive
  history-conditioned control must remain visible.
- `META-CATALOGUE-AS-SUPPORT`, hidden valuation, raw relation to geometry,
  incidence cycle to topology, event order to time, amplitude to ontology, and
  normalization to actualization are hard scope kills.
- `FLOAT-OR-EXPECTED-TABLE` must find zero float literals or expected-answer
  paths in scientific evaluation and must fire on an injected control.

## 14. Four-gate ontology ledger

| object | referent | necessity | no-smuggling | discriminator |
|---|---|---|---|---|
| finite source groupoid | typed zero-pattern presentations and explicit Boolean-isomorphism witnesses | raw names/cells cannot be ontology | hashes are provenance; no point/count/valuation/metric | whole fillings, operators, and endpoint laws transport; horizontal extension changes the source object |
| `Gamma_g` | postulated exact law family on complete typed arguments | Paper 12 has no global process law | source freezes before fresh cases/shadows; interfaces supply no dynamics; Born clause printed as postulate | root mutation moves every reachable pre-quotient shadow |
| active support extension | new typed Boolean role and context | fixed-carrier presentation cannot express relational change | uses only source formula and primitive `rho` | inverse merge plus later contact-sensitive response |
| carried record | derived branch effect at the registered boundary | lawful conditionalization requires stable readable alternatives | writer and continuations are same-law restrictions; grammar printed | all-word projector recovery, alternate-cut equality, and active-reuse erasure |
| raw relation | nonzero typed Boolean contact | reciprocal response needs a contextual operand | no valuation, distance, geometry, or response table | same-law incidence change moves later matter response |
| native nondivision | no positive source-independent restart kernel on the declared cut carrier | distinguishes one indivisible whole law from arbitrary Markov composition | definite configurations and history Markovization remain allowed | exact negative factor, lawful division comparison, positive history-conditioned scope control |
| blind comparator class | operational adversary with incidence erased | finite fixture can be copied by unrestricted lookup | resources and tokens derived before outputs; oracle/leak voids class | equal blind prefix and prior record law but unequal same-root response |

No row promotes the supplied mathematical representation to ontology.  The
candidate ontology is one actual relational configuration; law-sufficiency
belongs only to the complete typed source argument at an admissible source or
division boundary, not to a bare configuration or native nondivision cut.  The
law ranges over possible complete source and target configurations.  Which
configuration becomes actual remains postulated.

## 15. Source freeze and fresh-confirmation protocol

The authoritative evaluator is committed and SHA-256-frozen before any fresh
case is generated.  Development self-tests before that commit are allowed but
are permanently exposed and advisory.  After source commit:

1. a mutually blind reviewer supplies one 32-byte nonce not chosen by the
   evaluator author;
2. the generator computes
   `SHAKE256("P13-FRESH-v1" || source_sha256 || nonce)`;
3. a frozen deterministic rejection/increment rule selects large matching
   contexts and two to four common-prefix/challenge queries;
4. no reroll, replacement, shrinkage, or source edit is permitted;
5. source, generator, nonce, rejection count, case hashes, peak cell count,
   peak state count, and exposure class are written to the receipt;
6. the evaluator directly executes one global `Gamma_g` call per case; the
   analytic factorization may verify but may not supply the output.

Fresh cases are confirmation controls only.  A failure is a scientific or
integrity result under the registered ladder, not permission to reroll.

## 16. Receipt, lineage, and falsifier contract

The machine-reconstructible receipt contains:

- pin, RUNBOOK, predecessor, source, fresh-case, input, output, and receipt
  hashes plus a normalized payload self-hash;
- the exact read set at the point each file is opened, with path, expected
  hash, observed hash, and consumption key; an unused anchor is a failure;
- exact earliest outcome, cap, and all orthogonal coordinates;
- law primitive identity, exact `g` domain, source/target boundaries, full
  typed state catalogues including zero rows, filling AST, source state, and
  explicit groupoid witness;
- per-shadow lineages for the primitive, `B`, `C`, `B^2`, `K`, writer branches,
  record projectors, continuation certificate, alternate cuts, reciprocal
  joint, matching responses, blind projection, and fresh cases;
- primitive IDs and arithmetic/type DAGs consumed by each claim;
- every mutant as complete old/new primitive bytes, changed path, recomputed
  residual, affected claim, and resulting outcome;
- exposure and freshness ledgers, nonce, rejection count, resource parity,
  blind-prefix/prior-record equality, and peak sizes;
- the exact native-nondivision sentence from Section 9 and every scope wall.

Every claim renders from this gated receipt object.  Every numeric prose claim
in the Paper 13 candidate later renders from the receipt or is marked and gated
as an in-text derivation.  Every load-bearing row carries a falsifier or a
named exact forcing waiver.  Falsifier descriptions are checked against their
changed objects.  The outcome string is rebuilt segment-by-segment from
measurements, compared for exact equality, and attacked by an outcome-flip
mutant.

The seal manifest is recomputed at the promotion door and covers every emitted
claim, wall, scope coordinate, artifact, read, and mutation row.  Walls are
positive semantic predicates with injection mutants; removing or weakening a
wall fails the seal.  Claim tables are compared by keyed two-way equality, not
containment.  Counts are computed from emitted objects, never typed.  The
output/transcript is bound to the ledger event by its normalized content hash,
not only by a filename.

The independent verifier imports no evaluator function and rebuilds at least:
the essential-support quotient, one nontrivial groupoid transport, one complete
operator and endpoint law, the native negative factor, all-input division and
alternate cuts, the reciprocal joint, one global matching case, the blind
induction premises, and one source mutation.

## 17. CLI, determinism, runtime, and transactionality

The authoritative source uses Python standard-library exact arithmetic only,
principally `fractions.Fraction`.  Scientific paths contain no floats,
tolerances, `eval`, `exec`, network, Git queries, current-time dependence,
unrecorded randomness, CWD dependence, or expected answer tables.

Required strict modes are:

```text
--selftest
--generate-fresh --nonce HEX --source-sha SHA256 --fresh-out ABSENT_PATH
--run --fresh FRESH_JSON --output ABSENT_PATH --receipt ABSENT_PATH
--mutant NAME
```

Unknown, missing, duplicate, incompatible, or relative publication arguments
refuse before scientific evaluation.  Publication is transactional to absent
destinations and never overwrites an artifact.  A failing run leaves no partial
artifact.  `--selftest` must not read fresh cases or official artifacts.

The cap is 300 seconds for each complete mode, with a progress record at least
once per 60 seconds during long exact phases.  Two full runs are byte-identical
apart from separately excluded timing diagnostics.  Repository-root,
alien-CWD, and true off-tree/no-`.git` runs of authenticated bytes are
byte-identical.  A fixture-denial/import sentinel proves self-test separation.

Static checks include AST parsing, no-float/no-oracle/no-network/no-Git scans,
claim-lineage completeness, exact primitive-language closure, seal-manifest
totality recomputed at promotion time, and file-whitelist enforcement.

## 18. Committed anchors and exposure ledger

The construction authenticates these immutable inputs before evaluation:

| artifact | SHA-256 | permitted use |
|---|---|---|
| `RUNBOOK.md` | `5629dd083da923e216143c249ce0246da3238ddb9475bd6d67954ce0aa8aac58` | process and integrity rules |
| `v16/note-apr-one-gamma-paper-review-gate.md` | `06d171a3eea8109e177e2dfa3cb5536fe3785043e676f735c36e91d03834cb51` | methodological and ontology gates |
| `v16/paper-12-atomless-regions-and-the-missing-gluing-law.md` | `56cddeacbfe477d1af244b310e9a26b5622ef540b82deea5a96158819ba972f7` | terminal negative and scope only |
| `v16/note-apr-paper12-final-adjudication.md` | `5ef1440064b703bd04bf97f1774f7f5e03efe537aeee2669bd1471f0a402799e` | terminal disposition only |
| `v16/code/apr_paper12_exact.py` | `c209486a94016c00921c3b9edfeb2f53eef7d005180eb3c1d95153e56fec86a7` | independent anchor authentication only; never imported by the evaluator |
| `v16/code/apr_paper12_receipt.json` | `d4e16c262d1c929d6e0507ef482eef4c2ff26c7f41f9dc4bf0bc58b708adfd39` | frozen negative provenance only; never scientific input |

The APR `AB/BC` tables are simultaneous gluing diagnostics, not transition
data.  No APR scorer, fixture, output, receipt conclusion, or synthetic v5
positive package is an input to the new law.

All values and finite examples in Sections 8--11 are exposed analytic controls.
Current private exploratory bytes are not anchors, are never imported, and are
not copied as scientific evidence.

## 19. Staged whitelist and commit boundaries

No path outside the explicit stage may change.

### Stage A — authoritative evaluator source freeze

```text
v16/code/p13_gamma_exact.py
v16/note-paper13-gamma-source-freeze.md
v16/PLAN.md
v16/LOG.md
STATUS.md
```

Only development self-tests may run before this source commit.  No fresh case
or official publication path may exist at that commit.

### Stage B — one fresh generation and official construction

```text
v16/code/p13_gamma_fresh_cases.json
v16/code/p13_gamma_output.txt
v16/code/p13_gamma_receipt.json
v16/note-paper13-gamma-verification.md
v16/PLAN.md
v16/LOG.md
STATUS.md
```

Stage B uses exact committed Stage-A bytes.  Any later source edit invalidates
the cycle; it is not silently repaired under the same freeze.

### Stage C — result-known Paper 13 candidate

```text
v16/paper-13-one-relational-gamma.md
v16/paper13_code/run_all.py
v16/paper13_code/manifest.json
v16/paper13_code/receipts_table.json
v16/paper13_code/RUN.txt
v16/PLAN.md
v16/LOG.md
STATUS.md
```

The paper may be drafted only after the Stage-B receipt is independently
verified.  It cites external literature only and obeys the Markdown/KaTeX,
forbidden-token, references, proof, number-lineage, and self-contained bundle
rules in RUNBOOK Section 9.  Its claim scanner covers prose, tables, inline
code, and fenced blocks; fenced claims compare by multiset, not containment.
The Stage-C bundle contains no second scientific implementation:
`manifest.json` byte-binds the Stage-A evaluator and Stage-B fresh/input/output
artifacts; `run_all.py` authenticates those exact bytes, executes the frozen
master evaluator to absent temporary destinations, and compares the regenerated
objects; `receipts_table.json` maps every paper result/number to receipt keys;
`RUN.txt` gives exact clean/off-tree regeneration commands.  A separately
implemented no-import verifier remains an audit artifact outside this bundle.
A separate hostile protocol must freeze before any review dispatch.

Review protocols, reviews, adjudications, and any repair have separate later
whitelists and commits.  Git staging is explicit paths only.

## 20. Hostile protocol and terminal discipline

After a green Stage B and candidate, freeze a separate protocol before review.
At least three mutually independent repo-read-only seats are required:

1. exact operator/category/totality and anti-wrapper reconstruction;
2. stochastic indivisibility, division/record, and Barandes-scope review;
3. source-groupoid, point-free ontology, reciprocal/family, and eliminability
   review.

Each seat independently rebuilds the load-bearing objects without importing
the evaluator, reading another seat, or using receipt conclusions as evidence.
Each report freezes verbatim in its own commit and is jointly adjudicated.
Nothing is citable before terminal adjudication.

A reproducible blocker produces the earliest pre-registered outcome.  It does
not authorize moving metric, curvature, or GR into Paper 13.  A repair, if the
future adjudication authorizes one, requires a forward pin; source-freeze and
freshness claims are never retroactively rewritten.

## 21. Permanent scope walls and next event

Even the eligible positive ceiling would establish only one exact finite,
class-relative, point-free relational-law candidate with raw support change,
one grammar-relative division, one native nondivision witness, and one
resource-matched blind-class exclusion.

It would not establish:

- a uniquely selected `g`, law, catalogue, event grammar, or division doctrine;
- absolute relational irreducibility or configuration completeness against all
  enlarged representations;
- actualization, collapse, many worlds, particles, species, vacuum, or fields;
- emergent time, causality, no-signalling, Lorentz symmetry, or locality under
  arbitrary changing boundaries;
- valuation, length, metric, dimension, topology, curvature, continuum,
  Einstein dynamics, gravity, GR, or QFT;
- empirical adequacy or phenomenology.

Operational order plus extensive valuation, metric reconstruction,
curvature/response, and conditional GR recovery remain separately pinned later
units.  They are not Paper 13 phases and receive no evidence from architecture
alone.

The sole next authorized event is Stage A implementation of the evaluator on
the committed bytes of this pin.  No fresh confirmation, official run, paper
candidate, review, metric, curvature, or gravity construction begins before
the preceding committed gate.
