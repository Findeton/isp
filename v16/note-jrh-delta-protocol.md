# JRH independent hostile delta protocol

Date: 2026-08-17  
Frozen repaired commit: `99f2352`  
Unit: v16 paper 01, joint relational-history law

## Frozen target

The three seats review exactly the repaired checkpoint at commit `99f2352`:

| artifact | SHA-256 |
|---|---|
| `v16/paper-01-joint-relational-history-law.md` | `2f5d79266231d036fab180855a8b2ba80d652544e5b3ff55f8a0bee60e228a78` |
| `v16/code/jrh_exact.py` | `05979a992eb9b3b5c21b29af5736b85cadba744cf847404cb84ab7e6516ff53c` |
| `v16/code/jrh_output.txt` | `396f931ade115a2c6cf644bcb670fa2160c6696dc2925426451bf3d8b05901ff` |
| `v16/code/jrh_receipt.json` | `abe43f42492e88dfc9a279ca95d7143ce8e6e024bca318122d6ff99c3681de99` |
| `v16/note-jrh-adjudication.md` | `56f8e67bb22484ee5d366bcfc5a5adc325f103c7f33c200a0c8fa9567291d14f` |

Each reviewer may read the original candidate, its own original report, the
joint adjudication, the repaired target above, the corpus anchors needed to
rebuild a claim, and primary literature.  A reviewer must not read either
other reviewer's original or delta report before delivering.  No reviewer may
modify a candidate artifact, the protocol, a different review, or git state.

## Common mandatory audit

Each seat must independently answer all of the following.

1. Recompute the load-bearing exact claims rather than trusting the transcript:
   feed-forward eliminability, Bell-state entanglement breaking, `49/625`
   versus `337/625`, cycle ranks, proper triangle holonomy and gauge
   invariance, fixed-factor HJW/no-signalling, logarithm ambiguity, and the
   Z/X rival predictions.
2. Decide whether every original panel defect is now either repaired,
   explicitly refused, or still disclosed as open.  A renamed defect does not
   count as a repair.
3. Audit the paper/receipt equality, consequence vocabulary, choice inventory,
   source anchoring, CLI behavior, diagnostic non-writing, determinism, and
   off-tree/no-git reproducibility in proportion to the seat's expertise.
4. Attack the distinction between three different objects:
   the exact CP boundary instrument, the general decoherence-functional
   framework, and the concrete local-weight ansatz.  No property of one may be
   silently promoted to either other object.
5. Decide whether the formula
   `D(A,B)=Tr(K_A rho K_B^dagger)` with
   `K_A=sum_(h in A) a[h]V[h]` is a coherent candidate at the declared finite
   level, and list every additional condition required for changing relation
   carriers, gluing, normalization, strong positivity, coarse graining,
   conditioning, and objective actualization.  Coherence is not selection.
6. Check that “one actual relational history” is an ontological postulate, not
   an illicit classical distribution over unrecorded paths, and that exact
   division is not confused with an unproved approximate-decoherence rule.
7. Reclassify every direct and indirect consequence concerning EPR,
   no-signalling, two-versus-three actors, all-n interactions, Hamiltonians,
   particles/species, the three affine/constants, scale, GR, QFT, and possible
   deviations.  A deviation is not “absent” merely because none is typed.

Every finding must be labelled `CLOSED`, `PARTIAL`, `OPEN`, or `NEW-BLOCKER`,
with a precise theorem, counterexample, computation, or missing definition.
The report must end with exactly one grade: `ACCEPT`, `ACCEPT-WITH-FIXES`, or
`REJECT`.

## Seat O — operator / exact demolition

Write only `v16/review-jrh-operator-delta.md`.

In addition to the common audit, independently rebuild the exact linear
algebra and mutation surface.  Inspect full relabelling and diamond payload
coverage, distinguish an open path from a closed holonomy, test the normalized
Choi/separability claim, and look for circular gates or claims inferred from
their own declarations.  Attack whether the proposed history functional is
well typed when state spaces and relation carriers change.  State whether
`BOUNDARY-INSTRUMENT-CONSISTENT-BUT-FUNDAMENTAL-DYNAMICS-UNSELECTED` is exactly
licensed.

## Seat G — gravity / covariance / ontology

Write only `v16/review-jrh-gravity-delta.md`.

In addition to the common audit, verify that the repaired paper makes no claim
that copied labels are geometry.  Judge the proposed branchwise
matter–geometry history law against the minimum requirements for a genuine
relation rewrite, downstream geometric response, independently calibrated
matter load, boundary balance, overlapping refinement, closure/Bianchi data,
and continuum refoliation/deformation closure.  Attack the fixed-point
selector, the two gravitational decoherence forks, constants/scales, and every
GR/deviation statement.  Separate a viable research route from an achieved
gravity model.

## Seat Q — quantum / EPR / QFT

Write only `v16/review-jrh-quantum-delta.md`.

In addition to the common audit, independently check HJW preparation blindness,
the registered nonlinear signalling control, Bell entanglement breaking, and
the interference placement calculation.  Decide whether CP instruments as
division-boundary shadows are compatible with retained pre-division coherence
without proving the proposed law.  Audit changing-factorization no-signalling,
the pure-state ontic fork, the general-boundary/process-history parallels,
Hamiltonian emergence, field/interactions, particles/species, and the absence
of a typed QFT deviation.

## Kill conditions

Any one of the following requires `REJECT` unless the target already states it
as a refusal or open debt:

- a false exact promoted claim;
- any surviving assertion that the delivered toy realizes dynamical geometry;
- use of the entanglement-breaking boundary map as the universal microscopic
  successor;
- a hidden decomposition-sensitive or expectation-sourced nonlinear composite
  law presented as no-signalling;
- a classical positive history probability presented as sufficient for the
  measured interference witness;
- an untyped sum or trace across changing state spaces;
- the local-weight ansatz presented as selected dynamics;
- a particle list, affine/cosmological value, absolute scale, GR/QFT limit, or
  observable deviation presented as derived;
- a status stronger than the evidence distinguishes.

Reports will be frozen verbatim in separate commits before delta adjudication.
