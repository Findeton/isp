# Paper 13 typed-groupoid v5 source-delta pin

Date: 2026-08-20

Status: **RESULT-NEUTRAL PIN / NO OUTCOME PRE-AWARDED**

normalized_sha256: f17927da751e41854bd30062a22b07d7ba49461ec25e25e6cddedae3c3c8c056

## 1. Authority and stopping boundary

This pin is authorized only by the adjudicated Paper 13 v4 source `NO-GO` in
`v16/note-paper13-typed-groupoid-v4-source-audit-adjudication.md`, ordinary
SHA-256
`5acd5c43ead4a4a7ded8698f331328261c918f9ac0d6dc23ad515755306cf075`.
It binds the two exact counterexamples accepted there:

1. all 24 factor-certificate lineage entries can be deleted from tensor case 0
   while tensor verification and promotion remain true; and
2. the six real changed-source records retain hashes and prose but no committed
   patch/probe/capture preimages, so their claimed changed objects cannot be
   independently reconstructed.

This pin may repair only those source/evidence surfaces and their direct
regressions.  It may not change the Paper 13 scientific law, fixtures, fresh-
case derivation, outcome ladder, claim wording, or ontology ceiling.

The rejected v4 source remains immutable at
`v16/code/p13_gamma_exact_v4.py`, ordinary SHA-256
`272b64972e3eb620867cf6ad0decf8db1fcc03092adfd1d69d77aa10e2605910`.
It is a regression reference only, never an imported implementation or source
of expected scientific answers.

Before any v5 source byte is created, two mutually blind read-only audits must
authenticate and accept one immutable snapshot of this pin.  The first exact
contradiction stops a pin audit.  A revised snapshot requires a complete fresh
read and fresh hashes; findings may not be blended across revisions.

## 2. Inherited immutable scientific contract

The v5 candidate inherits without alteration:

- one exact `GammaLaw` primitive and its closed exact AST language;
- the finite Boolean-context/support-split calculus and 72 ambient / 42
  contextual quotient census;
- exact source-boundary, derived-target, filling-AST, source-configuration and
  law identity;
- typed total configuration and certificate action under the source groupoid;
- `R`, `B`, `C`, `B^2`, `K`, and the `527/175` native nondivision bound;
- the positive history/enlarged-carrier Markovization control;
- grammar-relative record persistence/division and all-input cut equality;
- reciprocal relation-mediated response;
- the resource-matched, incidence-blind-class-relative matching family;
- Born squaring as a postulated law clause;
- actualization as postulated, not derived; and
- all permanent metric/geometry/gravity/QFT walls.

The exact native restart coordinate remains

\[
CB^{-1}=\frac1{175}
\begin{pmatrix}351&-176\\-176&351\end{pmatrix}.
\]

It excludes a positive source-independent restart at that cut on the declared
carrier.  It does not say that an actual configuration is unreal or incomplete
and does not prohibit a declared history, phase, or carrier enlargement.

A stable record is a persistent local fact.  It is not automatically a
complete Markov checkpoint.  Factorization remains licensed only at a complete
typed division boundary whose full source argument passes the existing gates.

## 3. Closed v5 proof-language delta

The v5 source may add exactly two evidence-layer data types.  They are verifier
objects, not physical ontology.

### 3.1 `TensorFactorLineageManifest`

An immutable, frozen, slotted manifest has exactly these semantic fields:

```text
law_ref
left_source_presentation_ref
right_source_presentation_ref
left_witness_ref
right_witness_ref
left_factor_address
right_factor_address
retained_entries
manifest_binding_sha256
```

The factor addresses are exactly `(0,)` and `(1,)` for the declared tensor
root.  Each retained entry has exactly:

```text
factor_generator_address
input_ref
certificate_ref
pairing_key
```

Every `*_ref` is a content address into the case's complete-byte object store;
the store retains both the SHA-256 identity and the full canonical bytes.
`retained_entries` is sorted canonically by
`(factor_generator_address, pairing_key)`.  It is not an expected table: the
independent verifier must derive the expected tuple from decoded source data.

The binding hash covers the decoded/re-encoded law, presentations, witnesses,
factor addresses and full retained tuple.  It is diagnostic unless the verifier
has already reconstructed every covered byte.

### 3.2 `SourceMutationEvidenceBundle`

This immutable evidence bundle is review/provenance data, never an input to
Gamma, the candidate evaluator, a scientific gate, or a fresh-case result.  It
binds one exact base source, a canonical list of source mutations, a replay
program and a full replay receipt.  Its bytes live in separate committed paths
specified in Section 10.

No other new proof-language type, cache, history slot, native-context payload,
expected answer table, or scientific primitive is authorized.

## 4. Literal tensor-lineage producer

For each of the three inherited tensor cases, the producer receives exactly:

```text
law
left_source_presentation
left_witness
right_source_presentation
right_witness
complete_object_store
```

It validates exact candidate types and the following equations before
producing a manifest:

```text
left_witness.source  == left_source_presentation
right_witness.source == right_source_presentation
left_witness.target  == relabel(left_source_presentation, left_witness)
right_witness.target == relabel(right_source_presentation, right_witness)
```

It then derives, rather than receives, every generator-leaf address from each
complete factor Arrow.  Structural identity/unit/tensor/composite nodes do not
invent certificates.  A nested factor contributes every generator leaf at its
complete prefixed AST address.

For each derived generator address, the producer enumerates every declared
source configuration and every nonzero bound transition admitted by the
unchanged generator certificate constructor.  It constructs the complete
`CertificateActionInput`, stores that input and its actual embedded certificate
bytes, recomputes the pairing key, prefixes the address by the factor address,
and canonicalizes the complete tuple.

The three baseline manifest cardinalities remain exactly `24`, `12`, and `24`.
Those counts are consequences of the reconstructed tuples, never acceptance
criteria by themselves.

## 5. Independent raw-byte lineage verifier

The promotive verifier accepts only a complete tensor case and its complete-
byte store.  It does not accept an expected lineage, expected cardinality,
stored `all_exact`, cached manifest, caller-selected presentation, or final
boolean.

It must perform this exact route:

1. verify every store entry's SHA-256 against its supplied canonical bytes;
2. decode `law_ref`, both source presentations and both witnesses from those
   bytes using the public exact decoders;
3. re-encode each decoded object and require byte equality with its stored
   payload;
4. validate witness source/target typing and derive both factor root source and
   target nodes;
5. traverse both decoded factor Arrow trees independently and derive the exact
   generator-leaf address set;
6. independently rebuild every complete `CertificateActionInput` for every
   nonzero transition at each derived address;
7. independently build the canonical expected entry tuple from those rebuilt
   objects;
8. require full canonical-byte equality between expected and retained tuples;
9. require the stored per-case count to equal the retained tuple length and
   require the top-level flattened lineage and cardinality tuple to equal the
   exact concatenation and lengths of all three cases;
10. for every retained entry, decode `input_ref`, require `certificate_ref` to
    be the canonical bytes of that input's actual certificate, and recompute
    the pairing key from the decoded input; and
11. recompute and compare the manifest binding hash only after Steps 1--10.

The literal producer and the independent raw verifier may share primitive
canonical encoders/decoders and the unchanged certificate constructor.  They
may not share a final manifest object, expected tuple, expected cardinality,
memoized generator-address list, or acceptance boolean.

An empty lineage is lawful only when the independently decoded factor trees
contain no generator leaf with a nonzero bound transition.  Thus the unit
factor in the second case contributes zero while the generator factor still
contributes its complete nonempty tuple.

## 6. Promotion and backward-slice binding

Tensor promotion is true only if all of the following independently pass:

- exact factor root source and derived target nodes;
- exact typed tensor configuration action;
- complete-byte store authentication;
- the Section 5 raw lineage verifier for every case;
- equality of each per-case retained tuple with the independently rebuilt
  expected tuple;
- equality of per-case counts with tuple lengths;
- equality of top-level flattening/cardinalities with the three case tuples;
- every ordinary certificate action and pairing-key binding; and
- the inherited configuration/operator/Gamma naturality laws.

The groupoid measurement, gate table, independent outcome comparator, action
lineage, shadow lineage, claim-input DAG, receipt dependency slice and seal must
consume the canonical bytes of the independently rebuilt lineage evidence.
No consumer may read a copied `all_exact`, stored count, membership-only flag or
manifest hash without its reconstructed preimage.

The static source gate must AST-bind the exact producer, raw decoder/verifier,
top-level flattener, tensor verifier, promotion predicate, measurement, gate,
outcome comparator, lineage, claim and seal functions.  It requires exact
signatures, no variadics/default target, no global registry, no history/cache
lookup, and no expected cardinality used as acceptance evidence.  A real source
mutation of any bound function must change the static digest and demote before
scientific evaluation.

## 7. Mandatory changed objects

All 153 v4 registered attacks remain executable and must retain their exact
old/new object and evidence semantics.  V5 adds these eight registered attacks:

```text
L1 TENSOR-FACTOR-LINEAGE-DROP-ALL
L2 TENSOR-FACTOR-LINEAGE-DROP-ONE
L3 TENSOR-FACTOR-LINEAGE-DUPLICATE
L4 TENSOR-FACTOR-LINEAGE-SAME-COUNT-SWAP
L5 TENSOR-FACTOR-LINEAGE-FOREIGN-INPUT
L6 TENSOR-FACTOR-CERTIFICATE-REF-SEVER
L7 TENSOR-FACTOR-PAIRING-KEY-SEVER
L8 TENSOR-LINEAGE-PRODUCER-BYPASS
```

The minimum v5 registry is therefore exactly `161` attacks before any further
independently motivated attack.  Each L attack mutates an authoritative changed
object, preserves every coordinate not named by the mutation, and must move the
tensor measurement, groupoid gate, outcome index, lineage, claim and seal.

Required non-kills are:

- pure pre-pairing enumeration reorder followed by canonicalization;
- an identity/unit factor with zero generator certificates;
- nested tensor factors contributing zero/one/many generator-leaf families;
- simultaneous covariant relabeling of both factors and all manifest objects;
  and
- canonical store order permutation.

The eight real post-freeze source mutations are exactly the six inherited v4
mutations plus:

```text
L-SOURCE-PRODUCER-DROP
L-SOURCE-VERIFIER-BYPASS
```

The first changes the literal producer so a nonempty factor loses at least one
entry while the independent verifier stays unchanged.  The second changes the
raw verifier to the rejected membership/count-only rule while the literal
producer stays unchanged.  Each requires an independent semantic probe showing
the forbidden object, followed by a full source-only selftest refusal and
movement of measurement, gate, outcome, action lineage, shadow, claim and seal.

## 8. Canonical replayable source-mutation evidence

The source-mutation specification artifact is canonical JSON with schema
`p13-v5-source-mutations-v1`.  It contains:

```text
base_source_path
base_source_sha256
mutations[]
```

Each mutation row contains exactly:

```text
identifier
encoding = "utf8-exact-replace-v1"
old_bytes_base64
new_bytes_base64
expected_match_count = 1
patch_object_sha256
expected_mutant_source_sha256
probe_kind
```

`patch_object_sha256` is SHA-256 of the canonical JSON bytes of the row after
deleting only that field.  The replay program base64-decodes both byte strings,
requires the old bytes to occur exactly once in the authenticated base source,
replaces that one occurrence, and derives the mutant-source SHA-256.  Zero or
multiple matches refuse.

The replay program is a committed standard-library-only script.  It accepts
only absolute paths to the frozen base source, mutation spec and an absent
scratch destination.  It has no repository import, expected result table,
network, Git query, fallback path, or write outside its supplied private
scratch directory.  It reconstructs all eight mutants from the exact specs,
runs their semantic probes and source-only selftests as subprocesses, and emits
one canonical JSON receipt transactionally.

The committed replay receipt uses schema `p13-v5-source-mutation-receipt-v1`
and retains, for every mutation:

```text
complete canonical mutation-spec row
derived mutant-source SHA-256
complete semantic-probe object and canonical bytes
probe exit/stdout/stderr bytes and SHA-256 values
full-selftest exit/stdout/stderr bytes and SHA-256 values
runtime and max-RSS observations
publication-write count
clean and mutant measurement/gate/outcome/lineage/shadow/claim/seal objects
exact moved-field tuple
```

Large empty or repeated byte strings may be retained once in a content-
addressed receipt store only if both their full bytes and SHA-256 are present.
No digest without its preimage is evidence.

The replay program has its own fixture-free `--selftest` that kills:

```text
R1 PATCH-BYTES-DROPPED
R2 PATCH-HASH-SEVERED
R3 BASE-SOURCE-HASH-SEVERED
R4 ZERO-OR-MULTIPLE-MATCH
R5 MUTANT-SOURCE-HASH-SEVERED
R6 PROBE-PROGRAM-BYTES-DROPPED
R7 CAPTURE-BYTES-DROPPED
R8 DEPENDENCY-OBJECT-COPIED
```

The candidate evaluator never reads the mutation spec, replay program or
receipt.  Source audits execute the replay program independently after the
candidate selftest.  The source-freeze note authenticates all three committed
artifacts and records exact command, exit, runtime, RSS and zero-write evidence.

## 9. Finite census, runtime and CLI gates

The scientific/groupoid census remains finite:

```text
34 abstract bijections
14,050 composable triples
13 native rows
12 generator families
312 source columns
468 bound transitions = 156 CREATE + 156 MERGE + 156 UNCHANGED
480 complete certificate actions = 468 identity + 12 nonidentity
3 tensor cases with reconstructed baseline lineage 24/12/24
```

The source selftest must visit every declared public exact type, run all checks,
execute all 161 or more registered attacks, rebuild the full tensor lineage by
both routes, and serialize every promotive byte.  Root, alien-CWD and true
one-file/no-`.git` runs each have a hard 300-second wall-clock cap and emit
progress at least every 60 seconds.  All three stdout byte streams and normalized
payloads must be identical.  Peak RSS is measured and printed.

Evidence may be streamed, canonical fragments may be reused after their full
bytes are authenticated, and immutable attack setup may be shared copy-on-write.
No scientific coordinate, lineage entry, changed object, zero-probability row,
or receipt preimage may be trimmed to meet the cap.  A run exceeding 300 seconds
is a source-freeze `NO-GO`, not a reason to weaken the contract.

The candidate evaluator retains the inherited strict CLI.  Before source-audit
acceptance only `--selftest` and exact named development mutants may run.  No
argument, unknown/duplicate/incompatible arguments, relative artifact paths,
existing destinations and partial writes refuse.  `--generate-fresh` and
official `--run` remain forbidden until later authorization.

## 10. Exact stage whitelists and chronology

No path outside the exact active stage whitelist may change.  The six unrelated
pre-existing untracked `v15/scoutt` paths are never touched or staged.

### Stage A — this v5 pin

```text
v16/note-paper13-typed-groupoid-v5-source-delta-pin.md
v16/PLAN.md
v16/LOG.md
STATUS.md
```

After two mutually blind pin audits accept one immutable snapshot, those four
paths commit together.

### Stage B — v5 source and replayable evidence freeze

```text
v16/code/p13_gamma_exact_v5.py
v16/code/p13_gamma_exact_v5_source_mutations.json
v16/code/p13_gamma_exact_v5_source_mutation_replay.py
v16/code/p13_gamma_exact_v5_source_mutation_receipt.json
v16/note-paper13-typed-groupoid-source-freeze-v5.md
v16/PLAN.md
v16/LOG.md
STATUS.md
```

The source is held first; the spec derives from those held bytes; the replay
program derives every mutant and receipt; all paths reauthenticate; then the
source and evidence freeze in one commit.  The freeze note records the source,
spec, replay-program and receipt hashes.  Its own ordinary hash is recorded
afterward in PLAN/LOG/STATUS, avoiding a self-hash cycle.

### Stage C — two mutually blind v5 source audits

Each report is the sole path in its own commit:

```text
v16/review-paper13-typed-groupoid-v5-source-operator.md
v16/review-paper13-typed-groupoid-v5-source-records.md
```

The operator seat independently reconstructs action/category semantics and
attacks lineage completeness.  The records seat independently decodes every
store/manifest/mutation artifact and replays all eight source mutations.  They
do not inspect or contact each other.  Either stops at its first exact blocker.

After both freeze, adjudication changes exactly:

```text
v16/note-paper13-typed-groupoid-v5-source-audit-adjudication.md
v16/PLAN.md
v16/LOG.md
STATUS.md
```

Only joint acceptance may resume the publication chronology.

### Stage D — fresh case, official artifacts and verification

No Stage-D path may exist before accepted Stage-C adjudication.  A later
accepted source may authorize exactly:

```text
v16/code/p13_gamma_fresh_cases_v5.json
v16/code/p13_gamma_output_v5.txt
v16/code/p13_gamma_receipt_v5.json
v16/note-paper13-gamma-verification-v5.md
v16/PLAN.md
v16/LOG.md
STATUS.md
```

The nonce is external, single-generation and no-reroll.  The source commit
precedes nonce receipt and case derivation.  The independent verifier imports
no evaluator function and reconstructs the complete receipt.

### Stage E — result-known paper and bundle

Only an accepted Stage-D verification may authorize exact versioned paper and
bundle paths in a separately frozen publication pin.  No v4 or earlier paper,
bundle, fresh case, output or receipt is overwritten.

## 11. Source-audit acceptance contract

Before inspecting implementation semantics, each source auditor must:

1. authenticate committed HEAD, this pin, source, freeze note, mutation spec,
   replay program and mutation receipt;
2. read this pin and freeze note completely;
3. independently reconstruct the finite tensor-lineage and mutation-evidence
   models without importing the candidate evaluator; and
4. run the black-box source only in authorized modes.

Required source-audit attacks include all L1--L8 and R1--R8, the two new real
source mutations, a foreign same-count manifest, a store-order non-kill, and a
fresh independently constructed generator nesting.  The records seat must
apply the committed patch specs itself and reproduce all derived mutant-source
hashes and capture objects; trusting the frozen receipt is forbidden.

The first exact source/evidence counterexample yields `NO-GO` and stops later
science.  Acceptance means only that the source is eligible to resume the
result-neutral publication protocol; it does not pre-award any Paper 13 rung.

## 12. Outcome ladder and permanent walls

The inherited ordered Paper 13 ladder is unchanged.  A malformed specification
or non-reconstructible mandatory object yields
`P13-SPECIFICATION-INCONSISTENT`.  A failed point-free action/lineage theorem
yields no higher than `P13-REFERENT-PRESENTATION-ONLY`.  The intended positive
ceiling remains merely eligible, never promised:

```text
P13-RELATIONAL-GAMMA-CLASS-RELATIVE-EVENT-GRAMMAR-PRICED
```

No v5 result may claim:

- absolute non-Markovianity or absolute relational irreducibility;
- that a definite intermediate configuration is unreal or incomplete;
- that every stable record/happening is a complete division boundary;
- derivation of Born squaring, actualization, coupling, event selection or the
  continuation/division grammar;
- a valuation, time, causal order, topology, dimension, metric, curvature,
  continuum, energy flux, entropy, gravity, Einstein dynamics, GR/QFT,
  particles or phenomenology.

Presentation node classes, content hashes, AST addresses, amplitude coordinates
and path labels remain verifier/representation data.  The candidate physical
referent is at most the accepted groupoid orbit of complete finite typed
relational source arguments.

Nonbinding private Paper 14 definitions, literature notes and toy mathematics
may continue off-tree without reading or evaluating v5.  They have no
evidentiary status.  Authoritative Paper 14 construction waits for terminal
Paper 13 acceptance; Paper 15 waits for Paper 14's order and valuation gates.

## 13. Sole next event

At this pin snapshot no v5 source, mutation artifact, fresh case, output,
receipt, verification note, paper, bundle or hostile protocol exists.

The sole next event is two mutually blind read-only implementability audits of
these exact pin bytes.  If both accept, this pin and PLAN/LOG/STATUS may commit,
after which Stage B source/evidence construction is authorized.  If either
finds an exact contradiction, revise and redispatch one immutable snapshot.

No other scientific or publication action is authorized.
