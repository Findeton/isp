# APR Paper 12 — v5 executable-law weld pin

**Date:** 2026-08-19

**Status:** BINDING, OUTCOME-NEUTRAL, SCORER-ONLY FINAL ARCHITECTURAL REPAIR

**Parent failure:** `v16/note-apr-v4-verification-failure.md`

**Scientific authority:** NONE. This pin may repair only generic scorer
reachability. It may not change the APR fixture, physical law, scientific
coordinate, primary vocabulary or precedence, ontology ceiling, or v4
negative result.

## 1. Why v5 exists

V4's individual classical, quantum, influence, and finite algebra modules are
mathematically sound, but its integration layer can assemble neighboring
modules into a fictional one-law result. It treats dependency injection as
physics: matching roots, identifiers, dimensions, or separately green
residuals can stand in for actual typed composition.

This immediate failure is not specifically quantum and does not require
Barandes to diagnose. Swapping a selected transition without updating its
consumers, or joining a writer and rewrite on disconnected carriers, would
invalidate an ordinary classical engineering model. V5 repairs that basic
one-law typing defect in the verifier.

There is also a distinct, deeper scientific lesson. APR repeatedly began from
separately supplied regional laws, instruments, record writers, rewrites, and
comparison maps, and then attempted to weld their successful outputs. That
modular construction tacitly treats the theory as divisible into independently
meaningful pieces. An indivisible-law programme must reverse the order: begin
with one law on the complete configuration, and derive local descriptions and
intermediate representations as its shadows. V5 does not construct that
physical law; it only prevents the scorer from pretending that neighboring
modules already constitute one.

V5 is therefore an architectural repair, not another Boolean guard. The
scorer must consume one executable primitive law and recompute every claimed
property from that object. No module may submit a cached or precomputed green
result to the classifier.

This is the one bounded final attempt to certify APR's generic positive
ladder. If either independent post-freeze verifier finds another route by
which disconnected positive objects earn one-law or ontology promotion, no
v6 instrumentation repair is authorized. APR then terminalizes at the
independently secure scientific result:

```text
APR-BLOCKED-AT-BOUNDARY-GLUING
```

The real successor would then construct the missing physical law rather than
continue extending a synthetic classifier.

## 2. Immutable scope

Historical source commits, v1--v4 artifacts and failure notes remain
immutable. The following scientific objects cannot move:

- `v16/code/apr_core.py`;
- `v16/code/apr_fixtures.py` and its canonical payload;
- every APR pin/protocol/fixture law byte;
- the registered outcome words, precedence, qualifier, and ontology walls;
- the exact v4 output, receipt, and payload hashes;
- the permanent result-known exposure debt;
- the interpretation of `AB/BC` as a simultaneous regional-gluing diagnostic
  rather than a transition law.

After this pin freezes, implementation may modify only
`v16/code/apr_score.py`. Synthetic v5 law objects are scorer controls and may
not enter the APR scientific receipt.

## 3. The one executable primitive law

The generic positive classifier accepts one internal object of the following
semantic form:

```text
ExecutableLaw {
    carriers
    boundaries
    transitions
    valid_compositions
    instruments
    comparisons
    interventions
    readers
    regional_supports
    regional_rewrites
    tensor_and_gluing_structure
}
```

Every primitive has one exact type, source and target carrier/boundary, and
one immutable primitive identifier. Every derived map is built by resolving
and composing those primitives. The evaluator records an arithmetic/dataflow
DAG whose edges arise only when a primitive is an actual operand of the
derived operator, state, effect, instrument, comparison square, or regional
map.

The following never create a dependency edge:

- a root string;
- identifier equality;
- byte/hash equality;
- a declared `transition_reference_id`;
- dataclass/container membership;
- a detached calibration equation;
- an adjacent green module;
- a supplied expected output or residual.

Every reported residual must serialize:

1. the exact primitive IDs and typed occurrence paths it consumed;
2. the derived map/state/effect before comparison;
3. the independently recomputed expected object;
4. the exact residual;
5. the carrier/boundary composition trace.

Deleting or severing a consumed primitive occurrence while retaining all
names and hashes must make the derived object ill-typed or invalidate its
native gate.

Connectivity is computed only on the backward arithmetic/type slice of
classifier-consumed native coordinates. An auxiliary `uses T` calculation,
an identity composition whose result is discarded, or a connectivity-only
residual creates no admissible edge. Every admitted edge must serialize at
least one classifier-consumed native coordinate reachable from it, and
deleting that edge must change, invalidate, or make ill-typed that coordinate,
not merely a separate dependency check. Sharing a carrier establishes type
compatibility, not transition dependence.

## 4. Complete candidate-law bundles

Replace detached `LawCandidatePrimitive` and `ComponentTransitionUse` rows by
complete `ExecutableLaw` candidates. The frozen positive family contains
exactly two bundles, `L_I` and `L_X`, instantiated from one schema. They differ
primitively only at one registered two-state transition:

```text
T_I = I
T_X = X
```

Both transitions are exact reversible stochastic/permutation arrows on the
same typed carrier. For each candidate, the scorer reruns the complete chain:

```text
L_i
 -> composition and cuts
 -> gluing and tensor interfaces
 -> instruments and quantum representation controls
 -> comparisons
 -> influence/contact schedules
 -> regional support/rewrite controls
 -> composed calibration
 -> residual vector
```

The classifier may select a law only after both complete bundles pass every
native typing/consistency gate and exactly one has a zero selection residual.
The selection calibration is generated by composing the candidate's own
registered schedule from `delta_0`; no external prediction table is accepted:

```text
T_I delta_0 = delta_0
T_X delta_0 = delta_1
observed endpoint = delta_1
```

The unique candidate transition primitive is used through one typed occurrence
in the same schedule as the ontology control. With
`Q_tot=sum_j Q_j`, define

```text
P_i = (Theta tensor I_F) o W o Q_tot o T_i : G2 -> G3 x F
r_i = R_F P_i delta_0.
```

The two required moving objects are the map `P_i` and the separately typed
effect contraction `r_i`; they may not be duplicate interface rows. The
selection residual compares `r_i` with the one held-fixed observed endpoint
`delta_1`. No second copy of `T_i` and no candidate-specific recalibration is
permitted.

### 4.1 Real consumption rather than a declared star graph

A bundle owns one shared `T` primitive; every use is a distinct typed
occurrence node that resolves to that primitive. A component is a
`T`-consumer only when such an occurrence is an operand in that component's
native process/expression DAG.
Deleting `T` must make the calculation ill-typed. Replacing `T` and
recomputing must change a pre-quotient derived object or fail typing. A final
gauge-invariant result may remain equal only when the evaluator derives and
serializes the invariance from the changed pre-quotient object.

Components that are genuinely kinematic or `T`-invariant remain complete
bundle members but must not be connected directly to `T` by a fake reference.
Sharing a carrier or boundary establishes only type compatibility and never,
by itself, an inter-component dependency edge. A cross-component edge
requires a recomputed composition, restriction, tensor, cut equality, or
output-to-input feed whose derived object consumes operands from both
component subgraphs. Seven green modules on the same carrier remain
disconnected unless such a joint expression exists. The complete bundle's
typed expression/dependency graph must be connected through these actual
consumed operands.

The predeclared `P_i` and `r_i` objects must move under `T_I <-> T_X` in the
positive control, and the selection residual must move with them. This is not
a demand that every invariant module hash change.

### 4.2 Overlap and cut consistency

Restrictions of the complete bundle must agree on every registered overlap
and alternate cut. The finite `AB/BC` Markov selector remains scorer-only test
machinery. It may check that a complete synthetic bundle chooses one extension
within a printed finite family, but it is not the APR microscopic law and may
not be used to select the frozen APR extensions.

In the synthetic positive control, one bundle-owned global `ABC` object is the
operand; `AB` and `BC` are computed from it by registered restriction/marginal
maps. Equality of two separately supplied pair tables cannot satisfy this
gate.

## 5. One composable ontology chain

The ontology positive is evaluated from one carrier chain:

```text
G2 --T_i--> G2 --Q_j--> G2 --W--> G2 x F
   --(Theta x I_F)--> G3 x F
   --{new-support probe, delayed flag reader}--> outcomes
```

This chain is a sub-DAG of each complete `L_i`, not a separately constructed
ontology law.

The objects and equations are:

- `Q={Q_j}` is the typed conditioning instrument on `G2`; individual
  branches may be subnormalised, while the flagged total is stochastic;
- `W|s> = |s,s>` and the writer source is literally the conditioning output;
- the rewrite source is literally the writer target `G2 x F`;
- `Theta x I_F` maps the written input `|1,1>` to `|n,1>` in `G3 x F` and
  preserves the flag. It must be recomputed from the typed `Theta`, the
  output-flag identity, and the frozen tensor ordering; a supplied `6 x 4`
  matrix bearing that label does not count;
- the passive control is the typed map
  `(iota x I_F):G2 x F -> G3 x F`; it maps the same input into the old `G2`
  support and has zero response to the `n` effect;
- the new-support effect is compiled from the actual `G3 x F` output carrier;
- the delayed reader factors through the same output flag and recovers the
  written value after every licensed continuation. Every continuation has
  type `U_k:G3 x F -> G3 x F`, or is explicitly transported to that carrier
  through the rewrite. For every word `w` in the generated closed continuation
  set, the scorer recomputes
  `R_F U_w (Theta x I_F) W = R_F (Theta x I_F) W`;
- every response is obtained by typed composition of this chain.

A single joint successor

```text
J: G2 -> G3 x F
```

may replace `W` followed by `Theta x I_F` only if it is explicitly factored
and the writer and rewrite projections are both recomputed from `J`.

Equal dimensions, isomorphic but distinct carriers, common roots, matching
labels, equal output numbers, or membership in one container cannot establish
the ontology weld. If the writer codomain is not literally the rewrite domain
or connected by an explicit lawful typed transport, the maximum role is
`RECORD-WRITING-ON-FIXED-ALGEBRA`.

## 6. Issues are fatal

For every v5 evaluator, any nonempty `issues` field makes the affected
measurement invalid before role/classifier promotion. Duplicate, missing, or
ambiguous primitive IDs; unresolved occurrences; root mismatches; broken
carrier paths; incomplete index maps; or noncomposable words may not be
reported while a positive rung survives.

Demotion uses the already frozen earliest-compatible precedence. Refusal is
permitted only where an inherited interface already requires it.

## 7. Phase-bearing exact quantum control

V4's conjugation formula was textually correct but its witness could not
distinguish it from the wrong formula. V5 replaces that witness by the exact
Gaussian-rational histories

```text
H_0 = I/2
H_1 = i X/2
rho_+ = (1/2) [[1,1],[1,1]]
z = (1,i)
```

With the corpus convention

```text
D(h,h';rho) = Tr(H_h rho H_h'^dagger),
```

the derived Gram matrix is

```text
D = (1/4) [[1,-i],[i,1]],
```

with exact principal minors `1/4,1/4,0`. The correct coefficient orientation
uses

```text
A_bar = conjugate(z_0) H_0 + conjugate(z_1) H_1
      = (I+X)/2,
z^dagger D z = Tr(A_bar rho_+ A_bar^dagger) = 1.
```

The forbidden unconjugated construction gives

```text
A_wrong = z_0 H_0 + z_1 H_1
        = (I-X)/2,
Tr(A_wrong rho_+ A_wrong^dagger) = 0.
```

The division coefficient rows are

```text
(1,-i) -> L_+ = (I+X)/2
(1, i) -> L_- = (I-X)/2.
```

The scorer must recompute

```text
L_+^dagger L_+ + L_-^dagger L_- = I,
```

the isometric flag dilation, orthogonal flags, stable recovery, coherent
`p_+=1`, incoherent history sum `1/2`, and nonzero interference `1/2`.
Here interference means the port-weighted cross term. For
`c_+=(1,-i)`,

```text
I_+ = sum_(h != h') c_h conjugate(c_h') D(h,h';rho_+) = 1/2.
```

The unweighted operator
`H_0 rho H_1^dagger + H_1 rho H_0^dagger` is zero in this control and is not
the registered witness.
The independent `P^dagger P` versus `P^T P` phase control remains mandatory.

## 8. Mandatory attacks

Every attack changes a primitive/source object and is judged by recomputed
semantics, never an expected Boolean.

### 8.1 Law-bundle attacks

- **RESET-ONE:** replace only `T_X` by
  `R_1=[[0,0],[1,1]]`, preserving IDs and the `delta_0 -> delta_1`
  calibration. It must fail reversible/permutation typing, and cached old
  component outputs may not rescue selection or the joint primary.
- **NONINVOLUTIVE:** replace `T_X` by
  `[[0,1/2],[1,1/2]]`. It also maps `delta_0` to `delta_1` but is not the
  registered reversible transition. Every dependent object must be
  recomputed; the old v4 all-green result is forbidden.
- **STALE-CACHE:** replace the selected transition while retaining all
  previous measurement payloads. Cached payloads must be ignored or refused;
  the evaluator reruns from the mutated bundle.
- **CLONE-ID:** clone a byte-identical transition under a new primitive ID and
  rewire one component. The single-resolved-`T` schema must fail before
  connectivity is scored; connectedness alone cannot rescue a clone, and
  hashes cannot weld it.
- **SEVER-OCCURRENCE:** retain IDs and roots but remove `T` from one declared
  consumer's arithmetic DAG. That component must become ill-typed/invalid.
- **MOVE-DOWNSTREAM:** replace `T_I` by `T_X` in a lawful candidate and prove
  on that isolated mutated candidate that `P_i`, `r_i`, and the selection
  residual are recomputed and move. After this movement assay, the two-member
  family must refuse if the replacement has made its candidates duplicate.

### 8.2 Ontology attacks

- **ALIEN-CARRIER:** keep roots, dimensions, and matrices, but replace the
  rewrite source by a same-dimensional alien carrier. Typed composition fails
  and the role stops at or below record-writing.
- **ISOMORPHIC-DISCONNECT:** place writer and rewrite on distinct isomorphic
  carriers without an explicit transport. Isomorphism-by-shape does not weld.
- **REMOVE-BRIDGE:** delete `W -> Theta x I_F` composition. Rewrite promotion
  fails even if both separate maps remain valid.
- **DUPLICATE-ID:** duplicate one transition/continuation/arrow identifier.
  Nonempty issues invalidate the affected positive role.
- **LABEL-ONLY-JOINT:** replace the composed chain by the former separate v4
  objects plus a common root/action label. It must not promote.

### 8.3 Quantum source attack

- **CONJUGATION-SOURCE:** on a private off-tree copy of the final source,
  change only `coefficient.conjugate()` to `coefficient` in the `A_bar`
  construction. The same frozen primitive must change the operator-identity
  residual from `0` to `1` and fail the quantum positive rung. The output must
  serialize `D,z,A_bar,A_wrong`, both scalars, and the changed source SHA.

## 9. Classifier and receipt rules

The classifier consumes only freshly evaluated measurement objects generated
from one `ExecutableLaw`. It refuses plain mappings, forged nominal result
classes, detached residuals, cached component payloads, and arbitrary common
root strings.

The v5 scientific run must reproduce the v4 scientific output and receipt
byte-for-byte at new nonoverwriting paths. The receipt retains the oldest
permanent exposure token
`RESULT-KNOWN-BEFORE-V4-IMPLEMENTATION` so byte identity is possible; the v5
source-freeze note must additionally state that the result was known before
v5 implementation. No synthetic root, witness, residual, or candidate bundle
may appear in the scientific artifacts.

The v5 pin, final scorer, freeze-note, and post-freeze evidence hashes are
authenticated in the freeze note and independent verifier reports, and may be
checked internally before evaluation, but are not appended to the scientific
receipt. No new v5 provenance or exposure key may enter that receipt, because
its content is required to remain byte-identical to v4.

Any scientific artifact change is a hard kill because v5 has no authority to
change the APR branch.

## 10. Barandes/ISP scope correction

The exact canonical sentence is now:

> ISP's candidate ontology is one actual, law-sufficient relational
> configuration; its missing fundamental dynamics is an indivisible
> stochastic law of transition probabilities between complete
> configurations, conditioned at admissible division events/times.
> Hilbert/history machinery is representational, and APR's AB/BC table is
> only a simultaneous regional-gluing diagnostic—not that law.

`Division configuration` is forbidden terminology unless separately defined.
In Barandes, division is a legal conditioning event/time, not a special
configuration species and not automatically an ISP durable record. This
scope correction adds no point-free catalogue, geometry, backreaction,
actualisation, or physical law.

Barandes directly supports the configuration/transition/Hilbert-
representation distinction. Treating history and decoherence-functional
machinery as representational is ISP's scoped methodological choice, not a
theorem of his paper.

## 11. Freeze and independent verification

The binding sequence is:

1. Freeze and commit this pin before scorer edits.
2. During implementation, development self-tests are permitted but are
   advisory and disclosed. No fixture-loading mode may run.
3. Freeze final scorer bytes, publish their SHA, and stop editing.
4. From those exact bytes, run a fresh repository and true off-tree/no-`.git`
   fixture-denial self-test plus RESET-ONE, NONINVOLUTIVE, STALE-CACHE,
   CLONE-ID, SEVER-OCCURRENCE, MOVE-DOWNSTREAM, ALIEN-CARRIER,
   ISOMORPHIC-DISCONNECT, REMOVE-BRIDGE, DUPLICATE-ID, LABEL-ONLY-JOINT, and
   CONJUGATION-SOURCE.
5. Commit the scorer source and freeze note before any fixture evaluation.
6. Run the official fixture once to new absent v5 paths. Its output/receipt
   content must be byte-identical to v4.
7. Require two mutually independent post-freeze reconstructions. Neither may
   import scorer measurement functions or use scorer-provided expected
   answers. At least one must replay the changed-source conjugation attack;
   both must reconstruct the executable dependency DAG and composable ontology
   chain.
8. Before the committed final source freeze, an edit invalidates advisory
   evidence and requires a fresh pre-freeze run. After step 5 commits the
   scorer and freeze note, no scorer restart is authorized: any reproducible
   post-freeze counterexample to a mandatory semantic or integrity gate
   terminalizes APR under Section 12. A demonstrated bug in an independent
   verifier may be corrected only without changing frozen scorer bytes.

Self-test output must be machine-reconstructible: complete primitive bundles,
typed occurrence/dataflow DAGs, carrier paths, derived maps, exact residuals,
attack objects, source hashes, coordinates, and drop results. Named PASS rows
without the underlying objects are insufficient.

## 12. Certification and stopping rule

V5 passes only if:

- every inherited v4 exact gate remains green;
- all negative attacks demote or refuse for the registered semantic reasons,
  and `MOVE-DOWNSTREAM` exhibits the registered movement;
- changing the shared transition recomputes the full candidate bundle;
- no dependency arises from IDs, roots, hashes, or cached results;
- ontology promotion uses one literal composable carrier chain;
- the phase-bearing source mutant fails exactly;
- issues are fatal;
- v5 scientific artifacts are byte-identical to v4;
- both independent reconstructions are green.

If either verifier supplies a reproducible counterexample, confirmed by
independent replay, the instrumentation line ends. No v5b or v6
instrumentation repair is authorized. APR is terminalized at
`APR-BLOCKED-AT-BOUNDARY-GLUING`; the positive synthetic ladder is retired
rather than patched again.

Only after a green v5 audit may Paper 12 candidate drafting and its scientific
three-seat panel begin. Even then, the candidate may claim only the frozen APR
negative and surviving finite theorems. It may not claim the synthetic v5
law as physics.

## 13. Ontology ceiling

V5 can certify dependency plumbing, not construct the missing world. APR
still lacks one indivisible stochastic transition law on a complete relational
configuration, a physical atomless regional referent, generated dynamic
locality, causal/contact structure, matter--geometry backreaction, continuum,
metric, curvature, GR/QFT, particles, Hamiltonian, and actualisation. The real
physics successor must build one law from which regional `AB` and `BC` views,
record writing, and relational rewriting arise as typed shadows of the same
process.

The required physical starting form is schematically

```text
X = complete relational configuration
Gamma(X' | X) = one transition law, conditioned at admissible division events/times
```

Only after `Gamma` exists may the programme derive `AB` and `BC` restrictions,
division-level instruments, record facts, comparison transports, and
relational/geometry rewrites. If an intermediate `B` is not a lawful division,
no positive kernel through `B` and no factorisation through `B` may be assumed.

Barandes supplies this indivisible architecture, not the missing ISP physics.
His formulation starts with a fixed configuration space. ISP must still
construct the complete relational catalogue, allow its geometry-bearing
content to change, identify lawful divisions, select the numerical transition
law, establish locality and no-signalling for changing subsystem boundaries,
earn the geometry interpretation, and state actualisation. Indivisibility is
therefore a design constraint, not a law-selection theorem.
