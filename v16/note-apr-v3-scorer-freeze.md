# APR Paper 12 — v3 scorer source freeze

**Date:** 2026-08-19

**Status:** V3 SOURCE FROZEN BEFORE V3 FIXTURE RUN; PRIOR EXPOSURE DEBT RETAINED

**Scientific artifacts:** NO V3 OUTPUT OR RECEIPT EXISTS AT THIS FREEZE

## 1. Frozen source

The scorer-only v3 delta is frozen at:

```text
v16/code/apr_score.py
SHA-256 3284484d91f83440f9472ba9e9605b90065bfa0c30094b9225806ee01724fef8
10,260 lines
```

The implementation binds the v3 delta contract and its positive-path guard:

- `v16/note-apr-v3-delta-repair-pin.md`, SHA-256
  `88c2941f2fb4c90ed8476a5e814ab116b66369f23661dbacf4f89555ea6dd7f0`;
- `v16/note-apr-v3-delta-repair-pin-addendum.md`, SHA-256
  `22eb137fc5496a73ae46fadaaba26f98451dbc5f45c266826c135f7542f63e88`.

Only `v16/code/apr_score.py` moved. The APR pins, core, fixtures, protocols,
v1/v2 artifacts, and their failure records remain byte-immutable.

## 2. What the delta repairs

The frozen source implements:

- assignment-typed identities over the active B0--B3 process domain;
- the legal five-word process vocabulary with semantic positive classical and
  quantum packages and ingredient-drop demotions;
- the consistent M16 `c -> d` candidate-regional substitution and refusal of
  inconsistent projections;
- actual raw-syntax, identifier-hash, and finite-depth M15 oracle controls;
- serialized, recomputed L2/L4 finite analytical families;
- typed continuation closure or explicit `UNCONSTRUCTED`, rather than
  catalogue membership standing for composition;
- end-to-end formula, branch, record-word, cospan-port, and reader binding;
- separate measured `ontology_role` and postulated `ontology_candidate`
  fields, with a semantic four-rung role ladder;
- generated contact as an independent same-law capability and the registered
  contact-only classifier wall;
- the sole registered syntax-scoped qualifier and mandatory exposure fields.

The source does not add a process assignment, tensor, naturality square,
overlap selector, generated complete-probe compiler, causal schedule,
regional `Tau`, quantum regional law, or ontology. Synthetic positive packages
test scorer reachability only and are not APR evidence.

## 3. Pre-run verification

No v3 fixture-evaluating mode was invoked before this freeze. In particular,
neither the implementation worker nor root ran `--run`, `--mutant`, or
`--mutants-all` on the v3 bytes. Root independently repeated only source-safe
checks:

- SHA-256 and line-count measurement;
- Python AST parsing;
- `git diff --check`;
- static searches for forbidden v1/v2 artifact paths, the removed illegal
  process word, and the exact qualifier/exposure tokens;
- the synthetic-only `--selftest` from the repository and from a true
  off-tree/no-`.git` directory containing only `apr_score.py` and
  `apr_core.py`, under an alien working directory.

Both self-test runs are byte-identical, pass 27 named groups, and report
`scientific_fixture_evaluated:false`:

1. `exact-prefix-boolean-and-valuation`
2. `semantic-tree-partition-and-zero-port`
3. `mixed-constructor-refusal`
4. `finite-pushout-vs-validation-and-tagged-union`
5. `paired-four-coordinate-stable-null`
6. `three-replacement-closures`
7. `strict-record-reader-typing`
8. `typed-continuation-closure-and-absence`
9. `expression-driven-record-recovery`
10. `assignment-typed-B0-B3-identity-attacks`
11. `semantic-port-id-order-metamorphic`
12. `end-to-end-semantic-port-binding-and-negatives`
13. `candidate-regional-projection-and-relabel-canonicalization`
14. `m27-scoped-boolean-preparation-covariance`
15. `reachable-g2-leg-mutation`
16. `scope-indexed-active-roots`
17. `typed-provenance-path-and-sever`
18. `overlap-selector-nonuniqueness`
19. `semantic-classical-quantum-positive-and-drop-branches`
20. `ontology-role-ladder-demotion-and-candidate-invariance`
21. `result-neutral-capability-classifier-both-directions`
22. `result-neutral-boundary-coordinate-detection`
23. `repaired-analytical-mutant-constructors`
24. `transformation-descriptor-schema`
25. `mutant-registry-coverage`
26. `exact-qualifier-scope-and-exposure-fields`
27. `transactional-no-overwrite-publish`

## 4. Permanent exposure disclosure

This is a source-before-v3-run freeze, not a claim of pre-truth blindness.
During the v2 repair, the implementation worker accidentally evaluated M01
and the complete mutant battery before the v2 source commit. That result was
known before v3 implementation. The v3 receipt and transcript must therefore
carry exactly:

```text
blinding_status: RESULT-KNOWN-BEFORE-V3-IMPLEMENTATION
exposure_debt: PERMANENT-PREFREEZE-M01-AND-ALL-MUTANTS-EXPOSURE
v3_source_frozen_before_v3_run: true
```

Independent black-box reconstruction remains mandatory. This freeze cannot
restore mutual blindness or erase the incomplete v2 exposure transcript.

## 5. Authorized next event

After this source and note are committed, the only scientific event is one
transactional run from those committed bytes to the previously absent paths:

```text
v16/apr_output_v3.txt
v16/apr_receipt_v3.json
```

The artifacts must replay byte-identically from repository-root, alien-CWD,
and true off-tree/no-`.git` environments. The independent verifier must then
reconstruct all v2-green checks, all v2 failures, every v3 delta gate,
descriptor semantics, positive process branches, identity attachments,
ontology-role demotions, contact precedence, scope strings, and exposure
metadata without importing scorer measurement functions. Candidate drafting
and the three-seat panel remain prohibited until that audit closes.

## 6. Ontology ceiling

The measured APR construction remains at most `STATIC-RESPONSE`. The proposed
ontology--one actual partial compatible web of durable relational facts--is a
postulate, not a scorer result. Prefix words, Boolean algebras, matrices,
cospans, and instruments remain representations. This freeze establishes no
physical atomless regional referent, joint process, causal order, geometry,
matter--geometry backreaction, metric, curvature, continuum, GR/QFT,
particles, Hamiltonian, or actualization.
