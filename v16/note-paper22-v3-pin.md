# Paper 22 v3 completion pin — bound-apparatus restoration and exact partial visibility

Date: 2026-08-22

Status: **FROZEN BEFORE CONSTRUCTION — AUTHORIZED ONE-STRIKE SUCCESSOR**

This is the separately motivated and explicitly user-authorized successor
unit required by the Paper 22 v2 adjudication (#301, Section 9) and by the
no-automatic-repair rules of both predecessor pins. Authorization was given
by the user on 2026-08-22 after #301 and ledger #302. Nothing else in this
document is authorized by that adjudication.

## 1. Question

Can the twice-adjudicated homogeneous-source structural instrument be
completed, without any new physical hypothesis, by exactly two repairs?

1. **Restoration.** Restore the bound uniform fine-seed apparatus of the
   original Paper 22 construction verbatim — the per-cross-pair uniform
   `[25]` seed, its product law, its Householder purification, and the
   reversible witness computations that read seeds as data — on the
   unanimously confirmed homogeneous-source domain of Paper 22 v2.
2. **Exact partial visibility.** State before construction, and then prove,
   the general partial-record interference law with complex environment
   overlap, replacing the false `Re(v)` sentence of the rejected candidate;
   register its consequence at `v = i`, `phi = pi/2` as a mandatory
   regression control.

Every adjudicated v2 survivor is inherited unchanged. No coarse-bin
construction, seed bias, carrier change, reader-statistic change, phase
reconvention, coefficient change, or fitted parameter enters. The target is
the complete triggered local instrument: fiberwise reversible query on the
bound apparatus, dependent commit, exact local mode law including the full
complex-visibility family. It remains conditional physics: no occurrence,
activity, root, chronology, dimension, geometry, or actuality law is
authorized or claimed.

## 2. Bound evidence

All hashes below were verified on disk at freeze time.

| object | role | ordinary SHA-256 |
|---|---|---|
| `v16/paper-13d-typed-executable-gamma.md` | terminal conditional law; atomic sorts, tensor, simultaneous fusion, child kernels, exact seed law and beta map | `3b91766fe269e8145766f4622f99e9009c3270530670119735e6045a473de5c9` |
| `v16/paper-21-exact-structural-interferometer.md` | exact interferometer; Sections 4.2, 7 supply the correct `gamma`-overlap law and visibility formulas | `ca0f709b00906971c6aac2b25b12fea11f168411ab2f440ccc49d982aab4ba80` |
| `v16/note-paper22-reversible-structural-instrument-pin.md` | v1 local-instrument contract | `de32c02ee1be613eef4a867dadf9bc1c84fc8ed492b764f7545bb54fb91a5ae4` |
| `v16/paper-22-reversible-structural-instrument.md` | v1 candidate; Sections 5.1–5.3 and 6 supply the restored apparatus bytes | `6d75a072fb3c51c5c267448fd329895f94cd4f9ee4ba4d96ea9660be80c1c6b7` |
| `v16/note-paper22-reversible-structural-instrument-adjudication.md` | v1 adjudication (REVISE, heterogeneous-source totality) | `c261520aa142bf07a489f87cd0364628f094794c7523c03d8ba3dde05d824a07` |
| `v16/note-paper22-reversible-structural-instrument-v2-pin.md` | v2 homogeneous-source repair contract | `a4c1c2ecd10edad73ed64b12f699c09d7cfd169d4cd264939990589554693627` |
| `v16/paper-22-reversible-structural-instrument-v2.md` | v2 candidate; Sections 2–4, 7–14 survive; Sections 5.1, 6 fail and are replaced | `30340295ccd5f8371a9020cb76c0a93cc24ab14cbbf78f05c01a85ca5ce86468` |
| `v16/note-paper22-reversible-structural-instrument-v2-construction.md` | v2 construction note | `e90bdd14ca04742bb09eb3c9ec928b4aea04ff6429b0896158ce83170280a0ae` |
| `v16/note-paper22-reversible-structural-instrument-v2-review-protocol.md` | v2 review protocol (pattern reused here) | `88a2609988628e8e9fe1ad2c96a0b65a9cf230a2750feab2207bca5ebfcfd30e` |
| `v16/review-paper22-reversible-structural-instrument-v2-category.md` | v2 Seat C report | `880f1135a2d781e1978c9f38d0ffb056bed2825c8bb200ff4b184d5eb0bf9cba` |
| `v16/review-paper22-reversible-structural-instrument-v2-probability.md` | v2 Seat P report | `98156514295e40a35d987597ed6b78f1cb6d67a2e915707eca928091f898d0b5` |
| `v16/review-paper22-reversible-structural-instrument-v2-quantum.md` | v2 Seat Q report | `3868771ac7b517fef90898b65f12ee203b70e8131f2d3d5475185e331db10cfb` |
| `v16/note-paper22-reversible-structural-instrument-v2-adjudication.md` | v2 adjudication (REVISE; names the two defects repaired here) | `31c0d7ca973ad62c188e946b2d10433705c9c6644ab1bd0ea8579a963e706c04` |
| `v16/note-paper23-covariant-opportunity-fusion-bootstrap-pin.md` | downstream consumer; binds a successor only after acceptance | `1a9bd4d1d624d4cd8f7cf553420c453aff67dd732fbae02ecf7e6eb0927bea85` |

## 3. Immutable scientific core

The following are frozen. A change to any of them is outside this pin.

### 3.1 Adjudicated survivors (inherited verbatim)

From the v2 adjudication, all unanimous:

- the homogeneous-source coproduct groupoid
  `Src_hom^{>=2} = coprod_s Src_s^{>=2}`, its membership predicates, and the
  refusal of heterogeneous active pairs and sort-changing arrows before any
  evaluation;
- the total typed tensor/simultaneous-fusion child pair with carried
  spectators and equivariant kernels;
- the source-indexed dependent commit isometry and coproduct accessible
  output with no dormant unchosen child;
- positive-to-positive restriction naturality with seed marginalization;
  branchwise zero/one-active restriction retaining the stable record
  `(m_R)` without target identification or a new mode opportunity;
- external-tensor-only composition; no same-source multi-mark object;
- Theorems 14–16 level results: local process plurality at neutral tensor
  input, and activity/root noninheritance (arbitrary `(activity, root)`
  pairs share the same triggered instrument).

### 3.2 Exact numerical anchors

$$
B=\frac1{25}\begin{pmatrix}9&16\\16&9\end{pmatrix},
\qquad
R=\begin{pmatrix}3/5&-4/5\\4/5&3/5\end{pmatrix},
$$

$$
C_\phi=\frac1{625}
\begin{pmatrix}
337-288\cos\phi&288(1+\cos\phi)\\
288(1+\cos\phi)&337-288\cos\phi
\end{pmatrix},
\qquad
B^2=\frac1{625}\begin{pmatrix}337&288\\288&337\end{pmatrix},
$$

$$
K_\phi=C_\phi B^{-1}=\frac1{175}
\begin{pmatrix}
63+288\cos\phi&112-288\cos\phi\\
112-288\cos\phi&63+288\cos\phi
\end{pmatrix},
\qquad
-\frac7{32}\le\cos\phi\le\frac7{18},
$$

neutral tensor-input odds `Pr(T)=49/625`, `Pr(F)=576/625`, and the gauge
statement that `R` is unique only up to diagonal input phases, diagonal
output phases, and simultaneous exchange of the two physically typed modes.
`K_phi` and its interval remain certified at full-coherence scope only; this
pin claims no generalized continuation.

## 4. Apparatus inventory clause (repair 1)

Every apparatus object below is bound by name, role, and content to the v1
candidate, Sections 5.1–5.3. The construction may not alter any of them.

For source `X` with unordered cross-active pair set `P_X`:

1. **Seed space and law.** `Xi_X=[25]^{P_X}` with uniform product law
   `mu_X(xi)=25^{-|P_X|}`; independence across pairs is the accepted Paper
   13D cross-pair seed law (Section 6.1/6.3 and Section 10 there).
2. **Purified seed state.**
   `|Omega_X> = 5^{-|P_X|} sum_{xi in Xi_X} |xi>` — exact because
   `sqrt(25^{-|P_X|}) = 5^{-|P_X|}` — prepared from the blank `|0_Xi>` by
   the Householder reflection `S_X = I - 2|v_X><v_X|`,
   `|v_X> = (|0_Xi> - |Omega_X>)/sqrt 2`, with
   `S_X|0_Xi> = |Omega_X>` and `S_X^2 = I`.
3. **Open-query reader statistics.** The complete middle reader distinguishes
   tensor and fused witnesses on the fine `[25]`-indexed register while the
   query is open. No coarse statistic is introduced.
4. **Witness computations.** Reversible injective basis maps on
   `(complete source, fine seed, blank query)`:
   `C_{T,X}: |X,0_Q> -> |X,T(X)>` and
   `C_{F,X}: |X,xi,0_Q> -> |X,xi,F(X,xi)>`, extended by inverse pairing;
   controlled composition into `U_{Q,X}`; the fused-witness projector
   `Z_{Q,F}` defined by the complete query target type; kickback
   `U_{Q,X}^{-1} P_phi U_{Q,X} = D_phi (x) I`; exact query closure returning
   source, seed, witness, complement, and environment fields to blank.

**Explicit bars.** The rejected substitutions of v2 are named so they cannot
recur silently: no biased two-state carrier `|sigma> = (3/5)|0>+(4/5)|1>`;
no coarse bond bit; no color-dependent repartition of `[25]` inside the
instrument — the nine-versus-sixteen split belongs exclusively to the
accepted Paper 13D beta map and is read, never redefined; no undefined
fine-to-coarse isometry; no change to open-query reader statistics, phase
convention, or any coefficient. Because no coarse binning occurs, no
coarse-bin reversibility proof is required; the fine seed is retained and
uncomputed exactly as in v1.

### 4.1 Four gates for the restoration

**Referent.** Every restored object is a byte-level reuse of the v1
construction, itself built only from Paper 13D sorts, tensors, fusion
generators, kernels, and the accepted seed law.

**Necessity.** The v2 adjudication found the substituted carrier outside the
authorized delta and the resulting reversible query unconstructed; only the
bound apparatus can carry the pinned query coordinate.

**No smuggling.** Membership, seeds, witnesses, and readers are fixed before
any mode is sampled; none inspects a mode probability or downstream
estimator.

**Discriminator.** The construction must exhibit: the uniform-seed
purification acting on an admitted pair; refusal of a purported biased-seed
substitution as outside the pin; a mid-query seed trace detected by the
complete reader; and exact closure erasing every seed address.

## 5. Partial-visibility law, stated before construction (repair 2)

Let `|e_T>`, `|e_F>` be all residual witness, record, and environment states
immediately before recombination, with overlap `gamma = <e_T|e_F>`,
`|gamma| <= 1`, and define

$$
q_\phi=\operatorname{Re}\!\left(\gamma\,e^{i\phi}\right).
$$

The complete reduced probe law, for arbitrary complex `gamma`, is

$$
C_{\phi,\gamma}
=\frac1{625}
\begin{pmatrix}
337-288\,q_\phi&288(1+q_\phi)\\
288(1+q_\phi)&337-288\,q_\phi
\end{pmatrix}.
\tag{V}
$$

Its consequences are registered now, before construction, and must appear
as named results:

1. **Regression control (mandatory).** At `gamma = i`, `phi = pi/2`:
   `q = -1`, hence `C_{pi/2,i} = I_2`: every input mode returns
   deterministically from its own detector. The rejected candidate's rule
   `Re(v) = 0` instead left the law phase-independent at `B^2`; that rule is
   false and forbidden. (The `Re(v)` rule predicts `C = B^2` at every phase
   for `Re(v)=0`; the true law at this control point is `I_2`.)
2. **Endpoints.** `gamma = 1` gives `C_phi`; `gamma = 0` gives `B^2`
   (stable record or orthogonal classical erasure trace); complete
   reversible erasure of every record copy restores `C_phi`. Along
   `gamma = e^{i theta}` the law is `C_phi` with `phi` shifted by
   `+theta`: a pure fringe displacement, unit visibility on both rows.
3. **Visibility audit.** Scanning `phi` at fixed `gamma`: the
   `337-288q`-row has visibility `(288/337)|gamma|` and the
   `288(1+q)`-row has visibility `|gamma|`
   (Paper 21, Eqs. 20–21, labels preserved).
4. **Scope.** Columns of (V) sum to one and all entries are nonnegative
   for every admissible `gamma` — a genuine stochastic matrix.
   The `K_phi` continuation and its positivity interval remain at
   `gamma = 1` scope; no generalized continuation is claimed.

### 5.1 Four gates for the visibility law

**Referent.** `gamma` is the overlap of residual apparatus states whose
existence and typing are forced by the no-hiding theorems; no new physical
object is introduced.

**Necessity.** The v2 adjudication produced the `v = i` counterexample
against the printed `Re(v)` sentence; the general complex law is the
smallest replacement containing both endpoint regimes and the
counterexample.

**No smuggling.** (V) is derived from the fixed route amplitudes and the
residual overlap; no fitted exponent, temperature, deformation, or channel
phase enters, and nothing is selected from downstream output.

**Discriminator.** The construction must distinguish: coherent `gamma = 1`;
orthogonal stable record `gamma = 0`; same-ray environment
`gamma = |gamma| e^{i theta}` producing a pure fringe shift; and the
registered `gamma = i` control — each with a different printed law.

## 6. Scope engravings (unchanged walls)

Even full acceptance constructs only a triggered local instrument on
homogeneous source fibers. Unconstructed and unclaimed: why a source exists;
which active family is selected; how often the instrument occurs; any joint
law over sources; global dilation or source superposition; same-source
multi-mark composition; simultaneous-fusion algebra or Frobenius–Perron
odds; onset multiplicity; complete chronology; varying-size ensemble;
dimension, Lorentzian signature, metric, curvature, backreaction, Einstein
dynamics, continuum, QFT; physical regional referent; actualization. The
fiberwise `W_X` stays fiberwise. Staged fusion remains a distinct history
from one simultaneous n-ary fusion. Paper 23 may bind an accepted v3 only
through its own fresh hash-bound freeze.

## 7. Required theorems

In this order:

1. Domain decidability and refusal (Proposition, inherited).
2. Total typed child pair (inherited).
3. Finite reversible-erasure obstruction and quantum no-hiding (inherited).
4. Seed preparation naturality on the bound apparatus (restored).
5. Exact query closure (restored, fine seeds).
6. Phase-kickback identity (restored).
7. Minimal calibrated lift `R` with gauge statement (anchor).
8. Coherent probe law `C_phi` (anchor).
9. **General partial-visibility law (V)** with the registered consequences —
   the repaired coordinate.
10. Fiberwise commit isometry, joint law, normalization, child recovery,
    no dormant child (inherited).
11. Naturality of the complete instrument including seed purification
    (inherited form, restored content).
12. Positive restriction naturality and branchwise degenerate restriction
    (inherited).
13. External tensor composition (inherited).
14. Local process plurality and activity/root noninheritance (inherited).

## 8. Mandatory hostile controls

All v2 controls apply where still meaningful, including: heterogeneous pair
refusal; sort-changing arrow absence; empty/one-active refusal; type
equality refusals for formal units; degenerate restrictions creating no
opportunity and dropping no record; staged-versus-simultaneous distinction;
unindexed exhaust sharing; exhaust orthogonality inferring source odds;
spectator sort moving `C_phi`; route-dependent residue after closure;
query renamed as accessible erasing fusion; dormant child; naked label
swaps; decoder swaps; post-hoc phase selection; mutation of any anchor;
activity/root inference; chronology/dimension/metric/gravity/actuality
inference. Additionally:

1. Predict (V) at `gamma = i`, `phi = pi/2` and along `gamma = e^{i theta}`;
   the answers must match the registered values (`I_2` at the control point)
   and the pure-fringe-shift family.
2. Any sentence reducing the interference term to `Re(v)` is a defect.
3. A biased or coarse seed carrier anywhere in the construction is outside
   the pin and must be reported as such.
4. A seed traced at recombination must be caught by the closure theorem.
5. Visibility values `|gamma|` and `(288/337)|gamma|` must follow from (V),
   not be asserted separately.

## 9. Kill conditions

- Any immutable anchor of Section 3.2 moves, or any survivor of
  Section 3.1 is weakened.
- Any apparatus object of Section 4 is altered, replaced, or supplemented
  (including any coarse binning).
- Any visibility sentence equivalent to `Re(v)` appears in the delivered
  construction.
- A new independent semantic counterexample survives review. **One strike:
  the Paper 22 line then terminates permanently; v3 becomes the last
  attempt; fragments remain result-neutral at their adjudicated scopes.**

## 10. Outcome product

```text
P22V3-HOMOGENEOUS-SOURCE-GROUPOID:        CONSTRUCTED | UNCONSTRUCTED
P22V3-TOTAL-TENSOR-FUSION-CHILD-PAIR:     CONSTRUCTED | UNCONSTRUCTED
P22V3-FIBERWISE-REVERSIBLE-QUERY:         CONSTRUCTED | UNCONSTRUCTED
P22V3-FIBERWISE-COMMIT-INSTRUMENT:        CONSTRUCTED | UNCONSTRUCTED
P22V3-EXACT-LOCAL-MODE-LAW:               CONSTRUCTED | UNCONSTRUCTED
P22V3-PARTIAL-VISIBILITY-LAW:             CONSTRUCTED | UNCONSTRUCTED
P22V3-POSITIVE-RESTRICTION-NATURALITY:    CONSTRUCTED | UNCONSTRUCTED
P22V3-DEGENERATE-BRANCHWISE-RESTRICTION:  CONSTRUCTED | UNCONSTRUCTED
P22V3-EXTERNAL-TENSOR-COMPOSITION:        CONSTRUCTED | UNCONSTRUCTED

P22V3-SAME-SOURCE-MULTIMARK-COMPOSITION:  UNCONSTRUCTED
P22V3-SIMULTANEOUS-FUSION-ALGEBRA:        UNCONSTRUCTED
P22V3-ACTIVITY-ROOT-LAW:                  UNCONSTRUCTED
P22V3-PHYSICAL-REGIONAL-REFERENT:         UNCONSTRUCTED
P22V3-CHRONOLOGY-DIMENSION-METRIC-GR:     UNCONSTRUCTED
P22V3-ACTUALIZATION:                      UNCONSTRUCTED
```

The product is componentwise, not a ladder. Acceptance requires every
constructed coordinate to hold simultaneously with both repaired
coordinates (`REVERSIBLE-QUERY`, `PARTIAL-VISIBILITY-LAW`) among them.

## 11. Construction and evidence discipline

This pin authorizes nothing until committed at this exact path. After that
freeze, the only construction-stage writable paths are:

```text
v16/paper-22-reversible-structural-instrument-v3.md
v16/note-paper22-reversible-structural-instrument-v3-construction.md
```

No code, evaluator, fixture, or generated number is authorized or needed;
exact arithmetic may be checked independently but cannot substitute for the
typed proofs. The construction note must bind: the frozen pin hash, all
input hashes, theorem/control coverage against Sections 7–8, line and byte
counts, a no-code statement, absence of downstream artifacts, and a
path-scoped whitespace/hash check.

## 12. Review protocol

After construction freezes, one three-seat blind delta review, repo
read-only, each seat rebuilding independently from the published prose:

1. **Seat C — category/source:** groupoid, child pair, naturality,
   restriction, composition; regression sweep of every v2 survivor.
2. **Seat P — probability/instrument:** anchors, `C_phi`, law (V) with all
   four registered consequences, commit normalization, child recovery.
3. **Seat Q — quantum/no-hiding:** closure, kickback, no-hiding, dormant
   child, visibility controls including the `gamma = i` regression.

Each report: verdict ACCEPT / ACCEPT-WITH-FIXES / REJECT, findings ranked
most-severe-first with severities, replacement sentences verbatim. The two
repairs and the `gamma = i` control are mandatory regressions. Adjudication
follows; only the adjudicator confers terminal status in the ledger.

## 13. Authorization record

The v2 adjudication (Section 9) required any future attempt to be separately
motivated and explicitly authorized, to begin from a new committed pin, to
preserve the uniform fine seed or justify a genuinely new apparatus, and to
state the complex visibility law before construction. All four conditions
are met: motivation is the completion of a twice-confirmed instrument for
use as the Paper 23 local input; authorization was given by the user on
2026-08-22; this pin is the new committed freeze; Section 4 preserves the
fine seed; Section 5 states the visibility law first.
