# Paper 21 exact structural-interferometer pin

Date: 2026-08-21

Status: **FROZEN BEFORE EXACT EVALUATION — RESULT NEUTRAL**

## 1. Purpose

This protocol evaluates the smallest exact Barandes-Feynman structural
interferometer. It asks whether two operationally inequivalent structural
routes can belong to one common parent, be filtered and recorded, return to
one common final interface, and display a direct indivisible law that differs
from every recorded classical division.

The experiment is a mathematical physics construction. It is not an empirical
laboratory run and does not claim that the accepted Paper 13D law already
contains the required structural operations.

## 2. Bound inputs

| artifact | role | ordinary SHA-256 |
|---|---|---|
| `v16/paper-13d-typed-executable-gamma.md` | accepted indivisible calibration | `3b91766fe269e8145766f4622f99e9009c3270530670119735e6045a473de5c9` |
| `v16/paper-20-predictive-structural-parent.md` | common-parent typing | `64111d7764bf70984b959e00bc1da30ad0d6c3ae7b5c6d51b94227ad2f5c35e6` |
| `v16/note-paper20-predictive-structural-parent-adjudication.md` | internal scope | `18ea13e2bd609439d2cb4e2a03c1f0c4f0fbf7e37e2e38bba15272570001ac14` |
| `v16/note-paper21-structural-recombination-investigation.md` | discriminator contract | `6febe138678668aa32ebb3b8ceb1b61966dee042f6998b23d8c16cc64e090379` |

Paper 20 is internally, not independently, accepted. This protocol cannot
promote it.

## 3. Route meaning

The two route tags are

```text
T = tensor-preserving structural transformation
F = fusion-changing structural transformation
```

They must differ under a complete operational witness while the route is
open. A renamed abstract bit does not pass.

The experiment may use an enlarged reversible dilation. If a route changes a
structural witness from `w0` to `w_T` or `w_F`, a controlled inverse must
return the witness and every environment field to one common state before
recombination. Any surviving route record is retained and changes the
predicted context.

## 4. Exact mixer and phase control

The candidate shared mixer is the accepted exact matrix

\[
 R=\begin{pmatrix}3/5&-4/5\\4/5&3/5\end{pmatrix}.
\]

The route phase control is

\[
 D_\phi=\operatorname{diag}(1,e^{i\phi}).
\]

This is a secondary amplitude representation of the candidate complete
stochastic family. The experiment must also report every final law directly
as an ordinary stochastic matrix.

Freeze the three phase settings

\[
 \phi\in\{0,\pi/2,\pi\}.
\]

No phase is selected after inspecting an output.

## 5. Filter contexts

Use one common preparation and one complete final outcome space

```text
{0, 1, loss}.
```

Evaluate:

1. `AB`: both routes open;
2. `T`: only the tensor route open;
3. `F`: only the fusion route open; and
4. `empty`: both routes blocked.

Blocked probability goes to `loss`; it is never removed by renormalization.

For each detected output `z`, compute

\[
 I_2(z)=p_{AB}(z)-p_T(z)-p_F(z)+p_\varnothing(z).
\]

## 6. Record contexts

Evaluate separately:

1. `unrecorded`: no route information remains after exact structural
   uncomputation;
2. `recorded`: a stable orthogonal route record remains;
3. `classically-erased`: the record is deterministically forgotten after it
   was written; and
4. `coherently-unrecorded`: the record-writing interaction itself is exactly
   reversed before recombination.

The protocol must not equate the third and fourth contexts.

## 7. Barandes factorization control

For the unrecorded direct law `C` and the recorded intermediate law `B`, test
every positive normalized `K` in

\[
 C=KB.
\]

When `B` is invertible, compute the unique candidate `CB^{-1}` exactly.

The recorded final law must be the positive classical composition `B^2`.

## 8. Required exact outputs

The report must include:

1. every filter-context distribution including loss;
2. every `I_2` value at all three phase settings;
3. the unrecorded, recorded, classically erased, and coherently unrecorded
   laws;
4. the exact direct-minus-recorded residual;
5. the exact unique continuation candidate;
6. detector visibility;
7. a partial-route-information formula;
8. normalization checks;
9. presentation and label-swap checks; and
10. the current-Gamma instantiation audit.

## 9. Hostile controls

1. Route tags have no operationally different transformation.
2. Fusion has no reversible dilation.
3. An environment retains route information after claimed uncomputation.
4. Route blocking changes the source.
5. Blocked probability is renormalized away.
6. Different final readers are used across contexts.
7. The phase shifter changes the structural witness directly.
8. A phase is chosen after output inspection.
9. A stable record remains in the unrecorded context.
10. Deterministic erasure is called coherent uncomputation.
11. Recorded probabilities do not equal the positive classical sum.
12. A positive continuation exists on the complete carrier.
13. A hidden carrier is introduced after a negative continuation is found.
14. Separate amplitude tables are fitted to each context.
15. Swapping `T` and `F` changes invariant predictions.
16. A route is a serialization or loop index.
17. The exact calibration is renamed as whole-process evidence.
18. A one-shot apparatus law is called a cosmological selector.
19. The experiment is claimed to select its initial structural state.
20. The result is promoted to chronology, dimension, metric, or gravity.

## 10. Outcome coordinates

```text
P21-ABSTRACT-STRUCTURAL-INTERFEROMETER-UNCONSTRUCTED / CONSTRUCTED
P21-FILTER-INTERFERENCE-UNCONSTRUCTED / CONSTRUCTED
P21-STOCHASTIC-NONDIVISION-UNCONSTRUCTED / CONSTRUCTED
P21-STABLE-RECORD-CLASSICALIZATION-UNCONSTRUCTED / CONSTRUCTED
P21-ERASURE-RECOHERENCE-SEPARATION-UNCONSTRUCTED / CONSTRUCTED
P21-PARTIAL-COHERENCE-LAW-UNCONSTRUCTED / CONSTRUCTED
P21-CURRENT-GAMMA-STRUCTURAL-ROUTES-UNCONSTRUCTED / CONSTRUCTED
P21-CURRENT-GAMMA-REVERSIBLE-STRUCTURAL-DILATION-UNCONSTRUCTED / CONSTRUCTED
P21-CURRENT-GAMMA-STRUCTURAL-INTERFERENCE-UNCONSTRUCTED / CONSTRUCTED
P21-STRUCTURAL-INITIAL-STATE-UNSELECTED / SELECTED
P21-P17-GLOBAL-GATE-CLOSED / OPEN
P21-METRIC-UNCONSTRUCTED / CONSTRUCTED
```

An exact abstract success with a failed current-Gamma instantiation is a valid
complete result.

## 11. Permanent walls

No output selects an actual outcome, root state, activity law, dimension,
signature, geometry, metric, curvature, gravity, or continuum.

No code, numerical fit, random sampling, Paper 17 run, or post-hoc parameter
selection belongs to this exact experiment.
