# Independent hostile review of SRW — operator, representation, and quantum consistency seat

Date: 2026-08-18  
Seat: O — operators, representation, and quantum consistency  
Target: v16 Paper 4, `Support--rewrite weld and reciprocal creation`  
Verdict: **ACCEPT-WITH-FIXES**

## 1. Executive finding

The candidate's exact finite mathematics is reproducible.  In particular, it
really does establish two useful negative/conditional results:

1. an anonymous linear map does not determine either its kinematic support
   grammar or its persistence span; and
2. for a declared isometric creation map, the adjoint is the canonical exact
   eraser on the reached image, while neither that fact nor all-input
   conservation selects probabilities for mutually exclusive recorded
   successors.

The `12 -> 2` dictionary census is also exact.  The two survivors are blind to
the candidate's registered current signature and are distinguished by the
registered graph-local and persistence futures.  That conclusion is physical
only relative to that finite, declared continuation family.  No law in the
candidate generates or closes the family of futures.

The positive bundle headline is one construction step ahead of the evidence.
The program constructs a typed pair `(rewrite span, transport)` and checks one
relabeling square, but it never constructs a global block-sparse instrument or
a two-rewrite composite, and hence does not prove coherent composition across
changing configuration carriers.  Its recurrence gate likewise inserts the
same coupling at the second occurrence and then verifies equality; it is a
conditional locality/universality axiom, not a selection theorem.  These are
material scope defects, but they do not destroy the independently exact
map-only refutation, finite dictionary result, or reciprocal-image theorem.

I therefore recommend `ACCEPT-WITH-FIXES`: retain the first finding; retain the
second only with its finite declared-future quantifier; demote the third from a
compositional "weld" to a single-step typed bundle construction; and retain the
fourth only as conditional erasure on the reached image with history weights
free.  No claim about cross-sector coherence, complete instruments,
no-signalling, steering, EPR, gravity, or the joint successor law is earned.

## 2. Independence, chronology, and authentication

I read the frozen protocol and all required SRW target materials before doing
substantive work.  I did not read or consult another SRW report.  My independent
reconstruction is a separate standard-library `Fraction`/Gaussian-rational
implementation at `/private/tmp/srw_operator_independent.py`; it does not
import either candidate implementation.

The chronology authenticates a real pre-registration and repair sequence:

- the pin precedes the core;
- the original core and fixture freezes precede the first refusal;
- the first candidate attempt refuses on an exact-scalar construction path;
- the repair is frozen before the repaired core is generated;
- the repaired candidate artifacts precede the repaired verification note.

The repaired source change closes constructor coercion and provenance; it does
not alter the declared mathematical objects or the scientific outputs.

The frozen bytes at candidate commit
`cd3ad8f61c244ef8703a736bc68cdcbcdb397e30` reproduce exactly:

| artifact | frozen and observed SHA-256 |
|---|---|
| repaired core | `dd902c37375f87185f693f8b1e4b22ba3ddeaf9de5641e5d7d951cbba1d3c585` |
| scorer | `73dfb580b056b4ed2cee511542684bdbe9943633bd2c9888fd4934cd521742f9` |
| result-free fixture | `e40650f04c60635e68fd91938dbba201afec6e426c2e1cfaa0b4f4d8dcefd2e3` |
| transcript | `e52c5573c0c784a83419de368286152302bbbfe02cf0c12fb132bd568f0c8695` |
| receipt | `c9b036c9d6382bfd8f1402fe5eee39d3a362842b82b1690e28e5a0130a4d5675` |
| Paper 4 | `f61dde79e5fc0e10db1e5dbe13dec25dceaff9842d5e0c5c06ba2ae90eb4bcae` |
| repaired verification | `bb270d86ddb79b9f2dae1f4275f6ce2e5025a32f11b899efb4d18df6046a7eb1` |

A no-`.git` archive replay in `/private/tmp/srw-operator-replay-v16`, run from
an alien working directory, regenerated byte-identical transcript, receipt,
and paper artifacts.  The result indices are independent and all receipt seals
recompute.  The fixture contains no result-bearing field.  The candidate
reports 37 green gates and 27 registered mutant deaths; the source/receipt
counts agree.  I additionally ran all lens mutants relevant to this seat:
dictionary drop, swap-call gauge, future-reactivation drop, support equality,
internal-call gauge, internal-reactivation drop, recurrence-site drift,
angle-lock constructor, reciprocity assume/remove, phase-frame break,
runtime-scalar leak, and holonomy flatten.  Each exited nonzero.

The repaired registered paths are closed in `Q(i)`: the source AST has no
floating-point literal, matrix/fixture parsing rejects floating inputs, and
the direct constructor call sites coerce their real and imaginary coordinates
to `Fraction`.  The 213-value census is not by itself a proof about every
future call to the public constructor--a caller could explicitly pass a Python
float and inherit `Fraction(float)` semantics--but no registered parsing,
fixture, scorer, or constructor path does so.  This is an API-hardening issue,
not a present scientific escape.

## 3. Exact operator reconstruction

### 3.1 The anonymous-map no-go is real

The same anonymous matrix is supplied with two different lawful typed spans.
Since the matrix bytes are identical, every basis-dependent or
basis-independent invariant of the anonymous map is identical as well:
rank, singular values, Gram operators, kernels, ranges, minors, and polynomial
invariants.  Yet the persistence/creation relation differs.  Therefore no
invariant of the anonymous map alone can recover the registered span.  A
purported recovery necessarily imports a basis-to-configuration dictionary or
other typed data.

The support calculation independently gives actual-support counts

```text
2, 3, 3, 3, 2
```

over the five registered coupling angles, while the allowed kinematic grammar
has three arrows throughout.  At the endpoint angles one allowed arrow has
zero amplitude.  Thus the correct compatibility is

$$
\operatorname{supp}(\Theta)\subseteq E_{\rm allowed},
$$

not equality.  A zero coupling is not a proof that a transition is
kinematically forbidden.  Conversely the map's support relates basis states
across a step; it is not the spatial adjacency relation within the output
graph.  The paper mostly observes this after repair, and the first finding is
operator-theoretically sound.

Grade: **theorem for the registered discriminator**, plus an **exact
finite-fixture zero-coupling control**.

### 3.2 Dictionary census and the quantifier on future-physical

The independent census reproduces exactly:

```text
candidate dictionaries        12
survivors                       2
current screens (survivor 1)   (16/25, 9/25)
current screens (survivor 2)   (16/25, 9/25)
future persistence screens     (16/25, 9/25) versus (9/25, 16/25)
```

The input dictionary is forced.  The residual pair exchanges which fed
intermediate direction is named the persisting cell and which is named the
created cell.  Every registered current observable contracts that intermediate
index and is therefore blind to the exchange.  The declared graph-local probe
and persistence probe expose it, and removing the future reactivation kills
that classification exactly.

This proves:

> The dictionary difference is current-blind but future-physical for the
> registered finite continuation grammar.

It does **not** prove that the candidate law generates that continuation, that
the grammar is closed under every licensed future, or that the difference is
absolutely permanent.  The future projectors are separately declared test
operators.  The word `future-physical` must carry that qualifier everywhere.

Grade: **exact finite-fixture fact under a declared future grammar**.

### 3.3 Continuous growth/recombiner variety

Write the creation column and recombiner row as unit vectors

$$
g=(x,z),\qquad r=(u,v),\qquad x^2+z^2=u^2+v^2=1.
$$

The two calibrated output probabilities are exactly

$$
p_0=(xu+zv)^2,\qquad p_1=(-xv+zu)^2,
$$

and their sum is one by the Lagrange identity.  Hence the lawful parameter
space before quotienting is a two-torus (or two circles in the real slice),
while the displayed probability screen depends only on the relative angle.
The five-by-five table is 25 sampled points on that continuous identity; its
nine distinct screens are the nine values reached by that sample, not a census
of the full law space.

Reciprocity imposes `u=x` and `v=z` in the registered real frame.  It selects a
one-dimensional diagonal subfamily, of which the five rows are only sampled
points.  Adding the independent recorded-successor weight angle gives three
continuous moduli before reciprocity and two after it.  The paper's dimension
language is correct only when read as the dimension of this analytic family,
not as a conclusion inferred from 25 rows.

The recurrence gate does not reduce this variety by derivation.  It constructs
two occurrences with the same base-growth parameter and verifies that their
screens agree.  A second occurrence with another angle still passes
normalization, complete positivity, and all one-site gates.  Equality follows
only after declaring that the two occurrences instantiate the same local type
with a universal coupling.  That is a legitimate nomological locality axiom,
but it is not selected by the candidate surface.

Grade: **analytic theorem for the declared real two-angle ansatz**;
recurrence is a **conditional result from a declared type-universality axiom**.

### 3.4 Reciprocity fixes the image, not the whole future law

For an isometry `V:H -> K`,

$$
V^\dagger V=I_H,
$$

so `V^dagger` is the canonical left inverse and erases exactly on
`ran(V)`.  Every linear left inverse has the form

$$
L=V^\dagger+A(I_K-VV^\dagger),
$$

with arbitrary `A:K -> H`.  Therefore reciprocity fixes the action on the
reached image and leaves the orthogonal null direction completely free.  My
independent null-extension mutant changes `A`, preserves `LV=I`, and changes a
future that injects the previously unreached direction.  This matches the
candidate's own reactivation lesson.

Exact erasure is consequently a conditional dynamics/calibration requirement:
it follows if the adjoint continuation is licensed, but conservation does not
force nature to supply that continuation.  Nor can this inverse select weights
for distinct recorded successors.  The exact normalized examples

```text
(1,0), (16/25,9/25), (9/25,16/25),
(25/169,144/169), (0,1)
```

all coexist with the same reciprocal transport.  No completeness, covariance,
or composition equation in this candidate couples those weights to the
creation angle.

Grade: **elementary conditional theorem on an isometric reached image** and
**exact finite nonselection witness**.

### 3.5 Gaussian phases and representation freedom

The phase census independently reproduces 64 raw connections, four orbits of
16, and holonomy-class screens

```text
1, 1/2, 0, 1/2.
```

Boundary rephasings act on open transport representatives; the closed cycle
product survives.  Configuration relabelings that transport the graph,
dictionary, record projectors, and matrices together are gauge.  A relabeling
of matrices alone changes the calibrated physical question.  Kraus rotations
are gauge for an unconditioned channel, but need not be gauge after its ports
are record-individuated.  None of those groups should be collapsed into
entrywise phase freedom.

My mandatory extra countermodel is a target-record-algebra-preserving diagonal
unitary `D` (a nontrivial sign/phase frame change).  Replace

$$
V\mapsto DV,\qquad U\mapsto UD^\dagger.
$$

The coordinate record projectors commute with `D`; support, the two-survivor
dictionary count, bundle incidence, all probabilities, and the composite
`UV` remain exact, while individual matrix entries move.  Thus:

- the map-only refutation survives;
- the typed span and dictionary claims survive after covariant transport;
- the single-step bundle claim survives only modulo this boundary gauge;
- coupling **moduli** and closed holonomy survive, while open-entry phases do
  not.

This is a representation quotient the paper should state explicitly.  It does
not rescue the missing composition law.

## 4. Instruments, direct sums, and quantum-consistency wall

Each displayed creation map is an isometry and hence defines a completely
positive trace-preserving embedding channel `rho -> V rho V^dagger`.
The rotations/reflections are unitary.  Those local facts are sound.

The global expression

$$
\mathcal H=\bigoplus_G \mathcal H_G
$$

does not, by itself, impose a superselection rule: vectors and density
operators may have cross-block coherences unless the observable algebra or a
dephasing instrument forbids them.  Conversely, merely writing the direct sum
does not prove that such coherences are physically available or transported.
The candidate constructs neither a global CP instrument nor a block-sparse
operator with all source/target blocks, record ports, and overlap-composition
rules.  It tests one local isometry and one relabeling square.

Thus there is no basis for concluding either that carrier changes preserve
coherent superpositions of geometries or that every change becomes
entanglement breaking.  The question is uninstantiated.  A compositional weld
would require at minimum:

1. an independently individuated configuration/event algebra;
2. a block for every licensed rewrite generator;
3. a coherent composition/interchange law for consecutive and overlapping
   blocks;
4. a complete instrument/normalization law for competing successors; and
5. a representation-safe observable algebra on the resulting quotient.

The candidate also supplies no bipartite changing-factorization instrument,
so it earns no new theorem about no-signalling, conditional steering, EPR, or
decomposition sensitivity.  Those results remain missing.  Paper 4 mostly
refuses them; no repair should turn the absence into a positive claim.

Grade: **open/type-incomplete**.

## 5. Numbered hostile findings and smallest repairs

### SRW-O1 — MAJOR — the bundle is single-step data, not a compositional weld

The pair `(rewrite span, transport)` is well typed for one fixture, but no
two-step/overlap composite or global instrument is built.  The phrase
`SRW-BUNDLE-WELD-CONSTRUCTED...` overstates the exact result.

**Smallest repair:** replace every global/compositional claim by “a
single-step typed bundle referent and one relabeling calibration are
constructed.”  Withhold the frozen third finding until a separate coherent
composition fixture exists.  Do not repair the immutable candidate during
review.

### SRW-O2 — MAJOR — recurring locality is assumed at the probability layer

The recurrence gate reuses the base parameter; it does not derive its reuse.
All local operator gates admit different normalized angles at two
token-disjoint occurrences.

**Smallest repair:** label equality across occurrences a declared
type-universality/locality axiom, with the analytic counterfamily stated.  Do
not call the angle a law-selected physical constant until independently
identified recurring sites and calibrated observable movement exist.

### SRW-O3 — MAJOR — future-physical is grammar-relative

The two dictionaries are distinguished only by separately declared future
projectors.  No generated/closed continuation theorem exists.

**Smallest repair:** add “under the registered finite declared future grammar”
to the finding and every paraphrase.  Preserve the exact `12 -> 2` result.

### SRW-O4 — MAJOR — the direct-sum quantum theory is not constructed

The direct-sum notation neither proves cross-sector coherence nor
superselection, and no global CP instrument or all-input normalization law is
given.

**Smallest repair:** explicitly keep cross-configuration coherence,
no-signalling, steering, and EPR open.  Require a block-instrument and
overlap-composition unit before promoting the bundle.

### SRW-O5 — MINOR — grid counts must not stand in for the continuous variety

The `25`, `9`, and `5` counts describe registered samples.  The underlying
identities describe a continuous two-angle family, a one-angle probability
image, and a reciprocal diagonal.

**Smallest repair:** print the analytic equations beside each sample count and
label the counts “registered rows,” not exhaustive laws.

### SRW-O6 — MINOR — reciprocity is conditional and null-relative

The adjoint is canonical only after the isometry and licensed reverse
operation are supplied, and a general left inverse is free off the reached
image.

**Smallest repair:** retain the exact image theorem and recorded-weight
nonselection; delete any wording suggesting a global unique eraser or weight
selection.

### SRW-O7 — MINOR — exactness closure is registered-path closure

The repaired runtime is exact on every registered path, but the public scalar
constructor is not a universal rejection boundary for future float callers.

**Smallest repair:** either reject floats in the constructor itself or state
the narrower registered-path theorem.  No numerical result changes.

## 6. Proposed adjudicated finding list

In frozen order:

1. **Preserve** `SRW-MAP-ONLY-WELD-REFUTED`.
2. **Preserve with mandatory qualifier**
   `SRW-DICTIONARY-CURRENT-BLIND-BUT-FUTURE-PHYSICAL`: “future-physical under
   the registered finite declared continuation grammar.”
3. **Withhold/demote**
   `SRW-BUNDLE-WELD-CONSTRUCTED-BUT-CATALOGUE-AND-COUPLINGS-UNSELECTED`:
   the exact result is a single-step typed bundle construction modulo boundary
   gauge; coherent composition and a global instrument remain absent.
4. **Preserve only conditionally and without weight-selection language**
   `SRW-RECIPROCAL-WELD-CONSTRUCTED-BUT-HISTORY-WEIGHTS-FREE`: for a declared
   isometric creation and licensed adjoint continuation, erasure is exact on
   the reached image, the null extension is free, and recorded-successor
   weights remain unselected.

The minimal scientific kill set is O1--O4.  O5--O7 are bounded prose/API
repairs.  A future overlap counterexample is not needed to reject the stronger
composition wording: no registered overlap composition was constructed in the
first place.

## 7. Contribution to the joint successor law

SRW contributes a useful **kinematic signature** for a possible joint
successor: a relational rewrite span must be carried alongside a compatible
transport, because neither support nor the anonymous matrix recovers ancestry
or the allowed grammar.  It also contributes a conditional reversible
creation/erasure calibration on a reached image.

It does not supply the joint successor law

```text
(relations, geometry, process state)
    -> (new relations, new geometry, new process state).
```

Still missing are the event-selection rule, probabilities/history weights,
the global state-update instrument, coherent composition of consecutive and
overlapping rewrites, a law-generated future/permanence grammar, configuration
individuation, and actualization.  The registered phase holonomy is inserted
transport data; it is neither metric curvature nor gravitational dynamics.
The candidate is therefore an exact local architecture result, not unified
dynamics.

## 8. Review self-authentication

This report's canonical SHA-256, computed after replacing the 64 hexadecimal
characters on the next line by 64 ASCII zeroes, is:

`a4814e6cce412f55af81c6ab8968f1a99a54aa1e770e1bb40dce71f6c42f326e`
