# CEL physical fixture and scorer freeze

Status: **PHYSICAL FIXTURE/SCORER FROZEN BEFORE FIRST OFFICIAL EXECUTION**.

Ledger event: v16 #70. Immutable generic-core commit: `3a99285`.

| artifact | SHA-256 |
|---|---|
| `v16/code/cel_fixture.json` | `8a18a70f1e1b7781806d800c54afd5dcbd10dbac1307db4420bafcb4b57854f2` |
| `v16/code/cel_score.py` | `27ee69af161382dfda3de81e1ea4d0edf4d6b4afb8d11d5f30ec7d3e075749c8` |

The JSON fixture is data only. It contains no JSON float, expected-answer
field, target word, primary word, pass count, selected witness, or solution-
dimension field. It binds eleven antecedent/core paths by hash and consumed
text tokens. Its four physical arms are:

1. two overlapping-CNOT histories, two lawful kernels, idle dressing, an
   identical-history null control, a licensed exchange, explicit shared-token
   restrictions, and independent-versus-universal parameter constraints;
2. reset/relabel/append/refire/redundancy/branch-retention/recovery-licence
   record fixtures, with full finite continuation generators;
3. the two JCV coefficient families plus named pre/post catalogues, a created
   flag attachment, kinematic support, relabel maps, an anonymous-ancilla
   control, and two continuation grammars; and
4. scalar `7/5`, rank-two `diag(7/5,1)`, four real/complex/singular PSD
   matrices, an explicit three-row factor, a non-PSD control, a field-role
   declaration, and fifteen scope walls.

The scorer imports the already frozen CEL core without modifying it. It
derives all histories, channels, semigroup closures, Gram factors, dimensions,
outcome words, qualifiers, claim prose, and artifact seals. It has 44 named
gates and 41 registered mutants. The primary comparator separately asks
whether recurrence, recoverability, mathematical dilation, relational weld,
exact resources, integrity, and scope pass; no fixture value names its branch.

Constructor runs to alternate temporary targets are deterministic. Two runs
and an alien-CWD run produce transcript, receipt, and paper bytes identically.
All 41 mutants exit `1`, write no artifact, and emit no traceback. They cover
every required anchor, event/kernel, naturality/null/symmetry/gluing,
recoverability/grammar/branch, JCV/dilation/catalogue/attachment/support,
resource/PSD/field, scope, comparator, transcript, and prewrite surface. The
selftest and existing-target controls refuse without writes; an unknown option
exits `2`. The scorer has zero Python float literals.

At this freeze the official result paths are absent:

```text
v16/code/cel_output.txt
v16/code/cel_receipt.json
v16/paper-07-creation-event-universality-recoverable-records.md
```

The next authorized action is exactly one official invocation of the frozen
scorer. Whatever bytes it produces must be committed as the candidate before
candidate verification. No constructor file may change after this commit.
Papers 3–6 and the unrelated untracked v15 SCOUT-T files remain untouched.
