# RFB Paper 10 candidate generation

**Frozen:** 2026-08-18, v16 ledger #134.

**Status:** GREEN-UNREVIEWED CANDIDATE — NOT PANEL-REVIEWED, ADJUDICATED, OR
TERMINAL.

## 1. Frozen result chain

| object | SHA-256 |
|---|---|
| `v16/code/rfb_core.py` | `7d0a787d108ac16229dc6819a81f74f7b80203eaefa71d28f30f1f31b27e9ada` |
| `v16/code/rfb_fixture.json` | `f3557b3400584d01984c6a4f38d40744c9e2cf2f0e36c7c4aa225b045e5bd362` |
| `v16/code/rfb_score.py` | `4a2c9590e1d64f9e40c6e5828d132b9b746aa8938e18aee95a484caabc8c87ff` |
| `v16/code/rfb_output.txt` | `a7ebef77a507bac62c64fa594ed539902ed7a333fe5c77a677a4aad1f124aace` |
| `v16/code/rfb_receipt.json` | `e8cefe5b28d343fe76fdad89283a02c8c2f477243bbc7455b83324a0fe7a4659` |
| `v16/paper-10-record-feedback-boundary.md` | `360b4e861add1a5eac0b09296fe0e52438787697aa22097801f0e43cc2c2a5f4` |

The receipt's internal content digest is
`5d63d8df0d8e1b94b9374c8bea452ab95985ba440432d931cbf16722438321b2`.

## 2. Derived result

The scorer emits primary **RFB-FORCING-BOUNDARY-MAPPED** with the following
separate coordinates:

```text
division interface       DIVISION-KERNEL-SUFFICIENT
microscopic process      METHOD-INCONCLUSIVE
quantum representation   METHOD-INCONCLUSIVE
higher order             NOT-REQUIRED-AT-FIXED-ORDER-FIXTURE
```

This is not a contradiction. The primary says the registered conditional
boundary was measured with impose and return-on-drop legs. The coordinates say
that the measured surface does not select a unique microscopic process or
quantum representation.

The exact finite findings are:

- full accumulation makes every reversible pointer writer a full cycle and
  all such cycles are relabeling-conjugate; dropping it restores multi-cycle
  writers;
- vertex-phase gauge at the registered finite phase subgroup leaves one cycle
  exponent, which changes a port only after a complete winding reconverges;
- type universality reduces two declarations to one but does not select its
  form; translation composition reduces readers to cyclic character charges;
- the simultaneous writer-step/charge quotient is their product, while both
  zero and nonzero charges survive;
- the zero-charge controlled writer already decoheres matter, so reciprocal
  feedback does not force active steering;
- a classical feedback instrument and a reversible coherent dilation have the
  same exact ports and unconditioned channel;
- classical, exact `3/5` hybrid, and coherent tags all pass the common final
  interface with plus probabilities `1/2`, `4/5`, and `1`;
- an exclusive intermediate rewrite kernel is phase-blind, while an
  enriched-state kernel and an indivisible/decoherence-functional history law
  both reproduce the coherent final ports;
- all four representation rows have the same registered predictive quotients
  at equal resources through the held-out horizon;
- record recovery is finite-grammar-relative, redundancy survives a primary
  reset, and an uncopied reset destroys the record; and
- all registered complete Alice-local operations preserve Bob's fixed-factor
  marginal, while a trace amplifier does not.

## 3. Pre-commit integrity

The official invocation passes all forty-nine gates. A second clean generation
is byte-identical for transcript, receipt, and paper. All thirty-nine targeted
mutants refuse at their registered named gates and leave no candidate artifact.
`--selftest`, unknown-argument, target-collision, and existing-target controls
pass. A minimal source/archive tree with no `.git`, executed from
`/private/tmp`, reproduces all three artifacts byte-for-byte.

These are pre-commit checks. Terminal status remains withheld until a fresh
post-commit replay and the separately frozen hostile-review process.

## 4. Known scope that a hostile panel must attack

The writer phase census uses the fixture's finite `Z_q` phase-exponent subgroup;
it demonstrates the quotient mechanism and winding discriminator but is not a
classification of an arbitrary continuous `U(1)` theta angle. The hybrid
dimension is the exact semialgebraic Gram family at one fixed interface, not a
constructed long-time feedback dynamics. Predictive minimality is finite and
deterministic at the registered interventions. Recoverability is relative to
the supplied continuation grammar. Locality is fixed-factor only.

Most importantly, RFB has no relational graph, event-selection law, carrier
growth, dynamic subsystem factorization, geometry, continuum, QFT, or GR. It
classifies a lawful finite feedback boundary; it does not supply the missing
joint generative theory.
