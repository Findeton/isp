# QSF Paper 9 bounded repair — panel corrections implemented

**Date:** 2026-08-18

**Ordered by:** `v16/note-qsf-adjudication.md` at commit `4632d2b`.

**Primary:** `QSF-METHOD-INCONCLUSIVE` (unchanged).

## 1. What changed

The scorer, receipt, transcript, and generated Paper 9 now implement the ten
orders of the joint panel without changing the generic core, physical fixture,
WRC source values, or primary word.

- Record-indexed A0 is replaced by the exact same-record/same-outcome/
  different-ray collision. State/history indexing is called a lookup, not an
  affine law.
- Raw and translation-aligned recurrence censuses are both computed. A
  terminal phase family moves all `10,527` branch rays while preserving every
  registered terminal basis aggregate, separating branch failure from the
  unentered aggregate A1/A2 problem.
- The natural fixed-factor HJW witness now binds ordered-history, count-record,
  and record-erased-screen TVs through all five available ticks; a second MUB,
  Alice-outcome relabeling, a second affine control, and feedback erasure are
  explicit controls.
- Arm C is retyped as failure of a `rho`-sufficient retained state-record map,
  not failure of every stochastic division. The independent tick-two full-
  density witness has `72` differing entries, off-diagonal difference
  `(448+224 omega)/151875`, probe difference `112/50625`, and tick-three
  diagonal TV `99328/4100625`.
- The old S1 table is labeled future-trigger-suffix-only. Calibrated final-
  record quotients are `3,3,27,27,486,486`. The only pair of past-depth-three
  histories sharing a horizon-two suffix law, `(1,3,12)` and `(1,3,14)`, has
  different exact horizon-three laws. This proves non-stabilization without a
  runtime-prohibitive full horizon-three census. Complete intervention-level
  JS-S1a remains unentered.
- The primary is derived from four measured arm-status objects. Eight new
  upstream mutants target the new claim lineage.

## 2. Ontology correction

Paper 9 now uses the Barandes-safe split:

```text
fixed meta-catalogue                         kinematic input
one actual configuration trajectory         candidate ontology if postulated
stochastic transition kernel                nomological law
distribution over possible trajectories     epistemic ensemble description
Hilbert/ray/density/Kraus data               representation unless promoted
```

A fundamental stochastic kernel must normalize and compose on its actual
configuration space. Affinity and complete positivity on `rho` are additional
requirements only when `rho` is asserted to be the complete operational
state. QSF proves that literal WRC retention and `rho`-completeness cannot both
hold. It does not select an affine completion, an ontic-ray theory, CRL, a
classical-feedback law, or any growth/geometry law.

## 3. Deterministic generation

Two clean invocations from the adjudicated tree produce byte-identical
artifacts. Each passes `20/20` scientific gates in approximately `184` seconds,
below the frozen `360`-second ceiling.

| artifact | SHA-256 |
|---|---|
| `v16/code/qsf_score.py` | `de12902eac3cb6683385fd3f1060b7a6e5ea03c24c0893ecb9273e3e5c3ea5b5` |
| `v16/code/qsf_output.txt` | `d9e6317ac31caf8f7a5e6e0a9999400a1b063291120a221a4cc1d66863c85a2d` |
| `v16/code/qsf_receipt.json` | `664ab0aeee87dfa4be7e7579b4b338299efabf4480f1d345dadca75586f3c873` |
| `v16/paper-09-quantum-seam.md` | `a84c1fedce219dc996f85ae4e3d374a7a36ce2948c38b3518f7c25e2af67802d` |

Terminal promotion is not asserted here. It requires all `28` registered
mutants, a second post-commit clean generation, and true off-tree/no-`.git`
replay with the exact committed source set.

## 4. Scope

No relational composite, graph-generated history, carrier growth, spacetime,
gravity, locality cone, continuum/Lorentz limit, QFT, species, Hamiltonian,
constant, actualization rule, or empirical deviation is constructed or
selected by this repair. Paper 10 remains unpinned until QSF terminal
verification is separately frozen.
