# APR Paper 12 — v3 delta: positive-path and exposure guards

**Date:** 2026-08-19  
**Status:** BINDING PRE-FREEZE ADDENDUM  
**Parent:** `v16/note-apr-v3-delta-repair-pin.md`

This addendum closes six ambiguities while the v3 scorer remains unfrozen and
before any v3 fixture evaluation. It adds no APR physics.

## 1. Positive process branches are mandatory

The scorer must implement and synthetically exercise lawful positive branches;
an always-static implementation is not result-neutral.

`HORIZONTAL-CLASSICAL` requires all of the following in one semantic package:

1. a total active-boundary/frontier factory over its declared domain;
2. a typed filling-to-positive-map/instrument assignment;
3. identity assignments at every active process object;
4. sequential composition and alternate-cut equality;
5. positivity, affinity, mass preservation, and retained zero ports;
6. a typed tensor, unit, and interchange law;
7. every nontrivial vertical/horizontal naturality square required by the
   declared process interface;
8. generated record/effect semantics for any record claim.

A complete synthetic classical package must reach `HORIZONTAL-CLASSICAL`.
Nulling, deleting, disconnecting, or replacing with `None`, an empty string, or
an empty container any one required component must demote or refuse.

`HORIZONTAL-QUANTUM` requires the entire classical typed process package plus:

- a nonzero same-process interference witness;
- a strongly positive history/Gram structure or equivalent all-input quantum
  representation;
- all-input complete stable division instruments;
- consistent coherent coarse-graining/refinement at the claimed cuts.

A complete synthetic quantum package must reach `HORIZONTAL-QUANTUM`.
Deleting interference, strong positivity, or all-input division completeness
must demote/refuse. A classical package without interference can never be
quantum. Every emitted word is statically checked against the five-word frozen
process vocabulary.

No strict-primary classifier may outrun the process coordinate produced from
the same semantic package. Manually setting capability Booleans without the
underlying validated package cannot manufacture a process or joint-law result.

## 2. Active identity domain is exactly B0–B3

For the APR record-tree process claim, `all_boundary_identities` quantifies over
the four semantic active boundary levels `B0`, `B1`, `B2`, and `B3`, derived
from the registered record-frontier factory. Other typed fixture boundaries
are reported as out-of-domain/unassigned and neither help nor hurt this
coordinate.

Only `qw_000 -> hf_015` witnesses the B0 identity. Required attacks:

- an assigned true empty-process identity passes;
- an identical raw but unassigned arrow fails to witness;
- an assigned same-boundary replacement/nonidentity fails;
- assigning `hf_015` to a nonempty tree fails;
- assigning `qw_000` to `hf_016` fails;
- consistent relabeling of every ID/reference preserves the census.

B1–B3 remain absent unless active assignment rows are genuinely constructed.

## 3. Exact generated-contact-only classification

`generated_contact` is computed from one typed law, not supplied as a census
literal. It requires a generated schedule, a non-overlap/contact intervention,
a delayed calibrated response, and joint-law provenance. Static contact
declarations, arrows, correlations, empty/disconnected schedules, and controls
outside a joint root never promote.

When every other genuine capability is true but `generated_contact=false`, the
exact primary is:

```text
APR-DYNAMIC-ATOMLESS-REGIONAL-REFERENT-CONSTRUCTED-LAW-UNSELECTED
```

with a wall explicitly naming `missing generated_contact`. A fully semantic
synthetic contact package may pass this gate. Setting `causal_order=true` does
not alias or imply contact.

## 4. Measured ontology-role ladder

`ontology_candidate` is never an input to the measured role or primary. Remove,
rename, or change the postulated candidate and every scientific coordinate
must remain invariant (or the candidate schema may refuse before scoring).

The role ladder is semantic:

- `STATIC-RESPONSE`: response pairing/control on a fixed declared algebra,
  without a typed horizontal process;
- `FIXED-ALGEBRA-CONDITIONING`: a typed affine conditioning/restriction process
  on the same algebra with the required process equations;
- `RECORD-WRITING-ON-FIXED-ALGEBRA`: the preceding process plus a typed writer,
  delayed reader, and continuation-stable recovery at declared scope;
- `REGION-REWRITING`: a same-law output algebra/support rewrite whose changed
  regional object controls a later calibrated response.

Synthetic positive examples must reach every rung; deleting its load-bearing
evidence must demote. A raw append label, cospan, replacement-control name, or
catalogue declaration cannot promote. APR's frozen baseline remains
`STATIC-RESPONSE` unless its actual evidence satisfies a higher definition.

## 5. Exact qualifier

The sole registered baseline qualifier is:

```text
APR-STATIC-RAW-PREFIX-SYNTAX-ATOMLESS-RESPONSE-CONSTRUCTED-PROCESS-UNBUILT
```

No old qualifier, abbreviation, or syntax-free spelling may appear in the v3
receipt or transcript. The qualifier is not a primary and cannot promote a
physical regional referent.

## 6. Artifact exposure/blinding fields

V3 source may freeze before its own run, but APR results were known before its
implementation. Both receipt and transcript must contain:

```text
blinding_status: RESULT-KNOWN-BEFORE-V3-IMPLEMENTATION
exposure_debt: PERMANENT-PREFREEZE-M01-AND-ALL-MUTANTS-EXPOSURE
v3_source_frozen_before_v3_run: true
```

The fields are integrity metadata, not classifier inputs. Removing or changing
them must fail artifact verification or move only the integrity coordinate.
Candidate prose may say “source-frozen before v3 run,” never “pre-truth,”
“blind,” or “mutually blind.”

## 7. Mandatory post-freeze attacks

The v3 black-box verifier must run all v2 checks plus:

- process-vocabulary static scan and all positive/ingredient-drop branches;
- active B0–B3 identity attachment/relabel attacks;
- ontology-candidate invariance and every role promotion/demotion;
- independent causal-order/contact separation and contact-only primary;
- exact qualifier and scope phrases;
- exposure fields in both artifacts;
- a search proving capability literals cannot bypass semantic packages.

