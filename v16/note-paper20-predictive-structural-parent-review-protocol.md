# Paper 20 internal semantic hostile-review protocol

Date: 2026-08-21

Status: **FROZEN — NONBLIND INTERNAL AUDIT ONLY**

## 1. Corpus

| artifact | ordinary SHA-256 |
|---|---|
| mathematical pin | `06f80fc38bb9d54944cac4ab0f75509bee4f4ebcce48d7098fe490b0205432a8` |
| Paper 20 candidate | `64111d7764bf70984b959e00bc1da30ad0d6c3ae7b5c6d51b94227ad2f5c35e6` |
| construction note | `67e2bef72a21bed1e598201e108595aaab5ec16e966ef03388fcb6969d407ef3` |

The construction note is descriptive and cannot override the pin or paper.

## 2. Review status

This audit is performed by the constructing agent. It is not blind,
independent, or terminal hostile review. Its strongest possible verdict is

```text
ACCEPT-INTERNAL-SCOPE
```

An exact semantic counterexample produces `REVISE` or `REJECT` regardless of
whether the paper's final product is conservative.

## 3. Audit order

The audit proceeds in this order:

1. authenticate every bound input and frozen Paper 20 artifact;
2. read the complete pin and paper;
3. reconstruct all definitions without relying on section conclusions;
4. reconstruct all exact probability arithmetic;
5. test the parent typing and predictive quotient;
6. test Paper 18 terminology and branching scope;
7. test all quantum and symmetry no-go statements;
8. complete all forty pinned hostile controls;
9. compare the product vector with the proved result; and
10. reauthenticate the frozen bytes.

## 4. Lens A — type, quotient, and measure

### A1–A12

1. Verify a dependent disjoint union realizes one fiber, unlike a Cartesian
   product.
2. Check that the raw target-type index is not predeclared physical structure.
3. Verify predictive equivalence is reflexive, symmetric, and transitive.
4. Check comparison across endpoint types uses an admitted transport.
5. Verify every complete future probability, not one reader, enters the
   relation.
6. Reconstruct the minimal-sufficiency factorization and its direction.
7. Test a finer sufficient statistic and a falsely coarser statistic.
8. Verify the quotient is point-free under the accepted groupoid action.
9. Check the quotient sigma algebra and identify where regular conditional
   probabilities are not proved.
10. Reconstruct normalized predictive pushforward.
11. Reconstruct the identity-parent representation no-go.
12. Verify deletion is claimed only where it descends through the quotient.

Fresh controls must include two raw fibers with identical coarse outputs but
different complete futures, a quotient with a nonmeasurable-looking class
description but a valid final sigma algebra, and a deletion map that fails to
respect an arbitrary coarse quotient.

## 5. Lens B — accepted-Gamma specialization

### B1–B12

1. Authenticate the accepted Paper 13D law and adjudication.
2. Reconstruct the fixed-size grand observational parent.
3. Verify its source law and private seed law are those of Paper 13D.
4. Prove endpoint colors are iid fair.
5. Recompute the conditional and unconditional bond probabilities.
6. Recompute expected edge count and degree.
7. Recompute the exact triangle probability.
8. Prove two bond values give distinct positive endpoint orbits.
9. Verify the complete identity reader makes the predictive quotient the
   endpoint-orbit quotient in this specialization.
10. Reconstruct uniform deletion and the all-size mixture.
11. Prove the process-class pushforward remains Dirac.
12. Confirm no bond, packet, target value, or size becomes execution syntax,
    chronology, adjacency, volume, or dimension.

Fresh controls must include a same-marginal non-iid bond model, a dense
projective graph family, and two target configurations inside one fixed
execution class.

## 6. Lens C — physical interpretation and Paper 17

### C1–C12

1. Separate target-configuration plurality from Paper 18 target-sector
   branching.
2. Separate target sectors from resolved channels.
3. Separate resolved channels from activity and root laws.
4. Test the accepted-`R` structural-bit wrapper and its swapped decoder.
5. Test binary-tree bracketing for more than two structural channels.
6. Test connectivity and support decoders against predictive minimality.
7. Reconstruct the amplitude representation no-go.
8. Reconstruct the sector-diagonal phase theorem.
9. Verify a physical off-diagonal recombiner would be necessary for
   structural interference.
10. Test the conditional invariant-measure theorem against presentation-only
    symmetry.
11. Verify the restricted Paper 17 target investigation excludes chronology
    and dimension.
12. Verify the original Paper 17 reopening conditions do not use downstream
    outputs to select the parent.

Fresh controls must include two block-diagonal sector states differing only
by relative phase, a transitive action on labels that fixes every physical
channel, and a target law shared by two different whole-process marginals.

## 7. Pinned hostile controls

All forty controls in the mathematical pin must be answered literally. A
summary reference to the manuscript list is insufficient: the audit report
must state the result of each control or group only controls whose proof is
identical and explicitly enumerate their identifiers.

## 8. Verdicts

- `ACCEPT-INTERNAL-SCOPE`: no exact semantic counterexample; the product
  vector is exactly supported.
- `ACCEPT-WITH-FIXES`: only bounded wording or certificate corrections; no
  product coordinate changes.
- `REVISE`: a theorem, type, probability, or product coordinate must change.
- `REJECT`: the common-parent or predictive-quotient construction fails at
  its earliest claimed positive coordinate.

## 9. Completion

The audit is complete only after a frozen report, a final adjudication,
path-scoped whitespace checks, ordinary SHA-256 hashes, and an explicit
statement that no implementation or Paper 17 inference ran.
