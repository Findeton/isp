# SRW — support–rewrite weld, persistent identity, and local couplings

Status: **PIN — FROZEN BEFORE CONSTRUCTION**.

Authorization: on 2026-08-17 the user explicitly instructed, “ok go ahead,
investigate all that,” after supplying an external proposal and critique about
the possible weld between Paper 3's heterogeneous carrier and relational
rewrite. This starts Paper 4 as a separate v16 continuation. It does not edit,
repair, rescore, or terminalize the green-unreviewed Paper 3 candidate.

Immutable base commit: `a8e7cb698be92ef09c859526ff0dddfc0c0f8eaa`.

## One boxed question

> Can a changing relational configuration and its quantum/process transport
> be represented by one typed successor object whose kinematic rewrite,
> carrier dimensions, persistence data, actual transport, operational gauge,
> and recurring local couplings are distinguished without becoming separate
> feed-forward machines; and do continuation closure, covariance,
> completeness, recurrence, and reversible reconvergence select that object or
> leave physically different catalogues, dictionaries, and couplings?

The strong proposal to be tested is **map-only**: one anonymous linear map
should determine the relational rewrite from its support and the dynamics from
its entries. The corrected positive candidate is a **bundle weld**: a typed
relational rewrite arrow `r : G -> G'`, including its persistence/creation
incidence, together with a fiber map `Theta_r : H_G -> H_G'`, with composition,
relabeling, records, and gauge defined for the pair. Calling the pair one
successor object does not license recovering `r` from `Theta_r` unless the
fiber representation is independently proved faithful.

## Immutable antecedents and path-value anchors

Runtime code may read only the paths declared below, through the immutable
base commit, and each read product must be consumed by a named gate. The user-
supplied scratchpad claims and scripts are motivations, not anchors and not
runtime inputs.

| committed path | SHA-256 at immutable base | binding use |
|---|---|---|
| `v16/paper-03-contextual-pullbacks-permanent-records.md` | `ca7b06e9e5540d81afb4a401beb66cb2834e3e74033fd742ac5257108a19654f` | Paper 3's exact common-future, graph-rewrite, law-freedom, and scope statements |
| `v16/code/ppr_fixture.json` | `cecc3b0d3c7bf46503481fa7b422e915ba0ff6aac42e3cec5f61c395e565b389` | anonymous matrices and separately labelled relational wedge to be retyped, not silently identified |
| `v16/code/ppr_receipt.json` | `dc88d6a2fbcf350785cc5f12cdcb8ea0805c4df6deaacac9ceffd34b1699c630` | exact Paper 3 candidate values to reproduce before extension |
| `v16/note-ppr-candidate-verification.md` | `b81f6e7ec191e40040c508f16c4f90961c154da5503f9bbd7d0eacd887853d17` | replay and candidate-only status |
| `v16/note-ppr-hostile-protocol.md` | `cc3d89eb6bcd6fdc7b17d849d982300a85a1773caa50def98a96f9dab40ed21e` | the pre-existing heterogeneous-carrier/graph-weld attack; not a returned review |
| `v12/paper1-composition-defect.md` | `81bdab5673fb67b63cd10c08fbb80870f8aa01088047718c5b4bf447e1669128` | fixed configuration catalogue, configuration basis, records/composition scope, and reduced representation gauge |
| `v15/note-homonym-audit.md` | `4dbdb8f932e1b4e3d3813c7dcb9d2905f37b4c42819cc14537afa993e2ce51d9` | E-34 terminology and sample-space separation |

The following exact Paper 3 identities are mandatory exit-1 anchors, not
Paper 4 conclusions:

1. `supp(Vgrow) = {(0,0),(1,1),(2,0)}`;
2. `Ualpha = R` with one zero output row;
3. `Ubeta Vgrow = R J` with one zero output row;
4. for `n = (4/5,0,-3/5)^T`, `Vgrow^dagger n = 0` and
   `Ubeta n = (0,0,1)^T`;
5. the two Paper 3 rival screens remain `[49/625,576/625]` and `[0,1]`.

Reproducing these identities does not prove that the anonymous matrix rows are
the named graph vertices. That cross-object identification is the object of
this unit.

No v15 SCOUT-T path belongs to the read, cite, write, stage, or archive set.

## Declared arena

The boundary/family/law/state/arena/provenance coordinates are data:

| coordinate | declaration |
|---|---|
| **boundary** | one initial relational catalogue, one dimension-changing successor boundary, and one common screen boundary; a second overlapping successor supplies recurrence |
| **family** | finite typed relational rewrite spans and exact finite-dimensional fibers; the primary positive sector is the one-excitation vertex functor, with port-stabilized, edge-sector, and internal-multiplicity controls |
| **law** | exact real or Gaussian-rational isometries and instruments; the growth and history/readout angles begin as independent variables |
| **state** | all basis preparations for type and completeness gates, plus explicitly declared coherent preparations for interference screens |
| **arena** | a two-to-three internal-vertex growth wedge, a typed boundary probe, a recurring local creation/reconvergence pair, and a three-cycle phase control |
| **provenance** | immutable base `a8e7cb698be92ef09c859526ff0dddfc0c0f8eaa`; new fixture truth absent until its scorer is frozen |

The exact finite catalogue is not a continuum, manifold, spacetime metric,
field theory, or all-possible-physics grammar.

## Term-binding table

| term used here | bound object | explicitly not identified with |
|---|---|---|
| **complete configuration** | one basis-labelled member of the declared finite ontic configuration catalogue | one graph vertex in general; one Kraus label; one measurement outcome |
| **one-excitation vertex sector** | the special sector whose basis state means “the excitation is at internal vertex `v`” | the general configuration basis; a proof that vertex count is always Hilbert dimension |
| **relational configuration** | a finite graph with typed internal vertices and boundary ports, modulo its declared relabeling action | the support of a transition matrix |
| **rewrite span** | a typed arrow `r:G->G'` carrying preserved, created, and removed relational items and their incidence | an anonymous matrix zero pattern; spatial adjacency inside `G` or `G'` |
| **persistence map** | the injective part of the rewrite span identifying which old relational items survive | any nonzero transition from an old basis state; a mere equality of vertex names |
| **kinematic grammar** | the set of rewrite arrows and local fiber entries permitted by type | the actual support of one selected law |
| **actual support** | the nonzero block/entry set of `Theta_r` at one coupling value | the whole kinematic grammar; proof that a zero coupling forbids a rewrite |
| **fiber functor** | an assignment `G |-> H_G` and relabeling maps, with a declared sector and internal multiplicity | a dimension numeral copied into graph metadata |
| **bundle weld** | one typed pair `(r,Theta_r)` composed and transformed together | the claim that `r` is reconstructible from `Theta_r` alone |
| **dictionary** | an isomorphism from anonymous matrix coordinates to the independently typed configuration/fiber basis | an ontic fact before operational or structural calibration |
| **current-observable equivalence** | equality for every readout already registered at the present boundary | continuation-stable gauge |
| **continuation-stable gauge** | an equivalence preserved by every arrow generated by the frozen future grammar, including spectators and readouts | blindness of one preparation or one continuation |
| **boundary-frame gauge** | composition-compatible basis rephasings/relabelings at typed interfaces | arbitrary entrywise phase deletion; physical loop holonomy |
| **dynamical holonomy** | the gauge-invariant phase/product around physically different transport routes | a same-fact comparison defect |
| **growth coupling** | the angle/modulus inside an isometric carrier-enlarging transport | a probability over mutually exclusive recorded successors |
| **history/readout coupling** | the independent angle/modulus weighting or recombining complete alternatives at the later interface | the growth coupling merely because the same numerals recur |
| **reciprocity** | the conditional requirement that an unrecorded exact eraser is a left inverse/adjoint on the reached image | a universal time-reversal postulate; selection of branch probabilities |
| **boundary probe** | a typed external port/effect used to define a graph-local future readout | automatically another internal carrier basis state |
| **record** | a distinction recoverable under the registered continuation grammar | a created vertex merely because it was created; actualization |

Probability is normalized over mutually exclusive complete final screen
records. The simultaneously nonzero entries of a transport column and the
simultaneous relations inside a successor graph are consequences within a
candidate successor, not probability alternatives.

## Four-gate audit for new objects

### A. Bundle weld `(r,Theta_r)`

- **Referent:** a rewrite arrow in the declared finite relational category and
  a linear map between its assigned fibers.
- **Necessity:** Paper 3 currently presents a dimension-changing process fork
  and a named graph rewrite in separate fixtures.
- **No-smuggling:** the arrow and map are input data of a candidate law; the
  unit may prove compatibility or faithfulness but may not call either
  derived merely because they are stored together.
- **Discriminator:** support/type, composition, relabeling, graph-local probes,
  and erasure controls can accept one pair and reject another.

### B. Persistence map

- **Referent:** the preserved-object leg of a typed rewrite span.
- **Necessity:** one source direction may feed both a persisting target and a
  created target, so nonzero support alone does not say which is which.
- **No-smuggling:** the persistence map cannot be read from repeated vertex
  spellings; it must transform naturally under relabeling and be independently
  consumed by a continuation or record gate.
- **Discriminator:** swap the persisting and created targets while keeping the
  anonymous matrix fixed; a lawful persistence- or graph-sensitive future may
  distinguish them.

### C. Continuation-stable dictionary gauge

- **Referent:** the automorphism group of the typed weld that acts trivially on
  every experiment generated by the frozen continuation grammar.
- **Necessity:** the delivered twelve-to-two dictionary claim establishes only
  present blindness unless closure is tested.
- **No-smuggling:** no candidate is called gauge because its name is
  “unrecorded”; the full generator action is evaluated.
- **Discriminator:** an appended graph-local, persistence-local, or internal-
  degree continuation either moves a calibrated statistic or proves the
  transformation remains in the common commutant.

### D. Coupling-identification principle

- **Referent:** an equation relating the growth and history/readout moduli.
- **Necessity:** Paper 3 reuses the `3/5,4/5` pair in objects of different type
  while admitting a rival law with the complementary pair.
- **No-smuggling:** numeral reuse and a type-name assertion are not equations.
  Any reduction must follow from a separately stated composition,
  reciprocity, covariance, record, or recurrence condition.
- **Discriminator:** solve the joint exact constraint variety with the two
  angles independent, then add each candidate principle separately and
  measure the dimension and held-out screen.

## Locked investigation order

### 0. Referent and type census — first gate

Before solving a dictionary, enumerate the complete declared objects:

1. graphs and rewrite spans, including the persistence/creation legs;
2. fiber functors and their computed dimensions;
3. anonymous maps and their source/target types;
4. internal vertices versus boundary ports;
5. kinematically allowed entries versus actually nonzero entries;
6. record outcomes versus simultaneous support components.

If the Paper 3 matrix and graph cannot be placed in one typed diagram without
adding an undeclared object, `SRW-NO-TYPED-WELD-REFERENT` is a primary result.

### 1. Legacy identity reconstruction

Independently reproduce all five exact Paper 3 identities above. Enumerate the
full `2! x 3! = 12` anonymous vertex dictionaries, with counts derived rather
than typed. The support/rewrite constraints and the later graph-local controls
are reported as separate filters; no post hoc filter may be described as
selection by the earlier one.

### 2. Current blindness versus future-stable gauge

For every support-compatible dictionary:

1. replay the currently frozen contracted observables;
2. append a relabeling-covariant graph-local probe defined without matrix-row
   names, including the unique internal neighbor of the typed boundary port;
3. append a history-local persistence/creation probe;
4. close the declared future generators under composition and compute the
   transformation's common commutant on the reached subspace;
5. classify the swap as current-blind, graph-distinguishable,
   history-distinguishable, or continuation-stable gauge.

A result at this finite grammar may not be promoted to gauge under every
possible future law.

### 3. Kinematic grammar versus actual support

Use the exact family

```text
V(x,z) = [[x,0],[0,1],[z,0]],     x^2 + z^2 = 1.
```

The allowed pattern includes persistence, creation, and the unchanged second
direction. Generic and zero-coupling endpoints are both mandatory. A lawful
endpoint must satisfy `actual support subseteq allowed support`; it must not
delete the kinematic rewrite merely because one amplitude vanishes. A
forbidden-entry positive control must be rejected.

### 4. Fiber-functor and catalogue enlargement

At minimum compare:

- the one-excitation internal-vertex functor;
- the same functor tensored with a two-valued internal degree;
- a port-stabilized carrier with the typed probe added as an idle boundary
  factor;
- an edge-excitation functor that reads the same graph but has different
  dimensions.

Dimensions are computed from the catalogue and sector. Relabeling naturality,
spectator composition, and the exact weld are checked for every compatible
case. A supposedly dark internal degree receives both a blind continuation
and an explicit reactivation probe. The unit must distinguish: incompatible,
compatible but empirically distinct, stable-equivalent at the registered
observable algebra, and continuation-stable gauge.

### 5. Independent-angle and recurring-vertex variety

Begin with independent exact coordinates

```text
x^2 + z^2 = 1,       u^2 + v^2 = 1,
```

for growth and later history/readout transport. Repeated occurrences of the
same declared local type must share their own coordinate pair. The following
are solved separately:

1. completeness/isometry only;
2. relabeling, spectator, disjoint composition, and recurring-type locality;
3. record permanence and comparison flatness;
4. exact coherent reconvergence with a left-inverse requirement;
5. the stronger adjoint/reciprocity requirement on the reached image.

The registered screen pair is computed from the independent coordinates; an
explicit two-parameter family with moving predictions is a law-nonselection
result. A constraint may identify the angles only if its antecedent is
independently typed and its solution variety is computed. In particular,
reciprocity may constrain a reconvergence transport without selecting
probabilities over different recorded successors.

### 6. Boundary gauge and physical holonomy

On a directed three-cycle with phases in `{1,i,-1,-i}`, exhaustively enumerate
connections and composition-compatible vertex-frame transformations. Verify:

- actual support is invariant;
- open-path matrices transform covariantly;
- the closed cycle product is invariant;
- gauge orbits are classified by the cycle product at this finite group; and
- a two-route interference screen moves when that invariant product moves.

This is a representation/transport control, not a spacetime-curvature or
gravity claim.

### 7. Consequence audit

Classify, without promotion, what the surviving object implies for Paper 3's
graph/carrier weld, persistence, record permanence, internal degrees,
couplings, Hamiltonian reconstruction, particle/species freedom, EPR/no-
signalling, backreaction, QFT/GR, and phenomenology. “Permitted by a finite
fixture” is not “forced by the theory.”

## Pre-registered outcomes and feasibility

The primary is the earliest applicable rung, with the registered qualifier
segments allowed below.

1. `SRW-NO-TYPED-WELD-REFERENT` — the graph rewrite, anonymous carrier, and
   probe cannot inhabit one typed diagram. Feasible if dimensions or boundary
   roles conflict before any dictionary is tested.
2. `SRW-LEGACY-WELD-INCONSISTENT` — a typed diagram exists but one of the five
   exact Paper 3 identities or every candidate dictionary fails. Feasible if
   the proposed numerical coincidence does not survive independent rebuild.
3. `SRW-MAP-ONLY-WELD-REFUTED` — a compatible realization exists, but actual
   support cannot recover the kinematic rewrite, spatial incidence, or
   persistence structure. Feasible at zero couplings or when two typed rewrite
   spans share one anonymous map.
4. `SRW-DICTIONARY-CURRENT-BLIND-BUT-FUTURE-PHYSICAL` — two dictionaries agree
   on the frozen Paper 3 observables but a licensed appended continuation
   distinguishes them. Feasible because a current contraction need not
   generate the future observable algebra.
5. `SRW-DICTIONARY-CONTINUATION-STABLE-GAUGE` — all surviving dictionaries are
   one orbit acting trivially under the complete frozen future grammar.
   Feasible when the swap lies in the generated commutant.
6. `SRW-BUNDLE-WELD-CONSTRUCTED-BUT-CATALOGUE-AND-COUPLINGS-UNSELECTED` — the
   typed pair `(r,Theta_r)` composes, transforms, and predicts coherently, but
   more than one catalogue, dictionary, or independent-angle law class moves a
   calibrated future statistic. Feasible because safety and covariance can
   constrain each coupling circle without coupling the circles.
7. `SRW-RECIPROCAL-WELD-CONSTRUCTED-BUT-HISTORY-WEIGHTS-FREE` — reversible
   unrecorded reconvergence uniquely fixes the inverse transport on the reached
   image, while weights of mutually exclusive recorded histories remain free.
   Feasible because inverse transport and successor probability have different
   types.
8. `SRW-LOCAL-LAW-CLASS-SELECTED-AT-FINITE-ARENA` — after the full catalogue,
   continuation, recurrence, reciprocity, gauge, and held-out screen tests, one
   empirically degenerate law class remains. Feasible only if the exact joint
   variety is a point or one gauge orbit and all registered rivals die.

Rungs 3 and 4 may prefix rung 6 or 7 when the strong map-only claim fails but
the corrected bundle weld succeeds. Rung 8 cannot be earned by declaring the
persistence map, identifying two differently typed couplings, or restricting
the candidate family after seeing a screen.

## Kill conditions

Any of the following kills the corresponding positive claim:

- support of a transition map is called spatial adjacency without a typed
  construction connecting them;
- actual nonzero support is equated to the full kinematic grammar and rejects a
  lawful zero-coupling endpoint;
- a persistence target is selected by its spelling or by a planted expected
  dictionary;
- a dictionary called gauge moves any statistic under a licensed future;
- a “graph-local” probe reads an anonymous row number or a planted dictionary;
- a future-stable claim quantifies only over the current preparation or one
  continuation;
- carrier dimension is copied from metadata instead of computed from the
  declared catalogue/sector;
- an internal multiplicity is called gauge before the reactivation control;
- recurrence identifies couplings belonging to differently typed local
  vertices without an independent equivalence;
- reciprocity is used to select weights of mutually exclusive recorded
  successors;
- the two-angle family is reduced by inserting `x=u` or `z=v` into the
  constructor rather than deriving it as a solved condition;
- a boundary-frame transformation changes the closed holonomy or a holonomy
  change is dismissed as gauge;
- a result about one-excitation graph fibers is stated for general complete
  configuration spaces;
- any substantive path uses floats, tolerances, mutable repository reads,
  hard-coded census totals, or a comparator sharing the result builder.

## Receipt, CLI, seals, and falsifiers

- Substantive arithmetic is exact over `Q` and `Q(i)`; formal polynomial
  identities use exact integer/rational coefficients.
- The generic engine is frozen on public calibrations before the physical
  fixture and scorer exist. The fixture is data-only, contains no expected,
  result, verdict, or outcome value, and is frozen with a verdict-neutral
  scorer before its first official execution.
- The delivery CLI rejects unknown arguments with exit 2 and supports
  `--selftest`, `--mutant NAME`, `--output PATH`, and `--receipt PATH`.
- `--selftest` corrupts an anchor, exits 1, and writes nothing.
- Two clean runs, an alien-CWD run, and a true no-`.git` archive replay are
  byte-identical.
- Every published key is sealed when its gate passes; totality and bytes are
  recomputed at promotion. Transcript rows are parsed back and reconciled with
  the sealed gate ledger by multiset equality.
- The verdict is independently reconstructed from measurement coordinates and
  exact equality, with no shared verdict literals or builder function.
- Every load-bearing gate has a falsifier that moves its measured object and
  dies at that gate, or an exact forcing with a disclosed waiver.

At least these named mutants must exist and die blind to their names:

```text
anchor-corrupt, legacy-product-move, dictionary-drop, dictionary-preplant,
persistence-spelling, graph-probe-row, swap-call-gauge,
future-reactivation-drop, support-equality, forbidden-support,
port-role-erase, functor-dimension-type, internal-call-gauge,
internal-reactivation-drop, relabel-break, recurrence-site-drift,
angle-lock-constructor, reciprocity-assume, reciprocity-remove,
phase-frame-break, holonomy-flatten, cycle-call-gauge,
result-count-type, verdict-flip, transcript-forge, seal-after-write
```

The runtime cap for one ordinary solve is 300 seconds. Exhaustive versus
symbolic versus sampled scopes are printed beside every count.

## Frozen file whitelist

Only these new SRW paths may be created; programme boards may move only in the
commits whose ledger entries name the change:

```text
v16/note-srw-pin.md
v16/code/srw_core.py
v16/code/srw_public_output.txt
v16/code/srw_public_receipt.json
v16/note-srw-core-freeze.md
v16/code/srw_fixture.json
v16/code/srw_score.py
v16/note-srw-fixture-freeze.md
v16/code/srw_output.txt
v16/code/srw_receipt.json
v16/paper-04-support-rewrite-weld.md
v16/note-srw-candidate-verification.md
v16/note-srw-hostile-protocol.md
v16/review-srw-operator.md
v16/review-srw-rewrite.md
v16/review-srw-physics.md
v16/note-srw-adjudication.md
v16/note-srw-repair-freeze.md
v16/note-srw-repaired-artifacts.md
v16/note-srw-terminal-verification.md
v16/README.md
v16/PLAN.md
v16/QUESTIONS.md
v16/LOG.md
STATUS.md
```

Every git stage names explicit paths. The unrelated untracked v15 SCOUT-T
files remain untouched.

## Scope walls

This unit can establish or refute a finite support/rewrite architecture and
classify exact local moduli. It does not derive the complete configuration
catalogue, actualization, a universal future grammar, a continuum, Lorentz
symmetry, metric spacetime, Einstein dynamics, a quantum field, a vacuum,
particle species, scattering, a Hamiltonian, an affine/cosmological constant,
an absolute scale, or a deviation from QFT/GR. An internal multiplicity is not
a particle species. A cycle phase is not automatically gravitational
curvature. A local coupling modulus becomes a physical constant only after
recurrence, gauge invariance, calibration, and cross-arena identity are all
proved.
