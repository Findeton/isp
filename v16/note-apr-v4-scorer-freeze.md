# APR Paper 12 — v4 scorer source freeze

**Date:** 2026-08-19

**Status:** V4 SOURCE FROZEN BEFORE V4 FIXTURE RUN; PRIOR EXPOSURE DEBT RETAINED

**Scientific artifacts:** NO V4 OUTPUT OR RECEIPT EXISTS AT THIS FREEZE

## 1. Frozen source

The scorer-only v4 semantic repair is frozen at:

```text
v16/code/apr_score.py
SHA-256 a7167e21e0a3e6b582acd8ebc8b7464476964cc09131a9721721a4dc99009e80
13,998 lines
```

Its binding semantic-repair pin is:

```text
v16/note-apr-v4-semantic-repair-pin.md
SHA-256 3b0737e3f9a37966659f32a521f56df892cba8d9194530ea4560bc3bbc1d313d
```

The generic core remains byte-identical:

```text
v16/code/apr_core.py
SHA-256 cd51fd36bc26701fdc649ee81f4b048dadde03e645860a7b885c501e2e180ca9
```

Only `v16/code/apr_score.py` moved during implementation. APR fixtures,
protocols, law data, outcome words and precedence, earlier artifacts, and the
indivisible-law scope engraving remain byte-immutable.

## 2. What v4 repairs

The frozen source replaces the four v3 semantic false-positive paths with
exact typed constructions:

- stochastic arrows and subnormalised instruments are different types;
- identities, two-occurrence composition, alternate cuts, tensor products,
  associators, interchange, and nontrivial permutation naturality are
  recomputed from frontier-typed matrices;
- the quantum branch derives histories, Gram rows, unit-normalised divisions,
  a flag dilation, continuation-stable recovery, and a nonzero operational
  interference witness over exact Gaussian rationals;
- generated influence applies distinct interventions through one typed
  schedule and reader; disjoint-source/target contact uses an explicit
  two-factor source-reset, CNOT propagation, and target reader;
- the classifier consumes one primitive law bundle, recomputes every rung,
  and requires a connected consumed-identifier dependency graph and a unique
  residual-selected candidate;
- ontology roles are recomputed from same-law conditioning, writer/reader,
  continuation, and regional rewrite/probe maps rather than action labels or
  supplied before/after tables.

Opaque copied equations, proof booleans, labels, hashes, root strings, and
provenance cannot promote a coordinate. The synthetic positive packages are
scorer-reachability controls only and add no APR physics.

## 3. Source-safe verification

No v4 fixture-evaluating mode was invoked before this freeze. Neither the
implementation worker nor root ran `--run`, `--mutant`, or `--mutants-all` on
these bytes. Root independently repeated only source-safe checks:

- exact SHA-256 and line-count measurement;
- Python AST parsing and `git diff --check`;
- strict no-argument, unknown-argument, forbidden-selftest-destination, and
  relative-run-destination refusals;
- a static call-graph audit over 205 top-level functions: 75 are reachable
  from the self-test, and no fixture loader, official scorer, mutant runner,
  or scientific scorer is reachable;
- no floating-point literal and no `eval` or `exec` call;
- the sole `apr_fixtures` import remains inside `load_frozen_fixture`;
- repository and true off-tree/no-`.git` self-tests. The off-tree directory
  contained only byte-identical scorer/core files plus an `apr_fixtures.py`
  denial sentinel; the sentinel was not triggered.

Both self-tests are byte-identical and report:

```text
status: PASS
checks: 27/27
scientific_fixture_evaluated: false
stderr bytes: 0
stdout bytes: 3,912,631
stdout SHA-256: 0e633936c47d5baf4c5349fac7f5a49e00da04c0848418ff87ad1a4f712721b5
semantic witness SHA-256: 2dd13cc3f2a8e0850ce09b764e147cf37130bdc32d1c93d336f3da932a25cc90
```

Root independently canonicalised the semantic witness tree and reproduced
its outer digest. It also reconstructed all 206 embedded primitive and
measurement payload hashes with zero mismatches.

The 27 exact groups are:

1. `exact-prefix-boolean-and-valuation`
2. `semantic-tree-partition-and-zero-port`
3. `mixed-constructor-refusal`
4. `finite-pushout-vs-validation-and-tagged-union`
5. `typed-classical-positive-coordinate`
6. `typed-classical-frontier-and-composition-drops`
7. `typed-classical-stochastic-and-zero-port-drops`
8. `typed-classical-tensor-law-drops`
9. `typed-classical-naturality-and-identity-drops`
10. `typed-classical-opaque-proof-inert-and-copy-refusal`
11. `typed-quantum-positive-coordinate-and-unit-normalization`
12. `typed-quantum-history-gram-and-state-drops`
13. `typed-quantum-division-flag-and-cut-drops`
14. `typed-quantum-interference-phase-conjugation-controls`
15. `generated-causal-order-positive-and-drops`
16. `generated-contact-cnot-target-reader-positive`
17. `generated-contact-locality-propagation-and-provenance-drops`
18. `primitive-compiler-normalization-atomlessness-heldout`
19. `primitive-overlap-markov-selector-drops`
20. `primitive-congruence-quotient-comparison-drops`
21. `primitive-locality-generated-space-drops`
22. `primitive-dependency-graph-and-forgery-refusals`
23. `primitive-law-selection-and-contact-only-primary`
24. `ontology-role-ladder-and-load-bearing-demotions`
25. `ontology-writer-rewrite-attacks-and-candidate-invariance`
26. `exact-qualifier-scope-exposure-and-law-scope`
27. `machine-reconstructible-witnesses-and-transactional-publish`

## 4. Permanent exposure disclosure

This is source-before-v4-run, not pre-truth blindness. During v2 scorer
repair the implementation worker accidentally evaluated M01 and the full
mutant battery before source freeze. APR's provisional result was therefore
known before v3 and v4 implementation. The v4 receipt and transcript must
carry exactly:

```text
blinding_status: RESULT-KNOWN-BEFORE-V4-IMPLEMENTATION
exposure_debt: PERMANENT-PREFREEZE-M01-AND-ALL-MUTANTS-EXPOSURE
v4_source_frozen_before_v4_run: true
```

Independent black-box reconstruction remains mandatory. This freeze cannot
restore mutual blindness or erase the v2 exposure.

## 5. Law-type and ontology scope

The internal classical, quantum, contact, classifier, writer, and rewrite
packages certify only that the scorer can recognise genuine typed witnesses
and reject their corruptions. They are not the APR candidate law and may not
enter its scientific receipt as evidence.

APR's `AB/BC` object remains a simultaneous regional-gluing diagnostic, not
a transition law or a microscopic classical copula. The sought ISP dynamics
remains an unconstructed indivisible stochastic law between admissible
division configurations. Ordinary probability is lawful; arbitrary inserted
probabilistic divisions are not. Hilbert/history objects remain
representations, Barandes division events are not automatically durable ISP
records, and actualisation remains postulated.

## 6. Authorized next event

After this source and note are committed, the only scientific event is one
transactional run from those committed bytes to the previously absent paths:

```text
v16/apr_output_v4.txt
v16/apr_receipt_v4.json
```

The artifacts must replay byte-identically from repository root, alien CWD,
and true off-tree/no-`.git` environments. An independent verifier must rebuild
the typed classical, quantum, influence, classifier, and ontology controls,
the inherited mutant/descriptor ledger, the APR baseline, and every hash and
scope field without importing scorer measurement functions. Candidate
drafting and the three-seat panel remain prohibited until that audit closes.

## 7. Ontology ceiling

The measured APR construction remains at most a static finite restriction
response on a predeclared commutative prefix algebra, with raw syntactic
atomlessness. The proposed one actual relational web is a candidate, not a
result. This freeze establishes no horizontal regional process, physical
atomless referent, generated contact or causal order, dynamic locality,
indivisible relational transition law, geometry, backreaction, metric,
curvature, continuum, GR/QFT, particles, Hamiltonian, or actualisation.
