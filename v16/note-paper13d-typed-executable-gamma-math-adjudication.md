# Paper 13D mathematical adjudication

Date: 2026-08-20

Status: **TERMINAL MATHEMATICAL ACCEPT-WITH-SCOPE / RUST PIN AUTHORIZED**

## 1. Immutable authority

### 1.1 Candidate corpus

| artifact | commit | SHA-256 | size |
|---|---|---|---:|
| physics pin | `a20c52a` | `722dc3bfe528fc3a52f2d2f5afcaba2c7858250e63d24439e43d0a17ab5c049e` | 459 LF / 14,240 bytes |
| mathematical law | `efe9d97` | `3b91766fe269e8145766f4622f99e9009c3270530670119735e6045a473de5c9` | 1,285 LF / 42,928 bytes |
| construction certificate | `efe9d97` | `93f1e84444d60365fe729d73058dae1f3dede63a87bf1663d4865511255b50bb` | 203 LF / 9,792 bytes |
| hostile protocol | `04f503e` | `4754f89b01c7975ef79de2d9465e54304bbcf2d6e02b942f9957062a63c9eba3` | 301 LF / 13,009 bytes |

### 1.2 Independent reports

| lens | commit | verdict | SHA-256 | size |
|---|---|---|---|---:|
| category/groupoid/readers/tensor/fusion | `0c2bd7e` | `ACCEPT` | `190f6ea125b0d8319714f74cd104ad3b6464fce7c816f08eb2d597ecb8cf64d9` | 434 LF / 20,097 bytes |
| probability/global evaluator/cuts/records | `c2c9743` | `ACCEPT` | `4110f2149c3a424104bd526433314c5370431674ace1e097d9782f9eaf26ed4c` | 424 LF / 20,890 bytes |
| physics/ontology/intervention | `f4a367c` | `ACCEPT-WITH-SCOPE` | `60eaff32be938006e84df124986e3ae4f72c60399380316b7a14b67034676647` | 571 LF / 25,370 bytes |

All hashes and sizes were reauthenticated before adjudication. The reports
were constructed mutually blind, used no implementation, and completed all
62 registered attacks plus nineteen fresh countermodels.

## 2. Verdict

```text
TERMINAL MATHEMATICAL ACCEPT-WITH-SCOPE
```

No report finds a semantic counterexample. The three independent
reconstructions agree on the same ten-coordinate product. Paper 13D therefore
repairs the Paper 13C semantic failures at the level where they occurred:

- mechanism stages form a genuine typed category;
- process programs form a separate exact execution category;
- every boundary invariant is local to its own boundary object;
- a mutable later readout belongs to a new post-endpoint type;
- every stable future is a typed arrow in a generated subcategory;
- erasure is an executable many-to-one arrow outside that subcategory;
- readers do not define the physical quotient;
- intervention and probe addresses have finite constructors;
- response uses a complete point-free contrast object;
- independent tensor is internal to the law's domain; and
- physical fusion is a distinct simultaneous n-ary operation.

This verdict accepts one mathematical candidate. It does not prove that the
candidate is the uniquely selected or fundamental law of nature.

## 3. Binding scope clauses

The following readings are part of the terminal adjudication. They narrow
quantifiers already uniquely determined by the frozen law and do not change a
boundary, probability, generator, hom-set, quotient, reader, or parameter.

### 3.1 Mediation response

Without an $E'$ override,

$$
z_Y=\epsilon\oplus x.
$$

The unit `set(X)` response on $z_Y$ is the coordinate at fixed $E$ (or fixed
$e^0=\epsilon$). Under the tautological context, fair $\epsilon$ makes both
scalar $z_Y$ marginals fair, so that coarse scalar coordinate is zero. The
complete context-indexed response tensor contains both rows. “Unit mediation
response” must never be read as an every-context marginal statement.

### 3.2 Reachable erasure witness

The eraser $E_r$ is total on the entire legal source boundary. Its claim of a
reachable positive-mass identified pair chooses a positive-support relational
packet $a$. Syntactically legal packets satisfying only $d=e'$ may violate the
evaluator truth equations and have zero path mass. Reachability is not claimed
for those zero-mass packets.

For a positive-support packet, both record sectors and a common retained
endpoint have positive mass because every $B$ entry is positive. The eraser
removes perfect future-boundary readability. It need not remove every
statistical correlation with the old record, and it does not delete the
retained past trace.

### 3.3 Empty occurrence unit

Nontrivial “both-sector” language applies when $I\ne\varnothing$. For the
empty occurrence set, the unique empty record word is the correct tensor unit
and projector transport is vacuous. This does not weaken any nonempty record
theorem or the all-size family.

### 3.4 Explicit structural conventions

The presentation groupoid composition uniquely used by the reviews is

$$
(\rho,\upsilon)\circ(\sigma,\tau)
=\left(\rho\circ\sigma,
i\mapsto\tau(i)+\upsilon(\sigma(i))\right).
$$

The zero-component fusion is the deterministic arrow from the tensor unit to
the unique empty atomic boundary; one-component fusion forgets only the
trivial partition and draws no cross seed. These are expansions of the frozen
finite-family/unit definitions, not additional law choices.

## 4. Accepted mathematical product

```text
referent    P13D-POINT-FREE-EXECUTABLE-GAMMA-CONSTRUCTED
law         P13D-ONE-TYPED-EXECUTABLE-GAMMA-CONSTRUCTED
experiment  P13D-TYPED-EXPERIMENT-CATEGORY-CONSTRUCTED
nondivision P13D-NATIVE-B1-CUT-NONDIVISIBLE
record      P13D-TYPED-STABLE-FUTURE-CATEGORY-CONSTRUCTED
eraser      P13D-EXECUTABLE-ERASER-CONTROL-CONSTRUCTED
division    P13D-COMPLETE-DIVISION-FRONTIERS-CONSTRUCTED
size        P13D-VARYING-SIZE-COVARIANT-FAMILY-CONSTRUCTED
response    P13D-RECIPROCAL-RELATIONAL-RESPONSE-CONSTRUCTED
actuality   P13D-ACTUALIZATION-UNCONSTRUCTED
```

### 4.1 Executable referent and experiment category

The referent is the groupoid orbit of a complete reader-free typed experiment
and its possible physical trace. The control hom-table rejects reversed-stage
writes by type mismatch. The execution category separately types primitive
processes, queried continuations, tensor, fusion, stable futures, and erasure.
Program metadata and readers are absent from physical traces.

### 4.2 Point-free quotient and response

Physical mass is the full stabilizer-orbit pushforward; representative mass
is rejected. Diagnostic readers act after the physical fiber is fixed.
Unanchored coordinates use invariant orbit-profiles; oriented coordinates
require physical landmarks. Alternative contrasts use the intersection
stabilizer, so a value-dependent accidental automorphism cannot misalign the
two laws.

The accepted response is exact relational counterfactual sensitivity. It is
not causal direction, chronology, geometric response, backreaction, energy
transfer, or gravity.

### 4.3 Native nondivision and queried divisions

The accepted obstruction is

$$
CB^{-1}=\frac1{175}
\begin{pmatrix}351&-176\\-176&351\end{pmatrix},
$$

and its nonempty tensor powers. It applies only to the declared native carrier,
which excludes $q_0$, seeds, history, phase, and cache. The enlarged
$(m,q_0)$ carrier, identity cuts, and empty process remain positive nonkills.
This is not absolute nonfactorizability or evidence of incomplete reality.

The complete queried $B_1^0$ and $B_1^r$ frontiers pass future sufficiency and
all-field trace equality. Their endpoint law is $B^2$, not $C$; querying is a
different experiment.

### 4.4 Record, future, and eraser

The recorded $B_1^r$ frontier carries through $R_c$, enters a distinct
$B_3^r$ type, and remains exactly readable under every finite word generated
by the stable category, tensor, and record-carrying fusion. Record-label swaps
transport sector names and do not erase distinguishability.

$E_r:B_2^r\to B_3^e$ is a genuine executable control outside the stable
subcategory. It proves that stability is grammar-relative rather than
absolute. Stable record and complete division remain independent predicates.

### 4.5 Varying size, tensor, and fusion

The accepted family is natural over arbitrary finite sets, with exact orbit
multiplicity, uniform deletion, and order-independent exposure of fresh
occurrence and unordered-pair seeds. It is not dormant fixed memory.

Independent tensor retains component structure, has product law, and has no
cross bonds. Physical fusion removes the partition and generates all cross
bonds simultaneously. A deliberately staged fusion is a different physical
trace, not a hidden serialization of the n-ary generator.

## 5. Permanent walls

The accepted mathematics supplies possible histories only. Actualization is
unconstructed. No theorem supplies operational chronology, causal order,
signal cone, dimension, signature, topology, extensive measure, volume,
duration, scale, clock, metric, connection, curvature, stress tensor, energy,
entropy, gravity, general relativity, continuum physics, quantum field theory,
particles, or phenomenology.

Operational control phases are not time. Execution order is not automatically
spacetime order. Cardinality is not event number or volume. Bonds are not
spatial adjacency. A stable record is not automatically a complete division
or actual happening. A later geometric failure may not retune this law.

## 6. Rust authorization boundary

The user authorized Rust implementation only after mathematical acceptance.
That condition is now satisfied. The next and only authorized event is a
separate Rust implementation pin binding:

- the mathematical law SHA
  `3b91766fe269e8145766f4622f99e9009c3270530670119735e6045a473de5c9`;
- this adjudication and its scope clauses;
- exact source, test, result, receipt, and artifact paths;
- transaction and no-overwrite behavior;
- mathematical reconstruction and hostile semantic controls; and
- a strict distinction between code-only and semantic defects.

No Rust source may precede that pin. After the pin, ownership, borrowing,
mutation, serialization, CLI, transaction, memory, and performance defects
may be repaired provided every accepted mathematical object and output stays
unchanged. A changed boundary, probability, seed law, generator, hom-set,
group action, mark, reader, contrast, fusion, future map, eraser, division, or
parameter is semantic and stops implementation for user direction.

There is no automatic Paper 13E and no post-hoc selection of parameters,
dimension, geometry, or desired result.

## 7. Terminal disposition

Paper 13D mathematics is terminally accepted with the three scope clauses in
Section 3. It is now eligible for one separately pinned Rust realization. The
Rust realization, when it exists, must demonstrate fidelity to this accepted
law; it cannot upgrade, repair, or select the physics.
