# QSF Paper 9 panel adjudication — the seam remains unselected

**Date:** 2026-08-18

**Frozen candidate:** `67d8b76e4479bce29e23521ab9eb200d1105e077`

**Protocol:** `75810d2f1bb8ca57ff73525502709ab7d6ce932c`

**Panel grades:** three times `ACCEPT-WITH-FIXES`.

## 1. Frozen panel evidence

| seat | report | ordinary SHA-256 | normalized/self SHA-256 |
|---|---|---|---|
| operator / instrument / completion family | `v16/review-qsf-operator.md` | `58ef7709d41238d9adafe650f00c0d160a1ba9db5ff532f2aeac954663a7c52a` | `0bc88f69b2bd79799bdb092d1c4c2c1b9a7bd543ce29e4af4b0f94eccec16502` |
| composite / steering / causality | `v16/review-qsf-causality.md` | `3f828ff05b1f058ffad8633e12d2a59f8de8f971c6890753b3f8b83f69449143` | `0a206f1775f595e622332c17180bad0ac701d219c2533efd4cbcbc6883794786` |
| histories / division / Barandes / sufficiency | `v16/review-qsf-histories.md` | `5c1d5a65bd5dad283284588c772204e8282085a4299b0fcea688bdfdcda9714e` | `1b24a33e0ce395ddbbc83a786d6173d31e0489e31e63a878fd6542178d7bf4bc` |

All three authenticate the candidate bytes and independently reproduce their
seat's load-bearing arithmetic. Each runs all 20 registered mutants with no
artifact writes and obtains byte-identical clean and true off-tree/no-`.git`
replay from an alien working directory.

The adjudicator independently regenerates the six candidate artifacts with
their frozen hashes and confirms the panel's four decisive new controls:

```text
same-record/same-cell A0 collision                 PASS
terminal phase: moved rays / unchanged basis law  10527 / 10527
tick-2 discarded off-diagonal difference          (448+224 omega)/151875
tick-3 diagonal TV for the hostile pair            99328/4100625
complete final-record quotients                    3,3,27,27,486,486
```

The adjudicator scratch implementation is outside the corpus, SHA-256
`f720468b6e383195ed09de0dea8388a104be53d7638996034ca50fe378abcaf3`.
It is a verification receipt, not a new source.

## 2. Primary ruling

The panel is unanimous and the adjudicator agrees:

```text
QSF-METHOD-INCONCLUSIVE
```

survives as the candidate's primary. It survives for a narrower and more
informative reason than the delivered paper states. QSF has ruled out several
specific identifications, but it has not selected one complete operational
base law:

```text
Arm A: standard affine completion family not solved or selected;
Arm B: natural fixed-factor pure-ray extension has remote-setting dependence;
Arm C: rho is insufficient at every retained boundary tested, while a finer
       stochastic kernel exists and is not thereby a standard quantum theory;
S1:    suffix partitions measured; complete predictive-state assay unexecuted.
```

The literal WRC rule is not repaired. CRL, a classical-feedback law, a future
relational composite, and a Barandes fine-state law are not selected by this
adjudication. Dynamic geometry and growth remain downstream-unbuilt.

## 3. Arm A — lookup, branch law, and aggregate law are three objects

The rank-one theorem remains exact. Every affine CP operation with effect
`E_c=|e_c><e_c|` is

```text
J_c(rho) = Tr(E_c rho) sigma_c,
```

with one fixed normalized positive `sigma_c`; arbitrary choices form a
complete instrument because the effects sum to the identity. The displayed
projective control still preserves zero of nine WRC families, while another
member fits one registered continuation and a cyclic third member confirms
the family is not exhausted by those two points.

The delivered A0 formula is false at the record grain frozen by the pin. The
histories `(0,12,13)` and `(0,13,12)` reach the same complete count record,
admit the same next outcome `c=0` with nonzero weight, and demand different
normalized output rays. Their normalized coordinate two is respectively

```text
11/3 - (2/3) omega,
8/3  + (1/3) omega.
```

Thus one `sigma_(record,c)` cannot interpolate the reached process. Enlarging
the key to include the ray or full history makes interpolation exact only as a
finite state/history lookup; it is not one affine density-operator map. The
candidate's hard-coded `a0_context_exact=true` is rejected.

The raw local signature census remains a valid A1 branch obstruction. A
translation-aligned census independently leaves all 30 registered local keys
conflicting, so covariance does not rescue literal branch reproduction. But
branch failure does not imply aggregate failure. Multiplying coordinate zero
of every terminal output by `omega` changes the ray in all 10,527 terminal
contexts while preserving every basis probability and all nine registered
aggregate families. The aggregate A1/A2 question therefore remains distinct.

The scorer constructs no aggregate output-state variables, positivity cone,
mixed-state propagation, recurrence/covariance equations, target equations,
or operational-null quotient. `aggregate_variety_closed=false` is an
answer-bearing literal, not a measurement. The licensed Arm-A word is the
registered method-nonentry word; the paper must not call the unconstructed
object a measured variety or infer its emptiness.

## 4. Arm B — a real fixed-factor count-record witness, not yet spacetime

The exact `2 x 27` Bell/HJW surrogate is correct. Alice's two complete local
instruments remotely prepare Z and X decompositions of one Bob density
operator. After Alice outcomes are summed, Bob's literal ordered-history and
count-record laws have total variations

```text
ticks 1..5 history: 0, 1/2, 211/324,
                    180223/236196, 1694745299/2066242608
ticks 1..5 count:   0, 1/2, 211/324,
                    12415/17496, 820725659/1162261467.
```

The count record is the decisive calibrated witness at ticks two and three;
the result does not depend on treating Bob's ray or full ordered trace as a
local observable. A second exact mutually unbiased Alice basis reproduces the
first three rows, while outcome relabeling and a no-steering control return
zero. Any Alice-independent affine local Bob instrument yields a history
probability linear in the common `rho_B`, so the projective and a distinct
fixed-output control correctly give zero.

The ruling is deliberately not “faster-than-light signalling.” The surrogate
has no relational separation, distance, light cone, changing Bob algebra, or
post-preparation cross-factor interaction. What is proved is

```text
REMOTE-SETTING-DEPENDENCE IN THE NATURAL NO-INTERACTION FIXED-FACTOR
EXTENSION, AT BOB'S COUNT-RECORD GRAIN.
```

That rejects this extension when Alice outcomes and Bob count facts are
physical. A relational composite is still unbuilt; its absence is
incompleteness, never a no-signalling theorem. Any future ontic decomposition
law owes both steering reproduction and a general no-signalling proof.

Erasing Bob's readout and erasing feedback are different controls. The former
hides the early count signal but leaves an ordinary process screen difference
`1664/19683` at tick four; the latter gives the affine/unitary null. The repair
must bind that distinction and all five available no-interaction windows.

## 5. Arm C — density-state insufficiency is not failure of stochastic law

The five-window retained **state-record block** witness survives. At tick one
the count distributions agree; the correlation between record sector and
conditioned process state differs. The paper's phrase “retained count-record
sectors distinguish them at every window” is therefore corrected.

The registered Z/X diagonal screen is too weak to characterize an all-input
map. For the equal-density preparations

```text
{|0>,|3>} / 2
and
{(3|0>+4|3>)/5, (-4|0>+3|3>)/5} / 2,
```

the discarded full state after tick two differs in 72 entries while every
basis diagonal agrees. Entry `(1,7)` differs by
`(448+224 omega)/151875`; the projector onto `(|1>+|7>)/sqrt(2)` detects
`112/50625`. Nonaffinity is therefore present at tick two, not first at the
paper's later diagonal screen.

The registered Arm-C token is retained only with this binding expansion:

```text
QSF-HISTORY-NO-AFFINE-RECORD-BOUNDARY-WITHIN-1-5
[NO rho-SUFFICIENT RETAINED STATE-RECORD MAP]
```

It does not say no fine stochastic division exists. If one explicitly takes
the state to be `([psi],n)`, WRC defines the normalized kernel

```text
([psi],n) --c, probability |(C_n psi)_c|^2-->
([S C_n psi], n+e_c).
```

This kernel acts linearly on probability measures over the fine state. Calling
the pure ray ontic is an additional ontology; its natural fixed-factor
extension is exactly what Arm B rejects. Density-operator CP is a condition
for a `rho`-complete quantum representation, not Barandes's definition of a
division event. The v12 converse from division to record is model-scoped and
may not be universalized here.

QSF has not tested cut composition, record recoverability under a closed
continuation catalogue, or actualization at a fine stochastic boundary. No
history-base promotion is licensed.

## 6. S1 — the published table is suffix-only

The six published numbers are exact for distributions of future ordered
trigger suffixes. They omit the calibrated final count record and final
process state, so they are not JS-S1a's complete future instrument. At the
final-record grain the six quotients are

```text
3, 3, 27, 27, 486, 486,
```

not `1,1,2,15,126,485`. The complete record law distinguishes every reached
history in this finite range. That does not prove ordered history is ontology:
the law-native present state `([psi],n)` screens off the past by construction.
For suffix prediction, `[psi]` plus `n mod 3` suffices; for the absolute future
count record, exact `n` is required.

The next-hit vector supplies an unregistered coarser sufficient statistic at
the `past=3,future=1` suffix row. At future horizon three the suffix quotients
reach `3,27,486`, so no stabilization occurs within the extended finite range.
The repair must relabel the old table `ORDERED-FUTURE-SUFFIX-ONLY`, add the
complete-grain table and present-state control, and replace “S1a executed” by
“complete S1a not entered.” S1b remains unentered for both reasons: the law
family is unclosed and its prerequisite complete S1a object is absent.

## 7. Ontology ruling

The candidate's phrase “what remains real is a fixed-carrier pure-ray and
count-record process” is too strong. The exact ontology ledger is:

| object | status after QSF |
|---|---|
| one WRC algorithmic pure-ray/count trajectory | a mathematically coherent fine stochastic candidate when explicitly postulated |
| pure ray | ontic only in that extension; representational in Barandes's default reading |
| count field | a declared finite-window readout/feed-forward variable; absolute permanence and actualization unproved |
| density operator | insufficient for WRC's literal retained output |
| standard affine completion | lawful family, aggregate feasibility/selection unclosed |
| fixed-factor composite | constructed surrogate; natural pure-ray extension rejected by its count-record witness |
| relational composite / geometry | unbuilt |

Barandes's fixed configuration space can in principle contain fine states and
one actual trajectory, with Hilbert objects relegated to representations. QSF
does not construct that full configuration law or prove that WRC realizes the
published stochastic-quantum correspondence. It identifies the exact price of
taking the ray as additional reality.

## 8. Integrity and hard-coded lineage

The candidate's sealing and replay machinery is strong. The scientific defect
is that A0 exactness, aggregate nonclosure, A1/A2 dispositions, S1b non-entry,
and the synthesis comparator are partly carried by answer-bearing literals.
The `a0-output` mutant changes a horizon rather than an A0 operation. The
primary happens to be conservative and correct, but must be rebuilt from
derived arm-status objects under RUNBOOK E-36.

## 9. Scope wall

QSF does not select a fundamental dynamics or construct a relational
composite, carrier/relation rewrite, dynamic geometry, gravity, metric,
curvature, Lorentz/continuum spacetime, fields/Fock space, particles/species,
Hamiltonian, constants, empirical deviations, record actualization, or a
general EPR/Bell model. A fixed-factor witness may not be promoted to a
spacetime theorem; a fine stochastic kernel may not be promoted to standard
quantum mechanics.

## 10. Ordered bounded repair

A forward-only bounded repair is authorized before terminal promotion. The
generic core, physical fixture, WRC source packet, old exact branch and TV
values, and primary may not move. The repair must:

1. replace record-indexed A0 with the exact collision and retype exact
   state/history interpolation as a lookup;
2. derive every Arm-A/method-status Boolean, add a translation-aligned branch
   census, a third completion, and the terminal-phase aggregate control;
3. call A1/A2 aggregate feasibility **unentered**, not a measured variety,
   unless the full positive mixed-state problem is actually solved;
4. derive the primary from independently constructed arm-status objects;
5. gate Bob count-record TVs through ticks one to five, the phase-basis and
   outcome-relabel controls, the generic affine-history theorem, and the
   readout-versus-feedback distinction;
6. construct full discarded densities and full retained state-record blocks,
   add the tick-two off-diagonal witness, and retype Arm C to `rho`
   sufficiency;
7. relabel S1 as suffix-only, add the complete final-record quotients,
   future-horizon-three nonstabilization, and fine present-state controls;
8. state the process-level versus representation-level lawfulness distinction
   and remove ontic-ray language except as an explicit extension;
9. add direct, upstream mutants for A0 collision, aggregate control, count
   readout, outcome relabeling, tick-four screen, density witness, complete S1,
   and primary derivation; and
10. rerun two clean generations, every old and new mutant, and a true
    off-tree/no-`.git` alien-CWD replay before a separate terminal entry.

Any change to the fixed source process, HJW preparation, old five-window
values, WRC regression, primary, or frozen fixture is a new candidate rather
than this repair. Paper 10 may be pinned only after this bounded repair is
terminally verified; its construction remains separately held.
