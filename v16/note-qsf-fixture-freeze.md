# QSF result-neutral physical-fixture freeze

**Frozen:** 2026-08-18, v16 ledger #114.

The physical binding in `v16/code/qsf_fixture.json` is frozen after the generic
core commit and before any QSF physical scorer exists. It contains the terminal
WRC source packet, hash-bound observable receipt path, three registered assay
arms, S1 windows, controls, and scope walls.

It contains no answer word, expected branch, survivor count, completion
dimension, signalling value, affinity verdict, predictive quotient, or paper
claim. The natural tensor-factor construction is explicitly typed as a
fixed-factor surrogate; `relational_composite_built` is false. Thus any later
fixed-factor signalling witness cannot silently become a theorem over every
future relational composite.

The fixture SHA-256 is
`7fe949383d1e27017b972f941c4cf1875d3c574d3120d9993240bd7a0756404f`.
No QSF physics result is citable at this event.
