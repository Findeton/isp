# Paper 14 stable-happenings and premetric-order pin

Date: 2026-08-20

Status: **RESULT-NEUTRAL PIN / NO OUTCOME PRE-AWARDED**

normalized_sha256: 5054cfd38e89b66d1dc2b82dd510e7bb1616253c84cfd7ec18760db69e19878d

## 1. Authority, chronology, and hard boundary

The user explicitly stopped the Paper 13 verifier-repair chain and authorized
scientific work on Paper 14, followed conditionally by Papers 15 and 16. This
pin opens one bounded analytical Paper 14 construction. It does not reopen,
import, repair, execute, or cite any Paper 13 candidate as an accepted physical
law.

The working research drafts

- `v16/paper-14-stable-happenings-and-premetric-order.md`,
- `v16/paper-15-conditional-spacetime-reconstruction.md`, and
- `v16/paper-16-joint-curvature-response-and-conditional-gr.md`

are preparation only. Their pre-pin bytes and positive sentences are not
evidence and do not award an outcome. The Paper 14 construction may replace
and narrow the first draft after this pin is committed. Papers 15 and 16 may
not be authoritatively constructed, evaluated, or promoted during this unit.

The preparation boundary is committed at
`b9fd67f1484868e627fb10b1492400ed529fbf27`. Its three research-draft hashes
are, respectively,

```text
8649856ca32217333af8cd92051cd02d1706cab054e4ad4cad0fb37a5482acdc
5d6342b724c0dc036d298dcec58771af646a1d3136b1a40c42bdd8c877668a22
e67e18fbe9a714b2f7111327d3c086f585f5903144d94db38be31b15ad92a0e2
```

They are authenticated provenance only, never expected-answer anchors.

The shared worktree also contains seven pre-existing untracked, out-of-scope
paths. They are not whitelist entries, must never be read by the Paper 14
instrument, and must remain byte-identical throughout this cycle:

```text
ce895faf612448c58961412c9e9d3a5f8521234ebef13b904eb15a96a2105140  v15/code/scoutt_exact.py
743ca7b55282c9b754101f4fac465e0000799c327bc3769be44e1c5cbe345428  v15/code/scoutt_output.txt
38c3f6cb288e8f502c678c3d2a67b292e4b209a54129e5f8cf0e5f2248876a74  v15/code/scoutt_parent_scoutk74.py
5af53face0937b771fe58d122720c7cb0dfebb3c01ba11c23d55f1a369502a1f  v15/code/scoutt_parent_scoutk74_receipt.json
330f9e215294f42d2a0435230d6123352654e7696e86b281f950e5e0fc30c652  v15/code/scoutt_receipt.json
36fd0a0efaee4909d5a7b0a39ae326ceccbbed59541e5ab3a1c3cb3a05f04947  v15/note-scoutt.md
a4dfdee1ea43f8b83f5e3e04cf5dbe77d2f332505b5727824fbe82faef083268  v16/code/p13_gamma_exact_v5.py
```

Their continued appearance in `git status --short` is a declared dirty-tree
baseline, not permission to stage, modify, import, or cite them. Stage audits
compare these hashes before evaluating the active whitelist.

The unit is analytical and physics-facing. It permits one compact exact
reconstruction instrument for finite censuses and hostile countermodels. It
does not permit an iterative verifier-repair programme. A failure of the
mathematics produces the preregistered negative result and ends the unit; it
does not authorize v2/v3 source repairs.

## 2. One-box question

> Can one explicitly declared point-free history law construct
> presentation-independent stable-happening bundles, distinguish them from
> complete stochastic division frontiers, derive an intrinsic locally finite
> dependency order and interval-finite additive valuation, and exhibit
> reciprocal matter--relation--matter response without importing a clock,
> geometry, metric, stress tensor, or actualization rule?

The question has two possible high-level answers:

1. a bounded point-free abstract physical model exists at the stated scope;
   or
2. the construction stops at the first failed referent, stability, frontier,
   order, growth, or valuation coordinate.

Neither answer selects the declared law as Nature's law.

## 3. Operational sequence

The construction and receipt perform these steps in this order.

1. **Authentication and referent census.** List every primitive, derived
   object, representation, sample space, equality, and actual/possible status.
   State whether the law is inherited, derived, or newly postulated.
2. **Complete-law census.** Define the full finite/varying-size history law,
   its domains, typed boundaries, records, admissible continuations, and
   presentation groupoid before defining a happening.
3. **Stable-record theorem.** Prove record-sector orthogonality, completeness,
   and persistence for every finite licensed continuation word. Retain an
   explicit eraser outside that grammar as a negative control.
4. **Point-free onset identity.** Define onset germs using complete local
   law, boundary, record, and continuation data; quotient whole structures by
   typed relabeling and harmless refinement while preserving multiplicity.
5. **Possible/actual separation.** Construct possible stable-happening types.
   Actual happenings remain conditional on the actualization postulate.
6. **Frontier classification.** Test stable records and complete division
   frontiers independently using exhaustiveness, exactly-once crossing,
   complete state, future sufficiency, positivity, normalization, cut
   equality, grammar closure, and no-history-smuggling.
7. **Dependency extraction.** Derive essential operational dependency without
   reading a word position, event ID, port address, rank, or serialization
   order. Quotient mutual dependence into indivisible bundles.
8. **Order and clock test.** Prove the bundle relation is a partial order and
   that exchanging incomparable operations leaves the complete law,
   histories, records, frontiers, and valuations unchanged.
9. **Uniform growth and local finiteness.** Give one recursive fresh-sector
   rule valid for every finite size; prove genuine carrier growth, finite
   branching, and finite closed intervals without using a fixed dormant
   memory bank.
10. **Valuation.** Separate unit count, contextual whole-history surprisal,
    and intrinsic occurrence weight. Require strong-diamond/context descent
    before an unequal weight is attached to a happening.
11. **Reciprocal response.** Holding one law fixed, vary matter input, measure
    the relational record response, and then measure a later matter response.
    Keep this coordinate explicitly pre-geometric.
12. **Outcome derivation.** Derive every coordinate from the named preceding
    measurements. The earliest failed prerequisite caps only the affected
    coordinate; it must not erase an independently surviving theorem.

The referent census is the first scientific step. No later positive result can
repair a failed referent retrospectively.

## 4. Abstract physical input and ontology bindings

The candidate may introduce exactly one declared law package

\[
\mathfrak I=
(\mathcal C,\mathcal G,\Gamma,\operatorname{Div},\mathcal B,\rho).
\]

Its meanings are fixed as follows.

- `C` is the typed varying-boundary configuration/history language.
- `G` is the groupoid of complete typed presentation changes.
- `Gamma` is one normalized probability law over complete possible histories.
- `Div` is a set of candidate frontiers, not a declaration that they divide.
- `B` is the typed family of record-sector projectors.
- `rho` is the actualization postulate, external to `Gamma`.

The construction must choose and print exactly one provenance label for
`Gamma`:

```text
DERIVED-FROM-ACCEPTED-LAW
DECLARED-NEW-LAW-POSTULATE
UNBOUND-LAW-INTERFACE
```

Because Paper 13 is nonterminal and noncitable, this pin supplies no accepted
parent from which `Gamma` can presently be derived. Unless a different
terminal committed parent is exhibited in the referent census, the honest
positive provenance is `DECLARED-NEW-LAW-POSTULATE`. Calling that declaration
derived is a kill.

`Gamma` governs possible histories. `rho` chooses at most one actual history.
The law, amplitude/probability representation, possible events, actual events,
record sectors, divisions, order, and valuation must occupy separate rows in
the term/sample-space table.

### Four-gate ledger

Every new object must expose all four fields below.

| object | referent | necessity | no-smuggling | discriminator |
|---|---|---|---|---|
| onset germ | complete local boundary/law/record change | raw occurrence rows overcount presentation | contains no label/address identity | clone/refinement controls |
| stable happening type | onset germ plus all-word record persistence | possible alternatives may later interfere | contains no actuality flag | eraser and reconvergence controls |
| actual happening | selected stable type | ontology needs one actual history | supplied only by `rho` | possible-versus-actual table |
| division frontier | exhaustive typed cut and sufficient complete state | stable facts need not restart the whole law | contains no hidden history ID | stable/incomplete four-product controls |
| dependency edge | essential operational/law dependence | order cannot be read from execution text | contains no sequence index or rank | commuting diamond/common-cause controls |
| happening bundle | mutual-dependence class with multiplicity | symmetric co-creation need not be oriented | contains no arbitrary representative | swap-symmetry control |
| unit measure | number of physical bundle occurrences | count is the minimal extensive valuation | counts structures, not orbits/labels | clone and two-versus-one controls |
| intrinsic unequal weight | descended conditional law factor | unit count may miss law intensity | no hand-entered weight | strong-diamond and correlated-antichain controls |

## 5. Point-free equality and multiplicity

Physical equality is isomorphism of the complete typed incidence, record,
value, law, and continuation structure. It is not equality of hashes, labels,
addresses, list order, or node-orbit counts.

An automorphism exchanging two anonymous incomparable nodes preserves two
physical occurrences; it does not collapse them to one orbit. Conversely, a
duplicate presentation row with no changed onset, record, support, provenance,
or future profile is not a new happening.

Harmless refinement must come with explicit maps showing that record readout,
future law, dependency, frontier status, and measure descend. A refinement
that changes any of those is a changed physical object, not presentation.

## 6. Stability and division are independent coordinates

For writer branch maps `V_r` and record projectors `P_r`, the construction
must prove

\[
\sum_rV_r^\dagger V_r=I,
\qquad
P_rV_s=\delta_{rs}V_s.
\]

Every generator `F` of the sealed future grammar must satisfy the typed
intertwiner

\[
P_r^{\rm out}F=FP_r^{\rm in}.
\]

Induction must cover every finite word, not a finite depth table. The theorem
is grammar-relative: reactivating the record port with an exact inverse may
erase it and must remain a visible counterexample.

A division frontier is separately required to pass all of:

1. record persistence when a record is present;
2. exhaustive and exactly-once history crossing;
3. complete typed carrier, matter, relation, and carried-record state;
4. future sufficiency for equal complete frontier arguments;
5. a positive normalized continuation kernel;
6. direct-versus-cut equality on every admitted input;
7. closure under the licensed future grammar;
8. no hidden history, phase, cache, path label, or clock; and
9. invariance under reordering incomparable local happenings.

The receipt must exhibit all four logical combinations of stable-happening
status and complete-frontier status. No sentence may say every stable
happening is a Markov step.

## 7. Dependency, bundles, and local finiteness

The raw dependency relation is an operationally defined preorder. If
`e` and `f` depend essentially on each other, they form one indivisible bundle
under the equivalence

\[
e\sim f
\Longleftrightarrow
(e\preceq f\ \text{and}\ f\preceq e).
\]

The quotient must be proved reflexive, antisymmetric, and transitive. A
symmetric co-onset such as `(0,0) -> (1,1)` is the mandatory countermodel: it
earns one two-component bundle, not an arbitrary order between components.

The no-hidden-clock theorem must compare complete physical outputs for both
serializations of every declared commuting diamond. Equality of final labels
alone is insufficient.

The uniform family must use genuinely fresh carried sectors. For every finite
shape with `n` cells, the construction derives rather than types its bundle,
record, and positive-history counts. General local finiteness must be proved
from finite branching and intrinsic dependency depth; a numerical census at
small `n` is only a control.

## 8. Valuation and strong-diamond descent

The unit counting measure is

\[
\mu_1(R)=\#R
\]

on physical bundle occurrences, with additivity on disjoint regions and
invariance under whole-structure isomorphism.

A contextual history surprisal

\[
S_\Gamma(H)=-\kappa\log\Gamma(H)
\]

is always a history quantity when `Gamma(H)>0`. It becomes a sum of intrinsic
occurrence weights only if every admissible predecessor context and every
neutral relinearization yields the same local conditional factor. The strong
diamond test compares those factors, not merely their product.

Correlated incomparable events whose two path products agree but whose local
conditionals move are a mandatory negative. A typed common-cause fork that
screens the children is the mandatory positive unequal-weight control.

No logarithm is evaluated numerically. Exact rational probabilities and
symbolic formal-log atoms are the substantive representation.

## 9. Registered exact constructions

The construction must independently derive, rather than accept as expected
answers, these open mathematical targets.

### 9.1 Minimal point-free history frame

It must determine whether one finite persistent-set frame can have:

- two exchange-symmetric co-created record components;
- one independent commuting onset;
- one strict downstream dependent onset; and
- a whole-history presentation quotient preserving multiplicity.

The candidate design has four raw persistent facts, three physical onset
classes, five reachable record states, five trace-history classes after the
commuting-word quotient, and three bundles with one incomparable pair and one
two-parent successor. Any smaller/minimality sentence requires a proof; a
wrong count or unproved minimality narrows the claim rather than being fixed by
typing the target number.

### 9.2 Reciprocal finite law

The declared binary calibration kernel is

\[
B=\frac1{25}
\begin{pmatrix}
9&16\\
16&9
\end{pmatrix}.
\]

For root matter bits `a,b`, relational record `g`, and later matter record
`y`, the candidate law is

\[
\Gamma(a,b,g,y)=\frac14 B_{g\mid a\oplus b}B_{y\mid g}.
\]

The instrument must enumerate the complete finite sample space, normalization,
presentation symmetries, frontier sufficiency, strong-diamond factors, and
the changed-law controls. The values `7/25`, `7/25`, and `49/625` are target
coordinates to be derived, not hard-coded acceptance constants.

### 9.3 Uniform fresh-port family

Fix a declared finite branching bound `q >= 1`. Each consumed open port may
create fresh local bundles `A_pi,B_pi,G_pi,Y_pi`; `Y_pi` exposes `q` fresh
ports. Port addresses and sibling order are presentation only. The complete
unlabeled rooted incidence-and-value structure is physical.

For every declared finite prefix-closed shape `T`, the candidate law is the
product of one common local normalized factor. The paper must prove the
all-size normalization, trace invariance, genuine growth, and locally finite
direct-limit statements. Finite evidence must cover multiple nonisomorphic
shapes and more than one `q`; it cannot replace the proof.

## 10. Preregistered product outcomes

The receipt reports each coordinate separately.

### Law/referent

```text
P14-LAW-REFERENT-UNBOUND
P14-DECLARED-POINT-FREE-HISTORY-LAW
P14-DERIVED-POINT-FREE-HISTORY-LAW
```

### Stable-happening identity

```text
P14-ONSET-GERMS-PRESENTATION-ONLY
P14-POSSIBLE-STABLE-HAPPENING-TYPES
P14-INDIVISIBLE-STABLE-HAPPENING-BUNDLES
P14-ACTUAL-STABLE-HAPPENINGS-CONDITIONAL-ON-ACTUALIZATION
```

### Dependency/order

```text
P14-DEPENDENCY-UNCONSTRUCTED
P14-PRESENTATION-COVARIANT-DEPENDENCY-PREORDER
P14-INDIVISIBLE-HAPPENING-BUNDLE-POSET
P14-LOCALLY-FINITE-BUNDLE-POSET
```

### Frontier, reported per cut family

```text
P14-FRONTIER-INCOMPLETE
P14-COMPLETE-INTERFACE
P14-COMPLETE-DIVISION-FRONTIER
```

### Valuation

```text
P14-NO-EVENT-MEASURE
P14-UNIT-COUNTING-MEASURE
P14-CONTEXTUAL-GAMMA-HISTORY-WEIGHT
P14-INTRINSIC-GAMMA-ATOMIC-WEIGHT
P14-INTERVAL-FINITE-ATOMIC-MEASURE
```

### Geometry

```text
P14-NO-METRIC
P14-FINITE-PREMETRIC-WITNESS
P14-PREMETRIC-ORDER-AND-INTERVAL-MEASURE
```

The strongest allowed mathematical ceiling is

`P14-PREMETRIC-ORDER-AND-INTERVAL-MEASURE`

alongside either `P14-DECLARED-POINT-FREE-HISTORY-LAW` or a genuinely
authenticated derived-law label. It does not imply chronology, dimension,
volume, duration, topology, metric, curvature, energy, gravity, GR, or QFT.

If the law is merely declared, the paper must say so at every contribution
that depends on it. The word `physical` then means an object of that declared
model, not an empirically selected law.

## 11. Scientific hostile controls

All controls change the mathematical object, not Python implementation trivia.

| ID | changed object | required result |
|---|---|---|
| H1 | clone a record label with no changed support, provenance, or future | no new happening or measure |
| H2 | split one bookkeeping arrow into two with no intermediate fact | one happening |
| H3 | reverse a commuting diamond serialization | exact non-kill everywhere |
| H4 | add a licensed record eraser | stability fails for that grammar |
| H5 | expose one stable record while a hidden bit controls the future | stable yes, frontier incomplete |
| H6 | corrupt a cached count | recomputed measure unchanged |
| H7 | claim unbounded growth with fixed dormant memory | collide at finite capacity |
| H8 | insert unequal weights by hand at fixed `Gamma` | intrinsic-weight gate fails |
| H9 | hold order fixed and change positive history law | valuation may change |
| H10 | compare equal-count chain and antichain | order differs |
| H11 | let one history hit a frontier twice | exactly-once/normalization fails |
| H12 | omit positive histories from a frontier | exhaustiveness fails |
| H13 | use a correlated antichain with moving local factors | history surprisal only |
| H14 | insert infinitely many events into a fixed interval | local finiteness fails |
| H15 | multiply records from boundaries lacking common transport | product undefined |
| H16 | declare dependency without operational entailment/influence | edge refuses |
| H17 | add a history hash after future sufficiency fails | enlarged-history non-kill only |
| H18 | add coordinates or distances to orient incomparable events | imported geometry quarantined |
| H19 | symmetrically co-create two facts | one two-component bundle |
| H20 | count automorphism orbits instead of occurrences | multiplicity gate fails |
| H21 | use weak product equality with context-dependent factors | unequal atom descent fails |
| H22 | condition a fork on a typed screening common cause | unequal intrinsic weights may pass |
| H23 | replace fresh sectors by reusable fixed slots | genuine-growth gate fails |
| H24 | relabel or reorder serialization only | required full-law non-kill |
| H25 | mark every stable happening as a division | four-product frontier control fails |
| H26 | use construction rank as physical time | no-hidden-clock gate fails |

The instrument must also include at least six fresh scientific countermodels
chosen after its source is frozen. Fresh cases may not retune definitions,
outcomes, or the finite law.

## 12. Evidence and receipt contract

The analytical paper is the primary scientific artifact. A compact exact
instrument supplies finite reconstruction and receipt evidence; it is not the
source of the general theorems.

The instrument must:

- use exact integers, `Fraction`, and symbolic formal-log atoms only;
- contain no float literal, random seed, fitted tolerance, hidden answer table,
  Paper 13 import, network access, or Git dependence;
- construct every finite state, history, quotient, frontier, dependency row,
  and control from typed primitives;
- compute every count and residual rather than typing it into the receipt;
- run twice with byte-identical canonical JSON output;
- complete from repository root, an alien current directory, and a true
  source-only/no-`.git` copy;
- reject unknown/incompatible CLI modes and refuse overwrites;
- deliberately mutate one declared calibration and exit nonzero before any
  positive outcome is emitted;
- remain under 60 seconds and print progress at 30 seconds if still running;
  and
- write no file except an explicitly absent destination in publication mode.

The receipt must bind:

1. source, pin, paper, construction note, and input hashes;
2. the complete term/sample-space and four-gate tables;
3. provenance of `Gamma`;
4. every finite census and general theorem dependency;
5. all frontier rows and direct-versus-cut comparisons;
6. all dependency/bundle/order and trace-invariance rows;
7. unit and unequal-weight descent rows;
8. every H1--H26 changed object and disposition;
9. fresh-control chronology;
10. outcome prerequisites and earliest-failure derivation;
11. total read/write ledger and exact path whitelist; and
12. a promotion-time total seal over every published key.

Every promotive paper number must render from the receipt or be marked
`PROOF-DERIVED-NONNUMERIC`. A boolean, count, hash, or copied paper sentence
cannot substitute for the complete object it claims to verify.

## 13. Runtime and implementation bound

This is one bounded source, not an engineering research programme.

- Candidate source limit: 1,500 nonblank lines.
- Candidate runtime cap: 60 seconds per complete mode.
- Peak-memory target: 512 MiB; exceeding 1 GiB is procedural invalidity.
- Public modes: `--selftest`, `--run --output PATH --receipt PATH`, and one
  named changed-object mode.
- No optimization or source repair is authorized after fresh controls or an
  official run begin.
- A mathematical negative or procedural invalidity is frozen as delivered.

Implementation defects that do not move a scientific object may narrow
reproducibility status, but they do not automatically demote a proven physics
coordinate. Conversely, a scientific countermodel stops at its earliest
affected coordinate even if every software check is green.

## 14. Exact staged whitelist

No path outside the active stage may change.

### Stage A — pin freeze

```text
v16/note-paper14-stable-happenings-premetric-pin.md
v16/LOG.md
STATUS.md
```

The pin commit precedes every construction-source byte and every official
finite receipt. Existing research-draft bytes remain nonbinding.

### Stage B — construction and source freeze

```text
v16/paper-14-stable-happenings-and-premetric-order.md
v16/code/p14_premetric_exact.py
v16/note-paper14-premetric-construction.md
v16/LOG.md
STATUS.md
```

At source freeze, fresh-control contents and official output/receipt do not
exist.

### Stage C — fresh controls and official result

```text
v16/p14_premetric_fresh_cases.json
v16/p14_premetric_output.json
v16/p14_premetric_receipt.json
v16/note-paper14-premetric-verification.md
v16/LOG.md
STATUS.md
```

The frozen source is not edited in this stage.

### Stage D — hostile protocol freeze

```text
v16/note-paper14-premetric-hostile-protocol.md
v16/LOG.md
STATUS.md
```

### Stage E — three blind reports, separate commits

```text
v16/review-paper14-premetric-referent.md
v16/review-paper14-premetric-probability.md
v16/review-paper14-premetric-order-measure.md
v16/LOG.md
STATUS.md
```

Each report is the sole new scientific path in its report commit.

### Stage F — adjudication

```text
v16/note-paper14-premetric-hostile-adjudication.md
v16/LOG.md
STATUS.md
```

Any repair requires a separately authorized pin. This pin does not authorize
one. Paper 15 remains nonbinding until Stage F confers a terminal Paper 14
coordinate and explicitly authorizes a Paper 15 pin.

## 15. Hostile-review lenses

The Stage D protocol must bind at least these independent lenses.

1. **Referent/ontology:** whole-structure quotient, multiplicity, refinement,
   possible/actual separation, law provenance, no imported geometry.
2. **Probability/division:** stable-word theorem, eraser scope, exhaustive
   frontiers, future sufficiency, positivity, cut equality, nondivision.
3. **Order/valuation:** operational dependency, mutual bundles, trace
   invariance, local finiteness, unit measure, strong-diamond descent, growth.

Every reviewer reconstructs the open finite law independently from the pin
and paper without importing the candidate source. Verdicts are `ACCEPT`,
`ACCEPT-WITH-FIXES`, or `REJECT`, with the first exact scientific
counterexample and earliest affected coordinate.

## 16. Permanent scope engravings

This unit does not decide or construct:

- which possible history is actual;
- a selected fundamental law or coupling;
- a universal event grammar;
- an absolute or approximately universal division doctrine;
- chronology, causal cones, topology, dimension, signature, or scale;
- spatial volume, proper time, a metric, connection, or curvature;
- stress-energy, entropy, temperature, energy flux, gravity, or GR;
- a continuum limit, QFT, particles, or phenomenology; or
- attribution of ISP's happening construction to Barandes.

The Barandes-compatible content is only the scoped indivisible-process idea:
probabilities need not factor at arbitrary cuts, while selected complete
division frontiers may admit conditioning. Stable happenings, the point-free
quotient, fresh-sector growth, and the order/valuation construction are ISP
proposals.

## 17. Stopping rules

1. First referent failure stops every dependent positive coordinate.
2. Stable-record failure does not by itself decide the frontier coordinate,
   and frontier failure does not erase a separately stable record.
3. Dependency-preorder failure blocks bundle-order and geometry entrance but
   not the finite stable-record theorem.
4. Intrinsic-weight failure leaves unit measure and contextual history
   surprisal available.
5. Runtime, transaction, or receipt failure yields procedural invalidity, not
   a scientific no-go unless a scientific object also changed.
6. No repair cycle is implied by failure.
7. After the Stage C result commit, halt for the frozen hostile protocol.
8. After Stage F, halt for explicit user authorization. No automatic Paper 15
   construction follows.

The only authorized next event after this pin commit is Stage B Paper 14
construction and source freeze.
