# APR Paper 12 — bounded scorer-repair pin after attempt 1

**Date:** 2026-08-18  
**Status:** FROZEN REPAIR CONTRACT; NO REPAIRED SCORER OR V2 TRUTH EXISTS  
**Scope:** instrumentation/provenance only; scientific fixtures and ontology frozen

## 1. Authority and immutable inputs

This repair is downstream of v16 ledger #154–#163.  The following scientific
inputs remain immutable:

- `v16/note-apr-pin.md`
- `v16/note-apr-pin-addendum.md`
- `v16/note-apr-pin-addendum-2.md`
- `v16/code/apr_core.py`
- `v16/code/apr_fixtures.py`
- `v16/note-apr-scorer-protocol.md`
- `v16/note-apr-scorer-protocol-addendum.md`
- `v16/note-apr-scorer-protocol-addendum-2.md`

The attempt-1 scorer and artifacts are immutable evidence:

- scorer SHA-256
  `a145bf619781705b439defaf62177896a069c34a81b90b22e29522cf77528444`;
- output SHA-256
  `54bc80878ae3ef5769337d00e33d9ed174933181e25ac1433a9e2c0ae70c7b88`;
- receipt SHA-256
  `6876d1ff2e844db5101a76b5e230566a1f566bfcbe7fe6cc512442d56106eb33`;
- attempt-1 audit SHA-256
  `fe48a17e8ff36dbb078a2620ac83731892c7f7fe663f31a56e3912dcf28601c1`.

The repair may modify only `v16/code/apr_score.py`.  It may add generic
self-tests inside that file.  It may not edit the core, fixtures, pins,
protocols, attempt-1 artifacts, result vocabulary, outcome precedence, or any
physical law/preparation/calibration datum.

## 2. Outcome-neutral rule

The repair must not encode, preserve, or target the attempt-1 primary.  It
must recompute the frozen ladder from repaired measurements.  The audit's
independent expectation that `APR-BLOCKED-AT-BOUNDARY-GLUING` survives is not
an executable oracle.  If a repaired earlier gate fails, the scorer must
report the earlier registered word.

No expected-value table, fixture-ID branch, result literal, receipt lookup, or
attempt-1 artifact read may participate in a scientific predicate.  A mutant
counts only when it changes a serialized upstream object and a generic
measurement is recomputed, or when it is explicitly classified
`UNAVAILABLE`, `NOT-CONSTRUCTED`, or `SCOPE-CONTROL` because the frozen
baseline does not exist.

## 3. Mandatory repaired transformations

### 3.1 Gluing and process

1. **G2 — reachable leg mutation.** Select a horizontal filling actually used
   by at least one frozen factorization and having two distinct compatible
   output images. Change one leg, rebuild the tagged pushout, and recompute
   boundary-fixed isomorphism and affected factorization equality. Record the
   changed filling, affected factorization IDs, pre/post canonical payloads,
   and hashes.
2. **G4 — no manufactured process assignment.** Because no frozen
   filling-to-process assignment exists, report the intended
   process-assignment mutant `UNAVAILABLE`. Standalone restriction maps may be
   retained only as a mathematical control and may not count as a process kill.
3. **G5 — no zero-support pseudo-mutation.** Report alternate-cut
   process-assignment mutation `UNAVAILABLE` unless a separately frozen whole
   versus step assignment exists. Do not mutate a branch that is identically
   zero, and do not derive both comparands from the same mutated tree.
4. **P7 versus G3.** Keep the registered meanings distinct. `P7` constructs a
   typed-leg/union-only impostor and verifies that it does not establish a
   pushout or process composite. `G3` stops after validation and reports the
   pushout absent. They may share helpers, not one result object.
5. **Finite controls wording.** The receipt must say that question-tree
   branch/cut calculations and graph-cospan pushouts are separate neighboring
   controls. No conjunction of them is named a functorial assignment.

### 3.2 Regional relation and E-37

6. **M16 — actual relation erasure.** Derive physical overlap/incidence from
   the canonical region antichains and cross-check any declared incidence.
   The mutant must alter the region-bearing object so the relevant overlap is
   really erased, then recompute the physical relation and projected blind
   object. Clearing a declared incidence list is insufficient.
7. **Projection consumption.** Apply the frozen physical-to-blind projection,
   rather than reconstructing a substitute from word lengths or insertion
   order. Missing, nonfunctional, or inconsistent assignments refuse.
8. **Relabel-canonical blind object.** Canonicalize the projected graph with
   boundary/interface/resource colors under graph isomorphism. Raw node IDs,
   insertion order, region words, and relation-mode text may not survive into
   the blind hash. A pure node relabel must preserve the hash; a genuine
   colored incidence change must move it.
9. **E-37 scope.** Because no regional physical `Tau`, exhaustive resource
   class, or public family generator is frozen, `M28`, `M29`, and `M30` remain
   explicit absence/scope controls. Blind factor-through equality is an
   interface theorem, not physical irreducibility.
10. Add a semantic port-ID/list-order metamorphic and an E-37 pure relabel
    metamorphic. Both change neutral presentation bytes while preserving all
    computed physical outputs and canonical hashes.

### 3.3 Locality, contact, and causality

11. **L5 — real conjugation deletion.** Start from an explicitly represented
    conjugation-closed replacement family, remove a required transported
    conjugate, and recompute closure/naturality failure. No verdict Boolean may
    be assigned directly.
12. **L6 — supplied-map provenance.** Compare a supplied support/equalizer
    object with the independently derived finite fixed/support space and its
    active-law ancestry. If no generated support object exists at the claimed
    scope, label the shortcut unavailable rather than a physical mutation.
13. **M34 — static declaration only.** With no generated non-overlap-contact
    response, classify the toggle as declaration sensitivity and keep contact
    `PRICED`/unconstructed. Do not count it as a physical mutant kill.
14. `M21` and `M22` remain static influence/cycle controls because no same-law
    schedule is frozen. `P6` remains the exact identity-channel refutation of
    using nonselective questions as exterior replacements. `P8` remains
    unavailable as a positive tensor test.
15. Print both registered replacement grains: the finite depth-2 `A=[0]`
    success control and the depth-3 `A=[00]` listed/recursive/intrinsic orbit
    counts. Never infer all-partition transitivity from either finite row.

### 3.4 Quotients, boundaries, and generic mutant construction

16. **M13.** Build explicit zero and constant profile laws, run the profile
    quotient and faithfulness diagnostics, and report the actual collapsed
    classes. Do not derive a class count from the existence of names.
17. **M14.** Derive equal-rank unequal subspaces from transformed frozen
    matrices or a generic constructed matrix object, compute bidirectional
    inclusion residuals, and separately test whether a supplied exterior map
    has active-law provenance. Keep the algebraic control distinct from the
    ontology claim.
18. **M15.** Instantiate syntax-copy, identifier-hash, and finite-depth
    whitelist/oracle candidates. Test fresh valid regions, Boolean relabeling,
    refinement, and provenance. Reversing an already canonical list is not an
    oracle mutant.
19. **M33.** Construct five actual transformed branch/process objects—deleted
    port, duplicated port, per-branch renormalization, negative coefficient,
    and wrong branch cell—and pass each through the ordinary completeness,
    normalization, positivity, affinity/composition, and support checks.
20. **P3.** Apply last-bit overwrite to an actually generated depth-at-least-2
    record family; compute the pre/post partition, collisions, and lost reader
    distinctions.
21. **P5.** Instantiate a finite whitelist implementation, show that it accepts
    its registered members, and run a fresh valid deeper region through it.
22. `M05` remains a valid generator-closure invariance control: changing only
    redundant listed longer words should not change the stable null.

### 3.5 Records, law roots, and provenance

23. **M23 versus M24.** `M23` must test destruction of the affected record
    channel by an actual reset/last-token erasure followed by a typed reader,
    or report same-law continuation unavailable. `M24` separately tests the
    redundant-copy control: erase one copy and recover; erase both and fail.
    Their changed objects and evidence hashes must differ.
24. Validate operation expressions, carriers, tree IDs, reader schedules, and
    reachability. Changing an identity delay into a reset while retaining its
    name/kind must move recovery; a nonexistent tree or reader must refuse.
25. Restrict continuation-stable recovery to the actually constructed typed
    subgrammar. Mixed, tensor, malformed, or unassigned continuations remain
    unconstructed and cannot be counted as preserved.
26. **Active-law root.** Construct the law root from semantic nodes reachable
    from registered law entry points, including the active continuation,
    writer, reader, and record-expression semantics. Exclude unreachable
    declarations. Adding a reachable reset or deleting an active reader must
    move the root; editing unreachable `hf_008` must not.
27. **M35 and provenance DAG.** Build and validate roots, typed edges, and a
    path from every claimed output to a declared active-law root. Then inject a
    byte-identical but ancestry-severed object and recompute the failure. No
    provenance conclusion may be a literal.
28. Binding a question-tree row to a graph-cospan row is not a process proof.
    Corrupting that annotation must move a provenance coordinate but cannot
    create or kill an unconstructed filling-to-process functor.

### 3.6 Covariance and scope

29. **M27.** Narrow its claim to the objects actually transported. At minimum,
    transport the Boolean presentation and valuation together and verify the
    generated question/preparation outputs at that scope. It may not license
    global process, replacement, E-37, or family covariance unless those
    objects are also transformed and recomputed.
30. `M31` remains resource-class sensitivity, not required failure. `M32`
    remains a quantum-scope control because no same-law quantum process exists.
31. All unavailable baselines—total adaptive forest functor, identities above
    `B0`, positive tensor factory, nontrivial vertical naturality square,
    regional-overlap selector, generated complete probes, physical atomless
    quotient, causal/contact schedule, regional `Tau`, public family generator,
    and quantum interference law—must appear explicitly in the receipt.

## 4. Receipt and transformation evidence

Every transformation row must contain:

- classification: `SEMANTIC-MUTATION`, `ANALYTICAL-CONTROL`,
  `SCOPE-CONTROL`, `UNAVAILABLE`, or `REFUSED`;
- frozen description and implemented action;
- pre/post canonical changed-object payload hashes, or a printed reason no
  baseline object exists;
- affected active-law/provenance roots;
- the generic measurement recomputed;
- before/after value or exact structural relation;
- a statement of which scientific coordinate, if any, it can falsify.

The scorer must reject answer fields and unexpected supplied diagnoses. It
must demonstrate that scientific predicates do not branch on fixture IDs,
expected outcomes, attempt-1 values, or receipt content.

## 5. Freeze, run, and publication sequence

1. Implement and self-test the repaired scorer without evaluating APR fixtures.
2. Freeze the repaired scorer and a source-audit note in a separate commit.
3. Run once to new absent paths:

   ```text
   v16/apr_output_v2.txt
   v16/apr_receipt_v2.json
   ```

4. Replay cleanly from repo root, alien CWD, and a true off-tree/no-`.git`
   copy; require byte identity.
5. Independently reconstruct every load-bearing baseline, every
   `SEMANTIC-MUTATION`, and all active-law/provenance tests without importing
   scorer measurement functions.
6. Freeze the v2 artifacts and verification only if the full audit closes.
7. Only then may an APR candidate paper be drafted and a mutually blind
   three-seat panel begin.

Attempt-1 paths are protected by no-overwrite tests. Any repaired run that
touches them is an integrity block.

## 6. Frozen ontology ceiling

This repair cannot promote prefix syntax, Boolean regions, flags, trees,
matrices, cospans, kernels, or graph labels into beables. The current positive
construction is at most record-writing on a fixed commutative event algebra,
with separate regional-support and cospan controls. It does not create or
rewrite regions, select causal order, instantiate relational geometry, weld
matter to geometry, produce a metric or curvature, take a continuum limit,
recover GR/QFT, derive particles, or explain actualization.

The candidate ontology under test remains one actual, partial, compatible web
of durable relational facts. A mathematical regional representation becomes
physical only if it is a faithful future-complete quotient of one compositional
law and survives independent dynamic-support, gluing, covariance, and
eliminability gates. This repair supplies none of those missing axioms; it only
makes the existing assay say exactly what was and was not constructed.

