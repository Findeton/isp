# CEL panel adjudication — the mathematical ladder survives; the relational flag weld does not

**Date:** 2026-08-18

**Frozen candidate:** `f3c3ef99f1506f01208a670198a91abe27c952d5`

**Panel grades:** OPERATOR `ACCEPT-WITH-FIXES`; RECORDS `REJECT`; PHYSICS
`REJECT`.

## 1. Frozen evidence

| seat | report | ordinary SHA-256 | normalized self-SHA-256 |
|---|---|---|---|
| operator algebra / null quotient / exact resources | `v16/review-cel-operator.md` | `8c90e9a730375e7c876b05f1db303bd760f905641f4fbcf81fd66f384d83635b` | `0c4f3f472d8ec49cfe45710c4786e493f28d9ab783c3c7dbc4ddf9640c886455` |
| records / recurrence / relational flag typing | `v16/review-cel-records.md` | `b7fb0a9006b7e38637885af0f51ac35b4572c90e36928f126203447f6c85a6fb` | `3217d0ba85254cbf27e0de52b3179740942d352cd35210d0a3313745b1e1b553` |
| locality / dynamics / physical ontology | `v16/review-cel-physics.md` | `41d8e59b1bc5e9fabcd9ff061b7d1e76bb7322c01450bbdeceec8076a29410c9` | `fcbbec15fc19bfb07b489cc0a5e0570f182c70214878e1ac5301f01703a8e8cb` |

All three authenticate the immutable candidate, clean/off-tree replays,
seals, and mutant deaths. They independently reproduce the registered CNOT,
recurrence, reset/relabel, recovery, JCV dilation, PSD, rank, determinant, and
Gaussian-rational calculations. The ruling below is physical and typed, not
an arithmetic or provenance rejection.

## 2. Primary ruling

The frozen positive primary

```text
CEL-CREATION-EVENT-LAYER-CONSTRUCTED-BUT-COUPLINGS-AND-CATALOGUE-UNSELECTED
```

is rejected. The strongest licensed pre-registered outcome is

```text
CEL-MATHEMATICAL-LADDER-CONSTRUCTED-BUT-RELATIONAL-FLAG-WELD-UNBUILT
```

The decisive exact control keeps both JCV coefficient families and dilation
matrices fixed while deleting or changing the declared graph attachment.
Every kernel, channel, stack, support count, and calibrated port probability
is unchanged. The same four-dimensional codomain can therefore be read as an
attached new cell, an internal matter port, or an anonymous target without
moving a frozen observable. Metadata gates reject the alternate names; the
physical law does not.

The three CEL arms are also neighboring fixtures rather than one indivisible
successor. Recurrence uses fixed CNOT circuits, permanence uses classical bit
grammars, and the fiber arm uses `(I,Z)` histories with a declared dilation.
No one map jointly selects a relational rewrite, process transport, kernel,
port factorization, and future grammar.

## 3. Recurrence ladder

The exact ladder survives with its provenance exposed:

1. Locality and all-input completeness do **not** select the history kernel:
   the `16/25` and `1/2` CNOT laws are both complete and move the registered
   screen, including under idle dressing.
2. Spectator equality follows after invoking the standing spectator-
   naturality axiom; it is not forced by completeness alone.
3. Exchange rejects the biased packet and admits the balanced packet only
   after exchange covariance is imposed as a law symmetry.
4. The shared-token fixture is equality of two restrictions of one supplied
   rational coordinate packet. It is a consistency control, not a joint
   channel/rewrite gluing theorem.
5. Token-disjoint universality adds the equation equating two otherwise free
   coordinates and cuts dimension `2 -> 1`. This is the exact price of a
   nomological postulate. The fixture does not independently establish that
   the repeated token suffixes denote one physical event type.

Raw kernel identity is quotiented only relative to a specified operational
algebra. Coincident histories admit distinct kernels with the same
unconditioned channel, but retained calibrated ports distinguish them by
outcome vectors `(1,0)` and `(0,1)`. Hence the operational-null qualifier is
valid only when every licensed continuation factors through the discarded-
port channel.

## 4. Records and recoverability

The reset control exactly kills bare algebra covariance as a general
permanence criterion: in the Heisenberg picture the diagonal algebra maps
into itself, while in the Schrödinger picture both sectors merge.

CEL nevertheless constructs useful finite, grammar-relative recovery facts.
Relabeling preserves the partition, a second copy can preserve a distinction
after the first flag is erased, and retaining an identity/flip branch allows
exact branchwise recovery while discarding it destroys the distinction.
Enlarging the licensed grammar can erase both flag copies; remaining source
information is not the same as a readable flag record. The honest result is:

> The registered classical flag partition is recoverable after every word of
> the declared finite grammar. Catalogue enlargement and actualization remain
> open.

No objective, catalogue-independent permanence theorem is obtained.

## 5. Dilation and calibration

Both JCV Stinespring stacks are exact isometries and induce the same
unconditioned channel. At fixed input/output labels their retained first-port
probabilities are `0` and `49/625`, so the calibrated instrument fiber is
operationally nontrivial.

That statement is calibration-relative. The two coefficient matrices are
related by an exact port rotation, and under the candidate's simultaneous
input/output relabel the corresponding-port comparison is `0=0`. Therefore
the paper must choose: either fixed labels are physical calibrations and the
reversal is covariance between different settings, or the reversal is gauge
and `0 -> 49/625` is not an invariant contrast.

The dilation may be annotated as candidate creation-coupling data, but it is
not yet a relational cell. `PORT-FIBER-RETYPED-AS-CREATION-COUPLING-DATA` and
`FLAG-DILATION-WELDED-BUT-ACTUALIZATION-UNBUILT` are withdrawn at physical
scope.

## 6. Exact-resource theorem

The operator seat independently proves the general statement:

> Every finite rank-`r` positive-semidefinite Hermitian matrix over `Q(i)`
> admits a Gaussian-rational Gram factor with at most `2r` rows.

The zero/singular/complex cases and the `7/5` and `diag(7/5,1)` direct-row
minima survive. This is a theorem about exact representation resources.
Because scalar extension to `C` can reduce the row count, and because a Gram
factor need not obey a relational support grammar, it is not yet a physical
flag-cell cost or a species theorem.

## 7. Scope and successor boundary

CEL securely supplies a common mathematical interface for candidate
recurrence, finite classical recoverability, and calibrated dilations. It
does not select the event catalogue, rewrite, kernel, coupling, continuation
grammar, or occurrence rule.

A successor must put all five in one joint law, make a later probe depend on
the output relation graph, and include a graph-erasure/reconstruction control.
It must independently establish recurring event-type identity before pricing
universality, define the complete operational algebra before quotienting null
directions, and state calibration covariance explicitly.

Actualization, conditional EPR steering, changing-Bob no-signalling,
arbitrary-`n` composition, fields/Fock structure, particles/species,
Hamiltonian reconstruction, Lorentz/continuum spacetime, backreaction,
gravity, constants, and QFT/GR deviations remain unconstructed.
