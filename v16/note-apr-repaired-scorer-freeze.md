# APR Paper 12 — repaired scorer freeze with disclosed prefreeze exposure

**Date:** 2026-08-19  
**Status:** SOURCE FROZEN; PREFREEZE FIXTURE EXPOSURE DISCLOSED  
**Scientific artifacts:** NO V2 OUTPUT OR RECEIPT EXISTS AT THIS FREEZE

## 1. Frozen scorer

The repaired scorer is frozen at:

```text
v16/code/apr_score.py
SHA-256 29834a15289ef64b6727a86a609bc5cbbf2b432420afc027b8f58617db7ea285
6863 lines
```

It binds all three repair contracts:

- `v16/note-apr-scorer-repair-pin.md`
- `v16/note-apr-scorer-repair-pin-addendum.md`
- `v16/note-apr-scorer-repair-pin-addendum-2.md`

The file keeps question-tree instruments separate from graph-cospan controls;
builds a computed capability census; derives the registered primary through a
generic precedence classifier; uses scope-indexed semantic dependency roots
and reports no global joint root; reconstructs exact overlap-gluing ambiguity;
executes expression-typed record controls; derives candidate-regional E-37
projections with colored graph-isomorphism canonicalization; and publishes
machine-reconstructible transformation descriptors.

## 2. Mandatory exposure disclosure

The intended freeze-before-truth sequence was violated during the final CLI
refusal audit. Before the source commit, the implementation worker ran, from
`/Users/felixrobles/workspace/isp`, in this order:

```text
python3 v16/code/apr_score.py --mutant M01 2>&1
python3 v16/code/apr_score.py --mutants-all 2>&1
```

The CLI legitimately permits stdout-only mutant evaluation, so both commands
authenticated and loaded the frozen fixture. The first evaluated the baseline
and M01; the second evaluated the baseline and all 56 registered
transformations/controls. The merged shell call returned `0`. Its stdout was
tool-truncated and was not redirected, so a complete exposure transcript and
hash do not exist. Visible output included the frozen fixture payload digest,
`APR-BLOCKED-AT-BOUNDARY-GLUING`, and repaired mutant rows including P7.

No `--run` command executed. No output or receipt destination was supplied.
No v2 artifact or other intended file was created. The worker had not read the
attempt-1 output/receipt and the scorer contains no path that consults them.

Most importantly, the worker stopped immediately on recognizing the violation
and made no edit after exposure. The bytes frozen here are the bytes that were
exposed. Root independently measured their SHA-256 after the stop and found no
v2 artifact or APR byte other than the modified scorer. This is therefore a
procedural prefreeze exposure, not a hidden tune-after-result loop—but it is a
real protocol violation and remains attached to every later APR claim.

The repaired scorer may not be described as pre-truth or mutually blind. Its
results require the stronger independent black-box reconstruction frozen by
the repair verifier design. Candidate drafting and panel review remain
prohibited until that audit closes.

## 3. Generic verification on the frozen bytes

Root ran only the synthetic, fixture-free self-test after the stop:

```text
python3 -B v16/code/apr_score.py --selftest
```

It passed 21 groups and printed `scientific_fixture_evaluated:false`. The same
file plus frozen `apr_core.py`, copied to a true off-tree directory with no
`.git`, produced byte-identical self-test JSON under an alien working
directory and a minimal environment using Python 3.13.5.

The 21 groups cover:

- exact Boolean/valuation and semantic tree partitions;
- zero ports and mixed-constructor refusal;
- pushout versus validation/naive-union separation;
- stable nulls and three replacement closures;
- strict and expression-driven record semantics;
- semantic port-ID/order and E-37 relabel metamorphics;
- scoped covariance, reachable G2, scoped roots, and provenance severing;
- overlap-selector nonuniqueness;
- classifier movement both later and earlier;
- detected boundary coordinates;
- repaired analytical mutant constructors;
- mutation descriptor and M35 envelope schemas;
- all 56 transformation IDs;
- transactional no-overwrite publication.

AST parsing passes at 6,863 lines; `git diff --check` is clean. Root source
search found no attempt-1 artifact path, expected-answer/result field, random
scientific path, network client, subprocess/Git dependency, or generated
Python cache. The off-tree scorer hash equals the repository scorer hash.

## 4. What this freeze does not establish

This commit freezes an exposed but no-post-exposure-edit scorer. It freezes no
APR v2 scientific result. Registry coverage, a dependency root, a canonical
projection, and green analytical controls do not create missing positive
physics.

The next authorized event is one transactional v2 run from these committed
bytes to the already pinned absent paths:

```text
v16/apr_output_v2.txt
v16/apr_receipt_v2.json
```

Those artifacts must be replayed byte-identically from clean, alien-CWD, and
true off-tree/no-git environments. An independent verifier must authenticate
the sources, treat the scorer as a subprocess black box, reconstruct all
load-bearing baselines and semantic mutations without importing scorer
measurement functions, and audit every descriptor and capability witness.

## 5. Ontology ceiling

Nothing in the repair or exposure changes the ontology. The ontic proposal is
still a postulated candidate: one actual partial compatible web of durable
relational facts. The scorer manipulates representations of regions, records,
instruments, and cospans. It has not established a physical atomless regional
referent, a joint process, causal order, geometry, backreaction, metric,
curvature, continuum, GR/QFT, particles, or actualization.

