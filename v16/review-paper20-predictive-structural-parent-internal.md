# Internal hostile review of Paper 20

Date: 2026-08-21

Status: **COMPLETE — NONBLIND INTERNAL AUDIT**

Verdict: **ACCEPT-INTERNAL-SCOPE**

First decisive semantic counterexample: **none**

This report was produced by the constructing agent. It is not blind,
independent, or terminal peer review. Its verdict cannot exceed the status
authorized by the frozen protocol.

## 1. Authenticated corpus

| artifact | ordinary SHA-256 |
|---|---|
| mathematical pin | `06f80fc38bb9d54944cac4ab0f75509bee4f4ebcce48d7098fe490b0205432a8` |
| Paper 20 candidate | `64111d7764bf70984b959e00bc1da30ad0d6c3ae7b5c6d51b94227ad2f5c35e6` |
| construction note | `67e2bef72a21bed1e598201e108595aaab5ec16e966ef03388fcb6969d407ef3` |
| review protocol | `7629e3d90a5a9a860d349c5e1244d6e8e62ef63818840c88a64fed8399512649` |

The bound Paper 13D, Paper 18, and Paper 19 hashes were independently
recomputed before the audit and match the values in the pin. No implementation
or generated evidence was used.

## 2. Verdict in one sentence

The dependent parent and predictive quotient are mathematically sound, and
the accepted Paper 13D grand observational law induces a genuine point-free
law over complete predictive target configurations. The same law still has a
Dirac whole-process marginal, so it supplies no selector over execution
architectures and does not reopen global Paper 17 chronology or dimension.

## 3. Earliest positive and limiting coordinates

The earliest positive coordinate is

```text
P20-PARENT-DEPENDENT-TARGET-TYPE-CONSTRUCTED
```

The first live scientific limitation is

```text
P20-CURRENT-GAMMA-RESOLVED-STRUCTURAL-BRANCHING-UNCONSTRUCTED
```

This is not a defect in the target law. It states that distinct endpoint
values inside one fixed primitive process are not distinct resolved process
channels.

## 4. Lens A — type, quotient, and measure

### A1 — dependent sum versus product: PASS

An element of a dependent disjoint union has one tag and one value. It has no
coordinate in an unchosen fiber. A Cartesian product has one coordinate in
every factor. The dormant-hardware countermodel therefore has a different
type and cannot be confused with the parent.

### A2 — no predeclared physical structure: PASS

The index `Lambda_x` is only a raw target-type index. The reported physical
outcome is the predictive class in `P_x`. Two raw fibers are not declared
physically inequivalent merely by assigning them different serial labels;
comparison requires an admitted transport of their complete operational
interfaces.

### A3 — equivalence relation: PASS

Identity transport gives reflexivity, inverse transport gives symmetry, and
composition of transports plus transitivity of equality of probability laws
gives transitivity. No outcome decoder enters this proof.

### A4 — typed cross-fiber comparison: PASS

Endpoints of different types are compared only through an admitted transport
that aligns continuations, intervention addresses, readers, and outcomes. If
such a physical comparison is absent, the paper does not manufacture one.

### A5 — complete behavior rather than one reader: PASS

The relation quantifies over every frozen aligned lawful future test and
retains the complete reader. Equality under a parity, connectivity, or result
reader alone does not identify endpoints.

### A6 — minimal future sufficiency: PASS

If a statistic `s` is future sufficient, every `s`-fiber lies inside one
predictive-equivalence class. Therefore the predictive map factors uniquely
through `s`. The factorization direction is correct: a finer sufficient
statistic maps to the coarser predictive quotient, not conversely.

### A7 — finer and falsely coarser controls: PASS

A statistic `(epsilon(t), auxiliary physical detail)` is a valid finer
sufficient statistic. A statistic retaining only a coarse reader is not
sufficient whenever two endpoints share that reader output but differ under
the complete identity reader.

### A8 — point-free covariance: PASS

Presentation transport moves experiment, endpoint, interventions, reader,
and reader outcomes together. Full orbit mass is preserved. Consequently it
maps an entire complete behavior family to the same physical family.

### A9 — measurability scope: PASS WITH EXPLICIT LIMIT

The operational sigma algebra is generated by the descended future-law
coordinates. In the accepted finite specialization it is finite and all
pushforwards and conditionals used in the paper exist. The paper does not
claim that an arbitrary nonstandard quotient is standard Borel or possesses
regular conditional probabilities.

Fresh control: an uncountable behavior image can be equipped with the trace
of the product sigma algebra even when an informal description of its
equivalence classes looks nonconstructive. This makes the behavior
coordinates measurable, but it does not by itself prove standard-Borel
regularity. The paper retains exactly that distinction.

### A10 — normalized pushforward: PASS

The map used is `epsilon o pi_x`: histories first map to their typed endpoint,
then to predictive class. This avoids counting two histories ending at one
child as two structures. A measurable pushforward preserves total mass one.

### A11 — identity-parent no-go: PASS

For any desired law `nu`, taking history space, target space, endpoint map,
and predictive map to be identities formally realizes `nu`. Therefore mere
existence of a parent representation cannot select a physical law.

### A12 — deletion descent: PASS

The positive deletion theorem is asserted only for the complete endpoint-
orbit quotient, where accepted Paper 13D deletion is equivariant. The paper
does not claim that deletion descends through an arbitrary coarse predictive
quotient.

Fresh failure control: let a three-state source quotient identify `a~b` but
let deletion send `a` and `b` to distinct quotient classes. No quotient
deletion map exists. The manuscript explicitly conditions general descent on
the required congruence and therefore survives this control.

## 5. Lens B — accepted-Gamma specialization

### B1 — bound law: PASS

The Paper 13D candidate and terminal mathematical adjudication authenticate
to the pinned hashes.

### B2 — fixed-size parent: PASS

`U_empty` is the accepted unmarked grand observational primitive with
tautological source context. It is not selected using a downstream Paper 17
statistic.

### B3 — source and private law: PASS

The specialization uses the accepted independent fair public source bits,
the accepted biased private occurrence bits, uniform `[25]` seeds, and
independent unordered-pair seeds. No probability was retuned.

### B4 — iid fair endpoint colors: PASS

For each occurrence,

\[
 d_i=e_i^0\oplus x_i\oplus y_i.
\]

The bit `e_i^0` is fair and independent of `x_i,y_i`, so `d_i` is fair even
after fixing those two bits. Independent occurrence seeds give independence
across `i`.

### B5 — bond probabilities: PASS

Two iid fair colors agree or disagree with probability one half. Averaging
the accepted conditional values gives

\[
 \frac12\frac9{25}+\frac12\frac{16}{25}=\frac12.
\]

### B6 — edge and degree expectations: PASS

Linearity over the `n(n-1)/2` unordered pairs gives expected one-bond count
`n(n-1)/4` and expected degree `(n-1)/2`.

### B7 — triangle control: PASS

The exact all-one probability is

\[
 \frac14\left(\frac9{25}\right)^3
 +\frac34\left(\frac9{25}\right)
             \left(\frac{16}{25}\right)^2
 =\frac{7641}{62500}.
\]

It differs from `1/8`. Thus equal edge marginals do not make the bond field
iid.

### B8 — positive distinct endpoint classes: PASS

For `n>=2`, fix a positive-support packet and all coordinates except one
bond. Independent pair seeds give positive probability to both bond values.
The endpoints differ in total one-bond count, so no presentation
automorphism identifies them.

### B9 — complete-reader quotient: PASS

The identity future and complete endpoint reader distinguish any two
endpoint orbits. Covariance identifies representatives inside one orbit.
Hence predictive classes and endpoint orbits coincide in this finite
specialization.

### B10 — physical endpoint projection and deletion: PASS

The accepted law is already a law on history orbits. The paper correctly
uses `[H] -> [end(H)]`, not a map from an orbit to a selected labelled
representative. Uniform occurrence deletion commutes with this endpoint map.
The geometric size weights `2^{-(n+1)}` sum to one, but are not interpreted as
spacetime volume or cosmological duration.

### B11 — fixed execution class: PASS

All endpoint values are generated inside the same primitive `U_empty`
history type. The pushforward to unmarked process class is therefore
`delta_[U_empty]`, despite the nontrivial endpoint marginal.

### B12 — ontology wall: PASS

The bond family is dense in expected degree and has no accepted direction or
geometric decoder. The paper calls it a relational target-field control, not
a lattice, causal set, adjacency graph, volume, or dimension.

Fresh same-marginal control: the exact two-color model and an iid fair-edge
model have the same one-edge marginal but different triangle law. Fresh
process control: two positive endpoint configurations differ physically but
map to the same fixed execution class. Both are handled correctly.

## 6. Lens C — physics and Paper 17

### C1 — target configuration versus target sector: PASS

Paper 20 reports plurality of complete predictive configurations only. It
does not overturn the terminal Paper 18 finding of one resolved simultaneous
fusion channel and one structural target class.

### C2 — target versus resolved channel: PASS

A target value does not encode a path by which it was generated. The paper
keeps target-law and resolved-channel coordinates separate.

### C3 — channel, activity, and root: PASS

No conditional channel law is called a propensity for the opportunity to
occur, and no activity law is called a cosmological initial distribution.

### C4 — inherited-R wrapper: PASS

Attaching a new bit and applying the accepted numerical `R` is a new physical
extension. Swapping the meanings of its outputs changes proposed structural
odds while leaving inherited Gamma unchanged. Thus inherited numbers do not
select the decoder.

### C5 — binary-tree coherence: PASS

For three or more channels, sequential binary branching assigns different
products of local probabilities to leaves under different bracketings unless
an independent associator/coherence law is supplied. No such tree is used.

### C6 — connectivity decoder: PASS

Connectivity is invariant but need not be future sufficient. Conditioning on
connectedness also changes the accepted unconditional bond law. It remains a
coarse diagnostic, not a structural selector.

### C7 — amplitude representation: PASS

Every finite positive law has square-root amplitudes with arbitrary phases.
Born normalization therefore represents a selected law but does not choose
it.

### C8 — diagonal-phase theorem: PASS

For block-diagonal lawful effects, conjugating any state by a sector-phase
unitary changes no probability. The trace proof applies to pure and mixed
states.

### C9 — recombination criterion: PASS

Operational structural interference requires an admitted off-diagonal effect
or recombiner. Paper 13D supplies none between inequivalent whole execution
classes.

### C10 — invariant-measure scope: PASS

A unique invariant measure is selective only when an independently physical
symmetry acts on the physical channels. A transitive action on presentation
labels that fixes every physical channel gives no equality of channel odds.

### C11 — restricted Paper 17 opening: PASS

The opened investigation classifies invariant target statistics under the
accepted fixed process. It awards no chronology, dimension, signature,
metric, scale, topology, or manifoldlikeness.

### C12 — original Paper 17 gate: PASS

The reopening alternatives fix the missing parent law independently, fix a
finite residue before downstream inspection, or prove the downstream result
constant across that residue. None uses a Paper 17 output to tune the parent.

Fresh phase control: two pure states with amplitudes
`(sqrt(p),sqrt(1-p))` and `(sqrt(p),-sqrt(1-p))` agree under every diagonal
effect. Fresh residue control: two parent laws can have the same target
marginal while attaching every history to different constant process labels;
their target statistics agree and process marginals differ. These controls
support, rather than defeat, the paper's scope boundary.

## 7. Literal hostile controls H1–H40

1. **PASS:** the identity-parent construction proves representability is not
   derivation.
2. **PASS:** a renamed fair or biased bit is exposed as a wrapper.
3. **PASS:** swapping the wrapper labels changes structural odds but no
   inherited Gamma datum.
4. **PASS:** binary-tree bracketing remains unlicensed without coherence.
5. **PASS:** direct-sum amplitudes remain arbitrary state data.
6. **PASS:** relative phases are idle without a recombiner.
7. **PASS:** probability-neutral dilation sectors are not promoted.
8. **PASS:** a product containing all children fails the one-realized-fiber
   type.
9. **PASS:** the dependent sum realizes exactly one tagged fiber.
10. **PASS:** the complete identity reader makes the accepted finite quotient
    maximally fine up to presentation.
11. **PASS:** an incomplete reader cannot merge endpoints distinguished by
    the complete reader.
12. **PASS:** the quotient is explicitly relative to the frozen future
    grammar; a larger physical grammar may refine it.
13. **PASS:** presentation transport leaves physical class mass invariant.
14. **PASS:** full orbit mass, never representative mass, defines the law.
15. **PASS:** the operational sigma algebra is frozen from complete future
    tests before the pushforward.
16. **PASS:** no post-hoc coarse graining repairs an invalid parent.
17. **PASS:** connectivity, threshold, and motif decoders remain diagnostics.
18. **PASS:** a zero bond remains a typed zero value unless a separate
    sufficient quotient removes it.
19. **PASS:** connectivity conditioning is not called child-generation
    recovery.
20. **PASS:** bond values remain history/endpoint values, not execution
    branches.
21. **PASS:** a merely mathematical transitive group is nonselective.
22. **PASS:** uniform presentation labels do not determine physical odds.
23. **PASS:** detailed-balance rates reverse-engineered from a desired law are
    rejected as circular.
24. **PASS:** Gibbs and maximum-entropy constructions retain their base
    measure as input.
25. **PASS:** path-integral action, measure, and boundary state remain inputs.
26. **PASS:** decoherence is not called prior-weight selection.
27. **PASS:** Born or Gleason form is not called state selection.
28. **PASS:** quantum dimensions are not imported without a physical
    branching algebra.
29. **PASS:** fusion and erasure are not treated as a reversible pair of
    whole-structure moves.
30. **PASS:** fixed-size conditioning is separated from the accepted root
    cardinality law and from cosmological interpretation.
31. **PASS:** target-configuration plurality is not promoted to Paper 18
    target-sector or resolved-channel branching.
32. **PASS:** no resolved-channel law is promoted to activity.
33. **PASS:** no activity law is promoted to a root state.
34. **PASS:** one actual configuration is not used to infer typicality.
35. **PASS:** no dimension statistic selects a parent parameter.
36. **PASS:** finite bond controls do not impose a discrete ontology.
37. **PASS:** finite projective approximants need not have a discrete inverse
    limit. As an exact control, consistent uniform laws on the first `n`
    binary digits converge to the nonatomic Bernoulli product measure; modulo
    the standard binary-expansion null set this is Lebesgue measure on
    `[0,1]`.
38. **PASS:** generic quotient deletion is not claimed without congruence.
39. **PASS:** no local odds are inferred from unrooted automorphism weights;
    adding an isomorphic spectator can introduce a genuine permutation
    multiplicity.
40. **PASS:** the surviving whole-process-law residue changes global Paper 17
    statistics, so the original gate remains closed.

## 8. Product result

The frozen paper's product vector is supported exactly:

```text
P20-PARENT-DEPENDENT-TARGET-TYPE-CONSTRUCTED
P20-PREDICTIVE-QUOTIENT-CONSTRUCTED
P20-PREDICTIVE-QUOTIENT-MEASURABILITY-CONSTRUCTED-FOR-ACCEPTED-FINITE-FAMILY
P20-GENERAL-NONSTANDARD-PREDICTIVE-MEASURABILITY-UNPROVEN
P20-CURRENT-GAMMA-PREDICTIVE-TARGET-LAW-CONSTRUCTED
P20-CURRENT-GAMMA-TARGET-CONFIGURATION-PLURALITY-CONSTRUCTED
P20-CURRENT-GAMMA-RESOLVED-STRUCTURAL-BRANCHING-UNCONSTRUCTED
P20-CURRENT-GAMMA-WHOLE-PROCESS-PARENT-UNCONSTRUCTED
P20-STRUCTURAL-INTERFERENCE-UNCONSTRUCTED
P20-WHOLE-PROCESS-PARENT-PROBABILITIES-UNCONSTRUCTED
P20-P17-RESTRICTED-TARGET-TEST-OPEN
P20-P17-ORIGINAL-GLOBAL-GATE-CLOSED
P20-ACTUALIZATION-UNCONSTRUCTED
P20-METRIC-UNCONSTRUCTED
P20-GRAVITY-UNCONSTRUCTED
```

## 9. Final assessment

The common-parent idea is physically coherent once the alternatives are
values of one dependent-output experiment and their identity is fixed by
complete operational behavior. The accepted Gamma already provides a
nontrivial example at the target-configuration level.

The investigation also proves why this does not yet solve the selector
problem that blocks global Paper 17. Random endpoint fields occur inside one
fixed execution class. A genuine successor must place inequivalent
whole-process fibers in one independently fixed law, or prove the desired
downstream result insensitive to every surviving law.

No implementation, sampler, fresh-case generation, official run, Paper 17
chronology inference, or dimension inference was performed.
