# Paper 22 v2 independent quantum/no-hiding delta review

Date: 2026-08-21

Seat: **Q — quantum, no-hiding, and physical scope**

Verdict: **REVISE**

First decisive semantic counterexample: **the stated partial-record rule uses
only `Re v`, but a general route-environment overlap enters as
`Re(v e^{i phi})`.  With `v=i`, the environment states are the same ray and
retain no route information, so the fringe has unit visibility with a phase
shift; the candidate's rule predicts a vanished interference term.**

The homogeneous-source repair, no-hiding results, reversible query, exact
`C_phi`, stable-record law, fiberwise commit, restrictions, and composition
boundary otherwise survive.  The error is localized and readily repairable,
but the frozen protocol makes failure of the visibility law and a required
probe theorem at least `REVISE`.

## 1. Integrity, authentication, and review isolation

The repository state was authenticated before inspection:

```text
HEAD 42d815a
```

The delta-review protocol and every immutable scientific input were
authenticated before scientific use:

| object | ordinary SHA-256 | result |
|---|---|---|
| v2 review protocol | `88a2609988628e8e9fe1ad2c96a0b65a9cf230a2750feab2207bca5ebfcfd30e` | exact |
| terminal Paper 13D law | `3b91766fe269e8145766f4622f99e9009c3270530670119735e6045a473de5c9` | exact |
| predecessor adjudication | `c261520aa142bf07a489f87cd0364628f094794c7523c03d8ba3dde05d824a07` | exact |
| Paper 22 v2 pin | `a4c1c2ecd10edad73ed64b12f699c09d7cfd169d4cd264939990589554693627` | exact |
| Paper 22 v2 candidate | `30340295ccd5f8371a9020cb76c0a93cc24ab14cbbf78f05c01a85ca5ce86468` | exact |
| Paper 22 v2 construction note | `e90bdd14ca04742bb09eb3c9ec928b4aea04ff6429b0896158ce83170280a0ae` | exact |

The v2 pin and candidate were reauthenticated with the same exact hashes
immediately before this report was frozen.  Moving-byte and HEAD stop
conditions did not occur.

I read no sibling report, frontier census, semantic input audit, Paper 23
artifact, or private regional investigation, and I did not contact another
seat.  The rejected predecessor candidate was not opened; it was used only
through the binding adjudication and numerical anchors, as required.  I
inspected or ran no implementation.  I edited no corpus file, staged nothing,
and made no commit.  This file is the only artifact I wrote.

## 2. Independent reconstruction

### 2.1 Corrected domain and predecessor refusal

For each accepted Paper 13D atomic sort `s`, the candidate defines

\[
 \mathsf{Src}^{\ge2}_s
\]

from finite unordered active families of at least two nonempty, pairwise
disjoint components all lying in `B_s`, plus complete separately typed
spectators.  The total positive source is the coproduct over `s`, with no
arrows between summands and no arrow exchanging an active role with a
spectator role.

This kills the predecessor counterexample at membership time.  In particular,

\[
 X_i\in B_1^0(\{i\}),\qquad X_j\in B_2^0(\{j\})
\]

has no common active-sort summand.  It is refused before tensor/fusion child,
query, commit, or probability evaluation.  A differently sorted spectator is
allowed because its identity-carried type is not the active fusion sort.

For every admitted `X` the tensor experiment is the identity history on the
formal tensor source, while the fusion experiment is exactly one simultaneous
Paper 13D `Phi_s` tensored with spectator identities.  Stochastic fusion is a
kernel on complete histories, not a deterministic target map.  Thus the
source and child correction is total at the intended scope.

### 2.2 Finite and quantum no-hiding

If a finite accessible fusion map has `f(x)=f(x')` for `x != x'`, an
injective dilation

\[
 \widetilde f(x)=(f(x),c_x)
\]

must have `c_x != c_x'`; otherwise two distinct inputs have the same joint
output.  In the pure-output Hilbert case,

\[
 V|x_k\rangle=|y\rangle|c_k\rangle
\]

for orthogonal inputs and one common normalized accessible state gives

\[
 0=\langle x_0|x_1\rangle
   =\langle y|y\rangle\langle c_0|c_1\rangle,
\]

so the complements are orthogonal.  Purity of the accessible reduction is
what permits the displayed product form.

These proofs establish the required deterministic/pure collision result.
They do not assert branchwise orthogonal complements for every mixed-output
channel, and the candidate does not need that stronger theorem: the accepted
stochastic fusion is purified into retained seed basis states before the
deterministic witness computation.

### 2.3 Seed purification and reversible query

For each cross-active pair, the candidate prepares

\[
 |\sigma\rangle=(3/5)|0\rangle+(4/5)|1\rangle.
\]

This is sufficient to purify the Paper 13D binary bond kernel.  With
`e=1` when the endpoint colors agree, the exact reversible output convention

\[
 \ell=z\mathbin\oplus e
\]

gives `P(ell=1)=9/25` for equal colors and `16/25` for unequal colors.  The
candidate leaves this controlled interpretation inside the phrase “accepted
fused witness”; retaining `X` and `z` makes it injective.  No new seed bias is
needed.

For fixed classical source `X`, both computations

\[
 |X,z,0_W,0_C\rangle
 \longmapsto |X,z,w_m(X,z),c_m(X,z)\rangle
\]

are injective on the retained finite typed basis and hence admit permutation
unitary extensions.  Their mode-controlled direct sum is unitary on that
fiber.  Compute, apply the fused-witness projector phase, and inverse-compute
therefore restore every declared seed, witness, complement, and apparatus
coordinate to its common pre-query value.

This is an ideal closed-apparatus theorem.  An unmodelled environment that
copies the route is not reversed by notation; the orthogonal-residue attack
in Section 5 shows exactly how it changes the law.

### 2.4 Physical fused-witness phase

The tensor and fusion witnesses occupy different typed sectors, already
distinguished by formal tensor versus simultaneous-fusion child behavior.
The projector `Pi_{F,X}` is transported with that typed sector under every
source presentation isomorphism.  Thus

\[
 Q_X^{-1}e^{i\phi\Pi_{F,X}}Q_X
\]

acts as `diag(1,e^{i phi})` on the initialized structural mode while returning
the query registers to blank.

If the phase were put on an unrelated route-label ancilla, replacing the
fusion query by a second tensor query would leave the phase response intact.
For the candidate's witness projector the same substitution makes the
projector eigenvalue equal on both routes and removes the relative phase.
The operation is therefore structurally attached, although its physical
availability and phase reference remain new apparatus hypotheses.

### 2.5 Exact coherent and recorded laws

With

\[
 R=\begin{pmatrix}3/5&-4/5\\4/5&3/5\end{pmatrix},\qquad
 D_\phi=\operatorname{diag}(1,e^{i\phi}),
\]

direct multiplication yields

\[
 C_\phi=|RD_\phi R|^2
 =\frac1{625}\begin{pmatrix}
 337-288\cos\phi&288(1+\cos\phi)\\
 288(1+\cos\phi)&337-288\cos\phi
 \end{pmatrix}.
\]

Consequently

\[
 C_0=\frac1{625}\begin{pmatrix}49&576\\576&49\end{pmatrix},
 \quad
 C_{\pi/2}=B^2=\frac1{625}\begin{pmatrix}337&288\\288&337\end{pmatrix},
 \quad C_\pi=I.
\]

The lift classification from the moduli of `B` is correct up to row phases,
column phases, and transported basis exchange.  It classifies a gauge orbit;
the real representative, repeated-mixer convention, and phase zero are
separately fixed physical choices, not consequences of the stochastic matrix
alone.

For tensor input, the full inherited filter distributions, with denominator
625, are

| context | `T` | `F` | `loss` |
|---|---:|---:|---:|
| both | `337-288 cos(phi)` | `288(1+cos(phi))` | `0` |
| tensor only | `81` | `144` | `400` |
| fusion only | `256` | `144` | `225` |
| empty | `0` | `0` | `625` |

so

\[
 I_2(T)=-288\cos\phi/625,\qquad
 I_2(F)=288\cos\phi/625,\qquad I_2(loss)=0.
\]

The candidate's matrices imply this table, but its claimed Theorem 6 does not
actually state or prove the pinned filter/loss result.  This omission is
secondary to the explicit partial-visibility error below.

A stable orthogonal route record has zero overlap and removes the cross term,
giving `B^2`.  Reversing the record write and every physical copy before
recombination returns the overlap to one and restores `C_phi`.  Merely
resetting a visible record while a readable environment copy remains does not
restore interference.

The continuation calculation is also exact:

\[
 K_\phi=C_\phi B^{-1}
 =\frac1{175}\begin{pmatrix}
 63+288\cos\phi&112-288\cos\phi\\
 112-288\cos\phi&63+288\cos\phi
 \end{pmatrix},
\]

positive exactly for
`-7/32 <= cos(phi) <= 7/18`.

### 2.6 Decisive partial-visibility failure

Let `|e_T>` and `|e_F>` denote all residual record, complement, and
environment states immediately before the second mixer, and define

\[
 v=\langle e_T|e_F\rangle.
\]

The mode off-diagonal term is multiplied by `v`, while probabilities take its
real part together with the controlled route phase.  The correct law is

\[
 C_{\phi,v}
 =\frac1{625}\begin{pmatrix}
 337-288q_{\phi,v}&288(1+q_{\phi,v})\\
 288(1+q_{\phi,v})&337-288q_{\phi,v}
 \end{pmatrix},
 \qquad
 q_{\phi,v}=\operatorname{Re}(v e^{i\phi}),
\tag{Q.1}
\]

up to the harmless convention that complex-conjugates both `v` and the sign
of `phi`.  Scanning `phi` gives

\[
 \mathcal V_F=|v|,\qquad
 \mathcal V_T=(288/337)|v|.
\tag{Q.2}
\]

Paper 22 v2 instead says that a partial record with overlap `v` multiplies
the interference term by `Re v`.  That is correct only after an additional
restriction to a real, phase-neutral dephasing overlap, with the phase-zero
convention shifted consistently.  No such restriction is stated.

Take

\[
 |e_F\rangle=i|e_T\rangle,
 \qquad v=i.
\]

The two environment vectors are the same physical ray, so they encode no
which-route information and `|v|=1`.  Equation (Q.1) at `phi=pi/2` has
`q=-1` and gives `C_{pi/2,i}=I`; equation (Q.2) gives full `F`-detector
visibility.  A rule depending only on `Re v=0` instead gives the recorded
law `B^2` and zero visibility.  This is a direct quantum counterexample to the
advertised partial-record/visibility statement.

The repair is to replace the sentence by (Q.1)–(Q.2), or explicitly define a
phase-neutral record family with real nonnegative overlap and recalibrate
`phi`.  Since the protocol freezes the general visibility family as part of
the exact local mode law, this cannot be supplied silently by this review.

### 2.7 Commit and no dormant child

For each fixed admitted classical source, the commit isometry is

\[
 W_X|m,X,0_Y,0_C,0_R\rangle
 =|m_R\rangle\sum_{[H]}
 \sqrt{\Gamma_m([H]\mid[X])}
 |H\rangle_Y|c_{[X],m,[H]}\rangle_C.
\]

Normalized child kernels give unit norms; different modes have orthogonal
records; different histories and different source orbits have orthogonal
exhaust labels.  Thus same-source/different-mode,
same-source/different-history, and different-source inner products all pass.
The source index repairs the predecessor's possible direct-sum collision but
does not assign amplitudes among source fibers.

Projection on `m` and partial trace over the sealed exhaust give

\[
 \widehat\Gamma_{\phi,X}(m,[H]\mid j)
 =C_\phi(m\mid j)\Gamma_m([H]\mid[X]).
\]

The joint law normalizes, and every positive-probability mode conditioning
recovers its exact Paper 13D child law.  The accessible output is a dependent
coproduct containing the stable record and selected history, not a product
with an active unchosen child.  Opening the exhaust is a different experiment
and is not part of accepted child futures.

## 3. Mandatory predecessor regressions

| regression | disposition |
|---|---|
| heterogeneous active-sort source | **killed** at coproduct membership before child evaluation |
| sort-changing source morphism | **killed**; no cross-summand hom-set exists |
| zero/one-active target conflation | **killed**; branchwise restriction retains distinct target types and `m_R` |
| same-source multi-mark closure inferred from disjoint tensoring | **killed**; only external tensor is defined |
| staged fusion identified with simultaneous fusion | **killed**; the staged word retains extra traversed boundaries |

All five predecessor regressions pass.  None repairs the independent
visibility counterexample.

## 4. Twenty-two common hostile controls

| # | control | independent disposition |
|---:|---|---|
| 1 | heterogeneous `B_1^0/B_2^0` active pair | refused before evaluation |
| 2 | sort-changing source morphism | absent |
| 3 | empty active family as positive source | refused by cardinality |
| 4 | one-active family as positive source | refused by cardinality |
| 5 | tensor unit identified with empty atomic boundary | refused as type equality |
| 6 | one-factor tensor identified with atomic factor | refused without alignment |
| 7 | degenerate restriction forges a new opportunity | killed; only an old branch is restricted |
| 8 | restriction silently drops stable record | killed by explicit `m_R` in the dependent output |
| 9 | same-source two-mark commutation | explicitly unconstructed |
| 10 | staged binary fusion equals simultaneous fusion | refused by history type |
| 11 | distinct sources share unindexed exhaust | killed by `[X]` exhaust index |
| 12 | exhaust orthogonality selects source odds | killed; no cross-source amplitudes or measure exists |
| 13 | spectator changes `C_phi` | killed; spectator is identity-carried |
| 14 | route-dependent residue after query | kills full coherence; absent only in the declared exact inverse model |
| 15 | query renamed accessible erasing fusion | killed by no-hiding and separate commit |
| 16 | accessible dormant unchosen child | killed by dependent coproduct output |
| 17 | naked mode-label swap | not a gauge unless controlled operations transport |
| 18 | output decoder changed at fixed instrument | a different instrument, not a relabeling |
| 19 | downstream result selects `phi` | refused; phase is supplied upstream |
| 20 | mutate numerical or child-law anchor | refused; `B`, `R`, `C_phi`, neutral odds, and kernels are unchanged |
| 21 | infer activity/root odds from local odds | killed by arbitrary external source propensities |
| 22 | infer chronology, dimension, geometry, or actuality | killed by absence from every definition |

The twenty-two pinned controls pass.  The failed complex-overlap visibility
case is an additional required-Q/fresh quantum attack, not one of these
twenty-two source-and-scope regressions.

## 5. Seat Q required attacks

### Q-A. Orthogonal residual complement after apparent uncomputation

Let a leaked field satisfy `<e_T|e_F>=0`.  Equation (Q.1) becomes `B^2` for
all phases: full query interference is gone.  Therefore an inverse that
restores only the visible witness is insufficient.  The candidate passes only
for its ideal declared `Q_X^{-1}` acting on every coupled coordinate; it does
not prove experimental isolation from an unmodelled environment.

### Q-B. Phase on an untyped route-label ancilla

An ancilla phase survives when both controlled queries are replaced by tensor
queries and so fails the substitution discriminator.  The candidate's phase
projector is instead the presentation-transported fused-witness sector and
passes this attack.

### Q-C. Readable environment copy after record erasure

Reset the visible record but keep orthogonal environment copies.  Their
overlap remains zero, so the law remains `B^2`, not `C_phi`.  The candidate
correctly requires reversal of the record and every copy.  The phrase
“reversibly erased” must be read as exact uncomputation, not many-to-one
deletion.

### Q-D. Accessible unchosen child

Add a live `Y_not-m` register to the selected output and merely trace it in a
coarse reader.  The physical output becomes a product and fails the no-dormant
theorem.  Equation (16) contains only one dependent summand and passes; its
sealed exhaust purifies the selected child kernel rather than storing the
unchosen child.

## 6. Additional fresh semantic attacks

These controls are independent of the four required Seat Q attacks.

### F1. Purely imaginary overlap — decisive

The `v=i` construction in Section 2.6 has no which-route record but shifts the
fringe.  It refutes dependence on `Re v` alone and fixes the verdict.

### F2. Seed-qubit polarity attack

If the seed bit were interpreted as the bond value for both color relations,
equal-color bonds would incorrectly have probability `16/25`.  The
source-controlled polarity `ell=z xor [d_i=d_j]` restores the exact Paper 13D
kernel and remains reversible because source and seed are retained.  The
candidate's “accepted fused witness” can realize this, but the explicit map
should be included in the revision to remove ambiguity.

### F3. Gauge-equivalent mixer used twice

Let `U=R diag(1,e^{i theta})`, which has the same squared moduli `B`.  Naively
using `U` at both mixer slots shifts the effective interferometer phase to
`phi+theta` up to outer phases.  Hence `B` does not select the neutral fringe.
The candidate survives because the real `R`, repeated-mixer orientation, and
phase reference are independently fixed apparatus data; no autonomous
selection theorem is earned.

### F4. External direct sum without source-indexed exhaust

Remove `[X]` from the exhaust label and choose distinct classical sources
that reach the same `m,[H]`.  Their orthogonal input fibers can then acquire
overlapping output vectors.  The v2 label `c_[X],m,[H]` kills this collision.
It still supplies only a family of fiberwise isometries, not a coherent source
state or root measure.

### F5. Arbitrary unitary extension off the initialized subspace

Injective basis computations have many unitary extensions, and arbitrary
choices need not be natural on unused basis states.  The initialized action
and all reported probabilities commute with presentation transport.  Thus the
candidate earns a natural initialized fiberwise instrument, not a canonical
global unitary on every unused apparatus vector.

### F6. Open-exhaust child future

Allow a future to act jointly on accessible history and exhaust.  It can
distinguish purifications and need not obey the Paper 13D child kernel.  The
candidate explicitly seals the exhaust; exact child recovery is valid only at
that declared open-system boundary and is not global reversibility.

## 7. Common-gate dispositions

| gate | disposition |
|---|---|
| homogeneous source referent and refusal | constructed |
| total tensor/fusion child pair | constructed as stochastic experiments |
| point-free naturality | constructed on each fixed sort summand |
| finite and pure-output quantum no-hiding | constructed at claimed scope |
| reversible query closure | constructed on the initialized ideal closed fiber |
| fused-witness physical phase | constructed and passes tensor substitution |
| exact `C_phi`, special phases, `B^2`, and `K_phi` | constructed |
| full pinned filter theorem | algebraically recoverable but omitted from the stated proof |
| general partial-record and visibility law | **failed** by complex overlap |
| fiberwise commit inner products | constructed, including different-source labels |
| joint normalization and supported child recovery | constructed |
| no accessible dormant child | constructed |
| positive restriction | constructed |
| degenerate branchwise restriction | constructed without target conflation |
| external tensor composition | constructed; no internal multi-mark theorem |
| simultaneous versus staged fusion | correctly distinct; no fusion algebra |
| local process plurality | constructed at triggered scope |
| activity/root/source measure | correctly unconstructed |
| regional, chronology, dimension, metric, gravity, actuality | correctly unconstructed |

## 8. Full product vector

Exactly one value is returned for every required coordinate:

```text
P22V2-HOMOGENEOUS-SOURCE-GROUPOID:
  CONSTRUCTED

P22V2-TOTAL-TENSOR-FUSION-CHILD-PAIR:
  CONSTRUCTED

P22V2-FIBERWISE-REVERSIBLE-QUERY:
  CONSTRUCTED

P22V2-FIBERWISE-COMMIT-INSTRUMENT:
  CONSTRUCTED

P22V2-EXACT-LOCAL-MODE-LAW:
  UNCONSTRUCTED

P22V2-POSITIVE-RESTRICTION-NATURALITY:
  CONSTRUCTED

P22V2-DEGENERATE-BRANCHWISE-RESTRICTION:
  CONSTRUCTED

P22V2-EXTERNAL-TENSOR-COMPOSITION:
  CONSTRUCTED

P22V2-SAME-SOURCE-MULTIMARK-COMPOSITION:
  UNCONSTRUCTED

P22V2-SIMULTANEOUS-FUSION-ALGEBRA:
  UNCONSTRUCTED

P22V2-ACTIVITY-ROOT-LAW:
  UNCONSTRUCTED

P22V2-PHYSICAL-REGIONAL-REFERENT:
  UNCONSTRUCTED

P22V2-CHRONOLOGY-DIMENSION-METRIC-GR:
  UNCONSTRUCTED

P22V2-ACTUALIZATION:
  UNCONSTRUCTED
```

`P22V2-EXACT-LOCAL-MODE-LAW` is negative for the frozen paper because that
coordinate includes the pinned partial-visibility family, not merely the
correct `C_phi`, `B^2`, and `K_phi` subcalculations.  No other coordinate is
demoted by this local error.

## 9. Strongest honest scope and required revision

The strongest surviving interpretation is:

> On every homogeneous positive Paper 13D source fiber, the candidate defines
> a point-free triggered instrument with a reversible structural query, a
> physically typed fused-witness phase, and a source-indexed dependent commit
> exposing exactly one accepted child.  It refuses heterogeneous sources
> before evaluation, restricts degenerate realized branches without merging
> their types, and composes only by external tensor product.

Its exact coherent law `C_phi`, stable-record law `B^2`, continuation interval,
normalization, and conditional child recovery are correct.  What is not
correct is the claimed general partial-record dependence on `Re v` alone.

A bounded revision must:

1. replace that rule by `q=Re(v e^{i phi})` and state the two visibility
   formulas, or explicitly restrict and calibrate a real phase-neutral record
   family;
2. state the complete filter/loss distributions and `I_2` theorem required by
   the pin; and
3. preferably spell out the source-controlled polarity mapping from the
   binary purification seed to the accepted equal/unequal-color bond kernel.

The first item is mandatory and decides this review.  It changes no source
domain, child kernel, neutral odds, or global scope, but it is a correction to
a frozen quantum law and therefore requires revision rather than an
adjudication wording clause.

Even after that correction, the object remains conditional on a supplied
classical source, active mark, input mode, and apparatus phase.  It gives no
source propensity, activity or root law, source superposition, physical
regional referent, global ensemble, chronology, dimension, signature,
topology, scale, metric, curvature, gravity, continuum physics, or
actualization.  Hilbert and Stinespring representations do not autonomously
select any of those data.

## 10. Freeze statement

This report is frozen as the sole Seat Q v2 delta-review artifact.  Its
ordinary post-freeze SHA-256, LF count, and byte count are returned with the
panel delivery; they are external to the bytes whose ordinary hash they
report.  No normalized or self-referential hash convention is used.
