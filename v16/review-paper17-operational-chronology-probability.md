# Paper 17 probability and identifiability review

Date: 2026-08-20

Seat: probability, quotient mass, response classification, and identifiability

Status: **REVISE / PARTIAL GAMMA-RELATIVE PRECEDENCE SURVIVES / DIMENSION CLOSED**

## 1. Corpus authentication and independence

I reviewed the frozen construction independently and did not inspect either
sibling Paper 17 report. I used no implementation, generated cases, Paper 15
or Paper 16 draft, or private preparation note.

| artifact | commit | independently reproduced SHA-256 | size |
|---|---|---|---:|
| Paper 17 pin | `f729d95` | `90a3dac7a1a7cf9eec063ae67475da908d9298d961635c3eb53d9152ad4b34bb` | 490 LF / 21,247 bytes |
| Paper 17 mathematical construction | `9433fb2` | `d90a20dc475a19834c1b0bcb9e33354aae987052a5e59ea229e26b5364c18a24` | 500 LF / 21,396 bytes |
| Paper 17 construction note | `9433fb2` | `667195be58a2ea1079f1b3bac6cd4ae5a5e9541c57c0135ea118ba78d9a9621f` | 145 LF / 5,931 bytes |
| Paper 17 review protocol | frozen working corpus | `51695ccb348c73649367f892455ca067c76f302741deab486e9480b8550d5949` | 167 LF / 7,871 bytes |
| accepted Paper 13D law | `a8cc45e` authority | `3b91766fe269e8145766f4622f99e9009c3270530670119735e6045a473de5c9` | 1,285 LF / 42,928 bytes |
| Paper 13D adjudication | `a8cc45e` | `ae2c4ef066335c39a0b8057c897c947b06e4270be67d1ed3ec59bf8d6d0a00c9` | 230 LF / 10,395 bytes |

All hashes match the review contract. The accepted Paper 13D mathematical law,
not its Rust realization, is the authority used below.

The user’s binding Barandes-aligned correction is observed exactly: the
localized reverse intervention at fusion remains **UNTESTED**. This report does
not convert its absence into a zero.

## 2. Verdict and first decisive semantic counterexample

**Verdict: REVISE.**

The accepted Paper 13D law need not change. The frozen Paper 17 result statement
is too weak when it concludes that the fusion pair supports no oriented
relation at all.

The first decisive counterexample to Corollary 4.1 is the normalized
trace-composition law already frozen in Paper 13D. Let

$$
f:S\longrightarrow D
$$

be any prefix ending at a complete typed division value and let

$$
g:D\longrightarrow T
$$

be any lawful suffix. For a prefix trace $H_f$ ending at $y$ and a compatible
suffix trace $H_g$, Paper 13D defines

$$
\Gamma_{g\circ f}(H_f\star_y H_g\mid z)
=\Gamma_f(H_f\mid z)\Gamma_g(H_g\mid y).
$$

Every suffix kernel is normalized. Therefore

$$
\sum_{H_g}
\Gamma_{g\circ f}(H_f\star_y H_g\mid z)
=\Gamma_f(H_f\mid z).
$$

The same equality holds after summing over any prefix-reader cell. It is
independent of which lawful normalized suffix $g$ is chosen. Thus every lawful
suffix operation has exact zero response on the complete prefix-history law.
This is **prefix-history autonomy**.

That theorem is not the missing localized reverse fusion intervention. The
latter remains unavailable and its coordinate remains `UNTESTED`. Prefix
autonomy instead universally quantifies over every suffix already admitted by
the fixed executable law. It supplies a distinct, law-level no-retroaction
statement.

For the smallest two-singleton fusion, a legal earlier source intervention
changes the unique newly written cross-bond probability by exactly $7/25$,
while the autonomy theorem proves that choosing any lawful fusion/future
suffix cannot change the earlier complete prefix distribution. Together these
facts support a scoped relation

$$
A\prec^{\mathrm{div}}_{\Gamma}\Phi,
$$

read only as “the accepted executable Gamma places the controlled component
prefix before this responsive fusion write across a complete division.” It is
not a complete chronology, causal order, spacetime arrow, or tested reverse
localized response.

There is a second reproducible correction. In a queried continuation with at
least two occurrences, the earlier program changes a carried color $d_i$, and
$D$ or $R_c$ then uses that color to write a **fresh endpoint bond**. Its
probability also moves by $7/25$. The frozen construction correctly says that
$q_2$ has zero response, but its final classification of the whole continuation
effect as mere carry overlooks this newly written bond coordinate.

These are mathematical result-statement corrections inside Paper 17. They are
not requests for a new intervention port, a changed Paper 13D kernel, or code.

## 3. Independent reconstruction

### 3.1 Point-free quotient and multiplicity

A response comparison transports the execution diagram, generator vertex,
complete local trace, intervention mark, alternatives, context, target probe,
reader, and outcome together. For a fixed reader-free experiment, physical
mass is the sum over the complete stabilizer orbit. Hence an automorphism that
exchanges two presentations does not halve the mass, and duplicating one
presentation creates no new physical happening.

For a two-singleton fusion contrast, the intervention mark distinguishes the
controlled component relationally. The unique cross pair is the unordered
set $\{i,j\}$, so its bond reader is invariant under endpoint exchange. The
two bond outcomes remain separate physical cells, and their orbit-pushforward
masses are the threshold probabilities themselves. The $7/25$ difference
therefore survives quotienting.

Occurrence multiplicity must be understood as the finite vertex family inside
the quotient execution object, not as the set of distinct isomorphism types of
pointed germs. Two tensor vertices exchanged by an automorphism remain two
elements of that finite family even when they have one orbit type. With this
groupoid-multiplicity reading, the construction descends correctly. Reading
$\mathcal H$ literally as a set with duplicate orbit types discarded would
violate the pin and is not accepted by this review.

### 3.2 Complete experiment classes

The accepted localized marks are exactly $X,Y,E,E'$ addresses at generator
vertices sourced by $B_0$. A complete source context is held fixed between
alternatives. Exact later-boundary values are lawful whole-boundary
preparations, but changing such a value is not silently promoted to a local
`set` mark. Diagnostic readers are equivariant maps chosen after the physical
fiber; a complete reader is the identity on that fiber.

There are also accepted **operation choices** between typed arrows, for example
the identity and $F_q^A$ as $B_3^r$ endomorphisms. Such a choice is a lawful
suffix comparison but is not one of Paper 13D’s localized intervention
contrasts. Prefix autonomy covers all these suffix choices. It does not turn a
whole-boundary preparation change into a local causal break.

No localized mark exists at $D,R_c,\Phi,\iota,F_q,F_t,F_{XY},F_r$, or $E_r$.
In particular, the requested reverse fusion intervention is mathematically
absent from the accepted experiment interface and remains `UNTESTED`.

### 3.3 Internal response and mediation

At one $B_0$-source generator and fixed $y,\epsilon$,

$$
e'=\epsilon\oplus x\oplus y
$$

is flipped by `set(X)`. At fixed $x,y$, `set(E)` flips $(x',y')$. Without an
$E'$ override,

$$
z_Y=y\oplus e'=\epsilon\oplus x,
$$

so `set(X)` has unit response at fixed $E$. Holding $E'$ fixed makes
$z_Y=y\oplus E'$ independent of $X$, and the residual is zero. Parity can
cancel while the complete pair reader remains nonzero. These are internal
relations among fields written at one typed generator occurrence, not edges
between separate occurrences.

### 3.4 Exact tensor zero

For independent factors,

$$
\Gamma_D(e_A\boxtimes e_B)
=\Gamma_D(e_A)\otimes\Gamma_D(e_B).
$$

Changing a legal program in $A$ changes only the first factor. For every common
context and every complete or equivariant coarse reader on $B$, summing over
the $A$ factor leaves precisely the unchanged normalized $B$ law. The signed
measure is therefore zero cell by cell. The same proof holds with $A,B$
exchanged.

If an automorphism exchanges two isomorphic components, the transported mark
and target profile are included in the diagonal contrast. Orbit pushforward of
two equal zero signed measures is still zero. Thus tensor zero is complete and
bidirectional after quotient, not a representative-coordinate cancellation.
It certifies scoped incomparability of the two independently manipulable
factor occurrences.

### 3.5 Smallest fusion response

Take singleton components $I_A=\{i\}$ and $I_B=\{j\}$ whose relational packets
have already been constructed. There are no within-component bonds. Fusion
writes the sole cross bond $\ell_{ij}$ from one fresh uniform seed:

$$
P(\ell_{ij}=1\mid d_i=d_j)=\frac9{25},
\qquad
P(\ell_{ij}=1\mid d_i\ne d_j)=\frac{16}{25}.
$$

With no $E'_i$ override and fixed $Y_i,E_i,d_j$, toggling $X_i$ toggles
$d_i=E_i\oplus X_i\oplus Y_i$. Consequently

$$
\left|\Delta^Gamma_{X_i\to\ell_{ij}}(1)\right|
=\frac{16-9}{25}=\frac7{25}.
$$

The zero outcome has the opposite signed difference, so the signed measure
normalizes to zero and its total-variation distance is $7/25$. The cross bond
is absent from the tensor prefix and is written only by fusion. This is a true
new-write response at a distinct generator occurrence.

It is not direct in the pin’s mediator-free sense. The intervention first
changes the complete boundary color $d_i$, which fusion reads to write the
bond. It is a total response mediated by a declared carried boundary field.
That classification still supports scoped precedence when combined with
prefix autonomy; it does not establish primitive causal adjacency.

The exact $7/25$ statement is context-indexed. If the other endpoint color is
unfixed and fair, marginal signs can cancel. The complete atlas retains the
fixed equal- and unequal-color rows and does not promote an averaged zero.

### 3.6 Queried continuations: carry versus new write

Let $I=\{i,j\}$ and use $D\circ Q_J^0$ or $R_c\circ Q_J^r$. Choose a common
source context/base program fixing $Y_i,E_i$ and the neighbor color $d_j$, with
no $E'_i$ override. The two alternatives `set(X_i,0)` and `set(X_i,1)` flip
$d_i$ in the relational packet at the complete $B_1$ frontier.

The continuation copies $a$, so the visible change in $a$ is carried
persistence. Its newly generated process field obeys

$$
q_{2k}=\beta(m_k,u_{2k}).

Because the registered relational control changes neither $m_k$ nor $u_{2k}$,
every complete $q_2$ response coordinate is exactly zero. That part of the
frozen classification is correct.

But $B_1$ has no bond field. $D$ and $R_c$ draw fresh pair seeds and generate
$\ell_{ij}$ from the carried $d_i,d_j$. The two alternatives therefore change
the newly written continuation-bond probability by $\pm7/25$. The mark anchors
$i$, the unordered bond reader descends, and the result survives orbit
pushforward. This is a mediated prefix-to-continuation new-write response, not
mere carry.

### 3.7 Prefix-history autonomy

For any prefix reader $R_f$ and reader cell $A$, normalized structural
recursion gives

$$
P_{g\circ f}(R_f\in A)
=\sum_{H_f:R_f(H_f)\in A}\Gamma_f(H_f)
  \underbrace{\sum_{H_g}\Gamma_g(H_g\mid y(H_f))}_{1}
=P_f(R_f\in A).
$$

This holds for every lawful suffix: stochastic continuations, fusion, stable
future words, erasure, tensor combinations, identities, and their arbitrary
finite composites. It also holds for two same-sourced suffix choices $g,g'$,
even if their target types differ, because their prefix marginals live on the
same prefix fiber.

The theorem requires a genuine typed composition boundary carrying the full
law-relevant value. It cannot be inserted through the primitive $U$ path’s
native nondivision cut, and postselection is not a normalized lawful suffix
operation for this purpose. Thus the theorem respects rather than Markovizes
Paper 13D’s indivisibility.

Prefix autonomy is more than the visual direction of a string diagram: a
globally assigned or postselected joint law could use the same drawn diagram
while reweighting prefix histories as a function of the chosen suffix. Paper
13D’s normalized structural recursion forbids that countermodel. Still, the
result is explicitly relative to the primitive executable composition of
$\Gamma_D$; it is not an independently derived spacetime arrow.

## 4. Mandatory semantic questions

| question | result | probability/identifiability finding |
|---|---|---|
| 1 point-free generator occurrence | PASS WITH MULTIPLICITY SCOPE | The complete pointed local trace descends diagonally; the finite vertex family, not only distinct orbit types, must be retained. |
| 2 minimal co-onset | PASS ONLY RELATIVE TO `Exec_D` | Algebraic kernel factorization does not split an atomic typed arrow. A finer physical onset would require another accepted arrow, which is absent. |
| 3 automorphism multiplicity | PASS WITH THE SAME SCOPE | Orbit mass and the cardinality of the vertex family survive; representative mass or a set of orbit types would fail. |
| 4 braiding/permutation/reparenthesization | PASS | They transport vertices, marks, bonds, and readers and do not alter response or the finite occurrence family. |
| 5 exact experiment classes | PASS | Localized $B_0$ marks, exact preparations, suffix operation choices, contexts, and readers are distinct constructors. |
| 6 later-boundary preparations | NOT A LOCAL MARK | They define accepted conditional kernels but do not supply the missing localized reverse contrast. |
| 7 same-typed arrow choices | ACCEPTED AS SUFFIX CHOICES | They are covered by prefix autonomy but are not silently inserted into the localized response tensor. |
| 8 reverse experiments | UNTESTED AT FUSION/CONTINUATION/FUTURE/ERASER | No accepted localized address exists there. This report does not infer zero. |
| 9 prefix-history independence | PASS, INDEPENDENT LAW THEOREM | It follows from normalized structural recursion for every suffix, not from absence of a reverse API method. It remains Gamma/composition-relative. |
| 10 internal response | PASS | The $X/E/E'$ mediation and complete-reader rows reconstruct exactly. |
| 11 bidirectional tensor zero | PASS | Product normalization proves cellwise zero in every common context after quotient. |
| 12 singleton fusion $7/25$ | PASS | The unique fresh unordered bond changes from $9/25$ to $16/25$ or conversely. |
| 13 directness of fusion response | TOTAL/MEDIATED, NOT PRIMITIVE DIRECT | The carried color $d_i$ is the declared mediator into the fusion write. |
| 14 carried versus newly written | FROZEN CLASSIFICATION REQUIRES REVISION | $q_2$ is zero and $a$ is carried, but endpoint bonds written by $D,R_c$ have exact $7/25$ response. |
| 15 record/division/nondivision | PASS AS DISTINCT CONTROLS | Persistence, erasure, restart sufficiency, and native-cut obstruction do not themselves orient an edge. |
| 16 need for bidirectional census | NOT NECESSARY FOR A SCOPED EDGE | Forward new-write response plus universal suffix-choice prefix autonomy orients a Gamma-relative precedence edge. It remains necessary for the construction’s stronger complete localized chronology claim. |
| 17 partial influence relation | CONSTRUCTED | Fusion and queried-continuation cases yield scoped mediated precedence; tensor gives tested incomparability. |
| 18 empty relation | REJECTED | It discards known forward response and the new autonomy asymmetry; vacuous order axioms are not a scientific result. |
| 19 hidden order inputs | PASS WITH SCOPE | The edge predicate uses a typed complete division, normalized Gamma recursion, and response. It is invariant under labels and serialization, but explicitly relative to compositional process direction. |
| 20 dimension stopping line | PASS | Partial precedence and an incomplete census do not supply the complete chronology-valued ensemble required by the preregistered dimension battery. |

## 5. Fresh countermodels and controls

### C1 — factorized kernel inside an atomic arrow

The primitive endpoint kernel admits formal identities such as $C=CI=IC$.
That algebraic factorization does not create an intermediate typed physical
boundary or split the atomic $U_J$ generator germ. Kernel factorization alone
cannot count two onsets.

### C2 — equal endpoint information, different retained traces

A primitive arrow and a staged typed composite can be arranged to have a
chosen equal coarse endpoint marginal while the composite retains an
intermediate boundary and the primitive does not. The complete trace reader
distinguishes them. Endpoint equality cannot identify onset structure or erase
a physical division.

### C3 — later whole-boundary preparation

Supplying $B_1$ with $m=0$ versus $m=1$ changes the standalone $D$ law for
$q_2$. This is a lawful preparation contrast between two complete source
values, but it has no localized intervention mark within $D$. Treating it as
the missing reverse local experiment would silently extend the contrast
object.

### C4 — same-typed suffix operation choice

At $B_3^r$, choose the identity suffix or $F_q^A$. Their final $q^+$ laws
differ deterministically, yet normalized recursion gives exactly the same
complete prefix-history marginal. This realizes prefix autonomy with an actual
accepted operation choice rather than an absent fusion port.

### C5 — positive forward response without a target local port

The two-singleton fusion experiment has a $7/25$ forward cross-bond response
and no localized fusion intervention address. The reverse localized coordinate
is untested, while suffix-choice prefix autonomy is exact. This countermodel
separates those two statements.

### C6 — serial mediated new write

In $Q_J^0;D$, `set(X_i)` changes carried $d_i$, and $D$ writes a fresh bond
whose law changes by $7/25$. Holding the mediator $d_i$ fixed makes that
residual zero. This is a serial mediated response and the direct counterexample
to classifying the whole continuation result as carry only.

### C7 — XOR cancellation and common cause

Changing $E$ flips $(x',y')$ but not their parity, so a coarse reader reports
zero while the complete reader does not. Separately, conditioning on $x=1$
gives $P(y=1)=337/625$, whereas `set(X,1)` leaves $y$ fair. Neither cancellation
nor common cause can replace the complete intervention tensor.

### C8 — tensor automorphism

Take two isomorphic singleton tensor experiments with an automorphism
exchanging factors. The full diagram contains two vertices. A transported mark
anchors the manipulated one, and the other factor’s complete law remains
unchanged. Counting orbit types would yield one vertex; retaining the finite
vertex family and orbit mass yields two occurrences and exact mutual zero.

### C9 — stable but incomplete, complete but unrecorded

The record-only restriction of $B_1^r$ is stable but omits $h,a$ and is not
future-sufficient. Complete $B_1^0$ is a positive division but writes no record.
Thus neither records nor divisions may be used as an automatic precedence or
happening rule.

### C10 — nonlawful postselected suffix

A postselection weight depending on the suffix outcome can reweight prefix
histories and violate prefix autonomy. It is not a normalized unconditional
suffix kernel in $\mathsf{Exec}_D$. This shows both that autonomy is substantive
and that its quantifier must remain “every lawful normalized suffix.”

## 6. Full product vector

```text
input       P17-INPUT-P13D-TERMINAL-GAMMA-BOUND
quotient    P17-POINT-FREE-ONSET-AND-RESPONSE-DESCENT-CONSTRUCTED
experiment  P17-BIDIRECTIONAL-EXPERIMENT-CENSUS-INCOMPLETE
response    P17-COMPLETE-ACCEPTED-RESPONSE-ATLAS-CONSTRUCTED
co-onset    P17-PRIMITIVE-GENERATOR-ONSET-BUNDLES-CONSTRUCTED
chronology  P17-PARTIAL-GAMMA-RELATIVE-PRECEDENCE-CONSTRUCTED
ensemble    P17-CHRONOLOGY-VALUED-ENSEMBLE-NONE
dimension   P17-DIMENSION-NONE-CHRONOLOGY-GATE-UNPASSED
signature   P17-SIGNATURE-UNCONSTRUCTED
metric      P17-METRIC-UNCONSTRUCTED
actuality   P17-ACTUALIZATION-UNCONSTRUCTED
```

The response coordinate survives because the complete Paper 13D contrast
family already contains every bond cell; Paper 17’s prose classification of
the continuation row is what failed. The revision must expose that existing
new-write coordinate and add the prefix-autonomy theorem. It must not change a
probability or add an intervention.

`PARTIAL` is deliberately weaker than the pin’s locally finite operational
chronology target. It consists only of presentation-covariant, division-scoped
precedence edges certified by both a nonzero forward new-write response and
universal normalized-suffix prefix autonomy, together with the bidirectionally
tested tensor incomparability control. All missing localized reverse fusion
coordinates remain `UNTESTED`.

## 7. Ensemble and dimension consequences

Each accepted history remains finite, and the scoped edge predicate is
presentation-covariant, so one may decorate such a history with the certified
partial precedence facts without introducing a loop index. That observation
does not construct the preregistered **complete operational chronology** on the
candidate happening family. Untested pair classes remain unresolved, and the
direct/mediated census is incomplete.

Accordingly the full chronology-valued pushforward demanded by the pin remains
`NONE`. A separate “partial-edge-decorated history ensemble” could be named as
an upstream object, but it is not the ensemble on which the frozen dimension
battery was authorized to run.

Ordering fraction, longest chains, higher-chain counts, midpoint scaling,
interval abundance, and small-poset frequencies would all depend on how the
unresolved pairs are completed. Running them on the typed execution order or
on only the certified sparse edges would answer a different question. The
dimension result therefore remains exactly

```text
P17-DIMENSION-NONE-CHRONOLOGY-GATE-UNPASSED
```

This provides no evidence for or against any dimension and does not authorize
a nonmanifoldlike verdict, signature, scale, or metric.

## 8. Semantic versus implementation classification

The revision is entirely semantic and theorem-level. It uses the already
accepted normalization, structural recursion, complete divisions, source
interventions, and bond generator. No Python or Rust behavior is relevant.

Changing Paper 13D to add a reverse fusion port would be a successor-law
proposal and is not authorized here. By contrast, deriving prefix autonomy and
recognizing the already present continuation-bond coordinate change no
boundary, generator, hom-set, reader, seed, or probability. They revise only
Paper 17’s classification and stopping statement.

## 9. Scope and ontology walls

The scoped edge is relative to the executable composition of $\Gamma_D$ at a
complete division. It is not a proof that category-arrow direction is an
emergent spacetime time, nor a theorem about interventions absent from the
law. “Prefix” and “suffix” refer to the accepted process composition, not a
global clock or coordinate time.

The fusion and continuation responses are mediated total responses into newly
written bonds. They are not primitive direct causal adjacencies. Tensor zero is
operational incomparability only within the accepted experiment class. Stable
transport, erasure, division, and native nondivision remain separate from
causal influence.

No result here constructs a complete chronology, actual history, dimension,
Lorentzian signature, topology, volume, duration, scale, metric, curvature,
gravity, continuum physics, QFT, particles, or phenomenology.

## 10. Bounded revision required

Paper 17 should be revised, without altering Paper 13D, to:

1. state and prove normalized-suffix prefix-history autonomy;
2. retain reverse localized fusion response as `UNTESTED`;
3. promote only the scoped $\Gamma$-relative partial precedence coordinate;
4. classify fusion response as mediated through carried $d$ into a new bond;
5. add the analogous $D/R_c$ newly written endpoint-bond response for
   nonempty pair sets while retaining exact zero for newly written $q_2$;
6. clarify that onset multiplicity is the finite vertex family in the quotient
   object, not the set of distinct orbit types; and
7. leave the complete chronology, ensemble, dimension, signature, metric, and
   actuality gates closed.

This is a `REVISE` under the protocol: the frozen Paper 17 result statement is
wrong in scope, but the accepted Paper 13D law remains unchanged.
