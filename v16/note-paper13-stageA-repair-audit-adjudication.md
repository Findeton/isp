# Paper 13 Stage-A support-split repair audit adjudication

Date: 2026-08-19

## 1. Authority

This note adjudicates only the bounded Stage-A forward repair frozen by
`note-paper13-stageA-support-split-forward-repair-pin.md`, SHA-256
`8ae54ada2a97f347a18b90adcab86dcb2e7c18c04c748cc5e0779b8251449a36`.
It does not adjudicate a Paper 13 scientific result.

The immutable chain is:

| object | identity |
|---|---|
| repaired-source commit | `f77e184b3ff6067c8014e5ff790df5cae806857a` |
| repaired evaluator | `3da3161c7eef63b90da9c6cb85f7bc918d6e5c99fa431f07d273efd1f18e519e` |
| source-freeze v2 note | `216ffd6ad9559f68d54dedc46cc4be82146619659f0493b120689a814965f9f6` |
| Physics audit commit | `4a48f2a59581ba5b399148d24737731eab8001f3` |
| Physics report | `f51d4213c2b440a54ea17d5c9d28ee078de16a9b57f03d9381cfdfb7ed5faaf2` |
| Records audit commit | `acaebced1ecba453857f622d33241c2a69bcbe50` |
| Records report | `13b9a1cedc7e546e9d2f0f9937d355831a9fe0de2f28172f5371721f741e520e` |

The two reports were mutually blind during scientific work and both return
`ACCEPT`.  Neither report imported evaluator measurements, ran a fresh or
official mode, or changed the source under audit.

## 2. Question and precedence

The only question is whether the #197 bytes close the adjudicated
`TAUTOLOGICAL-CHILD` support-recognition failure and satisfy the #196
forward-repair contract.  Earliest applicable #196 disposition governs:

1. `REPAIR-SPECIFICATION-INCONSISTENT`;
2. `P13-REFERENT-PRESENTATION-ONLY`;
3. `P13-SUPPORT-CHANGE-UNPROVEN`;
4. `REPAIR-GREEN-UNREVIEWED`.

The last label is the frozen successful repair disposition.  Its literal word
`UNREVIEWED` is not rewritten after the fact; the independent reviews recorded
above are the evidence that licenses this adjudication and the next stage.

## 3. Reconciled findings

### 3.1 Authentication and replay

Both seats authenticate the same source, note, pin, and commit.  Repository
root, alien CWD, and true source-only off-tree/no-`.git` runs are byte-identical
at 27,380,971 bytes, stdout SHA-256
`8ce3c4afbf1cb99be5f5408155af9a3123a6cdf485a8221248b5f31ec893d3e1`,
and normalized payload SHA-256
`0894a736dd27ac2be619c0c4e9f24e79ac897b1d8675e2176096df40e7d307fa`.
All 42 checks pass and all 92 registered attacks are killed.  No fixture,
fresh case, official artifact, or publication path is read or written.

### 3.2 Exact support theorem and contextual quotient

Both seats independently reconstruct the literal source-cell fibers.  A false
parent has child bits `{0}`; a true parent has `{0,1}`.  Exact roles, old type
preservation, target exhaustiveness, disjoint fibers, count identity,
forgetting, child freshness/type, `P and N`, and `P and not N` are all native
coordinates.  Recovery, role count, cell count, labels, or a supplied Boolean
cannot substitute.

The contextual census is exactly:

```text
C1  3/3
C2 15/15
C3 14/7
C4 14/7
C5 14/7
C6 12/3
total 72 ambient representatives / 42 contextual classes.
```

The C3 aliases `B` and `A and B` retain distinct raw syntax hashes but have the
same contextual parent, source, Arrow, target, operator, endpoint law, lineage,
and bound certificate.  Raw formula bytes are provenance only.

### 3.3 Bound process use

The diagnostic `ContextSplitProof` is nonpromotive.  Promotion consumes the
separate immutable `BoundSplitCertificate`, rebuilt from a nonzero candidate
Gamma transition and bound to the actual law, source, Arrow, occurrence, port,
input/output bit, sector, contextual parent, child, target, operation, and
classifier hash.

The Records seat independently derives the 468-transition total as 156 CREATE
+ 156 MERGE + 156 UNCHANGED over 312 source columns.  Both seats rebuild all
twelve fully serialized representative certificates as 4/4/4 and reproduce
their literal fibers and classifier-consumed hashes.  The promotive AST
contains all required certificate keys and no legacy role/cell-count key.

### 3.4 Mandatory attacks

Both seats replay the real final-byte S1 source mutation.  It reproduces mutant
SHA-256
`edb8e4900ab77f61c264348080d18140d918489d2653e173059bc2e88babb2c7`
and exits nonzero.  Forgetting survives, but the satisfying fiber contains
only child bit `{1}`, the count residual is `-1`, `P and not N` is false, and
support/variable-carrier promotion fails.

Across the two reports, independently constructed S2, S4, S6, S8, S9, S10,
S11, supplied-Boolean, branch/type/merge, freshness, and ambient controls all
refuse or demote for their registered semantic reason.  The real S5 source
mutation and its registered certificate-object companion move the required
source/gate/seal and measurement/lineage/claim dependencies.  No copied final
Boolean, stale certificate, or unrelated valid split can earn promotion.

### 3.5 Preserved science and ontology wall

The exact `R/B/C/B2/K`, `527/175` native nondivision certificate, positive
history-conditioned scope control, division/eraser, reciprocal joint, source
groupoid, matching resources, and class-relative blind argument are unchanged.
This repair certifies a proper finite Boolean-algebra support extension and its
recognition by the evaluator.  It does not derive a valuation, distance,
dimension, topology, metric, curvature, continuum, gravity, GR, QFT, law
selection, autonomous event selection, or actualization.

## 4. Adjudication

The two `ACCEPT` reports are adopted without repair or narrowing.  No
independent survivor exists.  The governing disposition is therefore:

> **`REPAIR-GREEN-UNREVIEWED` — ADJUDICATED ACCEPT FOR STAGE-B ELIGIBILITY.**

This closes only the #195 support-recognition debt in the instrumentation.  It
does not erase the historical #192 rejection, award the Stage-A printed
scientific ceiling, or make any Paper 13 claim citable.

## 5. Orders and next authorized event

1. The #197 evaluator and v2 note are immutable.  No source edit is permitted.
2. The sole next authorized scientific event is Stage B from the original
   Paper 13 construction pin: one nonce-bound fresh generation followed by one
   official construction run using the exact #197 bytes.
3. Stage-B changed paths are exactly:

   ```text
   v16/code/p13_gamma_fresh_cases.json
   v16/code/p13_gamma_output.txt
   v16/code/p13_gamma_receipt.json
   v16/note-paper13-gamma-verification.md
   v16/PLAN.md
   v16/LOG.md
   STATUS.md
   ```

4. Fresh cases use the frozen SHAKE256/no-reroll protocol.  Existing exposed
   anchors are never relabeled as fresh.  Source bytes commit before nonce/case
   generation, as they now do.
5. `--generate-fresh` runs once to the absent frozen destination.  Official
   `--run` follows only after authentication and also writes only absent
   destinations transactionally.  A later source edit voids the cycle.
6. Stage C remains closed until the Stage-B receipt is independently verified.
   Paper drafting, the hostile Paper 13 panel, and every metric-or-later unit
   remain unauthorized now.
7. Any Stage-B integrity failure, semantic survivor, reroll, oracle leak,
   output collision, or scope promotion is recorded and adjudicated.  It is
   never silently repaired under these frozen bytes.

## 6. Final scope sentence

The accepted repair shows that the verifier now distinguishes a genuine
point-free support split from a coextensive presentation label.  Whether the
one-Gamma candidate survives fresh construction, whether its raw relation is
load-bearing beyond the frozen blind class, and whether any later order,
valuation, metric, curvature, or GR structure can be derived remain open.
