# Paper 22 v2 independent mathematical delta-review protocol

Date: 2026-08-21

Status: **FROZEN THREE-SEAT PROTOCOL — RESULT UNKNOWN**

This protocol reviews the one bounded homogeneous-source replacement. It
authorizes no implementation, repair, Paper 23 construction, regional law,
chronology, dimension, metric, or gravity result.

## 1. Immutable corpus

| object | ordinary SHA-256 |
|---|---|
| terminal Paper 13D law | `3b91766fe269e8145766f4622f99e9009c3270530670119735e6045a473de5c9` |
| predecessor adjudication | `c261520aa142bf07a489f87cd0364628f094794c7523c03d8ba3dde05d824a07` |
| Paper 22 v2 pin | `a4c1c2ecd10edad73ed64b12f699c09d7cfd169d4cd264939990589554693627` |
| Paper 22 v2 candidate | `30340295ccd5f8371a9020cb76c0a93cc24ab14cbbf78f05c01a85ca5ce86468` |
| Paper 22 v2 construction note | `e90bdd14ca04742bb09eb3c9ec928b4aea04ff6429b0896158ce83170280a0ae` |

Reviewers authenticate every file they use before scientific inspection and
reauthenticate the v2 pin and candidate before freezing their report. Moving
bytes stop the review.

The old Paper 22 candidate may be consulted only through the binding
adjudication and immutable numerical anchors. The frontier census, semantic
input audit, Paper 23 pin, private regional investigations, and every sibling
report are excluded.

## 2. Mutual blindness and write isolation

The three seats are:

1. source/category/functor totality;
2. probability/instrument normalization; and
3. quantum/no-hiding/physical scope.

Each reviewer writes exactly one assigned report, does not read or contact a
sibling before all reports freeze, and does not edit any corpus file. No
reviewer stages or commits.

Assigned paths:

```text
v16/review-paper22-reversible-structural-instrument-v2-category.md
v16/review-paper22-reversible-structural-instrument-v2-probability.md
v16/review-paper22-reversible-structural-instrument-v2-quantum.md
```

## 3. Common review order

Every seat must:

1. authenticate the complete corpus;
2. read the pin and candidate in full;
3. reconstruct its assigned definitions and proofs independently;
4. reproduce the heterogeneous predecessor counterexample and verify refusal
   occurs before child evaluation;
5. check all immutable numerical anchors relevant to the lens;
6. evaluate all twenty-two hostile controls, including controls outside the
   primary lens at semantic-summary level;
7. add at least four fresh countermodels or adversarial constructions;
8. return every product coordinate independently;
9. state the first decisive semantic counterexample, if any; and
10. reauthenticate the frozen candidate.

Code may be used privately for exact arithmetic, but implementation behavior,
serialization, runtime, or a passing script cannot prove a typed theorem.

## 4. Seat C — source, category, and functor totality

Seat C must decide:

### C1 — source referent

Does the coproduct

$$
\coprod_s\mathsf{Src}^{\ge2}_s
$$

define complete point-free source objects without an ordering, downstream
child, result, or geometry field?

### C2 — membership and refusal

Verify all four source discriminators:

- a homogeneous positive pair is admitted;
- the exact (B^0_1,B^0_2) heterogeneous pair is refused;
- a sort-changing purported arrow is absent; and
- a differently sorted spectator is admitted without changing the active
  summand.

Refusal must precede any call to fusion, query, commit, or probability.

### C3 — child totality

For every admitted object, verify that the tensor and simultaneous-fusion
assignments have complete typed source, target, arrow, and conditional kernel.
Check that stochastic fusion is not treated as a deterministic point map.

### C4 — naturality

Reconstruct the naturality squares for child experiments, seeds, witnesses,
phase projector, inverse query, commit, child readers, futures, and exhaust
labels. Test component permutations, occurrence relabelings, spectator
transports, and a forbidden sort change.

### C5 — restriction

Check separately:

- positive-to-positive restriction and seed marginalization;
- zero-active tensor versus fusion target types;
- one-active tensor versus fusion target types;
- retention of the stable mode record; and
- absence of a newly triggered opportunity at a degenerate target.

### C6 — composition

Prove or refute external tensor naturality. Verify that it does not establish
a same-source multi-mark object or identify staged fusion with one
simultaneous n-ary generator.

### Required fresh attacks

At minimum add:

1. two equal-looking values at distinct atomic stage sorts;
2. a spectator isomorphic in presentation to an active component but of a
   different typed role;
3. a restriction removing an entire active component orbit; and
4. an attempted braiding that exchanges an active component with a spectator.

## 5. Seat P — probability and instrument normalization

Seat P must independently derive:

### P1 — mode lift

Recover the gauge class of `R` from `B` and verify orthogonality and squared
moduli exactly.

### P2 — phase family

Recompute (C_\phi), the values at (0,\pi/2,\pi), the stable-record
(B^2), the continuation (K_\phi=C_\phi B^{-1}), and the exact positivity
interval.

### P3 — commit isometry

Verify equation (15) from full history and source-indexed exhaust labels.
Check same-source, different-mode, different-history, and different-source
inner products.

### P4 — joint normalization

Prove that the complete joint law normalizes for every admitted source,
input mode, and phase, and that conditioning on the stable mode record recovers
the exact child kernel.

### P5 — no dormant child

Check that the accessible output is a dependent coproduct rather than a
product with a traced but still physical unchosen child.

### P6 — noninheritance

Construct at least two distinct external source propensities yielding the same
fiberwise local law. Verify that exhaust orthogonality and local mode odds do
not select either propensity.

### Required fresh attacks

At minimum add:

1. an undefined heterogeneous child assigned zero probability;
2. two source fibers sharing an unindexed exhaust basis;
3. a normalized local law multiplied by two different source measures; and
4. a postselected phase chosen from the final mode.

## 6. Seat Q — quantum, no-hiding, and physical scope

Seat Q must decide:

### Q1 — finite and quantum no-hiding

Reconstruct both no-hiding proofs and test whether they really force a
complement for a many-to-one accessible fusion.

### Q2 — reversible query

Verify that the controlled witness computations are injective on the retained
typed basis and can extend to unitaries. Determine whether every seed,
witness, complement, and environment field returns to one common state before
recombination.

### Q3 — physical phase

Check that the fused-witness projector is presentation invariant and attached
to an operationally different route. A phase on a renamed abstract bit does
not pass.

### Q4 — record, erasure, and visibility

Reconstruct the coherent, stable-record, reversible-erasure, and partial
visibility laws. Verify that erasure removes every physical record copy.

### Q5 — query versus commit

Check that the reversible query is not called accessible erasing fusion and
that the commit exposes exactly one selected child with a stable record.

### Q6 — physics walls

Verify that the construction remains conditional on a supplied source,
active mark, input mode, and phase, and that no activity, root, chronology,
dimension, metric, curvature, gravity, or actuality claim is smuggled in.

### Required fresh attacks

At minimum add:

1. an orthogonal residual complement after apparent uncomputation;
2. a phase applied to an untyped route-label ancilla;
3. a readable environment copy surviving record erasure; and
4. an accessible register retaining the unchosen child.

## 7. Mandatory predecessor regressions

Every seat must state whether the following are killed:

1. heterogeneous active-sort source;
2. sort-changing source morphism;
3. zero/one-active target conflation;
4. same-source multi-mark closure inferred from disjoint tensoring; and
5. staged fusion identified with simultaneous fusion.

A failure of any regression is at least `REVISE`.

## 8. Product coordinates

Each report must return exactly one value for every coordinate:

```text
P22V2-HOMOGENEOUS-SOURCE-GROUPOID
P22V2-TOTAL-TENSOR-FUSION-CHILD-PAIR
P22V2-FIBERWISE-REVERSIBLE-QUERY
P22V2-FIBERWISE-COMMIT-INSTRUMENT
P22V2-EXACT-LOCAL-MODE-LAW
P22V2-POSITIVE-RESTRICTION-NATURALITY
P22V2-DEGENERATE-BRANCHWISE-RESTRICTION
P22V2-EXTERNAL-TENSOR-COMPOSITION
P22V2-SAME-SOURCE-MULTIMARK-COMPOSITION
P22V2-SIMULTANEOUS-FUSION-ALGEBRA
P22V2-ACTIVITY-ROOT-LAW
P22V2-PHYSICAL-REGIONAL-REFERENT
P22V2-CHRONOLOGY-DIMENSION-METRIC-GR
P22V2-ACTUALIZATION
```

Positive and negative coordinates are independent. No ladder promotion is
permitted.

## 9. Verdicts and stopping rule

Allowed verdicts:

```text
ACCEPT
ACCEPT-WITH-SCOPE
REVISE
REJECT
INTEGRITY-BLOCK
```

`ACCEPT-WITH-SCOPE` permits only wording bounds already entailed by the unique
mathematical object. A failed definition, totality theorem, numerical anchor,
mandatory control, or product coordinate is at least `REVISE`.

This is the only bounded v2 review. Reports are evidence, not acceptance.
After all three freeze in separate commits, a joint adjudication is required.
If a semantic counterexample survives, no automatic Paper 22 v3 is
authorized.

## 10. Report freeze requirements

Each report contains:

- authenticated corpus hashes;
- lens reconstruction;
- predecessor-regression table;
- all assigned and common controls;
- at least four fresh attacks;
- first decisive counterexample or `none`;
- full product vector;
- verdict and exact scope;
- ordinary SHA-256, LF count, and byte count after freeze; and
- a statement that no corpus edit, implementation, staging, or commit was
  performed.

No report may cite a sibling, private investigation, or future artifact.
