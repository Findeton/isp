# Paper 13D mathematical construction certificate

Date: 2026-08-20

Status: **MATHEMATICAL CANDIDATE / GREEN-UNREVIEWED / NO IMPLEMENTATION**

## 1. Bound authority

Physics pin:

- path: `v16/note-paper13d-typed-executable-gamma-physics-pin.md`
- commit: `a20c52a`
- SHA-256: `722dc3bfe528fc3a52f2d2f5afcaba2c7858250e63d24439e43d0a17ab5c049e`
- size: 459 LF lines / 14,240 bytes

Terminal parent adjudication:

- path: `v16/note-paper13c-interventional-gamma-math-adjudication.md`
- commit: `7377b53`
- SHA-256: `f9a36eb8d59996627e6aff68f52eaf5c0526be181cc80b8fc3550a0b4e930cd9`

Mathematical candidate:

- path: `v16/paper-13d-typed-executable-gamma.md`
- SHA-256: `3b91766fe269e8145766f4622f99e9009c3270530670119735e6045a473de5c9`
- size: 1,285 LF lines / 42,928 bytes

The candidate was reconstructed from the Stage-A packet. Paper 13C was used
only as the source of independently surviving rational calculations and as a
catalogue of semantic failures. No implementation source or generated result
was used.

## 2. Construction inventory

The candidate freezes one packet with all required components:

| component | exact construction |
|---|---|
| boundary species | arbitrary finite occurrence sets; atomic $B_0,B_1^0,B_1^r,B_2^0,B_2^r,B_3^r,B_3^e$ and formal tensor boundaries |
| presentation groupoid | occurrence bijections semidirect local $X/Y$ swaps; exact action on fields, bonds, controls, marks, and readers |
| control category | three typed objects, two override monoids, every hom-set, every composition, and explicit write-free stage transitions |
| execution category | free typed symmetric-monoidal syntax on process, tensor, fusion, stable-future, and eraser generators |
| histories | exact recursively glued physical boundary traces with no seed, program, reader, cache, or serialization metadata |
| global law | one finite seed family plus one structural recursion for identities, generators, composition, and tensor |
| marks | exact intervention addresses on typed string-diagram vertices and exact target probe-address constructors |
| quotient | reader-free diagonal experiment--history orbit pushforward with exact stabilizer multiplicity |
| readers | derived equivariant maps; complete identity; anchored oriented readers; invariant orbit-profile readers otherwise |
| response | exact contrast object with intersection stabilizer and complete/common comparison fiber |
| independent tensor | internal domain object with product law, product reader, symmetry, and empty unit |
| fusion | simultaneous n-ary physical generator with fresh cross-pair seeds and no fold order |
| future | record-carrying $B_1^r\to B_2^r$, typed entry into a new post-endpoint boundary, a closed endomorphism monoid, and transported record labels |
| eraser | executable many-to-one $B_2^r\to B_3^e$ arrow outside the stable subcategory |
| divisions | future sufficiency plus all-field direct/cut equality; native carrier fixed before testing |
| size | finite-set natural product law, geometric cardinality mixture, uniform deletion, and order-independent extension |

## 3. Closure of the Paper 13C semantic failures

### 3.1 Illegal future toggle

Paper 13C imposed $t=h$ at its endpoint and then toggled only $t$ as an
endomorphism. Paper 13D carries both $t$ and $h$ in $B_2$, makes their equality
a local type invariant, and introduces a distinct $B_3^r$ with a new field
$t^+$. The stable entry copies $t$ into $t^+$; later $F_t$ acts only on
$t^+$. The stable subcategory begins with the exact record-carrying kernel
$R_c:B_1^r\to B_2^r$. Every generator is total on its declared domain.

### 3.2 Informal eraser

Paper 13D defines $E_r:B_2^r\to B_3^e$ as an executable deterministic arrow.
It forgets $r$, maps an explicit positive-mass cross-sector pair to one target,
and is excluded from the stable subcategory. Past-trace retention is separated
from future-boundary readability.

### 3.3 Stage-order contradiction

Source and mediator writes now inhabit endomorphism monoids at different
objects. The write-free arrows $\mathsf S\to\mathsf M\to\mathsf C$ make a
legal program's order explicit. A mediator write followed by a source write
meets an empty hom-set. Refusal is therefore categorical rather than a word
normalizer exception.

### 3.4 Reader-dependent physical quotient

The physical stabilizer contains the reader-free execution, source context,
intervention marks, and any physical apparatus landmarks, but no reader
function. Readers act only after the orbit fiber is fixed. A target coordinate
may be read individually only when a physical landmark anchors it; otherwise
the exact reader returns an invariant orbit-profile. Changing only a reader
cannot alter history cells or masses.

### 3.5 External tensor and ambiguous fusion

Tensor boundary objects and tensor arrows are internal to the execution
category, with exact product semantics and an empty unit. Physical fusion is
a different simultaneous n-ary generator. It removes component partition at
the target and generates fresh cross bonds. A staged physical fusion is a
different trace rather than an auxiliary serialization of one fusion.

### 3.6 Vague probe marks

Intervention addresses are exact finite sets attached to physical generator
vertices. Probe addresses enumerate every legal target field and bond.
Oriented landmarks and readers use only these constructors. Strings, loop
indices, and unregistered fields are refused.

### 3.7 Response quotient

Each intervention law has its reader-independent physical quotient. A signed
comparison is then a separately typed ordered contrast whose stabilizer is
the intersection of the two experiment stabilizers. This remains exact when
alternative values change accidental automorphisms. The complete contrast
fiber is retained beside every diagnostic reader.

## 4. Exact inherited numerical reconstruction

The paper re-derives

$$
B=\frac1{25}\begin{pmatrix}9&16\\16&9\end{pmatrix},
\quad
C=\frac1{625}\begin{pmatrix}49&576\\576&49\end{pmatrix},
\quad
B^2=\frac1{625}\begin{pmatrix}337&288\\288&337\end{pmatrix}.
$$

The unique continuation through the declared native two-state process carrier
is

$$
CB^{-1}=\frac1{175}
\begin{pmatrix}351&-176\\-176&351\end{pmatrix},
$$

so the nonempty tensor candidate has a negative entry at every size. This is
only native-carrier, source-independent nondivision. Enlarging the carrier by
$q_0$, using an identity cut, or using the empty process remains an explicit
nonkill.

The queried paths independently evaluate to positive $B$ continuations and
$B^2$ endpoints. Complete $B_1^0$ and $B_1^r$ frontiers pass full-field future
sufficiency and direct/cut equality. The record-only restriction fails
sufficiency, preserving the record/division product square.

## 5. Pre-freeze gate table

| gate | construction evidence | status |
|---|---|---|
| reverse-stage refusal | empty $\operatorname{Hom}(\mathsf M,\mathsf S)$ | PASS |
| category closure | exact hom-sets, override, free typed execution syntax | PASS |
| reader independence | reader absent from $\mathcal G_{\widehat e}$ | PASS |
| oriented probes | exact landmarks and orbit-profile fallback | PASS |
| tensor/fusion distinction | different source/target types and kernels | PASS |
| generator totality | complete generator clauses on every legal value | PASS |
| arbitrary-depth future | generated monoid $W(I)$ and projector induction | PASS |
| executable eraser | $E_r$ typed in Exec and excluded from Fut | PASS |
| enlarged-carrier nonkill | $(m,q_0)$ named before testing | PASS |
| no metadata response | metadata absent from histories/readers | PASS |
| orbit multiplicity | stabilizer pushforward, representative-mass warning | PASS |
| deletion naturality | restriction of occurrence and pair factors | PASS |
| no hidden clock | finite-set and n-ary constructors have no order input | PASS |
| reader cancellation | parity zero plus complete-reader nonzero | PASS |
| anti-refactorization | equality required on full executable function | PASS |
| result neutrality | no parameter/dimension/geometry selection | PASS |
| actuality firewall | possible histories only | PASS |

## 6. Provisional product

The construction claims only the paper's `GREEN-UNREVIEWED` vector:

```text
P13D-POINT-FREE-EXECUTABLE-GAMMA-CONSTRUCTED
P13D-ONE-TYPED-EXECUTABLE-GAMMA-CONSTRUCTED
P13D-TYPED-EXPERIMENT-CATEGORY-CONSTRUCTED
P13D-NATIVE-B1-CUT-NONDIVISIBLE
P13D-TYPED-STABLE-FUTURE-CATEGORY-CONSTRUCTED
P13D-EXECUTABLE-ERASER-CONTROL-CONSTRUCTED
P13D-COMPLETE-DIVISION-FRONTIERS-CONSTRUCTED
P13D-VARYING-SIZE-COVARIANT-FAMILY-CONSTRUCTED
P13D-RECIPROCAL-RELATIONAL-RESPONSE-CONSTRUCTED
P13D-ACTUALIZATION-UNCONSTRUCTED
```

No rung is terminal before independent review and adjudication.

## 7. Permanent scope walls

The construction contains no derived actuality, chronology, causal order,
signal cone, dimension, signature, topology, volume, duration, scale, clock,
metric, connection, curvature, stress tensor, energy, entropy, gravity,
general relativity, continuum limit, quantum field theory, particles, or
phenomenology. Operational control stages are not time. Cardinality is not
volume. Relational response is not causal influence or backreaction.

No Paper 14–17 result, target dimension, or geometry was used to select the
law. A later geometric failure may not change the frozen packet.

## 8. Implementation and review state

No Rust, Python, fixture, generated case, official result, receipt, or bundle
exists. The mathematical bytes must next be bound by a separate hostile-review
protocol. Three independent semantic reports and a terminal adjudication are
required. Only terminal acceptance may create a Rust pin. Code-only repair
authority does not exist yet and can never change these mathematical bytes.
