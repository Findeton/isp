# Paper 13C physics pin — one executable point-free Gamma

Date: 2026-08-20

Status: **PIN / NEW MATHEMATICAL LAW UNCONSTRUCTED / RUST PROHIBITED**

## 1. Authority and parent

The user explicitly authorizes Paper 13C after the terminal Paper 13B
adjudication at commit `738498e`, path
`v16/note-paper13b-pointfree-gamma-math-adjudication.md`, SHA-256
`95e97c139efcc0aa7591192d7aa54791208d5e17a6bbc315eb8203a337a25a37`.

Paper 13C is a new mathematical law. It does not edit, repair, wrap, or
silently reinterpret Paper 13B. Paper 13B's static orbit law and rejected
claims remain immutable historical evidence.

The question is:

> Can one exact, presentation-covariant, globally evaluated interventional
> law map every admitted complete experiment to a probability measure on
> complete physical histories, with the observational distribution as its
> identity experiment and a genuinely native candidate boundary whose
> unqueried transition is indivisible through that boundary?

The default result is unknown. No implementation is authorized before the
new mathematics is frozen and semantically accepted.

## 2. Primitive replacement

Paper 13B promoted a static measure

$$
\Gamma_*\in\operatorname{Prob}(\mathsf{Hist}).
$$

That object does not determine `do` semantics. Paper 13C instead seeks one
primitive executable law

$$
\mathbf\Gamma:
\mathsf{Exp}\longrightarrow
\operatorname{Prob}(\mathsf{CompleteHist}),
$$

with

$$
\Gamma_*=\mathbf\Gamma(\mathrm{id}).
$$

The domain, action, and evaluation rule are part of the physical law. A
chain-rule refactorization that changes interventions changes
$\mathbf\Gamma$ and is therefore a different law even if it preserves
$\Gamma_*$.

This introduces an operational manipulation interface. It does not insert a
spacetime chronology, dimension, topology, metric, clock, or signal cone.
Response direction and any later chronology remain derived questions.

## 3. Binding inheritance and noninheritance

Paper 13C may reuse only independently surviving Paper 13B mathematics:

- the exact rational seed $R$ and stochastic matrices $B,C,B^2$;
- the finite typed atom grammar as a candidate alphabet;
- the $X\leftrightarrow Y$ presentation action;
- endpoint-generated bond covariance;
- orbit pushforward and occurrence multiplicity;
- the all-size cardinality and deletion mathematics;
- the finite licensed record-transport grammar;
- the positive recorded/division controls; and
- the exact cut-relative negativity of $CB^{-1}$.

It does not inherit:

- Paper 13B's universal experiment packet;
- truncated-factor `do` semantics;
- reciprocal response as a physical result;
- a native unrecorded division boundary;
- the full four-cell record/division square;
- chronology or causal direction; or
- any Rust/Python implementation.

Every inherited item must be reconstructed in the new law rather than read
from a prior evaluator.

## 4. Mandatory mathematical packet

The new paper must determine one immutable packet

$$
\mathfrak G_C=(\mathsf{Ty},\mathsf{Bnd},\mathsf{Hist},\mathcal G,
\mathsf{Exp},\circ,\otimes,\mathbf\Gamma,
\mathsf{Read},\mathsf{Fut},\mathsf{Div}).
$$

It must define completely:

1. every boundary type and legal field on that boundary;
2. every complete physical history and its presentation action;
3. every admitted experiment object, marked slot, value, exterior context,
   and reader;
4. composition, tensoring, identities, refusals, and type errors in
   $\mathsf{Exp}$;
5. one uniform global evaluation rule for every experiment;
6. normalization and presentation covariance of every
   $\mathbf\Gamma(e)$;
7. observational, regional, and reader distributions as derived shadows;
8. the native candidate boundary chain;
9. stable-record and complete-division predicates; and
10. the varying-size rule.

No phrase such as “complete reader,” “relevant context,” “declared slot,” or
“same-law intervention” may remain without an exact finite definition or an
all-size constructor.

## 5. Exact experiment category

### 5.1 Objects

An experiment object must include:

$$
e=(H,Z,A,a,R,E),
$$

but each coordinate is now typed by an explicit object set:

- $H$: a source physical-history presentation;
- $Z$: a complete boundary argument of a named boundary type;
- $A$: one admitted marked mechanism slot at that boundary;
- $a$: a legal replacement from that slot's alternative set;
- $R$: one reader from the complete reader catalogue for the target boundary;
- $E$: every nonreplaced exterior field required by the evaluator.

The paper must enumerate the finite generator catalogue and give one uniform
constructor for all-size atom/bond occurrences. Unlisted slots are refused,
not silently interpreted.

### 5.2 Mechanism actions

Each admitted intervention acts on a named mechanism object, not on an
arbitrary chain-rule factorization. The law must state:

- which mechanism is replaced;
- which dependent fields are recomputed;
- which exterior fields are held fixed;
- which types appear or disappear;
- which readers remain well-typed; and
- how sequential interventions compose.

A mechanism-changing operation such as switching between unqueried and
record-query behavior must be an explicit experiment generator. A generic
`do(w=R)` with undefined retyping is forbidden.

### 5.3 Covariance

The presentation groupoid transports the whole experiment and resulting
measure:

$$
\mathbf\Gamma(g\cdot e)=g_*\mathbf\Gamma(e).
$$

At a symmetric history, `do(X=a)` and `do(Y=a)` are transported marked
experiments, not naked functions on the history orbit. The experiment
category retains marked-slot stabilizers and orbit multiplicity.

### 5.4 Composition

Composition is permitted only when target and source boundary types match.
The paper must prove identity and associativity, and distinguish:

- composing experiment programs;
- conditioning on an observed result;
- tensoring independent experiments; and
- factorizing a stochastic law through a division frontier.

These are not interchangeable operations.

## 6. Global evaluation, not DAG truncation

One exact evaluator

$$
\mathcal E(e,\xi)\longmapsto H_e
$$

may be used, with a fixed presentation-invariant seed law for $\xi$, provided
that $\mathcal E$ and the seed law are completely frozen mathematical data.
Then

$$
\mathbf\Gamma(e)=\mathcal E(e,\cdot)_*P_\xi.
$$

Equivalent formulations are allowed, but every $\mathbf\Gamma(e)$ must come
from one global rule. Independently fitted kernels, per-experiment tables, and
post-hoc factorization choices are forbidden.

The evaluator may have operational dependency. It must not call that
dependency spacetime chronology. Paper 13C must expose exactly what is
primitive so Paper 17 can later test which response relation, if any, closes
to a locally finite order.

## 7. Native candidate boundary and nondivision

The law must contain three typed interfaces

$$
B_0\longrightarrow B_1\longrightarrow B_2.
$$

$B_1$ is a native candidate boundary/interface in $\mathsf{Exp}$ but need not
be a realized classical checkpoint in the identity experiment.

The law must supply:

$$
\mathbf\Gamma(Q_1):
P(b_1\mid b_0)=B,
$$

for the native boundary query $Q_1$, and

$$
\mathbf\Gamma(\mathrm{id}):
P(b_2\mid b_0)=C
$$

for the unqueried whole transition.

The division question is explicitly counterfactual and cut-relative:

> Does a positive normalized kernel $K(b_2\mid b_1)$ exist such that
> $C=KB$ while preserving the declared boundary types and exterior context?

The unique candidate remains

$$
K=CB^{-1}=
\frac1{175}
\begin{pmatrix}
351&-176\\
-176&351
\end{pmatrix}.
$$

If the boundary/query typing is genuinely part of $\mathbf\Gamma$, its
negative entries earn only:

```text
P13C-NATIVE-B1-CUT-NONDIVISIBLE
```

They do not imply absolute nonfactorizability through every carrier. Identity
factorizations $C=CI=IC$ remain explicit nonkills because they use different
candidate cuts.

## 8. Query changes and stable records

The native query $Q_1$ may change the endpoint law by writing a record. The
paper must distinguish:

- the unqueried identity experiment, whose endpoint law is $C$;
- a nonrecording boundary probe, if admitted;
- a recording query with exact joint branch law and endpoint marginal
  $B^2$; and
- a true eraser/reset experiment.

The recording query may produce a complete division frontier. This does not
turn the unqueried $B_1$ interface into a realized Markov checkpoint.

Record stability remains grammar-relative and uses transported projectors.
A reversible sector permutation is a nonkill; a many-to-one reset is an
eraser.

## 9. Response and chronology firewall

Only response tensors computed from the accepted
$\mathbf\Gamma(e)$ may be promoted. Observational conditioning, a selected
factorization, and a restricted-reader zero are not interventions.

For each admitted marked slot, the paper must retain the complete signed
tensor over exterior context, alternative pair, complete reader, and every
outcome. It must include exact controls for:

- common cause;
- direct versus mediated response;
- cancellation at an incomplete reader;
- context reversal;
- symmetric co-onset;
- a spectator zero;
- matter-to-relation response; and
- relation-to-matter response.

Paper 13C may earn an interventional interface and reciprocal response. It may
not promote their transitive closure as chronology. That is a later result.

## 10. Point-free and varying-size requirements

Every experiment and output history is quotiented only after its complete
marks, endpoints, fields, readers, and context are transported. Physical mass
is an orbit pushforward, never representative mass.

The law must generate unbounded finite sizes from one rule and prove:

- normalization at every size and globally;
- exchangeability/covariance;
- exact orbit multiplicity under automorphisms;
- consistency under a declared restriction/deletion map;
- order independence of incomparable extensions; and
- no fixed dormant carrier, hidden loop index, or global clock.

## 11. One-law/no-selection firewall

All constants, mechanism generators, type sets, experiment alternatives,
reader catalogues, seed laws, cardinality distributions, and evaluation rules
are frozen before any output is inspected.

No parameter, dimension, topology, metric, geometry, or desired response
profile may select the law. If one fixed law cannot be stated, the result is
`P13C-LAW-UNSELECTED` and the unit stops.

## 12. Four-gate table

| object | referent | necessity | no-smuggling | discriminator |
|---|---|---|---|---|
| executable $\mathbf\Gamma$ | complete typed experiment-to-measure map | static laws do not identify `do` | no chosen chain-rule factorization | same observational law/different mechanism maps are different laws |
| experiment category | exact slot/context/reader objects and typed composition | Paper 13B packet was schematic | no undefined mode retyping | every listed generator evaluates uniquely; every unlisted one refuses |
| native $B_1$ cut | typed candidate boundary query in $\mathsf{Exp}$ | external $B$ did not prove native nondivision | no history ID or realized checkpoint inserted | $B$ query is native while no positive $K$ completes unqueried $C$ |
| reciprocal response | complete signed tensors from $\mathbf\Gamma$ | observational correlations are insufficient | no DAG truncation or incomplete reader | both directions survive common-cause, mediation, and cancellation controls |

## 13. Pre-registered product outcomes

```text
referent:
  P13C-EXECUTABLE-GAMMA-REFERENT-UNCONSTRUCTED
  P13C-POINT-FREE-EXECUTABLE-GAMMA-CONSTRUCTED

law:
  P13C-LAW-UNSELECTED
  P13C-EXPERIMENT-LAW-UNNORMALIZED
  P13C-ONE-INTERVENTIONALLY-COMPLETE-GAMMA-CONSTRUCTED

experiment:
  P13C-EXPERIMENT-CATEGORY-UNDEFINED
  P13C-TYPED-EXPERIMENT-CATEGORY-CONSTRUCTED

nondivision:
  P13C-NATIVE-CUT-UNPROVEN
  P13C-NATIVE-B1-CUT-NONDIVISIBLE

record:
  P13C-STABLE-RECORD-UNCONSTRUCTED
  P13C-GRAMMAR-STABLE-RECORD-CONSTRUCTED

division:
  P13C-DIVISION-UNCONSTRUCTED
  P13C-COMPLETE-DIVISION-FRONTIERS-CONSTRUCTED

size:
  P13C-FIXED-SIZE-ONLY
  P13C-VARYING-SIZE-COVARIANT-FAMILY-CONSTRUCTED

response:
  P13C-RECIPROCAL-RESPONSE-UNCONSTRUCTED
  P13C-RECIPROCAL-RELATIONAL-RESPONSE-CONSTRUCTED

actuality:
  P13C-ACTUALIZATION-UNCONSTRUCTED
  P13C-ACTUALIZATION-POSTULATED
  P13C-ACTUALIZATION-DERIVED
```

No outcome implies chronology, dimension, signature, topology, volume,
duration, metric, curvature, gravity, GR, continuum physics, or QFT.

## 14. Semantic kill conditions

The new law is rejected or demoted if:

1. two mechanism maps with identical observational law are treated as the
   same $\mathbf\Gamma$;
2. `do` depends on a freely chosen chain-rule factorization;
3. any admitted slot, replacement, context, reader, or composition rule is
   undefined;
4. `do(w=R)`-style dependent retyping has multiple readings;
5. an unlisted intervention is silently accepted;
6. the experiment action fails presentation covariance;
7. representative mass replaces orbit pushforward;
8. the $B_1$ boundary is only a comparison matrix and not a native typed
   interface;
9. absolute indivisibility is inferred from the $B$-relative obstruction;
10. performing the query is confused with the unqueried identity process;
11. a record is equated with a complete division;
12. a reversible sector swap is called erasure;
13. a hidden history, clock, phase, or cache is added after a negative cut;
14. per-experiment or per-size tables replace one global evaluator;
15. a response uses observational conditioning or an incomplete reader;
16. a parameter or mechanism is selected from desired outputs;
17. a dimension, geometry, or metric enters law construction; or
18. possible histories are called actual without a selection rule.

## 15. Mathematical review and Rust gate

The mathematical law is frozen before review. Three independent lenses must
attack:

1. experiment category/groupoid and native boundary typing;
2. probability, global evaluation, query/division, and nondivision; and
3. physics/ontology, intervention identity, response, and no-geometry scope.

Only terminal mathematical `ACCEPT` authorizes a separate Rust pin. Rust will
implement the unchanged accepted experiment category and evaluator.

Later ownership, mutability, iterator, serialization, CLI, transaction,
memory, or performance defects may receive bounded conformance repairs only
if no mathematical object changes. A defect exposing ambiguity in the law is
semantic and returns to user authority. There is no automatic repair chain.

## 16. Exact stage paths

### Stage A

- `v16/note-paper13c-interventional-gamma-physics-pin.md`
- `v16/LOG.md`
- `STATUS.md`

### Stage B

- `v16/paper-13c-executable-pointfree-gamma.md`
- `v16/note-paper13c-interventional-gamma-mathematical-construction.md`
- `v16/LOG.md`
- `STATUS.md`

### Stage C

- `v16/note-paper13c-interventional-gamma-math-review-protocol.md`
- `v16/LOG.md`

### Stage D

- `v16/review-paper13c-interventional-gamma-category.md`
- `v16/review-paper13c-interventional-gamma-probability.md`
- `v16/review-paper13c-interventional-gamma-physics.md`
- `v16/LOG.md`

Each review freezes in its own commit.

### Stage E

- `v16/note-paper13c-interventional-gamma-math-adjudication.md`
- `v16/LOG.md`
- `STATUS.md`

No Rust path is authorized before Stage E acceptance.

## 17. Stage-A stopping rule

Commit this pin, ledger entry, and status update only. Then construct the new
mathematics. Do not create Rust, Python, generated fixtures, or official
results. The first unresolved object is the complete executable law itself.
