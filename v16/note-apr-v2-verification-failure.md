# APR Paper 12 — v2 black-box verification failure

**Date:** 2026-08-19  
**Status:** V2 ARTIFACTS REPRODUCIBLE BUT NOT CERTIFIABLE  
**Disposition:** REPAIR REQUIRED; CANDIDATE/PANEL REMAIN PROHIBITED

## 1. Frozen v2 evidence

The scorer frozen at v16 #167 produced:

- `v16/apr_output_v2.txt`, SHA-256
  `32371e37fabeeecef1de942f6084f97f4f5ff5383838792e18909e981a31a79f`;
- `v16/apr_receipt_v2.json`, SHA-256
  `812cdff8dba7fbb11b1e25afd49ac7e1337ad15caf4ae6396a81bfdd9788aec5`;
- canonical receipt payload SHA-256
  `39c9bb883d91dc61c7480cdc850f6310095b8e04c061089d8c8301c3a8012984`.

Repository-root, alien-working-directory, and true off-tree/no-`.git` runs are
byte-identical. The payload self-hash recomputes and the transcript matches the
receipt. These bytes are preserved and never overwritten, but they are not a
certified APR result.

## 2. Robust scientific result

Independent reconstruction again returns:

```text
APR-BLOCKED-AT-BOUNDARY-GLUING
```

Normalization and raw prefix-syntax atomlessness are present. The next rung
fails independently because the frozen construction has no total adaptive-
frontier factory, identities at every boundary, tensor process, filling-to-
process assignment, or regional-overlap selector. The two registered `AB/BC`
extensions have identical local pair marginals `1/4` and different
`P(A=C)=1/2` versus `1`. No defect below supplies a selector or moves this
primary.

The repaired receipt also correctly separates finite question instruments
from graph-cospan controls, reports `NO-GLOBAL-JOINT-ROOT`, keeps E-37 at a
candidate-regional blind-interface theorem with no `Tau`, and refuses physical
atomlessness, dynamic locality, contact, causality, geometry, GR/QFT, and
actualization.

## 3. Mandatory failures

### 3.1 M16 projection inconsistency

M16 deletes the shared `c` occurrence from `rf_000` without substituting the
already allocated `d` occurrence. The represented overlap moves, but the
frozen blind projection/interface becomes internally inconsistent. The repair
contract requires inconsistent projection assignments to refuse. An exact
valid control substitutes `c -> d`: the represented overlap count moves
`2 -> 1`, projection consistency remains true, and the blind hash stays fixed.

### 3.2 M15 did not instantiate a syntax-copy oracle

The row named `syntax_copy` uses the correct symbolic `PrefixRegion`
constructor. It accepts fresh regions and is refinement/relabel invariant, so
it is a positive canonical representation rather than the registered raw-
syntax oracle trap. The finite-depth whitelist/P5 linkage is valid; the
syntax-copy and identifier-hash oracle arms remain owed.

### 3.3 L2 and L4 lack changed upstream objects

L2 and L4 alter hidden scorer options only. Their source and transformed
fixture hashes are identical, and their descriptors contain no serialized
changed object that an independent verifier can reconstruct. They therefore
cannot count as semantic mutations under the repair contracts.

### 3.4 Continuation recovery is not a typed closed subgrammar

Every `qc` catalogue is labeled `typed_subgrammar_only:true`, yet the scorer
infers preservation/destruction from catalogue membership and operation labels
while counting unconstructed tensor, mixed, replacement, reader, and effect
operations. In particular `qc_002` is called reset-destroyed although the same
receipt correctly reports no typed same-law reset-to-reader word. Recovery
must quantify only over explicitly composable, assigned continuations; absent
composition stays unconstructed.

### 3.5 Port semantics stop too early

The semantic-port metamorphic validates question formulas but ignores
`next_record_word` and later port-to-bit maps used by cospans, recovery, and
readers. It therefore cannot refuse a contradictory record binding required
by the scorer protocol. All downstream semantic bindings must be transported
and audited together.

### 3.6 Ontology role reports the proposal, not the construction

`POSTULATED-CANDIDATE-RELATIONAL-WEB` is the a priori ontic proposal, not the
protocol's diagnostic of the process role actually built. The measured role
at v2 is at most `STATIC-RESPONSE`; even catalogue-relative record recovery is
not yet established over a typed continuation grammar. The proposal belongs
in a separate ontology-candidate field.

### 3.7 Identity census is assignment-ill-typed

The all-boundary identity census scans raw horizontal fillings rather than the
frozen tree-to-filling/process assignments. It reports `hf_015` and `hf_016`
as `bd_008` identities, although only `qw_000 -> hf_015` is registered and
`hf_016` is a replacement/swap filling. Conversely, structural identity-like
rows with absent rather than empty relation fields are mishandled. The final
all-boundary Boolean remains false, but its witness list is wrong.

## 4. Generic source defects

1. The positive process branch emits unregistered
   `HORIZONTAL-PROCESS-CONSTRUCTED` when two interface dictionaries merely
   contain required keys. Even all-`None` stubs promote. The binding vocabulary
   permits only `SYNTAX-ONLY`, `STATIC-RESPONSE-ONLY`, `HORIZONTAL-CLASSICAL`,
   `HORIZONTAL-QUANTUM`, or `INCONSISTENT`, and positive promotion requires
   semantic assignment/composition validation.
2. The generic highest-rung classifier ignores `generated_contact`. A
   synthetic census with every other capability true can return
   `APR-JOINT-POINT-FREE-REGIONAL-LAW-CONSTRUCTED` while contact is false.
3. The locality scope string `regional support only` should read `finite
   regional-support controls only; the regional-support coordinate is
   unearned`. Likewise the static-atomless qualifier must explicitly mean raw
   prefix syntax, never a physical regional referent.

## 5. Independent verification record

The independent verifier was implemented outside the repository at:

```text
/private/tmp/apr_v2_independent.py
SHA-256 5f1d5b1df3ef12b86791662ffcb92842666c4e1bd83739b657534732c1db92c6
```

It imports fixture data but never imports scorer measurement functions. It
reimplements exact fractions, Boolean antichains, tree semantics, rational
linear algebra, graph pushouts/isomorphism, colored blind projection,
record typing, dependency roots, descriptor replay, and classifier precedence.
It passed 197 checks and failed seven mandatory checks listed above.

Independently green items include all artifact/input hashes; byte-identical
replays; transactional refusal; full-cone normalization; every registered tree
partition and preparation weight; all three actual graph pushouts; stable-null
ranks; predictive quotient and future extension; `AB/BC` underdetermination;
baseline E-37 consistency and relabel invariance; scoped-root metamorphics; all
56 rows and every genuine exact-change descriptor; G2; M13/M14/M24/M33/P3/M35;
M15/P5 whitelist linkage; and L5's finite analytical deletion.

## 6. Exposure debt and next rule

The prefreeze M01/all-mutants run remains a permanent blindness debt for the
v2 scorer. Independent reconstruction can validate its arithmetic and mitigate
tuning risk, but cannot restore a pre-truth status or a complete missing
exposure transcript. Any successor scorer inherits this disclosure.

The next authorized event is a separately pinned minimal delta repair of only
the ten issues in §§3–4. It may not change fixtures, law data, outcome words,
primary precedence, or any green baseline. It must freeze before any fixture
evaluation and publish to new v3 paths. V2 artifacts remain immutable and
cannot support candidate prose or the three-seat panel.

