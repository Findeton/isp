# Paper 13C mathematical hostile-review protocol

Date: 2026-08-20

Status: **FROZEN PROTOCOL / SEMANTIC REVIEW ONLY / NO IMPLEMENTATION**

## 1. Immutable corpus

| artifact | commit | SHA-256 |
|---|---|---|
| physics pin, `v16/note-paper13c-interventional-gamma-physics-pin.md` | `9acac0c` | `6f6acdfdc3065e7376eabb118adcf9c9b09cc89770f3616d31927366e7ab4c4f` |
| mathematical paper, `v16/paper-13c-executable-pointfree-gamma.md` | `562f623` | `51c16c5bf85bb7dd0db44e3477233a6f331a0efe01f306600379165fcf932bed` |
| construction note, `v16/note-paper13c-interventional-gamma-mathematical-construction.md` | `562f623` | `c93e0ca95ba3bf75601a7dd1d258167f545fb60c733cdff6ad1d7e1b5ffc2a7f` |

Reviewers authenticate these bytes before scientific inspection. Paper 13B
may be read only as the terminal counterexample that motivates the pin; none
of its evaluators, experimental packets, or claimed interventions may be
imported into Paper 13C.

## 2. Question and stopping rule

> Does the frozen packet define one normalized, presentation-covariant
> executable law on a complete typed experiment interface, with physical
> diagonal experiment--history outcomes, a genuinely native nonempty $B_1$
> cut, exact record/division controls, and response derived only from that
> law—without factorization-defined intervention, source-history smuggling,
> hidden labels, parameter selection, geometry, or actuality?

Only a mathematical or physical-semantic counterexample may reject a
coordinate. Ownership, mutability, serialization, CLI, cache, transaction,
memory, performance, or prospective Rust architecture are out of scope.

No reviewer repairs the law. If a probability, boundary field, experiment
generator, reader constructor, group action, evaluator rule, future grammar,
or parameter must change, report the semantic defect and stop that dependent
coordinate. There is no automatic successor law.

## 3. Three blind seats

The reports are:

1. `v16/review-paper13c-interventional-gamma-category.md` — experiment
   category, groupoid, referent, and native boundary typing;
2. `v16/review-paper13c-interventional-gamma-probability.md` — global
   probability law, composition, query/division, and nondivision; and
3. `v16/review-paper13c-interventional-gamma-physics.md` — intervention
   identity, response, record ontology, and no-geometry scope.

Each seat is blind to its siblings, reconstructs the mathematics independently,
records its first decisive semantic counterexample if any, completes every
still-defined assigned duty, adds at least four new mathematical countermodels,
and edits only its one report path. Each report freezes in a separate commit.

## 4. Common reconstruction

Every seat independently derives:

1. $B$, $C$, $B^2$, $B^{-1}$, and $CB^{-1}$ from the threshold rules;
2. the exact field set at every boundary and every path's history type;
3. all process and mechanism normal forms, identities, composites, and
   refusals;
4. every source-conditioned seed law and normalization;
5. direct evaluation of $U,Q^0,Q^r,D,R_c$ and both composites;
6. direct-composite equality for the queried paths without using a selected
   factorization of the observational law;
7. $X/Y$ covariance of source, mechanism, relational packet, bonds, marks,
   readers, and output;
8. one diagonal experiment--history orbit with nontrivial stabilizer and one
   with trivial stabilizer;
9. fixed-size and grand normalization, deletion projectivity, and two-order
   extension agreement;
10. all four stable-record/complete-division cells;
11. the full signed-response constructor and the displayed response controls;
12. every provisional product coordinate and all permanent walls.

Private scratch arithmetic is permitted, but it may check rather than define
the law. No repository evaluator, fixture, Rust source, or generated result is
authorized.

## 5. Mandatory attacks

### H1 — static-law refactorization

Construct two mechanism maps with the same observational law but different
`set(X)` response. They must be different executable laws, not equivalent
representations of this one.

### H2 — program metadata in history

Insert the experiment name or mechanism program into the physical history.
Show that it creates a spurious perfect response and is forbidden.

### H3 — history-only quotient

Quotient outcomes by the full presentation group while leaving the marked
experiment outside the orbit. Test whether the intervention location is
erased. The frozen diagonal quotient is the comparator.

### H4 — representative mass

Use one representative mass instead of stabilizer-orbit mass on a nontrivial
orbit. Check normalization and covariance.

### H5 — fixed point and automorphism

Use one $X/Y$ fixed history and one repeated-occurrence history with graph
automorphisms. Verify exact orbit sizes and no double counting.

### H6 — naked symmetric slot

At a swap-symmetric frame, compare naked `X` with the transported complete
contrast `(set(X,a),set(X,b),reader-cell)`. Only the latter may descend.

### H7 — incomplete packet transport

Transport the marked slot but not the context, reader, bond endpoint, or
source frame. The malformed packet must not be a physical experiment orbit.

### H8 — reader-selected physics

Change only a reader partition. The physical history law must remain the same
while its diagnostic pushforward may change.

### H9 — unlisted slot and dependent retyping

Attempt `set(W,a)`, a stage-reversed program, and a generic mode write that
changes boundary fields. All must refuse rather than choose an interpretation.

### H10 — composition versus conditioning

Compare execution-word composition, positive-cell postselection, and
composition of two already normalized conditionals. Only the first two named
operations exist, and they are unequal in general.

### H11 — last-write associativity

Use three writes with repeated and disjoint slots. Check both parenthesizations
and every boundary-stage type constraint.

### H12 — tensor versus fusion

Compare the independent external tensor with one fused size-$(n+m)$ trial.
The latter has fresh cross-bonds and must not be assigned the product law.

### H13 — graph enumeration and hidden order

Permute occurrences, unordered-pair enumeration, and extension order. Any
index-dependent probability is a failure.

### H14 — dormant carrier

Replace the varying-size union by a fixed carrier with active bits. It is a
different sample space and may not prove projectivity.

### H15 — per-size replacement

Change one threshold or bond rule only at an unchecked size. This must change
the law and defeat the uniform constructor.

### H16 — deletion after quotient

Delete an occurrence, every traversed field, and incident bonds before and
after quotient. Check equality on the unmarked observational family.

### H17 — native-boundary absence

Verify that $1_n^0,1_n^r$, $Q^0,Q^r,D,R_c$ are actual typed objects and
arrows with standalone evaluations, not matrices outside the law.

### H18 — input-history smuggling

Enlarge $B_1$ to carry $q_0$, a seed, or a past-history identifier. The
resulting positive restart is an enlarged-carrier nonkill, not a repair of the
declared cut.

### H19 — cut-relative nondivision

Solve $C=KB$ and the nonempty tensor-power problems exactly. Also retain
$C=CI=IC$, the empty process, and explicit carrier enlargement as nonkills.

### H20 — exterior averaging

Condition on every common exterior context and verify that the $B/C$ cut
comparison is not created by averaging away $h,c,e^0$.

### H21 — query/unqueried confusion

The queried endpoint is $B^2$ and the unqueried endpoint is $C$. Performing a
query must not be described as revealing a pre-existing $B_1$ value in $U$.

### H22 — marginal-only division

Check direct/cut equality on complete target histories, relational packets,
records, and bonds—not only the displayed two-state marginal.

### H23 — record implies division

Hold the record fixed and vary omitted $h$ or relational data. The restricted
frontier must fail future sufficiency.

### H24 — division implies record

Verify the complete $1_n^0$ queried frontier is a positive division although
it contains no record register.

### H25 — reversible swap versus erasure

Transport projectors through $F_r$, then apply the many-to-one reset. The
first remains stable and the second does not.

### H26 — observational conditioning as intervention

Derive $P(Y=1\mid X=1)=337/625$ and
$P(Y=1\mid\operatorname{set}(X,1))=1/2$. A selected chain rule may not define
the latter.

### H27 — incomplete-reader cancellation

The parity reader must miss the $E\to(X',Y')$ response while the discrete
reader detects it. No coarse-reader zero may be promoted.

### H28 — mediation and directness

Reconstruct total $X\to Z_Y$ response and the residual after holding $E'$
fixed. Total response must not be relabelled direct response.

### H29 — context reversal and scalar averaging

Retain both signs of the $u_X$ response at $c=0,1$. Averaging to zero is not a
spectator result.

### H30 — bond response sign

Toggle one endpoint color in equal- and unequal-color contexts. The signed
change reverses and its magnitude remains $7/25$.

### H31 — joint-stage overpromotion

The symmetric joint relational stage is not by itself a co-onset bundle,
chronology edge, spacetime event, or backreaction theorem.

### H32 — selection, geometry, and actuality

Search for any output-selected parameter, dimension, lattice, topology,
metric, clock, signal cone, actual history, or future permission to retune the
law. Each is forbidden.

## 6. Seat-specific emphasis

### Category/groupoid seat

Own H2--H17. Prove the exact experiment-program category, source and target
typing of every generator, diagonal quotient, stabilizer formula, and
deletion/extension naturality. Add countermodels involving a marked-slot
stabilizer, a nontrivial graph automorphism, an invariant reader partition,
and an intermediate source boundary.

### Probability seat

Own H18--H30. Reconstruct every complete finite kernel, all reader outcomes
for at least four response tensors, full direct/cut equality, future
sufficiency, and uniqueness/negativity of each restart candidate. Add an
incomplete exterior, a normalized but erasing reset, a postselection posed as
an intervention, and a positive enlarged-state restart.

### Physics/ontology seat

Own H1 and H21--H32. Decide whether the primitive executable interface really
solves Paper 13B's intervention nonidentifiability, whether record and division
remain independent, and whether response is only relational. Add
countermodels with the same observational law/different mechanisms, same
record/different future profile, same count/different graph, and same graph
with different law.

## 7. Verdict and adjudication

Each report returns `ACCEPT`, `ACCEPT-WITH-FIXES`, or `REJECT`, states its
first decisive semantic counterexample, and prints the full product vector.
`ACCEPT-WITH-FIXES` permits only proof or prose repairs that move no
mathematical object or probability.

Adjudication independently reproduces every decisive finding against the
unchanged corpus. It may narrow prose, but it may not change the law. Only a
terminal mathematical `ACCEPT` authorizes a separate Rust pin. Until then no
implementation path exists.
