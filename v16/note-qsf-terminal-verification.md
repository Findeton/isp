# QSF Paper 9 terminal verification

**Date:** 2026-08-18

**Candidate commit:** `781f292c501aba5ae3d58d2b0d771bdefb35182c`

**Terminal primary:** `QSF-METHOD-INCONCLUSIVE`

## Result

QSF is terminal at its registered finite scope. A fresh post-commit invocation
passes all `20/20` scientific gates and reproduces the committed transcript,
receipt, and paper byte-for-byte. A second invocation from a git-free archive,
with the scorer launched from an alien working directory, produces the same
three hashes:

| artifact | SHA-256 |
|---|---|
| `v16/code/qsf_output.txt` | `a110d5d6c21a3d5cc27109216bb299c08c5b77ee60c547ce7e63da53c9a27ed9` |
| `v16/code/qsf_receipt.json` | `70c4b63712aba053177ded4d2fabfe865371d16e42e2e6865f9cace4961a2d45` |
| `v16/paper-09-quantum-seam.md` | `f11fe9e124ed70181dc86422b6db528265599dc4dc9f5d56c31f295654677b71` |

The final `28/28` registered mutants refuse and leave no result artifact. This
includes the repaired `paper-claim` seal that caused terminal attempt 1 to
refuse. The self-test succeeds; unknown arguments, pre-existing output targets,
and a deleted frozen anchor refuse; the deleted-anchor refusal writes nothing.
The archive replay contains no `.git` directory. The untracked v15 SCOUT-T
files are outside the archive source set and remain untouched.

## Scientific terminal statement

The panel-adjudicated result does not choose a quantum base law.

- Record-indexed A0 is exactly false. State/history-indexed A0 is a lookup,
  not an affine local law. Both raw and aligned branch dictionaries retain 30
  conflicts, while the exact terminal-phase control proves branch failure does
  not settle aggregate A1/A2 feasibility. That aggregate problem is unentered.
- The natural no-interaction fixed-factor pure-ray extension has exact
  remote-setting dependence in Bob's calibrated count-record law. This rejects
  that extension; no relational spacetime composite or Lorentz-signalling
  theorem was built.
- No retained `rho`-sufficient state-record boundary exists in ticks one
  through five. The displayed fine `([psi],record)` kernel is normalized at
  those cuts, but it is not a standard quantum instrument and is not selected
  as a microscopic one-step law.
- The published predictive quotient is suffix-only. Final-record quotients are
  `3,3,27,27,486,486`; an exact pair equal at horizon two separates at horizon
  three; intervention-complete JS-S1a remains unentered.

The binding ontology remains the #127 correction:

```text
one actual relational/configuration history       ontological candidate
indivisible multi-time process law                 nomological candidate
decoherence functional / Hilbert construction      quantum representation
ordinary stochastic kernel / CP instrument         division-level shadow
```

This hierarchy is a safe typing, not a QSF selection result. In particular,
QSF does not prove that an ordinary positive kernel may be assigned after every
unrecorded elementary rewrite. Paper 10 must classify the competing law types
and the assumptions that select or exclude them; Paper 11 may not predeclare a
microscopic kernel.

## Scope

Terminal means that the registered Paper 9 question, artifacts, panel, bounded
repair, mutant surface, and integrity checks are complete. It does not close
aggregate affine feasibility, complete predictive sufficiency, relational
composition, stable division selection, dynamic geometry, carrier growth,
actualization, continuum/QFT/GR recovery, or empirical calibration.
