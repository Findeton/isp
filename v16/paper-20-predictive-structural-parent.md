# Predictive structural descent from one common relational parent

## What the accepted Gamma already selects, and what it still does not

### Abstract

Paper 19 proved that the accepted Paper 13D law assigns probabilities to
histories on execution syntax fixed before stochastic evaluation. It therefore
does not contain a hidden probability law over whole process complexes. This
paper asks the next constructive question: can one common parent experiment
nevertheless produce genuine relational structures as its outcomes without
assuming a lattice, a discrete web, or an arbitrary selector decoder?

We first define the correct parent type. Its target is a dependent disjoint
union of child sectors, not a product containing every alternative as dormant
machinery. We then define the structure of a realized outcome by predictive
equivalence: two complete endpoints represent the same structure exactly when
all aligned lawful future interventions and readers have the same probability
law. The predictive quotient is the coarsest future-sufficient quotient. This
fixes structural identity from the operational content of the law rather than
from a hand-chosen graph invariant or result bit.

The construction gives a new positive specialization. At every fixed size,
the accepted grand observational Paper 13D experiment already induces a
normalized point-free distribution over predictive target configurations. In
the accepted finite setting the complete reader makes the predictive quotient
equal to the full endpoint-orbit quotient. The family is natural under
deletion. Its endpoint bond marginal is an exact two-color exchangeable model
with conditional probabilities `9/25` and `16/25` and unconditional edge
probability `1/2`.

This is a target-configuration law, not a Paper 18 target-sector law and not a
whole-process selector. Every history still
traverses the same primitive execution class. The derived family supplies no
occurrence propensity for tensor versus fusion, no multi-happening
chronology, no structural interference, and no dimension. A binary use of the
accepted matrix `R`, a connectivity decoder, a Hilbert direct sum, stationary
rates, or decoherence can represent a parent but cannot independently select
its probabilities.

The result therefore opens a restricted generated-target investigation while
leaving the original global Paper 17 chronology and dimension gate closed.
It also identifies the smallest remaining positive construction: one
dependent-output structural instrument with two or more inequivalent
whole-process fibers, an independently physical probability law, and exact
conditional recovery of Paper 13D.

## 1. Status and binding scope

This is a mathematical and physical construction governed by the frozen pin

`v16/note-paper20-predictive-structural-parent-pin.md`

with ordinary SHA-256

`06f80fc38bb9d54944cac4ab0f75509bee4f4ebcce48d7098fe490b0205432a8`.

It binds:

- the terminally accepted Paper 13D mathematical law;
- the terminal Paper 18 version-2 through version-6 mathematical composite;
- the internally accepted negative/partial Paper 19 result; and
- the no-post-hoc-selection walls inherited from those papers.

The present paper changes none of those objects. It constructs a new
predictive quotient and evaluates it on one already accepted parent law.

The paper performs no implementation, sampling, numerical fit, parameter
search, Paper 17 dimension test, or actualization rule.

## 2. Five probability questions that must remain separate

For a complete input `x`, the expression "probability of a structure" can
refer to at least five different objects.

1. **Target-configuration law.** Which complete target configuration results
   from one fixed process?
2. **Resolved-channel law.** Which structurally distinct path occurs within
   one fixed physical opportunity?
3. **Activity law.** Does the structural operation occur?
4. **Whole-process law.** Which inequivalent execution complex occurs?
5. **Root law.** Which initial or cosmological structural state is realized?

Paper 13D constructs the first kind for each admitted fixed process. Paper 18
types the second and exposes the missing measure. Paper 19 proves that the
third through fifth are not secretly contained in the accepted conditional
history laws.

This paper will derive a point-free version of item 1. It will not rename that
result as items 2 through 5.

## 3. The dependent-output parent

### 3.1 Structural inputs and child fibers

Let `X` be a point-free measurable family of complete parent inputs. For
every `x` let

\[
 \Lambda_x
\]

be a raw target-type fiber frozen by physical typing before a probability
law is tested. Membership in different raw fibers does not by itself assert
that two outcomes are physically different structures. Final physical
structural identity will be the predictive quotient of complete behavior.
The raw fiber need not be discrete or globally numbered.

For each `lambda` in `Lambda_x`, let

\[
 T_{\lambda,x}
\]

be the complete child target type and let

\[
 \mathsf H_{\lambda,x}
\]

be its complete physical-history fiber.

Define the total target and history bundles by dependent disjoint union:

\[
 \mathsf T_x
 =\bigsqcup_{\lambda\in\Lambda_x}T_{\lambda,x},
 \qquad
 \mathsf Y_x
 =\bigsqcup_{\lambda\in\Lambda_x}\mathsf H_{\lambda,x}.
\tag{1}
\]

Every complete parent history has a typed endpoint projection

\[
 \pi_x:\mathsf Y_x\longrightarrow\mathsf T_x,
\]

and the restriction of `pi_x` to each history fiber lands in its matching
child target type. Structural identity below is defined on these complete
endpoints, not on execution traces. The traces remain in the parent law so
that two different generation histories ending at the same child are not
silently counted as two structures.

For a continuous or hybrid branch, equation (1) denotes the corresponding
measurable field. Nothing here introduces counting measure.

### 3.2 One parent, one realized fiber

A structural parent is one typed experiment

\[
 \Sigma_x:S_x\longrightarrow\mathsf T_x
\tag{2}
\]

with one normalized indivisible law

\[
 \widehat\Gamma_x(dH),
 \qquad H\in\mathsf Y_x.
\tag{3}
\]

Each realized history `H`, and therefore its endpoint `pi_x(H)`, belongs to
one and only one matching fiber. Equation (1) lists possible values of one
random object. It is not the Cartesian product

\[
 \prod_{\lambda\in\Lambda_x}T_{\lambda,x},
\]

which would retain a simultaneous field for every unchosen alternative.

The distinction is the same as the distinction between a random variable
taking one value in a configuration space and a machine containing one live
copy of every possible configuration.

### Proposition 1 — dependent sums do not imply dormant worlds

If the physical state space is the disjoint union (1), then each state has a
unique sector projection. Merely including other sectors in the law's
configuration space introduces possibilities, not additional realized
subsystems.

#### Proof

By definition of disjoint union, an element is one tagged pair
`(lambda,h)`. It has no coordinate in any fiber with a different tag. A
product state would instead contain one coordinate for every tag. The two
types are not isomorphic when more than one nontrivial fiber exists.
\(\square\)

## 4. Complete future behavior

### 4.1 Frozen physical tests

For a complete endpoint `t`, let `Test(t)` be the family of all pairs

\[
 (F,R)
\]

where:

- `F` is a lawful typed continuation from the endpoint type of `t`;
- every intervention in `F` is an admitted physical address;
- `R` is an admitted equivariant reader on the resulting complete history;
  and
- the complete reader remains present alongside every coarse reader.

Program names, syntax serialization, random seeds, caches, and unavailable
interventions do not belong to `Test(t)`.

The complete future behavior of `t` is the family

\[
 \mathsf B_t(F,R)
 =R_*\mathbf\Gamma_D(F\mid t).
\tag{4}
\]

Paper 13D explicitly defines a later-boundary source as one exact complete
value and reconstructs no fictional past. Therefore equation (4) is already
typed by the accepted evaluator whenever `F` exists.

### 4.2 Aligned comparison

Let `t` and `t'` be two complete endpoints. They are **predictively
equivalent**, written

\[
 t\sim_{\rm pred}t',
\tag{5}
\]

exactly when there is an admitted point-free type transport `alpha` aligning
their endpoint types, interventions, future arrows, readers, and reader
outcomes such that

\[
 \mathsf B_t(F,R)
 =
 \mathsf B_{t'}(\alpha F,\alpha R)
\tag{6}
\]

for every complete aligned test.

If no admitted type transport exists, the endpoints are not identified.
Mere equality of a selected coarse reader is insufficient.

### Theorem 1 — predictive equivalence is an equivalence relation

Equation (5) is reflexive, symmetric, and transitive.

#### Proof

The identity type transport proves reflexivity. Inverting the admitted type
transport and equation (6) proves symmetry. Composition of two admitted type
transports aligns the corresponding future tests, and equality of probability
laws is transitive. \(\square\)

### 4.3 The predictive structural outcome

Define

\[
 \epsilon(t)=[t]_{\rm pred},
 \qquad
 \mathcal P_x=\mathsf T_x/{\sim_{\rm pred}}.
\tag{7}
\]

Identify `P_x` with the image of the complete behavior map and equip it with
the sigma algebra generated by all descended frozen future-behavior
coordinates. Equivalently, this is the trace on that image of the product
sigma algebra of the aligned reader-law spaces. This operational sigma
algebra is not silently replaced by the largest abstract quotient sigma
algebra.

If the behavior coordinates are not measurable, or if a requested
conditioning needs regular conditional probabilities whose hypotheses are
not proved, that corresponding law remains unconstructed. No standard-Borel
property is inferred merely from notation.

## 5. Minimality and covariance

### Definition 1 — future-sufficient structural statistic

A measurable map

\[
 s:\mathsf T_x\longrightarrow Z
\]

is future sufficient when

\[
 s(t)=s(t')
 \quad\Longrightarrow\quad
 t\sim_{\rm pred}t'.
\tag{8}
\]

Thus a sufficient statistic may retain extra physical distinctions but may
not merge endpoints with different complete future laws.

### Theorem 2 — the predictive quotient is the coarsest future-sufficient
quotient

The map `epsilon` is future sufficient. For every future-sufficient map `s`,
there is a unique map on the image of `s`,

\[
 \bar\epsilon:s(\mathsf T_x)\longrightarrow\mathcal P_x,
\]

such that

\[
 \epsilon=\bar\epsilon\circ s.
\tag{9}
\]

#### Proof

The fibers of `epsilon` are predictive-equivalence classes, so `epsilon` is
future sufficient by definition. If `s(t)=s(t')`, equation (8) implies
`epsilon(t)=epsilon(t')`. Hence assigning `bar epsilon(s(t))=epsilon(t)` is
well defined. Surjectivity onto the image gives uniqueness. \(\square\)

The theorem prevents a connectivity convention, graph threshold, result bit,
or serialization from being called the minimal physical structure unless it
agrees with the complete predictive quotient.

### Theorem 3 — point-free covariance

Assume the parent law, future grammar, interventions, and readers are
covariant under the accepted presentation groupoid. Then presentation
transport sends each predictive class to the same physical class and
transports its entire behavior family naturally.

#### Proof

Paper 13D covariance transports the complete experiment, history,
intervention mark, reader, and output together without changing orbit mass.
Applying that equality to every pair `(F,R)` preserves equation (6). The
inverse transport gives the converse. \(\square\)

## 6. The endogenous predictive law

### Theorem 4 — predictive pushforward

If the quotient (7) is measurable, then

\[
 \mu_x^{\rm pred}
 =(\epsilon\circ\pi_x)_*\widehat\Gamma_x
\tag{10}
\]

is a normalized point-free probability law on predictive structural outcomes.

#### Proof

A measurable pushforward of a normalized probability kernel is a normalized
probability kernel. The endpoint projection prevents generation-trace
multiplicity from being mistaken for structural multiplicity. Theorem 3
makes the result presentation independent. \(\square\)

The theorem derives the marginal from an independently fixed parent. It does
not select the parent law.

### Theorem 5 — parent representation is still nonexplanatory by itself

For every normalized probability law `nu` on any measurable structural
outcome space, there is a formal parent satisfying equation (10): take the
parent history and target spaces to be that outcome space, take `pi_x` and
`epsilon` to be the identity, and set the parent law to `nu`.

Therefore existence of some dependent-output parent does not derive its
probabilities.

#### Proof

The identity pushforward of `nu` is `nu`. \(\square\)

The physical work lies in fixing `Sigma_x`, its complete law, and its future
tests independently of the desired structural distribution.

## 7. Exact child recovery

### 7.1 Generation versus future evolution

Let `y` be a realized endpoint of a parent. There are two different laws:

1. the probability that the parent generates `y`; and
2. the law of a new lawful future beginning from the exact value `y`.

The second is the accepted Paper 13D later-boundary kernel

\[
 \mathbf\Gamma_D(F\mid y).
\tag{11}
\]

Conditioning on a predictive class generally does not reconstruct the
unconditioned generation kernel of some pre-existing execution arrow. It
instead supplies the mixture of exact future laws supported inside that
class.

### Proposition 2 — exact endpoint recovery

For a parent whose realized child is the complete endpoint value `y`, every
lawful future from that child is exactly the accepted Paper 13D law (11).

#### Proof

This is the later-boundary source clause of Paper 13D structural recursion.
The source argument is one exact boundary value, and no past or alternative
parent is reconstructed. \(\square\)

This proposition is sufficient for a physical target-configuration child. It
is not
a proof of a resolved path law over different ways of generating that child.

### Corollary 1 — predictive-class future recovery

On every branch admitting a regular conditional law, conditioning the parent
on a supported predictive class gives the unique common future behavior of
that class.

#### Proof

Choose any representative endpoint of the class and transport every aligned
test and output to that representative. Every endpoint in the class then has
the same law for every complete future test by definition. A conditional
mixture of identical probability laws is that same law. Automorphism choices
do not change the physical output law because readers are equivariant.
\(\square\)

## 8. The accepted grand observational parent

### 8.1 Fixed-size parent

For every finite occurrence set `I`, choose the already accepted experiment

\[
 \Sigma_I
 =U_\varnothing:B_0(I)\longrightarrow B_2^0(I)
\tag{12}
\]

with tautological source context, no interventions, no landmarks, and the
complete physical-history quotient.

This choice is not selected by a Paper 17 result. It is exactly the grand
observational primitive already singled out by the accepted all-size law.

Let

\[
 \Omega_I
 =\mathsf{Hist}(U_\varnothing)/\mathcal G_{U_\varnothing}
\]

be the accepted physical history-cell space, and let

\[
 \pi_I^{\rm orb}:\Omega_I
 \longrightarrow B_2^0(I)/\mathcal G_{U_\varnothing},
 \qquad
 [H]\longmapsto[\operatorname{end}(H)]
\]

be the endpoint-orbit projection. It is well defined because the experiment
stabilizer acts on the complete trace and its endpoint together. Let `q_I`
denote the corresponding presentation-orbit map on presented endpoints.

### Theorem 6 — the current Gamma has a predictive target law

For every finite `I`,

\[
 \mu_I^{\rm target}
 =
 (\bar\epsilon_I\circ\pi_I^{\rm orb})_*
 \mathbf\Gamma_D([U_\varnothing]\mid I)
\tag{13}
\]

is a normalized finite point-free predictive target law.

Here `epsilon_I` is the predictive map on presented endpoints. By Theorem 3
it is constant on presentation orbits and therefore factors uniquely as

\[
 \epsilon_I=\bar\epsilon_I\circ q_I.
\]

With the accepted complete immediate reader included among lawful tests, two
endpoints are predictively equivalent exactly when their complete endpoint
values belong to the same presentation orbit. Thus in this specialization
the predictive quotient equals the full point-free endpoint-orbit quotient.

Equivalently, before physical quotienting, equation (13) is

\[
 \mu_I^{\rm target}
 =
 (\epsilon_I\circ\pi_{\rm end})_*
 \widetilde{\mathbf\Gamma}(U_\varnothing\mid I),
\]

where `pi_end` retains the labelled endpoint of a presented history. This
second formula is included only to show that no representative mass is used;
the defining physical formula is equation (13).

#### Proof

At fixed finite `I`, the source and private seed spaces are finite. The
accepted evaluator gives a normalized kernel and the presentation quotient
pushes full orbit mass. The endpoint assignment is equivariant, so it descends
to `pi_I^orb`. Covariance gives the forward implication: endpoints in one
orbit have aligned future laws. Conversely, use the identity future and the
complete endpoint reader. Distinct endpoint orbits yield distinct complete
outputs, so they cannot satisfy equation (6). Theorem 4 now proves equation
(13). \(\square\)

### Corollary 2 — predictive target-configuration plurality is nontrivial

For `|I|>=2`, equation (13) assigns positive probability to endpoints that
are identical in a chosen complete positive-support packet except for one
bond value `ell_ij=0` versus `ell_ij=1`. The complete reader distinguishes
them. Hence the predictive target law has at least two positive physical
classes.

#### Proof

Conditional on any fixed endpoint colors, each new bond is one with
probability either `9/25` or `16/25` and zero with the complementary positive
probability. Holding every other coordinate fixed, the two endpoints have
different total one-bond counts, so no presentation automorphism identifies
their orbits. \(\square\)

This is predictive target-configuration plurality. It is not Paper 18
target-sector branching and it is not a second execution arrow.

## 9. Exact endpoint-bond ensemble

### 9.1 Latent endpoint colors

In the tautological source context,

\[
 d_i=e'_i=\epsilon_i\mathbin\oplus x_i\mathbin\oplus y_i.
\]

The bit `epsilon_i=e_i^0` is independent and fair. Therefore every `d_i` is
fair, regardless of the biased private `eta` bits, and the family `{d_i}` is
iid across occurrences.

Conditional on the complete color field,

\[
 \Pr(\ell_{ij}=1\mid d_i=d_j)=\frac9{25},
 \qquad
 \Pr(\ell_{ij}=1\mid d_i\ne d_j)=\frac{16}{25},
\tag{14}
\]

and distinct bond seeds are independent.

### Proposition 3 — exact marginal density

For every unordered pair,

\[
 \Pr(\ell_{ij}=1)=\frac12.
\tag{15}
\]

Consequently, at size `n`,

\[
 \mathbb E|L|=\frac{n(n-1)}4,
 \qquad
 \mathbb E\deg(i)=\frac{n-1}{2}.
\tag{16}
\]

#### Proof

Two iid fair colors agree and disagree with probability one half each.
Averaging equation (14) gives `(9+16)/50=1/2`. Linearity of expectation gives
equation (16). \(\square\)

### Proposition 4 — the bond family is not merely an asserted iid graph

The conditional law is the exact two-color exchangeable model (14). For a
triangle,

\[
 \Pr(\ell_{12}=\ell_{23}=\ell_{13}=1)
 =\frac{7641}{62500},
\tag{17}
\]

which differs from the iid fair-edge value `1/8`.

#### Proof

All three colors agree with probability `1/4`, contributing `(9/25)^3`.
Exactly one color differs with probability `3/4`, contributing
`(9/25)(16/25)^2`. Their weighted sum is equation (17). \(\square\)

No result interprets `L` as spatial adjacency. If one provisionally used
bond incidence as a graph control, equation (16) would describe a dense,
not locally finite, large-size regime. That is a hostile control, not an
emergent-space claim.

## 10. All-size target law and deletion

The accepted grand observational law gives

\[
 \Pr(|I|=n)=2^{-(n+1)},\qquad n\ge0.
\tag{18}
\]

Combining equation (18) with equation (13) gives a normalized countable
mixture over finite predictive target spaces.

### Theorem 7 — endpoint-orbit projectivity

For `n>=1`, uniform deletion of one occurrence from the full point-free
endpoint law at size `n` gives the size-`n-1` endpoint law.

#### Proof

This is the endpoint pushforward of accepted Paper 13D Theorem 7. Deletion
removes the occurrence, its fields, and incident bonds; every retained
occurrence seed and pair seed keeps its original law. Orbit pushforward
commutes with presentation transport. \(\square\)

A further coarse predictive quotient inherits a deletion kernel only if
deletion is a congruence for that quotient. In the present complete-reader
specialization the quotient is the endpoint-orbit quotient, so Theorem 7 is
already the required statement.

Conditioning equation (18) on size `n` is legitimate for comparing the
accepted family across sizes. It is not a derivation of cosmological volume,
duration, or a new size law.

## 11. The fixed-syntax obstruction survives

Let `Proc` map a complete history to its unmarked execution class. Paper 19
proved that for every accepted fixed experiment

\[
 \operatorname{Proc}_*\mathbf\Gamma_D
 =\delta_{[f]}.
\tag{19}
\]

### Theorem 8 — predictive target-configuration plurality is not
whole-process branching

For the parent (12), the target law (13) may have many positive predictive
classes while the process-class law remains the Dirac measure

\[
 \operatorname{Proc}_*
 \mathbf\Gamma_D([U_\varnothing]\mid I)
 =\delta_{[U_\varnothing]}.
\tag{20}
\]

#### Proof

The endpoint fields and bonds vary with source values and private seeds, but
the recursive history type retains the same primitive arrow in every
history. Applying different measurable maps to one history law can therefore
produce a nontrivial endpoint marginal and a constant process marginal.
\(\square\)

Theorem 8 is the central scope boundary of this paper.

## 12. Why the simplest apparent parents fail

### 12.1 The accepted-R structural wrapper

One may append a new binary structural mode, apply

\[
 R=
 \begin{pmatrix}3/5&-4/5\\4/5&3/5\end{pmatrix},
\]

and label its two outputs `tensor` and `fusion`. This gives probabilities
`9/25` and `16/25` without introducing a new numerical constant.

It is not a derivation. Swapping the two labels changes the physical
tensor/fusion odds while leaving every inherited Paper 13D probability
unchanged. No accepted symmetry transports tensor separation into physical
fusion. For more than two channels, a binary tree also makes the answer
depend on leaf bracketing unless new coherence data are supplied.

### Proposition 5 — inherited numbers do not make a new decoder native

Using an accepted numerical matrix on a newly introduced structural degree
of freedom is an extension of the law. It is uniquely physical only if the
new degree, its preparation, the channel correspondence, and all coherence
maps are independently operational.

#### Proof

The inherited law contains no observable that distinguishes the two possible
channel labelings of the new bit. The two wrappers agree on the inherited
domain and disagree on structural odds. Hence the inherited domain does not
select between them. \(\square\)

### 12.2 Connectivity descent

For a fixed fusion target one can form connected components of the realized
one-bond support and call them child structures. This is an invariant map,
but it is not the predictive quotient unless every lawful future probability
depends exactly on that connectivity partition.

Moreover conditioning on connectedness changes the accepted unconditioned
fusion bond law. It cannot be reported as exact recovery of the original
fusion-generation kernel.

Connectivity remains a useful coarse reader or hostile control. It is not a
canonical selector merely because it is familiar graph theory.

### 12.3 Stationary structural dynamics

For any strictly positive desired finite law `pi`, one can design reversible
rates satisfying detailed balance with respect to `pi`. Therefore uniqueness
of a stationary law is explanatory only when the structural move set and all
rates are fixed independently of `pi`.

Paper 13D contains physical fusion but no accepted split transition between
whole execution classes. The eraser removes record readability; it is not an
inverse structural creation operator.

## 13. Quantum parent representations

### 13.1 Direct-integral representation

A quantum representation of the derived predictive structural sectors may
use

\[
 \mathcal H_x
 =\int_{\mathcal P_x}^{\oplus}
   \mathcal H_{\kappa,x}\,m_x(d\kappa)
\tag{21}
\]

and sector projectors `P_E`. For a state `rho_x`, the Born law is

\[
 \mu_x(E)=\operatorname{tr}(\rho_xP_E).
\tag{22}
\]

This types classical mixtures, coherent superpositions, and hybrid sector
families. It does not determine `m_x`, `rho_x`, or the dynamics that prepares
`rho_x`.

### Theorem 9 — amplitude representation does not select the selector

Every finite positive law `mu(kappa)` has a pure-state representation

\[
 |\psi_\mu\rangle
 =\sum_\kappa\sqrt{\mu(\kappa)}
   e^{i\theta_\kappa}|\kappa\rangle.
\tag{23}
\]

Hence the existence of complex amplitudes and the Born rule does not select
`mu`.

#### Proof

The sector projector expectation in the state (23) is `mu(kappa)` for every
choice of phases. \(\square\)

### Theorem 10 — structural phases require recombination

Suppose every lawful structural effect is block diagonal in the decomposition
(21). Let

\[
 U_\theta=\int_{\mathcal P_x}^{\oplus}
 e^{i\theta(\kappa)}I_\kappa\,m_x(d\kappa).
\]

Then replacing any state `rho` by `U_theta rho U_theta^dagger` leaves every
lawful probability invariant.

#### Proof

For a block-diagonal effect `E=direct-sum E_kappa`, `U_theta` commutes with
`E`. Cyclicity of the trace gives
`tr(U_theta rho U_theta^dagger E)=tr(rho E)`. \(\square\)

Therefore a claim of structural interference requires a typed physical
recombiner or another admitted effect with off-diagonal sensitivity between
structural sectors. Paper 13D contains no such operation between inequivalent
execution classes.

Quantum combs and process matrices provide useful higher-order typing, but a
comb, process matrix, control state, or boundary state remains supplied
physical data. Decoherence can make structural alternatives classical; it
does not choose their prior amplitudes.

## 14. Symmetry and operational algebra

### Theorem 11 — unique invariant measure is conditional on a physical
channel action

Let a compact physical symmetry group act transitively and measurably on a
compact channel fiber. If that action has a unique invariant probability
measure and the parent preparation is invariant, then the channel law is the
unique invariant measure.

This theorem cannot be applied when the group acts only on presentation
labels or when no physical symmetry maps one channel into another.

#### Proof

Invariance places the parent marginal in the set of invariant probability
measures. Uniqueness makes that set a singleton. The qualification follows
because presentation gauge leaves physical channels fixed and therefore
places no equality condition on their relative masses. \(\square\)

The accepted occurrence permutations and local port swaps act within a fixed
Paper 13D process. They do not act transitively on tensor, staged, fused, and
primitive whole-process complexes. In particular, retained fusion count is
an invariant under the accepted equations.

### 14.1 Branching operational algebra

A stronger future construction could begin from the complete algebra of
Paper 13D interventions, transformations, and effects. If independently
derived superselection sectors have a genuine multichannel composition law,
then a finite based ring, measurable hypergroup, or direct-integral analogue
may supply nontrivial channel multiplicities and positive dimensions.

Associativity and an independently physical invariant state could then fix
conditional resolved-channel odds. They still need not fix whether the
composition opportunity occurs or which cosmological root state is prepared.

The current simultaneous fusion has one resolved unmarked channel. Its fresh
bonds are internal history outcomes, so its structural fusion algebra remains
probability trivial.

## 15. Barandes interpretation

The stochastic ontology suggests the following reading.

1. A predictive structural class is an effective configuration: it retains
   exactly the physical distinctions that change lawful future probabilities.
2. To make whole structure dynamical, enlarge the configuration space so that
   structural type is a genuine configuration variable.
3. Define one indivisible stochastic transition law on that enlarged space.
4. Keep a root or initial probability law distinct unless the dynamics itself
   selects one.

Hilbert-space direct sums, complex potentials, and dilations are secondary
representations. A dilation sector that does not change the underlying
stochastic probabilities is not a new physical structure.

The predictive quotient is therefore close to the Barandes ontology, while
the arbitrary structural-bit wrapper is not.

## 16. What current quantum and gravity programs teach

### 16.1 Higher-order quantum processes

Quantum networks and process matrices show how transformations of processes
and indefinite causal correlations can be typed without treating a circuit
serialization as fundamental. They do not choose the higher-order process or
its input state.

### 16.2 Fusion from information constraints

Information-convex and entanglement-consistency results show that fusion
sectors and multiplicities can sometimes be derived rather than postulated.
Their known derivations assume strong properties of gapped systems and
spatial regions. Paper 20 may reuse the strategy of deriving sectors from
operational consistency, but may not import two-dimensional geometry or an
area law before geometry exists.

### 16.3 Quantum measure theory

A decoherence functional can assign nonclassical weights to structural
history events. It requires a physical event algebra, strong positivity or
another declared condition, extension to the intended history sigma algebra,
and a boundary state. It is not supplied by the phrase "sum over
structures."

### 16.4 Group field theory and sums over complexes

Group field theory demonstrates a serious variable-structure Fock-space
architecture. Its group, field rank, action, couplings, combinatorial class,
and boundary state are inputs. Those ingredients are useful design examples,
not a selector derivation for the present theory.

## 17. Paper 17 consequence

### 17.1 Restricted target investigation

Theorems 6 and 7 open one restricted, result-neutral investigation:

> Classify the predictive target configurations produced by the accepted
> grand observational `U_empty` law at each size, and test which relational
> statistics are invariant under presentation and deletion.

This investigation may use full endpoint fields, complete future profiles,
and exact conditional response. It may not interpret bond incidence as
spatial adjacency or cardinality as volume without additional theorems.

### 17.2 Why original Paper 17 remains closed

The original Paper 17 asks whether the same law generates a locally finite
operational chronology and a stable dimension across varying whole histories.
The current parent has one primitive execution vertex per occurrence family
and no probability law over alternative process complexes. It supplies no
multi-happening global ensemble from which that chronology can be inferred.

The bond control has expected degree `(n-1)/2` and no accepted direction or
geometric interpretation. Neither its density nor its two-color latent field
is a spacetime result.

### Theorem 12 — exact reopening boundary

The present construction opens predictive target-law analysis but does not
open global Paper 17 chronology or dimension. The original gate opens only
after at least one of the following is independently accepted:

1. a dependent-output parent with positive mass on inequivalent whole-process
   fibers;
2. a finite residual parent family calibrated before Paper 17 outputs are
   inspected; or
3. a proof that the desired Paper 17 conclusion is identical over the entire
   frozen parent-law residue.

#### Proof

Theorem 8 leaves the current process marginal Dirac. Paper 19 supplies
distinct admissible whole-process laws whose ordering statistics vary. Thus
the target marginal cannot determine the global process ensemble. The three
listed alternatives respectively fix the missing law, fix its finite
parameters independently, or remove its relevance to the downstream result.
\(\square\)

## 18. Evaluation of the pinned candidate routes

| route | mathematical status | physical verdict |
|---|---|---|
| predictive `U_empty` pushforward | constructed | genuine target law only |
| fixed-fusion predictive pushforward | constructible by the same theorem | target law, not fusion activity |
| accepted-`R` structural bit | normalized representation | arbitrary wrapper |
| connectivity/support decoder | invariant coarse map | not canonical without future sufficiency |
| unique invariant symmetry law | valid conditional theorem | no current cross-channel symmetry |
| branching fusion/hypergroup | promising architecture | no current branching algebra |
| higher-order quantum instrument | valid type architecture | process/state remains unselected |
| quantum structural measure | possible future law | no event functional or recombiner |
| stationary structural dynamics | possible future law | rates and reverse moves absent |
| contingent cosmological state | logically consistent | state postulate, not Gamma derivation |

## 19. Hostile controls

The frozen forty controls have the following outcomes.

1. Identity-parent representation is rejected by Theorem 5.
2. A renamed selector bit fails Proposition 5.
3. Swapping structural labels changes the wrapper law.
4. Binary decision trees require independent coherence.
5. Arbitrary direct-sum amplitudes are exposed by Theorem 9.
6. Phases without a recombiner are killed by Theorem 10.
7. A probability-neutral dilation is not promoted to physical structure.
8. Product-valued dormant machinery fails Proposition 1.
9. A dependent sum realizing one fiber passes the type gate.
10. The complete reader makes the current predictive quotient maximally
    physically fine up to presentation.
11. A coarse-reader equality cannot define predictive identity.
12. Every predictive result is explicitly relative to the frozen future
    grammar; a stronger grammar may refine it.
13. Presentation changes transport complete classes without changing mass.
14. Full orbit pushforward, not representative mass, is used.
15. The future-test sigma algebra is frozen before the pushforward.
16. Post-hoc coarse graining cannot repair a failed parent.
17. Connectivity and motif maps remain coarse controls.
18. A zero bond is a physical zero value; deleting its coordinate requires a
    separately proved sufficient quotient.
19. Connectivity conditioning is not called the original generation law.
20. Bond outcomes are never promoted to execution branches.
21. A merely mathematical transitive group does not trigger Theorem 11.
22. Uniform presentation labels do not select physical class odds.
23. Reverse-engineered detailed-balance rates are rejected.
24. Gibbs and maximum-entropy bases remain explicit inputs.
25. Path-integral action, measure, and boundary state remain inputs.
26. Decoherence is not prior-weight selection.
27. Born or Gleason form does not choose a state.
28. Quantum dimensions are not used without physical branching.
29. Fusion and erasure are not misreported as a reversible structural pair.
30. Size conditioning is distinguished from the root size law.
31. The target-configuration law is not called either a Paper 18 target-sector
    law or a resolved-channel law.
32. No resolved-channel statement is called an activity law.
33. No activity statement is called a root state.
34. One actual configuration would not establish typicality.
35. No dimension output selects a parent parameter.
36. Finite bond controls do not impose a discrete ontology.
37. The general parent support remains non-discrete capable.
38. Deletion is proved on the complete endpoint-orbit quotient; no generic
    quotient congruence is assumed.
39. Rooted local odds are not inferred from unrooted automorphism factors.
40. The whole-process residue still changes global Paper 17 statistics, so
    the original gate stays closed.

## 20. Product result

The exact result is:

```text
P20-PARENT-DEPENDENT-TARGET-TYPE-CONSTRUCTED
P20-PREDICTIVE-QUOTIENT-CONSTRUCTED
P20-PREDICTIVE-QUOTIENT-MEASURABILITY-CONSTRUCTED-FOR-ACCEPTED-FINITE-FAMILY
P20-GENERAL-NONSTANDARD-PREDICTIVE-MEASURABILITY-UNPROVEN
P20-CURRENT-GAMMA-PREDICTIVE-TARGET-LAW-CONSTRUCTED
P20-CURRENT-GAMMA-TARGET-CONFIGURATION-PLURALITY-CONSTRUCTED
P20-CURRENT-GAMMA-RESOLVED-STRUCTURAL-BRANCHING-UNCONSTRUCTED
P20-CURRENT-GAMMA-WHOLE-PROCESS-PARENT-UNCONSTRUCTED
P20-STRUCTURAL-INTERFERENCE-UNCONSTRUCTED
P20-WHOLE-PROCESS-PARENT-PROBABILITIES-UNCONSTRUCTED
P20-P17-RESTRICTED-TARGET-TEST-OPEN
P20-P17-ORIGINAL-GLOBAL-GATE-CLOSED
P20-ACTUALIZATION-UNCONSTRUCTED
P20-METRIC-UNCONSTRUCTED
P20-GRAVITY-UNCONSTRUCTED
```

## 21. Smallest genuine positive successor

The next positive construction should not begin with a selector formula. It
should construct one structural instrument

\[
 \Sigma_x:S_x\longrightarrow
 \bigsqcup_{\lambda\in\Lambda_x}T_{\lambda,x}
\]

such that:

1. at least two supported fibers have inequivalent whole-process behavior;
2. the outcome descriptor is the predictive quotient or a proved refinement;
3. the complete probability law is independently fixed;
4. conditioning recovers every accepted Paper 13D child future;
5. unchosen sectors are absent from the realized state;
6. rooted spectators and presentation changes preserve local odds;
7. composition and deletion are coherent;
8. any structural phase has a physical recombiner;
9. activity and root laws are reported separately; and
10. Paper 17 outputs play no role in selecting the construction.

A particularly promising route is to derive an operational effect and
transformation algebra from complete Paper 13D experiments, then ask whether
its independently defined sector composition genuinely branches. If it does,
an invariant state or measurable fusion law may become physical. If it does
not, a new structural law or an explicit contingent state is unavoidable.

## 22. Literature boundary

- Barandes, *Quantum Systems as Indivisible Stochastic Processes*:
  https://arxiv.org/abs/2507.21192
- Chiribella, D'Ariano, and Perinotti, *Theoretical framework for quantum
  networks*: https://arxiv.org/abs/0904.4483
- Oreshkov, Costa, and Brukner, *Quantum correlations with no causal order*:
  https://arxiv.org/abs/1105.4464
- Sorkin, *Quantum Mechanics as Quantum Measure Theory*:
  https://arxiv.org/abs/gr-qc/9401003
- Shi, Kato, and Kim, *Fusion rules from entanglement*:
  https://arxiv.org/abs/1906.09376
- Oriti, Ryan, and Thurigen, *Group field theories for all loop quantum
  gravity*: https://arxiv.org/abs/1409.3150
- Crutchfield and Young, computational-mechanics causal-state construction:
  https://csc.ucdavis.edu/~cmg/papers/cmppss.pdf
- de Vink and Rutten, probabilistic bisimulation and continuous coalgebraic
  extension: https://doi.org/10.1016/S0304-3975(99)00035-3

These sources motivate architecture and controls. None supplies the missing
Paper 13D whole-process parent law.

## 23. Conclusion

One common parent experiment is a coherent physical idea when its target is a
dependent sum and its structural outcome is fixed by complete predictive
behavior. This removes the arbitrary decoder and dormant-hardware problems.

The accepted Gamma already realizes the construction at the target level. It
produces a normalized, projective, point-free family of complete predictive
relational configurations. This is more than a formal possibility and less
than a cosmological process selector.

The same calculation also sharpens the remaining obstruction. Random fields
and bonds do not randomize execution syntax. Complex amplitudes do not repair
that absence unless inequivalent structures physically recombine. Symmetry
does not fix class odds unless it acts on the classes themselves.

Paper 20 therefore opens a restricted generated-target program and closes no
spacetime claim. Original Paper 17 still requires one genuinely branching
whole-process parent, independent calibration of its residue, or a theorem
that the desired result is invariant over every admissible parent law.
