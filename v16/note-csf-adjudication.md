# CSF panel adjudication — the spectrahedron survives; the recurring physical law is not individuated

**Date:** 2026-08-18

**Frozen candidate:** `61c32d884d688f49f29d3863fe5959d1053d382e`

**Panel grades:** OPERATOR `ACCEPT-WITH-FIXES`; RECORDS `REJECT`; PHYSICS
`REJECT`.

## 1. Frozen evidence

| seat | report | ordinary SHA-256 | normalized self-SHA-256 |
|---|---|---|---|
| operator algebra / convex law geometry | `v16/review-csf-operator.md` | `d679072ab5341bc986e67a1327e8d756b835de4abb38d472afe9090c04df99b7` | `e3fde6cfe5dd98a0b70264126326dc21c9c68afe55134d3edc999a3e7a5cda57` |
| records / representation / recurrence | `v16/review-csf-records.md` | `2493723d3deafbec6c1dbcfc17490009cfb7495584faa201c93e40f7880ab884` | `c15b5c7a9a116b26d255f5d44109a8dfb0f616bbdbbab0f30ff40f21b1a6a4ec` |
| locality / no-signalling / ontology | `v16/review-csf-physics.md` | `b7902de853e8a8311b8e72825db8e20c4962ef7ef0bef55b4620194b3f74eade` | `2033a01d7f6d1f3c292644e332c0bb34411db79a186265672904515b3df5918b` |

All three authenticate the frozen chronology and independently reproduce the
candidate's exact operator arithmetic. The panel finds no false rational
number in the spectrahedral, JCV, rich-spectrum, port-fiber, flag, tangent, or
fixed-factor no-signalling calculations.

## 2. Primary ruling

The earliest applicable pre-registered outcome is

```text
CSF-BLOCKED-AT-HISTORY-INDIVIDUATION
```

The scorer supplies each context with actor names, a relation list, and an
anonymous relative operator, then defines its histories as `(I, Omega)`. The
actor/relation data do not generate either operator, an elementary event
sequence, a common relational boundary, or a record/null quotient. Erasing
the relational labels while retaining the operator packets leaves every CSF
equation, selected kernel, channel, flag, and safety statistic unchanged.

Consequently the frozen positive primary

```text
CSF-RECURRING-LAW-SELECTED-MODULO-GAUGE
```

is rejected. A frozen naming dictionary prevents postselection; it does not
individuate a physical history.

## 3. Exact conditional result

At fixed, already typed histories, the mathematical core is sound. For

```text
K_j = sum_h c[j,h] V_h,
M = C^dagger C,
```

all-input completeness and the unconditioned channel factor through `M`.
The lawful complex kernels form the spectrahedron

```text
{ M >= 0 : L_V(M) = I }.
```

The five registered context families have exact affine dimensions
`2,2,1,1,2`. Treating the first three token-disjoint contexts as occurrences
of one full kernel packet reduces their independent total dimension from
`5` to the one-dimensional family

```text
M(t) = diag(t,1-t),  0 <= t <= 1.
```

This `5 -> 1` cut is the exact price of a **full-kernel universality
postulate**, not a consequence of the local completeness equations. Imposing
endpoint exchange as a physical **law invariance** then fixes `t=1/2`. If
exchange is instead passive gauge, quotienting `t ~ 1-t` leaves a continuum;
it does not select the midpoint. The correct conditional sentence is:

> Given the declared history coordinates and full-kernel recurrence across
> the three contexts, completeness leaves a one-dimensional diagonal family;
> imposing endpoint exchange as a physical symmetry fixes `M=I/2`.

The held-out rich context adds exact row rank zero beyond the training rich
context. Its pass is algebraically redundant, not held-out prediction.

## 4. Surviving findings

The following survive at their narrowed scopes:

1. `COMPLETENESS-SPECTRAHEDRON-CONSTRUCTED` for fixed typed finite histories
   over complex PSD kernels. Exact-field and port-count restrictions may
   narrow the realizable factorization set.
2. `JCV-UNCONDITIONED-BASE-AND-CALIBRATED-FIBER-EMBEDDED` as an exact
   operator/instrument reconstruction.
3. `RICH-SPECTRUM-UNCONDITIONED-CROSS-MOMENT-ZERO` for the registered
   two-unitary history class. It is not yet a record, causal-order, or
   geometry theorem.
4. The two displayed factorizations of `M=I/2` give retained-port
   probabilities `1` and `9/25`. This proves a calibrated instrument fiber
   can be operationally nontrivial; it does not select the factorization.
5. The registered restriction removes exchange and makes the selected point
   non-extreme, so extreme-point selection is constraint-relative and
   unstable.
6. Flag orthogonality is constructed at one boundary, while the exact
   reconvergence control shows permanence is unproved.
7. A complete local instrument preserves Bob's unconditioned marginal on a
   fixed factor. Conditional steering and changing subsystem/carrier
   factorizations remain open.

Two generic qualifications are binding. First, different kernels can induce
the same unconditioned channel, so `M` can itself possess an operational null
quotient. Second, complex PSD feasibility need not equal fixed-port
`Q(i)` realizability.

## 5. Killed and withdrawn claims

`RECURRENCE-DOCTRINE-MOVES-PHYSICS` is killed as delivered. Its `[1,0]` and
`[0,1]` “predictions” are literal source constants; no preparation, history
map, kernel, port factorization, or calibrated probe derives them.

The phrases “selected modulo gauge,” “derived recurring locality,” and
“held-out validation” are withdrawn. The relational flag, durable record,
elementary transport/rewrite law, catalogue, actualization, primitive arity,
fields, particles, Hamiltonian, Lorentz/continuum structure, gravity, and any
QFT/GR deviation remain unconstructed.

## 6. Repair boundary

This is not a bounded prose repair. A successor must:

1. generate the histories from one relational event grammar and transport
   law, including their configurations and common boundary;
2. establish event-type identity before imposing recurrence, or price the
   identification explicitly as universality;
3. separate passive gauge quotient from active law symmetry;
4. replace the redundant held-out row with an independent equation or
   calibrated screen; and
5. derive any doctrine-sensitive prediction from a frozen common observable.

The CSF operator machinery remains useful as the exact law-space apparatus
for that successor. It is not, by itself, the selected ontic dynamics.
