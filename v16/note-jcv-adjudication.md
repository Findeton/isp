# JCV PANEL ADJUDICATION — THE LOCAL COMPATIBILITY VARIETY SURVIVES; THE FIXED POINT DOES NOT

**Date:** 2026-08-17

**Frozen candidate target:** `35c2511657efbee6c1c1887f2d7626faa4d396ea`

**Frozen protocol:** `a25516a`

**Panel grades:** ALGEBRA `ACCEPT-WITH-FIXES`; OPERATOR
`ACCEPT-WITH-FIXES`; GRAVITY `ACCEPT-WITH-FIXES`.

## 1. Frozen independent evidence

The three reports were written without cross-reading and committed verbatim
before this adjudication:

| seat | report | SHA-256 | numerical discrepancies |
|---|---|---|---:|
| algebraic geometry / quantifiers | `v16/review-jcv-algebra.md` | `eae135ebbb974d2ad1086fe3f2b9947c162cdfbf43ddbd2a899e90cfc285147f` | 0 |
| operator / instrument / ontology | `v16/review-jcv-operator.md` | `33e8afb5da87fd0dd5cf59a4aa72cc48d4457b208e0a27f7a4aa11b31d6abc5c` | 0 |
| gravity / covariance / composition | `v16/review-jcv-gravity.md` | `7523f6e97731e7643ba58ba88b099deb8e8de0c34509ec7a5bfab267b1c8c052` | 0 |

All three independently reproduce every artifact hash and total seal, the
1,024-to-16 chart-sign quotient, all shared and control sectors, every
dimension, both registered rational witnesses, all-input trace preservation,
the calibrated probability values, the frozen decision table, and the
serializer-only repair.  No equation or registered number is corrected.

## 2. Joint ruling

The exact finite result is accepted, but its physical translation is narrowed:

```text
DECLARED REAL CALIBRATED DOCTRINE:
  one local two-triangle compatibility variety is solved exactly.

ACTIVE FULL-COHERENT LOCUS:
  flat four-holonomy key unique modulo chart-sign gauge;
  boundary-instrument weights form a real two-dimensional family.

DARK LOCUS:
  six nontrivial-holonomy sectors survive only in law-null channels;
  their mismatch is operationally silent for this one-step instrument.

RECORD-GENERATED COMPARISON/LAW FIXED POINT:
  not constructed and not selected.
```

The frozen machine words `JCV-STRATIFIED` and
`JCV-PAIRING-SELECTED-WEIGHTS-FREE` remain exact outputs of the pre-registered
classifier.  They may be cited only with their definitions and declared
scope.  The adjudicated explanatory wording is:

```text
LOCAL DECLARED-DOCTRINE COMPATIBILITY VARIETY;
ACTIVE FLAT HOLONOMY / BOUNDARY-INSTRUMENT WEIGHTS FREE;
DARK MISMATCH ONE-STEP NULL;
GLOBAL RECORD-GENERATED SELECTION OPEN.
```

In particular, `selected pairing` must not be read as a carrier-derived
physical identification.  What is selected on the active locus is flat
holonomy inside a predeclared real isometric, nondegenerately calibrated chart
doctrine.

## 3. Exact algebraic result confirmed

1. The connected five-edge/four-vertex graph has cycle rank two per calibrated
   channel.  The chart-sign action has orbit size `8` per channel and `64`
   jointly.  The four triangle products separate the `1,024/64=16` gauge
   orbits.
2. Raw and quotient defects agree exactly because, for example,
   `ac-e=e(ace-1)` on the sign locus.  The registered 4,096 raw-channel and
   2,048 path-composition checks are exact.
3. A history column is available precisely when both of its triangle
   holonomies are `+1`.  Completeness therefore leaves seven shared-law
   sectors: one two-dimensional full-coherent sector at `(1,1,1,1)` and six
   one-dimensional sectors with one column identically zero.
4. With independent local weights, nine sectors survive.  The two additional
   sectors are exactly the mixed plus-to-minus and minus-to-plus handoffs.
5. The active equations become transparent under

   `A=x+y`, `B=x-y`, `C=u+v`, `D=u-v`:

   `A^2+C^2=1`, `B^2+D^2=1`.

   Thus the active real locus is `S^1 x S^1`, not merely a complex
   positive-dimensional variety with two sampled real points.  It is smooth,
   reduced, and genuinely real two-dimensional.  The calibrated value
   `p_plus=A^2` is nonconstant there.
6. The six reported dark rows count holonomy sectors whose registered
   full-coherent open locus is empty.  They do not exhaust the inactive
   divisors `Delta*x*y=0` inside the all-trivial-holonomy component.

The explicit product-of-circles proof is binding in the repair.  A generic
inference from algebraic-closure dimension plus two real witnesses would not
have sufficed.

## 4. Operator meaning of the two-dimensional freedom

For the declared interface,

`K0=x I+y Z`, `K1=u I+v Z`,

and the two polynomial equations give

`K0^dagger K0+K1^dagger K1=I`

for every solution and every input, with complete positivity supplied by the
Kraus form.  Appending the orthogonal outcome register therefore gives a valid
fixed-boundary CPTP instrument.

The two registered active witnesses require a qualification.  Both have
`x^2+u^2=16/25` and `y^2+v^2=9/25`, and hence the same unconditioned channel:

`E(rho)=(16/25) rho+(9/25) Z rho Z`.

They differ in the outcome-resolved Kraus unravelling and move the registered
outcome-zero probability from `0` to `49/625`.  That is operational movement
because this fixture declares a calibrated outcome record.  It is not, by
itself, movement of the unconditioned transfer or proof of an ontic history
decomposition.

The active family also contains a direction that changes the unconditioned
channel.  The panel supplied the exact post-review witness

`(x,y,u,v)=(3/13,-48/65,4/13,36/65)`,

whose coherence eigenvalue is `-119/169`, rather than `7/25` at the two frozen
witnesses.  This is accepted as a post-review analytic corollary, not silently
relabelled as a preregistered measurement.  It confirms that weight freedom
survives quotienting ordinary Kraus-unravelling freedom.

The words `interference`, `history channel`, `durable`, and `dynamically
silent` remain conditional.  The `I/Z` column decomposition and outcome-record
typing are declared.  Durability under future continuation, a carrier-history
referent, and objective actualization are not derived.

## 5. What the comparison and homogeneity tests actually establish

The comparison maps are exact calibrated inter-chart coordinate dictionaries.
No relation rewrite, continuation algebra, record extractor, or held-out
transport experiment establishes that they identify the same physical unread
fact across distinct carriers.

Full rank at both triangles already forces all four holonomies to `+1`, even
when the two triangles use independent full-rank laws.  Reusing the same
numeric `W` is therefore not responsible for active flatness.  The exact
control statement is:

```text
ACTIVE FLAT-HOLONOMY RESULT: HOMOGENEITY-INDEPENDENT.
DARK MIXED-HANDOFF EXCLUSION: HOMOGENEITY-DEPENDENT.
```

Moreover, equality of numeric arrays in two declared chart trivializations is
not yet a transported local law.  A physical homogeneity claim needs a
transport rule or one relational rule from which the local arrays follow.

## 6. Local overlap is not global coherence

The two overlapping triangles test both cycle generators of the declared
five-edge graph.  They do not establish associativity, cylindrical
consistency, or extension to a larger atlas.

The gravity seat's exact countermodel is adopted.  Keep every existing edge
sign `+1`, use either active witness, and add a direct edge `03`.  Giving its
channel signs `(-1,+1)` preserves every old JCV row while making the new plus
cycles inconsistent with the active law.  Giving it `(+1,+1)` yields a flat
extension.  The current solution therefore does not determine the next
overlap.  Every positive claim must retain the tag `LOCAL-TWO-TRIANGLE`.

This is the v16 form of the v13 COC warning: closure at a declared atlas need
not survive the extension atlas.

## 7. Adjudicator theorem: the surviving mismatch is law-null

The panel's dark-sector findings imply a short general theorem that was not
stated in the frozen candidate.  It is recorded as an adjudicator-derived
corollary, not panel evidence.

Let `H` be the declared history-channel space, `W:H->O` the outcome-amplitude
map, and `Q:H->H` a comparison action satisfying cut equality

`W Q = W`.

Let `N=ker W`.  Then:

1. if `n` is in `N`, `WQn=Wn=0`, so `Q(N)` is contained in `N`;
2. for every `h`, `W(Qh-h)=0`, so `Qh-h` is in `N`;
3. consequently `Q` induces the identity on the quotient `H/N`.

In this fixture, every nontrivial holonomy surviving normalization acts only
on an identically zero column.  The six dark mismatches are therefore not
observable differences of the delivered one-step boundary instrument.  They
are alternative extensions on data the current law never uses.

This does **not** prove that they are gauge or forever unphysical.  A later
continuation may repopulate a presently null direction.  The physically
relevant equivalence must therefore use the **complete continuation-stable
null ideal**: two history expressions are equivalent only if every admitted
future composition, preparation, and readout gives the same statistic.  The
quotient must be a congruence under gluing and refinement.

This changes the interpretation of `JCV-STRATIFIED`: it is an exact
stratification of the declared joint algebraic solution set, while the
physical distinction of its dark components remains open until a complete
operational quotient is built.

## 8. Why this is not a fixed point

No self-map in JCV performs the required cycle:

1. the law generates stable record partitions;
2. those records determine which boundaries are identical, comparable, or
   orthogonal;
3. that comparison structure types and coarse-grains the law;
4. refinement returns the same record structure and effective law.

JCV instead freezes the chart algebra, outcome rows, coherent columns, gauge,
and comparison family, then solves simultaneous compatibility equations in
`(q,W)`.  That is a useful joint constraint variety, but it neither runs nor
closes the record-to-comparison-to-law loop.  `Fixed point` is withdrawn from
the title and positive conclusion.

## 9. Refined successor object

The investigation now has a more focused serious candidate for the missing
object.  Start from a free typed algebra/category of syntactic complete
histories and a strongly positive decoherence functional `D`.  Strong
positivity gives a semi-inner product and a null radical.  Quotient only by the
radical that remains null under every licensed continuation, producing the
physical history Hilbert space/boundary algebra.  The comparison maps are then
induced by equality in that quotient rather than inserted before the law.

The decisive fixed-point conditions are:

- the null radical is a congruence under gluing, spectators, and refinement;
- durable record partitions are orthogonal in the induced space and remain
  so under future continuation;
- unrecorded alternatives share one quotient boundary and may interfere;
- coarse and fine constructions give the same induced functional and
  operational probabilities under at least three overlapping refinements;
- all-input/ancilla-stable instrument completeness holds at every licensed
  division boundary;
- the law's own stable records reproduce the quotient used to type that law.

This is a construction target, not a result.  Strong positivity alone does
not select the weights, and bare actualization remains a separate postulate.
The relevant mathematical precedents are the histories-Hilbert-space/GNS
constructions of [Craig](https://arxiv.org/abs/quant-ph/9704031) and
[Gudder](https://arxiv.org/abs/1011.1694), the exact record/decoherence use in
[Dowker and Halliwell](https://journals.aps.org/prd/abstract/10.1103/PhysRevD.46.1580),
and the composability role of strong positivity discussed by
[Boes and Navascues](https://arxiv.org/abs/1609.09723).  These are structural
precedents only; none supplies ISP's missing selected functional or
back-reacting carrier law.

## 10. Serializer chronology ruling

The physical fixture, equations, solver calls, witnesses, classifier, and
renderer did not move in the repair; the scorer delta is exactly the total
set/frozenset serializer branch.  There is no evidence of exposed result
leakage or tuning.

The earlier phrase `FAILED-BEFORE-VERDICT` was too broad.  Source order shows
that the first process internally solved and rendered before the mutation
survey reached the serializer exception.  It emitted, wrote, promoted, and
committed no result.  The accurate chronology is:

```text
FAILED AFTER INTERNAL CLASSIFICATION,
BEFORE RESULT EXPOSURE, SERIALIZATION, OR PROMOTION.
```

The hard-coded `P-CHRONOLOGY=True` is not evidence; the git history and absent
result paths are.  Future scorers should test infrastructure serialization
before physical classification.

## 11. Consequence ruling

| topic | adjudicated status | finding |
|---|---|---|
| exact local sign/gauge variety | CONFIRMED | All panel reconstructions agree with every count and dimension. |
| active flat holonomy | CONDITIONAL-PARTIAL | Unique modulo chart-sign gauge only on the registered full-coherent locus inside the declared doctrine. |
| physical cross-carrier identification | OPEN | No rewrite-generated boundary algebra or held-out continuation calibrates the dictionaries. |
| boundary-instrument weights | UNSELECTED | A real two-dimensional family remains and changes both outcome-resolved and, along another direction, unconditioned behavior. |
| global comparison/refinement structure | OPEN | A third-edge countermodel shows the local fixture does not determine its extension. |
| record-generated fixed point | REFUSED | No record/comparison/law self-map is defined or closed here. |
| fixed-boundary instrument | CONFIRMED | The declared two-outcome map is CPTP for every input and ancilla. |
| growing-factorization no-signalling | OPEN | No relational composite system or transported subsystem algebra exists. |
| durable record permanence | OPEN | Orthogonal outcome typing is declared; future-stable availability is untested. |
| actualization | POSTULATE | The solution variety does not make one outcome occur. |
| geometry and backreaction | REFUSED | There is no relation rewrite, metric, source, or held-out geometry response. |
| Hamiltonian/generator | OPEN | One boundary-instrument family supplies neither clock nor selected evolution law. |
| particles/species/all-arity | REFUSED | No vacuum, transport spectrum, statistics, scattering, or all-`n` law is present. |
| constants/scale/QFT/GR deviations | REFUSED | No dimensionful datum or typed phenomenological comparison is computed. |

## 12. Binding bounded repair

Before terminalization at this scoped result:

1. rename the paper from `fixed point` to `joint compatibility variety` and
   demote every self-generation/circularity claim;
2. call the maps declared calibrated inter-chart dictionaries and the charts
   interface surrogates, not instantiated carrier rewrites;
3. add the exact `S^1 x S^1` real-locus proof and define the sector-level use
   of `dark` without claiming it exhausts inactive divisors;
4. state the operator decomposition of the two-dimensional freedom, disclose
   that the two registered witnesses share one unconditioned channel, and
   label the third witness as a post-review analytic corollary;
5. narrow `interference`, `durable`, and `dynamically silent` to their declared
   one-step interface meanings;
6. state that active flatness is homogeneity-independent and that homogeneity
   excludes only the two dark mixed handoffs;
7. add the direct-`03` extension countermodel and `LOCAL-TWO-TRIANGLE` scope;
8. add the law-null quotient theorem and make complete continuation-stable
   operational equivalence the next selector;
9. sharpen the external analogies to motivation only;
10. correct the first-run chronology and remove the hard-coded chronology gate
    from any claim of runtime proof; and
11. regenerate and reseal the paper, transcript, and receipt from the repaired
    renderer, then run full worktree and true off-tree/no-git verification.

No fixture equation, gauge quotient, exact solver result, witness value,
frozen classifier word, or original report is to be changed.  The repaired
artifacts must retain the original candidate commit as provenance and mark all
post-review corollaries explicitly.

## 13. Status

```text
FROZEN EXACT FINITE CLASSIFIER: CONFIRMED AT REGISTERED SCOPE
LOCAL ACTIVE FLAT-HOLONOMY RESULT: CONFIRMED CONDITIONALLY
BOUNDARY-INSTRUMENT WEIGHT SELECTION: REFUTED
DARK-SECTOR PHYSICAL DISTINCTNESS: OPEN; ONE-STEP NULL
RECORD-GENERATED COMPARISON/LAW FIXED POINT: NOT CONSTRUCTED
GEOMETRY/BACKREACTION/ACTUALIZATION/ALL-N/QFT-GR: NOT ADVANCED
```

One bounded repair and one terminal verifier are authorized.  No new physics
unit is authorized by this adjudication.  After the repaired Paper 2 reaches
its scoped terminal or refused state, halt and report to the user.
