# APR Paper 12 — v3 minimal delta-repair pin

**Date:** 2026-08-19  
**Status:** FROZEN DELTA CONTRACT; NO V3 SCORER OR ARTIFACT EXISTS  
**Parent failure:** `v16/note-apr-v2-verification-failure.md`

## 1. Scope and immutability

The v2 scientific arithmetic and strict-primary ladder are not editable. The
APR pins, core, fixtures, scorer protocols, repair contracts, v1/v2 artifacts,
and their failure notes remain immutable. This delta may modify only:

```text
v16/code/apr_score.py
```

It may add synthetic self-tests inside that file. It may not run the frozen
fixture before the new source is frozen, including through `--mutant` or
`--mutants-all`. Only AST/static checks and `--selftest` are permitted during
implementation. The permanent v2 prefreeze-exposure disclosure remains part
of every successor; v3 is not retrospectively blind.

The implementation must address exactly the ten items below. It may not add a
missing process assignment, tensor, overlap selector, probe compiler, causal
schedule, support equalizer, regional `Tau`, quantum law, or ontology.

## 2. Assignment-typed identity census

Compute process identities from registered assignment provenance, not from
raw horizontal fillings. In the frozen question-tree assignment, only the
registered empty-tree mapping `qw_000 -> hf_015` may witness the `B0` identity.
`hf_016` is a replacement/swap filling and cannot count merely because its
source and target boundary coincide. Raw structural rows such as `hf_000` or
`hf_006` also cannot count without an active assignment.

The receipt must show, per boundary:

- the assignment row and empty-process semantic witness, if present;
- why every raw-but-unassigned identity-like filling is excluded;
- that identities above the registered `B0` boundary remain absent.

Synthetic controls must distinguish an assigned identity from an unassigned
identity-shaped arrow and from a nonidentity endomorphism.

## 3. Semantically valid process coordinate

Delete the unregistered word `HORIZONTAL-PROCESS-CONSTRUCTED`. The only legal
process-coordinate values are:

```text
SYNTAX-ONLY
STATIC-RESPONSE-ONLY
HORIZONTAL-CLASSICAL
HORIZONTAL-QUANTUM
INCONSISTENT
```

Required keys with `None`, empty stubs, or disconnected declarations never
promote. `HORIZONTAL-CLASSICAL` requires an explicit typed assignment plus
validated identity, composition/cut, mass preservation, and the other gates
claimed by that coordinate. `HORIZONTAL-QUANTUM` additionally requires the
registered interference and all-input quantum structure. If a complete
positive synthetic package is used to test reachability, every required
semantic equation must be represented and recomputed; deleting or nulling one
required component must demote it. Controls cannot supply APR physics.

## 4. Consistent M16 relation erasure

Replace the invalid deletion with an exact `c -> d` substitution on the first
matched interface of `rf_000`:

- replace the node-1 occurrence of `edge_0_c` by the already allocated
  `edge_0_d` occurrence in the region antichain;
- update the cross-check incidence from `edge_0_c` to `edge_0_d`;
- retain the component catalogue, resource declaration, and frozen blind map,
  in which `c` and `d` share the same blind interface role.

Recompute from antichains. The represented candidate-regional overlap count
must move `2 -> 1`; projection consistency must remain true; the blind colored
graph/hash must remain equal. The old deletion-only mutation must be retained
as a negative that refuses for projection/interface inconsistency. Missing,
nonfunctional, or contradictory projections also refuse.

## 5. Actual M15 oracle family

M15 must instantiate three wrong implementations, not call the correct
canonical `PrefixRegion` constructor:

1. a raw-syntax copy keyed to an unreduced expression tree, so
   `join(cyl(000),cyl(001))` fails to agree with its canonical region `[00]`;
2. an identifier-hash copy that disagrees on a fresh-ID clone of the same
   canonical region;
3. the already measured finite-depth whitelist that accepts its registered
   rows and rejects valid `[000]`.

Run fresh-region, Boolean-relabel, and refinement tests on each implementation.
P5 remains only the concrete fresh `[000]` failure of arm 3 and cross-references
the same changed-object hash; it is not an independent mutation.

## 6. Reconstructible L2 and L4 objects

L2 and L4 become `ANALYTICAL-CONTROL`, not active-law semantic mutations.

- **L2:** construct an explicit finite child-swap family from frozen
  replacement-control rows, delete one serialized generator, and recompute the
  orbit/fixed-space movement. Publish full pre/post family payloads and hashes.
- **L4:** construct an explicit finite transitive exterior permutation/mixing
  family, replace it by a serialized multiple-orbit subfamily, and recompute
  transitivity/orbit/fixed-space movement. Publish full pre/post payloads and
  hashes.

Hidden options, fixture-identical pseudo-mutations, or
`constructed_measurement:RECOMPUTED` literals are forbidden. These finite
families remain analytical and cannot instantiate the infinite intrinsic
replacement grammar or promote locality.

## 7. Typed continuation and recovery scope

Remove `typed_subgrammar_only:true` unless an actual closed typed subgrammar is
constructed. For each `qc` catalogue:

- type every operation's carrier/domain/codomain and process assignment;
- construct only composable words from assigned operations;
- identify readers/effects separately from state transformations;
- exclude unconstructed tensor, mixed, replacement, identity-at-new-boundary,
  and unattached reader/effect rows;
- quantify recovery only over the resulting closed typed word set.

If no such word set exists, report `TYPED-CONTINUATION-SUBGRAMMAR-UNCONSTRUCTED`.
In particular, do not call `qc_002` reset-destroyed without a typed same-law
reset-to-reader word. The standalone reset and redundant-copy controls remain
valid finite controls, and `qr_000` remains only its explicitly typed delayed-
reader schedule. M23 stays `UNAVAILABLE` unless the frozen interfaces genuinely
compose it.

## 8. End-to-end semantic port binding

Port semantics must be transported and checked through every frozen consumer:

```text
question formula -> branch restriction -> bit -> next_record_word
-> tree frontier/cospan port -> delayed reader/recovery binding
```

The neutral-ID/list-order metamorphic must consistently rewrite every
reference and preserve all computed signatures. Negative controls must alter
one downstream map—especially `next_record_word` or a later port-to-bit map—
while keeping the formula fixed and must refuse for contradiction. Missing or
duplicate semantic ports also refuse. No list order or ID suffix may repair a
broken chain.

## 9. Separate measured role from ontology proposal

The receipt contains two fields:

- `ontology_role`: the protocol diagnostic of the construction actually
  delivered, using `STATIC-RESPONSE`, `FIXED-ALGEBRA-CONDITIONING`,
  `RECORD-WRITING-ON-FIXED-ALGEBRA`, or `REGION-REWRITING`;
- `ontology_candidate`: `POSTULATED-CANDIDATE-RELATIONAL-WEB`, explicitly not a
  measurement or actualization result.

At the frozen APR baseline, the role cannot exceed `STATIC-RESPONSE` until a
typed recoverable record process exists. The transcript and status renderer
must not substitute the proposal for the measured role.

## 10. Generated-contact classifier gate

The highest-rung classifier must require `generated_contact` independently of
`causal_order`. With every other synthetic capability true and generated
contact false, it must not return
`APR-JOINT-POINT-FREE-REGIONAL-LAW-CONSTRUCTED`; use the earliest compatible
registered block and print the missing contact capability. A full synthetic
package with both causal order and generated contact may reach the highest
word if every other gate is genuinely true.

## 11. Scope wording

Replace overbroad wording by exact statements:

- `finite regional-support controls only; regional-support coordinate
  unearned`;
- `raw prefix-syntax atomlessness only; physical regional referent
  unconstructed`.

`locality=FAIL` means promotion failure/unconstructed capability, not observed
physical nonlocality. The qualifier may not use “atomless response” without
the word `syntax`.

## 12. Freeze and v3 sequence

Before fixture evaluation:

1. run AST/static/diff checks and synthetic self-tests only;
2. prove all ten delta items with new generic negatives;
3. freeze the scorer and a source note in a separate commit;
4. do not invoke `--mutant`, `--mutants-all`, or `--run` before that commit.

After source freeze, publish once to new absent paths:

```text
v16/apr_output_v3.txt
v16/apr_receipt_v3.json
```

Require repository-root, alien-CWD, and true off-tree/no-git byte identity.
Then rerun the independent black-box verifier, including all 197 green v2
checks, the seven failed gates, the identity/process/classifier source attacks,
and every reconstructed descriptor. Candidate prose and the APR panel remain
prohibited until v3 verification is fully green.

## 13. Ontology ceiling

This delta repairs diagnostics only. It cannot make the Boolean syntax a
physical region, a dependency root a joint law, a flag a durable fact, a
blind graph geometry, or finite controls a continuum. The postulated candidate
ontology and permanent exposure debt remain explicit. No causal spacetime,
matter-geometry backreaction, metric, curvature, GR/QFT, particles,
Hamiltonian, or actualization claim is authorized.

