# Scoped synthesis — what terminal Γ_D does and does not supply

Date: 2026-08-22

Status: **SYNTHESIS NOTE — no theorem, construction, or coordinate is
created here**; it consolidates adjudicated results at their engraved
scopes (#271, #282, #237, #307, #321–#323, #324–#330) after the #330
correction. Scope: **the certified fixtures of terminal Paper 13D
(`3b91766f…`) and its direct descendants only.** Nothing here is a
claim about background-free relational process laws in general, about
future enlarged laws, or about nature.

## 1. What Γ_D actually supplies

- A finite conditional mathematical law: typed experiments →
  distributions over stabilizer-orbit history cells (13D §9). Accepted
  as mathematics; not empirically established physics.
- Typed execution traces retaining every traversed boundary (§7.1);
  simultaneous fusion distinct from staged sequences (§10.2).
- An **undirected** endpoint-bond structure (`P23C-UNORIENTED-BOND-
  STRUCTURE-DERIVED`), exchangeable under the full presentation
  groupoid including per-occurrence X/Y port swaps.
- Bidirectional relational response (Thm 8) and — via Paper 18/#282 —
  **partial Γ-relative operational precedence**: all-suffix prefix
  autonomy and a scoped precedence relation. Direction was not lost
  everywhere; what survives is operational, conditional, and
  Γ-relative, not a chronology.
- The trace-sensitive sector family of Paper 23a v2 (corrected):
  commutative graded nonassociative magma without unit; finite closure
  fails by carrier-or-trace monotonicity.

## 2. What is proved absent *for this law* (each at its own scope)

1. **No native orientation/order supply.** No covariant single total
   order on free carriers (Prop B); the exchangeable realizer-pair
   class consumed by Paper 15 §4.8 is **not determined** by Γ_D —
   antiparallel and parallel classes carry one identical law while
   differing in oriented pattern densities (Lemma C + Prop D), and
   rigid-experiment assignments to genuinely distinct classes are both
   admissible with no law-selected choice between them (#330;
   `P23C-REALIZER-CLASS-UNIQUENESS-FAILURE`). Class-valued covariant
   existence is *not* excluded — the failure is determination, not
   existence.
2. **Decoder nonselection** (#237): B and two-point order data leave
   curvature undetermined; smoothness/aesthetics are inadmissible.
3. **Sector-arithmetic vacuity**: nonassociativity plus unit-withdrawal
   means the FP fusion-ring bootstrap route has no subject matter on
   these fixtures (#321–#323), fixture-scoped, not a global no-go.
4. **Dependency ≠ chronology** (Paper 15 §1, standing): structural
   precedence never became operational influence within the accepted
   record.

The honest one-line summary: *present Γ_D determines combinatorial and
operational-relational structure up to exchangeability, but does not
determine oriented, geometric, or sector-statistical structure.* This
is a statement about one accepted law — the grand no-go reading across
all background-free laws is not licensed and is not claimed.

## 3. Where orientation could still enter — two distinct doors

These must never be conflated:

**(a) Symmetry breaking IN THE LAW.** Adding asymmetric primitives,
generators, kernels, or port structure directly to Γ_D's syntax. This
is a new physical postulate; the failed ancestors (13B/13C terminal
REJECTs) counsel care, and nothing in this programme authorizes it.

**(b) Symmetry breaking IN THE STATE / ROOT / WEIGHT.** Keeping a
symmetric law but sourcing asymmetry from above it:

- an asymmetric boundary condition or cosmological/root state;
- spontaneous-symmetry-breaking sectors within an enlarged law;
- a relational clock subsystem whose records orient others;
- actuality weights over histories (asymmetric actual histories);
- environment-selected records.

Formally this is exactly the missing whole-process object recorded
since #308:

$$\Gamma_{\rm struct}(d\chi,dH),\qquad (\Gamma_{\rm struct})_\chi=\Pi_{\rm phys},$$

whose χ-marginal `Pi_phys` and regular conditional (= Γ_D) would let a
root/boundary/state inject orientation without touching the primitive
law. All of Unit C's obstructions are blind to such a source by design:
they prove Γ_D alone does not select orientation; they do not touch any
selection supplied by a root state or declared weight. Conversely,
supplying one is precisely where the missing physics would live — which
is why Unit D's declared-model calibration is rehearsal, not resolution.

## 4. Consequence map for the gates

| gate | status | blocking fact |
|---|---|---|
| Paper 15 rigidity input | CONDITIONAL | realizer class not determined (#330) |
| Chronology (complete) | closed | only partial Γ-relative precedence exists |
| Decoder selection | closed (#237) | full-law rigidity absent; aesthetics barred |
| Dimension selection | closed | ensemble gate shut; no Π_phys |
| FP channel odds | vacuous here | nonassociative, unitless sectors |
| Emergence route | OPEN | requires Γ_struct/Pi_phys or a law change |

## 5. Standing walls (unchanged)

Scope walls per pin #324 §7; Paper 17 gate CLOSED; Paper 22 v3 consumed
nowhere; representative mass forbidden; harnesses are audit scaffolding.
No coordinate in §2 was awarded beyond its engraved scope; the #330
withdrawal is respected everywhere above. Next authorized event, per
user instruction ordering: the decision on Unit D (declared-model
calibration only) rests with the user; this note authorizes nothing.
