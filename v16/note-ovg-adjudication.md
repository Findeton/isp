# OVG panel adjudication — the fixed-carrier instrument variety survives; relational order does not

**Date:** 2026-08-18

**Frozen candidate:** `bb0f13aedadc354068ea2bcc08478bcd8c43ded1`

**Panel grades:** OPERATOR `ACCEPT-WITH-FIXES`; REWRITE
`ACCEPT-WITH-FIXES`; PHYSICS `ACCEPT-WITH-FIXES`.

## 1. Frozen evidence

| seat | report | ordinary SHA-256 | normalized self-SHA-256 |
|---|---|---|---|
| operator / instrument / representation | `v16/review-ovg-operator.md` | `43ceeb00e127034dc61d5c51f107b7162dde32bcbc9bf0365bbe85967a960421` | `b346b88c88dfa4715265c2c7cca05b01665d99bec8d751a1220ab6db30cdae63` |
| rewrite / concurrency / arity | `v16/review-ovg-rewrite.md` | `ed25254881a993dc8d44f77d69ad2bec58703b3d0fcf73edd308e6f6053abaa0` | `52af4a049e13becc51a87b4a26ee6fd9ad94cfdaa0e1943e87466d3d8f94b545` |
| causality / ontology / physical scope | `v16/review-ovg-physics.md` | `961996cc66cb1e63c7bdd0d357b34b3f5cd0e23f83b4ce3e75ba7165d28b8b10` | `16950436efaeea80a6b835f63393b5d91d2a55a2fea83240205a859ecee31fcc` |

All three independently authenticate the immutable candidate and reproduce
the central arithmetic: CNOT order operator, real-weight failure,
`(3/5,4i/5)` completion, parity ports, spectral ranks/nullities, nonnormal
control, Pauli-triad family, same-channel/different-port rows, factorization
words, Toffoli obstruction, spectator marginal, and amplifier failure. No
exact equation is corrected.

## 2. Joint ruling

OVG is accepted with a binding physical retyping:

```text
FIXED-CARRIER EVENT-LABELLED OPERATOR HISTORIES:
  implicit Gram/instrument equations and exact witness families constructed.

RELATIONAL CONFIGURATION HISTORIES:
  not welded to those operators.

PHYSICAL OVERLAP LAW:
  unselected.
```

The frozen machine segments remain exact only under these definitions:

1. `OVG-GRAM-INSTRUMENT-VARIETY-CONSTRUCTED` means an **implicit finite-
   history operator variety**, not a solved irreducible-component/moduli
   classification and not a relational successor variety.
2. `SINGLE-PORT-PHASE-CONSTRAINED` is a gauge-covariant constraint on
   `conjugate(a)b Omega`, not an absolute weight phase, exchange statistic,
   curvature, or Hamiltonian phase.
3. `MULTIPORT-COHERENCE-EXISTS-BUT-PORT-LAW-UNSELECTED` is mathematical CP-
   instrument existence. Physical port distinction requires a calibrated
   record algebra.
4. `LOCAL-FLAG-KINEMATICALLY-PERMITTED-BUT-IMPLEMENTATION-UNSELECTED` is
   narrowed to a formal Stinespring-style flag dilation plus declared
   catalogue placement. Local attachment, implementation, durability, and
   actualization are absent.
5. `COMPOSITE-SUPPORT-DOES-NOT-FORCE-PRIMITIVE-ARITY` survives as a refusal of
   inference. It does not select binary or higher primitive events.
6. `CAUSAL-NONSEPARABILITY-UNTESTED` remains binding.
7. `OVERLAP-LAW-UNSELECTED` refers first to the operator/instrument law; the
   elementary relational overlap family itself is not yet constructed.

## 3. Exact theorem confirmed

For common-boundary isometries `V_h` and port coefficients `c_jh`, all-input
completeness is

```text
sum_j K_j^dagger K_j = I,
K_j = sum_h c_jh V_h.
```

For two histories `A,B`, with `Omega=A^dagger B`, this becomes

```text
S I + C Omega + conjugate(C) Omega^dagger = I.
```

For one port and unitary `Omega`, the real-linear equation has nonzero
coherent solutions exactly when the distinct eigenphases occupy at most two
points. At two phases, the relative coefficient product phase is constrained
modulo pi; magnitudes remain free. For nonnormal/dimension-changing overlaps,
the full operator equation—not eigenphase counting—is the classifier.

The earlier proposed real-weight no-go is decisively false. In the registered
CNOT arena, `Omega` is non-scalar and `(3/5,4i/5)` gives a complete one-port
map. The parity pair `(A+B)/2,(A-B)/2` is complete for arbitrary common-
boundary isometries. It is an existence theorem, not a selected port law;
splitting one port supplies exact complete three-port refinements.

The Pauli-triad construction establishes a positive-dimensional embedded
family. Its three printed rows contain only two distinct rational parameter
points/screens. The analytic family, not the row count, carries the theorem.

## 4. Representation quotient

History rephasing accompanied by inverse coefficient rephasing leaves every
class map fixed. Thus `Omega` and a displayed coefficient phase are separately
frame-dependent; their product is invariant. Likewise a unitary rotation of
unretained ports is Kraus/unravelling gauge for the unconditioned channel. It
becomes physical only after a port record calibration is fixed and transported.

The exact one-port class map

```text
K = (3/5) A + (4i/5) B
```

is itself an ordinary unitary. Replacing the named two-history decomposition
by the single history `K` preserves every current channel and spectator
statistic. Therefore OVG's operator theorem alone does not establish physical
superposition of causal orders. A relational event algebra plus a calibrated
future responding to the fine-path cross term is still required.

## 5. Relational and all-n scope

The two CNOT order maps and four token-rewrite critical pairs are neighboring
fixtures. Matrix construction never consumes the token rewrite incidence, and
the token machine never consumes the CNOT maps. Equal `8 x 8` codomains provide
an operator common boundary, not a relation-derived common future. The fifth
pinned dimension-changing relational critical pair is absent.

The four token classifications survive only as independent grammar examples:
disjoint commuting, joinable overlap, delete/use dependency, and divergent
endpoints. Dependency is not a durable order record, and divergence is not a
no-go.

The `AB/BC/CD` extension has six orders but four distinct composites. Pairwise
Gram strata do not type a three-generator law, grouping coherence, or
associator/cocycle. The candidate correctly refuses arbitrary-`n` composition.

Toffoli is irreducible relative to the complete CNOT-only linear grammar,
including zero-initialized/zero-returned CNOT ancillas. That validates the
factorization assay and its grammar relativity. It neither selects Toffoli as
an ISP primitive nor establishes ontology-level indivisibility.

## 6. Physical consequences

All-input completeness implies ancilla-stable complete positivity and fixed-
factor, unconditioned no-signalling. It does not answer conditioned steering,
remote decomposition selection, changing Bob algebras, or carrier-changing
factorizations.

No OVG result establishes a locally implemented/durable order flag, quantum
switch, causal witness, causal nonseparability, relational backreaction,
fields/Fock structure, particles/species/statistics, a universal coupling,
Hamiltonian, Lorentz/continuum spacetime, gravity/GR, QFT, or empirical
deviation.

## 7. Repair boundary and disposition

The mathematical candidate does not need a new equation. Its citation scope
does. The adjudicated headline is:

```text
LOCAL FIXED-CARRIER GRAM/INSTRUMENT VARIETY CONFIRMED;
COMPLEX COHERENT PORTS EXIST;
PORT AND OVERLAP LAWS UNSELECTED;
RELATIONAL ORDER WELD, LOCAL RECORD IMPLEMENTATION,
CAUSAL NONSEPARABILITY, AND ALL-N COMPOSITION UNBUILT.
```

A successor—not an in-place OVG repair—must assign the operators to concrete
relational rewrites, derive a common future, include a dimension-changing
critical pair, test the full relabelling groupoid and three-overlap coherence,
and supply a calibrated discriminator against the one-history replacement.
