# CSF hostile review protocol

Status: **FROZEN BEFORE REVIEWER ASSIGNMENT**.

This protocol binds the replay-verified green-unreviewed CSF candidate. It
does not authorize reviewer dispatch, adjudication, repair, or terminal
promotion.

## Immutable target

| object | binding |
|---|---|
| candidate commit | `61c32d884d688f49f29d3863fe5959d1053d382e` |
| verification commit | `9f98e5aa5817c3001f8fe396f909d43c974a77b5` |
| pin | `c953618c66685b20705bef7436ebfa29d4b0370b076493bc1997aea898e1bcba` |
| generic core | `93a093d6ce72be4167d277719daf37aa7df7704510819f3b2e264546a14362b4` |
| physical fixture | `8c10210b6fee0a5477f3f70593cca080c26a4c91d678ad60bf691f6d853fbd37` |
| repaired scorer | `d3adf994e1c89fca5b53a0969cf0eed256488790b361477116b7cd1a76da84ba` |
| freeze/refusal/repair record | `b2a140a123cab91fe1aba19a87aa2ee9d9c09c97992260338123b3bd7be1ddf1` |
| transcript | `59077d8ad0f9e9ba4cf5afc0a44fea242d7a6032f1d998e088b3433cf4541785` |
| receipt | `7ae9b4a17fd38883bbff39b212f0edf819e2edf17942c9d54f8cf9f772414fdc` |
| Paper 6 | `543a2c927ecc7bd184fc758e4d72ebd4d4974327ae5ae2bb279d1fe33086c5d9` |
| candidate verification | `c0b3e7072ae2ba5a5fe45e1a26c988d36fe989b33cb02e71e57490db077b7cd5` |

Frozen machine findings, in order:

```text
CSF-RECURRING-LAW-SELECTED-MODULO-GAUGE
COMPLETENESS-SPECTRAHEDRON-CONSTRUCTED
JCV-UNCONDITIONED-BASE-AND-CALIBRATED-FIBER-EMBEDDED
RICH-SPECTRUM-UNCONDITIONED-CROSS-MOMENT-ZERO
CALIBRATED-RECORD-FIBER-OPERATIONALLY-NONTRIVIAL
SELECTION-CONDITIONAL-ON-EXCHANGE-SYMMETRY
RECURRENCE-DOCTRINE-MOVES-PHYSICS
EXTREME-POINT-SELECTION-UNSTABLE
FLAG-ORTHOGONALITY-CONSTRUCTED-BUT-PERMANENCE-UNPROVED
CONDITIONAL-STEERING-OPEN
ELEMENTARY-TRANSPORTS-AND-CATALOGUE-UNSELECTED
```

The candidate is not terminal. No reviewer may strengthen “conditional
selection” into a derived universal dynamics, identify an unconditioned
kernel with a selected calibrated instrument, or promote a finite fixed-
factor fixture into gravity, QFT, particles, arbitrary-`n`, steering, or
phenomenology. The disclosed #63 no-artifact refusal and #64 one-token repair
are part of the target.

## Independence contract

Three seats are mandatory. Each reviewer must:

1. receive this protocol, immutable target paths, `RUNBOOK.md`, and relevant
   antecedents;
2. work read-only and make no repository mutation;
3. not read, request, summarize, or infer either other report before freezing
   their own;
4. rebuild load-bearing claims independently rather than treating receipt
   gates or the #66 audit as proof;
5. state exact disagreements, counterexamples, and scope corrections;
6. grade `ACCEPT`, `ACCEPT-WITH-FIXES`, or `REJECT`; and
7. end with a numbered repair/kill list and SHA-256 of the report.

Reports are frozen separately as:

```text
v16/review-csf-operator.md
v16/review-csf-records.md
v16/review-csf-physics.md
```

A proof or exact counterexample outranks a preferred interpretation. A
predeclared convention prevents postselection but does not by itself turn the
convention into a physical law.

## Seat O — operator algebra and convex law geometry

This seat independently rebuilds the fixed-history mathematics and attacks
whether “spectrahedron,” “fiber,” and “selected” are licensed at the stated
scope.

### Mandatory recomputations

1. Starting only from typed histories `V_h` and port coefficients `C`, derive
   `M=C^dagger C`,
   `sum_j K_j^dagger K_j=L_V(M)`, and the stated index orientation of
   `Phi_M`. Audit complete positivity, trace preservation for every input,
   affinity, and ancilla stability.
2. Prove or refute that every positive-semidefinite `M` has the needed port
   factorization and that the lawful set is exactly the PSD cone intersected
   with the affine completeness equations. Distinguish the affine-hull
   dimension printed by CSF from the dimension and faces of the feasible PSD
   set.
3. Rebuild all three JCV coefficient matrices. Verify the common first-two
   kernel, probabilities `0` and `49/625`, the third kernel, and its changed
   unconditioned channel. Determine whether any nonzero difference lies in
   the history-to-channel null space.
4. Derive all five context systems over unrestricted complex Hermitian
   kernels, not a sampled rational grid. Confirm or refute dimensions
   `2,2,1,1,2`, the rich-spectrum cross-moment theorem, and feasibility/PSD
   throughout the claimed families.
5. Stack only the three registered training contexts. Independently derive
   the reduction `5 -> 1`; add the exchange invariance equations and derive or
   refute the unique `M=diag(1/2,1/2)`. Verify that the held-out context was
   absent from the fit and gives a non-vacuous additional equation or state
   clearly if it is algebraically redundant.
6. Rebuild coordinate rephase covariance, channel equality, held-context
   exchange covariance, and the asymmetric calibration control. Separate an
   operator identity from the physical licence to impose it.
7. Reconstruct both calibrated factorizations of the selected kernel and the
   probabilities `1` and `9/25`. Determine the exact residual port/Kraus gauge
   after a retained-port calibration is fixed.
8. Rebuild the nonnormal `C^2 -> C^3` control using the full real-linear
   operator equation. Test whether eigenvalue or normal-operator language
   leaks into the nonnormal claim.
9. Recompute the tangent-support extremality criterion at the endpoint, the
   exchange-fixed singleton, and its rich-context restriction. Audit the
   statement that rank two can be extreme relative to constraints and that
   extremality is not stable under restriction.
10. Independently check PSD certificates, payload seals, 36 mutants, claim
    occurrences, transcript reconciliation, and true off-tree execution.

### Mandatory attacks

- Could distinct positive kernels induce the same unconditioned channel for
  these histories? If so, is `M` a physical ensemble law or a representation
  above a further quotient?
- Does “spectrahedron constructed” mean the full exact feasible set is
  characterized, or only that a standard form and several witnesses are
  exhibited?
- Is the held-out rich context merely a sign-conjugate of a training equation,
  so that “prediction” is weaker than the prose suggests?
- Does the selected point follow only because exchange symmetry explicitly
  sets the remaining diagonal bias to zero? If yes, require this to remain in
  the primary rather than only a qualifier.
- Is the same-`M` port movement invariant under the actually admitted
  calibration and history gauge, or can it be removed by a simultaneous
  relabelling of outcomes and apparatus?
- Are principal-minor checks adequate for every matrix used, including
  boundary points and continuous directions, rather than only the printed
  witnesses?
- Does any generic statement silently pass from fixed history count and fixed
  source/target algebras to changing carriers or catalogues?

## Seat R — records, representation, and recurring-history identity

This seat attacks the physical identification that lets one kernel recur
across contexts and the claim that a calibrated factorization is a record law.

### Mandatory recomputations

1. Reconstruct the two history coordinates in every context from the declared
   relational roles. Decide whether they are configuration-individuated
   histories as required by Papers 3–5, or merely two named operator orders on
   anonymous equal-dimensional spaces.
2. Audit the full recurrence dictionary. For identity, rephase, exchange, and
   asymmetric controls, distinguish boundary gauge, a comparison of the same
   physical fact, and a new law-identification postulate.
3. Enumerate or algebraically classify the admissible alternative recurrence
   maps at this finite arena, modulo the reduced history/boundary gauge. Test
   whether any alternative preserves all declared relational and calibration
   data yet moves the selected prediction.
4. Determine whether intersecting context systems in one coordinate vector is
   genuine recurring-vertex locality, a uniform-coupling postulate, or a
   coordinate identification by fiat. Freeze the correct physical reading of
   `5 -> 1 -> 0`.
5. Audit the exchange licence against the full relational automorphism and
   the retained calibration. Test whether the selected half-half kernel is
   merely the symmetrized answer inserted by this licence.
6. Reconstruct the doctrine-control prediction. The scorer stores the two
   polynomials `[1,0]` and `[0,1]`; derive them from a typed calibrated probe or
   classify that gate as declaration-only. This is a load-bearing attack.
7. Distinguish three levels above one `M`: unobserved Kraus unravellings,
   calibrated instrument outcomes, and durable relational records. Decide
   which level the `1` versus `9/25` witness actually reaches.
8. Rebuild flag overlaps `1,3/5,0`, partial visibility, rich-spectrum failure,
   and the eraser. Apply Paper 3's continuation-stable null/permanence doctrine
   rather than treating one-cut orthogonality as a record theorem.
9. Test port refinement, spectator extension, and catalogue embedding under
   the physical quotient, not only equality of matrices. Ask what happens
   when history number, record partition, or output carrier changes.
10. Reconcile the result with JCV, PPR, SRW, and OVG: identify exactly which
    declaration-relativity debts have been reduced and which have merely moved
    into recurrence, calibration, catalogue, or actualization.

### Mandatory attacks

- A dictionary frozen before solving is auditable, but what non-circular fact
  selects that dictionary from its alternatives?
- Does `SELECTED-MODULO-GAUGE` overstate a result whose own doctrine control
  says inequivalent recurrence choices move physics?
- Is “recurring history” well-defined without one physical successor map or
  catalogue that identifies the event type across the five contexts?
- Are the five fixtures genuinely independent contexts or variants designed
  in a common basis around the same intended kernel?
- If the retained port calibration is external metadata, does the record-fiber
  witness establish ISP record ontology or only apparatus-dependent quantum
  instruments?
- Does an orthogonal flag that can reconverge ever qualify as a durable record?
  What exact future-reachability census is still owed?
- Actualization remains a postulate. Ensure no ensemble or port factorization
  is described as explaining why one outcome happens.

## Seat P — relational locality, no-signalling, and physical ontology

This seat audits the physical meaning of the finite selection, its relation to
EPR, and every refused gravity/QFT promotion.

### Mandatory recomputations and literature checks

1. Rebuild the Bell-state spectator calculation and amplifier contrast.
   Separately prove the general fixed-factor statement for an arbitrary local
   CPTP instrument, or restrict the candidate explicitly to its one registered
   state and channel.
2. Determine what remains open for conditional steering, changing carrier
   factorization, and a changing Bob algebra. Test whether decomposition-
   sensitive recorded ports could signal if remote steering later becomes
   phrasable.
3. Audit whether each three-actor/two-relation context represents a relational
   local event or merely labels an operator pair. No actor count, primitive
   arity, two-to-`n` rule, or field ontology may be inferred from repeated
   three-actor fixtures.
4. Search for a single object in CSF that rewrites relations or geometry and
   changes a later probe through the output carrier. If none exists, classify
   the result as fixed-history quantum-instrument selection, not backreaction.
5. Decide whether the flag is a physically created local cell, a Hilbert-space
   ancilla, or only a mathematical dilation. Record locality and permanence
   require distinct evidence.
6. Audit the operational content of the recurrence and held-out contexts.
   Identify a calibrated observable that differs between surviving doctrines,
   and state whether the stored doctrine polynomials are derived or declared.
7. Consult primary literature on instruments/unravellings, ensemble steering
   and no-signalling, process histories, and semidefinite channel constraints.
   Compare only at matched mathematical type; do not import fixed spacetime as
   an ISP assumption.
8. Audit the Barandes-inspired ontology: complete relational histories and
   their stochastic/process law may be ontic while `M`, `C`, and a Hamiltonian
   are representations. State which object CSF actually selects.
9. Check every sentence concerning fields, Fock structure, particles,
   species, statistics, Hamiltonians, Lorentz invariance, continuum gravity,
   QFT/GR deviations, constants, or an affine term. Require a constructed
   object and invariant calibrated discriminator before any promotion.
10. Assess the empirical status: does any prediction differ from standard
    finite quantum instruments across every surviving doctrine, or has CSF
    only organized the candidate-law space?

### Mandatory attacks

- Is the physical headline really conditional symmetry selection of an
  unconditioned kernel, with the record law and fundamental dynamics still
  free?
- Could the recurrence condition be the desired locality/uniformity principle,
  or is that conclusion circular until the same event type is independently
  derived from the relational bundle?
- Does fixed-Bob unconditioned invariance say anything about EPR steering or
  no-signalling on a back-reacting, locally changing spacetime?
- Is rich-spectrum cross-moment zero merely ensemble decoherence at one cut,
  rather than a fact about actual order, causal structure, or time?
- Does the selected kernel correspond to a physical law of nature, or only to
  the maximally mixed point compatible with an imposed exchange symmetry?
- What new experiment could distinguish the surviving calibrated port laws?
  If none is typed, law selection has not reached phenomenology.

## Required report structure

Every report must contain, in order:

1. immutable-target/hash audit;
2. independent method and tools;
3. exact recomputation table with candidate value, reviewer value, and status;
4. theorem/proof audit;
5. recurrence/gauge or physical-ontology audit appropriate to the seat;
6. counterexamples and unrun controls;
7. consequence/scope reclassification;
8. grade;
9. numbered repairs or kill conditions; and
10. report SHA-256.
