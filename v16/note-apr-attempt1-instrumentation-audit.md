# APR Paper 12 — first official run: integrity-valid, instrumentation-invalid

**Date:** 2026-08-18  
**Status:** FROZEN ATTEMPT-1 AUDIT; CANDIDATE PUBLICATION PROHIBITED  
**Scientific disposition:** PROVISIONAL PRIMARY STABLE; MUTANT/PROVENANCE BATTERY NOT CLOSED

## 1. Frozen object and scope

The result-neutral APR scorer was frozen at v16 ledger #162 before fixture
evaluation.  Its first transactional execution produced:

- `v16/apr_output.txt`, SHA-256
  `54bc80878ae3ef5769337d00e33d9ed174933181e25ac1433a9e2c0ae70c7b88`;
- `v16/apr_receipt.json`, ordinary SHA-256
  `6876d1ff2e844db5101a76b5e230566a1f566bfcbe7fe6cc512442d56106eb33`;
- receipt payload SHA-256
  `eb21acec9a47c57cf5bb289b895d1f9344903af572e2712ffa4f31444881b73c`.

These bytes are retained as the first official attempt.  They are not the
terminal APR artifacts and may not support candidate prose, panel review, or
promotion.  A repaired run must publish to new, absent paths; attempt 1 is
never overwritten or deleted.

## 2. What passed

Artifact integrity is clean.  The payload self-hash recomputes, the transcript
is the exact receipt projection, a clean second run is byte-identical, and a
true off-tree/no-`.git` run from an alien working directory is byte-identical.
All nine generic self-test suites pass.  No frozen input was mutated and no
baseline arithmetic error was found.

Independent exact reconstruction reproduced:

- every question branch cell and Bernoulli control weight;
- the three registered `15`-node, `14`-edge record-tree graph pushouts;
- stable-null rank history `1 -> 2 -> 3`;
- predictive-boundary rank `2 -> 3` after a new future row;
- exterior orbit counts `3 / 2 / 1` for listed, recursive-child, and intrinsic
  replacement closures at `A=[00]`;
- the comparison screens and quotient controls;
- the two compatible `AB/BC` global extensions with identical local
  marginals but `P(A=C)=1/2` versus `1`.

The first-run strict primary is therefore not displaced by this audit:

```text
APR-BLOCKED-AT-BOUNDARY-GLUING
```

The block is independently witnessed by a valid adaptive frontier
`{0,10,11}` absent from the frozen uniform-depth boundary factory; identities
only at `B0`; no tensor-process factory; no nontrivial vertical/horizontal
naturality square; and the unselected `AB/BC` global extension.  These facts
do not depend on any defective mutant below.

## 3. Why attempt 1 is invalid for review

The scorer established registry reachability for all 56 transformation names,
but its first scientific run exposed that several names do not transform the
object named by the protocol or do not recompute the promised semantic
predicate.  Consequently the receipt sentence that all registered
transformations ran semantically is false.  This is instrumentation invalidity,
not a new APR physics result.

### 3.1 Vacuous or false transformations

1. `G2` changes `hf_008`, which participates in neither a registered
   factorization nor the relevant factory.  No composite changes.
2. `G5` changes a question on a zero-supported subbranch.  Every branch cell
   remains identical, and the direct and cut forms are rebuilt from the same
   mutated tree.
3. `M16` deletes declared incidence rows while leaving the actual region
   antichains—and therefore their physical overlaps—unchanged.  The reported
   equalization comes from a signature that does not evaluate the declared
   physical-to-blind projection.
4. `L5` stipulates `intrinsic_closed=false`; it deletes no conjugate and
   recomputes no closure.
5. `G4` invents two unattached restriction maps although the missing
   filling-to-process assignment is exactly the construction under test.
6. `M34` toggles contact labels but neither exchanges the two contact laws nor
   changes a generated response.  No generated contact law exists.
7. `M35` changes no provenance object or edge; its conclusions are literals.

### 3.2 Partial controls presented too broadly

`M13`, `M14`, `M15`, `M23`, `M27`, `M33`, `P3`, `P5`, and `L6` contain valid
local observations or scopes but do not execute all transformations their
registered descriptions require.  `M23` duplicates the standalone recovery
calculation of `M24`; `P7` and `G3` share one no-pushout implementation despite
registering different impostors.  `M28` and `M30` are legitimate only as
`NOT-CONSTRUCTED` scope controls because no physical regional `Tau` exists.
`M21`, `M22`, `M29`, `M31`, `M32`, and `P8` likewise remain scope or
sensitivity controls, not positive transformed-law replays.

### 3.3 Provenance and active-law defects

1. The finite branch controls and graph-cospan controls are neighboring
   checks, not one verified filling-to-process assignment.  Corrupting their
   declared link leaves the alleged functor control unchanged.
2. The law-root digest omits active continuation/readout semantics: adding a
   reset that destroys record recovery or deleting reader schedules can leave
   it unchanged.  It also includes unreachable declarations such as
   `hf_008`, which can move the root without moving the active law.
3. `ProvenanceDAG` does not validate roots and paths; it is an annotation, not
   a one-law ancestry proof.
4. The E-37 scorer does not consume the frozen physical-to-blind projection.
   Equal separately declared signatures do not yet establish a derived blind
   projection.
5. The E-37 blind hash is not graph-isomorphism canonical: a pure relabeling
   of an unchanged regional member can move the encoded edge list and hash.
   Boundary/resource-colored graph canonicalization is required before any
   relabel-invariance or blind-byte claim.
6. Record evidence trusts operation kinds and names rather than the operation
   expressions and schedule ancestry.  Recovery is therefore established
   only for independently reconstructed finite controls, not for every
   declared continuation word.

## 4. Bounded repair rule

The next scorer revision may repair only instrumentation and wording.  It may
not change the pin, fixtures, physical law data, outcome ladder, primary
precedence, or any baseline APR measurement.  In particular it must:

- retarget `G2` to a leg used by a registered factorization and recompute the
  pushout effect;
- mark `G4` and `G5` unavailable unless a genuine whole-versus-step process
  assignment is first frozen in a future unit;
- make `M16` evaluate the actual region relation and physical-to-blind map;
- replace raw node-index hashing by boundary/resource-colored graph
  canonicalization and verify pure relabel invariance;
- delete a real conjugate and recompute closure for `L5`;
- demote `M34` to declaration sensitivity until contact is law-generated;
- instantiate or honestly narrow every partial control listed above;
- derive active-law roots and provenance from reachable semantic ancestry;
- validate reader/tree/schedule semantics rather than identifiers;
- distinguish separate instrument, graph-pushout, tensor, naturality,
  regional-gluing, locality, contact, causality, and E-37 coordinates;
- add semantic branch-port identifier/order metamorphics;
- preserve all unavailable baselines as unavailable.

Any edit that supplies a missing tensor, forest functor, regional gluing rule,
causal schedule, contact response, E-37 `Tau`, or quantum interference law is
new scientific construction and is forbidden under this repair.

## 5. Ontology and claim discipline

Attempt 1 constructs a classical question/record instrument on one fixed
atomless Boolean syntax plus separate graph-cospan and comparison controls.
It does not construct region rewriting, a physical atomless regional image,
causal order, geometry, a metric, backreaction, continuum, GR, QFT, particles,
or actualization.  `locality=FAIL` means the promotion is unconstructed at the
registered gates; it is not an observed violation of physical locality.
`finite_tree_controls=true` is not a functor claim.

The ontology remains: one actual partial compatible web of durable relational
facts is the candidate beable structure; words, trees, matrices, algebras,
instruments, and cospans are representations until a future-complete quotient,
support doctrine, and process law make them dynamically faithful.  Actualization
remains a postulate.
