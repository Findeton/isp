# Paper 12 negative-candidate construction pin

**Date:** 2026-08-19

**Status:** PIN — RESULT-KNOWN RECONSTRUCTION, SCIENTIFICALLY NON-PROMOTING

**Base commit:** `5b7ed3f`

**Binding parent:** `v16/note-apr-one-gamma-paper-review-gate.md`, SHA-256
`06d171a3eea8109e177e2dfa3cb5536fe3785043e676f735c36e91d03834cb51`

This pin authorizes one self-contained negative Paper 12 candidate and its
exact regeneration bundle. It authorizes no new physical law, positive
regional referent, geometry, successor experiment, or change to the frozen
APR artifacts. The APR result is already known; this cycle tests whether the
paper reconstructs that result without laundering the scorer, fixtures, or
synthetic positive controls into scientific evidence.

## 1. Question

> Do the exact finite constructions establish an atomless physical regional
> referent governed by one compositional process, or at what earliest typed
> gate does the construction stop?

The paper succeeds if it proves the earliest honest gate and preserves every
surviving theorem at its actual scope. A negative answer is a scientific
result, not a failed paper.

## 2. Immutable inputs and anchors

The construction authenticates but does not import scientific measurement
functions from:

| role | path | SHA-256 |
|---|---|---|
| ONE-GAMMA gate | `v16/note-apr-one-gamma-paper-review-gate.md` | `06d171a3eea8109e177e2dfa3cb5536fe3785043e676f735c36e91d03834cb51` |
| v5 verification | `v16/note-apr-v5-verification.md` | `d2eae0fdc187317d7ee39c8efaca8fa2a94b6b8f06a3a0524cee0289396a077d` |
| frozen output | `v16/apr_output_v5.txt` | `68374ea18576466ccc40553f8b221360fdfce3fc43d5b555a6eeb0d2827a2f56` |
| frozen receipt | `v16/apr_receipt_v5.json` | `ab9ea941fceebf5b57c7955d483730f3a5f0b317bb5a21da9cc0820331919a61` |
| canonical receipt payload | receipt without `payload_sha256` | `04a1e370c601f9d7e3d5310f9bf20296d7be5c5428010f0f7af6c073d0b438d8` |

The standalone bundle independently reconstructs the following public
mathematical anchors. An anchor mismatch exits 1; the paper outcome is never
read from an anchor.

- zero failures in the registered finite Boolean identities;
- nine displayed proper raw-syntax split certificates;
- exact question normalization by summing, never averaging, both retained
  branches;
- the contextual-volume counterexample `1/2 = 1/2` before context and
  `1/2 != 0` after meeting with the left half;
- a two-element atomic ultrafilter-character quotient;
- the registered complete binary frontiers of sizes `1,2,4,8`;
- one active empty-process identity at the one-port boundary and no assigned
  identities at the `2,4,8`-port boundaries;
- a complete prefix-free adaptive frontier `{0,10,110,111}` not represented
  by the uniform-depth boundary family;
- three finite graph-pushout controls whose whole binary tree has `15`
  vertices and `14` edges, kept separate from any process assignment;
- two complete `ABC` laws with all `AB` and `BC` cells equal to `1/4`, while
  `P(A=C)` is respectively `1/2` and `1`;
- the frozen strict primary word is reproduced only after the independent
  ordered gate classifier runs.

## 3. Term-binding table

| term in this unit | binding | prohibited conflation |
|---|---|---|
| presentation region | finite union of binary prefix cylinders, represented by its reduced prefix-free antichain | a physical region |
| physical region | a class in a faithful contextual/future quotient whose operations and process actions descend | raw syntax, graph node, prefix word, matrix coordinate |
| atomless | every nonzero element of the stated algebra has a proper nonzero subelement | a finite leaf count or syntactic subdivision alone |
| question branch | the subnormalised restriction `nu(E meet C)` or `nu(E meet not-C)` with the zero port retained | posterior renormalisation or outcome selection |
| frontier | a finite prefix-free complete set of record histories | a global instant or ontic time slice |
| structured cospan | a typed open-graph presentation composed by a genuine pushout | a physical process map |
| process | one total assignment from typed fillings to positive maps, preserving identity, composition, tensor, and required naturality | neighboring tree and graph checks |
| division event | a legal conditioning event/time in an indivisible transition law | automatically a durable record or a special configuration species |
| stable record | a same-law written value recoverable after every licensed continuation in a closed future grammar | an appended token |
| `Gamma` | the unique complete nomological transition object required by the ONE-GAMMA gate | the APR question maps, the synthetic v5 law, or a wrapper of modules |

The paper uses no internal unit labels, ledger language, or repository paths in
its scientific prose. Those belong only to this pin, verification note, and
ledger.

## 4. Four-gate audit

### 4.1 Raw regional algebra

- **Referent:** explicitly `SYNTAX`; finite binary prefix antichains.
- **Necessity:** supplies a point-free atomless existence model.
- **No-smuggling:** no response or physical status enters its definition.
- **Discriminator:** an ultrafilter character and contextual-volume test show
  that operational quotients can destroy or misidentify its distinctions.

### 4.2 Candidate physical regional quotient

- **Referent:** complete contextual equivalence under one generated process.
- **Necessity:** raw syntax cannot be promoted to physical ontology.
- **No-smuggling:** equality is computed from generated future behaviour and
  must be a Boolean/gluing/process congruence.
- **Discriminator:** a faithful held-out question separates distinct regions;
  a constant or incomplete profile collapses them.
- **Current status before construction:** not assumed.

### 4.3 Horizontal process

- **Referent:** a total typed filling-to-map assignment.
- **Necessity:** static restriction responses and open graphs are not dynamics.
- **No-smuggling:** identities, composites, tensor, and naturality are
  recomputed from the assignment rather than declared beside it.
- **Discriminator:** the adaptive-frontier and missing-identity controls.
- **Current status before construction:** not assumed.

### 4.4 Ontology candidate

- **Referent:** one actual compatible relational record web is a postulate.
- **Necessity:** distinguishes possible law arguments from actual facts.
- **No-smuggling:** raw words, nodes, flags, cuts, and histories remain
  representations; actualization is not derived.
- **Discriminator:** no physical promotion occurs unless a same-law faithful
  quotient, stable record, and nonconstant support response are constructed.

## 5. Operational reconstruction order

The standalone bundle performs these steps in order.

1. Authenticate the immutable files and canonical receipt hash.
2. Build prefix-antichain Boolean operations independently.
3. Recompute the displayed proper splits and prove the generic cylinder split
   `C(w)=C(w0) join C(w1)`.
4. Construct the volume-only noncongruence and atomic-character controls.
5. Construct exact restriction branches on an arbitrary finite atom basis;
   verify positivity, affinity, retained zero ports, and `Q1+Q0=I`.
6. Build the finite record-tree frontiers, including the adaptive held-out
   frontier, and classify their boundary coverage.
7. Independently form the three registered tagged graph pushouts and check
   boundary-fixing graph isomorphism to the whole tree. Do not attach a
   process map that is not part of the primitive data.
8. Census active empty-process assignments over the active `1,2,4,8`-port
   boundary family.
9. Enumerate both complete `ABC` laws and marginalise them exactly.
10. Run a capability classifier in the frozen precedence order. Every gate is
    returned by a measurement object; no bare `present` mapping is accepted.
11. Render the ontology and scope table from the measured coordinates.
12. Run every registered control, the anchor-failure self-test, strict CLI,
    deterministic double run, and transactional publication tests.

## 6. Pre-registered outcome ladder

The candidate may report only the following inherited primary words, with the
regional-congruence insertion in its inherited position:

```text
APR-INCONSISTENT
APR-BLOCKED-AT-ATOMLESS-REGION-ALGEBRA
APR-BLOCKED-AT-BOUNDARY-GLUING
APR-BLOCKED-AT-TWO-ARROW-TYPING
APR-BLOCKED-AT-FUTURE-PROFILE-COMPLETENESS
APR-BLOCKED-AT-REGIONAL-CONGRUENCE
APR-ATOMLESS-KINEMATICS-CONSTRUCTED-COMPARISON-PRICED
APR-COMPARISON-QUOTIENT-CONSTRUCTED-BUT-DYNAMICAL-LOCALITY-FAILS
APR-DYNAMIC-REGIONAL-REFERENT-CONSTRUCTED-BUT-CAUSAL-ORDER-PRICED
APR-DYNAMIC-ATOMLESS-REGIONAL-REFERENT-CONSTRUCTED-LAW-UNSELECTED
APR-JOINT-POINT-FREE-REGIONAL-LAW-CONSTRUCTED
```

The strict primary is the earliest failed prerequisite. A later finite
positive theorem cannot override an earlier missing construction.

Orthogonal coordinates use only:

- atomlessness: `SYNTAX-ONLY`, `PHYSICAL-QUOTIENT-ATOMLESS`,
  `PHYSICAL-QUOTIENT-ATOMIC`, `UNCONSTRUCTED`;
- process: `SYNTAX-ONLY`, `STATIC-RESPONSE-ONLY`,
  `HORIZONTAL-CLASSICAL`, `HORIZONTAL-QUANTUM`, `INCONSISTENT`;
- ontology role: `STATIC-RESPONSE`, `FIXED-ALGEBRA-CONDITIONING`,
  `RECORD-WRITING-ON-FIXED-ALGEBRA`, `REGION-REWRITING`;
- law selection: `UNSELECTED`, `PRICED`, `SELECTED`, `INCONSISTENT`.

## 7. Registered controls and mutants

Every control changes an upstream object and recomputes the affected result.

1. `RAW-ATOMLESS`: a fresh deep cylinder splits properly.
2. `ATOMIC-CHARACTER`: an ultrafilter character yields a two-element atomic
   image.
3. `VOLUME-NONCONGRUENCE`: equal volumes separate under a meet context.
4. `ZERO-PORT`: deleting the zero branch fails typed completeness.
5. `AVERAGE-BRANCHES`: averaging branches gives one half of the identity.
6. `FRESH-PROBE`: a depth whitelist rejects a valid fresh cylinder and is not
   a complete compiler.
7. `ADAPTIVE-FRONTIER`: the held-out prefix-free cover is absent from the
   uniform-depth factory.
8. `IDENTITY-DOMAIN`: an unassigned identity-like graph and a nonidentity
   replacement do not count as active process identities.
9. `PUSHOUT-NOT-PROCESS`: graph pushout success cannot set a process gate.
10. `CACHED-MARGINAL`: mutate one global `ABC` entry while retaining cached
    pair tables; independent marginalisation detects the stale cache.
11. `ARBITRARY-SELECTOR`: order, entropy, sparsity, or a Markov convention may
    select a completion only as new law data.
12. `SYNTHETIC-LAW-EXCLUSION`: the separate executable-law self-test object
    cannot supply a missing physical APR baseline.
13. `RAW-NODE-ONTOLOGY`: replacing neutral identifiers leaves every physical
    coordinate invariant; treating the identifiers as beables fails.
14. `PRIMARY-PRECEDENCE`: deleting Boolean normalization moves the classifier
    earlier, while supplying a complete abstract gluing package moves it later.
15. `ANCHOR-FAILURE`: corrupting one authenticated immutable byte exits 1
    before scientific evaluation.

## 8. Paper claim contract

The candidate paper must contain, in scientific language:

- the exact raw atomlessness theorem;
- the quotient/noncongruence and atomic-image controls;
- the exact restriction-instrument theorem;
- finite tree and graph-pushout controls, explicitly separate;
- the exact `AB/BC` underdetermination theorem;
- a load-bearing section titled substantially **“Indivisibility was not
  operationalized”**;
- the earliest derived outcome and every surviving narrower theorem;
- an ontology table distinguishing syntax, mathematical control, physical
  construction, representation, priced input, postulate, and absence;
- an explicit statement that the accompanying executable-law verification is
  plumbing only and contributes no physical positive row;
- the complete nonclaim list.

The paper may not mention internal versions, ledger events, pins, scorer
attempts, task names, or review chronology. It cites external literature only.
The verification note, not the paper, records repository provenance.

## 9. Receipt and source discipline

- Exact substantive arithmetic uses `fractions.Fraction` and integers only.
- No random evidence, network access, runtime Git query, CWD dependence,
  tolerance, or float literal is permitted.
- The bundle is standalone and may not import `apr_score.py`,
  `apr_fixtures.py`, or any receipt conclusion.
- `--selftest` uses only synthetic generic primitives and writes nothing.
- `--run --output ABS --receipt ABS` is the only publication mode.
- Missing, relative, existing, aliased, or partially supplied destinations
  refuse before publication.
- Publication is transactional and rolls back an injected second-write
  failure.
- Unknown arguments refuse. Default invocation refuses.
- Two full runs from repository root and a true off-tree/no-`.git` copy must
  be byte-identical.
- The complete run cap is 120 seconds. No individual silent command exceeds
  eight minutes.
- The receipt prints all scopes, primitive payload hashes, capability
  measurements, mutant outcomes, paper-number rows, and the canonical payload
  SHA-256.
- Counts are computed from constructed objects, never typed into result rows.
- The paper's every displayed number and outcome sentence maps to one receipt
  key or an in-paper proof.

## 10. Construction whitelist

The construction worker may create or modify only:

```text
v16/code/apr_paper12_exact.py
v16/code/apr_paper12_output.txt
v16/code/apr_paper12_receipt.json
v16/paper-12-atomless-regions-and-the-missing-gluing-law.md
v16/note-apr-paper12-candidate-verification.md
```

The adjudicator alone may additionally update:

```text
v16/LOG.md
v16/PLAN.md
STATUS.md
```

No existing APR source, fixture, artifact, pin, review, or historical paper may
move.

## 11. Scope walls

Even the strongest available Paper 12 outcome does not construct or select:

- a physical point-free regional quotient unless its complete process and
  congruence gates pass;
- a total horizontal process from finite tree or graph controls;
- a unique global completion from compatible local marginals;
- an indivisible stochastic `Gamma`;
- reciprocal matter–geometry backreaction;
- operational chronology, extensive valuation, volume form, metric,
  connection, curvature, stress response, or gravity;
- continuum, dimension, signature, Lorentz invariance, QFT, particles,
  Hamiltonian, vacuum, constants, or predictions;
- law selection or actualization.

Barandes supplies a fixed-configuration-space indivisible stochastic
architecture and a representational reading of Hilbert space. It does not
derive the proposed relational catalogue or any item in the list above.

## 12. Review and halt

After the candidate and verification note freeze, a separate hostile protocol
must freeze before any reviewer is assigned. Three mutually blind seats then
reconstruct regional/refinement, process/gluing, and ontology/ONE-GAMMA claims
without importing the bundle. Their reports freeze separately and are jointly
adjudicated.

No Gamma-first successor construction begins before Paper 12 is terminal.
Freezing this pin authorizes construction of the whitelisted negative
candidate only.
