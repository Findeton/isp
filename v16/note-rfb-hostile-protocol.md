# RFB Paper 10 hostile-review protocol

**Frozen:** 2026-08-18, v16 ledger #136.

**Status:** PROTOCOL FROZEN — THREE REVIEW SEATS DEFINED, NO REVIEWER ASSIGNED,
NO REVIEW REPORT AUTHORIZED BY THIS NOTE ALONE.

## 1. Immutable candidate

Every seat reviews these exact bytes:

| object | SHA-256 |
|---|---|
| `v16/note-rfb-pin.md` | `acc2cca5b46b4512a3b1298ec45623a621539da83691bebb6c4ab2dc8a431bc2` |
| `v16/code/rfb_core.py` | `7d0a787d108ac16229dc6819a81f74f7b80203eaefa71d28f30f1f31b27e9ada` |
| `v16/code/rfb_fixture.json` | `f3557b3400584d01984c6a4f38d40744c9e2cf2f0e36c7c4aa225b045e5bd362` |
| `v16/code/rfb_score.py` | `4a2c9590e1d64f9e40c6e5828d132b9b746aa8938e18aee95a484caabc8c87ff` |
| `v16/code/rfb_output.txt` | `a7ebef77a507bac62c64fa594ed539902ed7a333fe5c77a677a4aad1f124aace` |
| `v16/code/rfb_receipt.json` | `e8cefe5b28d343fe76fdad89283a02c8c2f477243bbc7455b83324a0fe7a4659` |
| `v16/paper-10-record-feedback-boundary.md` | `360b4e861add1a5eac0b09296fe0e52438787697aa22097801f0e43cc2c2a5f4` |
| `v16/note-rfb-candidate-verification.md` | `9dc606172278d7595531603e4a3bc272d227cf665f297fc4ed16ea1ae3178531` |

Candidate, fixture, core, scorer, and sibling reports are immutable. Seats may
write only their own report. They are mutually blind until all three reports
are frozen. No seat may read an external scratchpad or any untracked SCOUT-T
file.

Each report must state `ACCEPT`, `ACCEPT-WITH-FIXES`, or `REJECT`; recommend a
primary; separate exact mathematical survival from physical interpretation;
list every scope wall; include hashes; independently rerun or rebuild the
load-bearing claims; and attempt every mandatory kill below.

## 2. Seat O — operator, histories, and law-type classification

Required independent work:

1. Rebuild the writer counts, phase quotient, additive readers, charge orbits,
   two-history functionals, port completeness, classical instrument, coherent
   dilation, hybrid Gram family, and fixed-factor channels without calling the
   scorer's measurement functions.
2. Replace the finite `Z_q` edge-phase subgroup by arbitrary `U(1)` phase
   variables. Decide exactly which writer theorem survives, whether the
   residual is a continuous theta angle, and whether the paper's finite claim
   is correctly scoped.
3. Attack the history law-type inference. The scorer marks an enriched-state
   kernel as surviving because two final distributions normalize, and an
   indivisible law as surviving because a two-history functional is positive
   and glues at one cut. Construct the actual processes and their second-cut
   composition, or demote both to boundary-compatible candidates.
4. Decide whether `DIVISION-KERNEL-SUFFICIENT` is established at every
   operational boundary in the fixture or only at one selected final port.
5. Test whether a decoherence functional is required, merely available, or
   representationally equivalent to the Hilbert construction at this arena.
6. Refuse the hard-coded `higher_order_required=false` unless a definite-order
   process witness actually excludes the need for a higher-order object.
7. Audit the semialgebraic dimension claim over the declared exact field and
   over the real/complex closure separately.

Mandatory kill conditions:

- an exclusive intermediate kernel that retains the registered phase without
  enlarging its state kills the stated exclusion;
- failure to construct either alleged surviving microscopic process kills the
  `METHOD-INCONCLUSIVE` comparison as delivered and narrows it to
  `UNENTERED-AT-PROCESS-LAW`;
- a nonpositive hybrid row, incomplete port, or failed second-cut identity
  kills the corresponding headline; and
- a continuous-phase counterexample moving a claimed finite classification
  forces an explicit subgroup qualifier.

## 3. Seat R — records, ontology, predictive state, and actualization

Required independent work:

1. Audit every ontology noun at the Barandes level: actual configuration,
   actual history, stochastic law, division event, durable record,
   wavefunction/Hilbert representation, and actualization. Identify which are
   measured, compatible candidates, representations, or postulates.
2. Test whether the classical feedback instrument, coherent dilation, and
   hybrid MZI tag are three implementations of one typed event law. In the
   scorer they are neighboring constructions; a common port name is not a
   weld. Delete or exchange each construction and measure which paper claims
   move.
3. Test whether the hybrid tag is a record before the final division or only a
   proto-record/environment degree of freedom. Nonorthogonal tags may not be
   called readable records without a recovery map.
4. Rebuild predictive quotients from three separately implemented dynamics.
   The scorer currently assigns every mode a copy of one interface census.
   Treat that as an E-36 hard-coded-prediction suspect, not as evidence of
   equal predictive complexity.
5. Join each record implementation to the recoverability grammar. The current
   append/reset/copy test is separate from the mode dynamics and cannot by
   itself license durability of those mode records.
6. Attack `PRICED-POSTULATE-1`: distinguish one shared rule from one continuous
   parameter and from one remaining function. A count of declarations is not
   automatically a dimension.
7. Preserve the actualization wall. No compatibility of final instruments may
   be described as explaining why one outcome occurs.

Mandatory kill conditions:

- if anonymous ancilla, classical branch label, and relationally attached
  record give all registered observations, kill any ontological promotion;
- if mode-specific dynamics yield different predictive quotients, kill the
  record-mode nonselection argument as delivered;
- if a licensed continuation erases a claimed final record, kill permanence;
  and
- if the three mode rows cannot be represented in one event algebra, demote
  the family headline to three neighboring existence controls.

## 4. Seat P — locality, causality, physical content, and forward theory

Required independent work:

1. Rebuild the Bell/HJW and local-channel rows. Distinguish invariance of Bob's
   unconditioned marginal from steering, conditional signalling, spacetime
   separation, and changing-factorization no-signalling.
2. Test the feedback families, not just generic Alice-local channels, under the
   HJW preparations. If no composite feedback process exists, state
   `UNPHRASABLE`, not `SAFE`.
3. Audit the chirality discriminator. Determine whether its biased phase
   reference is an operational calibration, a gauge choice, or a typed literal
   unconnected to the feedback dynamics.
4. Attack the “forcing boundary mapped” primary. The full matrix defaults every
   unassayed cell to `UNTOUCHED`; the eleven named cells are the only measured
   part. Decide whether the primary must narrow to
   `RFB-LOCAL-FORCING-CELLS-MAPPED` or remain as frozen.
5. Attack outcome reachability and primary independence. A synthetic Boolean
   truth table is not physical feasibility, and two functions of the same flags
   are not an independent scientific reconstruction.
6. Enforce the no-geometry wall. There is no graph, causal support, distance,
   volume, carrier growth, or curvature. Nothing in RFB is quantum gravity.
7. Audit all literature and forward statements: existing quantum-network
   kinematics, indivisible stochastic processes, quantum histories, process
   tensors, and process matrices are precedents, not results of this fixture.

Mandatory kill conditions:

- any remote-setting dependence at a calibrated Bob record kills a claimed
  no-signalling family at that composite scope;
- a graph-free or memory-only replacement preserving every result kills any
  relational or geometric reading;
- a physically unentered cell counted as “mapped” forces primary narrowing;
  and
- any QFT, GR, particle, Hamiltonian, continuum, or empirical inference is a
  major scope violation.

## 5. Shared integrity and final report schema

Every seat must run:

- clean worktree and true off-tree/no-`.git` replay;
- all thirty-nine frozen mutants plus at least three seat-specific mutants;
- source/fixture/artifact hash verification;
- exactness and no-overwrite checks;
- receipt/transcript/paper reconciliation; and
- a changed-object test for each new falsifier.

Each report ends with:

```text
grade:
recommended primary:
exact claims surviving:
claims killed or narrowed:
new counterexample hashes:
scope walls:
ordered repairs:
```

The adjudicator must wait for all three frozen reports. Majority vote does not
replace argument. The strictest jointly supported type and scope win; any
scientific repair requires a new candidate generation and full replay.
