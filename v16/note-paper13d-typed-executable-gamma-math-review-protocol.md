# Paper 13D mathematical hostile-review protocol

Date: 2026-08-20

Status: **FROZEN PROTOCOL / RESULT UNKNOWN / NO IMPLEMENTATION**

## 1. Immutable corpus

Review only these mathematical bytes:

| role | path | SHA-256 |
|---|---|---|
| physics pin | `v16/note-paper13d-typed-executable-gamma-physics-pin.md` | `722dc3bfe528fc3a52f2d2f5afcaba2c7858250e63d24439e43d0a17ab5c049e` |
| candidate law | `v16/paper-13d-typed-executable-gamma.md` | `3b91766fe269e8145766f4622f99e9009c3270530670119735e6045a473de5c9` |
| construction note | `v16/note-paper13d-typed-executable-gamma-mathematical-construction.md` | `93f1e84444d60365fe729d73058dae1f3dede63a87bf1663d4865511255b50bb` |

Binding commits:

- Stage-A pin: `a20c52a`
- Stage-B mathematics: `efe9d97`
- terminal parent adjudication: `7377b53`

Each reviewer must authenticate all three file hashes and read the pin and
candidate completely before adjudicating. The construction note may orient
navigation but is not proof. No implementation exists and none may be
created, imagined as authoritative, or used to excuse a mathematical gap.

## 2. Independence and conduct

Three reports are mutually blind during construction:

1. category/groupoid/referent;
2. probability/division/record; and
3. physics/ontology/intervention.

Reviewers may inspect the terminal Paper 13C adjudication only to understand
the inherited failures. They may not inspect sibling Paper 13D reports until
all three are frozen. Each seat writes only its assigned report path and does
not stage or commit it.

Every seat must:

- reconstruct its assigned mathematics without software;
- test every assigned registered attack;
- invent at least four additional semantic countermodels;
- report the first decisive counterexample, if any;
- report the full product vector even after a kill;
- distinguish a semantic defect from a later code-only defect; and
- preserve every surviving coordinate rather than issuing an undifferentiated
  verdict.

No reviewer may repair the law. A changed probability, boundary field,
generator, hom-set, group action, mark constructor, reader rule, fusion rule,
future grammar, eraser, or parameter is a new mathematical candidate and
requires user authority.

## 3. Verdicts and stopping rule

Allowed verdicts are:

```text
ACCEPT
ACCEPT-WITH-SCOPE
REJECT
INTEGRITY-BLOCK
```

Use `REJECT` only for a reproducible semantic counterexample affecting at
least one product coordinate. Typographical or explanatory repairs that leave
the mathematical object unique may be listed as bounded scope fixes but may
not silently alter the frozen bytes.

Any hash mismatch is `INTEGRITY-BLOCK`. Python ownership, Rust lifetimes,
serialization, CLI, transaction, memory, and performance are outside this
protocol and cannot cause `REJECT`.

## 4. Bound product vector

Each report returns one value for every coordinate:

```text
referent:
  P13D-EXECUTABLE-GAMMA-REFERENT-UNCONSTRUCTED
  P13D-POINT-FREE-EXECUTABLE-GAMMA-CONSTRUCTED

law:
  P13D-LAW-UNSELECTED
  P13D-ONE-TYPED-EXECUTABLE-GAMMA-CONSTRUCTED

experiment:
  P13D-EXPERIMENT-CATEGORY-UNDEFINED
  P13D-TYPED-EXPERIMENT-CATEGORY-CONSTRUCTED

nondivision:
  P13D-NATIVE-CUT-UNPROVEN
  P13D-NATIVE-B1-CUT-NONDIVISIBLE

record:
  P13D-STABLE-RECORD-UNCONSTRUCTED
  P13D-TYPED-STABLE-FUTURE-CATEGORY-CONSTRUCTED

eraser:
  P13D-ERASER-UNCONSTRUCTED
  P13D-EXECUTABLE-ERASER-CONTROL-CONSTRUCTED

division:
  P13D-DIVISION-UNCONSTRUCTED
  P13D-COMPLETE-DIVISION-FRONTIERS-CONSTRUCTED

size:
  P13D-FIXED-SIZE-ONLY
  P13D-VARYING-SIZE-COVARIANT-FAMILY-CONSTRUCTED

response:
  P13D-RECIPROCAL-RESPONSE-UNCONSTRUCTED
  P13D-RECIPROCAL-RELATIONAL-RESPONSE-CONSTRUCTED

actuality:
  P13D-ACTUALIZATION-UNCONSTRUCTED
  P13D-ACTUALIZATION-POSTULATED
  P13D-ACTUALIZATION-DERIVED
```

Constructing one candidate law does not select it as the fundamental law.
The `law` coordinate means the executable candidate is mathematically
defined, not uniquely derived.

## 5. Seat C — category, groupoid, readers, tensor, and fusion

Assigned report:

`v16/review-paper13d-typed-executable-gamma-category.md`

### C1–C18 registered attacks

1. Reconstruct every $\mathsf{Ctrl}$ hom-set and all possible compositions.
   Check units and associativity for every object triple.
2. Attempt mediator-write then source-write composition. It must meet an empty
   hom-set, not normalize to a valid pair.
3. Attempt two different typed words with the same $(J_0,J_1)$ normal form.
   They may agree only by the declared override/category equations.
4. Check that the write-free stage arrows do not become physical boundaries,
   history rows, divisions, or chronology.
5. Reconstruct $\mathsf{Exec}_D$ hom-sets as typed string diagrams. Test
   identity, associator, braiding, and boundary mismatch.
6. Move an intervention vertex under tensor association and braiding. Its
   intrinsic string-diagram address must transport without a loop index.
7. Verify every boundary invariant under occurrence bijection and local
   $X/Y$ exchange, including $r=m$, $t=h$, $d=e'$, and bonds.
8. Change only a diagnostic reader. The physical stabilizer, history cells,
   and masses must stay fixed.
9. Read a coordinate not fixed by the stabilizer. A naked representative
   must fail; the exact orbit-profile must be well defined.
10. Anchor a singleton probe landmark and verify that an oriented reader is
    then well defined without placing the reader in the stabilizer.
11. Change intervention alternatives so accidental automorphisms differ.
    The ordered contrast and intersection stabilizer must remain exact and
    presentation independent.
12. Recompute representative mass versus stabilizer-orbit mass on a symmetric
    two-occurrence example, including a fixed point.
13. Verify tensor objects, tensor arrows, product readers, symmetry, and the
    empty unit are inside the declared domain.
14. Verify simultaneous fusion on zero, one, two, nested, and permuted
    component families. No fold order may enter one n-ary generator.
15. Distinguish a staged physical fusion trace from a simultaneous fusion
    without declaring either presentation gauge.
16. Verify tensor and fusion groupoid actions on component tags, target
    occurrence sets, marks, and cross-pair endpoints.
17. Verify deletion commutes with relabelling, addresses, and a single n-ary
    fusion after empty components are removed.
18. Attempt unlisted fields, string addresses, reader names, and serialization
    positions. Every one must be outside the exact constructors.

The seat must add at least four fresh category/groupoid countermodels,
including one with nontrivial automorphism and one with tensor/fusion nesting.

## 6. Seat P — probability, global evaluation, cuts, records, and erasure

Assigned report:

`v16/review-paper13d-typed-executable-gamma-probability.md`

### P1–P22 registered attacks

1. Recompute $R,B,C,B^2$, $\beta$, and $\kappa$ exactly.
2. Verify every atomic generator is normalized on every legal source value,
   including the empty occurrence set.
3. Reconstruct the trace-valued composition rule, not only endpoint kernels.
4. Verify direct seed expansion equals categorical convolution for both
   queried paths on all carried fields and bonds.
5. Check source-cylinder normalization and refusal of an empty cylinder.
6. Verify tensor product normalization and fusion cross-pair normalization.
7. Recompute orbit pushforward normalization with automorphism multiplicity;
   make representative mass fail on a nontrivial orbit.
8. Recompute $CB^{-1}$ with the paper's column convention.
9. Prove the all-n tensor candidate is unique and has a negative entry for
   every nonempty finite set.
10. Test enlarged $(m,q_0)$, identity cuts, and the empty process as mandatory
    nonkills.
11. Attempt to smuggle $q_0$, a seed, source history, control phase, or cache
    through the native carrier.
12. Reconstruct full-field future sufficiency at $B_1^0$ and $B_1^r$.
13. Find two equal-record pasts with different omitted $h$ or relational
    packet and verify record-only future insufficiency.
14. Verify $R_c$ carries projectors despite stochasticity of other fields.
15. Verify projector transport for every future generator, tensor, fusion,
    and arbitrary composition.
16. Exhibit a reachable positive-mass cross-sector pair identified by $E_r$.
    Distinguish perfect record erasure from possible statistical inference via
    retained correlated fields.
17. Confirm the eraser is normalized in Exec and absent from Fut.
18. Reconstruct uniform deletion and order-independent finite extension at the
    seed and output levels.
19. Recompute common-cause, mediation, cancellation, reversal, spectator, and
    both reciprocal response rows.
20. Verify each contrast measure is normalized, its signed total is zero, and
    a coarse-reader zero cannot remove the complete comparison measure.
21. Attempt a static chain-rule refactorization with unchanged observational
    mass. It must define a different executable function if response moves.
22. Test zero-probability, support-only, and selected-coordinate shortcuts.
    None may replace an all-input theorem.

The seat must add at least four fresh probability countermodels, including an
alternative-dependent stabilizer and a correlated retained-field erasure
control.

## 7. Seat O — physics, intervention identity, record meaning, and walls

Assigned report:

`v16/review-paper13d-typed-executable-gamma-physics.md`

### O1–O22 registered attacks

1. Decide whether the full executable map is primitive law data rather than a
   factorization of one observational joint law.
2. Attempt conditioning/intervention substitution on the common-cause row.
3. Determine whether the stage-typed control order is only operational syntax
   or has silently become physical time.
4. Determine whether intervention vertices, landmarks, or readers can create
   response from metadata alone.
5. Check that point-free experiment identity preserves the physical relation
   between manipulation and result rather than deleting it with labels.
6. Check that unanchored oriented probes are refused rather than chosen by
   presentation.
7. Interpret independent tensor and physical fusion separately; test whether
   either imports a fixed background or global clock.
8. Test n-ary fusion against a hidden binary serialization and distinguish a
   genuinely staged physical program.
9. Verify every future generator is physically and mathematically typed.
10. Check that later $t^+$ mutability does not relax or overwrite the earlier
    $t=h$ fact.
11. Check that record-label swaps preserve distinguishability without being
    mislabeled erasure.
12. Check that $E_r$ destroys future-boundary perfect readability without
    claiming to erase the recorded past.
13. Reconstruct all four stable-record/complete-division combinations.
14. Test a local stable record mistaken for a complete restart frontier.
15. Check that native nondivision is only relative to the frozen carrier and
    does not imply incomplete configuration reality or absolute memory.
16. Check that varying cardinality is not dormant fixed memory, growth time,
    event count, volume, or duration.
17. Check that response is relational sensitivity, not causal influence,
    chronology, geometry response, energy transfer, backreaction, or gravity.
18. Use the complete reader to expose a coarse-reader cancellation.
19. Attempt to derive actualization from normalization, records, or response.
20. Search for any post-hoc dependence on a target outcome, dimension,
    geometry, Paper 14–17 result, or desired spacetime behavior.
21. Attack every permanent wall: chronology, dimension, signature, topology,
    measure, time, metric, curvature, gravity, continuum, QFT, and particles.
22. State the strongest honest physical interpretation of each surviving
    coordinate without promotional language.

The seat must add at least four fresh physics/ontology countermodels,
including one stable-but-incomplete frontier and one response-without-causal-
direction example.

## 8. Mandatory report structure

Each report must contain, in order:

1. corpus authentication;
2. verdict and first decisive counterexample;
3. independent reconstruction summary;
4. every assigned registered attack with PASS/FAIL and evidence;
5. at least four fresh countermodels;
6. the full ten-coordinate product vector;
7. explicit semantic-versus-code classification;
8. scope and ontology walls;
9. any bounded wording repairs that leave the law unique; and
10. ordinary SHA-256, LF line count, and byte count.

Reports are evidence, not terminal adjudication. Even three accepts do not
authorize Rust until a separate adjudication binds the unchanged mathematical
bytes.

## 9. Stage stop

Freeze this protocol before dispatch. Then run the three independent seats in
parallel. Do not create Rust, Python, fixtures, generated cases, receipts,
official artifacts, or a Paper 13E successor.
