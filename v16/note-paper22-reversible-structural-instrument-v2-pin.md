# Paper 22 v2 homogeneous-source structural instrument pin

Date: 2026-08-21

Status: **FROZEN BEFORE CONSTRUCTION — RESULT NEUTRAL**

This is one bounded semantic replacement for the rejected totality claims of
Paper 22. It changes only the source and functor domains. It does not retune
the local instrument, add a selector, or reopen Paper 13D.

## 1. Question

Can the locally valid Paper 22 reversible query-and-commit instrument be made
exactly total and point-free by restricting its positive source to families
whose active components share one Paper 13D atomic boundary sort, while:

1. leaving every local amplitude and probability unchanged;
2. refusing heterogeneous active families before fusion is evaluated;
3. treating zero- and one-active restrictions branchwise rather than falsely
   identifying tensor and fusion targets;
4. restricting disjoint composition to external tensor products of already
   typed instruments; and
5. keeping the dilation fiberwise in its classical source?

The target is a typed *triggered local instrument*. It is not an occurrence,
activity, root, cosmological-state, chronology, dimension, or geometry law.

## 2. Bound evidence

| artifact | role | ordinary SHA-256 |
|---|---|---|
| `v16/paper-13d-typed-executable-gamma.md` | accepted atomic sorts, tensor, simultaneous fusion, and conditional child laws | `3b91766fe269e8145766f4622f99e9009c3270530670119735e6045a473de5c9` |
| `v16/note-paper22-reversible-structural-instrument-pin.md` | predecessor frozen question and immutable local numerical anchors | `de32c02ee1be613eef4a867dadf9bc1c84fc8ed492b764f7545bb54fb91a5ae4` |
| `v16/paper-22-reversible-structural-instrument.md` | rejected predecessor construction; local fixed-source proofs may be reused only after retyping | `6d75a072fb3c51c5c267448fd329895f94cd4f9ee4ba4d96ea9660be80c1c6b7` |
| `v16/note-paper22-reversible-structural-instrument-adjudication.md` | binding semantic diagnosis and smallest-replacement boundary | `c261520aa142bf07a489f87cd0364628f094794c7523c03d8ba3dde05d824a07` |

The three blind predecessor reports remain evidence through the adjudication;
they are not silently overwritten. Paper 22 v2 must cite the adjudicated
failure and prove that its counterexample is refused.

## 3. Immutable scientific core

The following objects and values are frozen. A change to any of them is not a
Paper 22 v2 repair and requires a separately authorized scientific unit.

### 3.1 Mode law and reversible lift

\[
 B=\frac1{25}
 \begin{pmatrix}9&16\\16&9\end{pmatrix},
 \qquad
 R=\begin{pmatrix}3/5&-4/5\\4/5&3/5\end{pmatrix}.
\]

The same gauge statement is retained: `R` is unique only up to diagonal
input phases, diagonal output phases, and simultaneous exchange of the two
physically typed modes.

### 3.2 Phase control and probe law

\[
 D_\phi=\operatorname{diag}(1,e^{i\phi}),
 \qquad
 C_\phi=|R D_\phi R|^2
 =\frac1{625}
 \begin{pmatrix}
 337-288\cos\phi&288(1+\cos\phi)\\
 288(1+\cos\phi)&337-288\cos\phi
 \end{pmatrix}.
\]

For tensor input and neutral phase,

\[
 \Pr(T)=\frac{49}{625},
 \qquad
 \Pr(F)=\frac{576}{625}.
\]

The stable-record law remains

\[
 B^2=\frac1{625}
 \begin{pmatrix}337&288\\288&337\end{pmatrix},
\]

and the unique continuation through the proposed two-state cut remains

\[
 K_\phi=\frac1{175}
 \begin{pmatrix}
 63+288\cos\phi&112-288\cos\phi\\
 112-288\cos\phi&63+288\cos\phi
 \end{pmatrix},
\]

positive exactly when

\[
 -\frac7{32}\leq\cos\phi\leq\frac7{18}.
\]

### 3.3 Fixed local architecture

The following predecessor results may be retained only on the corrected
domain:

- finite and pure-output no-hiding;
- seed purification;
- reversible tensor and fusion witness queries;
- exact uncomputation before recombination;
- fused-witness phase kickback;
- the `C_phi` filter, record, erasure, visibility, and nondivision family;
- outcome-indexed commit;
- exact conditional recovery of the accepted Paper 13D tensor or simultaneous
  fusion child law;
- absence of an accessible dormant unchosen child; and
- simultaneous fusion remaining distinct from every staged fusion trace.

No new coefficient, phase, seed bias, generator weight, coupling, dimension,
or geometry may be fitted in Paper 22 v2.

## 4. Corrected positive source groupoid

Let `Sort_13D` be the set of accepted Paper 13D atomic boundary sorts. A sort
includes every stage and record qualifier that Paper 13D treats as part of a
boundary type.

For each `s in Sort_13D`, define an object of
`Src_s^{>=2}` to be

\[
 X=\bigl(s,\{X_\alpha\}_{\alpha\in A},
          \{X_\epsilon\}_{\epsilon\in E},A\bigr),
\]

where:

1. `A` is a finite physically marked family with at least two nonempty
   components;
2. every active value has the same atomic sort,
   \(X_\alpha\in B_s(I_\alpha)\);
3. the active occurrence sets are pairwise disjoint;
4. every `X_alpha` is a complete legal Paper 13D boundary value;
5. each spectator is a complete separately typed Paper 13D boundary value,
   but spectator sorts need not equal `s`; and
6. no component order, serialization order, loop index, or history identifier
   is a field of the object.

The positive source is the coproduct

\[
 \mathsf{Src}^{\ge2}_{\rm hom}
 =\coprod_{s\in\mathsf{Sort}_{13D}}\mathsf{Src}^{\ge2}_s.
\tag{P22v2.1}
\]

A source morphism consists only of accepted presentation maps, component
bijections, occurrence bijections, and spectator transports that preserve:

- the active atomic sort `s`;
- the active/spectator mark;
- every complete boundary field; and
- the unordered active-family incidence.

There is no morphism between different coproduct summands. In particular, a
presentation map cannot repair a heterogeneous active family by renaming its
sort.

Write

\[
 \underline X=
 \left(\boxtimes_{\alpha\in A}X_\alpha\right)
 \boxtimes
 \left(\boxtimes_{\epsilon\in E}X_\epsilon\right)
\]

for the underlying formal Paper 13D tensor boundary value. The active mark and
the chosen decomposition belong to the presented experiment frame; they do
not alter the fields of `underline X` or create a new atomic boundary sort.

### 4.1 Four gates for the corrected source

**Referent.** Every field is inherited from a complete Paper 13D boundary or
from the already operational active/spectator mark.

**Necessity.** The predecessor admitted
`B_1^0({i})` and `B_2^0({j})` as one active pair even though Paper 13D has no
fusion arrow for different atomic sorts. Equation (P22v2.1) must reject that
pair before any child, query, or probability is evaluated.

**No smuggling.** Membership uses only source-side types and values. It may
not inspect a proposed child, a fusion outcome, a mode result, a probability,
or any downstream dimension or geometry estimator.

**Discriminator.** The construction must exhibit at least:

1. one legal homogeneous pair accepted;
2. the exact heterogeneous pair above refused;
3. a sort-changing purported morphism refused; and
4. a spectator of another sort accepted without changing the active sort.

## 5. Corrected child experiment functors

For `X in Src_s^{>=2}`, first define the tensor target type

\[
 \mathsf Y_{T,s,X}=
 \left(\boxtimes_{\alpha\in A}B_s(I_\alpha)\right)
 \boxtimes
 \left(\boxtimes_{\epsilon\in E}B_{s_\epsilon}(I_\epsilon)\right),
\tag{P22v2.2}
\]

and the fusion target type

\[
 \mathsf Y_{F,s,X}=
 B_s\!\left(\bigsqcup_{\alpha\in A}I_\alpha\right)
 \boxtimes
 \left(\boxtimes_{\epsilon\in E}B_{s_\epsilon}(I_\epsilon)\right).
\tag{P22v2.3}
\]

The corresponding child experiment functors are

\[
 \mathcal T_s(X)=
 \bigl(\underline X,\operatorname{id}_{\underline X},
       \mathsf Y_{T,s,X},\Gamma_{T,s,X}\bigr)
\]

and

\[
 \mathcal F_s(X)=
 \left(
 \underline X,
 \Phi_s^{\{I_\alpha\}_{\alpha\in A}}
 \boxtimes\operatorname{id}_{E},
 \mathsf Y_{F,s,X},\Gamma_{F,s,X}
 \right).
\tag{P22v2.4}
\]

The identity notation in `mathcal T_s` means the accepted tensor history on
the formal tensor source, not identification of the formal tensor with an
atomic boundary. `Phi_s` is exactly the accepted single simultaneous n-ary
Paper 13D fusion generator. At a bond-carrying sort it is a stochastic arrow:
it does not assign one deterministic target value. A realized child is a
target value together with its complete history sampled by the accepted
conditional kernel. No binary fold or auxiliary bracket becomes physical.

Thus “functor” means a natural assignment into the category of typed
experiments and stochastic kernels, not an ordinary point map that erases the
random fusion history.

The paper must type and prove naturality, on every coproduct summand, for:

- `mathcal T_s` and `mathcal F_s`;
- the tensor and fusion witness computations;
- the fused-witness projector and phase kickback;
- the query inverse;
- the outcome-indexed commit;
- every accepted child future; and
- the exact conditional child laws.

The totality theorem must quantify only over
`Src_hom^{>=2}`. It must not claim a partial evaluator is total by assigning
zero probability to an undefined child.

### 5.1 Four gates for the child pair

**Referent.** `mathcal T_s` and `mathcal F_s` are built from Paper 13D tensor,
its simultaneous fusion generator, its accepted kernels, and carried
spectators.

**Necessity.** They are the two operational alternatives interrogated by the
instrument.

**No smuggling.** Their construction is fixed before the mode is sampled and
does not use the mode probabilities.

**Discriminator.** On every positive source, the tensor child retains the
active partition and has no cross-active bond address, while the fusion child
drops that partition and has the accepted positive cross-active bond law.

## 6. Fiberwise reversible query and commit

All coherent equations are indexed by one fixed classical source
`X in Src_s^{>=2}`. The query is a controlled isometry/unitary on the fiber
over `X`; it is not an amplitude over different source objects.

The outcome-indexed isometry must use source-indexed exhaust labels:

\[
 W_X|m,X,0_Y,0_C,0_R\rangle
 =|m_R\rangle
  \sum_{[H]}
  \sqrt{\Gamma_m([H]\mid[X])}
  |H\rangle_Y|c_{[X],m,[H]}\rangle_C.
\tag{P22v2.5}
\]

The source index prevents a future external direct sum from identifying
private exhaust vectors belonging to different classical sources. Paper 22
v2 nevertheless claims only the family `X -> W_X`; it does not introduce a
coherent superposition, probability law, or root measure over source objects.

The accessible joint law remains exactly

\[
 \widehat\Gamma_{\phi,X}(m,[H]\mid j)
 =C_\phi(m\mid j)\Gamma_m([H]\mid[X]).
\tag{P22v2.6}
\]

Equation (P22v2.6) is derived fiberwise from the mode evolution and the
selected commit block. It is not an autonomous selector factorization.

### 6.1 Four gates for the fiberwise dilation

**Referent.** Its source, mode, child histories, record, and exhaust are all
typed apparatus or Paper 13D objects.

**Necessity.** The no-hiding theorem requires a complement for reversible
query, and measurement dilation requires an exhaust for commit.

**No smuggling.** Source indexing distinguishes already supplied classical
sources; it assigns no amplitudes or probabilities among them.

**Discriminator.** Distinct source fibers must have orthogonal exhaust labels
in an optional external direct sum, while the reduced accessible child law on
each source remains equation (P22v2.6).

## 7. Restriction and degenerate branches

Let a covariant occurrence restriction act on a positive source and its
histories. There are two cases.

### 7.1 Positive-to-positive restriction

If at least two nonempty active components of the same sort remain, the
restricted source lies in the same positive groupoid. The existing seed
marginalization and naturality argument must be reproved on this domain.

### 7.2 Restriction leaving zero or one active component

If fewer than two nonempty active components remain, the result leaves
`Src_hom^{>=2}`. Paper 22 v2 must not claim that the tensor and fusion target
objects merge.

Instead, define a typed branchwise restricted-output object

\[
 \mathsf{Res}_{J}(X,m,H)
 =\bigl(m_R,\operatorname{res}_{J}\mathsf Y_{m,X},
          \operatorname{res}_{J}H\bigr),
 \qquad m\in\{T,F\},
\tag{P22v2.7}
\]

as a dependent sum over the retained stable apparatus record `m_R`.

The tensor restriction may land in the formal monoidal unit or a formal
one-factor tensor. The fusion restriction may land in the corresponding
atomic Paper 13D boundary. These targets remain distinct typed objects unless
an independent future theorem supplies an exact alignment. No such theorem
is asserted here.

Consequences:

1. restriction of an already committed history is defined branchwise;
2. no new structural opportunity is triggered at the degenerate target;
3. no new `C_phi` law is assigned to that target;
4. no tensor/fusion predictive equivalence is claimed; and
5. deleting or erasing the apparatus record is a separate physical operation,
   not a quotient imposed by restriction.

The inherited marginal weight of a restricted realized branch is still the
parent instrument's `C_phi(m|j)`. What is forbidden is reinterpreting that
pushforward weight as a newly triggered mode law intrinsic to the degenerate
target.

### 7.3 Four gates for branchwise restriction

**Referent.** It is the ordinary covariant restriction of an already realized
typed child history together with its retained stable record.

**Necessity.** Restrictions can remove all but zero or one active component,
but the two formal child targets need not coincide.

**No smuggling.** It introduces neither a new opportunity nor a new mode law;
it only transports existing branch data.

**Discriminator.** The paper must separately exhibit zero-active and
one-active restrictions and verify that their tensor and fusion target types
are retained rather than conflated.

## 8. Composition boundary

Paper 22 v2 permits only the external tensor product of two separately typed
and separately triggered instruments on disjoint source objects:

\[
 \mathcal I_{X_1}\boxtimes\mathcal I_{X_2}.
\tag{P22v2.8}
\]

The product has two independent active marks, two mode carriers, two records,
and two exhausts. Symmetric braiding proves order-independence of the formal
external tensor factors.

Paper 22 v2 introduces no source with multiple internal active marks. It
therefore makes no claim that two marks inside one larger source commute, and
no implementation loop order is promoted to physical time.

Simultaneous n-ary fusion remains one Paper 13D generator. Staged fusion
remains a distinct history with additional traversed boundaries. No
associator, fusion coefficient, fusion ring, or Frobenius--Perron probability
is earned.

## 9. Required theorems

The construction must prove, in this order:

1. **Domain decidability and refusal:** homogeneous membership is determined
   from source types alone, and heterogeneous active families are refused.
2. **Functor totality:** `mathcal T_s` and `mathcal F_s` are total natural
   typed stochastic experiments on every admitted source.
3. **Naturality:** presentation transport commutes with both child functors,
   queries, phase projector, inverse, commit, and child readers.
4. **No-hiding:** the finite and quantum complement theorems remain exact.
5. **Query closure:** all source, seed, witness, complement, and uncontrolled
   environment fields return to a common state before recombination.
6. **Probe law:** `C_phi`, `B^2`, filters, record erasure, visibility, and the
   `K_phi` interval remain exactly the frozen values.
7. **Fiberwise commit:** equation (P22v2.5) is an isometry for every admitted
   fixed source.
8. **Normalization and child recovery:** equation (P22v2.6) normalizes and
   conditions exactly to the accepted child laws.
9. **No dormant child:** the accessible selected block contains no active
   register for the unchosen child.
10. **Positive restriction:** restriction staying in the positive groupoid is
    natural and seed-marginalizing.
11. **Degenerate branchwise restriction:** equation (P22v2.7) is typed without
    identifying formal tensor and atomic fusion targets.
12. **External tensor composition:** equation (P22v2.8) is natural without
    implying a same-source multi-mark theorem.
13. **Local process plurality:** tensor and simultaneous-fusion commits are
    different triggered whole-process fibers on the corrected source.
14. **Noninheritance:** no activity, root, source superposition, actuality,
    chronology, dimension, or geometry law follows.

## 10. Mandatory hostile controls

The construction must contain exact counterexamples or refusals for:

1. heterogeneous active pair `B_1^0({i})` with `B_2^0({j})`;
2. sort-changing source morphism;
3. empty active family presented as a positive source;
4. one-active family presented as a positive source;
5. formal tensor unit identified with an empty atomic boundary;
6. formal one-factor tensor identified with its atomic factor without an
   explicit alignment;
7. restriction that forges a new mode opportunity;
8. restriction that drops the stable apparatus record silently;
9. same-source two-mark commutation asserted without a multi-mark source;
10. staged binary fusion substituted for one simultaneous n-ary fusion;
11. different classical sources sharing one unindexed exhaust vector;
12. source identity or source amplitudes inferred from exhaust orthogonality;
13. spectator identity changing `C_phi`;
14. route-dependent residual seed, witness, complement, or environment after
    uncomputation;
15. reversible query renamed as accessible erasing fusion;
16. dormant unchosen child stored in an accessible or future-readable field;
17. mode label swapped without transporting controlled operations;
18. output decoder changed while the instrument is held fixed;
19. `phi` chosen after observing a downstream result;
20. any mutation of `B`, `R`, `C_phi`, the neutral odds, or a child kernel;
21. activity/root probabilities inferred from local conditional odds; and
22. any chronology, dimension, metric, curvature, gravity, or actuality label
    inferred from this local repair.

## 11. Kill conditions

The candidate is rejected if any of the following occurs:

- an admitted source lacks a typed `mathcal F_s` child experiment;
- heterogeneous active sorts reach query or commit evaluation;
- a source morphism changes the active sort;
- totality is rescued by assigning probability zero to an undefined value;
- any immutable numerical or child-law anchor changes;
- a zero/one-active restriction claims target equality without a new theorem;
- a same-source multi-mark law is claimed without a typed source object;
- `W_X` is promoted to a source law or global coherent process;
- simultaneous and staged fusion traces are identified;
- a presentation label, serialization order, or loop index changes a physical
  law; or
- any downstream outcome is used to select a source, phase, coefficient,
  dimension, or geometry.

## 12. Outcome product

The construction must report each coordinate independently:

```text
P22V2-HOMOGENEOUS-SOURCE-GROUPOID:
  CONSTRUCTED | UNCONSTRUCTED

P22V2-TOTAL-TENSOR-FUSION-CHILD-PAIR:
  CONSTRUCTED | UNCONSTRUCTED

P22V2-FIBERWISE-REVERSIBLE-QUERY:
  CONSTRUCTED | UNCONSTRUCTED

P22V2-FIBERWISE-COMMIT-INSTRUMENT:
  CONSTRUCTED | UNCONSTRUCTED

P22V2-EXACT-LOCAL-MODE-LAW:
  CONSTRUCTED | UNCONSTRUCTED

P22V2-POSITIVE-RESTRICTION-NATURALITY:
  CONSTRUCTED | UNCONSTRUCTED

P22V2-DEGENERATE-BRANCHWISE-RESTRICTION:
  CONSTRUCTED | UNCONSTRUCTED

P22V2-EXTERNAL-TENSOR-COMPOSITION:
  CONSTRUCTED | UNCONSTRUCTED

P22V2-SAME-SOURCE-MULTIMARK-COMPOSITION:
  UNCONSTRUCTED

P22V2-SIMULTANEOUS-FUSION-ALGEBRA:
  UNCONSTRUCTED

P22V2-ACTIVITY-ROOT-LAW:
  UNCONSTRUCTED

P22V2-PHYSICAL-REGIONAL-REFERENT:
  UNCONSTRUCTED

P22V2-CHRONOLOGY-DIMENSION-METRIC-GR:
  UNCONSTRUCTED

P22V2-ACTUALIZATION:
  UNCONSTRUCTED
```

The strongest available result is the componentwise product, not the highest
positive coordinate read as a ladder.

## 13. Construction and evidence discipline

This pin authorizes nothing until it is committed at this exact path. After
that RUNBOOK freeze, the only construction-stage writable paths are:

```text
v16/paper-22-reversible-structural-instrument-v2.md
v16/note-paper22-reversible-structural-instrument-v2-construction.md
```

The construction note must bind:

- the frozen pin commit and hash;
- all input hashes;
- exact theorem/control coverage;
- line and byte counts;
- a no-code statement;
- absence of downstream source, evaluator, result, and review artifacts; and
- a full path-scoped whitespace/hash check.

No Python, Rust, evaluator, fresh case, or generated result is needed for this
bounded mathematical repair. Exact arithmetic may be checked independently,
but executable verification cannot substitute for the typed proofs.

After construction freezes, one delta review must cover at least three blind
lenses:

1. source/category/functor totality;
2. probability and instrument normalization; and
3. quantum/no-hiding and physical scope.

The reviewers must treat the predecessor counterexamples as mandatory
regressions. Acceptance requires adjudication before Paper 23 may rebind this
instrument.

## 14. Permanent walls

Even complete acceptance would establish only a triggered local instrument
on a homogeneous Paper 13D source fiber. It would not establish:

- why a source exists;
- why an active family is selected;
- how often the instrument occurs;
- a joint law over source objects;
- a global or cosmological state;
- localized reverse interventions at every later generator;
- an onset multiplicity object;
- a complete operational chronology;
- a varying-size physical ensemble;
- dimension or Lorentzian signature;
- order-plus-valuation metric reconstruction;
- clocks, rods, radar, or absolute scale;
- a physical regional referent;
- dynamical curvature or matter--geometry backreaction;
- Einstein dynamics, gravity, QFT, or continuum control; or
- actualization.

Those remain independent future gates. In particular, Paper 22 v2 may become
an input to a structural opportunity investigation, but it cannot itself
choose `Pi_phys` or promote the conditional Paper 17/15/16 constructions.

## 15. No automatic repair chain

This is the one bounded successor specified by the Paper 22 adjudication. If
an independent semantic counterexample survives this exact correction, no
automatic Paper 22 v3 is authorized. The surviving local coordinates must be
reported, the failed coordinate demoted, and any further redesign separately
motivated and authorized.
