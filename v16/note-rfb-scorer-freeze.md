# RFB physical scorer freeze

**Frozen:** 2026-08-18, v16 ledger #133.

**Status:** SCORER FROZEN BEFORE FIRST PHYSICAL RUN — NO RFB RESULT,
TRANSCRIPT, RECEIPT, OR PAPER 10 CANDIDATE EXISTS.

## 1. Frozen source chain

| object | SHA-256 |
|---|---|
| `v16/code/rfb_core.py` | `7d0a787d108ac16229dc6819a81f74f7b80203eaefa71d28f30f1f31b27e9ada` |
| `v16/code/rfb_fixture.json` | `f3557b3400584d01984c6a4f38d40744c9e2cf2f0e36c7c4aa225b045e5bd362` |
| `v16/code/rfb_score.py` | `4a2c9590e1d64f9e40c6e5828d132b9b746aa8938e18aee95a484caabc8c87ff` |

The scorer hard-binds the first two hashes and both public-core artifact
hashes. It reads all scientific sources through one audited accessor and
requires the live read set to equal the fixture's declared set.

## 2. Derivation contract

The scorer registers forty-nine ordered gates and thirty-nine targeted
mutants. It derives, rather than types as an answer:

- writer full-cycle conjugacy, the return of multi-cycle writers on dropping
  accumulation, phase-gauge orbits, and winding-port movement;
- type-universality pricing separately from additive-reader selection,
  writer/reader quotient classes, calibrated chirality, inert feedback, and
  the zero-charge decoherence leak;
- all-input classical feedback, its reversible coherent dilation, their
  same-port/same-channel control, and the hybrid overlap family;
- exclusive-kernel failure, strong positivity, two-cut cancellation,
  all-input final division, and the surviving law-type coordinates;
- intervention-complete predictive quotients at resource parity, the held-out
  horizon, finite recoverability, redundancy, fixed-factor locality, and HJW
  controls; and
- the full assumption-by-freedom matrix, the named compound cells, primary
  outcome feasibility, an independent primary truth table, scope walls,
  exactness, read totality, transcript binding, paper claims, and receipt seal.

The primary can be `RFB-FORCING-BOUNDARY-MAPPED` while a coordinate remains
`METHOD-INCONCLUSIVE`; those words answer different questions. The former says
the registered impose/drop boundary was measured. The latter says that
multiple microscopic or representational law types survive it.

## 3. Pre-run checks actually performed

Only non-physical inspection modes have run:

- Python compilation;
- `--selftest`, which calls the already-public core calibration and a synthetic
  primary truth-table row without reading `rfb_fixture.json`;
- exact enumeration of the forty-nine gate names and thirty-nine mutant names;
- unknown-argument refusal; and
- an AST check finding no floating-point literal.

The normal scoring path has not been invoked. The three whitelisted result
artifacts remain absent. Consequently this event contains no measurement,
candidate primary, or Paper 10 prose generated from the physical fixture.

## 4. First-run rule

After this commit, one normal invocation may attempt the frozen fixture. A
refusal is an investigation event, not permission to edit around a result. Any
repair must name the failed gate, preserve the core and fixture, freeze a new
scorer hash before rerun, and disclose whether scientific predicates changed.
If the run succeeds, the artifacts are committed as generated before hostile
review.
