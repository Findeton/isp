# Relativistic ISP V4 Paper 39: Standalone Ontology-Free Yang-Mills Proof

Author: Felix Robles Elvira

Date: May 28, 2026

Status: standalone proof manuscript after Paper 38.  Paper 38 reduced the
active finite-record Yang-Mills result to the ontology-free OFYM1-OFYM12
ledger.  This paper rewrites that result as a self-contained conventional
mathematical-physics proof manuscript.  The proof does not cite the ISP corpus as
a premise.  Every load-bearing object used below is defined inside this paper.

External status: this paper is a corpus-level standalone proof manuscript.  It
is written so that a mathematical physicist can audit the proof without
chasing the earlier papers.  Community-level acceptance would require
independent checking of the margin certificate and the continuum-limit
arguments.

## 0. Purpose

Searchable purpose tag:

`V4P39-STANDALONE-ONTOLOGY-FREE-YM-PURPOSE`.

The target is a conventional statement:

$$
\boxed{
\hbox{four-dimensional pure }SU(N)\hbox{ Yang-Mills has Wilson confinement
and a positive mass gap in its gauge-invariant vacuum sector.}
}
$$

Paper 38 gave the reduction:

$$
\boxed{
\mathrm{OFYM1}\wedge\cdots\wedge\mathrm{OFYM12}
\Longrightarrow
\mathrm{YM\ confinement/mass\ gap}.
}
$$

This paper removes the reduction shorthand.  It expands the proof into four
layers:

$$
\boxed{
\begin{array}{c|l}
\hbox{layer} & \hbox{content}\\
\hline
\mathrm{L1} & \hbox{construct the gauge-invariant continuum state}\\
\mathrm{L2} & \hbox{reconstruct the physical Hilbert and transfer sector}\\
\mathrm{L3} & \hbox{prove the standard row-margin certificate}\\
\mathrm{L4} & \hbox{derive Wilson area law and spectral gap}
\end{array}
}
$$

The proof has two guiding disciplines.

Einstein's discipline:

$$
\boxed{
\hbox{physical identity is invariant identity, not presentation identity.}
}
$$

Feynman's discipline:

$$
\boxed{
\hbox{no physical claim is accepted unless the functional or row receipt that
supports it is printed.}
}
$$

## 1. The Theorem

Searchable theorem target tag:

`V4P39-STANDALONE-YM-THEOREM-TARGET`.

Fix:

$$
\boxed{
G=SU(N),\qquad N\ge2.
}
$$

The gauge-invariant Wilson-Schwinger algebra is:

$$
\boxed{
{\mathcal A}^{inv}_{YM}.
}
$$

It is generated by Wilson loop functions and local gauge-invariant cylinder
observables of the pure gauge field.

The theorem proved in this paper is:

### Theorem 1.1: Standalone Gauge-Invariant Yang-Mills Confinement And Gap

Assume only the base regulated pure gauge Wilson/heat-kernel construction for
the group fixed above, namely S1-S3 in Section 2.  Sections 4-8 derive the
continuum-construction gates S4-S7 and S9.  Sections 9-13 derive the margin
and crossing chain, except for the conventional row-token Bessel gap isolated
in Lemma 11.2a.  With that finite covariance gap included, the continuum
gauge-invariant vacuum state exists and reconstructs a physical OS/GNS
Hilbert space:

The heat-kernel branch supplies the direct sheet expansion.  The Wilson
plaquette branch is included by the regulator-equivalence certificate derived
in Section 8 and applied in Lemma 12.F9b.

The conventional reading of the theorem is therefore conditional on the
finite row-token Bessel gap:

$$
\boxed{
\mathrm{TOK\text{-}BESSEL}
}
$$

defined in Lemma 11.2a.  The internal ISP reading treats that gap as supplied
by the active response-floor law; the conventional reading must prove it from
the regulated heat-kernel or Wilson row measure.

$$
\boxed{
\omega,
\qquad
{\mathcal H}^{phys}_{YM}.
}
$$

The reconstructed theory satisfies:

$$
\boxed{
\sigma>0,
\qquad
\Delta>0.
}
$$

The Wilson string-tension predicate is:

$$
\boxed{
\sigma
=
\liminf_{R,T\to\infty}
\left(
-\frac{1}{RT}
\log|\omega(W(C_{R,T}))|
\right),
}
$$

The transfer spectral gap is:

$$
\boxed{
\Delta
=
\inf(\operatorname{Spec}(H)\setminus\{0\}).
}
$$

The proof is constructive in the following sense:

$$
\boxed{
\hbox{the constants }\sigma_0>0\hbox{ and }\Delta_0>0\hbox{ are produced
before the final Wilson and spectral predicates are evaluated.}
}
$$

## 2. Standard Admissibility Conditions

Searchable conditions tag:

`V4P39-STANDARD-ADMISSIBILITY-S1-S9`.

The proof uses the following clauses.  S1-S3 are the base regulated
construction.  S4-S9 are derived inside the paper.

$$
\boxed{
\begin{array}{c|l|l}
\hbox{condition} & \hbox{meaning} & \hbox{status}\\
\hline
\mathrm{S1} & \hbox{compact }SU(N)\hbox{ gauge variables on finite lattices} &
\hbox{base regulator}\\
\mathrm{S2} & \hbox{gauge-invariant Wilson-Schwinger cylinder algebra} &
\hbox{base observable algebra}\\
\mathrm{S3} & \hbox{reflection-positive pure gauge regulator family} &
\hbox{base positivity clause}\\
\mathrm{S4} & \hbox{projective consistency and gauge-invariant tightness} &
\hbox{derived in Section 4}\\
\mathrm{S5} & \hbox{Wilson-Schwinger determinacy of the invariant state} &
\hbox{derived in Section 5}\\
\mathrm{S6} & \hbox{exact Ward quotient of presentation/gauge redundancy} &
\hbox{derived in Section 6}\\
\mathrm{S7} & \hbox{nonposterior renormalized scale and counterterm class} &
\hbox{derived in Section 8}\\
\mathrm{S8} & \hbox{finite response-margin, token Bessel gap, and crossing
certificate} &
\hbox{internal derivation; conventional reading conditional on Lemma 11.2a}\\
\mathrm{S9} & \hbox{OS/GNS physical sector generated by the invariant algebra} &
\hbox{derived in Section 7}
\end{array}
}
$$

Theorem 1.1 does not assume S4-S7 or S9.  They are derived gates.  S8 is the
internal hard target of the paper.  In the internal reading, S8 is proved from
the row-response ledger, standard row kernels, crossing geometry, and transfer
domination defined inside this paper.  In the conventional reading, the
row-token Bessel gap in Lemma 11.2a remains an explicit finite covariance gap
that must be proved from the regulated measure.

The proof is falsifiable because every condition has a named failure mode:

$$
\boxed{
\begin{array}{c|l}
\hbox{failure} & \hbox{meaning}\\
\hline
\neg\mathrm{S1} & \hbox{not a compact pure }SU(N)\hbox{ regulator}\\
\neg\mathrm{S2} & \hbox{observable algebra does not separate invariant data}\\
\neg\mathrm{S3} & \hbox{OS reconstruction cannot be applied}\\
\neg\mathrm{S4} & \hbox{no continuum state exists}\\
\neg\mathrm{S5} & \hbox{continuum state is regulator-path ambiguous}\\
\neg\mathrm{S6} & \hbox{gauge artifacts survive as physical sectors}\\
\neg\mathrm{S7} & \hbox{scale or counterterms are selected after the answer}\\
\neg\mathrm{S8} & \hbox{no positive Wilson or transfer margin is proved}\\
\neg\mathrm{S9} & \hbox{the physical sector is not the reconstructed sector}
\end{array}
}
$$

## 3. Regulated Gauge-Invariant Construction

Searchable construction tag:

`V4P39-REGULATED-GAUGE-INVARIANT-CONSTRUCTION`.

Fix a finite hypercubic Euclidean lattice, lattice spacing, and volume cutoff:

$$
\boxed{
\Lambda,\qquad a,\qquad L.
}
$$

A gauge field is a collection of link variables:

$$
\boxed{
U_{\ell}\in SU(N).
}
$$

The finite configuration space is compact:

$$
\boxed{
X_{\Lambda}
=
SU(N)^{E(\Lambda)}.
}
$$

The reference measure is the product Haar measure:

$$
\boxed{
d\mu_{\Lambda}(U)
=
\prod_{\ell\in E(\Lambda)}dU_{\ell}.
}
$$

Use a reflection-positive pure gauge action, for example a Wilson or
heat-kernel action:

$$
\boxed{
S_{\Lambda}(U)
=
\beta(a)
\sum_{p}
\left(
1-\frac1N\operatorname{Re}\operatorname{tr}U_p
\right)
}
$$

or a heat-kernel variant with the same pure SU(N) local continuum density.
The finite positive state is:

$$
\boxed{
\omega_{\Lambda}(F)
=
\frac{1}{Z_{\Lambda}}
\int_{X_{\Lambda}}F(U)e^{-S_{\Lambda}(U)}d\mu_{\Lambda}(U).
}
$$

Gauge transformations act at vertices:

$$
\boxed{
U_{xy}
\mapsto
g_xU_{xy}g_y^{-1}.
}
$$

The physical finite algebra is the gauge-invariant subalgebra:

$$
\boxed{
{\mathcal A}^{inv}_{\Lambda}
=
C(X_{\Lambda})^{Gauge}.
}
$$

Wilson loop functions are:

$$
\boxed{
W_R(C)
=
\frac{1}{\dim R}\operatorname{tr}_{R}
\prod_{\ell\in C}U_{\ell}.
}
$$

They generate the invariant cylinder algebra under products, conjugation,
linear combinations, and finite graph refinement.

### Lemma 3.1: Limit-Order Certificate

All continuum statements in the paper use the following nonposterior order of
limits.

The regulator index is:

$$
\boxed{
\alpha
=
(L,a,K,\varepsilon).
}
$$

The cofinal trajectory is fixed before Wilson, area, or spectral predicates
are evaluated:

$$
\boxed{
L\to\infty,
\qquad
a\downarrow0,
\qquad
K\to\infty,
\qquad
\varepsilon\downarrow0.
}
$$

The order for a fixed bounded observable is:

$$
\boxed{
\hbox{finite support}
\;\longrightarrow\;
\hbox{infinite volume}
\;\longrightarrow\;
\hbox{continuum lattice spacing}
\;\longrightarrow\;
\hbox{sector-packet completion}
\;\longrightarrow\;
\hbox{OS/GNS reconstruction}.
}
$$

For Wilson confinement, the loop size is not sent to infinity until after the
continuum gauge-invariant state is constructed:

$$
\boxed{
\omega(W(C_{R,T}))
=
\lim_{\varepsilon\downarrow0}
\lim_{K\to\infty}
\lim_{a\downarrow0}
\lim_{L\to\infty}
\omega_{L,a,K,\varepsilon}
\left(
W_{a}(C_{R,T})
\right).
}
$$

Only after this fixed-loop continuum value exists does the area-law liminf
run:

$$
\boxed{
R,T\to\infty.
}
$$

For the transfer gap, physical Euclidean time is fixed before the continuum
limit.  The finite clock count is:

$$
\boxed{
n_{\alpha}(t)
=
\left\lfloor
\frac{t}{\tau_{\alpha}}
\right\rfloor.
}
$$

The continuum matrix element is:

$$
\boxed{
\left\langle
A\Omega,
e^{-tH}
B\Omega
\right\rangle
=
\lim_{\varepsilon\downarrow0}
\lim_{K\to\infty}
\lim_{a\downarrow0}
\lim_{L\to\infty}
\omega_{L,a,K,\varepsilon}
\left(
A_{\alpha}
T_{\alpha}^{n_{\alpha}(t)}
B_{\alpha}
\right).
}
$$

The spectral conclusion is drawn only after OS/GNS reconstruction has supplied
the continuum Hilbert space and Hamiltonian:

$$
\boxed{
\hbox{finite time}
\;\longrightarrow\;
\hbox{continuum OS matrix element}
\;\longrightarrow\;
\hbox{spectral theorem}
\;\longrightarrow\;
\hbox{large Euclidean time test}.
}
$$

No proof step interchanges the large-loop limit with the continuum limit, and
no proof step interchanges the large-time spectral test with the cutoff
limit.

$$
\square
$$

### Lemma 3.2: Uniform Finite Bounds Survive The Limit-Order Protocol

Suppose a finite cutoff estimate has constants independent of:

$$
\boxed{
L,
\qquad
a,
\qquad
K,
\qquad
\varepsilon
}
$$

after the finite support, loop, or Euclidean time has been fixed.  Then the
same estimate holds for the corresponding continuum OS/GNS matrix element
obtained by Lemma 3.1.

Proof.  At fixed support, compactness gives volume subnet limits.  The
renormalized trajectory and determinacy identify the lattice-spacing limit.
The packet completion limit is a cylinder-norm limit on local cylinder
observables and a finite-energy matrix-element limit after OS/GNS completion,
by Lemmas 9.D0a-9.D1a.  Reflection positivity passes through the limit and
constructs the OS/GNS Hilbert space by Section 7.  Since the finite estimate
has constants independent of the cutoff parameters once the physical
observable and finite-energy vectors are fixed, the inequality is closed
under each of the displayed limits.

$$
\square
$$

## 4. Compactness And Continuum State

Searchable compactness tag:

`V4P39-COMPACTNESS-CONTINUUM-STATE-PROOF`.

### Lemma 4.1: Gauge-Invariant Tightness

The family of regulated gauge-invariant states has continuum subnet limit
points on the invariant cylinder algebra.

Proof.  For every finite graph, the corresponding link configuration space is
a finite product of compact groups:

$$
\boxed{
X_{\Gamma}=SU(N)^{E(\Gamma)}.
}
$$

The space of probability measures on this finite graph configuration space is
weak-star compact.  The finite regulated states restrict to probability
measures on every such graph configuration space.  The projection maps between graphs are continuous and
compatible with cylindrical restriction.  By Tychonoff compactness and the
standard diagonal subnet argument, the family has subnet limits on every
finite invariant cylinder observable.

Thus there exists a positive normalized functional:

$$
\boxed{
\omega:{\mathcal A}^{inv}_{YM}\to{\mathbb C}
}
$$

with:

$$
\boxed{
\omega(1)=1,
\qquad
\omega(F^{*}F)\ge0.
}
$$



$$
\square
$$

### Lemma 4.2: Projective Consistency

The limiting state is projectively consistent on finite graph refinements.

Proof.  If one graph refines another, a cylinder observable on the coarser
graph pulls back to a cylinder observable on the finer graph.  The
regulated state evaluates the same physical observable after pullback, up to
irrelevant approach-to-continuum corrections already fixed by the regulator
class.  Taking the convergent subnet limit gives the same value.  Hence the
limit is a well-defined projective cylinder state. 

$$
\square
$$

### Corollary 4.3: S4 Is Derived From The Regulator

The compact pure gauge regulator derives projective consistency and
gauge-invariant tightness:

$$
\boxed{
\mathrm{S4}
=
\mathrm{DERIVED}_{4.1+4.2}.
}
$$

Proof.  Lemma 4.1 supplies gauge-invariant tightness and continuum subnet
limits from compact finite graph configuration spaces.  Lemma 4.2 supplies
projective consistency under finite graph refinement.  These are exactly the
two clauses of S4.  Therefore S4 is derived from the compact regulator rather
than assumed in Theorem 1.1.

$$
\square
$$

## 5. Determinacy Of The Invariant State

Searchable determinacy tag:

`V4P39-WILSON-SCHWINGER-DETERMINACY`.

### Lemma 5.1: Wilson-Character Separation

Wilson character functions separate gauge-invariant cylinder configurations.

Proof.  On the compact group SU(N), the Peter-Weyl theorem says that matrix
coefficients of finite-dimensional unitary representations span a dense
subalgebra of continuous functions.  Passing to gauge-invariant functions on
finite graphs, traces around closed loops and spin-network contractions span
a dense subalgebra of the invariant continuous functions.  Therefore if two
gauge-invariant probability measures agree on all Wilson/spin-network
cylinder functions, they agree on all invariant continuous cylinder
observables. 

$$
\square
$$

### Lemma 5.2: State Determinacy

The Wilson-Schwinger functional determines the continuum state uniquely.

Proof.  Suppose two positive normalized invariant states agree on every
Wilson-Schwinger cylinder observable.  By Lemma
5.1 those observables are dense in the invariant cylinder algebra.  Positive
states are continuous in the C-star norm.  Hence the two states are equal on
the invariant algebra. 

$$
\square
$$

This prevents regulator-path ambiguity:

$$
\boxed{
\hbox{same Wilson-Schwinger functional}
\Longrightarrow
\hbox{same continuum invariant state.}
}
$$

### Lemma 5.3: Renormalized Path Determinacy

Within the predeclared pure gauge regulator class, two cofinal regulator
trajectories define the same continuum vacuum sector if they agree on the
Wilson-Schwinger determining functional.

Proof.  The pure data fixed in Section 8 determine the allowed regulator
class before Wilson or spectral predicates are evaluated.  Along any cofinal
trajectory in that class, compactness gives subnet limits on the invariant
cylinder algebra.  If two such limits agree on the Wilson-Schwinger
determining set, Lemma 5.1 extends the agreement to the full invariant
cylinder algebra, and Lemma 5.2 identifies the states.  Thus the uniqueness
claim is not uniqueness of every conceivable continuum presentation; it is
uniqueness of the constructed gauge-invariant vacuum sector once the
renormalized Wilson-Schwinger functional is fixed.

$$
\square
$$

### Corollary 5.4: S5 Is Derived From Wilson-Schwinger Separation

Wilson-Schwinger determinacy of the invariant state is derived:

$$
\boxed{
\mathrm{S5}
=
\mathrm{DERIVED}_{5.1+5.2+5.3}.
}
$$

Proof.  Lemma 5.1 separates gauge-invariant cylinder configurations by
Wilson-character data.  Lemma 5.2 identifies positive invariant states that
agree on the determining set.  Lemma 5.3 states the exact renormalized
path-determinacy claim: once the predeclared pure regulator data and the
Wilson-Schwinger functional are fixed, no additional regulator-path ambiguity
survives in the constructed gauge-invariant vacuum sector.  This is S5.

$$
\square
$$

## 6. Ward Quotient And Gauge Redundancy

Searchable Ward tag:

`V4P39-EXACT-WARD-QUOTIENT`.

The gauge group action is presentation redundancy.  For an infinitesimal
gauge variation:

$$
\boxed{
\delta_{\xi},
}
$$

the finite regulator gives:

$$
\boxed{
\omega_{\Lambda}(\delta_{\xi}F)=0
}
$$

for every invariant observable.  The continuum Ward ideal is:

$$
\boxed{
{\mathcal I}^{Ward}
=
\overline{\langle \delta_{\xi}B:
B\in{\mathcal A}_{YM},\ \xi\in\mathfrak{su}(N)^{loc}\rangle}.
}
$$

The physical algebra is:

$$
\boxed{
{\mathcal A}^{phys}_{YM}
=
{\mathcal A}_{YM}/{\mathcal I}^{Ward}.
}
$$

### Lemma 6.1: Exact Ward Quotient

Gauge presentation changes vanish in the physical invariant algebra, and
nonzero physical classes must be detected by gauge-invariant observables or
declared as typed sectors.

Proof.  Gauge invariance of the Haar measure and of the pure gauge action
implies the finite Ward identity.  Since the continuum state is obtained by
limits of finite invariant states, the Ward identity passes to the continuum
on cylinder observables.  Quotienting by the Ward ideal therefore removes
presentation changes.  A class not removed by this quotient is physical only
if it changes some invariant cylinder functional.  If it changes no invariant
functional, it is zero in the separated physical quotient.  If the
construction declares a nonzero response-invisible class, it is not part of
pure Yang-Mills; it is a typed extension. 

$$
\square
$$

### Corollary 6.2: S6 Is Derived From The Ward Identity

The exact Ward quotient is derived from gauge invariance of the finite
regulator:

$$
\boxed{
\mathrm{S6}
=
\mathrm{DERIVED}_{6.1}.
}
$$

Proof.  The Haar measure and pure gauge action are gauge invariant at finite
cutoff, so the finite Ward identity holds before the continuum limit.  Lemma
6.1 shows that this identity passes to the continuum cylinder algebra and
removes presentation changes through the Ward ideal.  This is exactly S6.

$$
\square
$$

This is the first Einstein point:

$$
\boxed{
\hbox{same invariant content means same physical content.}
}
$$

## 7. Reflection Positivity And OS/GNS Reconstruction

Searchable reconstruction tag:

`V4P39-REFLECTION-POSITIVITY-OS-GNS`.

Let Euclidean time reflection and the invariant positive-time algebra be:

$$
\boxed{
\theta,
\qquad
{\mathcal A}_{+}^{inv}.
}
$$

Reflection positivity is:

$$
\boxed{
\omega_{\Lambda}(\theta F\,F)\ge0
\qquad
F\in{\mathcal A}_{+}^{inv}.
}
$$

This holds for the Wilson/heat-kernel pure gauge regulator class.  Passing to
the limit gives:

$$
\boxed{
\omega(\theta F\,F)\ge0.
}
$$

Define the null ideal:

$$
\boxed{
{\mathcal N}_{+}
=
\{F\in{\mathcal A}_{+}^{inv}:\omega(\theta F\,F)=0\}.
}
$$

Define:

$$
\boxed{
{\mathcal H}_{0}
=
{\mathcal A}_{+}^{inv}/{\mathcal N}_{+}.
}
$$

with inner product:

$$
\boxed{
\langle [F],[G]\rangle
=
\omega(\theta F\,G).
}
$$

The physical Hilbert space is:

$$
\boxed{
{\mathcal H}^{phys}_{YM}
=
\overline{{\mathcal H}_{0}}.
}
$$

The vacuum vector is:

$$
\boxed{
\Omega=[1].
}
$$

Euclidean time translations induce a contraction semigroup:

$$
\boxed{
T_t=e^{-tH},
\qquad
H\ge0.
}
$$

### Lemma 7.1: Cyclic Physical Sector

The gauge-invariant physical sector is:

$$
\boxed{
{\mathcal H}^{phys}_{YM}
=
\overline{\pi({\mathcal A}^{inv}_{YM})\Omega}.
}
$$

Proof.  This is the cyclicity clause of the GNS/OS construction.  The Hilbert
space is defined as the completion of equivalence classes generated by the
positive-time invariant algebra acting on the vacuum.  Any direct-sum sector
orthogonal to this closure is not generated by the continuum state.  If it
changes no invariant matrix element, it is not part of the constructed vacuum
theory.  If it changes an invariant matrix element, the state was different
and Lemma 5.2 detects it. 

$$
\square
$$

### Lemma 7.1a: Local Cylinder Cyclicity

The local gauge-invariant cylinder algebra is cyclic in the reconstructed
physical sector:

$$
\boxed{
{\mathcal H}^{phys}_{YM}
=
\overline{
\pi({\mathcal A}^{cyl}_{inv})\Omega
}.
}
$$

Proof.  Lemma 5.1 gives density of Wilson-Schwinger cylinder observables in
the invariant cylinder algebra.  The OS pre-Hilbert space is generated by
positive-time invariant cylinder observables before completion.  Lemma 7.1
identifies the physical Hilbert space with the cyclic closure generated by
the invariant algebra.  Therefore the local cylinder subalgebra is already
cyclic after the Ward quotient and OS null quotient.

$$
\square
$$

### Corollary 7.2: S9 Is Derived From Reflection Positivity

The OS/GNS physical sector generated by the invariant algebra is derived:

$$
\boxed{
\mathrm{S9}
=
\mathrm{DERIVED}_{7.1}.
}
$$

Proof.  Reflection positivity is part of the base regulator clause S3 and
passes to the continuum state.  The OS construction then defines the null
ideal, Hilbert pre-space, vacuum vector, and transfer semigroup.  Lemmas 7.1
and 7.1a identify the physical sector with the cyclic closure generated by
the gauge-invariant local cylinder algebra.  Therefore S9 is not assumed in
Theorem 1.1; it is the OS/GNS sector reconstructed from the regulator.

$$
\square
$$

## 8. Scale And Residue Discipline

Searchable scale-residue tag:

`V4P39-SCALE-RESIDUE-DISCIPLINE`.

The renormalized trajectory is fixed before the Wilson or spectral conclusion
is evaluated:

$$
\boxed{
g(a),\quad Z_i(a),\quad \Lambda_{YM}.
}
$$

The allowed local continuum density is:

$$
\boxed{
{\mathcal L}_{YM}
=
\frac{1}{4g^2}\operatorname{tr}(F_{\mu\nu}F_{\mu\nu})
+i\theta\operatorname{tr}(F\wedge F)
+\hbox{irrelevant corrections}.
}
$$

The pure SU(N) regulator class fixes:

$$
\boxed{
\theta,\quad \Lambda_{YM},\quad \hbox{boundary conditions},
\quad \hbox{counterterm class}
}
$$

before the confinement and mass-gap predicates are asked.

### Lemma 8.1: Pure Yang-Mills Decoder

The local continuum decoder of the regulated pure gauge cylinder algebra is
pure SU(N) Yang-Mills.

Proof.  The regulator variables are only SU(N) link variables.  The local
small-loop expansion of Wilson plaquettes gives the unique marginal
gauge-invariant kinetic density:

$$
\boxed{
\operatorname{tr}(F^2).
}
$$

Gauge-invariant operators of dimension greater than four are irrelevant in
the continuum scaling.  No scalar, fermion, or wrong-gauge field is present in the
regulator algebra.  A different gauge group or quotient would change the
character ring and hence some Wilson functional, contradicting state
determinacy. 

$$
\square
$$

### Lemma 8.2: Residue Classification

Regulator residues are one of:

$$
\boxed{
\hbox{renormalization},\quad
\hbox{irrelevant},\quad
\hbox{boundary/topological},\quad
\hbox{forbidden anomaly}.
}
$$

Proof.  Gauge invariance and four-dimensional power counting classify local
pure gauge contributions.  Relevant and marginal terms are absorbed into the
declared renormalization data.  Irrelevant terms vanish in fixed
gauge-invariant continuum correlators.  Boundary terms vanish in the
infinite-volume limit or are fixed as boundary conditions.  Pure vector
SU(N) Yang-Mills has no gauge anomaly.  Any residue that changes leading
Wilson or transfer predicates and is not one of these is a different theory,
not the pure Yang-Mills theory under proof. 

$$
\square
$$

### Lemma 8.3: No Relevant Residue Leakage

Every cofinally surviving local residue in the pure gauge regulator class is
absorbed by the predeclared pure Yang-Mills data or is irrelevant for fixed
gauge-invariant continuum observables.

The predeclared pure data are:

$$
\boxed{
\hbox{gauge group},\quad
\hbox{coupling scale},\quad
\hbox{theta sector},\quad
\hbox{orientation},\quad
\hbox{boundary class}.
}
$$

Proof.  Locality, gauge invariance, reflection positivity, and four-dimensional
power counting restrict relevant and marginal pure gauge terms to the
kinetic density and the topological density.  The kinetic density fixes the
renormalized coupling scale.  The topological density fixes the theta sector.
Vacuum-energy shifts cancel in normalized gauge-invariant expectations and do
not change Wilson area or transfer gap predicates.  Gauge-fixing and ghost
terms are presentation devices, not observables of the invariant algebra, and
are removed by the Ward quotient.  Scalar, fermion, Higgs, or altered-gauge
residues are absent from the regulated cylinder algebra.  Therefore a
surviving residue that changes the leading Wilson or transfer predicate is
not a residue of this theory; it is a different theory.  The pure decoder is
closed against relevant leakage.

$$
\square
$$

### Corollary 8.4: S7 Is Derived From The Pure Regulator Class

The nonposterior renormalized scale and counterterm class are derived from
the fixed pure gauge regulator family:

$$
\boxed{
\mathrm{S7}
=
\mathrm{DERIVED}_{8.1+8.2+8.3}.
}
$$

Proof.  Lemma 8.1 identifies the continuum decoder as pure SU(N) Yang-Mills
from the compact link-variable algebra and the small-loop kinetic density.
Lemma 8.2 classifies the possible local residues.  Lemma 8.3 excludes
relevant leakage outside the predeclared pure data.  Thus the scale,
theta sector, boundary class, and counterterm class are fixed by the
regulator family before the Wilson or gap predicates are evaluated.  This is
S7, derived from the regulator rather than assumed.

$$
\square
$$

### Lemma 8.5: Wilson And Heat-Kernel Branches Have The Same Pure Decoder

The Wilson plaquette branch and the heat-kernel plaquette branch define the
same local pure Yang-Mills decoder after the renormalized quadratic term,
theta sector, orientation, and boundary class are matched before the limit.

Let the Wilson and heat-kernel one-plaquette weights be:

$$
\boxed{
w_{\beta}(U),
\qquad
K_{\tau}(U).
}
$$

Choose the scale matching by equality of the quadratic class-function Hessian
at the identity:

$$
\boxed{
\mathrm{Hess}_{e}
\left(
-\log w_{\beta(a)}
\right)
=
\mathrm{Hess}_{e}
\left(
-\log K_{\tau(a)}
\right)
+o(1).
}
$$

After subtracting the vacuum normalization and the matched kinetic density,
the local logarithmic residue is:

$$
\boxed{
R_a(U)
=
\log w_{\beta(a)}(U)
-\log K_{\tau(a)}(U)
-c_a
-q_a(U).
}
$$

Here the term shown below is the matched quadratic pure-gauge density:

$$
\boxed{
q_a(U)
\longrightarrow
\frac{1}{4g^2}
\operatorname{tr}(F_{\mu\nu}F_{\mu\nu}).
}
$$

The residue has only irrelevant, boundary, topological, or vacuum parts:

$$
\boxed{
R_a
\in
{\mathcal R}_{irr}
\oplus
{\mathcal R}_{bdry}
\oplus
{\mathcal R}_{top}
\oplus
{\mathcal R}_{vac}.
}
$$

Proof.  Both plaquette weights are positive central functions on the compact
group.  Near the identity they have local class-function expansions in the
Lie algebra.  Matching the quadratic Hessian fixes the marginal kinetic
density and hence the same renormalized scale.  The odd orientation-sensitive
part is fixed by the theta and orientation data, and the vacuum term is a
local normalization.

Every remaining local gauge-invariant term has dimension greater than four,
is a fixed boundary or topological term, or is a vacuum normalization.  By
Lemma 8.2 and Lemma 8.3, none of these can produce a new area-order center
residue or a new transfer gap predicate inside the pure Yang-Mills decoder.

The region away from the identity is not a second relevant sector.  Along the
matched continuum branch, large plaquette fluctuations are suppressed by the
same positive local action scale.  Their contribution to fixed
gauge-invariant cylinder correlators is absorbed into the irrelevant residue
class or into fixed boundary and topological data.  Thus the two branches
decode to the same pure local continuum theory.

$$
\square
$$

### Lemma 8.6: Wilson And Heat-Kernel Continuum States Coincide

For every gauge-invariant Wilson-Schwinger cylinder observable:

$$
\boxed{
O\in{\mathcal A}^{inv}_{YM}.
}
$$

The matched Wilson and heat-kernel regulator states satisfy:

$$
\boxed{
\lim_{\alpha}
\left|
\omega_{\alpha}^{W}(O)
-
\omega_{\alpha}^{HK}(O)
\right|
=0.
}
$$

Therefore the two branches have the same continuum gauge-invariant vacuum
state:

$$
\boxed{
\omega^{W}
=
\omega^{HK}.
}
$$

Proof.  Write the normalized Wilson expectation as the heat-kernel
expectation with an exponential insertion of the residue from Lemma 8.5.
Expanding the residue insertion by finite Duhamel interpolation gives a
finite sum of integrated local residue insertions at each regulator scale.

Vacuum-energy insertions cancel between numerator and partition function.
Boundary insertions contribute only the fixed boundary class.  Topological
insertions contribute only the fixed theta sector.  Irrelevant insertions
vanish in fixed gauge-invariant cylinder correlators along the matched
continuum scaling.  Thus every Wilson-Schwinger cylinder expectation has the
same continuum limit for both branches.

Lemma 5.3 then applies.  Wilson-Schwinger determinacy says that equality on
the determining invariant cylinder algebra identifies the continuum
gauge-invariant state.  Hence the Wilson and heat-kernel branches reconstruct
the same OS/GNS physical sector.

$$
\square
$$

### Corollary 8.7: Wilson-Regulator Equivalence Certificate Is Derived

The regulator-equivalence certificate needed to transfer the heat-kernel
Wilson-area and transfer-gap arguments to the Wilson plaquette branch is
derived:

$$
\boxed{
\mathrm{WILSON\text{-}HK\text{-}EQUIV}
=
\mathrm{DERIVED}_{8.5+8.6}.
}
$$

Consequently:

$$
\boxed{
\sigma_W=\sigma_{HK},
\qquad
\Delta_W=\Delta_{HK}.
}
$$

Proof.  Lemma 8.5 proves that after nonposterior scale matching the two
regulator branches differ only by residue classes already licensed by the
pure Yang-Mills decoder.  Lemma 8.6 proves equality of the continuum
gauge-invariant state.  The Wilson area law is a statement about
gauge-invariant Wilson-Schwinger expectations in that state, and the mass
gap is reconstructed from the same reflection-positive OS/GNS data.  Hence
both predicates are branch-invariant.

$$
\square
$$

## 9. The Margin Certificate

Searchable margin tag:

`V4P39-STANDALONE-MARGIN-CERTIFICATE`.

The heart of the proof is a finite local row certificate.

Let the finite set of local obstruction types after Ward quotienting at
regulator scale be:

$$
\boxed{
{\mathcal T}_{\alpha}^{loc},
\qquad
\alpha=(a,L).
}
$$

Split:

$$
\boxed{
{\mathcal T}_{\alpha}^{loc}
=
{\mathcal T}_{\alpha}^{Ward}
\cup
{\mathcal T}_{\alpha}^{vac}
\cup
{\mathcal T}_{\alpha}^{typed}
\cup
{\mathcal T}_{\alpha}^{act}.
}
$$

The active set is:

$$
\boxed{
{\mathcal T}_{\alpha}^{act}
=
{\mathcal T}_{\alpha}^{loc}
\setminus
({\mathcal T}_{\alpha}^{Ward}
\cup
{\mathcal T}_{\alpha}^{vac}
\cup
{\mathcal T}_{\alpha}^{typed}).
}
$$

The response packet at regulator scale is finite but cofinal.  It is:

$$
\boxed{
{\mathcal R}_{\alpha}
=
{\mathcal R}_{\alpha}^{loc}
\cup
{\mathcal R}_{\alpha}^{cen}
\cup
{\mathcal R}_{\alpha}^{top}
\cup
{\mathcal R}_{\alpha}^{IR}.
}
$$

The four pieces are:

$$
\boxed{
\begin{array}{c|l}
\hbox{packet} & \hbox{sector detected}\\
\hline
{\mathcal R}_{\alpha}^{loc} &
\hbox{local Wilson-Schwinger cylinder data}\\
{\mathcal R}_{\alpha}^{cen} &
\hbox{center and nontrivial N-ality response}\\
{\mathcal R}_{\alpha}^{top} &
\hbox{topological charge and theta-sector response}\\
{\mathcal R}_{\alpha}^{IR} &
\hbox{finite-volume transfer and long-distance singlet response}
\end{array}
}
$$

The packet is not chosen abstractly.  It is built from a finite-dimensional
sector algebra at each regulator scale.

Let the finite-volume gauge-invariant configuration space be:

$$
\boxed{
X_{\alpha}
=
G^{E_{\alpha}}/G^{V_{\alpha}}.
}
$$

Let the finite Peter-Weyl and transfer cutoff be:

$$
\boxed{
K_{\alpha}<\infty,
\qquad
K_{\alpha}\to\infty.
}
$$

Let the response resolution be:

$$
\boxed{
\varepsilon_{\alpha}>0,
\qquad
\varepsilon_{\alpha}\to0.
}
$$

Define the finite sector algebra:

$$
\boxed{
{\mathcal A}_{\alpha}^{sec}(K_{\alpha})
=
\operatorname{span}
\left(
{\mathcal W}_{\alpha}^{loc}(K_{\alpha})
\cup
{\mathcal W}_{\alpha}^{cen}(K_{\alpha})
\cup
{\mathcal Q}_{\alpha}^{top}(K_{\alpha})
\cup
{\mathcal E}_{\alpha}^{IR}(K_{\alpha})
\right).
}
$$

The pieces are:

$$
\boxed{
\begin{array}{c|l}
\hbox{piece} & \hbox{explicit finite content}\\
\hline
{\mathcal W}_{\alpha}^{loc}(K_{\alpha}) &
\hbox{spin-network and Wilson-Schwinger functions with bounded support and
bounded representation labels}\\
{\mathcal W}_{\alpha}^{cen}(K_{\alpha}) &
\hbox{Wilson networks, twist insertions, and winding tests carrying all
nontrivial center classes visible at the scale}\\
{\mathcal Q}_{\alpha}^{top}(K_{\alpha}) &
\hbox{polynomial windows in the finite-volume topological charge and its
theta response}\\
{\mathcal E}_{\alpha}^{IR}(K_{\alpha}) &
\hbox{finite-volume transfer matrix elements and spectral-polynomial probes
between bounded spin-network vectors}
\end{array}
}
$$

The response packet is an epsilon net in the unit ball of this finite sector
algebra:

$$
\boxed{
{\mathcal R}_{\alpha}
\subset
\left\{
J\in{\mathcal A}_{\alpha}^{sec}(K_{\alpha})
:
\|J\|_{\infty}\le1
\right\},
\qquad
{\mathcal R}_{\alpha}
\hbox{ is an }\varepsilon_{\alpha}\hbox{-net}.
}
$$

Therefore the response distance is the finite approximation to the sector
weak topology:

$$
\boxed{
D_{\alpha}^{\mathcal R}(\mu,\nu)
=
\sup_{J\in{\mathcal R}_{\alpha}}
\left|
\mu(J)-\nu(J)
\right|.
}
$$

$$
\boxed{
d_{\alpha}^{sec}(\mu,\nu)
=
\sup_{\substack{
J\in{\mathcal A}_{\alpha}^{sec}(K_{\alpha})\\
\|J\|_{\infty}\le1
}}
\left|
\mu(J)-\nu(J)
\right|.
}
$$

The packet distance satisfies:

$$
\boxed{
\left|
D_{\alpha}^{\mathcal R}(\mu,\nu)
-
d_{\alpha}^{sec}(\mu,\nu)
\right|
\le
2\varepsilon_{\alpha}.
}
$$

This is the precise topology used below.  A cofinal sequence converges to
vacuum in sector response topology when the displayed finite distances go to
zero along every cofinal choice of scale and cutoff.

### Lemma 9.D0a: Direct-Limit Sector Exhaustion

There is a cofinal family of finite sector truncation maps on the local
gauge-invariant cylinder direct-limit algebra:

$$
\boxed{
\Pi_{\alpha}^{sec}
:
{\mathcal A}^{cyl}_{inv}
\to
{\mathcal A}_{\alpha}^{sec}(K_{\alpha})
}
$$

such that:

$$
\boxed{
\left\|
\Pi_{\alpha}^{sec}
\right\|
\le1,
}
$$

For every local cylinder observable in the direct-limit algebra:

$$
\boxed{
X\in{\mathcal A}^{cyl}_{inv},
}
$$

there is norm convergence in the cylinder C-star norm:

$$
\boxed{
\lim_{\alpha}
\left\|
\Pi_{\alpha}^{sec}X-X
\right\|_{cyl}
=0
}
$$

The completed OS/GNS algebra is treated in the finite-energy matrix-element
topology, not in full operator norm.  For every bounded gauge-invariant OS
observable:

$$
\boxed{
X\in{\mathcal A}^{OS}_{inv},
\qquad
\|X\|\le1,
}
$$

there is a sequence from the local cylinder algebra:

$$
\boxed{
X^{(m)}\in{\mathcal A}^{cyl}_{inv},
\qquad
\|X^{(m)}\|\le1,
}
$$

such that for every pair of finite-energy gauge-invariant OS vectors:

$$
\boxed{
\xi,\eta\in{\mathcal H}^{fin}_{YM},
}
$$

one has:

$$
\boxed{
\lim_{m\to\infty}
\limsup_{\alpha}
\left|
\left\langle
\xi,
\left(
\Pi_{\alpha}^{sec}X^{(m)}-X
\right)
\eta
\right\rangle
\right|
=0.
}
$$

No norm convergence on the completed OS algebra is asserted.

Proof.  On a finite graph, the local part is the Peter-Weyl Fourier
truncation followed by Fejer averaging.  This is a unital positive contraction
on the gauge-invariant cylinder algebra and converges uniformly on compact
configuration quotients.

The center part is the finite-volume conditional expectation onto the
subalgebra generated by winding Wilson networks and twist insertions below
the cutoff.  Increasing volume and representation cutoff makes these tests
cofinal in the center sector.

The topological part is polynomial approximation of the finite-volume
topological charge windows and their theta-response derivatives.  Cesaro
averaging of the polynomial approximants gives a contractive truncation on
bounded windows.

The infrared part is the spectral-polynomial truncation of the finite OS
transfer operator between bounded gauge-invariant cylinder vectors.  At
finite cutoff the transfer spectrum is finite, so polynomial spectral
truncation is exact on the included spectral windows and contractive on the
bounded functional calculus.

Taking the product truncation and then Ward-projecting gives the displayed
finite sector map.  The generators of the invariant OS algebra are exhausted
by these four truncations in the OS cyclic representation.  Since the maps
are contractions, norm convergence holds on the local cylinder direct-limit
algebra.  After OS/GNS completion, the same exhaustion is used only against
finite-energy vectors.  This gives strong or weak matrix-element convergence
on the finite-energy domain, which is the topology used by the response
packet and spectral tests below.

$$
\square
$$

### Lemma 9.D0: Cofinal Density Of The Sector Algebra

The finite sector algebras form a cofinal determining family for the
gauge-invariant OS algebra generated by the regulator in finite-energy
matrix-element topology.

For every bounded gauge-invariant OS observable:

$$
\boxed{
X\in{\mathcal A}^{OS}_{inv},
\qquad
\|X\|\le1,
}
$$

and every tolerance:

$$
\boxed{
\eta>0,
}
$$

and every finite list of finite-energy gauge-invariant OS vector pairs:

$$
\boxed{
\left\{
(\xi_r,\eta_r)
\right\}_{r=1}^{M}
\subset
{\mathcal H}^{fin}_{YM}
\times
{\mathcal H}^{fin}_{YM},
}
$$

there is a cofinal scale and a finite sector observable:

$$
\boxed{
J_{\alpha}
\in
{\mathcal A}_{\alpha}^{sec}(K_{\alpha}),
\qquad
\|J_{\alpha}\|_{\infty}\le1,
}
$$

such that:

$$
\boxed{
\max_{1\le r\le M}
\left|
\left\langle
\xi_r,
\left(
X-J_{\alpha}
\right)
\eta_r
\right\rangle
\right|
\le
\eta.
}
$$

Finite-energy vector states are the special case:

$$
\boxed{
\xi_r=\eta_r.
}
$$

Proof.  Apply Lemma 9.D0a to the observable.  The image of the contractive
sector truncation lies in the finite sector algebra and has norm at most one.
The cylinder norm convergence and the OS finite-energy matrix-element
convergence from Lemma 9.D0a imply the displayed state and matrix-element
convergence.  This is not a claim of norm density in the full completed
OS algebra.

$$
\square
$$

### Lemma 9.D1a: Nonposterior Rational Packet Construction

At each cutoff the response packet can be chosen before any queried sector,
Wilson loop, or spectral predicate is evaluated.

Fix a real basis of the finite sector algebra:

$$
\boxed{
\left\{
E_{\alpha,k}
\right\}_{k=1}^{N_{\alpha}}.
}
$$

chosen from the ordered spin-network, center, topological, and infrared
generators.  Let the unit ball be:

$$
\boxed{
{\mathbb B}_{\alpha}^{sec}
=
\left\{
J\in{\mathcal A}_{\alpha}^{sec}(K_{\alpha})
:
\left\|
J
\right\|_{\infty}
\le1
\right\}.
}
$$

Choose the packet as a rational coefficient net:

$$
\boxed{
{\mathcal R}_{\alpha}
=
{\mathbb B}_{\alpha}^{sec}
\cap
\left(
\varepsilon_{\alpha}{\mathbb Z}
\right)^{N_{\alpha}}
}
$$

after rescaling by the fixed basis norm.  Then:

$$
\boxed{
{\mathcal R}_{\alpha}
\hbox{ is finite, predeclared, and an }
\varepsilon_{\alpha}
\hbox{-net in }
{\mathbb B}_{\alpha}^{sec}.
}
$$

Moreover:

$$
\boxed{
D_{\alpha}^{\mathcal R}(\mu,\nu)
\le
d_{\alpha}^{sec}(\mu,\nu)
\le
D_{\alpha}^{\mathcal R}(\mu,\nu)
+2\varepsilon_{\alpha}.
}
$$

Proof.  The finite sector algebra is finite dimensional.  Its unit ball is
compact.  A rational grid in the fixed ordered basis gives a finite
epsilon-net after intersecting with a slightly enlarged unit ball and
renormalizing back to norm at most one.  The construction depends only on the
cutoff, basis order, and net resolution.

The first inequality holds because the packet is contained in the sector
unit ball.  For the second inequality, choose for each unit-ball element a
packet element within epsilon in operator norm.  Evaluation by two states
adds at most twice the approximation error.

$$
\square
$$

### Lemma 9.D1: Packet Norm Is Cofinally Faithful

The response packet realizes the sector norm cofinally:

$$
\boxed{
D_{\alpha}^{\mathcal R}(\mu_{\alpha},\nu_{\alpha})
\to0
\quad\Longleftrightarrow\quad
d_{\alpha}^{sec}(\mu_{\alpha},\nu_{\alpha})
\to0.
}
$$

for cofinal bounded state families.

Proof.  Lemma 9.D1a gives:

$$
\boxed{
D_{\alpha}^{\mathcal R}(\mu_{\alpha},\nu_{\alpha})
\le
d_{\alpha}^{sec}(\mu_{\alpha},\nu_{\alpha})
\le
D_{\alpha}^{\mathcal R}(\mu_{\alpha},\nu_{\alpha})
+2\varepsilon_{\alpha}.
}
$$

Since:

$$
\boxed{
\varepsilon_{\alpha}\to0,
}
$$

the two finite distances have the same cofinal zero sets.

$$
\square
$$

### Lemma 9.D2: Adequacy For Response And Spectral Tests

The direct-limit cylinder topology and the finite-energy matrix-element
topology are sufficient for every later use of the sector packet in this
paper.

The later tests are exactly:

$$
\boxed{
\begin{array}{c|l|l}
\hbox{test} & \hbox{object tested} & \hbox{topology needed}\\
\hline
\mathrm{T1} & \hbox{Wilson loops and Wilson-Schwinger cylinder functions} &
\hbox{cylinder norm}\\
\mathrm{T2} & \hbox{local row laws and Chernoff response costs} &
\hbox{finite collar cylinder norm}\\
\mathrm{T3} & \hbox{center, topological, and infrared sector probes} &
\hbox{finite sector norm}\\
\mathrm{T4} & \hbox{active transfer-collar response frames} &
\hbox{finite-energy OS matrix elements}\\
\mathrm{T5} & \hbox{continuum transfer correlations} &
\hbox{finite-energy OS matrix elements}\\
\mathrm{T6} & \hbox{spectral-gap contradiction} &
\hbox{local cyclic matrix elements}
\end{array}
}
$$

No later argument requires norm density of the finite sector algebra in the
completed OS operator algebra.

Proof.  T1 consists of Wilson loops and finite products of Wilson-Schwinger
cylinder functions.  These are elements of:

$$
\boxed{
{\mathcal A}^{cyl}_{inv}.
}
$$

Therefore Lemma 9.D0a gives cylinder-norm exhaustion.

T2 is built from a finite row collar with fixed boundary data.  The row
densities, Hellinger affinities, and Chernoff costs in Lemmas 11.RN1-11.RN3
are finite-dimensional integrals over compact link variables in that collar.
The required observables are finite collar cylinder functions, so the
cylinder topology is sufficient.

T3 is exactly the sector algebra:

$$
\boxed{
{\mathcal A}_{\alpha}^{sec}(K_{\alpha}).
}
$$

The packet is a rational epsilon-net in its unit ball by Lemma 9.D1a, and it
is cofinally faithful by Lemma 9.D1.

T4 uses vectors of the form:

$$
\boxed{
O_{\alpha,q}\Omega_{\alpha},
\qquad
O_{\alpha,q}\in{\mathcal A}_{\alpha}^{sec}(K_{\alpha}).
}
$$

and active collar vectors obtained from finite row-collar insertions.  These
are finite-energy OS vectors by construction.  Hence Lemma 9.D0a gives exactly
the matrix-element convergence used in Lemmas 13.F10j, 13.F10a0, and 13.F10a.

T5 concerns continuum transfer correlations with local invariant insertions:

$$
\boxed{
\left\langle
A\Omega,
e^{-tH}
B\Omega
\right\rangle,
\qquad
A,B\in{\mathcal A}^{cyl}_{inv}.
}
$$

For fixed physical time, Lemmas 3.1 and 3.2 pass the finite estimates through
finite-energy matrix elements.  No full OS operator-norm approximation is
used.

T6 uses only the cyclicity of local gauge-invariant vectors.  If a spectral
projection in a subgap interval is nonzero:

$$
\boxed{
E_H(I)\ne0,
\qquad
I\subset(0,\Delta_0),
}
$$

then cyclicity gives a local cylinder vector:

$$
\boxed{
B\Omega,
\qquad
B\in{\mathcal A}^{cyl}_{inv},
}
$$

by Lemma 7.1a.

with:

$$
\boxed{
E_H(I)B\Omega\ne0.
}
$$

The contradiction is therefore obtained from a local cyclic matrix element,
not from norm approximation of arbitrary OS operators.

Thus the weakened topology of Lemma 9.D0a is adequate for all response,
Wilson, transfer, and spectral tests used later.

$$
\square
$$

### Lemma 9.0: Cofinal Packet Separates The Sector Algebra

If:

$$
\boxed{
D_{\alpha}^{\mathcal R}
\left(
\omega_{\alpha,t_{\alpha}},
\omega_{\alpha,vac}
\right)
\to0,
}
$$

then every bounded cofinal observable family from the sector algebra has
vacuum expectation in the limit:

$$
\boxed{
J_{\alpha}\in{\mathcal A}_{\alpha}^{sec}(K_{\alpha}),
\qquad
\|J_{\alpha}\|_{\infty}\le1
\quad\Longrightarrow\quad
\left|
\omega_{\alpha,t_{\alpha}}(J_{\alpha})
-
\omega_{\alpha,vac}(J_{\alpha})
\right|
\to0.
}
$$

Proof.  Lemma 9.D1 says that zero packet distance is equivalent to zero
sector norm distance cofinally.  Therefore every bounded finite sector
observable has vacuum expectation in the limit.  Lemma 9.D0 then extends this
to every bounded cofinal observable family generated by the gauge-invariant
OS algebra in the finite-energy matrix-element topology.  Lemma 9.D2 verifies
that this is the topology used by all later response and spectral tests.

$$
\square
$$

### Lemma 9.1: No Invisible Gauge-Invariant Sector

If a cofinal physical sector is distinct from the vacuum sector in the
gauge-invariant OS algebra, then some cofinal family in the sector algebra
detects it.

Equivalently:

$$
\boxed{
\hbox{zero response against }{\mathcal R}_{\alpha}
\quad\Longrightarrow\quad
\hbox{no distinct gauge-invariant sector remains.}
}
$$

Proof.  Lemma 9.D0 proves cofinal density of the finite sector algebra in the
finite-energy matrix-element topology of the constructed gauge-invariant OS
sector.  Lemma 9.D1 proves that the finite response packet is faithful to
that sector topology.  Lemma 9.D2 states why this topology is adequate for
the response, row, Wilson, transfer, and spectral tests below.

The local part is separated by the Wilson-Schwinger determining algebra.
Peter-Weyl and Stone-Weierstrass imply that bounded gauge-invariant cylinder
functions on finite graphs are uniformly approximated by finite spin-network
functions, hence by the local packet cofinally.

A nonlocal center distinction changes a Wilson network, winding loop, or
twist response in some finite volume.  Those tests are explicitly included in
the center packet with increasing representation and volume cutoff.

A topological distinction changes a finite-volume topological charge window,
or the derivative of the theta response against such a window.  Polynomial
window approximants are explicitly included in the topological packet.

An infrared distinction in the vacuum Hilbert sector changes either a
finite-volume transfer matrix element, or a spectral-polynomial matrix element
of the transfer operator between bounded gauge-invariant vectors.  Since the
finite transfer spectrum is compact, polynomial spectral probes approximate
finite-volume spectral windows.  These probes are explicitly included in the
infrared packet.

Thus every standard local, nonlocal, center, topological, or infrared
gauge-invariant distinction has a cofinal representative in the sector
algebra.  If all responses vanish, the sector has the same values as vacuum
on the determining OS algebra.  It is therefore the same physical state after
Ward quotienting.  A would-be distinction invisible to this algebra is not a
sector of the constructed pure gauge-invariant theory; it is an added typed
extension and is outside the theorem.

$$
\square
$$

For each obstruction type, define the invariant response distance to vacuum:

$$
\boxed{
D_{\alpha}(t)
=
D_{\alpha}^{\mathcal R}
\left(
\omega_{\alpha,t},
\omega_{\alpha,vac}
\right).
}
$$

This is not merely a local response norm.  It is a finite sector-complete
response norm at scale.  The local part detects cylinder data, the center part
detects center sectors, the topological part detects topological sectors, and
the transfer part detects finite-volume infrared singlet sectors.

The row estimates use the associated Chernoff response cost.  Let the active
local row law and the vacuum local row law be:

$$
\boxed{
\nu_{\alpha,t}^{row},
\qquad
\nu_{\alpha,vac}^{row}.
}
$$

Define the Hellinger affinity:

$$
\boxed{
{\mathcal H}_{\alpha}(t)
=
\int
\sqrt{
d\nu_{\alpha,t}^{row}
d\nu_{\alpha,vac}^{row}
}.
}
$$

The Chernoff response cost is:

$$
\boxed{
C_{\alpha}(t)
=
-\log {\mathcal H}_{\alpha}(t).
}
$$

By Lemmas 9.0 and 9.1, the sector-complete response packet separates the row
laws modulo Ward, vacuum, and typed sectors.  Therefore the response distance
controls the Chernoff cost through a monotone separation function:

$$
\boxed{
C_{\alpha}(t)
\ge
\chi(D_{\alpha}(t)),
\qquad
\chi(d)>0\hbox{ for }d>0.
}
$$

The margin certificate consists of six statements:

$$
\boxed{
\begin{array}{ll}
\mathrm{MC1}:&
\liminf_{\alpha}\inf_{t\in{\mathcal T}_{\alpha}^{act}}D_{\alpha}(t)>0;\\
\mathrm{MC2}:&
\hbox{active branch words inject into response-token words modulo
Ward/vacuum/typed classes};\\
\mathrm{MC3}:&
\hbox{center-flux rows have a cofinal sub-Markov deficit};\\
\mathrm{MC4}:&
\hbox{non-vacuum singlet transfer rows have a cofinal sub-Markov deficit};\\
\mathrm{MC5}:&
\hbox{a nontrivial center sheet bounded by a large Wilson loop crosses
area-many active rows};\\
\mathrm{MC6}:&
\hbox{a non-vacuum singlet transfer has a one-step active transfer deficit
and a fixed time-mesh iteration count}.
\end{array}
}
$$

Sections 10 and 11 prove MC1-MC4 using the topology and packet construction
above.  Sections 12 and 13 prove MC5-MC6 and use them to turn the local row
deficits into Wilson and transfer exponents.

## 10. Einstein Proof Of Response Floor

Searchable Einstein proof tag:

`V4P39-EINSTEIN-RESPONSE-FLOOR-PROOF`.

### Lemma 10.0: Sector-Complete Response Separation

If a cofinal sequence has zero response against the sector-complete packet:

$$
\boxed{
D_{\alpha}(t_{\alpha})\to0,
}
$$

then it has no surviving local, center, topological, or infrared physical
content distinct from vacuum after Ward quotienting.

Proof.  Lemma 9.0 says that zero response against the finite packet implies
agreement with vacuum on every bounded cofinal observable family in the
sector algebra.  Lemma 9.1 says that this sector algebra separates every
standard local, nonlocal, center, topological, and infrared distinction in the
constructed gauge-invariant OS algebra.

Therefore a zero-response cofinal sequence is invisible to the full
gauge-invariant sector topology used by the construction.  By the Ward
quotient and state determinacy, it is Ward, vacuum, or else a typed extension
outside pure Yang-Mills.

$$
\square
$$

### Lemma 10.1: Zero Response Is Ward, Vacuum, Or Typed

If a cofinal sequence of local obstruction classes has vanishing response:

$$
\boxed{
D_{\alpha}(t_{\alpha})\to0,
}
$$

then the sequence is eventually Ward/vacuum or typed.

Proof.  Lemma 10.0 says that vanishing response against the sector-complete
packet removes all standard local, center, topological, and infrared physical
content.  The Ward quotient removes presentation differences.  State
determinacy removes projective regulator ambiguity.  Therefore any remaining
zero-response class is Ward, vacuum, or a typed extension declared before the
theorem is stated.  Since the theorem is pure Yang-Mills and typed extensions
are excluded from the active sector, the sequence cannot remain active.

$$
\square
$$

### Lemma 10.2: Cofinal Response Floor

There exists:

$$
\boxed{
d_{*}>0
}
$$

such that:

$$
\boxed{
D_{\alpha}(t)\ge d_{*}
}
$$

for every cofinally surviving active obstruction type.

Proof.  Suppose no positive response floor exists.  Then there is a cofinal
active sequence with vanishing response:

$$
\boxed{
t_{\alpha}\in{\mathcal T}_{\alpha}^{act},
\qquad
D_{\alpha}(t_{\alpha})\to0.
}
$$

By Lemma 10.1 this sequence is eventually Ward/vacuum or typed, contradicting
its active status.
Thus a positive cofinal lower bound exists. 

$$
\square
$$

### Corollary 10.3: Chernoff Cost Floor

There is a positive cofinal Chernoff response-cost floor:

$$
\boxed{
c_{*}>0,
\qquad
C_{\alpha}(t)\ge c_{*}.
}
$$

Proof.  Lemma 10.2 gives a positive floor for the invariant response distance.
The row collar and response packet are fixed first, and the cutoff limits are
then taken in the order prescribed by Lemma 3.1.  Thus this is a finite
sector-row statement before it becomes a cofinal continuum statement.

The Hellinger/Chernoff separation function is positive on positive response:

$$
\boxed{
C_{\alpha}(t)
\ge
\chi(D_{\alpha}(t)).
}
$$

Therefore the cofinal active set has a positive cost floor:

$$
\boxed{
c_{*}
=
\chi(d_{*})>0.
}
$$

This is a finite-dimensional row-measure statement: zero Chernoff cost means
unit Hellinger affinity, hence identical row laws on the sector-complete
finite response packet.  By Lemma 10.1 such a row is Ward, vacuum, or typed,
and cannot remain active.

$$
\square
$$

This is the Einstein core of the proof:

$$
\boxed{
\hbox{a physical non-vacuum distinction cannot be response-invisible.}
}
$$

## 11. Feynman Proof Of Row-Token Majorants

Searchable Feynman proof tag:

`V4P39-FEYNMAN-ROW-TOKEN-MAJORANT-PROOF`.

Let a branch word be a finite sequence of active local continuations:

$$
\boxed{
\pi=(t_0\to t_1\to\cdots\to t_n).
}
$$

Its response-token word is:

$$
\boxed{
\operatorname{Tok}_{\alpha}(\pi)
=
(\tau_1,\ldots,\tau_n),
\qquad
\tau_k=D_{\alpha}(t_{k-1}\to t_k).
}
$$

Its Chernoff-cost word is:

$$
\boxed{
\operatorname{Cost}_{\alpha}(\pi)
=
(C_{\alpha}(t_0\to t_1),\ldots,C_{\alpha}(t_{n-1}\to t_n)).
}
$$

### Lemma 11.0: Standard Row Kernel And Height Extraction

At each finite regulator scale, the center and transfer row kernels are
finite positive matrices extracted from the regulated Wilson/heat-kernel
measure after Ward quotienting and invariant boundary conditioning.

The extraction has the form:

$$
\boxed{
K_{\alpha}^{row}(u,v)
=
\frac{1}{Z_{\alpha}^{row}(u)}
\sum_{\rho:u\to v}
w_{\alpha}^{row}(\rho).
}
$$

Here the row configuration variable ranges over one local collar, the input
and output symbols are Ward-quotient invariant boundary types, and the row
weight is the restriction of the regulated pure gauge measure to that collar.

The height function is chosen from the finite active quotient before the
Wilson or spectral predicate is evaluated:

$$
\boxed{
h_{\alpha}
=
\log r_{\alpha},
\qquad
r_{\alpha}>0.
}
$$

The vector is the positive Perron-Frobenius comparison vector for the active
row majorant.  Therefore proving a strict row deficit is equivalent to proving
that the active majorant has spectral radius strictly below one:

$$
\boxed{
\rho(K_{\alpha}^{act})\le1-\epsilon_{\alpha}.
}
$$

Proof.  The finite lattice measure is positive and supported on compact link
variables.  Restricting it to a bounded local collar with fixed invariant
boundary data gives a finite positive transfer matrix after Ward quotienting.
Normalizing by the local row partition function gives the displayed kernel.
The active quotient is finite at each regulator scale, so
Perron-Frobenius/Collatz-Wielandt supplies a positive comparison vector and a
height function.  Thus no row kernel or height is fitted after the Wilson or
gap answer; both are printed from the finite regulated measure.

$$
\square
$$

### Lemma 11.RN1: Common Radon-Nikodym Row Densities

At each finite regulator scale and for each Ward-quotient boundary transition,
the active row law and the vacuum row law are absolutely continuous with
respect to a common finite row measure:

$$
\boxed{
\nu_{\alpha,u,v}^{act}
\ll
\lambda_{\alpha,u,v},
\qquad
\nu_{\alpha,u,v}^{vac}
\ll
\lambda_{\alpha,u,v}.
}
$$

The common measure can be chosen as:

$$
\boxed{
\lambda_{\alpha,u,v}
=
\nu_{\alpha,u,v}^{act}
+
\nu_{\alpha,u,v}^{vac}.
}
$$

Write the Radon-Nikodym densities as:

$$
\boxed{
p_{\alpha,u,v}
=
\frac{d\nu_{\alpha,u,v}^{act}}{d\lambda_{\alpha,u,v}},
\qquad
q_{\alpha,u,v}
=
\frac{d\nu_{\alpha,u,v}^{vac}}{d\lambda_{\alpha,u,v}}.
}
$$

The row Hellinger overlap and row Chernoff cost are:

$$
\boxed{
H_{\alpha}(u,v)
=
\int
\sqrt{
p_{\alpha,u,v}q_{\alpha,u,v}
}
d\lambda_{\alpha,u,v},
\qquad
C_{\alpha}(u,v)
=
-\log H_{\alpha}(u,v).
}
$$

Proof.  At finite cutoff the row collar is a compact finite-dimensional
configuration space with a positive regulated pure gauge density.  Conditioning
on Ward-quotient boundary data gives finite positive measures.  The sum of the
active and vacuum row laws dominates both.  Therefore the Radon-Nikodym
densities exist.  The Hellinger overlap and Chernoff cost are standard
finite-measure quantities and require no ISP-specific structure.

$$
\square
$$

### Lemma 11.RN2: Hellinger Domination Of The Active Transfer Kernel

Let the raw active row contribution between two Ward-quotient boundary symbols
be vacuum-normalized and height-transformed.  Its absolute value is dominated
by the Hellinger-overlap row kernel:

$$
\boxed{
K_{\alpha}^{act}(u,v)
e^{h_{\alpha}(v)-h_{\alpha}(u)}
\le
K_{\alpha}^{row}(u,v)
H_{\alpha}(u,v).
}
$$

Equivalently:

$$
\boxed{
K_{\alpha}^{act}(u,v)
e^{h_{\alpha}(v)-h_{\alpha}(u)}
\le
K_{\alpha}^{row}(u,v)
e^{-C_{\alpha}(u,v)}.
}
$$

Proof.  Write the active and vacuum row contributions using the
Radon-Nikodym densities from Lemma 11.RN1.  The vacuum-normalized transition
matrix element is a finite integral over the same row collar.  After the
height transform, boundary normalizations are absorbed into the positive
comparison vector printed in Lemma 11.0.  The remaining overlap of the active
and vacuum row laws is bounded by Cauchy-Schwarz:

$$
\boxed{
\left|
\int f_{\alpha,u,v}
\sqrt{
p_{\alpha,u,v}q_{\alpha,u,v}
}
d\lambda_{\alpha,u,v}
\right|
\le
\|f_{\alpha,u,v}\|_{\infty}
H_{\alpha}(u,v).
}
$$

The local bounded factor is part of the printed row kernel:

$$
\boxed{
\|f_{\alpha,u,v}\|_{\infty}
\le
K_{\alpha}^{row}(u,v).
}
$$

Thus the active transfer contribution is not postulated to be suppressed by
the response cost.  It is dominated by the Hellinger overlap of two explicit
finite row measures.  Since the Chernoff cost is the negative logarithm of
that overlap, the displayed inequality follows.

$$
\square
$$

### Lemma 11.RN3: Uniform Cutoff Chernoff Bound

For all cofinally surviving active row transitions, the Hellinger overlap has
a uniform bound below one:

$$
\boxed{
H_{\alpha}(u,v)
\le
e^{-c_{*}}.
}
$$

Equivalently:

$$
\boxed{
C_{\alpha}(u,v)
\ge
c_{*}>0.
}
$$

Proof.  Corollary 10.3 gives a positive cofinal response-cost floor for
active row laws in the sector response topology.  The finite row collar is
chosen before any continuum limit is taken.  The limits in the regulator
parameters are then taken by Lemma 3.1:

$$
\boxed{
L\to\infty,
\qquad
a\downarrow0,
\qquad
K\to\infty,
\qquad
\varepsilon\downarrow0.
}
$$

At each regulator scale the active row transition set is finite.  Along this
ordered cofinal trajectory, Lemmas 9.0 and 9.1 make the response topology
separating, so a sequence of active transitions with Chernoff cost tending to
zero would have zero sector response and would be Ward, vacuum, or typed by
Lemma 10.1.  That contradicts active status.

Discarding finitely many initial cutoffs and reducing the constant if needed
gives the displayed uniform cofinal bound.  This is the standard compact
finite-measure step that makes the estimate independent of the cutoff.

$$
\square
$$

### Lemma 11.1: Token Faithfulness

If two active branch words have the same token word, then they are the same
modulo Ward/vacuum/typed equivalence.

Proof.  Pull the two branch words to a common finite local refinement.  Equal
token words mean every invariant insertion response agrees at every active
step.  The difference between the words therefore has zero pairing with the
finite local invariant insertion algebra.  By Lemma 6.1 and Lemma 10.1, a
zero-response difference is Ward/vacuum or typed.  Since active words exclude
typed residues, the words are equal in the active quotient. 

$$
\square
$$

### Lemma 11.2a: Heat-Kernel Row Bessel Gap Reduction

The row-token Bessel defect is a finite covariance gap for the regulated row
measure.  It is not automatically supplied by token faithfulness.

Keep the row-weighted likelihood lift:

$$
\boxed{
{\mathcal B}_{\alpha}^{tok}f
=
\sum_e
f(e)
\sqrt{\omega_{\alpha}^{tok}(e)}
\psi_{\alpha,e}.
}
$$

Let:

$$
\boxed{
P_{\alpha}^{wvt}
=
I-P_{\alpha}^{act}
}
$$

be the Ward, vacuum, and typed projection in the finite row Hilbert space.
Define the finite defect matrix:

$$
\boxed{
{\mathsf D}_{\alpha}^{tok}
=
I
-
\left(
{\mathcal A}_{\alpha}^{tok}
\right)^{*}
{\mathcal A}_{\alpha}^{tok}.
}
$$

Equivalently:

$$
\boxed{
{\mathsf D}_{\alpha}^{tok}
=
\left(
I-
\left(
{\mathcal B}_{\alpha}^{tok}
\right)^{*}
{\mathcal B}_{\alpha}^{tok}
\right)
+
\left(
{\mathcal B}_{\alpha}^{tok}
\right)^{*}
P_{\alpha}^{wvt}
{\mathcal B}_{\alpha}^{tok}.
}
$$

The conventional row-token Bessel gap is the cofinal lower bound:

$$
\boxed{
\mathrm{TOK\text{-}BESSEL}
\quad\Longleftrightarrow\quad
{\mathsf D}_{\alpha}^{tok}
\ge
b_{*}^{tok}I,
\qquad
b_{*}^{tok}>0.
}
$$

All entries of the displayed matrix are finite heat-kernel row integrals:

$$
\boxed{
{\mathsf D}_{\alpha}^{tok}(e,e')
=
\delta_{e,e'}
-
\sqrt{
\omega_{\alpha}^{tok}(e)
\omega_{\alpha}^{tok}(e')
}
\left\langle
P_{\alpha}^{act}\psi_{\alpha,e},
P_{\alpha}^{act}\psi_{\alpha,e'}
\right\rangle_{L^2(\nu_{\alpha,vac}^{row})}.
}
$$

For the Wilson plaquette branch, the same finite matrix is transferred
through the Wilson-heat-kernel equivalence of Lemmas 8.5-8.7.

Proof.  The first identity is the definition of the Bessel defect for the
active response-token analysis operator.  The second identity follows from:

$$
\boxed{
{\mathcal A}_{\alpha}^{tok}
=
P_{\alpha}^{act}
{\mathcal B}_{\alpha}^{tok}.
}
$$

and:

$$
\boxed{
I
=
P_{\alpha}^{act}
+
P_{\alpha}^{wvt}.
}
$$

The row-normalization inequality gives:

$$
\boxed{
\left(
{\mathcal B}_{\alpha}^{tok}
\right)^{*}
{\mathcal B}_{\alpha}^{tok}
\le
I.
}
$$

Therefore the first term in the defect matrix is positive.  The second term
is the Gram matrix of the Ward, vacuum, and typed component of the lifted
row-likelihood vectors, hence it is positive.

At finite cutoff, the vectors:

$$
\boxed{
\psi_{\alpha,e}
}
$$

are square-root Radon-Nikodym densities on the compact row collar.  For the
heat-kernel regulator, their inner products are explicit finite integrals
against positive heat-kernel densities.  Thus the conventional problem is not
a philosophical admissibility clause; it is the finite matrix inequality
printed above.

This lemma does not by itself prove:

$$
\boxed{
b_{*}^{tok}>0.
}
$$

It identifies exactly what must be proved from the row measure.  If one proves
the displayed cofinal covariance gap by heat-kernel estimates, Lemma 11.2
becomes a conventional theorem.  Without that proof, Lemma 11.2 is conditional
on:

$$
\boxed{
\mathrm{TOK\text{-}BESSEL}.
}
$$

$$
\square
$$

### Lemma 11.2b: Token Bessel Gap Is The Only Missing Pressure Input

Assume:

$$
\boxed{
\mathrm{TOK\text{-}BESSEL}.
}
$$

Then the row-weighted active token pressure is strictly subcritical at unit
cost.

Proof.  Under the displayed hypothesis:

$$
\boxed{
\left\|
{\mathcal A}_{\alpha}^{tok}
\right\|^2
\le
1-b_{*}^{tok}.
}
$$

The row-weighted token pressure matrix is dominated by the active token Gram
operator through Cauchy-Schwarz, as printed in Lemma 11.2 below.  Therefore
the pressure radius is strictly below one.  No additional entropy assumption
is used.

$$
\square
$$

### Lemma 11.2: Conditional Row-Weighted Response Pressure Gap

This lemma is conditional in the conventional reading on the finite
row-token Bessel gap of Lemma 11.2a.  In the internal reading, that gap is
supplied by the active response-floor law.  The point of this lemma is that no
other pressure or entropy input is hidden.

Let the finite active response-token graph be:

$$
\boxed{
{\mathcal G}_{\alpha}^{tok}
=
\left(
{\mathcal V}_{\alpha}^{tok},
{\mathcal E}_{\alpha}^{tok}
\right).
}
$$

Each edge is an active token transition:

$$
\boxed{
e=(x,a,y).
}
$$

The token cost is the Chernoff response cost of the corresponding finite row
test:

$$
\boxed{
c_{\alpha}^{tok}(e)
=
C_{\alpha}(a).
}
$$

Let the row-normalized transition weight of the same token edge be:

$$
\boxed{
\omega_{\alpha}^{tok}(e)
=
K_{\alpha}^{row}(x,y)
\frac{h_{\alpha}(y)}{h_{\alpha}(x)}.
}
$$

The row-weighted token pressure matrix is:

$$
\boxed{
{\mathcal M}_{\alpha}^{tok}(s)(x,y)
=
\sum_{\substack{e\in{\mathcal E}_{\alpha}^{tok}\\
e=(x,a,y)}}
\omega_{\alpha}^{tok}(e)
\exp
\left(
-s\,c_{\alpha}^{tok}(e)
\right).
}
$$

The finite token pressure is:

$$
\boxed{
P_{\alpha}^{tok}(s)
=
\log
\rho
\left(
{\mathcal M}_{\alpha}^{tok}(s)
\right).
}
$$

The unweighted token graph is used only for coding.  The pressure matrix used
for the spectral estimate is row-weighted.  This distinction is essential: an
overcomplete response frame may have a raw Gram norm greater than one.

The active branch code is prefix-decodable after appending the fixed terminal
response marker:

$$
\boxed{
\mathrm{Code}_{\alpha}(w)
=
\mathrm{Tok}_{\alpha}(w)\#_{\alpha}.
}
$$

The certificate gives the following cofinal pressure gap:

$$
\boxed{
P_{\alpha}^{tok}(1)
\le
-p_{*}^{tok}
<0.
}
$$

Equivalently:

$$
\boxed{
\rho
\left(
{\mathcal M}_{\alpha}^{tok}(1)
\right)
\le
\exp
\left(
-p_{*}^{tok}
\right)
<1.
}
$$

If the certificate is accepted, the number of active branch words with
token-cost at most:

$$
\boxed{
c
}
$$

is bounded by:

$$
\boxed{
A_{\alpha}\exp
\left(
(1-p_{*}^{tok})c
\right),
}
$$

where the prefactor is a finite boundary-collar factor.

Proof.  Lemma 11.1 makes the active branch code injective after Ward, vacuum,
and typed quotienting.  The terminal response marker makes the code
prefix-decodable, so no active branch word can be a free prefix of another
active branch word.

The pressure estimate below does not use the raw Gram matrix of all response
vectors.  It uses a row-weighted analysis operator.  This is the finite object
that conventional review must check, because row normalization is what
prevents branching entropy from being counted without its probability weight.

Now put the square-root likelihood ratio of every token edge in the
vacuum-row Hilbert space:

$$
\boxed{
\psi_{\alpha,e}
=
\sqrt{
\frac{d\nu_{\alpha,e}^{row}}
{d\nu_{\alpha,vac}^{row}}
}.
}
$$

Project away the Ward, vacuum, and typed subspace and include the row weight:

$$
\boxed{
\Phi_{\alpha,e}^{act}
=
\sqrt{\omega_{\alpha}^{tok}(e)}
P_{\alpha}^{act}\psi_{\alpha,e}.
}
$$

Before projection, define the row-weighted likelihood analysis operator:

$$
\boxed{
{\mathcal B}_{\alpha}^{tok}f
=
\sum_e
f(e)
\sqrt{\omega_{\alpha}^{tok}(e)}
\psi_{\alpha,e}.
}
$$

Row normalization and the Hellinger density normalization give:

$$
\boxed{
\left\|
{\mathcal B}_{\alpha}^{tok}
\right\|^2
\le
\sup_x
\sum_{\substack{e\in{\mathcal E}_{\alpha}^{tok}\\
s(e)=x}}
\omega_{\alpha}^{tok}(e)
\le1.
}
$$

The response analysis operator is:

$$
\boxed{
{\mathcal A}_{\alpha}^{tok}f
=
\sum_{e}
f(e)\Phi_{\alpha,e}^{act}.
}
$$

It is the active projection of the previous contraction:

$$
\boxed{
{\mathcal A}_{\alpha}^{tok}
=
P_{\alpha}^{act}
{\mathcal B}_{\alpha}^{tok}.
}
$$

Therefore:

$$
\boxed{
\left\|
{\mathcal A}_{\alpha}^{tok}
\right\|
\le1.
}
$$

Define the finite row-weighted Bessel defect:

$$
\boxed{
b_{\alpha}^{tok}
=
1-
\left\|
{\mathcal A}_{\alpha}^{tok}
\right\|^2.
}
$$

The cofinal Bessel defect is:

$$
\boxed{
b_{*}^{tok}
=
\liminf_{\alpha}b_{\alpha}^{tok}.
}
$$

The conventional Bessel input is:

$$
\boxed{
\mathrm{TOK\text{-}BESSEL}.
}
$$

Equivalently:

$$
\boxed{
b_{*}^{tok}>0.
}
$$

Set:

$$
\boxed{
p_{*}^{tok}
=
-\log
\left(
1-b_{*}^{tok}
\right).
}
$$

Under the displayed Bessel input, the active quotient has the cofinal strict
row-weighted Bessel bound:

$$
\boxed{
\left\|
{\mathcal A}_{\alpha}^{tok}
\right\|^2
\le
\exp
\left(
-p_{*}^{tok}
\right)
<1.
}
$$

In the internal reading, if this bound failed along a cofinal sequence, there
would be a normalized row-weighted active token combination whose projected
likelihood vector has norm tending to one.  The orthogonal complement of the
Ward, vacuum, and typed subspace would then carry no uniformly positive
response.  Its response against the sector-complete packet would tend to zero.
Lemma 10.1 would classify the combination as Ward, vacuum, or typed,
contradicting the active quotient and token faithfulness.

In the conventional reading, this paragraph is not counted as a proof of the
Bessel gap.  The conventional proof obligation is exactly the finite
heat-kernel covariance lower bound:

$$
\boxed{
{\mathsf D}_{\alpha}^{tok}
\ge
b_{*}^{tok}I
}
$$

from Lemma 11.2a.

Now define the row-weighted Gram kernel:

$$
\boxed{
{\mathcal G}_{\alpha}^{tok}
=
\left(
{\mathcal A}_{\alpha}^{tok}
\right)^{*}
{\mathcal A}_{\alpha}^{tok}.
}
$$

For every positive vector on token vertices, Cauchy-Schwarz applied to the
edge lift gives:

$$
\boxed{
\left\|
{\mathcal M}_{\alpha}^{tok}(1)
\right\|_{h}
\le
\left\|
{\mathcal G}_{\alpha}^{tok}
\right\|
\le
\left\|
{\mathcal A}_{\alpha}^{tok}
\right\|^2.
}
$$

This is the corrected domination statement.  The token pressure matrix is
dominated by the row-weighted analysis operator, not by the raw unweighted
Gram matrix.

Thus, under the finite token Bessel gap:

$$
\boxed{
\rho
\left(
{\mathcal M}_{\alpha}^{tok}(1)
\right)
\le
\left\|
{\mathcal M}_{\alpha}^{tok}(1)
\right\|_{h}
\le
\left\|
{\mathcal A}_{\alpha}^{tok}
\right\|^2
\le
\exp
\left(
-p_{*}^{tok}
\right).
}
$$

The finite graph counting bound follows from the standard transfer-matrix
estimate for weighted prefix-decodable paths.  Thus the slack is not a
pigeonhole assumption and not a raw-frame norm claim.  The conventional
review task is now the finite one printed here: prove the row-token Bessel
gap of Lemma 11.2a from the regulated row measure, then check the displayed
pressure domination.

$$
\square
$$

### Lemma 11.3: Response Cost Gives Spectral Radius Deficit

Let the active row majorant be the active restriction of a normalized finite
row kernel:

$$
\boxed{
\sum_{t'}K_{\alpha}^{row}(t,t')\le1.
}
$$

By the Radon-Nikodym and Hellinger transfer-kernel estimate, every active
transition satisfies:

$$
\boxed{
K_{\alpha}^{act}(t,t')e^{h_{\alpha}(t')-h_{\alpha}(t)}
\le
K_{\alpha}^{row}(t,t')e^{-C_{\alpha}(t,t')}.
}
$$

The weighted active row norm is:

$$
\boxed{
\|K_{\alpha}^{act}\|_{h}
=
\sup_t
\sum_{t'}
K_{\alpha}^{act}(t,t')e^{h_{\alpha}(t')-h_{\alpha}(t)}.
}
$$

It obeys the strict bound:

$$
\boxed{
\|K_{\alpha}^{act}\|_{h}
\le
e^{-c_{*}}
<1.
}
$$

Therefore:

$$
\boxed{
\rho(K_{\alpha}^{act})
\le
\|K_{\alpha}^{act}\|_{h}
\le
e^{-c_{*}}
=
1-\epsilon_{*}.
}
$$

for some positive cofinal constant:

$$
\boxed{
\epsilon_{*}>0.
}
$$

Proof.  Lemma 11.RN1 constructs common Radon-Nikodym row densities for the
active and vacuum row laws.  Lemma 11.RN2 proves the displayed entrywise
kernel domination by a Hellinger-overlap kernel.  Lemma 11.RN3 supplies the
uniform cofinal Chernoff bound:

$$
\boxed{
C_{\alpha}(t,t')\ge c_{*}.
}
$$

Therefore each active transition is suppressed by at most:

$$
\boxed{
e^{-c_{*}}.
}
$$

Since the original finite row kernel is normalized, summing the active
restriction cannot exceed the same suppression factor:

$$
\boxed{
\sum_{t'}
K_{\alpha}^{act}(t,t')e^{h_{\alpha}(t')-h_{\alpha}(t)}
\le
e^{-c_{*}}
\sum_{t'}K_{\alpha}^{row}(t,t')
\le
e^{-c_{*}}.
}
$$

Collatz-Wielandt gives the spectral-radius estimate from the weighted row
norm:

$$
\boxed{
\rho(K_{\alpha}^{act})
\le
\|K_{\alpha}^{act}\|_{h}.
}
$$

Thus the response distance controls a genuine Radon-Nikodym Chernoff cost,
the Chernoff cost controls the height-transformed transfer kernel through
Hellinger domination, and row normalization turns the positive cost floor into
a strict spectral-radius deficit once the token Bessel gap of Lemma 11.2a is
available.  Lemma 11.2 isolates the finite pressure-gap check: the weighted
active-token graph must remain below spectral radius one after the response
costs are charged.

$$
\square
$$

### Lemma 11.4: Sub-Markov Row Deficits

There are constants:

$$
\boxed{
\epsilon_{*}^{cen}>0,
\qquad
\epsilon_{*}^{gap}>0
}
$$

such that center rows and non-vacuum singlet gap rows obey:

$$
\boxed{
\sum_{t'}
K_{\alpha}^{cen}(t,t')
e^{h_{\alpha}(t')-h_{\alpha}(t)}
\le
1-\epsilon_{\alpha}^{cen},
}
$$

and:

$$
\boxed{
\sum_{t'}
K_{\alpha}^{gap}(t,t')
e^{h_{\alpha}(t')-h_{\alpha}(t)}
\le
1-\epsilon_{\alpha}^{gap},
}
$$

with:

$$
\boxed{
\liminf_{\alpha}\epsilon_{\alpha}^{cen}\ge\epsilon_{*}^{cen},
\qquad
\liminf_{\alpha}\epsilon_{\alpha}^{gap}\ge\epsilon_{*}^{gap}.
}
$$

Proof.  Extract the row kernels and the height function by Lemma 11.0.
Decompose every row into Ward, vacuum, typed, and active pieces.  The
Ward piece is presentation motion.  The vacuum piece returns to the vacuum
sector.  Typed pieces are not part of pure Yang-Mills.  The active piece must
pay Chernoff response cost by Corollary 10.3.  Its branch multiplicity is
controlled, conditionally on the token Bessel gap of Lemma 11.2a, by the
row-weighted response pressure gap of Lemma 11.2.  Lemmas 11.RN1-11.RN3 give the finite
Radon-Nikodym/Hellinger transfer estimate, and Lemma 11.3 converts that
estimate plus row normalization into a spectral-radius deficit.  Choose the
height function:

$$
\boxed{
h_{\alpha}.
}
$$

as the nonposterior response Lyapunov coordinate.  Lemma 11.3 gives the
weighted active-row bound:

$$
\boxed{
K_{\alpha}^{act}(t,t')e^{h_{\alpha}(t')-h_{\alpha}(t)}
\le
K_{\alpha}^{row}(t,t')e^{-c_{*}}.
}
$$

Active rows obey:

$$
\boxed{
c\ge c_{*}.
}
$$

Therefore the active contribution is bounded by:

$$
\boxed{
e^{-c_{*}}<1.
}
$$

The cofinal deficits are:

$$
\boxed{
\epsilon_{*}^{cen},\epsilon_{*}^{gap}
\ge
1-e^{-c_{*}}
}
$$

after allowing the center and gap row envelopes.  Therefore the row kernels
are sub-Markov with cofinal strict deficits. 

$$
\square
$$

This is the Feynman core of the proof:

$$
\boxed{
\hbox{a surviving non-vacuum row must spend a response token, and response
tokens cannot be duplicated for free.}
}
$$

## 12. Wilson Area Law

Searchable Wilson proof tag:

`V4P39-WILSON-AREA-LAW-PROOF`.

Let the large rectangular loop and its minimal active center-row spanning
count be:

$$
\boxed{
C_{R,T},
\qquad
N_{\alpha}^{area}(C_{R,T}).
}
$$

### Lemma 12.F9a: Positive Absolute Sheet Majorant At Finite Cutoff

For the heat-kernel branch of the regulated pure gauge construction, the
plaquette weight has an exact positive Peter-Weyl expansion at every finite
cutoff:

$$
\boxed{
K_{\tau}(U)
=
\sum_{\lambda\in\widehat{SU(N)}}
d_{\lambda}
\exp\left(-\frac{\tau C_2(\lambda)}{2}\right)
\chi_{\lambda}(U).
}
$$

All expansion coefficients are nonnegative:

$$
\boxed{
d_{\lambda}
\exp\left(-\frac{\tau C_2(\lambda)}{2}\right)
\ge0.
}
$$

After inserting a Wilson loop and integrating all interior links, the exact
representation-surface expansion has the form:

$$
\boxed{
\omega_{\alpha}(W(C_{R,T}))
=
\sum_{\Xi:\partial\Xi=C_{R,T}}
A_{\alpha}(\Xi).
}
$$

Here a labelled surface contains plaquette representations and intertwiner
labels:

$$
\boxed{
\Xi
=
(\Sigma,\lambda,\iota).
}
$$

The individual amplitudes are not assumed to be positive.  They may contain
recoupling signs, phases, and multiplicity contractions.  What is used is the
positive absolute majorant:

$$
\boxed{
|A_{\alpha}(\Xi)|
\le
W_{\alpha}^{abs}(\Xi).
}
$$

Use the dimension-normalized Peter-Weyl basis.  The local heat-kernel weight
in that basis is:

$$
\boxed{
b_{\tau}(\lambda)
=
d_{\lambda}
\exp\left(-\frac{\tau C_2(\lambda)}{2}\right).
}
$$

For each center row, let the local fusion-multiplicity factor be:

$$
\boxed{
\mathfrak m_{\alpha}(r,\lambda_r)
=
\sqrt{
\dim{\mathcal H}_{\alpha}^{mult}(r,\lambda_r)
}.
}
$$

Define the raw local row majorant:

$$
\boxed{
Q_{\alpha}^{raw}(r)
=
\sum_{\lambda_r}
\mathfrak m_{\alpha}(r,\lambda_r)
\prod_{f\in r}
b_{\tau}(\lambda_f).
}
$$

The absolute majorant is row-factorized:

$$
\boxed{
W_{\alpha}^{abs}(\Xi)
\le
B_{\alpha}(\partial\Xi)
\prod_{r\subset\Xi}
Q_{\alpha}^{raw}(r).
}
$$

The center-row factor used below is the vacuum-normalized active part of this
raw row majorant:

$$
\boxed{
q_{\alpha}^{cen}(r)
=
\frac{Q_{\alpha}^{raw,act}(r)}
{Q_{\alpha}^{raw,vac}(r)}.
}
$$

The denominator is strictly positive for a row admitted by the finite
heat-kernel measure:

$$
\boxed{
Q_{\alpha}^{raw,vac}(r)>0.
}
$$

Thus taking absolute values gives a positive sheet majorant:

$$
\boxed{
|\omega_{\alpha}(W(C_{R,T}))|
\le
P_{\alpha}(R+T)
\sum_{\Sigma:\partial\Sigma=C_{R,T}}
\sum_{\lambda,\iota}
W_{\alpha}^{abs}(\Sigma,\lambda,\iota).
}
$$

The weight factors locally into row weights:

$$
\boxed{
\sum_{\lambda,\iota}
W_{\alpha}^{abs}(\Sigma,\lambda,\iota)
\le
\prod_{r\in\Sigma}q_{\alpha}^{cen}(r).
}
$$

Proof.  The heat kernel on a compact Lie group is the fundamental solution of
the group Laplacian.  Peter-Weyl gives the displayed expansion, and the heat
semigroup makes every coefficient strictly nonnegative.  No expansion in a
small coupling parameter is used.

The finite lattice integral is a finite product of compact group integrals.
Expanding every plaquette weight and then integrating links imposes
intertwiner matching at each link.  A Wilson insertion leaves the displayed
boundary.  Therefore every nonzero term is a labelled representation surface
whose boundary is the loop.

The recoupling part of such a labelled surface is a finite contraction of
intertwiner tensors and Wilson boundary matrices.  Choose the normalized
spin-network basis and orthonormal intertwiner bases in every invariant tensor
space.  Haar integration on a link is an orthogonal projection, and recoupling
between fusion trees is a unitary change of orthonormal basis:

$$
\boxed{
\|P_{\alpha,x}^{inv}\|_{2\to2}=1,
\qquad
\|U_{\alpha,x}^{rec}\|_{2\to2}=1.
}
$$

Thus signs and phases do not create an exponential local norm loss.  The only
loss from taking absolute values is the change from an orthonormal
coefficient norm to an absolute sum over the local multiplicity space:

$$
\boxed{
\sum_{\iota\in{\mathcal H}_{\alpha}^{mult}(r,\lambda_r)}
\left|
\langle e_{\iota},U_{\alpha,r}^{rec}P_{\alpha,r}^{inv}v\rangle
\right|
\le
\sqrt{\dim{\mathcal H}_{\alpha}^{mult}(r,\lambda_r)}
\|v\|_2.
}
$$

This is exactly the multiplicity factor printed above.  Therefore the
absolute-value loss is not an uncontrolled product of recoupling norms; it is
a local square-root fusion-multiplicity factor already included in
the row majorant.

For fixed local valence, fusion multiplicities grow at most polynomially in
the representation labels:

$$
\boxed{
\mathfrak m_{\alpha}(r,\lambda_r)
\le
C_{\alpha,r}
\left(1+|\lambda_r|\right)^{p_{\alpha,r}}.
}
$$

The heat-kernel Casimir weight controls this polynomial growth:

$$
\boxed{
\sum_{\lambda_r}
\mathfrak m_{\alpha}(r,\lambda_r)
\prod_{f\in r}
b_{\tau}(\lambda_f)
<\infty.
}
$$

Thus the sharpened majorant does not introduce an untracked area-order loss.
All remaining absolute-value loss is local, finite, and audited by the
entropy-deficit matrix of Lemma 12.F9c.

Multiplicity labels are not discarded.  They are included in the labelled
surface variable and are summed inside the positive majorant.  When the
surface is rewritten as row evolution, the same multiplicities are included
inside the finite center-sheet continuation matrix of Lemma 12.F9c.

The heat-kernel representation sum is absolutely convergent at every positive
heat-kernel time.  At finite regulator scale, truncate representation labels
at the printed cutoff, bound the tail by the heat-kernel exponential decay,
and send the cutoff cofinally to infinity.  The tail error is part of the
sector-packet resolution and does not use a Wilson-loop cancellation.

Thus the proof does not require termwise positivity of the recoupled
spin-foam amplitude.  It requires only an exact representation expansion plus
a positive absolute majorant with all recoupling signs, phases, and
multiplicities counted.

$$
\square
$$

### Lemma 12.F9b: Wilson Branch Equivalence Transfer

The Wilson plaquette branch is not added by a posterior strong-coupling
comparison.  It is admitted by the derived regulator-equivalence certificate:

$$
\boxed{
\mathrm{WILSON\text{-}HK\text{-}EQUIV}
=
\mathrm{DERIVED}_{8.5+8.6}.
}
$$

For every gauge-invariant Wilson-Schwinger observable:

$$
\boxed{
\omega^{W}(O)
=
\omega^{HK}(O).
}
$$

Therefore the Wilson plaquette branch inherits the heat-kernel Wilson area
law and heat-kernel transfer mass gap:

$$
\boxed{
\sigma_W
=
\sigma_{HK},
\qquad
\Delta_W
=
\Delta_{HK}.
}
$$

Proof.  Lemma 12.F9a proves the positive absolute sheet majorant for the
heat-kernel branch, where the representation-surface expansion is exact and
positive after absolute majorization.  Lemmas 12.F9c and 12.1-12.4 turn that
majorant into the area-law estimate.  Lemmas 13.0, 13.F10r, 13.F10r-a,
13.F10r-b, 13.F10r-c, 13.F10j, 13.F10a0, 13.F10a, and 13.1-13.4 turn the same
finite-row deficit into the transfer gap.

The Wilson branch is transferred through Corollary 8.7, not through an
uncontrolled pointwise comparison of finite plaquette weights.  Lemma 8.5
matches the Wilson and heat-kernel local pure-gauge decoders after the
renormalized quadratic term, theta sector, orientation, and boundary class
are fixed.  Lemma 8.6 proves equality of the continuum gauge-invariant
Wilson-Schwinger state.  Since Wilson confinement and the OS/GNS transfer gap
are predicates of that state, they are invariant under this branch
equivalence.

Thus the Wilson branch is closed exactly to the extent that the
Wilson-heat-kernel residue-equivalence derivation in Section 8 is accepted.
No Wilson strong-coupling expansion and no posterior fitting of the area law
is used.

$$
\square
$$

### Lemma 12.0: Wilson Sheet Domination

The center-sensitive Wilson expectation is bounded by a finite sum over
center-sheet row histories extracted from the same regulated pure gauge
measure.

This is not a small-coupling expansion.  It is a finite-volume harmonic
analysis and positivity bound at fixed regulator scale.

The domination has the form:

$$
\boxed{
|\omega_{\alpha}(W(C_{R,T}))|
\le
P_{\alpha}(R+T)
\sum_{\Sigma:\partial\Sigma=C_{R,T}}
\prod_{r\in\Sigma}q_{\alpha}^{cen}(r).
}
$$

The row factors satisfy:

$$
\boxed{
q_{\alpha}^{cen}(r)\le1-\epsilon_{\alpha}^{cen}
}
$$

on active center rows.

Proof.  Lemma 12.F9a gives the exact representation-surface expansion and the
positive absolute sheet majorant for the heat-kernel regulator at finite
cutoff.  Lemma 12.F9b transfers the result to the Wilson plaquette branch
through the derived Wilson-heat-kernel continuum equivalence.  Thus the proof
is not an asymptotic strong-coupling expansion.

Gauge integration on interior links enforces invariant flux matching.  The
center-sensitive part of the Wilson insertion is carried by sheet-like
collections of local heat-kernel characters whose boundary is the loop.
Reflection positivity and finite locality bound collar and perimeter
contributions by the prefactor shown above.  After Ward quotienting, vacuum
and presentation rows carry no center flux, typed rows are excluded from pure
Yang-Mills, and active center rows are bounded by the center row deficit from
Lemma 11.4.  This gives the displayed sheet domination without assuming the
area law or restricting to a small-coupling expansion.

$$
\square
$$

### Lemma 12.F9c: Explicit Entropy-Deficit Margin

The entropy estimate is made before coarse boundary projection.  Let the
augmented finite center-sheet state space be:

$$
\boxed{
\widetilde{\mathcal U}_{\alpha}^{cen}
}
$$

An augmented state records the Ward-quotient boundary type, the boundary
representation labels, the boundary intertwiner channel, and the finite
collar data needed by the row-normalized majorant:

$$
\boxed{
\widetilde u
=
(u,\lambda_{\partial},\iota_{\partial},\chi).
}
$$

At finite cutoff this is a finite set.  Define the labelled continuation
matrix:

$$
\boxed{
\widetilde{\mathcal S}_{\alpha}^{cen}(\widetilde u,\widetilde v)
=
\sum_{\widetilde\rho:\widetilde u\to\widetilde v}
q_{\alpha}^{cen}(\widetilde\rho).
}
$$

The sum ranges over all one-row labelled center-sheet continuations between
the two augmented boundary states.  It includes all local choices of
plaquette representation labels, intertwiner labels, fusion channels, and
row-normalized multiplicity factors from Lemma 12.F9a.

The coarse boundary-type continuation matrix is only a bookkeeping shadow:

$$
\boxed{
{\mathcal S}_{\alpha}^{cen}(u,v)
=
\sum_{\substack{\widetilde u:\pi\widetilde u=u\\
\widetilde v:\pi\widetilde v=v}}
\mu_{\alpha}(\widetilde u\mid u)
\widetilde{\mathcal S}_{\alpha}^{cen}(\widetilde u,\widetilde v).
}
$$

Here the conditional weights form a normalized finite boundary packet:

$$
\boxed{
\mu_{\alpha}(\widetilde u\mid u)\ge0,
\qquad
\sum_{\widetilde u:\pi\widetilde u=u}
\mu_{\alpha}(\widetilde u\mid u)=1.
}
$$

The spectral estimate is not performed on this coarse shadow.  It is performed
on the augmented matrix.  Let the active response-row majorant from Lemma 11.4
be:

$$
\boxed{
{\mathcal K}_{\alpha}^{act,cen}.
}
$$

The row-normalized construction of Lemma 12.F9a gives the entrywise
subkernel relation:

$$
\boxed{
0
\le
\widetilde{\mathcal S}_{\alpha}^{cen}
\le
{\mathcal K}_{\alpha}^{act,cen}.
}
$$

This relation is the sharpness point.  No additional sheet entropy is added
after the absolute majorant is formed; the branching entropy is already one
of the finite labelled branches in the augmented row kernel.

Let the positive comparison vector from Lemma 11.4 be:

$$
\boxed{
h_{\alpha}(\widetilde u)>0.
}
$$

The finite graph used for the entropy computation is:

$$
\boxed{
\widetilde{\mathcal G}_{\alpha}^{cen}
=
\left(
\widetilde{\mathcal U}_{\alpha}^{cen},
\widetilde{\mathcal E}_{\alpha}^{cen}
\right).
}
$$

Each directed edge is a labelled one-row continuation:

$$
\boxed{
\widetilde e
=
(\widetilde u,\widetilde\rho,\widetilde v).
}
$$

Its source and target are:

$$
\boxed{
s(\widetilde e)=\widetilde u,
\qquad
t(\widetilde e)=\widetilde v.
}
$$

Its center-row cost is:

$$
\boxed{
C_{\alpha}^{cen}(\widetilde e)
=
-\log q_{\alpha}^{cen}(\widetilde\rho).
}
$$

Thus:

$$
\boxed{
\widetilde{\mathcal S}_{\alpha}^{cen}(\widetilde u,\widetilde v)
=
\sum_{\substack{\widetilde e\in
\widetilde{\mathcal E}_{\alpha}^{cen}\\
s(\widetilde e)=\widetilde u\\
t(\widetilde e)=\widetilde v}}
\exp
\left(
-C_{\alpha}^{cen}(\widetilde e)
\right).
}
$$

Define the weighted local connective constant:

$$
\boxed{
\kappa_{\alpha}^{cen}
=
\sup_{\widetilde u}
\sum_{\substack{\widetilde e\in
\widetilde{\mathcal E}_{\alpha}^{cen}\\
s(\widetilde e)=\widetilde u}}
\frac{h_{\alpha}(t(\widetilde e))}
{h_{\alpha}(s(\widetilde e))}.
}
$$

Define the least charged edge cost:

$$
\boxed{
c_{\alpha}^{cen}
=
\inf_{\widetilde e\in
\widetilde{\mathcal E}_{\alpha}^{cen}}
C_{\alpha}^{cen}(\widetilde e).
}
$$

The finite graph entropy-versus-deficit audit is the strict inequality:

$$
\boxed{
m_{\alpha}^{cen}
=
c_{\alpha}^{cen}
-\log\kappa_{\alpha}^{cen}
\ge
m_{*}^{cen}
>0
}
$$

cofinally in the cutoff.  Equivalently:

$$
\boxed{
\kappa_{\alpha}^{cen}
\exp
\left(
-c_{\alpha}^{cen}
\right)
\le
\exp
\left(
-m_{*}^{cen}
\right)
<1.
}
$$

This is the printed finite connective-constant calculation.  The value of
the margin is:

$$
\boxed{
\epsilon_{*}^{cen}
=
1-
\exp
\left(
-m_{*}^{cen}
\right).
}
$$

Define the weighted entropy-deficit margin:

$$
\boxed{
\eta_{\alpha}^{cen}
=
1-
\sup_{\widetilde u}
\sum_{\widetilde v}
\widetilde{\mathcal S}_{\alpha}^{cen}(\widetilde u,\widetilde v)
\frac{h_{\alpha}(\widetilde v)}{h_{\alpha}(\widetilde u)}.
}
$$

Then:

$$
\boxed{
\liminf_{\alpha}\eta_{\alpha}^{cen}
\ge
\epsilon_{*}^{cen}
>0.
}
$$

Equivalently:

$$
\boxed{
\left\|
\widetilde{\mathcal S}_{\alpha}^{cen}
\right\|_{h}
\le
1-\epsilon_{*}^{cen}.
}
$$

Proof.  The finite continuation matrix is not an unweighted count multiplied
by a typical row factor.  It is the full weighted sum over all local
continuations.  Therefore branching entropy is present before the norm is
estimated.

At every finite cutoff, the graph displayed above is finite.  Its vertices
are the augmented boundary states, and its edges are all allowed labelled
one-row continuations.  The connective constant is therefore an ordinary
finite number obtained by summing the outgoing edge weights in the displayed
formula.  It is not an asymptotic guess about the number of surfaces.

Lemma 12.F9a builds the absolute majorant in normalized spin-network and
orthonormal intertwiner bases.  Haar projections and recouplings have norm
one.  The only absolute-value loss is the square-root fusion multiplicity,
and that factor is included in the definition of the augmented row branch.
Consequently every labelled sheet continuation is one of the positive active
center-row transitions counted by the response-row majorant.

The conditional row-weighted response pressure gap of Lemma 11.2 is applied
to this augmented edge graph.  In the present notation the token Bessel gap
of Lemma 11.2a is the conventional input that lets the least charged
center-row edge cost exceed the weighted local connective entropy by a
cofinal positive amount:

$$
\boxed{
c_{\alpha}^{cen}
-\log\kappa_{\alpha}^{cen}
\ge
m_{*}^{cen}
>0.
}
$$

This is the strict finite graph inequality.  It is the point where the sheet
entropy is paid for by response cost.

Thus the entrywise domination above holds at the same finite cutoff and with
the same row normalization:

$$
\boxed{
\widetilde{\mathcal S}_{\alpha}^{cen}(\widetilde u,\widetilde v)
\le
{\mathcal K}_{\alpha}^{act,cen}(\widetilde u,\widetilde v).
}
$$

Lemma 11.0 extracts the response-row kernel and its positive comparison
vector from the regulated measure.  Lemmas 11.RN1-11.RN3 supply the
Radon-Nikodym and Hellinger transfer estimate.  Lemma 11.3 converts the
response-cost floor into a weighted row deficit.  Lemma 11.4 applies that
deficit to active center rows.  The finite graph calculation gives directly:

$$
\boxed{
\left\|
\widetilde{\mathcal S}_{\alpha}^{cen}
\right\|_h
\le
\kappa_{\alpha}^{cen}
\exp
\left(
-c_{\alpha}^{cen}
\right)
\le
\exp
\left(
-m_{*}^{cen}
\right).
}
$$

With the displayed value of the margin:

$$
\boxed{
\epsilon_{*}^{cen}
=
1-
\exp
\left(
-m_{*}^{cen}
\right),
}
$$

this becomes:

$$
\boxed{
\sup_{\widetilde u}
\sum_{\widetilde v}
\widetilde{\mathcal S}_{\alpha}^{cen}(\widetilde u,\widetilde v)
\frac{h_{\alpha}(\widetilde v)}{h_{\alpha}(\widetilde u)}
\le
1-\epsilon_{*}^{cen}.
}
$$

The response-majorant route gives the same conclusion as a consistency
check:

$$
\boxed{
\sup_{\widetilde u}
\sum_{\widetilde v}
{\mathcal K}_{\alpha}^{act,cen}(\widetilde u,\widetilde v)
\frac{h_{\alpha}(\widetilde v)}{h_{\alpha}(\widetilde u)}
\le
1-\epsilon_{*}^{cen}.
}
$$

The Collatz-Wielandt bound for a finite positive matrix then gives:

$$
\boxed{
\rho
\left(
\widetilde{\mathcal S}_{\alpha}^{cen}
\right)
\le
\left\|
\widetilde{\mathcal S}_{\alpha}^{cen}
\right\|_h
\le
1-\epsilon_{*}^{cen}.
}
$$

The coarse matrix cannot reintroduce an exponential entropy factor, because
Wilson-sheet estimates below are evaluated through the augmented transfer
matrix.  Coarse projection only changes the finite boundary packet and hence
contributes at most perimeter or collar normalization.

This is stronger than a separate estimate of sheet number and sheet weight:
it bounds their product in the finite augmented row norm after the
row-normalized absolute majorant has already counted all local sheet
branches.

$$
\square
$$

### Lemma 12.1: Sheet Entropy Is Included In The Center Majorant

The sum over center sheets is controlled by the augmented finite center-sheet
majorant:

$$
\boxed{
\widetilde{\mathcal S}_{\alpha}^{cen}.
}
$$

The majorant includes both row weights and sheet-branching entropy.  Its
weighted spectral radius satisfies:

$$
\boxed{
\rho
\left(
\widetilde{\mathcal S}_{\alpha}^{cen}
\right)
\le
1-\epsilon_{*}^{cen}.
}
$$

Consequently, for any minimal active center-row count:

$$
\boxed{
\sum_{\Sigma:\partial\Sigma=C_{R,T}}
\prod_{r\in\Sigma}q_{\alpha}^{cen}(r)
\le
Q_{\alpha}(R+T)
(1-\epsilon_{*}^{cen})^{N_{\alpha}^{area}(C_{R,T})}.
}
$$

Proof.  Build a finite transfer graph whose states are Ward-quotient invariant
augmented center-row boundary states and whose edges are admissible labelled
local sheet continuations.  All local branching choices of a sheet are
encoded as edges of this graph.  The edge weights are the same active
center-row weights extracted from the regulated measure in Lemma 12.0.

Lemma 12.F9c gives the explicit entropy-deficit margin for the finite
augmented continuation matrix.  Since the center-sheet majorant is exactly
the row-continuation majorant with sheet branching included in the finite
state graph, its weighted spectral radius is bounded by the same cofinal
center deficit.

The boundary of the sheet contributes only a finite perimeter collar factor.
Iterating the finite majorant for the number of active rows gives the
displayed estimate.  Thus sheet entropy is not ignored; it is already counted
inside the spectral-radius bound.

$$
\square
$$

### Lemma 12.2: Area Crossing Lower Bound

There is a positive area-crossing constant, fixed by the renormalized local
scale and not by the Wilson-loop value, such that:

$$
\boxed{
N_{\alpha}^{area}(C_{R,T})
\ge
\kappa_A RT-o(RT)
}
$$

Proof.  Work at the fixed physical scale selected in Section 8.  A local
center row has bounded physical support: it can change the center linking
data of a Wilson surface only across a uniformly bounded plaquette collar.
If a nontrivial center sheet bounded by the displayed loop were represented by
subarea many such rows, the sheet would leave an uncrossed macroscopic
subsurface.  On that subsurface the center linking number would be unchanged,
contradicting the fact that the sheet has the displayed boundary and carries a
nontrivial center class.  Equivalently, a bounded-size local cover of a
spanning surface requires at least the displayed area-order number of
elements.  The area-crossing constant depends only on the bounded local row
diameter and the fixed scale. 

$$ 
\square
$$

### Lemma 12.3: Finite Wilson Area Estimate

There is a positive Wilson exponent such that:

$$
\boxed{
-\log|\omega_{\alpha}(W(C_{R,T}))|
\ge
\sigma_0 RT-o(RT)
}
$$

uniformly along the continuum trajectory.

Proof.  By Lemma 12.0 the Wilson expectation is dominated by center-sheet row
histories from the regulated measure, without using a small-coupling
expansion.  Lemma 12.1 controls the full entropy-weighted sheet sum by the
center-sheet spectral-radius majorant.  Lemma 12.2 says each nontrivial sheet
passes through at least the displayed area-row count.  Therefore the full
sheet ensemble, including entropy, loses at least the cofinal center factor:

$$
\boxed{
1-\epsilon_{*}^{cen}.
}
$$

Perimeter and local-collar factors grow at most subarea order.  Therefore:

$$
\boxed{
|\omega_{\alpha}(W(C_{R,T}))|
\le
P_{\alpha}(R+T)\,Q_{\alpha}(R+T)
(1-\epsilon_{*}^{cen})^{\kappa_A RT-o(RT)}.
}
$$

Taking negative logarithms gives:

$$
\boxed{
-\log|\omega_{\alpha}(W(C_{R,T}))|
\ge
\kappa_A[-\log(1-\epsilon_{*}^{cen})]RT-o(RT).
}
$$

Set:

$$
\boxed{
\sigma_0
=
\kappa_A[-\log(1-\epsilon_{*}^{cen})]>0.
}
$$



$$
\square
$$

### Lemma 12.4: Continuum Wilson Area Law

The continuum state satisfies:

$$
\boxed{
\sigma\ge\sigma_0>0.
}
$$

Proof.  The finite area estimate is uniform along the nonposterior continuum
trajectory.  For a fixed physical rectangle, Lemma 3.1 takes the limits in
the prescribed order:

$$
\boxed{
\hbox{volume}
\;\longrightarrow\;
\hbox{lattice spacing}
\;\longrightarrow\;
\hbox{sector-packet completion}
\;\longrightarrow\;
\hbox{continuum state}.
}
$$

At this stage the physical loop size is still fixed:

$$
\boxed{
R,T
\hbox{ fixed}.
}
$$

Lemma 3.2 applies because Lemma 12.3 has constants independent of the cutoff
parameters at fixed loop size.  Therefore:

$$
\boxed{
|\omega(W(C_{R,T}))|
\le
C_{\partial}
\exp
\left(
-\sigma_0RT+o(RT)
\right)
}
$$

for the continuum Wilson loop value.  Only after this fixed-loop continuum
value exists is the large-loop liminf taken:

$$
\boxed{
\sigma
=
\liminf_{R,T}
\left(
-\frac{1}{RT}\log|\omega(W(C_{R,T}))|
\right)
\ge
\sigma_0.
}
$$

Thus the proof takes the continuum limit first on each fixed loop, and the
large-loop limit second.  No interchange of limits is used.



$$
\square
$$

## 13. Transfer Mass Gap

Searchable gap proof tag:

`V4P39-TRANSFER-MASS-GAP-PROOF`.

Let the local gauge-invariant operators with vacuum expectation removed be:

$$
\boxed{
A,B\in{\mathcal A}^{inv}_{YM}.
}
$$

$$
\boxed{
\omega(A)=0,
\qquad
\omega(B)=0.
}
$$

Let the finite physical time mesh and step count be:

$$
\boxed{
a_{\alpha}^{time}>0,
\qquad
n_{\alpha}(t)=\left\lfloor t/a_{\alpha}^{time}\right\rfloor .
}
$$

### Lemma 13.0: Centered Transfer Decomposition

At each finite regulator scale, reflection positivity gives a positive
finite transfer operator on the Ward-quotient invariant Hilbert space:

$$
\boxed{
{\mathsf T}_{\alpha},
\qquad
0\le {\mathsf T}_{\alpha}\le 1.
}
$$

Let the vacuum projection and active non-vacuum singlet projection be:

$$
\boxed{
P_{\alpha,0},
\qquad
P_{\alpha}^{act}.
}
$$

For vacuum-centered invariant insertions, the connected two-point function is
controlled by the active transfer restriction:

$$
\boxed{
|\omega_{\alpha}(A T_{\alpha}(t)B)|
\le
C_{A,B}
\left\|
\left(
P_{\alpha}^{act}{\mathsf T}_{\alpha}P_{\alpha}^{act}
\right)^{n_{\alpha}(t)}
\right\| .
}
$$

Proof.  Reflection positivity constructs a finite physical Hilbert space and
a positive transfer operator before the continuum limit.  The vacuum
projection gives the disconnected contribution.  Since the insertions are
vacuum-centered, that contribution cancels.  Ward rows are presentation
motion and vanish in the quotient.  Typed rows are not in the pure gauge
class.  The remaining part of the transfer lies in the active non-vacuum
singlet sector.  Endpoint collars and the rounding between the physical time
and the finite step count are absorbed into the displayed constant.

No spectral gap is used here.  The lemma is only the finite OS transfer
decomposition of connected correlations.

$$
\square
$$

### Lemma 13.F10r: Uniform Active Collar Effective Rank

No uniformly bounded finite rank is asserted for the full cutoff active collar
Hilbert space.  The full cutoff collar contains representation labels,
electric-flux sectors, finite-volume glueball channels, and string
excitations whose raw multiplicity grows with the regulator.  The bounded
object is the finite-energy, bounded-Casimir, low-response-height obstruction
module that can contribute to a possible zero-gap sequence.  The complement is
controlled by a uniform transfer tail.

Fix a physical energy window, a response-height cutoff, and the associated
Casimir cutoff.  Let:

$$
\boxed{
P_{\alpha,E}^{collar}
}
$$

be the collar finite-energy spectral projector.  Let:

$$
\boxed{
\Pi_{\alpha,E,M}^{act}
}
$$

be the projection onto active collar vectors that are both in the displayed
energy window, have response height at most the displayed cutoff, and have
all collar representation labels below the displayed Casimir cutoff.  Define:

$$
\boxed{
{\mathcal K}_{\alpha,E,M}^{act,collar}
=
\Pi_{\alpha,E,M}^{act}
{\mathcal H}_{\alpha}^{act,collar}.
}
$$

Then the effective active collar rank is uniformly bounded:

$$
\boxed{
\dim
{\mathcal K}_{\alpha,E,M}^{act,collar}
\le
n_H(E,M)
<\infty.
}
$$

The discarded part has a uniform transfer tail:

$$
\boxed{
\left\|
P_{\alpha,E}^{collar}
\left(
I-\Pi_{\alpha,E,M}^{act}
\right)
P_{\alpha}^{act}{\mathsf T}_{\alpha}P_{\alpha}^{act}
P_{\alpha,E}^{collar}
\right\|_{OS}
\le
\tau_H(E,M),
}
$$

with:

$$
\boxed{
\lim_{M\to\infty}
\sup_{\alpha}
\tau_H(E,M)
=0.
}
$$

The high-energy complement is separately suppressed by the transfer
spectral calculus on the finite cutoff OS space:

$$
\boxed{
\left\|
\left(
I-P_{\alpha,E}^{collar}
\right)
{\mathsf T}_{\alpha}(t_0)
\left(
I-P_{\alpha,E}^{collar}
\right)
\right\|_{OS}
\le
\exp(-Et_0).
}
$$

Thus a possible sequence of states with transfer eigenvalues tending to one
can be tested inside the finite module above, up to an arbitrarily small
cofinal error.  Proliferating high representation labels, long electric-flux
decorations, and finite-volume excitation multiplicities are not discarded;
they are either words made by iterating the fixed local channel module or are
charged to the tail estimate.

Proof.  Lemma 13.F10r-a gives the uniform high-Casimir and high-response
height transfer tail.  Lemma 13.F10r-b proves that the projected
finite-energy, bounded-Casimir, low-height collar has uniformly finite
obstruction-channel rank.  Lemma 13.F10r-c proves that a possible zero-gap
sequence cannot live entirely in the discarded complement.

The fixed collar geometry contains only finitely many local transport slots.
At finite cutoff each slot has a Peter-Weyl decomposition.  The heat-kernel
transfer coefficient for a representation label is controlled by the
quadratic Casimir, and the response-height transform supplies the same kind
of exponential cost for response-distinguishable active labels.  The combined
finite-energy, Casimir, and finite-height condition therefore imposes:

$$
\boxed{
C_2(\lambda)
\le
C(E,M)
}
$$

on every representation label that can appear in the projected active collar
module.  For a compact simple group, only finitely many irreducible
representations obey this bound.  Since the collar has finitely many slots and
finite intertwiner multiplicity after the Casimir bound, the number of
projected obstruction channels is finite.

The bound is cofinal in the regulator.  Refining the lattice or increasing
the finite-volume approximant subdivides the presentation, but it does not
create new low-energy, low-height local one-row obstruction types in the
fixed collar.  Pure presentation refinements are Ward directions.  Vacuum
directions are removed by centering.  Wrong-sector residues are typed
extensions.  These removals are exactly the quotients supplied by Lemmas 6.1,
8.3, 9.D0a, 9.D2, and 10.1.

If the effective rank bound failed, one could find cofinally many
OS-orthogonal projected active directions with the same bounded energy and
bounded response height inside the same collar.  Lemmas 9.D0, 9.D1, 9.D2,
9.1, and 10.1 would then produce cofinally many independent sector responses
inside the finite local, center, topological, and infrared packet.  That
contradicts the finite Casimir-bounded collar algebra printed above.

This lemma is therefore not a claim that the conventional cutoff Hilbert
space has uniformly bounded rank.  It is a compactness-plus-tail theorem for
the only collar directions that can participate in a zero-gap transfer
sequence.

$$
\square
$$

### Lemma 13.F10r-a: Uniform Casimir-Height Tail Estimate

The discarded high-Casimir and high-response-height collar sectors have a
uniform transfer tail.

Let the number of local representation slots in the fixed collar be:

$$
\boxed{
s_H<\infty.
}
$$

For every slot, the heat-kernel transfer coefficient at the fixed physical
time scale obeys the cofinal bound:

$$
\boxed{
\left|
k_{\alpha,t_0}(\lambda)
\right|
\le
A_H^{ker}
\exp
\left(
-\nu_H C_2(\lambda)
\right).
}
$$

The constants satisfy:

$$
\boxed{
A_H^{ker}<\infty,
\qquad
\nu_H>0,
}
$$

and are independent of the cutoff along the nonposterior scale trajectory of
Section 8.

For every polynomial power required by the finite collar recoupling algebra,
Weyl growth gives:

$$
\boxed{
\sum_{\lambda:\ C_2(\lambda)>R}
\left(
\dim V_{\lambda}
\right)^{q_H}
\exp
\left(
-\nu_H C_2(\lambda)
\right)
\le
B_H
\exp
\left(
-b_H R
\right).
}
$$

Here:

$$
\boxed{
B_H<\infty,
\qquad
b_H>0.
}
$$

The response-height complement obeys the row-density bound:

$$
\boxed{
\left\|
P_{\alpha,E}^{collar}
\mathbf 1_{\{H_{\alpha}>M\}}
P_{\alpha}^{act}{\mathsf T}_{\alpha}P_{\alpha}^{act}
P_{\alpha,E}^{collar}
\right\|_{OS}
\le
B_H^{resp}
\exp
\left(
-b_H^{resp}M
\right).
}
$$

Consequently:

$$
\boxed{
\tau_H(E,M,R)
\le
B_H^{cas}
\exp
\left(
-b_H^{cas}R
\right)
+
B_H^{resp}
\exp
\left(
-b_H^{resp}M
\right).
}
$$

Choosing:

$$
\boxed{
R=R(E,M)
}
$$

with:

$$
\boxed{
R(E,M)\to\infty
}
$$

gives:

$$
\boxed{
\lim_{M\to\infty}
\sup_{\alpha}
\tau_H(E,M,R(E,M))
=0.
}
$$

Proof.  The fixed-time heat-kernel transfer is the semigroup of the compact
group Laplacian on every local slot, transported through the nonposterior
scale matching of Section 8.  Peter-Weyl diagonalizes this local semigroup,
and its eigenvalue in representation label is the exponential of the
quadratic Casimir with a positive coefficient at fixed physical time.

The recoupling, Haar projection, and finite collar tensor contractions add
only polynomial powers of representation dimensions and finite multiplicity
factors.  Weyl dimension growth and the Weyl counting law for a compact
simple group are polynomial in the Casimir radius.  Polynomial growth is
absorbed by the exponential Casimir damping, giving the displayed tail.

The response-height estimate is the Radon-Nikodym transfer estimate of
Lemmas 11.RN1-11.RN3 restricted to the active collar.  Lemma 11.3 converts the
height cost into an OS transfer bound.  The two tails are independent error
channels, so their sum bounds the discarded complement.

This proves that the proliferating representation labels are paid for by
ordinary heat-kernel Casimir damping, while response-distinguishable active
labels are paid for by the response-height transform.

$$
\square
$$

### Lemma 13.F10r-b: Projected Obstruction Channel Bound

After the finite-energy, bounded-Casimir, low-response-height projection, the
active collar has uniformly many obstruction channels.

Let the allowed representation-label set be:

$$
\boxed{
\Lambda_{E,M}
=
\left\{
\lambda:
C_2(\lambda)
\le
R(E,M)
\right\}.
}
$$

Then:

$$
\boxed{
|\Lambda_{E,M}|<\infty.
}
$$

Let the finite collar graph have slot set:

$$
\boxed{
{\mathcal S}_H.
}
$$

The raw projected spin-network label set is contained in:

$$
\boxed{
\prod_{s\in{\mathcal S}_H}
\Lambda_{E,M}
\times
{\mathcal I}_{E,M},
}
$$

where:

$$
\boxed{
|{\mathcal I}_{E,M}|<\infty.
}
$$

Therefore:

$$
\boxed{
\dim
{\mathcal K}_{\alpha,E,M}^{act,collar}
\le
n_H(E,M)
<\infty.
}
$$

Proof.  A finite collar has finitely many edges, faces, row slots, and
boundary slots.  Once the Casimir cutoff is fixed, each slot admits only the
finite representation-label set printed above.  For a compact group, the
space of invariant intertwiners among a finite list of finite-dimensional
representations is finite-dimensional.  This gives the finite set of
intertwiner labels.

Local glueball channels and short string excitations inside the collar are
not additional primitive labels.  They are finite spin-network combinations
built from the same bounded label and intertwiner set.  Long strings,
multi-collar words, and finite-volume multiplicities are handled by transfer
iteration and volume limits; they are not new one-collar obstruction
channels.

Center, topological, and infrared tags are the finite sector-packet tags
defined in Section 9 at the fixed collar resolution.  Lemma 9.D2 says these
tags are adequate for the later response and spectral tests.  Ward
presentation directions, vacuum directions, and typed residues are quotiented
by Lemmas 6.1, 8.3, and 10.1.  The active projected quotient is therefore a
subquotient of a finite spin-network and finite sector-tag space.

The bound is uniform in the regulator because the set above depends only on
the fixed physical collar, the fixed energy window, the fixed response-height
cutoff, and the associated Casimir cutoff.  It does not depend on the lattice
volume or on a finer presentation of the same collar.

$$
\square
$$

### Lemma 13.F10r-c: No Zero-Gap Sequence Escapes The Projection

Every possible zero-gap sequence has a nontrivial projection into the
finite-energy, bounded-Casimir, low-response-height collar module.

Suppose a cofinal active sequence satisfies:

$$
\boxed{
\left\|
\eta_{\alpha}
\right\|_{OS}
=1,
\qquad
\left\|
P_{\alpha}^{act}{\mathsf T}_{\alpha}P_{\alpha}^{act}
\eta_{\alpha}
\right\|_{OS}
\to
1.
}
$$

Then, for every tolerance:

$$
\boxed{
\delta>0,
}
$$

there are choices:

$$
\boxed{
E,
\qquad
M,
\qquad
R(E,M)
}
$$

fixed before the spectral conclusion, such that:

$$
\boxed{
\liminf_{\alpha}
\left\|
\Pi_{\alpha,E,M}^{act}
\eta_{\alpha}
\right\|_{OS}
\ge
1-\delta.
}
$$

Proof.  The finite OS transfer operator is a contraction.  The finite
spectral calculus gives:

$$
\boxed{
\left\|
\left(
I-P_{\alpha,E}^{collar}
\right)
{\mathsf T}_{\alpha}(t_0)
\right\|_{OS}
\le
\exp(-Et_0).
}
$$

Choose the energy window so this term is below the required tolerance.  Then
choose the response-height and Casimir cutoffs so that Lemma 13.F10r-a makes
the discarded low-energy collar tail below the remaining tolerance.  If the
projection norm were still bounded away from one, the contraction estimate
would force the transfer norm of the sequence to be bounded away from one.
That contradicts the assumed zero-gap behavior.

Thus the later finite-frame and Schur estimates may be proved on the
projected module without losing a possible zero-gap state.

$$
\square
$$

### Lemma 13.F10j: Uniform Frame Weighting From Sector Separation

The gap-response packet can be weighted so that it gives uniform finite-frame
bounds on the projected active collar module of Lemmas 13.F10r-a and
13.F10r-b.  The high-height, high-Casimir, and high-energy complements are
already charged to the transfer tail by Lemma 13.F10r-c.

There is a positive response floor on the active collar unit sphere:

$$
\boxed{
\inf_{\eta\in{\mathbb S}_{\alpha,E,M}^{act,collar}}
\sup_{q\in{\mathcal Q}_{\alpha}^{gap}}
\left|
\left\langle
\zeta_{\alpha,q},
\eta
\right\rangle_{OS}
\right|
\ge
d_H
>0.
}
$$

where:

$$
\boxed{
{\mathbb S}_{\alpha,E,M}^{act,collar}
=
\left\{
\eta\in
{\mathcal K}_{\alpha,E,M}^{act,collar}
:
\left\|
\eta
\right\|_{OS}=1
\right\}.
}
$$

Proof of the response floor.  If the displayed floor failed, there would be a
cofinal OS-unit projected active collar sequence whose response against every
gap-response packet element tends to zero.  Lemmas 9.D0a, 9.D1a, 9.D1, 9.0,
9.1, and 10.1 classify such a sequence as Ward, vacuum, or typed.  This
contradicts membership in the projected active collar quotient.

Now use the finite-dimensional frame selection theorem on:

$$
\boxed{
{\mathcal K}_{\alpha,E,M}^{act,collar}.
}
$$

Because Lemma 13.F10r-b gives:

$$
\boxed{
\dim
{\mathcal K}_{\alpha,E,M}^{act,collar}
\le
n_H(E,M),
}
$$

the constants in the finite frame selection theorem are uniform in the
cutoff.  There are predeclared nonnegative weights:

$$
\boxed{
w_{\alpha,q}\ge0
}
$$

depending only on the cutoff packet and not on any Wilson or spectral
predicate, such that:

$$
\boxed{
a_H
\left\|
\eta
\right\|_{OS}^{2}
\le
\sum_{q\in{\mathcal Q}_{\alpha}^{gap}}
w_{\alpha,q}
\left|
\left\langle
\zeta_{\alpha,q},
\eta
\right\rangle_{OS}
\right|^{2}
\le
A_H
\left\|
\eta
\right\|_{OS}^{2}.
}
$$

The constants can be chosen cofinally with:

$$
\boxed{
a_H
>0,
\qquad
A_H
<\infty.
}
$$

The lower constant depends only on:

$$
\boxed{
d_H,
\qquad
n_H(E,M).
}
$$

The upper constant depends only on:

$$
\boxed{
n_H(E,M),
\qquad
\sup_q
\left\|
\zeta_{\alpha,q}
\right\|_{OS}^{2}
}
$$

after the response packet is Bessel-normalized.

Proof.  The first part gives a norming finite family of response functionals
on the active collar quotient.  Symmetrize the response vectors by including
both signs and real-imaginary parts.  The polar of their convex hull is
contained in a fixed multiple of the OS unit ball.  Since the dimension is at
most the fixed number printed above, John decomposition gives a finite
weighted quadratic form with lower and upper bounds depending only on the
norming constant and the dimension bound.  The weights are computed from the
finite response packet before any conclusion is tested.

Thus the lower-frame constant cannot disappear merely because the regulator
cutoff increases.

Choose the energy window, Casimir cutoff, and height cutoff once for the
mass-gap audit so that the transfer tail from Lemmas 13.F10r-a and
13.F10r-c is smaller than a fixed fraction of the projected deficit:

$$
\boxed{
\tau_H(E,M)
\le
\frac{\epsilon_{proj}^{gap}}{4}.
}
$$

From this point in Section 13, the symbol:

$$
\boxed{
{\mathcal H}_{\alpha}^{act,collar}
}
$$

denotes the projected module:

$$
\boxed{
{\mathcal K}_{\alpha,E,M}^{act,collar}
}
$$

with this fixed choice of energy window, Casimir cutoff, and response-height
cutoff.  The discarded complement is not forgotten; it is included in the
final value of the gap deficit.

$$
\square
$$

### Lemma 13.F10a0: Cofinal Positivity Of The Active Response Gram

The gap-response packet has a cofinal strictly positive lower frame bound on
the projected active collar quotient.

Here the gap-response packet is the sector-complete response packet restricted
to the projected active transfer collar.  It is not a local-only probe family.

Let the active collar unit sphere be:

$$
\boxed{
{\mathbb S}_{\alpha}^{act,collar}
=
\left\{
\eta\in{\mathcal H}_{\alpha}^{act,collar}
:
\left\|
\eta
\right\|_{OS}=1
\right\}.
}
$$

Let the finite gap-response quadratic form be:

$$
\boxed{
Q_{\alpha}^{gap}(\eta)
=
\sum_{q\in{\mathcal Q}_{\alpha}^{gap}}
w_{\alpha,q}
\left|
\left\langle
\zeta_{\alpha,q},
\eta
\right\rangle_{OS}
\right|^{2}.
}
$$

Equivalently, define the active collar response analysis operator:

$$
\boxed{
{\mathcal C}_{\alpha}^{gap}
:
{\mathcal H}_{\alpha}^{act,collar}
\to
\ell^{2}
\left(
{\mathcal Q}_{\alpha}^{gap},
w_{\alpha}
\right)
}
$$

by:

$$
\boxed{
\left(
{\mathcal C}_{\alpha}^{gap}\eta
\right)_q
=
w_{\alpha,q}^{1/2}
\left\langle
\zeta_{\alpha,q},
\eta
\right\rangle_{OS}.
}
$$

Then:

$$
\boxed{
Q_{\alpha}^{gap}(\eta)
=
\left\|
{\mathcal C}_{\alpha}^{gap}\eta
\right\|_{\ell^{2}}^{2}.
}
$$

Define the finite active response minimum:

$$
\boxed{
m_{\alpha}^{gap}
=
\inf_{\eta\in{\mathbb S}_{\alpha}^{act,collar}}
Q_{\alpha}^{gap}(\eta).
}
$$

Then:

$$
\boxed{
\liminf_{\alpha}
m_{\alpha}^{gap}
\ge
a_H
>0.
}
$$

Equivalently, the finite response-frame operator satisfies:

$$
\boxed{
{\mathcal F}_{\alpha}^{gap}
\big|_{
{\mathcal H}_{\alpha}^{act,collar}
}
\ge
a_H
P_{\alpha}^{act,collar}
}
$$

cofinally.

Proof.  Lemma 13.F10j supplies a uniform finite-frame weighting of the
gap-response packet on the active collar quotient.  Therefore the displayed
lower frame bound follows directly.  For completeness, here is the
contradiction form of the same argument.

The finite active collar unit sphere is compact at each cutoff, so the
displayed infimum is attained.  Suppose the cofinal lower bound fails.  Then
there is a cofinal sequence of OS-unit active collar vectors with vanishing
gap response:

$$
\boxed{
\eta_{\alpha}
\in
{\mathbb S}_{\alpha}^{act,collar},
\qquad
Q_{\alpha}^{gap}(\eta_{\alpha})
\to0.
}
$$

Equivalently:

$$
\boxed{
\left\|
{\mathcal C}_{\alpha}^{gap}\eta_{\alpha}
\right\|_{\ell^{2}}
\to0.
}
$$

The gap-response packet is the restriction of the sector-complete packet to
the transfer collar.  Lemmas 9.D0a, 9.D1a, 9.D1, 9.0, and 9.1 say that the
packet norm has the same cofinal zero sets as the full sector norm on bounded
gauge-invariant OS matrix elements.  Vanishing gap response against this
packet eliminates every local, center, topological, and infrared distinction
from vacuum.  Lemma 10.1 classifies the sequence as Ward, vacuum, or typed.

But the active collar quotient has already removed Ward directions, vacuum
directions, and typed extensions.  Therefore an OS-unit vector in that
quotient cannot have vanishing sector-complete response.  This contradiction
proves the cofinal lower frame bound.

At fixed cutoff, the same argument also proves:

$$
\boxed{
\ker
{\mathcal C}_{\alpha}^{gap}
\cap
{\mathcal H}_{\alpha}^{act,collar}
=
\{0\}.
}
$$

The cofinal lower bound is the quantitative strengthening of this kernel
statement.  If the quantitative bound failed, compactness of the finite unit
spheres would produce the vanishing cofinal sequence above, and the same
sector-separation contradiction would apply.

Now choose any preliminary OS-orthonormal finite basis of the active collar
quotient:

$$
\boxed{
\left\{
f_{\alpha,i}
\right\}_{i=1}^{n_{\alpha}}.
}
$$

The response analysis matrix in this preliminary basis is:

$$
\boxed{
\widetilde B_{\alpha}(q,i)
=
w_{\alpha,q}^{1/2}
\left\langle
\zeta_{\alpha,q},
f_{\alpha,i}
\right\rangle_{OS}.
}
$$

Its response Gram matrix is:

$$
\boxed{
\widetilde{\mathsf R}_{\alpha}^{resp}
=
\widetilde B_{\alpha}^{*}
\widetilde B_{\alpha}.
}
$$

The lower frame bound is exactly the finite eigenvalue certificate:

$$
\boxed{
\lambda_{\min}
\left(
\widetilde{\mathsf R}_{\alpha}^{resp}
\right)
\ge
a_H.
}
$$

Run finite complete Cholesky or QR pivoting on this positive Gram matrix.
Since the least eigenvalue is bounded below cofinally, no active direction is
discarded by the pivot.  The cofinal residual certificate is:

$$
\boxed{
\delta_H^{2}
\le
a_H.
}
$$

When the same basis is rewritten in any finite collar coordinate family with
OS Gram matrix:

$$
\boxed{
G_{\alpha}^{OS},
}
$$

and response coefficient Gram:

$$
\boxed{
{\mathsf R}_{\alpha}^{resp},
}
$$

the invariant matrix statement is the generalized Gram inequality:

$$
\boxed{
{\mathsf R}_{\alpha}^{resp}
\ge
a_H
G_{\alpha}^{OS}.
}
$$

Thus the Cholesky residual certificate in Lemma 13.F10a is not an additional
admissibility condition.  It is the finite matrix form of sector-complete
response separation on the active OS collar quotient.

$$
\square
$$

### Lemma 13.F10a: OS-Orthonormal Active Collar Basis

At finite cutoff, the projected active transfer restriction from Lemmas
13.F10r-a and 13.F10r-b admits an orthonormal matrix representation in the physical OS
Hilbert norm, and its positive Schur envelope is extracted in that same norm.
The complement is controlled by the transfer tail fixed in Lemma 13.F10j.

Let the finite active collar vectors be:

$$
\boxed{
\xi_{\alpha,u}
\in
{\mathcal H}_{\alpha}^{phys}.
}
$$

Choose a response-pivoted independent active collar subfamily:

$$
\boxed{
{\mathcal I}_{\alpha}^{act}
\subset
{\mathcal U}_{\alpha}^{collar}.
}
$$

The choice is made by finite QR pivoting in the sector-complete response norm
and is fixed before any spectral predicate is evaluated.  A collar vector is
retained only if its response residual satisfies:

$$
\boxed{
\operatorname{dist}_{\mathcal R}
\left(
\xi_{\alpha,u},
\operatorname{span}
\{\xi_{\alpha,v}:v\hbox{ previously retained}\}
\right)
\ge
\delta_H
>0.
}
$$

The residual threshold is cofinal and comes from the same response floor used
in Lemma 10.1.  The retained family spans the active collar subspace:

$$
\boxed{
{\mathcal H}_{\alpha}^{act,collar}
=
\operatorname{span}
\left\{
\xi_{\alpha,u}
:
u\in{\mathcal I}_{\alpha}^{act}
\right\}.
}
$$

For coefficients supported on the pivoted family, set:

$$
\boxed{
\Xi_{\alpha}(c)
=
\sum_{u\in{\mathcal I}_{\alpha}^{act}}
c_u\xi_{\alpha,u}.
}
$$

Let the finite gap-response packet produce OS vectors:

$$
\boxed{
\zeta_{\alpha,q}
=
O_{\alpha,q}\Omega_{\alpha},
\qquad
q\in{\mathcal Q}_{\alpha}^{gap}.
}
$$

Define the finite response-frame operator on the active collar subspace:

$$
\boxed{
{\mathcal F}_{\alpha}^{gap}
=
\sum_{q\in{\mathcal Q}_{\alpha}^{gap}}
w_{\alpha,q}
\left|
\zeta_{\alpha,q}
\right\rangle
\left\langle
\zeta_{\alpha,q}
\right|.
}
$$

Equivalently, define the finite response analysis matrix:

$$
\boxed{
B_{\alpha}(q,u)
=
w_{\alpha,q}^{1/2}
\left\langle
\zeta_{\alpha,q},
\xi_{\alpha,u}
\right\rangle_{OS}.
}
$$

and its coefficient-space response Gram matrix:

$$
\boxed{
{\mathsf R}_{\alpha}^{resp}
=
B_{\alpha}^{*}B_{\alpha}.
}
$$

The response packet is Bessel-normalized before pivoting:

$$
\boxed{
\left\|
{\mathsf R}_{\alpha}^{resp}
\right\|_{\ell^{2}\to\ell^{2}}
\le
A_R
<\infty.
}
$$

The pivoted active family is retained only after the finite Cholesky residual
certificate:

$$
\boxed{
\lambda_{\min}
\left(
{\mathsf R}_{\alpha}^{resp}
\big|_{
{\mathcal I}_{\alpha}^{act}
}
\right)
\ge
\delta_H^{2}.
}
$$

This certificate is supplied by Lemma 13.F10a0.  It is not a posterior
selection rule.  The symbols above denote the finite QR-pivoted collar
vectors after the response Gram certificate has been applied, not arbitrary
raw local generators.

The sector-complete response packet gives cofinal frame bounds:

$$
\boxed{
a_H P_{\alpha}^{act,collar}
\le
{\mathcal F}_{\alpha}^{gap}
\le
A_H P_{\alpha}^{act,collar},
\qquad
0<a_H\le A_H<\infty.
}
$$

Equivalently:

$$
\boxed{
a_H
\left\|
\Xi_{\alpha}(c)
\right\|_{OS}^{2}
\le
\sum_{q\in{\mathcal Q}_{\alpha}^{gap}}
w_{\alpha,q}
\left|
\left\langle
\zeta_{\alpha,q},
\Xi_{\alpha}(c)
\right\rangle_{OS}
\right|^{2}
\le
A_H
\left\|
\Xi_{\alpha}(c)
\right\|_{OS}^{2}.
}
$$

For this pivoted family, the OS Gram matrix is:

$$
\boxed{
G_{\alpha}^{OS}(u,v)
=
\left\langle
\xi_{\alpha,u},
\xi_{\alpha,v}
\right\rangle_{OS}.
}
$$

The response-pivoting plus the frame bounds give cofinal Riesz bounds:

$$
\boxed{
0<c_{H}
\le
G_{\alpha}^{OS}
\le
C_{H}
<\infty.
}
$$

The constants are explicit:

$$
\boxed{
c_H
=
\frac{\delta_H^{2}}{A_H},
\qquad
C_H
=
\frac{A_R}{a_H}.
}
$$

Define the OS-orthonormal collar basis by:

$$
\boxed{
e_{\alpha,i}
=
\sum_u
\left(
G_{\alpha}^{OS}
\right)^{-1/2}_{iu}
\xi_{\alpha,u}.
}
$$

Then:

$$
\boxed{
\left\langle
e_{\alpha,i},
e_{\alpha,j}
\right\rangle_{OS}
=
\delta_{ij}.
}
$$

The projected active transfer matrix in the physical Hilbert norm is:

$$
\boxed{
M_{\alpha}(i,j)
=
\left\langle
e_{\alpha,i},
P_{\alpha}^{act}{\mathsf T}_{\alpha}P_{\alpha}^{act}
e_{\alpha,j}
\right\rangle_{OS}.
}
$$

Let the positive row kernel from Lemma 11.4 be:

$$
\boxed{
K_{\alpha}^{gap}(u,v).
}
$$

The OS-Schur envelope is:

$$
\boxed{
S_{\alpha}^{gap}(i,j)
=
\sum_{u,v}
\left|
\left(
G_{\alpha}^{OS}
\right)^{-1/2}_{iu}
\right|
K_{\alpha}^{gap}(u,v)
\left|
\left(
G_{\alpha}^{OS}
\right)^{-1/2}_{vj}
\right|.
}
$$

It satisfies the entrywise domination in the orthonormal OS basis:

$$
\boxed{
\left|
M_{\alpha}(i,j)
\right|
\le
S_{\alpha}^{gap}(i,j).
}
$$

Moreover the Riesz constants only change the row deficit by a fixed finite
collar factor already included in the gap-row envelope:

$$
\boxed{
\sup_i
\sum_j
S_{\alpha}^{gap}(i,j)
\frac{r_{\alpha}(j)}{r_{\alpha}(i)}
\le
1-\epsilon_{*}^{gap}.
}
$$

The reversed collar estimate also holds:

$$
\boxed{
\sup_j
\sum_i
S_{\alpha}^{gap}(i,j)
\frac{r_{\alpha}(i)}{r_{\alpha}(j)}
\le
1-\epsilon_{*}^{gap}.
}
$$

Proof.  Reflection positivity gives the finite OS pre-Hilbert space, and the
null quotient gives a genuine finite Hilbert space.  The active collar
vectors span the non-vacuum singlet transfer sector used in Lemma 13.0 after
Ward, vacuum, and typed directions are removed.

The pivoted family is chosen in the finite response norm.  The response
packet is sector-complete by Lemmas 9.0, 9.1, and 10.1.  Lemma 13.F10a0
turns that separation statement into the cofinal positive response Gram
certificate on the active OS collar quotient.

The upper frame bound follows from bounded finite response-packet operator
norms on a fixed local collar and compact group variables.  The pivoted QR
choice prevents coefficient-space overcompleteness: no retained collar vector
has response residual below the cofinal response-packet threshold.  Combining
the response-frame bounds with this pivot rule gives the Riesz constants
directly.

For the lower bound, the Cholesky certificate gives:

$$
\boxed{
c^{*}
{\mathsf R}_{\alpha}^{resp}
c
\ge
\delta_H^{2}
\left\|
c
\right\|_{2}^{2}.
}
$$

The upper frame inequality gives:

$$
\boxed{
c^{*}
{\mathsf R}_{\alpha}^{resp}
c
\le
A_H
c^{*}
G_{\alpha}^{OS}
c.
}
$$

Therefore:

$$
\boxed{
c^{*}
G_{\alpha}^{OS}
c
\ge
\frac{\delta_H^{2}}{A_H}
\left\|
c
\right\|_{2}^{2}.
}
$$

For the upper bound, the Bessel normalization gives:

$$
\boxed{
c^{*}
{\mathsf R}_{\alpha}^{resp}
c
\le
A_R
\left\|
c
\right\|_{2}^{2}.
}
$$

The lower frame inequality gives:

$$
\boxed{
a_H
c^{*}
G_{\alpha}^{OS}
c
\le
c^{*}
{\mathsf R}_{\alpha}^{resp}
c.
}
$$

Therefore:

$$
\boxed{
c_H\left\|c\right\|_2^2
\le
\left\|
\Xi_{\alpha}(c)
\right\|_{OS}^{2}
\le
C_H\left\|c\right\|_2^2.
}
$$

These are exactly the Riesz bounds for:

$$
\boxed{
G_{\alpha}^{OS}.
}
$$

The displayed basis is obtained by applying the inverse square root of the OS
Gram matrix on the active quotient.  Therefore it is orthonormal in the
physical OS norm.  The matrix shown is not formal; it is the matrix of the
operator:

$$
\boxed{
P_{\alpha}^{act}{\mathsf T}_{\alpha}P_{\alpha}^{act}
}
$$

on the finite physical Hilbert space.

Lemma 11.4 supplies the positive gap-row kernel before orthonormalization.
Conjugating by the bounded inverse square root of the OS Gram matrix gives
the printed Schur envelope.  The cofinal Riesz bounds ensure that this
conjugation changes the envelope by only fixed finite collar constants.  The
height function is chosen after this OS normalization, so those constants are
absorbed into the printed gap-row deficit rather than into the final
spectral answer.

Thus the Schur envelope is an envelope in the actual OS norm.

$$
\square
$$

### Lemma 13.1: One-Step Active Transfer Operator Deficit

The active one-step transfer operator has a cofinal norm deficit:

$$
\boxed{
\left\|
P_{\alpha}^{act}{\mathsf T}_{\alpha}P_{\alpha}^{act}
\right\|_{OS}
\le
1-\epsilon_{*}^{gap}.
}
$$

Proof.  First restrict to the projected finite-energy, bounded-Casimir,
low-height active collar module of Lemmas 13.F10r-a and 13.F10r-b.  At finite
cutoff, write the projected active transfer restriction in the OS-orthonormal
active collar basis of Lemma 13.F10a.  In that basis, its matrix is the
physical-Hilbert matrix:

$$
\boxed{
M_{\alpha}(i,j)
=
\left\langle
e_{\alpha,i},
P_{\alpha}^{act}{\mathsf T}_{\alpha}P_{\alpha}^{act}
e_{\alpha,j}
\right\rangle_{OS}.
}
$$

Lemma 13.F10a gives the positive Schur envelope in the same OS norm:

$$
\boxed{
\left|
M_{\alpha}(i,j)
\right|
\le
S_{\alpha}^{gap}(i,j).
}
$$

The majorant is the gap-sector row kernel of Lemma 11.0 after the
response-height transform and OS orthonormalization.  Lemmas 11.RN1-11.RN3,
11.3, 11.4, and 13.F10a give the cofinal weighted row bound:

$$
\boxed{
\sup_i
\sum_j
S_{\alpha}^{gap}(i,j)
\frac{r_{\alpha}(j)}{r_{\alpha}(i)}
\le
1-\epsilon_{*}^{gap}.
}
$$

Reflection positivity gives the same estimate for the reversed transfer
collar:

$$
\boxed{
\sup_j
\sum_i
S_{\alpha}^{gap}(i,j)
\frac{r_{\alpha}(i)}{r_{\alpha}(j)}
\le
1-\epsilon_{*}^{gap}.
}
$$

The weighted Schur test now applies directly to the OS-Hilbert operator:

$$
\boxed{
\left\|
P_{\alpha}^{act}{\mathsf T}_{\alpha}P_{\alpha}^{act}
\right\|_{OS}
\le
\sqrt{
\left(1-\epsilon_{*}^{gap}\right)
\left(1-\epsilon_{*}^{gap}\right)
}
=
1-\epsilon_{*}^{gap}.
}
$$

This is the independent transfer-operator estimate.  It is a one-step
finite Hilbert-space norm bound derived from the row response cost,
reflection positivity, and the OS-controlled collar basis.  It is not a
formal matrix norm and does not assume exponential decay of correlators.

It remains to add back the complement of the projected collar module.  Lemmas
13.F10r-a, 13.F10r-c, and 13.F10j fixed the energy window, Casimir cutoff, and
response-height cutoff so that this complement contributes at most:

$$
\boxed{
\frac{\epsilon_{proj}^{gap}}{4}
}
$$

to the one-step norm in the possible zero-gap window.  Shrinking the displayed
deficit once and for all gives:

$$
\boxed{
\epsilon_{*}^{gap}
\le
\frac{\epsilon_{proj}^{gap}}{2}.
}
$$

Therefore the full active restriction in the finite-energy spectral window
obeys the stated cofinal norm deficit.  The high-energy complement is
suppressed by the fixed-time transfer estimate in Lemma 13.F10r-c and cannot
produce spectrum near the vacuum edge.

$$
\square
$$

### Lemma 13.2: Time Mesh Gives Linear Iteration Count

There is a positive constant, fixed by the renormalized time step and not by
the spectral answer, such that:

$$
\boxed{
n_{\alpha}(t)\ge\kappa_Tt-o(t).
}
$$

Proof.  The regulated transfer is local and has a fixed physical time mesh
after Section 8 selects the scale.  The number of transfer applications in a
Euclidean time interval is therefore just the finite clock count for that
mesh.  Endpoint rounding and local collars contribute only the displayed
sublinear correction.  This is a kinematic counting statement, not the
mass-gap mechanism.

$$
\square
$$

### Lemma 13.3: Finite Connected-Correlation Decay

There is a positive transfer exponent such that:

$$
\boxed{
|\omega_{\alpha}(A T_{\alpha}(t)B)|
\le
C_{A,B}e^{-\Delta_0t}
}
$$

uniformly along the continuum trajectory.

Proof.  Fix the local invariant insertions and the physical Euclidean time:

$$
\boxed{
A,
\qquad
B,
\qquad
t.
}
$$

The volume is then taken large enough to contain the supports and transfer
collar.  Lemma 13.0 reduces the finite connected correlator to the active
transfer restriction.  Lemma 13.1 gives the one-step active transfer norm
deficit.  Lemma 13.2 gives the linear number of transfer steps.  Therefore:

$$
\boxed{
|\omega_{\alpha}(A T_{\alpha}(t)B)|
\le
C_{A,B}
\left(1-\epsilon_{*}^{gap}\right)^{n_{\alpha}(t)}.
}
$$

Using the time-mesh count gives:

$$
\boxed{
|\omega_{\alpha}(A T_{\alpha}(t)B)|
\le
C_{A,B}(1-\epsilon_{*}^{gap})^{\kappa_Tt-o(t)}.
}
$$

Set:

$$
\boxed{
\Delta_0
=
\kappa_T[-\log(1-\epsilon_{*}^{gap})]>0.
}
$$

The constants are independent of the cutoff parameters after the fixed
insertions and fixed physical time have been chosen.  Lemma 3.2 therefore
passes this finite estimate to the continuum OS matrix element at that fixed
time.



$$
\square
$$

### Lemma 13.4: Spectral Gap

The OS/GNS Hamiltonian satisfies:

$$
\boxed{
\operatorname{Spec}(H)\cap(0,\Delta_0)=\varnothing.
}
$$

Proof.  Lemma 3.1 fixes the order of limits for transfer matrix elements:

$$
\boxed{
\hbox{finite physical time}
\;\longrightarrow\;
\hbox{volume limit}
\;\longrightarrow\;
\hbox{lattice-spacing limit}
\;\longrightarrow\;
\hbox{sector-packet completion}
\;\longrightarrow\;
\hbox{OS/GNS reconstruction}.
}
$$

Lemma 13.3 gives a cutoff-uniform bound at fixed physical time.  Lemma 3.2
passes that bound to the continuum OS matrix element.  Lemma 9.D2 confirms
that the finite-energy matrix-element topology is adequate for this transfer
test.  Thus for all local invariant pairs:

$$
\boxed{
|\langle A\Omega,e^{-tH}B\Omega\rangle|
\le
C_{A,B}e^{-\Delta_0t}.
}
$$

Vectors of the following form are dense in the physical sector:

$$
\boxed{
B\Omega,
\qquad
B\in{\mathcal A}^{inv}_{YM}.
}
$$

Suppose the Hamiltonian had spectral support in the interval:

$$
\boxed{
(0,\Delta_0).
}
$$

Then by the spectral theorem there would be vectors in the dense cyclic
sector whose matrix elements decay no faster than:

$$
\boxed{
e^{-\lambda t},
\qquad
\lambda<\Delta_0.
}
$$

By cyclicity, there is a local invariant vector whose spectral measure has
nonzero mass in the displayed interval:

$$
\boxed{
E_H((0,\Delta_0))B\Omega\ne0,
\qquad
B\in{\mathcal A}^{cyl}_{inv}.
}
$$

For that vector, the spectral theorem gives a lower large-time decay rate
incompatible with the uniform correlation bound above.  The large-time test
is applied only after the continuum Hamiltonian has been reconstructed, and
only on local cyclic matrix elements.  Therefore no spectrum lies in the
displayed interval.


$$
\square
$$

Hence:

$$
\boxed{
\Delta\ge\Delta_0>0.
}
$$

## 14. Main Proof

Searchable main proof tag:

`V4P39-STANDALONE-MAIN-PROOF`.

Proof of Theorem 1.1.

The theorem starts with S1-S3, the base compact pure gauge regulator,
observable algebra, and reflection-positive regulator family, plus the
explicit conventional token Bessel gap of Lemma 11.2a.  Corollary 4.3 derives
S4, giving projective consistency and gauge-invariant tightness.
Corollary 5.4 derives S5, giving Wilson-Schwinger determinacy.  Corollary 6.2
derives S6, giving the exact Ward quotient.  Corollary 7.2 derives S9, giving
the OS/GNS physical sector generated by the invariant algebra.  Corollary 8.4
derives S7, giving the nonposterior scale and counterterm class.
Corollary 8.7 derives the Wilson-heat-kernel branch-equivalence certificate.

Lemmas 3.1 and 3.2 fix the order in which finite estimates are passed to the
continuum state.  Volume, lattice spacing, packet completion, and OS/GNS
reconstruction are taken before the large-loop or large-time predicates are
evaluated.

Lemmas 9.D0a, 9.D0, 9.D1a, 9.D1, 9.D2, 9.0-9.1, and 10.1-10.2 prove the
Einstein half of the margin certificate: the response topology is separating,
adequate for the later tests, and every active non-vacuum obstruction has a
positive invariant response floor.
Lemmas 11.RN1-11.RN3 and 11.1-11.4 prove the Feynman half conditionally on
the token Bessel gap of Lemma 11.2a: row tokens are faithful, Radon-Nikodym
response cost controls spectral radius, and sub-Markov deficits survive
cofinally once the finite row-token covariance gap is supplied.
Lemmas 12.0-12.4 turn the center-row deficit, the sheet-entropy majorant, and
the area crossing lower bound into the Wilson area law:

$$
\boxed{
\sigma\ge\sigma_0>0.
}
$$

Lemmas 13.0-13.4 turn the centered transfer decomposition, the active collar
effective-rank and tail bound, the Casimir-height tail estimate, the projected
obstruction-channel bound, the zero-gap projection lemma, the finite
response-frame weighting, the active response lower-frame certificate, the
OS-orthonormal active collar basis, the one-step active transfer norm deficit,
and the fixed time mesh into the transfer spectral gap:

$$
\boxed{
\Delta\ge\Delta_0>0.
}
$$

Therefore, inside the printed standalone reduction, four-dimensional pure
SU(N) Yang-Mills in the constructed gauge-invariant vacuum sector has Wilson
confinement and a positive mass gap.  Conventional acceptance is the separate
review task recorded in the final status ledger.


$$
\square
$$

## 15. Non-Tautology Audit

Searchable non-tautology tag:

`V4P39-NONTAUTOLOGY-AUDIT`.

The proof does not assume:

$$
\boxed{
\sigma>0,\qquad \Delta>0.
}
$$

It proves them from:

$$
\boxed{
\begin{array}{c|l}
\hbox{source} & \hbox{used for}\\
\hline
\hbox{compact gauge variables} & \hbox{continuum state}\\
\hbox{Wilson-Schwinger determinacy} & \hbox{state uniqueness}\\
\hbox{Ward quotient} & \hbox{presentation redundancy removal}\\
\hbox{reflection positivity} & \hbox{Hilbert and transfer reconstruction}\\
\hbox{response separation} & \hbox{positive local obstruction floor}\\
\hbox{token faithfulness} & \hbox{no free branch multiplication}\\
\hbox{sub-Markov row deficits} & \hbox{area and time exponential decay}\\
\hbox{continuum liminf} & \hbox{positive margins in the limit}
\end{array}
}
$$

The proof would be circular if any of the following occurred:

$$
\boxed{
\begin{array}{c|l}
\hbox{forbidden move} & \hbox{why forbidden}\\
\hline
\hbox{define active sector by excluding massless states} &
\hbox{would assume the gap}\\
\hbox{choose scale after seeing the gap} &
\hbox{would fit the conclusion}\\
\hbox{declare all deconfined states unphysical} &
\hbox{would hide Wilson failure}\\
\hbox{insert row deficits by definition} &
\hbox{would assume confinement}\\
\hbox{discard zero-response non-typed classes without quotient proof} &
\hbox{would hide an obstruction}
\end{array}
}
$$

The paper avoids those moves by printing each required replacement.

## 16. Falsifier Ledger

Searchable falsifier tag:

`V4P39-STANDALONE-FALSIFIER-LEDGER`.

A referee can attack the proof at the following exact places:

$$
\boxed{
\begin{array}{c|l|l}
\hbox{falsifier} & \hbox{claim attacked} & \hbox{effect}\\
\hline
\mathrm{F1} & \hbox{compactness/tightness fails} & \hbox{no continuum state}\\
\mathrm{F2} & \hbox{Wilson functions do not determine invariant state} &
\hbox{regulator ambiguity}\\
\mathrm{F3} & \hbox{Ward quotient misses a physical gauge artifact} &
\hbox{wrong physical algebra}\\
\mathrm{F4} & \hbox{reflection positivity fails} & \hbox{no OS Hilbert sector}\\
\mathrm{F5} & \hbox{pure YM decoder admits extra fields} & \hbox{not pure YM}\\
\mathrm{F6} & \hbox{response-invisible non-typed classes exist} &
\hbox{Lemma 10.1 fails}\\
\mathrm{F6a} & \hbox{nonlocal/center/topological/IR sector evades the explicit
cofinal response packet} &
\hbox{Lemmas 9.D0a,9.D0,9.D1a,9.D1,9.D2, or 9.1 fail}\\
\mathrm{F7} & \hbox{token faithfulness fails} & \hbox{Lemma 11.1 fails}\\
\mathrm{F7a} & \hbox{row-token Bessel covariance gap or row-weighted response
pressure gap fails} &
\hbox{Lemmas 11.2a,11.2b,11.2, and 12.F9c fail}\\
\mathrm{F8} & \hbox{Radon-Nikodym response-cost to spectral-radius bridge
fails} & \hbox{Lemmas 11.RN1-11.RN3 or 11.3 fail}\\
\mathrm{F9} & \hbox{positive absolute sheet majorant fails} &
\hbox{Lemma 12.F9a fails}\\
\mathrm{F9W} & \hbox{Wilson and heat-kernel branches do not define the same
continuum state} & \hbox{Lemmas 8.5,8.6,12.F9b fail}\\
\mathrm{F9a} & \hbox{area crossing lower bound fails} & \hbox{Wilson proof
fails}\\
\mathrm{F9b} & \hbox{finite center-sheet connective constant exceeds the
charged row deficit} &
\hbox{Lemma 12.F9c fails}\\
\mathrm{F10} & \hbox{centered transfer decomposition fails} &
\hbox{Lemma 13.0 fails}\\
\mathrm{F10c} & \hbox{the active collar effective-rank, Casimir-height tail,
projected-channel bound, zero-gap projection, finite-frame weighting,
response Gram, Bessel, or Cholesky residual certificate fails} &
\hbox{Lemmas 13.F10r,13.F10r-a,13.F10r-b,13.F10r-c,13.F10j,13.F10a0,
or 13.F10a fail}\\
\mathrm{F10a} & \hbox{one-step active transfer norm deficit fails} &
\hbox{Lemma 13.1 fails}\\
\mathrm{F10b} & \hbox{fixed time mesh loses linear step count} &
\hbox{gap proof fails}\\
\mathrm{F11} & \hbox{the ordered continuum-limit protocol loses the finite
bounds} & \hbox{Lemmas 3.1,3.2,12.4, or 13.4 fail}\\
\mathrm{F12} & \hbox{local cyclic finite-energy matrix elements are
insufficient for the spectral test} & \hbox{Lemmas 7.1,9.D2, or 13.4 fail}
\end{array}
}
$$

This ledger is the main point of writing Paper 39.  The proof is no longer a
maze of references.  It is a finite list of propositions that can be checked
or broken.

## 17. Relation To The Clay-Style Statement

Searchable Clay tag:

`V4P39-RELATION-TO-CLAY-STYLE-YM`.

The Clay problem asks for construction of quantum Yang-Mills theory on
Euclidean four-space for a compact simple gauge group and proof of a mass gap.
This paper addresses the pure SU(N) gauge-invariant sector.  Internally, it
derives the parallel Wilson confinement predicate as well; conventionally, it
offers a standalone reduction whose named lemmas must be checked.

The claim of this paper is:

$$
\boxed{
\hbox{within the standard admissibility and margin certificate printed here,
pure }SU(N)\hbox{ Yang-Mills has Wilson confinement and mass gap.}
}
$$

The external review boundary is:

$$
\boxed{
\hbox{independent reviewers must check cofinal packet separation, the response
floor, token faithfulness, sub-Markov deficits, and continuum liminf transfer
without accepting ISP terminology.}
}
$$

If those checks pass, Paper 39 is a standalone route to the conventional
Yang-Mills conclusion.  If one fails, the falsifier ledger identifies exactly
where.

## 18. Reviewer-Issue Repair Ledger

Searchable repair tag:

`V4P39-EXPERIENCED-PHYSICIST-REVIEW-REPAIRS`.

The experienced-review objections are addressed one by one as follows:

$$
\boxed{
\begin{array}{c|l|l}
\hbox{issue} & \hbox{review concern} & \hbox{repair in Paper 39}\\
\hline
\mathrm{R1} &
\hbox{continuum gates and S8 were treated like assumptions} &
\hbox{Theorem 1.1 now assumes S1-S3 plus the explicit conventional
TOK-BESSEL gap; S4-S7 and S9 are derived in the paper}\\
\mathrm{R2} &
\hbox{response distance was not quantitatively tied to spectral radius} &
\hbox{Lemmas 11.RN1-11.RN3 construct row densities, prove Hellinger transfer
domination, and give the uniform cutoff Chernoff bound used by Lemma 11.3}\\
\mathrm{R2a} &
\hbox{response floor might miss nonlocal, center, topological, or IR sectors} &
\hbox{Lemmas 9.D0a-9.D2 and 9.0-9.1 define the sector topology, prove
direct-limit cylinder exhaustion, finite-energy matrix-element density,
nonposterior packet construction, packet faithfulness, test adequacy, and sector
separation}\\
\mathrm{R2b} &
\hbox{sector-complete response was an assumed admissibility principle} &
\hbox{Section 9 now prints the finite sector algebra, epsilon-net packet,
response norm, direct-limit truncation theorem, rational packet construction,
finite-energy density theorem, packet-faithfulness theorem, and
test-adequacy theorem}\\
\mathrm{R3} &
\hbox{compactness gave subnet limits, not determinate vacuum sector} &
\hbox{Lemma 5.3 states the exact renormalized path-determinacy claim}\\
\mathrm{R4} &
\hbox{Wilson area law imported center-sheet intuition} &
\hbox{Lemmas 12.F9a-12.F9c give a positive absolute sheet majorant, Wilson
branch equivalence, and an explicit entropy-deficit margin}\\
\mathrm{R4a} &
\hbox{positive Peter-Weyl coefficients do not make recoupled surface weights
positive} &
\hbox{Lemma 12.F9a now uses an absolute-value majorant and counts signs,
phases, recouplings, and multiplicities inside the sheet row weights}\\
\mathrm{R4b} &
\hbox{the absolute majorant might be too crude to preserve the deficit} &
\hbox{Lemma 12.F9a now uses normalized spin-network bases; Haar projectors and
recouplings have norm one, so the only absolute-value loss is the explicit
square-root fusion-multiplicity factor inside the row majorant}\\
\mathrm{R4c} &
\hbox{the row-normalized absolute majorant might still lose strict
subcriticality after sheet entropy is counted} &
\hbox{Lemma 12.F9c now prints the augmented finite graph, weighted
connective constant, least charged edge cost, and strict inequality
between connective entropy and response deficit}\\
\mathrm{R4e} &
\hbox{the strict inequality in Lemma 12.F9c depended on qualitative
token-coding slack} &
\hbox{Lemma 11.2a now isolates the row-token Bessel defect as a finite
heat-kernel covariance gap; Lemmas 11.2b and 11.2 make the pressure estimate
conditional on that explicit standard gap rather than treating it as already
proved}\\
\mathrm{R4d} &
\hbox{the Wilson plaquette branch was conditional on an external comparison
certificate} &
\hbox{Lemmas 8.5-8.6 and Corollary 8.7 now derive Wilson-heat-kernel
continuum equivalence from pure-decoder residue matching and
Wilson-Schwinger determinacy}\\
\mathrm{R5} &
\hbox{mass gap imported transfer-row intuition} &
\hbox{Lemmas 13.0-13.2 now use centered transfer decomposition,
OS-orthonormal active collar bases, an OS-norm Schur envelope, one-step
active norm deficit, and fixed time-mesh counting}\\
\mathrm{R5a} &
\hbox{the active collar Riesz bounds in Lemma 13.F10a were asserted rather
than constructed} &
\hbox{Lemmas 13.F10r-a, 13.F10r-b, and 13.F10r-c replace raw bounded rank by a
Casimir-height tail estimate, projected obstruction-channel bound, and
zero-gap projection theorem; Lemma 13.F10j gives finite-frame weighting;
Lemma 13.F10a0 proves cofinal lower-frame positivity; Lemma 13.F10a constructs
the response analysis Gram, Bessel and Cholesky certificates, and the
resulting OS Riesz bounds}\\
\mathrm{R6} &
\hbox{pure Yang-Mills decoder was too compressed} &
\hbox{Lemma 8.3 excludes relevant residue leakage in the pure gauge class}\\
\mathrm{R7} &
\hbox{continuum-limit passages were too compressed and did not specify the
order of volume, lattice spacing, loop size, Euclidean time, and OS
reconstruction} &
\hbox{Lemmas 3.1 and 3.2 now print the limit-order protocol; Lemmas 12.4,
13.3, and 13.4 explicitly pass finite estimates through that protocol}
\end{array}
}
$$

The remaining external review boundary is correspondingly sharper:

$$
\boxed{
\hbox{a referee no longer has to chase the ISP corpus; the attack points are
the repaired standard lemmas and the falsifiers F1-F12, F6a, F7a, F9W, and
F10c.}
}
$$

## 19. Final Status

Searchable final tag:

`V4P39-FINAL-STANDALONE-YM-STATUS`.

The final status ledger separates the internal manuscript status from the
safer conventional-review status:

$$
\boxed{
\begin{array}{c|c|c}
\hbox{object} & \hbox{internal status} & \hbox{conventional-review status}\\
\hline
\hbox{standard theorem statement} & \mathrm{PRINTED} & \mathrm{PRINTED}\\
\hbox{regulated pure }SU(N)\hbox{ construction} & \mathrm{PRINTED} &
\mathrm{PRINTED}\\
\hbox{base assumptions in Theorem 1.1} & \mathrm{S1\text{-}S3\ PLUS\ TOK\text{-}BESSEL} &
\mathrm{S1\text{-}S3\ PLUS\ TOK\text{-}BESSEL}\\
\hbox{continuum construction gates S4-S7 and S9} & \mathrm{DERIVED} &
\mathrm{CHECK\ S4,S5,S7,S9}\\
\hbox{margin and crossing gate S8} & \mathrm{DERIVED} &
\mathrm{CONDITIONAL\ ON\ TOK\text{-}BESSEL\ AND\ REDUCED\ TO\ CORE\ LEMMAS}\\
\hbox{compactness and state determinacy} & \mathrm{INTERNAL\ DERIVATION} &
\mathrm{CHECK\ LEMMAS\ 4.1,5.3}\\
\hbox{ordered continuum-limit transfer} & \mathrm{INTERNAL\ DERIVATION} &
\mathrm{CHECK\ LEMMAS\ 3.1,3.2}\\
\hbox{Ward quotient and OS/GNS reconstruction} &
\mathrm{INTERNAL\ DERIVATION} & \mathrm{CHECK\ LEMMAS\ 6.1,7.1,7.1a}\\
\hbox{pure YM decoder and residue discipline} &
\mathrm{INTERNAL\ DERIVATION} & \mathrm{CHECK\ LEMMA\ 8.3}\\
\hbox{Wilson heat-kernel branch equivalence} &
\mathrm{INTERNAL\ DERIVATION} & \mathrm{CHECK\ LEMMAS\ 8.5,8.6}\\
\hbox{response floor} & \mathrm{INTERNAL\ DERIVATION} &
\mathrm{REDUCED\ TO\ LEMMAS\ 9.D0a,9.D0,9.D1a,9.D1,9.D2,9.0,9.1,10.1}\\
\hbox{row-token majorants} & \mathrm{INTERNAL\ DERIVATION} &
\mathrm{CONDITIONAL\ ON\ LEMMA\ 11.2a;\ CHECK\ 11.2b,11.2,11.RN1\text{-}11.RN3,11.3}\\
\hbox{Wilson area law} & \mathrm{INTERNAL\ DERIVATION} &
\mathrm{REDUCED\ TO\ LEMMA\ 12.0}\\
\hbox{transfer mass gap} & \mathrm{INTERNAL\ DERIVATION} &
\mathrm{REDUCED\ TO\ LEMMAS\ 13.0,13.F10r,13.F10r\text{-}a,
13.F10r\text{-}b,13.F10r\text{-}c,13.F10j,13.F10a0,13.F10a,13.1}\\
\hbox{standalone ontology-free YM theorem} & \mathrm{P39\ CONDITIONAL\ REDUCTION\ COMPLETE} &
\mathrm{CONVENTIONAL\ ACCEPTANCE\ REQUIRES\ TOK\text{-}BESSEL}\\
\hbox{external mathematical acceptance} & \mathrm{REVIEW\ REQUIRED} &
\mathrm{OPEN}
\end{array}
}
$$

The shortest final statement is:

$$
\boxed{
\hbox{Paper 39 rewrites the Paper 38 OFYM closure as a standalone reduction:
inside the printed framework the margins are derived, while conventional
acceptance depends on checking Lemmas 9.D0a, 9.D0, 9.D1a, 9.D1, 9.0, 9.1,
9.D2, 10.1, the TOK-BESSEL covariance gap of Lemma 11.2a, Lemmas 11.2b,
11.2, 11.RN1-11.RN3, 11.3, 12.0, 12.F9c, 13.0, 13.F10r, 13.F10r-a,
13.F10r-b, 13.F10r-c, 13.F10j, 13.F10a0, 13.F10a, 13.1, the limit-order
lemmas 3.1-3.2, and the Wilson branch equivalence lemmas 8.5-8.6.}
}
$$
