# v12 W4′ — ONE INVARIANT OR THREE (PIN)

**Status:** PIN, STRICT, 2026-07-28.  **Binding:** paper 0 v2.1 §4
W4′.  **The engraved warning:** U2's precedent — three structures
assumed one, measured three.  No unification is assumed; the
relation TABLE is the deliverable.  Lean NONE.

## The three objects, formalized at toy scale

1. **The temporal object:** the Δᴮ-family structure on declared
   compositions (W1′'s committed machinery reused by AST/slice,
   anchored).
2. **The contextual object:** the AB no-global-section obstruction
   on the CHSH context 4-cycle (the presheaf of distributions;
   [AB]'s formulation; [AMB]'s cohomological witness with its
   sufficient-not-necessary caution) — built exactly on declared
   empirical models (the singlet table; the PR table; a classical
   table).
3. **The measured BC obstructions:** BC1's division-event
   non-gluing and BC2's frame non-mappability — CITED from the
   committed bc state only (`git show` — the bc working tree is
   dirty with frozen partial edits; never read it directly), used
   as fixed data points, not re-run.

## The unit

Construct the candidate maps between the three; test co-variation
on exact models chosen to SEPARATE if separation exists: models
with Δᴮ-structure and no contextuality (single-context
compositions); contextuality without quantum realization (PR — [AB]
strong contextuality); BC2's model where legs divide at some
settings yet the frame obstruction stands (bc #4 M-1's inversion);
the W1′ Δᴮ(H,W) = 0 witness.  Deliverable: the exact relation
table — per pair: IDENTICAL (a constructed equivalence, gated) /
MAPPED-BUT-INEQUIVALENT (the map + the witness of inequivalence) /
INDEPENDENT (witnesses both ways) — with every entry carried by an
exact model, no prose-only verdicts.

Receipt rules: exact; anchors exit-1-only (W1′ committed numbers
where reused; the bc committed numbers as quoted constants);
substantive negatives exit 0; runtime < ~25 min; STRICT,
GREEN-UNREVIEWED; antecedents cited.  Files: v12/code/w4p_*,
v12/note-w4p-one-invariant-or-three.md.
