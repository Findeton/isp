# v12 W1 — THE RECONSTRUCTION TEST: the singlet from classical
# charts + a U(1) cocycle (PIN)

**Status:** PIN, STRICT, 2026-07-28.  **Binding:** paper 0 §4 W1 and
its kill conditions K1–K3, verbatim.  This is the executioner unit:
paper 0's thesis dies here or earns its next unit.  Lean: the paper
leans (declared [PROGRAMME]); THE PIN DOES NOT — every outcome
including all three kills is a reportable result.

## The construction (the unit's object, fixed before code)

A **two-chart weld model** of the bipartite scenario:
- Chart A: a classical stochastic description of wing A alone —
  configuration set, settings a, outcome law; likewise chart B.
  Each chart is Barandes-classical: definite outcomes, ordinary
  probabilities, NO amplitudes visible inside the chart.
- Overlap data: local amplitude-objects ψ_A(x|a), ψ_B(y|b) valued in
  a stated one-dimensional G-torsor (G swept: {1}, ℤ₂, U(1)), and a
  pairing rule producing P(x,y|a,b) from the pair through the
  cocycle — the rule STATED ABSTRACTLY FIRST (a bilinear pairing +
  squared modulus + normalization), then instantiated.  The
  worker's freedom is the instantiation; the pin's constraint is
  that the inputs are ONLY (i) the two classical charts, (ii) the
  cocycle datum in G, (iii) the declared pairing rule — nothing
  smuggled (no global state, no density matrix, no Hilbert space of
  the composite as a primitive).
- **The honesty gate against trivial rederivation:** the receipt
  must state exactly where the construction differs from "write the
  singlet wavefunction in two pieces" — the difference must be
  operational: the model class is parameterized by (charts, cocycle)
  and the CLASS is what gets measured, not one instance.

## Gates

- **G1 (singlet instance, K1's test):** exhibit a U(1) instance
  with P(x,y|a,b) = ¼(1 − xy cos(a−b)) EXACTLY at declared
  algebraic angles (the 0/45/22.5-degree family in ℚ(√2, cos π/8)
  as needed); CHSH = 2√2 exactly at the Tsirelson settings.
- **G2 (no-signaling):** every instance in every sweep has
  setting-independent marginals — a broken pairing rule fails here;
  exit-1 grade (it is a construction-correctness gate, not a
  finding).
- **G3 (trivial cocycle = classical, K3's test):** with g trivial,
  the model class at declared caps yields EXACTLY the classical
  polytope: max CHSH = 2, proven over the class by exact LP /
  vertex enumeration (16 deterministic strategies for CHSH), not
  sampled.
- **G4 (THE CLASS MAXIMUM, K2's test — the unit's headline):** the
  maximum CHSH over ALL instances of the stated construction with
  G = U(1), at declared finite caps (chart dimensions swept ≥ 2
  values; cocycle parameterized exactly; the extremization done
  exactly where possible — algebraic critical points — and by
  exact-certified bounds otherwise, method declared).  Report the
  value.  2√2 exactly ⇒ TSIRELSON FROM THE COCYCLE (the headline
  paper 0 needs).  > 2√2 ⇒ K2 fires: report what natural
  constraint (if any, e.g. cocycle unitarity/normalization
  conditions) restores the bound, and whether it is principled or
  ad hoc — the difference is the verdict.  < 2√2 at all caps ⇒ the
  construction underperforms QM; census why.
- **G5 (the group sweep):** the same class maximum for G = {1}
  (must equal G3), G = ℤ₂, and U(1); the comparison table is a
  deliverable (real-vs-complex at CHSH may not separate — that is a
  finding, not a failure; say so if so).
- **G6 (the T² hook, reporting-only):** state exactly how the
  cocycle datum here would map onto a character of the generated
  ℤ² (v11 U3's T²) — a correspondence sketch, NOT a claim; feeds
  W2.

## Pre-registered outcomes (any is the result)

- **W1-PASS:** G1 + G3 + G4 = 2√2 — the weld reproduces the quantum
  correlation set at this scenario; W2 opens.
- **W1-K1 / W1-K2 / W1-K3:** the corresponding kill fires — paper
  0's thesis is dead as stated; the census of the failure is the
  deliverable and the programme ends or repairs honestly.
- **W1-UNDER:** G1 passes but G4 < 2√2 over the swept caps — the
  instance exists but the class explanation of Tsirelson does not;
  report the gap.

## Receipt rules

Exact arithmetic end-to-end (ℚ(√2), cyclotomics; no floats in
substantive paths); the classical polytope by exact enumeration;
determinism; caps printed; substantive negatives exit 0; G2 exit-1;
runtime < ~25 min, progress prints (no silent interval > 8 min);
house style STRICT, GREEN-UNREVIEWED, no leans in the note; no
Bell-INEQUALITY-violating-nature claims (the models are toy
constructions); antecedents cited ([F], [AB]) wherever their
theorems are used.
