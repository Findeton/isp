# v12 W7 — THE PHASE-RETAINING LOOP SIGNATURE OF A COMPOSITIONAL AMPLITUDE LAW (PIN)

**Status:** PIN, STRICT, 2026-07-29.  **Binding:** paper 0 v2.2 §5
O2 (W7 supplies or kills O2's referent; failure of W7 DEMOTES O2),
W2's terminal successor target (LOG #16: a phase-retaining
invariant of ρ finer than B∘ρ), W5's recorded gauge (the
per-ordered-pair Schur–Hadamard phases; unitarity restriction only
a PARTIAL gauge fixing), W3′'s theorem (LOG #14), the three
adjudicator errors owned at LOG #18 (the founding Bargmann sketch
was gauge-typed wrong and overstated the record claim — this pin
replaces it).  Lean NONE.

## The question

Does a gauge-invariant, phase-retaining, COMPOSITIONAL signature
exist for the committed amplitude objects — one that (1) has a
precise referent, (2) composes (with what seam datum, if any),
(3) controls a committed structural phenomenon, (4) descends under
W3′ records?

    O2's missing referent  =  W2's phase-retaining successor.
    W7 is the single test of both.

## W7-0 — GAUGE CENSUS (first; it determines everything downstream)

Declare exactly, with W5-note citations for every committed item:

- **full Schur–Hadamard gauge:** U_ij ↦ e^{iθ_ij} U_ij, θ_ij
  independent per ORDERED PAIR [committed: W5's recast; unitarity
  = partial fixing only];
- **projective scalar gauge:** U ↦ ωU, ω ∈ U(1);
- **configuration-basis rephasing** (same-space conjugation);
- **source/target boundary gauge:** U ↦ D_out U D_in (bipartite —
  input and output spaces distinct);
- **compensated cut gauge:** (U₂, U₁) ↦ (U₂D, D⁻¹U₁) [committed:
  W2's cut gauge, distinguished there from uncompensated
  insertion];
- **physical change:** everything else.

FACTS to gate before anything downstream: (a) under full Schur
gauge NO phase invariant of a single matrix survives — only moduli
and support; (b) triangular products u_ij u_jk u_ki are NOT
full-Schur invariants and are not boundary-gauge invariants either
(odd cycles do not close in a bipartite support graph) — they are
ray/Gram-setting objects [MSS], the founding sketch's error.

**THE FIRST VERDICT (pre-registered, mandatory):**

- **G-REDUCED:** composition/unitarity LICENSES reduction of the
  full Schur gauge to a boundary/projective gauge — the reduction
  DERIVED, not chosen.  Candidate mechanism to prove or refute:
  full Schur transformations are not automorphisms of composition
  (they do not act on the composite consistently); the subgroup
  compatible with the declared compositional structure is exactly
  boundary + compensated cut + scalar.
- **G-POSTULATED:** no derivation — v12 explicitly CHOOSES the
  smaller gauge as new nomological structure (an EXTENSION,
  labelled as such; feeds O2's "extension, not Barandes-faithful"
  status).
- **G-ANNIHILATED:** the full Schur gauge remains physical and no
  phase-retaining single-arrow invariant exists (single-arrow
  referents dead; family/relation loops become O2's only hope).

**Kill (unit-wide):** any candidate that changes under the
finally-declared physical gauge.

## W7-1 — SINGLE-ARROW ORBIT THEOREM

For each support pattern, classify the matrix-entry phase data
modulo the declared boundary gauge by EVEN-CYCLE HOLONOMIES on the
bipartite support graph; the minimal invariants are Haagerup-type

    H_{ii′;jj′} = U_ij U_i′j′ conj(U_ij′) conj(U_i′j).

**[ANTECEDENT, not W7's theorem]:** the gain-graph/switching
classification — phases on a cycle basis classify edge-phase
assignments up to vertex switching, componentwise, with cycle rank
|E| − |V| + c [GG]; the ray/Gram Bargmann treatment [MSS].  W7's
own contribution is the exact adaptation to the committed matrix
families and to SUPPORT CHANGES (entries vanishing/appearing),
with exact-arithmetic receipts.

## W7-2 — DEGENERATE AND MONOMIAL SUPPORTS

Monomial matrices have matching supports — NO support cycles — so
the single-arrow cycle sector is EMPTY while the projective class
can be nontrivial.  **MANDATORY ANCHORS: the W2 clock/shift
families (X_N, Z_N^k), N = 2..6** — their k-classes MUST be
distinguished by the family-level invariant, or the candidate
fails (outcome W7-BARGMANN-INSUFFICIENT).  The family-level
object: operator-relation loop phases — for a projective family ρ
with lifts ρ̃, the commutator scalar

    ρ̃(g) ρ̃(h) ρ̃(g)⁻¹ ρ̃(h)⁻¹ = β(g,h)·I

is the phase retained by the relation loop; anchor β against W2's
committed six-distinct-β gate (B4x).

## W7-3 — COMPOSITIONAL CLOSURE (the deepest part)

FACT to gate first (no-descent): the gauge orbits of U₂ and U₁
SEPARATELY do not determine the orbit of U₂U₁ — witness: diagonal
D with (U₂D)U₁ ≠ U₂U₁ while each factor's isolated orbit data are
unchanged; only the compensated pair preserves the composite [W2's
cut gauge].  Hence:

    the gauge orbits of the two arrows separately carry NO closed
    composition law — a SHARED-BOUNDARY PHASE FRAME (seam torsor /
    seam datum / mixed invariant) is required, or the no-go naming
    the missing datum must be proved.

**THE COMMITTED MINIMAL MIXED CANDIDATE — the cut-coherence
tensor:** for a composable pair (U₂, U₁), path amplitudes and
tensor

    w_k^{ij}          = (U₂)_{ik} (U₁)_{kj}
    𝒞^{ij}_{kℓ}(U₂,U₁) = w_k^{ij} · conj(w_ℓ^{ij}).

GATES (each exact, each mandatory):

1. compensated-cut invariance of 𝒞;
2. outer boundary rephasings cancel;
3. projective scalars of both factors cancel;
4. diagonal = the classical path weights:
   𝒞^{ij}_{kk} = |(U₂)_{ik}|² |(U₁)_{kj}|²;
5. rank-one PSD: 𝒞^{ij} = w^{ij} (w^{ij})†;
6. the READOUT identity (a gate, NOT the main theorem):
   Δᴮ_ij = 2 Σ_{k<ℓ} Re 𝒞^{ij}_{kℓ};
7. 𝒞 does NOT detect monomial Weyl multipliers (one live path per
   endpoint pair) — gated as a FACT, the reason W7-2's relation
   loops are also required.

**THE MAIN THEOREM TARGET (completeness/minimality):** do
(single-arrow cycle holonomies) + (relation-loop phases) + (𝒞)
determine the signature of U₂U₁ up to the declared gauge, for
two-step composition — or exactly what further seam datum is
needed (prove the no-go and NAME the datum)?

## W7-4 — RECORD DESCENT

Target theorem: W3′ record hypotheses ⟹ 𝒞 BLOCK-DIAGONAL BY
RECORD SECTOR:

    𝒞^{ij}_{kℓ} = 0  whenever  r(k) ≠ r(ℓ).

NOT claimed and NOT to be claimed: phase triviality — within an
unresolved record sector, nontrivial phase loops may survive.
Full recording (every intermediate alternative individually
distinguished) ⟹ 𝒞 fully diagonal ⟹ B(U₂U₁) = B(U₂)B(U₁) on the
recorded algebra [consistency with W3′'s theorem — anchored, not
re-proved].  Controls: full vs coarse recording; record
preservation; the ERASER control (coherent recombination restores
off-diagonal blocks).  Degenerate supports: vanished amplitudes
have UNDEFINED phases, not trivial ones — handle explicitly in the
orbit bookkeeping.

## W7-5 — ONTOLOGICAL ADJUDICATION (pre-registered outcomes; combinable)

- **W7-PHASE-REFERENT:** a gauge-invariant compositional signature
  exists — O2 gains its referent;
- **W7-SINGLE-ARROW-INSUFFICIENT:** family/relation loops are
  necessary — the referent exists at family level only;
- **W7-SEAM-TORSOR-REQUIRED:** isolated local invariants do not
  compose; the seam datum is named;
- **W7-FULL-SCHUR-ANNIHILATION:** no licensed reduction — O2
  requires an explicit gauge-reduction postulate and is an
  EXTENSION (G-POSTULATED at W7-0);
- **W7-NO-RECORD-BRIDGE:** a signature exists but does not descend
  under W3′ records;
- **W7-BARGMANN-INSUFFICIENT:** the entire declared loop family
  fails the W2 collapse anchors.

The terminal verdict must state which of O2's four earning
conditions (precise referent / composes / controls a committed
phenomenon / record descent) hold.

## Anchors (mandatory; exit-1-only on every committed number reused)

1. the W2 clock/shift collapse families, N = 2..6
   (v12/code/w2_multiplier_ladder_exact.py + w2_output.txt; the
   B4x cap-free 36×36 collapse gate; the six distinct β values);
2. the W1′ Hadamard/Fourier full-support families (v12/code/w1p_*);
3. the exact rotation counterexample (paper 0 §1 T2′);
4. the reversal-odd ±14/625 witness (W2);
5. the monomial annihilator theorem (W2);
6. W3′'s stable-record model (v12/code/w3p_*);
7. W3′'s record-eraser control;
8. outer-torus and compensated-cut-gauge invariance (W2's
   committed gauges).

## Scope engraving

**W7 is one-chart/common-carrier mathematics.  It does not solve
cross-chart co-reference.  W6 remains responsible for that
bridge.**  (This is what keeps the order W7 → W6 logically valid.)

Receipt rules: EXACT arithmetic in all substantive paths
(fractions.Fraction + exact cyclotomics as integer coefficient
tuples over a declared root of unity; NO floats); anchors
exit-1-only; substantive negatives exit 0; runtime < ~60 min with
progress prints (< 8 min silent); STRICT; GREEN-UNREVIEWED until
its hostile round; antecedents cited, not claimed ([GG], [MSS]);
files v12/code/w7_*, v12/note-w7-loop-signature.md; do not touch
v11/, bc/, v13/ (read-only); the note single-threaded.
