# v12 PAPER 2 — RECORD CO-REFERENCE AND EFFECTIVE DESCENT (RETROACTIVE PIN)

**Status:** PIN (retroactive), 2026-07-29.  **Provenance:** the draft,
bundle and README were delivered by another agent post-halt
(2026-07-29 ~17:13, untracked); adopted at the user's order
("productise it") with the fix pass recorded at v12 LOG #44.  The
paper's status is **GREEN-UNREVIEWED** — its hostile round is
REQUIRED before any publishable claim and is **DEFERRED at the
user's explicit order** (token constraint).  Not citable as terminal.

## The declared stance (differs from paper 1 — by design, disclosed)

Self-contained AT THE THEOREM LAYER: every theorem proved in-paper;
every number regenerated by `paper2_code/` (standard library, exact,
39 checks, exit-nonzero-on-mismatch).  §7 is a separately scoped
comparison to the companion programme's record-co-reference
instrument (repository designation W6, cited at its TERMINAL revision
158ff8e); refs [12]/[13] name companion material deliberately.  This
is NOT the paper-1 zero-corpus-reference standard; if the user later
orders full self-containment, §7 and refs [12]/[13] are the only
sections touched.

## Adjudicator verification performed at adoption (LOG #44)

Bundle rerun from `paper2_code/`: exit 0, 39/39, RUN.txt regenerated;
float sweep 0 on all three modules; ref [13]'s full 40-char commit
hash verified against the repository; the five-configuration witness
hand-verified (column-stochasticity; realized distribution; the
predecessor-chain triviality proof; the forced opposite
identifications).  Fix pass applied: W6-status upgrade to terminal
(header, §7, ref [13] with the terminal hash); the §1.3 cross-ref
(6.2 → 6.3); three rendering repairs (`\not\Rightarrow` chain; the
§8.5 array line-breaks; the boxed conclusion); bundle paths
(`code/` → `paper2_code/`).

## The round, when the user unfreezes it

Mandatory gates: proof audit (Thm 4.1 branch realizations; Thm 5.1;
Lemma 6.1 + Prop 6.2 + Thm 6.3 rebuilt from prose in independent
code); number sweep vs the bundle; the stance audit (§7 scoping
honest; no claim leans on companion material); rendering + reference
audit (Vistoli/Moerdijk/Zaslavsky/Cavaleri-Donno/Weatherall/
Bradley-Weatherall attributions); the open-problems honesty check.
Files: paper2-record-coreference.md, paper2_code/ only.
