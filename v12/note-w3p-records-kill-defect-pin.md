# v12 W3′ — RECORDS KILL THE DEFECT (PIN)

**Status:** PIN, STRICT, 2026-07-28.  **Binding:** paper 0 v2.1 T3′
and §4 W3′, verbatim — the record is defined INDEPENDENTLY of the
defect (stable variable, orthogonal record sectors, correlation with
the alternatives, persistence under declared dynamics); the claims
hold only under RECORD-PRESERVING operations.  Lean NONE.

## The unit

- **THE THEOREM (finite dimensions):** for a stable record of {C_r}
  and any subsequent dynamics preserving the record sectors, the
  induced stochastic law on the commutative record algebra is
  DIVISIBLE — the actual residual D₂₁₀ vanishes there.  Prove
  generally at finite dimension (the conditional-expectation /
  decoherence argument, written honestly with every hypothesis
  named) AND gate on exact worked examples (the W1′ Arm-C record
  example extended; a two-step and a three-step case).
- **THE INSTANCE:** derive bc #4 M-1's biconditional ("legitimate
  division event ⟺ the process divides") as an instance of the
  theorem on the Bell model — READ THE COMMITTED STATE ONLY (the bc
  working tree carries partial uncommitted repair edits: use
  `git show cbb7279:bc/...` / the bc LOG text; never the dirty
  files).
- **THE GLOBAL CASE:** state v11's total-factuality arc as the
  theorem's global corollary SHAPE (cited to v11 LOG #14/#21;
  no v11 re-runs — v11 is frozen).
- **THE CONVERSE, scoped:** divisibility ⇒ generalized records,
  under stated purity/strong-record hypotheses — attempt at toy
  scale or scope honestly as open; the decoherent-histories
  antecedent (medium decoherence ⟺ generalized records, pure
  states) cited as the known result it is.
- **THE NEGATIVE CONTROL (the eraser), gated:** an explicit exact
  example where the record is coherently erased and the defect
  RETURNS — demonstrating the record-preserving hypothesis is
  load-bearing, not decorative.
- **KILL:** a stable record with surviving D₂₁₀ on its own algebra
  under record-preserving dynamics.

Receipt rules: exact; substantive negatives exit 0; runtime
< ~20 min; STRICT, GREEN-UNREVIEWED; antecedents cited.  Files:
v12/code/w3p_*, v12/note-w3p-records-kill-defect.md.
