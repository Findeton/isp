# v11 U1b — THE RENEWAL-GRAIN CLASS SWEEP: divisibility where the test
# has bite (PIN)

**Status:** PIN, STRICT, 2026-07-28.  **Binding context:** U1 TERMINAL
(LOG #11–#14; the delta round's mechanism finding), paper 0 §9b Phase I
outcome record, paper 0 §4 (with its Phase-I scope tags).  Parents: the
ARM-C2 telescoped construction (committed, u1 receipt), PORT-1/2/3, D62
row R4, d42b1 (the committed token structure), U1's null battery.

## The question

Is the renewal-to-renewal law divisible **where the interpolant test
has bite** — and is the answer map-robust?  U1 could not answer it: σ
gives a one-point renewal state (vacuous) and σ+ drops the arb base
(i.i.d. — column-constant transfer, test bites nothing).  U1b runs the
same three-division-event factorization over the WHOLE kinematically
admissible class of renewal-grain configuration maps.

## The admissible class (swept, not chosen)

A candidate map assigns each renewal a label computed from **committed
d42b1 token fields only** (the post-renewal serialized state and the
renewal token's own fields: base, value, provenance components — the
worker enumerates the field lattice: every subset of the declared field
set, σ and σ+ included as members, every base-retaining variant
included).  Kinematic admissibility, gated per map: **(i) fixed** — the
label set is identical at every renewal cut of the ensemble (PORT-style
gate); **(ii) committed-field-derived** — no invented data (the field
extraction cited to d42b1 by AST/slice); **(iii) declared** — the class
enumeration printed in full with its size cap.  Predictive sufficiency
is NOT an admissibility criterion and MUST NOT be used as one:
screening-off is the phenomenon under measurement, not a filter (paper
0 discussion, LOG #15 board note).

## The unit

- **BITE GATE (before any verdict, per map):** the renewal transfer's
  column-constancy tested exactly; column-constant ⇒ DEGENERATE,
  recorded, excluded from verdict rows.  No verdict may cite a
  degenerate map.  (This is the delta round's B1 made structural.)
- **Ensembles:** the committed minimal-interval ensemble (renewals at
  3/6/9; ARM-C2 re-anchored: 16/256/4,096, raw mass 1/32768, uniform
  joint 1/512 — anchors, exit 1) AND unequal-interval sub-ensembles
  reached by the same telescoping (worker declares which interval
  patterns are reachable within runtime; at least two unequal patterns
  required; every pattern's enumeration EXHAUSTIVENESS GATED in-receipt
  — the k-event continuation scan showing only the enumerated patterns
  reach renewals at the declared positions.  This is U1-delta m2,
  carried here as a hard requirement).
- **The test:** for each admissible non-degenerate map and each
  ensemble: Γ(r2←r1), Γ(r3←r1) built exactly; the factorization LP
  (column-stochastic M with Γ(r3←r1) = M·Γ(r2←r1)) decided exactly —
  Farkas certificate or exhibited interpolant, printed (digest-cap
  honesty caveat carried).  Both instruments where cheap: the eq. 22
  algebraic form beside the LP.
- **The null, beside every refusal:** random label assignments with
  matched class sizes on the SAME ensemble (printed LCG, ≥2 fixed
  seeds) — any N3 claim carries its noise comparison.
- **The census:** per-map, per-ensemble verdict table BOTH WAYS; the
  map-dependence structure explicit.

## Pre-registered outcomes (any is the result; lean NONE)

- **N1 — no bite anywhere:** every admissible map is column-constant ⇒
  a MEMORYLESS THEOREM at the swept class: the record's renewal states
  carry no cross-renewal memory under any faithful reading; the law at
  its own grain is i.i.d.; Barandes-classical; the enrichment fork is
  the program and σ+ was never defective.
- **N2 — bite + all divide:** map-robust divisibility with bite — the
  strongest U1-DIV; enrichment fork with the record-grain question
  CLOSED.
- **N3 — bite + refusal:** THE FIRST RECORD-GRAIN BRIDGE, artifact
  defense dead by construction (memory in the checkpoint state, all
  conditioning at division events, null beside it).  Census its
  location; hand to U2 immediately.
- **N4 — map-dependent verdicts:** divisibility at renewal grain is
  not map-invariant ⇒ THE SELECTION PROBLEM, posed precisely; the
  deliverable is the dependence structure (which fields flip the
  verdict) and the failed selection principles.

## Gates

Anchors exit-1-only (ARM-C2 committed numbers; d42b1 AST); exact
Fractions; determinism probe; caps and class enumeration printed;
substantive negatives exit 0; runtime < ~30 min printed; no command in
construction may run silent > 8 min (stall doctrine).

## Scope

Transport scope, the declared ensembles only; renewal grain; no claim
about non-renewal division-event candidates; no unistochasticity claim
(U3's); no measure-existence claim; scale doctrine.  L-1 quoted at the
configuration space.  No CP/Bell/covariance framing.
