# v11 P0a — THE REPRODUCTION CATALOG

## Every interesting physics-reproduction result in v1–v10, graded, with its v11 path

**Status:** ARCHAEOLOGY NOTE, 2026-07-27.  Companion to
`relativistic-isp-v11-paper0-the-indivisible-record-law.md` (v11 LEDGER
#1).  **This note proves nothing.**  It is a survey: it locates what the
corpus's ten version lines actually established about *physics domains* —
special relativity, gravity, quantum field theory, QCD, the Born rule,
decoherence, Bell — grades each result against the corpus's own errata and
graveyards, records what each result **presupposed**, and assigns each a
stratum and a reproduction path in v11's vocabulary.

It exists because paper 0 opens a program founded on v10's *negative* (the
generated grammar has no quantum phase at any native address) and on
Barandes' law-ontology — and a re-founding must not silently drop what the
earlier lines won, nor silently inherit what they only imported.  §6 is the
table paper 0's new section will cite.

---

## §0. How to read this, and the grading key

### 0.1 Grades

Grades are **this note's** judgement after carrying every correction the
corpus itself filed.  Where the corpus's own label differs, both are shown.

| grade | meaning |
|---|---|
| **[THEOREM]** | proved in the committed corpus, proof in a file, receipt behind it; the scope clause is always quoted |
| **[MEASURED]** | exhaustively verified at a stated scope; not proved beyond it |
| **[SUGGESTIVE]** | a real computation or structural observation pointing somewhere, explicitly not closed |
| **[CLAIMED-UNVERIFIED]** | asserted without a discharging receipt, or resting on an undischarged conditional |
| **[LATER-CORRECTED]** | a headline the corpus subsequently narrowed, dissolved, reopened or retracted — the correction stated inline |

### 0.2 The strata (paper 0 §3)

- **KINEMATICS** — the event grammar, actor alphabet, generated causal
  orders, configurations on cuts.  *Fixed.*  Results proved from the order
  alone transfer intact.
- **THE LAW** — the sparse indivisible family `Γ(cut′ ← cut)`, conditioned
  only at division events.  *To be constructed.*
- **THE REALIZER** — one fine-grained account of the bridges;
  ontologically optional.  v10's entire generated per-click law lives here.
  Results proved *at click grain* must be **re-derived at law grain**.
- **GAUGE** — pointwise phases (Schur–Hadamard), Hilbert-space
  trajectories (Foldy–Wouthuysen).  Results *about* pointwise phases are
  reclassified, not lost.

Opening units: **U1** indivisibility census · **U2** the J-conjecture ·
**U3** the minimal quantum record (ortho/unistochastic gap) · **U4** sparse
records on the crystals · **U5** dilation-emergeables.

### 0.3 Four corpus-wide errata carried in every entry below

1. **The `BC` / Cauchy–Schwarz erratum — LIVE, filed, PARTIALLY unrouted.**
   The quarter law's Bhattacharyya overlap `BC` is only the
   **Cauchy–Schwarz bound** on the off-diagonal multiplier, saturated
   **iff** the relative pointer phase is constant across the record
   alphabet.  The correction is **v6 paper 7 §12 item B1** (lines
   1178–1195), which exhibits phase-structured pointers with *identical*
   densities giving `V_QM = 0.726149` and `0.056135` against `B = 1.000000`
   — *"a classical-record (Bhattacharyya) clock is **FALSIFIED** by
   phase-structured which-path marking"* (5,000-trial C-S check, 0
   violations).  **Routing status, exactly:** filed at `v10/LOG.md:11472`
   and carried in `THE-THEORY-SO-FAR.md` §C1/§B2.12(i) as *"LIVE ERRATUM,
   filed and unrouted"*; **silently discharged** in `v8` paper 6 §1.2 (the
   seal overlap is a discrete influence functional
   `F = Σ_b √(P₀P₁)·e^{iΔθ(b)}` with `|F| = BC` only when recorded phases
   align, `|F| ≤ BC` in general; receipt `pPRIN_seal_record.py` 21/21);
   **never appended** to v6 paper 26, `v6/ARCHIVE-STATUS.md`, or v10 paper
   18.  **It is NOT in `v6/ERRATA.md`** — that file contains only the
   Renou sweep.  It does not move the leading coefficient; **it moves the
   theorem's hypothesis.**
2. **The `1.82` downgrade.**  v7 paper 30's dual-conjugation score of
   `1.82` for the complex-decay-constant rival (against `0` for the
   surviving amplitude form) was billed as *"the strongest single piece of
   bridge evidence in the corpus"*.  It is **not evidence**: `2·e^{−3/32}`
   reproduces all 32 published digits, `E = 3` is the argmax over
   `E ∈ 0..3999`, and adversarial integers score `0` on the same test —
   the number is a **closed-form constant of the ansatz**.  What it
   establishes is only that *some* record has `E = 3`
   (`note-d72-weld-result.md` §5(a); `THE-THEORY-SO-FAR.md` §0.3, §B2.12).
   Corollary: the 296-second, 131,526-record "anchor" for it is a **port
   fidelity check**, not evidence.
3. **The `119 → 137` correction.**  D49/B2's published completion-freedom
   count `119` becomes **137** corpus-wide; a port check reproduced `119`
   exactly *because porting the method ports the error* — **"a port check
   cannot be an independence check"** (v10 LOG #10501, absorbed at #454).
   The headline **strengthens**: the completion form is a choice with
   *more* freedom than published.  Not a physical constant; carried
   because the port-check lesson governs every reproduction attempt in §6.
4. **The "measured couplings" → "fixture-extracted" forward correction.**
   `note-d41a-nonselection-pentad.md`, appended 2026-07-19 (audit verdict
   A1, LOG #379), verbatim: *"The pentad's 'empirical' / 'measured
   couplings' wording above **OVERSTATES** … the 'measured couplings over a
   tested domain' there are values **NUMERICALLY EXTRACTED from the
   fixture's own matrices** (relative error 1e-15..1e-6), **not laboratory
   measurements**.  The wording is hereby corrected to
   '**fixture-extracted**'."*  So the corpus's own selection events are
   fixture extractions; the PDG numbers in paper 18 §4 enter as
   **[LITERATURE], pasted**.  §5.1 below depends on this.

### 0.4 The scope warning that governs all v6/v7 material

v6 and v7 are **FROZEN research logs** (2026-07-01).  Their authoritative
result map is `v8/LEDGER.md`; corrections after that date land in v8 and the
ledger **only**.  Where a frozen file disagrees with the ledger, this note
follows the ledger and says so.

### 0.5 The doctrine that bounds every entry

`THE-THEORY-SO-FAR.md` §C5.2, verbatim:

> **"THE BRIDGE IS EMPTY: zero receipts measure any v6 law on any web; the
> seal-is-record postulate (v8 paper 6 phenomenology §1.2, `[POSITED]`) has
> never been instantiated; and the two `σ`'s are a name collision."**

and §C5.4's RELATION UNESTABLISHED list, of which two rows bind hardest:
**the v10 generated grammar is not derived from the sealing formalism**,
and **v10's actors are not v6's division events** — with *measured* evidence
they are different kinds of object (§B5.7).  The honest count is *"three
bodies of work in one programme, joined by two unbridged postulates and zero
cross-measurements."*

> **Therefore: "reproduce" in this note ALWAYS means re-derive inside the
> v11 stratum assignment, and NEVER means import.**

---

## §1. SPECIAL RELATIVITY AND LORENTZ STRUCTURE

### 1.1 The `1+3` Lorentz cone from the complex rank-two algebra — v10 paper 11

**(a)** Three statements.  **Thm 2.1:** every complex Hermitian `2×2` matrix
has a unique expansion `X = tI + Σx_jσ_j`, eigenvalues `t ± |x|`, so
`X ⪰ 0 ⟺ t ≥ |x|` and `det X = t² − |x|²` — **the positive cone of
`Herm₂(ℂ)` IS the future Lorentz cone in 1+3**.  **Thm 2.2:** the
normalized rank-one projectors `P_u = (I + u·σ)/2` are exactly the
normalized null rays, `CP¹ ≃ S²`, and `Tr(P_u X) = t + u·x` — **the Bloch
sphere is the celestial sphere as manifolds**.  **Cor 2.3 ("many clocks,
four factors"):** for any `K`, the vector `(Tr P_{u_1}X, …, Tr P_{u_K}X)` is
a linear image of `(t, x₁, x₂, x₃)` — *"increasing `K` refines angular
interrogation but does not add latent dimension."*  So v9's celestial-clock
construction of Minkowski causality
(`M^{1,3}` causality `= ⋂_{u∈S²}{Δt − u·Δx ≥ 0}`) meets the finite
instrument grammar exactly at the four-real-parameter complex qubit.
Receipts: 109 frozen exact checks over `ℚ` and `ℚ(√2,i)` **including an
exact `SL(2,C)` boost** (`A = diag(2, ½)`, determinant preserved, with
`A†A ≠ I` and trace-non-preservation as counter-checks); 99 checks at 90
decimals for finite outer-cone error (tetra 200% radial excess → dual union
of 32 directions 18.75%); a bounded `H/T/SEAL` packet generating **113 exact
projective directions by word depth 12** with no external sphere sampler
inside the generator (hostile-rebuild convex-hull support
`0.912486956834076` = 9.5906% excess).

**(b)** `/Users/felixrobles/workspace/isp/v10/relativistic-isp-v10-paper11-many-clocks-few-factors-is-an-exact-kinematic-bridge.md`
§2 (Thms 2.1/2.2, Cor 2.3), §4 (Props 4.1/4.2), §§5–10 (the gaps), §12
(verdict), §13 (non-claims); ledger
`v10/note-d10-bloch-celestial-investigation.md`.

**(c)** **[THEOREM] algebraically — and NOT ORIGINAL.**  The literature
audit `v10/note-d10-literature-audit-bloch-celestial.md` §1 says so
outright: Arrighi & Patricot (quant-ph/0212135) already identify
unnormalized qubit states with the future cone in `ℝ^{1,3}`; *"D10
therefore claims no originality for `Herm₂(ℂ)₊` = the 3+1 Lorentz cone,
rank-one normalized rays = `CP¹` = `S²`, `det(tI+x·σ) = t²−|x|²`."*
**[CLAIMED-UNVERIFIED]** for any reading as a derivation of `3+1`, and the
corpus's frozen predeclared verdict is **`KINEMATICS-ONLY`** with layer
ledger: complex-qubit algebra PASS / finite outer approximation PASS /
infinite convergence CONDITIONAL on imported density / **unique complex
rank-two selection FAIL under present record axioms** / physical
Bloch-celestial identity DECLARED CANDIDATE / four items OPEN.  Prop 4.1:
real, complex, quaternionic and generic spin factors give Lorentz cones of
dimension `1+2, 1+3, 1+5, 1+9, 1+n` — *"Lorentz-cone positivity alone
cannot select `n = 3`."*  Prop 4.2: local tomography *would* select ℂ
(rebit `K_A = 3, K_AB = 10`, deficit `+1`; qubit `4/16`, deficit `0`) — but
**v8 paper 2 already proved the sealed degree-(1,1) record moments are
invariant under the complex→real comparison while the deficit changes**, so
*"the implication `sealed records ⟹ Herm₂(ℂ)` is **unavailable** under the
present axioms."*  **[LATER-CORRECTED]:** three hostile reviewers
(mathematics / ontology-locality-gauge / clean-room rebuild) all returned
**MAJOR REVISION**; the pre-review version *"omitted its registered
comparison-determinant gate and overgraded its capacity, sealing,
construction-order, influence, and supplied-connection tests"* — *"a
dictionary equality masqueraded as construction gauge; graph reachability
masqueraded as influence; `SEAL` was a string; finite dimension was
overread as record capacity."*  Round 3 closed at PASS.

**(d) Presupposed:** complex Hilbert space (one qubit collar); the `H/T`
gate alphabet; a projective `SEAL`; a **declared** shadow map
(`Δt = 1, Δx = r`); an imported Clifford+T density theorem for the
convergence limit; an imported complex tensor-product model for schedule
composition.  **No** background spacetime, **no** metric, **no** action, and
explicitly no scale: *"D10 sets neither physical metres/seconds nor `c` nor
Newton's `G`."*  The Lorentz cone enters through the *choice of algebra*.

**(e) [MY READING]** **KINEMATICS** by construction and by verdict — the one
place the corpus exhibits the Lorentz group acting on a record-native
object.  Its v11 path is **U5**: the internal factor `[B3] §4.2` dilates by
is *exactly* the complex rank-two factor paper 11 could not select, and
U5's device (impose the crystal's measured direction symmetries, ask what
internal observables the geometry forces) is the missing selection argument
Prop 4.2 declares open.

### 1.2 The complete Lorentz rulebook that cannot grow a universe — v10 paper 12

**(a) The positive half.**  Events transform `X′ = AXA†`; effects and the
unit transform **dually**, `E′ = A^{−†}EA^{−1}`; hence
`p(E|X,e) = Tr(EX)/Tr(eX)` is **frame invariant even for nonunitary
`A ∈ SL(2,C)`**.  Instrument legs transform `K′ = A_bKA_a^{−1}` with
completeness `Σ_o K_o†e_bK_o = e_a`; link paths transform only at endpoints;
diamond holonomy transforms by conjugation.  A **negative control** is
included: *"a physical-filter control changes the probability when the
effect is not transformed, so operational change is not erased as gauge."*
This closes paper 11's §7 gap (full `SL(2,C)` Born gauge) **as a separate
algebraic template**.  **73 exact checks over `ℚ(√2,i)`.**  §4's four-way
separation is the paper's cleanest conceptual product: rewrite ancestry ≠
positivity of coordinate displacement ≠ the `(t,x,y,z)` shadow ≠
interventional influence, with an exhibited counterexample (a
branch-disjoint ancilla descendant whose displacement is positive but whose
state and seal law are unchanged under the root intervention).
**The no-go.**  With `p` open ports and `j` enabled sibling joins, equal
activities give `Pr(ΔP = +1) = p/(2p+j)`, `Pr(ΔP = −1) = (p+j)/(2p+j)`, so
**`E[ΔP|H_n] = −j/(2p+j) ≤ 0`**; `P_n` is a nonnegative **supermartingale**
absorbed at zero, `Pr(hit M before 0) ≤ 1/M`, total SEAL probability `≥ 2/5`
uniformly.  Verbatim: **"The equal-activity D11 universe becomes extinct
almost surely."**  At the root only SPLIT and SEAL are enabled, so immediate
extinction has probability exactly **one-half**; JOIN never rescues growth.
Measured: **all 72 frozen histories terminated early** (longest lifetimes
`49, 171, 47`; medians `1, 3, 1`); zero ancestry/influence-cone violations;
median directional support `−1.0000, −0.8516, −1.0000` — *"the realized
directions did not even cover every hemisphere"*.  **No roundness result is
assigned.**  Frozen verdict **`INCOMPLETE-PACKET`**.

**(b)** `/Users/felixrobles/workspace/isp/v10/relativistic-isp-v10-paper12-complete-lorentz-rulebook-that-cannot-grow-a-universe.md`
§2, §3, §4, §5, **§6 (Population theorem)**, §7, §8, §9, §10; ledger
`v10/note-d11-complete-bloch-lorentz-scir-investigation.md`; **correction**
`v10/note-d11-postscript-diamond-omission.md`.

**(c)** **[THEOREM]** for extinction (standard subcritical branching, and
the paper says so); **[MEASURED]** for the 72-history campaign;
**[LATER-CORRECTED] three times.**  (i) The pre-review verdict
`COMPLETE-KINEMATICS/INFLUENCE-ENVELOPE-OPEN` was downgraded to
`INCOMPLETE-PACKET` — the kernel is complete *only once a global enabled-token
race is supplied*, and **the requested decentralized local click law is
not**.  (ii) The registry label `INTERACTION-INERT` is disowned in-paper:
*"not a valid mechanism diagnosis: the mechanism is interaction-capable but
population-extinct."*  (iii) **Decisively — the postscript narrows the no-go
to an ablation.**  Six SHARD structures D11 silently dropped are listed
(V6's primitive sealed finite diamond with screens/collar/transports;
**D8 Rule 4 is `seal and birth`, not `TERMINAL_SEAL: 1→0`**; V6/V7's clock
is survival in accumulated local RN/KL evidence, not equal activity in a
global race; whole-history compatible-family law vs Markov-on-augmented-state;
multileg interaction vs sibling-only recombination; construction-order
quotient vs one selected schedule cell).  Corrected reading, verbatim:
*"D11 proves: **if terminal observation is promoted to an ordinary physical
death token and raced globally at equal activity against SPLIT and
sibling-MERGE, the open population becomes extinct almost surely.**  It does
not refute sealed-diamond SHARD, V7's local evidence click law, or D8's
seal-and-birth SCIR architecture.  **It is retained as a negative-control
ablation.**"*  The successor candidate `COMMIT: one carrier → one sealed
outcome + one successor` flips the drift to `(p−j)/(2p+j) > 0` and is
**[POSITED]**.

**(d) Presupposed:** stated against interest in §1 — *"The complex rank-two
algebra, the root, the instrument matrices, unit activities, and the click
scale are primitive packet data.  This paper neither derives them from
sealed records nor claims that nature uses them."*  Plus **a globally
normalized sequential race over every enabled token** — a global
presentation clock, the one thing the rulebook was meant to avoid.

**(e) [MY READING]** **THE LAW (failed candidate)** — the corpus's only
previous attempt at exactly the object U1 must build.  Its extinction
theorem is a **pre-registered screen every v11 `Γ` family must pass**: any
renewal-grain law must be checked for supermartingale collapse before any
geometry is claimed.  And its postscript is a methodological gift: v11 must
not repeat the ablation — renewals are *seal-and-birth*, not terminal
deaths.  Path: **U1 (construction discipline) + U4** (crystals do not go
extinct because their growth is grammatical, not raced).

### 1.3 THE FINITE-STOCHASTIC LORENTZ NO-GO — v3 paper 8, Lemma 2.3 / Cor 2.4

**(a)** **Lemma 2.3:** if a row-stochastic `K` on a finite set has a
stochastic inverse, `K` is a permutation matrix (elementary proof in file).
**Corollary 2.4, verbatim:**

> **"An exact covariance action by invertible stochastic maps on a fixed
> finite configuration set can only act by permutations.  Therefore a
> nontrivial Lorentz boost cannot be represented in this paper as an exact
> stochastic automorphism of one fixed finite regulator state space."**

The only admissible finite covariance statements are then enumerated:
equality/convergence of declared finite-battery statistics for sampled
Lorentz-related tests; projective compatibility of finite batteries
approximating Lorentz-related continuum tests; or an imported continuum
covariance representation listed as enrichment — *"**none of these is a
finite-regulator proof of full Lorentz covariance.**"*  The same paper
installs **Lemma 2.2, the layer-ledger criterion**: *"a theorem proved at
the enriched QFT matching layer cannot be quoted later as a Γ-only
reconstruction theorem"*, and **Definition 2.5**, a nine-clause continuum
reconstruction audit.

**(b)** `/Users/felixrobles/workspace/isp/v3/relativistic-isp-v3-paper8-continuum-qft-reconstruction-no-go.md`
Lemma 2.2, Lemma 2.3, Corollary 2.4, Definition 2.5.

**(c)** **[THEOREM]** — elementary and airtight, and it is the **retroactive
downgrade of every "finite Lorentz covariance" phrasing in v1/v2**
(§1.9, §3.3 below inherit it).

**(d) Presupposed:** a finite configuration set and row-stochastic maps.
**Nothing else — no background metric, no action, no Hilbert space.**  This
is the cleanest-presupposition relativity result in the corpus.

**(e) [MY READING]** **KINEMATICS/THE LAW, and it collides directly with
paper 0 §4.**  Paper 0 posits that Barandes' fixed-configuration-space axiom
is *"exactly satisfied at renewal grain, because renewals reset to the root:
the configuration space at division events is small, fixed, and identical
across runs."*  Cor 2.4 then says: **on exactly such a space, no nontrivial
boost can act as an invertible stochastic map.**  This is not a refutation —
v11's `Γ(cut′ ← cut)` is not required to be invertible, and Barandes' own
interpolant test *is* the observation that `Γ⁻¹` is generically
pseudo-stochastic — but it is a **binding constraint that paper 0 does not
currently carry**, and it sharpens U1: the very non-invertibility that Cor
2.4 treats as the obstruction to covariance is what Barandes treats as the
signature of indivisibility.  **Reproduce as a v11 lemma before U1.**

### 1.4 Relativity with no global now — v10 chapters A3/B3

**(a)** There is **no global state**: "nows" are cuts (antichains) of the
generated order; causal order is physical and incomparable order is
**gauge**; actors act on **stale** views (with a lag census); menus run on
per-actor views at transport scope, not on the world; a delivery is a
**join in both directions** (sender-wire backflow); foliation-invariance is
therefore the *demand* on any law.  Load-bearing downstream: **any two
events sharing an actor wire are comparable** — a theorem of the layer,
corroborated over **218,795** pairs with zero violations, re-verified at
**226,223**.  The direct foliation test is exhaustive: **all 1,191 linear
extensions of all 427 canonical classes** at depth ≤ 4 `[EXACT]`.  One
recorded negative: *"foliation-invariance adds nothing to
depth-stationarity"* — constraint count more than doubles (25 vs 16; 210 vs
109; 1,374 vs 610) while the completion dimension is **exactly unchanged**;
*"the residual freedom is not gauge freedom."*

**(b)** `THE-THEORY-SO-FAR.md` §§A3.1–A3.5, §§B3.1–B3.5, §B7.3(i), §B3.5.

**(c)** **[THEOREM]** (comparability, cut/gauge distinction) + **[EXACT]**
(the linear-extension sweep) + **[MEASURED]** (the lag census, the negative).

**(d) Presupposed:** the grammar and the actor alphabet.  **Nothing else.**

**(e) [MY READING]** Pure **KINEMATICS**, and the single most important
transfer in the catalog: it is *why* paper 0's deviation from Barandes
(delete the external time axis, index `Γ` cut-to-cut) is legitimate rather
than aspirational.  **Transfers intact; no unit needed** — cite in paper 0
§2 as the discharge of "order-background, not spacetime-background".

### 1.5 Statistical Lorentz invariance of the division-event set — v6 paper 1 / paper 57 §5.1

**(a)** A **Poisson sprinkling** of division events is statistically
Lorentz-invariant: nearest-neighbour spacing CV flat across boosts, **no
frame recoverable**, whereas a regular lattice **is** frame-recoverable;
`s²` boost-invariant to **10⁻⁹⁷**.  The entropy-production arrow
`σ_arrow = D(P_AB‖P_BA)`, defined as *"what is committed at an event = its
causal past"*, reads the **frame-invariant causal partial order**:
**0/3200 mismatches** under boosts, while a constant-time-slice past changes
for **every** event — **so the arrow needs no foliation.**  And
**Hegerfeldt dissolves** for the free flash: a flash localizes a
point-*event* sampled from a density, not a positive-energy *state*.

**(b)** `v6/relativistic-isp-v6-paper1-indivisible-causal-set-gravity.md`;
verified at `v6/…-paper57-…` §5.1; summarized `THE-THEORY-SO-FAR.md` §C3.6.

**(c)** **[MEASURED]**, very high precision.  Honest status line: *"discreteness
covariantizes the kinematics outright, the interacting-distinguishable
dynamics is done, and the interacting-field dynamics is the
`[OBSTRUCTED-leaning]` wall."*  **Bounded by §1.6's BHS block.**

**(d) Presupposed:** **a background Minkowski space to sprinkle into**, and
the identification of division events with the sprinkled points.

**(e) [MY READING]** **KINEMATICS — but not v11's kinematics.**  §C5.4 row 5
records *measured* evidence that v10's records are not sprinklings (they are
worldline-woven; the shatter instrument separates the classes by a rung).
Path: **U4** — rebuild the crystals with renewal-only records and ask
whether the *renewal sublattice* ("the division events of a crystal form a
crystal") carries these properties.  Same claim, generated carrier.

### 1.6 The BHS block on point-locality — v7 note-C2

**(a)** The covariance premise factors into three legs.  **(P-Lor)
reduces** — a real, small win: the causal order is Lorentz-invariant on
owned machinery (receipt `c2`: on a finite 1+1 Minkowski sample
`s² = −t² + x²` is boost-invariant to machine precision and the causal order
is invariant at rapidity 0.9) — *"an illustration of the order-level
invariance, **not a proof of the full distributional Bombelli–Henson–Sorkin
theorem**."*  **(P-point) BLOCKED** — *"a Poisson sprinkling admits **no
Lorentz-invariant finite-valency graph** (BHS)"*; receipt `c2` exhibits it:
the `k = 5` nearest neighbours of a point in the rest frame overlap the
boosted-frame nearest neighbours in only **2 of 5**.  **(P-scalar)** is the
open emergence core; asserting it *"would be circular."*

**(b)** `/Users/felixrobles/workspace/isp/v7/note-C2-covariance-premise-deferral.md`.

**(c)** **[NO-GO, narrow scope] + [OPEN]** — and the honest deliverable is a
*note*, not a paper: *"routes to unconditionality may run through an unbuilt
matter sector, covariant nonlocal/order kernels, or another emergent Lorentz
mechanism, **but not through a frame-fixed finite-neighbour collar**."*
Converging: `v9/note-round-cone-mechanisms.md` §4.3 cites BHS as *"LI
discreteness requires that NO equivariant map to directions exists — and no
finite-valency structure is LI"*, with **the channel manifold (infinitely
many directions) as the escape BHS's theorem points at**.

**(d) Presupposed:** Poisson sprinkling into Minkowski; BHS as `[LITERATURE]`.

**(e) [MY READING]** **KINEMATICS, as a wall U4 must route around.**  v11's
crystals are finite-valency by construction, so BHS says their renewal
sublattice **cannot** be statistically Lorentz-invariant in the sprinkling
sense.  U4 must therefore test a *weaker* covariance (order-level, per
P-Lor) and say so in advance — otherwise it will manufacture a false
negative.  **Engrave in the U4 pin.**

### 1.7 Conformal structure and dimension from pure order — v7 paper 11, v8 paper 4

**(a)** From the pure partial order (plus count), with coordinates **held
out**, the conformal structure — null cones, dimension, scale-free
directions — is recovered by Malament–HKMM + Myrheim–Meyer: ordering
fractions `0.4963` vs exact `f(2) = 1/2` and `0.2259` vs `8/35 = 0.228571`;
dimension `1+1 → 2.01`, `2+1 → 3.01` **with no length input**; the link
relation concentrates on null directions (slope `0.83` at `N = 400` →
`0.93` at `N = 3000`).  **Invariances bit-identical:** a boost at rapidity
0.8 and dilations `λ = 0.137, 7.3` leave the order and link skeleton
unchanged; a reconstructed direction drifts `0.0` while its length scales
`2.70 → 19.70`.  Companion: **number → volume**, `E[N] = V/l_step^d`
(Poisson bias 0.5%, `Var/E = 0.97`) — volume **ratios** weight-0 and owned
(`0.355` recovered vs true `0.36`); **absolute** volume weight `+d` and
**walled**.

**(b)** `/Users/felixrobles/workspace/isp/v7/relativistic-isp-v7-paper11-order-owns-direction-manifoldlikeness-gate.md`
tiers I–III; `/Users/felixrobles/workspace/isp/v8/relativistic-isp-v8-paper4-gravity-continuum.md`
§1 (receipt `r1_order_to_conformal_direction.py` 9/9), §2 (receipt
`r2_number_volume_lstep.py` 6/6).

**(c)** **[THEOREM] for manifold-like orders (Malament/HKMM, IMPORT) +
[DEMONSTRATED] on the record order**; **[NO-GO]** for absolute volume.
**The gate is manifoldlikeness and it is open, field-shared:**
Kleitman–Rothschild orders dominate as `2^{n²/4+o(n²)}` and **fool a single
MM estimate** — KR returns a manifold-plausible `d_MM ≈ 2.38`, unmasked only
by a height-based estimator (longest chain **3** vs a sprinkling's **46**).

**(d) Presupposed:** background Minkowski for the demonstration; `l_step`
undetermined; `G` named as the one external input that would close it.

**(e) [MY READING]** **KINEMATICS.**  This is the corpus's cleanest
"relativity from order" result and it transfers to v11 the moment U4
produces a renewal sublattice: run MM + the height control on it.  The KR
warning is the important carry — **a dimension reading without a height
control is worthless**.  Path: **U4**.

### 1.8 The dimension discriminator and the two-clock doctrine — v10 §B5.7, §B7.1

**(a)** On genuine sprinkled records the max-shatter instrument
**reproduces the continuum ladder exactly**: sprinkled `M^{2+1}` shatters 3
and **never** 4; `M^{3+1}` shatters 4 and **never** 5.  Engineered transport
records reach **5** — one rung above every sprinkling of any tested
dimension.  Governing doctrine `[binding on all dimension claims]`:
**"DM-dimension ≤ 2 ⟺ 1+1-Minkowski embeddability, exactly"**; every
Minkowski space with ≥ 2 spatial dimensions contains crowns `S_N` of every
size, hence **infinite** order dimension; and dimension-3 orders exist
embedding in **no** Minkowski space.  *"Therefore order dimension is a
**1+1-ESCAPE DETECTOR** and a **CLOCK-COMPLEXITY GRADE** — never a
spacetime-dimension estimator.  **'3+1 spacetime is not four clocks; it is
infinitely many.'**"*  The dimensional signal is the **shatter offset**
(arcs on a circle shatter 3; caps on `S²` shatter 4; caps on `S³` shatter 5)
— *"a factor, not a divergence."*

**(b)** `THE-THEORY-SO-FAR.md` §B5.7 (`[REFEREE-CARRIED + coordinator-confirmed]`),
§A5.7, §B7.1, §B7.5; matched ladder `0/1/11/30/116/211` against zero.

**(c)** **[MEASURED]** + **[THEOREM]** (the two-clock ⟺, imported).
**[LATER-CORRECTED] repeatedly:** dimension *counting* was retired (§B7.2 —
*"the headline could not fail"*, the proxy counted idlers, numbers fell
`0.9808 → 0.672 → 0.414`); *"the layer does not select 3+1"* is a forbidden
sentence replaced by *"the layer does not **cap** the shatter ladder at the
sphere's rung"*; and the **infinite-clocks doctrine's Sperner route was
withdrawn** (§B7.4 — caps on `S²` have VC dimension 4, certified by an exact
Radon partition `λ = (−23/49, −19/49, −24/49, 17/49, 1)`, so *"the Sperner
route never fires on the sphere"*), rescued only by **trace counting**
(`Θ(n³)` cap traces vs one actor's `n+1` ⇒ actors `≥ Θ(n²)`), still
`[REFEREE-CARRIED]`.

**(d) Presupposed:** continuum trace systems for the calibration ladder;
sprinklings of `M^d` for the positive readings.

**(e) [MY READING]** **KINEMATICS** — transfers intact per paper 0 §3.  It
is the acceptance meter for **U4**, and paper 0 §10's third falsifier is
exactly this measurement.  The doctrine also settles, in advance, that U5
must not be framed as "reading 3+1 off the order".

### 1.9 The v1 operational-relativity block, and its ceiling — v1 papers 0/12/13/14/17

**(a)** **v1 paper 0** establishes the architecture (transition laws on
*pairs of Cauchy hypersurfaces*, not a preferred slicing) and its one
positive theorem: *"disjoint localized stochastic controls commute and
satisfy an operational **no-signaling theorem**."*  It also states the
disjunction v11 engraves (§3.2).  **v1 paper 12** proves, at finite
factorized scope: exact **foliation-compatible composition** (spacelike
layers commute, so foliations differing only in order give the same law);
an exact **no-signaling theorem**; exact **Bell-locality of the reduced
post-processing model** (so Bell content must live in setting-dependent full
circuits); exact **boundary-flux structure** (Gauss law makes naive
factorwise locality the wrong target); and a **conditional** Lorentz-covariant
detector/control theorem at benchmark scope under an explicit
sufficient-condition package.  **v1 paper 13** proves the Einstein-causality
bridge at the *induced-net* level and refuses the slogan: *"the bridge to
Einstein causality **cannot honestly be** the claim that the raw
comparison-map commutator is already a Haag–Kastler commutator"* — and paper
14 adds that *"the present stack already contains **explicit nonzero
disjoint-support raw exchange** in the free and gauge benchmarks."*
**v1 paper 14** repairs it: the physical-sector configuration space is a
**fiber product** over matching boundary-flux data, so the induced net is
**center-resolved** — on each admissible joint boundary-flux block the
regional algebras commute exactly and Einstein causality holds outright.
*"**The physical Gauss sector does not fail locality; it fails naive
factorization.**"*

**(b)** `v1/relativistic-isp-v1-paper0-…md`;
`v1/…paper12-localized-controls-detectors-operational-relativity.md` results
3/4/5/9/13; `v1/…paper13-einstein-causality-bridge-exchange-defect-haag-kastler.md`;
`v1/…paper14-boundary-flux-resolved-einstein-causality.md`;
`v1/…paper17-theorem-level-scope-synthesis-limits.md`.

**(c)** **[THEOREM]** at finite factorized/benchmark scope for the
no-signaling, composition, Bell-locality and center-resolution results;
**[CONDITIONAL]** for the Lorentz-covariant detector theorem;
**[LATER-CORRECTED]** in bulk by §1.3's Cor 2.4, which converts every
"finite Lorentz covariance" phrasing into sampled-battery language.  The v1
line's own ceiling, **v1 paper 17** verbatim: *"The present sequence **does
not prove a full Lorentz-covariant continuum ISP**, a Haag–Kastler QFT
reconstruction, arbitrary-coupling gauge locality, cutoff- or
volume-uniform compact-`U(1)` control, a non-Abelian theory, a continuum
Schwinger-model theorem, a Bell local-causality theorem for arbitrary
microscopic couplings, a general interacting exchange-defect theorem, or any
gravitational dynamics."*  And the closing self-grade: *"The relativistic
ISP sequence has earned **more than a metaphor and less than a
foundation**."*

**(d) Presupposed:** background spacetime with Cauchy hypersurfaces;
factorized finite configuration spaces; a finite matter-link `U(1)` lattice
benchmark with a Gauss constraint; ancilla/unitary/pointer realizations (a
finite Hilbert space as *enrichment*); Criterion A's sufficient-condition
package as a **hypothesis**.

**(e) [MY READING]** **REALIZER**, on a supplied spacetime — with one
exception that transfers: **the no-signaling/commutation theorem is a
statement about disjoint localized maps**, which at renewal grain becomes
"division events on disjoint carriers commute".  That is exactly what
U1 needs as its *null hypothesis* (the divisible case).  Path: **U1, as the
control arm.**

### 1.10 Two withdrawn Lorentz headlines, recorded so they are not re-inherited

**(a)** **(i) "Record growth CAN generate a Lorentzian-band cone" —
RETRACTED.**  LEDGER #134 / the D30 LOG entry originally claimed
`K_collar` enters the `M²` band.  The integrated hostile round (reviewer
regrew all 60 webs and reproduced every number digit-for-digit) **flipped
the verdict**: the round-0 rule *"compared incommensurable exponents (collar
`c₂ = 2.91` vs card `1.801`: +61.5%, **above the `m₂ = 2` asymptotic
ceiling** — structural exclusion at every `N`) and used the 0.35
estimator-accuracy bar (8.75× the card's band; the collar sits −6.3 card-SD
from `M²`), with ratio3 measured-and-ignored (−10 SD)."*  Corrected:
**BRANCH 3 — no frozen kernel enters any manifold band**; the survivor is a
*stable finite-scale **effective** dimension* `d̂ ≈ 1.76 ± 0.10` (the phrase
"non-integer-dimension order" was itself corrected by external review).
Consolidated in v10 paper 20: *"no kernel enters any manifold band (nearest
approach **≥ 10.4σ**)"*, and *"**interaction never lifts effective
dimension** in these families; the one directional family tested
anti-lifts."*  **(ii) D29's spatial/cone instrument row is VACANT** — the
delivered estimator *"consumes COORDINATE time differences, violating this
section's own order-only header"*, so **the corpus has no validated
order-only cone/spatial instrument for grown webs**, and *"the arc has
measured the order geometry of a particular event DAG — **not** an emergent
spatial metric or light cone."*  **(iii) D31A `[THEOREM]`:** in the
none-free unbounded-growth grammar, a stationary, path-covariant,
birth-positive kernel with `u`-only weights **cannot assign positive weight
to interactions**; the unique such kernel is pure birth `w_b(u) = 1/u` —
and with D28's T2, *"the stationary + path-covariant + graph-blind class
contains **no diamond-forming kernel**"*, i.e. **cannot make causal diamonds
at all**.  **(iv) D31B `[THEOREM]`:** port poset ≡ wire-DAG (both are the
transitive closure of "next op on a wire"); the **operational-influence
graph is never a poset**, because influence can cancel through compositions.

**(b)** `v10/LOG.md` 2026-07-12 entries; `v8/LEDGER.md` #134/#135;
`v10/note-d30-frozen-kernel-cone-comparison.md`;
`v10/note-d29-intrinsic-causal-rulers.md` §1 ARC-ROUND DISCLOSURE;
`v10/note-d31-stationarity-nogo-and-the-two-causal-structures.md` §§1–2;
`v10/relativistic-isp-v10-paper20-the-geometry-of-record-growth.md`.

**(c)** **[LATER-CORRECTED]** for (i)/(ii); **[THEOREM]** for (iii)/(iv),
hostile-verified.  Width of (iii) is carefully stated: *"**Locality is NOT
specifically forced**"* — only sensitivity to state richer than `u`; and
path-covariance itself is forked as open (*"if the order of operations is
itself recorded physical history, the covariance quotient is a hypothesis"*).

**(d) Presupposed:** for (iii)/(iv), a rewrite grammar with births and
interacts; path-covariance as a demand.  **No background spacetime, no
metric, no Hilbert space.**

**(e) [MY READING]** **KINEMATICS.**  (iii) is a genuine transfer and a
warning for **U4**: a law that is stationary, covariant and blind to
anything but a scalar occupancy count **cannot build diamonds**, hence
cannot build causal structure.  v11's renewal-grain `Γ` must therefore be
richer than any such scalar — which is a *prediction* U4 can check.  (iv) is
the vocabulary v11 should adopt verbatim: **order ≠ influence**, and only
the first is a poset.
