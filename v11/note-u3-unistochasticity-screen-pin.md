# v11 U3 — THE UNISTOCHASTICITY SCREEN, the loop-class reproduction,
# and the gauge candidate (PIN)

**Status:** PIN, STRICT, 2026-07-28.  **Binding specification:** paper
0 §7 (U3), §5 (the gauge stratum), §6; the catalog [V11-CAT] §7 rank 4
and its §3/§4 graded entries; U1's §11 handover (Born = K1 in exact
ℚ(√2), defect 0; the Ẑ-completed sibling, conflict row {1/7, 3/4,
3/28}, column sums 25/28, 1, 59/64, 55/64, 31/32, 19/14 — the screen's
flagged threat); U1's §6 sharpening: THE UNISTOCHASTICITY SCREEN, not
indivisibility, is Barandes' quantum criterion ([B3]: indivisibility
necessary, unistochasticity the criterion; Schur-Hadamard entrywise
phases are gauge).  Parents: v10 paper 31 §4.3, the D75 audit pin (b),
D74 (group ⟨2,3⟩, R⁺, odd sector empty), D71b (the v7 i-twist form).

## The question

Does ANY committed generated matrix pass the quantum criterion — Γ_ij
= |U_ij|² for a single unitary U — and for each one that fails, WHAT
exactly blocks it?  A failure is not a defeat: it prices enrichment
(what mass must move, which phases must exist) exactly.

## The arms

- **Arm A — THE SCREEN.**  For each committed matrix (the K1
  arbitration objects V_single/V_pair and their row/transfer readings;
  the Ẑ-completed sibling; the ARM-C2 column-constant transfer as the
  degenerate control): **(i)** the doubly-stochastic precondition —
  row AND column sums exactly 1, exact rationals; a failure is a
  first-class finding, quantified (the exact excess/deficit per column
  — the sibling's 19/14 either disqualifies at this scope or is scoped
  away with the reason printed); **(ii)** for every matrix passing
  (i): unistochasticity decided at its size — n = 2 by the classical
  fact (every 2×2 doubly-stochastic is unistochastic — orthostochastic
  even; cite and verify in-receipt), n = 3 by the known exact
  characterization (the worker states and cites the criterion used and
  implements it exactly — algebraic certificates, no floats), larger n
  by exact phase-elimination (resultants/Gröbner over the declared
  ring) at a printed size cap; deliverable per matrix: the unitary
  exhibited (exact/algebraic entries) or the obstruction named with
  exact margins.
- **Arm B — THE LOOP-CLASS REPRODUCTION** ([V11-CAT §7 rank 4]).  The
  corpus's committed U(1) loop-class theorem restated and its
  reproduction attempted on the generated carrier (D74's committed
  loop census — the ⟨2,3⟩ group, the 44 squares): does the committed
  theorem's loop-class structure exist on generated loops, or is the
  obstruction exactly the R⁺-only/odd-sector-empty verdict?  The
  catalog's graded entry is the source of the theorem's statement and
  location (worker locates and cites file:line; if the catalog's grade
  carries corrections, they are binding).
- **Arm C — THE GAUGE CANDIDATE.**  The v7 amplitude form
  A(R) ~ e^{−K·even}·e^{iΦ·odd} read as a [B3] Schur-Hadamard gauge:
  on the matrices that pass Arm A(i), does attaching the odd-sector
  phase entrywise constitute a gauge transformation in [B3]'s sense
  (leaves |·|² invariant by construction — gate the triviality
  honestly) and does ANY committed generated quantity select a
  non-trivial phase assignment (the D74/LD negatives bind: no native
  odd sector exists — this arm tests whether the GAUGE STRATUM can
  carry the v7 form consistently, not whether the grammar generates
  it; correspondence ≠ identity, the missing-map discipline).

## Pre-registered outcomes (lean NONE)

- **S-PASS:** some committed generated matrix is unistochastic — the
  corpus's first quantum-eligible generated object; census which, and
  hand to U2/the weld.
- **S-FAIL-DS:** the doubly-stochastic precondition fails —
  the exact mass imbalance IS the price of enrichment, stated as the
  unit's headline (what a quantum-eligible completion must add).
- **S-FAIL-UNI:** DS passes, unistochasticity fails — the obstruction
  (which criterion, what margin) named exactly; enrichment priced at
  the phase level.
- **Mixed per matrix** — the census is the result.
- Arm B/C outcomes: reproduction achieved / obstruction named; gauge
  carriable / trivial / inconsistent — each reportable either way.

## Gates

Anchors exit-1-only (the audit pin (b) sibling numbers; paper 31 §4.3
K1 objects; D74's committed census where Arm B reads it); exact
arithmetic end-to-end (ℚ(√2) and stated algebraic extensions; no
floats in substantive computation); every criterion used cited to a
source and verified on a known-answer control (one unistochastic and
one bistochastic-but-not-unistochastic reference matrix from the
literature, stated and checked in-receipt); determinism; caps printed;
substantive negatives exit 0; runtime < ~25 min; no silent command
> 8 min (stall doctrine).

## Scope

Matrix-level screening only — no claim that a passing matrix IS a
quantum system (the missing map); no CP framing; no Bell framing; the
sibling's scope is the audit pin (b)'s; U1b's biting transfers are NOT
awaited (file-disjoint parallel unit) — screening them is a declared
follow-up if U1b returns any.  Scale doctrine; L0's prohibition
untouched.
