# v11 U1 — THE INDIVISIBILITY CENSUS (PIN)

**Status:** PIN, STRICT, 2026-07-27.  **Binding specification:**
paper 0 §7 (U1: preamble + three arms), §6 constraints 1/4/6, §10
falsifiers; the catalog [V11-CAT] §4.5 (Born = K1), §4.9-preamble
(v1 p22's definition with the audit's named defect), §6 rows, §7
ranks 1 and 4.  Parents: v10 D70 (the relative-horizon kernels),
D72/D74 (the 44 descent-obstruction squares, §B2.12(iv)), D62 row
R4 (renewals), [B3] eqs. 22–23, v10 paper 29 (the descent
conditions), v10 paper 31 §4.3 (Born = K1), D75 audit pin (b) Arm 0
(the Ẑ sibling, specified to the receipt).

## The unit

- **Preamble:** restate v1 paper 22's definition of indivisibility
  with the audit's named defect repaired; this is the definitional
  ground every gate below cites.
- **Arm 0:** rebuild Born = K1 on the committed arbitration layer
  (exact rationals; the isometric lift re-verified: defect exactly
  0, branch amplitudes ½–½, menus reconstructed at both cuts) and
  construct its Ẑ-completed sibling per the audit's specification
  (conflict row {1/7, 3/4, 3/28}, total mass exactly 1).  These
  matrices anchor Arms 1–2 and are handed to U3.
- **Arm 1:** the renewal-to-renewal Γ family from the committed
  transport grammar (v10's relative-horizon kernels read at record
  grain; renewal = the D62-R4 predicate ported to transport scope
  with its port verified against the layer).  Run [B3] eq. 22:
  Γ̄ = Γ(c″←c)Γ(c′←c)⁻¹ through every intermediate cut reachable at
  the declared caps; where Γ(c′←c) is singular, the LP feasibility
  form.  Count negative entries EXACTLY (rationals).  Census by
  cut class; the 44 descent-obstruction squares first and named.
- **Arm 2:** paper 29's four descent conditions on the SAME family,
  same caps.  The two instruments' agreement/disagreement census is
  a first-class deliverable.
- **Constraints (engraved):** NOT a CP test (§6.6); no
  Bell/locality framing (§6.4); L-1's bound quoted wherever exact
  covariance could be implied.

## Gates

Anchors (exit 1 only here): the committed v10 layers by
AST/text-slice with gated exit-freedom; D70's kernel anchors; the
44-square census reproducing D74's committed numbers; Born = K1's
committed row (defect 0).  All else substantive-exit-0.  Exact
arithmetic end-to-end; both-ways censuses; determinism probe; no
bare-constant predicates; runtime printed; caps printed, nothing
silent.

## Pre-registered outcomes (either is the result)

- **U1-DIV:** stochastic interpolants and clean descent everywhere,
  at every grain and cap ⇒ the generated law is Barandes-classical
  by his own criterion at every tested grain; enrichment becomes
  the program.  State it in those words (§6.1).
- **U1-BRIDGE:** at least one pseudo-stochastic interpolant or
  descent failure at renewal grain ⇒ THE CORPUS'S FIRST GENERATED
  INDIVISIBLE BRIDGE; census its location (J-geography for U2,
  cut-class, kind-pairs).
- **U1-SPLIT:** the two instruments disagree ⇒ characterize the
  disagreement exactly; it outranks either verdict.
- **Lean: NONE** (the record is 0-for-5 on quantum-location
  intuitions).

## Scope

Transport scope, declared families/caps only; renewal grain per the
§4 posit (the posit is under test, not assumed proven); no
infinite-volume claims; no measure-existence claim (D70's bound
open); scale doctrine.
