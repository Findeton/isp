# LD — result: **LD-BROKEN.** The last door is not shut and not open — it is **walled**. The record-category reversal is a clean involution on records and is exactly poset reversal, but it is **not an operation on the committed grammar**: the reversed record of **every** holonomy-carrying loop (`662/662`, four arms), of the **Born = K1** arbitration row, and of **all 40** committed half-open squares is refused at admission. The obstruction has a one-kind address — a square's image is priced **if and only if** its record carries **no arbitration event** (`17,277/17,277`, exact in both directions) — and **every** holonomy-carrying loop carries one.

**Read the scope before the headline.** The reversal here is **FORMAL**
— category-level. Nothing in this unit claims that reversed records are
generated, that anything runs backwards, or that any dynamical or
thermodynamic asymmetry has been exhibited; no such language appears in
the receipt. Every number is a window-dependent measurement on the
committed d42b1 transport grammar at the declared depths and pools.
Weight-system level only: no measure claim.

**Status: TERMINAL (conferred by the hostile round of 2026-07-27,
adjudicated v11 LOG #10 — the delivery version of this line
self-declared TERMINAL before the round had run, a status defect the
round caught as its M1), STRICT, 2026-07-27.** Pin
`v11/note-ld-last-door-pin.md` (binding, frozen before any code was
written). Binding specification: paper 0 §9b, the catalog
[V11-CAT] §4.7 branch (a). Parents: v10 **D74 TERMINAL**
(`note-d74-transport-holonomy-result.md` — the transport holonomy is
`R⁺`, group `⟨2,3⟩`, odd sector empty on the tested carrier: the
0-for-5 record), **D72 TERMINAL** (the transport-scope census this unit
anchors on), **D71b** (the order-dual carrier), **D73** (carry an
asymmetric substrate). Receipt
`v11/code/ld_reversal_probe_exact.py`, output `v11/code/ld_output.txt`
— run from the repository root, **exit 0**, **35 PASS / 0 FAIL** of
which **3 carry no independent information and are labelled as such**
(**32 independent-evidence passes**), **13 s** wall clock against the
pin's ~25 min budget (the delivery note said 11 s; the committed
receipt line reads 13 s). Re-run at `PYTHONHASHSEED=7`: **output identical
apart from timings and the echoed seed** (0 differing lines).

**Lean: NONE**, as the pin declares. The 0-for-5 record is **not**
advanced to 0-for-6 by this unit; see §6.

**Every committed number this unit rests on reproduces exactly.** Eight
anchor gates over six committed families, `exit 1` reserved for them and
not reached: D72's `T6.1` census
at both windows (`3,969` histories, `1,546` closed, `88` non-unit,
spectrum `{1/2: 70, 2/3: 2, 3/2: 6, 2: 10}`, kinds `{(r,d): 68,
(d,r): 8, (d,n): 6, (d,d): 4, (n,d): 2}`, `142` both-blocked,
delivery-bearing `88/88`, shallowest depth 3; and `3,424` histories,
`1,554` closed, `12` non-unit, kinds `{(r,d): 12}`, `42` both-blocked);
`T6.2`'s half-open row (`40`, AB-only `28`, BA-only `12`, kinds
`{(r,d): 12, (d,r): 4, (p,d): 16, (d,p): 8}`); `T6.4`'s minimal witness
digit for digit; D74's group row (value set `{1/2, 2/3, 3/2, 2}`, prime
support `[2,3]`, rank 2, index 1 in `Z²` — `⟨2,3⟩`, recomputed as an
integer exponent lattice by Hermite reduction, not read off by eye);
D74's `D5.2` order-dual row (`176` endpoints, `304` linear extensions,
`304/304` admissible forwards, `0/304` reversed); and paper 31 §4.3's
**Born = K1** row (`1/2`–`1/2` on the 2-conflict, arbitration sector
`1/4`), recomputed from the committed layer.

Where this note and the receipt disagree, **the receipt is
authoritative**.

---

## 0. The one-paragraph answer

The record category of the committed transport grammar is **not
self-dual**. The opposite category exists — every category has one —
and the reversal that realises it on records is a clean involution
which is exactly poset reversal, gated `7,393/7,393` both ways. What
fails is **realisation**: the committed law does not price the opposite
record, and it fails on **the whole curvature**. Of the `662`
holonomy-carrying loops across four arms — the anchor windows' `88` and
`12` and the defected schedules' `334` and `228` — not one has a
reversed counterpart to classify: `0` INVERSION, `0` INVARIANT, `0`
OTHER, `662` UNDEFINED. The Born = K1 row is refused at its very first
event. All `40` half-open squares are refused. The obstruction is
**consumption-before-production** and it has a **one-kind address**: a
closed square's image is priced **iff** its record carries no
arbitration, with no exception in `17,277` squares in either direction,
and every curved loop carries one. So the wall across the last door and
the source of the transport curvature sit at the same address — the
join.

---

## 1. LD-A — the reversal functor, and the relabelling table

A committed record is a sequence of events; its causal order is the
committed `event_poset`, generated by register overlap in sequence
order. The record-category reversal is the formal opposite of that
order, realised on sequences by

```
R(e_1, ..., e_n) = (rho(e_n), ..., rho(e_1))
```

where `rho` is the event-kind relabelling reversal forces. Exactly one
committed kind carries a direction in its own data — delivery, whose
sender and receiver are distinct fields. A send read backwards is a
receive, so `rho` transposes them. Every other committed kind is a
single-carrier local event whose data carries no direction, so `rho`
fixes it: the polarity of `p`, `r`, `m` is carried by the **poset**,
and the poset is what `R` reverses.

**THE RELABELLING TABLE** (the pin's printed deliverable), with the
register set each kind occupies:

| kind | committed form | `rho(e)` | `regs_of` | dual |
|---|---|---|---|---|
| `n` | `('n', a)` | `('n', a)` | `{a}` | self |
| `p` | `('p', a, b, x)` | `('p', a, b, x)` | `{a}` | self |
| `r` | `('r', a, ck, wk)` | `('r', a, ck, wk)` | proposers + created version | self |
| `m` | `('m', a, pk, w)` | `('m', a, pk, w)` | `{a, mw}` | self |
| `d` | `('d', s, r, v)` | `('d', r, s, v)` | `{s, r}` | **SWAP** |

**A.1** `rho` is an involution on the committed event alphabet,
exhaustively: `309/309` realised events satisfy `rho² = id`, structure-
equal; the `98` delivery events are the ones it moves.

**A.2** `rho` preserves `regs_of`, exhaustively (`309/309`). This is
not decoration. The committed causal order is *generated* by register
overlap, so this identity is precisely why `R` is poset reversal. It
holds because the delivery's register set `{s, r}` is **unordered**:
the committed layer already treats a delivery as a **join** of two
carriers and records no direction in the register geometry. The
direction lives only in the field order, which is what `rho` acts on.

**A.3 — THE INVOLUTIVITY GATE (pin LD-A).** `R(R(h))` restores the
original record exactly, on **every** record of both anchor arms:
`7,393/7,393`, structural tuple equality on the full event sequence —
every field of every event, in order — not equality of a canonical form
and not equality of a derived invariant.

**A.4** `R` is **exactly** poset reversal, not an approximation of it:
for every record of both anchor arms the committed `event_poset` of
`R(h)`, pulled back along `i ↦ n−1−i`, is the opposite of the committed
`event_poset` of `h` — the full relation, pair for pair, both
directions, `7,393/7,393`. This is the sense in which the unit reverses
the **record category** rather than a traversal direction. D74's `D1` —
traversal reversal acts on `r` by inversion — is an algebraic identity
of the definition of `r` and says nothing about the category; it is not
available here and is not used.

**A.5, declared before the census and carrying no information.** One
can always define the opposite weighted category abstractly by
*decreeing* `q^op(rho(e) | R(h·e)) := q(e | h)`. Then
`mu^op(R(σ)) = mu(σ)` by construction, every holonomy value is
trivially INVARIANT, and the census answers its own question. The
content of the last door is not whether `C^op` **exists** but whether it
is **realised** by the committed law. That, and only that, is measured.

---

## 2. LD-B — the census

### 2.1 The quantity and the convention, stated before the run

A closed exchange square is the loop
`h → h·eA → h·eA·eB = h·eB·eA → h·eB → h`, with committed holonomy

```
v = q(eA|h) q(eB|h.eA) / q(eB|h) q(eA|h.eB) = mu(sigma_AB) / mu(sigma_BA)
```

for `sigma_AB = h·eA·eB` and `sigma_BA = h·eB·eA`; the common prefix
cancels, gated at **B.0** on `3,100/3,100` closed squares of the anchor
arms. Under `R` the two endpoints go to `R(sigma_AB)` and
`R(sigma_BA)` — the exchanged pair moves to the **front** and the
prefix becomes a common **suffix**, which is why the reversed side must
be computed from whole records — so

```
v_rev := mu(R(sigma_AB)) / mu(R(sigma_BA))          [functor-image convention]
```

with `mu` the **committed** measure: the product of the committed
conditional weights, read from the empty record by the committed
admission relation. Nothing is decreed; the committed law is asked to
price a record it did not build.

Four classes: **INVERSION** (`v_rev = v⁻¹`), **INVARIANT**
(`v_rev = v`), **OTHER** (defined, neither — the door opening), and
**UNDEFINED** (one or both reversed endpoints are not admissible
records of the committed grammar, so the law assigns them no weight at
all). On a loop with `v = 1` the first two coincide; those are counted
separately and never as evidence of either.

**The verdict rests on convention-free classes.** Flipping the
orientation exchanges INVERSION and INVARIANT and can do nothing else,
since the two readings differ by an exact reciprocal. OTHER and
UNDEFINED are untouched by it, and **B.5** measures that: `OTHER 0 vs
0`, `UNDEFINED 526 vs 526` on the anchor arms.

### 2.2 The census, both ways — by value class

| arm | `v` | loops | classification |
|---|---|---|---|
| AB4 `(A,B) d≤4` | `1` | 1,458 | UNDEFINED 360, unit (INV=INVAR) 1,098 |
| | `1/2` | 70 | **UNDEFINED 70** |
| | `2/3` | 2 | **UNDEFINED 2** |
| | `3/2` | 6 | **UNDEFINED 6** |
| | `2` | 10 | **UNDEFINED 10** |
| ABC3 `(A,B,C) d≤3` | `1` | 1,542 | UNDEFINED 66, unit (INV=INVAR) 1,476 |
| | `1/2` | 12 | **UNDEFINED 12** |
| ASYM1 `(A,B) d≤5`, one-way `A→B` | `1` | 5,170 | UNDEFINED 2,946, unit 2,224 |
| | `1/2` | 184 | **UNDEFINED 184** |
| | `2/3` | 10 | **UNDEFINED 10** |
| | `3/2` | 68 | **UNDEFINED 68** |
| | `2` | 72 | **UNDEFINED 72** |
| ASYM2 `(A,B,C) d≤4`, ring `A→B→C→A` | `1` | 8,445 | UNDEFINED 1,734, unit 6,711 |
| | `1/2` | 190 | **UNDEFINED 190** |
| | `2/3` | 2 | **UNDEFINED 2** |
| | `3/2` | 10 | **UNDEFINED 10** |
| | `2` | 26 | **UNDEFINED 26** |

Every non-unit row is UNDEFINED, at every value, in every arm.

### 2.3 The census, both ways — by loop class (kind pair)

The AB4 rows, complete (ABC3, ASYM1 and ASYM2 are in the output at the
same shape):

| kinds | loops | classification |
|---|---|---|
| `(d,d)` | 77 | UNDEFINED 12, unit 65 |
| `(d,n)` | 213 | UNDEFINED 18, unit 195 |
| `(d,p)` | 82 | UNDEFINED 4, unit 78 |
| `(d,r)` | 30 | **UNDEFINED 30** |
| `(n,d)` | 71 | UNDEFINED 6, unit 65 |
| `(n,n)` | 69 | UNDEFINED 4, unit 65 |
| `(n,p)` | 86 | UNDEFINED 8, unit 78 |
| `(n,r)` | 34 | **UNDEFINED 34** |
| `(p,d)` | 254 | UNDEFINED 20, unit 234 |
| `(p,n)` | 258 | UNDEFINED 24, unit 234 |
| `(p,p)` | 100 | UNDEFINED 16, unit 84 |
| `(p,r)` | 36 | **UNDEFINED 36** |
| `(r,d)` | 90 | **UNDEFINED 90** |
| `(r,n)` | 102 | **UNDEFINED 102** |
| `(r,p)` | 36 | **UNDEFINED 36** |
| `(r,r)` | 8 | **UNDEFINED 8** |

Read down the table: **every loop class containing an `r` is 100 %
UNDEFINED, in every arm.** That is the visible face of §3's exact
characterization. The classes without an `r` are mixed, because their
*prefixes* may still carry one.

### 2.4 The half-open squares and the Born = K1 row

**B.3.** A half-open square carries no holonomy value, so `R` is asked
a different question of it: does the functor carry its **admissible**
endpoint to an admissible record, and does the half-openness transpose?
All `40` committed half-open squares (AB-only `28`, BA-only `12`) have
their open endpoint **refused** under `R`, and their blocked endpoint
refused too. There is no transposed half-openness to read, because
there is no priced image to read it from.

**B.4 — the Born = K1 arbitration row.** The committed row is the
2-conflict. The minimal committed fixture is

```
h = [ ('p','A',v0,0), ('p','B',v0,1) ]
e = arbitration by A on the 2-conflict
K1(ckey) = { A's triple: 1/2 , B's triple: 1/2 }
committed branch weights = 1/8, 1/8 ;  arbitration sector = 1/4
```

— paper 31 §4.3's row, reproduced exactly from the layer (anchor K1.1),
and `mu(record) = 1/512`. Its image under `R` reads `[r, p, p]` and is
refused at index `0`, kind `r`: the arbitration stands **before** the
two proposals whose conflict it resolves. The `K1` kernel is a function
of the live-proposal component visible in the arbitrator's past cone;
in the reversed record that cone is empty, so there is no component, no
`ckey`, no `K1` weight and no Born split to compare. **The row does not
invert, does not stay fixed, and is not OTHER: it is not there.**

### 2.5 The relabelling control

**B.6.** The negative is not an artefact of the chosen `rho`. Twelve
**involutive** relabellings are run over the anchor arms — the
identity (which is D74's `D5` arm's operation), the forced delivery
transposition, and each composed with every involutive actor
permutation of the arm's pool, applied to every actor field the
alphabet carries: proposal triples, arb `ckey`s and `wkey`s, merge pair
keys, version authorship. Under **every one**, `0/88` and `0/12`
holonomy-carrying loops acquire a priced reversed counterpart. **B.6b**
sweeps the four non-involutive 3-cycles separately — they would fail
the involutivity gate by construction and are not relabellings this
unit admits — and they do not move the census either.

---

## 3. LD-C — the obstruction, characterized exactly

**C.1 — every first blocker is a consuming kind, and idle never
blocks.** Over `1,052` refused reversed records of the anchor
arms, the first-blocking kinds are `{r: 908, p: 104, d: 40}` — a
subset statement, not an equality: the merge, also consuming, never
appears because it is never realised at these depths (D.4). The idle
— the one committed kind that consumes nothing, whose admission is
unconditional and whose weight is the residual budget — blocks `0`
times; that clause is a corollary, not a discovery, since an
unconditional admission has no failing branch. The blocking positions
are `{0: 502, 1: 470, 2: 80}`.

**C.2 — the obstruction is consumption-before-production, and it is
exact.** For **every** refused reversed record (`1,052/1,052`) the
first blocking event has a **non-empty causal future** in the reversed
record — equivalently, by A.4, a non-empty causal past in the forward
record. The committed admission relation is past-relative: it reads the
event's own past cone and admits only what that cone supports. Reversal
moves an event's whole support into its future, so a consuming event is
offered to the admission relation before anything has produced what it
consumes. Nothing about the weights is involved; the refusal is at
**admission**.

**C.3 — why no relabelling can repair it.** A relabelling repairs the
reversal only if it sends each consuming kind to a kind that
**produces** what the original consumed: the dual of an arbitration
(consumes live proposals, produces a version) would have to consume a
version and produce live proposals. The committed alphabet is
`{n, p, r, m, d}` and each of `p, r, m, d` is monotone in the same
direction — each **adds** to the record state (a live triple, a created
version, a merged version, a holding) and none removes anything. The
committed layer has no retraction, no withdrawal, no un-delivery and no
un-merge. **The alphabet contains no annihilation kind, so no bijection
of it can be a duality**, and B.6 measures exactly that.

**C.3a — THE OBSTRUCTION HAS A ONE-KIND ADDRESS, EXACT IN BOTH
DIRECTIONS.** A closed square's reversed counterpart is priced by the
committed law **if and only if its endpoint record carries no
arbitration event**. Not "mostly", not "except for a residue":

| | image priced | image refused |
|---|---|---|
| record carries an arbitration | **0** | 5,768 |
| record carries none | 11,509 | **0** |

over all four arms (`17,277` closed squares; the anchor arms alone give
arb-carrying-and-priced `0`, arb-free-and-refused `0`, against
arb-carrying-and-refused `526` and arb-free-and-priced `2,574`). The
unifying mechanism: the arbitration is the only realised kind that
**creates** a version and supersedes a base, so reversal puts
consumers of created state before its creation. The `r` first
blockers (`908`) refuse on the emptied **joint** past cone — the
live-proposal component with its incomparability edges; the `p`
(`104`) and `d` (`40`) first blockers refuse on a created, non-`V0`
version read before its creator (the hostile round's classification,
verified over all `144` non-arbitration blockers). The arb-free
direction is **forced**, not coincidental: an arbitration-free record
holds only `V0` versions, so no consumer can precede a creator there
(the round verified additionally: at most one live proposal per
(actor, base), `0` violations, all four arms). The biconditional is
robust to the merge gap D.4 leaves open: a merge needs two created
versions and the first creator can only be an `r`, so every
merge-carrying record carries an arbitration.

**C.3b — every holonomy-carrying loop's record carries an
arbitration.** D72's committed kind census attributes only `68` of the
`88` defects at `(A,B) d≤4` to an exchanged pair containing an
arbitration; the other `20` are `(d,n)`, `(d,d)`, `(n,d)`. Measured on
the **whole endpoint record** rather than the exchanged pair, all of
them carry one — the arbitration sits in the common prefix. Over all
four arms: `662/662`. [MEASURED] on this window.

**C.4, a corollary of C.3a and C.3b and labelled as such.**
Reversal-definedness and holonomy-carrying are **disjoint**: every loop
whose reversed counterpart the law prices has `v = 1`; not one loop
with `v ≠ 1` has a priced counterpart. The implication is
one-directional — flatness does not buy reverse-definedness, since
`5,106` flat loops are refused too. **The sharp form is a strict
containment: the functor's domain is the arbitration-free locus
(C.3a), curvature implies arbitration (C.3b), so the curved locus
lies strictly inside the refusal set — which also holds every flat
arbitration-carrying loop.**

**C.5 — the refusal is non-generic, and is a property of the
arbitration-carrying locus (which contains the whole curved locus),
not of the support.** `5,552/7,368` records of the anchor arms
with `|h| ≥ 2` have a priced image under `R`. A clear majority of this
grammar's records reverse into records the committed law prices. D74's
`D5.0` found the same thing without relabelling; it survives the
relabelling.

**C.6 — the opposite record has no priced realisation in any order.**
For every holonomy-carrying endpoint of both anchor arms, every linear
extension of its committed event poset is built and offered to the
admission relation: `328/328` priced forwards — so the poset genuinely
is the record's own causal order and nothing is smuggled — and `0/328`
priced under `R`. The AB4 sub-row is D74's committed `D5.2` number
reproduced exactly (`176` endpoints, `304` extensions, `304/304`
forwards, `0/304` reversed), with the relabelled reading `0` as well.
**The refusal is of the opposite poset, not of an enumeration order.**

**C.7 — and it is not an artefact of starting from the empty record.**
Each of the `200` reversed holonomy-carrying endpoints is offered every
admissible prefix of length `≤ 2` of its own arm as context (up to
`235` prefixes per arm): `0` become priceable. Supplying the missing
past does not help, and the reason is structural — what the reversed
record needs in its past are the very events it itself contains later,
and the committed admission relation refuses a second live copy of a
triple already live.

---

## 4. LD-D — the defected schedule families

D73's lesson is that hand-built symmetry is a cage. The two committed
defected families are D74's own: **ASYM-1**, the one-way link `A→B`,
and **ASYM-2**, the defected directed ring `A→B→C→A`. Both are declared
sub-grammars — the support is restricted, the committed weights are
untouched.

**The honest opposite of a directed schedule is the mirrored
schedule.** `rho` transposes deliveries, so the `rho`-image of "A may
send to B" is "B may send to A". Asking the reversed record for
admission in the arm's **own** sub-grammar would be a rigged test — it
would fail on the link direction alone and say nothing about the record
category. The census therefore asks the reversed record for admission
in the **mirror** sub-grammar; the arm's own sub-grammar is kept as a
second reading.

| arm | closed | non-unit | spectrum | carrying loops, mirror | carrying loops, own |
|---|---|---|---|---|---|
| ASYM-1 `(A,B) d≤5` one-way `A→B` | 5,504 | 334 | `{1/2: 184, 2/3: 10, 3/2: 68, 2: 72}` | **UNDEFINED 334** | UNDEFINED 334 |
| ASYM-2 `(A,B,C) d≤4` ring | 8,673 | 228 | `{1/2: 190, 2/3: 2, 3/2: 10, 2: 26}` | **UNDEFINED 228** | UNDEFINED 228 |

**D.1** Breaking the schedule's symmetry does not move the answer:
`562/562` holonomy-carrying loops of the defected families again have
no reversed counterpart, in the honest mirror reading, at more than
five times the anchor window's count.

**D.2** The defected arms are **not vacuous** under `R`: `8,935` of
their flat loops **do** acquire priced reversed counterparts, all at
`v_rev = 1`, so the negative at the curved loops is a real refusal,
not an empty support. The mirror **filter** itself cannot bite on an
image of an arm record: `of(e) = f(ρ(e))` and every reversed event is
`ρ(e_fwd)` with `f(e_fwd)` true, so by A.1 involutivity `of` passes
identically — the mirror column is a reading of the **unrestricted**
grammar, and D.2's honest content is that the refusal is the law's,
not the filter's.

**D.3** The exact characterization of §3 survives the defected
schedules with no exception in either direction, and `0` of `662`
curved loops lacks an arbitration.

**D.4 — a declared scope limit.** **No merge event is realised at any
depth run here** (`309` distinct events over four arms; kinds
`{d: 98, n: 3, p: 106, r: 102}`). The census therefore says nothing
whatever about how `R` treats merges. The relabelling table assigns `m`
a self-dual label and C.3's state-action reading places it among the
producing kinds; neither is tested by a single square in this receipt.
Extending the verdict to the merge sector needs a window that reaches
one.

---

## 5. The verdict against the pin's pre-registered outcomes

| outcome | requirement | measured | met? |
|---|---|---|---|
| **LD-CLOSED** | everything inverts | INVERSION on holonomy-carrying loops = `0` of `662` | **no** |
| **LD-OPEN** | a non-inverting quantity, i.e. an OTHER entry | OTHER = `0` | **no** |
| **LD-BROKEN** | the reversal functor cannot be defined involutively **on the committed grammar** | involutive on **records** (A.3, `7,393/7,393`) and exactly poset reversal (A.4, `7,393/7,393`), but **not an endofunctor of the committed record category**: its image leaves the grammar's support on `662` of `662` holonomy-carrying loops, on the Born = K1 row, and on all `40` half-open squares | **yes** |

### **VERDICT: LD-BROKEN.**

The pin's own words apply: *"record irreversibility is structural;
characterize the obstruction exactly — that is a first-class finding,
not a failure."* §3 is that characterization, and it is exact in both
directions at the level of a single event kind.

The precise statement, in the terms the outcome was written in: the
opposite category `C^op` **exists** — every category does — and the
reversal realising it on records is a clean involution. What cannot be
defined is a functor `C → C` (or `C^op → C`) landing in the committed
grammar. `R` is an involution of the **free** record category of event
sequences; the committed record category is a sub-object of it that `R`
does not preserve, and the part `R` leaves is exactly the curvature.

---

## 6. What this does and does not carry

**It does not advance the phase record.** The 0-for-5 tally is **not**
0-for-6. Nothing was measured to invert or to fail to invert, because
there is no second value to compare against; the branch was pre-
registered with three outcomes and delivered the third. **The last door
is neither shut nor opened — it is walled**, and the pin's LD-CLOSED
consequence ("the phase hunt then lives ONLY in U1's interpolant census
and U3's gauge stratum") **is not licensed by this unit**. What is
licensed is narrower and different: *the record-category route to a
phase cannot be run at transport scope at all, for a stated structural
reason*, so it is not a live address rather than an exhausted one.

**It does not claim dynamical or thermodynamic irreversibility.** The
reversal is formal throughout. The measured object is the committed
admission relation's refusal to price a formally-constructed record;
that is a statement about a grammar, not about a process.

**It does not claim the negative survives greater depth or wider
pools.** [MEASURED] on four arms at the declared caps: `(A,B) d≤4`,
`(A,B,C) d≤3`, ASYM-1 `(A,B) d≤5`, ASYM-2 `(A,B,C) d≤4`; `17,277`
closed squares; `662` holonomy-carrying loops. The merge sector is
untouched (D.4).

**What it does carry, and is new.** Three results the corpus did not
have:

1. **The record category is not self-dual** — stated, constructed and
   gated, with the involutivity and poset-reversal properties separated
   from the realisation question so that neither borrows the other's
   evidence.
2. **The obstruction is one event kind.** A square's image is priced
   **iff** its record carries no arbitration, `17,277/17,277`, no
   exception either way, across two actor pools and two defected
   schedules. D74 knew that the defect locus was reversal-refusing
   (`0/304`, `D5.2`); it did not know **why**, and named the locus by
   its curvature. This unit names it by its **grammar**: the wall's
   address is the arbitration — it holds every curved loop (`662/662`)
   and extends over `5,106` flat arbitration-carrying loops besides,
   so the curvature is a proper sub-locus of the wall; what the two
   share is the join.
3. **Every holonomy-carrying loop's record carries an arbitration**
   (`662/662`) — a strictly stronger reading of D72's kind census,
   which sees only the exchanged pair and attributes `68` of `88`.

**Where these go.** Result 2 and result 3 are addressed to the
transport-curvature line, not to the phase hunt: they say the
generated curvature is arbitration-borne at the level of the whole
record, which is a claim about **U-scope joins**, and they are offered
there. Result 1 retires branch (a) of catalog §4.7 — the last door is
spent, and spent as BROKEN rather than as either of the two outcomes
that would have moved the phase tally.

---

## 7. Receipt map

| gate | what it settles |
|---|---|
| `SRC.1` | exit-freedom of the sliced committed layer (corollary — plumbing) |
| `AB4.1/.2`, `ABC3.1/.2`, `W.1`, `G.1`, `K1.1`, `D5.2` | the eight anchor gates; `exit 1` reserved for these and not reached |
| `A.1`–`A.4` | the functor: `rho` involutive, `regs_of`-preserving; `R` involutive on records; `R` exactly poset reversal |
| `A.5` | the pushforward reading declared informationless before the census (corollary) |
| `B.0` | the `mu`-ratio form of `v` is the census form, `3,100/3,100` |
| `B.1`, `B.2` | the census on the committed holonomy-carrying loops; where the functor lands, it lands on 1 |
| `B.3`, `B.4` | the 40 half-open squares; the Born = K1 row |
| `B.5` | CTL-ORDER: the verdict is convention-free |
| `B.6`, `B.6b` | the relabelling controls, involutive and non-involutive |
| `C.1`, `C.2` | the blocking kinds; consumption-before-production, exact |
| `C.3a`, `C.3b` | the one-kind address, exact both ways; every curved loop carries an arbitration |
| `C.4` | disjointness (corollary of C.3a + C.3b) |
| `C.5`, `C.6`, `C.7` | non-genericity; all linear extensions; non-empty prefixes |
| `D.1`–`D.4` | the defected families in their mirror sub-grammars; the merge scope limit |
| `DET.1` | determinism, and `PYTHONHASHSEED=7` reproduces the output |
