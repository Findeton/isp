# U2 — result: **W-CROSS.** The three ADDRESSES are three; two of the NAMES are one. Refusal and arbitration are two names for one axis — the address of irreversibility is exactly the arbitration (`17,277/17,277`, both directions, mirror reading) — and curvature sits **strictly inside** that axis (`662` curved squares inside `5,768` refused, boundary `5,106`). Non-lumpability lies on neither side of the chain: it **crosses** both. `326` of the `662` curved squares sit on **lumpable** configuration classes and `7,298` non-lumpable squares are **flat**; `4,840` non-lumpable squares are reversal-**priced** and `2,974` refused squares are **lumpable**. No single grammar quantity predicts all three statuses: of `16` candidates each predicts at most one address and none predicts two (the per-address positives are tautologies or committed theorems — `J` ⇒ curvature by definition, the arb-count family ⇒ refusal by LD's C.3a, `σ_base` ⇒ lumpability by construction — so the substantive content of the search is the NEGATIVE, which the hostile round stress-tested with eight further natural candidates: zero welds, zero two-address predictors); no pair predicts the triple, and every succeeding triple is one predictor per address bolted together. The ported configuration map is **order-blind at every closed square** (`17,277/17,277` at the endpoint) — but the round's correction stands (LOG #24): this does NOT mean the DC2 signal cannot live at a square. A square-local DC2 predicate (equal shoulder classes, unequal shoulder kernels) is **non-empty: 464 of 17,277 pooled, 48 on AB4, 88 of them curved** (round-computed, counterexample exhibited in §2.4) — and W-CROSS survives that reading too, as it survives all six readings of the third axis the round ran.

**Read the scope before the headline.** Every number here is a
window-dependent measurement on the committed d42b1 transport grammar at
the declared caps, at **cut grain**. The reversal is **FORMAL**
(category-level), exactly as LD declares it: nothing claims that
reversed records are generated or that any dynamical or thermodynamic
asymmetry is exhibited, and no such language appears in the receipt.
Weight-system level only: no measure claim. Record-grain claims belong
to U2b and are not made (§4). No quantity measured here is offered as
"the quantum layer"; U3's entry fee stands untouched. No CP, Bell,
locality or covariance framing appears anywhere.

**Status: TERMINAL (conferred by the hostile round of 2026-07-28,
ACCEPT-WITH-FIXES — the census reproduced by a fully independent
rebuild; W-CROSS survives all six readings of the third axis
including three square-local ones the round computed; fixes applied,
adjudicated v11 LOG #24), STRICT.** Pin
`v11/note-u2-three-address-weld-pin.md` (binding, frozen before any code
was written). Binding specification: paper 0 §9b, the catalog
`[V11-CAT]` §4.7. Parents: v10 **D74 TERMINAL**
(`note-d74-transport-holonomy-result.md` — the transport curvature, the
`88 + 40 + 12` census, the `44 + 44` descent split, the value set
`{1/2, 2/3, 3/2, 2}` of the group `⟨2,3⟩`), **D72 TERMINAL** (the
transport-scope census both anchor windows rest on), v11 **LD TERMINAL**
(`note-ld-last-door.md` — the reversal functor and the one-kind
address), v11 **U1 TERMINAL** (`note-u1-indivisibility-census.md` — the
DC2 ladder and the `44` descent-obstruction squares), v11 **U3
TERMINAL**. Receipt `v11/code/u2_three_address_weld_exact.py`, output
`v11/code/u2_output.txt` — run from the repository root, **exit 0**,
**38 PASS / 0 FAIL / 0 ANCHOR-FAIL** of which **3 carry no independent
information and are labelled as such** (**35 independent-evidence
passes**), **88 s** wall clock against the pin's ~40 min budget. Re-run
at `PYTHONHASHSEED=7`: **output identical apart from timings and the
echoed seed** (0 substantive differences).

**Lean: NONE**, as the pin declares. All three outcomes are reportable
and the census is the result.

**All three instruments are the committed ones, lifted and not
retyped.** D74's census machinery (`sk`, `evsk`, `enumerate_line`,
`square_census`, `A_menu`, `congruence`), LD's reversal functor (`rho`,
`rev`, `rev_with`, `mu_of_sequence`, `first_blocker`, `classify`,
`reversal_census`, `oneway`, `ring`) and U1's DC2 instrument
(`sigmaT`, `payload_of`, `horizon_measure`) are extracted by
`ast.FunctionDef` from their own committed receipts; the d42b1 transport
layer is text-sliced behind a 9-signature AST pass with exit-freedom
gated. A change to any of those definitions changes this receipt's
object.

**Every committed number this unit rests on reproduces exactly.**
Sixteen anchor gates held against eight quoted committed rows (the round: the registry carries eight keys) — D72's
`T6.1` and `T6.2`, D74's value-set row, its `44 + 44` split and its two
declared sub-grammars, LD's `C.3a` and `C.3b`, U1's DC2 ladder and its
`32`-of-`44` geography — with `exit 1` reserved for them and not
reached: D72's `T6.1` at both anchor windows (`3,969`
histories, `1,546` closed, `88` non-unit, spectrum `{1/2: 70, 2/3: 2,
3/2: 6, 2: 10}`, kinds `{(r,d): 68, (d,r): 8, (d,n): 6, (d,d): 4,
(n,d): 2}`, `142` both-blocked, shallowest depth `3`; `3,424` histories,
`1,554` closed, `12` non-unit, kinds `{(r,d): 12}`, `42` both-blocked);
`T6.2`'s half-open row (`40`, AB-only `28`, BA-only `12`, kinds
`{(r,d): 12, (d,r): 4, (p,d): 16, (d,p): 8}`); D74's two declared
sub-grammars (`5,504` closed / `334` non-unit / `{1/2: 184, 2/3: 10,
3/2: 68, 2: 72}`; `8,673` closed / `228` non-unit / `{1/2: 190, 2/3: 2,
3/2: 10, 2: 26}`); D74's value set `{1/2, 2/3, 3/2, 2}`; D74's `44 + 44`
split on AB4 (menu quotient closes `44`, congruence closes the same
`44`, invisible half `{1/2: 44}` with kinds `{(r,d): 44}` and base
depths `{1: 4, 2: 40}`; ABC3 closes `0` of `12`); LD's pooled
population and one-kind address (`17,277` closed squares, `662`
holonomy-carrying, cross-tab `{(arb, priced): 0, (arb, refused): 5,768,
(no arb, priced): 11,509, (no arb, refused): 0}`, anchor pair
`{0, 526, 2,574, 0}`, `662/662` curved loops carrying an arbitration and
`0` priced); and U1's DC2 ladder (`{0: (1,0), 1: (5,0), 2: (11,4),
3: (19,8), 4: (32,12)}`) with its `32` of `44` geography.

Where this note and the receipt disagree, **the receipt is
authoritative**.

---

## 0. The one-paragraph answer

Three instruments are pointed at every closed exchange square of the
four committed populations — `17,277` squares — and each returns an
exact status: `J ≠ 1` (curvature), *the committed law refuses the
reversed record* (irreversibility), *the square's base sits in a
non-lumpable configuration class* (DC2). Two of the three are nested and
one is not. The refusal set **is** the arbitration-carrying set, square
for square with no exception in either direction, and the curved set
lies strictly inside it with a boundary of `5,106` flat
arbitration-carrying squares — one address, seen twice. The
non-lumpable set is a third region that overlaps both and is contained
in neither: it holds `336` of the `662` curved squares and misses `326`
of them, and `4,840` of its members are squares the reversal functor
prices. The instrument that defines it — the ported configuration map
`σ` — is measured blind at the ENDPOINT: on every one of the `17,277`
squares the two traversal orders leave `σ(h.eA.eB)` **identical**.
(The round's correction, LOG #24: that endpoint fact does NOT confine
the DC2 signal to the base class — a square-local DC2 predicate on the
SHOULDERS is non-empty, 464/17,277, §2.4 — but the primary axis as
measured here IS the base-class reading, and the two loci are measured
on different objects; the crossing survives every reading either way.)
That difference of objects is the mechanism behind the crossing, and
it is also why no single quantity welds the three: the arbitration
count predicts irreversibility and nothing else, `J` predicts
curvature and nothing else, `σ(h)` predicts lumpability and nothing
else, and no pair of the `16` candidates predicts the triple.

---

## 1. W-A — the three-way census

### 1.1 The three statuses, and what fixes each

| status | positive when | instrument | source |
|---|---|---|---|
| **CURVED** | `v = q(eA\|h) q(eB\|h.eA) / q(eB\|h) q(eA\|h.eB) ≠ 1` | D74's `square_census`, lifted | D72/D74 |
| **REFUSED** | the committed law prices neither reversed endpoint (the code's form is "prices both → priced, else refused"; the two definitions coincide here — the round measured 0/17,277 squares with exactly one image priced) | LD's `reversal_census`, lifted | LD |
| **NON-LUMPABLE** | `σ(h)`'s class at depth `\|h\|` carries two different next-configuration laws under `k_r` | U1's DC2 test, lifted and parameterised in its window | U1 |

The reversal status is read in LD's committed convention: the anchor
populations reverse into the unrestricted grammar, the two declared
sub-grammars into their **mirror** sub-grammar. That mirror filter is
measured to be vacuous on images — `28,354/28,354` reversed endpoint
records pass it at every event — so the mirror column is a reading of
the unrestricted grammar and its refusals are the law's (gate `REV.1a`,
LD's `D.2` argument re-measured). The arm's own unmirrored filter is
carried as the second reading: it refuses `1,633` further ASYM-1
squares and `5,268` further ASYM-2 squares, **every one of them
arbitration-free** and none of them changing an arbitration-carrying
square's status, which is exactly the rigging LD names (gate `REV.1b`).

### 1.2 The port of the DC2 instrument, verified before it is used

The lumpability status of a square is the status of its **base's**
configuration class, read in the instrument run at the square's own
window. Two gates verify that this is U1's instrument and not a new
one, at U1's own window ((A,B) exhaustive to depth `5`, horizon `5`):
the ladder `{0: (1,0), 1: (5,0), 2: (11,4), 3: (19,8), 4: (32,12)}`
reproduces exactly, and read square by square, `32` of U1's `44` named
descent-obstruction squares sit on non-lumpable classes — U1's gate
`J2` to the unit. The split by base depth is `{1: (0 of 4),
2: (32 of 40)}`.

**The defect never appears below depth 2**, in any window run here
(gate `PORT.3`): U1's ARM-A and all four populations' own windows agree.
Since a closed square needs `|h| + 2 ≤ cap`, this fixes what the
lumpability axis can see on each population, and on ABC3 it can see
nothing: at cap `3` every ABC3 square has base depth `≤ 1`, so all
`1,554` are lumpable **by the depth of the anchor window** and not by
anything about three-actor pools — ASYM-2, a three-actor population
reaching depth `4`, carries `15` non-lumpable classes at depths `2` and
`3` (gate `PORT.4`). A deep-window control confirms it: ARM-D ((A,B,C)
exhaustive to depth `4`, unfiltered) carries the defect at depths `2`
and `3` and none at depths `0` and `1` (gate `PORT.5`).

### 1.3 The 2×2×2 table

**Pooled, all four populations** — cells `(curved, refused,
non-lumpable)`:

| curved | refused | non-lumpable | squares |
|---|---|---|---|
| T | T | **T** | **336** |
| T | T | F | **326** |
| T | F | T | **0** |
| T | F | F | **0** |
| F | T | T | 2,458 |
| F | T | F | 2,648 |
| F | F | T | 4,840 |
| F | F | F | 6,669 |

Marginals: curved `662`, refused `5,768` (= arbitration-carrying),
non-lumpable `7,634`, total `17,277`. All three: `336`. None of the
three: `6,669`. The two zero cells are the whole content of the
curvature-inside-irreversibility containment; every other cell is
occupied.

**By population:**

| population | closed | curved | refused | non-lump | TTT | TTF | FTT | FTF | FFT | FFF |
|---|---|---|---|---|---|---|---|---|---|---|
| AB4 `(A,B) d≤4` | 1,546 | 88 | 448 | 640 | 32 | 56 | 160 | 200 | 448 | 650 |
| ABC3 `(A,B,C) d≤3` | 1,554 | 12 | 78 | **0** | 0 | 12 | 0 | 66 | 0 | 1,476 |
| ASYM1 `(A,B) d≤5` | 5,504 | 334 | 3,280 | 3,178 | 232 | 102 | 1,650 | 1,296 | 1,296 | 928 |
| ASYM2 `(A,B,C) d≤4` | 8,673 | 228 | 1,962 | 3,816 | 72 | 156 | 648 | 1,086 | 3,096 | 3,615 |

The `TFT` and `TFF` columns are `0` in every population and are omitted.

**Both ways.** Every table is printed twice — as
`(J, reversal, lumpability)` and re-indexed as
`(lumpability, reversal, J)` by an independent pass — and the two
readings agree cell for cell in all five tables. The loop axis is
printed both by **holonomy value** and by **kind pair**:

| `v` | squares | refused, non-lump | refused, lumpable |
|---|---|---|---|
| `1` | 16,615 | 2,458 | 2,648 (and 11,509 priced) |
| `1/2` | 456 | 238 | 218 |
| `2/3` | 14 | 2 | 12 |
| `3/2` | 84 | 32 | 52 |
| `2` | 108 | 64 | 44 |

Every non-unit value is wholly refused and **split** on lumpability;
no holonomy value is wholly non-lumpable or wholly lumpable.

### 1.4 Three control readings

| reading | definition | non-lumpable squares |
|---|---|---|
| **LUMP-A** (primary) | the base class, at the population's own window | 7,634 |
| **LUMP-C** | the base class, at the deepest committed window for the population's grammar | 7,634 |
| **LUMP-W** | the square's whole window: the base class, or either shoulder class at depth `d+1` | 10,624 |
| **LUMP-B** | DC1's `σ`-commuting predicate at the square: `σ(h.eA.eB) ≠ σ(h.eB.eA)` | **0** |

LUMP-C and LUMP-A agree on **every** square (`0` of `17,277` differ)
— with the round's scope caveat (LOG #24): for ASYM1/ASYM2 LUMP-C is
literally the same instrument, and for AB4/ABC3 the two horizons can
differ only at depths the squares' bases never reach, so the
agreement certifies horizon-independence AT THE DEPTHS THE SQUARES
REACH, no further. LUMP-B is **identically empty**, which is a
measurement in its own right (§2.4) — of DC1's predicate, not DC2's.

---

## 2. W-B — the containment structure, and every boundary

### 2.1 The pairwise verdict matrix

| pair | \|X\| | \|Y\| | \|X ∩ Y\| | X\Y | Y\X | verdict |
|---|---|---|---|---|---|---|
| CURVED vs REFUSED | 662 | 5,768 | 662 | **0** | 5,106 | **CURVED ⊊ REFUSED** |
| CURVED vs NON-LUMPABLE | 662 | 7,634 | 336 | 326 | 7,298 | **CROSSING** |
| NON-LUMPABLE vs REFUSED | 7,634 | 5,768 | 2,794 | 4,840 | 2,974 | **CROSSING** |

Under the deep-window control LUMP-C the same three rows are identical
number for number.

### 2.2 The implications, tested exactly

| implication | verdict | boundary |
|---|---|---|
| CURVED ⇒ ARBITRATION-CARRYING | **HOLDS**, strict | `0` counterexamples; converse fails on `5,106` |
| REFUSED ⟺ ARBITRATION-CARRYING | **HOLDS BOTH WAYS** | `0` and `0` on all `17,277` |
| CURVED ⇒ REFUSED | **HOLDS**, strict | `0`; converse fails on `5,106` |
| CURVED ⇒ NON-LUMPABLE | **FAILS** | `326` counterexamples |
| NON-LUMPABLE ⇒ CURVED | **FAILS** | `7,298` counterexamples |
| NON-LUMPABLE ⇒ REFUSED | **FAILS** | `4,840` counterexamples |
| REFUSED ⇒ NON-LUMPABLE | **FAILS** | `2,974` counterexamples |

LD's biconditional is not merely reproduced as a pair of counts here:
the refusal set and the arbitration-carrying set are **the same subset**
of the pooled population, square for square, so the middle axis of the
2×2×2 may be read as either without ambiguity.

### 2.3 The boundary censuses

**The wall's excess over the curvature — `5,106` flat
arbitration-carrying squares.** By population `{AB4: 360, ABC3: 66,
ASYM1: 2,946, ASYM2: 1,734}`; by base depth `{1: 158, 2: 2,278,
3: 2,670}`; by holonomy value `{1: 5,106}` (flat by construction). Every
kind pair is represented, the four largest being `(r,n) 1,158`,
`(p,n) 528`, `(p,r) 468` and `(r,p) 468`.

**The curved squares the DC2 instrument does not see — `326`.** By
population `{AB4: 56, ABC3: 12, ASYM1: 102, ASYM2: 156}`; by kind pair
`{(r,d): 220, (d,n): 52, (d,r): 40, (d,d): 10, (n,d): 4}`; by base
depth `{1: 24, 2: 226, 3: 76}`; by holonomy value `{1/2: 218, 3/2: 52,
2: 44, 2/3: 12}`. **This is the deliverable boundary**: it contains
every holonomy value the group produces, both anchor windows and both
declared sub-grammars, and its dominant kind pair `(r,d)` is D72's own
dominant defect kind. A weld theorem would have to explain why these
`326` squares carry curvature on a configuration class whose
next-configuration law is constant.

**The non-lumpable squares that are flat — `7,298`.** By population
`{AB4: 608, ASYM1: 2,946, ASYM2: 3,744}` (ABC3 contributes none, §1.2);
by base depth `{2: 4,676, 3: 2,622}`; all at `v = 1`.

**The non-lumpable squares the reversal functor prices — `4,840`.** By
population `{AB4: 448, ASYM1: 1,296, ASYM2: 3,096}`; by base depth
`{2: 3,760, 3: 1,080}`; all flat; and by kind pair, **no pair
containing an `r`** — `{(d,d): 248, (d,n): 864, (d,p): 452, (n,d): 248,
(n,n): 416, (n,p): 452, (p,d): 804, (p,n): 1,068, (p,p): 288}`, which
is forced, since a priced square is arbitration-free in its whole
record.

**The refused squares on lumpable classes — `2,974`.** By population
`{AB4: 256, ABC3: 78, ASYM1: 1,398, ASYM2: 1,242}`; by base depth
`{1: 182, 2: 1,588, 3: 1,204}`; by holonomy value `{1: 2,648,
1/2: 218, 3/2: 52, 2: 44, 2/3: 12}` — it contains the whole `326`.

### 2.4 Why the crossing has a mechanism

**The ported configuration map is order-blind at every closed exchange
square**, exhaustively: `σ(h.eA.eB) = σ(h.eB.eA)` on all `17,277`
squares of all four populations, curved and flat alike, including all
`662` curved ones (gate `B.5a`). U1's DC1 census excludes the
`σ`-non-commuting pairs "by name"; at closed squares there are none to
exclude. So whatever the exchange does to the **weights** — and on
`662` squares it multiplies them by a non-unit element of `⟨2,3⟩` — it
does nothing whatever to the **endpoint configuration**.  ROUND
CORRECTION (LOG #24): the delivery inferred from this that the DC2
defect "cannot be located at a square"; that inference is a NON
SEQUITUR — endpoint order-blindness says nothing about the two
SHOULDERS.  The round computed the square-local DC2 predicate this
receipt skipped — `σ(h.eA) = σ(h.eB)` AND `k_r(·|h.eA) ≠ k_r(·|h.eB)`
— and it is **non-empty: 464 of 17,277 pooled, 48 of 1,546 on AB4, 88
of them curved.**  Exhibited witness (round-computed, AB4):
`h = (('p','A',('v','v0'),0),)`, `eA = ('d','A','B',('v','v0'))`,
`eB = ('n','A')`, `J = 1`; equal shoulder classes; shoulder kernels
`{1/8, 3/4, 1/16}` vs `{8/65, 48/65, 1/13}`; base class LUMPABLE.
The honest statement: the endpoint order-blindness is measured and
exhaustive; the DC2 signal CAN live at a square (464 witnesses); the
primary axis (LUMP-A) is a base-class reading, and the two loci are
measured on different objects — the crossing is that difference made
numerical.  Also against "carried entirely by the base class": the
round's shoulder-only reading (LUMP-SH, 7,640) and LUMP-A (7,634)
are nearly equal in size but largely DIFFERENT sets
(|A ∩ SH| = 4,650).

The verdict survives all SIX readings the round ran — the receipt's
three (LUMP-A primary; LUMP-C deep-window; LUMP-W whole-window) and
the round's three new ones (LUMP-SH shoulders-only; LUMP-WIT
base-as-disagreeing-member; **LUMP-S1, the fully square-local DC2
predicate above**, under which the table reads 574 curved-and-lumpable
/ 376 crossing / 5,562 / 258) — plus LUMP-END (endpoint class, deeper
window, 4,696 covered): W-CROSS under every one.  LUMP-B (DC1's
σ-commuting predicate) is identically empty — a measurement, not a
reading of DC2.  No reading makes curvature imply non-lumpability.

---

## 3. W-C — the weld candidates

A candidate quantity `q` is a **weld** iff the status triple is a
function of `q` on the pooled population. Sixteen candidates are read
on the endpoint record `σ_AB = h.eA.eB` (order-blindness measured
first: thirteen are order-blind on all `17,277` squares; `ncomp`,
`maxcomp` and `joinsig` differ between the two orders on `384` squares
and the `σ_AB` reading is used and declared). Each is scored against
each status separately as well as against the triple.

| candidate | values | predicts J | predicts REV | predicts LUMP | weld | failing squares |
|---|---|---|---|---|---|---|
| `narb` (arbitration count) | 3 | no | **yes** | no | no | 17,277 |
| `narb ≥ 1` | 2 | no | **yes** | no | no | 17,277 |
| `ncreated` (created versions) | 3 | no | **yes** | no | no | 17,277 |
| `ncreated ≥ 1` | 2 | no | **yes** | no | no | 17,277 |
| `nlive` | 4 | no | no | no | no | 17,277 |
| `ncomp` (join components) | 4 | no | no | no | no | 17,277 |
| `maxcomp` (join width) | 4 | no | no | no | no | 17,277 |
| `joinsig` (join structure) | 7 | no | no | no | no | 17,277 |
| `J` | 5 | **yes** | no | no | no | 17,277 |
| `kindpair` | 16 | no | no | no | no | 17,277 |
| `kindmultiset` | 67 | no | **yes** | no | no | 13,356 |
| `basedepth` | 4 | no | no | no | no | 17,067 |
| `narb_base` | 2 | no | no | no | no | 17,277 |
| `joinsig_base` | 4 | no | no | no | no | 17,277 |
| `σ_base` (the DC2 state) | 91 | no | no | **yes** | no | 12,755 |
| `σ_end` | 210 | no | no | no | no | 14,584 |

**No candidate predicts two addresses.** Each of the three addresses
has an exact single-quantity predictor and it predicts nothing else:

- **`narb ≥ 1` predicts the reversal status on every square** — LD's
  biconditional restated as a prediction — and its two value-classes
  each carry both curvature statuses and both lumpability statuses.
  Its failure split is `{narb = 0}: 6,669` triples `000` and `4,840`
  triples `001`; `{narb ≥ 1}: 2,648 / 2,458 / 326 / 336` over
  `010 / 011 / 110 / 111`.
- **`J` predicts curvature by definition** and predicts the reversal
  status only one-sidedly: every non-unit value class is wholly
  refused, but the unit class carries both statuses (`11,509` priced,
  `5,106` refused). Every non-unit class is split on lumpability.
- **`σ_base` predicts lumpability by construction** — it *is* the DC2
  instrument's state — and predicts neither of the other two. This is
  the sharp form of the crossing: the instrument that defines one
  address is blind to the other two.

**LD's mechanism sentence is an identity on this population**, measured:
`ncreated = narb` on every one of the `17,277` squares (gate `C.0`).
The arbitration is the only realised kind that creates a version — the
merge, the other creating kind, is not reached — and no two
arbitrations of one record create the same version. `narb` and
`ncreated` are the same quantity here.

**The minimal-set scan, reported as a secondary reading — and nothing
is welded by combining.** The subsets are scanned exhaustively to size
`3`: `0` of `16` singles predict the triple, `0` of `120` pairs do, and
`5` of `560` triples do. Every one of the five is *one exact predictor
per address bolted together* —

```
(narb, J, sigma_base)          281 value-classes
(narb>=1, J, sigma_base)       249
(ncreated, J, sigma_base)      281
(ncreated>=1, J, sigma_base)   249
(J, kindmultiset, sigma_base)  921
```

— and the number of triples that predict **jointly**, in the sense of
succeeding without carrying an exact single-status predictor for each of
the three addresses, is **`0`**. The exact single-status predictors are
`J` for curvature; `narb`, `narb ≥ 1`, `ncreated`, `ncreated ≥ 1` and
`kindmultiset` for the reversal status; `σ_base` for lumpability. It
takes exactly three of these quantities, one per address, and combining
them produces no structure that any one of them lacked.

---

## 4. W-D — the record-grain hook, named and not run

Everything above is measured at **cut grain**: a square's three
statuses are read from its base, its exchanged pair and its endpoint
record at the declared caps. The record-grain question — whether the
same address separates the refusing from the dividing masks of the
two-sided test at depth `15` — is **U2b's**, is conditional on U1c
returning `N4`, and is not run here. No number in this receipt bears on
it. The populations here do not reach depth `15`, do not carry the
lag `≥ 2` masks the U1b round names as the only door left in that
class, and **contain no realised merge event at all** (gate `D.1`:
kinds realised `{d, n, p, r}`), so LD's `D.4` scope limit is inherited
unchanged along with LD's functor. If U1c returns `N4`, U2b asks of
the refusing masks exactly the three questions W-A asks of a square; if
it does not, U2b does not open.

---

## 5. The verdict against the pin's pre-registered outcomes

| outcome | requirement | measured | met? |
|---|---|---|---|
| **W-ONE-ADDRESS** | all three statuses coincide, or an exact chain with empty boundaries | the three sets have sizes `662`, `5,768`, `7,634`; the CURVED ⊊ REFUSED boundary alone is `5,106` | **no** |
| **W-CHAIN** | exact strict containments, every pair nested | `1` of `3` pairs is nested | **no** |
| **W-CROSS** | at least one pair crosses | `2` of `3` pairs cross: CURVED vs NON-LUMPABLE and REFUSED vs NON-LUMPABLE | **yes** |

### **VERDICT: W-CROSS.**

The pin's own words apply: *"the three-address picture is FALSE and the
co-location observed so far was a small-window coincidence; the crossing
squares are the finding."* The first clause holds in the precise sense
the census measures — the three loci are not one address and are not a
chain — but the second is sharper than "coincidence" makes it sound
and is stated as measured: refusal and arbitration are two NAMES for
one axis, exactly and with a mechanism (the arbitration, the only
realised kind that creates a version — ENTAILED by the encoding given
zero merges, LOG #24), and the third axis is NOT NESTED with the
other two, its endpoint reading measured order-blind at every closed
square (which locates the primary reading at the base class; the
shoulder-local DC2 signal exists, §2.4, and crosses too). The
crossing squares are named in §2.3.

---

## 6. What this does and does not carry

**It does not extend LD's or U1's verdicts.** LD's biconditional and
U1's DC2 ladder are re-gated here as anchors, at the same windows, and
neither is strengthened. What is new is that both are re-measured
**square by square** rather than as counts, which is what makes the set
comparison possible at all.

**It does not claim non-lumpability is unrelated to the record law.**
It says the two loci are not nested on these populations at these
caps, and prints exactly which squares each instrument sees that the
others miss.

**It does not claim a weld quantity cannot exist.** It says none of the
pin's named candidates is one, prints each candidate's failure pattern,
and shows that the failure is not an artefact of a thin list: no pair
of the sixteen works either, and every triple that works is one exact
predictor per address bolted together.

**It does not claim the crossing survives greater depth or wider
pools.** `[MEASURED]` on four populations at the declared caps:
`(A,B) d≤4`, `(A,B,C) d≤3`, ASYM-1 `(A,B) d≤5` one-way `A→B`, ASYM-2
`(A,B,C) d≤4` ring; `17,277` closed squares; `662` holonomy-carrying.
The merge sector is untouched.

**It does not claim anything about ABC3's lumpability axis beyond the
depth fact.** ABC3's `1,554` squares are all lumpable because every one
of them has base depth `≤ 1` and the defect never appears below depth
`2` — a property of that anchor window, gated as such and confirmed one
level deeper at ARM-D, not a claim about three-actor pools.

**What it does carry, and is new.** Four results the corpus did not
have:

1. **The three-address picture is false, with the boundary named.**
   Curvature is strictly inside irreversibility (`5,106` boundary) and
   non-lumpability crosses both (`326 / 7,298` and `2,974 / 4,840`).
   The `326` curved-and-lumpable squares are censused by population,
   kind pair, depth and holonomy value, and they span every value the
   group `⟨2,3⟩` produces.
2. **The ported configuration map is order-blind at every closed
   exchange square AT THE ENDPOINT**, `17,277/17,277`.  Corrected per
   the round (LOG #24): this does not confine DC2 to the base class —
   the shoulder-local DC2 predicate is non-empty (`464/17,277`, `48`
   on AB4, witness in §2.4) — but the PRIMARY axis as measured is the
   base-class reading, curvature lives at the square, and the two are
   measured on different objects; that difference is the mechanism of
   the crossing, and the crossing survives the shoulder-local reading
   too.
3. **Each address has an exact single-quantity predictor, no quantity
   has two, and combining welds nothing.** `narb ≥ 1 → `
   irreversibility, `J → ` curvature, `σ(h) → ` lumpability, each exact
   on `17,277` squares and each predicting nothing else; `0` of `16`
   singles and `0` of `120` pairs predict the triple, `5` of `560`
   triples do, and every one of the five is one exact predictor per
   address — `0` joint predictions.
4. **LD's mechanism sentence is an identity here — ENTAILED, not
   discovered** (the round's relabel, LOG #24): `View.created` gains
   exactly one entry per arbitration and one per merge, and this
   population realises zero merges (gate `D.1`), so created-version
   count `=` arbitration count is forced by the encoding up to
   `vname` injectivity; the measured residue is the `248` squares at
   `narb = 2`, and the check passes on all `17,277`.

**Where these go.** Results 1 and 2 are addressed to the
transport-curvature line and to U1's DC2 line jointly: they say that
the descent obstruction and the transport curvature are **not** the
same defect seen twice, and they say why. Result 3 retires the weld
branch of catalog §4.7 at cut grain — there is no single grammar
quantity to promote — and hands the question to U2b at record grain,
conditional on U1c.

---

## 7. Receipt map

| gate | what it settles |
|---|---|
| `SRC.1`, `SRC.2` | the AST signature pass over four committed sources; exit-freedom of the sliced layer (corollary — plumbing) |
| `SRC.3`, `SRC.4`, `SRC.5` | the three instruments lifted by AST from D74, LD and U1 and not retyped |
| `AB4.1/.2`, `ABC3.1`, `ASYM1.1`, `ASYM2.1`, `G.1`, `LD.0`, `D74.1` | the committed census anchors: `T6.1`, `T6.2`, the two sub-grammars, the value set, the `17,277 / 662` pooled population, the `44 + 44` split |
| `LD.1`, `LD.2` | LD's one-kind address and `C.3b`, re-gated per population and pooled, both directions |
| `REV.1a`, `REV.1b` | the mirror filter vacuous on images; the own-sub-grammar reading strictly stricter and moving only arbitration-free squares |
| `PORT.1`, `PORT.2` | U1's DC2 ladder and its `32`-of-`44` geography, reproduced by the per-square port before it is used |
| `PORT.3`, `PORT.4`, `PORT.5` | the defect never below depth `2`; ABC3's window too shallow for the axis; the deep-window control |
| `A.1` | the 2×2×2 printed both ways, agreeing cell for cell (corollary — printing) |
| `A.6` | the horizon dependence measured: `0` of `17,277` squares differ (corollary — a scope fact) |
| `B.1` | refusal and arbitration are the same subset, square for square |
| `B.2` | curvature strictly inside irreversibility, boundary exhibited |
| `B.3`, `B.4` | the two crossings, both difference sets censused |
| `B.5`, `B.5a` | the crossing under all three control readings; `σ` order-blind at every closed square |
| `C.0` | created-version count `=` arbitration count, `17,277/17,277` |
| `C.1`, `C.2`, `C.3` | the arbitration predicts irreversibility alone; `J` predicts curvature alone; `σ_base` predicts lumpability alone |
| `C.4`, `C.5` | no single candidate is a weld; the minimal-set scan — no pair predicts, every predicting triple is one exact predictor per address, `0` joint |
| `D.1` | the merge sector is not reached; LD's `D.4` limit inherited |
| `DET.1` | determinism: the 2×2×2, the three deciding boundaries and the whole candidate table are identical under reverse traversal; `PYTHONHASHSEED=7` reproduces the output |
