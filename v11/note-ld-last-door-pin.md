# v11 LD — THE LAST DOOR: the record-category reversal probe (PIN)

**Status:** PIN, STRICT, 2026-07-27.  **Binding specification:**
paper 0 §9b (the last door), the catalog [V11-CAT] §4.7 branch (a).
Parents: v10 D71b (#485 — the odd sector is the phase's last known
address; H¹ = 0 along commit paths; the v7 i-twist form), D74
TERMINAL (the transport holonomy is R⁺, group ⟨2,3⟩, odd sector
empty on the tested carrier — the 0-for-5 record), D73 (carry an
asymmetric substrate).

## The question

Every v10 phase search decomposed structures under reversal ON THE
RECORD AS BUILT.  Branch (a) — never run — reverses the RECORD
CATEGORY itself: construct the opposite-category record (arrows
formally reversed: effect→cause reading of the same committed
transport records) and ask whether the generated quantities
(transport weights, holonomy values, the K1 arbitration row)
transform under record-reversal by INVERSION in R⁺ (classical), by
CONJUGATION-like pairing (the U(1) signature), or by NEITHER
(a genuinely irreversible record category — itself a finding).

## The unit

- **LD-A** construct the reversal functor on the committed
  transport layer (d42b1 by AST): the formal opposite of the
  record's causal order, with the event-kind relabelling that
  reversal forces (a send read backwards is a receive — the
  relabelling table is a deliverable, printed, and its involutivity
  gated: reversing twice must restore the original record exactly).
- **LD-B** the census: for every holonomy-carrying loop in D74's
  committed census (the 88/40/12 squares re-anchored) and for the
  Born = K1 row, compute the reversed-record counterpart EXACTLY;
  classify each value pair (v, v_rev) as v_rev = v⁻¹ (inversion),
  v_rev = v (invariant), or other — the full table both ways.
- **LD-C** the verdict gates: does ANY quantity fail to invert?
  Any "other" entry is the door opening; all-inversion is the door
  closing (the record category is classical-reversible and the
  0-for-5 becomes 0-for-6, with the phase hunt then living ONLY in
  U1's interpolant census and U3's gauge stratum).
- **LD-D** the asymmetric substrate (D73's lesson): the same census
  on one defected schedule family, so symmetry of the hand-built
  substrate cannot mask a reversal asymmetry.

## Gates

Anchors exit-1-only (d42b1 AST; D74's census numbers reproduced
exactly); involutivity of the reversal functor gated; exact
Fractions; both-ways censuses; determinism probe; substantive
negatives exit 0; runtime printed.

## Pre-registered outcomes

- **LD-CLOSED:** everything inverts — the last door shuts; record
  0-for-6; a first-class negative that finishes the v10-era phase
  hunt at native addresses.
- **LD-OPEN:** a non-inverting quantity — the corpus's first
  reversal-odd object; hand it to U1 (does its location coincide
  with interpolant failures?) and U3.
- **LD-BROKEN:** the reversal functor cannot be defined
  involutively on the committed grammar — record irreversibility is
  structural; characterize the obstruction exactly.
- **Lean: NONE** (0-for-5 stands).

## Scope

Transport scope, committed families only; the reversal is FORMAL
(category-level) — no claim that reversed records are physically
generated; no thermodynamic language; scale doctrine.
