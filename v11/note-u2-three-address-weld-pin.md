# v11 U2 — THE THREE-ADDRESS WELD: curvature, irreversibility,
# non-lumpability (PIN)

**Status:** PIN, STRICT, 2026-07-28.  **This is the re-pin** paper 0
§9b promised once U1b/U3 landed (LOG #16: "U2 held for re-pinning").
The original U2 ("does J locate the bridge?") presupposed a bridge;
Phase I/II left instead THREE CO-LOCATED STRUCTURES wanting one
explanation.  Parents: D74 TERMINAL (transport curvature: the 88+40+12
census, J-invariant J=1 ⟺ flat, group ⟨2,3⟩), LD TERMINAL (the
irreversibility wall: reversed-record priced IFF arbitration-free,
17,277/17,277; wall ⊋ curvature), U1 TERMINAL (non-lumpability: DC2's
ladder, the 44 descent-obstruction squares, 32/44 on non-lumpable
classes), U3 TERMINAL (the ⟨2,3⟩ positivity obstruction and the T² of
attachable phases).

## The question

Are curvature (J ≠ 1), irreversibility (arbitration-carrying), and
non-lumpability (DC2-failing) THE SAME ADDRESS seen by three
instruments, or three genuinely different loci in exact containment —
and what is the exact boundary census of each containment?

## The unit

- **W-A THE THREE-WAY CENSUS.**  Over EVERY closed square of the
  committed populations (the D72/D74 windows: AB4, ABC3, and LD's two
  asymmetric arms — re-anchored), compute all three statuses exactly:
  (i) J-status (J = 1 or the exact ratio; D74's machinery); (ii)
  reversal-status (priced/refused under LD's committed functor); (iii)
  lumpability-status (the σ-class refinement at the square's window —
  U1's DC2 instrument ported to per-square scope, the port verified).
  The full 2×2×2 contingency table, exact counts, BOTH WAYS, per
  population and pooled.
- **W-B THE CONTAINMENT STRUCTURE.**  Which implications hold
  exactly (candidates from the record: curved ⇒ arb-carrying ⇒
  refused [LD's biconditional makes (ii)⟺arb-free exact]; curved ⇒?
  non-lumpable; non-lumpable ⇒? arb-carrying)?  For every strict
  containment: the exact boundary census (the squares in the
  difference set, by kind-pair and depth) — the boundary is where a
  weld theorem would have to live.
- **W-C THE WELD CANDIDATE.**  If a single grammar quantity (the
  arbitration count? the join structure? the created-version count —
  LD's mechanism) exactly predicts all three statuses, exhibit it and
  gate the prediction on every square.  If none does, the failure
  pattern is the deliverable.
- **W-D THE RECORD-GRAIN HOOK (named, not run).**  IF U1c returns
  N4, the weld's next test is whether the same address separates the
  refusing from the dividing masks at depth 15 — that is U2b,
  conditional, not this unit.

## Pre-registered outcomes (lean NONE)

- **W-ONE-ADDRESS:** all three statuses coincide (or form an exact
  chain with empty boundaries at the tested caps) — one mechanism,
  three instruments; the weld quantity of W-C is then forced.
- **W-CHAIN:** exact strict containments with non-empty boundaries —
  the boundary censuses are the finding (they name what each
  instrument sees that the others miss).
- **W-CROSS:** at least one pair of statuses crosses (neither
  contains the other) — the three-address picture is FALSE and the
  co-location observed so far was a small-window coincidence; the
  crossing squares are the finding.

## Gates

Anchors exit-1-only (D74's census numbers; LD's 17,277/17,277 and
662/662; U1's 44-square geography and DC2 ladder — all committed);
exact Fractions; the DC2-per-square port VERIFIED against U1's
committed instrument before use; both-ways censuses; determinism;
caps printed; substantive negatives exit 0; runtime < ~40 min with
progress prints (stall doctrine).

## Scope

Cut grain / transport scope, the committed populations only —
record-grain claims are U2b's, conditional on U1c.  No claim that any
weld quantity is "the quantum layer" (the missing-map discipline; U3's
entry fee stands).  No CP/Bell/covariance framing.  Scale doctrine.
