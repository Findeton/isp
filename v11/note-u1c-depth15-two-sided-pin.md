# v11 U1c — DEPTH 15: THE FIRST TWO-SIDED TEST OF THE RECORD LAW (PIN)

**Status:** PIN, STRICT, 2026-07-28.  **Binding context:** U1b TERMINAL
(LOG #20–#21) — the record-grain question is UNASKED below depth 15;
the round's depth-15 prediction stands on record (§9 of the U1b note:
127 two-sided divisions / 108 two-sided refusals with collision
certificates, conditional on kernel uniformity through renewal 5).
Parents: the U1b receipt (ensembles, prune, fixedness, kernel
machinery — committed anchors), d42b1, U1's ARM-C2 telescoping.

## The question

At the first depth where a genuine two-sided interpolant test exists —
cut triples at renewals (3,4,5), depth 15, lag-≤2 maps — does the
generated law divide, refuse, or split by map?  This is the founding
question of v11 asked for the first time with bite on both transfers.

## Engraved design requirements (the U1b round's warnings, mandatory)

- **THE BITE PREDICATE IS (D-1) ∧ (D-2) ∧ FACTOR-WISE:** a row is a
  verdict row only if BOTH transfers are non-column-constant AND every
  factor component of the map individually passes non-column-constancy
  (a map whose bite is supplied by one component while a gap component
  is degenerate is COMPOUND-DEGENERATE, recorded, excluded from
  verdict rows).  No verdict may cite a row failing any clause.
- **THE KERNEL-UNIFORMITY GATE:** the leg-kernel uniformity (uniform
  1/8 conditionally on every parent) must be GATED at renewal 5 —
  per-parent, exhaustive over the enumerated ensemble.  If it FAILS,
  that is outcome MODEL-BREAK (the measured deviation is the finding)
  and the round's classification collapses — report, do not patch.
- **THE PREDICTION IS NOT AN ANCHOR.**  The U1b round's 127/108
  classification is model-computed, not committed-receipt.  The
  deliverable includes a predicted-vs-measured comparison table;
  disagreements are FINDINGS (which side erred and why), never gate
  failures.

## The unit

- **The ensemble:** renewals at 3/6/9/12/15 (minimal intervals; cuts
  at renewals 3, 4, 5 = depths 9/12/15).  Enumerate EXACTLY via the
  committed telescoping + the gated prune (premise (ii) provable from
  candidates_for; premise (i) and the live-count identity asserted at
  EVERY expansion — the U1b round's standard, in-receipt this time).
  Expected scale ~10⁶ leaves; if exact enumeration exceeds the runtime
  budget, the honest fallback is the largest exactly-enumerated
  sub-ensemble plus the model classification with its conditional
  status DECLARED — never a silent sample.
- **The class:** all 512 lag-≤2 masks (× the inert state bit as U1b
  handled it), fixedness gated at all three cuts; the classification
  into {one-label / forced-divisible / doubly-degenerate /
  forced-indivisible / two-sided} printed and compared to the
  prediction.
- **The test, per two-sided row:** Γ(r4←r3), Γ(r5←r3) exact; the
  factorization decided exactly — CK check first, then the LP with
  Farkas certificate or exhibited interpolant; every refusal carries
  its collision certificate (two conditioning labels, identical
  Γ(r4←r3) columns, different Γ(r5←r3) columns) verified atom-by-atom.
- **The nulls, BOTH:** (a) the FAIR null — the record's outer cuts
  with a randomised middle cut (must divide if the forcing analysis is
  right; a refusal there is a first-class surprise); (b) matched-size
  random maps (two printed LCG seeds).
- **The census:** verdicts × map structure BOTH WAYS; for any refusal
  set, the field-dependence structure (which components produce
  refusal) — the selection problem's data.

## Pre-registered outcomes (lean NONE — the prediction is on record
## but the pin does not adopt it)

- **N4-AT-DEPTH-15:** two-sided divisions AND two-sided refusals both
  realised — the selection problem is REAL and the corpus's first
  genuine record-grain refusals exist; hand the refusal geography to
  U2 immediately.
- **N2-AT-DEPTH-15:** every two-sided row divides — the prediction's
  refusal class fails to materialise; the disagreement's cause is the
  finding; the record law is divisible at the first askable grain.
- **N3-AT-DEPTH-15:** every two-sided row refuses.
- **MODEL-BREAK:** kernel uniformity fails at renewal 5 — the
  deviation is the finding and outranks the census.
- **NO-TWO-SIDED-ROW:** the factor-wise predicate empties the verdict
  set at depth 15 too — the question recedes again; characterize the
  next askable frontier exactly.

## Gates

Anchors exit-1-only (U1b committed numbers: the four-ensemble census,
the kernel fact at renewal ≤ 4; d42b1 AST); exact Fractions;
determinism; caps printed; substantive negatives exit 0; runtime
< ~90 min with section progress printed (stall doctrine: no silent
interval > 8 min); certificates printed (digest caps declared).

## Scope

Transport scope, two-actor pool, the declared ensemble only; renewal
grain; minimal intervals at depth 15 (unequal-interval depth-15+
variants are named follow-ups, not this unit); no unistochasticity
claim (U3's screen stands ready for any refusing transfer); no
CP/Bell/covariance framing; L-1 quoted at the configuration space.
