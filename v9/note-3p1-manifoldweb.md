# 3p1-manifoldweb — round 45: the grown channel-manifold web (growth vs the celestial-clock destination)

**Status:** design note, 2026-07-10 (v9 round 45; the LEDGER-#115 investigation's receipt-shaped question). Receipt: `v9/code/dimwall_manifoldweb.py` (pinned here, committed strictly before running). Reviews ON.

## 1. The question and the theory memo

The confetti probe (note-round-cone-mechanisms §2) proved the destination: K celestial clocks over a 4-dim latent space read ROUND by K ≈ 12–16 (F_iso 1.451 → 1.055 → M4's 1.046). The open physics: can GROWTH get there — or do growth correlations outrun the mechanism as they did at round 43? **The builder (pinned):** slots carry fixed preferred directions p_s ~ uniform(S²); a click at slot s draws u = p_s w.p. α else uniform(S²), magnitude e ~ Exp(0.109551); the K fixed clock directions v_k (Fibonacci sphere) each advance by **e · max(0, u · v_k)** (the half-cosine overlap); snapshots, per-clock churn (rate 1/L per clock, random victim), windows — all Lorentz-line conventions. Relation: b-strict ∧ all-K-clocks weak (dim ≤ K+1 by the weak lemma — now a feature: the bound grows with K). **The memo:** the half-cosine kernel on S² is monopole + dipole dominant in spherical harmonics (higher ℓ decay fast), so deposit displacements have a naturally ~4-dominant latent covariance (1 + 3) with small tails — the "many clocks, few factors" structure is geometric, not imposed. Registered risks: (K1) the monopole (every deposit advances most clocks) may act like the 42b common component — watch the collapse diagnostics; (K2) α-specialization may re-concentrate occupancy (the round-40 mechanism in directional form) — both α = 0 and 0.75 are on the grid; (K3) the win-d_MM rides the frozen 4-coordinate-family curve (volume-proxy tags apply); dimension claims ride refusals + witnesses.

## 2. Grid, anchors, gates (fresh seeds 20262800+; 5 per point)

- **Grid:** K ∈ {4, 8, 12, 16, 24} × α ∈ {0, 0.75}; (N, M, L) = (2048, 32, 16); F-window Δ = 1024/NW = 256; dimension window Δ = 512.
- **Gm0 (calibration):** the reference card + M4/orthant-4 F_iso anchors reproduce the 43/43b bands exactly; **the confetti K-curve reproduces the investigation note's numbers exactly (seeds 77000+, K ∈ {4, 8, 16, inf})** — the continuity anchor for this round (no byte-wiring to the one-hot classes exists: the deposit law is new, disclosed).
- **Gm1 [MEASURED]:** per grid point: F_iso, F_2D, native-(K+1)-frame F (INFO), eig spectrum ratios, pair counts, ordering fraction, win d_MM [volume-proxy], in-window 2-realizer refusals.
- **Gm2 (the witness ladder):** S₄ searches (tries = 8000, disclosed reduced; absence = not-found, never evidence of absence) at all K ∈ {4, 12, 16, 24} × both α; S₅ at K ≥ 12 points; S₆ INFO at K = 24 (tries 4000). The REGISTERED signature of the mechanism: max witness order GROWS with K (the corrected target's order-dimension-growth clause).
- **Gm3 (THE VERDICT; a read):** GROWTH-REACHES-ROUND iff some point has F_iso ≤ 1.1 × max(M4 band) AND refusals ≥ 8/10 AND win d_MM ≥ 3.7 AND S₄ found+verified ≥ 3/5 (non-degenerate, non-sparse). GROWTH-SPOILS iff no point reaches F_iso ≤ 1.2 × max(M4) with win d_MM ≥ 3.0. Else MIXED-FRONTIER (the K-trajectory printed against the confetti reference).
- **Gv1 (the F-covariance check [directional]):** order-only coordinates via the Johnston-style SVD embedding (top-4 singular vectors of the centered relation matrix, paper-14-cited); F computed on embedded coordinates; Spearman(F_native, F_embedded) across the 10 grid points ≥ 0.8 ⇒ the anisotropy is ORDER-READABLE (covariant, physical); below ⇒ the paper-XIV bookkeeping caveat FIRES and is recorded (F claims demoted to coordinate-facing pending stem-level instruments).
- Exit 1 only on Gm0.

## References

note-round-cone-mechanisms (LEDGER #115: the impossibility, the mechanism, the probe); v8 paper 14 (Thm 2.1 celestial clocks; §1 the detachment; Johnston SVD embeddings arXiv:2111.09331); rounds 40–44b (instruments verbatim; the card; the confound exhibits); the weak two-clock lemma (dim ≤ K+1); the frozen mm_reference.json.

## Round-45 amendment: the starvation diagnosis and the 45b pin (appended)

**The receipt's read:** GROWTH-SPOILS by CLOCK STARVATION — per-clock churn injects K independent perturbations, destroying the latent low-rank structure that makes K-clock dominance non-starving (the confetti's clocks are consistent projections of one 4-state; the web's were, until the resets). **Receipt 45b (`v9/code/dimwall_manifoldweb_b.py`; pinned here, committed strictly before running):** the identical grid and gates with FULL-VECTOR churn (one victim slot's whole K-vector reset per firing, rate 1/L — the 42b convention), which returns reset slots to the deposit surface's apex and preserves the latent structure. Wiring: two round-45 rows (K = 4, both α) reproduced byte-exactly under the per-clock flag before the full-vector grid runs. Registered expectations: fraction stays K-healthy; F_iso descends toward the confetti trajectory; dimension instruments live; the 42b common-shock collapse risk is watched via the eig columns (expected inert for direction-valued deposits). Verdict semantics unchanged (GROWTH-REACHES-ROUND / GROWTH-SPOILS / MIXED).

## Round-45b results + the 45c pin (the pure-dipole kernel; appended)

**45b (full-vector churn): the starvation is CURED** (fractions 0.08–0.36 at every K; all instruments live; refusals 10/10 everywhere) **and the mechanism's registered order-dimension signature FIRED: S₅ witnesses 5/5 at K = 12/16/24 (α = 0) — the first grown webs with witnessed order dimension ≥ 5, GROWING with K.** But F_iso stays flat (~1.7–1.9 across K; verdict GROWTH-SPOILS again), and the diagnosis is now kernel-theoretic: the confetti's clocks are PURE-DIPOLE (linear functionals t − u·x); the half-cosine kernel (u·v)₊ = |u·v|/2 + (u·v)/2 carries even harmonics ℓ = 0, 2, 4, … through the |·| term — the quadrupole-and-higher contamination is latent structure BEYOND dimension 4, and the occupied cloud reads its imbalance as anisotropy at every K. Also recorded: 45b's Gv1 read fired the caveat (ρ = −0.03 vs 45's +0.903) — the SVD embedding degrades on these denser relations (F_emb scale wild, one NaN row); the covariance conclusion stands on the round-45 grid; the embedding's limitation is the flagged item, upgraded order-only instruments (paper-14 harmonic) named for later.

### Receipt 45c (`v9/code/dimwall_manifoldweb_c.py`; pinned here, committed strictly before running)

**The kernel fix, with its theorem:** deposits advance clock k by e·(1 + u·v_k)/2 — the unique nonnegative monopole+dipole-ONLY kernel. Then acc = (A, D⃗) exactly: χ_k = A + D⃗·v_k with A = Σe/2, D⃗ = Σ(e/2)u — a PERFECT 4-dim latent at every K, and at K = ∞ dominance ⟺ ΔA ≥ |ΔD⃗| — **the Minkowski cone itself in latent coordinates (time = accumulated content, space = the dipole vector); finite K = its polyhedral approximation, exactly the confetti's structure.** Grid, gates, verdict semantics, wiring (two 45b rows byte-reproduced under the half-cosine flag), calibration — all carried verbatim; the registered expectation is the confetti trajectory (F descending toward the M4 band by K ≈ 12–16) with the witness ladder still climbing; the registered kill is the b-vs-A coupling (growth reads time twice) — the eig and d_MM columns adjudicate.

## Round-45c results + the 45d pin (the monopole-free ballistic relation; appended)

**45c (pure-dipole kernel): GROWTH-SPOILS, third mechanism — THE MONOPOLE DRIFT.** The latent cone theorem held, but the occupancy is drift-dominated: A (total content) is a deterministic clock redundant with b, putting the class on the dichotomy's DRIFT branch (fractions 0.44–0.48 at α = 0; window d_MM 2.34–2.46 = the trivialization horizon; eig3/1 0.14–0.31 = the collapse signature; F_iso 2.8–3.2 = the card's collapse regime, not shape). **The α = 0.75 rows are systematically healthier (eig 0.37–0.67; d_MM 2.81 → 3.16 climbing with K; S₄ AND S₅ 5/5 at every K ≥ 12): persistent slot directions are BALLISTIC WORLDLINES in dipole space — the ingredient M⁴ kinematics actually requires (linear, not diffusive, spatial spread).** The receipt-by-receipt requirement list for Minkowski-from-records now reads: (1) many clocks, dipole-pure (45b); (2) full-vector churn (45); (3) ballistic content evolution via direction persistence (45c); (4) a MONOPOLE-FREE relation with birth order as the sole temporal clock — receipt 45d.

### Receipt 45d (`v9/code/dimwall_manifoldweb_d.py`; pinned here, committed strictly before running)

**The relation (the theory-complete form):** clocks ℓ_k(x) = τ·b(x) + (χ_k(x) − χ̄(x)) where χ̄ = the mean over the K clocks (in-web computable; subtracting it deletes the monopole exactly), and the relation is weak dominance in all K clocks (b as tie-break). At K = ∞ this is Δb·τ ≥ |ΔD⃗| — **the Minkowski cone with time = birth order and space = the content dipole**; with ballistic slots (α-persistence), ΔD⃗ = ΔD⃗₀ + Δw⃗·Δb — positions AND velocities, the genuine M⁴ kinematic form. **The scale τ (pinned formula, derived per web):** τ = c · s_D/s_b with s_D = the pooled std of (χ_k − χ̄) and s_b = the std of b over the window sample (the confetti's matched-variance condition), dial c ∈ {0.5, 1, 2}. Builder = 45c's verbatim (dipole kernel, full churn), α ∈ {0.75, 0.9}; K ∈ {8, 16, 24}. Gates, calibration, verdict semantics carried verbatim; wiring = two 45c rows byte-reproduced under the old relation before the new relation runs. Registered expectation: the confetti trajectory at last; registered kills: churn teleportation (resets destroy positions — windows between resets carry the burden) and the bounded-velocity-cone artifact (cross-slot relations selecting |Δw| ≤ τ — the fraction and eig columns adjudicate).

## Round-45 arc review corrections (appended; ARC UPHELD AS MEASUREMENT — HEADLINES CORRECTED)

**M1 — RETRACTION: Gv1's ρ = 0.903 was a NaN-rank artifact; "F IS COVARIANT" is VOID.** On the round-45 grid F_emb was finite at only 2/10 points; the Spearman as coded ranks NaN blocks by index and returns 0.903 REGARDLESS of the data (the reviewer reproduced 0.903 from an all-NaN comparison). The SVD-embedded F was never validated on any control; order-readability of F is OPEN, not established. The 45b/45d "embedding degrades on denser relations" diagnosis was backwards. Replacement instrument prescribed (review O3): validation-gated order-only embedding (must reproduce the confetti K-ordering first), pairwise-complete Spearman with a minimum-finite-pairs floor, and — the right tool — the paper-14/15 causal-tomography stack read on the 45d best-point webs.

**M2 — the priority claim corrected:** round 43b's equal-split C = 4 webs were the first grown webs with verified S₅ (5/5, full tries); 45b's correct claim is "grown webs at latent dimension ~4 whose witnessed order dimension (≥ 5) exceeds the finite-C count law's allowance for their volume dimension"; GROWTH of the witnessed order with K is NOT yet observed (S₅ flat at all searched K; S₆ not found; no search below K = 12) — "the registered signature fired" is softened to "consistent with; growth unmeasured" (m4).

**M3 — the near-parking headline rescoped:** the 0.024 gap is INSIDE the instrument systematics. (a) The axis convention flips the verdict: the best point reads 1.267 under the dom-diagonal but **1.221 — below the 1.243 line — under the b-axis convention** (the physically natural axis for a relation whose sole temporal clock is b), and the confetti reference moves ~0.02–0.03 under the same swap; (b) the 1.267 is the minimum of 12 grid points on shared seeds (1.6 SE before multiplicity correction); (c) the volume gate was satisfied only by 5.00-CLAMPS (the actual dimension-window fraction, 0.0193, is 2.2× below the frozen M5 point — not volumetrically M4-like on the existing proxy). The corrected statement: **"the F-frontier sits AT the parking line within instrument systematics (+0.024 dom / −0.022 b-axis; selected minimum; 1.6 SE); the volume side is OPEN pending a ballistic-class calibration."**

**Minors recorded:** (m1) the F window and the dimension/witness instruments used DIFFERENT τ's (window-derived vs full-web, ~2.1×) — one split-sample τ per web everywhere at 45e; (m2) τ is the corpus's first relation constant derived from data — a gauge-like pinned formula (F moves only 1.27–1.42 over the 4× c-dial), named as fitted; (m3) **the worldline/cone tension (the kill partially fired, recorded):** at the best point the median slot dipole speed is 2.3× τ_window (worldlines exit the emergent cone) and 94–95% of same-slot links cross a reset (connectivity is reset-mediated) — and the c-dial that would make matter timelike reads LESS round: F-roundness and timelike worldlines currently anticorrelate — the sharpest open question the point poses (review O4); (m5) the LOG's "F_2D passes its veto" was wrong — no F_2D veto exists in the round-45 gates (a rotframe-spec construct never carried over); F_2D readings are INFO; (m6, positive) the ballistic relation is verified in-ontology: acyclic/transitive as implemented, monopole subtraction slot-local and exact, dim ≤ K+1 re-derived for the signed relation.

**The review-prescribed closing path (45e, awaiting user go):** Gate 0 — the same-pipeline parking line (M4/confetti through the IDENTICAL 45d pipeline, both axis conventions, 20 seeds; convention-STABLE verdict required); Gate 1 — pre-registered fresh-seed decision (the single point K=24/α=0.75/c=0.5 + at most two named neighbors, 10 fresh seeds, one-sided z ≤ −1.64, Bonferroni/3); Gate 2 — split-sample τ everywhere. Plus O2 (the ballistic-class volume calibration, REQUIRED before any parking claim), O4 (the slow-churn/timelike scan), O5 (the seeded S₆ hunt).

## Receipt 45e — the pre-registered decision protocol (pinned here, committed strictly before running)

Per the arc review's O1 + O2, verbatim adoption. **Gate 0 (the same-pipeline parking line):** iid 4D confetti pushed through the IDENTICAL 45d pipeline — 2048 points, central-t window, NW = 256 subsample, the ballistic relation (clocks τ·t + x·v_k, K = 24, c = 0.5, split-sample τ from the calibration half), F under BOTH axis conventions (dom-diagonal AND b/t-axis), 20 seeds (20263100+); the parking line per convention = 1.1 × max of that band; **any parking verdict must be CONVENTION-STABLE (pass under both).** **Gate 1 (the decision):** PRE-REGISTERED point P0 = (K = 24, α = 0.75, c = 0.5); named neighbors N1 = (K = 32, c = 0.5), N2 = (K = 24, c = 0.35), both α = 0.75 — nothing else may win; 10 FRESH seeds (20263000+); decision on P0 alone at one-sided z ≤ −1.64 against the Gate-0 line (per convention, both required); a neighbor may substitute only at the Bonferroni-corrected z ≤ −2.13; NO grid minima. **Gate 2 (τ hygiene):** ONE split-sample τ per web — derived from the calibration segment (snapshots 0–511), applied identically to the F window (central 1024, NW 256), the dimension window (Δ = 512, NW = 128), and the full-web witness relation; dF/dc printed. **O2 (the ballistic-class volume calibration, REQUIRED):** reference window fractions for latent d = 2..6 — iid d-dim confetti (t + (d−1)-dim space, K = 24 pinned-seed unit directions in the spatial factor) under the same ballistic relation and window protocol, 8 seeds each — giving d_MM_ballistic by interpolation (no clamps); **the volume gate for parking: win d_MM_ballistic ∈ [3.5, 4.5].** **PARKING (the full conjunction): P0 passes the z-test under BOTH conventions AND d_MM_ballistic ∈ [3.5, 4.5] AND in-window refusals ≥ 8/10 AND S₄ ≥ 3/5 (full-web, tries 8000).** Verdict classes: PARKED / NOT-PARKED / UNDECIDED-INSTRUMENT (if the two conventions disagree on the z-test — recorded as the systematics still winning). Exit 1 only on Gate-0 anchor wiring (the confetti trajectory reproduction).
