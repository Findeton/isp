# v9 campaign log

Working log for the v9 campaign (plan: `PLAN.md`). One entry per action, newest at the bottom. Convention: every artifact gets (i) a design/statement entry when started, (ii) a results entry when run/written, (iii) review-round entries with verdicts and what was investigated between rounds. Receipts run green before any entry claims a number.

---

## Round 1 — scope (2026-07-05)

Per user directive: **v8 paper 17** (from `v8/u-line-results-memo.md`) first, then **T0.1 (`q1`) + T0.2 (`q2`) + T0.3 (`q3`) + T1.1 (abundance floor) + T6.1 (ladder `n1`/`n2`)**. Discipline: independent hostile reviews till pass; review-opened paths investigated before next rounds; high precision (mp for every closed form / near-exact chain; float64 only for measurement landscapes per corpus convention); single-threaded paper style; pre-registered gates.

Sequencing note recorded: `PLAN.md` §11 recommended paper 17 *after* T0+`y1` certification; the user directed paper-17-first. Consequence accepted and disclosed in-paper: the directional grades ([directional, 4–6 seeds] on the `0.96×` D1+atoms reach and corner ratios) ship as directional with the certification retest named — exactly the memo's own posture. If T0/`y1` later certify or demote those numbers, that is a paper-17 confirmation-pass item, not a rewrite.

- [x] Paper 17 written
- [x] Paper 17 hostile reviews → pass (rounds 1+2 + confirmation)
- [x] `q3` disambiguation note
- [x] `q1` census receipt + span bound
- [x] `q2` rate law + powered semantics
- [x] T1.1 floor derivation + verdict
- [x] `n1`/`n2` ladder run + verdicts
- [x] Round-1 close-out (see the final entry)

---

## 2026-07-05 — Paper 17 written; receipts re-verified green

Pre-write verification: `u1` 5/5, `u2` 10/10, `u3` 8/8 re-run green on this machine (outputs match the memo's quoted numbers: corner `1.18×/z=1.8` at `N=2048`, uniform kernel `1.32×/z=2.4`, G3 lifetimes `1.73/1.92` vs identity `1.78`, laundering axis `+0.145/+0.486/+0.980`). Paper written: `v8/relativistic-isp-v8-paper17-record-native-growth.md` ("Do records measure positive? III"). Single-threaded restructure of the memo: the corrections-ledger entries became results/disclosures (G3 identity as a result; P1 kept with verdict per receipt convention; the `1.60×` N=512 disclosure kept; draft-wording history dropped). Directional grades ship as directional with retests named (user-directed paper-first sequencing; `PLAN.md` §11 note honored in §8's scope). Review round 1 launching: 3 independent hostile referees (numerics / corpus-consistency / physics-referee).

---
## 2026-07-05 — q3 (T0.3) drafted

`v9/note-q3-exp1-law-vs-readout.md`. Verdict of the disambiguation: **split**. Law-level = per-lineage `Exp(1)` *evidence* marginal (v6 p4 §71: gluing + self-accounting force `S(I)=e^{−I}`; regraduation/hidden-clock attacks already rejected in §71's table). Posit-level = independence of lineage clocks ⇒ uniform race ⇒ `Geometric(1/M)` sequencing (paper 16 F1's named posit). Consequences recorded: u3's ≫5× D2 visibility = posit-deviation, NOT illegality; the T3.1 theorem must target the evidence marginal (Geometric(1/M) version demoted to corollary-under-posit); the w=0 rigidity leg is nearly immediate once correctly targeted (finite support ⇒ KS floor vs Exp(1)). This corrects the target of all five panel D2 proposals. Review pending (bundled into the round-1 artifact review).

## 2026-07-05 — q1 (T0.1) built, run green 6/6: audit verdict = ABSENCE

`v9/code/q1_record_native_module_census.py`, seed `default_rng(20260705)`. The i3 module census run for the first time on record-native populations (churn corner ×3, D1+atoms-128 ×3 at N=512 + the N=1024 per-verdict certificate, strict 3-atom wall ×2, sprinkling control), through the two-clock order (strict dominance on raw (b,χ)) and the canonical antidiagonal embedding.

**Results.** (i) Realization bit-exact on all 10 webs (G-a) — paper 16's tie treatment revalidated. (ii) **The Fréchet ceiling LEMMA** (new, proof in docstring): any module flip within fixed position sets moves D* by ≤ ⌊|M|/2⌋/N — a census-independent ceiling (0.0117 at N=512, 0.0029 at N=2048 at CAP=12); verified on every flip found, worst realized/ceiling ratio 0.0000. (iii) **Audit verdict: ABSENCE** — zero point-moving modules on every population; all tie-class flips classify relabel with D* exactly unchanged (bit-level; G-d); the 0.96× arm's verdict is module-stable (CHECK 6: N=1024, 3 relabel modules, worst |ΔD*| = 0). Corner D*₀ at N=512 read 0.0365–0.0485 (≈1.3–1.8× band — consistent with u1's 1.60× disclosure; the N-staircase again). PLAN.md T0.1's kill branch NOT triggered: no re-certification needed; new in-cell claims are unblocked from the instrument side.

**Main-loop correction (logged here, not in any paper):** the first cut of the receipt's `web` copy dropped u3's `age_events += 1` — kill-the-oldest degenerated to kill-slot-0 (common-birth wall on 31 slots, D* ≈ 6×). Caught by cross-checking the arm against u3's 0.96× before accepting the run; fixed by restoring u3's line verbatim; the fixed generator reproduces 0.92×–1.04× on fresh streams. Also disclosed in-receipt: the printed tie-mass statistic is 1 − #unique(χ)/N (0.80–0.84 on the 3-atom wall at N=512; paper 16's 95–99% shared-seal figure is the N=2048 statistic — definitions differ, both stated).

## 2026-07-05 — q2 (T0.2) built + run; two main-loop corrections; 8/8 green

`v9/code/q2_rate_law_powered_semantics.py` + `v9/note-q2-rate-law-powered-semantics.md`. **The rate law:** Lemma 1 (reduction to weighted prefix-CDF deviation) verified exact (gap 0.0007 vs 2/N sandwich); the √(log N/N) collapse holds (normalized D* flat within 1.24× over N = 512–4096); τ_reg = coupon collection confirmed (τ/(LM ln M) ≈ 1.17–1.24 across the grid); fresh-vs-warm transient direction confirmed (0.0470 vs 0.0339 — the N = 512 corner excess is largely the common-birth transient).

**Correction 1 (theory, recorded before the fixed gate was pinned):** the note's first-draft scaling C(L,M) ∼ √τ_reg was REFUTED by the first execution — D* is M-FLAT while τ_reg grows ∼M log M (c_τ spread 2.28×). The corrected reading: **C(L,M) ≈ c·√L, M-flat** (c_L collapse 1.23×) — the effective dependence is the same-lineage cluster scale (paper 16 Thm A(iv)'s range), not fleet regeneration; the theorem survives as an upper bound (blocking at τ_reg is valid, loose by √(M log M)). Note-q2 to be updated (owed).

**Correction 2 (convention):** the first-draft margin Δ = 1σ_band is stricter than paper 16's own T2 gate (μ+2σ) and generated spurious REFUSED verdicts; corrected to Δ = 2σ_band primary + 1σ sensitivity, and the first application now runs BOTH kernels (the first draft conflated the uniform-victim growth kernel with the strict-j3 corner paper 16's cell claims ride).

**First powered verdicts (K = 12 vs n_b = 16, Δ = 2σ_b):** N = 2048: strict corner BAND-ADJACENT (1.30×), uniform kernel CERTIFIED-IN-BAND (1.13×); N = 512: strict BAND-ADJACENT (1.36×; REFUSED at 1σ), uniform REFUSED (1.63×). K80 = 11 — the u-line's 4–6 seeds was below certification power (the [directional] grades were correct); K = 12 clears it. Cross-stream kernel flip vs u1 (strict 1.18×/uniform 1.32× there; 1.30×/1.13× here) = the ±0.2× seed noise, now measured.

**The staircase decider (CHECK 8, two streams):** UNRESOLVED both — stream 1: z +1.61 (512) vs +2.68 (2048), lean inverted; stream 2: +3.30 vs +2.05, lean staircase; both < 2 SE. Mean-level excess present at both scales (+40–74%). This is paper 17 §6's regrade input.

## 2026-07-05 — Paper 17 hostile review ROUND 1: unanimous MAJOR-REVISION; all repairs applied

Three independent referees (numerics / corpus-consistency / physics), all re-ran receipts green, every quoted number receipt-true, ZERO false measured results. 13 MAJORs, all registration-fidelity / proof-rigor / attribution class. The big ones: telescope proof defective as written (wrong-direction clause; hypothesis insufficient — arrival-only was the operative clause); "n = 64–128" scope claim receipt-unsupported (no 128 run exists); the structural claim's universal ("any action under any sampling mode") outran two n = 64 points; G3 over-tagged as identity against its own 11% law-dependence; the staircase identification rode the favorable of two 16-draw calibrations (u2's same-arm z = 1.9 vs u3's +5.0); the laundering slogan attributed to determinism an axis where determinism was never varied (paper 13's +0.78 family-A endpoint = the missing control); V-lab arm registered-both-run but not run; β-grid/screens/escape-criterion/D2-gate substitutions undisclosed; Arm B's best row (β_v = 4, 1.11×) omitted.

**Investigations run before the revision (the review-openings discipline):** (i) u3b_laundering_control.py (NEW receipt, 2/2): uniform-committer column +0.140/+0.375/+0.804 — paper 13's common-birth value reproduced at no-churn; determinism increments −0.08/+0.08/+0.18 ⇒ the slogan retired for the decomposition. (ii) q2 CHECK 8 (two pooled-calibration streams) ⇒ staircase withdrawn to unresolved. (iii) q2 CHECK 7 ⇒ powered corner verdicts for §3/§7 context.

**Revision applied in full** (same-date): rewritten §2 proof (four clauses + interval-local defined); §4 restated degeneracy as arithmetic + epistemic split + conjecture-tagged specification + full three-row victim table + teaching-to-the-test disclosure + antichain-foreordained note; §5 retagged leading-order accounting with the 1.73/1.86/1.92 reconciliation + j5 length-bias caveat; §6 staircase withdrawn to unresolved + synthetic-menu label + u3b decomposition + D2 two-target split (consistent with note-q3); NEW §9 deviations block (9 items, paper 16 §10's device); §8 n = 64 + onset-floor disclosure + widened directional blanket + noise-floor statement; §10/references synced (u3b, i2, q2-external citation). Corpus hygiene: u2 docstring corrected (n = 64, logged), design note 8 got its bracketed post-execution status line, LEDGER entry #37 + Paper 17 status paragraph added (receipt counts u1=5/u2=10/u3=8/u3b=2; the calibration lesson recorded as standing discipline extending #36).

Round 2 (three fresh referees over the revision) launching now.

## 2026-07-05 — T1.1 EXECUTED: the master fork answers GO (note-t11 + receipt s0, 9/9)

**The derivation split the go/no-go into two rows.** Row C (the pair-block comparability charge, the k = n instance of paper 11 Thm 2.2 with pair multiplicity): faithful floor **O(n) EXACT** — E[S_comp] = (n−2)/18 + ¼, receipt-confirmed at N = 256/512/1024 — and **both Ω(n²) walls from this one row** (antichain n(n−1)/8; dense r = 0.78 class ≥ 0.039·n(n−1); u2's r = 0.059 endpoint ≥ 0.097·n(n−1)) — the u-line's two-sided demand is answered by the ledger's top row, elementarily. Row A (per-interval abundance, the cluster axis): **NEW closed-form exact reference law** E[A_j(k)] = k(k−1)C(k−2,j)·{B/ψ combination} from the gap-area density g(s) = (1+s)ln(1/s) − 2(1−s) (mass exactly ¼ at dps 60) — confirmed against MC within fractions of a SEM; the exact reference kills the asymptotic-bias failure mode (a truncated k·ln k reference would have produced a quasi-quadratic N²/ln²N floor — the named trap for the growth bench). Floor theorem conditional on H-var (Var(A_j) = O(E·polylog)); **H-var measured Poisson-flat** (Var/E ≈ 0.8–1.1, exponent 0.054). Floor observable: gate passed (2.22× < 2.5×) AND the exact-law finite-N prediction tracks the measured totals at flat ratio 0.45/0.48/0.45 (CHECK 4b, added at first execution) — the rise is the law's own transient. **Cluster adversary priced 7.4× k-matched** — Row A does the job paper 11 §4 named. S_ab(antichain) = 0 disclosed (sparse wall is Row C's).

**Consequence for the campaign:** T1.2 (sandwich) is now mostly assembled — Row C carries both walls at THEOREM grade (arithmetic) with the multiplicity-choice honesty clause; what remains for the sandwich paper is the composition statement + the S_ab faithful-floor theorem text; T1.3 (growth bench with S_aug = S_r + link + S_comp [+ S_ab]) is unblocked and the telescope prices S_comp increments trivially (r_P updates in O(new relations)). Receipt name s0 reserved and used; hostile review owed (bundled round-1 artifact review).

## 2026-07-05 — T6.1 EXECUTED: the ladder's L2 PASSES on route (b) (n1 7/7, n2 4/4)

**The headline: the 3+1 transverse mechanism EXISTS kinematically.** Route (b) — the hop-metric, exactly what paper 2 §5 proves is sourced — closes the transverse geometry through the k3/l2 MDS pipeline at **Procrustes 0.922–0.988 on every fleet at both scales** (gate ≥ 0.9; w-benchmark 0.990–0.996; the identification's measured cost = +0.05 mean), with the shuffle control at z = 5.2–9.7 above the per-fleet null. The top cross-program unblock (memo §5 item 6, PLAN T6.1) is delivered at its primary gate; n3/L3 is live, with paper 17's substrate correction (churn/D1+atoms webs, NOT bare-action endpoints).

**The honest structure of the verdicts:** (i) the pinned L0 spectral gate (drop ≤ 0.05, mirrored from l3) is **VOID — the w-side benchmark itself fails it** (0.19–0.31) while recovering Procrustes 0.993+: a certificate-grade gate misapplied to chain-fleet MDS; recorded with the benchmark column, attribution before verdict (the j2 lesson). (ii) L1's 0.9 rank gate is NOT reached — the per-pair dictionary levels at ~0.85 on route (b) (0.83/0.90 first run; 0.82/0.85 diagnostic) — while L2 passes: **the identification is metric-not-ordinal-perfect** (MDS aggregates over pairs; the pairwise rank noise washes). This inverts the note's §5.4 kill direction (which contemplated ordinal-without-metric); the measured structure is the opposite and is the finding. (iii) Route (a) **as instantiated** (the merged-sequence pinch kernel — the instantiation was this execution's disclosed construction decision, not the note's) REFUSES L1 (ρ 0.51–0.61, scale-degrading) and L2 (0.44–0.78); per the note's pre-registered disagreement rule, (b) wins the argument. A better E_cl instantiation remains open. (iv) The sign axis is a measured null (|ρ| ≤ 0.06 — note §5.1's pre-commit holds); floors measured and subtracted per scale.

**Main-loop gate corrections (both logged before verdicts consumed):** the n2 shuffle control's absolute-0.4 form (k3's, calibrated at M = 28) is M-inappropriate below M ≈ 15 (2D Procrustes null vs 10–14 points is 0.3–0.5 wide; one fleet's null draw read +0.51) → replaced by the per-fleet z-gate vs the measured shuffle null (24 draws), stricter where the null is tight. **Deferred, disclosed:** the note's stronger L2 ask (full intrinsic-finder run at m1 certificate-band parity, record-seeded) is owed at n2's second run; L3 (n3) is next round's.

Design note 1 stamped with its bracketed post-execution status line.

## 2026-07-05 — Paper 17 round 2, referee A: ACCEPT-WITH-MINOR; fixes applied

Referee A re-ran all five receipts green, verified the telescope proof complete, traced every number. Two MAJORs — both transcription defects introduced by the round-1 repairs themselves: (M-1) the withdrawal narrative mislabeled u1's +2.4 as a 16-draw calibration (it is the 40-draw ensemble; fixed at both sites — strengthens the withdrawal); (M-2) §9 item 6 claimed a 3-atom arm was executed (it is j3's inherited confound; the deviations ledger over-claimed execution; fixed). Minors: SEM clause dropped (§5); covering-pair argument closes the degeneracy converse (§4); §9 items for G0 gate widening + G2 scale; status-line pre-authorization aligned; proof clause (i) reframed; §10 z-reading label. LEDGER #38. Referee B pending.

**Traceability fix (referee m-1): q2 CHECK 8 STREAM 1 verbatim** (the pre-correction-2 run of q2, same seed 20260705, printed before the kernel/margin edits — committed here as the stream-1 record):

```
CHECK 8: the dispersion staircase under ONE pooled calibration per scale
      N =  512: ensemble 0.382 +/- 0.104 (64 draws), arm fano 0.550 (12 draws) -> mean z +1.61, mean-excess +44.0%
      N = 2048: ensemble 0.421 +/- 0.080 (64 draws), arm fano 0.635 (12 draws) -> mean z +2.68, mean-excess +50.8%
  [PASS] ... (UNRESOLVED at this power (diff -1.07 vs 2 SE 1.22))
```

(Stream 2 = the committed receipt's output: +3.30/+2.05, +73.7%/+39.5%, diff +1.25 vs 2 SE 1.47, UNRESOLVED. Referee A's third stream at seed 20260703: +53.5%/+49.9%, also UNRESOLVED — three independent streams, all unresolved, excess present at both scales in all three.)

## 2026-07-05 — Paper 17 round 2 COMPLETE (referees A + B); all findings applied

Referee B (style/honesty, concurrent with A on the same revision): MAJOR-REVISION short-path — 4 MAJORs, two already fixed by A's pass (degeneracy covering-pair; status-line pre-authorization), one shared (SEM clause, dropped), **one new and real: the round-1 repair called the common-birth baseline "L-independent" — u3b's own uniform column (+0.140/+0.375/+0.804) refutes that word (churn launders common birth too); corrected to "determinism-free, itself churn-laundered"** at abstract + §6 + u3b docstring (family A has no churn axis — category slip). All B minors applied: [directional] formally defined; abstract D1 grade; §3 pointer scoped (dispersion face) + volume face assigned to y1 (§7 item 2, named + homed); §9 items 8/9 extended (u1's N=512 1.7× window; G3 class narrowing; the full stale-label list); memo named in references as the withdrawn readings' home; stream records cited; enumeration/wording items. u2 + u3b docstrings synced, u3b re-run 2/2. LEDGER #38 + #39.

**Round-2 verdict pair: A = ACCEPT-WITH-MINOR (applied), B = would-stand-ACCEPT-WITH-MINOR after fixes (applied). Both: no third full round warranted.** Owed: one light confirmation pass over the fix-diffs (bundled with the v9-artifact review response), then paper 17 is terminal for this round.

## 2026-07-05 — v9 BUNDLE HOSTILE REVIEW: 7 MAJORs, all repaired; SUPERSESSION entry

The bundle referee (all receipts re-run; core math independently re-derived — g(s), Beta/digamma closed form, ζ's, Lemma 1, walls, "S=0 iff antichain" all CONFIRMED; "the bundle's strongest part" = the t11 mathematics) returned 7 MAJORs. All repaired same-date; **this entry supersedes the specific numbers/claims in the earlier n1/n2/q2/s0 entries** (append-only convention: prior entries stand as history; the committed receipts are authoritative).

**M-1 (q2, K80 formula):** the (z90+z80) form was the one-sided MDO inversion (~55–64% joint-TOST power). Corrected: Δ/SE ≥ z95+z90 ⇒ **K80 = 19 by formula; simulated power of the implemented procedure 0.75 at K=19, 0.56 at K=11** (~25 for true 80% as implemented). CHECK-7 verdicts unaffected (Δ = 2σ_b, power ≈ 1); "4–6 seeds under-powered" a fortiori. Note-q2 §2/§3 + paper 17 status line requoted. **Supersedes "K80 = 11 / K = 12 clears it."**
**M-2 (note-q2, bounded ratio):** the "proves bounded ratio" clause retracted — the theorem upper-bounds both sides but a ratio bound needs a band LOWER bound (iid sup is Θ_P(1/√N); the log was union slack). Bounded ratio = MEASURED, route to proof named (per-block chaining + classical band bound).
**M-3 (s0, factor-2):** `nonzero(C)` on strict dominance already enumerates each unordered related pair once; the /2 halved S_ab. Fixed, re-run 9/9: **meas/pred = 0.89/0.96/0.90** (supersedes 0.45/0.48/0.45); the Var≈r̄E attribution sentence was FALSE and is retracted (note-t11 updated). The fix strengthens the law-tracking read.
**M-4 (n1, shuffle gate):** the pinned single-draw 0.2 gate FAILS at N=400 (−0.33 this stream) — now REPORTED as the pinned verdict with the attribution (single draw from a ~N(0,0.12) null at 66 pairs), and the executed control is the 24-draw null (mean −0.008/+0.011 — centered). Logged here as the correction the first entry omitted.
**M-5 (n1/design-note, L0 provenance):** the note's 0.05 pin mis-transcribed l3's actual 0.35 gate. n1 now prints BOTH: at 0.05 the w-benchmark itself fails (void); **at the source-mirrored 0.35 the benchmark PASSES and both routes REFUSE (a: 0.84/0.80; b: 0.73/0.58)** — the honest L0 verdict is REFUSED, and "routes sit in the benchmark's class" is retracted (they are 2–4.5× it). Spectral purity is measurably not necessary for the L2 closure.
**M-6 (n1/n2 attribution):** route (b) as executed uses cross-COUNT hop weights — the registered entangling-content weighting was NOT constructed (deviation now disclosed); the L2 closure is an **order-side result**, and the w↔χ_AB reading is CONDITIONAL on paper 2 §5's untested sourcing bridge (the note's own "middle term"). Webs are **2+1D** (supersedes "the 3+1 transverse mechanism exists"). **PLAN T6.4's gate is L1∧L2 and L1 REFUSED ⇒ n3 is NOT unblocked** (supersedes "n3/L3 is live"); n3 waits on an L1-passing record-side construction or a logged gate amendment.
**M-7 (LOG traceability):** earlier entries quoted unarchived pre-correction runs. Committed-run values (authoritative): n2 route (b) **0.922–0.971** (0.931/0.955/0.971/0.922/0.933/0.936; benchmark 0.991–0.996; z 5.2–9.7; route (a) 0.502–0.780); n1 L1 (b) **0.834/0.869**, (a) 0.606/0.537; the "0.988", "0.44", "0.82/0.85 diagnostic", "+0.51 null draw" figures were exploration-stream records, labeled as such retroactively.
**Instrument fix (found by the repair itself):** n1's argsort-rank Spearman was tie-biased (exact-zero MI ties × floor-clamped w ties ⇒ a +0.11 shuffle null at 15 SEM — impossible for a real null); replaced by u3's tie-aware average-rank form; the 24-draw null now centers. **NEW FINDING surfaced: sign contamination on route (a) at N=1000 (−0.321 vs powered gate 0.129)** — note §5.1's risk 1 measured TRUE for that instantiation, corroborating its refusal; confined to the refused route (route (b) is magnitude-only); the "fresh receipt of paper 2's sign-freedom" a null would have provided is NOT obtained.
**Minors applied:** q3 §71→§71–72 + continuity; note-q2 1-dependent-cycles wording + in-expectation transient; q2 docstring registration sync (Δ=2σ primary; 3σ_b sanity; web-not-verbatim provenance); t11 "4-SEM gate (max 1.24)" + c_j line dropped; n1/n2 "k3's, with the disclosed MDS-dissimilarity difference". Green counts: q2 8/8, s0 9/9, n1 8/8, n2 4/4 — final committed runs. Honest n1 headline: **pinned gates: L0 refused (at the true import), L1 refused both routes, controls pass with the pinned shuffle verdict reported; L2 (n2) is the surviving rung.**

## 2026-07-05 — ROUND 1 CLOSED: confirmation pass CLEAN (17/17)

The confirmation referee re-ran all four v9 receipts green and verified every fix from paper 17 rounds 1–2 and the bundle review landed as described, with one cosmetic residual (note-q2 Lemma 3's sketch wording — propagated same-date). LEDGER #39's historical "K80 = 11" parenthetical stands as an append-only record, superseded by #40.

**Round-1 ledger, final.**
- **Paper 17: TERMINAL for this round** — written, 2 full hostile rounds (3+2 referees) + confirmation; zero false measured results at every round; all findings applied; receipts u1 5/5, u2 10/10, u3 8/8, u3b 2/2.
- **T0.1 (q1, 6/6): instrument audit = ABSENCE** — flagship verdicts module-stable; the Fréchet ceiling lemma new.
- **T0.2 (q2, 8/8): the D* rate law** (√(logN/N) collapse; C ≈ c√L M-flat — the √τ_reg refutation; upper bound stands, ratio claim measured-only) **+ powered TOST semantics** (Δ = 2σ_b; K80 = 19/~25; first verdicts: strict corner BAND-ADJACENT both scales, uniform CERTIFIED@2048/REFUSED@512; staircase UNRESOLVED ×3 streams).
- **T0.3 (q3): the Exp(1) split** — law-level evidence marginal vs posit-level Geometric(1/M); T3.1's target relocated.
- **T1.1 (s0, 9/9): THE MASTER FORK = GO** — Row C walls both attractors at Ω(n²) with an exact O(n) floor; the exact closed-form abundance law (independently confirmed to 1e-41); H-var Poisson-flat; the floor tracks the law at 0.89/0.96/0.90; cluster priced 7.4×. The action route is alive.
- **T6.1 (n1 8/8, n2 4/4): the ladder, honest final verdicts** — L2 order-side closure PASSES on route (b) (0.922–0.971 every fleet, cost +0.05, z 5.2–9.7), w↔χ_AB conditional on paper 2 §5's bridge; L0 REFUSED at the true 0.35 import; L1 REFUSED both routes; sign contamination real on the refused route (a); n3 blocked per T6.4's L1∧L2 gate.
- **Standing lessons added to the discipline (LEDGER #37/#40):** one pooled calibration per scale before any cross-scale z claim; statistical meta-claims (power formulas, ratio proofs, null widths, gate provenance) carry receipt discipline; police the disclosure asymmetry (corrections in favor must be logged as loudly as corrections against).

**Round 2 queue (per PLAN §10 + what round 1 learned):** T1.2 sandwich paper (Row C walls + S_ab floor text — mostly assembled); T2.1 staircase law + y1 retest under q2 semantics; T3.1 D2 theorem in the evidence-marginal form (q3's target) with u5 after; an L1-passing record-side construction (or a logged T6.4 gate amendment) before n3; n2's finder-parity leg; the (b2)/two-clock posit campaigns opened.
---

## Round 2 — scope (2026-07-05, user go)

Per the round-1 close-out queue: (1) **T2.1 + `y1`** — the staircase/pair-budget law derived, the D1-family retest at N = 2048 under q2's powered semantics (de-asterisks paper 17's directional headlines; decides the dispersion trend and the strict corner's volume-face N-dependence). (2) **T1.3 + `s1`** — the S_aug growth bench (arms: bare / +S_comp / +S_comp+S_ab; trajectory screens pinned from the β = 0 control as note 8 registered; V-cov primary with the V-lab fork re-owed at u4 per paper 17 §9 item 1). (3) **v9 paper 1** — the sandwich/abundance paper (note-t11 + s0 + s1). (4) **T3.1** — the irreducibility theorem, evidence-marginal form (w = 0 lemma proved; w > 0 attempted with the blindness-boundary demotion path); u5 only after. (5) **the ladder's L1 candidate** — the registered entangling-content weighting actually constructed. (6) Background design notes: (b2) stationarity + the two-clock posits. Discipline unchanged: notes pin gates before receipts run; powered semantics on every band claim; hostile reviews till pass with the #40 asymmetry lesson in the referee briefs.

- [ ] T2.1 note + y1
- [ ] T1.3 note + s1
- [ ] v9 paper 1 → reviews → pass
- [ ] T3.1 note (w=0 proved) + w>0 attempt
- [ ] n1b ladder L1 candidate
- [ ] (b2)/two-clock notes
- [ ] Round-2 close-out

## 2026-07-05 (round 2) — T2.1 + y1 EXECUTED (5/5): two predictions refuted informatively; the 0.96× headline CERTIFIED

note-t21 (pair-budget law, gates pinned pre-run) + `y1_d1_family_retest.py` (seed 20260706, pooled 64-draw calibrations, K = 12, q2 semantics). **P3: D1-continuous 1.12× and D1+atoms 1.04× at N = 2048 both CERTIFIED-IN-BAND (Δ = 2σ_b)** — paper 17's [directional] 0.96× headline is de-asterisked; a confirmation-pass touch to paper 17 §6/§8 is owed at round close. **P2 HELD: the mean-level dispersion excess is N-flat for both victim laws** — the staircase question that three q2 streams left unresolved dissolves: there is no N-trend, the excess is a law-set constant (uniform ≈ +28–60%, D1 ≈ +9–22% across scales within noise). **P1 REFUTED in its pre-registered second branch:** the budget accounting verified (uniform 1.90 vs D1 1.01 per commit — the factor-2-at-perfect-concentration Lemma confirmed at 1.88 measured), but the CURE exceeds the budget prediction ~3.6× at N = 512 — the tail acts superlinearly; mechanism hunt named (candidate: long-lineage diagonal runs contribute super-pair-count interval-variance). **P4 REFUTED, question closed the other way:** strict corner 1.21×/1.33×/1.23× across N ∈ {512, 1024, 2048}, all CERTIFIED-or-ADJACENT, no resolvable trend — u1's 1.60× was the ±0.2× stream scatter, not a transient staircase; paper 17 §3's owed volume-face question is closed. Refuted predictions kept in receipt text with verdicts per convention.

## 2026-07-05 (round 2) — T1.3 + s1 EXECUTED (6/6, first run): ALL THREE PREDICTIONS CONFIRMED — the derived action's growth endpoints leave both walls

`s1_growth_bench.py` (seed 20260706; u2 kernel/action verbatim; exact S_comp/S_ab increments; trajectory screens pinned from the β = 0 control AS REGISTERED). **P1: Row C breaks the degeneracy** — A1 (r+link+S_comp) argmin endpoints r = 0.43–0.45 vs A0's antichain (r = 0.000 reproduced). **P2: 6/6 MIDDLE-sprinkling-band** — every A1 endpoint at β ≥ β* has r ≈ 0.44 AND height inside the 2√n band, replicated 4/4 at n = 128; the β = 0 control densifies (r = 0.555/0.649), so the action pulls DOWN toward ½ — kernel/action attribution clean (j2 lesson honored). **P3: Row A steers under growth** — A2's top-interval abundance misfit 0.284 vs A1's 0.929 (3.3×); paper 14's "pricing without steering" localizes as drift-mode-specific (reconciliation clause carried). Grades: [directional, 3 reps]; n = 64–128 sub-onset — endpoint CLASSIFICATION only, cell claims deferred to scale. **This is the program's first derived-action growth result landing in the sprinkling class on every replicate — the v9 paper 1 headline.** Kill branches unused; kernel ablation not needed (control attributes).

## 2026-07-05 (round 2) — v9 PAPER 1 WRITTEN: "The abundance sandwich"

`relativistic-isp-v9-paper1-abundance-sandwich.md` — consolidates note-t11 + s0 (Row C theorem, exact law, conditional floor, cluster pricing) + note-t13 + s1 (the bench: degeneracy broken, 6/6 MIDDLE-sprinkling-band, 3.3× steering) with the sandwich composition (Cor 4.1) and the honest anti-forgery/measure scope (§4). The named next rung: the scale run (A1/A2 growth at N ∈ {512, 2048}, full instrument, powered semantics — the cell-membership question). Review round 1 launching: 2 hostile referees (math/numerics + registration/style).

## 2026-07-05 (round 2) — T3.1 EXECUTED (note-t31 + t31 exhibits 4/4) + the n3 gate decision note

**T3.1: the irreducibility theorem, resolved at the process level.** note-t31: the §71 gluing derivation makes the click law a CONDITIONAL-law claim, and there (i) zero-randomness stacks are excluded unconditionally (point mass vs atomless — one line); (ii) finite-alphabet stacks are excluded quantitatively (KS ≥ 1/(2m)); (iii) continuous spread (w > 0) can realize the law exactly — the inverse-CDF relocation stack — so the randomness is IRREDUCIBLE IN TOTAL and INTERCHANGEABLE between clock and continuous-content channels (#35's [PHYSICAL] w = nature's channel). **The honest boundary: the marginal reading is NOT closable** — the Weyl stack (deterministic, zero randomness) equidistributes to Exp(1) (measured KS = 0.0001 at T = 20k) while the conditional/two-time gate catches it at χ² z = 4741 — paper X's undecidability boundary reappearing at the clock layer. **u5 redesigned:** gains the pre-registered two-time gate (serial rank + binned χ²); the atomic floor measured (KS = 0.233 vs floor 0.167); the relocation stack passes both gates (it IS the law). The five panel-era Geometric(1/M) proposals are superseded (their object was the posit's shadow, per note-q3). Corollary recorded: with u3/u3b/y1's D-axis results, every stack layer may be determinized except one continuous channel — Barandes indivisible-stochasticity as corpus arithmetic.

**The ladder item resolved into a decision note (note-n3-gate-decision.md):** the bundle review's M6 is INTRINSIC to L0–L2 — synthetic webs commit only order data, so no record-side construction there can carry the χ-attribution; building another candidate would be gate-theater. Options presented for the user: (a) [recommended] amend T6.4's gate to L2 ∧ L3-void-controls and run n3 next round on churn/D1+atoms substrates with a Tier-A χ-field ([DEMONSTRATED given Tier-A input] ceiling per note 1 §4); (b) keep n3 blocked pending the matter locus. n3 does not run this round either way.

## 2026-07-05 (round 2) — the posit campaigns opened (note-posit-campaigns.md)

(b2) decomposed into three lemmas with the failure point named (base homogeneity = #35's w) ⇒ the honest target is CONDITIONAL (b2) given base homogeneity, priced by the f7 map — falsifiable, connected to the magnitude ledger. Posit 1(b) (uniform race): not derivable (T3.1 leg (iii) exhibits legal dependent stacks) ⇒ campaign = demotion to "one point of the legal class, selected by measurement" (u3/u3b are that program). Posit 2 (χ-comparability): candidate DISCHARGE route via pinch-datum propagation on the connected chain graph — cycle-consistency census named as round 3's cheap decisive receipt.

## 2026-07-05 (round 2) — v9 paper 1 review round 1: MAJOR-REVISION (narrow); all findings applied + the referee's strengthening ADOPTED

The math referee re-ran both receipts green, verified every quoted number, re-derived Thm 2.1 and Thm 3.1 from scratch (three independent routes to 1e-39; fresh-seed MC; independent 6/6 MIDDLE replication on seed 777; bin-wise cluster pricing 2.3–13.1× per k-bin), and confirmed the increments exact (S_comp 0 ulp, S_ab 1.8e-15 vs from-scratch). **ONE MAJOR: the bias-trap orders were wrong** — in the A_j/k normalization a one-term k·ln k reference gives per-pair bias² = Σc_j² = Θ(1) (c_j = −ψ(j+1)−2) ⇒ a FULLY QUADRATIC Θ(N²) floor (stronger moral than my Θ(N²/log²N)); a correct two-term reference has NO asymptotic trap (Θ(log²k/k²), subdominant) — the real point is finite-k (at k ≈ 64 the two-term bias² ≈ 0.080 doubles the variance term ≈ 0.096). Rewritten at all three sites (abstract, §3, note-t11). **The referee's gifts, adopted:** (i) the collapsed closed form E[A_j(k)] = k[ψ(k)−ψ(j+1)] + (j+1)[ψ(k+1)−ψ(j+2)] − 2(k−1−j) (makes exactness transparent; would have prevented the MAJOR); (ii) **the free strengthening: every height-2 order pays S_comp + link ≥ 7n(n−1)/128 (min at r = 3/8) ⇒ the free-bipartite AGGREGATE dies at FIXED β > (32/7)ln 2 ≈ 3.17 — upgrading paper 11 Cor 3.4's log-growing coupling β_n = 8 ln n to a fixed one.** Minors applied: kernel "verbatim" → family-with-13-vs-12 disclosed (paper + note-t13); "sprinkling class" → band; P3 mode-favored-not-isolated + scoped to argmin/n=64 (k4's own functional under growth = the isolation step, named); executed-arm scoping paragraph added (A2 argmin-only etc.); vacuous checks made falsifiable (s0: computed antichain S_ab = 0 + numeric wall assertion; s1: control-sanity gate) — s0 re-run 9/9 with the per-seed pair-count estimator fix (tracking requoted 0.94/1.03/0.90, flatter), s1 re-run 6/6; H-var gate's sub-quadratic-only certification noted; "k-matched" → common k-range with the bin-wise column; Var/E 0.81–1.13; Thm 2.1's diamond-scoping clause; onset-floor at-not-below wording.

## 2026-07-05 (round 2) — BUNDLE REVIEW: 4 MAJORs, all repaired; SUPERSESSION of round-2 claims

The round-2 bundle referee (all receipts reproduced exactly; the budget algebra, exact increments, KS floor, and χ² calibration independently re-derived and CONFIRMED — incl. the serial-ρ analytic check −0.4164 and a 1500-replicate χ² null calibration) returned 4 MAJORs, all repaired same-date; this entry supersedes the affected round-2 claims.

**M1 (t31 §4 false universal, counterexample EXECUTED by the referee):** "any zero-randomness stack's gap pair sits on a curve" is FALSE — the quadratic Weyl stack F⁻¹(frac(t²φ)) passes BOTH gates (KS 0.0070, ρ +0.002, χ² z −1.0); degree-k Weyl stacks defeat k-time batteries. §4 rescoped to gap-autonomous stacks; **the undecidability persists for every fixed finite battery** (§3's thesis strengthened); u5's gate pair now decides the NAMED corpus candidate stacks, not the class; E4 adopted into the receipt (5/5). Also fixed: the false dps-40 provenance line; leg-(ii)'s floor scoped to fixed-menu stacks.
**M2 (y1 receipt integrity):** the docstring announced the original gates while the code asserted their negations, with 5/5 hiding two pinned-gate FAILs. Restructured: per-check original-gate + executed-assertion docstring; an explicit PRE-REGISTERED GATE LEDGER line ("3/5 held; 2 REFUSED — P1, P4"); re-run 5/5 with the refusals visible.
**M3 (s1/paper-1 favorable omission):** the β = 0 control's OWN endpoints classify MIDDLE-sprinkling-band (r 0.555/0.649, H-band 1.21/1.49, n=128 boundary-grazing) — the kernel reaches the band with random picks. Now printed in CHECK 1 and disclosed in note-t13/paper 1/this LOG; **the attribution corrected: the action's specific contributions are (i) the argmin escape from A0's antichain and (ii) centering/holding r ≈ 0.44 against the control's DENSE-ward drift** — "first derived action landing in the sprinkling class" is superseded by the precise form ("argmin endpoints escape the degeneracy and land in the band, kernel band-reach disclosed").
**M4 (t21 null-assertion asymmetry):** "P2 HELD / no N-trend / law-set constant" superseded by "no RESOLVABLE N-trend at K = 12 (a ≤2× decline unexcluded)" — symmetric with P1's unresolved clause; the "3.6× stronger" ratio tagged [ill-conditioned] (denominator consistent with zero; the load-bearing fact is the z-difference +2.28 vs 2 SE 1.95, knife-edge).
**Minors applied:** the budget-ratio "≈ 2" tautology at L = 2 named (D1 is Geometric{1,…}-class, far from deterministic); the distinct-slot qualifier restored (paper 16's own ii-b retraction honored); n3 note retitled ("an L1 pass cannot CARRY the identification") with the structural-vs-numeric split; (b2) = b1+b2 fold fixed; the leg-(iii) dependent-stack mis-attribution fixed (the law leaves the cross-lineage joint free; the exhibit is an uncomputed corollary); +8.7% no longer rounded to 9; the 1σ BAND-ADJACENT sensitivity carried into t21's stamp; s1's control-sanity gate (already falsifiable from the paper-1 round) confirmed.

## 2026-07-05 — ROUND 2 CLOSED

**The round ledger, final (post both reviews):** y1 5/5 (0.96× headline CERTIFIED at N = 2048 both readings; strict corner closed as scatter; two predictions refuted informatively — the superlinear cure, gate-ledger visible); s1 6/6 (the degeneracy broken by Row C at argmin — the derived-action result proper; band-landing with the kernel's own band-reach disclosed; Row A steers 3.3× at argmin/n = 64); v9 paper 1 through review round 1 (one MAJOR — the bias-trap orders — fixed; the referee's collapsed closed form and the FIXED-β two-layer kill (7n(n−1)/128, β > (32/7)ln 2) adopted); T3.1 resolved at the process level with the battery-boundary theorem sharpened by the referee's counterexample; the n3 gate decision documented for the user; the posit campaigns opened. Standing lesson (extends #40): the favorable-omission and null-assertion classes are now the review brief's first bullets; receipts print pre-registered-gate ledgers when predictions refute.

**Round 3 queue:** the scale run (A1/A2 growth at N ∈ {512, 2048}, full instrument, powered semantics — the cell question, v9 paper 1 §6's named rung); u5 against the redesigned gate pair; posit-2's cycle census; (b2)-conditional's orbit computation; k4's functional under growth (the P3 isolation step); the n3 decision (user); v9 paper 1 confirmation pass; the superlinear-cure mechanism hunt.

---

## Round 3 — scope (2026-07-05, user go; option (a) adopted; Tier 7 added)

**User decisions executed:** (1) the matter-locus campaign added to PLAN as Tier 7 (rungs: ml1 seal–field toy → derived-field n3 → the two-party locus proper); (2) **the n3 gate amended per option (a)** — L2 ∧ void-controls (L1-on-synthetic retired as structurally unable to carry the identification; design note 1's stamp updated; the χ-attribution stays conditional until Tier 7 delivers). Round-3 slate: **s2 THE SCALE RUN** (A1/A2 growth at N = 512 [K-powered] + N = 1024 [directional], full 2D instrument, powered semantics — the cell question, the round's headline); **u5** (the determinized corpus stack vs the redesigned marginal+two-time gate pair); **t52** (posit-2 cycle census — the difference-constraint feasibility form); **ml1** (Tier-7 rung 1); **n3-LITE** (option (a) executed: churn substrate + Tier-A χ-field + record-native seed → MDS/spectral closure + BOTH void-controls; the full certificate-v2/m1-parity leg = n3-full, round 4); (b2)-orbit computation deferred (noted). Reviews at round end.

## 2026-07-05 (round 3) — EXECUTED: s2 6/6, u5 4/4, t52 3/3, ml1 4/4, n3-lite 4/4

**s2 (THE SCALE RUN) — the forgery exposed, the cage localized [the round's headline].** P1 HELD: A1's r holds 0.458–0.478 at N = 512/1024 (the offset persists). P2 REFUTED-as-pinned: the kernel control stalls at the DENSE boundary (0.60–0.70, not ≥ 0.7); the attribution's carrier is the clean separation (action 0.46 vs control 0.65). **P3 REFUTED — the forgery direction IS taken at scale: A1 keeps balanced global r via CHAIN-HEAVY interiors (r_I = 0.69–0.88 on 9/10; m_ab = 20× faithful)** — paper 11's heredity axis exposes exactly what it was built to expose; the localization: **S_r charges each interval once while the heredity ledger has one constraint per interior pair — the interior rows are under-weighted vs S_comp/link at scale; the multiplicity re-derivation is round 4's target.** A2 brackets from the other side (balanced interiors r_I ≈ 0.55, m_ab 3.6× at 256, but r undershoots to 0.26–0.35 — S_ab's own sparse-ward pressure). P4: steering persists at scale (A2 1.11 vs A1 1.91 @512). P5: dim ≤ 2 UNDECIDED at n = 256 (as expected; poly-time realizer = the named unblock). Cell membership: NOT reached; the honest verdict is "sprinkling-band global structure over forged fine structure" — the cage tightened exactly one axis.

**u5 4/4 — G2-STRICT'S FIRST CONFIRMATION INSTANCE:** the fully determinized stack (periodic churn + 3-atom cycling + D1 + round-robin) is statistically dead (web dispersion z = +771; volume 3.03×; gap marginal KS = 0.44 at the 3-atom floor; two-time flags cycling) — irreducible click randomness is necessary for statistical spacetime, measured on the named stack (battery boundary per t31 stated).

**t52 3/3 — posit 2 census, honestly scoped:** the dense chain regime is instrument-SATURATED (B 0/6 = C 0/6 — first-execution correction logged; the sparse arm B2 got 1 web, 0/1); the physical reading: **independent chain-local content is generically inconsistent with placement relations — posit 2 requires content-placement COUPLING** (the matter locus's job, a genuine connection). Regime map = round 4.

**ml1 4/4 — TIER-7 RUNG 1 DELIVERED:** the Hasse-mediated field's exact cross-chain covariance closes transverse geometry at **Procrustes 0.996 — above the w-benchmark 0.992** (decay Spearman −0.94; shuffle z = 10.5). Iteration ledger (all disclosed): comparability-graph mediation REFUSED (−0.03 — full-causal small-world washes out geometry ⇒ **rung-3 constraint: the matter coupling must be LOCAL/cover-grade**); MC covariance layer = pure estimator noise (0.50 → exact form). Rung 2 (derived field into n3) is unblocked.

**n3-lite 4/4 — L3's first execution under the amended gate:** churn substrate (L = 16, N = 2048; the corner's 2-commit lineages can't serve as chains — first-execution substrate fix logged; corner-grade chains = n3-full's design item), Tier-A planted χ-field, record-native seed: closure 0.963 at Mc = 95, shuffle-void z = 14.4, supply-confound probe NULL. **Instrument finding: the l3-style spectral gate refuses on the noisy AND clean field alike (0.525/0.522) — the ratio reads exponential-kernel curvature, not latent dimension; RETIRED for kernel-affinity MDS** (n1's benchmark lesson one level deeper); closure + shuffle-null carry d̂ = 2.

**Round-3 review: OWED** (launched in background at close; findings = next session's first item). Round-4 queue: the interior-row multiplicity re-derivation (the forgery's cure — the round's sharpest target); ml1→n3 rung 2; the posit-2 regime map; n3-full (certificate-v2 + corner-grade chains); v9 paper 1 confirmation + paper 2 (the scale-run/forgery paper); k4-under-growth.

## 2026-07-05 (round 3) — REVIEW: 5 MAJORs, all repaired; SUPERSESSION

The round-3 referee reproduced all five receipts byte-exactly and confirmed both headline negatives (the forgery; the determinized death). 5 MAJORs, all claims-layer, all repaired same-date; **this entry supersedes the affected round-3 claims.**

**M1 — "THE COUPLING MUST BE LOCAL" RETRACTED (refuted by execution):** the referee ran the 2×2 cell my iteration ledger skipped — comparability graph + exact estimator closes at Procrustes **0.999** (off-seed 0.996–0.999): iteration-1's failure was the ESTIMATOR's (MC noise + |abs| + σ-modulation), not the graph's. The supported constraint: full-causal mediation carries geometry at ~7× smaller relative amplitude (rel-amp 1.0% vs 0.14%, now receipt-measured) — **locality buys per-realization estimability, not geometry-in-principle.** The cell is now an executed arm (ml1 5/5); the rung-3 design constraint is restated as amplitude/sampling-robustness. **M2:** ml1's gate provenance disclosed — PLAN T7.1's magnitude-structure gate (ii) was silently substituted; now deferred-with-reason to rung 2 (the toy's field is Gaussian by construction). **M3:** iterations 1–2's numbers marked [logged, unreceipted]; the load-bearing arm executed. **M4 — the A2@512 interior collapse carried into the narrative:** A2's "balanced interiors r_I ≈ 0.55" is the N=256 reading ONLY; at N=512 the receipt prints r_I = 0.353/0.198 (sub-half — the opposite failure), candidate cause the k ≤ 64 truncation. A2's bracket is scale-dependent, both faces now stated. **M5 — "the localization is exact" DEMOTED to hypothesis:** no receipt measured the charge decomposition; paper 11 §3.1's ledger granularity is "one constraint per RELATED pair" (per-interior-pair is my NEW proposed convention, now labeled as such); chain-heavy interiors already pay quadratic aggregate ~0.25-rows, so the argmin chose them DESPITE that — the mechanism needs the round-4 re-derivation before any "exact" is available.

Minors applied: gate-ledger lines added to s2 + n3-lite (the round-2 convention); t52's positive control ADDED (manufactured-consistent χ: 6/6 vs shuffle 0/6 — the dense 0/6 = 0/6 is the INFEASIBLE PHASE, not saturation; "requires coupling" softened to the supported direction; the o-sign and arm-C descriptions fixed); u5's omitted χ²-half disclosed; n3's "never rank-2" corrected to curvature-dependent (0.85→0.005 over ℓ = 0.15→50, review-measured; the retirement survives as "conflates kernel and dimension at unknown ℓ"); ml1's "ABOVE the benchmark" demoted to seed-level (cost −0.003; off-seed it lands both sides); LEDGER's dropped "9/10" qualifier noted. Receipt counts: ml1 = 5, t52 = 4 (supersede #42's).

---

## Round 4 — scope (2026-07-05, user go)

Slate (post-#43): **s3 — the forgery-mechanism diagnostic** (why the argmin forges DESPITE quadratic aggregate interior charge: H-menu [closure through the chainy web starves the menu of diffuse candidates] vs H-greedy [diffuse candidates offered but marginally dearer — per-pair-cheap × many-pairs beats per-pair-dear × few-pairs]; the cure is named by whichever holds; the reweighted/enriched bench = round 5); **ml2 — Tier-7 rung 2 (lite)**: the DERIVED Hasse-field χ_AB on a record-native web (no planted latents) — does it read out the web's own two-clock geometry (Procrustes vs the canonical embedding's chain positions; shuffle + supply-confound voids); **v9 paper 2** (the scale-run/forgery/G2-strict paper) + review. Deferred to round 5 (stated): the posit-2 regime map, n3-full, paper-1 confirmation pass, k4-under-growth.

## 2026-07-05 (round 4) — EXECUTED: s3 3/3 (H-GREEDY), ml2 3/3 (rung 2 delivered), v9 paper 2 written

**s3 — THE MECHANISM DECIDED: H-GREEDY at 79.6%.** Diffuse candidates available at 55.2% of late arrivals (H-MENU refuted — the closure does NOT starve the menu); where offered-and-skipped, the picked candidate carries fewer new pairs (|D| 73 vs menu 107): the myopia trap — the argmin never pays the aggregate. Cure named and corpus-legal: charge-rate selection Φ = ΔS/|D| (still in C4's class) and/or the multiplicity re-derivation; the reweighted bench = round 5. The #43-demoted hypothesis is now a measured mechanism.

**ml2 — TIER-7 RUNG 2 (lite) DELIVERED:** the derived Hasse-field χ_AB on a record-native web (NO planted structure) reads out the web's own two-clock geometry: Procrustes 0.891 @ Mc = 88, shuffle-z 18.6, relative amplitude 6.4% (the #43 estimability gate as a design gate, passed). The matter-field mechanism carries record-intrinsic geometry end-to-end derived; the 3+1 form = n3-full.

**v9 paper 2 written** (`relativistic-isp-v9-paper2-forgery-and-determinism.md`): the forgery + mechanism + G2-strict + Tier-7 rungs 1–2 + flanking results, with the retraction and gate ledgers carried. Review launching at close. Round-5 queue: the charge-rate bench (the cure test); the interior-multiplicity derivation; n3-full; posit-2 regime map; paper-1 confirmation; paper-2 reviews till pass.

## 2026-07-05 (round 4) — REVIEW: 7 MAJORs; SUPERSESSION of round-4 verdicts

All seven receipts reproduced byte-exactly; zero false numbers. The MAJORs, with **the demoted verdicts (authoritative, superseding this round's earlier entries and LEDGER #44's framing)**:

**s3 — "H-GREEDY decided" is SUPERSEDED by: "the menu is not starved at the faithful-band-edge cut (0.65 = note-s2's P3 upper edge — pre-registered lineage but the dichotomy is threshold-decided: at 0.55 the verdict FLIPS to H-MENU); the registered per-pair-charge clause holds at 71.2% (referee-computed — the receipt's 79.6% rode a signature whose second conjunct is VACUOUS under argmin); one configuration (n = 256 — a SMALLER scale than the forgery's); the myopia reading's causal content (locally-optimal-globally-suboptimal) is UNMEASURED (no lookahead/forced-diffuse arm) and is round 5's bench."** Referee bonus: the Φ = ΔS/|D| cure simulation picks diffuse at 44.3% vs argmin's 13.8% — the cure's direction is real; its picks' interiors barely move (0.679 vs 0.691) — the bench must measure endpoints, not picks. **M3 (favorable omission, the #41/#43 class AGAIN): the β = 0 control's OWN interiors are chain-heavy (r_I 0.73–0.79, m_ab ~10–14× faithful)** — the kernel builds them by default; the argmin worsens (0.85–0.88, 20×); the forgery signature proper (balanced global r WITH chain-heavy interiors) stays A1-specific; now narrated.

**ml2 — "rung 2 delivered" is SUPERSEDED by: "single-seed [directional] NON-WASHOUT: the field functor does not destroy the web's own geometry (0.891; off-seed 0.733/0.897/0.919 — the 0.85 pin REFUSES at 1/3 off-seeds); the graph metric ALONE closes at 0.867 (the field's measured margin +0.024); probe and target are functionals of the same committed (b,χ) — the M6 caveat binds HARDER here than in ml1 and is now stated; the registered supply-confound void was silently dropped (the n3-lite form is unsatisfiable-by-construction for the own-geometry target — itself the sharpest rung-2-lite scoping fact; the size-residual form PASSES, referee-computed: size-only surrogate 0.053, residual 0.889)."**

**M6 (green-count padding across four receipts):** honest gated counts s2 = 5, u5 = 3, s3 = 2, t52 = 3 (the literal-True verdict-recording lines are print-not-count per #36; t52's check-2 label contradicts its fallback branch). **M1: paper 2's §1–5 placeholder is not corpus form** (plus a dangling §4 pointer) — the body must be written or the artifact re-genred to a results memo; un-reviewable as a paper. Minors logged: the 73-vs-107 means are unconditional (conditional pair 57.6 vs 96.3, more favorable); "at the floor" → "above the floor" (u5); the χ²-half caveat; n3's ℓ-scan is review-measured prose; ml1's gate-(ii) deferral chain now points at n3-full; round-5 thresholds must be note-pinned BEFORE the bench (the round-4 docstring-only discipline is the named regression); Φ = ΔS/|D|'s empty-candidate/negative-increment wrinkles to state at registration.

**Round-5 slate (revised by the review):** (1) paper 2's body written (or re-genre) + all round-4 repairs applied to receipts (s3 signature re-spec + sensitivity row; ml2 graph-only + size-void arms + seeds; de-count padded checks) → reviews till pass; (2) THE CURE BENCH (charge-rate Φ), thresholds note-pinned first, measuring ENDPOINTS; (3) the control-interior baseline carried into the forgery narrative everywhere; (4) then the prior queue (n3-full, posit-2 map, paper-1 confirmation, Tier 4 opening).

## 2026-07-05 (round 4) — CLOSED after review: de-counting applied (s3 2/2, s2 5/5, u5 3/3, t52 3/3 — matching the referee's honest counts); paper 2 stamped DRAFT with the #45 supersessions; the s3/ml2 re-specs + paper body = round 5 item 1. LEDGER #44+#45; memory synced.
---

## Round 5 — scope (2026-07-05, user go)

(1) **THE CURE BENCH** (note-s4 pins all thresholds pre-run per #45): Φ_rate = ΔS/max(|D|,1) growth at the FORGERY'S OWN SCALE (n = 512), judged on ENDPOINTS (heredity r_I, m_ab) against the argmin arm AND the β = 0 control's own interiors (the M3 baseline). (2) Round-4 repairs: s3 re-spec (registered per-pair clause computed; sensitivity row; falsifiably-gated verdict), ml2 (graph-only baseline arm; size-residual void; +2 seeds; not-held-out clause; void-drop disclosed). (3) Paper 2's body written with the #45 demotions baked in → review. Deferred if context ends: n3-full, posit-2 map, Tier 4 (stated).

## 2026-07-05 (round 5) — s4 CURE BENCH: **CURE-REFUTED** (pinned verdict; gate ledger P1/P2 REFUSED, P3 HELD)

Φ_rate = ΔS/max(|D|,1) at the forgery's scale: global r locks at 0.500–0.501 (the rate rule centers comparability PERFECTLY — P3 held with a bonus) but the interiors stay forged (r_I 0.757–0.820 vs argmin's 0.831–0.905; m_ab 1.71 vs 1.87 = 0.92×, vs the pinned 0.5×). **The M3 baseline completes the picture: the kernel's own random picks default to chain-heavy interiors (ctrl r_I 0.733–0.810, m_ab ~12–16×) — NO tested selection rule (uniform, argmin, rate) moves interiors below the kernel's floor.** Interpretation (superseding s3's myopia reading, which round 4's review had already demoted): **the forgery is KERNEL-CLASS-LEVEL** — candidate down-sets are transitive closures through an increasingly chainy web, so every candidate carries chain-heavy interiors; the big intervals' interiors are locked early and irreversible growth can never rewrite them. Selection-rule cures are exhausted; the cure space narrows to (a) kernel enrichment (a different candidate generator) or (b) the possibility that this kernel class CANNOT EXPRESS faithful webs at all. **Round 6's decisive diagnostic, named: the faithful-guided control** — grow with candidates drawn from an actual sprinkling's own down-sets (the kernel-support oracle): if even that stays forged, the class is expressively closed against faithfulness and the growth program needs a new move class; if it un-forges, kernel enrichment is the road. Either way the mechanism question resolves at the class level. Both directions live; this is the program's sharpest current fork.

## 2026-07-05 (round 5) — repairs + paper 2 body COMPLETE

s3 re-specced (2/2): the registered per-pair clause computed (71.2% — exactly the referee's number), the vacuous conjunct deleted, the sensitivity row printed (verdict cut-dependent, disclosed), the verdict falsifiably gated, and the kernel-class supersession cross-referenced. ml2 re-specced (5/5): the not-held-out clause + non-washout scoping in the docstring; G4 graph-only baseline added (0.704 on this web — the field's margin +0.19, seed-variable); G5 size-residual void added (size-only 0.048 vs residual 0.890 — clean); [directional, multi-seed] carried; the void-drop and its unsatisfiability-fact disclosed. **v9 paper 2's §1–6 body WRITTEN** with the #45 demotions and the round-5 results integrated (the cure refutation as §2's climax; the expressibility fork as §6's open). Review round 2 launching at close.

## 2026-07-05 (round 5) — paper 2 round-2 review APPLIED (MAJOR-REVISION, narrow): both MAJORs were un-revised round-4 SURFACES (the Receipts section's stale counts + resurrected "H-GREEDY at the pre-registered thresholds" + missing s4; abstract item 2's un-demoted 79.6%/"myopia trap" passage) — body verified receipt-true and registration-faithful throughout, zero false numbers. All fixes applied: Receipts rewritten at honest counts with s4's exit-1-by-design stated; abstract item 2 rewritten to the 71.2% registered clause + supported-not-decided + threshold caveat; kernel-default qualifier into item 1; "at the floor"→above; 0.500→0.500–0.501; control ranges attributed to s2+s4 pooled; 2–13σ tagged review-measured; margin quoted as range (+0.01…+0.19); note-s4 + LEDGER #42–#46 cited; §6 tag synced; note-s4's "none⇒"→"otherwise⇒"; **ml2's named seeds now LOOPED in-receipt (spread 0.919/0.743 — reproducing the referee's faithful re-run exactly; 5/5)**. OWED: one light round-3 confirmation pass over the abstract/Receipts diff (next session's first item), then paper 2 is terminal-grade.

## 2026-07-05 (round 5, addendum) — user catch + a silent-fix failure found and repaired

(1) **The round-2 M2 abstract fix had SILENTLY FAILED** (replacement string mismatch — "offered and skipped" vs "skipped"; the edit was a no-op with no assert): the superseded 79.6%/"myopia trap" passage was still in the abstract. Now actually applied, WITH asserts (the standing lesson: text edits assert their targets — a no-op replace is the silent-fix class). This is precisely what the owed confirmation pass exists to catch; the user's question caught it first. (2) **The division of labor elevated to a named result** (user catch: it was buried as "(a bonus: ...)"): stated once in §2 with grade and scope — **[DEMONSTRATED; one kernel/action family, three selection rules, n = 512]: the global comparability layer is selection-controllable (rate centers r at 0.500–0.501 every rep) while the interior layer is selection-invariant (all three rules within ±0.07 of the kernel floor) — global structure is chosen, fine structure is inherited from the move class** — with the paper-4 resonance (order/count division) noted and the consequence drawn (the cure space collapses to the kernel fork, not further action design). Abstract + §6 carry one-clause echoes. The owed confirmation pass now also covers these edits.

(Addendum to the addendum: the first edit script died at an assert BEFORE its write, silently losing edits 1–2 — caught by the post-write verification probe, re-applied, all four now file-verified with the superseded passages confirmed absent. Two consecutive silent-edit incidents in one pass: the standing rule is now assert-targets AND verify-file-after-write, both.)
---

## Round 6 — scope (2026-07-05, user go)

(1) Paper-2 confirmation pass (launched at open). (2) **s5 — the faithful-guided control, with the round-5 framing REFINED (logged as a correction):** "expressively closed" was too coarse — the attachment class trivially expresses every order (arrival-order + true down-sets rebuilds any web; the oracle arm anchors this). The operative fork: **Q1 (action-preference)** — along the faithful trajectory, does A1's marginal landscape prefer the TRUE down-set over kernel decoys when both are offered? TRUTH-REJECTED = no kernel can save argmin-A1 (the deep negative; redirect = action side / churn-class moves); TRUTH-PREFERRED = the forgery is purely the kernel's offer distribution (enrichment road, with **Q2 (the offer gap** — Jaccard of best decoy vs truth**)** as the spec). (3) Then: Tier-4 opening (Theorem U + u4 census design) and/or the rung-3 note, as context allows.

## 2026-07-05 (round 6) — s5 THE FAITHFUL-GUIDED CONTROL: **TRUTH-REJECTED at 3.9%** (gate ledger: G-O held, G1 = TRUTH-REJECTED, G2 REFUSED — exits 1 by design, the s4 convention)

**The fork resolves in the deep direction.** G-O: the attachment class trivially expresses faithfulness (oracle rebuild bit-exact; targets r_I = 0.510, m_ab = 0.136) — the round-5 "expressively closed" framing was refined pre-run (note-s5) and the refinement mattered. **G1: along the faithful trajectory with the TRUE down-set on every menu, A1's argmin picks it 2.1–7.3% of the time (mean 3.9%, pinned TRUTH-REJECTED threshold 30%).** G2 (consistently): free-run guided endpoints stay fully forged (r_I 0.80–0.92, 0/4 in-band; m_ab 1.28 vs 3×-faithful 0.25; truth-taken 5–7%). Offer gap (Jaccard best-decoy-vs-truth) = 0.553 — moot given G1.

**The mechanism, now exact:** the true ancestor-set at arrival t is large (~0.4t) and its new pairs pay full r_I/link/S_comp increments; small decoys always undercut it marginally. **Faithfulness is globally cheap but locally dear — and any marginal-increment-driven selection (argmin, rate, finite-β Gibbs over increments) therefore cannot follow it.** This kills the "growth by derived-action increment-optimization" line as a production mechanism — no kernel enrichment, no selection rule, no β can fix a landscape that penalizes faithful steps. It also retro-explains s1's n = 64 success (interiors below m₀ → charges small → S_comp dominated) and the s2 forgery's onset with scale.

**The redirects, ranked:** (1) **the increment-accounting re-derivation** — the interior-multiplicity question now has an exact target: find the per-arrival charge normalization under which the faithful trajectory IS an argmin path (the s5 instrumentation gives the charge profiles along truth-vs-decoy to derive against — a well-posed inverse problem, round 7's centerpiece); (2) **the churn class** — the placement mechanism that already passes the cell tests does not build by increment optimization at all; its promotion to THE placement law is now the default posture (Tier-4's growth-measure theorems become its theory home); (3) path-level (non-marginal) dynamics are out of note-8's class C4 as written — a class extension would need its own design note. **Round-7 queue:** the inverse-derivation (1); Tier-4 opening; the rung-3 note; paper 2 addendum-or-paper-3 for s5; n3-full; posit-2 map.

## 2026-07-05 (round 6) — paper-2 confirmation pass: 2 items failed, all repaired (file-verified)

Items 1/2/4/5 PASS (all round-2 fixes + division-of-labor structure confirmed; every number receipt-exact). FAILED + repaired with asserted edits + post-write verification: (a) the ±0.07 floor bracket — MY round-5-addendum numeral, refuted by the receipts (argmin 0.905 = +0.10 above the floor band) — replaced by the receipt-true "at-or-above the kernel's floor (pooled r_I 0.73–0.91), never below it"; (b) the abstract's stale round-4 successor slate ("the charge-rate bench" — this paper's own executed-and-REFUSED s4!) — replaced by the actual slate incl. the faithful-guided control; (c) "rounds 3–4" → "3–5"; (d) ml2's spread resynced to the in-receipt loop (0.743–0.919, refusal 1/2) with the review triple attributed. **Paper 2 is now TERMINAL-grade** (2 full hostile rounds + confirmation; zero false measured numbers at every round). NOTE for the s5 record: this paper's slate sentence is as-of-rounds-3–5; s5's TRUTH-REJECTED result (LEDGER #48) supersedes the fork's framing and belongs to paper 3 / the s5 addendum, not retrofitted here.

---

## Round 7 — scope (2026-07-05, user go)

(1) **s6 — the re-pricing test** (note-s6, gates pinned pre-run): the inverse problem's derivation-shaped candidate — charge EXCESS over the faithful expectation, i.e. the centered action. Arm A: r-term centered by the exact `Var(r_k)` (paper-1 closed form). Arm B: the **allowance form** `ΔS̃ = ΔS − c̄_t·|D|` (each new pair carries a subsidy at the faithful per-pair rate `c̄_t`, empirically calibrated from oracle profiles this round; the closed-form derivation of `c̄_t` from the exact laws = the accompanying theory task). Wall arithmetic pre-checked in-note (the allowance preserves the walls: link-heavy classes pay `(¼ − c̄)·Ω(n²)`; antichain pays S_comp; dense pays the excess). Measured: the s5 marginal-preference rate per arm; then free-run endpoints under the best arm. (2) u4 census + rung-3 design notes if context remains; paper 3 and the rest = round 8 (logged).


### Round 7 — results (2026-07-05)

**s6 (the re-pricing test) — REJECTED on both arms; the increment-optimization production route is closed.**
- `c̄_t` calibrated at **0.1034** (arrival-level faithful mean per-pair charge, 4 oracle profiles; < ¼, wall arithmetic preserved as pre-checked in-note).
- **G1**: arm A (Var-centered) **6.1%**, arm B (allowance) **3.6%** vs pinned lines 0.6/0.3 — both **REJECTED**. Raw baseline was 3.9% (s5). The anti-faithful marginal landscape is robust to the centering family: charging excess-over-faithful-expectation does not restore truth-preference.
- **G2** (arm A, pure kernel menus): **REFUSED** — but the overshoot guard *held* (r 0.453–0.468 in-window 4/4) while interiors stayed at the kernel floor (r_I 0.811–0.882, m_ab 5.0× faithful). **Third independent confirmation of division-of-labor** (after s4-Φ and the kernel-floor analysis), now under a centered action.
- Verdict: TRUTH-REJECTED now measured at three actions (raw 3.9%, centered 6.1%, allowance 3.6%). The closed-form c̄_t derivation is withdrawn as a priority (rejection is uniform over fixed arrival-level profiles). Production mechanism = churn class, unchallenged; the action's standing = grader.
- Receipt `v9/code/s6_repricing_test.py`: G1 branch-check PASS, G2 FAIL-by-design, gate ledger printed, exits 1 by design. Note-s6 addendum carries the deviations block (c̄_t smoothing disclosed) and the [directional] tag on arm A vs raw.
- Fix during build: s6's first run crashed at the G2 bench (`heredity_axis2`/`sampled_mab2` sat below the s5 extraction cut); helpers ported verbatim, rerun reproduced G1 bit-identically (seeded).

Next in-round: hostile review of the round-6/7 arc (s5 + s6 + notes — s5 has had no external review yet), then u4 census + rung-3 design notes if context remains. Paper 3 (the landscape/re-pricing arc) = round 8.

### Round 7 — secondary items delivered (2026-07-05)

- **note-u4-census.md written** (Tier 4 opening, T4.1): the exact n ≤ 7 induced-measure census design, re-scoped by rounds 5–7's law-list collapse — primary arms = the churn class (trivial-Φ / D1 / D1+atoms; the argmin family demoted to one labeled pathology-control column). The three owed items from the u-line are all in-plan: the V-lab arm + e(P) multiplicity accounting (design note 8 C6's pre-registration breach, repaired by design), and the frontier-Markov ablation. Gates pinned: G-A support (100% dim ≤ 2, Theorem U's census half), G-B bulk share (< 0.2405 success / ≥ 0.3424 failure — paper 7's goalposts), G-C covariance-fork TV (> 0.1 promotes T4.2), G-D g3 identity, G-E Markov-ablation measurement. Exact arithmetic in the measure accounting (no float64).
- **note-ml3-two-party-locus.md written** (Tier 7 rung 3, T7.3): cheap-first split — ml3a = the maximum-caliber route (χ_AB as unique least-bias joint law under the DERIVED constraints only: marginals, connectivity floor + manifoldlikeness obligation, the rung-1/2 amplitude band, the fence Γ ⪰ 0; three-branch verdict pinned incl. the quantum-point comparison at G3; mpmath dps ≥ 80 since the objective is known-flat in the entangling direction) → ml3b = the two-species interacting GW flow (paper 5's mechanism extended cross-species; own note only after ml3a's branch is known). No-go boundary respected by construction: grade ceiling [DERIVED given the matter sector]; the import ledger style adopted from paper 5.

Round 7 primary (s6) closed above; hostile review of the s5+s6 arc in flight (two independent referees: code audit + science/registration audit). Round 8 queue unchanged: paper 3 (the landscape/re-pricing arc), review-findings application, n3-full, posit-2 regime map, paper-1 confirmation pass, u4 execution, ml3a execution.

### Round 7 — the s5+s6 hostile review, applied (2026-07-05; two independent referees, code + science; LEDGER #50)

**Both referees reproduced every committed number and confirmed all pinned verdicts (~20σ-equivalent rep-level margins vs the 30% line); both found the same three MAJORs in the interpretive layer. All applied; the science is now stronger and one mechanism sentence of round 6 is SUPERSEDED.**

- **SUPERSESSION (the sharpest item — MAJOR at both referees, confirmed by my own independent replica, then receipted on the committed stream by the new `s6b_mechanism_family.py`, 3/3 with stream-fidelity proven):** round 6's "the mechanism, now exact: … small decoys always undercut it marginally" (and LEDGER #48's "skimpy decoys undercut") is REFUTED — that sentence was unreceipted (the #45 class). Committed-stream facts (s6b M1): the winner is LARGER than truth at 93.1% of losses (menu-max 73.7%); truth is Row-A-CHEAPER at 93.0%; Row C's block increment carries the decision at 93.8% (mean gaps: Row A −6.2 in truth's favor, Row C +16.7 against). Cause: the faithful prefix under the registered u+v order runs at comparability ≈ 0.33 vs Row C's equilibrium target ½ (hit only at completion: 0.327 → 0.383 → 0.504), so the transient gradient 2(r_t − ½) ≈ −0.3 rewards max-|D| moves throughout growth. **TRUTH-REJECTED stands; the mechanism is a transient-vs-equilibrium mismatch (increments of an endpoint functional ≠ a growth law). Both s6 arms re-priced the pair term — the NON-binding term — so round 7 could not have rescued production even in principle; the "B ≥ A" registered expectation's refutation (A 6.1% > B 3.9%) is retro-explained (the |D|-linear allowance subsidizes exactly the decoys Row C already favors).**
- **The chance line (science MAJOR, previously unstated anywhere):** 14 candidates ⇒ null 7.1–7.7%. Raw (t = −2.76) and arm B (t = −3.06) sit below chance — anti-faithful supported; **arm A (6.1%, t = −1.42) is chance-consistent — licensed reading "no truth-preference", not "anti-faithful"** (LEDGER #49's "anti-faithful … robust to the centering family" is corrected accordingly: the ABSENCE of truth-preference is what is robust).
- **The withdrawal re-justified by measurement (both MAJORs → s6b M2–M4):** allowance-family curve monotone-decreasing on the committed stream (3.9% → 0.4% across c = 0…0.24); 90.4% of steps dominated (a decoy larger AND raw-cheaper exists ⇒ truth loses at every c ≥ 0); **per-step-tuned oracle over c(t) ∈ [0, ¼) caps at 7.7% ≈ chance.** No admissible allowance profile — fixed, derived, or per-step-tuned — approaches the 30% line. **The surviving derivation-shaped opening: re-price the GLOBAL term (arm C: subtract the faithful prefix expectation from Row C's target along the trajectory) or trajectory-level pricing — round 8's candidate; paper 3 inherits the redirect.**
- **Favorable-direction transcription caught (science F3):** "m_ab 5.0× faithful" conflated the 3× ceiling with the base — receipt-true: **1.380 ≈ 15× faithful** (5.0× the pinned ceiling), unchanged from s5's 1.28 ≈ 15× — the centered action does not improve interiors at all. Corrected in note-s6 C4; #49's clause superseded by #50.
- **Wording weakened (science F5/F6):** "third independent confirmation of division-of-labor" → a further INSTANCE (fourth selection rule/second action form, same kernel/menu class; independence across move classes untested); "regulates as Φ did" → default landing in the raw argmin's own band (0.43–0.48) — s4's Φ (0.500–0.501) remains the only centering-grade control.
- **Receipt repairs, re-run green/identical:** s5 + the pinned G3 per-decile print now delivered (0.653…0.382, endpoint 0.641) + dead code out + float pledge honest (0/2304 decision flips verified); s6 edge-normalized smoothing (arm B 3.6% → **3.9%**; c̄ mean 0.1038, max 0.1781 < ¼ — the wall check now max-form; sd 0.0410 now printed), dead code out. note-s5 results addendum written (was owed); note-s6 gains the six-item Review-corrections section (C1–C6).
- Registration hygiene: "same 4-sprinkling trajectories" → fresh-per-arm, seed-pinned; comparisons unpaired/seed-confounded, [directional] as tagged.

**Verdict ledger after review: G1 arm A REJECTED (chance-consistent), arm B REJECTED (below chance), G2 REFUSED — all pinned verdicts intact; the round's conclusions rewritten around the Row-C attribution. Round-8 queue gains: the arm-C/trajectory-pricing investigation (the review's opening) ahead of, and feeding, paper 3.**

### Round 7 — CLOSED (2026-07-05): confirmation pass CLEAN

Third referee (confirmation, fix-diffs only): **CONFIRMATION-PASS** — all receipts re-run (s6b 3/3 exit 0; s5/s6 exit-1-by-design with matching ledgers); every number in note-s6 C1–C6, note-s5's addendum, the LOG review entry, and LEDGER #50 matches the committed outputs; s6b's I1 verified genuinely probative (per-rep win counts 4/7/14/5 of 192 require the exact decision sequence); the M3 "every c ≥ 0" claim verified exactly (0/768 dominated steps tie-only); the M4 oracle-bounds-every-profile argument verified airtight (any profile assigns one c per step ⇒ the per-step oracle upper-bounds it); supersessions weaken-not-strengthen, nothing unfavorable dropped. Two nuance notes, no fix required (the decile gloss compresses a mildly non-monotone printed vector; the gradient crosses zero exactly at the final arrival — disclosed). **Round 7 terminal: s6 + s6b + both note addenda + LEDGER #49/#50. Round 8 opens with: arm C's design note (gates pinned before any run), then paper 3.**

## 2026-07-05 (round 8) — OPENED at user go-ahead

**Scope:** (1) **s7 — the stage-priced growth test ("arm C", note-s7, gates pinned pre-run):** the review's surviving derivation-shaped candidate — replace Row C's endpoint target ½ with the faithful prefix expectation ρ(t) along growth (the endpoint functional is untouched: ρ(N) → ½, so the GRADER and its walls are literally unchanged; arm C re-prices only the path). ρ(t) calibrated from 4 oracle profiles (edge-normalized smoothing per the s6 lesson) with the flat regime DERIVED exactly in-note (two uniform points in a sub-diagonal triangle prefix: P(comparable) = 1/3 — so the transient gradient's early floor is 2(1/3 − ½) = −1/3 exact, matching s6b's measured 0.327/0.327). G1 same instrumentation as s5/s6 (14-candidate menus, forced-faithful, t ≥ 64, 4 reps, tie-generous), thresholds 0.6/0.3, **chance line 7.1–7.7% pre-registered this time**; the s6b-class mechanism decomposition runs IN-receipt (expected signature, registered: the Row-C dominance collapses). G2 production bench runs whatever G1's branch (s6 precedent). Seed 20260715. (2) **Paper 3** (the landscape arc: s5 + s6 + s6b + s7, the transient-vs-equilibrium mechanism as centerpiece) after s7's verdict + review — likely the next session if context runs out. Deferred (stated): u4 execution, ml3a execution, n3-full, posit-2 map, paper-1 confirmation.

### Round 8 — results (2026-07-05): s7 REJECTED with the registered signature — THE PINCER; the re-pricing program is closed at both terms

**Gate ledger (`s7_stage_priced.py`, seed 20260715, exits 1 by design): C-rho HELD; G1 = REJECTED; D1 COLLAPSED; G2 REFUSED.**

- **C-rho held:** calibrated flat-regime ρ = 0.3434 vs the in-note DERIVED exact ⅓ (sub-diagonal triangle: P(comparable) = 2·B(2,3)/(2·¼) = ⅓; note-s7 §2); endpoint ρ(N) = 0.4904 vs ½. The stage profile is the derived object.
- **G1 REJECTED at 1.3%** (reps 0.005/0.031/0.010/0.005) — **worse than raw (3.9%), and deeply below the pre-registered chance line** (null 7.1–7.7%; rep-level t = −9.42, the strongest below-chance reading of the campaign).
- **D1 — the registered mechanism signature CONFIRMED, and it exposes the pincer:** the Row-C dominance collapsed exactly as designed (93.8% → 5.7%; winner-larger 93.1% → **2.1%**) — the transient is neutralized — and the selection flipped to Row A: the Row-A gap reverses from −6.2 (truth cheaper, raw action) to **+6.2 (truth dearer, stage-priced)**; the winners are now SKIMPY candidates that pay ≈ 0 on Row A while their under-building costs only the two-sided stage quadratic (≈ Δ²/n_pair near the profile — vanishing per pair). **Both round-6 mechanism stories were real forces, misassigned as one: the endpoint-target transient favors BIG decoys; the per-pair charges favor SKIMPY ones. The raw action's 3.9% was their partial cancellation; removing either exposes the other. Truth is dominated from both sides — no admissible increment functional in this class prefers it.** Scaling note (derivable, flagged for paper 3 §5, not a rescue): the skimpy side's Row-C penalty is quadratic-only around the profile while truth's Row-A cost stays linear-in-pairs × a positive floor (¼ sub-m₀ + the Var floor), so the pincer *strengthens* with n.
- **G2 REFUSED** (r 0.430–0.447 in-window; r_I 0.842–0.905 at the kernel floor; m_ab 11.5× faithful) — endpoints forged as at every prior arm.
- **Per note-s7 §4's pinned kill branch (G1 REJECTED ∧ D1-collapse): the re-pricing program is CLOSED AT BOTH TERMS.** Increment-optimization is closed at four actions (raw #48, Var-centered + allowance #49, stage-priced this entry) with the mechanism receipted at each step (s6b + s7-D1). Division of labor is final at this move class: **the action grades; churn builds.** Trajectory-level pricing = a C4 class extension, out of scope by the note's own pin. **Paper 3's ending is fixed.**

Next in-round: hostile review of s7 (+ note-s7), then paper 3. Assembly-script note: the def-count verification regex died AFTER the file write (its own bug); verification re-run independently — single defs confirmed, receipt valid.

### Round 8 — the s7 hostile review, applied (2026-07-05; LEDGER #52)

**Referee: receipt SOUND-WITH-REPAIRS; note/LOG/LEDGER PASS-WITH-REPAIRS; zero false measured numbers; bit-identical re-run; the §2 derivation verified exact (mpmath quad + MC); indexing audited no-off-by-one; machinery AST-identical to s5/s6/s6b. THE PINCER CONFIRMED AND REINFORCED by the referee's paired C* control** (target = each rep's OWN realized faithful path — profile mismatch removed exactly): rate **0.26%** (dominance 0.0%, winner-larger 0.0%), and the loss-winner anatomy sharpened: **the empty set wins 97.0% of losses** (A_winner median exactly 0 vs A_truth mean 6.378; winner |D| median 0 vs truth's 80.1) — arm C's argmin prefers adding NOTHING to a faithful arrival; the committed 1.3% was generous to truth (the calibration residual, mean r−ρ = −0.023, net-helped it). Truth's wins are 9 strict + 1 degenerate tie — not tie artifacts.

**Corrections applied (supersede the round-8 results entry and LEDGER #51 where they conflict):**
- **MAJOR-1, the scaling sketch:** #51's "truth's Row-A cost stays linear-in-pairs × a positive floor (¼ sub-m₀ + Var floor)" is WRONG — there is no positive per-pair floor: the ¼-payers thin as Θ(log m) (measured 0.080/element at n = 256, falling; referee's structural count ≈ matches A_truth 6.4) and qualifying pairs pay Var(r_k) → 0, so **A_truth = Θ(polylog m), NOT Θ(m). The conclusion survives on the corrected structure** — A_truth grows without bound while the skimpy under-build quadratic stays Θ(1) (≈ 2ρ² flat-regime; measured +2.18) — "the pincer strengthens with n" stands, but paper 3 must inherit the referee's derivation, not the superseded sketch.
- **MAJOR-2, the [directional] pin restored:** "worse than raw (3.9%)" is cross-seed/unpaired (Welch t = 1.96, p ≈ 0.11) — tagged [directional] and demoted from load-bearing. **The "partial cancellation" claim now rides the paired within-stream evidence** (s6b raw: Row C binds, winners big, Row A −6.2 FOR truth; s7: Row A binds, winners empty, Row C −2.1 mildly for truth; C* exact-centering: 0.26%) — established without the seed-confounded rate comparison.
- **MINOR dispositions:** winner-anatomy sentences = review-measured (receipt print block owed, the #45 class — this time verified TRUE before repair); left-edge smoothing NOT normalized (0.5 fillers leak into rho_s[2..8], +0.125…+0.036 — no reported number touched, G1/C-ρ start at t = 64, G2's t = 1 pick verified non-flipping; fix owed); C-ρ pointwise-vs-mean ambiguity resolved-in-passing-direction (mean-form implemented; disclosed here; gate measured weak: ~21% false-refusal population rate — informational, held at z ≈ +0.5); D1's second clause ungated — the Row-C gap −2.124 = the calibration-residual imprint (RMS r−ρ = 0.033), favors truth ⇒ rejection conservative, "transient neutralized" now carries the residual.
- **NIT dispositions:** "endpoint functional literally unchanged" → "unchanged up to the printed calibration error (0.4904 vs ½; walls robust)"; dead `s_comp` in s7 (strip owed); float pledge → "carried from the round-7 review streams"; t = −9.42 is df = 3 (p ≈ 1.3e-3; pooled ~6σ) — cross-arm comparison receipt-true as same-statistic.

**OWED (next session, before paper 3):** s7 receipt repairs (winner-anatomy print block; left-edge normalization fix + rerun — referee pre-verified endpoint insensitivity; dead s_comp out; docstring pledge wording) + note-s7 wording (C-ρ mean-form; "literally" → qualified). **Then PAPER 3** (s5 + s6 + s6b + s7 + both reviews; the pincer as centerpiece with C*'s 0.26%/empty-97% anatomy; the corrected polylog scaling; ending per note-s7 §4's pinned branch: the action grades, churn builds).

## 2026-07-05 (round 9) — OPENED at user go-ahead: s7 repairs applied; paper 3 next

**s7 repairs (LEDGER #52's owed list) applied + re-run:** left-edge smoothing normalized (fillers excluded from the numerator); dead `s_comp` stripped; float pledge "carried from"; **the D1 winner-anatomy print block delivered in-receipt** — empty-set winner 97.0% of losses, A_winner mean 0.164/median 0.000 vs A_truth 6.401, winner |D| mean 1.6/median 0 vs truth 80.5, gapA > 0 at 98.2% (the receipt now owns the pincer's numbers; supersedes the review-measured attribution). Note-s7 wordings applied (C-ρ mean-form disclosure + weak-gate caveat; "literally unchanged" → up-to-printed-0.4904). Re-run: **C-ρ/G1/D1 bit-identical** (the edge fix touches only t ≤ 8); **G2 endpoints shifted as the review predicted** (early free-run picks differ): r 0.434–0.442 (in-window 4/4), r_I 0.866–0.895 (0/4 in-band), m_ab mean 1.5900 = **15.0× faithful** (was 11.5× — the committed record is now the repaired run). **Gate ledger identical: C-ρ HELD / G1 REJECTED / D1 COLLAPSED / G2 REFUSED; exit 1 by design.**

**Round-9 scope:** paper 3 (the growth-landscape arc: s5 + s6 + s6b + s7 + the three review campaigns, single-threaded per the house style with a deviations block; the pincer as centerpiece; the corrected polylog scaling; ending per note-s7 §4's pinned branch) → hostile reviews till pass → confirmation. Then, as context allows: u4 execution, ml3a execution, n3-full, posit-2 map, paper-1 confirmation.

### Round 9 — s8 results (2026-07-05): the joint arm REJECTED at the derived point but the two-term family has a LIVE WINDOW below the preference line; CHURN'S INTERIORS CERTIFIED

**Gate ledger (`s8_paper3_repairs.py`, seed 20260716, exits 1 by design, 7/9): G-A1 = REJECTED; A3 oracle ≥ 0.3 (LIVE WINDOW, the pinned surviving-arm branch); G-A2 REFUSED; Part B churn PASSED both N.**

- **Part A (the fully-centered joint arm, review MAJOR-1):** calibrations in-run (ρ flat 0.3159 held vs ⅓; c̄ = 0.1115). The joint family curve is **humped** (0.1% at c = 0 → 8.1% → **12.9% at c̄** → 10.7% → 7.3% → 9.6%): the review's registered prediction held (the allowance rescues truth from the empty set) and the joint arm **triples the raw action's rate — yet stays REJECTED** (12.9%, reps 0.0–22.4% [huge heterogeneity, disclosed], t vs chance +1.23 n.s.). Staged-base dominance nearly vanishes (7.4% vs the raw base's 90.4%). **A3, the two-term family's per-step oracle: 46.4%** — above the 30% rejection line (⇒ per the pinned branch: NO blanket class-rejection; a live seam exists between the pincer's jaws) — **but below the 60% preference line: no admissible two-term increment profile, fixed or per-step-tuned, can make truth PREFERRED** (a measured corollary of the oracle bound; the best constant-c member achieves 12.9% against the 46.4% ceiling, so the window demands per-step c variation no single profile provides — the profile question is a named open). **G-A2:** free-run endpoints at c̄ stay forged (r 0.491–0.503 — note: at the joint action the global rate centers near ½; r_I 0.751–0.900, 0/4 in-band; m_ab 10.1×) — production fails at the bench regardless.
- **Part B (churn's interiors, review MAJOR-2): CERTIFIED, in the strongest form.** The j3 corner (M = 32, L = 2, verbatim u1 port; order = dominance on (b, χ)): **N = 256: r_I 0.500–0.573 (4/4 in-band), m_ab 0.9× faithful; N = 512: r_I 0.496–0.511 (4/4), m_ab 0.8×** — the builder's interiors are indistinguishable from sprinklings at the arc's own instruments (which measured every action arm at 10–20×). The uniform-victim kernel variant (INFO, ungated): also in-band (0.526–0.557). **"The action grades, churn builds" is now receipted on both halves** [DEMONSTRATED, n ≤ 512, j3 corner + kernel variant].
- Paper 3's revision brief (from the two round-1 reports + s8): part (5) rewritten to the receipted form (five actions REJECTED; the family closed against PREFERENCE at the 46.4% < 60% oracle; the live MIXED window + profile question as named opens; churn graded); the scaling sentence rescoped to the single-lever arms (the review's corollary: the joint member's required c* falls with n — the class-wide "scale is not a rescue" was unlicensed and is withdrawn); all referee-1 attribution/count fixes (five rules/three forms; 0/768 and RMS 0.033 review-attributed; the ~4% convention; 15× → 10–20× at 512; "directionally below chance"; the §6 item-8 rewrite).

### Round 9 — CLOSED (2026-07-05): PAPER 3 TERMINAL

Round-2 review returned **PASS-WITH-MINOR** (zero MAJORs): the referee re-ran s8 (bit-identical, 2/9 refusals = the pinned branches), diffed web_j3 against u1 (true verbatim port, identical rng call sequence), verified the note→code→paper mtime ordering, hand-verified the ⅓ integral and the Hoeffding variance, and certified the two load-bearing logic steps — the preference-closure argument AIRTIGHT (any profile, even history-adaptive, realizes one c per step ⇒ oracle 46.4% bounds all, ~7σ below 60%) and the pinned surviving-arm branch honored exactly (no residual blanket-closure sentence). The churn certificate ruled LICENSED with no caveat owed (band-level credentials leave interval structure slack by ±O(D*·N); "global faithful, interiors forged" is a live outcome the corpus itself exhibits — the corner measured 0.8–0.9×). Three MINOR one-line fixes applied (the deviation-9 honesty record now names #49's ledger slip and round 1's third MAJOR; §4.3's registered-prediction sentence rescoped with [review-arithmetic] attribution — the large-decoy clause was the pinned open question, not a prediction; §4.2's "settled in the opposite direction" → "cut off, not settled — no claim in either direction") + all three NITs (C-ρ in the §6 enumeration; the c = 0 anchor expectation 1–2% vs measured 0.1% kept per convention; the churn bracket marks the variant INFO).

**PAPER 3 IS TERMINAL-GRADE** (1 dual-referee round + s8 + 1 round-2 pass; zero false measured numbers at any stage). **Round-10 queue: u4 census execution (the builder's ensemble test — now the program's sharpest open); ml3a execution; n3-full; posit-2 regime map; paper-1 confirmation pass; the joint-family profile question and the pincer-scaling receipt as named opens feeding future rounds.**

## 2026-07-05 (round 10) — OPENED at user go-ahead: u4 census execution begins

**Scope:** the builder's ensemble test (note-u4, gates G-A..G-E pinned round 7). Execution is staged: **(1) this session — the exact-law reduction + its validation receipt (u4a):** the census's foundation stone, derived into a note-u4 addendum — the churn web's value ranking follows a sequential RACE MODEL (each slot-life's χ values = arrival prefixes of independent unit-rate Poisson processes; memorylessness ⇒ at each rank position, the next value belongs to each life with pending arrivals with equal probability), so the induced unlabeled-order measure is EXACTLY computable in rational arithmetic: P(web) = Σ over (slot-partition × reset-pattern) DP [exact rationals, M = 32, L = 2 factors] × race-model interleaving probabilities [products of 1/j]. Corollary A (Theorem U support-leg, by construction): every churn web is the dominance order of (commit rank, value rank) — an explicit 2-realizer — so dim ≤ 2 with NO enumeration needed; G-A becomes a per-class verification of the construction. Corollary B (consistency anchor): the all-singleton-lives limit gives the uniform random ranking = the 2D random box order exactly. u4a validates the reduction: exact n ≤ 4 induced measures vs large-sample Monte Carlo of the actual j3 process. **(2) next session — the full n ≤ 7 run** (the paper-7 KR classifier reproduced from its §4 definition; G-B against 0.2405/0.3424; the D1 / D1+atoms arms; G-C/G-E columns; hostile review). Deferred in-queue: ml3a, n3-full, posit-2 map, paper-1 confirmation.

### Round 10 — u4a results (2026-07-05): the exact-law reduction VALIDATED (8/8, exit 0)

- **V1 (Corollary B, exact):** the L = 1 anchor equals the uniform-ranking (2D box) measure **Fraction-identically** at n = 3/4/5.
- **V2 (exact):** the enumerated measure sums to exactly 1 at every n — and the reached-class counts are **5/16/63 = ALL unlabeled posets** at n = 3/4/5: the corner's support is complete below the dimension threshold (the smallest dim-3 poset has 6 elements — so G-A's content begins exactly at n = 6, as Theorem U's support leg predicts; a clean structural cross-check, noted for the census).
- **V3 (the actual generator):** s8-ported web_j3 (M = 32, L = 2), 200k webs per n — every reached class matches the exact reduction (worst |z| = 0.84 at n = 3, 1.37 at n = 4; TV 0.0013/0.0029).
- **V4 (Corollary A):** the explicit 2-realizer asserted on every enumerated web.

The n ≤ 7 census now runs on a validated exact core. **Next session (round 10 cont.):** the paper-7 §4 KR classifier reproduced; G-B against 0.2405/0.3424 at n = 7; the D1 and D1+atoms arms (Corollary C); G-C/G-D/G-E columns; hostile review of u4a + the census; then ml3a, n3-full, posit-2 map, paper-1 confirmation in queue.

### Round 10 (cont., 2026-07-05) — census dependencies RESOLVED; execution spec banked

- **KR classifier + goalposts:** import directly from `v8/code/pE_phase_causalset.py` (`is_kr_type` = height-3 + dominant middle antichain ≥ n/3; `order_dim_le_2`; `poset_structure`; `process_n`) — the SAME code that produced 0.2405 (m_L) / 0.3424 (m_H) in `g3_measure_census_kr.py` (verified: shares computed per-measure over `.kr` with `lab_mult`/`e(P)` weights; the g3 identity = Σ lab·e(P) = n!·A006455). Importing (not reimplementing) kills classifier-mismatch risk.
- **e(P)/V-lab accounting (the C6 debt):** DISSOLVES under the race-model core — direct history enumeration weights each labeled history exactly and sums over histories into unlabeled classes (never divides); the V-cov read is the unlabeled reduction of the same object. G-C's TV = 0 structurally for the surviving arms (no Φ reads labels) — to be printed + stated as the C6 discharge.
- **G-E:** the corner and D1 are frontier-Markov by construction (slot choice/reset/victim read only fleet state) — ablation TV = 0 identically; print + record as a (small) finding.
- **D1 exact law (u3 verbatim: victim = argmax over per-slot churn-event ages, np first-index tie-break):** under physical-slot exchangeability the victim at a tie = uniform over the tied set with the used/unused split weighted (specific used slot 1/32 each; unused pool (32−u)/32 at the full tie) — exactly enumerable with state (used-slot ages, unused-age histogram). At M = 32 ≫ n = 7 most victims are unused slots (order-irrelevant) — the honest exact structure, to be disclosed.
- **Atoms arm:** u3's ATOM_LO/HI = 0.063376134128 / 0.156109200157 (the W constant), 128-geomspace rich menu confirmed; the exact 3-atom menu values (mean 0.109551 per u1's comment) need one source check (u1/j3 lineage) before the arm runs — the one open constant. Enumeration = 3ⁿ menu sequences × life partitions, ties ⇒ incomparability (Corollary C).
- **Canonicalization at n = 7:** refinement-based canon (invariant classes + within-class permutation search), gated in-receipt against u4a's brute canon at n ≤ 5 (equality = the canon-validation check). Item count at n = 7: Σ over life-partitions of interleavings = 37,633 (the a_n = n![x^n]e^{x/(1−x)} count) — tractable in pure Fractions.

**Next session executes:** `u4_growth_census.py` = the u4a core + pE imports + the corner/D1(/atoms if the menu pins) arms at n ≤ 7 + the pathology column (bare-argmin → antichain) + gates G-A..G-E per note-u4 → hostile review → LEDGER/memory. Then ml3a, n3-full, posit-2 map, paper-1 confirmation.

### Round 10 — THE CENSUS EXECUTED (2026-07-05): G-B REFUSED at the pinned gate — and the post-refusal baseline shows the corner sitting ON the box measure (TV = 0.0371 exact)

**Gate ledger (`u4_growth_census.py`, 17/18, exit 1 by design): G-A 100% dim ≤ 2 all arms; G-B REFUSED (corner 0.4080 vs 0.2405/0.3424 → the pinned ANTI-SELECTS branch); G-C TV = 0 (C6 discharged); G-D exact-mass + canon-validation + anchor HELD; G-E TV = 0 identically.**

- **G-A / Theorem U support leg, instantiated exactly:** the corner reaches **1956 of 2045** classes at n = 7 (315/318 at n = 6) — every reached class dim ≤ 2 under pE's imported exact test; the 89 unreached at n = 7 are precisely where dimension bites. All arms (corner exact, atoms exact, D1 MC) 100% dim ≤ 2.
- **G-B refused — and the refusal is the GATE's defect, shown by the labeled post-refusal baseline:** the **2D box measure itself** (the faithful target, = the L = 1 anchor) has KR-flag share **0.3972** at n = 7 (0.2792 at n = 6). The corner (0.4080) sits within 0.011 of it; **TV(corner, box) = 0.0371 exact at n = 7** (0.0304 at n = 6) — the builder's induced ensemble is ≈ 96% overlapping with the faithful ensemble over 1956 classes. The pinned goalposts (0.2405/0.3424) are ALL-POSET counting shares; the KR flag at n ≤ 7 is not dim-selective (paper 7's own caveat: "the structural flag, not the asymptotic class"), so any measure concentrated on faithful-like orders scores ≈ 0.40 — the gate punished resemblance to the target. Registration lesson (the #43/#45 family): G-B was pinned in round 7, before the race model (round 10) revealed the corner's support = all-dim ≤ 2; the goalposts do not transfer to support-restricted measures.
- **The atoms arm makes the miscalibration vivid:** it "beats counting" at **0.1601** — because its tie-degeneracy (paper 16 §4's content-degeneracy PATHOLOGY: ties ⇒ incomparability) sparsifies it AWAY from the box. At this flag and scale, scoring low correlates with pathology, not virtue. D1 (MC fallback, disclosed): 0.4166 ± 0.0018 ≈ corner ≈ box.
- **The true ensemble-level question is TV-to-box, and the corner's answer is 0.037** — the first ensemble-level (not single-web) manifoldlikeness evidence in the corpus. Per the discipline this stays POST-REFUSAL ANALYSIS until gated: **u4b (next receipt, gates to pin in a note first): TV(arm, box) with pinned bands, plus the contrast columns TV(atoms, box) and TV(counting-restricted-to-dim ≤ 2, box)** — the properly calibrated ensemble gate.
- Receipt notes: canon-validation check re-specified mid-round (first form compared incompatible key encodings — two valid canonical labelings; fixed to re-encode through the brute canon, now Fraction-identical at n = 3..5; class counts matched u4a throughout); the box baseline added post-refusal, labeled. Pathology column: bare-argmin endpoints are near-antichains (8 relations at n = 7, height 2) — the disqualified family's row as expected.

Hostile review of the census arc (u4a + u4 + note-u4 + this interpretation) launching. Round-10 close after review; u4b + ml3a + the rest queue on.

### Round 10 — census review applied (2026-07-05; LEDGER #58): MAJOR classifier-contract bug fixed — G-B INVERTS TO PASS ON ALL THREE ARMS; the prior census entry is SUPERSEDED

**The review (code+science, one referee) attacked everything and broke one thing — the biggest possible thing:** pE's `poset_structure` computes height by a single forward pass, valid ONLY on natural labelings (linear extensions); u4 fed it canonical (hash-ordered) labels → wrong heights → **wrong KR flags on 719/1956 classes at n = 7** (the referee's counterexample: the 7-chain flagged KR-type). "Importing kills classifier-mismatch risk" is REFUTED as written — importing code while violating its input contract CREATED the mismatch (standing lesson). Everything else survived attack: the race model (direct integration + {3,2}-pattern MC + trace-level reset semantics), the CRP/RGS collapse (brute-equality at M = 5), canon_fast at n = 6/7 (bijection with VF2 classes, zero collisions), **G-A strengthened to SET EQUALITY (the corner's reached set = exactly the dim ≤ 2 classes)**, TV(corner, box) = 0.0371 exact (classifier-free), the goalposts correctly sourced.

**Repairs applied + re-run (u4 now 20/20, exit 0; every number = the referee's independent corrected values exactly):** the linear-extension relabeling wrapper restores the classifier contract; the pinned PER-ARM G-B is now executed (review MAJOR-2); TV(atoms, box) receipted; the deviations block added to the docstring (arm-2 naming: CORNER dynamics + 3-atom menu, not u3's beneficial D1+128-geomspace arm; G-C/G-E print-not-count; g3-identity replaced by mass/canon/anchor; ranges; the rank-equivalent exponential scale).

**THE CORRECTED CENSUS VERDICT — G-B PASSES ON ALL THREE PRIMARY ARMS: corner 0.2210, atoms 0.2174, D1 0.2103 ± 0.0015 — all BEAT the 0.2405 counting goalpost** (box baseline 0.2308; corner n = 4/5 = 0.1288/0.3216). Per note-u4 §5's pinned branch: *the growth measure beats counting at the exact scale where paper 7 showed history measures anti-select (0.3424) — the first measure-level manifoldlikeness evidence in the corpus, and Theorem U's census half is instantiated.* Honest caveat carried (the referee's): the KR statistic is weakly informative here — all arms cluster at 0.21–0.23 ≈ the box's 0.2308 ≈ dim≤2-restricted counting's 0.2357 — while **TV-to-box separates decisively: corner 0.0371 vs atoms 0.5694** (the 3-atom pathology visible in the right metric). u4b (pinned TV bands) remains the calibrated instrument, now for a receipted reason. The prior "the gate punished resemblance / box scores 0.3972" narrative was the classifier artifact — WITHDRAWN; LEDGER #57's numbers superseded by #58.

## 2026-07-05 (round 11) — OPENED at user go-ahead

**Scope:** (1) **u4b — the calibrated ensemble gate** (note-u4b, bands pinned pre-run on the NEW quantities; the already-computed TV(corner,box) 0.0304/0.0371 and TV(atoms,box) 0.5694 are disclosed as known): the separation gate (corner beats the contrast columns by a pinned factor), the counting-restricted contrast column (m_L restricted to dim ≤ 2, renormalized — genuinely new), the n-trend read (5/6/7 exact + the pinned two-regime expectation: mild rise at census scale n ≪ L·M, the washout turnaround beyond — q2's √(logN/N) law is the large-N end of the same pillar), and the L-sweep (registered prediction: TV monotone non-decreasing in L at fixed n toward the common-birth wall, per paper 16's renewal law). (2) **ml3a — the least-bias coupling calculation** (note-ml3's pinned G1–G3): executed as derivation + dps-80 verification receipt. (3) **paper-1 confirmation pass** (the owed debt; light referee, background). Reviews till pass on new artifacts; LOG/LEDGER/memory throughout.

### Round 11 — results (2026-07-05): u4b ALL GATES HELD (7/7); ml3a lands the strongest form of its third branch (5/5) — THE ONE-BIT RESULT

**u4b (the calibrated ensemble gate, exact):** G-1 HELD — TV(corner, box) = 0.0371 vs the blind-lottery-over-legal-shapes contrast **0.2734** (m_L restricted to dim ≤ 2, computed classifier-free with the mapping verified against A001035; the restricted labeled share 0.9534 reproduces g3's geo share as a bonus cross-check) and the atoms pathology 0.5694: **the builder is 7.4× closer to the target than blind counting and 15× closer than the pathology — history-weighted toward spacetime, not merely legal-shaped, at pinned grade.** G-2 HELD: the n-trend 0.0240 → 0.0304 → 0.0371 (mild rise, exactly the registered pre-washout expectation; the large-N decay is q2's measured law — the two ends of the discharge's pillar two documented). G-3 HELD: the L-sweep 0.0304/0.0459/0.0537/0.0577 monotone with L = 2 the minimum — **paper 16's renewal law confirmed in exact arithmetic at census scale** (a registered prediction passing). Tier 4's ensemble slot for the discharge assembly is filled.

**ml3a (the least-bias coupling; note-ml3's pinned G1–G3; dps = 80):** G1 HELD — unique up to gauge × **exactly one discrete bit** (equal magnitudes |E| = c/2 forced; entropy sign-blind to 1e-75; the 16 sign patterns = 2 gauge orbits labeled by the CHSH parity). G2 HELD — the fence asserted via exact TLM: the odd branch **saturates the quantum boundary exactly at the Tsirelson budget** (4·asin(1/√2) = π to 4e-81), excluded above; even interior. G3 = **UNDERDETERMINED-AT-ONE-BIT with conditional-exact quantum match** (the odd branch at the Tsirelson budget = the quantum behavior to 80 digits, CHSH = 2√2). **The matter sector's job is now a one-line specification: fix one sign.** Structural consonance with paper 2's ker-R localization = a named conjecture for ml3b (not claimed). The budget c = the one calibrated import; the structure is budget-independent.

In flight at close: the paper-1 confirmation pass (the owed debt) + the hostile review of the round-11 artifacts (u4b + ml3a + notes). Round-12 queue: apply reviews; the discharge-assembly prep (the washout-theorem hardening + Tier-2 remainders); ml3b's design note (the one-sign target + the parity-vs-ker-R question); n3-full; posit-2 map.

### Round 11 — paper-1 confirmation pass applied (2026-07-05): CONFIRMATION-PASS after three one-line repairs

The owed debt cleared. Referee re-ran both receipts (s0 9/9, s1 6/6, exit 0; every paper number receipt-exact incl. the hand-recomputed two-term bias² 0.080 and the fixed-β kill (32/7)ln2); the round-1 fixes verified landed with ONE surviving pre-fix wording (§3 "k-matched" → common-k-range + bin-wise, applied); **no sentence makes a claim the later corpus refutes** — the endpoint classifications remain true at scale (the failure was interior forgery, exactly paper 1's own named "MIDDLE-forgery risk"), and paper 3's sub-m₀ retro-explanation contradicts nothing. Two forward pointers whose promised work is now done got the house status-brackets (§6: the scale run executed as s2 at 512/1024 — not the promised 2048 — refusal branch, production closed at five actions; §4: u4 executed re-scoped to the builder, route (ii) closed, route (i) live and part-measured). Floor claims verified in exactly the citable form papers 2–3 use. Observations (no repair): P3's k4-under-growth isolation step named-never-executed — restored to the round-12+ backlog here; the Status-line lineage list cosmetically stale. **Paper 1 is CONFIRMATION-PASSED — the full v9 paper set (1, 2, 3) is now terminal-grade.**

### Round 11 — CLOSED (2026-07-05; LEDGER #60): the round-11 review applied; both receipts re-run green

**Verdicts: u4b SOUND (every number independently reproduced to 10 digits; the mapping identity verified — the restricted share IS paper 7's geo share as an exact Fraction 5844259/6129859; the L-sweep externally validated at L = 16 by fresh MC; the registration judged genuinely at-risk, with the registered band-miss on the new column as evidence). ml3a PASS-WITH-REPAIRS, applied:** the self-flagged MAJOR (the pinned manifoldlikeness obligation silently unimplemented) now disclosed in-receipt and in-note with the legitimate reason (no behavior-level expression at 2×2×2) and the scope consequence (one bit = an UPPER BOUND on the freedom); "calibrated c" corrected everywhere to scanned-free-parameter with the quantum match explicitly conditional on c = √2; the TLM docstring fixed (all four placements; body not polytope; the equal-|E| reduction verified over all 16 patterns); "excluded above √2" scoped to the maxent solution (odd-parity quantum behaviors persist to c² = 2.25 on unequal families); the even-branch boundary point at c = 2 stated; the gauge-orbit check upgraded from the vacuous union form to proper transitive-orbit decomposition (still 2 × 8, parity-labeled); the pinned gates now QUOTED in-receipt with outcomes stated separately; ledger lines computed not hardcoded. **The referee's independent verification strengthened the mathematics:** the Schur-concavity leg proven analytically (h′(u) = −artanh(√u)/(2√u) < 0), a 20k-point adversarial search with zero violations, and the G2 Q̃-vs-quantum question resolved cleanly (for this set the projections of Q, Q̃, and NPA-1 coincide — the executed fence IS the pinned one, no deviation). LOG/LEDGER wording: "paper 16's renewal law CONFIRMED" softened to the monotone-in-L direction at fixed M, n (the L·M/N law proper untested). Re-runs: ml3a 5/5, u4b 7/7, both exit 0.

**Round 11 terminal: u4b + ml3a + paper-1 confirmation all applied. Round-12 queue: the discharge-assembly prep (the washout-theorem hardening + Tier-2 remainders); ml3b's design note (the one-sign target; the parity-vs-ker-R question); n3-full; posit-2 map; k4-under-growth (restored backlog item).**

## 2026-07-05 (round 12) — OPENED at user go-ahead

**Scope:** (1) **The discharge assembly's pillar two — the washout theorem hardened (note-w1 + receipt w1):** the theorem skeleton at [THEOREM-track] grade — the churn fleet is a uniformly ergodic finite-state-in-law Markov process (geometric resets kill slow variation); stationarity + window-wise Glivenko–Cantelli ⇒ the empirical joint law of (commit position, value rank) → product Lebesgue ⇒ **D* → 0 a.s.**, with the mixing route giving the rate O(√((LM + log N)·log N/N)) — the structure q2 measured as C(L,M)·√(log N/N). The receipt verifies every measurable intermediate: window-wise marginal convergence (the key lemma), the O(LM) mixing time, the D*-rate re-collapse with the predicted constant structure, and the O(LM) cold-start transient. Honest gap list carried in-note (what remains for the assembly paper's formal proof). (2) **ml3b design note** (think-first, no code): the one-sign machine — the parity-vs-ker-R identification question stated precisely with its candidate bridge and refutation conditions; the two-species GW-flow gates incl. the new sign readout. (3) Backlog as context allows: n3-full, posit-2 map, k4-under-growth. Reviews till pass throughout.

### Round 12 — results (2026-07-05): Theorem W's joints ALL VERIFIED (after a recorded refusal-and-amend cycle); ml3b designed with the identification question honestly corrected

**w1 (note-w1 + receipt): the washout theorem's skeleton stands with every measurable joint verified — pillar two is now [THEOREM-track].** First run: V1 (the key lemma — window-wise GC) and V5 (paper 7's bridge, slack by 14×) HELD; V2/V3/V4 REFUSED. Diagnosis recorded in note-w1 §5 before any verdict prose, all three instrument/pin class: V2 measured the near-white *output* series instead of Lemma W1's *fleet* functional (τ̂ = 1 exactly — diagnostic); V3's small-N end carried the O(L·M·log M) cold-start segment (the theorem is a stationary statement); V4's pin ignored the log M factor in the transient scale. Amended instruments re-run: **ALL HELD 5/5** — V2: the fleet-total's e-folding lag 67 → 121/123 under L- and M-doubling (ratios 1.80/1.84 vs the ×2 predictions — τ_mix ∝ L·M measured); V3: the stationary-start collapse D*·√(N/logN) flat at max/min 1.254 across N = 512–4096 (q2's law re-verified); V4: the cold/stationary profile peaks at W = 256 ≈ the coupon-collector scale (1.286) then falls to 0.982 at W = 4096 — the transient is finite and O(L·M·log M) as stated. The honest gap list (§3) stands: the drift/minorization formalization, the mixing-DKW import's conditions, and the continuity hypothesis (which *excludes the atomic corner by construction* — consistent with its TV 0.5694 pathology) are the assembly paper's jobs. **The discharge chain now reads: dim ≤ 2 [THEOREM, constructive + census set-equality] ∧ D* → 0 [THEOREM-track, joints verified] ⇒ volume-faithful [paper 12]. What remains before the assembly paper: the three §3 formalizations + Tier-2's statistical-layer remainders.**

**ml3b (note-ml3b, think-first): the one-sign machine designed — and the bit identified for what it is.** The honest sharpening (supersedes round 11's "consonance" conjecture as posed): ml3a's two orbits are physically inequivalent at the same budget — odd = Bell-violating-signed (|CHSH| = 2c), even = Bell-satisfying-signed (|CHSH| = c) — so **the un-fixable bit is the NONLOCALITY BIT itself**: least-bias cannot decide whether the world's coupling violates Bell. And the parity bit is *prima facie distinct* from paper 2's ker-R bit (parity = CHSH-visible; ker-R = CHSH-invisible — real QM reaches Tsirelson at CHSH, the very reason network tests exist): the round-11 "same bit?" question was ill-posed; the corrected ledger has three layers — (i) magnitudes [least-bias-fixed given budget], (ii) the nonlocality sign [ml3a's bit, ml3b's target], (iii) the composition datum [paper 2's bit, CHSH-invisible, out of ml3b's scope]. Gates pinned (G1 existence/decay; **G2 the sign readout with SELECTED / RELOCATED / UNDETERMINED semantics across flux sectors and couplings**; G3 ml2 regression; G4 Lüscher survival); the mode-selecting Hamiltonian = the named fallback; build rounds sequenced cheapest-kill-first.

Hostile review of the round-12 artifacts launching (the amendment cycle is the named audit target). Round-13 queue: apply review; Tier-2 remainders (the staircase/dispersion pieces the assembly needs); the assembly paper's design note; n3-full; posit-2; k4-under-growth.

### Round 12 — addendum (2026-07-05): ml3b's G2 gains its empirical anchor (user observation)

The parity bit is CHSH-visible ⇒ **experiment already fixes it: the world is on the ODD (Bell-violating) branch** (loophole-free Bell tests, CHSH → 2√2). Note-ml3b's G2 semantics re-pinned accordingly: ml3b is now a **falsifiable prediction test** — SELECTED-CORRECT (odd) = the matter sector derives the empirically right sign; SELECTED-WRONG (even) = the GW-flow locus is refuted, fallback activates; the bit's grade today = [MEASURED, external], ml3b's target = [DERIVED given the matter sector] with experiment as the standing falsifier. (The user's phrasing had the direction inverted — reality VIOLATES Bell — corrected in conversation; the structural insight, "the theory doesn't say but reality does," is the anchor now pinned.)

### Round 12 — CLOSED (2026-07-06; LEDGER #62): the review applied — the amendment cycle adjudicated; V4 re-powered; all artifacts at repaired grade

**The review's central adjudication (the named audit target): V2 LEGITIMATE — in the strongest form: PIN RESTORATION** (the §4 pin already named the fleet functional; the first run's instrument violated it; the referee replay-verified the entire first-run ledger from §5's prose under the pinned seed, with V5's value acting as an rng checksum — "the refuse-diagnose-amend record is genuine"). **V3 LEGITIMATE** (theorem-form alignment; diagnosis seed-robust; retained risk — an N^{−1/4} law would still have refused). **V4 MIXED — the honest bill:** no seed-shopping and fully disclosed, but the "log M" diagnosis was arithmetically specious (222 < 256 — it could not explain the miss; the true defect was the unexamined contamination→excess constant ≈ 0.7 and ±0.1-class noise), the added profile clause was near-vacuous as coded, §5 under-quoted the first-run row, and the amended pass was seed-grade (alternate seeds refuse 2/3). **Repairs applied:** V4 re-powered per the review's prescription (48 reps, mean ± SE, [directional]; gate = ratio(4096) − 1 ≤ 3·SE ∧ interior peak) — third-run: **1.141±0.045 / 1.250±0.049 / 1.124±0.048 / 1.024±0.042, peak interior, terminal within errors of 1 — the transient's death now at honest statistical grade**; gap 2's primary evidence re-assigned to the V3 untrimmed-vs-trimmed contrast, receipted as the new V4b (1.340 vs 1.254, seed-robust per the review's replication); §5 carries the full first-run row + the corrected diagnosis; the W4 bracket fixed (the receipt verifies the collapse at fixed (M, L); the L, M dependence is q2's — measured M-FLAT, the √(LM) bound loose in M, disclosed); [directional] tags on V3/V4; the module docstring synced; the N ≥ 7 bridge qualifier added; note-ml3's residual "calibrated" fixed. Re-run: **6/6, exit 0.** **note-ml3b: SOUND as written** (the §1 physics verified exhaustively; the supersession judged fair; the c > 1 qualifier — which LOG/LEDGER compressions dropped — is stated here: the "cannot decide whether the coupling violates Bell" reading holds for budgets c > 1; at c ≤ 1 both parities satisfy Bell and the question is decided by magnitude alone). Process note adopted (review n6): commit pins and refused-run code to VCS before amendments — the workspace is not a git repo; flagged for the user as an infrastructure decision.

**Round 12 terminal. The discharge chain after review: dim ≤ 2 [THEOREM] ∧ D* → 0 [THEOREM-track, joints verified at repaired grade — W1's τ ∝ LM the strongest joint (1.73–2.17 across seeds), W2 at 1.0025× the iid control, the stationary collapse 1.15–1.25 across seeds, the bridge slack 13–17×] ⇒ volume-faithful. Round-13 queue: Tier-2 remainders; the assembly paper's design note; n3-full; posit-2; k4-under-growth.**

## 2026-07-06 (round 13) — OPENED at user go-ahead (entry written after the pin — the article interlude; the pin commit d9189b3 timestamps the registration)

**Scope:** (1) **T2.3 / Theorem P — Poissonization of the churn class** (note-t23, gates pinned + committed pre-run per the freeze discipline): the cell's third layer (per-draw dispersion → Poisson) as a Chen–Stein theorem skeleton with verified joints — completing the discharge's coverage of the full three-layer cell. (2) **The assembly paper's design note** (the discharge theorem's structure: imports table, the w1+t23 gap lists as proof obligations, honest scope). (3) Backlog as context allows. Interludes this round (recorded): the SHARD divulgative article written into roblesnotes at user request.

### Round 13 — t23 state at session close (2026-07-06): two refuse-diagnose cycles, both content-bearing; Theorem P now in its P″ (band) form

Run 1 (absolute pins): V1/V2/V3 REFUSED, V4 HELD → **the reference-floor discovery** (the box itself reads |fano−1| ≈ 0.6 N-flat on the pooled instrument: absolute Poisson is unattainable even for genuine spacetime — the cell's band-calibration philosophy is NECESSARY, not conventional; note §5, frozen f959aeb). Run 2 (P′ control-relative pins): V1-rel/V2-rel/V4 HELD (churn ~5× closer to the reference than the reference to Poisson; the L-density mechanism monotone) — **V3-rel REFUSED, and the refusal indicts the pin: it contradicted T2.1's own cv² law** (det ≪ geom = the corpus's D1-benefit mechanism, re-confirmed here; P3's plateau is an N-trend prediction, mis-instrumented as a fixed-N ratio; note §6). Deeper: the pooled relative gap is N-flat at L = 2 (mean-model bias survives control-relatively) — **the clean theorem object is the small-interval band, where both ensembles decay and churn ≤ box at every N: Theorem P″** (the assembly paper's form). Next session: §7 band-based pins (P3 as per-law N-trend; cv²-correct qualifying predictions) → the re-instrumented run → review of the full t23 cycle. All states committed at each step (f959aeb → 9c053a1 → this close). The assembly design note follows t23's terminal state.

### Round 13 — t23 TERMINAL-pending-review (2026-07-06): the band form lands — Theorem P″ measured; P3 = a divergence, not a plateau; one refusal to the review as-is

Run 3 (P″ band pins, 080fd89; run frozen post-hoc): **V1″ HELD** (both ensembles decay, churn ≤ box everywhere — P″'s core); **V2″ HELD strongly** — geometric decay 4.20× vs the infinite-variance law GROWING (0.306 → 0.916; ratio 0.33×): **Lemma P3's sub-geometric hypothesis is load-bearing beyond expectation — the boundary is a divergence**; **V4 HELD** (3rd). **V3″ REFUSED as-is** (det/geom 1.17 vs [0.1, 1.0]) per §7's last-pin-round semantics — note §8 records the instrument-dependent ordering (pooled: det ≪ geom = T2.1's cv² law confirmed; band: det ≳ geom = run-rigidity candidate) as the review's adjudication item. The t23 cycle: 3 pins, 3 runs, every state committed (d9189b3/f959aeb/9c053a1/d0f9d87/080fd89 + this run) — the freeze discipline's first full multi-cycle demonstration. Hostile review of the complete cycle launching; the assembly design note next.

### Round 13 — the assembly design note delivered (2026-07-06)

**note-A1 written + committed:** Theorem D (the discharge) stated in full — geometry [Theorem U] ∧ volume [Theorem W] ∧ statistics [Theorem P″] ⇒ volume-faithful 2D webs entering all three cell layers, via paper 12's sufficiency and paper 7's finite-N bridge, with the exact census as the measure-level face. The imports table (6 rows), the consolidated proof-obligation list (9 items — the w1 §3 four + the t23 five, incl. the reference-floor instrument theorem and the V3″-pending shape factor), and the pinned honest-scope section (2D only; atomless hypothesis = the measured atoms pathology disclosed as consistency; band-calibration explained, not assumed; KR-beyond-census unclaimed). Blocked only on the t23 review (in flight). Paper 4 = rounds 14+.

### Round 13 — CLOSED (2026-07-06; LEDGER #63): t23 TERMINAL after the review cycle; run 4 ALL HELD; the discharge is unblocked

**The cycle review (the deepest audit yet):** all three frozen states re-run bit-exact; the git chain verified pin-before-receipt at every step; the blind pins proven blind from the frozen code; run-3 passes replicated seed-robustly; **the freeze discipline PASSED its first full multi-cycle test.** Adjudications: cycle 1 LEGITIMATE (category-forced — the floor is provably N-free); cycle 2 MIXED (right moves, two wrong recorded reasons); **V3″ = REFUSED-BY-MISPINNED-FUNCTIONAL, physics confirmed.** The review's own science contributions, adopted at §9: **(C1) the reference floor is a MARGINS THEOREM** (conditioned-margins variance deficit, closed form E[α+β−αβ] ≈ 5/9 — my "mean-model bias" attribution refuted; the N-flat pooled gap is t21-P2's own N-free law; A1's gap-item renamed — the assembly paper now proves the right lemma); **(C2) the correct functional** (signed control-relative excess — det/geom 0.13–0.26 everywhere, T2.1's direction passing at all bands and Ns); **(C3) the margin cap** (the true vanishing-sampling-fraction regime, where P″ is strongest); the P3 giant-life mechanism verified (largest life ~N^{2/3}, ×1.56 vs 1.59); det's N-trend hole filled.

**Run 4 (review-prescribed pins, §9, non-blind-disclosed; pinned at 1cee768): ALL HELD 5/5, exit 0** — V3-tp det/geom = **0.26** (review expectation 0.25; both excesses positive, SEs on both legs); V1-tp capped box floor decays monotonically and **churn-L2 sits at +0.0197 ± 0.0220 from exact Poisson at N = 4096 — within 1 SE of the ideal**; V1″/V2″/V4 stand. **t23 TERMINAL: Theorem P″ (margin-capped band form) = the discharge's third pillar, with the sub-geometric hypothesis measured as load-bearing (the heavy-tail divergence) and the reference floor as its instrument theorem.**

**Round 13 complete:** t23 terminal (4 pins, 4 runs, all git-frozen: d9189b3→f959aeb→9c053a1→f4388d1→d0f9d87→080fd89→cbbe6c9→5997d75→1cee768→this); note-A1 delivered (Theorem D; corrected gap-item 8); the SHARD article delivered to roblesnotes (interlude). **Round 14: PAPER 4 — the discharge ("Growth implies spacetime") — drafting begins; nothing blocks.**

## 2026-07-06 (round 14) — OPENED at user go-ahead: PAPER 4, the discharge

**Scope:** draft `relativistic-isp-v9-paper4-discharge.md` per note-A1 (Theorem D; the nine obligations written out — the coupling proof of W1/τ_mix, the cold-start bound, the DKW condition check, F-continuity, the ranking-measurability lemma, the Chen–Stein b1/b2 arithmetic, boundary/stratification, the margins instrument theorem at its §9-corrected attribution, the shape-factor refinement at its review-adjudicated resolution) → commit the draft → three-referee hostile round 1 (proof-work / receipt-truth / claims-and-grades) → apply → rounds till pass + confirmation. Freeze discipline throughout.

### Round 14 — referee-1 (proof-work) applied; revision 1 committed (2026-07-06, bb4011e)

**Referee 1: SOUND-WITH-REPAIRS — four MAJORs, all mathematical, all applied.** (F1) **Lemma W1's proof-grade claim was FALSE for part of the stated class** — the deterministic-L member carries a mod-L phase clock (total-age mod L increments deterministically) making the fleet chain periodic: β(t) ≥ 1 − 1/L forever, an outright counterexample, not a gap. Repair: the hypothesis split (geometric corner/hazard-floor laws at PROOF grade — the coupling argument verbatim, with the coupling-TV→β line and the derived-sequence β line added; aperiodic non-lattice laws = a carried offset-randomization obligation; lattice laws via the phase-decomposed L-skeleton, receipt-invisible and disclosed) + the §1 Class-disclosure block + **D1/kernel-variant pillar-(ii) coverage demoted to a NAMED OPEN** (the state-dependent victim breaks coupled-slots-stay-coupled; note-w1 §1's broader class sentence superseded). (F2/F3) **P1 was false for rank-space intervals** (the margins conditioning is global dependence — P4 at L = 1 is the in-paper counterexample) **and P2's O(λ²L/N) rate is provably unattainable under the margin cap** (Barbour–Hall lower bound; capped sides are Θ(√N), the diagonal Σp_i² = Θ(λ^{3/2}/√N) dominates). Repair: P1/P2 re-homed to VALUE SPACE (where the arithmetic is right, modulo two carried obligations: the b₂ constants + the de-conditioning lemma), the capped rank-space rate corrected to **Θ_λ(√(λ/N))** with the L-dependence living in the signed control-relative excess (exactly the receipts' V3‴ functional — prose now matches the instruments), and the near-zero +0.020 ± 0.022 reframed as the box-Poisson RACE POINT (a cancellation of opposite-signed vanishing terms, not proximity-proof). The paper's own measured decays (2.0×/1.66× per 8× N) side with the √ law. (F4) W2′'s "differ on at most T points" was false (ranks are global) → the 3T/N accounting. (F5/F6) P4's formula fixed (the deviation E[α+β−αβ] = 5/9 EXACT under the comparable-pair/Beta(1,2) pooling measure — the referee derived it — with the ≥10 truncation explaining the measured 0.57–0.63). (F9) The A1 obligations 6–7 discharged in-paper under the cap. (F10) the size-biased partner count E[S(S−1)]/E[S] — making P3 its corollary. Cleanly verified by the referee: the assembly's non-circularity, the object-identity chain (paper 12's ⇐ exact on the canonical embedding; the relabeling-lemma seam now cited), the bridge constants, the coupon-collector arithmetic, F-atomlessness, the P3 giant-life mechanism (N^{2/3}: 1.587 predicted vs 1.56 measured).

**Referees 2–3 (receipt-truth; claims-scope) were killed mid-run by the account spend limit; relaunched on revision 1 after the user raised the limit.** Watch-items handed to them: the t23-V4 density prints differ across committed runs (which is current?); the certificate-slack range; the status-line claim-ceiling vs the now-carried obligations; whether §8's deviations block must list the round-14 MAJORs; the note-w1 supersession's procedural form.

### Round 14 — referees 2–3 applied; revision 2 committed (2026-07-06, 8740a20); round-2 review launched

**Referee 2 (receipt-truth): PASS — zero MAJORs.** All five receipts re-run green and deterministic (w1 run twice); every load-bearing number bit-exact; the t23 git chain verified hash-by-hash with pin-before-run ordering; grade tags sound. Fixes applied: the §6 certificate slack quoted as the committed 0.058 (~17×) — the range's 0.075 upper end came from an uncommitted superseded run (the strict quote-the-committed-run standard enforced); the §8 chain gains 96a3194 (the run-4 freeze — the enumeration had omitted the headline run's own commit); "1.0025×" → ≈1.002×; the ×1.56 largest-life tagged review-measured (t23-V2″ prints the divergence, not the scaling).

**Referee 3 (claims-scope): MAJOR-REVISION — repairs verified honest, summary layer corrected.** M1: the abstract now names the discharged class precisely (the committing-slot churn corner) with the D1/kernel volume-pillar open INLINE, and **Theorem D itself carries the per-pillar class split** (grade and scope are different axes — the referee's phrase). M2: the status line reads "discharged or explicitly carried (three named obligations carried — §7)". M3: Barbour–Hall (the load-bearing lower bound), Merlevède–Peligrad–Rio, and Dushnik–Miller added to References. m4: the inline "audit F#" tags folded into physics-rationale authorship per the house single-threaded convention, and §8 gains the aggregate Review-record line **with the unfavorable direction disclosed** (the class narrowed; the rate slowed). m5: the +0.020 reframed in-abstract as an L=2-scoped cancellation with both streams' signs quoted (sign not determined). m6: the headline carries the band-calibrated qualifier; §6 maps the pillars onto paper 16's operational cell (ρ → 0 subsumed by D* → 0). m7: **note-w1 §6 correction entry written** (Theorem W's D1/kernel reach downgraded at the source, not only in the downstream paper). m8: the census-rise-vs-washout two-regime reconciliation sentence added to §5. n-items applied (obligation re-attribution; three application steps; "two instrument results"; the s8 anchor dropped; sketch-grade qualifier).

**Round-2 review launched** (single referee: verify all round-1 findings landed; hunt revision-introduced defects; adjudicate terminal-grade). Paper state: revision 2, 8740a20.

### Round 14 — round-2 PASS-WITH-MINOR applied (2026-07-06, 8f59d7a); confirmation pass launched

Round-2 referee: **all 20 round-1 findings LANDED faithfully (none partial, none missing)**; internal consistency verified three-way (abstract ≡ Theorem D ≡ §7 on both rates and the class split); the Barbour–Hall lower-bound arithmetic re-derived independently; the ρ→0-subsumed-by-D*→0 cell mapping certified mathematically sound (|ρ| ≤ 12·D* via the copula representation); both revision legs verified prose-only (receipts untouched); the one unprescribed edit (the §5 registered-prediction rename to the monotone-in-L direction) checked against u4b G-3's registration and adjudicated MORE accurate than the original. One revision-introduced defect: the M3 citation fix carried a round-tag into the References (the exact convention class m4 had folded out) — the one-line deletion applied as revision 3. **Paper 4 is terminal-grade pending the confirmation pass** (launched: the whole-document fresh read — the one thing no section-referee did — plus the diff check, 5 spot numbers, cross-reference resolution).

### Round 14 — CLOSED (2026-07-06; LEDGER #64): **PAPER 4 IS TERMINAL — THE CAMPAIGN TARGET IS DISCHARGED**

**Confirmation pass: CONFIRMATION-PASS after one one-word fix** (a paper-16 §2→§3 citation pointer, inherited from the pillar notes — fixed in the paper AND at the source notes, per the referee's hygiene line). The confirmation's sweep: the diff discipline verified exact (one line since the round-2 base); the whole-document fresh read clean (the one audit no section-referee had done); the load-bearing arithmetic re-derived independently (the 5/9 Beta(1,2) identity; the Θ_λ(√(λ/N)) chain; the 3T/N accounting; the coupon-collector bound; every quoted ratio); five spot-check clusters bit-exact; one deterministic receipt re-run green, confirming a paper-only number receipt-true (the capped floor −0.34 → −0.15); all 13 git hashes, 7 notes, and every in-series citation resolve verbatim.

**The record: `relativistic-isp-v9-paper4-discharge.md` — WRITTEN → 3-referee round 1 (proof-work SOUND-WITH-REPAIRS: 4 mathematical MAJORs incl. the lattice-clock counterexample and the Barbour–Hall rate correction; receipt-truth PASS with zero number defects; claims-scope MAJOR-REVISION: the summary layer aligned to the repaired body) → round 2 PASS-WITH-MINOR (all 20 findings landed; 1 revision-introduced tag) → confirmation (1 pointer). Four revisions (bd805a7 → bb4011e → 8740a20 → 8f59d7a → this), receipts untouched throughout, the corrections' unfavorable direction disclosed in-paper.** What stands at terminal: **Theorem D — committing-slot churn growth produces volume-faithful 2D record webs entering all three cell layers in the limit** (geometry [THEOREM, constructive + census-exact]; volume [THEOREM-track, geometric corner at proof grade]; statistics [THEOREM-track, two-regime rates with the load-bearing tail hypothesis measured as a divergence]) — with the honest ledger: three carried obligations, the D1/kernel volume-pillar open, the two instrument results (the margins floor at 5/9; the transient separation), and the exact ensemble face. **Paper 7 §5's discharge sentence — the v9 campaign's §0 target — is discharged.**

**Round-15+ queue:** ml3b (the one-sign machine — the falsifiable prediction test); n3-full; posit-2 map; k4-under-growth; u4b growth-trend extension; the SHARD article's third-pillar line can now read "done" (user's publication call).

## 2026-07-06 (round 15) — OPENED at user go-ahead: the road to 3+1 — ml3b build-step (a) + n3-full

**Scope:** (1) **ml3b build-step (a)** (note-ml3b's sequencing: cheapest-kill-first): the two-species record GW operator with the cross-species four-fermi term at seals; **G4 first** — the Lüscher exact-chirality survival check extended to the coupled system (paper 5's < 1e-90 standard at its precision regime); the build note with pinned gates precedes the receipt (freeze discipline). (2) **n3-full** — the full-scale record-native 3+1 readout (deferred since round 3; the amended gate L2 ∧ n3's own void-controls per the user's option-(a) decision; substrate = the certified builder's webs per the round-3 correction). (3) Backlog as context allows: posit-2 map, k4-under-growth. Commits at each pin/run.

### Round 15 — results (2026-07-06): BOTH FRONTS GREEN — the one-sign machine survives step (a); the 3+1 readout certified at full leg

**ml3b-a (ALL HELD 2/2; note-ml3b-a's pins at eea1de2):** the coupled two-species record GW system EXISTS, IS NOT STERILE, and PRESERVES EXACT CHIRALITY. **Ga2:** the coupled large-N gap system (M_A = g²Σ_A + g_xΣ_B, symmetric) solves at (Q_A, Q_B) = (0, 1), g² = 4 with M_A, M_B > 0 across g_x ∈ {0, ¼, ½}; the g_x = 0 column reproduces the single-species anchors (continuity exact at dps 60); **the cross-susceptibility is strongly positive: dM_A/dg_x = 0.632** (M_A: 0.455 → 0.771 as the coupling turns on; M_B: 0.810 → 0.936) — the scalar channel produces real cross-species dependence [directional expectation confirmed; symmetric control printed]. **G4c (the kill gate):** at dps = 120, L = 4, both sectors — GW identity 4.8e-122/9.8e-122, Lüscher Ward 1.9e-121, and **the coupled-mass breaking identity ‖D(M)γ̂₅ + γ₅D(M) − M(γ₅−s)‖ at 4.8e-122/2.9e-121 — thirty orders below the 1e-90 pin**: the cross-coupling enters only through the scalar masses and adds NO chirality structure, algebraically, AT the coupled masses. The cheapest kill did not fire. Step (b) (the cross-species correlators at seals, the ml1-estimator readout) = the next build round. Build fixes during assembly (recorded): the machinery extraction first captured main-body code (def-to-def slicing — re-extracted with the toplevel regex); the module-level gamma constants added; overlap_hp's 4-arity unpacking. Scope verbatim paper 5: large-N, 2d, quenched, small lattices (identities at L = 4, gap at L = 6 — disclosed), mode-relative; g_x = the disclosed new dial.

**n3-full (ALL HELD 8/8; note-n3-full's pins at 0cc77cf):** the record-native 3+1 readout at full leg — six seeds × two substrates under the amended composition (L2 ∧ voids; the spectral gate stays retired). **The named round-3 design item is RESOLVED: the certified builder's corner substrate reads out transverse geometry via SLOT-WORLDLINES** (chains = fleet slots, persistent at any lifetime): closure band **[0.957, 0.971]** at (2048, 64, 2), 6/6 seeds ≥ 0.9, certification floor 0.957 ≥ the pinned 0.85; the lite's segment arm re-certifies at [0.950, 0.974]. All shuffle voids > 3 null-sd and all confound voids clean (the corner arm's slot-level confound set: mean content, lifetime count, first commit) on every seed; quadrant coverage 6/6 (G-5, measured). **T6.4's L2 conjunct now stands multi-seed at certificate-band grade [DEMONSTRATED given Tier-A input]** — the ml3b derived-field handoff is the named grade upgrade.

Hostile review of the round-15 pair launching; LEDGER #65 at close.

### Round 15 — CLOSED (2026-07-06; LEDGER #66): the review applied — one vacuous check wired, two headlines deflated to their honest content

**Review verdicts: both PASS-WITH-REPAIRS; every committed number reproduced except one transcription (the z-range); the pin-before-receipt ordering verified; the machinery extraction byte-identical; the G4c breaking identity re-derived independently and confirmed EXACT (for any scalar M — which is also the deflation).** Applied:

- **MAJOR-1 (ml3b-a):** the continuity check was vacuous (self-comparison) and unwired — the LOG's "continuity anchor exact" described a verification that had not occurred (the review verified it true itself, via paper 5's own solver, before prescribing the repair). **Repaired and re-run green:** an independent grid-scan solver is now wired into Ga2's boolean (matches to 1e-40).
- **The kill-liveness deflation (MINOR-4, conceded):** neither round-15 gate was a live physics kill for this mean-field construction — the breaking identity holds for ANY scalar mass, so scalar-channel coupling cannot break chirality; non-sterility follows from existence + IFT. **Step (a)'s honest content: the coupled construction is well-defined, numerically sound, continuous with paper 5 (now independently verified in-gate), with one real measurement (susceptibility secant 0.632; local ~0.46, concave).** The operator-level kill stays live for future non-mean-field builds. "Survived its cheapest kill" is retired as a headline.
- **The n3 attribution deflation (MINOR-5, conceded):** the web contributes the chain roster + confound variables only (lat ⊥ web by construction; web-free replication reproduces the bands) — **the corner arm's genuine content is the harvest resolution** (slot-worldlines guarantee the roster round 3 could not) **plus the pipeline's multi-seed certification over the disclosed field distribution.** "The substrate reads out transverse geometry" is retired for the Tier-A leg. Grade tags were correct throughout — the finding is wording.
- **T6.4 precision (MINOR-7):** the amended gate's **must-fail correlated-supply control is OUTSTANDING** (executed only in its Tier-A placebo form, where no supply-χ channel exists by construction); it belongs to the derived-field leg. T6.4 = L2 conjunct certified; not fully met on the paper trail.
- **MINOR-2:** the shuffle-z range corrected to **13.3–27.4**. MINOR-3: the zero-mode-subtracted-both-channels convention + the g_x normalization and HS-positivity notes pinned in note-ml3b-a §4 for step (b)+. MINOR-6: G-3's placebo status + family calibration (~48% chance-void) recorded; max-statistic calibration named for the next leg. NITs: secant-vs-derivative wording; dead code out; re-runs green (2/2, 8/8).

**Round 15 terminal. Round-16 queue: ml3b step (b) — the cross-species seal correlators (the ml1-estimator readout; the conventions now pinned), with the operator-level chirality kill live again there; the derived-field n3 leg (the must-fail void's home); posit-2; k4-under-growth; u4b growth-trend.**

## 2026-07-06 (round 16) — OPENED at user go-ahead: the correlators + the must-fail leg

**Scope:** (1) **ml3b step (b)** — the connected cross-species correlator at the step-(a) coupled masses (the one-σ-exchange/contact form C_AB(x,y) = g_x·Σ_z Π_A(x,z)Π_B(z,y) from the massive overlap propagators); the LIVE kill = existence + decay (a flat/zero connected correlator ⇒ the channel communicates nothing); the ml1-estimator readout produces the campaign's first DERIVED two-party χ number (magnitude-interpretation withheld pending the §4 normalization conventions — disclosed). (2) **n3-derived** — the must-fail leg on ml2's genuinely derived (web-functional) Hasse field: the correlated-supply control finally BINDS (a supply channel exists), the max-statistic confound calibration (the round-15 review's fix), T6.4's outstanding void discharged or refused honestly. (3) Backlog as context allows. Freeze discipline throughout.

### Round 16 — results (2026-07-06): the first derived coupling EXISTS with textbook physics; the must-fail control DID ITS JOB — T6.4 refused at the void for a measured reason

**ml3b-b (ALL HELD 3/3; pins 4206520):** the connected cross-species correlator at the step-(a) coupled masses — **exists, decays 500× across the L = 6 torus (4.48e-2 → 9.30e-5 → 4.70e-5), and its fitted decay rate matches the two-propagator mass scale to 1%** (1.525 vs 2·min(M) = 1.541 — the exchange physics is textbook-correct on the record lattice). **The campaign's first DERIVED two-party coupling number: χ̂ = +3.90e-2** (seal-window w = 1; magnitude interpretation withheld pending the §4 normalization; sign positive). The g_x-scaling ratio 1.904 ∈ (1, 2) as registered. The live kill (flat/zero correlator) did not fire — and THIS one was genuinely live. Step (c) — the sign readout across mode pairs — is now the one-sign machine's remaining stage.

**n3-derived (Gn1 HELD; Gn2a/Gn2b/Gn3/T6.4 REFUSED — the pinned kill branch, frozen at the run commit):** the must-fail correlated-supply control, on its first binding outing, **exposed a design-level confound: the supply-only surrogate (content levels × birth times, nothing else) closes the "own two-clock geometry" at 0.713–0.797** (null ≈ 0.12) on all six seeds; the derived field's coordinates read supply at max-stat 0.86–0.92 vs null ≈ 0.26. **Diagnosis: the own-geometry target — mean two-clock rank positions — IS a supply readout (≈ (birth, level)); the target and the confound are nearly the same object.** The round-4 M6 caveat materialized as a gate. Per the pinned semantics: **T6.4 REFUSED at the void — not a paper-trail gap now but a measured reason**; the redesign is named (score derived fields against TOMOGRAPHIC invariants — papers 14/15's celestial/certificate class — which are causal-pattern functionals, not per-chain bookkeeping; the binding test = ml3b's matter field at fleet scale vs tomographic targets). **Retro-demotion recorded: ml2's rung-2 "reads out own geometry" survives as measurement, demoted as evidence** (closing a supply-aligned target is necessary, not probative) — LEDGER row owed.

Hostile review of the round-16 pair launching (the ml3b-b physics + the n3-derived diagnosis are its named targets).

### Round 16 — CLOSED (2026-07-06; LEDGER #68): the review applied — ml3b-b's gates stand, its narrative corrected; n3-derived certified "the round's best work"; the honest 3+1 status stated plainly

**Review: both receipts bit-exact; registration clean (pins before runs; gate text verbatim; zero post-run edits); the n3-derived diagnosis independently CONFIRMED and strengthened (corr(target, supply) = 0.92–0.97 per axis; joint R² 0.85–0.97 — "target ≈ confound is quantitatively exact"; the surrogate adjudicated FAIR; the kill branch taken cleanly; the ml2 demotion correctly scoped as an EXTENSION of the size-void, not a contradiction — size-only still fails at 0.02–0.06; the field beats the surrogate by +0.13–0.20 = the redesign's open question, now printed). The tomographic redesign pointer adjudicated well-founded-with-feasibility-gaps (papers 14/15 machinery real; pilot-scale; the intrinsic finder in its inconclusive band).**

**ml3b-b's three narrative MAJORs, applied (note §3; the gates themselves stand as pinned):** the "1% textbook match" WITHDRAWN (bin-choice luck — local rates never pass through the target; the honest claim = within the pinned 3× band, which is what Gb2 actually gated); the correlator-side zero-mode convention disclosed (the signed profile OSCILLATES — d = 1 is 100% negative; the far tail moves 3–5× across conventions: the review's split refuses strict-decrease, this receipt's eigenprojection holds it marginally — both printed as INFO); **χ̂'s "+" is window-contingent (contact-dominated)** — a landmine defused before step (c), the sign-readout stage, which now inherits four named pin obligations (zero-mode convention, window/vertex-sign, estimator definitions, dressing class). "O(1/N)" → tree-level-exchange truncation (RPA is leading-order and O(1) at g² = 4; the σ-pole argument preserves Gb2's scale).

**The big-picture adjudication, recorded verbatim-honest: the program currently holds ZERO demonstrated record-native geometry readouts.** Tier-A legs = instrument certification on planted input; ml2's rung-2 positive = demoted; the own-geometry target class = dead by design. The round's net 3+1 evidential movement = negative-to-neutral — #67's ml3b-b half lent the round a green-momentum reading its strongest artifact does not support (corrected here). What survives on the matter side: toy-scale existence — a coupled system whose cross-channel communicates and grossly decays — real but modest. **The named path (fleet-scale matter field vs tomographic invariants) is the right shape; both halves carry open feasibility gaps (L ≫ 6 correlators with the zero-mode/RPA questions settled; tomographic certificates beyond pilot/oracle scale).**

**Round-17 queue: ml3b step (c) design note (the sign readout — mode-pair structure + the four inherited pin obligations); the tomographic-target feasibility scout; posit-2; k4-under-growth; u4b growth-trend.**

## 2026-07-06 (round 17) — OPENED at user go-ahead: step (c) — the sign readout

**Scope:** (1) **note-ml3b-c** (the design): the four-cell mode-pair structure via the scalar/pseudoscalar bilinear assignment (the disclosed mode import), the four round-16-inherited conventions PINNED (zero-mode-subtracted propagators both sides; the window set {w = 1, d = 1 shell, d ≤ 2} with window-robustness required for any SELECTED verdict; the g_x > 0 orientation as reference with the g_x-flip printed; exact estimator formulas) — then the receipt: the four mixed-bubble correlators E_xy, the parity π = sign(∏E_xy), the pinned verdict semantics (SELECTED-ODD = the empirically correct sign / SELECTED-EVEN = the locus refuted / WINDOW-CONTINGENT = not selected / KINEMATIC-NULL = the assignment cannot express the cells), and the kinematic-vs-dynamic discriminator. (2) The tomographic feasibility scout (papers 14/15 machinery vs fleet-scale matter data) — this round if context allows, else round 18 first item. Freeze discipline throughout.

### Round-17 receipt: ml3b-c RAN — the verdict is WINDOW-CONTINGENT (not selected, not killed); the honest lean is EVEN

**The run** (`ml3b_c_sign_readout.py`, pins 58cb760 + amendment 38f0e94, receipt committed pre-run): Gc1 **HELD** — all four cells exist at sectors (1, 2), min |E| = 2.3e-05; the SS5 amendment's algebra confirmed in-receipt (the demoted (0, 1) row shows the A-side P-cells at 1e-19/1e-20 — the measured kinematic parity-kill, vs live cells at 1e-2..1e-5). Gc2 **REFUSED = WINDOW-CONTINGENT by the pinned semantics**: parities at (4, 1/2) = [even, odd, even] across {w=1, d=1 shell, d<=2}; at (4, 1/4) = [even, even, even]. Gc3: **DYNAMIC** (the parity moves with the coupling point — not kinematically forced; the free point reads [odd, even, odd]). Disclosures on record: (i) the g^2 = 2 point is VOID-by-solve — the gap CLOSES between g^2 = 2 and 4 (a measured feature of the coupled flow, new); (ii) g_x = -1/2 leaves the physical branch (M_B < 0 — the mass-weakening direction closes species B's gap), and at tree level the 4-cell parity is structurally prefactor-blind (g_x^4 > 0), so convention 3's RELOCATED risk is nil at this order; (iii) the full-propagator variant and the vector-pair probe both reproduce [even, odd, even] at the anchor — the window-contingency is NOT the zero-mode convention's and NOT assignment-specific.

**The honest adverse disclosure (stated before review, house discipline):** 7 of 9 evaluable window-parities at the two pinned coupling points read **EVEN — against the registered ODD expectation** (the world's branch). Had the pin been any single d-inclusive window, the verdict would have printed SELECTED-EVEN = the kill; the three-window robustness requirement (round 16's lesson, pinned exactly for this reason) is what softened it to inconclusive — and symmetrically, it is what blocked a lucky single-window SELECTED-ODD. The lean is fragile for a measured reason: the P-cells sit 1–3 orders below E_SS and near zero (E_SP flips sign across g_x at the shell), and the bubbles carry pointwise |imag| up to 5.2e-3 that the pinned real-part estimator discards while the P-cell means sit at 1e-5..1e-4 — the parity product is currently at the mercy of near-threshold cells. **The finding, per the pinned semantics, names what flips: the d = 1 shell (via E_SS's contact-vs-shell sign, round 16's signed profile) and the near-zero P-cells.**

**Named next step (the stabilization design, round 18 candidate):** a window-free estimator — the sign readout at the exchange POLE (residue signs at the lightest coupled mass) and/or a hermiticity-symmetrized cell estimator; both kill the contact-term reweighting that the window family measures. No refutation is declared: SELECTED-EVEN was the kill condition and it did not obtain under the pinned aggregation; the locus survives WITH the adverse lean on record.

### Round-17 item 2: the tomography feasibility scout (papers 14/15 vs the fleet-scale matter handoff) — REPORTED

Scout sweep over the k/l/m receipts (`k1`–`k4`, `l1`/`l3`, `m1`/`m2`) + both papers. **Every instrument's input is a causal POSET on N events** (dense N x N order); the only coupling matrix any of them touches is the fleet coupling W **they derive from the order themselves** (cross-comparability of chain worldlines, k3/l2/m1/m2); the w <-> chi_AB identification is [POSITED] in both papers and UNBUILT — no instrument has a correlation-matrix input port, and the chi_AB/propagator machinery (c3, q1) has no wired path into k/l/m. **Scale is a NON-gap:** the certified bands (N = 256..2200; fleets <= 72 worldlines; MDS only K x K or M x M) comfortably admit a grown web at N ~ 2048, fleet 32–64 (caution: the 4D intrinsic finder sits at the fragile edge at N ~ 2200 — bootstrap iterations must scale, m2's own finding). Secondary gap: all certification is Poisson-sprinkling-relative — a GROWN web is scored like the adversaries the finder is built to refuse (calibration, not type, incompatibility). Dimension note: lattice chi-hat is 36 x 36 over SITES; the W-port is M x M over WORLDLINES — the bridge must be built per thread-pair, not per site-pair.

**The binding gap, adjudicated: the unbuilt w <-> chi_AB bridge.** The named round-18 build: on ONE grown web, compute BOTH the order-derived W and a thread-pair matter correlator chi-hat, and test the posited identification directly (monotone agreement = the bridge exists and the matter field enters tomography through the W-port; refusal = the posited rung falls — either verdict advances the 3+1 redesign). This supersedes vague "fleet-scale matter field" language: the redesign's first concrete, falsifiable step is the bridge test.

### Round-17 hostile review: FAIL-as-recorded -> corrections applied -> corrected rerun CONFIRMS (the loop closed)

**The review** (independent referee, full reproduction): the machinery, the amendment (adjudicated LEGITIMATE — commit-verified pre-receipt; the Q = 0 vanishing identity independently verified at 1e-15), the bubble algebra (= direct spinor trace to 8.7e-19; = step (b)'s Pi exactly; the exchange wiring correct), the freeze ordering, and the WINDOW-CONTINGENT verdict all SURVIVED. **The findings layer failed**: (MAJOR-1) the receipt deviated from the note's pinned zero-mode formula — `S@P0.real` is not a projector (idempotency 2.4e-2), breaks g5-hermiticity (1.1e-2), and MANUFACTURED the 5.2e-3 imaginary parts plus 4/9 parities; (MAJOR-2) Gc3's "DYNAMIC" was that artifact — under the pinned form the pattern is point-INVARIANT; the vector probe was degenerate noise (cells 1e-34, below its own floor); (MAJOR-3) "g_x = -1/2 closes species B's gap" is FALSE — review-measured positive root (0.109667, 0.503996), residuals 3e-61, missed by the single-start Newton; (MAJOR-4) "7/9 lean EVEN" unreconstructible + dependence-inflated + missing its mirror. MINORs: the d<=2 near-equality is print truncation (2.4e-8; the four cells ARE independent); the g^2 = 2 closure is inherited single-species physics; the imag-fragility disclosure did not materialize (the real driver was MAJOR-1).

**Corrections applied** (note SS6 + corrected receipt at 80f3f8b, committed pre-rerun): joint complex eigenprojector (the pinned form); multi-start residual-verified Newton; per-window pattern classifier; probe degeneracy declaration; both void prints re-attributed. **The corrected rerun** (exit 1 by design): bubbles exactly real (max|imag| 1.98e-16); Gc1 HELD (min cell 6.3e-05); **Gc2 = WINDOW-CONTINGENT with both g_x now agreeing on the full pattern [even, odd, even]**; Gc3 = **PATTERN-INVARIANT** — per-window parity is point-invariant across anchor, (4, 1/4), free, AND the recovered (4, -1/2) root (masses match the review's to 6 digits); vector probe DEGENERATE (1.4e-34); the (0, 1) kinematic kill stands (1e-19). The rerun reproduces the review's independently computed pattern and root exactly — the correction loop is closed.

**The corrected finding of record (supersedes the round-16-style lean language):** the sign readout is not window-contingent noise — it is a POINT-INVARIANT WINDOW SPLIT: every contact-dominated window (w = 1, d <= 2) reads EVEN at every evaluable coupling point; the d = 1 shell — the only pinned window free of the d = 0 self-correlation contact term — reads **ODD at every evaluable coupling point**, under both zero-mode conventions, at both pinned g_x, and at the negative-coupling root. No selection is claimed (a shell-only pin would be post-hoc; symmetrically a contact pin would print the kill); the pinned verdict WINDOW-CONTINGENT stands. **What round 18 must do is now sharply posed: pin, on principle and BEFORE any receipt, which estimator IS the two-party seal correlation** — the pole-residue (window-free) readout, and/or the exclusion of coincident-point self-correlation from a "two-party" cell — then let that pin adjudicate SELECTED once, irrevocably.

## Round 17 CLOSED (2026-07-06)

Both items delivered under the freeze discipline (commit chain: c5de3b2 open -> 58cb760 pin -> 38f0e94 pre-receipt amendment -> 1f432bb receipt -> 2eecc79 records -> 80f3f8b corrected receipt -> 0eeb0d8 review records -> dafe8ca scout). **Item 1 (the sign readout):** the campaign's falsifiable moment ran and returned the third outcome — WINDOW-CONTINGENT under the pinned unanimity semantics; the hostile review (FAIL-as-recorded) caught a real receipt bug (the zero-mode projector deviation) whose repair revealed the clean structure: a POINT-INVARIANT WINDOW SPLIT — contact windows even, the contact-free d = 1 shell ODD, at every evaluable coupling point including the review-recovered negative-coupling root; the corrected rerun reproduces the referee's independent numbers exactly. No selection, no kill; the locus survives with the split on record. **Item 2 (the scout):** the tomography instruments consume posets only; w <-> chi_AB is the unbuilt binding gap; scale is a non-gap. **Round-18 queue:** (1) pin the principled two-party estimator (pole-residue / contact-excluded) and let it adjudicate SELECTED once; (2) the w <-> chi_AB bridge test (design note + execution if cheap); backlog rides.

## 2026-07-06 (round 18) — OPENED at user go-ahead: the principled estimator + the bridge test

**Scope:** (1) **note-ml3b-d + receipt — THE ONE-SHOT ADJUDICATION.** Pin, on principle argued BEFORE any new data, which estimator IS the two-party seal correlation: seals join DISTINCT record threads, so the surrogate is the SEPARATED two-point correlator (d = 0 is a thread reading itself — not a seal; in QFT language the contact term is the scheme-dependent piece, never the physical two-point data). Two window-free principled forms, both cutoff-free: E1 = the separated susceptibility (p = 0 mode of the off-diagonal correlator) and E2 = the lowest-momentum separated mode (star-averaged p_min = 2pi/L). Gd2 = the FINAL tree-level/L=6 adjudication: 2 estimators x 2 g_x x 2 zero-mode conventions = 8 parities; unanimity => SELECTED-ODD (success) / SELECTED-EVEN (the locus+assignment kill); estimator disagreement => ESTIMATOR-SPLIT = fails-at-this-order (principled negative). IRREVOCABILITY CLAUSE: no estimator/window/convention re-pins admissible at this locus+order after the receipt — any non-SELECTED outcome closes the tree-level chapter (next move structural: dressing/larger L/Hamiltonian fallback). DISCLOSURE: the d = 1 component is public (#70) and leans the odd way — [directional] registered expectation = SELECTED-ODD via d = 1 dominance; the receipt's live content = the multiplicity-weighted d >= 2 re-weighting (the SP cell's separated sum is near-cancelling on back-of-envelope — genuinely open), the momentum-star re-weighting, and the 8-way unanimity demand. (2) **note-wb1 + receipt — the w <-> chi_AB bridge at the constructible grade:** one grown web, the order-derived W (k3/l2 formula verbatim) vs the Hasse-GFF thread-pair chi-hat (ml2 machinery; grade disclosed: NOT the GW field), with the round-16 supply-confound control MANDATORY (Gw3: the supply surrogate must not own the association). Verdicts: BRIDGE-SUPPORTED (licenses the W-port at ml2 grade) / BRIDGE-REFUTED (the posited rung falls at this grade) / CONFOUNDED (the n3-derived diagnosis extends to the bridge). Freeze discipline throughout.

### Round-18 receipt 1: THE ONE-SHOT FIRED — ESTIMATOR+CONVENTION-SPLIT = FAILS-AT-THIS-ORDER; the tree-level/L = 6 chapter is CLOSED (irrevocable)

**The run** (`ml3b_d_one_shot.py`, pin bfe793b, receipt committed pre-run): Gd1 HELD (all eight separated cells exist, min 1.7e-04). **Gd2 REFUSED: the 8 readings split 6 odd / 2 even** — E2 (the p_min star mode) reads ODD on all four of its legs ({g_x = 1/4, 1/2} x {sub, full}); E1 (the p = 0 separated susceptibility) reads even under the subtracted convention at both g_x and odd under the full propagator: an ESTIMATOR+CONVENTION split. Per the pinned semantics: **FAILS-AT-THIS-ORDER — the long-wavelength separated sign is indeterminate at tree level/L = 6 — and per the irrevocability clause the chapter CLOSES**: no further estimator, window, or convention pins at this locus+order; admissible next moves are structural only.

**The measured mechanism candidate (from the INFO distance table, on record for round 19):** the per-distance profile shows a NON-DECAYING far tail (C_SS d3–d6 flat at +2.5..3.7e-4; all four cells turn over near d = 3) — the signature of the finite-torus zero-momentum term, which contributes a CONSTANT to all separations at O(1/V). The estimator arithmetic: a constant mode c contributes (V−1)·c = 35c to E1 but −1·c to E2 (the cos weights sum to −1 over separated pairs) — E1 weights the constant mode 35× and sign-flipped relative to E2. E1 is thereby the zero-mode-maximal estimator — exactly why it alone splits by zero-mode convention — while E2 reads the propagating p_min content and is convention-robust. **Registered expectation REFUTED-IN-PART (kept per the convention): "SELECTED-ODD via d = 1 dominance" — d = 1 does NOT dominate E1; the multiplicity-weighted constant tail rivals or owns it (E1_SP = −4.9e-3 against the d = 1 shell's +1.7e-4/site).** Gd3 [directional]: the free point reproduces the anchor split (E1 even / E2 odd); the (4, −1/2) root reads even-even — E2's odd does not extend to the negative-coupling branch (different physical branch; printed, not gated).

**The named structural next (round-19 candidate, admissible under the clause): the L-scaling receipt.** If the far tail is the O(1/V) zero-momentum term, it shrinks ~3–4x by L = 10–12 while the d = 1 shell physics persists — the falsifiable structural expectation is E1 -> E2 convergence with L, adjudicated under fresh pins at the new order/scale. The alternatives stay listed: RPA dressing; a different assignment family argued independently; the Hamiltonian fallback.

### Round-18 receipt 2: the bridge test — VERDICT CONFOUNDED, with a measured ANTI-CORRELATED residual (the posited w <-> chi_AB takes its first adverse measurement)

**The run** (`wb1_bridge_test.py`, pin e038946 pre-run; seeds 20260734–36, Mc = 89–93): Gw1 HELD (primary rho = +0.344, z = 17.1), Gw2 HELD (3/3: +0.271..+0.344, z 14.8–17.1) — the association exists and is robust. **Gw3 REFUSED 0/3 — and not marginally: the supply surrogate owns BOTH matrices (rho(sur, W) = 0.55–0.63; rho(sur, chi-hat) = 0.72–0.76), and the rank-partial residual association is significantly NEGATIVE on every seed (−0.205..−0.281, z −10.4..−17.8).** Pinned verdict: **CONFOUNDED — the n3-derived diagnosis extends to the coupling matrices themselves**: at the constructible (Hasse-GFF) grade, the raw W <-> chi_AB agreement is bookkeeping-mediated, and the supply-orthogonal components of the two objects are OPPOSED, not merely unrelated.

**The mechanism candidate (legible, for wb2's design):** W is a timelike-dominance reader — temporally separated chain pairs are near-fully comparable (W high) while being FAR in Hasse-graph distance (GFF covariance low); contemporaneous pairs invert both. Pooling pairs across heterogeneous births (this web's segments are born throughout the run) therefore drives W and chi-hat apart once the smooth birth-affinity channel is partialled out. **Fairness note (disclosed):** the tomography instruments' native W is computed on fleets of long chains spanning a COMMON diamond — a largely contemporaneous regime this pooled test does not isolate. The named next (wb2, round 19): the CONTEMPORANEOUS-RESTRICTED bridge (pairs conditioned on birth overlap — the instrument-native regime), where the posited identification predicts the residual flips positive; a second negative there would refute the rung at this grade outright rather than confound it.

**Status line for the 3+1 program:** the only input port into tomography (the W-port) now carries an ADVERSE first measurement at the constructible grade — the redesign's options are (i) wb2's restricted form, (ii) the GW-grade field on webs (the standing feasibility wall). LEDGER #72.

### Round-18 hostile review: PASS-WITH-CORRECTIONS both items — verdicts stand; both MECHANISM paragraphs corrected; wb2 pre-confirms (referee-run)

**The review** (independent referee, both receipts reproduced — the one-shot's estimators to 3e-17, the bridge's numbers exact; the W-port formula verified verbatim-equivalent to k3/l2/m1; item-1 freeze chain strict). **Pin integrity ADJUDICATED SUPPORTED** with the decisive observation on record: pinning E2 alone would have delivered SELECTED-ODD — the 8-way unanimity demand was strictly harder and refused, with the registered expectation partly refuted and recorded. **Corrections applied (b577c2b + this entry):** (MAJOR-1) the one-shot's recorded tail mechanism was WRONG — the d >= 3 plateau is NOT the finite-torus zero-momentum term (p = 0 coefficient 14.3x off, sign-flipped in 3/4 cells) but **the zero-mode-SUBTRACTION scar** (absent under the full convention — the corrected receipt now prints both tables: sub flat at +3.2e-4, full decaying to 1e-6; referee L = 10: tail shrinks x5.5–18, NOT x2.78, and **the split PERSISTS at L = 10** — the round-19 L-scaling premise ALREADY REFUSES); what survives: the 35x/−1 aggregation arithmetic (exact) and the E1-fragility account for the deciding PP cell. (MAJOR-2) the bridge's W-clause was BACKWARDS — W FALLS with temporal separation (rho(W, |dbirth|) = −0.61); the true mechanism is DIFFERENTIAL DECAY (chi-hat falls much faster, −0.77..−0.87) — the negative partial is its imprint; the partial itself is certified genuine (survives 2-regressor and raw-|dbirth| attacks, z −12.6..−19.4). (MINOR-1) the one-shot's INFO under-delivered (sub-only table) — repaired, rerun green-identical. (MINOR-2) wb1's note+receipt were co-committed (mitigated: the LOG-open commit carried the full gate semantics earlier; disclosed in the docstring; process rule reasserted). (MINOR-3) nits folded into the notes.

**Referee-run scouting on record (unpinned):** wb2's direction PRE-CONFIRMS on wb1's own data — the partial collapses to null on lifetime-overlap pairs and flips POSITIVE on strong-overlap pairs (2-reg +0.172/+0.074/+0.096); k3's native fleet is measured fully contemporaneous (median overlap 1.00) — the fairness note is quantitatively right. The one-shot's L = 10 legs (referee-run): sub still splits, full reads odd-odd.

**The corrected round-19 queue:** (1) **wb2** — the contemporaneous-restricted bridge, pinned fresh on the corrected differential-decay mechanism (the pre-confirmation makes this the sharpest cheap receipt in the program); (2) the sign readout's structural moves, L-scaling now EXCLUDED as premised: the live options are the RPA dressing class, an independently-argued assignment family, the Hamiltonian fallback, or a principled correlator-level zero-mode treatment at a new order/scale; (3) backlog (posit-2 map, k4-under-growth, u4b growth-trend).

## Round 18 CLOSED (2026-07-06)

The falsifiable moment fired and closed its own chapter: FAILS-AT-THIS-ORDER, irrevocable at tree level/L = 6 (LEDGER #71 + the #73 correction). The bridge took its first measurement: CONFOUNDED with a genuine anti-correlated residual at the pooled regime, mechanism corrected to differential decay, and the restricted regime pre-confirming (LEDGER #72 + #73). Both hostile reviews passed with corrections applied and reruns green. The program's two live fronts are now wb2 (cheap, sharp, pre-confirmed direction) and the sign readout's structural chapter (expensive, four named options).

## 2026-07-06 (round 19) — OPENED at user go-ahead: wb2 (the contemporaries test) + ml3b-e (the chirally-consistent assignment family)

**Scope:** (1) **note-wb2 + receipt:** the contemporaneous-restricted bridge on FRESH seeds (20260737–39; the referee scouting ran on wb1's seeds — gates may not be tuned on those), strong-overlap restriction (> 50% of the shorter lifespan, the referee definition pinned verbatim), 2-regressor rank-partial (the corrected stronger form), with the contrast gate (restricted > unrestricted-complement — the differential-decay mechanism's own signature) and a permuted-residual placebo. Verdicts: BRIDGE-SUPPORTED-RESTRICTED / REFUTED-AT-GRADE / UNDETERMINED. (2) **note-ml3b-e + receipt:** the sign readout at the IMPROVED-BILINEAR assignment family — argued independently twice, data-free: (i) chiral covariance under the exact GW symmetry selects psi-bar Gamma (1 − D/2) psi as the covariant densities; (ii) internal consistency: step (a)'s own gap equation prices exactly Tr[(1 − D/2) S] — the machine's mass is DEFINED by the improved scalar density, so the naive (1, gamma5) insertions of rounds 17–18 were inconsistent with the machine's own operator content. The receipt: S -> K = (1 − D/2) S in the bubbles (source, vertex, sink all improved — full consistency), everything else held fixed (sectors (1, 2), the principled estimator pair E1/E2, the 8-way unanimity protocol, sub/full conventions). This is the improved family's ONE shot (the round-18 clause propagates per family). Registered [directional]: the improvement is expected to REDUCE the convention fragility (the sub/full E1 split to narrow — the doubler-side suppression); the parity outcome itself is genuinely open, with SELECTED-CORRECT = odd per the standing anchor. Freeze discipline: pins commit STRICTLY before receipts (round-18 MINOR-2 rule).

### Round-19 receipts: wb2 ALL HELD — THE W-PORT'S FIRST SUPPORTED FORM; ml3b-e SPLITS — the improved family closes; the cross-family zero-mode fact emerges

**wb2 (`wb2_contemporaries.py`, pin 3ce7fc3 strictly pre-receipt, receipts ff02ede; FRESH seeds 20260737–39): ALL 4/4 HELD — VERDICT BRIDGE-SUPPORTED-RESTRICTED [DEMONSTRATED given the Hasse-GFF import; contemporaneous regime].** Strong-overlap pairs (37–41% of pairs, 1387–1457 per seed): 2-reg partial +0.118/+0.092/+0.142 (per-seed z 3.6/3.6/3.9; Stouffer z_comb = 6.3 vs the >= 3 pin); the complement is strongly negative on every seed (−0.272/−0.329/−0.446) — Gv3's mechanism signature held with a wide margin (the differential-decay imprint lives ONLY in the cross-era pairs, exactly as the corrected wb1 mechanism predicts); the placebo read z = −0.0 (wiring clean). **The posited w <-> chi_AB identification is SUPPORTED at the constructible grade in the instrument-native regime — the first wired path from the matter side into the tomography stack, and the program's first positive record-native identification since LEDGER #68's zero-readouts line.** Scope: ml2-grade field; contemporaneous regime only; the GW-grade bridge stays open.

**ml3b-e (`ml3b_e_improved_family.py`, same pin/receipt commits): Ge1 HELD (min cell 3.2e-06 — the improvement shrinks the P-cells ~an order); Ge2 = ESTIMATOR+CONVENTION-SPLIT — FAILS-AT-THIS-ORDER for the chirally-consistent family; the family CLOSES per the propagated clause.** Readings 6 odd / 2 even with the roles ROTATED vs round 18: E1 odd on all 4 legs (was even-leaning), E2-sub even at both g_x (E2 was the odd-unanimous one), full-convention odd on all 4 legs. Ge3-i REFUTED (kept): fragility WIDENED (2/4 sign flips vs the naive family's 1/4) — the consistency argument did not buy sign stability. Ge3-ii: free point reproduces the anchor split; the (4, −1/2) root reads even-even (third time). INFO cross-check: the naive family reproduces round 18 exactly (even/odd).

**The cross-family structural fact (the round's sharpest finding, on record for round 20):** across BOTH families x both estimators x both g_x, **every full-propagator leg reads ODD — 8/8** (plus the referee's unpinned L = 10 naive full legs, also odd-odd); **every split ever measured is manufactured on the zero-mode-SUBTRACTED side** (which round 18's review already showed carries the long-range scar). No post-hoc crowning of the full convention (the round-17 lesson stands); but the next structural question is now razor-sharp and PRINCIPLED, not estimator-shopping: **which treatment of exact topological zero modes is correct for quenched correlators at fixed Q != 0 and small V?** This is a known pathology class (quenched pseudoscalar correlators are zero-mode-contaminated at small volume; the subtraction is a recognized but scheme-ambiguous fix; the clean discriminator is volume growth OF THE CONTAMINATION, not of a p = 0 tail). Round-20 design note: resolve the zero-mode treatment on principle (ensemble/sector-weighting vs subtraction vs the large-V limit of the P-channel contamination specifically), with the RPA dressing and the Hamiltonian fallback weighed alongside.

### Round-19 hostile review: PASS-WITH-CORRECTIONS both items — zero false results; wb2's attacks STRENGTHENED it; corrections applied

**The review** (freeze clean incl. the strict pin-before-receipt rule; both receipts reproduced EXACT; every LOG/LEDGER number verified). **wb2:** the positive survived everything — threshold-robust and MONOTONE in contemporaneity (+0.087/+0.118/+0.137 at 30/50/70%); the pinned gates pass on wb1's old seeds too (Stouffer 7.70 — no reverse-engineering); the third-regressor attack fails decisively (adding overlap-fraction/|dbirth|/chain-length/lifespan regressors INCREASES the partial, up to +0.200 at z 4.3–7.0); the Stouffer >= 3 bar holds under all three null designs (7.4/4.8/3.76). Corrections (note-wb2 SS4): the placebo carries a measured +0.4–0.9 sigma bias (single-draw wiring check, NOT evidence — "placebo clean" was a gloss); the SUPPORTED headline now carries wb1 SS3's caveat inline (association != the physical identification; the complement's anti-correlation is the empirical answer to the same-graph circularity worry). **ml3b-e:** the improved-bilinear contraction algebra CERTIFIED (independently derived, one (1 − D/2) per leg, receipt matches 7e-18); the 8/8 cross-family fact verified leg-by-leg; MINOR-4 applied ("every split is subtraction-side" scoped to the 16 estimator-protocol legs — round 17's window split existed under both conventions); the round-20 pose adjudicated PHYSICS-CORRECT with one addition: the **S − P cancellation combination** (exact-zero-mode terms cancel) joins the option list. LEDGER #76.

## Round 19 CLOSED (2026-07-06)

The round delivered the program's first supported record-native identification — **the W-port's first form** (wb2: BRIDGE-SUPPORTED-RESTRICTED, review-hardened) — and closed the second assignment family at tree/L = 6 (ml3b-e), sharpening the sign readout's remaining question to a single principled fork: **the zero-mode treatment for quenched fixed-Q correlators at small V** (8/8 full legs odd; every estimator-protocol split subtraction-side). **Round-20 queue:** (1) the zero-mode design note — sector-weighting vs subtraction vs the S − P cancellation vs the contamination's V-scaling, one pinned choice, then (if affordable) the third and final family/convention adjudication; (2) the bridge line's next rung — either wb3 (the order-side W against the tomography instruments' actual reconstruction on a grown web — does the supported coupling RECONSTRUCT?) or the GW-grade feasibility note; (3) backlog (posit-2 map, k4-under-growth, u4b growth-trend; the SHARD article awaits user read).

## 2026-07-06 (round 20) — OPENED at user go-ahead: the zero-mode resolution + THE FINAL TREE-LEVEL ADJUDICATION; the GW-on-web feasibility note

**Scope:** (1) **note-ml3b-f + receipt:** the zero-mode fork resolved ON PRINCIPLE (the full-propagator convention: exact states of the record operator carry no deletion license; the O(1)-mass regime sits far from the quenched small-M pathology; the gap-side subtraction was a bulk-condensate DEFINITION in a scalar equation, not an unphysicality claim; S−P cancellation kills only the zm x zm piece — derived in-note — and does not map onto the 4-cell parity; sector-weighting adds imports and kills the parity-breaking backbone), with the KNOWN DIRECTION fully disclosed (8/8 full legs odd at L = 6; referee L = 10 naive full odd-odd, unpinned) and integrity carried by (i) the data-free argument, (ii) fresh unmeasured legs (the improved family at L = 8/10; E2-full off L = 6), (iii) a falsifiable V-scaling discriminator (the sub-vs-full discrepancy must SHRINK with V or the verdict downgrades to CONVENTION-CONDITIONAL). Gf2 = {E1, E2} x {g_x 1/4, 1/2} x {L = 8, 10}, full convention, 8-way unanimity — **THE FINAL tree-level adjudication of the sign readout, full stop** (split => the tree-level line ENDS; fallbacks RPA/Hamiltonian). (2) **note-mw1 (scoping, no receipt):** the GW-grade matter-on-web feasibility map — candidate constructions, the binding sub-problem, and the pinned success criterion for a future ml4. Freeze discipline; pins strictly before receipts.

### Round-20 receipt: THE FINAL ADJUDICATION — 7/8 ODD, ONE NEAR-ZERO EVEN LEG ⇒ FAILS-AT-TREE-LEVEL; THE TREE-LEVEL SIGN-READOUT LINE IS TERMINAL

**The run** (`ml3b_f_final.py`, pin 26e2adb, receipt acd60e8 pre-run; the improved family, FULL convention per the note's data-free resolution, masses re-solved per L): Gf1 HELD (all cells exist at L = 8 and 10). **Gf2 REFUSED: readings 7 odd / 1 even — the single even leg is E1 at (L = 8, g_x = 1/4), where E_SS = +1.769e-05 sits at a near-zero crossing (it flips to −1.337e-05 at L = 10); the L = 10 row is UNANIMOUS ODD (4/4); every E2 leg is odd.** Per the pinned semantics: ESTIMATOR+L+GX-SPLIT ⇒ **FAILS-AT-TREE-LEVEL, the line ENDS** — no selection, no kill, and no further tree-level attempts under the propagated one-shot clauses. Gf3 HELD by the pinned letter (max rel sub-vs-full discrepancy: E1 7.770 → 4.795 → 2.001 — faster than 1/V; E2 2.281 → 2.067 → 2.039 — nearly V-flat; the discriminator's support is strong for E1, weak for E2 — disclosed; moot for the downgrade since no SELECTED obtained). INFO: the S − P zero-mode cancellation verified in-receipt at 2.19e-32 (the chirality algebra exact).

**The terminal statement for the ml3b tree-level arc (rounds 15–20):** the one-sign machine was built and certified end to end (the coupled two-species GW system; the first derived coupling; the four-cell CHSH structure), and its sign was adjudicated three times under strictly hardening pinned protocols — the naive family at L = 6 (round 18), the chirally-consistent family at L = 6 (round 19), the consistent family under the principled convention at L = 8/10 (this round). **Three splits. The tree-level sign readout is TERMINAL-UNRESOLVED**: the accumulated record (7/8 here; 8/8 full legs at L = 6; every full-convention E2 leg ever measured reads odd; the even legs are near-zero E_SS crossings and subtraction-side readings) is suggestive and stays on record as MEASUREMENT — but suggestive is not selected, and the discipline exists precisely to refuse that promotion. **The registered continuations, both genuinely new programs: the RPA dressing class (one loop up — the sign at the sigma pole) and the mode-selecting Hamiltonian fallback (note-ml3b's original alternative).** LEDGER #77.

### Round-20 hostile review: PASS both items, zero MAJORs — the split certified numerically unambiguous; corrections applied

**The review** (freeze verified to the second; full dps-60 reproduction byte-identical twice; r18/r19 receipts re-run green; every LOG/LEDGER #77 number exact; the E2-full tally rerun-verified leg by leg). **The two adjudications that matter:** (i) **pin integrity SUPPORTED decisively** — the §1 argument is data-free in the pinned sense, the disclosure complete, and the outcome proves it: an E2-only pin OR an L = 10-only pin would EACH have delivered SELECTED-ODD (the L = 10 row was unanimous); the strictly harder 8-way demand refused and was recorded terminal. (ii) **The near-zero leg is NUMERICALLY UNAMBIGUOUS** — E_SS(E1, L = 8, 1/4) = +1.7690599e-05 sits 10–12 orders above the measured noise floor (independent inversion routes <= 2.4e-19 apart; translation covariance 1.5e-18; D-perturbations at 100x backward error spread 3.7e-17; mass certified 3.8e-31): a genuine physical zero-crossing (E1_SS crosses at g_x ~ 0.279 at L = 8, migrating below 1/4 by L = 10) — the split verdict stands cleanly, no noise clause needed. **Corrections applied:** MINOR-2 one word ("artifacts" -> "readings" — the odd-favorable gloss removed from the MEASUREMENT-only sentence); MINOR-1 on record — Gf3's E2 leg fits c0 ~ 1.90 V-PERSISTENT (the strict-decrease letter cannot distinguish that from dissolution; a revived discriminator needs a quantitative shrink factor; moot here — no SELECTED obtained); MINOR-4 — mw1 re-cites R1's license to the universal two-clock realizer (covers the wb-line's random-victim webs; the corner census was the wrong label), tightens ml4 criterion 3 to the explicit composite, and carries wb2's contemporaneous qualifier inline; MINOR-3 receipt-template note (a unanimous-even kill would also exit 1; the verdict string disambiguates). LEDGER #78.

## Round 20 CLOSED (2026-07-06)

**The tree-level sign-readout arc is TERMINAL** (rounds 15–20: the machine built and certified; three adjudications under hardening pinned protocols; three splits; the last with a review-certified genuine zero-crossing as the deciding leg). The suggestive record (7/8; 8/8 L = 6 full legs; 8/8 E2-full legs ever) stays as measurement, never promoted. **The campaign's live lines going forward:** (1) **ml4** — the GW-grade matter-on-web build (note-mw1's R1 route, review-corrected; a build decision for the user); (2) **the RPA dressing program** (the sigma-pole sign — one loop up; a new design arc); (3) **the mode-selecting Hamiltonian fallback** (note-ml3b's original alternative); (4) the wb-line's next rung and the standing backlog (posit-2 map, k4-under-growth, u4b growth-trend); the SHARD article awaits the user's read.

## 2026-07-06 (round 21) — OPENED on the user's amended plan: PLAN fork recorded; paper 5 initial draft; RPA at max effort

User decisions on record: ml4 + Hamiltonian DEFERRED (funding); token-restricted mode (reviews suspended EXCEPT one reserved for an RPA SELECTED); paper 5 = initial pass, DRAFT-UNREVIEWED status line; RPA = the funded attempt to close the sign. PLAN.md carries the authoritative four-line fork + the standing mode. Sequence: paper 5 draft -> note-ml3b-g (RPA) pinned -> receipt -> run -> records + paper-5 chapter.

### Round-21 receipts and close: paper 5 DRAFTED; the RPA one shot FIRED — FAILS-AT-RPA; the sign question exits the accessible orders

**Paper 5** (`relativistic-isp-v9-paper5-one-sign-machine.md`, 9957eef initial + the RPA chapter this commit): the full ml3b arc consolidated single-threaded — DRAFT-UNREVIEWED status line carried; every number from committed receipts; the review-record aggregate discloses both correction directions.

**ml3b-g (`ml3b_g_rpa.py`, pin 0c1495f strictly pre-receipt, receipt dc1f489 pre-run): Gr1 HELD (wiring exact 0.0; geometric convergence 5.6e-16); Gr2 HELD (RPA valid and stable — min Re eig(I − GΠ) = 0.807–0.839 at all four points); Gr3 REFUSED = FAILS-AT-RPA.** The readings: **E1 even on ALL FOUR dressed legs** (the dressing pushed the SS cell positive: E1_SS = +2.5e-4..+4.3e-4, was negative at tree); E2 odd on three, with E2_SS crossing positive at (L = 10, g_x = 1/4) (+6.07e-5) — **the crossing mechanism RELOCATED under dressing rather than resolving**. The mechanistic registration (dressed E1-vs-E2 spread < tree) held only marginally (1.67 vs 1.72). The subtracted INFO row still splits; the L = 6 continuity row matches the pattern. Per the route's one-shot clause: **the sign question exits the resummed reach; the ml3b sign program is closed at every accessible order** (tree x 3 protocols + RPA). No review fires (not SELECTED — the reserved-review rule). **The honest terminal statement (now §7 of paper 5): the odd lean is NOT order-robust — dressed E1 reads even 4/4 — so the parity is estimator- and order-dependent: this construction, as built, does not determine the bit. The remaining continuations are exactly the two deferred programs (Hamiltonian: the sign as a selection rule, not a correlator cell; ml4: the substrate change replaces the bubble content wholesale).** LEDGER #79.

## Round 21 CLOSED (2026-07-06)

Delivered per the user's amended plan: the PLAN fork + token-restricted mode recorded (b5c30b0); paper 5 initial draft (9957eef) + its RPA chapter and honest terminal ending (this commit); the RPA adjudication run at max effort under full freeze discipline — FAILS-AT-RPA, unreviewed-by-rule, disclosed in-paper. **The campaign state: the sign line is closed at accessible orders pending the deferred builds; the live cheap lines are the wb-line's next rung and the backlog; the SHARD article needs its sign-machine section updated to the terminal state before publication.**

## 2026-07-06 (round 22) — OPENED at user go: paper-5 results pass + THE HAMILTONIAN ROUTE (note-h1)

Paper 5 SS6 gains the RPA rows ((v): the dressed legs invert the lean) and the accessible-orders wording. PLAN line 3 flips DEFERRED -> FUNDED. **The Hamiltonian design (note-h1, the reduction):** gauge invariance on the ml3a manifold forbids all odd-cell cubics and makes the four-cell product PI E_xy the FIRST orbit discriminator; with equal magnitudes forced (ml3a), the two orbits differ ONLY through the quartic term, whose coefficient is minus the fourth joint cumulant of the four global cell charges q_xy = sum_z O_x^A(z) O_y^B(z). Selection by effective-potential minimization then reduces the ENTIRE question to the sign of one derived number: **SELECTED-ODD <=> kappa_box < 0; SELECTED-EVEN <=> kappa_box > 0** (the sign chain derived in-note step by step). kappa_box = the box-x-box (double-single-loop) topology of that cumulant — the leading fully-connected order, computed EXACTLY (all 36 ordering pairs, einsum tensor networks, no windows, no estimators, no distance structure: ONE number per grid point). Protocol: sign unanimity over {L = 6, 8, 10} x {g_x = 1/4, 1/2}, improved content, full propagator (the standing principled choices); wiring gate = brute-force Wick at L = 3 vs the einsum route; one shot per the propagated clause; the reserved review fires on ANY SELECTED.

### Round-22 receipt: h1 RAN — SELECTED-EVEN-H, UNANIMOUS AND STABLE; the reserved review FIRES before grading

**The run** (`h1_mode_hamiltonian.py`, pin 165ca47 + amendment 05a3349 both pre-run/pre-data): Gh1 HELD (the einsum network matches the independent brute-force Wick sum at 6.6e-16); Gh2 HELD (min |kappa| = 1.4e-3 — five orders above the floor); **Gh3: sign(kappa_box) = +1 at ALL SIX grid points** (+2.85e-3 / +4.08e-3 / +1.65e-3 / +1.42e-3 / +2.06e-3 / +1.53e-3; imag ~1e-19; the decoupled g_x = 0 row also positive +7.90e-3; aligned share 5–15% — the twisted orderings dominate). **No crossings anywhere: the Hamiltonian object is the stable observable the correlator route never had — and it selects the EVEN orbit.** By the pinned semantics: **SELECTED-EVEN-H — the locus refuted at the Hamiltonian object**, at the pinned scope [leading connected topology (box x box); the S/P assignment import; the equilibrium (Gamma-minimization) premise; the sign chain of note-h1 SS2].

**Grading is WITHHELD pending the reserved hostile review (fires now, per the PLAN rule: any SELECTED).** The review's named targets, in kill order: (1) THE SIGN CHAIN — three stacked conventions (fermion-loop signs; the Legendre step Gamma(4) = −W(4) INCLUDING the amputation by W2^{-1} legs and possible cell-basis mixing, the note's schematic step; the minimization direction) — any single error flips SELECTED-EVEN into SELECTED-ODD; (2) the W3-exclusion (the note drops W3-mediated Legendre trees at leading topology — are they same-order and sign-capable?); (3) the reduction lemma (gauge invariance, cubic exclusion, ml3a's equal-magnitude law); (4) independent reproduction of kappa_box; (5) records accuracy. The verdict either survives at its scope (the construction selects the wrong branch — the honest kill, with the scoped premises as the named escape routes) or flips/voids at a named link.

### Round-22 reserved review: VOIDED-AT-LINK-2 — the grading is FAILS-AT-H-EXACT; the sign line closes at every accessible object; disposition applied

**The review** (the reserved pass; the deepest of the campaign): kappa_box CONFIRMED (independent implementation rel 1.4e-7/1.0e-8; loop signs/counting re-derived from scratch via exact Berezin integration — coefficient +1, no over/under-counting; the receipt's brute check certified genuinely independent code); the freeze VERIFIED with anti-tuning evidence (registered direction odd, raw result even, exact completion voided both; pre-pin repo greps clean). **But the SELECTED-EVEN reading is VOIDED at note-h1 SS2(ii), the Legendre link, by exact computation of everything the note truncated:** (MAJOR-1) the exact cum4 of the PINNED (uncentered) charges is NEGATIVE grid-wide (−2.8e-2..−4.7e-2 vs kappa_box +1.4e-3..+4.1e-3) — the excluded tadpole families are 12–19x the box and REVERSE it ("leading topology" measured false; the note's own rule on its own object would have said ODD 4/4); (MAJOR-2) W3 is NOT zero (max ~5e-2), the W3-trees are 0.7–2.3x the amputated-W4 piece, and **the exact Gamma(4) SPLITS inside the grid (+5.39 / −7.62 / +2.29 / +6.62)**; (MAJOR-3) the reduction lemma's mathematics verified but its license fails for the actual non-invariant Gamma (orbit-averaging restores it exactly — and then the selector is the exact Gamma(4), which splits); (MAJOR-4) centered vs uncentered charges give OPPOSITE stable simplified verdicts (ODD 4/4 vs EVEN 4/4) — the object was not pinned tightly enough to carry any verdict. No defensible completion yields a stable answer in either direction. (MINOR-1) "C > 0 combinatorial" false as argued (two Grassmann "variances" negative; held by count parity; the minimization algebra itself correct); (MINOR-2) the Gh3 template conflates decisive-EVEN with refusal; (MINOR-3) aligned-share nit 4.6–15.0%. One structural positive: the q <-> E map is verdict-SAFE by a factorization theorem (per-party normalizations contribute a square to the parity) — the residual import is the S/P assignment alone.

**Disposition applied (LEDGER #81):** Gh1/Gh2 stand; kappa_box > 0 stands as a confirmed measured number; the grading is **FAILS-AT-H-EXACT (VOIDED-AT-LINK-2)** — the crossing disease relapses at the first exact completion of the route's own object. **THE SIGN LINE IS CLOSED AT EVERY ACCESSIBLE OBJECT: five adjudications (window family, two one-shots, RPA, Hamiltonian), five closures, one mechanism — every continuous deciding quantity this construction offers crosses zero inside the accessible region. Paper 5's terminal statement takes its final form: the construction as built does not determine the bit; within this class the derivable-vs-free adjudication leans FREE (the bit as reality's input — the round-12 framing), hardening to a finding if ml4's substrate change also refuses.** Paper 5 SS5.5 + the final SS7 written; the review-record discloses the reserved pass in full.

## Round 22 CLOSED (2026-07-06)

Delivered: paper-5 results pass (f1cd23a); the PLAN fork + funded line; note-h1 + receipt + the pre-data amendment + the run (SELECTED-EVEN-H raw) + the reserved review (VOIDED — FAILS-AT-H-EXACT) + the disposition + paper 5's final form. **The funded sign program is complete and honestly terminal. Live lines: ml4 (deferred; now carrying the sign's last accessible test as a passenger), the wb-line next rung, backlog; the SHARD article needs its sign section rewritten to the final terminal form before publication.**
