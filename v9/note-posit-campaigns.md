# The posit-discharge campaigns — proof shapes for (b2) and the two-clock posits

**Status:** design-note (proof shapes only), 2026-07-05 (round 2, background item; PLAN T5). No code, no claims — the two campaigns no round-1 lens proposed, opened so round 3 can attack them.

## 1. (b2) screen stationarity — paper 1's "single most valuable strengthening"

**The posit:** "survival is a function of accumulated content alone" is (b1) + (b2) per paper 1 §5, with (b1) forced — (b2) is the unproven remainder (the screen's contraction does not vary with content-location along the commit order). Its counterexample-if-dropped is measured (gap 0.18, receipt f3e); its discharge removes the (b2) conditionality from the dense-regime law (premises (a), (b1) and the typed imports remain).

**The named route (paper 1 §2), decomposed into three lemmas:** (L1) the screen at each seal is the same q = 2 one-contrast object *up to the count-dual symmetry* — the symmetry that fixes v6 paper 4 §5's constants acts transitively on the admissible screen configurations at fixed κ (a group-orbit statement on the §73–76 ledger arena); (L2) the collar machinery (v6 paper 10 T4) transports the screen along the commit order within the finite-primitive arena — contraction is conjugation-invariant under the transport (the T4 presentability theorem's covariance face); (L3) orbit-transitivity + transport-invariance ⇒ per-step contraction is commit-index-free ⇒ (b2). **Where it can fail:** L1 needs the base marginal homogeneous across seals — exactly what #35 says is NOT pinned (w free) — so the honest target is **conditional (b2)**: stationarity holds *given* base homogeneity, with the residual being w-drift, priced by the f7 precision map (w = 4.570·δp). That reframing makes (b2) falsifiable and connects it to the magnitude ledger rather than leaving it a plausibility clause. Round-3 receipt shape: the orbit-transitivity computation on the §73–76 arena (sympy-exact) + a w-drift sensitivity row.

## 2. The two-clock posits (paper 13 Thm 2.1's two [POSITED] bridges)

**Posit 1 (global commit sequencing).** Sub-posit (a) — a total commit order exists — is DEFINITIONAL at the record grain (b is the evidence-clock sequencing; paper 1's posit-1 reading). Sub-posit (b) — the uniform race — is the independence refinement (paper 16 F1), and per note-q3 it is the swappable layer. **Proof shape for (a)+(b) → the sequencing law:** independence of per-lineage Exp(1) clocks ⇒ uniform committer [one-line, standard superposition]; the genuinely open half is INDEPENDENCE itself, which is where T3.1's relocation freedom bears: the law pins only per-lineage conditionals, leaving the cross-lineage joint free (a shared-randomness relocation stack would be the dependent exhibit — an immediate corollary of leg (iii)'s construction, not yet built), so the honest campaign is to DEMOTE posit 1(b) from "assumed" to "one point of the legal class, selected by measurement" — the u3/u3b visibility results are exactly that measurement program. Deliverable: a scope note in the next consolidation, not a theorem.

**Posit 2 (cross-chain χ-comparability).** The candidate discharge: χ-comparability as *derived* from the record web's own connectivity (a cross-link commits a relative ordering of the two chains' odometer readings — the pinch datum of paper 2 §5), making posit 2 a THEOREM on webs whose cross-links are dense enough (a percolation threshold statement: comparability is decided web-wide iff the chain graph is connected, which the fleet-renewal law guarantees at L·M/N small). Proof shape: (i) a cross-link (x ∈ A) ≺ (y ∈ B) orders χ_A(x) relative to χ_B(y) up to the [PHYSICAL] per-chain offsets; (ii) connectivity of the chain graph propagates the ordering to all pairs — with cycles imposing consistency constraints whose failure probability is the measurable obstruction; (iii) the residual is the offsets' gauge — exactly one free constant per connected component (the same "one scale per component" shape as paper 2's hop-metric result). **Where it can fail:** cycle-consistency (ii) — the receipt shape is a cycle-violation census on churn webs. If violations are generic, posit 2 stays a model-class definition (the paper-12 flat-diamond analogy); if they vanish at the corner, posit 2 is DISCHARGED on the physical class. That is a real, cheap, decisive receipt for round 3.

## Sequencing

Round 3: the posit-2 cycle census first (cheap, decisive), then (b2)-conditional's orbit computation; posit 1(b)'s demotion note rides the next consolidation paper. None of this blocks the Tier-1/2/3 lines.
