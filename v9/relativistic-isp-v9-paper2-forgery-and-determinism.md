# The forgery at scale, its mechanism, and the death of the determinized stack

**Author:** Felix Robles Elvira (ORCID: 0009-0009-2017-4394; independent researcher)

**Status:** preprint, not peer reviewed, version 2026-07-05. Second paper of the v9 line — **new research** (rounds 3–5 of the campaign: the scale run, the G2-strict test, the mechanism arc through the cure bench, and Tier-7's first two rungs; builds on v9 paper 1 and v8 papers 11/16/17). Tags as in v8 paper 1 + [directional] per the campaign convention. Receipts (`v9/code/`, re-run this date with gate ledgers as pinned; counts post the #45 de-counting): `s2` (5/5), `u5` (3/3), `s3` (2/2), `s4` (exits 1 by design — the refutation is the result), `ml1` (5/5), `ml2` (5/5), `t52` (3/3), `n3_lite` (4/4). **Round-5 revision: the §1–6 body is written with the #45 demotions and the round-5 results (the s4 cure refutation; the s3/ml2 re-specs) integrated; receipts s3 (2/2), ml2 (5/5), s4 (gate ledger P1/P2 REFUSED, P3 HELD — the refutation is the result) re-run green this date.** Precision: float64 landscapes; exact references at `dps = 60`; seeds `20260707/20260708/20260709` (+ ml2's looped `20260710/11`), disclosed. Gate provenance per receipt docstrings; every refuted pinned gate is printed in an explicit gate ledger (the standing convention); the campaign log (`v9/LOG.md`, rounds 3–5 + supersessions) is the correction record, including one cover-grade claim retracted by hostile-review execution (§4).

## Abstract

Paper 1 left one named rung: does the derived action's growth hold at scale, and are its endpoints spacetime-grade? **(1) The forgery, exposed and measured [`s2`].** At `N = 512–1024` the A1 endpoints hold the sprinkling-band global structure (`r = 0.458–0.478`, separated from the β = 0 kernel control's dense-ward drift, `0.46` vs `0.65`) — but the order-only statistical layers catch them: **chain-heavy interiors** (heredity axis `r_I = 0.69–0.88` on 9/10 endpoints; sampled abundance misfit `20×` the faithful baseline — against a kernel whose own random picks already default to `r_I ≈ 0.73–0.81`). The endpoints are `r ≈ ½` **forgeries** — precisely the class paper 11's cage logic predicts each new axis must expose; the instruments built for that job did it. A2 (the abundance-augmented arm, increments truncated at `k ≤ 64`, disclosed) brackets the family's tension from the other side and scale-dependently: balanced interiors at `N = 256` (`r_I ≈ 0.55`, misfit `3.6×`) but **interior collapse at `N = 512`** (`r_I = 0.35/0.20`) with global `r` undershooting throughout — the truncation is the named candidate cause. `dim ≤ 2` is UNDECIDED at these sizes (the capped oracle; the poly-time realizer is the named unblock), so no cell verdict is claimed in either direction. **(2) The mechanism arc [`s3`, `s4`]: myopia supported, its cure refuted — the forgery is kernel-class-level.** Instrumenting every late arrival of the argmin growth: diffuse candidates (interior `r_I < 0.65`) are on the menu at `55%` of late arrivals — the closure does *not* starve the menu — and where they are offered and skipped, the **registered per-pair clause** holds at `71.2%` (the picked candidate is per-pair dearer with fewer new pairs — unconditional means `|D|` `73` vs `107`, conditional `58` vs `96`). That *supported* myopia at the `0.65` cut (threshold-decided: the dichotomy flips at `0.55`, disclosed); it did not decide it — consistent with the #43/#45 demotions. The named cure — **charge-rate selection** (`Φ = ΔS/max(|D|,1)`, corpus-legal in note 8's class C4) — was then benched with pre-pinned gates and **REFUSED** (`s4`): it centers global `r` at `0.500–0.501` yet leaves the interiors forged (`m_ab` at `0.92×` argmin's vs the pinned `0.5×`). The refutation carries a named positive: **the division of labor [DEMONSTRATED, this family] — selection rules control the global comparability layer (to exact centering) while the interior layer is selection-invariant at the kernel's floor: global structure is chosen, fine structure is inherited from the move class.** With no selection rule moving interiors below that floor (`r_I ≈ 0.73–0.81`), **the surviving hypothesis is that the forgery is kernel-class-level** — the interiors are committed early and irreversible growth cannot rewrite them; the faithful-guided control (growth restricted to a sprinkling's own down-sets) is the named round-6 decider between kernel enrichment and class inexpressibility. **(3) The determinized stack is statistically dead [`u5`] — G2-strict's first confirmation instance.** The fully determinized stack (periodic churn + 3-atom cycling + kill-the-oldest + round-robin; zero randomness anywhere) produces a web whose dispersion layer refuses at `z = +771` and whose volume layer reads `3.03×` band, with its gap marginal above the finite-alphabet floor (`KS = 0.44`, support 3, floor `1/6`) and the two-time gate flagging the cycling — scoped, per the irreducibility note's battery boundary, to the named stack: **irreducible click randomness is necessary for statistical spacetime, as measured on the corpus's own candidate determinization.** **(4) The matter-locus toy, rungs 1–2 — non-washout established, exports corrected in review [`ml1`, `ml2`].** A Gaussian field mediated on the record order, coupled at seals, yields a derived two-party quantity (the exact cross-chain coupled-content covariance) that closes transverse geometry on synthetic fleets at benchmark parity (Procrustes `0.996` vs `0.992`, cost `−0.003` seed-level) — and, at rung 2, **holds a record-native web's own two-clock geometry through the field functor with no planted structure anywhere** — non-washout, `[directional, multi-seed]` (Procrustes `0.891` at `Mc = 88`; in-receipt looped-seed spread `0.743–0.919`, the round-4 review's independent triple `0.733–0.919`; shuffle-void `z = 18.6`; graph-only baseline `0.704` on this web; the size-residual void clean; relative amplitude `6.4%`). The first draft's export ("the coupling must be local") was **retracted by hostile-review execution** — the full-causal mediation also closes (`0.999`) once the estimator is exact; the surviving, receipt-measured constraint is **amplitude**: locality buys `~7×` relative signal, i.e. per-realization estimability — a physical record is one realization. **(5) Two flanking results.** The posit-2 cycle census (`t52`, with its review-added positive control: manufactured-consistent `χ` reads 6/6-vs-0/6) finds independent chain-local content generically **deep in the infeasible phase** on placement-webs — the supported direction: cross-chain `χ`-comparability wants content-placement *coupling*, the matter locus's own job. And L3-lite (`n3_lite`, under the amended gate) runs the record-native pipeline end-to-end with both voids clean (closure `0.963`; shuffle `z = 14.4`; supply-confound null), retiring the `l3`-style spectral-drop gate for kernel-affinity MDS by attribution (clean = noisy = `0.52`; the ratio reads kernel curvature — it would pass at `ℓ ≳ 1.2` — conflating kernel and dimension at unknown `ℓ`). **Honest scope:** the forgery verdict is order-only-layer-based (no realizer at these sizes); the mechanism verdict is one kernel/action/scale configuration; the G2-strict instance is stack-scoped; rung 2 is 2D (the web's own geometry — the 3+1 form is n3-full's); every 2–8-rep number is [directional]. The successor slate as of this paper: the faithful-guided control (the kernel-fork decider named in §2/§6), the interior-multiplicity derivation, n3-full, the posit-2 regime map.

## 1. The forgery at scale [`s2`]

The scale run held paper 1's global structure and broke its innocence. A1's argmin endpoints keep `r = 0.458–0.478` at `N = 512/1024` (P1, held) while the β = 0 kernel control drifts dense-ward (`0.603–0.702` across `s2`+`s4`'s streams; the pinned `r ≥ 0.7` class-crossing gate refused — the attribution's carrier is the clean separation, `0.46` vs `0.65`). The order-only statistical layers then expose the endpoints: the heredity axis reads `r_I = 0.69–0.88` on 9/10 endpoints (2–13σ above the faithful ½-band) and the sampled abundance misfit sits at `20×` the faithful baseline. **The endpoints are `r ≈ ½` forgeries** — the class paper 11's cage logic exists to catch, caught by the axes built for it. Two facts scope the finding. First — the round-4 review's baseline, carried everywhere the forgery is narrated — **the kernel's own random picks default to chain-heavy interiors** (`r_I = 0.73–0.81`, `m_ab ≈ 10–16×`, pooled over `s2`+`s4`'s control arms): the argmin *worsens* a kernel default rather than inventing the structure, and the A1-specific signature is balanced-global-`r`-*with*-forged-interiors. Second, A2 (the abundance arm, increments truncated at `k ≤ 64`, disclosed) brackets the family scale-dependently: balanced interiors at `N = 256` (median `r_I ≈ 0.55`, misfit `3.6×`) but interior *collapse* at `N = 512` (`r_I = 0.35/0.20`) with global `r` undershooting throughout — the truncation is the named candidate cause. `dim ≤ 2` is UNDECIDED at these sizes (capped oracle; the poly-time realizer is the named unblock): no cell verdict is claimed in either direction.

## 2. The mechanism hunt: from myopia to the kernel class [`s3`, `s4`]

Two hypotheses were pinned for *why* the argmin forges: menu starvation (the percolation closure through a chainy web offers no diffuse candidates) and greedy myopia (diffuse candidates offered, marginally dearer). The instrumentation (576 late arrivals): diffuse candidates are available at `55.2%` of arrivals at the pinned `0.65` cut — starvation refused *at that cut* (the dichotomy is threshold-decided: at `0.55` it flips; the sensitivity row is printed in-receipt) — and the **registered** per-pair clause (the picked candidate's per-pair charge exceeds the best diffuse option's) holds at `71.2%` (the first draft's `79.6%` rode a conjunct vacuous under argmin; corrected per review). That supported — did not decide — myopia. **The cure bench then refused the cure** (`s4`, all gates pinned pre-run): charge-rate selection `Φ = ΔS/max(|D|, 1)` locks the global comparability at `r = 0.500–0.501` (a bonus: the rate rule centers Row C's target exactly) yet the interiors stay forged (`r_I = 0.757–0.820`; `m_ab` at `0.92×` argmin's against the pinned `0.5×`) — P1/P2 REFUSED, P3 held, verdict **CURE-REFUTED**. Assembling the three measurements — kernel default `0.73–0.81`, argmin `0.83–0.91`, rate `0.76–0.82` — *no tested selection rule moves interiors below the kernel's own floor*.

**The division of labor [DEMONSTRATED; one kernel/action family, three selection rules, `n = 512`].** Stated once as this campaign's measured regularity, since its two halves came from opposite verdicts: **the global comparability layer is selection-controllable** — the rate rule centers `r` on Row C's target to the third digit (`0.500–0.501`, every rep) — **while the interior/fine-structure layer is selection-invariant**: all three rules sit at-or-above the kernel's random-pick floor (pooled `r_I = 0.73–0.91`), never below it. Global structure is *chosen*; fine structure is *inherited from the move class*. This is the growth-mode echo of the corpus's standing division (paper 4: order owns the conformal class, count buys the volume): the selection rule owns the pair-count layer, the move class owns the interval texture — and it is why the cure space collapses to the kernel fork rather than to further action design. The surviving hypothesis is **kernel-class-level**: candidate down-sets are transitive closures through the growing web, the large intervals' interiors are committed early, and irreversible growth cannot rewrite them; selection chooses among carriers of the same forgery. The decisive round-6 diagnostic is named: the *faithful-guided control* — growth restricted to candidates drawn from an actual sprinkling's own down-sets. If even that stays forged, this move class is expressively closed against faithfulness and the placement program needs a new class; if it un-forges, kernel enrichment is the road. Either way the mechanism resolves at the class level.

## 3. The determinized stack is statistically dead [`u5`]

G2-strict's first real test: the fully determinized stack (periodic churn, 3-atom cycling, kill-the-oldest, round-robin — zero randomness anywhere) yields a web whose dispersion layer refuses at `z = +771` and whose volume layer reads `3.03×`, with the gap marginal above the finite-alphabet floor (`KS = 0.44`; support 3; floor `1/6`) and the serial-rank half of the two-time gate flagging the cycling (the χ²-half omitted, disclosed). Scoped per the irreducibility note's battery boundary — this decides the named corpus stack, not the zero-randomness class — the instance stands: **irreducible click randomness is necessary for statistical spacetime on the corpus's own candidate determinization.** A pass would have refuted G2-strict; both directions were live.

## 4. The matter-locus toy, rungs 1–2 — with its exports corrected in review [`ml1`, `ml2`]

Rung 1: a Gaussian field mediated on the record order and coupled at seals yields a derived two-party object — the exact cross-chain coupled-content covariance — that closes transverse geometry on synthetic fleets at benchmark parity (`0.996` vs `0.992`, cost `−0.003`, seed-level). The first draft exported "the coupling must be local"; **hostile-review execution retracted it** — the 2×2 cell the iteration ledger skipped (full-causal graph + exact estimator) closes at `0.999` — and the receipt now carries that cell as an executed arm. The surviving, receipt-measured constraint is **amplitude**: Hasse mediation carries `~7×` the relative signal (`1.0%` vs `0.14%`), i.e. locality buys *per-realization estimability* — a physical record is one realization. Rung 2 (lite): the same derived object on a record-native churn web, with no planted structure, holds the web's own two-clock geometry through the field functor (`0.891` at `Mc = 88`; shuffle-void `z = 18.6`; the review-added arms: graph-only baseline `0.704` on this web — the field's margin runs `+0.01` to `+0.19` across seeds and graph constructions, always non-negative — and the size-residual void clean, `0.048` vs `0.890`). Scoped per the review: probe and target derive from the same committed `(b, χ)` — the claim is **non-washout** of record-intrinsic geometry, `[directional, multi-seed]` (the `0.85` pin refuses on part of the spread — 1/2 in-receipt looped seeds, 1/3 of the review's triple), not emergence; the emergence-grade form is `n3`-full's, with a derived field and the certificate stack.

## 5. Flanking results [`t52`, `n3_lite`]

The posit-2 cycle census, with its review-added positive control (manufactured-consistent `χ`: 6/6 feasible, shuffle 0/6 — full dynamic range), finds independent chain-local content generically **deep in the infeasible phase** on placement-webs: the supported direction is that cross-chain `χ`-comparability wants content-placement *coupling* — the matter locus's own job, reached from an independent axis. And L3-lite, under the amended gate (option (a), logged), runs the record-native pipeline end to end with both voids clean (closure `0.963`; shuffle `z = 14.4`; supply-confound null) and retires the `l3`-style spectral-drop gate for kernel-affinity MDS by attribution (clean = noisy = `0.52`; the ratio is curvature-dependent — review-measured `0.85 → 0.005` over `ℓ = 0.15 → 50` — so it conflates kernel and dimension at unknown `ℓ`).

## 6. Honest scope, and the fork this paper leaves open

Everything is 2D; the forgery verdicts are order-only-layer-based (no realizer at these sizes); the mechanism arc is one kernel/action family, with `s3` at `n = 256` (below the forgery's scale — its verdicts are the cure bench's context, not its ground); the G2-strict instance is stack-scoped; rung 2 is `[directional, 3 seeds]`; every 2–8-rep number is `[directional]`. What this paper establishes: the derived action's growth is *globally* right and *locally* forged; **the division of labor** (§2 — global structure selection-controllable, fine structure move-class-inherited) is the positive law-structure fact the refutations bought; the forgery is not the selection rule's (three rules bracket the kernel's floor); and the program's sharpest open fork is now expressibility — **can this move class produce faithful interiors at all?** The faithful-guided control decides it, and either answer redirects the placement program: enrichment of the kernel, or a new move class (with the interior-multiplicity derivation as the action-side companion either way).


## Receipts

`v9/code/`, re-run this date at the honest gated counts (verdict-recording lines print-not-count per #45): `s2_scale_run.py` (5/5 + gate ledger: P1 held, P2/P3 refuted-as-pinned), `s3_forgery_mechanism.py` (2/2 — the re-specced registered clause at `71.2%`; sensitivity row printed), `s4_cure_bench.py` (**exits 1 by design** — gate ledger P1/P2 REFUSED, P3 HELD: the refutation is the result), `u5_determinized_stack.py` (3/3), `ml1_seal_field_toy.py` (5/5 — incl. the review's 2×2 cell), `ml2_derived_field_web.py` (5/5 — incl. the G4 graph-only baseline and G5 size-residual void; 3 seeds looped), `t52_posit2_census.py` (3/3 + the positive control), `n3_lite_record_native.py` (4/4). Conventions: q2's powered semantics where bands apply; gate ledgers printed wherever a pinned gate refused.

## References

**This series.** v9 paper 1 (the sandwich; the named rung this paper runs); v9 notes s2/s4 (the cure bench's registration)/t31/n3-gate/posit-campaigns + PLAN Tier 7; v8 papers 11 (the cage logic; the heredity axis; §3.1's per-related-pair granularity — the interior-multiplicity proposal is NEW, flagged), 16, 17; `v9/LOG.md`; `v8/LEDGER.md` #42–#46 (this paper's own correction record). **External.** Rideout–Sorkin (2000); Weyl (1916); the standard GFF/graph-Laplacian toolkit.
