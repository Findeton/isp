# note-stem-spectrum — the stem spectrum: Stone/Priestley duality for sequential-growth observables

**Status:** design note, 2026-07-10 (v9 round 38; extraction-bound per the user's plan). Receipt: `v9/code/stem_spectrum_receipts.py` (pinned here, committed strictly before). Paper: `v9/relativistic-isp-v9-paper7-stem-spectrum.md` (renamed from `stem-spectrum-paper.md` at the paper-numbering correction, 2026-07-10) (standalone-framed). Reviews ON (full hostile pass in v9; extraction-scoped pass after the move).

## The objects (definitions fixed here, used verbatim in the paper)

- Omega-tilde(n) = naturally-labeled n-element causal sets (each element added above a down-set of its predecessors); end-deletion maps Omega-tilde(n+1) -> Omega-tilde(n); **Omega-tilde = lim<- Omega-tilde(n)** = the labeled history space, compact metrizable totally disconnected with clopen basis the cylinders — a light profinite set.
- A **stem** of a causet: a finite down-set, taken up to isomorphism (BDGHS). stem(b) = the event "b embeds as a stem". Countably many stems => the Boolean algebra **B** generated by {stem(b)} is countable; the distributive lattice **L** (no complements) likewise.
- **X = Stone(B)** (the stem spectrum); **X_le = Priestley(L)** (the ordered version). phi: Omega-tilde -> X by evaluation (a history answers every stem-question); phi factors through relabeling (stems are iso-invariant).

## The theorems (pinned statements; proofs in the paper)

- **T1:** Omega-tilde is a light profinite set whose clopen algebra is the cylinder algebra and whose Borel sigma-algebra is BDGHS's R-tilde. (Essentially definitional; unclaimed in any literature — the identification is the point.)
- **T2:** X is a light profinite set (B countable); phi is Borel (stem sets are countable unions of cylinders — BDGHS); phi^{-1}(Clopen(X)) = B; the sigma-algebra generated by stem sets equals phi^{-1}(Borel X).
- **T3 (rogues = non-separation):** phi(c) = phi(c') iff c, c' realize the same stems; the non-trivial fibers are exactly the rogue classes of BDGHS; for every CSG dynamics mu, the union of non-trivial fibers is mu-null (BDGHS Prop 1). The separated object is X (already Hausdorff): the defect lives in phi's injectivity, and it is named, localized, and null.
- **T4 (the measure-algebra isomorphism):** for every CSG dynamics mu, phi induces an isomorphism of measure algebras (R mod mu-null) ~ (Borel(X) mod phi*mu-null). [BDGHS Thm 1 + Cor 1, restructured; the new content is the object X and the two corollaries below.]
- **N3 (automatic extension on the spectrum):** every finitely-additive probability (more generally, bounded charge) on B extends uniquely to a Radon measure on X — on a compact zero-dimensional space, countable additivity on clopens is automatic (a countable disjoint clopen cover of a compact clopen is finite). Consequently: classical states on the stem algebra ARE Radon measures on the spectrum; the Dowker–Johnston–Surya quantum non-extension is RELOCATED (not cured): a decoherence functional is a bi-charge on B x B, automatically countably additive on clopens, and its sigma-extension question becomes classical vector-measure extension theory (Kluvanek-class) on X. Care pinned: no claim that the obstruction vanishes.
- **N4 (the ordered version):** L's Priestley dual X_le is a profinite poset (Speed 1972); at each finite truncation the Birkhoff dual of the size-<= k stem lattice is the canonical finite quotient; the covtree contact stated ONLY as computed in r5.

## Receipt gates (mechanical; exit 1 on failure)

- **r1:** the tower to n = 5: enumeration by down-set growth; end-deletion surjectivity verified; |Omega-tilde(n)| printed (cross-checked against independent recount by direct filtering of all labeled posets for natural-labeling).
- **r2:** the separation table: for n = 4, 5, 6 (unlabeled causets), the number of pairs sharing all stems of size <= k, for k < n — the finite shadow of the rogue phenomenon, measured (expected: nonzero counts at small k, shrinking in k; any pair at k = n − 1 is a finite proto-rogue worth printing).
- **r3:** Birkhoff mechanized: for the lattice generated by size-<= k stem-questions over Omega-tilde(n) (k = 2, 3; n = 4, 5): compute atoms, join-irreducibles, and verify the double-dual identity (poset of join-irreducibles' down-set lattice = the lattice) — the finite-level duality receipt.
- **r5:** covtree contact: compute covtree nodes at levels 2, 3 (sets of level-size down-sets realized in a common future, per Surya–Zalel's definition as implementable at small n) and the B_k atoms; print the correspondence/mismatch verbatim — the paper claims exactly what this prints.

## The extraction plan (stage 6-7)

Standalone repo (user-named "shard"; walsh-delta pattern): the paper + receipts move; v9 keeps this note + a pointer + the record-web instantiation remarks. The extraction-scoped review checks ONLY: standalone coherence (no un-defined corpus jargon), independence (no result depends on unextracted corpus material), and fit. Reviews in v9 come first; the paper leaves home hardened.

## References

BDGHS (gr-qc/0210061: stems, R, Thm 1, Cor 1, rogues, Prop 1); Dowker–Johnston–Surya (1007.2725); Sorkin (1104.0997); Surya–Zalel covtree (2003.11311; Zalel 2008.02607; Gutzeit et al. 2026); Speed 1972 (profinite posets = Priestley spaces); Priestley 1970; Birkhoff; Clausen–Scholze light condensed (the language of T1/T2, one paragraph, machinery deferred); Martin–Panangaden CMP 2006 (adjacent, related-work); the duality map (`v9/note-duality-map.md`, the provenance).

## Round-38 review-pass-1 amendment (appended, not silently edited; the deviations note owed under NIT-3)

**The deviation, recorded:** the paper as drafted (9e9a879) weakened this note's T3 import silently — the note's "the union of non-trivial fibers is mu-null (BDGHS Prop 1)" was correct in substance, but BOTH note and paper over-scoped the dynamics class ("every CSG dynamics"), and the paper additionally hedged fiber measurability against its own source. The review (LOG round-38 pass 1) settled both directions against the primary text.

**Amended pinned statements (supersede the corresponding lines above; the paper's next revision carries these):**

- **Scope (all theorems importing BDGHS):** the covered dynamics class is BDGHS's own — generalized percolation (t_k > 0 for some k >= 2) plus the two deterministic exceptional models; the remaining non-generic CSG models are BDGHS's §7 conjecture. T4's gloss "(a probability measure on (Omega-tilde, R-tilde))" is DELETED — under it T4 is false (half-half point mass on a rogue pair).
- **T3' (the exact fiber theorem — the upgrade replacing the hedge):** BDGHS Lemmas 1+2 give Theta = Gamma exactly (Gamma = {some level contains infinitely many non-maximal elements}); the union of non-trivial fibers of phi is the labeled copy of Gamma — Borel, mu-null for the covered class, and measure-ONE in the excluded t0,t1-only model; over non-rogues, fibers are single relabeling classes. mu(Theta) = 0 additionally in the t0-only model (the infinite antichain is stem-determined).
- **T5'/N3 (the extension corollary, rescoped):** the scalar statement stands (bounded charge on B extends uniquely to Radon on X; for scalar charges bounded = bounded variation). The quantum relocation is restated on the strong-additivity axis: the exact extension condition for vector measures is strong additivity (Caratheodory–Hahn–Kluvanek), already applied to the cylinder algebra by Surya–Zalel Thm 2.1 (extension iff BV, complex case); "equivalently, by Theorem 4's translation" is withdrawn (B and the cylinder algebra are incomparable). The honest framing: by T1 the cylinder algebra was already clopen in Omega-tilde, so vacuous sigma-additivity was never the issue there; what the spectrum adds is that the COVARIANT algebra becomes clopen.
- **N4' (the tower correspondence — the opening-2 upgrade, to be proved in the paper):** (a) the extension lemma: any stem of size j < k of a causet with >= k elements extends to a k-stem (minimal element of the complement has past inside the stem; induct); hence the exact-k stem signature always determines the full <= k signature. (b) THEOREM CANDIDATE: for every n, covtree level-n nodes (Dowker–Imambaccus–Owens–Sorkin–Zalel, arXiv:1910.07292 — the corrected attribution) are in canonical bijection with the non-empty atoms of B_n on Omega-tilde: every infinite history's exact-n set is realized at a finite stage (finitely many n-stems, each realized finitely; stems of stems are stems), and every finite certificate extends to an infinite history WITHOUT new small stems (add each new element above everything). The edge/refinement intertwining is NOT claimed beyond what r5b computes.

## Receipt gates, part B (`v9/code/stem_spectrum_receipts_b.py`; pinned here, committed strictly before running)

- **rB1 (the 22):** enumerate all labeled 7-causets by down-set growth (count must equal 96428 and must equal the independent recount); the census of exact-size-3 stem signatures at n <= 7 must yield 22 classes, of which exactly one is new at n = 7, and that one must be the full set {3-chain, 3-antichain, L, V, Lambda}; an explicit 7-element witness is printed and re-verified element-by-element.
- **rB2 (the extension lemma, mechanized):** at n <= 7, for every causet and every j < 3, every j-stem extends to a 3-stem, and the exact-3 signature determines the <= 3 signature (the lemma's finite shadow; failure = the lemma is wrong, exit 1).
- **rB3 (the reviewer's machine, adopted-and-audited):** the review's interface state machine (copied verbatim into the receipt, credited) rerun: machine-reachable exact-3 sets at n <= 12 must equal the n = 7 brute-force census exactly (22; no extras, none missing) — the completeness certificate to n = 12; final completeness to all n rests on the cited witness bound (Gutzeit–Shaban–Yeats–Zalel), review-verified against the fetched source.
- **rB4 (tower correspondence, finite shadow):** at n = 6 and n = 7: the count of distinct <= 3 signatures equals the count of exact-3 signatures (per rB2's determination), and each class exhibits a certificate; printed verbatim — the paper's N4' claims at computed sizes are exactly these prints.

## Round-38 review-pass-2 amendment (appended)

- **Scopes split (supersedes the pass-1 "covered class"):** **P** = generalized percolation (t_k > 0, some k >= 2): BDGHS Prop 1 (rogue nullity) AND Cor 1 both hold, Cor 1 in the strong one-B-for-all-of-P form (A delta B inside Theta). **P+** = P plus the two degenerate deterministic families (unlabeled history a.s. a single causet; t0-only and t0,t1-only): Cor 1 holds per-model (trivially — every covariant event is mu-equivalent to empty or everything), Prop 1 FAILS at t0,t1 (mu(Theta) = 1). T3-nullity is a P statement; T4 is a P+ statement. The seam observation (citable, honestly attributed as reading BDGHS against themselves): their §7 "whence the rogues would be of measure zero" clause fails for the deterministic disjunct even as generation survives — nullity and generation come apart.
- **T6 upgraded UNCONDITIONAL:** DIOSZ's covtree order is DEFINED by O_-(Gamma_n) = {B : exists A in Gamma_n with B an (n-1)-stem of A} = exactly the refinement map — the edge-match hedge is deleted; X = inverse limit of covtree along its own order = the space of infinite covtree paths. **Corollary: phi is SURJECTIVE** (every covtree path has a certificate — DIOSZ/Zalel; conventions match: completed causets = countable past-finite orders, type-omega natural labelings). Zalel fn. 4 (cert-events generate sigma(stem events)) cited as the published shadow of T6.
- **Priority correction (the receipts' role recast as CONFIRMATION):** the level-3 census (22 nodes, 9 non-nodes, certificates incl. the 7-element all-five witnesses) is PUBLISHED in DIOSZ Fig. 10 + App. B.1 + Figs. 7-8. rB1/rB3 mechanically confirm it (and the machine independently re-certifies completeness to n = 12); they do not establish it. No gate changes; no new receipts.
- Openings landed in revision 2: phi-surjectivity corollary; covtree-dynamics-as-Radon-measures paragraph; the seam remark. Named for later: the LEVEL-4 census (absent from the literature; the rB3 machine design extends; Gutzeit bound caps the search) — recorded in the paper's outlook.
